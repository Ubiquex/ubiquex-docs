---
title: "azure_hybridcompute_openapi_extension_publisher"
description: "`azure_hybridcompute_openapi_extension_publisher` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_hybridcompute_openapi_extension_publisher` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hybridcompute_openapi_extension_publisher"})

    		ubx.Data(hybridcompute.OpenapiExtensionPublisher, "example", hybridcompute.OpenapiExtensionPublisherConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiExtensionPublisher } from "@ubx/sdk-azure/azure/data/hybridcompute/openapi_extension_publisher";

    export default stack("example", () => {
      intent({
        summary: "look up azure_hybridcompute_openapi_extension_publisher",
      });

      data(OpenapiExtensionPublisher, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.hybridcompute import OpenapiExtensionPublisher, OpenapiExtensionPublisherConfig

    def describe():
        ubx.intent("look up azure_hybridcompute_openapi_extension_publisher")

        ubx.data(OpenapiExtensionPublisher, "example", OpenapiExtensionPublisherConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ExtensionPublisher items on this page Returned by the lookup.
</ResponseField>
