---
title: "azure_hybridcompute_openapi_connection_detail"
description: "`azure_hybridcompute_openapi_connection_detail` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_hybridcompute_openapi_connection_detail` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hybridcompute_openapi_connection_detail"})

    		ubx.Data(hybridcompute.OpenapiConnectionDetail, "example", hybridcompute.OpenapiConnectionDetailConfig{
    			PrivateLinkScopeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiConnectionDetail } from "@ubx/sdk-azure/azure/data/hybridcompute/openapi_connection_detail";

    export default stack("example", () => {
      intent({ summary: "look up azure_hybridcompute_openapi_connection_detail" });

      data(OpenapiConnectionDetail, "example", {
        privateLinkScopeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.hybridcompute import OpenapiConnectionDetail, OpenapiConnectionDetailConfig

    def describe():
        ubx.intent("look up azure_hybridcompute_openapi_connection_detail")

        ubx.data(OpenapiConnectionDetail, "example", OpenapiConnectionDetailConfig(
            private_link_scope_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="private_link_scope_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connection_details" type="list(object)">
  List of Private Endpoint Connection details. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Azure resource Id Returned by the lookup.
</ResponseField>

<ResponseField name="public_network_access" type="string">
  Indicates whether machines associated with the private link scope can also use public Azure Arc service endpoints. Returned by the lookup.
</ResponseField>
