---
title: "azure_hybridcompute_openapi_gateway"
description: "`data_azure_hybridcompute_openapi_gateway` looks up a real, already-existing `azure_hybridcompute_openapi_gateway`."
---

It is an Azure Arc gateway, a regional proxy endpoint Arc-enabled servers can route their own outbound Azure connectivity through, reducing the set of external endpoints each server needs direct network access to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hybridcompute_openapi_gateway"})

    		ubx.Data(hybridcompute.OpenapiGateway, "example", hybridcompute.OpenapiGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiGateway } from "./local-sdk/azure/sdk/typescript/azure/data/hybridcompute/openapi_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_hybridcompute_openapi_gateway" });

      data(OpenapiGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.hybridcompute import OpenapiGateway, OpenapiGatewayConfig

    def describe():
        ubx.intent("look up azure_hybridcompute_openapi_gateway")

        ubx.data(OpenapiGateway, "example", OpenapiGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Gateway items on this page Returned by the lookup.
</ResponseField>
