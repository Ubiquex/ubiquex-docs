---
title: "azure_hybridcompute_openapi_extension_value"
description: "`azure_hybridcompute_openapi_extension_value` reads a real, available version of an extension type for Azure Arc-enabled servers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_hybridcompute_openapi_extension_value"})

    		ubx.Data(hybridcompute.OpenapiExtensionValue, "example", hybridcompute.OpenapiExtensionValueConfig{
    			ExtensionType: "example",
    			Location:      "example",
    			Publisher:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiExtensionValue } from "./local-sdk/azure/sdk/typescript/azure/data/hybridcompute/openapi_extension_value.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_hybridcompute_openapi_extension_value" });

      data(OpenapiExtensionValue, "example", {
        extensionType: "example",
        location: "example",
        publisher: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.hybridcompute import OpenapiExtensionValue, OpenapiExtensionValueConfig

    def describe():
        ubx.intent("look up azure_hybridcompute_openapi_extension_value")

        ubx.data(OpenapiExtensionValue, "example", OpenapiExtensionValueConfig(
            extension_type="example",
            location="example",
            publisher="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="extension_type" type="string" required>
  The real type of extension this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="location" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="publisher" type="string" required>
  The real publisher of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of extension metadata Returned by the lookup.
</ResponseField>
