---
title: "azure_postgresql_openapi_backup_automatic_and_on_demand"
description: "`azure_postgresql_openapi_backup_automatic_and_on_demand` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_postgresql_openapi_backup_automatic_and_on_demand` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	postgresql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/postgresql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_postgresql_openapi_backup_automatic_and_on_demand"})

    		ubx.Data(postgresql.OpenapiBackupAutomaticAndOnDemand, "example", postgresql.OpenapiBackupAutomaticAndOnDemandConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBackupAutomaticAndOnDemand } from "@ubx/sdk-azure/azure/data/postgresql/openapi_backup_automatic_and_on_demand";

    export default stack("example", () => {
      intent({
        summary: "look up azure_postgresql_openapi_backup_automatic_and_on_demand",
      });

      data(OpenapiBackupAutomaticAndOnDemand, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.postgresql import OpenapiBackupAutomaticAndOnDemand, OpenapiBackupAutomaticAndOnDemandConfig

    def describe():
        ubx.intent("look up azure_postgresql_openapi_backup_automatic_and_on_demand")

        ubx.data(OpenapiBackupAutomaticAndOnDemand, "example", OpenapiBackupAutomaticAndOnDemandConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BackupAutomaticAndOnDemand items on this page Returned by the lookup.
</ResponseField>
