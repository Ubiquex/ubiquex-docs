---
title: "azure_postgresql_openapi_private_endpoint_connection"
description: "`azure_postgresql_openapi_private_endpoint_connection` reads a real Private Endpoint connection attached to an Azure Database for PostgreSQL flexible..."
---

`azure_postgresql_openapi_private_endpoint_connection` reads a real Private Endpoint connection attached to an Azure Database for PostgreSQL flexible server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	postgresql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/postgresql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_postgresql_openapi_private_endpoint_connection"})

    		ubx.Data(postgresql.OpenapiPrivateEndpointConnection, "example", postgresql.OpenapiPrivateEndpointConnectionConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPrivateEndpointConnection } from "@ubx/sdk-azure/azure/data/postgresql/openapi_private_endpoint_connection";

    export default stack("example", () => {
      intent({
        summary: "look up azure_postgresql_openapi_private_endpoint_connection",
      });

      data(OpenapiPrivateEndpointConnection, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.postgresql import OpenapiPrivateEndpointConnection, OpenapiPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_postgresql_openapi_private_endpoint_connection")

        ubx.data(OpenapiPrivateEndpointConnection, "example", OpenapiPrivateEndpointConnectionConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateEndpointConnection items on this page Returned by the lookup.
</ResponseField>
