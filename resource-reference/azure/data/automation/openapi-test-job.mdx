---
title: "azure_automation_openapi_test_job"
description: "`azure_automation_openapi_test_job` reads the real, current status of a test run of an Azure Automation runbook draft."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_test_job"})

    		ubx.Data(automation.OpenapiTestJob, "example", automation.OpenapiTestJobConfig{
    			AutomationAccountName: "example-automation-account-name",
    			RunbookName:           "example-runbook-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiTestJob } from "@ubx/sdk-azure/azure/data/automation/openapi_test_job";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_test_job" });

      data(OpenapiTestJob, "example", {
        automationAccountName: "example-automation-account-name",
        runbookName: "example-runbook-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiTestJob, OpenapiTestJobConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_test_job")

        ubx.data(OpenapiTestJob, "example", OpenapiTestJobConfig(
            automation_account_name="example-automation-account-name",
            runbook_name="example-runbook-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  The real name of the Azure Automation account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="runbook_name" type="string" required>
  The real name of the runbook this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  Gets or sets the creation time of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  Gets or sets the end time of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="exception" type="string">
  Gets or sets the exception of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Gets or sets the last modified time of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="last_status_modified_time" type="string">
  Gets or sets the last status modified time of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="log_activity_trace" type="number">
  The activity-level tracing options of the runbook. Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Gets or sets the parameters of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="run_on" type="string">
  Gets or sets the runOn which specifies the group name where the job is to be executed. Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  Gets or sets the start time of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Gets or sets the status of the test job. Returned by the lookup.
</ResponseField>

<ResponseField name="status_details" type="string">
  Gets or sets the status details of the test job. Returned by the lookup.
</ResponseField>
