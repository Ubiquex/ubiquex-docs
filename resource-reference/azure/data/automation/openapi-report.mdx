---
title: "azure_automation_openapi_report"
description: "`azure_automation_openapi_report` reads a real Update Management or DSC compliance report generated by an Azure Automation account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_report"})

    		ubx.Data(automation.OpenapiReport, "example", automation.OpenapiReportConfig{
    			AutomationAccountName: "example-automation-account-name",
    			NodeId:                "example",
    			ReportId:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiReport } from "./local-sdk/azure/sdk/typescript/azure/data/automation/openapi_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_report" });

      data(OpenapiReport, "example", {
        automationAccountName: "example-automation-account-name",
        nodeId: "example",
        reportId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiReport, OpenapiReportConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_report")

        ubx.data(OpenapiReport, "example", OpenapiReportConfig(
            automation_account_name="example-automation-account-name",
            node_id="example",
            report_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  The real name of the Azure Automation account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="node_id" type="string" required>
  The real, unique identifier of the node this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="report_id" type="string" required>
  The real, unique identifier of the report this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
