---
title: "azure_automation_openapi_source_control_sync_job_stream"
description: "`azure_automation_openapi_source_control_sync_job_stream` reads the real, streamed output from a running Azure Automation source-control sync job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_source_control_sync_job_stream"})

    		ubx.Data(automation.OpenapiSourceControlSyncJobStream, "example", automation.OpenapiSourceControlSyncJobStreamConfig{
    			AutomationAccountName:  "example-automation-account-name",
    			SourceControlName:      "example-source-control-name",
    			SourceControlSyncJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSourceControlSyncJobStream } from "@ubx/sdk-azure/azure/data/automation/openapi_source_control_sync_job_stream";

    export default stack("example", () => {
      intent({
        summary: "look up azure_automation_openapi_source_control_sync_job_stream",
      });

      data(OpenapiSourceControlSyncJobStream, "example", {
        automationAccountName: "example-automation-account-name",
        sourceControlName: "example-source-control-name",
        sourceControlSyncJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiSourceControlSyncJobStream, OpenapiSourceControlSyncJobStreamConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_source_control_sync_job_stream")

        ubx.data(OpenapiSourceControlSyncJobStream, "example", OpenapiSourceControlSyncJobStreamConfig(
            automation_account_name="example-automation-account-name",
            source_control_name="example-source-control-name",
            source_control_sync_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  The real name of the Azure Automation account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="source_control_name" type="string" required>
  The real name of the source control connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="source_control_sync_job_id" type="string" required>
  The real, unique identifier of the source control sync job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SourceControlSyncJobStream items on this page Returned by the lookup.
</ResponseField>
