---
title: "azure_automation_openapi_linked_workspace"
description: "`azure_automation_openapi_linked_workspace` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_automation_openapi_linked_workspace` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_linked_workspace"})

    		ubx.Data(automation.OpenapiLinkedWorkspace, "example", automation.OpenapiLinkedWorkspaceConfig{
    			AutomationAccountName: "example-automation-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiLinkedWorkspace } from "@ubx/sdk-azure/azure/data/automation/openapi_linked_workspace";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_linked_workspace" });

      data(OpenapiLinkedWorkspace, "example", {
        automationAccountName: "example-automation-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiLinkedWorkspace, OpenapiLinkedWorkspaceConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_linked_workspace")

        ubx.data(OpenapiLinkedWorkspace, "example", OpenapiLinkedWorkspaceConfig(
            automation_account_name="example-automation-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="id" type="string">
  Gets the id of the linked workspace. Returned by the lookup.
</ResponseField>
