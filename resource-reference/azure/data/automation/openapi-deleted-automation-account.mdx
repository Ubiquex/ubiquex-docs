---
title: "azure_automation_openapi_deleted_automation_account"
description: "`azure_automation_openapi_deleted_automation_account` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_automation_openapi_deleted_automation_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_deleted_automation_account"})

    		ubx.Data(automation.OpenapiDeletedAutomationAccount, "example", automation.OpenapiDeletedAutomationAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDeletedAutomationAccount } from "@ubx/sdk-azure/azure/data/automation/openapi_deleted_automation_account";

    export default stack("example", () => {
      intent({
        summary: "look up azure_automation_openapi_deleted_automation_account",
      });

      data(OpenapiDeletedAutomationAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiDeletedAutomationAccount, OpenapiDeletedAutomationAccountConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_deleted_automation_account")

        ubx.data(OpenapiDeletedAutomationAccount, "example", OpenapiDeletedAutomationAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  Gets or sets the list of deleted automation accounts. Returned by the lookup.
</ResponseField>
