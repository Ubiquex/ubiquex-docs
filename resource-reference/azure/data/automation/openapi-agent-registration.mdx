---
title: "azure_automation_openapi_agent_registration"
description: "`azure_automation_openapi_agent_registration` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_automation_openapi_agent_registration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_agent_registration"})

    		ubx.Data(automation.OpenapiAgentRegistration, "example", automation.OpenapiAgentRegistrationConfig{
    			AutomationAccountName: "example-automation-account-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAgentRegistration } from "@ubx/sdk-azure/azure/data/automation/openapi_agent_registration";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_agent_registration" });

      data(OpenapiAgentRegistration, "example", {
        automationAccountName: "example-automation-account-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiAgentRegistration, OpenapiAgentRegistrationConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_agent_registration")

        ubx.data(OpenapiAgentRegistration, "example", OpenapiAgentRegistrationConfig(
            automation_account_name="example-automation-account-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dsc_meta_configuration" type="string">
  Gets or sets the dsc meta configuration. Returned by the lookup.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Gets or sets the dsc server endpoint. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Gets or sets the id. Returned by the lookup.
</ResponseField>

<ResponseField name="keys" type="object">
  Definition of the agent registration keys. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="primary" type="string">
      Gets or sets the primary key. Optional lookup argument.
    </ResponseField>
    <ResponseField name="secondary" type="string">
      Gets or sets the secondary key. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
