---
title: "azure_automation_openapi_draft"
description: "`azure_automation_openapi_draft` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_automation_openapi_draft` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_draft"})

    		ubx.Data(automation.OpenapiDraft, "example", automation.OpenapiDraftConfig{
    			AutomationAccountName: "example-automation-account-name",
    			RunbookName:           "example-runbook-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiDraft } from "@ubx/sdk-azure/azure/data/automation/openapi_draft";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_draft" });

      data(OpenapiDraft, "example", {
        automationAccountName: "example-automation-account-name",
        runbookName: "example-runbook-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiDraft, OpenapiDraftConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_draft")

        ubx.data(OpenapiDraft, "example", OpenapiDraftConfig(
            automation_account_name="example-automation-account-name",
            runbook_name="example-runbook-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="runbook_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
