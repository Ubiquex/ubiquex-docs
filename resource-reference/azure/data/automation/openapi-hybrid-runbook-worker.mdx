---
title: "azure_automation_openapi_hybrid_runbook_worker"
description: "`data_azure_automation_openapi_hybrid_runbook_worker` looks up a real, already-existing `azure_automation_openapi_hybrid_runbook_worker`."
---

It registers a machine (on-premises or another cloud) as a hybrid worker, letting Azure Automation runbooks execute directly on it rather than Azure-hosted sandboxes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_hybrid_runbook_worker"})

    		ubx.Data(automation.OpenapiHybridRunbookWorker, "example", automation.OpenapiHybridRunbookWorkerConfig{
    			AutomationAccountName:        "example-automation-account-name",
    			HybridRunbookWorkerGroupName: "example-hybrid-runbook-worker-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiHybridRunbookWorker } from "./local-sdk/azure/sdk/typescript/azure/data/automation/openapi_hybrid_runbook_worker.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_hybrid_runbook_worker" });

      data(OpenapiHybridRunbookWorker, "example", {
        automationAccountName: "example-automation-account-name",
        hybridRunbookWorkerGroupName: "example-hybrid-runbook-worker-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiHybridRunbookWorker, OpenapiHybridRunbookWorkerConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_hybrid_runbook_worker")

        ubx.data(OpenapiHybridRunbookWorker, "example", OpenapiHybridRunbookWorkerConfig(
            automation_account_name="example-automation-account-name",
            hybrid_runbook_worker_group_name="example-hybrid-runbook-worker-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  The real name of the Azure Automation account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="hybrid_runbook_worker_group_name" type="string" required>
  The real name of the Hybrid Runbook Worker group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The HybridRunbookWorker items on this page Returned by the lookup.
</ResponseField>
