---
title: "azure_automation_openapi_configuration"
description: "`azure_automation_openapi_configuration` looks up a real Desired State Configuration (DSC) configuration document in an Azure Automation account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_automation_openapi_configuration"})

    		ubx.Data(automation.OpenapiConfiguration, "example", automation.OpenapiConfigurationConfig{
    			AutomationAccountName: "example-automation-account-name",
    			ConfigurationName:     "example-configuration-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiConfiguration } from "@ubx/sdk-azure/azure/data/automation/openapi_configuration";

    export default stack("example", () => {
      intent({ summary: "look up azure_automation_openapi_configuration" });

      data(OpenapiConfiguration, "example", {
        automationAccountName: "example-automation-account-name",
        configurationName: "example-configuration-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.automation import OpenapiConfiguration, OpenapiConfigurationConfig

    def describe():
        ubx.intent("look up azure_automation_openapi_configuration")

        ubx.data(OpenapiConfiguration, "example", OpenapiConfigurationConfig(
            automation_account_name="example-automation-account-name",
            configuration_name="example-configuration-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_account_name" type="string" required>
  The real name of the Azure Automation account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="configuration_name" type="string" required>
  The real name of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
