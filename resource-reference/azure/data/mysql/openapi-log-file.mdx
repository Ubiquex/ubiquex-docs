---
title: "azure_mysql_openapi_log_file"
description: "`azure_mysql_openapi_log_file` looks up a real log file generated by an Azure Database for MySQL flexible server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	mysql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/mysql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_mysql_openapi_log_file"})

    		ubx.Data(mysql.OpenapiLogFile, "example", mysql.OpenapiLogFileConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiLogFile } from "./local-sdk/azure/sdk/typescript/azure/data/mysql/openapi_log_file.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_mysql_openapi_log_file" });

      data(OpenapiLogFile, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.mysql import OpenapiLogFile, OpenapiLogFileConfig

    def describe():
        ubx.intent("look up azure_mysql_openapi_log_file")

        ubx.data(OpenapiLogFile, "example", OpenapiLogFileConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LogFile items on this page Returned by the lookup.
</ResponseField>
