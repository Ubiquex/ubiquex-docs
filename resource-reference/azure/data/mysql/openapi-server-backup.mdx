---
title: "azure_mysql_openapi_server_backup"
description: "`data_azure_mysql_openapi_server_backup` looks up a real, already-existing `azure_mysql_openapi_server_backup`."
---

It is a point-in-time backup of an Azure Database for MySQL flexible server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mysql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/mysql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_mysql_openapi_server_backup"})

    		ubx.Data(mysql.OpenapiServerBackup, "example", mysql.OpenapiServerBackupConfig{
    			ServerName: "example-server-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiServerBackup } from "@ubx/sdk-azure/azure/data/mysql/openapi_server_backup";

    export default stack("example", () => {
      intent({ summary: "look up azure_mysql_openapi_server_backup" });

      data(OpenapiServerBackup, "example", {
        serverName: "example-server-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.mysql import OpenapiServerBackup, OpenapiServerBackupConfig

    def describe():
        ubx.intent("look up azure_mysql_openapi_server_backup")

        ubx.data(OpenapiServerBackup, "example", OpenapiServerBackupConfig(
            server_name="example-server-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_name" type="string" required>
  The real name of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ServerBackup items on this page Returned by the lookup.
</ResponseField>
