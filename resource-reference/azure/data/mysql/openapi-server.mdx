---
title: "azure_mysql_openapi_server"
description: "`data_azure_mysql_openapi_server` looks up a real, already-existing `azure_mysql_openapi_server`."
---

It is an Azure Database for MySQL flexible server, a fully managed MySQL database server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mysql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/mysql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_mysql_openapi_server"})

    		ubx.Data(mysql.OpenapiServer, "example", mysql.OpenapiServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiServer } from "@ubx/sdk-azure/azure/data/mysql/openapi_server";

    export default stack("example", () => {
      intent({ summary: "look up azure_mysql_openapi_server" });

      data(OpenapiServer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.mysql import OpenapiServer, OpenapiServerConfig

    def describe():
        ubx.intent("look up azure_mysql_openapi_server")

        ubx.data(OpenapiServer, "example", OpenapiServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Server items on this page Returned by the lookup.
</ResponseField>
