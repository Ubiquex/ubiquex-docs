---
title: "azure_app_openapi_available_workload_profile"
description: "`azure_app_openapi_available_workload_profile` reads a real, available workload profile (compute/memory class) a Container Apps environment can be..."
---

`azure_app_openapi_available_workload_profile` reads a real, available workload profile (compute/memory class) a Container Apps environment can be configured with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_openapi_available_workload_profile"})

    		ubx.Data(app.OpenapiAvailableWorkloadProfile, "example", app.OpenapiAvailableWorkloadProfileConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAvailableWorkloadProfile } from "@ubx/sdk-azure/azure/data/app/openapi_available_workload_profile";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_openapi_available_workload_profile" });

      data(OpenapiAvailableWorkloadProfile, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import OpenapiAvailableWorkloadProfile, OpenapiAvailableWorkloadProfileConfig

    def describe():
        ubx.intent("look up azure_app_openapi_available_workload_profile")

        ubx.data(OpenapiAvailableWorkloadProfile, "example", OpenapiAvailableWorkloadProfileConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AvailableWorkloadProfile items on this page Returned by the lookup.
</ResponseField>
