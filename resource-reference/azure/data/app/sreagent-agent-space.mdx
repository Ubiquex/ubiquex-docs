---
title: "azure_app_sreagent_agent_space"
description: "`data_azure_app_sreagent_agent_space` looks up a real, already-existing `azure_app_sreagent_agent_space`."
---

It is the top-level Azure SRE Agent resource, the scoped environment agent(s) and their own connector(s) are deployed within.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_sreagent_agent_space"})

    		ubx.Data(app.SreagentAgentSpace, "example", app.SreagentAgentSpaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SreagentAgentSpace } from "./local-sdk/azure/sdk/typescript/azure/data/app/sreagent_agent_space.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_sreagent_agent_space" });

      data(SreagentAgentSpace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.app import SreagentAgentSpace, SreagentAgentSpaceConfig

    def describe():
        ubx.intent("look up azure_app_sreagent_agent_space")

        ubx.data(SreagentAgentSpace, "example", SreagentAgentSpaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AgentSpace items on this page Returned by the lookup.
</ResponseField>
