---
title: "azure_app_openapi_billing_meter"
description: "`azure_app_openapi_billing_meter` reads a real Azure billing meter associated with Container Apps usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_openapi_billing_meter"})

    		ubx.Data(app.OpenapiBillingMeter, "example", app.OpenapiBillingMeterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiBillingMeter } from "@ubx/sdk-azure/azure/data/app/openapi_billing_meter";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_openapi_billing_meter" });

      data(OpenapiBillingMeter, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import OpenapiBillingMeter, OpenapiBillingMeterConfig

    def describe():
        ubx.intent("look up azure_app_openapi_billing_meter")

        ubx.data(OpenapiBillingMeter, "example", OpenapiBillingMeterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="value" type="list(object)">
  Collection of billing meters. Returned by the lookup.
</ResponseField>
