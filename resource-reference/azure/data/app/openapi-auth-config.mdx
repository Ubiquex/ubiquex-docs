---
title: "azure_app_openapi_auth_config"
description: "`data_azure_app_openapi_auth_config` looks up a real, already-existing `azure_app_openapi_auth_config`."
---

It configures authentication and authorization (identity provider(s), token validation) for a Container App.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_openapi_auth_config"})

    		ubx.Data(app.OpenapiAuthConfig, "example", app.OpenapiAuthConfigConfig{
    			ContainerAppName: "example-container-app-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAuthConfig } from "@ubx/sdk-azure/azure/data/app/openapi_auth_config";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_openapi_auth_config" });

      data(OpenapiAuthConfig, "example", {
        containerAppName: "example-container-app-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import OpenapiAuthConfig, OpenapiAuthConfigConfig

    def describe():
        ubx.intent("look up azure_app_openapi_auth_config")

        ubx.data(OpenapiAuthConfig, "example", OpenapiAuthConfigConfig(
            container_app_name="example-container-app-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_app_name" type="string" required>
  The real name of the Container App this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AuthConfig items on this page Returned by the lookup.
</ResponseField>
