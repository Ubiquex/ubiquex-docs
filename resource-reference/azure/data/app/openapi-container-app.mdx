---
title: "azure_app_openapi_container_app"
description: "`data_azure_app_openapi_container_app` looks up a real, already-existing `azure_app_openapi_container_app`."
---

It is a Container App, a deployed, autoscaling container application running within a Container Apps environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_openapi_container_app"})

    		ubx.Data(app.OpenapiContainerApp, "example", app.OpenapiContainerAppConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiContainerApp } from "@ubx/sdk-azure/azure/data/app/openapi_container_app";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_openapi_container_app" });

      data(OpenapiContainerApp, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import OpenapiContainerApp, OpenapiContainerAppConfig

    def describe():
        ubx.intent("look up azure_app_openapi_container_app")

        ubx.data(OpenapiContainerApp, "example", OpenapiContainerAppConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ContainerApp items on this page Returned by the lookup.
</ResponseField>
