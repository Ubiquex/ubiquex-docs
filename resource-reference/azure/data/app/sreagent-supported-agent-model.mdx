---
title: "azure_app_sreagent_supported_agent_model"
description: "`azure_app_sreagent_supported_agent_model` reads a real, supported underlying model for an Azure SRE Agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_sreagent_supported_agent_model"})

    		ubx.Data(app.SreagentSupportedAgentModel, "example", app.SreagentSupportedAgentModelConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SreagentSupportedAgentModel } from "@ubx/sdk-azure/azure/data/app/sreagent_supported_agent_model";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_sreagent_supported_agent_model" });

      data(SreagentSupportedAgentModel, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import SreagentSupportedAgentModel, SreagentSupportedAgentModelConfig

    def describe():
        ubx.intent("look up azure_app_sreagent_supported_agent_model")

        ubx.data(SreagentSupportedAgentModel, "example", SreagentSupportedAgentModelConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SupportedAgentModel items on this page Returned by the lookup.
</ResponseField>
