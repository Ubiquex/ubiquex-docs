---
title: "azure_app_openapi_list_usages_result"
description: "`azure_app_openapi_list_usages_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_app_openapi_list_usages_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_app_openapi_list_usages_result"})

    		ubx.Data(app.OpenapiListUsagesResult, "example", app.OpenapiListUsagesResultConfig{
    			Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiListUsagesResult } from "@ubx/sdk-azure/azure/data/app/openapi_list_usages_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_app_openapi_list_usages_result" });

      data(OpenapiListUsagesResult, "example", {
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.app import OpenapiListUsagesResult, OpenapiListUsagesResultConfig

    def describe():
        ubx.intent("look up azure_app_openapi_list_usages_result")

        ubx.data(OpenapiListUsagesResult, "example", OpenapiListUsagesResultConfig(
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Usage items on this page Returned by the lookup.
</ResponseField>
