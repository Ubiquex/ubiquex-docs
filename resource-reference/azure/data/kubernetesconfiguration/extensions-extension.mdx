---
title: "azure_kubernetesconfiguration_extensions_extension"
description: "`azure_kubernetesconfiguration_extensions_extension` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_kubernetesconfiguration_extensions_extension` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetesconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kubernetesconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kubernetesconfiguration_extensions_extension"})

    		ubx.Data(kubernetesconfiguration.ExtensionsExtension, "example", kubernetesconfiguration.ExtensionsExtensionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExtensionsExtension } from "@ubx/sdk-azure/azure/data/kubernetesconfiguration/extensions_extension";

    export default stack("example", () => {
      intent({
        summary: "look up azure_kubernetesconfiguration_extensions_extension",
      });

      data(ExtensionsExtension, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kubernetesconfiguration import ExtensionsExtension, ExtensionsExtensionConfig

    def describe():
        ubx.intent("look up azure_kubernetesconfiguration_extensions_extension")

        ubx.data(ExtensionsExtension, "example", ExtensionsExtensionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of extension objects, if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Extensions within a Kubernetes cluster. Returned by the lookup.
</ResponseField>
