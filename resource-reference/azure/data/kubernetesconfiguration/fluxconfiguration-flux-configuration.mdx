---
title: "azure_kubernetesconfiguration_fluxconfiguration_flux_configuration"
description: "`data_azure_kubernetesconfiguration_fluxconfiguration_flux_configuration` looks up a real, already-existing..."
---

`data_azure_kubernetesconfiguration_fluxconfiguration_flux_configuration` looks up a real, already-existing `azure_kubernetesconfiguration_fluxconfiguration_flux_configuration`. It configures GitOps-based continuous deployment for a Kubernetes cluster using Flux, reconciling cluster state against a Git repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetesconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kubernetesconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kubernetesconfiguration_fluxconfiguration_flux_configuration"})

    		ubx.Data(kubernetesconfiguration.FluxconfigurationFluxConfiguration, "example", kubernetesconfiguration.FluxconfigurationFluxConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FluxconfigurationFluxConfiguration } from "@ubx/sdk-azure/azure/data/kubernetesconfiguration/fluxconfiguration_flux_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_kubernetesconfiguration_fluxconfiguration_flux_configuration",
      });

      data(FluxconfigurationFluxConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kubernetesconfiguration import FluxconfigurationFluxConfiguration, FluxconfigurationFluxConfigurationConfig

    def describe():
        ubx.intent("look up azure_kubernetesconfiguration_fluxconfiguration_flux_configuration")

        ubx.data(FluxconfigurationFluxConfiguration, "example", FluxconfigurationFluxConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of configuration objects, if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Flux Configurations within a Kubernetes cluster. Returned by the lookup.
</ResponseField>
