---
title: "azure_kubernetesconfiguration_source_control_configuration"
description: "`azure_kubernetesconfiguration_source_control_configuration` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_kubernetesconfiguration_source_control_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kubernetesconfiguration "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/kubernetesconfiguration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_kubernetesconfiguration_source_control_configuration"})

    		ubx.Data(kubernetesconfiguration.SourceControlConfiguration, "example", kubernetesconfiguration.SourceControlConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SourceControlConfiguration } from "@ubx/sdk-azure/azure/data/kubernetesconfiguration/source_control_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_kubernetesconfiguration_source_control_configuration",
      });

      data(SourceControlConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.kubernetesconfiguration import SourceControlConfiguration, SourceControlConfigurationConfig

    def describe():
        ubx.intent("look up azure_kubernetesconfiguration_source_control_configuration")

        ubx.data(SourceControlConfiguration, "example", SourceControlConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of configuration objects, if any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Source Control Configurations within a Kubernetes cluster. Returned by the lookup.
</ResponseField>
