---
title: "azure_web_openapi_vnet_info_resource"
description: "`data_azure_web_openapi_vnet_info_resource` looks up a real, already-existing `azure_web_openapi_vnet_info_resource`."
---

It describes the virtual network an App Service web app is integrated with, letting the app reach resources inside it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_vnet_info_resource"})

    		ubx.Data(web.OpenapiVnetInfoResource, "example", web.OpenapiVnetInfoResourceConfig{
    			Name:     "example",
    			Slot:     "example",
    			VnetName: "example-vnet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiVnetInfoResource } from "@ubx/sdk-azure/azure/data/web/openapi_vnet_info_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_vnet_info_resource" });

      data(OpenapiVnetInfoResource, "example", {
        name: "example",
        slot: "example",
        vnetName: "example-vnet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiVnetInfoResource, OpenapiVnetInfoResourceConfig

    def describe():
        ubx.intent("look up azure_web_openapi_vnet_info_resource")

        ubx.data(OpenapiVnetInfoResource, "example", OpenapiVnetInfoResourceConfig(
            name="example",
            slot="example",
            vnet_name="example-vnet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  The real deployment slot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vnet_name" type="string" required>
  The real name of the virtual network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Virtual Network information contract. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cert_blob" type="string">
      A certificate file (.cer) blob containing the public key of the private key used to authenticate a \nPoint-To-Site VPN connection. Optional lookup argument.
    </ResponseField>
    <ResponseField name="cert_thumbprint" type="string">
      The client certificate thumbprint. Returned by the lookup.
    </ResponseField>
    <ResponseField name="dns_servers" type="string">
      DNS servers to be used by this Virtual Network. This should be a comma-separated list of IP addresses. Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_swift" type="bool">
      Flag that is used to denote if this is VNET injection Optional lookup argument.
    </ResponseField>
    <ResponseField name="resync_required" type="bool">
      &lt;code&gt;true&lt;/code&gt; if a resync is required; otherwise, &lt;code&gt;false&lt;/code&gt;. Returned by the lookup.
    </ResponseField>
    <ResponseField name="routes" type="list(object)">
      The routes that this Virtual Network connection uses. Returned by the lookup.
    </ResponseField>
    <ResponseField name="vnet_resource_id" type="string">
      The Virtual Network's resource ID. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
