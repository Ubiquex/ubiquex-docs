---
title: "azure_web_openapi_site_container"
description: "`data_azure_web_openapi_site_container` looks up a real, already-existing `azure_web_openapi_site_container`."
---

It defines a single container image and its own configuration for a multi-container App Service web app.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site_container"})

    		ubx.Data(web.OpenapiSiteContainer, "example", web.OpenapiSiteContainerConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSiteContainer } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_site_container.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_site_container" });

      data(OpenapiSiteContainer, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSiteContainer, OpenapiSiteContainerConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site_container")

        ubx.data(OpenapiSiteContainer, "example", OpenapiSiteContainerConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SiteContainer items on this page Returned by the lookup.
</ResponseField>
