---
title: "azure_web_openapi_route"
description: "`azure_web_openapi_route` reads a real, configured route on an App Service site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_route"})

    		ubx.Data(web.OpenapiRoute, "example", web.OpenapiRouteConfig{
    			Name:      "example",
    			RouteName: "example-route-name",
    			VnetName:  "example-vnet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiRoute } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_route.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_route" });

      data(OpenapiRoute, "example", {
        name: "example",
        routeName: "example-route-name",
        vnetName: "example-vnet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiRoute, OpenapiRouteConfig

    def describe():
        ubx.intent("look up azure_web_openapi_route")

        ubx.data(OpenapiRoute, "example", OpenapiRouteConfig(
            name="example",
            route_name="example-route-name",
            vnet_name="example-vnet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="route_name" type="string" required>
  The real name of the route this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vnet_name" type="string" required>
  The real name of the virtual network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
