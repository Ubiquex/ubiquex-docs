---
title: "azure_web_openapi_billing_meter"
description: "`azure_web_openapi_billing_meter` reads a real Azure billing meter associated with App Service usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_billing_meter"})

    		ubx.Data(web.OpenapiBillingMeter, "example", web.OpenapiBillingMeterConfig{
    			BillingLocation: "example",
    			OsType:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiBillingMeter } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_billing_meter.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_billing_meter" });

      data(OpenapiBillingMeter, "example", {
        billingLocation: "example",
        osType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiBillingMeter, OpenapiBillingMeterConfig

    def describe():
        ubx.intent("look up azure_web_openapi_billing_meter")

        ubx.data(OpenapiBillingMeter, "example", OpenapiBillingMeterConfig(
            billing_location="example",
            os_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_location" type="string">
  The real, registered country or region of the billing account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="os_type" type="string">
  The real operating system type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The BillingMeter items on this page Returned by the lookup.
</ResponseField>
