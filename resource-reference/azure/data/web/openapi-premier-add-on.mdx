---
title: "azure_web_openapi_premier_add_on"
description: "`azure_web_openapi_premier_add_on` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_premier_add_on` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_premier_add_on"})

    		ubx.Data(web.OpenapiPremierAddOn, "example", web.OpenapiPremierAddOnConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPremierAddOn } from "@ubx/sdk-azure/azure/data/web/openapi_premier_add_on";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_premier_add_on" });

      data(OpenapiPremierAddOn, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiPremierAddOn, OpenapiPremierAddOnConfig

    def describe():
        ubx.intent("look up azure_web_openapi_premier_add_on")

        ubx.data(OpenapiPremierAddOn, "example", OpenapiPremierAddOnConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  PremierAddOn resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="marketplace_offer" type="string">
      Premier add on Marketplace offer. Optional lookup argument.
    </ResponseField>
    <ResponseField name="marketplace_publisher" type="string">
      Premier add on Marketplace publisher. Optional lookup argument.
    </ResponseField>
    <ResponseField name="product" type="string">
      Premier add on Product. Optional lookup argument.
    </ResponseField>
    <ResponseField name="sku" type="string">
      Premier add on SKU. Optional lookup argument.
    </ResponseField>
    <ResponseField name="vendor" type="string">
      Premier add on Vendor. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
