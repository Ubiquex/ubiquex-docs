---
title: "azure_web_openapi_site_config_resource"
description: "`data_azure_web_openapi_site_config_resource` looks up a real, already-existing `azure_web_openapi_site_config_resource`."
---

It holds the detailed runtime configuration (stack, always-on, app settings behavior) for an App Service web app.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site_config_resource"})

    		ubx.Data(web.OpenapiSiteConfigResource, "example", web.OpenapiSiteConfigResourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSiteConfigResource } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_site_config_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_site_config_resource" });

      data(OpenapiSiteConfigResource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSiteConfigResource, OpenapiSiteConfigResourceConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site_config_resource")

        ubx.data(OpenapiSiteConfigResource, "example", OpenapiSiteConfigResourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SiteConfigResource items on this page Returned by the lookup.
</ResponseField>
