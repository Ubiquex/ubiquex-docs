---
title: "azure_web_openapi_workflow_run"
description: "`azure_web_openapi_workflow_run` looks up one real, executed run of a Logic App (Standard) workflow hosted in App Service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_workflow_run"})

    		ubx.Data(web.OpenapiWorkflowRun, "example", web.OpenapiWorkflowRunConfig{
    			Name:         "example",
    			WorkflowName: "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiWorkflowRun } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_workflow_run.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_workflow_run" });

      data(OpenapiWorkflowRun, "example", {
        name: "example",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiWorkflowRun, OpenapiWorkflowRunConfig

    def describe():
        ubx.intent("look up azure_web_openapi_workflow_run")

        ubx.data(OpenapiWorkflowRun, "example", OpenapiWorkflowRunConfig(
            name="example",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The WorkflowRun items on this page Returned by the lookup.
</ResponseField>
