---
title: "azure_web_openapi_site_configuration_snapshot_info"
description: "`azure_web_openapi_site_configuration_snapshot_info` looks up a real, saved snapshot of an App Service site's own configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site_configuration_snapshot_info"})

    		ubx.Data(web.OpenapiSiteConfigurationSnapshotInfo, "example", web.OpenapiSiteConfigurationSnapshotInfoConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSiteConfigurationSnapshotInfo } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_site_configuration_snapshot_info.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_web_openapi_site_configuration_snapshot_info",
      });

      data(OpenapiSiteConfigurationSnapshotInfo, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSiteConfigurationSnapshotInfo, OpenapiSiteConfigurationSnapshotInfoConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site_configuration_snapshot_info")

        ubx.data(OpenapiSiteConfigurationSnapshotInfo, "example", OpenapiSiteConfigurationSnapshotInfoConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SiteConfigurationSnapshotInfo items on this page Returned by the lookup.
</ResponseField>
