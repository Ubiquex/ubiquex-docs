---
title: "azure_web_openapi_user"
description: "`azure_web_openapi_user` reads the real publishing credentials (deployment username) for an App Service site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_user"})

    		ubx.Data(web.OpenapiUser, "example", web.OpenapiUserConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiUser } from "@ubx/sdk-azure/azure/data/web/openapi_user";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_user" });

      data(OpenapiUser, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiUser, OpenapiUserConfig

    def describe():
        ubx.intent("look up azure_web_openapi_user")

        ubx.data(OpenapiUser, "example", OpenapiUserConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  User resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="publishing_password" type="string">
      Password used for publishing. Optional lookup argument.
    </ResponseField>
    <ResponseField name="publishing_password_hash" type="string">
      Password hash used for publishing. Optional lookup argument.
    </ResponseField>
    <ResponseField name="publishing_password_hash_salt" type="string">
      Password hash salt used for publishing. Optional lookup argument.
    </ResponseField>
    <ResponseField name="publishing_user_name" type="string" required>
      Username used for publishing. Required lookup argument.
    </ResponseField>
    <ResponseField name="scm_uri" type="string">
      Url of SCM site. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
