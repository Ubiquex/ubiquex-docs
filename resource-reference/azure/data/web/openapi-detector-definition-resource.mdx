---
title: "azure_web_openapi_detector_definition_resource"
description: "`azure_web_openapi_detector_definition_resource` looks up a real, available diagnostic detector App Service can run against a site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_detector_definition_resource"})

    		ubx.Data(web.OpenapiDetectorDefinitionResource, "example", web.OpenapiDetectorDefinitionResourceConfig{
    			DiagnosticCategory: "example",
    			SiteName:           "example-site-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiDetectorDefinitionResource } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_detector_definition_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_detector_definition_resource" });

      data(OpenapiDetectorDefinitionResource, "example", {
        diagnosticCategory: "example",
        siteName: "example-site-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiDetectorDefinitionResource, OpenapiDetectorDefinitionResourceConfig

    def describe():
        ubx.intent("look up azure_web_openapi_detector_definition_resource")

        ubx.data(OpenapiDetectorDefinitionResource, "example", OpenapiDetectorDefinitionResourceConfig(
            diagnostic_category="example",
            site_name="example-site-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="diagnostic_category" type="string" required>
  The real diagnostic category this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="site_name" type="string" required>
  The real name of the App Service site this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The DetectorDefinitionResource items on this page Returned by the lookup.
</ResponseField>
