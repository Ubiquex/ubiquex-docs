---
title: "azure_web_openapi_triggered_web_job"
description: "`azure_web_openapi_triggered_web_job` looks up a real, on-demand or scheduled WebJob deployed to an App Service site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_triggered_web_job"})

    		ubx.Data(web.OpenapiTriggeredWebJob, "example", web.OpenapiTriggeredWebJobConfig{
    			Name: "example",
    			Slot: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiTriggeredWebJob } from "@ubx/sdk-azure/azure/data/web/openapi_triggered_web_job";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_triggered_web_job" });

      data(OpenapiTriggeredWebJob, "example", {
        name: "example",
        slot: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiTriggeredWebJob, OpenapiTriggeredWebJobConfig

    def describe():
        ubx.intent("look up azure_web_openapi_triggered_web_job")

        ubx.data(OpenapiTriggeredWebJob, "example", OpenapiTriggeredWebJobConfig(
            name="example",
            slot="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  The real deployment slot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The TriggeredWebJob items on this page Returned by the lookup.
</ResponseField>
