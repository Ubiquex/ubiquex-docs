---
title: "azure_web_openapi_hosting_environment"
description: "`azure_web_openapi_hosting_environment` looks up a real App Service Environment -- an isolated..."
---

`azure_web_openapi_hosting_environment` looks up a real App Service Environment -- an isolated, dedicated deployment of the App Service infrastructure inside a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_hosting_environment"})

    		ubx.Data(web.OpenapiHostingEnvironment, "example", web.OpenapiHostingEnvironmentConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiHostingEnvironment } from "@ubx/sdk-azure/azure/data/web/openapi_hosting_environment";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_hosting_environment" });

      data(OpenapiHostingEnvironment, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiHostingEnvironment, OpenapiHostingEnvironmentConfig

    def describe():
        ubx.intent("look up azure_web_openapi_hosting_environment")

        ubx.data(OpenapiHostingEnvironment, "example", OpenapiHostingEnvironmentConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
