---
title: "azure_web_openapi_swift_virtual_network"
description: "`azure_web_openapi_swift_virtual_network` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_swift_virtual_network` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_swift_virtual_network"})

    		ubx.Data(web.OpenapiSwiftVirtualNetwork, "example", web.OpenapiSwiftVirtualNetworkConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSwiftVirtualNetwork } from "@ubx/sdk-azure/azure/data/web/openapi_swift_virtual_network";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_swift_virtual_network" });

      data(OpenapiSwiftVirtualNetwork, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSwiftVirtualNetwork, OpenapiSwiftVirtualNetworkConfig

    def describe():
        ubx.intent("look up azure_web_openapi_swift_virtual_network")

        ubx.data(OpenapiSwiftVirtualNetwork, "example", OpenapiSwiftVirtualNetworkConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  SwiftVirtualNetwork resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="subnet_resource_id" type="string">
      The Virtual Network subnet's resource ID. This is the subnet that this Web App will join. This subnet must have a delegation to Microsoft.Web/serverFarms defined first. Optional lookup argument.
    </ResponseField>
    <ResponseField name="swift_supported" type="bool">
      A flag that specifies if the scale unit this Web App is on supports Swift integration. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
