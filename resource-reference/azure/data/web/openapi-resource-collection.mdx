---
title: "azure_web_openapi_resource_collection"
description: "`azure_web_openapi_resource_collection` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_resource_collection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_resource_collection"})

    		ubx.Data(web.OpenapiResourceCollection, "example", web.OpenapiResourceCollectionConfig{
    			Name:          "example",
    			NamespaceName: "example-namespace-name",
    			RelayName:     "example-relay-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiResourceCollection } from "@ubx/sdk-azure/azure/data/web/openapi_resource_collection";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_resource_collection" });

      data(OpenapiResourceCollection, "example", {
        name: "example",
        namespaceName: "example-namespace-name",
        relayName: "example-relay-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiResourceCollection, OpenapiResourceCollectionConfig

    def describe():
        ubx.intent("look up azure_web_openapi_resource_collection")

        ubx.data(OpenapiResourceCollection, "example", OpenapiResourceCollectionConfig(
            name="example",
            namespace_name="example-namespace-name",
            relay_name="example-relay-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="namespace_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="relay_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  Link to next page of resources. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(string)">
  Collection of resources. Returned by the lookup.
</ResponseField>
