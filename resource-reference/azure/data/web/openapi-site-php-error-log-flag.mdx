---
title: "azure_web_openapi_site_php_error_log_flag"
description: "`azure_web_openapi_site_php_error_log_flag` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_site_php_error_log_flag` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site_php_error_log_flag"})

    		ubx.Data(web.OpenapiSitePhpErrorLogFlag, "example", web.OpenapiSitePhpErrorLogFlagConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSitePhpErrorLogFlag } from "@ubx/sdk-azure/azure/data/web/openapi_site_php_error_log_flag";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_site_php_error_log_flag" });

      data(OpenapiSitePhpErrorLogFlag, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSitePhpErrorLogFlag, OpenapiSitePhpErrorLogFlagConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site_php_error_log_flag")

        ubx.data(OpenapiSitePhpErrorLogFlag, "example", OpenapiSitePhpErrorLogFlagConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  SitePhpErrorLogFlag resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="local_log_errors" type="string">
      Local log_errors setting. Optional lookup argument.
    </ResponseField>
    <ResponseField name="local_log_errors_max_length" type="string">
      Local log_errors_max_len setting. Optional lookup argument.
    </ResponseField>
    <ResponseField name="master_log_errors" type="string">
      Master log_errors setting. Optional lookup argument.
    </ResponseField>
    <ResponseField name="master_log_errors_max_length" type="string">
      Master log_errors_max_len setting. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
