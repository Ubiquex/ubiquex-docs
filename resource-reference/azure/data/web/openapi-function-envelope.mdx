---
title: "azure_web_openapi_function_envelope"
description: "`data_azure_web_openapi_function_envelope` looks up a real, already-existing `azure_web_openapi_function_envelope`."
---

It describes a single function within an Azure Functions app hosted on App Service infrastructure, including its own trigger, bindings, and configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_function_envelope"})

    		ubx.Data(web.OpenapiFunctionEnvelope, "example", web.OpenapiFunctionEnvelopeConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiFunctionEnvelope } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_function_envelope.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_function_envelope" });

      data(OpenapiFunctionEnvelope, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiFunctionEnvelope, OpenapiFunctionEnvelopeConfig

    def describe():
        ubx.intent("look up azure_web_openapi_function_envelope")

        ubx.data(OpenapiFunctionEnvelope, "example", OpenapiFunctionEnvelopeConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The FunctionEnvelope items on this page Returned by the lookup.
</ResponseField>
