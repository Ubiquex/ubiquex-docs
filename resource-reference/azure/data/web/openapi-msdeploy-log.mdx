---
title: "azure_web_openapi_msdeploy_log"
description: "`azure_web_openapi_msdeploy_log` reads the real log of the most recent Web Deploy (MSDeploy) operation against an App Service site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_msdeploy_log"})

    		ubx.Data(web.OpenapiMsdeployLog, "example", web.OpenapiMsdeployLogConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiMsdeployLog } from "@ubx/sdk-azure/azure/data/web/openapi_msdeploy_log";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_msdeploy_log" });

      data(OpenapiMsdeployLog, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiMsdeployLog, OpenapiMsdeployLogConfig

    def describe():
        ubx.intent("look up azure_web_openapi_msdeploy_log")

        ubx.data(OpenapiMsdeployLog, "example", OpenapiMsdeployLogConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="properties" type="object">
  MSDeployLog resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="entries" type="list(object)">
      List of log entry messages Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
