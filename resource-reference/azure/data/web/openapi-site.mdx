---
title: "azure_web_openapi_site"
description: "`data_azure_web_openapi_site` looks up a real, already-existing `azure_web_openapi_site`."
---

It is an App Service web app (or Function App), the deployable application resource that App Service Plan compute serves.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site"})

    		ubx.Data(web.OpenapiSite, "example", web.OpenapiSiteConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSite } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_site.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_site" });

      data(OpenapiSite, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSite, OpenapiSiteConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site")

        ubx.data(OpenapiSite, "example", OpenapiSiteConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="extended_location" type="object">
  Extended Location. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Name of extended location. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of extended location. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity" type="object">
  Managed service identity. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      Principal Id of managed service identity. Returned by the lookup.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Tenant of managed service identity. Returned by the lookup.
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of managed service identity. Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user assigned identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of resource. If the resource is an app, you can refer to https://github.com/Azure/app-service-linux-docs/blob/master/Things_You_Should_Know/kind_property.md#app-service-resource-kind-reference for details supported values for kind. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Site resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ai_integration" type="object">
      AI integration configuration for the app. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="api_spec_path" type="string">
          Relative path to the API specification file from /home in site files. Optional lookup argument.
        </ResponseField>
        <ResponseField name="mcp" type="object">
          MCP configuration settings. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="servers" type="list(object)">
              Array of MCP server configurations. This array is replaced in its entirety on update; individual servers cannot be patched independently. An empty array signals that MCP should be disabled/deleted. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="site_auth" type="object">
          External auth configuration for the MCP endpoint (used when EasyAuth is not enabled). At minimum, `scopes` and either `wellKnownOpenIdConfiguration` or `issuer` must be provided. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="audience" type="string">
              Expected 'aud' claim. Optional lookup argument.
            </ResponseField>
            <ResponseField name="issuer" type="string">
              Expected 'iss' claim. Required for custom site authentication if `wellKnownOpenIdConfiguration` is not provided. Optional lookup argument.
            </ResponseField>
            <ResponseField name="jwks_uri" type="string">
              JWKS URL for verifying JWT signatures. Optional lookup argument.
            </ResponseField>
            <ResponseField name="scopes" type="list(string)">
              OAuth scopes for Protected Resource Metadata (PRM). Required for custom site authentication. Optional lookup argument.
            </ResponseField>
            <ResponseField name="well_known_open_id_configuration" type="string">
              OIDC metadata URL. Required for custom site authentication if `issuer` is not provided. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="auto_generated_domain_name_label_scope" type="string">
      Specifies the scope of uniqueness for the default hostname during resource creation Optional lookup argument.
    </ResponseField>
    <ResponseField name="availability_state" type="string">
      Management information availability state for the app. Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_affinity_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to enable client affinity; &lt;code&gt;false&lt;/code&gt; to stop sending session affinity cookies, which route client requests in the same session to the same instance. Default is &lt;code&gt;true&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_affinity_partitioning_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to enable client affinity partitioning using CHIPS cookies, this will add the &lt;code&gt;partitioned&lt;/code&gt; property to the affinity cookies; &lt;code&gt;false&lt;/code&gt; to stop sending partitioned affinity cookies. Default is &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_affinity_proxy_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to override client affinity cookie domain with X-Forwarded-Host request header. &lt;code&gt;false&lt;/code&gt; to use default domain. Default is &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_cert_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to enable client certificate authentication (TLS mutual authentication); otherwise, &lt;code&gt;false&lt;/code&gt;. Default is &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_cert_exclusion_paths" type="string">
      client certificate authentication comma-separated exclusion paths Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_cert_mode" type="string">
      This composes with ClientCertEnabled setting. - ClientCertEnabled: false means ClientCert is ignored. - ClientCertEnabled: true and ClientCertMode: Required means ClientCert is required. - ClientCertEnabled: true and ClientCertMode: Optional means ClientCert is optional or accepted. Optional lookup argument.
    </ResponseField>
    <ResponseField name="cloning_info" type="object">
      Information needed for cloning operation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="app_settings_overrides" type="map(string)">
          Application setting overrides for cloned app. If specified, these settings override the settings cloned from source app. Otherwise, application settings from source app are retained. Optional lookup argument.
        </ResponseField>
        <ResponseField name="clone_custom_host_names" type="bool">
          &lt;code&gt;true&lt;/code&gt; to clone custom hostnames from source app; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="clone_source_control" type="bool">
          &lt;code&gt;true&lt;/code&gt; to clone source control from source app; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="configure_load_balancing" type="bool">
          &lt;code&gt;true&lt;/code&gt; to configure load balancing for source and destination app. Optional lookup argument.
        </ResponseField>
        <ResponseField name="correlation_id" type="string">
          Correlation ID of cloning operation. This ID ties multiple cloning operations together to use the same snapshot. Optional lookup argument.
        </ResponseField>
        <ResponseField name="hosting_environment" type="string">
          App Service Environment. Optional lookup argument.
        </ResponseField>
        <ResponseField name="overwrite" type="bool">
          &lt;code&gt;true&lt;/code&gt; to overwrite destination app; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="source_web_app_id" type="string" required>
          ARM resource ID of the source app. App resource ID is of the form /subscriptions/&#123;subId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Web/sites/&#123;siteName&#125; for production slots and /subscriptions/&#123;subId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Web/sites/&#123;siteName&#125;/slots/&#123;slotName&#125; for other slots. Required lookup argument.
        </ResponseField>
        <ResponseField name="source_web_app_location" type="string">
          Location of source app ex: West US or North Europe Optional lookup argument.
        </ResponseField>
        <ResponseField name="traffic_manager_profile_id" type="string">
          ARM resource ID of the Traffic Manager profile to use, if it exists. Traffic Manager resource ID is of the form /subscriptions/&#123;subId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Network/trafficManagerProfiles/&#123;profileName&#125;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="traffic_manager_profile_name" type="string">
          Name of Traffic Manager profile to create. This is only needed if Traffic Manager profile does not already exist. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_size" type="number">
      Size of the function container. Optional lookup argument.
    </ResponseField>
    <ResponseField name="custom_domain_verification_id" type="string">
      Unique identifier that verifies the custom domains assigned to the app. Customer will add this id to a txt record for verification. Optional lookup argument.
    </ResponseField>
    <ResponseField name="daily_memory_time_quota" type="number">
      Maximum allowed daily memory-time quota (applicable on dynamic apps only). Optional lookup argument.
    </ResponseField>
    <ResponseField name="dapr_config" type="object">
      App Dapr configuration. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="app_id" type="string">
          Dapr application identifier Optional lookup argument.
        </ResponseField>
        <ResponseField name="app_port" type="number">
          Tells Dapr which port your application is listening on Optional lookup argument.
        </ResponseField>
        <ResponseField name="enable_api_logging" type="bool">
          Enables API logging for the Dapr sidecar Optional lookup argument.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Boolean indicating if the Dapr side car is enabled Optional lookup argument.
        </ResponseField>
        <ResponseField name="http_max_request_size" type="number">
          Increasing max size of request body http servers parameter in MB to handle uploading of big files. Default is 4 MB. Optional lookup argument.
        </ResponseField>
        <ResponseField name="http_read_buffer_size" type="number">
          Dapr max size of http header read buffer in KB to handle when sending multi-KB headers. Default is 65KB. Optional lookup argument.
        </ResponseField>
        <ResponseField name="log_level" type="string">
          Sets the log level for the Dapr sidecar. Allowed values are debug, info, warn, error. Default is info. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_host_name" type="string">
      Default hostname of the app. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="dns_configuration" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="dns_alt_server" type="string">
          Alternate DNS server to be used by apps. This property replicates the WEBSITE_DNS_ALT_SERVER app setting. Optional lookup argument.
        </ResponseField>
        <ResponseField name="dns_legacy_sort_order" type="bool">
          Indicates that sites using Virtual network custom DNS servers are still sorting the list of DNS servers. Read-Only. Returned by the lookup.
        </ResponseField>
        <ResponseField name="dns_max_cache_timeout" type="number">
          Custom time for DNS to be cached in seconds. Allowed range: 0-60. Default is 30 seconds. 0 means caching disabled. Optional lookup argument.
        </ResponseField>
        <ResponseField name="dns_retry_attempt_count" type="number">
          Total number of retries for dns lookup. Allowed range: 1-5. Default is 3. Optional lookup argument.
        </ResponseField>
        <ResponseField name="dns_retry_attempt_timeout" type="number">
          Timeout for a single dns lookup in seconds. Allowed range: 1-30. Default is 3. Optional lookup argument.
        </ResponseField>
        <ResponseField name="dns_servers" type="list(string)">
          List of custom DNS servers to be used by an app for lookups. Maximum 5 dns servers can be set. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; if the app is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Setting this value to false disables the app (takes the app offline). Optional lookup argument.
    </ResponseField>
    <ResponseField name="enabled_host_names" type="list(string)">
      Enabled hostnames for the app.Hostnames need to be assigned (see HostNames) AND enabled. Otherwise, the app is not served on those hostnames. Returned by the lookup.
    </ResponseField>
    <ResponseField name="end_to_end_encryption_enabled" type="bool">
      Whether to use end to end encryption between the FrontEnd and the Worker Optional lookup argument.
    </ResponseField>
    <ResponseField name="function_app_config" type="object">
      Function app configuration. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="deployment" type="object">
          Configuration section for the function app deployment. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="storage" type="object">
              Storage for deployed package used by the function app. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="authentication" type="object">
                  Authentication method to access the storage account for deployment. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="storage_account_connection_string_name" type="string">
                      Use this property for StorageAccountConnectionString. Set the name of the app setting that has the storage account connection string. Do not set a value for this property when using other authentication type. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Property to select authentication type to access the selected storage account. Available options: SystemAssignedIdentity, UserAssignedIdentity, StorageAccountConnectionString. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="user_assigned_identity_resource_id" type="string">
                      Use this property for UserAssignedIdentity. Set the resource ID of the identity. Do not set a value for this property when using other authentication type. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="type" type="string">
                  Property to select Azure Storage type. Available options: blobContainer. Optional lookup argument.
                </ResponseField>
                <ResponseField name="value" type="string">
                  Property to set the URL for the selected Azure Storage type. Example: For blobContainer, the value could be https://&lt;storageAccountName&gt;.blob.core.windows.net/&lt;containerName&gt;. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="runtime" type="object">
          Function app runtime name and version. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Function app runtime name. Available options: dotnet-isolated, node, java, powershell, python, custom Optional lookup argument.
            </ResponseField>
            <ResponseField name="version" type="string">
              Function app runtime version. Example: 8 (for dotnet-isolated) Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scale_and_concurrency" type="object">
          Scale and concurrency settings for the function app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="always_ready" type="list(object)">
              'Always Ready' configuration for the function app. Optional lookup argument.
            </ResponseField>
            <ResponseField name="instance_memory_mb" type="number">
              Set the amount of memory allocated to each instance of the function app in MB. CPU and network bandwidth are allocated proportionally. Optional lookup argument.
            </ResponseField>
            <ResponseField name="maximum_instance_count" type="number">
              The maximum number of on demand instances per function group. Optional lookup argument.
            </ResponseField>
            <ResponseField name="triggers" type="object">
              Scale and concurrency settings for the function app triggers. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="http" type="object">
                  Scale and concurrency settings for the HTTP trigger. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="per_instance_concurrency" type="number">
                      The maximum number of concurrent HTTP trigger invocations per instance. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="site_update_strategy" type="object">
          Function app site update strategy configuration for deployments and site config updates. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              Function app site update strategy type. Available options: Recreate, RollingUpdate Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host_name_ssl_states" type="list(object)">
      Hostname SSL states are used to manage the SSL bindings for app's hostnames. Optional lookup argument.
    </ResponseField>
    <ResponseField name="host_names" type="list(string)">
      Hostnames associated with the app. Returned by the lookup.
    </ResponseField>
    <ResponseField name="host_names_disabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to disable the public hostnames of the app; otherwise, &lt;code&gt;false&lt;/code&gt;. If &lt;code&gt;true&lt;/code&gt;, the app is only accessible via API management process. Optional lookup argument.
    </ResponseField>
    <ResponseField name="hosting_environment_profile" type="object">
      Specification for an App Service Environment to use for this resource. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID of the App Service Environment. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the App Service Environment. Returned by the lookup.
        </ResponseField>
        <ResponseField name="type" type="string">
          Resource type of the App Service Environment. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="https_only" type="bool">
      HttpsOnly: configures a web site to accept only https requests. Issues redirect for http requests Optional lookup argument.
    </ResponseField>
    <ResponseField name="hyper_v" type="bool">
      Hyper-V sandbox. Optional lookup argument.
    </ResponseField>
    <ResponseField name="in_progress_operation_id" type="string">
      Specifies an operation id if this site has a pending operation. Returned by the lookup.
    </ResponseField>
    <ResponseField name="ip_mode" type="string">
      Specifies the IP mode of the app. Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_default_container" type="bool">
      &lt;code&gt;true&lt;/code&gt; if the app is a default container; otherwise, &lt;code&gt;false&lt;/code&gt;. Returned by the lookup.
    </ResponseField>
    <ResponseField name="is_xenon" type="bool">
      Obsolete: Hyper-V sandbox. Optional lookup argument.
    </ResponseField>
    <ResponseField name="key_vault_reference_identity" type="string">
      Identity to use for Key Vault Reference authentication. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_time_utc" type="string">
      Last time the app was modified, in UTC. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="maintenance_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; if the app is in maintenance mode; otherwise, &lt;code&gt;false&lt;/code&gt;. Setting this value to true puts the app in maintenance mode, making it return the maintenance custom error page. The maintenance custom error page is required to set the site to maintenance. Default value is &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="managed_environment_id" type="string">
      Azure Resource Manager ID of the customer's selected Managed Environment on which to host this app. This must be of the form /subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroup&#125;/providers/Microsoft.App/managedEnvironments/&#123;managedEnvironmentName&#125; Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_number_of_workers" type="number">
      Maximum number of workers. This only applies to Functions container. Returned by the lookup.
    </ResponseField>
    <ResponseField name="outbound_ip_addresses" type="string">
      List of IP addresses that the app uses for outbound connections (e.g. database access). Includes VIPs from tenants that site can be hosted with current settings. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="outbound_vnet_routing" type="object">
      Outbound traffic options over virtual network. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="all_traffic" type="bool">
          Enables all other routing options defined in OutboundVnetRouting if this setting is set to true. Optional lookup argument.
        </ResponseField>
        <ResponseField name="application_traffic" type="bool">
          This causes all outbound traffic to have Virtual Network Security Groups and User Defined Routes applied. Previously called VnetRouteAllEnabled. Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_restore_traffic" type="bool">
          Enables Backup and Restore operations over virtual network. Previously called VnetBackupRestoreEnabled Optional lookup argument.
        </ResponseField>
        <ResponseField name="content_share_traffic" type="bool">
          Enables accessing content over virtual network. Previously called VnetContentShareEnabled Optional lookup argument.
        </ResponseField>
        <ResponseField name="image_pull_traffic" type="bool">
          Enables pulling image over Virtual Network. Previously called VnetImagePullEnabled. Optional lookup argument.
        </ResponseField>
        <ResponseField name="managed_identity_traffic" type="bool">
          Enables Managed Identity operations over virtual network. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="platform_release_channel" type="string">
      The platform release channel for the site. Optional lookup argument.
    </ResponseField>
    <ResponseField name="possible_outbound_ip_addresses" type="string">
      List of IP addresses that the app uses for outbound connections (e.g. database access). Includes VIPs from all tenants except dataComponent. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Property to allow or block all public traffic. Allowed Values: 'Enabled', 'Disabled' or an empty string. Optional lookup argument.
    </ResponseField>
    <ResponseField name="redundancy_mode" type="string">
      Site redundancy mode Optional lookup argument.
    </ResponseField>
    <ResponseField name="repository_site_name" type="string">
      Name of the repository site. Returned by the lookup.
    </ResponseField>
    <ResponseField name="reserved" type="bool">
      &lt;code&gt;true&lt;/code&gt; if reserved; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_config" type="object">
      Function app resource requirements. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cpu" type="number">
          Required CPU in cores, e.g. 0.5 Optional lookup argument.
        </ResponseField>
        <ResponseField name="memory" type="string">
          Required memory, e.g. "1Gi" Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_group" type="string">
      Name of the resource group the app belongs to. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="scm_site_also_stopped" type="bool">
      &lt;code&gt;true&lt;/code&gt; to stop SCM (KUDU) site when the app is stopped; otherwise, &lt;code&gt;false&lt;/code&gt;. The default is &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="server_farm_id" type="string">
      Resource ID of the associated App Service plan, formatted as: "/subscriptions/&#123;subscriptionID&#125;/resourceGroups/&#123;groupName&#125;/providers/Microsoft.Web/serverfarms/&#123;appServicePlanName&#125;". Optional lookup argument.
    </ResponseField>
    <ResponseField name="site_config" type="object">
      Configuration of an App Service app. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="acr_use_managed_identity_creds" type="bool">
          Flag to use Managed Identity Creds for ACR pull Optional lookup argument.
        </ResponseField>
        <ResponseField name="acr_user_managed_identity_id" type="string">
          If using user managed identity, the user managed identity ClientId Optional lookup argument.
        </ResponseField>
        <ResponseField name="always_on" type="bool">
          &lt;code&gt;true&lt;/code&gt; if Always On is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="api_definition" type="object">
          Information about the formal API definition for the app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="url" type="string">
              The URL of the API definition. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="api_management_config" type="object">
          Azure API management (APIM) configuration linked to the app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              APIM-Api Identifier. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="app_command_line" type="string">
          App command line to launch. Optional lookup argument.
        </ResponseField>
        <ResponseField name="app_settings" type="list(object)">
          Application settings. This property is not returned in response to normal create and read requests since it may contain sensitive information. Optional lookup argument.
        </ResponseField>
        <ResponseField name="auto_heal_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if Auto Heal is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="auto_heal_rules" type="object">
          Rules that can be defined for auto-heal. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="actions" type="object">
              Actions which to take by the auto-heal module when a rule is triggered. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="action_type" type="string">
                  Predefined action to be taken. Optional lookup argument.
                </ResponseField>
                <ResponseField name="custom_action" type="object">
                  Custom action to be executed when an auto heal rule is triggered. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="exe" type="string">
                      Executable to be run. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="parameters" type="string">
                      Parameters for the executable. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="min_process_execution_time" type="string">
                  Minimum time the process must execute before taking the action Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="triggers" type="object">
              Triggers for auto-heal. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="private_bytes_in_kb" type="number">
                  A rule based on private bytes. Optional lookup argument.
                </ResponseField>
                <ResponseField name="requests" type="object">
                  Trigger based on total requests. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="count" type="number">
                      Request Count. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="time_interval" type="string">
                      Time interval. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="slow_requests" type="object">
                  Trigger based on request execution time. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="count" type="number">
                      Request Count. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      Request Path. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="time_interval" type="string">
                      Time interval. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="time_taken" type="string">
                      Time taken. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="slow_requests_with_path" type="list(object)">
                  A rule based on multiple Slow Requests Rule with path Optional lookup argument.
                </ResponseField>
                <ResponseField name="status_codes" type="list(object)">
                  A rule based on status codes. Optional lookup argument.
                </ResponseField>
                <ResponseField name="status_codes_range" type="list(object)">
                  A rule based on status codes ranges. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="auto_swap_slot_name" type="string">
          Auto-swap slot name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="azure_storage_accounts" type="map(object)">
          List of Azure Storage Accounts. Optional lookup argument.
        </ResponseField>
        <ResponseField name="connection_strings" type="list(object)">
          Connection strings. This property is not returned in response to normal create and read requests since it may contain sensitive information. Optional lookup argument.
        </ResponseField>
        <ResponseField name="cors" type="object">
          Cross-Origin Resource Sharing (CORS) settings for the app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="allowed_origins" type="list(string)">
              Gets or sets the list of origins that should be allowed to make cross-origin calls (for example: http://example.com:12345). Use "*" to allow all. Optional lookup argument.
            </ResponseField>
            <ResponseField name="support_credentials" type="bool">
              Gets or sets whether CORS requests with credentials are allowed. See https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS#Requests_with_credentials for more details. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_documents" type="list(string)">
          Default documents. Optional lookup argument.
        </ResponseField>
        <ResponseField name="detailed_error_logging_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if detailed error logging is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="document_root" type="string">
          Document root. Optional lookup argument.
        </ResponseField>
        <ResponseField name="elastic_web_app_scale_limit" type="number">
          Maximum number of workers that a site can scale out to. This setting only applies to apps in plans where ElasticScaleEnabled is &lt;code&gt;true&lt;/code&gt; Optional lookup argument.
        </ResponseField>
        <ResponseField name="experiments" type="object">
          Routing rules in production experiments. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="ramp_up_rules" type="list(object)">
              List of ramp-up rules. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ftps_state" type="string">
          State of FTP / FTPS service Optional lookup argument.
        </ResponseField>
        <ResponseField name="function_app_scale_limit" type="number">
          Maximum number of workers that a site can scale out to. This setting only applies to the Consumption and Elastic Premium Plans Optional lookup argument.
        </ResponseField>
        <ResponseField name="functions_runtime_scale_monitoring_enabled" type="bool">
          Gets or sets a value indicating whether functions runtime scale monitoring is enabled. When enabled, the ScaleController will not monitor event sources directly, but will instead call to the runtime to get scale status. Optional lookup argument.
        </ResponseField>
        <ResponseField name="handler_mappings" type="list(object)">
          Handler mappings. Optional lookup argument.
        </ResponseField>
        <ResponseField name="health_check_path" type="string">
          Health check path Optional lookup argument.
        </ResponseField>
        <ResponseField name="http20_enabled" type="bool">
          Http20Enabled: configures a web site to allow clients to connect over http2.0 Optional lookup argument.
        </ResponseField>
        <ResponseField name="http20_proxy_flag" type="number">
          Http20ProxyFlag: Configures a website to allow http2.0 to pass be proxied all the way to the app. 0 = disabled, 1 = pass through all http2 traffic, 2 = pass through gRPC only. Optional lookup argument.
        </ResponseField>
        <ResponseField name="http_logging_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if HTTP logging is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="ip_security_restrictions" type="list(object)">
          IP security restrictions for main. Optional lookup argument.
        </ResponseField>
        <ResponseField name="ip_security_restrictions_default_action" type="string">
          Default action for main access restriction if no rules are matched. Optional lookup argument.
        </ResponseField>
        <ResponseField name="java_container" type="string">
          Java container. Optional lookup argument.
        </ResponseField>
        <ResponseField name="java_container_version" type="string">
          Java container version. Optional lookup argument.
        </ResponseField>
        <ResponseField name="java_version" type="string">
          Java version. Optional lookup argument.
        </ResponseField>
        <ResponseField name="key_vault_reference_identity" type="string">
          Identity to use for Key Vault Reference authentication. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limits" type="object">
          Metric limits set on an app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="max_disk_size_in_mb" type="number">
              Maximum allowed disk size usage in MB. Optional lookup argument.
            </ResponseField>
            <ResponseField name="max_memory_in_mb" type="number">
              Maximum allowed memory usage in MB. Optional lookup argument.
            </ResponseField>
            <ResponseField name="max_percentage_cpu" type="number">
              Maximum allowed CPU usage percentage. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="linux_fx_version" type="string">
          Linux App Framework and version Optional lookup argument.
        </ResponseField>
        <ResponseField name="load_balancing" type="string">
          Site load balancing. Optional lookup argument.
        </ResponseField>
        <ResponseField name="local_my_sql_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; to enable local MySQL; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="logs_directory_size_limit" type="number">
          HTTP logs directory size limit. Optional lookup argument.
        </ResponseField>
        <ResponseField name="machine_key" type="object">
          MachineKey of an app. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="decryption" type="string">
              Algorithm used for decryption. Optional lookup argument.
            </ResponseField>
            <ResponseField name="decryption_key" type="string">
              Decryption key. Optional lookup argument.
            </ResponseField>
            <ResponseField name="validation" type="string">
              MachineKey validation. Optional lookup argument.
            </ResponseField>
            <ResponseField name="validation_key" type="string">
              Validation key. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="managed_pipeline_mode" type="string">
          Managed pipeline mode. Optional lookup argument.
        </ResponseField>
        <ResponseField name="managed_service_identity_id" type="number">
          Managed Service Identity Id Optional lookup argument.
        </ResponseField>
        <ResponseField name="metadata" type="list(object)">
          Application metadata. This property cannot be retrieved, since it may contain secrets. Optional lookup argument.
        </ResponseField>
        <ResponseField name="min_tls_cipher_suite" type="string">
          The minimum strength TLS cipher suite allowed for an application Optional lookup argument.
        </ResponseField>
        <ResponseField name="min_tls_version" type="string">
          MinTlsVersion: configures the minimum version of TLS required for SSL requests Optional lookup argument.
        </ResponseField>
        <ResponseField name="minimum_elastic_instance_count" type="number">
          Number of minimum instance count for a site This setting only applies to the Elastic Plans Optional lookup argument.
        </ResponseField>
        <ResponseField name="net_framework_version" type="string">
          .NET Framework version. Optional lookup argument.
        </ResponseField>
        <ResponseField name="node_version" type="string">
          Version of Node.js. Optional lookup argument.
        </ResponseField>
        <ResponseField name="number_of_workers" type="number">
          Number of workers. Optional lookup argument.
        </ResponseField>
        <ResponseField name="php_version" type="string">
          Version of PHP. Optional lookup argument.
        </ResponseField>
        <ResponseField name="power_shell_version" type="string">
          Version of PowerShell. Optional lookup argument.
        </ResponseField>
        <ResponseField name="pre_warmed_instance_count" type="number">
          Number of preWarmed instances. This setting only applies to the Consumption and Elastic Plans Optional lookup argument.
        </ResponseField>
        <ResponseField name="public_network_access" type="string">
          Property to allow or block all public traffic. Optional lookup argument.
        </ResponseField>
        <ResponseField name="publishing_username" type="string">
          Publishing user name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="push" type="object">
          Push settings for the App. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="properties" type="object">
              PushSettings resource specific properties Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="dynamic_tags_json" type="string">
                  Gets or sets a JSON string containing a list of dynamic tags that will be evaluated from user claims in the push registration endpoint. Optional lookup argument.
                </ResponseField>
                <ResponseField name="is_push_enabled" type="bool" required>
                  Gets or sets a flag indicating whether the Push endpoint is enabled. Required lookup argument.
                </ResponseField>
                <ResponseField name="tag_whitelist_json" type="string">
                  Gets or sets a JSON string containing a list of tags that are whitelisted for use by the push registration endpoint. Optional lookup argument.
                </ResponseField>
                <ResponseField name="tags_requiring_auth" type="string">
                  Gets or sets a JSON string containing a list of tags that require user authentication to be used in the push registration endpoint. Tags can consist of alphanumeric characters and the following: '_', '@', '#', '.', ':', '-'. Validation should be performed at the PushRequestHandler. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="python_version" type="string">
          Version of Python. Optional lookup argument.
        </ResponseField>
        <ResponseField name="remote_debugging_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if remote debugging is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="remote_debugging_version" type="string">
          Remote debugging version. Optional lookup argument.
        </ResponseField>
        <ResponseField name="request_tracing_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if request tracing is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="request_tracing_expiration_time" type="string">
          Request tracing expiration time. Optional lookup argument.
        </ResponseField>
        <ResponseField name="scm_ip_security_restrictions" type="list(object)">
          IP security restrictions for scm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="scm_ip_security_restrictions_default_action" type="string">
          Default action for main access restriction if no rules are matched. Optional lookup argument.
        </ResponseField>
        <ResponseField name="scm_ip_security_restrictions_use_main" type="bool">
          IP security restrictions for scm to use main. Optional lookup argument.
        </ResponseField>
        <ResponseField name="scm_min_tls_version" type="string">
          MinTlsVersion: configures the minimum version of TLS required for SSL requests Optional lookup argument.
        </ResponseField>
        <ResponseField name="scm_type" type="string">
          SCM type. Optional lookup argument.
        </ResponseField>
        <ResponseField name="tracing_options" type="string">
          Tracing options. Optional lookup argument.
        </ResponseField>
        <ResponseField name="use32_bit_worker_process" type="bool">
          &lt;code&gt;true&lt;/code&gt; to use 32-bit worker process; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="virtual_applications" type="list(object)">
          Virtual applications. Optional lookup argument.
        </ResponseField>
        <ResponseField name="vnet_name" type="string">
          Virtual Network name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="vnet_private_ports_count" type="number">
          The number of private ports assigned to this app. These will be assigned dynamically on runtime. Optional lookup argument.
        </ResponseField>
        <ResponseField name="vnet_route_all_enabled" type="bool">
          Virtual Network Route All enabled. This causes all outbound traffic to have Virtual Network Security Groups and User Defined Routes applied. Optional lookup argument.
        </ResponseField>
        <ResponseField name="web_sockets_enabled" type="bool">
          &lt;code&gt;true&lt;/code&gt; if WebSocket is enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
        </ResponseField>
        <ResponseField name="website_time_zone" type="string">
          Sets the time zone a site uses for generating timestamps. Compatible with Linux and Windows App Service. Setting the WEBSITE_TIME_ZONE app setting takes precedence over this config. For Linux, expects tz database values https://www.iana.org/time-zones (for a quick reference see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). For Windows, expects one of the time zones listed under HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Time Zones Optional lookup argument.
        </ResponseField>
        <ResponseField name="windows_fx_version" type="string">
          Xenon App Framework and version Optional lookup argument.
        </ResponseField>
        <ResponseField name="x_managed_service_identity_id" type="number">
          Explicit Managed Service Identity Id Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="site_scoped_certificates_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; if site scoped certificates are enabled; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="sku" type="string">
      Current SKU of application based on associated App Service Plan. Some valid SKU values are Free, Shared, Basic, Dynamic, FlexConsumption, Standard, Premium, PremiumV2, PremiumV3, Isolated, IsolatedV2 Returned by the lookup.
    </ResponseField>
    <ResponseField name="slot_swap_status" type="object">
      The status of the last successful slot swap operation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="destination_slot_name" type="string">
          The destination slot of the last swap operation. Returned by the lookup.
        </ResponseField>
        <ResponseField name="source_slot_name" type="string">
          The source slot of the last swap operation. Returned by the lookup.
        </ResponseField>
        <ResponseField name="timestamp_utc" type="string">
          The time the last successful slot swap completed. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssh_enabled" type="bool">
      Whether to enable ssh access. Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Current state of the app. Returned by the lookup.
    </ResponseField>
    <ResponseField name="storage_account_required" type="bool">
      Checks if Customer provided storage account is required Optional lookup argument.
    </ResponseField>
    <ResponseField name="suspended_till" type="string">
      App suspended till in case memory-time quota is exceeded. Returned by the lookup.
    </ResponseField>
    <ResponseField name="target_swap_slot" type="string">
      Specifies which deployment slot this app will swap into. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="traffic_manager_host_names" type="list(string)">
      Azure Traffic Manager hostnames associated with the app. Read-only. Returned by the lookup.
    </ResponseField>
    <ResponseField name="usage_state" type="string">
      State indicating whether the app has exceeded its quota usage. Read-only. Optional lookup argument.
    </ResponseField>
    <ResponseField name="virtual_network_subnet_id" type="string">
      Azure Resource Manager ID of the Virtual network and subnet to be joined by Regional VNET Integration. This must be of the form /subscriptions/&#123;subscriptionName&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Network/virtualNetworks/&#123;vnetName&#125;/subnets/&#123;subnetName&#125; Optional lookup argument.
    </ResponseField>
    <ResponseField name="workload_profile_name" type="string">
      Workload profile name for function app to execute on. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
