---
title: "azure_web_openapi_kube_environment"
description: "`data_azure_web_openapi_kube_environment` looks up a real, already-existing `azure_web_openapi_kube_environment`."
---

It is the legacy App Service Kubernetes Environment resource, an earlier Azure Arc-enabled Kubernetes hosting environment for App Service and Functions, since superseded by newer Container Apps-based hosting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_kube_environment"})

    		ubx.Data(web.OpenapiKubeEnvironment, "example", web.OpenapiKubeEnvironmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiKubeEnvironment } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_kube_environment.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_kube_environment" });

      data(OpenapiKubeEnvironment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiKubeEnvironment, OpenapiKubeEnvironmentConfig

    def describe():
        ubx.intent("look up azure_web_openapi_kube_environment")

        ubx.data(OpenapiKubeEnvironment, "example", OpenapiKubeEnvironmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The KubeEnvironment items on this page Returned by the lookup.
</ResponseField>
