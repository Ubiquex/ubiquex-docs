---
title: "azure_web_openapi_network_features"
description: "`azure_web_openapi_network_features` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_network_features` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_network_features"})

    		ubx.Data(web.OpenapiNetworkFeatures, "example", web.OpenapiNetworkFeaturesConfig{
    			Name: "example",
    			View: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiNetworkFeatures } from "@ubx/sdk-azure/azure/data/web/openapi_network_features";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_network_features" });

      data(OpenapiNetworkFeatures, "example", {
        name: "example",
        view: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiNetworkFeatures, OpenapiNetworkFeaturesConfig

    def describe():
        ubx.intent("look up azure_web_openapi_network_features")

        ubx.data(OpenapiNetworkFeatures, "example", OpenapiNetworkFeaturesConfig(
            name="example",
            view="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="view" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  NetworkFeatures resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="hybrid_connections" type="list(object)">
      The Hybrid Connections summary view. Returned by the lookup.
    </ResponseField>
    <ResponseField name="hybrid_connections_v2" type="list(object)">
      The Hybrid Connection V2 (Service Bus) view. Returned by the lookup.
    </ResponseField>
    <ResponseField name="virtual_network_connection" type="object">
      Virtual Network information contract. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cert_blob" type="string">
          A certificate file (.cer) blob containing the public key of the private key used to authenticate a \nPoint-To-Site VPN connection. Optional lookup argument.
        </ResponseField>
        <ResponseField name="cert_thumbprint" type="string">
          The client certificate thumbprint. Returned by the lookup.
        </ResponseField>
        <ResponseField name="dns_servers" type="string">
          DNS servers to be used by this Virtual Network. This should be a comma-separated list of IP addresses. Optional lookup argument.
        </ResponseField>
        <ResponseField name="is_swift" type="bool">
          Flag that is used to denote if this is VNET injection Optional lookup argument.
        </ResponseField>
        <ResponseField name="resync_required" type="bool">
          &lt;code&gt;true&lt;/code&gt; if a resync is required; otherwise, &lt;code&gt;false&lt;/code&gt;. Returned by the lookup.
        </ResponseField>
        <ResponseField name="routes" type="list(object)">
          The routes that this Virtual Network connection uses. Returned by the lookup.
        </ResponseField>
        <ResponseField name="vnet_resource_id" type="string">
          The Virtual Network's resource ID. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_network_name" type="string">
      The Virtual Network name. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
