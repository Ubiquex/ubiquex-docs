---
title: "azure_web_openapi_custom_dns_suffix_configuration"
description: "`azure_web_openapi_custom_dns_suffix_configuration` reads the real custom DNS suffix configuration for an App Service Environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_custom_dns_suffix_configuration"})

    		ubx.Data(web.OpenapiCustomDnsSuffixConfiguration, "example", web.OpenapiCustomDnsSuffixConfigurationConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiCustomDnsSuffixConfiguration } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_custom_dns_suffix_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_web_openapi_custom_dns_suffix_configuration",
      });

      data(OpenapiCustomDnsSuffixConfiguration, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiCustomDnsSuffixConfiguration, OpenapiCustomDnsSuffixConfigurationConfig

    def describe():
        ubx.intent("look up azure_web_openapi_custom_dns_suffix_configuration")

        ubx.data(OpenapiCustomDnsSuffixConfiguration, "example", OpenapiCustomDnsSuffixConfigurationConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  CustomDnsSuffixConfiguration resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="certificate_url" type="string">
      The URL referencing the Azure Key Vault certificate secret that should be used as the default SSL/TLS certificate for sites with the custom domain suffix. Optional lookup argument.
    </ResponseField>
    <ResponseField name="dns_suffix" type="string">
      The default custom domain suffix to use for all sites deployed on the ASE. Optional lookup argument.
    </ResponseField>
    <ResponseField name="key_vault_reference_identity" type="string">
      The user-assigned identity to use for resolving the key vault certificate reference. If not specified, the system-assigned ASE identity will be used if available. Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_details" type="string">
      Returned by the lookup.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
