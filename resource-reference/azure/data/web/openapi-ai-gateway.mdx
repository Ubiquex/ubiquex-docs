---
title: "azure_web_openapi_ai_gateway"
description: "`data_azure_web_openapi_ai_gateway` looks up a real, already-existing `azure_web_openapi_ai_gateway`."
---

It configures the AI Gateway tier of Azure API Management (public preview, 2026), a dedicated gateway for AI traffic that fronts model and MCP server endpoints behind one governed entry point with policies like semantic caching and token rate limiting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_ai_gateway"})

    		ubx.Data(web.OpenapiAiGateway, "example", web.OpenapiAiGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiAiGateway } from "@ubx/sdk-azure/azure/data/web/openapi_ai_gateway";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_ai_gateway" });

      data(OpenapiAiGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiAiGateway, OpenapiAiGatewayConfig

    def describe():
        ubx.intent("look up azure_web_openapi_ai_gateway")

        ubx.data(OpenapiAiGateway, "example", OpenapiAiGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The AiGateway items on this page Returned by the lookup.
</ResponseField>
