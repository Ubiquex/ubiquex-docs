---
title: "azure_web_openapi_slot"
description: "`azure_web_openapi_slot` looks up a real deployment slot -- a separately deployable, swappable copy of an App Service site."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_slot"})

    		ubx.Data(web.OpenapiSlot, "example", web.OpenapiSlotConfig{
    			Name: "example",
    			Slot: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSlot } from "@ubx/sdk-azure/azure/data/web/openapi_slot";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_slot" });

      data(OpenapiSlot, "example", {
        name: "example",
        slot: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSlot, OpenapiSlotConfig

    def describe():
        ubx.intent("look up azure_web_openapi_slot")

        ubx.data(OpenapiSlot, "example", OpenapiSlotConfig(
            name="example",
            slot="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  The real deployment slot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
