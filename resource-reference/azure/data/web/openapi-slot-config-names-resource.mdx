---
title: "azure_web_openapi_slot_config_names_resource"
description: "`azure_web_openapi_slot_config_names_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_web_openapi_slot_config_names_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_slot_config_names_resource"})

    		ubx.Data(web.OpenapiSlotConfigNamesResource, "example", web.OpenapiSlotConfigNamesResourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSlotConfigNamesResource } from "@ubx/sdk-azure/azure/data/web/openapi_slot_config_names_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_slot_config_names_resource" });

      data(OpenapiSlotConfigNamesResource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSlotConfigNamesResource, OpenapiSlotConfigNamesResourceConfig

    def describe():
        ubx.intent("look up azure_web_openapi_slot_config_names_resource")

        ubx.data(OpenapiSlotConfigNamesResource, "example", OpenapiSlotConfigNamesResourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  Names for connection strings, application settings, and external Azure storage account configuration identifiers to be marked as sticky to the deployment slot and not moved during a swap operation. This is valid for all deployment slots in an app. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="app_setting_names" type="list(string)">
      List of application settings names. Optional lookup argument.
    </ResponseField>
    <ResponseField name="azure_storage_config_names" type="list(string)">
      List of external Azure storage account identifiers. Optional lookup argument.
    </ResponseField>
    <ResponseField name="connection_string_names" type="list(string)">
      List of connection string names. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
