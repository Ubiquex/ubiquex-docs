---
title: "azure_web_openapi_vnet_gateway"
description: "`data_azure_web_openapi_vnet_gateway` looks up a real, already-existing `azure_web_openapi_vnet_gateway`."
---

It represents the App Service-side gateway endpoint of a virtual network integration connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_vnet_gateway"})

    		ubx.Data(web.OpenapiVnetGateway, "example", web.OpenapiVnetGatewayConfig{
    			GatewayName: "example-gateway-name",
    			Name:        "example",
    			Slot:        "example",
    			VnetName:    "example-vnet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiVnetGateway } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_vnet_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_vnet_gateway" });

      data(OpenapiVnetGateway, "example", {
        gatewayName: "example-gateway-name",
        name: "example",
        slot: "example",
        vnetName: "example-vnet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiVnetGateway, OpenapiVnetGatewayConfig

    def describe():
        ubx.intent("look up azure_web_openapi_vnet_gateway")

        ubx.data(OpenapiVnetGateway, "example", OpenapiVnetGatewayConfig(
            gateway_name="example-gateway-name",
            name="example",
            slot="example",
            vnet_name="example-vnet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_name" type="string" required>
  The real name of the gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  The real deployment slot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vnet_name" type="string" required>
  The real name of the virtual network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  VnetGateway resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="vnet_name" type="string">
      The Virtual Network name. Optional lookup argument.
    </ResponseField>
    <ResponseField name="vpn_package_uri" type="string" required>
      The URI where the VPN package can be downloaded. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
