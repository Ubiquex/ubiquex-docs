---
title: "azure_web_openapi_site_source_control"
description: "`azure_web_openapi_site_source_control` reads the real source control repository (and branch) an App Service site deploys from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_site_source_control"})

    		ubx.Data(web.OpenapiSiteSourceControl, "example", web.OpenapiSiteSourceControlConfig{
    			Name: "example",
    			Slot: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSiteSourceControl } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_site_source_control.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_site_source_control" });

      data(OpenapiSiteSourceControl, "example", {
        name: "example",
        slot: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSiteSourceControl, OpenapiSiteSourceControlConfig

    def describe():
        ubx.intent("look up azure_web_openapi_site_source_control")

        ubx.data(OpenapiSiteSourceControl, "example", OpenapiSiteSourceControlConfig(
            name="example",
            slot="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="slot" type="string" required>
  The real deployment slot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  SiteSourceControl resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="branch" type="string">
      Name of branch to use for deployment. Optional lookup argument.
    </ResponseField>
    <ResponseField name="deployment_rollback_enabled" type="bool">
      &lt;code&gt;true&lt;/code&gt; to enable deployment rollback; otherwise, &lt;code&gt;false&lt;/code&gt;. Optional lookup argument.
    </ResponseField>
    <ResponseField name="git_hub_action_configuration" type="object">
      The GitHub action configuration. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code_configuration" type="object">
          The GitHub action code configuration. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="runtime_stack" type="string">
              Runtime stack is used to determine the workflow file content for code base apps. Optional lookup argument.
            </ResponseField>
            <ResponseField name="runtime_version" type="string">
              Runtime version is used to determine what build version to set in the workflow file. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="container_configuration" type="object">
          The GitHub action container configuration. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="image_name" type="string">
              The image name for the build. Optional lookup argument.
            </ResponseField>
            <ResponseField name="password" type="string">
              The password used to upload the image to the container registry. Optional lookup argument.
            </ResponseField>
            <ResponseField name="server_url" type="string">
              The server URL for the container registry where the build will be hosted. Optional lookup argument.
            </ResponseField>
            <ResponseField name="username" type="string">
              The username used to upload the image to the container registry. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="generate_workflow_file" type="bool">
          Workflow option to determine whether the workflow file should be generated and written to the repository. Optional lookup argument.
        </ResponseField>
        <ResponseField name="is_linux" type="bool">
          This will help determine the workflow configuration to select. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_git_hub_action" type="bool">
      &lt;code&gt;true&lt;/code&gt; if this is deployed via GitHub action. Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_manual_integration" type="bool">
      &lt;code&gt;true&lt;/code&gt; to limit to manual integration; &lt;code&gt;false&lt;/code&gt; to enable continuous integration (which configures webhooks into online repos like GitHub). Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_mercurial" type="bool">
      &lt;code&gt;true&lt;/code&gt; for a Mercurial repository; &lt;code&gt;false&lt;/code&gt; for a Git repository. Optional lookup argument.
    </ResponseField>
    <ResponseField name="repo_url" type="string">
      Repository or source control URL. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
