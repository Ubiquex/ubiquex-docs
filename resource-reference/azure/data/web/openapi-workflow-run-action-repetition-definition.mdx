---
title: "azure_web_openapi_workflow_run_action_repetition_definition"
description: "`azure_web_openapi_workflow_run_action_repetition_definition` reads the real definition of one repetition (loop iteration) of an action within a Logic..."
---

`azure_web_openapi_workflow_run_action_repetition_definition` reads the real definition of one repetition (loop iteration) of an action within a Logic App (Standard) workflow run.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_workflow_run_action_repetition_definition"})

    		ubx.Data(web.OpenapiWorkflowRunActionRepetitionDefinition, "example", web.OpenapiWorkflowRunActionRepetitionDefinitionConfig{
    			ActionName:   "example-action-name",
    			Name:         "example",
    			RunName:      "example-run-name",
    			WorkflowName: "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiWorkflowRunActionRepetitionDefinition } from "@ubx/sdk-azure/azure/data/web/openapi_workflow_run_action_repetition_definition";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_web_openapi_workflow_run_action_repetition_definition",
      });

      data(OpenapiWorkflowRunActionRepetitionDefinition, "example", {
        actionName: "example-action-name",
        name: "example",
        runName: "example-run-name",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiWorkflowRunActionRepetitionDefinition, OpenapiWorkflowRunActionRepetitionDefinitionConfig

    def describe():
        ubx.intent("look up azure_web_openapi_workflow_run_action_repetition_definition")

        ubx.data(OpenapiWorkflowRunActionRepetitionDefinition, "example", OpenapiWorkflowRunActionRepetitionDefinitionConfig(
            action_name="example-action-name",
            name="example",
            run_name="example-run-name",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_name" type="string" required>
  The real name of the action this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="run_name" type="string" required>
  The real name of this run. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The WorkflowRunActionRepetitionDefinition items on this page Returned by the lookup.
</ResponseField>
