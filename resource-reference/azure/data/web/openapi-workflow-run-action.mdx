---
title: "azure_web_openapi_workflow_run_action"
description: "`azure_web_openapi_workflow_run_action` reads the real result of one action executed within a Logic App (Standard) workflow run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_workflow_run_action"})

    		ubx.Data(web.OpenapiWorkflowRunAction, "example", web.OpenapiWorkflowRunActionConfig{
    			Name:         "example",
    			RunName:      "example-run-name",
    			WorkflowName: "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiWorkflowRunAction } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_workflow_run_action.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_workflow_run_action" });

      data(OpenapiWorkflowRunAction, "example", {
        name: "example",
        runName: "example-run-name",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiWorkflowRunAction, OpenapiWorkflowRunActionConfig

    def describe():
        ubx.intent("look up azure_web_openapi_workflow_run_action")

        ubx.data(OpenapiWorkflowRunAction, "example", OpenapiWorkflowRunActionConfig(
            name="example",
            run_name="example-run-name",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="run_name" type="string" required>
  The real name of this run. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The WorkflowRunAction items on this page Returned by the lookup.
</ResponseField>
