---
title: "azure_web_openapi_source_control"
description: "`data_azure_web_openapi_source_control` looks up a real, already-existing `azure_web_openapi_source_control`."
---

It configures continuous deployment for an App Service web app from a linked source control repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_source_control"})

    		ubx.Data(web.OpenapiSourceControl, "example", web.OpenapiSourceControlConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiSourceControl } from "./local-sdk/azure/sdk/typescript/azure/data/web/openapi_source_control.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_source_control" });

      data(OpenapiSourceControl, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiSourceControl, OpenapiSourceControlConfig

    def describe():
        ubx.intent("look up azure_web_openapi_source_control")

        ubx.data(OpenapiSourceControl, "example", OpenapiSourceControlConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The SourceControl items on this page Returned by the lookup.
</ResponseField>
