---
title: "azure_web_openapi_migrate_my_sql_status"
description: "`azure_web_openapi_migrate_my_sql_status` reads the real, current status of migrating an App Service site's own in-app MySQL database to Azure Database..."
---

`azure_web_openapi_migrate_my_sql_status` reads the real, current status of migrating an App Service site's own in-app MySQL database to Azure Database for MySQL.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_web_openapi_migrate_my_sql_status"})

    		ubx.Data(web.OpenapiMigrateMySqlStatus, "example", web.OpenapiMigrateMySqlStatusConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiMigrateMySqlStatus } from "@ubx/sdk-azure/azure/data/web/openapi_migrate_my_sql_status";

    export default stack("example", () => {
      intent({ summary: "look up azure_web_openapi_migrate_my_sql_status" });

      data(OpenapiMigrateMySqlStatus, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.web import OpenapiMigrateMySqlStatus, OpenapiMigrateMySqlStatusConfig

    def describe():
        ubx.intent("look up azure_web_openapi_migrate_my_sql_status")

        ubx.data(OpenapiMigrateMySqlStatus, "example", OpenapiMigrateMySqlStatusConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kind" type="string">
  Kind of resource. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  MigrateMySqlStatus resource specific properties Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="local_my_sql_enabled" type="bool">
      True if the web app has in app MySql enabled Returned by the lookup.
    </ResponseField>
    <ResponseField name="migration_operation_status" type="string">
      The current status of the operation. Optional lookup argument.
    </ResponseField>
    <ResponseField name="operation_id" type="string">
      Operation ID for the migration task. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
