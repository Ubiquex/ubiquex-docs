---
title: "azure_streamanalytics_inputs_input"
description: "`data_azure_streamanalytics_inputs_input` looks up a real, already-existing `azure_streamanalytics_inputs_input`."
---

It defines a source (Event Hub, IoT Hub, Blob Storage) a Stream Analytics job reads real-time data from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	streamanalytics "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/streamanalytics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_streamanalytics_inputs_input"})

    		ubx.Data(streamanalytics.InputsInput, "example", streamanalytics.InputsInputConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InputsInput } from "@ubx/sdk-azure/azure/data/streamanalytics/inputs_input";

    export default stack("example", () => {
      intent({ summary: "look up azure_streamanalytics_inputs_input" });

      data(InputsInput, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.streamanalytics import InputsInput, InputsInputConfig

    def describe():
        ubx.intent("look up azure_streamanalytics_inputs_input")

        ubx.data(InputsInput, "example", InputsInputConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A list of inputs under a streaming job. Populated by a 'List' operation. Returned by the lookup.
</ResponseField>
