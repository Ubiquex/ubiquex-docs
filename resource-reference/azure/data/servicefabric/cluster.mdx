---
title: "azure_servicefabric_cluster"
description: "`data_azure_servicefabric_cluster` looks up a real, already-existing `azure_servicefabric_cluster`."
---

It is a Service Fabric cluster, a distributed systems platform for deploying and managing scalable, reliable microservices and container-based applications.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicefabric_cluster"})

    		ubx.Data(servicefabric.Cluster, "example", servicefabric.ClusterConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-azure/azure/data/servicefabric/cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_servicefabric_cluster" });

      data(Cluster, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.servicefabric import Cluster, ClusterConfig

    def describe():
        ubx.intent("look up azure_servicefabric_cluster")

        ubx.data(Cluster, "example", ClusterConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
