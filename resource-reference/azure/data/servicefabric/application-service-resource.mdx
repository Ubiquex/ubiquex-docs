---
title: "azure_servicefabric_application_service_resource"
description: "`data_azure_servicefabric_application_service_resource` looks up a real, already-existing `azure_servicefabric_application_service_resource`."
---

It is a single microservice within a Service Fabric application, running as either a stateless or stateful service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicefabric_application_service_resource"})

    		ubx.Data(servicefabric.ApplicationServiceResource, "example", servicefabric.ApplicationServiceResourceConfig{
    			ApiVersion:        "example",
    			ApplicationName:   "example-application-name",
    			ClusterName:       "example-cluster-name",
    			ResourceGroupName: "example-resource-group-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ApplicationServiceResource } from "./local-sdk/azure/sdk/typescript/azure/data/servicefabric/application_service_resource.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_servicefabric_application_service_resource",
      });

      data(ApplicationServiceResource, "example", {
        apiVersion: "example",
        applicationName: "example-application-name",
        clusterName: "example-cluster-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.servicefabric import ApplicationServiceResource, ApplicationServiceResourceConfig

    def describe():
        ubx.intent("look up azure_servicefabric_application_service_resource")

        ubx.data(ApplicationServiceResource, "example", ApplicationServiceResourceConfig(
            api_version="example",
            application_name="example-application-name",
            cluster_name="example-cluster-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="application_name" type="string" required>
  The real name of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of service list results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
