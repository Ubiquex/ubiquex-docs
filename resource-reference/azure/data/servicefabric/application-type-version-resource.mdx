---
title: "azure_servicefabric_application_type_version_resource"
description: "`azure_servicefabric_application_type_version_resource` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_servicefabric_application_type_version_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicefabric_application_type_version_resource"})

    		ubx.Data(servicefabric.ApplicationTypeVersionResource, "example", servicefabric.ApplicationTypeVersionResourceConfig{
    			ApiVersion:          "example",
    			ApplicationTypeName: "example-application-type-name",
    			ClusterName:         "example-cluster-name",
    			ResourceGroupName:   "example-resource-group-name",
    			SubscriptionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationTypeVersionResource } from "@ubx/sdk-azure/azure/data/servicefabric/application_type_version_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_servicefabric_application_type_version_resource",
      });

      data(ApplicationTypeVersionResource, "example", {
        apiVersion: "example",
        applicationTypeName: "example-application-type-name",
        clusterName: "example-cluster-name",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.servicefabric import ApplicationTypeVersionResource, ApplicationTypeVersionResourceConfig

    def describe():
        ubx.intent("look up azure_servicefabric_application_type_version_resource")

        ubx.data(ApplicationTypeVersionResource, "example", ApplicationTypeVersionResourceConfig(
            api_version="example",
            application_type_name="example-application-type-name",
            cluster_name="example-cluster-name",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="application_type_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of application type version list results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Returned by the lookup.
</ResponseField>
