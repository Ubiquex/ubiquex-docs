---
title: "azure_servicefabric_cluster_code_versions_result"
description: "`azure_servicefabric_cluster_code_versions_result` reads the real, available Service Fabric runtime code versions a cluster can be upgraded to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	servicefabric "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/servicefabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_servicefabric_cluster_code_versions_result"})

    		ubx.Data(servicefabric.ClusterCodeVersionsResult, "example", servicefabric.ClusterCodeVersionsResultConfig{
    			ApiVersion:     "example",
    			Location:       "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ClusterCodeVersionsResult } from "./local-sdk/azure/sdk/typescript/azure/data/servicefabric/cluster_code_versions_result.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_servicefabric_cluster_code_versions_result",
      });

      data(ClusterCodeVersionsResult, "example", {
        apiVersion: "example",
        location: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.servicefabric import ClusterCodeVersionsResult, ClusterCodeVersionsResultConfig

    def describe():
        ubx.intent("look up azure_servicefabric_cluster_code_versions_result")

        ubx.data(ClusterCodeVersionsResult, "example", ClusterCodeVersionsResultConfig(
            api_version="example",
            location="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="location" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to use for getting the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
