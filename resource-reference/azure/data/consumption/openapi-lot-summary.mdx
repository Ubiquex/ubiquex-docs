---
title: "azure_consumption_openapi_lot_summary"
description: "`azure_consumption_openapi_lot_summary` reads a real, individual credit or benefit lot applied against a billing account's own charges."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_lot_summary"})

    		ubx.Data(consumption.OpenapiLotSummary, "example", consumption.OpenapiLotSummaryConfig{
    			BillingAccountId: "example",
    			BillingProfileId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiLotSummary } from "@ubx/sdk-azure/azure/data/consumption/openapi_lot_summary";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_lot_summary" });

      data(OpenapiLotSummary, "example", {
        billingAccountId: "example",
        billingProfileId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiLotSummary, OpenapiLotSummaryConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_lot_summary")

        ubx.data(OpenapiLotSummary, "example", OpenapiLotSummaryConfig(
            billing_account_id="example",
            billing_profile_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_id" type="string" required>
  The real, unique identifier of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_id" type="string" required>
  The real, unique identifier of the billing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results if there are any. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of lot summary. Returned by the lookup.
</ResponseField>
