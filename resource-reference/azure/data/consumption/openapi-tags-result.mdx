---
title: "azure_consumption_openapi_tags_result"
description: "`azure_consumption_openapi_tags_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_consumption_openapi_tags_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_tags_result"})

    		ubx.Data(consumption.OpenapiTagsResult, "example", consumption.OpenapiTagsResultConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiTagsResult } from "@ubx/sdk-azure/azure/data/consumption/openapi_tags_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_tags_result" });

      data(OpenapiTagsResult, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiTagsResult, OpenapiTagsResultConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_tags_result")

        ubx.data(OpenapiTagsResult, "example", OpenapiTagsResultConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="e_tag" type="string">
  eTag of the resource. To handle concurrent update scenario, this field will be used to determine whether the user is updating the latest version or not. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the tag. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="next_link" type="string">
      The link (url) to the next page of results. Returned by the lookup.
    </ResponseField>
    <ResponseField name="previous_link" type="string">
      The link (url) to the previous page of results. Returned by the lookup.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      A list of Tag. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
