---
title: "azure_consumption_openapi_reservation_recommendation_details_model"
description: "`azure_consumption_openapi_reservation_recommendation_details_model` reads real, detailed data backing a system-generated Azure Reservation purchase..."
---

`azure_consumption_openapi_reservation_recommendation_details_model` reads real, detailed data backing a system-generated Azure Reservation purchase recommendation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_reservation_recommendation_details_model"})

    		ubx.Data(consumption.OpenapiReservationRecommendationDetailsModel, "example", consumption.OpenapiReservationRecommendationDetailsModelConfig{
    			LookBackPeriod:    "example",
    			Product:           "example",
    			Region:            "example",
    			ResourceScope:     "example",
    			Scope:             "example",
    			Term:              "example",
    			ManagementGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiReservationRecommendationDetailsModel } from "./local-sdk/azure/sdk/typescript/azure/data/consumption/openapi_reservation_recommendation_details_model.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_consumption_openapi_reservation_recommendation_details_model",
      });

      data(OpenapiReservationRecommendationDetailsModel, "example", {
        lookBackPeriod: "example",
        product: "example",
        region: "example",
        resourceScope: "example",
        scope: "example",
        term: "example",
        managementGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiReservationRecommendationDetailsModel, OpenapiReservationRecommendationDetailsModelConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_reservation_recommendation_details_model")

        ubx.data(OpenapiReservationRecommendationDetailsModel, "example", OpenapiReservationRecommendationDetailsModelConfig(
            look_back_period="example",
            product="example",
            region="example",
            resource_scope="example",
            scope="example",
            term="example",
            management_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="look_back_period" type="string" required>
  The real time window results are computed over, looking back from now. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="management_group_id" type="string">
  The real, unique identifier of the management group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  The real product this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="region" type="string" required>
  The real Azure region this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_scope" type="string" required>
  The real Azure Resource Manager scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="term" type="string" required>
  The real search term this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  The etag for the resource. Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  Resource Location. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the reservation recommendation. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="currency" type="string">
      An ISO 4217 currency code identifier for the costs and savings Returned by the lookup.
    </ResponseField>
    <ResponseField name="management_group_id" type="string">
      Management group id associated with management group scoped recommendation. Returned by the lookup.
    </ResponseField>
    <ResponseField name="projected_usage" type="object">
      Details about projected usage data that has been used for computing the recommendation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_retail_usage_in_cus" type="number">
          Total projected retail usage in commitment units (CUs). Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource" type="object">
      Details of the resource. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="applied_scopes" type="list(string)">
          List of subscriptions for which the reservation is applied. Returned by the lookup.
        </ResponseField>
        <ResponseField name="on_demand_rate" type="number">
          On-demand rate of the resource. Most resources use hourly rates, except for BlockBlob, ManagedDisk, Backup, and Azure Files, which use monthly rates. Only hardware rates are included; software rates are excluded. Note: there could be new resources that use hourly rates in the future. Returned by the lookup.
        </ResponseField>
        <ResponseField name="product" type="string">
          Azure product ex: Standard_E8s_v3 etc. Returned by the lookup.
        </ResponseField>
        <ResponseField name="region" type="string">
          Azure resource region ex:EastUS, WestUS etc. Returned by the lookup.
        </ResponseField>
        <ResponseField name="reservation_rate" type="number">
          Hourly reservation rate of the resource. Varies based on the term. Returned by the lookup.
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          The azure resource type. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_group" type="string">
      Resource Group. Returned by the lookup.
    </ResponseField>
    <ResponseField name="savings" type="object">
      Details of the estimated savings. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="calculated_savings" type="list(object)">
          List of calculated savings. Optional lookup argument.
        </ResponseField>
        <ResponseField name="look_back_period" type="number">
          Number of days of usage to look back used for computing the recommendation. Returned by the lookup.
        </ResponseField>
        <ResponseField name="recommended_quantity" type="number">
          Number of recommended units of the resource. Returned by the lookup.
        </ResponseField>
        <ResponseField name="reservation_order_term" type="string">
          Term period of the reservation. ex: P1M, P1Y or P3Y. Returned by the lookup.
        </ResponseField>
        <ResponseField name="savings_type" type="string">
          Type of savings, ex: instance. Returned by the lookup.
        </ResponseField>
        <ResponseField name="unit_of_measure" type="string">
          Measurement unit ex: hour etc. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope" type="string">
      Scope of the reservation, ex: Single or Shared. Returned by the lookup.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID associated with the management group. Populated only when managementGroupId is populated. Returned by the lookup.
    </ResponseField>
    <ResponseField name="usage" type="object">
      Details about historical usage data that has been used for computing the recommendation. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_consumption_date" type="string">
          The first usage date used for looking back for computing the recommendation. Returned by the lookup.
        </ResponseField>
        <ResponseField name="last_consumption_date" type="string">
          The last usage date used for looking back for computing the recommendation. Returned by the lookup.
        </ResponseField>
        <ResponseField name="look_back_unit_type" type="string">
          What the usage data values represent ex: virtual machine instance. Returned by the lookup.
        </ResponseField>
        <ResponseField name="usage_data" type="list(number)">
          The breakdown of historical resource usage. The values are in the order of usage between the firstConsumptionDate and the lastConsumptionDate. Returned by the lookup.
        </ResponseField>
        <ResponseField name="usage_grain" type="string">
          The grain of the values represented in the usage data ex: hourly. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="string">
  Resource sku Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Returned by the lookup.
</ResponseField>
