---
title: "azure_consumption_openapi_charge_summary"
description: "`azure_consumption_openapi_charge_summary` reads a real, aggregated summary of charges against a billing scope."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_charge_summary"})

    		ubx.Data(consumption.OpenapiChargeSummary, "example", consumption.OpenapiChargeSummaryConfig{
    			Scope:     "example",
    			EndDate:   "example",
    			StartDate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiChargeSummary } from "./local-sdk/azure/sdk/typescript/azure/data/consumption/openapi_charge_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_charge_summary" });

      data(OpenapiChargeSummary, "example", {
        scope: "example",
        endDate: "example",
        startDate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiChargeSummary, OpenapiChargeSummaryConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_charge_summary")

        ubx.data(OpenapiChargeSummary, "example", OpenapiChargeSummaryConfig(
            scope="example",
            end_date="example",
            start_date="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_date" type="string">
  The latest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The list of charge summary Returned by the lookup.
</ResponseField>
