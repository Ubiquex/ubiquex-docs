---
title: "azure_consumption_openapi_price_sheet_result"
description: "`azure_consumption_openapi_price_sheet_result` reads the real, negotiated price sheet applicable to a billing scope."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_price_sheet_result"})

    		ubx.Data(consumption.OpenapiPriceSheetResult, "example", consumption.OpenapiPriceSheetResultConfig{
    			BillingPeriodName: "example-billing-period-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiPriceSheetResult } from "@ubx/sdk-azure/azure/data/consumption/openapi_price_sheet_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_price_sheet_result" });

      data(OpenapiPriceSheetResult, "example", {
        billingPeriodName: "example-billing-period-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiPriceSheetResult, OpenapiPriceSheetResultConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_price_sheet_result")

        ubx.data(OpenapiPriceSheetResult, "example", OpenapiPriceSheetResultConfig(
            billing_period_name="example-billing-period-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_period_name" type="string" required>
  The real name of the billing period this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  eTag of the resource. To handle concurrent update scenario, this field will be used to determine whether the user is updating the latest version or not. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  price sheet result. It contains the pricesheet associated with billing period Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="download" type="object">
      The properties of the meter detail. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="meter_category" type="string">
          The category of the meter, for example, 'Cloud services', 'Networking', etc.. Returned by the lookup.
        </ResponseField>
        <ResponseField name="meter_location" type="string">
          The location in which the Azure service is available. Returned by the lookup.
        </ResponseField>
        <ResponseField name="meter_name" type="string">
          The name of the meter, within the given meter category Returned by the lookup.
        </ResponseField>
        <ResponseField name="meter_sub_category" type="string">
          The subcategory of the meter, for example, 'A6 Cloud services', 'ExpressRoute (IXP)', etc.. Returned by the lookup.
        </ResponseField>
        <ResponseField name="pretax_standard_rate" type="number">
          The pretax listing price. Returned by the lookup.
        </ResponseField>
        <ResponseField name="service_name" type="string">
          The name of the service. Returned by the lookup.
        </ResponseField>
        <ResponseField name="service_tier" type="string">
          The service tier. Returned by the lookup.
        </ResponseField>
        <ResponseField name="total_included_quantity" type="number">
          The total included quantity associated with the offer. Returned by the lookup.
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit in which the meter consumption is charged, for example, 'Hours', 'GB', etc. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="next_link" type="string">
      The link (url) to the next page of results. Returned by the lookup.
    </ResponseField>
    <ResponseField name="pricesheets" type="list(object)">
      Price sheet Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Returned by the lookup.
</ResponseField>
