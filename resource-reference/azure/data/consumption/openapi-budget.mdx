---
title: "azure_consumption_openapi_budget"
description: "`data_azure_consumption_openapi_budget` looks up a real, already-existing `azure_consumption_openapi_budget`."
---

It sets a spending threshold on a scope and triggers notification(s) when actual or forecasted spend crosses it, via the Consumption API's own budget resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_budget"})

    		ubx.Data(consumption.OpenapiBudget, "example", consumption.OpenapiBudgetConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiBudget } from "./local-sdk/azure/sdk/typescript/azure/data/consumption/openapi_budget.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_budget" });

      data(OpenapiBudget, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiBudget, OpenapiBudgetConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_budget")

        ubx.data(OpenapiBudget, "example", OpenapiBudgetConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  The real Azure Resource Manager scope (subscription, resource group, or resource) this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of budgets. Returned by the lookup.
</ResponseField>
