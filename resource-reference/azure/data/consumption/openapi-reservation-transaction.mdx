---
title: "azure_consumption_openapi_reservation_transaction"
description: "`data_azure_consumption_openapi_reservation_transaction` is a real, live lookup against Microsoft Azure..."
---

`data_azure_consumption_openapi_reservation_transaction` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_reservation_transaction"})

    		ubx.Data(consumption.OpenapiReservationTransaction, "example", consumption.OpenapiReservationTransactionConfig{
    			BillingAccountId:        "example",
    			PreviewMarkupPercentage: 1,
    			UseMarkupIfPartner:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiReservationTransaction } from "@ubx/sdk-azure/azure/data/consumption/openapi_reservation_transaction";

    export default stack("example", () => {
      intent({
        summary: "look up azure_consumption_openapi_reservation_transaction",
      });

      data(OpenapiReservationTransaction, "example", {
        billingAccountId: "example",
        previewMarkupPercentage: 1,
        useMarkupIfPartner: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiReservationTransaction, OpenapiReservationTransactionConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_reservation_transaction")

        ubx.data(OpenapiReservationTransaction, "example", OpenapiReservationTransactionConfig(
            billing_account_id="example",
            preview_markup_percentage=1,
            use_markup_if_partner=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="preview_markup_percentage" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="use_markup_if_partner" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of reservation recommendations. Returned by the lookup.
</ResponseField>
