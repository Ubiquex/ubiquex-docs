---
title: "azure_consumption_openapi_reservation_detail"
description: "`azure_consumption_openapi_reservation_detail` reads real, detailed usage data for an Azure Reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_reservation_detail"})

    		ubx.Data(consumption.OpenapiReservationDetail, "example", consumption.OpenapiReservationDetailConfig{
    			ReservationOrderId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiReservationDetail } from "@ubx/sdk-azure/azure/data/consumption/openapi_reservation_detail";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_reservation_detail" });

      data(OpenapiReservationDetail, "example", {
        reservationOrderId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiReservationDetail, OpenapiReservationDetailConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_reservation_detail")

        ubx.data(OpenapiReservationDetail, "example", OpenapiReservationDetailConfig(
            reservation_order_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="reservation_order_id" type="string" required>
  The real, unique identifier of the reservation order this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of reservation details. Returned by the lookup.
</ResponseField>
