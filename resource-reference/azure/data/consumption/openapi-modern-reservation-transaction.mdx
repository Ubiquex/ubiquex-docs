---
title: "azure_consumption_openapi_modern_reservation_transaction"
description: "`azure_consumption_openapi_modern_reservation_transaction` looks up a real reservation transaction (purchase, exchange..."
---

`azure_consumption_openapi_modern_reservation_transaction` looks up a real reservation transaction (purchase, exchange, refund) on a Microsoft Customer Agreement billing account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_modern_reservation_transaction"})

    		ubx.Data(consumption.OpenapiModernReservationTransaction, "example", consumption.OpenapiModernReservationTransactionConfig{
    			BillingAccountId: "example",
    			BillingProfileId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiModernReservationTransaction } from "./local-sdk/azure/sdk/typescript/azure/data/consumption/openapi_modern_reservation_transaction.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_consumption_openapi_modern_reservation_transaction",
      });

      data(OpenapiModernReservationTransaction, "example", {
        billingAccountId: "example",
        billingProfileId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiModernReservationTransaction, OpenapiModernReservationTransactionConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_modern_reservation_transaction")

        ubx.data(OpenapiModernReservationTransaction, "example", OpenapiModernReservationTransactionConfig(
            billing_account_id="example",
            billing_profile_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_id" type="string" required>
  The real, unique identifier of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_id" type="string" required>
  The real, unique identifier of the billing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of reservation recommendations. Returned by the lookup.
</ResponseField>
