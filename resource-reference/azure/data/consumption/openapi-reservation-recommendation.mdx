---
title: "azure_consumption_openapi_reservation_recommendation"
description: "`azure_consumption_openapi_reservation_recommendation` reads a real, system-generated recommendation for purchasing an Azure Reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_reservation_recommendation"})

    		ubx.Data(consumption.OpenapiReservationRecommendation, "example", consumption.OpenapiReservationRecommendationConfig{
    			ResourceScope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiReservationRecommendation } from "./local-sdk/azure/sdk/typescript/azure/data/consumption/openapi_reservation_recommendation.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_consumption_openapi_reservation_recommendation",
      });

      data(OpenapiReservationRecommendation, "example", {
        resourceScope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiReservationRecommendation, OpenapiReservationRecommendationConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_reservation_recommendation")

        ubx.data(OpenapiReservationRecommendation, "example", OpenapiReservationRecommendationConfig(
            resource_scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_scope" type="string" required>
  The real Azure Resource Manager scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link (url) to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="previous_link" type="string">
  The link (url) to the previous page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The list of reservation recommendations. Returned by the lookup.
</ResponseField>
