---
title: "azure_consumption_openapi_charges_list_result"
description: "`azure_consumption_openapi_charges_list_result` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_consumption_openapi_charges_list_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_charges_list_result"})

    		ubx.Data(consumption.OpenapiChargesListResult, "example", consumption.OpenapiChargesListResultConfig{
    			Scope:     "example",
    			EndDate:   "example",
    			StartDate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiChargesListResult } from "@ubx/sdk-azure/azure/data/consumption/openapi_charges_list_result";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_charges_list_result" });

      data(OpenapiChargesListResult, "example", {
        scope: "example",
        endDate: "example",
        startDate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiChargesListResult, OpenapiChargesListResultConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_charges_list_result")

        ubx.data(OpenapiChargesListResult, "example", OpenapiChargesListResultConfig(
            scope="example",
            end_date="example",
            start_date="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_date" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The list of charge summary Returned by the lookup.
</ResponseField>
