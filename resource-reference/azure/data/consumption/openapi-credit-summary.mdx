---
title: "azure_consumption_openapi_credit_summary"
description: "`azure_consumption_openapi_credit_summary` reads a real, aggregated summary of Azure credits available and consumed on a billing account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	consumption "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/consumption"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_consumption_openapi_credit_summary"})

    		ubx.Data(consumption.OpenapiCreditSummary, "example", consumption.OpenapiCreditSummaryConfig{
    			BillingAccountId: "example",
    			BillingProfileId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiCreditSummary } from "@ubx/sdk-azure/azure/data/consumption/openapi_credit_summary";

    export default stack("example", () => {
      intent({ summary: "look up azure_consumption_openapi_credit_summary" });

      data(OpenapiCreditSummary, "example", {
        billingAccountId: "example",
        billingProfileId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.consumption import OpenapiCreditSummary, OpenapiCreditSummaryConfig

    def describe():
        ubx.intent("look up azure_consumption_openapi_credit_summary")

        ubx.data(OpenapiCreditSummary, "example", OpenapiCreditSummaryConfig(
            billing_account_id="example",
            billing_profile_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_account_id" type="string" required>
  The real, unique identifier of the billing account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_profile_id" type="string" required>
  The real, unique identifier of the billing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="e_tag" type="string">
  eTag of the resource. To handle concurrent update scenario, this field will be used to determine whether the user is updating the latest version or not. Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the credit summary. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="balance_summary" type="object">
      Summary of credit balances. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="current_balance" type="object">
          The amount plus currency . Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="currency" type="string">
              Amount currency. Returned by the lookup.
            </ResponseField>
            <ResponseField name="value" type="number">
              Amount. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="estimated_balance" type="object">
          The amount plus currency . Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="currency" type="string">
              Amount currency. Returned by the lookup.
            </ResponseField>
            <ResponseField name="value" type="number">
              Amount. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="estimated_balance_in_billing_currency" type="object">
          The amount with exchange rate. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="exchange_rate" type="number">
              The exchange rate. Returned by the lookup.
            </ResponseField>
            <ResponseField name="exchange_rate_month" type="number">
              The exchange rate month. Returned by the lookup.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="billing_currency" type="string">
      The billing currency. Returned by the lookup.
    </ResponseField>
    <ResponseField name="credit_currency" type="string">
      The credit currency. Returned by the lookup.
    </ResponseField>
    <ResponseField name="e_tag" type="string">
      The eTag for the resource. Returned by the lookup.
    </ResponseField>
    <ResponseField name="expired_credit" type="object">
      The amount plus currency . Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="currency" type="string">
          Amount currency. Returned by the lookup.
        </ResponseField>
        <ResponseField name="value" type="number">
          Amount. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_estimated_balance" type="bool">
      If true, the listed details are based on an estimation and it will be subjected to change. Returned by the lookup.
    </ResponseField>
    <ResponseField name="pending_credit_adjustments" type="object">
      The amount plus currency . Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="currency" type="string">
          Amount currency. Returned by the lookup.
        </ResponseField>
        <ResponseField name="value" type="number">
          Amount. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pending_eligible_charges" type="object">
      The amount plus currency . Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="currency" type="string">
          Amount currency. Returned by the lookup.
        </ResponseField>
        <ResponseField name="value" type="number">
          Amount. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reseller" type="object">
      The reseller properties. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="reseller_description" type="string">
          The reseller property description. Returned by the lookup.
        </ResponseField>
        <ResponseField name="reseller_id" type="string">
          The reseller property ID. Returned by the lookup.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  A list of Tag. Returned by the lookup.
</ResponseField>
