---
title: "azure_signalr_private_link_resource"
description: "`azure_signalr_private_link_resource` reads a real Private Link resource exposed by a SignalR Service resource, that a Private Endpoint can connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_signalr_private_link_resource"})

    		ubx.Data(signalr.PrivateLinkResource, "example", signalr.PrivateLinkResourceConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrivateLinkResource } from "@ubx/sdk-azure/azure/data/signalr/private_link_resource";

    export default stack("example", () => {
      intent({ summary: "look up azure_signalr_private_link_resource" });

      data(PrivateLinkResource, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.signalr import PrivateLinkResource, PrivateLinkResourceConfig

    def describe():
        ubx.intent("look up azure_signalr_private_link_resource")

        ubx.data(PrivateLinkResource, "example", PrivateLinkResourceConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL the client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of PrivateLinkResource Returned by the lookup.
</ResponseField>
