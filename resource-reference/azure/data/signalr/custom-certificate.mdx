---
title: "azure_signalr_custom_certificate"
description: "`data_azure_signalr_custom_certificate` looks up a real, already-existing `azure_signalr_custom_certificate`."
---

It uploads a TLS certificate (referenced from Key Vault) to a SignalR Service resource, bindable to a custom domain.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_signalr_custom_certificate"})

    		ubx.Data(signalr.CustomCertificate, "example", signalr.CustomCertificateConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomCertificate } from "@ubx/sdk-azure/azure/data/signalr/custom_certificate";

    export default stack("example", () => {
      intent({ summary: "look up azure_signalr_custom_certificate" });

      data(CustomCertificate, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.signalr import CustomCertificate, CustomCertificateConfig

    def describe():
        ubx.intent("look up azure_signalr_custom_certificate")

        ubx.data(CustomCertificate, "example", CustomCertificateConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL the client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of custom certificates of this resource. Returned by the lookup.
</ResponseField>
