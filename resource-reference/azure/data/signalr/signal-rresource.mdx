---
title: "azure_signalr_signal_rresource"
description: "`data_azure_signalr_signal_rresource` looks up a real, already-existing `azure_signalr_signal_rresource`."
---

It is a SignalR Service resource, a fully managed service for adding real-time WebSocket-based functionality (chat, notifications, live dashboards) to applications.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_signalr_signal_rresource"})

    		ubx.Data(signalr.SignalRresource, "example", signalr.SignalRresourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { SignalRresource } from "./local-sdk/azure/sdk/typescript/azure/data/signalr/signal_rresource.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_signalr_signal_rresource" });

      data(SignalRresource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.signalr import SignalRresource, SignalRresourceConfig

    def describe():
        ubx.intent("look up azure_signalr_signal_rresource")

        ubx.data(SignalRresource, "example", SignalRresourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URL the client should use to fetch the next page (per server side paging). It's null for now, added for future use. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of the resources Returned by the lookup.
</ResponseField>
