---
title: "azure_applicationinsights_webtests_web_test"
description: "`azure_applicationinsights_webtests_web_test` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_applicationinsights_webtests_web_test` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_applicationinsights_webtests_web_test"})

    		ubx.Data(applicationinsights.WebtestsWebTest, "example", applicationinsights.WebtestsWebTestConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WebtestsWebTest } from "@ubx/sdk-azure/azure/data/applicationinsights/webtests_web_test_";

    export default stack("example", () => {
      intent({ summary: "look up azure_applicationinsights_webtests_web_test" });

      data(WebtestsWebTest, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.applicationinsights import WebtestsWebTest, WebtestsWebTestConfig

    def describe():
        ubx.intent("look up azure_applicationinsights_webtests_web_test")

        ubx.data(WebtestsWebTest, "example", WebtestsWebTestConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to get the next part of the returned list of web tests, should the return set be too large for a single request. May be null. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Set of Application Insights web test definitions. Returned by the lookup.
</ResponseField>
