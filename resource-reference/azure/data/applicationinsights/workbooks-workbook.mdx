---
title: "azure_applicationinsights_workbooks_workbook"
description: "`azure_applicationinsights_workbooks_workbook` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_applicationinsights_workbooks_workbook` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_applicationinsights_workbooks_workbook"})

    		ubx.Data(applicationinsights.WorkbooksWorkbook, "example", applicationinsights.WorkbooksWorkbookConfig{
    			Category:        "example",
    			CanFetchContent: true,
    			Tags:            []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkbooksWorkbook } from "@ubx/sdk-azure/azure/data/applicationinsights/workbooks_workbook";

    export default stack("example", () => {
      intent({ summary: "look up azure_applicationinsights_workbooks_workbook" });

      data(WorkbooksWorkbook, "example", {
        category: "example",
        canFetchContent: true,
        tags: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.applicationinsights import WorkbooksWorkbook, WorkbooksWorkbookConfig

    def describe():
        ubx.intent("look up azure_applicationinsights_workbooks_workbook")

        ubx.data(WorkbooksWorkbook, "example", WorkbooksWorkbookConfig(
            category="example",
            can_fetch_content=True,
            tags=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="can_fetch_content" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="category" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of workbooks. Returned by the lookup.
</ResponseField>
