---
title: "azure_applicationinsights_workbooks_workbook"
description: "`data_azure_applicationinsights_workbooks_workbook` looks up a real, already-existing `azure_applicationinsights_workbooks_workbook`."
---

It is an Azure Monitor workbook, an interactive report combining text, metrics, logs, and parameters into a single canvas for analysis and dashboards.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_applicationinsights_workbooks_workbook"})

    		ubx.Data(applicationinsights.WorkbooksWorkbook, "example", applicationinsights.WorkbooksWorkbookConfig{
    			Category:        "example",
    			CanFetchContent: true,
    			Tags:            []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { WorkbooksWorkbook } from "./local-sdk/azure/sdk/typescript/azure/data/applicationinsights/workbooks_workbook.ts";

    export default stack("example", () => {
      intent({ summary: "look up azure_applicationinsights_workbooks_workbook" });

      data(WorkbooksWorkbook, "example", {
        category: "example",
        canFetchContent: true,
        tags: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.applicationinsights import WorkbooksWorkbook, WorkbooksWorkbookConfig

    def describe():
        ubx.intent("look up azure_applicationinsights_workbooks_workbook")

        ubx.data(WorkbooksWorkbook, "example", WorkbooksWorkbookConfig(
            category="example",
            can_fetch_content=True,
            tags=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="can_fetch_content" type="bool">
  Whether the real content of this object can currently be fetched. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="category" type="string" required>
  The real category this object belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="list(string)">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  An array of workbooks. Returned by the lookup.
</ResponseField>
