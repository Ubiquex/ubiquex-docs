---
title: "azure_applicationinsights_components_application_insights_component"
description: "`data_azure_applicationinsights_components_application_insights_component` looks up a real, already-existing..."
---

`data_azure_applicationinsights_components_application_insights_component` looks up a real, already-existing `azure_applicationinsights_components_application_insights_component`. It is an Application Insights component, the top-level resource that collects and stores an application's own telemetry (requests, dependencies, exceptions, custom events) for monitoring and diagnostics.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_applicationinsights_components_application_insights_component"})

    		ubx.Data(applicationinsights.ComponentsApplicationInsightsComponent, "example", applicationinsights.ComponentsApplicationInsightsComponentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { ComponentsApplicationInsightsComponent } from "./local-sdk/azure/sdk/typescript/azure/data/applicationinsights/components_application_insights_component.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_applicationinsights_components_application_insights_component",
      });

      data(ComponentsApplicationInsightsComponent, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.applicationinsights import ComponentsApplicationInsightsComponent, ComponentsApplicationInsightsComponentConfig

    def describe():
        ubx.intent("look up azure_applicationinsights_components_application_insights_component")

        ubx.data(ComponentsApplicationInsightsComponent, "example", ComponentsApplicationInsightsComponentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The URI to get the next set of Application Insights component definitions if too many components where returned in the result set. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of Application Insights component definitions. Returned by the lookup.
</ResponseField>
