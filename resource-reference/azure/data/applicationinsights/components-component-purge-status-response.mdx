---
title: "azure_applicationinsights_components_component_purge_status_response"
description: "`azure_applicationinsights_components_component_purge_status_response` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_applicationinsights_components_component_purge_status_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_applicationinsights_components_component_purge_status_response"})

    		ubx.Data(applicationinsights.ComponentsComponentPurgeStatusResponse, "example", applicationinsights.ComponentsComponentPurgeStatusResponseConfig{
    			PurgeId:      "example",
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComponentsComponentPurgeStatusResponse } from "@ubx/sdk-azure/azure/data/applicationinsights/components_component_purge_status_response";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_applicationinsights_components_component_purge_status_response",
      });

      data(ComponentsComponentPurgeStatusResponse, "example", {
        purgeId: "example",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.applicationinsights import ComponentsComponentPurgeStatusResponse, ComponentsComponentPurgeStatusResponseConfig

    def describe():
        ubx.intent("look up azure_applicationinsights_components_component_purge_status_response")

        ubx.data(ComponentsComponentPurgeStatusResponse, "example", ComponentsComponentPurgeStatusResponseConfig(
            purge_id="example",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="purge_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  Status of the operation represented by the requested Id. Returned by the lookup.
</ResponseField>
