---
title: "azure_digitaltwins_digital_twins_endpoint_resource"
description: "`data_azure_digitaltwins_digital_twins_endpoint_resource` looks up a real, already-existing `azure_digitaltwins_digital_twins_endpoint_resource`."
---

It routes matching Azure Digital Twins event(s) (twin changes, telemetry) to an external destination (Event Hub, Event Grid, or Service Bus).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	digitaltwins "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/digitaltwins"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_digitaltwins_digital_twins_endpoint_resource"})

    		ubx.Data(digitaltwins.DigitalTwinsEndpointResource, "example", digitaltwins.DigitalTwinsEndpointResourceConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ResourceName:      "example-resource-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DigitalTwinsEndpointResource } from "@ubx/sdk-azure/azure/data/digitaltwins/digital_twins_endpoint_resource";

    export default stack("example", () => {
      intent({
        summary: "look up azure_digitaltwins_digital_twins_endpoint_resource",
      });

      data(DigitalTwinsEndpointResource, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        resourceName: "example-resource-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.digitaltwins import DigitalTwinsEndpointResource, DigitalTwinsEndpointResourceConfig

    def describe():
        ubx.intent("look up azure_digitaltwins_digital_twins_endpoint_resource")

        ubx.data(DigitalTwinsEndpointResource, "example", DigitalTwinsEndpointResourceConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            resource_name="example-resource-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of DigitalTwinsInstance Endpoints. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A list of DigitalTwinsInstance Endpoints. Returned by the lookup.
</ResponseField>
