---
title: "azure_digitaltwins_digital_twins_description"
description: "`azure_digitaltwins_digital_twins_description` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_digitaltwins_digital_twins_description` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	digitaltwins "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/digitaltwins"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_digitaltwins_digital_twins_description"})

    		ubx.Data(digitaltwins.DigitalTwinsDescription, "example", digitaltwins.DigitalTwinsDescriptionConfig{
    			ApiVersion:     "example",
    			SubscriptionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DigitalTwinsDescription } from "@ubx/sdk-azure/azure/data/digitaltwins/digital_twins_description";

    export default stack("example", () => {
      intent({ summary: "look up azure_digitaltwins_digital_twins_description" });

      data(DigitalTwinsDescription, "example", {
        apiVersion: "example",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.digitaltwins import DigitalTwinsDescription, DigitalTwinsDescriptionConfig

    def describe():
        ubx.intent("look up azure_digitaltwins_digital_twins_description")

        ubx.data(DigitalTwinsDescription, "example", DigitalTwinsDescriptionConfig(
            api_version="example",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of DigitalTwins description objects. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A list of DigitalTwins description objects. Returned by the lookup.
</ResponseField>
