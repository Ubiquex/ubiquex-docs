---
title: "azure_digitaltwins_time_series_database_connection"
description: "`data_azure_digitaltwins_time_series_database_connection` looks up a real, already-existing `azure_digitaltwins_time_series_database_connection`."
---

It continuously streams Azure Digital Twins twin property change(s) into a connected Azure Data Explorer database for historical time-series analysis.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	digitaltwins "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/digitaltwins"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_digitaltwins_time_series_database_connection"})

    		ubx.Data(digitaltwins.TimeSeriesDatabaseConnection, "example", digitaltwins.TimeSeriesDatabaseConnectionConfig{
    			ApiVersion:        "example",
    			ResourceGroupName: "example-resource-group-name",
    			ResourceName:      "example-resource-name",
    			SubscriptionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { TimeSeriesDatabaseConnection } from "./local-sdk/azure/sdk/typescript/azure/data/digitaltwins/time_series_database_connection.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_digitaltwins_time_series_database_connection",
      });

      data(TimeSeriesDatabaseConnection, "example", {
        apiVersion: "example",
        resourceGroupName: "example-resource-group-name",
        resourceName: "example-resource-name",
        subscriptionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.digitaltwins import TimeSeriesDatabaseConnection, TimeSeriesDatabaseConnectionConfig

    def describe():
        ubx.intent("look up azure_digitaltwins_time_series_database_connection")

        ubx.data(TimeSeriesDatabaseConnection, "example", TimeSeriesDatabaseConnectionConfig(
            api_version="example",
            resource_group_name="example-resource-group-name",
            resource_name="example-resource-name",
            subscription_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string" required>
  The real Azure REST API version this request targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  The real, unique identifier of the Azure subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link used to get the next page of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  A list of time series database connection resources. Returned by the lookup.
</ResponseField>
