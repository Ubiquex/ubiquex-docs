---
title: "azure_dashboard_grafana_managed_grafana"
description: "`data_azure_dashboard_grafana_managed_grafana` looks up a real, already-existing `azure_dashboard_grafana_managed_grafana`."
---

It is an Azure Managed Grafana instance, a fully managed deployment of Grafana for visualizing metrics from Azure Monitor and other data sources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dashboard_grafana_managed_grafana"})

    		ubx.Data(dashboard.GrafanaManagedGrafana, "example", dashboard.GrafanaManagedGrafanaConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GrafanaManagedGrafana } from "@ubx/sdk-azure/azure/data/dashboard/grafana_managed_grafana";

    export default stack("example", () => {
      intent({ summary: "look up azure_dashboard_grafana_managed_grafana" });

      data(GrafanaManagedGrafana, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dashboard import GrafanaManagedGrafana, GrafanaManagedGrafanaConfig

    def describe():
        ubx.intent("look up azure_dashboard_grafana_managed_grafana")

        ubx.data(GrafanaManagedGrafana, "example", GrafanaManagedGrafanaConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ManagedGrafana items on this page Returned by the lookup.
</ResponseField>
