---
title: "azure_dashboard_grafana_managed_dashboard"
description: "`data_azure_dashboard_grafana_managed_dashboard` looks up a real, already-existing `azure_dashboard_grafana_managed_dashboard`."
---

It is a Grafana dashboard resource managed and versioned as an Azure resource within an Azure Managed Grafana instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dashboard_grafana_managed_dashboard"})

    		ubx.Data(dashboard.GrafanaManagedDashboard, "example", dashboard.GrafanaManagedDashboardConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GrafanaManagedDashboard } from "@ubx/sdk-azure/azure/data/dashboard/grafana_managed_dashboard";

    export default stack("example", () => {
      intent({ summary: "look up azure_dashboard_grafana_managed_dashboard" });

      data(GrafanaManagedDashboard, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dashboard import GrafanaManagedDashboard, GrafanaManagedDashboardConfig

    def describe():
        ubx.intent("look up azure_dashboard_grafana_managed_dashboard")

        ubx.data(GrafanaManagedDashboard, "example", GrafanaManagedDashboardConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The ManagedDashboard items on this page Returned by the lookup.
</ResponseField>
