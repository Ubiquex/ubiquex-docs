---
title: "azure_dashboard_grafana_microsoft_dashboard"
description: "`azure_dashboard_grafana_microsoft_dashboard` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_dashboard_grafana_microsoft_dashboard` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dashboard_grafana_microsoft_dashboard"})

    		ubx.Data(dashboard.GrafanaMicrosoftDashboard, "example", dashboard.GrafanaMicrosoftDashboardConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GrafanaMicrosoftDashboard } from "@ubx/sdk-azure/azure/data/dashboard/grafana_microsoft_dashboard";

    export default stack("example", () => {
      intent({ summary: "look up azure_dashboard_grafana_microsoft_dashboard" });

      data(GrafanaMicrosoftDashboard, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dashboard import GrafanaMicrosoftDashboard, GrafanaMicrosoftDashboardConfig

    def describe():
        ubx.intent("look up azure_dashboard_grafana_microsoft_dashboard")

        ubx.data(GrafanaMicrosoftDashboard, "example", GrafanaMicrosoftDashboardConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  URL to get the next set of operation list results (if there are any). Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  List of operations supported by the resource provider Returned by the lookup.
</ResponseField>
