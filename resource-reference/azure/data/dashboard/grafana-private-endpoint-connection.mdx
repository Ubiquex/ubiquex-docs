---
title: "azure_dashboard_grafana_private_endpoint_connection"
description: "`data_azure_dashboard_grafana_private_endpoint_connection` looks up a real, already-existing `azure_dashboard_grafana_private_endpoint_connection`."
---

It represents the connection between a Private Endpoint and an Azure Managed Grafana instance, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_dashboard_grafana_private_endpoint_connection"})

    		ubx.Data(dashboard.GrafanaPrivateEndpointConnection, "example", dashboard.GrafanaPrivateEndpointConnectionConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GrafanaPrivateEndpointConnection } from "@ubx/sdk-azure/azure/data/dashboard/grafana_private_endpoint_connection";

    export default stack("example", () => {
      intent({
        summary: "look up azure_dashboard_grafana_private_endpoint_connection",
      });

      data(GrafanaPrivateEndpointConnection, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.dashboard import GrafanaPrivateEndpointConnection, GrafanaPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("look up azure_dashboard_grafana_private_endpoint_connection")

        ubx.data(GrafanaPrivateEndpointConnection, "example", GrafanaPrivateEndpointConnectionConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The PrivateEndpointConnection items on this page Returned by the lookup.
</ResponseField>
