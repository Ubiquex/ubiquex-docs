---
title: "azure_operationalinsights_openapi_log_analytics_query_pack_query"
description: "`azure_operationalinsights_openapi_log_analytics_query_pack_query` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_operationalinsights_openapi_log_analytics_query_pack_query` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_log_analytics_query_pack_query"})

    		ubx.Data(operationalinsights.OpenapiLogAnalyticsQueryPackQuery, "example", operationalinsights.OpenapiLogAnalyticsQueryPackQueryConfig{
    			QueryPackName: "example-query-pack-name",
    			IncludeBody:   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiLogAnalyticsQueryPackQuery } from "@ubx/sdk-azure/azure/data/operationalinsights/openapi_log_analytics_query_pack_query";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_operationalinsights_openapi_log_analytics_query_pack_query",
      });

      data(OpenapiLogAnalyticsQueryPackQuery, "example", {
        queryPackName: "example-query-pack-name",
        includeBody: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiLogAnalyticsQueryPackQuery, OpenapiLogAnalyticsQueryPackQueryConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_log_analytics_query_pack_query")

        ubx.data(OpenapiLogAnalyticsQueryPackQuery, "example", OpenapiLogAnalyticsQueryPackQueryConfig(
            query_pack_name="example-query-pack-name",
            include_body=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_body" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_pack_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The LogAnalyticsQueryPackQuery items on this page Returned by the lookup.
</ResponseField>
