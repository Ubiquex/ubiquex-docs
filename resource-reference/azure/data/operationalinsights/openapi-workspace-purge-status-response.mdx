---
title: "azure_operationalinsights_openapi_workspace_purge_status_response"
description: "`azure_operationalinsights_openapi_workspace_purge_status_response` reads the real, current status of a data-purge request submitted against a Log..."
---

`azure_operationalinsights_openapi_workspace_purge_status_response` reads the real, current status of a data-purge request submitted against a Log Analytics workspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_workspace_purge_status_response"})

    		ubx.Data(operationalinsights.OpenapiWorkspacePurgeStatusResponse, "example", operationalinsights.OpenapiWorkspacePurgeStatusResponseConfig{
    			PurgeId:       "example",
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiWorkspacePurgeStatusResponse } from "@ubx/sdk-azure/azure/data/operationalinsights/openapi_workspace_purge_status_response";

    export default stack("example", () => {
      intent({
        summary:
          "look up azure_operationalinsights_openapi_workspace_purge_status_response",
      });

      data(OpenapiWorkspacePurgeStatusResponse, "example", {
        purgeId: "example",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiWorkspacePurgeStatusResponse, OpenapiWorkspacePurgeStatusResponseConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_workspace_purge_status_response")

        ubx.data(OpenapiWorkspacePurgeStatusResponse, "example", OpenapiWorkspacePurgeStatusResponseConfig(
            purge_id="example",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="purge_id" type="string" required>
  The real, unique identifier of this data-purge request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  Status of the operation represented by the requested Id. Returned by the lookup.
</ResponseField>
