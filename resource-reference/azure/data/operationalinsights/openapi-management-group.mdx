---
title: "azure_operationalinsights_openapi_management_group"
description: "`azure_operationalinsights_openapi_management_group` reads the real Automation account management group linked to a Log Analytics workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_management_group"})

    		ubx.Data(operationalinsights.OpenapiManagementGroup, "example", operationalinsights.OpenapiManagementGroupConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiManagementGroup } from "@ubx/sdk-azure/azure/data/operationalinsights/openapi_management_group";

    export default stack("example", () => {
      intent({
        summary: "look up azure_operationalinsights_openapi_management_group",
      });

      data(OpenapiManagementGroup, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiManagementGroup, OpenapiManagementGroupConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_management_group")

        ubx.data(OpenapiManagementGroup, "example", OpenapiManagementGroupConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_link" type="string">
  The URL to get the next set of results. Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  Gets or sets a list of management groups attached to the workspace. Returned by the lookup.
</ResponseField>
