---
title: "azure_operationalinsights_openapi_cluster"
description: "`data_azure_operationalinsights_openapi_cluster` looks up a real, already-existing `azure_operationalinsights_openapi_cluster`."
---

It provisions a dedicated Log Analytics cluster, providing customer-managed key encryption and reserved ingestion capacity pricing across attached workspace(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_cluster"})

    		ubx.Data(operationalinsights.OpenapiCluster, "example", operationalinsights.OpenapiClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiCluster } from "@ubx/sdk-azure/azure/data/operationalinsights/openapi_cluster";

    export default stack("example", () => {
      intent({ summary: "look up azure_operationalinsights_openapi_cluster" });

      data(OpenapiCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiCluster, OpenapiClusterConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_cluster")

        ubx.data(OpenapiCluster, "example", OpenapiClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_link" type="string">
  The link to the next page of items Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="list(object)">
  The Cluster items on this page Returned by the lookup.
</ResponseField>
