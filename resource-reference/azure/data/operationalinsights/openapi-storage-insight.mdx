---
title: "azure_operationalinsights_openapi_storage_insight"
description: "`data_azure_operationalinsights_openapi_storage_insight` looks up a real, already-existing `azure_operationalinsights_openapi_storage_insight`."
---

It configures a Log Analytics workspace to ingest metrics and logs from an Azure Storage account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_storage_insight"})

    		ubx.Data(operationalinsights.OpenapiStorageInsight, "example", operationalinsights.OpenapiStorageInsightConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { OpenapiStorageInsight } from "./local-sdk/azure/sdk/typescript/azure/data/operationalinsights/openapi_storage_insight.ts";

    export default stack("example", () => {
      intent({
        summary: "look up azure_operationalinsights_openapi_storage_insight",
      });

      data(OpenapiStorageInsight, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiStorageInsight, OpenapiStorageInsightConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_storage_insight")

        ubx.data(OpenapiStorageInsight, "example", OpenapiStorageInsightConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  A list of storage insight items. Returned by the lookup.
</ResponseField>
