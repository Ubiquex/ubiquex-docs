---
title: "azure_operationalinsights_openapi_saved_search"
description: "`azure_operationalinsights_openapi_saved_search` is a real, live lookup against Microsoft Azure, generated from azure's own real schema -- read-only, never created or destroyed."
---

`azure_operationalinsights_openapi_saved_search` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Microsoft Azure's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	operationalinsights "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/data/operationalinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up azure_operationalinsights_openapi_saved_search"})

    		ubx.Data(operationalinsights.OpenapiSavedSearch, "example", operationalinsights.OpenapiSavedSearchConfig{
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpenapiSavedSearch } from "@ubx/sdk-azure/azure/data/operationalinsights/openapi_saved_search";

    export default stack("example", () => {
      intent({ summary: "look up azure_operationalinsights_openapi_saved_search" });

      data(OpenapiSavedSearch, "example", {
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.data.operationalinsights import OpenapiSavedSearch, OpenapiSavedSearchConfig

    def describe():
        ubx.intent("look up azure_operationalinsights_openapi_saved_search")

        ubx.data(OpenapiSavedSearch, "example", OpenapiSavedSearchConfig(
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="list(object)">
  The array of result values. Returned by the lookup.
</ResponseField>
