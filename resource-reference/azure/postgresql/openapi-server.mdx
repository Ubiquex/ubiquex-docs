---
title: "azure_postgresql_openapi_server"
description: "`azure_postgresql_openapi_server` is an Azure Database for PostgreSQL flexible server, the managed PostgreSQL instance every database, firewall rule..."
---

`azure_postgresql_openapi_server` is an Azure Database for PostgreSQL flexible server, the managed PostgreSQL instance every database, firewall rule, and administrator setting in this reference is scoped underneath.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	postgresql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/postgresql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi server"})

    		ubx.Resource(postgresql.OpenapiServer, "example", postgresql.OpenapiServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiServer } from "./local-sdk/azure/sdk/typescript/azure/postgresql/openapi_server.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi server" });

      resource(OpenapiServer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.postgresql import OpenapiServer, OpenapiServerConfig

    def describe():
        ubx.intent("example own openapi server")

        ubx.resource(OpenapiServer, "example", OpenapiServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_postgresql_openapi_server, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Identities associated with a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      Identifier of the object of the service principal associated to the user assigned managed identity. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Identifier of the tenant of a server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Types of identities associated with a server. Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      Map of user assigned managed identities. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_login" type="string">
      Name of the login designated as the first password based administrator assigned to your instance of PostgreSQL. Must be specified the first time that you enable password based authentication on a server. Once set to a given value, it cannot be changed for the rest of the life of a server. If you disable password based authentication on a server which had it enabled, this password based role isn't deleted. Optional.
    </ResponseField>
    <ResponseField name="administrator_login_password" type="string">
      Password assigned to the administrator login. As long as password authentication is enabled, this password can be changed at any time. Optional.
    </ResponseField>
    <ResponseField name="auth_config" type="object">
      Authentication configuration properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="active_directory_auth" type="string">
          Indicates if the server supports Microsoft Entra authentication. Optional.
        </ResponseField>
        <ResponseField name="password_auth" type="string">
          Indicates if the server supports password based authentication. Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Identifier of the tenant of the delegated resource. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="availability_zone" type="string">
      Availability zone of a server. Optional.
    </ResponseField>
    <ResponseField name="backup" type="object">
      Backup properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="backup_retention_days" type="number">
          Backup retention days for the server. Optional.
        </ResponseField>
        <ResponseField name="earliest_restore_date" type="string">
          Earliest restore point time (ISO8601 format) for a server. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="geo_redundant_backup" type="string">
          Indicates if the server is configured to create geographically redundant backups. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cluster" type="object">
      Cluster properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="cluster_size" type="number">
          Number of nodes assigned to the elastic cluster. Optional.
        </ResponseField>
        <ResponseField name="default_database_name" type="string">
          Default database name for the elastic cluster. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      Creation mode of a new server. Optional.
    </ResponseField>
    <ResponseField name="data_encryption" type="object">
      Data encryption properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="geo_backup_encryption_key_status" type="string">
          Status of key used by a server configured with data encryption based on customer managed key, to encrypt the primary storage associated to the server. Optional.
        </ResponseField>
        <ResponseField name="geo_backup_key_uri" type="string">
          Identifier of the user assigned managed identity used to access the key in Azure Key Vault for data encryption of the geographically redundant storage associated to a server that is configured to support geographically redundant backups. Optional.
        </ResponseField>
        <ResponseField name="geo_backup_user_assigned_identity_id" type="string">
          Identifier of the user assigned managed identity used to access the key in Azure Key Vault for data encryption of the geographically redundant storage associated to a server that is configured to support geographically redundant backups. Optional.
        </ResponseField>
        <ResponseField name="primary_encryption_key_status" type="string">
          Status of key used by a server configured with data encryption based on customer managed key, to encrypt the primary storage associated to the server. Optional.
        </ResponseField>
        <ResponseField name="primary_key_uri" type="string">
          URI of the key in Azure Key Vault used for data encryption of the primary storage associated to a server. Optional.
        </ResponseField>
        <ResponseField name="primary_user_assigned_identity_id" type="string">
          Identifier of the user assigned managed identity used to access the key in Azure Key Vault for data encryption of the primary storage associated to a server. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Data encryption type used by a server. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fully_qualified_domain_name" type="string">
      Fully qualified domain name of a server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="high_availability" type="object">
      High availability properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          High availability mode for a server. Optional.
        </ResponseField>
        <ResponseField name="standby_availability_zone" type="string">
          Availability zone associated to the standby server created when high availability is set to SameZone or ZoneRedundant. Optional.
        </ResponseField>
        <ResponseField name="state" type="string">
          Possible states of the standby server created when high availability is set to SameZone or ZoneRedundant. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="maintenance_window" type="object">
      Maintenance window properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="custom_window" type="string">
          Indicates whether custom window is enabled or disabled. Optional.
        </ResponseField>
        <ResponseField name="day_of_week" type="number">
          Day of the week to be used for maintenance window. Optional.
        </ResponseField>
        <ResponseField name="start_hour" type="number">
          Start hour to be used for maintenance window. Optional.
        </ResponseField>
        <ResponseField name="start_minute" type="number">
          Start minute to be used for maintenance window. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="minor_version" type="string">
      Minor version of PostgreSQL database engine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="network" type="object">
      Network properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="delegated_subnet_resource_id" type="string">
          Resource identifier of the delegated subnet. Required during creation of a new server, in case you want the server to be integrated into your own virtual network. For an update operation, you only have to provide this property if you want to change the value assigned for the private DNS zone. Optional.
        </ResponseField>
        <ResponseField name="private_dns_zone_arm_resource_id" type="string">
          Identifier of the private DNS zone. Required during creation of a new server, in case you want the server to be integrated into your own virtual network. For an update operation, you only have to provide this property if you want to change the value assigned for the private DNS zone. Optional.
        </ResponseField>
        <ResponseField name="public_network_access" type="string">
          Indicates if public network access is enabled or not. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="point_in_time_utc" type="string">
      Creation time (in ISO8601 format) of the backup which you want to restore in the new server. It's required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', or 'ReviveDropped'. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections associated with the specified server. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="properties" type="object">
          Optional; if omitted, computed by Microsoft Azure.
          <Expandable title="properties">
            <ResponseField name="group_ids" type="list(string)">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="private_endpoint" type="object">
              Optional; if omitted, computed by Microsoft Azure.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Optional; if omitted, computed by Microsoft Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="private_link_service_connection_state" type="object">
              Optional; if omitted, computed by Microsoft Azure.
              <Expandable title="properties">
                <ResponseField name="actions_required" type="string">
                  Optional; if omitted, computed by Microsoft Azure.
                </ResponseField>
                <ResponseField name="description" type="string">
                  Optional; if omitted, computed by Microsoft Azure.
                </ResponseField>
                <ResponseField name="status" type="string">
                  Optional; if omitted, computed by Microsoft Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="provisioning_state" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica" type="object">
      Replica properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="capacity" type="number">
          Maximum number of read replicas allowed for a server. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="promote_mode" type="string">
          Type of operation to apply on the read replica. This property is write only. Optional.
        </ResponseField>
        <ResponseField name="promote_option" type="string">
          Data synchronization option to use when processing the operation specified in the promoteMode property. This property is write only. Optional.
        </ResponseField>
        <ResponseField name="replication_state" type="string">
          Indicates the replication state of a read replica. This property is returned only when the target server is a read replica. Optional.
        </ResponseField>
        <ResponseField name="role" type="string">
          Role of the server in a replication set. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_capacity" type="number">
      Maximum number of read replicas allowed for a server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="replication_role" type="string">
      Role of the server in a replication set. Optional.
    </ResponseField>
    <ResponseField name="source_server_resource_id" type="string">
      Identifier of the server to be used as the source of the new server. Required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', 'Replica', or 'ReviveDropped'. This property is returned only when the target server is a read replica. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      State of a server. Optional.
    </ResponseField>
    <ResponseField name="storage" type="object">
      Storage properties of a server. Optional.
      <Expandable title="properties">
        <ResponseField name="auto_grow" type="string">
          Indicates if the server is configured to automatically grow storage size when available space is nearing zero and conditions allow for automatic growing storage size. Optional.
        </ResponseField>
        <ResponseField name="iops" type="number">
          Maximum IOPS supported for storage. Required when type of storage is PremiumV2_LRS or UltraSSD_LRS. Optional.
        </ResponseField>
        <ResponseField name="storage_size_gb" type="number">
          Size of storage assigned to a server. Optional.
        </ResponseField>
        <ResponseField name="throughput" type="number">
          Maximum throughput supported for storage. Required when type of storage is PremiumV2_LRS or UltraSSD_LRS. Optional.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Storage tier of a server. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of storage assigned to a server. If not specified, it defaults to Premium_LRS. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version" type="string">
      Major version of PostgreSQL database engine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Compute information of a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Name by which is known a given compute size assigned to a server. Required.
    </ResponseField>
    <ResponseField name="tier" type="string" required>
      Tier of the compute assigned to a server. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
