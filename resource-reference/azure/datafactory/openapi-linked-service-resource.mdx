---
title: "azure_datafactory_openapi_linked_service_resource"
description: "`azure_datafactory_openapi_linked_service_resource` defines the connection information and credentials a Data Factory uses to connect to an external..."
---

`azure_datafactory_openapi_linked_service_resource` defines the connection information and credentials a Data Factory uses to connect to an external data store or compute service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datafactory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/datafactory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi linked service resource"})

    		ubx.Resource(datafactory.OpenapiLinkedServiceResource, "example", datafactory.OpenapiLinkedServiceResourceConfig{
    			Properties: map[string]any{"type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiLinkedServiceResource } from "@ubx/sdk-azure/azure/datafactory/openapi_linked_service_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi linked service resource" });

      resource(OpenapiLinkedServiceResource, "example", {
        properties: { type: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.datafactory import OpenapiLinkedServiceResource, OpenapiLinkedServiceResourceConfig
    from ubx.azure.datafactory.openapi_linked_service_resource import OpenapiLinkedServiceResource_Properties

    def describe():
        ubx.intent("example own openapi linked service resource")

        ubx.resource(OpenapiLinkedServiceResource, "example", OpenapiLinkedServiceResourceConfig(
            properties=OpenapiLinkedServiceResource_Properties(type="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_datafactory_openapi_linked_service_resource, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The nested object which contains the information and credential which can be used to connect with related store or compute resource. Required.
  <Expandable title="properties">
    <ResponseField name="annotations" type="list(any)">
      List of tags that can be used for describing the linked service. Optional.
    </ResponseField>
    <ResponseField name="connect_via" type="object">
      Integration runtime reference type. Optional.
      <Expandable title="properties">
        <ResponseField name="parameters" type="map(any)">
          Arguments for integration runtime. Optional.
        </ResponseField>
        <ResponseField name="reference_name" type="string" required>
          Reference integration runtime name. Required.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Type of integration runtime. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Linked service description. Optional.
    </ResponseField>
    <ResponseField name="parameters" type="map(object)">
      Parameters for linked service. Optional.
      <Expandable title="properties">
        <ResponseField name="default_value" type="any">
          The value used when this field is left unset. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of linked service. Required.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the linked service. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
