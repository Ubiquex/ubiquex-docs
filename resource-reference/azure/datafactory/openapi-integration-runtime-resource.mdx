---
title: "azure_datafactory_openapi_integration_runtime_resource"
description: "`azure_datafactory_openapi_integration_runtime_resource` is the compute infrastructure (Azure-hosted, self-hosted..."
---

`azure_datafactory_openapi_integration_runtime_resource` is the compute infrastructure (Azure-hosted, self-hosted, or Azure-SSIS) a Data Factory pipeline's own activities actually execute on.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datafactory "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/datafactory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi integration runtime resource"})

    		ubx.Resource(datafactory.OpenapiIntegrationRuntimeResource, "example", datafactory.OpenapiIntegrationRuntimeResourceConfig{
    			Properties: map[string]any{"type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiIntegrationRuntimeResource } from "@ubx/sdk-azure/azure/datafactory/openapi_integration_runtime_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi integration runtime resource" });

      resource(OpenapiIntegrationRuntimeResource, "example", {
        properties: { type: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.datafactory import OpenapiIntegrationRuntimeResource, OpenapiIntegrationRuntimeResourceConfig
    from ubx.azure.datafactory.openapi_integration_runtime_resource import OpenapiIntegrationRuntimeResource_Properties

    def describe():
        ubx.intent("example own openapi integration runtime resource")

        ubx.resource(OpenapiIntegrationRuntimeResource, "example", OpenapiIntegrationRuntimeResourceConfig(
            properties=OpenapiIntegrationRuntimeResource_Properties(type="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_datafactory_openapi_integration_runtime_resource,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  "If etag is provided in the response body, it may also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields.") Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Azure Data Factory nested object which serves as a compute resource for activities. Required.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Integration runtime description. Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of integration runtime. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
