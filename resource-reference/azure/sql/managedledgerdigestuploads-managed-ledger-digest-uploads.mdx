---
title: "azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads"
description: "`azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads` configures Ledger digest uploads for a database hosted on an Azure SQL Managed..."
---

`azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads` configures Ledger digest uploads for a database hosted on an Azure SQL Managed Instance, independently proving data hasn't been tampered with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedledgerdigestuploads managed ledger digest uploads"})

    		ubx.Resource(sql.ManagedledgerdigestuploadsManagedLedgerDigestUploads, "example", sql.ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedledgerdigestuploadsManagedLedgerDigestUploads } from "@ubx/sdk-azure/azure/sql/managedledgerdigestuploads_managed_ledger_digest_uploads";

    export default stack("example", () => {
      intent({
        summary:
          "example own managedledgerdigestuploads managed ledger digest uploads",
      });

      resource(
        ManagedledgerdigestuploadsManagedLedgerDigestUploads,
        "example",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql.managedledgerdigestuploads_managed_ledger_digest_uploads import ManagedledgerdigestuploadsManagedLedgerDigestUploads, ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig

    def describe():
        ubx.intent("example own managedledgerdigestuploads managed ledger digest uploads")

        ubx.resource(ManagedledgerdigestuploadsManagedLedgerDigestUploads, "example", ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a database ledger digest upload settings. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="digest_storage_endpoint" type="string">
      The digest storage endpoint, which must be either an Azure blob storage endpoint or an URI for Azure Confidential Ledger. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Specifies the state of ledger digest upload. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
