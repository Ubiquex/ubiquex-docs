---
title: "azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads"
description: "`azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads` configures Ledger digest uploads for a database hosted on an Azure SQL Managed..."
---

`azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads` configures Ledger digest uploads for a database hosted on an Azure SQL Managed Instance, independently proving data hasn't been tampered with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_managedledgerdigestuploads --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_managedledgerdigestuploads/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_managedledgerdigestuploads/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managedledgerdigestuploads managed ledger digest uploads"})

    		ubx.Resource(sql.ManagedledgerdigestuploadsManagedLedgerDigestUploads, "ci-runner", sql.ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_managedledgerdigestuploads --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedledgerdigestuploadsManagedLedgerDigestUploads } from "./local-sdk/azure_sql_managedledgerdigestuploads/sdk/typescript/azure_sql_managedledgerdigestuploads/sql/managedledgerdigestuploads_managed_ledger_digest_uploads.ts";

    export default stack("payments", () => {
      intent({
        summary:
          "payments own managedledgerdigestuploads managed ledger digest uploads",
      });

      resource(
        ManagedledgerdigestuploadsManagedLedgerDigestUploads,
        "ci-runner",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_managedledgerdigestuploads --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_managedledgerdigestuploads/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_managedledgerdigestuploads.sql import ManagedledgerdigestuploadsManagedLedgerDigestUploads, ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig

    def describe():
        ubx.intent("payments own managedledgerdigestuploads managed ledger digest uploads")

        ubx.resource(ManagedledgerdigestuploadsManagedLedgerDigestUploads, "ci-runner", ManagedledgerdigestuploadsManagedLedgerDigestUploadsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_managedledgerdigestuploads_managed_ledger_digest_uploads,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a database ledger digest upload settings. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="digest_storage_endpoint" type="string">
      The digest storage endpoint, which must be either an Azure blob storage endpoint or an URI for Azure Confidential Ledger. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Specifies the state of ledger digest upload. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
