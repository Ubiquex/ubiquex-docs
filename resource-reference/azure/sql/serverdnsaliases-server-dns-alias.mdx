---
title: "azure_sql_serverdnsaliases_server_dns_alias"
description: "`azure_sql_serverdnsaliases_server_dns_alias` creates an alternate, alias DNS name pointing to an Azure SQL logical server..."
---

`azure_sql_serverdnsaliases_server_dns_alias` creates an alternate, alias DNS name pointing to an Azure SQL logical server, usable to redirect connections during failover without changing client connection strings.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own serverdnsaliases server dns alias"})

    		ubx.Resource(sql.ServerdnsaliasesServerDnsAlias, "example", sql.ServerdnsaliasesServerDnsAliasConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerdnsaliasesServerDnsAlias } from "@ubx/sdk-azure/azure/sql/serverdnsaliases_server_dns_alias";

    export default stack("example", () => {
      intent({ summary: "example own serverdnsaliases server dns alias" });

      resource(ServerdnsaliasesServerDnsAlias, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import ServerdnsaliasesServerDnsAlias, ServerdnsaliasesServerDnsAliasConfig

    def describe():
        ubx.intent("example own serverdnsaliases server dns alias")

        ubx.resource(ServerdnsaliasesServerDnsAlias, "example", ServerdnsaliasesServerDnsAliasConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_serverdnsaliases_server_dns_alias, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties of a server DNS alias. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="azure_dns_record" type="string">
      The fully qualified DNS record for alias Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
