---
title: "azure_sql_serverdnsaliases_server_dns_alias"
description: "`azure_sql_serverdnsaliases_server_dns_alias` creates an alternate, alias DNS name pointing to an Azure SQL logical server..."
---

`azure_sql_serverdnsaliases_server_dns_alias` creates an alternate, alias DNS name pointing to an Azure SQL logical server, usable to redirect connections during failover without changing client connection strings.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_serverdnsaliases --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_serverdnsaliases/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_serverdnsaliases/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own serverdnsaliases server dns alias"})

    		ubx.Resource(sql.ServerdnsaliasesServerDnsAlias, "ci-runner", sql.ServerdnsaliasesServerDnsAliasConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_serverdnsaliases --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerdnsaliasesServerDnsAlias } from "./local-sdk/azure_sql_serverdnsaliases/sdk/typescript/azure_sql_serverdnsaliases/sql/serverdnsaliases_server_dns_alias.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own serverdnsaliases server dns alias" });

      resource(ServerdnsaliasesServerDnsAlias, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_serverdnsaliases --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_serverdnsaliases/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_serverdnsaliases.sql import ServerdnsaliasesServerDnsAlias, ServerdnsaliasesServerDnsAliasConfig

    def describe():
        ubx.intent("payments own serverdnsaliases server dns alias")

        ubx.resource(ServerdnsaliasesServerDnsAlias, "ci-runner", ServerdnsaliasesServerDnsAliasConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_serverdnsaliases_server_dns_alias, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties of a server DNS alias. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="azure_dns_record" type="string">
      The fully qualified DNS record for alias Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
