---
title: "azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy"
description: "`azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy` sets how long point-in-time restore backups are retained for a single..."
---

`azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy` sets how long point-in-time restore backups are retained for a single Azure SQL Database.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own backupshorttermretentionpolicies backup short term retention policy"})

    		ubx.Resource(sql.BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, "example", sql.BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy } from "@ubx/sdk-azure/azure/sql/backupshorttermretentionpolicies_backup_short_term_retention_policy";

    export default stack("example", () => {
      intent({
        summary:
          "example own backupshorttermretentionpolicies backup short term retention policy",
      });

      resource(
        BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy,
        "example",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig

    def describe():
        ubx.intent("example own backupshorttermretentionpolicies backup short term retention policy")

        ubx.resource(BackupshorttermretentionpoliciesBackupShortTermRetentionPolicy, "example", BackupshorttermretentionpoliciesBackupShortTermRetentionPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_backupshorttermretentionpolicies_backup_short_term_retention_policy,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a short term retention policy Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="diff_backup_interval_in_hours" type="number">
      The differential backup interval in hours. This is how many interval hours between each differential backup will be supported. This is only applicable to live databases but not dropped databases. Optional.
    </ResponseField>
    <ResponseField name="retention_days" type="number">
      The backup retention period in days. This is how many days Point-in-Time Restore will be supported. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
