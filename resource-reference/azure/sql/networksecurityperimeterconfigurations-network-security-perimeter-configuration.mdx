---
title: "azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration"
description: "`azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration` associates an Azure SQL resource with a network security..."
---

`azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration` associates an Azure SQL resource with a network security perimeter, restricting its own network access to the perimeter's own boundary rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networksecurityperimeterconfigurations network security perimeter configuration"})

    		ubx.Resource(sql.NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, "example", sql.NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration } from "@ubx/sdk-azure/azure/sql/networksecurityperimeterconfigurations_network_security_perimeter_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "example own networksecurityperimeterconfigurations network security perimeter configuration",
      });

      resource(
        NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration,
        "example",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig

    def describe():
        ubx.intent("example own networksecurityperimeterconfigurations network security perimeter configuration")

        ubx.resource(NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfiguration, "example", NetworksecurityperimeterconfigurationsNetworkSecurityPerimeterConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_networksecurityperimeterconfigurations_network_security_perimeter_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  The properties of an NSP config. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="network_security_perimeter" type="object">
      A reference to the network security perimeter this applies to. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="location" type="string">
          The Azure region this resource is deployed to, e.g. `eastus`. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="perimeter_guid" type="string">
          The globally unique identifier of the associated network security perimeter. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile" type="object">
      A reference to the network security perimeter profile this applies to. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_rules" type="list(object)">
          The access rule(s) making up this network security perimeter profile. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="object">
              The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="access_rules_version" type="string">
          The version of this profile's own access rule set. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_issues" type="list(object)">
      Issue(s) detected while provisioning this network security perimeter association. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="resource_association" type="object">
      The association linking this resource to a network security perimeter profile. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_mode" type="string">
          Whether this network security perimeter is enforced or only auditing (learning mode). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
