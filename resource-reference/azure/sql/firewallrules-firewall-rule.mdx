---
title: "azure_sql_firewallrules_firewall_rule"
description: "`azure_sql_firewallrules_firewall_rule` allows a specific IP address range to connect to an Azure SQL logical server that hasn't been placed inside..."
---

`azure_sql_firewallrules_firewall_rule` allows a specific IP address range to connect to an Azure SQL logical server that hasn't been placed inside a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewallrules firewall rule"})

    		ubx.Resource(sql.FirewallrulesFirewallRule, "example", sql.FirewallrulesFirewallRuleConfig{
    			Values: []map[string]any{{"properties": map[string]any{"end_ip_address": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallrulesFirewallRule } from "@ubx/sdk-azure/azure/sql/firewallrules_firewall_rule";

    export default stack("example", () => {
      intent({ summary: "example own firewallrules firewall rule" });

      resource(FirewallrulesFirewallRule, "example", {
        values: [{ properties: { endIpAddress: "example" } }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql.firewallrules_firewall_rule import FirewallrulesFirewallRule, FirewallrulesFirewallRuleConfig

    def describe():
        ubx.intent("example own firewallrules firewall rule")

        ubx.resource(FirewallrulesFirewallRule, "example", FirewallrulesFirewallRuleConfig(
            values=[{"properties": {"end_ip_address": "example"}}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_firewallrules_firewall_rule, with values a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="values" type="list(object)">
  The firewall rule(s) making up this collection. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="properties" type="object">
      The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_ip_address" type="string">
          The last IP address in the range this firewall rule permits, inclusive. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_ip_address" type="string">
          The first IP address in the range this firewall rule permits, inclusive. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="properties" type="object">
  The properties of a server firewall rule. Computed by Microsoft Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="end_ip_address" type="string">
      The end IP address of the firewall rule. Must be IPv4 format. Must be greater than or equal to startIpAddress. Use value '0.0.0.0' for all Azure-internal IP addresses. Optional.
    </ResponseField>
    <ResponseField name="start_ip_address" type="string">
      The start IP address of the firewall rule. Must be IPv4 format. Use value '0.0.0.0' for all Azure-internal IP addresses. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
