---
title: "azure_sql_syncmembers_sync_member"
description: "`azure_sql_syncmembers_sync_member` is a single database participating in a SQL Data Sync group, either as the hub or a member."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_syncmembers --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_syncmembers/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_syncmembers/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own syncmembers sync member"})

    		ubx.Resource(sql.SyncmembersSyncMember, "ci-runner", sql.SyncmembersSyncMemberConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_syncmembers --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncmembersSyncMember } from "./local-sdk/azure_sql_syncmembers/sdk/typescript/azure_sql_syncmembers/sql/syncmembers_sync_member.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own syncmembers sync member" });

      resource(SyncmembersSyncMember, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_syncmembers --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_syncmembers/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_syncmembers.sql import SyncmembersSyncMember, SyncmembersSyncMemberConfig

    def describe():
        ubx.intent("payments own syncmembers sync member")

        ubx.resource(SyncmembersSyncMember, "ci-runner", SyncmembersSyncMemberConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_syncmembers_sync_member, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a sync member with support to MI. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="database_name" type="string">
      Database name of the member database in the sync member. Optional.
    </ResponseField>
    <ResponseField name="database_type" type="string">
      Type of the sync agent linked database. Optional.
    </ResponseField>
    <ResponseField name="password" type="string">
      Password of the member database in the sync member. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_name" type="string">
      Private endpoint name of the sync member if use private link connection is enabled, for sync members in Azure. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="server_name" type="string">
      Server name of the member database in the sync member Optional.
    </ResponseField>
    <ResponseField name="sql_server_database_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="sync_agent_id" type="string">
      ARM resource id of the sync agent in the sync member. Optional.
    </ResponseField>
    <ResponseField name="sync_direction" type="string">
      Sync direction of the sync member. Optional.
    </ResponseField>
    <ResponseField name="sync_member_azure_database_resource_id" type="string">
      ARM resource id of the sync member logical database, for sync members in Azure. Optional.
    </ResponseField>
    <ResponseField name="sync_state" type="string">
      Sync state of the sync member. Optional.
    </ResponseField>
    <ResponseField name="use_private_link_connection" type="bool">
      Whether to use private link connection. Optional.
    </ResponseField>
    <ResponseField name="user_name" type="string">
      User name of the member database in the sync member. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
