---
title: "azure_sql_virtualnetworkrules_virtual_network_rule"
description: "`azure_sql_virtualnetworkrules_virtual_network_rule` allows a specific virtual network subnet (via VNet service endpoint) to connect to an Azure SQL..."
---

`azure_sql_virtualnetworkrules_virtual_network_rule` allows a specific virtual network subnet (via VNet service endpoint) to connect to an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_virtualnetworkrules --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_virtualnetworkrules/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_virtualnetworkrules/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetworkrules virtual network rule"})

    		ubx.Resource(sql.VirtualnetworkrulesVirtualNetworkRule, "example", sql.VirtualnetworkrulesVirtualNetworkRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_virtualnetworkrules --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkrulesVirtualNetworkRule } from "./local-sdk/azure_sql_virtualnetworkrules/sdk/typescript/azure_sql_virtualnetworkrules/sql/virtualnetworkrules_virtual_network_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetworkrules virtual network rule" });

      resource(VirtualnetworkrulesVirtualNetworkRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_virtualnetworkrules --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_virtualnetworkrules/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_virtualnetworkrules.sql import VirtualnetworkrulesVirtualNetworkRule, VirtualnetworkrulesVirtualNetworkRuleConfig

    def describe():
        ubx.intent("example own virtualnetworkrules virtual network rule")

        ubx.resource(VirtualnetworkrulesVirtualNetworkRule, "example", VirtualnetworkrulesVirtualNetworkRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_virtualnetworkrules_virtual_network_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a virtual network rule. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="ignore_missing_vnet_service_endpoint" type="bool">
      Create firewall rule before the virtual network has vnet service endpoint enabled. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Virtual Network Rule State Optional.
    </ResponseField>
    <ResponseField name="virtual_network_subnet_id" type="string" required>
      The ARM resource id of the virtual network subnet. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
