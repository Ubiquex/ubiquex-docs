---
title: "azure_sql_serverconnectionpolicies_server_connection_policy"
description: "`azure_sql_serverconnectionpolicies_server_connection_policy` sets whether connections to an Azure SQL logical server use the redirect or proxy..."
---

`azure_sql_serverconnectionpolicies_server_connection_policy` sets whether connections to an Azure SQL logical server use the redirect or proxy connection policy, affecting connection latency and required outbound firewall rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own serverconnectionpolicies server connection policy"})

    		ubx.Resource(sql.ServerconnectionpoliciesServerConnectionPolicy, "example", sql.ServerconnectionpoliciesServerConnectionPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerconnectionpoliciesServerConnectionPolicy } from "@ubx/sdk-azure/azure/sql/serverconnectionpolicies_server_connection_policy";

    export default stack("example", () => {
      intent({
        summary: "example own serverconnectionpolicies server connection policy",
      });

      resource(ServerconnectionpoliciesServerConnectionPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import ServerconnectionpoliciesServerConnectionPolicy, ServerconnectionpoliciesServerConnectionPolicyConfig

    def describe():
        ubx.intent("example own serverconnectionpolicies server connection policy")

        ubx.resource(ServerconnectionpoliciesServerConnectionPolicy, "example", ServerconnectionpoliciesServerConnectionPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_serverconnectionpolicies_server_connection_policy, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a server connection policy. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string" required>
      The server connection type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
