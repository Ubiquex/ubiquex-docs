---
title: "azure_sql_virtualclusters_virtual_cluster"
description: "`azure_sql_virtualclusters_virtual_cluster` reports the underlying shared infrastructure cluster grouping Azure SQL Managed Instance(s) deployed into..."
---

`azure_sql_virtualclusters_virtual_cluster` reports the underlying shared infrastructure cluster grouping Azure SQL Managed Instance(s) deployed into the same subnet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_virtualclusters --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_virtualclusters/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_virtualclusters/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualclusters virtual cluster"})

    		ubx.Resource(sql.VirtualclustersVirtualCluster, "example", sql.VirtualclustersVirtualClusterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_virtualclusters --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualclustersVirtualCluster } from "./local-sdk/azure_sql_virtualclusters/sdk/typescript/azure_sql_virtualclusters/sql/virtualclusters_virtual_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualclusters virtual cluster" });

      resource(VirtualclustersVirtualCluster, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_virtualclusters --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_virtualclusters/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_virtualclusters.sql import VirtualclustersVirtualCluster, VirtualclustersVirtualClusterConfig

    def describe():
        ubx.intent("example own virtualclusters virtual cluster")

        ubx.resource(VirtualclustersVirtualCluster, "example", VirtualclustersVirtualClusterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_virtualclusters_virtual_cluster, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a virtual cluster. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="child_resources" type="list(string)">
      List of resources in this virtual cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="subnet_id" type="string">
      Subnet resource ID for the virtual cluster. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      Virtual cluster version. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
