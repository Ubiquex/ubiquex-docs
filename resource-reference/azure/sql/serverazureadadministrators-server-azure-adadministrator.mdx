---
title: "azure_sql_serverazureadadministrators_server_azure_adadministrator"
description: "`azure_sql_serverazureadadministrators_server_azure_adadministrator` designates a Microsoft Entra ID principal as the administrator for an Azure SQL..."
---

`azure_sql_serverazureadadministrators_server_azure_adadministrator` designates a Microsoft Entra ID principal as the administrator for an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_serverazureadadministrators --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_serverazureadadministrators/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_serverazureadadministrators/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own serverazureadadministrators server azure adadministrator"})

    		ubx.Resource(sql.ServerazureadadministratorsServerAzureAdadministrator, "ci-runner", sql.ServerazureadadministratorsServerAzureAdadministratorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_serverazureadadministrators --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerazureadadministratorsServerAzureAdadministrator } from "./local-sdk/azure_sql_serverazureadadministrators/sdk/typescript/azure_sql_serverazureadadministrators/sql/serverazureadadministrators_server_azure_adadministrator.ts";

    export default stack("payments", () => {
      intent({
        summary:
          "payments own serverazureadadministrators server azure adadministrator",
      });

      resource(
        ServerazureadadministratorsServerAzureAdadministrator,
        "ci-runner",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_serverazureadadministrators --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_serverazureadadministrators/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_serverazureadadministrators.sql import ServerazureadadministratorsServerAzureAdadministrator, ServerazureadadministratorsServerAzureAdadministratorConfig

    def describe():
        ubx.intent("payments own serverazureadadministrators server azure adadministrator")

        ubx.resource(ServerazureadadministratorsServerAzureAdadministrator, "ci-runner", ServerazureadadministratorsServerAzureAdadministratorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_serverazureadadministrators_server_azure_adadministrator,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a active directory administrator. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_type" type="string">
      Type of the sever administrator. Optional.
    </ResponseField>
    <ResponseField name="azure_adonly_authentication" type="bool">
      Azure Active Directory only Authentication enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      Login name of the server administrator. Required.
    </ResponseField>
    <ResponseField name="sid" type="string" required>
      Universally Unique Identifier Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
