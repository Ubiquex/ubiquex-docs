---
title: "azure_sql_serverazureadadministrators_server_azure_adadministrator"
description: "`azure_sql_serverazureadadministrators_server_azure_adadministrator` designates a Microsoft Entra ID principal as the administrator for an Azure SQL..."
---

`azure_sql_serverazureadadministrators_server_azure_adadministrator` designates a Microsoft Entra ID principal as the administrator for an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own serverazureadadministrators server azure adadministrator"})

    		ubx.Resource(sql.ServerazureadadministratorsServerAzureAdadministrator, "example", sql.ServerazureadadministratorsServerAzureAdadministratorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerazureadadministratorsServerAzureAdadministrator } from "@ubx/sdk-azure/azure/sql/serverazureadadministrators_server_azure_adadministrator";

    export default stack("example", () => {
      intent({
        summary:
          "example own serverazureadadministrators server azure adadministrator",
      });

      resource(
        ServerazureadadministratorsServerAzureAdadministrator,
        "example",
        {},
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql.serverazureadadministrators_server_azure_adadministrator import ServerazureadadministratorsServerAzureAdadministrator, ServerazureadadministratorsServerAzureAdadministratorConfig

    def describe():
        ubx.intent("example own serverazureadadministrators server azure adadministrator")

        ubx.resource(ServerazureadadministratorsServerAzureAdadministrator, "example", ServerazureadadministratorsServerAzureAdadministratorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_sql_serverazureadadministrators_server_azure_adadministrator,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a active directory administrator. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_type" type="string">
      Type of the sever administrator. Optional.
    </ResponseField>
    <ResponseField name="azure_adonly_authentication" type="bool">
      Azure Active Directory only Authentication enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      Login name of the server administrator. Required.
    </ResponseField>
    <ResponseField name="sid" type="string" required>
      Universally Unique Identifier Required.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
