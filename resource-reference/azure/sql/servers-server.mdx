---
title: "azure_sql_servers_server"
description: "`azure_sql_servers_server` is an Azure SQL logical server..."
---

`azure_sql_servers_server` is an Azure SQL logical server, the administrative and connectivity boundary that Azure SQL Database(s) and elastic pool(s) are created under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_servers --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_servers/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_servers/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own servers server"})

    		ubx.Resource(sql.ServersServer, "ci-runner", sql.ServersServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_servers --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServersServer } from "./local-sdk/azure_sql_servers/sdk/typescript/azure_sql_servers/sql/servers_server.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own servers server" });

      resource(ServersServer, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_servers --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_servers/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_servers.sql import ServersServer, ServersServerConfig

    def describe():
        ubx.intent("payments own servers server")

        ubx.resource(ServersServer, "ci-runner", ServersServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_servers_server, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Azure Active Directory identity configuration for a resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identity type. Set this to 'SystemAssigned' in order to automatically create and assign an Azure Active Directory principal for the resource. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The resource ids of the user assigned identities to use Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of sql server. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a server. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_login" type="string">
      Administrator username for the server. Once created it cannot be changed. Optional.
    </ResponseField>
    <ResponseField name="administrator_login_password" type="string">
      The administrator login password (required for server creation). Optional.
    </ResponseField>
    <ResponseField name="administrators" type="object">
      Properties of a active directory administrator. Optional.
      <Expandable title="properties">
        <ResponseField name="administrator_type" type="string">
          Type of the sever administrator. Optional.
        </ResponseField>
        <ResponseField name="azure_adonly_authentication" type="bool">
          Azure Active Directory only Authentication enabled. Optional.
        </ResponseField>
        <ResponseField name="login" type="string">
          Login name of the server administrator. Optional.
        </ResponseField>
        <ResponseField name="principal_type" type="string">
          Principal Type of the sever administrator. Optional.
        </ResponseField>
        <ResponseField name="sid" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      Create mode for server, only valid values for this are Normal and Restore. Optional.
    </ResponseField>
    <ResponseField name="external_governance_status" type="string">
      Status of external governance. Optional.
    </ResponseField>
    <ResponseField name="federated_client_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="fully_qualified_domain_name" type="string">
      The fully qualified domain name of the server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_ipv6_enabled" type="string">
      Whether or not to restrict outbound network access for this server. Value is optional but if passed in, must be 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="key_id" type="string">
      A CMK URI of the key to use for encryption. Optional.
    </ResponseField>
    <ResponseField name="minimal_tls_version" type="string">
      Minimal TLS version. Allowed values: 'None', 1.0', '1.1', '1.2', '1.3' Optional.
    </ResponseField>
    <ResponseField name="primary_user_assigned_identity_id" type="string">
      The resource id of a user assigned identity to be used by default. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections on a server Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Whether or not public endpoint access is allowed for this server. Value is optional but if passed in, must be 'Enabled' or 'Disabled' or 'SecuredByPerimeter' Optional.
    </ResponseField>
    <ResponseField name="restrict_outbound_network_access" type="string">
      Whether or not to restrict outbound network access for this server. Value is optional but if passed in, must be 'Enabled' or 'Disabled' Optional.
    </ResponseField>
    <ResponseField name="retention_days" type="number">
      Number of days this server will stay soft-deleted. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of the server. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the server. Optional.
    </ResponseField>
    <ResponseField name="workspace_feature" type="string">
      Whether or not existing server has a workspace created and if it allows connection from workspace Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
