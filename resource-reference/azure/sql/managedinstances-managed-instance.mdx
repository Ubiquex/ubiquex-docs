---
title: "azure_sql_managedinstances_managed_instance"
description: "`azure_sql_managedinstances_managed_instance` is an Azure SQL Managed Instance..."
---

`azure_sql_managedinstances_managed_instance` is an Azure SQL Managed Instance, a fully managed SQL Server engine deployment with near-full instance-level feature compatibility, deployed into a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_managedinstances --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_managedinstances/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_managedinstances/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own managedinstances managed instance"})

    		ubx.Resource(sql.ManagedinstancesManagedInstance, "example", sql.ManagedinstancesManagedInstanceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_managedinstances --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedinstancesManagedInstance } from "./local-sdk/azure_sql_managedinstances/sdk/typescript/azure_sql_managedinstances/sql/managedinstances_managed_instance.ts";

    export default stack("example", () => {
      intent({ summary: "example own managedinstances managed instance" });

      resource(ManagedinstancesManagedInstance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_managedinstances --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_managedinstances/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_managedinstances.sql import ManagedinstancesManagedInstance, ManagedinstancesManagedInstanceConfig

    def describe():
        ubx.intent("example own managedinstances managed instance")

        ubx.resource(ManagedinstancesManagedInstance, "example", ManagedinstancesManagedInstanceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_managedinstances_managed_instance, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Azure Active Directory identity configuration for a resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identity type. Set this to 'SystemAssigned' in order to automatically create and assign an Azure Active Directory principal for the resource. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The resource ids of the user assigned identities to use Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a managed instance. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="administrator_login" type="string">
      Administrator username for the managed instance. Can only be specified when the managed instance is being created (and is required for creation). Optional.
    </ResponseField>
    <ResponseField name="administrator_login_password" type="string">
      The administrator login password (required for managed instance creation). Optional.
    </ResponseField>
    <ResponseField name="administrators" type="object">
      Properties of a active directory administrator. Optional.
      <Expandable title="properties">
        <ResponseField name="administrator_type" type="string">
          Type of the sever administrator. Optional.
        </ResponseField>
        <ResponseField name="azure_adonly_authentication" type="bool">
          Azure Active Directory only Authentication enabled. Optional.
        </ResponseField>
        <ResponseField name="login" type="string">
          Login name of the server administrator. Optional.
        </ResponseField>
        <ResponseField name="principal_type" type="string">
          Principal Type of the sever administrator. Optional.
        </ResponseField>
        <ResponseField name="sid" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="authentication_metadata" type="string">
      The managed instance's authentication metadata lookup mode. Optional.
    </ResponseField>
    <ResponseField name="collation" type="string">
      Collation of the managed instance. Optional.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Specifies the point in time (ISO8601 format) of the Managed Instance creation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="current_backup_storage_redundancy" type="string">
      The storage account type used to store backups for this database. Optional.
    </ResponseField>
    <ResponseField name="database_format" type="string">
      Specifies the internal format of instance databases specific to the SQL engine version. Optional.
    </ResponseField>
    <ResponseField name="dns_zone" type="string">
      The Dns Zone that the managed instance is in. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="dns_zone_partner" type="string">
      The resource id of another managed instance whose DNS zone this managed instance will share after creation. Optional.
    </ResponseField>
    <ResponseField name="external_governance_status" type="string">
      Status of external governance. Optional.
    </ResponseField>
    <ResponseField name="fully_qualified_domain_name" type="string">
      The fully qualified domain name of the managed instance. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="hybrid_secondary_usage" type="string">
      Hybrid secondary usage. Possible values are 'Active' (default value) and 'Passive' (customer uses the secondary as Passive DR). Optional.
    </ResponseField>
    <ResponseField name="hybrid_secondary_usage_detected" type="string">
      Hybrid secondary usage detected. Possible values are 'Active' (customer does not meet the requirements to use the secondary as Passive DR) and 'Passive' (customer meets the requirements to use the secondary as Passive DR). Optional.
    </ResponseField>
    <ResponseField name="instance_pool_id" type="string">
      The Id of the instance pool this managed server belongs to. Optional.
    </ResponseField>
    <ResponseField name="is_general_purpose_v2" type="bool">
      Whether or not this is a GPv2 variant of General Purpose edition. Optional.
    </ResponseField>
    <ResponseField name="key_id" type="string">
      A CMK URI of the key to use for encryption. Optional.
    </ResponseField>
    <ResponseField name="license_type" type="string">
      The license type. Possible values are 'LicenseIncluded' (regular price inclusive of a new SQL license) and 'BasePrice' (discounted AHB price for bringing your own SQL licenses). Optional.
    </ResponseField>
    <ResponseField name="maintenance_configuration_id" type="string">
      Specifies maintenance configuration id to apply to this managed instance. Optional.
    </ResponseField>
    <ResponseField name="managed_instance_create_mode" type="string">
      Specifies the mode of database creation. Default: Regular instance creation. Restore: Creates an instance by restoring a set of backups to specific point in time. RestorePointInTime and SourceManagedInstanceId must be specified. Optional.
    </ResponseField>
    <ResponseField name="memory_size_in_gb" type="number">
      Memory size in GB. Minimum value: 28. Maximum value: 870. Minimum and maximum value depend on the number of vCores and service tier. Read more about resource limits: https://aka.ms/mi-resource-limits-api. Optional.
    </ResponseField>
    <ResponseField name="minimal_tls_version" type="string">
      Minimal TLS version. Allowed values: 'None', '1.0', '1.1', '1.2' Optional.
    </ResponseField>
    <ResponseField name="pricing_model" type="string">
      Pricing model of Managed Instance. Optional.
    </ResponseField>
    <ResponseField name="primary_user_assigned_identity_id" type="string">
      The resource id of a user assigned identity to be used by default. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint_connections" type="list(object)">
      List of private endpoint connections on a managed instance. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The ARM provisioning state of the job execution. Optional.
    </ResponseField>
    <ResponseField name="proxy_override" type="string">
      Connection type used for connecting to the instance. Optional.
    </ResponseField>
    <ResponseField name="public_data_endpoint_enabled" type="bool">
      Whether or not the public data endpoint is enabled. Optional.
    </ResponseField>
    <ResponseField name="requested_backup_storage_redundancy" type="string">
      The storage account type used to store backups for this database. Optional.
    </ResponseField>
    <ResponseField name="requested_logical_availability_zone" type="string">
      Specifies the availability zone the database is pinned to. Optional.
    </ResponseField>
    <ResponseField name="restore_point_in_time" type="string">
      Specifies the point in time (ISO8601 format) of the source database that will be restored to create the new database. Optional.
    </ResponseField>
    <ResponseField name="service_principal" type="object">
      The managed instance's service principal configuration for a resource. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The Azure Active Directory application client id. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          The Azure Active Directory application object id. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          The Azure Active Directory tenant id. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="type" type="string">
          Service principal type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_managed_instance_id" type="string">
      The resource identifier of the source managed instance associated with create operation of this instance. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of the managed instance. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_iops" type="number">
      Storage IOps. Minimum value: 300. Maximum value: 80000. Increments of 1 IOps allowed only. Maximum value depends on the selected hardware family and number of vCores. Optional.
    </ResponseField>
    <ResponseField name="storage_size_in_gb" type="number">
      Storage size in GB. Minimum value: 32. Maximum value: 32768. Increments of 32 GB allowed only. Maximum value depends on the selected hardware family and number of vCores. Optional.
    </ResponseField>
    <ResponseField name="storage_throughput_mbps" type="number">
      Storage throughput MBps parameter is not supported in the instance create/update operation. Optional.
    </ResponseField>
    <ResponseField name="subnet_id" type="string">
      Subnet resource ID for the managed instance. Optional.
    </ResponseField>
    <ResponseField name="timezone_id" type="string">
      Id of the timezone. Allowed values are timezones supported by Windows. Windows keeps details on supported timezones, including the id, in registry under KEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Time Zones. You can get those registry values via SQL Server by querying SELECT name AS timezone_id FROM sys.time_zone_info. List of Ids can also be obtained by executing [System.TimeZoneInfo]::GetSystemTimeZones() in PowerShell. An example of valid timezone id is "Pacific Standard Time" or "W. Europe Standard Time". Optional.
    </ResponseField>
    <ResponseField name="v_cores" type="number">
      The number of vCores. Allowed values: 4, 6, 8, 10, 12, 16, 20, 24, 32, 40, 48, 56, 64, 80, 96, 128. Supported vCores depends on the selected hardware family and service tier. Optional.
    </ResponseField>
    <ResponseField name="virtual_cluster_id" type="string">
      Virtual cluster resource id for the Managed Instance. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="zone_redundant" type="bool">
      Whether or not the zone-redundancy is enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  An ARM Resource SKU. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Capacity of the particular SKU. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU, typically, a letter + Number code, e.g. P3. Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      Size of the particular SKU Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier or edition of the particular SKU, e.g. Basic, Premium. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
