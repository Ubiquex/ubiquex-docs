---
title: "azure_sql_jobs_job"
description: "`azure_sql_jobs_job` is a scheduled or on-demand T-SQL script definition run by an Elastic Database Jobs agent against its own target group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own jobs job"})

    		ubx.Resource(sql.JobsJob, "example", sql.JobsJobConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobsJob } from "@ubx/sdk-azure/azure/sql/jobs_job";

    export default stack("example", () => {
      intent({ summary: "example own jobs job" });

      resource(JobsJob, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql.jobs_job import JobsJob, JobsJobConfig

    def describe():
        ubx.intent("example own jobs job")

        ubx.resource(JobsJob, "example", JobsJobConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_jobs_job, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a job. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      User-defined description of the job. Optional.
    </ResponseField>
    <ResponseField name="schedule" type="object">
      Scheduling properties of a job. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether or not the schedule is enabled. Optional.
        </ResponseField>
        <ResponseField name="end_time" type="string">
          Schedule end time. Optional.
        </ResponseField>
        <ResponseField name="interval" type="string">
          Value of the schedule's recurring interval, if the ScheduleType is recurring. ISO8601 duration format. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Schedule start time. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Schedule interval type Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version" type="number">
      The job version number. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
