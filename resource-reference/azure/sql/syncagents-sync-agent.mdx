---
title: "azure_sql_syncagents_sync_agent"
description: "`azure_sql_syncagents_sync_agent` is a local agent enabling SQL Data Sync between an on-premises SQL Server database and Azure SQL Database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_syncagents --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_syncagents/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_syncagents/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own syncagents sync agent"})

    		ubx.Resource(sql.SyncagentsSyncAgent, "ci-runner", sql.SyncagentsSyncAgentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_syncagents --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncagentsSyncAgent } from "./local-sdk/azure_sql_syncagents/sdk/typescript/azure_sql_syncagents/sql/syncagents_sync_agent.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own syncagents sync agent" });

      resource(SyncagentsSyncAgent, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_syncagents --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_syncagents/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_syncagents.sql import SyncagentsSyncAgent, SyncagentsSyncAgentConfig

    def describe():
        ubx.intent("payments own syncagents sync agent")

        ubx.resource(SyncagentsSyncAgent, "ci-runner", SyncagentsSyncAgentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_syncagents_sync_agent, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an Azure SQL Database sync agent. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="expiry_time" type="string">
      Expiration time of the sync agent version. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_up_to_date" type="bool">
      If the sync agent version is up to date. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_alive_time" type="string">
      Last alive time of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      State of the sync agent. Optional.
    </ResponseField>
    <ResponseField name="sync_database_id" type="string">
      ARM resource id of the sync database in the sync agent. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
