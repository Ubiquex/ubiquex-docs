---
title: "azure_sql_syncagents_sync_agent"
description: "`azure_sql_syncagents_sync_agent` is a local agent enabling SQL Data Sync between an on-premises SQL Server database and Azure SQL Database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own syncagents sync agent"})

    		ubx.Resource(sql.SyncagentsSyncAgent, "example", sql.SyncagentsSyncAgentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncagentsSyncAgent } from "@ubx/sdk-azure/azure/sql/syncagents_sync_agent";

    export default stack("example", () => {
      intent({ summary: "example own syncagents sync agent" });

      resource(SyncagentsSyncAgent, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import SyncagentsSyncAgent, SyncagentsSyncAgentConfig

    def describe():
        ubx.intent("example own syncagents sync agent")

        ubx.resource(SyncagentsSyncAgent, "example", SyncagentsSyncAgentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_syncagents_sync_agent, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an Azure SQL Database sync agent. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="expiry_time" type="string">
      Expiration time of the sync agent version. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_up_to_date" type="bool">
      If the sync agent version is up to date. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_alive_time" type="string">
      Last alive time of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      State of the sync agent. Optional.
    </ResponseField>
    <ResponseField name="sync_database_id" type="string">
      ARM resource id of the sync database in the sync agent. Optional.
    </ResponseField>
    <ResponseField name="version" type="string">
      Version of the sync agent. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
