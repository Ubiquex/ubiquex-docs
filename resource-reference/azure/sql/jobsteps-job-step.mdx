---
title: "azure_sql_jobsteps_job_step"
description: "`azure_sql_jobsteps_job_step` is a single T-SQL script step within an Elastic Database Jobs job, run in sequence against the job's own target group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_jobsteps --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_jobsteps/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_jobsteps/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own jobsteps job step"})

    		ubx.Resource(sql.JobstepsJobStep, "ci-runner", sql.JobstepsJobStepConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_jobsteps --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobstepsJobStep } from "./local-sdk/azure_sql_jobsteps/sdk/typescript/azure_sql_jobsteps/sql/jobsteps_job_step.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own jobsteps job step" });

      resource(JobstepsJobStep, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_jobsteps --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_jobsteps/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_jobsteps.sql import JobstepsJobStep, JobstepsJobStepConfig

    def describe():
        ubx.intent("payments own jobsteps job step")

        ubx.resource(JobstepsJobStep, "ci-runner", JobstepsJobStepConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_jobsteps_job_step, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a job step. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="action" type="object" required>
      The action to be executed by a job step. Required.
      <Expandable title="properties">
        <ResponseField name="source" type="string">
          The source of the action to execute. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of action being executed by the job step. Optional.
        </ResponseField>
        <ResponseField name="value" type="string" required>
          The action value, for example the text of the T-SQL script to execute. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="credential" type="string">
      The resource ID of the job credential that will be used to connect to the targets. Optional.
    </ResponseField>
    <ResponseField name="execution_options" type="object">
      The execution options of a job step. Optional.
      <Expandable title="properties">
        <ResponseField name="initial_retry_interval_seconds" type="number">
          Initial delay between retries for job step execution. Optional.
        </ResponseField>
        <ResponseField name="maximum_retry_interval_seconds" type="number">
          The maximum amount of time to wait between retries for job step execution. Optional.
        </ResponseField>
        <ResponseField name="retry_attempts" type="number">
          Maximum number of times the job step will be reattempted if the first attempt fails. Optional.
        </ResponseField>
        <ResponseField name="retry_interval_backoff_multiplier" type="number">
          The backoff multiplier for the time between retries. Optional.
        </ResponseField>
        <ResponseField name="timeout_seconds" type="number">
          Execution timeout for the job step. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output" type="object">
      The output configuration of a job step. Optional.
      <Expandable title="properties">
        <ResponseField name="credential" type="string">
          The resource ID of the credential to use to connect to the output destination. Optional.
        </ResponseField>
        <ResponseField name="database_name" type="string" required>
          The output destination database. Required.
        </ResponseField>
        <ResponseField name="resource_group_name" type="string">
          The output destination resource group. Optional.
        </ResponseField>
        <ResponseField name="schema_name" type="string">
          The output destination schema. Optional.
        </ResponseField>
        <ResponseField name="server_name" type="string" required>
          The output destination server name. Required.
        </ResponseField>
        <ResponseField name="subscription_id" type="string">
          Universally Unique Identifier Optional.
        </ResponseField>
        <ResponseField name="table_name" type="string" required>
          The output destination table. Required.
        </ResponseField>
        <ResponseField name="type" type="string">
          The output destination type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="step_id" type="number">
      The job step's index within the job. If not specified when creating the job step, it will be created as the last step. If not specified when updating the job step, the step id is not modified. Optional.
    </ResponseField>
    <ResponseField name="target_group" type="string" required>
      The resource ID of the target group that the job step will be executed on. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
