---
title: "azure_sql_ledgerdigestuploads_ledger_digest_uploads"
description: "`azure_sql_ledgerdigestuploads_ledger_digest_uploads` configures Azure SQL Database Ledger to periodically upload cryptographic digest(s) to..."
---

`azure_sql_ledgerdigestuploads_ledger_digest_uploads` configures Azure SQL Database Ledger to periodically upload cryptographic digest(s) to an external store (Azure Storage or a blockchain), independently proving data hasn't been tampered with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ledgerdigestuploads ledger digest uploads"})

    		ubx.Resource(sql.LedgerdigestuploadsLedgerDigestUploads, "example", sql.LedgerdigestuploadsLedgerDigestUploadsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LedgerdigestuploadsLedgerDigestUploads } from "@ubx/sdk-azure/azure/sql/ledgerdigestuploads_ledger_digest_uploads";

    export default stack("example", () => {
      intent({ summary: "example own ledgerdigestuploads ledger digest uploads" });

      resource(LedgerdigestuploadsLedgerDigestUploads, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import LedgerdigestuploadsLedgerDigestUploads, LedgerdigestuploadsLedgerDigestUploadsConfig

    def describe():
        ubx.intent("example own ledgerdigestuploads ledger digest uploads")

        ubx.resource(LedgerdigestuploadsLedgerDigestUploads, "example", LedgerdigestuploadsLedgerDigestUploadsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_ledgerdigestuploads_ledger_digest_uploads, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a database ledger digest upload settings. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="digest_storage_endpoint" type="string">
      The digest storage endpoint, which must be either an Azure blob storage endpoint or an URI for Azure Confidential Ledger. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Specifies the state of ledger digest upload. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
