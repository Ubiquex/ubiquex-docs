---
title: "azure_sql_databases_database"
description: "`azure_sql_databases_database` is an Azure SQL Database, a fully managed, single or elastic-pool-hosted relational database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own databases database"})

    		ubx.Resource(sql.DatabasesDatabase, "example", sql.DatabasesDatabaseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabasesDatabase } from "@ubx/sdk-azure/azure/sql/databases_database";

    export default stack("example", () => {
      intent({ summary: "example own databases database" });

      resource(DatabasesDatabase, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import DatabasesDatabase, DatabasesDatabaseConfig

    def describe():
        ubx.intent("example own databases database")

        ubx.resource(DatabasesDatabase, "example", DatabasesDatabaseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_databases_database, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Azure Active Directory identity configuration for a resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The identity type Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The resource ids of the user assigned identities to use Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of database. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="managed_by" type="string">
  Resource that manages the database. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The database's properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="auto_pause_delay" type="number">
      Time in minutes after which database is automatically paused. A value of -1 means that automatic pause is disabled Optional.
    </ResponseField>
    <ResponseField name="availability_zone" type="string">
      Specifies the availability zone the database is pinned to. Optional.
    </ResponseField>
    <ResponseField name="catalog_collation" type="string">
      Collation of the metadata catalog. Optional.
    </ResponseField>
    <ResponseField name="collation" type="string">
      The collation of the database. Optional.
    </ResponseField>
    <ResponseField name="create_mode" type="string">
      Specifies the mode of database creation. Default: regular database creation. Copy: creates a database as a copy of an existing database. sourceDatabaseId must be specified as the resource ID of the source database. Secondary: creates a database as a secondary replica of an existing database. sourceDatabaseId must be specified as the resource ID of the existing primary database. PointInTimeRestore: Creates a database by restoring a point in time backup of an existing database. sourceDatabaseId must be specified as the resource ID of the existing database, and restorePointInTime must be specified. Recovery: Creates a database by restoring a geo-replicated backup. sourceDatabaseId must be specified as the recoverable database resource ID to restore. Restore: Creates a database by restoring a backup of a deleted database. sourceDatabaseId must be specified. If sourceDatabaseId is the database's original resource ID, then sourceDatabaseDeletionDate must be specified. Otherwise sourceDatabaseId must be the restorable dropped database resource ID and sourceDatabaseDeletionDate is ignored. restorePointInTime may also be specified to restore from an earlier point in time. RestoreLongTermRetentionBackup: Creates a database by restoring from a long term retention vault. recoveryServicesRecoveryPointResourceId must be specified as the recovery point resource ID. Copy, Secondary, and RestoreLongTermRetentionBackup are not supported for DataWarehouse edition. Optional.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      The creation date of the database (ISO8601 format). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="current_backup_storage_redundancy" type="string">
      The storage account type used to store backups for this database. Optional.
    </ResponseField>
    <ResponseField name="current_service_objective_name" type="string">
      The current service level objective name of the database. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="current_sku" type="object">
      An ARM Resource SKU. Optional.
      <Expandable title="properties">
        <ResponseField name="capacity" type="number">
          Capacity of the particular SKU. Optional.
        </ResponseField>
        <ResponseField name="family" type="string">
          If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the SKU, typically, a letter + Number code, e.g. P3. Required.
        </ResponseField>
        <ResponseField name="size" type="string">
          Size of the particular SKU Optional.
        </ResponseField>
        <ResponseField name="tier" type="string">
          The tier or edition of the particular SKU, e.g. Basic, Premium. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="default_secondary_location" type="string">
      The default secondary region for this database. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="earliest_restore_date" type="string">
      This records the earliest start date and time that restore is available for this database (ISO8601 format). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="elastic_pool_id" type="string">
      The resource identifier of the elastic pool containing this database. Optional.
    </ResponseField>
    <ResponseField name="encryption_protector" type="string">
      The azure key vault URI of the database if it's configured with per Database Customer Managed Keys. Optional.
    </ResponseField>
    <ResponseField name="encryption_protector_auto_rotation" type="bool">
      The flag to enable or disable auto rotation of database encryption protector AKV key. Optional.
    </ResponseField>
    <ResponseField name="failover_group_id" type="string">
      Failover Group resource identifier that this database belongs to. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="federated_client_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="free_limit_exhaustion_behavior" type="string">
      Specifies the behavior when monthly free limits are exhausted for the free database. AutoPause: The database will be auto paused upon exhaustion of free limits for remainder of the month. BillForUsage: The database will continue to be online upon exhaustion of free limits and any overage will be billed. Optional.
    </ResponseField>
    <ResponseField name="high_availability_replica_count" type="number">
      The number of secondary replicas associated with the Business Critical, Premium, or Hyperscale edition database that are used to provide high availability. Not applicable to a Hyperscale database within an elastic pool. Optional.
    </ResponseField>
    <ResponseField name="is_infra_encryption_enabled" type="bool">
      Infra encryption is enabled for this database. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="is_ledger_on" type="bool">
      Whether or not this database is a ledger database, which means all tables in the database are ledger tables. Note: the value of this property cannot be changed after the database has been created. Optional.
    </ResponseField>
    <ResponseField name="keys" type="map(object)">
      The resource ids of the user assigned identities to use Optional.
      <Expandable title="properties">
        <ResponseField name="creation_date" type="string">
          When this transparent data encryption protector key was created. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key_version" type="string">
          The specific version of this encryption key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subregion" type="string">
          The Azure subregion this key is available in. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="thumbprint" type="string">
          A cryptographic thumbprint of this key. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="license_type" type="string">
      The license type to apply for this database. `LicenseIncluded` if you need a license, or `BasePrice` if you have a license and are eligible for the Azure Hybrid Benefit. Optional.
    </ResponseField>
    <ResponseField name="long_term_retention_backup_resource_id" type="string">
      The resource identifier of the long term retention backup associated with create operation of this database. Optional.
    </ResponseField>
    <ResponseField name="maintenance_configuration_id" type="string">
      Maintenance configuration id assigned to the database. This configuration defines the period when the maintenance updates will occur. Optional.
    </ResponseField>
    <ResponseField name="manual_cutover" type="bool">
      Whether or not customer controlled manual cutover needs to be done during Update Database operation to Hyperscale tier. This property is only applicable when scaling database from Business Critical/General Purpose/Premium/Standard tier to Hyperscale tier. When manualCutover is specified, the scaling operation will wait for user input to trigger cutover to Hyperscale database. To trigger cutover, please provide 'performCutover' parameter when the Scaling operation is in Waiting state. Optional.
    </ResponseField>
    <ResponseField name="max_log_size_bytes" type="number">
      The max log size for this database. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_size_bytes" type="number">
      The max size of the database expressed in bytes. Optional.
    </ResponseField>
    <ResponseField name="min_capacity" type="number">
      Minimal capacity that database will always have allocated, if not paused Optional.
    </ResponseField>
    <ResponseField name="paused_date" type="string">
      The date when database was paused by user configuration or action(ISO8601 format). Null if the database is ready. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="perform_cutover" type="bool">
      To trigger customer controlled manual cutover during the wait state while Scaling operation is in progress. This property parameter is only applicable for scaling operations that are initiated along with 'manualCutover' parameter. This property is only applicable when scaling database from Business Critical/General Purpose/Premium/Standard tier to Hyperscale tier is already in progress. When performCutover is specified, the scaling operation will trigger cutover and perform role-change to Hyperscale database. Optional.
    </ResponseField>
    <ResponseField name="preferred_enclave_type" type="string">
      Type of enclave requested on the database i.e. Default or VBS enclaves. Optional.
    </ResponseField>
    <ResponseField name="read_scale" type="string">
      The state of read-only routing. If enabled, connections that have application intent set to readonly in their connection string may be routed to a readonly secondary replica in the same region. Not applicable to a Hyperscale database within an elastic pool. Optional.
    </ResponseField>
    <ResponseField name="recoverable_database_id" type="string">
      The resource identifier of the recoverable database associated with create operation of this database. Optional.
    </ResponseField>
    <ResponseField name="recovery_services_recovery_point_id" type="string">
      The resource identifier of the recovery point associated with create operation of this database. Optional.
    </ResponseField>
    <ResponseField name="requested_backup_storage_redundancy" type="string">
      The storage account type used to store backups for this database. Optional.
    </ResponseField>
    <ResponseField name="requested_service_objective_name" type="string">
      The requested service level objective name of the database. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="restorable_dropped_database_id" type="string">
      The resource identifier of the restorable dropped database associated with create operation of this database. Optional.
    </ResponseField>
    <ResponseField name="restore_point_in_time" type="string">
      Specifies the point in time (ISO8601 format) of the source database that will be restored to create the new database. Optional.
    </ResponseField>
    <ResponseField name="resumed_date" type="string">
      The date when database was resumed by user action or database login (ISO8601 format). Null if the database is paused. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="sample_name" type="string">
      The name of the sample schema to apply when creating this database. Optional.
    </ResponseField>
    <ResponseField name="secondary_type" type="string">
      The secondary type of the database if it is a secondary. Valid values are Geo, Named and Standby. Optional.
    </ResponseField>
    <ResponseField name="source_database_deletion_date" type="string">
      Specifies the time that the database was deleted. Optional.
    </ResponseField>
    <ResponseField name="source_database_id" type="string">
      The resource identifier of the source database associated with create operation of this database. Optional.
    </ResponseField>
    <ResponseField name="source_resource_id" type="string">
      The resource identifier of the source associated with the create operation of this database. This property is only supported for DataWarehouse edition and allows to restore across subscriptions. When sourceResourceId is specified, sourceDatabaseId, recoverableDatabaseId, restorableDroppedDatabaseId and sourceDatabaseDeletionDate must not be specified and CreateMode must be PointInTimeRestore, Restore or Recover. When createMode is PointInTimeRestore, sourceResourceId must be the resource ID of the existing database or existing sql pool, and restorePointInTime must be specified. When createMode is Restore, sourceResourceId must be the resource ID of restorable dropped database or restorable dropped sql pool. When createMode is Recover, sourceResourceId must be the resource ID of recoverable database or recoverable sql pool. When source subscription belongs to a different tenant than target subscription, “x-ms-authorization-auxiliary” header must contain authentication token for the source tenant. For more details about “x-ms-authorization-auxiliary” header see https://docs.microsoft.com/en-us/azure/azure-resource-manager/management/authenticate-multi-tenant Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      The status of the database. Optional.
    </ResponseField>
    <ResponseField name="use_free_limit" type="bool">
      Whether or not the database uses free monthly limits. Allowed on one database in a subscription. Optional.
    </ResponseField>
    <ResponseField name="zone_redundant" type="bool">
      Whether or not this database is zone redundant, which means the replicas of this database will be spread across multiple availability zones. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  An ARM Resource SKU. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Capacity of the particular SKU. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU, typically, a letter + Number code, e.g. P3. Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      Size of the particular SKU Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier or edition of the particular SKU, e.g. Basic, Premium. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
