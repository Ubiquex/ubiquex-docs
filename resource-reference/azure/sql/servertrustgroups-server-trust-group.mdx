---
title: "azure_sql_servertrustgroups_server_trust_group"
description: "`azure_sql_servertrustgroups_server_trust_group` establishes mutual trust between Azure SQL Managed Instances (or Managed Instances and SQL Server) for..."
---

`azure_sql_servertrustgroups_server_trust_group` establishes mutual trust between Azure SQL Managed Instances (or Managed Instances and SQL Server) for cross-instance distributed transactions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servertrustgroups server trust group"})

    		ubx.Resource(sql.ServertrustgroupsServerTrustGroup, "example", sql.ServertrustgroupsServerTrustGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServertrustgroupsServerTrustGroup } from "@ubx/sdk-azure/azure/sql/servertrustgroups_server_trust_group";

    export default stack("example", () => {
      intent({ summary: "example own servertrustgroups server trust group" });

      resource(ServertrustgroupsServerTrustGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql.servertrustgroups_server_trust_group import ServertrustgroupsServerTrustGroup, ServertrustgroupsServerTrustGroupConfig

    def describe():
        ubx.intent("example own servertrustgroups server trust group")

        ubx.resource(ServertrustgroupsServerTrustGroup, "example", ServertrustgroupsServerTrustGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_servertrustgroups_server_trust_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a server trust group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_members" type="list(object)" required>
      Group members information for the server trust group. Required.
      <Expandable title="properties">
        <ResponseField name="server_id" type="string">
          The resource ID of a SQL server included in this trust group. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="trust_scopes" type="list(string)" required>
      Trust scope of the server trust group. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
