---
title: "azure_sql_jobexecutions_job_execution_2"
description: "`azure_sql_jobexecutions_job_execution_2` is a single run of an Elastic Database Jobs job against its own target group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own jobexecutions job execution 2"})

    		ubx.Resource(sql.JobexecutionsJobExecution2, "example", sql.JobexecutionsJobExecution2Config{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobexecutionsJobExecution2 } from "@ubx/sdk-azure/azure/sql/jobexecutions_job_execution_2";

    export default stack("example", () => {
      intent({ summary: "example own jobexecutions job execution 2" });

      resource(JobexecutionsJobExecution2, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import JobexecutionsJobExecution2, JobexecutionsJobExecution2Config

    def describe():
        ubx.intent("example own jobexecutions job execution 2")

        ubx.resource(JobexecutionsJobExecution2, "example", JobexecutionsJobExecution2Config(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_jobexecutions_job_execution_2, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="properties" type="object">
  Properties for an Azure SQL Database Elastic job execution. Computed by Azure after `ship`.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      The time that the job execution was created. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="current_attempt_start_time" type="string">
      Start time of the current attempt. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="current_attempts" type="number">
      Number of times the job execution has been attempted. Optional.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The time that the job execution completed. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="job_execution_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="job_version" type="number">
      The job version number. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_message" type="string">
      The last status or error message. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="lifecycle" type="string">
      The detailed state of the job execution. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The ARM provisioning state of the job execution. Optional.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      The time that the job execution started. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="step_id" type="number">
      The job step id. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="step_name" type="string">
      The job step name. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="target" type="object">
      The target that a job execution is executed on. Optional.
      <Expandable title="properties">
        <ResponseField name="database_name" type="string">
          The database name. Computed by Azure after `ship`.
        </ResponseField>
        <ResponseField name="server_name" type="string">
          The server name. Computed by Azure after `ship`.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the target. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
