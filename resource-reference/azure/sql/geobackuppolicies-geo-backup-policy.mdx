---
title: "azure_sql_geobackuppolicies_geo_backup_policy"
description: "`azure_sql_geobackuppolicies_geo_backup_policy` controls whether an Azure SQL Database's own automated backups are geo-replicated to a paired region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_geobackuppolicies --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_geobackuppolicies/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_geobackuppolicies/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own geobackuppolicies geo backup policy"})

    		ubx.Resource(sql.GeobackuppoliciesGeoBackupPolicy, "example", sql.GeobackuppoliciesGeoBackupPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_geobackuppolicies --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GeobackuppoliciesGeoBackupPolicy } from "./local-sdk/azure_sql_geobackuppolicies/sdk/typescript/azure_sql_geobackuppolicies/sql/geobackuppolicies_geo_backup_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own geobackuppolicies geo backup policy" });

      resource(GeobackuppoliciesGeoBackupPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_geobackuppolicies --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_geobackuppolicies/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_geobackuppolicies.sql import GeobackuppoliciesGeoBackupPolicy, GeobackuppoliciesGeoBackupPolicyConfig

    def describe():
        ubx.intent("example own geobackuppolicies geo backup policy")

        ubx.resource(GeobackuppoliciesGeoBackupPolicy, "example", GeobackuppoliciesGeoBackupPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_geobackuppolicies_geo_backup_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of geo backup policy. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Backup policy location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the geo backup policy. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="state" type="string" required>
      The state of the geo backup policy. Required.
    </ResponseField>
    <ResponseField name="storage_type" type="string">
      The storage type of the geo backup policy. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
