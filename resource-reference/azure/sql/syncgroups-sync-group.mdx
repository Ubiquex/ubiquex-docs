---
title: "azure_sql_syncgroups_sync_group"
description: "`azure_sql_syncgroups_sync_group` defines a bidirectional or one-way SQL Data Sync topology between an Azure SQL Database hub and its own member..."
---

`azure_sql_syncgroups_sync_group` defines a bidirectional or one-way SQL Data Sync topology between an Azure SQL Database hub and its own member database(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_sql_syncgroups --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_sql_syncgroups/sdk/go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_sql_syncgroups/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own syncgroups sync group"})

    		ubx.Resource(sql.SyncgroupsSyncGroup, "example", sql.SyncgroupsSyncGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_sql_syncgroups --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncgroupsSyncGroup } from "./local-sdk/azure_sql_syncgroups/sdk/typescript/azure_sql_syncgroups/sql/syncgroups_sync_group.ts";

    export default stack("example", () => {
      intent({ summary: "example own syncgroups sync group" });

      resource(SyncgroupsSyncGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_sql_syncgroups --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_sql_syncgroups/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_sql_syncgroups.sql import SyncgroupsSyncGroup, SyncgroupsSyncGroupConfig

    def describe():
        ubx.intent("example own syncgroups sync group")

        ubx.resource(SyncgroupsSyncGroup, "example", SyncgroupsSyncGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_syncgroups_sync_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a sync group with support to MI. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="conflict_logging_retention_in_days" type="number">
      Conflict logging retention period. Optional.
    </ResponseField>
    <ResponseField name="conflict_resolution_policy" type="string">
      Conflict resolution policy of the sync group. Optional.
    </ResponseField>
    <ResponseField name="enable_conflict_logging" type="bool">
      If conflict logging is enabled. Optional.
    </ResponseField>
    <ResponseField name="hub_database_password" type="string">
      Password for the sync group hub database credential. Optional.
    </ResponseField>
    <ResponseField name="hub_database_user_name" type="string">
      User name for the sync group hub database credential. Optional.
    </ResponseField>
    <ResponseField name="interval" type="number">
      Sync interval of the sync group. Optional.
    </ResponseField>
    <ResponseField name="last_sync_time" type="string">
      Last sync time of the sync group. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint_name" type="string">
      Private endpoint name of the sync group if use private link connection is enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="schema" type="object">
      Properties of sync group schema. Optional.
      <Expandable title="properties">
        <ResponseField name="master_sync_member_name" type="string">
          Name of master sync member where the schema is from. Optional.
        </ResponseField>
        <ResponseField name="tables" type="list(object)">
          List of tables in sync group schema. Optional.
          <Expandable title="properties">
            <ResponseField name="columns" type="list(object)">
              The column(s) of this table included in the sync group's own schema. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="data_size" type="string">
                  The size (e.g. length/precision) of this column's own data type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="data_type" type="string">
                  This column's own SQL data type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="quoted_name" type="string">
                  This column's own name, quoted as needed for use in SQL statements. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="quoted_name" type="string">
              This table's own name, quoted as needed for use in SQL statements. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sync_database_id" type="string">
      ARM resource id of the sync database in the sync group. Optional.
    </ResponseField>
    <ResponseField name="sync_state" type="string">
      Sync state of the sync group. Optional.
    </ResponseField>
    <ResponseField name="use_private_link_connection" type="bool">
      If use private link connection is enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  An ARM Resource SKU. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Capacity of the particular SKU. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU, typically, a letter + Number code, e.g. P3. Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      Size of the particular SKU Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier or edition of the particular SKU, e.g. Basic, Premium. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
