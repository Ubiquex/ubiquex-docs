---
title: "azure_sql_encryptionprotectors_encryption_protector"
description: "`azure_sql_encryptionprotectors_encryption_protector` designates the customer-managed key used to protect transparent data encryption keys on an Azure..."
---

`azure_sql_encryptionprotectors_encryption_protector` designates the customer-managed key used to protect transparent data encryption keys on an Azure SQL logical server.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own encryptionprotectors encryption protector"})

    		ubx.Resource(sql.EncryptionprotectorsEncryptionProtector, "example", sql.EncryptionprotectorsEncryptionProtectorConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EncryptionprotectorsEncryptionProtector } from "@ubx/sdk-azure/azure/sql/encryptionprotectors_encryption_protector";

    export default stack("example", () => {
      intent({ summary: "example own encryptionprotectors encryption protector" });

      resource(EncryptionprotectorsEncryptionProtector, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import EncryptionprotectorsEncryptionProtector, EncryptionprotectorsEncryptionProtectorConfig

    def describe():
        ubx.intent("example own encryptionprotectors encryption protector")

        ubx.resource(EncryptionprotectorsEncryptionProtector, "example", EncryptionprotectorsEncryptionProtectorConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_encryptionprotectors_encryption_protector, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of encryption protector. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties for an encryption protector execution. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="auto_rotation_enabled" type="bool">
      Key auto rotation opt-in flag. Either true or false. Optional.
    </ResponseField>
    <ResponseField name="key_version" type="string">
      The version of the server key being used as encryption protector Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="server_key_name" type="string">
      The name of the server key. Optional.
    </ResponseField>
    <ResponseField name="server_key_type" type="string" required>
      The encryption protector type like 'ServiceManaged', 'AzureKeyVault'. Required.
    </ResponseField>
    <ResponseField name="subregion" type="string">
      Subregion of the encryption protector. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="thumbprint" type="string">
      Thumbprint of the server key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="uri" type="string">
      The URI of the server key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
