---
title: "azure_sql_jobagents_job_agent"
description: "`azure_sql_jobagents_job_agent` is an Elastic Database Jobs agent, running scheduled T-SQL script execution across one or more Azure SQL Database(s)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/sql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own jobagents job agent"})

    		ubx.Resource(sql.JobagentsJobAgent, "example", sql.JobagentsJobAgentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobagentsJobAgent } from "@ubx/sdk-azure/azure/sql/jobagents_job_agent";

    export default stack("example", () => {
      intent({ summary: "example own jobagents job agent" });

      resource(JobagentsJobAgent, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.sql import JobagentsJobAgent, JobagentsJobAgentConfig

    def describe():
        ubx.intent("example own jobagents job agent")

        ubx.resource(JobagentsJobAgent, "example", JobagentsJobAgentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_sql_jobagents_job_agent, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Azure Active Directory identity configuration for a resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="tenant_id" type="string">
      Universally Unique Identifier Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The job agent identity type Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The resource ids of the user assigned identities to use Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of a job agent. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="database_id" type="string" required>
      Resource ID of the database to store job metadata in. Required.
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of the job agent. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  An ARM Resource SKU. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Capacity of the particular SKU. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU, typically, a letter + Number code, e.g. P3. Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      Size of the particular SKU Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier or edition of the particular SKU, e.g. Basic, Premium. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
