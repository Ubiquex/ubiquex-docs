---
title: "azure_maps_management_creator"
description: "`azure_maps_management_creator` provisions Azure Maps Creator..."
---

`azure_maps_management_creator` provisions Azure Maps Creator, letting custom indoor map data be uploaded and converted into a usable indoor mapping dataset.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_maps_management --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_maps_management/sdk/go
    package main

    import (
    	maps "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_maps_management/maps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maps management creator"})

    		ubx.Resource(maps.MapsManagementCreator, "example", maps.MapsManagementCreatorConfig{
    			Properties: map[string]any{"storage_units": 1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_maps_management --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MapsManagementCreator } from "./local-sdk/azure_maps_management/sdk/typescript/azure_maps_management/maps/maps_management_creator.ts";

    export default stack("example", () => {
      intent({ summary: "example own maps management creator" });

      resource(MapsManagementCreator, "example", {
        properties: { storageUnits: 1 },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_maps_management --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_maps_management/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_maps_management.maps import MapsManagementCreator, MapsManagementCreatorConfig

    def describe():
        ubx.intent("example own maps management creator")

        ubx.resource(MapsManagementCreator, "example", MapsManagementCreatorConfig(
            properties={"storage_units": 1},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_maps_management_creator, with properties a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Creator resource properties Required.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The state of the resource provisioning, terminal states: Succeeded, Failed, Canceled Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_units" type="number" required>
      The storage units to be allocated. Integer values from 1 to 100, inclusive. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
