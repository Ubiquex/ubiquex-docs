---
title: "azurerm_marketplace_agreement"
description: "Real, generated bindings for `azurerm_marketplace_agreement`."
---

`azurerm_marketplace_agreement` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	marketplace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agreement"})

    		ubx.Resource(marketplace.Agreement, "ci-runner", marketplace.AgreementConfig{
    			Offer:     "example",
    			Plan:      "example",
    			Publisher: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Agreement } from "jsr:@ubx/sdk-azure/azurerm/marketplace/agreement";

    export default stack("payments", () => {
      intent({ summary: "payments own agreement" });

      resource(Agreement, "ci-runner", {
        offer: "example",
        plan: "example",
        publisher: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.marketplace import Agreement, AgreementConfig

    def describe():
        ubx.intent("payments own agreement")

        ubx.resource(Agreement, "ci-runner", AgreementConfig(
            offer="example",
            plan="example",
            publisher="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_marketplace_agreement, with offer example, plan
    example, publisher example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="offer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="plan" type="string" required>
  Required.
</ResponseField>

<ResponseField name="publisher" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="license_text_link" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="privacy_policy_link" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
