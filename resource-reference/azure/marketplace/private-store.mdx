---
title: "azure_marketplace_private_store"
description: "`azure_marketplace_private_store` is an organization's own curated, access-restricted view of Azure Marketplace..."
---

`azure_marketplace_private_store` is an organization's own curated, access-restricted view of Azure Marketplace, limiting which published offers members can discover and deploy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	marketplace "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own private store"})

    		ubx.Resource(marketplace.PrivateStore, "example", marketplace.PrivateStoreConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateStore } from "./local-sdk/azure/sdk/typescript/azure/marketplace/private_store.ts";

    export default stack("example", () => {
      intent({ summary: "example own private store" });

      resource(PrivateStore, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.marketplace import PrivateStore, PrivateStoreConfig

    def describe():
        ubx.intent("example own private store")

        ubx.resource(PrivateStore, "example", PrivateStoreConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_marketplace_private_store, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the json payload on whether or not the private store is enabled for a given tenant Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="availability" type="string">
      Indicates private store availability Optional.
    </ResponseField>
    <ResponseField name="branding" type="map(string)">
      Gets or sets list of branding characteristics Optional.
    </ResponseField>
    <ResponseField name="collection_ids" type="list(string)">
      Gets list of associated collection ids Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="e_tag" type="string">
      Identifier for purposes of race condition Optional.
    </ResponseField>
    <ResponseField name="is_gov" type="bool">
      Is government Optional.
    </ResponseField>
    <ResponseField name="notifications_settings" type="object">
      Describes the json payload for notifications settings Optional.
      <Expandable title="properties">
        <ResponseField name="recipients" type="list(object)">
          Gets or sets list of notified recipients for new requests Optional.
          <Expandable title="properties">
            <ResponseField name="display_name" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="email_address" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
            <ResponseField name="principal_id" type="string">
              Optional; if omitted, computed by Microsoft Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="send_to_all_marketplace_admins" type="bool">
          Gets or sets whether to send email to all marketplace admins for new requests Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_store_id" type="string">
      Private Store id Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_store_name" type="string">
      Private Store Name Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      Tenant id Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
