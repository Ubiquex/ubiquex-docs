---
title: "azure_iothub_event_hub_consumer_group_info"
description: "`azure_iothub_event_hub_consumer_group_info` defines a named consumer group on an IoT Hub's own built-in Event Hub-compatible telemetry..."
---

`azure_iothub_event_hub_consumer_group_info` defines a named consumer group on an IoT Hub's own built-in Event Hub-compatible telemetry endpoint, letting multiple independent consumer(s) read device-to-cloud messages at their own pace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_iothub_iothub --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_iothub_iothub/sdk/go
    package main

    import (
    	iothub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_iothub_iothub/iothub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own iothub event hub consumer group info"})

    		ubx.Resource(iothub.IothubEventHubConsumerGroupInfo, "example", iothub.IothubEventHubConsumerGroupInfoConfig{
    			Properties: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_iothub_iothub --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { IothubEventHubConsumerGroupInfo } from "./local-sdk/azure_iothub_iothub/sdk/typescript/azure_iothub_iothub/iothub/iothub_event_hub_consumer_group_info.ts";

    export default stack("example", () => {
      intent({ summary: "example own iothub event hub consumer group info" });

      resource(IothubEventHubConsumerGroupInfo, "example", {
        properties: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_iothub_iothub --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_iothub_iothub/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_iothub_iothub.iothub import IothubEventHubConsumerGroupInfo, IothubEventHubConsumerGroupInfoConfig

    def describe():
        ubx.intent("example own iothub event hub consumer group info")

        ubx.resource(IothubEventHubConsumerGroupInfo, "example", IothubEventHubConsumerGroupInfoConfig(
            properties={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_iothub_event_hub_consumer_group_info, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object" required>
  The EventHub consumer group name. Required.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      EventHub consumer group name Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  The etag. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The Event Hub-compatible consumer group identifier. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The Event Hub-compatible consumer group name. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  the resource type. Computed by Microsoft Azure after `ship`.
</ResponseField>
