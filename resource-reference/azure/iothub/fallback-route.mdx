---
title: "azurerm_iothub_fallback_route"
description: "Real, generated bindings for `azurerm_iothub_fallback_route`."
---

`azurerm_iothub_fallback_route` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iothub "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/iothub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fallback route"})

    		ubx.Resource(iothub.FallbackRoute, "ci-runner", iothub.FallbackRouteConfig{
    			Enabled:           true,
    			EndpointNames:     []string{"example"},
    			IothubName:        "example-iothub-name",
    			ResourceGroupName: "example-resource-group-name",
    			Condition:         "example",
    			Source:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FallbackRoute } from "jsr:@ubx/sdk-azure/azurerm/iothub/fallback_route";

    export default stack("payments", () => {
      intent({ summary: "payments own fallback route" });

      resource(FallbackRoute, "ci-runner", {
        enabled: true,
        endpointNames: ["example"],
        iothubName: "example-iothub-name",
        resourceGroupName: "example-resource-group-name",
        condition: "example",
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.iothub import FallbackRoute, FallbackRouteConfig

    def describe():
        ubx.intent("payments own fallback route")

        ubx.resource(FallbackRoute, "ci-runner", FallbackRouteConfig(
            enabled=True,
            endpoint_names=["example"],
            iothub_name="example-iothub-name",
            resource_group_name="example-resource-group-name",
            condition="example",
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_iothub_fallback_route, with enabled true, endpoint
    names a set value, iothub name example-iothub-name, resource
    group name example-resource-group-name, condition example, source
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="condition" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="endpoint_names" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="iothub_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
