---
title: "azure_vmware_workload_network_public_ip"
description: "`azure_vmware_vmware_workload_network_public_ip` assigns a public IP address reachable by VM workload(s) within an Azure VMware Solution private..."
---

`azure_vmware_vmware_workload_network_public_ip` assigns a public IP address reachable by VM workload(s) within an Azure VMware Solution private cloud's own NSX-T environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware workload network public ip"})

    		ubx.Resource(vmware.VmwareWorkloadNetworkPublicIp, "example", vmware.VmwareWorkloadNetworkPublicIpConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareWorkloadNetworkPublicIp } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_workload_network_public_ip.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware workload network public ip" });

      resource(VmwareWorkloadNetworkPublicIp, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareWorkloadNetworkPublicIp, VmwareWorkloadNetworkPublicIpConfig

    def describe():
        ubx.intent("example own vmware workload network public ip")

        ubx.resource(VmwareWorkloadNetworkPublicIp, "example", VmwareWorkloadNetworkPublicIpConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_public_ip, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX Public IP Block Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Display name of the Public IP Block. Optional.
    </ResponseField>
    <ResponseField name="number_of_public_ips" type="number">
      Number of Public IPs requested. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network Public IP provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="public_ipblock" type="string">
      CIDR Block of the Public IP Block. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
