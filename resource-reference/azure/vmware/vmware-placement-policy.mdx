---
title: "azure_vmware_placement_policy"
description: "`azure_vmware_placement_policy` constrains which host(s) or VM(s) can run together (or must run apart) within an Azure VMware Solution cluster..."
---

`azure_vmware_placement_policy` constrains which host(s) or VM(s) can run together (or must run apart) within an Azure VMware Solution cluster, for licensing or availability requirements.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware placement policy"})

    		ubx.Resource(vmware.VmwarePlacementPolicy, "example", vmware.VmwarePlacementPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwarePlacementPolicy } from "./local-sdk/azure_vmware/sdk/typescript/azure_vmware/vmware/vmware_placement_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware placement policy" });

      resource(VmwarePlacementPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware.vmware import VmwarePlacementPolicy, VmwarePlacementPolicyConfig

    def describe():
        ubx.intent("example own vmware placement policy")

        ubx.resource(VmwarePlacementPolicy, "example", VmwarePlacementPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_placement_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Abstract placement policy properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Display name of the placement policy Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Placement Policy provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="state" type="string">
      Placement Policy state Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Placement Policy type Required.
    </ResponseField>
  </Expandable>
</ResponseField>
