---
title: "azure_vmware_pure_storage_policy"
description: "`azure_vmware_pure_storage_policy` defines the vVols storage policy applied to VM(s) using Pure Storage Cloud as a datastore within an Azure..."
---

`azure_vmware_pure_storage_policy` defines the vVols storage policy applied to VM(s) using Pure Storage Cloud as a datastore within an Azure VMware Solution private cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware pure storage policy"})

    		ubx.Resource(vmware.PureStoragePolicy, "example", vmware.PureStoragePolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PureStoragePolicy } from "@ubx/sdk-azure/azure/vmware/pure_storage_policy";

    export default stack("example", () => {
      intent({ summary: "example own vmware pure storage policy" });

      resource(PureStoragePolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vmware.pure_storage_policy import PureStoragePolicy, PureStoragePolicyConfig

    def describe():
        ubx.intent("example own vmware pure storage policy")

        ubx.resource(PureStoragePolicy, "example", PureStoragePolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_pure_storage_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a Pure Storage Policy Based Management policy Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Pure Storage Policy Based Management policy provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_policy_definition" type="string" required>
      Definition of a Pure Storage Policy Based Management policy Required.
    </ResponseField>
    <ResponseField name="storage_pool_id" type="string" required>
      Azure resource ID of the Pure Storage Pool associated with the storage policy Required.
    </ResponseField>
  </Expandable>
</ResponseField>
