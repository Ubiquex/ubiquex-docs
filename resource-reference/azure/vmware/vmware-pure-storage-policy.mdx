---
title: "azure_vmware_pure_storage_policy"
description: "`azure_vmware_vmware_pure_storage_policy` defines the vVols storage policy applied to VM(s) using Pure Storage Cloud as a datastore within an Azure..."
---

`azure_vmware_vmware_pure_storage_policy` defines the vVols storage policy applied to VM(s) using Pure Storage Cloud as a datastore within an Azure VMware Solution private cloud.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vmware pure storage policy"})

    		ubx.Resource(vmware.VmwarePureStoragePolicy, "ci-runner", vmware.VmwarePureStoragePolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwarePureStoragePolicy } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_pure_storage_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vmware pure storage policy" });

      resource(VmwarePureStoragePolicy, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwarePureStoragePolicy, VmwarePureStoragePolicyConfig

    def describe():
        ubx.intent("payments own vmware pure storage policy")

        ubx.resource(VmwarePureStoragePolicy, "ci-runner", VmwarePureStoragePolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_pure_storage_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a Pure Storage Policy Based Management policy Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Pure Storage Policy Based Management policy provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="storage_policy_definition" type="string" required>
      Definition of a Pure Storage Policy Based Management policy Required.
    </ResponseField>
    <ResponseField name="storage_pool_id" type="string" required>
      Azure resource ID of the Pure Storage Pool associated with the storage policy Required.
    </ResponseField>
  </Expandable>
</ResponseField>
