---
title: "azure_vmware_workload_network_dns_service"
description: "`azure_vmware_workload_network_dns_service` configures a DNS forwarding service for VM workload network(s) within an Azure VMware Solution..."
---

`azure_vmware_workload_network_dns_service` configures a DNS forwarding service for VM workload network(s) within an Azure VMware Solution private cloud's own NSX-T environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware workload network dns service"})

    		ubx.Resource(vmware.WorkloadNetworkDnsService, "example", vmware.WorkloadNetworkDnsServiceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadNetworkDnsService } from "@ubx/sdk-azure/azure/vmware/workload_network_dns_service";

    export default stack("example", () => {
      intent({ summary: "example own vmware workload network dns service" });

      resource(WorkloadNetworkDnsService, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vmware.workload_network_dns_service import WorkloadNetworkDnsService, WorkloadNetworkDnsServiceConfig

    def describe():
        ubx.intent("example own vmware workload network dns service")

        ubx.resource(WorkloadNetworkDnsService, "example", WorkloadNetworkDnsServiceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_dns_service, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX DNS Service Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="default_dns_zone" type="string">
      Default DNS zone of the DNS Service. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Display name of the DNS Service. Optional.
    </ResponseField>
    <ResponseField name="dns_service_ip" type="string">
      DNS service IP of the DNS Service. Optional.
    </ResponseField>
    <ResponseField name="fqdn_zones" type="list(string)">
      FQDN zones of the DNS Service. Optional.
    </ResponseField>
    <ResponseField name="log_level" type="string">
      DNS service log level Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network DNS Service provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      DNS service status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
