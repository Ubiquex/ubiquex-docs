---
title: "azure_vmware_workload_network_vmgroup"
description: "`azure_vmware_vmware_workload_network_vmgroup` groups VM(s) together within an Azure VMware Solution private cloud's own NSX-T environment..."
---

`azure_vmware_vmware_workload_network_vmgroup` groups VM(s) together within an Azure VMware Solution private cloud's own NSX-T environment, for applying network security or DFW rule(s) collectively.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware workload network vmgroup"})

    		ubx.Resource(vmware.WorkloadNetworkVmgroup, "example", vmware.WorkloadNetworkVmgroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadNetworkVmgroup } from "@ubx/sdk-azure/azure/vmware/workload_network_vmgroup";

    export default stack("example", () => {
      intent({ summary: "example own vmware workload network vmgroup" });

      resource(WorkloadNetworkVmgroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vmware import WorkloadNetworkVmgroup, WorkloadNetworkVmgroupConfig

    def describe():
        ubx.intent("example own vmware workload network vmgroup")

        ubx.resource(WorkloadNetworkVmgroup, "example", WorkloadNetworkVmgroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_vmgroup, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX VM Group Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Display name of the VM group. Optional.
    </ResponseField>
    <ResponseField name="members" type="list(string)">
      Virtual machine members of this group. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network VM Group provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      VM group status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
