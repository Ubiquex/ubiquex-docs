---
title: "azure_vmware_private_cloud"
description: "`azure_vmware_private_cloud` is the top-level Azure VMware Solution resource -- a dedicated, VMware-based private cloud environment (vCenter..."
---

`azure_vmware_private_cloud` is the top-level Azure VMware Solution resource -- a dedicated, VMware-based private cloud environment (vCenter, NSX-T, vSAN) running on Azure infrastructure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware private cloud"})

    		ubx.Resource(vmware.VmwarePrivateCloud, "example", vmware.VmwarePrivateCloudConfig{
    			Sku: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwarePrivateCloud } from "./local-sdk/azure_vmware/sdk/typescript/azure_vmware/vmware/vmware_private_cloud.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware private cloud" });

      resource(VmwarePrivateCloud, "example", {
        sku: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware.vmware import VmwarePrivateCloud, VmwarePrivateCloudConfig

    def describe():
        ubx.intent("example own vmware private cloud")

        ubx.resource(VmwarePrivateCloud, "example", VmwarePrivateCloudConfig(
            sku={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_private_cloud, with sku a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (either system assigned, or none) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (either system assigned, or none). Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a private cloud resource Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="availability" type="object">
      The properties describing private cloud availability zone distribution Optional.
      <Expandable title="properties">
        <ResponseField name="secondary_zone" type="number">
          The secondary availability zone for the private cloud Optional.
        </ResponseField>
        <ResponseField name="strategy" type="string">
          Whether the private clouds is available in a single zone or two zones Optional.
        </ResponseField>
        <ResponseField name="zone" type="number">
          The primary availability zone for the private cloud Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="circuit" type="object">
      An ExpressRoute Circuit Optional.
      <Expandable title="properties">
        <ResponseField name="express_route_id" type="string">
          Identifier of the ExpressRoute Circuit (Microsoft Colo only) Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="express_route_private_peering_id" type="string">
          ExpressRoute Circuit private peering identifier Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="primary_subnet" type="string">
          CIDR of primary subnet Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="secondary_subnet" type="string">
          CIDR of secondary subnet Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dns_zone_type" type="string">
      The type of DNS zone. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      The properties of customer managed encryption key Optional.
      <Expandable title="properties">
        <ResponseField name="key_vault_properties" type="object">
          An Encryption Key Optional.
          <Expandable title="properties">
            <ResponseField name="auto_detected_key_version" type="string">
              The auto-detected version of the key if versionType is auto-detected. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="key_name" type="string">
              The name of the key. Optional.
            </ResponseField>
            <ResponseField name="key_state" type="string">
              Whether the the encryption key is connected or access denied Optional.
            </ResponseField>
            <ResponseField name="key_vault_url" type="string">
              The URL of the vault. Optional.
            </ResponseField>
            <ResponseField name="key_version" type="string">
              The version of the key. Optional.
            </ResponseField>
            <ResponseField name="version_type" type="string">
              Whether the encryption version is fixed or auto-detected Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          Whether encryption is enabled or disabled Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoints" type="object">
      Endpoint addresses Optional.
      <Expandable title="properties">
        <ResponseField name="hcx_cloud_manager" type="string">
          Endpoint FQDN for the HCX Cloud Manager Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="hcx_cloud_manager_ip" type="string">
          Endpoint IP for the HCX Cloud Manager Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="nsxt_manager" type="string">
          Endpoint FQDN for the NSX-T Data Center manager Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="nsxt_manager_ip" type="string">
          Endpoint IP for the NSX-T Data Center manager Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="vcenter_ip" type="string">
          Endpoint IP for Virtual Center Server Appliance Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="vcsa" type="string">
          Endpoint FQDN for Virtual Center Server Appliance Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="extended_network_blocks" type="list(string)">
      Array of additional networks noncontiguous with networkBlock. Networks must be unique and non-overlapping across VNet in your subscription, on-premise, and this privateCloud networkBlock attribute. Make sure the CIDR format conforms to (A.B.C.D/X). Optional.
    </ResponseField>
    <ResponseField name="external_cloud_links" type="list(string)">
      Array of cloud link IDs from other clouds that connect to this one Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="identity_sources" type="list(object)">
      vCenter Single Sign On Identity Sources Optional.
      <Expandable title="properties">
        <ResponseField name="alias" type="string">
          An alternate name this resource is also known by. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="base_group_dn" type="string">
          The distinguished name of the LDAP group search base. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="base_user_dn" type="string">
          The distinguished name of the LDAP user search base. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="domain" type="string">
          The Active Directory domain this connection joins. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="password" type="string">
          The password credential used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="primary_server" type="string">
          The address of the primary Active Directory server. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secondary_server" type="string">
          The address of the secondary (failover) Active Directory server. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ssl" type="string">
          Whether SSL/TLS is used for this connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="username" type="string">
          The username used to authenticate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="internet" type="string">
      Connectivity to internet is enabled or disabled Optional.
    </ResponseField>
    <ResponseField name="management_cluster" type="object" required>
      The properties of a management cluster Required.
      <Expandable title="properties">
        <ResponseField name="cluster_id" type="number">
          The identity Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="cluster_size" type="number">
          The cluster size Optional.
        </ResponseField>
        <ResponseField name="hosts" type="list(string)">
          The hosts Optional.
        </ResponseField>
        <ResponseField name="provisioning_state" type="string">
          Cluster provisioning state Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="vsan_datastore_name" type="string">
          Name of the vsan datastore associated with the cluster Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="management_network" type="string">
      Network used to access vCenter Server and NSX-T Manager Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="network_block" type="string" required>
      The block of addresses should be unique across VNet in your subscription as well as on-premise. Make sure the CIDR format is conformed to (A.B.C.D/X) where A,B,C,D are between 0 and 255, and X is between 0 and 22 Required.
    </ResponseField>
    <ResponseField name="nsx_public_ip_quota_raised" type="string">
      NSX public IP quota raised Optional.
    </ResponseField>
    <ResponseField name="nsxt_certificate_thumbprint" type="string">
      Thumbprint of the NSX-T Manager SSL certificate Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="nsxt_password" type="string">
      Optionally, set the NSX-T Manager password when the private cloud is created Optional.
    </ResponseField>
    <ResponseField name="provisioning_network" type="string">
      Used for virtual machine cold migration, cloning, and snapshot migration Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      private cloud provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="secondary_circuit" type="object">
      An ExpressRoute Circuit Optional.
      <Expandable title="properties">
        <ResponseField name="express_route_id" type="string">
          Identifier of the ExpressRoute Circuit (Microsoft Colo only) Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="express_route_private_peering_id" type="string">
          ExpressRoute Circuit private peering identifier Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="primary_subnet" type="string">
          CIDR of primary subnet Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="secondary_subnet" type="string">
          CIDR of secondary subnet Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vcenter_certificate_thumbprint" type="string">
      Thumbprint of the vCenter Server SSL certificate Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="vcenter_password" type="string">
      Optionally, set the vCenter admin password when the private cloud is created Optional.
    </ResponseField>
    <ResponseField name="vcf_license" type="object">
      A VMware Cloud Foundation license Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string" required>
          The kind of license. Required.
        </ResponseField>
        <ResponseField name="provisioning_state" type="string">
          provisioning state of the license Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_network_id" type="string">
      Azure resource ID of the virtual network Optional.
    </ResponseField>
    <ResponseField name="vmotion_network" type="string">
      Used for live migration of virtual machines Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object" required>
  The resource model definition representing SKU Required.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. E.g. P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The availability zones. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
