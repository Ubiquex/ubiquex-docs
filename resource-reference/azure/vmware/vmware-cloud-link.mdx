---
title: "azure_vmware_cloud_link"
description: "`azure_vmware_vmware_cloud_link` connects two Azure VMware Solution private clouds together for cross-private-cloud VM migration and networking."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware cloud link"})

    		ubx.Resource(vmware.VmwareCloudLink, "example", vmware.VmwareCloudLinkConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareCloudLink } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_cloud_link.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware cloud link" });

      resource(VmwareCloudLink, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareCloudLink, VmwareCloudLinkConfig

    def describe():
        ubx.intent("example own vmware cloud link")

        ubx.resource(VmwareCloudLink, "example", VmwareCloudLinkConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_cloud_link, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a cloud link. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="linked_cloud" type="string">
      Identifier of the other private cloud participating in the link. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      cloud link provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Cloud Link status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
