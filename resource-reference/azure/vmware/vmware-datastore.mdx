---
title: "azure_vmware_datastore"
description: "`azure_vmware_vmware_datastore` attaches external storage (e.g. Azure NetApp Files..."
---

`azure_vmware_vmware_datastore` attaches external storage (e.g. Azure NetApp Files, Pure Storage Cloud) as a datastore to an Azure VMware Solution cluster, supplementing its own native vSAN capacity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware datastore"})

    		ubx.Resource(vmware.VmwareDatastore, "example", vmware.VmwareDatastoreConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareDatastore } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_datastore.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware datastore" });

      resource(VmwareDatastore, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareDatastore, VmwareDatastoreConfig

    def describe():
        ubx.intent("example own vmware datastore")

        ubx.resource(VmwareDatastore, "example", VmwareDatastoreConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_datastore, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a datastore Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="disk_pool_volume" type="object">
      An iSCSI volume from Microsoft.StoragePool provider Optional.
      <Expandable title="properties">
        <ResponseField name="lun_name" type="string" required>
          Name of the LUN to be used for datastore Required.
        </ResponseField>
        <ResponseField name="mount_option" type="string">
          Mode that describes whether the LUN has to be mounted as a datastore or attached as a LUN Optional.
        </ResponseField>
        <ResponseField name="path" type="string">
          The path for the vmware datastore's disk pool volume. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_id" type="string" required>
          Azure resource ID of the iSCSI target Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="elastic_san_volume" type="object">
      An Elastic SAN volume from Microsoft.ElasticSan provider Optional.
      <Expandable title="properties">
        <ResponseField name="target_id" type="string" required>
          Azure resource ID of the Elastic SAN Volume Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="net_app_volume" type="object">
      An Azure NetApp Files volume from Microsoft.NetApp provider Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string" required>
          Azure resource ID of the NetApp volume Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      datastore provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="pure_storage_volume" type="object">
      A Pure Storage volume from PureStorage.Block provider Optional.
      <Expandable title="properties">
        <ResponseField name="size_gb" type="number" required>
          Volume size to be used to create a Virtual Volumes (vVols) datastore Required.
        </ResponseField>
        <ResponseField name="storage_pool_id" type="string" required>
          Azure resource ID of the Pure Storage Pool Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      datastore status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
