---
title: "azurerm_vmware_express_route_authorization"
description: "Real, generated bindings for `azurerm_vmware_express_route_authorization`."
---

`azurerm_vmware_express_route_authorization` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own express route authorization"})

    		ubx.Resource(vmware.ExpressRouteAuthorization, "ci-runner", vmware.ExpressRouteAuthorizationConfig{
    			Name:           "ci-runner",
    			PrivateCloudId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressRouteAuthorization } from "jsr:@ubx/sdk-azure/azurerm/vmware/express_route_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own express route authorization" });

      resource(ExpressRouteAuthorization, "ci-runner", {
        name: "ci-runner",
        privateCloudId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.vmware import ExpressRouteAuthorization, ExpressRouteAuthorizationConfig

    def describe():
        ubx.intent("payments own express route authorization")

        ubx.resource(ExpressRouteAuthorization, "ci-runner", ExpressRouteAuthorizationConfig(
            name="ci-runner",
            private_cloud_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_vmware_express_route_authorization called
    "ci-runner", with private cloud id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="private_cloud_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="express_route_authorization_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="express_route_authorization_key" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
