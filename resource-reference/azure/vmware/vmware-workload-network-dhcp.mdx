---
title: "azure_vmware_workload_network_dhcp"
description: "`azure_vmware_workload_network_dhcp` configures a DHCP server or relay for VM workload network(s) within an Azure VMware Solution private..."
---

`azure_vmware_workload_network_dhcp` configures a DHCP server or relay for VM workload network(s) within an Azure VMware Solution private cloud's own NSX-T environment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware workload network dhcp"})

    		ubx.Resource(vmware.VmwareWorkloadNetworkDhcp, "example", vmware.VmwareWorkloadNetworkDhcpConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareWorkloadNetworkDhcp } from "./local-sdk/azure_vmware/sdk/typescript/azure_vmware/vmware/vmware_workload_network_dhcp.ts";

    export default stack("example", () => {
      intent({ summary: "example own vmware workload network dhcp" });

      resource(VmwareWorkloadNetworkDhcp, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware.vmware import VmwareWorkloadNetworkDhcp, VmwareWorkloadNetworkDhcpConfig

    def describe():
        ubx.intent("example own vmware workload network dhcp")

        ubx.resource(VmwareWorkloadNetworkDhcp, "example", VmwareWorkloadNetworkDhcpConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_dhcp, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Base class for WorkloadNetworkDhcpServer and WorkloadNetworkDhcpRelay to inherit from Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="dhcp_type" type="string" required>
      Type of DHCP: SERVER or RELAY. Required.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Display name of the DHCP entity. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network DHCP provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="segments" type="list(string)">
      NSX Segments consuming DHCP. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
