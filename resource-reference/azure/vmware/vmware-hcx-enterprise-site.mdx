---
title: "azure_vmware_hcx_enterprise_site"
description: "`azure_vmware_hcx_enterprise_site` activates a VMware HCX site connecting an on-premises VMware environment to an Azure VMware Solution private..."
---

`azure_vmware_hcx_enterprise_site` activates a VMware HCX site connecting an on-premises VMware environment to an Azure VMware Solution private cloud, for live workload migration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware hcx enterprise site"})

    		ubx.Resource(vmware.HcxEnterpriseSite, "example", vmware.HcxEnterpriseSiteConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HcxEnterpriseSite } from "@ubx/sdk-azure/azure/vmware/hcx_enterprise_site";

    export default stack("example", () => {
      intent({ summary: "example own vmware hcx enterprise site" });

      resource(HcxEnterpriseSite, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vmware.hcx_enterprise_site import HcxEnterpriseSite, HcxEnterpriseSiteConfig

    def describe():
        ubx.intent("example own vmware hcx enterprise site")

        ubx.resource(HcxEnterpriseSite, "example", HcxEnterpriseSiteConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_hcx_enterprise_site, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of an HCX Enterprise Site Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="activation_key" type="string">
      The activation key Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      HCX Enterprise Site provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="status" type="string">
      HCX Enterprise Site status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
