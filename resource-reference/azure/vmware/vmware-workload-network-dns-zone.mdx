---
title: "azure_vmware_workload_network_dns_zone"
description: "`azure_vmware_workload_network_dns_zone` defines a DNS zone resolved by an Azure VMware Solution private cloud's own workload network DNS service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vmware workload network dns zone"})

    		ubx.Resource(vmware.WorkloadNetworkDnsZone, "example", vmware.WorkloadNetworkDnsZoneConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadNetworkDnsZone } from "@ubx/sdk-azure/azure/vmware/workload_network_dns_zone";

    export default stack("example", () => {
      intent({ summary: "example own vmware workload network dns zone" });

      resource(WorkloadNetworkDnsZone, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.vmware.workload_network_dns_zone import WorkloadNetworkDnsZone, WorkloadNetworkDnsZoneConfig

    def describe():
        ubx.intent("example own vmware workload network dns zone")

        ubx.resource(WorkloadNetworkDnsZone, "example", WorkloadNetworkDnsZoneConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_dns_zone, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX DNS Zone Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Display name of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="dns_server_ips" type="list(string)">
      DNS Server IP array of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="dns_services" type="number">
      Number of DNS Services using the DNS zone. Optional.
    </ResponseField>
    <ResponseField name="domain" type="list(string)">
      Domain names of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network DNS Zone provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="source_ip" type="string">
      Source IP of the DNS Zone. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
