---
title: "azure_vmware_vmware_workload_network_dns_zone"
description: "`azure_vmware_vmware_workload_network_dns_zone` defines a DNS zone resolved by an Azure VMware Solution private cloud's own workload network DNS service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vmware workload network dns zone"})

    		ubx.Resource(vmware.VmwareWorkloadNetworkDnsZone, "ci-runner", vmware.VmwareWorkloadNetworkDnsZoneConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareWorkloadNetworkDnsZone } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_workload_network_dns_zone.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vmware workload network dns zone" });

      resource(VmwareWorkloadNetworkDnsZone, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareWorkloadNetworkDnsZone, VmwareWorkloadNetworkDnsZoneConfig

    def describe():
        ubx.intent("payments own vmware workload network dns zone")

        ubx.resource(VmwareWorkloadNetworkDnsZone, "ci-runner", VmwareWorkloadNetworkDnsZoneConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_vmware_workload_network_dns_zone, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX DNS Zone Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Display name of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="dns_server_ips" type="list(string)">
      DNS Server IP array of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="dns_services" type="number">
      Number of DNS Services using the DNS zone. Optional.
    </ResponseField>
    <ResponseField name="domain" type="list(string)">
      Domain names of the DNS Zone. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network DNS Zone provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="source_ip" type="string">
      Source IP of the DNS Zone. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
