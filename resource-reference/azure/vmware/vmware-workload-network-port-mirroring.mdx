---
title: "azure_vmware_workload_network_port_mirroring"
description: "`azure_vmware_vmware_workload_network_port_mirroring` mirrors traffic from one workload network segment to another within an Azure VMware Solution..."
---

`azure_vmware_vmware_workload_network_port_mirroring` mirrors traffic from one workload network segment to another within an Azure VMware Solution private cloud's own NSX-T environment, for monitoring.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vmware workload network port mirroring"})

    		ubx.Resource(vmware.VmwareWorkloadNetworkPortMirroring, "ci-runner", vmware.VmwareWorkloadNetworkPortMirroringConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareWorkloadNetworkPortMirroring } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_workload_network_port_mirroring.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vmware workload network port mirroring" });

      resource(VmwareWorkloadNetworkPortMirroring, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareWorkloadNetworkPortMirroring, VmwareWorkloadNetworkPortMirroringConfig

    def describe():
        ubx.intent("payments own vmware workload network port mirroring")

        ubx.resource(VmwareWorkloadNetworkPortMirroring, "ci-runner", VmwareWorkloadNetworkPortMirroringConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_workload_network_port_mirroring, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NSX Port Mirroring Properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="destination" type="string">
      Destination VM Group. Optional.
    </ResponseField>
    <ResponseField name="direction" type="string">
      Port Mirroring Direction Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string">
      Display name of the port mirroring profile. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Workload Network Port Mirroring provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="revision" type="number">
      NSX revision number. Optional.
    </ResponseField>
    <ResponseField name="source" type="string">
      Source VM Group. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Port Mirroring status Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
