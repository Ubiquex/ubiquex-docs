---
title: "azure_vmware_global_reach_connection"
description: "`azure_vmware_vmware_global_reach_connection` peers an Azure VMware Solution private cloud's own ExpressRoute circuit with another ExpressRoute..."
---

`azure_vmware_vmware_global_reach_connection` peers an Azure VMware Solution private cloud's own ExpressRoute circuit with another ExpressRoute circuit, extending connectivity to on-premises or another Azure VNet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_vmware_vmware --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_vmware_vmware/sdk/go
    package main

    import (
    	vmware "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_vmware_vmware/vmware"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vmware global reach connection"})

    		ubx.Resource(vmware.VmwareGlobalReachConnection, "ci-runner", vmware.VmwareGlobalReachConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_vmware_vmware --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VmwareGlobalReachConnection } from "./local-sdk/azure_vmware_vmware/sdk/typescript/azure_vmware_vmware/vmware/vmware_global_reach_connection.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vmware global reach connection" });

      resource(VmwareGlobalReachConnection, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_vmware_vmware --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_vmware_vmware/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_vmware_vmware.vmware import VmwareGlobalReachConnection, VmwareGlobalReachConnectionConfig

    def describe():
        ubx.intent("payments own vmware global reach connection")

        ubx.resource(VmwareGlobalReachConnection, "ci-runner", VmwareGlobalReachConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_vmware_global_reach_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The properties of a global reach connection Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="address_prefix" type="string">
      The network used for global reach carved out from the original network block provided for the private cloud Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="authorization_key" type="string">
      Authorization key from the peer express route used for the global reach connection Optional.
    </ResponseField>
    <ResponseField name="circuit_connection_status" type="string">
      Global Reach Connection status Optional.
    </ResponseField>
    <ResponseField name="express_route_id" type="string">
      The ID of the Private Cloud's ExpressRoute Circuit that is participating in the global reach connection Optional.
    </ResponseField>
    <ResponseField name="peer_express_route_circuit" type="string">
      Identifier of the ExpressRoute Circuit to peer with in the global reach connection Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Global Reach Connection provisioning state Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
