---
title: "azure_machinelearningservices_openapi_model_container"
description: "`azure_machinelearningservices_openapi_model_container` is the named container in an Azure Machine Learning workspace that version(s) of a registered..."
---

`azure_machinelearningservices_openapi_model_container` is the named container in an Azure Machine Learning workspace that version(s) of a registered model are grouped under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi model container"})

    		ubx.Resource(machinelearningservices.OpenapiModelContainer, "example", machinelearningservices.OpenapiModelContainerConfig{
    			Properties: map[string]any{"provisioning_state": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiModelContainer } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_model_container";

    export default stack("example", () => {
      intent({ summary: "example own openapi model container" });

      resource(OpenapiModelContainer, "example", {
        properties: { provisioningState: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiModelContainer, OpenapiModelContainerConfig

    def describe():
        ubx.intent("example own openapi model container")

        ubx.resource(OpenapiModelContainer, "example", OpenapiModelContainerConfig(
            properties={"provisioning_state": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_model_container,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of registry asset. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
