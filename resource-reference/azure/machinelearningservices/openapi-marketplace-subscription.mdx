---
title: "azure_machinelearningservices_openapi_marketplace_subscription"
description: "`azure_machinelearningservices_openapi_marketplace_subscription` subscribes an Azure Machine Learning workspace to a model or offer available through..."
---

`azure_machinelearningservices_openapi_marketplace_subscription` subscribes an Azure Machine Learning workspace to a model or offer available through the Azure Marketplace, required before deploying certain third-party Model Catalog models.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi marketplace subscription"})

    		ubx.Resource(machinelearningservices.OpenapiMarketplaceSubscription, "example", machinelearningservices.OpenapiMarketplaceSubscriptionConfig{
    			Name:       "example",
    			Properties: map[string]any{"model_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiMarketplaceSubscription } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_marketplace_subscription";

    export default stack("example", () => {
      intent({ summary: "example own openapi marketplace subscription" });

      resource(OpenapiMarketplaceSubscription, "example", {
        name: "example",
        properties: { modelId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiMarketplaceSubscription, OpenapiMarketplaceSubscriptionConfig
    from ubx.azure.machinelearningservices.openapi_marketplace_subscription import OpenapiMarketplaceSubscription_Properties

    def describe():
        ubx.intent("example own openapi marketplace subscription")

        ubx.resource(OpenapiMarketplaceSubscription, "example", OpenapiMarketplaceSubscriptionConfig(
            name="example",
            properties=OpenapiMarketplaceSubscription_Properties(model_id="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_machinelearningservices_openapi_marketplace_subscription
    called "example", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="marketplace_plan" type="object">
      A reference to the Azure Marketplace plan this model is licensed under. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="offer_id" type="string">
          The identifying name of the Offer of the Marketplace Plan. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="plan_id" type="string">
          The identifying name of the Plan of the Marketplace Plan. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="publisher_id" type="string">
          The identifying name of the Publisher of the Marketplace Plan. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="marketplace_subscription_status" type="string">
      The current status of the Azure Marketplace subscription backing this model. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="model_id" type="string" required>
      [Required] Target Marketplace Model ID to create a Marketplace Subscription for. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The last, current provisioning status ARM reported for this resource, e.g. `Succeeded`, `Failed`, or `Updating`. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
