---
title: "azure_machinelearningservices_openapi_batch_deployment"
description: "`azure_machinelearningservices_openapi_batch_deployment` is a specific model deployment behind an Azure Machine Learning batch endpoint..."
---

`azure_machinelearningservices_openapi_batch_deployment` is a specific model deployment behind an Azure Machine Learning batch endpoint, processing large offline datasets asynchronously rather than serving real-time requests.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi batch deployment"})

    		ubx.Resource(machinelearningservices.OpenapiBatchDeployment, "example", machinelearningservices.OpenapiBatchDeploymentConfig{
    			Properties: map[string]any{"compute": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiBatchDeployment } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_batch_deployment";

    export default stack("example", () => {
      intent({ summary: "example own openapi batch deployment" });

      resource(OpenapiBatchDeployment, "example", {
        properties: { compute: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices.openapi_batch_deployment import OpenapiBatchDeployment, OpenapiBatchDeploymentConfig

    def describe():
        ubx.intent("example own openapi batch deployment")

        ubx.resource(OpenapiBatchDeployment, "example", OpenapiBatchDeploymentConfig(
            properties={"compute": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_batch_deployment,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Batch inference settings per deployment. Required.
  <Expandable title="properties">
    <ResponseField name="compute" type="string">
      Compute target for batch inference operation. Optional.
    </ResponseField>
    <ResponseField name="deployment_configuration" type="object">
      Properties relevant to different deployment types. Optional.
      <Expandable title="properties">
        <ResponseField name="deployment_configuration_type" type="string" required>
          The enumerated property types for batch deployments. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_threshold" type="number">
      Error threshold, if the error count for the entire input goes above this value, the batch inference will be aborted. Range is [-1, int.MaxValue]. For FileDataset, this value is the count of file failures. For TabularDataset, this value is the count of record failures. If set to -1 (the lower bound), all failures during batch inference will be ignored. Optional.
    </ResponseField>
    <ResponseField name="logging_level" type="string">
      Log verbosity for batch inferencing. Increasing verbosity order for logging is : Warning, Info and Debug. The default value is Info. Optional.
    </ResponseField>
    <ResponseField name="max_concurrency_per_instance" type="number">
      Indicates maximum number of parallelism per instance. Optional.
    </ResponseField>
    <ResponseField name="mini_batch_size" type="number">
      Size of the mini-batch passed to each batch invocation. For FileDataset, this is the number of files per mini-batch. For TabularDataset, this is the size of the records in bytes, per mini-batch. Optional.
    </ResponseField>
    <ResponseField name="model" type="object">
      Base definition for asset references. Optional.
      <Expandable title="properties">
        <ResponseField name="reference_type" type="string" required>
          Enum to determine which reference method to use for an asset. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_action" type="string">
      Enum to determine how batch inferencing will handle output Optional.
    </ResponseField>
    <ResponseField name="output_file_name" type="string">
      Customized output file name for append_row output action. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Possible values for DeploymentProvisioningState. Optional.
    </ResponseField>
    <ResponseField name="resources" type="object">
      The compute resource(s) this applies to. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="instance_count" type="number">
          The number of compute instance(s) provisioned. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_type" type="string">
          The compute instance type (e.g. VM size) this resource uses. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="map(any)">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="retry_settings" type="object">
      Retry settings for a batch inference operation. Optional.
      <Expandable title="properties">
        <ResponseField name="max_retries" type="number">
          Maximum retry count for a mini-batch Optional.
        </ResponseField>
        <ResponseField name="timeout" type="string">
          Invocation timeout for a mini-batch, in ISO 8601 format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
