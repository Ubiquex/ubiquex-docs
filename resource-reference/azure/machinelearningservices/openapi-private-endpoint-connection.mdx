---
title: "azure_machinelearningservices_openapi_private_endpoint_connection"
description: "`azure_machinelearningservices_openapi_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Machine Learning..."
---

`azure_machinelearningservices_openapi_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure Machine Learning workspace, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi private endpoint connection"})

    		ubx.Resource(machinelearningservices.OpenapiPrivateEndpointConnection, "example", machinelearningservices.OpenapiPrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiPrivateEndpointConnection } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_private_endpoint_connection";

    export default stack("example", () => {
      intent({ summary: "example own openapi private endpoint connection" });

      resource(OpenapiPrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiPrivateEndpointConnection, OpenapiPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own openapi private endpoint connection")

        ubx.resource(OpenapiPrivateEndpointConnection, "example", OpenapiPrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_machinelearningservices_openapi_private_endpoint_connection,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string">
  *Same as workspace location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Private endpoint connection properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_endpoint" type="object">
      The Private Endpoint resource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          e.g. /subscriptions/&#123;networkSubscriptionId&#125;/resourceGroups/&#123;rgName&#125;/providers/Microsoft.Network/privateEndpoints/&#123;privateEndpointName&#125; Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="subnet_arm_id" type="string">
          The subnetId that the private endpoint is connected to. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object">
      A collection of information about the state of the connection between service consumer and provider. Optional.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          Some RP chose "None". Other RPs use this for region expansion. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          User-defined message that, per NRP doc, may be used for approval-related message. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Connection status of the service consumer with the service provider Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current provisioning state. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  User-defined key/value tags on this resource, usable for organizing and filtering resources across the subscription. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
