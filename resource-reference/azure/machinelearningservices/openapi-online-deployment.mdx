---
title: "azure_machinelearningservices_openapi_online_deployment"
description: "`azure_machinelearningservices_openapi_online_deployment` is a specific model deployment behind an Azure Machine Learning online endpoint..."
---

`azure_machinelearningservices_openapi_online_deployment` is a specific model deployment behind an Azure Machine Learning online endpoint, serving real-time inference requests.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi online deployment"})

    		ubx.Resource(machinelearningservices.OpenapiOnlineDeployment, "example", machinelearningservices.OpenapiOnlineDeploymentConfig{
    			Properties: map[string]any{"endpoint_compute_type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiOnlineDeployment } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_online_deployment";

    export default stack("example", () => {
      intent({ summary: "example own openapi online deployment" });

      resource(OpenapiOnlineDeployment, "example", {
        properties: { endpointComputeType: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiOnlineDeployment, OpenapiOnlineDeploymentConfig

    def describe():
        ubx.intent("example own openapi online deployment")

        ubx.resource(OpenapiOnlineDeployment, "example", OpenapiOnlineDeploymentConfig(
            properties={"endpoint_compute_type": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_online_deployment,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Metadata used by portal/tooling/etc to render different UX experiences for resources of the same type. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="app_insights_enabled" type="bool">
      If true, enables Application Insights logging. Optional.
    </ResponseField>
    <ResponseField name="data_collector" type="object">
      Configuration for collecting inference request/response data. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="collections" type="map(object)" required>
          [Required] The collection configuration. Each collection has it own configuration to collect model data and the name of collection can be arbitrary string. Model data collector can be used for either payload logging or custom logging or both of them. Collection request and response are reserved for payload logging, others are for custom logging. Required.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="data_collection_mode" type="string">
              Whether data collection is enabled, disabled, or disabled with data deletion. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="data_id" type="string">
              A reference to the data asset this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sampling_rate" type="number">
              The fraction of requests sampled for data collection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="request_logging" type="object">
          Configuration for logging inference request/response payloads. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="capture_headers" type="list(string)">
              For payload logging, we only collect payload by default. If customers also want to collect the specified headers, they can set them in captureHeaders so that backend will collect those headers along with payload. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rolling_rate" type="string">
          When model data is collected to blob storage, we need to roll the data to different path to avoid logging all of them in a single blob file. If the rolling rate is hour, all data will be collected in the blob path /yyyy/MM/dd/HH/. If it's day, all data will be collected in blob path /yyyy/MM/dd/. The other benefit of rolling path is that model monitoring ui is able to select a time range of data very quickly. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="egress_public_network_access" type="string">
      Enum to determine whether PublicNetworkAccess is Enabled or Disabled for egress of a deployment. Optional.
    </ResponseField>
    <ResponseField name="endpoint_compute_type" type="string" required>
      Enum to determine endpoint compute type. Required.
    </ResponseField>
    <ResponseField name="instance_type" type="string">
      Compute instance type. Default: Standard_F4s_v2. Optional.
    </ResponseField>
    <ResponseField name="liveness_probe" type="object">
      Deployment container liveness/readiness probe configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="failure_threshold" type="number">
          The number of failures to allow before returning an unhealthy status. Optional.
        </ResponseField>
        <ResponseField name="initial_delay" type="string">
          The delay before the first probe in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="period" type="string">
          The length of time between probes in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="success_threshold" type="number">
          The number of successful probes before returning a healthy status. Optional.
        </ResponseField>
        <ResponseField name="timeout" type="string">
          The probe timeout in ISO 8601 format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model" type="string">
      The URI path to the model. Optional.
    </ResponseField>
    <ResponseField name="model_mount_path" type="string">
      The path to mount the model in custom container. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Possible values for DeploymentProvisioningState. Optional.
    </ResponseField>
    <ResponseField name="readiness_probe" type="object">
      Deployment container liveness/readiness probe configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="failure_threshold" type="number">
          The number of failures to allow before returning an unhealthy status. Optional.
        </ResponseField>
        <ResponseField name="initial_delay" type="string">
          The delay before the first probe in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="period" type="string">
          The length of time between probes in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="success_threshold" type="number">
          The number of successful probes before returning a healthy status. Optional.
        </ResponseField>
        <ResponseField name="timeout" type="string">
          The probe timeout in ISO 8601 format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request_settings" type="object">
      Online deployment scoring requests configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="max_concurrent_requests_per_instance" type="number">
          The number of maximum concurrent requests per node allowed per deployment. Defaults to 1. Optional.
        </ResponseField>
        <ResponseField name="max_queue_wait" type="string">
          (Deprecated for Managed Online Endpoints) The maximum amount of time a request will stay in the queue in ISO 8601 format. Defaults to 500ms. (Now increase `request_timeout_ms` to account for any networking/queue delays) Optional.
        </ResponseField>
        <ResponseField name="request_timeout" type="string">
          The scoring timeout in ISO 8601 format. Defaults to 5000ms. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scale_settings" type="object">
      Online deployment scaling configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="scale_type" type="string" required>
          How this resource scales, e.g. manually or automatically. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="startup_probe" type="object">
      Deployment container liveness/readiness probe configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="failure_threshold" type="number">
          The number of failures to allow before returning an unhealthy status. Optional.
        </ResponseField>
        <ResponseField name="initial_delay" type="string">
          The delay before the first probe in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="period" type="string">
          The length of time between probes in ISO 8601 format. Optional.
        </ResponseField>
        <ResponseField name="success_threshold" type="number">
          The number of successful probes before returning a healthy status. Optional.
        </ResponseField>
        <ResponseField name="timeout" type="string">
          The probe timeout in ISO 8601 format. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The resource model definition representing SKU Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible for the resource this may be omitted. Optional.
    </ResponseField>
    <ResponseField name="family" type="string">
      If the service has different generations of hardware, for the same SKU, then that can be captured here. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the SKU. Ex - P3. It is typically a letter+number code Required.
    </ResponseField>
    <ResponseField name="size" type="string">
      The SKU size. When the name field is the combination of tier and some other value, this would be the standalone code. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required on a PUT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
