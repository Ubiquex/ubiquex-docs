---
title: "azure_machinelearningservices_openapi_outbound_rule_basic_resource"
description: "`azure_machinelearningservices_openapi_outbound_rule_basic_resource` defines an allowed outbound network destination for an Azure Machine Learning..."
---

`azure_machinelearningservices_openapi_outbound_rule_basic_resource` defines an allowed outbound network destination for an Azure Machine Learning workspace's own managed virtual network, permitting egress to a specific service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi outbound rule basic resource"})

    		ubx.Resource(machinelearningservices.OpenapiOutboundRuleBasicResource, "example", machinelearningservices.OpenapiOutboundRuleBasicResourceConfig{
    			Properties: map[string]any{"type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiOutboundRuleBasicResource } from "@ubx/sdk-azure/azure/machinelearningservices/openapi_outbound_rule_basic_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi outbound rule basic resource" });

      resource(OpenapiOutboundRuleBasicResource, "example", {
        properties: { type: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.machinelearningservices import OpenapiOutboundRuleBasicResource, OpenapiOutboundRuleBasicResourceConfig
    from ubx.azure.machinelearningservices.openapi_outbound_rule_basic_resource import OpenapiOutboundRuleBasicResource_Properties

    def describe():
        ubx.intent("example own openapi outbound rule basic resource")

        ubx.resource(OpenapiOutboundRuleBasicResource, "example", OpenapiOutboundRuleBasicResourceConfig(
            properties=OpenapiOutboundRuleBasicResource_Properties(type="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_machinelearningservices_openapi_outbound_rule_basic_resource,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Outbound Rule for the managed network of a machine learning workspace. Required.
  <Expandable title="properties">
    <ResponseField name="category" type="string">
      Category of a managed network Outbound Rule of a machine learning workspace. Optional.
    </ResponseField>
    <ResponseField name="error_information" type="string">
      Error information about an outbound rule of a machine learning workspace if RuleStatus is failed. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="parent_rule_names" type="list(string)">
      The name(s) of the parent rule(s) this suggestion or association derives from. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      Type of a managed network Outbound Rule of a machine learning workspace. Optional.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of a managed network Outbound Rule of a machine learning workspace. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
