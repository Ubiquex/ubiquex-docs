---
title: "azure_machinelearningservices_openapi_datastore"
description: "`azure_machinelearningservices_openapi_datastore` registers a connection to an external storage account (Blob, Data Lake..."
---

`azure_machinelearningservices_openapi_datastore` registers a connection to an external storage account (Blob, Data Lake, or similar) with an Azure Machine Learning workspace, that dataset(s) are sourced from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_machinelearningservices_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_machinelearningservices_openapi/sdk/go
    package main

    import (
    	machinelearningservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_machinelearningservices_openapi/machinelearningservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi datastore"})

    		ubx.Resource(machinelearningservices.OpenapiDatastore, "ci-runner", machinelearningservices.OpenapiDatastoreConfig{
    			Name:       "ci-runner",
    			Properties: map[string]any{"credentials": map[string]any{"credentials_type": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_machinelearningservices_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiDatastore } from "./local-sdk/azure_machinelearningservices_openapi/sdk/typescript/azure_machinelearningservices_openapi/machinelearningservices/openapi_datastore.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi datastore" });

      resource(OpenapiDatastore, "ci-runner", {
        name: "ci-runner",
        properties: { credentials: { credentialsType: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_machinelearningservices_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_machinelearningservices_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_machinelearningservices_openapi.machinelearningservices import OpenapiDatastore, OpenapiDatastoreConfig

    def describe():
        ubx.intent("payments own openapi datastore")

        ubx.resource(OpenapiDatastore, "ci-runner", OpenapiDatastoreConfig(
            name="ci-runner",
            properties={"credentials": {"credentials_type": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_machinelearningservices_openapi_datastore called
    "ci-runner", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Base definition for datastore contents configuration. Required.
  <Expandable title="properties">
    <ResponseField name="credentials" type="object" required>
      Base definition for datastore credentials. Required.
      <Expandable title="properties">
        <ResponseField name="credentials_type" type="string" required>
          Enum to determine the datastore credentials type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="datastore_type" type="string" required>
      Enum to determine the datastore contents type. Required.
    </ResponseField>
    <ResponseField name="is_default" type="bool">
      Readonly property to indicate if datastore is the workspace default datastore Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
