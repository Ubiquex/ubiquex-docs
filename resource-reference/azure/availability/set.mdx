---
title: "azure_availability_set"
description: "A complete, runnable azure_availability_set program, in every SDK language."
---

`azure_availability_set` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	availability "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/availability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own set"})

    		ubx.Resource(availability.Set, "ci-runner", availability.SetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Set } from "./local-sdk/azure/sdk/typescript/azure/availability/set.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own set" });

      resource(Set, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.availability import Set, SetConfig

    def describe():
        ubx.intent("payments own set")

        ubx.resource(Set, "ci-runner", SetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_availability_set, with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="properties" type="object">
  The instance view of a resource. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="platform_fault_domain_count" type="number">
      Fault Domain count. Optional.
    </ResponseField>
    <ResponseField name="platform_update_domain_count" type="number">
      Update Domain count. Optional.
    </ResponseField>
    <ResponseField name="proximity_placement_group" type="object">
      Specifies the proximity placement group that the availability set should be associated with, enabling co-location of resources for low network latency. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource Id Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduled_events_policy" type="object">
      Specifies Redeploy, Reboot and ScheduledEventsAdditionalPublishingTargets Scheduled Event related configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="all_instances_down" type="object">
          Specifies if Scheduled Events should be auto-approved when all instances are down. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies if Scheduled Events should be auto-approved when all instances are down. its default value is true Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scheduled_events_additional_publishing_targets" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="event_grid_and_resource_graph" type="object">
              Specifies eventGridAndResourceGraph related Scheduled Event related configurations. Optional.
              <Expandable title="properties">
                <ResponseField name="enable" type="bool">
                  Specifies if event grid and resource graph is enabled for Scheduled event related configurations. Optional.
                </ResponseField>
                <ResponseField name="scheduled_events_api_version" type="string">
                  Specifies the api-version to determine which Scheduled Events configuration schema version will be delivered. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_initiated_reboot" type="object">
          Specifies Reboot related Scheduled Event related configurations. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies Reboot Scheduled Event related configurations. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_initiated_redeploy" type="object">
          Specifies Redeploy related Scheduled Event related configurations. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies Redeploy Scheduled Event related configurations. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="statuses" type="list(object)">
      The resource status information. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The status code that identifies the current state of the availability set, such as 'ProvisioningState/succeeded'. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="display_status" type="string">
          A human-readable, user-facing description of the current status of the availability set, as part of a list of status objects that may also include a code, level, and timestamp. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="level" type="string">
          The severity level of the status, which can be one of Info, Warning, or Error. It indicates the impact or importance of the status message. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          The message provides additional information or details about the status of the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp when the status was recorded. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machine_scale_set_migration_info" type="object">
      Describes the Availability Set properties related to migration to Flexible Virtual Machine Scale Set. Optional.
      <Expandable title="properties">
        <ResponseField name="default_virtual_machine_scale_set_info" type="object">
          Indicates the target Virtual Machine ScaleSet properties upon triggering a seamless migration without downtime of the VMs via the ConvertToVirtualMachineScaleSet API. Optional.
          <Expandable title="properties">
            <ResponseField name="constrained_maximum_capacity" type="bool">
              Indicates if the the maximum capacity of the default migrated Virtual Machine Scale Set after its migration will be constrained to a limited number of VMs. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="default_virtual_machine_scale_set" type="object">
              The default virtual machine scale set that serves as the primary target when migrating the availability set to a scale set. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Resource Id Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="migrate_to_virtual_machine_scale_set" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource Id Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machines" type="list(object)">
      A list of references to all virtual machines in the availability set. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The Azure resource ID of a virtual machine associated with the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Describes a virtual machine scale set sku. NOTE: If the new VM SKU is not supported on the hardware the scale set is currently on, you need to deallocate the VMs in the scale set before you modify the SKU name. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Specifies the number of virtual machines in the scale set. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The sku name. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Specifies the tier of virtual machines in a scale set.&lt;br /&gt;&lt;br /&gt; Possible Values:&lt;br /&gt;&lt;br /&gt; **Standard**&lt;br /&gt;&lt;br /&gt; **Basic** Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="properties" type="object">
  The instance view of a resource. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="platform_fault_domain_count" type="number">
      Fault Domain count. Optional.
    </ResponseField>
    <ResponseField name="platform_update_domain_count" type="number">
      Update Domain count. Optional.
    </ResponseField>
    <ResponseField name="proximity_placement_group" type="object">
      Specifies the proximity placement group that the availability set should be associated with, enabling co-location of resources for low network latency. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource Id Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduled_events_policy" type="object">
      Specifies Redeploy, Reboot and ScheduledEventsAdditionalPublishingTargets Scheduled Event related configurations. Optional.
      <Expandable title="properties">
        <ResponseField name="all_instances_down" type="object">
          Specifies if Scheduled Events should be auto-approved when all instances are down. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies if Scheduled Events should be auto-approved when all instances are down. its default value is true Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scheduled_events_additional_publishing_targets" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="event_grid_and_resource_graph" type="object">
              Specifies eventGridAndResourceGraph related Scheduled Event related configurations. Optional.
              <Expandable title="properties">
                <ResponseField name="enable" type="bool">
                  Specifies if event grid and resource graph is enabled for Scheduled event related configurations. Optional.
                </ResponseField>
                <ResponseField name="scheduled_events_api_version" type="string">
                  Specifies the api-version to determine which Scheduled Events configuration schema version will be delivered. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_initiated_reboot" type="object">
          Specifies Reboot related Scheduled Event related configurations. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies Reboot Scheduled Event related configurations. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user_initiated_redeploy" type="object">
          Specifies Redeploy related Scheduled Event related configurations. Optional.
          <Expandable title="properties">
            <ResponseField name="automatically_approve" type="bool">
              Specifies Redeploy Scheduled Event related configurations. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="statuses" type="list(object)">
      The resource status information. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The status code that identifies the current state of the availability set, such as 'ProvisioningState/succeeded'. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="display_status" type="string">
          A human-readable, user-facing description of the current status of the availability set, as part of a list of status objects that may also include a code, level, and timestamp. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="level" type="string">
          The severity level of the status, which can be one of Info, Warning, or Error. It indicates the impact or importance of the status message. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          The message provides additional information or details about the status of the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time" type="string">
          The timestamp when the status was recorded. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machine_scale_set_migration_info" type="object">
      Describes the Availability Set properties related to migration to Flexible Virtual Machine Scale Set. Optional.
      <Expandable title="properties">
        <ResponseField name="default_virtual_machine_scale_set_info" type="object">
          Indicates the target Virtual Machine ScaleSet properties upon triggering a seamless migration without downtime of the VMs via the ConvertToVirtualMachineScaleSet API. Optional.
          <Expandable title="properties">
            <ResponseField name="constrained_maximum_capacity" type="bool">
              Indicates if the the maximum capacity of the default migrated Virtual Machine Scale Set after its migration will be constrained to a limited number of VMs. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="default_virtual_machine_scale_set" type="object">
              The default virtual machine scale set that serves as the primary target when migrating the availability set to a scale set. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Resource Id Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="migrate_to_virtual_machine_scale_set" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource Id Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machines" type="list(object)">
      A list of references to all virtual machines in the availability set. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The Azure resource ID of a virtual machine associated with the availability set. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Describes a virtual machine scale set sku. NOTE: If the new VM SKU is not supported on the hardware the scale set is currently on, you need to deallocate the VMs in the scale set before you modify the SKU name. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Specifies the number of virtual machines in the scale set. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The sku name. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Specifies the tier of virtual machines in a scale set.&lt;br /&gt;&lt;br /&gt; Possible Values:&lt;br /&gt;&lt;br /&gt; **Standard**&lt;br /&gt;&lt;br /&gt; **Basic** Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
