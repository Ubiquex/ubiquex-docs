---
title: "azure_security_datascanners_data_scanner"
description: "`azure_security_datascanners_data_scanner` configures Microsoft Defender for Cloud's own sensitive data discovery scanner for a data resource..."
---

`azure_security_datascanners_data_scanner` configures Microsoft Defender for Cloud's own sensitive data discovery scanner for a data resource, automatically classifying sensitive data at rest.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_security_datascanners --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_security_datascanners/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_security_datascanners/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own datascanners data scanner"})

    		ubx.Resource(security.DatascannersDataScanner, "ci-runner", security.DatascannersDataScannerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_security_datascanners --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatascannersDataScanner } from "./local-sdk/azure_security_datascanners/sdk/typescript/azure_security_datascanners/security/datascanners_data_scanner.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own datascanners data scanner" });

      resource(DatascannersDataScanner, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_security_datascanners --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_security_datascanners/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_security_datascanners.security import DatascannersDataScanner, DatascannersDataScannerConfig

    def describe():
        ubx.intent("payments own datascanners data scanner")

        ubx.resource(DatascannersDataScanner, "ci-runner", DatascannersDataScannerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_security_datascanners_data_scanner, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (either system assigned, or none) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (either system assigned, or none). Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="any">
  Data scanner resource properties. The data scanner is an identity-only resource defined solely by its assigned managed identity. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
