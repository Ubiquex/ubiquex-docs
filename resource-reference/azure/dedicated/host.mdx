---
title: "azurerm_dedicated_host"
description: "Real, generated bindings for `azurerm_dedicated_host`."
---

`azurerm_dedicated_host` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dedicated "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/dedicated"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host"})

    		ubx.Resource(dedicated.Host, "ci-runner", dedicated.HostConfig{
    			DedicatedHostGroupId: "example",
    			Location:             "example",
    			Name:                 "ci-runner",
    			PlatformFaultDomain:  1,
    			SkuName:              "example-sku-name",
    			AutoReplaceOnFailure: true,
    			LicenseType:          "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Host } from "jsr:@ubx/sdk-azure/azurerm/dedicated/host";

    export default stack("payments", () => {
      intent({ summary: "payments own host" });

      resource(Host, "ci-runner", {
        dedicatedHostGroupId: "example",
        location: "example",
        name: "ci-runner",
        platformFaultDomain: 1,
        skuName: "example-sku-name",
        autoReplaceOnFailure: true,
        licenseType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.dedicated import Host, HostConfig

    def describe():
        ubx.intent("payments own host")

        ubx.resource(Host, "ci-runner", HostConfig(
            dedicated_host_group_id="example",
            location="example",
            name="ci-runner",
            platform_fault_domain=1,
            sku_name="example-sku-name",
            auto_replace_on_failure=True,
            license_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_dedicated_host called "ci-runner", with dedicated
    host group id example, location example, platform fault domain 1,
    sku name example-sku-name, auto replace on failure true, license
    type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_replace_on_failure" type="bool">
  Optional.
</ResponseField>

<ResponseField name="dedicated_host_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="license_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform_fault_domain" type="number" required>
  Required.
</ResponseField>

<ResponseField name="sku_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
