---
title: "azurerm_aadb2c_directory"
description: "Real, generated bindings for `azurerm_aadb2c_directory`."
---

`azurerm_aadb2c_directory` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aadb2c "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/aadb2c"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory"})

    		ubx.Resource(aadb2c.Directory, "ci-runner", aadb2c.DirectoryConfig{
    			DataResidencyLocation: "example",
    			DomainName:            "example-domain-name",
    			ResourceGroupName:     "example-resource-group-name",
    			SkuName:               "example-sku-name",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Directory } from "jsr:@ubx/sdk-azure/azurerm/aadb2c/directory";

    export default stack("payments", () => {
      intent({ summary: "payments own directory" });

      resource(Directory, "ci-runner", {
        dataResidencyLocation: "example",
        domainName: "example-domain-name",
        resourceGroupName: "example-resource-group-name",
        skuName: "example-sku-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.aadb2c import Directory, DirectoryConfig

    def describe():
        ubx.intent("payments own directory")

        ubx.resource(Directory, "ci-runner", DirectoryConfig(
            data_residency_location="example",
            domain_name="example-domain-name",
            resource_group_name="example-resource-group-name",
            sku_name="example-sku-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_aadb2c_directory, with data residency location
    example, domain name example-domain-name, resource group name
    example-resource-group-name, sku name example-sku-name, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="country_code" type="string">
  Country code of the B2C tenant. See https://aka.ms/B2CDataResidency for valid country codes. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="data_residency_location" type="string" required>
  Location in which the B2C tenant is hosted and data resides. See https://aka.ms/B2CDataResidency for more information. Required.
</ResponseField>

<ResponseField name="display_name" type="string">
  The initial display name of the B2C tenant. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Domain name of the B2C tenant, including onmicrosoft.com suffix. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="sku_name" type="string" required>
  Billing SKU for the B2C tenant. See https://aka.ms/b2cBilling for more information. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="billing_type" type="string">
  The type of billing for the B2C tenant. Possible values include: `MAU` or `Auths`. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="country_code" type="string">
  Country code of the B2C tenant. See https://aka.ms/B2CDataResidency for valid country codes. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="display_name" type="string">
  The initial display name of the B2C tenant. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="effective_start_date" type="string">
  The date from which the billing type took effect. May not be populated until after the first billing cycle. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  The Tenant ID for the B2C tenant. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
