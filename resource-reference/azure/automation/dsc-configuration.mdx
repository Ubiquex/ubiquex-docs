---
title: "azurerm_automation_dsc_configuration"
description: "Real, generated bindings for `azurerm_automation_dsc_configuration`."
---

`azurerm_automation_dsc_configuration` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dsc configuration"})

    		ubx.Resource(automation.DscConfiguration, "ci-runner", automation.DscConfigurationConfig{
    			AutomationAccountName: "example-automation-account-name",
    			ContentEmbedded:       "example",
    			Location:              "example",
    			Name:                  "ci-runner",
    			ResourceGroupName:     "example-resource-group-name",
    			Description:           "Managed by ubx.",
    			LogVerbose:            true,
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DscConfiguration } from "jsr:@ubx/sdk-azure/azurerm/automation/dsc_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own dsc configuration" });

      resource(DscConfiguration, "ci-runner", {
        automationAccountName: "example-automation-account-name",
        contentEmbedded: "example",
        location: "example",
        name: "ci-runner",
        resourceGroupName: "example-resource-group-name",
        description: "Managed by ubx.",
        logVerbose: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.automation import DscConfiguration, DscConfigurationConfig

    def describe():
        ubx.intent("payments own dsc configuration")

        ubx.resource(DscConfiguration, "ci-runner", DscConfigurationConfig(
            automation_account_name="example-automation-account-name",
            content_embedded="example",
            location="example",
            name="ci-runner",
            resource_group_name="example-resource-group-name",
            description="Managed by ubx.",
            log_verbose=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_automation_dsc_configuration called "ci-runner",
    with automation account name example-automation-account-name,
    content embedded example, location example, resource group name
    example-resource-group-name, description Managed by ubx., log
    verbose true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="automation_account_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="content_embedded" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="log_verbose" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
