---
title: "azure_automation_openapi_connection_type"
description: "`azure_automation_openapi_connection_type` defines the schema (field names) for a category of Azure Automation connection..."
---

`azure_automation_openapi_connection_type` defines the schema (field names) for a category of Azure Automation connection, e.g. the fields needed to connect to Azure itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_automation_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_automation_openapi/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_automation_openapi/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi connection type"})

    		ubx.Resource(automation.OpenapiConnectionType, "example", automation.OpenapiConnectionTypeConfig{
    			Name:       "example",
    			Properties: map[string]any{"field_definitions": map[string]any{"key": "value"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_automation_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiConnectionType } from "./local-sdk/azure_automation_openapi/sdk/typescript/azure_automation_openapi/automation/openapi_connection_type.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi connection type" });

      resource(OpenapiConnectionType, "example", {
        name: "example",
        properties: { fieldDefinitions: { key: "value" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_automation_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_automation_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_automation_openapi.automation import OpenapiConnectionType, OpenapiConnectionTypeConfig

    def describe():
        ubx.intent("example own openapi connection type")

        ubx.resource(OpenapiConnectionType, "example", OpenapiConnectionTypeConfig(
            name="example",
            properties={"field_definitions": {"key": "value"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_connection_type called
    "example", with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  Gets or sets the name of the connection type. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The properties of the create connection type. Required.
  <Expandable title="properties">
    <ResponseField name="field_definitions" type="map(object)" required>
      Gets or sets the field definitions of the connection type. Required.
      <Expandable title="properties">
        <ResponseField name="is_encrypted" type="bool">
          Whether this variable's own value is encrypted at rest. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_optional" type="bool">
          Whether supplying this parameter is optional. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_global" type="bool">
      Gets or sets a Boolean value to indicate if the connection type is global. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
