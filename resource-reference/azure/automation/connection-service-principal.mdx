---
title: "azurerm_automation_connection_service_principal"
description: "Real, generated bindings for `azurerm_automation_connection_service_principal`."
---

`azurerm_automation_connection_service_principal` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connection service principal"})

    		ubx.Resource(automation.ConnectionServicePrincipal, "ci-runner", automation.ConnectionServicePrincipalConfig{
    			ApplicationId:         "example",
    			AutomationAccountName: "example-automation-account-name",
    			CertificateThumbprint: "example",
    			Name:                  "ci-runner",
    			ResourceGroupName:     "example-resource-group-name",
    			SubscriptionId:        "example",
    			TenantId:              "example",
    			Description:           "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectionServicePrincipal } from "jsr:@ubx/sdk-azure/azurerm/automation/connection_service_principal";

    export default stack("payments", () => {
      intent({ summary: "payments own connection service principal" });

      resource(ConnectionServicePrincipal, "ci-runner", {
        applicationId: "example",
        automationAccountName: "example-automation-account-name",
        certificateThumbprint: "example",
        name: "ci-runner",
        resourceGroupName: "example-resource-group-name",
        subscriptionId: "example",
        tenantId: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.automation import ConnectionServicePrincipal, ConnectionServicePrincipalConfig

    def describe():
        ubx.intent("payments own connection service principal")

        ubx.resource(ConnectionServicePrincipal, "ci-runner", ConnectionServicePrincipalConfig(
            application_id="example",
            automation_account_name="example-automation-account-name",
            certificate_thumbprint="example",
            name="ci-runner",
            resource_group_name="example-resource-group-name",
            subscription_id="example",
            tenant_id="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_automation_connection_service_principal called
    "ci-runner", with application id example, automation account name
    example-automation-account-name, certificate thumbprint example,
    resource group name example-resource-group-name, subscription id
    example, tenant id example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="automation_account_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_thumbprint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subscription_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tenant_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
