---
title: "azure_automation_openapi_job"
description: "`azure_automation_openapi_job` is a single execution of an Azure Automation runbook."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_automation_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_automation_openapi/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_automation_openapi/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi job"})

    		ubx.Resource(automation.OpenapiJob, "example", automation.OpenapiJobConfig{
    			Properties: map[string]any{"parameters": map[string]string{"key": "value"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_automation_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiJob } from "./local-sdk/azure_automation_openapi/sdk/typescript/azure_automation_openapi/automation/openapi_job.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi job" });

      resource(OpenapiJob, "example", {
        properties: { parameters: { key: "value" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_automation_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_automation_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_automation_openapi.automation import OpenapiJob, OpenapiJobConfig

    def describe():
        ubx.intent("example own openapi job")

        ubx.resource(OpenapiJob, "example", OpenapiJobConfig(
            properties={"parameters": {"key": "value"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_job, with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="parameters" type="map(string)">
      Gets or sets the parameters of the job. Optional.
    </ResponseField>
    <ResponseField name="run_on" type="string">
      Gets or sets the runOn which specifies the group name where the job is to be executed. Optional.
    </ResponseField>
    <ResponseField name="runbook" type="object">
      The runbook property associated with the entity. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Gets or sets the name of the runbook. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
