---
title: "azure_automation_openapi_certificate"
description: "`azure_automation_openapi_certificate` stores a certificate securely within an Azure Automation account..."
---

`azure_automation_openapi_certificate` stores a certificate securely within an Azure Automation account, referenced by runbook(s) needing it for authentication.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_automation_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_automation_openapi/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_automation_openapi/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi certificate"})

    		ubx.Resource(automation.OpenapiCertificate, "example", automation.OpenapiCertificateConfig{
    			Name:       "example",
    			Properties: map[string]any{"base64_value": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_automation_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiCertificate } from "./local-sdk/azure_automation_openapi/sdk/typescript/azure_automation_openapi/automation/openapi_certificate.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi certificate" });

      resource(OpenapiCertificate, "example", {
        name: "example",
        properties: { base64Value: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_automation_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_automation_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_automation_openapi.automation import OpenapiCertificate, OpenapiCertificateConfig

    def describe():
        ubx.intent("example own openapi certificate")

        ubx.resource(OpenapiCertificate, "example", OpenapiCertificateConfig(
            name="example",
            properties={"base64_value": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_certificate called "example",
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  Gets or sets the name of the certificate. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The properties of the create certificate operation. Required.
  <Expandable title="properties">
    <ResponseField name="base64_value" type="string" required>
      Gets or sets the base64 encoded value of the certificate. Required.
    </ResponseField>
    <ResponseField name="description" type="string">
      Gets or sets the description of the certificate. Optional.
    </ResponseField>
    <ResponseField name="is_exportable" type="bool">
      Gets or sets the is exportable flag of the certificate. Optional.
    </ResponseField>
    <ResponseField name="thumbprint" type="string">
      Gets or sets the thumbprint of the certificate. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
