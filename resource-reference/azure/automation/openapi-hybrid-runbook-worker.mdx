---
title: "azure_automation_openapi_hybrid_runbook_worker"
description: "`azure_automation_openapi_hybrid_runbook_worker` registers a machine (on-premises or another cloud) as a hybrid worker..."
---

`azure_automation_openapi_hybrid_runbook_worker` registers a machine (on-premises or another cloud) as a hybrid worker, letting Azure Automation runbooks execute directly on it rather than Azure-hosted sandboxes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi hybrid runbook worker"})

    		ubx.Resource(automation.OpenapiHybridRunbookWorker, "example", automation.OpenapiHybridRunbookWorkerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiHybridRunbookWorker } from "@ubx/sdk-azure/azure/automation/openapi_hybrid_runbook_worker";

    export default stack("example", () => {
      intent({ summary: "example own openapi hybrid runbook worker" });

      resource(OpenapiHybridRunbookWorker, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.automation.openapi_hybrid_runbook_worker import OpenapiHybridRunbookWorker, OpenapiHybridRunbookWorkerConfig

    def describe():
        ubx.intent("example own openapi hybrid runbook worker")

        ubx.resource(OpenapiHybridRunbookWorker, "example", OpenapiHybridRunbookWorkerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_hybrid_runbook_worker, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="properties" type="object">
  The parameters supplied to the create or update hybrid runbook worker operation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="vm_resource_id" type="string">
      Azure Resource Manager Id for a virtual machine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="name" type="string">
  Gets or sets the name of the resource. Computed by Microsoft Azure after `ship`.
</ResponseField>

<ResponseField name="properties" type="object">
  The parameters supplied to the create or update hybrid runbook worker operation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="vm_resource_id" type="string">
      Azure Resource Manager Id for a virtual machine. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
