---
title: "azurerm_automation_job_schedule"
description: "Real, generated bindings for `azurerm_automation_job_schedule`."
---

`azurerm_automation_job_schedule` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own job schedule"})

    		ubx.Resource(automation.JobSchedule, "ci-runner", automation.JobScheduleConfig{
    			AutomationAccountName: "example-automation-account-name",
    			ResourceGroupName:     "example-resource-group-name",
    			RunbookName:           "example-runbook-name",
    			ScheduleName:          "example-schedule-name",
    			Parameters:            map[string]string{"managed-by": "ubx"},
    			RunOn:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobSchedule } from "jsr:@ubx/sdk-azure/azurerm/automation/job_schedule";

    export default stack("payments", () => {
      intent({ summary: "payments own job schedule" });

      resource(JobSchedule, "ci-runner", {
        automationAccountName: "example-automation-account-name",
        resourceGroupName: "example-resource-group-name",
        runbookName: "example-runbook-name",
        scheduleName: "example-schedule-name",
        parameters: { "managed-by": "ubx" },
        runOn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.automation import JobSchedule, JobScheduleConfig

    def describe():
        ubx.intent("payments own job schedule")

        ubx.resource(JobSchedule, "ci-runner", JobScheduleConfig(
            automation_account_name="example-automation-account-name",
            resource_group_name="example-resource-group-name",
            runbook_name="example-runbook-name",
            schedule_name="example-schedule-name",
            parameters={"managed-by": "ubx"},
            run_on="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_automation_job_schedule, with automation account
    name example-automation-account-name, resource group name
    example-resource-group-name, runbook name example-runbook-name,
    schedule name example-schedule-name, parameters a set value, run
    on example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="automation_account_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="job_schedule_id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="run_on" type="string">
  Optional.
</ResponseField>

<ResponseField name="runbook_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schedule_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="job_schedule_id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="resource_manager_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
