---
title: "azure_automation_openapi_connection"
description: "`azure_automation_openapi_connection` stores connection details for an external service (of a specific connection type) that Azure Automation..."
---

`azure_automation_openapi_connection` stores connection details for an external service (of a specific connection type) that Azure Automation runbook(s) authenticate against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_automation_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_automation_openapi/sdk/go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_automation_openapi/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi connection"})

    		ubx.Resource(automation.OpenapiConnection, "example", automation.OpenapiConnectionConfig{
    			Name:       "example",
    			Properties: map[string]any{"connection_type": map[string]any{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_automation_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiConnection } from "./local-sdk/azure_automation_openapi/sdk/typescript/azure_automation_openapi/automation/openapi_connection.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi connection" });

      resource(OpenapiConnection, "example", {
        name: "example",
        properties: { connectionType: { name: "example-name" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_automation_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_automation_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_automation_openapi.automation import OpenapiConnection, OpenapiConnectionConfig

    def describe():
        ubx.intent("example own openapi connection")

        ubx.resource(OpenapiConnection, "example", OpenapiConnectionConfig(
            name="example",
            properties={"connection_type": {"name": "example-name"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_connection called "example",
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  Gets or sets the name of the connection. Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  The properties of the create connection properties Required.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="object" required>
      The connection type property associated with the entity. Required.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Gets or sets the name of the connection type. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Gets or sets the description of the connection. Optional.
    </ResponseField>
    <ResponseField name="field_definition_values" type="map(string)">
      Gets or sets the field definition properties of the connection. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
