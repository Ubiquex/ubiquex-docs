---
title: "azure_automation_openapi_hybrid_runbook_worker_group"
description: "`azure_automation_openapi_hybrid_runbook_worker_group` groups related hybrid runbook worker(s) together..."
---

`azure_automation_openapi_hybrid_runbook_worker_group` groups related hybrid runbook worker(s) together, that a runbook job can be targeted to run on any member of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	automation "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi hybrid runbook worker group"})

    		ubx.Resource(automation.OpenapiHybridRunbookWorkerGroup, "example", automation.OpenapiHybridRunbookWorkerGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiHybridRunbookWorkerGroup } from "@ubx/sdk-azure/azure/automation/openapi_hybrid_runbook_worker_group";

    export default stack("example", () => {
      intent({ summary: "example own openapi hybrid runbook worker group" });

      resource(OpenapiHybridRunbookWorkerGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.automation import OpenapiHybridRunbookWorkerGroup, OpenapiHybridRunbookWorkerGroupConfig

    def describe():
        ubx.intent("example own openapi hybrid runbook worker group")

        ubx.resource(OpenapiHybridRunbookWorkerGroup, "example", OpenapiHybridRunbookWorkerGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_automation_openapi_hybrid_runbook_worker_group
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  Gets or sets the name of the resource. Optional.
</ResponseField>

<ResponseField name="properties" type="object">
  The hybrid runbook worker group properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="credential" type="object">
      Definition of RunAs credential to use for hybrid worker. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Gets or sets the name of the credential. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
