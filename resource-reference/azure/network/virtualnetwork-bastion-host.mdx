---
title: "azure_network_virtualnetwork_bastion_host"
description: "`azure_network_virtualnetwork_bastion_host` provisions Azure Bastion..."
---

`azure_network_virtualnetwork_bastion_host` provisions Azure Bastion, a managed jump host providing secure RDP/SSH access to VMs over the Azure portal without exposing them to public IP addresses.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualnetwork bastion host"})

    		ubx.Resource(network.VirtualnetworkBastionHost, "ci-runner", network.VirtualnetworkBastionHostConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkBastionHost } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_bastion_host.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualnetwork bastion host" });

      resource(VirtualnetworkBastionHost, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkBastionHost, VirtualnetworkBastionHostConfig

    def describe():
        ubx.intent("payments own virtualnetwork bastion host")

        ubx.resource(VirtualnetworkBastionHost, "ci-runner", VirtualnetworkBastionHostConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_bastion_host, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Bastion Host. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="disable_copy_paste" type="bool">
      Enable/Disable Copy/Paste feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="dns_name" type="string">
      FQDN for the endpoint on which bastion host is accessible. Optional.
    </ResponseField>
    <ResponseField name="enable_file_copy" type="bool">
      Enable/Disable File Copy feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_ip_connect" type="bool">
      Enable/Disable IP Connect feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_kerberos" type="bool">
      Enable/Disable Kerberos feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_private_only_bastion" type="bool">
      Enable/Disable Private Only feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_session_recording" type="bool">
      Enable/Disable Session Recording feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_shareable_link" type="bool">
      Enable/Disable Shareable Link of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="enable_tunneling" type="bool">
      Enable/Disable Tunneling feature of the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="ip_configurations" type="list(object)">
      IP configuration of the Bastion Host resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_acls" type="object">
      Network access rule(s) restricting which network(s) can reach this resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_rules" type="list(object)">
          Sets the IP ACL rules for Developer Bastion Host. Optional.
          <Expandable title="properties">
            <ResponseField name="address_prefix" type="string">
              An IP address range in CIDR notation, e.g. `10.0.0.0/24`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="scale_units" type="number">
      The scale units for the Bastion Host resource. Optional.
    </ResponseField>
    <ResponseField name="virtual_network" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The sku of this Bastion Host. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the sku of this Bastion Host. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  A list of availability zones denoting where the resource needs to come from. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
