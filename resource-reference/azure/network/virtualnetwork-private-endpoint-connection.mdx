---
title: "azure_network_virtualnetwork_private_endpoint_connection"
description: "A complete, runnable azure_network_virtualnetwork_private_endpoint_connection program, in every SDK language."
---

`azure_network_virtualnetwork_private_endpoint_connection` -- real, typed bindings generated directly from
the real Azure Resource Manager API specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork private endpoint connection"})

    		ubx.Resource(network.VirtualnetworkPrivateEndpointConnection, "example", network.VirtualnetworkPrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkPrivateEndpointConnection } from "@ubx/sdk-azure/azure/network/virtualnetwork_private_endpoint_connection";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork private endpoint connection" });

      resource(VirtualnetworkPrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkPrivateEndpointConnection, VirtualnetworkPrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own virtualnetwork private endpoint connection")

        ubx.resource(VirtualnetworkPrivateEndpointConnection, "example", VirtualnetworkPrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_private_endpoint_connection,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the PrivateEndpointConnectProperties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="link_identifier" type="string">
      The consumer link id. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint" type="object">
      Private endpoint resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="extended_location" type="object">
          ExtendedLocation complex type. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the extended location. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of the private endpoint. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_endpoint_location" type="string">
      The location of the private endpoint. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object">
      A collection of information about the state of the connection between service consumer and provider. Optional.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A message indicating if changes on the service provider require any updates on the consumer. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The reason for approval/rejection of the connection. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
