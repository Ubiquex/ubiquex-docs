---
title: "azure_network_virtualnetwork_ip_group"
description: "`azure_network_virtualnetwork_ip_group` is a named..."
---

`azure_network_virtualnetwork_ip_group` is a named, reusable collection of IP address(es) or address range(s) that Azure Firewall rules can reference by name instead of enumerating addresses inline.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork ip group"})

    		ubx.Resource(network.VirtualnetworkIpGroup, "example", network.VirtualnetworkIpGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkIpGroup } from "@ubx/sdk-azure/azure/network/virtualnetwork_ip_group";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork ip group" });

      resource(VirtualnetworkIpGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.virtualnetwork_ip_group import VirtualnetworkIpGroup, VirtualnetworkIpGroupConfig

    def describe():
        ubx.intent("example own virtualnetwork ip group")

        ubx.resource(VirtualnetworkIpGroup, "example", VirtualnetworkIpGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_ip_group, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The IpGroups property information. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="firewall_policies" type="list(object)">
      List of references to Firewall Policies resources that this IpGroups is associated with. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firewalls" type="list(object)">
      List of references to Firewall resources that this IpGroups is associated with. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_addresses" type="list(string)">
      IpAddresses/IpAddressPrefixes in the IpGroups resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
