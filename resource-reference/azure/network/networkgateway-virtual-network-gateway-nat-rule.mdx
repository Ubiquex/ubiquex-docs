---
title: "azure_network_networkgateway_virtual_network_gateway_nat_rule"
description: "`azure_network_networkgateway_virtual_network_gateway_nat_rule` applies NAT translation to traffic passing through a virtual network gateway's own VPN..."
---

`azure_network_networkgateway_virtual_network_gateway_nat_rule` applies NAT translation to traffic passing through a virtual network gateway's own VPN connection, for connecting sites with overlapping address spaces.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_networkgateway --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_networkgateway/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_networkgateway/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own networkgateway virtual network gateway nat rule"})

    		ubx.Resource(network.NetworkgatewayVirtualNetworkGatewayNatRule, "ci-runner", network.NetworkgatewayVirtualNetworkGatewayNatRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_networkgateway --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkgatewayVirtualNetworkGatewayNatRule } from "./local-sdk/azure_network_networkgateway/sdk/typescript/azure_network_networkgateway/network/networkgateway_virtual_network_gateway_nat_rule.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own networkgateway virtual network gateway nat rule",
      });

      resource(NetworkgatewayVirtualNetworkGatewayNatRule, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_networkgateway --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_networkgateway/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_networkgateway.network import NetworkgatewayVirtualNetworkGatewayNatRule, NetworkgatewayVirtualNetworkGatewayNatRuleConfig

    def describe():
        ubx.intent("payments own networkgateway virtual network gateway nat rule")

        ubx.resource(NetworkgatewayVirtualNetworkGatewayNatRule, "ci-runner", NetworkgatewayVirtualNetworkGatewayNatRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_networkgateway_virtual_network_gateway_nat_rule,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Parameters for VirtualNetworkGatewayNatRule. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="external_mappings" type="list(object)">
      The private IP address external mapping for NAT. Optional.
      <Expandable title="properties">
        <ResponseField name="address_space" type="string">
          The virtual network's own address range(s), in CIDR notation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port_range" type="string">
          A range of ports, e.g. `1000-2000`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="internal_mappings" type="list(object)">
      The private IP address internal mapping for NAT. Optional.
      <Expandable title="properties">
        <ResponseField name="address_space" type="string">
          The virtual network's own address range(s), in CIDR notation. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port_range" type="string">
          A range of ports, e.g. `1000-2000`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_configuration_id" type="string">
      The IP Configuration ID this NAT rule applies to. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      The Source NAT direction of a VPN NAT. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of NAT rule for VPN NAT. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
