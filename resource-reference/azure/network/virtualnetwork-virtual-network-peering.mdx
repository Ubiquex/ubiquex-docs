---
title: "azure_network_virtualnetwork_virtual_network_peering"
description: "`azure_network_virtualnetwork_virtual_network_peering` connects two virtual networks together at the network layer..."
---

`azure_network_virtualnetwork_virtual_network_peering` connects two virtual networks together at the network layer, letting resources in each communicate using private IP addresses as if they were on the same network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork virtual network peering"})

    		ubx.Resource(network.VirtualnetworkVirtualNetworkPeering, "example", network.VirtualnetworkVirtualNetworkPeeringConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualNetworkPeering } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_virtual_network_peering.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork virtual network peering" });

      resource(VirtualnetworkVirtualNetworkPeering, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkVirtualNetworkPeering, VirtualnetworkVirtualNetworkPeeringConfig

    def describe():
        ubx.intent("example own virtualnetwork virtual network peering")

        ubx.resource(VirtualnetworkVirtualNetworkPeering, "example", VirtualnetworkVirtualNetworkPeeringConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_virtual_network_peering, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the virtual network peering. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allow_forwarded_traffic" type="bool">
      Whether the forwarded traffic from the VMs in the local virtual network will be allowed/disallowed in remote virtual network. Optional.
    </ResponseField>
    <ResponseField name="allow_gateway_transit" type="bool">
      If gateway links can be used in remote virtual networking to link to this virtual network. Optional.
    </ResponseField>
    <ResponseField name="allow_virtual_network_access" type="bool">
      Whether the VMs in the local virtual network space would be able to access the VMs in remote virtual network space. Optional.
    </ResponseField>
    <ResponseField name="do_not_verify_remote_gateways" type="bool">
      If we need to verify the provisioning state of the remote gateway. Optional.
    </ResponseField>
    <ResponseField name="enable_only_ipv6_peering" type="bool">
      Whether only Ipv6 address space is peered for subnet peering. Optional.
    </ResponseField>
    <ResponseField name="local_address_space" type="object">
      AddressSpace contains an array of IP address ranges that can be used by subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="address_prefixes" type="list(string)">
          A list of address blocks reserved for this virtual network in CIDR notation. Optional.
        </ResponseField>
        <ResponseField name="ipam_pool_prefix_allocations" type="list(object)">
          A list of IPAM Pools allocating IP address prefixes. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_address_prefixes" type="list(string)">
              Output only. The specific address ranges actually allocated from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="number_of_ip_addresses" type="string">
              How many IP addresses to allocate from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="object">
              A reference to the address or backend pool this resource draws from or belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="local_subnet_names" type="list(string)">
      List of local subnet names that are subnet peered with remote virtual network. Optional.
    </ResponseField>
    <ResponseField name="local_virtual_network_address_space" type="object">
      AddressSpace contains an array of IP address ranges that can be used by subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="address_prefixes" type="list(string)">
          A list of address blocks reserved for this virtual network in CIDR notation. Optional.
        </ResponseField>
        <ResponseField name="ipam_pool_prefix_allocations" type="list(object)">
          A list of IPAM Pools allocating IP address prefixes. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_address_prefixes" type="list(string)">
              Output only. The specific address ranges actually allocated from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="number_of_ip_addresses" type="string">
              How many IP addresses to allocate from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="object">
              A reference to the address or backend pool this resource draws from or belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peer_complete_vnets" type="bool">
      Whether complete virtual network address space is peered. Optional.
    </ResponseField>
    <ResponseField name="peering_state" type="string">
      The status of the virtual network peering. Optional.
    </ResponseField>
    <ResponseField name="peering_sync_level" type="string">
      The peering sync status of the virtual network peering. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="remote_address_space" type="object">
      AddressSpace contains an array of IP address ranges that can be used by subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="address_prefixes" type="list(string)">
          A list of address blocks reserved for this virtual network in CIDR notation. Optional.
        </ResponseField>
        <ResponseField name="ipam_pool_prefix_allocations" type="list(object)">
          A list of IPAM Pools allocating IP address prefixes. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_address_prefixes" type="list(string)">
              Output only. The specific address ranges actually allocated from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="number_of_ip_addresses" type="string">
              How many IP addresses to allocate from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="object">
              A reference to the address or backend pool this resource draws from or belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remote_bgp_communities" type="object">
      Bgp Communities sent over ExpressRoute with each route corresponding to a prefix in this VNET. Optional.
      <Expandable title="properties">
        <ResponseField name="regional_community" type="string">
          The BGP community associated with the region of the virtual network. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="virtual_network_community" type="string" required>
          The BGP community associated with the virtual network. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remote_subnet_names" type="list(string)">
      List of remote subnet names from remote virtual network that are subnet peered. Optional.
    </ResponseField>
    <ResponseField name="remote_virtual_network" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The unique identifier of the network virtualnetwork virtual network peering's remote virtual network. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remote_virtual_network_address_space" type="object">
      AddressSpace contains an array of IP address ranges that can be used by subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="address_prefixes" type="list(string)">
          A list of address blocks reserved for this virtual network in CIDR notation. Optional.
        </ResponseField>
        <ResponseField name="ipam_pool_prefix_allocations" type="list(object)">
          A list of IPAM Pools allocating IP address prefixes. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_address_prefixes" type="list(string)">
              Output only. The specific address ranges actually allocated from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="number_of_ip_addresses" type="string">
              How many IP addresses to allocate from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="object">
              A reference to the address or backend pool this resource draws from or belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remote_virtual_network_encryption" type="object">
      Indicates if encryption is enabled on virtual network and if VM without encryption is allowed in encrypted VNet. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool" required>
          Indicates if encryption is enabled on the virtual network. Required.
        </ResponseField>
        <ResponseField name="enforcement" type="string">
          If the encrypted VNet allows VM that does not support encryption. This field is for future support, AllowUnencrypted is the only supported value at general availability. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resourceGuid property of the Virtual Network peering resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="use_remote_gateways" type="bool">
      If remote gateways can be used on this virtual network. If the flag is set to true, and allowGatewayTransit on remote peering is also true, virtual network will use gateways of remote virtual network for transit. Only one peering can have this flag set to true. This flag cannot be set if virtual network already has a gateway. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
