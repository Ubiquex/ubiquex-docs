---
title: "azure_network_firewallpolicy_firewall_policy_kube_selector_group"
description: "`azure_network_firewallpolicy_firewall_policy_kube_selector_group` defines Kubernetes namespace/pod selector(s) that an Azure Firewall policy rule can..."
---

`azure_network_firewallpolicy_firewall_policy_kube_selector_group` defines Kubernetes namespace/pod selector(s) that an Azure Firewall policy rule can match traffic against, for AKS-aware filtering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewallpolicy firewall policy kube selector group"})

    		ubx.Resource(network.FirewallpolicyFirewallPolicyKubeSelectorGroup, "example", network.FirewallpolicyFirewallPolicyKubeSelectorGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallpolicyFirewallPolicyKubeSelectorGroup } from "@ubx/sdk-azure/azure/network/firewallpolicy_firewall_policy_kube_selector_group";

    export default stack("example", () => {
      intent({
        summary: "example own firewallpolicy firewall policy kube selector group",
      });

      resource(FirewallpolicyFirewallPolicyKubeSelectorGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.firewallpolicy_firewall_policy_kube_selector_group import FirewallpolicyFirewallPolicyKubeSelectorGroup, FirewallpolicyFirewallPolicyKubeSelectorGroupConfig

    def describe():
        ubx.intent("example own firewallpolicy firewall policy kube selector group")

        ubx.resource(FirewallpolicyFirewallPolicyKubeSelectorGroup, "example", FirewallpolicyFirewallPolicyKubeSelectorGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_firewallpolicy_firewall_policy_kube_selector_group,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Kubernetes Selector Group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="namespace_selector" type="object">
      Kubernetes Label Selector for matching labels in Kubernetes Selector Groups. Optional.
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Whether all or any of this rule collection's own rules must match. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pod_selector" type="object">
      Kubernetes Label Selector for matching labels in Kubernetes Selector Groups. Optional.
      <Expandable title="properties">
        <ResponseField name="match_expressions" type="list(object)">
          matchExpressions is a list of label selector requirements. The requirements are ANDed. Optional.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The key of this attribute or label. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Whether all or any of this rule collection's own rules must match. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The value(s) making up this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="match_labels" type="map(string)">
          matchLabels is a map of &#123;key,value&#125; pairs. A single &#123;key,value&#125; in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
