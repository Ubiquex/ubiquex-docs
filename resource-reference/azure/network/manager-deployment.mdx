---
title: "azurerm_network_manager_deployment"
description: "Real, generated bindings for `azurerm_network_manager_deployment`."
---

`azurerm_network_manager_deployment` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager deployment"})

    		ubx.Resource(network.ManagerDeployment, "ci-runner", network.ManagerDeploymentConfig{
    			ConfigurationIds: []string{"example"},
    			Location:         "example",
    			NetworkManagerId: "example",
    			ScopeAccess:      "example",
    			Triggers:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerDeployment } from "jsr:@ubx/sdk-azure/azurerm/network/manager_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own manager deployment" });

      resource(ManagerDeployment, "ci-runner", {
        configurationIds: ["example"],
        location: "example",
        networkManagerId: "example",
        scopeAccess: "example",
        triggers: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.network import ManagerDeployment, ManagerDeploymentConfig

    def describe():
        ubx.intent("payments own manager deployment")

        ubx.resource(ManagerDeployment, "ci-runner", ManagerDeploymentConfig(
            configuration_ids=["example"],
            location="example",
            network_manager_id="example",
            scope_access="example",
            triggers={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_network_manager_deployment, with configuration ids
    a set value, location example, network manager id example, scope
    access example, triggers a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration_ids" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_manager_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scope_access" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
