---
title: "azure_network_virtualnetwork_network_interface"
description: "`azure_network_virtualnetwork_network_interface` is the virtual network interface card attaching a VM to a subnet..."
---

`azure_network_virtualnetwork_network_interface` is the virtual network interface card attaching a VM to a subnet, carrying its own private (and optionally public) IP configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualnetwork network interface"})

    		ubx.Resource(network.VirtualnetworkNetworkInterface, "ci-runner", network.VirtualnetworkNetworkInterfaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkNetworkInterface } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_network_interface.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualnetwork network interface" });

      resource(VirtualnetworkNetworkInterface, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkNetworkInterface, VirtualnetworkNetworkInterfaceConfig

    def describe():
        ubx.intent("payments own virtualnetwork network interface")

        ubx.resource(VirtualnetworkNetworkInterface, "ci-runner", VirtualnetworkNetworkInterfaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_network_interface, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="extended_location" type="object">
  ExtendedLocation complex type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  NetworkInterface properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="auxiliary_mode" type="string">
      Auxiliary mode of Network Interface resource. Optional.
    </ResponseField>
    <ResponseField name="auxiliary_sku" type="string">
      Auxiliary sku of Network Interface resource. Optional.
    </ResponseField>
    <ResponseField name="default_outbound_connectivity_enabled" type="bool">
      Whether default outbound connectivity for nic was configured or not. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="disable_tcp_state_tracking" type="bool">
      Indicates whether to disable tcp state tracking. Optional.
    </ResponseField>
    <ResponseField name="dns_settings" type="object">
      DNS settings of a network interface. Optional.
      <Expandable title="properties">
        <ResponseField name="applied_dns_servers" type="list(string)">
          If the VM that uses this NIC is part of an Availability Set, then this list will have the union of all DNS servers from all NICs that are part of the Availability Set. This property is what is configured on each of those VMs. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="dns_servers" type="list(string)">
          List of DNS servers IP addresses. Use 'AzureProvidedDNS' to switch to azure provided DNS resolution. 'AzureProvidedDNS' value cannot be combined with other IPs, it must be the only value in dnsServers collection. Optional.
        </ResponseField>
        <ResponseField name="internal_dns_name_label" type="string">
          Relative DNS name for this NIC used for internal communications between VMs in the same virtual network. Optional.
        </ResponseField>
        <ResponseField name="internal_domain_name_suffix" type="string">
          Even if internalDnsNameLabel is not specified, a DNS entry is created for the primary NIC of the VM. This DNS name can be constructed by concatenating the VM name with the value of internalDomainNameSuffix. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="internal_fqdn" type="string">
          Fully qualified DNS name supporting internal communications between VMs in the same virtual network. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dscp_configuration" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_accelerated_networking" type="bool">
      If the network interface is configured for accelerated networking. Not applicable to VM sizes which require accelerated networking. Optional.
    </ResponseField>
    <ResponseField name="enable_ipforwarding" type="bool">
      Indicates whether IP forwarding is enabled on this network interface. Optional.
    </ResponseField>
    <ResponseField name="hosted_workloads" type="list(string)">
      A list of references to linked BareMetal resources. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="ip_configurations" type="list(object)">
      A list of IPConfigurations of the network interface. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Optional; if omitted, computed by Microsoft Azure.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mac_address" type="string">
      The MAC address of the network interface. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="migration_phase" type="string">
      Migration phase of Network Interface resource. Optional.
    </ResponseField>
    <ResponseField name="network_security_group" type="object">
      NetworkSecurityGroup resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Network Security Group resource. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="nic_type" type="string">
      Type of Network Interface resource. Optional.
    </ResponseField>
    <ResponseField name="primary" type="bool">
      Whether this is a primary network interface on a virtual machine. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint" type="object">
      Private endpoint resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="extended_location" type="object">
          ExtendedLocation complex type. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the extended location. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of the private endpoint. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service" type="object">
      Private link service resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="extended_location" type="object">
          ExtendedLocation complex type. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the extended location. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of the private link service. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the network interface resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tap_configurations" type="list(object)">
      A list of TapConfigurations of the network interface. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Optional; if omitted, computed by Microsoft Azure.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_machine" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vnet_encryption_supported" type="bool">
      Whether the virtual machine this nic is attached to supports encryption. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="workload_type" type="string">
      WorkloadType of the NetworkInterface for BareMetal resources Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
