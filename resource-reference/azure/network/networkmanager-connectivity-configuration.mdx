---
title: "azure_network_networkmanager_connectivity_configuration"
description: "`azure_network_networkmanager_connectivity_configuration` defines the connectivity topology (hub-and-spoke or mesh) Azure Virtual Network Manager..."
---

`azure_network_networkmanager_connectivity_configuration` defines the connectivity topology (hub-and-spoke or mesh) Azure Virtual Network Manager establishes across the virtual network(s) in a network group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_networkmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_networkmanager/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_networkmanager/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own networkmanager connectivity configuration"})

    		ubx.Resource(network.NetworkmanagerConnectivityConfiguration, "ci-runner", network.NetworkmanagerConnectivityConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_networkmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkmanagerConnectivityConfiguration } from "./local-sdk/azure_network_networkmanager/sdk/typescript/azure_network_networkmanager/network/networkmanager_connectivity_configuration.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own networkmanager connectivity configuration" });

      resource(NetworkmanagerConnectivityConfiguration, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_networkmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_networkmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_networkmanager.network import NetworkmanagerConnectivityConfiguration, NetworkmanagerConnectivityConfigurationConfig

    def describe():
        ubx.intent("payments own networkmanager connectivity configuration")

        ubx.resource(NetworkmanagerConnectivityConfiguration, "ci-runner", NetworkmanagerConnectivityConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkmanager_connectivity_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of network manager connectivity configuration Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="applies_to_groups" type="list(object)" required>
      Groups for configuration Required.
      <Expandable title="properties">
        <ResponseField name="group_connectivity" type="string">
          Whether direct connectivity is established between all members of this network group. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_global" type="string">
          Whether this connectivity configuration spans multiple regions. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_group_id" type="string">
          A reference to the network group this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="use_hub_gateway" type="string">
          Whether traffic between spoke virtual networks is routed through the hub's own gateway. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connectivity_capabilities" type="object">
      Collection of additional settings to enhance specific topology behaviors of the connectivity configuration resource. Optional.
      <Expandable title="properties">
        <ResponseField name="connected_group_address_overlap" type="string" required>
          Behavior to handle overlapped IP address space among members of the connected group of the connectivity configuration. Required.
        </ResponseField>
        <ResponseField name="connected_group_private_endpoints_scale" type="string" required>
          Option indicating the scale of private endpoints allowed in the connected group of the connectivity configuration. Required.
        </ResponseField>
        <ResponseField name="peering_enforcement" type="string" required>
          Option indicating enforcement of peerings created by the connectivity configuration. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connectivity_topology" type="string" required>
      Connectivity topology type. Required.
    </ResponseField>
    <ResponseField name="delete_existing_peering" type="string">
      Flag if need to remove current existing peerings. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the connectivity configuration. Optional.
    </ResponseField>
    <ResponseField name="hubs" type="list(object)">
      List of hubItems Optional.
      <Expandable title="properties">
        <ResponseField name="resource_id" type="string">
          A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          The category of resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_global" type="string">
      Flag if global mesh is supported. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      Unique identifier for this resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
