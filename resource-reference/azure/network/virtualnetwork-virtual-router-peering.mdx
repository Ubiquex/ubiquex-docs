---
title: "azure_network_virtualnetwork_virtual_router_peering"
description: "`azure_network_virtualnetwork_virtual_router_peering` establishes a BGP peering session between Azure Route Server and a network virtual appliance's..."
---

`azure_network_virtualnetwork_virtual_router_peering` establishes a BGP peering session between Azure Route Server and a network virtual appliance's own router.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualnetwork virtual router peering"})

    		ubx.Resource(network.VirtualnetworkVirtualRouterPeering, "ci-runner", network.VirtualnetworkVirtualRouterPeeringConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualRouterPeering } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_virtual_router_peering.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualnetwork virtual router peering" });

      resource(VirtualnetworkVirtualRouterPeering, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkVirtualRouterPeering, VirtualnetworkVirtualRouterPeeringConfig

    def describe():
        ubx.intent("payments own virtualnetwork virtual router peering")

        ubx.resource(VirtualnetworkVirtualRouterPeering, "ci-runner", VirtualnetworkVirtualRouterPeeringConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_virtual_router_peering, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the rule group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="peer_asn" type="number">
      Peer ASN. Optional.
    </ResponseField>
    <ResponseField name="peer_ip" type="string">
      Peer IP. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
