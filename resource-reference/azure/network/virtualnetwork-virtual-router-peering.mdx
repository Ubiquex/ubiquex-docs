---
title: "azure_network_virtualnetwork_virtual_router_peering"
description: "`azure_network_virtualnetwork_virtual_router_peering` establishes a BGP peering session between Azure Route Server and a network virtual appliance's..."
---

`azure_network_virtualnetwork_virtual_router_peering` establishes a BGP peering session between Azure Route Server and a network virtual appliance's own router.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork virtual router peering"})

    		ubx.Resource(network.VirtualnetworkVirtualRouterPeering, "example", network.VirtualnetworkVirtualRouterPeeringConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualRouterPeering } from "@ubx/sdk-azure/azure/network/virtualnetwork_virtual_router_peering";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork virtual router peering" });

      resource(VirtualnetworkVirtualRouterPeering, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkVirtualRouterPeering, VirtualnetworkVirtualRouterPeeringConfig

    def describe():
        ubx.intent("example own virtualnetwork virtual router peering")

        ubx.resource(VirtualnetworkVirtualRouterPeering, "example", VirtualnetworkVirtualRouterPeeringConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_virtual_router_peering, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the rule group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="peer_asn" type="number">
      Peer ASN. Optional.
    </ResponseField>
    <ResponseField name="peer_ip" type="string">
      Peer IP. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
