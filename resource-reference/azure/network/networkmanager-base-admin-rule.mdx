---
title: "azure_network_networkmanager_base_admin_rule"
description: "`azure_network_networkmanager_base_admin_rule` is the base type for a security admin or default rule enforced centrally across a Virtual Network..."
---

`azure_network_networkmanager_base_admin_rule` is the base type for a security admin or default rule enforced centrally across a Virtual Network Manager network group, evaluated before any local NSG rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_networkmanager --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_networkmanager/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_networkmanager/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkmanager base admin rule"})

    		ubx.Resource(network.NetworkmanagerBaseAdminRule, "example", network.NetworkmanagerBaseAdminRuleConfig{
    			Kind: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_networkmanager --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkmanagerBaseAdminRule } from "./local-sdk/azure_network_networkmanager/sdk/typescript/azure_network_networkmanager/network/networkmanager_base_admin_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own networkmanager base admin rule" });

      resource(NetworkmanagerBaseAdminRule, "example", {
        kind: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_networkmanager --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_networkmanager/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_networkmanager.network import NetworkmanagerBaseAdminRule, NetworkmanagerBaseAdminRuleConfig

    def describe():
        ubx.intent("example own networkmanager base admin rule")

        ubx.resource(NetworkmanagerBaseAdminRule, "example", NetworkmanagerBaseAdminRuleConfig(
            kind="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkmanager_base_admin_rule, with kind
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string" required>
  Whether the rule is custom or default. Required.
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
