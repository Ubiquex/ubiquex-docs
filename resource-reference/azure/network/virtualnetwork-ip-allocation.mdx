---
title: "azure_network_virtualnetwork_ip_allocation"
description: "`azure_network_virtualnetwork_ip_allocation` reserves a specific IP address block for a defined purpose (e.g. an Azure PaaS service delegation) within..."
---

`azure_network_virtualnetwork_ip_allocation` reserves a specific IP address block for a defined purpose (e.g. an Azure PaaS service delegation) within a virtual network's own address space.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork ip allocation"})

    		ubx.Resource(network.VirtualnetworkIpAllocation, "example", network.VirtualnetworkIpAllocationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkIpAllocation } from "@ubx/sdk-azure/azure/network/virtualnetwork_ip_allocation";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork ip allocation" });

      resource(VirtualnetworkIpAllocation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.virtualnetwork_ip_allocation import VirtualnetworkIpAllocation, VirtualnetworkIpAllocationConfig

    def describe():
        ubx.intent("example own virtualnetwork ip allocation")

        ubx.resource(VirtualnetworkIpAllocation, "example", VirtualnetworkIpAllocationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_ip_allocation, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the IpAllocation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allocation_tags" type="map(string)">
      IpAllocation tags. Optional.
    </ResponseField>
    <ResponseField name="ipam_allocation_id" type="string">
      The IPAM allocation ID. Optional.
    </ResponseField>
    <ResponseField name="prefix" type="string">
      The address prefix for the IpAllocation. Optional.
    </ResponseField>
    <ResponseField name="prefix_length" type="number">
      The address prefix length for the IpAllocation. Optional.
    </ResponseField>
    <ResponseField name="prefix_type" type="string">
      IP address version. Optional.
    </ResponseField>
    <ResponseField name="subnet" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      IpAllocation type. Optional.
    </ResponseField>
    <ResponseField name="virtual_network" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
