---
title: "azure_network_virtualnetwork_static_cidr"
description: "`azure_network_virtualnetwork_static_cidr` tracks an IP address range already in use outside Azure Virtual Network Manager's own control (e.g...."
---

`azure_network_virtualnetwork_static_cidr` tracks an IP address range already in use outside Azure Virtual Network Manager's own control (e.g. on-premises or manually allocated) within an IPAM pool, preventing it from being allocated again.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualnetwork static cidr"})

    		ubx.Resource(network.VirtualnetworkStaticCidr, "ci-runner", network.VirtualnetworkStaticCidrConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkStaticCidr } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_static_cidr.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualnetwork static cidr" });

      resource(VirtualnetworkStaticCidr, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkStaticCidr, VirtualnetworkStaticCidrConfig

    def describe():
        ubx.intent("payments own virtualnetwork static cidr")

        ubx.resource(VirtualnetworkStaticCidr, "ci-runner", VirtualnetworkStaticCidrConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_static_cidr, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of static CIDR resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="address_prefixes" type="list(string)">
      List of IP address prefixes of the resource. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      A user-supplied, human-readable note explaining this resource's own purpose. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="number_of_ipaddresses_to_allocate" type="string">
      Number of IP addresses to allocate for a static CIDR resource. The IP addresses will be assigned based on IpamPools available space. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="total_number_of_ipaddresses" type="string">
      Total number of IP addresses allocated for the static CIDR resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
