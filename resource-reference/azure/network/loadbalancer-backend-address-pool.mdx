---
title: "azure_network_loadbalancer_backend_address_pool"
description: "`azure_network_loadbalancer_backend_address_pool` is the set of backend instance(s) or IP address(es) an Azure Load Balancer distributes matching..."
---

`azure_network_loadbalancer_backend_address_pool` is the set of backend instance(s) or IP address(es) an Azure Load Balancer distributes matching traffic across.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_loadbalancer --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_loadbalancer/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_loadbalancer/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own loadbalancer backend address pool"})

    		ubx.Resource(network.LoadbalancerBackendAddressPool, "ci-runner", network.LoadbalancerBackendAddressPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_loadbalancer --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoadbalancerBackendAddressPool } from "./local-sdk/azure_network_loadbalancer/sdk/typescript/azure_network_loadbalancer/network/loadbalancer_backend_address_pool.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own loadbalancer backend address pool" });

      resource(LoadbalancerBackendAddressPool, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_loadbalancer --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_loadbalancer/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_loadbalancer.network import LoadbalancerBackendAddressPool, LoadbalancerBackendAddressPoolConfig

    def describe():
        ubx.intent("payments own loadbalancer backend address pool")

        ubx.resource(LoadbalancerBackendAddressPool, "ci-runner", LoadbalancerBackendAddressPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_loadbalancer_backend_address_pool, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the backend address pool. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="backend_ipconfigurations" type="list(object)">
      An array of references to IP addresses defined in network interfaces. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="drain_period_in_seconds" type="number">
      Amount of seconds Load Balancer waits for before sending RESET to client and backend address. Optional.
    </ResponseField>
    <ResponseField name="inbound_nat_rules" type="list(object)">
      An array of references to inbound NAT rules that use this backend address pool. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="load_balancer_backend_addresses" type="list(object)">
      An array of backend addresses. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="load_balancing_rules" type="list(object)">
      An array of references to load balancing rules that use this backend address pool. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="location" type="string">
      The location of the backend address pool. Optional.
    </ResponseField>
    <ResponseField name="outbound_rule" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="outbound_rules" type="list(object)">
      An array of references to outbound rules that use this backend address pool. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="sync_mode" type="string">
      Backend address synchronous mode for the backend pool Optional.
    </ResponseField>
    <ResponseField name="tunnel_interfaces" type="list(object)">
      An array of gateway load balancer tunnel interfaces. Optional.
      <Expandable title="properties">
        <ResponseField name="identifier" type="number">
          A unique identifier for this resource or configuration. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port" type="number">
          A TCP or UDP port number. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protocol" type="string">
          The network protocol this rule or listener applies to, e.g. `Tcp`, `Udp`, or `*` for any. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_network" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
