---
title: "azure_network_networkwatcher_network_watcher"
description: "`azure_network_networkwatcher_network_watcher` is the regional Network Watcher resource that diagnostic and monitoring tool(s) (flow logs..."
---

`azure_network_networkwatcher_network_watcher` is the regional Network Watcher resource that diagnostic and monitoring tool(s) (flow logs, connection monitors, packet capture) are created under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkwatcher network watcher"})

    		ubx.Resource(network.NetworkwatcherNetworkWatcher, "example", network.NetworkwatcherNetworkWatcherConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkwatcherNetworkWatcher } from "@ubx/sdk-azure/azure/network/networkwatcher_network_watcher";

    export default stack("example", () => {
      intent({ summary: "example own networkwatcher network watcher" });

      resource(NetworkwatcherNetworkWatcher, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import NetworkwatcherNetworkWatcher, NetworkwatcherNetworkWatcherConfig

    def describe():
        ubx.intent("example own networkwatcher network watcher")

        ubx.resource(NetworkwatcherNetworkWatcher, "example", NetworkwatcherNetworkWatcherConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkwatcher_network_watcher, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The network watcher properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
