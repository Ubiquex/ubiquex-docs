---
title: "azure_network_expressroute_express_route_circuit_authorization"
description: "`azure_network_expressroute_express_route_circuit_authorization` generates an authorization key letting another subscription's own virtual network..."
---

`azure_network_expressroute_express_route_circuit_authorization` generates an authorization key letting another subscription's own virtual network gateway connect to this ExpressRoute circuit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_expressroute --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_expressroute/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_expressroute/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own expressroute express route circuit authorization"})

    		ubx.Resource(network.ExpressrouteExpressRouteCircuitAuthorization, "example", network.ExpressrouteExpressRouteCircuitAuthorizationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_expressroute --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteCircuitAuthorization } from "./local-sdk/azure_network_expressroute/sdk/typescript/azure_network_expressroute/network/expressroute_express_route_circuit_authorization.ts";

    export default stack("example", () => {
      intent({
        summary: "example own expressroute express route circuit authorization",
      });

      resource(ExpressrouteExpressRouteCircuitAuthorization, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_expressroute --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_expressroute/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_expressroute.network import ExpressrouteExpressRouteCircuitAuthorization, ExpressrouteExpressRouteCircuitAuthorizationConfig

    def describe():
        ubx.intent("example own expressroute express route circuit authorization")

        ubx.resource(ExpressrouteExpressRouteCircuitAuthorization, "example", ExpressrouteExpressRouteCircuitAuthorizationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_expressroute_express_route_circuit_authorization,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of ExpressRouteCircuitAuthorization. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authorization_key" type="string">
      The authorization key. Optional.
    </ResponseField>
    <ResponseField name="authorization_use_status" type="string">
      The authorization use status. Optional.
    </ResponseField>
    <ResponseField name="connection_resource_uri" type="string">
      The reference to the ExpressRoute connection resource using the authorization. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
