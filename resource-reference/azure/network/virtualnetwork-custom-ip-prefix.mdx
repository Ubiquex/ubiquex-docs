---
title: "azure_network_virtualnetwork_custom_ip_prefix"
description: "`azure_network_virtualnetwork_custom_ip_prefix` brings a caller-owned public IP address range into Azure for advertisement and use..."
---

`azure_network_virtualnetwork_custom_ip_prefix` brings a caller-owned public IP address range into Azure for advertisement and use, via Custom IP Prefix (BYOIP).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork custom ip prefix"})

    		ubx.Resource(network.VirtualnetworkCustomIpPrefix, "example", network.VirtualnetworkCustomIpPrefixConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkCustomIpPrefix } from "@ubx/sdk-azure/azure/network/virtualnetwork_custom_ip_prefix";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork custom ip prefix" });

      resource(VirtualnetworkCustomIpPrefix, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkCustomIpPrefix, VirtualnetworkCustomIpPrefixConfig

    def describe():
        ubx.intent("example own virtualnetwork custom ip prefix")

        ubx.resource(VirtualnetworkCustomIpPrefix, "example", VirtualnetworkCustomIpPrefixConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_custom_ip_prefix, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="extended_location" type="object">
  ExtendedLocation complex type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Custom IP prefix properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="asn" type="string">
      The ASN for CIDR advertising. Should be an integer as string. Optional.
    </ResponseField>
    <ResponseField name="authorization_message" type="string">
      Authorization message for WAN validation. Optional.
    </ResponseField>
    <ResponseField name="child_custom_ip_prefixes" type="list(object)">
      The list of all Children for IPv6 /48 CustomIpPrefix. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="cidr" type="string">
      The prefix range in CIDR notation. Should include the start address and the prefix length. Optional.
    </ResponseField>
    <ResponseField name="commissioned_state" type="string">
      The commissioned state of the Custom IP Prefix. Optional.
    </ResponseField>
    <ResponseField name="custom_ip_prefix_parent" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="express_route_advertise" type="bool">
      Whether to do express route advertise. Optional.
    </ResponseField>
    <ResponseField name="failed_reason" type="string">
      The reason why resource is in failed state. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="geo" type="string">
      The Geo for CIDR advertising. Should be an Geo code. Optional.
    </ResponseField>
    <ResponseField name="no_internet_advertise" type="bool">
      Whether to Advertise the range to Internet. Optional.
    </ResponseField>
    <ResponseField name="prefix_type" type="string">
      Type of custom IP prefix. Should be Singular, Parent, or Child. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="public_ip_prefixes" type="list(object)">
      The list of all referenced PublicIpPrefixes. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the custom IP prefix resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="signed_message" type="string">
      Signed message for WAN validation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  A list of availability zones denoting where the resource needs to come from. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
