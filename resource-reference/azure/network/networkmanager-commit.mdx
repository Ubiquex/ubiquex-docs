---
title: "azure_network_networkmanager_commit"
description: "`azure_network_networkmanager_commit` deploys a Virtual Network Manager configuration (connectivity or security) to its own target scope..."
---

`azure_network_networkmanager_commit` deploys a Virtual Network Manager configuration (connectivity or security) to its own target scope, making it take actual effect.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkmanager commit"})

    		ubx.Resource(network.NetworkmanagerCommit, "example", network.NetworkmanagerCommitConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkmanagerCommit } from "@ubx/sdk-azure/azure/network/networkmanager_commit";

    export default stack("example", () => {
      intent({ summary: "example own networkmanager commit" });

      resource(NetworkmanagerCommit, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.networkmanager_commit import NetworkmanagerCommit, NetworkmanagerCommitConfig

    def describe():
        ubx.intent("example own networkmanager commit")

        ubx.resource(NetworkmanagerCommit, "example", NetworkmanagerCommitConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkmanager_commit, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of commit Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="active_locations" type="list(string)">
      List of active locations. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="commit_type" type="string" required>
      Configuration Deployment Type. Required.
    </ResponseField>
    <ResponseField name="configuration_ids" type="list(string)">
      List of configuration IDs. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the commit. Optional.
    </ResponseField>
    <ResponseField name="force_update_tag" type="string">
      A value that, when changed, forces the commit to be re-evaluated and redeployed. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      Unique identifier for this resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="target_locations" type="list(string)" required>
      List of target locations. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
