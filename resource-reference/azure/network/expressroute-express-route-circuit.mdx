---
title: "azure_network_expressroute_express_route_circuit"
description: "`azure_network_expressroute_express_route_circuit` is the logical connection to a connectivity provider's own ExpressRoute service..."
---

`azure_network_expressroute_express_route_circuit` is the logical connection to a connectivity provider's own ExpressRoute service, that private and Microsoft peering(s) are established over.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_expressroute --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_expressroute/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_expressroute/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own expressroute express route circuit"})

    		ubx.Resource(network.ExpressrouteExpressRouteCircuit, "ci-runner", network.ExpressrouteExpressRouteCircuitConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_expressroute --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteCircuit } from "./local-sdk/azure_network_expressroute/sdk/typescript/azure_network_expressroute/network/expressroute_express_route_circuit.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own expressroute express route circuit" });

      resource(ExpressrouteExpressRouteCircuit, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_expressroute --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_expressroute/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_expressroute.network import ExpressrouteExpressRouteCircuit, ExpressrouteExpressRouteCircuitConfig

    def describe():
        ubx.intent("payments own expressroute express route circuit")

        ubx.resource(ExpressrouteExpressRouteCircuit, "ci-runner", ExpressrouteExpressRouteCircuitConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_expressroute_express_route_circuit, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of ExpressRouteCircuit. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="activation_key" type="string">
      Activation Key from partner cloud provider. Optional.
    </ResponseField>
    <ResponseField name="allow_classic_operations" type="bool">
      Allow classic operations. Optional.
    </ResponseField>
    <ResponseField name="authorization_key" type="string">
      The authorizationKey. Optional.
    </ResponseField>
    <ResponseField name="authorization_status" type="string">
      The authorization status of the Circuit. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="authorizations" type="list(object)">
      The list of authorizations. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="bandwidth_in_gbps" type="number">
      The bandwidth of the circuit when the circuit is provisioned on an ExpressRoutePort resource. Optional.
    </ResponseField>
    <ResponseField name="circuit_provisioning_state" type="string">
      The CircuitProvisioningState state of the resource. Optional.
    </ResponseField>
    <ResponseField name="enable_direct_port_rate_limit" type="bool">
      Flag denoting rate-limiting status of the ExpressRoute direct-port circuit. Optional.
    </ResponseField>
    <ResponseField name="express_route_port" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gateway_manager_etag" type="string">
      The GatewayManager Etag. Optional.
    </ResponseField>
    <ResponseField name="global_reach_enabled" type="bool">
      Flag denoting global reach status. Optional.
    </ResponseField>
    <ResponseField name="partner_account_id" type="string">
      Account ID of customer account on partner cloud provider. Optional.
    </ResponseField>
    <ResponseField name="peerings" type="list(object)">
      The list of peerings. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resiliency_level" type="string">
      The resiliency level of the ExpressRoute circuit. Optional.
    </ResponseField>
    <ResponseField name="service_key" type="string">
      The ServiceKey. Optional.
    </ResponseField>
    <ResponseField name="service_provider_notes" type="string">
      The ServiceProviderNotes. Optional.
    </ResponseField>
    <ResponseField name="service_provider_properties" type="object">
      Contains ServiceProviderProperties in an ExpressRouteCircuit. Optional.
      <Expandable title="properties">
        <ResponseField name="bandwidth_in_mbps" type="number">
          The BandwidthInMbps. Optional.
        </ResponseField>
        <ResponseField name="peering_location" type="string">
          The peering location. Optional.
        </ResponseField>
        <ResponseField name="service_provider_name" type="string">
          The serviceProviderName. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_provider_provisioning_state" type="string">
      The ServiceProviderProvisioningState state of the resource. Optional.
    </ResponseField>
    <ResponseField name="stag" type="number">
      The identifier of the circuit traffic. Outer tag for QinQ encapsulation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Contains SKU in an ExpressRouteCircuit. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="family" type="string">
      The family of the SKU. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the SKU. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The tier of the SKU. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
