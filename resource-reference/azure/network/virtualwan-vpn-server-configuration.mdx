---
title: "azure_network_virtualwan_vpn_server_configuration"
description: "`azure_network_virtualwan_vpn_server_configuration` defines the authentication method and protocol(s) a Virtual WAN point-to-site VPN gateway accepts..."
---

`azure_network_virtualwan_vpn_server_configuration` defines the authentication method and protocol(s) a Virtual WAN point-to-site VPN gateway accepts client connections with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualwan vpn server configuration"})

    		ubx.Resource(network.VirtualwanVpnServerConfiguration, "example", network.VirtualwanVpnServerConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualwanVpnServerConfiguration } from "@ubx/sdk-azure/azure/network/virtualwan_vpn_server_configuration";

    export default stack("example", () => {
      intent({ summary: "example own virtualwan vpn server configuration" });

      resource(VirtualwanVpnServerConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.virtualwan_vpn_server_configuration import VirtualwanVpnServerConfiguration, VirtualwanVpnServerConfigurationConfig

    def describe():
        ubx.intent("example own virtualwan vpn server configuration")

        ubx.resource(VirtualwanVpnServerConfiguration, "example", VirtualwanVpnServerConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualwan_vpn_server_configuration, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Parameters for VpnServerConfiguration. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="aad_authentication_parameters" type="object">
      AAD Vpn authentication type related parameters. Optional.
      <Expandable title="properties">
        <ResponseField name="aad_audience" type="string">
          AAD Vpn authentication parameter AAD audience. Optional.
        </ResponseField>
        <ResponseField name="aad_issuer" type="string">
          AAD Vpn authentication parameter AAD issuer. Optional.
        </ResponseField>
        <ResponseField name="aad_tenant" type="string">
          AAD Vpn authentication parameter AAD tenant. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="configuration_policy_groups" type="list(object)">
      List of all VpnServerConfigurationPolicyGroups. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="etag" type="string">
      A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the VpnServerConfiguration that is unique within a resource group. Optional.
    </ResponseField>
    <ResponseField name="p2_svpn_gateways" type="list(object)">
      List of references to P2SVpnGateways. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the VpnServerConfiguration resource. Possible values are: 'Updating', 'Deleting', and 'Failed'. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="radius_client_root_certificates" type="list(object)">
      Radius client root certificate of VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="thumbprint" type="string">
          A cryptographic thumbprint of this certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="radius_server_address" type="string">
      The radius server address property of the VpnServerConfiguration resource for point to site client connection. Optional.
    </ResponseField>
    <ResponseField name="radius_server_root_certificates" type="list(object)">
      Radius Server root certificate of VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_cert_data" type="string">
          The base64-encoded public certificate data used to authenticate a point-to-site VPN client. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="radius_server_secret" type="string">
      The radius secret property of the VpnServerConfiguration resource for point to site client connection. We will no longer return radiusServerSecret in VpnServerConfiguration Create/Update/Get/List/UpdateTags APIs response. Please use VpnServerConfiguration ListRadiusSecrets API to fetch radius server secrets. Optional.
    </ResponseField>
    <ResponseField name="radius_servers" type="list(object)">
      Multiple Radius Server configuration for VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="radius_server_address" type="string">
          The IP address of the RADIUS authentication server. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="radius_server_score" type="number">
          The relative priority of this RADIUS server among several configured. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="radius_server_secret" type="string">
          The shared secret used to authenticate to this RADIUS server. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_authentication_types" type="list(string)">
      VPN authentication types for the VpnServerConfiguration. Optional.
    </ResponseField>
    <ResponseField name="vpn_client_ipsec_policies" type="list(object)">
      VpnClientIpsecPolicies for VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="dh_group" type="string">
          The Diffie-Hellman group used for IKE Phase 1 key exchange, e.g. `DHGroup14` or `DHGroup24`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ike_encryption" type="string">
          The encryption algorithm used for IKE Phase 1 (establishing the secure channel itself), e.g. `AES256`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ike_integrity" type="string">
          The integrity (hashing) algorithm used for IKE Phase 1, e.g. `SHA384`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipsec_encryption" type="string">
          The encryption algorithm used for IKE Phase 2 (the actual IPsec data tunnel), e.g. `AES256`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipsec_integrity" type="string">
          The integrity (hashing) algorithm used for IKE Phase 2, e.g. `SHA256`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pfs_group" type="string">
          The Perfect Forward Secrecy group used when establishing a new IKE Phase 2 security association, or `None` to disable PFS. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sa_data_size_kilobytes" type="number">
          The IPsec security association's own data-volume lifetime, in kilobytes -- the tunnel rekeys once either this or `sa_life_time_seconds` is reached. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sa_life_time_seconds" type="number">
          The IPsec security association's own time-based lifetime, in seconds -- the tunnel rekeys once either this or `sa_data_size_kilobytes` is reached. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_client_revoked_certificates" type="list(object)">
      VPN client revoked certificate of VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="thumbprint" type="string">
          A cryptographic thumbprint of this certificate. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_client_root_certificates" type="list(object)">
      VPN client root certificate of VpnServerConfiguration. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_cert_data" type="string">
          The base64-encoded public certificate data used to authenticate a point-to-site VPN client. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_protocols" type="list(string)">
      VPN protocols for the VpnServerConfiguration. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
