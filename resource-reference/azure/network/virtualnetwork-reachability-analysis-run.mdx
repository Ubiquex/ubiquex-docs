---
title: "azure_network_virtualnetwork_reachability_analysis_run"
description: "`azure_network_virtualnetwork_reachability_analysis_run` is a single execution of Azure Virtual Network Manager's own Network Verifier against..."
---

`azure_network_virtualnetwork_reachability_analysis_run` is a single execution of Azure Virtual Network Manager's own Network Verifier against a reachability analysis intent, statically checking NSGs, routes, peering, and other configuration for whether the defined traffic path would actually succeed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork reachability analysis run"})

    		ubx.Resource(network.VirtualnetworkReachabilityAnalysisRun, "example", network.VirtualnetworkReachabilityAnalysisRunConfig{
    			Properties: map[string]any{"intent_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkReachabilityAnalysisRun } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_reachability_analysis_run.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork reachability analysis run" });

      resource(VirtualnetworkReachabilityAnalysisRun, "example", {
        properties: { intentId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkReachabilityAnalysisRun, VirtualnetworkReachabilityAnalysisRunConfig

    def describe():
        ubx.intent("example own virtualnetwork reachability analysis run")

        ubx.resource(VirtualnetworkReachabilityAnalysisRun, "example", VirtualnetworkReachabilityAnalysisRunConfig(
            properties={"intent_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_reachability_analysis_run,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Represents the Reachability Analysis Run properties. Required.
  <Expandable title="properties">
    <ResponseField name="analysis_result" type="string">
      The outcome of analyzing this traffic pattern. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      A user-supplied, human-readable note explaining this resource's own purpose. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="error_message" type="string">
      A human-readable description of the error. Computed by Microsoft Azure after `ship`. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="intent_content" type="object">
      Intent information. Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          A user-supplied, human-readable note explaining this resource's own purpose. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="destination_resource_id" type="string" required>
          Destination resource id of the intent. Required.
        </ResponseField>
        <ResponseField name="ip_traffic" type="object" required>
          IP traffic information. Required.
          <Expandable title="properties">
            <ResponseField name="destination_ips" type="list(string)" required>
              List of destination IP addresses of the traffic.. Required.
            </ResponseField>
            <ResponseField name="destination_ports" type="list(string)" required>
              The destination ports of the traffic. Required.
            </ResponseField>
            <ResponseField name="protocols" type="list(string)" required>
              The network protocol(s) this applies to. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_ips" type="list(string)" required>
              List of source IP addresses of the traffic.. Required.
            </ResponseField>
            <ResponseField name="source_ports" type="list(string)" required>
              The source ports of the traffic. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_resource_id" type="string" required>
          Source resource id of the intent. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intent_id" type="string" required>
      Id of the intent resource to run analysis on. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
