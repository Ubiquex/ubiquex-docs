---
title: "azure_network_expressroute_express_route_port_authorization"
description: "`azure_network_expressroute_express_route_port_authorization` generates an authorization key letting another subscription create an ExpressRoute..."
---

`azure_network_expressroute_express_route_port_authorization` generates an authorization key letting another subscription create an ExpressRoute circuit on this ExpressRoute Direct port.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own expressroute express route port authorization"})

    		ubx.Resource(network.ExpressrouteExpressRoutePortAuthorization, "example", network.ExpressrouteExpressRoutePortAuthorizationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRoutePortAuthorization } from "@ubx/sdk-azure/azure/network/expressroute_express_route_port_authorization";

    export default stack("example", () => {
      intent({
        summary: "example own expressroute express route port authorization",
      });

      resource(ExpressrouteExpressRoutePortAuthorization, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import ExpressrouteExpressRoutePortAuthorization, ExpressrouteExpressRoutePortAuthorizationConfig

    def describe():
        ubx.intent("example own expressroute express route port authorization")

        ubx.resource(ExpressrouteExpressRoutePortAuthorization, "example", ExpressrouteExpressRoutePortAuthorizationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_expressroute_express_route_port_authorization, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of ExpressRoutePort Authorization. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="authorization_key" type="string">
      The authorization key. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="authorization_use_status" type="string">
      The authorization use status. Optional.
    </ResponseField>
    <ResponseField name="circuit_resource_uri" type="string">
      The reference to the ExpressRoute circuit resource using the authorization. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
