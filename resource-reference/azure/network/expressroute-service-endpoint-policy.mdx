---
title: "azure_network_expressroute_service_endpoint_policy"
description: "`azure_network_expressroute_service_endpoint_policy` restricts which specific Azure service resource(s) a subnet's own VNet service endpoint traffic..."
---

`azure_network_expressroute_service_endpoint_policy` restricts which specific Azure service resource(s) a subnet's own VNet service endpoint traffic can reach.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_expressroute --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_expressroute/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_expressroute/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own expressroute service endpoint policy"})

    		ubx.Resource(network.ExpressrouteServiceEndpointPolicy, "ci-runner", network.ExpressrouteServiceEndpointPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_expressroute --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteServiceEndpointPolicy } from "./local-sdk/azure_network_expressroute/sdk/typescript/azure_network_expressroute/network/expressroute_service_endpoint_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own expressroute service endpoint policy" });

      resource(ExpressrouteServiceEndpointPolicy, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_expressroute --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_expressroute/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_expressroute.network import ExpressrouteServiceEndpointPolicy, ExpressrouteServiceEndpointPolicyConfig

    def describe():
        ubx.intent("payments own expressroute service endpoint policy")

        ubx.resource(ExpressrouteServiceEndpointPolicy, "ci-runner", ExpressrouteServiceEndpointPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_expressroute_service_endpoint_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="kind" type="string">
  Kind of service endpoint policy. This is metadata used for the Azure portal experience. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Service Endpoint Policy resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="contextual_service_endpoint_policies" type="list(string)">
      A collection of contextual service endpoint policy. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the service endpoint policy resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_alias" type="string">
      The alias indicating if the policy belongs to a service Optional.
    </ResponseField>
    <ResponseField name="service_endpoint_policy_definitions" type="list(object)">
      A collection of service endpoint policy definitions of the service endpoint policy. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="subnets" type="list(object)">
      A collection of references to subnets. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
