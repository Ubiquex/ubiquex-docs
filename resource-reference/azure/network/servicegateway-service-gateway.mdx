---
title: "azure_network_servicegateway_service_gateway"
description: "`azure_network_servicegateway_service_gateway` is a service gateway resource extending on-premises Active Directory-style connectivity or other managed..."
---

`azure_network_servicegateway_service_gateway` is a service gateway resource extending on-premises Active Directory-style connectivity or other managed gateway functionality into a virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_servicegateway --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_servicegateway/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_servicegateway/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own servicegateway service gateway"})

    		ubx.Resource(network.ServicegatewayServiceGateway, "example", network.ServicegatewayServiceGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_servicegateway --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicegatewayServiceGateway } from "./local-sdk/azure_network_servicegateway/sdk/typescript/azure_network_servicegateway/network/servicegateway_service_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own servicegateway service gateway" });

      resource(ServicegatewayServiceGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_servicegateway --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_servicegateway/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_servicegateway.network import ServicegatewayServiceGateway, ServicegatewayServiceGatewayConfig

    def describe():
        ubx.intent("example own servicegateway service gateway")

        ubx.resource(ServicegatewayServiceGateway, "example", ServicegatewayServiceGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_servicegateway_service_gateway, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the service gateway. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the service gateway resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="route_target_address" type="object">
      Properties of route target address Optional.
      <Expandable title="properties">
        <ResponseField name="private_ipaddress" type="string">
          The private IPv4 or IPv6 address of the service gateway route target address. Optional.
        </ResponseField>
        <ResponseField name="private_ipallocation_method" type="string">
          IP address allocation method. Optional.
        </ResponseField>
        <ResponseField name="subnet" type="object">
          Subnet in a virtual network resource. Optional.
          <Expandable title="properties">
            <ResponseField name="etag" type="string">
              A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="properties" type="object">
              Properties of the subnet. Optional.
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="route_target_address_v6" type="object">
      Properties of route target address Optional.
      <Expandable title="properties">
        <ResponseField name="private_ipaddress" type="string">
          The private IPv4 or IPv6 address of the service gateway route target address. Optional.
        </ResponseField>
        <ResponseField name="private_ipallocation_method" type="string">
          IP address allocation method. Optional.
        </ResponseField>
        <ResponseField name="subnet" type="object">
          Subnet in a virtual network resource. Optional.
          <Expandable title="properties">
            <ResponseField name="etag" type="string">
              A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="properties" type="object">
              Properties of the subnet. Optional.
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="virtual_network" type="object">
      Virtual Network resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="extended_location" type="object">
          ExtendedLocation complex type. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the extended location. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of the virtual network. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SKU of a service gateway. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Name of a service gateway SKU. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Tier of a service gateway SKU. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  A list of availability zones denoting the zone in which service gateway should be deployed. - The zone values must be provided as strings representing numeric identifiers like "1", "2", "3" etc. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
