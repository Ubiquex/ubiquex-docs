---
title: "azure_network_expressroute_express_route_port"
description: "`azure_network_expressroute_express_route_port` provisions a dedicated, physical ExpressRoute Direct port pair at a Microsoft peering location..."
---

`azure_network_expressroute_express_route_port` provisions a dedicated, physical ExpressRoute Direct port pair at a Microsoft peering location, that circuit(s) can be created directly on top of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own expressroute express route port"})

    		ubx.Resource(network.ExpressrouteExpressRoutePort, "example", network.ExpressrouteExpressRoutePortConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRoutePort } from "@ubx/sdk-azure/azure/network/expressroute_express_route_port";

    export default stack("example", () => {
      intent({ summary: "example own expressroute express route port" });

      resource(ExpressrouteExpressRoutePort, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import ExpressrouteExpressRoutePort, ExpressrouteExpressRoutePortConfig

    def describe():
        ubx.intent("example own expressroute express route port")

        ubx.resource(ExpressrouteExpressRoutePort, "example", ExpressrouteExpressRoutePortConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_expressroute_express_route_port, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of identity used for the resource. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user assigned identities. The type 'None' will remove any identities from the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties specific to ExpressRoutePort resources. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allocation_date" type="string">
      Date of the physical port allocation to be used in Letter of Authorization. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="bandwidth_in_gbps" type="number">
      Bandwidth of procured ports in Gbps. Optional.
    </ResponseField>
    <ResponseField name="billing_type" type="string">
      The billing type of the ExpressRoutePort resource. Optional.
    </ResponseField>
    <ResponseField name="circuits" type="list(object)">
      Reference the ExpressRoute circuit(s) that are provisioned on this ExpressRoutePort resource. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encapsulation" type="string">
      Encapsulation method on physical ports. Optional.
    </ResponseField>
    <ResponseField name="ether_type" type="string">
      Ether type of the physical port. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="links" type="list(object)">
      The set of physical links of the ExpressRoutePort resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mtu" type="string">
      Maximum transmission unit of the physical port pair(s). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="peering_location" type="string">
      The name of the peering location that the ExpressRoutePort is mapped to physically. Optional.
    </ResponseField>
    <ResponseField name="provisioned_bandwidth_in_gbps" type="number">
      Aggregate Gbps of associated circuit bandwidths. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the express route port resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
