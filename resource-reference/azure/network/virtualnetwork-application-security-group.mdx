---
title: "azure_network_virtualnetwork_application_security_group"
description: "`azure_network_virtualnetwork_application_security_group` groups network interface(s) together as a named, logical unit..."
---

`azure_network_virtualnetwork_application_security_group` groups network interface(s) together as a named, logical unit, letting network security group rules reference the group instead of raw IP addresses.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork application security group"})

    		ubx.Resource(network.VirtualnetworkApplicationSecurityGroup, "example", network.VirtualnetworkApplicationSecurityGroupConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkApplicationSecurityGroup } from "@ubx/sdk-azure/azure/network/virtualnetwork_application_security_group";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork application security group" });

      resource(VirtualnetworkApplicationSecurityGroup, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.virtualnetwork_application_security_group import VirtualnetworkApplicationSecurityGroup, VirtualnetworkApplicationSecurityGroupConfig

    def describe():
        ubx.intent("example own virtualnetwork application security group")

        ubx.resource(VirtualnetworkApplicationSecurityGroup, "example", VirtualnetworkApplicationSecurityGroupConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_application_security_group,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Application security group properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the application security group resource. It uniquely identifies a resource, even if the user changes its name or migrate the resource across subscriptions or resource groups. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
