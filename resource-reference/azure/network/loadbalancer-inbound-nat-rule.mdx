---
title: "azure_network_loadbalancer_inbound_nat_rule"
description: "`azure_network_loadbalancer_inbound_nat_rule` forwards traffic on a specific frontend port of an Azure Load Balancer to one specific backend instance..."
---

`azure_network_loadbalancer_inbound_nat_rule` forwards traffic on a specific frontend port of an Azure Load Balancer to one specific backend instance, e.g. for direct RDP/SSH access to an individual VM.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_loadbalancer --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_loadbalancer/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_loadbalancer/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own loadbalancer inbound nat rule"})

    		ubx.Resource(network.LoadbalancerInboundNatRule, "example", network.LoadbalancerInboundNatRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_loadbalancer --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoadbalancerInboundNatRule } from "./local-sdk/azure_network_loadbalancer/sdk/typescript/azure_network_loadbalancer/network/loadbalancer_inbound_nat_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own loadbalancer inbound nat rule" });

      resource(LoadbalancerInboundNatRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_loadbalancer --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_loadbalancer/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_loadbalancer.network import LoadbalancerInboundNatRule, LoadbalancerInboundNatRuleConfig

    def describe():
        ubx.intent("example own loadbalancer inbound nat rule")

        ubx.resource(LoadbalancerInboundNatRule, "example", LoadbalancerInboundNatRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_loadbalancer_inbound_nat_rule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the inbound NAT rule. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="backend_address_pool" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="backend_ipconfiguration" type="object">
      IPConfiguration in a network interface. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of IP configuration. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="backend_port" type="number">
      The port used for the internal endpoint. Acceptable values range from 1 to 65535. Optional.
    </ResponseField>
    <ResponseField name="enable_floating_ip" type="bool">
      Configures a virtual machine's endpoint for the floating IP capability required to configure a SQL AlwaysOn Availability Group. This setting is required when using the SQL AlwaysOn Availability Groups in SQL server. This setting can't be changed after you create the endpoint. Optional.
    </ResponseField>
    <ResponseField name="enable_tcp_reset" type="bool">
      Receive bidirectional TCP Reset on TCP flow idle timeout or unexpected connection termination. This element is only used when the protocol is set to TCP. Optional.
    </ResponseField>
    <ResponseField name="frontend_ipconfiguration" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="frontend_port" type="number">
      The port for the external endpoint. Port numbers for each rule must be unique within the Load Balancer. Acceptable values range from 1 to 65534. Optional.
    </ResponseField>
    <ResponseField name="frontend_port_range_end" type="number">
      The port range end for the external endpoint. This property is used together with BackendAddressPool and FrontendPortRangeStart. Individual inbound NAT rule port mappings will be created for each backend address from BackendAddressPool. Acceptable values range from 1 to 65534. Optional.
    </ResponseField>
    <ResponseField name="frontend_port_range_start" type="number">
      The port range start for the external endpoint. This property is used together with BackendAddressPool and FrontendPortRangeEnd. Individual inbound NAT rule port mappings will be created for each backend address from BackendAddressPool. Acceptable values range from 1 to 65534. Optional.
    </ResponseField>
    <ResponseField name="idle_timeout_in_minutes" type="number">
      The timeout for the TCP idle connection. The value can be set between 4 and 30 minutes. The default value is 4 minutes. This element is only used when the protocol is set to TCP. Optional.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The transport protocol for the endpoint. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
