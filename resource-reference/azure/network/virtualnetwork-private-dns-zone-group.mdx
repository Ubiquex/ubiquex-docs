---
title: "azure_network_virtualnetwork_private_dns_zone_group"
description: "`azure_network_virtualnetwork_private_dns_zone_group` links a Private Endpoint to Private DNS zone(s)..."
---

`azure_network_virtualnetwork_private_dns_zone_group` links a Private Endpoint to Private DNS zone(s), automatically creating and maintaining the DNS record(s) that resolve to the endpoint's private IP address.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork private dns zone group"})

    		ubx.Resource(network.VirtualnetworkPrivateDnsZoneGroup, "example", network.VirtualnetworkPrivateDnsZoneGroupConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkPrivateDnsZoneGroup } from "@ubx/sdk-azure/azure/network/virtualnetwork_private_dns_zone_group";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork private dns zone group" });

      resource(VirtualnetworkPrivateDnsZoneGroup, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkPrivateDnsZoneGroup, VirtualnetworkPrivateDnsZoneGroupConfig

    def describe():
        ubx.intent("example own virtualnetwork private dns zone group")

        ubx.resource(VirtualnetworkPrivateDnsZoneGroup, "example", VirtualnetworkPrivateDnsZoneGroupConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_private_dns_zone_group called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the resource that is unique within a resource group. This name can be used to access the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the private dns zone group. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_dns_zone_configs" type="list(object)">
      A collection of private dns zone configurations of the private dns zone group. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
