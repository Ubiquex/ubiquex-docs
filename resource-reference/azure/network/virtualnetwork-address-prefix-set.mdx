---
title: "azure_network_virtualnetwork_address_prefix_set"
description: "`azure_network_virtualnetwork_address_prefix_set` defines a named, reusable set of IP address prefixes managed by Azure Virtual Network Manager..."
---

`azure_network_virtualnetwork_address_prefix_set` defines a named, reusable set of IP address prefixes managed by Azure Virtual Network Manager, referenced by IP address management pools and connectivity/security configurations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork address prefix set"})

    		ubx.Resource(network.VirtualnetworkAddressPrefixSet, "example", network.VirtualnetworkAddressPrefixSetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkAddressPrefixSet } from "@ubx/sdk-azure/azure/network/virtualnetwork_address_prefix_set";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork address prefix set" });

      resource(VirtualnetworkAddressPrefixSet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkAddressPrefixSet, VirtualnetworkAddressPrefixSetConfig

    def describe():
        ubx.intent("example own virtualnetwork address prefix set")

        ubx.resource(VirtualnetworkAddressPrefixSet, "example", VirtualnetworkAddressPrefixSetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_address_prefix_set, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of address prefix set. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="address_prefixes" type="list(string)" required>
      The list of address prefixes in CIDR notation. Supports both IPv4 and IPv6 CIDR notation (e.g. '10.0.0.0/16', '2001:db8::/32'). Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
