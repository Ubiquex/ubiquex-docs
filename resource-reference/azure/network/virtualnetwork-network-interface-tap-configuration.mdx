---
title: "azure_network_virtualnetwork_network_interface_tap_configuration"
description: "`azure_network_virtualnetwork_network_interface_tap_configuration` mirrors a network interface's own traffic to a Virtual Network TAP destination for..."
---

`azure_network_virtualnetwork_network_interface_tap_configuration` mirrors a network interface's own traffic to a Virtual Network TAP destination for out-of-band monitoring or analysis.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork network interface tap configuration"})

    		ubx.Resource(network.VirtualnetworkNetworkInterfaceTapConfiguration, "example", network.VirtualnetworkNetworkInterfaceTapConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkNetworkInterfaceTapConfiguration } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_network_interface_tap_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "example own virtualnetwork network interface tap configuration",
      });

      resource(VirtualnetworkNetworkInterfaceTapConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkNetworkInterfaceTapConfiguration, VirtualnetworkNetworkInterfaceTapConfigurationConfig

    def describe():
        ubx.intent("example own virtualnetwork network interface tap configuration")

        ubx.resource(VirtualnetworkNetworkInterfaceTapConfiguration, "example", VirtualnetworkNetworkInterfaceTapConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_virtualnetwork_network_interface_tap_configuration,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of Virtual Network Tap configuration. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="virtual_network_tap" type="object">
      Virtual Network Tap resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Virtual Network Tap properties. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
