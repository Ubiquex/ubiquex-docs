---
title: "azure_network_expressroute_route_filter_rule"
description: "`azure_network_expressroute_route_filter_rule` is a single BGP community match rule within an ExpressRoute route filter."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own expressroute route filter rule"})

    		ubx.Resource(network.ExpressrouteRouteFilterRule, "example", network.ExpressrouteRouteFilterRuleConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteRouteFilterRule } from "@ubx/sdk-azure/azure/network/expressroute_route_filter_rule";

    export default stack("example", () => {
      intent({ summary: "example own expressroute route filter rule" });

      resource(ExpressrouteRouteFilterRule, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import ExpressrouteRouteFilterRule, ExpressrouteRouteFilterRuleConfig

    def describe():
        ubx.intent("example own expressroute route filter rule")

        ubx.resource(ExpressrouteRouteFilterRule, "example", ExpressrouteRouteFilterRuleConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_expressroute_route_filter_rule called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="location" type="string">
  Resource location. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the resource that is unique within a resource group. This name can be used to access the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Route Filter Rule Resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="access" type="string" required>
      Access to be allowed or denied. Required.
    </ResponseField>
    <ResponseField name="communities" type="list(string)" required>
      The collection for bgp community values to filter on. e.g. ['12076:5010','12076:5020']. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="route_filter_rule_type" type="string" required>
      The rule type of the rule. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
