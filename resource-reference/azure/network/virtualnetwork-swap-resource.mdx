---
title: "azure_network_virtualnetwork_swap_resource"
description: "`azure_network_virtualnetwork_swap_resource` swaps a Virtual Network's own public IP prefix..."
---

`azure_network_virtualnetwork_swap_resource` swaps a Virtual Network's own public IP prefix, replacing every public IP address currently allocated from it with addresses from a new prefix in one atomic operation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork swap resource"})

    		ubx.Resource(network.VirtualnetworkSwapResource, "example", network.VirtualnetworkSwapResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkSwapResource } from "./local-sdk/azure/sdk/typescript/azure/network/virtualnetwork_swap_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork swap resource" });

      resource(VirtualnetworkSwapResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkSwapResource, VirtualnetworkSwapResourceConfig

    def describe():
        ubx.intent("example own virtualnetwork swap resource")

        ubx.resource(VirtualnetworkSwapResource, "example", VirtualnetworkSwapResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_swap_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Swap resource properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="slot_type" type="string">
      Specifies slot info on a cloud service Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
