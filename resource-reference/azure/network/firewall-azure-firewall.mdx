---
title: "azure_network_firewall_azure_firewall"
description: "`azure_network_firewall_azure_firewall` is an Azure Firewall instance, a managed, cloud-native network firewall providing stateful traffic filtering..."
---

`azure_network_firewall_azure_firewall` is an Azure Firewall instance, a managed, cloud-native network firewall providing stateful traffic filtering, threat intelligence, and TLS inspection for a virtual network or hub.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_firewall --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_firewall/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_firewall/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewall azure firewall"})

    		ubx.Resource(network.FirewallAzureFirewall, "example", network.FirewallAzureFirewallConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_firewall --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallAzureFirewall } from "./local-sdk/azure_network_firewall/sdk/typescript/azure_network_firewall/network/firewall_azure_firewall.ts";

    export default stack("example", () => {
      intent({ summary: "example own firewall azure firewall" });

      resource(FirewallAzureFirewall, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_firewall --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_firewall/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_firewall.network import FirewallAzureFirewall, FirewallAzureFirewallConfig

    def describe():
        ubx.intent("example own firewall azure firewall")

        ubx.resource(FirewallAzureFirewall, "example", FirewallAzureFirewallConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_firewall_azure_firewall, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="extended_location" type="object">
  ExtendedLocation complex type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the Azure Firewall. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="additional_properties" type="map(string)">
      The additional properties used to further config this azure firewall. Optional.
    </ResponseField>
    <ResponseField name="afc_configuration" type="object">
      AFC configuration for the Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="service_endpoint" type="string">
          The endpoint URL of the AFC control plane associated with this Azure Firewall. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="application_rule_collections" type="list(object)">
      Collection of application rule collections used by Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscale_configuration" type="object">
      Azure Firewall Autoscale Configuration parameters. Optional.
      <Expandable title="properties">
        <ResponseField name="max_capacity" type="number">
          The maximum number of capacity units for this azure firewall. Use null to reset the value to the service default. Optional.
        </ResponseField>
        <ResponseField name="min_capacity" type="number">
          The minimum number of capacity units for this azure firewall. Use null to reset the value to the service default. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="firewall_policy" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The unique identifier of the network firewall azure firewall's firewall policy. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hub_ipaddresses" type="object">
      IP addresses associated with azure firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="private_ipaddress" type="string">
          Private IP Address associated with azure firewall. Optional.
        </ResponseField>
        <ResponseField name="public_ips" type="object">
          Public IP addresses associated with azure firewall. Optional.
          <Expandable title="properties">
            <ResponseField name="addresses" type="list(object)">
              The list of Public IP addresses associated with azure firewall or IP addresses to be retained. Optional.
              <Expandable title="properties">
                <ResponseField name="address" type="string">
                  The network address this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="count" type="number">
              The number of Public IP addresses associated with azure firewall. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_configurations" type="list(object)">
      IP configuration of the Azure Firewall resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_groups" type="list(object)">
      IpGroups associated with AzureFirewall. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="change_number" type="string">
          A value incremented each time this rule collection's own content changes. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="management_ip_configuration" type="object">
      IP configuration of an Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the resource that is unique within a resource group. This name can be used to access the resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of IP configuration of an Azure Firewall. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of the resource. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="nat_rule_collections" type="list(object)">
      Collection of NAT rule collections used by Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_rule_collections" type="list(object)">
      Collection of network rule collections used by Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="sku" type="object">
      SKU of an Azure Firewall. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Name of an Azure Firewall SKU. Optional.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Tier of an Azure Firewall. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="threat_intel_mode" type="string">
      The operation mode for Threat Intel. Optional.
    </ResponseField>
    <ResponseField name="virtual_hub" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The unique identifier of the network firewall azure firewall's virtual hub. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  A list of availability zones denoting where the resource needs to come from. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
