---
title: "azure_network_virtualnetwork_ipam_pool"
description: "`azure_network_virtualnetwork_ipam_pool` reserves a block of IP address space under Azure Virtual Network Manager's own IP Address Management..."
---

`azure_network_virtualnetwork_ipam_pool` reserves a block of IP address space under Azure Virtual Network Manager's own IP Address Management capability, that virtual networks draw non-overlapping address ranges from automatically.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork ipam pool"})

    		ubx.Resource(network.VirtualnetworkIpamPool, "example", network.VirtualnetworkIpamPoolConfig{
    			Properties: map[string]any{"address_prefixes": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkIpamPool } from "@ubx/sdk-azure/azure/network/virtualnetwork_ipam_pool";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork ipam pool" });

      resource(VirtualnetworkIpamPool, "example", {
        properties: { addressPrefixes: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkIpamPool, VirtualnetworkIpamPoolConfig

    def describe():
        ubx.intent("example own virtualnetwork ipam pool")

        ubx.resource(VirtualnetworkIpamPool, "example", VirtualnetworkIpamPoolConfig(
            properties={"address_prefixes": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_ipam_pool, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of IpamPool resource properties which are specific to the Pool resource. Required.
  <Expandable title="properties">
    <ResponseField name="address_prefixes" type="list(string)" required>
      List of IP address prefixes of the resource. Required.
    </ResponseField>
    <ResponseField name="description" type="string">
      A user-supplied, human-readable note explaining this resource's own purpose. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      String representing a friendly name for the resource. Optional.
    </ResponseField>
    <ResponseField name="ip_address_type" type="list(string)">
      List of IP address type for the IpamPool. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="parent_pool_name" type="string">
      String representing parent IpamPool resource name. If empty the IpamPool will be a root pool. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
