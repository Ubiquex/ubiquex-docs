---
title: "azure_network_firstpartyservicetag_first_party_service_tag"
description: "`azure_network_firstpartyservicetag_first_party_service_tag` reports a Microsoft first-party service's own IP address range grouping..."
---

`azure_network_firstpartyservicetag_first_party_service_tag` reports a Microsoft first-party service's own IP address range grouping, usable in network security rules the same way a regular Azure service tag is.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firstpartyservicetag first party service tag"})

    		ubx.Resource(network.FirstpartyservicetagFirstPartyServiceTag, "example", network.FirstpartyservicetagFirstPartyServiceTagConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirstpartyservicetagFirstPartyServiceTag } from "@ubx/sdk-azure/azure/network/firstpartyservicetag_first_party_service_tag";

    export default stack("example", () => {
      intent({
        summary: "example own firstpartyservicetag first party service tag",
      });

      resource(FirstpartyservicetagFirstPartyServiceTag, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.firstpartyservicetag_first_party_service_tag import FirstpartyservicetagFirstPartyServiceTag, FirstpartyservicetagFirstPartyServiceTagConfig

    def describe():
        ubx.intent("example own firstpartyservicetag first party service tag")

        ubx.resource(FirstpartyservicetagFirstPartyServiceTag, "example", FirstpartyservicetagFirstPartyServiceTagConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_network_firstpartyservicetag_first_party_service_tag, with
    .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of the first party service tag. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="failed_reason" type="string">
      The reason for failure, if any. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the first party service tag resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="value" type="string" required>
      The value of the first party service tag. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
