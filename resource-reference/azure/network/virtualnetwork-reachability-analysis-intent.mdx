---
title: "azure_network_virtualnetwork_reachability_analysis_intent"
description: "`azure_network_virtualnetwork_reachability_analysis_intent` defines the source and destination traffic path Azure Virtual Network Manager's own Network..."
---

`azure_network_virtualnetwork_reachability_analysis_intent` defines the source and destination traffic path Azure Virtual Network Manager's own Network Verifier should evaluate for reachability, before any actual analysis run is executed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork reachability analysis intent"})

    		ubx.Resource(network.VirtualnetworkReachabilityAnalysisIntent, "example", network.VirtualnetworkReachabilityAnalysisIntentConfig{
    			Properties: map[string]any{"destination_resource_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkReachabilityAnalysisIntent } from "@ubx/sdk-azure/azure/network/virtualnetwork_reachability_analysis_intent";

    export default stack("example", () => {
      intent({
        summary: "example own virtualnetwork reachability analysis intent",
      });

      resource(VirtualnetworkReachabilityAnalysisIntent, "example", {
        properties: { destinationResourceId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkReachabilityAnalysisIntent, VirtualnetworkReachabilityAnalysisIntentConfig

    def describe():
        ubx.intent("example own virtualnetwork reachability analysis intent")

        ubx.resource(VirtualnetworkReachabilityAnalysisIntent, "example", VirtualnetworkReachabilityAnalysisIntentConfig(
            properties={"destination_resource_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_reachability_analysis_intent,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  Represents the Reachability Analysis Intent properties. Required.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      A user-supplied, human-readable note explaining this resource's own purpose. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="destination_resource_id" type="string" required>
      Destination resource id to verify the reachability path of. Required.
    </ResponseField>
    <ResponseField name="ip_traffic" type="object" required>
      IP traffic information. Required.
      <Expandable title="properties">
        <ResponseField name="destination_ips" type="list(string)" required>
          List of destination IP addresses of the traffic.. Required.
        </ResponseField>
        <ResponseField name="destination_ports" type="list(string)" required>
          The destination ports of the traffic. Required.
        </ResponseField>
        <ResponseField name="protocols" type="list(string)" required>
          The network protocol(s) this applies to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_ips" type="list(string)" required>
          List of source IP addresses of the traffic.. Required.
        </ResponseField>
        <ResponseField name="source_ports" type="list(string)" required>
          The source ports of the traffic. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="source_resource_id" type="string" required>
      Source resource id to verify the reachability path of. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
