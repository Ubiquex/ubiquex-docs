---
title: "azure_network_networkwatcher_flow_log"
description: "`azure_network_networkwatcher_flow_log` records accepted and denied IP traffic flow information for a Network Security Group..."
---

`azure_network_networkwatcher_flow_log` records accepted and denied IP traffic flow information for a Network Security Group, written to a Storage account for traffic analytics and security auditing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_networkwatcher --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_networkwatcher/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_networkwatcher/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkwatcher flow log"})

    		ubx.Resource(network.NetworkwatcherFlowLog, "example", network.NetworkwatcherFlowLogConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_networkwatcher --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkwatcherFlowLog } from "./local-sdk/azure_network_networkwatcher/sdk/typescript/azure_network_networkwatcher/network/networkwatcher_flow_log.ts";

    export default stack("example", () => {
      intent({ summary: "example own networkwatcher flow log" });

      resource(NetworkwatcherFlowLog, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_networkwatcher --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_networkwatcher/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_networkwatcher.network import NetworkwatcherFlowLog, NetworkwatcherFlowLogConfig

    def describe():
        ubx.intent("example own networkwatcher flow log")

        ubx.resource(NetworkwatcherFlowLog, "example", NetworkwatcherFlowLogConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkwatcher_flow_log, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of identity used for the resource. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user assigned identities. The type 'None' will remove any identities from the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Parameters that define the configuration of flow log. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Flag to enable/disable flow logging. Optional.
    </ResponseField>
    <ResponseField name="enabled_filtering_criteria" type="string">
      Optional field to filter network traffic logs based on SrcIP, SrcPort, DstIP, DstPort, Protocol, Encryption, Direction and Action. If not specified, all network traffic will be logged. Optional.
    </ResponseField>
    <ResponseField name="flow_analytics_configuration" type="object">
      Parameters that define the configuration of traffic analytics. Optional.
      <Expandable title="properties">
        <ResponseField name="network_watcher_flow_analytics_configuration" type="object">
          Parameters that define the configuration of traffic analytics. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Flag to enable/disable traffic analytics. Optional.
            </ResponseField>
            <ResponseField name="traffic_analytics_interval" type="number">
              The interval in minutes which would decide how frequently TA service should do flow analytics. Optional.
            </ResponseField>
            <ResponseField name="workspace_id" type="string">
              The resource guid of the attached workspace. Optional.
            </ResponseField>
            <ResponseField name="workspace_region" type="string">
              The location of the attached workspace. Optional.
            </ResponseField>
            <ResponseField name="workspace_resource_id" type="string">
              Resource Id of the attached workspace. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="format" type="object">
      Parameters that define the flow log format. Optional.
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          The file type of flow log. Optional.
        </ResponseField>
        <ResponseField name="version" type="number">
          The version (revision) of the flow log. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="record_types" type="string">
      Optional field to filter network traffic logs based on flow states. Value of this field could be any comma separated combination string of letters B,C,E or D. B represents Begin, when a flow is created. C represents Continue for an ongoing flow generated at every five-minute interval. E represents End, when a flow is terminated. D represents Deny, when a flow is denied. If not specified, all network traffic will be logged. Optional.
    </ResponseField>
    <ResponseField name="retention_policy" type="object">
      Parameters that define the retention policy for flow log. Optional.
      <Expandable title="properties">
        <ResponseField name="days" type="number">
          Number of days to retain flow log records. Optional.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Flag to enable/disable retention. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_id" type="string" required>
      ID of the storage account which is used to store the flow log. Required.
    </ResponseField>
    <ResponseField name="target_resource_guid" type="string">
      Guid of network security group to which flow log will be applied. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="target_resource_id" type="string" required>
      ID of network security group to which flow log will be applied. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
