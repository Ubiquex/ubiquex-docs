---
title: "azure_network_networkmanager_network_manager_connection"
description: "`azure_network_networkmanager_network_manager_connection` delegates management of a subscription or management group's own virtual networks to..."
---

`azure_network_networkmanager_network_manager_connection` delegates management of a subscription or management group's own virtual networks to a Virtual Network Manager instance in a different scope.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkmanager network manager connection"})

    		ubx.Resource(network.NetworkmanagerNetworkManagerConnection, "example", network.NetworkmanagerNetworkManagerConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkmanagerNetworkManagerConnection } from "@ubx/sdk-azure/azure/network/networkmanager_network_manager_connection";

    export default stack("example", () => {
      intent({ summary: "example own networkmanager network manager connection" });

      resource(NetworkmanagerNetworkManagerConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import NetworkmanagerNetworkManagerConnection, NetworkmanagerNetworkManagerConnectionConfig

    def describe():
        ubx.intent("example own networkmanager network manager connection")

        ubx.resource(NetworkmanagerNetworkManagerConnection, "example", NetworkmanagerNetworkManagerConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkmanager_network_manager_connection,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Information about the network manager connection. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="connection_state" type="string">
      The current scope connection state. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the network manager connection. Optional.
    </ResponseField>
    <ResponseField name="network_manager_id" type="string">
      Network Manager Id. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="system_data" type="object">
  Metadata pertaining to creation and last modification of the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      The timestamp of resource creation (UTC). Optional.
    </ResponseField>
    <ResponseField name="created_by" type="string">
      The identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="created_by_type" type="string">
      The type of identity that created the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_at" type="string">
      The timestamp of resource last modification (UTC) Optional.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      The identity that last modified the resource. Optional.
    </ResponseField>
    <ResponseField name="last_modified_by_type" type="string">
      The type of identity that last modified the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
