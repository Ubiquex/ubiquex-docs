---
title: "azure_network_virtualwan_vpn_gateway"
description: "`azure_network_virtualwan_vpn_gateway` provisions the site-to-site VPN gateway within a Virtual WAN hub, that VPN site connection(s) terminate on."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualwan --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualwan/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualwan/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualwan vpn gateway"})

    		ubx.Resource(network.VirtualwanVpnGateway, "example", network.VirtualwanVpnGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualwan --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualwanVpnGateway } from "./local-sdk/azure_network_virtualwan/sdk/typescript/azure_network_virtualwan/network/virtualwan_vpn_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own virtualwan vpn gateway" });

      resource(VirtualwanVpnGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualwan --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualwan/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualwan.network import VirtualwanVpnGateway, VirtualwanVpnGatewayConfig

    def describe():
        ubx.intent("example own virtualwan vpn gateway")

        ubx.resource(VirtualwanVpnGateway, "example", VirtualwanVpnGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualwan_vpn_gateway, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Parameters for VpnGateway. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="bgp_settings" type="object">
      BGP settings details. Optional.
      <Expandable title="properties">
        <ResponseField name="asn" type="number">
          The BGP speaker's ASN. Optional.
        </ResponseField>
        <ResponseField name="bgp_peering_address" type="string">
          The BGP peering address and BGP identifier of this BGP speaker. Optional.
        </ResponseField>
        <ResponseField name="bgp_peering_addresses" type="list(object)">
          BGP peering address with IP configuration ID for virtual network gateway. Optional.
          <Expandable title="properties">
            <ResponseField name="custom_bgp_ip_addresses" type="list(string)">
              Caller-chosen BGP peering IP addresses to use for this connection, instead of Azure's own auto-allocated defaults. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="default_bgp_ip_addresses" type="list(string)">
              Output only. The BGP peering IP address(es) Azure auto-allocated for this connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ipconfiguration_id" type="string">
              A reference to the IP configuration this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tunnel_ip_addresses" type="list(string)">
              Output only. The public IP address(es) of this VPN gateway's own tunnel endpoint(s). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="peer_weight" type="number">
          The weight added to routes learned from this BGP speaker. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connections" type="list(object)">
      List of all vpn connections to the gateway. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_bgp_route_translation_for_nat" type="bool">
      Enable BGP routes translation for NAT on this VpnGateway. Optional.
    </ResponseField>
    <ResponseField name="ip_configurations" type="list(object)">
      List of all IPs configured on the gateway. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="private_ip_address" type="string">
          The private (internal) IP address assigned to this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_ip_address" type="string">
          The public IP address assigned to this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_routing_preference_internet" type="bool">
      Enable Routing Preference property for the Public IP Interface of the VpnGateway. Optional.
    </ResponseField>
    <ResponseField name="nat_rules" type="list(object)">
      List of all the nat Rules associated with the gateway. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="virtual_hub" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The unique identifier of the network virtualwan vpn gateway's virtual hub. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_gateway_scale_unit" type="number">
      The scale unit for this vpn gateway. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
