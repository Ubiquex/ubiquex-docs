---
title: "azure_network_virtualnetwork_virtual_router"
description: "`azure_network_virtualnetwork_virtual_router` provisions Azure Route Server..."
---

`azure_network_virtualnetwork_virtual_router` provisions Azure Route Server, a managed BGP route reflector that lets network virtual appliances exchange dynamic routes directly with a virtual network without static route table configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own virtualnetwork virtual router"})

    		ubx.Resource(network.VirtualnetworkVirtualRouter, "example", network.VirtualnetworkVirtualRouterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkVirtualRouter } from "@ubx/sdk-azure/azure/network/virtualnetwork_virtual_router";

    export default stack("example", () => {
      intent({ summary: "example own virtualnetwork virtual router" });

      resource(VirtualnetworkVirtualRouter, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network import VirtualnetworkVirtualRouter, VirtualnetworkVirtualRouterConfig

    def describe():
        ubx.intent("example own virtualnetwork virtual router")

        ubx.resource(VirtualnetworkVirtualRouter, "example", VirtualnetworkVirtualRouterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_virtual_router, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Virtual Router definition. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="hosted_gateway" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hosted_subnet" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peerings" type="list(object)">
      List of references to VirtualRouterPeerings. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="virtual_router_asn" type="number">
      VirtualRouter ASN. Optional.
    </ResponseField>
    <ResponseField name="virtual_router_ips" type="list(string)">
      VirtualRouter IPs. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
