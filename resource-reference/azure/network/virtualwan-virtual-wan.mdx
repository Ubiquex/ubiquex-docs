---
title: "azure_network_virtualwan_virtual_wan"
description: "`azure_network_virtualwan_virtual_wan` is the top-level Virtual WAN resource that virtual hub(s) across regions are created within..."
---

`azure_network_virtualwan_virtual_wan` is the top-level Virtual WAN resource that virtual hub(s) across regions are created within, providing a unified, Microsoft-managed global network backbone.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualwan --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualwan/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualwan/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualwan virtual wan"})

    		ubx.Resource(network.VirtualwanVirtualWan, "ci-runner", network.VirtualwanVirtualWanConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualwan --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualwanVirtualWan } from "./local-sdk/azure_network_virtualwan/sdk/typescript/azure_network_virtualwan/network/virtualwan_virtual_wan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualwan virtual wan" });

      resource(VirtualwanVirtualWan, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualwan --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualwan/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualwan.network import VirtualwanVirtualWan, VirtualwanVirtualWanConfig

    def describe():
        ubx.intent("payments own virtualwan virtual wan")

        ubx.resource(VirtualwanVirtualWan, "ci-runner", VirtualwanVirtualWanConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualwan_virtual_wan, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Parameters for VirtualWAN. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allow_branch_to_branch_traffic" type="bool">
      True if branch to branch traffic is allowed. Optional.
    </ResponseField>
    <ResponseField name="allow_vnet_to_vnet_traffic" type="bool">
      True if Vnet to Vnet traffic is allowed. Optional.
    </ResponseField>
    <ResponseField name="disable_vpn_encryption" type="bool">
      Vpn encryption to be disabled or not. Optional.
    </ResponseField>
    <ResponseField name="office365_local_breakout_category" type="string">
      The office traffic category. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the VirtualWAN. Optional.
    </ResponseField>
    <ResponseField name="virtual_hubs" type="list(object)">
      List of VirtualHubs in the VirtualWAN. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpn_sites" type="list(object)">
      List of VpnSites in the VirtualWAN. Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
