---
title: "azure_network_networkgateway_local_network_gateway"
description: "`azure_network_networkgateway_local_network_gateway` represents an on-premises VPN endpoint's own public IP address and address space..."
---

`azure_network_networkgateway_local_network_gateway` represents an on-premises VPN endpoint's own public IP address and address space, referenced by a site-to-site VPN connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own networkgateway local network gateway"})

    		ubx.Resource(network.NetworkgatewayLocalNetworkGateway, "example", network.NetworkgatewayLocalNetworkGatewayConfig{
    			Properties: map[string]any{"bgp_settings": map[string]any{"asn": 1}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkgatewayLocalNetworkGateway } from "@ubx/sdk-azure/azure/network/networkgateway_local_network_gateway";

    export default stack("example", () => {
      intent({ summary: "example own networkgateway local network gateway" });

      resource(NetworkgatewayLocalNetworkGateway, "example", {
        properties: { bgpSettings: { asn: 1 } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.network.networkgateway_local_network_gateway import NetworkgatewayLocalNetworkGateway, NetworkgatewayLocalNetworkGatewayConfig

    def describe():
        ubx.intent("example own networkgateway local network gateway")

        ubx.resource(NetworkgatewayLocalNetworkGateway, "example", NetworkgatewayLocalNetworkGatewayConfig(
            properties={"bgp_settings": {"asn": 1}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_networkgateway_local_network_gateway, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  LocalNetworkGateway properties. Required.
  <Expandable title="properties">
    <ResponseField name="bgp_settings" type="object">
      BGP settings details. Optional.
      <Expandable title="properties">
        <ResponseField name="asn" type="number">
          The BGP speaker's ASN. Optional.
        </ResponseField>
        <ResponseField name="bgp_peering_address" type="string">
          The BGP peering address and BGP identifier of this BGP speaker. Optional.
        </ResponseField>
        <ResponseField name="bgp_peering_addresses" type="list(object)">
          BGP peering address with IP configuration ID for virtual network gateway. Optional.
          <Expandable title="properties">
            <ResponseField name="custom_bgp_ip_addresses" type="list(string)">
              Caller-chosen BGP peering IP addresses to use for this connection, instead of Azure's own auto-allocated defaults. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="default_bgp_ip_addresses" type="list(string)">
              Output only. The BGP peering IP address(es) Azure auto-allocated for this connection. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ipconfiguration_id" type="string">
              A reference to the IP configuration this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tunnel_ip_addresses" type="list(string)">
              Output only. The public IP address(es) of this VPN gateway's own tunnel endpoint(s). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="peer_weight" type="number">
          The weight added to routes learned from this BGP speaker. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fqdn" type="string">
      FQDN of local network gateway. Optional.
    </ResponseField>
    <ResponseField name="gateway_ip_address" type="string">
      IP address of local network gateway. Optional.
    </ResponseField>
    <ResponseField name="local_network_address_space" type="object">
      AddressSpace contains an array of IP address ranges that can be used by subnets of the virtual network. Optional.
      <Expandable title="properties">
        <ResponseField name="address_prefixes" type="list(string)">
          A list of address blocks reserved for this virtual network in CIDR notation. Optional.
        </ResponseField>
        <ResponseField name="ipam_pool_prefix_allocations" type="list(object)">
          A list of IPAM Pools allocating IP address prefixes. Optional.
          <Expandable title="properties">
            <ResponseField name="allocated_address_prefixes" type="list(string)">
              Output only. The specific address ranges actually allocated from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="number_of_ip_addresses" type="string">
              How many IP addresses to allocate from this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pool" type="object">
              A reference to the address or backend pool this resource draws from or belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the local network gateway resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
