---
title: "azure_network_virtualnetwork_public_ipaddress"
description: "`azure_network_virtualnetwork_public_ipaddress` is a static or dynamic public IPv4 or IPv6 address that a load balancer, gateway..."
---

`azure_network_virtualnetwork_public_ipaddress` is a static or dynamic public IPv4 or IPv6 address that a load balancer, gateway, or VM can be assigned for internet-reachable connectivity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_virtualnetwork --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_virtualnetwork/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_virtualnetwork/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtualnetwork public ipaddress"})

    		ubx.Resource(network.VirtualnetworkPublicIpaddress, "ci-runner", network.VirtualnetworkPublicIpaddressConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_virtualnetwork --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualnetworkPublicIpaddress } from "./local-sdk/azure_network_virtualnetwork/sdk/typescript/azure_network_virtualnetwork/network/virtualnetwork_public_ipaddress.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own virtualnetwork public ipaddress" });

      resource(VirtualnetworkPublicIpaddress, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_virtualnetwork --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_virtualnetwork/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_virtualnetwork.network import VirtualnetworkPublicIpaddress, VirtualnetworkPublicIpaddressConfig

    def describe():
        ubx.intent("payments own virtualnetwork public ipaddress")

        ubx.resource(VirtualnetworkPublicIpaddress, "ci-runner", VirtualnetworkPublicIpaddressConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_virtualnetwork_public_ipaddress, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="extended_location" type="object">
  ExtendedLocation complex type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The supported ExtendedLocation types. Currently only EdgeZone is supported in Microsoft.Network resources. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Public IP address properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="ddos_settings" type="object">
      Contains the DDoS protection settings of the public IP. Optional.
      <Expandable title="properties">
        <ResponseField name="ddos_custom_policy" type="object">
          Reference to another subresource. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource ID. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ddos_protection_plan" type="object">
          Reference to another subresource. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource ID. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="protection_mode" type="string">
          The DDoS protection mode of the public IP Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="delete_option" type="string">
      Specify what happens to the public IP address when the VM using it is deleted Optional.
    </ResponseField>
    <ResponseField name="dns_settings" type="object">
      Contains FQDN of the DNS record associated with the public IP address. Optional.
      <Expandable title="properties">
        <ResponseField name="domain_name_label" type="string">
          The domain name label. The concatenation of the domain name label and the regionalized DNS zone make up the fully qualified domain name associated with the public IP address. If a domain name label is specified, an A DNS record is created for the public IP in the Microsoft Azure DNS system. Optional.
        </ResponseField>
        <ResponseField name="domain_name_label_scope" type="string">
          The domain name label scope. If a domain name label and a domain name label scope are specified, an A DNS record is created for the public IP in the Microsoft Azure DNS system with a hashed value includes in FQDN. Optional.
        </ResponseField>
        <ResponseField name="fqdn" type="string">
          The Fully Qualified Domain Name of the A DNS record associated with the public IP. This is the concatenation of the domainNameLabel and the regionalized DNS zone. Optional.
        </ResponseField>
        <ResponseField name="reverse_fqdn" type="string">
          The reverse FQDN. A user-visible, fully qualified domain name that resolves to this public IP address. If the reverseFqdn is specified, then a PTR DNS record is created pointing from the IP address in the in-addr.arpa domain to the reverse FQDN. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="idle_timeout_in_minutes" type="number">
      The idle timeout of the public IP address. Optional.
    </ResponseField>
    <ResponseField name="ip_address" type="string">
      The IP address associated with the public IP address resource. Optional.
    </ResponseField>
    <ResponseField name="ip_configuration" type="object">
      IP configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the resource that is unique within a resource group. This name can be used to access the resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Properties of IP configuration. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_tags" type="list(object)">
      The list of tags associated with the public IP address. Optional.
      <Expandable title="properties">
        <ResponseField name="first_party_service_tag_id" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="ip_tag_type" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
        <ResponseField name="tag" type="string">
          Optional; if omitted, computed by Microsoft Azure.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="linked_public_ipaddress" type="any">
      Public IP address resource. Optional.
    </ResponseField>
    <ResponseField name="migration_phase" type="string">
      Migration phase of Public IP Address. Optional.
    </ResponseField>
    <ResponseField name="nat_gateway" type="object">
      Nat Gateway resource. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A unique read-only string that changes whenever the resource is updated. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Nat Gateway properties. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="sku" type="object">
          SKU of nat gateway. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Name of Nat Gateway SKU. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="zones" type="list(string)">
          A list of availability zones denoting the zone in which Nat Gateway should be deployed. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="public_ipaddress_version" type="string">
      IP address version. Optional.
    </ResponseField>
    <ResponseField name="public_ipallocation_method" type="string">
      IP address allocation method. Optional.
    </ResponseField>
    <ResponseField name="public_ipprefix" type="object">
      Reference to another subresource. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_guid" type="string">
      The resource GUID property of the public IP address resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_public_ipaddress" type="any">
      Public IP address resource. Optional.
    </ResponseField>
    <ResponseField name="upgraded_to_v2" type="bool">
      Whether the public IP address SKU has been upgraded from Standard to StandardV2. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  SKU of a public IP address. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Name of a public IP address SKU. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Tier of a public IP address SKU. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  A list of availability zones denoting the IP allocated for the resource needs to come from. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
