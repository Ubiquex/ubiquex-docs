---
title: "azure_network_expressroute_express_route_cross_connection"
description: "`azure_network_expressroute_express_route_cross_connection` is the connectivity provider-managed representation of a customer's own ExpressRoute..."
---

`azure_network_expressroute_express_route_cross_connection` is the connectivity provider-managed representation of a customer's own ExpressRoute circuit, used by providers to configure circuit-level settings on the customer's behalf.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_network_expressroute --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_network_expressroute/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_network_expressroute/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own expressroute express route cross connection"})

    		ubx.Resource(network.ExpressrouteExpressRouteCrossConnection, "ci-runner", network.ExpressrouteExpressRouteCrossConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_network_expressroute --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressrouteExpressRouteCrossConnection } from "./local-sdk/azure_network_expressroute/sdk/typescript/azure_network_expressroute/network/expressroute_express_route_cross_connection.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own expressroute express route cross connection",
      });

      resource(ExpressrouteExpressRouteCrossConnection, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_network_expressroute --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_network_expressroute/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_network_expressroute.network import ExpressrouteExpressRouteCrossConnection, ExpressrouteExpressRouteCrossConnectionConfig

    def describe():
        ubx.intent("payments own expressroute express route cross connection")

        ubx.resource(ExpressrouteExpressRouteCrossConnection, "ci-runner", ExpressrouteExpressRouteCrossConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_network_expressroute_express_route_cross_connection,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A unique read-only string that changes whenever the resource is updated. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Properties of ExpressRouteCrossConnection. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="bandwidth_in_mbps" type="number">
      The circuit bandwidth In Mbps. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="express_route_circuit" type="object">
      Reference to an express route circuit. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Corresponding Express Route Circuit Id. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="peering_location" type="string">
      The peering location of the ExpressRoute circuit. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="peerings" type="list(object)">
      The list of peerings. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          A read-only, server-generated value used for optimistic concurrency control -- an update whose own `etag` doesn't match the resource's current value is rejected rather than silently overwriting a concurrent change. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="properties" type="object">
          The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="primary_azure_port" type="string">
      The name of the primary port. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning states of a resource. Optional.
    </ResponseField>
    <ResponseField name="s_tag" type="number">
      The identifier of the circuit traffic. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="secondary_azure_port" type="string">
      The name of the secondary port. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_provider_notes" type="string">
      Additional read only notes set by the connectivity provider. Optional.
    </ResponseField>
    <ResponseField name="service_provider_provisioning_state" type="string">
      The ServiceProviderProvisioningState state of the resource. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
