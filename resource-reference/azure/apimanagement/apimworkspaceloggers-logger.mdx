---
title: "azure_apimanagement_apimworkspaceloggers_logger"
description: "`azure_apimanagement_apimworkspaceloggers_logger` registers a logging destination scoped to a single Azure API Management workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimworkspaceloggers logger"})

    		ubx.Resource(apimanagement.ApimworkspaceloggersLogger, "example", apimanagement.ApimworkspaceloggersLoggerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimworkspaceloggersLogger } from "@ubx/sdk-azure/azure/apimanagement/apimworkspaceloggers_logger";

    export default stack("example", () => {
      intent({ summary: "example own apimworkspaceloggers logger" });

      resource(ApimworkspaceloggersLogger, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement.apimworkspaceloggers_logger import ApimworkspaceloggersLogger, ApimworkspaceloggersLoggerConfig

    def describe():
        ubx.intent("example own apimworkspaceloggers logger")

        ubx.resource(ApimworkspaceloggersLogger, "example", ApimworkspaceloggersLoggerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimworkspaceloggers_logger, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The Logger entity in API Management represents an event sink that you can use to log API Management events. Currently the Logger entity supports logging API Management events to Azure Event Hubs. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="credentials" type="map(string)">
      The name and SendRule connection string of the event hub for azureEventHub logger. Instrumentation key for applicationInsights logger. Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      Logger description. Optional.
    </ResponseField>
    <ResponseField name="is_buffered" type="bool">
      Whether records are buffered in the logger before publishing. Default is assumed to be true. Optional.
    </ResponseField>
    <ResponseField name="logger_type" type="string" required>
      Logger type. Required.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      Azure Resource Id of a log target (either Azure Event Hub resource or Azure Application Insights resource). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
