---
title: "azure_apimanagement_apimnamedvalues_named_value_contract_2"
description: "`azure_apimanagement_apimnamedvalues_named_value_contract_2` looks up a real named value -- a reusable..."
---

`azure_apimanagement_apimnamedvalues_named_value_contract_2` looks up a real named value -- a reusable, policy-referenceable setting or secret -- in API Management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimnamedvalues named value contract 2"})

    		ubx.Resource(apimanagement.ApimnamedvaluesNamedValueContract2, "example", apimanagement.ApimnamedvaluesNamedValueContract2Config{
    			Properties: map[string]any{"display_name": "example-display-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimnamedvaluesNamedValueContract2 } from "@ubx/sdk-azure/azure/apimanagement/apimnamedvalues_named_value_contract_2";

    export default stack("example", () => {
      intent({ summary: "example own apimnamedvalues named value contract 2" });

      resource(ApimnamedvaluesNamedValueContract2, "example", {
        properties: { displayName: "example-display-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimnamedvaluesNamedValueContract2, ApimnamedvaluesNamedValueContract2Config
    from ubx.azure.apimanagement.apimnamedvalues_named_value_contract_2 import ApimnamedvaluesNamedValueContract2_Properties

    def describe():
        ubx.intent("example own apimnamedvalues named value contract 2")

        ubx.resource(ApimnamedvaluesNamedValueContract2, "example", ApimnamedvaluesNamedValueContract2Config(
            properties=ApimnamedvaluesNamedValueContract2_Properties(display_name="example-display-name"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_apimanagement_apimnamedvalues_named_value_contract_2, with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  NamedValue Contract properties. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Unique name of NamedValue. It may contain only letters, digits, period, dash, and underscore characters. Required.
    </ResponseField>
    <ResponseField name="key_vault" type="object">
      Create keyVault contract details. Optional.
      <Expandable title="properties">
        <ResponseField name="identity_client_id" type="string">
          Null for SystemAssignedIdentity or Client Id for UserAssignedIdentity , which will be used to access key vault secret. Optional.
        </ResponseField>
        <ResponseField name="secret_identifier" type="string">
          Key vault secret identifier for fetching secret. Providing a versioned secret will prevent auto-refresh. This requires API Management service to be configured with aka.ms/apimmsi Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="value" type="string">
      Value of the NamedValue. Can contain policy expressions. It may not be empty or consist only of whitespace. This property will not be filled on 'GET' operations! Use '/listSecrets' POST request to get the value. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
