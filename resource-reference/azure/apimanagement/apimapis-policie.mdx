---
title: "azure_apimanagement_apimapis_policie"
description: "`azure_apimanagement_apimapis_policie` (a Policy) defines the request/response processing logic (rate limiting, transformation..."
---

`azure_apimanagement_apimapis_policie` (a Policy) defines the request/response processing logic (rate limiting, transformation, authentication) applied to an API, operation, product, or the whole gateway in Azure API Management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_apimanagement_apimapis --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_apimanagement_apimapis/sdk/go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_apimanagement_apimapis/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own apimapis policie"})

    		ubx.Resource(apimanagement.ApimapisPolicie, "ci-runner", apimanagement.ApimapisPolicieConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_apimanagement_apimapis --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimapisPolicie } from "./local-sdk/azure_apimanagement_apimapis/sdk/typescript/azure_apimanagement_apimapis/apimanagement/apimapis_policie.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own apimapis policie" });

      resource(ApimapisPolicie, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_apimanagement_apimapis --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_apimanagement_apimapis/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_apimanagement_apimapis.apimanagement import ApimapisPolicie, ApimapisPolicieConfig

    def describe():
        ubx.intent("payments own apimapis policie")

        ubx.resource(ApimapisPolicie, "ci-runner", ApimapisPolicieConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimapis_policie, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  API Create or Update Properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="api_type" type="string">
      Type of API to create. * `http` creates a REST API * `soap` creates a SOAP pass-through API * `websocket` creates websocket API * `graphql` creates GraphQL API. New types can be added in the future. Optional.
    </ResponseField>
    <ResponseField name="format" type="string">
      Format of the Content in which the API is getting imported. New formats can be added in the future Optional.
    </ResponseField>
    <ResponseField name="translate_required_query_parameters" type="string">
      Strategy of translating required query parameters to template ones. By default has value 'template'. Possible values: 'template', 'query' Optional.
    </ResponseField>
    <ResponseField name="value" type="string">
      Content value when Importing an API. Optional.
    </ResponseField>
    <ResponseField name="wsdl_selector" type="object">
      Criteria to limit import of WSDL to a subset of the document. Optional.
      <Expandable title="properties">
        <ResponseField name="wsdl_endpoint_name" type="string">
          Name of endpoint(port) to import from WSDL Optional.
        </ResponseField>
        <ResponseField name="wsdl_service_name" type="string">
          Name of service to import from WSDL Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
