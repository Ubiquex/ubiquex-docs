---
title: "azure_apimanagement_apimbackends_backend"
description: "`azure_apimanagement_apimbackends_backend` defines a reusable backend service endpoint (URL, credentials..."
---

`azure_apimanagement_apimbackends_backend` defines a reusable backend service endpoint (URL, credentials, circuit breaker settings) that Azure API Management API operation(s) forward requests to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimbackends backend"})

    		ubx.Resource(apimanagement.ApimbackendsBackend, "example", apimanagement.ApimbackendsBackendConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimbackendsBackend } from "@ubx/sdk-azure/azure/apimanagement/apimbackends_backend";

    export default stack("example", () => {
      intent({ summary: "example own apimbackends backend" });

      resource(ApimbackendsBackend, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimbackendsBackend, ApimbackendsBackendConfig

    def describe():
        ubx.intent("example own apimbackends backend")

        ubx.resource(ApimbackendsBackend, "example", ApimbackendsBackendConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimbackends_backend, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Parameters supplied to the Create Backend operation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="protocol" type="string">
      Backend communication protocol. Required when backend type is 'Single'. Optional.
    </ResponseField>
    <ResponseField name="url" type="string">
      Runtime Url of the Backend. Required when backend type is 'Single'. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
