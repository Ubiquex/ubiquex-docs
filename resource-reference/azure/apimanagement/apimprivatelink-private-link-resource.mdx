---
title: "azure_apimanagement_apimprivatelink_private_link_resource"
description: "`azure_apimanagement_apimprivatelink_private_link_resource` describes the sub-resource(s) of an Azure API Management instance that a Private Endpoint..."
---

`azure_apimanagement_apimprivatelink_private_link_resource` describes the sub-resource(s) of an Azure API Management instance that a Private Endpoint can target.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimprivatelink private link resource"})

    		ubx.Resource(apimanagement.ApimprivatelinkPrivateLinkResource, "example", apimanagement.ApimprivatelinkPrivateLinkResourceConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimprivatelinkPrivateLinkResource } from "@ubx/sdk-azure/azure/apimanagement/apimprivatelink_private_link_resource";

    export default stack("example", () => {
      intent({ summary: "example own apimprivatelink private link resource" });

      resource(ApimprivatelinkPrivateLinkResource, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimprivatelinkPrivateLinkResource, ApimprivatelinkPrivateLinkResourceConfig

    def describe():
        ubx.intent("example own apimprivatelink private link resource")

        ubx.resource(ApimprivatelinkPrivateLinkResource, "example", ApimprivatelinkPrivateLinkResourceConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimprivatelink_private_link_resource,
    with id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="id" type="string">
  Private Endpoint Connection Resource Id. Optional.
</ResponseField>

<ResponseField name="properties" type="object">
  The connection state of the private endpoint connection. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="private_link_service_connection_state" type="object">
      A collection of information about the state of the connection between service consumer and provider. Optional.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A message indicating if changes on the service provider require any updates on the consumer. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The reason for approval/rejection of the connection. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          The private endpoint connection status. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
