---
title: "azure_apimanagement_apimworkspaces_operation"
description: "`azure_apimanagement_apimworkspaces_operation` is a single documented HTTP operation (e.g. `GET /users`) belonging to an API within an API Management..."
---

`azure_apimanagement_apimworkspaces_operation` is a single documented HTTP operation (e.g. `GET /users`) belonging to an API within an API Management workspace.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimworkspaces operation"})

    		ubx.Resource(apimanagement.ApimworkspacesOperation, "example", apimanagement.ApimworkspacesOperationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimworkspacesOperation } from "@ubx/sdk-azure/azure/apimanagement/apimworkspaces_operation";

    export default stack("example", () => {
      intent({ summary: "example own apimworkspaces operation" });

      resource(ApimworkspacesOperation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimworkspacesOperation, ApimworkspacesOperationConfig

    def describe():
        ubx.intent("example own apimworkspaces operation")

        ubx.resource(ApimworkspacesOperation, "example", ApimworkspacesOperationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimworkspaces_operation, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Workspace entity properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Description of the workspace. Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string" required>
      Name of the workspace. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
