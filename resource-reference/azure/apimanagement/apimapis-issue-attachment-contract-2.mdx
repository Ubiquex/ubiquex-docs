---
title: "azure_apimanagement_apimapis_issue_attachment_contract_2"
description: "`azure_apimanagement_apimapis_issue_attachment_contract_2` attaches a supporting file (e.g. documentation) to an API in Azure API Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimapis issue attachment contract 2"})

    		ubx.Resource(apimanagement.ApimapisIssueAttachmentContract2, "example", apimanagement.ApimapisIssueAttachmentContract2Config{
    			Properties: map[string]any{"content": "example", "content_format": "example", "title": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimapisIssueAttachmentContract2 } from "@ubx/sdk-azure/azure/apimanagement/apimapis_issue_attachment_contract_2";

    export default stack("example", () => {
      intent({ summary: "example own apimapis issue attachment contract 2" });

      resource(ApimapisIssueAttachmentContract2, "example", {
        properties: {
          content: "example",
          contentFormat: "example",
          title: "example",
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimapisIssueAttachmentContract2, ApimapisIssueAttachmentContract2Config
    from ubx.azure.apimanagement.apimapis_issue_attachment_contract_2 import ApimapisIssueAttachmentContract2_Properties

    def describe():
        ubx.intent("example own apimapis issue attachment contract 2")

        ubx.resource(ApimapisIssueAttachmentContract2, "example", ApimapisIssueAttachmentContract2Config(
            properties=ApimapisIssueAttachmentContract2_Properties(content="example", content_format="example", title="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimapis_issue_attachment_contract_2,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Issue Attachment contract Properties. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="content" type="string" required>
      An HTTP link or Base64-encoded binary data. Required.
    </ResponseField>
    <ResponseField name="content_format" type="string" required>
      Either 'link' if content is provided via an HTTP link or the MIME type of the Base64-encoded binary data provided in the 'content' property. Required.
    </ResponseField>
    <ResponseField name="title" type="string" required>
      Filename by which the binary data will be saved. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
