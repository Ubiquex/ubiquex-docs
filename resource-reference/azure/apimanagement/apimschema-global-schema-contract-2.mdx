---
title: "azure_apimanagement_apimschema_global_schema_contract_2"
description: "`azure_apimanagement_apimschema_global_schema_contract_2` looks up a real schema (such as an XSD or JSON schema) registered with an API in API Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimschema global schema contract 2"})

    		ubx.Resource(apimanagement.ApimschemaGlobalSchemaContract2, "example", apimanagement.ApimschemaGlobalSchemaContract2Config{
    			Properties: map[string]any{"schema_type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimschemaGlobalSchemaContract2 } from "@ubx/sdk-azure/azure/apimanagement/apimschema_global_schema_contract_2";

    export default stack("example", () => {
      intent({ summary: "example own apimschema global schema contract 2" });

      resource(ApimschemaGlobalSchemaContract2, "example", {
        properties: { schemaType: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimschemaGlobalSchemaContract2, ApimschemaGlobalSchemaContract2Config
    from ubx.azure.apimanagement.apimschema_global_schema_contract_2 import ApimschemaGlobalSchemaContract2_Properties

    def describe():
        ubx.intent("example own apimschema global schema contract 2")

        ubx.resource(ApimschemaGlobalSchemaContract2, "example", ApimschemaGlobalSchemaContract2Config(
            properties=ApimschemaGlobalSchemaContract2_Properties(schema_type="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimschema_global_schema_contract_2,
    with properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Schema create or update contract Properties. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Free-form schema entity description. Optional.
    </ResponseField>
    <ResponseField name="document" type="any">
      Global Schema Document Properties. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="schema_type" type="string" required>
      Schema Type. Immutable. Required.
    </ResponseField>
    <ResponseField name="value" type="any">
      Json-encoded string for non json-based schema. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
