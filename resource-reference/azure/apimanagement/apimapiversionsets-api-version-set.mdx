---
title: "azure_apimanagement_apimapiversionsets_api_version_set"
description: "`azure_apimanagement_apimapiversionsets_api_version_set` groups related versioned API(s) together in Azure API Management..."
---

`azure_apimanagement_apimapiversionsets_api_version_set` groups related versioned API(s) together in Azure API Management, so callers can navigate between version(s) of the same logical API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimapiversionsets api version set"})

    		ubx.Resource(apimanagement.ApimapiversionsetsApiVersionSet, "example", apimanagement.ApimapiversionsetsApiVersionSetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimapiversionsetsApiVersionSet } from "@ubx/sdk-azure/azure/apimanagement/apimapiversionsets_api_version_set";

    export default stack("example", () => {
      intent({ summary: "example own apimapiversionsets api version set" });

      resource(ApimapiversionsetsApiVersionSet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimapiversionsetsApiVersionSet, ApimapiversionsetsApiVersionSetConfig

    def describe():
        ubx.intent("example own apimapiversionsets api version set")

        ubx.resource(ApimapiversionsetsApiVersionSet, "example", ApimapiversionsetsApiVersionSetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimapiversionsets_api_version_set,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an API Version Set. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Name of API Version Set Required.
    </ResponseField>
    <ResponseField name="versioning_scheme" type="string" required>
      An value that determines where the API Version identifier will be located in a HTTP request. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
