---
title: "azure_apimanagement_apimapis_diagnostic"
description: "`azure_apimanagement_apimapis_diagnostic` configures request/response logging (to Application Insights, Event Hubs..."
---

`azure_apimanagement_apimapis_diagnostic` configures request/response logging (to Application Insights, Event Hubs, or Azure Monitor) for an API in Azure API Management.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimapis diagnostic"})

    		ubx.Resource(apimanagement.ApimapisDiagnostic, "example", apimanagement.ApimapisDiagnosticConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimapisDiagnostic } from "@ubx/sdk-azure/azure/apimanagement/apimapis_diagnostic";

    export default stack("example", () => {
      intent({ summary: "example own apimapis diagnostic" });

      resource(ApimapisDiagnostic, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement import ApimapisDiagnostic, ApimapisDiagnosticConfig

    def describe():
        ubx.intent("example own apimapis diagnostic")

        ubx.resource(ApimapisDiagnostic, "example", ApimapisDiagnosticConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimapis_diagnostic, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  API Create or Update Properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="api_type" type="string">
      Type of API to create. * `http` creates a REST API * `soap` creates a SOAP pass-through API * `websocket` creates websocket API * `graphql` creates GraphQL API. New types can be added in the future. Optional.
    </ResponseField>
    <ResponseField name="format" type="string">
      Format of the Content in which the API is getting imported. New formats can be added in the future Optional.
    </ResponseField>
    <ResponseField name="translate_required_query_parameters" type="string">
      Strategy of translating required query parameters to template ones. By default has value 'template'. Possible values: 'template', 'query' Optional.
    </ResponseField>
    <ResponseField name="value" type="string">
      Content value when Importing an API. Optional.
    </ResponseField>
    <ResponseField name="wsdl_selector" type="object">
      Criteria to limit import of WSDL to a subset of the document. Optional.
      <Expandable title="properties">
        <ResponseField name="wsdl_endpoint_name" type="string">
          Name of endpoint(port) to import from WSDL Optional.
        </ResponseField>
        <ResponseField name="wsdl_service_name" type="string">
          Name of service to import from WSDL Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
