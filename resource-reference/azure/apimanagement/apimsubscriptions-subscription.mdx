---
title: "azure_apimanagement_apimsubscriptions_subscription"
description: "`azure_apimanagement_apimsubscriptions_subscription` grants a developer or app access to product(s) in Azure API Management..."
---

`azure_apimanagement_apimsubscriptions_subscription` grants a developer or app access to product(s) in Azure API Management, carrying the subscription key(s) used to authenticate.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apimanagement "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/apimanagement"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own apimsubscriptions subscription"})

    		ubx.Resource(apimanagement.ApimsubscriptionsSubscription, "example", apimanagement.ApimsubscriptionsSubscriptionConfig{
    			Sid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApimsubscriptionsSubscription } from "@ubx/sdk-azure/azure/apimanagement/apimsubscriptions_subscription";

    export default stack("example", () => {
      intent({ summary: "example own apimsubscriptions subscription" });

      resource(ApimsubscriptionsSubscription, "example", {
        sid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.apimanagement.apimsubscriptions_subscription import ApimsubscriptionsSubscription, ApimsubscriptionsSubscriptionConfig

    def describe():
        ubx.intent("example own apimsubscriptions subscription")

        ubx.resource(ApimsubscriptionsSubscription, "example", ApimsubscriptionsSubscriptionConfig(
            sid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_apimanagement_apimsubscriptions_subscription, with
    sid example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Parameters supplied to the Create subscription operation. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allow_tracing" type="bool">
      Determines whether tracing can be enabled Optional.
    </ResponseField>
    <ResponseField name="display_name" type="string" required>
      Subscription name. Required.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      User (user id path) for whom subscription is being created in form /users/&#123;userId&#125; Optional.
    </ResponseField>
    <ResponseField name="primary_key" type="string">
      Primary subscription key. If not specified during request key will be generated automatically. Optional.
    </ResponseField>
    <ResponseField name="scope" type="string" required>
      Scope like /products/&#123;productId&#125; or /apis or /apis/&#123;apiId&#125;. Required.
    </ResponseField>
    <ResponseField name="secondary_key" type="string">
      Secondary subscription key. If not specified during request key will be generated automatically. Optional.
    </ResponseField>
    <ResponseField name="state" type="string">
      Initial subscription state. If no value is specified, subscription is created with Submitted state. Possible states are * active  --  the subscription is active, * suspended  --  the subscription is blocked, and the subscriber cannot call any APIs of the product, * submitted  --  the subscription request has been made by the developer, but has not yet been approved or rejected, * rejected  --  the subscription request has been denied by an administrator, * cancelled  --  the subscription has been cancelled by the developer or administrator, * expired  --  the subscription reached its expiration date and was deactivated. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sid" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
