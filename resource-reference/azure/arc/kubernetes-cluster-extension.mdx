---
title: "azurerm_arc_kubernetes_cluster_extension"
description: "Real, generated bindings for `azurerm_arc_kubernetes_cluster_extension`."
---

`azurerm_arc_kubernetes_cluster_extension` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arc "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/arc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own kubernetes cluster extension"})

    		ubx.Resource(arc.KubernetesClusterExtension, "ci-runner", arc.KubernetesClusterExtensionConfig{
    			ClusterId:                      "example",
    			ExtensionType:                  "example",
    			Name:                           "ci-runner",
    			ConfigurationProtectedSettings: map[string]string{"managed-by": "ubx"},
    			ConfigurationSettings:          map[string]string{"managed-by": "ubx"},
    			Version:                        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KubernetesClusterExtension } from "jsr:@ubx/sdk-azure/azurerm/arc/kubernetes_cluster_extension";

    export default stack("payments", () => {
      intent({ summary: "payments own kubernetes cluster extension" });

      resource(KubernetesClusterExtension, "ci-runner", {
        clusterId: "example",
        extensionType: "example",
        name: "ci-runner",
        configurationProtectedSettings: { "managed-by": "ubx" },
        configurationSettings: { "managed-by": "ubx" },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.arc import KubernetesClusterExtension, KubernetesClusterExtensionConfig

    def describe():
        ubx.intent("payments own kubernetes cluster extension")

        ubx.resource(KubernetesClusterExtension, "ci-runner", KubernetesClusterExtensionConfig(
            cluster_id="example",
            extension_type="example",
            name="ci-runner",
            configuration_protected_settings={"managed-by": "ubx"},
            configuration_settings={"managed-by": "ubx"},
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_arc_kubernetes_cluster_extension called
    "ci-runner", with cluster id example, extension type example,
    configuration protected settings a set value, configuration
    settings a set value, version example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configuration_protected_settings" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="configuration_settings" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="extension_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="identity" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="release_namespace" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="release_train" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="target_namespace" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="current_version" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="identity" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="release_namespace" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="release_train" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="target_namespace" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
