---
title: "azure_web_openapi_host_name_binding"
description: "`azure_web_openapi_host_name_binding` maps a custom domain hostname to an App Service web app, routing that hostname's own traffic to it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi host name binding"})

    		ubx.Resource(web.OpenapiHostNameBinding, "example", web.OpenapiHostNameBindingConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiHostNameBinding } from "@ubx/sdk-azure/azure/web/openapi_host_name_binding";

    export default stack("example", () => {
      intent({ summary: "example own openapi host name binding" });

      resource(OpenapiHostNameBinding, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.web.openapi_host_name_binding import OpenapiHostNameBinding, OpenapiHostNameBindingConfig

    def describe():
        ubx.intent("example own openapi host name binding")

        ubx.resource(OpenapiHostNameBinding, "example", OpenapiHostNameBindingConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_host_name_binding called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  HostNameBinding resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="azure_resource_name" type="string">
      Azure resource name. Optional.
    </ResponseField>
    <ResponseField name="azure_resource_type" type="string">
      Azure resource type. Optional.
    </ResponseField>
    <ResponseField name="custom_host_name_dns_record_type" type="string">
      Custom DNS record type. Optional.
    </ResponseField>
    <ResponseField name="domain_id" type="string">
      Fully qualified ARM domain resource URI. Optional.
    </ResponseField>
    <ResponseField name="host_name_type" type="string">
      Hostname type. Optional.
    </ResponseField>
    <ResponseField name="site_name" type="string">
      App Service app name. Optional.
    </ResponseField>
    <ResponseField name="ssl_state" type="string">
      SSL type Optional.
    </ResponseField>
    <ResponseField name="thumbprint" type="string">
      SSL certificate thumbprint Optional.
    </ResponseField>
    <ResponseField name="virtual_ip" type="string">
      Virtual IP address assigned to the hostname if IP based SSL is enabled. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
