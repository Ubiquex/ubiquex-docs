---
title: "azure_web_openapi_vnet_gateway"
description: "`azure_web_openapi_vnet_gateway` represents the App Service-side gateway endpoint of a virtual network integration connection."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi vnet gateway"})

    		ubx.Resource(web.OpenapiVnetGateway, "example", web.OpenapiVnetGatewayConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiVnetGateway } from "@ubx/sdk-azure/azure/web/openapi_vnet_gateway";

    export default stack("example", () => {
      intent({ summary: "example own openapi vnet gateway" });

      resource(OpenapiVnetGateway, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.web import OpenapiVnetGateway, OpenapiVnetGatewayConfig

    def describe():
        ubx.intent("example own openapi vnet gateway")

        ubx.resource(OpenapiVnetGateway, "example", OpenapiVnetGatewayConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_vnet_gateway called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  VnetGateway resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="vnet_name" type="string">
      The Virtual Network name. Optional.
    </ResponseField>
    <ResponseField name="vpn_package_uri" type="string" required>
      The URI where the VPN package can be downloaded. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
