---
title: "azure_web_openapi_ai_gateway"
description: "`azure_web_openapi_ai_gateway` configures the AI Gateway tier of Azure API Management (public preview, 2026)..."
---

`azure_web_openapi_ai_gateway` configures the AI Gateway tier of Azure API Management (public preview, 2026), a dedicated gateway for AI traffic that fronts model and MCP server endpoints behind one governed entry point with policies like semantic caching and token rate limiting.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi ai gateway"})

    		ubx.Resource(web.OpenapiAiGateway, "example", web.OpenapiAiGatewayConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAiGateway } from "@ubx/sdk-azure/azure/web/openapi_ai_gateway";

    export default stack("example", () => {
      intent({ summary: "example own openapi ai gateway" });

      resource(OpenapiAiGateway, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.web.openapi_ai_gateway import OpenapiAiGateway, OpenapiAiGatewayConfig

    def describe():
        ubx.intent("example own openapi ai gateway")

        ubx.resource(OpenapiAiGateway, "example", OpenapiAiGatewayConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_ai_gateway called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of an AI gateway. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="ai_gateway_id" type="string">
      The description of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
