---
title: "azurerm_web_pubsub_custom_domain"
description: "Real, generated bindings for `azurerm_web_pubsub_custom_domain`."
---

`azurerm_web_pubsub_custom_domain` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pubsub custom domain"})

    		ubx.Resource(web.PubsubCustomDomain, "ci-runner", web.PubsubCustomDomainConfig{
    			DomainName:                   "example-domain-name",
    			Name:                         "ci-runner",
    			WebPubsubCustomCertificateId: "example",
    			WebPubsubId:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PubsubCustomDomain } from "jsr:@ubx/sdk-azure/azurerm/web/pubsub_custom_domain";

    export default stack("payments", () => {
      intent({ summary: "payments own pubsub custom domain" });

      resource(PubsubCustomDomain, "ci-runner", {
        domainName: "example-domain-name",
        name: "ci-runner",
        webPubsubCustomCertificateId: "example",
        webPubsubId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.web import PubsubCustomDomain, PubsubCustomDomainConfig

    def describe():
        ubx.intent("payments own pubsub custom domain")

        ubx.resource(PubsubCustomDomain, "ci-runner", PubsubCustomDomainConfig(
            domain_name="example-domain-name",
            name="ci-runner",
            web_pubsub_custom_certificate_id="example",
            web_pubsub_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_web_pubsub_custom_domain called "ci-runner", with
    domain name example-domain-name, web pubsub custom certificate id
    example, web pubsub id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_pubsub_custom_certificate_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="web_pubsub_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
