---
title: "azure_web_openapi_app_service_environment_resource"
description: "`azure_web_openapi_app_service_environment_resource` provisions an App Service Environment, a single-tenant..."
---

`azure_web_openapi_app_service_environment_resource` provisions an App Service Environment, a single-tenant, isolated deployment of App Service running inside the caller's own virtual network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_web_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_web_openapi/sdk/go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_web_openapi/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi app service environment resource"})

    		ubx.Resource(web.OpenapiAppServiceEnvironmentResource, "example", web.OpenapiAppServiceEnvironmentResourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_web_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiAppServiceEnvironmentResource } from "./local-sdk/azure_web_openapi/sdk/typescript/azure_web_openapi/web/openapi_app_service_environment_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi app service environment resource" });

      resource(OpenapiAppServiceEnvironmentResource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_web_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_web_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_web_openapi.web import OpenapiAppServiceEnvironmentResource, OpenapiAppServiceEnvironmentResourceConfig

    def describe():
        ubx.intent("example own openapi app service environment resource")

        ubx.resource(OpenapiAppServiceEnvironmentResource, "example", OpenapiAppServiceEnvironmentResourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_app_service_environment_resource called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of resource. If the resource is an app, you can refer to https://github.com/Azure/app-service-linux-docs/blob/master/Things_You_Should_Know/kind_property.md#app-service-resource-kind-reference for details supported values for kind. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  Description of an App Service Environment. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="cluster_settings" type="list(object)">
      Custom settings for changing the behavior of the App Service Environment. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_dns_suffix_configuration" type="object">
      Full view of the custom domain suffix configuration for ASEv3. Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string">
          Kind of resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object">
          CustomDnsSuffixConfiguration resource specific properties Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dedicated_host_count" type="number">
      Dedicated Host Count Optional.
    </ResponseField>
    <ResponseField name="dns_suffix" type="string">
      DNS suffix of the App Service Environment. Optional.
    </ResponseField>
    <ResponseField name="front_end_scale_factor" type="number">
      Scale factor for front-ends. Optional.
    </ResponseField>
    <ResponseField name="has_linux_workers" type="bool">
      Flag that displays whether an ASE has linux workers or not Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="internal_load_balancing_mode" type="string">
      Specifies which endpoints to serve internally in the Virtual Network for the App Service Environment. Optional.
    </ResponseField>
    <ResponseField name="ipssl_address_count" type="number">
      Number of IP SSL addresses reserved for the App Service Environment. Optional.
    </ResponseField>
    <ResponseField name="maximum_number_of_machines" type="number">
      Maximum number of VMs in the App Service Environment. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="multi_role_count" type="number">
      Number of front-end instances. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="multi_size" type="string">
      Front-end VM size, e.g. "Medium", "Large". Optional.
    </ResponseField>
    <ResponseField name="networking_configuration" type="object">
      Full view of networking configuration for an ASE. Optional.
      <Expandable title="properties">
        <ResponseField name="kind" type="string">
          Kind of resource. Optional.
        </ResponseField>
        <ResponseField name="properties" type="object">
          AseV3NetworkingConfiguration resource specific properties Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the App Service Plan. Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Current status of the App Service Environment. Optional.
    </ResponseField>
    <ResponseField name="suspended" type="bool">
      &lt;code&gt;true&lt;/code&gt; if the App Service Environment is suspended; otherwise, &lt;code&gt;false&lt;/code&gt;. The environment can be suspended, e.g. when the management endpoint is no longer available (most likely because NSG blocked the incoming traffic). Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="upgrade_availability" type="string">
      Whether an upgrade is available for this App Service Environment. Optional.
    </ResponseField>
    <ResponseField name="upgrade_preference" type="string">
      Upgrade Preference Optional.
    </ResponseField>
    <ResponseField name="user_whitelisted_ip_ranges" type="list(string)">
      User added ip ranges to whitelist on ASE db Optional.
    </ResponseField>
    <ResponseField name="virtual_network" type="object" required>
      Specification for using a Virtual Network. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string" required>
          Resource id of the Virtual Network. Required.
        </ResponseField>
        <ResponseField name="name" type="string">
          Name of the Virtual Network (read-only). Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="subnet" type="string">
          Subnet within the Virtual Network. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          Resource type of the Virtual Network (read-only). Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone_redundant" type="bool">
      Whether or not this App Service Environment is zone-redundant. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
