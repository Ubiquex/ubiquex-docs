---
title: "azure_web_openapi_static_site_linked_backend_armresource"
description: "`azure_web_openapi_static_site_linked_backend_armresource` links an external API backend (e.g. an App Service or Container App) to a Static Web App..."
---

`azure_web_openapi_static_site_linked_backend_armresource` links an external API backend (e.g. an App Service or Container App) to a Static Web App, exposed through its own integrated `/api` routing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi static site linked backend armresource"})

    		ubx.Resource(web.OpenapiStaticSiteLinkedBackendArmresource, "example", web.OpenapiStaticSiteLinkedBackendArmresourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiStaticSiteLinkedBackendArmresource } from "@ubx/sdk-azure/azure/web/openapi_static_site_linked_backend_armresource";

    export default stack("example", () => {
      intent({
        summary: "example own openapi static site linked backend armresource",
      });

      resource(OpenapiStaticSiteLinkedBackendArmresource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.web.openapi_static_site_linked_backend_armresource import OpenapiStaticSiteLinkedBackendArmresource, OpenapiStaticSiteLinkedBackendArmresourceConfig

    def describe():
        ubx.intent("example own openapi static site linked backend armresource")

        ubx.resource(OpenapiStaticSiteLinkedBackendArmresource, "example", OpenapiStaticSiteLinkedBackendArmresourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_static_site_linked_backend_armresource
    called "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of resource. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  StaticSiteLinkedBackendARMResource resource specific properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="backend_resource_id" type="string">
      The resource id of the backend linked to the static site Optional.
    </ResponseField>
    <ResponseField name="created_on" type="string">
      The date and time on which the backend was linked to the static site. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the linking process. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="region" type="string">
      The region of the backend linked to the static site Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
