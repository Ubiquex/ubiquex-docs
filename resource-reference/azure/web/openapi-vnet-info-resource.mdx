---
title: "azure_web_openapi_vnet_info_resource"
description: "`azure_web_openapi_vnet_info_resource` describes the virtual network an App Service web app is integrated with, letting the app reach resources inside it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi vnet info resource"})

    		ubx.Resource(web.OpenapiVnetInfoResource, "example", web.OpenapiVnetInfoResourceConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiVnetInfoResource } from "@ubx/sdk-azure/azure/web/openapi_vnet_info_resource";

    export default stack("example", () => {
      intent({ summary: "example own openapi vnet info resource" });

      resource(OpenapiVnetInfoResource, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.web.openapi_vnet_info_resource import OpenapiVnetInfoResource, OpenapiVnetInfoResourceConfig

    def describe():
        ubx.intent("example own openapi vnet info resource")

        ubx.resource(OpenapiVnetInfoResource, "example", OpenapiVnetInfoResourceConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_web_openapi_vnet_info_resource called "example",
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="kind" type="string">
  Kind of resource. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object">
  Virtual Network information contract. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="cert_blob" type="string">
      A certificate file (.cer) blob containing the public key of the private key used to authenticate a \nPoint-To-Site VPN connection. Optional.
    </ResponseField>
    <ResponseField name="cert_thumbprint" type="string">
      The client certificate thumbprint. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="dns_servers" type="string">
      DNS servers to be used by this Virtual Network. This should be a comma-separated list of IP addresses. Optional.
    </ResponseField>
    <ResponseField name="is_swift" type="bool">
      Flag that is used to denote if this is VNET injection Optional.
    </ResponseField>
    <ResponseField name="resync_required" type="bool">
      &lt;code&gt;true&lt;/code&gt; if a resync is required; otherwise, &lt;code&gt;false&lt;/code&gt;. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="routes" type="list(object)">
      The routes that this Virtual Network connection uses. Computed by Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="kind" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="properties" type="object">
          Optional; if omitted, computed by Azure.
          <Expandable title="properties">
            <ResponseField name="end_address" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="route_type" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="start_address" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vnet_resource_id" type="string">
      The Virtual Network's resource ID. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
