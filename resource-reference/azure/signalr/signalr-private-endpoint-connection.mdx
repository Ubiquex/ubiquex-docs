---
title: "azure_signalr_private_endpoint_connection"
description: "`azure_signalr_private_endpoint_connection` represents the connection between a Private Endpoint and a SignalR Service resource..."
---

`azure_signalr_private_endpoint_connection` represents the connection between a Private Endpoint and a SignalR Service resource, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own signalr private endpoint connection"})

    		ubx.Resource(signalr.PrivateEndpointConnection, "example", signalr.PrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateEndpointConnection } from "@ubx/sdk-azure/azure/signalr/private_endpoint_connection";

    export default stack("example", () => {
      intent({ summary: "example own signalr private endpoint connection" });

      resource(PrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.signalr.private_endpoint_connection import PrivateEndpointConnection, PrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own signalr private endpoint connection")

        ubx.resource(PrivateEndpointConnection, "example", PrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_signalr_private_endpoint_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Private endpoint connection properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_ids" type="list(string)">
      Group IDs Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="private_endpoint" type="object">
      Private endpoint Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Full qualified Id of the private endpoint Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object">
      Connection state of the private endpoint connection Optional.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A message indicating if changes on the service provider require any updates on the consumer. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The reason for approval/rejection of the connection. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether the connection has been Approved/Rejected/Removed by the owner of the service. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
