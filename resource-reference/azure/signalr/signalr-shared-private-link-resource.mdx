---
title: "azure_signalr_shared_private_link_resource"
description: "`azure_signalr_shared_private_link_resource` creates a Private Endpoint from a SignalR Service resource to another Private Link-enabled Azure..."
---

`azure_signalr_shared_private_link_resource` creates a Private Endpoint from a SignalR Service resource to another Private Link-enabled Azure service, so outbound calls (e.g. to Event Grid) can stay private.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_signalr_signalr --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_signalr_signalr/sdk/go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_signalr_signalr/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own signalr shared private link resource"})

    		ubx.Resource(signalr.SignalrSharedPrivateLinkResource, "example", signalr.SignalrSharedPrivateLinkResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_signalr_signalr --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SignalrSharedPrivateLinkResource } from "./local-sdk/azure_signalr_signalr/sdk/typescript/azure_signalr_signalr/signalr/signalr_shared_private_link_resource.ts";

    export default stack("example", () => {
      intent({ summary: "example own signalr shared private link resource" });

      resource(SignalrSharedPrivateLinkResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_signalr_signalr --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_signalr_signalr/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_signalr_signalr.signalr import SignalrSharedPrivateLinkResource, SignalrSharedPrivateLinkResourceConfig

    def describe():
        ubx.intent("example own signalr shared private link resource")

        ubx.resource(SignalrSharedPrivateLinkResource, "example", SignalrSharedPrivateLinkResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_signalr_shared_private_link_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of an existing Shared Private Link Resource Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_id" type="string" required>
      The group id from the provider of resource the shared private link resource is for Required.
    </ResponseField>
    <ResponseField name="private_link_resource_id" type="string" required>
      The resource id of the resource the shared private link resource is for Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="request_message" type="string">
      The request message for requesting approval of the shared private link resource Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Status of the shared private link resource Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
