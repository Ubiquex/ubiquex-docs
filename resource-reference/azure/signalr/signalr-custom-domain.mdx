---
title: "azure_signalr_custom_domain"
description: "`azure_signalr_custom_domain` maps a custom domain to a SignalR Service resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_signalr --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_signalr/sdk/go
    package main

    import (
    	signalr "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_signalr/signalr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own signalr custom domain"})

    		ubx.Resource(signalr.SignalrCustomDomain, "example", signalr.SignalrCustomDomainConfig{
    			Name:       "example",
    			Properties: map[string]any{"custom_certificate": map[string]any{"id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_signalr --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SignalrCustomDomain } from "./local-sdk/azure_signalr/sdk/typescript/azure_signalr/signalr/signalr_custom_domain.ts";

    export default stack("example", () => {
      intent({ summary: "example own signalr custom domain" });

      resource(SignalrCustomDomain, "example", {
        name: "example",
        properties: { customCertificate: { id: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_signalr --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_signalr/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_signalr.signalr import SignalrCustomDomain, SignalrCustomDomainConfig

    def describe():
        ubx.intent("example own signalr custom domain")

        ubx.resource(SignalrCustomDomain, "example", SignalrCustomDomainConfig(
            name="example",
            properties={"custom_certificate": {"id": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_signalr_custom_domain called "example", with
    properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of a custom domain. Required.
  <Expandable title="properties">
    <ResponseField name="custom_certificate" type="object" required>
      Reference to a resource. Required.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource ID. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="domain_name" type="string" required>
      The custom domain name. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
