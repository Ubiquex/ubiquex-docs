---
title: "azure_search_private_endpoint_connection"
description: "`azure_search_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure AI Search service..."
---

`azure_search_private_endpoint_connection` represents the connection between a Private Endpoint and an Azure AI Search service, tracking its own approval state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	search "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own search private endpoint connection"})

    		ubx.Resource(search.PrivateEndpointConnection, "example", search.PrivateEndpointConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateEndpointConnection } from "@ubx/sdk-azure/azure/search/private_endpoint_connection";

    export default stack("example", () => {
      intent({ summary: "example own search private endpoint connection" });

      resource(PrivateEndpointConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.search.private_endpoint_connection import PrivateEndpointConnection, PrivateEndpointConnectionConfig

    def describe():
        ubx.intent("example own search private endpoint connection")

        ubx.resource(PrivateEndpointConnection, "example", PrivateEndpointConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_search_private_endpoint_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of an existing private endpoint connection to the search service. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="group_id" type="string">
      The group ID of the Azure resource for which the private link service is for. Optional.
    </ResponseField>
    <ResponseField name="private_endpoint" type="object">
      The private endpoint resource from Microsoft.Network provider. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The resource ID of the private endpoint resource from Microsoft.Network provider. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_service_connection_state" type="object">
      Describes the current state of an existing Azure Private Link service connection to the private endpoint. Optional.
      <Expandable title="properties">
        <ResponseField name="actions_required" type="string">
          A description of any extra actions that may be required. Optional.
        </ResponseField>
        <ResponseField name="description" type="string">
          The description for the private link service connection state. Optional.
        </ResponseField>
        <ResponseField name="status" type="string">
          Status of the the private link service connection. Valid values are Pending, Approved, Rejected, or Disconnected. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state of the private link service connection. Valid values are Updating, Deleting, Failed, Succeeded, Incomplete, or Canceled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
