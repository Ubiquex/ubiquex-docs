---
title: "azure_hybridkubernetes_connectedclusters_connected_cluster"
description: "`azure_hybridkubernetes_connectedclusters_connected_cluster` represents a Kubernetes cluster running outside Azure (on-premises or another cloud)..."
---

`azure_hybridkubernetes_connectedclusters_connected_cluster` represents a Kubernetes cluster running outside Azure (on-premises or another cloud), connected via Azure Arc for centralized management, GitOps, and policy enforcement.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_hybridkubernetes_connectedclusters --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_hybridkubernetes_connectedclusters/sdk/go
    package main

    import (
    	hybridkubernetes "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_hybridkubernetes_connectedclusters/hybridkubernetes"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connectedclusters connected cluster"})

    		ubx.Resource(hybridkubernetes.ConnectedclustersConnectedCluster, "example", hybridkubernetes.ConnectedclustersConnectedClusterConfig{
    			Identity:   map[string]any{"type": "example"},
    			Properties: map[string]any{"agent_public_key_certificate": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_hybridkubernetes_connectedclusters --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectedclustersConnectedCluster } from "./local-sdk/azure_hybridkubernetes_connectedclusters/sdk/typescript/azure_hybridkubernetes_connectedclusters/hybridkubernetes/connectedclusters_connected_cluster.ts";

    export default stack("example", () => {
      intent({ summary: "example own connectedclusters connected cluster" });

      resource(ConnectedclustersConnectedCluster, "example", {
        identity: { type: "example" },
        properties: { agentPublicKeyCertificate: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_hybridkubernetes_connectedclusters --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_hybridkubernetes_connectedclusters/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_hybridkubernetes_connectedclusters.hybridkubernetes import ConnectedclustersConnectedCluster, ConnectedclustersConnectedClusterConfig

    def describe():
        ubx.intent("example own connectedclusters connected cluster")

        ubx.resource(ConnectedclustersConnectedCluster, "example", ConnectedclustersConnectedClusterConfig(
            identity={"type": "example"},
            properties={"agent_public_key_certificate": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    azure_hybridkubernetes_connectedclusters_connected_cluster, with
    identity a set value, properties a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object" required>
  Identity for the connected cluster. Required.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of connected cluster identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id associated with the connected cluster. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of identity used for the connected cluster. The type 'SystemAssigned, includes a system created identity. The type 'None' means no identity is assigned to the connected cluster. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string">
  Indicates the kind of Arc connected cluster based on host infrastructure. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object" required>
  Properties of the connected cluster. Required.
  <Expandable title="properties">
    <ResponseField name="aad_profile" type="object">
      AAD Profile specifies attributes for Azure Active Directory integration. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_group_object_ids" type="list(string)">
          The list of AAD group object IDs that will have admin role of the cluster. Optional.
        </ResponseField>
        <ResponseField name="enable_azure_rbac" type="bool">
          Whether to enable Azure RBAC for Kubernetes authorization. Optional.
        </ResponseField>
        <ResponseField name="tenant_id" type="string">
          The AAD tenant ID to use for authentication. If not specified, will use the tenant of the deployment subscription. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="agent_public_key_certificate" type="string" required>
      Base64 encoded public certificate used by the agent to do the initial handshake to the backend services in Azure. Required.
    </ResponseField>
    <ResponseField name="agent_version" type="string">
      Version of the agent running on the connected cluster resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="arc_agent_profile" type="object">
      Defines the Arc Agent properties for the clusters. Optional.
      <Expandable title="properties">
        <ResponseField name="agent_auto_upgrade" type="string">
          Indicates whether the Arc agents on the be upgraded automatically to the latest version. Defaults to Enabled. Optional.
        </ResponseField>
        <ResponseField name="agent_errors" type="list(object)">
          List of arc agentry and system components errors on the cluster resource. Optional.
          <Expandable title="properties">
            <ResponseField name="component" type="string">
              The specific software component this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="severity" type="string">
              How serious this event is. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              When this event occurred. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="agent_state" type="string">
          Represents the current state of the Arc agentry and its dependent components. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="desired_agent_version" type="string">
          Version of the Arc agents to be installed on the cluster resource Optional.
        </ResponseField>
        <ResponseField name="system_components" type="list(object)">
          List of system extensions that are installed on the cluster resource. Optional.
          <Expandable title="properties">
            <ResponseField name="current_version" type="string">
              The version currently installed. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="major_version" type="number">
              The major version component this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The Azure resource type, in the form `&#123;resource-provider&#125;/&#123;resource-type&#125;`, e.g. `Microsoft.Network/loadBalancers`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_specified_version" type="string">
              The version explicitly requested by the caller, overriding the default. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="arc_agentry_configurations" type="list(object)">
      Configuration settings for customizing the behavior of the connected cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="feature" type="string">
          The specific feature this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protected_settings" type="map(string)">
          Extension setting(s) encrypted at rest and never returned in subsequent reads. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="settings" type="map(string)">
          The setting(s) configuring this extension. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="azure_hybrid_benefit" type="string">
      Indicates whether Azure Hybrid Benefit is opted in Optional.
    </ResponseField>
    <ResponseField name="connectivity_status" type="string">
      Represents the connectivity status of the connected cluster. Optional.
    </ResponseField>
    <ResponseField name="distribution" type="string">
      The Kubernetes distribution running on this connected cluster. Optional.
    </ResponseField>
    <ResponseField name="distribution_version" type="string">
      The Kubernetes distribution version on this connected cluster. Optional.
    </ResponseField>
    <ResponseField name="gateway" type="object">
      Configuration for the gateway used to connect this cluster back to Azure. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Indicates whether the gateway for arc router connectivity is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="infrastructure" type="string">
      The infrastructure on which the Kubernetes cluster represented by this connected cluster is running on. Optional.
    </ResponseField>
    <ResponseField name="kubernetes_version" type="string">
      The Kubernetes version of the connected cluster resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_connectivity_time" type="string">
      Time representing the last instance when heart beat was received from the cluster Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="managed_identity_certificate_expiration_time" type="string">
      Expiration time of the managed identity certificate Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="miscellaneous_properties" type="map(string)">
      More properties related to the Connected Cluster Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="offering" type="string">
      Connected cluster offering Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="oidc_issuer_profile" type="object">
      OIDC Issuer Profile specifies attributes for workload identity integration. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable oidc issuer for workload identity integration. Optional.
        </ResponseField>
        <ResponseField name="issuer_url" type="string">
          The issuer url for hybrid clusters connected to Arc used for the workload identity feature. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="self_hosted_issuer_url" type="string">
          The issuer url for public cloud clusters - AKS, EKS, GKE - used for the workload identity feature. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_link_scope_resource_id" type="string">
      This is populated only if privateLinkState is enabled. The resource id of the private link scope this connected cluster is assigned to, if any. Optional.
    </ResponseField>
    <ResponseField name="private_link_state" type="string">
      Property which describes the state of private link on a connected cluster resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The current deployment state of connectedClusters. Optional.
    </ResponseField>
    <ResponseField name="security_profile" type="object">
      Security Profile specifies attributes for cluster security features. Optional.
      <Expandable title="properties">
        <ResponseField name="workload_identity" type="object">
          The workload identity feature webhook. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether to enable or disable the workload identity Webhook Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_core_count" type="number">
      Number of CPU cores present in the connected cluster resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="total_node_count" type="number">
      Number of nodes present in the connected cluster resource Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
