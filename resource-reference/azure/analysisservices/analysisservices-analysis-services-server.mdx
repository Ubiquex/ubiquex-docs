---
title: "azure_analysisservices_analysis_services_server"
description: "`azure_analysisservices_analysisservices_analysis_services_server` is an Azure Analysis Services server..."
---

`azure_analysisservices_analysisservices_analysis_services_server` is an Azure Analysis Services server, a fully managed enterprise-grade tabular data modeling platform for BI applications.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	analysisservices "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/analysisservices"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own analysisservices analysis services server"})

    		ubx.Resource(analysisservices.AnalysisServicesServer, "example", analysisservices.AnalysisServicesServerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalysisServicesServer } from "@ubx/sdk-azure/azure/analysisservices/analysis_services_server";

    export default stack("example", () => {
      intent({ summary: "example own analysisservices analysis services server" });

      resource(AnalysisServicesServer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.analysisservices import AnalysisServicesServer, AnalysisServicesServerConfig

    def describe():
        ubx.intent("example own analysisservices analysis services server")

        ubx.resource(AnalysisServicesServer, "example", AnalysisServicesServerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_analysisservices_analysis_services_server, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of Analysis Services resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      The current deployment state of Analysis Services resource. The provisioningState is to indicate states for resource provisioning. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="server_full_name" type="string">
      The full name of the Analysis Services resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="sku" type="object">
      Represents the SKU name and Azure pricing tier for Analysis Services resource. Optional.
      <Expandable title="properties">
        <ResponseField name="capacity" type="number">
          The number of instances in the read only query pool. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Name of the SKU level. Required.
        </ResponseField>
        <ResponseField name="tier" type="string">
          The name of the Azure pricing tier to which the SKU applies. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      The current state of Analysis Services resource. The state is to indicate more states outside of resource provisioning. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
