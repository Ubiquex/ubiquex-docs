---
title: "azure_dns_zone"
description: "`azure_dns_zone` is an Azure DNS public zone, hosting the DNS record(s) for a publicly resolvable domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dns "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/dns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dns zone"})

    		ubx.Resource(dns.Zone, "example", dns.ZoneConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Zone } from "@ubx/sdk-azure/azure/dns/zone";

    export default stack("example", () => {
      intent({ summary: "example own dns zone" });

      resource(Zone, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.dns.zone import Zone, ZoneConfig

    def describe():
        ubx.intent("example own dns zone")

        ubx.resource(Zone, "example", ZoneConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_dns_zone, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  The etag of the zone. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Represents the properties of the zone. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="max_number_of_record_sets" type="number">
      The maximum number of record sets that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="max_number_of_records_per_record_set" type="number">
      The maximum number of records per record set that can be created in this DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="name_servers" type="list(string)">
      The name servers for this DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="number_of_record_sets" type="number">
      The current number of record sets in this DNS zone. This is a read-only property and any attempt to set this value will be ignored. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="registration_virtual_networks" type="list(object)">
      A list of references to virtual networks that register hostnames in this DNS zone. This is a only when ZoneType is Private. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resolution_virtual_networks" type="list(object)">
      A list of references to virtual networks that resolve records in this DNS zone. This is a only when ZoneType is Private. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone_type" type="string">
      The type of this DNS zone (Public or Private). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
