---
title: "azurerm_dns_ns_record"
description: "Real, generated bindings for `azurerm_dns_ns_record`."
---

`azurerm_dns_ns_record` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dns "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/dns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ns record"})

    		ubx.Resource(dns.NsRecord, "ci-runner", dns.NsRecordConfig{
    			Name:              "ci-runner",
    			Records:           []string{"example"},
    			ResourceGroupName: "example-resource-group-name",
    			Ttl:               1,
    			ZoneName:          "example-zone-name",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NsRecord } from "jsr:@ubx/sdk-azure/azurerm/dns/ns_record";

    export default stack("payments", () => {
      intent({ summary: "payments own ns record" });

      resource(NsRecord, "ci-runner", {
        name: "ci-runner",
        records: ["example"],
        resourceGroupName: "example-resource-group-name",
        ttl: 1,
        zoneName: "example-zone-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.dns import NsRecord, NsRecordConfig

    def describe():
        ubx.intent("payments own ns record")

        ubx.resource(NsRecord, "ci-runner", NsRecordConfig(
            name="ci-runner",
            records=["example"],
            resource_group_name="example-resource-group-name",
            ttl=1,
            zone_name="example-zone-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_dns_ns_record called "ci-runner", with records a
    set value, resource group name example-resource-group-name, ttl
    1, zone name example-zone-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="records" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="number" required>
  Required.
</ResponseField>

<ResponseField name="zone_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="fqdn" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
