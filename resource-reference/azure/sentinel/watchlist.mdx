---
title: "azurerm_sentinel_watchlist"
description: "Real, generated bindings for `azurerm_sentinel_watchlist`."
---

`azurerm_sentinel_watchlist` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sentinel "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/sentinel"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own watchlist"})

    		ubx.Resource(sentinel.Watchlist, "ci-runner", sentinel.WatchlistConfig{
    			DisplayName:             "example-display-name",
    			ItemSearchKey:           "example",
    			LogAnalyticsWorkspaceId: "example",
    			Name:                    "ci-runner",
    			DefaultDuration:         "example",
    			Description:             "Managed by ubx.",
    			Labels:                  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Watchlist } from "jsr:@ubx/sdk-azure/azurerm/sentinel/watchlist";

    export default stack("payments", () => {
      intent({ summary: "payments own watchlist" });

      resource(Watchlist, "ci-runner", {
        displayName: "example-display-name",
        itemSearchKey: "example",
        logAnalyticsWorkspaceId: "example",
        name: "ci-runner",
        defaultDuration: "example",
        description: "Managed by ubx.",
        labels: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.sentinel import Watchlist, WatchlistConfig

    def describe():
        ubx.intent("payments own watchlist")

        ubx.resource(Watchlist, "ci-runner", WatchlistConfig(
            display_name="example-display-name",
            item_search_key="example",
            log_analytics_workspace_id="example",
            name="ci-runner",
            default_duration="example",
            description="Managed by ubx.",
            labels=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_sentinel_watchlist called "ci-runner", with
    display name example-display-name, item search key example, log
    analytics workspace id example, default duration example,
    description Managed by ubx., labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="item_search_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="labels" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="log_analytics_workspace_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
