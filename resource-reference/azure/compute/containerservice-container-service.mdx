---
title: "azure_compute_containerservice_container_service"
description: "`azure_compute_containerservice_container_service` is the legacy Azure Container Service (ACS) resource, an early..."
---

`azure_compute_containerservice_container_service` is the legacy Azure Container Service (ACS) resource, an early, now largely superseded orchestrator deployment predating Azure Kubernetes Service.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_compute_containerservice --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_compute_containerservice/sdk/go
    package main

    import (
    	compute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_compute_containerservice/compute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own containerservice container service"})

    		ubx.Resource(compute.ContainerserviceContainerService, "ci-runner", compute.ContainerserviceContainerServiceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_compute_containerservice --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContainerserviceContainerService } from "./local-sdk/azure_compute_containerservice/sdk/typescript/azure_compute_containerservice/compute/containerservice_container_service.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own containerservice container service" });

      resource(ContainerserviceContainerService, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_compute_containerservice --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_compute_containerservice/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_compute_containerservice.compute import ContainerserviceContainerService, ContainerserviceContainerServiceConfig

    def describe():
        ubx.intent("payments own containerservice container service")

        ubx.resource(ContainerserviceContainerService, "ci-runner", ContainerserviceContainerServiceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_compute_containerservice_container_service, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the container service. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="agent_pool_profiles" type="list(object)" required>
      Properties of the agent pool. Required.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          The number of agent node(s) in this pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dns_prefix" type="string">
          The DNS name prefix used to construct this agent pool's own fully qualified domain name. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fqdn" type="string">
          Output only. The fully qualified domain name assigned to this agent pool. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vm_size" type="string">
          The VM size this agent pool's own nodes are provisioned as. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_profile" type="object">
      Properties to configure a custom container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="orchestrator" type="string" required>
          The name of the custom orchestrator to use. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="diagnostics_profile" type="object">
      Configuration for collecting diagnostic data from this container service's own VMs. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="vm_diagnostics" type="object" required>
          Profile for diagnostics on the container service VMs. Required.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool" required>
              Whether the VM diagnostic agent is provisioned on the VM. Required.
            </ResponseField>
            <ResponseField name="storage_uri" type="string">
              The URI of the storage account where diagnostics are stored. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="linux_profile" type="object" required>
      Profile for Linux VMs in the container service cluster. Required.
      <Expandable title="properties">
        <ResponseField name="admin_username" type="string" required>
          The administrator username to use for Linux VMs. Required.
        </ResponseField>
        <ResponseField name="ssh" type="object" required>
          SSH configuration for Linux-based VMs running on Azure. Required.
          <Expandable title="properties">
            <ResponseField name="public_keys" type="list(object)" required>
              the list of SSH public keys used to authenticate with Linux-based VMs. Required.
              <Expandable title="properties">
                <ResponseField name="key_data" type="string">
                  The SSH public key data used to authenticate to Linux nodes in this cluster. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="master_profile" type="object" required>
      Profile for the container service master. Required.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Number of masters (VMs) in the container service cluster. Allowed values are 1, 3, and 5. The default value is 1. Optional.
        </ResponseField>
        <ResponseField name="dns_prefix" type="string" required>
          DNS prefix to be used to create the FQDN for master. Required.
        </ResponseField>
        <ResponseField name="fqdn" type="string">
          FQDN for the master. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="orchestrator_profile" type="object">
      Profile for the container service orchestrator. Optional.
      <Expandable title="properties">
        <ResponseField name="orchestrator_type" type="string" required>
          The orchestrator to use to manage container service cluster resources. Valid values are Swarm, DCOS, and Custom. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      the current deployment or provisioning state, which only appears in the response. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="service_principal_profile" type="object">
      Information about a service principal identity for the cluster to use for manipulating Azure APIs. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string" required>
          The ID for the service principal. Required.
        </ResponseField>
        <ResponseField name="secret" type="string" required>
          The secret password associated with the service principal. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="windows_profile" type="object">
      Profile for Windows VMs in the container service cluster. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_password" type="string" required>
          The administrator password to use for Windows VMs. Required.
        </ResponseField>
        <ResponseField name="admin_username" type="string" required>
          The administrator username to use for Windows VMs. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
