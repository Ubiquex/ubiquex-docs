---
title: "azure_codesigning_codesigningaccount_code_signing_account"
description: "`azure_codesigning_codesigningaccount_code_signing_account` is a Trusted Signing account..."
---

`azure_codesigning_codesigningaccount_code_signing_account` is a Trusted Signing account, providing managed code-signing certificates without the caller having to procure and rotate them manually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codesigning "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/codesigning"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own codesigningaccount code signing account"})

    		ubx.Resource(codesigning.CodesigningaccountCodeSigningAccount, "example", codesigning.CodesigningaccountCodeSigningAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CodesigningaccountCodeSigningAccount } from "@ubx/sdk-azure/azure/codesigning/codesigningaccount_code_signing_account";

    export default stack("example", () => {
      intent({ summary: "example own codesigningaccount code signing account" });

      resource(CodesigningaccountCodeSigningAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.codesigning import CodesigningaccountCodeSigningAccount, CodesigningaccountCodeSigningAccountConfig

    def describe():
        ubx.intent("example own codesigningaccount code signing account")

        ubx.resource(CodesigningaccountCodeSigningAccount, "example", CodesigningaccountCodeSigningAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_codesigning_codesigningaccount_code_signing_account,
    with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of the artifact signing account. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="account_uri" type="string">
      The URI of the artifact signing account which is used during signing files. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The status of the current operation. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="sku" type="object">
      SKU of the artifact signing account. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
          Name of the sku. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
