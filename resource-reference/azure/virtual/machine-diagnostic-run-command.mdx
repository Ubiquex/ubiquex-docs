---
title: "azure_virtual_machine_diagnostic_run_command"
description: "`azure_virtual_machine_diagnostic_run_command` runs a script against one specific VM instance inside a scale set through the VM agent..."
---

`azure_virtual_machine_diagnostic_run_command` runs a script against one specific VM instance inside a scale set through the VM agent, the same real Run Command mechanism Azure uses for general VM management -- diagnosing and remediating an instance's access or network problems without needing direct remote access to it.

Real, typed bindings generated directly from the real Azure Resource Manager API specification, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own machine diagnostic run command"})

    		ubx.Resource(virtual.MachineDiagnosticRunCommand, "ci-runner", virtual.MachineDiagnosticRunCommandConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineDiagnosticRunCommand } from "./local-sdk/azure/sdk/typescript/azure/virtual/machine_diagnostic_run_command.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own machine diagnostic run command" });

      resource(MachineDiagnosticRunCommand, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.virtual import MachineDiagnosticRunCommand, MachineDiagnosticRunCommandConfig

    def describe():
        ubx.intent("payments own machine diagnostic run command")

        ubx.resource(MachineDiagnosticRunCommand, "ci-runner", MachineDiagnosticRunCommandConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_virtual_machine_diagnostic_run_command, with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="properties" type="object">
  Describes the properties of a Virtual Machine run command. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="async_execution" type="bool">
      Optional. If set to true, provisioning will complete as soon as the script starts and will not wait for script to complete. Optional.
    </ResponseField>
    <ResponseField name="error_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_blob_uri" type="string">
      Specifies the Azure storage blob where script error stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer errorBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      The instance view of a virtual machine run command. Optional.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          Script end time. Optional.
        </ResponseField>
        <ResponseField name="error" type="string">
          Script error stream. Optional.
        </ResponseField>
        <ResponseField name="execution_message" type="string">
          Communicate script configuration errors or execution messages. Optional.
        </ResponseField>
        <ResponseField name="execution_state" type="string">
          Script execution status. Optional.
        </ResponseField>
        <ResponseField name="exit_code" type="number">
          Exit code returned from script execution. Optional.
        </ResponseField>
        <ResponseField name="output" type="string">
          Script output stream. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Script start time. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the instance view status, indicating the state of the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the instance view status. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              The message that provides additional details about the status of the diagnostic run command, such as error information or execution progress. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp when the status was recorded, in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_uri" type="string">
      Specifies the Azure storage blob where script output stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer outputBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the parameter to pass to the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protected_parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the protected parameter to pass to the run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of a protected parameter, used to hold sensitive information such as passwords or keys that are passed to the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. If treatFailureAsDeploymentFailure set to true, any failure in the script will fail the deployment and ProvisioningState will be marked as Failed. If treatFailureAsDeploymentFailure set to false, ProvisioningState would only reflect whether the run command was run or not by the extensions platform, it would not indicate whether script failed in case of script failures. See instance view of run command in case of script failures to see executionMessage, output, error: https://aka.ms/runcommandmanaged#get-execution-status-and-results Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="run_as_password" type="string">
      Specifies the user account password on the VM when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="run_as_user" type="string">
      Specifies the user account on the VM when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="source" type="object">
      Describes the script sources for run command. Use only one of these script sources: script, scriptUri, commandId, galleryScriptReferenceId. Optional.
      <Expandable title="properties">
        <ResponseField name="command_id" type="string">
          Specifies a commandId of predefined built-in script. Command IDs available for Linux are listed at https://aka.ms/RunCommandManagedLinux#available-commands, Windows at https://aka.ms/RunCommandManagedWindows#available-commands. Optional.
        </ResponseField>
        <ResponseField name="gallery_script_reference_id" type="string">
          The resource ID of a Gallery Script version that needs to be executed. Example ID looks like /subscriptions/&#123;subId&#125;/resourceGroups/&#123;rgName&#125;/providers/Microsoft.Compute/galleries/&#123;galleryName&#125;/scripts/&#123;scriptName&#125;/versions/&#123;version&#125;. Optional.
        </ResponseField>
        <ResponseField name="script" type="string">
          Specifies the script content to be executed on the VM. Optional.
        </ResponseField>
        <ResponseField name="script_shell" type="string">
          Script shell types. Optional.
        </ResponseField>
        <ResponseField name="script_uri" type="string">
          Specifies the script download location. It can be either SAS URI of an Azure storage blob with read access or public URI. Optional.
        </ResponseField>
        <ResponseField name="script_uri_managed_identity" type="object">
          Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
            </ResponseField>
            <ResponseField name="object_id" type="string">
              Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout_in_seconds" type="number">
      The timeout in seconds to execute the run command. Optional.
    </ResponseField>
    <ResponseField name="treat_failure_as_deployment_failure" type="bool">
      Optional. If set to true, any failure in the script will fail the deployment and ProvisioningState will be marked as Failed. If set to false, ProvisioningState would only reflect whether the run command was run or not by the extensions platform, it would not indicate whether script failed in case of script failures. See instance view of run command in case of script failures to see executionMessage, output, error: https://aka.ms/runcommandmanaged#get-execution-status-and-results Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="properties" type="object">
  Describes the properties of a Virtual Machine run command. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="async_execution" type="bool">
      Optional. If set to true, provisioning will complete as soon as the script starts and will not wait for script to complete. Optional.
    </ResponseField>
    <ResponseField name="error_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_blob_uri" type="string">
      Specifies the Azure storage blob where script error stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer errorBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      The instance view of a virtual machine run command. Optional.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          Script end time. Optional.
        </ResponseField>
        <ResponseField name="error" type="string">
          Script error stream. Optional.
        </ResponseField>
        <ResponseField name="execution_message" type="string">
          Communicate script configuration errors or execution messages. Optional.
        </ResponseField>
        <ResponseField name="execution_state" type="string">
          Script execution status. Optional.
        </ResponseField>
        <ResponseField name="exit_code" type="number">
          Exit code returned from script execution. Optional.
        </ResponseField>
        <ResponseField name="output" type="string">
          Script output stream. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Script start time. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the instance view status, indicating the state of the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the instance view status. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              The message that provides additional details about the status of the diagnostic run command, such as error information or execution progress. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp when the status was recorded, in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_uri" type="string">
      Specifies the Azure storage blob where script output stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer outputBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Azure.
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the parameter to pass to the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protected_parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the protected parameter to pass to the run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of a protected parameter, used to hold sensitive information such as passwords or keys that are passed to the diagnostic run command. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. If treatFailureAsDeploymentFailure set to true, any failure in the script will fail the deployment and ProvisioningState will be marked as Failed. If treatFailureAsDeploymentFailure set to false, ProvisioningState would only reflect whether the run command was run or not by the extensions platform, it would not indicate whether script failed in case of script failures. See instance view of run command in case of script failures to see executionMessage, output, error: https://aka.ms/runcommandmanaged#get-execution-status-and-results Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="run_as_password" type="string">
      Specifies the user account password on the VM when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="run_as_user" type="string">
      Specifies the user account on the VM when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="source" type="object">
      Describes the script sources for run command. Use only one of these script sources: script, scriptUri, commandId, galleryScriptReferenceId. Optional.
      <Expandable title="properties">
        <ResponseField name="command_id" type="string">
          Specifies a commandId of predefined built-in script. Command IDs available for Linux are listed at https://aka.ms/RunCommandManagedLinux#available-commands, Windows at https://aka.ms/RunCommandManagedWindows#available-commands. Optional.
        </ResponseField>
        <ResponseField name="gallery_script_reference_id" type="string">
          The resource ID of a Gallery Script version that needs to be executed. Example ID looks like /subscriptions/&#123;subId&#125;/resourceGroups/&#123;rgName&#125;/providers/Microsoft.Compute/galleries/&#123;galleryName&#125;/scripts/&#123;scriptName&#125;/versions/&#123;version&#125;. Optional.
        </ResponseField>
        <ResponseField name="script" type="string">
          Specifies the script content to be executed on the VM. Optional.
        </ResponseField>
        <ResponseField name="script_shell" type="string">
          Script shell types. Optional.
        </ResponseField>
        <ResponseField name="script_uri" type="string">
          Specifies the script download location. It can be either SAS URI of an Azure storage blob with read access or public URI. Optional.
        </ResponseField>
        <ResponseField name="script_uri_managed_identity" type="object">
          Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
            </ResponseField>
            <ResponseField name="object_id" type="string">
              Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout_in_seconds" type="number">
      The timeout in seconds to execute the run command. Optional.
    </ResponseField>
    <ResponseField name="treat_failure_as_deployment_failure" type="bool">
      Optional. If set to true, any failure in the script will fail the deployment and ProvisioningState will be marked as Failed. If set to false, ProvisioningState would only reflect whether the run command was run or not by the extensions platform, it would not indicate whether script failed in case of script failures. See instance view of run command in case of script failures to see executionMessage, output, error: https://aka.ms/runcommandmanaged#get-execution-status-and-results Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
