---
title: "azure_virtual_machine_scale_set_vm"
description: "`azure_virtual_machine_scale_set_vm` represents one individual VM instance that's part of a scale set..."
---

`azure_virtual_machine_scale_set_vm` represents one individual VM instance that's part of a scale set, letting that specific instance be read or managed on its own rather than only through the scale set as a whole.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure/sdk/go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own machine scale set vm"})

    		ubx.Resource(virtual.MachineScaleSetVm, "ci-runner", virtual.MachineScaleSetVmConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineScaleSetVm } from "./local-sdk/azure/sdk/typescript/azure/virtual/machine_scale_set_vm.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own machine scale set vm" });

      resource(MachineScaleSetVm, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure.virtual import MachineScaleSetVm, MachineScaleSetVmConfig

    def describe():
        ubx.intent("payments own machine scale set vm")

        ubx.resource(MachineScaleSetVm, "ci-runner", MachineScaleSetVmConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_virtual_machine_scale_set_vm, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Etag is property returned in Update/Get response of the VMSS VM, so that customer can supply it in the header to ensure optimistic updates. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the virtual machine. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal id of virtual machine identity. This property will only be provided for a system assigned identity. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant id associated with the virtual machine. This property will only be provided for a system assigned identity. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of identity used for the virtual machine scale set. The type 'SystemAssigned, UserAssigned' includes both an implicitly created identity and a set of user assigned identities. The type 'None' will remove any identities from the virtual machine scale set. Optional.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The list of user identities associated with the Virtual Machine. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;'. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client ID (GUID) of the user-assigned managed identity, used to identify it when requesting tokens. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          The principal ID of the user-assigned identity assigned to the virtual machine scale set VM. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_id" type="string">
  The virtual machine instance ID. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="plan" type="object">
  Specifies information about the marketplace image used to create the virtual machine. This element is only used for marketplace images. Before you can use a marketplace image from an API, you must enable the image for programmatic use. In the Azure portal, find the marketplace image that you want to use and then click **Want to deploy programmatically, Get Started -&gt;**. Enter any required information and then click **Save**. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The plan ID. Optional.
    </ResponseField>
    <ResponseField name="product" type="string">
      Specifies the product of the image from the marketplace. This is the same value as Offer under the imageReference element. Optional.
    </ResponseField>
    <ResponseField name="promotion_code" type="string">
      The promotion code. Optional.
    </ResponseField>
    <ResponseField name="publisher" type="string">
      The publisher ID. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  Describes the properties of a virtual machine scale set virtual machine. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="additional_capabilities" type="object">
      Enables or disables a capability on the virtual machine or virtual machine scale set. Optional.
      <Expandable title="properties">
        <ResponseField name="enable_fips1403_encryption" type="bool">
          The flag enables the usage of FIPS 140-3 compliant cryptography on the protectedSettings of an extension. Learn more at: https://aka.ms/linuxagentfipssupport. Optional.
        </ResponseField>
        <ResponseField name="hibernation_enabled" type="bool">
          The flag that enables or disables hibernation capability on the VM. Optional.
        </ResponseField>
        <ResponseField name="ultra_ssdenabled" type="bool">
          The flag that enables or disables a capability to have one or more managed data disks with UltraSSD_LRS storage account type on the VM or VMSS. Managed disks with storage account type UltraSSD_LRS can be added to a virtual machine or virtual machine scale set only if this property is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="availability_set" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Resource Id Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="capacity_reservation" type="object">
      The parameters of a capacity reservation Profile. Optional.
      <Expandable title="properties">
        <ResponseField name="capacity_reservation_group" type="object">
          Specifies the capacity reservation group to associate with the virtual machine scale set VM. This allows the VM to use reserved capacity from the specified group. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource Id Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disable_capacity_reservation_assignment" type="bool">
          Specifies whether the virtual machine is explicitly opted out from being associated with any capacity reservation. When set to true, the virtual machine will not be allowed to implicitly or explicitly associate with any type of capacity reservation and will consume capacity from the publicly available capacity. Minimum api-version: 2026-04-01. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="diagnostics_profile" type="object">
      Specifies the boot diagnostic settings state. Minimum api-version: 2015-06-15. Optional.
      <Expandable title="properties">
        <ResponseField name="boot_diagnostics" type="object">
          Boot Diagnostics is a debugging feature which allows you to view Console Output and Screenshot to diagnose VM status. You can easily view the output of your console log. Azure also enables you to see a screenshot of the VM from the hypervisor. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Whether boot diagnostics should be enabled on the Virtual Machine. Optional.
            </ResponseField>
            <ResponseField name="storage_uri" type="string">
              Uri of the storage account to use for placing the console output and screenshot. If storageUri is not specified while enabling boot diagnostics, managed storage will be used. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hardware_profile" type="object">
      Specifies the hardware settings for the virtual machine. Optional.
      <Expandable title="properties">
        <ResponseField name="processor_mode" type="string">
          Specifies the processor frequency behavior for the virtual machine. See each member for the behavior it controls. Optional.
        </ResponseField>
        <ResponseField name="vm_size" type="string">
          Specifies the size of the virtual machine. The enum data type is currently deprecated and will be removed by December 23rd 2023. The recommended way to get the list of available sizes is using these APIs: [List all available virtual machine sizes in an availability set](https://docs.microsoft.com/rest/api/compute/availabilitysets/listavailablesizes), [List all available virtual machine sizes in a region]( https://docs.microsoft.com/rest/api/compute/resourceskus/list), [List all available virtual machine sizes for resizing](https://docs.microsoft.com/rest/api/compute/virtualmachines/listavailablesizes). For more information about virtual machine sizes, see [Sizes for virtual machines](https://docs.microsoft.com/azure/virtual-machines/sizes). The available VM sizes depend on region and availability set. Optional.
        </ResponseField>
        <ResponseField name="vm_size_properties" type="object">
          Specifies VM Size Property settings on the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="v_cpus_available" type="number">
              Specifies the number of vCPUs available for the VM. When this property is not specified in the request body the default behavior is to set it to the value of vCPUs available for that VM size exposed in api response of [List all available virtual machine sizes in a region](https://docs.microsoft.com/en-us/rest/api/compute/resource-skus/list). Optional.
            </ResponseField>
            <ResponseField name="v_cpus_per_core" type="number">
              Specifies the vCPU to physical core ratio. When this property is not specified in the request body the default behavior is set to the value of vCPUsPerCore for the VM Size exposed in api response of [List all available virtual machine sizes in a region](https://docs.microsoft.com/en-us/rest/api/compute/resource-skus/list). **Setting this property to 1 also means that hyper-threading is disabled.** Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      The instance view of a virtual machine scale set VM. Optional.
      <Expandable title="properties">
        <ResponseField name="assigned_host" type="string">
          Resource id of the dedicated host, on which the virtual machine is allocated through automatic placement, when the virtual machine is associated with a dedicated host group that has automatic placement enabled. Minimum api-version: 2020-06-01. Computed by Azure after `ship`.
        </ResponseField>
        <ResponseField name="boot_diagnostics" type="object">
          The instance view of a virtual machine boot diagnostics. Optional.
          <Expandable title="properties">
            <ResponseField name="console_screenshot_blob_uri" type="string">
              The console screenshot blob URI. **Note:** This will **not** be set if boot diagnostics is currently enabled with managed storage. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="serial_console_log_blob_uri" type="string">
              The serial console log blob Uri. **Note:** This will **not** be set if boot diagnostics is currently enabled with managed storage. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="status" type="object">
              Instance view status. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The status code. Optional.
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  The short localizable label for the status. Optional.
                </ResponseField>
                <ResponseField name="level" type="string">
                  The level code. Optional.
                </ResponseField>
                <ResponseField name="message" type="string">
                  The detailed status message, including for alerts and error messages. Optional.
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time of the status. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="capacity_reservation_type" type="string">
          Specifies which type of capacity reservation the virtual machine will consume capacity from if eligible or whether it is explicitly opted out from being associated and consuming capacity from any reserved capacity available in the subscription. Minimum api-version: 2026-04-01. Optional.
        </ResponseField>
        <ResponseField name="computer_name" type="string">
          Specifies the host OS name of the virtual machine. &lt;br&gt;&lt;br&gt; This name cannot be updated after the VM is created. &lt;br&gt;&lt;br&gt; **Max-length (Windows):** 15 characters &lt;br&gt;&lt;br&gt; **Max-length (Linux):** 64 characters. &lt;br&gt;&lt;br&gt; For naming conventions and restrictions see [Azure infrastructure services implementation guidelines](https://learn.microsoft.com/previous-versions/azure/virtual-machines/linux/infrastructure-example?toc=%2Fazure%2Fvirtual-machines%2Flinux%2Ftoc.json#1-naming-conventions). Optional.
        </ResponseField>
        <ResponseField name="disks" type="list(object)">
          The disks information. Optional.
          <Expandable title="properties">
            <ResponseField name="encryption_settings" type="list(object)">
              The encryption settings for the disk, as observed in the instance view. Each object represents the encryption configuration applied to this disk, typically including encryption status and key references. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="disk_encryption_key" type="object">
                  The disk encryption key used for encrypting the disk, containing a reference to the Azure Key Vault secret (secret URL and source vault). Optional; if omitted, computed by Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="secret_url" type="string">
                      The secret URL of the disk encryption key in the Key Vault. This URL points to the secret containing the encryption key used for disk encryption. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="source_vault" type="object">
                      Specifies the Azure Key Vault that contains the disk encryption key used for encrypting the disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          The resource ID of the Key Vault containing the disk encryption key. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="enabled" type="bool">
                  Indicates whether encryption is enabled for the disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key_encryption_key" type="object">
                  Represents the Key Encryption Key (KEK) used to wrap the disk encryption key. Includes the key URL and the source vault reference. Optional; if omitted, computed by Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="key_url" type="string">
                      The URL of the key encryption key (KEK) used to wrap the disk encryption key. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="source_vault" type="object">
                      The Key Vault that contains the key encryption key used for disk encryption. Optional; if omitted, computed by Azure. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          The Azure resource ID of the Key Vault that contains the key encryption key. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="statuses" type="list(object)">
              A list of statuses reported for the disk, with each status containing details such as code, level, display status, and message. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The code of the disk status, indicating the disk's current state in the instance view (e.g., 'Attached' or 'Detached'). Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  The human-readable status of the disk, such as 'Healthy' or 'Unhealthy'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The severity level of the disk status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message that provides additional details about the disk status. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The timestamp (in UTC) indicating when the disk status was last reported or updated. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_alignment_status" type="string">
              The storage alignment status of the disk. Possible values are 'Aligned' and 'Unaligned'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extensions" type="list(object)">
          The extensions information. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the virtual machine extension as reported in the instance view. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="statuses" type="list(object)">
              The list of statuses for the extension instance view, including status code, level, display status, message, and time. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The status code for the extension's current state, such as 'ProvisioningState/succeeded' or 'ProvisioningState/failed'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  The human-readable status description for this instance view status, such as 'Provisioning succeeded' or 'Provisioning failed'. It is the localized display equivalent of the status code. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The severity level of the status, indicating the impact or urgency. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message providing additional details about the extension status. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The timestamp, as a string, when the status was reported or last updated. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="substatuses" type="list(object)">
              The list of sub-statuses for the extension instance view, providing additional status information beyond the primary status. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
                <ResponseField name="level" type="string">
                  The level of the status, which indicates the severity. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  The status message, providing additional information about the extension's condition or substatus. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time when the substatus was recorded, typically in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of the virtual machine extension, specifying the publisher and extension type (e.g., Microsoft.Azure.Extensions.CustomScript). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_handler_version" type="string">
              The version number of the extension handler running on the VM instance. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hyper_vgeneration" type="string">
          The hypervisor generation of the Virtual Machine. Optional.
        </ResponseField>
        <ResponseField name="interconnect_instance_view" type="object">
          The Interconnect Block instance view details for a Virtual Machine or Scale Set VM instance. Optional.
          <Expandable title="properties">
            <ResponseField name="interconnect_subgroup_id" type="string">
              The ID (GUID) of the Interconnect subgroup in which the Virtual Machine was placed. Computed by Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="maintenance_redeploy_status" type="object">
          Maintenance Operation Status. Optional.
          <Expandable title="properties">
            <ResponseField name="is_customer_initiated_maintenance_allowed" type="bool">
              True, if customer is allowed to perform Maintenance. Optional.
            </ResponseField>
            <ResponseField name="last_operation_message" type="string">
              Message returned for the last Maintenance Operation. Optional.
            </ResponseField>
            <ResponseField name="last_operation_result_code" type="string">
              The Last Maintenance Operation Result Code. Optional.
            </ResponseField>
            <ResponseField name="maintenance_window_end_time" type="string">
              End Time for the Maintenance Window. Optional.
            </ResponseField>
            <ResponseField name="maintenance_window_start_time" type="string">
              Start Time for the Maintenance Window. Optional.
            </ResponseField>
            <ResponseField name="pre_maintenance_window_end_time" type="string">
              End Time for the Pre Maintenance Window. Optional.
            </ResponseField>
            <ResponseField name="pre_maintenance_window_start_time" type="string">
              Start Time for the Pre Maintenance Window. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="os_name" type="string">
          The Operating System running on the hybrid machine. Optional.
        </ResponseField>
        <ResponseField name="os_version" type="string">
          The version of Operating System running on the hybrid machine. Optional.
        </ResponseField>
        <ResponseField name="placement_group_id" type="string">
          The placement group in which the VM is running. If the VM is deallocated it will not have a placementGroupId. Optional.
        </ResponseField>
        <ResponseField name="platform_fault_domain" type="number">
          The Fault Domain count. Optional.
        </ResponseField>
        <ResponseField name="platform_update_domain" type="number">
          The Update Domain count. Optional.
        </ResponseField>
        <ResponseField name="rdp_thumb_print" type="string">
          The Remote desktop certificate thumbprint. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for the VM instance, such as 'ProvisioningState/succeeded' or 'PowerState/running'. This code identifies the specific status condition. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              The human-readable display status of the virtual machine instance, such as 'Provisioning succeeded' or 'Running'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the status. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the status. In Azure instance view statuses, this field contains the message text for the corresponding status code, such as the reason for a provisioning state or health state. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp indicating when the status was last updated, formatted as an ISO 8601 date-time string. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vm_agent" type="object">
          The instance view of the VM Agent running on the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="extension_handlers" type="list(object)">
              The virtual machine extension handler instance view. Optional.
              <Expandable title="properties">
                <ResponseField name="status" type="object">
                  The status of the extension handler, containing properties such as code, level (e.g., Info, Warning, Error), displayStatus, message, and time. Optional; if omitted, computed by Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      The machine-readable status code for the extension handler, such as 'ProvisioningState/succeeded' or 'ProvisioningState/failed'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="display_status" type="string">
                      The human-readable status text for the extension handler status, such as 'Provisioning succeeded' or 'Provisioning failed'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="level" type="string">
                      Indicates the severity level of the status. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      The status message providing additional details about the current state of the extension handler. This often contains error details or informational text corresponding to the status code. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="time" type="string">
                      The timestamp when the extension handler status was reported. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of the extension handler, such as Microsoft.Compute.CustomScriptExtension. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type_handler_version" type="string">
                  The version of the extension handler type installed on the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="statuses" type="list(object)">
              The resource status information. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The status code for the VM agent, typically indicating its provisioning state (e.g., 'ProvisioningState/succeeded' or 'ProvisioningState/failed'). Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  A human-readable label that describes the current status of the VM agent, suitable for display to users. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The severity level of the status: Info, Warning, or Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  The message associated with the VM agent status, providing additional details about the condition indicated by the status code. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time at which the status was reported by the VM agent. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="vm_agent_version" type="string">
              The VM Agent full version. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="vm_health" type="object">
          The health status of the VM. Optional.
          <Expandable title="properties">
            <ResponseField name="status" type="object">
              Instance view status. Optional.
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The status code. Optional.
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  The short localizable label for the status. Optional.
                </ResponseField>
                <ResponseField name="level" type="string">
                  The level code. Optional.
                </ResponseField>
                <ResponseField name="message" type="string">
                  The detailed status message, including for alerts and error messages. Optional.
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time of the status. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="interconnect_block_profile" type="object">
      The parameters of an Interconnect Block Profile. Optional.
      <Expandable title="properties">
        <ResponseField name="interconnect_block" type="object">
          The API entity reference. Optional.
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The ARM resource id in the form of /subscriptions/&#123;SubscriptionId&#125;/resourceGroups/&#123;ResourceGroupName&#125;/... Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="latest_model_applied" type="bool">
      Specifies whether the latest model has been applied to the virtual machine. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="license_type" type="string">
      Specifies that the image or disk that is being used was licensed on-premises. &lt;br&gt;&lt;br&gt; Possible values for Windows Server operating system are: &lt;br&gt;&lt;br&gt; Windows_Client &lt;br&gt;&lt;br&gt; Windows_Server &lt;br&gt;&lt;br&gt; Possible values for Linux Server operating system are: &lt;br&gt;&lt;br&gt; RHEL_BYOS (for RHEL) &lt;br&gt;&lt;br&gt; SLES_BYOS (for SUSE) &lt;br&gt;&lt;br&gt; For more information, see [Azure Hybrid Use Benefit for Windows Server](https://docs.microsoft.com/azure/virtual-machines/windows/hybrid-use-benefit-licensing) &lt;br&gt;&lt;br&gt; [Azure Hybrid Use Benefit for Linux Server](https://docs.microsoft.com/azure/virtual-machines/linux/azure-hybrid-benefit-linux) &lt;br&gt;&lt;br&gt; Minimum api-version: 2015-06-15 Optional.
    </ResponseField>
    <ResponseField name="model_definition_applied" type="string">
      Specifies whether the model applied to the virtual machine is the model of the virtual machine scale set or the customized model for the virtual machine. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="network_profile" type="object">
      Specifies the network interfaces or the networking configuration of the virtual machine. Optional.
      <Expandable title="properties">
        <ResponseField name="interconnect_group_profile" type="object">
          Specifies the interconnect group profile for a virtual machine, used to associate the VM with an interconnect group and subgroups. Optional.
          <Expandable title="properties">
            <ResponseField name="interconnect_group" type="object">
              Optional.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Resource Id Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="subgroups" type="list(object)">
              The list of subgroup references within the interconnect group. Optional.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_api_version" type="string">
          specifies the Microsoft.Network API version used when creating networking resources in the Network Interface Configurations Optional.
        </ResponseField>
        <ResponseField name="network_interface_configurations" type="list(object)">
          Specifies the networking configurations that will be used to create the virtual machine networking resources. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the network interface configuration. This name is used to identify the NIC configuration within the network profile of the VM scale set instance. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="properties" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
            <ResponseField name="tags" type="map(string)">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          Specifies the list of resource Ids for the network interfaces associated with the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="properties" type="object">
              The configuration properties of the network interface, including its IP configurations and whether it is the primary network interface. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_profile_configuration" type="object">
      Describes a virtual machine scale set VM network profile. Optional.
      <Expandable title="properties">
        <ResponseField name="interconnect_group_profile" type="object">
          Specifies the interconnect group profile for a virtual machine, used to associate the VM with an interconnect group and subgroups. Optional.
          <Expandable title="properties">
            <ResponseField name="interconnect_group" type="object">
              Optional.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Resource Id Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="subgroups" type="list(object)">
              The list of subgroup references within the interconnect group. Optional.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  Optional; if omitted, computed by Azure.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_interface_configurations" type="list(object)">
          The list of network configurations. Optional.
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="properties" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
            <ResponseField name="tags" type="map(string)">
              Optional; if omitted, computed by Azure.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="os_profile" type="object">
      Specifies the operating system settings for the virtual machine. Some of the settings cannot be changed once VM is provisioned. Optional.
      <Expandable title="properties">
        <ResponseField name="admin_password" type="string">
          Specifies the password of the administrator account. &lt;br&gt;&lt;br&gt; **Minimum-length (Windows):** 8 characters &lt;br&gt;&lt;br&gt; **Minimum-length (Linux):** 6 characters &lt;br&gt;&lt;br&gt; **Max-length (Windows):** 123 characters &lt;br&gt;&lt;br&gt; **Max-length (Linux):** 72 characters &lt;br&gt;&lt;br&gt; **Complexity requirements:** 3 out of 4 conditions below need to be fulfilled &lt;br&gt; Has lower characters &lt;br&gt;Has upper characters &lt;br&gt; Has a digit &lt;br&gt; Has a special character (Regex match [\W_]) &lt;br&gt;&lt;br&gt; **Disallowed values:** "abc@123", "P@$$w0rd", "P@ssw0rd", "P@ssword123", "Pa$$word", "pass@word1", "Password!", "Password1", "Password22", "iloveyou!" &lt;br&gt;&lt;br&gt; For resetting the password, see [How to reset the Remote Desktop service or its login password in a Windows VM](https://docs.microsoft.com/troubleshoot/azure/virtual-machines/reset-rdp) &lt;br&gt;&lt;br&gt; For resetting root password, see [Manage users, SSH, and check or repair disks on Azure Linux VMs using the VMAccess Extension](https://docs.microsoft.com/troubleshoot/azure/virtual-machines/troubleshoot-ssh-connection) Optional.
        </ResponseField>
        <ResponseField name="admin_username" type="string">
          Specifies the name of the administrator account. &lt;br&gt;&lt;br&gt; This property cannot be updated after the VM is created. &lt;br&gt;&lt;br&gt; **Windows-only restriction:** Cannot end in "." &lt;br&gt;&lt;br&gt; **Disallowed values:** "administrator", "admin", "user", "user1", "test", "user2", "test1", "user3", "admin1", "1", "123", "a", "actuser", "adm", "admin2", "aspnet", "backup", "console", "david", "guest", "john", "owner", "root", "server", "sql", "support", "support_388945a0", "sys", "test2", "test3", "user4", "user5". &lt;br&gt;&lt;br&gt; **Minimum-length (Linux):** 1 character &lt;br&gt;&lt;br&gt; **Max-length (Linux):** 64 characters &lt;br&gt;&lt;br&gt; **Max-length (Windows):** 20 characters. Optional.
        </ResponseField>
        <ResponseField name="allow_extension_operations" type="bool">
          Specifies whether extension operations should be allowed on the virtual machine. This may only be set to False when no extensions are present on the virtual machine. Optional.
        </ResponseField>
        <ResponseField name="computer_name" type="string">
          Specifies the host OS name of the virtual machine. This name cannot be updated after the VM is created. **Max-length (Windows):** 15 characters. **Max-length (Linux):** 64 characters. For naming conventions and restrictions see [Azure infrastructure services implementation guidelines](https://docs.microsoft.com/azure/azure-resource-manager/management/resource-name-rules). Optional.
        </ResponseField>
        <ResponseField name="custom_data" type="string">
          Specifies a base-64 encoded string of custom data. The base-64 encoded string is decoded to a binary array that is saved as a file on the Virtual Machine. The maximum length of the binary array is 65535 bytes. **Note: Do not pass any secrets or passwords in customData property.** This property cannot be updated after the VM is created. The property 'customData' is passed to the VM to be saved as a file, for more information see [Custom Data on Azure VMs](https://azure.microsoft.com/blog/custom-data-and-cloud-init-on-windows-azure/). For using cloud-init for your Linux VM, see [Using cloud-init to customize a Linux VM during creation](https://docs.microsoft.com/azure/virtual-machines/linux/using-cloud-init). Optional.
        </ResponseField>
        <ResponseField name="linux_configuration" type="object">
          Specifies the Linux operating system settings on the virtual machine. For a list of supported Linux distributions, see [Linux on Azure-Endorsed Distributions](https://docs.microsoft.com/azure/virtual-machines/linux/endorsed-distros). Optional.
          <Expandable title="properties">
            <ResponseField name="disable_password_authentication" type="bool">
              Specifies whether password authentication should be disabled. Optional.
            </ResponseField>
            <ResponseField name="enable_vmagent_platform_updates" type="bool">
              Indicates whether VMAgent Platform Updates is enabled for the Linux virtual machine. Default value is false. Optional.
            </ResponseField>
            <ResponseField name="patch_settings" type="object">
              Specifies settings related to VM Guest Patching on Linux. Optional.
              <Expandable title="properties">
                <ResponseField name="assessment_mode" type="string">
                  Specifies the mode of VM Guest Patch Assessment for the IaaS virtual machine.&lt;br /&gt;&lt;br /&gt; Possible values are:&lt;br /&gt;&lt;br /&gt; **ImageDefault** - You control the timing of patch assessments on a virtual machine. &lt;br /&gt;&lt;br /&gt; **AutomaticByPlatform** - The platform will trigger periodic patch assessments. The property provisionVMAgent must be true. Optional.
                </ResponseField>
                <ResponseField name="automatic_by_platform_settings" type="object">
                  Specifies additional settings to be applied when patch mode AutomaticByPlatform is selected in Linux patch settings. Optional.
                  <Expandable title="properties">
                    <ResponseField name="bypass_platform_safety_checks_on_user_schedule" type="bool">
                      Enables customer to schedule patching without accidental upgrades Optional.
                    </ResponseField>
                    <ResponseField name="reboot_setting" type="string">
                      Specifies the reboot setting for all AutomaticByPlatform patch installation operations. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="patch_mode" type="string">
                  Specifies the mode of VM Guest Patching to IaaS virtual machine or virtual machines associated to virtual machine scale set with OrchestrationMode as Flexible.&lt;br /&gt;&lt;br /&gt; Possible values are:&lt;br /&gt;&lt;br /&gt; **ImageDefault** - The virtual machine's default patching configuration is used. &lt;br /&gt;&lt;br /&gt; **AutomaticByPlatform** - The virtual machine will be automatically updated by the platform. The property provisionVMAgent must be true Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="provision_vmagent" type="bool">
              Indicates whether virtual machine agent should be provisioned on the virtual machine. When this property is not specified in the request body, default behavior is to set it to true. This will ensure that VM Agent is installed on the VM so that extensions can be added to the VM later. Optional.
            </ResponseField>
            <ResponseField name="ssh" type="object">
              SSH configuration for Linux based VMs running on Azure Optional.
              <Expandable title="properties">
                <ResponseField name="public_keys" type="list(object)">
                  The list of SSH public keys used to authenticate with linux based VMs. Optional.
                  <Expandable title="properties">
                    <ResponseField name="key_data" type="string">
                      The SSH public key data, typically the contents of a public key file in PEM format, used for authenticating to the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="path" type="string">
                      The full path on the Linux VM where the SSH public key is placed, typically ending in .ssh/authorized_keys for a specific user. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="require_guest_provision_signal" type="bool">
          Optional property which must either be set to True or omitted. Optional.
        </ResponseField>
        <ResponseField name="secrets" type="list(object)">
          Specifies set of certificates that should be installed onto the virtual machine. To install certificates on a virtual machine it is recommended to use the [Azure Key Vault virtual machine extension for Linux](https://docs.microsoft.com/azure/virtual-machines/extensions/key-vault-linux) or the [Azure Key Vault virtual machine extension for Windows](https://docs.microsoft.com/azure/virtual-machines/extensions/key-vault-windows). Optional.
          <Expandable title="properties">
            <ResponseField name="source_vault" type="object">
              The Azure Key Vault that contains the certificates to be installed. Each secret in the OS profile must specify a source vault, and the certificates are referenced by their URLs within that vault. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The Azure resource ID of the Key Vault that contains the secrets. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="vault_certificates" type="list(object)">
              A list of Key Vault certificates to be installed on the virtual machine. Each item references a certificate URL in Azure Key Vault. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="certificate_store" type="string">
                  The name of the certificate store on the Windows virtual machine where the certificate is added, such as 'My' or 'Root'. This is used when installing certificates from Azure Key Vault. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="certificate_url" type="string">
                  The URL of the certificate in the Azure Key Vault, including the certificate version. This URL identifies the certificate to install on the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="windows_configuration" type="object">
          Specifies Windows operating system settings on the virtual machine. Optional.
          <Expandable title="properties">
            <ResponseField name="additional_unattend_content" type="list(object)">
              Specifies additional base-64 encoded XML formatted information that can be included in the Unattend.xml file, which is used by Windows Setup. Optional.
              <Expandable title="properties">
                <ResponseField name="component_name" type="string">
                  The name of the Windows component to configure during unattended installation. The only allowed value is 'Microsoft-Windows-Shell-Setup'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="content" type="string">
                  Specifies the XML formatted content that is added to the unattend.xml file for the Windows configuration. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="pass_name" type="string">
                  The name of the Windows unattend pass for the additional unattended content. The allowed value is 'OobeSystem', which specifies the Out-of-Box Experience pass. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="setting_name" type="string">
                  Specifies the name of the additional Unattended Content setting. Allowed values: AutoLogon, FirstLogonCommands. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="enable_automatic_updates" type="bool">
              Indicates whether Automatic Updates is enabled for the Windows virtual machine. Default value is true. For virtual machine scale sets, this property can be updated and updates will take effect on OS reprovisioning. Optional.
            </ResponseField>
            <ResponseField name="enable_vmagent_platform_updates" type="bool">
              Indicates whether VMAgent Platform Updates are enabled for the Windows Virtual Machine. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="patch_settings" type="object">
              Specifies settings related to VM Guest Patching on Windows. Optional.
              <Expandable title="properties">
                <ResponseField name="assessment_mode" type="string">
                  Specifies the mode of VM Guest patch assessment for the IaaS virtual machine.&lt;br /&gt;&lt;br /&gt; Possible values are:&lt;br /&gt;&lt;br /&gt; **ImageDefault** - You control the timing of patch assessments on a virtual machine.&lt;br /&gt;&lt;br /&gt; **AutomaticByPlatform** - The platform will trigger periodic patch assessments. The property provisionVMAgent must be true. Optional.
                </ResponseField>
                <ResponseField name="automatic_by_platform_settings" type="object">
                  Specifies additional settings to be applied when patch mode AutomaticByPlatform is selected in Windows patch settings. Optional.
                  <Expandable title="properties">
                    <ResponseField name="bypass_platform_safety_checks_on_user_schedule" type="bool">
                      Enables customer to schedule patching without accidental upgrades Optional.
                    </ResponseField>
                    <ResponseField name="reboot_setting" type="string">
                      Specifies the reboot setting for all AutomaticByPlatform patch installation operations. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="enable_hotpatching" type="bool">
                  Enables customers to patch their Azure VMs without requiring a reboot. For enableHotpatching, the 'provisionVMAgent' must be set to true and 'patchMode' must be set to 'AutomaticByPlatform'. Optional.
                </ResponseField>
                <ResponseField name="patch_mode" type="string">
                  Specifies the mode of VM Guest Patching to IaaS virtual machine or virtual machines associated to virtual machine scale set with OrchestrationMode as Flexible.&lt;br /&gt;&lt;br /&gt; Possible values are:&lt;br /&gt;&lt;br /&gt; **Manual** - You control the application of patches to a virtual machine. You do this by applying patches manually inside the VM. In this mode, automatic updates are disabled; the property WindowsConfiguration.enableAutomaticUpdates must be false&lt;br /&gt;&lt;br /&gt; **AutomaticByOS** - The virtual machine will automatically be updated by the OS. The property WindowsConfiguration.enableAutomaticUpdates must be true. &lt;br /&gt;&lt;br /&gt; **AutomaticByPlatform** - the virtual machine will automatically updated by the platform. The properties provisionVMAgent and WindowsConfiguration.enableAutomaticUpdates must be true Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="provision_vmagent" type="bool">
              Indicates whether virtual machine agent should be provisioned on the virtual machine. When this property is not specified in the request body, it is set to true by default. This will ensure that VM Agent is installed on the VM so that extensions can be added to the VM later. Optional.
            </ResponseField>
            <ResponseField name="time_zone" type="string">
              Specifies the time zone of the virtual machine. e.g. "Pacific Standard Time". Possible values can be [TimeZoneInfo.Id](https://docs.microsoft.com/dotnet/api/system.timezoneinfo.id?#System_TimeZoneInfo_Id) value from time zones returned by [TimeZoneInfo.GetSystemTimeZones](https://docs.microsoft.com/dotnet/api/system.timezoneinfo.getsystemtimezones). Optional.
            </ResponseField>
            <ResponseField name="win_rm" type="object">
              Describes Windows Remote Management configuration of the VM Optional.
              <Expandable title="properties">
                <ResponseField name="listeners" type="list(object)">
                  The list of Windows Remote Management listeners Optional.
                  <Expandable title="properties">
                    <ResponseField name="certificate_url" type="string">
                      The URL of the certificate in Azure Key Vault used for the HTTPS WinRM listener on the virtual machine. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="protocol" type="string">
                      Specifies the protocol used by the WinRM listener. Allowed values are 'Http' and 'Https'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protection_policy" type="object">
      The protection policy of a virtual machine scale set VM. Optional.
      <Expandable title="properties">
        <ResponseField name="protect_from_scale_in" type="bool">
          Indicates that the virtual machine scale set VM shouldn't be considered for deletion during a scale-in operation. Optional.
        </ResponseField>
        <ResponseField name="protect_from_scale_set_actions" type="bool">
          Indicates that model updates or actions (including scale-in) initiated on the virtual machine scale set should not be applied to the virtual machine scale set VM. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="resilient_vmdeletion_status" type="string">
      Specifies the resilient VM deletion status for the virtual machine. Optional.
    </ResponseField>
    <ResponseField name="security_profile" type="object">
      Specifies the Security profile settings for the virtual machine or virtual machine scale set. Optional.
      <Expandable title="properties">
        <ResponseField name="encryption_at_host" type="bool">
          This property can be used by user in the request to enable or disable the Host Encryption for the virtual machine or virtual machine scale set. This will enable the encryption for all the disks including Resource/Temp disk at host itself. The default behavior is: The Encryption at host will be disabled unless this property is set to true for the resource. Optional.
        </ResponseField>
        <ResponseField name="encryption_identity" type="object">
          Specifies the Managed Identity used by ADE to get access token for keyvault operations. Optional.
          <Expandable title="properties">
            <ResponseField name="user_assigned_identity_resource_id" type="string">
              Specifies ARM Resource ID of one of the user identities associated with the VM. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="proxy_agent_settings" type="object">
          Specifies ProxyAgent settings for the virtual machine or virtual machine scale set. Minimum api-version: 2023-09-01. Optional.
          <Expandable title="properties">
            <ResponseField name="add_proxy_agent_extension" type="bool">
              Specify whether to implicitly install the ProxyAgent Extension. This option is currently applicable only for Linux Os. Optional.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Specifies whether ProxyAgent feature should be enabled on the virtual machine or virtual machine scale set. Optional.
            </ResponseField>
            <ResponseField name="imds" type="object">
              Specifies particular host endpoint settings. Optional.
              <Expandable title="properties">
                <ResponseField name="in_vmaccess_control_profile_reference_id" type="string">
                  Specifies the InVMAccessControlProfileVersion resource id in the format of /subscriptions/&#123;SubscriptionId&#125;/resourceGroups/&#123;ResourceGroupName&#125;/providers/Microsoft.Compute/galleries/&#123;galleryName&#125;/inVMAccessControlProfiles/&#123;profile&#125;/versions/&#123;version&#125; Optional.
                </ResponseField>
                <ResponseField name="mode" type="string">
                  Specifies the execution mode. In Audit mode, the system acts as if it is enforcing the access control policy, including emitting access denial entries in the logs but it does not actually deny any requests to host endpoints. In Enforce mode, the system will enforce the access control and it is the recommended mode of operation. Optional.
                </ResponseField>
                <ResponseField name="use_local_file_rules" type="bool">
                  When set to true, instructs the GuestProxyAgent inside the VM to load additional access control rules defined in a local file on the VM. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_incarnation_id" type="number">
              Increase the value of this property allows users to reset the key used for securing communication channel between guest and host. Optional.
            </ResponseField>
            <ResponseField name="mode" type="string">
              Specifies the mode that ProxyAgent will execute on if the feature is enabled. ProxyAgent will start to audit or monitor but not enforce access control over requests to host endpoints in Audit mode, while in Enforce mode it will enforce access control. The default value is Enforce mode. Optional.
            </ResponseField>
            <ResponseField name="wire_server" type="object">
              Specifies particular host endpoint settings. Optional.
              <Expandable title="properties">
                <ResponseField name="in_vmaccess_control_profile_reference_id" type="string">
                  Specifies the InVMAccessControlProfileVersion resource id in the format of /subscriptions/&#123;SubscriptionId&#125;/resourceGroups/&#123;ResourceGroupName&#125;/providers/Microsoft.Compute/galleries/&#123;galleryName&#125;/inVMAccessControlProfiles/&#123;profile&#125;/versions/&#123;version&#125; Optional.
                </ResponseField>
                <ResponseField name="mode" type="string">
                  Specifies the execution mode. In Audit mode, the system acts as if it is enforcing the access control policy, including emitting access denial entries in the logs but it does not actually deny any requests to host endpoints. In Enforce mode, the system will enforce the access control and it is the recommended mode of operation. Optional.
                </ResponseField>
                <ResponseField name="use_local_file_rules" type="bool">
                  When set to true, instructs the GuestProxyAgent inside the VM to load additional access control rules defined in a local file on the VM. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="security_type" type="string">
          Specifies the VM securityType; UefiSettings are enabled only when set to TrustedLaunch or ConfidentialVM, and returns a Standard value starting API version 2025-11-01. Optional.
        </ResponseField>
        <ResponseField name="uefi_settings" type="object">
          Specifies the security settings like secure boot and vTPM used while creating the virtual machine. Minimum api-version: 2020-12-01. Optional.
          <Expandable title="properties">
            <ResponseField name="secure_boot_enabled" type="bool">
              Specifies whether secure boot should be enabled on the virtual machine. Minimum api-version: 2020-12-01. Optional.
            </ResponseField>
            <ResponseField name="v_tpm_enabled" type="bool">
              Specifies whether vTPM should be enabled on the virtual machine. Minimum api-version: 2020-12-01. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage_profile" type="object">
      Specifies the storage settings for the virtual machine disks. Optional.
      <Expandable title="properties">
        <ResponseField name="align_regional_disks_to_vmzone" type="bool">
          Specifies whether the regional disks should be aligned/moved to the VM zone. This is applicable only for VMs with placement property set. Please note that this change is irreversible. Minimum api-version: 2024-11-01. Optional.
        </ResponseField>
        <ResponseField name="data_disks" type="list(object)">
          Specifies the parameters that are used to add a data disk to a virtual machine. For more information about disks, see [About disks and VHDs for Azure virtual machines](https://docs.microsoft.com/azure/virtual-machines/managed-disks-overview). Optional.
          <Expandable title="properties">
            <ResponseField name="caching" type="string">
              Specifies the caching requirements for the data disk. Possible values are None, ReadOnly, and ReadWrite. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="create_option" type="string">
              Specifies how the data disk should be created. Allowed values are `FromImage` (use the data disk from the image), `Empty` (create a new blank disk), `Attach` (attach an existing managed disk), `Copy` (create the disk by copying from an existing disk or snapshot), and `Restore` (create the disk from a restore point). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="delete_option" type="string">
              Specifies the behavior of the data disk when the VM is deleted. When set to 'Delete', the disk is deleted along with the VM; when set to 'Detach', the disk is detached and retained. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="detach_option" type="string">
              Specifies the detach option for the data disk. The only supported value is 'ForceDetach', which allows detaching the disk even if it is currently in use or has pending operations. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_iopsread_write" type="number">
              The number of I/O operations per second (IOPS) that the disk can support for both read and write operations. This is configurable for certain disk types, such as Ultra disks. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_mbps_read_write" type="number">
              The maximum read/write throughput in megabytes per second (MB/s) that the data disk can sustain. This value is used to define the performance tier of the disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="disk_size_gb" type="number">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="image" type="object">
              Specifies the image reference for the data disk, containing the URI to a VHD in a storage account. Used when the disk is created from an image (createOption = 'FromImage'). Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="uri" type="string">
                  The URI of the virtual hard disk (VHD) blob used as the image for the data disk. This applies to unmanaged disks, where the image is referenced by its blob storage URI. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="lun" type="number">
              The Logical Unit Number (LUN) assigned to the data disk. This value uniquely identifies the disk within the virtual machine and is used by the operating system to address the disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="managed_disk" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <ResponseField name="additional_disk_properties" type="object">
                  Optional; if omitted, computed by Azure.
                  <Expandable title="properties">
                    <ResponseField name="managed_disk_properties" type="object">
                      Optional; if omitted, computed by Azure.
                      <Expandable title="properties">
                        <ResponseField name="availability_policy" type="object">
                          Optional; if omitted, computed by Azure.
                          <Expandable title="properties">
                            <ResponseField name="action_on_disk_delay" type="string">
                              The action to take when a disk delay is detected. Allowed values are 'None' (no action) and 'AutomaticReattach' (automatically reattach the disk). Optional; if omitted, computed by Azure. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="bursting_enabled" type="bool">
                          Whether performance bursting is enabled for the managed disk, allowing it to exceed its provisioned performance limits temporarily. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="disk_access_id" type="string">
                          Specifies the Azure resource ID of the DiskAccess resource to associate with the managed disk for enabling private endpoint access to the disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="disk_iopsread_only" type="number">
                          The provisioned read-only IOPS limit for the managed disk, expressed in operations per second. This value applies to scenarios where the disk is used in a read-only or shared configuration. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="disk_mbps_read_only" type="number">
                          The maximum read throughput, in MB/s, that the managed disk can support when mounted in read-only mode. This performance limit applies to certain disk types (e.g., Ultra disks or Premium SSD v2) within the VMSS VM's data disk configuration. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="logical_sector_size" type="number">
                          Optional; if omitted, computed by Azure.
                        </ResponseField>
                        <ResponseField name="max_shares" type="number">
                          The maximum number of VMs that can share the disk. The value must be at least 1. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="network_access_policy" type="string">
                          The network access policy for the managed data disk. Allowed values are 'AllowAll' (allow all network access), 'AllowPrivate' (allow only private network access), and 'DenyAll' (deny all network access). Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="optimized_for_frequent_attach" type="bool">
                          Boolean flag indicating whether the managed disk is optimized for frequent attach/detach operations. When true, the disk is configured to reduce the time for attaching and detaching, which is useful for scenarios like cluster failover. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="performance_plus" type="bool">
                          Specifies whether to enable Azure Managed Disk Performance Plus, which provides higher IOPS and throughput limits on the disk for an additional cost. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="tier" type="string">
                          Specifies the performance tier of the managed disk (e.g., P10, E10). Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="disk_encryption_set" type="object">
                  Specifies the customer-managed disk encryption set to use for encrypting this managed data disk. Provide the resource ID of the disk encryption set in the 'id' property. Optional; if omitted, computed by Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="id" type="string">
                      The resource ID of the disk encryption set associated with the managed disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_profile" type="object">
                  The security profile of the managed data disk, used to configure disk-level security options such as Trusted Launch or confidential VM disk encryption. Optional; if omitted, computed by Azure. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="disk_encryption_set" type="object">
                      The disk encryption set used to encrypt this managed disk at rest using customer-managed keys. Optional; if omitted, computed by Azure. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          The resource ID of the disk encryption set used to encrypt the managed disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="security_encryption_type" type="string">
                      Specifies the security encryption type for the managed disk. Allowed values: VMGuestStateOnly (encrypts only the VM guest state), DiskWithVMGuestState (encrypts both the disk and the VM guest state), NonPersistedTPM (uses a non-persisted TPM). Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="storage_account_type" type="string">
                  Specifies the storage account type for the managed disk. Allowed values are Standard_LRS, Premium_LRS, StandardSSD_LRS, UltraSSD_LRS, Premium_ZRS, StandardSSD_ZRS, and PremiumV2_LRS. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="source_resource" type="object">
              Optional; if omitted, computed by Azure.
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The Azure resource ID of the source resource used to create the data disk, such as a managed disk, snapshot, or image. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_fault_domain_alignment" type="string">
              Specifies the fault domain alignment mode for the data disk. Possible values are 'Aligned' (disk is placed in the same fault domain as the VM) and 'BestEffortAligned' (alignment is attempted but not guaranteed). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="to_be_detached" type="bool">
              Specifies whether the data disk should be detached from the virtual machine instead of being deleted when the VM is deleted. Set to true to detach the disk, false to delete it with the VM. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="vhd" type="object">
              The virtual hard disk (VHD) settings for the data disk, typically containing the URI of the VHD blob that backs the disk when using unmanaged storage. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="uri" type="string">
                  The URI of the virtual hard disk (VHD) in Azure Storage, specifying the blob location for the data disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="write_accelerator_enabled" type="bool">
              Specifies whether write accelerator is enabled for this data disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disk_api_version" type="string">
          Specifies the Disk API version used when applying additionalDiskProperties to managed disks. The value must be in the format YYYY-MM-DD. Optional.
        </ResponseField>
        <ResponseField name="disk_controller_type" type="string">
          Specifies the disk controller type configured for the VM and VirtualMachineScaleSet. This property is only supported for virtual machines whose operating system disk and VM sku supports Generation 2 (https://docs.microsoft.com/en-us/azure/virtual-machines/generation-2), please check the HyperVGenerations capability returned as part of VM sku capabilities in the response of Microsoft.Compute SKUs api for the region contains V2 (https://docs.microsoft.com/rest/api/compute/resourceskus/list). For more information about Disk Controller Types supported please refer to https://aka.ms/azure-diskcontrollertypes. Optional.
        </ResponseField>
        <ResponseField name="image_reference" type="object">
          Specifies information about the image to use. You can specify information about platform images, marketplace images, or virtual machine images. This element is required when you want to use a platform image, marketplace image, or virtual machine image, but is not used in other creation operations. NOTE: Image reference publisher and offer can only be set when you create the scale set. Optional.
          <Expandable title="properties">
            <ResponseField name="community_gallery_image_id" type="string">
              Specified the community gallery image unique id for vm deployment. This can be fetched from community gallery image GET call. Optional.
            </ResponseField>
            <ResponseField name="exact_version" type="string">
              Specifies in decimal numbers, the version of platform image or marketplace image used to create the virtual machine. This readonly field differs from 'version', only if the value specified in 'version' field is 'latest'. Computed by Azure after `ship`.
            </ResponseField>
            <ResponseField name="offer" type="string">
              Specifies the offer of the platform image or marketplace image used to create the virtual machine. Optional.
            </ResponseField>
            <ResponseField name="publisher" type="string">
              The image publisher. Optional.
            </ResponseField>
            <ResponseField name="shared_gallery_image_id" type="string">
              Specified the shared gallery image unique id for vm deployment. This can be fetched from shared gallery image GET call. Optional.
            </ResponseField>
            <ResponseField name="sku" type="string">
              The image SKU. Optional.
            </ResponseField>
            <ResponseField name="version" type="string">
              Specifies the version of the platform image or marketplace image used to create the virtual machine. The allowed formats are Major.Minor.Build or 'latest'. Major, Minor, and Build are decimal numbers. Specify 'latest' to use the latest version of an image available at deploy time. Even if you use 'latest', the VM image will not automatically update after deploy time even if a new version becomes available. Please do not use field 'version' for gallery image deployment, gallery image should always use 'id' field for deployment, to use 'latest' version of gallery image, just set '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.Compute/galleries/&#123;galleryName&#125;/images/&#123;imageName&#125;' in the 'id' field without version input. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="os_disk" type="object">
          Specifies information about the operating system disk used by the virtual machine. For more information about disks, see [About disks and VHDs for Azure virtual machines](https://docs.microsoft.com/azure/virtual-machines/managed-disks-overview). Optional.
          <Expandable title="properties">
            <ResponseField name="caching" type="string">
              Specifies the caching requirements. Possible values are: **None,** **ReadOnly,** **ReadWrite.** The default values are: **None for Standard storage. ReadOnly for Premium storage** Optional.
            </ResponseField>
            <ResponseField name="create_option" type="string" required>
              Specifies how the virtual machine disk should be created. Possible values are **Attach:** This value is used when you are using a specialized disk to create the virtual machine. **FromImage:** This value is used when you are using an image to create the virtual machine. If you are using a platform image, you should also use the imageReference element described above. If you are using a marketplace image, you should also use the plan element previously described. **Empty:** This value is used when creating an empty data disk. **Copy:** This value is used to create a data disk from a snapshot or another disk. **Restore:** This value is used to create a data disk from a disk restore point. Required.
            </ResponseField>
            <ResponseField name="delete_option" type="string">
              Specifies the behavior of the managed disk when the VM gets deleted, for example whether the managed disk is deleted or detached. Supported values are: **Delete.** If this value is used, the managed disk is deleted when VM gets deleted. **Detach.** If this value is used, the managed disk is retained after VM gets deleted. Minimum api-version: 2021-03-01. Optional.
            </ResponseField>
            <ResponseField name="diff_disk_settings" type="object">
              Describes the parameters of ephemeral disk settings that can be specified for operating system disk. **Note:** The ephemeral disk settings can only be specified for managed disk. Optional.
              <Expandable title="properties">
                <ResponseField name="enable_full_caching" type="bool">
                  Specifies whether or not to enable full caching for this VM which will cache the OS disk locally on the host and make this VM more resilient to storage outages Optional.
                </ResponseField>
                <ResponseField name="option" type="string">
                  Specifies the ephemeral disk option for operating system disk. Optional.
                </ResponseField>
                <ResponseField name="placement" type="string">
                  Specifies the ephemeral disk placement for operating system disk. This property can be used by user in the request to choose the location i.e, cache disk, resource disk or nvme disk space for Ephemeral OS disk provisioning. For more information on Ephemeral OS disk size requirements, please refer Ephemeral OS disk size requirements for Windows VM at https://docs.microsoft.com/azure/virtual-machines/windows/ephemeral-os-disks#size-requirements and Linux VM at https://docs.microsoft.com/azure/virtual-machines/linux/ephemeral-os-disks#size-requirements. Minimum api-version for NvmeDisk: 2024-03-01. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="disk_size_gb" type="number">
              Specifies the size of an empty data disk in gigabytes. This element can be used to overwrite the size of the disk in a virtual machine image. The property 'diskSizeGB' is the number of bytes x 1024^3 for the disk and the value cannot be larger than 1023. Optional.
            </ResponseField>
            <ResponseField name="encryption_settings" type="object">
              Describes a Encryption Settings for a Disk Optional.
              <Expandable title="properties">
                <ResponseField name="disk_encryption_key" type="object">
                  Describes a reference to Key Vault Secret Optional.
                  <Expandable title="properties">
                    <ResponseField name="secret_url" type="string" required>
                      The URL referencing a secret in a Key Vault. Required.
                    </ResponseField>
                    <ResponseField name="source_vault" type="object" required>
                      A reference to the Azure Key Vault that stores the disk encryption key secret used for encrypting the OS disk. Required. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          Resource Id Optional.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="enabled" type="bool">
                  Specifies whether disk encryption should be enabled on the virtual machine. Optional.
                </ResponseField>
                <ResponseField name="key_encryption_key" type="object">
                  Describes a reference to Key Vault Key Optional.
                  <Expandable title="properties">
                    <ResponseField name="key_url" type="string" required>
                      The URL referencing a key encryption key in Key Vault. Required.
                    </ResponseField>
                    <ResponseField name="source_vault" type="object" required>
                      The Key Vault that contains the key encryption key used for disk encryption. Required. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          Resource Id Optional.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="object">
              Describes the uri of a disk. Optional.
              <Expandable title="properties">
                <ResponseField name="uri" type="string">
                  Specifies the virtual hard disk's uri. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="managed_disk" type="object">
              The parameters of a managed disk. Optional.
              <Expandable title="properties">
                <ResponseField name="additional_disk_properties" type="object">
                  Specifies additional properties for a managed disk that can be set at the time of implicit creation of the disk. Optional.
                  <Expandable title="properties">
                    <ResponseField name="managed_disk_properties" type="object">
                      Specifies the properties of a managed disk that can be set at the time of implicit creation of the disk. Optional.
                      <Expandable title="properties">
                        <ResponseField name="availability_policy" type="object">
                          In the case of an availability or connectivity issue with the disk, specify the behavior of your VM. Optional.
                          <Expandable title="properties">
                            <ResponseField name="action_on_disk_delay" type="string">
                              Determines how to handle disks with slow I/O. Optional.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="bursting_enabled" type="bool">
                          Set to true to enable bursting beyond the provisioned performance target of the disk. Bursting is disabled by default. Does not apply to Ultra disks. Optional.
                        </ResponseField>
                        <ResponseField name="disk_access_id" type="string">
                          Azure resource Id of the DiskAccess resource for using private endpoints on disks. Optional.
                        </ResponseField>
                        <ResponseField name="disk_iopsread_only" type="number">
                          The total number of IOPS that will be allowed across all VMs mounting the shared disk as ReadOnly. One operation can transfer between 4k and 256k bytes. Optional.
                        </ResponseField>
                        <ResponseField name="disk_mbps_read_only" type="number">
                          The total throughput (MBps) that will be allowed across all VMs mounting the shared disk as ReadOnly. MBps means millions of bytes per second - MB here uses the ISO notation, of powers of 10. Optional.
                        </ResponseField>
                        <ResponseField name="logical_sector_size" type="number">
                          Logical sector size in bytes for Ultra Disks. Supported values are 512 and 4096. 4096 is the default. Optional.
                        </ResponseField>
                        <ResponseField name="max_shares" type="number">
                          The maximum number of VMs that can attach to the disk at the same time. Value greater than one indicates a disk that can be mounted on multiple VMs at the same time. Applies to data disks only. Optional.
                        </ResponseField>
                        <ResponseField name="network_access_policy" type="string">
                          Policy for accessing the disk via network. Optional.
                        </ResponseField>
                        <ResponseField name="optimized_for_frequent_attach" type="bool">
                          Setting this property to true improves reliability and performance of data disks that are frequently (more than 5 times a day) detached from one virtual machine and attached to another. This property should not be set for disks that are not detached and attached frequently as it causes the disks to not align with the fault domain of the virtual machine. Optional.
                        </ResponseField>
                        <ResponseField name="performance_plus" type="bool">
                          Set this flag to true to get a boost on the performance target of the disk deployed. This flag can only be set on disk creation time and cannot be disabled after enabled. Optional.
                        </ResponseField>
                        <ResponseField name="tier" type="string">
                          Performance tier of the disk (e.g., P4, S10) as described here: https://azure.microsoft.com/en-us/pricing/details/managed-disks/. Does not apply to Ultra disks. Optional.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="disk_encryption_set" type="object">
                  Describes the parameter of customer managed disk encryption set resource id that can be specified for disk. **Note:** The disk encryption set resource id can only be specified for managed disk. Please refer https://aka.ms/mdssewithcmkoverview for more details. Optional.
                  <Expandable title="properties">
                    <ResponseField name="id" type="string">
                      The resource ID of the Azure Disk Encryption Set used to encrypt the OS disk of the scale set VM. Optional; if omitted, computed by Azure. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="security_profile" type="object">
                  Specifies the security profile settings for the managed disk. **Note:** It can only be set for Confidential VMs. Optional.
                  <Expandable title="properties">
                    <ResponseField name="disk_encryption_set" type="object">
                      Describes the parameter of customer managed disk encryption set resource id that can be specified for disk. **Note:** The disk encryption set resource id can only be specified for managed disk. Please refer https://aka.ms/mdssewithcmkoverview for more details. Optional.
                      <Expandable title="properties">
                        <ResponseField name="id" type="string">
                          The Azure resource ID of the disk encryption set used to encrypt the managed OS disk. Optional; if omitted, computed by Azure. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="security_encryption_type" type="string">
                      Specifies the EncryptionType of the managed disk. It is set to DiskWithVMGuestState for encryption of the managed disk along with VMGuestState blob, VMGuestStateOnly for encryption of just the VMGuestState blob, and NonPersistedTPM for not persisting firmware state in the VMGuestState blob.. **Note:** It can be set for only Confidential VMs. Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="storage_account_type" type="string">
                  Specifies the storage account type for the managed disk. Managed OS disk storage account type can only be set when you create the scale set. NOTE: UltraSSD_LRS can only be used with data disks. It cannot be used with OS Disk. Standard_LRS uses Standard HDD. StandardSSD_LRS uses Standard SSD. Premium_LRS uses Premium SSD. UltraSSD_LRS uses Ultra disk. Premium_ZRS uses Premium SSD zone redundant storage. StandardSSD_ZRS uses Standard SSD zone redundant storage. For more information regarding disks supported for Windows Virtual Machines, refer to https://docs.microsoft.com/azure/virtual-machines/windows/disks-types and, for Linux Virtual Machines, refer to https://docs.microsoft.com/azure/virtual-machines/linux/disks-types Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              The disk name. Optional.
            </ResponseField>
            <ResponseField name="os_type" type="string">
              This property allows you to specify the type of the OS that is included in the disk if creating a VM from user-image or a specialized VHD. Possible values are: **Windows,** **Linux.** Optional.
            </ResponseField>
            <ResponseField name="storage_fault_domain_alignment" type="string">
              Specifies the storage fault domain alignment type for the disk. Optional.
            </ResponseField>
            <ResponseField name="vhd" type="object">
              Describes the uri of a disk. Optional.
              <Expandable title="properties">
                <ResponseField name="uri" type="string">
                  Specifies the virtual hard disk's uri. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="write_accelerator_enabled" type="bool">
              Specifies whether writeAccelerator should be enabled or disabled on the disk. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="time_created" type="string">
      Specifies the time at which the Virtual Machine resource was created. Minimum api-version: 2021-11-01. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="user_data" type="string">
      UserData for the VM, which must be base-64 encoded. Customer should not pass any secrets in here. Minimum api-version: 2021-03-01 Optional.
    </ResponseField>
    <ResponseField name="virtual_machine_resource_id" type="string">
      Specifies the ARM resource ID of the standalone virtual machine associated with this VMSS VM. This property is only applicable to Virtual Machine Scale Sets with Flexible orchestration mode. Minimum api-version: 2025-11-01. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="vm_id" type="string">
      Azure VM unique ID. Computed by Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The virtual machine child extension resources. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="properties" type="object">
      Optional; if omitted, computed by Azure.
      <Expandable title="properties">
        <ResponseField name="auto_upgrade_minor_version" type="bool">
          Specifies whether the virtual machine automatically upgrades to the latest minor version of the operating system image when it becomes available. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enable_automatic_upgrade" type="bool">
          Enable or disable automatic upgrades for this virtual machine scale set VM instance. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="force_update_tag" type="string">
          A user-specified tag value that, when changed, forces the virtual machine instance to be updated or reimaged even if it already has the latest scale set model. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_view" type="object">
          Contains the runtime state and status of the virtual machine instance, including health statuses, disk and extension statuses, as viewed by Azure. Optional; if omitted, computed by Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="statuses" type="list(object)">
              A list of statuses for the VM instance, such as provisioning and power states, with each status including details like code, level, display status, and time. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The machine-readable status code for this status, such as 'ProvisioningState/succeeded' or 'PowerState/running'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  A human-readable status of the virtual machine scale set instance, such as 'Provisioning succeeded' or 'Running'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The severity level of the status. Possible values are 'Info', 'Warning', and 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message providing additional details about the status of the virtual machine scale set instance. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time at which the status was reported, in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="substatuses" type="list(object)">
              A list of substatuses that provide additional details about the VM's status in the instance view. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="code" type="string">
                  The code of the substatus, representing a specific status or condition reported in the instance view, such as 'ProvisioningState/succeeded'. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="display_status" type="string">
                  The short localized label describing the substatus. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="level" type="string">
                  The status level of the substatus, one of: Info, Warning, Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="message" type="string">
                  A human-readable message providing additional details about this substatus. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="string">
                  The time at which the substatus was reported, typically in ISO 8601 format. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="type" type="string">
              Optional; if omitted, computed by Azure.
            </ResponseField>
            <ResponseField name="type_handler_version" type="string">
              The version of the extension handler that is running or provisioned for this virtual machine extension. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="protected_settings" type="any">
          The protected settings for the VM extension, containing sensitive configuration that is encrypted and not visible in plaintext. These settings are transmitted securely to the VM. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protected_settings_from_key_vault" type="object">
          Specifies a reference to an Azure Key Vault secret that contains the protected settings for the extension. This allows sensitive configuration data to be stored securely in Key Vault rather than being exposed in the template. Optional; if omitted, computed by Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="secret_url" type="string">
              The URI of the secret in Azure Key Vault that holds the protected settings for the extension. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="source_vault" type="object">
              References the Azure Key Vault that contains the protected setting secret. In the ARM schema, this object includes the 'id' of the Key Vault. Optional; if omitted, computed by Azure. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="id" type="string">
                  The Azure Resource Manager (ARM) resource ID of the Key Vault that contains the secret referenced by the protected settings. For example: /subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.KeyVault/vaults/&#123;vaultName&#125;. Optional; if omitted, computed by Azure. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="provision_after_extensions" type="list(string)">
          Specifies the names of extensions that must be provisioned before this extension. This extension will only be provisioned after all referenced extensions have been successfully provisioned. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioning_state" type="string">
          The current provisioning state of the virtual machine scale set instance, indicating whether the VM has been successfully provisioned or is still in progress. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="publisher" type="string">
          The publisher of the VM extension, such as Microsoft.Compute or Microsoft.Azure.Extensions. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="settings" type="any">
          The configuration settings for the virtual machine scale set VM extension. This dynamic JSON object contains extension-specific properties, and its structure and valid values depend on the extension publisher and type. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="suppress_failures" type="bool">
          Indicates whether failures reported by the extension should be suppressed. When true, an extension failure does not mark the virtual machine as failed; when false or omitted, the failure affects the VM's provisioning state. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the resource or extension defined in the properties block, e.g., 'CustomScriptExtension' for a VM extension. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type_handler_version" type="string">
          Specifies the version of the extension handler that is used by the VM extension. Optional; if omitted, computed by Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  Describes a virtual machine scale set sku. NOTE: If the new VM SKU is not supported on the hardware the scale set is currently on, you need to deallocate the VMs in the scale set before you modify the SKU name. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="capacity" type="number">
      Specifies the number of virtual machines in the scale set. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The sku name. Optional.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Specifies the tier of virtual machines in a scale set.&lt;br /&gt;&lt;br /&gt; Possible Values:&lt;br /&gt;&lt;br /&gt; **Standard**&lt;br /&gt;&lt;br /&gt; **Basic** Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zones" type="list(string)">
  The virtual machine zones. Optional; if omitted, computed by Azure.
</ResponseField>
