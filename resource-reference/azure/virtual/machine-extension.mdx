---
title: "azurerm_virtual_machine_extension"
description: "Real, generated bindings for `azurerm_virtual_machine_extension`."
---

`azurerm_virtual_machine_extension` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own machine extension"})

    		ubx.Resource(virtual.MachineExtension, "ci-runner", virtual.MachineExtensionConfig{
    			Name:                      "ci-runner",
    			Publisher:                 "example",
    			Type:                      "example",
    			TypeHandlerVersion:        "example",
    			VirtualMachineId:          "example",
    			AutoUpgradeMinorVersion:   true,
    			AutomaticUpgradeEnabled:   true,
    			FailureSuppressionEnabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineExtension } from "jsr:@ubx/sdk-azure/azurerm/virtual/machine_extension";

    export default stack("payments", () => {
      intent({ summary: "payments own machine extension" });

      resource(MachineExtension, "ci-runner", {
        name: "ci-runner",
        publisher: "example",
        type: "example",
        typeHandlerVersion: "example",
        virtualMachineId: "example",
        autoUpgradeMinorVersion: true,
        automaticUpgradeEnabled: true,
        failureSuppressionEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import MachineExtension, MachineExtensionConfig

    def describe():
        ubx.intent("payments own machine extension")

        ubx.resource(MachineExtension, "ci-runner", MachineExtensionConfig(
            name="ci-runner",
            publisher="example",
            type="example",
            type_handler_version="example",
            virtual_machine_id="example",
            auto_upgrade_minor_version=True,
            automatic_upgrade_enabled=True,
            failure_suppression_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_machine_extension called "ci-runner", with
    publisher example, type example, type handler version example,
    virtual machine id example, auto upgrade minor version true,
    automatic upgrade enabled true, failure suppression enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_upgrade_minor_version" type="bool">
  Optional.
</ResponseField>

<ResponseField name="automatic_upgrade_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="failure_suppression_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protected_settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="protected_settings_from_key_vault" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="secret_url" type="string" required>
    </ResponseField>
    <ResponseField name="source_vault_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="provision_after_extensions" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="publisher" type="string" required>
  Required.
</ResponseField>

<ResponseField name="settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type_handler_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="virtual_machine_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="protected_settings_from_key_vault" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="secret_url" type="string" required>
    </ResponseField>
    <ResponseField name="source_vault_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
