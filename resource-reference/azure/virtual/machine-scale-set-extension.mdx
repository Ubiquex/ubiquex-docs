---
title: "azure_virtual_machine_scale_set_extension"
description: "`azure_virtual_machine_scale_set_extension` applies the same real VM extension mechanism (post-deployment configuration and automation) to every..."
---

`azure_virtual_machine_scale_set_extension` applies the same real VM extension mechanism (post-deployment configuration and automation) to every instance in a scale set at once, defined a single time at the scale-set level instead of being installed on each VM instance individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own machine scale set extension"})

    		ubx.Resource(virtual.MachineScaleSetExtension, "example", virtual.MachineScaleSetExtensionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineScaleSetExtension } from "@ubx/sdk-azure/azure/virtual/machine_scale_set_extension";

    export default stack("example", () => {
      intent({ summary: "example own machine scale set extension" });

      resource(MachineScaleSetExtension, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.virtual.machine_scale_set_extension import MachineScaleSetExtension, MachineScaleSetExtensionConfig

    def describe():
        ubx.intent("example own machine scale set extension")

        ubx.resource(MachineScaleSetExtension, "example", MachineScaleSetExtensionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_virtual_machine_scale_set_extension called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="name" type="string">
  Resource name Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Describes the properties of a Virtual Machine Scale Set Extension. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="auto_upgrade_minor_version" type="bool">
      Indicates whether the extension should use a newer minor version if one is available at deployment time. Once deployed, however, the extension will not upgrade minor versions unless redeployed, even with this property set to true. Optional.
    </ResponseField>
    <ResponseField name="enable_automatic_upgrade" type="bool">
      Indicates whether the extension should be automatically upgraded by the platform if there is a newer version of the extension available. Optional.
    </ResponseField>
    <ResponseField name="force_update_tag" type="string">
      If a value is provided and is different from the previous value, the extension handler will be forced to update even if the extension configuration has not changed. Optional.
    </ResponseField>
    <ResponseField name="protected_settings" type="any">
      The extension can contain either protectedSettings or protectedSettingsFromKeyVault or no protected settings at all. Optional.
    </ResponseField>
    <ResponseField name="protected_settings_from_key_vault" type="object">
      Describes a reference to Key Vault Secret Optional.
      <Expandable title="properties">
        <ResponseField name="secret_url" type="string" required>
          The URL referencing a secret in a Key Vault. Required.
        </ResponseField>
        <ResponseField name="source_vault" type="object" required>
          Specifies the Key Vault resource that contains the secret for the protected settings. The object typically includes the Azure resource ID of the Key Vault. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource Id Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provision_after_extensions" type="list(string)">
      Collection of extension names after which this extension needs to be provisioned. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="publisher" type="string">
      The name of the extension handler publisher. Optional.
    </ResponseField>
    <ResponseField name="settings" type="any">
      Json formatted public settings for the extension. Optional.
    </ResponseField>
    <ResponseField name="suppress_failures" type="bool">
      Indicates whether failures stemming from the extension will be suppressed (Operational failures such as not connecting to the VM will not be suppressed regardless of this value). The default is false. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
    </ResponseField>
    <ResponseField name="type_handler_version" type="string">
      Specifies the Major.Minor version of the script handler. Customer is able to specify only the Major.Minor version of an extension, Azure platform will deliver the latest Patch.Hotfix version in the Major.Minor series. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Resource type Optional; if omitted, computed by Azure.
</ResponseField>
