---
title: "azurerm_virtual_machine_restore_point"
description: "Real, generated bindings for `azurerm_virtual_machine_restore_point`."
---

`azurerm_virtual_machine_restore_point` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own machine restore point"})

    		ubx.Resource(virtual.MachineRestorePoint, "ci-runner", virtual.MachineRestorePointConfig{
    			Name:                                   "ci-runner",
    			VirtualMachineRestorePointCollectionId: "example",
    			CrashConsistencyModeEnabled:            true,
    			ExcludedDisks:                          []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineRestorePoint } from "jsr:@ubx/sdk-azure/azurerm/virtual/machine_restore_point";

    export default stack("payments", () => {
      intent({ summary: "payments own machine restore point" });

      resource(MachineRestorePoint, "ci-runner", {
        name: "ci-runner",
        virtualMachineRestorePointCollectionId: "example",
        crashConsistencyModeEnabled: true,
        excludedDisks: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import MachineRestorePoint, MachineRestorePointConfig

    def describe():
        ubx.intent("payments own machine restore point")

        ubx.resource(MachineRestorePoint, "ci-runner", MachineRestorePointConfig(
            name="ci-runner",
            virtual_machine_restore_point_collection_id="example",
            crash_consistency_mode_enabled=True,
            excluded_disks=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_machine_restore_point called "ci-runner",
    with virtual machine restore point collection id example, crash
    consistency mode enabled true, excluded disks a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crash_consistency_mode_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="excluded_disks" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_machine_restore_point_collection_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
