---
title: "azurerm_virtual_hub"
description: "Real, generated bindings for `azurerm_virtual_hub`."
---

`azurerm_virtual_hub` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub"})

    		ubx.Resource(virtual.Hub, "ci-runner", virtual.HubConfig{
    			Location:                     "example",
    			Name:                         "ci-runner",
    			ResourceGroupName:            "example-resource-group-name",
    			AddressPrefix:                "example",
    			BranchToBranchTrafficEnabled: true,
    			HubRoutingPreference:         "example",
    			Sku:                          "example",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Hub } from "jsr:@ubx/sdk-azure/azurerm/virtual/hub";

    export default stack("payments", () => {
      intent({ summary: "payments own hub" });

      resource(Hub, "ci-runner", {
        location: "example",
        name: "ci-runner",
        resourceGroupName: "example-resource-group-name",
        addressPrefix: "example",
        branchToBranchTrafficEnabled: true,
        hubRoutingPreference: "example",
        sku: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import Hub, HubConfig

    def describe():
        ubx.intent("payments own hub")

        ubx.resource(Hub, "ci-runner", HubConfig(
            location="example",
            name="ci-runner",
            resource_group_name="example-resource-group-name",
            address_prefix="example",
            branch_to_branch_traffic_enabled=True,
            hub_routing_preference="example",
            sku="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_hub called "ci-runner", with location
    example, resource group name example-resource-group-name, address
    prefix example, branch to branch traffic enabled true, hub
    routing preference example, sku example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="branch_to_branch_traffic_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="hub_routing_preference" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="address_prefixes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="next_hop_ip_address" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_router_auto_scale_min_capacity" type="number">
  Optional.
</ResponseField>

<ResponseField name="virtual_wan_id" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="default_route_table_id" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="address_prefixes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="next_hop_ip_address" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_router_asn" type="number">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="virtual_router_ips" type="list(string)">
  Computed by Azure after `ship`.
</ResponseField>
