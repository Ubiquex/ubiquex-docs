---
title: "azurerm_virtual_network_dns_servers"
description: "Real, generated bindings for `azurerm_virtual_network_dns_servers`."
---

`azurerm_virtual_network_dns_servers` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network dns servers"})

    		ubx.Resource(virtual.NetworkDnsServers, "ci-runner", virtual.NetworkDnsServersConfig{
    			VirtualNetworkId: "example",
    			DnsServers:       []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkDnsServers } from "jsr:@ubx/sdk-azure/azurerm/virtual/network_dns_servers";

    export default stack("payments", () => {
      intent({ summary: "payments own network dns servers" });

      resource(NetworkDnsServers, "ci-runner", {
        virtualNetworkId: "example",
        dnsServers: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import NetworkDnsServers, NetworkDnsServersConfig

    def describe():
        ubx.intent("payments own network dns servers")

        ubx.resource(NetworkDnsServers, "ci-runner", NetworkDnsServersConfig(
            virtual_network_id="example",
            dns_servers=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_network_dns_servers, with virtual network
    id example, dns servers a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dns_servers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_network_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
