---
title: "azure_virtual_machine_scale_set_vmextension"
description: "`azure_virtual_machine_scale_set_vmextension` installs a VM extension on one specific instance within a scale set..."
---

`azure_virtual_machine_scale_set_vmextension` installs a VM extension on one specific instance within a scale set, rather than on every instance the way the scale-set-level extension resource does -- the per-instance counterpart for cases that don't need a uniform, set-wide rollout.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own machine scale set vmextension"})

    		ubx.Resource(virtual.MachineScaleSetVmextension, "example", virtual.MachineScaleSetVmextensionConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MachineScaleSetVmextension } from "@ubx/sdk-azure/azure/virtual/machine_scale_set_vmextension";

    export default stack("example", () => {
      intent({ summary: "example own machine scale set vmextension" });

      resource(MachineScaleSetVmextension, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.virtual import MachineScaleSetVmextension, MachineScaleSetVmextensionConfig

    def describe():
        ubx.intent("example own machine scale set vmextension")

        ubx.resource(MachineScaleSetVmextension, "example", MachineScaleSetVmextensionConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_virtual_machine_scale_set_vmextension called
    "example", with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="location" type="string">
  The location of the extension. Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string">
  Resource name Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  Describes the properties of a Virtual Machine Extension. Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="auto_upgrade_minor_version" type="bool">
      Indicates whether the extension should use a newer minor version if one is available at deployment time. Once deployed, however, the extension will not upgrade minor versions unless redeployed, even with this property set to true. Optional.
    </ResponseField>
    <ResponseField name="enable_automatic_upgrade" type="bool">
      Indicates whether the extension should be automatically upgraded by the platform if there is a newer version of the extension available. Optional.
    </ResponseField>
    <ResponseField name="force_update_tag" type="string">
      How the extension handler should be forced to update even if the extension configuration has not changed. Optional.
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      The instance view of a virtual machine extension. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The virtual machine extension name. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The resource status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The machine-readable status code for the extension instance view, such as 'ProvisioningState/succeeded' or 'ProvisioningState/failed'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              The short, human-readable label for the status. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The level of the status message, indicating severity. Possible values are Info, Warning, and Error. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the status. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp when the status was recorded, in ISO 8601 format (e.g., 2024-01-01T12:00:00Z). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="substatuses" type="list(object)">
          The resource status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code for this substatus, providing a machine-readable identifier for the specific condition (for example, 'ProvisioningState/succeeded' or 'ProvisioningState/failed'). Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              The short, localizable label for the extension substatus, providing a human-readable summary of the state. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of the substatus. Allowed values are 'Info', 'Warning', or 'Error'. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable message providing additional details about the substatus, complementing the code and level fields. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              The timestamp (in ISO 8601 format) indicating when the substatus was last updated or reported. Optional; if omitted, computed by Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
        </ResponseField>
        <ResponseField name="type_handler_version" type="string">
          Specifies the Major.Minor.Patch.Hotfix version of the script handler. Azure platform will deliver the latest Patch.Hotfix version in the Major.Minor series. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protected_settings" type="any">
      The extension can contain either protectedSettings or protectedSettingsFromKeyVault or no protected settings at all. Optional.
    </ResponseField>
    <ResponseField name="protected_settings_from_key_vault" type="object">
      Describes a reference to Key Vault Secret Optional.
      <Expandable title="properties">
        <ResponseField name="secret_url" type="string" required>
          The URL referencing a secret in a Key Vault. Required.
        </ResponseField>
        <ResponseField name="source_vault" type="object" required>
          Specifies the Azure Key Vault from which the secret for the protected settings is retrieved. This is required when using protected settings from a Key Vault. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              Resource Id Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provision_after_extensions" type="list(string)">
      Collection of extension names after which this extension needs to be provisioned. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Azure after `ship`.
    </ResponseField>
    <ResponseField name="publisher" type="string">
      The name of the extension handler publisher. Optional.
    </ResponseField>
    <ResponseField name="settings" type="any">
      Json formatted public settings for the extension. Optional.
    </ResponseField>
    <ResponseField name="suppress_failures" type="bool">
      Indicates whether failures stemming from the extension will be suppressed (Operational failures such as not connecting to the VM will not be suppressed regardless of this value). The default is false. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
    </ResponseField>
    <ResponseField name="type_handler_version" type="string">
      Specifies the Major.Minor version of the script handler. Customer is able to specify only the Major.Minor version of an extension, Azure platform will deliver the latest Patch.Hotfix version in the Major.Minor series. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Resource type Optional; if omitted, computed by Azure.
</ResponseField>
