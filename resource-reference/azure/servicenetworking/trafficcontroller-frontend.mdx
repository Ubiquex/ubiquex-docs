---
title: "azure_servicenetworking_trafficcontroller_frontend"
description: "`azure_servicenetworking_trafficcontroller_frontend` is a public IP-bearing entry point on an Azure Application Load Balancer that client traffic..."
---

`azure_servicenetworking_trafficcontroller_frontend` is a public IP-bearing entry point on an Azure Application Load Balancer that client traffic connects to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicenetworking "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/servicenetworking"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own trafficcontroller frontend"})

    		ubx.Resource(servicenetworking.TrafficcontrollerFrontend, "example", servicenetworking.TrafficcontrollerFrontendConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficcontrollerFrontend } from "@ubx/sdk-azure/azure/servicenetworking/trafficcontroller_frontend";

    export default stack("example", () => {
      intent({ summary: "example own trafficcontroller frontend" });

      resource(TrafficcontrollerFrontend, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.servicenetworking import TrafficcontrollerFrontend, TrafficcontrollerFrontendConfig

    def describe():
        ubx.intent("example own trafficcontroller frontend")

        ubx.resource(TrafficcontrollerFrontend, "example", TrafficcontrollerFrontendConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_servicenetworking_trafficcontroller_frontend, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Frontend Properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="fqdn" type="string">
      The Fully Qualified Domain Name of the DNS record associated to a Traffic Controller frontend. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Resource Provisioning State Enum Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
