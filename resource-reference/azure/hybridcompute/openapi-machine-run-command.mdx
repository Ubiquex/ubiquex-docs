---
title: "azure_hybridcompute_openapi_machine_run_command"
description: "`azure_hybridcompute_openapi_machine_run_command` runs a one-time script on an Arc-enabled server via the Azure Arc connected machine agent..."
---

`azure_hybridcompute_openapi_machine_run_command` runs a one-time script on an Arc-enabled server via the Azure Arc connected machine agent, without needing direct network access or a pre-installed remote-execution tool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_hybridcompute_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_hybridcompute_openapi/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_hybridcompute_openapi/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi machine run command"})

    		ubx.Resource(hybridcompute.OpenapiMachineRunCommand, "ci-runner", hybridcompute.OpenapiMachineRunCommandConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_hybridcompute_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiMachineRunCommand } from "./local-sdk/azure_hybridcompute_openapi/sdk/typescript/azure_hybridcompute_openapi/hybridcompute/openapi_machine_run_command.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi machine run command" });

      resource(OpenapiMachineRunCommand, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_hybridcompute_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_hybridcompute_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_hybridcompute_openapi.hybridcompute import OpenapiMachineRunCommand, OpenapiMachineRunCommandConfig

    def describe():
        ubx.intent("payments own openapi machine run command")

        ubx.resource(OpenapiMachineRunCommand, "ci-runner", OpenapiMachineRunCommandConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_hybridcompute_openapi_machine_run_command, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of a run command. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="async_execution" type="bool">
      Optional. If set to true, provisioning will complete as soon as script starts and will not wait for script to complete. Optional.
    </ResponseField>
    <ResponseField name="error_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="error_blob_uri" type="string">
      Specifies the Azure storage blob where script error stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer errorBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      The instance view of a machine run command. Optional.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          Script end time. Optional.
        </ResponseField>
        <ResponseField name="error" type="string">
          Script error stream. Optional.
        </ResponseField>
        <ResponseField name="execution_message" type="string">
          Communicate script configuration errors or execution messages. Optional.
        </ResponseField>
        <ResponseField name="execution_state" type="string">
          Script execution status. Optional.
        </ResponseField>
        <ResponseField name="exit_code" type="number">
          Exit code returned from script execution. Optional.
        </ResponseField>
        <ResponseField name="output" type="string">
          Script output stream. Optional.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Script start time. Optional.
        </ResponseField>
        <ResponseField name="statuses" type="list(object)">
          The status information. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              A machine-readable status or error code. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_status" type="string">
              A human-readable summary of this status. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="level" type="string">
              The severity level of this status, e.g. `Info`, `Warning`, or `Error`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="message" type="string">
              A human-readable description of this result. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time" type="string">
              When this status was last reported. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_managed_identity" type="object">
      Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
        </ResponseField>
        <ResponseField name="object_id" type="string">
          Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_blob_uri" type="string">
      Specifies the Azure storage blob where script output stream will be uploaded. Use a SAS URI with read, append, create, write access OR use managed identity to provide the VM access to the blob. Refer outputBlobManagedIdentity parameter. Optional.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          This parameter's own value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protected_parameters" type="list(object)">
      The parameters used by the script. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The resource's own name, unique within its parent scope. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          This parameter's own value. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="run_as_password" type="string">
      Specifies the user account password on the machine when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="run_as_user" type="string">
      Specifies the user account on the machine when executing the run command. Optional.
    </ResponseField>
    <ResponseField name="source" type="object">
      Describes the script sources for run command. Use only one of script, scriptUri, commandId. Optional.
      <Expandable title="properties">
        <ResponseField name="command_id" type="string">
          Specifies the commandId of predefined built-in script. Optional.
        </ResponseField>
        <ResponseField name="script" type="string">
          Specifies the script content to be executed on the machine. Optional.
        </ResponseField>
        <ResponseField name="script_uri" type="string">
          Specifies the script download location. It can be either SAS URI of an Azure storage blob with read access or public URI. Optional.
        </ResponseField>
        <ResponseField name="script_uri_managed_identity" type="object">
          Contains clientId or objectId (use only one, not both) of a user-assigned managed identity that has access to storage blob used in Run Command. Use an empty RunCommandManagedIdentity object in case of system-assigned identity. Make sure the Azure storage blob exists in case of scriptUri, and managed identity has been given access to blob's container with 'Storage Blob Data Reader' role assignment with scriptUri blob and 'Storage Blob Data Contributor' for Append blobs(outputBlobUri, errorBlobUri). In case of user assigned identity, make sure you add it under VM's identity. For more info on managed identity and Run Command, refer https://aka.ms/ManagedIdentity and https://aka.ms/RunCommandManaged. Optional.
          <Expandable title="properties">
            <ResponseField name="client_id" type="string">
              Client Id (GUID value) of the user-assigned managed identity. ObjectId should not be used if this is provided. Optional.
            </ResponseField>
            <ResponseField name="object_id" type="string">
              Object Id (GUID value) of the user-assigned managed identity. ClientId should not be used if this is provided. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeout_in_seconds" type="number">
      The timeout in seconds to execute the run command. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
