---
title: "azure_hybridcompute_openapi_gateway"
description: "`azure_hybridcompute_openapi_gateway` is an Azure Arc gateway..."
---

`azure_hybridcompute_openapi_gateway` is an Azure Arc gateway, a regional proxy endpoint Arc-enabled servers can route their own outbound Azure connectivity through, reducing the set of external endpoints each server needs direct network access to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_hybridcompute_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_hybridcompute_openapi/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_hybridcompute_openapi/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi gateway"})

    		ubx.Resource(hybridcompute.OpenapiGateway, "ci-runner", hybridcompute.OpenapiGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_hybridcompute_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiGateway } from "./local-sdk/azure_hybridcompute_openapi/sdk/typescript/azure_hybridcompute_openapi/hybridcompute/openapi_gateway.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi gateway" });

      resource(OpenapiGateway, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_hybridcompute_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_hybridcompute_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_hybridcompute_openapi.hybridcompute import OpenapiGateway, OpenapiGatewayConfig

    def describe():
        ubx.intent("payments own openapi gateway")

        ubx.resource(OpenapiGateway, "ci-runner", OpenapiGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_hybridcompute_openapi_gateway, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of a Gateway Profile. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allowed_features" type="list(string)">
      Specifies the list of features that are enabled for this Gateway. Optional.
    </ResponseField>
    <ResponseField name="gateway_bypass" type="list(string)">
      Specifies the list of domain names that should bypass the gateway. Each entry must be a valid DNS hostname. Optional.
    </ResponseField>
    <ResponseField name="gateway_endpoint" type="string">
      The endpoint fqdn for the Gateway. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="gateway_id" type="string">
      A unique, immutable, identifier for the Gateway. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="gateway_type" type="string">
      The type of the Gateway resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
