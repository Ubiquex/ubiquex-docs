---
title: "azure_hybridcompute_openapi_gateway"
description: "`azure_hybridcompute_openapi_gateway` is an Azure Arc gateway..."
---

`azure_hybridcompute_openapi_gateway` is an Azure Arc gateway, a regional proxy endpoint Arc-enabled servers can route their own outbound Azure connectivity through, reducing the set of external endpoints each server needs direct network access to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi gateway"})

    		ubx.Resource(hybridcompute.OpenapiGateway, "example", hybridcompute.OpenapiGatewayConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiGateway } from "@ubx/sdk-azure/azure/hybridcompute/openapi_gateway";

    export default stack("example", () => {
      intent({ summary: "example own openapi gateway" });

      resource(OpenapiGateway, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.hybridcompute import OpenapiGateway, OpenapiGatewayConfig

    def describe():
        ubx.intent("example own openapi gateway")

        ubx.resource(OpenapiGateway, "example", OpenapiGatewayConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_hybridcompute_openapi_gateway, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of a Gateway Profile. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="allowed_features" type="list(string)">
      Specifies the list of features that are enabled for this Gateway. Optional.
    </ResponseField>
    <ResponseField name="gateway_bypass" type="list(string)">
      Specifies the list of domain names that should bypass the gateway. Each entry must be a valid DNS hostname. Optional.
    </ResponseField>
    <ResponseField name="gateway_endpoint" type="string">
      The endpoint fqdn for the Gateway. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="gateway_id" type="string">
      A unique, immutable, identifier for the Gateway. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="gateway_type" type="string">
      The type of the Gateway resource. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
