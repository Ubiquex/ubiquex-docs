---
title: "azure_hybridcompute_openapi_machine_extension"
description: "`azure_hybridcompute_openapi_machine_extension` is a VM extension installed on an Arc-enabled server..."
---

`azure_hybridcompute_openapi_machine_extension` is a VM extension installed on an Arc-enabled server, extending it with capabilities like monitoring agents, configuration management, or security tooling -- the same extension model Azure VMs themselves use.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_hybridcompute_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_hybridcompute_openapi/sdk/go
    package main

    import (
    	hybridcompute "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_hybridcompute_openapi/hybridcompute"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi machine extension"})

    		ubx.Resource(hybridcompute.OpenapiMachineExtension, "ci-runner", hybridcompute.OpenapiMachineExtensionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_hybridcompute_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiMachineExtension } from "./local-sdk/azure_hybridcompute_openapi/sdk/typescript/azure_hybridcompute_openapi/hybridcompute/openapi_machine_extension.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi machine extension" });

      resource(OpenapiMachineExtension, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_hybridcompute_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_hybridcompute_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_hybridcompute_openapi.hybridcompute import OpenapiMachineExtension, OpenapiMachineExtensionConfig

    def describe():
        ubx.intent("payments own openapi machine extension")

        ubx.resource(OpenapiMachineExtension, "ci-runner", OpenapiMachineExtensionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_hybridcompute_openapi_machine_extension, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Describes the properties of a Machine Extension. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="auto_upgrade_minor_version" type="bool">
      Indicates whether the extension should use a newer minor version if one is available at deployment time. Once deployed, however, the extension will not upgrade minor versions unless redeployed, even with this property set to true. Optional.
    </ResponseField>
    <ResponseField name="enable_automatic_upgrade" type="bool">
      Indicates whether the extension should be automatically upgraded by the platform if there is a newer version available. Optional.
    </ResponseField>
    <ResponseField name="force_update_tag" type="string">
      How the extension handler should be forced to update even if the extension configuration has not changed. Optional.
    </ResponseField>
    <ResponseField name="instance_view" type="object">
      Describes the Machine Extension Instance View. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The machine extension name. Optional.
        </ResponseField>
        <ResponseField name="status" type="object">
          Instance view status. Optional.
          <Expandable title="properties">
            <ResponseField name="code" type="string">
              The status code. Optional.
            </ResponseField>
            <ResponseField name="display_status" type="string">
              The short localizable label for the status. Optional.
            </ResponseField>
            <ResponseField name="level" type="string">
              The level code. Optional.
            </ResponseField>
            <ResponseField name="message" type="string">
              The detailed status message, including for alerts and error messages. Optional.
            </ResponseField>
            <ResponseField name="time" type="string">
              The time of the status. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
        </ResponseField>
        <ResponseField name="type_handler_version" type="string">
          Specifies the version of the script handler. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protected_settings" type="map(any)">
      The extension can contain either protectedSettings or protectedSettingsFromKeyVault or no protected settings at all. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      The provisioning state, which only appears in the response. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="publisher" type="string">
      The name of the extension handler publisher. Optional.
    </ResponseField>
    <ResponseField name="settings" type="map(any)">
      Json formatted public settings for the extension. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of the extension; an example is "CustomScriptExtension". Optional.
    </ResponseField>
    <ResponseField name="type_handler_version" type="string">
      Specifies the version of the script handler. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
