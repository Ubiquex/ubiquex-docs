---
title: "azure_loadtestservice_load_test_resource"
description: "`azure_loadtestservice_loadtestservice_load_test_resource` is an Azure Load Testing resource..."
---

`azure_loadtestservice_loadtestservice_load_test_resource` is an Azure Load Testing resource, a fully managed service for generating high-scale load against an application to test its own performance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	loadtestservice "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/loadtestservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own loadtestservice load test resource"})

    		ubx.Resource(loadtestservice.LoadTestResource, "example", loadtestservice.LoadTestResourceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoadTestResource } from "@ubx/sdk-azure/azure/loadtestservice/load_test_resource";

    export default stack("example", () => {
      intent({ summary: "example own loadtestservice load test resource" });

      resource(LoadTestResource, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.loadtestservice import LoadTestResource, LoadTestResourceConfig

    def describe():
        ubx.intent("example own loadtestservice load test resource")

        ubx.resource(LoadTestResource, "example", LoadTestResourceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_loadtestservice_load_test_resource, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity" type="object">
  Managed service identity (system assigned and/or user assigned identities) Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/&#123;subscriptionId&#125;/resourceGroups/&#123;resourceGroupName&#125;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/&#123;identityName&#125;. The dictionary values can be empty objects (&#123;&#125;) in requests. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  LoadTest resource properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="data_plane_uri" type="string">
      Resource data plane URI. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Description of the resource. Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      Key and identity details for Customer Managed Key encryption of load test resource. Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="object">
          All identity configuration for Customer-managed key settings defining which identity should be used to auth to Key Vault. Optional.
          <Expandable title="properties">
            <ResponseField name="resource_id" type="string">
              User assigned identity to use for accessing key encryption key Url. Ex: /subscriptions/a0a0a0a0-bbbb-cccd-dddd-e1e1e1e1e1e1/resourceGroups/&lt;resource group&gt;/providers/Microsoft.ManagedIdentity/userAssignedIdentities/myId. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              Managed identity type to use for accessing encryption key Url. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key_url" type="string">
          key encryption key Url, versioned. Ex: https://contosovault.vault.azure.net/keys/contosokek/562a4bb76b524a1493a6afe8e536ee78 or https://contosovault.vault.azure.net/keys/contosokek. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Resources provisioning states. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
