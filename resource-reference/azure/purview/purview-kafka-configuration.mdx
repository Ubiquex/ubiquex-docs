---
title: "azure_purview_kafka_configuration"
description: "`azure_purview_purview_kafka_configuration` configures the Event Hubs-based Kafka-compatible endpoint(s) a Microsoft Purview account uses to publish..."
---

`azure_purview_purview_kafka_configuration` configures the Event Hubs-based Kafka-compatible endpoint(s) a Microsoft Purview account uses to publish and consume metadata event(s).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_purview_purview --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_purview_purview/sdk/go
    package main

    import (
    	purview "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_purview_purview/purview"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own purview kafka configuration"})

    		ubx.Resource(purview.PurviewKafkaConfiguration, "example", purview.PurviewKafkaConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_purview_purview --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PurviewKafkaConfiguration } from "./local-sdk/azure_purview_purview/sdk/typescript/azure_purview_purview/purview/purview_kafka_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own purview kafka configuration" });

      resource(PurviewKafkaConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_purview_purview --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_purview_purview/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_purview_purview.purview import PurviewKafkaConfiguration, PurviewKafkaConfigurationConfig

    def describe():
        ubx.intent("example own purview kafka configuration")

        ubx.resource(PurviewKafkaConfiguration, "example", PurviewKafkaConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_purview_kafka_configuration, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The kafka configuration properties of the event streaming service attached to the Purview account for kafka notifications. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="consumer_group" type="string">
      Consumer group for hook event hub. Optional.
    </ResponseField>
    <ResponseField name="credentials" type="object">
      Credentials to access the event streaming service attached to the purview account. Optional.
      <Expandable title="properties">
        <ResponseField name="identity_id" type="string">
          Identity identifier for UserAssign type. Optional.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the purview kafka configuration's credentials. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="event_hub_partition_id" type="string">
      Optional partition Id for notification event hub. If not set, all partitions will be leveraged. Optional.
    </ResponseField>
    <ResponseField name="event_hub_resource_id" type="string">
      A reference to the Event Hubs namespace this account's own Kafka-compatible endpoints connect through. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="event_hub_type" type="string">
      The event hub type. Optional.
    </ResponseField>
    <ResponseField name="event_streaming_state" type="string">
      The state of the event streaming service Optional.
    </ResponseField>
    <ResponseField name="event_streaming_type" type="string">
      The event streaming service type Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
