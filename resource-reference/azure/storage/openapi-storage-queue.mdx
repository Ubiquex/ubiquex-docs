---
title: "azure_storage_openapi_storage_queue"
description: "`azure_storage_openapi_storage_queue` is a Queue Storage queue within a storage account, holding message(s) for asynchronous processing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_storage_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_storage_openapi/sdk/go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_storage_openapi/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openapi storage queue"})

    		ubx.Resource(storage.OpenapiStorageQueue, "ci-runner", storage.OpenapiStorageQueueConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_storage_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiStorageQueue } from "./local-sdk/azure_storage_openapi/sdk/typescript/azure_storage_openapi/storage/openapi_storage_queue.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own openapi storage queue" });

      resource(OpenapiStorageQueue, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_storage_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_storage_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_storage_openapi.storage import OpenapiStorageQueue, OpenapiStorageQueueConfig

    def describe():
        ubx.intent("payments own openapi storage queue")

        ubx.resource(OpenapiStorageQueue, "ci-runner", OpenapiStorageQueueConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_storage_openapi_storage_queue, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="approximate_message_count" type="number">
      Integer indicating an approximate number of messages in the queue. This number is not lower than the actual number of messages in the queue, but could be higher. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      A name-value pair that represents queue metadata. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
