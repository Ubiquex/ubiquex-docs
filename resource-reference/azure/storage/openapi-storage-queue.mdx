---
title: "azure_storage_openapi_storage_queue"
description: "`azure_storage_openapi_storage_queue` is a Queue Storage queue within a storage account, holding message(s) for asynchronous processing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi storage queue"})

    		ubx.Resource(storage.OpenapiStorageQueue, "example", storage.OpenapiStorageQueueConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiStorageQueue } from "@ubx/sdk-azure/azure/storage/openapi_storage_queue";

    export default stack("example", () => {
      intent({ summary: "example own openapi storage queue" });

      resource(OpenapiStorageQueue, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.storage.openapi_storage_queue import OpenapiStorageQueue, OpenapiStorageQueueConfig

    def describe():
        ubx.intent("example own openapi storage queue")

        ubx.resource(OpenapiStorageQueue, "example", OpenapiStorageQueueConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_storage_openapi_storage_queue, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The resource type-specific configuration for this resource, wrapping every field that isn't part of ARM's own standard envelope (id/name/type/location/tags). Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="approximate_message_count" type="number">
      Integer indicating an approximate number of messages in the queue. This number is not lower than the actual number of messages in the queue, but could be higher. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      A name-value pair that represents queue metadata. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
