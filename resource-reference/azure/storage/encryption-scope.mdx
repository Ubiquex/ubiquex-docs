---
title: "azurerm_storage_encryption_scope"
description: "Real, generated bindings for `azurerm_storage_encryption_scope`."
---

`azurerm_storage_encryption_scope` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own encryption scope"})

    		ubx.Resource(storage.EncryptionScope, "ci-runner", storage.EncryptionScopeConfig{
    			Name:                             "ci-runner",
    			Source:                           "example",
    			StorageAccountId:                 "example",
    			InfrastructureEncryptionRequired: true,
    			KeyVaultKeyId:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EncryptionScope } from "jsr:@ubx/sdk-azure/azurerm/storage/encryption_scope";

    export default stack("payments", () => {
      intent({ summary: "payments own encryption scope" });

      resource(EncryptionScope, "ci-runner", {
        name: "ci-runner",
        source: "example",
        storageAccountId: "example",
        infrastructureEncryptionRequired: true,
        keyVaultKeyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.storage import EncryptionScope, EncryptionScopeConfig

    def describe():
        ubx.intent("payments own encryption scope")

        ubx.resource(EncryptionScope, "ci-runner", EncryptionScopeConfig(
            name="ci-runner",
            source="example",
            storage_account_id="example",
            infrastructure_encryption_required=True,
            key_vault_key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_storage_encryption_scope called "ci-runner", with
    source example, storage account id example, infrastructure
    encryption required true, key vault key id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="infrastructure_encryption_required" type="bool">
  Optional.
</ResponseField>

<ResponseField name="key_vault_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="storage_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
