---
title: "azure_storage_openapi_file_share"
description: "`azure_storage_openapi_file_share` is an Azure Files share within a storage account, an SMB/NFS-mountable file system."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_storage_openapi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_storage_openapi/sdk/go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_storage_openapi/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi file share"})

    		ubx.Resource(storage.OpenapiFileShare, "example", storage.OpenapiFileShareConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_storage_openapi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiFileShare } from "./local-sdk/azure_storage_openapi/sdk/typescript/azure_storage_openapi/storage/openapi_file_share.ts";

    export default stack("example", () => {
      intent({ summary: "example own openapi file share" });

      resource(OpenapiFileShare, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_storage_openapi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_storage_openapi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_storage_openapi.storage import OpenapiFileShare, OpenapiFileShareConfig

    def describe():
        ubx.intent("example own openapi file share")

        ubx.resource(OpenapiFileShare, "example", OpenapiFileShareConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_storage_openapi_file_share, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  A value used for optimistic concurrency control on this storage openapi file share, changed by the server on every update. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of the file share. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="access_tier" type="string">
      Access tier for specific share. GpV2 account can choose between TransactionOptimized (default), Hot, and Cool. FileStorage account can choose Premium. Optional.
    </ResponseField>
    <ResponseField name="access_tier_change_time" type="string">
      Indicates the last modification time for share access tier. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="access_tier_status" type="string">
      Indicates if there is a pending transition for access tier. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deleted" type="bool">
      Indicates whether the share was deleted. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deleted_time" type="string">
      The deleted time if the share was deleted. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="enabled_protocols" type="string">
      The authentication protocol that is used for the file share. Can only be specified when creating a share. Optional.
    </ResponseField>
    <ResponseField name="file_share_paid_bursting" type="object">
      File Share Paid Bursting properties. Optional.
      <Expandable title="properties">
        <ResponseField name="paid_bursting_enabled" type="bool">
          Indicates whether paid bursting is enabled for the share. This property is only for file shares created under Files Provisioned v1 SSD account type. Optional.
        </ResponseField>
        <ResponseField name="paid_bursting_max_bandwidth_mibps" type="number">
          The maximum paid bursting bandwidth for the share, in mebibytes per second. This property is only for file shares created under Files Provisioned v1 SSD account type. The maximum allowed value is 10340 which is the maximum allowed bandwidth for a share. Optional.
        </ResponseField>
        <ResponseField name="paid_bursting_max_iops" type="number">
          The maximum paid bursting IOPS for the share. This property is only for file shares created under Files Provisioned v1 SSD account type. The maximum allowed value is 102400 which is the maximum allowed IOPS for a share. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="included_burst_iops" type="number">
      The calculated burst IOPS of the share. This property is only for file shares created under Files Provisioned v2 account type. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="last_modified_time" type="string">
      Returns the date and time the share was last modified. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="lease_duration" type="string">
      Specifies whether the lease on a container is of infinite or fixed duration, only when the container is leased. Optional.
    </ResponseField>
    <ResponseField name="lease_state" type="string">
      Lease state of the container. Optional.
    </ResponseField>
    <ResponseField name="lease_status" type="string">
      The lease status of the container. Optional.
    </ResponseField>
    <ResponseField name="max_burst_credits_for_iops" type="number">
      The calculated maximum burst credits for the share. This property is only for file shares created under Files Provisioned v2 account type. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      A name-value pair to associate with the share as metadata. Optional.
    </ResponseField>
    <ResponseField name="next_allowed_provisioned_bandwidth_downgrade_time" type="string">
      Returns the next allowed provisioned bandwidth downgrade time for the share. This property is only for file shares created under Files Provisioned v2 account type. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="next_allowed_provisioned_iops_downgrade_time" type="string">
      Returns the next allowed provisioned IOPS downgrade time for the share. This property is only for file shares created under Files Provisioned v2 account type. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="next_allowed_quota_downgrade_time" type="string">
      Returns the next allowed provisioned storage size downgrade time for the share. This property is only for file shares created under Files Provisioned v1 SSD and Files Provisioned v2 account type Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="provisioned_bandwidth_mibps" type="number">
      The provisioned bandwidth of the share, in mebibytes per second. This property is only for file shares created under Files Provisioned v2 account type. Please refer to the GetFileServiceUsage API response for the minimum and maximum allowed value for provisioned bandwidth. Optional.
    </ResponseField>
    <ResponseField name="provisioned_iops" type="number">
      The provisioned IOPS of the share. This property is only for file shares created under Files Provisioned v2 account type. Please refer to the GetFileServiceUsage API response for the minimum and maximum allowed value for provisioned IOPS. Optional.
    </ResponseField>
    <ResponseField name="remaining_retention_days" type="number">
      Remaining retention days for share that was soft deleted. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="root_squash" type="string">
      The property is for NFS share only. The default is NoRootSquash. Optional.
    </ResponseField>
    <ResponseField name="share_quota" type="number">
      The provisioned size of the share, in gibibytes. Must be greater than 0, and less than or equal to 5TB (5120). For Large File Shares, the maximum size is 102400. For file shares created under Files Provisioned v2 account type, please refer to the GetFileServiceUsage API response for the minimum and maximum allowed provisioned storage size. Optional.
    </ResponseField>
    <ResponseField name="share_usage_bytes" type="number">
      The approximate size of the data stored on the share. Note that this value may not include all recently created or recently resized files. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="signed_identifiers" type="list(object)">
      List of stored access policies specified on the share. Optional.
      <Expandable title="properties">
        <ResponseField name="access_policy" type="object">
          A stored access policy defining shared permissions and validity period for associated shared access signatures. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="expiry_time" type="string">
              When this resource or grant expires. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="permission" type="string">
              The access permission granted, e.g. read or write. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="start_time" type="string">
              When this event or window started. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="snapshot_time" type="string">
      Creation time of share snapshot returned in the response of list shares with expand param "snapshots". Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the share. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
