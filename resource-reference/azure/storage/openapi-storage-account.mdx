---
title: "azure_storage_openapi_storage_account"
description: "`azure_storage_openapi_storage_account` is the top-level Azure Storage resource, providing Blob, File, Queue..."
---

`azure_storage_openapi_storage_account` is the top-level Azure Storage resource, providing Blob, File, Queue, and Table service endpoint(s) under a shared namespace and access policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi storage account"})

    		ubx.Resource(storage.OpenapiStorageAccount, "example", storage.OpenapiStorageAccountConfig{
    			Kind:     "example",
    			Location: "example",
    			Sku:      map[string]any{"name": "example-name"},
    			Tags:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiStorageAccount } from "@ubx/sdk-azure/azure/storage/openapi_storage_account";

    export default stack("example", () => {
      intent({ summary: "example own openapi storage account" });

      resource(OpenapiStorageAccount, "example", {
        kind: "example",
        location: "example",
        sku: { name: "example-name" },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.storage.openapi_storage_account import OpenapiStorageAccount, OpenapiStorageAccountConfig

    def describe():
        ubx.intent("example own openapi storage account")

        ubx.resource(OpenapiStorageAccount, "example", OpenapiStorageAccountConfig(
            kind="example",
            location="example",
            sku={"name": "example-name"},
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_storage_openapi_storage_account, with kind example,
    location example, sku a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="extended_location" type="object">
  The complex type of the extended location. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the extended location. Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of extendedLocation. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity" type="object">
  Identity for the resource. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
      The principal ID of resource identity. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The tenant ID of resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The identity type. Required.
    </ResponseField>
    <ResponseField name="user_assigned_identities" type="map(object)">
      Gets or sets a list of key value pairs that describe the set of User Assigned identities that will be used with this storage account. The key is the ARM resource identifier of the identity. Only 1 User Assigned identity is permitted here. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client (application) ID of the user-assigned managed identity to use. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="principal_id" type="string">
          Output only. The Microsoft Entra ID object ID of this resource's own managed identity or service principal. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kind" type="string" required>
  Indicates the type of storage account. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Required. Gets or sets the location of the resource. This will be one of the supported and registered Azure Geo Regions (e.g. West US, East US, Southeast Asia, etc.). The geo region of a resource cannot be changed once it is created, but if an identical geo region is specified on update, the request will succeed. Required.
</ResponseField>

<ResponseField name="placement" type="object">
  The complex type of the zonal placement details. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="zone_placement_policy" type="string">
      The availability zone pinning policy for the storage account. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="object">
  The parameters used to create the storage account. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="access_tier" type="string">
      The default access tier for block blobs in the storage account. Required for storage accounts where kind = BlobStorage. See more details in: https://learn.microsoft.com/azure/storage/blobs/access-tiers-overview. Optional.
    </ResponseField>
    <ResponseField name="allow_blob_public_access" type="bool">
      Allow or disallow public access to all blobs or containers in the storage account. The default interpretation is false for this property. Optional.
    </ResponseField>
    <ResponseField name="allow_cross_tenant_replication" type="bool">
      Allow or disallow cross AAD tenant object replication. Set this property to true for new or existing accounts only if object replication policies will involve storage accounts in different AAD tenants. The default interpretation is false for new accounts to follow best security practices by default. Optional.
    </ResponseField>
    <ResponseField name="allow_shared_key_access" type="bool">
      Indicates whether the storage account permits requests to be authorized with the account access key via Shared Key. If false, then all requests, including shared access signatures, must be authorized with Azure Active Directory (Azure AD). The default value is null, which is equivalent to true. Optional.
    </ResponseField>
    <ResponseField name="allow_shared_key_access_for_services" type="object">
      Defines shared key access properties for a storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="blob" type="object">
          Defines shared key access settings for an individual storage service. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Indicates whether shared key access is enabled for the service. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="file" type="object">
          Defines shared key access settings for an individual storage service. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Indicates whether shared key access is enabled for the service. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="queue" type="object">
          Defines shared key access settings for an individual storage service. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Indicates whether shared key access is enabled for the service. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="table" type="object">
          Defines shared key access settings for an individual storage service. Optional.
          <Expandable title="properties">
            <ResponseField name="enabled" type="bool">
              Indicates whether shared key access is enabled for the service. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allowed_copy_scope" type="string">
      Restrict copy to and from Storage Accounts within an AAD tenant or with Private Links to the same VNet. Optional.
    </ResponseField>
    <ResponseField name="azure_files_identity_based_authentication" type="object">
      Settings for Azure Files identity based authentication. Optional.
      <Expandable title="properties">
        <ResponseField name="active_directory_properties" type="object">
          Settings properties for Active Directory (AD). Optional.
          <Expandable title="properties">
            <ResponseField name="account_type" type="string">
              Specifies the Active Directory account type for Azure Storage. If directoryServiceOptions is set to AD (AD DS authentication), this property is optional. If provided, samAccountName should also be provided. For directoryServiceOptions AADDS (Entra DS authentication) or AADKERB (Entra authentication), this property can be omitted. Optional.
            </ResponseField>
            <ResponseField name="azure_storage_sid" type="string">
              Specifies the security identifier (SID) for Azure Storage. If directoryServiceOptions is set to AD (AD DS authentication), this property is required. Otherwise, it can be omitted. Optional.
            </ResponseField>
            <ResponseField name="domain_guid" type="string">
              Specifies the domain GUID. If directoryServiceOptions is set to AD (AD DS authentication), this property is required. If directoryServiceOptions is set to AADDS (Entra DS authentication), this property can be omitted. If directoryServiceOptions is set to AADKERB (Entra authentication), this property is optional; it is needed to support configuration of directory- and file-level permissions via Windows File Explorer, but is not required for authentication. Optional.
            </ResponseField>
            <ResponseField name="domain_name" type="string">
              Specifies the primary domain that the AD DNS server is authoritative for. This property is required if directoryServiceOptions is set to AD (AD DS authentication). If directoryServiceOptions is set to AADDS (Entra DS authentication), providing this property is optional, as it will be inferred automatically if omitted. If directoryServiceOptions is set to AADKERB (Entra authentication), this property is optional; it is needed to support configuration of directory- and file-level permissions via Windows File Explorer, but is not required for authentication. Optional.
            </ResponseField>
            <ResponseField name="domain_sid" type="string">
              Specifies the security identifier (SID) of the AD domain. If directoryServiceOptions is set to AD (AD DS authentication), this property is required. Otherwise, it can be omitted. Optional.
            </ResponseField>
            <ResponseField name="forest_name" type="string">
              Specifies the Active Directory forest to get. If directoryServiceOptions is set to AD (AD DS authentication), this property is required. Otherwise, it can be omitted. Optional.
            </ResponseField>
            <ResponseField name="net_bios_domain_name" type="string">
              Specifies the NetBIOS domain name. If directoryServiceOptions is set to AD (AD DS authentication), this property is required. Otherwise, it can be omitted. Optional.
            </ResponseField>
            <ResponseField name="sam_account_name" type="string">
              Specifies the Active Directory SAMAccountName for Azure Storage. If directoryServiceOptions is set to AD (AD DS authentication), this property is optional. If provided, accountType should also be provided. For directoryServiceOptions AADDS (Entra DS authentication) or AADKERB (Entra authentication), this property can be omitted. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_share_permission" type="string">
          Default share permission for users using Kerberos authentication if RBAC role is not assigned. Optional.
        </ResponseField>
        <ResponseField name="directory_service_options" type="string" required>
          Indicates the directory service used. Note that this enum may be extended in the future. Required.
        </ResponseField>
        <ResponseField name="smb_oauth_settings" type="object">
          Setting property for Managed Identity access over SMB using OAuth Optional.
          <Expandable title="properties">
            <ResponseField name="is_smb_oauth_enabled" type="bool">
              Specifies if managed identities can access SMB shares using OAuth. The default interpretation is false for this property. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_domain" type="object">
      The custom domain assigned to this storage account. This can be set via Update. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
          Gets or sets the custom domain name assigned to the storage account. Name is the CNAME source. Required.
        </ResponseField>
        <ResponseField name="use_sub_domain_name" type="bool">
          Indicates whether indirect CName validation is enabled. Default value is false. This should only be set on updates. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_collaboration_policy_properties" type="object">
      Defines Data Collaboration Policy for a storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_cross_tenant_data_sharing" type="bool">
          Indicates whether cross-entra tenant data sharing is allowed on the storage account. Optional.
        </ResponseField>
        <ResponseField name="allow_storage_connectors" type="bool">
          Indicates whether storage connectors are allowed to created or managed on the storage account. Optional.
        </ResponseField>
        <ResponseField name="allow_storage_data_shares" type="bool">
          Indicates whether data shares are allowed to be created or managed on the storage account. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_to_oauth_authentication" type="bool">
      A boolean flag which indicates whether the default authentication is OAuth or not. The default interpretation is false for this property. Optional.
    </ResponseField>
    <ResponseField name="dns_endpoint_type" type="string">
      Allows you to specify the type of endpoint. Set this to AzureDNSZone to create a large number of accounts in a single subscription, which creates accounts in an Azure DNS Zone and the endpoint URL will have an alphanumeric DNS Zone identifier. Optional.
    </ResponseField>
    <ResponseField name="dual_stack_endpoint_preference" type="object">
      Dual-stack endpoint preference defines whether IPv6 endpoints are going to be published. Optional.
      <Expandable title="properties">
        <ResponseField name="publish_ipv6_endpoint" type="bool">
          A boolean flag which indicates whether IPv6 storage endpoints are to be published. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enable_extended_groups" type="bool">
      Enables extended group support with local users feature, if set to true Optional.
    </ResponseField>
    <ResponseField name="encryption" type="object">
      The encryption settings on the storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="identity" type="object">
          Encryption identity for the storage account. Optional.
          <Expandable title="properties">
            <ResponseField name="federated_identity_client_id" type="string">
              ClientId of the multi-tenant application to be used in conjunction with the user-assigned identity for cross-tenant customer-managed-keys server-side encryption on the storage account. Optional.
            </ResponseField>
            <ResponseField name="user_assigned_identity" type="string">
              Resource identifier of the UserAssigned identity to be associated with server-side encryption on the storage account. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key_source" type="string">
          The encryption keySource (provider). Possible values (case-insensitive): Microsoft.Storage, Microsoft.Keyvault Optional.
        </ResponseField>
        <ResponseField name="keyvaultproperties" type="object">
          Properties of key vault. Optional.
          <Expandable title="properties">
            <ResponseField name="current_versioned_key_expiration_timestamp" type="string">
              This is a read only property that represents the expiration time of the current version of the customer managed key used for encryption. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="current_versioned_key_identifier" type="string">
              The object identifier of the current versioned Key Vault Key in use. Computed by Microsoft Azure after `ship`.
            </ResponseField>
            <ResponseField name="keyname" type="string">
              The name of KeyVault key. Optional.
            </ResponseField>
            <ResponseField name="keyvaulturi" type="string">
              The Uri of KeyVault. Optional.
            </ResponseField>
            <ResponseField name="keyversion" type="string">
              The version of KeyVault key. Optional.
            </ResponseField>
            <ResponseField name="last_key_rotation_timestamp" type="string">
              Timestamp of last rotation of the Key Vault Key. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="require_infrastructure_encryption" type="bool">
          A boolean indicating whether or not the service applies a secondary layer of encryption with platform managed keys for data at rest. Optional.
        </ResponseField>
        <ResponseField name="services" type="object">
          A list of services that support encryption. Optional.
          <Expandable title="properties">
            <ResponseField name="blob" type="object">
              A service that allows server-side encryption to be used. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  A boolean indicating whether or not the service encrypts the data as it is stored. Encryption at rest is enabled by default today and cannot be disabled. Optional.
                </ResponseField>
                <ResponseField name="key_type" type="string">
                  Encryption key type to be used for the encryption service. 'Account' key type implies that an account-scoped encryption key will be used. 'Service' key type implies that a default service key is used. Optional.
                </ResponseField>
                <ResponseField name="last_enabled_time" type="string">
                  Gets a rough estimate of the date/time when the encryption was last enabled by the user. Data is encrypted at rest by default today and cannot be disabled. Computed by Microsoft Azure after `ship`.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="file" type="object">
              A service that allows server-side encryption to be used. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  A boolean indicating whether or not the service encrypts the data as it is stored. Encryption at rest is enabled by default today and cannot be disabled. Optional.
                </ResponseField>
                <ResponseField name="key_type" type="string">
                  Encryption key type to be used for the encryption service. 'Account' key type implies that an account-scoped encryption key will be used. 'Service' key type implies that a default service key is used. Optional.
                </ResponseField>
                <ResponseField name="last_enabled_time" type="string">
                  Gets a rough estimate of the date/time when the encryption was last enabled by the user. Data is encrypted at rest by default today and cannot be disabled. Computed by Microsoft Azure after `ship`.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="queue" type="object">
              A service that allows server-side encryption to be used. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  A boolean indicating whether or not the service encrypts the data as it is stored. Encryption at rest is enabled by default today and cannot be disabled. Optional.
                </ResponseField>
                <ResponseField name="key_type" type="string">
                  Encryption key type to be used for the encryption service. 'Account' key type implies that an account-scoped encryption key will be used. 'Service' key type implies that a default service key is used. Optional.
                </ResponseField>
                <ResponseField name="last_enabled_time" type="string">
                  Gets a rough estimate of the date/time when the encryption was last enabled by the user. Data is encrypted at rest by default today and cannot be disabled. Computed by Microsoft Azure after `ship`.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="table" type="object">
              A service that allows server-side encryption to be used. Optional.
              <Expandable title="properties">
                <ResponseField name="enabled" type="bool">
                  A boolean indicating whether or not the service encrypts the data as it is stored. Encryption at rest is enabled by default today and cannot be disabled. Optional.
                </ResponseField>
                <ResponseField name="key_type" type="string">
                  Encryption key type to be used for the encryption service. 'Account' key type implies that an account-scoped encryption key will be used. 'Service' key type implies that a default service key is used. Optional.
                </ResponseField>
                <ResponseField name="last_enabled_time" type="string">
                  Gets a rough estimate of the date/time when the encryption was last enabled by the user. Data is encrypted at rest by default today and cannot be disabled. Computed by Microsoft Azure after `ship`.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="geo_priority_replication_status" type="object">
      Geo Priority Replication enablement status for the storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="is_blob_enabled" type="bool">
          Indicates whether Blob Geo Priority Replication is enabled for the storage account. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="immutable_storage_with_versioning" type="object">
      This property enables and defines account-level immutability. Enabling the feature auto-enables Blob Versioning. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          A boolean flag which enables account-level immutability. All the containers under such an account have object-level immutability enabled by default. Optional.
        </ResponseField>
        <ResponseField name="immutability_policy" type="object">
          This defines account-level immutability policy properties. Optional.
          <Expandable title="properties">
            <ResponseField name="allow_protected_append_writes" type="bool">
              This property can only be changed for disabled and unlocked time-based retention policies. When enabled, new blocks can be written to an append blob while maintaining immutability protection and compliance. Only new blocks can be added and any existing blocks cannot be modified or deleted. Optional.
            </ResponseField>
            <ResponseField name="immutability_period_since_creation_in_days" type="number">
              The immutability period for the blobs in the container since the policy creation, in days. Optional.
            </ResponseField>
            <ResponseField name="state" type="string">
              The ImmutabilityPolicy state defines the mode of the policy. Disabled state disables the policy, Unlocked state allows increase and decrease of immutability retention time and also allows toggling allowProtectedAppendWrites property, Locked state only allows the increase of the immutability retention time. A policy can only be created in a Disabled or Unlocked state and can be toggled between the two states. Only a policy in an Unlocked state can transition to a Locked state which cannot be reverted. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_hns_enabled" type="bool">
      Account HierarchicalNamespace enabled if sets to true. Optional.
    </ResponseField>
    <ResponseField name="is_local_user_enabled" type="bool">
      Enables local users feature, if set to true Optional.
    </ResponseField>
    <ResponseField name="is_nfs_v3_enabled" type="bool">
      NFS 3.0 protocol support enabled if set to true. Optional.
    </ResponseField>
    <ResponseField name="is_sftp_enabled" type="bool">
      Enables Secure File Transfer Protocol, if set to true Optional.
    </ResponseField>
    <ResponseField name="key_policy" type="object">
      KeyPolicy assigned to the storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="key_expiration_period_in_days" type="number" required>
          The key expiration period in days. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="large_file_shares_state" type="string">
      Allow large file shares if sets to Enabled. It cannot be disabled once it is enabled. Optional.
    </ResponseField>
    <ResponseField name="minimum_tls_version" type="string">
      Set the minimum TLS version to be permitted on requests to storage. The default interpretation is TLS 1.0 for this property. Minimum TLS version 1.3 version is not supported. Optional.
    </ResponseField>
    <ResponseField name="network_acls" type="object">
      Network rule set Optional.
      <Expandable title="properties">
        <ResponseField name="bypass" type="string">
          Specifies whether traffic is bypassed for Logging/Metrics/AzureServices. Possible values are any combination of Logging|Metrics|AzureServices (For example, "Logging, Metrics"), or None to bypass none of those traffics. Optional.
        </ResponseField>
        <ResponseField name="default_action" type="string" required>
          Specifies the default action of allow or deny when no other rules match. Required.
        </ResponseField>
        <ResponseField name="ip_rules" type="list(object)">
          Sets the IP ACL rules Optional.
          <Expandable title="properties">
            <ResponseField name="action" type="string">
              The action taken when this rule matches. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipv6_rules" type="list(object)">
          Sets the IPv6 ACL rules. Optional.
          <Expandable title="properties">
            <ResponseField name="action" type="string">
              The action taken when this rule matches. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The literal value of this field. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource_access_rules" type="list(object)">
          Sets the resource access rules Optional.
          <Expandable title="properties">
            <ResponseField name="resource_id" type="string">
              A reference to the resource this applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tenant_id" type="string">
              Output only. The Microsoft Entra ID tenant this resource's own managed identity belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="virtual_network_rules" type="list(object)">
          Sets the virtual network rules Optional.
          <Expandable title="properties">
            <ResponseField name="action" type="string">
              The action taken when this rule matches. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="string">
              The fully qualified Azure Resource Manager resource ID, in the form `/subscriptions/&#123;id&#125;/resourceGroups/&#123;group&#125;/providers/&#123;provider&#125;/.../&#123;name&#125;`. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="state" type="string">
              The current status of this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_network_access" type="string">
      Allow, disallow, or let Network Security Perimeter configuration to evaluate public network access to Storage Account. Value is optional but if passed in, must be 'Enabled', 'Disabled' or 'SecuredByPerimeter'. Optional.
    </ResponseField>
    <ResponseField name="routing_preference" type="object">
      Routing preference defines the type of network, either microsoft or internet routing to be used to deliver the user data, the default option is microsoft routing Optional.
      <Expandable title="properties">
        <ResponseField name="publish_internet_endpoints" type="bool">
          A boolean flag which indicates whether internet routing storage endpoints are to be published Optional.
        </ResponseField>
        <ResponseField name="publish_microsoft_endpoints" type="bool">
          A boolean flag which indicates whether microsoft routing storage endpoints are to be published Optional.
        </ResponseField>
        <ResponseField name="routing_choice" type="string">
          Routing Choice defines the kind of network routing opted by the user. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sas_policy" type="object">
      SasPolicy assigned to the storage account. Optional.
      <Expandable title="properties">
        <ResponseField name="expiration_action" type="string" required>
          The SAS Expiration Action defines the action to be performed when sasPolicy.sasExpirationPeriod is violated. The 'Log' action can be used for audit purposes and the 'Block' action can be used to block and deny the usage of SAS tokens that do not adhere to the sas policy expiration period. Required.
        </ResponseField>
        <ResponseField name="sas_expiration_period" type="string" required>
          The SAS expiration period, DD.HH:MM:SS. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="supports_https_traffic_only" type="bool">
      Allows https traffic only to storage service if sets to true. The default value is true since API version 2019-04-01. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object" required>
  The SKU of the storage account. Required.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called accountType. Required.
    </ResponseField>
    <ResponseField name="tier" type="string">
      The SKU tier. This is based on the SKU name. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Gets or sets a list of key value pairs that describe the resource. These tags can be used for viewing and grouping this resource (across resource groups). A maximum of 15 tags can be provided for a resource. Each tag must have a key with a length no greater than 128 characters and a value with a length no greater than 256 characters. Optional.
</ResponseField>

<ResponseField name="zones" type="list(string)">
  Optional. Gets or sets the pinned logical availability zone for the storage account. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
