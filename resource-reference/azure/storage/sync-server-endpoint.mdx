---
title: "azurerm_storage_sync_server_endpoint"
description: "Real, generated bindings for `azurerm_storage_sync_server_endpoint`."
---

`azurerm_storage_sync_server_endpoint` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sync server endpoint"})

    		ubx.Resource(storage.SyncServerEndpoint, "ci-runner", storage.SyncServerEndpointConfig{
    			Name:                   "ci-runner",
    			RegisteredServerId:     "example",
    			ServerLocalPath:        "example",
    			StorageSyncGroupId:     "example",
    			CloudTieringEnabled:    true,
    			InitialDownloadPolicy:  "example",
    			LocalCacheMode:         "example",
    			TierFilesOlderThanDays: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncServerEndpoint } from "jsr:@ubx/sdk-azure/azurerm/storage/sync_server_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own sync server endpoint" });

      resource(SyncServerEndpoint, "ci-runner", {
        name: "ci-runner",
        registeredServerId: "example",
        serverLocalPath: "example",
        storageSyncGroupId: "example",
        cloudTieringEnabled: true,
        initialDownloadPolicy: "example",
        localCacheMode: "example",
        tierFilesOlderThanDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.storage import SyncServerEndpoint, SyncServerEndpointConfig

    def describe():
        ubx.intent("payments own sync server endpoint")

        ubx.resource(SyncServerEndpoint, "ci-runner", SyncServerEndpointConfig(
            name="ci-runner",
            registered_server_id="example",
            server_local_path="example",
            storage_sync_group_id="example",
            cloud_tiering_enabled=True,
            initial_download_policy="example",
            local_cache_mode="example",
            tier_files_older_than_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_storage_sync_server_endpoint called "ci-runner",
    with registered server id example, server local path example,
    storage sync group id example, cloud tiering enabled true,
    initial download policy example, local cache mode example, tier
    files older than days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_tiering_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="initial_download_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="local_cache_mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="registered_server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="server_local_path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="storage_sync_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tier_files_older_than_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="volume_free_space_percent" type="number">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
