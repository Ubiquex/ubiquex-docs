---
title: "azure_storage_openapi_blob_container"
description: "`azure_storage_openapi_blob_container` is a Blob Storage container within a storage account..."
---

`azure_storage_openapi_blob_container` is a Blob Storage container within a storage account, the grouping unit blob(s) are organized under with their own access policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own openapi blob container"})

    		ubx.Resource(storage.OpenapiBlobContainer, "example", storage.OpenapiBlobContainerConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenapiBlobContainer } from "@ubx/sdk-azure/azure/storage/openapi_blob_container";

    export default stack("example", () => {
      intent({ summary: "example own openapi blob container" });

      resource(OpenapiBlobContainer, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.storage.openapi_blob_container import OpenapiBlobContainer, OpenapiBlobContainerConfig

    def describe():
        ubx.intent("example own openapi blob container")

        ubx.resource(OpenapiBlobContainer, "example", OpenapiBlobContainerConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_storage_openapi_blob_container, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="etag" type="string">
  Resource Etag. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>

<ResponseField name="properties" type="object">
  The properties of a container. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="default_encryption_scope" type="string">
      Default the container to use specified encryption scope for all writes. Optional.
    </ResponseField>
    <ResponseField name="deleted" type="bool">
      Indicates whether the blob container was deleted. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deleted_time" type="string">
      Blob container deletion time. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="deny_encryption_scope_override" type="bool">
      Block override of encryption scope from the container default. Optional.
    </ResponseField>
    <ResponseField name="enable_nfs_v3_all_squash" type="bool">
      Enable NFSv3 all squash on blob container. Optional.
    </ResponseField>
    <ResponseField name="enable_nfs_v3_root_squash" type="bool">
      Enable NFSv3 root squash on blob container. Optional.
    </ResponseField>
    <ResponseField name="has_immutability_policy" type="bool">
      The hasImmutabilityPolicy public property is set to true by SRP if ImmutabilityPolicy has been created for this container. The hasImmutabilityPolicy public property is set to false by SRP if ImmutabilityPolicy has not been created for this container. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="has_legal_hold" type="bool">
      The hasLegalHold public property is set to true by SRP if there are at least one existing tag. The hasLegalHold public property is set to false by SRP if all existing legal hold tags are cleared out. There can be a maximum of 1000 blob containers with hasLegalHold=true for a given account. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="immutability_policy" type="object">
      The properties of an ImmutabilityPolicy of a blob container. Optional.
      <Expandable title="properties">
        <ResponseField name="etag" type="string">
          ImmutabilityPolicy Etag. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="properties" type="object">
          The properties of an ImmutabilityPolicy of a blob container. Optional.
          <Expandable title="properties">
            <Note>`properties` recurs here (its own type contains itself); see the `properties` field's own properties above for the repeating pattern.</Note>
          </Expandable>
        </ResponseField>
        <ResponseField name="update_history" type="list(object)">
          The ImmutabilityPolicy update history of the blob container. Computed by Microsoft Azure after `ship`.
          <Expandable title="properties">
            <ResponseField name="allow_protected_append_writes" type="bool">
              Whether new blocks can still be appended to an append blob under legal hold or an active immutability policy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allow_protected_append_writes_all" type="bool">
              Whether new blocks can still be appended to any protected blob type under legal hold or an active immutability policy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="immutability_period_since_creation_in_days" type="number">
              How many days after creation a blob remains immutable under this policy. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object_identifier" type="string">
              The Microsoft Entra ID object identifier this permission applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tenant_id" type="string">
              Output only. The Microsoft Entra ID tenant this resource's own managed identity belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              When this event occurred. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="update" type="string">
              The update applied. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="upn" type="string">
              The User Principal Name this permission applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="immutable_storage_with_versioning" type="object">
      Object level immutability properties of the container. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          This is an immutable property, when set to true it enables object level immutability at the container level. Optional.
        </ResponseField>
        <ResponseField name="migration_state" type="string">
          This property denotes the container level immutability to object level immutability migration state. Optional.
        </ResponseField>
        <ResponseField name="time_stamp" type="string">
          Returns the date and time the object level immutability was enabled. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_modified_time" type="string">
      Returns the date and time the container was last modified. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="lease_duration" type="string">
      Specifies whether the lease on a container is of infinite or fixed duration, only when the container is leased. Optional.
    </ResponseField>
    <ResponseField name="lease_state" type="string">
      Lease state of the container. Optional.
    </ResponseField>
    <ResponseField name="lease_status" type="string">
      The lease status of the container. Optional.
    </ResponseField>
    <ResponseField name="legal_hold" type="object">
      The LegalHold property of a blob container. Optional.
      <Expandable title="properties">
        <ResponseField name="has_legal_hold" type="bool">
          The hasLegalHold public property is set to true by SRP if there are at least one existing tag. The hasLegalHold public property is set to false by SRP if all existing legal hold tags are cleared out. There can be a maximum of 1000 blob containers with hasLegalHold=true for a given account. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="protected_append_writes_history" type="object">
          Protected append writes history setting for the blob container with Legal holds. Optional.
          <Expandable title="properties">
            <ResponseField name="allow_protected_append_writes_all" type="bool">
              When enabled, new blocks can be written to both 'Append and Bock Blobs' while maintaining legal hold protection and compliance. Only new blocks can be added and any existing blocks cannot be modified or deleted. Optional.
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Returns the date and time the tag was added. Computed by Microsoft Azure after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tags" type="list(object)">
          The list of LegalHold tags of a blob container. Optional.
          <Expandable title="properties">
            <ResponseField name="object_identifier" type="string">
              The Microsoft Entra ID object identifier this permission applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tag" type="string">
              A label attached to this resource. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tenant_id" type="string">
              Output only. The Microsoft Entra ID tenant this resource's own managed identity belongs to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              When this event occurred. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="upn" type="string">
              The User Principal Name this permission applies to. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="metadata" type="map(string)">
      A name-value pair to associate with the container as metadata. Optional.
    </ResponseField>
    <ResponseField name="public_access" type="string">
      Specifies whether data in the container may be accessed publicly and the level of access. Optional.
    </ResponseField>
    <ResponseField name="remaining_retention_days" type="number">
      Remaining retention days for soft deleted blob container. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the deleted blob container. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
