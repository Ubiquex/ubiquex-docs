---
title: "azurerm_function_app_active_slot"
description: "Real, generated bindings for `azurerm_function_app_active_slot`."
---

`azurerm_function_app_active_slot` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	function "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/function"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app active slot"})

    		ubx.Resource(function.AppActiveSlot, "ci-runner", function.AppActiveSlotConfig{
    			SlotId:                 "example",
    			OverwriteNetworkConfig: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppActiveSlot } from "jsr:@ubx/sdk-azure/azurerm/function/app_active_slot";

    export default stack("payments", () => {
      intent({ summary: "payments own app active slot" });

      resource(AppActiveSlot, "ci-runner", {
        slotId: "example",
        overwriteNetworkConfig: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.function import AppActiveSlot, AppActiveSlotConfig

    def describe():
        ubx.intent("payments own app active slot")

        ubx.resource(AppActiveSlot, "ci-runner", AppActiveSlotConfig(
            slot_id="example",
            overwrite_network_config=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_function_app_active_slot, with slot id example,
    overwrite network config true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="overwrite_network_config" type="bool">
  Optional.
</ResponseField>

<ResponseField name="slot_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="last_successful_swap" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
