---
title: "azurerm_function_app_function"
description: "Real, generated bindings for `azurerm_function_app_function`."
---

`azurerm_function_app_function` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	function "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/function"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app function"})

    		ubx.Resource(function.AppFunction, "ci-runner", function.AppFunctionConfig{
    			ConfigJson:    "example",
    			FunctionAppId: "example",
    			Name:          "ci-runner",
    			Enabled:       true,
    			Language:      "example",
    			TestData:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppFunction } from "jsr:@ubx/sdk-azure/azurerm/function/app_function";

    export default stack("payments", () => {
      intent({ summary: "payments own app function" });

      resource(AppFunction, "ci-runner", {
        configJson: "example",
        functionAppId: "example",
        name: "ci-runner",
        enabled: true,
        language: "example",
        testData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.function import AppFunction, AppFunctionConfig

    def describe():
        ubx.intent("payments own app function")

        ubx.resource(AppFunction, "ci-runner", AppFunctionConfig(
            config_json="example",
            function_app_id="example",
            name="ci-runner",
            enabled=True,
            language="example",
            test_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_function_app_function called "ci-runner", with
    config json example, function app id example, enabled true,
    language example, test data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config_json" type="string" required>
  The config for this Function in JSON format. Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Should this function be enabled. Defaults to `true`. Optional.
</ResponseField>

<ResponseField name="file" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="content" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="function_app_id" type="string" required>
  The ID of the Function App in which this function should reside. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="language" type="string">
  The language the Function is written in. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the function. Required.
</ResponseField>

<ResponseField name="test_data" type="string">
  The test data for the function. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="config_url" type="string">
  The URL of the configuration JSON. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="file" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="content" type="string" required>
    </ResponseField>
    <ResponseField name="name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="invocation_url" type="string">
  The invocation URL. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="script_root_path_url" type="string">
  The Script root path URL. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="script_url" type="string">
  The script URL. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="secrets_file_url" type="string">
  The URL for the Secrets File. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="test_data_url" type="string">
  The Test data URL. Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="url" type="string">
  The function URL. Computed by Azure after `ship`.
</ResponseField>
