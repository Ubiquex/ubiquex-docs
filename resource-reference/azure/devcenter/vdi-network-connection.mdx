---
title: "azure_devcenter_vdi_network_connection"
description: "`azure_devcenter_vdi_network_connection` defines the virtual network a Microsoft Dev Box's own dev box definition is domain-joined and deployed into."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vdi network connection"})

    		ubx.Resource(devcenter.VdiNetworkConnection, "example", devcenter.VdiNetworkConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VdiNetworkConnection } from "@ubx/sdk-azure/azure/devcenter/vdi_network_connection";

    export default stack("example", () => {
      intent({ summary: "example own vdi network connection" });

      resource(VdiNetworkConnection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devcenter import VdiNetworkConnection, VdiNetworkConnectionConfig

    def describe():
        ubx.intent("example own vdi network connection")

        ubx.resource(VdiNetworkConnection, "example", VdiNetworkConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_vdi_network_connection, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Network properties Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="domain_join_type" type="string" required>
      Active Directory join type Required.
    </ResponseField>
    <ResponseField name="health_check_status" type="string">
      Health check status values Optional.
    </ResponseField>
    <ResponseField name="networking_resource_group_name" type="string">
      The name for resource group where NICs will be placed. Optional.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
