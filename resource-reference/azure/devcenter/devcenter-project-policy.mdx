---
title: "azure_devcenter_project_policy"
description: "`azure_devcenter_project_policy` restricts which user(s) or group(s) can access resources across multiple Dev Box projects collectively..."
---

`azure_devcenter_project_policy` restricts which user(s) or group(s) can access resources across multiple Dev Box projects collectively, via a centrally defined policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own devcenter project policy"})

    		ubx.Resource(devcenter.ProjectPolicy, "example", devcenter.ProjectPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProjectPolicy } from "@ubx/sdk-azure/azure/devcenter/project_policy";

    export default stack("example", () => {
      intent({ summary: "example own devcenter project policy" });

      resource(ProjectPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devcenter.project_policy import ProjectPolicy, ProjectPolicyConfig

    def describe():
        ubx.intent("example own devcenter project policy")

        ubx.resource(ProjectPolicy, "example", ProjectPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_project_policy, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an project policy. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
