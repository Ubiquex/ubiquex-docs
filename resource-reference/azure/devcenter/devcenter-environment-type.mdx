---
title: "azure_devcenter_environment_type"
description: "`azure_devcenter_devcenter_environment_type` defines a named category of deployment environment (e.g. `Sandbox` or `Production`) at the dev center..."
---

`azure_devcenter_devcenter_environment_type` defines a named category of deployment environment (e.g. `Sandbox` or `Production`) at the dev center level, that project(s) can enable for self-service provisioning.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devcenter_devcenter --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devcenter_devcenter/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devcenter_devcenter/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own devcenter environment type"})

    		ubx.Resource(devcenter.DevcenterEnvironmentType, "example", devcenter.DevcenterEnvironmentTypeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devcenter_devcenter --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DevcenterEnvironmentType } from "./local-sdk/azure_devcenter_devcenter/sdk/typescript/azure_devcenter_devcenter/devcenter/devcenter_environment_type.ts";

    export default stack("example", () => {
      intent({ summary: "example own devcenter environment type" });

      resource(DevcenterEnvironmentType, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devcenter_devcenter --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devcenter_devcenter/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devcenter_devcenter.devcenter import DevcenterEnvironmentType, DevcenterEnvironmentTypeConfig

    def describe():
        ubx.intent("example own devcenter environment type")

        ubx.resource(DevcenterEnvironmentType, "example", DevcenterEnvironmentTypeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_environment_type, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an environment type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Tags to apply to this devcenter environment type. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
</ResponseField>
