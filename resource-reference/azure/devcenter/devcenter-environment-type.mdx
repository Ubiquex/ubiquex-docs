---
title: "azure_devcenter_environment_type"
description: "`azure_devcenter_environment_type` defines a named category of deployment environment (e.g. `Sandbox` or `Production`) at the dev center..."
---

`azure_devcenter_environment_type` defines a named category of deployment environment (e.g. `Sandbox` or `Production`) at the dev center level, that project(s) can enable for self-service provisioning.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own devcenter environment type"})

    		ubx.Resource(devcenter.EnvironmentType, "example", devcenter.EnvironmentTypeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentType } from "@ubx/sdk-azure/azure/devcenter/environment_type";

    export default stack("example", () => {
      intent({ summary: "example own devcenter environment type" });

      resource(EnvironmentType, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devcenter.environment_type import EnvironmentType, EnvironmentTypeConfig

    def describe():
        ubx.intent("example own devcenter environment type")

        ubx.resource(EnvironmentType, "example", EnvironmentTypeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_environment_type, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of an environment type. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Resource tags. Optional; if omitted, computed by Microsoft Azure.
</ResponseField>
