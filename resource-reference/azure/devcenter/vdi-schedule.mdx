---
title: "azure_devcenter_vdi_schedule"
description: "`azure_devcenter_vdi_schedule` configures an automatic shutdown schedule for dev box(es) in a pool, reducing idle compute cost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devcenter_vdi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devcenter_vdi/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devcenter_vdi/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vdi schedule"})

    		ubx.Resource(devcenter.VdiSchedule, "ci-runner", devcenter.VdiScheduleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devcenter_vdi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VdiSchedule } from "./local-sdk/azure_devcenter_vdi/sdk/typescript/azure_devcenter_vdi/devcenter/vdi_schedule.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vdi schedule" });

      resource(VdiSchedule, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devcenter_vdi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devcenter_vdi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devcenter_vdi.devcenter import VdiSchedule, VdiScheduleConfig

    def describe():
        ubx.intent("payments own vdi schedule")

        ubx.resource(VdiSchedule, "ci-runner", VdiScheduleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_vdi_schedule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The Schedule properties defining when and what to execute. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
