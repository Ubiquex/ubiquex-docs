---
title: "azure_devcenter_vdi_schedule"
description: "`azure_devcenter_vdi_schedule` configures an automatic shutdown schedule for dev box(es) in a pool, reducing idle compute cost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vdi schedule"})

    		ubx.Resource(devcenter.VdiSchedule, "example", devcenter.VdiScheduleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VdiSchedule } from "@ubx/sdk-azure/azure/devcenter/vdi_schedule";

    export default stack("example", () => {
      intent({ summary: "example own vdi schedule" });

      resource(VdiSchedule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devcenter.vdi_schedule import VdiSchedule, VdiScheduleConfig

    def describe():
        ubx.intent("example own vdi schedule")

        ubx.resource(VdiSchedule, "example", VdiScheduleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_vdi_schedule, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The Schedule properties defining when and what to execute. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
