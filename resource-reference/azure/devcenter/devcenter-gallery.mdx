---
title: "azure_devcenter_gallery"
description: "`azure_devcenter_devcenter_gallery` attaches an Azure Compute Gallery to a dev center, making its own custom VM images available for dev box definitions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own devcenter gallery"})

    		ubx.Resource(devcenter.Gallery, "example", devcenter.GalleryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Gallery } from "@ubx/sdk-azure/azure/devcenter/gallery";

    export default stack("example", () => {
      intent({ summary: "example own devcenter gallery" });

      resource(Gallery, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.devcenter import Gallery, GalleryConfig

    def describe():
        ubx.intent("example own devcenter gallery")

        ubx.resource(Gallery, "example", GalleryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_gallery, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a gallery. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="gallery_resource_id" type="string" required>
      The resource ID of the backing Azure Compute Gallery. Required.
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
