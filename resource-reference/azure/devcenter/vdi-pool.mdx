---
title: "azure_devcenter_vdi_pool"
description: "`azure_devcenter_vdi_pool` is a Dev Box pool..."
---

`azure_devcenter_vdi_pool` is a Dev Box pool, combining a dev box definition and network connection to define what developers in a project can self-service provision.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_devcenter_vdi --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_devcenter_vdi/sdk/go
    package main

    import (
    	devcenter "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_devcenter_vdi/devcenter"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vdi pool"})

    		ubx.Resource(devcenter.VdiPool, "ci-runner", devcenter.VdiPoolConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_devcenter_vdi --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VdiPool } from "./local-sdk/azure_devcenter_vdi/sdk/typescript/azure_devcenter_vdi/devcenter/vdi_pool.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vdi pool" });

      resource(VdiPool, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_devcenter_vdi --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_devcenter_vdi/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_devcenter_vdi.devcenter import VdiPool, VdiPoolConfig

    def describe():
        ubx.intent("payments own vdi pool")

        ubx.resource(VdiPool, "ci-runner", VdiPoolConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_devcenter_vdi_pool, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  Properties of a Pool Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="dev_box_count" type="number">
      Indicates the number of provisioned Dev Boxes in this pool. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="health_status" type="string">
      Health status indicating whether a pool is available to create Dev Boxes. Optional.
    </ResponseField>
    <ResponseField name="health_status_details" type="list(object)">
      Details on the Pool health status to help diagnose issues. This is only populated when the pool status indicates the pool is in a non-healthy state Computed by Microsoft Azure after `ship`.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          A short, machine-readable code identifying this pool health issue. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          A human-readable description of this pool health issue. Optional; if omitted, computed by Microsoft Azure. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioning_state" type="string">
      Provisioning state of the resource. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
