---
title: "azure_logic_integration_account"
description: "`azure_logic_logic_integration_account` is the top-level B2B integration resource for Logic Apps, holding the trading partner(s), agreement(s)..."
---

`azure_logic_logic_integration_account` is the top-level B2B integration resource for Logic Apps, holding the trading partner(s), agreement(s), schema(s), and map(s) EDI/XML workflows reference.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own logic integration account"})

    		ubx.Resource(logic.IntegrationAccount, "example", logic.IntegrationAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationAccount } from "@ubx/sdk-azure/azure/logic/integration_account";

    export default stack("example", () => {
      intent({ summary: "example own logic integration account" });

      resource(IntegrationAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azure.logic import IntegrationAccount, IntegrationAccountConfig

    def describe():
        ubx.intent("example own logic integration account")

        ubx.resource(IntegrationAccount, "example", IntegrationAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_logic_integration_account, with .
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object">
  The integration account properties. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="integration_service_environment" type="object">
      The resource reference. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The resource id. Optional.
        </ResponseField>
        <ResponseField name="name" type="string">
          Gets the resource name. Computed by Microsoft Azure after `ship`.
        </ResponseField>
        <ResponseField name="type" type="string">
          Gets the resource type. Computed by Microsoft Azure after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      The workflow state. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sku" type="object">
  The integration account sku. Optional; if omitted, computed by Microsoft Azure.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The integration account sku name. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
