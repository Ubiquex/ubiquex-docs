---
title: "azure_logic_integration_account_session"
description: "`azure_logic_integration_account_session` tracks state (e.g. AS2 message IDs) used to detect duplicate B2B message delivery within a Logic Apps..."
---

`azure_logic_integration_account_session` tracks state (e.g. AS2 message IDs) used to detect duplicate B2B message delivery within a Logic Apps integration account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only azure_logic --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-azure/sdk/go => ./local-sdk/azure_logic/sdk/go
    package main

    import (
    	logic "github.com/ubiquex/ubx-sdk-azure/sdk/go/azure_logic/logic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own logic integration account session"})

    		ubx.Resource(logic.LogicIntegrationAccountSession, "example", logic.LogicIntegrationAccountSessionConfig{
    			Properties: map[string]any{"changed_time": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only azure_logic --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogicIntegrationAccountSession } from "./local-sdk/azure_logic/sdk/typescript/azure_logic/logic/logic_integration_account_session.ts";

    export default stack("example", () => {
      intent({ summary: "example own logic integration account session" });

      resource(LogicIntegrationAccountSession, "example", {
        properties: { changedTime: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only azure_logic --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/azure_logic/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.azure_logic.logic import LogicIntegrationAccountSession, LogicIntegrationAccountSessionConfig

    def describe():
        ubx.intent("example own logic integration account session")

        ubx.resource(LogicIntegrationAccountSession, "example", LogicIntegrationAccountSessionConfig(
            properties={"changed_time": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azure_logic_integration_account_session, with properties a
    set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="properties" type="object" required>
  The integration account session properties. Required.
  <Expandable title="properties">
    <ResponseField name="changed_time" type="string">
      The changed time. Computed by Microsoft Azure after `ship`.
    </ResponseField>
    <ResponseField name="content" type="any">
      The literal content of this schema or configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_time" type="string">
      The created time. Computed by Microsoft Azure after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>
