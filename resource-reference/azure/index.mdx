---
title: "Microsoft Azure"
description: "Microsoft Azure resource types, grouped by real service."
---

Microsoft Azure covers 1 resource types (as of the
version this reference was generated from), sourced directly from
the real Azure Resource Manager API specification via ubx-provider-dynamic, grouped here
the same way the real generated bindings are, mechanically by
`Microsoft Azure` service boundary (see each generated repo's own
`ServiceAndLocalName`). Every page's field list is parsed directly from
the real provider schema, not hand-authored.

<CardGroup cols={2}>
  <Card title="Authorization" icon="cube" href="/resource-reference/azure/authorization/roleassignmentschedulerequest-role-assignment-schedule-request">
    1 resource type documented
  </Card>
</CardGroup>
