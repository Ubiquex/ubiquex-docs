---
title: "azurerm_site_recovery_vmware_replication_policy"
description: "Real, generated bindings for `azurerm_site_recovery_vmware_replication_policy`."
---

`azurerm_site_recovery_vmware_replication_policy` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	site "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/site"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own recovery vmware replication policy"})

    		ubx.Resource(site.RecoveryVmwareReplicationPolicy, "ci-runner", site.RecoveryVmwareReplicationPolicyConfig{
    			ApplicationConsistentSnapshotFrequencyInMinutes: 1,
    			Name:                            "ci-runner",
    			RecoveryPointRetentionInMinutes: 1,
    			RecoveryVaultId:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecoveryVmwareReplicationPolicy } from "jsr:@ubx/sdk-azure/azurerm/site/recovery_vmware_replication_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own recovery vmware replication policy" });

      resource(RecoveryVmwareReplicationPolicy, "ci-runner", {
        applicationConsistentSnapshotFrequencyInMinutes: 1,
        name: "ci-runner",
        recoveryPointRetentionInMinutes: 1,
        recoveryVaultId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.site import RecoveryVmwareReplicationPolicy, RecoveryVmwareReplicationPolicyConfig

    def describe():
        ubx.intent("payments own recovery vmware replication policy")

        ubx.resource(RecoveryVmwareReplicationPolicy, "ci-runner", RecoveryVmwareReplicationPolicyConfig(
            application_consistent_snapshot_frequency_in_minutes=1,
            name="ci-runner",
            recovery_point_retention_in_minutes=1,
            recovery_vault_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_site_recovery_vmware_replication_policy called
    "ci-runner", with application consistent snapshot frequency in
    minutes 1, recovery point retention in minutes 1, recovery vault
    id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_consistent_snapshot_frequency_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recovery_point_retention_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="recovery_vault_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
