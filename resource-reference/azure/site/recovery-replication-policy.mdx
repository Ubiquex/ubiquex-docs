---
title: "azurerm_site_recovery_replication_policy"
description: "Real, generated bindings for `azurerm_site_recovery_replication_policy`."
---

`azurerm_site_recovery_replication_policy` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	site "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/site"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own recovery replication policy"})

    		ubx.Resource(site.RecoveryReplicationPolicy, "ci-runner", site.RecoveryReplicationPolicyConfig{
    			ApplicationConsistentSnapshotFrequencyInMinutes: 1,
    			Name:                            "ci-runner",
    			RecoveryPointRetentionInMinutes: 1,
    			RecoveryVaultName:               "example-recovery-vault-name",
    			ResourceGroupName:               "example-resource-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecoveryReplicationPolicy } from "jsr:@ubx/sdk-azure/azurerm/site/recovery_replication_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own recovery replication policy" });

      resource(RecoveryReplicationPolicy, "ci-runner", {
        applicationConsistentSnapshotFrequencyInMinutes: 1,
        name: "ci-runner",
        recoveryPointRetentionInMinutes: 1,
        recoveryVaultName: "example-recovery-vault-name",
        resourceGroupName: "example-resource-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.site import RecoveryReplicationPolicy, RecoveryReplicationPolicyConfig

    def describe():
        ubx.intent("payments own recovery replication policy")

        ubx.resource(RecoveryReplicationPolicy, "ci-runner", RecoveryReplicationPolicyConfig(
            application_consistent_snapshot_frequency_in_minutes=1,
            name="ci-runner",
            recovery_point_retention_in_minutes=1,
            recovery_vault_name="example-recovery-vault-name",
            resource_group_name="example-resource-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_site_recovery_replication_policy called
    "ci-runner", with application consistent snapshot frequency in
    minutes 1, recovery point retention in minutes 1, recovery vault
    name example-recovery-vault-name, resource group name
    example-resource-group-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_consistent_snapshot_frequency_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recovery_point_retention_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="recovery_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
