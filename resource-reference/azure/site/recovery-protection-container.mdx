---
title: "azurerm_site_recovery_protection_container"
description: "Real, generated bindings for `azurerm_site_recovery_protection_container`."
---

`azurerm_site_recovery_protection_container` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	site "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/site"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own recovery protection container"})

    		ubx.Resource(site.RecoveryProtectionContainer, "ci-runner", site.RecoveryProtectionContainerConfig{
    			Name:               "ci-runner",
    			RecoveryFabricName: "example-recovery-fabric-name",
    			RecoveryVaultName:  "example-recovery-vault-name",
    			ResourceGroupName:  "example-resource-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecoveryProtectionContainer } from "jsr:@ubx/sdk-azure/azurerm/site/recovery_protection_container";

    export default stack("payments", () => {
      intent({ summary: "payments own recovery protection container" });

      resource(RecoveryProtectionContainer, "ci-runner", {
        name: "ci-runner",
        recoveryFabricName: "example-recovery-fabric-name",
        recoveryVaultName: "example-recovery-vault-name",
        resourceGroupName: "example-resource-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.site import RecoveryProtectionContainer, RecoveryProtectionContainerConfig

    def describe():
        ubx.intent("payments own recovery protection container")

        ubx.resource(RecoveryProtectionContainer, "ci-runner", RecoveryProtectionContainerConfig(
            name="ci-runner",
            recovery_fabric_name="example-recovery-fabric-name",
            recovery_vault_name="example-recovery-vault-name",
            resource_group_name="example-resource-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_site_recovery_protection_container called
    "ci-runner", with recovery fabric name
    example-recovery-fabric-name, recovery vault name
    example-recovery-vault-name, resource group name
    example-resource-group-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recovery_fabric_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recovery_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
