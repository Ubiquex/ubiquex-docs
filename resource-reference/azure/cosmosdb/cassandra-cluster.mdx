---
title: "azurerm_cosmosdb_cassandra_cluster"
description: "Real, generated bindings for `azurerm_cosmosdb_cassandra_cluster`."
---

`azurerm_cosmosdb_cassandra_cluster` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmosdb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/cosmosdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cassandra cluster"})

    		ubx.Resource(cosmosdb.CassandraCluster, "ci-runner", cosmosdb.CassandraClusterConfig{
    			DefaultAdminPassword:          "example",
    			DelegatedManagementSubnetId:   "example",
    			Location:                      "example",
    			Name:                          "ci-runner",
    			ResourceGroupName:             "example-resource-group-name",
    			AuthenticationMethod:          "example",
    			ClientCertificatePems:         []string{"example"},
    			ExternalGossipCertificatePems: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CassandraCluster } from "jsr:@ubx/sdk-azure/azurerm/cosmosdb/cassandra_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cassandra cluster" });

      resource(CassandraCluster, "ci-runner", {
        defaultAdminPassword: "example",
        delegatedManagementSubnetId: "example",
        location: "example",
        name: "ci-runner",
        resourceGroupName: "example-resource-group-name",
        authenticationMethod: "example",
        clientCertificatePems: ["example"],
        externalGossipCertificatePems: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.cosmosdb import CassandraCluster, CassandraClusterConfig

    def describe():
        ubx.intent("payments own cassandra cluster")

        ubx.resource(CassandraCluster, "ci-runner", CassandraClusterConfig(
            default_admin_password="example",
            delegated_management_subnet_id="example",
            location="example",
            name="ci-runner",
            resource_group_name="example-resource-group-name",
            authentication_method="example",
            client_certificate_pems=["example"],
            external_gossip_certificate_pems=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_cosmosdb_cassandra_cluster called "ci-runner",
    with default admin password example, delegated management subnet
    id example, location example, resource group name
    example-resource-group-name, authentication method example,
    client certificate pems a set value, external gossip certificate
    pems a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authentication_method" type="string">
  Optional.
</ResponseField>

<ResponseField name="client_certificate_pems" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="default_admin_password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="delegated_management_subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="external_gossip_certificate_pems" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="external_seed_node_ip_addresses" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="hours_between_backups" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="identity" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="repair_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="identity" type="list(object)">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="principal_id" type="string">
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
