---
title: "azurerm_cosmosdb_postgresql_coordinator_configuration"
description: "Real, generated bindings for `azurerm_cosmosdb_postgresql_coordinator_configuration`."
---

`azurerm_cosmosdb_postgresql_coordinator_configuration` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmosdb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/cosmosdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own postgresql coordinator configuration"})

    		ubx.Resource(cosmosdb.PostgresqlCoordinatorConfiguration, "ci-runner", cosmosdb.PostgresqlCoordinatorConfigurationConfig{
    			ClusterId: "example",
    			Name:      "ci-runner",
    			Value:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PostgresqlCoordinatorConfiguration } from "jsr:@ubx/sdk-azure/azurerm/cosmosdb/postgresql_coordinator_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own postgresql coordinator configuration" });

      resource(PostgresqlCoordinatorConfiguration, "ci-runner", {
        clusterId: "example",
        name: "ci-runner",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.cosmosdb import PostgresqlCoordinatorConfiguration, PostgresqlCoordinatorConfigurationConfig

    def describe():
        ubx.intent("payments own postgresql coordinator configuration")

        ubx.resource(PostgresqlCoordinatorConfiguration, "ci-runner", PostgresqlCoordinatorConfigurationConfig(
            cluster_id="example",
            name="ci-runner",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_cosmosdb_postgresql_coordinator_configuration
    called "ci-runner", with cluster id example, value example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
