---
title: "azurerm_cosmosdb_postgresql_role"
description: "Real, generated bindings for `azurerm_cosmosdb_postgresql_role`."
---

`azurerm_cosmosdb_postgresql_role` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cosmosdb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/cosmosdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own postgresql role"})

    		ubx.Resource(cosmosdb.PostgresqlRole, "ci-runner", cosmosdb.PostgresqlRoleConfig{
    			ClusterId: "example",
    			Name:      "ci-runner",
    			Password:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PostgresqlRole } from "jsr:@ubx/sdk-azure/azurerm/cosmosdb/postgresql_role";

    export default stack("payments", () => {
      intent({ summary: "payments own postgresql role" });

      resource(PostgresqlRole, "ci-runner", {
        clusterId: "example",
        name: "ci-runner",
        password: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.cosmosdb import PostgresqlRole, PostgresqlRoleConfig

    def describe():
        ubx.intent("payments own postgresql role")

        ubx.resource(PostgresqlRole, "ci-runner", PostgresqlRoleConfig(
            cluster_id="example",
            name="ci-runner",
            password="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_cosmosdb_postgresql_role called "ci-runner", with
    cluster id example, password example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
