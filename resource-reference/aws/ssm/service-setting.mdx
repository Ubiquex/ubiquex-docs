---
title: "aws_ssm_service_setting"
description: "Real, generated bindings for `aws_ssm_service_setting`."
---

`aws_ssm_service_setting` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service setting"})

    		ubx.Resource(ssm.ServiceSetting, "ci-runner", ssm.ServiceSettingConfig{
    			SettingId:    "example",
    			SettingValue: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceSetting } from "jsr:@ubx/sdk-aws/aws/ssm/service_setting";

    export default stack("payments", () => {
      intent({ summary: "payments own service setting" });

      resource(ServiceSetting, "ci-runner", {
        settingId: "example",
        settingValue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssm import ServiceSetting, ServiceSettingConfig

    def describe():
        ubx.intent("payments own service setting")

        ubx.resource(ServiceSetting, "ci-runner", ServiceSettingConfig(
            setting_id="example",
            setting_value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssm_service_setting, with setting id example, setting
    value example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="setting_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="setting_value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
