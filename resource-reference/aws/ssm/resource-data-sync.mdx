---
title: "aws_ssm_resource_data_sync"
description: "Real, generated bindings for `aws_ssm_resource_data_sync`."
---

`aws_ssm_resource_data_sync` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ssm"

    cfg := ssm.ResourceDataSyncConfig{
        Name: "example-name",
    }
    ubx.Resource(ssm.ResourceDataSync, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceDataSync } from
      "jsr:@ubx/sdk-aws/aws/ssm/resource_data_sync";

    resource(ResourceDataSync, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssm import (
        ResourceDataSync,
        ResourceDataSyncConfig,
    )

    cfg = ResourceDataSyncConfig(
        name="example",
    )
    ubx.resource(ResourceDataSync, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssm_resource_data_sync with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
