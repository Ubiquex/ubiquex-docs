---
title: "aws_ssm_maintenance_window_target"
description: "Real, generated bindings for `aws_ssm_maintenance_window_target`."
---

`aws_ssm_maintenance_window_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own maintenance window target"})

    		ubx.Resource(ssm.MaintenanceWindowTarget, "ci-runner", ssm.MaintenanceWindowTargetConfig{
    			ResourceType:     "example",
    			WindowId:         "example",
    			Name:             "ci-runner",
    			Description:      "Managed by ubx.",
    			OwnerInformation: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MaintenanceWindowTarget } from "jsr:@ubx/sdk-aws/aws/ssm/maintenance_window_target";

    export default stack("payments", () => {
      intent({ summary: "payments own maintenance window target" });

      resource(MaintenanceWindowTarget, "ci-runner", {
        resourceType: "example",
        windowId: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        ownerInformation: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssm import MaintenanceWindowTarget, MaintenanceWindowTargetConfig

    def describe():
        ubx.intent("payments own maintenance window target")

        ubx.resource(MaintenanceWindowTarget, "ci-runner", MaintenanceWindowTargetConfig(
            resource_type="example",
            window_id="example",
            name="ci-runner",
            description="Managed by ubx.",
            owner_information="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssm_maintenance_window_target called "ci-runner", with
    resource type example, window id example, description Managed by
    ubx., owner information example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="owner_information" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="window_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
