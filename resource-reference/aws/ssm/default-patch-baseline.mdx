---
title: "aws_ssm_default_patch_baseline"
description: "Real, generated bindings for `aws_ssm_default_patch_baseline`."
---

`aws_ssm_default_patch_baseline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ssm"

    cfg := ssm.DefaultPatchBaselineConfig{
        BaselineId: "example",
        OperatingSystem: "example",
    }
    ubx.Resource(ssm.DefaultPatchBaseline, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DefaultPatchBaseline } from
      "jsr:@ubx/sdk-aws/aws/ssm/default_patch_baseline";

    resource(DefaultPatchBaseline, "example", {
      baselineId: "example",
      operatingSystem: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssm import (
        DefaultPatchBaseline,
        DefaultPatchBaselineConfig,
    )

    cfg = DefaultPatchBaselineConfig(
        baseline_id="example",
        operating_system="example",
    )
    ubx.resource(DefaultPatchBaseline, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssm_default_patch_baseline with baseline_id,
operating_system.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="baseline_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
