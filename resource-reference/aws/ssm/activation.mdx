---
title: "aws_ssm_activation"
description: "Real, generated bindings for `aws_ssm_activation`."
---

`aws_ssm_activation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ssm"

    cfg := ssm.ActivationConfig{
        IamRole: "example",
        Description: "example",
        ExpirationDate: "example",
    }
    ubx.Resource(ssm.Activation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Activation } from "jsr:@ubx/sdk-aws/aws/ssm/activation";

    resource(Activation, "example", {
      iamRole: "example",
      description: "example",
      expirationDate: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssm import Activation, ActivationConfig

    cfg = ActivationConfig(
        iam_role="example",
        description="example",
        expiration_date="example",
    )
    ubx.resource(Activation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssm_activation with iam_role, description,
expiration_date.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="iam_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registration_limit" type="number">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="activation_code" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="expired" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registration_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
