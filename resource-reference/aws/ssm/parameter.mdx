---
title: "aws_ssm_parameter"
description: "`aws_ssm_parameter` stores a real string, string list, or secure string value in Parameter Store..."
---

`aws_ssm_parameter` stores a real string, string list, or secure string value in Parameter Store, that applications and other AWS resources reference by name at runtime instead of hardcoding configuration or (for SecureString) secrets directly.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own parameter"})

    		ubx.Resource(ssm.Parameter, "example", ssm.ParameterConfig{
    			Type:           "example",
    			Value:          "example",
    			Name:           "example",
    			AllowedPattern: "example",
    			DataType:       "example",
    			Description:    "Managed by ubx.",
    			Policies:       "example",
    			Tags:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Parameter } from "@ubx/sdk-aws/aws/ssm/parameter";

    export default stack("example", () => {
      intent({ summary: "example own parameter" });

      resource(Parameter, "example", {
        type: "example",
        value: "example",
        name: "example",
        allowedPattern: "example",
        dataType: "example",
        description: "Managed by ubx.",
        policies: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssm import Parameter, ParameterConfig

    def describe():
        ubx.intent("example own parameter")

        ubx.resource(Parameter, "example", ParameterConfig(
            type="example",
            value="example",
            name="example",
            allowed_pattern="example",
            data_type="example",
            description="Managed by ubx.",
            policies="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssm_parameter called "example", with type example,
    value example, allowed pattern example, data type example,
    description Managed by ubx., policies example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_pattern" type="string">
  A regular expression used to validate the parameter value. For example, for ``String`` types with values restricted to numbers, you can specify the following: ``AllowedPattern=^\d+$`` Optional.
</ResponseField>

<ResponseField name="data_type" type="string">
  The data type of the parameter, such as ``text`` or ``aws:ec2:image``. The default is ``text``. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Information about the parameter. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the parameter. The reported maximum length of 2048 characters for a parameter name includes 1037 characters that are reserved for internal use by SYS. The maximum length for a parameter name that you specify is 1011 characters. This count of 1011 characters includes the characters in the ARN that precede the name you specify. This ARN length will vary depending on your partition and Region. For example, the following 45 characters count toward the 1011 character maximum for a parameter created in the US East (Ohio) Region: ``arn:aws:ssm:us-east-2:111122223333:parameter/``. Optional.
</ResponseField>

<ResponseField name="policies" type="string">
  Information about the policies assigned to a parameter. [Assigning parameter policies](https://docs.aws.amazon.com/systems-manager/latest/userguide/parameter-store-policies.html) in the *User Guide*. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  Optional metadata that you assign to a resource in the form of an arbitrary set of tags (key-value pairs). Tags enable you to categorize a resource in different ways, such as by purpose, owner, or environment. For example, you might want to tag a SYS parameter to identify the type of resource to which it applies, the environment, or the type of configuration data referenced by the parameter. Optional.
</ResponseField>

<ResponseField name="tier" type="string">
  The parameter tier. Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of parameter. Parameters of type ``SecureString`` are not supported by CFNlong. Required.
</ResponseField>

<ResponseField name="value" type="string" required>
  The parameter value. If type is ``StringList``, the system returns a comma-separated string with no spaces between commas in the ``Value`` field. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) assigned to the parameter, which uniquely identifies it within AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
