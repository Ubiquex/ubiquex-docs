---
title: "aws_ssm_maintenance_window"
description: "Real, generated bindings for `aws_ssm_maintenance_window`."
---

`aws_ssm_maintenance_window` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own maintenance window"})

    		ubx.Resource(ssm.MaintenanceWindow, "ci-runner", ssm.MaintenanceWindowConfig{
    			Cutoff:                   1,
    			Duration:                 7200,
    			Name:                     "ci-runner",
    			Schedule:                 "example",
    			AllowUnassociatedTargets: true,
    			Description:              "Managed by ubx.",
    			Enabled:                  true,
    			EndDate:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MaintenanceWindow } from "jsr:@ubx/sdk-aws/aws/ssm/maintenance_window";

    export default stack("payments", () => {
      intent({ summary: "payments own maintenance window" });

      resource(MaintenanceWindow, "ci-runner", {
        cutoff: 1,
        duration: 7200,
        name: "ci-runner",
        schedule: "example",
        allowUnassociatedTargets: true,
        description: "Managed by ubx.",
        enabled: true,
        endDate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssm import MaintenanceWindow, MaintenanceWindowConfig

    def describe():
        ubx.intent("payments own maintenance window")

        ubx.resource(MaintenanceWindow, "ci-runner", MaintenanceWindowConfig(
            cutoff=1,
            duration=7200,
            name="ci-runner",
            schedule="example",
            allow_unassociated_targets=True,
            description="Managed by ubx.",
            enabled=True,
            end_date="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssm_maintenance_window called "ci-runner", with cutoff
    1, duration 7200, schedule example, allow unassociated targets
    true, description Managed by ubx., enabled true, end date
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow_unassociated_targets" type="bool">
  Optional.
</ResponseField>

<ResponseField name="cutoff" type="number" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="end_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schedule_offset" type="number">
  Optional.
</ResponseField>

<ResponseField name="schedule_timezone" type="string">
  Optional.
</ResponseField>

<ResponseField name="start_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
