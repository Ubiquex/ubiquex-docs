---
title: "aws_cloudcontrolapi_resource"
description: "Real, generated bindings for `aws_cloudcontrolapi_resource`."
---

`aws_cloudcontrolapi_resource` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudcontrolapi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudcontrolapi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource"})

    		ubx.Resource(cloudcontrolapi.Resource, "ci-runner", cloudcontrolapi.ResourceConfig{
    			DesiredState:  "example",
    			TypeName:      "example-type-name",
    			RoleArn:       "arn:aws:iam::123456789012:policy/ci",
    			TypeVersionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Resource } from "jsr:@ubx/sdk-aws/aws/cloudcontrolapi/resource";

    export default stack("payments", () => {
      intent({ summary: "payments own resource" });

      resource(Resource, "ci-runner", {
        desiredState: "example",
        typeName: "example-type-name",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        typeVersionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudcontrolapi import Resource, ResourceConfig

    def describe():
        ubx.intent("payments own resource")

        ubx.resource(Resource, "ci-runner", ResourceConfig(
            desired_state="example",
            type_name="example-type-name",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            type_version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudcontrolapi_resource, with desired state example,
    type name example-type-name, role arn
    arn:aws:iam::123456789012:policy/ci, type version id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="desired_state" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="schema" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type_version_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="properties" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schema" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
