---
title: "aws_account_alternate_contact"
description: "Real, generated bindings for `aws_account_alternate_contact`."
---

`aws_account_alternate_contact` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	account "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/account"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own alternate contact"})

    		ubx.Resource(account.AlternateContact, "ci-runner", account.AlternateContactConfig{
    			AlternateContactType: "example",
    			EmailAddress:         "example",
    			Name:                 "ci-runner",
    			PhoneNumber:          "example",
    			Title:                "example",
    			AccountId:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlternateContact } from "jsr:@ubx/sdk-aws/aws/account/alternate_contact";

    export default stack("payments", () => {
      intent({ summary: "payments own alternate contact" });

      resource(AlternateContact, "ci-runner", {
        alternateContactType: "example",
        emailAddress: "example",
        name: "ci-runner",
        phoneNumber: "example",
        title: "example",
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.account import AlternateContact, AlternateContactConfig

    def describe():
        ubx.intent("payments own alternate contact")

        ubx.resource(AlternateContact, "ci-runner", AlternateContactConfig(
            alternate_contact_type="example",
            email_address="example",
            name="ci-runner",
            phone_number="example",
            title="example",
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_account_alternate_contact called "ci-runner", with
    alternate contact type example, email address example, phone
    number example, title example, account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="alternate_contact_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="email_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="phone_number" type="string" required>
  Required.
</ResponseField>

<ResponseField name="title" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
