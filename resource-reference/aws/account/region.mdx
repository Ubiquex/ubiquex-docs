---
title: "aws_account_region"
description: "Real, generated bindings for `aws_account_region`."
---

`aws_account_region` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/account"

    cfg := account.RegionConfig{
        Enabled: true,
        RegionName: "example-region-name",
        AccountId: "example",
    }
    ubx.Resource(account.Region, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Region } from "jsr:@ubx/sdk-aws/aws/account/region";

    resource(Region, "example", {
      enabled: true,
      regionName: "example",
      accountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.account import Region, RegionConfig

    cfg = RegionConfig(
        enabled=True,
        region_name="example",
        account_id="example",
    )
    ubx.resource(Region, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_account_region with enabled, region_name,
account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="opt_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
