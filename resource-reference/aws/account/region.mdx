---
title: "aws_account_region"
description: "Real, generated bindings for `aws_account_region`."
---

`aws_account_region` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	account "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/account"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region"})

    		ubx.Resource(account.Region, "ci-runner", account.RegionConfig{
    			Enabled:    true,
    			RegionName: "example-region-name",
    			AccountId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Region } from "jsr:@ubx/sdk-aws/aws/account/region";

    export default stack("payments", () => {
      intent({ summary: "payments own region" });

      resource(Region, "ci-runner", {
        enabled: true,
        regionName: "example-region-name",
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.account import Region, RegionConfig

    def describe():
        ubx.intent("payments own region")

        ubx.resource(Region, "ci-runner", RegionConfig(
            enabled=True,
            region_name="example-region-name",
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_account_region, with enabled true, region name
    example-region-name, account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="opt_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
