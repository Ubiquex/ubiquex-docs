---
title: "aws_deadline_queue_limit_association"
description: "`aws_deadline_queue_limit_association` is the real, actual link between an `aws_deadline_queue` and an `aws_deadline_limit`."
---

A limit defined but never associated with a queue never actually constrains any job.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deadline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/deadline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own queue limit association"})

    		ubx.Resource(deadline.QueueLimitAssociation, "example", deadline.QueueLimitAssociationConfig{
    			FarmId:  "example",
    			LimitId: "example",
    			QueueId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QueueLimitAssociation } from "@ubx/sdk-aws/aws/deadline/queue_limit_association";

    export default stack("example", () => {
      intent({ summary: "example own queue limit association" });

      resource(QueueLimitAssociation, "example", {
        farmId: "example",
        limitId: "example",
        queueId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.deadline import QueueLimitAssociation, QueueLimitAssociationConfig

    def describe():
        ubx.intent("example own queue limit association")

        ubx.resource(QueueLimitAssociation, "example", QueueLimitAssociationConfig(
            farm_id="example",
            limit_id="example",
            queue_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_deadline_queue_limit_association, with farm id
    example, limit id example, queue id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="farm_id" type="string" required>
  The unique identifier of the farm that contains the queue and limit to be associated. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="limit_id" type="string" required>
  The unique identifier of the Deadline Cloud limit to associate with the queue. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="queue_id" type="string" required>
  The unique identifier of the Deadline Cloud queue that is associated with the specified limit. Required. **(AI-inferred)**
</ResponseField>
