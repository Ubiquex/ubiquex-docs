---
title: "aws_deadline_monitor"
description: "`aws_deadline_monitor` is the real, separate web application Deadline Cloud provides for creating, managing, and monitoring farms, fleets, jobs..."
---

`aws_deadline_monitor` is the real, separate web application Deadline Cloud provides for creating, managing, and monitoring farms, fleets, jobs, and users, distinct from the underlying `aws_deadline_farm` and its resources, which can also be managed entirely through the API or CLI without it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	deadline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/deadline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own monitor"})

    		ubx.Resource(deadline.Monitor, "ci-runner", deadline.MonitorConfig{
    			DisplayName:               "example-display-name",
    			IdentityCenterInstanceArn: "arn:aws:iam::123456789012:policy/ci",
    			RoleArn:                   "arn:aws:iam::123456789012:policy/ci",
    			Subdomain:                 "example",
    			IdentityCenterRegion:      "example",
    			Tags:                      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Monitor } from "./local-sdk/aws/sdk/typescript/aws/deadline/monitor.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own monitor" });

      resource(Monitor, "ci-runner", {
        displayName: "example-display-name",
        identityCenterInstanceArn: "arn:aws:iam::123456789012:policy/ci",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        subdomain: "example",
        identityCenterRegion: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.deadline import Monitor, MonitorConfig

    def describe():
        ubx.intent("payments own monitor")

        ubx.resource(Monitor, "ci-runner", MonitorConfig(
            display_name="example-display-name",
            identity_center_instance_arn="arn:aws:iam::123456789012:policy/ci",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            subdomain="example",
            identity_center_region="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_deadline_monitor, with display name
    example-display-name, identity center instance arn
    arn:aws:iam::123456789012:policy/ci, role arn
    arn:aws:iam::123456789012:policy/ci, subdomain example, identity
    center region example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="display_name" type="string" required>
  A human-readable name for the Deadline Cloud monitor, used to identify it in the AWS console and APIs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity_center_instance_arn" type="string" required>
  The ARN of the IAM Identity Center instance used to manage user access and single sign-on for the AWS Deadline Cloud monitor. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity_center_region" type="string">
  The AWS region where IAM Identity Center is enabled. Required when Identity Center is in a different region than the monitor. Optional.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The Amazon Resource Name (ARN) of the IAM role that the Deadline Cloud monitor uses to access the storage resources (such as the S3 bucket and file system) associated with the Deadline Cloud environment. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="subdomain" type="string" required>
  Specifies the subdomain that forms part of the monitor's unique web address, used to access the Deadline Cloud monitor endpoint. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      A key for a tag assigned to the AWS Deadline Cloud monitor, used to categorize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Deadline Cloud monitor. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity_center_application_arn" type="string">
  The ARN of the AWS IAM Identity Center application that AWS Deadline Cloud creates and associates with this monitor. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="monitor_id" type="string">
  The unique identifier assigned to the monitor. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The URL of the AWS Deadline monitor, used to access the monitor's web-based user interface. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
