---
title: "aws_appmesh_route"
description: "Real, generated bindings for `aws_appmesh_route`."
---

`aws_appmesh_route` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appmesh "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appmesh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own route"})

    		ubx.Resource(appmesh.Route, "ci-runner", appmesh.RouteConfig{
    			MeshName:          "example-mesh-name",
    			Name:              "ci-runner",
    			VirtualRouterName: "example-virtual-router-name",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Route } from "jsr:@ubx/sdk-aws/aws/appmesh/route";

    export default stack("payments", () => {
      intent({ summary: "payments own route" });

      resource(Route, "ci-runner", {
        meshName: "example-mesh-name",
        name: "ci-runner",
        virtualRouterName: "example-virtual-router-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appmesh import Route, RouteConfig

    def describe():
        ubx.intent("payments own route")

        ubx.resource(Route, "ci-runner", RouteConfig(
            mesh_name="example-mesh-name",
            name="ci-runner",
            virtual_router_name="example-virtual-router-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appmesh_route called "ci-runner", with mesh name
    example-mesh-name, virtual router name
    example-virtual-router-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mesh_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mesh_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_router_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mesh_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
