---
title: "aws_appmesh_virtual_gateway"
description: "Real, generated bindings for `aws_appmesh_virtual_gateway`."
---

`aws_appmesh_virtual_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appmesh"

    cfg := appmesh.VirtualGatewayConfig{
        MeshName: "example-mesh-name",
        Name: "example-name",
        MeshOwner: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appmesh.VirtualGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VirtualGateway } from
      "jsr:@ubx/sdk-aws/aws/appmesh/virtual_gateway";

    resource(VirtualGateway, "example", {
      meshName: "example",
      name: "example",
      meshOwner: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appmesh import (
        VirtualGateway,
        VirtualGatewayConfig,
    )

    cfg = VirtualGatewayConfig(
        mesh_name="example",
        name="example",
        mesh_owner="example",
        tags={"key": "value"},
    )
    ubx.resource(VirtualGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appmesh_virtual_gateway with mesh_name, name,
mesh_owner, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mesh_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mesh_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mesh_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
