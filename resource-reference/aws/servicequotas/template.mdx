---
title: "aws_servicequotas_template"
description: "Real, generated bindings for `aws_servicequotas_template`."
---

`aws_servicequotas_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/servicequotas"

    cfg := servicequotas.TemplateConfig{
        QuotaCode: "example",
        ServiceCode: "example",
        Value: 1,
        AwsRegion: "example",
    }
    ubx.Resource(servicequotas.Template, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Template } from
      "jsr:@ubx/sdk-aws/aws/servicequotas/template";

    resource(Template, "example", {
      quotaCode: "example",
      serviceCode: "example",
      value: 1,
      awsRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicequotas import Template, TemplateConfig

    cfg = TemplateConfig(
        quota_code="example",
        service_code="example",
        value=1,
        aws_region="example",
    )
    ubx.resource(Template, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicequotas_template with quota_code,
service_code, value, aws_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="quota_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="value" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="global_quota" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="quota_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="unit" type="string">
  Computed by AWS after `ship`.
</ResponseField>
