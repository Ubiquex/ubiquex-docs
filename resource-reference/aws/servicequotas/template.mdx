---
title: "aws_servicequotas_template"
description: "Real, generated bindings for `aws_servicequotas_template`."
---

`aws_servicequotas_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicequotas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicequotas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own template"})

    		ubx.Resource(servicequotas.Template, "ci-runner", servicequotas.TemplateConfig{
    			QuotaCode:   "example",
    			ServiceCode: "example",
    			Value:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Template } from "jsr:@ubx/sdk-aws/aws/servicequotas/template";

    export default stack("payments", () => {
      intent({ summary: "payments own template" });

      resource(Template, "ci-runner", {
        quotaCode: "example",
        serviceCode: "example",
        value: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicequotas import Template, TemplateConfig

    def describe():
        ubx.intent("payments own template")

        ubx.resource(Template, "ci-runner", TemplateConfig(
            quota_code="example",
            service_code="example",
            value=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicequotas_template, with quota code example,
    service code example, value 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="quota_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="value" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="global_quota" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="quota_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="unit" type="string">
  Computed by AWS after `ship`.
</ResponseField>
