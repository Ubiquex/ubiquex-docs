---
title: "aws_servicequotas_template_association"
description: "Real, generated bindings for `aws_servicequotas_template_association`."
---

`aws_servicequotas_template_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/servicequotas"

    cfg := servicequotas.TemplateAssociationConfig{
        SkipDestroy: true,
    }
    ubx.Resource(servicequotas.TemplateAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TemplateAssociation } from
      "jsr:@ubx/sdk-aws/aws/servicequotas/template_association";

    resource(TemplateAssociation, "example", {
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicequotas import (
        TemplateAssociation,
        TemplateAssociationConfig,
    )

    cfg = TemplateAssociationConfig(
        skip_destroy=True,
    )
    ubx.resource(TemplateAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicequotas_template_association with
skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
