---
title: "aws_inspector2_delegated_admin_account"
description: "Real, generated bindings for `aws_inspector2_delegated_admin_account`."
---

`aws_inspector2_delegated_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delegated admin account"})

    		ubx.Resource(inspector2.DelegatedAdminAccount, "ci-runner", inspector2.DelegatedAdminAccountConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DelegatedAdminAccount } from "jsr:@ubx/sdk-aws/aws/inspector2/delegated_admin_account";

    export default stack("payments", () => {
      intent({ summary: "payments own delegated admin account" });

      resource(DelegatedAdminAccount, "ci-runner", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.inspector2 import DelegatedAdminAccount, DelegatedAdminAccountConfig

    def describe():
        ubx.intent("payments own delegated admin account")

        ubx.resource(DelegatedAdminAccount, "ci-runner", DelegatedAdminAccountConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_inspector2_delegated_admin_account, with account id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="relationship_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
