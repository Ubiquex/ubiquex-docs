---
title: "aws_inspector2_enabler"
description: "Real, generated bindings for `aws_inspector2_enabler`."
---

`aws_inspector2_enabler` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector2"

    cfg := inspector2.EnablerConfig{
        AccountIds: []string{"example"},
        ResourceTypes: []string{"example"},
    }
    ubx.Resource(inspector2.Enabler, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Enabler } from
      "jsr:@ubx/sdk-aws/aws/inspector2/enabler";

    resource(Enabler, "example", {
      accountIds: ["example"],
      resourceTypes: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.inspector2 import Enabler, EnablerConfig

    cfg = EnablerConfig(
        account_ids=["example"],
        resource_types=["example"],
    )
    ubx.resource(Enabler, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_inspector2_enabler with account_ids,
resource_types.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_types" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
