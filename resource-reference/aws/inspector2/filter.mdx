---
title: "aws_inspector2_filter"
description: "Real, generated bindings for `aws_inspector2_filter`."
---

`aws_inspector2_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector2"

    cfg := inspector2.FilterConfig{
        Action: "example",
        Name: "example-name",
        Description: "example",
        Reason: "example",
    }
    ubx.Resource(inspector2.Filter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Filter } from "jsr:@ubx/sdk-aws/aws/inspector2/filter";

    resource(Filter, "example", {
      action: "example",
      name: "example",
      description: "example",
      reason: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.inspector2 import Filter, FilterConfig

    cfg = FilterConfig(
        action="example",
        name="example",
        description="example",
        reason="example",
    )
    ubx.resource(Filter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_inspector2_filter with action, name, description,
reason.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="reason" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
