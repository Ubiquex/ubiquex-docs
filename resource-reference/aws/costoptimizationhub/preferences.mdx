---
title: "aws_costoptimizationhub_preferences"
description: "Real, generated bindings for `aws_costoptimizationhub_preferences`."
---

`aws_costoptimizationhub_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/costoptimizationhub"

    cfg := costoptimizationhub.PreferencesConfig{
        MemberAccountDiscountVisibility: "example",
        SavingsEstimationMode: "example",
    }
    ubx.Resource(costoptimizationhub.Preferences, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Preferences } from
      "jsr:@ubx/sdk-aws/aws/costoptimizationhub/preferences";

    resource(Preferences, "example", {
      memberAccountDiscountVisibility: "example",
      savingsEstimationMode: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.costoptimizationhub import (
        Preferences,
        PreferencesConfig,
    )

    cfg = PreferencesConfig(
        member_account_discount_visibility="example",
        savings_estimation_mode="example",
    )
    ubx.resource(Preferences, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_costoptimizationhub_preferences with
member_account_discount_visibility, savings_estimation_mode.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="member_account_discount_visibility" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_account_discount_visibility" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
