---
title: "aws_costoptimizationhub_preferences"
description: "Real, generated bindings for `aws_costoptimizationhub_preferences`."
---

`aws_costoptimizationhub_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	costoptimizationhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/costoptimizationhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own preferences"})

    		ubx.Resource(costoptimizationhub.Preferences, "ci-runner", costoptimizationhub.PreferencesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Preferences } from "jsr:@ubx/sdk-aws/aws/costoptimizationhub/preferences";

    export default stack("payments", () => {
      intent({ summary: "payments own preferences" });

      resource(Preferences, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.costoptimizationhub import Preferences, PreferencesConfig

    def describe():
        ubx.intent("payments own preferences")

        ubx.resource(Preferences, "ci-runner", PreferencesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_costoptimizationhub_preferences, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="member_account_discount_visibility" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_account_discount_visibility" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
