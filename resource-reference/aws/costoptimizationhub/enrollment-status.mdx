---
title: "aws_costoptimizationhub_enrollment_status"
description: "Real, generated bindings for `aws_costoptimizationhub_enrollment_status`."
---

`aws_costoptimizationhub_enrollment_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/costoptimizationhub"

    cfg := costoptimizationhub.EnrollmentStatusConfig{
        IncludeMemberAccounts: true,
    }
    ubx.Resource(
        costoptimizationhub.EnrollmentStatus,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EnrollmentStatus } from
      "jsr:@ubx/sdk-aws/aws/costoptimizationhub/enrollment_status";

    resource(EnrollmentStatus, "example", {
      includeMemberAccounts: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.costoptimizationhub import (
        EnrollmentStatus,
        EnrollmentStatusConfig,
    )

    cfg = EnrollmentStatusConfig(
        include_member_accounts=True,
    )
    ubx.resource(EnrollmentStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_costoptimizationhub_enrollment_status with
include_member_accounts.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
