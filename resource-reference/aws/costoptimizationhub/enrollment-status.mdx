---
title: "aws_costoptimizationhub_enrollment_status"
description: "Real, generated bindings for `aws_costoptimizationhub_enrollment_status`."
---

`aws_costoptimizationhub_enrollment_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	costoptimizationhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/costoptimizationhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own enrollment status"})

    		ubx.Resource(costoptimizationhub.EnrollmentStatus, "ci-runner", costoptimizationhub.EnrollmentStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnrollmentStatus } from "jsr:@ubx/sdk-aws/aws/costoptimizationhub/enrollment_status";

    export default stack("payments", () => {
      intent({ summary: "payments own enrollment status" });

      resource(EnrollmentStatus, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.costoptimizationhub import EnrollmentStatus, EnrollmentStatusConfig

    def describe():
        ubx.intent("payments own enrollment status")

        ubx.resource(EnrollmentStatus, "ci-runner", EnrollmentStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_costoptimizationhub_enrollment_status, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
