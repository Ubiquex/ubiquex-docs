---
title: "aws_lakeformation_lf_tag_expression"
description: "Real, generated bindings for `aws_lakeformation_lf_tag_expression`."
---

`aws_lakeformation_lf_tag_expression` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lf tag expression"})

    		ubx.Resource(lakeformation.LfTagExpression, "ci-runner", lakeformation.LfTagExpressionConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LfTagExpression } from "jsr:@ubx/sdk-aws/aws/lakeformation/lf_tag_expression";

    export default stack("payments", () => {
      intent({ summary: "payments own lf tag expression" });

      resource(LfTagExpression, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import LfTagExpression, LfTagExpressionConfig

    def describe():
        ubx.intent("payments own lf tag expression")

        ubx.resource(LfTagExpression, "ci-runner", LfTagExpressionConfig(
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_lf_tag_expression called "ci-runner",
    with description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="catalog_id" type="string">
  The ID of the Data Catalog. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the LF-Tag Expression. Optional.
</ResponseField>

<ResponseField name="expression" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="tag_key" type="string" required>
    </ResponseField>
    <ResponseField name="tag_values" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the LF-Tag Expression. Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="catalog_id" type="string">
  The ID of the Data Catalog. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="expression" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="tag_key" type="string" required>
    </ResponseField>
    <ResponseField name="tag_values" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
