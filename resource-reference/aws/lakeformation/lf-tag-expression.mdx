---
title: "aws_lakeformation_lf_tag_expression"
description: "Real, generated bindings for `aws_lakeformation_lf_tag_expression`."
---

`aws_lakeformation_lf_tag_expression` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lakeformation"

    cfg := lakeformation.LfTagExpressionConfig{
        Name: "example-name",
        CatalogId: "example",
        Description: "example",
    }
    ubx.Resource(lakeformation.LfTagExpression, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LfTagExpression } from
      "jsr:@ubx/sdk-aws/aws/lakeformation/lf_tag_expression";

    resource(LfTagExpression, "example", {
      name: "example",
      catalogId: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lakeformation import (
        LfTagExpression,
        LfTagExpressionConfig,
    )

    cfg = LfTagExpressionConfig(
        name="example",
        catalog_id="example",
        description="example",
    )
    ubx.resource(LfTagExpression, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lakeformation_lf_tag_expression with name,
catalog_id, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
