---
title: "aws_lakeformation_permissions"
description: "Real, generated bindings for `aws_lakeformation_permissions`."
---

`aws_lakeformation_permissions` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own permissions"})

    		ubx.Resource(lakeformation.Permissions, "ci-runner", lakeformation.PermissionsConfig{
    			Permissions:     []string{"example"},
    			Principal:       "example",
    			CatalogId:       "example",
    			CatalogResource: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Permissions } from "jsr:@ubx/sdk-aws/aws/lakeformation/permissions";

    export default stack("payments", () => {
      intent({ summary: "payments own permissions" });

      resource(Permissions, "ci-runner", {
        permissions: ["example"],
        principal: "example",
        catalogId: "example",
        catalogResource: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import Permissions, PermissionsConfig

    def describe():
        ubx.intent("payments own permissions")

        ubx.resource(Permissions, "ci-runner", PermissionsConfig(
            permissions=["example"],
            principal="example",
            catalog_id="example",
            catalog_resource=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_permissions, with permissions a set
    value, principal example, catalog id example, catalog resource
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="catalog_resource" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="permissions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="permissions_with_grant_option" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="permissions_with_grant_option" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
