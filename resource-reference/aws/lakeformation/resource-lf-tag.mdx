---
title: "aws_lakeformation_resource_lf_tag"
description: "Real, generated bindings for `aws_lakeformation_resource_lf_tag`."
---

`aws_lakeformation_resource_lf_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lakeformation"

    cfg := lakeformation.ResourceLfTagConfig{
        CatalogId: "example",
    }
    ubx.Resource(lakeformation.ResourceLfTag, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceLfTag } from
      "jsr:@ubx/sdk-aws/aws/lakeformation/resource_lf_tag";

    resource(ResourceLfTag, "example", {
      catalogId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lakeformation import (
        ResourceLfTag,
        ResourceLfTagConfig,
    )

    cfg = ResourceLfTagConfig(
        catalog_id="example",
    )
    ubx.resource(ResourceLfTag, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lakeformation_resource_lf_tag with catalog_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
