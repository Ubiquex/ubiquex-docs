---
title: "aws_lakeformation_resource_lf_tag"
description: "Real, generated bindings for `aws_lakeformation_resource_lf_tag`."
---

`aws_lakeformation_resource_lf_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource lf tag"})

    		ubx.Resource(lakeformation.ResourceLfTag, "ci-runner", lakeformation.ResourceLfTagConfig{
    			CatalogId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceLfTag } from "jsr:@ubx/sdk-aws/aws/lakeformation/resource_lf_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own resource lf tag" });

      resource(ResourceLfTag, "ci-runner", {
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import ResourceLfTag, ResourceLfTagConfig

    def describe():
        ubx.intent("payments own resource lf tag")

        ubx.resource(ResourceLfTag, "ci-runner", ResourceLfTagConfig(
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_resource_lf_tag, with catalog id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
