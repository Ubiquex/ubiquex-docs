---
title: "aws_lakeformation_opt_in"
description: "Real, generated bindings for `aws_lakeformation_opt_in`."
---

`aws_lakeformation_opt_in` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"

    cfg := lakeformation.OptInConfig{
    }
    ubx.Resource(lakeformation.OptIn, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OptIn } from
      "jsr:@ubx/sdk-aws/aws/lakeformation/opt_in";

    resource(OptIn, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lakeformation import OptIn, OptInConfig

    cfg = OptInConfig(
    )
    ubx.resource(OptIn, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lakeformation_opt_in.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="last_modified" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
