---
title: "aws_lakeformation_lf_tag"
description: "Real, generated bindings for `aws_lakeformation_lf_tag`."
---

`aws_lakeformation_lf_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lf tag"})

    		ubx.Resource(lakeformation.LfTag, "ci-runner", lakeformation.LfTagConfig{
    			Key:    "example",
    			Values: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LfTag } from "jsr:@ubx/sdk-aws/aws/lakeformation/lf_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own lf tag" });

      resource(LfTag, "ci-runner", {
        key: "example",
        values: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import LfTag, LfTagConfig

    def describe():
        ubx.intent("payments own lf tag")

        ubx.resource(LfTag, "ci-runner", LfTagConfig(
            key="example",
            values=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_lf_tag, with key example, values a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="values" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
