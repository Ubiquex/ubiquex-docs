---
title: "aws_lakeformation_identity_center_configuration"
description: "Real, generated bindings for `aws_lakeformation_identity_center_configuration`."
---

`aws_lakeformation_identity_center_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity center configuration"})

    		ubx.Resource(lakeformation.IdentityCenterConfiguration, "ci-runner", lakeformation.IdentityCenterConfigurationConfig{
    			InstanceArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityCenterConfiguration } from "jsr:@ubx/sdk-aws/aws/lakeformation/identity_center_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own identity center configuration" });

      resource(IdentityCenterConfiguration, "ci-runner", {
        instanceArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import IdentityCenterConfiguration, IdentityCenterConfigurationConfig

    def describe():
        ubx.intent("payments own identity center configuration")

        ubx.resource(IdentityCenterConfiguration, "ci-runner", IdentityCenterConfigurationConfig(
            instance_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_identity_center_configuration, with
    instance arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="application_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_share" type="string">
  Computed by AWS after `ship`.
</ResponseField>
