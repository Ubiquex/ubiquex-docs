---
title: "aws_lakeformation_identity_center_configuration"
description: "Real, generated bindings for `aws_lakeformation_identity_center_configuration`."
---

`aws_lakeformation_identity_center_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lakeformation"

    cfg := lakeformation.IdentityCenterConfigurationConfig{
        InstanceArn: "example",
        CatalogId: "example",
    }
    ubx.Resource(
        lakeformation.IdentityCenterConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityCenterConfiguration } from
      "jsr:@ubx/sdk-aws/aws/lakeformation/identity_center_configuration";

    resource(IdentityCenterConfiguration, "example", {
      instanceArn: "example",
      catalogId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lakeformation import (
        IdentityCenterConfiguration,
        IdentityCenterConfigurationConfig,
    )

    cfg = IdentityCenterConfigurationConfig(
        instance_arn="example",
        catalog_id="example",
    )
    ubx.resource(IdentityCenterConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lakeformation_identity_center_configuration with
instance_arn, catalog_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="application_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_share" type="string">
  Computed by AWS after `ship`.
</ResponseField>
