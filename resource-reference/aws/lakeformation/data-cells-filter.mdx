---
title: "aws_lakeformation_data_cells_filter"
description: "Real, generated bindings for `aws_lakeformation_data_cells_filter`."
---

`aws_lakeformation_data_cells_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data cells filter"})

    		ubx.Resource(lakeformation.DataCellsFilter, "ci-runner", lakeformation.DataCellsFilterConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataCellsFilter } from "jsr:@ubx/sdk-aws/aws/lakeformation/data_cells_filter";

    export default stack("payments", () => {
      intent({ summary: "payments own data cells filter" });

      resource(DataCellsFilter, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lakeformation import DataCellsFilter, DataCellsFilterConfig

    def describe():
        ubx.intent("payments own data cells filter")

        ubx.resource(DataCellsFilter, "ci-runner", DataCellsFilterConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lakeformation_data_cells_filter, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
