---
title: "aws_lakeformation_data_cells_filter"
description: "Real, generated bindings for `aws_lakeformation_data_cells_filter`."
---

`aws_lakeformation_data_cells_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lakeformation"

    cfg := lakeformation.DataCellsFilterConfig{
    }
    ubx.Resource(lakeformation.DataCellsFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DataCellsFilter } from
      "jsr:@ubx/sdk-aws/aws/lakeformation/data_cells_filter";

    resource(DataCellsFilter, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lakeformation import (
        DataCellsFilter,
        DataCellsFilterConfig,
    )

    cfg = DataCellsFilterConfig(
    )
    ubx.resource(DataCellsFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lakeformation_data_cells_filter.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
