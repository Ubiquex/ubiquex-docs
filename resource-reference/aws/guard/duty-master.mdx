---
title: "aws_guard_duty_master"
description: "`aws_guard_duty_master` associates a real GuardDuty member account with an administrator account through GuardDuty's original..."
---

`aws_guard_duty_master` associates a real GuardDuty member account with an administrator account through GuardDuty's original, invitation-based multi-account mechanism, sending or accepting the actual invitation between the two. "Master" is legacy terminology AWS has since replaced with "administrator" across GuardDuty's own API (GetMasterAccount and similar calls are deprecated in favor of administrator-account equivalents), and this whole invitation-based mechanism itself is superseded, though not removed, by GuardDuty's newer AWS Organizations-integrated administrator delegation, which AWS now recommends instead.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	guard "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/guard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own duty master"})

    		ubx.Resource(guard.DutyMaster, "ci-runner", guard.DutyMasterConfig{
    			DetectorId:   "example",
    			MasterId:     "example",
    			InvitationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DutyMaster } from "./local-sdk/aws/sdk/typescript/aws/guard/duty_master.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own duty master" });

      resource(DutyMaster, "ci-runner", {
        detectorId: "example",
        masterId: "example",
        invitationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.guard import DutyMaster, DutyMasterConfig

    def describe():
        ubx.intent("payments own duty master")

        ubx.resource(DutyMaster, "ci-runner", DutyMasterConfig(
            detector_id="example",
            master_id="example",
            invitation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guard_duty_master, with detector id example, master id
    example, invitation id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="detector_id" type="string" required>
  Unique ID of the detector of the GuardDuty member account. Required.
</ResponseField>

<ResponseField name="invitation_id" type="string">
  Value used to validate the master account to the member account. Optional.
</ResponseField>

<ResponseField name="master_id" type="string" required>
  ID of the account used as the master account. Required.
</ResponseField>
