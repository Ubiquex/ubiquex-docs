---
title: "aws_datazone_domain"
description: "Real, generated bindings for `aws_datazone_domain`."
---

`aws_datazone_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"

    cfg := datazone.DomainConfig{
        DomainExecutionRole: "example",
        Name: "example-name",
        Description: "example",
        DomainVersion: "example",
    }
    ubx.Resource(datazone.Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Domain } from "jsr:@ubx/sdk-aws/aws/datazone/domain";

    resource(Domain, "example", {
      domainExecutionRole: "example",
      name: "example",
      description: "example",
      domainVersion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datazone import Domain, DomainConfig

    cfg = DomainConfig(
        domain_execution_role="example",
        name="example",
        description="example",
        domain_version="example",
    )
    ubx.resource(Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datazone_domain with domain_execution_role, name,
description, domain_version.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_execution_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="skip_deletion_check" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="portal_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="root_domain_unit_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
