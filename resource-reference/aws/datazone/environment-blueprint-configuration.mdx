---
title: "aws_datazone_environment_blueprint_configuration"
description: "Real, generated bindings for `aws_datazone_environment_blueprint_configuration`."
---

`aws_datazone_environment_blueprint_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"

    cfg := datazone.EnvironmentBlueprintConfigurationConfig{
        DomainId: "example",
        EnabledRegions: []string{"example"},
        EnvironmentBlueprintId: "example",
        GlobalParameters: map[string]string{"key": "value"},
        ManageAccessRoleArn: "example",
    }
    ubx.Resource(
        datazone.EnvironmentBlueprintConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EnvironmentBlueprintConfiguration } from
      "jsr:@ubx/sdk-aws/aws/datazone/environment_blueprint_configuration";

    resource(EnvironmentBlueprintConfiguration, "example", {
      domainId: "example",
      enabledRegions: ["example"],
      environmentBlueprintId: "example",
      globalParameters: { key: "value" },
      manageAccessRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datazone import (
        EnvironmentBlueprintConfiguration,
        EnvironmentBlueprintConfigurationConfig,
    )

    cfg = EnvironmentBlueprintConfigurationConfig(
        domain_id="example",
        enabled_regions=["example"],
        environment_blueprint_id="example",
        global_parameters={"key": "value"},
        manage_access_role_arn="example",
    )
    ubx.resource(EnvironmentBlueprintConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datazone_environment_blueprint_configuration with
domain_id, enabled_regions, environment_blueprint_id,
global_parameters, manage_access_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled_regions" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="environment_blueprint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="global_parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="manage_access_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="provisioning_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regional_parameters" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
