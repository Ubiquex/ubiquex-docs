---
title: "aws_datazone_environment_blueprint_configuration"
description: "Real, generated bindings for `aws_datazone_environment_blueprint_configuration`."
---

`aws_datazone_environment_blueprint_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment blueprint configuration"})

    		ubx.Resource(datazone.EnvironmentBlueprintConfiguration, "ci-runner", datazone.EnvironmentBlueprintConfigurationConfig{
    			DomainId:               "example",
    			EnabledRegions:         []string{"example"},
    			EnvironmentBlueprintId: "example",
    			GlobalParameters:       map[string]string{"managed-by": "ubx"},
    			ManageAccessRoleArn:    "arn:aws:iam::123456789012:policy/ci",
    			ProvisioningRoleArn:    "arn:aws:iam::123456789012:policy/ci",
    			RegionalParameters:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentBlueprintConfiguration } from "jsr:@ubx/sdk-aws/aws/datazone/environment_blueprint_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own environment blueprint configuration" });

      resource(EnvironmentBlueprintConfiguration, "ci-runner", {
        domainId: "example",
        enabledRegions: ["example"],
        environmentBlueprintId: "example",
        globalParameters: { "managed-by": "ubx" },
        manageAccessRoleArn: "arn:aws:iam::123456789012:policy/ci",
        provisioningRoleArn: "arn:aws:iam::123456789012:policy/ci",
        regionalParameters: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import EnvironmentBlueprintConfiguration, EnvironmentBlueprintConfigurationConfig

    def describe():
        ubx.intent("payments own environment blueprint configuration")

        ubx.resource(EnvironmentBlueprintConfiguration, "ci-runner", EnvironmentBlueprintConfigurationConfig(
            domain_id="example",
            enabled_regions=["example"],
            environment_blueprint_id="example",
            global_parameters={"managed-by": "ubx"},
            manage_access_role_arn="arn:aws:iam::123456789012:policy/ci",
            provisioning_role_arn="arn:aws:iam::123456789012:policy/ci",
            regional_parameters={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datazone_environment_blueprint_configuration, with
    domain id example, enabled regions a set value, environment
    blueprint id example, global parameters a set value, manage
    access role arn arn:aws:iam::123456789012:policy/ci, provisioning
    role arn arn:aws:iam::123456789012:policy/ci, regional parameters
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled_regions" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="environment_blueprint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="global_parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="manage_access_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="provisioning_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regional_parameters" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
