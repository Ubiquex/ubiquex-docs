---
title: "aws_data_zone_policy_grant"
description: "`aws_data_zone_policy_grant` is the real, actual authorization grant that lets a specific principal perform an action, publishing an asset..."
---

`aws_data_zone_policy_grant` is the real, actual authorization grant that lets a specific principal perform an action, publishing an asset, creating an environment, within an `aws_data_zone_domain`, the enforcement mechanism behind DataZone's fine-grained governance model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own policy grant"})

    		ubx.Resource(datazone.PolicyGrant, "example", datazone.PolicyGrantConfig{
    			DomainIdentifier: "example",
    			EntityIdentifier: "example",
    			EntityType:       "example",
    			PolicyType:       "example",
    			Detail:           map[string]any{"add_to_project_member_pool": map[string]any{"include_child_domain_units": true}},
    			Principal:        map[string]any{"domain_unit": map[string]any{"domain_unit_designation": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyGrant } from "@ubx/sdk-aws/aws/datazone/policy_grant";

    export default stack("example", () => {
      intent({ summary: "example own policy grant" });

      resource(PolicyGrant, "example", {
        domainIdentifier: "example",
        entityIdentifier: "example",
        entityType: "example",
        policyType: "example",
        detail: { addToProjectMemberPool: { includeChildDomainUnits: true } },
        principal: { domainUnit: { domainUnitDesignation: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import PolicyGrant, PolicyGrantConfig
    from ubx.aws.datazone.policy_grant import PolicyGrant_Detail, PolicyGrant_Detail_AddToProjectMemberPool, PolicyGrant_Principal, PolicyGrant_Principal_DomainUnit

    def describe():
        ubx.intent("example own policy grant")

        ubx.resource(PolicyGrant, "example", PolicyGrantConfig(
            domain_identifier="example",
            entity_identifier="example",
            entity_type="example",
            policy_type="example",
            detail=PolicyGrant_Detail(add_to_project_member_pool=PolicyGrant_Detail_AddToProjectMemberPool(include_child_domain_units=True)),
            principal=PolicyGrant_Principal(domain_unit=PolicyGrant_Principal_DomainUnit(domain_unit_designation="example")),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_policy_grant, with domain identifier
    example, entity identifier example, entity type example, policy
    type example, detail a set value, principal a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="detail" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="add_to_project_member_pool" type="object">
      Configures the ADD_TO_PROJECT_MEMBER_POOL policy grant, determining the settings that allow the grantee to add members to a project's member pool. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          Whether the policy grant also applies to child domain units when adding a member to the project member pool. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_asset_type" type="object">
      For an AWS DataZone policy grant, this nested object configures the CreateAssetType policy, where its 'include_child_domain_units' field determines whether the permission to create asset types also applies to child domain units. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          Determines whether the permissions granted by this CreateAssetType policy are also applied to child domain units within the parent domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_domain_unit" type="object">
      Configures the 'Create Domain Unit' policy grant detail, an empty object that, when present, grants the principal permission to create domain units within the DataZone domain. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          When creating a domain unit policy grant, this boolean indicates whether the grant applies to child domain units as well as the target domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_environment" type="any">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="create_environment_from_blueprint" type="any">
      Specifies the policy grant detail that grants the principal permission to create an environment from a blueprint in Amazon Data Zone. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_environment_profile" type="object">
      This object contains the policy grant details for allowing a principal to create environment profiles in AWS DataZone, including the domain units under which the permission applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="domain_unit_id" type="string">
          The identifier of the DataZone domain unit to which the create environment profile policy grant applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_form_type" type="object">
      Specifies the policy grant details for creating form types, including whether the permission also applies to child domain units. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          Specifies whether the permission to create form types also applies to child domain units within the domain, extending the grant to subdomains. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_glossary" type="object">
      This object defines the policy grant details for creating glossaries in Amazon DataZone, allowing the specified principal to create glossary entries within the target domain or project. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          Specifies whether the permission granted to create glossaries also applies to child domain units of the specified domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_project" type="object">
      The create_project details of the policy grant, used when the policy type is set to CREATE_PROJECT, specifying the permissions for creating projects in Amazon Data Zone. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          When true, the permission to create projects is extended to all child domain units beneath the target domain unit, rather than being limited to that domain unit alone. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_project_from_project_profile" type="object">
      The create_project_from_project_profile object defines the policy grant configuration for allowing a principal to create projects from a project profile, typically containing the list of project profile identifiers the grant applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          Whether the policy grant for creating a project from a project profile also applies to child domain units, enabling the create-project action to be performed in those child domain units as well. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="project_profiles" type="list(string)">
          The list of DataZone project profile identifiers that the policy grant applies to, allowing the grantee to create projects from these specific project profiles. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="delegate_create_environment_profile" type="any">
      When enabled, this flag allows the policy grantee to delegate the ability to create environment profiles to other principals. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_domain_unit_owners" type="object">
      This nested object represents the details of an 'override domain unit owners' policy grant, with an `enabled` boolean indicating whether the grant overrides the domain unit's existing owner list. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          A boolean flag that, when true, extends the domain unit owner override to also apply to all child domain units of the specified domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="override_project_owners" type="object">
      This object contains the detail settings for a policy grant of type OVERRIDE_PROJECT_OWNERS, which grants the principal the ability to override project ownership within AWS DataZone. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_child_domain_units" type="bool">
          When set to true, the project owner override also applies to child domain units, propagating the ownership change down the domain unit hierarchy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The identifier of the Amazon DataZone domain where the policy grant is created. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="entity_identifier" type="string" required>
  The identifier of the Data Zone entity (such as a project, domain, or user) to which the policy grant applies. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="entity_type" type="string" required>
  Specifies the type of DataZone entity (such as 'project', 'user', or 'group') that this policy grant targets, used together with the entity identifier to define the grant's scope. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="policy_type" type="string" required>
  Specifies the type of policy grant being created, such as CREATE_DOMAIN_UNIT or DELETE_DOMAIN_UNIT, which dictates the specific permission granted on the target entity. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="principal" type="object">
  The user, group, or role to which the policy grant is assigned, specifying the principal type and identifier to control which entity receives the granted permissions. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="domain_unit" type="object">
      Identifies a domain unit as the principal for the policy grant, so that the grant applies to all users and groups within that domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="domain_unit_designation" type="string">
          Specifies the role (owner or member) within the domain unit principal that the policy grant applies to, controlling which users in that domain unit are granted the policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="domain_unit_grant_filter" type="object">
          Defines a filter that specifies whether the policy grant applies to all domain units within the domain unit principal or only to a specific list of domain unit IDs, used to scope the grant for a domain unit principal. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="all_domain_units_grant_filter" type="any">
              When set to an empty object, this field configures the policy grant to apply to all domain units within the DataZone domain. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="domain_unit_identifier" type="string">
          The identifier of the Amazon DataZone domain unit that acts as the principal for this policy grant, determining which domain unit receives the granted permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="group" type="object">
      Specifies the group principal for the policy grant, containing the unique identifier of the AWS DataZone group that receives the granted permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="group_identifier" type="string">
          The identifier of the group that is the principal receiving the policy grant in AWS DataZone. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="project" type="object">
      The project object representing the Amazon Data Zone project that will receive the permissions granted by this policy grant. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="project_designation" type="string">
          Specifies whether the project principal is identified by its project ID or its project name, enabling the policy grant to reference a project as the grant principal. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="project_grant_filter" type="object">
          Configures a domain unit filter that restricts the policy grant to only members of the project who belong to the specified domain unit. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="domain_unit_filter" type="object">
              Specifies a domain unit filter that scopes the project-level policy grant to apply only to the designated domain units within the data zone. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="domain_unit" type="string">
                  The identifier of the AWS Data Zone domain unit used by the domain unit filter to scope this policy grant for the specified project principal. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="include_child_domain_units" type="bool">
                  Specifies whether the policy grant to the project principal also applies to all child domain units underneath the domain unit selected in this filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="project_identifier" type="string">
          The identifier of the Amazon DataZone project that serves as the principal for the policy grant, granting permissions to that project. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user" type="object">
      Defines the user principal to which the policy grant is assigned, specifying the Amazon DataZone user by its unique identifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="all_users_grant_filter" type="any">
          A boolean flag that, when set to true, grants the policy to all users in the DataZone domain rather than to a specific user identifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_identifier" type="string">
          Specifies the user identifier of the AWS DataZone user who is the principal receiving this policy grant, determining which user the granted permissions apply to. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  Specifies the timestamp at which policy grant member was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Specifies the user who created the policy grant member. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grant_id" type="string">
  The unique identifier of the policy grant returned by the AddPolicyGrant API Computed by AWS after `ship`.
</ResponseField>
