---
title: "aws_datazone_project"
description: "Real, generated bindings for `aws_datazone_project`."
---

`aws_datazone_project` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"

    cfg := datazone.ProjectConfig{
        DomainIdentifier: "example",
        Name: "example-name",
        Description: "example",
        GlossaryTerms: []string{"example"},
    }
    ubx.Resource(datazone.Project, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Project } from "jsr:@ubx/sdk-aws/aws/datazone/project";

    resource(Project, "example", {
      domainIdentifier: "example",
      name: "example",
      description: "example",
      glossaryTerms: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datazone import Project, ProjectConfig

    cfg = ProjectConfig(
        domain_identifier="example",
        name="example",
        description="example",
        glossary_terms=["example"],
    )
    ubx.resource(Project, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datazone_project with domain_identifier, name,
description, glossary_terms.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="glossary_terms" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_deletion_check" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reasons" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
