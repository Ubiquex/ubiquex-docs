---
title: "aws_datazone_user_profile"
description: "Real, generated bindings for `aws_datazone_user_profile`."
---

`aws_datazone_user_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user profile"})

    		ubx.Resource(datazone.UserProfile, "ci-runner", datazone.UserProfileConfig{
    			DomainIdentifier: "example",
    			UserIdentifier:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserProfile } from "jsr:@ubx/sdk-aws/aws/datazone/user_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own user profile" });

      resource(UserProfile, "ci-runner", {
        domainIdentifier: "example",
        userIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import UserProfile, UserProfileConfig

    def describe():
        ubx.intent("payments own user profile")

        ubx.resource(UserProfile, "ci-runner", UserProfileConfig(
            domain_identifier="example",
            user_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datazone_user_profile, with domain identifier example,
    user identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="details" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="iam" type="object">
      <Expandable title="properties">
        <ResponseField name="arn" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sso" type="object">
      <Expandable title="properties">
        <ResponseField name="first_name" type="string">
        </ResponseField>
        <ResponseField name="last_name" type="string">
        </ResponseField>
        <ResponseField name="user_name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
