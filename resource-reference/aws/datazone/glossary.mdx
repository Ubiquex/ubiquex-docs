---
title: "aws_datazone_glossary"
description: "Real, generated bindings for `aws_datazone_glossary`."
---

`aws_datazone_glossary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own glossary"})

    		ubx.Resource(datazone.Glossary, "ci-runner", datazone.GlossaryConfig{
    			DomainIdentifier:        "example",
    			Name:                    "ci-runner",
    			OwningProjectIdentifier: "example",
    			Description:             "Managed by ubx.",
    			Status:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Glossary } from "jsr:@ubx/sdk-aws/aws/datazone/glossary";

    export default stack("payments", () => {
      intent({ summary: "payments own glossary" });

      resource(Glossary, "ci-runner", {
        domainIdentifier: "example",
        name: "ci-runner",
        owningProjectIdentifier: "example",
        description: "Managed by ubx.",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import Glossary, GlossaryConfig

    def describe():
        ubx.intent("payments own glossary")

        ubx.resource(Glossary, "ci-runner", GlossaryConfig(
            domain_identifier="example",
            name="ci-runner",
            owning_project_identifier="example",
            description="Managed by ubx.",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datazone_glossary called "ci-runner", with domain
    identifier example, owning project identifier example,
    description Managed by ubx., status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owning_project_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
