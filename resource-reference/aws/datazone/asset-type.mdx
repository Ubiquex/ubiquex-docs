---
title: "aws_datazone_asset_type"
description: "Real, generated bindings for `aws_datazone_asset_type`."
---

`aws_datazone_asset_type` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own asset type"})

    		ubx.Resource(datazone.AssetType, "ci-runner", datazone.AssetTypeConfig{
    			DomainIdentifier:        "example",
    			Name:                    "ci-runner",
    			OwningProjectIdentifier: "example",
    			Description:             "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AssetType } from "jsr:@ubx/sdk-aws/aws/datazone/asset_type";

    export default stack("payments", () => {
      intent({ summary: "payments own asset type" });

      resource(AssetType, "ci-runner", {
        domainIdentifier: "example",
        name: "ci-runner",
        owningProjectIdentifier: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import AssetType, AssetTypeConfig

    def describe():
        ubx.intent("payments own asset type")

        ubx.resource(AssetType, "ci-runner", AssetTypeConfig(
            domain_identifier="example",
            name="ci-runner",
            owning_project_identifier="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datazone_asset_type called "ci-runner", with domain
    identifier example, owning project identifier example,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owning_project_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision" type="string">
  Computed by AWS after `ship`.
</ResponseField>
