---
title: "aws_datazone_asset_type"
description: "Real, generated bindings for `aws_datazone_asset_type`."
---

`aws_datazone_asset_type` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/datazone"

    cfg := datazone.AssetTypeConfig{
        DomainIdentifier: "example",
        Name: "example-name",
        OwningProjectIdentifier: "example",
        Description: "example",
    }
    ubx.Resource(datazone.AssetType, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AssetType } from
      "jsr:@ubx/sdk-aws/aws/datazone/asset_type";

    resource(AssetType, "example", {
      domainIdentifier: "example",
      name: "example",
      owningProjectIdentifier: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datazone import AssetType, AssetTypeConfig

    cfg = AssetTypeConfig(
        domain_identifier="example",
        name="example",
        owning_project_identifier="example",
        description="example",
    )
    ubx.resource(AssetType, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datazone_asset_type with domain_identifier, name,
owning_project_identifier, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owning_project_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision" type="string">
  Computed by AWS after `ship`.
</ResponseField>
