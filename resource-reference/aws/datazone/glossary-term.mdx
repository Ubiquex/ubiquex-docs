---
title: "aws_datazone_glossary_term"
description: "Real, generated bindings for `aws_datazone_glossary_term`."
---

`aws_datazone_glossary_term` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own glossary term"})

    		ubx.Resource(datazone.GlossaryTerm, "ci-runner", datazone.GlossaryTermConfig{
    			GlossaryIdentifier: "example",
    			Name:               "ci-runner",
    			DomainIdentifier:   "example",
    			LongDescription:    "example",
    			ShortDescription:   "example",
    			Status:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlossaryTerm } from "jsr:@ubx/sdk-aws/aws/datazone/glossary_term";

    export default stack("payments", () => {
      intent({ summary: "payments own glossary term" });

      resource(GlossaryTerm, "ci-runner", {
        glossaryIdentifier: "example",
        name: "ci-runner",
        domainIdentifier: "example",
        longDescription: "example",
        shortDescription: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datazone import GlossaryTerm, GlossaryTermConfig

    def describe():
        ubx.intent("payments own glossary term")

        ubx.resource(GlossaryTerm, "ci-runner", GlossaryTermConfig(
            glossary_identifier="example",
            name="ci-runner",
            domain_identifier="example",
            long_description="example",
            short_description="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datazone_glossary_term called "ci-runner", with
    glossary identifier example, domain identifier example, long
    description example, short description example, status example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="glossary_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="long_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="short_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
