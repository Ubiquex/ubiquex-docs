---
title: "aws_cloud_front_cloud_front_origin_access_identity"
description: "`aws_cloud_front_cloud_front_origin_access_identity` is CloudFront's original..."
---

`aws_cloud_front_cloud_front_origin_access_identity` is CloudFront's original, legacy mechanism for restricting a private S3 origin to CloudFront-only access, a special CloudFront-managed identity granted read permission on the bucket. AWS now recommends `aws_cloud_front_origin_access_control` for new distributions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own front cloud front origin access identity"})

    		ubx.Resource(cloud.FrontCloudFrontOriginAccessIdentity, "example", cloud.FrontCloudFrontOriginAccessIdentityConfig{
    			CloudFrontOriginAccessIdentityConfig: map[string]any{"comment": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FrontCloudFrontOriginAccessIdentity } from "@ubx/sdk-aws/aws/cloud/front_cloud_front_origin_access_identity";

    export default stack("example", () => {
      intent({ summary: "example own front cloud front origin access identity" });

      resource(FrontCloudFrontOriginAccessIdentity, "example", {
        cloudFrontOriginAccessIdentityConfig: { comment: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloud import FrontCloudFrontOriginAccessIdentity, FrontCloudFrontOriginAccessIdentityConfig

    def describe():
        ubx.intent("example own front cloud front origin access identity")

        ubx.resource(FrontCloudFrontOriginAccessIdentity, "example", FrontCloudFrontOriginAccessIdentityConfig(
            cloud_front_origin_access_identity_config={"comment": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_front_cloud_front_origin_access_identity, with
    cloud front origin access identity config a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_front_origin_access_identity_config" type="object" required>
  Origin access identity configuration. Send a ``GET`` request to the ``/CloudFront API version/CloudFront/identity ID/config`` resource. Required.
  <Expandable title="properties">
    <ResponseField name="comment" type="string" required>
      A comment to describe the origin access identity. The comment cannot be longer than 128 characters. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned to the CloudFront origin access identity, which is used as the principal in S3 bucket policies and referenced in CloudFront distribution origin settings. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="s3_canonical_user_id" type="string">
  The canonical user ID associated with the CloudFront origin access identity, which you can use to grant the OAI permissions to access S3 objects via a bucket policy. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
