---
title: "aws_cloud_formation_hook_type_config"
description: "`aws_cloud_formation_hook_type_config` supplies real, account-level configuration data, target stack filters, failure mode..."
---

`aws_cloud_formation_hook_type_config` supplies real, account-level configuration data, target stack filters, failure mode, to an `aws_cloud_formation_lambda_hook`, controlling how it behaves across every stack operation it's invoked for.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own formation hook type config"})

    		ubx.Resource(cloud.FormationHookTypeConfig, "example", cloud.FormationHookTypeConfigConfig{
    			Configuration:      "example",
    			ConfigurationAlias: "example",
    			TypeArn:            "arn:aws:iam::123456789012:policy/example",
    			TypeName:           "example-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FormationHookTypeConfig } from "./local-sdk/aws/sdk/typescript/aws/cloud/formation_hook_type_config.ts";

    export default stack("example", () => {
      intent({ summary: "example own formation hook type config" });

      resource(FormationHookTypeConfig, "example", {
        configuration: "example",
        configurationAlias: "example",
        typeArn: "arn:aws:iam::123456789012:policy/example",
        typeName: "example-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cloud import FormationHookTypeConfig, FormationHookTypeConfigConfig

    def describe():
        ubx.intent("example own formation hook type config")

        ubx.resource(FormationHookTypeConfig, "example", FormationHookTypeConfigConfig(
            configuration="example",
            configuration_alias="example",
            type_arn="arn:aws:iam::123456789012:policy/example",
            type_name="example-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_formation_hook_type_config, with configuration
    example, configuration alias example, type arn
    arn:aws:iam::123456789012:policy/example, type name
    example-type-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration" type="string">
  The configuration data for the extension, in this account and region. Optional.
</ResponseField>

<ResponseField name="configuration_alias" type="string">
  An alias by which to refer to this extension configuration data. Optional.
</ResponseField>

<ResponseField name="type_arn" type="string">
  The Amazon Resource Name (ARN) of the type without version number. Optional.
</ResponseField>

<ResponseField name="type_name" type="string">
  The name of the type being registered. We recommend that type names adhere to the following pattern: company_or_organization::service::type. Optional.
</ResponseField>

## Output properties

<ResponseField name="configuration_arn" type="string">
  The Amazon Resource Name (ARN) for the configuration data, in this account and region. Computed by AWS after `ship`.
</ResponseField>
