---
title: "aws_cloud_front_distribution_tenant"
description: "`aws_cloud_front_distribution_tenant` is the real, routable front door for one tenant's domain in CloudFront's multi-tenant distribution model..."
---

`aws_cloud_front_distribution_tenant` is the real, routable front door for one tenant's domain in CloudFront's multi-tenant distribution model (CloudFront SaaS Manager). The multi-tenant distribution it belongs to has no domain name of its own; only its distribution tenants are actually reachable by viewers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own front distribution tenant"})

    		ubx.Resource(cloudfront.DistributionTenant, "example", cloudfront.DistributionTenantConfig{
    			DistributionId:            "example",
    			Domains:                   []string{"example"},
    			Name:                      "example",
    			ConnectionGroupId:         "example",
    			Customizations:            map[string]any{"certificate": map[string]any{"arn": "example"}},
    			Enabled:                   true,
    			ManagedCertificateRequest: map[string]any{"certificate_transparency_logging_preference": "example"},
    			Parameters:                []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DistributionTenant } from "@ubx/sdk-aws/aws/cloudfront/distribution_tenant";

    export default stack("example", () => {
      intent({ summary: "example own front distribution tenant" });

      resource(DistributionTenant, "example", {
        distributionId: "example",
        domains: ["example"],
        name: "example",
        connectionGroupId: "example",
        customizations: { certificate: { arn: "example" } },
        enabled: true,
        managedCertificateRequest: {
          certificateTransparencyLoggingPreference: "example",
        },
        parameters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront.distribution_tenant import DistributionTenant, DistributionTenantConfig

    def describe():
        ubx.intent("example own front distribution tenant")

        ubx.resource(DistributionTenant, "example", DistributionTenantConfig(
            distribution_id="example",
            domains=["example"],
            name="example",
            connection_group_id="example",
            customizations={"certificate": {"arn": "example"}},
            enabled=True,
            managed_certificate_request={"certificate_transparency_logging_preference": "example"},
            parameters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_front_distribution_tenant called "example", with
    distribution id example, domains a set value, connection group id
    example, customizations a set value, enabled true, managed
    certificate request a set value, parameters a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connection_group_id" type="string">
  The ID of the connection group for the distribution tenant. If you don't specify a connection group, CloudFront uses the default connection group. Optional.
</ResponseField>

<ResponseField name="customizations" type="object">
  Customizations for the distribution tenant. For each distribution tenant, you can specify the geographic restrictions, and the Amazon Resource Names (ARNs) for the ACM certificate and WAF web ACL. These are specific values that you can override or disable from the multi-tenant distribution that was used to create the distribution tenant. Optional.
  <Expandable title="properties">
    <ResponseField name="certificate" type="object">
      The ACMlong (ACM) certificate associated with your distribution. Optional.
      <Expandable title="properties">
        <ResponseField name="arn" type="string">
          The Amazon Resource Name (ARN) of the ACM certificate. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="geo_restrictions" type="object">
      The customizations that you specified for the distribution tenant for geographic restrictions. Optional.
      <Expandable title="properties">
        <ResponseField name="locations" type="list(string)">
          The locations for geographic restrictions. Optional.
        </ResponseField>
        <ResponseField name="restriction_type" type="string">
          The method that you want to use to restrict distribution of your content by country: + ``none``: No geographic restriction is enabled, meaning access to content is not restricted by client geo location. + ``blacklist``: The ``Location`` elements specify the countries in which you don't want CloudFront to distribute your content. + ``whitelist``: The ``Location`` elements specify the countries in which you want CloudFront to distribute your content. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="web_acl" type="object">
      The WAF web ACL customization specified for the distribution tenant. Optional.
      <Expandable title="properties">
        <ResponseField name="action" type="string">
          The action for the WAF web ACL customization. You can specify ``override`` to specify a separate WAF web ACL for the distribution tenant. If you specify ``disable``, the distribution tenant won't have WAF web ACL protections and won't inherit from the multi-tenant distribution. Optional.
        </ResponseField>
        <ResponseField name="arn" type="string">
          The Amazon Resource Name (ARN) of the WAF web ACL. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="distribution_id" type="string" required>
  The ID of the multi-tenant distribution. Required.
</ResponseField>

<ResponseField name="domains" type="list(string)" required>
  The domains associated with the distribution tenant. Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Indicates whether the distribution tenant is in an enabled state. If disabled, the distribution tenant won't serve traffic. Optional.
</ResponseField>

<ResponseField name="managed_certificate_request" type="object">
  An object that represents the request for the Amazon CloudFront managed ACM certificate. Optional.
  <Expandable title="properties">
    <ResponseField name="certificate_transparency_logging_preference" type="string">
      You can opt out of certificate transparency logging by specifying the ``disabled`` option. Opt in by specifying ``enabled``. For more information, see [Certificate Transparency Logging](https://docs.aws.amazon.com/acm/latest/userguide/acm-concepts.html#concept-transparency) in the *User Guide*. Optional.
    </ResponseField>
    <ResponseField name="primary_domain_name" type="string">
      The primary domain name associated with the CloudFront managed ACM certificate. Optional.
    </ResponseField>
    <ResponseField name="validation_token_host" type="string">
      Specify how the HTTP validation token will be served when requesting the CloudFront managed ACM certificate. + For ``cloudfront``, CloudFront will automatically serve the validation token. Choose this mode if you can point the domain's DNS to CloudFront immediately. + For ``self-hosted``, you serve the validation token from your existing infrastructure. Choose this mode when you need to maintain current traffic flow while your certificate is being issued. You can place the validation token at the well-known path on your existing web server, wait for ACM to validate and issue the certificate, and then update your DNS to point to CloudFront. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the distribution tenant. Required.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  A list of parameter values to add to the resource. A parameter is specified as a key-value pair. A valid parameter value must exist for any parameter that is marked as required in the multi-tenant distribution. Optional.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A complex type that contains zero or more ``Tag`` elements. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  The timestamp (in ISO 8601 format) indicating when the CloudFront distribution was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_results" type="list(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="domain" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
