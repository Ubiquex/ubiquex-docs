---
title: "aws_cloud_formation_wait_condition_handle"
description: "`aws_cloud_formation_wait_condition_handle` generates the real, pre-signed URL an external process signals success or failure to..."
---

`aws_cloud_formation_wait_condition_handle` generates the real, pre-signed URL an external process signals success or failure to, referenced by an `aws_cloud_formation_wait_condition`'s own Handle property. AWS now recommends a stack resource's CreationPolicy attribute over this mechanism for new templates.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own formation wait condition handle"})

    		ubx.Resource(cloud.FormationWaitConditionHandle, "example", cloud.FormationWaitConditionHandleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FormationWaitConditionHandle } from "@ubx/sdk-aws/aws/cloud/formation_wait_condition_handle";

    export default stack("example", () => {
      intent({ summary: "example own formation wait condition handle" });

      resource(FormationWaitConditionHandle, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloud import FormationWaitConditionHandle, FormationWaitConditionHandleConfig

    def describe():
        ubx.intent("example own formation wait condition handle")

        ubx.resource(FormationWaitConditionHandle, "example", FormationWaitConditionHandleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_formation_wait_condition_handle, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="id" type="string">
  The unique identifier for the wait condition handle, which is a pre-signed URL used by external signals to notify CloudFormation of completion. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
