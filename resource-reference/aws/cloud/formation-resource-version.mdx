---
title: "aws_cloud_formation_resource_version"
description: "`aws_cloud_formation_resource_version` is a specific, registered version of a custom resource type in the CloudFormation registry..."
---

`aws_cloud_formation_resource_version` is a specific, registered version of a custom resource type in the CloudFormation registry, the real mechanism that versions a private or third-party extension's own schema and provisioning logic independently of any stack that uses it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own formation resource version"})

    		ubx.Resource(cloudformation.ResourceVersion, "example", cloudformation.ResourceVersionConfig{
    			SchemaHandlerPackage: "example",
    			TypeName:             "example-type-name",
    			ExecutionRoleArn:     "arn:aws:iam::123456789012:policy/example",
    			LoggingConfig:        map[string]any{"log_group_name": "example-log-group-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceVersion } from "@ubx/sdk-aws/aws/cloudformation/resource_version";

    export default stack("example", () => {
      intent({ summary: "example own formation resource version" });

      resource(ResourceVersion, "example", {
        schemaHandlerPackage: "example",
        typeName: "example-type-name",
        executionRoleArn: "arn:aws:iam::123456789012:policy/example",
        loggingConfig: { logGroupName: "example-log-group-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudformation.resource_version import ResourceVersion, ResourceVersionConfig

    def describe():
        ubx.intent("example own formation resource version")

        ubx.resource(ResourceVersion, "example", ResourceVersionConfig(
            schema_handler_package="example",
            type_name="example-type-name",
            execution_role_arn="arn:aws:iam::123456789012:policy/example",
            logging_config={"log_group_name": "example-log-group-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_formation_resource_version, with schema handler
    package example, type name example-type-name, execution role arn
    arn:aws:iam::123456789012:policy/example, logging config a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="execution_role_arn" type="string">
  The Amazon Resource Name (ARN) of the IAM execution role to use to register the type. If your resource type calls AWS APIs in any of its handlers, you must create an IAM execution role that includes the necessary permissions to call those AWS APIs, and provision that execution role in your account. CloudFormation then assumes that execution role to provide your resource type with the appropriate credentials. Optional.
</ResponseField>

<ResponseField name="logging_config" type="object">
  Specifies the logging configuration for the resource type, including the CloudWatch Logs log group name and the IAM role ARN used to deliver logs from the resource provider. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="log_group_name" type="string">
      The Amazon CloudWatch log group to which CloudFormation sends error logging information when invoking the type's handlers. Optional.
    </ResponseField>
    <ResponseField name="log_role_arn" type="string">
      The ARN of the role that CloudFormation should assume when sending log entries to CloudWatch logs. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schema_handler_package" type="string" required>
  A url to the S3 bucket containing the schema handler package that contains the schema, event handlers, and associated files for the type you want to register. For information on generating a schema handler package for the type you want to register, see submit in the CloudFormation CLI User Guide. Required.
</ResponseField>

<ResponseField name="type_name" type="string" required>
  The name of the type being registered. We recommend that type names adhere to the following pattern: company_or_organization::service::type. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the type, here the ResourceVersion. This is used to uniquely identify a ResourceVersion resource Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default_version" type="bool">
  Indicates if this type version is the current default version Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioning_type" type="string">
  The provisioning behavior of the type. AWS CloudFormation determines the provisioning type during registration, based on the types of handlers in the schema handler package submitted. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type_arn" type="string">
  The Amazon Resource Name (ARN) of the type without the versionID. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_id" type="string">
  The ID of the version of the type represented by this resource instance. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="visibility" type="string">
  The scope at which the type is visible and usable in CloudFormation operations. Valid values include: PRIVATE: The type is only visible and usable within the account in which it is registered. Currently, AWS CloudFormation marks any types you register as PRIVATE. PUBLIC: The type is publically visible and usable within any Amazon account. Computed by AWS after `ship`.
</ResponseField>
