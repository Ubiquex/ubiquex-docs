---
title: "aws_cloud_formation_hook_default_version"
description: "`aws_cloud_formation_hook_default_version` designates which registered version of an `aws_cloud_formation_lambda_hook` CloudFormation actually invokes..."
---

`aws_cloud_formation_hook_default_version` designates which registered version of an `aws_cloud_formation_lambda_hook` CloudFormation actually invokes by default, when multiple versions of the same hook have been registered.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own formation hook default version"})

    		ubx.Resource(cloud.FormationHookDefaultVersion, "example", cloud.FormationHookDefaultVersionConfig{
    			TypeName:       "example-type-name",
    			TypeVersionArn: "arn:aws:iam::123456789012:policy/example",
    			VersionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FormationHookDefaultVersion } from "@ubx/sdk-aws/aws/cloud/formation_hook_default_version";

    export default stack("example", () => {
      intent({ summary: "example own formation hook default version" });

      resource(FormationHookDefaultVersion, "example", {
        typeName: "example-type-name",
        typeVersionArn: "arn:aws:iam::123456789012:policy/example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloud import FormationHookDefaultVersion, FormationHookDefaultVersionConfig

    def describe():
        ubx.intent("example own formation hook default version")

        ubx.resource(FormationHookDefaultVersion, "example", FormationHookDefaultVersionConfig(
            type_name="example-type-name",
            type_version_arn="arn:aws:iam::123456789012:policy/example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_formation_hook_default_version, with type name
    example-type-name, type version arn
    arn:aws:iam::123456789012:policy/example, version id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="type_name" type="string">
  The name of the type being registered. We recommend that type names adhere to the following pattern: company_or_organization::service::type. Optional.
</ResponseField>

<ResponseField name="type_version_arn" type="string">
  The Amazon Resource Name (ARN) of the type version. Optional.
</ResponseField>

<ResponseField name="version_id" type="string">
  The ID of an existing version of the hook to set as the default. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the type. This is used to uniquely identify a HookDefaultVersion Computed by AWS after `ship`.
</ResponseField>
