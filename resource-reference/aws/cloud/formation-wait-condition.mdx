---
title: "aws_cloud_formation_wait_condition"
description: "`aws_cloud_formation_wait_condition` pauses an `aws_cloud_formation_stack`'s creation until it receives a real success signal, or times out..."
---

`aws_cloud_formation_wait_condition` pauses an `aws_cloud_formation_stack`'s creation until it receives a real success signal, or times out, from an external process, the mechanism for coordinating stack creation with configuration work that happens outside CloudFormation itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own formation wait condition"})

    		ubx.Resource(cloud.FormationWaitCondition, "ci-runner", cloud.FormationWaitConditionConfig{
    			Count:   1,
    			Handle:  "example",
    			Timeout: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FormationWaitCondition } from "./local-sdk/aws/sdk/typescript/aws/cloud/formation_wait_condition.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own formation wait condition" });

      resource(FormationWaitCondition, "ci-runner", {
        count: 1,
        handle: "example",
        timeout: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cloud import FormationWaitCondition, FormationWaitConditionConfig

    def describe():
        ubx.intent("payments own formation wait condition")

        ubx.resource(FormationWaitCondition, "ci-runner", FormationWaitConditionConfig(
            count=1,
            handle="example",
            timeout="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_formation_wait_condition, with count 1, handle
    example, timeout example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="count" type="number">
  The number of success signals that must be received by the CloudFormation wait condition before it is considered complete, matching the Count property of the associated CloudFormation WaitCondition. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="handle" type="string">
  The handle property is the pre-signed URL (from the associated WaitConditionHandle) that a signal is sent to, marking the wait as complete and allowing CloudFormation to finish creating this WaitCondition resource. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="timeout" type="string">
  The number of seconds to wait for the required number of signals to arrive before the wait condition times out. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="data" type="any">
  The map of data returned by the signaled resource or application once the wait condition is fulfilled, providing the payload sent via the signal URL. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
