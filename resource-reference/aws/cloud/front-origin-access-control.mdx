---
title: "aws_cloud_front_origin_access_control"
description: "`aws_cloud_front_origin_access_control` lets an `aws_cloud_front_distribution` sign its own requests to a private S3 bucket or other supported origin..."
---

`aws_cloud_front_origin_access_control` lets an `aws_cloud_front_distribution` sign its own requests to a private S3 bucket or other supported origin, so the origin can be restricted to CloudFront-only access, the current, real replacement for the older `aws_cloud_front_cloud_front_origin_access_identity` mechanism.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own front origin access control"})

    		ubx.Resource(cloud.FrontOriginAccessControl, "example", cloud.FrontOriginAccessControlConfig{
    			OriginAccessControlConfig: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FrontOriginAccessControl } from "./local-sdk/aws/sdk/typescript/aws/cloud/front_origin_access_control.ts";

    export default stack("example", () => {
      intent({ summary: "example own front origin access control" });

      resource(FrontOriginAccessControl, "example", {
        originAccessControlConfig: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cloud import FrontOriginAccessControl, FrontOriginAccessControlConfig

    def describe():
        ubx.intent("example own front origin access control")

        ubx.resource(FrontOriginAccessControl, "example", FrontOriginAccessControlConfig(
            origin_access_control_config={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_front_origin_access_control, with origin access
    control config a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="origin_access_control_config" type="object" required>
  Creates a new origin access control in CloudFront. After you create an origin access control, you can add it to an origin in a CloudFront distribution so that CloudFront sends authenticated (signed) requests to the origin. This makes it possible to block public access to the origin, allowing viewers (users) to access the origin's content only through CloudFront. For more information about using a CloudFront origin access control, see [Restricting access to an origin](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-origin.html) in the *Amazon CloudFront Developer Guide*. Required.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      A description of the origin access control. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      A name to identify the origin access control. You can specify up to 64 characters. Required.
    </ResponseField>
    <ResponseField name="origin_access_control_origin_type" type="string" required>
      The type of origin that this origin access control is for. Required.
    </ResponseField>
    <ResponseField name="signing_behavior" type="string" required>
      Specifies which requests CloudFront signs (adds authentication information to). Specify ``always`` for the most common use case. For more information, see [origin access control advanced settings](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-s3.html#oac-advanced-settings) in the *Amazon CloudFront Developer Guide*. This field can have one of the following values: + ``always``  --  CloudFront signs all origin requests, overwriting the ``Authorization`` header from the viewer request if one exists. + ``never``  --  CloudFront doesn't sign any origin requests. This value turns off origin access control for all origins in all distributions that use this origin access control. + ``no-override``  --  If the viewer request doesn't contain the ``Authorization`` header, then CloudFront signs the origin request. If the viewer request contains the ``Authorization`` header, then CloudFront doesn't sign the origin request and instead passes along the ``Authorization`` header from the viewer request. *WARNING: To pass along the Authorization header from the viewer request, you must add the Authorization header to a cache policy for all cache behaviors that use origins associated with this origin access control.* Required.
    </ResponseField>
    <ResponseField name="signing_protocol" type="string" required>
      The signing protocol of the origin access control, which determines how CloudFront signs (authenticates) requests. The only valid value is ``sigv4``. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The id is the unique CloudFront-assigned identifier for the origin access control, which is used when associating the OAC with a distribution's cache behavior. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
