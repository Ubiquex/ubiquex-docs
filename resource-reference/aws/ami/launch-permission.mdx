---
title: "aws_ami_launch_permission"
description: "Real, generated bindings for `aws_ami_launch_permission`."
---

`aws_ami_launch_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ami "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ami"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own launch permission"})

    		ubx.Resource(ami.LaunchPermission, "ci-runner", ami.LaunchPermissionConfig{
    			ImageId:               "example",
    			AccountId:             "example",
    			Group:                 "example",
    			OrganizationArn:       "arn:aws:iam::123456789012:policy/ci",
    			OrganizationalUnitArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LaunchPermission } from "jsr:@ubx/sdk-aws/aws/ami/launch_permission";

    export default stack("payments", () => {
      intent({ summary: "payments own launch permission" });

      resource(LaunchPermission, "ci-runner", {
        imageId: "example",
        accountId: "example",
        group: "example",
        organizationArn: "arn:aws:iam::123456789012:policy/ci",
        organizationalUnitArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ami import LaunchPermission, LaunchPermissionConfig

    def describe():
        ubx.intent("payments own launch permission")

        ubx.resource(LaunchPermission, "ci-runner", LaunchPermissionConfig(
            image_id="example",
            account_id="example",
            group="example",
            organization_arn="arn:aws:iam::123456789012:policy/ci",
            organizational_unit_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ami_launch_permission, with image id example, account
    id example, group example, organization arn
    arn:aws:iam::123456789012:policy/ci, organizational unit arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="group" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="organizational_unit_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
