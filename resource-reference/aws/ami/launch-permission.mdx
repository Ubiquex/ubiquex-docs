---
title: "aws_ami_launch_permission"
description: "Real, generated bindings for `aws_ami_launch_permission`."
---

`aws_ami_launch_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ami"

    cfg := ami.LaunchPermissionConfig{
        ImageId: "example",
        AccountId: "example",
        Group: "example",
    }
    ubx.Resource(ami.LaunchPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LaunchPermission } from
      "jsr:@ubx/sdk-aws/aws/ami/launch_permission";

    resource(LaunchPermission, "example", {
      imageId: "example",
      accountId: "example",
      group: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ami import (
        LaunchPermission,
        LaunchPermissionConfig,
    )

    cfg = LaunchPermissionConfig(
        image_id="example",
        account_id="example",
        group="example",
    )
    ubx.resource(LaunchPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ami_launch_permission with image_id, account_id,
group.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="group" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="organizational_unit_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
