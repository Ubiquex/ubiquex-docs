---
title: "aws_ami_from_instance"
description: "Real, generated bindings for `aws_ami_from_instance`."
---

`aws_ami_from_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ami "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ami"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own from instance"})

    		ubx.Resource(ami.FromInstance, "ci-runner", ami.FromInstanceConfig{
    			Name:                  "ci-runner",
    			SourceInstanceId:      "example",
    			DeprecationTime:       "example",
    			Description:           "Managed by ubx.",
    			SnapshotWithoutReboot: true,
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FromInstance } from "jsr:@ubx/sdk-aws/aws/ami/from_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own from instance" });

      resource(FromInstance, "ci-runner", {
        name: "ci-runner",
        sourceInstanceId: "example",
        deprecationTime: "example",
        description: "Managed by ubx.",
        snapshotWithoutReboot: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ami import FromInstance, FromInstanceConfig

    def describe():
        ubx.intent("payments own from instance")

        ubx.resource(FromInstance, "ci-runner", FromInstanceConfig(
            name="ci-runner",
            source_instance_id="example",
            deprecation_time="example",
            description="Managed by ubx.",
            snapshot_without_reboot=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ami_from_instance called "ci-runner", with source
    instance id example, deprecation time example, description
    Managed by ubx., snapshot without reboot true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deprecation_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_without_reboot" type="bool">
  Optional.
</ResponseField>

<ResponseField name="source_instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="architecture" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="boot_mode" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ena_support" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hypervisor" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="image_owner_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="image_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="imds_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kernel_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_launched_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="manage_ebs_snapshots" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="platform" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="platform_details" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ramdisk_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="root_device_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="root_snapshot_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sriov_net_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tpm_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="uefi_data" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="usage_operation" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="virtualization_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
