---
title: "aws_cloudtrail_event_data_store"
description: "Real, generated bindings for `aws_cloudtrail_event_data_store`."
---

`aws_cloudtrail_event_data_store` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event data store"})

    		ubx.Resource(cloudtrail.EventDataStore, "ci-runner", cloudtrail.EventDataStoreConfig{
    			Name:                "ci-runner",
    			BillingMode:         "example",
    			KmsKeyId:            "example",
    			MultiRegionEnabled:  true,
    			OrganizationEnabled: true,
    			RetentionPeriod:     1,
    			Suspend:             "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventDataStore } from "jsr:@ubx/sdk-aws/aws/cloudtrail/event_data_store";

    export default stack("payments", () => {
      intent({ summary: "payments own event data store" });

      resource(EventDataStore, "ci-runner", {
        name: "ci-runner",
        billingMode: "example",
        kmsKeyId: "example",
        multiRegionEnabled: true,
        organizationEnabled: true,
        retentionPeriod: 1,
        suspend: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudtrail import EventDataStore, EventDataStoreConfig

    def describe():
        ubx.intent("payments own event data store")

        ubx.resource(EventDataStore, "ci-runner", EventDataStoreConfig(
            name="ci-runner",
            billing_mode="example",
            kms_key_id="example",
            multi_region_enabled=True,
            organization_enabled=True,
            retention_period=1,
            suspend="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudtrail_event_data_store called "ci-runner", with
    billing mode example, kms key id example, multi region enabled
    true, organization enabled true, retention period 1, suspend
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="billing_mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="multi_region_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="suspend" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="termination_protection_enabled" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
