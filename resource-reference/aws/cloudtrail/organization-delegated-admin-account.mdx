---
title: "aws_cloudtrail_organization_delegated_admin_account"
description: "Real, generated bindings for `aws_cloudtrail_organization_delegated_admin_account`."
---

`aws_cloudtrail_organization_delegated_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudtrail"

    cfg := cloudtrail.OrganizationDelegatedAdminAccountConfig{
        AccountId: "example",
    }
    ubx.Resource(
        cloudtrail.OrganizationDelegatedAdminAccount,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OrganizationDelegatedAdminAccount } from
      "jsr:@ubx/sdk-aws/aws/cloudtrail/organization_delegated_admin_account";

    resource(OrganizationDelegatedAdminAccount, "example", {
      accountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudtrail import (
        OrganizationDelegatedAdminAccount,
        OrganizationDelegatedAdminAccountConfig,
    )

    cfg = OrganizationDelegatedAdminAccountConfig(
        account_id="example",
    )
    ubx.resource(OrganizationDelegatedAdminAccount, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudtrail_organization_delegated_admin_account
with account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>
