---
title: "aws_cloudtrail_organization_delegated_admin_account"
description: "Real, generated bindings for `aws_cloudtrail_organization_delegated_admin_account`."
---

`aws_cloudtrail_organization_delegated_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization delegated admin account"})

    		ubx.Resource(cloudtrail.OrganizationDelegatedAdminAccount, "ci-runner", cloudtrail.OrganizationDelegatedAdminAccountConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationDelegatedAdminAccount } from "jsr:@ubx/sdk-aws/aws/cloudtrail/organization_delegated_admin_account";

    export default stack("payments", () => {
      intent({ summary: "payments own organization delegated admin account" });

      resource(OrganizationDelegatedAdminAccount, "ci-runner", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudtrail import OrganizationDelegatedAdminAccount, OrganizationDelegatedAdminAccountConfig

    def describe():
        ubx.intent("payments own organization delegated admin account")

        ubx.resource(OrganizationDelegatedAdminAccount, "ci-runner", OrganizationDelegatedAdminAccountConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudtrail_organization_delegated_admin_account, with
    account id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>
