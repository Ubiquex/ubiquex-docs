---
title: "aws_cloudtrail"
description: "Real, generated bindings for `aws_cloudtrail`."
---

`aws_cloudtrail` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cloudtrail"})

    		ubx.Resource(cloudtrail.Cloudtrail, "ci-runner", cloudtrail.CloudtrailConfig{
    			Name:                       "ci-runner",
    			S3BucketName:               "example-s3-bucket-name",
    			CloudWatchLogsGroupArn:     "arn:aws:iam::123456789012:policy/ci",
    			CloudWatchLogsRoleArn:      "arn:aws:iam::123456789012:policy/ci",
    			EnableLogFileValidation:    true,
    			EnableLogging:              true,
    			IncludeGlobalServiceEvents: true,
    			IsMultiRegionTrail:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cloudtrail } from "jsr:@ubx/sdk-aws/aws/cloudtrail/cloudtrail";

    export default stack("payments", () => {
      intent({ summary: "payments own cloudtrail" });

      resource(Cloudtrail, "ci-runner", {
        name: "ci-runner",
        s3BucketName: "example-s3-bucket-name",
        cloudWatchLogsGroupArn: "arn:aws:iam::123456789012:policy/ci",
        cloudWatchLogsRoleArn: "arn:aws:iam::123456789012:policy/ci",
        enableLogFileValidation: true,
        enableLogging: true,
        includeGlobalServiceEvents: true,
        isMultiRegionTrail: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudtrail import Cloudtrail, CloudtrailConfig

    def describe():
        ubx.intent("payments own cloudtrail")

        ubx.resource(Cloudtrail, "ci-runner", CloudtrailConfig(
            name="ci-runner",
            s3_bucket_name="example-s3-bucket-name",
            cloud_watch_logs_group_arn="arn:aws:iam::123456789012:policy/ci",
            cloud_watch_logs_role_arn="arn:aws:iam::123456789012:policy/ci",
            enable_log_file_validation=True,
            enable_logging=True,
            include_global_service_events=True,
            is_multi_region_trail=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudtrail called "ci-runner", with s3 bucket name
    example-s3-bucket-name, cloud watch logs group arn
    arn:aws:iam::123456789012:policy/ci, cloud watch logs role arn
    arn:aws:iam::123456789012:policy/ci, enable log file validation
    true, enable logging true, include global service events true, is
    multi region trail true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cloud_watch_logs_group_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="cloud_watch_logs_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable_log_file_validation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_logging" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="include_global_service_events" type="bool">
  Optional.
</ResponseField>

<ResponseField name="is_multi_region_trail" type="bool">
  Optional.
</ResponseField>

<ResponseField name="is_organization_trail" type="bool">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="sns_topic_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
