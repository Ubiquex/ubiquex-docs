---
title: "aws_odb_cloud_exadata_infrastructure"
description: "Real, generated bindings for `aws_odb_cloud_exadata_infrastructure`."
---

`aws_odb_cloud_exadata_infrastructure` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cloud exadata infrastructure"})

    		ubx.Resource(odb.CloudExadataInfrastructure, "ci-runner", odb.CloudExadataInfrastructureConfig{
    			AvailabilityZoneId:          "example",
    			DisplayName:                 "example-display-name",
    			Shape:                       "example",
    			CustomerContactsToSendToOci: []map[string]any{{"email": "example"}},
    			DatabaseServerType:          "example",
    			StorageServerType:           "example",
    			Tags:                        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudExadataInfrastructure } from "jsr:@ubx/sdk-aws/aws/odb/cloud_exadata_infrastructure";

    export default stack("payments", () => {
      intent({ summary: "payments own cloud exadata infrastructure" });

      resource(CloudExadataInfrastructure, "ci-runner", {
        availabilityZoneId: "example",
        displayName: "example-display-name",
        shape: "example",
        customerContactsToSendToOci: [{ email: "example" }],
        databaseServerType: "example",
        storageServerType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.odb import CloudExadataInfrastructure, CloudExadataInfrastructureConfig

    def describe():
        ubx.intent("payments own cloud exadata infrastructure")

        ubx.resource(CloudExadataInfrastructure, "ci-runner", CloudExadataInfrastructureConfig(
            availability_zone_id="example",
            display_name="example-display-name",
            shape="example",
            customer_contacts_to_send_to_oci=[{"email": "example"}],
            database_server_type="example",
            storage_server_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_odb_cloud_exadata_infrastructure, with availability
    zone id example, display name example-display-name, shape
    example, customer contacts to send to oci a set value, database
    server type example, storage server type example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_zone" type="string">
  The name of the Availability Zone (AZ) where the Exadata infrastructure is located. Changing this will force terraform to create new resource Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="availability_zone_id" type="string" required>
  The AZ ID of the AZ where the Exadata infrastructure is located. Changing this will force terraform to create new resource Required.
</ResponseField>

<ResponseField name="compute_count" type="number">
  The number of compute instances that the Exadata infrastructure is located Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="customer_contacts_to_send_to_oci" type="set(object)">
  The email addresses of contacts to receive notification from Oracle about maintenance updates for the Exadata infrastructure. Changing this will force terraform to create new resource Optional.
  <Expandable title="properties">
    <ResponseField name="email" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="database_server_type" type="string">
  The database server model type of the Exadata infrastructure. For the list of valid model names, use the ListDbSystemShapes operation Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The user-friendly name for the Exadata infrastructure. Changing this will force terraform to create a new resource Required.
</ResponseField>

<ResponseField name="maintenance_window" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="custom_action_timeout_in_mins" type="number" required>
    </ResponseField>
    <ResponseField name="days_of_week" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hours_of_day" type="set(number)">
    </ResponseField>
    <ResponseField name="is_custom_action_timeout_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="lead_time_in_weeks" type="number">
    </ResponseField>
    <ResponseField name="months" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="patching_mode" type="string" required>
    </ResponseField>
    <ResponseField name="preference" type="string" required>
    </ResponseField>
    <ResponseField name="weeks_of_month" type="set(number)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shape" type="string" required>
  The model name of the Exadata infrastructure. Changing this will force terraform to create new resource Required.
</ResponseField>

<ResponseField name="storage_count" type="number">
  TThe number of storage servers that are activated for the Exadata infrastructure Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_server_type" type="string">
  The storage server model type of the Exadata infrastructure. For the list of valid model names, use the ListDbSystemShapes operation Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="activated_storage_count" type="number">
  The number of storage servers requested for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="additional_storage_count" type="number">
  The number of storage servers requested for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  The name of the Availability Zone (AZ) where the Exadata infrastructure is located. Changing this will force terraform to create new resource Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="available_storage_size_in_gbs" type="number">
  The amount of available storage, in gigabytes (GB), for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="compute_count" type="number">
  The number of compute instances that the Exadata infrastructure is located Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="compute_model" type="string">
  The OCI model compute model used when you create or clone an instance: ECPU or OCPU. An ECPU is an abstracted measure of compute resources. ECPUs are based on the number of cores elastically allocated from a pool of compute and storage servers. An OCPU is a legacy physical measure of compute resources. OCPUs are based on the physical core of a processor with hyper-threading enabled. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cpu_count" type="number">
  The total number of CPU cores that are allocated to the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The time when the Exadata infrastructure was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_storage_size_in_tbs" type="number">
  The size of the Exadata infrastructure's data disk group, in terabytes (TB) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_node_storage_size_in_gbs" type="number">
  The size of the Exadata infrastructure's local node storage, in gigabytes (GB) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_server_version" type="string">
  The software version of the database servers (dom0) in the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_maintenance_run_id" type="string">
  The Oracle Cloud Identifier (OCID) of the last maintenance run for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="maintenance_window" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="custom_action_timeout_in_mins" type="number" required>
    </ResponseField>
    <ResponseField name="days_of_week" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hours_of_day" type="set(number)">
    </ResponseField>
    <ResponseField name="is_custom_action_timeout_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="lead_time_in_weeks" type="number">
    </ResponseField>
    <ResponseField name="months" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="patching_mode" type="string" required>
    </ResponseField>
    <ResponseField name="preference" type="string" required>
    </ResponseField>
    <ResponseField name="weeks_of_month" type="set(number)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_cpu_count" type="number">
  The total number of CPU cores available on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="max_data_storage_in_tbs" type="number">
  The total amount of data disk group storage, in terabytes (TB), that's available on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="max_db_node_storage_size_in_gbs" type="number">
  The total amount of local node storage, in gigabytes (GB), that's available on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="max_memory_in_gbs" type="number">
  The total amount of memory in gigabytes (GB) available on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="memory_size_in_gbs" type="number">
  The amount of memory, in gigabytes (GB), that's allocated on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="monthly_db_server_version" type="string">
  The monthly software version of the database servers in the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="monthly_storage_server_version" type="string">
  The monthly software version of the storage servers installed on the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="next_maintenance_run_id" type="string">
  The OCID of the next maintenance run for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_resource_anchor_name" type="string">
  The name of the OCI resource anchor for the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_url" type="string">
  The HTTPS link to the Exadata infrastructure in OCI Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ocid" type="string">
  The OCID of the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  The amount of progress made on the current operation on the Exadata infrastructure, expressed as a percentage Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Additional information about the status of the Exadata infrastructure Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_count" type="number">
  TThe number of storage servers that are activated for the Exadata infrastructure Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_server_version" type="string">
  The software version of the storage servers on the Exadata infrastructure. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_storage_size_in_gbs" type="number">
  The total amount of storage, in gigabytes (GB), on the Exadata infrastructure. Computed by AWS after `ship`.
</ResponseField>
