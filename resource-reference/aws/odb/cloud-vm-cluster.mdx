---
title: "aws_odb_cloud_vm_cluster"
description: "Real, generated bindings for `aws_odb_cloud_vm_cluster`."
---

`aws_odb_cloud_vm_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cloud vm cluster"})

    		ubx.Resource(odb.CloudVmCluster, "ci-runner", odb.CloudVmClusterConfig{
    			CpuCoreCount:         1,
    			DataStorageSizeInTbs: 1,
    			DbServers:            []string{"example"},
    			DisplayName:          "example-display-name",
    			GiVersion:            "example",
    			HostnamePrefix:       "example",
    			SshPublicKeys:        []string{"example"},
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudVmCluster } from "jsr:@ubx/sdk-aws/aws/odb/cloud_vm_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cloud vm cluster" });

      resource(CloudVmCluster, "ci-runner", {
        cpuCoreCount: 1,
        dataStorageSizeInTbs: 1,
        dbServers: ["example"],
        displayName: "example-display-name",
        giVersion: "example",
        hostnamePrefix: "example",
        sshPublicKeys: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.odb import CloudVmCluster, CloudVmClusterConfig

    def describe():
        ubx.intent("payments own cloud vm cluster")

        ubx.resource(CloudVmCluster, "ci-runner", CloudVmClusterConfig(
            cpu_core_count=1,
            data_storage_size_in_tbs=1,
            db_servers=["example"],
            display_name="example-display-name",
            gi_version="example",
            hostname_prefix="example",
            ssh_public_keys=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_odb_cloud_vm_cluster, with cpu core count 1, data
    storage size in tbs 1, db servers a set value, display name
    example-display-name, gi version example, hostname prefix
    example, ssh public keys a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_exadata_infrastructure_arn" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_id" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cluster_name" type="string">
  The name of the Grid Infrastructure (GI) cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cpu_core_count" type="number" required>
  The number of CPU cores to enable on the VM cluster. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="data_collection_options" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="is_diagnostics_events_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="is_health_monitoring_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="is_incident_logs_enabled" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_storage_size_in_tbs" type="number" required>
  The size of the data disk group, in terabytes (TBs), to allocate for the VM cluster. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="db_node_storage_size_in_gbs" type="number">
  The amount of local node storage, in gigabytes (GBs), to allocate for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="db_servers" type="set(string)" required>
  The list of database servers for the VM cluster. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  A user-friendly name for the VM cluster. This member is required. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="gi_version" type="string" required>
  A valid software version of Oracle Grid Infrastructure (GI). To get the list of valid values, use the ListGiVersions operation and specify the shape of the Exadata infrastructure. Example: 19.0.0.0 This member is required. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="hostname_prefix" type="string" required>
  The host name prefix for the VM cluster. Constraints: - Can't be "localhost" or "hostname". - Can't contain "-version". - The maximum length of the combined hostname and domain is 63 characters. - The hostname must be unique within the subnet. This member is required. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="is_local_backup_enabled" type="bool">
  Specifies whether to enable database backups to local Exadata storage for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_sparse_diskgroup_enabled" type="bool">
  Specifies whether to create a sparse disk group for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_model" type="string">
  The Oracle license model to apply to the VM cluster. Default: LICENSE_INCLUDED. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="memory_size_in_gbs" type="number">
  The amount of memory, in gigabytes (GBs), to allocate for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_network_arn" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_network_id" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scan_listener_port_tcp" type="number">
  The port number for TCP connections to the single client access name (SCAN) listener. Valid values: 1024 -- 8999 with the following exceptions: 2484 , 6100 , 6200 , 7060, 7070 , 7085 , and 7879Default: 1521. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ssh_public_keys" type="set(string)" required>
  The public key portion of one or more key pairs used for SSH access to the VM cluster. This member is required. Changing this will create a new resource. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timezone" type="string">
  The configured time zone of the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_arn" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_id" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cluster_name" type="string">
  The name of the Grid Infrastructure (GI) cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="compute_model" type="string">
  The compute model used when the instance is created or cloned  --  either ECPU or OCPU. ECPU is a virtualized compute unit; OCPU is a physical processor core with hyper-threading. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp when the VM cluster was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_collection_options" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="is_diagnostics_events_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="is_health_monitoring_enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="is_incident_logs_enabled" type="bool" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="db_node_storage_size_in_gbs" type="number">
  The amount of local node storage, in gigabytes (GBs), to allocate for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disk_redundancy" type="string">
  The type of redundancy for the VM cluster: NORMAL (2-way) or HIGH (3-way). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain" type="string">
  The domain name associated with the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gi_version_computed" type="string">
  A complete software version of Oracle Grid Infrastructure (GI). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hostname_prefix_computed" type="string">
  The host name for the VM cluster. Constraints: - Can't be "localhost" or "hostname". - Can't contain "-version". - The maximum length of the combined hostname and domain is 63 characters. - The hostname must be unique within the subnet. This member is required. Changing this will create a new resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="iorm_config_cache" type="list(object)">
  The Exadata IORM (I/O Resource Manager) configuration cache details for the VM cluster. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="db_plans" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="db_name" type="string">
        </ResponseField>
        <ResponseField name="flash_cache_limit" type="string">
        </ResponseField>
        <ResponseField name="share" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lifecycle_details" type="string">
    </ResponseField>
    <ResponseField name="lifecycle_state" type="string">
    </ResponseField>
    <ResponseField name="objective" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_local_backup_enabled" type="bool">
  Specifies whether to enable database backups to local Exadata storage for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_sparse_diskgroup_enabled" type="bool">
  Specifies whether to create a sparse disk group for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_update_history_entry_id" type="string">
  The OCID of the most recent maintenance update history entry. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="license_model" type="string">
  The Oracle license model to apply to the VM cluster. Default: LICENSE_INCLUDED. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_port" type="number">
  The listener port number configured on the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="memory_size_in_gbs" type="number">
  The amount of memory, in gigabytes (GBs), to allocate for the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="node_count" type="number">
  The total number of nodes in the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_resource_anchor_name" type="string">
  The name of the OCI resource anchor associated with the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_url" type="string">
  The HTTPS link to the VM cluster resource in OCI. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ocid" type="string">
  The OCID (Oracle Cloud Identifier) of the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="odb_network_arn" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_network_id" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  The percentage of progress made on the current operation for the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scan_dns_name" type="string">
  The fully qualified domain name (FQDN) for the SCAN IP addresses associated with the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="scan_dns_record_id" type="string">
  The OCID of the DNS record for the SCAN IPs linked to the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="scan_ip_ids" type="list(string)">
  The list of OCIDs for SCAN IP addresses associated with the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="scan_listener_port_tcp" type="number">
  The port number for TCP connections to the single client access name (SCAN) listener. Valid values: 1024 -- 8999 with the following exceptions: 2484 , 6100 , 6200 , 7060, 7070 , 7085 , and 7879Default: 1521. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shape" type="string">
  The hardware model name of the Exadata infrastructure running the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle status of the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Additional information regarding the current status of the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_size_in_gbs" type="number">
  The local node storage allocated to the VM cluster, in gigabytes (GB). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="system_version" type="string">
  The operating system version of the image chosen for the VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timezone" type="string">
  The configured time zone of the VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vip_ids" type="list(string)">
  The virtual IP (VIP) addresses assigned to the VM cluster. CRS assigns one VIP per node for failover support. Computed by AWS after `ship`.
</ResponseField>
