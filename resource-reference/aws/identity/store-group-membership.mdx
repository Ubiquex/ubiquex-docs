---
title: "aws_identity_store_group_membership"
description: "`aws_identity_store_group_membership` adds a real user as a member of an `aws_identity_store_group`..."
---

`aws_identity_store_group_membership` adds a real user as a member of an `aws_identity_store_group`, the resource that actually grants that user whatever access has been assigned to the group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own store group membership"})

    		ubx.Resource(identity.StoreGroupMembership, "ci-runner", identity.StoreGroupMembershipConfig{
    			GroupId:         "example",
    			IdentityStoreId: "example",
    			MemberId:        map[string]any{"user_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { StoreGroupMembership } from "./local-sdk/aws/sdk/typescript/aws/identity/store_group_membership.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own store group membership" });

      resource(StoreGroupMembership, "ci-runner", {
        groupId: "example",
        identityStoreId: "example",
        memberId: { userId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.identity import StoreGroupMembership, StoreGroupMembershipConfig

    def describe():
        ubx.intent("payments own store group membership")

        ubx.resource(StoreGroupMembership, "ci-runner", StoreGroupMembershipConfig(
            group_id="example",
            identity_store_id="example",
            member_id={"user_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_identity_store_group_membership, with group id
    example, identity store id example, member id a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="group_id" type="string" required>
  The unique identifier for a group in the identity store. Required.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  The globally unique identifier for the identity store. Required.
</ResponseField>

<ResponseField name="member_id" type="object" required>
  An object containing the identifier of a group member. Required.
  <Expandable title="properties">
    <ResponseField name="user_id" type="string" required>
      The identifier for a user in the identity store. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="membership_id" type="string">
  The identifier for a GroupMembership in the identity store. Computed by AWS after `ship`.
</ResponseField>
