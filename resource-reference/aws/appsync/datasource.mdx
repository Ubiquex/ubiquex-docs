---
title: "aws_appsync_datasource"
description: "Real, generated bindings for `aws_appsync_datasource`."
---

`aws_appsync_datasource` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"

    cfg := appsync.DatasourceConfig{
        ApiId: "example",
        Name: "example-name",
        Type: "example",
        Description: "example",
        ServiceRoleArn: "example",
    }
    ubx.Resource(appsync.Datasource, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Datasource } from
      "jsr:@ubx/sdk-aws/aws/appsync/datasource";

    resource(Datasource, "example", {
      apiId: "example",
      name: "example",
      type: "example",
      description: "example",
      serviceRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import Datasource, DatasourceConfig

    cfg = DatasourceConfig(
        api_id="example",
        name="example",
        type="example",
        description="example",
        service_role_arn="example",
    )
    ubx.resource(Datasource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_datasource with api_id, name, type,
description, service_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
