---
title: "aws_appsync_resolver"
description: "Real, generated bindings for `aws_appsync_resolver`."
---

`aws_appsync_resolver` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resolver"})

    		ubx.Resource(appsync.Resolver, "ci-runner", appsync.ResolverConfig{
    			ApiId:           "example",
    			Field:           "example",
    			Type:            "example",
    			Code:            "example",
    			DataSource:      "example",
    			Kind:            "example",
    			MaxBatchSize:    1,
    			RequestTemplate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Resolver } from "jsr:@ubx/sdk-aws/aws/appsync/resolver";

    export default stack("payments", () => {
      intent({ summary: "payments own resolver" });

      resource(Resolver, "ci-runner", {
        apiId: "example",
        field: "example",
        type: "example",
        code: "example",
        dataSource: "example",
        kind: "example",
        maxBatchSize: 1,
        requestTemplate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import Resolver, ResolverConfig

    def describe():
        ubx.intent("payments own resolver")

        ubx.resource(Resolver, "ci-runner", ResolverConfig(
            api_id="example",
            field="example",
            type="example",
            code="example",
            data_source="example",
            kind="example",
            max_batch_size=1,
            request_template="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_resolver, with api id example, field example,
    type example, code example, data source example, kind example,
    max batch size 1, request template example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="caching_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="caching_keys" type="set(string)">
    </ResponseField>
    <ResponseField name="ttl" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="code" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_source" type="string">
  Optional.
</ResponseField>

<ResponseField name="field" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kind" type="string">
  Optional.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Optional.
</ResponseField>

<ResponseField name="pipeline_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="functions" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="request_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="response_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="runtime" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="runtime_version" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sync_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="conflict_detection" type="string">
    </ResponseField>
    <ResponseField name="conflict_handler" type="string">
    </ResponseField>
    <ResponseField name="lambda_conflict_handler_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="lambda_conflict_handler_arn" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caching_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="caching_keys" type="set(string)">
    </ResponseField>
    <ResponseField name="ttl" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pipeline_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="functions" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="runtime" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="runtime_version" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sync_config" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="conflict_detection" type="string">
    </ResponseField>
    <ResponseField name="conflict_handler" type="string">
    </ResponseField>
    <ResponseField name="lambda_conflict_handler_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="lambda_conflict_handler_arn" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
