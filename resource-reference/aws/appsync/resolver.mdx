---
title: "aws_appsync_resolver"
description: "Real, generated bindings for `aws_appsync_resolver`."
---

`aws_appsync_resolver` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"

    cfg := appsync.ResolverConfig{
        ApiId: "example",
        Field: "example",
        Type: "example",
        Code: "example",
        DataSource: "example",
    }
    ubx.Resource(appsync.Resolver, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Resolver } from "jsr:@ubx/sdk-aws/aws/appsync/resolver";

    resource(Resolver, "example", {
      apiId: "example",
      field: "example",
      type: "example",
      code: "example",
      dataSource: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import Resolver, ResolverConfig

    cfg = ResolverConfig(
        api_id="example",
        field="example",
        type="example",
        code="example",
        data_source="example",
    )
    ubx.resource(Resolver, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_resolver with api_id, field, type, code,
data_source.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="code" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_source" type="string">
  Optional.
</ResponseField>

<ResponseField name="field" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kind" type="string">
  Optional.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="request_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="response_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
