---
title: "aws_appsync_domain_name_api_association"
description: "Real, generated bindings for `aws_appsync_domain_name_api_association`."
---

`aws_appsync_domain_name_api_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appsync"

    cfg := appsync.DomainNameApiAssociationConfig{
        ApiId: "example",
        DomainName: "example-domain-name",
    }
    ubx.Resource(appsync.DomainNameApiAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainNameApiAssociation } from
      "jsr:@ubx/sdk-aws/aws/appsync/domain_name_api_association";

    resource(DomainNameApiAssociation, "example", {
      apiId: "example",
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import (
        DomainNameApiAssociation,
        DomainNameApiAssociationConfig,
    )

    cfg = DomainNameApiAssociationConfig(
        api_id="example",
        domain_name="example",
    )
    ubx.resource(DomainNameApiAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_domain_name_api_association with api_id,
domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
