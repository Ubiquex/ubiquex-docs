---
title: "aws_appsync_domain_name_api_association"
description: "Real, generated bindings for `aws_appsync_domain_name_api_association`."
---

`aws_appsync_domain_name_api_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain name api association"})

    		ubx.Resource(appsync.DomainNameApiAssociation, "ci-runner", appsync.DomainNameApiAssociationConfig{
    			ApiId:      "example",
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainNameApiAssociation } from "jsr:@ubx/sdk-aws/aws/appsync/domain_name_api_association";

    export default stack("payments", () => {
      intent({ summary: "payments own domain name api association" });

      resource(DomainNameApiAssociation, "ci-runner", {
        apiId: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import DomainNameApiAssociation, DomainNameApiAssociationConfig

    def describe():
        ubx.intent("payments own domain name api association")

        ubx.resource(DomainNameApiAssociation, "ci-runner", DomainNameApiAssociationConfig(
            api_id="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_domain_name_api_association, with api id
    example, domain name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
