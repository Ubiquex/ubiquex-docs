---
title: "aws_app_sync_domain_name_api_association"
description: "`aws_app_sync_domain_name_api_association` is the actual link between an `aws_app_sync_domain_name` and the `aws_app_sync_api` it should route to."
---

A registered domain with no association routes nowhere.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own domain name api association"})

    		ubx.Resource(appsync.DomainNameApiAssociation, "example", appsync.DomainNameApiAssociationConfig{
    			ApiId:      "example",
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainNameApiAssociation } from "@ubx/sdk-aws/aws/appsync/domain_name_api_association";

    export default stack("example", () => {
      intent({ summary: "example own domain name api association" });

      resource(DomainNameApiAssociation, "example", {
        apiId: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import DomainNameApiAssociation, DomainNameApiAssociationConfig

    def describe():
        ubx.intent("example own domain name api association")

        ubx.resource(DomainNameApiAssociation, "example", DomainNameApiAssociationConfig(
            api_id="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_sync_domain_name_api_association, with api id
    example, domain name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The ID of the AppSync API to associate with the custom domain name. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  The custom domain name (e.g., api.example.com) that is associated with the AWS AppSync API. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="api_association_identifier" type="string">
  The unique identifier that AWS AppSync assigns to the domain name -- API association, usable to reference the association in API calls and automation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
