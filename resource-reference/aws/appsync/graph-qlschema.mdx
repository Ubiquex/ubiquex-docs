---
title: "aws_app_sync_graph_qlschema"
description: "`aws_app_sync_graph_qlschema` is the GraphQL schema document attached to an `aws_app_sync_graph_qlapi`..."
---

`aws_app_sync_graph_qlschema` is the GraphQL schema document attached to an `aws_app_sync_graph_qlapi`, the real type definitions AppSync validates queries and mutations against and routes to resolvers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own graph qlschema"})

    		ubx.Resource(appsync.GraphQlschema, "example", appsync.GraphQlschemaConfig{
    			ApiId:                "example",
    			Definition:           "example",
    			DefinitionS3Location: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GraphQlschema } from "@ubx/sdk-aws/aws/appsync/graph_qlschema";

    export default stack("example", () => {
      intent({ summary: "example own graph qlschema" });

      resource(GraphQlschema, "example", {
        apiId: "example",
        definition: "example",
        definitionS3Location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import GraphQlschema, GraphQlschemaConfig

    def describe():
        ubx.intent("example own graph qlschema")

        ubx.resource(GraphQlschema, "example", GraphQlschemaConfig(
            api_id="example",
            definition="example",
            definition_s3_location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_sync_graph_qlschema, with api id example,
    definition example, definition s3 location example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The unique identifier (API ID) of the AWS AppSync GraphQL API to which this schema definition is attached. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="definition" type="string">
  The GraphQL schema definition text, which defines the types, queries, mutations, and subscriptions for the AppSync GraphQL API. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="definition_s3_location" type="string">
  The S3 URI (e.g., s3://bucket/key) of the GraphQL schema definition file, used as an alternative to providing the schema inline via the `definition` argument. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The id is the GraphQL API ID that this schema is attached to. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
