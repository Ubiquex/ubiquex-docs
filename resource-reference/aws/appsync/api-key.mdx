---
title: "aws_appsync_api_key"
description: "Real, generated bindings for `aws_appsync_api_key`."
---

`aws_appsync_api_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api key"})

    		ubx.Resource(appsync.ApiKey, "ci-runner", appsync.ApiKeyConfig{
    			ApiId:       "example",
    			Description: "Managed by ubx.",
    			Expires:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiKey } from "jsr:@ubx/sdk-aws/aws/appsync/api_key";

    export default stack("payments", () => {
      intent({ summary: "payments own api key" });

      resource(ApiKey, "ci-runner", {
        apiId: "example",
        description: "Managed by ubx.",
        expires: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import ApiKey, ApiKeyConfig

    def describe():
        ubx.intent("payments own api key")

        ubx.resource(ApiKey, "ci-runner", ApiKeyConfig(
            api_id="example",
            description="Managed by ubx.",
            expires="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_api_key, with api id example, description
    Managed by ubx., expires example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="expires" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="api_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
