---
title: "aws_appsync_api_key"
description: "Real, generated bindings for `aws_appsync_api_key`."
---

`aws_appsync_api_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appsync"

    cfg := appsync.ApiKeyConfig{
        ApiId: "example",
        Description: "example",
        Expires: "example",
    }
    ubx.Resource(appsync.ApiKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ApiKey } from "jsr:@ubx/sdk-aws/aws/appsync/api_key";

    resource(ApiKey, "example", {
      apiId: "example",
      description: "example",
      expires: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import ApiKey, ApiKeyConfig

    cfg = ApiKeyConfig(
        api_id="example",
        description="example",
        expires="example",
    )
    ubx.resource(ApiKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_api_key with api_id, description, expires.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="expires" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="api_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
