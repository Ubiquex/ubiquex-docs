---
title: "aws_appsync_channel_namespace"
description: "Real, generated bindings for `aws_appsync_channel_namespace`."
---

`aws_appsync_channel_namespace` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own channel namespace"})

    		ubx.Resource(appsync.ChannelNamespace, "ci-runner", appsync.ChannelNamespaceConfig{
    			ApiId:        "example",
    			Name:         "ci-runner",
    			CodeHandlers: "example",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ChannelNamespace } from "jsr:@ubx/sdk-aws/aws/appsync/channel_namespace";

    export default stack("payments", () => {
      intent({ summary: "payments own channel namespace" });

      resource(ChannelNamespace, "ci-runner", {
        apiId: "example",
        name: "ci-runner",
        codeHandlers: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import ChannelNamespace, ChannelNamespaceConfig

    def describe():
        ubx.intent("payments own channel namespace")

        ubx.resource(ChannelNamespace, "ci-runner", ChannelNamespaceConfig(
            api_id="example",
            name="ci-runner",
            code_handlers="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_channel_namespace called "ci-runner", with api
    id example, code handlers example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="code_handlers" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="channel_namespace_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
