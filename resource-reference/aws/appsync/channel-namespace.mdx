---
title: "aws_appsync_channel_namespace"
description: "Real, generated bindings for `aws_appsync_channel_namespace`."
---

`aws_appsync_channel_namespace` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"

    cfg := appsync.ChannelNamespaceConfig{
        ApiId: "example",
        Name: "example-name",
        CodeHandlers: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appsync.ChannelNamespace, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ChannelNamespace } from
      "jsr:@ubx/sdk-aws/aws/appsync/channel_namespace";

    resource(ChannelNamespace, "example", {
      apiId: "example",
      name: "example",
      codeHandlers: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import (
        ChannelNamespace,
        ChannelNamespaceConfig,
    )

    cfg = ChannelNamespaceConfig(
        api_id="example",
        name="example",
        code_handlers="example",
        tags={"key": "value"},
    )
    ubx.resource(ChannelNamespace, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_channel_namespace with api_id, name,
code_handlers, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="code_handlers" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="channel_namespace_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
