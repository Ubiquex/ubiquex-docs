---
title: "aws_appsync_domain_name"
description: "Real, generated bindings for `aws_appsync_domain_name`."
---

`aws_appsync_domain_name` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appsync"

    cfg := appsync.DomainNameConfig{
        CertificateArn: "example",
        DomainName: "example-domain-name",
        Description: "example",
    }
    ubx.Resource(appsync.DomainName, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainName } from
      "jsr:@ubx/sdk-aws/aws/appsync/domain_name";

    resource(DomainName, "example", {
      certificateArn: "example",
      domainName: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import DomainName, DomainNameConfig

    cfg = DomainNameConfig(
        certificate_arn="example",
        domain_name="example",
        description="example",
    )
    ubx.resource(DomainName, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_domain_name with certificate_arn,
domain_name, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="appsync_domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hosted_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
