---
title: "aws_appsync_function"
description: "Real, generated bindings for `aws_appsync_function`."
---

`aws_appsync_function` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function"})

    		ubx.Resource(appsync.Function, "ci-runner", appsync.FunctionConfig{
    			ApiId:                   "example",
    			DataSource:              "example",
    			Name:                    "ci-runner",
    			Code:                    "example",
    			Description:             "Managed by ubx.",
    			MaxBatchSize:            1,
    			RequestMappingTemplate:  "example",
    			ResponseMappingTemplate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Function } from "jsr:@ubx/sdk-aws/aws/appsync/function";

    export default stack("payments", () => {
      intent({ summary: "payments own function" });

      resource(Function, "ci-runner", {
        apiId: "example",
        dataSource: "example",
        name: "ci-runner",
        code: "example",
        description: "Managed by ubx.",
        maxBatchSize: 1,
        requestMappingTemplate: "example",
        responseMappingTemplate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import Function, FunctionConfig

    def describe():
        ubx.intent("payments own function")

        ubx.resource(Function, "ci-runner", FunctionConfig(
            api_id="example",
            data_source="example",
            name="ci-runner",
            code="example",
            description="Managed by ubx.",
            max_batch_size=1,
            request_mapping_template="example",
            response_mapping_template="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_function called "ci-runner", with api id
    example, data source example, code example, description Managed
    by ubx., max batch size 1, request mapping template example,
    response mapping template example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="code" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="function_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="request_mapping_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="response_mapping_template" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
