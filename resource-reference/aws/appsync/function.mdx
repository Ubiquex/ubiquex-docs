---
title: "aws_appsync_function"
description: "Real, generated bindings for `aws_appsync_function`."
---

`aws_appsync_function` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appsync"

    cfg := appsync.FunctionConfig{
        ApiId: "example",
        DataSource: "example",
        Name: "example-name",
        Code: "example",
        Description: "example",
    }
    ubx.Resource(appsync.Function, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Function } from "jsr:@ubx/sdk-aws/aws/appsync/function";

    resource(Function, "example", {
      apiId: "example",
      dataSource: "example",
      name: "example",
      code: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import Function, FunctionConfig

    cfg = FunctionConfig(
        api_id="example",
        data_source="example",
        name="example",
        code="example",
        description="example",
    )
    ubx.resource(Function, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_function with api_id, data_source, name,
code, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="code" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="function_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="request_mapping_template" type="string">
  Optional.
</ResponseField>

<ResponseField name="response_mapping_template" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
