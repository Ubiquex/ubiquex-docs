---
title: "aws_appsync_api"
description: "Real, generated bindings for `aws_appsync_api`."
---

`aws_appsync_api` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appsync"

    cfg := appsync.ApiConfig{
        Name: "example-name",
        OwnerContact: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appsync.Api, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Api } from "jsr:@ubx/sdk-aws/aws/appsync/api";

    resource(Api, "example", {
      name: "example",
      ownerContact: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appsync import Api, ApiConfig

    cfg = ApiConfig(
        name="example",
        owner_contact="example",
        tags={"key": "value"},
    )
    ubx.resource(Api, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appsync_api with name, owner_contact, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owner_contact" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="api_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="api_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="waf_web_acl_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="xray_enabled" type="bool">
  Computed by AWS after `ship`.
</ResponseField>
