---
title: "aws_appsync_api_cache"
description: "Real, generated bindings for `aws_appsync_api_cache`."
---

`aws_appsync_api_cache` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api cache"})

    		ubx.Resource(appsync.ApiCache, "ci-runner", appsync.ApiCacheConfig{
    			ApiCachingBehavior: "example",
    			ApiId:              "example",
    			Ttl:                1,
    			Type:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiCache } from "jsr:@ubx/sdk-aws/aws/appsync/api_cache";

    export default stack("payments", () => {
      intent({ summary: "payments own api cache" });

      resource(ApiCache, "ci-runner", {
        apiCachingBehavior: "example",
        apiId: "example",
        ttl: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync import ApiCache, ApiCacheConfig

    def describe():
        ubx.intent("payments own api cache")

        ubx.resource(ApiCache, "ci-runner", ApiCacheConfig(
            api_caching_behavior="example",
            api_id="example",
            ttl=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appsync_api_cache, with api caching behavior example,
    api id example, ttl 1, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_caching_behavior" type="string" required>
  Required.
</ResponseField>

<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="at_rest_encryption_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_encryption_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ttl" type="number" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="at_rest_encryption_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_encryption_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
