---
title: "aws_app_sync_api_cache"
description: "`aws_app_sync_api_cache` enables server-side caching for an `aws_app_sync_api`, holding resolver results in a managed..."
---

`aws_app_sync_api_cache` enables server-side caching for an `aws_app_sync_api`, holding resolver results in a managed, in-memory cache so repeated queries return with lower latency instead of re-invoking their data sources every time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sync api cache"})

    		ubx.Resource(appsync.ApiCache, "example", appsync.ApiCacheConfig{
    			ApiCachingBehavior:       "example",
    			ApiId:                    "example",
    			Ttl:                      1,
    			Type:                     "example",
    			AtRestEncryptionEnabled:  true,
    			HealthMetricsConfig:      "example",
    			TransitEncryptionEnabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiCache } from "@ubx/sdk-aws/aws/appsync/api_cache";

    export default stack("example", () => {
      intent({ summary: "example own sync api cache" });

      resource(ApiCache, "example", {
        apiCachingBehavior: "example",
        apiId: "example",
        ttl: 1,
        type: "example",
        atRestEncryptionEnabled: true,
        healthMetricsConfig: "example",
        transitEncryptionEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appsync.api_cache import ApiCache, ApiCacheConfig

    def describe():
        ubx.intent("example own sync api cache")

        ubx.resource(ApiCache, "example", ApiCacheConfig(
            api_caching_behavior="example",
            api_id="example",
            ttl=1,
            type="example",
            at_rest_encryption_enabled=True,
            health_metrics_config="example",
            transit_encryption_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_sync_api_cache, with api caching behavior example,
    api id example, ttl 1, type example, at rest encryption enabled
    true, health metrics config example, transit encryption enabled
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_caching_behavior" type="string" required>
  Determines the caching behavior for the AppSync API cache, either caching entire requests (FULL_REQUEST_CACHING) or only per-resolver results (PER_RESOLVER_CACHING). Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="api_id" type="string" required>
  The unique ID of the AppSync API to which this cache configuration applies. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="at_rest_encryption_enabled" type="bool">
  Indicates whether encryption at rest is enabled for the AppSync API cache, protecting cached data in the underlying ElastiCache for Redis cluster. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="health_metrics_config" type="string">
  Configures whether cache health metrics are enabled for the AppSync API cache, with valid values ENABLED or DISABLED. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="transit_encryption_enabled" type="bool">
  Indicates whether encryption in transit is enabled for the AppSync API cache. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ttl" type="number" required>
  Specifies the time-to-live (TTL) in seconds for cached responses, controlling how long items remain in the API cache before being purged. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string" required>
  The cache instance type for the AppSync API cache (e.g., T2_SMALL, T2_MEDIUM, R4_LARGE), which determines the cache size and performance characteristics. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
