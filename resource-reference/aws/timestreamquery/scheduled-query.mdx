---
title: "aws_timestreamquery_scheduled_query"
description: "Real, generated bindings for `aws_timestreamquery_scheduled_query`."
---

`aws_timestreamquery_scheduled_query` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/timestreamquery"

    cfg := timestreamquery.ScheduledQueryConfig{
        ExecutionRoleArn: "example",
        Name: "example-name",
        QueryString: "example",
        KmsKeyId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(timestreamquery.ScheduledQuery, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ScheduledQuery } from
      "jsr:@ubx/sdk-aws/aws/timestreamquery/scheduled_query";

    resource(ScheduledQuery, "example", {
      executionRoleArn: "example",
      name: "example",
      queryString: "example",
      kmsKeyId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.timestreamquery import (
        ScheduledQuery,
        ScheduledQueryConfig,
    )

    cfg = ScheduledQueryConfig(
        execution_role_arn="example",
        name="example",
        query_string="example",
        kms_key_id="example",
        tags={"key": "value"},
    )
    ubx.resource(ScheduledQuery, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_timestreamquery_scheduled_query with
execution_role_arn, name, query_string, kms_key_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="next_invocation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="previous_invocation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
