---
title: "aws_timestreamquery_scheduled_query"
description: "Real, generated bindings for `aws_timestreamquery_scheduled_query`."
---

`aws_timestreamquery_scheduled_query` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	timestreamquery "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/timestreamquery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scheduled query"})

    		ubx.Resource(timestreamquery.ScheduledQuery, "ci-runner", timestreamquery.ScheduledQueryConfig{
    			ExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:             "ci-runner",
    			QueryString:      "example",
    			KmsKeyId:         "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScheduledQuery } from "jsr:@ubx/sdk-aws/aws/timestreamquery/scheduled_query";

    export default stack("payments", () => {
      intent({ summary: "payments own scheduled query" });

      resource(ScheduledQuery, "ci-runner", {
        executionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        queryString: "example",
        kmsKeyId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.timestreamquery import ScheduledQuery, ScheduledQueryConfig

    def describe():
        ubx.intent("payments own scheduled query")

        ubx.resource(ScheduledQuery, "ci-runner", ScheduledQueryConfig(
            execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            query_string="example",
            kms_key_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_timestreamquery_scheduled_query called "ci-runner",
    with execution role arn arn:aws:iam::123456789012:policy/ci,
    query string example, kms key id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="next_invocation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="previous_invocation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
