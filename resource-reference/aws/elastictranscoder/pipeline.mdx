---
title: "aws_elastictranscoder_pipeline"
description: "Real, generated bindings for `aws_elastictranscoder_pipeline`."
---

`aws_elastictranscoder_pipeline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastictranscoder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastictranscoder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pipeline"})

    		ubx.Resource(elastictranscoder.Pipeline, "ci-runner", elastictranscoder.PipelineConfig{
    			InputBucket:  "example",
    			Role:         "example",
    			Name:         "ci-runner",
    			AwsKmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Pipeline } from "jsr:@ubx/sdk-aws/aws/elastictranscoder/pipeline";

    export default stack("payments", () => {
      intent({ summary: "payments own pipeline" });

      resource(Pipeline, "ci-runner", {
        inputBucket: "example",
        role: "example",
        name: "ci-runner",
        awsKmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elastictranscoder import Pipeline, PipelineConfig

    def describe():
        ubx.intent("payments own pipeline")

        ubx.resource(Pipeline, "ci-runner", PipelineConfig(
            input_bucket="example",
            role="example",
            name="ci-runner",
            aws_kms_key_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastictranscoder_pipeline called "ci-runner", with
    input bucket example, role example, aws kms key arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="input_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="output_bucket" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="output_bucket" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
