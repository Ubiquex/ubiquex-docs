---
title: "aws_networkflowmonitor_scope"
description: "Real, generated bindings for `aws_networkflowmonitor_scope`."
---

`aws_networkflowmonitor_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkflowmonitor"

    cfg := networkflowmonitor.ScopeConfig{
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkflowmonitor.Scope, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Scope } from
      "jsr:@ubx/sdk-aws/aws/networkflowmonitor/scope";

    resource(Scope, "example", {
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkflowmonitor import Scope, ScopeConfig

    cfg = ScopeConfig(
        tags={"key": "value"},
    )
    ubx.resource(Scope, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkflowmonitor_scope with tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="scope_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
