---
title: "aws_networkflowmonitor_scope"
description: "Real, generated bindings for `aws_networkflowmonitor_scope`."
---

`aws_networkflowmonitor_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkflowmonitor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkflowmonitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scope"})

    		ubx.Resource(networkflowmonitor.Scope, "ci-runner", networkflowmonitor.ScopeConfig{
    			Tags: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Scope } from "jsr:@ubx/sdk-aws/aws/networkflowmonitor/scope";

    export default stack("payments", () => {
      intent({ summary: "payments own scope" });

      resource(Scope, "ci-runner", {
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkflowmonitor import Scope, ScopeConfig

    def describe():
        ubx.intent("payments own scope")

        ubx.resource(Scope, "ci-runner", ScopeConfig(
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkflowmonitor_scope, with tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="scope_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
