---
title: "aws_networkflowmonitor_monitor"
description: "Real, generated bindings for `aws_networkflowmonitor_monitor`."
---

`aws_networkflowmonitor_monitor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkflowmonitor"

    cfg := networkflowmonitor.MonitorConfig{
        MonitorName: "example-monitor-name",
        ScopeArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkflowmonitor.Monitor, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Monitor } from
      "jsr:@ubx/sdk-aws/aws/networkflowmonitor/monitor";

    resource(Monitor, "example", {
      monitorName: "example",
      scopeArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkflowmonitor import Monitor, MonitorConfig

    cfg = MonitorConfig(
        monitor_name="example",
        scope_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(Monitor, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkflowmonitor_monitor with monitor_name,
scope_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="monitor_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
