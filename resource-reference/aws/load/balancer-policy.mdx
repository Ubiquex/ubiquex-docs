---
title: "aws_load_balancer_policy"
description: "Real, generated bindings for `aws_load_balancer_policy`."
---

`aws_load_balancer_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	load "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/load"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own balancer policy"})

    		ubx.Resource(load.BalancerPolicy, "ci-runner", load.BalancerPolicyConfig{
    			LoadBalancerName: "example-load-balancer-name",
    			PolicyName:       "example-policy-name",
    			PolicyTypeName:   "example-policy-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BalancerPolicy } from "jsr:@ubx/sdk-aws/aws/load/balancer_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own balancer policy" });

      resource(BalancerPolicy, "ci-runner", {
        loadBalancerName: "example-load-balancer-name",
        policyName: "example-policy-name",
        policyTypeName: "example-policy-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.load import BalancerPolicy, BalancerPolicyConfig

    def describe():
        ubx.intent("payments own balancer policy")

        ubx.resource(BalancerPolicy, "ci-runner", BalancerPolicyConfig(
            load_balancer_name="example-load-balancer-name",
            policy_name="example-policy-name",
            policy_type_name="example-policy-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_load_balancer_policy, with load balancer name
    example-load-balancer-name, policy name example-policy-name,
    policy type name example-policy-type-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="load_balancer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
