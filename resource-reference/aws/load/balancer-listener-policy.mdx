---
title: "aws_load_balancer_listener_policy"
description: "Real, generated bindings for `aws_load_balancer_listener_policy`."
---

`aws_load_balancer_listener_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	load "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/load"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own balancer listener policy"})

    		ubx.Resource(load.BalancerListenerPolicy, "ci-runner", load.BalancerListenerPolicyConfig{
    			LoadBalancerName: "example-load-balancer-name",
    			LoadBalancerPort: 1,
    			PolicyNames:      []string{"example"},
    			Triggers:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BalancerListenerPolicy } from "jsr:@ubx/sdk-aws/aws/load/balancer_listener_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own balancer listener policy" });

      resource(BalancerListenerPolicy, "ci-runner", {
        loadBalancerName: "example-load-balancer-name",
        loadBalancerPort: 1,
        policyNames: ["example"],
        triggers: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.load import BalancerListenerPolicy, BalancerListenerPolicyConfig

    def describe():
        ubx.intent("payments own balancer listener policy")

        ubx.resource(BalancerListenerPolicy, "ci-runner", BalancerListenerPolicyConfig(
            load_balancer_name="example-load-balancer-name",
            load_balancer_port=1,
            policy_names=["example"],
            triggers={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_load_balancer_listener_policy, with load balancer name
    example-load-balancer-name, load balancer port 1, policy names a
    set value, triggers a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="load_balancer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="policy_names" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
