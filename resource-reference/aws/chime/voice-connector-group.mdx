---
title: "aws_chime_voice_connector_group"
description: "Real, generated bindings for `aws_chime_voice_connector_group`."
---

`aws_chime_voice_connector_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chime"

    cfg := chime.VoiceConnectorGroupConfig{
        Name: "example-name",
    }
    ubx.Resource(chime.VoiceConnectorGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnectorGroup } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector_group";

    resource(VoiceConnectorGroup, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import (
        VoiceConnectorGroup,
        VoiceConnectorGroupConfig,
    )

    cfg = VoiceConnectorGroupConfig(
        name="example",
    )
    ubx.resource(VoiceConnectorGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector_group with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
