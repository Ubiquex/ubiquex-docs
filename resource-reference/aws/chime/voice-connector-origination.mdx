---
title: "aws_chime_voice_connector_origination"
description: "Real, generated bindings for `aws_chime_voice_connector_origination`."
---

`aws_chime_voice_connector_origination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own voice connector origination"})

    		ubx.Resource(chime.VoiceConnectorOrigination, "ci-runner", chime.VoiceConnectorOriginationConfig{
    			VoiceConnectorId: "example",
    			Disabled:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VoiceConnectorOrigination } from "jsr:@ubx/sdk-aws/aws/chime/voice_connector_origination";

    export default stack("payments", () => {
      intent({ summary: "payments own voice connector origination" });

      resource(VoiceConnectorOrigination, "ci-runner", {
        voiceConnectorId: "example",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.chime import VoiceConnectorOrigination, VoiceConnectorOriginationConfig

    def describe():
        ubx.intent("payments own voice connector origination")

        ubx.resource(VoiceConnectorOrigination, "ci-runner", VoiceConnectorOriginationConfig(
            voice_connector_id="example",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_chime_voice_connector_origination, with voice
    connector id example, disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="host" type="string" required>
    </ResponseField>
    <ResponseField name="port" type="number">
    </ResponseField>
    <ResponseField name="priority" type="number" required>
    </ResponseField>
    <ResponseField name="protocol" type="string" required>
    </ResponseField>
    <ResponseField name="weight" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="host" type="string" required>
    </ResponseField>
    <ResponseField name="port" type="number">
    </ResponseField>
    <ResponseField name="priority" type="number" required>
    </ResponseField>
    <ResponseField name="protocol" type="string" required>
    </ResponseField>
    <ResponseField name="weight" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>
