---
title: "aws_chime_voice_connector_origination"
description: "Real, generated bindings for `aws_chime_voice_connector_origination`."
---

`aws_chime_voice_connector_origination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chime"

    cfg := chime.VoiceConnectorOriginationConfig{
        VoiceConnectorId: "example",
        Disabled: true,
    }
    ubx.Resource(chime.VoiceConnectorOrigination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnectorOrigination } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector_origination";

    resource(VoiceConnectorOrigination, "example", {
      voiceConnectorId: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import (
        VoiceConnectorOrigination,
        VoiceConnectorOriginationConfig,
    )

    cfg = VoiceConnectorOriginationConfig(
        voice_connector_id="example",
        disabled=True,
    )
    ubx.resource(VoiceConnectorOrigination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector_origination with
voice_connector_id, disabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
