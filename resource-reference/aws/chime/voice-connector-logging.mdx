---
title: "aws_chime_voice_connector_logging"
description: "Real, generated bindings for `aws_chime_voice_connector_logging`."
---

`aws_chime_voice_connector_logging` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own voice connector logging"})

    		ubx.Resource(chime.VoiceConnectorLogging, "ci-runner", chime.VoiceConnectorLoggingConfig{
    			VoiceConnectorId:      "example",
    			EnableMediaMetricLogs: true,
    			EnableSipLogs:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VoiceConnectorLogging } from "jsr:@ubx/sdk-aws/aws/chime/voice_connector_logging";

    export default stack("payments", () => {
      intent({ summary: "payments own voice connector logging" });

      resource(VoiceConnectorLogging, "ci-runner", {
        voiceConnectorId: "example",
        enableMediaMetricLogs: true,
        enableSipLogs: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.chime import VoiceConnectorLogging, VoiceConnectorLoggingConfig

    def describe():
        ubx.intent("payments own voice connector logging")

        ubx.resource(VoiceConnectorLogging, "ci-runner", VoiceConnectorLoggingConfig(
            voice_connector_id="example",
            enable_media_metric_logs=True,
            enable_sip_logs=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_chime_voice_connector_logging, with voice connector id
    example, enable media metric logs true, enable sip logs true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enable_media_metric_logs" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_sip_logs" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
