---
title: "aws_chime_voice_connector_logging"
description: "Real, generated bindings for `aws_chime_voice_connector_logging`."
---

`aws_chime_voice_connector_logging` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chime"

    cfg := chime.VoiceConnectorLoggingConfig{
        VoiceConnectorId: "example",
        EnableMediaMetricLogs: true,
        EnableSipLogs: true,
    }
    ubx.Resource(chime.VoiceConnectorLogging, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnectorLogging } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector_logging";

    resource(VoiceConnectorLogging, "example", {
      voiceConnectorId: "example",
      enableMediaMetricLogs: true,
      enableSipLogs: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import (
        VoiceConnectorLogging,
        VoiceConnectorLoggingConfig,
    )

    cfg = VoiceConnectorLoggingConfig(
        voice_connector_id="example",
        enable_media_metric_logs=True,
        enable_sip_logs=True,
    )
    ubx.resource(VoiceConnectorLogging, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector_logging with
voice_connector_id, enable_media_metric_logs, enable_sip_logs.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enable_media_metric_logs" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_sip_logs" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
