---
title: "aws_chime_voice_connector_termination"
description: "Real, generated bindings for `aws_chime_voice_connector_termination`."
---

`aws_chime_voice_connector_termination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own voice connector termination"})

    		ubx.Resource(chime.VoiceConnectorTermination, "ci-runner", chime.VoiceConnectorTerminationConfig{
    			CallingRegions:     []string{"example"},
    			CidrAllowList:      []string{"example"},
    			VoiceConnectorId:   "example",
    			CpsLimit:           1,
    			DefaultPhoneNumber: "example",
    			Disabled:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VoiceConnectorTermination } from "jsr:@ubx/sdk-aws/aws/chime/voice_connector_termination";

    export default stack("payments", () => {
      intent({ summary: "payments own voice connector termination" });

      resource(VoiceConnectorTermination, "ci-runner", {
        callingRegions: ["example"],
        cidrAllowList: ["example"],
        voiceConnectorId: "example",
        cpsLimit: 1,
        defaultPhoneNumber: "example",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.chime import VoiceConnectorTermination, VoiceConnectorTerminationConfig

    def describe():
        ubx.intent("payments own voice connector termination")

        ubx.resource(VoiceConnectorTermination, "ci-runner", VoiceConnectorTerminationConfig(
            calling_regions=["example"],
            cidr_allow_list=["example"],
            voice_connector_id="example",
            cps_limit=1,
            default_phone_number="example",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_chime_voice_connector_termination, with calling
    regions a set value, cidr allow list a set value, voice connector
    id example, cps limit 1, default phone number example, disabled
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="calling_regions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="cidr_allow_list" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="cps_limit" type="number">
  Optional.
</ResponseField>

<ResponseField name="default_phone_number" type="string">
  Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
