---
title: "aws_chime_voice_connector"
description: "Real, generated bindings for `aws_chime_voice_connector`."
---

`aws_chime_voice_connector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chime"

    cfg := chime.VoiceConnectorConfig{
        Name: "example-name",
        RequireEncryption: true,
        AwsRegion: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(chime.VoiceConnector, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnector } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector";

    resource(VoiceConnector, "example", {
      name: "example",
      requireEncryption: true,
      awsRegion: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import VoiceConnector, VoiceConnectorConfig

    cfg = VoiceConnectorConfig(
        name="example",
        require_encryption=True,
        aws_region="example",
        tags={"key": "value"},
    )
    ubx.resource(VoiceConnector, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector with name,
require_encryption, aws_region, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_encryption" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="outbound_host_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
