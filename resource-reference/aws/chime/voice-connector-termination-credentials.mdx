---
title: "aws_chime_voice_connector_termination_credentials"
description: "Real, generated bindings for `aws_chime_voice_connector_termination_credentials`."
---

`aws_chime_voice_connector_termination_credentials` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chime"

    cfg := chime.VoiceConnectorTerminationCredentialsConfig{
        VoiceConnectorId: "example",
    }
    ubx.Resource(
        chime.VoiceConnectorTerminationCredentials,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnectorTerminationCredentials } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector_termination_credentials";

    resource(VoiceConnectorTerminationCredentials, "example", {
      voiceConnectorId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import (
        VoiceConnectorTerminationCredentials,
        VoiceConnectorTerminationCredentialsConfig,
    )

    cfg = VoiceConnectorTerminationCredentialsConfig(
        voice_connector_id="example",
    )
    ubx.resource(
        VoiceConnectorTerminationCredentials,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector_termination_credentials with
voice_connector_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
