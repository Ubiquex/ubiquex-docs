---
title: "aws_chime_voice_connector_streaming"
description: "Real, generated bindings for `aws_chime_voice_connector_streaming`."
---

`aws_chime_voice_connector_streaming` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own voice connector streaming"})

    		ubx.Resource(chime.VoiceConnectorStreaming, "ci-runner", chime.VoiceConnectorStreamingConfig{
    			DataRetention:                1,
    			VoiceConnectorId:             "example",
    			Disabled:                     true,
    			StreamingNotificationTargets: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VoiceConnectorStreaming } from "jsr:@ubx/sdk-aws/aws/chime/voice_connector_streaming";

    export default stack("payments", () => {
      intent({ summary: "payments own voice connector streaming" });

      resource(VoiceConnectorStreaming, "ci-runner", {
        dataRetention: 1,
        voiceConnectorId: "example",
        disabled: true,
        streamingNotificationTargets: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.chime import VoiceConnectorStreaming, VoiceConnectorStreamingConfig

    def describe():
        ubx.intent("payments own voice connector streaming")

        ubx.resource(VoiceConnectorStreaming, "ci-runner", VoiceConnectorStreamingConfig(
            data_retention=1,
            voice_connector_id="example",
            disabled=True,
            streaming_notification_targets=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_chime_voice_connector_streaming, with data retention
    1, voice connector id example, disabled true, streaming
    notification targets a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_retention" type="number" required>
  Required.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="streaming_notification_targets" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
