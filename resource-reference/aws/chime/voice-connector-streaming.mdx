---
title: "aws_chime_voice_connector_streaming"
description: "Real, generated bindings for `aws_chime_voice_connector_streaming`."
---

`aws_chime_voice_connector_streaming` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chime"

    cfg := chime.VoiceConnectorStreamingConfig{
        DataRetention: 1,
        VoiceConnectorId: "example",
        Disabled: true,
        StreamingNotificationTargets: []string{"example"},
    }
    ubx.Resource(chime.VoiceConnectorStreaming, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceConnectorStreaming } from
      "jsr:@ubx/sdk-aws/aws/chime/voice_connector_streaming";

    resource(VoiceConnectorStreaming, "example", {
      dataRetention: 1,
      voiceConnectorId: "example",
      disabled: true,
      streamingNotificationTargets: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chime import (
        VoiceConnectorStreaming,
        VoiceConnectorStreamingConfig,
    )

    cfg = VoiceConnectorStreamingConfig(
        data_retention=1,
        voice_connector_id="example",
        disabled=True,
        streaming_notification_targets=["example"],
    )
    ubx.resource(VoiceConnectorStreaming, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chime_voice_connector_streaming with
data_retention, voice_connector_id, disabled,
streaming_notification_targets.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_retention" type="number" required>
  Required.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="streaming_notification_targets" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
