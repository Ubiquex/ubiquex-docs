---
title: "aws_rekognition_project"
description: "Real, generated bindings for `aws_rekognition_project`."
---

`aws_rekognition_project` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rekognition"

    cfg := rekognition.ProjectConfig{
        Name: "example-name",
        AutoUpdate: "example",
        Feature: "example",
    }
    ubx.Resource(rekognition.Project, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Project } from
      "jsr:@ubx/sdk-aws/aws/rekognition/project";

    resource(Project, "example", {
      name: "example",
      autoUpdate: "example",
      feature: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rekognition import Project, ProjectConfig

    cfg = ProjectConfig(
        name="example",
        auto_update="example",
        feature="example",
    )
    ubx.resource(Project, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rekognition_project with name, auto_update,
feature.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auto_update" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="feature" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="auto_update" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="feature" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
