---
title: "aws_rekognition_collection"
description: "Real, generated bindings for `aws_rekognition_collection`."
---

`aws_rekognition_collection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rekognition"

    cfg := rekognition.CollectionConfig{
        CollectionId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(rekognition.Collection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Collection } from
      "jsr:@ubx/sdk-aws/aws/rekognition/collection";

    resource(Collection, "example", {
      collectionId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rekognition import Collection, CollectionConfig

    cfg = CollectionConfig(
        collection_id="example",
        tags={"key": "value"},
    )
    ubx.resource(Collection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rekognition_collection with collection_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="collection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="face_model_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
