---
title: "aws_rekognition_stream_processor"
description: "Real, generated bindings for `aws_rekognition_stream_processor`."
---

`aws_rekognition_stream_processor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rekognition"

    cfg := rekognition.StreamProcessorConfig{
        Name: "example-name",
        RoleArn: "example",
        KmsKeyId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(rekognition.StreamProcessor, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StreamProcessor } from
      "jsr:@ubx/sdk-aws/aws/rekognition/stream_processor";

    resource(StreamProcessor, "example", {
      name: "example",
      roleArn: "example",
      kmsKeyId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rekognition import (
        StreamProcessor,
        StreamProcessorConfig,
    )

    cfg = StreamProcessorConfig(
        name="example",
        role_arn="example",
        kms_key_id="example",
        tags={"key": "value"},
    )
    ubx.resource(StreamProcessor, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rekognition_stream_processor with name, role_arn,
kms_key_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stream_processor_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
