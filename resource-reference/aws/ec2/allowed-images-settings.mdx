---
title: "aws_ec2_allowed_images_settings"
description: "Real, generated bindings for `aws_ec2_allowed_images_settings`."
---

`aws_ec2_allowed_images_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own allowed images settings"})

    		ubx.Resource(ec2.AllowedImagesSettings, "ci-runner", ec2.AllowedImagesSettingsConfig{
    			State: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AllowedImagesSettings } from "jsr:@ubx/sdk-aws/aws/ec2/allowed_images_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own allowed images settings" });

      resource(AllowedImagesSettings, "ci-runner", {
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import AllowedImagesSettings, AllowedImagesSettingsConfig

    def describe():
        ubx.intent("payments own allowed images settings")

        ubx.resource(AllowedImagesSettings, "ci-runner", AllowedImagesSettingsConfig(
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_allowed_images_settings, with state example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
