---
title: "aws_ec2_vpnconnection_route"
description: "`aws_ec2_vpnconnection_route` defines a real static route for a specific CIDR over an `aws_ec2_vpnconnection`..."
---

`aws_ec2_vpnconnection_route` defines a real static route for a specific CIDR over an `aws_ec2_vpnconnection`, used when the connection isn't using BGP for dynamic route exchange.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpnconnection route"})

    		ubx.Resource(ec2.VpnconnectionRoute, "ci-runner", ec2.VpnconnectionRouteConfig{
    			DestinationCidrBlock: "example",
    			VpnConnectionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpnconnectionRoute } from "./local-sdk/aws/sdk/typescript/aws/ec2/vpnconnection_route.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own vpnconnection route" });

      resource(VpnconnectionRoute, "ci-runner", {
        destinationCidrBlock: "example",
        vpnConnectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VpnconnectionRoute, VpnconnectionRouteConfig

    def describe():
        ubx.intent("payments own vpnconnection route")

        ubx.resource(VpnconnectionRoute, "ci-runner", VpnconnectionRouteConfig(
            destination_cidr_block="example",
            vpn_connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_vpnconnection_route, with destination cidr block
    example, vpn connection id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="destination_cidr_block" type="string" required>
  The CIDR block associated with the local subnet of the customer network. Required.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  The ID of the VPN connection. Required.
</ResponseField>
