---
title: "aws_ec2_transit_gateway_route_table"
description: "`aws_ec2_transit_gateway_route_table` controls real routing between the attachments on an `aws_ec2_transit_gateway`, which VPCs, VPNs..."
---

`aws_ec2_transit_gateway_route_table` controls real routing between the attachments on an `aws_ec2_transit_gateway`, which VPCs, VPNs, and other attachments can reach which others through it, rather than every attachment being able to reach every other one by default.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own transit gateway route table"})

    		ubx.Resource(ec2.TransitGatewayRouteTable, "example", ec2.TransitGatewayRouteTableConfig{
    			TransitGatewayId: "example",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayRouteTable } from "./local-sdk/aws/sdk/typescript/aws/ec2/transit_gateway_route_table.ts";

    export default stack("example", () => {
      intent({ summary: "example own transit gateway route table" });

      resource(TransitGatewayRouteTable, "example", {
        transitGatewayId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayRouteTable, TransitGatewayRouteTableConfig

    def describe():
        ubx.intent("example own transit gateway route table")

        ubx.resource(TransitGatewayRouteTable, "example", TransitGatewayRouteTableConfig(
            transit_gateway_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_route_table, with transit gateway
    id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="list(object)">
  Tags are composed of a Key/Value pair. You can use tags to categorize and track each parameter group. The tag value null is permitted. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to attach to the transit gateway route table, used for identifying and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a key-value tag applied to this EC2 Transit Gateway Route Table, used for identifying or categorizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  The ID of the transit gateway. Required.
</ResponseField>

## Output properties

<ResponseField name="transit_gateway_route_table_id" type="string">
  Transit Gateway Route Table primary identifier Computed by AWS after `ship`.
</ResponseField>
