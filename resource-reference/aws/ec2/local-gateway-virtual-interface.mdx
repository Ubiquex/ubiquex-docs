---
title: "aws_ec2_local_gateway_virtual_interface"
description: "`aws_ec2_local_gateway_virtual_interface` is one real, physical network interface on an AWS Outposts local gateway..."
---

`aws_ec2_local_gateway_virtual_interface` is one real, physical network interface on an AWS Outposts local gateway, the actual link-layer connection an `aws_ec2_local_gateway_route_table` routes traffic across.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own local gateway virtual interface"})

    		ubx.Resource(ec2.LocalGatewayVirtualInterface, "ci-runner", ec2.LocalGatewayVirtualInterfaceConfig{
    			LocalAddress:                        "example",
    			LocalGatewayVirtualInterfaceGroupId: "example",
    			OutpostLagId:                        "example",
    			PeerAddress:                         "example",
    			Vlan:                                1,
    			PeerBgpAsn:                          1,
    			PeerBgpAsnExtended:                  1,
    			Tags:                                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayVirtualInterface } from "./local-sdk/aws/sdk/typescript/aws/ec2/local_gateway_virtual_interface.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own local gateway virtual interface" });

      resource(LocalGatewayVirtualInterface, "ci-runner", {
        localAddress: "example",
        localGatewayVirtualInterfaceGroupId: "example",
        outpostLagId: "example",
        peerAddress: "example",
        vlan: 1,
        peerBgpAsn: 1,
        peerBgpAsnExtended: 1,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayVirtualInterface, LocalGatewayVirtualInterfaceConfig

    def describe():
        ubx.intent("payments own local gateway virtual interface")

        ubx.resource(LocalGatewayVirtualInterface, "ci-runner", LocalGatewayVirtualInterfaceConfig(
            local_address="example",
            local_gateway_virtual_interface_group_id="example",
            outpost_lag_id="example",
            peer_address="example",
            vlan=1,
            peer_bgp_asn=1,
            peer_bgp_asn_extended=1,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_local_gateway_virtual_interface, with local
    address example, local gateway virtual interface group id
    example, outpost lag id example, peer address example, vlan 1,
    peer bgp asn 1, peer bgp asn extended 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="local_address" type="string" required>
  The local address. Required.
</ResponseField>

<ResponseField name="local_gateway_virtual_interface_group_id" type="string" required>
  The ID of the virtual interface group Required.
</ResponseField>

<ResponseField name="outpost_lag_id" type="string" required>
  The Outpost LAG ID. Required.
</ResponseField>

<ResponseField name="peer_address" type="string" required>
  The peer address. Required.
</ResponseField>

<ResponseField name="peer_bgp_asn" type="number">
  The peer BGP ASN. Optional.
</ResponseField>

<ResponseField name="peer_bgp_asn_extended" type="number">
  The extended 32-bit ASN of the BGP peer for use with larger ASN values. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The user-defined key for a tag attached to the EC2 Local Gateway Virtual Interface, used to organize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the Local Gateway Virtual Interface, used to categorize or identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vlan" type="number" required>
  The ID of the VLAN. Required.
</ResponseField>

## Output properties

<ResponseField name="configuration_state" type="string">
  The current state of the local gateway virtual interface Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_bgp_asn" type="number">
  The Autonomous System Number(ASN) for the local Border Gateway Protocol (BGP) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_id" type="string">
  The ID of the local gateway Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_virtual_interface_id" type="string">
  The ID of the virtual interface Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The ID of the Amazon Web Services account that owns the local gateway virtual interface group Computed by AWS after `ship`.
</ResponseField>
