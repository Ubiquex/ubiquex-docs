---
title: "aws_ec2_snapshot_block_public_access"
description: "`aws_ec2_snapshot_block_public_access` sets a real, account-wide block on public access to EBS snapshots..."
---

`aws_ec2_snapshot_block_public_access` sets a real, account-wide block on public access to EBS snapshots, preventing any `aws_ec2_volume` snapshot from being made public even if an individual snapshot's own permissions would otherwise allow it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own snapshot block public access"})

    		ubx.Resource(ec2.SnapshotBlockPublicAccess, "example", ec2.SnapshotBlockPublicAccessConfig{
    			State: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SnapshotBlockPublicAccess } from "./local-sdk/aws/sdk/typescript/aws/ec2/snapshot_block_public_access.ts";

    export default stack("example", () => {
      intent({ summary: "example own snapshot block public access" });

      resource(SnapshotBlockPublicAccess, "example", {
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import SnapshotBlockPublicAccess, SnapshotBlockPublicAccessConfig

    def describe():
        ubx.intent("example own snapshot block public access")

        ubx.resource(SnapshotBlockPublicAccess, "example", SnapshotBlockPublicAccessConfig(
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_snapshot_block_public_access, with state example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="state" type="string" required>
  The state of EBS Snapshot Block Public Access. Required.
</ResponseField>

## Output properties

<ResponseField name="account_id" type="string">
  The identifier for the specified AWS account. Computed by AWS after `ship`.
</ResponseField>
