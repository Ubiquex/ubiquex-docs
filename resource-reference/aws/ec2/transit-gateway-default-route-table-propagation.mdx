---
title: "aws_ec2_transit_gateway_default_route_table_propagation"
description: "Real, generated bindings for `aws_ec2_transit_gateway_default_route_table_propagation`."
---

`aws_ec2_transit_gateway_default_route_table_propagation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway default route table propagation"})

    		ubx.Resource(ec2.TransitGatewayDefaultRouteTablePropagation, "ci-runner", ec2.TransitGatewayDefaultRouteTablePropagationConfig{
    			TransitGatewayId:           "example",
    			TransitGatewayRouteTableId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayDefaultRouteTablePropagation } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_default_route_table_propagation";

    export default stack("payments", () => {
      intent({
        summary: "payments own transit gateway default route table propagation",
      });

      resource(TransitGatewayDefaultRouteTablePropagation, "ci-runner", {
        transitGatewayId: "example",
        transitGatewayRouteTableId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayDefaultRouteTablePropagation, TransitGatewayDefaultRouteTablePropagationConfig

    def describe():
        ubx.intent("payments own transit gateway default route table propagation")

        ubx.resource(TransitGatewayDefaultRouteTablePropagation, "ci-runner", TransitGatewayDefaultRouteTablePropagationConfig(
            transit_gateway_id="example",
            transit_gateway_route_table_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_default_route_table_propagation,
    with transit gateway id example, transit gateway route table id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="transit_gateway_route_table_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="original_default_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
