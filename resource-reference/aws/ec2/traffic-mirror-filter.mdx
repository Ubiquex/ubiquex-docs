---
title: "aws_ec2_traffic_mirror_filter"
description: "`aws_ec2_traffic_mirror_filter` defines which real traffic, by protocol, port, and direction..."
---

`aws_ec2_traffic_mirror_filter` defines which real traffic, by protocol, port, and direction, actually gets copied out of a monitored network interface, the rules an `aws_ec2_traffic_mirror_session` applies rather than mirroring every packet indiscriminately.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic mirror filter"})

    		ubx.Resource(ec2.TrafficMirrorFilter, "ci-runner", ec2.TrafficMirrorFilterConfig{
    			Description:     "Managed by ubx.",
    			NetworkServices: []string{"example"},
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficMirrorFilter } from "./local-sdk/aws/sdk/typescript/aws/ec2/traffic_mirror_filter.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic mirror filter" });

      resource(TrafficMirrorFilter, "ci-runner", {
        description: "Managed by ubx.",
        networkServices: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TrafficMirrorFilter, TrafficMirrorFilterConfig

    def describe():
        ubx.intent("payments own traffic mirror filter")

        ubx.resource(TrafficMirrorFilter, "ci-runner", TrafficMirrorFilterConfig(
            description="Managed by ubx.",
            network_services=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_traffic_mirror_filter, with description Managed by
    ubx., network services a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of a traffic mirror filter. Optional.
</ResponseField>

<ResponseField name="network_services" type="list(string)">
  The network service that is associated with the traffic mirror filter. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for a traffic mirror filter. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag entry attached to the EC2 Traffic Mirror Filter, used to store arbitrary metadata for identification or categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The ID of a traffic mirror filter. Computed by AWS after `ship`.
</ResponseField>
