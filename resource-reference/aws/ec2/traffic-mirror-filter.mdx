---
title: "aws_ec2_traffic_mirror_filter"
description: "Real, generated bindings for `aws_ec2_traffic_mirror_filter`."
---

`aws_ec2_traffic_mirror_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.TrafficMirrorFilterConfig{
        Description: "example",
        NetworkServices: []string{"example"},
    }
    ubx.Resource(ec2.TrafficMirrorFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TrafficMirrorFilter } from
      "jsr:@ubx/sdk-aws/aws/ec2/traffic_mirror_filter";

    resource(TrafficMirrorFilter, "example", {
      description: "example",
      networkServices: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TrafficMirrorFilter,
        TrafficMirrorFilterConfig,
    )

    cfg = TrafficMirrorFilterConfig(
        description="example",
        network_services=["example"],
    )
    ubx.resource(TrafficMirrorFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_traffic_mirror_filter with description,
network_services.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_services" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
