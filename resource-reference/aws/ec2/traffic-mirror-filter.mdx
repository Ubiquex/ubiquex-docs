---
title: "aws_ec2_traffic_mirror_filter"
description: "Real, generated bindings for `aws_ec2_traffic_mirror_filter`."
---

`aws_ec2_traffic_mirror_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic mirror filter"})

    		ubx.Resource(ec2.TrafficMirrorFilter, "ci-runner", ec2.TrafficMirrorFilterConfig{
    			Description:     "Managed by ubx.",
    			NetworkServices: []string{"example"},
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficMirrorFilter } from "jsr:@ubx/sdk-aws/aws/ec2/traffic_mirror_filter";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic mirror filter" });

      resource(TrafficMirrorFilter, "ci-runner", {
        description: "Managed by ubx.",
        networkServices: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TrafficMirrorFilter, TrafficMirrorFilterConfig

    def describe():
        ubx.intent("payments own traffic mirror filter")

        ubx.resource(TrafficMirrorFilter, "ci-runner", TrafficMirrorFilterConfig(
            description="Managed by ubx.",
            network_services=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_traffic_mirror_filter, with description Managed by
    ubx., network services a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_services" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
