---
title: "aws_ec2_transit_gateway_peering_attachment"
description: "`aws_ec2_transit_gateway_peering_attachment` connects two `aws_ec2_transit_gateway`s together, in the same or different Regions..."
---

`aws_ec2_transit_gateway_peering_attachment` connects two `aws_ec2_transit_gateway`s together, in the same or different Regions, the real mechanism for extending transit gateway routing across Regions without every VPC needing its own cross-Region connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own transit gateway peering attachment"})

    		ubx.Resource(ec2.TransitGatewayPeeringAttachment, "example", ec2.TransitGatewayPeeringAttachmentConfig{
    			PeerAccountId:        "example",
    			PeerRegion:           "example",
    			PeerTransitGatewayId: "example",
    			TransitGatewayId:     "example",
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayPeeringAttachment } from "./local-sdk/aws/sdk/typescript/aws/ec2/transit_gateway_peering_attachment.ts";

    export default stack("example", () => {
      intent({ summary: "example own transit gateway peering attachment" });

      resource(TransitGatewayPeeringAttachment, "example", {
        peerAccountId: "example",
        peerRegion: "example",
        peerTransitGatewayId: "example",
        transitGatewayId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayPeeringAttachment, TransitGatewayPeeringAttachmentConfig

    def describe():
        ubx.intent("example own transit gateway peering attachment")

        ubx.resource(TransitGatewayPeeringAttachment, "example", TransitGatewayPeeringAttachmentConfig(
            peer_account_id="example",
            peer_region="example",
            peer_transit_gateway_id="example",
            transit_gateway_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_peering_attachment, with peer
    account id example, peer region example, peer transit gateway id
    example, transit gateway id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="peer_account_id" type="string" required>
  The ID of the peer account Required.
</ResponseField>

<ResponseField name="peer_region" type="string" required>
  The peer region for this ec2 transit gateway peering attachment. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="peer_transit_gateway_id" type="string" required>
  The ID of the peer transit gateway. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for the transit gateway peering attachment. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to attach to the transit gateway peering attachment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag assigned to the transit gateway peering attachment, used to identify or organize the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  The ID of the transit gateway. Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time the transit gateway peering attachment was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the transit gateway peering attachment. Note that the initiating state has been deprecated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="object">
  The current status of the transit gateway peering attachment, reported as an object containing a code (e.g., available or deleted) and a message describing the state in more detail. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      The status code. Optional.
    </ResponseField>
    <ResponseField name="message" type="string">
      The status message, if applicable. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  The ID of the transit gateway peering attachment. Computed by AWS after `ship`.
</ResponseField>
