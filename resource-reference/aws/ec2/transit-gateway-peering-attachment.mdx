---
title: "aws_ec2_transit_gateway_peering_attachment"
description: "Real, generated bindings for `aws_ec2_transit_gateway_peering_attachment`."
---

`aws_ec2_transit_gateway_peering_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway peering attachment"})

    		ubx.Resource(ec2.TransitGatewayPeeringAttachment, "ci-runner", ec2.TransitGatewayPeeringAttachmentConfig{
    			PeerRegion:           "example",
    			PeerTransitGatewayId: "example",
    			TransitGatewayId:     "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayPeeringAttachment } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_peering_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway peering attachment" });

      resource(TransitGatewayPeeringAttachment, "ci-runner", {
        peerRegion: "example",
        peerTransitGatewayId: "example",
        transitGatewayId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayPeeringAttachment, TransitGatewayPeeringAttachmentConfig

    def describe():
        ubx.intent("payments own transit gateway peering attachment")

        ubx.resource(TransitGatewayPeeringAttachment, "ci-runner", TransitGatewayPeeringAttachmentConfig(
            peer_region="example",
            peer_transit_gateway_id="example",
            transit_gateway_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_peering_attachment, with peer
    region example, peer transit gateway id example, transit gateway
    id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="peer_transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
