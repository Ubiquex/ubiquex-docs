---
title: "aws_ec2_tag"
description: "Real, generated bindings for `aws_ec2_tag`."
---

`aws_ec2_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.TagConfig{
        Key: "example",
        ResourceId: "example",
        Value: "example",
    }
    ubx.Resource(ec2.Tag, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Tag } from "jsr:@ubx/sdk-aws/aws/ec2/tag";

    resource(Tag, "example", {
      key: "example",
      resourceId: "example",
      value: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import Tag, TagConfig

    cfg = TagConfig(
        key="example",
        resource_id="example",
        value="example",
    )
    ubx.resource(Tag, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_tag with key, resource_id, value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
