---
title: "aws_ec2_ipampool_cidr"
description: "`aws_ec2_ipampool_cidr` provisions a real CIDR block into an `aws_ec2_ipampool`..."
---

`aws_ec2_ipampool_cidr` provisions a real CIDR block into an `aws_ec2_ipampool`, the actual address space that instances and subnets can then allocate from within that pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ipampool cidr"})

    		ubx.Resource(ec2.IpampoolCidr, "example", ec2.IpampoolCidrConfig{
    			IpamPoolId:    "example",
    			Cidr:          "example",
    			NetmaskLength: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpampoolCidr } from "@ubx/sdk-aws/aws/ec2/ipampool_cidr";

    export default stack("example", () => {
      intent({ summary: "example own ipampool cidr" });

      resource(IpampoolCidr, "example", {
        ipamPoolId: "example",
        cidr: "example",
        netmaskLength: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import IpampoolCidr, IpampoolCidrConfig

    def describe():
        ubx.intent("example own ipampool cidr")

        ubx.resource(IpampoolCidr, "example", IpampoolCidrConfig(
            ipam_pool_id="example",
            cidr="example",
            netmask_length=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_ipampool_cidr, with ipam pool id example, cidr
    example, netmask length 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr" type="string">
  Represents a single IPv4 or IPv6 CIDR Optional.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string" required>
  Id of the IPAM Pool. Required.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  The desired netmask length of the provision. If set, IPAM will choose a block of free space with this size and return the CIDR representing it. Optional.
</ResponseField>

## Output properties

<ResponseField name="ipam_pool_cidr_id" type="string">
  Id of the IPAM Pool Cidr. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Provisioned state of the cidr. Computed by AWS after `ship`.
</ResponseField>
