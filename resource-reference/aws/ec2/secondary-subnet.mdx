---
title: "aws_ec2_secondary_subnet"
description: "Real, generated bindings for `aws_ec2_secondary_subnet`."
---

`aws_ec2_secondary_subnet` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secondary subnet"})

    		ubx.Resource(ec2.SecondarySubnet, "ci-runner", ec2.SecondarySubnetConfig{
    			Ipv4CidrBlock:      "example",
    			SecondaryNetworkId: "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecondarySubnet } from "jsr:@ubx/sdk-aws/aws/ec2/secondary_subnet";

    export default stack("payments", () => {
      intent({ summary: "payments own secondary subnet" });

      resource(SecondarySubnet, "ci-runner", {
        ipv4CidrBlock: "example",
        secondaryNetworkId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import SecondarySubnet, SecondarySubnetConfig

    def describe():
        ubx.intent("payments own secondary subnet")

        ubx.resource(SecondarySubnet, "ci-runner", SecondarySubnetConfig(
            ipv4_cidr_block="example",
            secondary_network_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_secondary_subnet, with ipv4 cidr block example,
    secondary network id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zone" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv4_cidr_block" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secondary_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipv4_cidr_block_associations" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="association_id" type="string">
    </ResponseField>
    <ResponseField name="cidr_block" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secondary_network_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="secondary_subnet_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
