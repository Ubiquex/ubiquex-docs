---
title: "aws_ec2_network_insights_path"
description: "Real, generated bindings for `aws_ec2_network_insights_path`."
---

`aws_ec2_network_insights_path` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network insights path"})

    		ubx.Resource(ec2.NetworkInsightsPath, "ci-runner", ec2.NetworkInsightsPathConfig{
    			Protocol:        "example",
    			Source:          "example",
    			Destination:     "example",
    			DestinationIp:   "example",
    			DestinationPort: 1,
    			SourceIp:        "example",
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkInsightsPath } from "jsr:@ubx/sdk-aws/aws/ec2/network_insights_path";

    export default stack("payments", () => {
      intent({ summary: "payments own network insights path" });

      resource(NetworkInsightsPath, "ci-runner", {
        protocol: "example",
        source: "example",
        destination: "example",
        destinationIp: "example",
        destinationPort: 1,
        sourceIp: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import NetworkInsightsPath, NetworkInsightsPathConfig

    def describe():
        ubx.intent("payments own network insights path")

        ubx.resource(NetworkInsightsPath, "ci-runner", NetworkInsightsPathConfig(
            protocol="example",
            source="example",
            destination="example",
            destination_ip="example",
            destination_port=1,
            source_ip="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_network_insights_path, with protocol example,
    source example, destination example, destination ip example,
    destination port 1, source ip example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination_ip" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_ip" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="destination_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
