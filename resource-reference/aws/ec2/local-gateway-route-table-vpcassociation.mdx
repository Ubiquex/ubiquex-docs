---
title: "aws_ec2_local_gateway_route_table_vpcassociation"
description: "`aws_ec2_local_gateway_route_table_vpcassociation` is the real, actual link between an `aws_ec2_local_gateway_route_table` and a specific VPC..."
---

`aws_ec2_local_gateway_route_table_vpcassociation` is the real, actual link between an `aws_ec2_local_gateway_route_table` and a specific VPC, determining which VPC's traffic that local gateway route table actually governs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own local gateway route table vpcassociation"})

    		ubx.Resource(ec2.LocalGatewayRouteTableVpcassociation, "ci-runner", ec2.LocalGatewayRouteTableVpcassociationConfig{
    			LocalGatewayRouteTableId: "example",
    			VpcId:                    "example",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayRouteTableVpcassociation } from "./local-sdk/aws/sdk/typescript/aws/ec2/local_gateway_route_table_vpcassociation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own local gateway route table vpcassociation" });

      resource(LocalGatewayRouteTableVpcassociation, "ci-runner", {
        localGatewayRouteTableId: "example",
        vpcId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayRouteTableVpcassociation, LocalGatewayRouteTableVpcassociationConfig

    def describe():
        ubx.intent("payments own local gateway route table vpcassociation")

        ubx.resource(LocalGatewayRouteTableVpcassociation, "ci-runner", LocalGatewayRouteTableVpcassociationConfig(
            local_gateway_route_table_id="example",
            vpc_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_local_gateway_route_table_vpcassociation, with
    local gateway route table id example, vpc id example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="local_gateway_route_table_id" type="string" required>
  The ID of the local gateway route table. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags are user-defined key-value pairs that label and identify the VPC association with the local gateway route table, enabling cost allocation and resource management. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Local Gateway Route Table VPC Association, serving as the name portion of a user-defined metadata label for the association. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the association between a local gateway route table and a VPC. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC. Required.
</ResponseField>

## Output properties

<ResponseField name="local_gateway_id" type="string">
  The ID of the local gateway. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_vpc_association_id" type="string">
  The ID of the association. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the association. Computed by AWS after `ship`.
</ResponseField>
