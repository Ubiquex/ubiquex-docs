---
title: "aws_ec2_client_vpn_network_association"
description: "Real, generated bindings for `aws_ec2_client_vpn_network_association`."
---

`aws_ec2_client_vpn_network_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own client vpn network association"})

    		ubx.Resource(ec2.ClientVpnNetworkAssociation, "ci-runner", ec2.ClientVpnNetworkAssociationConfig{
    			ClientVpnEndpointId: "example",
    			SubnetId:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClientVpnNetworkAssociation } from "jsr:@ubx/sdk-aws/aws/ec2/client_vpn_network_association";

    export default stack("payments", () => {
      intent({ summary: "payments own client vpn network association" });

      resource(ClientVpnNetworkAssociation, "ci-runner", {
        clientVpnEndpointId: "example",
        subnetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import ClientVpnNetworkAssociation, ClientVpnNetworkAssociationConfig

    def describe():
        ubx.intent("payments own client vpn network association")

        ubx.resource(ClientVpnNetworkAssociation, "ci-runner", ClientVpnNetworkAssociationConfig(
            client_vpn_endpoint_id="example",
            subnet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_client_vpn_network_association, with client vpn
    endpoint id example, subnet id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="client_vpn_endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
