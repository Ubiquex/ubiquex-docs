---
title: "aws_ec2_transit_gateway_policy_table_entry"
description: "`aws_ec2_transit_gateway_policy_table_entry` is one real, specific matching rule inside an `aws_ec2_transit_gateway_policy_table`, the actual..."
---

`aws_ec2_transit_gateway_policy_table_entry` is one real, specific matching rule inside an `aws_ec2_transit_gateway_policy_table`, the actual, individually addressable rule within a policy table's broader rule set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own transit gateway policy table entry"})

    		ubx.Resource(ec2.TransitGatewayPolicyTableEntry, "example", ec2.TransitGatewayPolicyTableEntryConfig{
    			PolicyRule:                  map[string]any{"destination_cidr_block": "example"},
    			PolicyRuleNumber:            "example",
    			TargetRouteTableId:          "example",
    			TransitGatewayPolicyTableId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayPolicyTableEntry } from "@ubx/sdk-aws/aws/ec2/transit_gateway_policy_table_entry";

    export default stack("example", () => {
      intent({ summary: "example own transit gateway policy table entry" });

      resource(TransitGatewayPolicyTableEntry, "example", {
        policyRule: { destinationCidrBlock: "example" },
        policyRuleNumber: "example",
        targetRouteTableId: "example",
        transitGatewayPolicyTableId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2.transit_gateway_policy_table_entry import TransitGatewayPolicyTableEntry, TransitGatewayPolicyTableEntryConfig

    def describe():
        ubx.intent("example own transit gateway policy table entry")

        ubx.resource(TransitGatewayPolicyTableEntry, "example", TransitGatewayPolicyTableEntryConfig(
            policy_rule={"destination_cidr_block": "example"},
            policy_rule_number="example",
            target_route_table_id="example",
            transit_gateway_policy_table_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_policy_table_entry, with policy
    rule a set value, policy rule number example, target route table
    id example, transit gateway policy table id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy_rule" type="object" required>
  The policy rule that defines the source and destination CIDR blocks, protocol, port ranges, and action (allow or drop) for the transit gateway policy table entry. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="destination_cidr_block" type="string">
      The destination CIDR block for the transit gateway policy rule. Optional.
    </ResponseField>
    <ResponseField name="destination_port_range" type="string">
      The destination port range for the transit gateway policy rule. Optional.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The protocol for the transit gateway policy rule. Optional.
    </ResponseField>
    <ResponseField name="source_cidr_block" type="string">
      The source CIDR block for the transit gateway policy rule. Optional.
    </ResponseField>
    <ResponseField name="source_port_range" type="string">
      The source port range for the transit gateway policy rule. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_rule_number" type="string" required>
  The rule number for the policy table entry. Required.
</ResponseField>

<ResponseField name="target_route_table_id" type="string" required>
  The ID of the target route table. Required.
</ResponseField>

<ResponseField name="transit_gateway_policy_table_id" type="string" required>
  The ID of the transit gateway policy table. Required.
</ResponseField>

## Output properties

<ResponseField name="state" type="string">
  The state of the policy table entry. Computed by AWS after `ship`.
</ResponseField>
