---
title: "aws_ec2_vpnconcentrator"
description: "`aws_ec2_vpnconcentrator` aggregates up to 100 real, low-bandwidth (under 100 Mbps each) Site-to-Site VPN connections through a single..."
---

`aws_ec2_vpnconcentrator` aggregates up to 100 real, low-bandwidth (under 100 Mbps each) Site-to-Site VPN connections through a single, BGP-only resource attached to an `aws_ec2_transit_gateway`, built for connecting a large number of small remote sites, retail locations, branch offices, rather than provisioning a separate `aws_ec2_vpnconnection` for each one. Launched November 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vpnconcentrator"})

    		ubx.Resource(ec2.Vpnconcentrator, "example", ec2.VpnconcentratorConfig{
    			TransitGatewayId: "example",
    			Type:             "example",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Vpnconcentrator } from "@ubx/sdk-aws/aws/ec2/vpnconcentrator";

    export default stack("example", () => {
      intent({ summary: "example own vpnconcentrator" });

      resource(Vpnconcentrator, "example", {
        transitGatewayId: "example",
        type: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import Vpnconcentrator, VpnconcentratorConfig
    from ubx.aws.ec2.vpnconcentrator import Vpnconcentrator_Tags

    def describe():
        ubx.intent("example own vpnconcentrator")

        ubx.resource(Vpnconcentrator, "example", VpnconcentratorConfig(
            transit_gateway_id="example",
            type="example",
            tags=[Vpnconcentrator_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_vpnconcentrator, with transit gateway id example,
    type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="list(object)">
  Any tags assigned to the VPN concentrator. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  The ID of the transit gateway associated with the VPN concentrator. Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of VPN concentrator. Required.
</ResponseField>

## Output properties

<ResponseField name="transit_gateway_attachment_id" type="string">
  The unique identifier of the transit gateway attachment that is automatically provisioned when the VPN connection is associated with a transit gateway. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="vpn_concentrator_id" type="string">
  The unique identifier automatically assigned by AWS to the VPN concentrator when it is created, used for referencing the resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
