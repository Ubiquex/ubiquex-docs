---
title: "aws_ec2_managed_prefix_list"
description: "Real, generated bindings for `aws_ec2_managed_prefix_list`."
---

`aws_ec2_managed_prefix_list` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.ManagedPrefixListConfig{
        AddressFamily: "example",
        MaxEntries: 1,
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ec2.ManagedPrefixList, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedPrefixList } from
      "jsr:@ubx/sdk-aws/aws/ec2/managed_prefix_list";

    resource(ManagedPrefixList, "example", {
      addressFamily: "example",
      maxEntries: 1,
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        ManagedPrefixList,
        ManagedPrefixListConfig,
    )

    cfg = ManagedPrefixListConfig(
        address_family="example",
        max_entries=1,
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(ManagedPrefixList, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_managed_prefix_list with address_family,
max_entries, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="address_family" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_entries" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
