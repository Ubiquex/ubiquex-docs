---
title: "aws_ec2_eipassociation"
description: "`aws_ec2_eipassociation` is the real, actual grant that attaches an existing Elastic IP address to a specific EC2 instance or network interface."
---

An allocated Elastic IP with no association isn't routing any traffic to anything yet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eipassociation"})

    		ubx.Resource(ec2.Eipassociation, "example", ec2.EipassociationConfig{
    			AllocationId:       "example",
    			Eip:                "example",
    			InstanceId:         "example",
    			NetworkInterfaceId: "example",
    			PrivateIpAddress:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Eipassociation } from "@ubx/sdk-aws/aws/ec2/eipassociation";

    export default stack("example", () => {
      intent({ summary: "example own eipassociation" });

      resource(Eipassociation, "example", {
        allocationId: "example",
        eip: "example",
        instanceId: "example",
        networkInterfaceId: "example",
        privateIpAddress: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2.eipassociation import Eipassociation, EipassociationConfig

    def describe():
        ubx.intent("example own eipassociation")

        ubx.resource(Eipassociation, "example", EipassociationConfig(
            allocation_id="example",
            eip="example",
            instance_id="example",
            network_interface_id="example",
            private_ip_address="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_eipassociation, with allocation id example, eip
    example, instance id example, network interface id example,
    private ip address example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allocation_id" type="string">
  The allocation ID. This is required. Optional.
</ResponseField>

<ResponseField name="eip" type="any">
  The Elastic IP address (IPv4) to associate with the specified instance or network interface; this can be an IP string or a reference to an EIP resource. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="instance_id" type="string">
  The ID of the instance. The instance must have exactly one attached network interface. You can specify either the instance ID or the network interface ID, but not both. Optional.
</ResponseField>

<ResponseField name="network_interface_id" type="string">
  The ID of the network interface. If the instance has more than one network interface, you must specify a network interface ID. You can specify either the instance ID or the network interface ID, but not both. Optional.
</ResponseField>

<ResponseField name="private_ip_address" type="string">
  The primary or secondary private IP address to associate with the Elastic IP address. If no private IP address is specified, the Elastic IP address is associated with the primary private IP address. Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier of the Elastic IP association, which is the association ID (e.g., eipassoc-...) returned by AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
