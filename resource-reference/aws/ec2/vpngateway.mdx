---
title: "aws_ec2_vpngateway"
description: "`aws_ec2_vpngateway` is the real, AWS-side endpoint, a virtual private gateway..."
---

`aws_ec2_vpngateway` is the real, AWS-side endpoint, a virtual private gateway, that a `aws_ec2_vpnconnection`'s tunnels terminate against inside a VPC, the counterpart to an `aws_customer_gateway` on the caller's own side of a Site-to-Site VPN connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vpngateway"})

    		ubx.Resource(ec2.Vpngateway, "example", ec2.VpngatewayConfig{
    			Type:          "example",
    			AmazonSideAsn: 1,
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Vpngateway } from "./local-sdk/aws/sdk/typescript/aws/ec2/vpngateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own vpngateway" });

      resource(Vpngateway, "example", {
        type: "example",
        amazonSideAsn: 1,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import Vpngateway, VpngatewayConfig

    def describe():
        ubx.intent("example own vpngateway")

        ubx.resource(Vpngateway, "example", VpngatewayConfig(
            type="example",
            amazon_side_asn=1,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_vpngateway, with type example, amazon side asn 1,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="amazon_side_asn" type="number">
  The private Autonomous System Number (ASN) for the Amazon side of a BGP session. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Any tags assigned to the virtual private gateway. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the VPN Gateway, used to label and categorize the resource for identification and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag applied to the AWS VPN gateway, allowing you to store arbitrary metadata such as a name, environment, or cost-center alongside the tag key. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of VPN connection the virtual private gateway supports. Required.
</ResponseField>

## Output properties

<ResponseField name="vpngateway_id" type="string">
  The unique identifier assigned by AWS to the VPN gateway upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
