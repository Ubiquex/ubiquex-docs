---
title: "aws_ec2_transit_gateway_vpc_attachment_accepter"
description: "Real, generated bindings for `aws_ec2_transit_gateway_vpc_attachment_accepter`."
---

`aws_ec2_transit_gateway_vpc_attachment_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.TransitGatewayVpcAttachmentAccepterConfig{
        TransitGatewayAttachmentId: "example",
        Tags: map[string]string{"key": "value"},
        TransitGatewayDefaultRouteTableAssociation: true,
    }
    ubx.Resource(
        ec2.TransitGatewayVpcAttachmentAccepter,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayVpcAttachmentAccepter } from
      "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_vpc_attachment_accepter";

    resource(TransitGatewayVpcAttachmentAccepter, "example", {
      transitGatewayAttachmentId: "example",
      tags: { key: "value" },
      transitGatewayDefaultRouteTableAssociation: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TransitGatewayVpcAttachmentAccepter,
        TransitGatewayVpcAttachmentAccepterConfig,
    )

    cfg = TransitGatewayVpcAttachmentAccepterConfig(
        transit_gateway_attachment_id="example",
        tags={"key": "value"},
        transit_gateway_default_route_table_association=True,
    )
    ubx.resource(TransitGatewayVpcAttachmentAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_transit_gateway_vpc_attachment_accepter with
transit_gateway_attachment_id, tags,
transit_gateway_default_route_table_association.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="appliance_mode_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_referencing_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
