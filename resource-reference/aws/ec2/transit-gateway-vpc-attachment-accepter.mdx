---
title: "aws_ec2_transit_gateway_vpc_attachment_accepter"
description: "Real, generated bindings for `aws_ec2_transit_gateway_vpc_attachment_accepter`."
---

`aws_ec2_transit_gateway_vpc_attachment_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway vpc attachment accepter"})

    		ubx.Resource(ec2.TransitGatewayVpcAttachmentAccepter, "ci-runner", ec2.TransitGatewayVpcAttachmentAccepterConfig{
    			TransitGatewayAttachmentId: "example",
    			Tags:                       map[string]string{"managed-by": "ubx"},
    			TransitGatewayDefaultRouteTableAssociation: true,
    			TransitGatewayDefaultRouteTablePropagation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayVpcAttachmentAccepter } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_vpc_attachment_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway vpc attachment accepter" });

      resource(TransitGatewayVpcAttachmentAccepter, "ci-runner", {
        transitGatewayAttachmentId: "example",
        tags: { "managed-by": "ubx" },
        transitGatewayDefaultRouteTableAssociation: true,
        transitGatewayDefaultRouteTablePropagation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayVpcAttachmentAccepter, TransitGatewayVpcAttachmentAccepterConfig

    def describe():
        ubx.intent("payments own transit gateway vpc attachment accepter")

        ubx.resource(TransitGatewayVpcAttachmentAccepter, "ci-runner", TransitGatewayVpcAttachmentAccepterConfig(
            transit_gateway_attachment_id="example",
            tags={"managed-by": "ubx"},
            transit_gateway_default_route_table_association=True,
            transit_gateway_default_route_table_propagation=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_vpc_attachment_accepter, with
    transit gateway attachment id example, tags a set value, transit
    gateway default route table association true, transit gateway
    default route table propagation true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="appliance_mode_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_referencing_support" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
