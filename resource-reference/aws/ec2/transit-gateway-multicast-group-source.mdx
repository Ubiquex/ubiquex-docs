---
title: "aws_ec2_transit_gateway_multicast_group_source"
description: "Real, generated bindings for `aws_ec2_transit_gateway_multicast_group_source`."
---

`aws_ec2_transit_gateway_multicast_group_source` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway multicast group source"})

    		ubx.Resource(ec2.TransitGatewayMulticastGroupSource, "ci-runner", ec2.TransitGatewayMulticastGroupSourceConfig{
    			GroupIpAddress:                  "example",
    			NetworkInterfaceId:              "example",
    			TransitGatewayMulticastDomainId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayMulticastGroupSource } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_multicast_group_source";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway multicast group source" });

      resource(TransitGatewayMulticastGroupSource, "ci-runner", {
        groupIpAddress: "example",
        networkInterfaceId: "example",
        transitGatewayMulticastDomainId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayMulticastGroupSource, TransitGatewayMulticastGroupSourceConfig

    def describe():
        ubx.intent("payments own transit gateway multicast group source")

        ubx.resource(TransitGatewayMulticastGroupSource, "ci-runner", TransitGatewayMulticastGroupSourceConfig(
            group_ip_address="example",
            network_interface_id="example",
            transit_gateway_multicast_domain_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_multicast_group_source, with group
    ip address example, network interface id example, transit gateway
    multicast domain id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_ip_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_multicast_domain_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
