---
title: "aws_ec2_vpcendpoint_service_permissions"
description: "`aws_ec2_vpcendpoint_service_permissions` controls which real AWS principals are actually allowed to create an `aws_ec2_vpcendpoint` against..."
---

`aws_ec2_vpcendpoint_service_permissions` controls which real AWS principals are actually allowed to create an `aws_ec2_vpcendpoint` against an `aws_ec2_vpcendpoint_service`, an allowlist a service owner manages rather than leaving their PrivateLink service open to any account that discovers it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vpcendpoint service permissions"})

    		ubx.Resource(ec2.VpcendpointServicePermissions, "example", ec2.VpcendpointServicePermissionsConfig{
    			ServiceId:         "example",
    			AllowedPrincipals: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcendpointServicePermissions } from "./local-sdk/aws/sdk/typescript/aws/ec2/vpcendpoint_service_permissions.ts";

    export default stack("example", () => {
      intent({ summary: "example own vpcendpoint service permissions" });

      resource(VpcendpointServicePermissions, "example", {
        serviceId: "example",
        allowedPrincipals: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VpcendpointServicePermissions, VpcendpointServicePermissionsConfig

    def describe():
        ubx.intent("example own vpcendpoint service permissions")

        ubx.resource(VpcendpointServicePermissions, "example", VpcendpointServicePermissionsConfig(
            service_id="example",
            allowed_principals=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_vpcendpoint_service_permissions, with service id
    example, allowed principals a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="allowed_principals" type="list(string)">
  Specifies the ARNs of principals (IAM users, roles, or AWS accounts) that are allowed to create interface VPC endpoints to the VPC endpoint service. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_id" type="string" required>
  The ID of the VPC endpoint service to which the permissions are granted. Required. **(AI-inferred)**
</ResponseField>
