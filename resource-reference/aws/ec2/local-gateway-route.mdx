---
title: "aws_ec2_local_gateway_route"
description: "`aws_ec2_local_gateway_route` defines a real route in an `aws_ec2_local_gateway_route_table`..."
---

`aws_ec2_local_gateway_route` defines a real route in an `aws_ec2_local_gateway_route_table`, directing traffic for a specific CIDR to an Outposts local gateway rather than back into the parent AWS Region.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own local gateway route"})

    		ubx.Resource(ec2.LocalGatewayRoute, "example", ec2.LocalGatewayRouteConfig{
    			DestinationCidrBlock:                "example",
    			LocalGatewayRouteTableId:            "example",
    			LocalGatewayVirtualInterfaceGroupId: "example",
    			NetworkInterfaceId:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayRoute } from "@ubx/sdk-aws/aws/ec2/local_gateway_route";

    export default stack("example", () => {
      intent({ summary: "example own local gateway route" });

      resource(LocalGatewayRoute, "example", {
        destinationCidrBlock: "example",
        localGatewayRouteTableId: "example",
        localGatewayVirtualInterfaceGroupId: "example",
        networkInterfaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayRoute, LocalGatewayRouteConfig

    def describe():
        ubx.intent("example own local gateway route")

        ubx.resource(LocalGatewayRoute, "example", LocalGatewayRouteConfig(
            destination_cidr_block="example",
            local_gateway_route_table_id="example",
            local_gateway_virtual_interface_group_id="example",
            network_interface_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_local_gateway_route, with destination cidr block
    example, local gateway route table id example, local gateway
    virtual interface group id example, network interface id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destination_cidr_block" type="string">
  The CIDR block used for destination matches. Optional.
</ResponseField>

<ResponseField name="local_gateway_route_table_id" type="string">
  The ID of the local gateway route table. Optional.
</ResponseField>

<ResponseField name="local_gateway_virtual_interface_group_id" type="string">
  The ID of the virtual interface group. Optional.
</ResponseField>

<ResponseField name="network_interface_id" type="string">
  The ID of the network interface. Optional.
</ResponseField>

## Output properties

<ResponseField name="state" type="string">
  The state of the route. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  The route type. Computed by AWS after `ship`.
</ResponseField>
