---
title: "aws_volume_attachment"
description: "`aws_volume_attachment` attaches a real `aws_ec2_volume` to a running EC2 instance at a chosen device name..."
---

`aws_volume_attachment` attaches a real `aws_ec2_volume` to a running EC2 instance at a chosen device name, the resource that actually represents the attach action rather than the volume itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own volume attachment"})

    		ubx.Resource(ec2.VolumeAttachment, "example", ec2.VolumeAttachmentConfig{
    			InstanceId:   "example",
    			VolumeId:     "example",
    			Device:       "example",
    			EbsCardIndex: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VolumeAttachment } from "@ubx/sdk-aws/aws/ec2/volume_attachment";

    export default stack("example", () => {
      intent({ summary: "example own volume attachment" });

      resource(VolumeAttachment, "example", {
        instanceId: "example",
        volumeId: "example",
        device: "example",
        ebsCardIndex: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VolumeAttachment, VolumeAttachmentConfig

    def describe():
        ubx.intent("example own volume attachment")

        ubx.resource(VolumeAttachment, "example", VolumeAttachmentConfig(
            instance_id="example",
            volume_id="example",
            device="example",
            ebs_card_index=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_volume_attachment, with instance id example, volume id
    example, device example, ebs card index 1.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="device" type="string">
  The device name Optional.
</ResponseField>

<ResponseField name="ebs_card_index" type="number">
  The index of the EBS card. Some instance types support multiple EBS cards. The default EBS card index is 0. Optional.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The ID of the instance to which the volume attaches Required.
</ResponseField>

<ResponseField name="volume_id" type="string" required>
  The ID of the Amazon EBS volume Required.
</ResponseField>
