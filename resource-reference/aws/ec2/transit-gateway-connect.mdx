---
title: "aws_ec2_transit_gateway_connect"
description: "Real, generated bindings for `aws_ec2_transit_gateway_connect`."
---

`aws_ec2_transit_gateway_connect` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway connect"})

    		ubx.Resource(ec2.TransitGatewayConnect, "ci-runner", ec2.TransitGatewayConnectConfig{
    			TransitGatewayId:      "example",
    			TransportAttachmentId: "example",
    			Protocol:              "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    			TransitGatewayDefaultRouteTableAssociation: true,
    			TransitGatewayDefaultRouteTablePropagation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayConnect } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_connect";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway connect" });

      resource(TransitGatewayConnect, "ci-runner", {
        transitGatewayId: "example",
        transportAttachmentId: "example",
        protocol: "example",
        tags: { "managed-by": "ubx" },
        transitGatewayDefaultRouteTableAssociation: true,
        transitGatewayDefaultRouteTablePropagation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayConnect, TransitGatewayConnectConfig

    def describe():
        ubx.intent("payments own transit gateway connect")

        ubx.resource(TransitGatewayConnect, "ci-runner", TransitGatewayConnectConfig(
            transit_gateway_id="example",
            transport_attachment_id="example",
            protocol="example",
            tags={"managed-by": "ubx"},
            transit_gateway_default_route_table_association=True,
            transit_gateway_default_route_table_propagation=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_connect, with transit gateway id
    example, transport attachment id example, protocol example, tags
    a set value, transit gateway default route table association
    true, transit gateway default route table propagation true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="transport_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
