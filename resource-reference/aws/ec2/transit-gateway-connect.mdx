---
title: "aws_ec2_transit_gateway_connect"
description: "Real, generated bindings for `aws_ec2_transit_gateway_connect`."
---

`aws_ec2_transit_gateway_connect` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.TransitGatewayConnectConfig{
        TransitGatewayId: "example",
        TransportAttachmentId: "example",
        Protocol: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ec2.TransitGatewayConnect, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayConnect } from
      "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_connect";

    resource(TransitGatewayConnect, "example", {
      transitGatewayId: "example",
      transportAttachmentId: "example",
      protocol: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TransitGatewayConnect,
        TransitGatewayConnectConfig,
    )

    cfg = TransitGatewayConnectConfig(
        transit_gateway_id="example",
        transport_attachment_id="example",
        protocol="example",
        tags={"key": "value"},
    )
    ubx.resource(TransitGatewayConnect, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_transit_gateway_connect with
transit_gateway_id, transport_attachment_id, protocol, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="transport_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
