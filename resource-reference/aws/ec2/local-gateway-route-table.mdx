---
title: "aws_ec2_local_gateway_route_table"
description: "`aws_ec2_local_gateway_route_table` defines real routing for an AWS Outposts local gateway..."
---

`aws_ec2_local_gateway_route_table` defines real routing for an AWS Outposts local gateway, controlling how traffic moves between the Outpost's own local network and the VPC it's associated with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own local gateway route table"})

    		ubx.Resource(ec2.LocalGatewayRouteTable, "example", ec2.LocalGatewayRouteTableConfig{
    			LocalGatewayId: "example",
    			Mode:           "example",
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayRouteTable } from "@ubx/sdk-aws/aws/ec2/local_gateway_route_table";

    export default stack("example", () => {
      intent({ summary: "example own local gateway route table" });

      resource(LocalGatewayRouteTable, "example", {
        localGatewayId: "example",
        mode: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayRouteTable, LocalGatewayRouteTableConfig
    from ubx.aws.ec2.local_gateway_route_table import LocalGatewayRouteTable_Tags

    def describe():
        ubx.intent("example own local gateway route table")

        ubx.resource(LocalGatewayRouteTable, "example", LocalGatewayRouteTableConfig(
            local_gateway_id="example",
            mode="example",
            tags=[LocalGatewayRouteTable_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_local_gateway_route_table, with local gateway id
    example, mode example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="local_gateway_id" type="string" required>
  The ID of the local gateway. Required.
</ResponseField>

<ResponseField name="mode" type="string">
  The mode of the local gateway route table. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs to associate with the local gateway route table for identification and categorization, where each object consists of a Key and a Value string. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag that can be attached to an EC2 Local Gateway Route Table for resource identification and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="local_gateway_route_table_arn" type="string">
  The ARN of the local gateway route table. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_id" type="string">
  The ID of the local gateway route table. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  The ARN of the outpost. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The owner of the local gateway route table. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the local gateway route table. Computed by AWS after `ship`.
</ResponseField>
