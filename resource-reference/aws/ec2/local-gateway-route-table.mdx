---
title: "aws_ec2_local_gateway_route_table"
description: "Real, generated bindings for `aws_ec2_local_gateway_route_table`."
---

`aws_ec2_local_gateway_route_table` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.LocalGatewayRouteTableConfig{
        LocalGatewayId: "example",
        Mode: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ec2.LocalGatewayRouteTable, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LocalGatewayRouteTable } from
      "jsr:@ubx/sdk-aws/aws/ec2/local_gateway_route_table";

    resource(LocalGatewayRouteTable, "example", {
      localGatewayId: "example",
      mode: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        LocalGatewayRouteTable,
        LocalGatewayRouteTableConfig,
    )

    cfg = LocalGatewayRouteTableConfig(
        local_gateway_id="example",
        mode="example",
        tags={"key": "value"},
    )
    ubx.resource(LocalGatewayRouteTable, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_local_gateway_route_table with
local_gateway_id, mode, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="local_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
