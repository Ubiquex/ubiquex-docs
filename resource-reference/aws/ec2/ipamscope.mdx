---
title: "aws_ec2_ipamscope"
description: "`aws_ec2_ipamscope` partitions an `aws_ec2_ipam`'s address space into a real, distinct pool of CIDRs, public, private..."
---

`aws_ec2_ipamscope` partitions an `aws_ec2_ipam`'s address space into a real, distinct pool of CIDRs, public, private, or a caller-defined custom scope, kept separate so address planning for internet-routable and private ranges don't overlap or interfere.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ipamscope"})

    		ubx.Resource(ec2.Ipamscope, "example", ec2.IpamscopeConfig{
    			IpamId:                         "example",
    			Description:                    "Managed by ubx.",
    			ExternalAuthorityConfiguration: map[string]any{"external_resource_identifier": "example"},
    			Tags:                           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Ipamscope } from "@ubx/sdk-aws/aws/ec2/ipamscope";

    export default stack("example", () => {
      intent({ summary: "example own ipamscope" });

      resource(Ipamscope, "example", {
        ipamId: "example",
        description: "Managed by ubx.",
        externalAuthorityConfiguration: { externalResourceIdentifier: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2.ipamscope import Ipamscope, IpamscopeConfig

    def describe():
        ubx.intent("example own ipamscope")

        ubx.resource(Ipamscope, "example", IpamscopeConfig(
            ipam_id="example",
            description="Managed by ubx.",
            external_authority_configuration={"external_resource_identifier": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_ipamscope, with ipam id example, description
    Managed by ubx., external authority configuration a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A user-defined description for the IPAM scope. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="external_authority_configuration" type="object">
  External service configuration to connect your AWS IPAM scope. Optional.
  <Expandable title="properties">
    <ResponseField name="external_resource_identifier" type="string" required>
      Resource identifier of the scope in the external service connecting to your AWS IPAM scope. Required.
    </ResponseField>
    <ResponseField name="ipam_scope_external_authority_type" type="string" required>
      An external service connecting to your AWS IPAM scope. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ipam_id" type="string" required>
  The Id of the IPAM this scope is a part of. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the IPAM scope. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_arn" type="string">
  The Amazon Resource Name (ARN) of the IPAM this scope is a part of. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_scope_id" type="string">
  Id of the IPAM scope. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_scope_type" type="string">
  Determines whether this scope contains publicly routable space or space for a private network Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Is this one of the default scopes created with the IPAM. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="pool_count" type="number">
  The number of pools that currently exist in this scope. Computed by AWS after `ship`.
</ResponseField>
