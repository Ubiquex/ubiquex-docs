---
title: "aws_ec2_local_gateway_route_table_virtual_interface_group_association"
description: "`aws_ec2_local_gateway_route_table_virtual_interface_group_association` is the real, actual link between an `aws_ec2_local_gateway_route_table` and..."
---

`aws_ec2_local_gateway_route_table_virtual_interface_group_association` is the real, actual link between an `aws_ec2_local_gateway_route_table` and an `aws_ec2_local_gateway_virtual_interface_group`, determining which physical interfaces that route table's routes apply across.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own local gateway route table virtual interface group association"})

    		ubx.Resource(ec2.LocalGatewayRouteTableVirtualInterfaceGroupAssociation, "example", ec2.LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig{
    			LocalGatewayRouteTableId:            "example",
    			LocalGatewayVirtualInterfaceGroupId: "example",
    			Tags:                                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayRouteTableVirtualInterfaceGroupAssociation } from "./local-sdk/aws/sdk/typescript/aws/ec2/local_gateway_route_table_virtual_interface_group_association.ts";

    export default stack("example", () => {
      intent({
        summary:
          "example own local gateway route table virtual interface group association",
      });

      resource(LocalGatewayRouteTableVirtualInterfaceGroupAssociation, "example", {
        localGatewayRouteTableId: "example",
        localGatewayVirtualInterfaceGroupId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayRouteTableVirtualInterfaceGroupAssociation, LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig

    def describe():
        ubx.intent("example own local gateway route table virtual interface group association")

        ubx.resource(LocalGatewayRouteTableVirtualInterfaceGroupAssociation, "example", LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig(
            local_gateway_route_table_id="example",
            local_gateway_virtual_interface_group_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_ec2_local_gateway_route_table_virtual_interface_group_association,
    with local gateway route table id example, local gateway virtual
    interface group id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="local_gateway_route_table_id" type="string" required>
  The ID of the local gateway route table. Required.
</ResponseField>

<ResponseField name="local_gateway_virtual_interface_group_id" type="string" required>
  The ID of the local gateway route table virtual interface group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      A user-defined key for a tag on this AWS Local Gateway Route Table Virtual Interface Group Association, used to categorize, organize, or filter the resource in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key attached to the Local Gateway Route Table Virtual Interface Group Association, used for identifying and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="local_gateway_id" type="string">
  The ID of the local gateway. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_arn" type="string">
  The ARN of the local gateway route table. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_virtual_interface_group_association_id" type="string">
  The ID of the local gateway route table virtual interface group association. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The owner of the local gateway route table virtual interface group association. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the local gateway route table virtual interface group association. Computed by AWS after `ship`.
</ResponseField>
