---
title: "aws_ec2_local_gateway_route_table_virtual_interface_group_association"
description: "Real, generated bindings for `aws_ec2_local_gateway_route_table_virtual_interface_group_association`."
---

`aws_ec2_local_gateway_route_table_virtual_interface_group_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own local gateway route table virtual interface group association"})

    		ubx.Resource(ec2.LocalGatewayRouteTableVirtualInterfaceGroupAssociation, "ci-runner", ec2.LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig{
    			LocalGatewayRouteTableId:            "example",
    			LocalGatewayVirtualInterfaceGroupId: "example",
    			Tags:                                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocalGatewayRouteTableVirtualInterfaceGroupAssociation } from "jsr:@ubx/sdk-aws/aws/ec2/local_gateway_route_table_virtual_interface_group_association";

    export default stack("payments", () => {
      intent({
        summary:
          "payments own local gateway route table virtual interface group association",
      });

      resource(
        LocalGatewayRouteTableVirtualInterfaceGroupAssociation,
        "ci-runner",
        {
          localGatewayRouteTableId: "example",
          localGatewayVirtualInterfaceGroupId: "example",
          tags: { "managed-by": "ubx" },
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import LocalGatewayRouteTableVirtualInterfaceGroupAssociation, LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig

    def describe():
        ubx.intent("payments own local gateway route table virtual interface group association")

        ubx.resource(LocalGatewayRouteTableVirtualInterfaceGroupAssociation, "ci-runner", LocalGatewayRouteTableVirtualInterfaceGroupAssociationConfig(
            local_gateway_route_table_id="example",
            local_gateway_virtual_interface_group_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_ec2_local_gateway_route_table_virtual_interface_group_association,
    with local gateway route table id example, local gateway virtual
    interface group id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="local_gateway_route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="local_gateway_virtual_interface_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="local_gateway_route_table_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
