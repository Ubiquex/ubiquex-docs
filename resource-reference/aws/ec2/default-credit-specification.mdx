---
title: "aws_ec2_default_credit_specification"
description: "Real, generated bindings for `aws_ec2_default_credit_specification`."
---

`aws_ec2_default_credit_specification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default credit specification"})

    		ubx.Resource(ec2.DefaultCreditSpecification, "ci-runner", ec2.DefaultCreditSpecificationConfig{
    			CpuCredits:     "example",
    			InstanceFamily: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultCreditSpecification } from "jsr:@ubx/sdk-aws/aws/ec2/default_credit_specification";

    export default stack("payments", () => {
      intent({ summary: "payments own default credit specification" });

      resource(DefaultCreditSpecification, "ci-runner", {
        cpuCredits: "example",
        instanceFamily: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import DefaultCreditSpecification, DefaultCreditSpecificationConfig

    def describe():
        ubx.intent("payments own default credit specification")

        ubx.resource(DefaultCreditSpecification, "ci-runner", DefaultCreditSpecificationConfig(
            cpu_credits="example",
            instance_family="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_default_credit_specification, with cpu credits
    example, instance family example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cpu_credits" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_family" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
