---
title: "aws_ec2_default_credit_specification"
description: "Real, generated bindings for `aws_ec2_default_credit_specification`."
---

`aws_ec2_default_credit_specification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.DefaultCreditSpecificationConfig{
        CpuCredits: "example",
        InstanceFamily: "example",
    }
    ubx.Resource(ec2.DefaultCreditSpecification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DefaultCreditSpecification } from
      "jsr:@ubx/sdk-aws/aws/ec2/default_credit_specification";

    resource(DefaultCreditSpecification, "example", {
      cpuCredits: "example",
      instanceFamily: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        DefaultCreditSpecification,
        DefaultCreditSpecificationConfig,
    )

    cfg = DefaultCreditSpecificationConfig(
        cpu_credits="example",
        instance_family="example",
    )
    ubx.resource(DefaultCreditSpecification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_default_credit_specification with cpu_credits,
instance_family.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cpu_credits" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_family" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
