---
title: "aws_ec2_transit_gateway_route_table_association"
description: "`aws_ec2_transit_gateway_route_table_association` associates a real transit gateway attachment with a specific route table on..."
---

`aws_ec2_transit_gateway_route_table_association` associates a real transit gateway attachment with a specific route table on an `aws_ec2_transit_gateway`, controlling which routes that attachment's own traffic is evaluated against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway route table association"})

    		ubx.Resource(ec2.TransitGatewayRouteTableAssociation, "ci-runner", ec2.TransitGatewayRouteTableAssociationConfig{
    			TransitGatewayAttachmentId: "example",
    			TransitGatewayRouteTableId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayRouteTableAssociation } from "./local-sdk/aws/sdk/typescript/aws/ec2/transit_gateway_route_table_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway route table association" });

      resource(TransitGatewayRouteTableAssociation, "ci-runner", {
        transitGatewayAttachmentId: "example",
        transitGatewayRouteTableId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayRouteTableAssociation, TransitGatewayRouteTableAssociationConfig

    def describe():
        ubx.intent("payments own transit gateway route table association")

        ubx.resource(TransitGatewayRouteTableAssociation, "ci-runner", TransitGatewayRouteTableAssociationConfig(
            transit_gateway_attachment_id="example",
            transit_gateway_route_table_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_route_table_association, with
    transit gateway attachment id example, transit gateway route
    table id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  The ID of transit gateway attachment. Required.
</ResponseField>

<ResponseField name="transit_gateway_route_table_id" type="string" required>
  The ID of transit gateway route table. Required.
</ResponseField>
