---
title: "aws_ec2_transit_gateway_metering_policy"
description: "Real, generated bindings for `aws_ec2_transit_gateway_metering_policy`."
---

`aws_ec2_transit_gateway_metering_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.TransitGatewayMeteringPolicyConfig{
        TransitGatewayId: "example",
        MiddleboxAttachmentIds: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ec2.TransitGatewayMeteringPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayMeteringPolicy } from
      "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_metering_policy";

    resource(TransitGatewayMeteringPolicy, "example", {
      transitGatewayId: "example",
      middleboxAttachmentIds: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TransitGatewayMeteringPolicy,
        TransitGatewayMeteringPolicyConfig,
    )

    cfg = TransitGatewayMeteringPolicyConfig(
        transit_gateway_id="example",
        middlebox_attachment_ids=["example"],
        tags={"key": "value"},
    )
    ubx.resource(TransitGatewayMeteringPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_transit_gateway_metering_policy with
transit_gateway_id, middlebox_attachment_ids, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="middlebox_attachment_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_metering_policy_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
