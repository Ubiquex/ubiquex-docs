---
title: "aws_ec2_availability_zone_group"
description: "Real, generated bindings for `aws_ec2_availability_zone_group`."
---

`aws_ec2_availability_zone_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own availability zone group"})

    		ubx.Resource(ec2.AvailabilityZoneGroup, "ci-runner", ec2.AvailabilityZoneGroupConfig{
    			GroupName:   "example-group-name",
    			OptInStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AvailabilityZoneGroup } from "jsr:@ubx/sdk-aws/aws/ec2/availability_zone_group";

    export default stack("payments", () => {
      intent({ summary: "payments own availability zone group" });

      resource(AvailabilityZoneGroup, "ci-runner", {
        groupName: "example-group-name",
        optInStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import AvailabilityZoneGroup, AvailabilityZoneGroupConfig

    def describe():
        ubx.intent("payments own availability zone group")

        ubx.resource(AvailabilityZoneGroup, "ci-runner", AvailabilityZoneGroupConfig(
            group_name="example-group-name",
            opt_in_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_availability_zone_group, with group name
    example-group-name, opt in status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="opt_in_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
