---
title: "aws_ec2_vpcdhcpoptions_association"
description: "`aws_ec2_vpcdhcpoptions_association` associates a real `aws_ec2_dhcpoptions` set with a specific VPC..."
---

`aws_ec2_vpcdhcpoptions_association` associates a real `aws_ec2_dhcpoptions` set with a specific VPC, the resource that actually applies that DHCP configuration, domain name, DNS servers, to instances launched in it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vpcdhcpoptions association"})

    		ubx.Resource(ec2.VpcdhcpoptionsAssociation, "example", ec2.VpcdhcpoptionsAssociationConfig{
    			DhcpOptionsId: "example",
    			VpcId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcdhcpoptionsAssociation } from "@ubx/sdk-aws/aws/ec2/vpcdhcpoptions_association";

    export default stack("example", () => {
      intent({ summary: "example own vpcdhcpoptions association" });

      resource(VpcdhcpoptionsAssociation, "example", {
        dhcpOptionsId: "example",
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VpcdhcpoptionsAssociation, VpcdhcpoptionsAssociationConfig

    def describe():
        ubx.intent("example own vpcdhcpoptions association")

        ubx.resource(VpcdhcpoptionsAssociation, "example", VpcdhcpoptionsAssociationConfig(
            dhcp_options_id="example",
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_vpcdhcpoptions_association, with dhcp options id
    example, vpc id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="dhcp_options_id" type="string" required>
  The ID of the DHCP options set, or default to associate no DHCP options with the VPC. Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC. Required.
</ResponseField>
