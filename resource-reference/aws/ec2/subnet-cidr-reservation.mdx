---
title: "aws_ec2_subnet_cidr_reservation"
description: "Real, generated bindings for `aws_ec2_subnet_cidr_reservation`."
---

`aws_ec2_subnet_cidr_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.SubnetCidrReservationConfig{
        CidrBlock: "example",
        ReservationType: "example",
        SubnetId: "example",
        Description: "example",
    }
    ubx.Resource(ec2.SubnetCidrReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SubnetCidrReservation } from
      "jsr:@ubx/sdk-aws/aws/ec2/subnet_cidr_reservation";

    resource(SubnetCidrReservation, "example", {
      cidrBlock: "example",
      reservationType: "example",
      subnetId: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        SubnetCidrReservation,
        SubnetCidrReservationConfig,
    )

    cfg = SubnetCidrReservationConfig(
        cidr_block="example",
        reservation_type="example",
        subnet_id="example",
        description="example",
    )
    ubx.resource(SubnetCidrReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_subnet_cidr_reservation with cidr_block,
reservation_type, subnet_id, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_block" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservation_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
