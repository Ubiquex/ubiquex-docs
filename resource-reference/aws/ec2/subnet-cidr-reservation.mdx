---
title: "aws_ec2_subnet_cidr_reservation"
description: "Real, generated bindings for `aws_ec2_subnet_cidr_reservation`."
---

`aws_ec2_subnet_cidr_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subnet cidr reservation"})

    		ubx.Resource(ec2.SubnetCidrReservation, "ci-runner", ec2.SubnetCidrReservationConfig{
    			CidrBlock:       "example",
    			ReservationType: "example",
    			SubnetId:        "example",
    			Description:     "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SubnetCidrReservation } from "jsr:@ubx/sdk-aws/aws/ec2/subnet_cidr_reservation";

    export default stack("payments", () => {
      intent({ summary: "payments own subnet cidr reservation" });

      resource(SubnetCidrReservation, "ci-runner", {
        cidrBlock: "example",
        reservationType: "example",
        subnetId: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import SubnetCidrReservation, SubnetCidrReservationConfig

    def describe():
        ubx.intent("payments own subnet cidr reservation")

        ubx.resource(SubnetCidrReservation, "ci-runner", SubnetCidrReservationConfig(
            cidr_block="example",
            reservation_type="example",
            subnet_id="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_subnet_cidr_reservation, with cidr block example,
    reservation type example, subnet id example, description Managed
    by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_block" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservation_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
