---
title: "aws_ec2_transit_gateway_peering_attachment_accepter"
description: "Real, generated bindings for `aws_ec2_transit_gateway_peering_attachment_accepter`."
---

`aws_ec2_transit_gateway_peering_attachment_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.TransitGatewayPeeringAttachmentAccepterConfig{
        TransitGatewayAttachmentId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        ec2.TransitGatewayPeeringAttachmentAccepter,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayPeeringAttachmentAccepter } from
      "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_peering_attachment_accepter";

    resource(TransitGatewayPeeringAttachmentAccepter, "example", {
      transitGatewayAttachmentId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TransitGatewayPeeringAttachmentAccepter,
        TransitGatewayPeeringAttachmentAccepterConfig,
    )

    cfg = TransitGatewayPeeringAttachmentAccepterConfig(
        transit_gateway_attachment_id="example",
        tags={"key": "value"},
    )
    ubx.resource(
        TransitGatewayPeeringAttachmentAccepter,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_transit_gateway_peering_attachment_accepter
with transit_gateway_attachment_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peer_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peer_transit_gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
