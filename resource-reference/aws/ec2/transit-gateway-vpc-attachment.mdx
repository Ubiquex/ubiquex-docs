---
title: "aws_ec2_transit_gateway_vpc_attachment"
description: "Real, generated bindings for `aws_ec2_transit_gateway_vpc_attachment`."
---

`aws_ec2_transit_gateway_vpc_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway vpc attachment"})

    		ubx.Resource(ec2.TransitGatewayVpcAttachment, "ci-runner", ec2.TransitGatewayVpcAttachmentConfig{
    			SubnetIds:            []string{"example"},
    			TransitGatewayId:     "example",
    			VpcId:                "example",
    			ApplianceModeSupport: "example",
    			DnsSupport:           "example",
    			Ipv6Support:          "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayVpcAttachment } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_vpc_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway vpc attachment" });

      resource(TransitGatewayVpcAttachment, "ci-runner", {
        subnetIds: ["example"],
        transitGatewayId: "example",
        vpcId: "example",
        applianceModeSupport: "example",
        dnsSupport: "example",
        ipv6Support: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayVpcAttachment, TransitGatewayVpcAttachmentConfig

    def describe():
        ubx.intent("payments own transit gateway vpc attachment")

        ubx.resource(TransitGatewayVpcAttachment, "ci-runner", TransitGatewayVpcAttachmentConfig(
            subnet_ids=["example"],
            transit_gateway_id="example",
            vpc_id="example",
            appliance_mode_support="example",
            dns_support="example",
            ipv6_support="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_vpc_attachment, with subnet ids a
    set value, transit gateway id example, vpc id example, appliance
    mode support example, dns support example, ipv6 support example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="appliance_mode_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="dns_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_referencing_support" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_referencing_support" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_association" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_default_route_table_propagation" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
