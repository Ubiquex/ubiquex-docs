---
title: "aws_ec2_instance_state"
description: "Real, generated bindings for `aws_ec2_instance_state`."
---

`aws_ec2_instance_state` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance state"})

    		ubx.Resource(ec2.InstanceState, "ci-runner", ec2.InstanceStateConfig{
    			InstanceId: "example",
    			State:      "example",
    			Force:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceState } from "jsr:@ubx/sdk-aws/aws/ec2/instance_state";

    export default stack("payments", () => {
      intent({ summary: "payments own instance state" });

      resource(InstanceState, "ci-runner", {
        instanceId: "example",
        state: "example",
        force: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import InstanceState, InstanceStateConfig

    def describe():
        ubx.intent("payments own instance state")

        ubx.resource(InstanceState, "ci-runner", InstanceStateConfig(
            instance_id="example",
            state="example",
            force=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_instance_state, with instance id example, state
    example, force true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="force" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
