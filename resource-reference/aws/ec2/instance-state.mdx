---
title: "aws_ec2_instance_state"
description: "Real, generated bindings for `aws_ec2_instance_state`."
---

`aws_ec2_instance_state` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.InstanceStateConfig{
        InstanceId: "example",
        State: "example",
        Force: true,
    }
    ubx.Resource(ec2.InstanceState, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceState } from
      "jsr:@ubx/sdk-aws/aws/ec2/instance_state";

    resource(InstanceState, "example", {
      instanceId: "example",
      state: "example",
      force: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import InstanceState, InstanceStateConfig

    cfg = InstanceStateConfig(
        instance_id="example",
        state="example",
        force=True,
    )
    ubx.resource(InstanceState, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_instance_state with instance_id, state, force.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="force" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
