---
title: "aws_ec2_fleet"
description: "Real, generated bindings for `aws_ec2_fleet`."
---

`aws_ec2_fleet` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fleet"})

    		ubx.Resource(ec2.Fleet, "ci-runner", ec2.FleetConfig{
    			Context:                          "example",
    			ExcessCapacityTerminationPolicy:  "example",
    			ReplaceUnhealthyInstances:        true,
    			Tags:                             map[string]string{"managed-by": "ubx"},
    			TerminateInstances:               true,
    			TerminateInstancesWithExpiration: true,
    			Type:                             "example",
    			ValidFrom:                        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Fleet } from "jsr:@ubx/sdk-aws/aws/ec2/fleet";

    export default stack("payments", () => {
      intent({ summary: "payments own fleet" });

      resource(Fleet, "ci-runner", {
        context: "example",
        excessCapacityTerminationPolicy: "example",
        replaceUnhealthyInstances: true,
        tags: { "managed-by": "ubx" },
        terminateInstances: true,
        terminateInstancesWithExpiration: true,
        type: "example",
        validFrom: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import Fleet, FleetConfig

    def describe():
        ubx.intent("payments own fleet")

        ubx.resource(Fleet, "ci-runner", FleetConfig(
            context="example",
            excess_capacity_termination_policy="example",
            replace_unhealthy_instances=True,
            tags={"managed-by": "ubx"},
            terminate_instances=True,
            terminate_instances_with_expiration=True,
            type="example",
            valid_from="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_fleet, with context example, excess capacity
    termination policy example, replace unhealthy instances true,
    tags a set value, terminate instances true, terminate instances
    with expiration true, type example, valid from example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="context" type="string">
  Optional.
</ResponseField>

<ResponseField name="excess_capacity_termination_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="fleet_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="fulfilled_capacity" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="fulfilled_on_demand_capacity" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replace_unhealthy_instances" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="terminate_instances" type="bool">
  Optional.
</ResponseField>

<ResponseField name="terminate_instances_with_expiration" type="bool">
  Optional.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

<ResponseField name="valid_from" type="string">
  Optional.
</ResponseField>

<ResponseField name="valid_until" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="fleet_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="fulfilled_capacity" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="fulfilled_on_demand_capacity" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
