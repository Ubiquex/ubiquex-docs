---
title: "aws_ec2_fleet"
description: "`aws_ec2_fleet` launches a real, defined mix of On-Demand, Spot..."
---

`aws_ec2_fleet` launches a real, defined mix of On-Demand, Spot, and Reserved Instance capacity across multiple instance types and Availability Zones from one request, rather than a caller managing separate launch requests per purchasing option.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fleet"})

    		ubx.Resource(ec2.Fleet, "ci-runner", ec2.FleetConfig{
    			LaunchTemplateConfigs:           []map[string]any{{"launch_template_specification": map[string]any{"launch_template_id": "example"}}},
    			TargetCapacitySpecification:     map[string]any{"total_target_capacity": 1},
    			Context:                         "example",
    			ExcessCapacityTerminationPolicy: "example",
    			OnDemandOptions:                 map[string]any{"allocation_strategy": "example"},
    			ReplaceUnhealthyInstances:       true,
    			ReservedCapacityOptions:         map[string]any{"reservation_types": []string{"example"}},
    			SpotOptions:                     map[string]any{"allocation_strategy": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Fleet } from "./local-sdk/aws/sdk/typescript/aws/ec2/fleet.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own fleet" });

      resource(Fleet, "ci-runner", {
        launchTemplateConfigs: [{
          launchTemplateSpecification: { launchTemplateId: "example" },
        }],
        targetCapacitySpecification: { totalTargetCapacity: 1 },
        context: "example",
        excessCapacityTerminationPolicy: "example",
        onDemandOptions: { allocationStrategy: "example" },
        replaceUnhealthyInstances: true,
        reservedCapacityOptions: { reservationTypes: ["example"] },
        spotOptions: { allocationStrategy: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import Fleet, FleetConfig

    def describe():
        ubx.intent("payments own fleet")

        ubx.resource(Fleet, "ci-runner", FleetConfig(
            launch_template_configs=[{"launch_template_specification": {"launch_template_id": "example"}}],
            target_capacity_specification={"total_target_capacity": 1},
            context="example",
            excess_capacity_termination_policy="example",
            on_demand_options={"allocation_strategy": "example"},
            replace_unhealthy_instances=True,
            reserved_capacity_options={"reservation_types": ["example"]},
            spot_options={"allocation_strategy": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_fleet, with launch template configs a set value,
    target capacity specification a set value, context example,
    excess capacity termination policy example, on demand options a
    set value, replace unhealthy instances true, reserved capacity
    options a set value, spot options a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="context" type="string">
  The `context` field specifies a string that AWS reserves for future use in the EC2 Fleet, and it does not currently alter fleet behavior. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="excess_capacity_termination_policy" type="string">
  Controls whether excess instances are terminated when the EC2 Fleet's target capacity is reduced below its current running capacity, accepting values 'termination' (default) or 'no-termination'. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="launch_template_configs" type="list(object)" required>
  Specifies the launch template configurations (including launch template ID/name and overrides for instance type, subnet, etc.) that the EC2 Fleet uses to launch instances. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="launch_template_specification" type="object">
      Specifies the launch template ID or name and its version, which the EC2 Fleet uses as the base configuration for launching instances in this fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="launch_template_id" type="string">
          Specifies the ID of the EC2 launch template that defines the instance configuration (such as AMI, instance type, and network settings) for the instances launched by the EC2 Fleet from this launch template config. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="launch_template_name" type="string">
          The name of the EC2 launch template to use for instances launched in this fleet's launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="launch_template_specification_user_data" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="version" type="string">
          The version of the launch template (e.g., a specific version number, $Latest, or $Default) that the EC2 Fleet uses when launching instances for this launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="overrides" type="list(object)">
      Specifies the instance types, subnets, and other parameters that override the associated launch template when EC2 Fleet selects them for launching instances. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="availability_zone" type="string">
          Specifies the Availability Zone in which to launch the instances for this fleet, overriding the placement specified in the launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="availability_zone_id" type="string">
          Specifies the ID of the Availability Zone (e.g., use1-az1) in which the EC2 Fleet launches instances for this override, replacing any Availability Zone from the launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="block_device_mappings" type="list(object)">
          Defines the block device mapping overrides applied to instances launched using this launch template override, such as attaching additional EBS volumes or modifying root volume settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="device_name" type="string">
              The device name (e.g., /dev/xvda or /dev/sdh) exposed to the instance for a block device mapping override applied to a launch template configuration in the EC2 Fleet, determining where the attached volume or instance store volume appears within the guest OS. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ebs" type="object">
              Specifies the EBS volume settings (e.g., volume size, type, IOPS, encryption, and delete-on-termination behavior) for a block device in an EC2 Fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="delete_on_termination" type="bool">
                  Indicates whether the EBS volume created by this block device mapping is automatically deleted when the EC2 instance terminates. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="encrypted" type="bool">
                  Specifies whether the EBS volume for this block device mapping in the EC2 Fleet launch template override should be encrypted, allowing enforcement of encryption on launched instances. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="iops" type="number">
                  Specifies the provisioned IOPS (I/O operations per second) for the EBS volume in a launch template override's block device mapping, used when configuring an EC2 Fleet with volume types that support custom IOPS (io1, io2, or gp3). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_id" type="string">
                  The ID or ARN of the AWS KMS key used to encrypt the EBS volume created by this block device mapping override in the EC2 Fleet launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="snapshot_id" type="string">
                  The ID of the snapshot from which to create the EBS volume, used to initialize the volume with the data from that snapshot. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_size" type="number">
                  Specifies the size of the EBS volume in GiB for the block device mapping override in the EC2 Fleet launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_type" type="string">
                  Specifies the EBS volume type (e.g., gp2, gp3, io1) to use for the block device in the launch template override when the EC2 Fleet launches instances. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="no_device" type="string">
              Indicates that the specified device name should not be mapped to a block device on the launched instance, effectively suppressing that device from the instance's block device mapping. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="virtual_name" type="string">
              Specifies the virtual device name (e.g., ephemeral0) for an ephemeral instance store volume in the block device mapping of a launch template override used by the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="iam_instance_profile" type="object">
          Specifies the IAM instance profile to associate with instances launched according to this launch template override, which overrides any IAM instance profile set in the launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="arn" type="string">
              The ARN of the IAM instance profile to associate with the instances launched from this EC2 Fleet override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Specifies the name of the IAM instance profile assigned to instances launched from this fleet override, providing them with the permissions of the associated IAM role. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="instance_requirements" type="object">
          Defines the hardware requirements (such as vCPU count and memory) that an instance type must meet for EC2 Fleet to select it for this launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="accelerator_count" type="object">
              Specifies the minimum and maximum number of accelerators (such as GPUs) that an instance must have to be considered for the fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum number of accelerators (such as GPUs or FPGAs) that an instance type can have in order to match the instance requirements for the EC2 Fleet override. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum number of accelerators (such as GPUs) that an instance type must have to satisfy the instance requirements for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="accelerator_manufacturers" type="list(string)">
              Defines which accelerator (GPU) manufacturers are acceptable for instance types that match this launch template override, filtering by vendors such as NVIDIA or AMD. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="accelerator_names" type="list(string)">
              Lists the accelerator names (e.g., 'a100', 'v100') that instance types must have, so the EC2 Fleet only selects instances with those accelerators when using this launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="accelerator_total_memory_mi_b" type="object">
              Defines the minimum and maximum total memory (in MiB) required for accelerators (such as GPUs) on instance types that the EC2 Fleet can launch. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum total accelerator memory, in MiB, that an instance type must have to match the instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum total accelerator (GPU) memory, in MiB, that an instance type must have to be considered for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="accelerator_types" type="list(string)">
              Specifies the types of hardware accelerators (such as GPU, FPGA, or inference) that instances must have to meet the instance requirements, filtering which instance types are eligible for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allowed_instance_types" type="list(string)">
              Specifies a list of instance types that are allowed for the fleet to use, ensuring that the fleet launches instances only on these permitted types. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bare_metal" type="string">
              Specifies whether the instance type must be a bare metal instance, with allowed values included, excluded, or required. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="baseline_ebs_bandwidth_mbps" type="object">
              Specifies the minimum and maximum baseline EBS bandwidth (in Mbps) that an instance type must meet to be eligible for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  Specifies the maximum baseline EBS bandwidth in Mbps that an instance type must have to be considered for the EC2 Fleet, used as a filter in the instance requirements overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  Specifies the minimum baseline EBS bandwidth (in Mbps) that an instance must meet to be considered for the EC2 Fleet's instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="baseline_performance_factors" type="object">
              Specifies the baseline performance requirements for CPU and memory of instance types that the EC2 Fleet can launch, enabling filtering based on a minimum performance threshold. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cpu" type="object">
                  Specifies the CPU baseline performance factor, which uses a set of reference instance families to define the minimum expected CPU performance that instance types must meet for the fleet's launch configuration overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="references" type="list(object)">
                      A list of CPU performance factor references, each defining an instance family (e.g., m5, c5) that serves as the baseline for CPU performance when selecting instance types for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="instance_family" type="string">
                          The instance family name (e.g., c5, m5) used as a baseline CPU performance reference when specifying instance requirements for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="burstable_performance" type="string">
              Specifies whether to include, require, or exclude burstable performance instance types (such as T3, T4g) in the EC2 Fleet's instance requirements, with valid values 'included', 'required', or 'excluded'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cpu_manufacturers" type="list(string)">
              A list of CPU manufacturers (such as Intel, AMD, and Amazon Web Services) that the instance requirements will accept when selecting instance types for the EC2 Fleet to launch. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="excluded_instance_types" type="list(string)">
              List of EC2 instance types that are explicitly excluded from consideration when the fleet launches instances under these instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="instance_generations" type="list(string)">
              Specifies the list of instance generations (for example, 'current' or 'previous') that the instance types must belong to in order to be used for this fleet's launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="local_storage" type="string">
              Specifies whether the instance types selected for the fleet must have local storage (such as NVMe) or not, with values like `required`, `preferred`, or `excluded`. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="local_storage_types" type="list(string)">
              Filters candidate instances for the fleet to those whose local storage type matches the specified values (hdd and/or ssd), so only instances with that kind of local storage are considered. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_spot_price_as_percentage_of_optimal_on_demand_price" type="number">
              Specifies the maximum Spot Instance price you are willing to pay as a percentage of the optimal On-Demand price for that instance type, used as a price protection threshold to filter candidate instance types in the fleet's instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="memory_gi_bper_vcpu" type="object">
              Specifies the minimum and maximum memory in GiB per vCPU for an instance type to be considered when fulfilling the fleet's launch template overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum memory per vCPU (in GiB) that an instance must have to match the specified instance requirements in the EC2 Fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum amount of memory (in GiB) per vCPU that an instance type must have to match the instance requirements when launching an EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="memory_mi_b" type="object">
              Defines the range of memory (in MiB) for instance types that the EC2 Fleet can select, with minimum and maximum values. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum amount of memory, in MiB, that an instance type must have to be eligible for the fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum amount of memory, in MiB, that an instance type must have to be included in the fleet's selection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="network_bandwidth_gbps" type="object">
              Defines the minimum and maximum network bandwidth in gigabits per second (Gbps) that an instance type must offer to be considered a match for the EC2 Fleet's overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  Sets the maximum network bandwidth (in Gbps) that an instance type must offer to be selected by the EC2 Fleet when using this launch template override's instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum network bandwidth in gigabits per second that instances must meet to be considered by the EC2 Fleet's launch template override instance requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="network_interface_count" type="object">
              Specifies the minimum and maximum number of network interfaces that an instance must have to satisfy the instance requirements for an EC2 Fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum number of network interfaces that an instance type must support for it to be considered by the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum number of network interfaces that an instance must have to satisfy the instance requirements in an EC2 Fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="on_demand_max_price_percentage_over_lowest_price" type="number">
              Sets the maximum price for On-Demand instances as a percentage above the lowest-priced instance type that meets the specified requirements, used to control costs when EC2 Fleet selects instance types. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_encryption_in_transit" type="bool">
              When set to true, only instance types that support encryption in transit are considered when fulfilling the fleet's launch template overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_hibernate_support" type="bool">
              Indicates whether the instance types must support hibernation; when set to true, only instance types that can hibernate are considered for the fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="spot_max_price_percentage_over_lowest_price" type="number">
              Specifies the maximum price you are willing to pay for a Spot Instance as a percentage above the lowest Spot price among the eligible instance types, used within a launch template override's instance requirements to control Spot selection in the fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_local_storage_gb" type="object">
              Defines the minimum and maximum amount of local storage (in GB) that an instance type must provide for the fleet to consider it, filtering candidate instance types. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum amount of local storage (in GB) that an instance must have to satisfy the instance requirements for this launch template override in the EC2 fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum total amount of local storage (in GB) that an instance type must have to be considered for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="vcpu_count" type="object">
              Defines the minimum and maximum number of vCPUs that an instance type must have to be eligible for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum number of vCPUs an instance type can have to be eligible for the launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum number of vCPUs that an instance type must offer to be eligible for the EC2 Fleet's capacity. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="instance_type" type="string">
          The EC2 instance type to launch in this override of the launch template configuration for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key_name" type="string">
          Specifies the EC2 key pair name to associate with instances launched by this launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="max_price" type="string">
          The maximum price per unit hour that you are willing to pay for a Spot Instance in this EC2 Fleet, which overrides the max price set in the associated launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata_options" type="object">
          Sets the instance metadata service (IMDS) options -- such as requiring token usage with HttpTokens and controlling the HTTP endpoint with HttpEndpoint -- for EC2 instances launched by this specific fleet override. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="http_endpoint" type="string">
              Indicates whether the IPv4 instance metadata endpoint (enabled or disabled) is enabled for instances launched from this launch template override in the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="http_put_response_hop_limit" type="number">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="http_tokens" type="string">
              Determines whether HTTP tokens are required for accessing the instance metadata service for instances launched with this override, enforcing the use of IMDSv2 when set to 'required'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_interfaces" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="associate_public_ip_address" type="bool">
              Indicates whether to automatically assign a public IPv4 address to the primary network interface of an instance launched using this launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="delete_on_termination" type="bool">
              Indicates whether this network interface is deleted when the EC2 instance is terminated. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              Provides a text description for the network interface created for the fleet's instances, overriding the description in the launch template if provided. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="device_index" type="number">
              The zero-based position of the network interface on the instance (e.g., 0 for the primary interface) for this launch template override, which determines the attachment order when multiple interfaces are specified. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="groups" type="list(string)">
              The list of security group IDs to attach to the network interface defined by this override in the EC2 Fleet launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interface_type" type="string">
              Specifies the type of network interface for the override, with valid values being 'interface' for a standard VPC network interface, 'efa' for an Elastic Fabric Adapter, or 'trunk' for a trunk network interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ipv6_address_count" type="number">
              Specifies the number of IPv6 addresses to assign to the network interface for instances launched using this EC2 Fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ipv6_addresses" type="list(object)">
              The IPv6 addresses to assign to the network interface when overridden for a launch template configuration in the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="ipv6_address" type="string">
                  The specific IPv6 address to assign to the network interface for the EC2 Fleet instance override, enabling a static address from the subnet's IPv6 range. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="network_card_index" type="number">
              The index of the network card on the instance, which associates the network interface with a specific network card for instance types that support multiple network cards. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="network_interface_id" type="string">
              The ID of an existing Elastic Network Interface (ENI) that overrides the network interface configuration from the launch template for instances launched by this fleet override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="private_ip_address" type="string">
              Specifies the primary private IPv4 address to assign to the network interface; if omitted, AWS automatically selects an available address from the subnet's range. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="private_ip_addresses" type="list(object)">
              Defines private IPv4 addresses to assign to the network interface for instances launched by the EC2 Fleet, where each entry specifies the IP address and whether it is the primary private IP. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="primary" type="bool">
                  Indicates whether the specified private IP address is the primary private IP address for the network interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="private_ip_address" type="string">
                  The private IPv4 address to assign to a network interface in the EC2 Fleet launch template override, where each entry in the private_ip_addresses list specifies one such address. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="secondary_private_ip_address_count" type="number">
              Sets the number of secondary private IPv4 addresses to automatically assign to the network interface when an EC2 Fleet instance is launched with this launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnet_id" type="string">
              The ID of the subnet in which the instances will be launched when applying this network interface override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="placement" type="object">
          Overrides the EC2 placement settings (e.g., Availability Zone, Affinity, Tenancy) for instances launched with this launch template override in the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="affinity" type="string">
              The affinity setting (such as 'host' or 'default') that determines whether instances launched with this placement override are associated with a dedicated host in the EC2 Fleet's launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="availability_zone" type="string">
              Specifies the Availability Zone in which to launch the instances for this fleet override, overriding the placement setting from the launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="group_name" type="string">
              The name of the placement group in which to launch the instances for this fleet launch template override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="host_id" type="string">
              The ID of the Dedicated Host on which to launch the instances in this fleet override. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="host_resource_group_arn" type="string">
              The ARN of the host resource group in which to launch the instances, used to place instances on a group of dedicated hosts for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="partition_number" type="number">
              Specifies the partition number within a partition placement group where the instances launched from this launch template override should be placed. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="spread_domain" type="string">
              Specifies a specific spread domain within a spread placement group for the instance overrides, controlling which fault-isolated partition the fleet-launched instances are placed into. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tenancy" type="string">
              Specifies the tenancy (default, dedicated, or host) for instances launched using this placement override in the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="priority" type="number">
          Defines the order in which EC2 Fleet prioritizes this instance type override when fulfilling capacity, where lower numbers indicate higher priority. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnet_id" type="string">
          The ID of the subnet in which to launch the instances, overriding any subnet specified in the associated launch template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="weighted_capacity" type="number">
          The number of capacity units that each instance of this instance type contributes to the fleet's target capacity, enabling instance weighting for mixed instance types in the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="on_demand_options" type="object">
  Specifies the allocation strategy and other options for On-Demand Instances in the EC2 Fleet, such as how capacity is prioritized and whether to use capacity reservations. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="allocation_strategy" type="string">
      Determines the order in which On-Demand Instances are launched across instance types in the fleet, with 'lowest-price' selecting the lowest-priced type first (default) and 'prioritized' following the priority order defined in the launch template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="capacity_reservation_options" type="object">
      Specifies whether to use unused Capacity Reservations to fulfill On-Demand Instance capacity in the EC2 Fleet, including the usage strategy (e.g., use-capacity-reservations-first). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="usage_strategy" type="string">
          Determines whether the EC2 Fleet uses unused Capacity Reservations first to fulfill On-Demand capacity before launching new On-Demand Instances, with the only valid value being 'use-capacity-reservations-first'. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_total_price" type="string">
      Specifies the maximum amount per hour (in USD) you are willing to pay for all On-Demand Instances launched by the EC2 Fleet. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="min_target_capacity" type="number">
      The minimum number of On-Demand Instances that the EC2 Fleet must maintain, ensuring a baseline of On-Demand capacity within the fleet's total target capacity. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="single_availability_zone" type="bool">
      Indicates whether the fleet launches all On-Demand Instances into a single Availability Zone. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="single_instance_type" type="bool">
      When enabled, this makes the On-Demand portion of the EC2 Fleet launch only a single instance type (as specified in the launch template or its overrides) rather than multiple types chosen by the allocation strategy. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="replace_unhealthy_instances" type="bool">
  Indicates whether EC2 Fleet automatically replaces instances that become unhealthy, launching new instances to maintain the fleet's target capacity. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="reserved_capacity_options" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="reservation_types" type="list(string)">
      Specifies the types of reserved capacity (e.g., capacity-reservation or capacity-block) that the EC2 Fleet can use to fulfill On-Demand capacity. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="spot_options" type="object">
  Configures the Spot Instance purchasing behavior for the fleet, including allocation strategy, instance interruption behavior, and maximum price per hour. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="allocation_strategy" type="string">
      Determines how the EC2 Fleet allocates Spot Instance capacity across Spot pools, using strategies such as lowest-price, diversified, or capacity-optimized. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_interruption_behavior" type="string">
      Controls the behavior of Spot Instances in the fleet when they are interrupted by EC2, allowing values such as 'terminate', 'stop', or 'hibernate' to specify whether instances are terminated, stopped, or hibernated. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_pools_to_use_count" type="number">
      Specifies the number of Spot capacity pools across which the EC2 Fleet distributes its target Spot capacity, and is only used when the Spot allocation strategy is set to 'lowestPrice'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="maintenance_strategies" type="object">
      Specifies the maintenance strategies for the Spot Instances in the fleet, such as enabling capacity rebalancing to automatically replace instances that are at risk of interruption. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capacity_rebalance" type="object">
          Configures the capacity rebalance behavior for the fleet's Spot Instances, allowing EC2 Fleet to attempt to launch a replacement instance before an existing Spot Instance is interrupted, with a configurable replacement strategy. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="replacement_strategy" type="string">
              Determines whether EC2 Fleet launches a replacement Spot Instance when the current instance is at risk of interruption (launch) or launches a replacement before terminating the current instance (launch-before-terminate). Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="termination_delay" type="number">
              The number of minutes to wait after receiving a Spot Instance rebalance notification before terminating the instance, allowing proactive replacement during capacity rebalancing. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_total_price" type="string">
      The maximum hourly price you are willing to pay for Spot Instances in the EC2 Fleet, specified as a string representing a dollar amount (e.g., '0.50'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="min_target_capacity" type="number">
      Specifies the minimum target capacity for Spot Instances in the fleet; if the fleet cannot reach this capacity, it launches no instances. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="single_availability_zone" type="bool">
      When set to true, the Spot Fleet launches all instances in the same single Availability Zone, instead of potentially spreading across multiple, to keep them co-located for low latency. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="single_instance_type" type="bool">
      Indicates whether the Spot Fleet will launch a single instance type to fulfill the entire target capacity, rather than diversifying across multiple instance types. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tag_specifications" type="list(object)">
  The list of tag specifications that define the tags to apply to the EC2 Fleet resource itself, typically using the resource type 'fleet'. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="resource_type" type="string">
      Specifies the type of resource to tag, such as an instance or the fleet itself, within a tag specification for the EC2 Fleet. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Specifies the list of tags (key-value pairs) to apply to the fleet's launched resources, such as instances or volumes, as part of the parent tag specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The tag key (e.g., 'Name') of a tag to apply to EC2 instances that the fleet launches, as defined in this tag specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the value of a tag assigned to the EC2 Fleet, used to categorize and identify the fleet among your AWS resources. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_capacity_specification" type="object" required>
  Configures the overall target capacity for the EC2 Fleet, including total, On-Demand, and Spot capacity values, the default capacity type for provisioning, and the unit type (instances or vCPU) used for measurement. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="default_target_capacity_type" type="string">
      Sets the default purchase type (on-demand or spot) for the EC2 Fleet's target capacity when individual capacity types are not otherwise specified. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="on_demand_target_capacity" type="number">
      The number of On-Demand units to request as the target capacity for the EC2 Fleet. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="spot_target_capacity" type="number">
      Specifies the number of units to request for Spot Instance capacity in the EC2 Fleet's overall target capacity. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="target_capacity_unit_type" type="string">
      The unit of measure for the target capacity of the EC2 Fleet, either 'vcpu' or 'memory-mib'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_target_capacity" type="number" required>
      The total number of capacity units (instances or other units) that the EC2 Fleet aims to launch and maintain across all specified capacity reservation and instance type options. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terminate_instances_with_expiration" type="bool">
  Indicates whether running instances should be terminated when the EC2 Fleet reaches its expiration time (validUntil); if false, instances continue to run beyond expiration. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string">
  Specifies the type of EC2 Fleet (maintain, request, or instant), which controls whether the fleet maintains a target capacity over time, fulfills a one-time request, or launches the capacity immediately. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="valid_from" type="string">
  The timestamp (in UTC) at which the EC2 Fleet request becomes valid, after which the fleet can begin launching instances. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="valid_until" type="string">
  The date and time when the EC2 Fleet request expires, and after which AWS will not launch any new instances for the fleet. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="fleet_id" type="string">
  The unique identifier assigned by AWS to the EC2 Fleet, used to reference the fleet in API calls and other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
