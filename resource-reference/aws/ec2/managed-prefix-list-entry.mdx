---
title: "aws_ec2_managed_prefix_list_entry"
description: "Real, generated bindings for `aws_ec2_managed_prefix_list_entry`."
---

`aws_ec2_managed_prefix_list_entry` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.ManagedPrefixListEntryConfig{
        Cidr: "example",
        PrefixListId: "example",
        Description: "example",
    }
    ubx.Resource(ec2.ManagedPrefixListEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedPrefixListEntry } from
      "jsr:@ubx/sdk-aws/aws/ec2/managed_prefix_list_entry";

    resource(ManagedPrefixListEntry, "example", {
      cidr: "example",
      prefixListId: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        ManagedPrefixListEntry,
        ManagedPrefixListEntryConfig,
    )

    cfg = ManagedPrefixListEntryConfig(
        cidr="example",
        prefix_list_id="example",
        description="example",
    )
    ubx.resource(ManagedPrefixListEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_managed_prefix_list_entry with cidr,
prefix_list_id, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix_list_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
