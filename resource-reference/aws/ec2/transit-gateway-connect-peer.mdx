---
title: "aws_ec2_transit_gateway_connect_peer"
description: "Real, generated bindings for `aws_ec2_transit_gateway_connect_peer`."
---

`aws_ec2_transit_gateway_connect_peer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway connect peer"})

    		ubx.Resource(ec2.TransitGatewayConnectPeer, "ci-runner", ec2.TransitGatewayConnectPeerConfig{
    			InsideCidrBlocks:           []string{"example"},
    			PeerAddress:                "example",
    			TransitGatewayAttachmentId: "example",
    			Tags:                       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayConnectPeer } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_connect_peer";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway connect peer" });

      resource(TransitGatewayConnectPeer, "ci-runner", {
        insideCidrBlocks: ["example"],
        peerAddress: "example",
        transitGatewayAttachmentId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayConnectPeer, TransitGatewayConnectPeerConfig

    def describe():
        ubx.intent("payments own transit gateway connect peer")

        ubx.resource(TransitGatewayConnectPeer, "ci-runner", TransitGatewayConnectPeerConfig(
            inside_cidr_blocks=["example"],
            peer_address="example",
            transit_gateway_attachment_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_connect_peer, with inside cidr
    blocks a set value, peer address example, transit gateway
    attachment id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bgp_asn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="inside_cidr_blocks" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="peer_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bgp_asn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="bgp_peer_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bgp_transit_gateway_addresses" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
