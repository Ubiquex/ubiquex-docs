---
title: "aws_ec2_transit_gateway"
description: "Real, generated bindings for `aws_ec2_transit_gateway`."
---

`aws_ec2_transit_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway"})

    		ubx.Resource(ec2.TransitGateway, "ci-runner", ec2.TransitGatewayConfig{
    			AmazonSideAsn:                   1,
    			AutoAcceptSharedAttachments:     "example",
    			DefaultRouteTableAssociation:    "example",
    			DefaultRouteTablePropagation:    "example",
    			Description:                     "Managed by ubx.",
    			DnsSupport:                      "example",
    			MulticastSupport:                "example",
    			SecurityGroupReferencingSupport: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGateway } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway" });

      resource(TransitGateway, "ci-runner", {
        amazonSideAsn: 1,
        autoAcceptSharedAttachments: "example",
        defaultRouteTableAssociation: "example",
        defaultRouteTablePropagation: "example",
        description: "Managed by ubx.",
        dnsSupport: "example",
        multicastSupport: "example",
        securityGroupReferencingSupport: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGateway, TransitGatewayConfig

    def describe():
        ubx.intent("payments own transit gateway")

        ubx.resource(TransitGateway, "ci-runner", TransitGatewayConfig(
            amazon_side_asn=1,
            auto_accept_shared_attachments="example",
            default_route_table_association="example",
            default_route_table_propagation="example",
            description="Managed by ubx.",
            dns_support="example",
            multicast_support="example",
            security_group_referencing_support="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway, with amazon side asn 1, auto
    accept shared attachments example, default route table
    association example, default route table propagation example,
    description Managed by ubx., dns support example, multicast
    support example, security group referencing support example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="amazon_side_asn" type="number">
  Optional.
</ResponseField>

<ResponseField name="auto_accept_shared_attachments" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_route_table_association" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_route_table_propagation" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="dns_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="encryption_support" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="multicast_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_referencing_support" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_cidr_blocks" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="vpn_ecmp_support" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="association_default_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_support" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="propagation_default_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
