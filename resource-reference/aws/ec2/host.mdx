---
title: "aws_ec2_host"
description: "Real, generated bindings for `aws_ec2_host`."
---

`aws_ec2_host` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host"})

    		ubx.Resource(ec2.Host, "ci-runner", ec2.HostConfig{
    			AvailabilityZone: "example",
    			AutoPlacement:    "example",
    			HostRecovery:     "example",
    			InstanceFamily:   "example",
    			InstanceType:     "example",
    			OutpostArn:       "arn:aws:iam::123456789012:policy/ci",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Host } from "jsr:@ubx/sdk-aws/aws/ec2/host";

    export default stack("payments", () => {
      intent({ summary: "payments own host" });

      resource(Host, "ci-runner", {
        availabilityZone: "example",
        autoPlacement: "example",
        hostRecovery: "example",
        instanceFamily: "example",
        instanceType: "example",
        outpostArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import Host, HostConfig

    def describe():
        ubx.intent("payments own host")

        ubx.resource(Host, "ci-runner", HostConfig(
            availability_zone="example",
            auto_placement="example",
            host_recovery="example",
            instance_family="example",
            instance_type="example",
            outpost_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_host, with availability zone example, auto
    placement example, host recovery example, instance family
    example, instance type example, outpost arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="asset_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="auto_placement" type="string">
  Optional.
</ResponseField>

<ResponseField name="availability_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="host_recovery" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_family" type="string">
  Optional.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="asset_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
