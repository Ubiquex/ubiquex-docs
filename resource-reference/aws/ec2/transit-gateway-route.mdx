---
title: "aws_ec2_transit_gateway_route"
description: "`aws_ec2_transit_gateway_route` defines a real route in an `aws_ec2_transit_gateway`'s route table..."
---

`aws_ec2_transit_gateway_route` defines a real route in an `aws_ec2_transit_gateway`'s route table, directing traffic for a specific CIDR to a chosen attachment, VPC, VPN, Direct Connect, or peering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway route"})

    		ubx.Resource(ec2.TransitGatewayRoute, "ci-runner", ec2.TransitGatewayRouteConfig{
    			DestinationCidrBlock:       "example",
    			TransitGatewayRouteTableId: "example",
    			Blackhole:                  true,
    			TransitGatewayAttachmentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayRoute } from "./local-sdk/aws/sdk/typescript/aws/ec2/transit_gateway_route.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway route" });

      resource(TransitGatewayRoute, "ci-runner", {
        destinationCidrBlock: "example",
        transitGatewayRouteTableId: "example",
        blackhole: true,
        transitGatewayAttachmentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayRoute, TransitGatewayRouteConfig

    def describe():
        ubx.intent("payments own transit gateway route")

        ubx.resource(TransitGatewayRoute, "ci-runner", TransitGatewayRouteConfig(
            destination_cidr_block="example",
            transit_gateway_route_table_id="example",
            blackhole=True,
            transit_gateway_attachment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_route, with destination cidr block
    example, transit gateway route table id example, blackhole true,
    transit gateway attachment id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="blackhole" type="bool">
  Indicates whether to drop traffic that matches this route. Optional.
</ResponseField>

<ResponseField name="destination_cidr_block" type="string" required>
  The CIDR range used for destination matches. Routing decisions are based on the most specific match. Required.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  The ID of transit gateway attachment. Optional.
</ResponseField>

<ResponseField name="transit_gateway_route_table_id" type="string" required>
  The ID of transit gateway route table. Required.
</ResponseField>
