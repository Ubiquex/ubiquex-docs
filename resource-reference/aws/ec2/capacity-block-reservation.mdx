---
title: "aws_ec2_capacity_block_reservation"
description: "Real, generated bindings for `aws_ec2_capacity_block_reservation`."
---

`aws_ec2_capacity_block_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.CapacityBlockReservationConfig{
        CapacityBlockOfferingId: "example",
        InstancePlatform: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ec2.CapacityBlockReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CapacityBlockReservation } from
      "jsr:@ubx/sdk-aws/aws/ec2/capacity_block_reservation";

    resource(CapacityBlockReservation, "example", {
      capacityBlockOfferingId: "example",
      instancePlatform: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        CapacityBlockReservation,
        CapacityBlockReservationConfig,
    )

    cfg = CapacityBlockReservationConfig(
        capacity_block_offering_id="example",
        instance_platform="example",
        tags={"key": "value"},
    )
    ubx.resource(CapacityBlockReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_capacity_block_reservation with
capacity_block_offering_id, instance_platform, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="capacity_block_offering_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_platform" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ebs_optimized" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="end_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="end_date_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="placement_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservation_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="start_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenancy" type="string">
  Computed by AWS after `ship`.
</ResponseField>
