---
title: "aws_ec2_capacity_block_reservation"
description: "Real, generated bindings for `aws_ec2_capacity_block_reservation`."
---

`aws_ec2_capacity_block_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own capacity block reservation"})

    		ubx.Resource(ec2.CapacityBlockReservation, "ci-runner", ec2.CapacityBlockReservationConfig{
    			CapacityBlockOfferingId: "example",
    			InstancePlatform:        "example",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityBlockReservation } from "jsr:@ubx/sdk-aws/aws/ec2/capacity_block_reservation";

    export default stack("payments", () => {
      intent({ summary: "payments own capacity block reservation" });

      resource(CapacityBlockReservation, "ci-runner", {
        capacityBlockOfferingId: "example",
        instancePlatform: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import CapacityBlockReservation, CapacityBlockReservationConfig

    def describe():
        ubx.intent("payments own capacity block reservation")

        ubx.resource(CapacityBlockReservation, "ci-runner", CapacityBlockReservationConfig(
            capacity_block_offering_id="example",
            instance_platform="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_capacity_block_reservation, with capacity block
    offering id example, instance platform example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="capacity_block_offering_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_platform" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ebs_optimized" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="end_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="end_date_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="placement_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservation_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="start_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenancy" type="string">
  Computed by AWS after `ship`.
</ResponseField>
