---
title: "aws_ec2_carrier_gateway"
description: "`aws_ec2_carrier_gateway` connects a VPC in an AWS Wavelength Zone to a real telecommunications carrier's own network..."
---

`aws_ec2_carrier_gateway` connects a VPC in an AWS Wavelength Zone to a real telecommunications carrier's own network, letting traffic route to and from mobile devices on that carrier's 4G/5G network with the low latency Wavelength is built for.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own carrier gateway"})

    		ubx.Resource(ec2.CarrierGateway, "example", ec2.CarrierGatewayConfig{
    			VpcId: "example",
    			Tags:  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CarrierGateway } from "@ubx/sdk-aws/aws/ec2/carrier_gateway";

    export default stack("example", () => {
      intent({ summary: "example own carrier gateway" });

      resource(CarrierGateway, "example", {
        vpcId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import CarrierGateway, CarrierGatewayConfig
    from ubx.aws.ec2.carrier_gateway import CarrierGateway_Tags

    def describe():
        ubx.intent("example own carrier gateway")

        ubx.resource(CarrierGateway, "example", CarrierGatewayConfig(
            vpc_id="example",
            tags=[CarrierGateway_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_carrier_gateway, with vpc id example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag associated with this EC2 Carrier Gateway, used for identifying, organizing, and cost allocation of the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the AWS EC2 Carrier Gateway, used for metadata, organization, and cost tracking alongside its corresponding tag key. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC. Required.
</ResponseField>

## Output properties

<ResponseField name="carrier_gateway_id" type="string">
  The ID of the carrier gateway. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The ID of the owner. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the carrier gateway. Computed by AWS after `ship`.
</ResponseField>
