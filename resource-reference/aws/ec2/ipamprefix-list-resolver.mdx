---
title: "aws_ec2_ipamprefix_list_resolver"
description: "`aws_ec2_ipamprefix_list_resolver` automatically selects real CIDRs out of an `aws_ec2_ipam`'s own advanced-tier address database based on defined..."
---

`aws_ec2_ipamprefix_list_resolver` automatically selects real CIDRs out of an `aws_ec2_ipam`'s own advanced-tier address database based on defined matching rules, and keeps a customer-managed prefix list synchronized with the result, so a prefix list used in a security group or route table stays current with IPAM's own data instead of being updated by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ipamprefix list resolver"})

    		ubx.Resource(ec2.IpamprefixListResolver, "example", ec2.IpamprefixListResolverConfig{
    			AddressFamily: "example",
    			Description:   "Managed by ubx.",
    			IpamId:        "example",
    			Rules:         []map[string]any{{"conditions": []map[string]any{{"cidr": "example"}}}},
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpamprefixListResolver } from "@ubx/sdk-aws/aws/ec2/ipamprefix_list_resolver";

    export default stack("example", () => {
      intent({ summary: "example own ipamprefix list resolver" });

      resource(IpamprefixListResolver, "example", {
        addressFamily: "example",
        description: "Managed by ubx.",
        ipamId: "example",
        rules: [{ conditions: [{ cidr: "example" }] }],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import IpamprefixListResolver, IpamprefixListResolverConfig
    from ubx.aws.ec2.ipamprefix_list_resolver import IpamprefixListResolver_Rules, IpamprefixListResolver_Rules_Conditions, IpamprefixListResolver_Rules_Conditions_ResourceTag

    def describe():
        ubx.intent("example own ipamprefix list resolver")

        ubx.resource(IpamprefixListResolver, "example", IpamprefixListResolverConfig(
            address_family="example",
            description="Managed by ubx.",
            ipam_id="example",
            rules=[IpamprefixListResolver_Rules(conditions=[IpamprefixListResolver_Rules_Conditions(cidr="example")])],
            tags=[IpamprefixListResolver_Rules_Conditions_ResourceTag(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_ipamprefix_list_resolver, with address family
    example, description Managed by ubx., ipam id example, rules a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address_family" type="string" required>
  The address family of the address space in this Prefix List Resolver. Either IPv4 or IPv6. Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="ipam_id" type="string">
  The Id of the IPAM this Prefix List Resolver is a part of. Optional.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  Rules define the business logic for selecting CIDRs from IPAM. Optional.
  <Expandable title="properties">
    <ResponseField name="conditions" type="list(object)">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="cidr" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="ipam_pool_id" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="operation" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          Limits the rule to prefix list entries that are associated with the given AWS resource ID, such as a VPC or subnet. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_owner" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="resource_region" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="resource_tag" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="value" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ipam_scope_id" type="string">
      The ID of the IPAM scope that this resolver rule is associated with, used to determine which IPAM-managed prefix list the rule selects during resolution. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="resource_type" type="string">
      Specifies the type of AWS resource (e.g., VPC, subnet) that a rule matches when resolving resources into the prefix list. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="static_cidr" type="string">
      Specifies the static CIDR block included in a rule entry for the prefix list resolver, defining the IP address range used to match traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="ipam_arn" type="string">
  The Amazon Resource Name (ARN) of the IPAM this Prefix List Resolver is a part of. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_arn" type="string">
  The Amazon Resource Name (ARN) of the IPAM Prefix List Resolver Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_id" type="string">
  Id of the IPAM Prefix List Resolver. Computed by AWS after `ship`.
</ResponseField>
