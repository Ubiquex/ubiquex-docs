---
title: "aws_ec2_serial_console_access"
description: "Real, generated bindings for `aws_ec2_serial_console_access`."
---

`aws_ec2_serial_console_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"

    cfg := ec2.SerialConsoleAccessConfig{
        Enabled: true,
    }
    ubx.Resource(ec2.SerialConsoleAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SerialConsoleAccess } from
      "jsr:@ubx/sdk-aws/aws/ec2/serial_console_access";

    resource(SerialConsoleAccess, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        SerialConsoleAccess,
        SerialConsoleAccessConfig,
    )

    cfg = SerialConsoleAccessConfig(
        enabled=True,
    )
    ubx.resource(SerialConsoleAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_serial_console_access with enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
