---
title: "aws_ec2_ip_pool_route_table_association"
description: "`aws_ec2_ip_pool_route_table_association` associates a real `aws_route_table` with an IPAM pool's own address space..."
---

`aws_ec2_ip_pool_route_table_association` associates a real `aws_route_table` with an IPAM pool's own address space, letting IPAM propagate routes for that pool's CIDRs automatically rather than requiring each route to be added by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ip pool route table association"})

    		ubx.Resource(ec2.IpPoolRouteTableAssociation, "example", ec2.IpPoolRouteTableAssociationConfig{
    			PublicIpv4Pool: "example",
    			RouteTableId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpPoolRouteTableAssociation } from "@ubx/sdk-aws/aws/ec2/ip_pool_route_table_association";

    export default stack("example", () => {
      intent({ summary: "example own ip pool route table association" });

      resource(IpPoolRouteTableAssociation, "example", {
        publicIpv4Pool: "example",
        routeTableId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import IpPoolRouteTableAssociation, IpPoolRouteTableAssociationConfig

    def describe():
        ubx.intent("example own ip pool route table association")

        ubx.resource(IpPoolRouteTableAssociation, "example", IpPoolRouteTableAssociationConfig(
            public_ipv4_pool="example",
            route_table_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_ip_pool_route_table_association, with public ipv4
    pool example, route table id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="public_ipv4_pool" type="string" required>
  The ID of the public IPv4 pool. Required.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  The ID of the route table. Required.
</ResponseField>

## Output properties

<ResponseField name="association_id" type="string">
  The route table association ID. Computed by AWS after `ship`.
</ResponseField>
