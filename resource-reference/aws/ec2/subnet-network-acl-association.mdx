---
title: "aws_ec2_subnet_network_acl_association"
description: "`aws_ec2_subnet_network_acl_association` is the real, actual link between a subnet and the network ACL whose `aws_ec2_network_acl_entry` rules govern..."
---

`aws_ec2_subnet_network_acl_association` is the real, actual link between a subnet and the network ACL whose `aws_ec2_network_acl_entry` rules govern it, a subnet is always associated with exactly one network ACL, this association is what determines which one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subnet network acl association"})

    		ubx.Resource(ec2.SubnetNetworkAclAssociation, "ci-runner", ec2.SubnetNetworkAclAssociationConfig{
    			NetworkAclId: "example",
    			SubnetId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SubnetNetworkAclAssociation } from "./local-sdk/aws/sdk/typescript/aws/ec2/subnet_network_acl_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own subnet network acl association" });

      resource(SubnetNetworkAclAssociation, "ci-runner", {
        networkAclId: "example",
        subnetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import SubnetNetworkAclAssociation, SubnetNetworkAclAssociationConfig

    def describe():
        ubx.intent("payments own subnet network acl association")

        ubx.resource(SubnetNetworkAclAssociation, "ci-runner", SubnetNetworkAclAssociationConfig(
            network_acl_id="example",
            subnet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_subnet_network_acl_association, with network acl
    id example, subnet id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="network_acl_id" type="string" required>
  The ID of the network ACL Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  The ID of the subnet Required.
</ResponseField>

## Output properties

<ResponseField name="association_id" type="string">
  The unique identifier assigned by AWS to the subnet-network ACL association, used to reference the association in AWS APIs and other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
