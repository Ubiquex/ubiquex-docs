---
title: "aws_ec2_transit_gateway_multicast_group_member"
description: "`aws_ec2_transit_gateway_multicast_group_member` registers a real network interface as a receiving member of a multicast group within..."
---

`aws_ec2_transit_gateway_multicast_group_member` registers a real network interface as a receiving member of a multicast group within an `aws_ec2_transit_gateway_multicast_domain`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway multicast group member"})

    		ubx.Resource(ec2.TransitGatewayMulticastGroupMember, "ci-runner", ec2.TransitGatewayMulticastGroupMemberConfig{
    			GroupIpAddress:                  "example",
    			NetworkInterfaceId:              "example",
    			TransitGatewayMulticastDomainId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayMulticastGroupMember } from "./local-sdk/aws/sdk/typescript/aws/ec2/transit_gateway_multicast_group_member.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway multicast group member" });

      resource(TransitGatewayMulticastGroupMember, "ci-runner", {
        groupIpAddress: "example",
        networkInterfaceId: "example",
        transitGatewayMulticastDomainId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayMulticastGroupMember, TransitGatewayMulticastGroupMemberConfig

    def describe():
        ubx.intent("payments own transit gateway multicast group member")

        ubx.resource(TransitGatewayMulticastGroupMember, "ci-runner", TransitGatewayMulticastGroupMemberConfig(
            group_ip_address="example",
            network_interface_id="example",
            transit_gateway_multicast_domain_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_multicast_group_member, with group
    ip address example, network interface id example, transit gateway
    multicast domain id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="group_ip_address" type="string" required>
  The IP address assigned to the transit gateway multicast group. Required.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  The ID of the transit gateway attachment. Required.
</ResponseField>

<ResponseField name="transit_gateway_multicast_domain_id" type="string" required>
  The ID of the transit gateway multicast domain. Required.
</ResponseField>

## Output properties

<ResponseField name="group_member" type="bool">
  Indicates that the resource is a transit gateway multicast group member. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_source" type="bool">
  Indicates that the resource is a transit gateway multicast group member. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_type" type="string">
  The member type (for example, static). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The ID of the resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The type of resource, for example a VPC attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  The ID of the subnet. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  The ID of the transit gateway attachment. Computed by AWS after `ship`.
</ResponseField>
