---
title: "aws_ec2_verified_access_trust_provider"
description: "`aws_ec2_verified_access_trust_provider` supplies the real identity or device-posture signal, an OIDC identity provider or a device-management agent..."
---

`aws_ec2_verified_access_trust_provider` supplies the real identity or device-posture signal, an OIDC identity provider or a device-management agent, that Verified Access evaluates a request against, the actual source of trust its policies are written in terms of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own verified access trust provider"})

    		ubx.Resource(ec2.VerifiedAccessTrustProvider, "example", ec2.VerifiedAccessTrustProviderConfig{
    			PolicyReferenceName:          "example-policy-reference-name",
    			TrustProviderType:            "example",
    			Description:                  "Managed by ubx.",
    			DeviceOptions:                map[string]any{"public_signing_key_url": "example"},
    			DeviceTrustProviderType:      "example",
    			NativeApplicationOidcOptions: map[string]any{"authorization_endpoint": "example"},
    			OidcOptions:                  map[string]any{"authorization_endpoint": "example"},
    			SseSpecification:             map[string]any{"customer_managed_key_enabled": true},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { VerifiedAccessTrustProvider } from "./local-sdk/aws/sdk/typescript/aws/ec2/verified_access_trust_provider.ts";

    export default stack("example", () => {
      intent({ summary: "example own verified access trust provider" });

      resource(VerifiedAccessTrustProvider, "example", {
        policyReferenceName: "example-policy-reference-name",
        trustProviderType: "example",
        description: "Managed by ubx.",
        deviceOptions: { publicSigningKeyUrl: "example" },
        deviceTrustProviderType: "example",
        nativeApplicationOidcOptions: { authorizationEndpoint: "example" },
        oidcOptions: { authorizationEndpoint: "example" },
        sseSpecification: { customerManagedKeyEnabled: true },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VerifiedAccessTrustProvider, VerifiedAccessTrustProviderConfig

    def describe():
        ubx.intent("example own verified access trust provider")

        ubx.resource(VerifiedAccessTrustProvider, "example", VerifiedAccessTrustProviderConfig(
            policy_reference_name="example-policy-reference-name",
            trust_provider_type="example",
            description="Managed by ubx.",
            device_options={"public_signing_key_url": "example"},
            device_trust_provider_type="example",
            native_application_oidc_options={"authorization_endpoint": "example"},
            oidc_options={"authorization_endpoint": "example"},
            sse_specification={"customer_managed_key_enabled": True},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_verified_access_trust_provider, with policy
    reference name example-policy-reference-name, trust provider type
    example, description Managed by ubx., device options a set value,
    device trust provider type example, native application oidc
    options a set value, oidc options a set value, sse specification
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description for the Amazon Web Services Verified Access trust provider. Optional.
</ResponseField>

<ResponseField name="device_options" type="object">
  The options for device identity based trust providers. Optional.
  <Expandable title="properties">
    <ResponseField name="public_signing_key_url" type="string">
      URL Verified Access will use to verify authenticity of the device tokens. Optional.
    </ResponseField>
    <ResponseField name="tenant_id" type="string">
      The ID of the tenant application with the device-identity provider. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="device_trust_provider_type" type="string">
  The type of device-based trust provider. Possible values: jamf|crowdstrike Optional.
</ResponseField>

<ResponseField name="native_application_oidc_options" type="object">
  The OpenID Connect details for an oidc -type, user-identity based trust provider for L4. Optional.
  <Expandable title="properties">
    <ResponseField name="authorization_endpoint" type="string">
      The OIDC authorization endpoint. Optional.
    </ResponseField>
    <ResponseField name="client_id" type="string">
      The client identifier. Optional.
    </ResponseField>
    <ResponseField name="client_secret" type="string">
      The client secret. Optional.
    </ResponseField>
    <ResponseField name="issuer" type="string">
      The OIDC issuer. Optional.
    </ResponseField>
    <ResponseField name="public_signing_key_endpoint" type="string">
      The public signing key for endpoint Optional.
    </ResponseField>
    <ResponseField name="scope" type="string">
      OpenID Connect (OIDC) scopes are used by an application during authentication to authorize access to details of a user. Each scope returns a specific set of user attributes. Optional.
    </ResponseField>
    <ResponseField name="token_endpoint" type="string">
      The OIDC token endpoint. Optional.
    </ResponseField>
    <ResponseField name="user_info_endpoint" type="string">
      The OIDC user info endpoint. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="oidc_options" type="object">
  The OpenID Connect details for an oidc -type, user-identity based trust provider. Optional.
  <Expandable title="properties">
    <ResponseField name="authorization_endpoint" type="string">
      The OIDC authorization endpoint. Optional.
    </ResponseField>
    <ResponseField name="client_id" type="string">
      The client identifier. Optional.
    </ResponseField>
    <ResponseField name="client_secret" type="string">
      The client secret. Optional.
    </ResponseField>
    <ResponseField name="issuer" type="string">
      The OIDC issuer. Optional.
    </ResponseField>
    <ResponseField name="scope" type="string">
      OpenID Connect (OIDC) scopes are used by an application during authentication to authorize access to details of a user. Each scope returns a specific set of user attributes. Optional.
    </ResponseField>
    <ResponseField name="token_endpoint" type="string">
      The OIDC token endpoint. Optional.
    </ResponseField>
    <ResponseField name="user_info_endpoint" type="string">
      The OIDC user info endpoint. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_reference_name" type="string" required>
  The identifier to be used when working with policy rules. Required.
</ResponseField>

<ResponseField name="sse_specification" type="object">
  The configuration options for customer provided KMS encryption. Optional.
  <Expandable title="properties">
    <ResponseField name="customer_managed_key_enabled" type="bool">
      Whether to encrypt the policy with the provided key or disable encryption Optional.
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
      KMS Key Arn used to encrypt the group policy Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the Verified Access trust provider, used to organize, identify, and manage the resource through AWS tagging. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trust_provider_type" type="string" required>
  Type of trust provider. Possible values: user|device Required.
</ResponseField>

<ResponseField name="user_trust_provider_type" type="string">
  The type of device-based trust provider. Possible values: oidc|iam-identity-center Optional.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The creation time. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The last updated time. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="verified_access_trust_provider_id" type="string">
  The ID of the Amazon Web Services Verified Access trust provider. Computed by AWS after `ship`.
</ResponseField>
