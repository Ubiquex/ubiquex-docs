---
title: "aws_ec2_transit_gateway_multicast_domain"
description: "`aws_ec2_transit_gateway_multicast_domain` enables real multicast traffic to be routed between VPC attachments on an `aws_ec2_transit_gateway`..."
---

`aws_ec2_transit_gateway_multicast_domain` enables real multicast traffic to be routed between VPC attachments on an `aws_ec2_transit_gateway`, a capability a transit gateway's own standard unicast routing doesn't provide.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own transit gateway multicast domain"})

    		ubx.Resource(ec2.TransitGatewayMulticastDomain, "example", ec2.TransitGatewayMulticastDomainConfig{
    			TransitGatewayId: "example",
    			Options:          map[string]any{"auto_accept_shared_associations": "example"},
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayMulticastDomain } from "@ubx/sdk-aws/aws/ec2/transit_gateway_multicast_domain";

    export default stack("example", () => {
      intent({ summary: "example own transit gateway multicast domain" });

      resource(TransitGatewayMulticastDomain, "example", {
        transitGatewayId: "example",
        options: { autoAcceptSharedAssociations: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2.transit_gateway_multicast_domain import TransitGatewayMulticastDomain, TransitGatewayMulticastDomainConfig

    def describe():
        ubx.intent("example own transit gateway multicast domain")

        ubx.resource(TransitGatewayMulticastDomain, "example", TransitGatewayMulticastDomainConfig(
            transit_gateway_id="example",
            options={"auto_accept_shared_associations": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_multicast_domain, with transit
    gateway id example, options a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="options" type="object">
  The options for the transit gateway multicast domain. Optional.
  <Expandable title="properties">
    <ResponseField name="auto_accept_shared_associations" type="string">
      Indicates whether to automatically cross-account subnet associations that are associated with the transit gateway multicast domain. Valid Values: enable | disable Optional.
    </ResponseField>
    <ResponseField name="igmpv2_support" type="string">
      Indicates whether Internet Group Management Protocol (IGMP) version 2 is turned on for the transit gateway multicast domain. Valid Values: enable | disable Optional.
    </ResponseField>
    <ResponseField name="static_sources_support" type="string">
      Indicates whether support for statically configuring transit gateway multicast group sources is turned on. Valid Values: enable | disable Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for the transit gateway multicast domain. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair attached to the transit gateway multicast domain, used to assign arbitrary metadata for identification and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  The ID of the transit gateway. Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time the transit gateway multicast domain was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the transit gateway multicast domain. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_multicast_domain_arn" type="string">
  The Amazon Resource Name (ARN) of the transit gateway multicast domain. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_multicast_domain_id" type="string">
  The ID of the transit gateway multicast domain. Computed by AWS after `ship`.
</ResponseField>
