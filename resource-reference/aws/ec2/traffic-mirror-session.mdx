---
title: "aws_ec2_traffic_mirror_session"
description: "Real, generated bindings for `aws_ec2_traffic_mirror_session`."
---

`aws_ec2_traffic_mirror_session` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ec2"

    cfg := ec2.TrafficMirrorSessionConfig{
        NetworkInterfaceId: "example",
        SessionNumber: 1,
        TrafficMirrorFilterId: "example",
        TrafficMirrorTargetId: "example",
        Description: "example",
        PacketLength: 1,
    }
    ubx.Resource(ec2.TrafficMirrorSession, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TrafficMirrorSession } from
      "jsr:@ubx/sdk-aws/aws/ec2/traffic_mirror_session";

    resource(TrafficMirrorSession, "example", {
      networkInterfaceId: "example",
      sessionNumber: 1,
      trafficMirrorFilterId: "example",
      trafficMirrorTargetId: "example",
      description: "example",
      packetLength: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ec2 import (
        TrafficMirrorSession,
        TrafficMirrorSessionConfig,
    )

    cfg = TrafficMirrorSessionConfig(
        network_interface_id="example",
        session_number=1,
        traffic_mirror_filter_id="example",
        traffic_mirror_target_id="example",
        description="example",
        packet_length=1,
    )
    ubx.resource(TrafficMirrorSession, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ec2_traffic_mirror_session with
network_interface_id, session_number, traffic_mirror_filter_id,
traffic_mirror_target_id, description, packet_length.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="packet_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_number" type="number" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="traffic_mirror_filter_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="traffic_mirror_target_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="virtual_network_id" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="packet_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_network_id" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
