---
title: "aws_ec2_traffic_mirror_session"
description: "Real, generated bindings for `aws_ec2_traffic_mirror_session`."
---

`aws_ec2_traffic_mirror_session` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic mirror session"})

    		ubx.Resource(ec2.TrafficMirrorSession, "ci-runner", ec2.TrafficMirrorSessionConfig{
    			NetworkInterfaceId:    "example",
    			SessionNumber:         1,
    			TrafficMirrorFilterId: "example",
    			TrafficMirrorTargetId: "example",
    			Description:           "Managed by ubx.",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficMirrorSession } from "jsr:@ubx/sdk-aws/aws/ec2/traffic_mirror_session";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic mirror session" });

      resource(TrafficMirrorSession, "ci-runner", {
        networkInterfaceId: "example",
        sessionNumber: 1,
        trafficMirrorFilterId: "example",
        trafficMirrorTargetId: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TrafficMirrorSession, TrafficMirrorSessionConfig

    def describe():
        ubx.intent("payments own traffic mirror session")

        ubx.resource(TrafficMirrorSession, "ci-runner", TrafficMirrorSessionConfig(
            network_interface_id="example",
            session_number=1,
            traffic_mirror_filter_id="example",
            traffic_mirror_target_id="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_traffic_mirror_session, with network interface id
    example, session number 1, traffic mirror filter id example,
    traffic mirror target id example, description Managed by ubx.,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="packet_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_number" type="number" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="traffic_mirror_filter_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="traffic_mirror_target_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="virtual_network_id" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="packet_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_network_id" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
