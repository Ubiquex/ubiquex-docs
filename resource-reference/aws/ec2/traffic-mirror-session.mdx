---
title: "aws_ec2_traffic_mirror_session"
description: "`aws_ec2_traffic_mirror_session` is the real, actual mirroring in progress..."
---

`aws_ec2_traffic_mirror_session` is the real, actual mirroring in progress, pairing a source network interface with an `aws_ec2_traffic_mirror_filter` and an `aws_ec2_traffic_mirror_target` the filtered traffic actually gets sent to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own traffic mirror session"})

    		ubx.Resource(ec2.TrafficMirrorSession, "example", ec2.TrafficMirrorSessionConfig{
    			NetworkInterfaceId:    "example",
    			SessionNumber:         1,
    			TrafficMirrorFilterId: "example",
    			TrafficMirrorTargetId: "example",
    			Description:           "Managed by ubx.",
    			OwnerId:               "example",
    			PacketLength:          1,
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficMirrorSession } from "@ubx/sdk-aws/aws/ec2/traffic_mirror_session";

    export default stack("example", () => {
      intent({ summary: "example own traffic mirror session" });

      resource(TrafficMirrorSession, "example", {
        networkInterfaceId: "example",
        sessionNumber: 1,
        trafficMirrorFilterId: "example",
        trafficMirrorTargetId: "example",
        description: "Managed by ubx.",
        ownerId: "example",
        packetLength: 1,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TrafficMirrorSession, TrafficMirrorSessionConfig
    from ubx.aws.ec2.traffic_mirror_session import TrafficMirrorSession_Tags

    def describe():
        ubx.intent("example own traffic mirror session")

        ubx.resource(TrafficMirrorSession, "example", TrafficMirrorSessionConfig(
            network_interface_id="example",
            session_number=1,
            traffic_mirror_filter_id="example",
            traffic_mirror_target_id="example",
            description="Managed by ubx.",
            owner_id="example",
            packet_length=1,
            tags=[TrafficMirrorSession_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_traffic_mirror_session, with network interface id
    example, session number 1, traffic mirror filter id example,
    traffic mirror target id example, description Managed by ubx.,
    owner id example, packet length 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the Traffic Mirror session. Optional.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  The ID of the source network interface. Required.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The ID of the account that owns the Traffic Mirror session. Optional.
</ResponseField>

<ResponseField name="packet_length" type="number">
  The number of bytes in each packet to mirror. Optional.
</ResponseField>

<ResponseField name="session_number" type="number" required>
  The session number determines the order in which sessions are evaluated when an interface is used by multiple sessions. The first session with a matching filter is the one that mirrors the packets. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags assigned to the Traffic Mirror session. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag key attached to the EC2 Traffic Mirror Session, which you can use to organize, identify, and manage the resource within your AWS environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="traffic_mirror_filter_id" type="string" required>
  The ID of a Traffic Mirror filter. Required.
</ResponseField>

<ResponseField name="traffic_mirror_target_id" type="string" required>
  The ID of a Traffic Mirror target. Required.
</ResponseField>

<ResponseField name="virtual_network_id" type="number">
  The VXLAN ID for the Traffic Mirror session. Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The ID of a Traffic Mirror session. Computed by AWS after `ship`.
</ResponseField>
