---
title: "aws_ec2_ipamprefix_list_resolver_target"
description: "`aws_ec2_ipamprefix_list_resolver_target` is the real, actual association between a specific customer-managed prefix list and..."
---

`aws_ec2_ipamprefix_list_resolver_target` is the real, actual association between a specific customer-managed prefix list and an `aws_ec2_ipamprefix_list_resolver`, the resolver's selected CIDRs have nowhere to actually be synchronized to without one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ipamprefix list resolver target"})

    		ubx.Resource(ec2.IpamprefixListResolverTarget, "ci-runner", ec2.IpamprefixListResolverTargetConfig{
    			IpamPrefixListResolverId: "example",
    			PrefixListId:             "example",
    			PrefixListRegion:         "example",
    			TrackLatestVersion:       true,
    			DesiredVersion:           1,
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpamprefixListResolverTarget } from "./local-sdk/aws/sdk/typescript/aws/ec2/ipamprefix_list_resolver_target.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own ipamprefix list resolver target" });

      resource(IpamprefixListResolverTarget, "ci-runner", {
        ipamPrefixListResolverId: "example",
        prefixListId: "example",
        prefixListRegion: "example",
        trackLatestVersion: true,
        desiredVersion: 1,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ec2 import IpamprefixListResolverTarget, IpamprefixListResolverTargetConfig

    def describe():
        ubx.intent("payments own ipamprefix list resolver target")

        ubx.resource(IpamprefixListResolverTarget, "ci-runner", IpamprefixListResolverTargetConfig(
            ipam_prefix_list_resolver_id="example",
            prefix_list_id="example",
            prefix_list_region="example",
            track_latest_version=True,
            desired_version=1,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_ipamprefix_list_resolver_target, with ipam prefix
    list resolver id example, prefix list id example, prefix list
    region example, track latest version true, desired version 1,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="desired_version" type="number">
  The desired version of the Prefix List Resolver that this Target should synchronize with. Optional.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_id" type="string" required>
  The Id of the IPAM Prefix List Resolver associated with this Target. Required.
</ResponseField>

<ResponseField name="prefix_list_id" type="string" required>
  The Id of the Managed Prefix List. Required.
</ResponseField>

<ResponseField name="prefix_list_region" type="string" required>
  The region that the Managed Prefix List is located in. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag assigned to the IPAM prefix list resolver target, used for organizing and managing the resource in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="track_latest_version" type="bool" required>
  Indicates whether this Target automatically tracks the latest version of the Prefix List Resolver. Required.
</ResponseField>

## Output properties

<ResponseField name="ipam_prefix_list_resolver_target_arn" type="string">
  Id of the IPAM Prefix List Resolver Target. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_target_id" type="string">
  Id of the IPAM Prefix List Resolver Target. Computed by AWS after `ship`.
</ResponseField>
