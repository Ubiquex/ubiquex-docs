---
title: "aws_ec2_verified_access_endpoint"
description: "`aws_ec2_verified_access_endpoint` is the real, actual application Verified Access provides access to, an IP address, load balancer..."
---

`aws_ec2_verified_access_endpoint` is the real, actual application Verified Access provides access to, an IP address, load balancer, or network interface, evaluated against its `aws_ec2_verified_access_group`'s policy on every request before traffic is allowed through.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own verified access endpoint"})

    		ubx.Resource(ec2.VerifiedAccessEndpoint, "example", ec2.VerifiedAccessEndpointConfig{
    			AttachmentType:        "example",
    			EndpointType:          "example",
    			VerifiedAccessGroupId: "example",
    			ApplicationDomain:     "example",
    			CidrOptions:           map[string]any{"cidr": "example"},
    			Description:           "Managed by ubx.",
    			DomainCertificateArn:  "arn:aws:iam::123456789012:policy/example",
    			EndpointDomainPrefix:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VerifiedAccessEndpoint } from "@ubx/sdk-aws/aws/ec2/verified_access_endpoint";

    export default stack("example", () => {
      intent({ summary: "example own verified access endpoint" });

      resource(VerifiedAccessEndpoint, "example", {
        attachmentType: "example",
        endpointType: "example",
        verifiedAccessGroupId: "example",
        applicationDomain: "example",
        cidrOptions: { cidr: "example" },
        description: "Managed by ubx.",
        domainCertificateArn: "arn:aws:iam::123456789012:policy/example",
        endpointDomainPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import VerifiedAccessEndpoint, VerifiedAccessEndpointConfig
    from ubx.aws.ec2.verified_access_endpoint import VerifiedAccessEndpoint_CidrOptions

    def describe():
        ubx.intent("example own verified access endpoint")

        ubx.resource(VerifiedAccessEndpoint, "example", VerifiedAccessEndpointConfig(
            attachment_type="example",
            endpoint_type="example",
            verified_access_group_id="example",
            application_domain="example",
            cidr_options=VerifiedAccessEndpoint_CidrOptions(cidr="example"),
            description="Managed by ubx.",
            domain_certificate_arn="arn:aws:iam::123456789012:policy/example",
            endpoint_domain_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_verified_access_endpoint, with attachment type
    example, endpoint type example, verified access group id example,
    application domain example, cidr options a set value, description
    Managed by ubx., domain certificate arn
    arn:aws:iam::123456789012:policy/example, endpoint domain prefix
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_domain" type="string">
  The DNS name for users to reach your application. Optional.
</ResponseField>

<ResponseField name="attachment_type" type="string" required>
  The type of attachment used to provide connectivity between the AWS Verified Access endpoint and the application. Required.
</ResponseField>

<ResponseField name="cidr_options" type="object">
  The options for cidr type endpoint. Optional.
  <Expandable title="properties">
    <ResponseField name="cidr" type="string">
      The IP address range, in CIDR notation. Optional.
    </ResponseField>
    <ResponseField name="port_ranges" type="list(object)">
      The list of port range. Optional.
      <Expandable title="properties">
        <ResponseField name="from_port" type="number">
          Defines the starting port in a port range that the Verified Access endpoint accepts traffic on for CIDR-based access. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="to_port" type="number">
          The inclusive upper bound of the port range that the Verified Access endpoint will accept for traffic coming from the specified CIDR. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The IP protocol. Optional.
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      The IDs of the subnets. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description for the AWS Verified Access endpoint. Optional.
</ResponseField>

<ResponseField name="domain_certificate_arn" type="string">
  The ARN of a public TLS/SSL certificate imported into or created with ACM. Optional.
</ResponseField>

<ResponseField name="endpoint_domain_prefix" type="string">
  A custom identifier that gets prepended to a DNS name that is generated for the endpoint. Optional.
</ResponseField>

<ResponseField name="endpoint_type" type="string" required>
  The type of AWS Verified Access endpoint. Incoming application requests will be sent to an IP address, load balancer or a network interface depending on the endpoint type specified.The type of AWS Verified Access endpoint. Incoming application requests will be sent to an IP address, load balancer or a network interface depending on the endpoint type specified. Required.
</ResponseField>

<ResponseField name="load_balancer_options" type="object">
  The load balancer details if creating the AWS Verified Access endpoint as load-balancertype. Optional.
  <Expandable title="properties">
    <ResponseField name="load_balancer_arn" type="string">
      The ARN of the load balancer. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      The IP port number. Optional.
    </ResponseField>
    <ResponseField name="port_ranges" type="list(object)">
      The list of port range. Optional.
      <Expandable title="properties">
        <ResponseField name="from_port" type="number">
          The inclusive first port in a range of ports that the load balancer will listen on and forward to the Verified Access endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="to_port" type="number">
          The upper bound of the port range that the Verified Access endpoint uses for routing traffic to the load balancer. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The IP protocol. Optional.
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      The IDs of the subnets. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_interface_options" type="object">
  The options for network-interface type endpoint. Optional.
  <Expandable title="properties">
    <ResponseField name="network_interface_id" type="string">
      The ID of the network interface. Optional.
    </ResponseField>
    <ResponseField name="port" type="number">
      The IP port number. Optional.
    </ResponseField>
    <ResponseField name="port_ranges" type="list(object)">
      The list of port ranges. Optional.
      <Expandable title="properties">
        <ResponseField name="from_port" type="number">
          Specifies the inclusive starting port number in a port range that the Verified Access endpoint's network interface accepts traffic on. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="to_port" type="number">
          The upper bound of a port range that the Verified Access endpoint's network interface uses for forwarding traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The IP protocol. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_document" type="string">
  The AWS Verified Access policy document. Optional.
</ResponseField>

<ResponseField name="policy_enabled" type="bool">
  The status of the Verified Access policy. Optional.
</ResponseField>

<ResponseField name="rds_options" type="object">
  The options for rds type endpoint. Optional.
  <Expandable title="properties">
    <ResponseField name="port" type="number">
      The IP port number. Optional.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The IP protocol. Optional.
    </ResponseField>
    <ResponseField name="rds_db_cluster_arn" type="string">
      The ARN of the RDS DB cluster. Optional.
    </ResponseField>
    <ResponseField name="rds_db_instance_arn" type="string">
      The ARN of the RDS DB instance. Optional.
    </ResponseField>
    <ResponseField name="rds_db_proxy_arn" type="string">
      The ARN of the RDS DB proxy. Optional.
    </ResponseField>
    <ResponseField name="rds_endpoint" type="string">
      The RDS endpoint. Optional.
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      The IDs of the subnets. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  The IDs of the security groups for the endpoint. Optional.
</ResponseField>

<ResponseField name="sse_specification" type="object">
  The configuration options for customer provided KMS encryption. Optional.
  <Expandable title="properties">
    <ResponseField name="customer_managed_key_enabled" type="bool">
      Whether to encrypt the policy with the provided key or disable encryption Optional.
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
      KMS Key Arn used to encrypt the group policy Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="verified_access_group_id" type="string" required>
  The ID of the AWS Verified Access group. Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The creation time. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="device_validation_domain" type="string">
  Returned if endpoint has a device trust provider attached. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_domain" type="string">
  A DNS name that is generated for the endpoint. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The last updated time. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The endpoint status. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="verified_access_endpoint_id" type="string">
  The ID of the AWS Verified Access endpoint. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="verified_access_instance_id" type="string">
  The ID of the AWS Verified Access instance. Computed by AWS after `ship`.
</ResponseField>
