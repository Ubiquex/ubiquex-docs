---
title: "aws_medialive_multiplex"
description: "Real, generated bindings for `aws_medialive_multiplex`."
---

`aws_medialive_multiplex` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/medialive"

    cfg := medialive.MultiplexConfig{
        AvailabilityZones: []string{"example"},
        Name: "example-name",
        StartMultiplex: true,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(medialive.Multiplex, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Multiplex } from
      "jsr:@ubx/sdk-aws/aws/medialive/multiplex";

    resource(Multiplex, "example", {
      availabilityZones: ["example"],
      name: "example",
      startMultiplex: true,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.medialive import Multiplex, MultiplexConfig

    cfg = MultiplexConfig(
        availability_zones=["example"],
        name="example",
        start_multiplex=True,
        tags={"key": "value"},
    )
    ubx.resource(Multiplex, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_medialive_multiplex with availability_zones, name,
start_multiplex, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zones" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_multiplex" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
