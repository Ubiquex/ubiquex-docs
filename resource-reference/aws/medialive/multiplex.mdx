---
title: "aws_medialive_multiplex"
description: "Real, generated bindings for `aws_medialive_multiplex`."
---

`aws_medialive_multiplex` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own multiplex"})

    		ubx.Resource(medialive.Multiplex, "ci-runner", medialive.MultiplexConfig{
    			AvailabilityZones: []string{"example"},
    			Name:              "ci-runner",
    			StartMultiplex:    true,
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Multiplex } from "jsr:@ubx/sdk-aws/aws/medialive/multiplex";

    export default stack("payments", () => {
      intent({ summary: "payments own multiplex" });

      resource(Multiplex, "ci-runner", {
        availabilityZones: ["example"],
        name: "ci-runner",
        startMultiplex: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.medialive import Multiplex, MultiplexConfig

    def describe():
        ubx.intent("payments own multiplex")

        ubx.resource(Multiplex, "ci-runner", MultiplexConfig(
            availability_zones=["example"],
            name="ci-runner",
            start_multiplex=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_medialive_multiplex called "ci-runner", with
    availability zones a set value, start multiplex true, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zones" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_multiplex" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
