---
title: "aws_medialive_multiplex_program"
description: "Real, generated bindings for `aws_medialive_multiplex_program`."
---

`aws_medialive_multiplex_program` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/medialive"

    cfg := medialive.MultiplexProgramConfig{
        MultiplexId: "example",
        ProgramName: "example-program-name",
    }
    ubx.Resource(medialive.MultiplexProgram, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MultiplexProgram } from
      "jsr:@ubx/sdk-aws/aws/medialive/multiplex_program";

    resource(MultiplexProgram, "example", {
      multiplexId: "example",
      programName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.medialive import (
        MultiplexProgram,
        MultiplexProgramConfig,
    )

    cfg = MultiplexProgramConfig(
        multiplex_id="example",
        program_name="example",
    )
    ubx.resource(MultiplexProgram, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_medialive_multiplex_program with multiplex_id,
program_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="multiplex_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="program_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
