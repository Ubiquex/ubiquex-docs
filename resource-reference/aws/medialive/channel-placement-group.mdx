---
title: "aws_media_live_channel_placement_group"
description: "`aws_media_live_channel_placement_group` organizes a real group of `aws_media_live_channel`s within an `aws_media_live_cluster`..."
---

`aws_media_live_channel_placement_group` organizes a real group of `aws_media_live_channel`s within an `aws_media_live_cluster`, so channels running on the same on-premises or Outposts infrastructure can be managed and allocated together.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own live channel placement group"})

    		ubx.Resource(medialive.ChannelPlacementGroup, "example", medialive.ChannelPlacementGroupConfig{
    			Name:      "example",
    			ClusterId: "example",
    			Nodes:     []string{"example"},
    			Tags:      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ChannelPlacementGroup } from "@ubx/sdk-aws/aws/medialive/channel_placement_group";

    export default stack("example", () => {
      intent({ summary: "example own live channel placement group" });

      resource(ChannelPlacementGroup, "example", {
        name: "example",
        clusterId: "example",
        nodes: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.medialive.channel_placement_group import ChannelPlacementGroup, ChannelPlacementGroupConfig

    def describe():
        ubx.intent("example own live channel placement group")

        ubx.resource(ChannelPlacementGroup, "example", ChannelPlacementGroupConfig(
            name="example",
            cluster_id="example",
            nodes=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_channel_placement_group called "example",
    with cluster id example, nodes a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_id" type="string">
  The ID of the cluster the node is on. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the channel placement group. Optional.
</ResponseField>

<ResponseField name="nodes" type="list(string)">
  List of nodes added to the channel placement group Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A collection of key-value pairs. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag key attached to the AWS MediaLive channel placement group, used for resource identification, organization, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the channel placement group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="channels" type="list(string)">
  List of channel IDs added to the channel placement group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Unique internal identifier. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the ChannelPlacementGroupState Computed by AWS after `ship`.
</ResponseField>
