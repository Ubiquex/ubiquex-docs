---
title: "aws_customerprofiles_domain"
description: "Real, generated bindings for `aws_customerprofiles_domain`."
---

`aws_customerprofiles_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/customerprofiles"

    cfg := customerprofiles.DomainConfig{
        DefaultExpirationDays: 1,
        DomainName: "example-domain-name",
        DeadLetterQueueUrl: "example",
        DefaultEncryptionKey: "example",
    }
    ubx.Resource(customerprofiles.Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Domain } from
      "jsr:@ubx/sdk-aws/aws/customerprofiles/domain";

    resource(Domain, "example", {
      defaultExpirationDays: 1,
      domainName: "example",
      deadLetterQueueUrl: "example",
      defaultEncryptionKey: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.customerprofiles import Domain, DomainConfig

    cfg = DomainConfig(
        default_expiration_days=1,
        domain_name="example",
        dead_letter_queue_url="example",
        default_encryption_key="example",
    )
    ubx.resource(Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_customerprofiles_domain with
default_expiration_days, domain_name, dead_letter_queue_url,
default_encryption_key.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="dead_letter_queue_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_encryption_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_expiration_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
