---
title: "aws_customerprofiles_domain"
description: "Real, generated bindings for `aws_customerprofiles_domain`."
---

`aws_customerprofiles_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	customerprofiles "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/customerprofiles"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain"})

    		ubx.Resource(customerprofiles.Domain, "ci-runner", customerprofiles.DomainConfig{
    			DefaultExpirationDays: 1,
    			DomainName:            "example-domain-name",
    			DeadLetterQueueUrl:    "example",
    			DefaultEncryptionKey:  "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Domain } from "jsr:@ubx/sdk-aws/aws/customerprofiles/domain";

    export default stack("payments", () => {
      intent({ summary: "payments own domain" });

      resource(Domain, "ci-runner", {
        defaultExpirationDays: 1,
        domainName: "example-domain-name",
        deadLetterQueueUrl: "example",
        defaultEncryptionKey: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.customerprofiles import Domain, DomainConfig

    def describe():
        ubx.intent("payments own domain")

        ubx.resource(Domain, "ci-runner", DomainConfig(
            default_expiration_days=1,
            domain_name="example-domain-name",
            dead_letter_queue_url="example",
            default_encryption_key="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_customerprofiles_domain, with default expiration days
    1, domain name example-domain-name, dead letter queue url
    example, default encryption key example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="dead_letter_queue_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_encryption_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_expiration_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
