---
title: "aws_sesv2_email_identity"
description: "Real, generated bindings for `aws_sesv2_email_identity`."
---

`aws_sesv2_email_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own email identity"})

    		ubx.Resource(sesv2.EmailIdentity, "ci-runner", sesv2.EmailIdentityConfig{
    			EmailIdentity:        "example",
    			ConfigurationSetName: "example-configuration-set-name",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EmailIdentity } from "jsr:@ubx/sdk-aws/aws/sesv2/email_identity";

    export default stack("payments", () => {
      intent({ summary: "payments own email identity" });

      resource(EmailIdentity, "ci-runner", {
        emailIdentity: "example",
        configurationSetName: "example-configuration-set-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sesv2 import EmailIdentity, EmailIdentityConfig

    def describe():
        ubx.intent("payments own email identity")

        ubx.resource(EmailIdentity, "ci-runner", EmailIdentityConfig(
            email_identity="example",
            configuration_set_name="example-configuration-set-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sesv2_email_identity, with email identity example,
    configuration set name example-configuration-set-name, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="configuration_set_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="email_identity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verification_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="verified_for_sending_status" type="bool">
  Computed by AWS after `ship`.
</ResponseField>
