---
title: "aws_sesv2_account_suppression_attributes"
description: "Real, generated bindings for `aws_sesv2_account_suppression_attributes`."
---

`aws_sesv2_account_suppression_attributes` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account suppression attributes"})

    		ubx.Resource(sesv2.AccountSuppressionAttributes, "ci-runner", sesv2.AccountSuppressionAttributesConfig{
    			SuppressedReasons: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountSuppressionAttributes } from "jsr:@ubx/sdk-aws/aws/sesv2/account_suppression_attributes";

    export default stack("payments", () => {
      intent({ summary: "payments own account suppression attributes" });

      resource(AccountSuppressionAttributes, "ci-runner", {
        suppressedReasons: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sesv2 import AccountSuppressionAttributes, AccountSuppressionAttributesConfig

    def describe():
        ubx.intent("payments own account suppression attributes")

        ubx.resource(AccountSuppressionAttributes, "ci-runner", AccountSuppressionAttributesConfig(
            suppressed_reasons=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sesv2_account_suppression_attributes, with suppressed
    reasons a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="suppressed_reasons" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
