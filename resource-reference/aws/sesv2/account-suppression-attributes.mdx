---
title: "aws_sesv2_account_suppression_attributes"
description: "Real, generated bindings for `aws_sesv2_account_suppression_attributes`."
---

`aws_sesv2_account_suppression_attributes` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"

    cfg := sesv2.AccountSuppressionAttributesConfig{
        SuppressedReasons: []string{"example"},
    }
    ubx.Resource(sesv2.AccountSuppressionAttributes, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountSuppressionAttributes } from
      "jsr:@ubx/sdk-aws/aws/sesv2/account_suppression_attributes";

    resource(AccountSuppressionAttributes, "example", {
      suppressedReasons: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sesv2 import (
        AccountSuppressionAttributes,
        AccountSuppressionAttributesConfig,
    )

    cfg = AccountSuppressionAttributesConfig(
        suppressed_reasons=["example"],
    )
    ubx.resource(AccountSuppressionAttributes, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sesv2_account_suppression_attributes with
suppressed_reasons.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="suppressed_reasons" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
