---
title: "aws_sesv2_dedicated_ip_assignment"
description: "Real, generated bindings for `aws_sesv2_dedicated_ip_assignment`."
---

`aws_sesv2_dedicated_ip_assignment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dedicated ip assignment"})

    		ubx.Resource(sesv2.DedicatedIpAssignment, "ci-runner", sesv2.DedicatedIpAssignmentConfig{
    			DestinationPoolName: "example-destination-pool-name",
    			Ip:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DedicatedIpAssignment } from "jsr:@ubx/sdk-aws/aws/sesv2/dedicated_ip_assignment";

    export default stack("payments", () => {
      intent({ summary: "payments own dedicated ip assignment" });

      resource(DedicatedIpAssignment, "ci-runner", {
        destinationPoolName: "example-destination-pool-name",
        ip: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sesv2 import DedicatedIpAssignment, DedicatedIpAssignmentConfig

    def describe():
        ubx.intent("payments own dedicated ip assignment")

        ubx.resource(DedicatedIpAssignment, "ci-runner", DedicatedIpAssignmentConfig(
            destination_pool_name="example-destination-pool-name",
            ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sesv2_dedicated_ip_assignment, with destination pool
    name example-destination-pool-name, ip example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination_pool_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
