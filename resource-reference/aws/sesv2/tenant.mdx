---
title: "aws_sesv2_tenant"
description: "Real, generated bindings for `aws_sesv2_tenant`."
---

`aws_sesv2_tenant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"

    cfg := sesv2.TenantConfig{
        TenantName: "example-tenant-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(sesv2.Tenant, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Tenant } from "jsr:@ubx/sdk-aws/aws/sesv2/tenant";

    resource(Tenant, "example", {
      tenantName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sesv2 import Tenant, TenantConfig

    cfg = TenantConfig(
        tenant_name="example",
        tags={"key": "value"},
    )
    ubx.resource(Tenant, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sesv2_tenant with tenant_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tenant_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sending_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenant_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
