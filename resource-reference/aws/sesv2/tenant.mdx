---
title: "aws_sesv2_tenant"
description: "Real, generated bindings for `aws_sesv2_tenant`."
---

`aws_sesv2_tenant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tenant"})

    		ubx.Resource(sesv2.Tenant, "ci-runner", sesv2.TenantConfig{
    			TenantName: "example-tenant-name",
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Tenant } from "jsr:@ubx/sdk-aws/aws/sesv2/tenant";

    export default stack("payments", () => {
      intent({ summary: "payments own tenant" });

      resource(Tenant, "ci-runner", {
        tenantName: "example-tenant-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sesv2 import Tenant, TenantConfig

    def describe():
        ubx.intent("payments own tenant")

        ubx.resource(Tenant, "ci-runner", TenantConfig(
            tenant_name="example-tenant-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sesv2_tenant, with tenant name example-tenant-name,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tenant_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sending_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenant_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
