---
title: "aws_computeoptimizer_recommendation_preferences"
description: "Real, generated bindings for `aws_computeoptimizer_recommendation_preferences`."
---

`aws_computeoptimizer_recommendation_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	computeoptimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/computeoptimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own recommendation preferences"})

    		ubx.Resource(computeoptimizer.RecommendationPreferences, "ci-runner", computeoptimizer.RecommendationPreferencesConfig{
    			ResourceType:                  "example",
    			EnhancedInfrastructureMetrics: "example",
    			InferredWorkloadTypes:         "example",
    			SavingsEstimationMode:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecommendationPreferences } from "jsr:@ubx/sdk-aws/aws/computeoptimizer/recommendation_preferences";

    export default stack("payments", () => {
      intent({ summary: "payments own recommendation preferences" });

      resource(RecommendationPreferences, "ci-runner", {
        resourceType: "example",
        enhancedInfrastructureMetrics: "example",
        inferredWorkloadTypes: "example",
        savingsEstimationMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.computeoptimizer import RecommendationPreferences, RecommendationPreferencesConfig

    def describe():
        ubx.intent("payments own recommendation preferences")

        ubx.resource(RecommendationPreferences, "ci-runner", RecommendationPreferencesConfig(
            resource_type="example",
            enhanced_infrastructure_metrics="example",
            inferred_workload_types="example",
            savings_estimation_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_computeoptimizer_recommendation_preferences, with
    resource type example, enhanced infrastructure metrics example,
    inferred workload types example, savings estimation mode example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enhanced_infrastructure_metrics" type="string">
  Optional.
</ResponseField>

<ResponseField name="inferred_workload_types" type="string">
  Optional.
</ResponseField>

<ResponseField name="look_back_period" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="look_back_period" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
