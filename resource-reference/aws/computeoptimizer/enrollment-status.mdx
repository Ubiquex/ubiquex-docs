---
title: "aws_computeoptimizer_enrollment_status"
description: "Real, generated bindings for `aws_computeoptimizer_enrollment_status`."
---

`aws_computeoptimizer_enrollment_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/computeoptimizer"

    cfg := computeoptimizer.EnrollmentStatusConfig{
        Status: "example",
        IncludeMemberAccounts: true,
    }
    ubx.Resource(computeoptimizer.EnrollmentStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EnrollmentStatus } from
      "jsr:@ubx/sdk-aws/aws/computeoptimizer/enrollment_status";

    resource(EnrollmentStatus, "example", {
      status: "example",
      includeMemberAccounts: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.computeoptimizer import (
        EnrollmentStatus,
        EnrollmentStatusConfig,
    )

    cfg = EnrollmentStatusConfig(
        status="example",
        include_member_accounts=True,
    )
    ubx.resource(EnrollmentStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_computeoptimizer_enrollment_status with status,
include_member_accounts.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="number_of_member_accounts_opted_in" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
