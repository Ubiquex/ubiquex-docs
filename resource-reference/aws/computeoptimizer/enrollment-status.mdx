---
title: "aws_computeoptimizer_enrollment_status"
description: "Real, generated bindings for `aws_computeoptimizer_enrollment_status`."
---

`aws_computeoptimizer_enrollment_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	computeoptimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/computeoptimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own enrollment status"})

    		ubx.Resource(computeoptimizer.EnrollmentStatus, "ci-runner", computeoptimizer.EnrollmentStatusConfig{
    			Status: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnrollmentStatus } from "jsr:@ubx/sdk-aws/aws/computeoptimizer/enrollment_status";

    export default stack("payments", () => {
      intent({ summary: "payments own enrollment status" });

      resource(EnrollmentStatus, "ci-runner", {
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.computeoptimizer import EnrollmentStatus, EnrollmentStatusConfig

    def describe():
        ubx.intent("payments own enrollment status")

        ubx.resource(EnrollmentStatus, "ci-runner", EnrollmentStatusConfig(
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_computeoptimizer_enrollment_status, with status
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="include_member_accounts" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="number_of_member_accounts_opted_in" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
