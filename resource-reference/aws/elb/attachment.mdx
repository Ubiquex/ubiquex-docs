---
title: "aws_elb_attachment"
description: "Real, generated bindings for `aws_elb_attachment`."
---

`aws_elb_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/elb"

    cfg := elb.AttachmentConfig{
        Elb: "example",
        Instance: "example",
    }
    ubx.Resource(elb.Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Attachment } from "jsr:@ubx/sdk-aws/aws/elb/attachment";

    resource(Attachment, "example", {
      elb: "example",
      instance: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.elb import Attachment, AttachmentConfig

    cfg = AttachmentConfig(
        elb="example",
        instance="example",
    )
    ubx.resource(Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_elb_attachment with elb, instance.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="elb" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
