---
title: "aws_ram_resource_share_accepter"
description: "Real, generated bindings for `aws_ram_resource_share_accepter`."
---

`aws_ram_resource_share_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ram"

    cfg := ram.ResourceShareAccepterConfig{
        ShareArn: "example",
    }
    ubx.Resource(ram.ResourceShareAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceShareAccepter } from
      "jsr:@ubx/sdk-aws/aws/ram/resource_share_accepter";

    resource(ResourceShareAccepter, "example", {
      shareArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ram import (
        ResourceShareAccepter,
        ResourceShareAccepterConfig,
    )

    cfg = ResourceShareAccepterConfig(
        share_arn="example",
    )
    ubx.resource(ResourceShareAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ram_resource_share_accepter with share_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="share_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invitation_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="receiver_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resources" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sender_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="share_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="share_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
