---
title: "aws_ram_sharing_with_organization"
description: "Real, generated bindings for `aws_ram_sharing_with_organization`."
---

`aws_ram_sharing_with_organization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ram"

    cfg := ram.SharingWithOrganizationConfig{
    }
    ubx.Resource(ram.SharingWithOrganization, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SharingWithOrganization } from
      "jsr:@ubx/sdk-aws/aws/ram/sharing_with_organization";

    resource(SharingWithOrganization, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ram import (
        SharingWithOrganization,
        SharingWithOrganizationConfig,
    )

    cfg = SharingWithOrganizationConfig(
    )
    ubx.resource(SharingWithOrganization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ram_sharing_with_organization.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
