---
title: "aws_ram_resource_association"
description: "Real, generated bindings for `aws_ram_resource_association`."
---

`aws_ram_resource_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ram"

    cfg := ram.ResourceAssociationConfig{
        ResourceArn: "example",
        ResourceShareArn: "example",
    }
    ubx.Resource(ram.ResourceAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceAssociation } from
      "jsr:@ubx/sdk-aws/aws/ram/resource_association";

    resource(ResourceAssociation, "example", {
      resourceArn: "example",
      resourceShareArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ram import (
        ResourceAssociation,
        ResourceAssociationConfig,
    )

    cfg = ResourceAssociationConfig(
        resource_arn="example",
        resource_share_arn="example",
    )
    ubx.resource(ResourceAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ram_resource_association with resource_arn,
resource_share_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_share_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
