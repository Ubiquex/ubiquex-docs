---
title: "aws_ram_resource_share_associations_exclusive"
description: "Real, generated bindings for `aws_ram_resource_share_associations_exclusive`."
---

`aws_ram_resource_share_associations_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource share associations exclusive"})

    		ubx.Resource(ram.ResourceShareAssociationsExclusive, "ci-runner", ram.ResourceShareAssociationsExclusiveConfig{
    			ResourceShareArn: "arn:aws:iam::123456789012:policy/ci",
    			Principals:       []string{"example"},
    			ResourceArns:     []string{"example"},
    			Sources:          []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceShareAssociationsExclusive } from "jsr:@ubx/sdk-aws/aws/ram/resource_share_associations_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own resource share associations exclusive" });

      resource(ResourceShareAssociationsExclusive, "ci-runner", {
        resourceShareArn: "arn:aws:iam::123456789012:policy/ci",
        principals: ["example"],
        resourceArns: ["example"],
        sources: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ram import ResourceShareAssociationsExclusive, ResourceShareAssociationsExclusiveConfig

    def describe():
        ubx.intent("payments own resource share associations exclusive")

        ubx.resource(ResourceShareAssociationsExclusive, "ci-runner", ResourceShareAssociationsExclusiveConfig(
            resource_share_arn="arn:aws:iam::123456789012:policy/ci",
            principals=["example"],
            resource_arns=["example"],
            sources=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ram_resource_share_associations_exclusive, with
    resource share arn arn:aws:iam::123456789012:policy/ci,
    principals a set value, resource arns a set value, sources a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="principals" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="resource_share_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="sources" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
