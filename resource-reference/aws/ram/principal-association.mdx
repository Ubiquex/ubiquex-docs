---
title: "aws_ram_principal_association"
description: "Real, generated bindings for `aws_ram_principal_association`."
---

`aws_ram_principal_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ram"

    cfg := ram.PrincipalAssociationConfig{
        Principal: "example",
        ResourceShareArn: "example",
    }
    ubx.Resource(ram.PrincipalAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PrincipalAssociation } from
      "jsr:@ubx/sdk-aws/aws/ram/principal_association";

    resource(PrincipalAssociation, "example", {
      principal: "example",
      resourceShareArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ram import (
        PrincipalAssociation,
        PrincipalAssociationConfig,
    )

    cfg = PrincipalAssociationConfig(
        principal="example",
        resource_share_arn="example",
    )
    ubx.resource(PrincipalAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ram_principal_association with principal,
resource_share_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_share_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
