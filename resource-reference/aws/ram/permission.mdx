---
title: "aws_ram_permission"
description: "Real, generated bindings for `aws_ram_permission`."
---

`aws_ram_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ram"

    cfg := ram.PermissionConfig{
        Name: "example-name",
        PolicyTemplate: "example",
        ResourceType: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ram.Permission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Permission } from "jsr:@ubx/sdk-aws/aws/ram/permission";

    resource(Permission, "example", {
      name: "example",
      policyTemplate: "example",
      resourceType: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ram import Permission, PermissionConfig

    cfg = PermissionConfig(
        name="example",
        policy_template="example",
        resource_type="example",
        tags={"key": "value"},
    )
    ubx.resource(Permission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ram_permission with name, policy_template,
resource_type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_template" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_version" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
