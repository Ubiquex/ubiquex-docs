---
title: "aws_amazon_mq_configuration_association"
description: "`aws_amazon_mq_configuration_association` is the real link between an `aws_amazon_mq_configuration` and the `aws_amazon_mq_broker` it applies to."
---

A configuration on its own changes nothing, this association is what actually applies a given configuration revision to a running broker.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amazonmq "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/amazonmq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own configuration association"})

    		ubx.Resource(amazonmq.ConfigurationAssociation, "example", amazonmq.ConfigurationAssociationConfig{
    			Broker:        "example",
    			Configuration: map[string]any{"id": "example", "revision": 1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationAssociation } from "@ubx/sdk-aws/aws/amazonmq/configuration_association";

    export default stack("example", () => {
      intent({ summary: "example own configuration association" });

      resource(ConfigurationAssociation, "example", {
        broker: "example",
        configuration: { id: "example", revision: 1 },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.amazonmq import ConfigurationAssociation, ConfigurationAssociationConfig
    from ubx.aws.amazonmq.configuration_association import ConfigurationAssociation_Configuration

    def describe():
        ubx.intent("example own configuration association")

        ubx.resource(ConfigurationAssociation, "example", ConfigurationAssociationConfig(
            broker="example",
            configuration=ConfigurationAssociation_Configuration(id="example", revision=1),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amazon_mq_configuration_association, with broker
    example, configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="broker" type="string" required>
  ID of the Broker that the configuration should be applied to Required.
</ResponseField>

<ResponseField name="configuration" type="object" required>
  Specifies the Amazon MQ configuration (ID and revision) to associate with the broker in a ConfigurationAssociation resource. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id" type="string" required>
      ID of the Configuration to apply to a Broker Required.
    </ResponseField>
    <ResponseField name="revision" type="number" required>
      Revision of the Configuration to apply to a Broker Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The ID of the ConfigurationAssociation Resource Computed by AWS after `ship`.
</ResponseField>
