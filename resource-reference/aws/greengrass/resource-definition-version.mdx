---
title: "aws_greengrass_resource_definition_version"
description: "`aws_greengrass_resource_definition_version` is a real, specific version of an `aws_greengrass_resource_definition`..."
---

`aws_greengrass_resource_definition_version` is a real, specific version of an `aws_greengrass_resource_definition`, part of AWS IoT Greengrass V1's maintenance-only lifecycle since June 2023.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource definition version"})

    		ubx.Resource(greengrass.ResourceDefinitionVersion, "ci-runner", greengrass.ResourceDefinitionVersionConfig{
    			ResourceDefinitionId: "example",
    			Resources:            []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceDefinitionVersion } from "./local-sdk/aws/sdk/typescript/aws/greengrass/resource_definition_version.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own resource definition version" });

      resource(ResourceDefinitionVersion, "ci-runner", {
        resourceDefinitionId: "example",
        resources: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.greengrass import ResourceDefinitionVersion, ResourceDefinitionVersionConfig

    def describe():
        ubx.intent("payments own resource definition version")

        ubx.resource(ResourceDefinitionVersion, "ci-runner", ResourceDefinitionVersionConfig(
            resource_definition_id="example",
            resources=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_greengrass_resource_definition_version, with resource
    definition id example, resources a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="resource_definition_id" type="string" required>
  The ID of the AWS Greengrass resource definition that this version belongs to, required to associate the version with an existing resource definition. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="resources" type="list(object)" required>
  Specifies the list of resources (such as local volumes, devices, or secrets) that are included in this AWS Greengrass resource definition version. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The user-defined ID that uniquely identifies this resource within the Greengrass resource definition version. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      A user-defined name for the resource (such as a local device, local volume, or S3 object) in an AWS Greengrass resource definition version. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="resource_data_container" type="object">
      Defines the resource data container for each Greengrass resource, specifying either a local device or local volume resource (e.g., source path, permissions). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="local_device_resource_data" type="object">
          Configures a local device resource by specifying its source path on the host file system and optional group owner settings, used when the resource is a physical device such as /dev/ttyAMA0. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="group_owner_setting" type="object">
              Configures the Linux group owner, group ID, and read/write/execute permissions for a local device resource in a Greengrass group, enabling the Greengrass core to access the device with specific ownership and access rights. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="auto_add_group_owner" type="bool">
                  Indicates whether the Greengrass group owner is automatically added as the owner of the local device resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="group_owner" type="string">
                  The name of the Linux group that owns the local device resource and has read/write access to it. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_path" type="string">
              The source_path specifies the local device path on the Greengrass core device that this local device resource maps to, such as /dev/ttyUSB0 or /dev/video0. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="local_volume_resource_data" type="object">
          Configures a local volume resource definition for a Greengrass group, specifying the device's source path and the destination path inside the Lambda runtime environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="destination_path" type="string">
              The absolute path on the Greengrass core where the source directory is mounted and available to the Lambda function. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="group_owner_setting" type="object">
              Determines whether the Greengrass group owner is automatically granted ownership of the local volume, and optionally specifies a custom group owner for the volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="auto_add_group_owner" type="bool">
                  If true, automatically adds the group owner (the core device's OS group) to the Linux permissions of the local volume resource, allowing the group to access the volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="group_owner" type="string">
                  Specifies the Linux group name that will own the local volume resource when group ownership is configured for a Greengrass group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="source_path" type="string">
              The source path of the local volume resource, specifying the directory on the Greengrass core device that is used as the source for the volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="s3_machine_learning_model_resource_data" type="object">
          Specifies the S3 machine learning model resource data, containing the S3 URI of the model and the destination path on the core device. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="destination_path" type="string">
              The absolute filesystem path on the Greengrass core device where the S3 machine learning model is downloaded and stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="owner_setting" type="object">
              Specifies the Linux group owner and group read/write permission for the S3 machine learning model artifact, controlling its file ownership and access on the AWS IoT Greengrass core. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="group_owner" type="string">
                  The name of the operating system group that owns the machine learning model artifact downloaded from S3 on the Greengrass core device, used in the owner setting for the S3 machine learning model resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="group_permission" type="string">
                  The permission level (either 'ro' for read-only or 'rw' for read-write) that the specified group owner has on the referenced S3 machine learning model resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="s3_uri" type="string">
              The S3 URI of the machine learning model object that this resource references. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sage_maker_machine_learning_model_resource_data" type="object">
          Specifies the resource data container for a SageMaker machine learning model resource, defining the S3 URI of the model artifact and the local destination path on the Greengrass core. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="destination_path" type="string">
              Specifies the absolute local path on the Greengrass core where the SageMaker machine learning model artifact is downloaded or mounted for use by the Lambda function. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="owner_setting" type="object">
              Specifies the owner setting for the SageMaker machine learning model resource, including the Linux group owner and its permissions on the model artifact used by the Greengrass core. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="group_owner" type="string">
                  Defines the operating system group owner for the SageMaker machine learning model resource when it is deployed to the Greengrass core. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="group_permission" type="string">
                  The permission level, either 'rw' (read/write) or 'ro' (read-only), that the group owner is granted on the SageMaker machine learning model resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sage_maker_job_arn" type="string">
              ARN of the SageMaker training job whose model artifact is used to create the local machine learning model resource in Greengrass. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="secrets_manager_secret_resource_data" type="object">
          Defines the configuration for referencing a secret from AWS Secrets Manager in the Greengrass resource, specifying the secret ARN and any additional properties needed for access. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="additional_staging_labels_to_download" type="list(string)">
              Specifies additional AWS Secrets Manager staging labels (beyond the default AWSCURRENT) whose secret versions are downloaded to the Greengrass core for this secret resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="arn" type="string">
              The ARN of the AWS Secrets Manager secret that this Greengrass resource references, used to retrieve the secret's value on the Greengrass core. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned by AWS to this Greengrass resource definition version. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
