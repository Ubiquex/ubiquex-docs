---
title: "aws_customer_profiles_domain_object_type"
description: "`aws_customer_profiles_domain_object_type` is the real, domain-specific configuration of an `aws_customer_profiles_object_type`..."
---

`aws_customer_profiles_domain_object_type` is the real, domain-specific configuration of an `aws_customer_profiles_object_type`, applying that object type's schema within one particular `aws_customer_profiles_domain` rather than the type definition existing only in the abstract.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	customer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/customer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own profiles domain object type"})

    		ubx.Resource(customer.ProfilesDomainObjectType, "example", customer.ProfilesDomainObjectTypeConfig{
    			DomainName:     "example-domain-name",
    			Fields:         "example",
    			ObjectTypeName: "example-object-type-name",
    			Description:    "Managed by ubx.",
    			EncryptionKey:  "example",
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProfilesDomainObjectType } from "@ubx/sdk-aws/aws/customer/profiles_domain_object_type";

    export default stack("example", () => {
      intent({ summary: "example own profiles domain object type" });

      resource(ProfilesDomainObjectType, "example", {
        domainName: "example-domain-name",
        fields: "example",
        objectTypeName: "example-object-type-name",
        description: "Managed by ubx.",
        encryptionKey: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.customer import ProfilesDomainObjectType, ProfilesDomainObjectTypeConfig

    def describe():
        ubx.intent("example own profiles domain object type")

        ubx.resource(ProfilesDomainObjectType, "example", ProfilesDomainObjectTypeConfig(
            domain_name="example-domain-name",
            fields="example",
            object_type_name="example-object-type-name",
            description="Managed by ubx.",
            encryption_key="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_customer_profiles_domain_object_type, with domain name
    example-domain-name, fields example, object type name
    example-object-type-name, description Managed by ubx., encryption
    key example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the domain object type. Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  The unique name of the domain. Required.
</ResponseField>

<ResponseField name="encryption_key" type="string">
  The default encryption key Optional.
</ResponseField>

<ResponseField name="fields" type="any" required>
  A map of the name and ObjectType field. Required.
</ResponseField>

<ResponseField name="object_type_name" type="string" required>
  The name of the domain object type. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Customer Profiles domain object type, used to organize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp of when the domain object type was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The timestamp of when the domain object type was most recently edited. Computed by AWS after `ship`.
</ResponseField>
