---
title: "aws_customer_gateway"
description: "Real, generated bindings for `aws_customer_gateway`."
---

`aws_customer_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/customer"

    cfg := customer.GatewayConfig{
        Type: "example",
        BgpAsn: "example",
        BgpAsnExtended: "example",
    }
    ubx.Resource(customer.Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Gateway } from "jsr:@ubx/sdk-aws/aws/customer/gateway";

    resource(Gateway, "example", {
      type: "example",
      bgpAsn: "example",
      bgpAsnExtended: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.customer import Gateway, GatewayConfig

    cfg = GatewayConfig(
        type="example",
        bgp_asn="example",
        bgp_asn_extended="example",
    )
    ubx.resource(Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_customer_gateway with type, bgp_asn,
bgp_asn_extended.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bgp_asn" type="string">
  Optional.
</ResponseField>

<ResponseField name="bgp_asn_extended" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="device_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
