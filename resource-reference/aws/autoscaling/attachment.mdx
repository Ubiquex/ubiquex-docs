---
title: "aws_autoscaling_attachment"
description: "Real, generated bindings for `aws_autoscaling_attachment`."
---

`aws_autoscaling_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/autoscaling"

    cfg := autoscaling.AttachmentConfig{
        AutoscalingGroupName: "example-autoscaling-group-name",
        Elb: "example",
        LbTargetGroupArn: "example",
    }
    ubx.Resource(autoscaling.Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Attachment } from
      "jsr:@ubx/sdk-aws/aws/autoscaling/attachment";

    resource(Attachment, "example", {
      autoscalingGroupName: "example",
      elb: "example",
      lbTargetGroupArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.autoscaling import Attachment, AttachmentConfig

    cfg = AttachmentConfig(
        autoscaling_group_name="example",
        elb="example",
        lb_target_group_arn="example",
    )
    ubx.resource(Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_autoscaling_attachment with autoscaling_group_name,
elb, lb_target_group_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="elb" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_target_group_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
