---
title: "aws_autoscaling_attachment"
description: "Real, generated bindings for `aws_autoscaling_attachment`."
---

`aws_autoscaling_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attachment"})

    		ubx.Resource(autoscaling.Attachment, "ci-runner", autoscaling.AttachmentConfig{
    			AutoscalingGroupName: "example-autoscaling-group-name",
    			Elb:                  "example",
    			LbTargetGroupArn:     "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Attachment } from "jsr:@ubx/sdk-aws/aws/autoscaling/attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own attachment" });

      resource(Attachment, "ci-runner", {
        autoscalingGroupName: "example-autoscaling-group-name",
        elb: "example",
        lbTargetGroupArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.autoscaling import Attachment, AttachmentConfig

    def describe():
        ubx.intent("payments own attachment")

        ubx.resource(Attachment, "ci-runner", AttachmentConfig(
            autoscaling_group_name="example-autoscaling-group-name",
            elb="example",
            lb_target_group_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_autoscaling_attachment, with autoscaling group name
    example-autoscaling-group-name, elb example, lb target group arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="elb" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_target_group_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
