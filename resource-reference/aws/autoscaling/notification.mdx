---
title: "aws_autoscaling_notification"
description: "Real, generated bindings for `aws_autoscaling_notification`."
---

`aws_autoscaling_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/autoscaling"

    cfg := autoscaling.NotificationConfig{
        GroupNames: []string{"example"},
        Notifications: []string{"example"},
        TopicArn: "example",
    }
    ubx.Resource(autoscaling.Notification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Notification } from
      "jsr:@ubx/sdk-aws/aws/autoscaling/notification";

    resource(Notification, "example", {
      groupNames: ["example"],
      notifications: ["example"],
      topicArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.autoscaling import (
        Notification,
        NotificationConfig,
    )

    cfg = NotificationConfig(
        group_names=["example"],
        notifications=["example"],
        topic_arn="example",
    )
    ubx.resource(Notification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_autoscaling_notification with group_names,
notifications, topic_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="notifications" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="topic_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
