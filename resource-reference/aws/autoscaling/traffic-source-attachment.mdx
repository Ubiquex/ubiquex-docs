---
title: "aws_autoscaling_traffic_source_attachment"
description: "Real, generated bindings for `aws_autoscaling_traffic_source_attachment`."
---

`aws_autoscaling_traffic_source_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/autoscaling"

    cfg := autoscaling.TrafficSourceAttachmentConfig{
        AutoscalingGroupName: "example-autoscaling-group-name",
    }
    ubx.Resource(autoscaling.TrafficSourceAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TrafficSourceAttachment } from
      "jsr:@ubx/sdk-aws/aws/autoscaling/traffic_source_attachment";

    resource(TrafficSourceAttachment, "example", {
      autoscalingGroupName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.autoscaling import (
        TrafficSourceAttachment,
        TrafficSourceAttachmentConfig,
    )

    cfg = TrafficSourceAttachmentConfig(
        autoscaling_group_name="example",
    )
    ubx.resource(TrafficSourceAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_autoscaling_traffic_source_attachment with
autoscaling_group_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
