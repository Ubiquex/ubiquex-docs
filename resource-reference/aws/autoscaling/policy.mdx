---
title: "aws_autoscaling_policy"
description: "Real, generated bindings for `aws_autoscaling_policy`."
---

`aws_autoscaling_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/autoscaling"

    cfg := autoscaling.PolicyConfig{
        AutoscalingGroupName: "example-autoscaling-group-name",
        Name: "example-name",
        AdjustmentType: "example",
        Cooldown: 1,
    }
    ubx.Resource(autoscaling.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from "jsr:@ubx/sdk-aws/aws/autoscaling/policy";

    resource(Policy, "example", {
      autoscalingGroupName: "example",
      name: "example",
      adjustmentType: "example",
      cooldown: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.autoscaling import Policy, PolicyConfig

    cfg = PolicyConfig(
        autoscaling_group_name="example",
        name="example",
        adjustment_type="example",
        cooldown=1,
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_autoscaling_policy with autoscaling_group_name,
name, adjustment_type, cooldown.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="adjustment_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cooldown" type="number">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="estimated_instance_warmup" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric_aggregation_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="min_adjustment_magnitude" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scaling_adjustment" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric_aggregation_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
