---
title: "aws_autoscaling_lifecycle_hook"
description: "Real, generated bindings for `aws_autoscaling_lifecycle_hook`."
---

`aws_autoscaling_lifecycle_hook` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lifecycle hook"})

    		ubx.Resource(autoscaling.LifecycleHook, "ci-runner", autoscaling.LifecycleHookConfig{
    			AutoscalingGroupName:  "example-autoscaling-group-name",
    			LifecycleTransition:   "example",
    			Name:                  "ci-runner",
    			HeartbeatTimeout:      1,
    			NotificationMetadata:  "example",
    			NotificationTargetArn: "arn:aws:iam::123456789012:policy/ci",
    			RoleArn:               "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LifecycleHook } from "jsr:@ubx/sdk-aws/aws/autoscaling/lifecycle_hook";

    export default stack("payments", () => {
      intent({ summary: "payments own lifecycle hook" });

      resource(LifecycleHook, "ci-runner", {
        autoscalingGroupName: "example-autoscaling-group-name",
        lifecycleTransition: "example",
        name: "ci-runner",
        heartbeatTimeout: 1,
        notificationMetadata: "example",
        notificationTargetArn: "arn:aws:iam::123456789012:policy/ci",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.autoscaling import LifecycleHook, LifecycleHookConfig

    def describe():
        ubx.intent("payments own lifecycle hook")

        ubx.resource(LifecycleHook, "ci-runner", LifecycleHookConfig(
            autoscaling_group_name="example-autoscaling-group-name",
            lifecycle_transition="example",
            name="ci-runner",
            heartbeat_timeout=1,
            notification_metadata="example",
            notification_target_arn="arn:aws:iam::123456789012:policy/ci",
            role_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_autoscaling_lifecycle_hook called "ci-runner", with
    autoscaling group name example-autoscaling-group-name, lifecycle
    transition example, heartbeat timeout 1, notification metadata
    example, notification target arn
    arn:aws:iam::123456789012:policy/ci, role arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_result" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="heartbeat_timeout" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lifecycle_transition" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="notification_metadata" type="string">
  Optional.
</ResponseField>

<ResponseField name="notification_target_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="default_result" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
