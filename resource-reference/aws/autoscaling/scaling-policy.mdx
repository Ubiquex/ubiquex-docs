---
title: "aws_auto_scaling_scaling_policy"
description: "`aws_auto_scaling_scaling_policy` defines the condition under which an `aws_auto_scaling_auto_scaling_group` launches or terminates instances..."
---

`aws_auto_scaling_scaling_policy` defines the condition under which an `aws_auto_scaling_auto_scaling_group` launches or terminates instances, target tracking, step scaling, or predictive scaling against historical load, real logic a group has none of on its own beyond its static min/max/desired bounds.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own scaling policy"})

    		ubx.Resource(autoscaling.ScalingPolicy, "example", autoscaling.ScalingPolicyConfig{
    			AutoScalingGroupName:           "example-auto-scaling-group-name",
    			AdjustmentType:                 "example",
    			Cooldown:                       "example",
    			EstimatedInstanceWarmup:        1,
    			MetricAggregationType:          "example",
    			MinAdjustmentMagnitude:         1,
    			PolicyType:                     "example",
    			PredictiveScalingConfiguration: map[string]any{"metric_specifications": []map[string]any{{"customized_capacity_metric_specification": map[string]any{"metric_data_queries": []map[string]any{{"expression": "example"}}}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScalingPolicy } from "@ubx/sdk-aws/aws/autoscaling/scaling_policy";

    export default stack("example", () => {
      intent({ summary: "example own scaling policy" });

      resource(ScalingPolicy, "example", {
        autoScalingGroupName: "example-auto-scaling-group-name",
        adjustmentType: "example",
        cooldown: "example",
        estimatedInstanceWarmup: 1,
        metricAggregationType: "example",
        minAdjustmentMagnitude: 1,
        policyType: "example",
        predictiveScalingConfiguration: {
          metricSpecifications: [{
            customizedCapacityMetricSpecification: {
              metricDataQueries: [{ expression: "example" }],
            },
          }],
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.autoscaling import ScalingPolicy, ScalingPolicyConfig
    from ubx.aws.autoscaling.scaling_policy import ScalingPolicy_PredictiveScalingConfiguration, ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications, ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications_CustomizedCapacityMetricSpecification, ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications_CustomizedCapacityMetricSpecification_MetricDataQueries

    def describe():
        ubx.intent("example own scaling policy")

        ubx.resource(ScalingPolicy, "example", ScalingPolicyConfig(
            auto_scaling_group_name="example-auto-scaling-group-name",
            adjustment_type="example",
            cooldown="example",
            estimated_instance_warmup=1,
            metric_aggregation_type="example",
            min_adjustment_magnitude=1,
            policy_type="example",
            predictive_scaling_configuration=ScalingPolicy_PredictiveScalingConfiguration(metric_specifications=[ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications(customized_capacity_metric_specification=ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications_CustomizedCapacityMetricSpecification(metric_data_queries=[ScalingPolicy_PredictiveScalingConfiguration_MetricSpecifications_CustomizedCapacityMetricSpecification_MetricDataQueries(expression="example")]))]),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auto_scaling_scaling_policy, with auto scaling group
    name example-auto-scaling-group-name, adjustment type example,
    cooldown example, estimated instance warmup 1, metric aggregation
    type example, min adjustment magnitude 1, policy type example,
    predictive scaling configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="adjustment_type" type="string">
  Specifies how the scaling adjustment is interpreted. The valid values are ChangeInCapacity, ExactCapacity, and PercentChangeInCapacity. Optional.
</ResponseField>

<ResponseField name="auto_scaling_group_name" type="string" required>
  The name of the Auto Scaling group. Required.
</ResponseField>

<ResponseField name="cooldown" type="string">
  The duration of the policy's cooldown period, in seconds. When a cooldown period is specified here, it overrides the default cooldown period defined for the Auto Scaling group. Optional.
</ResponseField>

<ResponseField name="estimated_instance_warmup" type="number">
  The estimated time, in seconds, until a newly launched instance can contribute to the CloudWatch metrics. If not provided, the default is to use the value from the default cooldown period for the Auto Scaling group. Valid only if the policy type is TargetTrackingScaling or StepScaling. Optional.
</ResponseField>

<ResponseField name="metric_aggregation_type" type="string">
  The aggregation type for the CloudWatch metrics. The valid values are Minimum, Maximum, and Average. If the aggregation type is null, the value is treated as Average. Valid only if the policy type is StepScaling. Optional.
</ResponseField>

<ResponseField name="min_adjustment_magnitude" type="number">
  The minimum value to scale by when the adjustment type is PercentChangeInCapacity. For example, suppose that you create a step scaling policy to scale out an Auto Scaling group by 25 percent and you specify a MinAdjustmentMagnitude of 2. If the group has 4 instances and the scaling policy is performed, 25 percent of 4 is 1. However, because you specified a MinAdjustmentMagnitude of 2, Amazon EC2 Auto Scaling scales out the group by 2 instances. Optional.
</ResponseField>

<ResponseField name="policy_type" type="string">
  One of the following policy types: TargetTrackingScaling, StepScaling, SimpleScaling (default), PredictiveScaling Optional.
</ResponseField>

<ResponseField name="predictive_scaling_configuration" type="object">
  Defines the predictive scaling policy configuration for the Auto Scaling group, which uses machine learning to forecast future load and provision capacity in advance to minimize scaling latency. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_capacity_breach_behavior" type="string">
      Determines how the predictive scaling policy handles forecasted capacity that exceeds the maximum, either honoring the maximum capacity (HonorMaxCapacity) or increasing it (IncreaseMaxCapacity). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="max_capacity_buffer" type="number">
      The percentage of capacity that the predictive scaling policy can use to scale out beyond the maximum capacity of the Auto Scaling group. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="metric_specifications" type="list(object)" required>
      Specifies the list of metric specifications, including load/scaling metrics and target utilization values, that Amazon EC2 Auto Scaling uses for predictive scaling forecasts and scheduled scaling actions. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="customized_capacity_metric_specification" type="object">
          This object defines a customized capacity metric for predictive scaling by specifying metric data queries that Amazon EC2 Auto Scaling uses to measure the group's actual capacity, which is one of the metrics used to calculate the predictive scaling model. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metric_data_queries" type="list(object)">
              Specifies the metric data queries (CloudWatch metrics and expressions) that define the customized capacity metric for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="expression" type="string">
                  A math expression that derives the capacity metric value from one or more metric data queries in the same customized capacity metric specification, using CloudWatch metrics math syntax (e.g., a sum, average, or arithmetic combination of returned metrics). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="id" type="string">
                  A short name that uniquely identifies this metric data query within the customized capacity metric specification, and can be referenced by other queries' 'Expression' fields to combine data from multiple metrics. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="label" type="string">
                  The label provides a human-readable name for the metric data query, used to identify and reference the query's results within the predictive scaling customized capacity metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_stat" type="object">
                  This object specifies the metric statistics (such as the CloudWatch metric, statistic, period, and unit) for a single metric data query used to define a custom capacity metric for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="metric" type="object">
                      Specifies the CloudWatch metric (namespace, metric name, and dimensions) used in the metric statistic for the customized capacity metric data query of the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          Specifies the CloudWatch metric dimensions (name/value pairs) that identify the custom capacity metric used in the predictive scaling policy's metric data query, scoping the metric data to the specified dimension values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of a dimension for the metric used in the customized capacity metric specification of a predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of a CloudWatch metric dimension that, together with the dimension name, identifies a specific resource (such as an Auto Scaling group or instance) for the customized capacity metric used in predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The name of the CloudWatch metric used in the metric data query for the customized capacity metric specification of the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace" type="string">
                          The namespace of the CloudWatch metric that defines the customized capacity metric for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stat" type="string">
                      The statistic to apply to the specified metric (such as Sum, Average, Minimum, Maximum, or SampleCount) within a metric data query for the customized capacity metric specification of a predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="unit" type="string">
                      Specifies the unit of measure for the metric used in the customized capacity metric specification (e.g., 'Percent', 'Count', 'Bytes'), which CloudWatch uses for the predictive scaling model. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="return_data" type="bool">
                  Indicates whether this metric data query returns the actual capacity value for the customized capacity metric in predictive scaling; only the query that provides the capacity should set this to true, while supporting queries (e.g., used in math expressions) set it to false. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="customized_load_metric_specification" type="object">
          Defines the customized load metric specification for a predictive scaling policy, including the metric data queries that supply the load metric used to forecast future capacity requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metric_data_queries" type="list(object)">
              A list of CloudWatch metric data queries that define the customized load metric used for predictive scaling, specifying one or more metrics or mathematical expressions to represent application load. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="expression" type="string">
                  Specifies the math expression that is evaluated to derive the custom load metric value for predictive scaling, using CloudWatch metrics and functions such as SUM, AVG, or IF to combine the metric data returned by the query's metric stat. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="id" type="string">
                  The short name that identifies this metric data query, used to reference it within the metric expression of a customized load metric specification for an Auto Scaling predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="label" type="string">
                  A human-readable name for the metric data query, used to identify the returned time series in the customized load metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_stat" type="object">
                  Defines the specific CloudWatch metric and statistic (such as Sum or Average) that the metric data query uses to compute the custom load for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="metric" type="object">
                      Defines the source CloudWatch metric (metric name, namespace, and dimensions) that is used as the basis for the metric statistic in the customized load metric specification of a predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          The dimensions (name/value pairs) of the CloudWatch metric used as the customized load metric for predictive scaling, which uniquely identify the metric within its namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name (key) of a CloudWatch metric dimension that identifies the customized load metric used for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of a dimension key-value pair that identifies a specific metric in the custom load metric specification for predictive scaling, used to match the metric's dimensions in CloudWatch. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The name of the CloudWatch metric used as the custom load metric for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace" type="string">
                          Specifies the namespace of the CloudWatch metric used as the custom load metric for predictive scaling, identifying the service or custom application that the metric belongs to. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stat" type="string">
                      The statistic to use for the specified metric (for example, Sum or Average) when calculating the customized load metric for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="unit" type="string">
                      The unit of measure for the CloudWatch metric used in a customized load metric specification for predictive scaling, such as Count, Bytes, or Percent. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="return_data" type="bool">
                  Indicates whether the metric data for this query is included in the customized load metric specification used by the predictive scaling policy, enabling or suppressing its use in the metric evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="customized_scaling_metric_specification" type="object">
          Defines the customized scaling metric specification for a predictive scaling policy, using metric data queries to specify the load metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metric_data_queries" type="list(object)">
              A list of metric data queries that define the custom metrics used for predictive scaling, allowing you to specify CloudWatch metrics or math expressions for the customized scaling metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="expression" type="string">
                  Expression defines the mathematical expression to apply to the metric data query, referencing the Ids of other queries in the same customized scaling metric specification for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="id" type="string">
                  A unique short label that identifies this metric data query within the predictive scaling customized metric specification, and that can be referenced in the metric expression to combine or transform the data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="label" type="string">
                  A human-readable label for the metric data query that provides context for the metric in the predictive scaling policy's customized scaling metric specification, typically displayed in the console for identification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_stat" type="object">
                  Specifies the CloudWatch metric and statistic (such as Average, Sum, Minimum, Maximum) used to evaluate a metric data query in a customized predictive scaling metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="metric" type="object">
                      Specifies the CloudWatch metric (namespace, metric name, and dimensions) that serves as the source for the metric statistic in a customized predictive scaling metric data query. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          Specifies the list of CloudWatch metric dimensions (key-value pairs) that uniquely identify the metric used in the metric stat of a customized scaling metric specification for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of a CloudWatch metric dimension (e.g., AutoScalingGroupName or InstanceId) that, together with the dimension value, uniquely identifies the metric used in the customized scaling metric specification for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of a dimension that, together with the dimension name, identifies a CloudWatch metric for a customized predictive scaling metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The name of the CloudWatch metric that this metric data query references for the customized predictive scaling metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace" type="string">
                          The namespace of the CloudWatch metric used in the customized predictive scaling metric specification, identifying the service or application that emits the metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="stat" type="string">
                      Specifies the CloudWatch statistic (e.g., Sum, Average, Minimum, Maximum, SampleCount) to use for the custom metric in the predictive scaling metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="unit" type="string">
                      The unit of measure for the metric statistic, such as Count, Percent, or Bytes, used in the customized scaling metric specification for the predictive scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="return_data" type="bool">
                  Indicates whether this metric data query's result is included when the customized scaling metric is calculated, typically true for the primary metric and false for intermediate helper queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="predefined_load_metric_specification" type="object">
          Specifies the predefined load metric for predictive scaling, including the metric type and an optional resource label to identify the specific load balancer or resource being used. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="predefined_metric_type" type="string">
              Specifies the predefined load metric type (for example, ASGTotalCPUUtilization, ASGTotalNetworkIn, or ASGTotalNetworkOut) that the predictive scaling policy uses to measure the load on the Auto Scaling group. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_label" type="string">
              The resource label uniquely identifies the specific resource, such as an Application Load Balancer target group, that the predefined load metric is associated with, required for metrics like 'ALBRequestCountPerTarget'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="predefined_metric_pair_specification" type="object">
          Defines a predefined metric pair specification, which uses the ASGCPUUtilization metric type to pair CPU utilization and memory utilization metrics for the predictive scaling policy's metric specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="predefined_metric_type" type="string">
              Specifies the name of a predefined metric pair (for example, ASGCPUUtilization, ASGNetworkIn, or ASGNetworkOut) that the predictive scaling policy uses to determine the load and scaling metrics. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_label" type="string">
              A label (typically the ARN) identifying the Application Load Balancer target group from which the predefined metric pair, such as ALBRequestCountPerTarget, derives its request count data for predictive scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="predefined_scaling_metric_specification" type="object">
          This object defines a predefined scaling metric specification for predictive scaling, selecting the metric type (e.g., ASGAverageCPUUtilization) and optionally a resource label for the target resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="predefined_metric_type" type="string">
              The predefined metric type (such as ASGAverageCPUUtilization, ASGAverageNetworkIn, or ALBRequestCountPerTarget) that specifies which scaling metric the predictive scaling policy uses for forecasting. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resource_label" type="string">
              The resource label that uniquely identifies the specific AWS resource (such as an Application Load Balancer target group) to which the predefined scaling metric applies, required when the metric is `ALBRequestCountPerTarget`. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_value" type="number">
          The target value for the predictive scaling metric, which the model uses to determine the capacity to add for predicted load. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string">
      Specifies whether predictive scaling will actually scale capacity based on forecasts ('ForecastAndScale') or only generate forecasts without taking action ('ForecastOnly'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scheduling_buffer_time" type="number">
      The amount of time, in seconds, that the predictive scaling policy can use to execute scaling actions in advance of the forecasted load, with a default of 300 seconds. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scaling_adjustment" type="number">
  The amount by which to scale, based on the specified adjustment type. A positive value adds to the current capacity while a negative number removes from the current capacity. For exact capacity, you must specify a positive value. Required if the policy type is SimpleScaling. (Not used with any other policy type.) Optional.
</ResponseField>

<ResponseField name="step_adjustments" type="list(object)">
  A set of adjustments that enable you to scale based on the size of the alarm breach. Required if the policy type is StepScaling. (Not used with any other policy type.) Optional.
  <Expandable title="properties">
    <ResponseField name="metric_interval_lower_bound" type="number">
      Defines the lower bound for the difference between the alarm threshold and the CloudWatch metric, determining the metric interval that triggers this step's scaling adjustment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="metric_interval_upper_bound" type="number">
      Specifies the upper boundary of the CloudWatch metric value range for a step adjustment, determining the maximum metric threshold at which the corresponding scaling adjustment is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scaling_adjustment" type="number">
      The number of instances to add (positive value) or remove (negative value) when the alarm breach falls within the bounds of this step adjustment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_tracking_configuration" type="object">
  Specifies the target tracking configuration for the scaling policy, defining the metric to track and the target value that the Auto Scaling group maintains. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="customized_metric_specification" type="object">
      Defines the customized CloudWatch metric specification (namespace, metric name, dimensions, and statistic) that the target tracking policy uses to compute the scaling metric rather than using a predefined metric. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dimensions" type="list(object)">
          Specifies the CloudWatch metric dimensions (key-value pairs) used to identify the customized metric for the target tracking scaling policy. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the dimension key for the customized CloudWatch metric used in the target tracking policy, such as 'InstanceId' or 'AutoScalingGroupName'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Specifies the value of a dimension for the custom metric that the target tracking scaling policy monitors. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metric_name" type="string">
          The name of the CloudWatch metric to be used for target tracking scaling. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metrics" type="list(object)">
          Specifies a list of metric data queries that define the custom metric (including metric math expressions) used for target tracking scaling. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="expression" type="string">
              The CloudWatch metrics math expression that AWS Auto Scaling evaluates to derive the target metric value for the target tracking policy; it can combine multiple metric data queries referenced by their Id. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="string">
              A short name that uniquely identifies this metric data query within the list, allowing other queries to reference its returned data in their expressions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="label" type="string">
              The label is a human-readable name for a metric data query in the customized metric specification, used for display in CloudWatch when the target tracking policy is active. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric_stat" type="object">
              Specifies the CloudWatch metric and its statistic (such as Average or Sum) used by this metric data query in the customized target tracking scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="metric" type="object">
                  Specifies the CloudWatch metric (including metric name, namespace, and dimensions) whose value is used in the metric stat for this customized target tracking scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dimensions" type="list(object)">
                      Specifies the dimensions of the custom metric used with the target tracking scaling policy, where each dimension is a name-value pair that identifies a particular metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the CloudWatch metric dimension, which combines with the dimension value to uniquely identify a specific metric for the target tracking policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          The specific value for the CloudWatch metric dimension (e.g., an instance ID or instance type) that, combined with the dimension name, identifies the metric used for the target tracking policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="metric_name" type="string">
                      The name of the CloudWatch metric to use for the custom metric specification in the target tracking scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="namespace" type="string">
                      The namespace of the custom metric referenced in the metric stat, used to identify the metric (e.g., 'AWS/EC2' or a custom namespace) that the target tracking scaling policy will use for scaling decisions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="period" type="number">
                  The period (in seconds) over which the specified statistic is applied to the customized metric in the target tracking scaling policy, defining the metric's data granularity for CloudWatch. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stat" type="string">
                  The statistic (e.g., Average, Sum, Minimum, Maximum, SampleCount, or a percentile such as p99) to apply to the custom metric for the target tracking scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit" type="string">
                  Specifies the unit of measure for the metric's value (such as Percent, Count, or Bytes) that Amazon EC2 Auto Scaling uses when evaluating the target tracking policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="period" type="number">
              Specifies the period (in seconds) over which CloudWatch aggregates the metric data for the corresponding metric query in the custom metric specification used by this Auto Scaling target tracking policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="return_data" type="bool">
              Indicates whether this metric data query's result should be included in the target tracking metric evaluation; set to false for intermediate query results used only in expressions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="namespace" type="string">
          Specifies the CloudWatch namespace of the customized metric used by the target tracking scaling policy, identifying the source or service that published the metric. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="period" type="number">
          Optional.
        </ResponseField>
        <ResponseField name="statistic" type="string">
          The statistic to apply to the custom CloudWatch metric in the target tracking policy (e.g., Average, Sum, Minimum, Maximum, SampleCount). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of measure for the custom CloudWatch metric specified in the target tracking configuration, such as 'Count' or 'Percent', which is used by the scaling policy to interpret the metric's value. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disable_scale_in" type="bool">
      When set to true, the target tracking scaling policy does not scale in (terminate) instances in response to the target metric, restricting actions to scale-out only. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="predefined_metric_specification" type="object">
      Specifies the predefined Auto Scaling metric type (such as ASGAverageCPUUtilization or ALBRequestCountPerTarget) that the target tracking policy will track, and optionally a resource label for metrics that apply to a specific resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="predefined_metric_type" type="string" required>
          Specifies the name of the predefined Amazon EC2 Auto Scaling metric (e.g., ASGAverageCPUUtilization, ASGAverageNetworkIn, ASGAverageNetworkOut, or ALBRequestCountPerTarget) that the target tracking policy will use to scale the Auto Scaling group. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_label" type="string">
          For the ALBRequestCountPerTarget predefined metric, this label identifies the Application Load Balancer target group (e.g., app/my-alb/1234567890abcdef/targetgroup/my-tg/1234567890abcdef) that the scaling policy tracks request count per target for. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_value" type="number" required>
      The target value for the CloudWatch metric that the scaling policy tracks; Auto Scaling adjusts capacity to keep the metric at or near this value. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the AutoScaling scaling policy Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_name" type="string">
  The name of the scaling policy, automatically assigned by Auto Scaling and returned as a read-only attribute. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
