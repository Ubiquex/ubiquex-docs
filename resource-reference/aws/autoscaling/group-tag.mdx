---
title: "aws_autoscaling_group_tag"
description: "Real, generated bindings for `aws_autoscaling_group_tag`."
---

`aws_autoscaling_group_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group tag"})

    		ubx.Resource(autoscaling.GroupTag, "ci-runner", autoscaling.GroupTagConfig{
    			AutoscalingGroupName: "example-autoscaling-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupTag } from "jsr:@ubx/sdk-aws/aws/autoscaling/group_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own group tag" });

      resource(GroupTag, "ci-runner", {
        autoscalingGroupName: "example-autoscaling-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.autoscaling import GroupTag, GroupTagConfig

    def describe():
        ubx.intent("payments own group tag")

        ubx.resource(GroupTag, "ci-runner", GroupTagConfig(
            autoscaling_group_name="example-autoscaling-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_autoscaling_group_tag, with autoscaling group name
    example-autoscaling-group-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
