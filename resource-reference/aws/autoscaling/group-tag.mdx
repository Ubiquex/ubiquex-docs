---
title: "aws_autoscaling_group_tag"
description: "Real, generated bindings for `aws_autoscaling_group_tag`."
---

`aws_autoscaling_group_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/autoscaling"

    cfg := autoscaling.GroupTagConfig{
        AutoscalingGroupName: "example-autoscaling-group-name",
    }
    ubx.Resource(autoscaling.GroupTag, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GroupTag } from
      "jsr:@ubx/sdk-aws/aws/autoscaling/group_tag";

    resource(GroupTag, "example", {
      autoscalingGroupName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.autoscaling import GroupTag, GroupTagConfig

    cfg = GroupTagConfig(
        autoscaling_group_name="example",
    )
    ubx.resource(GroupTag, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_autoscaling_group_tag with autoscaling_group_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="autoscaling_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
