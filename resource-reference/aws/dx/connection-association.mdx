---
title: "aws_dx_connection_association"
description: "Real, generated bindings for `aws_dx_connection_association`."
---

`aws_dx_connection_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"

    cfg := dx.ConnectionAssociationConfig{
        ConnectionId: "example",
        LagId: "example",
    }
    ubx.Resource(dx.ConnectionAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionAssociation } from
      "jsr:@ubx/sdk-aws/aws/dx/connection_association";

    resource(ConnectionAssociation, "example", {
      connectionId: "example",
      lagId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dx import (
        ConnectionAssociation,
        ConnectionAssociationConfig,
    )

    cfg = ConnectionAssociationConfig(
        connection_id="example",
        lag_id="example",
    )
    ubx.resource(ConnectionAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dx_connection_association with connection_id,
lag_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lag_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
