---
title: "aws_dx_hosted_private_virtual_interface_accepter"
description: "Real, generated bindings for `aws_dx_hosted_private_virtual_interface_accepter`."
---

`aws_dx_hosted_private_virtual_interface_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hosted private virtual interface accepter"})

    		ubx.Resource(dx.HostedPrivateVirtualInterfaceAccepter, "ci-runner", dx.HostedPrivateVirtualInterfaceAccepterConfig{
    			VirtualInterfaceId: "example",
    			DxGatewayId:        "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    			VpnGatewayId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostedPrivateVirtualInterfaceAccepter } from "jsr:@ubx/sdk-aws/aws/dx/hosted_private_virtual_interface_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own hosted private virtual interface accepter" });

      resource(HostedPrivateVirtualInterfaceAccepter, "ci-runner", {
        virtualInterfaceId: "example",
        dxGatewayId: "example",
        tags: { "managed-by": "ubx" },
        vpnGatewayId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dx import HostedPrivateVirtualInterfaceAccepter, HostedPrivateVirtualInterfaceAccepterConfig

    def describe():
        ubx.intent("payments own hosted private virtual interface accepter")

        ubx.resource(HostedPrivateVirtualInterfaceAccepter, "ci-runner", HostedPrivateVirtualInterfaceAccepterConfig(
            virtual_interface_id="example",
            dx_gateway_id="example",
            tags={"managed-by": "ubx"},
            vpn_gateway_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dx_hosted_private_virtual_interface_accepter, with
    virtual interface id example, dx gateway id example, tags a set
    value, vpn gateway id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="dx_gateway_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpn_gateway_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
