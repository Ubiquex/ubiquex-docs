---
title: "aws_dx_hosted_public_virtual_interface"
description: "Real, generated bindings for `aws_dx_hosted_public_virtual_interface`."
---

`aws_dx_hosted_public_virtual_interface` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hosted public virtual interface"})

    		ubx.Resource(dx.HostedPublicVirtualInterface, "ci-runner", dx.HostedPublicVirtualInterfaceConfig{
    			AddressFamily:       "example",
    			BgpAsn:              1,
    			ConnectionId:        "example",
    			Name:                "ci-runner",
    			OwnerAccountId:      "example",
    			RouteFilterPrefixes: []string{"example"},
    			Vlan:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostedPublicVirtualInterface } from "jsr:@ubx/sdk-aws/aws/dx/hosted_public_virtual_interface";

    export default stack("payments", () => {
      intent({ summary: "payments own hosted public virtual interface" });

      resource(HostedPublicVirtualInterface, "ci-runner", {
        addressFamily: "example",
        bgpAsn: 1,
        connectionId: "example",
        name: "ci-runner",
        ownerAccountId: "example",
        routeFilterPrefixes: ["example"],
        vlan: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dx import HostedPublicVirtualInterface, HostedPublicVirtualInterfaceConfig

    def describe():
        ubx.intent("payments own hosted public virtual interface")

        ubx.resource(HostedPublicVirtualInterface, "ci-runner", HostedPublicVirtualInterfaceConfig(
            address_family="example",
            bgp_asn=1,
            connection_id="example",
            name="ci-runner",
            owner_account_id="example",
            route_filter_prefixes=["example"],
            vlan=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dx_hosted_public_virtual_interface called "ci-runner",
    with address family example, bgp asn 1, connection id example,
    owner account id example, route filter prefixes a set value, vlan
    1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="address_family" type="string" required>
  Required.
</ResponseField>

<ResponseField name="amazon_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="bgp_asn" type="number" required>
  Required.
</ResponseField>

<ResponseField name="bgp_auth_key" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="customer_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owner_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_filter_prefixes" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="vlan" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="amazon_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="amazon_side_asn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_device" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bgp_auth_key" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="customer_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
