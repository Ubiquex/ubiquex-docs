---
title: "aws_dx_connection_confirmation"
description: "Real, generated bindings for `aws_dx_connection_confirmation`."
---

`aws_dx_connection_confirmation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connection confirmation"})

    		ubx.Resource(dx.ConnectionConfirmation, "ci-runner", dx.ConnectionConfirmationConfig{
    			ConnectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectionConfirmation } from "jsr:@ubx/sdk-aws/aws/dx/connection_confirmation";

    export default stack("payments", () => {
      intent({ summary: "payments own connection confirmation" });

      resource(ConnectionConfirmation, "ci-runner", {
        connectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dx import ConnectionConfirmation, ConnectionConfirmationConfig

    def describe():
        ubx.intent("payments own connection confirmation")

        ubx.resource(ConnectionConfirmation, "ci-runner", ConnectionConfirmationConfig(
            connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dx_connection_confirmation, with connection id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
