---
title: "aws_dx_connection_confirmation"
description: "Real, generated bindings for `aws_dx_connection_confirmation`."
---

`aws_dx_connection_confirmation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/dx"

    cfg := dx.ConnectionConfirmationConfig{
        ConnectionId: "example",
    }
    ubx.Resource(dx.ConnectionConfirmation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionConfirmation } from
      "jsr:@ubx/sdk-aws/aws/dx/connection_confirmation";

    resource(ConnectionConfirmation, "example", {
      connectionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dx import (
        ConnectionConfirmation,
        ConnectionConfirmationConfig,
    )

    cfg = ConnectionConfirmationConfig(
        connection_id="example",
    )
    ubx.resource(ConnectionConfirmation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dx_connection_confirmation with connection_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
