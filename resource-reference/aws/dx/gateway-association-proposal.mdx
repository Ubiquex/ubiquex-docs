---
title: "aws_dx_gateway_association_proposal"
description: "Real, generated bindings for `aws_dx_gateway_association_proposal`."
---

`aws_dx_gateway_association_proposal` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway association proposal"})

    		ubx.Resource(dx.GatewayAssociationProposal, "ci-runner", dx.GatewayAssociationProposalConfig{
    			AssociatedGatewayId:     "example",
    			DxGatewayId:             "example",
    			DxGatewayOwnerAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayAssociationProposal } from "jsr:@ubx/sdk-aws/aws/dx/gateway_association_proposal";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway association proposal" });

      resource(GatewayAssociationProposal, "ci-runner", {
        associatedGatewayId: "example",
        dxGatewayId: "example",
        dxGatewayOwnerAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dx import GatewayAssociationProposal, GatewayAssociationProposalConfig

    def describe():
        ubx.intent("payments own gateway association proposal")

        ubx.resource(GatewayAssociationProposal, "ci-runner", GatewayAssociationProposalConfig(
            associated_gateway_id="example",
            dx_gateway_id="example",
            dx_gateway_owner_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dx_gateway_association_proposal, with associated
    gateway id example, dx gateway id example, dx gateway owner
    account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_prefixes" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="associated_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dx_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dx_gateway_owner_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="allowed_prefixes" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="associated_gateway_owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="associated_gateway_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
