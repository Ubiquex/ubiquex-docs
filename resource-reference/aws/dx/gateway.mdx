---
title: "aws_dx_gateway"
description: "Real, generated bindings for `aws_dx_gateway`."
---

`aws_dx_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"

    cfg := dx.GatewayConfig{
        AmazonSideAsn: "example",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(dx.Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Gateway } from "jsr:@ubx/sdk-aws/aws/dx/gateway";

    resource(Gateway, "example", {
      amazonSideAsn: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dx import Gateway, GatewayConfig

    cfg = GatewayConfig(
        amazon_side_asn="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dx_gateway with amazon_side_asn, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="amazon_side_asn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
