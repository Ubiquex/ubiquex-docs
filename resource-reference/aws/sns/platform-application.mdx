---
title: "aws_sns_platform_application"
description: "Real, generated bindings for `aws_sns_platform_application`."
---

`aws_sns_platform_application` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform application"})

    		ubx.Resource(sns.PlatformApplication, "ci-runner", sns.PlatformApplicationConfig{
    			Name:                         "ci-runner",
    			Platform:                     "example",
    			PlatformCredential:           "example",
    			ApplePlatformBundleId:        "example",
    			ApplePlatformTeamId:          "example",
    			EventDeliveryFailureTopicArn: "arn:aws:iam::123456789012:policy/ci",
    			EventEndpointCreatedTopicArn: "arn:aws:iam::123456789012:policy/ci",
    			EventEndpointDeletedTopicArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformApplication } from "jsr:@ubx/sdk-aws/aws/sns/platform_application";

    export default stack("payments", () => {
      intent({ summary: "payments own platform application" });

      resource(PlatformApplication, "ci-runner", {
        name: "ci-runner",
        platform: "example",
        platformCredential: "example",
        applePlatformBundleId: "example",
        applePlatformTeamId: "example",
        eventDeliveryFailureTopicArn: "arn:aws:iam::123456789012:policy/ci",
        eventEndpointCreatedTopicArn: "arn:aws:iam::123456789012:policy/ci",
        eventEndpointDeletedTopicArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sns import PlatformApplication, PlatformApplicationConfig

    def describe():
        ubx.intent("payments own platform application")

        ubx.resource(PlatformApplication, "ci-runner", PlatformApplicationConfig(
            name="ci-runner",
            platform="example",
            platform_credential="example",
            apple_platform_bundle_id="example",
            apple_platform_team_id="example",
            event_delivery_failure_topic_arn="arn:aws:iam::123456789012:policy/ci",
            event_endpoint_created_topic_arn="arn:aws:iam::123456789012:policy/ci",
            event_endpoint_deleted_topic_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sns_platform_application called "ci-runner", with
    platform example, platform credential example, apple platform
    bundle id example, apple platform team id example, event delivery
    failure topic arn arn:aws:iam::123456789012:policy/ci, event
    endpoint created topic arn arn:aws:iam::123456789012:policy/ci,
    event endpoint deleted topic arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="apple_platform_bundle_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="apple_platform_team_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_delivery_failure_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_endpoint_created_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_endpoint_deleted_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_endpoint_updated_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="failure_feedback_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform_credential" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform_principal" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="success_feedback_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="success_feedback_sample_rate" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
