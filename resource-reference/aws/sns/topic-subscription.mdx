---
title: "aws_sns_topic_subscription"
description: "Real, generated bindings for `aws_sns_topic_subscription`."
---

`aws_sns_topic_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own topic subscription"})

    		ubx.Resource(sns.TopicSubscription, "ci-runner", sns.TopicSubscriptionConfig{
    			Endpoint:                     "example",
    			Protocol:                     "example",
    			TopicArn:                     "arn:aws:iam::123456789012:policy/ci",
    			ConfirmationTimeoutInMinutes: 1,
    			DeliveryPolicy:               "example",
    			EndpointAutoConfirms:         true,
    			FilterPolicy:                 "example",
    			RawMessageDelivery:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TopicSubscription } from "jsr:@ubx/sdk-aws/aws/sns/topic_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own topic subscription" });

      resource(TopicSubscription, "ci-runner", {
        endpoint: "example",
        protocol: "example",
        topicArn: "arn:aws:iam::123456789012:policy/ci",
        confirmationTimeoutInMinutes: 1,
        deliveryPolicy: "example",
        endpointAutoConfirms: true,
        filterPolicy: "example",
        rawMessageDelivery: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sns import TopicSubscription, TopicSubscriptionConfig

    def describe():
        ubx.intent("payments own topic subscription")

        ubx.resource(TopicSubscription, "ci-runner", TopicSubscriptionConfig(
            endpoint="example",
            protocol="example",
            topic_arn="arn:aws:iam::123456789012:policy/ci",
            confirmation_timeout_in_minutes=1,
            delivery_policy="example",
            endpoint_auto_confirms=True,
            filter_policy="example",
            raw_message_delivery=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sns_topic_subscription, with endpoint example,
    protocol example, topic arn arn:aws:iam::123456789012:policy/ci,
    confirmation timeout in minutes 1, delivery policy example,
    endpoint auto confirms true, filter policy example, raw message
    delivery true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="confirmation_timeout_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="delivery_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="endpoint_auto_confirms" type="bool">
  Optional.
</ResponseField>

<ResponseField name="filter_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="filter_policy_scope" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="raw_message_delivery" type="bool">
  Optional.
</ResponseField>

<ResponseField name="redrive_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replay_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="subscription_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="topic_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="confirmation_was_authenticated" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="filter_policy_scope" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="pending_confirmation" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
