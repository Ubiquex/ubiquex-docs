---
title: "aws_dynamodb_table_item"
description: "Real, generated bindings for `aws_dynamodb_table_item`."
---

`aws_dynamodb_table_item` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table item"})

    		ubx.Resource(dynamodb.TableItem, "ci-runner", dynamodb.TableItemConfig{
    			HashKey:   "example",
    			Item:      "example",
    			TableName: "example-table-name",
    			RangeKey:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TableItem } from "jsr:@ubx/sdk-aws/aws/dynamodb/table_item";

    export default stack("payments", () => {
      intent({ summary: "payments own table item" });

      resource(TableItem, "ci-runner", {
        hashKey: "example",
        item: "example",
        tableName: "example-table-name",
        rangeKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dynamodb import TableItem, TableItemConfig

    def describe():
        ubx.intent("payments own table item")

        ubx.resource(TableItem, "ci-runner", TableItemConfig(
            hash_key="example",
            item="example",
            table_name="example-table-name",
            range_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dynamodb_table_item, with hash key example, item
    example, table name example-table-name, range key example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hash_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="item" type="string" required>
  Required.
</ResponseField>

<ResponseField name="range_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="hash_key_value" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="range_key_value" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
