---
title: "aws_dynamodb_global_secondary_index"
description: "Real, generated bindings for `aws_dynamodb_global_secondary_index`."
---

`aws_dynamodb_global_secondary_index` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global secondary index"})

    		ubx.Resource(dynamodb.GlobalSecondaryIndex, "ci-runner", dynamodb.GlobalSecondaryIndexConfig{
    			IndexName: "example-index-name",
    			TableName: "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalSecondaryIndex } from "jsr:@ubx/sdk-aws/aws/dynamodb/global_secondary_index";

    export default stack("payments", () => {
      intent({ summary: "payments own global secondary index" });

      resource(GlobalSecondaryIndex, "ci-runner", {
        indexName: "example-index-name",
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dynamodb import GlobalSecondaryIndex, GlobalSecondaryIndexConfig

    def describe():
        ubx.intent("payments own global secondary index")

        ubx.resource(GlobalSecondaryIndex, "ci-runner", GlobalSecondaryIndexConfig(
            index_name="example-index-name",
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dynamodb_global_secondary_index, with index name
    example-index-name, table name example-table-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="index_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="warm_throughput" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="read_units_per_second" type="number">
    </ResponseField>
    <ResponseField name="write_units_per_second" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="warm_throughput" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="read_units_per_second" type="number">
    </ResponseField>
    <ResponseField name="write_units_per_second" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>
