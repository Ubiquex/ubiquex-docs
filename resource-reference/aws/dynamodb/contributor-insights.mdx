---
title: "aws_dynamodb_contributor_insights"
description: "Real, generated bindings for `aws_dynamodb_contributor_insights`."
---

`aws_dynamodb_contributor_insights` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dynamodb"

    cfg := dynamodb.ContributorInsightsConfig{
        TableName: "example-table-name",
        IndexName: "example-index-name",
        Mode: "example",
    }
    ubx.Resource(dynamodb.ContributorInsights, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContributorInsights } from
      "jsr:@ubx/sdk-aws/aws/dynamodb/contributor_insights";

    resource(ContributorInsights, "example", {
      tableName: "example",
      indexName: "example",
      mode: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dynamodb import (
        ContributorInsights,
        ContributorInsightsConfig,
    )

    cfg = ContributorInsightsConfig(
        table_name="example",
        index_name="example",
        mode="example",
    )
    ubx.resource(ContributorInsights, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dynamodb_contributor_insights with table_name,
index_name, mode.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="index_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
