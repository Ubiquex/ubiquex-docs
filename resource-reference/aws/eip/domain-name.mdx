---
title: "aws_eip_domain_name"
description: "Real, generated bindings for `aws_eip_domain_name`."
---

`aws_eip_domain_name` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/eip"

    cfg := eip.DomainNameConfig{
        AllocationId: "example",
        DomainName: "example-domain-name",
    }
    ubx.Resource(eip.DomainName, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainName } from
      "jsr:@ubx/sdk-aws/aws/eip/domain_name";

    resource(DomainName, "example", {
      allocationId: "example",
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.eip import DomainName, DomainNameConfig

    cfg = DomainNameConfig(
        allocation_id="example",
        domain_name="example",
    )
    ubx.resource(DomainName, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_eip_domain_name with allocation_id, domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allocation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ptr_record" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
