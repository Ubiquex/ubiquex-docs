---
title: "aws_eip_domain_name"
description: "Real, generated bindings for `aws_eip_domain_name`."
---

`aws_eip_domain_name` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eip "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eip"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain name"})

    		ubx.Resource(eip.DomainName, "ci-runner", eip.DomainNameConfig{
    			AllocationId: "example",
    			DomainName:   "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainName } from "jsr:@ubx/sdk-aws/aws/eip/domain_name";

    export default stack("payments", () => {
      intent({ summary: "payments own domain name" });

      resource(DomainName, "ci-runner", {
        allocationId: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eip import DomainName, DomainNameConfig

    def describe():
        ubx.intent("payments own domain name")

        ubx.resource(DomainName, "ci-runner", DomainNameConfig(
            allocation_id="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eip_domain_name, with allocation id example, domain
    name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allocation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ptr_record" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
