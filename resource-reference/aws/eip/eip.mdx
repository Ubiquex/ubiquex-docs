---
title: "aws_eip"
description: "`aws_eip` allocates a real, static public IPv4 (or IPv6) address to a caller's own AWS account..."
---

`aws_eip` allocates a real, static public IPv4 (or IPv6) address to a caller's own AWS account, remaining reserved and reachable at the same address even as it's associated with, or moved between, different EC2 instances or network interfaces over time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eip "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eip"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own eip"})

    		ubx.Resource(eip.Eip, "example", eip.EipConfig{
    			Address:            "example",
    			Domain:             "example",
    			InstanceId:         "example",
    			IpamPoolId:         "example",
    			NetworkBorderGroup: "example",
    			PublicIpv4Pool:     "example",
    			Tags:               []map[string]any{{"key": "example"}},
    			TransferAddress:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Eip } from "@ubx/sdk-aws/aws/eip/eip";

    export default stack("example", () => {
      intent({ summary: "example own eip" });

      resource(Eip, "example", {
        address: "example",
        domain: "example",
        instanceId: "example",
        ipamPoolId: "example",
        networkBorderGroup: "example",
        publicIpv4Pool: "example",
        tags: [{ key: "example" }],
        transferAddress: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eip import Eip, EipConfig

    def describe():
        ubx.intent("example own eip")

        ubx.resource(Eip, "example", EipConfig(
            address="example",
            domain="example",
            instance_id="example",
            ipam_pool_id="example",
            network_border_group="example",
            public_ipv4_pool="example",
            tags=[{"key": "example"}],
            transfer_address="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eip, with address example, domain example, instance id
    example, ipam pool id example, network border group example,
    public ipv4 pool example, tags a set value, transfer address
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address" type="string">
  An Elastic IP address or a carrier IP address in a Wavelength Zone. Optional.
</ResponseField>

<ResponseField name="domain" type="string">
  The network (``vpc``). If you define an Elastic IP address and associate it with a VPC that is defined in the same template, you must declare a dependency on the VPC-gateway attachment by using the [DependsOn Attribute](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-attribute-dependson.html) on this resource. Optional.
</ResponseField>

<ResponseField name="instance_id" type="string">
  The ID of the instance. Updates to the ``InstanceId`` property may require *some interruptions*. Updates on an EIP reassociates the address on its associated resource. Optional.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string">
  The ID of an IPAM pool which has an Amazon-provided or BYOIP public IPv4 CIDR provisioned to it. For more information, see [Allocate sequential Elastic IP addresses from an IPAM pool](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-eip-pool.html) in the *Amazon VPC IPAM User Guide*. Optional.
</ResponseField>

<ResponseField name="network_border_group" type="string">
  A unique set of Availability Zones, Local Zones, or Wavelength Zones from which AWS advertises IP addresses. Use this parameter to limit the IP address to this location. IP addresses cannot move between network border groups. Use [DescribeAvailabilityZones](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeAvailabilityZones.html) to view the network border groups. Optional.
</ResponseField>

<ResponseField name="public_ipv4_pool" type="string">
  The ID of an address pool that you own. Use this parameter to let Amazon EC2 select an address from the address pool. Updates to the ``PublicIpv4Pool`` property may require *some interruptions*. Updates on an EIP reassociates the address on its associated resource. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Any tags assigned to the Elastic IP address. Updates to the ``Tags`` property may require *some interruptions*. Updates on an EIP reassociates the address on its associated resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag attached to the Elastic IP address. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transfer_address" type="string">
  The Elastic IP address you are accepting for transfer. You can only accept one transferred address. For more information on Elastic IP address transfers, see [Transfer Elastic IP addresses](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-eips.html#transfer-EIPs-intro) in the *Amazon Virtual Private Cloud User Guide*. Optional.
</ResponseField>

## Output properties

<ResponseField name="allocation_id" type="string">
  The unique identifier assigned by AWS to this Elastic IP address when it was allocated for use in a VPC. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="public_ip" type="string">
  The public IPv4 address that AWS allocates to the Elastic IP (EIP) resource, which you can associate with an EC2 instance or network interface. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
