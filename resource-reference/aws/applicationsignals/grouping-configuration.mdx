---
title: "aws_application_signals_grouping_configuration"
description: "`aws_application_signals_grouping_configuration` defines custom attributes, telemetry, AWS tags, or predefined mappings..."
---

`aws_application_signals_grouping_configuration` defines custom attributes, telemetry, AWS tags, or predefined mappings, that determine how CloudWatch Application Signals groups services in its console and APIs, overriding Application Signals's own default grouping by service dependency graph.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationsignals "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/applicationsignals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own grouping configuration"})

    		ubx.Resource(applicationsignals.GroupingConfiguration, "example", applicationsignals.GroupingConfigurationConfig{
    			GroupingAttributeDefinitions: []map[string]any{{"default_grouping_value": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupingConfiguration } from "@ubx/sdk-aws/aws/applicationsignals/grouping_configuration";

    export default stack("example", () => {
      intent({ summary: "example own grouping configuration" });

      resource(GroupingConfiguration, "example", {
        groupingAttributeDefinitions: [{ defaultGroupingValue: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.applicationsignals import GroupingConfiguration, GroupingConfigurationConfig
    from ubx.aws.applicationsignals.grouping_configuration import GroupingConfiguration_GroupingAttributeDefinitions

    def describe():
        ubx.intent("example own grouping configuration")

        ubx.resource(GroupingConfiguration, "example", GroupingConfigurationConfig(
            grouping_attribute_definitions=[GroupingConfiguration_GroupingAttributeDefinitions(default_grouping_value="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_application_signals_grouping_configuration, with
    grouping attribute definitions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="grouping_attribute_definitions" type="list(object)" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="default_grouping_value" type="string">
      Specifies the fallback value used to group telemetry under a particular application or service when the source attribute defined by this grouping attribute definition is missing from the telemetry data. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="grouping_name" type="string">
      The name of the grouping attribute (used as the key in a grouping attribute definition) that identifies how telemetry is grouped in Application Signals, such as an environment or service name. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="grouping_source_keys" type="list(string)">
      Specifies the list of source telemetry attribute keys (e.g., service.name, environment) that are combined to derive the value of the corresponding grouping attribute in this grouping attribute definition, determining how Application Signals groups telemetry into applications or services. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="account_id" type="string">
  The identifier for the specified AWS account. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of when the grouping configuration was last updated, as set by the AWS service. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
