---
title: "aws_route53recoveryreadiness_cell"
description: "Real, generated bindings for `aws_route53recoveryreadiness_cell`."
---

`aws_route53recoveryreadiness_cell` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoveryreadiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoveryreadiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cell"})

    		ubx.Resource(route53recoveryreadiness.Cell, "ci-runner", route53recoveryreadiness.CellConfig{
    			CellName: "example-cell-name",
    			Cells:    []string{"example"},
    			Tags:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cell } from "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/cell";

    export default stack("payments", () => {
      intent({ summary: "payments own cell" });

      resource(Cell, "ci-runner", {
        cellName: "example-cell-name",
        cells: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoveryreadiness import Cell, CellConfig

    def describe():
        ubx.intent("payments own cell")

        ubx.resource(Cell, "ci-runner", CellConfig(
            cell_name="example-cell-name",
            cells=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53recoveryreadiness_cell, with cell name
    example-cell-name, cells a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cell_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cells" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parent_readiness_scopes" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
