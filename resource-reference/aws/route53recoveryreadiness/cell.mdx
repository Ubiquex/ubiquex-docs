---
title: "aws_route53recoveryreadiness_cell"
description: "Real, generated bindings for `aws_route53recoveryreadiness_cell`."
---

`aws_route53recoveryreadiness_cell` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/route53recoveryreadiness"

    cfg := route53recoveryreadiness.CellConfig{
        CellName: "example-cell-name",
        Cells: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(route53recoveryreadiness.Cell, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Cell } from
      "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/cell";

    resource(Cell, "example", {
      cellName: "example",
      cells: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoveryreadiness import Cell, CellConfig

    cfg = CellConfig(
        cell_name="example",
        cells=["example"],
        tags={"key": "value"},
    )
    ubx.resource(Cell, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoveryreadiness_cell with cell_name,
cells, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cell_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cells" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parent_readiness_scopes" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
