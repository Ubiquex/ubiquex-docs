---
title: "aws_route53recoveryreadiness_resource_set"
description: "Real, generated bindings for `aws_route53recoveryreadiness_resource_set`."
---

`aws_route53recoveryreadiness_resource_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoveryreadiness"

    cfg := route53recoveryreadiness.ResourceSetConfig{
        ResourceSetName: "example-resource-set-name",
        ResourceSetType: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        route53recoveryreadiness.ResourceSet,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceSet } from
      "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/resource_set";

    resource(ResourceSet, "example", {
      resourceSetName: "example",
      resourceSetType: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoveryreadiness import (
        ResourceSet,
        ResourceSetConfig,
    )

    cfg = ResourceSetConfig(
        resource_set_name="example",
        resource_set_type="example",
        tags={"key": "value"},
    )
    ubx.resource(ResourceSet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoveryreadiness_resource_set with
resource_set_name, resource_set_type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_set_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_set_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
