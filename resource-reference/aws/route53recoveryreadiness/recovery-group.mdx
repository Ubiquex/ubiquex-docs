---
title: "aws_route53recoveryreadiness_recovery_group"
description: "Real, generated bindings for `aws_route53recoveryreadiness_recovery_group`."
---

`aws_route53recoveryreadiness_recovery_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoveryreadiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoveryreadiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own recovery group"})

    		ubx.Resource(route53recoveryreadiness.RecoveryGroup, "ci-runner", route53recoveryreadiness.RecoveryGroupConfig{
    			RecoveryGroupName: "example-recovery-group-name",
    			Cells:             []string{"example"},
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecoveryGroup } from "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/recovery_group";

    export default stack("payments", () => {
      intent({ summary: "payments own recovery group" });

      resource(RecoveryGroup, "ci-runner", {
        recoveryGroupName: "example-recovery-group-name",
        cells: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoveryreadiness import RecoveryGroup, RecoveryGroupConfig

    def describe():
        ubx.intent("payments own recovery group")

        ubx.resource(RecoveryGroup, "ci-runner", RecoveryGroupConfig(
            recovery_group_name="example-recovery-group-name",
            cells=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53recoveryreadiness_recovery_group, with recovery
    group name example-recovery-group-name, cells a set value, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cells" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="recovery_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
