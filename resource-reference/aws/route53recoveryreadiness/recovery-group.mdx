---
title: "aws_route53recoveryreadiness_recovery_group"
description: "Real, generated bindings for `aws_route53recoveryreadiness_recovery_group`."
---

`aws_route53recoveryreadiness_recovery_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoveryreadiness"

    cfg := route53recoveryreadiness.RecoveryGroupConfig{
        RecoveryGroupName: "example-recovery-group-name",
        Cells: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        route53recoveryreadiness.RecoveryGroup,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RecoveryGroup } from
      "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/recovery_group";

    resource(RecoveryGroup, "example", {
      recoveryGroupName: "example",
      cells: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoveryreadiness import (
        RecoveryGroup,
        RecoveryGroupConfig,
    )

    cfg = RecoveryGroupConfig(
        recovery_group_name="example",
        cells=["example"],
        tags={"key": "value"},
    )
    ubx.resource(RecoveryGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoveryreadiness_recovery_group with
recovery_group_name, cells, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cells" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="recovery_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
