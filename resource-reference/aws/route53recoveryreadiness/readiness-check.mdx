---
title: "aws_route53recoveryreadiness_readiness_check"
description: "Real, generated bindings for `aws_route53recoveryreadiness_readiness_check`."
---

`aws_route53recoveryreadiness_readiness_check` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoveryreadiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoveryreadiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own readiness check"})

    		ubx.Resource(route53recoveryreadiness.ReadinessCheck, "ci-runner", route53recoveryreadiness.ReadinessCheckConfig{
    			ReadinessCheckName: "example-readiness-check-name",
    			ResourceSetName:    "example-resource-set-name",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReadinessCheck } from "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/readiness_check";

    export default stack("payments", () => {
      intent({ summary: "payments own readiness check" });

      resource(ReadinessCheck, "ci-runner", {
        readinessCheckName: "example-readiness-check-name",
        resourceSetName: "example-resource-set-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoveryreadiness import ReadinessCheck, ReadinessCheckConfig

    def describe():
        ubx.intent("payments own readiness check")

        ubx.resource(ReadinessCheck, "ci-runner", ReadinessCheckConfig(
            readiness_check_name="example-readiness-check-name",
            resource_set_name="example-resource-set-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53recoveryreadiness_readiness_check, with
    readiness check name example-readiness-check-name, resource set
    name example-resource-set-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="readiness_check_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_set_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
