---
title: "aws_route53recoveryreadiness_readiness_check"
description: "Real, generated bindings for `aws_route53recoveryreadiness_readiness_check`."
---

`aws_route53recoveryreadiness_readiness_check` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/route53recoveryreadiness"

    cfg := route53recoveryreadiness.ReadinessCheckConfig{
        ReadinessCheckName: "example-readiness-check-name",
        ResourceSetName: "example-resource-set-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        route53recoveryreadiness.ReadinessCheck,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReadinessCheck } from
      "jsr:@ubx/sdk-aws/aws/route53recoveryreadiness/readiness_check";

    resource(ReadinessCheck, "example", {
      readinessCheckName: "example",
      resourceSetName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoveryreadiness import (
        ReadinessCheck,
        ReadinessCheckConfig,
    )

    cfg = ReadinessCheckConfig(
        readiness_check_name="example",
        resource_set_name="example",
        tags={"key": "value"},
    )
    ubx.resource(ReadinessCheck, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoveryreadiness_readiness_check with
readiness_check_name, resource_set_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="readiness_check_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_set_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
