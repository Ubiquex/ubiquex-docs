---
title: "aws_lex_intent"
description: "Real, generated bindings for `aws_lex_intent`."
---

`aws_lex_intent` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own intent"})

    		ubx.Resource(lex.Intent, "ci-runner", lex.IntentConfig{
    			Name:                  "ci-runner",
    			CreateVersion:         true,
    			Description:           "Managed by ubx.",
    			ParentIntentSignature: "example",
    			SampleUtterances:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Intent } from "jsr:@ubx/sdk-aws/aws/lex/intent";

    export default stack("payments", () => {
      intent({ summary: "payments own intent" });

      resource(Intent, "ci-runner", {
        name: "ci-runner",
        createVersion: true,
        description: "Managed by ubx.",
        parentIntentSignature: "example",
        sampleUtterances: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lex import Intent, IntentConfig

    def describe():
        ubx.intent("payments own intent")

        ubx.resource(Intent, "ci-runner", IntentConfig(
            name="ci-runner",
            create_version=True,
            description="Managed by ubx.",
            parent_intent_signature="example",
            sample_utterances=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lex_intent called "ci-runner", with create version
    true, description Managed by ubx., parent intent signature
    example, sample utterances a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="create_version" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent_intent_signature" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sample_utterances" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="checksum" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
