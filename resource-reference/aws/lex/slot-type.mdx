---
title: "aws_lex_slot_type"
description: "Real, generated bindings for `aws_lex_slot_type`."
---

`aws_lex_slot_type` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lex"

    cfg := lex.SlotTypeConfig{
        Name: "example-name",
        CreateVersion: true,
        Description: "example",
    }
    ubx.Resource(lex.SlotType, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SlotType } from "jsr:@ubx/sdk-aws/aws/lex/slot_type";

    resource(SlotType, "example", {
      name: "example",
      createVersion: true,
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lex import SlotType, SlotTypeConfig

    cfg = SlotTypeConfig(
        name="example",
        create_version=True,
        description="example",
    )
    ubx.resource(SlotType, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lex_slot_type with name, create_version,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="create_version" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value_selection_strategy" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="checksum" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
