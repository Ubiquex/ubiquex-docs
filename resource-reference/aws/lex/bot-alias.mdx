---
title: "aws_lex_bot_alias"
description: "Real, generated bindings for `aws_lex_bot_alias`."
---

`aws_lex_bot_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lex"

    cfg := lex.BotAliasConfig{
        BotName: "example-bot-name",
        BotVersion: "example",
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(lex.BotAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BotAlias } from "jsr:@ubx/sdk-aws/aws/lex/bot_alias";

    resource(BotAlias, "example", {
      botName: "example",
      botVersion: "example",
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lex import BotAlias, BotAliasConfig

    cfg = BotAliasConfig(
        bot_name="example",
        bot_version="example",
        name="example",
        description="example",
    )
    ubx.resource(BotAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lex_bot_alias with bot_name, bot_version, name,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bot_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="checksum" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
