---
title: "aws_evidently_launch"
description: "Real, generated bindings for `aws_evidently_launch`."
---

`aws_evidently_launch` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/evidently"

    cfg := evidently.LaunchConfig{
        Name: "example-name",
        Project: "example",
        Description: "example",
        RandomizationSalt: "example",
    }
    ubx.Resource(evidently.Launch, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Launch } from "jsr:@ubx/sdk-aws/aws/evidently/launch";

    resource(Launch, "example", {
      name: "example",
      project: "example",
      description: "example",
      randomizationSalt: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.evidently import Launch, LaunchConfig

    cfg = LaunchConfig(
        name="example",
        project="example",
        description="example",
        randomization_salt="example",
    )
    ubx.resource(Launch, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_evidently_launch with name, project, description,
randomization_salt.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string" required>
  Required.
</ResponseField>

<ResponseField name="randomization_salt" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="execution" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="ended_time" type="string">
    </ResponseField>
    <ResponseField name="started_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
