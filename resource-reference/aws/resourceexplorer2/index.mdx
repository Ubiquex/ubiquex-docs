---
title: "aws_resource_explorer2_index"
description: "`aws_resource_explorer2_index` creates a real, per-Region search index for AWS Resource Explorer..."
---

`aws_resource_explorer2_index` creates a real, per-Region search index for AWS Resource Explorer, the prerequisite that must exist in a Region before an `aws_resource_explorer2_view` can be created there.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resourceexplorer2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/resourceexplorer2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own index"})

    		ubx.Resource(resourceexplorer2.Index, "example", resourceexplorer2.IndexConfig{
    			Type: "example",
    			Tags: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Index } from "@ubx/sdk-aws/aws/resourceexplorer2/index";

    export default stack("example", () => {
      intent({ summary: "example own index" });

      resource(Index, "example", {
        type: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.resourceexplorer2 import Index, IndexConfig

    def describe():
        ubx.intent("example own index")

        ubx.resource(Index, "example", IndexConfig(
            type="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resource_explorer2_index, with type example, tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Specifies the index type, either LOCAL (indexes resources in your account and region) or AGGREGATOR (aggregates indices from multiple accounts and regions). Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Resource Explorer index, assigned by AWS when the index is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="index_state" type="string">
  The current lifecycle state of the index, such as ACTIVE, CREATING, UPDATING, or DELETING, as reported by AWS Resource Explorer. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
