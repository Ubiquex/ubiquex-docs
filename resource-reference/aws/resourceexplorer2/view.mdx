---
title: "aws_resourceexplorer2_view"
description: "Real, generated bindings for `aws_resourceexplorer2_view`."
---

`aws_resourceexplorer2_view` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/resourceexplorer2"

    cfg := resourceexplorer2.ViewConfig{
        Name: "example-name",
        DefaultView: true,
        Scope: "example",
    }
    ubx.Resource(resourceexplorer2.View, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { View } from
      "jsr:@ubx/sdk-aws/aws/resourceexplorer2/view";

    resource(View, "example", {
      name: "example",
      defaultView: true,
      scope: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.resourceexplorer2 import View, ViewConfig

    cfg = ViewConfig(
        name="example",
        default_view=True,
        scope="example",
    )
    ubx.resource(View, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_resourceexplorer2_view with name, default_view,
scope.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_view" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_view" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
