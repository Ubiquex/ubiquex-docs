---
title: "aws_rolesanywhere_profile"
description: "Real, generated bindings for `aws_rolesanywhere_profile`."
---

`aws_rolesanywhere_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rolesanywhere "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rolesanywhere"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own profile"})

    		ubx.Resource(rolesanywhere.Profile, "ci-runner", rolesanywhere.ProfileConfig{
    			Name:                      "ci-runner",
    			AcceptRoleSessionName:     true,
    			Enabled:                   true,
    			ManagedPolicyArns:         []string{"example"},
    			RequireInstanceProperties: true,
    			RoleArns:                  []string{"example"},
    			SessionPolicy:             "example",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Profile } from "jsr:@ubx/sdk-aws/aws/rolesanywhere/profile";

    export default stack("payments", () => {
      intent({ summary: "payments own profile" });

      resource(Profile, "ci-runner", {
        name: "ci-runner",
        acceptRoleSessionName: true,
        enabled: true,
        managedPolicyArns: ["example"],
        requireInstanceProperties: true,
        roleArns: ["example"],
        sessionPolicy: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rolesanywhere import Profile, ProfileConfig

    def describe():
        ubx.intent("payments own profile")

        ubx.resource(Profile, "ci-runner", ProfileConfig(
            name="ci-runner",
            accept_role_session_name=True,
            enabled=True,
            managed_policy_arns=["example"],
            require_instance_properties=True,
            role_arns=["example"],
            session_policy="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rolesanywhere_profile called "ci-runner", with accept
    role session name true, enabled true, managed policy arns a set
    value, require instance properties true, role arns a set value,
    session policy example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accept_role_session_name" type="bool">
  Optional.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="managed_policy_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="require_instance_properties" type="bool">
  Optional.
</ResponseField>

<ResponseField name="role_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="session_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
