---
title: "aws_dlm_lifecycle_policy"
description: "Real, generated bindings for `aws_dlm_lifecycle_policy`."
---

`aws_dlm_lifecycle_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dlm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dlm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lifecycle policy"})

    		ubx.Resource(dlm.LifecyclePolicy, "ci-runner", dlm.LifecyclePolicyConfig{
    			Description:      "Managed by ubx.",
    			ExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			DefaultPolicy:    "example",
    			State:            "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LifecyclePolicy } from "jsr:@ubx/sdk-aws/aws/dlm/lifecycle_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own lifecycle policy" });

      resource(LifecyclePolicy, "ci-runner", {
        description: "Managed by ubx.",
        executionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        defaultPolicy: "example",
        state: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dlm import LifecyclePolicy, LifecyclePolicyConfig

    def describe():
        ubx.intent("payments own lifecycle policy")

        ubx.resource(LifecyclePolicy, "ci-runner", LifecyclePolicyConfig(
            description="Managed by ubx.",
            execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            default_policy="example",
            state="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dlm_lifecycle_policy, with description Managed by
    ubx., execution role arn arn:aws:iam::123456789012:policy/ci,
    default policy example, state example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
