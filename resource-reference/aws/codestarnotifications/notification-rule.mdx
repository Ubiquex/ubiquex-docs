---
title: "aws_codestarnotifications_notification_rule"
description: "Real, generated bindings for `aws_codestarnotifications_notification_rule`."
---

`aws_codestarnotifications_notification_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestarnotifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codestarnotifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own notification rule"})

    		ubx.Resource(codestarnotifications.NotificationRule, "ci-runner", codestarnotifications.NotificationRuleConfig{
    			DetailType:   "example",
    			EventTypeIds: []string{"example"},
    			Name:         "ci-runner",
    			Resource:     "example",
    			Status:       "example",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NotificationRule } from "jsr:@ubx/sdk-aws/aws/codestarnotifications/notification_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own notification rule" });

      resource(NotificationRule, "ci-runner", {
        detailType: "example",
        eventTypeIds: ["example"],
        name: "ci-runner",
        resource: "example",
        status: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codestarnotifications import NotificationRule, NotificationRuleConfig

    def describe():
        ubx.intent("payments own notification rule")

        ubx.resource(NotificationRule, "ci-runner", NotificationRuleConfig(
            detail_type="example",
            event_type_ids=["example"],
            name="ci-runner",
            resource="example",
            status="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codestarnotifications_notification_rule called
    "ci-runner", with detail type example, event type ids a set
    value, resource example, status example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="detail_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="event_type_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
