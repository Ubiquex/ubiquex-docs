---
title: "aws_direct_connect_direct_connect_gateway"
description: "`aws_direct_connect_direct_connect_gateway` is a real, global resource that lets a single Direct Connect connection reach multiple VPCs across..."
---

`aws_direct_connect_direct_connect_gateway` is a real, global resource that lets a single Direct Connect connection reach multiple VPCs across different AWS accounts and Regions, aggregating what would otherwise need a separate connection or virtual interface per VPC.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	direct "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/direct"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connect direct connect gateway"})

    		ubx.Resource(direct.ConnectDirectConnectGateway, "example", direct.ConnectDirectConnectGatewayConfig{
    			DirectConnectGatewayName: "example-direct-connect-gateway-name",
    			AmazonSideAsn:            "example",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectDirectConnectGateway } from "./local-sdk/aws/sdk/typescript/aws/direct/connect_direct_connect_gateway.ts";

    export default stack("example", () => {
      intent({ summary: "example own connect direct connect gateway" });

      resource(ConnectDirectConnectGateway, "example", {
        directConnectGatewayName: "example-direct-connect-gateway-name",
        amazonSideAsn: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.direct import ConnectDirectConnectGateway, ConnectDirectConnectGatewayConfig

    def describe():
        ubx.intent("example own connect direct connect gateway")

        ubx.resource(ConnectDirectConnectGateway, "example", ConnectDirectConnectGatewayConfig(
            direct_connect_gateway_name="example-direct-connect-gateway-name",
            amazon_side_asn="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_direct_connect_direct_connect_gateway, with direct
    connect gateway name example-direct-connect-gateway-name, amazon
    side asn example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="amazon_side_asn" type="string">
  The autonomous system number (ASN) for the Amazon side of the connection. Optional.
</ResponseField>

<ResponseField name="direct_connect_gateway_name" type="string" required>
  The name of the Direct Connect gateway. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags associated with the Direct Connect gateway. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the Direct Connect gateway, used to categorize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag key attached to the Direct Connect gateway, used for identifying and categorizing the gateway resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="direct_connect_gateway_arn" type="string">
  The ARN of the Direct Connect gateway. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="direct_connect_gateway_id" type="string">
  The ID of the Direct Connect gateway. Computed by AWS after `ship`.
</ResponseField>
