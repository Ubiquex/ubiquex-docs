---
title: "aws_ivschat_room"
description: "Real, generated bindings for `aws_ivschat_room`."
---

`aws_ivschat_room` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivschat "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ivschat"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own room"})

    		ubx.Resource(ivschat.Room, "ci-runner", ivschat.RoomConfig{
    			Name:                            "ci-runner",
    			LoggingConfigurationIdentifiers: []string{"example"},
    			Tags:                            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Room } from "jsr:@ubx/sdk-aws/aws/ivschat/room";

    export default stack("payments", () => {
      intent({ summary: "payments own room" });

      resource(Room, "ci-runner", {
        name: "ci-runner",
        loggingConfigurationIdentifiers: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ivschat import Room, RoomConfig

    def describe():
        ubx.intent("payments own room")

        ubx.resource(Room, "ci-runner", RoomConfig(
            name="ci-runner",
            logging_configuration_identifiers=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ivschat_room called "ci-runner", with logging
    configuration identifiers a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="logging_configuration_identifiers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="maximum_message_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maximum_message_rate_per_second" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maximum_message_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maximum_message_rate_per_second" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
