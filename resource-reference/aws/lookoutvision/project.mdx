---
title: "aws_lookout_vision_project"
description: "`aws_lookout_vision_project` was a real project container for training and hosting Amazon Lookout for Vision computer-vision defect-detection models."
---

Lookout for Vision is now fully discontinued: it closed to new customers on October 10, 2024, and shut down entirely on October 31, 2025, a date now in the past, so this resource describes a service no longer operable rather than a current one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutvision "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/lookoutvision"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own project"})

    		ubx.Resource(lookoutvision.Project, "example", lookoutvision.ProjectConfig{
    			ProjectName: "example-project-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Project } from "@ubx/sdk-aws/aws/lookoutvision/project";

    export default stack("example", () => {
      intent({ summary: "example own project" });

      resource(Project, "example", {
        projectName: "example-project-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lookoutvision import Project, ProjectConfig

    def describe():
        ubx.intent("example own project")

        ubx.resource(Project, "example", ProjectConfig(
            project_name="example-project-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lookout_vision_project, with project name
    example-project-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="project_name" type="string" required>
  The name of the Amazon Lookout for Vision project Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the Lookout for Vision project, which uniquely identifies the project in AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
