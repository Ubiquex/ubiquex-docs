---
title: "aws_scn_namespace"
description: "`aws_scn_namespace` groups related `aws_scn_dataset`s under AWS Supply Chain, giving a logical organizational boundary..."
---

`aws_scn_namespace` groups related `aws_scn_dataset`s under AWS Supply Chain, giving a logical organizational boundary, by business unit or data domain, within the same account rather than every dataset sitting in one flat list.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scn "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/scn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own namespace"})

    		ubx.Resource(scn.Namespace, "example", scn.NamespaceConfig{
    			InstanceId:  "example",
    			Name:        "example",
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-aws/aws/scn/namespace";

    export default stack("example", () => {
      intent({ summary: "example own namespace" });

      resource(Namespace, "example", {
        instanceId: "example",
        name: "example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.scn.namespace import Namespace, NamespaceConfig

    def describe():
        ubx.intent("example own namespace")

        ubx.resource(Namespace, "example", NamespaceConfig(
            instance_id="example",
            name="example",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_scn_namespace called "example", with instance id
    example, description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the namespace. Optional.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The Amazon Web Services Supply Chain instance identifier. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the namespace. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for the namespace. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the namespace. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  The creation time of the namespace. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The last modified time of the namespace. Computed by AWS after `ship`.
</ResponseField>
