---
title: "aws_scn_dataset"
description: "`aws_scn_dataset` defines a real dataset, its schema, partition spec, and primary keys, within AWS Supply Chain (internally namespaced SCN)..."
---

`aws_scn_dataset` defines a real dataset, its schema, partition spec, and primary keys, within AWS Supply Chain (internally namespaced SCN), the structure that data connectors and integration flows load supply chain data into for AWS Supply Chain's own analytics and demand-planning features. This resource's dedicated conceptual documentation page was not found live this session; the description above is verified against AWS's own field-level schema, which confirms it is real, rather than a fetched overview page.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	scn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/scn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dataset"})

    		ubx.Resource(scn.Dataset, "example", scn.DatasetConfig{
    			InstanceId:    "example",
    			Name:          "example",
    			Namespace:     "example",
    			Description:   "Managed by ubx.",
    			PartitionSpec: map[string]any{"fields": []map[string]any{{"name": "example-name"}}},
    			Schema:        map[string]any{"name": "example-name"},
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Dataset } from "./local-sdk/aws/sdk/typescript/aws/scn/dataset.ts";

    export default stack("example", () => {
      intent({ summary: "example own dataset" });

      resource(Dataset, "example", {
        instanceId: "example",
        name: "example",
        namespace: "example",
        description: "Managed by ubx.",
        partitionSpec: { fields: [{ name: "example-name" }] },
        schema: { name: "example-name" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.scn import Dataset, DatasetConfig

    def describe():
        ubx.intent("example own dataset")

        ubx.resource(Dataset, "example", DatasetConfig(
            instance_id="example",
            name="example",
            namespace="example",
            description="Managed by ubx.",
            partition_spec={"fields": [{"name": "example-name"}]},
            schema={"name": "example-name"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_scn_dataset called "example", with instance id
    example, namespace example, description Managed by ubx.,
    partition spec a set value, schema a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the dataset. Optional.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The Amazon Web Services Supply Chain instance identifier. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the dataset. Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The namespace of the dataset. Required.
</ResponseField>

<ResponseField name="partition_spec" type="object">
  The partition specification of the dataset. Optional.
  <Expandable title="properties">
    <ResponseField name="fields" type="list(object)" required>
      The partition fields. Required.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of a partition field (column) used in the dataset's partition specification to define how the dataset data is partitioned. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="transform" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="type" type="string">
              Defines the target data type (such as STRING, INTEGER, DATE, or TIMESTAMP) that a source field's value is transformed to when generating the partition column for the dataset's partition specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schema" type="object">
  The schema of the dataset. Optional.
  <Expandable title="properties">
    <ResponseField name="fields" type="list(object)" required>
      The list of field details of the dataset schema. Required.
      <Expandable title="properties">
        <ResponseField name="is_required" type="bool">
          Indicates whether this field must be present in every record of the dataset, affecting data validation during ingestion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of a field (column) within the schema of an AWS Supply Chain dataset, serving as the unique identifier for that attribute in the dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The data type of the field (column) in the schema for the AWS Supply Chain dataset, such as STRING, INTEGER, DOUBLE, or TIMESTAMP. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the dataset schema. Required.
    </ResponseField>
    <ResponseField name="primary_keys" type="list(object)">
      The list of primary key fields for the dataset. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of a column designated as a primary key in the dataset's schema definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for the dataset. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag attached to the AWS Supply Chain dataset, used to organize, identify, or track the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the dataset. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  The creation time of the dataset. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The last modified time of the dataset. Computed by AWS after `ship`.
</ResponseField>
