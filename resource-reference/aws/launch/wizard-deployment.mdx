---
title: "aws_launch_wizard_deployment"
description: "`aws_launch_wizard_deployment` deploys a real, sized and configured workload, third-party software such as SQL Server or SAP..."
---

`aws_launch_wizard_deployment` deploys a real, sized and configured workload, third-party software such as SQL Server or SAP, or a supported AWS workload pattern, using AWS Launch Wizard's own guided, Well-Architected-aligned automation rather than a hand-built template.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	launch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/launch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own wizard deployment"})

    		ubx.Resource(launch.WizardDeployment, "ci-runner", launch.WizardDeploymentConfig{
    			DeploymentPatternName: "example-deployment-pattern-name",
    			Name:                  "ci-runner",
    			WorkloadName:          "example-workload-name",
    			Specifications:        "example",
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { WizardDeployment } from "./local-sdk/aws/sdk/typescript/aws/launch/wizard_deployment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own wizard deployment" });

      resource(WizardDeployment, "ci-runner", {
        deploymentPatternName: "example-deployment-pattern-name",
        name: "ci-runner",
        workloadName: "example-workload-name",
        specifications: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.launch import WizardDeployment, WizardDeploymentConfig

    def describe():
        ubx.intent("payments own wizard deployment")

        ubx.resource(WizardDeployment, "ci-runner", WizardDeploymentConfig(
            deployment_pattern_name="example-deployment-pattern-name",
            name="ci-runner",
            workload_name="example-workload-name",
            specifications="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_launch_wizard_deployment called "ci-runner", with
    deployment pattern name example-deployment-pattern-name, workload
    name example-workload-name, specifications example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deployment_pattern_name" type="string" required>
  Workload deployment pattern name Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of LaunchWizard deployment Required.
</ResponseField>

<ResponseField name="specifications" type="any">
  The map of key-value pairs that define the application and infrastructure parameters (for example, instance types, subnet IDs, and security group names) used to configure the deployment for the chosen Launch Wizard pattern. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags for LaunchWizard deployment Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Launch Wizard deployment, allowing you to assign arbitrary metadata to the deployment for organizational or cost-tracking purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag assigned to the AWS Launch Wizard deployment, which lets you attach arbitrary metadata to the deployment for categorization, access control, and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_name" type="string" required>
  Workload Name for LaunchWizard deployment Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  ARN of the LaunchWizard deployment Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Timestamp of LaunchWizard deployment creation Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deleted_at" type="string">
  Timestamp of LaunchWizard deployment deletion Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_id" type="string">
  Deployment ID of the LaunchWizard deployment Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_group" type="string">
  Resource Group Name created for LaunchWizard deployment Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the Launch Wizard deployment, indicating its current lifecycle state such as 'COMPLETED', 'IN_PROGRESS', or 'FAILED'. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
