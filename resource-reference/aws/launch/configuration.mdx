---
title: "aws_launch_configuration"
description: "Real, generated bindings for `aws_launch_configuration`."
---

`aws_launch_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	launch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/launch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration"})

    		ubx.Resource(launch.Configuration, "ci-runner", launch.ConfigurationConfig{
    			ImageId:            "example",
    			InstanceType:       "example",
    			Name:               "ci-runner",
    			EnableMonitoring:   true,
    			IamInstanceProfile: "example",
    			PlacementTenancy:   "example",
    			SecurityGroups:     []string{"example"},
    			SpotPrice:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Configuration } from "jsr:@ubx/sdk-aws/aws/launch/configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration" });

      resource(Configuration, "ci-runner", {
        imageId: "example",
        instanceType: "example",
        name: "ci-runner",
        enableMonitoring: true,
        iamInstanceProfile: "example",
        placementTenancy: "example",
        securityGroups: ["example"],
        spotPrice: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.launch import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("payments own configuration")

        ubx.resource(Configuration, "ci-runner", ConfigurationConfig(
            image_id="example",
            instance_type="example",
            name="ci-runner",
            enable_monitoring=True,
            iam_instance_profile="example",
            placement_tenancy="example",
            security_groups=["example"],
            spot_price="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_launch_configuration called "ci-runner", with image id
    example, instance type example, enable monitoring true, iam
    instance profile example, placement tenancy example, security
    groups a set value, spot price example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="associate_public_ip_address" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ebs_optimized" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enable_monitoring" type="bool">
  Optional.
</ResponseField>

<ResponseField name="iam_instance_profile" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="key_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="placement_tenancy" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="spot_price" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_data" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_data_base64" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="associate_public_ip_address" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ebs_optimized" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
