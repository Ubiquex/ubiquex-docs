---
title: "aws_work_spaces_web_user_access_logging_settings"
description: "`aws_work_spaces_web_user_access_logging_settings` configures real logging of user access events, sign-ins and session starts..."
---

`aws_work_spaces_web_user_access_logging_settings` configures real logging of user access events, sign-ins and session starts, for an `aws_work_spaces_web_portal`, delivered to CloudWatch Logs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	work "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/work"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own spaces web user access logging settings"})

    		ubx.Resource(work.SpacesWebUserAccessLoggingSettings, "ci-runner", work.SpacesWebUserAccessLoggingSettingsConfig{
    			KinesisStreamArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SpacesWebUserAccessLoggingSettings } from "./local-sdk/aws/sdk/typescript/aws/work/spaces_web_user_access_logging_settings.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own spaces web user access logging settings" });

      resource(SpacesWebUserAccessLoggingSettings, "ci-runner", {
        kinesisStreamArn: "arn:aws:iam::123456789012:policy/ci",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.work import SpacesWebUserAccessLoggingSettings, SpacesWebUserAccessLoggingSettingsConfig

    def describe():
        ubx.intent("payments own spaces web user access logging settings")

        ubx.resource(SpacesWebUserAccessLoggingSettings, "ci-runner", SpacesWebUserAccessLoggingSettingsConfig(
            kinesis_stream_arn="arn:aws:iam::123456789012:policy/ci",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_work_spaces_web_user_access_logging_settings, with
    kinesis stream arn arn:aws:iam::123456789012:policy/ci, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="kinesis_stream_arn" type="string" required>
  Kinesis stream ARN to which log events are published. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tags to assign to this user access logging settings configuration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="associated_portal_arns" type="list(string)">
  The ARNs of the WorkSpaces Web portals that are associated with this user access logging settings resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_access_logging_settings_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this user access logging settings resource, used as a reference for attaching the settings to a WorkSpaces Web portal. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
