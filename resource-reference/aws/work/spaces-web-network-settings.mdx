---
title: "aws_work_spaces_web_network_settings"
description: "`aws_work_spaces_web_network_settings` defines a real VPC, subnets..."
---

`aws_work_spaces_web_network_settings` defines a real VPC, subnets, and security groups that an `aws_work_spaces_web_portal`'s browsing sessions actually run inside, so outbound traffic from the managed browser can reach private, VPC-only resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own spaces web network settings"})

    		ubx.Resource(workspacesweb.NetworkSettings, "example", workspacesweb.NetworkSettingsConfig{
    			SecurityGroupIds: []string{"example"},
    			SubnetIds:        []string{"example"},
    			VpcId:            "example",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkSettings } from "@ubx/sdk-aws/aws/workspacesweb/network_settings";

    export default stack("example", () => {
      intent({ summary: "example own spaces web network settings" });

      resource(NetworkSettings, "example", {
        securityGroupIds: ["example"],
        subnetIds: ["example"],
        vpcId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb.network_settings import NetworkSettings, NetworkSettingsConfig

    def describe():
        ubx.intent("example own spaces web network settings")

        ubx.resource(NetworkSettings, "example", NetworkSettingsConfig(
            security_group_ids=["example"],
            subnet_ids=["example"],
            vpc_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_work_spaces_web_network_settings, with security group
    ids a set value, subnet ids a set value, vpc id example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="security_group_ids" type="list(string)" required>
  One or more security group IDs that define the network traffic rules for the WorkSpaces Web portal, controlling access to the VPC resources it connects to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The list of subnet IDs in the VPC where the WorkSpaces Web portal's network interfaces are created for the network settings. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Assigns a list of tag objects (each containing a key and value) to the WorkSpaces Web network settings resource for identifying, organizing, and managing the resource. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the Amazon WorkSpaces Web network settings resource, used to categorize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC that this WorkSpaces Web network settings configuration is associated with, used to define the network boundary for the web portal. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="associated_portal_arns" type="list(string)">
  The list of ARNs of the web portals associated with this network settings resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="network_settings_arn" type="string">
  The Amazon Resource Name (ARN) assigned by AWS to uniquely identify this WorkSpaces Web network settings resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
