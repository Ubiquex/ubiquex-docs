---
title: "aws_work_spaces_web_trust_store"
description: "`aws_work_spaces_web_trust_store` imports real, trusted root certificates into an `aws_work_spaces_web_portal`, so its managed browser trusts internal..."
---

`aws_work_spaces_web_trust_store` imports real, trusted root certificates into an `aws_work_spaces_web_portal`, so its managed browser trusts internal, privately signed HTTPS endpoints that a public CA wouldn't otherwise validate.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	work "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/work"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own spaces web trust store"})

    		ubx.Resource(work.SpacesWebTrustStore, "example", work.SpacesWebTrustStoreConfig{
    			CertificateList: []string{"example"},
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SpacesWebTrustStore } from "./local-sdk/aws/sdk/typescript/aws/work/spaces_web_trust_store.ts";

    export default stack("example", () => {
      intent({ summary: "example own spaces web trust store" });

      resource(SpacesWebTrustStore, "example", {
        certificateList: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.work import SpacesWebTrustStore, SpacesWebTrustStoreConfig

    def describe():
        ubx.intent("example own spaces web trust store")

        ubx.resource(SpacesWebTrustStore, "example", SpacesWebTrustStoreConfig(
            certificate_list=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_work_spaces_web_trust_store, with certificate list a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_list" type="list(string)" required>
  A list of PEM-encoded certificates to include in this WorkSpaces Web trust store, used to establish trust for web sessions. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies a list of tags (key-value pairs) to attach to this WorkSpaces Web trust store, supporting resource organization and cost allocation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="associated_portal_arns" type="list(string)">
  The list of ARNs of the WorkSpaces Web portals that are associated with this trust store. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="trust_store_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the WorkSpaces Web trust store. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
