---
title: "aws_work_spaces_web_data_protection_settings"
description: "`aws_work_spaces_web_data_protection_settings` configures real inline data-loss-prevention rules..."
---

`aws_work_spaces_web_data_protection_settings` configures real inline data-loss-prevention rules, redacting or blocking sensitive data patterns in the browser, that an `aws_work_spaces_web_portal` enforces during a session.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	work "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/work"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own spaces web data protection settings"})

    		ubx.Resource(work.SpacesWebDataProtectionSettings, "ci-runner", work.SpacesWebDataProtectionSettingsConfig{
    			AdditionalEncryptionContext:  "example",
    			CustomerManagedKey:           "example",
    			Description:                  "Managed by ubx.",
    			DisplayName:                  "example-display-name",
    			InlineRedactionConfiguration: map[string]any{"inline_redaction_patterns": []map[string]any{{"built_in_pattern_id": "example"}}},
    			Tags:                         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SpacesWebDataProtectionSettings } from "./local-sdk/aws/sdk/typescript/aws/work/spaces_web_data_protection_settings.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own spaces web data protection settings" });

      resource(SpacesWebDataProtectionSettings, "ci-runner", {
        additionalEncryptionContext: "example",
        customerManagedKey: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        inlineRedactionConfiguration: {
          inlineRedactionPatterns: [{ builtInPatternId: "example" }],
        },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.work import SpacesWebDataProtectionSettings, SpacesWebDataProtectionSettingsConfig

    def describe():
        ubx.intent("payments own spaces web data protection settings")

        ubx.resource(SpacesWebDataProtectionSettings, "ci-runner", SpacesWebDataProtectionSettingsConfig(
            additional_encryption_context="example",
            customer_managed_key="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            inline_redaction_configuration={"inline_redaction_patterns": [{"built_in_pattern_id": "example"}]},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_work_spaces_web_data_protection_settings, with
    additional encryption context example, customer managed key
    example, description Managed by ubx., display name
    example-display-name, inline redaction configuration a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="additional_encryption_context" type="any">
  Provides additional encryption context key-value pairs that are included in the encryption context for AWS KMS keys used to protect data, enabling finer-grained access control and audit logging. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="customer_managed_key" type="string">
  The ARN of the customer-managed AWS KMS key used to encrypt sensitive data in the WorkSpaces Web data protection settings. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  Provides a user-defined textual summary of the WorkSpaces Web data protection settings, enabling administrators to easily identify and manage the configuration in the AWS console and API. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name is a user-friendly, optional identifier for the data protection settings, used to distinguish them in the WorkSpaces Web console. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="inline_redaction_configuration" type="object">
  Configures automatic inline redaction of sensitive data (e.g., PII, financial numbers) by defining a set of redaction patterns and associated settings applied to web content delivered through WorkSpaces Web. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="global_confidence_level" type="number">
      Defines the minimum confidence score (0-100) that detected sensitive data must reach for the inline redaction engine to apply redaction, serving as the default threshold for all rules in the inline redaction configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="global_enforced_urls" type="list(string)">
      Specifies a list of URL patterns whose web content always has inline redaction applied, so sensitive data is redacted on those pages even if no other redaction rule matches. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="global_exempt_urls" type="list(string)">
      Specifies a list of URL patterns that are exempt from inline redaction, so content on those URLs is not redacted. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="inline_redaction_patterns" type="list(object)" required>
      Defines the required list of inline redaction patterns, each specifying a regular expression to match sensitive data that should be redacted from web content in this AWS WorkSpaces Web data protection settings resource. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="built_in_pattern_id" type="string">
          The unique identifier for a built-in redaction pattern (e.g., credit card numbers, US social security numbers) used by this inline redaction rule to detect and redact sensitive information in web content. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="confidence_level" type="number">
          The minimum confidence threshold that the inline redaction pattern must meet for a detected sensitive data instance to be considered a match and redacted in WorkSpaces Web data protection settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_pattern" type="object">
          In an AWS WorkSpaces Web data protection inline redaction pattern, the custom_pattern object defines a custom regular expression and the redaction action (e.g., redact or block) used to match and protect sensitive data in web content. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="keyword_regex" type="string">
              Defines the regular expression used to match keywords that identify the context for applying the custom inline redaction pattern in WorkSpaces Web data protection settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pattern_description" type="string">
              Provides a human-readable description for a custom inline redaction pattern, documenting the pattern's intended matching behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pattern_name" type="string">
              The name that identifies this custom inline redaction pattern within the WorkSpaces Web data protection settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pattern_regex" type="string">
              The regular expression that defines the specific text sequence to be matched and redacted inline when found in web content. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enforced_urls" type="list(string)">
          A list of URLs (or URL patterns) on which the inline redaction pattern is enforced, meaning that content matching the pattern will be redacted only when accessed from those URLs. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exempt_urls" type="list(string)">
          Specifies a list of URL patterns that are exempt from the inline redaction rule, so content on those URLs will not be redacted. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="redaction_place_holder" type="object">
          Defines the placeholder text and type that replace matched content for each inline redaction pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="redaction_place_holder_text" type="string">
              The text string that is inserted in place of the redacted content when a sensitive data pattern matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redaction_place_holder_type" type="string">
              The `redaction_place_holder_type` field determines whether the matched sensitive text is replaced with a custom placeholder string (`CUSTOM`) or left as the original text (`PRESERVED`) when the inline redaction pattern is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Adds one or more tag objects (Key and Value) to the WorkSpaces Web data protection settings resource. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="associated_portal_arns" type="list(string)">
  The list of ARNs of the WorkSpaces Web portals that are currently associated with these data protection settings, populated automatically by AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="creation_date" type="string">
  The date and time when the WorkSpaces Web data protection settings were created, returned as a read-only string. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="data_protection_settings_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this WorkSpaces Web data protection settings resource, used to reference it in IAM policies and other AWS services. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
