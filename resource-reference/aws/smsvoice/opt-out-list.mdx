---
title: "aws_smsvoice_opt_out_list"
description: "`aws_smsvoice_opt_out_list` maintains a real list of destination phone numbers that should not receive messages..."
---

`aws_smsvoice_opt_out_list` maintains a real list of destination phone numbers that should not receive messages, automatically populated when a recipient replies STOP, that an `aws_smsvoice_phone_number` or `aws_smsvoice_pool` is assigned to respect.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	smsvoice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/smsvoice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own opt out list"})

    		ubx.Resource(smsvoice.OptOutList, "example", smsvoice.OptOutListConfig{
    			OptOutListName: "example-opt-out-list-name",
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OptOutList } from "@ubx/sdk-aws/aws/smsvoice/opt_out_list";

    export default stack("example", () => {
      intent({ summary: "example own opt out list" });

      resource(OptOutList, "example", {
        optOutListName: "example-opt-out-list-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.smsvoice.opt_out_list import OptOutList, OptOutListConfig

    def describe():
        ubx.intent("example own opt out list")

        ubx.resource(OptOutList, "example", OptOutListConfig(
            opt_out_list_name="example-opt-out-list-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_smsvoice_opt_out_list, with opt out list name
    example-opt-out-list-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="opt_out_list_name" type="string">
  The name of the new OptOutList. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair assigned to the AWS SMS Voice opt-out list, used for categorizing or identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) for the OptOutList. Computed by AWS after `ship`.
</ResponseField>
