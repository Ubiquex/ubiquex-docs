---
title: "aws_smsvoice_sender_id"
description: "`aws_smsvoice_sender_id` registers a real, alphanumeric sender ID, a recognizable brand name shown as the message sender instead of a phone number..."
---

`aws_smsvoice_sender_id` registers a real, alphanumeric sender ID, a recognizable brand name shown as the message sender instead of a phone number, for use in countries that support it, under an `aws_smsvoice_configuration_set`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	smsvoice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/smsvoice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sender id"})

    		ubx.Resource(smsvoice.SenderId, "ci-runner", smsvoice.SenderIdConfig{
    			IsoCountryCode:            "example",
    			SenderId:                  "example",
    			DeletionProtectionEnabled: true,
    			Tags:                      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SenderId } from "./local-sdk/aws/sdk/typescript/aws/smsvoice/sender_id.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sender id" });

      resource(SenderId, "ci-runner", {
        isoCountryCode: "example",
        senderId: "example",
        deletionProtectionEnabled: true,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.smsvoice import SenderId, SenderIdConfig

    def describe():
        ubx.intent("payments own sender id")

        ubx.resource(SenderId, "ci-runner", SenderIdConfig(
            iso_country_code="example",
            sender_id="example",
            deletion_protection_enabled=True,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_smsvoice_sender_id, with iso country code example,
    sender id example, deletion protection enabled true, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_protection_enabled" type="bool">
  When set to true the sender ID can't be deleted. By default this is set to false. Optional.
</ResponseField>

<ResponseField name="iso_country_code" type="string" required>
  The two-character code, in ISO 3166-1 alpha-2 format, for the country or region. Required.
</ResponseField>

<ResponseField name="sender_id" type="string" required>
  The sender ID string to request. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) associated with the SenderId. Computed by AWS after `ship`.
</ResponseField>
