---
title: "aws_smsvoice_phone_number"
description: "`aws_smsvoice_phone_number` provisions a real phone number, long code, toll-free, or short code, for sending and receiving SMS, MMS..."
---

`aws_smsvoice_phone_number` provisions a real phone number, long code, toll-free, or short code, for sending and receiving SMS, MMS, or voice messages through AWS End User Messaging.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	smsvoice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/smsvoice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own phone number"})

    		ubx.Resource(smsvoice.PhoneNumber, "example", smsvoice.PhoneNumberConfig{
    			IsoCountryCode:            "example",
    			MandatoryKeywords:         map[string]any{"help": map[string]any{"message": "example"}},
    			NumberCapabilities:        []string{"example"},
    			NumberType:                "example",
    			DeletionProtectionEnabled: true,
    			OptOutListName:            "example-opt-out-list-name",
    			OptionalKeywords:          []map[string]any{{"action": "example"}},
    			SelfManagedOptOutsEnabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PhoneNumber } from "./local-sdk/aws/sdk/typescript/aws/smsvoice/phone_number.ts";

    export default stack("example", () => {
      intent({ summary: "example own phone number" });

      resource(PhoneNumber, "example", {
        isoCountryCode: "example",
        mandatoryKeywords: { help: { message: "example" } },
        numberCapabilities: ["example"],
        numberType: "example",
        deletionProtectionEnabled: true,
        optOutListName: "example-opt-out-list-name",
        optionalKeywords: [{ action: "example" }],
        selfManagedOptOutsEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.smsvoice import PhoneNumber, PhoneNumberConfig

    def describe():
        ubx.intent("example own phone number")

        ubx.resource(PhoneNumber, "example", PhoneNumberConfig(
            iso_country_code="example",
            mandatory_keywords={"help": {"message": "example"}},
            number_capabilities=["example"],
            number_type="example",
            deletion_protection_enabled=True,
            opt_out_list_name="example-opt-out-list-name",
            optional_keywords=[{"action": "example"}],
            self_managed_opt_outs_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_smsvoice_phone_number, with iso country code example,
    mandatory keywords a set value, number capabilities a set value,
    number type example, deletion protection enabled true, opt out
    list name example-opt-out-list-name, optional keywords a set
    value, self managed opt outs enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_protection_enabled" type="bool">
  When set to true the sender ID can't be deleted. By default this is set to false. Optional.
</ResponseField>

<ResponseField name="iso_country_code" type="string" required>
  The two-character code, in ISO 3166-1 alpha-2 format, for the country or region. Required.
</ResponseField>

<ResponseField name="mandatory_keywords" type="object" required>
  A keyword is a word that you can search for on a particular phone number or pool. It is also a specific word or phrase that an end user can send to your number to elicit a response, such as an informational message or a special offer. When your number receives a message that begins with a keyword, AWS End User Messaging SMS and Voice responds with a customizable message. Keywords "HELP" and "STOP" are mandatory keywords Required.
  <Expandable title="properties">
    <ResponseField name="help" type="object" required>
      A keyword is a word that you can search for on a particular phone number or pool. It is also a specific word or phrase that an end user can send to your number to elicit a response, such as an informational message or a special offer. When your number receives a message that begins with a keyword, AWS End User Messaging SMS and Voice responds with a customizable message. Keywords "HELP" and "STOP" are mandatory keywords Required.
      <Expandable title="properties">
        <ResponseField name="message" type="string" required>
          The message that the phone number automatically sends to an SMS sender when it receives the mandatory HELP keyword, configuring the help response required for compliant phone numbers. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stop" type="object" required>
      A keyword is a word that you can search for on a particular phone number or pool. It is also a specific word or phrase that an end user can send to your number to elicit a response, such as an informational message or a special offer. When your number receives a message that begins with a keyword, AWS End User Messaging SMS and Voice responds with a customizable message. Keywords "HELP" and "STOP" are mandatory keywords Required.
      <Expandable title="properties">
        <ResponseField name="message" type="string" required>
          The message that Amazon Pinpoint SMS Voice v2 automatically sends in reply when a sender texts the mandatory 'STOP' keyword to this phone number. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="number_capabilities" type="list(string)" required>
  Indicates if the phone number will be used for text messages, voice messages, or both. Required.
</ResponseField>

<ResponseField name="number_type" type="string" required>
  The type of phone number to request. Required.
</ResponseField>

<ResponseField name="opt_out_list_name" type="string">
  The name of the OptOutList to associate with the phone number. You can use the OptOutListName or OptOutListArn. Optional.
</ResponseField>

<ResponseField name="optional_keywords" type="list(object)">
  A keyword is a word that you can search for on a particular phone number or pool. It is also a specific word or phrase that an end user can send to your number to elicit a response, such as an informational message or a special offer. When your number receives a message that begins with a keyword, AWS End User Messaging SMS and Voice responds with a customizable message. Optional.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Defines the opt-in/opt-out behavior (REGISTER or UNREGISTER) that is applied when an inbound SMS message matches the associated optional keyword on this phone number. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="keyword" type="string">
      Defines a custom keyword that, when received as an inbound SMS message to the phone number, triggers the pre-configured two-way messaging response associated with that keyword. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="message" type="string">
      The auto-reply text sent when the associated keyword from the same `optional_keywords` list entry is received by this phone number. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_managed_opt_outs_enabled" type="bool">
  By default this is set to false. When an end recipient sends a message that begins with HELP or STOP to one of your dedicated numbers, AWS End User Messaging SMS and Voice automatically replies with a customizable message and adds the end recipient to the OptOutList. When set to true you're responsible for responding to HELP and STOP requests. You're also responsible for tracking and honoring opt-out requests. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a key-value tag that you associate with the phone number to help organize and manage it. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag attached to the SMS voice phone number. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="two_way" type="object">
  When you set up two-way SMS, you can receive incoming messages from your customers. When one of your customers sends a message to your phone number, the message body is sent to an Amazon SNS topic or Amazon Connect for processing. Optional.
  <Expandable title="properties">
    <ResponseField name="channel_arn" type="string">
      The Amazon Resource Name (ARN) of the two way channel. Optional.
    </ResponseField>
    <ResponseField name="channel_role" type="string">
      An optional IAM Role Arn for a service to assume, to be able to post inbound SMS messages. Optional.
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
      By default this is set to false. When set to true you can receive incoming text messages from your end recipients. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the phone number resource in Amazon Pinpoint SMS Voice. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="phone_number" type="string">
  The E.164-formatted phone number assigned by AWS for this resource, used as the caller ID for sending voice messages. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="phone_number_id" type="string">
  The service-assigned unique identifier for the phone number, returned by AWS SMS Voice (AWS End User Messaging) after provisioning, used to reference the phone number in API operations and other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
