---
title: "aws_transcribe_medical_vocabulary"
description: "Real, generated bindings for `aws_transcribe_medical_vocabulary`."
---

`aws_transcribe_medical_vocabulary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own medical vocabulary"})

    		ubx.Resource(transcribe.MedicalVocabulary, "ci-runner", transcribe.MedicalVocabularyConfig{
    			LanguageCode:      "example",
    			VocabularyFileUri: "example",
    			VocabularyName:    "example-vocabulary-name",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MedicalVocabulary } from "jsr:@ubx/sdk-aws/aws/transcribe/medical_vocabulary";

    export default stack("payments", () => {
      intent({ summary: "payments own medical vocabulary" });

      resource(MedicalVocabulary, "ci-runner", {
        languageCode: "example",
        vocabularyFileUri: "example",
        vocabularyName: "example-vocabulary-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transcribe import MedicalVocabulary, MedicalVocabularyConfig

    def describe():
        ubx.intent("payments own medical vocabulary")

        ubx.resource(MedicalVocabulary, "ci-runner", MedicalVocabularyConfig(
            language_code="example",
            vocabulary_file_uri="example",
            vocabulary_name="example-vocabulary-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transcribe_medical_vocabulary, with language code
    example, vocabulary file uri example, vocabulary name
    example-vocabulary-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vocabulary_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
