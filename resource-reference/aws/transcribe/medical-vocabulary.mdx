---
title: "aws_transcribe_medical_vocabulary"
description: "Real, generated bindings for `aws_transcribe_medical_vocabulary`."
---

`aws_transcribe_medical_vocabulary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/transcribe"

    cfg := transcribe.MedicalVocabularyConfig{
        LanguageCode: "example",
        VocabularyFileUri: "example",
        VocabularyName: "example-vocabulary-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(transcribe.MedicalVocabulary, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MedicalVocabulary } from
      "jsr:@ubx/sdk-aws/aws/transcribe/medical_vocabulary";

    resource(MedicalVocabulary, "example", {
      languageCode: "example",
      vocabularyFileUri: "example",
      vocabularyName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transcribe import (
        MedicalVocabulary,
        MedicalVocabularyConfig,
    )

    cfg = MedicalVocabularyConfig(
        language_code="example",
        vocabulary_file_uri="example",
        vocabulary_name="example",
        tags={"key": "value"},
    )
    ubx.resource(MedicalVocabulary, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transcribe_medical_vocabulary with language_code,
vocabulary_file_uri, vocabulary_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vocabulary_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
