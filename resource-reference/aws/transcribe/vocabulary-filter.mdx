---
title: "aws_transcribe_vocabulary_filter"
description: "Real, generated bindings for `aws_transcribe_vocabulary_filter`."
---

`aws_transcribe_vocabulary_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"

    cfg := transcribe.VocabularyFilterConfig{
        LanguageCode: "example",
        VocabularyFilterName: "example-vocabulary-filter-name",
        Tags: map[string]string{"key": "value"},
        VocabularyFilterFileUri: "example",
    }
    ubx.Resource(transcribe.VocabularyFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VocabularyFilter } from
      "jsr:@ubx/sdk-aws/aws/transcribe/vocabulary_filter";

    resource(VocabularyFilter, "example", {
      languageCode: "example",
      vocabularyFilterName: "example",
      tags: { key: "value" },
      vocabularyFilterFileUri: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transcribe import (
        VocabularyFilter,
        VocabularyFilterConfig,
    )

    cfg = VocabularyFilterConfig(
        language_code="example",
        vocabulary_filter_name="example",
        tags={"key": "value"},
        vocabulary_filter_file_uri="example",
    )
    ubx.resource(VocabularyFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transcribe_vocabulary_filter with language_code,
vocabulary_filter_name, tags, vocabulary_filter_file_uri.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_filter_file_uri" type="string">
  Optional.
</ResponseField>

<ResponseField name="vocabulary_filter_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="words" type="list(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
