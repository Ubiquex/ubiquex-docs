---
title: "aws_transcribe_vocabulary_filter"
description: "Real, generated bindings for `aws_transcribe_vocabulary_filter`."
---

`aws_transcribe_vocabulary_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vocabulary filter"})

    		ubx.Resource(transcribe.VocabularyFilter, "ci-runner", transcribe.VocabularyFilterConfig{
    			LanguageCode:            "example",
    			VocabularyFilterName:    "example-vocabulary-filter-name",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    			VocabularyFilterFileUri: "example",
    			Words:                   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VocabularyFilter } from "jsr:@ubx/sdk-aws/aws/transcribe/vocabulary_filter";

    export default stack("payments", () => {
      intent({ summary: "payments own vocabulary filter" });

      resource(VocabularyFilter, "ci-runner", {
        languageCode: "example",
        vocabularyFilterName: "example-vocabulary-filter-name",
        tags: { "managed-by": "ubx" },
        vocabularyFilterFileUri: "example",
        words: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transcribe import VocabularyFilter, VocabularyFilterConfig

    def describe():
        ubx.intent("payments own vocabulary filter")

        ubx.resource(VocabularyFilter, "ci-runner", VocabularyFilterConfig(
            language_code="example",
            vocabulary_filter_name="example-vocabulary-filter-name",
            tags={"managed-by": "ubx"},
            vocabulary_filter_file_uri="example",
            words=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transcribe_vocabulary_filter, with language code
    example, vocabulary filter name example-vocabulary-filter-name,
    tags a set value, vocabulary filter file uri example, words a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_filter_file_uri" type="string">
  Optional.
</ResponseField>

<ResponseField name="vocabulary_filter_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="words" type="list(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
