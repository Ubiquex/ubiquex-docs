---
title: "aws_transcribe_vocabulary"
description: "Real, generated bindings for `aws_transcribe_vocabulary`."
---

`aws_transcribe_vocabulary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vocabulary"})

    		ubx.Resource(transcribe.Vocabulary, "ci-runner", transcribe.VocabularyConfig{
    			LanguageCode:   "example",
    			VocabularyName: "example-vocabulary-name",
    			Phrases:        []string{"example"},
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Vocabulary } from "jsr:@ubx/sdk-aws/aws/transcribe/vocabulary";

    export default stack("payments", () => {
      intent({ summary: "payments own vocabulary" });

      resource(Vocabulary, "ci-runner", {
        languageCode: "example",
        vocabularyName: "example-vocabulary-name",
        phrases: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transcribe import Vocabulary, VocabularyConfig

    def describe():
        ubx.intent("payments own vocabulary")

        ubx.resource(Vocabulary, "ci-runner", VocabularyConfig(
            language_code="example",
            vocabulary_name="example-vocabulary-name",
            phrases=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transcribe_vocabulary, with language code example,
    vocabulary name example-vocabulary-name, phrases a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="phrases" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
