---
title: "aws_transcribe_vocabulary"
description: "Real, generated bindings for `aws_transcribe_vocabulary`."
---

`aws_transcribe_vocabulary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/transcribe"

    cfg := transcribe.VocabularyConfig{
        LanguageCode: "example",
        VocabularyName: "example-vocabulary-name",
        Phrases: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(transcribe.Vocabulary, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Vocabulary } from
      "jsr:@ubx/sdk-aws/aws/transcribe/vocabulary";

    resource(Vocabulary, "example", {
      languageCode: "example",
      vocabularyName: "example",
      phrases: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transcribe import Vocabulary, VocabularyConfig

    cfg = VocabularyConfig(
        language_code="example",
        vocabulary_name="example",
        phrases=["example"],
        tags={"key": "value"},
    )
    ubx.resource(Vocabulary, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transcribe_vocabulary with language_code,
vocabulary_name, phrases, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="phrases" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="download_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vocabulary_file_uri" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
