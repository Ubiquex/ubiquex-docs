---
title: "aws_transcribe_language_model"
description: "Real, generated bindings for `aws_transcribe_language_model`."
---

`aws_transcribe_language_model` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own language model"})

    		ubx.Resource(transcribe.LanguageModel, "ci-runner", transcribe.LanguageModelConfig{
    			BaseModelName: "example-base-model-name",
    			LanguageCode:  "example",
    			ModelName:     "example-model-name",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LanguageModel } from "jsr:@ubx/sdk-aws/aws/transcribe/language_model";

    export default stack("payments", () => {
      intent({ summary: "payments own language model" });

      resource(LanguageModel, "ci-runner", {
        baseModelName: "example-base-model-name",
        languageCode: "example",
        modelName: "example-model-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transcribe import LanguageModel, LanguageModelConfig

    def describe():
        ubx.intent("payments own language model")

        ubx.resource(LanguageModel, "ci-runner", LanguageModelConfig(
            base_model_name="example-base-model-name",
            language_code="example",
            model_name="example-model-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transcribe_language_model, with base model name
    example-base-model-name, language code example, model name
    example-model-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="base_model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
