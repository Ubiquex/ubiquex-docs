---
title: "aws_transcribe_language_model"
description: "Real, generated bindings for `aws_transcribe_language_model`."
---

`aws_transcribe_language_model` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transcribe"

    cfg := transcribe.LanguageModelConfig{
        BaseModelName: "example-base-model-name",
        LanguageCode: "example",
        ModelName: "example-model-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(transcribe.LanguageModel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LanguageModel } from
      "jsr:@ubx/sdk-aws/aws/transcribe/language_model";

    resource(LanguageModel, "example", {
      baseModelName: "example",
      languageCode: "example",
      modelName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transcribe import (
        LanguageModel,
        LanguageModelConfig,
    )

    cfg = LanguageModelConfig(
        base_model_name="example",
        language_code="example",
        model_name="example",
        tags={"key": "value"},
    )
    ubx.resource(LanguageModel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transcribe_language_model with base_model_name,
language_code, model_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="base_model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
