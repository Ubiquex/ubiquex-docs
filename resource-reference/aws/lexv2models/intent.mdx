---
title: "aws_lexv2models_intent"
description: "Real, generated bindings for `aws_lexv2models_intent`."
---

`aws_lexv2models_intent` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lexv2models"

    cfg := lexv2models.IntentConfig{
        BotId: "example",
        BotVersion: "example",
        LocaleId: "example",
        Name: "example-name",
        Description: "example",
        ParentIntentSignature: "example",
    }
    ubx.Resource(lexv2models.Intent, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Intent } from "jsr:@ubx/sdk-aws/aws/lexv2models/intent";

    resource(Intent, "example", {
      botId: "example",
      botVersion: "example",
      localeId: "example",
      name: "example",
      description: "example",
      parentIntentSignature: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lexv2models import Intent, IntentConfig

    cfg = IntentConfig(
        bot_id="example",
        bot_version="example",
        locale_id="example",
        name="example",
        description="example",
        parent_intent_signature="example",
    )
    ubx.resource(Intent, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lexv2models_intent with bot_id, bot_version,
locale_id, name, description, parent_intent_signature.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bot_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent_intent_signature" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="creation_date_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="intent_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
