---
title: "aws_lexv2models_bot_version"
description: "Real, generated bindings for `aws_lexv2models_bot_version`."
---

`aws_lexv2models_bot_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lexv2models "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lexv2models"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bot version"})

    		ubx.Resource(lexv2models.BotVersion, "ci-runner", lexv2models.BotVersionConfig{
    			BotId:               "example",
    			LocaleSpecification: map[string]string{"managed-by": "ubx"},
    			Description:         "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BotVersion } from "jsr:@ubx/sdk-aws/aws/lexv2models/bot_version";

    export default stack("payments", () => {
      intent({ summary: "payments own bot version" });

      resource(BotVersion, "ci-runner", {
        botId: "example",
        localeSpecification: { "managed-by": "ubx" },
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lexv2models import BotVersion, BotVersionConfig

    def describe():
        ubx.intent("payments own bot version")

        ubx.resource(BotVersion, "ci-runner", BotVersionConfig(
            bot_id="example",
            locale_specification={"managed-by": "ubx"},
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lexv2models_bot_version, with bot id example, locale
    specification a set value, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bot_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="bot_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="locale_specification" type="map(string)" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="source_bot_version" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="bot_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
