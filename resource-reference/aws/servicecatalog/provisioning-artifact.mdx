---
title: "aws_servicecatalog_provisioning_artifact"
description: "Real, generated bindings for `aws_servicecatalog_provisioning_artifact`."
---

`aws_servicecatalog_provisioning_artifact` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provisioning artifact"})

    		ubx.Resource(servicecatalog.ProvisioningArtifact, "ci-runner", servicecatalog.ProvisioningArtifactConfig{
    			ProductId:                 "example",
    			Name:                      "ci-runner",
    			AcceptLanguage:            "example",
    			Active:                    true,
    			DisableTemplateValidation: true,
    			Guidance:                  "example",
    			TemplatePhysicalId:        "example",
    			TemplateUrl:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProvisioningArtifact } from "jsr:@ubx/sdk-aws/aws/servicecatalog/provisioning_artifact";

    export default stack("payments", () => {
      intent({ summary: "payments own provisioning artifact" });

      resource(ProvisioningArtifact, "ci-runner", {
        productId: "example",
        name: "ci-runner",
        acceptLanguage: "example",
        active: true,
        disableTemplateValidation: true,
        guidance: "example",
        templatePhysicalId: "example",
        templateUrl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import ProvisioningArtifact, ProvisioningArtifactConfig

    def describe():
        ubx.intent("payments own provisioning artifact")

        ubx.resource(ProvisioningArtifact, "ci-runner", ProvisioningArtifactConfig(
            product_id="example",
            name="ci-runner",
            accept_language="example",
            active=True,
            disable_template_validation=True,
            guidance="example",
            template_physical_id="example",
            template_url="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_provisioning_artifact called
    "ci-runner", with product id example, accept language example,
    active true, disable template validation true, guidance example,
    template physical id example, template url example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="active" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disable_template_validation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="guidance" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="product_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_physical_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="template_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provisioning_artifact_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
