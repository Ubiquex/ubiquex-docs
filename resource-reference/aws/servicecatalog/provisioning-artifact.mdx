---
title: "aws_servicecatalog_provisioning_artifact"
description: "Real, generated bindings for `aws_servicecatalog_provisioning_artifact`."
---

`aws_servicecatalog_provisioning_artifact` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/servicecatalog"

    cfg := servicecatalog.ProvisioningArtifactConfig{
        ProductId: "example",
        AcceptLanguage: "example",
        Active: true,
    }
    ubx.Resource(servicecatalog.ProvisioningArtifact, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProvisioningArtifact } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/provisioning_artifact";

    resource(ProvisioningArtifact, "example", {
      productId: "example",
      acceptLanguage: "example",
      active: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        ProvisioningArtifact,
        ProvisioningArtifactConfig,
    )

    cfg = ProvisioningArtifactConfig(
        product_id="example",
        accept_language="example",
        active=True,
    )
    ubx.resource(ProvisioningArtifact, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_provisioning_artifact with
product_id, accept_language, active.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="active" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disable_template_validation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="guidance" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="product_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_physical_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="template_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provisioning_artifact_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
