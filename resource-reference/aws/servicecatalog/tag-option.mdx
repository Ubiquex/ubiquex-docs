---
title: "aws_servicecatalog_tag_option"
description: "Real, generated bindings for `aws_servicecatalog_tag_option`."
---

`aws_servicecatalog_tag_option` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"

    cfg := servicecatalog.TagOptionConfig{
        Key: "example",
        Value: "example",
        Active: true,
    }
    ubx.Resource(servicecatalog.TagOption, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TagOption } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/tag_option";

    resource(TagOption, "example", {
      key: "example",
      value: "example",
      active: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import TagOption, TagOptionConfig

    cfg = TagOptionConfig(
        key="example",
        value="example",
        active=True,
    )
    ubx.resource(TagOption, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_tag_option with key, value, active.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="active" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
