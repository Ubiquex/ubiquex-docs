---
title: "aws_service_catalog_tag_option"
description: "`aws_service_catalog_tag_option` defines a real, allowed tag key-value pair that can be associated with an `aws_service_catalog_portfolio` or product..."
---

`aws_service_catalog_tag_option` defines a real, allowed tag key-value pair that can be associated with an `aws_service_catalog_portfolio` or product, so end users pick from a governed, pre-approved set of tags rather than typing arbitrary ones.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own catalog tag option"})

    		ubx.Resource(servicecatalog.TagOption, "example", servicecatalog.TagOptionConfig{
    			Key:    "example",
    			Value:  "example",
    			Active: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagOption } from "@ubx/sdk-aws/aws/servicecatalog/tag_option";

    export default stack("example", () => {
      intent({ summary: "example own catalog tag option" });

      resource(TagOption, "example", {
        key: "example",
        value: "example",
        active: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog.tag_option import TagOption, TagOptionConfig

    def describe():
        ubx.intent("example own catalog tag option")

        ubx.resource(TagOption, "example", TagOptionConfig(
            key="example",
            value="example",
            active=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_tag_option, with key example, value
    example, active true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="active" type="bool">
  The TagOption active state. Optional.
</ResponseField>

<ResponseField name="key" type="string" required>
  The TagOption key. Required.
</ResponseField>

<ResponseField name="value" type="string" required>
  The TagOption value. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The TagOption identifier. Computed by AWS after `ship`.
</ResponseField>
