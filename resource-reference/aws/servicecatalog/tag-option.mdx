---
title: "aws_servicecatalog_tag_option"
description: "Real, generated bindings for `aws_servicecatalog_tag_option`."
---

`aws_servicecatalog_tag_option` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag option"})

    		ubx.Resource(servicecatalog.TagOption, "ci-runner", servicecatalog.TagOptionConfig{
    			Key:    "example",
    			Value:  "example",
    			Active: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagOption } from "jsr:@ubx/sdk-aws/aws/servicecatalog/tag_option";

    export default stack("payments", () => {
      intent({ summary: "payments own tag option" });

      resource(TagOption, "ci-runner", {
        key: "example",
        value: "example",
        active: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import TagOption, TagOptionConfig

    def describe():
        ubx.intent("payments own tag option")

        ubx.resource(TagOption, "ci-runner", TagOptionConfig(
            key="example",
            value="example",
            active=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_tag_option, with key example, value
    example, active true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="active" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
