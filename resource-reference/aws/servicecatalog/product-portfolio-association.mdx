---
title: "aws_servicecatalog_product_portfolio_association"
description: "Real, generated bindings for `aws_servicecatalog_product_portfolio_association`."
---

`aws_servicecatalog_product_portfolio_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own product portfolio association"})

    		ubx.Resource(servicecatalog.ProductPortfolioAssociation, "ci-runner", servicecatalog.ProductPortfolioAssociationConfig{
    			PortfolioId:       "example",
    			ProductId:         "example",
    			AcceptLanguage:    "example",
    			SourcePortfolioId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProductPortfolioAssociation } from "jsr:@ubx/sdk-aws/aws/servicecatalog/product_portfolio_association";

    export default stack("payments", () => {
      intent({ summary: "payments own product portfolio association" });

      resource(ProductPortfolioAssociation, "ci-runner", {
        portfolioId: "example",
        productId: "example",
        acceptLanguage: "example",
        sourcePortfolioId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import ProductPortfolioAssociation, ProductPortfolioAssociationConfig

    def describe():
        ubx.intent("payments own product portfolio association")

        ubx.resource(ProductPortfolioAssociation, "ci-runner", ProductPortfolioAssociationConfig(
            portfolio_id="example",
            product_id="example",
            accept_language="example",
            source_portfolio_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_product_portfolio_association, with
    portfolio id example, product id example, accept language
    example, source portfolio id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="product_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_portfolio_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
