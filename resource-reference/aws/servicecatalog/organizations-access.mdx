---
title: "aws_servicecatalog_organizations_access"
description: "Real, generated bindings for `aws_servicecatalog_organizations_access`."
---

`aws_servicecatalog_organizations_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organizations access"})

    		ubx.Resource(servicecatalog.OrganizationsAccess, "ci-runner", servicecatalog.OrganizationsAccessConfig{
    			Enabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationsAccess } from "jsr:@ubx/sdk-aws/aws/servicecatalog/organizations_access";

    export default stack("payments", () => {
      intent({ summary: "payments own organizations access" });

      resource(OrganizationsAccess, "ci-runner", {
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import OrganizationsAccess, OrganizationsAccessConfig

    def describe():
        ubx.intent("payments own organizations access")

        ubx.resource(OrganizationsAccess, "ci-runner", OrganizationsAccessConfig(
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_organizations_access, with enabled
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
