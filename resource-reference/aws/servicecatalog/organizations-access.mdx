---
title: "aws_servicecatalog_organizations_access"
description: "Real, generated bindings for `aws_servicecatalog_organizations_access`."
---

`aws_servicecatalog_organizations_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"

    cfg := servicecatalog.OrganizationsAccessConfig{
        Enabled: true,
    }
    ubx.Resource(servicecatalog.OrganizationsAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OrganizationsAccess } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/organizations_access";

    resource(OrganizationsAccess, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        OrganizationsAccess,
        OrganizationsAccessConfig,
    )

    cfg = OrganizationsAccessConfig(
        enabled=True,
    )
    ubx.resource(OrganizationsAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_organizations_access with enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
