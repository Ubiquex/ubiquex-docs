---
title: "aws_servicecatalog_budget_resource_association"
description: "Real, generated bindings for `aws_servicecatalog_budget_resource_association`."
---

`aws_servicecatalog_budget_resource_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"

    cfg := servicecatalog.BudgetResourceAssociationConfig{
        BudgetName: "example-budget-name",
        ResourceId: "example",
    }
    ubx.Resource(
        servicecatalog.BudgetResourceAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BudgetResourceAssociation } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/budget_resource_association";

    resource(BudgetResourceAssociation, "example", {
      budgetName: "example",
      resourceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        BudgetResourceAssociation,
        BudgetResourceAssociationConfig,
    )

    cfg = BudgetResourceAssociationConfig(
        budget_name="example",
        resource_id="example",
    )
    ubx.resource(BudgetResourceAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_budget_resource_association with
budget_name, resource_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="budget_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
