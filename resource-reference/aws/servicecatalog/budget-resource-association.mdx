---
title: "aws_servicecatalog_budget_resource_association"
description: "Real, generated bindings for `aws_servicecatalog_budget_resource_association`."
---

`aws_servicecatalog_budget_resource_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own budget resource association"})

    		ubx.Resource(servicecatalog.BudgetResourceAssociation, "ci-runner", servicecatalog.BudgetResourceAssociationConfig{
    			BudgetName: "example-budget-name",
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BudgetResourceAssociation } from "jsr:@ubx/sdk-aws/aws/servicecatalog/budget_resource_association";

    export default stack("payments", () => {
      intent({ summary: "payments own budget resource association" });

      resource(BudgetResourceAssociation, "ci-runner", {
        budgetName: "example-budget-name",
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import BudgetResourceAssociation, BudgetResourceAssociationConfig

    def describe():
        ubx.intent("payments own budget resource association")

        ubx.resource(BudgetResourceAssociation, "ci-runner", BudgetResourceAssociationConfig(
            budget_name="example-budget-name",
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_budget_resource_association, with
    budget name example-budget-name, resource id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="budget_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
