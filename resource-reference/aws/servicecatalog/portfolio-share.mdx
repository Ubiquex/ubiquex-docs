---
title: "aws_servicecatalog_portfolio_share"
description: "Real, generated bindings for `aws_servicecatalog_portfolio_share`."
---

`aws_servicecatalog_portfolio_share` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/servicecatalog"

    cfg := servicecatalog.PortfolioShareConfig{
        PortfolioId: "example",
        PrincipalId: "example",
        Type: "example",
        AcceptLanguage: "example",
        SharePrincipals: true,
    }
    ubx.Resource(servicecatalog.PortfolioShare, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PortfolioShare } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/portfolio_share";

    resource(PortfolioShare, "example", {
      portfolioId: "example",
      principalId: "example",
      type: "example",
      acceptLanguage: "example",
      sharePrincipals: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        PortfolioShare,
        PortfolioShareConfig,
    )

    cfg = PortfolioShareConfig(
        portfolio_id="example",
        principal_id="example",
        type="example",
        accept_language="example",
        share_principals=True,
    )
    ubx.resource(PortfolioShare, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_portfolio_share with portfolio_id,
principal_id, type, accept_language, share_principals.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="share_principals" type="bool">
  Optional.
</ResponseField>

<ResponseField name="share_tag_options" type="bool">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="wait_for_acceptance" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="accepted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
