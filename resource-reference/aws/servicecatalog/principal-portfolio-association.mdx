---
title: "aws_servicecatalog_principal_portfolio_association"
description: "Real, generated bindings for `aws_servicecatalog_principal_portfolio_association`."
---

`aws_servicecatalog_principal_portfolio_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/servicecatalog"

    cfg := servicecatalog.PrincipalPortfolioAssociationConfig{
        PortfolioId: "example",
        PrincipalArn: "example",
        AcceptLanguage: "example",
        PrincipalType: "example",
    }
    ubx.Resource(
        servicecatalog.PrincipalPortfolioAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PrincipalPortfolioAssociation } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/principal_portfolio_association";

    resource(PrincipalPortfolioAssociation, "example", {
      portfolioId: "example",
      principalArn: "example",
      acceptLanguage: "example",
      principalType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        PrincipalPortfolioAssociation,
        PrincipalPortfolioAssociationConfig,
    )

    cfg = PrincipalPortfolioAssociationConfig(
        portfolio_id="example",
        principal_arn="example",
        accept_language="example",
        principal_type="example",
    )
    ubx.resource(PrincipalPortfolioAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_principal_portfolio_association with
portfolio_id, principal_arn, accept_language, principal_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
