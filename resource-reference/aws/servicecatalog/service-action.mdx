---
title: "aws_servicecatalog_service_action"
description: "Real, generated bindings for `aws_servicecatalog_service_action`."
---

`aws_servicecatalog_service_action` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"

    cfg := servicecatalog.ServiceActionConfig{
        Name: "example-name",
        AcceptLanguage: "example",
        Description: "example",
    }
    ubx.Resource(servicecatalog.ServiceAction, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceAction } from
      "jsr:@ubx/sdk-aws/aws/servicecatalog/service_action";

    resource(ServiceAction, "example", {
      name: "example",
      acceptLanguage: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.servicecatalog import (
        ServiceAction,
        ServiceActionConfig,
    )

    cfg = ServiceActionConfig(
        name="example",
        accept_language="example",
        description="example",
    )
    ubx.resource(ServiceAction, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_servicecatalog_service_action with name,
accept_language, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
