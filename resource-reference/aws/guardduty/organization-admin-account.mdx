---
title: "aws_guardduty_organization_admin_account"
description: "Real, generated bindings for `aws_guardduty_organization_admin_account`."
---

`aws_guardduty_organization_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization admin account"})

    		ubx.Resource(guardduty.OrganizationAdminAccount, "ci-runner", guardduty.OrganizationAdminAccountConfig{
    			AdminAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationAdminAccount } from "jsr:@ubx/sdk-aws/aws/guardduty/organization_admin_account";

    export default stack("payments", () => {
      intent({ summary: "payments own organization admin account" });

      resource(OrganizationAdminAccount, "ci-runner", {
        adminAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.guardduty import OrganizationAdminAccount, OrganizationAdminAccountConfig

    def describe():
        ubx.intent("payments own organization admin account")

        ubx.resource(OrganizationAdminAccount, "ci-runner", OrganizationAdminAccountConfig(
            admin_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guardduty_organization_admin_account, with admin
    account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="admin_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
