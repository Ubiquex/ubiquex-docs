---
title: "aws_guardduty_detector_feature"
description: "Real, generated bindings for `aws_guardduty_detector_feature`."
---

`aws_guardduty_detector_feature` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own detector feature"})

    		ubx.Resource(guardduty.DetectorFeature, "ci-runner", guardduty.DetectorFeatureConfig{
    			DetectorId: "example",
    			Name:       "ci-runner",
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DetectorFeature } from "jsr:@ubx/sdk-aws/aws/guardduty/detector_feature";

    export default stack("payments", () => {
      intent({ summary: "payments own detector feature" });

      resource(DetectorFeature, "ci-runner", {
        detectorId: "example",
        name: "ci-runner",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.guardduty import DetectorFeature, DetectorFeatureConfig

    def describe():
        ubx.intent("payments own detector feature")

        ubx.resource(DetectorFeature, "ci-runner", DetectorFeatureConfig(
            detector_id="example",
            name="ci-runner",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guardduty_detector_feature called "ci-runner", with
    detector id example, status example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="detector_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
