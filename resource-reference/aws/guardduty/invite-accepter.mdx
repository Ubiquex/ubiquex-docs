---
title: "aws_guardduty_invite_accepter"
description: "Real, generated bindings for `aws_guardduty_invite_accepter`."
---

`aws_guardduty_invite_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/guardduty"

    cfg := guardduty.InviteAccepterConfig{
        DetectorId: "example",
        MasterAccountId: "example",
    }
    ubx.Resource(guardduty.InviteAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InviteAccepter } from
      "jsr:@ubx/sdk-aws/aws/guardduty/invite_accepter";

    resource(InviteAccepter, "example", {
      detectorId: "example",
      masterAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.guardduty import (
        InviteAccepter,
        InviteAccepterConfig,
    )

    cfg = InviteAccepterConfig(
        detector_id="example",
        master_account_id="example",
    )
    ubx.resource(InviteAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_guardduty_invite_accepter with detector_id,
master_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="detector_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="master_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
