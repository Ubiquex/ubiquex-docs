---
title: "aws_guardduty_invite_accepter"
description: "Real, generated bindings for `aws_guardduty_invite_accepter`."
---

`aws_guardduty_invite_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own invite accepter"})

    		ubx.Resource(guardduty.InviteAccepter, "ci-runner", guardduty.InviteAccepterConfig{
    			DetectorId:      "example",
    			MasterAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InviteAccepter } from "jsr:@ubx/sdk-aws/aws/guardduty/invite_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own invite accepter" });

      resource(InviteAccepter, "ci-runner", {
        detectorId: "example",
        masterAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.guardduty import InviteAccepter, InviteAccepterConfig

    def describe():
        ubx.intent("payments own invite accepter")

        ubx.resource(InviteAccepter, "ci-runner", InviteAccepterConfig(
            detector_id="example",
            master_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guardduty_invite_accepter, with detector id example,
    master account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="detector_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="master_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
