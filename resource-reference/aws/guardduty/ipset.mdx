---
title: "aws_guard_duty_ipset"
description: "`aws_guard_duty_ipset` is a real, trusted list of IP addresses an `aws_guard_duty_detector` never generates findings against..."
---

`aws_guard_duty_ipset` is a real, trusted list of IP addresses an `aws_guard_duty_detector` never generates findings against, for a caller's own known-safe scanners or internal tools that would otherwise look like malicious activity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ipset"})

    		ubx.Resource(guardduty.Ipset, "example", guardduty.IpsetConfig{
    			Format:              "example",
    			Location:            "example",
    			Name:                "example",
    			Activate:            true,
    			DetectorId:          "example",
    			ExpectedBucketOwner: "example",
    			Tags:                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Ipset } from "@ubx/sdk-aws/aws/guardduty/ipset";

    export default stack("example", () => {
      intent({ summary: "example own ipset" });

      resource(Ipset, "example", {
        format: "example",
        location: "example",
        name: "example",
        activate: true,
        detectorId: "example",
        expectedBucketOwner: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.guardduty import Ipset, IpsetConfig
    from ubx.aws.guardduty.ipset import Ipset_Tags

    def describe():
        ubx.intent("example own ipset")

        ubx.resource(Ipset, "example", IpsetConfig(
            format="example",
            location="example",
            name="example",
            activate=True,
            detector_id="example",
            expected_bucket_owner="example",
            tags=[Ipset_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guard_duty_ipset called "example", with format
    example, location example, activate true, detector id example,
    expected bucket owner example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="activate" type="bool">
  Indicates whether the GuardDuty IP set is activated (enabled) for use in threat detection, where true activates the set and false deactivates it. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="detector_id" type="string">
  The detector_id specifies the unique ID of the Amazon GuardDuty detector to which this IPSet is associated, identifying the GuardDuty account and region context for the IP set. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The AWS account ID of the owner of the S3 bucket where the GuardDuty IPSet file is stored, used to validate that the bucket is owned by the expected account. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="format" type="string" required>
  Specifies the format of the file that contains the IP set, such as TXT, STIX, OTX_CSV, ALIEN_VAULT, or PROOF_POINT. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="location" type="string" required>
  The URI (typically an S3 object URL) of the file that contains the IP addresses used by this GuardDuty IPSet. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The friendly name of the GuardDuty IP set, used to identify it in the GuardDuty console and API. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags to apply to the GuardDuty IPSet, which can be used for organizing, identifying, and controlling access to the resource via IAM policies. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to this GuardDuty IP set, used to organize and manage the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value for a tag key used to manage and categorize the GuardDuty IP set. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned by GuardDuty to this IPSet. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
