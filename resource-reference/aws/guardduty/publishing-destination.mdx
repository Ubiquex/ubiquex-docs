---
title: "aws_guardduty_publishing_destination"
description: "Real, generated bindings for `aws_guardduty_publishing_destination`."
---

`aws_guardduty_publishing_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own publishing destination"})

    		ubx.Resource(guardduty.PublishingDestination, "ci-runner", guardduty.PublishingDestinationConfig{
    			DestinationArn:  "arn:aws:iam::123456789012:policy/ci",
    			DetectorId:      "example",
    			KmsKeyArn:       "arn:aws:iam::123456789012:policy/ci",
    			DestinationType: "example",
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublishingDestination } from "jsr:@ubx/sdk-aws/aws/guardduty/publishing_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own publishing destination" });

      resource(PublishingDestination, "ci-runner", {
        destinationArn: "arn:aws:iam::123456789012:policy/ci",
        detectorId: "example",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        destinationType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.guardduty import PublishingDestination, PublishingDestinationConfig

    def describe():
        ubx.intent("payments own publishing destination")

        ubx.resource(PublishingDestination, "ci-runner", PublishingDestinationConfig(
            destination_arn="arn:aws:iam::123456789012:policy/ci",
            detector_id="example",
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            destination_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_guardduty_publishing_destination, with destination arn
    arn:aws:iam::123456789012:policy/ci, detector id example, kms key
    arn arn:aws:iam::123456789012:policy/ci, destination type
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destination_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="detector_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="destination_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
