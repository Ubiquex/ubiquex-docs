---
title: "aws_dsql_cluster_peering"
description: "Real, generated bindings for `aws_dsql_cluster_peering`."
---

`aws_dsql_cluster_peering` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dsql"

    cfg := dsql.ClusterPeeringConfig{
        Clusters: []string{"example"},
        Identifier: "example",
        WitnessRegion: "example",
    }
    ubx.Resource(dsql.ClusterPeering, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterPeering } from
      "jsr:@ubx/sdk-aws/aws/dsql/cluster_peering";

    resource(ClusterPeering, "example", {
      clusters: ["example"],
      identifier: "example",
      witnessRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dsql import ClusterPeering, ClusterPeeringConfig

    cfg = ClusterPeeringConfig(
        clusters=["example"],
        identifier="example",
        witness_region="example",
    )
    ubx.resource(ClusterPeering, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dsql_cluster_peering with clusters, identifier,
witness_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="clusters" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="witness_region" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
