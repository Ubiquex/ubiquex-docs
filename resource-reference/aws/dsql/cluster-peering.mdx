---
title: "aws_dsql_cluster_peering"
description: "Real, generated bindings for `aws_dsql_cluster_peering`."
---

`aws_dsql_cluster_peering` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dsql "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dsql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster peering"})

    		ubx.Resource(dsql.ClusterPeering, "ci-runner", dsql.ClusterPeeringConfig{
    			Clusters:      []string{"example"},
    			Identifier:    "example",
    			WitnessRegion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterPeering } from "jsr:@ubx/sdk-aws/aws/dsql/cluster_peering";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster peering" });

      resource(ClusterPeering, "ci-runner", {
        clusters: ["example"],
        identifier: "example",
        witnessRegion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dsql import ClusterPeering, ClusterPeeringConfig

    def describe():
        ubx.intent("payments own cluster peering")

        ubx.resource(ClusterPeering, "ci-runner", ClusterPeeringConfig(
            clusters=["example"],
            identifier="example",
            witness_region="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dsql_cluster_peering, with clusters a set value,
    identifier example, witness region example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="clusters" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="witness_region" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
