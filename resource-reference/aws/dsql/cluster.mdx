---
title: "aws_dsql_cluster"
description: "`aws_dsql_cluster` creates a real Aurora DSQL cluster, a serverless, PostgreSQL-compatible distributed SQL database with independently scaling compute..."
---

`aws_dsql_cluster` creates a real Aurora DSQL cluster, a serverless, PostgreSQL-compatible distributed SQL database with independently scaling compute, commit, and storage components and no single leader, built for 99.99% single-Region or 99.999% multi-Region availability without a caller sharding or upgrading instances.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dsql "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/dsql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own cluster"})

    		ubx.Resource(dsql.Cluster, "example", dsql.ClusterConfig{
    			DeletionProtectionEnabled: true,
    			KmsEncryptionKey:          "example",
    			MultiRegionProperties:     map[string]any{"clusters": []string{"example"}},
    			PolicyDocument:            "example",
    			Tags:                      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "@ubx/sdk-aws/aws/dsql/cluster";

    export default stack("example", () => {
      intent({ summary: "example own cluster" });

      resource(Cluster, "example", {
        deletionProtectionEnabled: true,
        kmsEncryptionKey: "example",
        multiRegionProperties: { clusters: ["example"] },
        policyDocument: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dsql import Cluster, ClusterConfig
    from ubx.aws.dsql.cluster import Cluster_MultiRegionProperties, Cluster_Tags

    def describe():
        ubx.intent("example own cluster")

        ubx.resource(Cluster, "example", ClusterConfig(
            deletion_protection_enabled=True,
            kms_encryption_key="example",
            multi_region_properties=Cluster_MultiRegionProperties(clusters=["example"]),
            policy_document="example",
            tags=[Cluster_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dsql_cluster, with deletion protection enabled true,
    kms encryption key example, multi region properties a set value,
    policy document example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_protection_enabled" type="bool">
  Whether deletion protection is enabled in this cluster. Optional.
</ResponseField>

<ResponseField name="kms_encryption_key" type="string">
  The KMS key that encrypts data on the cluster. Optional.
</ResponseField>

<ResponseField name="multi_region_properties" type="object">
  The Multi-region properties associated to this cluster. Optional.
  <Expandable title="properties">
    <ResponseField name="clusters" type="list(string)">
      Specifies the Amazon Resource Names (ARNs) of the linked regional DSQL clusters that form the multi-region cluster. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="witness_region" type="string">
      The witness region in a multi-region cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_document" type="string">
  The IAM policy applied to the cluster resource. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the DSQL cluster, used to organize and identify the cluster for management and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag attached to the DSQL cluster, used for metadata and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time of when the cluster was created in ISO-8601 format. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_details" type="object">
  The encryption configuration details for the cluster. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="encryption_status" type="string">
      The status of encryption for the cluster. Optional.
    </ResponseField>
    <ResponseField name="encryption_type" type="string">
      The type of encryption that protects data in the cluster. Optional.
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
      The Amazon Resource Name (ARN) of the KMS key that encrypts data in the cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="endpoint" type="string">
  The DSQL cluster endpoint. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identifier" type="string">
  The ID of the created cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_version" type="string">
  The version number of the cluster's resource based policy Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The Amazon Resource Name (ARN) for the cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_endpoint" type="string">
  The DSQL cluster VPC endpoint. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_endpoint_service_name" type="string">
  The VPC endpoint service name. Computed by AWS after `ship`.
</ResponseField>
