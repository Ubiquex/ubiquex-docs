---
title: "aws_dsql_cluster"
description: "Real, generated bindings for `aws_dsql_cluster`."
---

`aws_dsql_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/dsql"

    cfg := dsql.ClusterConfig{
        DeletionProtectionEnabled: true,
        ForceDestroy: true,
    }
    ubx.Resource(dsql.Cluster, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Cluster } from "jsr:@ubx/sdk-aws/aws/dsql/cluster";

    resource(Cluster, "example", {
      deletionProtectionEnabled: true,
      forceDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dsql import Cluster, ClusterConfig

    cfg = ClusterConfig(
        deletion_protection_enabled=True,
        force_destroy=True,
    )
    ubx.resource(Cluster, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dsql_cluster with deletion_protection_enabled,
force_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deletion_protection_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_encryption_key" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deletion_protection_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="encryption_details" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="encryption_status" type="string">
    </ResponseField>
    <ResponseField name="encryption_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_encryption_key" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_endpoint_service_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>
