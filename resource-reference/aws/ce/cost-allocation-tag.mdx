---
title: "aws_ce_cost_allocation_tag"
description: "Real, generated bindings for `aws_ce_cost_allocation_tag`."
---

`aws_ce_cost_allocation_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cost allocation tag"})

    		ubx.Resource(ce.CostAllocationTag, "ci-runner", ce.CostAllocationTagConfig{
    			Status: "example",
    			TagKey: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CostAllocationTag } from "jsr:@ubx/sdk-aws/aws/ce/cost_allocation_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own cost allocation tag" });

      resource(CostAllocationTag, "ci-runner", {
        status: "example",
        tagKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ce import CostAllocationTag, CostAllocationTagConfig

    def describe():
        ubx.intent("payments own cost allocation tag")

        ubx.resource(CostAllocationTag, "ci-runner", CostAllocationTagConfig(
            status="example",
            tag_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ce_cost_allocation_tag, with status example, tag key
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
