---
title: "aws_ce_cost_allocation_tag"
description: "Real, generated bindings for `aws_ce_cost_allocation_tag`."
---

`aws_ce_cost_allocation_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ce"

    cfg := ce.CostAllocationTagConfig{
        Status: "example",
        TagKey: "example",
    }
    ubx.Resource(ce.CostAllocationTag, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CostAllocationTag } from
      "jsr:@ubx/sdk-aws/aws/ce/cost_allocation_tag";

    resource(CostAllocationTag, "example", {
      status: "example",
      tagKey: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ce import (
        CostAllocationTag,
        CostAllocationTagConfig,
    )

    cfg = CostAllocationTagConfig(
        status="example",
        tag_key="example",
    )
    ubx.resource(CostAllocationTag, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ce_cost_allocation_tag with status, tag_key.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
