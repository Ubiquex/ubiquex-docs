---
title: "aws_ce_cost_category"
description: "Real, generated bindings for `aws_ce_cost_category`."
---

`aws_ce_cost_category` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cost category"})

    		ubx.Resource(ce.CostCategory, "ci-runner", ce.CostCategoryConfig{
    			Name:         "ci-runner",
    			RuleVersion:  "example",
    			DefaultValue: "example",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CostCategory } from "jsr:@ubx/sdk-aws/aws/ce/cost_category";

    export default stack("payments", () => {
      intent({ summary: "payments own cost category" });

      resource(CostCategory, "ci-runner", {
        name: "ci-runner",
        ruleVersion: "example",
        defaultValue: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ce import CostCategory, CostCategoryConfig

    def describe():
        ubx.intent("payments own cost category")

        ubx.resource(CostCategory, "ci-runner", CostCategoryConfig(
            name="ci-runner",
            rule_version="example",
            default_value="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ce_cost_category called "ci-runner", with rule version
    example, default value example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_value" type="string">
  Optional.
</ResponseField>

<ResponseField name="effective_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="effective_end" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="effective_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
