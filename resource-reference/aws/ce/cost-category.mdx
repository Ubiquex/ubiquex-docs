---
title: "aws_ce_cost_category"
description: "Real, generated bindings for `aws_ce_cost_category`."
---

`aws_ce_cost_category` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ce"

    cfg := ce.CostCategoryConfig{
        Name: "example-name",
        RuleVersion: "example",
        DefaultValue: "example",
        EffectiveStart: "example",
    }
    ubx.Resource(ce.CostCategory, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CostCategory } from
      "jsr:@ubx/sdk-aws/aws/ce/cost_category";

    resource(CostCategory, "example", {
      name: "example",
      ruleVersion: "example",
      defaultValue: "example",
      effectiveStart: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ce import CostCategory, CostCategoryConfig

    cfg = CostCategoryConfig(
        name="example",
        rule_version="example",
        default_value="example",
        effective_start="example",
    )
    ubx.resource(CostCategory, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ce_cost_category with name, rule_version,
default_value, effective_start.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_value" type="string">
  Optional.
</ResponseField>

<ResponseField name="effective_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="effective_end" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="effective_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
