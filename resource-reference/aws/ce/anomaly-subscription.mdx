---
title: "aws_ce_anomaly_subscription"
description: "Real, generated bindings for `aws_ce_anomaly_subscription`."
---

`aws_ce_anomaly_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own anomaly subscription"})

    		ubx.Resource(ce.AnomalySubscription, "ci-runner", ce.AnomalySubscriptionConfig{
    			Frequency:      "example",
    			MonitorArnList: []string{"example"},
    			Name:           "ci-runner",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnomalySubscription } from "jsr:@ubx/sdk-aws/aws/ce/anomaly_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own anomaly subscription" });

      resource(AnomalySubscription, "ci-runner", {
        frequency: "example",
        monitorArnList: ["example"],
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ce import AnomalySubscription, AnomalySubscriptionConfig

    def describe():
        ubx.intent("payments own anomaly subscription")

        ubx.resource(AnomalySubscription, "ci-runner", AnomalySubscriptionConfig(
            frequency="example",
            monitor_arn_list=["example"],
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ce_anomaly_subscription called "ci-runner", with
    frequency example, monitor arn list a set value, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="frequency" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monitor_arn_list" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
