---
title: "aws_ce_anomaly_subscription"
description: "Real, generated bindings for `aws_ce_anomaly_subscription`."
---

`aws_ce_anomaly_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ce"

    cfg := ce.AnomalySubscriptionConfig{
        Frequency: "example",
        MonitorArnList: []string{"example"},
        Name: "example-name",
        AccountId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ce.AnomalySubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AnomalySubscription } from
      "jsr:@ubx/sdk-aws/aws/ce/anomaly_subscription";

    resource(AnomalySubscription, "example", {
      frequency: "example",
      monitorArnList: ["example"],
      name: "example",
      accountId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ce import (
        AnomalySubscription,
        AnomalySubscriptionConfig,
    )

    cfg = AnomalySubscriptionConfig(
        frequency="example",
        monitor_arn_list=["example"],
        name="example",
        account_id="example",
        tags={"key": "value"},
    )
    ubx.resource(AnomalySubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ce_anomaly_subscription with frequency,
monitor_arn_list, name, account_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="frequency" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monitor_arn_list" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
