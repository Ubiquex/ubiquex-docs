---
title: "aws_ce_anomaly_monitor"
description: "`aws_ce_anomaly_monitor` watches spend segmented by AWS service, linked account, cost allocation tag, or `aws_ce_cost_category`..."
---

`aws_ce_anomaly_monitor` watches spend segmented by AWS service, linked account, cost allocation tag, or `aws_ce_cost_category`, using machine learning to detect unusual patterns against that segment's own normal spend, the real detection engine an `aws_ce_anomaly_subscription`'s alerts are triggered by.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own anomaly monitor"})

    		ubx.Resource(ce.AnomalyMonitor, "ci-runner", ce.AnomalyMonitorConfig{
    			MonitorName:          "example-monitor-name",
    			MonitorType:          "example",
    			MonitorDimension:     "example",
    			MonitorSpecification: "example",
    			ResourceTags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnomalyMonitor } from "./local-sdk/aws/sdk/typescript/aws/ce/anomaly_monitor.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own anomaly monitor" });

      resource(AnomalyMonitor, "ci-runner", {
        monitorName: "example-monitor-name",
        monitorType: "example",
        monitorDimension: "example",
        monitorSpecification: "example",
        resourceTags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ce import AnomalyMonitor, AnomalyMonitorConfig

    def describe():
        ubx.intent("payments own anomaly monitor")

        ubx.resource(AnomalyMonitor, "ci-runner", AnomalyMonitorConfig(
            monitor_name="example-monitor-name",
            monitor_type="example",
            monitor_dimension="example",
            monitor_specification="example",
            resource_tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ce_anomaly_monitor, with monitor name
    example-monitor-name, monitor type example, monitor dimension
    example, monitor specification example, resource tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="monitor_dimension" type="string">
  The dimensions to evaluate Optional.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  The name of the monitor. Required.
</ResponseField>

<ResponseField name="monitor_specification" type="string">
  A JSON-formatted string that defines the custom monitoring criteria for the cost anomaly monitor, such as filtering by services, linked accounts, or cost categories, used when the monitor type is CUSTOM. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="monitor_type" type="string" required>
  Specifies whether the anomaly monitor is a DIMENSIONAL monitor (which watches a specific dimension like service or linked account) or a CUSTOM monitor (which uses a custom threshold on the anomaly score). Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_tags" type="list(object)">
  Tags to assign to monitor. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key for a resource tag applied to the Cost Explorer anomaly monitor, used for identifying and organizing the monitor among AWS resources. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a resource tag attached to the AWS Cost Explorer anomaly monitor, used for tagging and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_date" type="string">
  The date when the monitor was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dimensional_value_count" type="number">
  The value for evaluated dimensions. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_evaluated_date" type="string">
  The date when the monitor last evaluated for anomalies. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  The date when the monitor was last updated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="monitor_arn" type="string">
  Monitor ARN Computed by AWS after `ship`.
</ResponseField>
