---
title: "aws_personalize_dataset_group"
description: "`aws_personalize_dataset_group` creates a real top-level container that groups the `aws_personalize_dataset`s Amazon Personalize trains..."
---

`aws_personalize_dataset_group` creates a real top-level container that groups the `aws_personalize_dataset`s Amazon Personalize trains an `aws_personalize_solution` from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dataset group"})

    		ubx.Resource(personalize.DatasetGroup, "ci-runner", personalize.DatasetGroupConfig{
    			Name:      "ci-runner",
    			Domain:    "example",
    			KmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
    			RoleArn:   "arn:aws:iam::123456789012:policy/ci",
    			Tags:      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatasetGroup } from "./local-sdk/aws/sdk/typescript/aws/personalize/dataset_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dataset group" });

      resource(DatasetGroup, "ci-runner", {
        name: "ci-runner",
        domain: "example",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.personalize import DatasetGroup, DatasetGroupConfig

    def describe():
        ubx.intent("payments own dataset group")

        ubx.resource(DatasetGroup, "ci-runner", DatasetGroupConfig(
            name="ci-runner",
            domain="example",
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_personalize_dataset_group called "ci-runner", with
    domain example, kms key arn arn:aws:iam::123456789012:policy/ci,
    role arn arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain" type="string">
  The domain of a Domain dataset group. Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  The Amazon Resource Name(ARN) of a AWS Key Management Service (KMS) key used to encrypt the datasets. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name for the new dataset group. Required.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The ARN of the AWS Identity and Access Management (IAM) role that has permissions to access the AWS Key Management Service (KMS) key. Supplying an IAM role is only valid when also specifying a KMS key. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags used to organize, track, or control access for this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag (key-value pair) attached to the Amazon Personalize dataset group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="dataset_group_arn" type="string">
  The Amazon Resource Name (ARN) of the dataset group. Computed by AWS after `ship`.
</ResponseField>
