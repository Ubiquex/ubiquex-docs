---
title: "aws_cloud9_environment_membership"
description: "Real, generated bindings for `aws_cloud9_environment_membership`."
---

`aws_cloud9_environment_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud9 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud9"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment membership"})

    		ubx.Resource(cloud9.EnvironmentMembership, "ci-runner", cloud9.EnvironmentMembershipConfig{
    			EnvironmentId: "example",
    			Permissions:   "example",
    			UserArn:       "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentMembership } from "jsr:@ubx/sdk-aws/aws/cloud9/environment_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own environment membership" });

      resource(EnvironmentMembership, "ci-runner", {
        environmentId: "example",
        permissions: "example",
        userArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloud9 import EnvironmentMembership, EnvironmentMembershipConfig

    def describe():
        ubx.intent("payments own environment membership")

        ubx.resource(EnvironmentMembership, "ci-runner", EnvironmentMembershipConfig(
            environment_id="example",
            permissions="example",
            user_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud9_environment_membership, with environment id
    example, permissions example, user arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="environment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="permissions" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
