---
title: "aws_cloud9_environment_membership"
description: "Real, generated bindings for `aws_cloud9_environment_membership`."
---

`aws_cloud9_environment_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloud9"

    cfg := cloud9.EnvironmentMembershipConfig{
        EnvironmentId: "example",
        Permissions: "example",
        UserArn: "example",
    }
    ubx.Resource(cloud9.EnvironmentMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EnvironmentMembership } from
      "jsr:@ubx/sdk-aws/aws/cloud9/environment_membership";

    resource(EnvironmentMembership, "example", {
      environmentId: "example",
      permissions: "example",
      userArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloud9 import (
        EnvironmentMembership,
        EnvironmentMembershipConfig,
    )

    cfg = EnvironmentMembershipConfig(
        environment_id="example",
        permissions="example",
        user_arn="example",
    )
    ubx.resource(EnvironmentMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloud9_environment_membership with environment_id,
permissions, user_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="environment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="permissions" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
