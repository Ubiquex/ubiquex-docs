---
title: "aws_arczonalshift_zonal_autoshift_configuration"
description: "Real, generated bindings for `aws_arczonalshift_zonal_autoshift_configuration`."
---

`aws_arczonalshift_zonal_autoshift_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arczonalshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/arczonalshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own zonal autoshift configuration"})

    		ubx.Resource(arczonalshift.ZonalAutoshiftConfiguration, "ci-runner", arczonalshift.ZonalAutoshiftConfigurationConfig{
    			ResourceArn:          "arn:aws:iam::123456789012:policy/ci",
    			ZonalAutoshiftStatus: "example",
    			AllowedWindows:       []string{"example"},
    			BlockedDates:         []string{"example"},
    			BlockedWindows:       []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZonalAutoshiftConfiguration } from "jsr:@ubx/sdk-aws/aws/arczonalshift/zonal_autoshift_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own zonal autoshift configuration" });

      resource(ZonalAutoshiftConfiguration, "ci-runner", {
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        zonalAutoshiftStatus: "example",
        allowedWindows: ["example"],
        blockedDates: ["example"],
        blockedWindows: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.arczonalshift import ZonalAutoshiftConfiguration, ZonalAutoshiftConfigurationConfig

    def describe():
        ubx.intent("payments own zonal autoshift configuration")

        ubx.resource(ZonalAutoshiftConfiguration, "ci-runner", ZonalAutoshiftConfigurationConfig(
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            zonal_autoshift_status="example",
            allowed_windows=["example"],
            blocked_dates=["example"],
            blocked_windows=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_arczonalshift_zonal_autoshift_configuration, with
    resource arn arn:aws:iam::123456789012:policy/ci, zonal autoshift
    status example, allowed windows a set value, blocked dates a set
    value, blocked windows a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_windows" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="blocked_dates" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="blocked_windows" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="zonal_autoshift_status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
