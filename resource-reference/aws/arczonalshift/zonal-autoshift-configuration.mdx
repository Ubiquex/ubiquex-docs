---
title: "aws_arczonalshift_zonal_autoshift_configuration"
description: "Real, generated bindings for `aws_arczonalshift_zonal_autoshift_configuration`."
---

`aws_arczonalshift_zonal_autoshift_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/arczonalshift"

    cfg := arczonalshift.ZonalAutoshiftConfigurationConfig{
        ResourceArn: "example",
        ZonalAutoshiftStatus: "example",
        AllowedWindows: []string{"example"},
        BlockedDates: []string{"example"},
    }
    ubx.Resource(
        arczonalshift.ZonalAutoshiftConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ZonalAutoshiftConfiguration } from
      "jsr:@ubx/sdk-aws/aws/arczonalshift/zonal_autoshift_configuration";

    resource(ZonalAutoshiftConfiguration, "example", {
      resourceArn: "example",
      zonalAutoshiftStatus: "example",
      allowedWindows: ["example"],
      blockedDates: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.arczonalshift import (
        ZonalAutoshiftConfiguration,
        ZonalAutoshiftConfigurationConfig,
    )

    cfg = ZonalAutoshiftConfigurationConfig(
        resource_arn="example",
        zonal_autoshift_status="example",
        allowed_windows=["example"],
        blocked_dates=["example"],
    )
    ubx.resource(ZonalAutoshiftConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_arczonalshift_zonal_autoshift_configuration with
resource_arn, zonal_autoshift_status, allowed_windows,
blocked_dates.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_windows" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="blocked_dates" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="blocked_windows" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="zonal_autoshift_status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
