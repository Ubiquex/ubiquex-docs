---
title: "aws_arczonal_shift_autoshift_observer_notification_status"
description: "`aws_arczonal_shift_autoshift_observer_notification_status` turns on autoshift observer notifications..."
---

`aws_arczonal_shift_autoshift_observer_notification_status` turns on autoshift observer notifications, EventBridge events for every autoshift ARC starts account-wide, even for resources that never had `aws_arczonal_shift_zonal_autoshift_configuration` enabled on them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arczonalshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/arczonalshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own shift autoshift observer notification status"})

    		ubx.Resource(arczonalshift.AutoshiftObserverNotificationStatus, "example", arczonalshift.AutoshiftObserverNotificationStatusConfig{
    			Status: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutoshiftObserverNotificationStatus } from "@ubx/sdk-aws/aws/arczonalshift/autoshift_observer_notification_status";

    export default stack("example", () => {
      intent({
        summary: "example own shift autoshift observer notification status",
      });

      resource(AutoshiftObserverNotificationStatus, "example", {
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.arczonalshift.autoshift_observer_notification_status import AutoshiftObserverNotificationStatus, AutoshiftObserverNotificationStatusConfig

    def describe():
        ubx.intent("example own shift autoshift observer notification status")

        ubx.resource(AutoshiftObserverNotificationStatus, "example", AutoshiftObserverNotificationStatusConfig(
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_arczonal_shift_autoshift_observer_notification_status,
    with status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="status" type="string" required>
  Specifies whether the autoshift observer notification for the account is enabled (ENABLED) or disabled (DISABLED). Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="account_id" type="string">
  User account id, used as part of the primary identifier for the resource Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region, used as part of the primary identifier for the resource Computed by AWS after `ship`.
</ResponseField>
