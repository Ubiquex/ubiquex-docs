---
title: "aws_arczonalshift_autoshift_observer_notification_status"
description: "Real, generated bindings for `aws_arczonalshift_autoshift_observer_notification_status`."
---

`aws_arczonalshift_autoshift_observer_notification_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arczonalshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/arczonalshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own autoshift observer notification status"})

    		ubx.Resource(arczonalshift.AutoshiftObserverNotificationStatus, "ci-runner", arczonalshift.AutoshiftObserverNotificationStatusConfig{
    			Status: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutoshiftObserverNotificationStatus } from "jsr:@ubx/sdk-aws/aws/arczonalshift/autoshift_observer_notification_status";

    export default stack("payments", () => {
      intent({ summary: "payments own autoshift observer notification status" });

      resource(AutoshiftObserverNotificationStatus, "ci-runner", {
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.arczonalshift import AutoshiftObserverNotificationStatus, AutoshiftObserverNotificationStatusConfig

    def describe():
        ubx.intent("payments own autoshift observer notification status")

        ubx.resource(AutoshiftObserverNotificationStatus, "ci-runner", AutoshiftObserverNotificationStatusConfig(
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_arczonalshift_autoshift_observer_notification_status,
    with status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
