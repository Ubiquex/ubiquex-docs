---
title: "aws_arczonalshift_autoshift_observer_notification_status"
description: "Real, generated bindings for `aws_arczonalshift_autoshift_observer_notification_status`."
---

`aws_arczonalshift_autoshift_observer_notification_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/arczonalshift"

    cfg := arczonalshift.AutoshiftObserverNotificationStatusConfig{
        Status: "example",
    }
    ubx.Resource(
        arczonalshift.AutoshiftObserverNotificationStatus,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AutoshiftObserverNotificationStatus } from
      "jsr:@ubx/sdk-aws/aws/arczonalshift/autoshift_observer_notification_status";

    resource(AutoshiftObserverNotificationStatus, "example", {
      status: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.arczonalshift import (
        AutoshiftObserverNotificationStatus,
        AutoshiftObserverNotificationStatusConfig,
    )

    cfg = AutoshiftObserverNotificationStatusConfig(
        status="example",
    )
    ubx.resource(AutoshiftObserverNotificationStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision
aws_arczonalshift_autoshift_observer_notification_status with
status.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
