---
title: "aws_certificate_manager_acme_external_account_binding"
description: "`aws_certificate_manager_acme_external_account_binding` generates a real credential pair, a key identifier and HMAC key..."
---

`aws_certificate_manager_acme_external_account_binding` generates a real credential pair, a key identifier and HMAC key, that authorizes a specific ACME client to register an account against an `aws_certificate_manager_acme_endpoint`, with an IAM role controlling exactly what that client is allowed to do.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	certificatemanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/certificatemanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager acme external account binding"})

    		ubx.Resource(certificatemanager.AcmeExternalAccountBinding, "example", certificatemanager.AcmeExternalAccountBindingConfig{
    			AcmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
    			RoleArn:         "arn:aws:iam::123456789012:policy/example",
    			Expiration:      map[string]any{"type": "example"},
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AcmeExternalAccountBinding } from "@ubx/sdk-aws/aws/certificatemanager/acme_external_account_binding";

    export default stack("example", () => {
      intent({ summary: "example own manager acme external account binding" });

      resource(AcmeExternalAccountBinding, "example", {
        acmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
        expiration: { type: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.certificatemanager.acme_external_account_binding import AcmeExternalAccountBinding, AcmeExternalAccountBindingConfig

    def describe():
        ubx.intent("example own manager acme external account binding")

        ubx.resource(AcmeExternalAccountBinding, "example", AcmeExternalAccountBindingConfig(
            acme_endpoint_arn="arn:aws:iam::123456789012:policy/example",
            role_arn="arn:aws:iam::123456789012:policy/example",
            expiration={"type": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_certificate_manager_acme_external_account_binding,
    with acme endpoint arn arn:aws:iam::123456789012:policy/example,
    role arn arn:aws:iam::123456789012:policy/example, expiration a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="acme_endpoint_arn" type="string" required>
  The ARN of the ACME endpoint this binding is associated with. Required.
</ResponseField>

<ResponseField name="expiration" type="object">
  The expiration configuration for the external account binding. Optional.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      The time unit for the expiration value. Required.
    </ResponseField>
    <ResponseField name="value" type="number" required>
      The expiration value. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The IAM role ARN for cross-account access. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags associated with the external account binding. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="acme_external_account_binding_arn" type="string">
  The Amazon Resource Name (ARN) of the external account binding. Computed by AWS after `ship`.
</ResponseField>
