---
title: "aws_pinpointsmsvoicev2_event_destination"
description: "Real, generated bindings for `aws_pinpointsmsvoicev2_event_destination`."
---

`aws_pinpointsmsvoicev2_event_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpointsmsvoicev2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpointsmsvoicev2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event destination"})

    		ubx.Resource(pinpointsmsvoicev2.EventDestination, "ci-runner", pinpointsmsvoicev2.EventDestinationConfig{
    			ConfigurationSetName: "example-configuration-set-name",
    			EventDestinationName: "example-event-destination-name",
    			MatchingEventTypes:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventDestination } from "jsr:@ubx/sdk-aws/aws/pinpointsmsvoicev2/event_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own event destination" });

      resource(EventDestination, "ci-runner", {
        configurationSetName: "example-configuration-set-name",
        eventDestinationName: "example-event-destination-name",
        matchingEventTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpointsmsvoicev2 import EventDestination, EventDestinationConfig

    def describe():
        ubx.intent("payments own event destination")

        ubx.resource(EventDestination, "ci-runner", EventDestinationConfig(
            configuration_set_name="example-configuration-set-name",
            event_destination_name="example-event-destination-name",
            matching_event_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpointsmsvoicev2_event_destination, with
    configuration set name example-configuration-set-name, event
    destination name example-event-destination-name, matching event
    types a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="configuration_set_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="event_destination_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="matching_event_types" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="configuration_set_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
