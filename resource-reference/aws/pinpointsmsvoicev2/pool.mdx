---
title: "aws_pinpointsmsvoicev2_pool"
description: "Real, generated bindings for `aws_pinpointsmsvoicev2_pool`."
---

`aws_pinpointsmsvoicev2_pool` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpointsmsvoicev2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpointsmsvoicev2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pool"})

    		ubx.Resource(pinpointsmsvoicev2.Pool, "ci-runner", pinpointsmsvoicev2.PoolConfig{
    			MessageType:           "example",
    			OriginationIdentities: []string{"example"},
    			IsoCountryCode:        "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    			TwoWayChannelArn:      "arn:aws:iam::123456789012:policy/ci",
    			TwoWayChannelRole:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Pool } from "jsr:@ubx/sdk-aws/aws/pinpointsmsvoicev2/pool";

    export default stack("payments", () => {
      intent({ summary: "payments own pool" });

      resource(Pool, "ci-runner", {
        messageType: "example",
        originationIdentities: ["example"],
        isoCountryCode: "example",
        tags: { "managed-by": "ubx" },
        twoWayChannelArn: "arn:aws:iam::123456789012:policy/ci",
        twoWayChannelRole: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpointsmsvoicev2 import Pool, PoolConfig

    def describe():
        ubx.intent("payments own pool")

        ubx.resource(Pool, "ci-runner", PoolConfig(
            message_type="example",
            origination_identities=["example"],
            iso_country_code="example",
            tags={"managed-by": "ubx"},
            two_way_channel_arn="arn:aws:iam::123456789012:policy/ci",
            two_way_channel_role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpointsmsvoicev2_pool, with message type example,
    origination identities a set value, iso country code example,
    tags a set value, two way channel arn
    arn:aws:iam::123456789012:policy/ci, two way channel role
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_protection_enabled" type="bool">
  Whether deletion protection is enabled. When `true`, the pool cannot be deleted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="iso_country_code" type="string">
  Two-character code, in ISO 3166-1 alpha-2 format, for the country or region of the pool. This field is optional for origination identity types that are not country-specific. Optional.
</ResponseField>

<ResponseField name="message_type" type="string" required>
  Type of message. Required.
</ResponseField>

<ResponseField name="opt_out_list_name" type="string">
  Name of the opt-out list to associate with the pool. Inherited from the initial origination identity when omitted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="origination_identities" type="set(string)" required>
  Set of origination identity ARNs to associate with the pool. At least one origination identity is required at creation. Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="self_managed_opt_outs_enabled" type="bool">
  Whether the pool relies on self-managed opt-out handling. When `false`, AWS auto-replies to HELP/STOP requests and manages the opt-out list. Inherited from the initial origination identity when omitted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_routes_enabled" type="bool">
  Whether shared routes are enabled for the pool. When `true`, messages may use shared phone numbers or sender IDs in countries that allow it. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="two_way_channel_arn" type="string">
  ARN of the two-way channel that receives inbound messages. Optional.
</ResponseField>

<ResponseField name="two_way_channel_role" type="string">
  ARN of the IAM role that End User Messaging SMS assumes to publish inbound messages to the two-way channel. Optional.
</ResponseField>

<ResponseField name="two_way_enabled" type="bool">
  Whether inbound message reception is enabled for the pool. Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deletion_protection_enabled" type="bool">
  Whether deletion protection is enabled. When `true`, the pool cannot be deleted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="opt_out_list_name" type="string">
  Name of the opt-out list to associate with the pool. Inherited from the initial origination identity when omitted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="self_managed_opt_outs_enabled" type="bool">
  Whether the pool relies on self-managed opt-out handling. When `false`, AWS auto-replies to HELP/STOP requests and manages the opt-out list. Inherited from the initial origination identity when omitted. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_routes_enabled" type="bool">
  Whether shared routes are enabled for the pool. When `true`, messages may use shared phone numbers or sender IDs in countries that allow it. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="two_way_enabled" type="bool">
  Whether inbound message reception is enabled for the pool. Optional; if omitted, computed by AWS.
</ResponseField>
