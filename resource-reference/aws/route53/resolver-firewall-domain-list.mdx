---
title: "aws_route53_resolver_firewall_domain_list"
description: "`aws_route53_resolver_firewall_domain_list` defines a real list of domains that an `aws_route53_resolver_firewall_rule_group`'s rules match outbound..."
---

`aws_route53_resolver_firewall_domain_list` defines a real list of domains that an `aws_route53_resolver_firewall_rule_group`'s rules match outbound DNS queries against, inside a VPC, distinct from the newer Global Resolver's own `aws_route53_global_resolver_firewall_domain_list`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resolver firewall domain list"})

    		ubx.Resource(route53.ResolverFirewallDomainList, "example", route53.ResolverFirewallDomainListConfig{
    			Name:          "example",
    			DomainFileUrl: "example",
    			Domains:       []string{"example"},
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResolverFirewallDomainList } from "./local-sdk/aws/sdk/typescript/aws/route53/resolver_firewall_domain_list.ts";

    export default stack("example", () => {
      intent({ summary: "example own resolver firewall domain list" });

      resource(ResolverFirewallDomainList, "example", {
        name: "example",
        domainFileUrl: "example",
        domains: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import ResolverFirewallDomainList, ResolverFirewallDomainListConfig

    def describe():
        ubx.intent("example own resolver firewall domain list")

        ubx.resource(ResolverFirewallDomainList, "example", ResolverFirewallDomainListConfig(
            name="example",
            domain_file_url="example",
            domains=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_resolver_firewall_domain_list called
    "example", with domain file url example, domains a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_file_url" type="string">
  S3 URL to import domains from. Optional.
</ResponseField>

<ResponseField name="domains" type="list(string)">
  An inline list of domains to use for this domain list. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  FirewallDomainListName Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to apply to this route53 resolver firewall domain list. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Route 53 Resolver firewall domain list, used to assign metadata for identifying and organizing the resource in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of this route53 resolver firewall domain list. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="creation_time" type="string">
  Rfc3339TimeString Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creator_request_id" type="string">
  The id of the creator request. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_count" type="number">
  The domain count for this route53 resolver firewall domain list. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of this route53 resolver firewall domain list. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="managed_owner_name" type="string">
  ServicePrincipal Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="modification_time" type="string">
  Rfc3339TimeString Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  ResolverFirewallDomainList, possible values are COMPLETE, DELETING, UPDATING, COMPLETE_IMPORT_FAILED, IMPORTING, and INACTIVE_OWNER_ACCOUNT_CLOSED. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  FirewallDomainListAssociationStatus Computed by AWS after `ship`.
</ResponseField>
