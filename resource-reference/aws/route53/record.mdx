---
title: "aws_route53_record"
description: "Real, generated bindings for `aws_route53_record`."
---

`aws_route53_record` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own record"})

    		ubx.Resource(route53.Record, "ci-runner", route53.RecordConfig{
    			Name:                          "ci-runner",
    			Type:                          "example",
    			ZoneId:                        "example",
    			HealthCheckId:                 "example",
    			MultivalueAnswerRoutingPolicy: true,
    			Records:                       []string{"example"},
    			SetIdentifier:                 "example",
    			Ttl:                           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Record } from "jsr:@ubx/sdk-aws/aws/route53/record";

    export default stack("payments", () => {
      intent({ summary: "payments own record" });

      resource(Record, "ci-runner", {
        name: "ci-runner",
        type: "example",
        zoneId: "example",
        healthCheckId: "example",
        multivalueAnswerRoutingPolicy: true,
        records: ["example"],
        setIdentifier: "example",
        ttl: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import Record, RecordConfig

    def describe():
        ubx.intent("payments own record")

        ubx.resource(Record, "ci-runner", RecordConfig(
            name="ci-runner",
            type="example",
            zone_id="example",
            health_check_id="example",
            multivalue_answer_routing_policy=True,
            records=["example"],
            set_identifier="example",
            ttl=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_record called "ci-runner", with type example,
    zone id example, health check id example, multivalue answer
    routing policy true, records a set value, set identifier example,
    ttl 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_overwrite" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="multivalue_answer_routing_policy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="records" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="set_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="zone_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="allow_overwrite" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="fqdn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
