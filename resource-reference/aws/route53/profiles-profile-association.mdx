---
title: "aws_route53_profiles_profile_association"
description: "`aws_route53_profiles_profile_association` attaches a real `aws_route53_profiles_profile` to a specific VPC..."
---

`aws_route53_profiles_profile_association` attaches a real `aws_route53_profiles_profile` to a specific VPC, the resource that actually applies the profile's bundled DNS configuration to that VPC.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own profiles profile association"})

    		ubx.Resource(route53.ProfilesProfileAssociation, "ci-runner", route53.ProfilesProfileAssociationConfig{
    			Name:       "ci-runner",
    			ProfileId:  "example",
    			ResourceId: "example",
    			Arn:        "arn:aws:iam::123456789012:policy/ci",
    			Tags:       []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProfilesProfileAssociation } from "./local-sdk/aws/sdk/typescript/aws/route53/profiles_profile_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own profiles profile association" });

      resource(ProfilesProfileAssociation, "ci-runner", {
        name: "ci-runner",
        profileId: "example",
        resourceId: "example",
        arn: "arn:aws:iam::123456789012:policy/ci",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import ProfilesProfileAssociation, ProfilesProfileAssociationConfig

    def describe():
        ubx.intent("payments own profiles profile association")

        ubx.resource(ProfilesProfileAssociation, "ci-runner", ProfilesProfileAssociationConfig(
            name="ci-runner",
            profile_id="example",
            resource_id="example",
            arn="arn:aws:iam::123456789012:policy/ci",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_profiles_profile_association called
    "ci-runner", with profile id example, resource id example, arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the profile association. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of an association between a Profile and a VPC. Required.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  The ID of the profile that you associated with the resource that is specified by ResourceId. Required.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The resource that you associated the profile with. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag (key-value pair) attached to the Route 53 Profile association, used for metadata, identification, and cost allocation of the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Primary Identifier for Profile Association Computed by AWS after `ship`.
</ResponseField>
