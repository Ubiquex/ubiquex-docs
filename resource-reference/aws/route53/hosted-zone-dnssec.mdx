---
title: "aws_route53_hosted_zone_dnssec"
description: "Real, generated bindings for `aws_route53_hosted_zone_dnssec`."
---

`aws_route53_hosted_zone_dnssec` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hosted zone dnssec"})

    		ubx.Resource(route53.HostedZoneDnssec, "ci-runner", route53.HostedZoneDnssecConfig{
    			HostedZoneId:  "example",
    			SigningStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostedZoneDnssec } from "jsr:@ubx/sdk-aws/aws/route53/hosted_zone_dnssec";

    export default stack("payments", () => {
      intent({ summary: "payments own hosted zone dnssec" });

      resource(HostedZoneDnssec, "ci-runner", {
        hostedZoneId: "example",
        signingStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import HostedZoneDnssec, HostedZoneDnssecConfig

    def describe():
        ubx.intent("payments own hosted zone dnssec")

        ubx.resource(HostedZoneDnssec, "ci-runner", HostedZoneDnssecConfig(
            hosted_zone_id="example",
            signing_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_hosted_zone_dnssec, with hosted zone id
    example, signing status example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hosted_zone_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="signing_status" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
