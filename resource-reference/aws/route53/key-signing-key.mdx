---
title: "aws_route53_key_signing_key"
description: "`aws_route53_key_signing_key` creates a real cryptographic key pair used to sign an `aws_route53_hosted_zone`'s own DNS records..."
---

`aws_route53_key_signing_key` creates a real cryptographic key pair used to sign an `aws_route53_hosted_zone`'s own DNS records, the key an `aws_route53_dnssec` configuration actually signs with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own key signing key"})

    		ubx.Resource(route53.KeySigningKey, "example", route53.KeySigningKeyConfig{
    			HostedZoneId:            "example",
    			KeyManagementServiceArn: "arn:aws:iam::123456789012:policy/example",
    			Name:                    "example",
    			Status:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeySigningKey } from "./local-sdk/aws/sdk/typescript/aws/route53/key_signing_key.ts";

    export default stack("example", () => {
      intent({ summary: "example own key signing key" });

      resource(KeySigningKey, "example", {
        hostedZoneId: "example",
        keyManagementServiceArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import KeySigningKey, KeySigningKeyConfig

    def describe():
        ubx.intent("example own key signing key")

        ubx.resource(KeySigningKey, "example", KeySigningKeyConfig(
            hosted_zone_id="example",
            key_management_service_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_key_signing_key called "example", with hosted
    zone id example, key management service arn
    arn:aws:iam::123456789012:policy/example, status example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="hosted_zone_id" type="string" required>
  The unique string (ID) used to identify a hosted zone. Required.
</ResponseField>

<ResponseField name="key_management_service_arn" type="string" required>
  The Amazon resource name (ARN) for a customer managed key (CMK) in AWS Key Management Service (KMS). The KeyManagementServiceArn must be unique for each key signing key (KSK) in a single hosted zone. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  An alphanumeric string used to identify a key signing key (KSK). Name must be unique for each key signing key in the same hosted zone. Required.
</ResponseField>

<ResponseField name="status" type="string" required>
  A string specifying the initial status of the key signing key (KSK). You can set the value to ACTIVE or INACTIVE. Required.
</ResponseField>
