---
title: "aws_route53_global_resolver_access_source"
description: "`aws_route53_global_resolver_access_source` authorizes a real external network, an on-premises CIDR range..."
---

`aws_route53_global_resolver_access_source` authorizes a real external network, an on-premises CIDR range, to query an `aws_route53_global_resolver_global_resolver`, the resource that actually grants a specific outside network permission to reach the resolver's own anycast endpoints.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own global resolver access source"})

    		ubx.Resource(route53.GlobalResolverAccessSource, "example", route53.GlobalResolverAccessSourceConfig{
    			Cidr:          "example",
    			DnsViewId:     "example",
    			Protocol:      "example",
    			Name:          "example",
    			ClientToken:   "example",
    			IpAddressType: "example",
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalResolverAccessSource } from "@ubx/sdk-aws/aws/route53/global_resolver_access_source";

    export default stack("example", () => {
      intent({ summary: "example own global resolver access source" });

      resource(GlobalResolverAccessSource, "example", {
        cidr: "example",
        dnsViewId: "example",
        protocol: "example",
        name: "example",
        clientToken: "example",
        ipAddressType: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import GlobalResolverAccessSource, GlobalResolverAccessSourceConfig

    def describe():
        ubx.intent("example own global resolver access source")

        ubx.resource(GlobalResolverAccessSource, "example", GlobalResolverAccessSourceConfig(
            cidr="example",
            dns_view_id="example",
            protocol="example",
            name="example",
            client_token="example",
            ip_address_type="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_global_resolver_access_source called
    "example", with cidr example, dns view id example, protocol
    example, client token example, ip address type example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_token" type="string">
  Optional.
</ResponseField>

<ResponseField name="dns_view_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="access_source_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
