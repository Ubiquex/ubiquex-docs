---
title: "aws_route53_query_log"
description: "Real, generated bindings for `aws_route53_query_log`."
---

`aws_route53_query_log` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"

    cfg := route53.QueryLogConfig{
        CloudwatchLogGroupArn: "example",
        ZoneId: "example",
    }
    ubx.Resource(route53.QueryLog, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { QueryLog } from
      "jsr:@ubx/sdk-aws/aws/route53/query_log";

    resource(QueryLog, "example", {
      cloudwatchLogGroupArn: "example",
      zoneId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53 import QueryLog, QueryLogConfig

    cfg = QueryLogConfig(
        cloudwatch_log_group_arn="example",
        zone_id="example",
    )
    ubx.resource(QueryLog, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53_query_log with cloudwatch_log_group_arn,
zone_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cloudwatch_log_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zone_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
