---
title: "aws_route53_query_log"
description: "Real, generated bindings for `aws_route53_query_log`."
---

`aws_route53_query_log` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own query log"})

    		ubx.Resource(route53.QueryLog, "ci-runner", route53.QueryLogConfig{
    			CloudwatchLogGroupArn: "arn:aws:iam::123456789012:policy/ci",
    			ZoneId:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QueryLog } from "jsr:@ubx/sdk-aws/aws/route53/query_log";

    export default stack("payments", () => {
      intent({ summary: "payments own query log" });

      resource(QueryLog, "ci-runner", {
        cloudwatchLogGroupArn: "arn:aws:iam::123456789012:policy/ci",
        zoneId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import QueryLog, QueryLogConfig

    def describe():
        ubx.intent("payments own query log")

        ubx.resource(QueryLog, "ci-runner", QueryLogConfig(
            cloudwatch_log_group_arn="arn:aws:iam::123456789012:policy/ci",
            zone_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_query_log, with cloudwatch log group arn
    arn:aws:iam::123456789012:policy/ci, zone id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloudwatch_log_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zone_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
