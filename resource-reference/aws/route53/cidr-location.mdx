---
title: "aws_route53_cidr_location"
description: "Real, generated bindings for `aws_route53_cidr_location`."
---

`aws_route53_cidr_location` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cidr location"})

    		ubx.Resource(route53.CidrLocation, "ci-runner", route53.CidrLocationConfig{
    			CidrBlocks:       []string{"example"},
    			CidrCollectionId: "example",
    			Name:             "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CidrLocation } from "jsr:@ubx/sdk-aws/aws/route53/cidr_location";

    export default stack("payments", () => {
      intent({ summary: "payments own cidr location" });

      resource(CidrLocation, "ci-runner", {
        cidrBlocks: ["example"],
        cidrCollectionId: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import CidrLocation, CidrLocationConfig

    def describe():
        ubx.intent("payments own cidr location")

        ubx.resource(CidrLocation, "ci-runner", CidrLocationConfig(
            cidr_blocks=["example"],
            cidr_collection_id="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_cidr_location called "ci-runner", with cidr
    blocks a set value, cidr collection id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr_blocks" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="cidr_collection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
