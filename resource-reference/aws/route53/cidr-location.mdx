---
title: "aws_route53_cidr_location"
description: "Real, generated bindings for `aws_route53_cidr_location`."
---

`aws_route53_cidr_location` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/route53"

    cfg := route53.CidrLocationConfig{
        CidrBlocks: []string{"example"},
        CidrCollectionId: "example",
        Name: "example-name",
    }
    ubx.Resource(route53.CidrLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CidrLocation } from
      "jsr:@ubx/sdk-aws/aws/route53/cidr_location";

    resource(CidrLocation, "example", {
      cidrBlocks: ["example"],
      cidrCollectionId: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53 import CidrLocation, CidrLocationConfig

    cfg = CidrLocationConfig(
        cidr_blocks=["example"],
        cidr_collection_id="example",
        name="example",
    )
    ubx.resource(CidrLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53_cidr_location with cidr_blocks,
cidr_collection_id, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_blocks" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="cidr_collection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
