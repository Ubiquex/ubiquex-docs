---
title: "aws_route53_resolver_dnssec_config"
description: "Real, generated bindings for `aws_route53_resolver_dnssec_config`."
---

`aws_route53_resolver_dnssec_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resolver dnssec config"})

    		ubx.Resource(route53.ResolverDnssecConfig, "ci-runner", route53.ResolverDnssecConfigConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResolverDnssecConfig } from "jsr:@ubx/sdk-aws/aws/route53/resolver_dnssec_config";

    export default stack("payments", () => {
      intent({ summary: "payments own resolver dnssec config" });

      resource(ResolverDnssecConfig, "ci-runner", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import ResolverDnssecConfig, ResolverDnssecConfigConfig

    def describe():
        ubx.intent("payments own resolver dnssec config")

        ubx.resource(ResolverDnssecConfig, "ci-runner", ResolverDnssecConfigConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_resolver_dnssec_config, with resource id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validation_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
