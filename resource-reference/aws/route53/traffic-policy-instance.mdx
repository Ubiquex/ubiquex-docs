---
title: "aws_route53_traffic_policy_instance"
description: "Real, generated bindings for `aws_route53_traffic_policy_instance`."
---

`aws_route53_traffic_policy_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic policy instance"})

    		ubx.Resource(route53.TrafficPolicyInstance, "ci-runner", route53.TrafficPolicyInstanceConfig{
    			HostedZoneId:         "example",
    			Name:                 "ci-runner",
    			TrafficPolicyId:      "example",
    			TrafficPolicyVersion: 1,
    			Ttl:                  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficPolicyInstance } from "jsr:@ubx/sdk-aws/aws/route53/traffic_policy_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic policy instance" });

      resource(TrafficPolicyInstance, "ci-runner", {
        hostedZoneId: "example",
        name: "ci-runner",
        trafficPolicyId: "example",
        trafficPolicyVersion: 1,
        ttl: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import TrafficPolicyInstance, TrafficPolicyInstanceConfig

    def describe():
        ubx.intent("payments own traffic policy instance")

        ubx.resource(TrafficPolicyInstance, "ci-runner", TrafficPolicyInstanceConfig(
            hosted_zone_id="example",
            name="ci-runner",
            traffic_policy_id="example",
            traffic_policy_version=1,
            ttl=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_traffic_policy_instance called "ci-runner",
    with hosted zone id example, traffic policy id example, traffic
    policy version 1, ttl 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hosted_zone_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="traffic_policy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="traffic_policy_version" type="number" required>
  Required.
</ResponseField>

<ResponseField name="ttl" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
