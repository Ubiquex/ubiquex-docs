---
title: "aws_route53_record_set"
description: "`aws_route53_record_set` creates a real DNS record, A, CNAME, MX, and more, including alias records that point at other AWS resources..."
---

`aws_route53_record_set` creates a real DNS record, A, CNAME, MX, and more, including alias records that point at other AWS resources, within an `aws_route53_hosted_zone`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own record set"})

    		ubx.Resource(route53.RecordSet, "example", route53.RecordSetConfig{
    			Name:                 "example",
    			Type:                 "example",
    			AliasTarget:          map[string]any{"dnsname": "example"},
    			CidrRoutingConfig:    map[string]any{"collection_id": "example"},
    			Comment:              "example",
    			Failover:             "example",
    			GeoLocation:          map[string]any{"continent_code": "example"},
    			GeoProximityLocation: map[string]any{"awsregion": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RecordSet } from "./local-sdk/aws/sdk/typescript/aws/route53/record_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own record set" });

      resource(RecordSet, "example", {
        name: "example",
        type: "example",
        aliasTarget: { dnsname: "example" },
        cidrRoutingConfig: { collectionId: "example" },
        comment: "example",
        failover: "example",
        geoLocation: { continentCode: "example" },
        geoProximityLocation: { awsregion: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import RecordSet, RecordSetConfig

    def describe():
        ubx.intent("example own record set")

        ubx.resource(RecordSet, "example", RecordSetConfig(
            name="example",
            type="example",
            alias_target={"dnsname": "example"},
            cidr_routing_config={"collection_id": "example"},
            comment="example",
            failover="example",
            geo_location={"continent_code": "example"},
            geo_proximity_location={"awsregion": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_record_set called "example", with type
    example, alias target a set value, cidr routing config a set
    value, comment example, failover example, geo location a set
    value, geo proximity location a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alias_target" type="object">
  Specifies alias record properties, enabling the record set to route traffic to an AWS resource (e.g., Elastic Load Balancer, CloudFront distribution, or S3 bucket) by providing the target's hosted zone ID and DNS name, with an optional evaluate-target-health flag. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="dnsname" type="string" required>
      The DNS name of the AWS resource (such as an Elastic Load Balancer, CloudFront distribution, or S3 website endpoint) to which this alias record routes traffic. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="evaluate_target_health" type="bool">
      Indicates whether Route 53 evaluates the health of the target resource and uses that health information to determine whether to route traffic to the alias target. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hosted_zone_id" type="string" required>
      The hosted zone ID of the AWS resource (such as an Elastic Load Balancer, CloudFront distribution, or S3 website bucket) that this alias record points to. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cidr_routing_config" type="object">
  Configures CIDR-based routing for the record set by specifying the CIDR collection ID and location name that determine which traffic is directed to this record. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="collection_id" type="string" required>
      The unique identifier of the CIDR collection whose locations and CIDR blocks are used to determine the applicable routing policy for this record set. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location_name" type="string" required>
      Specifies the name of the CIDR location within the referenced CIDR collection that this record set should be associated with for CIDR routing. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="comment" type="string">
  Specifies an optional human-readable comment that provides descriptive information about the record set, useful for identifying the purpose of the record. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="failover" type="string">
  Indicates whether this record set is the primary or secondary resource for a Route 53 failover routing policy, controlling which record answers queries based on health check status. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="geo_location" type="object">
  Specifies the geographic location (continent, country, or subdivision) that this Route 53 record set serves, enabling geolocation routing so DNS queries from that location are answered with this record. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="continent_code" type="string">
      The continent code (e.g., 'NA' for North America) that restricts the DNS record to queries originating from that continent when using geolocation routing. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="country_code" type="string">
      The ISO 3166-1 alpha-2 country code (e.g., 'US') that specifies the country for which this Route 53 record will respond to DNS queries when geolocation routing is used. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subdivision_code" type="string">
      The two-letter code for the US state or other country subdivision (e.g., 'CA' for California) that determines the geographic region from which this record set should be consulted, used when geo location routing policy is enabled with a country_code. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="geo_proximity_location" type="object">
  Specifies the AWS Region or coordinates and bias used by Route 53 for geoproximity routing, which determines the resource that receives traffic based on the geographic origin of the request. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="awsregion" type="string">
      Specifies the AWS region (e.g., us-east-1) that defines the geographic location for this geo proximity routing rule, used by Route 53 to route traffic based on user proximity to that region. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="bias" type="number">
      Specifies a value that biases the size of the geographic region for a geo proximity routing policy, where positive values expand the region and negative values shrink it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="coordinates" type="object">
      Contains the latitude and longitude values that define the geographic point for geo-proximity routing, used when no AWS region is specified in the geo-proximity location. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="latitude" type="string" required>
          Specifies the latitude coordinate (in decimal degrees) of the geographic point used for geoproximity routing, which determines how AWS Route 53 routes traffic based on the user's proximity to this location. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="longitude" type="string" required>
          The longitude of the geographic coordinate used by Amazon Route 53 to determine the distance for geo-proximity routing for the record set. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="local_zone_group" type="string">
      Specifies the local zone group associated with a group of AWS Local Zones used for geoproximity routing, such as 'us-west-2-lax-1'. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="health_check_id" type="string">
  The ID of the Amazon Route 53 health check associated with this record set, which Route 53 uses to monitor the health of the resource and determine whether to include the record in DNS responses for failover or latency-based routing. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="hosted_zone_id" type="string">
  The ID of the hosted zone in which to create the record set, allowing you to specify the zone by its unique identifier instead of its domain name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="hosted_zone_name" type="string">
  The DNS name of an existing Route 53 hosted zone (such as example.com) in which to create the record set; when provided, it is used as an alternative to the hosted zone ID to identify the zone. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="multi_value_answer" type="bool">
  When enabled, this record set uses multivalue answer routing, allowing Route 53 to return multiple healthy values (e.g., IP addresses) in response to DNS queries. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The DNS record name (fully qualified domain name) such as 'www.example.com', which defines the domain name that the record set answers queries for in Amazon Route 53. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="region" type="string">
  Specifies the AWS Region where the resource referenced by this record set resides, which Route 53 uses to route DNS queries to the closest region when the routing policy is set to latency-based routing. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_records" type="list(string)">
  The list of values for the record set, such as IP addresses for A records, hostnames for CNAME records, or text strings for TXT records, and cannot be set when an alias target is specified. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="set_identifier" type="string">
  Specifies a unique identifier for the record set, used to distinguish it from other record sets with the same DNS name and type when the routing policy is weighted, latency, geolocation, multivalue, or failover. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ttl" type="string">
  The time-to-live (TTL) in seconds that DNS resolvers cache this record before requesting an updated value from Route 53. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string" required>
  The DNS record type (e.g., A, AAAA, CNAME, MX) for the Route 53 record set. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="weight" type="number">
  The relative weight to assign to this record set when using the weighted routing policy, controlling the proportion of DNS responses that point to this record. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
