---
title: "aws_route53_records_exclusive"
description: "Real, generated bindings for `aws_route53_records_exclusive`."
---

`aws_route53_records_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"

    cfg := route53.RecordsExclusiveConfig{
        ZoneId: "example",
    }
    ubx.Resource(route53.RecordsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RecordsExclusive } from
      "jsr:@ubx/sdk-aws/aws/route53/records_exclusive";

    resource(RecordsExclusive, "example", {
      zoneId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53 import (
        RecordsExclusive,
        RecordsExclusiveConfig,
    )

    cfg = RecordsExclusiveConfig(
        zone_id="example",
    )
    ubx.resource(RecordsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53_records_exclusive with zone_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="zone_id" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
