---
title: "aws_route53_global_resolver_dns_view"
description: "`aws_route53_global_resolver_dns_view` defines a real DNS view, which of an account's private hosted zones and rules apply..."
---

`aws_route53_global_resolver_dns_view` defines a real DNS view, which of an account's private hosted zones and rules apply, that an `aws_route53_global_resolver_global_resolver` resolves queries against for a given set of clients.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global resolver dns view"})

    		ubx.Resource(route53.GlobalResolverDnsView, "ci-runner", route53.GlobalResolverDnsViewConfig{
    			GlobalResolverId:      "example",
    			Name:                  "ci-runner",
    			ClientToken:           "example",
    			Description:           "Managed by ubx.",
    			DnssecValidation:      "example",
    			EdnsClientSubnet:      "example",
    			FirewallRulesFailOpen: "example",
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalResolverDnsView } from "./local-sdk/aws/sdk/typescript/aws/route53/global_resolver_dns_view.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own global resolver dns view" });

      resource(GlobalResolverDnsView, "ci-runner", {
        globalResolverId: "example",
        name: "ci-runner",
        clientToken: "example",
        description: "Managed by ubx.",
        dnssecValidation: "example",
        ednsClientSubnet: "example",
        firewallRulesFailOpen: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import GlobalResolverDnsView, GlobalResolverDnsViewConfig

    def describe():
        ubx.intent("payments own global resolver dns view")

        ubx.resource(GlobalResolverDnsView, "ci-runner", GlobalResolverDnsViewConfig(
            global_resolver_id="example",
            name="ci-runner",
            client_token="example",
            description="Managed by ubx.",
            dnssec_validation="example",
            edns_client_subnet="example",
            firewall_rules_fail_open="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_global_resolver_dns_view called "ci-runner",
    with global resolver id example, client token example,
    description Managed by ubx., dnssec validation example, edns
    client subnet example, firewall rules fail open example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_token" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  An optional human-readable description that you provide for the DNS view to help manage and identify it. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="dnssec_validation" type="string">
  Optional.
</ResponseField>

<ResponseField name="edns_client_subnet" type="string">
  Optional.
</ResponseField>

<ResponseField name="firewall_rules_fail_open" type="string">
  Determines whether DNS queries are allowed to pass through (fail open) or are rejected (fail closed) when the Route 53 Resolver DNS Firewall cannot evaluate the configured firewall rules due to an internal error. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="global_resolver_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns_view_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  A read-only timestamp that records when the global resolver DNS view was last modified. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
