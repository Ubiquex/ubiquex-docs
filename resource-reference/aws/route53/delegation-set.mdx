---
title: "aws_route53_delegation_set"
description: "Real, generated bindings for `aws_route53_delegation_set`."
---

`aws_route53_delegation_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delegation set"})

    		ubx.Resource(route53.DelegationSet, "ci-runner", route53.DelegationSetConfig{
    			ReferenceName: "example-reference-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DelegationSet } from "jsr:@ubx/sdk-aws/aws/route53/delegation_set";

    export default stack("payments", () => {
      intent({ summary: "payments own delegation set" });

      resource(DelegationSet, "ci-runner", {
        referenceName: "example-reference-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import DelegationSet, DelegationSetConfig

    def describe():
        ubx.intent("payments own delegation set")

        ubx.resource(DelegationSet, "ci-runner", DelegationSetConfig(
            reference_name="example-reference-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_delegation_set, with reference name
    example-reference-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reference_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_servers" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>
