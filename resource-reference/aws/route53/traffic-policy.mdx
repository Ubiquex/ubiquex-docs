---
title: "aws_route53_traffic_policy"
description: "Real, generated bindings for `aws_route53_traffic_policy`."
---

`aws_route53_traffic_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic policy"})

    		ubx.Resource(route53.TrafficPolicy, "ci-runner", route53.TrafficPolicyConfig{
    			Document: "example",
    			Name:     "ci-runner",
    			Comment:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficPolicy } from "jsr:@ubx/sdk-aws/aws/route53/traffic_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic policy" });

      resource(TrafficPolicy, "ci-runner", {
        document: "example",
        name: "ci-runner",
        comment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import TrafficPolicy, TrafficPolicyConfig

    def describe():
        ubx.intent("payments own traffic policy")

        ubx.resource(TrafficPolicy, "ci-runner", TrafficPolicyConfig(
            document="example",
            name="ci-runner",
            comment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_traffic_policy called "ci-runner", with
    document example, comment example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
