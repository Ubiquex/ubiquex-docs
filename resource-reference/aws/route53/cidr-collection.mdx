---
title: "aws_route53_cidr_collection"
description: "Real, generated bindings for `aws_route53_cidr_collection`."
---

`aws_route53_cidr_collection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cidr collection"})

    		ubx.Resource(route53.CidrCollection, "ci-runner", route53.CidrCollectionConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CidrCollection } from "jsr:@ubx/sdk-aws/aws/route53/cidr_collection";

    export default stack("payments", () => {
      intent({ summary: "payments own cidr collection" });

      resource(CidrCollection, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import CidrCollection, CidrCollectionConfig

    def describe():
        ubx.intent("payments own cidr collection")

        ubx.resource(CidrCollection, "ci-runner", CidrCollectionConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_cidr_collection called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
