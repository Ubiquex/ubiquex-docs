---
title: "aws_route53_cidr_collection"
description: "Real, generated bindings for `aws_route53_cidr_collection`."
---

`aws_route53_cidr_collection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"

    cfg := route53.CidrCollectionConfig{
        Name: "example-name",
    }
    ubx.Resource(route53.CidrCollection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CidrCollection } from
      "jsr:@ubx/sdk-aws/aws/route53/cidr_collection";

    resource(CidrCollection, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53 import (
        CidrCollection,
        CidrCollectionConfig,
    )

    cfg = CidrCollectionConfig(
        name="example",
    )
    ubx.resource(CidrCollection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53_cidr_collection with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
