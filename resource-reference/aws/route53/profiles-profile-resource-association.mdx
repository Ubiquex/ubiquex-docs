---
title: "aws_route53_profiles_profile_resource_association"
description: "`aws_route53_profiles_profile_association` associates a real DNS resource, a Resolver rule or DNS Firewall rule group..."
---

`aws_route53_profiles_profile_association` associates a real DNS resource, a Resolver rule or DNS Firewall rule group, with an `aws_route53_profiles_profile`, the resource that actually bundles that DNS configuration into the profile applied across many VPCs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own profiles profile resource association"})

    		ubx.Resource(route53.ProfilesProfileResourceAssociation, "example", route53.ProfilesProfileResourceAssociationConfig{
    			Name:               "example",
    			ProfileId:          "example",
    			ResourceArn:        "arn:aws:iam::123456789012:policy/example",
    			ResourceProperties: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProfilesProfileResourceAssociation } from "./local-sdk/aws/sdk/typescript/aws/route53/profiles_profile_resource_association.ts";

    export default stack("example", () => {
      intent({ summary: "example own profiles profile resource association" });

      resource(ProfilesProfileResourceAssociation, "example", {
        name: "example",
        profileId: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceProperties: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import ProfilesProfileResourceAssociation, ProfilesProfileResourceAssociationConfig

    def describe():
        ubx.intent("example own profiles profile resource association")

        ubx.resource(ProfilesProfileResourceAssociation, "example", ProfilesProfileResourceAssociationConfig(
            name="example",
            profile_id="example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_properties="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_profiles_profile_resource_association called
    "example", with profile id example, resource arn
    arn:aws:iam::123456789012:policy/example, resource properties
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  The name of an association between the Profile and resource. Required.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  The ID of the profile that you associated the resource to that is specified by ResourceArn. Required.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The arn of the resource that you associated to the Profile. Required.
</ResponseField>

<ResponseField name="resource_properties" type="string">
  A JSON-formatted string with key-value pairs specifying the properties of the associated resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Primary Identifier for Profile Resource Association Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The type of the resource associated to the Profile. Computed by AWS after `ship`.
</ResponseField>
