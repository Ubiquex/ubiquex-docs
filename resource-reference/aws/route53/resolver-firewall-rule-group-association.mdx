---
title: "aws_route53_resolver_firewall_rule_group_association"
description: "`aws_route53_resolver_firewall_rule_group_association` applies a real `aws_route53_resolver_firewall_rule_group` to a specific VPC..."
---

`aws_route53_resolver_firewall_rule_group_association` applies a real `aws_route53_resolver_firewall_rule_group` to a specific VPC, the resource that actually turns the rule group's DNS filtering on for that VPC's traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resolver firewall rule group association"})

    		ubx.Resource(route53resolver.FirewallRuleGroupAssociation, "example", route53resolver.FirewallRuleGroupAssociationConfig{
    			FirewallRuleGroupId: "example",
    			Priority:            1,
    			VpcId:               "example",
    			Name:                "example",
    			MutationProtection:  "example",
    			Tags:                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallRuleGroupAssociation } from "@ubx/sdk-aws/aws/route53resolver/firewall_rule_group_association";

    export default stack("example", () => {
      intent({ summary: "example own resolver firewall rule group association" });

      resource(FirewallRuleGroupAssociation, "example", {
        firewallRuleGroupId: "example",
        priority: 1,
        vpcId: "example",
        name: "example",
        mutationProtection: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53resolver.firewall_rule_group_association import FirewallRuleGroupAssociation, FirewallRuleGroupAssociationConfig

    def describe():
        ubx.intent("example own resolver firewall rule group association")

        ubx.resource(FirewallRuleGroupAssociation, "example", FirewallRuleGroupAssociationConfig(
            firewall_rule_group_id="example",
            priority=1,
            vpc_id="example",
            name="example",
            mutation_protection="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_resolver_firewall_rule_group_association
    called "example", with firewall rule group id example, priority
    1, vpc id example, mutation protection example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="firewall_rule_group_id" type="string" required>
  FirewallRuleGroupId Required.
</ResponseField>

<ResponseField name="mutation_protection" type="string">
  MutationProtectionStatus Optional.
</ResponseField>

<ResponseField name="name" type="string">
  FirewallRuleGroupAssociationName Optional.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Priority Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag attached to the Route 53 Resolver firewall rule group association, used to attach arbitrary metadata like environment or owner for identifying and managing the association. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  VpcId Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Arn Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Rfc3339TimeString Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creator_request_id" type="string">
  The id of the creator request. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Id Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="managed_owner_name" type="string">
  ServicePrincipal Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="modification_time" type="string">
  Rfc3339TimeString Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  ResolverFirewallRuleGroupAssociation, possible values are COMPLETE, DELETING, UPDATING, and INACTIVE_OWNER_ACCOUNT_CLOSED. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  FirewallDomainListAssociationStatus Computed by AWS after `ship`.
</ResponseField>
