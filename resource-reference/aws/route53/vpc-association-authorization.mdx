---
title: "aws_route53_vpc_association_authorization"
description: "Real, generated bindings for `aws_route53_vpc_association_authorization`."
---

`aws_route53_vpc_association_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc association authorization"})

    		ubx.Resource(route53.VpcAssociationAuthorization, "ci-runner", route53.VpcAssociationAuthorizationConfig{
    			VpcId:  "example",
    			ZoneId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcAssociationAuthorization } from "jsr:@ubx/sdk-aws/aws/route53/vpc_association_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc association authorization" });

      resource(VpcAssociationAuthorization, "ci-runner", {
        vpcId: "example",
        zoneId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53 import VpcAssociationAuthorization, VpcAssociationAuthorizationConfig

    def describe():
        ubx.intent("payments own vpc association authorization")

        ubx.resource(VpcAssociationAuthorization, "ci-runner", VpcAssociationAuthorizationConfig(
            vpc_id="example",
            zone_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_vpc_association_authorization, with vpc id
    example, zone id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zone_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
