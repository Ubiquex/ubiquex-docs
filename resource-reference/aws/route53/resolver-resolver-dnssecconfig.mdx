---
title: "aws_route53_resolver_resolver_dnssecconfig"
description: "`aws_route53_resolver_resolver_dnssecconfig` configures whether Route 53 Resolver validates DNSSEC signatures for outbound queries a VPC sends through..."
---

`aws_route53_resolver_resolver_dnssecconfig` configures whether Route 53 Resolver validates DNSSEC signatures for outbound queries a VPC sends through an `aws_route53_resolver_resolver_endpoint`, rejecting responses that fail validation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resolver resolver dnssecconfig"})

    		ubx.Resource(route53.ResolverResolverDnssecconfig, "example", route53.ResolverResolverDnssecconfigConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResolverResolverDnssecconfig } from "./local-sdk/aws/sdk/typescript/aws/route53/resolver_resolver_dnssecconfig.ts";

    export default stack("example", () => {
      intent({ summary: "example own resolver resolver dnssecconfig" });

      resource(ResolverResolverDnssecconfig, "example", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import ResolverResolverDnssecconfig, ResolverResolverDnssecconfigConfig

    def describe():
        ubx.intent("example own resolver resolver dnssecconfig")

        ubx.resource(ResolverResolverDnssecconfig, "example", ResolverResolverDnssecconfigConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_resolver_resolver_dnssecconfig, with resource
    id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="resource_id" type="string">
  The unique identifier of this route53 resolver resolver dnssecconfig. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier of this route53 resolver resolver dnssecconfig. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="owner_id" type="string">
  The owner ID for this route53 resolver resolver dnssecconfig. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="validation_status" type="string">
  ResolverDNSSECValidationStatus, possible values are ENABLING, ENABLED, DISABLING AND DISABLED. Computed by AWS after `ship`.
</ResponseField>
