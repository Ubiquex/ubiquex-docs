---
title: "aws_route53_profiles_profile"
description: "`aws_route53_profiles_profile` creates a real Route 53 Profile, a reusable bundle of DNS configuration, private hosted zone associations..."
---

`aws_route53_profiles_profile` creates a real Route 53 Profile, a reusable bundle of DNS configuration, private hosted zone associations, Resolver rules, DNS Firewall rules, that can be applied consistently across many VPCs at once rather than configured VPC by VPC.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own profiles profile"})

    		ubx.Resource(route53.ProfilesProfile, "ci-runner", route53.ProfilesProfileConfig{
    			Name: "ci-runner",
    			Tags: []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProfilesProfile } from "./local-sdk/aws/sdk/typescript/aws/route53/profiles_profile.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own profiles profile" });

      resource(ProfilesProfile, "ci-runner", {
        name: "ci-runner",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.route53 import ProfilesProfile, ProfilesProfileConfig

    def describe():
        ubx.intent("payments own profiles profile")

        ubx.resource(ProfilesProfile, "ci-runner", ProfilesProfileConfig(
            name="ci-runner",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_profiles_profile called "ci-runner", with tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  The name of the profile. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Route 53 Profiles profile, used to identify and categorize the profile in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag assigned to the Route 53 Profile, used to organize, identify, and manage access to the profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the resolver profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="client_token" type="string">
  The id of the creator request Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="share_status" type="string">
  The sharing status of the profile. Computed by AWS after `ship`.
</ResponseField>
