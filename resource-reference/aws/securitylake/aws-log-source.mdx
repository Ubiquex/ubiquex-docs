---
title: "aws_security_lake_aws_log_source"
description: "`aws_security_lake_aws_log_source` enables collection of a specific AWS log or event type, CloudTrail, VPC Flow Logs, Route 53 resolver logs..."
---

`aws_security_lake_aws_log_source` enables collection of a specific AWS log or event type, CloudTrail, VPC Flow Logs, Route 53 resolver logs, into an `aws_security_lake_data_lake`, the resource that actually turns on ingestion for that one source rather than the data lake as a whole.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securitylake "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/securitylake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own lake aws log source"})

    		ubx.Resource(securitylake.AwsLogSource, "example", securitylake.AwsLogSourceConfig{
    			DataLakeArn:   "arn:aws:iam::123456789012:policy/example",
    			SourceName:    "example-source-name",
    			SourceVersion: "example",
    			Accounts:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AwsLogSource } from "@ubx/sdk-aws/aws/securitylake/aws_log_source";

    export default stack("example", () => {
      intent({ summary: "example own lake aws log source" });

      resource(AwsLogSource, "example", {
        dataLakeArn: "arn:aws:iam::123456789012:policy/example",
        sourceName: "example-source-name",
        sourceVersion: "example",
        accounts: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securitylake.aws_log_source import AwsLogSource, AwsLogSourceConfig

    def describe():
        ubx.intent("example own lake aws log source")

        ubx.resource(AwsLogSource, "example", AwsLogSourceConfig(
            data_lake_arn="arn:aws:iam::123456789012:policy/example",
            source_name="example-source-name",
            source_version="example",
            accounts=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_lake_aws_log_source, with data lake arn
    arn:aws:iam::123456789012:policy/example, source name
    example-source-name, source version example, accounts a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accounts" type="list(string)">
  AWS account where you want to collect logs from. Optional.
</ResponseField>

<ResponseField name="data_lake_arn" type="string" required>
  The ARN for the data lake. Required.
</ResponseField>

<ResponseField name="source_name" type="string" required>
  The name for a AWS source. This must be a Regionally unique value. Required.
</ResponseField>

<ResponseField name="source_version" type="string" required>
  The version for a AWS source. This must be a Regionally unique value. Required.
</ResponseField>
