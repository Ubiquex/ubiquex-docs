---
title: "aws_securitylake_subscriber_notification"
description: "Real, generated bindings for `aws_securitylake_subscriber_notification`."
---

`aws_securitylake_subscriber_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securitylake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securitylake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subscriber notification"})

    		ubx.Resource(securitylake.SubscriberNotification, "ci-runner", securitylake.SubscriberNotificationConfig{
    			SubscriberId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SubscriberNotification } from "jsr:@ubx/sdk-aws/aws/securitylake/subscriber_notification";

    export default stack("payments", () => {
      intent({ summary: "payments own subscriber notification" });

      resource(SubscriberNotification, "ci-runner", {
        subscriberId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securitylake import SubscriberNotification, SubscriberNotificationConfig

    def describe():
        ubx.intent("payments own subscriber notification")

        ubx.resource(SubscriberNotification, "ci-runner", SubscriberNotificationConfig(
            subscriber_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securitylake_subscriber_notification, with subscriber
    id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subscriber_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="endpoint_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subscriber_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>
