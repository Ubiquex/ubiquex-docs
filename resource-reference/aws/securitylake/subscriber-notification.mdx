---
title: "aws_securitylake_subscriber_notification"
description: "Real, generated bindings for `aws_securitylake_subscriber_notification`."
---

`aws_securitylake_subscriber_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securitylake"

    cfg := securitylake.SubscriberNotificationConfig{
        SubscriberId: "example",
    }
    ubx.Resource(securitylake.SubscriberNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SubscriberNotification } from
      "jsr:@ubx/sdk-aws/aws/securitylake/subscriber_notification";

    resource(SubscriberNotification, "example", {
      subscriberId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securitylake import (
        SubscriberNotification,
        SubscriberNotificationConfig,
    )

    cfg = SubscriberNotificationConfig(
        subscriber_id="example",
    )
    ubx.resource(SubscriberNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securitylake_subscriber_notification with
subscriber_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subscriber_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="endpoint_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subscriber_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>
