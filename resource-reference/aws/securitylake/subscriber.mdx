---
title: "aws_securitylake_subscriber"
description: "Real, generated bindings for `aws_securitylake_subscriber`."
---

`aws_securitylake_subscriber` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securitylake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securitylake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subscriber"})

    		ubx.Resource(securitylake.Subscriber, "ci-runner", securitylake.SubscriberConfig{
    			SubscriberDescription: "example",
    			SubscriberName:        "example-subscriber-name",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subscriber } from "jsr:@ubx/sdk-aws/aws/securitylake/subscriber";

    export default stack("payments", () => {
      intent({ summary: "payments own subscriber" });

      resource(Subscriber, "ci-runner", {
        subscriberDescription: "example",
        subscriberName: "example-subscriber-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securitylake import Subscriber, SubscriberConfig

    def describe():
        ubx.intent("payments own subscriber")

        ubx.resource(Subscriber, "ci-runner", SubscriberConfig(
            subscriber_description="example",
            subscriber_name="example-subscriber-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securitylake_subscriber, with subscriber description
    example, subscriber name example-subscriber-name, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subscriber_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="subscriber_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="access_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_share_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_share_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="s3_bucket_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subscriber_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subscriber_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
