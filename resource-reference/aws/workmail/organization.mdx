---
title: "aws_workmail_organization"
description: "Real, generated bindings for `aws_workmail_organization`."
---

`aws_workmail_organization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workmail"

    cfg := workmail.OrganizationConfig{
        OrganizationAlias: "example",
        DeleteDirectory: true,
        DeleteIdentityCenterApplication: true,
    }
    ubx.Resource(workmail.Organization, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Organization } from
      "jsr:@ubx/sdk-aws/aws/workmail/organization";

    resource(Organization, "example", {
      organizationAlias: "example",
      deleteDirectory: true,
      deleteIdentityCenterApplication: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workmail import Organization, OrganizationConfig

    cfg = OrganizationConfig(
        organization_alias="example",
        delete_directory=True,
        delete_identity_center_application=True,
    )
    ubx.resource(Organization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workmail_organization with organization_alias,
delete_directory, delete_identity_center_application.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delete_directory" type="bool">
  Optional.
</ResponseField>

<ResponseField name="delete_identity_center_application" type="bool">
  Optional.
</ResponseField>

<ResponseField name="directory_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="interoperability_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="organization_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="completed_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_mail_domain" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="directory_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="directory_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="interoperability_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="migration_admin" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
