---
title: "aws_workmail_domain"
description: "Real, generated bindings for `aws_workmail_domain`."
---

`aws_workmail_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/workmail"

    cfg := workmail.DomainConfig{
        DomainName: "example-domain-name",
        OrganizationId: "example",
    }
    ubx.Resource(workmail.Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Domain } from "jsr:@ubx/sdk-aws/aws/workmail/domain";

    resource(Domain, "example", {
      domainName: "example",
      organizationId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workmail import Domain, DomainConfig

    cfg = DomainConfig(
        domain_name="example",
        organization_id="example",
    )
    ubx.resource(Domain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workmail_domain with domain_name, organization_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="dkim_verification_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_test_domain" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ownership_verification_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="records" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="hostname" type="string">
    </ResponseField>
    <ResponseField name="type" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
