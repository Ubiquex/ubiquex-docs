---
title: "aws_workmail_default_domain"
description: "Real, generated bindings for `aws_workmail_default_domain`."
---

`aws_workmail_default_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/workmail"

    cfg := workmail.DefaultDomainConfig{
        DomainName: "example-domain-name",
        OrganizationId: "example",
    }
    ubx.Resource(workmail.DefaultDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DefaultDomain } from
      "jsr:@ubx/sdk-aws/aws/workmail/default_domain";

    resource(DefaultDomain, "example", {
      domainName: "example",
      organizationId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workmail import DefaultDomain, DefaultDomainConfig

    cfg = DefaultDomainConfig(
        domain_name="example",
        organization_id="example",
    )
    ubx.resource(DefaultDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workmail_default_domain with domain_name,
organization_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
