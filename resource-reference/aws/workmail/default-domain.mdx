---
title: "aws_workmail_default_domain"
description: "Real, generated bindings for `aws_workmail_default_domain`."
---

`aws_workmail_default_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default domain"})

    		ubx.Resource(workmail.DefaultDomain, "ci-runner", workmail.DefaultDomainConfig{
    			DomainName:     "example-domain-name",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultDomain } from "jsr:@ubx/sdk-aws/aws/workmail/default_domain";

    export default stack("payments", () => {
      intent({ summary: "payments own default domain" });

      resource(DefaultDomain, "ci-runner", {
        domainName: "example-domain-name",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workmail import DefaultDomain, DefaultDomainConfig

    def describe():
        ubx.intent("payments own default domain")

        ubx.resource(DefaultDomain, "ci-runner", DefaultDomainConfig(
            domain_name="example-domain-name",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workmail_default_domain, with domain name
    example-domain-name, organization id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
