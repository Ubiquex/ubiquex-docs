---
title: "aws_workmail_group"
description: "Real, generated bindings for `aws_workmail_group`."
---

`aws_workmail_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workmail"

    cfg := workmail.GroupConfig{
        Email: "example",
        Name: "example-name",
        OrganizationId: "example",
        HiddenFromGlobalAddressList: true,
    }
    ubx.Resource(workmail.Group, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Group } from "jsr:@ubx/sdk-aws/aws/workmail/group";

    resource(Group, "example", {
      email: "example",
      name: "example",
      organizationId: "example",
      hiddenFromGlobalAddressList: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workmail import Group, GroupConfig

    cfg = GroupConfig(
        email="example",
        name="example",
        organization_id="example",
        hidden_from_global_address_list=True,
    )
    ubx.resource(Group, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workmail_group with email, name, organization_id,
hidden_from_global_address_list.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="disabled_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
