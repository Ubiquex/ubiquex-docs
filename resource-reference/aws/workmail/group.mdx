---
title: "aws_workmail_group"
description: "Real, generated bindings for `aws_workmail_group`."
---

`aws_workmail_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group"})

    		ubx.Resource(workmail.Group, "ci-runner", workmail.GroupConfig{
    			Email:          "example",
    			Name:           "ci-runner",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Group } from "jsr:@ubx/sdk-aws/aws/workmail/group";

    export default stack("payments", () => {
      intent({ summary: "payments own group" });

      resource(Group, "ci-runner", {
        email: "example",
        name: "ci-runner",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workmail import Group, GroupConfig

    def describe():
        ubx.intent("payments own group")

        ubx.resource(Group, "ci-runner", GroupConfig(
            email="example",
            name="ci-runner",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workmail_group called "ci-runner", with email example,
    organization id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="email" type="string" required>
  Primary email address used to register the group with WorkMail. Required.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Whether to hide the group from the global address list. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the group. Required.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Identifier of the WorkMail organization where the group is managed. Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="disabled_date" type="string">
  Timestamp when the group was disabled from WorkMail use. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled_date" type="string">
  Timestamp when the group was enabled for WorkMail use. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_id" type="string">
  Identifier of the group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Whether to hide the group from the global address list. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Current WorkMail state of the group. Computed by AWS after `ship`.
</ResponseField>
