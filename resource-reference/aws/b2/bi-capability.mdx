---
title: "aws_b2_bi_capability"
description: "`aws_b2_bi_capability` connects an `aws_b2_bi_transformer` to specific Amazon S3 input and output locations, automating the processing workflow..."
---

`aws_b2_bi_capability` connects an `aws_b2_bi_transformer` to specific Amazon S3 input and output locations, automating the processing workflow, EDI document arrives, gets transformed, lands in S3, that an `aws_b2_bi_partnership` then references.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	b2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/b2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bi capability"})

    		ubx.Resource(b2.BiCapability, "ci-runner", b2.BiCapabilityConfig{
    			Configuration:         map[string]any{"edi": map[string]any{"capability_direction": "example"}},
    			Name:                  "ci-runner",
    			Type:                  "example",
    			InstructionsDocuments: []map[string]any{{"bucket_name": "example-bucket-name"}},
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BiCapability } from "./local-sdk/aws/sdk/typescript/aws/b2/bi_capability.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own bi capability" });

      resource(BiCapability, "ci-runner", {
        configuration: { edi: { capabilityDirection: "example" } },
        name: "ci-runner",
        type: "example",
        instructionsDocuments: [{ bucketName: "example-bucket-name" }],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.b2 import BiCapability, BiCapabilityConfig

    def describe():
        ubx.intent("payments own bi capability")

        ubx.resource(BiCapability, "ci-runner", BiCapabilityConfig(
            configuration={"edi": {"capability_direction": "example"}},
            name="ci-runner",
            type="example",
            instructions_documents=[{"bucket_name": "example-bucket-name"}],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_b2_bi_capability called "ci-runner", with
    configuration a set value, type example, instructions documents a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration" type="object" required>
  The configuration object contains the EDI transformation settings for the B2Bi capability, specifying the EDI type, input/output document locations, and the transformer that processes interchange data. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="edi" type="object">
      The configuration.edi field holds the EdiConfiguration for the B2Bi capability, specifying the EDI standard (such as X12), the input and output Amazon S3 locations for EDI documents, and the transform options applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capability_direction" type="string">
          Specifies whether this EDI capability handles inbound or outbound EDI documents, using the value 'INBOUND' or 'OUTBOUND'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_location" type="object">
          The S3 location (bucket and key) where inbound EDI documents are stored for processing by this AWS B2Bi capability's EDI configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string">
              The name of the S3 bucket where input EDI files are stored for this capability's EDI configuration, serving as the input location for document exchange. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The Amazon S3 object key (file name) within the input bucket where the source EDI document is located for the B2Bi capability's EDI configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_location" type="object">
          For the EDI configuration of a capability, this object specifies the Amazon S3 location (bucket and key) where AWS B2Bi stores the output files after performing the EDI transformation. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string">
              The name of the S3 bucket where processed EDI output documents are stored, as specified in the output location of the EDI configuration for this B2B capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The S3 object key prefix under the output bucket where the transformed EDI documents are stored for this capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transformer_id" type="string">
          The transformer_id specifies the AWS B2Bi transformer used to process EDI documents for this capability, defining how incoming or outgoing EDI files are mapped and converted according to the transformer's configured settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="object">
          Specifies the EDI standard type (e.g., X12) and its associated details, such as transaction set and version, for the EDI configuration of this AWS B2BI capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="x12_details" type="object">
              Specifies the X12-specific settings, such as the transaction set identifier and version, for the EDI capability when the EDI type is X12. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="transaction_set" type="string">
                  Specifies the X12 transaction set (such as X12_850 or X12_810) for which the EDI capability is configured, determining the exact business document standard handled by this capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="version" type="string">
                  The X12 version for the EDI capability (e.g., VERSION_4010, VERSION_5010), determining the transaction set standard used for EDI document parsing and validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instructions_documents" type="list(object)">
  Lists the Amazon S3 locations of instruction documents that provide supplemental guidance for this capability in AWS B2B Data Interchange. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="bucket_name" type="string">
      The S3 bucket name where the instructions document for this B2BI capability is stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key" type="string">
      The S3 object key of an instructions document located in the bucket specified by the parent instructions_documents block, used to provide supplemental EDI processing instructions for the capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  A user-specified name for the AWS B2BI capability, which is required when creating the resource and serves as a human-readable identifier for the capability in the AWS B2BI service. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag to associate with the AWS B2B Data Interchange capability. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of the B2BI capability, which determines the document exchange protocol, with 'edi' currently the only supported value. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="capability_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the B2Bi capability, automatically assigned by AWS when the capability is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="capability_id" type="string">
  The unique identifier that AWS assigns to this B2BI capability when it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp indicating when the capability was created in AWS B2B Data Interchange. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="modified_at" type="string">
  The timestamp indicating when the B2B capability was last modified. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
