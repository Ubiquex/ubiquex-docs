---
title: "aws_b2_bi_profile"
description: "`aws_b2_bi_profile` stores an organization's own business contact information in AWS B2B Data Interchange..."
---

`aws_b2_bi_profile` stores an organization's own business contact information in AWS B2B Data Interchange, the foundation every `aws_b2_bi_partnership` is built on, since a partnership always ties a profile to a trading partner's capability.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	b2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/b2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bi profile"})

    		ubx.Resource(b2.BiProfile, "ci-runner", b2.BiProfileConfig{
    			BusinessName: "example-business-name",
    			Logging:      "example",
    			Name:         "ci-runner",
    			Phone:        "example",
    			Email:        "example",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BiProfile } from "./local-sdk/aws/sdk/typescript/aws/b2/bi_profile.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own bi profile" });

      resource(BiProfile, "ci-runner", {
        businessName: "example-business-name",
        logging: "example",
        name: "ci-runner",
        phone: "example",
        email: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.b2 import BiProfile, BiProfileConfig

    def describe():
        ubx.intent("payments own bi profile")

        ubx.resource(BiProfile, "ci-runner", BiProfileConfig(
            business_name="example-business-name",
            logging="example",
            name="ci-runner",
            phone="example",
            email="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_b2_bi_profile called "ci-runner", with business name
    example-business-name, logging example, phone example, email
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="business_name" type="string" required>
  The name of the business organization that this B2BI profile represents, used to identify the company in B2B EDI interchanges. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="email" type="string">
  The email address associated with the AWS B2Bi profile, used for service notifications and communications. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="logging" type="string" required>
  Determines whether CloudWatch logging is enabled for the profile, with valid values ENABLED or DISABLED, governing the capture of API activity and transaction events. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  Sets a human-readable name for the AWS B2Bi profile, used to identify it in the AWS console and API operations. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="phone" type="string" required>
  The phone number associated with the AWS B2Bi profile, a required contact detail used for the profile's business contact information. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the AWS B2B Data Interchange profile, used for cost allocation, organizing resources, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp indicating when the B2B Data Interchange profile was created, returned as a read-only string. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="log_group_name" type="string">
  The name of the CloudWatch Logs log group that AWS B2B Data Interchange automatically creates for the profile's activity and error logs. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="modified_at" type="string">
  The timestamp indicating when the B2B Data Interchange profile was last modified. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="profile_arn" type="string">
  The Amazon Resource Name (ARN) uniquely identifying the B2BI profile, assigned by AWS and used for cross-service references and IAM policies. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="profile_id" type="string">
  The unique identifier assigned by AWS to the B2Bi profile, generated upon creation and used to reference the profile in subsequent API operations and CloudFormation resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
