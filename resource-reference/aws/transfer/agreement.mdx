---
title: "aws_transfer_agreement"
description: "Real, generated bindings for `aws_transfer_agreement`."
---

`aws_transfer_agreement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agreement"})

    		ubx.Resource(transfer.Agreement, "ci-runner", transfer.AgreementConfig{
    			AccessRole:       "example",
    			BaseDirectory:    "example",
    			LocalProfileId:   "example",
    			PartnerProfileId: "example",
    			ServerId:         "example",
    			Description:      "Managed by ubx.",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Agreement } from "jsr:@ubx/sdk-aws/aws/transfer/agreement";

    export default stack("payments", () => {
      intent({ summary: "payments own agreement" });

      resource(Agreement, "ci-runner", {
        accessRole: "example",
        baseDirectory: "example",
        localProfileId: "example",
        partnerProfileId: "example",
        serverId: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transfer import Agreement, AgreementConfig

    def describe():
        ubx.intent("payments own agreement")

        ubx.resource(Agreement, "ci-runner", AgreementConfig(
            access_role="example",
            base_directory="example",
            local_profile_id="example",
            partner_profile_id="example",
            server_id="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_agreement, with access role example, base
    directory example, local profile id example, partner profile id
    example, server id example, description Managed by ubx., tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="base_directory" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="local_profile_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="partner_profile_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="agreement_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
