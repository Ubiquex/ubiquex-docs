---
title: "aws_transfer_connector"
description: "Real, generated bindings for `aws_transfer_connector`."
---

`aws_transfer_connector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connector"})

    		ubx.Resource(transfer.Connector, "ci-runner", transfer.ConnectorConfig{
    			AccessRole:  "example",
    			LoggingRole: "example",
    			Tags:        map[string]string{"managed-by": "ubx"},
    			Url:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Connector } from "jsr:@ubx/sdk-aws/aws/transfer/connector";

    export default stack("payments", () => {
      intent({ summary: "payments own connector" });

      resource(Connector, "ci-runner", {
        accessRole: "example",
        loggingRole: "example",
        tags: { "managed-by": "ubx" },
        url: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transfer import Connector, ConnectorConfig

    def describe():
        ubx.intent("payments own connector")

        ubx.resource(Connector, "ci-runner", ConnectorConfig(
            access_role="example",
            logging_role="example",
            tags={"managed-by": "ubx"},
            url="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_connector, with access role example, logging
    role example, tags a set value, url example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="logging_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
