---
title: "aws_transfer_connector"
description: "Real, generated bindings for `aws_transfer_connector`."
---

`aws_transfer_connector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"

    cfg := transfer.ConnectorConfig{
        AccessRole: "example",
        LoggingRole: "example",
        SecurityPolicyName: "example-security-policy-name",
    }
    ubx.Resource(transfer.Connector, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Connector } from
      "jsr:@ubx/sdk-aws/aws/transfer/connector";

    resource(Connector, "example", {
      accessRole: "example",
      loggingRole: "example",
      securityPolicyName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import Connector, ConnectorConfig

    cfg = ConnectorConfig(
        access_role="example",
        logging_role="example",
        security_policy_name="example",
    )
    ubx.resource(Connector, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_connector with access_role, logging_role,
security_policy_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="logging_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
