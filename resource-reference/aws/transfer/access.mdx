---
title: "aws_transfer_access"
description: "Real, generated bindings for `aws_transfer_access`."
---

`aws_transfer_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/transfer"

    cfg := transfer.AccessConfig{
        ExternalId: "example",
        ServerId: "example",
        HomeDirectory: "example",
        HomeDirectoryType: "example",
    }
    ubx.Resource(transfer.Access, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Access } from "jsr:@ubx/sdk-aws/aws/transfer/access";

    resource(Access, "example", {
      externalId: "example",
      serverId: "example",
      homeDirectory: "example",
      homeDirectoryType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import Access, AccessConfig

    cfg = AccessConfig(
        external_id="example",
        server_id="example",
        home_directory="example",
        home_directory_type="example",
    )
    ubx.resource(Access, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_access with external_id, server_id,
home_directory, home_directory_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="external_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="home_directory" type="string">
  Optional.
</ResponseField>

<ResponseField name="home_directory_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string">
  Optional.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
