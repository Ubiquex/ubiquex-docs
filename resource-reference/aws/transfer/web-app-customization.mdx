---
title: "aws_transfer_web_app_customization"
description: "Real, generated bindings for `aws_transfer_web_app_customization`."
---

`aws_transfer_web_app_customization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web app customization"})

    		ubx.Resource(transfer.WebAppCustomization, "ci-runner", transfer.WebAppCustomizationConfig{
    			WebAppId: "example",
    			Title:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebAppCustomization } from "jsr:@ubx/sdk-aws/aws/transfer/web_app_customization";

    export default stack("payments", () => {
      intent({ summary: "payments own web app customization" });

      resource(WebAppCustomization, "ci-runner", {
        webAppId: "example",
        title: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transfer import WebAppCustomization, WebAppCustomizationConfig

    def describe():
        ubx.intent("payments own web app customization")

        ubx.resource(WebAppCustomization, "ci-runner", WebAppCustomizationConfig(
            web_app_id="example",
            title="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_web_app_customization, with web app id
    example, title example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="favicon_file" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="logo_file" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="title" type="string">
  Optional.
</ResponseField>

<ResponseField name="web_app_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="favicon_file" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="logo_file" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
