---
title: "aws_transfer_ssh_key"
description: "Real, generated bindings for `aws_transfer_ssh_key`."
---

`aws_transfer_ssh_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/transfer"

    cfg := transfer.SshKeyConfig{
        Body: "example",
        ServerId: "example",
        UserName: "example-user-name",
    }
    ubx.Resource(transfer.SshKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SshKey } from "jsr:@ubx/sdk-aws/aws/transfer/ssh_key";

    resource(SshKey, "example", {
      body: "example",
      serverId: "example",
      userName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import SshKey, SshKeyConfig

    cfg = SshKeyConfig(
        body="example",
        server_id="example",
        user_name="example",
    )
    ubx.resource(SshKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_ssh_key with body, server_id, user_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ssh_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
