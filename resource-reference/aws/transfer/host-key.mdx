---
title: "aws_transfer_host_key"
description: "Real, generated bindings for `aws_transfer_host_key`."
---

`aws_transfer_host_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"

    cfg := transfer.HostKeyConfig{
        ServerId: "example",
        Description: "example",
        HostKeyBody: "example",
    }
    ubx.Resource(transfer.HostKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { HostKey } from "jsr:@ubx/sdk-aws/aws/transfer/host_key";

    resource(HostKey, "example", {
      serverId: "example",
      description: "example",
      hostKeyBody: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import HostKey, HostKeyConfig

    cfg = HostKeyConfig(
        server_id="example",
        description="example",
        host_key_body="example",
    )
    ubx.resource(HostKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_host_key with server_id, description,
host_key_body.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="host_key_body" type="string">
  Optional.
</ResponseField>

<ResponseField name="host_key_body_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="host_key_fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="host_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
