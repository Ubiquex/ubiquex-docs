---
title: "aws_transfer_host_key"
description: "Real, generated bindings for `aws_transfer_host_key`."
---

`aws_transfer_host_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host key"})

    		ubx.Resource(transfer.HostKey, "ci-runner", transfer.HostKeyConfig{
    			ServerId:      "example",
    			Description:   "Managed by ubx.",
    			HostKeyBody:   "example",
    			HostKeyBodyWo: "example",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostKey } from "jsr:@ubx/sdk-aws/aws/transfer/host_key";

    export default stack("payments", () => {
      intent({ summary: "payments own host key" });

      resource(HostKey, "ci-runner", {
        serverId: "example",
        description: "Managed by ubx.",
        hostKeyBody: "example",
        hostKeyBodyWo: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.transfer import HostKey, HostKeyConfig

    def describe():
        ubx.intent("payments own host key")

        ubx.resource(HostKey, "ci-runner", HostKeyConfig(
            server_id="example",
            description="Managed by ubx.",
            host_key_body="example",
            host_key_body_wo="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_host_key, with server id example, description
    Managed by ubx., host key body example, host key body wo example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="host_key_body" type="string">
  Optional.
</ResponseField>

<ResponseField name="host_key_body_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="host_key_fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="host_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
