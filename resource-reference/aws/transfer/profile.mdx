---
title: "aws_transfer_profile"
description: "`aws_transfer_profile` defines a real AS2 identity, local or partner, its AS2 ID and `aws_transfer_certificate`s..."
---

`aws_transfer_profile` defines a real AS2 identity, local or partner, its AS2 ID and `aws_transfer_certificate`s, that an `aws_transfer_agreement` references for both sides of an AS2 exchange.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own profile"})

    		ubx.Resource(transfer.Profile, "ci-runner", transfer.ProfileConfig{
    			As2Id:          "example",
    			ProfileType:    "example",
    			CertificateIds: []string{"example"},
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Profile } from "./local-sdk/aws/sdk/typescript/aws/transfer/profile.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own profile" });

      resource(Profile, "ci-runner", {
        as2Id: "example",
        profileType: "example",
        certificateIds: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.transfer import Profile, ProfileConfig

    def describe():
        ubx.intent("payments own profile")

        ubx.resource(Profile, "ci-runner", ProfileConfig(
            as2_id="example",
            profile_type="example",
            certificate_ids=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_profile, with as2 id example, profile type
    example, certificate ids a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="as2_id" type="string" required>
  AS2 identifier agreed with a trading partner. Required.
</ResponseField>

<ResponseField name="certificate_ids" type="list(string)">
  List of the certificate IDs associated with this profile to be used for encryption and signing of AS2 messages. Optional.
</ResponseField>

<ResponseField name="profile_type" type="string" required>
  Enum specifying whether the profile is local or associated with a trading partner. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      A user-defined string that serves as the key in a tag attached to an AWS Transfer Family profile, enabling resource categorization and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value part of a tag attached to this AWS Transfer Family profile, allowing you to categorize and manage the profile with custom metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Specifies the unique Amazon Resource Name (ARN) for the profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="profile_id" type="string">
  A unique identifier for the profile Computed by AWS after `ship`.
</ResponseField>
