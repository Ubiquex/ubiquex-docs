---
title: "aws_transfer_profile"
description: "Real, generated bindings for `aws_transfer_profile`."
---

`aws_transfer_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"

    cfg := transfer.ProfileConfig{
        As2Id: "example",
        ProfileType: "example",
        CertificateIds: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(transfer.Profile, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Profile } from "jsr:@ubx/sdk-aws/aws/transfer/profile";

    resource(Profile, "example", {
      as2Id: "example",
      profileType: "example",
      certificateIds: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import Profile, ProfileConfig

    cfg = ProfileConfig(
        as2_id="example",
        profile_type="example",
        certificate_ids=["example"],
        tags={"key": "value"},
    )
    ubx.resource(Profile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_profile with as2_id, profile_type,
certificate_ids, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="as2_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="profile_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="profile_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
