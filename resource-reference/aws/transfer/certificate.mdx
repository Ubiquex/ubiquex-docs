---
title: "aws_transfer_certificate"
description: "Real, generated bindings for `aws_transfer_certificate`."
---

`aws_transfer_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/transfer"

    cfg := transfer.CertificateConfig{
        Certificate: "example",
        Usage: "example",
        CertificateChain: "example",
        Description: "example",
    }
    ubx.Resource(transfer.Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Certificate } from
      "jsr:@ubx/sdk-aws/aws/transfer/certificate";

    resource(Certificate, "example", {
      certificate: "example",
      usage: "example",
      certificateChain: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.transfer import Certificate, CertificateConfig

    cfg = CertificateConfig(
        certificate="example",
        usage="example",
        certificate_chain="example",
        description="example",
    )
    ubx.resource(Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_transfer_certificate with certificate, usage,
certificate_chain, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="private_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="usage" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="active_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="inactive_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
