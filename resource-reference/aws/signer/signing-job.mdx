---
title: "aws_signer_signing_job"
description: "Real, generated bindings for `aws_signer_signing_job`."
---

`aws_signer_signing_job` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/signer"

    cfg := signer.SigningJobConfig{
        ProfileName: "example-profile-name",
        IgnoreSigningJobFailure: true,
    }
    ubx.Resource(signer.SigningJob, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SigningJob } from
      "jsr:@ubx/sdk-aws/aws/signer/signing_job";

    resource(SigningJob, "example", {
      profileName: "example",
      ignoreSigningJobFailure: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.signer import SigningJob, SigningJobConfig

    cfg = SigningJobConfig(
        profile_name="example",
        ignore_signing_job_failure=True,
    )
    ubx.resource(SigningJob, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_signer_signing_job with profile_name,
ignore_signing_job_failure.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ignore_signing_job_failure" type="bool">
  Optional.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="completed_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="job_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_invoker" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="platform_display_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="platform_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="profile_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="requested_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="revocation_record" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="reason" type="string">
    </ResponseField>
    <ResponseField name="revoked_at" type="string">
    </ResponseField>
    <ResponseField name="revoked_by" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="signature_expires_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="signed_object" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="s3" type="object">
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
        </ResponseField>
        <ResponseField name="key" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>
