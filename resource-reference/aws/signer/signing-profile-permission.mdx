---
title: "aws_signer_signing_profile_permission"
description: "Real, generated bindings for `aws_signer_signing_profile_permission`."
---

`aws_signer_signing_profile_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own signing profile permission"})

    		ubx.Resource(signer.SigningProfilePermission, "ci-runner", signer.SigningProfilePermissionConfig{
    			Action:      "example",
    			Principal:   "example",
    			ProfileName: "example-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SigningProfilePermission } from "jsr:@ubx/sdk-aws/aws/signer/signing_profile_permission";

    export default stack("payments", () => {
      intent({ summary: "payments own signing profile permission" });

      resource(SigningProfilePermission, "ci-runner", {
        action: "example",
        principal: "example",
        profileName: "example-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.signer import SigningProfilePermission, SigningProfilePermissionConfig

    def describe():
        ubx.intent("payments own signing profile permission")

        ubx.resource(SigningProfilePermission, "ci-runner", SigningProfilePermissionConfig(
            action="example",
            principal="example",
            profile_name="example-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_signer_signing_profile_permission, with action
    example, principal example, profile name example-profile-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="profile_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="profile_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
