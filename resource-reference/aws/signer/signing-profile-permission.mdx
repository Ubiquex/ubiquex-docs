---
title: "aws_signer_signing_profile_permission"
description: "Real, generated bindings for `aws_signer_signing_profile_permission`."
---

`aws_signer_signing_profile_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/signer"

    cfg := signer.SigningProfilePermissionConfig{
        Action: "example",
        Principal: "example",
        ProfileName: "example-profile-name",
        ProfileVersion: "example",
        StatementId: "example",
    }
    ubx.Resource(signer.SigningProfilePermission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SigningProfilePermission } from
      "jsr:@ubx/sdk-aws/aws/signer/signing_profile_permission";

    resource(SigningProfilePermission, "example", {
      action: "example",
      principal: "example",
      profileName: "example",
      profileVersion: "example",
      statementId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.signer import (
        SigningProfilePermission,
        SigningProfilePermissionConfig,
    )

    cfg = SigningProfilePermissionConfig(
        action="example",
        principal="example",
        profile_name="example",
        profile_version="example",
        statement_id="example",
    )
    ubx.resource(SigningProfilePermission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_signer_signing_profile_permission with action,
principal, profile_name, profile_version, statement_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="profile_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="profile_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
