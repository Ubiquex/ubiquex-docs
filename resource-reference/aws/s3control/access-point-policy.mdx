---
title: "aws_s3control_access_point_policy"
description: "Real, generated bindings for `aws_s3control_access_point_policy`."
---

`aws_s3control_access_point_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3control"

    cfg := s3control.AccessPointPolicyConfig{
        AccessPointArn: "example",
        Policy: "example",
    }
    ubx.Resource(s3control.AccessPointPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccessPointPolicy } from
      "jsr:@ubx/sdk-aws/aws/s3control/access_point_policy";

    resource(AccessPointPolicy, "example", {
      accessPointArn: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        AccessPointPolicy,
        AccessPointPolicyConfig,
    )

    cfg = AccessPointPolicyConfig(
        access_point_arn="example",
        policy="example",
    )
    ubx.resource(AccessPointPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_access_point_policy with
access_point_arn, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_point_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="has_public_access_policy" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
