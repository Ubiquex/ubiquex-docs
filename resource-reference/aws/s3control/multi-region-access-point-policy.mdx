---
title: "aws_s3control_multi_region_access_point_policy"
description: "Real, generated bindings for `aws_s3control_multi_region_access_point_policy`."
---

`aws_s3control_multi_region_access_point_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own multi region access point policy"})

    		ubx.Resource(s3control.MultiRegionAccessPointPolicy, "ci-runner", s3control.MultiRegionAccessPointPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MultiRegionAccessPointPolicy } from "jsr:@ubx/sdk-aws/aws/s3control/multi_region_access_point_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own multi region access point policy" });

      resource(MultiRegionAccessPointPolicy, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import MultiRegionAccessPointPolicy, MultiRegionAccessPointPolicyConfig

    def describe():
        ubx.intent("payments own multi region access point policy")

        ubx.resource(MultiRegionAccessPointPolicy, "ci-runner", MultiRegionAccessPointPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_multi_region_access_point_policy, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="established" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="proposed" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
