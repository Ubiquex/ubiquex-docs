---
title: "aws_s3control_access_grants_location"
description: "Real, generated bindings for `aws_s3control_access_grants_location`."
---

`aws_s3control_access_grants_location` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3control"

    cfg := s3control.AccessGrantsLocationConfig{
        IamRoleArn: "example",
        LocationScope: "example",
        AccountId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(s3control.AccessGrantsLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccessGrantsLocation } from
      "jsr:@ubx/sdk-aws/aws/s3control/access_grants_location";

    resource(AccessGrantsLocation, "example", {
      iamRoleArn: "example",
      locationScope: "example",
      accountId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        AccessGrantsLocation,
        AccessGrantsLocationConfig,
    )

    cfg = AccessGrantsLocationConfig(
        iam_role_arn="example",
        location_scope="example",
        account_id="example",
        tags={"key": "value"},
    )
    ubx.resource(AccessGrantsLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_access_grants_location with iam_role_arn,
location_scope, account_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location_scope" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="access_grants_location_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="access_grants_location_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
