---
title: "aws_s3control_bucket_lifecycle_configuration"
description: "Real, generated bindings for `aws_s3control_bucket_lifecycle_configuration`."
---

`aws_s3control_bucket_lifecycle_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket lifecycle configuration"})

    		ubx.Resource(s3control.BucketLifecycleConfiguration, "ci-runner", s3control.BucketLifecycleConfigurationConfig{
    			Bucket: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketLifecycleConfiguration } from "jsr:@ubx/sdk-aws/aws/s3control/bucket_lifecycle_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket lifecycle configuration" });

      resource(BucketLifecycleConfiguration, "ci-runner", {
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import BucketLifecycleConfiguration, BucketLifecycleConfigurationConfig

    def describe():
        ubx.intent("payments own bucket lifecycle configuration")

        ubx.resource(BucketLifecycleConfiguration, "ci-runner", BucketLifecycleConfigurationConfig(
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_bucket_lifecycle_configuration, with bucket
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
