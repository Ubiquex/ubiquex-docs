---
title: "aws_s3control_bucket_lifecycle_configuration"
description: "Real, generated bindings for `aws_s3control_bucket_lifecycle_configuration`."
---

`aws_s3control_bucket_lifecycle_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3control"

    cfg := s3control.BucketLifecycleConfigurationConfig{
        Bucket: "example",
    }
    ubx.Resource(
        s3control.BucketLifecycleConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketLifecycleConfiguration } from
      "jsr:@ubx/sdk-aws/aws/s3control/bucket_lifecycle_configuration";

    resource(BucketLifecycleConfiguration, "example", {
      bucket: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        BucketLifecycleConfiguration,
        BucketLifecycleConfigurationConfig,
    )

    cfg = BucketLifecycleConfigurationConfig(
        bucket="example",
    )
    ubx.resource(BucketLifecycleConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_bucket_lifecycle_configuration with
bucket.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
