---
title: "aws_s3control_access_grant"
description: "Real, generated bindings for `aws_s3control_access_grant`."
---

`aws_s3control_access_grant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access grant"})

    		ubx.Resource(s3control.AccessGrant, "ci-runner", s3control.AccessGrantConfig{
    			AccessGrantsLocationId: "example",
    			Permission:             "example",
    			S3PrefixType:           "example",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessGrant } from "jsr:@ubx/sdk-aws/aws/s3control/access_grant";

    export default stack("payments", () => {
      intent({ summary: "payments own access grant" });

      resource(AccessGrant, "ci-runner", {
        accessGrantsLocationId: "example",
        permission: "example",
        s3PrefixType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import AccessGrant, AccessGrantConfig

    def describe():
        ubx.intent("payments own access grant")

        ubx.resource(AccessGrant, "ci-runner", AccessGrantConfig(
            access_grants_location_id="example",
            permission="example",
            s3_prefix_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_access_grant, with access grants location id
    example, permission example, s3 prefix type example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_grants_location_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="s3_sub_prefix" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="access_grants_location_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="grantee" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="grantee_identifier" type="string" required>
    </ResponseField>
    <ResponseField name="grantee_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="permission" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_prefix_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="access_grant_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="access_grant_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="access_grants_location_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="s3_sub_prefix" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="grant_scope" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grantee" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="grantee_identifier" type="string" required>
    </ResponseField>
    <ResponseField name="grantee_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
