---
title: "aws_s3control_access_grants_instance_resource_policy"
description: "Real, generated bindings for `aws_s3control_access_grants_instance_resource_policy`."
---

`aws_s3control_access_grants_instance_resource_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"

    cfg := s3control.AccessGrantsInstanceResourcePolicyConfig{
        Policy: "example",
        AccountId: "example",
    }
    ubx.Resource(
        s3control.AccessGrantsInstanceResourcePolicy,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccessGrantsInstanceResourcePolicy } from
      "jsr:@ubx/sdk-aws/aws/s3control/access_grants_instance_resource_policy";

    resource(AccessGrantsInstanceResourcePolicy, "example", {
      policy: "example",
      accountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        AccessGrantsInstanceResourcePolicy,
        AccessGrantsInstanceResourcePolicyConfig,
    )

    cfg = AccessGrantsInstanceResourcePolicyConfig(
        policy="example",
        account_id="example",
    )
    ubx.resource(AccessGrantsInstanceResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_access_grants_instance_resource_policy
with policy, account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
