---
title: "aws_s3control_access_grants_instance"
description: "Real, generated bindings for `aws_s3control_access_grants_instance`."
---

`aws_s3control_access_grants_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"

    cfg := s3control.AccessGrantsInstanceConfig{
        AccountId: "example",
        IdentityCenterArn: "example",
    }
    ubx.Resource(s3control.AccessGrantsInstance, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccessGrantsInstance } from
      "jsr:@ubx/sdk-aws/aws/s3control/access_grants_instance";

    resource(AccessGrantsInstance, "example", {
      accountId: "example",
      identityCenterArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        AccessGrantsInstance,
        AccessGrantsInstanceConfig,
    )

    cfg = AccessGrantsInstanceConfig(
        account_id="example",
        identity_center_arn="example",
    )
    ubx.resource(AccessGrantsInstance, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_access_grants_instance with account_id,
identity_center_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_center_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="access_grants_instance_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="access_grants_instance_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identity_center_application_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
