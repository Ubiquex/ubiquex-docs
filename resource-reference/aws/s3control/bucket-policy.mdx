---
title: "aws_s3control_bucket_policy"
description: "Real, generated bindings for `aws_s3control_bucket_policy`."
---

`aws_s3control_bucket_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"

    cfg := s3control.BucketPolicyConfig{
        Bucket: "example",
        Policy: "example",
    }
    ubx.Resource(s3control.BucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketPolicy } from
      "jsr:@ubx/sdk-aws/aws/s3control/bucket_policy";

    resource(BucketPolicy, "example", {
      bucket: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import BucketPolicy, BucketPolicyConfig

    cfg = BucketPolicyConfig(
        bucket="example",
        policy="example",
    )
    ubx.resource(BucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_bucket_policy with bucket, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
