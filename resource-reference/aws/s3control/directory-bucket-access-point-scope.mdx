---
title: "aws_s3control_directory_bucket_access_point_scope"
description: "Real, generated bindings for `aws_s3control_directory_bucket_access_point_scope`."
---

`aws_s3control_directory_bucket_access_point_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3control"

    cfg := s3control.DirectoryBucketAccessPointScopeConfig{
        AccountId: "example",
        Name: "example-name",
    }
    ubx.Resource(
        s3control.DirectoryBucketAccessPointScope,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DirectoryBucketAccessPointScope } from
      "jsr:@ubx/sdk-aws/aws/s3control/directory_bucket_access_point_scope";

    resource(DirectoryBucketAccessPointScope, "example", {
      accountId: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        DirectoryBucketAccessPointScope,
        DirectoryBucketAccessPointScopeConfig,
    )

    cfg = DirectoryBucketAccessPointScopeConfig(
        account_id="example",
        name="example",
    )
    ubx.resource(DirectoryBucketAccessPointScope, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_directory_bucket_access_point_scope with
account_id, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
