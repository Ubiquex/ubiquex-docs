---
title: "aws_s3control_directory_bucket_access_point_scope"
description: "Real, generated bindings for `aws_s3control_directory_bucket_access_point_scope`."
---

`aws_s3control_directory_bucket_access_point_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory bucket access point scope"})

    		ubx.Resource(s3control.DirectoryBucketAccessPointScope, "ci-runner", s3control.DirectoryBucketAccessPointScopeConfig{
    			AccountId: "example",
    			Name:      "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryBucketAccessPointScope } from "jsr:@ubx/sdk-aws/aws/s3control/directory_bucket_access_point_scope";

    export default stack("payments", () => {
      intent({ summary: "payments own directory bucket access point scope" });

      resource(DirectoryBucketAccessPointScope, "ci-runner", {
        accountId: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import DirectoryBucketAccessPointScope, DirectoryBucketAccessPointScopeConfig

    def describe():
        ubx.intent("payments own directory bucket access point scope")

        ubx.resource(DirectoryBucketAccessPointScope, "ci-runner", DirectoryBucketAccessPointScopeConfig(
            account_id="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_directory_bucket_access_point_scope called
    "ci-runner", with account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
