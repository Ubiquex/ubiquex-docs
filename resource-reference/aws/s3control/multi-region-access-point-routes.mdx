---
title: "aws_s3control_multi_region_access_point_routes"
description: "Real, generated bindings for `aws_s3control_multi_region_access_point_routes`."
---

`aws_s3control_multi_region_access_point_routes` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"

    cfg := s3control.MultiRegionAccessPointRoutesConfig{
        Mrap: "example",
        AccountId: "example",
    }
    ubx.Resource(
        s3control.MultiRegionAccessPointRoutes,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MultiRegionAccessPointRoutes } from
      "jsr:@ubx/sdk-aws/aws/s3control/multi_region_access_point_routes";

    resource(MultiRegionAccessPointRoutes, "example", {
      mrap: "example",
      accountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        MultiRegionAccessPointRoutes,
        MultiRegionAccessPointRoutesConfig,
    )

    cfg = MultiRegionAccessPointRoutesConfig(
        mrap="example",
        account_id="example",
    )
    ubx.resource(MultiRegionAccessPointRoutes, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_multi_region_access_point_routes with
mrap, account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mrap" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
