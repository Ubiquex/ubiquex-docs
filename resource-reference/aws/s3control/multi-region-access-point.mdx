---
title: "aws_s3control_multi_region_access_point"
description: "Real, generated bindings for `aws_s3control_multi_region_access_point`."
---

`aws_s3control_multi_region_access_point` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3control"

    cfg := s3control.MultiRegionAccessPointConfig{
        AccountId: "example",
    }
    ubx.Resource(s3control.MultiRegionAccessPoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MultiRegionAccessPoint } from
      "jsr:@ubx/sdk-aws/aws/s3control/multi_region_access_point";

    resource(MultiRegionAccessPoint, "example", {
      accountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3control import (
        MultiRegionAccessPoint,
        MultiRegionAccessPointConfig,
    )

    cfg = MultiRegionAccessPointConfig(
        account_id="example",
    )
    ubx.resource(MultiRegionAccessPoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3control_multi_region_access_point with
account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
