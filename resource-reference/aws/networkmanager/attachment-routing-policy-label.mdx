---
title: "aws_networkmanager_attachment_routing_policy_label"
description: "Real, generated bindings for `aws_networkmanager_attachment_routing_policy_label`."
---

`aws_networkmanager_attachment_routing_policy_label` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"

    cfg := networkmanager.AttachmentRoutingPolicyLabelConfig{
        AttachmentId: "example",
        CoreNetworkId: "example",
        RoutingPolicyLabel: "example",
    }
    ubx.Resource(
        networkmanager.AttachmentRoutingPolicyLabel,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AttachmentRoutingPolicyLabel } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/attachment_routing_policy_label";

    resource(AttachmentRoutingPolicyLabel, "example", {
      attachmentId: "example",
      coreNetworkId: "example",
      routingPolicyLabel: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        AttachmentRoutingPolicyLabel,
        AttachmentRoutingPolicyLabelConfig,
    )

    cfg = AttachmentRoutingPolicyLabelConfig(
        attachment_id="example",
        core_network_id="example",
        routing_policy_label="example",
    )
    ubx.resource(AttachmentRoutingPolicyLabel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_attachment_routing_policy_label with
attachment_id, core_network_id, routing_policy_label.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="routing_policy_label" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
