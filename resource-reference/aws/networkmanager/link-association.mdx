---
title: "aws_network_manager_link_association"
description: "`aws_network_manager_link_association` associates a real `aws_network_manager_link` with a specific device in an `aws_network_manager_global_network`..."
---

`aws_network_manager_link_association` associates a real `aws_network_manager_link` with a specific device in an `aws_network_manager_global_network`, the resource that actually attaches that network connection to a device in Network Manager's own topology.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager link association"})

    		ubx.Resource(networkmanager.LinkAssociation, "example", networkmanager.LinkAssociationConfig{
    			DeviceId:        "example",
    			GlobalNetworkId: "example",
    			LinkId:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LinkAssociation } from "@ubx/sdk-aws/aws/networkmanager/link_association";

    export default stack("example", () => {
      intent({ summary: "example own manager link association" });

      resource(LinkAssociation, "example", {
        deviceId: "example",
        globalNetworkId: "example",
        linkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager.link_association import LinkAssociation, LinkAssociationConfig

    def describe():
        ubx.intent("example own manager link association")

        ubx.resource(LinkAssociation, "example", LinkAssociationConfig(
            device_id="example",
            global_network_id="example",
            link_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_link_association, with device id
    example, global network id example, link id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="device_id" type="string" required>
  The ID of the device Required.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  The ID of the global network. Required.
</ResponseField>

<ResponseField name="link_id" type="string" required>
  The ID of the link Required.
</ResponseField>
