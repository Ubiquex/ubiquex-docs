---
title: "aws_networkmanager_dx_gateway_attachment"
description: "Real, generated bindings for `aws_networkmanager_dx_gateway_attachment`."
---

`aws_networkmanager_dx_gateway_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.DxGatewayAttachmentConfig{
        CoreNetworkId: "example",
        DirectConnectGatewayArn: "example",
        EdgeLocations: []string{"example"},
        RoutingPolicyLabel: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkmanager.DxGatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DxGatewayAttachment } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/dx_gateway_attachment";

    resource(DxGatewayAttachment, "example", {
      coreNetworkId: "example",
      directConnectGatewayArn: "example",
      edgeLocations: ["example"],
      routingPolicyLabel: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        DxGatewayAttachment,
        DxGatewayAttachmentConfig,
    )

    cfg = DxGatewayAttachmentConfig(
        core_network_id="example",
        direct_connect_gateway_arn="example",
        edge_locations=["example"],
        routing_policy_label="example",
        tags={"key": "value"},
    )
    ubx.resource(DxGatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_dx_gateway_attachment with
core_network_id, direct_connect_gateway_arn, edge_locations,
routing_policy_label, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="direct_connect_gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="edge_locations" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
