---
title: "aws_networkmanager_dx_gateway_attachment"
description: "Real, generated bindings for `aws_networkmanager_dx_gateway_attachment`."
---

`aws_networkmanager_dx_gateway_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dx gateway attachment"})

    		ubx.Resource(networkmanager.DxGatewayAttachment, "ci-runner", networkmanager.DxGatewayAttachmentConfig{
    			CoreNetworkId:           "example",
    			DirectConnectGatewayArn: "arn:aws:iam::123456789012:policy/ci",
    			EdgeLocations:           []string{"example"},
    			RoutingPolicyLabel:      "example",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DxGatewayAttachment } from "jsr:@ubx/sdk-aws/aws/networkmanager/dx_gateway_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own dx gateway attachment" });

      resource(DxGatewayAttachment, "ci-runner", {
        coreNetworkId: "example",
        directConnectGatewayArn: "arn:aws:iam::123456789012:policy/ci",
        edgeLocations: ["example"],
        routingPolicyLabel: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import DxGatewayAttachment, DxGatewayAttachmentConfig

    def describe():
        ubx.intent("payments own dx gateway attachment")

        ubx.resource(DxGatewayAttachment, "ci-runner", DxGatewayAttachmentConfig(
            core_network_id="example",
            direct_connect_gateway_arn="arn:aws:iam::123456789012:policy/ci",
            edge_locations=["example"],
            routing_policy_label="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_dx_gateway_attachment, with core
    network id example, direct connect gateway arn
    arn:aws:iam::123456789012:policy/ci, edge locations a set value,
    routing policy label example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="direct_connect_gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="edge_locations" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
