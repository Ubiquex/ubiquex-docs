---
title: "aws_networkmanager_site"
description: "Real, generated bindings for `aws_networkmanager_site`."
---

`aws_networkmanager_site` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"

    cfg := networkmanager.SiteConfig{
        GlobalNetworkId: "example",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkmanager.Site, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Site } from "jsr:@ubx/sdk-aws/aws/networkmanager/site";

    resource(Site, "example", {
      globalNetworkId: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import Site, SiteConfig

    cfg = SiteConfig(
        global_network_id="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(Site, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_site with global_network_id,
description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
