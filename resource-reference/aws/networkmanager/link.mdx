---
title: "aws_networkmanager_link"
description: "Real, generated bindings for `aws_networkmanager_link`."
---

`aws_networkmanager_link` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own link"})

    		ubx.Resource(networkmanager.Link, "ci-runner", networkmanager.LinkConfig{
    			GlobalNetworkId: "example",
    			SiteId:          "example",
    			Description:     "Managed by ubx.",
    			ProviderName:    "example-provider-name",
    			Tags:            map[string]string{"managed-by": "ubx"},
    			Type:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Link } from "jsr:@ubx/sdk-aws/aws/networkmanager/link";

    export default stack("payments", () => {
      intent({ summary: "payments own link" });

      resource(Link, "ci-runner", {
        globalNetworkId: "example",
        siteId: "example",
        description: "Managed by ubx.",
        providerName: "example-provider-name",
        tags: { "managed-by": "ubx" },
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import Link, LinkConfig

    def describe():
        ubx.intent("payments own link")

        ubx.resource(Link, "ci-runner", LinkConfig(
            global_network_id="example",
            site_id="example",
            description="Managed by ubx.",
            provider_name="example-provider-name",
            tags={"managed-by": "ubx"},
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_link, with global network id example,
    site id example, description Managed by ubx., provider name
    example-provider-name, tags a set value, type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provider_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="site_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
