---
title: "aws_networkmanager_transit_gateway_peering"
description: "Real, generated bindings for `aws_networkmanager_transit_gateway_peering`."
---

`aws_networkmanager_transit_gateway_peering` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"

    cfg := networkmanager.TransitGatewayPeeringConfig{
        CoreNetworkId: "example",
        TransitGatewayArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        networkmanager.TransitGatewayPeering,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayPeering } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/transit_gateway_peering";

    resource(TransitGatewayPeering, "example", {
      coreNetworkId: "example",
      transitGatewayArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        TransitGatewayPeering,
        TransitGatewayPeeringConfig,
    )

    cfg = TransitGatewayPeeringConfig(
        core_network_id="example",
        transit_gateway_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(TransitGatewayPeering, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_transit_gateway_peering with
core_network_id, transit_gateway_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peering_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_peering_attachment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
