---
title: "aws_networkmanager_core_network_policy_attachment"
description: "Real, generated bindings for `aws_networkmanager_core_network_policy_attachment`."
---

`aws_networkmanager_core_network_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own core network policy attachment"})

    		ubx.Resource(networkmanager.CoreNetworkPolicyAttachment, "ci-runner", networkmanager.CoreNetworkPolicyAttachmentConfig{
    			CoreNetworkId:  "example",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CoreNetworkPolicyAttachment } from "jsr:@ubx/sdk-aws/aws/networkmanager/core_network_policy_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own core network policy attachment" });

      resource(CoreNetworkPolicyAttachment, "ci-runner", {
        coreNetworkId: "example",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import CoreNetworkPolicyAttachment, CoreNetworkPolicyAttachmentConfig

    def describe():
        ubx.intent("payments own core network policy attachment")

        ubx.resource(CoreNetworkPolicyAttachment, "ci-runner", CoreNetworkPolicyAttachmentConfig(
            core_network_id="example",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_core_network_policy_attachment, with
    core network id example, policy document example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
