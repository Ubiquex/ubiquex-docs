---
title: "aws_networkmanager_transit_gateway_connect_peer_association"
description: "Real, generated bindings for `aws_networkmanager_transit_gateway_connect_peer_association`."
---

`aws_networkmanager_transit_gateway_connect_peer_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway connect peer association"})

    		ubx.Resource(networkmanager.TransitGatewayConnectPeerAssociation, "ci-runner", networkmanager.TransitGatewayConnectPeerAssociationConfig{
    			DeviceId:                     "example",
    			GlobalNetworkId:              "example",
    			TransitGatewayConnectPeerArn: "arn:aws:iam::123456789012:policy/ci",
    			LinkId:                       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayConnectPeerAssociation } from "jsr:@ubx/sdk-aws/aws/networkmanager/transit_gateway_connect_peer_association";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway connect peer association" });

      resource(TransitGatewayConnectPeerAssociation, "ci-runner", {
        deviceId: "example",
        globalNetworkId: "example",
        transitGatewayConnectPeerArn: "arn:aws:iam::123456789012:policy/ci",
        linkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import TransitGatewayConnectPeerAssociation, TransitGatewayConnectPeerAssociationConfig

    def describe():
        ubx.intent("payments own transit gateway connect peer association")

        ubx.resource(TransitGatewayConnectPeerAssociation, "ci-runner", TransitGatewayConnectPeerAssociationConfig(
            device_id="example",
            global_network_id="example",
            transit_gateway_connect_peer_arn="arn:aws:iam::123456789012:policy/ci",
            link_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_networkmanager_transit_gateway_connect_peer_association, with
    device id example, global network id example, transit gateway
    connect peer arn arn:aws:iam::123456789012:policy/ci, link id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="device_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="link_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_connect_peer_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
