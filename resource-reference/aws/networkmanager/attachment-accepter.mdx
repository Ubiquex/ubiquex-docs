---
title: "aws_networkmanager_attachment_accepter"
description: "Real, generated bindings for `aws_networkmanager_attachment_accepter`."
---

`aws_networkmanager_attachment_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.AttachmentAccepterConfig{
        AttachmentId: "example",
        AttachmentType: "example",
    }
    ubx.Resource(networkmanager.AttachmentAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AttachmentAccepter } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/attachment_accepter";

    resource(AttachmentAccepter, "example", {
      attachmentId: "example",
      attachmentType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        AttachmentAccepter,
        AttachmentAccepterConfig,
    )

    cfg = AttachmentAccepterConfig(
        attachment_id="example",
        attachment_type="example",
    )
    ubx.resource(AttachmentAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_attachment_accepter with
attachment_id, attachment_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="attachment_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_locations" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
