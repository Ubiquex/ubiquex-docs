---
title: "aws_networkmanager_transit_gateway_registration"
description: "Real, generated bindings for `aws_networkmanager_transit_gateway_registration`."
---

`aws_networkmanager_transit_gateway_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway registration"})

    		ubx.Resource(networkmanager.TransitGatewayRegistration, "ci-runner", networkmanager.TransitGatewayRegistrationConfig{
    			GlobalNetworkId:   "example",
    			TransitGatewayArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayRegistration } from "jsr:@ubx/sdk-aws/aws/networkmanager/transit_gateway_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway registration" });

      resource(TransitGatewayRegistration, "ci-runner", {
        globalNetworkId: "example",
        transitGatewayArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import TransitGatewayRegistration, TransitGatewayRegistrationConfig

    def describe():
        ubx.intent("payments own transit gateway registration")

        ubx.resource(TransitGatewayRegistration, "ci-runner", TransitGatewayRegistrationConfig(
            global_network_id="example",
            transit_gateway_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_transit_gateway_registration, with
    global network id example, transit gateway arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
