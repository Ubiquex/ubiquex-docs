---
title: "aws_networkmanager_transit_gateway_registration"
description: "Real, generated bindings for `aws_networkmanager_transit_gateway_registration`."
---

`aws_networkmanager_transit_gateway_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.TransitGatewayRegistrationConfig{
        GlobalNetworkId: "example",
        TransitGatewayArn: "example",
    }
    ubx.Resource(
        networkmanager.TransitGatewayRegistration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TransitGatewayRegistration } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/transit_gateway_registration";

    resource(TransitGatewayRegistration, "example", {
      globalNetworkId: "example",
      transitGatewayArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        TransitGatewayRegistration,
        TransitGatewayRegistrationConfig,
    )

    cfg = TransitGatewayRegistrationConfig(
        global_network_id="example",
        transit_gateway_arn="example",
    )
    ubx.resource(TransitGatewayRegistration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_transit_gateway_registration with
global_network_id, transit_gateway_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
