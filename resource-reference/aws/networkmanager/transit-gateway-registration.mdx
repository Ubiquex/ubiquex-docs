---
title: "aws_network_manager_transit_gateway_registration"
description: "`aws_network_manager_transit_gateway_registration` registers a real, existing EC2 Transit Gateway with an `aws_network_manager_global_network`..."
---

`aws_network_manager_transit_gateway_registration` registers a real, existing EC2 Transit Gateway with an `aws_network_manager_global_network`, so Network Manager can represent and monitor it alongside an organization's other network infrastructure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager transit gateway registration"})

    		ubx.Resource(networkmanager.TransitGatewayRegistration, "example", networkmanager.TransitGatewayRegistrationConfig{
    			GlobalNetworkId:   "example",
    			TransitGatewayArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayRegistration } from "@ubx/sdk-aws/aws/networkmanager/transit_gateway_registration";

    export default stack("example", () => {
      intent({ summary: "example own manager transit gateway registration" });

      resource(TransitGatewayRegistration, "example", {
        globalNetworkId: "example",
        transitGatewayArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager.transit_gateway_registration import TransitGatewayRegistration, TransitGatewayRegistrationConfig

    def describe():
        ubx.intent("example own manager transit gateway registration")

        ubx.resource(TransitGatewayRegistration, "example", TransitGatewayRegistrationConfig(
            global_network_id="example",
            transit_gateway_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_transit_gateway_registration, with
    global network id example, transit gateway arn
    arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="global_network_id" type="string" required>
  The ID of the global network. Required.
</ResponseField>

<ResponseField name="transit_gateway_arn" type="string" required>
  The Amazon Resource Name (ARN) of the transit gateway. Required.
</ResponseField>
