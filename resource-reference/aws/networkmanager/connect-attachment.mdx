---
title: "aws_networkmanager_connect_attachment"
description: "Real, generated bindings for `aws_networkmanager_connect_attachment`."
---

`aws_networkmanager_connect_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.ConnectAttachmentConfig{
        CoreNetworkId: "example",
        EdgeLocation: "example",
        TransportAttachmentId: "example",
        RoutingPolicyLabel: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkmanager.ConnectAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectAttachment } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/connect_attachment";

    resource(ConnectAttachment, "example", {
      coreNetworkId: "example",
      edgeLocation: "example",
      transportAttachmentId: "example",
      routingPolicyLabel: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        ConnectAttachment,
        ConnectAttachmentConfig,
    )

    cfg = ConnectAttachmentConfig(
        core_network_id="example",
        edge_location="example",
        transport_attachment_id="example",
        routing_policy_label="example",
        tags={"key": "value"},
    )
    ubx.resource(ConnectAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_connect_attachment with
core_network_id, edge_location, transport_attachment_id,
routing_policy_label, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="edge_location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transport_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
