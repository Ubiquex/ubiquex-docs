---
title: "aws_networkmanager_global_network"
description: "Real, generated bindings for `aws_networkmanager_global_network`."
---

`aws_networkmanager_global_network` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.GlobalNetworkConfig{
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkmanager.GlobalNetwork, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GlobalNetwork } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/global_network";

    resource(GlobalNetwork, "example", {
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        GlobalNetwork,
        GlobalNetworkConfig,
    )

    cfg = GlobalNetworkConfig(
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(GlobalNetwork, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_global_network with description,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
