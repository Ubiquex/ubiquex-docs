---
title: "aws_networkmanager_prefix_list_association"
description: "Real, generated bindings for `aws_networkmanager_prefix_list_association`."
---

`aws_networkmanager_prefix_list_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own prefix list association"})

    		ubx.Resource(networkmanager.PrefixListAssociation, "ci-runner", networkmanager.PrefixListAssociationConfig{
    			CoreNetworkId:   "example",
    			PrefixListAlias: "example",
    			PrefixListArn:   "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrefixListAssociation } from "jsr:@ubx/sdk-aws/aws/networkmanager/prefix_list_association";

    export default stack("payments", () => {
      intent({ summary: "payments own prefix list association" });

      resource(PrefixListAssociation, "ci-runner", {
        coreNetworkId: "example",
        prefixListAlias: "example",
        prefixListArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import PrefixListAssociation, PrefixListAssociationConfig

    def describe():
        ubx.intent("payments own prefix list association")

        ubx.resource(PrefixListAssociation, "ci-runner", PrefixListAssociationConfig(
            core_network_id="example",
            prefix_list_alias="example",
            prefix_list_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_prefix_list_association, with core
    network id example, prefix list alias example, prefix list arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="prefix_list_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="prefix_list_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
