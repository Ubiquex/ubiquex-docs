---
title: "aws_networkmanager_prefix_list_association"
description: "Real, generated bindings for `aws_networkmanager_prefix_list_association`."
---

`aws_networkmanager_prefix_list_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmanager"

    cfg := networkmanager.PrefixListAssociationConfig{
        CoreNetworkId: "example",
        PrefixListAlias: "example",
        PrefixListArn: "example",
    }
    ubx.Resource(
        networkmanager.PrefixListAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PrefixListAssociation } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/prefix_list_association";

    resource(PrefixListAssociation, "example", {
      coreNetworkId: "example",
      prefixListAlias: "example",
      prefixListArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        PrefixListAssociation,
        PrefixListAssociationConfig,
    )

    cfg = PrefixListAssociationConfig(
        core_network_id="example",
        prefix_list_alias="example",
        prefix_list_arn="example",
    )
    ubx.resource(PrefixListAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_prefix_list_association with
core_network_id, prefix_list_alias, prefix_list_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="prefix_list_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="prefix_list_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
