---
title: "aws_networkmanager_customer_gateway_association"
description: "Real, generated bindings for `aws_networkmanager_customer_gateway_association`."
---

`aws_networkmanager_customer_gateway_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own customer gateway association"})

    		ubx.Resource(networkmanager.CustomerGatewayAssociation, "ci-runner", networkmanager.CustomerGatewayAssociationConfig{
    			CustomerGatewayArn: "arn:aws:iam::123456789012:policy/ci",
    			DeviceId:           "example",
    			GlobalNetworkId:    "example",
    			LinkId:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomerGatewayAssociation } from "jsr:@ubx/sdk-aws/aws/networkmanager/customer_gateway_association";

    export default stack("payments", () => {
      intent({ summary: "payments own customer gateway association" });

      resource(CustomerGatewayAssociation, "ci-runner", {
        customerGatewayArn: "arn:aws:iam::123456789012:policy/ci",
        deviceId: "example",
        globalNetworkId: "example",
        linkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import CustomerGatewayAssociation, CustomerGatewayAssociationConfig

    def describe():
        ubx.intent("payments own customer gateway association")

        ubx.resource(CustomerGatewayAssociation, "ci-runner", CustomerGatewayAssociationConfig(
            customer_gateway_arn="arn:aws:iam::123456789012:policy/ci",
            device_id="example",
            global_network_id="example",
            link_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_customer_gateway_association, with
    customer gateway arn arn:aws:iam::123456789012:policy/ci, device
    id example, global network id example, link id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="device_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="link_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
