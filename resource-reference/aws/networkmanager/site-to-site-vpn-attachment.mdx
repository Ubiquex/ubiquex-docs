---
title: "aws_networkmanager_site_to_site_vpn_attachment"
description: "Real, generated bindings for `aws_networkmanager_site_to_site_vpn_attachment`."
---

`aws_networkmanager_site_to_site_vpn_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"

    cfg := networkmanager.SiteToSiteVpnAttachmentConfig{
        CoreNetworkId: "example",
        VpnConnectionArn: "example",
        RoutingPolicyLabel: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        networkmanager.SiteToSiteVpnAttachment,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SiteToSiteVpnAttachment } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/site_to_site_vpn_attachment";

    resource(SiteToSiteVpnAttachment, "example", {
      coreNetworkId: "example",
      vpnConnectionArn: "example",
      routingPolicyLabel: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import (
        SiteToSiteVpnAttachment,
        SiteToSiteVpnAttachmentConfig,
    )

    cfg = SiteToSiteVpnAttachmentConfig(
        core_network_id="example",
        vpn_connection_arn="example",
        routing_policy_label="example",
        tags={"key": "value"},
    )
    ubx.resource(SiteToSiteVpnAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_site_to_site_vpn_attachment with
core_network_id, vpn_connection_arn, routing_policy_label, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpn_connection_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
