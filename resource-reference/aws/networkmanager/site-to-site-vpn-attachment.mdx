---
title: "aws_networkmanager_site_to_site_vpn_attachment"
description: "Real, generated bindings for `aws_networkmanager_site_to_site_vpn_attachment`."
---

`aws_networkmanager_site_to_site_vpn_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own site to site vpn attachment"})

    		ubx.Resource(networkmanager.SiteToSiteVpnAttachment, "ci-runner", networkmanager.SiteToSiteVpnAttachmentConfig{
    			CoreNetworkId:      "example",
    			VpnConnectionArn:   "arn:aws:iam::123456789012:policy/ci",
    			RoutingPolicyLabel: "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SiteToSiteVpnAttachment } from "jsr:@ubx/sdk-aws/aws/networkmanager/site_to_site_vpn_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own site to site vpn attachment" });

      resource(SiteToSiteVpnAttachment, "ci-runner", {
        coreNetworkId: "example",
        vpnConnectionArn: "arn:aws:iam::123456789012:policy/ci",
        routingPolicyLabel: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import SiteToSiteVpnAttachment, SiteToSiteVpnAttachmentConfig

    def describe():
        ubx.intent("payments own site to site vpn attachment")

        ubx.resource(SiteToSiteVpnAttachment, "ci-runner", SiteToSiteVpnAttachmentConfig(
            core_network_id="example",
            vpn_connection_arn="arn:aws:iam::123456789012:policy/ci",
            routing_policy_label="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_site_to_site_vpn_attachment, with core
    network id example, vpn connection arn
    arn:aws:iam::123456789012:policy/ci, routing policy label
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpn_connection_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
