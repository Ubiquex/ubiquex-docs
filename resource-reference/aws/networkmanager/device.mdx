---
title: "aws_networkmanager_device"
description: "Real, generated bindings for `aws_networkmanager_device`."
---

`aws_networkmanager_device` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"

    cfg := networkmanager.DeviceConfig{
        GlobalNetworkId: "example",
        Description: "example",
        Model: "example",
    }
    ubx.Resource(networkmanager.Device, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Device } from
      "jsr:@ubx/sdk-aws/aws/networkmanager/device";

    resource(Device, "example", {
      globalNetworkId: "example",
      description: "example",
      model: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmanager import Device, DeviceConfig

    cfg = DeviceConfig(
        global_network_id="example",
        description="example",
        model="example",
    )
    ubx.resource(Device, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmanager_device with global_network_id,
description, model.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="model" type="string">
  Optional.
</ResponseField>

<ResponseField name="serial_number" type="string">
  Optional.
</ResponseField>

<ResponseField name="site_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

<ResponseField name="vendor" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
