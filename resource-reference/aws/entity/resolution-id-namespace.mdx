---
title: "aws_entity_resolution_id_namespace"
description: "`aws_entity_resolution_id_namespace` defines a real, distinct source of entity identifiers, one party's own customer ID space, for instance..."
---

`aws_entity_resolution_id_namespace` defines a real, distinct source of entity identifiers, one party's own customer ID space, for instance, that an `aws_entity_resolution_id_mapping_workflow` maps against another namespace's own identifiers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	entity "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/entity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resolution id namespace"})

    		ubx.Resource(entity.ResolutionIdNamespace, "example", entity.ResolutionIdNamespaceConfig{
    			IdNamespaceName:             "example-id-namespace-name",
    			Type:                        "example",
    			Description:                 "Managed by ubx.",
    			IdMappingWorkflowProperties: []map[string]any{{"id_mapping_type": "example"}},
    			InputSourceConfig:           []map[string]any{{"input_source_arn": "example"}},
    			RoleArn:                     "arn:aws:iam::123456789012:policy/example",
    			Tags:                        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResolutionIdNamespace } from "./local-sdk/aws/sdk/typescript/aws/entity/resolution_id_namespace.ts";

    export default stack("example", () => {
      intent({ summary: "example own resolution id namespace" });

      resource(ResolutionIdNamespace, "example", {
        idNamespaceName: "example-id-namespace-name",
        type: "example",
        description: "Managed by ubx.",
        idMappingWorkflowProperties: [{ idMappingType: "example" }],
        inputSourceConfig: [{ inputSourceArn: "example" }],
        roleArn: "arn:aws:iam::123456789012:policy/example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.entity import ResolutionIdNamespace, ResolutionIdNamespaceConfig

    def describe():
        ubx.intent("example own resolution id namespace")

        ubx.resource(ResolutionIdNamespace, "example", ResolutionIdNamespaceConfig(
            id_namespace_name="example-id-namespace-name",
            type="example",
            description="Managed by ubx.",
            id_mapping_workflow_properties=[{"id_mapping_type": "example"}],
            input_source_config=[{"input_source_arn": "example"}],
            role_arn="arn:aws:iam::123456789012:policy/example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_entity_resolution_id_namespace, with id namespace name
    example-id-namespace-name, type example, description Managed by
    ubx., id mapping workflow properties a set value, input source
    config a set value, role arn
    arn:aws:iam::123456789012:policy/example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  An optional free-form description that helps identify the purpose or contents of the ID namespace. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="id_mapping_workflow_properties" type="list(object)">
  A list of configurations for ID mapping workflows that define how identifiers are mapped within this namespace, where each item specifies a workflow type (e.g., ID_MAPPING) and its provider properties. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id_mapping_type" type="string">
      Specifies the cardinality of the ID mapping workflow: ONE_TO_ONE for a unique source-to-ID mapping or ONE_TO_MANY for one source record mapping to multiple IDs. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="provider_properties" type="object">
      Specifies the provider-specific configuration for an ID mapping workflow, including the provider configuration map and optional intermediate source S3 location. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="provider_configuration" type="any">
          Map of provider-specific configuration parameters passed to the entity resolution provider for this ID mapping workflow. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provider_service_arn" type="string">
          The ARN of the AWS Marketplace provider service used to perform the ID mapping workflow for this ID namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rule_based_properties" type="object">
      Configures the rule-based matching configuration for this ID namespace mapping workflow, including the attribute matching model and the list of matching rules that define how source and target attributes are compared. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attribute_matching_model" type="string">
          Specifies the matching model for rule-based ID mapping, determining whether a record is considered a match when all attributes in the rule match (ALL_MATCH) or when any single attribute matches (ANY_MATCH). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="record_matching_models" type="list(string)">
          Specifies the record matching models (e.g., ONE_TO_ONE, ONE_TO_MANY, MANY_TO_MANY) that define the mapping cardinality used by the rule-based matching logic in this ID mapping workflow property. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rule_definition_types" type="list(string)">
          Specifies the rule definition types (e.g., SOURCE or TARGET) used to categorize the matching rules in the rule-based ID mapping workflow for the ID namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rules" type="list(object)">
          Defines the list of source-to-target attribute mapping rules used by the rule-based ID mapping workflow, where each rule specifies a source attribute and the corresponding target attribute in the ID namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="matching_keys" type="list(string)">
              The list of record attribute names (columns) that are compared between source records to determine if they match under this rule in the rule-based mapping workflow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rule_name" type="string">
              The name that uniquely identifies this matching rule within the rule-based properties of the ID mapping workflow for the entity resolution ID namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id_namespace_name" type="string" required>
  Specifies the name of the ID namespace, which uniquely identifies it within the AWS account and Region. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="input_source_config" type="list(object)">
  Configures the input sources for the ID namespace, specifying each data source's ARN and schema to be used for entity resolution. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="input_source_arn" type="string">
      The Amazon Resource Name (ARN) of the input data source (such as an S3 bucket) from which the entity resolution ID namespace reads its records for matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="schema_name" type="string">
      Specifies the name of the schema that describes the structure of the input data, which AWS Entity Resolution uses to interpret the fields for record matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string">
  The ARN of the IAM role that AWS Entity Resolution assumes to read from the input sources configured for this ID namespace. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Assigns key-value metadata tags to this AWS Entity Resolution ID Namespace resource, enabling cost allocation, access control, and organization of the namespace. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag applied to the AWS Entity Resolution ID namespace, used for metadata organization and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Specifies whether the ID namespace is a SOURCE or TARGET type, determining its role in the entity resolution mapping workflow. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The date and time when the IdNamespace was created Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id_namespace_arn" type="string">
  The arn associated with the IdNamespace Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time when the IdNamespace was updated Computed by AWS after `ship`.
</ResponseField>
