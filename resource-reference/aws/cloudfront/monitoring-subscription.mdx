---
title: "aws_cloudfront_monitoring_subscription"
description: "Real, generated bindings for `aws_cloudfront_monitoring_subscription`."
---

`aws_cloudfront_monitoring_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.MonitoringSubscriptionConfig{
        DistributionId: "example",
    }
    ubx.Resource(cloudfront.MonitoringSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MonitoringSubscription } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/monitoring_subscription";

    resource(MonitoringSubscription, "example", {
      distributionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        MonitoringSubscription,
        MonitoringSubscriptionConfig,
    )

    cfg = MonitoringSubscriptionConfig(
        distribution_id="example",
    )
    ubx.resource(MonitoringSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_monitoring_subscription with
distribution_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="distribution_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
