---
title: "aws_cloudfront_monitoring_subscription"
description: "Real, generated bindings for `aws_cloudfront_monitoring_subscription`."
---

`aws_cloudfront_monitoring_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own monitoring subscription"})

    		ubx.Resource(cloudfront.MonitoringSubscription, "ci-runner", cloudfront.MonitoringSubscriptionConfig{
    			DistributionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MonitoringSubscription } from "jsr:@ubx/sdk-aws/aws/cloudfront/monitoring_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own monitoring subscription" });

      resource(MonitoringSubscription, "ci-runner", {
        distributionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import MonitoringSubscription, MonitoringSubscriptionConfig

    def describe():
        ubx.intent("payments own monitoring subscription")

        ubx.resource(MonitoringSubscription, "ci-runner", MonitoringSubscriptionConfig(
            distribution_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_monitoring_subscription, with distribution
    id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="distribution_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
