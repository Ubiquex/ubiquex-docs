---
title: "aws_cloudfront_public_key"
description: "Real, generated bindings for `aws_cloudfront_public_key`."
---

`aws_cloudfront_public_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.PublicKeyConfig{
        EncodedKey: "example",
        Comment: "example",
        Name: "example-name",
    }
    ubx.Resource(cloudfront.PublicKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PublicKey } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/public_key";

    resource(PublicKey, "example", {
      encodedKey: "example",
      comment: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import PublicKey, PublicKeyConfig

    cfg = PublicKeyConfig(
        encoded_key="example",
        comment="example",
        name="example",
    )
    ubx.resource(PublicKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_public_key with encoded_key, comment,
name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="encoded_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
