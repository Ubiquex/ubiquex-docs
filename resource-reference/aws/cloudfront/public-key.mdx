---
title: "aws_cloud_front_public_key"
description: "`aws_cloud_front_public_key` registers the real public half of a signing key pair with CloudFront..."
---

`aws_cloud_front_public_key` registers the real public half of a signing key pair with CloudFront, used inside an `aws_cloud_front_key_group` to validate signed URLs and signed cookies for private content.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own public key"})

    		ubx.Resource(cloudfront.PublicKey, "example", cloudfront.PublicKeyConfig{
    			PublicKeyConfig: map[string]any{"caller_reference": "example", "encoded_key": "example", "name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublicKey } from "@ubx/sdk-aws/aws/cloudfront/public_key";

    export default stack("example", () => {
      intent({ summary: "example own public key" });

      resource(PublicKey, "example", {
        publicKeyConfig: {
          callerReference: "example",
          encodedKey: "example",
          name: "example-name",
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import PublicKey, PublicKeyConfig
    from ubx.aws.cloudfront.public_key import PublicKey_PublicKeyConfig

    def describe():
        ubx.intent("example own public key")

        ubx.resource(PublicKey, "example", PublicKeyConfig(
            public_key_config=PublicKey_PublicKeyConfig(caller_reference="example", encoded_key="example", name="example-name"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloud_front_public_key, with public key config a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="public_key_config" type="object" required>
  Configuration information about a public key that you can use with [signed URLs and signed cookies](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html), or with [field-level encryption](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/field-level-encryption.html). CloudFront supports signed URLs and signed cookies with RSA 2048 or ECDSA 256 key signatures. Field-level encryption is only compatible with RSA 2048 key signatures. Required.
  <Expandable title="properties">
    <ResponseField name="caller_reference" type="string" required>
      A string included in the request to help make sure that the request can't be replayed. Required.
    </ResponseField>
    <ResponseField name="comment" type="string">
      A comment to describe the public key. The comment cannot be longer than 128 characters. Optional.
    </ResponseField>
    <ResponseField name="encoded_key" type="string" required>
      The public key that you can use with [signed URLs and signed cookies](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html), or with [field-level encryption](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/field-level-encryption.html). Required.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      A name to help identify the public key. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_time" type="string">
  The date and time when the CloudFront public key was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
