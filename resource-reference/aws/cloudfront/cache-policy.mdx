---
title: "aws_cloudfront_cache_policy"
description: "Real, generated bindings for `aws_cloudfront_cache_policy`."
---

`aws_cloudfront_cache_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cache policy"})

    		ubx.Resource(cloudfront.CachePolicy, "ci-runner", cloudfront.CachePolicyConfig{
    			Name:       "ci-runner",
    			Comment:    "example",
    			DefaultTtl: 1,
    			MaxTtl:     1,
    			MinTtl:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CachePolicy } from "jsr:@ubx/sdk-aws/aws/cloudfront/cache_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own cache policy" });

      resource(CachePolicy, "ci-runner", {
        name: "ci-runner",
        comment: "example",
        defaultTtl: 1,
        maxTtl: 1,
        minTtl: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import CachePolicy, CachePolicyConfig

    def describe():
        ubx.intent("payments own cache policy")

        ubx.resource(CachePolicy, "ci-runner", CachePolicyConfig(
            name="ci-runner",
            comment="example",
            default_ttl=1,
            max_ttl=1,
            min_ttl=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_cache_policy called "ci-runner", with
    comment example, default ttl 1, max ttl 1, min ttl 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="min_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
