---
title: "aws_cloudfront_cache_policy"
description: "Real, generated bindings for `aws_cloudfront_cache_policy`."
---

`aws_cloudfront_cache_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.CachePolicyConfig{
        Name: "example-name",
        Comment: "example",
        DefaultTtl: 1,
    }
    ubx.Resource(cloudfront.CachePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CachePolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/cache_policy";

    resource(CachePolicy, "example", {
      name: "example",
      comment: "example",
      defaultTtl: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import CachePolicy, CachePolicyConfig

    cfg = CachePolicyConfig(
        name="example",
        comment="example",
        default_ttl=1,
    )
    ubx.resource(CachePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_cache_policy with name, comment,
default_ttl.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="min_ttl" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
