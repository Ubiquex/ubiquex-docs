---
title: "aws_cloudfront_realtime_log_config"
description: "Real, generated bindings for `aws_cloudfront_realtime_log_config`."
---

`aws_cloudfront_realtime_log_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.RealtimeLogConfigConfig{
        Fields: []string{"example"},
        Name: "example-name",
        SamplingRate: 1,
    }
    ubx.Resource(cloudfront.RealtimeLogConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RealtimeLogConfig } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/realtime_log_config";

    resource(RealtimeLogConfig, "example", {
      fields: ["example"],
      name: "example",
      samplingRate: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        RealtimeLogConfig,
        RealtimeLogConfigConfig,
    )

    cfg = RealtimeLogConfigConfig(
        fields=["example"],
        name="example",
        sampling_rate=1,
    )
    ubx.resource(RealtimeLogConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_realtime_log_config with fields, name,
sampling_rate.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="fields" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="sampling_rate" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
