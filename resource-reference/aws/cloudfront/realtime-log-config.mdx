---
title: "aws_cloudfront_realtime_log_config"
description: "Real, generated bindings for `aws_cloudfront_realtime_log_config`."
---

`aws_cloudfront_realtime_log_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own realtime log config"})

    		ubx.Resource(cloudfront.RealtimeLogConfig, "ci-runner", cloudfront.RealtimeLogConfigConfig{
    			Fields:       []string{"example"},
    			Name:         "ci-runner",
    			SamplingRate: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RealtimeLogConfig } from "jsr:@ubx/sdk-aws/aws/cloudfront/realtime_log_config";

    export default stack("payments", () => {
      intent({ summary: "payments own realtime log config" });

      resource(RealtimeLogConfig, "ci-runner", {
        fields: ["example"],
        name: "ci-runner",
        samplingRate: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import RealtimeLogConfig, RealtimeLogConfigConfig

    def describe():
        ubx.intent("payments own realtime log config")

        ubx.resource(RealtimeLogConfig, "ci-runner", RealtimeLogConfigConfig(
            fields=["example"],
            name="ci-runner",
            sampling_rate=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_realtime_log_config called "ci-runner",
    with fields a set value, sampling rate 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="fields" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="sampling_rate" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
