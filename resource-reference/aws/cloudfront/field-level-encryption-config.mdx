---
title: "aws_cloudfront_field_level_encryption_config"
description: "Real, generated bindings for `aws_cloudfront_field_level_encryption_config`."
---

`aws_cloudfront_field_level_encryption_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.FieldLevelEncryptionConfigConfig{
        Comment: "example",
    }
    ubx.Resource(
        cloudfront.FieldLevelEncryptionConfig,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FieldLevelEncryptionConfig } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/field_level_encryption_config";

    resource(FieldLevelEncryptionConfig, "example", {
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        FieldLevelEncryptionConfig,
        FieldLevelEncryptionConfigConfig,
    )

    cfg = FieldLevelEncryptionConfigConfig(
        comment="example",
    )
    ubx.resource(FieldLevelEncryptionConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_field_level_encryption_config with
comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
