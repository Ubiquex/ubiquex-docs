---
title: "aws_cloudfront_distribution_tenant"
description: "Real, generated bindings for `aws_cloudfront_distribution_tenant`."
---

`aws_cloudfront_distribution_tenant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own distribution tenant"})

    		ubx.Resource(cloudfront.DistributionTenant, "ci-runner", cloudfront.DistributionTenantConfig{
    			DistributionId: "example",
    			Name:           "ci-runner",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DistributionTenant } from "jsr:@ubx/sdk-aws/aws/cloudfront/distribution_tenant";

    export default stack("payments", () => {
      intent({ summary: "payments own distribution tenant" });

      resource(DistributionTenant, "ci-runner", {
        distributionId: "example",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import DistributionTenant, DistributionTenantConfig

    def describe():
        ubx.intent("payments own distribution tenant")

        ubx.resource(DistributionTenant, "ci-runner", DistributionTenantConfig(
            distribution_id="example",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_distribution_tenant called "ci-runner",
    with distribution id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="distribution_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
