---
title: "aws_cloudfront_distribution_tenant"
description: "Real, generated bindings for `aws_cloudfront_distribution_tenant`."
---

`aws_cloudfront_distribution_tenant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.DistributionTenantConfig{
        DistributionId: "example",
        Name: "example-name",
        ConnectionGroupId: "example",
        Enabled: true,
    }
    ubx.Resource(cloudfront.DistributionTenant, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DistributionTenant } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/distribution_tenant";

    resource(DistributionTenant, "example", {
      distributionId: "example",
      name: "example",
      connectionGroupId: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        DistributionTenant,
        DistributionTenantConfig,
    )

    cfg = DistributionTenantConfig(
        distribution_id="example",
        name="example",
        connection_group_id="example",
        enabled=True,
    )
    ubx.resource(DistributionTenant, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_distribution_tenant with
distribution_id, name, connection_group_id, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="distribution_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
