---
title: "aws_cloudfront_key_group"
description: "Real, generated bindings for `aws_cloudfront_key_group`."
---

`aws_cloudfront_key_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.KeyGroupConfig{
        Items: []string{"example"},
        Name: "example-name",
        Comment: "example",
    }
    ubx.Resource(cloudfront.KeyGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KeyGroup } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/key_group";

    resource(KeyGroup, "example", {
      items: ["example"],
      name: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import KeyGroup, KeyGroupConfig

    cfg = KeyGroupConfig(
        items=["example"],
        name="example",
        comment="example",
    )
    ubx.resource(KeyGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_key_group with items, name, comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="items" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
