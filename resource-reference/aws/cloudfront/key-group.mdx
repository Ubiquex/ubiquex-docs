---
title: "aws_cloudfront_key_group"
description: "Real, generated bindings for `aws_cloudfront_key_group`."
---

`aws_cloudfront_key_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key group"})

    		ubx.Resource(cloudfront.KeyGroup, "ci-runner", cloudfront.KeyGroupConfig{
    			Items:   []string{"example"},
    			Name:    "ci-runner",
    			Comment: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyGroup } from "jsr:@ubx/sdk-aws/aws/cloudfront/key_group";

    export default stack("payments", () => {
      intent({ summary: "payments own key group" });

      resource(KeyGroup, "ci-runner", {
        items: ["example"],
        name: "ci-runner",
        comment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import KeyGroup, KeyGroupConfig

    def describe():
        ubx.intent("payments own key group")

        ubx.resource(KeyGroup, "ci-runner", KeyGroupConfig(
            items=["example"],
            name="ci-runner",
            comment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_key_group called "ci-runner", with items a
    set value, comment example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="items" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
