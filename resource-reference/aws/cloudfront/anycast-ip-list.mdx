---
title: "aws_cloudfront_anycast_ip_list"
description: "Real, generated bindings for `aws_cloudfront_anycast_ip_list`."
---

`aws_cloudfront_anycast_ip_list` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own anycast ip list"})

    		ubx.Resource(cloudfront.AnycastIpList, "ci-runner", cloudfront.AnycastIpListConfig{
    			IpCount: 1,
    			Name:    "ci-runner",
    			Tags:    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnycastIpList } from "jsr:@ubx/sdk-aws/aws/cloudfront/anycast_ip_list";

    export default stack("payments", () => {
      intent({ summary: "payments own anycast ip list" });

      resource(AnycastIpList, "ci-runner", {
        ipCount: 1,
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import AnycastIpList, AnycastIpListConfig

    def describe():
        ubx.intent("payments own anycast ip list")

        ubx.resource(AnycastIpList, "ci-runner", AnycastIpListConfig(
            ip_count=1,
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_anycast_ip_list called "ci-runner", with ip
    count 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="ip_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="anycast_ips" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
