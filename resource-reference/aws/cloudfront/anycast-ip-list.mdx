---
title: "aws_cloudfront_anycast_ip_list"
description: "Real, generated bindings for `aws_cloudfront_anycast_ip_list`."
---

`aws_cloudfront_anycast_ip_list` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.AnycastIpListConfig{
        IpCount: 1,
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudfront.AnycastIpList, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AnycastIpList } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/anycast_ip_list";

    resource(AnycastIpList, "example", {
      ipCount: 1,
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        AnycastIpList,
        AnycastIpListConfig,
    )

    cfg = AnycastIpListConfig(
        ip_count=1,
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(AnycastIpList, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_anycast_ip_list with ip_count, name,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="ip_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="anycast_ips" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
