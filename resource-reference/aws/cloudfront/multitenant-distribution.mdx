---
title: "aws_cloudfront_multitenant_distribution"
description: "Real, generated bindings for `aws_cloudfront_multitenant_distribution`."
---

`aws_cloudfront_multitenant_distribution` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.MultitenantDistributionConfig{
        Comment: "example",
        Enabled: true,
        DefaultRootObject: "example",
        HttpVersion: "example",
    }
    ubx.Resource(cloudfront.MultitenantDistribution, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MultitenantDistribution } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/multitenant_distribution";

    resource(MultitenantDistribution, "example", {
      comment: "example",
      enabled: true,
      defaultRootObject: "example",
      httpVersion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        MultitenantDistribution,
        MultitenantDistributionConfig,
    )

    cfg = MultitenantDistributionConfig(
        comment="example",
        enabled=True,
        default_root_object="example",
        http_version="example",
    )
    ubx.resource(MultitenantDistribution, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_multitenant_distribution with comment,
enabled, default_root_object, http_version.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_root_object" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="http_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="web_acl_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_mode" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="http_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="in_progress_invalidation_batches" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
