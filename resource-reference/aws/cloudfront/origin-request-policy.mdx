---
title: "aws_cloudfront_origin_request_policy"
description: "Real, generated bindings for `aws_cloudfront_origin_request_policy`."
---

`aws_cloudfront_origin_request_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.OriginRequestPolicyConfig{
        Name: "example-name",
        Comment: "example",
    }
    ubx.Resource(cloudfront.OriginRequestPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OriginRequestPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/origin_request_policy";

    resource(OriginRequestPolicy, "example", {
      name: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        OriginRequestPolicy,
        OriginRequestPolicyConfig,
    )

    cfg = OriginRequestPolicyConfig(
        name="example",
        comment="example",
    )
    ubx.resource(OriginRequestPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_origin_request_policy with name,
comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
