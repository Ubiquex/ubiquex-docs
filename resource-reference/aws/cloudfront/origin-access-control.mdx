---
title: "aws_cloudfront_origin_access_control"
description: "Real, generated bindings for `aws_cloudfront_origin_access_control`."
---

`aws_cloudfront_origin_access_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own origin access control"})

    		ubx.Resource(cloudfront.OriginAccessControl, "ci-runner", cloudfront.OriginAccessControlConfig{
    			Name:                          "ci-runner",
    			OriginAccessControlOriginType: "example",
    			SigningBehavior:               "example",
    			SigningProtocol:               "example",
    			Description:                   "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OriginAccessControl } from "jsr:@ubx/sdk-aws/aws/cloudfront/origin_access_control";

    export default stack("payments", () => {
      intent({ summary: "payments own origin access control" });

      resource(OriginAccessControl, "ci-runner", {
        name: "ci-runner",
        originAccessControlOriginType: "example",
        signingBehavior: "example",
        signingProtocol: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import OriginAccessControl, OriginAccessControlConfig

    def describe():
        ubx.intent("payments own origin access control")

        ubx.resource(OriginAccessControl, "ci-runner", OriginAccessControlConfig(
            name="ci-runner",
            origin_access_control_origin_type="example",
            signing_behavior="example",
            signing_protocol="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_origin_access_control called "ci-runner",
    with origin access control origin type example, signing behavior
    example, signing protocol example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="origin_access_control_origin_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="signing_behavior" type="string" required>
  Required.
</ResponseField>

<ResponseField name="signing_protocol" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
