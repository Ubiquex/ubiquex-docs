---
title: "aws_cloudfront_origin_access_control"
description: "Real, generated bindings for `aws_cloudfront_origin_access_control`."
---

`aws_cloudfront_origin_access_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.OriginAccessControlConfig{
        Name: "example-name",
        OriginAccessControlOriginType: "example",
        SigningBehavior: "example",
        SigningProtocol: "example",
        Description: "example",
    }
    ubx.Resource(cloudfront.OriginAccessControl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OriginAccessControl } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/origin_access_control";

    resource(OriginAccessControl, "example", {
      name: "example",
      originAccessControlOriginType: "example",
      signingBehavior: "example",
      signingProtocol: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        OriginAccessControl,
        OriginAccessControlConfig,
    )

    cfg = OriginAccessControlConfig(
        name="example",
        origin_access_control_origin_type="example",
        signing_behavior="example",
        signing_protocol="example",
        description="example",
    )
    ubx.resource(OriginAccessControl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_origin_access_control with name,
origin_access_control_origin_type, signing_behavior,
signing_protocol, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="origin_access_control_origin_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="signing_behavior" type="string" required>
  Required.
</ResponseField>

<ResponseField name="signing_protocol" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
