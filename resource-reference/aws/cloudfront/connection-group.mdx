---
title: "aws_cloudfront_connection_group"
description: "Real, generated bindings for `aws_cloudfront_connection_group`."
---

`aws_cloudfront_connection_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.ConnectionGroupConfig{
        Name: "example-name",
        AnycastIpListId: "example",
        Enabled: true,
    }
    ubx.Resource(cloudfront.ConnectionGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionGroup } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/connection_group";

    resource(ConnectionGroup, "example", {
      name: "example",
      anycastIpListId: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        ConnectionGroup,
        ConnectionGroupConfig,
    )

    cfg = ConnectionGroupConfig(
        name="example",
        anycast_ip_list_id="example",
        enabled=True,
    )
    ubx.resource(ConnectionGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_connection_group with name,
anycast_ip_list_id, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="anycast_ip_list_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipv6_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="routing_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="wait_for_deployment" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
