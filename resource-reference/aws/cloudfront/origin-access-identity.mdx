---
title: "aws_cloudfront_origin_access_identity"
description: "Real, generated bindings for `aws_cloudfront_origin_access_identity`."
---

`aws_cloudfront_origin_access_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.OriginAccessIdentityConfig{
        Comment: "example",
    }
    ubx.Resource(cloudfront.OriginAccessIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OriginAccessIdentity } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/origin_access_identity";

    resource(OriginAccessIdentity, "example", {
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        OriginAccessIdentity,
        OriginAccessIdentityConfig,
    )

    cfg = OriginAccessIdentityConfig(
        comment="example",
    )
    ubx.resource(OriginAccessIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_origin_access_identity with comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloudfront_access_identity_path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="iam_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_canonical_user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
