---
title: "aws_cloudfront_response_headers_policy"
description: "Real, generated bindings for `aws_cloudfront_response_headers_policy`."
---

`aws_cloudfront_response_headers_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.ResponseHeadersPolicyConfig{
        Name: "example-name",
        Comment: "example",
    }
    ubx.Resource(cloudfront.ResponseHeadersPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResponseHeadersPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/response_headers_policy";

    resource(ResponseHeadersPolicy, "example", {
      name: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        ResponseHeadersPolicy,
        ResponseHeadersPolicyConfig,
    )

    cfg = ResponseHeadersPolicyConfig(
        name="example",
        comment="example",
    )
    ubx.resource(ResponseHeadersPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_response_headers_policy with name,
comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
