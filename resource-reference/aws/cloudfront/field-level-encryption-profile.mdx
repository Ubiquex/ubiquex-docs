---
title: "aws_cloudfront_field_level_encryption_profile"
description: "Real, generated bindings for `aws_cloudfront_field_level_encryption_profile`."
---

`aws_cloudfront_field_level_encryption_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.FieldLevelEncryptionProfileConfig{
        Name: "example-name",
        Comment: "example",
    }
    ubx.Resource(
        cloudfront.FieldLevelEncryptionProfile,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FieldLevelEncryptionProfile } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/field_level_encryption_profile";

    resource(FieldLevelEncryptionProfile, "example", {
      name: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        FieldLevelEncryptionProfile,
        FieldLevelEncryptionProfileConfig,
    )

    cfg = FieldLevelEncryptionProfileConfig(
        name="example",
        comment="example",
    )
    ubx.resource(FieldLevelEncryptionProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_field_level_encryption_profile with
name, comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
