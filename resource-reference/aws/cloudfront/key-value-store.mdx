---
title: "aws_cloudfront_key_value_store"
description: "Real, generated bindings for `aws_cloudfront_key_value_store`."
---

`aws_cloudfront_key_value_store` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.KeyValueStoreConfig{
        Name: "example-name",
        Comment: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudfront.KeyValueStore, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KeyValueStore } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/key_value_store";

    resource(KeyValueStore, "example", {
      name: "example",
      comment: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        KeyValueStore,
        KeyValueStoreConfig,
    )

    cfg = KeyValueStoreConfig(
        name="example",
        comment="example",
        tags={"key": "value"},
    )
    ubx.resource(KeyValueStore, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_key_value_store with name, comment,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
