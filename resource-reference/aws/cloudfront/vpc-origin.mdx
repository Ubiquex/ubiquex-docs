---
title: "aws_cloudfront_vpc_origin"
description: "Real, generated bindings for `aws_cloudfront_vpc_origin`."
---

`aws_cloudfront_vpc_origin` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"

    cfg := cloudfront.VpcOriginConfig{
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudfront.VpcOrigin, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcOrigin } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/vpc_origin";

    resource(VpcOrigin, "example", {
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import VpcOrigin, VpcOriginConfig

    cfg = VpcOriginConfig(
        tags={"key": "value"},
    )
    ubx.resource(VpcOrigin, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_vpc_origin with tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
