---
title: "aws_cloudfront_vpc_origin"
description: "Real, generated bindings for `aws_cloudfront_vpc_origin`."
---

`aws_cloudfront_vpc_origin` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc origin"})

    		ubx.Resource(cloudfront.VpcOrigin, "ci-runner", cloudfront.VpcOriginConfig{
    			Tags: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcOrigin } from "jsr:@ubx/sdk-aws/aws/cloudfront/vpc_origin";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc origin" });

      resource(VpcOrigin, "ci-runner", {
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import VpcOrigin, VpcOriginConfig

    def describe():
        ubx.intent("payments own vpc origin")

        ubx.resource(VpcOrigin, "ci-runner", VpcOriginConfig(
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_vpc_origin, with tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
