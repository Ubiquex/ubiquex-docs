---
title: "aws_cloudfront_continuous_deployment_policy"
description: "Real, generated bindings for `aws_cloudfront_continuous_deployment_policy`."
---

`aws_cloudfront_continuous_deployment_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.ContinuousDeploymentPolicyConfig{
        Enabled: true,
    }
    ubx.Resource(
        cloudfront.ContinuousDeploymentPolicy,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContinuousDeploymentPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/continuous_deployment_policy";

    resource(ContinuousDeploymentPolicy, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        ContinuousDeploymentPolicy,
        ContinuousDeploymentPolicyConfig,
    )

    cfg = ContinuousDeploymentPolicyConfig(
        enabled=True,
    )
    ubx.resource(ContinuousDeploymentPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_continuous_deployment_policy with
enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
