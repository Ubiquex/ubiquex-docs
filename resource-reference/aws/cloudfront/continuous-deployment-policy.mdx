---
title: "aws_cloudfront_continuous_deployment_policy"
description: "Real, generated bindings for `aws_cloudfront_continuous_deployment_policy`."
---

`aws_cloudfront_continuous_deployment_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own continuous deployment policy"})

    		ubx.Resource(cloudfront.ContinuousDeploymentPolicy, "ci-runner", cloudfront.ContinuousDeploymentPolicyConfig{
    			Enabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContinuousDeploymentPolicy } from "jsr:@ubx/sdk-aws/aws/cloudfront/continuous_deployment_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own continuous deployment policy" });

      resource(ContinuousDeploymentPolicy, "ci-runner", {
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import ContinuousDeploymentPolicy, ContinuousDeploymentPolicyConfig

    def describe():
        ubx.intent("payments own continuous deployment policy")

        ubx.resource(ContinuousDeploymentPolicy, "ci-runner", ContinuousDeploymentPolicyConfig(
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_continuous_deployment_policy, with enabled
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
