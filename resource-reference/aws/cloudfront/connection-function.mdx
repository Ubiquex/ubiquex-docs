---
title: "aws_cloudfront_connection_function"
description: "Real, generated bindings for `aws_cloudfront_connection_function`."
---

`aws_cloudfront_connection_function` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.ConnectionFunctionConfig{
        ConnectionFunctionCode: "example",
        Name: "example-name",
        Publish: true,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudfront.ConnectionFunction, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionFunction } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/connection_function";

    resource(ConnectionFunction, "example", {
      connectionFunctionCode: "example",
      name: "example",
      publish: true,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import (
        ConnectionFunction,
        ConnectionFunctionConfig,
    )

    cfg = ConnectionFunctionConfig(
        connection_function_code="example",
        name="example",
        publish=True,
        tags={"key": "value"},
    )
    ubx.resource(ConnectionFunction, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_connection_function with
connection_function_code, name, publish, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_function_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="publish" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="connection_function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="live_stage_etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="publish" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
