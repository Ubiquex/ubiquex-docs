---
title: "aws_cloudfront_connection_function"
description: "Real, generated bindings for `aws_cloudfront_connection_function`."
---

`aws_cloudfront_connection_function` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connection function"})

    		ubx.Resource(cloudfront.ConnectionFunction, "ci-runner", cloudfront.ConnectionFunctionConfig{
    			ConnectionFunctionCode: "example",
    			Name:                   "ci-runner",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectionFunction } from "jsr:@ubx/sdk-aws/aws/cloudfront/connection_function";

    export default stack("payments", () => {
      intent({ summary: "payments own connection function" });

      resource(ConnectionFunction, "ci-runner", {
        connectionFunctionCode: "example",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import ConnectionFunction, ConnectionFunctionConfig

    def describe():
        ubx.intent("payments own connection function")

        ubx.resource(ConnectionFunction, "ci-runner", ConnectionFunctionConfig(
            connection_function_code="example",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_connection_function called "ci-runner",
    with connection function code example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_function_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="publish" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="connection_function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="live_stage_etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="publish" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
