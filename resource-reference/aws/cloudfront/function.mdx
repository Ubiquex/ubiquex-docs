---
title: "aws_cloudfront_function"
description: "Real, generated bindings for `aws_cloudfront_function`."
---

`aws_cloudfront_function` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudfront"

    cfg := cloudfront.FunctionConfig{
        Code: "example",
        Name: "example-name",
        Runtime: "example",
        Comment: "example",
        KeyValueStoreAssociations: []string{"example"},
    }
    ubx.Resource(cloudfront.Function, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Function } from
      "jsr:@ubx/sdk-aws/aws/cloudfront/function";

    resource(Function, "example", {
      code: "example",
      name: "example",
      runtime: "example",
      comment: "example",
      keyValueStoreAssociations: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfront import Function, FunctionConfig

    cfg = FunctionConfig(
        code="example",
        name="example",
        runtime="example",
        comment="example",
        key_value_store_associations=["example"],
    )
    ubx.resource(Function, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfront_function with code, name, runtime,
comment, key_value_store_associations.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_value_store_associations" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="publish" type="bool">
  Optional.
</ResponseField>

<ResponseField name="runtime" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="live_stage_etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
