---
title: "aws_security_hub_policy_association"
description: "`aws_security_hub_policy_association` applies a real `aws_security_hub_configuration_policy` to a specific account, OU, or the whole organization..."
---

`aws_security_hub_policy_association` applies a real `aws_security_hub_configuration_policy` to a specific account, OU, or the whole organization, the resource that actually turns the policy's centrally defined standards and controls on for that target.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub policy association"})

    		ubx.Resource(security.HubPolicyAssociation, "example", security.HubPolicyAssociationConfig{
    			ConfigurationPolicyId: "example",
    			TargetId:              "example",
    			TargetType:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubPolicyAssociation } from "@ubx/sdk-aws/aws/security/hub_policy_association";

    export default stack("example", () => {
      intent({ summary: "example own hub policy association" });

      resource(HubPolicyAssociation, "example", {
        configurationPolicyId: "example",
        targetId: "example",
        targetType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.security import HubPolicyAssociation, HubPolicyAssociationConfig

    def describe():
        ubx.intent("example own hub policy association")

        ubx.resource(HubPolicyAssociation, "example", HubPolicyAssociationConfig(
            configuration_policy_id="example",
            target_id="example",
            target_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_policy_association, with configuration
    policy id example, target id example, target type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration_policy_id" type="string" required>
  The universally unique identifier (UUID) of the configuration policy or a value of SELF_MANAGED_SECURITY_HUB for a self-managed configuration Required.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  The identifier of the target account, organizational unit, or the root Required.
</ResponseField>

<ResponseField name="target_type" type="string" required>
  Indicates whether the target is an AWS account, organizational unit, or the organization root Required.
</ResponseField>

## Output properties

<ResponseField name="association_identifier" type="string">
  A unique identifier to indicates if the target has an association Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="association_status" type="string">
  The current status of the association between the specified target and the configuration Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="association_status_message" type="string">
  An explanation for a FAILED value for AssociationStatus Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="association_type" type="string">
  Indicates whether the association between the specified target and the configuration was directly applied by the Security Hub delegated administrator or inherited from a parent Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time, in UTC and ISO 8601 format, that the configuration policy association was last updated Computed by AWS after `ship`.
</ResponseField>
