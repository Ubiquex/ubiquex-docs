---
title: "aws_security_hub_finding_aggregator"
description: "`aws_security_hub_finding_aggregator` configures real cross-Region aggregation of findings into a single Region under the original Security Hub model..."
---

`aws_security_hub_finding_aggregator` configures real cross-Region aggregation of findings into a single Region under the original Security Hub model, so findings from every enabled Region can be viewed and acted on from one place.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub finding aggregator"})

    		ubx.Resource(security.HubFindingAggregator, "ci-runner", security.HubFindingAggregatorConfig{
    			RegionLinkingMode: "example",
    			Regions:           []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubFindingAggregator } from "./local-sdk/aws/sdk/typescript/aws/security/hub_finding_aggregator.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own hub finding aggregator" });

      resource(HubFindingAggregator, "ci-runner", {
        regionLinkingMode: "example",
        regions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubFindingAggregator, HubFindingAggregatorConfig

    def describe():
        ubx.intent("payments own hub finding aggregator")

        ubx.resource(HubFindingAggregator, "ci-runner", HubFindingAggregatorConfig(
            region_linking_mode="example",
            regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_finding_aggregator, with region linking
    mode example, regions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region_linking_mode" type="string" required>
  Indicates whether to aggregate findings from all of the available Regions in the current partition. Also determines whether to automatically aggregate findings from new Regions as Security Hub supports them and you opt into them. The selected option also determines how to use the Regions provided in the Regions list. In CFN, the options for this property are as follows: + ``ALL_REGIONS`` - Indicates to aggregate findings from all of the Regions where Security Hub is enabled. When you choose this option, Security Hub also automatically aggregates findings from new Regions as Security Hub supports them and you opt into them. + ``ALL_REGIONS_EXCEPT_SPECIFIED`` - Indicates to aggregate findings from all of the Regions where Security Hub is enabled, except for the Regions listed in the ``Regions`` parameter. When you choose this option, Security Hub also automatically aggregates findings from new Regions as Security Hub supports them and you opt into them. + ``SPECIFIED_REGIONS`` - Indicates to aggregate findings only from the Regions listed in the ``Regions`` parameter. Security Hub does not automatically aggregate findings from new Regions. Required.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  If ``RegionLinkingMode`` is ``ALL_REGIONS_EXCEPT_SPECIFIED``, then this is a space-separated list of Regions that do not aggregate findings to the aggregation Region. If ``RegionLinkingMode`` is ``SPECIFIED_REGIONS``, then this is a space-separated list of Regions that do aggregate findings to the aggregation Region. Optional.
</ResponseField>

## Output properties

<ResponseField name="finding_aggregation_region" type="string">
  The AWS Region where the finding aggregator is created and where findings from linked Regions are aggregated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="finding_aggregator_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the finding aggregator. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
