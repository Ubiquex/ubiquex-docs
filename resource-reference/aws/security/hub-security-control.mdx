---
title: "aws_security_hub_security_control"
description: "`aws_security_hub_security_control` enables or disables a real, individual security control, and can override its default severity..."
---

`aws_security_hub_security_control` enables or disables a real, individual security control, and can override its default severity, within an `aws_security_hub_standard`, letting a specific check be turned off account-wide without disabling the whole standard.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub security control"})

    		ubx.Resource(security.HubSecurityControl, "ci-runner", security.HubSecurityControlConfig{
    			Parameters:         "example",
    			LastUpdateReason:   "example",
    			SecurityControlArn: "arn:aws:iam::123456789012:policy/ci",
    			SecurityControlId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubSecurityControl } from "./local-sdk/aws/sdk/typescript/aws/security/hub_security_control.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own hub security control" });

      resource(HubSecurityControl, "ci-runner", {
        parameters: "example",
        lastUpdateReason: "example",
        securityControlArn: "arn:aws:iam::123456789012:policy/ci",
        securityControlId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubSecurityControl, HubSecurityControlConfig

    def describe():
        ubx.intent("payments own hub security control")

        ubx.resource(HubSecurityControl, "ci-runner", HubSecurityControlConfig(
            parameters="example",
            last_update_reason="example",
            security_control_arn="arn:aws:iam::123456789012:policy/ci",
            security_control_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_security_control, with parameters
    example, last update reason example, security control arn
    arn:aws:iam::123456789012:policy/ci, security control id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="last_update_reason" type="string">
  The most recent reason for updating the customizable properties of a security control. This differs from the UpdateReason field of the BatchUpdateStandardsControlAssociations API, which tracks the reason for updating the enablement status of a control. This field accepts alphanumeric characters in addition to white spaces, dashes, and underscores. Optional.
</ResponseField>

<ResponseField name="parameters" type="any" required>
  A map of parameter names to their configurations (with value type and actual value) that customize the behavior of the security control. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="security_control_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Security Hub security control to which this resource's settings (such as parameters or enablement) apply. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="security_control_id" type="string">
  The unique identifier (e.g., 'ACM.1') of the AWS Security Hub security control to configure, which targets that control for parameter updates. Optional. **(AI-inferred)**
</ResponseField>
