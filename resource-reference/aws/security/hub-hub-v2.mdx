---
title: "aws_security_hub_hub_v2"
description: "`aws_security_hub_hub_v2` enables Security Hub's next-generation (HubV2) model in an account and Region..."
---

`aws_security_hub_hub_v2` enables Security Hub's next-generation (HubV2) model in an account and Region, the resource behind its December 2025 relaunch delivering near-real-time risk analytics and unified findings across GuardDuty, Inspector, Macie, and Security Hub CSPM, up to one year of historical trend data, and the V2 counterpart to the original `aws_security_hub_hub`. Only one HubV2 resource can exist per account per Region.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub hub v2"})

    		ubx.Resource(security.HubHubV2, "example", security.HubHubV2Config{
    			Tags: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubHubV2 } from "./local-sdk/aws/sdk/typescript/aws/security/hub_hub_v2.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub hub v2" });

      resource(HubHubV2, "example", {
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubHubV2, HubHubV2Config

    def describe():
        ubx.intent("example own hub hub v2")

        ubx.resource(HubHubV2, "example", HubHubV2Config(
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_hub_v2, with tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="any">
  A key-value pair to associate with the Security Hub V2 resource. You can specify a key that is 1 to 128 Unicode characters in length and cannot be prefixed with aws:. You can use any of the following characters: the set of Unicode letters, digits, whitespace, _, ., /, =, +, and -. Optional.
</ResponseField>

## Output properties

<ResponseField name="hub_v2_arn" type="string">
  The Amazon Resource Name of the Security Hub V2 resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subscribed_at" type="string">
  The ISO 8601 timestamp indicating when the Security Hub hub was subscribed (enabled) in the account, returned as a computed string. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
