---
title: "aws_security_hub_delegated_admin"
description: "`aws_security_hub_delegated_admin` designates a real member account of an AWS Organization as the Security Hub delegated administrator..."
---

`aws_security_hub_delegated_admin` designates a real member account of an AWS Organization as the Security Hub delegated administrator, letting that account manage Security Hub across the whole organization instead of requiring the management account itself to do so.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub delegated admin"})

    		ubx.Resource(security.HubDelegatedAdmin, "example", security.HubDelegatedAdminConfig{
    			AdminAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubDelegatedAdmin } from "./local-sdk/aws/sdk/typescript/aws/security/hub_delegated_admin.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub delegated admin" });

      resource(HubDelegatedAdmin, "example", {
        adminAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubDelegatedAdmin, HubDelegatedAdminConfig

    def describe():
        ubx.intent("example own hub delegated admin")

        ubx.resource(HubDelegatedAdmin, "example", HubDelegatedAdminConfig(
            admin_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_delegated_admin, with admin account id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="admin_account_id" type="string" required>
  The AWS-account identifier of the account to designate as the Security Hub CSPM administrator account. Required.
</ResponseField>

## Output properties

<ResponseField name="delegated_admin_identifier" type="string">
  The AWS account identifier of the delegated Security Hub administrator. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  The current delegation status of the Security Hub delegated administrator account, typically ENABLED or DISABLED. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
