---
title: "aws_security_group_rule"
description: "Real, generated bindings for `aws_security_group_rule`."
---

`aws_security_group_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group rule"})

    		ubx.Resource(security.GroupRule, "ci-runner", security.GroupRuleConfig{
    			FromPort:        1,
    			Protocol:        "example",
    			SecurityGroupId: "example",
    			ToPort:          1,
    			Type:            "example",
    			CidrBlocks:      []string{"example"},
    			Description:     "Managed by ubx.",
    			Ipv6CidrBlocks:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupRule } from "jsr:@ubx/sdk-aws/aws/security/group_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own group rule" });

      resource(GroupRule, "ci-runner", {
        fromPort: 1,
        protocol: "example",
        securityGroupId: "example",
        toPort: 1,
        type: "example",
        cidrBlocks: ["example"],
        description: "Managed by ubx.",
        ipv6CidrBlocks: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.security import GroupRule, GroupRuleConfig

    def describe():
        ubx.intent("payments own group rule")

        ubx.resource(GroupRule, "ci-runner", GroupRuleConfig(
            from_port=1,
            protocol="example",
            security_group_id="example",
            to_port=1,
            type="example",
            cidr_blocks=["example"],
            description="Managed by ubx.",
            ipv6_cidr_blocks=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_group_rule, with from port 1, protocol
    example, security group id example, to port 1, type example, cidr
    blocks a set value, description Managed by ubx., ipv6 cidr blocks
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_blocks" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="from_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_blocks" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="prefix_list_ids" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="self" type="bool">
  Optional.
</ResponseField>

<ResponseField name="source_security_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="to_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_rule_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_security_group_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
