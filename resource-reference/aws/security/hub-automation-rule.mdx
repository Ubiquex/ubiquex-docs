---
title: "aws_security_hub_automation_rule"
description: "`aws_security_hub_automation_rule` defines a real rule that automatically updates matching findings, suppressing, reprioritizing, adding notes..."
---

`aws_security_hub_automation_rule` defines a real rule that automatically updates matching findings, suppressing, reprioritizing, adding notes, in the original Security Hub model, without a human triaging each one individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub automation rule"})

    		ubx.Resource(security.HubAutomationRule, "example", security.HubAutomationRuleConfig{
    			Actions:     []map[string]any{{"finding_fields_update": map[string]any{"confidence": 1}}},
    			Criteria:    map[string]any{"aws_account_id": []map[string]any{{"comparison": "example"}}},
    			Description: "Managed by ubx.",
    			RuleName:    "example-rule-name",
    			RuleOrder:   1,
    			IsTerminal:  true,
    			RuleStatus:  "example",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubAutomationRule } from "./local-sdk/aws/sdk/typescript/aws/security/hub_automation_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub automation rule" });

      resource(HubAutomationRule, "example", {
        actions: [{ findingFieldsUpdate: { confidence: 1 } }],
        criteria: { awsAccountId: [{ comparison: "example" }] },
        description: "Managed by ubx.",
        ruleName: "example-rule-name",
        ruleOrder: 1,
        isTerminal: true,
        ruleStatus: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubAutomationRule, HubAutomationRuleConfig

    def describe():
        ubx.intent("example own hub automation rule")

        ubx.resource(HubAutomationRule, "example", HubAutomationRuleConfig(
            actions=[{"finding_fields_update": {"confidence": 1}}],
            criteria={"aws_account_id": [{"comparison": "example"}]},
            description="Managed by ubx.",
            rule_name="example-rule-name",
            rule_order=1,
            is_terminal=True,
            rule_status="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_automation_rule, with actions a set
    value, criteria a set value, description Managed by ubx., rule
    name example-rule-name, rule order 1, is terminal true, rule
    status example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="list(object)" required>
  One or more actions to update finding fields if a finding matches the conditions specified in ``Criteria``. Required.
  <Expandable title="properties">
    <ResponseField name="finding_fields_update" type="object">
      Defines the updates to apply to a finding's fields (such as severity, workflow status, or other configurable attributes) when this Security Hub automation rule action is executed. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="confidence" type="number">
          Sets the finding confidence score (0 -- 100) to the specified value when the automation rule action is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="criticality" type="number">
          Sets the criticality value (0 -- 100) assigned to the finding when the automation rule's finding fields update action is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="note" type="object">
          The note to add to findings when the automation rule triggers a finding-fields update, including the note text and the updated-by principal. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="text" type="string">
              The text of the note that will be applied to the finding when this automation rule action updates the finding's note. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="updated_by" type="string">
              Specifies the AWS principal (IAM user or role) that is recorded as the updater of the note when a Security Hub automation rule updates a finding's note. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="related_findings" type="list(object)">
          Specifies the new list of related findings (each with product ARN and finding ID) to assign to the finding, overwriting any existing related findings when the automation rule performs the finding-field update. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The unique identifier of a related finding to associate with the finding when the automation rule updates the finding's related findings list. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="product_arn" type="string">
              The ARN of the AWS Security Hub product (e.g., an AWS service or partner solution) that generated the related finding to be attached when the automation rule updates the finding's related findings list. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="severity" type="object">
          The severity object in the finding fields update specifies the new severity label and product severity score to be assigned to matching findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="label" type="string">
              Specifies the new severity label (e.g., INFORMATIONAL, LOW, MEDIUM, HIGH, CRITICAL) to apply to a finding when this automation rule action updates finding fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="normalized" type="number">
              Specifies the normalized severity value (0-100) to assign to the finding's severity when the automation rule updates the finding fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="product" type="number">
              Defines the numeric severity value (0 to 100) to assign to the finding's product severity field when a Security Hub automation rule updates a finding's severity. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="types" type="list(string)">
          Specifies the list of finding types to set on the finding when this automation rule action updates the finding fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_defined_fields" type="any">
          Specifies the custom key-value pairs to set as the finding's user-defined fields when this automation rule action updates the finding. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="verification_state" type="string">
          When a finding matches this automation rule, this value sets the finding's verification state to the specified enum value (UNKNOWN, TRUE_POSITIVE, FALSE_POSITIVE, or BENIGN_POSITIVE). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="workflow" type="object">
          Specifies the workflow status (e.g., NEW, NOTIFIED, SUPPRESSED, RESOLVED) to apply to findings when this automation rule action is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="status" type="string">
              Specifies the workflow status to apply to the finding when the automation rule triggers, with allowed values such as NEW, NOTIFIED, SUPPRESSED, and RESOLVED. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Defines the type of automation rule action, which determines whether the rule performs a finding update (like changing workflow status or severity) or sends a notification to a chat or custom action. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="criteria" type="object" required>
  The criteria that determine which findings a rule applies to. Required.
  <Expandable title="properties">
    <ResponseField name="aws_account_id" type="list(object)">
      The AWS-account ID in which a finding was generated. Array Members: Minimum number of 1 item. Maximum number of 100 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Determines the string matching operator (e.g., EQUALS, PREFIX, SUFFIX, NOT_EQUALS) used to compare the finding's AWS account ID against the value specified in the automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The exact AWS account ID that a finding's AwsAccountId field must equal for the automation rule to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="company_name" type="list(object)">
      The name of the company for the product that generated the finding. For control-based findings, the company is AWS. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator (e.g., EQUALS or PREFIX) that determines how the finding's company name is matched against the configured value in this automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value that the automation rule compares against the 'Company' field of a Security Hub finding, which identifies the provider or vendor that generated the finding. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compliance_associated_standards_id" type="list(object)">
      The unique identifier of a standard in which a control is enabled. This field consists of the resource portion of the Amazon Resource Name (ARN) returned for a standard in the [DescribeStandards](https://docs.aws.amazon.com/securityhub/1.0/APIReference/API_DescribeStandards.html) API response. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS, PREFIX, NOT_EQUALS, or PREFIX_NOT_EQUALS) used to match a finding's compliance associated standards ID (e.g., 'standards/aws-foundational-security-best-practices/v/1.0.0') in an automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the ARN of a Security Hub standard (e.g., arn:aws:securityhub:us-east-1::standards/aws-foundational-security-best-practices) that the automation rule uses to filter findings by their associated compliance standards. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compliance_security_control_id" type="list(object)">
      The security control ID for which a finding was generated. Security control IDs are the same across standards. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator used to match the finding's compliance security control ID against the supplied value, such as 'EQUALS' or 'PREFIX'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the security control ID (e.g., 'EC2.1') that must match the finding's compliance.securityControlId for the automation rule criteria to be satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="compliance_status" type="list(object)">
      The result of a security check. This field is only used for findings generated from controls. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (e.g., EQUALS) used to match a finding's compliance status against the specified value in the criteria of a Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The compliance status value (such as FAILED, PASSED, WARNING, or NOT_AVAILABLE) that a finding must match to trigger the automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="confidence" type="list(object)">
      The likelihood that a finding accurately identifies the behavior or issue that it was intended to identify. ``Confidence`` is scored on a 0 -- 100 basis using a ratio scale. A value of ``0`` means 0 percent confidence, and a value of ``100`` means 100 percent confidence. For example, a data exfiltration detection based on a statistical deviation of network traffic has low confidence because an actual exfiltration hasn't been verified. For more information, see [Confidence](https://docs.aws.amazon.com/securityhub/latest/userguide/asff-top-level-attributes.html#asff-confidence) in the *User Guide*. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="eq" type="number">
          The confidence score (0-100) that a finding's Confidence attribute must equal for the automation rule's condition to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gte" type="number">
          Matches findings where the confidence score (0-100) is greater than or equal to the specified numeric value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lte" type="number">
          Specifies the upper bound (less than or equal to) for the confidence score of a Security Hub finding that will trigger this automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="list(object)">
      A timestamp that indicates when this finding record was created. For more information about the validation and formatting of timestamp fields in ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="date_range" type="object">
          Configures the relative date range (e.g., last 7 days) used by the automation rule to match findings based on their 'CreatedAt' timestamp. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unit" type="string">
              Specifies the time unit (e.g., DAYS or HOURS) used to interpret the value of the relative date range for the CreatedAt criterion, enabling filters such as 'last 7 days' or 'last 24 hours'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The numeric value that, combined with the associated unit (e.g., DAYS), sets the relative date range for matching a finding's createdAt timestamp in the automation rule, such as 7 for the last 7 days. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="end" type="string">
          The end of the timestamp range for the `created_at` criterion, matching findings created on or before this ISO 8601 date and time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The start of the ISO 8601 timestamp range used to match findings whose creation time (createdAt) is at or after this value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="criticality" type="list(object)">
      The level of importance that is assigned to the resources that are associated with a finding. ``Criticality`` is scored on a 0 -- 100 basis, using a ratio scale that supports only full integers. A score of ``0`` means that the underlying resources have no criticality, and a score of ``100`` is reserved for the most critical resources. For more information, see [Criticality](https://docs.aws.amazon.com/securityhub/latest/userguide/asff-top-level-attributes.html#asff-criticality) in the *User Guide*. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="eq" type="number">
          Specifies the exact criticality value (0-100) that a finding must match for the automation rule's criticality criterion to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gte" type="number">
          The minimum criticality (0 -- 100) that a finding's criticality attribute must have for the automation rule's criteria to match, using a greater-than-or-equal comparison. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lte" type="number">
          Specifies the upper bound (less than or equal to) for the finding's criticality value, so that findings with a criticality score at or below this number match the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="list(object)">
      A finding's description. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Sets the comparison operator (e.g., EQUALS or PREFIX) used to match a finding's description against the specified value in the automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The string value that Security Hub compares against a finding's description field to determine whether the automation rule applies, based on the specified comparison operator. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="first_observed_at" type="list(object)">
      A timestamp that indicates when the potential security issue captured by a finding was first observed by the security findings product. For more information about the validation and formatting of timestamp fields in ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="date_range" type="object">
          Defines a relative date range (with a unit and value) that filters automation rules to findings whose first observed timestamp falls within a specified number of days ago, such as the last 7 days. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unit" type="string">
              Specifies the time unit (e.g., DAYS) for the date range used to filter findings by their first observed timestamp in an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The numeric quantity (e.g., number of days) that, combined with the associated unit, defines the relative date range for filtering findings by first observed timestamp in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="end" type="string">
          Specifies the end of the date range for the 'first observed at' criterion, filtering automation rule matches to findings first observed on or before this timestamp. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The start of the date range used to match findings whose first observed at timestamp falls within this range, formatted as an ISO 8601 string. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="generator_id" type="list(object)">
      The identifier for the solution-specific component that generated a finding. Array Members: Minimum number of 1 item. Maximum number of 100 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator used to match a finding's generator ID against the configured value, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The generator ID value to match against a finding's GeneratorId field, used as a string filter criterion in a Security Hub automation rule to target findings from specific security products or services. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="list(object)">
      The product-specific identifier for a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (e.g., EQUALS, PREFIX) used to match a finding's ID against the specified values in the Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the finding identifier (Id) value to match when the automation rule evaluates Security Hub findings against the criteria's id filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_observed_at" type="list(object)">
      A timestamp that indicates when the security findings provider most recently observed a change in the resource that is involved in the finding. For more information about the validation and formatting of timestamp fields in ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="date_range" type="object">
          Defines a relative time window (e.g., last N days) using a unit and value to match findings by their last observed timestamp in an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unit" type="string">
              The unit of time (such as DAYS) paired with the date range value to filter findings by their last observed time in an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The number of time units (as specified by the associated Unit, such as days) in the relative date range used to filter findings by when they were last observed. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="end" type="string">
          The end of the date range used to match findings based on when they were last observed, so a finding's last observed timestamp must be on or before this value to trigger the automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The start (lower bound) of a timestamp range used to filter findings by the time they were last observed, matching findings whose last observed time is at or after this value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="note_text" type="list(object)">
      The text of a user-defined note that's added to a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the string comparison operator (e.g., EQUALS, PREFIX, SUFFIX, CONTAINS) used to match a finding's note text against the configured value in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          In a Security Hub automation rule, this string is the value for the note_text criterion, which the rule compares against the text of a note (Note.Text) on a finding to determine whether the rule's actions apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="note_updated_at" type="list(object)">
      The timestamp of when the note was updated. For more information about the validation and formatting of timestamp fields in ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="date_range" type="object">
          Specifies the inclusive start and end timestamps that a finding's note-updated time must fall within for the 'note_updated_at' criterion, enabling the automation rule to match findings whose notes were updated during that period. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unit" type="string">
              Specifies the time unit (e.g., DAYS) used with the date range's value to set a relative period for the note_updated_at finding field when evaluating the automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The number of units (e.g., days) for the relative date range that defines when the finding's note was last updated, used to match automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="end" type="string">
          Sets the upper-bound timestamp of the date range used by the note_updated_at criterion to match findings whose note was last updated at or before that time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The starting date-time (ISO 8601) of the range used to filter findings based on when their note was last updated. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="note_updated_by" type="list(object)">
      The principal that created a note. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the string comparison operator (e.g., EQUALS or PREFIX) used to match the principal that last updated a finding's note in the Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value to match against the principal (user or role) that last updated the note on a finding, used as a filter criterion in the Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="product_arn" type="list(object)">
      The Amazon Resource Name (ARN) for a third-party product that generated a finding in Security Hub CSPM. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or PREFIX) used to match the finding's product ARN against the given value in the automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the Amazon Resource Name (ARN) of the AWS service or integrated product that generated the finding, used to match findings from a specific product in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="product_name" type="list(object)">
      Provides the name of the product that generated the finding. For control-based findings, the product name is Security Hub CSPM. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to evaluate the finding's product name against the supplied value in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the name of the AWS service (e.g., 'GuardDuty', 'Inspector') that must match the ProductName field of a finding for this automation rule's criteria to be satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="record_state" type="list(object)">
      Provides the current state of a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator used to match a finding's record state against the specified value, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the finding record state to match (e.g., 'ACTIVE' or 'ARCHIVED') when the automation rule's criteria evaluate a finding. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="related_findings_id" type="list(object)">
      The product-generated identifier for a related finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator used by the Security Hub automation rule to match findings based on the related findings ID value (e.g., whether the finding's related finding ID equals or is prefixed by the provided value). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the identifier of a related finding that must appear in the finding's RelatedFindings list for the automation rule's criteria to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="related_findings_product_arn" type="list(object)">
      The ARN for the product that generated a related finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (such as EQUALS or NOT_EQUALS) that determines how the related findings product ARN is matched against the specified value in the automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The ARN of the AWS service that generated the related finding, used as a match condition in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_details_other" type="list(object)">
      Custom fields and values about the resource that a finding pertains to. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS, PREFIX, CONTAINS) for the 'resource_details_other' criterion in the automation rule, determining how the finding's custom resource detail key is matched against the provided value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The name of a custom resource detail attribute that the automation rule's criteria checks against the resource's 'Other' details map. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value string to match against a custom 'Other' resource detail attribute in Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_id" type="list(object)">
      The identifier for the given resource type. For AWS resources that are identified by Amazon Resource Names (ARNs), this is the ARN. For AWS resources that lack ARNs, this is the identifier as defined by the AWS-service that created the resource. For non-AWS resources, this is a unique identifier that is associated with the resource. Array Members: Minimum number of 1 item. Maximum number of 100 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or PREFIX) used to match a finding's resource ID against the value provided in this automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The exact resource ID value that the Security Hub automation rule compares against finding resources when the resourceId criterion is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_partition" type="list(object)">
      The partition in which the resource that the finding pertains to is located. A partition is a group of AWS-Regions. Each AWS-account is scoped to one partition. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the operator (e.g., EQUALS or NOT_EQUALS) used to compare a finding's resource partition (e.g., aws, aws-cn, aws-us-gov) against a configured value in this Security Hub automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value must be a valid AWS partition name (such as aws, aws-cn, or aws-us-gov) that matches the resource's partition to trigger the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_region" type="list(object)">
      The AWS-Region where the resource that a finding pertains to is located. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (such as EQUALS or NOT_EQUALS) used to match the resource's AWS Region against the values specified in the resource_region filter of the Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The AWS Region value that the resource must be in for the automation rule's resource-region criterion to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_tags" type="list(object)">
      A list of AWS tags associated with a resource at the time the finding was processed. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator (such as EQUALS, NOT_EQUALS, PREFIX, SUFFIX, CONTAINS) used to match a resource's tag value against the configured value in the Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The resource tag key that must be present on the affected resource for this automation rule's resource tag criterion to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The tag value that must match the resource's tag for a finding to be included in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_type" type="list(object)">
      A finding's title. Array Members: Minimum number of 1 item. Maximum number of 100 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator used to match the resource type filter, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the resource type to match in the automation rule, such as AwsEc2Instance or AwsS3Bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="severity_label" type="list(object)">
      The severity value of the finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Determines how the finding's severity label is compared with the criterion's value, such as EQUALS or PREFIX. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the severity label value (e.g., LOW, MEDIUM, HIGH, CRITICAL, or INFORMATIONAL) that the Security Hub automation rule matches against findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_url" type="list(object)">
      Provides a URL that links to a page about the current finding in the finding product. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, PREFIX) used to match the finding's source URL against the configured value in this automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The exact URL string that a finding's SourceUrl attribute must match (per the associated comparison operator) for the Security Hub automation rule criterion to be satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="title" type="list(object)">
      A finding's title. Array Members: Minimum number of 1 item. Maximum number of 100 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Determines how the finding's title is matched against the specified value, using operators such as EQUALS, NOT_EQUALS, PREFIX, or NOT_PREFIX. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The string value that a finding's title must match (based on the selected comparison operator) for this automation rule criterion to be satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="list(object)">
      One or more finding types in the format of namespace/category/classifier that classify a finding. For a list of namespaces, classifiers, and categories, see [Types taxonomy for ASFF](https://docs.aws.amazon.com/securityhub/latest/userguide/securityhub-findings-format-type-taxonomy.html) in the *User Guide*. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator used to match the value of a finding's 'Type' field against the provided value in this Security Hub automation rule criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Defines the exact finding type string (e.g., 'Software and Configuration Checks/Network Reachability/Unprotected Port') that the automation rule matches when filtering findings by the 'Type' criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updated_at" type="list(object)">
      A timestamp that indicates when the finding record was most recently updated. For more information about the validation and formatting of timestamp fields in ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="date_range" type="object">
          This DateRange object tells Security Hub to match only findings whose 'UpdatedAt' timestamp falls within a relative window, expressed as an integer value and a unit of time (for example, DAYS), from the current time. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unit" type="string">
              Specifies the time unit (such as days, weeks, months, or years) for the date range used to filter findings based on when they were last updated. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The numeric length of the look-back window (used with the corresponding unit, e.g., days) for filtering findings based on their 'UpdatedAt' timestamp in an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="end" type="string">
          The end (upper bound) of the timeframe used to match findings whose last updated time falls within a range, when defining an updated_at criterion for a Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start" type="string">
          The start of the date/time range used to filter findings by when they were last updated in Security Hub. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_defined_fields" type="list(object)">
      A list of user-defined name and value string pairs added to a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to evaluate the user-defined field value against the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The key is the name of a user-defined field that the automation rule checks against a finding's UserDefinedFields map to match the criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the expected string value of a user-defined field on a Security Hub finding that must be matched for the automation rule to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="verification_state" type="list(object)">
      Provides the veracity of a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Determines the operator (such as EQUALS) that the automation rule uses to compare a finding's verification state against the configured Value in this criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The exact verification state string (such as TRUE_POSITIVE, FALSE_POSITIVE, BENIGN_POSITIVE, or UNKNOWN) that the Security Hub automation rule uses to match findings in its criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="workflow_status" type="list(object)">
      Provides information about the status of the investigation into a finding. Array Members: Minimum number of 1 item. Maximum number of 20 items. Optional.
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Determines whether the finding's workflow status must equal or not equal the specified value (e.g., EQUALS or NOT_EQUALS) for the Security Hub automation rule criteria to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the workflow status of a finding (e.g., NEW, NOTIFIED, RESOLVED, SUPPRESSED) that the automation rule's criteria must match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string" required>
  A description of the rule. Required.
</ResponseField>

<ResponseField name="is_terminal" type="bool">
  Specifies whether a rule is the last to be applied with respect to a finding that matches the rule criteria. This is useful when a finding matches the criteria for multiple rules, and each rule has different actions. If a rule is terminal, Security Hub CSPM applies the rule action to a finding that matches the rule criteria and doesn't evaluate other rules for the finding. By default, a rule isn't terminal. Optional.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The name of the rule. Required.
</ResponseField>

<ResponseField name="rule_order" type="number" required>
  An integer ranging from 1 to 1000 that represents the order in which the rule action is applied to findings. Security Hub CSPM applies rules with lower values for this parameter first. Required.
</ResponseField>

<ResponseField name="rule_status" type="string">
  Whether the rule is active after it is created. If this parameter is equal to ``ENABLED``, ASH applies the rule to findings and finding updates after the rule is created. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The date and time, in UTC and ISO 8601 format. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  The AWS Identity and Access Management principal (IAM user or role) that created the automation rule, returned as a read-only attribute. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="rule_arn" type="string">
  The Amazon Resource Name (ARN) uniquely assigned to the Security Hub automation rule when it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time, in UTC and ISO 8601 format. Computed by AWS after `ship`.
</ResponseField>
