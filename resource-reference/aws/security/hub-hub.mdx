---
title: "aws_security_hub_hub"
description: "`aws_security_hub_hub` enables Security Hub's original model in an account and Region..."
---

`aws_security_hub_hub` enables Security Hub's original model in an account and Region, the resource that must exist before `aws_security_hub_standard`s, `aws_security_hub_insight`s, or `aws_security_hub_finding_aggregator`s can be created, and the predecessor `aws_security_hub_hub_v2`'s own December 2025 relaunch built alongside rather than replacing outright.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub hub"})

    		ubx.Resource(security.HubHub, "example", security.HubHubConfig{
    			AutoEnableControls:      true,
    			ControlFindingGenerator: "example",
    			EnableDefaultStandards:  true,
    			Tags:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubHub } from "./local-sdk/aws/sdk/typescript/aws/security/hub_hub.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub hub" });

      resource(HubHub, "example", {
        autoEnableControls: true,
        controlFindingGenerator: "example",
        enableDefaultStandards: true,
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.security import HubHub, HubHubConfig

    def describe():
        ubx.intent("example own hub hub")

        ubx.resource(HubHub, "example", HubHubConfig(
            auto_enable_controls=True,
            control_finding_generator="example",
            enable_default_standards=True,
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_hub, with auto enable controls true,
    control finding generator example, enable default standards true,
    tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_enable_controls" type="bool">
  Whether to automatically enable new controls when they are added to standards that are enabled Optional.
</ResponseField>

<ResponseField name="control_finding_generator" type="string">
  This field, used when enabling Security Hub, specifies whether the calling account has consolidated control findings turned on. If the value for this field is set to SECURITY_CONTROL, Security Hub generates a single finding for a control check even when the check applies to multiple enabled standards. If the value for this field is set to STANDARD_CONTROL, Security Hub generates separate findings for a control check when the check applies to multiple enabled standards. Optional.
</ResponseField>

<ResponseField name="enable_default_standards" type="bool">
  Whether to enable the security standards that Security Hub has designated as automatically enabled. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  An ARN is automatically created for the customer. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subscribed_at" type="string">
  The date and time when Security Hub was enabled in the account. Computed by AWS after `ship`.
</ResponseField>
