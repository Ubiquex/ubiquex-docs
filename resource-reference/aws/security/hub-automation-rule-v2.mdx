---
title: "aws_security_hub_automation_rule_v2"
description: "`aws_security_hub_automation_rule_v2` defines a real, equivalent automation rule under Security Hub's next-generation (HubV2) model..."
---

`aws_security_hub_automation_rule_v2` defines a real, equivalent automation rule under Security Hub's next-generation (HubV2) model, launched at re:Invent in December 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub automation rule v2"})

    		ubx.Resource(security.HubAutomationRuleV2, "example", security.HubAutomationRuleV2Config{
    			Actions:     []map[string]any{{"external_integration_configuration": map[string]any{"connector_arn": "example"}}},
    			Criteria:    map[string]any{"ocsf_finding_criteria": map[string]any{"composite_filters": []map[string]any{{"boolean_filters": []map[string]any{{"field_name": "example-field-name"}}}}}},
    			Description: "Managed by ubx.",
    			RuleName:    "example-rule-name",
    			RuleOrder:   1,
    			RuleStatus:  "example",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubAutomationRuleV2 } from "@ubx/sdk-aws/aws/security/hub_automation_rule_v2";

    export default stack("example", () => {
      intent({ summary: "example own hub automation rule v2" });

      resource(HubAutomationRuleV2, "example", {
        actions: [{
          externalIntegrationConfiguration: { connectorArn: "example" },
        }],
        criteria: {
          ocsfFindingCriteria: {
            compositeFilters: [{
              booleanFilters: [{ fieldName: "example-field-name" }],
            }],
          },
        },
        description: "Managed by ubx.",
        ruleName: "example-rule-name",
        ruleOrder: 1,
        ruleStatus: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.security import HubAutomationRuleV2, HubAutomationRuleV2Config

    def describe():
        ubx.intent("example own hub automation rule v2")

        ubx.resource(HubAutomationRuleV2, "example", HubAutomationRuleV2Config(
            actions=[{"external_integration_configuration": {"connector_arn": "example"}}],
            criteria={"ocsf_finding_criteria": {"composite_filters": [{"boolean_filters": [{"field_name": "example-field-name"}]}]}},
            description="Managed by ubx.",
            rule_name="example-rule-name",
            rule_order=1,
            rule_status="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_automation_rule_v2, with actions a set
    value, criteria a set value, description Managed by ubx., rule
    name example-rule-name, rule order 1, rule status example, tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="list(object)" required>
  A list of actions to be performed when the rule criteria is met Required.
  <Expandable title="properties">
    <ResponseField name="external_integration_configuration" type="object">
      Contains the configuration for sending findings to an external integration, including the ARN of the external integration defined in Security Hub. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="connector_arn" type="string">
          The ARN of the external integration connector (such as a Slack or PagerDuty integration) to which this automation rule action sends findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="finding_fields_update" type="object">
      Specifies the finding fields to update when this automation rule action is triggered, allowing changes to values such as severity, note, workflow status, and related findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comment" type="string">
          The comment text to attach to the finding when this automation rule action updates the finding's fields (sets the note on the finding). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="severity_id" type="number">
          The numeric normalized severity score (0-100) that will be assigned to the finding's severity when this automation rule action updates the finding. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status_id" type="number">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of automated response action, such as 'FINDING_FIELDS_UPDATE' to update a finding's fields, 'SNS' to publish to an SNS topic, or 'CUSTOM_ACTION' to invoke a custom action. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="criteria" type="object" required>
  Defines the parameters and conditions used to evaluate and filter security findings Required.
  <Expandable title="properties">
    <ResponseField name="ocsf_finding_criteria" type="object">
      The filtering conditions that align with OCSF standards Optional.
      <Expandable title="properties">
        <ResponseField name="composite_filters" type="list(object)">
          Enables the creation of complex filtering conditions by combining filter Optional.
          <Expandable title="properties">
            <ResponseField name="boolean_filters" type="list(object)">
              This field is the list of BooleanFilter objects within a composite filter group, where each object specifies an OCSF attribute of boolean type and the required boolean value to match in the automation rule's OCSF finding criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field_name" type="string">
                  Specifies the name of the OCSF finding attribute whose boolean value is compared in this filter, such as whether the finding is a false positive. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter" type="object">
                  Specifies the matching criterion for a boolean OCSF filter in a Security Hub automation rule, including the OCSF field name, operator, and value used to evaluate findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="value" type="bool">
                      Defines the boolean value that the OCSF finding attribute specified in the filter's key must equal to satisfy this boolean filter condition within an automation rule's composite OCSF criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="date_filters" type="list(object)">
              Defines a list of date-based filter conditions used within a composite filter to match OCSF findings in Security Hub automation rules based on date fields like creation or update time. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field_name" type="string">
                  Specifies the name of the OCSF (Open Cybersecurity Schema Framework) field that the date filter condition evaluates, determining which timestamp field from the OCSF finding is compared to the provided date range or value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter" type="object">
                  Defines the actual date-based condition (e.g., a numeric timestamp, date range, or relative offset) applied when evaluating OCSF finding criteria in the automation rule's composite filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="date_range" type="object">
                      Specifies the start and end date values that define the time window for a date-based filter condition within an OCSF composite criteria in a Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="unit" type="string">
                          The time unit (DAYS, WEEKS, MONTHS, or YEARS) that, with the date range value, defines a relative window for matching OCSF finding criteria in the Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="number">
                          The numeric quantity of the relative date range (e.g., number of days) used to match OCSF findings that occurred within a specified timeframe in the automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="end" type="string">
                      Specifies the upper (end) boundary of a date/time range used in a date filter within a composite filter for matching OCSF-format findings against an automation rule's criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="start" type="string">
                      The start of the date range for a DateFilter in a composite filter, specified as an ISO 8601 string (e.g., '2023-01-01T00:00:00Z'), used to match findings with activity timestamps on or after this datetime. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="map_filters" type="list(object)">
              Defines a list of map filter conditions within a composite criteria block that match OCSF findings based on key-value pairs in map-type fields (such as resource tags), used to determine if the automation rule applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field_name" type="string">
                  The name of the specific OCSF finding field that this map filter condition evaluates, used within a composite filter to match against a defined value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter" type="object">
                  The filter object within a map filter of an OCSF finding criteria defines the condition (key, comparison operator, and value) used to match findings against map-type fields in a Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="comparison" type="string">
                      Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) that determines how the map filter's value is matched against the finding's map field in the automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key" type="string">
                      The map key used in a map filter within the composite filter to match on a specific OCSF finding attribute in the Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      In the OCSF finding criteria composite filter, this string defines the expected value that the specified map key must match for a finding to satisfy the map filter and trigger the Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="number_filters" type="list(object)">
              Defines numeric filter conditions (such as equals, greater than, less than) for OCSF fields within a composite filter, enabling the automation rule to match findings based on those numeric field values. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field_name" type="string">
                  Specifies the OCSF finding field (in the finding's OCSF representation) whose numeric value is evaluated against the configured number filter condition, such as a count or score attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter" type="object">
                  Defines a numeric filter for an OCSF finding field, including the field identifier and the comparison operation (e.g., equals, greater than, less than) to match against the finding's numeric value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="eq" type="number">
                      Specifies the exact numeric value that the corresponding OCSF finding attribute must equal to satisfy the number filter within a composite filter in the Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="gte" type="number">
                      Defines the lower bound (greater-than-or-equal-to) value for the numeric filter, so that the automation rule matches OCSF findings whose corresponding numeric attribute is at least this value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="lte" type="number">
                      Specifies the maximum (less-than-or-equal-to) numeric threshold for a composite number filter on an OCSF finding attribute, so the automation rule matches only findings whose value is at or below this number. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="operator" type="string">
              The logical operator (e.g., `AND` or `OR`) used to combine the multiple OCSF filters within this composite filter, determining whether all or any of the sub-conditions must match for the automation rule's criteria to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="string_filters" type="list(object)">
              Defines a list of string filter conditions, each specifying an OCSF field, a comparison operator, and a value, used to match findings for this automation rule's composite filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="field_name" type="string">
                  The name of the OCSF finding field that the string filter evaluates within the composite filter, matching findings where that field contains the specified string value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter" type="object">
                  Defines a single string filter condition inside a composite filter of an OCSF finding criteria, specifying the OCSF field name, comparison operator, and value used to match security findings for an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="comparison" type="string">
                      Determines the comparison operator used by the string filter to match the OCSF finding field, such as EQUALS or PREFIX, within a composite filter of an AWS Security Hub automation rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The string value to match against the OCSF finding attribute specified by this filter's key in the Security Hub automation rule criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="composite_operator" type="string">
          The logical operator used to combine multiple conditions Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string" required>
  A description of the automation rule Required.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The name of the automation rule Required.
</ResponseField>

<ResponseField name="rule_order" type="number" required>
  The value for the rule priority Required.
</ResponseField>

<ResponseField name="rule_status" type="string">
  The status of the automation rule Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp formatted in ISO8601 Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rule_arn" type="string">
  The ARN of the automation rule Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rule_id" type="string">
  The ID of the automation rule Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp formatted in ISO8601 Computed by AWS after `ship`.
</ResponseField>
