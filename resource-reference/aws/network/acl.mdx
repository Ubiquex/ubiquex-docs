---
title: "aws_network_acl"
description: "`aws_network_acl` creates a real, stateless network access control list scoped to a VPC..."
---

`aws_network_acl` creates a real, stateless network access control list scoped to a VPC, a numbered set of allow/deny rules evaluated in order per subnet, distinct from a security group's stateful, instance-scoped rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own acl"})

    		ubx.Resource(network.Acl, "example", network.AclConfig{
    			VpcId: "example",
    			Tags:  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Acl } from "./local-sdk/aws/sdk/typescript/aws/network/acl.ts";

    export default stack("example", () => {
      intent({ summary: "example own acl" });

      resource(Acl, "example", {
        vpcId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import Acl, AclConfig

    def describe():
        ubx.intent("example own acl")

        ubx.resource(Acl, "example", AclConfig(
            vpc_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_acl, with vpc id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="list(object)">
  The tags for the network ACL. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the network ACL, used to label and organize the resource for identification and management within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag applied to the network ACL, used to assign metadata such as purpose or ownership to the ACL. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC for the network ACL. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The AWS-assigned identifier for the network ACL, which uses the format acl-xxxxxxxx. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
