---
title: "aws_network_interface_permission"
description: "`aws_network_interface_permission` grants a real cross-account permission on an `aws_network_interface`..."
---

`aws_network_interface_permission` grants a real cross-account permission on an `aws_network_interface`, letting another AWS account attach that interface to an instance it owns.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interface permission"})

    		ubx.Resource(network.InterfacePermission, "ci-runner", network.InterfacePermissionConfig{
    			AwsAccountId:       "example",
    			NetworkInterfaceId: "example",
    			Permission:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterfacePermission } from "./local-sdk/aws/sdk/typescript/aws/network/interface_permission.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own interface permission" });

      resource(InterfacePermission, "ci-runner", {
        awsAccountId: "example",
        networkInterfaceId: "example",
        permission: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import InterfacePermission, InterfacePermissionConfig

    def describe():
        ubx.intent("payments own interface permission")

        ubx.resource(InterfacePermission, "ci-runner", InterfacePermissionConfig(
            aws_account_id="example",
            network_interface_id="example",
            permission="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_interface_permission, with aws account id
    example, network interface id example, permission example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string" required>
  The AWS account ID that is granted permission to attach the network interface to an instance or associate it with an Elastic IP. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  The ID of the Elastic Network Interface (ENI) to which this permission grants cross-account attachment access. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="permission" type="string" required>
  The type of permission to grant on the network interface, either INSTANCE-ATTACH (allows an instance to attach to the interface) or EIP-ASSOCIATE (allows an Elastic IP to be associated with the interface). Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned by AWS to this network interface permission, used to reference the permission in other API calls. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
