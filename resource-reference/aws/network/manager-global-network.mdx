---
title: "aws_network_manager_global_network"
description: "`aws_network_manager_global_network` creates a real top-level container that groups an organization's `aws_network_manager_device`s..."
---

`aws_network_manager_global_network` creates a real top-level container that groups an organization's `aws_network_manager_device`s, `aws_network_manager_site`s, and `aws_network_manager_link`s into a single network Network Manager can visualize and monitor.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager global network"})

    		ubx.Resource(network.ManagerGlobalNetwork, "ci-runner", network.ManagerGlobalNetworkConfig{
    			CreatedAt:   "example",
    			Description: "Managed by ubx.",
    			State:       "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerGlobalNetwork } from "./local-sdk/aws/sdk/typescript/aws/network/manager_global_network.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own manager global network" });

      resource(ManagerGlobalNetwork, "ci-runner", {
        createdAt: "example",
        description: "Managed by ubx.",
        state: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import ManagerGlobalNetwork, ManagerGlobalNetworkConfig

    def describe():
        ubx.intent("payments own manager global network")

        ubx.resource(ManagerGlobalNetwork, "ci-runner", ManagerGlobalNetworkConfig(
            created_at="example",
            description="Managed by ubx.",
            state="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_global_network, with created at
    example, description Managed by ubx., state example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="created_at" type="string">
  The date and time that the global network was created. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the global network. Optional.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the global network. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags for the global network. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key part of a tag attached to the AWS Network Manager global network, used to label and organize the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag associated with this AWS Network Manager global network, used to store an arbitrary string that helps identify or categorize the network for management and cost allocation purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the global network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the global network. Computed by AWS after `ship`.
</ResponseField>
