---
title: "aws_network_acl_rule"
description: "Real, generated bindings for `aws_network_acl_rule`."
---

`aws_network_acl_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own acl rule"})

    		ubx.Resource(network.AclRule, "ci-runner", network.AclRuleConfig{
    			NetworkAclId: "example",
    			Protocol:     "example",
    			RuleAction:   "example",
    			RuleNumber:   1,
    			CidrBlock:    "example",
    			Egress:       true,
    			FromPort:     1,
    			IcmpCode:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AclRule } from "jsr:@ubx/sdk-aws/aws/network/acl_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own acl rule" });

      resource(AclRule, "ci-runner", {
        networkAclId: "example",
        protocol: "example",
        ruleAction: "example",
        ruleNumber: 1,
        cidrBlock: "example",
        egress: true,
        fromPort: 1,
        icmpCode: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.network import AclRule, AclRuleConfig

    def describe():
        ubx.intent("payments own acl rule")

        ubx.resource(AclRule, "ci-runner", AclRuleConfig(
            network_acl_id="example",
            protocol="example",
            rule_action="example",
            rule_number=1,
            cidr_block="example",
            egress=True,
            from_port=1,
            icmp_code=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_acl_rule, with network acl id example,
    protocol example, rule action example, rule number 1, cidr block
    example, egress true, from port 1, icmp code 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_block" type="string">
  Optional.
</ResponseField>

<ResponseField name="egress" type="bool">
  Optional.
</ResponseField>

<ResponseField name="from_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="icmp_code" type="number">
  Optional.
</ResponseField>

<ResponseField name="icmp_type" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block" type="string">
  Optional.
</ResponseField>

<ResponseField name="network_acl_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_number" type="number" required>
  Required.
</ResponseField>

<ResponseField name="to_port" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
