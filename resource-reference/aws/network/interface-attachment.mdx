---
title: "aws_network_interface_attachment"
description: "Real, generated bindings for `aws_network_interface_attachment`."
---

`aws_network_interface_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interface attachment"})

    		ubx.Resource(network.InterfaceAttachment, "ci-runner", network.InterfaceAttachmentConfig{
    			DeviceIndex:        1,
    			InstanceId:         "example",
    			NetworkInterfaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterfaceAttachment } from "jsr:@ubx/sdk-aws/aws/network/interface_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own interface attachment" });

      resource(InterfaceAttachment, "ci-runner", {
        deviceIndex: 1,
        instanceId: "example",
        networkInterfaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.network import InterfaceAttachment, InterfaceAttachmentConfig

    def describe():
        ubx.intent("payments own interface attachment")

        ubx.resource(InterfaceAttachment, "ci-runner", InterfaceAttachmentConfig(
            device_index=1,
            instance_id="example",
            network_interface_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_interface_attachment, with device index 1,
    instance id example, network interface id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="device_index" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_card_index" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="attachment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_card_index" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
