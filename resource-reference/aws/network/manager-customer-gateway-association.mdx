---
title: "aws_network_manager_customer_gateway_association"
description: "`aws_network_manager_customer_gateway_association` associates a real `aws_customer_gateway` with a device and site in..."
---

`aws_network_manager_customer_gateway_association` associates a real `aws_customer_gateway` with a device and site in an `aws_network_manager_global_network`, so Network Manager can represent that on-premises VPN endpoint in its own topology.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager customer gateway association"})

    		ubx.Resource(network.ManagerCustomerGatewayAssociation, "example", network.ManagerCustomerGatewayAssociationConfig{
    			CustomerGatewayArn: "arn:aws:iam::123456789012:policy/example",
    			DeviceId:           "example",
    			GlobalNetworkId:    "example",
    			LinkId:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerCustomerGatewayAssociation } from "./local-sdk/aws/sdk/typescript/aws/network/manager_customer_gateway_association.ts";

    export default stack("example", () => {
      intent({ summary: "example own manager customer gateway association" });

      resource(ManagerCustomerGatewayAssociation, "example", {
        customerGatewayArn: "arn:aws:iam::123456789012:policy/example",
        deviceId: "example",
        globalNetworkId: "example",
        linkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import ManagerCustomerGatewayAssociation, ManagerCustomerGatewayAssociationConfig

    def describe():
        ubx.intent("example own manager customer gateway association")

        ubx.resource(ManagerCustomerGatewayAssociation, "example", ManagerCustomerGatewayAssociationConfig(
            customer_gateway_arn="arn:aws:iam::123456789012:policy/example",
            device_id="example",
            global_network_id="example",
            link_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_customer_gateway_association, with
    customer gateway arn arn:aws:iam::123456789012:policy/example,
    device id example, global network id example, link id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="customer_gateway_arn" type="string" required>
  The Amazon Resource Name (ARN) of the customer gateway. Required.
</ResponseField>

<ResponseField name="device_id" type="string" required>
  The ID of the device Required.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  The ID of the global network. Required.
</ResponseField>

<ResponseField name="link_id" type="string">
  The ID of the link Optional.
</ResponseField>
