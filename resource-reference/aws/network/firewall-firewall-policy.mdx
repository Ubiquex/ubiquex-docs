---
title: "aws_network_firewall_firewall_policy"
description: "`aws_network_firewall_firewall_policy` defines the real set of stateless and stateful rule groups, and default actions..."
---

`aws_network_firewall_firewall_policy` defines the real set of stateless and stateful rule groups, and default actions, that one or more `aws_network_firewall_firewall`s enforce against passing traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewall firewall policy"})

    		ubx.Resource(network.FirewallFirewallPolicy, "example", network.FirewallFirewallPolicyConfig{
    			FirewallPolicy:     map[string]any{"stateless_default_actions": []string{"example"}},
    			FirewallPolicyName: "example-firewall-policy-name",
    			Description:        "Managed by ubx.",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallFirewallPolicy } from "./local-sdk/aws/sdk/typescript/aws/network/firewall_firewall_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own firewall firewall policy" });

      resource(FirewallFirewallPolicy, "example", {
        firewallPolicy: { statelessDefaultActions: ["example"] },
        firewallPolicyName: "example-firewall-policy-name",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import FirewallFirewallPolicy, FirewallFirewallPolicyConfig

    def describe():
        ubx.intent("example own firewall firewall policy")

        ubx.resource(FirewallFirewallPolicy, "example", FirewallFirewallPolicyConfig(
            firewall_policy={"stateless_default_actions": ["example"]},
            firewall_policy_name="example-firewall-policy-name",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_firewall_firewall_policy, with firewall policy
    a set value, firewall policy name example-firewall-policy-name,
    description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A user-provided description of the firewall policy, used to identify its purpose or contents. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="firewall_policy" type="object" required>
  Defines the core AWS Network Firewall policy configuration, including references to stateless and stateful rule groups and the default actions for unmatched traffic. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enable_tlssession_holding" type="bool">
      Enables the Network Firewall to hold the TLS session and buffer handshake packets during TLS inspection, ensuring the certificate is evaluated before the connection continues. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="policy_variables" type="object">
      Defines named policy variables (such as IP set and port set values) that are substituted into Suricata rules within stateful rule groups associated with this firewall policy, allowing reuse of common values across rules. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="rule_variables" type="any">
          Defines the rule variables (IP sets and port sets) for the firewall policy, which are used by the Suricata rule engine to enable dynamic matching of source and destination IPs or ports. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stateful_default_actions" type="list(string)">
      Specifies the default actions to apply to traffic that does not match any stateful rule group in the firewall policy (e.g., 'aws:drop' or 'aws:pass'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stateful_engine_options" type="object">
      Specifies the stateful engine behavior for the firewall policy, including the rule group evaluation order (STRICT_ORDER or DEFAULT_ACTION_ORDER) and the action to take for stream exceptions (DROP or CONTINUE). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="flow_timeouts" type="object">
          Specifies the idle timeout durations for TCP and UDP flows processed by the stateful inspection engine. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="tcp_idle_timeout_seconds" type="number">
              Specifies the idle timeout in seconds for TCP flows, after which the firewall considers the connection idle and may terminate it. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rule_order" type="string">
          Determines the order in which stateful rules are evaluated, with valid values such as DEFAULT_ACTION_ORDER, STRICT_ORDER, and LAST_ACTION_ORDER, affecting how traffic is matched and actions are applied. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_exception_policy" type="string">
          Controls whether packets that do not match any stateful rule are dropped (DROP) or passed to the stateless engine for further inspection (CONTINUE) within the firewall policy's stateful engine options. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stateful_rule_group_references" type="list(object)">
      Specifies the stateful rule groups, identified by their ARNs, that are used in the firewall policy's stateful rule engine, with an optional priority controlling the order in which their rules are evaluated. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="deep_threat_inspection" type="bool">
          Enables deep threat inspection for the referenced stateful rule group, allowing the firewall to perform deeper packet analysis on traffic matching that rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="override" type="object">
          This object configures an override action (DROP_TO_FIRST or PASS_TO_FIRST) that alters the default evaluation behavior of the associated stateful rule group reference within the firewall policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="action" type="string">
              Specifies the override action (DROP, PASS, or CONTINUE) for a stateful rule group reference in the firewall policy, determining how traffic matching the rule group's rules is handled. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="priority" type="number">
          Determines the order in which the referenced stateful rule group is evaluated relative to other stateful rule groups in the policy, with lower numbers evaluated first. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_arn" type="string">
          The ARN of the stateful rule group to associate with the firewall policy, used to apply stateful inspection to matching traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stateless_custom_actions" type="list(object)">
      Defines the list of custom actions you can reference in stateless rules within this firewall policy, where each action specifies a name and an action definition such as publishing metrics to CloudWatch. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action_definition" type="object">
          Defines the action taken by a stateless custom action in an AWS Network Firewall policy, which currently supports a publish_metric_action that publishes metrics to Amazon CloudWatch. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="publish_metric_action" type="object">
              Configures CloudWatch metric publishing for this stateless custom action, including the metric dimensions to include when metrics are emitted. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="dimensions" type="list(object)">
                  Specifies the list of key-value dimensions that are attached to the CloudWatch metric published by this stateless custom action's publish-metric action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="value" type="string">
                      The value for a CloudWatch dimension that is attached to the metric published when a stateless custom action is invoked, as part of the firewall policy's publish metric action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="action_name" type="string">
          The action_name is a user-defined, policy-unique friendly name for a custom stateless action, which is referenced by stateless rules in the firewall policy to invoke the defined action. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stateless_default_actions" type="list(string)" required>
      Specifies the default actions (e.g., 'aws:pass' or 'aws:drop') to apply to packets that do not match any stateless rule in the firewall policy. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stateless_fragment_default_actions" type="list(string)" required>
      A required list of default actions applied to fragmented packets that do not match any stateless rule in the firewall policy, such as 'aws:drop' or 'aws:forward_to_sfe'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stateless_rule_group_references" type="list(object)">
      Defines the stateless rule groups to associate with the firewall policy, each with a priority and rule group ARN, controlling the order in which stateless rules are evaluated. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="priority" type="number">
          The priority of a stateless rule group reference, where lower numbers are evaluated first, determining the order in which stateless rule groups are processed within the firewall policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_arn" type="string">
          The Amazon Resource Name (ARN) of the stateless rule group that this reference points to, which the firewall policy uses to apply stateless packet inspection in the specified priority order. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tlsinspection_configuration_arn" type="string">
      A resource ARN. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="firewall_policy_name" type="string" required>
  The descriptive name of the firewall policy, used to identify it within AWS Network Firewall. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      A key of a tag that is assigned to the Network Firewall firewall policy to help organize and identify it. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="firewall_policy_arn" type="string">
  A resource ARN. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="firewall_policy_id" type="string">
  The unique identifier assigned to the firewall policy by AWS Network Firewall, used to reference this policy in other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
