---
title: "aws_network_interface_sg_attachment"
description: "Real, generated bindings for `aws_network_interface_sg_attachment`."
---

`aws_network_interface_sg_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interface sg attachment"})

    		ubx.Resource(network.InterfaceSgAttachment, "ci-runner", network.InterfaceSgAttachmentConfig{
    			NetworkInterfaceId: "example",
    			SecurityGroupId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterfaceSgAttachment } from "jsr:@ubx/sdk-aws/aws/network/interface_sg_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own interface sg attachment" });

      resource(InterfaceSgAttachment, "ci-runner", {
        networkInterfaceId: "example",
        securityGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.network import InterfaceSgAttachment, InterfaceSgAttachmentConfig

    def describe():
        ubx.intent("payments own interface sg attachment")

        ubx.resource(InterfaceSgAttachment, "ci-runner", InterfaceSgAttachmentConfig(
            network_interface_id="example",
            security_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_interface_sg_attachment, with network
    interface id example, security group id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
