---
title: "aws_network_firewall_rule_group"
description: "`aws_network_firewall_rule_group` defines a real, reusable..."
---

`aws_network_firewall_rule_group` defines a real, reusable, named set of stateless or stateful traffic-inspection rules that can be referenced from one or more `aws_network_firewall_firewall_policy` resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkfirewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/networkfirewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own firewall rule group"})

    		ubx.Resource(networkfirewall.RuleGroup, "example", networkfirewall.RuleGroupConfig{
    			Capacity:             1,
    			RuleGroupName:        "example-rule-group-name",
    			Type:                 "example",
    			Description:          "Managed by ubx.",
    			RuleGroup:            map[string]any{"rules_source": map[string]any{"rules_source_list": map[string]any{"generated_rules_type": "example"}}},
    			SummaryConfiguration: map[string]any{"rule_options": []string{"example"}},
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleGroup } from "@ubx/sdk-aws/aws/networkfirewall/rule_group";

    export default stack("example", () => {
      intent({ summary: "example own firewall rule group" });

      resource(RuleGroup, "example", {
        capacity: 1,
        ruleGroupName: "example-rule-group-name",
        type: "example",
        description: "Managed by ubx.",
        ruleGroup: {
          rulesSource: { rulesSourceList: { generatedRulesType: "example" } },
        },
        summaryConfiguration: { ruleOptions: ["example"] },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkfirewall.rule_group import RuleGroup, RuleGroupConfig

    def describe():
        ubx.intent("example own firewall rule group")

        ubx.resource(RuleGroup, "example", RuleGroupConfig(
            capacity=1,
            rule_group_name="example-rule-group-name",
            type="example",
            description="Managed by ubx.",
            rule_group={"rules_source": {"rules_source_list": {"generated_rules_type": "example"}}},
            summary_configuration={"rule_options": ["example"]},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_firewall_rule_group, with capacity 1, rule
    group name example-rule-group-name, type example, description
    Managed by ubx., rule group a set value, summary configuration a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity" type="number" required>
  The maximum number of capacity units the rule group can consume, limiting the total number of rules and rule actions that can be defined in the group. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A user-provided description for the rule group, used to identify its purpose or contents within AWS Network Firewall. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="rule_group" type="object">
  Configuration object for the rule group, specifying the stateful or stateless rules source, rule variables, and reference sets that define the network traffic inspection behavior. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="reference_sets" type="object">
      Specifies named sets of IP addresses or domain names that can be referenced by the rule group's stateful rules (e.g., using the REF keyword in Suricata rules). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ipset_references" type="any">
          Specifies the named IP set references for the rule group, mapping each reference name to the ARN of an AWS Network Firewall IP set that rules in this rule group can use. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rule_variables" type="object">
      Defines the IP sets and port sets that can be referenced by the rule group's rules for matching traffic attributes. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ipsets" type="any">
          Defines named IP set variables, each mapping a key to an object containing a list of CIDR ranges, which can be referenced in the rule group's rules to match source or destination addresses. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port_sets" type="any">
          Defines named port sets, each mapping a name to a list of port ranges, which are referenced in the rule group's rules to match source or destination port numbers. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rules_source" type="object" required>
      Defines the rules for the rule group, specifying either stateless rules and custom actions or stateful rule configuration using a rules string, rules source list, or individual stateful rules. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="rules_source_list" type="object">
          Specifies the list of IP addresses or domain names that the rule group inspects, the target types (such as HTTP_HOST, TLS_SNI, SOURCE, or DESTINATION) that determine how the targets are matched, and whether matching traffic is allowed or dropped via the GeneratedRulesType. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="generated_rules_type" type="string" required>
              Specifies whether the rule group generates allow or deny rules for the referenced domain list, with values 'ALLOWLIST' or 'DENYLIST'. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_types" type="list(string)" required>
              Specifies the target types to inspect (such as TLS_SNI for TLS server name indication or HTTP_HOST for HTTP host header) when using the domain list rules source in the rule group. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="targets" type="list(string)" required>
              The list of IP addresses or CIDR blocks that the stateful rule group inspects traffic to or from, as defined in the rules source list. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rules_string" type="string">
          Specifies the Suricata-compatible rules string that defines the stateful inspection rules for the rule group, used when the rule group is configured as a stateful rule group. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stateful_rules" type="list(object)">
          Defines a list of ordered stateful rules, each combining a header (protocol, source, destination, action) with Suricata rule options, used by AWS Network Firewall to perform stateful inspection of traffic. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="action" type="string">
              Defines the action taken when traffic matches the stateful rule, such as PASS, DROP, ALERT, or REJECT. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="header" type="object">
              Defines the match criteria for a stateful rule's traffic header, specifying the protocol, source and destination IP addresses and ports, and the direction of traffic to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="destination" type="string">
                  The destination IP address or CIDR range that the stateful rule matches in the traffic header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="destination_port" type="string">
                  Defines the destination port (or port range) to match in the stateful rule's header, expressed as a number, a range separated by a colon (e.g., 20:80), or the keyword ANY. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="direction" type="string">
                  Specifies the traffic direction to match, either FORWARD or ANY, for stateful rules in the rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="protocol" type="string">
                  The IP protocol (e.g., TCP, UDP, ICMP, IP) that the stateful rule header matches, determining which network protocol the rule applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source" type="string">
                  Specifies the source IP address or CIDR range that the stateful rule matches on. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source_port" type="string">
                  The source port (or port range) to match in the stateful rule's header, representing the originating port of the traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="rule_options" type="list(object)">
              Configures the keyword options applied to each stateful rule in the Network Firewall rule group, such as the rule's sid, rev, msg, or protocol-specific keywords. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="keyword" type="string">
                  This field specifies the keyword (name) of a key-value rule option for a stateful rule, such as 'sid', 'rev', 'content', or 'flow', which is used by the Suricata-compatible rule engine to set matching conditions or metadata for the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="settings" type="list(string)">
                  Specifies the values for a Suricata rule option in a stateful rule, such as the rule ID for a `sid` keyword or the message text for a `msg` keyword, used to customize matching and inspection in AWS Network Firewall. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="stateless_rules_and_custom_actions" type="object">
          Defines the stateless rules and custom actions to use in a stateless rule group. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_actions" type="list(object)">
              Specifies custom actions that can be referenced by stateless rules to perform additional behaviors such as publishing metrics to Amazon CloudWatch. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="action_definition" type="object">
                  Defines the action definition for a custom stateless action, which contains the PublishMetricAction configuration that specifies the CloudWatch metric dimensions to report when the action matches traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="publish_metric_action" type="object">
                      Configures the CloudWatch metric publication for this custom action by specifying the dimension(s) that the published metrics are tagged with. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          Specifies the list of key-value dimension pairs attached to the CloudWatch metric published when this custom action is invoked, enabling you to filter or group the metric by these dimensions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="value" type="string">
                              The value of a dimension to include in the CloudWatch metric published by the custom action's publish_metric_action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="action_name" type="string">
                  A unique name that identifies a custom action within the rule group, which stateless rules reference to invoke the action's defined behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="stateless_rules" type="list(object)" required>
              Defines the list of stateless rules that specify match criteria and actions for packets processed without stateful inspection in the Network Firewall rule group. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The priority value for the stateless rule, which determines the order in which rules are evaluated (lower numbers take precedence). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rule_definition" type="object">
                  Defines the actions to take and the matching criteria (such as source and destination addresses, ports, and protocols) for a stateless rule in the rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="actions" type="list(string)">
                      Specifies the actions to take on packets that match the stateless rule, such as 'aws:pass', 'aws:drop', or custom action names defined in the rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_attributes" type="object">
                      Defines the traffic match criteria (such as source/destination addresses, ports, protocols, and TCP flags) against which the stateless rule evaluates packets. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="destination_ports" type="list(object)">
                          Defines the destination port ranges (from and to) that a stateless packet must match for the rule to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="from_port" type="number">
                              The lower bound of the destination port range that a stateless rule uses to match traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="to_port" type="number">
                              The upper bound (inclusive) of a destination port range to match in stateless rule evaluation, with valid values 0-65535 and defaulting to the lower bound if omitted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="destinations" type="list(object)">
                          Specifies the destination IP address or CIDR range that the stateless rule matches against traffic to evaluate the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="address_definition" type="string">
                              The destination IP address or CIDR block to match in the stateless rule's traffic attributes, such as '192.0.2.0/24'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="protocols" type="list(number)">
                          Specifies the IP protocol numbers (for example, 6 for TCP, 17 for UDP) that the stateless rule matches, or if omitted, matches all protocols. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="source_ports" type="list(object)">
                          Defines the source port ranges (with FromPort and ToPort) that the stateless rule matches on for incoming traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="from_port" type="number">
                              The lower bound (inclusive) of the source port range to match in the stateless rule's source ports. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="to_port" type="number">
                              The upper bound (inclusive) of the source port range that a stateless rule uses to match traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="sources" type="list(object)">
                          Specifies the source IP addresses (as CIDR blocks or individual IPs) that this stateless rule matches; if empty, matches any source address. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="address_definition" type="string">
                              The IP address or CIDR range that defines a source address to match in a stateless rule's match attributes, used by AWS Network Firewall to filter traffic based on packet source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="tcpflags" type="list(object)">
                          Defines the TCP flags (e.g., SYN, FIN, ACK) that this stateless rule matches on, including optional masks to limit which flag bits are inspected. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="flags" type="list(string)">
                              Specifies the TCP flags (e.g., FIN, SYN, RST, PSH, ACK, URG, ECE, CWR) to match in the packet's TCP header for the stateless rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="masks" type="list(string)">
                              The list of TCP flag names (for example, 'SYN', 'ACK', 'FIN', 'RST') that define which flags in the packet's TCP header are examined (masked) when evaluating this stateless rule's match conditions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stateful_rule_options" type="object">
      This object specifies the rule order and stream exception policy for a stateful rule group in AWS Network Firewall. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="rule_order" type="string">
          Specifies how the stateful rule group evaluates rules: either in the default action order (DEFAULT_ACTION_ORDER) based on action type and priority, or in the strict order in which rules are defined (STRICT_ORDER). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_group_name" type="string" required>
  Specifies the name of the rule group, which must be unique within the AWS account and Region. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="summary_configuration" type="object">
  Configures the optional rule group summary feature, which enables generation of per-rule match counts for stateful rules in the group, with an enabled flag and a subject (such as `rulegroupsummary`) that specifies the type of summary. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="rule_options" type="list(string)">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the AWS resource tags (key-value pairs) to attach to the Network Firewall rule group, used for cost allocation, access control, and resource organization. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Specifies whether the rule group is a stateful rule group (STATEFUL) or a stateless rule group (STATELESS), which determines the kind of rules and matching behavior it supports. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="rule_group_arn" type="string">
  A resource ARN. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rule_group_id" type="string">
  The unique identifier (UUID) assigned by AWS to the rule group when it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
