---
title: "aws_network_manager_transit_gateway_route_table_attachment"
description: "`aws_network_manager_transit_gateway_route_table_attachment` attaches a real transit gateway route table..."
---

`aws_network_manager_transit_gateway_route_table_attachment` attaches a real transit gateway route table, exposed through an `aws_network_manager_transit_gateway_peering`, to an `aws_network_manager_core_network`'s own routing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager transit gateway route table attachment"})

    		ubx.Resource(network.ManagerTransitGatewayRouteTableAttachment, "example", network.ManagerTransitGatewayRouteTableAttachmentConfig{
    			PeeringId:                          "example",
    			TransitGatewayRouteTableArn:        "arn:aws:iam::123456789012:policy/example",
    			NetworkFunctionGroupName:           "example-network-function-group-name",
    			ProposedNetworkFunctionGroupChange: map[string]any{"attachment_policy_rule_number": 1},
    			ProposedSegmentChange:              map[string]any{"attachment_policy_rule_number": 1},
    			RoutingPolicyLabel:                 "example",
    			Tags:                               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerTransitGatewayRouteTableAttachment } from "./local-sdk/aws/sdk/typescript/aws/network/manager_transit_gateway_route_table_attachment.ts";

    export default stack("example", () => {
      intent({
        summary: "example own manager transit gateway route table attachment",
      });

      resource(ManagerTransitGatewayRouteTableAttachment, "example", {
        peeringId: "example",
        transitGatewayRouteTableArn: "arn:aws:iam::123456789012:policy/example",
        networkFunctionGroupName: "example-network-function-group-name",
        proposedNetworkFunctionGroupChange: { attachmentPolicyRuleNumber: 1 },
        proposedSegmentChange: { attachmentPolicyRuleNumber: 1 },
        routingPolicyLabel: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import ManagerTransitGatewayRouteTableAttachment, ManagerTransitGatewayRouteTableAttachmentConfig

    def describe():
        ubx.intent("example own manager transit gateway route table attachment")

        ubx.resource(ManagerTransitGatewayRouteTableAttachment, "example", ManagerTransitGatewayRouteTableAttachmentConfig(
            peering_id="example",
            transit_gateway_route_table_arn="arn:aws:iam::123456789012:policy/example",
            network_function_group_name="example-network-function-group-name",
            proposed_network_function_group_change={"attachment_policy_rule_number": 1},
            proposed_segment_change={"attachment_policy_rule_number": 1},
            routing_policy_label="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_network_manager_transit_gateway_route_table_attachment, with
    peering id example, transit gateway route table arn
    arn:aws:iam::123456789012:policy/example, network function group
    name example-network-function-group-name, proposed network
    function group change a set value, proposed segment change a set
    value, routing policy label example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="network_function_group_name" type="string">
  The name of the network function group attachment. Optional.
</ResponseField>

<ResponseField name="peering_id" type="string" required>
  The Id of peering between transit gateway and core network. Required.
</ResponseField>

<ResponseField name="proposed_network_function_group_change" type="object">
  The attachment to move from one network function group to another. Optional.
  <Expandable title="properties">
    <ResponseField name="attachment_policy_rule_number" type="number">
      The rule number in the policy document that applies to this change. Optional.
    </ResponseField>
    <ResponseField name="network_function_group_name" type="string">
      The name of the network function group to change. Optional.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      The key-value tags that changed for the network function group. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Specifies the key (name) of a tag to apply to the proposed network function group change for the transit gateway route table attachment. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of a tag defined in the proposed network function group change for the AWS Network Manager transit gateway route table attachment. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proposed_segment_change" type="object">
  The attachment to move from one segment to another. Optional.
  <Expandable title="properties">
    <ResponseField name="attachment_policy_rule_number" type="number">
      The rule number in the policy document that applies to this change. Optional.
    </ResponseField>
    <ResponseField name="segment_name" type="string">
      The name of the segment to change. Optional.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      The key-value tags that changed for the segment. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key of a tag object within the proposed segment change's tags list for the transit gateway route table attachment, serving as the identifier in the key-value metadata pair. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          In the proposed segment change for an AWS Network Manager transit gateway route table attachment, this value is the tag value for the associated tag key that will be applied to the attachment when the change takes effect. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="routing_policy_label" type="string">
  Routing policy label Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Defines the key portion of a user-defined tag applied to this Network Manager Transit Gateway Route Table Attachment, enabling identification, cost allocation, and access control for the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag applied to the transit gateway route table attachment, used for metadata, organization, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_route_table_arn" type="string" required>
  The Arn of transit gateway route table. Required.
</ResponseField>

## Output properties

<ResponseField name="attachment_id" type="string">
  The ID of the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_policy_rule_number" type="number">
  The policy rule number associated with the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attachment_type" type="string">
  The type of attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_arn" type="string">
  The ARN of a core network for the VPC attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  The ID of a core network where you're creating a site-to-site VPN attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Creation time of the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  The Region where the edge is located. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modification_errors" type="list(string)">
  Errors from the last modification of the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Owner account of the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The ARN of the Resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="segment_name" type="string">
  The name of the segment that attachment is in. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the attachment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Last update time of the attachment. Computed by AWS after `ship`.
</ResponseField>
