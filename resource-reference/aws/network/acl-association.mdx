---
title: "aws_network_acl_association"
description: "Real, generated bindings for `aws_network_acl_association`."
---

`aws_network_acl_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"

    cfg := network.AclAssociationConfig{
        NetworkAclId: "example",
        SubnetId: "example",
    }
    ubx.Resource(network.AclAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AclAssociation } from
      "jsr:@ubx/sdk-aws/aws/network/acl_association";

    resource(AclAssociation, "example", {
      networkAclId: "example",
      subnetId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.network import (
        AclAssociation,
        AclAssociationConfig,
    )

    cfg = AclAssociationConfig(
        network_acl_id="example",
        subnet_id="example",
    )
    ubx.resource(AclAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_network_acl_association with network_acl_id,
subnet_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_acl_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
