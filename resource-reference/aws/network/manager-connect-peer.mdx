---
title: "aws_network_manager_connect_peer"
description: "`aws_network_manager_connect_peer` establishes a real BGP peering session over an `aws_network_manager_connect_attachment`..."
---

`aws_network_manager_connect_peer` establishes a real BGP peering session over an `aws_network_manager_connect_attachment`, exchanging routes between the core network and an external or on-premises device.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own manager connect peer"})

    		ubx.Resource(network.ManagerConnectPeer, "example", network.ManagerConnectPeerConfig{
    			ConnectAttachmentId: "example",
    			PeerAddress:         "example",
    			BgpOptions:          map[string]any{"peer_asn": 1},
    			CoreNetworkAddress:  "example",
    			InsideCidrBlocks:    []string{"example"},
    			SubnetArn:           "arn:aws:iam::123456789012:policy/example",
    			Tags:                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerConnectPeer } from "./local-sdk/aws/sdk/typescript/aws/network/manager_connect_peer.ts";

    export default stack("example", () => {
      intent({ summary: "example own manager connect peer" });

      resource(ManagerConnectPeer, "example", {
        connectAttachmentId: "example",
        peerAddress: "example",
        bgpOptions: { peerAsn: 1 },
        coreNetworkAddress: "example",
        insideCidrBlocks: ["example"],
        subnetArn: "arn:aws:iam::123456789012:policy/example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import ManagerConnectPeer, ManagerConnectPeerConfig

    def describe():
        ubx.intent("example own manager connect peer")

        ubx.resource(ManagerConnectPeer, "example", ManagerConnectPeerConfig(
            connect_attachment_id="example",
            peer_address="example",
            bgp_options={"peer_asn": 1},
            core_network_address="example",
            inside_cidr_blocks=["example"],
            subnet_arn="arn:aws:iam::123456789012:policy/example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_connect_peer, with connect attachment
    id example, peer address example, bgp options a set value, core
    network address example, inside cidr blocks a set value, subnet
    arn arn:aws:iam::123456789012:policy/example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bgp_options" type="object">
  The bgp options for this network manager connect peer. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="peer_asn" type="number">
      The autonomous system number (ASN) of the BGP peer used by the Connect Peer to establish a BGP session. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connect_attachment_id" type="string" required>
  The ID of the attachment to connect. Required.
</ResponseField>

<ResponseField name="core_network_address" type="string">
  The IP address of a core network. Optional.
</ResponseField>

<ResponseField name="inside_cidr_blocks" type="list(string)">
  The inside IP addresses used for a Connect peer configuration. Optional.
</ResponseField>

<ResponseField name="peer_address" type="string" required>
  The IP address of the Connect peer. Required.
</ResponseField>

<ResponseField name="subnet_arn" type="string">
  The subnet ARN for the connect peer. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key part of a tag applied to the Network Manager Connect Peer, used to assign metadata for identifying and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key attached to the Network Manager Connect Peer, used for resource metadata and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="configuration" type="object">
  The computed configuration of the Connect Peer, including the inside CIDR blocks, the protocol (GRE or IPSec), and the BGP configuration details such as peer and core network ASNs, IP addresses, and the Core Network edge endpoint address. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="bgp_configurations" type="list(object)">
      The BGP configurations of the connect peer, where each object contains the core network address and peer address that define the BGP peering for the connection. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="core_network_address" type="string">
          The IPv4 address on the core network (Transit Gateway) side used for BGP peering in this Connect peer BGP configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="core_network_asn" type="number">
          The autonomous system number (ASN) of the core network used for the BGP session of the connect peer. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="peer_address" type="string">
          The IP address of the BGP peer used to establish and identify the BGP session for the Connect Peer's BGP configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="peer_asn" type="number">
          The autonomous system number (ASN) of the BGP peer for the Connect peer. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="core_network_address" type="string">
      The IP address of a core network. Optional.
    </ResponseField>
    <ResponseField name="inside_cidr_blocks" type="list(string)">
      The inside IP addresses used for a Connect peer configuration. Optional.
    </ResponseField>
    <ResponseField name="peer_address" type="string">
      The IP address of the Connect peer. Optional.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The protocol used for a Connect peer configuration. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connect_peer_id" type="string">
  The ID of the Connect peer. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  The ID of the core network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Connect peer creation time. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="edge_location" type="string">
  The Connect peer Regions where edges are located. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modification_errors" type="list(string)">
  Errors from the last modification of the connect peer. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  State of the connect peer. Computed by AWS after `ship`.
</ResponseField>
