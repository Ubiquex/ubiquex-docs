---
title: "aws_network_manager_core_network_prefix_list_association"
description: "`aws_network_manager_core_network_prefix_list_association` associates a real managed prefix list with an `aws_network_manager_core_network`..."
---

`aws_network_manager_core_network_prefix_list_association` associates a real managed prefix list with an `aws_network_manager_core_network`, letting Cloud WAN routing policies reference the prefix list's own set of CIDRs by name instead of listing them individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager core network prefix list association"})

    		ubx.Resource(network.ManagerCoreNetworkPrefixListAssociation, "ci-runner", network.ManagerCoreNetworkPrefixListAssociationConfig{
    			CoreNetworkId:   "example",
    			PrefixListAlias: "example",
    			PrefixListArn:   "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerCoreNetworkPrefixListAssociation } from "./local-sdk/aws/sdk/typescript/aws/network/manager_core_network_prefix_list_association.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own manager core network prefix list association",
      });

      resource(ManagerCoreNetworkPrefixListAssociation, "ci-runner", {
        coreNetworkId: "example",
        prefixListAlias: "example",
        prefixListArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.network import ManagerCoreNetworkPrefixListAssociation, ManagerCoreNetworkPrefixListAssociationConfig

    def describe():
        ubx.intent("payments own manager core network prefix list association")

        ubx.resource(ManagerCoreNetworkPrefixListAssociation, "ci-runner", ManagerCoreNetworkPrefixListAssociationConfig(
            core_network_id="example",
            prefix_list_alias="example",
            prefix_list_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_network_manager_core_network_prefix_list_association,
    with core network id example, prefix list alias example, prefix
    list arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="core_network_id" type="string" required>
  The ID of the core network. Required.
</ResponseField>

<ResponseField name="prefix_list_alias" type="string" required>
  The alias of the prefix list Required.
</ResponseField>

<ResponseField name="prefix_list_arn" type="string" required>
  The Amazon Resource Name (ARN) of the prefix list. Required.
</ResponseField>
