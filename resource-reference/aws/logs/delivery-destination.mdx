---
title: "aws_logs_delivery_destination"
description: "`aws_logs_delivery_destination` represents a real AWS service, CloudWatch Logs, S3, Firehose, or X-Ray..."
---

`aws_logs_delivery_destination` represents a real AWS service, CloudWatch Logs, S3, Firehose, or X-Ray, configured to receive vended logs through the newer CloudWatch Logs delivery framework, the target end of an `aws_logs_delivery`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delivery destination"})

    		ubx.Resource(logs.DeliveryDestination, "ci-runner", logs.DeliveryDestinationConfig{
    			Name:                      "ci-runner",
    			DeliveryDestinationPolicy: map[string]any{"delivery_destination_name": "example-delivery-destination-name"},
    			DeliveryDestinationType:   "example",
    			DestinationResourceArn:    "arn:aws:iam::123456789012:policy/ci",
    			OutputFormat:              "example",
    			Tags:                      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeliveryDestination } from "./local-sdk/aws/sdk/typescript/aws/logs/delivery_destination.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own delivery destination" });

      resource(DeliveryDestination, "ci-runner", {
        name: "ci-runner",
        deliveryDestinationPolicy: {
          deliveryDestinationName: "example-delivery-destination-name",
        },
        deliveryDestinationType: "example",
        destinationResourceArn: "arn:aws:iam::123456789012:policy/ci",
        outputFormat: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.logs import DeliveryDestination, DeliveryDestinationConfig

    def describe():
        ubx.intent("payments own delivery destination")

        ubx.resource(DeliveryDestination, "ci-runner", DeliveryDestinationConfig(
            name="ci-runner",
            delivery_destination_policy={"delivery_destination_name": "example-delivery-destination-name"},
            delivery_destination_type="example",
            destination_resource_arn="arn:aws:iam::123456789012:policy/ci",
            output_format="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_logs_delivery_destination called "ci-runner", with
    delivery destination policy a set value, delivery destination
    type example, destination resource arn
    arn:aws:iam::123456789012:policy/ci, output format example, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="delivery_destination_policy" type="object">
  The IAM resource policy attached to the delivery destination, specifying which AWS accounts or services are granted permission to deliver log events to this destination. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="delivery_destination_name" type="string">
      The name of the delivery destination to assign this policy to Optional.
    </ResponseField>
    <ResponseField name="delivery_destination_policy" type="any">
      The contents of the policy attached to the delivery destination Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delivery_destination_type" type="string">
  Displays whether this delivery destination is CloudWatch Logs, Amazon S3, Kinesis Data Firehose, or XRay. Optional.
</ResponseField>

<ResponseField name="destination_resource_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies a resource. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of this delivery destination. Required.
</ResponseField>

<ResponseField name="output_format" type="string">
  The format of the logs that are sent to this delivery destination. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags that have been assigned to this delivery destination. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      This field specifies the key of a tag applied to the CloudWatch Logs delivery destination, allowing you to categorize and filter the resource for cost tracking and access management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the CloudWatch Logs delivery destination, used for organizing, categorizing, and controlling access to the destination via IAM policies. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies a resource. Computed by AWS after `ship`.
</ResponseField>
