---
title: "aws_lightsail_bucket_resource_access"
description: "Real, generated bindings for `aws_lightsail_bucket_resource_access`."
---

`aws_lightsail_bucket_resource_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.BucketResourceAccessConfig{
        BucketName: "example-bucket-name",
        ResourceName: "example-resource-name",
    }
    ubx.Resource(lightsail.BucketResourceAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketResourceAccess } from
      "jsr:@ubx/sdk-aws/aws/lightsail/bucket_resource_access";

    resource(BucketResourceAccess, "example", {
      bucketName: "example",
      resourceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        BucketResourceAccess,
        BucketResourceAccessConfig,
    )

    cfg = BucketResourceAccessConfig(
        bucket_name="example",
        resource_name="example",
    )
    ubx.resource(BucketResourceAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_bucket_resource_access with bucket_name,
resource_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
