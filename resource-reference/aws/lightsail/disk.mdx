---
title: "aws_lightsail_disk"
description: "Real, generated bindings for `aws_lightsail_disk`."
---

`aws_lightsail_disk` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.DiskConfig{
        AvailabilityZone: "example",
        Name: "example-name",
        SizeInGb: 1,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(lightsail.Disk, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Disk } from "jsr:@ubx/sdk-aws/aws/lightsail/disk";

    resource(Disk, "example", {
      availabilityZone: "example",
      name: "example",
      sizeInGb: 1,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import Disk, DiskConfig

    cfg = DiskConfig(
        availability_zone="example",
        name="example",
        size_in_gb=1,
        tags={"key": "value"},
    )
    ubx.resource(Disk, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_disk with availability_zone, name,
size_in_gb, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="size_in_gb" type="number" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="support_code" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
