---
title: "aws_lightsail_container_service_deployment_version"
description: "Real, generated bindings for `aws_lightsail_container_service_deployment_version`."
---

`aws_lightsail_container_service_deployment_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own container service deployment version"})

    		ubx.Resource(lightsail.ContainerServiceDeploymentVersion, "ci-runner", lightsail.ContainerServiceDeploymentVersionConfig{
    			ServiceName: "example-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContainerServiceDeploymentVersion } from "jsr:@ubx/sdk-aws/aws/lightsail/container_service_deployment_version";

    export default stack("payments", () => {
      intent({ summary: "payments own container service deployment version" });

      resource(ContainerServiceDeploymentVersion, "ci-runner", {
        serviceName: "example-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import ContainerServiceDeploymentVersion, ContainerServiceDeploymentVersionConfig

    def describe():
        ubx.intent("payments own container service deployment version")

        ubx.resource(ContainerServiceDeploymentVersion, "ci-runner", ContainerServiceDeploymentVersionConfig(
            service_name="example-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_container_service_deployment_version, with
    service name example-service-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
