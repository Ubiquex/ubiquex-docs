---
title: "aws_lightsail_container_service_deployment_version"
description: "Real, generated bindings for `aws_lightsail_container_service_deployment_version`."
---

`aws_lightsail_container_service_deployment_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.ContainerServiceDeploymentVersionConfig{
        ServiceName: "example-service-name",
    }
    ubx.Resource(
        lightsail.ContainerServiceDeploymentVersion,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContainerServiceDeploymentVersion } from
      "jsr:@ubx/sdk-aws/aws/lightsail/container_service_deployment_version";

    resource(ContainerServiceDeploymentVersion, "example", {
      serviceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        ContainerServiceDeploymentVersion,
        ContainerServiceDeploymentVersionConfig,
    )

    cfg = ContainerServiceDeploymentVersionConfig(
        service_name="example",
    )
    ubx.resource(ContainerServiceDeploymentVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_container_service_deployment_version with
service_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
