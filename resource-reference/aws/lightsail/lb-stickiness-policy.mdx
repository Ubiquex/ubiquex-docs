---
title: "aws_lightsail_lb_stickiness_policy"
description: "Real, generated bindings for `aws_lightsail_lb_stickiness_policy`."
---

`aws_lightsail_lb_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.LbStickinessPolicyConfig{
        CookieDuration: 1,
        Enabled: true,
        LbName: "example-lb-name",
    }
    ubx.Resource(lightsail.LbStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LbStickinessPolicy } from
      "jsr:@ubx/sdk-aws/aws/lightsail/lb_stickiness_policy";

    resource(LbStickinessPolicy, "example", {
      cookieDuration: 1,
      enabled: true,
      lbName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        LbStickinessPolicy,
        LbStickinessPolicyConfig,
    )

    cfg = LbStickinessPolicyConfig(
        cookie_duration=1,
        enabled=True,
        lb_name="example",
    )
    ubx.resource(LbStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_lb_stickiness_policy with
cookie_duration, enabled, lb_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
