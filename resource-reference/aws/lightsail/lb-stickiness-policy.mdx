---
title: "aws_lightsail_lb_stickiness_policy"
description: "Real, generated bindings for `aws_lightsail_lb_stickiness_policy`."
---

`aws_lightsail_lb_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lb stickiness policy"})

    		ubx.Resource(lightsail.LbStickinessPolicy, "ci-runner", lightsail.LbStickinessPolicyConfig{
    			CookieDuration: 7200,
    			Enabled:        true,
    			LbName:         "example-lb-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LbStickinessPolicy } from "jsr:@ubx/sdk-aws/aws/lightsail/lb_stickiness_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own lb stickiness policy" });

      resource(LbStickinessPolicy, "ci-runner", {
        cookieDuration: 7200,
        enabled: true,
        lbName: "example-lb-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import LbStickinessPolicy, LbStickinessPolicyConfig

    def describe():
        ubx.intent("payments own lb stickiness policy")

        ubx.resource(LbStickinessPolicy, "ci-runner", LbStickinessPolicyConfig(
            cookie_duration=7200,
            enabled=True,
            lb_name="example-lb-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_lb_stickiness_policy, with cookie duration
    7200, enabled true, lb name example-lb-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
