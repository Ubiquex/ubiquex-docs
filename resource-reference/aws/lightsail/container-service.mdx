---
title: "aws_lightsail_container_service"
description: "Real, generated bindings for `aws_lightsail_container_service`."
---

`aws_lightsail_container_service` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.ContainerServiceConfig{
        Name: "example-name",
        Power: "example",
        Scale: 1,
        IsDisabled: true,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(lightsail.ContainerService, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContainerService } from
      "jsr:@ubx/sdk-aws/aws/lightsail/container_service";

    resource(ContainerService, "example", {
      name: "example",
      power: "example",
      scale: 1,
      isDisabled: true,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        ContainerService,
        ContainerServiceConfig,
    )

    cfg = ContainerServiceConfig(
        name="example",
        power="example",
        scale=1,
        is_disabled=True,
        tags={"key": "value"},
    )
    ubx.resource(ContainerService, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_container_service with name, power,
scale, is_disabled, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="power" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scale" type="number" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="power_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="principal_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="private_domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
