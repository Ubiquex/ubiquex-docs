---
title: "aws_lightsail_lb_https_redirection_policy"
description: "Real, generated bindings for `aws_lightsail_lb_https_redirection_policy`."
---

`aws_lightsail_lb_https_redirection_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lb https redirection policy"})

    		ubx.Resource(lightsail.LbHttpsRedirectionPolicy, "ci-runner", lightsail.LbHttpsRedirectionPolicyConfig{
    			Enabled: true,
    			LbName:  "example-lb-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LbHttpsRedirectionPolicy } from "jsr:@ubx/sdk-aws/aws/lightsail/lb_https_redirection_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own lb https redirection policy" });

      resource(LbHttpsRedirectionPolicy, "ci-runner", {
        enabled: true,
        lbName: "example-lb-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import LbHttpsRedirectionPolicy, LbHttpsRedirectionPolicyConfig

    def describe():
        ubx.intent("payments own lb https redirection policy")

        ubx.resource(LbHttpsRedirectionPolicy, "ci-runner", LbHttpsRedirectionPolicyConfig(
            enabled=True,
            lb_name="example-lb-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_lb_https_redirection_policy, with enabled
    true, lb name example-lb-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
