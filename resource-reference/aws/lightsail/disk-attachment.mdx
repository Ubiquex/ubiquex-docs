---
title: "aws_lightsail_disk_attachment"
description: "Real, generated bindings for `aws_lightsail_disk_attachment`."
---

`aws_lightsail_disk_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own disk attachment"})

    		ubx.Resource(lightsail.DiskAttachment, "ci-runner", lightsail.DiskAttachmentConfig{
    			DiskName:     "example-disk-name",
    			DiskPath:     "example",
    			InstanceName: "example-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DiskAttachment } from "jsr:@ubx/sdk-aws/aws/lightsail/disk_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own disk attachment" });

      resource(DiskAttachment, "ci-runner", {
        diskName: "example-disk-name",
        diskPath: "example",
        instanceName: "example-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import DiskAttachment, DiskAttachmentConfig

    def describe():
        ubx.intent("payments own disk attachment")

        ubx.resource(DiskAttachment, "ci-runner", DiskAttachmentConfig(
            disk_name="example-disk-name",
            disk_path="example",
            instance_name="example-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_disk_attachment, with disk name
    example-disk-name, disk path example, instance name
    example-instance-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disk_path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
