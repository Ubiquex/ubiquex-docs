---
title: "aws_lightsail_disk_attachment"
description: "Real, generated bindings for `aws_lightsail_disk_attachment`."
---

`aws_lightsail_disk_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.DiskAttachmentConfig{
        DiskName: "example-disk-name",
        DiskPath: "example",
        InstanceName: "example-instance-name",
    }
    ubx.Resource(lightsail.DiskAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DiskAttachment } from
      "jsr:@ubx/sdk-aws/aws/lightsail/disk_attachment";

    resource(DiskAttachment, "example", {
      diskName: "example",
      diskPath: "example",
      instanceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        DiskAttachment,
        DiskAttachmentConfig,
    )

    cfg = DiskAttachmentConfig(
        disk_name="example",
        disk_path="example",
        instance_name="example",
    )
    ubx.resource(DiskAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_disk_attachment with disk_name,
disk_path, instance_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disk_path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
