---
title: "aws_lightsail_domain_entry"
description: "Real, generated bindings for `aws_lightsail_domain_entry`."
---

`aws_lightsail_domain_entry` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"

    cfg := lightsail.DomainEntryConfig{
        DomainName: "example-domain-name",
        Name: "example-name",
        Target: "example",
        Type: "example",
        IsAlias: true,
    }
    ubx.Resource(lightsail.DomainEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainEntry } from
      "jsr:@ubx/sdk-aws/aws/lightsail/domain_entry";

    resource(DomainEntry, "example", {
      domainName: "example",
      name: "example",
      target: "example",
      type: "example",
      isAlias: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import DomainEntry, DomainEntryConfig

    cfg = DomainEntryConfig(
        domain_name="example",
        name="example",
        target="example",
        type="example",
        is_alias=True,
    )
    ubx.resource(DomainEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_domain_entry with domain_name, name,
target, type, is_alias.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_alias" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
