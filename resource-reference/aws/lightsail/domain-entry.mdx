---
title: "aws_lightsail_domain_entry"
description: "Real, generated bindings for `aws_lightsail_domain_entry`."
---

`aws_lightsail_domain_entry` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain entry"})

    		ubx.Resource(lightsail.DomainEntry, "ci-runner", lightsail.DomainEntryConfig{
    			DomainName: "example-domain-name",
    			Name:       "ci-runner",
    			Target:     "example",
    			Type:       "example",
    			IsAlias:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainEntry } from "jsr:@ubx/sdk-aws/aws/lightsail/domain_entry";

    export default stack("payments", () => {
      intent({ summary: "payments own domain entry" });

      resource(DomainEntry, "ci-runner", {
        domainName: "example-domain-name",
        name: "ci-runner",
        target: "example",
        type: "example",
        isAlias: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import DomainEntry, DomainEntryConfig

    def describe():
        ubx.intent("payments own domain entry")

        ubx.resource(DomainEntry, "ci-runner", DomainEntryConfig(
            domain_name="example-domain-name",
            name="ci-runner",
            target="example",
            type="example",
            is_alias=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_domain_entry called "ci-runner", with domain
    name example-domain-name, target example, type example, is alias
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_alias" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
