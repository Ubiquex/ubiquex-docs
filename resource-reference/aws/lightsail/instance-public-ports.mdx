---
title: "aws_lightsail_instance_public_ports"
description: "Real, generated bindings for `aws_lightsail_instance_public_ports`."
---

`aws_lightsail_instance_public_ports` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance public ports"})

    		ubx.Resource(lightsail.InstancePublicPorts, "ci-runner", lightsail.InstancePublicPortsConfig{
    			InstanceName: "example-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstancePublicPorts } from "jsr:@ubx/sdk-aws/aws/lightsail/instance_public_ports";

    export default stack("payments", () => {
      intent({ summary: "payments own instance public ports" });

      resource(InstancePublicPorts, "ci-runner", {
        instanceName: "example-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import InstancePublicPorts, InstancePublicPortsConfig

    def describe():
        ubx.intent("payments own instance public ports")

        ubx.resource(InstancePublicPorts, "ci-runner", InstancePublicPortsConfig(
            instance_name="example-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_instance_public_ports, with instance name
    example-instance-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
