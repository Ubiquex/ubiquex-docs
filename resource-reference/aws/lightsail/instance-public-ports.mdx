---
title: "aws_lightsail_instance_public_ports"
description: "Real, generated bindings for `aws_lightsail_instance_public_ports`."
---

`aws_lightsail_instance_public_ports` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.InstancePublicPortsConfig{
        InstanceName: "example-instance-name",
    }
    ubx.Resource(lightsail.InstancePublicPorts, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstancePublicPorts } from
      "jsr:@ubx/sdk-aws/aws/lightsail/instance_public_ports";

    resource(InstancePublicPorts, "example", {
      instanceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        InstancePublicPorts,
        InstancePublicPortsConfig,
    )

    cfg = InstancePublicPortsConfig(
        instance_name="example",
    )
    ubx.resource(InstancePublicPorts, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_instance_public_ports with instance_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
