---
title: "aws_lightsail_static_ip_attachment"
description: "Real, generated bindings for `aws_lightsail_static_ip_attachment`."
---

`aws_lightsail_static_ip_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own static ip attachment"})

    		ubx.Resource(lightsail.StaticIpAttachment, "ci-runner", lightsail.StaticIpAttachmentConfig{
    			InstanceName: "example-instance-name",
    			StaticIpName: "example-static-ip-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StaticIpAttachment } from "jsr:@ubx/sdk-aws/aws/lightsail/static_ip_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own static ip attachment" });

      resource(StaticIpAttachment, "ci-runner", {
        instanceName: "example-instance-name",
        staticIpName: "example-static-ip-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import StaticIpAttachment, StaticIpAttachmentConfig

    def describe():
        ubx.intent("payments own static ip attachment")

        ubx.resource(StaticIpAttachment, "ci-runner", StaticIpAttachmentConfig(
            instance_name="example-instance-name",
            static_ip_name="example-static-ip-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_static_ip_attachment, with instance name
    example-instance-name, static ip name example-static-ip-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="static_ip_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
