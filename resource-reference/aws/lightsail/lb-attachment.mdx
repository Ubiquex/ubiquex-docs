---
title: "aws_lightsail_lb_attachment"
description: "Real, generated bindings for `aws_lightsail_lb_attachment`."
---

`aws_lightsail_lb_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lightsail"

    cfg := lightsail.LbAttachmentConfig{
        InstanceName: "example-instance-name",
        LbName: "example-lb-name",
    }
    ubx.Resource(lightsail.LbAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LbAttachment } from
      "jsr:@ubx/sdk-aws/aws/lightsail/lb_attachment";

    resource(LbAttachment, "example", {
      instanceName: "example",
      lbName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import LbAttachment, LbAttachmentConfig

    cfg = LbAttachmentConfig(
        instance_name="example",
        lb_name="example",
    )
    ubx.resource(LbAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_lb_attachment with instance_name,
lb_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
