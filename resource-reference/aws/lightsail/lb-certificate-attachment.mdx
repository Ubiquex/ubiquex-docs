---
title: "aws_lightsail_lb_certificate_attachment"
description: "Real, generated bindings for `aws_lightsail_lb_certificate_attachment`."
---

`aws_lightsail_lb_certificate_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"

    cfg := lightsail.LbCertificateAttachmentConfig{
        CertificateName: "example-certificate-name",
        LbName: "example-lb-name",
    }
    ubx.Resource(lightsail.LbCertificateAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LbCertificateAttachment } from
      "jsr:@ubx/sdk-aws/aws/lightsail/lb_certificate_attachment";

    resource(LbCertificateAttachment, "example", {
      certificateName: "example",
      lbName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        LbCertificateAttachment,
        LbCertificateAttachmentConfig,
    )

    cfg = LbCertificateAttachmentConfig(
        certificate_name="example",
        lb_name="example",
    )
    ubx.resource(LbCertificateAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_lb_certificate_attachment with
certificate_name, lb_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
