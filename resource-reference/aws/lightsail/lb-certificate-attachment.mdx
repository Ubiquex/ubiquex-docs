---
title: "aws_lightsail_lb_certificate_attachment"
description: "Real, generated bindings for `aws_lightsail_lb_certificate_attachment`."
---

`aws_lightsail_lb_certificate_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lb certificate attachment"})

    		ubx.Resource(lightsail.LbCertificateAttachment, "ci-runner", lightsail.LbCertificateAttachmentConfig{
    			CertificateName: "example-certificate-name",
    			LbName:          "example-lb-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LbCertificateAttachment } from "jsr:@ubx/sdk-aws/aws/lightsail/lb_certificate_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own lb certificate attachment" });

      resource(LbCertificateAttachment, "ci-runner", {
        certificateName: "example-certificate-name",
        lbName: "example-lb-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import LbCertificateAttachment, LbCertificateAttachmentConfig

    def describe():
        ubx.intent("payments own lb certificate attachment")

        ubx.resource(LbCertificateAttachment, "ci-runner", LbCertificateAttachmentConfig(
            certificate_name="example-certificate-name",
            lb_name="example-lb-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_lb_certificate_attachment, with certificate
    name example-certificate-name, lb name example-lb-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
