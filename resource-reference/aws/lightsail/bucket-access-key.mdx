---
title: "aws_lightsail_bucket_access_key"
description: "Real, generated bindings for `aws_lightsail_bucket_access_key`."
---

`aws_lightsail_bucket_access_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"

    cfg := lightsail.BucketAccessKeyConfig{
        BucketName: "example-bucket-name",
    }
    ubx.Resource(lightsail.BucketAccessKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketAccessKey } from
      "jsr:@ubx/sdk-aws/aws/lightsail/bucket_access_key";

    resource(BucketAccessKey, "example", {
      bucketName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import (
        BucketAccessKey,
        BucketAccessKeyConfig,
    )

    cfg = BucketAccessKeyConfig(
        bucket_name="example",
    )
    ubx.resource(BucketAccessKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_bucket_access_key with bucket_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="access_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_access_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
