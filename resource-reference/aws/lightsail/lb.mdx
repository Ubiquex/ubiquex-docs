---
title: "aws_lightsail_lb"
description: "Real, generated bindings for `aws_lightsail_lb`."
---

`aws_lightsail_lb` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"

    cfg := lightsail.LbConfig{
        InstancePort: 1,
        Name: "example-name",
        HealthCheckPath: "example",
        IpAddressType: "example",
    }
    ubx.Resource(lightsail.Lb, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Lb } from "jsr:@ubx/sdk-aws/aws/lightsail/lb";

    resource(Lb, "example", {
      instancePort: 1,
      name: "example",
      healthCheckPath: "example",
      ipAddressType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lightsail import Lb, LbConfig

    cfg = LbConfig(
        instance_port=1,
        name="example",
        health_check_path="example",
        ip_address_type="example",
    )
    ubx.resource(Lb, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lightsail_lb with instance_port, name,
health_check_path, ip_address_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="health_check_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public_ports" type="list(number)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="support_code" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
