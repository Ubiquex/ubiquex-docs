---
title: "aws_code_pipeline_pipeline"
description: "`aws_code_pipeline_pipeline` models the real, automated stages, source, build, test, deploy, that a code change moves through on its way to release..."
---

`aws_code_pipeline_pipeline` models the real, automated stages, source, build, test, deploy, that a code change moves through on its way to release, the core, top-level resource every other CodePipeline resource attaches to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pipeline pipeline"})

    		ubx.Resource(code.PipelinePipeline, "ci-runner", code.PipelinePipelineConfig{
    			RoleArn:                        "arn:aws:iam::123456789012:policy/ci",
    			Stages:                         []map[string]any{{"name": "example-name"}},
    			Name:                           "ci-runner",
    			ArtifactStore:                  map[string]any{"location": "example"},
    			ArtifactStores:                 []map[string]any{{"artifact_store": map[string]any{"encryption_key": map[string]any{"id": "example"}}}},
    			DisableInboundStageTransitions: []map[string]any{{"reason": "example"}},
    			ExecutionMode:                  "example",
    			PipelineType:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PipelinePipeline } from "./local-sdk/aws/sdk/typescript/aws/code/pipeline_pipeline.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own pipeline pipeline" });

      resource(PipelinePipeline, "ci-runner", {
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        stages: [{ name: "example-name" }],
        name: "ci-runner",
        artifactStore: { location: "example" },
        artifactStores: [{ artifactStore: { encryptionKey: { id: "example" } } }],
        disableInboundStageTransitions: [{ reason: "example" }],
        executionMode: "example",
        pipelineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.code import PipelinePipeline, PipelinePipelineConfig

    def describe():
        ubx.intent("payments own pipeline pipeline")

        ubx.resource(PipelinePipeline, "ci-runner", PipelinePipelineConfig(
            role_arn="arn:aws:iam::123456789012:policy/ci",
            stages=[{"name": "example-name"}],
            name="ci-runner",
            artifact_store={"location": "example"},
            artifact_stores=[{"artifact_store": {"encryption_key": {"id": "example"}}}],
            disable_inbound_stage_transitions=[{"reason": "example"}],
            execution_mode="example",
            pipeline_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_code_pipeline_pipeline called "ci-runner", with role
    arn arn:aws:iam::123456789012:policy/ci, stages a set value,
    artifact store a set value, artifact stores a set value, disable
    inbound stage transitions a set value, execution mode example,
    pipeline type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="artifact_store" type="object">
  The S3 bucket where artifacts for the pipeline are stored. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption_key" type="object">
      Represents information about the key used to encrypt data in the artifact store, such as an AWS Key Management Service (AWS KMS) key Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string" required>
          The ID used to identify the key. For an AWS KMS key, you can use the key ID, the key ARN, or the alias ARN. Required.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of encryption key, such as an AWS KMS key. When creating or updating a pipeline, the value must be set to 'KMS'. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="location" type="string" required>
      The S3 bucket used for storing the artifacts for a pipeline. You can specify the name of an S3 bucket but not a folder in the bucket. A folder to contain the pipeline artifacts is created for you based on the name of the pipeline. You can use any S3 bucket in the same AWS Region as the pipeline to store your pipeline artifacts. Required.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the artifact store, such as S3. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="artifact_stores" type="list(object)">
  A mapping of artifactStore objects and their corresponding AWS Regions. There must be an artifact store for the pipeline Region and for each cross-region action in the pipeline. Optional.
  <Expandable title="properties">
    <ResponseField name="artifact_store" type="object">
      This object defines a single artifact store for the pipeline, specifying the S3 bucket name (location) and type, along with optional encryption key settings, used to store input and output artifacts for the CodePipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="encryption_key" type="object">
          Specifies the customer-managed AWS KMS key (by ID and type) that CodePipeline uses to encrypt the artifacts stored in the S3 bucket for this artifact store. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The AWS KMS key ID or ARN of the KMS key used to encrypt the S3 artifact store for the CodePipeline pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of encryption key used for the artifact store, which must be 'KMS' for an AWS KMS key or 'S3' for an Amazon S3-managed key. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="location" type="string">
          The ARN or name of the S3 bucket that CodePipeline uses as the artifact store for the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of the artifact store, which must be `S3` for the pipeline to use an Amazon S3 bucket for storing input and output artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="region" type="string">
      The AWS region in which the S3 bucket that stores pipeline artifacts is located, enabling cross-region artifact stores. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="disable_inbound_stage_transitions" type="list(object)">
  Represents the input of a DisableStageTransition action. Optional.
  <Expandable title="properties">
    <ResponseField name="reason" type="string">
      The user-provided reason explaining why inbound transition to the stage is disabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stage_name" type="string">
      The name of a stage in the AWS CodePipeline pipeline for which inbound stage transitions are disabled, preventing automatic execution from preceding stages into this stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="execution_mode" type="string">
  The method that the pipeline will use to handle multiple executions. The default mode is SUPERSEDED. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the pipeline. Optional.
</ResponseField>

<ResponseField name="pipeline_type" type="string">
  CodePipeline provides the following pipeline types, which differ in characteristics and price, so that you can tailor your pipeline features and cost to the needs of your applications. Optional.
</ResponseField>

<ResponseField name="restart_execution_on_update" type="bool">
  Indicates whether to rerun the CodePipeline pipeline after you update it. Optional.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The Amazon Resource Name (ARN) for CodePipeline to use to either perform actions with no actionRoleArn, or to use to assume roles for actions with an actionRoleArn Required.
</ResponseField>

<ResponseField name="stages" type="list(object)" required>
  Represents information about a stage and its definition. Required.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(object)">
      Lists the actions that are part of a stage in the CodePipeline pipeline, where each action defines its type (e.g., Source, Build, Deploy), configuration, and run order within the stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action_type_id" type="object">
          Identifies the action type for a CodePipeline action by specifying its category, owner, provider, and version (e.g., Source, AWS, CodeCommit, 1). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="category" type="string">
              The category of the action, such as Source, Build, Deploy, Test, Invoke, or Approval, which identifies the type of action and determines how it is executed in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="owner" type="string">
              The owner field specifies which entity provides the action type (AWS, ThirdParty, or Custom) used by the stage action in the CodePipeline pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="provider" type="string">
              The name of the service or action provider that performs the action, such as CodeDeploy, Lambda, or S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="version" type="string">
              The version of the action type, which identifies the specific revision of the action provider (e.g., '1' for the default version of AWS-managed actions). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="commands" type="list(string)">
          List of shell commands that the Command action runs when this stage action is executed in the CodePipeline pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="configuration" type="any">
          Specifies the provider-specific configuration parameters for the CodePipeline action, such as Owner, Repo, and Branch for a GitHub source action, or other service-specific settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="environment_variables" type="list(object)">
          Specifies a list of environment variables, each defined by a name, value, and optional type (such as PLAINTEXT or PARAMETER_STORE), that are passed to the action's provider for use during its execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name (key) of the environment variable defined for an action, which is used to reference it when the action runs in AWS CodePipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of the environment variable, which can be PLAINTEXT, PARAMETER_STORE, or SECRETS_MANAGER, indicating whether the value is a plaintext string, a reference to an AWS Systems Manager Parameter Store parameter, or a reference to an AWS Secrets Manager secret. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Specifies the value of an environment variable passed to the action, which can be a static string or a reference to a CodePipeline variable expression (e.g., #&#123;SourceVariables.CommitId&#125;) for dynamic substitution. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="input_artifacts" type="list(object)">
          Specifies the list of input artifacts for the action, where each artifact's Name property must match the name of an output artifact from a source or previous action in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the artifact that this action consumes as an input, which must match an output artifact from an upstream action in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Specifies the name of the action, which must be unique within its stage and is used to identify the action in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace" type="string">
          Defines the variable namespace for the action, which is used to expose action variables to subsequent actions in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="output_artifacts" type="list(object)">
          Specifies the list of artifacts that this action produces as outputs, making them available to downstream actions as input artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="files" type="list(string)">
              Specifies the list of file paths that the output artifact will contain, allowing you to select only the relevant files produced by the action for use by downstream actions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the output artifact, which downstream actions reference in their input artifacts to consume the output. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_variables" type="list(string)">
          List of variable names that the action exports to the pipeline, allowing downstream actions to reference their values using the #&#123;...&#125; syntax. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="region" type="string">
          The AWS region in which the action is executed; if omitted, the pipeline's region is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the IAM role that AWS CodePipeline assumes when performing the action, overriding the pipeline-level service role for this specific action. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="run_order" type="number">
          The relative order in which this action runs within its stage, where actions with lower runOrder values execute first. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timeout_in_minutes" type="number">
          The number of minutes that AWS CodePipeline waits for the action to complete before considering it failed, overriding the action type's default timeout. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="before_entry" type="object">
      Defines the before-entry conditions for a stage, which are rules that must be satisfied for the stage's actions to begin, enabling entry gates on the pipeline stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="conditions" type="list(object)">
          Specifies the list of conditions that must be satisfied before the stage is allowed to start, enabling entry gates based on variables, actions, or stage-level rules. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="result" type="string">
              Specifies the outcome (e.g., 'ROLLBACK' or 'FAIL') that CodePipeline applies to the stage when the condition's rules are not satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rules" type="list(object)">
              Lists the rules that CodePipeline evaluates in the stage's before-entry conditions; each rule defines a command (such as a Lambda function or shell command) to run with an associated IAM role, and all rules must succeed for the pipeline to enter the stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="commands" type="list(string)">
                  A list of shell commands that the before-entry rule runs as part of the condition evaluation in a CodePipeline stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="configuration" type="any">
                  The configuration map of rule-provider-specific key-value pairs (schema determined by the rule's type) that CodePipeline passes to the rule when evaluating a stage's before-entry condition, controlling whether the stage is allowed to run. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="input_artifacts" type="list(object)">
                  Specifies the names of the artifacts passed to a rule within a stage's before-entry condition, providing the rule with the pipeline artifacts it needs to evaluate whether the stage can proceed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the artifact that the rule consumes as input, matching an artifact produced by an upstream stage in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of a rule in a before-entry condition for a stage, uniquely identifying the rule within that condition in the AWS CodePipeline pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="region" type="string">
                  Specifies the AWS region in which this rule of the stage's before-entry condition is evaluated, enabling the rule to run in a particular regional context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="role_arn" type="string">
                  The ARN of the IAM role that AWS CodePipeline assumes to evaluate the rule as part of the before-entry condition of a pipeline stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rule_type_id" type="object">
                  Identifies the rule type by specifying its category, owner, provider, and version, determining which rule engine executes the condition check. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="category" type="string">
                      The category of the rule type identifier, which must be 'Rule' for rules used in CodePipeline stage conditions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="owner" type="string">
                      Specifies the owner of the rule provider, which can be AWS, a third-party partner, or a custom rule, indicating who authored and maintains the rule used in the stage's before_entry condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="provider" type="string">
                      The provider name of the rule type (e.g., ImageScanningRule, DeploymentWindow) that identifies which rule service evaluates the before-entry condition for the pipeline stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="version" type="string">
                      Specifies the version of the rule provider invoked by CodePipeline when this condition rule runs, part of the rule type identifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="blockers" type="list(object)">
      This is the list of blocker declarations for a pipeline stage, each with a name and a type (the only supported type is 'Schedule'), which act as manual gates that prevent the pipeline from proceeding past the stage until the blocker is removed or unblocked. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Specifies the name of the blocker, a user-defined gate that must be manually acknowledged or cleared before the stage can continue to run. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of a pipeline blocker, which in AWS CodePipeline is set to `Schedule` to define a time-based blocker that pauses the stage until a scheduled time is reached. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the stage, which uniquely identifies it within the pipeline and is used to reference the stage in pipeline configuration and execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="on_failure" type="object">
      Configures the failure behavior for the stage, including the condition that triggers the failure handling (e.g., ABORT, RETRY, ROLLBACK) and optional retry settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="conditions" type="list(object)">
          Specifies the list of conditions that must be satisfied for the stage's on-failure handling (such as rollback) to be triggered, where each condition includes a type like 'ExternalTrigger' and its configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="result" type="string">
              Specifies the stage execution result (either FAILED or ABORTED) that triggers the on-failure condition behavior for the stage in AWS CodePipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rules" type="list(object)">
              Specifies the list of rule definitions that AWS CodePipeline evaluates when the stage's on-failure condition is triggered, including each rule's name, rule type ID, and optional configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="commands" type="list(string)">
                  In an AWS CodePipeline stage's on-failure condition, this field specifies the list of shell commands executed by the rule to evaluate whether the condition is satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="configuration" type="any">
                  A dynamic map of key-value pairs that provides rule-specific configuration settings to the rule when the stage's on_failure condition is evaluated, where the allowed keys vary by the rule's provider and version (for example, for a Lambda rule the map may include FunctionName and UserParameters). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="input_artifacts" type="list(object)">
                  Lists the artifacts that the rule, defined under a stage's on-failure conditions, receives as input to evaluate whether the failure condition is satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of an artifact that serves as input to a rule within a stage's on-failure condition, referencing an artifact output from an upstream stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the rule in the on-failure condition of a stage, used as a unique identifier for that rule within the condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="region" type="string">
                  Specifies the AWS Region in which the rule action is executed, allowing the rule provider (e.g., a Lambda function or managed rule resource) to be invoked in that designated regional scope. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="role_arn" type="string">
                  The ARN of the IAM role that AWS CodePipeline assumes to run the rule defined in the stage's failure condition, which determines when the stage transition fails. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rule_type_id" type="object">
                  Defines the rule type identifier (category, owner, provider, and version) that determines which AWS CodePipeline rule is invoked in the condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="category" type="string">
                      The category of the rule type, which must be 'Rule' for CodePipeline rules. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="owner" type="string">
                      Identifies the owner of the rule type (e.g., AWS or a third-party/custom owner) for a rule used in a stage's on-failure condition, determining which provider's rule implementation is invoked. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="provider" type="string">
                      The name of the provider that implements the rule, which together with the category, owner, and version identifies the rule type in AWS CodePipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="version" type="string">
                      The version of the rule provider (e.g., '1' or '2') that identifies which implementation of the rule to use for this condition rule in the pipeline's on-failure stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="result" type="string">
          Determines the pipeline behavior when a stage fails: set to FAILED to stop and fail the pipeline, or ROLLBACK to roll the stage back to its last successful checkpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="retry_configuration" type="object">
          Specifies the retry configuration for a stage's on-failure behavior, including the retry mode (e.g., ALL_ACTIONS) that determines which failed actions are retried automatically. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="retry_mode" type="string">
              Specifies whether retries on a stage failure apply only to failed actions (FAILED_ACTIONS) or to all actions in the stage (ALL_ACTIONS). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="on_success" type="object">
      Specifies the success conditions for a stage in the pipeline, including a list of conditions and the evaluation rule (ALL or ANY) that determines whether the stage is considered successful before the pipeline proceeds. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="conditions" type="list(object)">
          Specifies the list of conditions evaluated when a stage completes, where each condition contains rules to be checked and a required result value (such as APPROVED) that determines whether the stage is considered successful and the pipeline proceeds to the next stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="result" type="string">
              In a pipeline stage's on-success conditions, this field specifies the outcome (either ROLLBACK or FAIL) that occurs if the condition is not met. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="rules" type="list(object)">
              The list of rules that AWS CodePipeline evaluates as part of the stage's on_success condition; each rule defines a command or condition that must be satisfied for the stage to be considered successfully completed. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="commands" type="list(string)">
                  For a Command-type rule within a stage's onSuccess condition, this list defines the shell commands that the rule executes. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="configuration" type="any">
                  The set of provider-specific configuration parameters for the rule, used by the rule provider to evaluate the stage's on_success condition (e.g., inputs or thresholds for the rule to succeed). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="input_artifacts" type="list(object)">
                  Specifies the artifacts from the stage's action outputs that are passed as inputs to the rule when evaluating the on-success condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the artifact that the rule consumes as its input, which must match the artifact name declared by an upstream action's output artifact in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of a rule in an on-success condition for a stage in AWS CodePipeline, which identifies the rule and must be unique within the condition's rule list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="region" type="string">
                  Specifies the AWS region in which the rule action for this on-success condition rule is executed, overriding the pipeline's default region. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="role_arn" type="string">
                  The ARN of the IAM role that AWS CodePipeline assumes to execute the rule defined in the on_success condition of the stage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="rule_type_id" type="object">
                  The rule type identifier that specifies the category, owner, provider, and version of the rule to evaluate in the success condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="category" type="string">
                      Specifies the high-level rule category (such as 'Rule' for AWS-managed CodePipeline rules) of the rule type used in the stage success condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="owner" type="string">
                      Identifies the entity that owns the rule type, such as AWS for AWS-provided rules, Custom for custom rules, or a third-party provider, used to uniquely identify the rule within the pipeline's stage conditions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="provider" type="string">
                      Specifies the provider (such as 'AWS') of the rule type, identifying the service or partner that implements the rule in the pipeline condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="version" type="string">
                      Specifies the version of the rule provider (for example, '1') that identifies which iteration of the rule's logic to use when the rule runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the tags applied to the pipeline. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value for a tag key attached to the AWS CodePipeline pipeline, used to organize and identify the pipeline for cost allocation and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="triggers" type="list(object)">
  The trigger configuration specifying a type of event, such as Git tags, that starts the pipeline. Optional.
  <Expandable title="properties">
    <ResponseField name="git_configuration" type="object">
      Specifies the Git configuration for a trigger, including the source action name and the pull request and push event settings that determine when the pipeline runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="pull_request" type="list(object)">
          Specifies the pull request filter rules for a Git-based source connection that determine which pull request events trigger the pipeline, based on branch and event criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="branches" type="object">
              Specifies the branch filter criteria (include/exclude lists of branch names) that determine which pull request events trigger the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excludes" type="list(string)">
                  Specifies the list of branch names to exclude from triggering the pipeline for pull requests, so pull requests from these branches will not start the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="includes" type="list(string)">
                  Specifies the branch names or patterns for which pull requests trigger the pipeline, so that only PRs targeting these branches start an execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="events" type="list(string)">
              The pull request events (such as OPEN, UPDATED, or CLOSED) that cause the pipeline to run when they occur on the configured Git repository. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_paths" type="object">
              This field specifies a list of glob patterns for file paths that must be changed in a pull request to trigger the pipeline execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excludes" type="list(string)">
                  Specifies a list of file path patterns (glob-style) that, if matched by any changed file in the pull request, exclude the pull request from triggering the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="includes" type="list(string)">
                  Specifies the glob patterns for file paths in the source repository that must be changed for the pull request trigger to invoke the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="push" type="list(object)">
          Specifies a list of Git push filters, each defining branch and/or file-path patterns that control which code changes pushed to the connected source repository automatically trigger the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="branches" type="object">
              Defines the branch names of the source repository that a push must occur on to trigger the pipeline execution, allowing you to limit the push trigger to specific branches. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excludes" type="list(string)">
                  A list of branch names or patterns that are excluded from triggering the pipeline when a push is made to those branches. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="includes" type="list(string)">
                  Defines the list of branch name patterns (e.g., 'main' or 'feature/*') that must match for a push event to trigger the pipeline run. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="file_paths" type="object">
              Defines the file-path filter criteria (with include/exclude glob patterns) that determines which changed files in a pushed commit trigger the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excludes" type="list(string)">
                  This field defines a list of glob patterns for file paths that are excluded from push trigger evaluation, meaning changes to files matching these patterns will not start the pipeline even if they match the included patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="includes" type="list(string)">
                  Specifies the file path patterns that must match for the push event to trigger the pipeline, so the pipeline runs only when commits change files matching any of these patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="tags" type="object">
              Defines the inclusion and exclusion criteria for Git tags that are evaluated on push events to determine whether the pipeline should trigger. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="excludes" type="list(string)">
                  A list of git tag names that, when matched on a push event, prevent the pipeline trigger from executing. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="includes" type="list(string)">
                  Specifies the list of Git tag names that must match the pushed tag in order for the push event to activate this trigger and start the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_action_name" type="string">
          The name of the source action in the pipeline that this trigger watches for Git events. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provider_type" type="string">
      Specifies the type of event source for the pipeline trigger, such as CodeStarSourceConnection for an external Git-based source connection or CloudWatchEvent for an Amazon EventBridge (CloudWatch Events) rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="variables" type="list(object)">
  A list that defines the pipeline variables for a pipeline resource. Variable names can have alphanumeric and underscore characters, and the values must match [A-Za-z0-9@\-_]+. Optional.
  <Expandable title="properties">
    <ResponseField name="default_value" type="string">
      The default value for a CodePipeline pipeline variable, which is used when the variable is not provided a value during a pipeline run. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Provides an optional description that explains the purpose of a pipeline-level variable defined in the AWS CodePipeline pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The unique name of the pipeline variable, used to reference the variable in pipeline configuration, actions, and within the execution environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the pipeline. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  The version of the pipeline. Computed by AWS after `ship`.
</ResponseField>
