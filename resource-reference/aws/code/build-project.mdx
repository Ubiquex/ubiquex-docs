---
title: "aws_code_build_project"
description: "`aws_code_build_project` defines a real, fully managed build, its source, build environment, and buildspec, that AWS CodeBuild compiles, tests..."
---

`aws_code_build_project` defines a real, fully managed build, its source, build environment, and buildspec, that AWS CodeBuild compiles, tests, and packages into deployable artifacts, run standalone or as a stage in an `aws_code_pipeline_pipeline`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own build project"})

    		ubx.Resource(code.BuildProject, "example", code.BuildProjectConfig{
    			Artifacts:        map[string]any{"name": "example-name"},
    			Environment:      map[string]any{"compute_type": "example"},
    			ServiceRole:      "example",
    			Source:           map[string]any{"type": "example"},
    			Name:             "example",
    			AutoRetryLimit:   1,
    			BadgeEnabled:     true,
    			BuildBatchConfig: map[string]any{"batch_report_mode": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BuildProject } from "./local-sdk/aws/sdk/typescript/aws/code/build_project.ts";

    export default stack("example", () => {
      intent({ summary: "example own build project" });

      resource(BuildProject, "example", {
        artifacts: { name: "example-name" },
        environment: { computeType: "example" },
        serviceRole: "example",
        source: { type: "example" },
        name: "example",
        autoRetryLimit: 1,
        badgeEnabled: true,
        buildBatchConfig: { batchReportMode: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.code import BuildProject, BuildProjectConfig

    def describe():
        ubx.intent("example own build project")

        ubx.resource(BuildProject, "example", BuildProjectConfig(
            artifacts={"name": "example-name"},
            environment={"compute_type": "example"},
            service_role="example",
            source={"type": "example"},
            name="example",
            auto_retry_limit=1,
            badge_enabled=True,
            build_batch_config={"batch_report_mode": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_code_build_project called "example", with artifacts a
    set value, environment a set value, service role example, source
    a set value, auto retry limit 1, badge enabled true, build batch
    config a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="artifacts" type="object" required>
  Defines where CodeBuild stores the build output, including artifact type (e.g., S3), location, path, packaging, and optional encryption settings. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="artifact_identifier" type="string">
      Specifies an identifier for this output artifact so it can be matched against the artifact outputs declared in the project's buildspec when a build produces multiple artifact sets. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="encryption_disabled" type="bool">
      Indicates whether encryption for the build output artifacts is disabled, allowing artifacts to be stored in S3 without server-side encryption. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      Specifies the name of the S3 bucket where CodeBuild stores build output artifacts when the artifact type is set to S3. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the build output artifact, which for S3 artifact type is the file name that CodeBuild writes to the output bucket. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace_type" type="string">
      Determines whether the build output artifacts are stored in a path that includes the build ID (as a subdirectory) or without it, affecting the S3 key structure for the artifact location. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_artifact_name" type="bool">
      When true, the name of the build output artifact is overridden by the artifact name specified in the buildspec (e.g., via the `name` property of the buildspec's `artifacts` section); otherwise, CodeBuild uses the project name and build ID as the artifact name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="packaging" type="string">
      Determines whether the build output artifacts are packaged into a ZIP archive (ZIP) or left as raw files (NONE) when uploaded to the artifact repository, such as an S3 bucket. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      The path within the S3 bucket where the build output artifact is stored, relative to the bucket root; if omitted, the artifact is placed at the bucket root. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Specifies the type of build output artifact, which can be set to CODEPIPELINE to have CodePipeline manage the artifact, S3 to store the output in an S3 bucket, or NO_ARTIFACTS to not generate an artifact. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_retry_limit" type="number">
  Sets the maximum number of times CodeBuild automatically retries a failed build as part of the project's batch build configuration. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="badge_enabled" type="bool">
  Indicates whether the CodeBuild project has a build badge enabled, which provides an embeddable URL for showing the build's status in an external repository. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="build_batch_config" type="object">
  Configures the batch build settings for the AWS CodeBuild project, including the service role, timeout, maximum number of builds, and restrictions for running builds in a batch. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="batch_report_mode" type="string">
      Specifies how AWS CodeBuild generates test reports for batch builds, either as a single aggregated report for the entire batch or as separate reports for each individual build. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="combine_artifacts" type="bool">
      Whether to combine all build artifacts from the batch into a single artifact location. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="restrictions" type="object">
      Specifies the restrictions for a batch build, including the maximum number of builds that can run concurrently and the compute types allowed for the batch build. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="compute_types_allowed" type="list(string)">
          Restricts the batch build to only use the compute types (e.g., BUILD_GENERAL1_SMALL, BUILD_GENERAL1_MEDIUM) listed here for its individual builds; if the list is empty, all compute types are allowed. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="maximum_builds_allowed" type="number">
          Specifies the maximum number of builds that can be executed concurrently in a batch build for this CodeBuild project. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_role" type="string">
      Specifies the ARN of the IAM service role that AWS CodeBuild assumes to run batch builds for this project. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="timeout_in_mins" type="number">
      The number of minutes after which a batch build is considered expired and is cancelled. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cache" type="object">
  Configures the build cache for the CodeBuild project, allowing specification of either an S3 bucket for caching or a local cache with cache modes such as LOCAL_SOURCE_CACHE, LOCAL_CUSTOM_CACHE, and LOCAL_DOCKER_LAYER_CACHE. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cache_namespace" type="string">
      The cache namespace groups build output cache entries so that multiple build projects sharing the same namespace can reuse the same cache. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      The S3 bucket path (e.g., 'bucket-name/path') where CodeBuild stores the cache when the cache type is S3. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="modes" type="list(string)">
      Specifies the local cache modes (e.g., LOCAL_SOURCE_CACHE, LOCAL_DOCKER_LAYER_CACHE, LOCAL_CUSTOM_CACHE) to enable for the build project when using a local cache type. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of cache to use for the build project, such as NO_CACHE, S3, or LOCAL, which determines how build outputs and dependencies are cached. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="concurrent_build_limit" type="number">
  Maximum number of concurrent builds that can run for this CodeBuild project. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A short, optional description of the CodeBuild build project. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="encryption_key" type="string">
  The ARN of the AWS KMS customer master key (CMK) that CodeBuild uses to encrypt the build output artifacts; if not specified, the default AWS-managed key for Amazon S3 is used. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="environment" type="object" required>
  Specifies the build environment, including the compute type, container image, environment variables, and whether to use privileged mode for the CodeBuild project. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="certificate" type="string">
      The ARN of an AWS Certificate Manager certificate that AWS CodeBuild associates with the project's build environment to support cryptographic operations such as signing build artifacts. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="compute_type" type="string" required>
      Specifies the compute resources for the build environment (for example, BUILD_GENERAL1_SMALL or BUILD_GENERAL1_LARGE), determining the CPU and memory allocated to the build job. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="docker_server" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="compute_type" type="string" required>
          Required.
        </ResponseField>
        <ResponseField name="security_group_ids" type="list(string)">
          The list of security group IDs to associate with the Docker server used by the CodeBuild build environment, controlling network access to the Docker server. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_variables" type="list(object)">
      Defines custom environment variables passed to the build container, each specifying a name, value, and optional type such as PLAINTEXT, PARAMETER_STORE, or SECRETS_MANAGER. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the environment variable to be set in the CodeBuild environment, used as the key to reference the variable in build commands. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the environment variable, which can be PLAINTEXT, PARAMETER_STORE, or SECRETS_MANAGER, determining how the variable's value is interpreted and retrieved. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the value of the environment variable that is injected into the CodeBuild container at build time, which can be a plain string or, when the variable's type is PARAMETER_STORE, the name of a Systems Manager Parameter Store parameter whose value is resolved at runtime. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fleet" type="object">
      Specifies the compute fleet (via its ARN) that provides the compute resources for this CodeBuild project environment. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="fleet_arn" type="string">
          The ARN of the compute fleet to use for the build environment, enabling the build project to run on a specific managed or reserved capacity fleet. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="host_kernel" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="image" type="string" required>
      The Docker image identifier (such as aws/codebuild/standard:5.0) that specifies the build environment for the CodeBuild project. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_pull_credentials_type" type="string">
      Controls whether CodeBuild uses the build project's Docker credentials (CODEBUILD) or the service role (SERVICE_ROLE) to authenticate when pulling a private container image. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="privileged_mode" type="bool">
      Enables running the build in privileged mode, which is required when building Docker images or using Docker-in-Docker inside the CodeBuild environment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="registry_credential" type="object">
      Specifies the credentials, typically from AWS Secrets Manager, used to access a private Docker registry for the CodeBuild build environment. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="credential" type="string" required>
          The ARN or name of the credentials (such as a secret in AWS Secrets Manager) used to authenticate to a private Docker registry or other container image registry. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="credential_provider" type="string" required>
          Specifies the service responsible for creating the registry credential, which must be set to SECRETS_MANAGER for AWS CodeBuild to retrieve the login credentials. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Specifies the type of build environment for the CodeBuild project, such as LINUX_CONTAINER, WINDOWS_CONTAINER, or ARM_CONTAINER. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="file_system_locations" type="list(object)">
  Specifies the file system locations (such as Amazon EFS mount points) to mount to the build container during a build. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="identifier" type="string">
      In an AWS CodeBuild project's file system location configuration, the identifier is a user-defined logical name that uniquely identifies the file system mount, used to reference and distinguish it among multiple file system locations. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      The DNS name or path that identifies the file system to be mounted into the CodeBuild build environment, such as an Amazon EFS file system's DNS name (e.g., fs-12345678.efs.us-west-2.amazonaws.com). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mount_options" type="string">
      Defines the mount options (for example, NFS options such as nfsvers, rsize, wsize, hard, timeo, and retrans) that are used when mounting the file system in the CodeBuild environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mount_point" type="string">
      Mount point is the directory inside the CodeBuild container where the specified file system (e.g., Amazon EFS) is mounted, such as `/efs`. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Indicates the type of file system mounted in the CodeBuild project, which must be 'EFS' for an Amazon Elastic File System mount. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logs_config" type="object">
  Configures log output for the build project, specifying CloudWatch Logs and/or S3 log settings such as status, group/stream names, path, and encryption key. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cloud_watch_logs" type="object">
      Defines the CloudWatch Logs settings for the build project's logs, including the status (enabled/disabled), the log group name, the stream name, and whether encryption using the default AWS-managed CMK is disabled. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="group_name" type="string">
          The name of the CloudWatch Logs group to which CodeBuild publishes build logs. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string" required>
          Indicates whether CloudWatch Logs are enabled (ENABLED) or disabled (DISABLED) for storing the build project's logs. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_name" type="string">
          Specifies the name of the CloudWatch Logs log stream where the CodeBuild project's build logs are delivered, overriding the default build-ID-based stream name. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="s3_logs" type="object">
      Defines how build logs are delivered to an S3 bucket, including the S3 bucket location and path, whether S3 logging is enabled, and whether encryption is disabled. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="encryption_disabled" type="bool">
          Indicates whether S3 build logs are encrypted; when set to true, the logs are stored unencrypted, whereas the default (false or omitted) enables encryption. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="location" type="string">
          The ARN of the S3 bucket (and optional path prefix) where CodeBuild stores build logs when S3 logging is enabled. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string" required>
          Indicates whether S3 logging is enabled for the build project, with valid values being ENABLED or DISABLED. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Specifies a unique, user-defined name for the CodeBuild build project, which cannot be changed after creation; if omitted, CloudFormation automatically generates a unique name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="queued_timeout_in_minutes" type="number">
  Specifies the number of minutes a build can remain in the QUEUED state before CodeBuild times it out, after which the build is marked as failed. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_access_role" type="string">
  The ARN of an IAM role that AWS CodeBuild assumes to access and manage resources in the VPC configured for the project, such as creating network interfaces. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="secondary_artifacts" type="list(object)">
  Specifies a list of additional build output artifacts (in addition to the primary artifact) that CodeBuild can produce, each configured with settings like type, location, path, namespace, packaging, and encryption. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="artifact_identifier" type="string">
      A string that uniquely identifies this secondary artifact within the build project, used to reference it in build commands and to distinguish it from other secondary artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="encryption_disabled" type="bool">
      If set to true, the secondary build output artifact is stored unencrypted in S3, otherwise it is encrypted, typically using an AWS-managed or customer-managed KMS key. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      Specifies the S3 bucket name (or the image/container path for other artifact types) where the secondary build artifacts are output. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The unique name for this secondary artifact, which must be distinct from other secondary artifact names in the project and is used to identify the artifact in build output. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace_type" type="string">
      Controls the directory structure of the secondary artifact in the output location, where the namespaceType value (NONE, BUILD_ID, or TIMESTAMP) determines whether the build ID or timestamp is included in the artifact path. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_artifact_name" type="bool">
      For secondary artifacts in an AWS CodeBuild project, setting this boolean to true overrides the default output artifact name with the value specified in the artifact's Name property, while false (or unset) lets the build spec determine the name. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="packaging" type="string">
      Whether the build output artifact is packaged as a single ZIP file (ZIP) or left as-is without compression (NONE). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="path" type="string">
      Specifies the directory path within the output S3 bucket where the build output for this secondary artifact is stored, with the final object key also influenced by the artifact's namespace type and name. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of the secondary artifact, such as S3 to store output in an S3 bucket or CODEPIPELINE to deliver it to AWS CodePipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secondary_source_versions" type="list(object)">
  Specifies the source version (e.g., branch, tag, or commit ID) for each secondary source associated with the project, enabling the build to pull a specific revision of each additional repository. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="source_identifier" type="string">
      Specifies the identifier of the secondary source (as defined in the project's secondary sources) that this particular source version applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_version" type="string">
      Specifies the version (such as a Git commit ID, branch name, or tag) of the secondary source that CodeBuild uses for the build, identified by the associated source identifier in the project's secondary source versions list. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secondary_sources" type="list(object)">
  Specifies a list of additional source repositories (such as GitHub, Bitbucket, S3, or CodeCommit) that CodeBuild uses for the build project, in addition to the primary source. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auth" type="object">
      Specifies the authentication method for a Git-based secondary source (GitHub, GitHub Enterprise, or Bitbucket), including the authorization type (e.g., OAUTH or PERSONAL_ACCESS_TOKEN) and the optional ARN of the AWS Secrets Manager secret containing the credentials. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="resource" type="string">
          The Amazon Resource Name (ARN) of an AWS Secrets Manager secret containing the OAuth token or personal access token used to authenticate to the secondary source repository. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the authentication type (e.g., OAUTH) used by CodeBuild to access the secondary source repository, as defined in the source's auth settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="build_spec" type="string">
      Specifies the buildspec file path or inline buildspec YAML/JSON content for this secondary source, allowing it to override the project-level buildspec for that source. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="build_status_config" type="object">
      Specifies the context and target URL used for reporting the build status of this secondary source to the source repository provider. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="context" type="string">
          The context string that identifies the build status entry for this secondary source in the repository's commit status (for example, a custom label displayed in GitHub or Bitbucket). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_url" type="string">
          Specifies the URL of the repository that contains the source code, which is used as the target link for the build status report for this secondary source. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="git_clone_depth" type="number">
      The number of commits to include in the Git clone for the secondary source, where a lower value speeds up the clone by fetching only the most recent commits. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_submodules_config" type="object">
      Specifies whether git submodules are fetched for the secondary source repository in an AWS CodeBuild project. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="fetch_submodules" type="bool">
          Determines whether Git submodules are fetched for the associated secondary source repository in the AWS CodeBuild project. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="insecure_ssl" type="bool">
      Indicates whether the connection to the source repository is allowed to use insecure SSL (e.g., ignoring self-signed certificate errors). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      Specifies the repository URL or source location (such as an S3 bucket path, GitHub repository, or Bitbucket) for the secondary source of the CodeBuild project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="report_build_status" type="bool">
      For each secondary source, report_build_status controls whether CodeBuild reports build status information back to the source provider, which is only meaningful when that secondary source's type is GitHub, GitHub Enterprise, or Bitbucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_identifier" type="string">
      The identifier that uniquely identifies this secondary source among all sources in the CodeBuild project, and is used to construct the environment variable names (e.g., `CODEBUILD_SOURCE_DIR_&lt;identifier&gt;`) that buildspec commands can use to reference the source's directory and version. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Defines the source provider type for this secondary source, such as CODECOMMIT, S3, BITBUCKET, GITHUB, or GITHUB_ENTERPRISE. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_role" type="string" required>
  The ARN of the IAM role that AWS CodeBuild assumes to perform the build, granting permissions for actions like fetching source code, publishing artifacts, and writing logs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="source" type="object" required>
  Defines the source code repository for the build, including the repository type (e.g., S3, CodeCommit, GitHub, Bitbucket) and its location, along with optional settings like buildspec file path and git clone depth. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auth" type="object">
      Specifies the authorization configuration for the source code repository, including the authentication type (such as OAuth) and optional resource ARN, enabling AWS CodeBuild to access the source. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="resource" type="string">
          The resource field specifies the endpoint URL of the source provider (such as a GitHub Enterprise Server) when the auth type is PERSONAL_ACCESS_TOKEN, and is not used for OAuth-based authorization. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Specifies the authentication method to use when accessing the source repository, such as OAUTH, BASIC_AUTH, PERSONAL_ACCESS_TOKEN, or CODECONNECTIONS. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="build_spec" type="string">
      The build specification for the build project, either the name of a buildspec file or an inline YAML/JSON string that defines the build commands. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="build_status_config" type="object">
      Determines how CodeBuild reports the build status back to the source provider (e.g., GitHub, Bitbucket, or CodeCommit) by setting the status context and a custom target URL for the status update. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="context" type="string">
          The identifier string for the build status update, used to distinguish this build status from others in the source provider (such as a GitHub commit status context label). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_url" type="string">
          Specifies the URL that AWS CodeBuild sends as the target link when reporting build status to the source control system (e.g., GitHub or Bitbucket), providing users with a direct link to the build details. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="git_clone_depth" type="number">
      Sets the depth of the git clone, limiting the number of commits fetched from the repository (default is 1) to create a shallow clone. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_submodules_config" type="object">
      Configures Git submodules for the build source, with a 'FetchSubmodules' flag that determines whether submodules are fetched before the build runs. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="fetch_submodules" type="bool" required>
          Specifies whether to fetch git submodules for the build project's source repository. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="insecure_ssl" type="bool">
      Set to true to ignore SSL certificate warnings when connecting to the source repository, such as when it uses a self-signed certificate. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="location" type="string">
      The URL or location of the source repository (for example, an S3 bucket name, GitHub or Bitbucket repository URL, or CodeCommit ARN) that CodeBuild uses as the build input. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="report_build_status" type="bool">
      Indicates whether to report the build's start and finish status back to the source provider (e.g., GitHub, Bitbucket) for commits and pull requests, valid only for certain source types. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_identifier" type="string">
      Assigns a logical identifier to the primary source that can be referenced in buildspec commands, enabling the build to distinguish this source when the project uses multiple sources. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of source repository for the CodeBuild project, which determines how the build fetches source code (e.g., S3, CODECOMMIT, GITHUB, BITBUCKET, or NO_SOURCE). Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_version" type="string">
  Specifies the version of the source code to build, such as a commit ID, branch, or tag for Git-based repositories (CodeCommit, GitHub, Bitbucket) or an object key/version ID for Amazon S3; if omitted, the default branch or latest source version is used. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Assigns a list of tag objects (key-value pairs) to the AWS CodeBuild project, used for cost allocation, access control, and identifying project resources. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to associate with the AWS CodeBuild project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the CodeBuild project, providing arbitrary metadata for cost allocation or organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeout_in_minutes" type="number">
  The number of minutes after which a build in this project is automatically stopped and marked as failed if it has not completed. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="triggers" type="object">
  Defines the webhook trigger configuration, including the build type and filter groups that control which repository events (e.g., branch pushes or pull requests) automatically initiate a build. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="build_type" type="string">
      Specifies whether the project's webhook triggers should initiate a build, with a value of `BUILD` enabling builds and `NO_BUILD` disabling them for incoming source events. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="filter_groups" type="list(any)">
      Specifies a list of filter groups that determine which webhook events trigger a build, where each filter group is a list of webhook filters (e.g., event type, branch, file path) that must all match for the build to start. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pull_request_build_policy" type="object">
      Determines which pull requests trigger a build for the CodeBuild project's webhook: ALL builds every pull request, BRANCH builds only those targeting branches that match the filter pattern, and NON_BRANCH builds only those targeting branches that do not match the pattern. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="approver_roles" type="list(string)">
          A list of IAM role ARNs that can approve pull requests to trigger a build, defined within the project's pull request build policy for webhook-triggered builds. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="requires_comment_approval" type="string" required>
          Indicates whether an approval comment on a pull request is required to trigger the webhook build, so that builds only run when an authorized user comments with an approval phrase. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scope_configuration" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="domain" type="string">
          The domain of the Git repository hosting service (for example, 'github.com' or a GitHub Enterprise server hostname) that scopes the webhook trigger configuration for this CodeBuild project. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the repository or organization (as determined by the associated scope) that this webhook filter applies to, restricting which source code events trigger the build. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scope" type="string">
          Specifies whether the webhook trigger is scoped to an entire Bitbucket workspace ('WORKSPACE') or to a specific repository ('REPOSITORY'). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="webhook" type="bool">
      Determines whether the CodeBuild project's webhook is enabled, allowing builds to be automatically triggered by code changes pushed to the linked source repository (such as GitHub or Bitbucket). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="visibility" type="string">
  Specifies the visibility of the build project, with valid values PRIVATE, PUBLIC_READ, or PUBLIC, controlling whether unauthenticated users can view or trigger builds. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="vpc_config" type="object">
  Configures the build project to run in a customer-managed Amazon Virtual Private Cloud (VPC), specifying the VPC ID, subnet IDs, and security group IDs for the build environment. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="security_group_ids" type="list(string)">
      This field specifies the security group IDs for the VPC configuration of the CodeBuild project, defining which security groups apply to the build's network interfaces. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnets" type="list(string)">
      The list of subnet IDs within the VPC that AWS CodeBuild uses to run the build project's containers. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vpc_id" type="string">
      The ID of the Amazon Virtual Private Cloud (VPC) in which the CodeBuild project's builds run. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the CodeBuild project. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier for the AWS CodeBuild project, which is the project name as assigned when the project is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
