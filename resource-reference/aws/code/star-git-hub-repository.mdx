---
title: "aws_code_star_git_hub_repository"
description: "`aws_code_star_git_hub_repository` creates a GitHub repository as part of the original AWS CodeStar service's own project setup."
---

AWS discontinued creating and viewing CodeStar projects on July 31, 2024; resources CodeStar already created, including this repository, its source repos, pipelines, and builds, continue functioning unaffected.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/codestar"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own git hub repository"})

    		ubx.Resource(codestar.GitHubRepository, "example", codestar.GitHubRepositoryConfig{
    			RepositoryName:        "example-repository-name",
    			RepositoryOwner:       "example",
    			Code:                  map[string]any{"s3": map[string]any{"bucket": "example", "key": "example"}},
    			ConnectionArn:         "arn:aws:iam::123456789012:policy/example",
    			EnableIssues:          true,
    			IsPrivate:             true,
    			RepositoryAccessToken: "example",
    			RepositoryDescription: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GitHubRepository } from "@ubx/sdk-aws/aws/codestar/git_hub_repository";

    export default stack("example", () => {
      intent({ summary: "example own git hub repository" });

      resource(GitHubRepository, "example", {
        repositoryName: "example-repository-name",
        repositoryOwner: "example",
        code: { s3: { bucket: "example", key: "example" } },
        connectionArn: "arn:aws:iam::123456789012:policy/example",
        enableIssues: true,
        isPrivate: true,
        repositoryAccessToken: "example",
        repositoryDescription: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codestar import GitHubRepository, GitHubRepositoryConfig
    from ubx.aws.codestar.git_hub_repository import GitHubRepository_Code, GitHubRepository_Code_S3

    def describe():
        ubx.intent("example own git hub repository")

        ubx.resource(GitHubRepository, "example", GitHubRepositoryConfig(
            repository_name="example-repository-name",
            repository_owner="example",
            code=GitHubRepository_Code(s3=GitHubRepository_Code_S3(bucket="example", key="example")),
            connection_arn="arn:aws:iam::123456789012:policy/example",
            enable_issues=True,
            is_private=True,
            repository_access_token="example",
            repository_description="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_code_star_git_hub_repository, with repository name
    example-repository-name, repository owner example, code a set
    value, connection arn arn:aws:iam::123456789012:policy/example,
    enable issues true, is private true, repository access token
    example, repository description example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="code" type="object">
  Specifies the S3 bucket, key, and optional object version of a ZIP file containing the source code to be initially committed to the GitHub repository. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="s3" type="object" required>
      Specifies the S3 bucket and object key (and optionally object version) of the source code archive that AWS CodeStar uses to populate the GitHub repository. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          The name of the S3 bucket that contains the source code archive used to populate the GitHub repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string" required>
          The S3 object key (file path) of the source code archive in the specified S3 bucket that CodeStar uses to import the GitHub repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="object_version" type="string">
          The object version identifier of the S3 object that contains the source code to be committed to the GitHub repository, used to reference a specific version of the object when multiple versions exist in the bucket. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="connection_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS CodeStar Connections connection used to authorize access to the GitHub repository. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="enable_issues" type="bool">
  Whether to enable GitHub Issues (the repository's issue tracker) for the GitHub repository. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="is_private" type="bool">
  Indicates whether the GitHub repository is private (true) or public (false). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="repository_access_token" type="string">
  The GitHub personal access token used to authenticate with GitHub so AWS CodeStar can create and manage the repository on your behalf. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="repository_description" type="string">
  The description of the GitHub repository, which is displayed on the repository's main page. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The name of the GitHub repository to create or associate with this resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="repository_owner" type="string" required>
  The GitHub user or organization that owns the repository. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier for the AWS CodeStar GitHub repository, which CloudFormation returns as the GitHub owner and repository name combined with a slash (e.g., `owner/repo`). Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
