---
title: "aws_code_guru_reviewer_repository_association"
description: "`aws_code_guru_reviewer_repository_association` connects a real source repository to Amazon CodeGuru Reviewer for automated code review."
---

As of November 7, 2025, CodeGuru Reviewer is in maintenance mode and closed to new repository associations; existing associations continue working, with Amazon Q Developer's own code review capabilities positioned as the real successor.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own guru reviewer repository association"})

    		ubx.Resource(code.GuruReviewerRepositoryAssociation, "ci-runner", code.GuruReviewerRepositoryAssociationConfig{
    			Name:          "ci-runner",
    			Type:          "example",
    			BucketName:    "example-bucket-name",
    			ConnectionArn: "arn:aws:iam::123456789012:policy/ci",
    			Owner:         "example",
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GuruReviewerRepositoryAssociation } from "./local-sdk/aws/sdk/typescript/aws/code/guru_reviewer_repository_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own guru reviewer repository association" });

      resource(GuruReviewerRepositoryAssociation, "ci-runner", {
        name: "ci-runner",
        type: "example",
        bucketName: "example-bucket-name",
        connectionArn: "arn:aws:iam::123456789012:policy/ci",
        owner: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.code import GuruReviewerRepositoryAssociation, GuruReviewerRepositoryAssociationConfig

    def describe():
        ubx.intent("payments own guru reviewer repository association")

        ubx.resource(GuruReviewerRepositoryAssociation, "ci-runner", GuruReviewerRepositoryAssociationConfig(
            name="ci-runner",
            type="example",
            bucket_name="example-bucket-name",
            connection_arn="arn:aws:iam::123456789012:policy/ci",
            owner="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_code_guru_reviewer_repository_association called
    "ci-runner", with type example, bucket name example-bucket-name,
    connection arn arn:aws:iam::123456789012:policy/ci, owner
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket_name" type="string">
  The name of the S3 bucket associated with an associated S3 repository. It must start with `codeguru-reviewer-`. Optional.
</ResponseField>

<ResponseField name="connection_arn" type="string">
  The Amazon Resource Name (ARN) of an AWS CodeStar Connections connection. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the repository to be associated. Required.
</ResponseField>

<ResponseField name="owner" type="string">
  The owner of the repository. For a Bitbucket repository, this is the username for the account that owns the repository. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags associated with a repository association. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag attached to the CodeGuru Reviewer repository association, using an arbitrary string for metadata or categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of repository to be associated. Required.
</ResponseField>

## Output properties

<ResponseField name="association_arn" type="string">
  The Amazon Resource Name (ARN) of the repository association. Computed by AWS after `ship`.
</ResponseField>
