---
title: "aws_route53domains_delegation_signer_record"
description: "Real, generated bindings for `aws_route53domains_delegation_signer_record`."
---

`aws_route53domains_delegation_signer_record` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53domains"

    cfg := route53domains.DelegationSignerRecordConfig{
        DomainName: "example-domain-name",
    }
    ubx.Resource(
        route53domains.DelegationSignerRecord,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DelegationSignerRecord } from
      "jsr:@ubx/sdk-aws/aws/route53domains/delegation_signer_record";

    resource(DelegationSignerRecord, "example", {
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53domains import (
        DelegationSignerRecord,
        DelegationSignerRecordConfig,
    )

    cfg = DelegationSignerRecordConfig(
        domain_name="example",
    )
    ubx.resource(DelegationSignerRecord, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53domains_delegation_signer_record with
domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="dnssec_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
