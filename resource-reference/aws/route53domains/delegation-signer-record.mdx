---
title: "aws_route53domains_delegation_signer_record"
description: "Real, generated bindings for `aws_route53domains_delegation_signer_record`."
---

`aws_route53domains_delegation_signer_record` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53domains "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53domains"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delegation signer record"})

    		ubx.Resource(route53domains.DelegationSignerRecord, "ci-runner", route53domains.DelegationSignerRecordConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DelegationSignerRecord } from "jsr:@ubx/sdk-aws/aws/route53domains/delegation_signer_record";

    export default stack("payments", () => {
      intent({ summary: "payments own delegation signer record" });

      resource(DelegationSignerRecord, "ci-runner", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53domains import DelegationSignerRecord, DelegationSignerRecordConfig

    def describe():
        ubx.intent("payments own delegation signer record")

        ubx.resource(DelegationSignerRecord, "ci-runner", DelegationSignerRecordConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53domains_delegation_signer_record, with domain
    name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="dnssec_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
