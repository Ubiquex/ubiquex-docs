---
title: "aws_route53domains_registered_domain"
description: "Real, generated bindings for `aws_route53domains_registered_domain`."
---

`aws_route53domains_registered_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53domains "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53domains"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registered domain"})

    		ubx.Resource(route53domains.RegisteredDomain, "ci-runner", route53domains.RegisteredDomainConfig{
    			DomainName:        "example-domain-name",
    			AdminPrivacy:      true,
    			AutoRenew:         true,
    			BillingPrivacy:    true,
    			RegistrantPrivacy: true,
    			Tags:              map[string]string{"managed-by": "ubx"},
    			TechPrivacy:       true,
    			TransferLock:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegisteredDomain } from "jsr:@ubx/sdk-aws/aws/route53domains/registered_domain";

    export default stack("payments", () => {
      intent({ summary: "payments own registered domain" });

      resource(RegisteredDomain, "ci-runner", {
        domainName: "example-domain-name",
        adminPrivacy: true,
        autoRenew: true,
        billingPrivacy: true,
        registrantPrivacy: true,
        tags: { "managed-by": "ubx" },
        techPrivacy: true,
        transferLock: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53domains import RegisteredDomain, RegisteredDomainConfig

    def describe():
        ubx.intent("payments own registered domain")

        ubx.resource(RegisteredDomain, "ci-runner", RegisteredDomainConfig(
            domain_name="example-domain-name",
            admin_privacy=True,
            auto_renew=True,
            billing_privacy=True,
            registrant_privacy=True,
            tags={"managed-by": "ubx"},
            tech_privacy=True,
            transfer_lock=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53domains_registered_domain, with domain name
    example-domain-name, admin privacy true, auto renew true, billing
    privacy true, registrant privacy true, tags a set value, tech
    privacy true, transfer lock true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="admin_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="auto_renew" type="bool">
  Optional.
</ResponseField>

<ResponseField name="billing_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registrant_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tech_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transfer_lock" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="abuse_contact_email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="abuse_contact_phone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registrar_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="registrar_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="reseller" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_list" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="whois_server" type="string">
  Computed by AWS after `ship`.
</ResponseField>
