---
title: "aws_route53domains_registered_domain"
description: "Real, generated bindings for `aws_route53domains_registered_domain`."
---

`aws_route53domains_registered_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53domains"

    cfg := route53domains.RegisteredDomainConfig{
        DomainName: "example-domain-name",
        AdminPrivacy: true,
        AutoRenew: true,
    }
    ubx.Resource(route53domains.RegisteredDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegisteredDomain } from
      "jsr:@ubx/sdk-aws/aws/route53domains/registered_domain";

    resource(RegisteredDomain, "example", {
      domainName: "example",
      adminPrivacy: true,
      autoRenew: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53domains import (
        RegisteredDomain,
        RegisteredDomainConfig,
    )

    cfg = RegisteredDomainConfig(
        domain_name="example",
        admin_privacy=True,
        auto_renew=True,
    )
    ubx.resource(RegisteredDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53domains_registered_domain with domain_name,
admin_privacy, auto_renew.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="admin_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="auto_renew" type="bool">
  Optional.
</ResponseField>

<ResponseField name="billing_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registrant_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tech_privacy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="transfer_lock" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="abuse_contact_email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="abuse_contact_phone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registrar_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="registrar_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="reseller" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_list" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="whois_server" type="string">
  Computed by AWS after `ship`.
</ResponseField>
