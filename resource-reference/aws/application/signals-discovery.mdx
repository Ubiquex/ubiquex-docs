---
title: "aws_application_signals_discovery"
description: "`aws_application_signals_discovery` enables real, automatic discovery of an account's services, APIs..."
---

`aws_application_signals_discovery` enables real, automatic discovery of an account's services, APIs, and their dependencies for CloudWatch Application Signals, the mechanism that populates its Services and Application Map views from OpenTelemetry-instrumented traces without each service being registered by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationsignals "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/applicationsignals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own signals discovery"})

    		ubx.Resource(applicationsignals.Discovery, "example", applicationsignals.DiscoveryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Discovery } from "@ubx/sdk-aws/aws/applicationsignals/discovery";

    export default stack("example", () => {
      intent({ summary: "example own signals discovery" });

      resource(Discovery, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.applicationsignals.discovery import Discovery, DiscoveryConfig

    def describe():
        ubx.intent("example own signals discovery")

        ubx.resource(Discovery, "example", DiscoveryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_application_signals_discovery, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="account_id" type="string">
  The identifier for the specified AWS account. Computed by AWS after `ship`.
</ResponseField>
