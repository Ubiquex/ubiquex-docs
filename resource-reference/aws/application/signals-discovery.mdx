---
title: "aws_application_signals_discovery"
description: "`aws_application_signals_discovery` enables real, automatic discovery of an account's services, APIs..."
---

`aws_application_signals_discovery` enables real, automatic discovery of an account's services, APIs, and their dependencies for CloudWatch Application Signals, the mechanism that populates its Services and Application Map views from OpenTelemetry-instrumented traces without each service being registered by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own signals discovery"})

    		ubx.Resource(application.SignalsDiscovery, "ci-runner", application.SignalsDiscoveryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SignalsDiscovery } from "./local-sdk/aws/sdk/typescript/aws/application/signals_discovery.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own signals discovery" });

      resource(SignalsDiscovery, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.application import SignalsDiscovery, SignalsDiscoveryConfig

    def describe():
        ubx.intent("payments own signals discovery")

        ubx.resource(SignalsDiscovery, "ci-runner", SignalsDiscoveryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_application_signals_discovery, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="account_id" type="string">
  The identifier for the specified AWS account. Computed by AWS after `ship`.
</ResponseField>
