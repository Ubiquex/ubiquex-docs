---
title: "aws_application_signals_service_level_objective"
description: "`aws_application_signals_service_level_objective` sets a target level of reliability or availability for a specific service in CloudWatch Application..."
---

`aws_application_signals_service_level_objective` sets a target level of reliability or availability for a specific service in CloudWatch Application Signals, tracked against a service level indicator, typically latency or availability, so a team has a concrete, ongoing number for whether a service is meeting its own reliability bar.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationsignals "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/applicationsignals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service level objective"})

    		ubx.Resource(applicationsignals.ServiceLevelObjective, "example", applicationsignals.ServiceLevelObjectiveConfig{
    			Name:                   "example",
    			BurnRateConfigurations: []map[string]any{{"look_back_window_minutes": 1}},
    			Description:            "Managed by ubx.",
    			ExclusionWindows:       []map[string]any{{"reason": "example"}},
    			Goal:                   map[string]any{"attainment_goal": 1},
    			RequestBasedSli:        map[string]any{"request_based_sli_metric": map[string]any{"composite_sli_config": map[string]any{"selection_config": map[string]any{"type": "example"}}}},
    			Sli:                    map[string]any{"comparison_operator": "example", "metric_threshold": 1, "sli_metric": map[string]any{"composite_sli_config": map[string]any{"selection_config": map[string]any{"type": "example"}}}},
    			Tags:                   []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceLevelObjective } from "@ubx/sdk-aws/aws/applicationsignals/service_level_objective";

    export default stack("example", () => {
      intent({ summary: "example own service level objective" });

      resource(ServiceLevelObjective, "example", {
        name: "example",
        burnRateConfigurations: [{ lookBackWindowMinutes: 1 }],
        description: "Managed by ubx.",
        exclusionWindows: [{ reason: "example" }],
        goal: { attainmentGoal: 1 },
        requestBasedSli: {
          requestBasedSliMetric: {
            compositeSliConfig: { selectionConfig: { type: "example" } },
          },
        },
        sli: {
          comparisonOperator: "example",
          metricThreshold: 1,
          sliMetric: {
            compositeSliConfig: { selectionConfig: { type: "example" } },
          },
        },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.applicationsignals import ServiceLevelObjective, ServiceLevelObjectiveConfig
    from ubx.aws.applicationsignals.service_level_objective import ServiceLevelObjective_BurnRateConfigurations, ServiceLevelObjective_ExclusionWindows, ServiceLevelObjective_Goal, ServiceLevelObjective_RequestBasedSli, ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric, ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig, ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig_SelectionConfig, ServiceLevelObjective_Sli, ServiceLevelObjective_Sli_SliMetric, ServiceLevelObjective_Tags

    def describe():
        ubx.intent("example own service level objective")

        ubx.resource(ServiceLevelObjective, "example", ServiceLevelObjectiveConfig(
            name="example",
            burn_rate_configurations=[ServiceLevelObjective_BurnRateConfigurations(look_back_window_minutes=1)],
            description="Managed by ubx.",
            exclusion_windows=[ServiceLevelObjective_ExclusionWindows(reason="example")],
            goal=ServiceLevelObjective_Goal(attainment_goal=1),
            request_based_sli=ServiceLevelObjective_RequestBasedSli(request_based_sli_metric=ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric(composite_sli_config=ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig(selection_config=ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig_SelectionConfig(type="example")))),
            sli=ServiceLevelObjective_Sli(comparison_operator="example", metric_threshold=1, sli_metric=ServiceLevelObjective_Sli_SliMetric(composite_sli_config=ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig(selection_config=ServiceLevelObjective_RequestBasedSli_RequestBasedSliMetric_CompositeSliConfig_SelectionConfig(type="example")))),
            tags=[ServiceLevelObjective_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_application_signals_service_level_objective called
    "example", with burn rate configurations a set value, description
    Managed by ubx., exclusion windows a set value, goal a set value,
    request based sli a set value, sli a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="burn_rate_configurations" type="list(object)">
  Each object in this array defines the length of the look-back window used to calculate one burn rate metric for this SLO. The burn rate measures how fast the service is consuming the error budget, relative to the attainment goal of the SLO. Optional.
  <Expandable title="properties">
    <ResponseField name="look_back_window_minutes" type="number">
      The length of the look-back window, in minutes, over which the burn rate is evaluated for this burn rate configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description for this SLO. Default is 'No description' Optional.
</ResponseField>

<ResponseField name="exclusion_windows" type="list(object)">
  Each object in this array defines a time exclusion window for this SLO. The time exclusion window is used to exclude breaching data points from affecting attainment rate, error budget, and burn rate metrics. Optional.
  <Expandable title="properties">
    <ResponseField name="reason" type="string">
      The reason or annotation for excluding this time window from the SLO's evaluation, commonly used to document planned maintenance or expected anomalies. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="recurrence_rule" type="object">
      Defines the recurrence schedule (cron or rate expression) for a time window during which the service level objective's evaluation is excluded. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="expression" type="string">
          The cron expression that defines the recurring schedule for this exclusion window, during which the SLO is not evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Defines the start of an exclusion window, a time period during which the SLO's evaluation is paused so that planned maintenance or other excluded events do not affect the objective's availability calculation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="window" type="object">
      This object specifies the start and end times of the exclusion window, during which the SLO's attainment metric is not evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="duration" type="number">
          The duration in minutes of the exclusion window, during which the SLO's metric data is excluded from evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="duration_unit" type="string">
          The unit of time (such as Days or Hours) for the numeric duration of the exclusion window, determining how long the service level objective evaluation is paused. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="goal" type="object">
  A structure that contains the attributes that determine the goal of the SLO. This includes the time period for evaluation and the attainment threshold. Optional.
  <Expandable title="properties">
    <ResponseField name="attainment_goal" type="number">
      The threshold that determines if the goal is being met. An attainment goal is the ratio of good periods that meet the threshold requirements to the total periods within the interval. For example, an attainment goal of 99.9% means that within your interval, you are targeting 99.9% of the periods to be in healthy state. If you omit this parameter, 99 is used to represent 99% as the attainment goal. Optional.
    </ResponseField>
    <ResponseField name="interval" type="object">
      The time period used to evaluate the SLO. It can be either a calendar interval or rolling interval. If you omit this parameter, a rolling interval of 7 days is used. Optional.
      <Expandable title="properties">
        <ResponseField name="calendar_interval" type="object">
          If the interval for this service level objective is a calendar interval, this structure contains the interval specifications. Optional.
          <Expandable title="properties">
            <ResponseField name="duration" type="number" required>
              Specifies the duration of each interval. For example, if `Duration` is 1 and `DurationUnit` is `MONTH`, each interval is one month, aligned with the calendar. Required.
            </ResponseField>
            <ResponseField name="duration_unit" type="string" required>
              Specifies the interval unit. Required.
            </ResponseField>
            <ResponseField name="start_time" type="number" required>
              Epoch time in seconds you want the first interval to start. Be sure to choose a time that configures the intervals the way that you want. For example, if you want weekly intervals starting on Mondays at 6 a.m., be sure to specify a start time that is a Monday at 6 a.m. As soon as one calendar interval ends, another automatically begins. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rolling_interval" type="object">
          If the interval is a calendar interval, this structure contains the interval specifications. Optional.
          <Expandable title="properties">
            <ResponseField name="duration" type="number" required>
              Specifies the duration of each interval. For example, if `Duration` is 1 and `DurationUnit` is `MONTH`, each interval is one month, aligned with the calendar. Required.
            </ResponseField>
            <ResponseField name="duration_unit" type="string" required>
              Specifies the interval unit. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="warning_threshold" type="number">
      The percentage of remaining budget over total budget that you want to get warnings for. If you omit this parameter, the default of 50.0 is used. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of this SLO. Required.
</ResponseField>

<ResponseField name="request_based_sli" type="object">
  This structure contains information about the performance metric that a request-based SLO monitors. Optional.
  <Expandable title="properties">
    <ResponseField name="comparison_operator" type="string">
      The arithmetic operation used when comparing the specified metric to the threshold. Optional.
    </ResponseField>
    <ResponseField name="metric_threshold" type="number">
      The value that the SLI metric is compared to. Optional.
    </ResponseField>
    <ResponseField name="request_based_sli_metric" type="object" required>
      This structure contains the information about the metric that is used for a request-based SLO. Required.
      <Expandable title="properties">
        <ResponseField name="composite_sli_config" type="object">
          Configures the request-based SLI as a composite SLI, which aggregates multiple underlying service level indicators into a single SLI metric for the SLO. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="composite_sli_components" type="list(object)">
              Defines the list of individual SLI components (each with its own SLI metric configuration and weight) that are combined to calculate the composite SLI metric for this service level objective. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="operation_name" type="string">
                  The name of the specific operation (e.g., an HTTP endpoint or API call) that this composite SLI component uses to evaluate performance against the SLO. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="selection_config" type="object" required>
              Defines which underlying service level indicators (SLIs) are selected and combined to calculate the composite SLI for the SLO. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pattern" type="string">
                  A string specifying the boolean formula (e.g., `$&#123;sl1&#125; AND $&#123;sl2&#125;`) that determines how the referenced individual SLIs are combined to form the composite service level objective. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string" required>
                  Determines the selection mode for a composite SLI, using 'ANY' to require that at least one of the selected operations meets the SLO, or 'ALL' to require that every selected operation meets the SLO. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dependency_config" type="object">
          Configuration for identifying a dependency and its operation Optional.
          <Expandable title="properties">
            <ResponseField name="dependency_key_attributes" type="any" required>
              If this SLO is related to a metric collected by Application Signals, you must use this field to specify which dependency the SLO metric is related to. Required.
            </ResponseField>
            <ResponseField name="dependency_operation_name" type="string" required>
              When the SLO monitors a specific operation of the dependency, this field specifies the name of that operation in the dependency. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key_attributes" type="any">
          This is a string-to-string map that contains information about the type of object that this SLO is related to. Optional.
        </ResponseField>
        <ResponseField name="metric_name" type="string">
          The name of the metric for non-Application Signals services Optional.
        </ResponseField>
        <ResponseField name="metric_source" type="object">
          Configuration for identifying the source of metrics for non-Application Signals services Optional.
          <Expandable title="properties">
            <ResponseField name="metric_source_attributes" type="any">
              Optional additional attributes for the metric source Optional.
            </ResponseField>
            <ResponseField name="metric_source_key_attributes" type="any" required>
              Required attributes that identify the metric source Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metric_type" type="string">
          If the SLO monitors either the LATENCY or AVAILABILITY metric that Application Signals collects, this field displays which of those metrics is used. Optional.
        </ResponseField>
        <ResponseField name="monitored_request_count_metric" type="object">
          This structure defines the metric that is used as the "good request" or "bad request" value for a request-based SLO. This value observed for the metric defined in `TotalRequestCountMetric` is divided by the number found for `MonitoredRequestCountMetric` to determine the percentage of successful requests that this SLO tracks. Optional.
          <Expandable title="properties">
            <ResponseField name="bad_count_metric" type="list(object)">
              If this SLO monitors a CloudWatch metric or the result of a CloudWatch metric math expression, this structure includes the information about that metric or expression. Optional.
              <Expandable title="properties">
                <ResponseField name="account_id" type="string">
                  The AWS account ID that owns the CloudWatch metric used as the bad count metric for the monitored request count in the SLO, typically needed when the metric is in a different account. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expression" type="string">
                  The CloudWatch metrics expression that defines the bad count metric used to calculate the service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="id" type="string">
                  The unique identifier assigned to this bad-count metric definition within the request-based SLI, used to reference the metric in the SLO's monitored request count configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_stat" type="object">
                  This object defines the CloudWatch metric statistic (including namespace, metric name, dimensions, period, and statistic) that is used to count the number of bad requests for the request-based SLI's monitored request count metric in the SLO. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="metric" type="object">
                      The CloudWatch metric that defines the bad count metric, including its namespace, metric name, and dimensions, used to calculate the number of bad requests for the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          Defines the CloudWatch metric dimensions (name-value pairs) that identify the bad count metric used in the request-based SLI's monitored request count metric, enabling the SLI to track unsuccessful requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              Specifies the name of a CloudWatch metric dimension (for example, 'ServiceName' or 'Operation') that, together with the dimension value, identifies the bad count metric used in the request-based SLO's monitored request count metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of a dimension that identifies the CloudWatch metric used to count bad requests for the request-based SLO. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The name of the CloudWatch metric that represents the count of bad or failing requests used to calculate the service level objective's request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace" type="string">
                          Specifies the CloudWatch namespace of the metric used as the bad request count for the monitored request count metric in the request-based SLI, identifying the service or application that emits the metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="period" type="number">
                      The period, in seconds, for the CloudWatch metric statistic that collects the bad count metric, which is used as part of the monitored request count metric in the request-based SLI for this Service Level Objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="stat" type="string">
                      The statistic (e.g., Sum, Average) to apply to the metric data for the bad request count metric in the request-based SLI's monitored request count metric, used to evaluate requests that do not meet the SLI objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="unit" type="string">
                      The unit of the CloudWatch metric statistic (e.g., Count, Bytes, Seconds) that defines the bad request count metric used in the monitored request count metric for this request-based service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="return_data" type="bool">
                  Controls whether the value of the bad count metric is returned in the CloudWatch metric math expression that defines this request-based SLI, where 'true' surfaces the bad count data as an output and 'false' uses it solely as an intermediate calculation step. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="good_count_metric" type="list(object)">
              If this SLO monitors a CloudWatch metric or the result of a CloudWatch metric math expression, this structure includes the information about that metric or expression. Optional.
              <Expandable title="properties">
                <ResponseField name="account_id" type="string">
                  The AWS account ID of the CloudWatch metric that counts good (successful) requests, used to compute the request-based service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="expression" type="string">
                  The CloudWatch metric math expression string that defines the good count metric for the request-based SLI, used to calculate the number of successful requests counted against the service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="id" type="string">
                  The unique identifier (Id) of the CloudWatch metric data definition used as the good count metric within the monitored request count metric for a request-based SLO, enabling the metric to be referenced in SLI expressions and burn-rate calculations. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_stat" type="object">
                  The metric_stat object defines the CloudWatch metric and statistic (e.g., metric name, namespace, dimensions, statistic, period) used to obtain the count of good (successful) requests for the request-based SLO calculation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="metric" type="object">
                      Specifies the CloudWatch metric (namespace, metric name, and dimensions) that defines the 'good' count for the request-based SLO's monitored request count metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="dimensions" type="list(object)">
                          A list of dimension key-value pairs that define the scope of the CloudWatch metric used as the good count metric in the request-based service level objective's monitored request count metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name (key) of a CloudWatch metric dimension that, along with its value in the corresponding 'value' field, identifies and filters the good count metric referenced in the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value" type="string">
                              The value of a dimension on the CloudWatch metric that counts good (successful) requests for this request-based service level objective, used to identify or filter the metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="metric_name" type="string">
                          The CloudWatch metric name for the metric that provides the count of good requests, used as the numerator in the request-based SLI's availability calculation for the service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="namespace" type="string">
                          The CloudWatch namespace (such as 'AWS/ApplicationELB' or a custom namespace) that identifies the metric used as the good count metric in the request-based SLI's monitored request count for this service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="period" type="number">
                      Specifies the evaluation period in seconds for the CloudWatch metric statistic of the good count metric, determining the time granularity over which the request-based SLI's good request count is aggregated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="stat" type="string">
                      The CloudWatch statistic (e.g., Sum, Average) applied to the good count metric to evaluate the number of good requests for the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="unit" type="string">
                      Specifies the unit of measurement for the metric statistic (e.g., Count, Bytes, Seconds) that defines the good count of monitored requests in the request-based SLO's metric, enabling CloudWatch to correctly interpret and compare metric values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="return_data" type="bool">
                  Whether the metric data from this good count metric is returned and included in the calculated good count for the request-based service level objective, matching the CloudWatch MetricDataQuery ReturnData behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="operation_name" type="string">
          If the SLO monitors a specific operation of the service, this field displays that operation name. Optional.
        </ResponseField>
        <ResponseField name="total_request_count_metric" type="list(object)">
          If this SLO monitors a CloudWatch metric or the result of a CloudWatch metric math expression, this structure includes the information about that metric or expression. Optional.
          <Expandable title="properties">
            <ResponseField name="account_id" type="string">
              The AWS account ID that owns the CloudWatch metric used for the total request count metric in the request-based SLI; if omitted, defaults to the current account. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expression" type="string">
              Specifies the CloudWatch metrics insights expression that calculates the total request count for the request-based service level objective's metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="string">
              The unique identifier for the total request count metric, used to reference this metric in the SLO's metric data queries and math expressions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric_stat" type="object">
              Defines the CloudWatch metric statistic (metric name, dimensions, period, stat, and unit) for the total request count metric used to calculate the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="metric" type="object">
                  CloudWatch metric definition (namespace, metric name, and dimensions) that identifies the metric whose value represents the total number of requests for the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dimensions" type="list(object)">
                      Defines the list of CloudWatch metric dimension name-value pairs that uniquely identify the metric used for the total request count in the request-based service level objective metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Specifies the name of a dimension that, combined with a corresponding dimension value, identifies the CloudWatch metric used for the total request count in the request-based service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          Specifies the value for a metric dimension (such as an operation name or resource identifier) that, together with the corresponding dimension key, uniquely identifies the total request count metric used in the request-based SLI's metric statistic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="metric_name" type="string">
                      The name of the CloudWatch metric that measures the total request count, used as the source for the metric_stat in the request-based service level objective's SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="namespace" type="string">
                      This field specifies the CloudWatch namespace of the metric used to measure the total request count for the request-based SLO. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="period" type="number">
                  The period (in seconds) over which the total request count metric is aggregated or evaluated, defining the granularity of the metric stat used for the request-based SLI. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stat" type="string">
                  The CloudWatch statistic (for example, 'Sum') used to aggregate the total request count metric over the evaluation period for the service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit" type="string">
                  The unit of measurement for the CloudWatch metric used as the total request count in the request-based SLI, such as 'Count' or 'Bytes', which must be consistent with the metric's actual unit to compute the SLO correctly. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="return_data" type="bool">
              Indicates whether the total request count metric data query returns its data for inclusion in the request-based service level objective calculation. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sli" type="object">
  This structure contains information about the performance metric that an SLO monitors. Optional.
  <Expandable title="properties">
    <ResponseField name="comparison_operator" type="string" required>
      The arithmetic operation used when comparing the specified metric to the threshold. Required.
    </ResponseField>
    <ResponseField name="metric_threshold" type="number" required>
      The value that the SLI metric is compared to. Required.
    </ResponseField>
    <ResponseField name="sli_metric" type="object" required>
      A structure that contains information about the metric that the SLO monitors. Required.
      <Expandable title="properties">
        <ResponseField name="composite_sli_config" type="object">
          Specifies the composite SLI configuration, which lets you combine multiple service level indicators using boolean AND/OR operators to evaluate the overall SLI for the service level objective. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="composite_sli_components" type="list(object)">
              The list of component service level indicators that are combined to compute the composite SLI, where each component contains its own SLI metric configuration and optional weight. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="operation_name" type="string">
                  The name of the service operation (e.g., a specific API action like 'GetItem' for DynamoDB) for which this component's metric is defined, used to filter the CloudWatch metric contributing to the composite SLO. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="selection_config" type="object" required>
              Specifies the metric selection criteria (service, operation, and metric type filters) used to determine which underlying metrics are aggregated into the composite service level indicator. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pattern" type="string">
                  Optional.
                </ResponseField>
                <ResponseField name="type" type="string" required>
                  Specifies whether the composite SLO requires all selected member SLOs to meet their objectives (ALL) or at least one (ANY). Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dependency_config" type="object">
          Configuration for identifying a dependency and its operation Optional.
          <Expandable title="properties">
            <ResponseField name="dependency_key_attributes" type="any" required>
              If this SLO is related to a metric collected by Application Signals, you must use this field to specify which dependency the SLO metric is related to. Required.
            </ResponseField>
            <ResponseField name="dependency_operation_name" type="string" required>
              When the SLO monitors a specific operation of the dependency, this field specifies the name of that operation in the dependency. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key_attributes" type="any">
          This is a string-to-string map that contains information about the type of object that this SLO is related to. Optional.
        </ResponseField>
        <ResponseField name="metric_data_queries" type="list(object)">
          If this SLO monitors a CloudWatch metric or the result of a CloudWatch metric math expression, this structure includes the information about that metric or expression. Optional.
          <Expandable title="properties">
            <ResponseField name="account_id" type="string">
              Specifies the AWS account ID that the metric data query will query metrics from, enabling cross-account metric retrieval for the SLO's SLI metric evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="expression" type="string">
              The CloudWatch Metrics Insights or metric math expression that defines the service level indicator metric, enabling calculations such as SUM(METRICS()) or arithmetic over the referenced metric data queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="string">
              A short name that uniquely identifies this metric data query within the SLI's metric data queries list, so that other queries or metric math expressions can reference it. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric_stat" type="object">
              Specifies the CloudWatch metric to use for this query, including the metric namespace, metric name, dimensions, period, and the statistic (e.g., Average, Sum) to compute for the SLO's service level indicator. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="metric" type="object">
                  Defines the CloudWatch metric (namespace, metric name, and optional dimensions) that is used in the metric statistic for this SLO's metric data query. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="dimensions" type="list(object)">
                      Specifies the CloudWatch metric dimensions (as name-value pairs) that identify the specific metric used in this metric stat of the SLI's metric query. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Specifies the dimension key (name) for a CloudWatch metric dimension that identifies the metric used in the service level objective's SLI metric query. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          The value of the dimension, paired with the dimension name, identifies the specific metric dimension for the metric used in the service level objective's SLI metric data query. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="metric_name" type="string">
                      The name of the CloudWatch metric that the metric stat uses for this metric data query in the SLO's SLI metric definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="namespace" type="string">
                      Specifies the CloudWatch namespace of the metric used in the metric statistic for the service level objective's SLI metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="period" type="number">
                  The period (in seconds) specifies the granularity of the metric used in the SLI metric stat, determining the time interval over which the metric values are aggregated for the service level objective calculation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="stat" type="string">
                  Specifies the CloudWatch metric statistic (e.g., Sum, Average, Minimum, Maximum, SampleCount) to use for the SLI metric in the service level objective. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit" type="string">
                  The unit of measurement (e.g., Count, Milliseconds, Bytes) for the metric statistic used in the service level objective's metric data query, which specifies the unit that the SLO calculation applies to the metric value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="return_data" type="bool">
              Specifies whether the results of this metric data query are returned for use as the service level objective's metric, where queries with ReturnData set to false are used only as intermediate inputs to other queries (such as math expressions) and are not directly evaluated as the SLI metric. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metric_name" type="string">
          The name of the metric for non-Application Signals services Optional.
        </ResponseField>
        <ResponseField name="metric_source" type="object">
          Configuration for identifying the source of metrics for non-Application Signals services Optional.
          <Expandable title="properties">
            <ResponseField name="metric_source_attributes" type="any">
              Optional additional attributes for the metric source Optional.
            </ResponseField>
            <ResponseField name="metric_source_key_attributes" type="any" required>
              Required attributes that identify the metric source Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metric_type" type="string">
          If the SLO monitors either the LATENCY or AVAILABILITY metric that Application Signals collects, this field displays which of those metrics is used. Optional.
        </ResponseField>
        <ResponseField name="operation_name" type="string">
          If the SLO monitors a specific operation of the service, this field displays that operation name. Optional.
        </ResponseField>
        <ResponseField name="period_seconds" type="number">
          The number of seconds to use as the period for SLO evaluation. Your application's performance is compared to the SLI during each period. For each period, the application is determined to have either achieved or not achieved the necessary performance. Optional.
        </ResponseField>
        <ResponseField name="statistic" type="string">
          The statistic to use for comparison to the threshold. It can be any CloudWatch statistic or extended statistic Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The list of tag keys and values associated with the resource you specified Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key of a tag to attach to the service level objective, enabling you to categorize and manage SLOs by metadata such as environment, team, or cost center. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to the Application Signals Service Level Objective, used to store arbitrary user-defined metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of this SLO. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="number">
  Epoch time in seconds of the time that this SLO was created Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="evaluation_type" type="string">
  Displays whether this is a period-based SLO or a request-based SLO. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="number">
  Epoch time in seconds of the time that this SLO was most recently updated Computed by AWS after `ship`.
</ResponseField>
