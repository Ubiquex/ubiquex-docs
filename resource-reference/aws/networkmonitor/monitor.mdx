---
title: "aws_networkmonitor_monitor"
description: "Real, generated bindings for `aws_networkmonitor_monitor`."
---

`aws_networkmonitor_monitor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/networkmonitor"

    cfg := networkmonitor.MonitorConfig{
        MonitorName: "example-monitor-name",
        AggregationPeriod: 1,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(networkmonitor.Monitor, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Monitor } from
      "jsr:@ubx/sdk-aws/aws/networkmonitor/monitor";

    resource(Monitor, "example", {
      monitorName: "example",
      aggregationPeriod: 1,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmonitor import Monitor, MonitorConfig

    cfg = MonitorConfig(
        monitor_name="example",
        aggregation_period=1,
        tags={"key": "value"},
    )
    ubx.resource(Monitor, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmonitor_monitor with monitor_name,
aggregation_period, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aggregation_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="aggregation_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
