---
title: "aws_networkmonitor_monitor"
description: "Real, generated bindings for `aws_networkmonitor_monitor`."
---

`aws_networkmonitor_monitor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmonitor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmonitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own monitor"})

    		ubx.Resource(networkmonitor.Monitor, "ci-runner", networkmonitor.MonitorConfig{
    			MonitorName: "example-monitor-name",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Monitor } from "jsr:@ubx/sdk-aws/aws/networkmonitor/monitor";

    export default stack("payments", () => {
      intent({ summary: "payments own monitor" });

      resource(Monitor, "ci-runner", {
        monitorName: "example-monitor-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmonitor import Monitor, MonitorConfig

    def describe():
        ubx.intent("payments own monitor")

        ubx.resource(Monitor, "ci-runner", MonitorConfig(
            monitor_name="example-monitor-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmonitor_monitor, with monitor name
    example-monitor-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aggregation_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="aggregation_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
