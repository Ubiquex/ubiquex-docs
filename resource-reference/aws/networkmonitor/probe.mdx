---
title: "aws_networkmonitor_probe"
description: "Real, generated bindings for `aws_networkmonitor_probe`."
---

`aws_networkmonitor_probe` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmonitor"

    cfg := networkmonitor.ProbeConfig{
        Destination: "example",
        MonitorName: "example-monitor-name",
        Protocol: "example",
        SourceArn: "example",
        DestinationPort: 1,
        PacketSize: 1,
    }
    ubx.Resource(networkmonitor.Probe, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Probe } from
      "jsr:@ubx/sdk-aws/aws/networkmonitor/probe";

    resource(Probe, "example", {
      destination: "example",
      monitorName: "example",
      protocol: "example",
      sourceArn: "example",
      destinationPort: 1,
      packetSize: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.networkmonitor import Probe, ProbeConfig

    cfg = ProbeConfig(
        destination="example",
        monitor_name="example",
        protocol="example",
        source_arn="example",
        destination_port=1,
        packet_size=1,
    )
    ubx.resource(Probe, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_networkmonitor_probe with destination,
monitor_name, protocol, source_arn, destination_port,
packet_size.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="packet_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="address_family" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="packet_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="probe_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
