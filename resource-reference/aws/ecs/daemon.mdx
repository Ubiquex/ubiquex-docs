---
title: "aws_ecs_daemon"
description: "Real, generated bindings for `aws_ecs_daemon`."
---

`aws_ecs_daemon` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ecs"

    cfg := ecs.DaemonConfig{
        CapacityProviderArns: []string{"example"},
        DaemonTaskDefinitionArn: "example",
        Name: "example-name",
        ClusterArn: "example",
        EnableEcsManagedTags: true,
    }
    ubx.Resource(ecs.Daemon, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Daemon } from "jsr:@ubx/sdk-aws/aws/ecs/daemon";

    resource(Daemon, "example", {
      capacityProviderArns: ["example"],
      daemonTaskDefinitionArn: "example",
      name: "example",
      clusterArn: "example",
      enableEcsManagedTags: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecs import Daemon, DaemonConfig

    cfg = DaemonConfig(
        capacity_provider_arns=["example"],
        daemon_task_definition_arn="example",
        name="example",
        cluster_arn="example",
        enable_ecs_managed_tags=True,
    )
    ubx.resource(Daemon, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecs_daemon with capacity_provider_arns,
daemon_task_definition_arn, name, cluster_arn,
enable_ecs_managed_tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="capacity_provider_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="cluster_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="daemon_task_definition_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_ecs_managed_tags" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_execute_command" type="bool">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="propagate_tags" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="deployment_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
