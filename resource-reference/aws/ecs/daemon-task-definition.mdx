---
title: "aws_ecs_daemon_task_definition"
description: "Real, generated bindings for `aws_ecs_daemon_task_definition`."
---

`aws_ecs_daemon_task_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecs"

    cfg := ecs.DaemonTaskDefinitionConfig{
        Family: "example",
        Cpu: "example",
        ExecutionRoleArn: "example",
    }
    ubx.Resource(ecs.DaemonTaskDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DaemonTaskDefinition } from
      "jsr:@ubx/sdk-aws/aws/ecs/daemon_task_definition";

    resource(DaemonTaskDefinition, "example", {
      family: "example",
      cpu: "example",
      executionRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecs import (
        DaemonTaskDefinition,
        DaemonTaskDefinitionConfig,
    )

    cfg = DaemonTaskDefinitionConfig(
        family="example",
        cpu="example",
        execution_role_arn="example",
    )
    ubx.resource(DaemonTaskDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecs_daemon_task_definition with family, cpu,
execution_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cpu" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="family" type="string" required>
  Required.
</ResponseField>

<ResponseField name="memory" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="task_role_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
