---
title: "aws_ecs_service"
description: "Real, generated bindings for `aws_ecs_service`."
---

`aws_ecs_service` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service"})

    		ubx.Resource(ecs.Service, "ci-runner", ecs.ServiceConfig{
    			Name:                            "ci-runner",
    			DeploymentMaximumPercent:        1,
    			DeploymentMinimumHealthyPercent: 1,
    			DesiredCount:                    1,
    			EnableEcsManagedTags:            true,
    			EnableExecuteCommand:            true,
    			ForceDelete:                     true,
    			ForceNewDeployment:              true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Service } from "jsr:@ubx/sdk-aws/aws/ecs/service";

    export default stack("payments", () => {
      intent({ summary: "payments own service" });

      resource(Service, "ci-runner", {
        name: "ci-runner",
        deploymentMaximumPercent: 1,
        deploymentMinimumHealthyPercent: 1,
        desiredCount: 1,
        enableEcsManagedTags: true,
        enableExecuteCommand: true,
        forceDelete: true,
        forceNewDeployment: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecs import Service, ServiceConfig

    def describe():
        ubx.intent("payments own service")

        ubx.resource(Service, "ci-runner", ServiceConfig(
            name="ci-runner",
            deployment_maximum_percent=1,
            deployment_minimum_healthy_percent=1,
            desired_count=1,
            enable_ecs_managed_tags=True,
            enable_execute_command=True,
            force_delete=True,
            force_new_deployment=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecs_service called "ci-runner", with deployment
    maximum percent 1, deployment minimum healthy percent 1, desired
    count 1, enable ecs managed tags true, enable execute command
    true, force delete true, force new deployment true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zone_rebalancing" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cluster" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="deployment_maximum_percent" type="number">
  Optional.
</ResponseField>

<ResponseField name="deployment_minimum_healthy_percent" type="number">
  Optional.
</ResponseField>

<ResponseField name="desired_count" type="number">
  Optional.
</ResponseField>

<ResponseField name="enable_ecs_managed_tags" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_execute_command" type="bool">
  Optional.
</ResponseField>

<ResponseField name="force_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="force_new_deployment" type="bool">
  Optional.
</ResponseField>

<ResponseField name="health_check_grace_period_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="iam_role" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="launch_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="propagate_tags" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scheduling_strategy" type="string">
  Optional.
</ResponseField>

<ResponseField name="sigint_rollback" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="task_definition" type="string">
  Optional.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="wait_for_steady_state" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone_rebalancing" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cluster" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="iam_role" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="launch_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="platform_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
