---
title: "aws_ecs_capacity_provider"
description: "`aws_ecs_capacity_provider` defines a real source of compute, an Auto Scaling group or Fargate..."
---

`aws_ecs_capacity_provider` defines a real source of compute, an Auto Scaling group or Fargate, that an `aws_ecs_cluster` can actually launch tasks onto, and the scaling behavior ECS should apply as task demand changes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own capacity provider"})

    		ubx.Resource(ecs.CapacityProvider, "example", ecs.CapacityProviderConfig{
    			Name:                     "example",
    			AutoScalingGroupProvider: map[string]any{"auto_scaling_group_arn": "example"},
    			ClusterName:              "example-cluster-name",
    			ManagedInstancesProvider: map[string]any{"infrastructure_role_arn": "example"},
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityProvider } from "@ubx/sdk-aws/aws/ecs/capacity_provider";

    export default stack("example", () => {
      intent({ summary: "example own capacity provider" });

      resource(CapacityProvider, "example", {
        name: "example",
        autoScalingGroupProvider: { autoScalingGroupArn: "example" },
        clusterName: "example-cluster-name",
        managedInstancesProvider: { infrastructureRoleArn: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecs.capacity_provider import CapacityProvider, CapacityProviderConfig

    def describe():
        ubx.intent("example own capacity provider")

        ubx.resource(CapacityProvider, "example", CapacityProviderConfig(
            name="example",
            auto_scaling_group_provider={"auto_scaling_group_arn": "example"},
            cluster_name="example-cluster-name",
            managed_instances_provider={"infrastructure_role_arn": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecs_capacity_provider called "example", with auto
    scaling group provider a set value, cluster name
    example-cluster-name, managed instances provider a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="auto_scaling_group_provider" type="object">
  Configures the Auto Scaling group that backs this ECS capacity provider, including its ARN, optional managed scaling parameters, and managed termination protection settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auto_scaling_group_arn" type="string" required>
      The Amazon Resource Name (ARN) of the Auto Scaling group that this ECS capacity provider manages to supply container instances for the cluster. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="managed_draining" type="string">
      Indicates whether Amazon ECS automatically manages draining of the Auto Scaling group instances during scale-in events, allowing tasks to stop gracefully before instance termination. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="managed_scaling" type="object">
      The managed scaling settings for the Auto Scaling group capacity provider. Optional.
      <Expandable title="properties">
        <ResponseField name="instance_warmup_period" type="number">
          Sets the number of seconds that a newly launched EC2 instance must warm up before its capacity counts toward the managed scaling target of the capacity provider, allowing it to initialize and start accepting tasks. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="maximum_scaling_step_size" type="number">
          The maximum number of container instances that can be added to or removed from the Auto Scaling group during a single managed scaling activity for the ECS capacity provider. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="minimum_scaling_step_size" type="number">
          Specifies the minimum number of instances that the managed scaling operation can add or remove in a single scaling activity for the Auto Scaling group used by this capacity provider. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Specifies whether managed scaling for the Auto Scaling Group is enabled or disabled, controlling ECS's automatic adjustment of the group's capacity. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_capacity" type="number">
          The target capacity percentage for the Auto Scaling group, used by managed scaling to determine when to scale out or in, with a default of 100. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_termination_protection" type="string">
      Enables or disables ECS-managed termination protection for the Auto Scaling group, which prevents ECS-managed instances from being terminated during scale-in events. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="managed_instances_provider" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="auto_repair_configuration" type="object">
      Configuration that enables or disables automatic repair (replacement) of unhealthy managed EC2 instances for the ECS capacity provider, controlled by the nested 'enabled' flag. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="actions_status" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="infrastructure_optimization" type="object">
      Defines how Amazon ECS Managed Instances optimizes the infrastructure in your capacity provider. Configure it to turn on or off the infrastructure optimization in your capacity provider, and to control the idle EC2 instances optimization delay. Optional.
      <Expandable title="properties">
        <ResponseField name="scale_in_after" type="number">
          This parameter defines the number of seconds Amazon ECS Managed Instances waits before optimizing EC2 instances that have become idle or underutilized. A longer delay increases the likelihood of placing new tasks on idle instances, reducing startup time. A shorter delay helps reduce infrastructure costs by optimizing idle instances more quickly. Valid values are: Not set (null) - Uses the default optimization behavior, `-1` - Disables automatic infrastructure optimization, `0` to `3600` (inclusive) - Specifies the number of seconds to wait before optimizing instances. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="infrastructure_role_arn" type="string" required>
      The ARN of the IAM role that Amazon ECS uses to manage the EC2 instances in the Auto Scaling group associated with this capacity provider. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_launch_template" type="object" required>
      Specifies the EC2 launch template that the capacity provider uses to launch instances into the Auto Scaling group it manages. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capacity_option_type" type="string">
          Determines whether the capacity provider launches On-Demand or Spot instances for the managed instances using this launch template. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="capacity_reservations" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="reservation_group_arn" type="string">
              The ARN of the EC2 Capacity Reservation Group that the launch template targets, ensuring instances launched by the capacity provider use capacity from that reservation group. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="reservation_preference" type="string">
              Specifies the capacity reservation preference for the instance launch template, with valid values 'open' (automatically match any open Capacity Reservation) and 'none' (do not use Capacity Reservations). Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ec2_instance_profile_arn" type="string" required>
          The ARN of the IAM instance profile that the EC2 instances launched by this capacity provider's managed instances will use to interact with AWS services. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fips_enabled" type="bool">
          Boolean flag that enables FIPS 140-2 validated cryptographic modules on the container instances launched using this capacity provider's managed instance launch template. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_metadata_tags_propagation" type="bool">
          Controls whether instance tags are propagated to the instance metadata service (IMDS) on EC2 instances launched from the launch template specified in the managed instances provider of the ECS capacity provider. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_requirements" type="object">
          Defines the instance requirements (e.g., vCPU count, memory, and other attributes) that the Auto Scaling group uses to automatically select EC2 instance types for the managed instances of the ECS capacity provider. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="accelerator_count" type="object">
              Specifies the minimum and maximum number of GPU accelerators (e.g., for compute-intensive workloads) that instances must have to match the instance requirements for the Auto Scaling group used by this ECS capacity provider. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  Specifies the maximum number of GPU accelerators (e.g., GPUs) that an instance must have to be selected for the capacity provider's managed instance fleet. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  Specifies the minimum number of accelerators (such as GPUs) that an instance must have to satisfy the instance requirements for the capacity provider's managed instance launch template. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="accelerator_manufacturers" type="list(string)">
              Specifies the acceptable manufacturers of hardware accelerators (GPUs) for the EC2 instance types that the capacity provider's managed instances can launch, restricting eligible instance types to those with accelerators from the listed manufacturers. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="accelerator_names" type="list(string)">
              Specifies the names of GPU accelerators (e.g., 'a100', 'v100') that the instance types must have to be considered for the capacity provider's managed instances. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="accelerator_total_memory_mi_b" type="object">
              Specifies the minimum and maximum total memory (in MiB) across all accelerators (e.g., GPUs) that an instance type must have to be eligible for use in the managed Auto Scaling group for this ECS capacity provider, filtering the instance types that can be launched. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum total accelerator memory, in MiB, that an instance can have to satisfy the instance requirements when ECS launches instances for the capacity provider's managed scaling. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum total accelerator memory in MiB that an instance type must have to be considered for the managed instance requirements of the ECS capacity provider's launch template. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="accelerator_types" type="list(string)">
              Specifies the types of hardware accelerators (such as gpu, fpga, or inference) that the EC2 instances must have when using instance requirements to select instance types for the capacity provider's managed instances. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="allowed_instance_types" type="list(string)">
              Restricts the eligible EC2 instance types for the capacity provider's managed instances to the explicitly listed instance types within the launch template's instance requirements. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bare_metal" type="string">
              Specifies whether the launch template's instance requirements must target bare metal instances, with values `included`, `excluded`, or `required` to indicate whether bare metal instance types are allowed, disallowed, or required. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="baseline_ebs_bandwidth_mbps" type="object">
              Specifies the minimum baseline EBS bandwidth in Mbps that an instance type must offer for the capacity provider's managed EC2 instances, used as part of the instance requirements to filter eligible instance types. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum baseline EBS bandwidth in Mbps that an instance type must not exceed when matching the instance requirements for the capacity provider's managed instances. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum baseline EBS bandwidth in Mbps that the instance type must support. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="burstable_performance" type="string">
              Specifies whether instances with burstable performance (e.g., T2, T3 families) are included or excluded when the capacity provider selects instance types for its managed instances. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cpu_manufacturers" type="list(string)">
              Restricts the EC2 instance types eligible for the capacity provider's managed instances to those with a CPU manufacturer listed (e.g., 'intel', 'amd', or 'amazon-web-services' for AWS Graviton processors). Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="excluded_instance_types" type="list(string)">
              Specifies a list of EC2 instance types to explicitly exclude from the capacity provider's managed Auto Scaling group when instance requirements are used to automatically select compute resources. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="instance_generations" type="list(string)">
              Specifies the instance generations (e.g., '5' for fifth-generation instances) that are allowed when selecting eligible EC2 instance types under the instance requirements. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="local_storage" type="string">
              Specifies whether the instance types used by the ECS capacity provider's Auto Scaling group must include local instance storage, with allowed values of included, excluded, or required. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="local_storage_types" type="list(string)">
              Specifies the local storage types (e.g., hdd or ssd) that an instance must support to be eligible for the capacity provider's managed instance fleet, as defined in the instance requirements of the launch template. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_spot_price_as_percentage_of_optimal_on_demand_price" type="number">
              The maximum spot price as a percentage of the optimal On-Demand price for the instance, used to set a ceiling on Spot Instance pricing within the Auto Scaling group managed by the ECS capacity provider. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="memory_gi_bper_vcpu" type="object">
              Defines the minimum and maximum amount of memory (in GiB) per vCPU that the EC2 instances used by the capacity provider's managed instance launch template must have. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum amount of memory per vCPU (in GiB) that an instance type must have to be included in the instance requirements for the capacity provider's managed instances. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  Defines the minimum amount of memory per vCPU (in GiB) that an instance must provide to be selected by the Auto Scaling group created for the ECS capacity provider's managed instance provisioning. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="memory_mi_b" type="object" required>
              Specifies the minimum and maximum memory (in MiB) that instances must have to be selected for the capacity provider, using the min and max sub-fields of this object. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum memory, in MiB, that an instance must have to be considered for use by this ECS capacity provider's managed instance fleet. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number" required>
                  The minimum amount of memory, in MiB, that an instance must have to satisfy the instance requirements for the ECS capacity provider's managed instance launch template. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="network_bandwidth_gbps" type="object">
              Specifies the minimum and maximum network bandwidth (in gigabits per second) that an instance must meet when the capacity provider launches managed instances, used to filter eligible EC2 instance types. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum network bandwidth in gigabits per second that an instance type must support to be considered eligible for the ECS capacity provider's managed instance pool. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  Defines the minimum network bandwidth (in Gbps) that an instance must offer to be selected for the capacity provider's managed instance launch template. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="network_interface_count" type="object">
              The minimum and maximum number of network interfaces that an instance type must support, used to filter eligible instance types when the capacity provider launches instances. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  The maximum number of network interfaces that an instance type must have for it to be considered for the capacity provider's managed instances. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  Sets the minimum number of network interfaces required on an EC2 instance type for the capacity provider to consider it when launching managed instances via the instance launch template. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="on_demand_max_price_percentage_over_lowest_price" type="number">
              For the managed EC2 Auto Scaling group behind this ECS capacity provider, this sets the maximum price you are willing to pay for On-Demand instances as a percentage above the lowest On-Demand price among the instance types that meet the specified requirements, influencing instance selection. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_hibernate_support" type="bool">
              Indicates whether the ECS capacity provider's managed instances must be of a type that supports EC2 hibernation. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="spot_max_price_percentage_over_lowest_price" type="number">
              Sets the maximum price you are willing to pay for a Spot Instance as a percentage of the lowest-priced Spot Instance, allowing you to bid above the baseline Spot price to increase the chance of instance fulfillment. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="total_local_storage_gb" type="object">
              Configures the minimum and maximum total local storage (in GiB) that an instance type must have for the capacity provider's managed instance launch template. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  Sets the maximum total local storage (in GB) that an instance must have to satisfy the instance requirements for the ECS capacity provider's managed instances. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number">
                  The minimum total local storage in GiB that an instance type must offer, used to filter which instance types are considered for the capacity provider. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="vcpu_count" type="object" required>
              Specifies the minimum and maximum number of vCPUs that the instance type must have, used to filter EC2 instances for the capacity provider's managed instance fleet. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="max" type="number">
                  Specifies the maximum number of vCPUs for the EC2 instance types that can be used by the managed instance provisioning of the ECS capacity provider. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="min" type="number" required>
                  The minimum number of vCPUs that an instance type must have to be considered for launching managed instances in the ECS capacity provider. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="local_storage_configuration" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="use_local_storage" type="bool">
              Indicates whether to enable the use of local ephemeral storage on the managed EC2 instances for tasks running in this ECS capacity provider. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monitoring" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="network_configuration" type="object" required>
          Specifies the VPC networking configuration for the EC2 instances launched by the capacity provider, including subnets, security groups, and whether to assign a public IP address, as part of the instance launch template in the managed instances provider. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="security_groups" type="list(string)" required>
              The list of security group IDs to assign to the EC2 instances launched by the managed instance provider's instance launch template, used as part of the network configuration for the ECS capacity provider. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subnets" type="list(string)" required>
              The list of subnet IDs in which the ECS capacity provider's container instances are launched, specified within the network configuration of the instance launch template. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="storage_configuration" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="storage_size_gi_b" type="number" required>
              Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="propagate_tags" type="string">
      Determines whether tags from the Auto Scaling group are propagated to the container instances launched by the capacity provider's managed instance provider; accepts values like `TAG_FROM_AUTO_SCALING_GROUP` and `NONE`. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Specifies the name of the capacity provider, which must be unique within the account and Region and contain only letters, numbers, underscores, and hyphens (up to 255 characters); if omitted, CloudFormation generates a unique name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the metadata tags (key-value pairs) to attach to the ECS capacity provider for identifying, organizing, and managing the resource. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key for a user-defined tag attached to the ECS capacity provider, which can be used for cost allocation, resource organization, and IAM-based access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a user-defined tag applied to the ECS capacity provider, used for metadata organization and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
