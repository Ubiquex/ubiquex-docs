---
title: "aws_ecs_capacity_provider"
description: "Real, generated bindings for `aws_ecs_capacity_provider`."
---

`aws_ecs_capacity_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ecs"

    cfg := ecs.CapacityProviderConfig{
        Name: "example-name",
        Cluster: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(ecs.CapacityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CapacityProvider } from
      "jsr:@ubx/sdk-aws/aws/ecs/capacity_provider";

    resource(CapacityProvider, "example", {
      name: "example",
      cluster: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecs import (
        CapacityProvider,
        CapacityProviderConfig,
    )

    cfg = CapacityProviderConfig(
        name="example",
        cluster="example",
        tags={"key": "value"},
    )
    ubx.resource(CapacityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecs_capacity_provider with name, cluster, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
