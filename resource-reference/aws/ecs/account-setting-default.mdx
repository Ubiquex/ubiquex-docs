---
title: "aws_ecs_account_setting_default"
description: "Real, generated bindings for `aws_ecs_account_setting_default`."
---

`aws_ecs_account_setting_default` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account setting default"})

    		ubx.Resource(ecs.AccountSettingDefault, "ci-runner", ecs.AccountSettingDefaultConfig{
    			Name:  "ci-runner",
    			Value: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountSettingDefault } from "jsr:@ubx/sdk-aws/aws/ecs/account_setting_default";

    export default stack("payments", () => {
      intent({ summary: "payments own account setting default" });

      resource(AccountSettingDefault, "ci-runner", {
        name: "ci-runner",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecs import AccountSettingDefault, AccountSettingDefaultConfig

    def describe():
        ubx.intent("payments own account setting default")

        ubx.resource(AccountSettingDefault, "ci-runner", AccountSettingDefaultConfig(
            name="ci-runner",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecs_account_setting_default called "ci-runner", with
    value example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
