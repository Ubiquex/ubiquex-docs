---
title: "aws_ecs_cluster_capacity_providers"
description: "Real, generated bindings for `aws_ecs_cluster_capacity_providers`."
---

`aws_ecs_cluster_capacity_providers` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecs"

    cfg := ecs.ClusterCapacityProvidersConfig{
        ClusterName: "example-cluster-name",
        CapacityProviders: []string{"example"},
    }
    ubx.Resource(ecs.ClusterCapacityProviders, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterCapacityProviders } from
      "jsr:@ubx/sdk-aws/aws/ecs/cluster_capacity_providers";

    resource(ClusterCapacityProviders, "example", {
      clusterName: "example",
      capacityProviders: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecs import (
        ClusterCapacityProviders,
        ClusterCapacityProvidersConfig,
    )

    cfg = ClusterCapacityProvidersConfig(
        cluster_name="example",
        capacity_providers=["example"],
    )
    ubx.resource(ClusterCapacityProviders, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecs_cluster_capacity_providers with cluster_name,
capacity_providers.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="capacity_providers" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
