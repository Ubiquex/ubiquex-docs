---
title: "aws_dax_cluster"
description: "Real, generated bindings for `aws_dax_cluster`."
---

`aws_dax_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dax "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster"})

    		ubx.Resource(dax.Cluster, "ci-runner", dax.ClusterConfig{
    			ClusterName:                   "example-cluster-name",
    			IamRoleArn:                    "arn:aws:iam::123456789012:policy/ci",
    			NodeType:                      "example",
    			ReplicationFactor:             1,
    			AvailabilityZones:             []string{"example"},
    			ClusterEndpointEncryptionType: "example",
    			Description:                   "Managed by ubx.",
    			NotificationTopicArn:          "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "jsr:@ubx/sdk-aws/aws/dax/cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster" });

      resource(Cluster, "ci-runner", {
        clusterName: "example-cluster-name",
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        nodeType: "example",
        replicationFactor: 1,
        availabilityZones: ["example"],
        clusterEndpointEncryptionType: "example",
        description: "Managed by ubx.",
        notificationTopicArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dax import Cluster, ClusterConfig

    def describe():
        ubx.intent("payments own cluster")

        ubx.resource(Cluster, "ci-runner", ClusterConfig(
            cluster_name="example-cluster-name",
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            node_type="example",
            replication_factor=1,
            availability_zones=["example"],
            cluster_endpoint_encryption_type="example",
            description="Managed by ubx.",
            notification_topic_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dax_cluster, with cluster name example-cluster-name,
    iam role arn arn:aws:iam::123456789012:policy/ci, node type
    example, replication factor 1, availability zones a set value,
    cluster endpoint encryption type example, description Managed by
    ubx., notification topic arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zones" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="cluster_endpoint_encryption_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="node_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="notification_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="parameter_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_factor" type="number" required>
  Required.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="configuration_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="nodes" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="address" type="string">
    </ResponseField>
    <ResponseField name="availability_zone" type="string">
    </ResponseField>
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="port" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="parameter_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="port" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
