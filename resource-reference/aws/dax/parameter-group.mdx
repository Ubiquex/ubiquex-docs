---
title: "aws_dax_parameter_group"
description: "`aws_dax_parameter_group` sets real, engine-level configuration, TTL and record count limits among them, for an `aws_dax_cluster`..."
---

`aws_dax_parameter_group` sets real, engine-level configuration, TTL and record count limits among them, for an `aws_dax_cluster`, kept as its own resource so the same parameter set can be applied consistently across multiple clusters.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dax "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own parameter group"})

    		ubx.Resource(dax.ParameterGroup, "example", dax.ParameterGroupConfig{
    			Description:         "Managed by ubx.",
    			ParameterGroupName:  "example-parameter-group-name",
    			ParameterNameValues: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ParameterGroup } from "@ubx/sdk-aws/aws/dax/parameter_group";

    export default stack("example", () => {
      intent({ summary: "example own parameter group" });

      resource(ParameterGroup, "example", {
        description: "Managed by ubx.",
        parameterGroupName: "example-parameter-group-name",
        parameterNameValues: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dax import ParameterGroup, ParameterGroupConfig

    def describe():
        ubx.intent("example own parameter group")

        ubx.resource(ParameterGroup, "example", ParameterGroupConfig(
            description="Managed by ubx.",
            parameter_group_name="example-parameter-group-name",
            parameter_name_values="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dax_parameter_group, with description Managed by ubx.,
    parameter group name example-parameter-group-name, parameter name
    values example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  A description of the parameter group. Optional.
</ResponseField>

<ResponseField name="parameter_group_name" type="string">
  The name of the parameter group. Optional.
</ResponseField>

<ResponseField name="parameter_name_values" type="any">
  An array of name-value pairs for the parameters in the group. Each element in the array represents a single parameter. Optional.
</ResponseField>
