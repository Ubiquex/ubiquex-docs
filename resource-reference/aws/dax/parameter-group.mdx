---
title: "aws_dax_parameter_group"
description: "Real, generated bindings for `aws_dax_parameter_group`."
---

`aws_dax_parameter_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dax "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own parameter group"})

    		ubx.Resource(dax.ParameterGroup, "ci-runner", dax.ParameterGroupConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ParameterGroup } from "jsr:@ubx/sdk-aws/aws/dax/parameter_group";

    export default stack("payments", () => {
      intent({ summary: "payments own parameter group" });

      resource(ParameterGroup, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dax import ParameterGroup, ParameterGroupConfig

    def describe():
        ubx.intent("payments own parameter group")

        ubx.resource(ParameterGroup, "ci-runner", ParameterGroupConfig(
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dax_parameter_group called "ci-runner", with
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
