---
title: "aws_dax_parameter_group"
description: "Real, generated bindings for `aws_dax_parameter_group`."
---

`aws_dax_parameter_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dax"

    cfg := dax.ParameterGroupConfig{
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(dax.ParameterGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ParameterGroup } from
      "jsr:@ubx/sdk-aws/aws/dax/parameter_group";

    resource(ParameterGroup, "example", {
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dax import ParameterGroup, ParameterGroupConfig

    cfg = ParameterGroupConfig(
        name="example",
        description="example",
    )
    ubx.resource(ParameterGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dax_parameter_group with name, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
