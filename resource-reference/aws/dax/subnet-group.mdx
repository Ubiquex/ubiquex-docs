---
title: "aws_dax_subnet_group"
description: "Real, generated bindings for `aws_dax_subnet_group`."
---

`aws_dax_subnet_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dax"

    cfg := dax.SubnetGroupConfig{
        Name: "example-name",
        SubnetIds: []string{"example"},
        Description: "example",
    }
    ubx.Resource(dax.SubnetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SubnetGroup } from
      "jsr:@ubx/sdk-aws/aws/dax/subnet_group";

    resource(SubnetGroup, "example", {
      name: "example",
      subnetIds: ["example"],
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dax import SubnetGroup, SubnetGroupConfig

    cfg = SubnetGroupConfig(
        name="example",
        subnet_ids=["example"],
        description="example",
    )
    ubx.resource(SubnetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dax_subnet_group with name, subnet_ids,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
