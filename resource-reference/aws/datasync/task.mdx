---
title: "aws_datasync_task"
description: "Real, generated bindings for `aws_datasync_task`."
---

`aws_datasync_task` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/datasync"

    cfg := datasync.TaskConfig{
        DestinationLocationArn: "example",
        SourceLocationArn: "example",
        CloudwatchLogGroupArn: "example",
        Name: "example-name",
    }
    ubx.Resource(datasync.Task, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Task } from "jsr:@ubx/sdk-aws/aws/datasync/task";

    resource(Task, "example", {
      destinationLocationArn: "example",
      sourceLocationArn: "example",
      cloudwatchLogGroupArn: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datasync import Task, TaskConfig

    cfg = TaskConfig(
        destination_location_arn="example",
        source_location_arn="example",
        cloudwatch_log_group_arn="example",
        name="example",
    )
    ubx.resource(Task, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datasync_task with destination_location_arn,
source_location_arn, cloudwatch_log_group_arn, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cloudwatch_log_group_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination_location_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_location_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="task_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="task_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
