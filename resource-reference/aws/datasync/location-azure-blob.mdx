---
title: "aws_datasync_location_azure_blob"
description: "Real, generated bindings for `aws_datasync_location_azure_blob`."
---

`aws_datasync_location_azure_blob` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/datasync"

    cfg := datasync.LocationAzureBlobConfig{
        AgentArns: []string{"example"},
        AuthenticationType: "example",
        ContainerUrl: "example",
        AccessTier: "example",
        BlobType: "example",
    }
    ubx.Resource(datasync.LocationAzureBlob, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LocationAzureBlob } from
      "jsr:@ubx/sdk-aws/aws/datasync/location_azure_blob";

    resource(LocationAzureBlob, "example", {
      agentArns: ["example"],
      authenticationType: "example",
      containerUrl: "example",
      accessTier: "example",
      blobType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datasync import (
        LocationAzureBlob,
        LocationAzureBlobConfig,
    )

    cfg = LocationAzureBlobConfig(
        agent_arns=["example"],
        authentication_type="example",
        container_url="example",
        access_tier="example",
        blob_type="example",
    )
    ubx.resource(LocationAzureBlob, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datasync_location_azure_blob with agent_arns,
authentication_type, container_url, access_tier, blob_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_tier" type="string">
  Optional.
</ResponseField>

<ResponseField name="agent_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="authentication_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="blob_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="container_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
