---
title: "aws_datasync_location_object_storage"
description: "Real, generated bindings for `aws_datasync_location_object_storage`."
---

`aws_datasync_location_object_storage` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datasync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datasync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own location object storage"})

    		ubx.Resource(datasync.LocationObjectStorage, "ci-runner", datasync.LocationObjectStorageConfig{
    			BucketName:        "example-bucket-name",
    			ServerHostname:    "example",
    			AccessKey:         "example",
    			AgentArns:         []string{"example"},
    			SecretKey:         "example",
    			ServerCertificate: "example",
    			ServerPort:        1,
    			ServerProtocol:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocationObjectStorage } from "jsr:@ubx/sdk-aws/aws/datasync/location_object_storage";

    export default stack("payments", () => {
      intent({ summary: "payments own location object storage" });

      resource(LocationObjectStorage, "ci-runner", {
        bucketName: "example-bucket-name",
        serverHostname: "example",
        accessKey: "example",
        agentArns: ["example"],
        secretKey: "example",
        serverCertificate: "example",
        serverPort: 1,
        serverProtocol: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datasync import LocationObjectStorage, LocationObjectStorageConfig

    def describe():
        ubx.intent("payments own location object storage")

        ubx.resource(LocationObjectStorage, "ci-runner", LocationObjectStorageConfig(
            bucket_name="example-bucket-name",
            server_hostname="example",
            access_key="example",
            agent_arns=["example"],
            secret_key="example",
            server_certificate="example",
            server_port=1,
            server_protocol="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datasync_location_object_storage, with bucket name
    example-bucket-name, server hostname example, access key example,
    agent arns a set value, secret key example, server certificate
    example, server port 1, server protocol example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="agent_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="server_certificate" type="string">
  Optional.
</ResponseField>

<ResponseField name="server_hostname" type="string" required>
  Required.
</ResponseField>

<ResponseField name="server_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="server_protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
