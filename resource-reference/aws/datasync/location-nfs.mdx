---
title: "aws_datasync_location_nfs"
description: "Real, generated bindings for `aws_datasync_location_nfs`."
---

`aws_datasync_location_nfs` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datasync"

    cfg := datasync.LocationNfsConfig{
        ServerHostname: "example-server-hostname",
        Subdirectory: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(datasync.LocationNfs, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LocationNfs } from
      "jsr:@ubx/sdk-aws/aws/datasync/location_nfs";

    resource(LocationNfs, "example", {
      serverHostname: "example",
      subdirectory: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datasync import LocationNfs, LocationNfsConfig

    cfg = LocationNfsConfig(
        server_hostname="example",
        subdirectory="example",
        tags={"key": "value"},
    )
    ubx.resource(LocationNfs, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datasync_location_nfs with server_hostname,
subdirectory, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_hostname" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subdirectory" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
