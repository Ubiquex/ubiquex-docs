---
title: "aws_datasync_location_smb"
description: "Real, generated bindings for `aws_datasync_location_smb`."
---

`aws_datasync_location_smb` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datasync"

    cfg := datasync.LocationSmbConfig{
        AgentArns: []string{"example"},
        Password: "example",
        ServerHostname: "example-server-hostname",
        Subdirectory: "example",
        User: "example",
        Domain: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(datasync.LocationSmb, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LocationSmb } from
      "jsr:@ubx/sdk-aws/aws/datasync/location_smb";

    resource(LocationSmb, "example", {
      agentArns: ["example"],
      password: "example",
      serverHostname: "example",
      subdirectory: "example",
      user: "example",
      domain: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.datasync import LocationSmb, LocationSmbConfig

    cfg = LocationSmbConfig(
        agent_arns=["example"],
        password="example",
        server_hostname="example",
        subdirectory="example",
        user="example",
        domain="example",
        tags={"key": "value"},
    )
    ubx.resource(LocationSmb, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_datasync_location_smb with agent_arns, password,
server_hostname, subdirectory, user, domain, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="agent_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="domain" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_hostname" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subdirectory" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
