---
title: "aws_datasync_location_smb"
description: "Real, generated bindings for `aws_datasync_location_smb`."
---

`aws_datasync_location_smb` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datasync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datasync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own location smb"})

    		ubx.Resource(datasync.LocationSmb, "ci-runner", datasync.LocationSmbConfig{
    			AgentArns:      []string{"example"},
    			Password:       "example",
    			ServerHostname: "example",
    			Subdirectory:   "example",
    			User:           "example",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocationSmb } from "jsr:@ubx/sdk-aws/aws/datasync/location_smb";

    export default stack("payments", () => {
      intent({ summary: "payments own location smb" });

      resource(LocationSmb, "ci-runner", {
        agentArns: ["example"],
        password: "example",
        serverHostname: "example",
        subdirectory: "example",
        user: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datasync import LocationSmb, LocationSmbConfig

    def describe():
        ubx.intent("payments own location smb")

        ubx.resource(LocationSmb, "ci-runner", LocationSmbConfig(
            agent_arns=["example"],
            password="example",
            server_hostname="example",
            subdirectory="example",
            user="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datasync_location_smb, with agent arns a set value,
    password example, server hostname example, subdirectory example,
    user example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="agent_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="domain" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_hostname" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subdirectory" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
