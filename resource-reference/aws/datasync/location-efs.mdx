---
title: "aws_data_sync_location_efs"
description: "`aws_data_sync_location_efs` registers a real Amazon EFS file system as a DataSync transfer endpoint..."
---

`aws_data_sync_location_efs` registers a real Amazon EFS file system as a DataSync transfer endpoint, the same real endpoint role as `aws_data_sync_location_s3` but for EFS instead of S3.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	datasync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/datasync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own location efs"})

    		ubx.Resource(datasync.LocationEfs, "example", datasync.LocationEfsConfig{
    			Ec2Config:               map[string]any{"security_group_arns": []string{"example"}, "subnet_arn": "example"},
    			AccessPointArn:          "arn:aws:iam::123456789012:policy/example",
    			EfsFilesystemArn:        "arn:aws:iam::123456789012:policy/example",
    			FileSystemAccessRoleArn: "arn:aws:iam::123456789012:policy/example",
    			InTransitEncryption:     "example",
    			Subdirectory:            "example",
    			Tags:                    []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocationEfs } from "./local-sdk/aws/sdk/typescript/aws/datasync/location_efs.ts";

    export default stack("example", () => {
      intent({ summary: "example own location efs" });

      resource(LocationEfs, "example", {
        ec2Config: { securityGroupArns: ["example"], subnetArn: "example" },
        accessPointArn: "arn:aws:iam::123456789012:policy/example",
        efsFilesystemArn: "arn:aws:iam::123456789012:policy/example",
        fileSystemAccessRoleArn: "arn:aws:iam::123456789012:policy/example",
        inTransitEncryption: "example",
        subdirectory: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.datasync import LocationEfs, LocationEfsConfig
    from ubx.aws.datasync.location_efs import LocationEfs_Ec2Config, LocationEfs_Tags

    def describe():
        ubx.intent("example own location efs")

        ubx.resource(LocationEfs, "example", LocationEfsConfig(
            ec2_config=LocationEfs_Ec2Config(security_group_arns=["example"], subnet_arn="example"),
            access_point_arn="arn:aws:iam::123456789012:policy/example",
            efs_filesystem_arn="arn:aws:iam::123456789012:policy/example",
            file_system_access_role_arn="arn:aws:iam::123456789012:policy/example",
            in_transit_encryption="example",
            subdirectory="example",
            tags=[LocationEfs_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_sync_location_efs, with ec2 config a set value,
    access point arn arn:aws:iam::123456789012:policy/example, efs
    filesystem arn arn:aws:iam::123456789012:policy/example, file
    system access role arn arn:aws:iam::123456789012:policy/example,
    in transit encryption example, subdirectory example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_point_arn" type="string">
  The Amazon Resource Name (ARN) for the Amazon EFS Access point that DataSync uses when accessing the EFS file system. Optional.
</ResponseField>

<ResponseField name="ec2_config" type="object" required>
  The subnet and security group that DataSync uses to access target EFS file system. Required.
  <Expandable title="properties">
    <ResponseField name="security_group_arns" type="list(string)" required>
      The Amazon Resource Names (ARNs) of the security groups that are configured for the Amazon EC2 resource. Required.
    </ResponseField>
    <ResponseField name="subnet_arn" type="string" required>
      The ARN of the subnet that DataSync uses to access the target EFS file system. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="efs_filesystem_arn" type="string">
  The Amazon Resource Name (ARN) for the Amazon EFS file system. Optional.
</ResponseField>

<ResponseField name="file_system_access_role_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS IAM role that the DataSync will assume when mounting the EFS file system. Optional.
</ResponseField>

<ResponseField name="in_transit_encryption" type="string">
  Protocol that is used for encrypting the traffic exchanged between the DataSync Agent and the EFS file system. Optional.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  A subdirectory in the location's path. This subdirectory in the EFS file system is used to read data from the EFS source location or write data to the EFS destination. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to this AWS DataSync EFS location, used to identify and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair attached to this AWS DataSync location for EFS, used for metadata and resource categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location_arn" type="string">
  The Amazon Resource Name (ARN) of the Amazon EFS file system location that is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="location_uri" type="string">
  The URL of the EFS location that was described. Computed by AWS after `ship`.
</ResponseField>
