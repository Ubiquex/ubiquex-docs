---
title: "aws_datasync_location_fsx_lustre_file_system"
description: "Real, generated bindings for `aws_datasync_location_fsx_lustre_file_system`."
---

`aws_datasync_location_fsx_lustre_file_system` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datasync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datasync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own location fsx lustre file system"})

    		ubx.Resource(datasync.LocationFsxLustreFileSystem, "ci-runner", datasync.LocationFsxLustreFileSystemConfig{
    			FsxFilesystemArn:  "arn:aws:iam::123456789012:policy/ci",
    			SecurityGroupArns: []string{"example"},
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LocationFsxLustreFileSystem } from "jsr:@ubx/sdk-aws/aws/datasync/location_fsx_lustre_file_system";

    export default stack("payments", () => {
      intent({ summary: "payments own location fsx lustre file system" });

      resource(LocationFsxLustreFileSystem, "ci-runner", {
        fsxFilesystemArn: "arn:aws:iam::123456789012:policy/ci",
        securityGroupArns: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datasync import LocationFsxLustreFileSystem, LocationFsxLustreFileSystemConfig

    def describe():
        ubx.intent("payments own location fsx lustre file system")

        ubx.resource(LocationFsxLustreFileSystem, "ci-runner", LocationFsxLustreFileSystemConfig(
            fsx_filesystem_arn="arn:aws:iam::123456789012:policy/ci",
            security_group_arns=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datasync_location_fsx_lustre_file_system, with fsx
    filesystem arn arn:aws:iam::123456789012:policy/ci, security
    group arns a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="fsx_filesystem_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subdirectory" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>
