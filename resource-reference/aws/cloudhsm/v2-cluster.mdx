---
title: "aws_cloudhsm_v2_cluster"
description: "Real, generated bindings for `aws_cloudhsm_v2_cluster`."
---

`aws_cloudhsm_v2_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudhsm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudhsm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 cluster"})

    		ubx.Resource(cloudhsm.V2Cluster, "ci-runner", cloudhsm.V2ClusterConfig{
    			HsmType:                "example",
    			SubnetIds:              []string{"example"},
    			SourceBackupIdentifier: "example",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2Cluster } from "jsr:@ubx/sdk-aws/aws/cloudhsm/v2_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 cluster" });

      resource(V2Cluster, "ci-runner", {
        hsmType: "example",
        subnetIds: ["example"],
        sourceBackupIdentifier: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudhsm import V2Cluster, V2ClusterConfig

    def describe():
        ubx.intent("payments own v2 cluster")

        ubx.resource(V2Cluster, "ci-runner", V2ClusterConfig(
            hsm_type="example",
            subnet_ids=["example"],
            source_backup_identifier="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudhsm_v2_cluster, with hsm type example, subnet ids
    a set value, source backup identifier example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hsm_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_backup_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="cluster_certificates" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="aws_hardware_certificate" type="string">
    </ResponseField>
    <ResponseField name="cluster_certificate" type="string">
    </ResponseField>
    <ResponseField name="cluster_csr" type="string">
    </ResponseField>
    <ResponseField name="hsm_certificate" type="string">
    </ResponseField>
    <ResponseField name="manufacturer_hardware_certificate" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
