---
title: "aws_robo_maker_robot"
description: "`aws_robo_maker_robot` registered a real physical robot as a member of an `aws_robo_maker_fleet`."
---

Like the rest of AWS RoboMaker, this describes a service AWS fully discontinued on September 10, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	robomaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/robomaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker robot"})

    		ubx.Resource(robomaker.Robot, "example", robomaker.RobotConfig{
    			Architecture:      "example",
    			GreengrassGroupId: "example",
    			Name:              "example",
    			Fleet:             "example",
    			Tags:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Robot } from "@ubx/sdk-aws/aws/robomaker/robot";

    export default stack("example", () => {
      intent({ summary: "example own maker robot" });

      resource(Robot, "example", {
        architecture: "example",
        greengrassGroupId: "example",
        name: "example",
        fleet: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.robomaker.robot import Robot, RobotConfig

    def describe():
        ubx.intent("example own maker robot")

        ubx.resource(Robot, "example", RobotConfig(
            architecture="example",
            greengrass_group_id="example",
            name="example",
            fleet="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_robo_maker_robot called "example", with architecture
    example, greengrass group id example, fleet example, tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="architecture" type="string" required>
  The target architecture of the robot. Required.
</ResponseField>

<ResponseField name="fleet" type="string">
  The Amazon Resource Name (ARN) of the fleet. Optional.
</ResponseField>

<ResponseField name="greengrass_group_id" type="string" required>
  The Greengrass group id. Required.
</ResponseField>

<ResponseField name="name" type="string">
  The name for the robot. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the AWS RoboMaker robot within your AWS account. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
