---
title: "aws_docdbelastic_cluster"
description: "Real, generated bindings for `aws_docdbelastic_cluster`."
---

`aws_docdbelastic_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	docdbelastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/docdbelastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster"})

    		ubx.Resource(docdbelastic.Cluster, "ci-runner", docdbelastic.ClusterConfig{
    			AdminUserName:     "example-admin-user-name",
    			AdminUserPassword: "example",
    			AuthType:          "example",
    			Name:              "ci-runner",
    			ShardCapacity:     1,
    			ShardCount:        1,
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "jsr:@ubx/sdk-aws/aws/docdbelastic/cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster" });

      resource(Cluster, "ci-runner", {
        adminUserName: "example-admin-user-name",
        adminUserPassword: "example",
        authType: "example",
        name: "ci-runner",
        shardCapacity: 1,
        shardCount: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.docdbelastic import Cluster, ClusterConfig

    def describe():
        ubx.intent("payments own cluster")

        ubx.resource(Cluster, "ci-runner", ClusterConfig(
            admin_user_name="example-admin-user-name",
            admin_user_password="example",
            auth_type="example",
            name="ci-runner",
            shard_capacity=1,
            shard_count=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_docdbelastic_cluster called "ci-runner", with admin
    user name example-admin-user-name, admin user password example,
    auth type example, shard capacity 1, shard count 1, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="admin_user_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="admin_user_password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="auth_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="backup_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="preferred_backup_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="preferred_maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shard_capacity" type="number" required>
  Required.
</ResponseField>

<ResponseField name="shard_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="shard_instance_count" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="backup_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="preferred_backup_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="preferred_maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shard_instance_count" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
