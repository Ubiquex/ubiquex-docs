---
title: "aws_codedeploy_deployment_group"
description: "Real, generated bindings for `aws_codedeploy_deployment_group`."
---

`aws_codedeploy_deployment_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own deployment group"})

    		ubx.Resource(codedeploy.DeploymentGroup, "ci-runner", codedeploy.DeploymentGroupConfig{
    			AppName:                   "example-app-name",
    			DeploymentGroupName:       "example-deployment-group-name",
    			ServiceRoleArn:            "arn:aws:iam::123456789012:policy/ci",
    			AutoscalingGroups:         []string{"example"},
    			DeploymentConfigName:      "example-deployment-config-name",
    			OutdatedInstancesStrategy: "example",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    			TerminationHookEnabled:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeploymentGroup } from "jsr:@ubx/sdk-aws/aws/codedeploy/deployment_group";

    export default stack("payments", () => {
      intent({ summary: "payments own deployment group" });

      resource(DeploymentGroup, "ci-runner", {
        appName: "example-app-name",
        deploymentGroupName: "example-deployment-group-name",
        serviceRoleArn: "arn:aws:iam::123456789012:policy/ci",
        autoscalingGroups: ["example"],
        deploymentConfigName: "example-deployment-config-name",
        outdatedInstancesStrategy: "example",
        tags: { "managed-by": "ubx" },
        terminationHookEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codedeploy import DeploymentGroup, DeploymentGroupConfig

    def describe():
        ubx.intent("payments own deployment group")

        ubx.resource(DeploymentGroup, "ci-runner", DeploymentGroupConfig(
            app_name="example-app-name",
            deployment_group_name="example-deployment-group-name",
            service_role_arn="arn:aws:iam::123456789012:policy/ci",
            autoscaling_groups=["example"],
            deployment_config_name="example-deployment-config-name",
            outdated_instances_strategy="example",
            tags={"managed-by": "ubx"},
            termination_hook_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codedeploy_deployment_group, with app name
    example-app-name, deployment group name
    example-deployment-group-name, service role arn
    arn:aws:iam::123456789012:policy/ci, autoscaling groups a set
    value, deployment config name example-deployment-config-name,
    outdated instances strategy example, tags a set value,
    termination hook enabled true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="autoscaling_groups" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="deployment_config_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="deployment_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="outdated_instances_strategy" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="termination_hook_enabled" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="compute_platform" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
