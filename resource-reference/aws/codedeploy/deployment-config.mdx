---
title: "aws_codedeploy_deployment_config"
description: "Real, generated bindings for `aws_codedeploy_deployment_config`."
---

`aws_codedeploy_deployment_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codedeploy"

    cfg := codedeploy.DeploymentConfigConfig{
        DeploymentConfigName: "example-deployment-config-name",
        ComputePlatform: "example",
    }
    ubx.Resource(codedeploy.DeploymentConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DeploymentConfig } from
      "jsr:@ubx/sdk-aws/aws/codedeploy/deployment_config";

    resource(DeploymentConfig, "example", {
      deploymentConfigName: "example",
      computePlatform: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codedeploy import (
        DeploymentConfig,
        DeploymentConfigConfig,
    )

    cfg = DeploymentConfigConfig(
        deployment_config_name="example",
        compute_platform="example",
    )
    ubx.resource(DeploymentConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codedeploy_deployment_config with
deployment_config_name, compute_platform.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="compute_platform" type="string">
  Optional.
</ResponseField>

<ResponseField name="deployment_config_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_config_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
