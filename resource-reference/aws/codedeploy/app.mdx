---
title: "aws_codedeploy_app"
description: "Real, generated bindings for `aws_codedeploy_app`."
---

`aws_codedeploy_app` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codedeploy"

    cfg := codedeploy.AppConfig{
        Name: "example-name",
        ComputePlatform: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(codedeploy.App, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { App } from "jsr:@ubx/sdk-aws/aws/codedeploy/app";

    resource(App, "example", {
      name: "example",
      computePlatform: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codedeploy import App, AppConfig

    cfg = AppConfig(
        name="example",
        compute_platform="example",
        tags={"key": "value"},
    )
    ubx.resource(App, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codedeploy_app with name, compute_platform, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="compute_platform" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="application_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="github_account_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="linked_to_github" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
