---
title: "aws_cloudformation_stack_set_instance"
description: "Real, generated bindings for `aws_cloudformation_stack_set_instance`."
---

`aws_cloudformation_stack_set_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stack set instance"})

    		ubx.Resource(cloudformation.StackSetInstance, "ci-runner", cloudformation.StackSetInstanceConfig{
    			StackSetName:       "example-stack-set-name",
    			CallAs:             "example",
    			ParameterOverrides: map[string]string{"managed-by": "ubx"},
    			RetainStack:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StackSetInstance } from "jsr:@ubx/sdk-aws/aws/cloudformation/stack_set_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own stack set instance" });

      resource(StackSetInstance, "ci-runner", {
        stackSetName: "example-stack-set-name",
        callAs: "example",
        parameterOverrides: { "managed-by": "ubx" },
        retainStack: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudformation import StackSetInstance, StackSetInstanceConfig

    def describe():
        ubx.intent("payments own stack set instance")

        ubx.resource(StackSetInstance, "ci-runner", StackSetInstanceConfig(
            stack_set_name="example-stack-set-name",
            call_as="example",
            parameter_overrides={"managed-by": "ubx"},
            retain_stack=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudformation_stack_set_instance, with stack set name
    example-stack-set-name, call as example, parameter overrides a
    set value, retain stack true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="call_as" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parameter_overrides" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retain_stack" type="bool">
  Optional.
</ResponseField>

<ResponseField name="stack_set_instance_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_set_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="organizational_unit_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="stack_instance_summaries" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
    </ResponseField>
    <ResponseField name="organizational_unit_id" type="string">
    </ResponseField>
    <ResponseField name="stack_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stack_set_instance_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
