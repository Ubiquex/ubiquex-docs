---
title: "aws_cloudformation_stack_set"
description: "Real, generated bindings for `aws_cloudformation_stack_set`."
---

`aws_cloudformation_stack_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stack set"})

    		ubx.Resource(cloudformation.StackSet, "ci-runner", cloudformation.StackSetConfig{
    			Name:                  "ci-runner",
    			AdministrationRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			CallAs:                "example",
    			Capabilities:          []string{"example"},
    			Description:           "Managed by ubx.",
    			Parameters:            map[string]string{"managed-by": "ubx"},
    			PermissionModel:       "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StackSet } from "jsr:@ubx/sdk-aws/aws/cloudformation/stack_set";

    export default stack("payments", () => {
      intent({ summary: "payments own stack set" });

      resource(StackSet, "ci-runner", {
        name: "ci-runner",
        administrationRoleArn: "arn:aws:iam::123456789012:policy/ci",
        callAs: "example",
        capabilities: ["example"],
        description: "Managed by ubx.",
        parameters: { "managed-by": "ubx" },
        permissionModel: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudformation import StackSet, StackSetConfig

    def describe():
        ubx.intent("payments own stack set")

        ubx.resource(StackSet, "ci-runner", StackSetConfig(
            name="ci-runner",
            administration_role_arn="arn:aws:iam::123456789012:policy/ci",
            call_as="example",
            capabilities=["example"],
            description="Managed by ubx.",
            parameters={"managed-by": "ubx"},
            permission_model="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudformation_stack_set called "ci-runner", with
    administration role arn arn:aws:iam::123456789012:policy/ci, call
    as example, capabilities a set value, description Managed by
    ubx., parameters a set value, permission model example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="administration_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="call_as" type="string">
  Optional.
</ResponseField>

<ResponseField name="capabilities" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="permission_model" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_body" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_url" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="execution_role_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_set_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_body" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
