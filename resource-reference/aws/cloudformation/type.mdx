---
title: "aws_cloudformation_type"
description: "Real, generated bindings for `aws_cloudformation_type`."
---

`aws_cloudformation_type` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudformation"

    cfg := cloudformation.TypeConfig{
        SchemaHandlerPackage: "example",
        TypeName: "example-type-name",
        ExecutionRoleArn: "example",
        Type: "example",
    }
    ubx.Resource(cloudformation.Type, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Type } from "jsr:@ubx/sdk-aws/aws/cloudformation/type";

    resource(Type, "example", {
      schemaHandlerPackage: "example",
      typeName: "example",
      executionRoleArn: "example",
      type: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudformation import Type, TypeConfig

    cfg = TypeConfig(
        schema_handler_package="example",
        type_name="example",
        execution_role_arn="example",
        type="example",
    )
    ubx.resource(Type, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudformation_type with schema_handler_package,
type_name, execution_role_arn, type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schema_handler_package" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_version_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deprecated_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="documentation_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_default_version" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioning_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schema" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="visibility" type="string">
  Computed by AWS after `ship`.
</ResponseField>
