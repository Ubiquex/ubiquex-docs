---
title: "aws_memorydb_multi_region_cluster"
description: "Real, generated bindings for `aws_memorydb_multi_region_cluster`."
---

`aws_memorydb_multi_region_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memorydb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/memorydb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own multi region cluster"})

    		ubx.Resource(memorydb.MultiRegionCluster, "ci-runner", memorydb.MultiRegionClusterConfig{
    			MultiRegionClusterNameSuffix: "example",
    			NodeType:                     "example",
    			Description:                  "Managed by ubx.",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    			UpdateStrategy:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MultiRegionCluster } from "jsr:@ubx/sdk-aws/aws/memorydb/multi_region_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own multi region cluster" });

      resource(MultiRegionCluster, "ci-runner", {
        multiRegionClusterNameSuffix: "example",
        nodeType: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
        updateStrategy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.memorydb import MultiRegionCluster, MultiRegionClusterConfig

    def describe():
        ubx.intent("payments own multi region cluster")

        ubx.resource(MultiRegionCluster, "ci-runner", MultiRegionClusterConfig(
            multi_region_cluster_name_suffix="example",
            node_type="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
            update_strategy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_memorydb_multi_region_cluster, with multi region
    cluster name suffix example, node type example, description
    Managed by ubx., tags a set value, update strategy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="engine" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="multi_region_cluster_name_suffix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="multi_region_parameter_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="node_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="num_shards" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tls_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="update_strategy" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="multi_region_cluster_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="multi_region_parameter_group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="num_shards" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tls_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
