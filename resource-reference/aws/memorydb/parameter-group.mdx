---
title: "aws_memory_db_parameter_group"
description: "`aws_memory_db_parameter_group` defines a real set of engine configuration parameters that can be applied to one or more `aws_memory_db_cluster`s..."
---

`aws_memory_db_parameter_group` defines a real set of engine configuration parameters that can be applied to one or more `aws_memory_db_cluster`s instead of hand-tuning each cluster individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memorydb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/memorydb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own parameter group"})

    		ubx.Resource(memorydb.ParameterGroup, "example", memorydb.ParameterGroupConfig{
    			Family:             "example",
    			ParameterGroupName: "example-parameter-group-name",
    			Description:        "Managed by ubx.",
    			Parameters:         "example",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ParameterGroup } from "@ubx/sdk-aws/aws/memorydb/parameter_group";

    export default stack("example", () => {
      intent({ summary: "example own parameter group" });

      resource(ParameterGroup, "example", {
        family: "example",
        parameterGroupName: "example-parameter-group-name",
        description: "Managed by ubx.",
        parameters: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.memorydb import ParameterGroup, ParameterGroupConfig
    from ubx.aws.memorydb.parameter_group import ParameterGroup_Tags

    def describe():
        ubx.intent("example own parameter group")

        ubx.resource(ParameterGroup, "example", ParameterGroupConfig(
            family="example",
            parameter_group_name="example-parameter-group-name",
            description="Managed by ubx.",
            parameters="example",
            tags=[ParameterGroup_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_memory_db_parameter_group, with family example,
    parameter group name example-parameter-group-name, description
    Managed by ubx., parameters example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description of the parameter group. Optional.
</ResponseField>

<ResponseField name="family" type="string" required>
  The name of the parameter group family that this parameter group is compatible with. Required.
</ResponseField>

<ResponseField name="parameter_group_name" type="string" required>
  The name of the parameter group. Required.
</ResponseField>

<ResponseField name="parameters" type="any">
  An map of parameter names and values for the parameter update. You must supply at least one parameter name and value; subsequent arguments are optional. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this parameter group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of a tag to attach to the MemoryDB parameter group, used for identifying and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag assigned to the MemoryDB parameter group, used to organize, identify, and manage the resource in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the parameter group. Computed by AWS after `ship`.
</ResponseField>
