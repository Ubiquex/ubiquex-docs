---
title: "aws_memorydb_user"
description: "Real, generated bindings for `aws_memorydb_user`."
---

`aws_memorydb_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memorydb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/memorydb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(memorydb.User, "ci-runner", memorydb.UserConfig{
    			AccessString: "example",
    			UserName:     "example-user-name",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-aws/aws/memorydb/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        accessString: "example",
        userName: "example-user-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.memorydb import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            access_string="example",
            user_name="example-user-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_memorydb_user, with access string example, user name
    example-user-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="minimum_engine_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
