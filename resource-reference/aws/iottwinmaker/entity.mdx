---
title: "aws_io_ttwin_maker_entity"
description: "`aws_io_ttwin_maker_entity` is the real, digital representation of one physical object, a specific machine, a room..."
---

`aws_io_ttwin_maker_entity` is the real, digital representation of one physical object, a specific machine, a room, built from one or more `aws_io_ttwin_maker_component_type`s inside an `aws_io_ttwin_maker_workspace`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own entity"})

    		ubx.Resource(iottwinmaker.Entity, "example", iottwinmaker.EntityConfig{
    			EntityName:          "example-entity-name",
    			WorkspaceId:         "example",
    			Components:          "example",
    			CompositeComponents: "example",
    			Description:         "Managed by ubx.",
    			EntityId:            "example",
    			ParentEntityId:      "example",
    			Tags:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Entity } from "@ubx/sdk-aws/aws/iottwinmaker/entity";

    export default stack("example", () => {
      intent({ summary: "example own entity" });

      resource(Entity, "example", {
        entityName: "example-entity-name",
        workspaceId: "example",
        components: "example",
        compositeComponents: "example",
        description: "Managed by ubx.",
        entityId: "example",
        parentEntityId: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iottwinmaker import Entity, EntityConfig

    def describe():
        ubx.intent("example own entity")

        ubx.resource(Entity, "example", EntityConfig(
            entity_name="example-entity-name",
            workspace_id="example",
            components="example",
            composite_components="example",
            description="Managed by ubx.",
            entity_id="example",
            parent_entity_id="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_ttwin_maker_entity, with entity name
    example-entity-name, workspace id example, components example,
    composite components example, description Managed by ubx., entity
    id example, parent entity id example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="components" type="any">
  A map that sets information about a component type. Optional.
</ResponseField>

<ResponseField name="composite_components" type="any">
  A map that sets information about a composite component. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the entity. Optional.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The ID of the entity. Optional.
</ResponseField>

<ResponseField name="entity_name" type="string" required>
  The name of the entity. Required.
</ResponseField>

<ResponseField name="parent_entity_id" type="string">
  The ID of the parent entity. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The ID of the workspace. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the entity. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date_time" type="string">
  The date and time when the IoT TwinMaker entity was created, in ISO 8601 format. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="has_child_entities" type="bool">
  A Boolean value that specifies whether the entity has child entities or not. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="object">
  The current status of the entity, including its state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED) and any error details if an operation failed. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      Contains the error code and message returned by AWS IoT TwinMaker when the entity is in an ERROR state, providing details about why an entity operation such as creation or update failed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The machine-readable error code returned in the entity's status when AWS IoT TwinMaker fails to create or update the entity, providing a programmatic identifier for the failure type. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          The error message from AWS IoT TwinMaker explaining why the entity is in the failed or error state. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      The current lifecycle state of the IoT TwinMaker entity, such as CREATING, ACTIVE, UPDATING, DELETING, or ERROR, as reported by AWS IoT TwinMaker. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_date_time" type="string">
  The date and time when the entity was last updated, returned as a string in ISO 8601 format. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
