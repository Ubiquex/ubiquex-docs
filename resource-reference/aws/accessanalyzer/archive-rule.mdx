---
title: "aws_accessanalyzer_archive_rule"
description: "Real, generated bindings for `aws_accessanalyzer_archive_rule`."
---

`aws_accessanalyzer_archive_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	accessanalyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/accessanalyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own archive rule"})

    		ubx.Resource(accessanalyzer.ArchiveRule, "ci-runner", accessanalyzer.ArchiveRuleConfig{
    			AnalyzerName: "example-analyzer-name",
    			RuleName:     "example-rule-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ArchiveRule } from "jsr:@ubx/sdk-aws/aws/accessanalyzer/archive_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own archive rule" });

      resource(ArchiveRule, "ci-runner", {
        analyzerName: "example-analyzer-name",
        ruleName: "example-rule-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.accessanalyzer import ArchiveRule, ArchiveRuleConfig

    def describe():
        ubx.intent("payments own archive rule")

        ubx.resource(ArchiveRule, "ci-runner", ArchiveRuleConfig(
            analyzer_name="example-analyzer-name",
            rule_name="example-rule-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_accessanalyzer_archive_rule, with analyzer name
    example-analyzer-name, rule name example-rule-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="analyzer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
