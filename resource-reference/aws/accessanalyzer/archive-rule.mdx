---
title: "aws_accessanalyzer_archive_rule"
description: "Real, generated bindings for `aws_accessanalyzer_archive_rule`."
---

`aws_accessanalyzer_archive_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/accessanalyzer"

    cfg := accessanalyzer.ArchiveRuleConfig{
        AnalyzerName: "example-analyzer-name",
        RuleName: "example-rule-name",
    }
    ubx.Resource(accessanalyzer.ArchiveRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ArchiveRule } from
      "jsr:@ubx/sdk-aws/aws/accessanalyzer/archive_rule";

    resource(ArchiveRule, "example", {
      analyzerName: "example",
      ruleName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.accessanalyzer import (
        ArchiveRule,
        ArchiveRuleConfig,
    )

    cfg = ArchiveRuleConfig(
        analyzer_name="example",
        rule_name="example",
    )
    ubx.resource(ArchiveRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_accessanalyzer_archive_rule with analyzer_name,
rule_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="analyzer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
