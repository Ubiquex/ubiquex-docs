---
title: "aws_accessanalyzer_analyzer"
description: "Real, generated bindings for `aws_accessanalyzer_analyzer`."
---

`aws_accessanalyzer_analyzer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/accessanalyzer"

    cfg := accessanalyzer.AnalyzerConfig{
        AnalyzerName: "example-analyzer-name",
        Tags: map[string]string{"key": "value"},
        Type: "example",
    }
    ubx.Resource(accessanalyzer.Analyzer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Analyzer } from
      "jsr:@ubx/sdk-aws/aws/accessanalyzer/analyzer";

    resource(Analyzer, "example", {
      analyzerName: "example",
      tags: { key: "value" },
      type: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.accessanalyzer import Analyzer, AnalyzerConfig

    cfg = AnalyzerConfig(
        analyzer_name="example",
        tags={"key": "value"},
        type="example",
    )
    ubx.resource(Analyzer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_accessanalyzer_analyzer with analyzer_name, tags,
type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="analyzer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
