---
title: "aws_vpclattice_target_group_attachment"
description: "Real, generated bindings for `aws_vpclattice_target_group_attachment`."
---

`aws_vpclattice_target_group_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpclattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpclattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target group attachment"})

    		ubx.Resource(vpclattice.TargetGroupAttachment, "ci-runner", vpclattice.TargetGroupAttachmentConfig{
    			TargetGroupIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetGroupAttachment } from "jsr:@ubx/sdk-aws/aws/vpclattice/target_group_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own target group attachment" });

      resource(TargetGroupAttachment, "ci-runner", {
        targetGroupIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpclattice import TargetGroupAttachment, TargetGroupAttachmentConfig

    def describe():
        ubx.intent("payments own target group attachment")

        ubx.resource(TargetGroupAttachment, "ci-runner", TargetGroupAttachmentConfig(
            target_group_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpclattice_target_group_attachment, with target group
    identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_group_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
