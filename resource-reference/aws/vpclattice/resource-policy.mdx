---
title: "aws_vpc_lattice_resource_policy"
description: "`aws_vpc_lattice_resource_policy` attaches a real resource-based policy to an `aws_vpc_lattice_service_network`..."
---

`aws_vpc_lattice_resource_policy` attaches a real resource-based policy to an `aws_vpc_lattice_service_network`, controlling which principals or accounts can associate their own VPCs or services with it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpclattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/vpclattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resource policy"})

    		ubx.Resource(vpclattice.ResourcePolicy, "example", vpclattice.ResourcePolicyConfig{
    			Policy:      "example",
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/vpclattice/resource_policy";

    export default stack("example", () => {
      intent({ summary: "example own resource policy" });

      resource(ResourcePolicy, "example", {
        policy: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpclattice import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("example own resource policy")

        ubx.resource(ResourcePolicy, "example", ResourcePolicyConfig(
            policy="example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_lattice_resource_policy, with policy example,
    resource arn arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="policy" type="any" required>
  The JSON policy document that defines the permissions for the VPC Lattice resource, such as a service network or service, controlling which principals can access the resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The Amazon Resource Name (ARN) of the VPC Lattice resource (such as a service or service network) to which this resource policy is attached. Required. **(AI-inferred)**
</ResponseField>
