---
title: "aws_vpc_lattice_target_group"
description: "`aws_vpc_lattice_target_group` defines a real set of targets, EC2 instances, IP addresses, Lambda functions, or an ALB, and their health check..."
---

`aws_vpc_lattice_target_group` defines a real set of targets, EC2 instances, IP addresses, Lambda functions, or an ALB, and their health check, that an `aws_vpc_lattice_rule` forwards matching traffic to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpclattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/vpclattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own lattice target group"})

    		ubx.Resource(vpclattice.TargetGroup, "example", vpclattice.TargetGroupConfig{
    			Type:    "example",
    			Name:    "example",
    			Config:  map[string]any{"health_check": map[string]any{"enabled": true}},
    			Tags:    []map[string]any{{"key": "example"}},
    			Targets: []map[string]any{{"id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetGroup } from "@ubx/sdk-aws/aws/vpclattice/target_group";

    export default stack("example", () => {
      intent({ summary: "example own lattice target group" });

      resource(TargetGroup, "example", {
        type: "example",
        name: "example",
        config: { healthCheck: { enabled: true } },
        tags: [{ key: "example" }],
        targets: [{ id: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpclattice.target_group import TargetGroup, TargetGroupConfig

    def describe():
        ubx.intent("example own lattice target group")

        ubx.resource(TargetGroup, "example", TargetGroupConfig(
            type="example",
            name="example",
            config={"health_check": {"enabled": True}},
            tags=[{"key": "example"}],
            targets=[{"id": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_lattice_target_group called "example", with type
    example, config a set value, tags a set value, targets a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config" type="object">
  Config specifies the target group's configuration settings, such as the protocol, port, VPC identifier, and health check parameters. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="health_check" type="object">
      Specifies the health check configuration for the VPC Lattice target group, including protocol, port, path, matcher, interval, timeout, and threshold counts used to determine target health. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Indicates whether health checks are enabled for the target group; when set to false, the target group does not perform health-check probing on its registered targets. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="health_check_interval_seconds" type="number">
          The time in seconds between each health check of a target in the VPC Lattice target group, with a default of 30 and a valid range of 5 to 300. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="health_check_timeout_seconds" type="number">
          Specifies the number of seconds to wait for a target's health check response before considering it unhealthy. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="healthy_threshold_count" type="number">
          The number of consecutive successful health checks that must occur before a target is marked as healthy. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="matcher" type="object">
          Defines the expected HTTP response status codes (e.g., '200' or '200-299') that indicate a healthy target for the health check, structured as an object containing the 'httpCode' property. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="http_code" type="string" required>
              Specifies the HTTP status code or range (e.g., '200' or '200-299') that a target must return in response to a health check request for it to be considered healthy for this VPC Lattice target group. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="path" type="string">
          The destination path for HTTP/HTTPS health check requests, used to determine target health, such as '/health'. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port" type="number">
          The port number on the target that the health check requests are sent to, which overrides the target's traffic port if specified. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protocol" type="string">
          Specifies the protocol (HTTP, HTTPS, or TCP) used by VPC Lattice for health checks on targets in this target group, where TCP performs a connection check and HTTP/HTTPS send a request to the configured health check path. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protocol_version" type="string">
          Determines whether health check requests to targets use HTTP/1.1 or HTTP/2 when the health check protocol is HTTP or HTTPS. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="unhealthy_threshold_count" type="number">
          The number of consecutive failed health checks required to mark a target as unhealthy in the VPC Lattice target group's health check configuration. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ip_address_type" type="string">
      Determines the IP address version (IPv4 or IPv6) used for targets when the target type is IP, with IPv4 as the default if omitted. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="lambda_event_structure_version" type="string">
      Specifies the version of the event structure (V1 or V2) that VPC Lattice uses when invoking the Lambda target for this target group, controlling the format of the event payload delivered to the function. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port number on which the VPC Lattice target group's targets listen for traffic, specified within the target group's configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="protocol" type="string">
      Specifies the network protocol used by the target group to route traffic to its targets (e.g., HTTP, HTTPS, or TCP). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="protocol_version" type="string">
      The HTTP protocol version (HTTP1 or HTTP2) that the target group uses for routing requests to its targets. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vpc_identifier" type="string">
      The ID of the VPC in which the target group's targets are located, required for target groups of type IP or ALB but not for Lambda targets. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The name of the VPC Lattice target group, providing a friendly identifier for the resource. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Defines the tags to associate with the VPC Lattice target group, where each tag is an object with a Key and Value, used to manage, identify, and categorize the target group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag applied to the VPC Lattice target group, used for organizing or identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="targets" type="list(object)">
  Specifies the list of targets (by ID or IP address) to register with this target group, optionally with a port. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The ID of the target resource, such as an EC2 instance ID, an IP address, or a Lambda function ARN. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The network port on which the target receives traffic; if omitted, the target group's default port is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Determines whether the target group routes traffic to IP addresses ('IP'), Lambda functions ('LAMBDA'), or Application Load Balancers ('ALB'). Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the VPC Lattice target group, automatically assigned by AWS upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp (in ISO 8601 format) representing when the target group was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique VPC Lattice target group identifier assigned by AWS upon creation, a string with a 'tg-' prefix (e.g., 'tg-0a1b2c3d4e5f67890') used to reference the target group in VPC Lattice operations. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The timestamp indicating when the target group was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  Indicates the current lifecycle status of the target group, such as ACTIVE or DRAFT, reflecting whether the target group is ready for use. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
