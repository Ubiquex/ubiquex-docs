---
title: "aws_vpclattice_domain_verification"
description: "Real, generated bindings for `aws_vpclattice_domain_verification`."
---

`aws_vpclattice_domain_verification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpclattice"

    cfg := vpclattice.DomainVerificationConfig{
        DomainName: "example-domain-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpclattice.DomainVerification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainVerification } from
      "jsr:@ubx/sdk-aws/aws/vpclattice/domain_verification";

    resource(DomainVerification, "example", {
      domainName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpclattice import (
        DomainVerification,
        DomainVerificationConfig,
    )

    cfg = DomainVerificationConfig(
        domain_name="example",
        tags={"key": "value"},
    )
    ubx.resource(DomainVerification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpclattice_domain_verification with domain_name,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_verified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="txt_record_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="txt_record_value" type="string">
  Computed by AWS after `ship`.
</ResponseField>
