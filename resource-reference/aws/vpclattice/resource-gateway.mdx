---
title: "aws_vpclattice_resource_gateway"
description: "Real, generated bindings for `aws_vpclattice_resource_gateway`."
---

`aws_vpclattice_resource_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpclattice"

    cfg := vpclattice.ResourceGatewayConfig{
        Name: "example-name",
        SubnetIds: []string{"example"},
        VpcId: "example",
        IpAddressType: "example",
        Ipv4AddressesPerEni: 1,
    }
    ubx.Resource(vpclattice.ResourceGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceGateway } from
      "jsr:@ubx/sdk-aws/aws/vpclattice/resource_gateway";

    resource(ResourceGateway, "example", {
      name: "example",
      subnetIds: ["example"],
      vpcId: "example",
      ipAddressType: "example",
      ipv4AddressesPerEni: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpclattice import (
        ResourceGateway,
        ResourceGatewayConfig,
    )

    cfg = ResourceGatewayConfig(
        name="example",
        subnet_ids=["example"],
        vpc_id="example",
        ip_address_type="example",
        ipv4_addresses_per_eni=1,
    )
    ubx.resource(ResourceGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpclattice_resource_gateway with name, subnet_ids,
vpc_id, ip_address_type, ipv4_addresses_per_eni.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="ip_address_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv4_addresses_per_eni" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_config_dns_resolution" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv4_addresses_per_eni" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_config_dns_resolution" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
