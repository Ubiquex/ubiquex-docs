---
title: "aws_lambda_layer_version"
description: "Real, generated bindings for `aws_lambda_layer_version`."
---

`aws_lambda_layer_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own layer version"})

    		ubx.Resource(lambda.LayerVersion, "ci-runner", lambda.LayerVersionConfig{
    			LayerName:               "example-layer-name",
    			CompatibleArchitectures: []string{"example"},
    			CompatibleRuntimes:      []string{"example"},
    			Description:             "Managed by ubx.",
    			Filename:                "example",
    			LicenseInfo:             "example",
    			S3Bucket:                "example",
    			S3Key:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LayerVersion } from "jsr:@ubx/sdk-aws/aws/lambda/layer_version";

    export default stack("payments", () => {
      intent({ summary: "payments own layer version" });

      resource(LayerVersion, "ci-runner", {
        layerName: "example-layer-name",
        compatibleArchitectures: ["example"],
        compatibleRuntimes: ["example"],
        description: "Managed by ubx.",
        filename: "example",
        licenseInfo: "example",
        s3Bucket: "example",
        s3Key: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import LayerVersion, LayerVersionConfig

    def describe():
        ubx.intent("payments own layer version")

        ubx.resource(LayerVersion, "ci-runner", LayerVersionConfig(
            layer_name="example-layer-name",
            compatible_architectures=["example"],
            compatible_runtimes=["example"],
            description="Managed by ubx.",
            filename="example",
            license_info="example",
            s3_bucket="example",
            s3_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_layer_version, with layer name
    example-layer-name, compatible architectures a set value,
    compatible runtimes a set value, description Managed by ubx.,
    filename example, license info example, s3 bucket example, s3 key
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="compatible_architectures" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="compatible_runtimes" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="filename" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="layer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="license_info" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_object_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="source_code_hash" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="code_sha256" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="layer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="signing_job_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="signing_profile_version_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_code_hash" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_code_size" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
