---
title: "aws_lambda_runtime_management_config"
description: "Real, generated bindings for `aws_lambda_runtime_management_config`."
---

`aws_lambda_runtime_management_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"

    cfg := lambda.RuntimeManagementConfigConfig{
        FunctionName: "example-function-name",
        Qualifier: "example",
        RuntimeVersionArn: "example",
    }
    ubx.Resource(lambda.RuntimeManagementConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RuntimeManagementConfig } from
      "jsr:@ubx/sdk-aws/aws/lambda/runtime_management_config";

    resource(RuntimeManagementConfig, "example", {
      functionName: "example",
      qualifier: "example",
      runtimeVersionArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import (
        RuntimeManagementConfig,
        RuntimeManagementConfigConfig,
    )

    cfg = RuntimeManagementConfigConfig(
        function_name="example",
        qualifier="example",
        runtime_version_arn="example",
    )
    ubx.resource(RuntimeManagementConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_runtime_management_config with
function_name, qualifier, runtime_version_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="runtime_version_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="update_runtime_on" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
