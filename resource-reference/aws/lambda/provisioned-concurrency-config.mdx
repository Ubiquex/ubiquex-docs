---
title: "aws_lambda_provisioned_concurrency_config"
description: "Real, generated bindings for `aws_lambda_provisioned_concurrency_config`."
---

`aws_lambda_provisioned_concurrency_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provisioned concurrency config"})

    		ubx.Resource(lambda.ProvisionedConcurrencyConfig, "ci-runner", lambda.ProvisionedConcurrencyConfigConfig{
    			FunctionName:                    "example-function-name",
    			ProvisionedConcurrentExecutions: 1,
    			Qualifier:                       "example",
    			SkipDestroy:                     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProvisionedConcurrencyConfig } from "jsr:@ubx/sdk-aws/aws/lambda/provisioned_concurrency_config";

    export default stack("payments", () => {
      intent({ summary: "payments own provisioned concurrency config" });

      resource(ProvisionedConcurrencyConfig, "ci-runner", {
        functionName: "example-function-name",
        provisionedConcurrentExecutions: 1,
        qualifier: "example",
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import ProvisionedConcurrencyConfig, ProvisionedConcurrencyConfigConfig

    def describe():
        ubx.intent("payments own provisioned concurrency config")

        ubx.resource(ProvisionedConcurrencyConfig, "ci-runner", ProvisionedConcurrencyConfigConfig(
            function_name="example-function-name",
            provisioned_concurrent_executions=1,
            qualifier="example",
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_provisioned_concurrency_config, with function
    name example-function-name, provisioned concurrent executions 1,
    qualifier example, skip destroy true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provisioned_concurrent_executions" type="number" required>
  Required.
</ResponseField>

<ResponseField name="qualifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
