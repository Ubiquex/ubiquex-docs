---
title: "aws_lambda_provisioned_concurrency_config"
description: "Real, generated bindings for `aws_lambda_provisioned_concurrency_config`."
---

`aws_lambda_provisioned_concurrency_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"

    cfg := lambda.ProvisionedConcurrencyConfigConfig{
        FunctionName: "example-function-name",
        ProvisionedConcurrentExecutions: 1,
        Qualifier: "example",
        SkipDestroy: true,
    }
    ubx.Resource(lambda.ProvisionedConcurrencyConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProvisionedConcurrencyConfig } from
      "jsr:@ubx/sdk-aws/aws/lambda/provisioned_concurrency_config";

    resource(ProvisionedConcurrencyConfig, "example", {
      functionName: "example",
      provisionedConcurrentExecutions: 1,
      qualifier: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import (
        ProvisionedConcurrencyConfig,
        ProvisionedConcurrencyConfigConfig,
    )

    cfg = ProvisionedConcurrencyConfigConfig(
        function_name="example",
        provisioned_concurrent_executions=1,
        qualifier="example",
        skip_destroy=True,
    )
    ubx.resource(ProvisionedConcurrencyConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_provisioned_concurrency_config with
function_name, provisioned_concurrent_executions, qualifier,
skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provisioned_concurrent_executions" type="number" required>
  Required.
</ResponseField>

<ResponseField name="qualifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
