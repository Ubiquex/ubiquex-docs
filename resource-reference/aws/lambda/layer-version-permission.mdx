---
title: "aws_lambda_layer_version_permission"
description: "Real, generated bindings for `aws_lambda_layer_version_permission`."
---

`aws_lambda_layer_version_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"

    cfg := lambda.LayerVersionPermissionConfig{
        Action: "example",
        LayerName: "example-layer-name",
        Principal: "example",
        StatementId: "example",
        VersionNumber: 1,
        OrganizationId: "example",
        SkipDestroy: true,
    }
    ubx.Resource(lambda.LayerVersionPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LayerVersionPermission } from
      "jsr:@ubx/sdk-aws/aws/lambda/layer_version_permission";

    resource(LayerVersionPermission, "example", {
      action: "example",
      layerName: "example",
      principal: "example",
      statementId: "example",
      versionNumber: 1,
      organizationId: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import (
        LayerVersionPermission,
        LayerVersionPermissionConfig,
    )

    cfg = LayerVersionPermissionConfig(
        action="example",
        layer_name="example",
        principal="example",
        statement_id="example",
        version_number=1,
        organization_id="example",
        skip_destroy=True,
    )
    ubx.resource(LayerVersionPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_layer_version_permission with action,
layer_name, principal, statement_id, version_number,
organization_id, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="layer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="statement_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="version_number" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
