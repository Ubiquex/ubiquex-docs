---
title: "aws_lambda_layer_version_permission"
description: "`aws_lambda_layer_version_permission` grants a real other AWS account, or the whole account's organization..."
---

`aws_lambda_layer_version_permission` grants a real other AWS account, or the whole account's organization, permission to use a specific `aws_lambda_layer_version`, letting a layer be shared without making it public.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own layer version permission"})

    		ubx.Resource(lambda.LayerVersionPermission, "example", lambda.LayerVersionPermissionConfig{
    			Action:          "example",
    			LayerVersionArn: "arn:aws:iam::123456789012:policy/example",
    			Principal:       "example",
    			OrganizationId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LayerVersionPermission } from "@ubx/sdk-aws/aws/lambda/layer_version_permission";

    export default stack("example", () => {
      intent({ summary: "example own layer version permission" });

      resource(LayerVersionPermission, "example", {
        action: "example",
        layerVersionArn: "arn:aws:iam::123456789012:policy/example",
        principal: "example",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_.layer_version_permission import LayerVersionPermission, LayerVersionPermissionConfig

    def describe():
        ubx.intent("example own layer version permission")

        ubx.resource(LayerVersionPermission, "example", LayerVersionPermissionConfig(
            action="example",
            layer_version_arn="arn:aws:iam::123456789012:policy/example",
            principal="example",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_layer_version_permission, with action example,
    layer version arn arn:aws:iam::123456789012:policy/example,
    principal example, organization id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action" type="string" required>
  The API action that grants access to the layer. Required.
</ResponseField>

<ResponseField name="layer_version_arn" type="string" required>
  The name or Amazon Resource Name (ARN) of the layer. Required.
</ResponseField>

<ResponseField name="organization_id" type="string">
  With the principal set to *, grant permission to all accounts in the specified organization. Optional.
</ResponseField>

<ResponseField name="principal" type="string" required>
  An account ID, or * to grant layer usage permission to all accounts in an organization, or all AWS accounts (if organizationId is not specified). Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  ID generated by service Computed by AWS after `ship`.
</ResponseField>
