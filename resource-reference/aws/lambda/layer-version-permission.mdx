---
title: "aws_lambda_layer_version_permission"
description: "Real, generated bindings for `aws_lambda_layer_version_permission`."
---

`aws_lambda_layer_version_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own layer version permission"})

    		ubx.Resource(lambda.LayerVersionPermission, "ci-runner", lambda.LayerVersionPermissionConfig{
    			Action:         "example",
    			LayerName:      "example-layer-name",
    			Principal:      "example",
    			StatementId:    "example",
    			VersionNumber:  1,
    			OrganizationId: "example",
    			SkipDestroy:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LayerVersionPermission } from "jsr:@ubx/sdk-aws/aws/lambda/layer_version_permission";

    export default stack("payments", () => {
      intent({ summary: "payments own layer version permission" });

      resource(LayerVersionPermission, "ci-runner", {
        action: "example",
        layerName: "example-layer-name",
        principal: "example",
        statementId: "example",
        versionNumber: 1,
        organizationId: "example",
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import LayerVersionPermission, LayerVersionPermissionConfig

    def describe():
        ubx.intent("payments own layer version permission")

        ubx.resource(LayerVersionPermission, "ci-runner", LayerVersionPermissionConfig(
            action="example",
            layer_name="example-layer-name",
            principal="example",
            statement_id="example",
            version_number=1,
            organization_id="example",
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_layer_version_permission, with action example,
    layer name example-layer-name, principal example, statement id
    example, version number 1, organization id example, skip destroy
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="layer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="statement_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="version_number" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
