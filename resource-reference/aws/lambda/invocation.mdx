---
title: "aws_lambda_invocation"
description: "Real, generated bindings for `aws_lambda_invocation`."
---

`aws_lambda_invocation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"

    cfg := lambda.InvocationConfig{
        FunctionName: "example-function-name",
        Input: "example",
        LifecycleScope: "example",
        Qualifier: "example",
    }
    ubx.Resource(lambda.Invocation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Invocation } from
      "jsr:@ubx/sdk-aws/aws/lambda/invocation";

    resource(Invocation, "example", {
      functionName: "example",
      input: "example",
      lifecycleScope: "example",
      qualifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import Invocation, InvocationConfig

    cfg = InvocationConfig(
        function_name="example",
        input="example",
        lifecycle_scope="example",
        qualifier="example",
    )
    ubx.resource(Invocation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_invocation with function_name, input,
lifecycle_scope, qualifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="input" type="string" required>
  Required.
</ResponseField>

<ResponseField name="lifecycle_scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="terraform_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="result" type="string">
  Computed by AWS after `ship`.
</ResponseField>
