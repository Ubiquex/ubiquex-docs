---
title: "aws_lambda_permission"
description: "Real, generated bindings for `aws_lambda_permission`."
---

`aws_lambda_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own permission"})

    		ubx.Resource(lambda.Permission, "ci-runner", lambda.PermissionConfig{
    			Action:                "example",
    			FunctionName:          "example-function-name",
    			Principal:             "example",
    			EventSourceToken:      "example",
    			FunctionUrlAuthType:   "example",
    			InvokedViaFunctionUrl: true,
    			PrincipalOrgId:        "example",
    			Qualifier:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Permission } from "jsr:@ubx/sdk-aws/aws/lambda/permission";

    export default stack("payments", () => {
      intent({ summary: "payments own permission" });

      resource(Permission, "ci-runner", {
        action: "example",
        functionName: "example-function-name",
        principal: "example",
        eventSourceToken: "example",
        functionUrlAuthType: "example",
        invokedViaFunctionUrl: true,
        principalOrgId: "example",
        qualifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import Permission, PermissionConfig

    def describe():
        ubx.intent("payments own permission")

        ubx.resource(Permission, "ci-runner", PermissionConfig(
            action="example",
            function_name="example-function-name",
            principal="example",
            event_source_token="example",
            function_url_auth_type="example",
            invoked_via_function_url=True,
            principal_org_id="example",
            qualifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_permission, with action example, function name
    example-function-name, principal example, event source token
    example, function url auth type example, invoked via function url
    true, principal org id example, qualifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="event_source_token" type="string">
  Optional.
</ResponseField>

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="function_url_auth_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invoked_via_function_url" type="bool">
  Optional.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_org_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_account" type="string">
  Optional.
</ResponseField>

<ResponseField name="source_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
