---
title: "aws_lambda_function_event_invoke_config"
description: "Real, generated bindings for `aws_lambda_function_event_invoke_config`."
---

`aws_lambda_function_event_invoke_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function event invoke config"})

    		ubx.Resource(lambda.FunctionEventInvokeConfig, "ci-runner", lambda.FunctionEventInvokeConfigConfig{
    			FunctionName:             "example-function-name",
    			MaximumEventAgeInSeconds: 7200,
    			MaximumRetryAttempts:     1,
    			Qualifier:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FunctionEventInvokeConfig } from "jsr:@ubx/sdk-aws/aws/lambda/function_event_invoke_config";

    export default stack("payments", () => {
      intent({ summary: "payments own function event invoke config" });

      resource(FunctionEventInvokeConfig, "ci-runner", {
        functionName: "example-function-name",
        maximumEventAgeInSeconds: 7200,
        maximumRetryAttempts: 1,
        qualifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import FunctionEventInvokeConfig, FunctionEventInvokeConfigConfig

    def describe():
        ubx.intent("payments own function event invoke config")

        ubx.resource(FunctionEventInvokeConfig, "ci-runner", FunctionEventInvokeConfigConfig(
            function_name="example-function-name",
            maximum_event_age_in_seconds=7200,
            maximum_retry_attempts=1,
            qualifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_function_event_invoke_config, with function
    name example-function-name, maximum event age in seconds 7200,
    maximum retry attempts 1, qualifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maximum_event_age_in_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="maximum_retry_attempts" type="number">
  Optional.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
