---
title: "aws_lambda_function_recursion_config"
description: "Real, generated bindings for `aws_lambda_function_recursion_config`."
---

`aws_lambda_function_recursion_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lambda"

    cfg := lambda.FunctionRecursionConfigConfig{
        FunctionName: "example-function-name",
        RecursiveLoop: "example",
    }
    ubx.Resource(lambda.FunctionRecursionConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FunctionRecursionConfig } from
      "jsr:@ubx/sdk-aws/aws/lambda/function_recursion_config";

    resource(FunctionRecursionConfig, "example", {
      functionName: "example",
      recursiveLoop: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import (
        FunctionRecursionConfig,
        FunctionRecursionConfigConfig,
    )

    cfg = FunctionRecursionConfigConfig(
        function_name="example",
        recursive_loop="example",
    )
    ubx.resource(FunctionRecursionConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_function_recursion_config with
function_name, recursive_loop.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recursive_loop" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
