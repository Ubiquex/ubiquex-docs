---
title: "aws_lambda_alias"
description: "Real, generated bindings for `aws_lambda_alias`."
---

`aws_lambda_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lambda"

    cfg := lambda.AliasConfig{
        FunctionName: "example-function-name",
        FunctionVersion: "example",
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(lambda.Alias, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Alias } from "jsr:@ubx/sdk-aws/aws/lambda/alias";

    resource(Alias, "example", {
      functionName: "example",
      functionVersion: "example",
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import Alias, AliasConfig

    cfg = AliasConfig(
        function_name="example",
        function_version="example",
        name="example",
        description="example",
    )
    ubx.resource(Alias, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_alias with function_name, function_version,
name, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="function_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invoke_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
