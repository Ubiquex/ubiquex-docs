---
title: "aws_lambda_function_scaling_config"
description: "Real, generated bindings for `aws_lambda_function_scaling_config`."
---

`aws_lambda_function_scaling_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function scaling config"})

    		ubx.Resource(lambda.FunctionScalingConfig, "ci-runner", lambda.FunctionScalingConfigConfig{
    			FunctionName: "example-function-name",
    			Qualifier:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FunctionScalingConfig } from "jsr:@ubx/sdk-aws/aws/lambda/function_scaling_config";

    export default stack("payments", () => {
      intent({ summary: "payments own function scaling config" });

      resource(FunctionScalingConfig, "ci-runner", {
        functionName: "example-function-name",
        qualifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import FunctionScalingConfig, FunctionScalingConfigConfig

    def describe():
        ubx.intent("payments own function scaling config")

        ubx.resource(FunctionScalingConfig, "ci-runner", FunctionScalingConfigConfig(
            function_name="example-function-name",
            qualifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_function_scaling_config, with function name
    example-function-name, qualifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="qualifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
