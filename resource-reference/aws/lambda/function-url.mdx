---
title: "aws_lambda_function_url"
description: "Real, generated bindings for `aws_lambda_function_url`."
---

`aws_lambda_function_url` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lambda"

    cfg := lambda.FunctionUrlConfig{
        AuthorizationType: "example",
        FunctionName: "example-function-name",
        InvokeMode: "example",
        Qualifier: "example",
    }
    ubx.Resource(lambda.FunctionUrl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FunctionUrl } from
      "jsr:@ubx/sdk-aws/aws/lambda/function_url";

    resource(FunctionUrl, "example", {
      authorizationType: "example",
      functionName: "example",
      invokeMode: "example",
      qualifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lambda import FunctionUrl, FunctionUrlConfig

    cfg = FunctionUrlConfig(
        authorization_type="example",
        function_name="example",
        invoke_mode="example",
        qualifier="example",
    )
    ubx.resource(FunctionUrl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lambda_function_url with authorization_type,
function_name, invoke_mode, qualifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorization_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invoke_mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="function_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
