---
title: "aws_neptune_cluster_snapshot"
description: "Real, generated bindings for `aws_neptune_cluster_snapshot`."
---

`aws_neptune_cluster_snapshot` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/neptune"

    cfg := neptune.ClusterSnapshotConfig{
        DbClusterIdentifier: "example",
        DbClusterSnapshotIdentifier: "example",
    }
    ubx.Resource(neptune.ClusterSnapshot, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterSnapshot } from
      "jsr:@ubx/sdk-aws/aws/neptune/cluster_snapshot";

    resource(ClusterSnapshot, "example", {
      dbClusterIdentifier: "example",
      dbClusterSnapshotIdentifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.neptune import (
        ClusterSnapshot,
        ClusterSnapshotConfig,
    )

    cfg = ClusterSnapshotConfig(
        db_cluster_identifier="example",
        db_cluster_snapshot_identifier="example",
    )
    ubx.resource(ClusterSnapshot, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_neptune_cluster_snapshot with
db_cluster_identifier, db_cluster_snapshot_identifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="db_cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="db_cluster_snapshot_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="allocated_storage" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zones" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_cluster_snapshot_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="license_model" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="port" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_db_cluster_snapshot_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_encrypted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
