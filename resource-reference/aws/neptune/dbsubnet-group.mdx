---
title: "aws_neptune_dbsubnet_group"
description: "`aws_neptune_dbsubnet_group` defines a real set of VPC subnets that an `aws_neptune_dbcluster`'s instances are allowed to be placed into."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/neptune"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dbsubnet group"})

    		ubx.Resource(neptune.DbsubnetGroup, "example", neptune.DbsubnetGroupConfig{
    			DbsubnetGroupDescription: "example",
    			SubnetIds:                []string{"example"},
    			DbsubnetGroupName:        "example-dbsubnet-group-name",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbsubnetGroup } from "@ubx/sdk-aws/aws/neptune/dbsubnet_group";

    export default stack("example", () => {
      intent({ summary: "example own dbsubnet group" });

      resource(DbsubnetGroup, "example", {
        dbsubnetGroupDescription: "example",
        subnetIds: ["example"],
        dbsubnetGroupName: "example-dbsubnet-group-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.neptune.dbsubnet_group import DbsubnetGroup, DbsubnetGroupConfig

    def describe():
        ubx.intent("example own dbsubnet group")

        ubx.resource(DbsubnetGroup, "example", DbsubnetGroupConfig(
            dbsubnet_group_description="example",
            subnet_ids=["example"],
            dbsubnet_group_name="example-dbsubnet-group-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_neptune_dbsubnet_group, with dbsubnet group
    description example, subnet ids a set value, dbsubnet group name
    example-dbsubnet-group-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="dbsubnet_group_description" type="string" required>
  The description for the DB subnet group. Required.
</ResponseField>

<ResponseField name="dbsubnet_group_name" type="string">
  The name for the DB subnet group. This value is stored as a lowercase string. Constraints: Must contain no more than 255 lowercase alphanumeric characters or hyphens. Must not be "Default". Example: mysubnetgroup Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The Amazon EC2 subnet IDs for the DB subnet group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An optional array of key-value pairs to apply to this DB subnet group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to associate with the Neptune DB subnet group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag associated with the Neptune DB subnet group, used to categorize and manage the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
