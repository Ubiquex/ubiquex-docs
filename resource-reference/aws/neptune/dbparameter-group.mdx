---
title: "aws_neptune_dbparameter_group"
description: "`aws_neptune_dbparameter_group` defines a real set of instance-level engine parameters that can be applied to an `aws_neptune_dbinstance` instead of..."
---

`aws_neptune_dbparameter_group` defines a real set of instance-level engine parameters that can be applied to an `aws_neptune_dbinstance` instead of hand-tuning the instance individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/neptune"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dbparameter group"})

    		ubx.Resource(neptune.DbparameterGroup, "ci-runner", neptune.DbparameterGroupConfig{
    			Description: "Managed by ubx.",
    			Family:      "example",
    			Parameters:  "example",
    			Name:        "ci-runner",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbparameterGroup } from "./local-sdk/aws/sdk/typescript/aws/neptune/dbparameter_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dbparameter group" });

      resource(DbparameterGroup, "ci-runner", {
        description: "Managed by ubx.",
        family: "example",
        parameters: "example",
        name: "ci-runner",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.neptune import DbparameterGroup, DbparameterGroupConfig

    def describe():
        ubx.intent("payments own dbparameter group")

        ubx.resource(DbparameterGroup, "ci-runner", DbparameterGroupConfig(
            description="Managed by ubx.",
            family="example",
            parameters="example",
            name="ci-runner",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_neptune_dbparameter_group called "ci-runner", with
    description Managed by ubx., family example, parameters example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string" required>
  Provides the customer-specified description for this DB parameter group. Required.
</ResponseField>

<ResponseField name="family" type="string" required>
  Must be `neptune1` for engine versions prior to 1.2.0.0, or `neptune1.2` for engine version `1.2.0.0` and higher. Required.
</ResponseField>

<ResponseField name="name" type="string">
  Provides the name of the DB parameter group. Optional.
</ResponseField>

<ResponseField name="parameters" type="any" required>
  The parameters to set for this DB parameter group. The parameters are expressed as a JSON object consisting of key-value pairs. Changes to dynamic parameters are applied immediately. During an update, if you have static parameters (whether they were changed or not), it triggers AWS CloudFormation to reboot the associated DB instance without failover. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An optional array of key-value pairs to apply to this DB parameter group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The customer-defined key portion of a tag attached to the Neptune DB parameter group, used to label and organize the resource according to your own metadata scheme. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a key-value tag that can be attached to the AWS Neptune DB parameter group, used for identifying and categorizing the resource for cost allocation and operational management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
