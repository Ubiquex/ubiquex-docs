---
title: "aws_api_gateway_base_path_mapping_v2"
description: "`aws_api_gateway_base_path_mapping_v2` maps a real base path on an `aws_api_gateway_v2_domain_name` to a specific API and stage..."
---

`aws_api_gateway_base_path_mapping_v2` maps a real base path on an `aws_api_gateway_v2_domain_name` to a specific API and stage, the HTTP/WebSocket API counterpart to the REST API's own `aws_api_gateway_base_path_mapping`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway base path mapping v2"})

    		ubx.Resource(apigateway.BasePathMappingV2, "example", apigateway.BasePathMappingV2Config{
    			DomainNameArn: "arn:aws:iam::123456789012:policy/example",
    			RestApiId:     "example",
    			BasePath:      "example",
    			Stage:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BasePathMappingV2 } from "@ubx/sdk-aws/aws/apigateway/base_path_mapping_v2";

    export default stack("example", () => {
      intent({ summary: "example own gateway base path mapping v2" });

      resource(BasePathMappingV2, "example", {
        domainNameArn: "arn:aws:iam::123456789012:policy/example",
        restApiId: "example",
        basePath: "example",
        stage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigateway.base_path_mapping_v2 import BasePathMappingV2, BasePathMappingV2Config

    def describe():
        ubx.intent("example own gateway base path mapping v2")

        ubx.resource(BasePathMappingV2, "example", BasePathMappingV2Config(
            domain_name_arn="arn:aws:iam::123456789012:policy/example",
            rest_api_id="example",
            base_path="example",
            stage="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_base_path_mapping_v2, with domain name arn
    arn:aws:iam::123456789012:policy/example, rest api id example,
    base path example, stage example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="base_path" type="string">
  The base path name that callers of the API must provide in the URL after the domain name. Optional.
</ResponseField>

<ResponseField name="domain_name_arn" type="string" required>
  The Arn of an AWS::ApiGateway::DomainNameV2 resource. Required.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The ID of the API. Required.
</ResponseField>

<ResponseField name="stage" type="string">
  The name of the API's stage. Optional.
</ResponseField>

## Output properties

<ResponseField name="base_path_mapping_arn" type="string">
  Amazon Resource Name (ARN) of the resource. Computed by AWS after `ship`.
</ResponseField>
