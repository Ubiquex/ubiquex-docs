---
title: "aws_api_gateway_request_validator"
description: "`aws_api_gateway_request_validator` attaches to an `aws_api_gateway_method` and rejects malformed requests..."
---

`aws_api_gateway_request_validator` attaches to an `aws_api_gateway_method` and rejects malformed requests, missing required parameters or a body that doesn't match its `aws_api_gateway_model`, before they ever reach the backend integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own request validator"})

    		ubx.Resource(apigateway.RequestValidator, "example", apigateway.RequestValidatorConfig{
    			RestApiId:                 "example",
    			Name:                      "example",
    			ValidateRequestBody:       true,
    			ValidateRequestParameters: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RequestValidator } from "@ubx/sdk-aws/aws/apigateway/request_validator";

    export default stack("example", () => {
      intent({ summary: "example own request validator" });

      resource(RequestValidator, "example", {
        restApiId: "example",
        name: "example",
        validateRequestBody: true,
        validateRequestParameters: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigateway import RequestValidator, RequestValidatorConfig

    def describe():
        ubx.intent("example own request validator")

        ubx.resource(RequestValidator, "example", RequestValidatorConfig(
            rest_api_id="example",
            name="example",
            validate_request_body=True,
            validate_request_parameters=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_request_validator called "example", with
    rest api id example, validate request body true, validate request
    parameters true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string">
  Specifies the name of the API Gateway request validator for identification; if omitted, API Gateway generates a name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The ID of the REST API to which this request validator belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="validate_request_body" type="bool">
  Indicates whether the request validator checks the request body against the configured model schema for the API method. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="validate_request_parameters" type="bool">
  Indicates whether this request validator validates the query string and header parameters of incoming API requests. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="request_validator_id" type="string">
  The unique identifier assigned by API Gateway to the request validator, returned after creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
