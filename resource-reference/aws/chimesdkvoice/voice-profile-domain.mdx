---
title: "aws_chimesdkvoice_voice_profile_domain"
description: "Real, generated bindings for `aws_chimesdkvoice_voice_profile_domain`."
---

`aws_chimesdkvoice_voice_profile_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/chimesdkvoice"

    cfg := chimesdkvoice.VoiceProfileDomainConfig{
        Name: "example-name",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(chimesdkvoice.VoiceProfileDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VoiceProfileDomain } from
      "jsr:@ubx/sdk-aws/aws/chimesdkvoice/voice_profile_domain";

    resource(VoiceProfileDomain, "example", {
      name: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.chimesdkvoice import (
        VoiceProfileDomain,
        VoiceProfileDomainConfig,
    )

    cfg = VoiceProfileDomainConfig(
        name="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(VoiceProfileDomain, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_chimesdkvoice_voice_profile_domain with name,
description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
