---
title: "aws_open_search_serverless_lifecycle_policy"
description: "`aws_open_search_serverless_lifecycle_policy` defines a real retention rule for an `aws_open_search_serverless_collection`'s time-series indexes..."
---

`aws_open_search_serverless_lifecycle_policy` defines a real retention rule for an `aws_open_search_serverless_collection`'s time-series indexes, automatically deleting indexes older than a configured age rather than requiring manual cleanup.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	opensearchserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/opensearchserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own lifecycle policy"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(opensearchserverless.LifecyclePolicy, "example", opensearchserverless.LifecyclePolicyConfig{
    			Name:        "example",
    			Policy:      string(accessPolicy),
    			Type:        "example",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LifecyclePolicy } from "@ubx/sdk-aws/aws/opensearchserverless/lifecycle_policy";

    export default stack("example", () => {
      intent({ summary: "example own lifecycle policy" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(LifecyclePolicy, "example", {
        name: "example",
        policy: accessPolicy,
        type: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.aws.opensearchserverless import LifecyclePolicy, LifecyclePolicyConfig

    def describe():
        ubx.intent("example own lifecycle policy")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(LifecyclePolicy, "example", LifecyclePolicyConfig(
            name="example",
            policy=access_policy,
            type="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_open_search_serverless_lifecycle_policy called
    "example", with policy a real, valid value, type example,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  The description of the policy Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the policy Required.
</ResponseField>

<ResponseField name="policy" type="string" required>
  The JSON policy document that is the content for the policy Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of lifecycle policy Required.
</ResponseField>
