---
title: "aws_opensearchserverless_vpc_endpoint"
description: "Real, generated bindings for `aws_opensearchserverless_vpc_endpoint`."
---

`aws_opensearchserverless_vpc_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearchserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearchserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc endpoint"})

    		ubx.Resource(opensearchserverless.VpcEndpoint, "ci-runner", opensearchserverless.VpcEndpointConfig{
    			Name:      "ci-runner",
    			SubnetIds: []string{"example"},
    			VpcId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcEndpoint } from "jsr:@ubx/sdk-aws/aws/opensearchserverless/vpc_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc endpoint" });

      resource(VpcEndpoint, "ci-runner", {
        name: "ci-runner",
        subnetIds: ["example"],
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearchserverless import VpcEndpoint, VpcEndpointConfig

    def describe():
        ubx.intent("payments own vpc endpoint")

        ubx.resource(VpcEndpoint, "ci-runner", VpcEndpointConfig(
            name="ci-runner",
            subnet_ids=["example"],
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearchserverless_vpc_endpoint called "ci-runner",
    with subnet ids a set value, vpc id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
