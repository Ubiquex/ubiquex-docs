---
title: "aws_opensearchserverless_vpc_endpoint"
description: "Real, generated bindings for `aws_opensearchserverless_vpc_endpoint`."
---

`aws_opensearchserverless_vpc_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearchserverless"

    cfg := opensearchserverless.VpcEndpointConfig{
        Name: "example-name",
        SubnetIds: []string{"example"},
        VpcId: "example",
        SecurityGroupIds: []string{"example"},
    }
    ubx.Resource(opensearchserverless.VpcEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcEndpoint } from
      "jsr:@ubx/sdk-aws/aws/opensearchserverless/vpc_endpoint";

    resource(VpcEndpoint, "example", {
      name: "example",
      subnetIds: ["example"],
      vpcId: "example",
      securityGroupIds: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearchserverless import (
        VpcEndpoint,
        VpcEndpointConfig,
    )

    cfg = VpcEndpointConfig(
        name="example",
        subnet_ids=["example"],
        vpc_id="example",
        security_group_ids=["example"],
    )
    ubx.resource(VpcEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearchserverless_vpc_endpoint with name,
subnet_ids, vpc_id, security_group_ids.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
