---
title: "aws_open_search_serverless_collection_group"
description: "`aws_open_search_serverless_collection_group` creates a real group of `aws_open_search_serverless_collection`s that share OpenSearch Compute Units at..."
---

`aws_open_search_serverless_collection_group` creates a real group of `aws_open_search_serverless_collection`s that share OpenSearch Compute Units at the group level, even across collections encrypted with different KMS keys, reducing cost versus provisioning capacity per collection. Collection groups are a recent addition to OpenSearch Serverless's next-generation architecture, introduced in February 2026.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearchserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/opensearchserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own search serverless collection group"})

    		ubx.Resource(opensearchserverless.CollectionGroup, "example", opensearchserverless.CollectionGroupConfig{
    			Name:            "example",
    			StandbyReplicas: "example",
    			CapacityLimits:  map[string]any{"max_indexing_capacity_in_ocu": 1},
    			Description:     "Managed by ubx.",
    			Generation:      "example",
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CollectionGroup } from "@ubx/sdk-aws/aws/opensearchserverless/collection_group";

    export default stack("example", () => {
      intent({ summary: "example own search serverless collection group" });

      resource(CollectionGroup, "example", {
        name: "example",
        standbyReplicas: "example",
        capacityLimits: { maxIndexingCapacityInOcu: 1 },
        description: "Managed by ubx.",
        generation: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearchserverless.collection_group import CollectionGroup, CollectionGroupConfig

    def describe():
        ubx.intent("example own search serverless collection group")

        ubx.resource(CollectionGroup, "example", CollectionGroupConfig(
            name="example",
            standby_replicas="example",
            capacity_limits={"max_indexing_capacity_in_ocu": 1},
            description="Managed by ubx.",
            generation="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_open_search_serverless_collection_group called
    "example", with standby replicas example, capacity limits a set
    value, description Managed by ubx., generation example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity_limits" type="object">
  Defines the maximum indexing and search capacity in OpenSearch Compute Units (OCUs) that the collection group is allowed to use, effectively limiting the total capacity available to all collections within the group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_indexing_capacity_in_ocu" type="number">
      The maximum indexing capacity for collections in the group. Optional.
    </ResponseField>
    <ResponseField name="max_search_capacity_in_ocu" type="number">
      The maximum search capacity for collections in the group. Optional.
    </ResponseField>
    <ResponseField name="min_indexing_capacity_in_ocu" type="number">
      The minimum indexing capacity for collections in the group. Optional.
    </ResponseField>
    <ResponseField name="min_search_capacity_in_ocu" type="number">
      The minimum search capacity for collections in the group. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the collection group. Optional.
</ResponseField>

<ResponseField name="generation" type="string">
  The generation of Amazon OpenSearch Serverless for the collection group. Valid values are CLASSIC and NEXTGEN. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the collection group. Required.
</ResponseField>

<ResponseField name="standby_replicas" type="string" required>
  Indicates whether standby replicas are used for the collection group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the collection group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the collection group. Computed by AWS after `ship`.
</ResponseField>
