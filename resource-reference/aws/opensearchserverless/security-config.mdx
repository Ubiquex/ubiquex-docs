---
title: "aws_opensearchserverless_security_config"
description: "Real, generated bindings for `aws_opensearchserverless_security_config`."
---

`aws_opensearchserverless_security_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearchserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearchserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security config"})

    		ubx.Resource(opensearchserverless.SecurityConfig, "ci-runner", opensearchserverless.SecurityConfigConfig{
    			Name:        "ci-runner",
    			Type:        "example",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityConfig } from "jsr:@ubx/sdk-aws/aws/opensearchserverless/security_config";

    export default stack("payments", () => {
      intent({ summary: "payments own security config" });

      resource(SecurityConfig, "ci-runner", {
        name: "ci-runner",
        type: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearchserverless import SecurityConfig, SecurityConfigConfig

    def describe():
        ubx.intent("payments own security config")

        ubx.resource(SecurityConfig, "ci-runner", SecurityConfigConfig(
            name="ci-runner",
            type="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearchserverless_security_config called
    "ci-runner", with type example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the security configuration. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the policy. Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="saml_options" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="group_attribute" type="string">
    </ResponseField>
    <ResponseField name="metadata" type="string" required>
    </ResponseField>
    <ResponseField name="session_timeout" type="number">
    </ResponseField>
    <ResponseField name="user_attribute" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Type of configuration. Must be `saml`. Required.
</ResponseField>

## Output properties

<ResponseField name="config_version" type="string">
  Version of the configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="saml_options" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="group_attribute" type="string">
    </ResponseField>
    <ResponseField name="metadata" type="string" required>
    </ResponseField>
    <ResponseField name="session_timeout" type="number">
    </ResponseField>
    <ResponseField name="user_attribute" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
