---
title: "aws_opensearchserverless_security_config"
description: "Real, generated bindings for `aws_opensearchserverless_security_config`."
---

`aws_opensearchserverless_security_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/opensearchserverless"

    cfg := opensearchserverless.SecurityConfigConfig{
        Name: "example-name",
        Type: "example",
        Description: "example",
    }
    ubx.Resource(opensearchserverless.SecurityConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityConfig } from
      "jsr:@ubx/sdk-aws/aws/opensearchserverless/security_config";

    resource(SecurityConfig, "example", {
      name: "example",
      type: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearchserverless import (
        SecurityConfig,
        SecurityConfigConfig,
    )

    cfg = SecurityConfigConfig(
        name="example",
        type="example",
        description="example",
    )
    ubx.resource(SecurityConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearchserverless_security_config with name,
type, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="config_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
