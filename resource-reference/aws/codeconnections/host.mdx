---
title: "aws_codeconnections_host"
description: "Real, generated bindings for `aws_codeconnections_host`."
---

`aws_codeconnections_host` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host"})

    		ubx.Resource(codeconnections.Host, "ci-runner", codeconnections.HostConfig{
    			Name:             "ci-runner",
    			ProviderEndpoint: "example",
    			ProviderType:     "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Host } from "jsr:@ubx/sdk-aws/aws/codeconnections/host";

    export default stack("payments", () => {
      intent({ summary: "payments own host" });

      resource(Host, "ci-runner", {
        name: "ci-runner",
        providerEndpoint: "example",
        providerType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codeconnections import Host, HostConfig

    def describe():
        ubx.intent("payments own host")

        ubx.resource(Host, "ci-runner", HostConfig(
            name="ci-runner",
            provider_endpoint="example",
            provider_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codeconnections_host called "ci-runner", with provider
    endpoint example, provider type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
