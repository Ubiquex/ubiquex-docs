---
title: "aws_codeconnections_host"
description: "Real, generated bindings for `aws_codeconnections_host`."
---

`aws_codeconnections_host` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codeconnections"

    cfg := codeconnections.HostConfig{
        Name: "example-name",
        ProviderEndpoint: "example",
        ProviderType: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(codeconnections.Host, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Host } from "jsr:@ubx/sdk-aws/aws/codeconnections/host";

    resource(Host, "example", {
      name: "example",
      providerEndpoint: "example",
      providerType: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codeconnections import Host, HostConfig

    cfg = HostConfig(
        name="example",
        provider_endpoint="example",
        provider_type="example",
        tags={"key": "value"},
    )
    ubx.resource(Host, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codeconnections_host with name, provider_endpoint,
provider_type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
