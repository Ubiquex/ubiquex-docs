---
title: "aws_codeconnections_connection"
description: "Real, generated bindings for `aws_codeconnections_connection`."
---

`aws_codeconnections_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codeconnections"

    cfg := codeconnections.ConnectionConfig{
        Name: "example-name",
        HostArn: "example",
        ProviderType: "example",
    }
    ubx.Resource(codeconnections.Connection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Connection } from
      "jsr:@ubx/sdk-aws/aws/codeconnections/connection";

    resource(Connection, "example", {
      name: "example",
      hostArn: "example",
      providerType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codeconnections import (
        Connection,
        ConnectionConfig,
    )

    cfg = ConnectionConfig(
        name="example",
        host_arn="example",
        provider_type="example",
    )
    ubx.resource(Connection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codeconnections_connection with name, host_arn,
provider_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="host_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provider_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
