---
title: "aws_qbusiness_data_source"
description: "`aws_qbusiness_data_source` configures a real connector, SharePoint, S3, a web crawler, and many more, that feeds content into an `aws_qbusiness_index`."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data source"})

    		ubx.Resource(qbusiness.DataSource, "example", qbusiness.DataSourceConfig{
    			ApplicationId:                   "example",
    			Configuration:                   "example",
    			DisplayName:                     "example-display-name",
    			IndexId:                         "example",
    			Description:                     "Managed by ubx.",
    			DocumentEnrichmentConfiguration: map[string]any{"inline_configurations": []map[string]any{{"condition": map[string]any{"key": "example"}}}},
    			MediaExtractionConfiguration:    map[string]any{"audio_extraction_configuration": map[string]any{"audio_extraction_status": "example"}},
    			RoleArn:                         "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataSource } from "./local-sdk/aws/sdk/typescript/aws/qbusiness/data_source.ts";

    export default stack("example", () => {
      intent({ summary: "example own data source" });

      resource(DataSource, "example", {
        applicationId: "example",
        configuration: "example",
        displayName: "example-display-name",
        indexId: "example",
        description: "Managed by ubx.",
        documentEnrichmentConfiguration: {
          inlineConfigurations: [{ condition: { key: "example" } }],
        },
        mediaExtractionConfiguration: {
          audioExtractionConfiguration: { audioExtractionStatus: "example" },
        },
        roleArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.qbusiness import DataSource, DataSourceConfig

    def describe():
        ubx.intent("example own data source")

        ubx.resource(DataSource, "example", DataSourceConfig(
            application_id="example",
            configuration="example",
            display_name="example-display-name",
            index_id="example",
            description="Managed by ubx.",
            document_enrichment_configuration={"inline_configurations": [{"condition": {"key": "example"}}]},
            media_extraction_configuration={"audio_extraction_configuration": {"audio_extraction_status": "example"}},
            role_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_qbusiness_data_source, with application id example,
    configuration example, display name example-display-name, index
    id example, description Managed by ubx., document enrichment
    configuration a set value, media extraction configuration a set
    value, role arn arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  The unique identifier of the Amazon Q Business application to which this data source belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="configuration" type="any" required>
  The connector-specific configuration for the data source, provided as a JSON object whose keys and values vary by the DataSourceType (for example, S3, Salesforce, or SharePoint). Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  Provides a user-defined description that helps identify the purpose or content of the data source. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The human-readable name of the data source, used to identify it in the Amazon Q Business console and APIs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="document_enrichment_configuration" type="object">
  This object configures the document enrichment process for the data source, defining pre-extraction and post-extraction transformations such as inline configuration rules or custom Lambda-based hook functions. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="inline_configurations" type="list(object)">
      Specifies a list of inline document enrichment configurations that conditionally modify document attributes (e.g., setting, deleting, or altering attribute values) during data ingestion. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="condition" type="object">
          Defines the document attribute condition (key, operator, and value) that a document must meet for the associated enrichment action to be applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The name of the document attribute that this condition evaluates, such as a metadata field or content property, to determine whether the enrichment action applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string">
              Determines how the document attribute key's value is compared against the condition's value, with allowed values such as EQUALS, NOT_EQUALS, CONTAINS, NOT_CONTAINS, EXISTS, or NOT_EXISTS. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              Defines the attribute value (such as a string, string list, number, or date) that the document attribute condition compares against, determining whether the inline document enrichment action is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="date_value" type="string">
                  Provides the date used as the comparison value in the document enrichment condition when the document attribute's value type is DATE, allowing the rule to match documents based on that specific date. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="long_value" type="number">
                  Defines the numeric (long) value to compare against a document attribute in an inline enrichment condition, determining whether the enrichment action applies to documents in the AWS QBusiness data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_list_value" type="list(string)">
                  This field provides the list of string values used as the condition value in a QBusiness data source document enrichment inline configuration, allowing multiple attribute values to be matched against when evaluating the condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  The string value used as the comparison target for a document attribute condition in an inline document enrichment configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="document_content_operator" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="target" type="object">
          Specifies the document attribute that the inline enrichment operation modifies, including the attribute name, the action (such as ADD, DELETE, or REPLACE), and the value to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="attribute_value_operator" type="string">
              Specifies whether to overwrite the existing value of the target document attribute with the supplied value or delete the attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key" type="string">
              The key (name) of the document attribute that the inline document enrichment configuration will update or add for the target in the QBusiness data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              Specifies the value to assign to the target document attribute in an inline document enrichment configuration, expressed as a DocumentAttributeValue object (which can hold a string, string list, long, or date). Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="date_value" type="string">
                  Specifies the date value, in ISO 8601 format, to assign to the document attribute when the inline enrichment target is applied during data source synchronization. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="long_value" type="number">
                  Sets the document attribute to the specified long integer value when the inline enrichment condition is met. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_list_value" type="list(string)">
                  Provides the list of string values to assign to the document attribute (target key) in this inline document enrichment rule, used when the rule's condition is satisfied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  Specifies the string value to assign to the target document attribute in an inline document enrichment action for the AWS Q Business data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="post_extraction_hook_configuration" type="object">
      Defines the Lambda function invoked after document attributes are extracted, enabling custom post-processing and enrichment of the document before indexing. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="invocation_condition" type="object">
          Specifies the document attribute condition (key, operator, value) that must be satisfied for the post-extraction hook to be invoked during document enrichment. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string" required>
              For the invocation condition of a post-extraction hook, this is the document attribute key (field name) whose value the condition evaluates (e.g., a custom metadata field) to decide whether the hook runs. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS, CONTAINS) used to evaluate the document attribute condition that determines whether the post-extraction hook is invoked. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              The document attribute value that is compared against the condition key using the operator to determine whether the post-extraction hook is invoked. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="date_value" type="string">
                  Specifies the date value (as a string in ISO 8601 format) that the document attribute must match, per the condition operator, to trigger the post-extraction hook for the data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="long_value" type="number">
                  The long integer value used as the document attribute value in the invocation condition that determines whether the post-extraction hook is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_list_value" type="list(string)">
                  Specifies the list of string values to compare against the document attribute value when evaluating the invocation condition for the post-extraction hook in the data source's document enrichment configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  The string value used in the attribute condition that determines whether the post-extraction hook is invoked on a document, compared against the document attribute's value using the specified operator. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lambda_arn" type="string">
          The ARN of the Lambda function invoked after document extraction to perform custom post-extraction enrichment on the Q Business data source. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the IAM role that AWS Q Business assumes to invoke the post-extraction Lambda function during document enrichment for this data source. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_bucket_name" type="string">
          The name of the S3 bucket where AWS QBusiness stores the extracted documents that are passed to the post-extraction hook's Lambda function for enrichment. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pre_extraction_hook_configuration" type="object">
      Specifies the AWS Lambda function configuration that is invoked before document extraction to perform pre-extraction enrichment on documents from the data source. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="invocation_condition" type="object">
          Specifies the condition on a document attribute that determines whether the pre-extraction hook is invoked for a document. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string" required>
              The key (document attribute name) that the invocation condition uses as the attribute to evaluate (with the specified operator and value) to determine whether the pre-extraction hook should be invoked. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operator" type="string" required>
              This required string specifies the comparison operator (e.g., EQUALS or CONTAINS) that defines how the invocation condition's value is evaluated against the document attribute, determining whether the pre-extraction hook is triggered. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              Provides the attribute value to match in the invocation condition for the pre-extraction hook, using one of several supported value formats (string, string list, long, or date). Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="date_value" type="string">
                  The date value used as the attribute value in the hook invocation condition, matched against document attributes to determine whether the pre-extraction hook runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="long_value" type="number">
                  Specifies the numeric (long) value that a document attribute must match to satisfy the invocation condition that triggers the pre-extraction hook in the data source's document enrichment configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_list_value" type="list(string)">
                  Defines the list of string values that the document attribute's value is compared against in the invocation condition for the pre-extraction hook. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  The string value to match in the document attribute condition that triggers the pre-extraction hook during document enrichment. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lambda_arn" type="string">
          The ARN of the AWS Lambda function invoked by the pre-extraction hook to process documents before they are indexed by the Amazon Q Business data source. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of an IAM role that AWS Q Business assumes to invoke the Lambda function for the pre-extraction document enrichment hook. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_bucket_name" type="string">
          The name of the Amazon S3 bucket that contains the Lambda function code for the pre-extraction hook, which is invoked before document extraction. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The unique identifier of the Amazon Q Business index to which this data source is associated. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="media_extraction_configuration" type="object">
  Specifies the configuration for extracting text from media files (such as images and audio) in source documents, including whether extraction is enabled for each media type. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="audio_extraction_configuration" type="object">
      Configures whether audio content from media files in the data source is extracted and indexed, typically by enabling or disabling audio transcription for searchable text. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="audio_extraction_status" type="string" required>
          Specifies whether audio extraction from media files in the Amazon Q Business data source is enabled (ENABLED) or disabled (DISABLED). Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image_extraction_configuration" type="object">
      Configures image extraction for the data source by specifying whether image extraction is enabled or disabled, allowing text and metadata to be extracted from images. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="image_extraction_status" type="string" required>
          Indicates whether extraction of text from images (such as scanned documents or photos) in the data source is enabled or disabled. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="video_extraction_configuration" type="object">
      Configures whether AWS Q Business extracts text from video files (e.g., using speech-to-text or optical character recognition) in the data source, based on its enabled or disabled extraction status. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="video_extraction_status" type="string" required>
          Indicates whether video extraction is enabled (ENABLED) or disabled (DISABLED) for the QBusiness data source, controlling whether text is extracted from video files. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string">
  The IAM role ARN that AWS Q Business assumes to access the underlying data source and retrieve documents for indexing. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="sync_schedule" type="string">
  Specifies the cron expression that defines how often the data source automatically syncs with its source repository. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tags (key-value pairs) to attach to the QBusiness data source, used for resource identification, organization, and management. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a key-value tag assigned to the AWS QBusiness data source, used for resource organization and metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_configuration" type="object">
  Specifies the VPC configuration, including security group IDs and subnet IDs, used by the data source to connect to resources within an Amazon VPC. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="security_group_ids" type="list(string)" required>
      The list of security group IDs to associate with the VPC configuration for the AWS QBusiness data source. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)" required>
      Identifies the subnets in the VPC where the Amazon Q Business data source connector runs to access private resources within your network. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp indicating when the data source was created in AWS QBusiness. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="data_source_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Q Business data source, assigned by AWS upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="data_source_id" type="string">
  The unique identifier assigned by AWS QBusiness to this data source. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the data source, indicating whether it is active, being created, updated, deleted, or has failed. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string">
  The connector type (e.g., S3, Salesforce, SharePoint) reported by the service for this Amazon Q Business data source. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp (in ISO 8601 format) of the most recent update to the data source, returned by the service. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
