---
title: "aws_qbusiness_index"
description: "`aws_qbusiness_index` creates a real search index within an `aws_qbusiness_application` that one or more `aws_qbusiness_data_source`s populate and..."
---

`aws_qbusiness_index` creates a real search index within an `aws_qbusiness_application` that one or more `aws_qbusiness_data_source`s populate and that an `aws_qbusiness_retriever` reads from to ground the application's generative answers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own index"})

    		ubx.Resource(qbusiness.Index, "example", qbusiness.IndexConfig{
    			ApplicationId:                   "example",
    			DisplayName:                     "example-display-name",
    			CapacityConfiguration:           map[string]any{"units": 1},
    			Description:                     "Managed by ubx.",
    			DocumentAttributeConfigurations: []map[string]any{{"name": "example-name"}},
    			Tags:                            []map[string]any{{"key": "example"}},
    			Type:                            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Index } from "@ubx/sdk-aws/aws/qbusiness/index";

    export default stack("example", () => {
      intent({ summary: "example own index" });

      resource(Index, "example", {
        applicationId: "example",
        displayName: "example-display-name",
        capacityConfiguration: { units: 1 },
        description: "Managed by ubx.",
        documentAttributeConfigurations: [{ name: "example-name" }],
        tags: [{ key: "example" }],
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.qbusiness import Index, IndexConfig
    from ubx.aws.qbusiness.index import Index_CapacityConfiguration, Index_DocumentAttributeConfigurations, Index_Tags

    def describe():
        ubx.intent("example own index")

        ubx.resource(Index, "example", IndexConfig(
            application_id="example",
            display_name="example-display-name",
            capacity_configuration=Index_CapacityConfiguration(units=1),
            description="Managed by ubx.",
            document_attribute_configurations=[Index_DocumentAttributeConfigurations(name="example-name")],
            tags=[Index_Tags(key="example")],
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_qbusiness_index, with application id example, display
    name example-display-name, capacity configuration a set value,
    description Managed by ubx., document attribute configurations a
    set value, tags a set value, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  The unique identifier of the AWS QBusiness application that this index belongs to, required when creating the index. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="capacity_configuration" type="object">
  Specifies the capacity configuration for the index, selecting either the ENTERPRISE or STARTER index type and the number of capacity units to allocate. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="units" type="number">
      The number of capacity units to allocate to the Amazon Q Business index, where each unit adds a fixed amount of storage and query capacity. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description for the Amazon Q Business index. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name of the Amazon Q Business index, which is a required, user-visible name that identifies the index in the Q Business console and API operations. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="document_attribute_configurations" type="list(object)">
  Defines which document metadata attributes the index will index and how they can be used for querying and filtering, by specifying each attribute's name, type, and usage configuration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The unique name of the document attribute that this configuration applies to, used to match metadata fields in ingested documents and reference the attribute in data source schemas. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="search" type="string">
      Determines whether the document attribute can be used in search queries, with allowed values ENABLED or DISABLED. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the data type of the document attribute in the index configuration, such as STRING, NUMBER, or DATE, which determines how the attribute is indexed and searched. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tag objects to attach to the Amazon Q Business index, used for organizing, managing, and identifying the resource in AWS. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag pair attached to the Amazon Q Business index, enabling resource categorization and access management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Specifies whether the Amazon Q Business index is an ENTERPRISE index for general enterprise search or an ANALYTICS index for analytical and data-intensive workloads. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The ISO 8601 timestamp indicating when this Amazon Q Business index was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="index_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Q Business index, assigned by AWS when the index is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="index_id" type="string">
  The unique AWS-assigned identifier for the QBusiness index. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="index_statistics" type="object">
  This computed attribute provides usage statistics for the QBusiness index, such as the total number of indexed documents and the combined byte size of the indexed text. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="text_document_statistics" type="object">
      Contains the total number of text documents and the number of indexed text documents for the AWS QBusiness index. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="indexed_text_bytes" type="number">
          The total size in bytes of text content that has been indexed for the index. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="indexed_text_document_count" type="number">
          The total number of text documents that have been indexed in the Amazon Q Business index. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle status of the Amazon Q Business index, such as CREATING during provisioning or ACTIVE when ready to use. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp indicating when the QBusiness index was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
