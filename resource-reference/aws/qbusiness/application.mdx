---
title: "aws_qbusiness_application"
description: "Real, generated bindings for `aws_qbusiness_application`."
---

`aws_qbusiness_application` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application"})

    		ubx.Resource(qbusiness.Application, "ci-runner", qbusiness.ApplicationConfig{
    			DisplayName:               "example-display-name",
    			IamServiceRoleArn:         "arn:aws:iam::123456789012:policy/ci",
    			IdentityCenterInstanceArn: "arn:aws:iam::123456789012:policy/ci",
    			Description:               "Managed by ubx.",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Application } from "jsr:@ubx/sdk-aws/aws/qbusiness/application";

    export default stack("payments", () => {
      intent({ summary: "payments own application" });

      resource(Application, "ci-runner", {
        displayName: "example-display-name",
        iamServiceRoleArn: "arn:aws:iam::123456789012:policy/ci",
        identityCenterInstanceArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.qbusiness import Application, ApplicationConfig

    def describe():
        ubx.intent("payments own application")

        ubx.resource(Application, "ci-runner", ApplicationConfig(
            display_name="example-display-name",
            iam_service_role_arn="arn:aws:iam::123456789012:policy/ci",
            identity_center_instance_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_qbusiness_application, with display name
    example-display-name, iam service role arn
    arn:aws:iam::123456789012:policy/ci, identity center instance arn
    arn:aws:iam::123456789012:policy/ci, description Managed by ubx.,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_service_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="identity_center_instance_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identity_center_application_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
