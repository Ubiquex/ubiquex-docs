---
title: "aws_sagemaker_space"
description: "Real, generated bindings for `aws_sagemaker_space`."
---

`aws_sagemaker_space` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own space"})

    		ubx.Resource(sagemaker.Space, "ci-runner", sagemaker.SpaceConfig{
    			DomainId:         "example",
    			SpaceName:        "example-space-name",
    			SpaceDisplayName: "example-space-display-name",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Space } from "jsr:@ubx/sdk-aws/aws/sagemaker/space";

    export default stack("payments", () => {
      intent({ summary: "payments own space" });

      resource(Space, "ci-runner", {
        domainId: "example",
        spaceName: "example-space-name",
        spaceDisplayName: "example-space-display-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import Space, SpaceConfig

    def describe():
        ubx.intent("payments own space")

        ubx.resource(Space, "ci-runner", SpaceConfig(
            domain_id="example",
            space_name="example-space-name",
            space_display_name="example-space-display-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_space, with domain id example, space name
    example-space-name, space display name
    example-space-display-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="space_display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="space_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_efs_file_system_uid" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
