---
title: "aws_sagemaker_model_package_group_policy"
description: "Real, generated bindings for `aws_sagemaker_model_package_group_policy`."
---

`aws_sagemaker_model_package_group_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own model package group policy"})

    		ubx.Resource(sagemaker.ModelPackageGroupPolicy, "ci-runner", sagemaker.ModelPackageGroupPolicyConfig{
    			ModelPackageGroupName: "example-model-package-group-name",
    			ResourcePolicy:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ModelPackageGroupPolicy } from "jsr:@ubx/sdk-aws/aws/sagemaker/model_package_group_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own model package group policy" });

      resource(ModelPackageGroupPolicy, "ci-runner", {
        modelPackageGroupName: "example-model-package-group-name",
        resourcePolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import ModelPackageGroupPolicy, ModelPackageGroupPolicyConfig

    def describe():
        ubx.intent("payments own model package group policy")

        ubx.resource(ModelPackageGroupPolicy, "ci-runner", ModelPackageGroupPolicyConfig(
            model_package_group_name="example-model-package-group-name",
            resource_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_model_package_group_policy, with model
    package group name example-model-package-group-name, resource
    policy example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="model_package_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_policy" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
