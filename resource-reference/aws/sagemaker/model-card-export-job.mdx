---
title: "aws_sagemaker_model_card_export_job"
description: "Real, generated bindings for `aws_sagemaker_model_card_export_job`."
---

`aws_sagemaker_model_card_export_job` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.ModelCardExportJobConfig{
        ModelCardExportJobName: "example-model-card-export-job-name",
        ModelCardName: "example-model-card-name",
        ModelCardVersion: 1,
    }
    ubx.Resource(sagemaker.ModelCardExportJob, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ModelCardExportJob } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/model_card_export_job";

    resource(ModelCardExportJob, "example", {
      modelCardExportJobName: "example",
      modelCardName: "example",
      modelCardVersion: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import (
        ModelCardExportJob,
        ModelCardExportJobConfig,
    )

    cfg = ModelCardExportJobConfig(
        model_card_export_job_name="example",
        model_card_name="example",
        model_card_version=1,
    )
    ubx.resource(ModelCardExportJob, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_model_card_export_job with
model_card_export_job_name, model_card_name, model_card_version.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="model_card_export_job_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_card_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_card_version" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="export_artifacts" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="s3_export_artifacts" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_card_export_job_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_card_version" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
