---
title: "aws_sagemaker_data_quality_job_definition"
description: "Real, generated bindings for `aws_sagemaker_data_quality_job_definition`."
---

`aws_sagemaker_data_quality_job_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data quality job definition"})

    		ubx.Resource(sagemaker.DataQualityJobDefinition, "ci-runner", sagemaker.DataQualityJobDefinitionConfig{
    			RoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:    "ci-runner",
    			Tags:    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataQualityJobDefinition } from "jsr:@ubx/sdk-aws/aws/sagemaker/data_quality_job_definition";

    export default stack("payments", () => {
      intent({ summary: "payments own data quality job definition" });

      resource(DataQualityJobDefinition, "ci-runner", {
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import DataQualityJobDefinition, DataQualityJobDefinitionConfig

    def describe():
        ubx.intent("payments own data quality job definition")

        ubx.resource(DataQualityJobDefinition, "ci-runner", DataQualityJobDefinitionConfig(
            role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_data_quality_job_definition called
    "ci-runner", with role arn arn:aws:iam::123456789012:policy/ci,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
