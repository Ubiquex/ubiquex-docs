---
title: "aws_sagemaker_app_image_config"
description: "Real, generated bindings for `aws_sagemaker_app_image_config`."
---

`aws_sagemaker_app_image_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.AppImageConfigConfig{
        AppImageConfigName: "example-app-image-config-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(sagemaker.AppImageConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AppImageConfig } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/app_image_config";

    resource(AppImageConfig, "example", {
      appImageConfigName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import (
        AppImageConfig,
        AppImageConfigConfig,
    )

    cfg = AppImageConfigConfig(
        app_image_config_name="example",
        tags={"key": "value"},
    )
    ubx.resource(AppImageConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_app_image_config with
app_image_config_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_image_config_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
