---
title: "aws_sagemaker_model_card"
description: "Real, generated bindings for `aws_sagemaker_model_card`."
---

`aws_sagemaker_model_card` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/sagemaker"

    cfg := sagemaker.ModelCardConfig{
        Content: "example",
        ModelCardName: "example-model-card-name",
        ModelCardStatus: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(sagemaker.ModelCard, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ModelCard } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/model_card";

    resource(ModelCard, "example", {
      content: "example",
      modelCardName: "example",
      modelCardStatus: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import ModelCard, ModelCardConfig

    cfg = ModelCardConfig(
        content="example",
        model_card_name="example",
        model_card_status="example",
        tags={"key": "value"},
    )
    ubx.resource(ModelCard, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_model_card with content, model_card_name,
model_card_status, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_card_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_card_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="model_card_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
