---
title: "aws_sagemaker_model_package_group"
description: "Real, generated bindings for `aws_sagemaker_model_package_group`."
---

`aws_sagemaker_model_package_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own model package group"})

    		ubx.Resource(sagemaker.ModelPackageGroup, "ci-runner", sagemaker.ModelPackageGroupConfig{
    			ModelPackageGroupName:        "example-model-package-group-name",
    			ModelPackageGroupDescription: "example",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ModelPackageGroup } from "jsr:@ubx/sdk-aws/aws/sagemaker/model_package_group";

    export default stack("payments", () => {
      intent({ summary: "payments own model package group" });

      resource(ModelPackageGroup, "ci-runner", {
        modelPackageGroupName: "example-model-package-group-name",
        modelPackageGroupDescription: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import ModelPackageGroup, ModelPackageGroupConfig

    def describe():
        ubx.intent("payments own model package group")

        ubx.resource(ModelPackageGroup, "ci-runner", ModelPackageGroupConfig(
            model_package_group_name="example-model-package-group-name",
            model_package_group_description="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_model_package_group, with model package
    group name example-model-package-group-name, model package group
    description example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="model_package_group_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="model_package_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
