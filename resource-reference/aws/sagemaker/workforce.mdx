---
title: "aws_sagemaker_workforce"
description: "Real, generated bindings for `aws_sagemaker_workforce`."
---

`aws_sagemaker_workforce` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/sagemaker"

    cfg := sagemaker.WorkforceConfig{
        WorkforceName: "example-workforce-name",
    }
    ubx.Resource(sagemaker.Workforce, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Workforce } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/workforce";

    resource(Workforce, "example", {
      workforceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import Workforce, WorkforceConfig

    cfg = WorkforceConfig(
        workforce_name="example",
    )
    ubx.resource(Workforce, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_workforce with workforce_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workforce_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subdomain" type="string">
  Computed by AWS after `ship`.
</ResponseField>
