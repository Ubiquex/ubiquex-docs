---
title: "aws_sagemaker_hub"
description: "Real, generated bindings for `aws_sagemaker_hub`."
---

`aws_sagemaker_hub` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.HubConfig{
        HubDescription: "example",
        HubName: "example-hub-name",
        HubDisplayName: "example-hub-display-name",
        HubSearchKeywords: []string{"example"},
    }
    ubx.Resource(sagemaker.Hub, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Hub } from "jsr:@ubx/sdk-aws/aws/sagemaker/hub";

    resource(Hub, "example", {
      hubDescription: "example",
      hubName: "example",
      hubDisplayName: "example",
      hubSearchKeywords: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import Hub, HubConfig

    cfg = HubConfig(
        hub_description="example",
        hub_name="example",
        hub_display_name="example",
        hub_search_keywords=["example"],
    )
    ubx.resource(Hub, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_hub with hub_description, hub_name,
hub_display_name, hub_search_keywords.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hub_description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hub_display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hub_search_keywords" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
