---
title: "aws_sagemaker_notebook_instance_lifecycle_configuration"
description: "Real, generated bindings for `aws_sagemaker_notebook_instance_lifecycle_configuration`."
---

`aws_sagemaker_notebook_instance_lifecycle_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own notebook instance lifecycle configuration"})

    		ubx.Resource(sagemaker.NotebookInstanceLifecycleConfiguration, "ci-runner", sagemaker.NotebookInstanceLifecycleConfigurationConfig{
    			Name:     "ci-runner",
    			OnCreate: "example",
    			OnStart:  "example",
    			Tags:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NotebookInstanceLifecycleConfiguration } from "jsr:@ubx/sdk-aws/aws/sagemaker/notebook_instance_lifecycle_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own notebook instance lifecycle configuration" });

      resource(NotebookInstanceLifecycleConfiguration, "ci-runner", {
        name: "ci-runner",
        onCreate: "example",
        onStart: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import NotebookInstanceLifecycleConfiguration, NotebookInstanceLifecycleConfigurationConfig

    def describe():
        ubx.intent("payments own notebook instance lifecycle configuration")

        ubx.resource(NotebookInstanceLifecycleConfiguration, "ci-runner", NotebookInstanceLifecycleConfigurationConfig(
            name="ci-runner",
            on_create="example",
            on_start="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_notebook_instance_lifecycle_configuration
    called "ci-runner", with on create example, on start example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="on_create" type="string">
  Optional.
</ResponseField>

<ResponseField name="on_start" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
