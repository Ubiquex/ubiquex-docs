---
title: "aws_sagemaker_hub_content_reference"
description: "Real, generated bindings for `aws_sagemaker_hub_content_reference`."
---

`aws_sagemaker_hub_content_reference` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub content reference"})

    		ubx.Resource(sagemaker.HubContentReference, "ci-runner", sagemaker.HubContentReferenceConfig{
    			HubContentName:               "example-hub-content-name",
    			HubName:                      "example-hub-name",
    			SagemakerPublicHubContentArn: "arn:aws:iam::123456789012:policy/ci",
    			MinVersion:                   "example",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubContentReference } from "jsr:@ubx/sdk-aws/aws/sagemaker/hub_content_reference";

    export default stack("payments", () => {
      intent({ summary: "payments own hub content reference" });

      resource(HubContentReference, "ci-runner", {
        hubContentName: "example-hub-content-name",
        hubName: "example-hub-name",
        sagemakerPublicHubContentArn: "arn:aws:iam::123456789012:policy/ci",
        minVersion: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import HubContentReference, HubContentReferenceConfig

    def describe():
        ubx.intent("payments own hub content reference")

        ubx.resource(HubContentReference, "ci-runner", HubContentReferenceConfig(
            hub_content_name="example-hub-content-name",
            hub_name="example-hub-name",
            sagemaker_public_hub_content_arn="arn:aws:iam::123456789012:policy/ci",
            min_version="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_hub_content_reference, with hub content name
    example-hub-content-name, hub name example-hub-name, sagemaker
    public hub content arn arn:aws:iam::123456789012:policy/ci, min
    version example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="hub_content_name" type="string" required>
  Name of the hub content reference. Required.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  Name of the private SageMaker Hub to add the content reference to. Required.
</ResponseField>

<ResponseField name="min_version" type="string">
  Minimum version of the hub content to reference. Use "1.0.0" to support all versions. Changing this value to an empty string forces replacement of the resource. Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sagemaker_public_hub_content_arn" type="string" required>
  ARN of the public SageMaker JumpStart hub content to reference. The ARN must not include a version suffix. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="hub_arn" type="string">
  ARN of the private SageMaker Hub that contains the content reference. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hub_content_arn" type="string">
  ARN of the hub content reference (without version suffix). The min_version is stripped off from the end of this ARN to make it usable to list tags. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hub_content_status" type="string">
  Status of the hub content reference. Valid values include `Available`, `Importing`, `Deleting`, `ImportFailed`, `DeleteFailed`. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hub_content_version" type="string">
  Version of the hub content reference. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
