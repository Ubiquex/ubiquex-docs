---
title: "aws_sagemaker_domain"
description: "Real, generated bindings for `aws_sagemaker_domain`."
---

`aws_sagemaker_domain` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain"})

    		ubx.Resource(sagemaker.Domain, "ci-runner", sagemaker.DomainConfig{
    			AuthMode:                   "example",
    			DomainName:                 "example-domain-name",
    			SubnetIds:                  []string{"example"},
    			VpcId:                      "example",
    			AppNetworkAccessType:       "example",
    			AppSecurityGroupManagement: "example",
    			KmsKeyId:                   "example",
    			TagPropagation:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Domain } from "jsr:@ubx/sdk-aws/aws/sagemaker/domain";

    export default stack("payments", () => {
      intent({ summary: "payments own domain" });

      resource(Domain, "ci-runner", {
        authMode: "example",
        domainName: "example-domain-name",
        subnetIds: ["example"],
        vpcId: "example",
        appNetworkAccessType: "example",
        appSecurityGroupManagement: "example",
        kmsKeyId: "example",
        tagPropagation: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import Domain, DomainConfig

    def describe():
        ubx.intent("payments own domain")

        ubx.resource(Domain, "ci-runner", DomainConfig(
            auth_mode="example",
            domain_name="example-domain-name",
            subnet_ids=["example"],
            vpc_id="example",
            app_network_access_type="example",
            app_security_group_management="example",
            kms_key_id="example",
            tag_propagation="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_domain, with auth mode example, domain name
    example-domain-name, subnet ids a set value, vpc id example, app
    network access type example, app security group management
    example, kms key id example, tag propagation example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_network_access_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="app_security_group_management" type="string">
  Optional.
</ResponseField>

<ResponseField name="auth_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tag_propagation" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_efs_file_system_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id_for_domain_boundary" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="single_sign_on_application_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="single_sign_on_managed_application_instance_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
