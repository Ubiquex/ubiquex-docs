---
title: "aws_sagemaker_device"
description: "Real, generated bindings for `aws_sagemaker_device`."
---

`aws_sagemaker_device` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.DeviceConfig{
        DeviceFleetName: "example-device-fleet-name",
    }
    ubx.Resource(sagemaker.Device, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Device } from "jsr:@ubx/sdk-aws/aws/sagemaker/device";

    resource(Device, "example", {
      deviceFleetName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import Device, DeviceConfig

    cfg = DeviceConfig(
        device_fleet_name="example",
    )
    ubx.resource(Device, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_device with device_fleet_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="device_fleet_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="agent_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
