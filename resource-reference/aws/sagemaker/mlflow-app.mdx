---
title: "aws_sagemaker_mlflow_app"
description: "Real, generated bindings for `aws_sagemaker_mlflow_app`."
---

`aws_sagemaker_mlflow_app` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mlflow app"})

    		ubx.Resource(sagemaker.MlflowApp, "ci-runner", sagemaker.MlflowAppConfig{
    			ArtifactStoreUri:     "example",
    			Name:                 "ci-runner",
    			RoleArn:              "arn:aws:iam::123456789012:policy/ci",
    			AccountDefaultStatus: "example",
    			DefaultDomainIdList:  []string{"example"},
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MlflowApp } from "jsr:@ubx/sdk-aws/aws/sagemaker/mlflow_app";

    export default stack("payments", () => {
      intent({ summary: "payments own mlflow app" });

      resource(MlflowApp, "ci-runner", {
        artifactStoreUri: "example",
        name: "ci-runner",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        accountDefaultStatus: "example",
        defaultDomainIdList: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import MlflowApp, MlflowAppConfig

    def describe():
        ubx.intent("payments own mlflow app")

        ubx.resource(MlflowApp, "ci-runner", MlflowAppConfig(
            artifact_store_uri="example",
            name="ci-runner",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            account_default_status="example",
            default_domain_id_list=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_mlflow_app called "ci-runner", with artifact
    store uri example, role arn arn:aws:iam::123456789012:policy/ci,
    account default status example, default domain id list a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_default_status" type="string">
  Optional.
</ResponseField>

<ResponseField name="artifact_store_uri" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_domain_id_list" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="model_registration_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="weekly_maintenance_window_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_registration_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="weekly_maintenance_window_start" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
