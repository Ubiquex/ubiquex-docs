---
title: "aws_sagemaker_image"
description: "Real, generated bindings for `aws_sagemaker_image`."
---

`aws_sagemaker_image` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own image"})

    		ubx.Resource(sagemaker.Image, "ci-runner", sagemaker.ImageConfig{
    			ImageName:   "example-image-name",
    			RoleArn:     "arn:aws:iam::123456789012:policy/ci",
    			Description: "Managed by ubx.",
    			DisplayName: "example-display-name",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Image } from "jsr:@ubx/sdk-aws/aws/sagemaker/image";

    export default stack("payments", () => {
      intent({ summary: "payments own image" });

      resource(Image, "ci-runner", {
        imageName: "example-image-name",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import Image, ImageConfig

    def describe():
        ubx.intent("payments own image")

        ubx.resource(Image, "ci-runner", ImageConfig(
            image_name="example-image-name",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            display_name="example-display-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_image, with image name example-image-name,
    role arn arn:aws:iam::123456789012:policy/ci, description Managed
    by ubx., display name example-display-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
