---
title: "aws_sagemaker_servicecatalog_portfolio_status"
description: "Real, generated bindings for `aws_sagemaker_servicecatalog_portfolio_status`."
---

`aws_sagemaker_servicecatalog_portfolio_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own servicecatalog portfolio status"})

    		ubx.Resource(sagemaker.ServicecatalogPortfolioStatus, "ci-runner", sagemaker.ServicecatalogPortfolioStatusConfig{
    			Status: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicecatalogPortfolioStatus } from "jsr:@ubx/sdk-aws/aws/sagemaker/servicecatalog_portfolio_status";

    export default stack("payments", () => {
      intent({ summary: "payments own servicecatalog portfolio status" });

      resource(ServicecatalogPortfolioStatus, "ci-runner", {
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import ServicecatalogPortfolioStatus, ServicecatalogPortfolioStatusConfig

    def describe():
        ubx.intent("payments own servicecatalog portfolio status")

        ubx.resource(ServicecatalogPortfolioStatus, "ci-runner", ServicecatalogPortfolioStatusConfig(
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_servicecatalog_portfolio_status, with status
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
