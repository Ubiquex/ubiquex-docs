---
title: "aws_sagemaker_code_repository"
description: "Real, generated bindings for `aws_sagemaker_code_repository`."
---

`aws_sagemaker_code_repository` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own code repository"})

    		ubx.Resource(sagemaker.CodeRepository, "ci-runner", sagemaker.CodeRepositoryConfig{
    			CodeRepositoryName: "example-code-repository-name",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CodeRepository } from "jsr:@ubx/sdk-aws/aws/sagemaker/code_repository";

    export default stack("payments", () => {
      intent({ summary: "payments own code repository" });

      resource(CodeRepository, "ci-runner", {
        codeRepositoryName: "example-code-repository-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import CodeRepository, CodeRepositoryConfig

    def describe():
        ubx.intent("payments own code repository")

        ubx.resource(CodeRepository, "ci-runner", CodeRepositoryConfig(
            code_repository_name="example-code-repository-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_code_repository, with code repository name
    example-code-repository-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="code_repository_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
