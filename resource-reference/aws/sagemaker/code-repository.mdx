---
title: "aws_sagemaker_code_repository"
description: "Real, generated bindings for `aws_sagemaker_code_repository`."
---

`aws_sagemaker_code_repository` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.CodeRepositoryConfig{
        CodeRepositoryName: "example-code-repository-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(sagemaker.CodeRepository, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CodeRepository } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/code_repository";

    resource(CodeRepository, "example", {
      codeRepositoryName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import (
        CodeRepository,
        CodeRepositoryConfig,
    )

    cfg = CodeRepositoryConfig(
        code_repository_name="example",
        tags={"key": "value"},
    )
    ubx.resource(CodeRepository, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_code_repository with
code_repository_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="code_repository_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
