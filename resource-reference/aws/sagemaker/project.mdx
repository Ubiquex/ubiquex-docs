---
title: "aws_sagemaker_project"
description: "Real, generated bindings for `aws_sagemaker_project`."
---

`aws_sagemaker_project` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own project"})

    		ubx.Resource(sagemaker.Project, "ci-runner", sagemaker.ProjectConfig{
    			ProjectName:        "example-project-name",
    			ProjectDescription: "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Project } from "jsr:@ubx/sdk-aws/aws/sagemaker/project";

    export default stack("payments", () => {
      intent({ summary: "payments own project" });

      resource(Project, "ci-runner", {
        projectName: "example-project-name",
        projectDescription: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import Project, ProjectConfig

    def describe():
        ubx.intent("payments own project")

        ubx.resource(Project, "ci-runner", ProjectConfig(
            project_name="example-project-name",
            project_description="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_project, with project name
    example-project-name, project description example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="project_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_catalog_provisioning_details" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="path_id" type="string">
    </ResponseField>
    <ResponseField name="product_id" type="string" required>
    </ResponseField>
    <ResponseField name="provisioning_artifact_id" type="string">
    </ResponseField>
    <ResponseField name="provisioning_parameter" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key" type="string" required>
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="project_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_catalog_provisioning_details" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="path_id" type="string">
    </ResponseField>
    <ResponseField name="product_id" type="string" required>
    </ResponseField>
    <ResponseField name="provisioning_artifact_id" type="string">
    </ResponseField>
    <ResponseField name="provisioning_parameter" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="key" type="string" required>
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
