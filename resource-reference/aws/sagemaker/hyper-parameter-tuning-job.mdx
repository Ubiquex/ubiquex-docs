---
title: "aws_sagemaker_hyper_parameter_tuning_job"
description: "Real, generated bindings for `aws_sagemaker_hyper_parameter_tuning_job`."
---

`aws_sagemaker_hyper_parameter_tuning_job` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.HyperParameterTuningJobConfig{
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(sagemaker.HyperParameterTuningJob, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { HyperParameterTuningJob } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/hyper_parameter_tuning_job";

    resource(HyperParameterTuningJob, "example", {
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import (
        HyperParameterTuningJob,
        HyperParameterTuningJobConfig,
    )

    cfg = HyperParameterTuningJobConfig(
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(HyperParameterTuningJob, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_hyper_parameter_tuning_job with name,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
