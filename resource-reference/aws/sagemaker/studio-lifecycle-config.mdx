---
title: "aws_sagemaker_studio_lifecycle_config"
description: "Real, generated bindings for `aws_sagemaker_studio_lifecycle_config`."
---

`aws_sagemaker_studio_lifecycle_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own studio lifecycle config"})

    		ubx.Resource(sagemaker.StudioLifecycleConfig, "ci-runner", sagemaker.StudioLifecycleConfigConfig{
    			StudioLifecycleConfigAppType: "example",
    			StudioLifecycleConfigContent: "example",
    			StudioLifecycleConfigName:    "example-studio-lifecycle-config-name",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StudioLifecycleConfig } from "jsr:@ubx/sdk-aws/aws/sagemaker/studio_lifecycle_config";

    export default stack("payments", () => {
      intent({ summary: "payments own studio lifecycle config" });

      resource(StudioLifecycleConfig, "ci-runner", {
        studioLifecycleConfigAppType: "example",
        studioLifecycleConfigContent: "example",
        studioLifecycleConfigName: "example-studio-lifecycle-config-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import StudioLifecycleConfig, StudioLifecycleConfigConfig

    def describe():
        ubx.intent("payments own studio lifecycle config")

        ubx.resource(StudioLifecycleConfig, "ci-runner", StudioLifecycleConfigConfig(
            studio_lifecycle_config_app_type="example",
            studio_lifecycle_config_content="example",
            studio_lifecycle_config_name="example-studio-lifecycle-config-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_studio_lifecycle_config, with studio
    lifecycle config app type example, studio lifecycle config
    content example, studio lifecycle config name
    example-studio-lifecycle-config-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="studio_lifecycle_config_app_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="studio_lifecycle_config_content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="studio_lifecycle_config_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
