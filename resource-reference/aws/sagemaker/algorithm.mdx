---
title: "aws_sagemaker_algorithm"
description: "Real, generated bindings for `aws_sagemaker_algorithm`."
---

`aws_sagemaker_algorithm` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"

    cfg := sagemaker.AlgorithmConfig{
        AlgorithmName: "example-algorithm-name",
        AlgorithmDescription: "example",
        CertifyForMarketplace: true,
    }
    ubx.Resource(sagemaker.Algorithm, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Algorithm } from
      "jsr:@ubx/sdk-aws/aws/sagemaker/algorithm";

    resource(Algorithm, "example", {
      algorithmName: "example",
      algorithmDescription: "example",
      certifyForMarketplace: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sagemaker import Algorithm, AlgorithmConfig

    cfg = AlgorithmConfig(
        algorithm_name="example",
        algorithm_description="example",
        certify_for_marketplace=True,
    )
    ubx.resource(Algorithm, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sagemaker_algorithm with algorithm_name,
algorithm_description, certify_for_marketplace.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="algorithm_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="algorithm_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certify_for_marketplace" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="algorithm_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certify_for_marketplace" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="product_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
