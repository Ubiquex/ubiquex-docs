---
title: "aws_s3_bucket_ownership_controls"
description: "Real, generated bindings for `aws_s3_bucket_ownership_controls`."
---

`aws_s3_bucket_ownership_controls` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket ownership controls"})

    		ubx.Resource(s3.BucketOwnershipControls, "ci-runner", s3.BucketOwnershipControlsConfig{
    			Bucket: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketOwnershipControls } from "jsr:@ubx/sdk-aws/aws/s3/bucket_ownership_controls";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket ownership controls" });

      resource(BucketOwnershipControls, "ci-runner", {
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketOwnershipControls, BucketOwnershipControlsConfig

    def describe():
        ubx.intent("payments own bucket ownership controls")

        ubx.resource(BucketOwnershipControls, "ci-runner", BucketOwnershipControlsConfig(
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_ownership_controls, with bucket example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
