---
title: "aws_s3_bucket_versioning"
description: "Real, generated bindings for `aws_s3_bucket_versioning`."
---

`aws_s3_bucket_versioning` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket versioning"})

    		ubx.Resource(s3.BucketVersioning, "ci-runner", s3.BucketVersioningConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    			Mfa:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketVersioning } from "jsr:@ubx/sdk-aws/aws/s3/bucket_versioning";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket versioning" });

      resource(BucketVersioning, "ci-runner", {
        bucket: "example",
        expectedBucketOwner: "example",
        mfa: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketVersioning, BucketVersioningConfig

    def describe():
        ubx.intent("payments own bucket versioning")

        ubx.resource(BucketVersioning, "ci-runner", BucketVersioningConfig(
            bucket="example",
            expected_bucket_owner="example",
            mfa="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_versioning, with bucket example, expected
    bucket owner example, mfa example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mfa" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
