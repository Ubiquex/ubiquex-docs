---
title: "aws_s3_bucket_versioning"
description: "Real, generated bindings for `aws_s3_bucket_versioning`."
---

`aws_s3_bucket_versioning` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3"

    cfg := s3.BucketVersioningConfig{
        Bucket: "example",
        ExpectedBucketOwner: "example",
        Mfa: "example",
    }
    ubx.Resource(s3.BucketVersioning, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketVersioning } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_versioning";

    resource(BucketVersioning, "example", {
      bucket: "example",
      expectedBucketOwner: "example",
      mfa: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import BucketVersioning, BucketVersioningConfig

    cfg = BucketVersioningConfig(
        bucket="example",
        expected_bucket_owner="example",
        mfa="example",
    )
    ubx.resource(BucketVersioning, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_versioning with bucket,
expected_bucket_owner, mfa.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mfa" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
