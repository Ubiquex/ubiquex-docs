---
title: "aws_s3_bucket_cors_configuration"
description: "Real, generated bindings for `aws_s3_bucket_cors_configuration`."
---

`aws_s3_bucket_cors_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3"

    cfg := s3.BucketCorsConfigurationConfig{
        Bucket: "example",
        ExpectedBucketOwner: "example",
    }
    ubx.Resource(s3.BucketCorsConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketCorsConfiguration } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_cors_configuration";

    resource(BucketCorsConfiguration, "example", {
      bucket: "example",
      expectedBucketOwner: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import (
        BucketCorsConfiguration,
        BucketCorsConfigurationConfig,
    )

    cfg = BucketCorsConfigurationConfig(
        bucket="example",
        expected_bucket_owner="example",
    )
    ubx.resource(BucketCorsConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_cors_configuration with bucket,
expected_bucket_owner.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
