---
title: "aws_s3_bucket_request_payment_configuration"
description: "Real, generated bindings for `aws_s3_bucket_request_payment_configuration`."
---

`aws_s3_bucket_request_payment_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket request payment configuration"})

    		ubx.Resource(s3.BucketRequestPaymentConfiguration, "ci-runner", s3.BucketRequestPaymentConfigurationConfig{
    			Bucket:              "example",
    			Payer:               "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketRequestPaymentConfiguration } from "jsr:@ubx/sdk-aws/aws/s3/bucket_request_payment_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket request payment configuration" });

      resource(BucketRequestPaymentConfiguration, "ci-runner", {
        bucket: "example",
        payer: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketRequestPaymentConfiguration, BucketRequestPaymentConfigurationConfig

    def describe():
        ubx.intent("payments own bucket request payment configuration")

        ubx.resource(BucketRequestPaymentConfiguration, "ci-runner", BucketRequestPaymentConfigurationConfig(
            bucket="example",
            payer="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_request_payment_configuration, with bucket
    example, payer example, expected bucket owner example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="payer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
