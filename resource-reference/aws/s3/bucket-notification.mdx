---
title: "aws_s3_bucket_notification"
description: "Real, generated bindings for `aws_s3_bucket_notification`."
---

`aws_s3_bucket_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"

    cfg := s3.BucketNotificationConfig{
        Bucket: "example",
        Eventbridge: true,
    }
    ubx.Resource(s3.BucketNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketNotification } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_notification";

    resource(BucketNotification, "example", {
      bucket: "example",
      eventbridge: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import (
        BucketNotification,
        BucketNotificationConfig,
    )

    cfg = BucketNotificationConfig(
        bucket="example",
        eventbridge=True,
    )
    ubx.resource(BucketNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_notification with bucket, eventbridge.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="eventbridge" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
