---
title: "aws_s3_bucket_notification"
description: "Real, generated bindings for `aws_s3_bucket_notification`."
---

`aws_s3_bucket_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket notification"})

    		ubx.Resource(s3.BucketNotification, "ci-runner", s3.BucketNotificationConfig{
    			Bucket:      "example",
    			Eventbridge: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketNotification } from "jsr:@ubx/sdk-aws/aws/s3/bucket_notification";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket notification" });

      resource(BucketNotification, "ci-runner", {
        bucket: "example",
        eventbridge: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketNotification, BucketNotificationConfig

    def describe():
        ubx.intent("payments own bucket notification")

        ubx.resource(BucketNotification, "ci-runner", BucketNotificationConfig(
            bucket="example",
            eventbridge=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_notification, with bucket example,
    eventbridge true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="eventbridge" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
