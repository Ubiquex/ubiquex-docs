---
title: "aws_s3_files_file_system_policy"
description: "`aws_s3_files_file_system_policy` attaches a real resource-based access policy to an `aws_s3_files_file_system`..."
---

`aws_s3_files_file_system_policy` attaches a real resource-based access policy to an `aws_s3_files_file_system`, controlling which principals can mount and access it as a file system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own files file system policy"})

    		ubx.Resource(s3.FilesFileSystemPolicy, "example", s3.FilesFileSystemPolicyConfig{
    			FileSystemId: "example",
    			Policy:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FilesFileSystemPolicy } from "./local-sdk/aws/sdk/typescript/aws/s3/files_file_system_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own files file system policy" });

      resource(FilesFileSystemPolicy, "example", {
        fileSystemId: "example",
        policy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.s3 import FilesFileSystemPolicy, FilesFileSystemPolicyConfig

    def describe():
        ubx.intent("example own files file system policy")

        ubx.resource(FilesFileSystemPolicy, "example", FilesFileSystemPolicyConfig(
            file_system_id="example",
            policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_files_file_system_policy, with file system id
    example, policy example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy" type="any" required>
  The policy field specifies the JSON-formatted resource-based policy that grants or denies access to the associated Amazon EFS file system for specified principals, actions, and conditions. Required. **(AI-inferred)**
</ResponseField>
