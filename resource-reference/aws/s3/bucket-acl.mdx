---
title: "aws_s3_bucket_acl"
description: "Real, generated bindings for `aws_s3_bucket_acl`."
---

`aws_s3_bucket_acl` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket acl"})

    		ubx.Resource(s3.BucketAcl, "ci-runner", s3.BucketAclConfig{
    			Bucket:              "example",
    			Acl:                 "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketAcl } from "jsr:@ubx/sdk-aws/aws/s3/bucket_acl";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket acl" });

      resource(BucketAcl, "ci-runner", {
        bucket: "example",
        acl: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketAcl, BucketAclConfig

    def describe():
        ubx.intent("payments own bucket acl")

        ubx.resource(BucketAcl, "ci-runner", BucketAclConfig(
            bucket="example",
            acl="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_acl, with bucket example, acl example,
    expected bucket owner example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="acl" type="string">
  Optional.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
