---
title: "aws_s3_bucket"
description: "`aws_s3_bucket` creates the bucket itself."
---

Versioning, encryption, public-access blocking, and lifecycle rules are each their own separate resource, attached to this bucket by reference, real Terraform provider behavior since v4.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own bucket"})

    		ubx.Resource(s3.Bucket, "example", s3.BucketConfig{
    			AbacStatus:              "example",
    			AccelerateConfiguration: map[string]any{"acceleration_status": "example"},
    			AccessControl:           "example",
    			AnalyticsConfigurations: []map[string]any{{"id": "example"}},
    			BucketEncryption:        map[string]any{"server_side_encryption_configuration": []map[string]any{{"blocked_encryption_types": map[string]any{"encryption_type": []string{"example"}}}}},
    			BucketName:              "example-bucket-name",
    			BucketNamePrefix:        "example",
    			BucketNamespace:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Bucket } from "@ubx/sdk-aws/aws/s3/bucket";

    export default stack("example", () => {
      intent({ summary: "example own bucket" });

      resource(Bucket, "example", {
        abacStatus: "example",
        accelerateConfiguration: { accelerationStatus: "example" },
        accessControl: "example",
        analyticsConfigurations: [{ id: "example" }],
        bucketEncryption: {
          serverSideEncryptionConfiguration: [{
            blockedEncryptionTypes: { encryptionType: ["example"] },
          }],
        },
        bucketName: "example-bucket-name",
        bucketNamePrefix: "example",
        bucketNamespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import Bucket, BucketConfig

    def describe():
        ubx.intent("example own bucket")

        ubx.resource(Bucket, "example", BucketConfig(
            abac_status="example",
            accelerate_configuration={"acceleration_status": "example"},
            access_control="example",
            analytics_configurations=[{"id": "example"}],
            bucket_encryption={"server_side_encryption_configuration": [{"blocked_encryption_types": {"encryption_type": ["example"]}}]},
            bucket_name="example-bucket-name",
            bucket_name_prefix="example",
            bucket_namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket, with abac status example, accelerate
    configuration a set value, access control example, analytics
    configurations a set value, bucket encryption a set value, bucket
    name example-bucket-name, bucket name prefix example, bucket
    namespace example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="abac_status" type="string">
  The ABAC status of the general purpose bucket. When ABAC is enabled for the general purpose bucket, you can use tags to manage access to the general purpose buckets as well as for cost tracking purposes. When ABAC is disabled for the general purpose buckets, you can only use tags for cost tracking purposes. For more information, see [Using tags with S3 general purpose buckets](https://docs.aws.amazon.com/AmazonS3/latest/userguide/buckets-tagging.html). Optional.
</ResponseField>

<ResponseField name="accelerate_configuration" type="object">
  Configures the transfer acceleration state for an Amazon S3 bucket. For more information, see [Amazon S3 Transfer Acceleration](https://docs.aws.amazon.com/AmazonS3/latest/dev/transfer-acceleration.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="acceleration_status" type="string" required>
      Specifies the transfer acceleration status of the bucket. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="access_control" type="string">
  This is a legacy property, and it is not recommended for most use cases. A majority of modern use cases in Amazon S3 no longer require the use of ACLs, and we recommend that you keep ACLs disabled. For more information, see [Controlling object ownership](https://docs.aws.amazon.com//AmazonS3/latest/userguide/about-object-ownership.html) in the *Amazon S3 User Guide*. A canned access control list (ACL) that grants predefined permissions to the bucket. For more information about canned ACLs, see [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/dev/acl-overview.html#canned-acl) in the *Amazon S3 User Guide*. S3 buckets are created with ACLs disabled by default. Therefore, unless you explicitly set the [AWS::S3::OwnershipControls](https://docs.aws.amazon.com//AWSCloudFormation/latest/UserGuide/aws-properties-s3-bucket-ownershipcontrols.html) property to enable ACLs, your resource will fail to deploy with any value other than Private. Use cases requiring ACLs are uncommon. The majority of access control configurations can be successfully and more easily achieved with bucket policies. For more information, see [AWS::S3::BucketPolicy](https://docs.aws.amazon.com//AWSCloudFormation/latest/UserGuide/aws-properties-s3-policy.html). For examples of common policy configurations, including S3 Server Access Logs buckets and more, see [Bucket policy examples](https://docs.aws.amazon.com/AmazonS3/latest/userguide/example-bucket-policies.html) in the *Amazon S3 User Guide*. Optional.
</ResponseField>

<ResponseField name="analytics_configurations" type="list(object)">
  Specifies the configuration and any analyses for the analytics filter of an Amazon S3 bucket. Optional.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      A unique identifier for the S3 bucket analytics configuration, used to distinguish it from other analytics configurations on the same bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="prefix" type="string">
      The object key prefix used to filter which objects in the bucket are included in this S3 analytics configuration, so only objects with keys starting with this prefix are analyzed. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="storage_class_analysis" type="object">
      Specifies the data export configuration for storage class analysis, defining where and how the analytics results are delivered (destination bucket, prefix, and output format). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="data_export" type="object">
          Configuration for exporting the results of S3 Storage Class Analysis, defining the destination S3 bucket and the output format (e.g., CSV) for the analysis data. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="destination" type="object">
              Specifies the destination S3 bucket (with its ARN, account ID, prefix, and format) to which the storage class analysis data export is delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_account_id" type="string">
                  The AWS account ID of the destination bucket that receives the storage-class analysis data export, which is the account that owns the destination bucket for cross-account exports. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_arn" type="string">
                  The Amazon Resource Name (ARN) of the S3 bucket where the S3 analytics data export file is delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="format" type="string">
                  Specifies the file format of the exported storage class analysis data to the destination S3 bucket, with the only supported value being 'CSV'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix" type="string">
                  The prefix used to store the storage class analysis data export files in the destination S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="output_schema_version" type="string">
              Defines the version of the output schema used when S3 exports storage class analysis results to the configured destination, with the only supported value being 'V_1'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tag_filters" type="list(object)">
      The tag filters that specify which objects with matching tag key-value pairs are included in the S3 analytics export configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The tag key that defines which objects are included in the S3 analytics configuration's tag-based filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the tag value used to filter objects for the S3 analytics configuration; objects with this tag value are included in the analytics export. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bucket_encryption" type="object">
  Specifies default encryption for a bucket using server-side encryption with Amazon S3-managed keys (SSE-S3), AWS KMS-managed keys (SSE-KMS), or dual-layer server-side encryption with KMS-managed keys (DSSE-KMS). For information about the Amazon S3 default encryption feature, see [Amazon S3 Default Encryption for S3 Buckets](https://docs.aws.amazon.com/AmazonS3/latest/dev/bucket-encryption.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="server_side_encryption_configuration" type="list(object)" required>
      Specifies the default server-side-encryption configuration. Required.
      <Expandable title="properties">
        <ResponseField name="blocked_encryption_types" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="encryption_type" type="list(string)">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="bucket_key_enabled" type="bool">
          Whether to enable an S3 Bucket Key for the SSE-KMS encryption rule, which uses a bucket-level key to reduce KMS request costs for all objects encrypted under this bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="server_side_encryption_by_default" type="object">
          Specifies the default server-side encryption method applied to objects uploaded to the S3 bucket, including the encryption algorithm (e.g., AES256 or aws:kms) and optional AWS KMS key ID. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kmsmaster_key_id" type="string">
              Specifies the AWS KMS key ID (or ARN) that Amazon S3 uses to encrypt objects by default when the default server-side encryption is set to aws:kms for this bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ssealgorithm" type="string">
              Specifies the default server-side encryption algorithm applied to objects written to the S3 bucket, accepting values like AES256, aws:kms, or aws:kms:dsse. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bucket_name" type="string">
  A name for the bucket. If you don't specify a name, AWS CloudFormation generates a unique ID and uses that ID for the bucket name. The bucket name must contain only lowercase letters, numbers, periods (.), and dashes (-) and must follow [Amazon S3 bucket restrictions and limitations](https://docs.aws.amazon.com/AmazonS3/latest/dev/BucketRestrictions.html). For more information, see [Rules for naming Amazon S3 buckets](https://docs.aws.amazon.com/AmazonS3/latest/userguide/bucketnamingrules.html) in the *Amazon S3 User Guide*. If you specify a name, you can't perform updates that require replacement of this resource. You can perform updates that require no or some interruption. If you need to replace the resource, specify a new name. Optional.
</ResponseField>

<ResponseField name="bucket_name_prefix" type="string">
  This prefix is used to generate a unique S3 bucket name by combining the specified prefix with a random suffix, ensuring the bucket name is globally unique. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="bucket_namespace" type="string">
  Optional.
</ResponseField>

<ResponseField name="cors_configuration" type="object">
  Describes the cross-origin access configuration for objects in an Amazon S3 bucket. For more information, see [Enabling Cross-Origin Resource Sharing](https://docs.aws.amazon.com/AmazonS3/latest/dev/cors.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="cors_rules" type="list(object)" required>
      A set of origins and methods (cross-origin access that you want to allow). You can add up to 100 rules to the configuration. Required.
      <Expandable title="properties">
        <ResponseField name="allowed_headers" type="list(string)">
          Specifies the HTTP headers that are allowed in a request to the S3 bucket from a cross-origin origin; each entry can be a specific header name (e.g., 'x-amz-date') or the wildcard '*' to allow all headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allowed_methods" type="list(string)">
          Specifies the list of HTTP methods (such as GET, PUT, POST, DELETE, HEAD) that are allowed for cross-origin requests to the S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allowed_origins" type="list(string)">
          Specifies the origins (scheme, host, and port) that are allowed to make cross-origin requests to the bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exposed_headers" type="list(string)">
          Specifies the response headers that are exposed to the client (browser) in the actual S3 CORS response, allowing client-side scripts to access them. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="string">
          The ID is an optional unique identifier for this CORS rule, up to 255 characters, used to identify the rule within the CORS configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="max_age" type="number">
          Specifies the time in seconds that the browser caches the response to a preflight (OPTIONS) request. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="intelligent_tiering_configurations" type="list(object)">
  Defines how Amazon S3 handles Intelligent-Tiering storage. Optional.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      A unique identifier for this S3 Intelligent-Tiering configuration, used to distinguish it from other configurations on the same bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="prefix" type="string">
      Specifies an object key name prefix that identifies the subset of objects to which this S3 Intelligent-Tiering configuration applies, so only objects with keys beginning with this prefix are included. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      Indicates whether the S3 Intelligent-Tiering configuration is enabled or disabled, with allowed values 'Enabled' and 'Disabled'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tag_filters" type="list(object)">
      Defines the tag filters (key and value pairs) that determine which objects are eligible for this S3 Intelligent-Tiering configuration, so that only objects with matching tags are included in the tiering behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The tag key that, together with the tag value, defines a filter for objects to include in or exclude from an S3 Intelligent-Tiering configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Defines the tag value in an S3 Intelligent-Tiering configuration's tag filter, used to match objects against the specified tag key for applying the tiering policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tierings" type="list(object)">
      Defines each S3 Intelligent-Tiering tier (ARCHIVE_ACCESS or DEEP_ARCHIVE_ACCESS) and the number of days after which objects are moved to that tier. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_tier" type="string">
          The access tier (either ARCHIVE_ACCESS or DEEP_ARCHIVE_ACCESS) to which objects are transitioned after the specified number of days in this Intelligent-Tiering configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="days" type="number">
          The number of days after object creation when S3 Intelligent-Tiering moves the object to the specified access tier (e.g., ARCHIVE_ACCESS or DEEP_ARCHIVE_ACCESS). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inventory_configurations" type="list(object)">
  Specifies the S3 Inventory configuration for an Amazon S3 bucket. For more information, see [GET Bucket inventory](https://docs.aws.amazon.com/AmazonS3/latest/API/RESTBucketGETInventoryConfig.html) in the *Amazon S3 API Reference*. Optional.
  <Expandable title="properties">
    <ResponseField name="destination" type="object">
      The destination configuration for an S3 inventory report, specifying where the inventory list is delivered (e.g., the target S3 bucket ARN, optional prefix, and output format). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="bucket_account_id" type="string">
          The AWS account ID that owns the destination bucket where the inventory list is delivered, allowing inventory delivery to a bucket in a different account. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="bucket_arn" type="string">
          The Amazon Resource Name (ARN) of the S3 bucket where the inventory reports are delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="format" type="string">
          Specifies the file format for the S3 inventory results (e.g., CSV, ORC, or Parquet) stored at the destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix" type="string">
          The prefix to prepend to the object keys of the inventory report files stored in the destination bucket, so that all inventory results are written under this key prefix. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Indicates whether the inventory configuration is enabled for the S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier for this S3 inventory configuration, used to distinguish it from other inventory configurations on the bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="included_object_versions" type="string">
      Determines whether the inventory report includes all versions of each object ('All') or only the current version ('Current'). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="optional_fields" type="list(string)">
      Specifies the list of additional object metadata fields (such as Size, LastModifiedDate, StorageClass, ETag) to be included in the S3 inventory output for the bucket, in addition to the mandatory fields (bucket name and object key). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="prefix" type="string">
      Filters the inventory to only include objects whose keys begin with the specified prefix, such as 'documents/' or 'logs/'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="schedule_frequency" type="string">
      Specifies how often the S3 inventory report is generated, either Daily or Weekly. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lifecycle_configuration" type="object">
  Specifies the lifecycle configuration for objects in an Amazon S3 bucket. For more information, see [Object Lifecycle Management](https://docs.aws.amazon.com/AmazonS3/latest/dev/object-lifecycle-mgmt.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)" required>
      A lifecycle rule for individual objects in an Amazon S3 bucket. Required.
      <Expandable title="properties">
        <ResponseField name="abort_incomplete_multipart_upload" type="object">
          Specifies the number of days after a multipart upload is initiated that the upload must be aborted, as part of an S3 lifecycle rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="days_after_initiation" type="number">
              Specifies the number of days after a multipart upload is initiated that S3 will abort the incomplete multipart upload, based on the upload's initiation date. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="expiration_date" type="string">
          Specifies the exact date (in ISO 8601 format) on which Amazon S3 permanently deletes objects matching this lifecycle rule, used as an alternative to specifying a number of days. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="expiration_in_days" type="number">
          The number of days after object creation when Amazon S3 permanently deletes objects that match this lifecycle rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="expired_object_delete_marker" type="bool">
          Indicates whether Amazon S3 removes expired object delete markers when enforcing this lifecycle rule's expiration action. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="string">
          A unique identifier for the S3 lifecycle rule, used to distinguish it from other rules in the same bucket's lifecycle configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="noncurrent_version_expiration" type="object">
          Specifies the expiration behavior for noncurrent versions of objects in the S3 bucket, including the number of days to keep them and optionally how many newer versions to retain. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="newer_noncurrent_versions" type="number">
              The number of newer noncurrent versions of an object to preserve before the oldest noncurrent version becomes eligible for expiration, enabling you to retain a specified number of recent versions while expiring older ones. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="noncurrent_days" type="number">
              The number of days after which a noncurrent version of an object is expired and permanently deleted in the S3 lifecycle rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="noncurrent_version_expiration_in_days" type="number">
          Specifies the number of days after which an object version that is no longer current is automatically deleted by the lifecycle rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="noncurrent_version_transition" type="object">
          Specifies when noncurrent versions of an object transition to a different storage class, including the number of days after becoming noncurrent and the target storage class. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="newer_noncurrent_versions" type="number">
              The number of newer noncurrent versions to retain before this lifecycle transition applies, allowing the action to skip recent noncurrent versions and only affect older ones. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="storage_class" type="string">
              The storage class that noncurrent object versions are transitioned to, such as GLACIER, DEEP_ARCHIVE, or STANDARD_IA, as defined in the S3 lifecycle noncurrent version transition rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_in_days" type="number">
              The number of days from when an object version becomes noncurrent to when it is transitioned to the storage class specified in the lifecycle rule's noncurrent version transition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="noncurrent_version_transitions" type="list(object)">
          Defines the transition actions for noncurrent versions of objects in an S3 lifecycle rule, specifying when to move them to a different storage class and how many newer versions to consider. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="newer_noncurrent_versions" type="number">
              The number of newer noncurrent versions of an object that must exist before S3 applies the transition action to a noncurrent version. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="storage_class" type="string">
              Sets the storage class (e.g., GLACIER, STANDARD_IA, DEEP_ARCHIVE) that noncurrent object versions are transitioned to when an S3 lifecycle rule's noncurrent version transition action triggers. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_in_days" type="number">
              Specifies the number of days after an object version becomes noncurrent (i.e., no longer the current version) before it is transitioned to the storage class defined in the same noncurrent_version_transitions block. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="object_size_greater_than" type="string">
          Specifies the minimum object size in bytes for which the lifecycle rule applies to objects. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="object_size_less_than" type="string">
          Specifies the maximum object size (in bytes) for which the lifecycle rule applies, meaning only objects smaller than this value are eligible for the rule's actions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Specifies the object key prefix that this lifecycle rule applies to, limiting the rule's effect to objects with keys that begin with this string. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Whether the lifecycle rule is currently active, with allowed values 'Enabled' or 'Disabled', controlling whether Amazon S3 executes the rule's expiration, transition, or other actions on matching objects. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tag_filters" type="list(object)">
          Defines tag key-value pairs that must match an object's tags for the lifecycle rule to apply, thereby scoping the rule to a subset of objects. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The tag key to match in the lifecycle rule's tag filter, used to target objects with that specific tag for lifecycle actions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The tag value that must match an object's tag for the lifecycle rule to apply, used together with the tag key in the same tag filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transition" type="object">
          Defines a lifecycle transition action that moves objects to a specified storage class after a specified number of days or on a specific date. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="storage_class" type="string">
              The S3 storage class to which the objects are transitioned when the lifecycle rule's transition action triggers (for example, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER, or DEEP_ARCHIVE). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_date" type="string">
              Specifies the date (in ISO 8601 format) when objects in the bucket are transitioned to the storage class defined by the same transition object, and is mutually exclusive with the 'days' property. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_in_days" type="number">
              Specifies the number of days after object creation when the lifecycle transition action (to a specified storage class) takes effect for objects in the S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transitions" type="list(object)">
          Defines a lifecycle transition, specifying when objects should transition to a different storage class (such as STANDARD_IA or GLACIER) based on age in days or a specific date. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="storage_class" type="string">
              The Amazon S3 storage class to which the object transitions when the lifecycle rule's transition action is triggered (e.g., STANDARD_IA, GLACIER, or DEEP_ARCHIVE). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_date" type="string">
              Specifies the exact date (in ISO 8601 format) on which Amazon S3 should transition objects to the target storage class defined in the parent transition object, used when the transition is date-based rather than age-based. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transition_in_days" type="number">
              Specifies the number of days after object creation when the lifecycle transition moves the objects to the storage class defined in the same transition action. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="transition_default_minimum_object_size" type="string">
      Indicates which default minimum object size behavior is applied to the lifecycle configuration. This parameter applies to general purpose buckets only. It isn't supported for directory bucket lifecycle configurations. + ``all_storage_classes_128K`` - Objects smaller than 128 KB will not transition to any storage class by default. + ``varies_by_storage_class`` - Objects smaller than 128 KB will transition to Glacier Flexible Retrieval or Glacier Deep Archive storage classes. By default, all other storage classes will prevent transitions smaller than 128 KB. To customize the minimum object size for any transition you can add a filter that specifies a custom ``ObjectSizeGreaterThan`` or ``ObjectSizeLessThan`` in the body of your transition rule. Custom filters always take precedence over the default transition behavior. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logging_configuration" type="object">
  Describes where logs are stored and the prefix that Amazon S3 assigns to all log object keys for a bucket. For examples and more information, see [PUT Bucket logging](https://docs.aws.amazon.com/AmazonS3/latest/API/RESTBucketPUTlogging.html) in the *Amazon S3 API Reference*. To successfully complete the ``AWS::S3::Bucket LoggingConfiguration`` request, you must have ``s3:PutObject`` and ``s3:PutObjectAcl`` in your IAM permissions. Optional.
  <Expandable title="properties">
    <ResponseField name="destination_bucket_name" type="string">
      The name of the bucket where Amazon S3 should store server access log files. You can store log files in any bucket that you own. By default, logs are stored in the bucket where the ``LoggingConfiguration`` property is defined. Optional.
    </ResponseField>
    <ResponseField name="log_file_prefix" type="string">
      A prefix for all log object keys. If you store log files from multiple Amazon S3 buckets in a single bucket, you can use a prefix to distinguish which log files came from which bucket. Optional.
    </ResponseField>
    <ResponseField name="target_object_key_format" type="any">
      Describes the key format for server access log file in the target bucket. You can choose between SimplePrefix and PartitionedPrefix. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metrics_configurations" type="list(object)">
  Specifies a metrics configuration for the CloudWatch request metrics (specified by the metrics configuration ID) from an Amazon S3 bucket. If you're updating an existing metrics configuration, note that this is a full replacement of the existing metrics configuration. If you don't include the elements you want to keep, they are erased. For more information, see [PutBucketMetricsConfiguration](https://docs.aws.amazon.com/AmazonS3/latest/API/RESTBucketPUTMetricConfiguration.html). Optional.
  <Expandable title="properties">
    <ResponseField name="access_point_arn" type="string">
      The ARN of the S3 access point whose requests are filtered into this metrics configuration, restricting CloudWatch request metrics to that access point. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier for this S3 metrics configuration, used to reference or manage the configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="prefix" type="string">
      The object key prefix that filters the objects included in the S3 metrics configuration, so metrics are collected only for objects whose keys begin with this prefix. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tag_filters" type="list(object)">
      Defines the tag key-value pairs that filter which objects are included in this S3 bucket metrics configuration, so metrics are only collected for objects matching all specified tags. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The object tag key used by this metrics configuration filter to include objects in the S3 bucket's metrics reporting. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of the tag key used to filter which objects are included in the S3 bucket's metrics configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="notification_configuration" type="object">
  Describes the notification configuration for an Amazon S3 bucket. If you create the target resource and related permissions in the same template, you might have a circular dependency. For example, you might use the ``AWS::Lambda::Permission`` resource to grant the bucket permission to invoke an AWS Lambda function. However, AWS CloudFormation can't create the bucket until the bucket has permission to invoke the function (AWS CloudFormation checks whether the bucket can invoke the function). If you're using Refs to pass the bucket name, this leads to a circular dependency. To avoid this dependency, you can create all resources without specifying the notification configuration. Then, update the stack with a notification configuration. For more information on permissions, see [AWS::Lambda::Permission](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-lambda-permission.html) and [Granting Permissions to Publish Event Notification Messages to a Destination](https://docs.aws.amazon.com/AmazonS3/latest/dev/NotificationHowTo.html#grant-destinations-permissions-to-s3). Optional.
  <Expandable title="properties">
    <ResponseField name="event_bridge_configuration" type="object">
      Amazon S3 can send events to Amazon EventBridge whenever certain events happen in your bucket, see [Using EventBridge](https://docs.aws.amazon.com/AmazonS3/latest/userguide/EventBridge.html) in the *Amazon S3 User Guide*. Unlike other destinations, delivery of events to EventBridge can be either enabled or disabled for a bucket. If enabled, all events will be sent to EventBridge and you can use EventBridge rules to route events to additional targets. For more information, see [What Is Amazon EventBridge](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-what-is.html) in the *Amazon EventBridge User Guide* Optional.
      <Expandable title="properties">
        <ResponseField name="event_bridge_enabled" type="bool" required>
          Enables delivery of events to Amazon EventBridge. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_configurations" type="list(object)">
      Describes the LAMlong functions to invoke and the events for which to invoke them. Optional.
      <Expandable title="properties">
        <ResponseField name="event" type="string">
          The S3 event type (e.g., s3:ObjectCreated:*) that triggers the Lambda function for this notification configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter" type="object">
          Defines the object key name prefix and/or suffix filters that determine which S3 object events trigger the associated Lambda function notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_key" type="object">
              Specifies the S3 object key filter rules (prefix or suffix) that determine which objects trigger the Lambda function notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="rules" type="list(object)">
                  A list of S3 key filter rules (each specifying a Name of 'prefix' or 'suffix' and a Value) that determine which object key names trigger the Lambda function notification for this configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies whether the filter rule matches the object key by prefix or suffix, with allowed values of 'prefix' or 'suffix'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of a filter rule in an S3 bucket's notification configuration that specifies a prefix or suffix pattern which object key names must match to trigger the Lambda function. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="function" type="string">
          The Amazon Resource Name (ARN) of the AWS Lambda function that Amazon S3 invokes when the specified event type occurs for this filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="queue_configurations" type="list(object)">
      The Amazon Simple Queue Service queues to publish messages to and the events for which to publish messages. Optional.
      <Expandable title="properties">
        <ResponseField name="event" type="string">
          The S3 event type (e.g., s3:ObjectCreated:*) that triggers the notification to the SQS queue. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter" type="object">
          Defines the object key name filter criteria (prefix and/or suffix rules) that determine which S3 objects trigger the notification to the configured queue. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_key" type="object">
              Specifies the S3 object key filter (prefix/suffix rules) that determines which object keys trigger the notification to the configured queue. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="rules" type="list(object)">
                  Specifies a list of filter rules (each a name/value pair with name either 'prefix' or 'suffix') used to match S3 object key names so that only objects whose keys contain the specified prefix or suffix trigger a notification to the configured queue. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the filter rule, which must be either 'prefix' or 'suffix', determining whether the rule matches the object key's prefix or suffix. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      Specifies the value of an S3 object key filter rule (such as a prefix or suffix) used to determine which objects trigger the notification sent to the configured SQS queue. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="queue" type="string">
          The ARN of the Amazon SQS queue to which the S3 bucket sends event notifications for this queue configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="topic_configurations" type="list(object)">
      The topic to which notifications are sent and the events for which notifications are generated. Optional.
      <Expandable title="properties">
        <ResponseField name="event" type="string">
          The S3 event type (e.g., s3:ObjectCreated:* or s3:ObjectRemoved:*) that triggers the notification to the SNS topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter" type="object">
          Configures the S3KeyFilter for this SNS topic notification, restricting which object key names (by prefix and/or suffix) trigger the event. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_key" type="object">
              Defines the S3 object key name filter (prefix and/or suffix rules) that determines which object events trigger the SNS topic notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="rules" type="list(object)">
                  The rules specify the object key name filter with prefix and/or suffix matching patterns that determine which S3 object create events send a notification to the configured SNS topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the S3 key filter rule, which must be either "prefix" or "suffix" and determines how the Value is matched against the object key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the filter rule applied to object key names, used as the prefix or suffix to match for notification events. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="topic" type="string">
          The Amazon Resource Name (ARN) of the SNS topic that S3 publishes event notifications to for this topic configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="object_lock_configuration" type="object">
  Places an Object Lock configuration on the specified bucket. The rule specified in the Object Lock configuration will be applied by default to every new object placed in the specified bucket. For more information, see [Locking Objects](https://docs.aws.amazon.com/AmazonS3/latest/dev/object-lock.html). Optional.
  <Expandable title="properties">
    <ResponseField name="object_lock_enabled" type="string">
      Indicates whether this bucket has an Object Lock configuration enabled. Enable ``ObjectLockEnabled`` when you apply ``ObjectLockConfiguration`` to a bucket. Optional.
    </ResponseField>
    <ResponseField name="rule" type="object">
      Specifies the Object Lock rule for the specified object. Enable the this rule when you apply ``ObjectLockConfiguration`` to a bucket. Optional.
      <Expandable title="properties">
        <ResponseField name="default_retention" type="object">
          The container element for optionally specifying the default Object Lock retention settings for new objects placed in the specified bucket. + The ``DefaultRetention`` settings require both a mode and a period. + The ``DefaultRetention`` period can be either ``Days`` or ``Years`` but you must select one. You cannot specify ``Days`` and ``Years`` at the same time. Optional.
          <Expandable title="properties">
            <ResponseField name="days" type="number">
              The number of days that you want to specify for the default retention period. If Object Lock is turned on, you must specify ``Mode`` and specify either ``Days`` or ``Years``. Optional.
            </ResponseField>
            <ResponseField name="mode" type="string">
              The default Object Lock retention mode you want to apply to new objects placed in the specified bucket. If Object Lock is turned on, you must specify ``Mode`` and specify either ``Days`` or ``Years``. Optional.
            </ResponseField>
            <ResponseField name="years" type="number">
              The number of years that you want to specify for the default retention period. If Object Lock is turned on, you must specify ``Mode`` and specify either ``Days`` or ``Years``. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="object_lock_enabled" type="bool">
  Indicates whether this bucket has an Object Lock configuration enabled. Enable ``ObjectLockEnabled`` when you apply ``ObjectLockConfiguration`` to a bucket. Optional.
</ResponseField>

<ResponseField name="ownership_controls" type="object">
  Specifies the container element for Object Ownership rules. S3 Object Ownership is an Amazon S3 bucket-level setting that you can use to disable access control lists (ACLs) and take ownership of every object in your bucket, simplifying access management for data stored in Amazon S3. For more information, see [Controlling ownership of objects and disabling ACLs](https://docs.aws.amazon.com/AmazonS3/latest/userguide/about-object-ownership.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)" required>
      Specifies the container element for Object Ownership rules. Required.
      <Expandable title="properties">
        <ResponseField name="object_ownership" type="string">
          Specifies the object ownership rule applied to new objects in the S3 bucket, with valid values including BucketOwnerPreferred, ObjectWriter, and BucketOwnerEnforced. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="public_access_block_configuration" type="object">
  The PublicAccessBlock configuration that you want to apply to this Amazon S3 bucket. You can enable the configuration options in any combination. Bucket-level settings work alongside account-level settings (which may inherit from organization-level policies). For more information about when Amazon S3 considers a bucket or object public, see [The Meaning of "Public"](https://docs.aws.amazon.com/AmazonS3/latest/dev/access-control-block-public-access.html#access-control-block-public-access-policy-status) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="block_public_acls" type="bool">
      Specifies whether Amazon S3 should block public access control lists (ACLs) for this bucket and objects in this bucket. Setting this element to ``TRUE`` causes the following behavior: + PUT Bucket ACL and PUT Object ACL calls fail if the specified ACL is public. + PUT Object calls fail if the request includes a public ACL. + PUT Bucket calls fail if the request includes a public ACL. Enabling this setting doesn't affect existing policies or ACLs. Optional.
    </ResponseField>
    <ResponseField name="block_public_policy" type="bool">
      Specifies whether Amazon S3 should block public bucket policies for this bucket. Setting this element to ``TRUE`` causes Amazon S3 to reject calls to PUT Bucket policy if the specified bucket policy allows public access. Enabling this setting doesn't affect existing bucket policies. Optional.
    </ResponseField>
    <ResponseField name="ignore_public_acls" type="bool">
      Specifies whether Amazon S3 should ignore public ACLs for this bucket and objects in this bucket. Setting this element to ``TRUE`` causes Amazon S3 to ignore all public ACLs on this bucket and objects in this bucket. Enabling this setting doesn't affect the persistence of any existing ACLs and doesn't prevent new public ACLs from being set. Optional.
    </ResponseField>
    <ResponseField name="restrict_public_buckets" type="bool">
      Specifies whether Amazon S3 should restrict public bucket policies for this bucket. Setting this element to ``TRUE`` restricts access to this bucket to only AWS-service principals and authorized users within this account if the bucket has a public policy. Enabling this setting doesn't affect previously stored bucket policies, except that public and cross-account access within any public bucket policy, including non-public delegation to specific accounts, is blocked. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="replication_configuration" type="object">
  A container for replication rules. You can add up to 1,000 rules. The maximum size of a replication configuration is 2 MB. The latest version of the replication configuration XML is V2. For more information about XML V2 replication configurations, see [Replication configuration](https://docs.aws.amazon.com/AmazonS3/latest/userguide/replication-add-config.html) in the *Amazon S3 User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="role" type="string" required>
      The Amazon Resource Name (ARN) of the IAMlong (IAM) role that Amazon S3 assumes when replicating objects. For more information, see [How to Set Up Replication](https://docs.aws.amazon.com/AmazonS3/latest/dev/replication-how-setup.html) in the *Amazon S3 User Guide*. Required.
    </ResponseField>
    <ResponseField name="rules" type="list(object)" required>
      A container for one or more replication rules. A replication configuration must have at least one rule and can contain a maximum of 1,000 rules. Required.
      <Expandable title="properties">
        <ResponseField name="delete_marker_replication" type="object">
          Specifies whether delete markers created in the source bucket are replicated to the destination bucket, with Status set to Enabled to replicate them or Disabled to skip them. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="status" type="string">
              Specifies whether S3 replicates delete markers created in the source bucket to the destination bucket; set to Enabled to replicate them or Disabled to prevent replication. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination" type="object">
          The destination configuration for a replication rule, specifying the target S3 bucket and optional settings such as storage class, account, encryption, access control translation, metrics, and replication time. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="access_control_translation" type="object">
              Specifies the access control translation settings that change the ownership of replicated objects to the destination bucket's AWS account, used when replicating across different AWS accounts (cross-account replication). Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="owner" type="string">
                  Overrides the replica owner to the destination bucket owner by specifying `Destination`, required when replicating objects across AWS accounts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="account" type="string">
              The AWS account ID of the destination bucket's owner, required when replicating objects into a bucket in a different AWS account. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bucket" type="string">
              The Amazon Resource Name (ARN) of the S3 bucket to which objects are replicated as part of this replication rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="encryption_configuration" type="object">
              Specifies the encryption configuration for objects replicated to the destination bucket, including the KMS key ID used to encrypt the replicas. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="replica_kms_key_id" type="string">
                  Specifies the AWS KMS key ID (or ARN) used by Amazon S3 to encrypt the replicated objects in the destination bucket when the destination uses SSE-KMS encryption. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="metrics" type="object">
              Specifies the replication metrics configuration for the destination bucket, including whether S3 emits metrics (Status) and the event threshold in minutes (EventThreshold) after which replication events are emitted. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="event_threshold" type="object">
                  Specifies the minimum replication time threshold in minutes (via the 'Minutes' property) that an object must meet for the replication event to be counted and reported in the S3 replication metrics for this destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="minutes" type="number">
                      The number of minutes after which S3 emits a replication metrics event, used as the event threshold for replication time control (RTC) metrics in the destination bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="status" type="string">
                  Specifies whether replication metrics are enabled for this destination bucket, with valid values 'Enabled' or 'Disabled'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="replication_time" type="object">
              Configures S3 Replication Time Control (RTC) for the destination bucket, including whether RTC is enabled and the replication time threshold in minutes. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="status" type="string">
                  Indicates whether S3 Replication Time Control (RTC) is enabled for the replication destination, with valid values 'Enabled' or 'Disabled'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="time" type="object">
                  Specifies the time threshold for S3 Replication Time Control (RTC), defining the number of minutes within which the replicated object is expected to be available in the destination bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="minutes" type="number">
                      Sets the time threshold in minutes (up to 15) for S3 Replication Time Control (RTC) to complete replication of objects, establishing the SLA for replication speed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage_class" type="string">
              The storage class to assign to objects replicated to the destination bucket, overriding the default storage class of the destination bucket; for example, STANDARD, STANDARD_IA, or GLACIER. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="filter" type="object">
          The filter that defines the object key prefix, tag, or logical AND combination of conditions that determine which objects in the source bucket are replicated by this replication rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="and" type="object">
              Specifies a logical AND combination of filter predicates (typically a prefix and one or more tags) that S3 applies when deciding which objects match the replication rule, requiring all specified criteria to be true. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="prefix" type="string">
                  The object key name prefix that must match for objects to be included in an S3 replication rule when the rule's filter uses an AND combination of predicates, typically paired with at least one tag condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tag_filters" type="list(object)">
                  Specifies a list of tag key-value pairs that an object must include for the replication rule's filter 'and' condition to match, limiting replication to objects that have all of the specified tags. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="key" type="string">
                      Specifies the tag key used in the AND condition of an S3 replication rule's filter, allowing the rule to apply only to objects that have a tag with that key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The tag value that an S3 object must match in order to be included in the replication rule's filter, used within an AND condition that combines this tag filter with other filters. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="prefix" type="string">
              The object key prefix that identifies which objects the replication rule applies to, used as a filter condition in an S3 replication rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tag_filter" type="object">
              This field specifies the tag key and value (as nested Key and Value properties) that an object must have for the S3 replication rule's filter to apply, defining the tag-based criteria for selecting objects to replicate. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="key" type="string">
                  The tag key used to filter which objects are replicated; only objects with this tag key (and matching value) are included in the replication rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value of the tag that, together with the key, determines which objects are replicated; S3 replicates only objects that have this exact tag value for the specified tag key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          A unique identifier for the replication rule, used to distinguish it from other rules within the same bucket's replication configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Specifies the object key prefix in the source bucket that identifies which objects this replication rule applies to, with an empty or omitted prefix matching all objects. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          The numeric priority for this replication rule, where higher values take precedence when multiple rules match an object, and it is required when multiple rules exist with filters. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_selection_criteria" type="object">
          Specifies additional source selection criteria for the replication rule, such as whether to replicate objects encrypted with SSE-KMS. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="replica_modifications" type="object">
              Specifies whether Amazon S3 should replicate objects that are modified after being replicated to the destination bucket, using a Status property (Enabled or Disabled) to control the behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="status" type="string">
                  Specifies whether Amazon S3 replicates modifications made to replicas, with the value set to either 'Enabled' or 'Disabled'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sse_kms_encrypted_objects" type="object">
              A filter that, when its Status is 'Enabled', tells S3 replication to replicate source objects encrypted with SSE-KMS, and when 'Disabled', excludes them from replication. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="status" type="string">
                  Controls whether S3 replicates objects that are encrypted with SSE-KMS from the source bucket, where the value must be 'Enabled' to replicate those objects or 'Disabled' to exclude them. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          Specifies whether the replication rule is enabled (Enabled) or disabled (Disabled). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this S3 bucket. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key component of a user-defined tag applied to the S3 bucket, serving as the identifier for cost allocation, access control, and resource metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag attached to the S3 bucket, enabling you to associate arbitrary metadata with the bucket for cost allocation, access control, or operational purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="versioning_configuration" type="object">
  Describes the versioning state of an Amazon S3 bucket. For more information, see [PUT Bucket versioning](https://docs.aws.amazon.com/AmazonS3/latest/API/RESTBucketPUTVersioningStatus.html) in the *Amazon S3 API Reference*. Keep the following timing in mind when enabling, suspending, or transitioning between versioning states: + *Enabling versioning* - Changes may take up to 15 minutes to propagate across all AWS regions for full consistency. + *Suspending versioning* - Takes effect immediately with no propagation delay. + *Transitioning between states* - Any change from Suspended to Enabled has a 15-minute delay. Optional.
  <Expandable title="properties">
    <ResponseField name="status" type="string" required>
      The versioning state of the bucket. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="website_configuration" type="object">
  Specifies website configuration parameters for an Amazon S3 bucket. Optional.
  <Expandable title="properties">
    <ResponseField name="error_document" type="string">
      The name of the error document for the website. Optional.
    </ResponseField>
    <ResponseField name="index_document" type="string">
      The name of the index document for the website. Optional.
    </ResponseField>
    <ResponseField name="redirect_all_requests_to" type="object">
      Specifies the redirect behavior of all requests to a website endpoint of an Amazon S3 bucket. Optional.
      <Expandable title="properties">
        <ResponseField name="host_name" type="string" required>
          Name of the host where requests are redirected. Required.
        </ResponseField>
        <ResponseField name="protocol" type="string">
          Protocol to use when redirecting requests. The default is the protocol that is used in the original request. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="routing_rules" type="list(object)">
      Rules that define when a redirect is applied and the redirect behavior. Optional.
      <Expandable title="properties">
        <ResponseField name="redirect_rule" type="object">
          Specifies the redirect behavior for a routing rule, including protocol, host name, port, key replacement, and HTTP redirect code, applied when the rule's condition is met. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="host_name" type="string">
              Specifies the host name to use in the redirect request for a routing rule in the S3 website configuration; for example, if the rule matches, requests are redirected to this host instead of the original bucket's website endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="http_redirect_code" type="string">
              Specifies the HTTP redirect code (such as 301, 302, or 307) that S3 returns in the redirect response when this routing rule matches, overriding the default 301. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="protocol" type="string">
              Specifies the protocol (http or https) to use in the redirect location for requests that match the routing rule's condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace_key_prefix_with" type="string">
              Specifies the object key prefix to use in the redirect request, replacing the matched prefix from the original request. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="replace_key_with" type="string">
              Specifies the object key name to use in the redirect request, replacing the entire original key for requests that match the routing rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="routing_rule_condition" type="object">
          Defines the condition (such as a specific HTTP error code or a key prefix) that must be matched for the S3 website routing rule to take effect. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="http_error_code_returned_equals" type="string">
              The HTTP error code (e.g., '404') that the origin must return in order for this routing rule condition to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_prefix_equals" type="string">
              Specifies the object key prefix that must match for the routing rule condition to apply when S3 serves requests from the website endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  the Amazon Resource Name (ARN) of the specified bucket. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name" type="string">
  The bucket's DNS domain name, which is the global S3 endpoint of the form `&lt;bucket-name&gt;.s3.amazonaws.com`, exposed as a computed attribute for resource references. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="dual_stack_domain_name" type="string">
  The dual-stack domain name for the S3 bucket, which is the automatically generated endpoint that supports both IPv4 and IPv6 connections, typically in the format bucket-name.s3.dualstack.region.amazonaws.com. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="metadata_configuration" type="object">
  Creates a V2 S3 Metadata configuration of a general purpose bucket. For more information, see [Accelerating data discovery with S3 Metadata](https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-overview.html) in the *Amazon S3 User Guide*. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="annotation_table_configuration" type="object">
      The annotation table configuration for an S3 Metadata configuration. The annotation table tracks all annotations on objects in your bucket so that you can query annotation data at scale. If you've disabled your annotation table configuration and now want to re-enable it, you must first manually delete the old annotation table from your AWS managed table bucket. Otherwise, the newly re-enabled annotation table configuration will enter a failed state because the annotation table already exists in the table bucket. Optional.
      <Expandable title="properties">
        <ResponseField name="configuration_state" type="string" required>
          Specifies whether the annotation table configuration is enabled or disabled. Required.
        </ResponseField>
        <ResponseField name="encryption_configuration" type="object">
          The encryption settings for an S3 Metadata journal table or inventory table configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_arn" type="string">
              If server-side encryption with KMSlong (KMS) keys (SSE-KMS) is specified, you must also specify the KMS key Amazon Resource Name (ARN). You must specify a customer-managed KMS key that's located in the same Region as the general purpose bucket that corresponds to the metadata table configuration. Optional.
            </ResponseField>
            <ResponseField name="sse_algorithm" type="string" required>
              The encryption type specified for a metadata table. To specify server-side encryption with KMSlong (KMS) keys (SSE-KMS), use the ``aws:kms`` value. To specify server-side encryption with Amazon S3 managed keys (SSE-S3), use the ``AES256`` value. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="role" type="string">
          The ARN of the IAM role that grants Amazon S3 Metadata permission to read annotations from your bucket. Optional.
        </ResponseField>
        <ResponseField name="table_arn" type="string">
          The Amazon Resource Name (ARN) for the annotation table. Optional.
        </ResponseField>
        <ResponseField name="table_name" type="string">
          The name of the annotation table. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination" type="object">
      The destination information for the S3 Metadata configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="table_bucket_arn" type="string">
          The Amazon Resource Name (ARN) of the table bucket where the metadata configuration is stored. Optional.
        </ResponseField>
        <ResponseField name="table_bucket_type" type="string" required>
          The type of the table bucket where the metadata configuration is stored. The ``aws`` value indicates an AWS managed table bucket, and the ``customer`` value indicates a customer-managed table bucket. V2 metadata configurations are stored in AWS managed table buckets, and V1 metadata configurations are stored in customer-managed table buckets. Required.
        </ResponseField>
        <ResponseField name="table_namespace" type="string">
          The namespace in the table bucket where the metadata tables for a metadata configuration are stored. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="inventory_table_configuration" type="object">
      The inventory table configuration for an S3 Metadata configuration. If you've disabled your inventory table configuration and now want to re-enable it, you must first manually delete the old inventory table from your AWS managed table bucket. Otherwise, the newly re-enabled inventory table configuration will enter a failed state because the inventory table already exists in the table bucket. Optional.
      <Expandable title="properties">
        <ResponseField name="configuration_state" type="string" required>
          The configuration state of the inventory table, indicating whether the inventory table is enabled or disabled. Required.
        </ResponseField>
        <ResponseField name="encryption_configuration" type="object">
          The encryption settings for an S3 Metadata journal table or inventory table configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_arn" type="string">
              If server-side encryption with KMSlong (KMS) keys (SSE-KMS) is specified, you must also specify the KMS key Amazon Resource Name (ARN). You must specify a customer-managed KMS key that's located in the same Region as the general purpose bucket that corresponds to the metadata table configuration. Optional.
            </ResponseField>
            <ResponseField name="sse_algorithm" type="string" required>
              The encryption type specified for a metadata table. To specify server-side encryption with KMSlong (KMS) keys (SSE-KMS), use the ``aws:kms`` value. To specify server-side encryption with Amazon S3 managed keys (SSE-S3), use the ``AES256`` value. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="table_arn" type="string">
          The Amazon Resource Name (ARN) for the inventory table. Optional.
        </ResponseField>
        <ResponseField name="table_name" type="string">
          The name of the inventory table. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="journal_table_configuration" type="object" required>
      The journal table configuration for an S3 Metadata configuration. The journal table is required for each metadata table configuration and cannot be disabled. The journal configuration will enter a failed state if a journal table already exists in the table bucket. The journal table of a previous configuration must be deleted before a new journal table can be created successfully. Required.
      <Expandable title="properties">
        <ResponseField name="encryption_configuration" type="object">
          The encryption settings for an S3 Metadata journal table or inventory table configuration. Optional.
          <Expandable title="properties">
            <ResponseField name="kms_key_arn" type="string">
              If server-side encryption with KMSlong (KMS) keys (SSE-KMS) is specified, you must also specify the KMS key Amazon Resource Name (ARN). You must specify a customer-managed KMS key that's located in the same Region as the general purpose bucket that corresponds to the metadata table configuration. Optional.
            </ResponseField>
            <ResponseField name="sse_algorithm" type="string" required>
              The encryption type specified for a metadata table. To specify server-side encryption with KMSlong (KMS) keys (SSE-KMS), use the ``aws:kms`` value. To specify server-side encryption with Amazon S3 managed keys (SSE-S3), use the ``AES256`` value. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="record_expiration" type="object" required>
          The journal table record expiration settings for a journal table in an S3 Metadata configuration. Required.
          <Expandable title="properties">
            <ResponseField name="days" type="number">
              If you enable journal table record expiration, you can set the number of days to retain your journal table records. Journal table records must be retained for a minimum of 7 days. To set this value, specify any whole number from ``7`` to ``2147483647``. For example, to retain your journal table records for one year, set this value to ``365``. Optional.
            </ResponseField>
            <ResponseField name="expiration" type="string" required>
              Specifies whether journal table record expiration is enabled or disabled. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="table_arn" type="string">
          The Amazon Resource Name (ARN) for the journal table. Optional.
        </ResponseField>
        <ResponseField name="table_name" type="string">
          The name of the journal table. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata_table_configuration" type="object">
  We recommend that you create your S3 Metadata configurations by using the V2 [MetadataConfiguration](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-properties-s3-bucket-metadataconfiguration.html) resource type. We no longer recommend using the V1 ``MetadataTableConfiguration`` resource type. If you created your S3 Metadata configuration before July 15, 2025, we recommend that you delete and re-create your configuration by using the [MetadataConfiguration](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-properties-s3-bucket-metadataconfiguration.html) resource type so that you can expire journal table records and create a live inventory table. Creates a V1 S3 Metadata configuration for a general purpose bucket. For more information, see [Accelerating data discovery with S3 Metadata](https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-overview.html) in the *Amazon S3 User Guide*. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="s3_tables_destination" type="object" required>
      The destination information for a V1 S3 Metadata configuration. The destination table bucket must be in the same Region and AWS-account as the general purpose bucket. The specified metadata table name must be unique within the ``aws_s3_metadata`` namespace in the destination table bucket. Required.
      <Expandable title="properties">
        <ResponseField name="table_arn" type="string">
          The Amazon Resource Name (ARN) for the metadata table in the metadata table configuration. The specified metadata table name must be unique within the ``aws_s3_metadata`` namespace in the destination table bucket. Optional.
        </ResponseField>
        <ResponseField name="table_bucket_arn" type="string" required>
          The Amazon Resource Name (ARN) for the table bucket that's specified as the destination in the metadata table configuration. The destination table bucket must be in the same Region and AWS-account as the general purpose bucket. Required.
        </ResponseField>
        <ResponseField name="table_name" type="string" required>
          The name for the metadata table in your metadata table configuration. The specified metadata table name must be unique within the ``aws_s3_metadata`` namespace in the destination table bucket. Required.
        </ResponseField>
        <ResponseField name="table_namespace" type="string">
          The table bucket namespace for the metadata table in your metadata table configuration. This value is always ``aws_s3_metadata``. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="regional_domain_name" type="string">
  The AWS S3 bucket's regional domain name, e.g., 'my-bucket.s3.us-west-2.amazonaws.com', which is a constructed endpoint for accessing the bucket in the region where it was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="website_url" type="string">
  The website URL endpoint for the S3 bucket when static website hosting is configured, such as http://bucket-name.s3-website-region.amazonaws.com. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
