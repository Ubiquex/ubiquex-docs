---
title: "aws_s3_multi_region_access_point"
description: "`aws_s3_multi_region_access_point` creates a real, single global endpoint that routes requests to the closest of several `aws_s3_bucket`s replicated..."
---

`aws_s3_multi_region_access_point` creates a real, single global endpoint that routes requests to the closest of several `aws_s3_bucket`s replicated across multiple Regions, so an application can use one endpoint regardless of which Region actually serves a given request.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own multi region access point"})

    		ubx.Resource(s3.MultiRegionAccessPoint, "example", s3.MultiRegionAccessPointConfig{
    			Regions:                        []map[string]any{{"bucket": "example"}},
    			Name:                           "example",
    			PublicAccessBlockConfiguration: map[string]any{"block_public_acls": true},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MultiRegionAccessPoint } from "@ubx/sdk-aws/aws/s3/multi_region_access_point";

    export default stack("example", () => {
      intent({ summary: "example own multi region access point" });

      resource(MultiRegionAccessPoint, "example", {
        regions: [{ bucket: "example" }],
        name: "example",
        publicAccessBlockConfiguration: { blockPublicAcls: true },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3.multi_region_access_point import MultiRegionAccessPoint, MultiRegionAccessPointConfig

    def describe():
        ubx.intent("example own multi region access point")

        ubx.resource(MultiRegionAccessPoint, "example", MultiRegionAccessPointConfig(
            regions=[{"bucket": "example"}],
            name="example",
            public_access_block_configuration={"block_public_acls": True},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_multi_region_access_point called "example", with
    regions a set value, public access block configuration a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string">
  The name you want to assign to this Multi Region Access Point. Optional.
</ResponseField>

<ResponseField name="public_access_block_configuration" type="object">
  This object configures the Multi-Region Access Point's public access block settings, allowing you to block public ACLs, ignore existing public ACLs, block public bucket policies, and restrict cross-account access to buckets with public policies. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="block_public_acls" type="bool">
      Specifies whether Amazon S3 should block public access control lists (ACLs) for buckets in this account. Setting this element to TRUE causes the following behavior: - PUT Bucket acl and PUT Object acl calls fail if the specified ACL is public. - PUT Object calls fail if the request includes a public ACL. . - PUT Bucket calls fail if the request includes a public ACL. Enabling this setting doesn't affect existing policies or ACLs. Optional.
    </ResponseField>
    <ResponseField name="block_public_policy" type="bool">
      Specifies whether Amazon S3 should block public bucket policies for buckets in this account. Setting this element to TRUE causes Amazon S3 to reject calls to PUT Bucket policy if the specified bucket policy allows public access. Enabling this setting doesn't affect existing bucket policies. Optional.
    </ResponseField>
    <ResponseField name="ignore_public_acls" type="bool">
      Specifies whether Amazon S3 should ignore public ACLs for buckets in this account. Setting this element to TRUE causes Amazon S3 to ignore all public ACLs on buckets in this account and any objects that they contain. Enabling this setting doesn't affect the persistence of any existing ACLs and doesn't prevent new public ACLs from being set. Optional.
    </ResponseField>
    <ResponseField name="restrict_public_buckets" type="bool">
      Specifies whether Amazon S3 should restrict public bucket policies for this bucket. Setting this element to TRUE restricts access to this bucket to only AWS services and authorized users within this account if the bucket has a public policy. Enabling this setting doesn't affect previously stored bucket policies, except that public and cross-account access within any public bucket policy, including non-public delegation to specific accounts, is blocked. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="regions" type="list(object)" required>
  The list of buckets that you want to associate this Multi Region Access Point with. Required.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string">
      The name of the S3 bucket to route requests to in this Region of the Multi-Region Access Point. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="bucket_account_id" type="string">
      The AWS account ID that owns the bucket in the specified Region for the Multi-Region Access Point. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="alias" type="string">
  The alias is a unique identifier to, and is part of the public DNS name for this Multi Region Access Point Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of the when the Multi Region Access Point is created Computed by AWS after `ship`.
</ResponseField>
