---
title: "aws_s3_bucket_metric"
description: "Real, generated bindings for `aws_s3_bucket_metric`."
---

`aws_s3_bucket_metric` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3"

    cfg := s3.BucketMetricConfig{
        Bucket: "example",
        Name: "example-name",
    }
    ubx.Resource(s3.BucketMetric, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketMetric } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_metric";

    resource(BucketMetric, "example", {
      bucket: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import BucketMetric, BucketMetricConfig

    cfg = BucketMetricConfig(
        bucket="example",
        name="example",
    )
    ubx.resource(BucketMetric, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_metric with bucket, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
