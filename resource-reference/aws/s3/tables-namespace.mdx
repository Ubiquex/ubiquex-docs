---
title: "aws_s3_tables_namespace"
description: "`aws_s3_tables_namespace` creates a real, logical grouping of `aws_s3_tables_table`s within an `aws_s3_tables_table_bucket`..."
---

`aws_s3_tables_namespace` creates a real, logical grouping of `aws_s3_tables_table`s within an `aws_s3_tables_table_bucket`, a SQL-style schema/namespace for organizing Iceberg tables rather than leaving them all flat in the bucket.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own tables namespace"})

    		ubx.Resource(s3.TablesNamespace, "example", s3.TablesNamespaceConfig{
    			Namespace:      "example",
    			TableBucketArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TablesNamespace } from "@ubx/sdk-aws/aws/s3/tables_namespace";

    export default stack("example", () => {
      intent({ summary: "example own tables namespace" });

      resource(TablesNamespace, "example", {
        namespace: "example",
        tableBucketArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import TablesNamespace, TablesNamespaceConfig

    def describe():
        ubx.intent("example own tables namespace")

        ubx.resource(TablesNamespace, "example", TablesNamespaceConfig(
            namespace="example",
            table_bucket_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_tables_namespace, with namespace example, table
    bucket arn arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="namespace" type="string" required>
  A name for the namespace. Required.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  The Amazon Resource Name (ARN) of the specified table bucket. Required.
</ResponseField>
