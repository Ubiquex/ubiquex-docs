---
title: "aws_s3_multi_region_access_point_policy"
description: "`aws_s3_multi_region_access_point_policy` attaches a real resource-based access policy to an `aws_s3_multi_region_access_point`..."
---

`aws_s3_multi_region_access_point_policy` attaches a real resource-based access policy to an `aws_s3_multi_region_access_point`, controlling which principals can use that single global endpoint to reach the underlying replicated buckets.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own multi region access point policy"})

    		ubx.Resource(s3.MultiRegionAccessPointPolicy, "ci-runner", s3.MultiRegionAccessPointPolicyConfig{
    			MrapName: "example-mrap-name",
    			Policy:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MultiRegionAccessPointPolicy } from "./local-sdk/aws/sdk/typescript/aws/s3/multi_region_access_point_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own multi region access point policy" });

      resource(MultiRegionAccessPointPolicy, "ci-runner", {
        mrapName: "example-mrap-name",
        policy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.s3 import MultiRegionAccessPointPolicy, MultiRegionAccessPointPolicyConfig

    def describe():
        ubx.intent("payments own multi region access point policy")

        ubx.resource(MultiRegionAccessPointPolicy, "ci-runner", MultiRegionAccessPointPolicyConfig(
            mrap_name="example-mrap-name",
            policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_multi_region_access_point_policy, with mrap name
    example-mrap-name, policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="mrap_name" type="string" required>
  The name of the Multi Region Access Point to apply policy Required.
</ResponseField>

<ResponseField name="policy" type="any" required>
  Policy document to apply to a Multi Region Access Point Required.
</ResponseField>

## Output properties

<ResponseField name="policy_status" type="object">
  The Policy Status associated with this Multi Region Access Point Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="is_public" type="string" required>
      Specifies whether the policy is public or not. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
