---
title: "aws_s3_bucket_logging"
description: "Real, generated bindings for `aws_s3_bucket_logging`."
---

`aws_s3_bucket_logging` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3"

    cfg := s3.BucketLoggingConfig{
        Bucket: "example",
        TargetBucket: "example",
        TargetPrefix: "example",
        ExpectedBucketOwner: "example",
    }
    ubx.Resource(s3.BucketLogging, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketLogging } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_logging";

    resource(BucketLogging, "example", {
      bucket: "example",
      targetBucket: "example",
      targetPrefix: "example",
      expectedBucketOwner: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import BucketLogging, BucketLoggingConfig

    cfg = BucketLoggingConfig(
        bucket="example",
        target_bucket="example",
        target_prefix="example",
        expected_bucket_owner="example",
    )
    ubx.resource(BucketLogging, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_logging with bucket, target_bucket,
target_prefix, expected_bucket_owner.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_prefix" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
