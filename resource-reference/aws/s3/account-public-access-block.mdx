---
title: "aws_s3_account_public_access_block"
description: "Real, generated bindings for `aws_s3_account_public_access_block`."
---

`aws_s3_account_public_access_block` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account public access block"})

    		ubx.Resource(s3.AccountPublicAccessBlock, "ci-runner", s3.AccountPublicAccessBlockConfig{
    			BlockPublicAcls:       true,
    			BlockPublicPolicy:     true,
    			IgnorePublicAcls:      true,
    			RestrictPublicBuckets: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountPublicAccessBlock } from "jsr:@ubx/sdk-aws/aws/s3/account_public_access_block";

    export default stack("payments", () => {
      intent({ summary: "payments own account public access block" });

      resource(AccountPublicAccessBlock, "ci-runner", {
        blockPublicAcls: true,
        blockPublicPolicy: true,
        ignorePublicAcls: true,
        restrictPublicBuckets: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import AccountPublicAccessBlock, AccountPublicAccessBlockConfig

    def describe():
        ubx.intent("payments own account public access block")

        ubx.resource(AccountPublicAccessBlock, "ci-runner", AccountPublicAccessBlockConfig(
            block_public_acls=True,
            block_public_policy=True,
            ignore_public_acls=True,
            restrict_public_buckets=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_account_public_access_block, with block public acls
    true, block public policy true, ignore public acls true, restrict
    public buckets true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="block_public_acls" type="bool">
  Optional.
</ResponseField>

<ResponseField name="block_public_policy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ignore_public_acls" type="bool">
  Optional.
</ResponseField>

<ResponseField name="restrict_public_buckets" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
