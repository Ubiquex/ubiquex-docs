---
title: "aws_s3_bucket_replication_configuration"
description: "Real, generated bindings for `aws_s3_bucket_replication_configuration`."
---

`aws_s3_bucket_replication_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket replication configuration"})

    		ubx.Resource(s3.BucketReplicationConfiguration, "ci-runner", s3.BucketReplicationConfigurationConfig{
    			Bucket: "example",
    			Role:   "example",
    			Token:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketReplicationConfiguration } from "jsr:@ubx/sdk-aws/aws/s3/bucket_replication_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket replication configuration" });

      resource(BucketReplicationConfiguration, "ci-runner", {
        bucket: "example",
        role: "example",
        token: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketReplicationConfiguration, BucketReplicationConfigurationConfig

    def describe():
        ubx.intent("payments own bucket replication configuration")

        ubx.resource(BucketReplicationConfiguration, "ci-runner", BucketReplicationConfigurationConfig(
            bucket="example",
            role="example",
            token="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_replication_configuration, with bucket
    example, role example, token example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="token" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
