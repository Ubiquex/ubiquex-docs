---
title: "aws_s3_bucket_replication_configuration"
description: "Real, generated bindings for `aws_s3_bucket_replication_configuration`."
---

`aws_s3_bucket_replication_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3"

    cfg := s3.BucketReplicationConfigurationConfig{
        Bucket: "example",
        Role: "example",
        Token: "example",
    }
    ubx.Resource(s3.BucketReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BucketReplicationConfiguration } from
      "jsr:@ubx/sdk-aws/aws/s3/bucket_replication_configuration";

    resource(BucketReplicationConfiguration, "example", {
      bucket: "example",
      role: "example",
      token: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3 import (
        BucketReplicationConfiguration,
        BucketReplicationConfigurationConfig,
    )

    cfg = BucketReplicationConfigurationConfig(
        bucket="example",
        role="example",
        token="example",
    )
    ubx.resource(BucketReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3_bucket_replication_configuration with bucket,
role, token.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="token" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
