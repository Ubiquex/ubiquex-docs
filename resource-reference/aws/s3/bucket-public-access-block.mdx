---
title: "aws_s3_bucket_public_access_block"
description: "Real, generated bindings for `aws_s3_bucket_public_access_block`."
---

`aws_s3_bucket_public_access_block` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket public access block"})

    		ubx.Resource(s3.BucketPublicAccessBlock, "ci-runner", s3.BucketPublicAccessBlockConfig{
    			Bucket:                "example",
    			BlockPublicAcls:       true,
    			BlockPublicPolicy:     true,
    			IgnorePublicAcls:      true,
    			RestrictPublicBuckets: true,
    			SkipDestroy:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketPublicAccessBlock } from "jsr:@ubx/sdk-aws/aws/s3/bucket_public_access_block";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket public access block" });

      resource(BucketPublicAccessBlock, "ci-runner", {
        bucket: "example",
        blockPublicAcls: true,
        blockPublicPolicy: true,
        ignorePublicAcls: true,
        restrictPublicBuckets: true,
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import BucketPublicAccessBlock, BucketPublicAccessBlockConfig

    def describe():
        ubx.intent("payments own bucket public access block")

        ubx.resource(BucketPublicAccessBlock, "ci-runner", BucketPublicAccessBlockConfig(
            bucket="example",
            block_public_acls=True,
            block_public_policy=True,
            ignore_public_acls=True,
            restrict_public_buckets=True,
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_bucket_public_access_block, with bucket example,
    block public acls true, block public policy true, ignore public
    acls true, restrict public buckets true, skip destroy true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="block_public_acls" type="bool">
  Optional.
</ResponseField>

<ResponseField name="block_public_policy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ignore_public_acls" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="restrict_public_buckets" type="bool">
  Optional.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
