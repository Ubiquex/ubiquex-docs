---
title: "aws_s3_outposts_bucket_policy"
description: "`aws_s3_outposts_bucket_policy` attaches a real resource-based access policy to an `aws_s3_outposts_bucket`..."
---

`aws_s3_outposts_bucket_policy` attaches a real resource-based access policy to an `aws_s3_outposts_bucket`, controlling which principals on the local Outpost network can access it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own outposts bucket policy"})

    		ubx.Resource(s3.OutpostsBucketPolicy, "example", s3.OutpostsBucketPolicyConfig{
    			Bucket:         "example",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OutpostsBucketPolicy } from "@ubx/sdk-aws/aws/s3/outposts_bucket_policy";

    export default stack("example", () => {
      intent({ summary: "example own outposts bucket policy" });

      resource(OutpostsBucketPolicy, "example", {
        bucket: "example",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3 import OutpostsBucketPolicy, OutpostsBucketPolicyConfig

    def describe():
        ubx.intent("example own outposts bucket policy")

        ubx.resource(OutpostsBucketPolicy, "example", OutpostsBucketPolicyConfig(
            bucket="example",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_outposts_bucket_policy, with bucket example, policy
    document example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bucket" type="string" required>
  The Amazon Resource Name (ARN) of the specified bucket. Required.
</ResponseField>

<ResponseField name="policy_document" type="any" required>
  A policy document containing permissions to add to the specified bucket. Required.
</ResponseField>
