---
title: "aws_s3_express_bucket_policy"
description: "`aws_s3_express_bucket_policy` attaches a real resource-based access policy to an `aws_s3_express_directory_bucket`..."
---

`aws_s3_express_bucket_policy` attaches a real resource-based access policy to an `aws_s3_express_directory_bucket`, controlling which principals can access that S3 Express One Zone bucket.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own express bucket policy"})

    		ubx.Resource(s3.ExpressBucketPolicy, "ci-runner", s3.ExpressBucketPolicyConfig{
    			Bucket:         "example",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExpressBucketPolicy } from "./local-sdk/aws/sdk/typescript/aws/s3/express_bucket_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own express bucket policy" });

      resource(ExpressBucketPolicy, "ci-runner", {
        bucket: "example",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.s3 import ExpressBucketPolicy, ExpressBucketPolicyConfig

    def describe():
        ubx.intent("payments own express bucket policy")

        ubx.resource(ExpressBucketPolicy, "ci-runner", ExpressBucketPolicyConfig(
            bucket="example",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_express_bucket_policy, with bucket example, policy
    document example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bucket" type="string" required>
  The name of the S3 directory bucket to which the policy applies. Required.
</ResponseField>

<ResponseField name="policy_document" type="any" required>
  A policy document containing permissions to add to the specified bucket. In IAM, you must provide policy documents in JSON format. However, in CloudFormation you can provide the policy in JSON or YAML format because CloudFormation converts YAML to JSON before submitting it to IAM. Required.
</ResponseField>
