---
title: "aws_appstream_user_stack_association"
description: "Real, generated bindings for `aws_appstream_user_stack_association`."
---

`aws_appstream_user_stack_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appstream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user stack association"})

    		ubx.Resource(appstream.UserStackAssociation, "ci-runner", appstream.UserStackAssociationConfig{
    			AuthenticationType:    "example",
    			StackName:             "example-stack-name",
    			UserName:              "example-user-name",
    			SendEmailNotification: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserStackAssociation } from "jsr:@ubx/sdk-aws/aws/appstream/user_stack_association";

    export default stack("payments", () => {
      intent({ summary: "payments own user stack association" });

      resource(UserStackAssociation, "ci-runner", {
        authenticationType: "example",
        stackName: "example-stack-name",
        userName: "example-user-name",
        sendEmailNotification: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appstream import UserStackAssociation, UserStackAssociationConfig

    def describe():
        ubx.intent("payments own user stack association")

        ubx.resource(UserStackAssociation, "ci-runner", UserStackAssociationConfig(
            authentication_type="example",
            stack_name="example-stack-name",
            user_name="example-user-name",
            send_email_notification=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appstream_user_stack_association, with authentication
    type example, stack name example-stack-name, user name
    example-user-name, send email notification true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authentication_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="send_email_notification" type="bool">
  Optional.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
