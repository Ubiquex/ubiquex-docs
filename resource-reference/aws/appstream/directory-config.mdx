---
title: "aws_appstream_directory_config"
description: "Real, generated bindings for `aws_appstream_directory_config`."
---

`aws_appstream_directory_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appstream"

    cfg := appstream.DirectoryConfigConfig{
        DirectoryName: "example-directory-name",
        OrganizationalUnitDistinguishedNames: []string{"example"},
    }
    ubx.Resource(appstream.DirectoryConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DirectoryConfig } from
      "jsr:@ubx/sdk-aws/aws/appstream/directory_config";

    resource(DirectoryConfig, "example", {
      directoryName: "example",
      organizationalUnitDistinguishedNames: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appstream import (
        DirectoryConfig,
        DirectoryConfigConfig,
    )

    cfg = DirectoryConfigConfig(
        directory_name="example",
        organizational_unit_distinguished_names=["example"],
    )
    ubx.resource(DirectoryConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appstream_directory_config with directory_name,
organizational_unit_distinguished_names.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="organizational_unit_distinguished_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
