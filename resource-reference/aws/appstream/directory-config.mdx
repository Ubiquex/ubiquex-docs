---
title: "aws_appstream_directory_config"
description: "Real, generated bindings for `aws_appstream_directory_config`."
---

`aws_appstream_directory_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appstream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory config"})

    		ubx.Resource(appstream.DirectoryConfig, "ci-runner", appstream.DirectoryConfigConfig{
    			DirectoryName:                        "example-directory-name",
    			OrganizationalUnitDistinguishedNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryConfig } from "jsr:@ubx/sdk-aws/aws/appstream/directory_config";

    export default stack("payments", () => {
      intent({ summary: "payments own directory config" });

      resource(DirectoryConfig, "ci-runner", {
        directoryName: "example-directory-name",
        organizationalUnitDistinguishedNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appstream import DirectoryConfig, DirectoryConfigConfig

    def describe():
        ubx.intent("payments own directory config")

        ubx.resource(DirectoryConfig, "ci-runner", DirectoryConfigConfig(
            directory_name="example-directory-name",
            organizational_unit_distinguished_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appstream_directory_config, with directory name
    example-directory-name, organizational unit distinguished names a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="organizational_unit_distinguished_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
