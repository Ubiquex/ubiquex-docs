---
title: "aws_appstream_user"
description: "Real, generated bindings for `aws_appstream_user`."
---

`aws_appstream_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appstream"

    cfg := appstream.UserConfig{
        AuthenticationType: "example",
        UserName: "example-user-name",
        Enabled: true,
        FirstName: "example-first-name",
    }
    ubx.Resource(appstream.User, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { User } from "jsr:@ubx/sdk-aws/aws/appstream/user";

    resource(User, "example", {
      authenticationType: "example",
      userName: "example",
      enabled: true,
      firstName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appstream import User, UserConfig

    cfg = UserConfig(
        authentication_type="example",
        user_name="example",
        enabled=True,
        first_name="example",
    )
    ubx.resource(User, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appstream_user with authentication_type, user_name,
enabled, first_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="first_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="send_email_notification" type="bool">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
