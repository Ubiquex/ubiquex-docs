---
title: "aws_app_stream_stack_user_association"
description: "`aws_app_stream_stack_user_association` grants a real AppStream 2.0 user access to an `aws_app_stream_stack`..."
---

`aws_app_stream_stack_user_association` grants a real AppStream 2.0 user access to an `aws_app_stream_stack`, the resource that actually authorizes that specific user to launch streaming sessions from it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/appstream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own stream stack user association"})

    		ubx.Resource(appstream.StackUserAssociation, "example", appstream.StackUserAssociationConfig{
    			AuthenticationType:    "example",
    			StackName:             "example-stack-name",
    			UserName:              "example-user-name",
    			SendEmailNotification: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StackUserAssociation } from "@ubx/sdk-aws/aws/appstream/stack_user_association";

    export default stack("example", () => {
      intent({ summary: "example own stream stack user association" });

      resource(StackUserAssociation, "example", {
        authenticationType: "example",
        stackName: "example-stack-name",
        userName: "example-user-name",
        sendEmailNotification: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appstream.stack_user_association import StackUserAssociation, StackUserAssociationConfig

    def describe():
        ubx.intent("example own stream stack user association")

        ubx.resource(StackUserAssociation, "example", StackUserAssociationConfig(
            authentication_type="example",
            stack_name="example-stack-name",
            user_name="example-user-name",
            send_email_notification=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_stream_stack_user_association, with authentication
    type example, stack name example-stack-name, user name
    example-user-name, send email notification true.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="authentication_type" type="string" required>
  The authentication type for the user who is associated with the stack. You must specify USERPOOL. Required.
</ResponseField>

<ResponseField name="send_email_notification" type="bool">
  Specifies whether a welcome email is sent to a user after the user is created in the user pool. Optional.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  The name of the stack that is associated with the user. Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  The name of the user who is associated with the stack. Required.
</ResponseField>
