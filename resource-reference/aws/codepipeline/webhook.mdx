---
title: "aws_codepipeline_webhook"
description: "Real, generated bindings for `aws_codepipeline_webhook`."
---

`aws_codepipeline_webhook` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codepipeline"

    cfg := codepipeline.WebhookConfig{
        Authentication: "example",
        Name: "example-name",
        TargetAction: "example",
        TargetPipeline: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(codepipeline.Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Webhook } from
      "jsr:@ubx/sdk-aws/aws/codepipeline/webhook";

    resource(Webhook, "example", {
      authentication: "example",
      name: "example",
      targetAction: "example",
      targetPipeline: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codepipeline import Webhook, WebhookConfig

    cfg = WebhookConfig(
        authentication="example",
        name="example",
        target_action="example",
        target_pipeline="example",
        tags={"key": "value"},
    )
    ubx.resource(Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codepipeline_webhook with authentication, name,
target_action, target_pipeline, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_pipeline" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
