---
title: "aws_codepipeline_custom_action_type"
description: "Real, generated bindings for `aws_codepipeline_custom_action_type`."
---

`aws_codepipeline_custom_action_type` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codepipeline"

    cfg := codepipeline.CustomActionTypeConfig{
        Category: "example",
        ProviderName: "example-provider-name",
        Version: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(codepipeline.CustomActionType, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CustomActionType } from
      "jsr:@ubx/sdk-aws/aws/codepipeline/custom_action_type";

    resource(CustomActionType, "example", {
      category: "example",
      providerName: "example",
      version: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codepipeline import (
        CustomActionType,
        CustomActionTypeConfig,
    )

    cfg = CustomActionTypeConfig(
        category="example",
        provider_name="example",
        version="example",
        tags={"key": "value"},
    )
    ubx.resource(CustomActionType, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codepipeline_custom_action_type with category,
provider_name, version, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="category" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
