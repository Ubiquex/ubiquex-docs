---
title: "aws_ebs_snapshot_copy"
description: "Real, generated bindings for `aws_ebs_snapshot_copy`."
---

`aws_ebs_snapshot_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ebs"

    cfg := ebs.SnapshotCopyConfig{
        SourceRegion: "example",
        SourceSnapshotId: "example",
        CompletionDurationMinutes: 1,
        Description: "example",
    }
    ubx.Resource(ebs.SnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SnapshotCopy } from
      "jsr:@ubx/sdk-aws/aws/ebs/snapshot_copy";

    resource(SnapshotCopy, "example", {
      sourceRegion: "example",
      sourceSnapshotId: "example",
      completionDurationMinutes: 1,
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ebs import SnapshotCopy, SnapshotCopyConfig

    cfg = SnapshotCopyConfig(
        source_region="example",
        source_snapshot_id="example",
        completion_duration_minutes=1,
        description="example",
    )
    ubx.resource(SnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ebs_snapshot_copy with source_region,
source_snapshot_id, completion_duration_minutes, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="completion_duration_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="encrypted" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="permanent_restore" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_snapshot_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="storage_tier" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="temporary_restore_days" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_encryption_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_tier" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="volume_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_size" type="number">
  Computed by AWS after `ship`.
</ResponseField>
