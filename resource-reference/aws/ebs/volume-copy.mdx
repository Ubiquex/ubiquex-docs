---
title: "aws_ebs_volume_copy"
description: "Real, generated bindings for `aws_ebs_volume_copy`."
---

`aws_ebs_volume_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ebs"

    cfg := ebs.VolumeCopyConfig{
        SourceVolumeId: "example",
        Iops: 1,
        Size: 1,
    }
    ubx.Resource(ebs.VolumeCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VolumeCopy } from
      "jsr:@ubx/sdk-aws/aws/ebs/volume_copy";

    resource(VolumeCopy, "example", {
      sourceVolumeId: "example",
      iops: 1,
      size: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ebs import VolumeCopy, VolumeCopyConfig

    cfg = VolumeCopyConfig(
        source_volume_id="example",
        iops=1,
        size=1,
    )
    ubx.resource(VolumeCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ebs_volume_copy with source_volume_id, iops, size.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="iops" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_volume_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="throughput" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="volume_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="iops" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="throughput" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="volume_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
