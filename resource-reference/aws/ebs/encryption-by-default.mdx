---
title: "aws_ebs_encryption_by_default"
description: "Real, generated bindings for `aws_ebs_encryption_by_default`."
---

`aws_ebs_encryption_by_default` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ebs"

    cfg := ebs.EncryptionByDefaultConfig{
        Enabled: true,
    }
    ubx.Resource(ebs.EncryptionByDefault, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EncryptionByDefault } from
      "jsr:@ubx/sdk-aws/aws/ebs/encryption_by_default";

    resource(EncryptionByDefault, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ebs import (
        EncryptionByDefault,
        EncryptionByDefaultConfig,
    )

    cfg = EncryptionByDefaultConfig(
        enabled=True,
    )
    ubx.resource(EncryptionByDefault, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ebs_encryption_by_default with enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
