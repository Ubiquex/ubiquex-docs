---
title: "aws_ebs_default_kms_key"
description: "Real, generated bindings for `aws_ebs_default_kms_key`."
---

`aws_ebs_default_kms_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ebs"

    cfg := ebs.DefaultKmsKeyConfig{
        KeyArn: "example",
    }
    ubx.Resource(ebs.DefaultKmsKey, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DefaultKmsKey } from
      "jsr:@ubx/sdk-aws/aws/ebs/default_kms_key";

    resource(DefaultKmsKey, "example", {
      keyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ebs import DefaultKmsKey, DefaultKmsKeyConfig

    cfg = DefaultKmsKeyConfig(
        key_arn="example",
    )
    ubx.resource(DefaultKmsKey, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ebs_default_kms_key with key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
