---
title: "aws_ebs_snapshot_block_public_access"
description: "Real, generated bindings for `aws_ebs_snapshot_block_public_access`."
---

`aws_ebs_snapshot_block_public_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ebs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ebs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own snapshot block public access"})

    		ubx.Resource(ebs.SnapshotBlockPublicAccess, "ci-runner", ebs.SnapshotBlockPublicAccessConfig{
    			State: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SnapshotBlockPublicAccess } from "jsr:@ubx/sdk-aws/aws/ebs/snapshot_block_public_access";

    export default stack("payments", () => {
      intent({ summary: "payments own snapshot block public access" });

      resource(SnapshotBlockPublicAccess, "ci-runner", {
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ebs import SnapshotBlockPublicAccess, SnapshotBlockPublicAccessConfig

    def describe():
        ubx.intent("payments own snapshot block public access")

        ubx.resource(SnapshotBlockPublicAccess, "ci-runner", SnapshotBlockPublicAccessConfig(
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ebs_snapshot_block_public_access, with state example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
