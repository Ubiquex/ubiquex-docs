---
title: "aws_ebs_fast_snapshot_restore"
description: "Real, generated bindings for `aws_ebs_fast_snapshot_restore`."
---

`aws_ebs_fast_snapshot_restore` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ebs"

    cfg := ebs.FastSnapshotRestoreConfig{
        AvailabilityZone: "example",
        SnapshotId: "example",
    }
    ubx.Resource(ebs.FastSnapshotRestore, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FastSnapshotRestore } from
      "jsr:@ubx/sdk-aws/aws/ebs/fast_snapshot_restore";

    resource(FastSnapshotRestore, "example", {
      availabilityZone: "example",
      snapshotId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ebs import (
        FastSnapshotRestore,
        FastSnapshotRestoreConfig,
    )

    cfg = FastSnapshotRestoreConfig(
        availability_zone="example",
        snapshot_id="example",
    )
    ubx.resource(FastSnapshotRestore, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ebs_fast_snapshot_restore with availability_zone,
snapshot_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
