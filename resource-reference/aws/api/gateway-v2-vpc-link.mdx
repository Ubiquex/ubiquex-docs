---
title: "aws_api_gateway_v2_vpc_link"
description: "`aws_api_gateway_v2_vpc_link` lets an `aws_api_gateway_v2_api` integration reach a private resource inside a VPC (an internal load balancer..."
---

`aws_api_gateway_v2_vpc_link` lets an `aws_api_gateway_v2_api` integration reach a private resource inside a VPC (an internal load balancer, for instance) without that resource being exposed to the public internet, the v2 counterpart to a v1 REST API VPC link.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway v2 vpc link"})

    		ubx.Resource(api.GatewayV2VpcLink, "example", api.GatewayV2VpcLinkConfig{
    			Name:             "example",
    			SubnetIds:        []string{"example"},
    			SecurityGroupIds: []string{"example"},
    			Tags:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayV2VpcLink } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_v2_vpc_link.ts";

    export default stack("example", () => {
      intent({ summary: "example own gateway v2 vpc link" });

      resource(GatewayV2VpcLink, "example", {
        name: "example",
        subnetIds: ["example"],
        securityGroupIds: ["example"],
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayV2VpcLink, GatewayV2VpcLinkConfig

    def describe():
        ubx.intent("example own gateway v2 vpc link")

        ubx.resource(GatewayV2VpcLink, "example", GatewayV2VpcLinkConfig(
            name="example",
            subnet_ids=["example"],
            security_group_ids=["example"],
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_v2_vpc_link called "example", with subnet
    ids a set value, security group ids a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  The name of the VPC link. Required.
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  A list of security group IDs for the VPC link. Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  A list of subnet IDs to include in the VPC link. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  The collection of tags. Each tag element is associated with a given resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="vpc_link_id" type="string">
  The service-generated unique identifier (VpcLinkId) assigned to the VPC link when it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
