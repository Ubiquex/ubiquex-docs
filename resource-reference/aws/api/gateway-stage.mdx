---
title: "aws_api_gateway_stage"
description: "Real, generated bindings for `aws_api_gateway_stage`."
---

`aws_api_gateway_stage` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway stage"})

    		ubx.Resource(api.GatewayStage, "ci-runner", api.GatewayStageConfig{
    			DeploymentId:         "example",
    			RestApiId:            "example",
    			StageName:            "example-stage-name",
    			CacheClusterEnabled:  true,
    			CacheClusterSize:     "example",
    			ClientCertificateId:  "example",
    			Description:          "Managed by ubx.",
    			DocumentationVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayStage } from "jsr:@ubx/sdk-aws/aws/api/gateway_stage";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway stage" });

      resource(GatewayStage, "ci-runner", {
        deploymentId: "example",
        restApiId: "example",
        stageName: "example-stage-name",
        cacheClusterEnabled: true,
        cacheClusterSize: "example",
        clientCertificateId: "example",
        description: "Managed by ubx.",
        documentationVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayStage, GatewayStageConfig

    def describe():
        ubx.intent("payments own gateway stage")

        ubx.resource(GatewayStage, "ci-runner", GatewayStageConfig(
            deployment_id="example",
            rest_api_id="example",
            stage_name="example-stage-name",
            cache_cluster_enabled=True,
            cache_cluster_size="example",
            client_certificate_id="example",
            description="Managed by ubx.",
            documentation_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_stage, with deployment id example, rest
    api id example, stage name example-stage-name, cache cluster
    enabled true, cache cluster size example, client certificate id
    example, description Managed by ubx., documentation version
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cache_cluster_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="cache_cluster_size" type="string">
  Optional.
</ResponseField>

<ResponseField name="client_certificate_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="deployment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="documentation_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="stage_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="variables" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="xray_tracing_enabled" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="execution_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invoke_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="web_acl_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
