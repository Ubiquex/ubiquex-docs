---
title: "aws_api_gateway_deployment"
description: "Real, generated bindings for `aws_api_gateway_deployment`."
---

`aws_api_gateway_deployment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway deployment"})

    		ubx.Resource(api.GatewayDeployment, "ci-runner", api.GatewayDeploymentConfig{
    			RestApiId:   "example",
    			Description: "Managed by ubx.",
    			Triggers:    map[string]string{"managed-by": "ubx"},
    			Variables:   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayDeployment } from "jsr:@ubx/sdk-aws/aws/api/gateway_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway deployment" });

      resource(GatewayDeployment, "ci-runner", {
        restApiId: "example",
        description: "Managed by ubx.",
        triggers: { "managed-by": "ubx" },
        variables: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayDeployment, GatewayDeploymentConfig

    def describe():
        ubx.intent("payments own gateway deployment")

        ubx.resource(GatewayDeployment, "ci-runner", GatewayDeploymentConfig(
            rest_api_id="example",
            description="Managed by ubx.",
            triggers={"managed-by": "ubx"},
            variables={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_deployment, with rest api id example,
    description Managed by ubx., triggers a set value, variables a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="variables" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
