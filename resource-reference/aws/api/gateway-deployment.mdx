---
title: "aws_api_gateway_deployment"
description: "Real, generated bindings for `aws_api_gateway_deployment`."
---

`aws_api_gateway_deployment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/api"

    cfg := api.GatewayDeploymentConfig{
        RestApiId: "example",
        Description: "example",
        Triggers: map[string]string{"key": "value"},
    }
    ubx.Resource(api.GatewayDeployment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayDeployment } from
      "jsr:@ubx/sdk-aws/aws/api/gateway_deployment";

    resource(GatewayDeployment, "example", {
      restApiId: "example",
      description: "example",
      triggers: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.api import (
        GatewayDeployment,
        GatewayDeploymentConfig,
    )

    cfg = GatewayDeploymentConfig(
        rest_api_id="example",
        description="example",
        triggers={"key": "value"},
    )
    ubx.resource(GatewayDeployment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_api_gateway_deployment with rest_api_id,
description, triggers.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="variables" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
