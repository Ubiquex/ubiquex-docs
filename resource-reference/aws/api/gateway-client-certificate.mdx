---
title: "aws_api_gateway_client_certificate"
description: "`aws_api_gateway_client_certificate` generates a certificate an `aws_api_gateway_rest_api` presents to its own backend..."
---

`aws_api_gateway_client_certificate` generates a certificate an `aws_api_gateway_rest_api` presents to its own backend, so the backend can verify requests genuinely came through API Gateway rather than directly from the internet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway client certificate"})

    		ubx.Resource(api.GatewayClientCertificate, "example", api.GatewayClientCertificateConfig{
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayClientCertificate } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_client_certificate.ts";

    export default stack("example", () => {
      intent({ summary: "example own gateway client certificate" });

      resource(GatewayClientCertificate, "example", {
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayClientCertificate, GatewayClientCertificateConfig

    def describe():
        ubx.intent("example own gateway client certificate")

        ubx.resource(GatewayClientCertificate, "example", GatewayClientCertificateConfig(
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_client_certificate, with description
    Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A user-provided description for the client certificate, used to annotate or identify it. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Associates a list of key-value tag pairs with the API Gateway client certificate, enabling cost allocation, access control, and resource categorization. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of an individual tag assigned to the API Gateway client certificate, used for metadata and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="client_certificate_id" type="string">
  The unique identifier assigned by Amazon API Gateway to the client certificate, used to reference this certificate in other API Gateway resources (e.g., domain names) and returned automatically upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
