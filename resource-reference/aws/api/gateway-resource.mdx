---
title: "aws_api_gateway_resource"
description: "`aws_api_gateway_resource` defines one path segment in a REST API's URL tree, nested under an `aws_api_gateway_rest_api` (or under another resource)..."
---

`aws_api_gateway_resource` defines one path segment in a REST API's URL tree, nested under an `aws_api_gateway_rest_api` (or under another resource), with one or more `aws_api_gateway_method`s attached to actually handle requests at that path.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway resource"})

    		ubx.Resource(api.GatewayResource, "ci-runner", api.GatewayResourceConfig{
    			ParentId:  "example",
    			PathPart:  "example",
    			RestApiId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayResource } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway resource" });

      resource(GatewayResource, "ci-runner", {
        parentId: "example",
        pathPart: "example",
        restApiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayResource, GatewayResourceConfig

    def describe():
        ubx.intent("payments own gateway resource")

        ubx.resource(GatewayResource, "ci-runner", GatewayResourceConfig(
            parent_id="example",
            path_part="example",
            rest_api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_resource, with parent id example, path
    part example, rest api id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="parent_id" type="string" required>
  The identifier of the parent Resource in this REST API's resource hierarchy, determining the path of this resource relative to the API's root resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="path_part" type="string" required>
  The URL path segment (e.g., 'users' or a path parameter like '&#123;userId&#125;') that defines this resource's location in the API's URL hierarchy, appended to the parent resource's path to form the full resource path. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The ID of the parent RestApi in API Gateway to which this resource belongs. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="resource_id" type="string">
  The unique identifier assigned to this API Gateway resource by the service, used to reference the resource in method and integration configurations within the same REST API. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
