---
title: "aws_api_gateway_domain_name"
description: "Real, generated bindings for `aws_api_gateway_domain_name`."
---

`aws_api_gateway_domain_name` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway domain name"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(api.GatewayDomainName, "ci-runner", api.GatewayDomainNameConfig{
    			DomainName:            "example-domain-name",
    			CertificateArn:        "arn:aws:iam::123456789012:policy/ci",
    			CertificateBody:       "example",
    			CertificateChain:      "example",
    			CertificateName:       "example-certificate-name",
    			CertificatePrivateKey: "example",
    			EndpointAccessMode:    "example",
    			Policy:                string(accessPolicy),
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayDomainName } from "jsr:@ubx/sdk-aws/aws/api/gateway_domain_name";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway domain name" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(GatewayDomainName, "ci-runner", {
        domainName: "example-domain-name",
        certificateArn: "arn:aws:iam::123456789012:policy/ci",
        certificateBody: "example",
        certificateChain: "example",
        certificateName: "example-certificate-name",
        certificatePrivateKey: "example",
        endpointAccessMode: "example",
        policy: accessPolicy,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayDomainName, GatewayDomainNameConfig

    def describe():
        ubx.intent("payments own gateway domain name")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(GatewayDomainName, "ci-runner", GatewayDomainNameConfig(
            domain_name="example-domain-name",
            certificate_arn="arn:aws:iam::123456789012:policy/ci",
            certificate_body="example",
            certificate_chain="example",
            certificate_name="example-certificate-name",
            certificate_private_key="example",
            endpoint_access_mode="example",
            policy=access_policy,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_domain_name, with domain name
    example-domain-name, certificate arn
    arn:aws:iam::123456789012:policy/ci, certificate body example,
    certificate chain example, certificate name
    example-certificate-name, certificate private key example,
    endpoint access mode example, policy a real, valid value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_body" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_private_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="endpoint_access_mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ownership_verification_certificate_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regional_certificate_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="regional_certificate_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="routing_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_upload_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloudfront_domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloudfront_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ownership_verification_certificate_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regional_domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="regional_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="routing_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
