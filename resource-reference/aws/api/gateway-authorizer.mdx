---
title: "aws_api_gateway_authorizer"
description: "Real, generated bindings for `aws_api_gateway_authorizer`."
---

`aws_api_gateway_authorizer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway authorizer"})

    		ubx.Resource(api.GatewayAuthorizer, "ci-runner", api.GatewayAuthorizerConfig{
    			Name:                         "ci-runner",
    			RestApiId:                    "example",
    			AuthorizerCredentials:        "example",
    			AuthorizerResultTtlInSeconds: 7200,
    			AuthorizerUri:                "example",
    			IdentitySource:               "example",
    			IdentityValidationExpression: "example",
    			ProviderArns:                 []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayAuthorizer } from "jsr:@ubx/sdk-aws/aws/api/gateway_authorizer";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway authorizer" });

      resource(GatewayAuthorizer, "ci-runner", {
        name: "ci-runner",
        restApiId: "example",
        authorizerCredentials: "example",
        authorizerResultTtlInSeconds: 7200,
        authorizerUri: "example",
        identitySource: "example",
        identityValidationExpression: "example",
        providerArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayAuthorizer, GatewayAuthorizerConfig

    def describe():
        ubx.intent("payments own gateway authorizer")

        ubx.resource(GatewayAuthorizer, "ci-runner", GatewayAuthorizerConfig(
            name="ci-runner",
            rest_api_id="example",
            authorizer_credentials="example",
            authorizer_result_ttl_in_seconds=7200,
            authorizer_uri="example",
            identity_source="example",
            identity_validation_expression="example",
            provider_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_authorizer called "ci-runner", with rest
    api id example, authorizer credentials example, authorizer result
    ttl in seconds 7200, authorizer uri example, identity source
    example, identity validation expression example, provider arns a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorizer_credentials" type="string">
  Optional.
</ResponseField>

<ResponseField name="authorizer_result_ttl_in_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="authorizer_uri" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_source" type="string">
  Optional.
</ResponseField>

<ResponseField name="identity_validation_expression" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
