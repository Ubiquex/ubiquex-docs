---
title: "aws_api_gateway_domain_name_access_association"
description: "`aws_api_gateway_domain_name_access_association` links a real VPC endpoint to an `aws_api_gateway_domain_name`..."
---

`aws_api_gateway_domain_name_access_association` links a real VPC endpoint to an `aws_api_gateway_domain_name`, restricting that private custom domain to be reachable only through the associated endpoint rather than from anywhere with network access.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway domain name access association"})

    		ubx.Resource(apigateway.DomainNameAccessAssociation, "example", apigateway.DomainNameAccessAssociationConfig{
    			AccessAssociationSource:     "example",
    			AccessAssociationSourceType: "example",
    			DomainNameArn:               "arn:aws:iam::123456789012:policy/example",
    			Tags:                        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainNameAccessAssociation } from "@ubx/sdk-aws/aws/apigateway/domain_name_access_association";

    export default stack("example", () => {
      intent({ summary: "example own gateway domain name access association" });

      resource(DomainNameAccessAssociation, "example", {
        accessAssociationSource: "example",
        accessAssociationSourceType: "example",
        domainNameArn: "arn:aws:iam::123456789012:policy/example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigateway.domain_name_access_association import DomainNameAccessAssociation, DomainNameAccessAssociationConfig

    def describe():
        ubx.intent("example own gateway domain name access association")

        ubx.resource(DomainNameAccessAssociation, "example", DomainNameAccessAssociationConfig(
            access_association_source="example",
            access_association_source_type="example",
            domain_name_arn="arn:aws:iam::123456789012:policy/example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_domain_name_access_association, with
    access association source example, access association source type
    example, domain name arn
    arn:aws:iam::123456789012:policy/example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_association_source" type="string" required>
  The source of the domain name access association resource. Required.
</ResponseField>

<ResponseField name="access_association_source_type" type="string" required>
  The source type of the domain name access association resource. Required.
</ResponseField>

<ResponseField name="domain_name_arn" type="string" required>
  The amazon resource name (ARN) of the domain name resource. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of arbitrary tags (key-value pairs) to associate with the domainname access association. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="domain_name_access_association_arn" type="string">
  The amazon resource name (ARN) of the domain name access association resource. Computed by AWS after `ship`.
</ResponseField>
