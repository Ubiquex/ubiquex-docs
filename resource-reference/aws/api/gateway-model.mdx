---
title: "aws_api_gateway_model"
description: "Real, generated bindings for `aws_api_gateway_model`."
---

`aws_api_gateway_model` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway model"})

    		ubx.Resource(api.GatewayModel, "ci-runner", api.GatewayModelConfig{
    			ContentType: "example",
    			Name:        "ci-runner",
    			RestApiId:   "example",
    			Description: "Managed by ubx.",
    			Schema:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayModel } from "jsr:@ubx/sdk-aws/aws/api/gateway_model";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway model" });

      resource(GatewayModel, "ci-runner", {
        contentType: "example",
        name: "ci-runner",
        restApiId: "example",
        description: "Managed by ubx.",
        schema: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayModel, GatewayModelConfig

    def describe():
        ubx.intent("payments own gateway model")

        ubx.resource(GatewayModel, "ci-runner", GatewayModelConfig(
            content_type="example",
            name="ci-runner",
            rest_api_id="example",
            description="Managed by ubx.",
            schema="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_model called "ci-runner", with content
    type example, rest api id example, description Managed by ubx.,
    schema example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schema" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
