---
title: "aws_api_gateway_model"
description: "`aws_api_gateway_model` is a JSON Schema definition attached to an `aws_api_gateway_rest_api`..."
---

`aws_api_gateway_model` is a JSON Schema definition attached to an `aws_api_gateway_rest_api`, used by request validators and documentation to describe the real shape of a request or response body rather than leaving it untyped.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway model"})

    		ubx.Resource(api.GatewayModel, "example", api.GatewayModelConfig{
    			RestApiId:   "example",
    			Name:        "example",
    			ContentType: "example",
    			Description: "Managed by ubx.",
    			Schema:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayModel } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_model.ts";

    export default stack("example", () => {
      intent({ summary: "example own gateway model" });

      resource(GatewayModel, "example", {
        restApiId: "example",
        name: "example",
        contentType: "example",
        description: "Managed by ubx.",
        schema: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayModel, GatewayModelConfig

    def describe():
        ubx.intent("example own gateway model")

        ubx.resource(GatewayModel, "example", GatewayModelConfig(
            rest_api_id="example",
            name="example",
            content_type="example",
            description="Managed by ubx.",
            schema="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_model called "example", with rest api id
    example, content type example, description Managed by ubx.,
    schema example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="content_type" type="string">
  The content type (e.g., application/json) for the API Gateway model, used to associate the model with a specific media type in API requests and responses. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  This field specifies a human-readable description of the API Gateway model, used to document the model's purpose and assist in management within the API definition. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  A name for the model. If you don't specify a name, CFN generates a unique physical ID and uses that ID for the model name. For more information, see [Name Type](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-name.html). If you specify a name, you cannot perform updates that require replacement of this resource. You can perform updates that require no or some interruption. If you must replace the resource, specify a new name. Optional.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The ID of the RestApi that this model belongs to, linking the model to its parent API Gateway REST API. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="schema" type="any">
  Defines the JSON schema (as a string) that describes the data structure for the API Gateway model, used for request/response validation. Optional. **(AI-inferred)**
</ResponseField>
