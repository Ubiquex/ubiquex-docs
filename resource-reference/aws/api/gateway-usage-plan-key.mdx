---
title: "aws_api_gateway_usage_plan_key"
description: "Real, generated bindings for `aws_api_gateway_usage_plan_key`."
---

`aws_api_gateway_usage_plan_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway usage plan key"})

    		ubx.Resource(api.GatewayUsagePlanKey, "ci-runner", api.GatewayUsagePlanKeyConfig{
    			KeyId:       "example",
    			KeyType:     "example",
    			UsagePlanId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayUsagePlanKey } from "jsr:@ubx/sdk-aws/aws/api/gateway_usage_plan_key";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway usage plan key" });

      resource(GatewayUsagePlanKey, "ci-runner", {
        keyId: "example",
        keyType: "example",
        usagePlanId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayUsagePlanKey, GatewayUsagePlanKeyConfig

    def describe():
        ubx.intent("payments own gateway usage plan key")

        ubx.resource(GatewayUsagePlanKey, "ci-runner", GatewayUsagePlanKeyConfig(
            key_id="example",
            key_type="example",
            usage_plan_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_usage_plan_key, with key id example, key
    type example, usage plan id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="key_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="usage_plan_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string">
  Computed by AWS after `ship`.
</ResponseField>
