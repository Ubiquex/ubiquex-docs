---
title: "aws_api_gateway_usage_plan"
description: "Real, generated bindings for `aws_api_gateway_usage_plan`."
---

`aws_api_gateway_usage_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/api"

    cfg := api.GatewayUsagePlanConfig{
        Name: "example-name",
        Description: "example",
        ProductCode: "example",
    }
    ubx.Resource(api.GatewayUsagePlan, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayUsagePlan } from
      "jsr:@ubx/sdk-aws/aws/api/gateway_usage_plan";

    resource(GatewayUsagePlan, "example", {
      name: "example",
      description: "example",
      productCode: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.api import (
        GatewayUsagePlan,
        GatewayUsagePlanConfig,
    )

    cfg = GatewayUsagePlanConfig(
        name="example",
        description="example",
        product_code="example",
    )
    ubx.resource(GatewayUsagePlan, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_api_gateway_usage_plan with name, description,
product_code.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="product_code" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
