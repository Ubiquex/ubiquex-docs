---
title: "aws_api_gateway_v2_portal_product"
description: "`aws_api_gateway_v2_portal_product` publishes a real API..."
---

`aws_api_gateway_v2_portal_product` publishes a real API, or a curated group of operations from it, as a self-service product in an API Gateway developer portal, so external or internal developers can discover, subscribe to, and get credentials for it without a support ticket.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway v2 portal product"})

    		ubx.Resource(api.GatewayV2PortalProduct, "example", api.GatewayV2PortalProductConfig{
    			DisplayName: "example-display-name",
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayV2PortalProduct } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_v2_portal_product.ts";

    export default stack("example", () => {
      intent({ summary: "example own gateway v2 portal product" });

      resource(GatewayV2PortalProduct, "example", {
        displayName: "example-display-name",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayV2PortalProduct, GatewayV2PortalProductConfig

    def describe():
        ubx.intent("example own gateway v2 portal product")

        ubx.resource(GatewayV2PortalProduct, "example", GatewayV2PortalProductConfig(
            display_name="example-display-name",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_v2_portal_product, with display name
    example-display-name, description Managed by ubx., tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description of the portal product. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The name of the portal product as it appears in a published portal. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The collection of tags associated with the portal product. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="last_modified" type="string">
  The timestamp when the portal product was last modified. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="portal_product_arn" type="string">
  The ARN of the portal product. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="portal_product_id" type="string">
  The portal product identifier. Computed by AWS after `ship`.
</ResponseField>
