---
title: "aws_api_gateway_request_validator"
description: "Real, generated bindings for `aws_api_gateway_request_validator`."
---

`aws_api_gateway_request_validator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway request validator"})

    		ubx.Resource(api.GatewayRequestValidator, "ci-runner", api.GatewayRequestValidatorConfig{
    			Name:                      "ci-runner",
    			RestApiId:                 "example",
    			ValidateRequestBody:       true,
    			ValidateRequestParameters: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayRequestValidator } from "jsr:@ubx/sdk-aws/aws/api/gateway_request_validator";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway request validator" });

      resource(GatewayRequestValidator, "ci-runner", {
        name: "ci-runner",
        restApiId: "example",
        validateRequestBody: true,
        validateRequestParameters: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayRequestValidator, GatewayRequestValidatorConfig

    def describe():
        ubx.intent("payments own gateway request validator")

        ubx.resource(GatewayRequestValidator, "ci-runner", GatewayRequestValidatorConfig(
            name="ci-runner",
            rest_api_id="example",
            validate_request_body=True,
            validate_request_parameters=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_request_validator called "ci-runner", with
    rest api id example, validate request body true, validate request
    parameters true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="validate_request_body" type="bool">
  Optional.
</ResponseField>

<ResponseField name="validate_request_parameters" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
