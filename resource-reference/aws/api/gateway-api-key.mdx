---
title: "aws_api_gateway_api_key"
description: "Real, generated bindings for `aws_api_gateway_api_key`."
---

`aws_api_gateway_api_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway api key"})

    		ubx.Resource(api.GatewayApiKey, "ci-runner", api.GatewayApiKeyConfig{
    			Name:        "ci-runner",
    			CustomerId:  "example",
    			Description: "Managed by ubx.",
    			Enabled:     true,
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayApiKey } from "jsr:@ubx/sdk-aws/aws/api/gateway_api_key";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway api key" });

      resource(GatewayApiKey, "ci-runner", {
        name: "ci-runner",
        customerId: "example",
        description: "Managed by ubx.",
        enabled: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayApiKey, GatewayApiKeyConfig

    def describe():
        ubx.intent("payments own gateway api key")

        ubx.resource(GatewayApiKey, "ci-runner", GatewayApiKeyConfig(
            name="ci-runner",
            customer_id="example",
            description="Managed by ubx.",
            enabled=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_api_key called "ci-runner", with customer
    id example, description Managed by ubx., enabled true, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
