---
title: "aws_api_gateway_vpc_link"
description: "Real, generated bindings for `aws_api_gateway_vpc_link`."
---

`aws_api_gateway_vpc_link` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/api"

    cfg := api.GatewayVpcLinkConfig{
        Name: "example-name",
        TargetArns: []string{"example"},
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(api.GatewayVpcLink, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayVpcLink } from
      "jsr:@ubx/sdk-aws/aws/api/gateway_vpc_link";

    resource(GatewayVpcLink, "example", {
      name: "example",
      targetArns: ["example"],
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.api import GatewayVpcLink, GatewayVpcLinkConfig

    cfg = GatewayVpcLinkConfig(
        name="example",
        target_arns=["example"],
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(GatewayVpcLink, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_api_gateway_vpc_link with name, target_arns,
description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_arns" type="list(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
