---
title: "aws_api_gateway_v2_deployment"
description: "`aws_api_gateway_v2_deployment` is a snapshot of an `aws_api_gateway_v2_api`'s routes and integrations at a point in time..."
---

`aws_api_gateway_v2_deployment` is a snapshot of an `aws_api_gateway_v2_api`'s routes and integrations at a point in time, the v2 counterpart to a v1 REST API deployment, made reachable once an `aws_api_gateway_v2_stage` points to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway v2 deployment"})

    		ubx.Resource(api.GatewayV2Deployment, "example", api.GatewayV2DeploymentConfig{
    			ApiId:       "example",
    			Description: "Managed by ubx.",
    			StageName:   "example-stage-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayV2Deployment } from "./local-sdk/aws/sdk/typescript/aws/api/gateway_v2_deployment.ts";

    export default stack("example", () => {
      intent({ summary: "example own gateway v2 deployment" });

      resource(GatewayV2Deployment, "example", {
        apiId: "example",
        description: "Managed by ubx.",
        stageName: "example-stage-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayV2Deployment, GatewayV2DeploymentConfig

    def describe():
        ubx.intent("example own gateway v2 deployment")

        ubx.resource(GatewayV2Deployment, "example", GatewayV2DeploymentConfig(
            api_id="example",
            description="Managed by ubx.",
            stage_name="example-stage-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_v2_deployment, with api id example,
    description Managed by ubx., stage name example-stage-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The API identifier. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description for the deployment resource. Optional.
</ResponseField>

<ResponseField name="stage_name" type="string">
  The name of an existing stage to associate with the deployment. Optional.
</ResponseField>

## Output properties

<ResponseField name="deployment_id" type="string">
  The unique identifier assigned by AWS to this API Gateway v2 deployment. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
