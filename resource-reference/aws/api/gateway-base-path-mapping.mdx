---
title: "aws_api_gateway_base_path_mapping"
description: "Real, generated bindings for `aws_api_gateway_base_path_mapping`."
---

`aws_api_gateway_base_path_mapping` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway base path mapping"})

    		ubx.Resource(api.GatewayBasePathMapping, "ci-runner", api.GatewayBasePathMappingConfig{
    			ApiId:        "example",
    			DomainName:   "example-domain-name",
    			BasePath:     "example",
    			DomainNameId: "example",
    			StageName:    "example-stage-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayBasePathMapping } from "jsr:@ubx/sdk-aws/aws/api/gateway_base_path_mapping";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway base path mapping" });

      resource(GatewayBasePathMapping, "ci-runner", {
        apiId: "example",
        domainName: "example-domain-name",
        basePath: "example",
        domainNameId: "example",
        stageName: "example-stage-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayBasePathMapping, GatewayBasePathMappingConfig

    def describe():
        ubx.intent("payments own gateway base path mapping")

        ubx.resource(GatewayBasePathMapping, "ci-runner", GatewayBasePathMappingConfig(
            api_id="example",
            domain_name="example-domain-name",
            base_path="example",
            domain_name_id="example",
            stage_name="example-stage-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_base_path_mapping, with api id example,
    domain name example-domain-name, base path example, domain name
    id example, stage name example-stage-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="base_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stage_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
