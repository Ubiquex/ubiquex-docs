---
title: "aws_api_gateway_method_settings"
description: "Real, generated bindings for `aws_api_gateway_method_settings`."
---

`aws_api_gateway_method_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway method settings"})

    		ubx.Resource(api.GatewayMethodSettings, "ci-runner", api.GatewayMethodSettingsConfig{
    			MethodPath: "example",
    			RestApiId:  "example",
    			StageName:  "example-stage-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayMethodSettings } from "jsr:@ubx/sdk-aws/aws/api/gateway_method_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway method settings" });

      resource(GatewayMethodSettings, "ci-runner", {
        methodPath: "example",
        restApiId: "example",
        stageName: "example-stage-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.api import GatewayMethodSettings, GatewayMethodSettingsConfig

    def describe():
        ubx.intent("payments own gateway method settings")

        ubx.resource(GatewayMethodSettings, "ci-runner", GatewayMethodSettingsConfig(
            method_path="example",
            rest_api_id="example",
            stage_name="example-stage-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_method_settings, with method path example,
    rest api id example, stage name example-stage-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="method_path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="stage_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
