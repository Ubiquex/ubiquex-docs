---
title: "aws_codecommit_trigger"
description: "Real, generated bindings for `aws_codecommit_trigger`."
---

`aws_codecommit_trigger` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trigger"})

    		ubx.Resource(codecommit.Trigger, "ci-runner", codecommit.TriggerConfig{
    			RepositoryName: "example-repository-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Trigger } from "jsr:@ubx/sdk-aws/aws/codecommit/trigger";

    export default stack("payments", () => {
      intent({ summary: "payments own trigger" });

      resource(Trigger, "ci-runner", {
        repositoryName: "example-repository-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codecommit import Trigger, TriggerConfig

    def describe():
        ubx.intent("payments own trigger")

        ubx.resource(Trigger, "ci-runner", TriggerConfig(
            repository_name="example-repository-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codecommit_trigger, with repository name
    example-repository-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="configuration_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
