---
title: "aws_codecommit_trigger"
description: "Real, generated bindings for `aws_codecommit_trigger`."
---

`aws_codecommit_trigger` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codecommit"

    cfg := codecommit.TriggerConfig{
        RepositoryName: "example-repository-name",
    }
    ubx.Resource(codecommit.Trigger, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Trigger } from
      "jsr:@ubx/sdk-aws/aws/codecommit/trigger";

    resource(Trigger, "example", {
      repositoryName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codecommit import Trigger, TriggerConfig

    cfg = TriggerConfig(
        repository_name="example",
    )
    ubx.resource(Trigger, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codecommit_trigger with repository_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="configuration_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
