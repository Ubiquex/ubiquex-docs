---
title: "aws_codecommit_approval_rule_template"
description: "Real, generated bindings for `aws_codecommit_approval_rule_template`."
---

`aws_codecommit_approval_rule_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own approval rule template"})

    		ubx.Resource(codecommit.ApprovalRuleTemplate, "ci-runner", codecommit.ApprovalRuleTemplateConfig{
    			Content:     "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApprovalRuleTemplate } from "jsr:@ubx/sdk-aws/aws/codecommit/approval_rule_template";

    export default stack("payments", () => {
      intent({ summary: "payments own approval rule template" });

      resource(ApprovalRuleTemplate, "ci-runner", {
        content: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codecommit import ApprovalRuleTemplate, ApprovalRuleTemplateConfig

    def describe():
        ubx.intent("payments own approval rule template")

        ubx.resource(ApprovalRuleTemplate, "ci-runner", ApprovalRuleTemplateConfig(
            content="example",
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codecommit_approval_rule_template called "ci-runner",
    with content example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="approval_rule_template_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_user" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_content_sha256" type="string">
  Computed by AWS after `ship`.
</ResponseField>
