---
title: "aws_codecommit_approval_rule_template_association"
description: "Real, generated bindings for `aws_codecommit_approval_rule_template_association`."
---

`aws_codecommit_approval_rule_template_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own approval rule template association"})

    		ubx.Resource(codecommit.ApprovalRuleTemplateAssociation, "ci-runner", codecommit.ApprovalRuleTemplateAssociationConfig{
    			ApprovalRuleTemplateName: "example-approval-rule-template-name",
    			RepositoryName:           "example-repository-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApprovalRuleTemplateAssociation } from "jsr:@ubx/sdk-aws/aws/codecommit/approval_rule_template_association";

    export default stack("payments", () => {
      intent({ summary: "payments own approval rule template association" });

      resource(ApprovalRuleTemplateAssociation, "ci-runner", {
        approvalRuleTemplateName: "example-approval-rule-template-name",
        repositoryName: "example-repository-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codecommit import ApprovalRuleTemplateAssociation, ApprovalRuleTemplateAssociationConfig

    def describe():
        ubx.intent("payments own approval rule template association")

        ubx.resource(ApprovalRuleTemplateAssociation, "ci-runner", ApprovalRuleTemplateAssociationConfig(
            approval_rule_template_name="example-approval-rule-template-name",
            repository_name="example-repository-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codecommit_approval_rule_template_association, with
    approval rule template name example-approval-rule-template-name,
    repository name example-repository-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="approval_rule_template_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
