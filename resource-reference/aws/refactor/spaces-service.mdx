---
title: "aws_refactor_spaces_service"
description: "`aws_refactor_spaces_service` registers a real extracted microservice..."
---

`aws_refactor_spaces_service` registers a real extracted microservice, and the routing rule that sends its share of an `aws_refactor_spaces_application`'s traffic to it, the mechanism Refactor Spaces uses to migrate traffic incrementally rather than all at once. Like the rest of Refactor Spaces, this closed to new customers November 7, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	refactor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/refactor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own spaces service"})

    		ubx.Resource(refactor.SpacesService, "ci-runner", refactor.SpacesServiceConfig{
    			ApplicationIdentifier: "example",
    			EndpointType:          "example",
    			EnvironmentIdentifier: "example",
    			Name:                  "ci-runner",
    			Description:           "Managed by ubx.",
    			LambdaEndpoint:        map[string]any{"arn": "example"},
    			Tags:                  []map[string]any{{"key": "example"}},
    			UrlEndpoint:           map[string]any{"url": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SpacesService } from "./local-sdk/aws/sdk/typescript/aws/refactor/spaces_service.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own spaces service" });

      resource(SpacesService, "ci-runner", {
        applicationIdentifier: "example",
        endpointType: "example",
        environmentIdentifier: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        lambdaEndpoint: { arn: "example" },
        tags: [{ key: "example" }],
        urlEndpoint: { url: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.refactor import SpacesService, SpacesServiceConfig

    def describe():
        ubx.intent("payments own spaces service")

        ubx.resource(SpacesService, "ci-runner", SpacesServiceConfig(
            application_identifier="example",
            endpoint_type="example",
            environment_identifier="example",
            name="ci-runner",
            description="Managed by ubx.",
            lambda_endpoint={"arn": "example"},
            tags=[{"key": "example"}],
            url_endpoint={"url": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_refactor_spaces_service called "ci-runner", with
    application identifier example, endpoint type example,
    environment identifier example, description Managed by ubx.,
    lambda endpoint a set value, tags a set value, url endpoint a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_identifier" type="string" required>
  The unique identifier of the Refactor Spaces application to which this service belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A user-defined description for the AWS Refactor Spaces service, used to identify the service within the environment. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="endpoint_type" type="string" required>
  Specifies the type of endpoint the AWS Refactor Spaces service uses, with valid values of API_GATEWAY, AWS_LAMBDA, or URL. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="environment_identifier" type="string" required>
  The unique identifier of the AWS Refactor Spaces environment in which this service is created. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="lambda_endpoint" type="object">
  The Lambda endpoint configuration for the service, which specifies the ARN of the Lambda function that the Refactor Spaces service uses as its endpoint. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="arn" type="string" required>
      The ARN of the AWS Lambda function that serves as the endpoint for this Refactor Spaces service. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the Refactor Spaces service, which is a user-defined identifier for the service within its environment. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Metadata that you can assign to help organize the frameworks that you create. Each tag is a key-value pair. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Refactor Spaces service, used to organize, identify, and manage the service within your AWS environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="url_endpoint" type="object">
  The configuration for the service's URL endpoint, which specifies the HTTP/HTTPS URL that the service exposes, used when the endpoint type is URL. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="health_url" type="string">
      The optional health check URL for a URL-type service, which AWS Migration Hub Refactor Spaces probes to determine whether the service is healthy before routing traffic to it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL that defines the endpoint address of the AWS Refactor Spaces service, which is the network address at which the service is exposed when using a URL endpoint. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Specifies the ID of the VPC in which the AWS Refactor Spaces service will be deployed, defining the network environment for its endpoints and resources. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Refactor Spaces service across AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_identifier" type="string">
  The unique identifier assigned to the service by AWS Refactor Spaces, used to reference the service within the application and environment. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
