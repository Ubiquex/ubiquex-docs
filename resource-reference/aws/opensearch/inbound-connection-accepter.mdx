---
title: "aws_opensearch_inbound_connection_accepter"
description: "Real, generated bindings for `aws_opensearch_inbound_connection_accepter`."
---

`aws_opensearch_inbound_connection_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/opensearch"

    cfg := opensearch.InboundConnectionAccepterConfig{
        ConnectionId: "example",
    }
    ubx.Resource(
        opensearch.InboundConnectionAccepter,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InboundConnectionAccepter } from
      "jsr:@ubx/sdk-aws/aws/opensearch/inbound_connection_accepter";

    resource(InboundConnectionAccepter, "example", {
      connectionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import (
        InboundConnectionAccepter,
        InboundConnectionAccepterConfig,
    )

    cfg = InboundConnectionAccepterConfig(
        connection_id="example",
    )
    ubx.resource(InboundConnectionAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_inbound_connection_accepter with
connection_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="connection_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
