---
title: "aws_opensearch_inbound_connection_accepter"
description: "Real, generated bindings for `aws_opensearch_inbound_connection_accepter`."
---

`aws_opensearch_inbound_connection_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own inbound connection accepter"})

    		ubx.Resource(opensearch.InboundConnectionAccepter, "ci-runner", opensearch.InboundConnectionAccepterConfig{
    			ConnectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InboundConnectionAccepter } from "jsr:@ubx/sdk-aws/aws/opensearch/inbound_connection_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own inbound connection accepter" });

      resource(InboundConnectionAccepter, "ci-runner", {
        connectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import InboundConnectionAccepter, InboundConnectionAccepterConfig

    def describe():
        ubx.intent("payments own inbound connection accepter")

        ubx.resource(InboundConnectionAccepter, "ci-runner", InboundConnectionAccepterConfig(
            connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_inbound_connection_accepter, with
    connection id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="connection_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
