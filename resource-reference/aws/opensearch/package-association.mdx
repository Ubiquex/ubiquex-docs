---
title: "aws_opensearch_package_association"
description: "Real, generated bindings for `aws_opensearch_package_association`."
---

`aws_opensearch_package_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/opensearch"

    cfg := opensearch.PackageAssociationConfig{
        DomainName: "example-domain-name",
        PackageId: "example",
    }
    ubx.Resource(opensearch.PackageAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PackageAssociation } from
      "jsr:@ubx/sdk-aws/aws/opensearch/package_association";

    resource(PackageAssociation, "example", {
      domainName: "example",
      packageId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import (
        PackageAssociation,
        PackageAssociationConfig,
    )

    cfg = PackageAssociationConfig(
        domain_name="example",
        package_id="example",
    )
    ubx.resource(PackageAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_package_association with domain_name,
package_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reference_path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
