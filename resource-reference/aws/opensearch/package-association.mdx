---
title: "aws_opensearch_package_association"
description: "Real, generated bindings for `aws_opensearch_package_association`."
---

`aws_opensearch_package_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own package association"})

    		ubx.Resource(opensearch.PackageAssociation, "ci-runner", opensearch.PackageAssociationConfig{
    			DomainName: "example-domain-name",
    			PackageId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PackageAssociation } from "jsr:@ubx/sdk-aws/aws/opensearch/package_association";

    export default stack("payments", () => {
      intent({ summary: "payments own package association" });

      resource(PackageAssociation, "ci-runner", {
        domainName: "example-domain-name",
        packageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import PackageAssociation, PackageAssociationConfig

    def describe():
        ubx.intent("payments own package association")

        ubx.resource(PackageAssociation, "ci-runner", PackageAssociationConfig(
            domain_name="example-domain-name",
            package_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_package_association, with domain name
    example-domain-name, package id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reference_path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
