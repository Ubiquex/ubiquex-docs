---
title: "aws_opensearch_outbound_connection"
description: "Real, generated bindings for `aws_opensearch_outbound_connection`."
---

`aws_opensearch_outbound_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own outbound connection"})

    		ubx.Resource(opensearch.OutboundConnection, "ci-runner", opensearch.OutboundConnectionConfig{
    			ConnectionAlias:  "example",
    			AcceptConnection: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OutboundConnection } from "jsr:@ubx/sdk-aws/aws/opensearch/outbound_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own outbound connection" });

      resource(OutboundConnection, "ci-runner", {
        connectionAlias: "example",
        acceptConnection: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import OutboundConnection, OutboundConnectionConfig

    def describe():
        ubx.intent("payments own outbound connection")

        ubx.resource(OutboundConnection, "ci-runner", OutboundConnectionConfig(
            connection_alias="example",
            accept_connection=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_outbound_connection, with connection alias
    example, accept connection true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_connection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="connection_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="connection_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="connection_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="connection_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
