---
title: "aws_opensearch_domain_policy"
description: "Real, generated bindings for `aws_opensearch_domain_policy`."
---

`aws_opensearch_domain_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"

    cfg := opensearch.DomainPolicyConfig{
        AccessPolicies: "example",
        DomainName: "example-domain-name",
    }
    ubx.Resource(opensearch.DomainPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainPolicy } from
      "jsr:@ubx/sdk-aws/aws/opensearch/domain_policy";

    resource(DomainPolicy, "example", {
      accessPolicies: "example",
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import DomainPolicy, DomainPolicyConfig

    cfg = DomainPolicyConfig(
        access_policies="example",
        domain_name="example",
    )
    ubx.resource(DomainPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_domain_policy with access_policies,
domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policies" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
