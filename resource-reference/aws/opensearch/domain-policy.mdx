---
title: "aws_opensearch_domain_policy"
description: "Real, generated bindings for `aws_opensearch_domain_policy`."
---

`aws_opensearch_domain_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain policy"})

    		ubx.Resource(opensearch.DomainPolicy, "ci-runner", opensearch.DomainPolicyConfig{
    			AccessPolicies: "example",
    			DomainName:     "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainPolicy } from "jsr:@ubx/sdk-aws/aws/opensearch/domain_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own domain policy" });

      resource(DomainPolicy, "ci-runner", {
        accessPolicies: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import DomainPolicy, DomainPolicyConfig

    def describe():
        ubx.intent("payments own domain policy")

        ubx.resource(DomainPolicy, "ci-runner", DomainPolicyConfig(
            access_policies="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_domain_policy, with access policies
    example, domain name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policies" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
