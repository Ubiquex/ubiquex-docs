---
title: "aws_opensearch_authorize_vpc_endpoint_access"
description: "Real, generated bindings for `aws_opensearch_authorize_vpc_endpoint_access`."
---

`aws_opensearch_authorize_vpc_endpoint_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/opensearch"

    cfg := opensearch.AuthorizeVpcEndpointAccessConfig{
        Account: "example",
        DomainName: "example-domain-name",
    }
    ubx.Resource(
        opensearch.AuthorizeVpcEndpointAccess,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AuthorizeVpcEndpointAccess } from
      "jsr:@ubx/sdk-aws/aws/opensearch/authorize_vpc_endpoint_access";

    resource(AuthorizeVpcEndpointAccess, "example", {
      account: "example",
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import (
        AuthorizeVpcEndpointAccess,
        AuthorizeVpcEndpointAccessConfig,
    )

    cfg = AuthorizeVpcEndpointAccessConfig(
        account="example",
        domain_name="example",
    )
    ubx.resource(AuthorizeVpcEndpointAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_authorize_vpc_endpoint_access with
account, domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="authorized_principal" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="principal" type="string">
    </ResponseField>
    <ResponseField name="principal_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
