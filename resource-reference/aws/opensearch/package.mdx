---
title: "aws_opensearch_package"
description: "Real, generated bindings for `aws_opensearch_package`."
---

`aws_opensearch_package` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"

    cfg := opensearch.PackageConfig{
        PackageName: "example-package-name",
        PackageType: "example",
        EngineVersion: "example",
        PackageDescription: "example",
    }
    ubx.Resource(opensearch.Package, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Package } from
      "jsr:@ubx/sdk-aws/aws/opensearch/package";

    resource(Package, "example", {
      packageName: "example",
      packageType: "example",
      engineVersion: "example",
      packageDescription: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import Package, PackageConfig

    cfg = PackageConfig(
        package_name="example",
        package_type="example",
        engine_version="example",
        package_description="example",
    )
    ubx.resource(Package, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_package with package_name, package_type,
engine_version, package_description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="engine_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="package_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="package_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="available_package_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
