---
title: "aws_opensearch_package"
description: "Real, generated bindings for `aws_opensearch_package`."
---

`aws_opensearch_package` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own package"})

    		ubx.Resource(opensearch.Package, "ci-runner", opensearch.PackageConfig{
    			PackageName:        "example-package-name",
    			PackageType:        "example",
    			EngineVersion:      "example",
    			PackageDescription: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Package } from "jsr:@ubx/sdk-aws/aws/opensearch/package";

    export default stack("payments", () => {
      intent({ summary: "payments own package" });

      resource(Package, "ci-runner", {
        packageName: "example-package-name",
        packageType: "example",
        engineVersion: "example",
        packageDescription: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import Package, PackageConfig

    def describe():
        ubx.intent("payments own package")

        ubx.resource(Package, "ci-runner", PackageConfig(
            package_name="example-package-name",
            package_type="example",
            engine_version="example",
            package_description="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_package, with package name
    example-package-name, package type example, engine version
    example, package description example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="engine_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="package_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="package_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="available_package_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="package_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
