---
title: "aws_opensearch_vpc_endpoint"
description: "Real, generated bindings for `aws_opensearch_vpc_endpoint`."
---

`aws_opensearch_vpc_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"

    cfg := opensearch.VpcEndpointConfig{
        DomainArn: "example",
    }
    ubx.Resource(opensearch.VpcEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcEndpoint } from
      "jsr:@ubx/sdk-aws/aws/opensearch/vpc_endpoint";

    resource(VpcEndpoint, "example", {
      domainArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.opensearch import VpcEndpoint, VpcEndpointConfig

    cfg = VpcEndpointConfig(
        domain_arn="example",
    )
    ubx.resource(VpcEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_opensearch_vpc_endpoint with domain_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
