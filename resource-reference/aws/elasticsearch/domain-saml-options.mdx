---
title: "aws_elasticsearch_domain_saml_options"
description: "Real, generated bindings for `aws_elasticsearch_domain_saml_options`."
---

`aws_elasticsearch_domain_saml_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticsearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elasticsearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain saml options"})

    		ubx.Resource(elasticsearch.DomainSamlOptions, "ci-runner", elasticsearch.DomainSamlOptionsConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainSamlOptions } from "jsr:@ubx/sdk-aws/aws/elasticsearch/domain_saml_options";

    export default stack("payments", () => {
      intent({ summary: "payments own domain saml options" });

      resource(DomainSamlOptions, "ci-runner", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elasticsearch import DomainSamlOptions, DomainSamlOptionsConfig

    def describe():
        ubx.intent("payments own domain saml options")

        ubx.resource(DomainSamlOptions, "ci-runner", DomainSamlOptionsConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elasticsearch_domain_saml_options, with domain name
    example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
