---
title: "aws_elasticsearch_domain_saml_options"
description: "Real, generated bindings for `aws_elasticsearch_domain_saml_options`."
---

`aws_elasticsearch_domain_saml_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elasticsearch"

    cfg := elasticsearch.DomainSamlOptionsConfig{
        DomainName: "example-domain-name",
    }
    ubx.Resource(elasticsearch.DomainSamlOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainSamlOptions } from
      "jsr:@ubx/sdk-aws/aws/elasticsearch/domain_saml_options";

    resource(DomainSamlOptions, "example", {
      domainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.elasticsearch import (
        DomainSamlOptions,
        DomainSamlOptionsConfig,
    )

    cfg = DomainSamlOptionsConfig(
        domain_name="example",
    )
    ubx.resource(DomainSamlOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_elasticsearch_domain_saml_options with domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
