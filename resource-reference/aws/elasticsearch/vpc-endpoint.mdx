---
title: "aws_elasticsearch_vpc_endpoint"
description: "Real, generated bindings for `aws_elasticsearch_vpc_endpoint`."
---

`aws_elasticsearch_vpc_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticsearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elasticsearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc endpoint"})

    		ubx.Resource(elasticsearch.VpcEndpoint, "ci-runner", elasticsearch.VpcEndpointConfig{
    			DomainArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcEndpoint } from "jsr:@ubx/sdk-aws/aws/elasticsearch/vpc_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc endpoint" });

      resource(VpcEndpoint, "ci-runner", {
        domainArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elasticsearch import VpcEndpoint, VpcEndpointConfig

    def describe():
        ubx.intent("payments own vpc endpoint")

        ubx.resource(VpcEndpoint, "ci-runner", VpcEndpointConfig(
            domain_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elasticsearch_vpc_endpoint, with domain arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
