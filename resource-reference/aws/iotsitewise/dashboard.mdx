---
title: "aws_io_tsite_wise_dashboard"
description: "`aws_io_tsite_wise_dashboard` is a real, saved visualization, trend charts, KPIs, of `aws_io_tsite_wise_asset` data inside an `aws_io_tsite_wise_project`."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dashboard"})

    		ubx.Resource(iotsitewise.Dashboard, "example", iotsitewise.DashboardConfig{
    			DashboardDefinition:  "example",
    			DashboardDescription: "example",
    			DashboardName:        "example-dashboard-name",
    			ProjectId:            "example",
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Dashboard } from "@ubx/sdk-aws/aws/iotsitewise/dashboard";

    export default stack("example", () => {
      intent({ summary: "example own dashboard" });

      resource(Dashboard, "example", {
        dashboardDefinition: "example",
        dashboardDescription: "example",
        dashboardName: "example-dashboard-name",
        projectId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iotsitewise import Dashboard, DashboardConfig
    from ubx.aws.iotsitewise.dashboard import Dashboard_Tags

    def describe():
        ubx.intent("example own dashboard")

        ubx.resource(Dashboard, "example", DashboardConfig(
            dashboard_definition="example",
            dashboard_description="example",
            dashboard_name="example-dashboard-name",
            project_id="example",
            tags=[Dashboard_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tsite_wise_dashboard, with dashboard definition
    example, dashboard description example, dashboard name
    example-dashboard-name, project id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dashboard_definition" type="string" required>
  The dashboard definition specified in a JSON literal. Required.
</ResponseField>

<ResponseField name="dashboard_description" type="string" required>
  A description for the dashboard. Required.
</ResponseField>

<ResponseField name="dashboard_name" type="string" required>
  A friendly name for the dashboard. Required.
</ResponseField>

<ResponseField name="project_id" type="string">
  The ID of the project in which to create the dashboard. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs that contain metadata for the dashboard. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key that identifies a tag applied to the AWS IoT SiteWise dashboard, used for metadata and resource categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag (a key-value pair) attached to the AWS IoT SiteWise dashboard, allowing you to categorize and manage the dashboard resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="dashboard_arn" type="string">
  The ARN of the dashboard. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dashboard_id" type="string">
  The ID of the dashboard. Computed by AWS after `ship`.
</ResponseField>
