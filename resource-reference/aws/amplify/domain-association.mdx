---
title: "aws_amplify_domain_association"
description: "Real, generated bindings for `aws_amplify_domain_association`."
---

`aws_amplify_domain_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/amplify"

    cfg := amplify.DomainAssociationConfig{
        AppId: "example",
        DomainName: "example-domain-name",
        EnableAutoSubDomain: true,
        WaitForVerification: true,
    }
    ubx.Resource(amplify.DomainAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainAssociation } from
      "jsr:@ubx/sdk-aws/aws/amplify/domain_association";

    resource(DomainAssociation, "example", {
      appId: "example",
      domainName: "example",
      enableAutoSubDomain: true,
      waitForVerification: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.amplify import (
        DomainAssociation,
        DomainAssociationConfig,
    )

    cfg = DomainAssociationConfig(
        app_id="example",
        domain_name="example",
        enable_auto_sub_domain=True,
        wait_for_verification=True,
    )
    ubx.resource(DomainAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_amplify_domain_association with app_id,
domain_name, enable_auto_sub_domain, wait_for_verification.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_auto_sub_domain" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="wait_for_verification" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_verification_dns_record" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
