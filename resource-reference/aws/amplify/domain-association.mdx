---
title: "aws_amplify_domain_association"
description: "Real, generated bindings for `aws_amplify_domain_association`."
---

`aws_amplify_domain_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain association"})

    		ubx.Resource(amplify.DomainAssociation, "ci-runner", amplify.DomainAssociationConfig{
    			AppId:               "example",
    			DomainName:          "example-domain-name",
    			EnableAutoSubDomain: true,
    			WaitForVerification: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainAssociation } from "jsr:@ubx/sdk-aws/aws/amplify/domain_association";

    export default stack("payments", () => {
      intent({ summary: "payments own domain association" });

      resource(DomainAssociation, "ci-runner", {
        appId: "example",
        domainName: "example-domain-name",
        enableAutoSubDomain: true,
        waitForVerification: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.amplify import DomainAssociation, DomainAssociationConfig

    def describe():
        ubx.intent("payments own domain association")

        ubx.resource(DomainAssociation, "ci-runner", DomainAssociationConfig(
            app_id="example",
            domain_name="example-domain-name",
            enable_auto_sub_domain=True,
            wait_for_verification=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_domain_association, with app id example,
    domain name example-domain-name, enable auto sub domain true,
    wait for verification true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_settings" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="certificate_verification_dns_record" type="string">
    </ResponseField>
    <ResponseField name="custom_certificate_arn" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_auto_sub_domain" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sub_domain" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="branch_name" type="string" required>
    </ResponseField>
    <ResponseField name="dns_record" type="string">
    </ResponseField>
    <ResponseField name="prefix" type="string" required>
    </ResponseField>
    <ResponseField name="verified" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="wait_for_verification" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_settings" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="certificate_verification_dns_record" type="string">
    </ResponseField>
    <ResponseField name="custom_certificate_arn" type="string">
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="certificate_verification_dns_record" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sub_domain" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="branch_name" type="string" required>
    </ResponseField>
    <ResponseField name="dns_record" type="string">
    </ResponseField>
    <ResponseField name="prefix" type="string" required>
    </ResponseField>
    <ResponseField name="verified" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>
