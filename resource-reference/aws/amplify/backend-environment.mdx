---
title: "aws_amplify_backend_environment"
description: "Real, generated bindings for `aws_amplify_backend_environment`."
---

`aws_amplify_backend_environment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/amplify"

    cfg := amplify.BackendEnvironmentConfig{
        AppId: "example",
        EnvironmentName: "example-environment-name",
        DeploymentArtifacts: "example",
        StackName: "example-stack-name",
    }
    ubx.Resource(amplify.BackendEnvironment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BackendEnvironment } from
      "jsr:@ubx/sdk-aws/aws/amplify/backend_environment";

    resource(BackendEnvironment, "example", {
      appId: "example",
      environmentName: "example",
      deploymentArtifacts: "example",
      stackName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.amplify import (
        BackendEnvironment,
        BackendEnvironmentConfig,
    )

    cfg = BackendEnvironmentConfig(
        app_id="example",
        environment_name="example",
        deployment_artifacts="example",
        stack_name="example",
    )
    ubx.resource(BackendEnvironment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_amplify_backend_environment with app_id,
environment_name, deployment_artifacts, stack_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deployment_artifacts" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="environment_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_artifacts" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
