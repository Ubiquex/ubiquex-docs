---
title: "aws_amplify_webhook"
description: "Real, generated bindings for `aws_amplify_webhook`."
---

`aws_amplify_webhook` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"

    cfg := amplify.WebhookConfig{
        AppId: "example",
        BranchName: "example-branch-name",
        Description: "example",
    }
    ubx.Resource(amplify.Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Webhook } from "jsr:@ubx/sdk-aws/aws/amplify/webhook";

    resource(Webhook, "example", {
      appId: "example",
      branchName: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.amplify import Webhook, WebhookConfig

    cfg = WebhookConfig(
        app_id="example",
        branch_name="example",
        description="example",
    )
    ubx.resource(Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_amplify_webhook with app_id, branch_name,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="branch_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
