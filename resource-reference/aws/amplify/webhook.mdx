---
title: "aws_amplify_webhook"
description: "`aws_amplify_webhook` creates a real, callable URL that triggers a new build of an `aws_amplify_branch` when invoked..."
---

`aws_amplify_webhook` creates a real, callable URL that triggers a new build of an `aws_amplify_branch` when invoked, letting an external system kick off a deployment without needing a full source-provider Git integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own webhook"})

    		ubx.Resource(amplify.Webhook, "ci-runner", amplify.WebhookConfig{
    			BranchName:  "example-branch-name",
    			AppId:       "example",
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Webhook } from "./local-sdk/aws/sdk/typescript/aws/amplify/webhook.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own webhook" });

      resource(Webhook, "ci-runner", {
        branchName: "example-branch-name",
        appId: "example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.amplify import Webhook, WebhookConfig

    def describe():
        ubx.intent("payments own webhook")

        ubx.resource(Webhook, "ci-runner", WebhookConfig(
            branch_name="example-branch-name",
            app_id="example",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_webhook, with branch name example-branch-name,
    app id example, description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string">
  The unique ID for an Amplify app. Optional.
</ResponseField>

<ResponseField name="branch_name" type="string" required>
  The name for a branch that is part of an Amplify app. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description for a webhook. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags for the webhook. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) for the webhook. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="webhook_id" type="string">
  The unique ID for a webhook. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="webhook_url" type="string">
  The URL of the webhook. Computed by AWS after `ship`.
</ResponseField>
