---
title: "aws_amplify_app"
description: "Real, generated bindings for `aws_amplify_app`."
---

`aws_amplify_app` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app"})

    		ubx.Resource(amplify.App, "ci-runner", amplify.AppConfig{
    			Name:                       "ci-runner",
    			AccessToken:                "example",
    			AutoBranchCreationPatterns: []string{"example"},
    			BasicAuthCredentials:       "example",
    			ComputeRoleArn:             "arn:aws:iam::123456789012:policy/ci",
    			Description:                "Managed by ubx.",
    			EnableAutoBranchCreation:   true,
    			EnableBasicAuth:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { App } from "jsr:@ubx/sdk-aws/aws/amplify/app";

    export default stack("payments", () => {
      intent({ summary: "payments own app" });

      resource(App, "ci-runner", {
        name: "ci-runner",
        accessToken: "example",
        autoBranchCreationPatterns: ["example"],
        basicAuthCredentials: "example",
        computeRoleArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        enableAutoBranchCreation: true,
        enableBasicAuth: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.amplify import App, AppConfig

    def describe():
        ubx.intent("payments own app")

        ubx.resource(App, "ci-runner", AppConfig(
            name="ci-runner",
            access_token="example",
            auto_branch_creation_patterns=["example"],
            basic_auth_credentials="example",
            compute_role_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            enable_auto_branch_creation=True,
            enable_basic_auth=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_app called "ci-runner", with access token
    example, auto branch creation patterns a set value, basic auth
    credentials example, compute role arn
    arn:aws:iam::123456789012:policy/ci, description Managed by ubx.,
    enable auto branch creation true, enable basic auth true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_token" type="string">
  Optional.
</ResponseField>

<ResponseField name="auto_branch_creation_patterns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="basic_auth_credentials" type="string">
  Optional.
</ResponseField>

<ResponseField name="build_spec" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="compute_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="custom_headers" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable_auto_branch_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_basic_auth" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_branch_auto_build" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_branch_auto_deletion" type="bool">
  Optional.
</ResponseField>

<ResponseField name="environment_variables" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="iam_service_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="oauth_token" type="string">
  Optional.
</ResponseField>

<ResponseField name="platform" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="build_spec" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="custom_headers" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="default_domain" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="production_branch" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="branch_name" type="string">
    </ResponseField>
    <ResponseField name="last_deploy_time" type="string">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
    <ResponseField name="thumbnail_url" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
