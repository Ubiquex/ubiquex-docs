---
title: "aws_amplify_uibuilder_component"
description: "`aws_amplify_uibuilder_component` is a UI component generated by Amplify Studio..."
---

`aws_amplify_uibuilder_component` is a UI component generated by Amplify Studio, commonly converted automatically from a Figma design into real, editable React component code rather than hand-written from scratch.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own uibuilder component"})

    		ubx.Resource(amplify.UibuilderComponent, "example", amplify.UibuilderComponentConfig{
    			Name:                 "example",
    			AppId:                "example",
    			BindingProperties:    "example",
    			Children:             []map[string]any{{"name": "example-name"}},
    			CollectionProperties: "example",
    			ComponentType:        "example",
    			EnvironmentName:      "example-environment-name",
    			Events:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { UibuilderComponent } from "./local-sdk/aws/sdk/typescript/aws/amplify/uibuilder_component.ts";

    export default stack("example", () => {
      intent({ summary: "example own uibuilder component" });

      resource(UibuilderComponent, "example", {
        name: "example",
        appId: "example",
        bindingProperties: "example",
        children: [{ name: "example-name" }],
        collectionProperties: "example",
        componentType: "example",
        environmentName: "example-environment-name",
        events: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.amplify import UibuilderComponent, UibuilderComponentConfig

    def describe():
        ubx.intent("example own uibuilder component")

        ubx.resource(UibuilderComponent, "example", UibuilderComponentConfig(
            name="example",
            app_id="example",
            binding_properties="example",
            children=[{"name": "example-name"}],
            collection_properties="example",
            component_type="example",
            environment_name="example-environment-name",
            events="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_uibuilder_component called "example", with app
    id example, binding properties example, children a set value,
    collection properties example, component type example,
    environment name example-environment-name, events example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string">
  The unique ID of the Amplify app that this UI Builder component is associated with, scoping the component to the correct application. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="binding_properties" type="any">
  Defines the data binding configuration for the Amplify UI Builder component, mapping component properties to data source fields or static values. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="children" type="list(object)">
  Defines the list of child components nested within this Amplify UI Builder component, allowing hierarchical composition of UI elements where each child can have its own properties and bindings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="children" type="list(any)">
      This property contains the nested child elements for each child component in the parent children list, recursively defining the UI component hierarchy. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="component_type" type="string">
      Specifies the UI component type for each child in the children list (e.g., 'Button', 'TextField'), defining what kind of element that child renders in the Amplify UI Builder component. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events" type="any">
      Defines the event bindings for a child component, mapping event names (such as 'onClick') to the actions that should be triggered when that event occurs. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The unique name of a child component within the parent component's children list, used to identify and reference the child in the Amplify UI Builder component hierarchy. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="properties" type="any">
      Defines the properties of each child component in the component's children list, mapping property names to property values (such as text, colors, or data bindings) that determine how the child component renders within the Amplify UI Builder component. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_id" type="string">
      Identifies the unique ID of the source Amplify UI Builder component that this child component is instantiated from, used to reuse or reference a previously defined component within a parent component's hierarchy. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="collection_properties" type="any">
  Defines the data-binding configuration for a collection-based component, specifying properties such as the data model, query predicate, sorting, and pagination settings. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="component_type" type="string">
  Specifies the type of UI component (e.g., Button, Text, Image), which determines the rendering behavior and available properties for the component in AWS Amplify UI Builder. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="environment_name" type="string">
  The name of the Amplify environment (e.g., 'dev' or 'prod') that the UI builder component is associated with. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="events" type="any">
  Configures event handlers for the component, mapping event names (such as onClick) to action definitions for user interactions. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The name of the Amplify UI Builder component, which serves as its unique identifier within the app and is used when the component is referenced by other Amplify UI Builder resources or in code generation. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="overrides" type="any">
  Overrides specifies a JSON object of custom property values that override the default configurations of the component's UI elements, allowing targeted styling and behavior changes without modifying the underlying component definition. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="properties" type="any">
  Specifies the map of component properties for the Amplify UI Builder component, where each key-value pair represents a configurable attribute (e.g., text, style, or event handler) available on the component. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="schema_version" type="string">
  The schema version of the component, indicating which version of the Amplify UI Builder component schema the component adheres to. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="source_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A map of key-value pairs used to assign metadata tags to the Amplify UI Builder component for identification and organization. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="variants" type="list(object)">
  Specifies a list of variants for the component, each containing variant values and overrides that adjust the component's properties for different conditions such as breakpoints or themes. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="overrides" type="any">
      A map of property overrides applied to the component when it is rendered in this variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="variant_values" type="any">
      Defines the key-value pairs that specify a variant's properties, such as viewport or type, for a component variant in AWS Amplify UI Builder. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp of when the Amplify UI Builder component was created, returned as a read-only string value. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="modified_at" type="string">
  The timestamp (in ISO 8601 format) indicating when the component was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
