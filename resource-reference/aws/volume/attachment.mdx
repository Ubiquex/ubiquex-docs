---
title: "aws_volume_attachment"
description: "Real, generated bindings for `aws_volume_attachment`."
---

`aws_volume_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/volume"

    cfg := volume.AttachmentConfig{
        DeviceName: "example-device-name",
        InstanceId: "example",
        VolumeId: "example",
        ForceDetach: true,
        SkipDestroy: true,
    }
    ubx.Resource(volume.Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Attachment } from
      "jsr:@ubx/sdk-aws/aws/volume/attachment";

    resource(Attachment, "example", {
      deviceName: "example",
      instanceId: "example",
      volumeId: "example",
      forceDetach: true,
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.volume import Attachment, AttachmentConfig

    cfg = AttachmentConfig(
        device_name="example",
        instance_id="example",
        volume_id="example",
        force_detach=True,
        skip_destroy=True,
    )
    ubx.resource(Attachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_volume_attachment with device_name, instance_id,
volume_id, force_detach, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="device_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_detach" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="stop_instance_before_detaching" type="bool">
  Optional.
</ResponseField>

<ResponseField name="volume_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
