---
title: "aws_volume_attachment"
description: "`aws_volume_attachment` attaches a real `aws_ec2_volume` to a running EC2 instance at a chosen device name..."
---

`aws_volume_attachment` attaches a real `aws_ec2_volume` to a running EC2 instance at a chosen device name, the resource that actually represents the attach action rather than the volume itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	volume "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/volume"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attachment"})

    		ubx.Resource(volume.Attachment, "ci-runner", volume.AttachmentConfig{
    			InstanceId:   "example",
    			VolumeId:     "example",
    			Device:       "example",
    			EbsCardIndex: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Attachment } from "./local-sdk/aws/sdk/typescript/aws/volume/attachment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own attachment" });

      resource(Attachment, "ci-runner", {
        instanceId: "example",
        volumeId: "example",
        device: "example",
        ebsCardIndex: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.volume import Attachment, AttachmentConfig

    def describe():
        ubx.intent("payments own attachment")

        ubx.resource(Attachment, "ci-runner", AttachmentConfig(
            instance_id="example",
            volume_id="example",
            device="example",
            ebs_card_index=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_volume_attachment, with instance id example, volume id
    example, device example, ebs card index 1.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="device" type="string">
  The device name Optional.
</ResponseField>

<ResponseField name="ebs_card_index" type="number">
  The index of the EBS card. Some instance types support multiple EBS cards. The default EBS card index is 0. Optional.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The ID of the instance to which the volume attaches Required.
</ResponseField>

<ResponseField name="volume_id" type="string" required>
  The ID of the Amazon EBS volume Required.
</ResponseField>
