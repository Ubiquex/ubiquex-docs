---
title: "aws_health_imaging_datastore"
description: "`aws_health_imaging_datastore` is the real, central repository AWS HealthImaging stores medical imaging data in..."
---

`aws_health_imaging_datastore` is the real, central repository AWS HealthImaging stores medical imaging data in, DICOM P10 files imported from S3 and transformed into image sets of metadata and pixel-data image frames, built for petabyte-scale, subsecond image retrieval.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own imaging datastore"})

    		ubx.Resource(health.ImagingDatastore, "example", health.ImagingDatastoreConfig{
    			DatastoreName: "example-datastore-name",
    			KmsKeyArn:     "arn:aws:iam::123456789012:policy/example",
    			Tags:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ImagingDatastore } from "./local-sdk/aws/sdk/typescript/aws/health/imaging_datastore.ts";

    export default stack("example", () => {
      intent({ summary: "example own imaging datastore" });

      resource(ImagingDatastore, "example", {
        datastoreName: "example-datastore-name",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.health import ImagingDatastore, ImagingDatastoreConfig

    def describe():
        ubx.intent("example own imaging datastore")

        ubx.resource(ImagingDatastore, "example", ImagingDatastoreConfig(
            datastore_name="example-datastore-name",
            kms_key_arn="arn:aws:iam::123456789012:policy/example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_health_imaging_datastore, with datastore name
    example-datastore-name, kms key arn
    arn:aws:iam::123456789012:policy/example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="datastore_name" type="string">
  User friendly name for Datastore. Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  ARN referencing a KMS key or KMS key alias. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A Map of key value pairs for Tags. Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the data store was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="datastore_arn" type="string">
  The Datastore's ARN. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="datastore_id" type="string">
  The unique identifier assigned to the datastore by AWS HealthImaging when it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="datastore_status" type="string">
  A string to denote the Datastore's state. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the data store was created. Computed by AWS after `ship`.
</ResponseField>
