---
title: "aws_xray_indexing_rule"
description: "Real, generated bindings for `aws_xray_indexing_rule`."
---

`aws_xray_indexing_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own indexing rule"})

    		ubx.Resource(xray.IndexingRule, "ci-runner", xray.IndexingRuleConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IndexingRule } from "jsr:@ubx/sdk-aws/aws/xray/indexing_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own indexing rule" });

      resource(IndexingRule, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import IndexingRule, IndexingRuleConfig

    def describe():
        ubx.intent("payments own indexing rule")

        ubx.resource(IndexingRule, "ci-runner", IndexingRuleConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_indexing_rule called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
