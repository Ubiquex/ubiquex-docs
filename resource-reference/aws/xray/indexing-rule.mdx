---
title: "aws_xray_indexing_rule"
description: "Real, generated bindings for `aws_xray_indexing_rule`."
---

`aws_xray_indexing_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/xray"

    cfg := xray.IndexingRuleConfig{
        Name: "example-name",
    }
    ubx.Resource(xray.IndexingRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IndexingRule } from
      "jsr:@ubx/sdk-aws/aws/xray/indexing_rule";

    resource(IndexingRule, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.xray import IndexingRule, IndexingRuleConfig

    cfg = IndexingRuleConfig(
        name="example",
    )
    ubx.resource(IndexingRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_xray_indexing_rule with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
