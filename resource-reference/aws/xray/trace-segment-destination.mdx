---
title: "aws_xray_trace_segment_destination"
description: "Real, generated bindings for `aws_xray_trace_segment_destination`."
---

`aws_xray_trace_segment_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trace segment destination"})

    		ubx.Resource(xray.TraceSegmentDestination, "ci-runner", xray.TraceSegmentDestinationConfig{
    			Destination: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TraceSegmentDestination } from "jsr:@ubx/sdk-aws/aws/xray/trace_segment_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own trace segment destination" });

      resource(TraceSegmentDestination, "ci-runner", {
        destination: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import TraceSegmentDestination, TraceSegmentDestinationConfig

    def describe():
        ubx.intent("payments own trace segment destination")

        ubx.resource(TraceSegmentDestination, "ci-runner", TraceSegmentDestinationConfig(
            destination="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_trace_segment_destination, with destination
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
