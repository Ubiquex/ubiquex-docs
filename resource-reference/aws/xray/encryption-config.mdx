---
title: "aws_xray_encryption_config"
description: "Real, generated bindings for `aws_xray_encryption_config`."
---

`aws_xray_encryption_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own encryption config"})

    		ubx.Resource(xray.EncryptionConfig, "ci-runner", xray.EncryptionConfigConfig{
    			Type:  "example",
    			KeyId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EncryptionConfig } from "jsr:@ubx/sdk-aws/aws/xray/encryption_config";

    export default stack("payments", () => {
      intent({ summary: "payments own encryption config" });

      resource(EncryptionConfig, "ci-runner", {
        type: "example",
        keyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import EncryptionConfig, EncryptionConfigConfig

    def describe():
        ubx.intent("payments own encryption config")

        ubx.resource(EncryptionConfig, "ci-runner", EncryptionConfigConfig(
            type="example",
            key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_encryption_config, with type example, key id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
