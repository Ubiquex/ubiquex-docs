---
title: "aws_xray_encryption_config"
description: "Real, generated bindings for `aws_xray_encryption_config`."
---

`aws_xray_encryption_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/xray"

    cfg := xray.EncryptionConfigConfig{
        Type: "example",
        KeyId: "example",
    }
    ubx.Resource(xray.EncryptionConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EncryptionConfig } from
      "jsr:@ubx/sdk-aws/aws/xray/encryption_config";

    resource(EncryptionConfig, "example", {
      type: "example",
      keyId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.xray import (
        EncryptionConfig,
        EncryptionConfigConfig,
    )

    cfg = EncryptionConfigConfig(
        type="example",
        key_id="example",
    )
    ubx.resource(EncryptionConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_xray_encryption_config with type, key_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
