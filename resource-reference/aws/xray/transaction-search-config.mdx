---
title: "aws_xray_transaction_search_config"
description: "`aws_xray_transaction_search_config` enables X-Ray Transaction Search..."
---

`aws_xray_transaction_search_config` enables X-Ray Transaction Search, ingesting spans into a dedicated CloudWatch Logs group (aws/spans) so CloudWatch Application Signals can generate a curated APM experience from them. This sits alongside, not instead of, the older X-Ray SDK/Daemon stack, which enters maintenance mode February 25, 2026 and reaches end of support February 25, 2027, with AWS steering new instrumentation toward OpenTelemetry.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own transaction search config"})

    		ubx.Resource(xray.TransactionSearchConfig, "example", xray.TransactionSearchConfigConfig{
    			IndexingPercentage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransactionSearchConfig } from "@ubx/sdk-aws/aws/xray/transaction_search_config";

    export default stack("example", () => {
      intent({ summary: "example own transaction search config" });

      resource(TransactionSearchConfig, "example", {
        indexingPercentage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import TransactionSearchConfig, TransactionSearchConfigConfig

    def describe():
        ubx.intent("example own transaction search config")

        ubx.resource(TransactionSearchConfig, "example", TransactionSearchConfigConfig(
            indexing_percentage=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_transaction_search_config, with indexing
    percentage 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="indexing_percentage" type="number">
  Determines the percentage of traces indexed from CloudWatch Logs to X-Ray Optional.
</ResponseField>

## Output properties

<ResponseField name="account_id" type="string">
  User account id, used as the primary identifier for the resource Computed by AWS after `ship`.
</ResponseField>
