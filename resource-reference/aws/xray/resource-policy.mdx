---
title: "aws_xray_resource_policy"
description: "`aws_xray_resource_policy` attaches a real resource-based access policy to X-Ray, controlling which principals, including from other accounts..."
---

`aws_xray_resource_policy` attaches a real resource-based access policy to X-Ray, controlling which principals, including from other accounts, can send trace data into it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resource policy"})

    		ubx.Resource(xray.ResourcePolicy, "example", xray.ResourcePolicyConfig{
    			PolicyDocument:           "example",
    			PolicyName:               "example-policy-name",
    			BypassPolicyLockoutCheck: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/xray/resource_policy";

    export default stack("example", () => {
      intent({ summary: "example own resource policy" });

      resource(ResourcePolicy, "example", {
        policyDocument: "example",
        policyName: "example-policy-name",
        bypassPolicyLockoutCheck: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("example own resource policy")

        ubx.resource(ResourcePolicy, "example", ResourcePolicyConfig(
            policy_document="example",
            policy_name="example-policy-name",
            bypass_policy_lockout_check=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_resource_policy, with policy document example,
    policy name example-policy-name, bypass policy lockout check
    true.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="bypass_policy_lockout_check" type="bool">
  A flag to indicate whether to bypass the resource policy lockout safety check Optional.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  The resource policy document, which can be up to 5kb in size. Required.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The name of the resource policy. Must be unique within a specific AWS account. Required.
</ResponseField>
