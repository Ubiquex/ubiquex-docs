---
title: "aws_m2_environment"
description: "Real, generated bindings for `aws_m2_environment`."
---

`aws_m2_environment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	m2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/m2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment"})

    		ubx.Resource(m2.Environment, "ci-runner", m2.EnvironmentConfig{
    			EngineType:                          "example",
    			InstanceType:                        "example",
    			Name:                                "ci-runner",
    			ApplyChangesDuringMaintenanceWindow: true,
    			Description:                         "Managed by ubx.",
    			ForceUpdate:                         true,
    			KmsKeyId:                            "example",
    			Tags:                                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Environment } from "jsr:@ubx/sdk-aws/aws/m2/environment";

    export default stack("payments", () => {
      intent({ summary: "payments own environment" });

      resource(Environment, "ci-runner", {
        engineType: "example",
        instanceType: "example",
        name: "ci-runner",
        applyChangesDuringMaintenanceWindow: true,
        description: "Managed by ubx.",
        forceUpdate: true,
        kmsKeyId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.m2 import Environment, EnvironmentConfig

    def describe():
        ubx.intent("payments own environment")

        ubx.resource(Environment, "ci-runner", EnvironmentConfig(
            engine_type="example",
            instance_type="example",
            name="ci-runner",
            apply_changes_during_maintenance_window=True,
            description="Managed by ubx.",
            force_update=True,
            kms_key_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_m2_environment called "ci-runner", with engine type
    example, instance type example, apply changes during maintenance
    window true, description Managed by ubx., force update true, kms
    key id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="apply_changes_during_maintenance_window" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="engine_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="force_update" type="bool">
  Optional.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="preferred_maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="environment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="load_balancer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="preferred_maintenance_window" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
