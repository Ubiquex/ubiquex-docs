---
title: "aws_m2_deployment"
description: "Real, generated bindings for `aws_m2_deployment`."
---

`aws_m2_deployment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/m2"

    cfg := m2.DeploymentConfig{
        ApplicationId: "example",
        ApplicationVersion: 1,
        EnvironmentId: "example",
        Start: true,
        ForceStop: true,
    }
    ubx.Resource(m2.Deployment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Deployment } from "jsr:@ubx/sdk-aws/aws/m2/deployment";

    resource(Deployment, "example", {
      applicationId: "example",
      applicationVersion: 1,
      environmentId: "example",
      start: true,
      forceStop: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.m2 import Deployment, DeploymentConfig

    cfg = DeploymentConfig(
        application_id="example",
        application_version=1,
        environment_id="example",
        start=True,
        force_stop=True,
    )
    ubx.resource(Deployment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_m2_deployment with application_id,
application_version, environment_id, start, force_stop.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="application_version" type="number" required>
  Required.
</ResponseField>

<ResponseField name="environment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_stop" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start" type="bool" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="deployment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
