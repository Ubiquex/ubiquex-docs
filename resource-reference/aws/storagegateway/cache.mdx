---
title: "aws_storagegateway_cache"
description: "Real, generated bindings for `aws_storagegateway_cache`."
---

`aws_storagegateway_cache` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/storagegateway"

    cfg := storagegateway.CacheConfig{
        DiskId: "example",
        GatewayArn: "example",
    }
    ubx.Resource(storagegateway.Cache, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Cache } from
      "jsr:@ubx/sdk-aws/aws/storagegateway/cache";

    resource(Cache, "example", {
      diskId: "example",
      gatewayArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.storagegateway import Cache, CacheConfig

    cfg = CacheConfig(
        disk_id="example",
        gateway_arn="example",
    )
    ubx.resource(Cache, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_storagegateway_cache with disk_id, gateway_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
