---
title: "aws_storagegateway_tape_pool"
description: "Real, generated bindings for `aws_storagegateway_tape_pool`."
---

`aws_storagegateway_tape_pool` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tape pool"})

    		ubx.Resource(storagegateway.TapePool, "ci-runner", storagegateway.TapePoolConfig{
    			PoolName:                "example-pool-name",
    			StorageClass:            "example",
    			RetentionLockTimeInDays: 1,
    			RetentionLockType:       "example",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TapePool } from "jsr:@ubx/sdk-aws/aws/storagegateway/tape_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own tape pool" });

      resource(TapePool, "ci-runner", {
        poolName: "example-pool-name",
        storageClass: "example",
        retentionLockTimeInDays: 1,
        retentionLockType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.storagegateway import TapePool, TapePoolConfig

    def describe():
        ubx.intent("payments own tape pool")

        ubx.resource(TapePool, "ci-runner", TapePoolConfig(
            pool_name="example-pool-name",
            storage_class="example",
            retention_lock_time_in_days=1,
            retention_lock_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_storagegateway_tape_pool, with pool name
    example-pool-name, storage class example, retention lock time in
    days 1, retention lock type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_lock_time_in_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="retention_lock_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="storage_class" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
