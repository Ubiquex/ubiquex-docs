---
title: "aws_storagegateway_tape_pool"
description: "Real, generated bindings for `aws_storagegateway_tape_pool`."
---

`aws_storagegateway_tape_pool` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/storagegateway"

    cfg := storagegateway.TapePoolConfig{
        PoolName: "example-pool-name",
        StorageClass: "example",
        RetentionLockTimeInDays: 1,
        RetentionLockType: "example",
    }
    ubx.Resource(storagegateway.TapePool, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TapePool } from
      "jsr:@ubx/sdk-aws/aws/storagegateway/tape_pool";

    resource(TapePool, "example", {
      poolName: "example",
      storageClass: "example",
      retentionLockTimeInDays: 1,
      retentionLockType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.storagegateway import TapePool, TapePoolConfig

    cfg = TapePoolConfig(
        pool_name="example",
        storage_class="example",
        retention_lock_time_in_days=1,
        retention_lock_type="example",
    )
    ubx.resource(TapePool, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_storagegateway_tape_pool with pool_name,
storage_class, retention_lock_time_in_days, retention_lock_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pool_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_lock_time_in_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="retention_lock_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="storage_class" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
