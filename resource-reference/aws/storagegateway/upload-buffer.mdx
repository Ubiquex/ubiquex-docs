---
title: "aws_storagegateway_upload_buffer"
description: "Real, generated bindings for `aws_storagegateway_upload_buffer`."
---

`aws_storagegateway_upload_buffer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/storagegateway"

    cfg := storagegateway.UploadBufferConfig{
        GatewayArn: "example",
        DiskId: "example",
        DiskPath: "example",
    }
    ubx.Resource(storagegateway.UploadBuffer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UploadBuffer } from
      "jsr:@ubx/sdk-aws/aws/storagegateway/upload_buffer";

    resource(UploadBuffer, "example", {
      gatewayArn: "example",
      diskId: "example",
      diskPath: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.storagegateway import (
        UploadBuffer,
        UploadBufferConfig,
    )

    cfg = UploadBufferConfig(
        gateway_arn="example",
        disk_id="example",
        disk_path="example",
    )
    ubx.resource(UploadBuffer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_storagegateway_upload_buffer with gateway_arn,
disk_id, disk_path.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disk_path" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="disk_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disk_path" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
