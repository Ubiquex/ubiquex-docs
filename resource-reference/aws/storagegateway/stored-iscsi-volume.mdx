---
title: "aws_storagegateway_stored_iscsi_volume"
description: "Real, generated bindings for `aws_storagegateway_stored_iscsi_volume`."
---

`aws_storagegateway_stored_iscsi_volume` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stored iscsi volume"})

    		ubx.Resource(storagegateway.StoredIscsiVolume, "ci-runner", storagegateway.StoredIscsiVolumeConfig{
    			DiskId:               "example",
    			GatewayArn:           "arn:aws:iam::123456789012:policy/ci",
    			NetworkInterfaceId:   "example",
    			PreserveExistingData: true,
    			TargetName:           "example-target-name",
    			KmsEncrypted:         true,
    			KmsKey:               "example",
    			SnapshotId:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StoredIscsiVolume } from "jsr:@ubx/sdk-aws/aws/storagegateway/stored_iscsi_volume";

    export default stack("payments", () => {
      intent({ summary: "payments own stored iscsi volume" });

      resource(StoredIscsiVolume, "ci-runner", {
        diskId: "example",
        gatewayArn: "arn:aws:iam::123456789012:policy/ci",
        networkInterfaceId: "example",
        preserveExistingData: true,
        targetName: "example-target-name",
        kmsEncrypted: true,
        kmsKey: "example",
        snapshotId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.storagegateway import StoredIscsiVolume, StoredIscsiVolumeConfig

    def describe():
        ubx.intent("payments own stored iscsi volume")

        ubx.resource(StoredIscsiVolume, "ci-runner", StoredIscsiVolumeConfig(
            disk_id="example",
            gateway_arn="arn:aws:iam::123456789012:policy/ci",
            network_interface_id="example",
            preserve_existing_data=True,
            target_name="example-target-name",
            kms_encrypted=True,
            kms_key="example",
            snapshot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_storagegateway_stored_iscsi_volume, with disk id
    example, gateway arn arn:aws:iam::123456789012:policy/ci, network
    interface id example, preserve existing data true, target name
    example-target-name, kms encrypted true, kms key example,
    snapshot id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="disk_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_encrypted" type="bool">
  Optional.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="preserve_existing_data" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="chap_enabled" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lun_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="network_interface_port" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_attachment_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_size_in_bytes" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
