---
title: "aws_storagegateway_working_storage"
description: "Real, generated bindings for `aws_storagegateway_working_storage`."
---

`aws_storagegateway_working_storage` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/storagegateway"

    cfg := storagegateway.WorkingStorageConfig{
        DiskId: "example",
        GatewayArn: "example",
    }
    ubx.Resource(storagegateway.WorkingStorage, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkingStorage } from
      "jsr:@ubx/sdk-aws/aws/storagegateway/working_storage";

    resource(WorkingStorage, "example", {
      diskId: "example",
      gatewayArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.storagegateway import (
        WorkingStorage,
        WorkingStorageConfig,
    )

    cfg = WorkingStorageConfig(
        disk_id="example",
        gateway_arn="example",
    )
    ubx.resource(WorkingStorage, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_storagegateway_working_storage with disk_id,
gateway_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
