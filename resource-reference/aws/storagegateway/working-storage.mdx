---
title: "aws_storagegateway_working_storage"
description: "Real, generated bindings for `aws_storagegateway_working_storage`."
---

`aws_storagegateway_working_storage` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own working storage"})

    		ubx.Resource(storagegateway.WorkingStorage, "ci-runner", storagegateway.WorkingStorageConfig{
    			DiskId:     "example",
    			GatewayArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkingStorage } from "jsr:@ubx/sdk-aws/aws/storagegateway/working_storage";

    export default stack("payments", () => {
      intent({ summary: "payments own working storage" });

      resource(WorkingStorage, "ci-runner", {
        diskId: "example",
        gatewayArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.storagegateway import WorkingStorage, WorkingStorageConfig

    def describe():
        ubx.intent("payments own working storage")

        ubx.resource(WorkingStorage, "ci-runner", WorkingStorageConfig(
            disk_id="example",
            gateway_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_storagegateway_working_storage, with disk id example,
    gateway arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disk_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="gateway_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
