---
title: "aws_kinesisanalytics_applications"
description: "`aws_kinesisanalytics_applications` looks up a real Kinesis Data Analytics (SQL) application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisanalytics "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kinesisanalytics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisanalytics_applications"})

    		ubx.Data(kinesisanalytics.Applications, "example", kinesisanalytics.ApplicationsConfig{
    			ExclusiveStartApplicationName: "example-exclusive-start-application-name",
    			Limit:                         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Applications } from "@ubx/sdk-aws/aws/data/kinesisanalytics/applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisanalytics_applications" });

      data(Applications, "example", {
        exclusiveStartApplicationName: "example-exclusive-start-application-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisanalytics import Applications, ApplicationsConfig

    def describe():
        ubx.intent("look up aws_kinesisanalytics_applications")

        ubx.data(Applications, "example", ApplicationsConfig(
            exclusive_start_application_name="example-exclusive-start-application-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclusive_start_application_name" type="string">
  The real name of the exclusive start application this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_summaries" type="list(object)">
  The real, summarized applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="has_more_applications" type="bool">
  Whether additional real applications are available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
