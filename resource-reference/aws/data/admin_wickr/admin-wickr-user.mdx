---
title: "aws_admin_wickr_user"
description: "`aws_admin_wickr_user` looks up one real Wickr user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_user"})

    		ubx.Data(admin_wickr.AdminWickrUser, "example", admin_wickr.AdminWickrUserConfig{
    			NetworkId: "example",
    			UserId:    "example",
    			EndTime:   "example",
    			StartTime: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrUser } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_user" });

      data(AdminWickrUser, "example", {
        networkId: "example",
        userId: "example",
        endTime: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrUser, AdminWickrUserConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_user")

        ubx.data(AdminWickrUser, "example", AdminWickrUserConfig(
            network_id="example",
            user_id="example",
            end_time="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="first_name" type="string">
  The real name of the first this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_admin" type="bool">
  Whether this identity has administrator privileges. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_activity" type="number">
  The real, most recent activity on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_login" type="number">
  The real timestamp this identity last logged in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_name" type="string">
  The real name of the last this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  The real, unique identifiers of the security groups this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="suspended" type="bool">
  Whether this object is currently suspended. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
