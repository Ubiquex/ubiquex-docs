---
title: "aws_admin_wickr_user"
description: "`aws_admin_wickr_user` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_admin_wickr_user` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_user"})

    		ubx.Data(admin_wickr.AdminWickrUser, "example", admin_wickr.AdminWickrUserConfig{
    			NetworkId: "example",
    			UserId:    "example",
    			EndTime:   "example",
    			StartTime: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrUser } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_user" });

      data(AdminWickrUser, "example", {
        networkId: "example",
        userId: "example",
        endTime: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrUser, AdminWickrUserConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_user")

        ubx.data(AdminWickrUser, "example", AdminWickrUserConfig(
            network_id="example",
            user_id="example",
            end_time="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="first_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_admin" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_activity" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_login" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="suspended" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="username" type="string">
  Returned by the lookup.
</ResponseField>
