---
title: "aws_admin_wickr_opentdf_config"
description: "`aws_admin_wickr_opentdf_config` reads the real OpenTDF data protection configuration of a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_opentdf_config"})

    		ubx.Data(admin_wickr.AdminWickrOpentdfConfig, "example", admin_wickr.AdminWickrOpentdfConfigConfig{
    			NetworkId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrOpentdfConfig } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_opentdf_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_opentdf_config" });

      data(AdminWickrOpentdfConfig, "example", {
        networkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrOpentdfConfig, AdminWickrOpentdfConfigConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_opentdf_config")

        ubx.data(AdminWickrOpentdfConfig, "example", AdminWickrOpentdfConfigConfig(
            network_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_id" type="string">
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="client_secret" type="string">
  The real, confidential secret used to authenticate the client. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="domain" type="string">
  The real domain this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provider" type="string">
  The real provider this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
