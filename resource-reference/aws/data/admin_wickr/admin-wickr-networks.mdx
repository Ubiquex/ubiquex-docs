---
title: "aws_admin_wickr_networks"
description: "`aws_admin_wickr_networks` looks up a real Wickr network, the top-level container for Wickr users and security policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_networks"})

    		ubx.Data(admin_wickr.AdminWickrNetworks, "example", admin_wickr.AdminWickrNetworksConfig{
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrNetworks } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_networks";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_networks" });

      data(AdminWickrNetworks, "example", {
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrNetworks, AdminWickrNetworksConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_networks")

        ubx.data(AdminWickrNetworks, "example", AdminWickrNetworksConfig(
            max_results=1,
            sort_direction="example",
            sort_fields="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The real direction (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  The real fields results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="networks" type="list(object)">
  The real networks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
