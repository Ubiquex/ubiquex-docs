---
title: "aws_admin_wickr_security_group_users"
description: "`aws_admin_wickr_security_group_users` reads the real users belonging to a Wickr security group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_security_group_users"})

    		ubx.Data(admin_wickr.AdminWickrSecurityGroupUsers, "example", admin_wickr.AdminWickrSecurityGroupUsersConfig{
    			GroupId:       "example",
    			NetworkId:     "example",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AdminWickrSecurityGroupUsers } from "./local-sdk/aws/sdk/typescript/aws/data/admin_wickr/admin_wickr_security_group_users.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_security_group_users" });

      data(AdminWickrSecurityGroupUsers, "example", {
        groupId: "example",
        networkId: "example",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrSecurityGroupUsers, AdminWickrSecurityGroupUsersConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_security_group_users")

        ubx.data(AdminWickrSecurityGroupUsers, "example", AdminWickrSecurityGroupUsersConfig(
            group_id="example",
            network_id="example",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The real direction (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  The real fields results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
