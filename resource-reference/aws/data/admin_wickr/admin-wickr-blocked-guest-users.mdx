---
title: "aws_admin_wickr_blocked_guest_users"
description: "`aws_admin_wickr_blocked_guest_users` reads the real guest users blocked from a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_blocked_guest_users"})

    		ubx.Data(admin_wickr.AdminWickrBlockedGuestUsers, "example", admin_wickr.AdminWickrBlockedGuestUsersConfig{
    			NetworkId:     "example",
    			Admin:         "example",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    			Username:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrBlockedGuestUsers } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_blocked_guest_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_blocked_guest_users" });

      data(AdminWickrBlockedGuestUsers, "example", {
        networkId: "example",
        admin: "example",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrBlockedGuestUsers, AdminWickrBlockedGuestUsersConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_blocked_guest_users")

        ubx.data(AdminWickrBlockedGuestUsers, "example", AdminWickrBlockedGuestUsersConfig(
            network_id="example",
            admin="example",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="admin" type="string">
  Whether this object has administrator privileges. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The real direction (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  The real fields results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blocklist" type="list(object)">
  The real blocklist entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
