---
title: "aws_admin_wickr_bots_count"
description: "`aws_admin_wickr_bots_count` reads the real count of bots registered on a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_bots_count"})

    		ubx.Data(admin_wickr.AdminWickrBotsCount, "example", admin_wickr.AdminWickrBotsCountConfig{
    			NetworkId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrBotsCount } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_bots_count";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_bots_count" });

      data(AdminWickrBotsCount, "example", {
        networkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrBotsCount, AdminWickrBotsCountConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_bots_count")

        ubx.data(AdminWickrBotsCount, "example", AdminWickrBotsCountConfig(
            network_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active" type="number">
  Whether this object is currently active. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pending" type="number">
  Whether this object is currently pending. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total" type="number">
  The real, total count of matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
