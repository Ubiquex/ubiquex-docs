---
title: "aws_admin_wickr_bots"
description: "`aws_admin_wickr_bots` looks up a real bot registered on a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_bots"})

    		ubx.Data(admin_wickr.AdminWickrBots, "example", admin_wickr.AdminWickrBotsConfig{
    			NetworkId:     "example",
    			DisplayName:   "example-display-name",
    			GroupId:       "example",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    			Status:        1,
    			Username:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrBots } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_bots";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_bots" });

      data(AdminWickrBots, "example", {
        networkId: "example",
        displayName: "example-display-name",
        groupId: "example",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
        status: 1,
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrBots, AdminWickrBotsConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_bots")

        ubx.data(AdminWickrBots, "example", AdminWickrBotsConfig(
            network_id="example",
            display_name="example-display-name",
            group_id="example",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
            status=1,
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="display_name" type="string">
  The real, human-readable display name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_id" type="string">
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The real direction (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  The real fields results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bots" type="list(object)">
  The real bots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
