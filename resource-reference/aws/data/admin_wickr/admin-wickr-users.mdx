---
title: "aws_admin_wickr_users"
description: "`aws_admin_wickr_users` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_admin_wickr_users` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_users"})

    		ubx.Data(admin_wickr.AdminWickrUsers, "example", admin_wickr.AdminWickrUsersConfig{
    			NetworkId:     "example",
    			FirstName:     "example-first-name",
    			GroupId:       "example",
    			LastName:      "example-last-name",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    			Status:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrUsers } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_users" });

      data(AdminWickrUsers, "example", {
        networkId: "example",
        firstName: "example-first-name",
        groupId: "example",
        lastName: "example-last-name",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
        status: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrUsers, AdminWickrUsersConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_users")

        ubx.data(AdminWickrUsers, "example", AdminWickrUsersConfig(
            network_id="example",
            first_name="example-first-name",
            group_id="example",
            last_name="example-last-name",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
            status=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="first_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="last_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  Returned by the lookup.
</ResponseField>
