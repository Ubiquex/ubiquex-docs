---
title: "aws_admin_wickr_users"
description: "`aws_admin_wickr_users` reads the real users on a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_users"})

    		ubx.Data(admin_wickr.AdminWickrUsers, "example", admin_wickr.AdminWickrUsersConfig{
    			NetworkId:     "example",
    			FirstName:     "example-first-name",
    			GroupId:       "example",
    			LastName:      "example-last-name",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    			Status:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrUsers } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_users" });

      data(AdminWickrUsers, "example", {
        networkId: "example",
        firstName: "example-first-name",
        groupId: "example",
        lastName: "example-last-name",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
        status: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrUsers, AdminWickrUsersConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_users")

        ubx.data(AdminWickrUsers, "example", AdminWickrUsersConfig(
            network_id="example",
            first_name="example-first-name",
            group_id="example",
            last_name="example-last-name",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
            status=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="first_name" type="string">
  The real name of the first this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_id" type="string">
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="last_name" type="string">
  The real name of the last this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  The real direction (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  The real fields results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
