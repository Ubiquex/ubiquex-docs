---
title: "aws_admin_wickr_security_groups"
description: "`aws_admin_wickr_security_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_admin_wickr_security_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_security_groups"})

    		ubx.Data(admin_wickr.AdminWickrSecurityGroups, "example", admin_wickr.AdminWickrSecurityGroupsConfig{
    			NetworkId:     "example",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrSecurityGroups } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_security_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_security_groups" });

      data(AdminWickrSecurityGroups, "example", {
        networkId: "example",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrSecurityGroups, AdminWickrSecurityGroupsConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_security_groups")

        ubx.data(AdminWickrSecurityGroups, "example", AdminWickrSecurityGroupsConfig(
            network_id="example",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="security_groups" type="list(object)">
  Returned by the lookup.
</ResponseField>
