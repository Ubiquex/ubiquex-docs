---
title: "aws_admin_wickr_users_count"
description: "`aws_admin_wickr_users_count` reads the real count of users on a Wickr network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_users_count"})

    		ubx.Data(admin_wickr.AdminWickrUsersCount, "example", admin_wickr.AdminWickrUsersCountConfig{
    			NetworkId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrUsersCount } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_users_count";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_users_count" });

      data(AdminWickrUsersCount, "example", {
        networkId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrUsersCount, AdminWickrUsersCountConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_users_count")

        ubx.data(AdminWickrUsersCount, "example", AdminWickrUsersCountConfig(
            network_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active" type="number">
  Whether this object is currently active. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pending" type="number">
  Whether this object is currently pending. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rejected" type="number">
  Whether this object has been rejected. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="remaining" type="number">
  The real, remaining count for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total" type="number">
  The real, total count of matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
