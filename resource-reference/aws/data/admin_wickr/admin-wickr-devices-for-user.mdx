---
title: "aws_admin_wickr_devices_for_user"
description: "`aws_admin_wickr_devices_for_user` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_admin_wickr_devices_for_user` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	admin_wickr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/admin_wickr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_admin_wickr_devices_for_user"})

    		ubx.Data(admin_wickr.AdminWickrDevicesForUser, "example", admin_wickr.AdminWickrDevicesForUserConfig{
    			NetworkId:     "example",
    			UserId:        "example",
    			MaxResults:    1,
    			SortDirection: "example",
    			SortFields:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminWickrDevicesForUser } from "@ubx/sdk-aws/aws/data/admin_wickr/admin_wickr_devices_for_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_admin_wickr_devices_for_user" });

      data(AdminWickrDevicesForUser, "example", {
        networkId: "example",
        userId: "example",
        maxResults: 1,
        sortDirection: "example",
        sortFields: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.admin_wickr import AdminWickrDevicesForUser, AdminWickrDevicesForUserConfig

    def describe():
        ubx.intent("look up aws_admin_wickr_devices_for_user")

        ubx.data(AdminWickrDevicesForUser, "example", AdminWickrDevicesForUserConfig(
            network_id="example",
            user_id="example",
            max_results=1,
            sort_direction="example",
            sort_fields="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_fields" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="devices" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
