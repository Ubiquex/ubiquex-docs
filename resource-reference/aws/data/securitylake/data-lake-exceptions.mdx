---
title: "aws_securitylake_data_lake_exceptions"
description: "`aws_securitylake_data_lake_exceptions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securitylake_data_lake_exceptions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securitylake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securitylake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securitylake_data_lake_exceptions"})

    		ubx.Data(securitylake.DataLakeExceptions, "example", securitylake.DataLakeExceptionsConfig{
    			MaxResults: 1,
    			Regions:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataLakeExceptions } from "@ubx/sdk-aws/aws/data/securitylake/data_lake_exceptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_securitylake_data_lake_exceptions" });

      data(DataLakeExceptions, "example", {
        maxResults: 1,
        regions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securitylake import DataLakeExceptions, DataLakeExceptionsConfig

    def describe():
        ubx.intent("look up aws_securitylake_data_lake_exceptions")

        ubx.data(DataLakeExceptions, "example", DataLakeExceptionsConfig(
            max_results=1,
            regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="exceptions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
