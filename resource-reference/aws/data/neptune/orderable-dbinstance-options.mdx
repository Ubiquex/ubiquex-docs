---
title: "aws_neptune_orderable_dbinstance_options"
description: "`aws_neptune_orderable_dbinstance_options` reads the real, orderable combinations of Neptune engine version, instance class, and Availability Zone."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_orderable_dbinstance_options"})

    		ubx.Data(neptune.OrderableDbinstanceOptions, "example", neptune.OrderableDbinstanceOptionsConfig{
    			Engine:          "example",
    			DbinstanceClass: "example",
    			EngineVersion:   "example",
    			Filters:         []map[string]any{{"name": "example-name"}},
    			LicenseModel:    "example",
    			MaxRecords:      1,
    			Vpc:             true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrderableDbinstanceOptions } from "./local-sdk/aws/sdk/typescript/aws/data/neptune/orderable_dbinstance_options.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_orderable_dbinstance_options" });

      data(OrderableDbinstanceOptions, "example", {
        engine: "example",
        dbinstanceClass: "example",
        engineVersion: "example",
        filters: [{ name: "example-name" }],
        licenseModel: "example",
        maxRecords: 1,
        vpc: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune import OrderableDbinstanceOptions, OrderableDbinstanceOptionsConfig

    def describe():
        ubx.intent("look up aws_neptune_orderable_dbinstance_options")

        ubx.data(OrderableDbinstanceOptions, "example", OrderableDbinstanceOptionsConfig(
            engine="example",
            dbinstance_class="example",
            engine_version="example",
            filters=[{"name": "example-name"}],
            license_model="example",
            max_records=1,
            vpc=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_class" type="string">
  The real compute and memory class of the database instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string" required>
  The real database or compute engine this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The real version of the database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="license_model" type="string">
  The real database license model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc" type="bool">
  The real VPC this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="orderable_dbinstance_options" type="list(object)">
  The real, orderable database instance configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
