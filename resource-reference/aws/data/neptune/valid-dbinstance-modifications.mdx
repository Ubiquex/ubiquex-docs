---
title: "aws_neptune_valid_dbinstance_modifications"
description: "`aws_neptune_valid_dbinstance_modifications` reads the real, valid modification options currently available for a Neptune DB instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/neptune"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_valid_dbinstance_modifications"})

    		ubx.Data(neptune.ValidDbinstanceModifications, "example", neptune.ValidDbinstanceModificationsConfig{
    			DbinstanceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ValidDbinstanceModifications } from "@ubx/sdk-aws/aws/data/neptune/valid_dbinstance_modifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_valid_dbinstance_modifications" });

      data(ValidDbinstanceModifications, "example", {
        dbinstanceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune import ValidDbinstanceModifications, ValidDbinstanceModificationsConfig

    def describe():
        ubx.intent("look up aws_neptune_valid_dbinstance_modifications")

        ubx.data(ValidDbinstanceModifications, "example", ValidDbinstanceModificationsConfig(
            dbinstance_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_identifier" type="string" required>
  The real, unique identifier of the database instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="valid_dbinstance_modifications_message" type="object">
  &lt;p&gt;Information about valid modifications that you can make to your DB instance. Contains the result of a successful call to the &lt;a&gt;DescribeValidDBInstanceModifications&lt;/a&gt; action. You can use this information when you call &lt;a&gt;ModifyDBInstance&lt;/a&gt;. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="storage" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
