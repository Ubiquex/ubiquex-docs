---
title: "aws_neptune_tags_for_resource"
description: "`aws_neptune_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_tags_for_resource"})

    		ubx.Data(neptune.TagsForResource, "example", neptune.TagsForResourceConfig{
    			ResourceName: "example-resource-name",
    			Filters:      []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForResource } from "@ubx/sdk-aws/aws/data/neptune/tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_tags_for_resource" });

      data(TagsForResource, "example", {
        resourceName: "example-resource-name",
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune import TagsForResource, TagsForResourceConfig

    def describe():
        ubx.intent("look up aws_neptune_tags_for_resource")

        ubx.data(TagsForResource, "example", TagsForResourceConfig(
            resource_name="example-resource-name",
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tag_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
