---
title: "aws_polly_lexicons"
description: "`aws_polly_lexicons` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_polly_lexicons` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	polly "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/polly"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_polly_lexicons"})

    		ubx.Data(polly.Lexicons, "example", polly.LexiconsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Lexicons } from "@ubx/sdk-aws/aws/data/polly/lexicons";

    export default stack("example", () => {
      intent({ summary: "look up aws_polly_lexicons" });

      data(Lexicons, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.polly import Lexicons, LexiconsConfig

    def describe():
        ubx.intent("look up aws_polly_lexicons")

        ubx.data(Lexicons, "example", LexiconsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="lexicons" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
