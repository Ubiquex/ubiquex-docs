---
title: "aws_polly_voices"
description: "`aws_polly_voices` reads the real, available Polly voices."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	polly "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/polly"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_polly_voices"})

    		ubx.Data(polly.Voices, "example", polly.VoicesConfig{
    			Engine:                         "example",
    			IncludeAdditionalLanguageCodes: true,
    			LanguageCode:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Voices } from "@ubx/sdk-aws/aws/data/polly/voices";

    export default stack("example", () => {
      intent({ summary: "look up aws_polly_voices" });

      data(Voices, "example", {
        engine: "example",
        includeAdditionalLanguageCodes: true,
        languageCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.polly import Voices, VoicesConfig

    def describe():
        ubx.intent("look up aws_polly_voices")

        ubx.data(Voices, "example", VoicesConfig(
            engine="example",
            include_additional_language_codes=True,
            language_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine" type="string">
  The real database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_additional_language_codes" type="bool">
  Whether additional language codes are included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="language_code" type="string">
  The real, unique code identifying the language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="voices" type="list(object)">
  The real Amazon Polly voices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
