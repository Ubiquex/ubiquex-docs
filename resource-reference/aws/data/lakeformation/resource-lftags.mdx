---
title: "aws_lakeformation_resource_lftags"
description: "`aws_lakeformation_resource_lftags` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_resource_lftags` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_resource_lftags"})

    		ubx.Data(lakeformation.ResourceLftags, "example", lakeformation.ResourceLftagsConfig{
    			Resource:           map[string]any{"catalog": map[string]any{"id": "example"}},
    			CatalogId:          "example",
    			ShowAssignedLftags: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceLftags } from "@ubx/sdk-aws/aws/data/lakeformation/resource_lftags";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_resource_lftags" });

      data(ResourceLftags, "example", {
        resource: { catalog: { id: "example" } },
        catalogId: "example",
        showAssignedLftags: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import ResourceLftags, ResourceLftagsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_resource_lftags")

        ubx.data(ResourceLftags, "example", ResourceLftagsConfig(
            resource={"catalog": {"id": "example"}},
            catalog_id="example",
            show_assigned_lftags=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource" type="object" required>
  &lt;p&gt;A structure for the resource.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="catalog" type="object">
      &lt;p&gt;A structure for the catalog object.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_cells_filter" type="object">
      &lt;p&gt;A structure for a data cells filter resource. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="database_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="table_catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="table_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_location" type="object">
      &lt;p&gt;A structure for a data location object where permissions are granted or revoked. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="resource_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database" type="object">
      &lt;p&gt;A structure for the database object.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lftag" type="object">
      &lt;p&gt;A structure containing an LF-tag key and values for a resource.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="tag_key" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="tag_values" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lftag_expression" type="object">
      &lt;p&gt;A structure containing a LF-Tag expression (keys and values).&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lftag_policy" type="object">
      &lt;p&gt;A structure containing a list of LF-tag conditions or saved LF-Tag expressions that apply to a resource's LF-tag policy.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="expression" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="expression_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="resource_type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="table" type="object">
      &lt;p&gt;A structure for the table object. A table is a metadata definition that represents your data. You can Grant and Revoke table privileges to a principal. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="database_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="table_wildcard" type="any">
          &lt;p&gt;A wildcard object representing every table under a database.&lt;/p&gt; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="table_with_columns" type="object">
      &lt;p&gt;A structure for a table with columns object. This object is only used when granting a SELECT permission.&lt;/p&gt; &lt;p&gt;This object must take a value for at least one of &lt;code&gt;ColumnsNames&lt;/code&gt;, &lt;code&gt;ColumnsIndexes&lt;/code&gt;, or &lt;code&gt;ColumnsWildcard&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="column_names" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="column_wildcard" type="object">
          &lt;p&gt;A wildcard object, consisting of an optional list of excluded column names or indexes.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="excluded_column_names" type="list(string)">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="database_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="show_assigned_lftags" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lftag_on_database" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="lftags_on_columns" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="lftags_on_table" type="list(object)">
  Returned by the lookup.
</ResponseField>
