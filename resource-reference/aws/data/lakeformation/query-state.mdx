---
title: "aws_lakeformation_query_state"
description: "`aws_lakeformation_query_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_query_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_query_state"})

    		ubx.Data(lakeformation.QueryState, "example", lakeformation.QueryStateConfig{
    			QueryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueryState } from "@ubx/sdk-aws/aws/data/lakeformation/query_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_query_state" });

      data(QueryState, "example", {
        queryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import QueryState, QueryStateConfig

    def describe():
        ubx.intent("look up aws_lakeformation_query_state")

        ubx.data(QueryState, "example", QueryStateConfig(
            query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>
