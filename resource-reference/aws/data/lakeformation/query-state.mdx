---
title: "aws_lakeformation_query_state"
description: "`aws_lakeformation_query_state` reads the real, current state of a Lake Formation governed table query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_query_state"})

    		ubx.Data(lakeformation.QueryState, "example", lakeformation.QueryStateConfig{
    			QueryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { QueryState } from "./local-sdk/aws/sdk/typescript/aws/data/lakeformation/query_state.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_query_state" });

      data(QueryState, "example", {
        queryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import QueryState, QueryStateConfig

    def describe():
        ubx.intent("look up aws_lakeformation_query_state")

        ubx.data(QueryState, "example", QueryStateConfig(
            query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query_id" type="string" required>
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error" type="string">
  The real error encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
