---
title: "aws_lakeformation_data_lake_settings"
description: "`aws_lakeformation_data_lake_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_data_lake_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_data_lake_settings"})

    		ubx.Data(lakeformation.DataLakeSettings, "example", lakeformation.DataLakeSettingsConfig{
    			CatalogId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataLakeSettings } from "@ubx/sdk-aws/aws/data/lakeformation/data_lake_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_data_lake_settings" });

      data(DataLakeSettings, "example", {
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import DataLakeSettings, DataLakeSettingsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_data_lake_settings")

        ubx.data(DataLakeSettings, "example", DataLakeSettingsConfig(
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_lake_settings" type="object">
  &lt;p&gt;A structure representing a list of Lake Formation principals designated as data lake administrators and lists of principal permission entries for default create database and default create table permissions.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allow_external_data_filtering" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="allow_full_table_external_data_access" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="authorized_session_tag_value_list" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="create_database_default_permissions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="create_table_default_permissions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="data_lake_admins" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="external_data_filtering_allow_list" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parameters" type="map(string)">
      &lt;p&gt;A key-value map that provides additional configuration for Lake Formation. Supported keys include:&lt;/p&gt; &lt;ul&gt; &lt;li&gt;&lt;code&gt;CROSS_ACCOUNT_VERSION&lt;/code&gt; - Specifies the cross-account access version.&lt;/li&gt; &lt;li&gt;&lt;code&gt;SET_SOURCE_IDENTITY&lt;/code&gt; - When set to "TRUE", enables inclusion of the IAM role identifier that was used to query(sourceIdentity) in CloudTrail logs for S3 data access events. This helps track which IAM role (such as Athena or EMR job role) accessed table data. Requires the &lt;code&gt;sts:SetSourceIdentity&lt;/code&gt; permission in the trust policy of the IAM role used to register S3 buckets with Lake Formation. Default is "FALSE".&lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="read_only_admins" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="trusted_resource_owners" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
