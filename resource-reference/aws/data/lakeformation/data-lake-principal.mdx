---
title: "aws_lakeformation_data_lake_principal"
description: "`aws_lakeformation_data_lake_principal` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_data_lake_principal` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_data_lake_principal"})

    		ubx.Data(lakeformation.DataLakePrincipal, "example", lakeformation.DataLakePrincipalConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataLakePrincipal } from "@ubx/sdk-aws/aws/data/lakeformation/data_lake_principal";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_data_lake_principal" });

      data(DataLakePrincipal, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import DataLakePrincipal, DataLakePrincipalConfig

    def describe():
        ubx.intent("look up aws_lakeformation_data_lake_principal")

        ubx.data(DataLakePrincipal, "example", DataLakePrincipalConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="identity" type="string">
  Returned by the lookup.
</ResponseField>
