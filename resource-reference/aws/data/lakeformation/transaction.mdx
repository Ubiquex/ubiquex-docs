---
title: "aws_lakeformation_transaction"
description: "`aws_lakeformation_transaction` reads the real, current status of a Lake Formation governed table transaction."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_transaction"})

    		ubx.Data(lakeformation.Transaction, "example", lakeformation.TransactionConfig{
    			TransactionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Transaction } from "@ubx/sdk-aws/aws/data/lakeformation/transaction";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_transaction" });

      data(Transaction, "example", {
        transactionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import Transaction, TransactionConfig

    def describe():
        ubx.intent("look up aws_lakeformation_transaction")

        ubx.data(Transaction, "example", TransactionConfig(
            transaction_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="transaction_id" type="string" required>
  The real, unique identifier of the transaction this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="transaction_description" type="object">
  &lt;p&gt;A structure that contains information about a transaction.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="transaction_end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transaction_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transaction_start_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transaction_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
