---
title: "aws_lakeformation_temporary_glue_table_credentials"
description: "`aws_lakeformation_temporary_glue_table_credentials` reads real, temporary..."
---

`aws_lakeformation_temporary_glue_table_credentials` reads real, temporary, permission-scoped credentials for accessing a Glue table through Lake Formation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_temporary_glue_table_credentials"})

    		ubx.Data(lakeformation.TemporaryGlueTableCredentials, "example", lakeformation.TemporaryGlueTableCredentialsConfig{
    			TableArn:                 "arn:aws:iam::123456789012:policy/example",
    			AuditContext:             map[string]any{"additional_audit_context": "example"},
    			DurationSeconds:          7200,
    			Permissions:              []string{"example"},
    			QuerySessionContext:      map[string]any{"additional_context": map[string]string{"key": "value"}},
    			S3Path:                   "example",
    			SupportedPermissionTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TemporaryGlueTableCredentials } from "./local-sdk/aws/sdk/typescript/aws/data/lakeformation/temporary_glue_table_credentials.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_lakeformation_temporary_glue_table_credentials",
      });

      data(TemporaryGlueTableCredentials, "example", {
        tableArn: "arn:aws:iam::123456789012:policy/example",
        auditContext: { additionalAuditContext: "example" },
        durationSeconds: 7200,
        permissions: ["example"],
        querySessionContext: { additionalContext: { key: "value" } },
        s3Path: "example",
        supportedPermissionTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import TemporaryGlueTableCredentials, TemporaryGlueTableCredentialsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_temporary_glue_table_credentials")

        ubx.data(TemporaryGlueTableCredentials, "example", TemporaryGlueTableCredentialsConfig(
            table_arn="arn:aws:iam::123456789012:policy/example",
            audit_context={"additional_audit_context": "example"},
            duration_seconds=7200,
            permissions=["example"],
            query_session_context={"additional_context": {"key": "value"}},
            s3_path="example",
            supported_permission_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audit_context" type="object">
  &lt;p&gt;A structure used to include auditing information on the privileged API. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="additional_audit_context" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="permissions" type="list(string)">
  The real permissions granted on this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_session_context" type="object">
  &lt;p&gt;A structure used as a protocol between query engines and Lake Formation or Glue. Contains both a Lake Formation generated authorization identifier and information from the request's authorization context.&lt;/p&gt; &lt;p&gt;For more information about how to utilize QuerySessionContext, see &lt;a href="https://docs.aws.amazon.com/lake-formation/latest/dg/api-overview.html"&gt;Lake Formation workflow for application integration API operations&lt;/a&gt; in the developer guide.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="additional_context" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cluster_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_authorization_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_start_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="s3_path" type="string">
  The real path of the s3. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="supported_permission_types" type="list(string)">
  The real permission types supported by this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_key_id" type="string">
  The real AWS access key ID this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="secret_access_key" type="string">
  The real, unique key of the secret access this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_token" type="string">
  The real, opaque session token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vended_s3_path" type="list(string)">
  The real path of the vended s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>
