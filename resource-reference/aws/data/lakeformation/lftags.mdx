---
title: "aws_lakeformation_lftags"
description: "`aws_lakeformation_lftags` looks up a real LF-Tag, a key-value label used to grant Lake Formation permissions at scale."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_lftags"})

    		ubx.Data(lakeformation.Lftags, "example", lakeformation.LftagsConfig{
    			CatalogId:         "example",
    			MaxResults:        1,
    			ResourceShareType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Lftags } from "./local-sdk/aws/sdk/typescript/aws/data/lakeformation/lftags.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_lftags" });

      data(Lftags, "example", {
        catalogId: "example",
        maxResults: 1,
        resourceShareType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import Lftags, LftagsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_lftags")

        ubx.data(Lftags, "example", LftagsConfig(
            catalog_id="example",
            max_results=1,
            resource_share_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_type" type="string">
  The real, discriminating type of the resource share this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lftags" type="list(object)">
  The real Lake Formation LF-tags included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
