---
title: "aws_lakeformation_table_objects"
description: "`aws_lakeformation_table_objects` reads the real, individual data file objects registered for a Lake Formation governed table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_table_objects"})

    		ubx.Data(lakeformation.TableObjects, "example", lakeformation.TableObjectsConfig{
    			DatabaseName:       "example-database-name",
    			TableName:          "example-table-name",
    			CatalogId:          "example",
    			MaxResults:         1,
    			PartitionPredicate: "example",
    			QueryAsOfTime:      "example",
    			TransactionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TableObjects } from "@ubx/sdk-aws/aws/data/lakeformation/table_objects";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_table_objects" });

      data(TableObjects, "example", {
        databaseName: "example-database-name",
        tableName: "example-table-name",
        catalogId: "example",
        maxResults: 1,
        partitionPredicate: "example",
        queryAsOfTime: "example",
        transactionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import TableObjects, TableObjectsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_table_objects")

        ubx.data(TableObjects, "example", TableObjectsConfig(
            database_name="example-database-name",
            table_name="example-table-name",
            catalog_id="example",
            max_results=1,
            partition_predicate="example",
            query_as_of_time="example",
            transaction_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="partition_predicate" type="string">
  The real predicate expression used to select partitions. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_as_of_time" type="string">
  The real timestamp of the query as of. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="transaction_id" type="string">
  The real, unique identifier of the transaction this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="objects" type="list(object)">
  The real objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
