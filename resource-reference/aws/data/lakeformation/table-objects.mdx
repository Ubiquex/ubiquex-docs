---
title: "aws_lakeformation_table_objects"
description: "`aws_lakeformation_table_objects` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_table_objects` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_table_objects"})

    		ubx.Data(lakeformation.TableObjects, "example", lakeformation.TableObjectsConfig{
    			DatabaseName:       "example-database-name",
    			TableName:          "example-table-name",
    			CatalogId:          "example",
    			MaxResults:         1,
    			PartitionPredicate: "example",
    			QueryAsOfTime:      "example",
    			TransactionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TableObjects } from "@ubx/sdk-aws/aws/data/lakeformation/table_objects";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_table_objects" });

      data(TableObjects, "example", {
        databaseName: "example-database-name",
        tableName: "example-table-name",
        catalogId: "example",
        maxResults: 1,
        partitionPredicate: "example",
        queryAsOfTime: "example",
        transactionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import TableObjects, TableObjectsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_table_objects")

        ubx.data(TableObjects, "example", TableObjectsConfig(
            database_name="example-database-name",
            table_name="example-table-name",
            catalog_id="example",
            max_results=1,
            partition_predicate="example",
            query_as_of_time="example",
            transaction_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="partition_predicate" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_as_of_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="transaction_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="objects" type="list(object)">
  Returned by the lookup.
</ResponseField>
