---
title: "aws_lakeformation_work_unit_results"
description: "`aws_lakeformation_work_unit_results` reads the real result data of a work unit within a Lake Formation governed table query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_work_unit_results"})

    		ubx.Data(lakeformation.WorkUnitResults, "example", lakeformation.WorkUnitResultsConfig{
    			QueryId:       "example",
    			WorkUnitId:    1,
    			WorkUnitToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkUnitResults } from "@ubx/sdk-aws/aws/data/lakeformation/work_unit_results";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_work_unit_results" });

      data(WorkUnitResults, "example", {
        queryId: "example",
        workUnitId: 1,
        workUnitToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import WorkUnitResults, WorkUnitResultsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_work_unit_results")

        ubx.data(WorkUnitResults, "example", WorkUnitResultsConfig(
            query_id="example",
            work_unit_id=1,
            work_unit_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query_id" type="string" required>
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="work_unit_id" type="number" required>
  The real, unique identifier of the work unit this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="work_unit_token" type="string" required>
  The real, opaque work unit token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="result_stream" type="string">
  The real, streamed content of this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
