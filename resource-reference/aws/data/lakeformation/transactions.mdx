---
title: "aws_lakeformation_transactions"
description: "`aws_lakeformation_transactions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lakeformation_transactions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_transactions"})

    		ubx.Data(lakeformation.Transactions, "example", lakeformation.TransactionsConfig{
    			CatalogId:    "example",
    			MaxResults:   1,
    			StatusFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Transactions } from "@ubx/sdk-aws/aws/data/lakeformation/transactions";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_transactions" });

      data(Transactions, "example", {
        catalogId: "example",
        maxResults: 1,
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import Transactions, TransactionsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_transactions")

        ubx.data(Transactions, "example", TransactionsConfig(
            catalog_id="example",
            max_results=1,
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transactions" type="list(object)">
  Returned by the lookup.
</ResponseField>
