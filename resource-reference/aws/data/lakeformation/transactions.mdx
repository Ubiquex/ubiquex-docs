---
title: "aws_lakeformation_transactions"
description: "`aws_lakeformation_transactions` reads the real transactions run against Lake Formation governed tables."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_transactions"})

    		ubx.Data(lakeformation.Transactions, "example", lakeformation.TransactionsConfig{
    			CatalogId:    "example",
    			MaxResults:   1,
    			StatusFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Transactions } from "@ubx/sdk-aws/aws/data/lakeformation/transactions";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_transactions" });

      data(Transactions, "example", {
        catalogId: "example",
        maxResults: 1,
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import Transactions, TransactionsConfig

    def describe():
        ubx.intent("look up aws_lakeformation_transactions")

        ubx.data(Transactions, "example", TransactionsConfig(
            catalog_id="example",
            max_results=1,
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="string">
  Restricts results to objects matching this status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="transactions" type="list(object)">
  The real transactions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
