---
title: "aws_lakeformation_effective_permissions_for_path"
description: "`aws_lakeformation_effective_permissions_for_path` reads the real, effective Lake Formation permissions applying to an Amazon S3 path."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_effective_permissions_for_path"})

    		ubx.Data(lakeformation.EffectivePermissionsForPath, "example", lakeformation.EffectivePermissionsForPathConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			CatalogId:   "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EffectivePermissionsForPath } from "@ubx/sdk-aws/aws/data/lakeformation/effective_permissions_for_path";

    export default stack("example", () => {
      intent({
        summary: "look up aws_lakeformation_effective_permissions_for_path",
      });

      data(EffectivePermissionsForPath, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        catalogId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import EffectivePermissionsForPath, EffectivePermissionsForPathConfig

    def describe():
        ubx.intent("look up aws_lakeformation_effective_permissions_for_path")

        ubx.data(EffectivePermissionsForPath, "example", EffectivePermissionsForPathConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            catalog_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
