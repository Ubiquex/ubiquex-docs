---
title: "aws_lakeformation_table_storage_optimizers"
description: "`aws_lakeformation_table_storage_optimizers` reads the real storage optimizers (such as compaction) enabled for a Lake Formation governed table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lakeformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lakeformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lakeformation_table_storage_optimizers"})

    		ubx.Data(lakeformation.TableStorageOptimizers, "example", lakeformation.TableStorageOptimizersConfig{
    			DatabaseName:         "example-database-name",
    			TableName:            "example-table-name",
    			CatalogId:            "example",
    			MaxResults:           1,
    			StorageOptimizerType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TableStorageOptimizers } from "@ubx/sdk-aws/aws/data/lakeformation/table_storage_optimizers";

    export default stack("example", () => {
      intent({ summary: "look up aws_lakeformation_table_storage_optimizers" });

      data(TableStorageOptimizers, "example", {
        databaseName: "example-database-name",
        tableName: "example-table-name",
        catalogId: "example",
        maxResults: 1,
        storageOptimizerType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lakeformation import TableStorageOptimizers, TableStorageOptimizersConfig

    def describe():
        ubx.intent("look up aws_lakeformation_table_storage_optimizers")

        ubx.data(TableStorageOptimizers, "example", TableStorageOptimizersConfig(
            database_name="example-database-name",
            table_name="example-table-name",
            catalog_id="example",
            max_results=1,
            storage_optimizer_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="storage_optimizer_type" type="string">
  The real, discriminating type of the storage optimizer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="storage_optimizer_list" type="list(object)">
  The real storage optimizers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
