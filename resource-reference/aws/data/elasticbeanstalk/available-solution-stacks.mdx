---
title: "aws_elasticbeanstalk_available_solution_stacks"
description: "`aws_elasticbeanstalk_available_solution_stacks` reads the real, available Elastic Beanstalk solution stacks (platform and OS combinations)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_available_solution_stacks"})

    		ubx.Data(elasticbeanstalk.AvailableSolutionStacks, "example", elasticbeanstalk.AvailableSolutionStacksConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailableSolutionStacks } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/available_solution_stacks";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_available_solution_stacks" });

      data(AvailableSolutionStacks, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import AvailableSolutionStacks, AvailableSolutionStacksConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_available_solution_stacks")

        ubx.data(AvailableSolutionStacks, "example", AvailableSolutionStacksConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="solution_stack_details" type="list(object)">
  The real, detailed information about the solution stack. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="solution_stacks" type="list(string)">
  The real Elastic Beanstalk solution stacks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
