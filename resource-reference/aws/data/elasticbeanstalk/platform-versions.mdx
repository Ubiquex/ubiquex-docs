---
title: "aws_elasticbeanstalk_platform_versions"
description: "`aws_elasticbeanstalk_platform_versions` reads the real, available Elastic Beanstalk platform versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_platform_versions"})

    		ubx.Data(elasticbeanstalk.PlatformVersions, "example", elasticbeanstalk.PlatformVersionsConfig{
    			Filters:    []map[string]any{{"operator": "example"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlatformVersions } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/platform_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_platform_versions" });

      data(PlatformVersions, "example", {
        filters: [{ operator: "example" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import PlatformVersions, PlatformVersionsConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_platform_versions")

        ubx.data(PlatformVersions, "example", PlatformVersionsConfig(
            filters=[{"operator": "example"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platform_summary_list" type="list(object)">
  The real platform summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
