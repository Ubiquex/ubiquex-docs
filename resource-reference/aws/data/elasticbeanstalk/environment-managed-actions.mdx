---
title: "aws_elasticbeanstalk_environment_managed_actions"
description: "`aws_elasticbeanstalk_environment_managed_actions` reads the real, scheduled managed platform updates pending for an Elastic Beanstalk environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_environment_managed_actions"})

    		ubx.Data(elasticbeanstalk.EnvironmentManagedActions, "example", elasticbeanstalk.EnvironmentManagedActionsConfig{
    			EnvironmentId:   "example",
    			EnvironmentName: "example-environment-name",
    			Status:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnvironmentManagedActions } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/environment_managed_actions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elasticbeanstalk_environment_managed_actions",
      });

      data(EnvironmentManagedActions, "example", {
        environmentId: "example",
        environmentName: "example-environment-name",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import EnvironmentManagedActions, EnvironmentManagedActionsConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_environment_managed_actions")

        ubx.data(EnvironmentManagedActions, "example", EnvironmentManagedActionsConfig(
            environment_id="example",
            environment_name="example-environment-name",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string">
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string">
  The real name of the environment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_actions" type="list(object)">
  The real, managed Elastic Beanstalk actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
