---
title: "aws_elasticbeanstalk_account_attributes"
description: "`aws_elasticbeanstalk_account_attributes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticbeanstalk_account_attributes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_account_attributes"})

    		ubx.Data(elasticbeanstalk.AccountAttributes, "example", elasticbeanstalk.AccountAttributesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAttributes } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/account_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_account_attributes" });

      data(AccountAttributes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="resource_quotas" type="object">
  &lt;p&gt;A set of per-resource AWS Elastic Beanstalk quotas associated with an AWS account. They reflect Elastic Beanstalk resource limits for this account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_quota" type="object">
      &lt;p&gt;The AWS Elastic Beanstalk quota information for a single resource type in an AWS account. It reflects the resource's limits for this account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="application_version_quota" type="object">
      &lt;p&gt;The AWS Elastic Beanstalk quota information for a single resource type in an AWS account. It reflects the resource's limits for this account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="configuration_template_quota" type="object">
      &lt;p&gt;The AWS Elastic Beanstalk quota information for a single resource type in an AWS account. It reflects the resource's limits for this account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_platform_quota" type="object">
      &lt;p&gt;The AWS Elastic Beanstalk quota information for a single resource type in an AWS account. It reflects the resource's limits for this account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_quota" type="object">
      &lt;p&gt;The AWS Elastic Beanstalk quota information for a single resource type in an AWS account. It reflects the resource's limits for this account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
