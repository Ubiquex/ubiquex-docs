---
title: "aws_elasticbeanstalk_instances_health"
description: "`aws_elasticbeanstalk_instances_health` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticbeanstalk_instances_health` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_instances_health"})

    		ubx.Data(elasticbeanstalk.InstancesHealth, "example", elasticbeanstalk.InstancesHealthConfig{
    			AttributeNames:  []string{"example"},
    			EnvironmentId:   "example",
    			EnvironmentName: "example-environment-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstancesHealth } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/instances_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_instances_health" });

      data(InstancesHealth, "example", {
        attributeNames: ["example"],
        environmentId: "example",
        environmentName: "example-environment-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import InstancesHealth, InstancesHealthConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_instances_health")

        ubx.data(InstancesHealth, "example", InstancesHealthConfig(
            attribute_names=["example"],
            environment_id="example",
            environment_name="example-environment-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="environment_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_health_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="refreshed_at" type="string">
  Returned by the lookup.
</ResponseField>
