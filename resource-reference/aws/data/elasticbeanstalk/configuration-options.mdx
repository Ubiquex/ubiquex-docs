---
title: "aws_elasticbeanstalk_configuration_options"
description: "`aws_elasticbeanstalk_configuration_options` reads the real, available configuration options for an Elastic Beanstalk environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_configuration_options"})

    		ubx.Data(elasticbeanstalk.ConfigurationOptions, "example", elasticbeanstalk.ConfigurationOptionsConfig{
    			ApplicationName: "example-application-name",
    			EnvironmentName: "example-environment-name",
    			TemplateName:    "example-template-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConfigurationOptions } from "./local-sdk/aws/sdk/typescript/aws/data/elasticbeanstalk/configuration_options.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_configuration_options" });

      data(ConfigurationOptions, "example", {
        applicationName: "example-application-name",
        environmentName: "example-environment-name",
        templateName: "example-template-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import ConfigurationOptions, ConfigurationOptionsConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_configuration_options")

        ubx.data(ConfigurationOptions, "example", ConfigurationOptionsConfig(
            application_name="example-application-name",
            environment_name="example-environment-name",
            template_name="example-template-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_name" type="string">
  The real name of the application this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string">
  The real name of the environment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_name" type="string">
  The real name of the template this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="options" type="list(object)">
  The real, available options for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platform_arn" type="string">
  The real Amazon Resource Name (ARN) of the platform this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="solution_stack_name" type="string">
  The real name of the solution stack this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
