---
title: "aws_elasticbeanstalk_platform_branches"
description: "`aws_elasticbeanstalk_platform_branches` reads the real, available Elastic Beanstalk platform branches."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticbeanstalk_platform_branches"})

    		ubx.Data(elasticbeanstalk.PlatformBranches, "example", elasticbeanstalk.PlatformBranchesConfig{
    			Filters:    []map[string]any{{"attribute": "example"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlatformBranches } from "@ubx/sdk-aws/aws/data/elasticbeanstalk/platform_branches";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticbeanstalk_platform_branches" });

      data(PlatformBranches, "example", {
        filters: [{ attribute: "example" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticbeanstalk import PlatformBranches, PlatformBranchesConfig

    def describe():
        ubx.intent("look up aws_elasticbeanstalk_platform_branches")

        ubx.data(PlatformBranches, "example", PlatformBranchesConfig(
            filters=[{"attribute": "example"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platform_branch_summary_list" type="list(object)">
  The real platform branch summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
