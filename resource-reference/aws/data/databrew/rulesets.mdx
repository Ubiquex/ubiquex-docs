---
title: "aws_databrew_rulesets"
description: "`aws_databrew_rulesets` looks up a real DataBrew ruleset, a set of data quality rules evaluated against a dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	databrew "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/databrew"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_databrew_rulesets"})

    		ubx.Data(databrew.Rulesets, "example", databrew.RulesetsConfig{
    			MaxResults: 1,
    			TargetArn:  "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Rulesets } from "./local-sdk/aws/sdk/typescript/aws/data/databrew/rulesets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_databrew_rulesets" });

      data(Rulesets, "example", {
        maxResults: 1,
        targetArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.databrew import Rulesets, RulesetsConfig

    def describe():
        ubx.intent("look up aws_databrew_rulesets")

        ubx.data(Rulesets, "example", RulesetsConfig(
            max_results=1,
            target_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_arn" type="string">
  The real Amazon Resource Name (ARN) of the target this action applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rulesets" type="list(object)">
  The real rule sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
