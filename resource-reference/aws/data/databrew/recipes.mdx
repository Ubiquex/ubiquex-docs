---
title: "aws_databrew_recipes"
description: "`aws_databrew_recipes` looks up a real DataBrew recipe, a saved sequence of data transformation steps."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	databrew "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/databrew"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_databrew_recipes"})

    		ubx.Data(databrew.Recipes, "example", databrew.RecipesConfig{
    			MaxResults:    1,
    			RecipeVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Recipes } from "./local-sdk/aws/sdk/typescript/aws/data/databrew/recipes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_databrew_recipes" });

      data(Recipes, "example", {
        maxResults: 1,
        recipeVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.databrew import Recipes, RecipesConfig

    def describe():
        ubx.intent("look up aws_databrew_recipes")

        ubx.data(Recipes, "example", RecipesConfig(
            max_results=1,
            recipe_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recipe_version" type="string">
  The real version of the recipe. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recipes" type="list(object)">
  The real recipes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
