---
title: "aws_comprehend_document_classifier_summaries"
description: "`data_aws_comprehend_document_classifier_summaries` looks up a real, already-existing `aws_comprehend_document_classifier`."
---

It trains a real, custom model on caller-labeled documents, plain text, PDF, Word, or images, to categorize new documents into the caller's own classes, single-label or multi-label, run synchronously in real time or as a batch job against a whole document set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehend_document_classifier_summaries"})

    		ubx.Data(comprehend.DocumentClassifierSummaries, "example", comprehend.DocumentClassifierSummariesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentClassifierSummaries } from "@ubx/sdk-aws/aws/data/comprehend/document_classifier_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehend_document_classifier_summaries" });

      data(DocumentClassifierSummaries, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.comprehend import DocumentClassifierSummaries, DocumentClassifierSummariesConfig

    def describe():
        ubx.intent("look up aws_comprehend_document_classifier_summaries")

        ubx.data(DocumentClassifierSummaries, "example", DocumentClassifierSummariesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document_classifier_summaries_list" type="list(object)">
  The real document classifier summariess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
