---
title: "aws_comprehend_entity_recognizer_summaries"
description: "`aws_comprehend_entity_recognizer_summaries` reads a real, summarized listing of Comprehend custom entity recognizers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehend_entity_recognizer_summaries"})

    		ubx.Data(comprehend.EntityRecognizerSummaries, "example", comprehend.EntityRecognizerSummariesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityRecognizerSummaries } from "@ubx/sdk-aws/aws/data/comprehend/entity_recognizer_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehend_entity_recognizer_summaries" });

      data(EntityRecognizerSummaries, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.comprehend import EntityRecognizerSummaries, EntityRecognizerSummariesConfig

    def describe():
        ubx.intent("look up aws_comprehend_entity_recognizer_summaries")

        ubx.data(EntityRecognizerSummaries, "example", EntityRecognizerSummariesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_recognizer_summaries_list" type="list(object)">
  The real entity recognizer summariess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
