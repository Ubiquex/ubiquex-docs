---
title: "aws_comprehend_pii_entities_detection_jobs"
description: "`aws_comprehend_pii_entities_detection_jobs` reads the real Comprehend PII entity detection jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehend_pii_entities_detection_jobs"})

    		ubx.Data(comprehend.PiiEntitiesDetectionJobs, "example", comprehend.PiiEntitiesDetectionJobsConfig{
    			Filter:     map[string]any{"job_name": "example-job-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PiiEntitiesDetectionJobs } from "./local-sdk/aws/sdk/typescript/aws/data/comprehend/pii_entities_detection_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehend_pii_entities_detection_jobs" });

      data(PiiEntitiesDetectionJobs, "example", {
        filter: { jobName: "example-job-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.comprehend import PiiEntitiesDetectionJobs, PiiEntitiesDetectionJobsConfig

    def describe():
        ubx.intent("look up aws_comprehend_pii_entities_detection_jobs")

        ubx.data(PiiEntitiesDetectionJobs, "example", PiiEntitiesDetectionJobsConfig(
            filter={"job_name": "example-job-name"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Provides information for filtering a list of PII entity detection jobs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pii_entities_detection_job_properties_list" type="list(object)">
  The real pii entities detection job propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
