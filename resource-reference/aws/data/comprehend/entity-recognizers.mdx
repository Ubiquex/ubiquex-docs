---
title: "aws_comprehend_entity_recognizers"
description: "`aws_comprehend_entity_recognizers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_comprehend_entity_recognizers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehend_entity_recognizers"})

    		ubx.Data(comprehend.EntityRecognizers, "example", comprehend.EntityRecognizersConfig{
    			Filter:     map[string]any{"recognizer_name": "example-recognizer-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityRecognizers } from "@ubx/sdk-aws/aws/data/comprehend/entity_recognizers";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehend_entity_recognizers" });

      data(EntityRecognizers, "example", {
        filter: { recognizerName: "example-recognizer-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.comprehend import EntityRecognizers, EntityRecognizersConfig

    def describe():
        ubx.intent("look up aws_comprehend_entity_recognizers")

        ubx.data(EntityRecognizers, "example", EntityRecognizersConfig(
            filter={"recognizer_name": "example-recognizer-name"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Provides information for filtering a list of entity recognizers. You can only specify one filtering parameter in a request. For more information, see the &lt;code&gt;ListEntityRecognizers&lt;/code&gt; operation./&gt;&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="recognizer_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_recognizer_properties_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
