---
title: "aws_comprehend_key_phrases_detection_jobs"
description: "`aws_comprehend_key_phrases_detection_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_comprehend_key_phrases_detection_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehend_key_phrases_detection_jobs"})

    		ubx.Data(comprehend.KeyPhrasesDetectionJobs, "example", comprehend.KeyPhrasesDetectionJobsConfig{
    			Filter:     map[string]any{"job_name": "example-job-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeyPhrasesDetectionJobs } from "@ubx/sdk-aws/aws/data/comprehend/key_phrases_detection_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehend_key_phrases_detection_jobs" });

      data(KeyPhrasesDetectionJobs, "example", {
        filter: { jobName: "example-job-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.comprehend import KeyPhrasesDetectionJobs, KeyPhrasesDetectionJobsConfig

    def describe():
        ubx.intent("look up aws_comprehend_key_phrases_detection_jobs")

        ubx.data(KeyPhrasesDetectionJobs, "example", KeyPhrasesDetectionJobsConfig(
            filter={"job_name": "example-job-name"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Provides information for filtering a list of dominant language detection jobs. For more information, see the operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_phrases_detection_job_properties_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
