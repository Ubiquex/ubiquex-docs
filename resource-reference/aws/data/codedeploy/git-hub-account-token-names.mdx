---
title: "aws_codedeploy_git_hub_account_token_names"
description: "`aws_codedeploy_git_hub_account_token_names` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codedeploy_git_hub_account_token_names` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_git_hub_account_token_names"})

    		ubx.Data(codedeploy.GitHubAccountTokenNames, "example", codedeploy.GitHubAccountTokenNamesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GitHubAccountTokenNames } from "@ubx/sdk-aws/aws/data/codedeploy/git_hub_account_token_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_git_hub_account_token_names" });

      data(GitHubAccountTokenNames, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import GitHubAccountTokenNames, GitHubAccountTokenNamesConfig

    def describe():
        ubx.intent("look up aws_codedeploy_git_hub_account_token_names")

        ubx.data(GitHubAccountTokenNames, "example", GitHubAccountTokenNamesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="token_name_list" type="list(string)">
  Returned by the lookup.
</ResponseField>
