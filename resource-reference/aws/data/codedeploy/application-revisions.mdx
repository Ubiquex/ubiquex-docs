---
title: "aws_codedeploy_application_revisions"
description: "`aws_codedeploy_application_revisions` reads the real, registered revisions for a CodeDeploy application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_application_revisions"})

    		ubx.Data(codedeploy.ApplicationRevisions, "example", codedeploy.ApplicationRevisionsConfig{
    			ApplicationName: "example-application-name",
    			Deployed:        "example",
    			S3Bucket:        "example",
    			S3KeyPrefix:     "example",
    			SortBy:          "example",
    			SortOrder:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationRevisions } from "@ubx/sdk-aws/aws/data/codedeploy/application_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_application_revisions" });

      data(ApplicationRevisions, "example", {
        applicationName: "example-application-name",
        deployed: "example",
        s3Bucket: "example",
        s3KeyPrefix: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import ApplicationRevisions, ApplicationRevisionsConfig

    def describe():
        ubx.intent("look up aws_codedeploy_application_revisions")

        ubx.data(ApplicationRevisions, "example", ApplicationRevisionsConfig(
            application_name="example-application-name",
            deployed="example",
            s3_bucket="example",
            s3_key_prefix="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_name" type="string" required>
  The real name of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="deployed" type="string">
  Whether this object has been deployed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="s3_bucket" type="string">
  The real S3 bucket used by this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  The real S3 key prefix used by this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revisions" type="list(object)">
  The real revisions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
