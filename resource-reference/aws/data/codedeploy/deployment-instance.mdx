---
title: "aws_codedeploy_deployment_instance"
description: "`aws_codedeploy_deployment_instance` looks up one real, targeted instance within a CodeDeploy deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployment_instance"})

    		ubx.Data(codedeploy.DeploymentInstance, "example", codedeploy.DeploymentInstanceConfig{
    			DeploymentId: "example",
    			InstanceId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DeploymentInstance } from "./local-sdk/aws/sdk/typescript/aws/data/codedeploy/deployment_instance.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployment_instance" });

      data(DeploymentInstance, "example", {
        deploymentId: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import DeploymentInstance, DeploymentInstanceConfig

    def describe():
        ubx.intent("look up aws_codedeploy_deployment_instance")

        ubx.data(DeploymentInstance, "example", DeploymentInstanceConfig(
            deployment_id="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_id" type="string" required>
  The real, unique identifier of the deployment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_summary" type="object">
  &lt;p&gt;Information about an instance in a deployment.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deployment_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="lifecycle_events" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
