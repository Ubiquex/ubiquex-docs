---
title: "aws_codedeploy_deployments"
description: "`aws_codedeploy_deployments` looks up one real CodeDeploy deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployments"})

    		ubx.Data(codedeploy.Deployments, "example", codedeploy.DeploymentsConfig{
    			ApplicationName:     "example-application-name",
    			CreateTimeRange:     map[string]any{"end": "example"},
    			DeploymentGroupName: "example-deployment-group-name",
    			ExternalId:          "example",
    			IncludeOnlyStatuses: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Deployments } from "@ubx/sdk-aws/aws/data/codedeploy/deployments";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployments" });

      data(Deployments, "example", {
        applicationName: "example-application-name",
        createTimeRange: { end: "example" },
        deploymentGroupName: "example-deployment-group-name",
        externalId: "example",
        includeOnlyStatuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import Deployments, DeploymentsConfig
    from ubx.aws.data.codedeploy.deployments import Deployments_CreateTimeRange

    def describe():
        ubx.intent("look up aws_codedeploy_deployments")

        ubx.data(Deployments, "example", DeploymentsConfig(
            application_name="example-application-name",
            create_time_range=Deployments_CreateTimeRange(end="example"),
            deployment_group_name="example-deployment-group-name",
            external_id="example",
            include_only_statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_name" type="string">
  The real name of the application this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="create_time_range" type="object">
  &lt;p&gt;Information about a time range.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="end" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deployment_group_name" type="string">
  The real name of the deployment group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="external_id" type="string">
  The real, unique identifier of the external this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_only_statuses" type="list(string)">
  The real, current statuses of the include onlys included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployments" type="list(string)">
  The real deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
