---
title: "aws_codedeploy_on_premises_instance"
description: "`aws_codedeploy_on_premises_instance` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codedeploy_on_premises_instance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_on_premises_instance"})

    		ubx.Data(codedeploy.OnPremisesInstance, "example", codedeploy.OnPremisesInstanceConfig{
    			InstanceName: "example-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OnPremisesInstance } from "@ubx/sdk-aws/aws/data/codedeploy/on_premises_instance";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_on_premises_instance" });

      data(OnPremisesInstance, "example", {
        instanceName: "example-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import OnPremisesInstance, OnPremisesInstanceConfig

    def describe():
        ubx.intent("look up aws_codedeploy_on_premises_instance")

        ubx.data(OnPremisesInstance, "example", OnPremisesInstanceConfig(
            instance_name="example-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_info" type="object">
  &lt;p&gt;Information about an on-premises instance.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deregister_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_session_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_user_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="register_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
