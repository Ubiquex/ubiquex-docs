---
title: "aws_codedeploy_deployment_targets"
description: "`aws_codedeploy_deployment_targets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codedeploy_deployment_targets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployment_targets"})

    		ubx.Data(codedeploy.DeploymentTargets, "example", codedeploy.DeploymentTargetsConfig{
    			DeploymentId:  "example",
    			TargetFilters: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeploymentTargets } from "@ubx/sdk-aws/aws/data/codedeploy/deployment_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployment_targets" });

      data(DeploymentTargets, "example", {
        deploymentId: "example",
        targetFilters: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import DeploymentTargets, DeploymentTargetsConfig

    def describe():
        ubx.intent("look up aws_codedeploy_deployment_targets")

        ubx.data(DeploymentTargets, "example", DeploymentTargetsConfig(
            deployment_id="example",
            target_filters={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="target_filters" type="map(list(string))">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="target_ids" type="list(string)">
  Returned by the lookup.
</ResponseField>
