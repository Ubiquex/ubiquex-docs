---
title: "aws_codedeploy_deployment_configs"
description: "`aws_codedeploy_deployment_configs` looks up a real CodeDeploy deployment configuration, defining how fast a deployment rolls out."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployment_configs"})

    		ubx.Data(codedeploy.DeploymentConfigs, "example", codedeploy.DeploymentConfigsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeploymentConfigs } from "@ubx/sdk-aws/aws/data/codedeploy/deployment_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployment_configs" });

      data(DeploymentConfigs, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import DeploymentConfigs, DeploymentConfigsConfig

    def describe():
        ubx.intent("look up aws_codedeploy_deployment_configs")

        ubx.data(DeploymentConfigs, "example", DeploymentConfigsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="deployment_configs_list" type="list(string)">
  The real deployment configss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
