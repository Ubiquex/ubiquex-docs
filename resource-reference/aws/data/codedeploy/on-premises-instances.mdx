---
title: "aws_codedeploy_on_premises_instances"
description: "`aws_codedeploy_on_premises_instances` reads the real, on-premises instances registered with CodeDeploy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_on_premises_instances"})

    		ubx.Data(codedeploy.OnPremisesInstances, "example", codedeploy.OnPremisesInstancesConfig{
    			RegistrationStatus: "example",
    			TagFilters:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OnPremisesInstances } from "@ubx/sdk-aws/aws/data/codedeploy/on_premises_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_on_premises_instances" });

      data(OnPremisesInstances, "example", {
        registrationStatus: "example",
        tagFilters: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import OnPremisesInstances, OnPremisesInstancesConfig
    from ubx.aws.data.codedeploy.on_premises_instances import OnPremisesInstances_TagFilters

    def describe():
        ubx.intent("look up aws_codedeploy_on_premises_instances")

        ubx.data(OnPremisesInstances, "example", OnPremisesInstancesConfig(
            registration_status="example",
            tag_filters=[OnPremisesInstances_TagFilters(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="registration_status" type="string">
  The real, current status of the registration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tag_filters" type="list(object)">
  The real filters applied to the tags in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_names" type="list(string)">
  The real names of the instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
