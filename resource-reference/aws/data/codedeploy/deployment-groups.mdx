---
title: "aws_codedeploy_deployment_groups"
description: "`aws_codedeploy_deployment_groups` looks up a real CodeDeploy deployment group, a named set of instances or compute targets deployed to together."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployment_groups"})

    		ubx.Data(codedeploy.DeploymentGroups, "example", codedeploy.DeploymentGroupsConfig{
    			ApplicationName: "example-application-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeploymentGroups } from "@ubx/sdk-aws/aws/data/codedeploy/deployment_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployment_groups" });

      data(DeploymentGroups, "example", {
        applicationName: "example-application-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import DeploymentGroups, DeploymentGroupsConfig

    def describe():
        ubx.intent("look up aws_codedeploy_deployment_groups")

        ubx.data(DeploymentGroups, "example", DeploymentGroupsConfig(
            application_name="example-application-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_name" type="string" required>
  The real name of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployment_groups" type="list(string)">
  The real deployment groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
