---
title: "aws_codedeploy_applications"
description: "`aws_codedeploy_applications` looks up a real CodeDeploy application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_applications"})

    		ubx.Data(codedeploy.Applications, "example", codedeploy.ApplicationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Applications } from "@ubx/sdk-aws/aws/data/codedeploy/applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_applications" });

      data(Applications, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import Applications, ApplicationsConfig

    def describe():
        ubx.intent("look up aws_codedeploy_applications")

        ubx.data(Applications, "example", ApplicationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="applications" type="list(string)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
