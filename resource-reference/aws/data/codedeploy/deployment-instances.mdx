---
title: "aws_codedeploy_deployment_instances"
description: "`aws_codedeploy_deployment_instances` reads the real instances targeted by a CodeDeploy deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codedeploy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codedeploy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codedeploy_deployment_instances"})

    		ubx.Data(codedeploy.DeploymentInstances, "example", codedeploy.DeploymentInstancesConfig{
    			DeploymentId:         "example",
    			InstanceStatusFilter: []string{"example"},
    			InstanceTypeFilter:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DeploymentInstances } from "./local-sdk/aws/sdk/typescript/aws/data/codedeploy/deployment_instances.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codedeploy_deployment_instances" });

      data(DeploymentInstances, "example", {
        deploymentId: "example",
        instanceStatusFilter: ["example"],
        instanceTypeFilter: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codedeploy import DeploymentInstances, DeploymentInstancesConfig

    def describe():
        ubx.intent("look up aws_codedeploy_deployment_instances")

        ubx.data(DeploymentInstances, "example", DeploymentInstancesConfig(
            deployment_id="example",
            instance_status_filter=["example"],
            instance_type_filter=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_id" type="string" required>
  The real, unique identifier of the deployment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_status_filter" type="list(string)">
  Restricts results to this instance status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_type_filter" type="list(string)">
  Restricts results to this instance type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances_list" type="list(string)">
  The real instancess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
