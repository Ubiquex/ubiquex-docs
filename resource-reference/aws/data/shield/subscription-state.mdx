---
title: "aws_shield_subscription_state"
description: "`aws_shield_subscription_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_shield_subscription_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_shield_subscription_state"})

    		ubx.Data(shield.SubscriptionState, "example", shield.SubscriptionStateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionState } from "@ubx/sdk-aws/aws/data/shield/subscription_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_shield_subscription_state" });

      data(SubscriptionState, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.shield import SubscriptionState, SubscriptionStateConfig

    def describe():
        ubx.intent("look up aws_shield_subscription_state")

        ubx.data(SubscriptionState, "example", SubscriptionStateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="subscription_state" type="string">
  Returned by the lookup.
</ResponseField>
