---
title: "aws_shield_attacks"
description: "`aws_shield_attacks` reads the real DDoS attacks detected by AWS Shield."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_shield_attacks"})

    		ubx.Data(shield.Attacks, "example", shield.AttacksConfig{
    			EndTime:      map[string]any{"from_inclusive": "example"},
    			MaxResults:   1,
    			ResourceArns: []string{"example"},
    			StartTime:    map[string]any{"from_inclusive": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attacks } from "@ubx/sdk-aws/aws/data/shield/attacks";

    export default stack("example", () => {
      intent({ summary: "look up aws_shield_attacks" });

      data(Attacks, "example", {
        endTime: { fromInclusive: "example" },
        maxResults: 1,
        resourceArns: ["example"],
        startTime: { fromInclusive: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.shield import Attacks, AttacksConfig

    def describe():
        ubx.intent("look up aws_shield_attacks")

        ubx.data(Attacks, "example", AttacksConfig(
            end_time={"from_inclusive": "example"},
            max_results=1,
            resource_arns=["example"],
            start_time={"from_inclusive": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="object">
  &lt;p&gt;The time range. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="from_inclusive" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="to_exclusive" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="object">
  &lt;p&gt;The time range. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="from_inclusive" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="to_exclusive" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="attack_summaries" type="list(object)">
  The real, summarized attacks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
