---
title: "aws_shield_protection_groups"
description: "`data_aws_shield_protection_groups` looks up a real, already-existing `aws_shield_protection_group`."
---

It groups real `aws_shield_protection`-covered resources together so Shield Advanced evaluates and reports on their combined traffic pattern as one unit, useful when resources share load or are meant to be assessed together rather than individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_shield_protection_groups"})

    		ubx.Data(shield.ProtectionGroups, "example", shield.ProtectionGroupsConfig{
    			InclusionFilters: map[string]any{"aggregations": []string{"example"}},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProtectionGroups } from "@ubx/sdk-aws/aws/data/shield/protection_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_shield_protection_groups" });

      data(ProtectionGroups, "example", {
        inclusionFilters: { aggregations: ["example"] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.shield import ProtectionGroups, ProtectionGroupsConfig

    def describe():
        ubx.intent("look up aws_shield_protection_groups")

        ubx.data(ProtectionGroups, "example", ProtectionGroupsConfig(
            inclusion_filters={"aggregations": ["example"]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="inclusion_filters" type="object">
  &lt;p&gt;Narrows the set of protection groups that the call retrieves. You can retrieve a single protection group by its name and you can retrieve all protection groups that are configured with a specific pattern, aggregation, or resource type. You can provide up to one criteria per filter type. Shield Advanced returns the protection groups that exactly match all of the search criteria that you provide.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="aggregations" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="patterns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="protection_group_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="protection_groups" type="list(object)">
  The real protection groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
