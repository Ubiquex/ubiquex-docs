---
title: "aws_shield_emergency_contact_settings"
description: "`aws_shield_emergency_contact_settings` reads the real emergency contact list configured for an AWS Shield Advanced subscription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_shield_emergency_contact_settings"})

    		ubx.Data(shield.EmergencyContactSettings, "example", shield.EmergencyContactSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EmergencyContactSettings } from "@ubx/sdk-aws/aws/data/shield/emergency_contact_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_shield_emergency_contact_settings" });

      data(EmergencyContactSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.shield import EmergencyContactSettings, EmergencyContactSettingsConfig

    def describe():
        ubx.intent("look up aws_shield_emergency_contact_settings")

        ubx.data(EmergencyContactSettings, "example", EmergencyContactSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="emergency_contact_list" type="list(object)">
  The real emergency contacts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
