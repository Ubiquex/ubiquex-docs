---
title: "aws_refactor_spaces_routes"
description: "`aws_refactor_spaces_routes` looks up a real Migration Hub Refactor Spaces route, directing traffic matching a path prefix to a service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	refactor_spaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/refactor_spaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_refactor_spaces_routes"})

    		ubx.Data(refactor_spaces.RefactorSpacesRoutes, "example", refactor_spaces.RefactorSpacesRoutesConfig{
    			ApplicationIdentifier: "example",
    			EnvironmentIdentifier: "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RefactorSpacesRoutes } from "@ubx/sdk-aws/aws/data/refactor_spaces/refactor_spaces_routes";

    export default stack("example", () => {
      intent({ summary: "look up aws_refactor_spaces_routes" });

      data(RefactorSpacesRoutes, "example", {
        applicationIdentifier: "example",
        environmentIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.refactor_spaces import RefactorSpacesRoutes, RefactorSpacesRoutesConfig

    def describe():
        ubx.intent("look up aws_refactor_spaces_routes")

        ubx.data(RefactorSpacesRoutes, "example", RefactorSpacesRoutesConfig(
            application_identifier="example",
            environment_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_identifier" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment_identifier" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="route_summary_list" type="list(object)">
  The real route summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
