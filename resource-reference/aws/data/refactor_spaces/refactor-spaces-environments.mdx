---
title: "aws_refactor_spaces_environments"
description: "`data_aws_refactor_spaces_environments` looks up a real, already-existing `aws_refactor_spaces_environment`."
---

It creates a real top-level container, a shared transit gateway and default settings, that `aws_refactor_spaces_application`s under AWS Migration Hub Refactor Spaces are organized inside while an application is incrementally strangled apart into microservices. Refactor Spaces closed to new customers on November 7, 2025, a date roughly nine months in the past; existing customers can continue their migrations, with AWS maintaining security and reliability but adding no new features, and pointing new work toward AWS Transform (launched May 2025) instead.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	refactor_spaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/refactor_spaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_refactor_spaces_environments"})

    		ubx.Data(refactor_spaces.RefactorSpacesEnvironments, "example", refactor_spaces.RefactorSpacesEnvironmentsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RefactorSpacesEnvironments } from "@ubx/sdk-aws/aws/data/refactor_spaces/refactor_spaces_environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_refactor_spaces_environments" });

      data(RefactorSpacesEnvironments, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.refactor_spaces import RefactorSpacesEnvironments, RefactorSpacesEnvironmentsConfig

    def describe():
        ubx.intent("look up aws_refactor_spaces_environments")

        ubx.data(RefactorSpacesEnvironments, "example", RefactorSpacesEnvironmentsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="environment_summary_list" type="list(object)">
  The real environment summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
