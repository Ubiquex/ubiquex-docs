---
title: "aws_refactor_spaces_resource_policy"
description: "`aws_refactor_spaces_resource_policy` reads the real resource-based policy attached to a Migration Hub Refactor Spaces environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	refactor_spaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/refactor_spaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_refactor_spaces_resource_policy"})

    		ubx.Data(refactor_spaces.RefactorSpacesResourcePolicy, "example", refactor_spaces.RefactorSpacesResourcePolicyConfig{
    			Identifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RefactorSpacesResourcePolicy } from "@ubx/sdk-aws/aws/data/refactor_spaces/refactor_spaces_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_refactor_spaces_resource_policy" });

      data(RefactorSpacesResourcePolicy, "example", {
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.refactor_spaces import RefactorSpacesResourcePolicy, RefactorSpacesResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_refactor_spaces_resource_policy")

        ubx.data(RefactorSpacesResourcePolicy, "example", RefactorSpacesResourcePolicyConfig(
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
