---
title: "aws_refactor_spaces_services"
description: "`aws_refactor_spaces_services` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_refactor_spaces_services` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	refactor_spaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/refactor_spaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_refactor_spaces_services"})

    		ubx.Data(refactor_spaces.RefactorSpacesServices, "example", refactor_spaces.RefactorSpacesServicesConfig{
    			ApplicationIdentifier: "example",
    			EnvironmentIdentifier: "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RefactorSpacesServices } from "@ubx/sdk-aws/aws/data/refactor_spaces/refactor_spaces_services";

    export default stack("example", () => {
      intent({ summary: "look up aws_refactor_spaces_services" });

      data(RefactorSpacesServices, "example", {
        applicationIdentifier: "example",
        environmentIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.refactor_spaces import RefactorSpacesServices, RefactorSpacesServicesConfig

    def describe():
        ubx.intent("look up aws_refactor_spaces_services")

        ubx.data(RefactorSpacesServices, "example", RefactorSpacesServicesConfig(
            application_identifier="example",
            environment_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
