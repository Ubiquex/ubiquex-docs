---
title: "aws_refactor_spaces_applications"
description: "`data_aws_refactor_spaces_applications` looks up a real, already-existing `aws_refactor_spaces_application`."
---

It registers a real monolith application, its proxy and default route, inside an `aws_refactor_spaces_environment`, the resource that `aws_refactor_spaces_service`s are incrementally carved out of and routed to. Like the rest of Refactor Spaces, this closed to new customers November 7, 2025, with AWS recommending AWS Transform for new work.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	refactor_spaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/refactor_spaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_refactor_spaces_applications"})

    		ubx.Data(refactor_spaces.RefactorSpacesApplications, "example", refactor_spaces.RefactorSpacesApplicationsConfig{
    			EnvironmentIdentifier: "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RefactorSpacesApplications } from "./local-sdk/aws/sdk/typescript/aws/data/refactor_spaces/refactor_spaces_applications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_refactor_spaces_applications" });

      data(RefactorSpacesApplications, "example", {
        environmentIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.refactor_spaces import RefactorSpacesApplications, RefactorSpacesApplicationsConfig

    def describe():
        ubx.intent("look up aws_refactor_spaces_applications")

        ubx.data(RefactorSpacesApplications, "example", RefactorSpacesApplicationsConfig(
            environment_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_identifier" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_summary_list" type="list(object)">
  The real application summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
