---
title: "aws_license_manager_license_conversion_task"
description: "`aws_license_manager_license_conversion_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_license_conversion_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_license_conversion_task"})

    		ubx.Data(license_manager.LicenseManagerLicenseConversionTask, "example", license_manager.LicenseManagerLicenseConversionTaskConfig{
    			LicenseConversionTaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLicenseConversionTask } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_license_conversion_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_license_manager_license_conversion_task" });

      data(LicenseManagerLicenseConversionTask, "example", {
        licenseConversionTaskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerLicenseConversionTask, LicenseManagerLicenseConversionTaskConfig

    def describe():
        ubx.intent("look up aws_license_manager_license_conversion_task")

        ubx.data(LicenseManagerLicenseConversionTask, "example", LicenseManagerLicenseConversionTaskConfig(
            license_conversion_task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="license_conversion_task_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="destination_license_context" type="object">
  &lt;p&gt;Information about a license type conversion task.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="product_codes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="usage_operation" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="license_conversion_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="source_license_context" type="object">
  &lt;p&gt;Information about a license type conversion task.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="product_codes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="usage_operation" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>
