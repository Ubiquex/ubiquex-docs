---
title: "aws_license_manager_license_configurations_for_organization"
description: "`aws_license_manager_license_configurations_for_organization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_license_configurations_for_organization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_license_configurations_for_organization"})

    		ubx.Data(license_manager.LicenseManagerLicenseConfigurationsForOrganization, "example", license_manager.LicenseManagerLicenseConfigurationsForOrganizationConfig{
    			Filters:                  []map[string]any{{"name": "example-name"}},
    			LicenseConfigurationArns: []string{"example"},
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLicenseConfigurationsForOrganization } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_license_configurations_for_organization";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_license_manager_license_configurations_for_organization",
      });

      data(LicenseManagerLicenseConfigurationsForOrganization, "example", {
        filters: [{ name: "example-name" }],
        licenseConfigurationArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerLicenseConfigurationsForOrganization, LicenseManagerLicenseConfigurationsForOrganizationConfig

    def describe():
        ubx.intent("look up aws_license_manager_license_configurations_for_organization")

        ubx.data(LicenseManagerLicenseConfigurationsForOrganization, "example", LicenseManagerLicenseConfigurationsForOrganizationConfig(
            filters=[{"name": "example-name"}],
            license_configuration_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="license_configuration_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="license_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
