---
title: "aws_license_manager_access_token"
description: "`aws_license_manager_access_token` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_access_token` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_access_token"})

    		ubx.Data(license_manager.LicenseManagerAccessToken, "example", license_manager.LicenseManagerAccessTokenConfig{
    			Token:           "example",
    			TokenProperties: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerAccessToken } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_access_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_license_manager_access_token" });

      data(LicenseManagerAccessToken, "example", {
        token: "example",
        tokenProperties: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerAccessToken, LicenseManagerAccessTokenConfig

    def describe():
        ubx.intent("look up aws_license_manager_access_token")

        ubx.data(LicenseManagerAccessToken, "example", LicenseManagerAccessTokenConfig(
            token="example",
            token_properties=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="token_properties" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_token" type="string">
  Returned by the lookup.
</ResponseField>
