---
title: "aws_license_manager_license_manager_report_generators"
description: "`data_aws_license_manager_license_manager_report_generators` looks up a real, already-existing `aws_license_manager_license`."
---

It represents a real granted license for a product purchased from AWS Marketplace or from a seller integrated with AWS License Manager's managed entitlements, tracking the license's entitlements, validity period, and consumption rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_license_manager_report_generators"})

    		ubx.Data(license_manager.LicenseManagerLicenseManagerReportGenerators, "example", license_manager.LicenseManagerLicenseManagerReportGeneratorsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLicenseManagerReportGenerators } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_license_manager_report_generators";

    export default stack("example", () => {
      intent({
        summary: "look up aws_license_manager_license_manager_report_generators",
      });

      data(LicenseManagerLicenseManagerReportGenerators, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerLicenseManagerReportGenerators, LicenseManagerLicenseManagerReportGeneratorsConfig

    def describe():
        ubx.intent("look up aws_license_manager_license_manager_report_generators")

        ubx.data(LicenseManagerLicenseManagerReportGenerators, "example", LicenseManagerLicenseManagerReportGeneratorsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="report_generators" type="list(object)">
  The real report generators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
