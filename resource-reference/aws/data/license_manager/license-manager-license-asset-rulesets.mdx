---
title: "aws_license_manager_license_asset_rulesets"
description: "`data_aws_license_manager_license_asset_rulesets` looks up a real, already-existing `aws_license_manager_license`."
---

It represents a real granted license for a product purchased from AWS Marketplace or from a seller integrated with AWS License Manager's managed entitlements, tracking the license's entitlements, validity period, and consumption rules.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_license_asset_rulesets"})

    		ubx.Data(license_manager.LicenseManagerLicenseAssetRulesets, "example", license_manager.LicenseManagerLicenseAssetRulesetsConfig{
    			Filters:                            []map[string]any{{"name": "example-name"}},
    			MaxResults:                         1,
    			ShowAwsmanagedLicenseAssetRulesets: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLicenseAssetRulesets } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_license_asset_rulesets";

    export default stack("example", () => {
      intent({ summary: "look up aws_license_manager_license_asset_rulesets" });

      data(LicenseManagerLicenseAssetRulesets, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        showAwsmanagedLicenseAssetRulesets: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerLicenseAssetRulesets, LicenseManagerLicenseAssetRulesetsConfig

    def describe():
        ubx.intent("look up aws_license_manager_license_asset_rulesets")

        ubx.data(LicenseManagerLicenseAssetRulesets, "example", LicenseManagerLicenseAssetRulesetsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            show_awsmanaged_license_asset_rulesets=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="show_awsmanaged_license_asset_rulesets" type="bool">
  Whether AWS-managed License Manager asset rulesets are included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="license_asset_rulesets" type="list(object)">
  The real License Manager asset rulesets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
