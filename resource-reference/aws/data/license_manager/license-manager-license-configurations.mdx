---
title: "aws_license_manager_license_configurations"
description: "`aws_license_manager_license_configurations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_license_configurations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_license_configurations"})

    		ubx.Data(license_manager.LicenseManagerLicenseConfigurations, "example", license_manager.LicenseManagerLicenseConfigurationsConfig{
    			Filters:                  []map[string]any{{"name": "example-name"}},
    			LicenseConfigurationArns: []string{"example"},
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLicenseConfigurations } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_license_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_license_manager_license_configurations" });

      data(LicenseManagerLicenseConfigurations, "example", {
        filters: [{ name: "example-name" }],
        licenseConfigurationArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerLicenseConfigurations, LicenseManagerLicenseConfigurationsConfig

    def describe():
        ubx.intent("look up aws_license_manager_license_configurations")

        ubx.data(LicenseManagerLicenseConfigurations, "example", LicenseManagerLicenseConfigurationsConfig(
            filters=[{"name": "example-name"}],
            license_configuration_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="license_configuration_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="license_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
