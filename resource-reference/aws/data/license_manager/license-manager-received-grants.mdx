---
title: "aws_license_manager_received_grants"
description: "`data_aws_license_manager_received_grants` looks up a real, already-existing `aws_license_manager_grant`."
---

It shares a real `aws_license_manager_license`'s entitlements with a specific AWS account, organizational unit, or organization, the mechanism a license owner uses to let another account consume a license it granted from AWS Marketplace or a direct seller integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_received_grants"})

    		ubx.Data(license_manager.LicenseManagerReceivedGrants, "example", license_manager.LicenseManagerReceivedGrantsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			GrantArns:  []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerReceivedGrants } from "@ubx/sdk-aws/aws/data/license_manager/license_manager_received_grants";

    export default stack("example", () => {
      intent({ summary: "look up aws_license_manager_received_grants" });

      data(LicenseManagerReceivedGrants, "example", {
        filters: [{ name: "example-name" }],
        grantArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager import LicenseManagerReceivedGrants, LicenseManagerReceivedGrantsConfig
    from ubx.aws.data.license_manager.license_manager_received_grants import LicenseManagerReceivedGrants_Filters

    def describe():
        ubx.intent("look up aws_license_manager_received_grants")

        ubx.data(LicenseManagerReceivedGrants, "example", LicenseManagerReceivedGrantsConfig(
            filters=[LicenseManagerReceivedGrants_Filters(name="example-name")],
            grant_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="grant_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the grants this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grants" type="list(object)">
  The real grants included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
