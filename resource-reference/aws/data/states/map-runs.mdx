---
title: "aws_states_map_runs"
description: "`aws_states_map_runs` reads the real Distributed Map runs within a Step Functions state machine execution."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	states "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/states"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_states_map_runs"})

    		ubx.Data(states.MapRuns, "example", states.MapRunsConfig{
    			ExecutionArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MapRuns } from "./local-sdk/aws/sdk/typescript/aws/data/states/map_runs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_states_map_runs" });

      data(MapRuns, "example", {
        executionArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.states import MapRuns, MapRunsConfig

    def describe():
        ubx.intent("look up aws_states_map_runs")

        ubx.data(MapRuns, "example", MapRunsConfig(
            execution_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="execution_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the execution this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="map_runs" type="list(object)">
  The real AWS Step Functions Map state runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
