---
title: "aws_states_state_machine_aliases"
description: "`aws_states_state_machine_aliases` looks up a real Step Functions state machine alias, a named pointer that can split traffic between versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	states "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/states"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_states_state_machine_aliases"})

    		ubx.Data(states.StateMachineAliases, "example", states.StateMachineAliasesConfig{
    			StateMachineArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StateMachineAliases } from "@ubx/sdk-aws/aws/data/states/state_machine_aliases";

    export default stack("example", () => {
      intent({ summary: "look up aws_states_state_machine_aliases" });

      data(StateMachineAliases, "example", {
        stateMachineArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.states import StateMachineAliases, StateMachineAliasesConfig

    def describe():
        ubx.intent("look up aws_states_state_machine_aliases")

        ubx.data(StateMachineAliases, "example", StateMachineAliasesConfig(
            state_machine_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state_machine_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the state machine this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state_machine_aliases" type="list(object)">
  The real AWS Step Functions state machine aliases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
