---
title: "aws_states_state_machines"
description: "`aws_states_state_machines` looks up a real Step Functions state machine."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	states "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/states"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_states_state_machines"})

    		ubx.Data(states.StateMachines, "example", states.StateMachinesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { StateMachines } from "./local-sdk/aws/sdk/typescript/aws/data/states/state_machines.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_states_state_machines" });

      data(StateMachines, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.states import StateMachines, StateMachinesConfig

    def describe():
        ubx.intent("look up aws_states_state_machines")

        ubx.data(StateMachines, "example", StateMachinesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state_machines" type="list(object)">
  The real AWS Step Functions state machines included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
