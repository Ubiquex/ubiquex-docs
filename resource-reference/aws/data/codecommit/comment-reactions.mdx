---
title: "aws_codecommit_comment_reactions"
description: "`aws_codecommit_comment_reactions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecommit_comment_reactions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_comment_reactions"})

    		ubx.Data(codecommit.CommentReactions, "example", codecommit.CommentReactionsConfig{
    			CommentId:       "example",
    			MaxResults:      1,
    			ReactionUserArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommentReactions } from "@ubx/sdk-aws/aws/data/codecommit/comment_reactions";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_comment_reactions" });

      data(CommentReactions, "example", {
        commentId: "example",
        maxResults: 1,
        reactionUserArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import CommentReactions, CommentReactionsConfig

    def describe():
        ubx.intent("look up aws_codecommit_comment_reactions")

        ubx.data(CommentReactions, "example", CommentReactionsConfig(
            comment_id="example",
            max_results=1,
            reaction_user_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="comment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reaction_user_arn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reactions_for_comment" type="list(object)">
  Returned by the lookup.
</ResponseField>
