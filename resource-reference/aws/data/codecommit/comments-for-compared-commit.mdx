---
title: "aws_codecommit_comments_for_compared_commit"
description: "`aws_codecommit_comments_for_compared_commit` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecommit_comments_for_compared_commit` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_comments_for_compared_commit"})

    		ubx.Data(codecommit.CommentsForComparedCommit, "example", codecommit.CommentsForComparedCommitConfig{
    			AfterCommitId:  "example",
    			RepositoryName: "example-repository-name",
    			BeforeCommitId: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommentsForComparedCommit } from "@ubx/sdk-aws/aws/data/codecommit/comments_for_compared_commit";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_comments_for_compared_commit" });

      data(CommentsForComparedCommit, "example", {
        afterCommitId: "example",
        repositoryName: "example-repository-name",
        beforeCommitId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import CommentsForComparedCommit, CommentsForComparedCommitConfig

    def describe():
        ubx.intent("look up aws_codecommit_comments_for_compared_commit")

        ubx.data(CommentsForComparedCommit, "example", CommentsForComparedCommitConfig(
            after_commit_id="example",
            repository_name="example-repository-name",
            before_commit_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_commit_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="before_commit_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="comments_for_compared_commit_data" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
