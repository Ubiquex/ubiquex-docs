---
title: "aws_codecommit_pull_request_override_state"
description: "`aws_codecommit_pull_request_override_state` reads whether a real CodeCommit pull request's own approval rules have been manually overridden."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_pull_request_override_state"})

    		ubx.Data(codecommit.PullRequestOverrideState, "example", codecommit.PullRequestOverrideStateConfig{
    			PullRequestId: "example",
    			RevisionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PullRequestOverrideState } from "./local-sdk/aws/sdk/typescript/aws/data/codecommit/pull_request_override_state.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_pull_request_override_state" });

      data(PullRequestOverrideState, "example", {
        pullRequestId: "example",
        revisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import PullRequestOverrideState, PullRequestOverrideStateConfig

    def describe():
        ubx.intent("look up aws_codecommit_pull_request_override_state")

        ubx.data(PullRequestOverrideState, "example", PullRequestOverrideStateConfig(
            pull_request_id="example",
            revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pull_request_id" type="string" required>
  The real, unique identifier of the pull request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="revision_id" type="string" required>
  The real, unique identifier of this object's own revision. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="overridden" type="bool">
  Whether this value has been overridden. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="overrider" type="string">
  The real identity that overrode this value. **(AI-inferred)** Returned by the lookup.
</ResponseField>
