---
title: "aws_codecommit_pull_request_override_state"
description: "`aws_codecommit_pull_request_override_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecommit_pull_request_override_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_pull_request_override_state"})

    		ubx.Data(codecommit.PullRequestOverrideState, "example", codecommit.PullRequestOverrideStateConfig{
    			PullRequestId: "example",
    			RevisionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PullRequestOverrideState } from "@ubx/sdk-aws/aws/data/codecommit/pull_request_override_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_pull_request_override_state" });

      data(PullRequestOverrideState, "example", {
        pullRequestId: "example",
        revisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import PullRequestOverrideState, PullRequestOverrideStateConfig

    def describe():
        ubx.intent("look up aws_codecommit_pull_request_override_state")

        ubx.data(PullRequestOverrideState, "example", PullRequestOverrideStateConfig(
            pull_request_id="example",
            revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pull_request_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="revision_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="overridden" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="overrider" type="string">
  Returned by the lookup.
</ResponseField>
