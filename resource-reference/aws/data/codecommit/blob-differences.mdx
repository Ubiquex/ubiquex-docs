---
title: "aws_codecommit_blob_differences"
description: "`aws_codecommit_blob_differences` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecommit_blob_differences` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_blob_differences"})

    		ubx.Data(codecommit.BlobDifferences, "example", codecommit.BlobDifferencesConfig{
    			AfterBlobId:      "example",
    			RepositoryName:   "example-repository-name",
    			BeforeBlobId:     "example",
    			ContextLines:     1,
    			IgnoreWhitespace: true,
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BlobDifferences } from "@ubx/sdk-aws/aws/data/codecommit/blob_differences";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_blob_differences" });

      data(BlobDifferences, "example", {
        afterBlobId: "example",
        repositoryName: "example-repository-name",
        beforeBlobId: "example",
        contextLines: 1,
        ignoreWhitespace: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import BlobDifferences, BlobDifferencesConfig

    def describe():
        ubx.intent("look up aws_codecommit_blob_differences")

        ubx.data(BlobDifferences, "example", BlobDifferencesConfig(
            after_blob_id="example",
            repository_name="example-repository-name",
            before_blob_id="example",
            context_lines=1,
            ignore_whitespace=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_blob_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="before_blob_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="context_lines" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ignore_whitespace" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="after_blob_size" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="before_blob_size" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="hunks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_binary" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
