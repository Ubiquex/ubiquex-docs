---
title: "aws_codecommit_associated_approval_rule_templates_for_repository"
description: "`aws_codecommit_associated_approval_rule_templates_for_repository` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecommit_associated_approval_rule_templates_for_repository` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_associated_approval_rule_templates_for_repository"})

    		ubx.Data(codecommit.AssociatedApprovalRuleTemplatesForRepository, "example", codecommit.AssociatedApprovalRuleTemplatesForRepositoryConfig{
    			RepositoryName: "example-repository-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedApprovalRuleTemplatesForRepository } from "@ubx/sdk-aws/aws/data/codecommit/associated_approval_rule_templates_for_repository";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_codecommit_associated_approval_rule_templates_for_repository",
      });

      data(AssociatedApprovalRuleTemplatesForRepository, "example", {
        repositoryName: "example-repository-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import AssociatedApprovalRuleTemplatesForRepository, AssociatedApprovalRuleTemplatesForRepositoryConfig

    def describe():
        ubx.intent("look up aws_codecommit_associated_approval_rule_templates_for_repository")

        ubx.data(AssociatedApprovalRuleTemplatesForRepository, "example", AssociatedApprovalRuleTemplatesForRepositoryConfig(
            repository_name="example-repository-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approval_rule_template_names" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
