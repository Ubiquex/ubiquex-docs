---
title: "aws_codecommit_blob"
description: "`aws_codecommit_blob` reads the real content of a Git blob object within a CodeCommit repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_blob"})

    		ubx.Data(codecommit.Blob, "example", codecommit.BlobConfig{
    			BlobId:         "example",
    			RepositoryName: "example-repository-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Blob } from "@ubx/sdk-aws/aws/data/codecommit/blob";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_blob" });

      data(Blob, "example", {
        blobId: "example",
        repositoryName: "example-repository-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import Blob, BlobConfig

    def describe():
        ubx.intent("look up aws_codecommit_blob")

        ubx.data(Blob, "example", BlobConfig(
            blob_id="example",
            repository_name="example-repository-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="blob_id" type="string" required>
  The real, unique identifier of the blob this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content" type="string">
  The real content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
