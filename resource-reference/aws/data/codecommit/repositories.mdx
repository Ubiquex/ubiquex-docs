---
title: "aws_codecommit_repositories"
description: "`data_aws_codecommit_repositories` looks up a real, already-existing `aws_code_artifact_repository`."
---

It stores real software packages, polyglot by default, npm, Maven, PyPI, and others in the same repository, inside an `aws_code_artifact_domain`, either published directly or fetched on demand from a connected public repository like npmjs or Maven Central.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_repositories"})

    		ubx.Data(codecommit.Repositories, "example", codecommit.RepositoriesConfig{
    			Order:  "example",
    			SortBy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Repositories } from "@ubx/sdk-aws/aws/data/codecommit/repositories";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_repositories" });

      data(Repositories, "example", {
        order: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import Repositories, RepositoriesConfig

    def describe():
        ubx.intent("look up aws_codecommit_repositories")

        ubx.data(Repositories, "example", RepositoriesConfig(
            order="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="order" type="string">
  The real order results are returned in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repositories" type="list(object)">
  The real repositories included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
