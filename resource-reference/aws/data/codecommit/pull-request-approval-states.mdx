---
title: "aws_codecommit_pull_request_approval_states"
description: "`aws_codecommit_pull_request_approval_states` reads the real, per-reviewer approval state of a CodeCommit pull request."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codecommit "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecommit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecommit_pull_request_approval_states"})

    		ubx.Data(codecommit.PullRequestApprovalStates, "example", codecommit.PullRequestApprovalStatesConfig{
    			PullRequestId: "example",
    			RevisionId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PullRequestApprovalStates } from "./local-sdk/aws/sdk/typescript/aws/data/codecommit/pull_request_approval_states.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecommit_pull_request_approval_states" });

      data(PullRequestApprovalStates, "example", {
        pullRequestId: "example",
        revisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codecommit import PullRequestApprovalStates, PullRequestApprovalStatesConfig

    def describe():
        ubx.intent("look up aws_codecommit_pull_request_approval_states")

        ubx.data(PullRequestApprovalStates, "example", PullRequestApprovalStatesConfig(
            pull_request_id="example",
            revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pull_request_id" type="string" required>
  The real, unique identifier of the pull request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="revision_id" type="string" required>
  The real, unique identifier of this object's own revision. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approvals" type="list(object)">
  The real approvals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
