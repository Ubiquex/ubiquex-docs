---
title: "aws_mediastore_metric_policy"
description: "`aws_mediastore_metric_policy` reads the real CloudWatch metrics policy of a MediaStore container."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mediastore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediastore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediastore_metric_policy"})

    		ubx.Data(mediastore.MetricPolicy, "example", mediastore.MetricPolicyConfig{
    			ContainerName: "example-container-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetricPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/mediastore/metric_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediastore_metric_policy" });

      data(MetricPolicy, "example", {
        containerName: "example-container-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mediastore import MetricPolicy, MetricPolicyConfig

    def describe():
        ubx.intent("look up aws_mediastore_metric_policy")

        ubx.data(MetricPolicy, "example", MetricPolicyConfig(
            container_name="example-container-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_name" type="string" required>
  The real name of the container this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_policy" type="object">
  &lt;p&gt;The metric policy that is associated with the container. A metric policy allows AWS Elemental MediaStore to send metrics to Amazon CloudWatch. In the policy, you must indicate whether you want MediaStore to send container-level metrics. You can also include rules to define groups of objects that you want MediaStore to send object-level metrics for.&lt;/p&gt; &lt;p&gt;To view examples of how to construct a metric policy for your use case, see &lt;a href="https://docs.aws.amazon.com/mediastore/latest/ug/policies-metric-examples.html"&gt;Example Metric Policies&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="container_level_metrics" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="metric_policy_rules" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
