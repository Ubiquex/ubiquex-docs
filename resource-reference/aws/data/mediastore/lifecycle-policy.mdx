---
title: "aws_mediastore_lifecycle_policy"
description: "`aws_mediastore_lifecycle_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mediastore_lifecycle_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediastore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediastore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediastore_lifecycle_policy"})

    		ubx.Data(mediastore.LifecyclePolicy, "example", mediastore.LifecyclePolicyConfig{
    			ContainerName: "example-container-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LifecyclePolicy } from "@ubx/sdk-aws/aws/data/mediastore/lifecycle_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediastore_lifecycle_policy" });

      data(LifecyclePolicy, "example", {
        containerName: "example-container-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediastore import LifecyclePolicy, LifecyclePolicyConfig

    def describe():
        ubx.intent("look up aws_mediastore_lifecycle_policy")

        ubx.data(LifecyclePolicy, "example", LifecyclePolicyConfig(
            container_name="example-container-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lifecycle_policy" type="string">
  Returned by the lookup.
</ResponseField>
