---
title: "aws_appsync_domain_names"
description: "`aws_appsync_domain_names` looks up a real custom domain name configured for an AppSync API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_domain_names"})

    		ubx.Data(appsync.DomainNames, "example", appsync.DomainNamesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainNames } from "@ubx/sdk-aws/aws/data/appsync/domain_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_domain_names" });

      data(DomainNames, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_appsync_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_name_configs" type="list(object)">
  The real, configured domain names for this API. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
