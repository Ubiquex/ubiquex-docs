---
title: "aws_appsync_schema_creation_status"
description: "`aws_appsync_schema_creation_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appsync_schema_creation_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_schema_creation_status"})

    		ubx.Data(appsync.SchemaCreationStatus, "example", appsync.SchemaCreationStatusConfig{
    			ApiId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SchemaCreationStatus } from "@ubx/sdk-aws/aws/data/appsync/schema_creation_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_schema_creation_status" });

      data(SchemaCreationStatus, "example", {
        apiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import SchemaCreationStatus, SchemaCreationStatusConfig

    def describe():
        ubx.intent("look up aws_appsync_schema_creation_status")

        ubx.data(SchemaCreationStatus, "example", SchemaCreationStatusConfig(
            api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="details" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
