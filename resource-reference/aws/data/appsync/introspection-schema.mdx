---
title: "aws_appsync_introspection_schema"
description: "`aws_appsync_introspection_schema` reads the real, introspected GraphQL schema of an AppSync API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_introspection_schema"})

    		ubx.Data(appsync.IntrospectionSchema, "example", appsync.IntrospectionSchemaConfig{
    			ApiId:             "example",
    			Format:            "example",
    			IncludeDirectives: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntrospectionSchema } from "@ubx/sdk-aws/aws/data/appsync/introspection_schema";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_introspection_schema" });

      data(IntrospectionSchema, "example", {
        apiId: "example",
        format: "example",
        includeDirectives: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import IntrospectionSchema, IntrospectionSchemaConfig

    def describe():
        ubx.intent("look up aws_appsync_introspection_schema")

        ubx.data(IntrospectionSchema, "example", IntrospectionSchemaConfig(
            api_id="example",
            format="example",
            include_directives=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_directives" type="bool">
  Whether directives are included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="schema" type="string">
  The real schema this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
