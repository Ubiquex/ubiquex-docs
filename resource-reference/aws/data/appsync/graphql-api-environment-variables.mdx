---
title: "aws_appsync_graphql_api_environment_variables"
description: "`aws_appsync_graphql_api_environment_variables` reads the real environment variables configured for an AppSync GraphQL API's own resolvers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_graphql_api_environment_variables"})

    		ubx.Data(appsync.GraphqlApiEnvironmentVariables, "example", appsync.GraphqlApiEnvironmentVariablesConfig{
    			ApiId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GraphqlApiEnvironmentVariables } from "./local-sdk/aws/sdk/typescript/aws/data/appsync/graphql_api_environment_variables.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_graphql_api_environment_variables" });

      data(GraphqlApiEnvironmentVariables, "example", {
        apiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import GraphqlApiEnvironmentVariables, GraphqlApiEnvironmentVariablesConfig

    def describe():
        ubx.intent("look up aws_appsync_graphql_api_environment_variables")

        ubx.data(GraphqlApiEnvironmentVariables, "example", GraphqlApiEnvironmentVariablesConfig(
            api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="environment_variables" type="map(string)">
  The real environment variables set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
