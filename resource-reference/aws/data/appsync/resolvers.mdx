---
title: "aws_appsync_resolvers"
description: "`aws_appsync_resolvers` looks up a real AppSync resolver, mapping a GraphQL field to a data source."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_resolvers"})

    		ubx.Data(appsync.Resolvers, "example", appsync.ResolversConfig{
    			ApiId:      "example",
    			TypeName:   "example-type-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resolvers } from "@ubx/sdk-aws/aws/data/appsync/resolvers";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_resolvers" });

      data(Resolvers, "example", {
        apiId: "example",
        typeName: "example-type-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import Resolvers, ResolversConfig

    def describe():
        ubx.intent("look up aws_appsync_resolvers")

        ubx.data(Resolvers, "example", ResolversConfig(
            api_id="example",
            type_name="example-type-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string" required>
  The real name of this object's own type. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolvers" type="list(object)">
  The real resolvers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
