---
title: "aws_appsync_data_source_introspection"
description: "`aws_appsync_data_source_introspection` reads the real, introspected schema of an AppSync data source (such as an RDS database)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_data_source_introspection"})

    		ubx.Data(appsync.DataSourceIntrospection, "example", appsync.DataSourceIntrospectionConfig{
    			IntrospectionId:  "example",
    			IncludeModelsSdl: true,
    			MaxResults:       1,
    			NextToken:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataSourceIntrospection } from "./local-sdk/aws/sdk/typescript/aws/data/appsync/data_source_introspection.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_data_source_introspection" });

      data(DataSourceIntrospection, "example", {
        introspectionId: "example",
        includeModelsSdl: true,
        maxResults: 1,
        nextToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import DataSourceIntrospection, DataSourceIntrospectionConfig

    def describe():
        ubx.intent("look up aws_appsync_data_source_introspection")

        ubx.data(DataSourceIntrospection, "example", DataSourceIntrospectionConfig(
            introspection_id="example",
            include_models_sdl=True,
            max_results=1,
            next_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_models_sdl" type="bool">
  Whether the GraphQL schema definition language (SDL) for models is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="introspection_id" type="string" required>
  The real, unique identifier of the introspection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="introspection_result" type="object">
  &lt;p&gt;Represents the output of a &lt;code&gt;DataSourceIntrospectionResult&lt;/code&gt;. This is the populated result of a &lt;code&gt;GetDataSourceIntrospection&lt;/code&gt; operation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="models" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="introspection_status" type="string">
  The real, current status of the introspection this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="introspection_status_detail" type="string">
  The real, detailed information about the introspection status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
