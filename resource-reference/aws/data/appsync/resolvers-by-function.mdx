---
title: "aws_appsync_resolvers_by_function"
description: "`aws_appsync_resolvers_by_function` reads the real AppSync pipeline resolvers that include a given function."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_resolvers_by_function"})

    		ubx.Data(appsync.ResolversByFunction, "example", appsync.ResolversByFunctionConfig{
    			ApiId:      "example",
    			FunctionId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolversByFunction } from "@ubx/sdk-aws/aws/data/appsync/resolvers_by_function";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_resolvers_by_function" });

      data(ResolversByFunction, "example", {
        apiId: "example",
        functionId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import ResolversByFunction, ResolversByFunctionConfig

    def describe():
        ubx.intent("look up aws_appsync_resolvers_by_function")

        ubx.data(ResolversByFunction, "example", ResolversByFunctionConfig(
            api_id="example",
            function_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="function_id" type="string" required>
  The real, unique identifier of the function this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolvers" type="list(object)">
  The real resolvers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
