---
title: "aws_appsync_channel_namespaces"
description: "`aws_appsync_channel_namespaces` looks up a real channel namespace within an AppSync Events API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_channel_namespaces"})

    		ubx.Data(appsync.ChannelNamespaces, "example", appsync.ChannelNamespacesConfig{
    			ApiId:      "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChannelNamespaces } from "@ubx/sdk-aws/aws/data/appsync/channel_namespaces";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_channel_namespaces" });

      data(ChannelNamespaces, "example", {
        apiId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import ChannelNamespaces, ChannelNamespacesConfig

    def describe():
        ubx.intent("look up aws_appsync_channel_namespaces")

        ubx.data(ChannelNamespaces, "example", ChannelNamespacesConfig(
            api_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_namespaces" type="list(object)">
  The real Amazon Connect channel namespaces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
