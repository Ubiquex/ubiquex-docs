---
title: "aws_appsync_types_by_association"
description: "`aws_appsync_types_by_association` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appsync_types_by_association` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appsync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appsync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appsync_types_by_association"})

    		ubx.Data(appsync.TypesByAssociation, "example", appsync.TypesByAssociationConfig{
    			AssociationId:       "example",
    			Format:              "example",
    			MergedApiIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TypesByAssociation } from "@ubx/sdk-aws/aws/data/appsync/types_by_association";

    export default stack("example", () => {
      intent({ summary: "look up aws_appsync_types_by_association" });

      data(TypesByAssociation, "example", {
        associationId: "example",
        format: "example",
        mergedApiIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appsync import TypesByAssociation, TypesByAssociationConfig

    def describe():
        ubx.intent("look up aws_appsync_types_by_association")

        ubx.data(TypesByAssociation, "example", TypesByAssociationConfig(
            association_id="example",
            format="example",
            merged_api_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="merged_api_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="types" type="list(object)">
  Returned by the lookup.
</ResponseField>
