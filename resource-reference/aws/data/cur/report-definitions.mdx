---
title: "aws_cur_report_definitions"
description: "`aws_cur_report_definitions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cur_report_definitions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cur "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cur"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cur_report_definitions"})

    		ubx.Data(cur.ReportDefinitions, "example", cur.ReportDefinitionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReportDefinitions } from "@ubx/sdk-aws/aws/data/cur/report_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_cur_report_definitions" });

      data(ReportDefinitions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cur import ReportDefinitions, ReportDefinitionsConfig

    def describe():
        ubx.intent("look up aws_cur_report_definitions")

        ubx.data(ReportDefinitions, "example", ReportDefinitionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results that Amazon Web Services returns for the operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;A generic string.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="report_definitions" type="list(object)">
  &lt;p&gt;A list of report definitions.&lt;/p&gt; Returned by the lookup.
</ResponseField>
