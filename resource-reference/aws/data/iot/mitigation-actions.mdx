---
title: "aws_iot_mitigation_actions"
description: "`aws_iot_mitigation_actions` looks up a real mitigation action Device Defender can apply automatically to an audit finding or violation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_mitigation_actions"})

    		ubx.Data(iot.MitigationActions, "example", iot.MitigationActionsConfig{
    			ActionType: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MitigationActions } from "@ubx/sdk-aws/aws/data/iot/mitigation_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_mitigation_actions" });

      data(MitigationActions, "example", {
        actionType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import MitigationActions, MitigationActionsConfig

    def describe():
        ubx.intent("look up aws_iot_mitigation_actions")

        ubx.data(MitigationActions, "example", MitigationActionsConfig(
            action_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string">
  The real type of this action. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_identifiers" type="list(object)">
  The real, unique identifiers of the actions this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
