---
title: "aws_iot_cacertificates"
description: "`aws_iot_cacertificates` reads the real CA certificates registered with IoT Core."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_cacertificates"})

    		ubx.Data(iot.Cacertificates, "example", iot.CacertificatesConfig{
    			AscendingOrder: true,
    			Marker:         "example",
    			PageSize:       1,
    			TemplateName:   "example-template-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cacertificates } from "@ubx/sdk-aws/aws/data/iot/cacertificates";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_cacertificates" });

      data(Cacertificates, "example", {
        ascendingOrder: true,
        marker: "example",
        pageSize: 1,
        templateName: "example-template-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Cacertificates, CacertificatesConfig

    def describe():
        ubx.intent("look up aws_iot_cacertificates")

        ubx.data(Cacertificates, "example", CacertificatesConfig(
            ascending_order=True,
            marker="example",
            page_size=1,
            template_name="example-template-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Whether results are returned in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_name" type="string">
  The real name of the template this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificates" type="list(object)">
  The real certificates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
