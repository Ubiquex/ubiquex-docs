---
title: "aws_iot_policy_versions"
description: "`aws_iot_policy_versions` reads a real, saved version of an IoT policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_policy_versions"})

    		ubx.Data(iot.PolicyVersions, "example", iot.PolicyVersionsConfig{
    			PolicyName: "example-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PolicyVersions } from "./local-sdk/aws/sdk/typescript/aws/data/iot/policy_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_policy_versions" });

      data(PolicyVersions, "example", {
        policyName: "example-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import PolicyVersions, PolicyVersionsConfig

    def describe():
        ubx.intent("look up aws_iot_policy_versions")

        ubx.data(PolicyVersions, "example", PolicyVersionsConfig(
            policy_name="example-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_versions" type="list(object)">
  The real versions of the policy included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
