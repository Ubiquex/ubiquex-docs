---
title: "aws_iot_jobs"
description: "`aws_iot_jobs` looks up a real IoT Jobs job, a set of remote operations sent to one or more devices."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_jobs"})

    		ubx.Data(iot.Jobs, "example", iot.JobsConfig{
    			MaxResults:      1,
    			NamespaceId:     "example",
    			Status:          "example",
    			TargetSelection: "example",
    			ThingGroupId:    "example",
    			ThingGroupName:  "example-thing-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Jobs } from "@ubx/sdk-aws/aws/data/iot/jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_jobs" });

      data(Jobs, "example", {
        maxResults: 1,
        namespaceId: "example",
        status: "example",
        targetSelection: "example",
        thingGroupId: "example",
        thingGroupName: "example-thing-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Jobs, JobsConfig

    def describe():
        ubx.intent("look up aws_iot_jobs")

        ubx.data(Jobs, "example", JobsConfig(
            max_results=1,
            namespace_id="example",
            status="example",
            target_selection="example",
            thing_group_id="example",
            thing_group_name="example-thing-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace_id" type="string">
  The real, unique identifier of the namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_selection" type="string">
  The real, selected target for this action. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_group_id" type="string">
  The real, unique identifier of the thing group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_group_name" type="string">
  The real name of the thing group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
