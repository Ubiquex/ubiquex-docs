---
title: "aws_iot_scheduled_audits"
description: "`aws_iot_scheduled_audits` looks up a real, recurring schedule for running a Device Defender audit automatically."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_scheduled_audits"})

    		ubx.Data(iot.ScheduledAudits, "example", iot.ScheduledAuditsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ScheduledAudits } from "./local-sdk/aws/sdk/typescript/aws/data/iot/scheduled_audits.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_scheduled_audits" });

      data(ScheduledAudits, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ScheduledAudits, ScheduledAuditsConfig

    def describe():
        ubx.intent("look up aws_iot_scheduled_audits")

        ubx.data(ScheduledAudits, "example", ScheduledAuditsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_audits" type="list(object)">
  The real AWS IoT Device Defender scheduled audits included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
