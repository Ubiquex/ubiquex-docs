---
title: "aws_iot_security_profiles_for_target"
description: "`aws_iot_security_profiles_for_target` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_security_profiles_for_target` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_security_profiles_for_target"})

    		ubx.Data(iot.SecurityProfilesForTarget, "example", iot.SecurityProfilesForTargetConfig{
    			SecurityProfileTargetArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:               1,
    			Recursive:                true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityProfilesForTarget } from "@ubx/sdk-aws/aws/data/iot/security_profiles_for_target";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_security_profiles_for_target" });

      data(SecurityProfilesForTarget, "example", {
        securityProfileTargetArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        recursive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import SecurityProfilesForTarget, SecurityProfilesForTargetConfig

    def describe():
        ubx.intent("look up aws_iot_security_profiles_for_target")

        ubx.data(SecurityProfilesForTarget, "example", SecurityProfilesForTargetConfig(
            security_profile_target_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            recursive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_target_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="security_profile_target_mappings" type="list(object)">
  Returned by the lookup.
</ResponseField>
