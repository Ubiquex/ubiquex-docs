---
title: "aws_iot_detect_mitigation_actions_tasks"
description: "`aws_iot_detect_mitigation_actions_tasks` reads the real tasks applying mitigation actions to Device Defender ML Detect violations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_detect_mitigation_actions_tasks"})

    		ubx.Data(iot.DetectMitigationActionsTasks, "example", iot.DetectMitigationActionsTasksConfig{
    			EndTime:    "example",
    			StartTime:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DetectMitigationActionsTasks } from "@ubx/sdk-aws/aws/data/iot/detect_mitigation_actions_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_detect_mitigation_actions_tasks" });

      data(DetectMitigationActionsTasks, "example", {
        endTime: "example",
        startTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import DetectMitigationActionsTasks, DetectMitigationActionsTasksConfig

    def describe():
        ubx.intent("look up aws_iot_detect_mitigation_actions_tasks")

        ubx.data(DetectMitigationActionsTasks, "example", DetectMitigationActionsTasksConfig(
            end_time="example",
            start_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  The real tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
