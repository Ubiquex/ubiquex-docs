---
title: "aws_iot_provisioning_template_versions"
description: "`aws_iot_provisioning_template_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_provisioning_template_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_provisioning_template_versions"})

    		ubx.Data(iot.ProvisioningTemplateVersions, "example", iot.ProvisioningTemplateVersionsConfig{
    			TemplateName: "example-template-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisioningTemplateVersions } from "@ubx/sdk-aws/aws/data/iot/provisioning_template_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_provisioning_template_versions" });

      data(ProvisioningTemplateVersions, "example", {
        templateName: "example-template-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ProvisioningTemplateVersions, ProvisioningTemplateVersionsConfig

    def describe():
        ubx.intent("look up aws_iot_provisioning_template_versions")

        ubx.data(ProvisioningTemplateVersions, "example", ProvisioningTemplateVersionsConfig(
            template_name="example-template-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="template_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
