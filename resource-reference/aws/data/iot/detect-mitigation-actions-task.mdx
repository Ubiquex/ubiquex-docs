---
title: "aws_iot_detect_mitigation_actions_task"
description: "`aws_iot_detect_mitigation_actions_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_detect_mitigation_actions_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_detect_mitigation_actions_task"})

    		ubx.Data(iot.DetectMitigationActionsTask, "example", iot.DetectMitigationActionsTaskConfig{
    			TaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DetectMitigationActionsTask } from "@ubx/sdk-aws/aws/data/iot/detect_mitigation_actions_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_detect_mitigation_actions_task" });

      data(DetectMitigationActionsTask, "example", {
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import DetectMitigationActionsTask, DetectMitigationActionsTaskConfig

    def describe():
        ubx.intent("look up aws_iot_detect_mitigation_actions_task")

        ubx.data(DetectMitigationActionsTask, "example", DetectMitigationActionsTaskConfig(
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="task_summary" type="object">
  &lt;p&gt; The summary of the mitigation action tasks. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="actions_definition" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="only_active_violations_included" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="suppressed_alerts_included" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target" type="object">
      &lt;p&gt; The target of a mitigation action task. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="behavior_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="security_profile_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="violation_ids" type="list(string)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="task_end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="task_start_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="task_statistics" type="object">
      &lt;p&gt; The statistics of a mitigation action task. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="actions_executed" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="actions_failed" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="actions_skipped" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="task_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="violation_event_occurrence_range" type="object">
      &lt;p&gt; Specifies the time period of which violation events occurred between. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="start_time" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
