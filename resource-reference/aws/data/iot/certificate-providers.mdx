---
title: "aws_iot_certificate_providers"
description: "`aws_iot_certificate_providers` looks up a real certificate provider, a Lambda function that issues just-in-time device certificates."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_certificate_providers"})

    		ubx.Data(iot.CertificateProviders, "example", iot.CertificateProvidersConfig{
    			AscendingOrder: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CertificateProviders } from "@ubx/sdk-aws/aws/data/iot/certificate_providers";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_certificate_providers" });

      data(CertificateProviders, "example", {
        ascendingOrder: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import CertificateProviders, CertificateProvidersConfig

    def describe():
        ubx.intent("look up aws_iot_certificate_providers")

        ubx.data(CertificateProviders, "example", CertificateProvidersConfig(
            ascending_order=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Whether results are returned in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate_providers" type="list(object)">
  The real certificate providers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
