---
title: "aws_iot_audit_findings"
description: "`aws_iot_audit_findings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_audit_findings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_findings"})

    		ubx.Data(iot.AuditFindings, "example", iot.AuditFindingsConfig{
    			CheckName:              "example-check-name",
    			EndTime:                "example",
    			ListSuppressedFindings: true,
    			MaxResults:             1,
    			ResourceIdentifier:     map[string]any{"account": "example"},
    			StartTime:              "example",
    			TaskId:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuditFindings } from "@ubx/sdk-aws/aws/data/iot/audit_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_findings" });

      data(AuditFindings, "example", {
        checkName: "example-check-name",
        endTime: "example",
        listSuppressedFindings: true,
        maxResults: 1,
        resourceIdentifier: { account: "example" },
        startTime: "example",
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditFindings, AuditFindingsConfig

    def describe():
        ubx.intent("look up aws_iot_audit_findings")

        ubx.data(AuditFindings, "example", AuditFindingsConfig(
            check_name="example-check-name",
            end_time="example",
            list_suppressed_findings=True,
            max_results=1,
            resource_identifier={"account": "example"},
            start_time="example",
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="check_name" type="string">
  &lt;p&gt;An audit check name. Checks must be enabled for your account. (Use &lt;code&gt;DescribeAccountAuditConfiguration&lt;/code&gt; to see the list of all checks, including those that are enabled or use &lt;code&gt;UpdateAccountAuditConfiguration&lt;/code&gt; to select which checks are enabled.)&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="list_suppressed_findings" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier" type="object">
  &lt;p&gt;Information that identifies the noncompliant resource.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ca_certificate_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cognito_identity_pool_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_certificate_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_certificate_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="issuer_certificate_identifier" type="object">
      &lt;p&gt;The certificate issuer indentifier.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="issuer_certificate_serial_number" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="issuer_certificate_subject" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="issuer_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="policy_version_identifier" type="object">
      &lt;p&gt;Information about the version of the policy associated with the resource.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="policy_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_version_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role_alias_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="task_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
