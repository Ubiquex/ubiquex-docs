---
title: "aws_iot_job_executions_for_thing"
description: "`aws_iot_job_executions_for_thing` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_job_executions_for_thing` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_job_executions_for_thing"})

    		ubx.Data(iot.JobExecutionsForThing, "example", iot.JobExecutionsForThingConfig{
    			ThingName:   "example-thing-name",
    			JobId:       "example",
    			MaxResults:  1,
    			NamespaceId: "example",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobExecutionsForThing } from "@ubx/sdk-aws/aws/data/iot/job_executions_for_thing";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_job_executions_for_thing" });

      data(JobExecutionsForThing, "example", {
        thingName: "example-thing-name",
        jobId: "example",
        maxResults: 1,
        namespaceId: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import JobExecutionsForThing, JobExecutionsForThingConfig

    def describe():
        ubx.intent("look up aws_iot_job_executions_for_thing")

        ubx.data(JobExecutionsForThing, "example", JobExecutionsForThingConfig(
            thing_name="example-thing-name",
            job_id="example",
            max_results=1,
            namespace_id="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="namespace_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="execution_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
