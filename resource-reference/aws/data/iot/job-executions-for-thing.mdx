---
title: "aws_iot_job_executions_for_thing"
description: "`aws_iot_job_executions_for_thing` reads the real IoT Jobs job executions assigned to a specific thing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_job_executions_for_thing"})

    		ubx.Data(iot.JobExecutionsForThing, "example", iot.JobExecutionsForThingConfig{
    			ThingName:   "example-thing-name",
    			JobId:       "example",
    			MaxResults:  1,
    			NamespaceId: "example",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { JobExecutionsForThing } from "./local-sdk/aws/sdk/typescript/aws/data/iot/job_executions_for_thing.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_job_executions_for_thing" });

      data(JobExecutionsForThing, "example", {
        thingName: "example-thing-name",
        jobId: "example",
        maxResults: 1,
        namespaceId: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import JobExecutionsForThing, JobExecutionsForThingConfig

    def describe():
        ubx.intent("look up aws_iot_job_executions_for_thing")

        ubx.data(JobExecutionsForThing, "example", JobExecutionsForThingConfig(
            thing_name="example-thing-name",
            job_id="example",
            max_results=1,
            namespace_id="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string">
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace_id" type="string">
  The real, unique identifier of the namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="execution_summaries" type="list(object)">
  The real, summarized executions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
