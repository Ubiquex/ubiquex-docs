---
title: "aws_iot_related_resources_for_audit_finding"
description: "`aws_iot_related_resources_for_audit_finding` reads the real resources related to a specific Device Defender audit finding."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_related_resources_for_audit_finding"})

    		ubx.Data(iot.RelatedResourcesForAuditFinding, "example", iot.RelatedResourcesForAuditFindingConfig{
    			FindingId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelatedResourcesForAuditFinding } from "@ubx/sdk-aws/aws/data/iot/related_resources_for_audit_finding";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_related_resources_for_audit_finding" });

      data(RelatedResourcesForAuditFinding, "example", {
        findingId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import RelatedResourcesForAuditFinding, RelatedResourcesForAuditFindingConfig

    def describe():
        ubx.intent("look up aws_iot_related_resources_for_audit_finding")

        ubx.data(RelatedResourcesForAuditFinding, "example", RelatedResourcesForAuditFindingConfig(
            finding_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="finding_id" type="string" required>
  The real, unique identifier of the finding this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="related_resources" type="list(object)">
  The real resources related to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
