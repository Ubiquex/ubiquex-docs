---
title: "aws_iot_provisioning_templates"
description: "`aws_iot_provisioning_templates` looks up a real IoT fleet provisioning template, defining how a new device registers itself at first connection."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_provisioning_templates"})

    		ubx.Data(iot.ProvisioningTemplates, "example", iot.ProvisioningTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisioningTemplates } from "@ubx/sdk-aws/aws/data/iot/provisioning_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_provisioning_templates" });

      data(ProvisioningTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ProvisioningTemplates, ProvisioningTemplatesConfig

    def describe():
        ubx.intent("look up aws_iot_provisioning_templates")

        ubx.data(ProvisioningTemplates, "example", ProvisioningTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="templates" type="list(object)">
  The real templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
