---
title: "aws_iot_thing_types"
description: "`aws_iot_thing_types` looks up a real thing type, a template defining the searchable attributes a thing of that type carries."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_types"})

    		ubx.Data(iot.ThingTypes, "example", iot.ThingTypesConfig{
    			MaxResults:    1,
    			ThingTypeName: "example-thing-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ThingTypes } from "./local-sdk/aws/sdk/typescript/aws/data/iot/thing_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_types" });

      data(ThingTypes, "example", {
        maxResults: 1,
        thingTypeName: "example-thing-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingTypes, ThingTypesConfig

    def describe():
        ubx.intent("look up aws_iot_thing_types")

        ubx.data(ThingTypes, "example", ThingTypesConfig(
            max_results=1,
            thing_type_name="example-thing-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_type_name" type="string">
  The real name of the thing type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thing_types" type="list(object)">
  The real, discriminating types of things this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
