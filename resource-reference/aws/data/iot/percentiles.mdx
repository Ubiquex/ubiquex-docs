---
title: "aws_iot_percentiles"
description: "`aws_iot_percentiles` reads real, computed percentile values across a numeric Fleet Indexing aggregation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_percentiles"})

    		ubx.Data(iot.Percentiles, "example", iot.PercentilesConfig{
    			QueryString:      "example",
    			AggregationField: "example",
    			IndexName:        "example-index-name",
    			Percents:         []any{1},
    			QueryVersion:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Percentiles } from "@ubx/sdk-aws/aws/data/iot/percentiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_percentiles" });

      data(Percentiles, "example", {
        queryString: "example",
        aggregationField: "example",
        indexName: "example-index-name",
        percents: [1],
        queryVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Percentiles, PercentilesConfig

    def describe():
        ubx.intent("look up aws_iot_percentiles")

        ubx.data(Percentiles, "example", PercentilesConfig(
            query_string="example",
            aggregation_field="example",
            index_name="example-index-name",
            percents=[1],
            query_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_field" type="string">
  The real field results are aggregated by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_name" type="string">
  The real name of the index this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="percents" type="list(number)">
  The real percentage values included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  The real, raw query string this request runs. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="query_version" type="string">
  The real version of the query this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="percentiles" type="list(object)">
  The real percentiles computed for this metric. **(AI-inferred)** Returned by the lookup.
</ResponseField>
