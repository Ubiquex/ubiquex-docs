---
title: "aws_iot_topic_rules"
description: "`aws_iot_topic_rules` looks up a real IoT topic rule, routing MQTT messages matching a SQL-like statement to one or more actions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_topic_rules"})

    		ubx.Data(iot.TopicRules, "example", iot.TopicRulesConfig{
    			MaxResults:   1,
    			RuleDisabled: true,
    			Topic:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicRules } from "@ubx/sdk-aws/aws/data/iot/topic_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_topic_rules" });

      data(TopicRules, "example", {
        maxResults: 1,
        ruleDisabled: true,
        topic: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import TopicRules, TopicRulesConfig

    def describe():
        ubx.intent("look up aws_iot_topic_rules")

        ubx.data(TopicRules, "example", TopicRulesConfig(
            max_results=1,
            rule_disabled=True,
            topic="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rule_disabled" type="bool">
  Whether this rule is currently disabled. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="topic" type="string">
  The real topic this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
