---
title: "aws_iot_audit_tasks"
description: "`aws_iot_audit_tasks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_audit_tasks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_tasks"})

    		ubx.Data(iot.AuditTasks, "example", iot.AuditTasksConfig{
    			EndTime:    "example",
    			StartTime:  "example",
    			MaxResults: 1,
    			TaskStatus: "example",
    			TaskType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuditTasks } from "@ubx/sdk-aws/aws/data/iot/audit_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_tasks" });

      data(AuditTasks, "example", {
        endTime: "example",
        startTime: "example",
        maxResults: 1,
        taskStatus: "example",
        taskType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditTasks, AuditTasksConfig

    def describe():
        ubx.intent("look up aws_iot_audit_tasks")

        ubx.data(AuditTasks, "example", AuditTasksConfig(
            end_time="example",
            start_time="example",
            max_results=1,
            task_status="example",
            task_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="task_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="task_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  Returned by the lookup.
</ResponseField>
