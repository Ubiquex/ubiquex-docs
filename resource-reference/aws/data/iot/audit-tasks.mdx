---
title: "aws_iot_audit_tasks"
description: "`aws_iot_audit_tasks` reads the real Device Defender audit tasks on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_tasks"})

    		ubx.Data(iot.AuditTasks, "example", iot.AuditTasksConfig{
    			EndTime:    "example",
    			StartTime:  "example",
    			MaxResults: 1,
    			TaskStatus: "example",
    			TaskType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AuditTasks } from "./local-sdk/aws/sdk/typescript/aws/data/iot/audit_tasks.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_tasks" });

      data(AuditTasks, "example", {
        endTime: "example",
        startTime: "example",
        maxResults: 1,
        taskStatus: "example",
        taskType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditTasks, AuditTasksConfig

    def describe():
        ubx.intent("look up aws_iot_audit_tasks")

        ubx.data(AuditTasks, "example", AuditTasksConfig(
            end_time="example",
            start_time="example",
            max_results=1,
            task_status="example",
            task_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="task_status" type="string">
  The real, current status of the task. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="task_type" type="string">
  The real, discriminating type of the task this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  The real tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
