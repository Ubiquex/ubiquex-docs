---
title: "aws_iot_thing_registration_tasks"
description: "`aws_iot_thing_registration_tasks` reads the real bulk thing registration tasks on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_registration_tasks"})

    		ubx.Data(iot.ThingRegistrationTasks, "example", iot.ThingRegistrationTasksConfig{
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingRegistrationTasks } from "@ubx/sdk-aws/aws/data/iot/thing_registration_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_registration_tasks" });

      data(ThingRegistrationTasks, "example", {
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingRegistrationTasks, ThingRegistrationTasksConfig

    def describe():
        ubx.intent("look up aws_iot_thing_registration_tasks")

        ubx.data(ThingRegistrationTasks, "example", ThingRegistrationTasksConfig(
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_ids" type="list(string)">
  The real, unique identifiers of the tasks this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
