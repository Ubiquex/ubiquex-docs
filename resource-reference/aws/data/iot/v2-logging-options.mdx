---
title: "aws_iot_v2_logging_options"
description: "`aws_iot_v2_logging_options` reads the real, account-wide CloudWatch logging configuration for IoT Core."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_v2_logging_options"})

    		ubx.Data(iot.V2LoggingOptions, "example", iot.V2LoggingOptionsConfig{
    			Verbose: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V2LoggingOptions } from "@ubx/sdk-aws/aws/data/iot/v2_logging_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_v2_logging_options" });

      data(V2LoggingOptions, "example", {
        verbose: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import V2LoggingOptions, V2LoggingOptionsConfig

    def describe():
        ubx.intent("look up aws_iot_v2_logging_options")

        ubx.data(V2LoggingOptions, "example", V2LoggingOptionsConfig(
            verbose=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="verbose" type="bool">
  Whether the response includes verbose, expanded detail. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_log_level" type="string">
  The real level of the default log. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="disable_all_logs" type="bool">
  Whether all logging is disabled for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_configurations" type="list(object)">
  The real event configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
