---
title: "aws_iot_buckets_aggregation"
description: "`aws_iot_buckets_aggregation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_buckets_aggregation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_buckets_aggregation"})

    		ubx.Data(iot.BucketsAggregation, "example", iot.BucketsAggregationConfig{
    			AggregationField:       "example",
    			BucketsAggregationType: map[string]any{"terms_aggregation": map[string]any{"max_buckets": 1}},
    			QueryString:            "example",
    			IndexName:              "example-index-name",
    			QueryVersion:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketsAggregation } from "@ubx/sdk-aws/aws/data/iot/buckets_aggregation";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_buckets_aggregation" });

      data(BucketsAggregation, "example", {
        aggregationField: "example",
        bucketsAggregationType: { termsAggregation: { maxBuckets: 1 } },
        queryString: "example",
        indexName: "example-index-name",
        queryVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import BucketsAggregation, BucketsAggregationConfig

    def describe():
        ubx.intent("look up aws_iot_buckets_aggregation")

        ubx.data(BucketsAggregation, "example", BucketsAggregationConfig(
            aggregation_field="example",
            buckets_aggregation_type={"terms_aggregation": {"max_buckets": 1}},
            query_string="example",
            index_name="example-index-name",
            query_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_field" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="buckets_aggregation_type" type="object" required>
  &lt;p&gt;The type of bucketed aggregation performed.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="terms_aggregation" type="object">
      &lt;p&gt;Performs an aggregation that will return a list of buckets. The list of buckets is a ranked list of the number of occurrences of an aggregation field value.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="max_buckets" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="index_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="query_version" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="buckets" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Returned by the lookup.
</ResponseField>
