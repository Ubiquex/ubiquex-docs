---
title: "aws_iot_package_configuration"
description: "`aws_iot_package_configuration` reads the real, account-wide configuration for IoT software package catalog versioning."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_package_configuration"})

    		ubx.Data(iot.PackageConfiguration, "example", iot.PackageConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageConfiguration } from "@ubx/sdk-aws/aws/data/iot/package_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_package_configuration" });

      data(PackageConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import PackageConfiguration, PackageConfigurationConfig

    def describe():
        ubx.intent("look up aws_iot_package_configuration")

        ubx.data(PackageConfiguration, "example", PackageConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="version_update_by_jobs_config" type="object">
  &lt;p&gt;Configuration to manage IoT Job's package version reporting. If configured, Jobs updates the thing's reserved named shadow with the package version information up on successful job completion.&lt;/p&gt; &lt;p&gt; &lt;b&gt;Note:&lt;/b&gt; For each job, the destinationPackageVersions attribute has to be set with the correct data for Jobs to report to the thing shadow. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
