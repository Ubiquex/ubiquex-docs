---
title: "aws_iot_logging_options"
description: "`aws_iot_logging_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_logging_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_logging_options"})

    		ubx.Data(iot.LoggingOptions, "example", iot.LoggingOptionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoggingOptions } from "@ubx/sdk-aws/aws/data/iot/logging_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_logging_options" });

      data(LoggingOptions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import LoggingOptions, LoggingOptionsConfig

    def describe():
        ubx.intent("look up aws_iot_logging_options")

        ubx.data(LoggingOptions, "example", LoggingOptionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="log_level" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Returned by the lookup.
</ResponseField>
