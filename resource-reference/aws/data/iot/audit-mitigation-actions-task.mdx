---
title: "aws_iot_audit_mitigation_actions_task"
description: "`aws_iot_audit_mitigation_actions_task` looks up a real task applying mitigation actions to Device Defender audit findings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_mitigation_actions_task"})

    		ubx.Data(iot.AuditMitigationActionsTask, "example", iot.AuditMitigationActionsTaskConfig{
    			TaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AuditMitigationActionsTask } from "./local-sdk/aws/sdk/typescript/aws/data/iot/audit_mitigation_actions_task.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_mitigation_actions_task" });

      data(AuditMitigationActionsTask, "example", {
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditMitigationActionsTask, AuditMitigationActionsTaskConfig

    def describe():
        ubx.intent("look up aws_iot_audit_mitigation_actions_task")

        ubx.data(AuditMitigationActionsTask, "example", AuditMitigationActionsTaskConfig(
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_id" type="string" required>
  The real, unique identifier of the task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions_definition" type="list(object)">
  The real, structured definition of the actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="audit_check_to_actions_mapping" type="map(list(string))">
  The real mapping between audit checks and their own remediation actions. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target" type="object">
  &lt;p&gt;Used in MitigationActionParams, this information identifies the target findings to which the mitigation actions are applied. Only one entry appears.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="audit_check_to_reason_code_filter" type="map(list(string))">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="audit_task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="finding_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="task_statistics" type="map(object)">
  The real, computed statistics for this task. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_status" type="string">
  The real, current status of the task. **(AI-inferred)** Returned by the lookup.
</ResponseField>
