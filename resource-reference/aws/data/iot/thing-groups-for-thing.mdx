---
title: "aws_iot_thing_groups_for_thing"
description: "`aws_iot_thing_groups_for_thing` reads the real thing groups a specific thing belongs to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_groups_for_thing"})

    		ubx.Data(iot.ThingGroupsForThing, "example", iot.ThingGroupsForThingConfig{
    			ThingName:  "example-thing-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingGroupsForThing } from "@ubx/sdk-aws/aws/data/iot/thing_groups_for_thing";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_groups_for_thing" });

      data(ThingGroupsForThing, "example", {
        thingName: "example-thing-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingGroupsForThing, ThingGroupsForThingConfig

    def describe():
        ubx.intent("look up aws_iot_thing_groups_for_thing")

        ubx.data(ThingGroupsForThing, "example", ThingGroupsForThingConfig(
            thing_name="example-thing-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thing_groups" type="list(object)">
  The real thing groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
