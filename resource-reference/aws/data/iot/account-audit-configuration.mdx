---
title: "aws_iot_account_audit_configuration"
description: "`aws_iot_account_audit_configuration` reads the real, account-wide Device Defender audit configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_account_audit_configuration"})

    		ubx.Data(iot.AccountAuditConfiguration, "example", iot.AccountAuditConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAuditConfiguration } from "@ubx/sdk-aws/aws/data/iot/account_audit_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_account_audit_configuration" });

      data(AccountAuditConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AccountAuditConfiguration, AccountAuditConfigurationConfig

    def describe():
        ubx.intent("look up aws_iot_account_audit_configuration")

        ubx.data(AccountAuditConfiguration, "example", AccountAuditConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="audit_check_configurations" type="map(object)">
  The real AWS IoT Device Defender audit check configurations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="audit_notification_target_configurations" type="map(object)">
  The real notification targets configured for audit results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
