---
title: "aws_iot_role_aliases"
description: "`aws_iot_role_aliases` looks up a real IoT role alias, letting a device assume an IAM role via the credentials provider without hardcoding a role ARN."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_role_aliases"})

    		ubx.Data(iot.RoleAliases, "example", iot.RoleAliasesConfig{
    			AscendingOrder: true,
    			Marker:         "example",
    			PageSize:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RoleAliases } from "@ubx/sdk-aws/aws/data/iot/role_aliases";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_role_aliases" });

      data(RoleAliases, "example", {
        ascendingOrder: true,
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import RoleAliases, RoleAliasesConfig

    def describe():
        ubx.intent("look up aws_iot_role_aliases")

        ubx.data(RoleAliases, "example", RoleAliasesConfig(
            ascending_order=True,
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Whether results are returned in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_aliases" type="list(string)">
  The real AWS IoT role aliases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
