---
title: "aws_iot_targets_for_policy"
description: "`aws_iot_targets_for_policy` reads the real targets (thing groups) an IoT policy is attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_targets_for_policy"})

    		ubx.Data(iot.TargetsForPolicy, "example", iot.TargetsForPolicyConfig{
    			PolicyName: "example-policy-name",
    			Marker:     "example",
    			PageSize:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TargetsForPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/iot/targets_for_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_targets_for_policy" });

      data(TargetsForPolicy, "example", {
        policyName: "example-policy-name",
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import TargetsForPolicy, TargetsForPolicyConfig

    def describe():
        ubx.intent("look up aws_iot_targets_for_policy")

        ubx.data(TargetsForPolicy, "example", TargetsForPolicyConfig(
            policy_name="example-policy-name",
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="targets" type="list(string)">
  The real targets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
