---
title: "aws_iot_job_document"
description: "`aws_iot_job_document` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_job_document` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_job_document"})

    		ubx.Data(iot.JobDocument, "example", iot.JobDocumentConfig{
    			JobId:              "example",
    			BeforeSubstitution: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobDocument } from "@ubx/sdk-aws/aws/data/iot/job_document";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_job_document" });

      data(JobDocument, "example", {
        jobId: "example",
        beforeSubstitution: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import JobDocument, JobDocumentConfig

    def describe():
        ubx.intent("look up aws_iot_job_document")

        ubx.data(JobDocument, "example", JobDocumentConfig(
            job_id="example",
            before_substitution=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="before_substitution" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document" type="string">
  Returned by the lookup.
</ResponseField>
