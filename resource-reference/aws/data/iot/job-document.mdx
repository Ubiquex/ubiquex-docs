---
title: "aws_iot_job_document"
description: "`aws_iot_job_document` reads the real JSON document describing the operations an IoT Jobs job instructs a device to perform."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_job_document"})

    		ubx.Data(iot.JobDocument, "example", iot.JobDocumentConfig{
    			JobId:              "example",
    			BeforeSubstitution: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobDocument } from "@ubx/sdk-aws/aws/data/iot/job_document";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_job_document" });

      data(JobDocument, "example", {
        jobId: "example",
        beforeSubstitution: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import JobDocument, JobDocumentConfig

    def describe():
        ubx.intent("look up aws_iot_job_document")

        ubx.data(JobDocument, "example", JobDocumentConfig(
            job_id="example",
            before_substitution=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="before_substitution" type="bool">
  The real value of this object before substitution. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document" type="string">
  The real document content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
