---
title: "aws_iot_thing_registration_task"
description: "`aws_iot_thing_registration_task` looks up a real bulk thing registration task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_registration_task"})

    		ubx.Data(iot.ThingRegistrationTask, "example", iot.ThingRegistrationTaskConfig{
    			TaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ThingRegistrationTask } from "./local-sdk/aws/sdk/typescript/aws/data/iot/thing_registration_task.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_registration_task" });

      data(ThingRegistrationTask, "example", {
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingRegistrationTask, ThingRegistrationTaskConfig

    def describe():
        ubx.intent("look up aws_iot_thing_registration_task")

        ubx.data(ThingRegistrationTask, "example", ThingRegistrationTaskConfig(
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_id" type="string" required>
  The real, unique identifier of the task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_count" type="number">
  The real count of failures. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="input_file_bucket" type="string">
  The real S3 bucket used for the input file. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="input_file_key" type="string">
  The real, unique key of the input file this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="percentage_progress" type="number">
  The real, current progress of this operation, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="success_count" type="number">
  The real count of successs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="template_body" type="string">
  The real, raw body of the template. **(AI-inferred)** Returned by the lookup.
</ResponseField>
