---
title: "aws_iot_thing_registration_task"
description: "`aws_iot_thing_registration_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_thing_registration_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_registration_task"})

    		ubx.Data(iot.ThingRegistrationTask, "example", iot.ThingRegistrationTaskConfig{
    			TaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingRegistrationTask } from "@ubx/sdk-aws/aws/data/iot/thing_registration_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_registration_task" });

      data(ThingRegistrationTask, "example", {
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingRegistrationTask, ThingRegistrationTaskConfig

    def describe():
        ubx.intent("look up aws_iot_thing_registration_task")

        ubx.data(ThingRegistrationTask, "example", ThingRegistrationTaskConfig(
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="input_file_bucket" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="input_file_key" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="percentage_progress" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="success_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="template_body" type="string">
  Returned by the lookup.
</ResponseField>
