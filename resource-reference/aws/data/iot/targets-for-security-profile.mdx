---
title: "aws_iot_targets_for_security_profile"
description: "`aws_iot_targets_for_security_profile` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_targets_for_security_profile` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_targets_for_security_profile"})

    		ubx.Data(iot.TargetsForSecurityProfile, "example", iot.TargetsForSecurityProfileConfig{
    			SecurityProfileName: "example-security-profile-name",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TargetsForSecurityProfile } from "@ubx/sdk-aws/aws/data/iot/targets_for_security_profile";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_targets_for_security_profile" });

      data(TargetsForSecurityProfile, "example", {
        securityProfileName: "example-security-profile-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import TargetsForSecurityProfile, TargetsForSecurityProfileConfig

    def describe():
        ubx.intent("look up aws_iot_targets_for_security_profile")

        ubx.data(TargetsForSecurityProfile, "example", TargetsForSecurityProfileConfig(
            security_profile_name="example-security-profile-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="security_profile_targets" type="list(object)">
  Returned by the lookup.
</ResponseField>
