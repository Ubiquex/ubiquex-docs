---
title: "aws_iot_certificates_by_ca"
description: "`aws_iot_certificates_by_ca` reads the real device certificates signed by a given CA certificate."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_certificates_by_ca"})

    		ubx.Data(iot.CertificatesByCa, "example", iot.CertificatesByCaConfig{
    			CaCertificateId: "example",
    			AscendingOrder:  true,
    			Marker:          "example",
    			PageSize:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CertificatesByCa } from "@ubx/sdk-aws/aws/data/iot/certificates_by_ca";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_certificates_by_ca" });

      data(CertificatesByCa, "example", {
        caCertificateId: "example",
        ascendingOrder: true,
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import CertificatesByCa, CertificatesByCaConfig

    def describe():
        ubx.intent("look up aws_iot_certificates_by_ca")

        ubx.data(CertificatesByCa, "example", CertificatesByCaConfig(
            ca_certificate_id="example",
            ascending_order=True,
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Whether results are returned in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ca_certificate_id" type="string" required>
  The real, unique identifier of the ca certificate this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificates" type="list(object)">
  The real certificates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
