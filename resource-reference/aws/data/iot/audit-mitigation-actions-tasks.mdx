---
title: "aws_iot_audit_mitigation_actions_tasks"
description: "`aws_iot_audit_mitigation_actions_tasks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_audit_mitigation_actions_tasks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_mitigation_actions_tasks"})

    		ubx.Data(iot.AuditMitigationActionsTasks, "example", iot.AuditMitigationActionsTasksConfig{
    			EndTime:     "example",
    			StartTime:   "example",
    			AuditTaskId: "example",
    			FindingId:   "example",
    			MaxResults:  1,
    			TaskStatus:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuditMitigationActionsTasks } from "@ubx/sdk-aws/aws/data/iot/audit_mitigation_actions_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_mitigation_actions_tasks" });

      data(AuditMitigationActionsTasks, "example", {
        endTime: "example",
        startTime: "example",
        auditTaskId: "example",
        findingId: "example",
        maxResults: 1,
        taskStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditMitigationActionsTasks, AuditMitigationActionsTasksConfig

    def describe():
        ubx.intent("look up aws_iot_audit_mitigation_actions_tasks")

        ubx.data(AuditMitigationActionsTasks, "example", AuditMitigationActionsTasksConfig(
            end_time="example",
            start_time="example",
            audit_task_id="example",
            finding_id="example",
            max_results=1,
            task_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audit_task_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="finding_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="task_status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  Returned by the lookup.
</ResponseField>
