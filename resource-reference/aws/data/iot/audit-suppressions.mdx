---
title: "aws_iot_audit_suppressions"
description: "`aws_iot_audit_suppressions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_audit_suppressions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_audit_suppressions"})

    		ubx.Data(iot.AuditSuppressions, "example", iot.AuditSuppressionsConfig{
    			AscendingOrder:     true,
    			CheckName:          "example-check-name",
    			MaxResults:         1,
    			ResourceIdentifier: map[string]any{"account": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuditSuppressions } from "@ubx/sdk-aws/aws/data/iot/audit_suppressions";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_audit_suppressions" });

      data(AuditSuppressions, "example", {
        ascendingOrder: true,
        checkName: "example-check-name",
        maxResults: 1,
        resourceIdentifier: { account: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AuditSuppressions, AuditSuppressionsConfig

    def describe():
        ubx.intent("look up aws_iot_audit_suppressions")

        ubx.data(AuditSuppressions, "example", AuditSuppressionsConfig(
            ascending_order=True,
            check_name="example-check-name",
            max_results=1,
            resource_identifier={"account": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="check_name" type="string">
  &lt;p&gt;An audit check name. Checks must be enabled for your account. (Use &lt;code&gt;DescribeAccountAuditConfiguration&lt;/code&gt; to see the list of all checks, including those that are enabled or use &lt;code&gt;UpdateAccountAuditConfiguration&lt;/code&gt; to select which checks are enabled.)&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier" type="object">
  &lt;p&gt;Information that identifies the noncompliant resource.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ca_certificate_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="client_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cognito_identity_pool_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_certificate_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_certificate_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="issuer_certificate_identifier" type="object">
      &lt;p&gt;The certificate issuer indentifier.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="issuer_certificate_serial_number" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="issuer_certificate_subject" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="issuer_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="policy_version_identifier" type="object">
      &lt;p&gt;Information about the version of the policy associated with the resource.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="policy_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_version_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role_alias_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="suppressions" type="list(object)">
  Returned by the lookup.
</ResponseField>
