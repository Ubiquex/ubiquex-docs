---
title: "aws_iot_policy_principals"
description: "`aws_iot_policy_principals` reads the real principals (certificates or Cognito identities) an IoT policy is attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_policy_principals"})

    		ubx.Data(iot.PolicyPrincipals, "example", iot.PolicyPrincipalsConfig{
    			PolicyName:     "example-policy-name",
    			AscendingOrder: true,
    			Marker:         "example",
    			PageSize:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PolicyPrincipals } from "@ubx/sdk-aws/aws/data/iot/policy_principals";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_policy_principals" });

      data(PolicyPrincipals, "example", {
        policyName: "example-policy-name",
        ascendingOrder: true,
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import PolicyPrincipals, PolicyPrincipalsConfig

    def describe():
        ubx.intent("look up aws_iot_policy_principals")

        ubx.data(PolicyPrincipals, "example", PolicyPrincipalsConfig(
            policy_name="example-policy-name",
            ascending_order=True,
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending_order" type="bool">
  Whether results are returned in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="principals" type="list(string)">
  The real IAM principals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
