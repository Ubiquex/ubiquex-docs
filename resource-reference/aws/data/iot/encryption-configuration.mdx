---
title: "aws_iot_encryption_configuration"
description: "`aws_iot_encryption_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_encryption_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_encryption_configuration"})

    		ubx.Data(iot.EncryptionConfiguration, "example", iot.EncryptionConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EncryptionConfiguration } from "@ubx/sdk-aws/aws/data/iot/encryption_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_encryption_configuration" });

      data(EncryptionConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import EncryptionConfiguration, EncryptionConfigurationConfig

    def describe():
        ubx.intent("look up aws_iot_encryption_configuration")

        ubx.data(EncryptionConfiguration, "example", EncryptionConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration_details" type="object">
  &lt;p&gt;The encryption configuration details that include the status information of the Key Management Service (KMS) key and the KMS access role.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="configuration_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="error_code" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="encryption_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="kms_access_role_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  Returned by the lookup.
</ResponseField>
