---
title: "aws_iot_statistics"
description: "`aws_iot_statistics` reads real, computed statistics (count, average, standard deviation) across a Fleet Indexing aggregation query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_statistics"})

    		ubx.Data(iot.Statistics, "example", iot.StatisticsConfig{
    			QueryString:      "example",
    			AggregationField: "example",
    			IndexName:        "example-index-name",
    			QueryVersion:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Statistics } from "./local-sdk/aws/sdk/typescript/aws/data/iot/statistics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_statistics" });

      data(Statistics, "example", {
        queryString: "example",
        aggregationField: "example",
        indexName: "example-index-name",
        queryVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Statistics, StatisticsConfig

    def describe():
        ubx.intent("look up aws_iot_statistics")

        ubx.data(Statistics, "example", StatisticsConfig(
            query_string="example",
            aggregation_field="example",
            index_name="example-index-name",
            query_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_field" type="string">
  The real field results are aggregated by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_name" type="string">
  The real name of the index this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  The real, raw query string this request runs. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="query_version" type="string">
  The real version of the query this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statistics" type="object">
  &lt;p&gt;A map of key-value pairs for all supported statistics. For issues with missing or unexpected values for this API, consult &lt;a href="https://docs.aws.amazon.com/iot/latest/developerguide/fleet-indexing-troubleshooting.html"&gt; Fleet indexing troubleshooting guide&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="average" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="maximum" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="minimum" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="std_deviation" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sum" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sum_of_squares" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="variance" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
