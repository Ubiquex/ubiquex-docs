---
title: "aws_iot_attached_policies"
description: "`aws_iot_attached_policies` reads the real IoT policies attached to a specific certificate or principal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_attached_policies"})

    		ubx.Data(iot.AttachedPolicies, "example", iot.AttachedPoliciesConfig{
    			Target:    "example",
    			Marker:    "example",
    			PageSize:  1,
    			Recursive: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachedPolicies } from "@ubx/sdk-aws/aws/data/iot/attached_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_attached_policies" });

      data(AttachedPolicies, "example", {
        target: "example",
        marker: "example",
        pageSize: 1,
        recursive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import AttachedPolicies, AttachedPoliciesConfig

    def describe():
        ubx.intent("look up aws_iot_attached_policies")

        ubx.data(AttachedPolicies, "example", AttachedPoliciesConfig(
            target="example",
            marker="example",
            page_size=1,
            recursive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Whether this action applies recursively. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target" type="string" required>
  The real object this action targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policies" type="list(object)">
  The real access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
