---
title: "aws_iot_things_in_billing_group"
description: "`aws_iot_things_in_billing_group` reads the real things that belong to a specific billing group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_things_in_billing_group"})

    		ubx.Data(iot.ThingsInBillingGroup, "example", iot.ThingsInBillingGroupConfig{
    			BillingGroupName: "example-billing-group-name",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingsInBillingGroup } from "@ubx/sdk-aws/aws/data/iot/things_in_billing_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_things_in_billing_group" });

      data(ThingsInBillingGroup, "example", {
        billingGroupName: "example-billing-group-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingsInBillingGroup, ThingsInBillingGroupConfig

    def describe():
        ubx.intent("look up aws_iot_things_in_billing_group")

        ubx.data(ThingsInBillingGroup, "example", ThingsInBillingGroupConfig(
            billing_group_name="example-billing-group-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_group_name" type="string" required>
  The real name of the billing group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="things" type="list(string)">
  The real AWS IoT things included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
