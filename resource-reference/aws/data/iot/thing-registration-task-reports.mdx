---
title: "aws_iot_thing_registration_task_reports"
description: "`aws_iot_thing_registration_task_reports` reads the real, generated result reports of a bulk thing registration task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_registration_task_reports"})

    		ubx.Data(iot.ThingRegistrationTaskReports, "example", iot.ThingRegistrationTaskReportsConfig{
    			ReportType: "example",
    			TaskId:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingRegistrationTaskReports } from "@ubx/sdk-aws/aws/data/iot/thing_registration_task_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_registration_task_reports" });

      data(ThingRegistrationTaskReports, "example", {
        reportType: "example",
        taskId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingRegistrationTaskReports, ThingRegistrationTaskReportsConfig

    def describe():
        ubx.intent("look up aws_iot_thing_registration_task_reports")

        ubx.data(ThingRegistrationTaskReports, "example", ThingRegistrationTaskReportsConfig(
            report_type="example",
            task_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="report_type" type="string" required>
  The real, discriminating type of the report this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="task_id" type="string" required>
  The real, unique identifier of the task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_links" type="list(string)">
  The real resource links included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
