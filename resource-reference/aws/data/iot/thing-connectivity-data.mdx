---
title: "aws_iot_thing_connectivity_data"
description: "`aws_iot_thing_connectivity_data` reads the real, current MQTT connection status of a thing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_connectivity_data"})

    		ubx.Data(iot.ThingConnectivityData, "example", iot.ThingConnectivityDataConfig{
    			ThingName:                "example-thing-name",
    			IncludeSocketInformation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingConnectivityData } from "@ubx/sdk-aws/aws/data/iot/thing_connectivity_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_connectivity_data" });

      data(ThingConnectivityData, "example", {
        thingName: "example-thing-name",
        includeSocketInformation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingConnectivityData, ThingConnectivityDataConfig

    def describe():
        ubx.intent("look up aws_iot_thing_connectivity_data")

        ubx.data(ThingConnectivityData, "example", ThingConnectivityDataConfig(
            thing_name="example-thing-name",
            include_socket_information=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_socket_information" type="bool">
  Whether the real, underlying socket information is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clean_session" type="bool">
  Whether this MQTT connection starts with a clean session. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="client_id" type="string">
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connected" type="bool">
  Whether this object is currently connected. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="disconnect_reason" type="string">
  The real, human-readable reason for the disconnect. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="keep_alive_duration" type="number">
  The real duration this connection is kept alive without activity, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_expiry" type="number">
  The real timestamp this session expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_ip" type="string">
  The real source IP address of this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_port" type="number">
  The real source port of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_ip" type="string">
  The real target IP address of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_port" type="number">
  The real target port of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="string">
  The real timestamp of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  The real, unique identifier of the vpc endpoint this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
