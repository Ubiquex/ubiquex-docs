---
title: "aws_iot_managed_job_template"
description: "`aws_iot_managed_job_template` looks up a real, AWS-managed IoT Jobs job template, such as one for an OTA update."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_managed_job_template"})

    		ubx.Data(iot.ManagedJobTemplate, "example", iot.ManagedJobTemplateConfig{
    			TemplateName: "example-template-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedJobTemplate } from "@ubx/sdk-aws/aws/data/iot/managed_job_template";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_managed_job_template" });

      data(ManagedJobTemplate, "example", {
        templateName: "example-template-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ManagedJobTemplate, ManagedJobTemplateConfig

    def describe():
        ubx.intent("look up aws_iot_managed_job_template")

        ubx.data(ManagedJobTemplate, "example", ManagedJobTemplateConfig(
            template_name="example-template-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="template_name" type="string" required>
  The real name of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document" type="string">
  The real document content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document_parameters" type="list(object)">
  The real, structured parameters of this document. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="environments" type="list(string)">
  The real environments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="template_arn" type="string">
  The real Amazon Resource Name (ARN) of the template this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="template_version" type="string">
  The real version of the template. **(AI-inferred)** Returned by the lookup.
</ResponseField>
