---
title: "aws_iot_thing_groups"
description: "`aws_iot_thing_groups` looks up a real thing group, an organizational grouping of IoT things."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_groups"})

    		ubx.Data(iot.ThingGroups, "example", iot.ThingGroupsConfig{
    			MaxResults:       1,
    			NamePrefixFilter: "example",
    			ParentGroup:      "example",
    			Recursive:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingGroups } from "@ubx/sdk-aws/aws/data/iot/thing_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_groups" });

      data(ThingGroups, "example", {
        maxResults: 1,
        namePrefixFilter: "example",
        parentGroup: "example",
        recursive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingGroups, ThingGroupsConfig

    def describe():
        ubx.intent("look up aws_iot_thing_groups")

        ubx.data(ThingGroups, "example", ThingGroupsConfig(
            max_results=1,
            name_prefix_filter="example",
            parent_group="example",
            recursive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix_filter" type="string">
  Restricts results to objects whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parent_group" type="string">
  The real parent group. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Whether this action applies recursively. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thing_groups" type="list(object)">
  The real thing groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
