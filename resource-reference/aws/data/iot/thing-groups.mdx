---
title: "aws_iot_thing_groups"
description: "`aws_iot_thing_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_thing_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_thing_groups"})

    		ubx.Data(iot.ThingGroups, "example", iot.ThingGroupsConfig{
    			MaxResults:       1,
    			NamePrefixFilter: "example",
    			ParentGroup:      "example",
    			Recursive:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingGroups } from "@ubx/sdk-aws/aws/data/iot/thing_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_thing_groups" });

      data(ThingGroups, "example", {
        maxResults: 1,
        namePrefixFilter: "example",
        parentGroup: "example",
        recursive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingGroups, ThingGroupsConfig

    def describe():
        ubx.intent("look up aws_iot_thing_groups")

        ubx.data(ThingGroups, "example", ThingGroupsConfig(
            max_results=1,
            name_prefix_filter="example",
            parent_group="example",
            recursive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix_filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="parent_group" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="thing_groups" type="list(object)">
  Returned by the lookup.
</ResponseField>
