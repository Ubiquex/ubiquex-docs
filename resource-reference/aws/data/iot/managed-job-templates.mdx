---
title: "aws_iot_managed_job_templates"
description: "`aws_iot_managed_job_templates` reads the real, AWS-managed IoT Jobs job templates available."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_managed_job_templates"})

    		ubx.Data(iot.ManagedJobTemplates, "example", iot.ManagedJobTemplatesConfig{
    			MaxResults:   1,
    			TemplateName: "example-template-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedJobTemplates } from "@ubx/sdk-aws/aws/data/iot/managed_job_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_managed_job_templates" });

      data(ManagedJobTemplates, "example", {
        maxResults: 1,
        templateName: "example-template-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ManagedJobTemplates, ManagedJobTemplatesConfig

    def describe():
        ubx.intent("look up aws_iot_managed_job_templates")

        ubx.data(ManagedJobTemplates, "example", ManagedJobTemplatesConfig(
            max_results=1,
            template_name="example-template-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_name" type="string">
  The real name of the template this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_job_templates" type="list(object)">
  The real, managed job templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
