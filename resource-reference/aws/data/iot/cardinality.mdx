---
title: "aws_iot_cardinality"
description: "`aws_iot_cardinality` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_cardinality` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_cardinality"})

    		ubx.Data(iot.Cardinality, "example", iot.CardinalityConfig{
    			QueryString:      "example",
    			AggregationField: "example",
    			IndexName:        "example-index-name",
    			QueryVersion:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Cardinality } from "@ubx/sdk-aws/aws/data/iot/cardinality";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_cardinality" });

      data(Cardinality, "example", {
        queryString: "example",
        aggregationField: "example",
        indexName: "example-index-name",
        queryVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Cardinality, CardinalityConfig

    def describe():
        ubx.intent("look up aws_iot_cardinality")

        ubx.data(Cardinality, "example", CardinalityConfig(
            query_string="example",
            aggregation_field="example",
            index_name="example-index-name",
            query_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_field" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="index_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="query_version" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cardinality" type="number">
  Returned by the lookup.
</ResponseField>
