---
title: "aws_iot_cardinality"
description: "`aws_iot_cardinality` reads the real, distinct-value count of a Fleet Indexing aggregation query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_cardinality"})

    		ubx.Data(iot.Cardinality, "example", iot.CardinalityConfig{
    			QueryString:      "example",
    			AggregationField: "example",
    			IndexName:        "example-index-name",
    			QueryVersion:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Cardinality } from "./local-sdk/aws/sdk/typescript/aws/data/iot/cardinality.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_cardinality" });

      data(Cardinality, "example", {
        queryString: "example",
        aggregationField: "example",
        indexName: "example-index-name",
        queryVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import Cardinality, CardinalityConfig

    def describe():
        ubx.intent("look up aws_iot_cardinality")

        ubx.data(Cardinality, "example", CardinalityConfig(
            query_string="example",
            aggregation_field="example",
            index_name="example-index-name",
            query_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_field" type="string">
  The real field results are aggregated by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_name" type="string">
  The real name of the index this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  The real, raw query string this request runs. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="query_version" type="string">
  The real version of the query this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cardinality" type="number">
  The real number of distinct values for this field. **(AI-inferred)** Returned by the lookup.
</ResponseField>
