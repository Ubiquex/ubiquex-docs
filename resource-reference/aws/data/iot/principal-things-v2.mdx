---
title: "aws_iot_principal_things_v2"
description: "`aws_iot_principal_things_v2` reads the real things a given principal (certificate) is attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_principal_things_v2"})

    		ubx.Data(iot.PrincipalThingsV2, "example", iot.PrincipalThingsV2Config{
    			Principal:          "example",
    			MaxResults:         1,
    			ThingPrincipalType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrincipalThingsV2 } from "@ubx/sdk-aws/aws/data/iot/principal_things_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_principal_things_v2" });

      data(PrincipalThingsV2, "example", {
        principal: "example",
        maxResults: 1,
        thingPrincipalType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import PrincipalThingsV2, PrincipalThingsV2Config

    def describe():
        ubx.intent("look up aws_iot_principal_things_v2")

        ubx.data(PrincipalThingsV2, "example", PrincipalThingsV2Config(
            principal="example",
            max_results=1,
            thing_principal_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal" type="string" required>
  The real IAM principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  The real, discriminating type of the thing principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="principal_thing_objects" type="list(object)">
  The real AWS IoT things this principal is attached to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
