---
title: "aws_iot_active_violations"
description: "`aws_iot_active_violations` reads a real, currently active Device Defender security profile violation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_active_violations"})

    		ubx.Data(iot.ActiveViolations, "example", iot.ActiveViolationsConfig{
    			BehaviorCriteriaType: "example",
    			ListSuppressedAlerts: true,
    			MaxResults:           1,
    			SecurityProfileName:  "example-security-profile-name",
    			ThingName:            "example-thing-name",
    			VerificationState:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ActiveViolations } from "./local-sdk/aws/sdk/typescript/aws/data/iot/active_violations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_active_violations" });

      data(ActiveViolations, "example", {
        behaviorCriteriaType: "example",
        listSuppressedAlerts: true,
        maxResults: 1,
        securityProfileName: "example-security-profile-name",
        thingName: "example-thing-name",
        verificationState: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ActiveViolations, ActiveViolationsConfig

    def describe():
        ubx.intent("look up aws_iot_active_violations")

        ubx.data(ActiveViolations, "example", ActiveViolationsConfig(
            behavior_criteria_type="example",
            list_suppressed_alerts=True,
            max_results=1,
            security_profile_name="example-security-profile-name",
            thing_name="example-thing-name",
            verification_state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="behavior_criteria_type" type="string">
  The real, discriminating type of the behavior criteria this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_suppressed_alerts" type="bool">
  Whether suppressed alerts are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_name" type="string">
  The real name of the security profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string">
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="verification_state" type="string">
  The real, current state of the verification this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_violations" type="list(object)">
  The real, currently active violations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
