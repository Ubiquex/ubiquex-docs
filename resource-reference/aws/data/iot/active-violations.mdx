---
title: "aws_iot_active_violations"
description: "`aws_iot_active_violations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_active_violations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_active_violations"})

    		ubx.Data(iot.ActiveViolations, "example", iot.ActiveViolationsConfig{
    			BehaviorCriteriaType: "example",
    			ListSuppressedAlerts: true,
    			MaxResults:           1,
    			SecurityProfileName:  "example-security-profile-name",
    			ThingName:            "example-thing-name",
    			VerificationState:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActiveViolations } from "@ubx/sdk-aws/aws/data/iot/active_violations";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_active_violations" });

      data(ActiveViolations, "example", {
        behaviorCriteriaType: "example",
        listSuppressedAlerts: true,
        maxResults: 1,
        securityProfileName: "example-security-profile-name",
        thingName: "example-thing-name",
        verificationState: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ActiveViolations, ActiveViolationsConfig

    def describe():
        ubx.intent("look up aws_iot_active_violations")

        ubx.data(ActiveViolations, "example", ActiveViolationsConfig(
            behavior_criteria_type="example",
            list_suppressed_alerts=True,
            max_results=1,
            security_profile_name="example-security-profile-name",
            thing_name="example-thing-name",
            verification_state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="behavior_criteria_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="list_suppressed_alerts" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="verification_state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_violations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
