---
title: "aws_iot_billing_groups"
description: "`aws_iot_billing_groups` looks up a real IoT billing group, used to track usage costs for a set of things."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_billing_groups"})

    		ubx.Data(iot.BillingGroups, "example", iot.BillingGroupsConfig{
    			MaxResults:       1,
    			NamePrefixFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BillingGroups } from "./local-sdk/aws/sdk/typescript/aws/data/iot/billing_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_billing_groups" });

      data(BillingGroups, "example", {
        maxResults: 1,
        namePrefixFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import BillingGroups, BillingGroupsConfig

    def describe():
        ubx.intent("look up aws_iot_billing_groups")

        ubx.data(BillingGroups, "example", BillingGroupsConfig(
            max_results=1,
            name_prefix_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix_filter" type="string">
  Restricts results to objects whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="billing_groups" type="list(object)">
  The real billing groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
