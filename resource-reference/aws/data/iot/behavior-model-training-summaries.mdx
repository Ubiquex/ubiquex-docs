---
title: "aws_iot_behavior_model_training_summaries"
description: "`aws_iot_behavior_model_training_summaries` reads a real, summarized status of training a Device Defender ML Detect behavior model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_behavior_model_training_summaries"})

    		ubx.Data(iot.BehaviorModelTrainingSummaries, "example", iot.BehaviorModelTrainingSummariesConfig{
    			MaxResults:          1,
    			SecurityProfileName: "example-security-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BehaviorModelTrainingSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/iot/behavior_model_training_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_behavior_model_training_summaries" });

      data(BehaviorModelTrainingSummaries, "example", {
        maxResults: 1,
        securityProfileName: "example-security-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import BehaviorModelTrainingSummaries, BehaviorModelTrainingSummariesConfig

    def describe():
        ubx.intent("look up aws_iot_behavior_model_training_summaries")

        ubx.data(BehaviorModelTrainingSummaries, "example", BehaviorModelTrainingSummariesConfig(
            max_results=1,
            security_profile_name="example-security-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_name" type="string">
  The real name of the security profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
