---
title: "aws_iot_things_in_thing_group"
description: "`aws_iot_things_in_thing_group` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iot_things_in_thing_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_things_in_thing_group"})

    		ubx.Data(iot.ThingsInThingGroup, "example", iot.ThingsInThingGroupConfig{
    			ThingGroupName: "example-thing-group-name",
    			MaxResults:     1,
    			Recursive:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingsInThingGroup } from "@ubx/sdk-aws/aws/data/iot/things_in_thing_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_things_in_thing_group" });

      data(ThingsInThingGroup, "example", {
        thingGroupName: "example-thing-group-name",
        maxResults: 1,
        recursive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iot import ThingsInThingGroup, ThingsInThingGroupConfig

    def describe():
        ubx.intent("look up aws_iot_things_in_thing_group")

        ubx.data(ThingsInThingGroup, "example", ThingsInThingGroupConfig(
            thing_group_name="example-thing-group-name",
            max_results=1,
            recursive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="things" type="list(string)">
  Returned by the lookup.
</ResponseField>
