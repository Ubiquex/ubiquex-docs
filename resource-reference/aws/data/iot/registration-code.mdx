---
title: "aws_iot_registration_code"
description: "`aws_iot_registration_code` reads the real registration code IoT Core requires to verify ownership of a CA certificate before registering it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iot_registration_code"})

    		ubx.Data(iot.RegistrationCode, "example", iot.RegistrationCodeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RegistrationCode } from "./local-sdk/aws/sdk/typescript/aws/data/iot/registration_code.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iot_registration_code" });

      data(RegistrationCode, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iot import RegistrationCode, RegistrationCodeConfig

    def describe():
        ubx.intent("look up aws_iot_registration_code")

        ubx.data(RegistrationCode, "example", RegistrationCodeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="registration_code" type="string">
  The real code identifying the registration. **(AI-inferred)** Returned by the lookup.
</ResponseField>
