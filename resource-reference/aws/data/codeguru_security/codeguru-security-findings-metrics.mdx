---
title: "aws_codeguru_security_findings_metrics"
description: "`aws_codeguru_security_findings_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_security_findings_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_security_findings_metrics"})

    		ubx.Data(codeguru_security.CodeguruSecurityFindingsMetrics, "example", codeguru_security.CodeguruSecurityFindingsMetricsConfig{
    			EndDate:    "example",
    			StartDate:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruSecurityFindingsMetrics } from "@ubx/sdk-aws/aws/data/codeguru_security/codeguru_security_findings_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_security_findings_metrics" });

      data(CodeguruSecurityFindingsMetrics, "example", {
        endDate: "example",
        startDate: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_security import CodeguruSecurityFindingsMetrics, CodeguruSecurityFindingsMetricsConfig

    def describe():
        ubx.intent("look up aws_codeguru_security_findings_metrics")

        ubx.data(CodeguruSecurityFindingsMetrics, "example", CodeguruSecurityFindingsMetricsConfig(
            end_date="example",
            start_date="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_date" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings_metrics" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
