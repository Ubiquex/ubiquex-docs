---
title: "aws_codeguru_security_metrics_summary"
description: "`aws_codeguru_security_metrics_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_security_metrics_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_security_metrics_summary"})

    		ubx.Data(codeguru_security.CodeguruSecurityMetricsSummary, "example", codeguru_security.CodeguruSecurityMetricsSummaryConfig{
    			Date: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruSecurityMetricsSummary } from "@ubx/sdk-aws/aws/data/codeguru_security/codeguru_security_metrics_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_security_metrics_summary" });

      data(CodeguruSecurityMetricsSummary, "example", {
        date: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_security import CodeguruSecurityMetricsSummary, CodeguruSecurityMetricsSummaryConfig

    def describe():
        ubx.intent("look up aws_codeguru_security_metrics_summary")

        ubx.data(CodeguruSecurityMetricsSummary, "example", CodeguruSecurityMetricsSummaryConfig(
            date="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="date" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metrics_summary" type="object">
  &lt;p&gt;A summary of metrics for an account as of a specified date.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="categories_with_most_findings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="open_findings" type="object">
      &lt;p&gt;A numeric value corresponding to the severity of a finding, such as the number of open findings or the average time it takes to close findings of a given severity.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="critical" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="high" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="info" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="low" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="medium" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scans_with_most_open_critical_findings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scans_with_most_open_findings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
