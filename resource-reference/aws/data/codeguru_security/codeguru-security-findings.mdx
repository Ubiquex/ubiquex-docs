---
title: "aws_codeguru_security_findings"
description: "`aws_codeguru_security_findings` reads the real findings from a CodeGuru Security scan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeguru_security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_security_findings"})

    		ubx.Data(codeguru_security.CodeguruSecurityFindings, "example", codeguru_security.CodeguruSecurityFindingsConfig{
    			ScanName:   "example-scan-name",
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodeguruSecurityFindings } from "./local-sdk/aws/sdk/typescript/aws/data/codeguru_security/codeguru_security_findings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_security_findings" });

      data(CodeguruSecurityFindings, "example", {
        scanName: "example-scan-name",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_security import CodeguruSecurityFindings, CodeguruSecurityFindingsConfig

    def describe():
        ubx.intent("look up aws_codeguru_security_findings")

        ubx.data(CodeguruSecurityFindings, "example", CodeguruSecurityFindingsConfig(
            scan_name="example-scan-name",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scan_name" type="string" required>
  The real name of the scan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
