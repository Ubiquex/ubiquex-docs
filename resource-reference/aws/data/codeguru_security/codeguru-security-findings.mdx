---
title: "aws_codeguru_security_findings"
description: "`aws_codeguru_security_findings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_security_findings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_security "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_security_findings"})

    		ubx.Data(codeguru_security.CodeguruSecurityFindings, "example", codeguru_security.CodeguruSecurityFindingsConfig{
    			ScanName:   "example-scan-name",
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruSecurityFindings } from "@ubx/sdk-aws/aws/data/codeguru_security/codeguru_security_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_security_findings" });

      data(CodeguruSecurityFindings, "example", {
        scanName: "example-scan-name",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_security import CodeguruSecurityFindings, CodeguruSecurityFindingsConfig

    def describe():
        ubx.intent("look up aws_codeguru_security_findings")

        ubx.data(CodeguruSecurityFindings, "example", CodeguruSecurityFindingsConfig(
            scan_name="example-scan-name",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scan_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
