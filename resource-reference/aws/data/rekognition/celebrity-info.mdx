---
title: "aws_rekognition_celebrity_info"
description: "`aws_rekognition_celebrity_info` reads real, published information about a celebrity Rekognition recognized in an image or video."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	rekognition "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rekognition"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rekognition_celebrity_info"})

    		ubx.Data(rekognition.CelebrityInfo, "example", rekognition.CelebrityInfoConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CelebrityInfo } from "./local-sdk/aws/sdk/typescript/aws/data/rekognition/celebrity_info.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_rekognition_celebrity_info" });

      data(CelebrityInfo, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.rekognition import CelebrityInfo, CelebrityInfoConfig

    def describe():
        ubx.intent("look up aws_rekognition_celebrity_info")

        ubx.data(CelebrityInfo, "example", CelebrityInfoConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="known_gender" type="object">
  &lt;p&gt;The known gender identity for the celebrity that matches the provided ID. The known gender identity can be Male, Female, Nonbinary, or Unlisted.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="type" type="string">
      &lt;p&gt;A list of enum string of possible gender values that Celebrity returns.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="urls" type="list(string)">
  The real URLs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
