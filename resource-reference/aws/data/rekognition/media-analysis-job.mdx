---
title: "aws_rekognition_media_analysis_job"
description: "`aws_rekognition_media_analysis_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rekognition_media_analysis_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rekognition "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rekognition"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rekognition_media_analysis_job"})

    		ubx.Data(rekognition.MediaAnalysisJob, "example", rekognition.MediaAnalysisJobConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediaAnalysisJob } from "@ubx/sdk-aws/aws/data/rekognition/media_analysis_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_rekognition_media_analysis_job" });

      data(MediaAnalysisJob, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rekognition import MediaAnalysisJob, MediaAnalysisJobConfig

    def describe():
        ubx.intent("look up aws_rekognition_media_analysis_job")

        ubx.data(MediaAnalysisJob, "example", MediaAnalysisJobConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="completion_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_details" type="object">
  &lt;p&gt;Details about the error that resulted in failure of the job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="input" type="object">
  &lt;p&gt;Contains input information for a media analysis job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_object" type="object" required>
      &lt;p&gt;Provides the S3 bucket name and object name.&lt;/p&gt; &lt;p&gt;The region for the S3 bucket containing the S3 object must match the region you use for Amazon Rekognition operations.&lt;/p&gt; &lt;p&gt;For Amazon Rekognition to process an S3 object, the user must have permission to access the S3 object. For more information, see How Amazon Rekognition works with IAM in the Amazon Rekognition Developer Guide. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="manifest_summary" type="object">
  &lt;p&gt;Summary that provides statistics on input manifest and errors identified in the input manifest.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_object" type="object">
      &lt;p&gt;Provides the S3 bucket name and object name.&lt;/p&gt; &lt;p&gt;The region for the S3 bucket containing the S3 object must match the region you use for Amazon Rekognition operations.&lt;/p&gt; &lt;p&gt;For Amazon Rekognition to process an S3 object, the user must have permission to access the S3 object. For more information, see How Amazon Rekognition works with IAM in the Amazon Rekognition Developer Guide. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="operations_config" type="object">
  &lt;p&gt;Configuration options for a media analysis job. Configuration is operation-specific.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="detect_moderation_labels" type="object">
      &lt;p&gt;Configuration for Moderation Labels Detection.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="min_confidence" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="project_version" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="output_config" type="object">
  &lt;p&gt;Output configuration provided in the job creation request.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_bucket" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="s3_key_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="results" type="object">
  &lt;p&gt;Contains the results for a media analysis job created with StartMediaAnalysisJob.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="model_versions" type="object">
      &lt;p&gt;Object containing information about the model versions of selected features in a given job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="moderation" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="s3_object" type="object">
      &lt;p&gt;Provides the S3 bucket name and object name.&lt;/p&gt; &lt;p&gt;The region for the S3 bucket containing the S3 object must match the region you use for Amazon Rekognition operations.&lt;/p&gt; &lt;p&gt;For Amazon Rekognition to process an S3 object, the user must have permission to access the S3 object. For more information, see How Amazon Rekognition works with IAM in the Amazon Rekognition Developer Guide. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
