---
title: "aws_rekognition_label_detection"
description: "`aws_rekognition_label_detection` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rekognition_label_detection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rekognition "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rekognition"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rekognition_label_detection"})

    		ubx.Data(rekognition.LabelDetection, "example", rekognition.LabelDetectionConfig{
    			JobId:       "example",
    			AggregateBy: "example",
    			MaxResults:  1,
    			SortBy:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LabelDetection } from "@ubx/sdk-aws/aws/data/rekognition/label_detection";

    export default stack("example", () => {
      intent({ summary: "look up aws_rekognition_label_detection" });

      data(LabelDetection, "example", {
        jobId: "example",
        aggregateBy: "example",
        maxResults: 1,
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rekognition import LabelDetection, LabelDetectionConfig

    def describe():
        ubx.intent("look up aws_rekognition_label_detection")

        ubx.data(LabelDetection, "example", LabelDetectionConfig(
            job_id="example",
            aggregate_by="example",
            max_results=1,
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregate_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="get_request_metadata" type="object">
  &lt;p&gt;Contains metadata about a label detection request, including the SortBy and AggregateBy options.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="aggregate_by" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sort_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_tag" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="label_model_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="labels" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="video" type="object">
  &lt;p&gt;Video file stored in an Amazon S3 bucket. Amazon Rekognition video start operations such as &lt;a&gt;StartLabelDetection&lt;/a&gt; use &lt;code&gt;Video&lt;/code&gt; to specify a video for analysis. The supported file formats are .mp4, .mov and .avi.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_object" type="object">
      &lt;p&gt;Provides the S3 bucket name and object name.&lt;/p&gt; &lt;p&gt;The region for the S3 bucket containing the S3 object must match the region you use for Amazon Rekognition operations.&lt;/p&gt; &lt;p&gt;For Amazon Rekognition to process an S3 object, the user must have permission to access the S3 object. For more information, see How Amazon Rekognition works with IAM in the Amazon Rekognition Developer Guide. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="version" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="video_metadata" type="object">
  &lt;p&gt;Information about a video that Amazon Rekognition analyzed. &lt;code&gt;Videometadata&lt;/code&gt; is returned in every page of paginated responses from a Amazon Rekognition video operation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="codec" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="color_range" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="duration_millis" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="format" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="frame_height" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="frame_rate" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="frame_width" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
