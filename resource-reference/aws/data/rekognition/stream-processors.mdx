---
title: "aws_rekognition_stream_processors"
description: "`data_aws_rekognition_stream_processors` looks up a real, already-existing `aws_rekognition_stream_processor`."
---

It creates a real processor that analyzes a live Kinesis video stream in near real time, detecting faces or labels frame by frame and publishing matches, distinct from Rekognition's own batch image and video analysis APIs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rekognition "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/rekognition"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rekognition_stream_processors"})

    		ubx.Data(rekognition.StreamProcessors, "example", rekognition.StreamProcessorsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamProcessors } from "@ubx/sdk-aws/aws/data/rekognition/stream_processors";

    export default stack("example", () => {
      intent({ summary: "look up aws_rekognition_stream_processors" });

      data(StreamProcessors, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rekognition import StreamProcessors, StreamProcessorsConfig

    def describe():
        ubx.intent("look up aws_rekognition_stream_processors")

        ubx.data(StreamProcessors, "example", StreamProcessorsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_processors" type="list(object)">
  The real Amazon Rekognition stream processors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
