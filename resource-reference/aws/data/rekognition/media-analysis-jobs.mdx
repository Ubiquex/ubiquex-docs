---
title: "aws_rekognition_media_analysis_jobs"
description: "`aws_rekognition_media_analysis_jobs` reads the real Rekognition bulk media analysis jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	rekognition "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rekognition"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rekognition_media_analysis_jobs"})

    		ubx.Data(rekognition.MediaAnalysisJobs, "example", rekognition.MediaAnalysisJobsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MediaAnalysisJobs } from "./local-sdk/aws/sdk/typescript/aws/data/rekognition/media_analysis_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_rekognition_media_analysis_jobs" });

      data(MediaAnalysisJobs, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.rekognition import MediaAnalysisJobs, MediaAnalysisJobsConfig

    def describe():
        ubx.intent("look up aws_rekognition_media_analysis_jobs")

        ubx.data(MediaAnalysisJobs, "example", MediaAnalysisJobsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="media_analysis_jobs" type="list(object)">
  The real Amazon Rekognition media analysis jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
