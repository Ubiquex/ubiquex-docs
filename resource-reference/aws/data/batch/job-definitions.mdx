---
title: "aws_batch_job_definitions"
description: "`data_aws_batch_job_definitions` looks up a real, already-existing `aws_batch_job_definition`."
---

It is the real template AWS Batch launches a job from, the container image, vCPU and memory requirements, command, and retry strategy, submitted against an `aws_batch_job_queue` each time a job actually runs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_job_definitions"})

    		ubx.Data(batch.JobDefinitions, "example", batch.JobDefinitionsConfig{
    			JobDefinitionName: "example-job-definition-name",
    			MaxResults:        1,
    			Status:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobDefinitions } from "@ubx/sdk-aws/aws/data/batch/job_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_job_definitions" });

      data(JobDefinitions, "example", {
        jobDefinitionName: "example-job-definition-name",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import JobDefinitions, JobDefinitionsConfig

    def describe():
        ubx.intent("look up aws_batch_job_definitions")

        ubx.data(JobDefinitions, "example", JobDefinitionsConfig(
            job_definition_name="example-job-definition-name",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_definition_name" type="string">
  The real name of the job definition this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_definitions" type="list(string)">
  The real AWS Batch job definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
