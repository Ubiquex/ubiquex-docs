---
title: "aws_batch_job_queue_snapshot"
description: "`aws_batch_job_queue_snapshot` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_batch_job_queue_snapshot` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_job_queue_snapshot"})

    		ubx.Data(batch.JobQueueSnapshot, "example", batch.JobQueueSnapshotConfig{
    			JobQueue: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobQueueSnapshot } from "@ubx/sdk-aws/aws/data/batch/job_queue_snapshot";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_job_queue_snapshot" });

      data(JobQueueSnapshot, "example", {
        jobQueue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import JobQueueSnapshot, JobQueueSnapshotConfig

    def describe():
        ubx.intent("look up aws_batch_job_queue_snapshot")

        ubx.data(JobQueueSnapshot, "example", JobQueueSnapshotConfig(
            job_queue="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_queue" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="front_of_queue" type="object">
  &lt;p&gt;Contains a list of the first 100 &lt;code&gt;RUNNABLE&lt;/code&gt; jobs associated to a single job queue.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="jobs" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_at" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="front_of_quota_shares" type="object">
  &lt;p&gt;An object that represents the details of the first &lt;code&gt;RUNNABLE&lt;/code&gt; job in each named quota share associated with a single job queue.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="last_updated_at" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quota_shares" type="map(list(object))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="queue_utilization" type="object">
  &lt;p&gt;The job queue utilization at a specific point in time, including total capacity usage, and quota share or fairshare utilization breakdown depending on the job queue scheduling policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="fairshare_utilization" type="object">
      &lt;p&gt;The fairshare utilization for a job queue, including the number of active shares and top capacity utilization.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="active_share_count" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="top_capacity_utilization" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_updated_at" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quota_share_utilization" type="object">
      &lt;p&gt;An object that represents the capacity utilization details of all quota shares associated with a single job queue.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="top_capacity_utilization" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_capacity_usage" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
