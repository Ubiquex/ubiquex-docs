---
title: "aws_batch_service_jobs"
description: "`aws_batch_service_jobs` reads the real AWS Batch service jobs on a job queue."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_service_jobs"})

    		ubx.Data(batch.ServiceJobs, "example", batch.ServiceJobsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			JobQueue:   "example",
    			JobStatus:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceJobs } from "@ubx/sdk-aws/aws/data/batch/service_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_service_jobs" });

      data(ServiceJobs, "example", {
        filters: [{ name: "example-name" }],
        jobQueue: "example",
        jobStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import ServiceJobs, ServiceJobsConfig
    from ubx.aws.data.batch.service_jobs import ServiceJobs_Filters

    def describe():
        ubx.intent("look up aws_batch_service_jobs")

        ubx.data(ServiceJobs, "example", ServiceJobsConfig(
            filters=[ServiceJobs_Filters(name="example-name")],
            job_queue="example",
            job_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_queue" type="string">
  The real job queue this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_summary_list" type="list(object)">
  The real job summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
