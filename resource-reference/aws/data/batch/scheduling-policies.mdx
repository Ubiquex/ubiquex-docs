---
title: "aws_batch_scheduling_policies"
description: "`data_aws_batch_scheduling_policies` looks up a real, already-existing `aws_batch_scheduling_policy`."
---

It defines fair-share scheduling for an `aws_batch_job_queue`, assigning share identifiers, decay time, and weight factors so one user or workload can't monopolize a queue's compute, real logic a queue defaults to plain FIFO without.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_scheduling_policies"})

    		ubx.Data(batch.SchedulingPolicies, "example", batch.SchedulingPoliciesConfig{
    			Arns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SchedulingPolicies } from "@ubx/sdk-aws/aws/data/batch/scheduling_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_scheduling_policies" });

      data(SchedulingPolicies, "example", {
        arns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import SchedulingPolicies, SchedulingPoliciesConfig

    def describe():
        ubx.intent("look up aws_batch_scheduling_policies")

        ubx.data(SchedulingPolicies, "example", SchedulingPoliciesConfig(
            arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arns" type="list(string)" required>
  The real Amazon Resource Names (ARNs) of the objects included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="scheduling_policies" type="list(object)">
  The real scheduling policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
