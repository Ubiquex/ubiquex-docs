---
title: "aws_batch_scheduling_policies"
description: "`aws_batch_scheduling_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_batch_scheduling_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_scheduling_policies"})

    		ubx.Data(batch.SchedulingPolicies, "example", batch.SchedulingPoliciesConfig{
    			Arns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SchedulingPolicies } from "@ubx/sdk-aws/aws/data/batch/scheduling_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_scheduling_policies" });

      data(SchedulingPolicies, "example", {
        arns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import SchedulingPolicies, SchedulingPoliciesConfig

    def describe():
        ubx.intent("look up aws_batch_scheduling_policies")

        ubx.data(SchedulingPolicies, "example", SchedulingPoliciesConfig(
            arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arns" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="scheduling_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>
