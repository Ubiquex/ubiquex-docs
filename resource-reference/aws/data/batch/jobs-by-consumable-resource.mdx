---
title: "aws_batch_jobs_by_consumable_resource"
description: "`data_aws_batch_jobs_by_consumable_resource` looks up a real, already-existing `aws_batch_consumable_resource`."
---

It defines a real constraint beyond vCPU, GPU, or memory, a third-party license token pool, a database connection limit, an API rate cap, that AWS Batch's resource-aware scheduling checks before starting a job, so a job doesn't launch, consume compute time, and then fail discovering the real resource it needed was unavailable.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_batch_jobs_by_consumable_resource"})

    		ubx.Data(batch.JobsByConsumableResource, "example", batch.JobsByConsumableResourceConfig{
    			ConsumableResource: "example",
    			Filters:            []map[string]any{{"name": "example-name"}},
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobsByConsumableResource } from "@ubx/sdk-aws/aws/data/batch/jobs_by_consumable_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_batch_jobs_by_consumable_resource" });

      data(JobsByConsumableResource, "example", {
        consumableResource: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.batch import JobsByConsumableResource, JobsByConsumableResourceConfig

    def describe():
        ubx.intent("look up aws_batch_jobs_by_consumable_resource")

        ubx.data(JobsByConsumableResource, "example", JobsByConsumableResourceConfig(
            consumable_resource="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consumable_resource" type="string" required>
  The real consumable resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
