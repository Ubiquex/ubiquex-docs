---
title: "aws_iotfleetwise_vehicle_status"
description: "`aws_iotfleetwise_vehicle_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotfleetwise_vehicle_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotfleetwise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotfleetwise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotfleetwise_vehicle_status"})

    		ubx.Data(iotfleetwise.VehicleStatus, "example", iotfleetwise.VehicleStatusConfig{
    			VehicleName: "example-vehicle-name",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VehicleStatus } from "@ubx/sdk-aws/aws/data/iotfleetwise/vehicle_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotfleetwise_vehicle_status" });

      data(VehicleStatus, "example", {
        vehicleName: "example-vehicle-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotfleetwise import VehicleStatus, VehicleStatusConfig

    def describe():
        ubx.intent("look up aws_iotfleetwise_vehicle_status")

        ubx.data(VehicleStatus, "example", VehicleStatusConfig(
            vehicle_name="example-vehicle-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vehicle_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="campaigns" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
