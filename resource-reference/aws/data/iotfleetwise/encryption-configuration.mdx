---
title: "aws_iotfleetwise_encryption_configuration"
description: "`aws_iotfleetwise_encryption_configuration` reads the real, account-wide encryption configuration for IoT FleetWise."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotfleetwise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotfleetwise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotfleetwise_encryption_configuration"})

    		ubx.Data(iotfleetwise.EncryptionConfiguration, "example", iotfleetwise.EncryptionConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EncryptionConfiguration } from "@ubx/sdk-aws/aws/data/iotfleetwise/encryption_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotfleetwise_encryption_configuration" });

      data(EncryptionConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotfleetwise import EncryptionConfiguration, EncryptionConfigurationConfig

    def describe():
        ubx.intent("look up aws_iotfleetwise_encryption_configuration")

        ubx.data(EncryptionConfiguration, "example", EncryptionConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_status" type="string">
  The real, current status of the encryption this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_type" type="string">
  The real, discriminating type of the encryption this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The real, unique identifier of the AWS KMS key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modification_time" type="string">
  The real timestamp of the last modification. **(AI-inferred)** Returned by the lookup.
</ResponseField>
