---
title: "aws_contact_lens_realtime_contact_analysis_segments"
description: "`aws_contact_lens_realtime_contact_analysis_segments` reads the real, real-time Contact Lens transcript and analysis segments for an in-progress..."
---

`aws_contact_lens_realtime_contact_analysis_segments` reads the real, real-time Contact Lens transcript and analysis segments for an in-progress Connect contact.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	contact_lens "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/contact_lens"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_contact_lens_realtime_contact_analysis_segments"})

    		ubx.Data(contact_lens.ContactLensRealtimeContactAnalysisSegments, "example", contact_lens.ContactLensRealtimeContactAnalysisSegmentsConfig{
    			ContactId:  "example",
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ContactLensRealtimeContactAnalysisSegments } from "./local-sdk/aws/sdk/typescript/aws/data/contact_lens/contact_lens_realtime_contact_analysis_segments.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_contact_lens_realtime_contact_analysis_segments",
      });

      data(ContactLensRealtimeContactAnalysisSegments, "example", {
        contactId: "example",
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.contact_lens import ContactLensRealtimeContactAnalysisSegments, ContactLensRealtimeContactAnalysisSegmentsConfig

    def describe():
        ubx.intent("look up aws_contact_lens_realtime_contact_analysis_segments")

        ubx.data(ContactLensRealtimeContactAnalysisSegments, "example", ContactLensRealtimeContactAnalysisSegmentsConfig(
            contact_id="example",
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  The real, unique identifier of the contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="segments" type="list(object)">
  The real segments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
