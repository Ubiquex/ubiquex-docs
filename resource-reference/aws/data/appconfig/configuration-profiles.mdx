---
title: "aws_appconfig_configuration_profiles"
description: "`aws_appconfig_configuration_profiles` looks up a real AppConfig configuration profile, defining a source and validators for one piece of configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_configuration_profiles"})

    		ubx.Data(appconfig.ConfigurationProfiles, "example", appconfig.ConfigurationProfilesConfig{
    			ApplicationId: "example",
    			MaxResults:    1,
    			Type:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationProfiles } from "@ubx/sdk-aws/aws/data/appconfig/configuration_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_configuration_profiles" });

      data(ConfigurationProfiles, "example", {
        applicationId: "example",
        maxResults: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import ConfigurationProfiles, ConfigurationProfilesConfig

    def describe():
        ubx.intent("look up aws_appconfig_configuration_profiles")

        ubx.data(ConfigurationProfiles, "example", ConfigurationProfilesConfig(
            application_id="example",
            max_results=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
