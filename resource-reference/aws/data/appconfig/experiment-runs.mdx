---
title: "aws_appconfig_experiment_runs"
description: "`aws_appconfig_experiment_runs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appconfig_experiment_runs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_experiment_runs"})

    		ubx.Data(appconfig.ExperimentRuns, "example", appconfig.ExperimentRunsConfig{
    			ApplicationIdentifier:          "example",
    			ExperimentDefinitionIdentifier: "example",
    			MaxResults:                     1,
    			Status:                         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExperimentRuns } from "@ubx/sdk-aws/aws/data/appconfig/experiment_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_experiment_runs" });

      data(ExperimentRuns, "example", {
        applicationIdentifier: "example",
        experimentDefinitionIdentifier: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import ExperimentRuns, ExperimentRunsConfig

    def describe():
        ubx.intent("look up aws_appconfig_experiment_runs")

        ubx.data(ExperimentRuns, "example", ExperimentRunsConfig(
            application_identifier="example",
            experiment_definition_identifier="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="experiment_definition_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
