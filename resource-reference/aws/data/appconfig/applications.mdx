---
title: "aws_appconfig_applications"
description: "`aws_appconfig_applications` looks up a real AppConfig application, the top-level container for configuration profiles and environments."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_applications"})

    		ubx.Data(appconfig.Applications, "example", appconfig.ApplicationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Applications } from "@ubx/sdk-aws/aws/data/appconfig/applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_applications" });

      data(Applications, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import Applications, ApplicationsConfig

    def describe():
        ubx.intent("look up aws_appconfig_applications")

        ubx.data(Applications, "example", ApplicationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
