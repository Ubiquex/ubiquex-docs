---
title: "aws_appconfig_hosted_configuration_versions"
description: "`aws_appconfig_hosted_configuration_versions` reads a real, saved version of a configuration hosted directly within AppConfig."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_hosted_configuration_versions"})

    		ubx.Data(appconfig.HostedConfigurationVersions, "example", appconfig.HostedConfigurationVersionsConfig{
    			ApplicationId:          "example",
    			ConfigurationProfileId: "example",
    			MaxResults:             1,
    			VersionLabel:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { HostedConfigurationVersions } from "./local-sdk/aws/sdk/typescript/aws/data/appconfig/hosted_configuration_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_hosted_configuration_versions" });

      data(HostedConfigurationVersions, "example", {
        applicationId: "example",
        configurationProfileId: "example",
        maxResults: 1,
        versionLabel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import HostedConfigurationVersions, HostedConfigurationVersionsConfig

    def describe():
        ubx.intent("look up aws_appconfig_hosted_configuration_versions")

        ubx.data(HostedConfigurationVersions, "example", HostedConfigurationVersionsConfig(
            application_id="example",
            configuration_profile_id="example",
            max_results=1,
            version_label="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="configuration_profile_id" type="string" required>
  The real, unique identifier of the configuration profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="version_label" type="string">
  The real, human-readable label of this object's own version. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
