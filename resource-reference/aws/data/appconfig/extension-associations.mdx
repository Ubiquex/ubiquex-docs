---
title: "aws_appconfig_extension_associations"
description: "`aws_appconfig_extension_associations` reads the real resources an AppConfig extension (a Lambda-backed hook) is attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_extension_associations"})

    		ubx.Data(appconfig.ExtensionAssociations, "example", appconfig.ExtensionAssociationsConfig{
    			ExtensionIdentifier:    "example",
    			ExtensionVersionNumber: 1,
    			MaxResults:             1,
    			ResourceIdentifier:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExtensionAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/appconfig/extension_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_extension_associations" });

      data(ExtensionAssociations, "example", {
        extensionIdentifier: "example",
        extensionVersionNumber: 1,
        maxResults: 1,
        resourceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import ExtensionAssociations, ExtensionAssociationsConfig

    def describe():
        ubx.intent("look up aws_appconfig_extension_associations")

        ubx.data(ExtensionAssociations, "example", ExtensionAssociationsConfig(
            extension_identifier="example",
            extension_version_number=1,
            max_results=1,
            resource_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="extension_identifier" type="string">
  The real, unique identifier of the extension this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="extension_version_number" type="number">
  The real number of the extension version. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
