---
title: "aws_appconfig_experiment_definitions"
description: "`aws_appconfig_experiment_definitions` reads the real, saved definition of an AppConfig experiment, running an A/B test alongside a deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_experiment_definitions"})

    		ubx.Data(appconfig.ExperimentDefinitions, "example", appconfig.ExperimentDefinitionsConfig{
    			ApplicationIdentifier:          "example",
    			ConfigurationProfileIdentifier: "example",
    			EnvironmentIdentifier:          "example",
    			MaxResults:                     1,
    			Status:                         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExperimentDefinitions } from "@ubx/sdk-aws/aws/data/appconfig/experiment_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_experiment_definitions" });

      data(ExperimentDefinitions, "example", {
        applicationIdentifier: "example",
        configurationProfileIdentifier: "example",
        environmentIdentifier: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import ExperimentDefinitions, ExperimentDefinitionsConfig

    def describe():
        ubx.intent("look up aws_appconfig_experiment_definitions")

        ubx.data(ExperimentDefinitions, "example", ExperimentDefinitionsConfig(
            application_identifier="example",
            configuration_profile_identifier="example",
            environment_identifier="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_identifier" type="string">
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="configuration_profile_identifier" type="string">
  The real, unique identifier of the configuration profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="environment_identifier" type="string">
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
