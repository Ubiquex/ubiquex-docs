---
title: "aws_appconfig_configuration"
description: "`aws_appconfig_configuration` reads the real, deployed configuration content an AppConfig client currently receives."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_configuration"})

    		ubx.Data(appconfig.Configuration, "example", appconfig.ConfigurationConfig{
    			Application:                "example",
    			ClientId:                   "example",
    			Configuration:              "example",
    			Environment:                "example",
    			ClientConfigurationVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Configuration } from "@ubx/sdk-aws/aws/data/appconfig/configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_configuration" });

      data(Configuration, "example", {
        application: "example",
        clientId: "example",
        configuration: "example",
        environment: "example",
        clientConfigurationVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("look up aws_appconfig_configuration")

        ubx.data(Configuration, "example", ConfigurationConfig(
            application="example",
            client_id="example",
            configuration="example",
            environment="example",
            client_configuration_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application" type="string" required>
  The real application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="client_configuration_version" type="string">
  The real version of the client configuration. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="client_id" type="string" required>
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="configuration" type="string" required>
  The real, structured configuration of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment" type="string" required>
  The real deployment environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_version" type="string">
  The real version of the configuration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content" type="string">
  The real content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
