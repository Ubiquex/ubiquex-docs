---
title: "aws_appconfig_experiment_run"
description: "`aws_appconfig_experiment_run` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appconfig_experiment_run` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfig_experiment_run"})

    		ubx.Data(appconfig.ExperimentRun, "example", appconfig.ExperimentRunConfig{
    			ApplicationIdentifier:          "example",
    			ExperimentDefinitionIdentifier: "example",
    			Run:                            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExperimentRun } from "@ubx/sdk-aws/aws/data/appconfig/experiment_run";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfig_experiment_run" });

      data(ExperimentRun, "example", {
        applicationIdentifier: "example",
        experimentDefinitionIdentifier: "example",
        run: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfig import ExperimentRun, ExperimentRunConfig

    def describe():
        ubx.intent("look up aws_appconfig_experiment_run")

        ubx.data(ExperimentRun, "example", ExperimentRunConfig(
            application_identifier="example",
            experiment_definition_identifier="example",
            run=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="experiment_definition_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ended_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="experiment_definition_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="experiment_definition_snapshot" type="object">
  &lt;p&gt;A snapshot of the experiment definition captured at the time an experiment run was started. This preserves the configuration that was active during the run.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="audience_description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="audience_rule" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="configuration_profile_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="control" type="object">
      &lt;p&gt;Describes a treatment in an experiment, including its traffic allocation weight and feature flag value.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="flag_value" type="object" required>
          &lt;p&gt;The feature flag value configuration for a treatment, including the enabled state and attribute values.&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="attribute_values" type="map(object)">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="enabled" type="bool" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="key" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="weight" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="flag_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="hypothesis" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="launch_criteria" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="treatments" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="exposure_percentage" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="result" type="object">
  &lt;p&gt;The result of an experiment run, including the executive summary and launch decision rationale.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="executive_summary" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="reasons_not_to_launch" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="reasons_to_launch" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="started_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="treatment_overrides" type="object">
  &lt;p&gt;Treatment assignment overrides that assign specific entity IDs to treatments, bypassing random assignment.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="inline" type="map(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="updated_at" type="string">
  Returned by the lookup.
</ResponseField>
