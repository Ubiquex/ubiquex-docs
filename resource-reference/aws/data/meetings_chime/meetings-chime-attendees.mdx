---
title: "aws_meetings_chime_attendees"
description: "`aws_meetings_chime_attendees` looks up a real attendee within a Chime SDK meeting."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	meetings_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/meetings_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_meetings_chime_attendees"})

    		ubx.Data(meetings_chime.MeetingsChimeAttendees, "example", meetings_chime.MeetingsChimeAttendeesConfig{
    			MeetingId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MeetingsChimeAttendees } from "./local-sdk/aws/sdk/typescript/aws/data/meetings_chime/meetings_chime_attendees.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_meetings_chime_attendees" });

      data(MeetingsChimeAttendees, "example", {
        meetingId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.meetings_chime import MeetingsChimeAttendees, MeetingsChimeAttendeesConfig

    def describe():
        ubx.intent("look up aws_meetings_chime_attendees")

        ubx.data(MeetingsChimeAttendees, "example", MeetingsChimeAttendeesConfig(
            meeting_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="meeting_id" type="string" required>
  The real, unique identifier of the meeting this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attendees" type="list(object)">
  The real Amazon Chime meeting attendees included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
