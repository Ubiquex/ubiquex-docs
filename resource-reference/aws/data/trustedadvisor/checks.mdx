---
title: "aws_trustedadvisor_checks"
description: "`aws_trustedadvisor_checks` looks up a real Trusted Advisor check, under the newer Trusted Advisor API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_checks"})

    		ubx.Data(trustedadvisor.Checks, "example", trustedadvisor.ChecksConfig{
    			AwsService: "example",
    			Language:   "example",
    			MaxResults: 1,
    			Pillar:     "example",
    			Source:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Checks } from "@ubx/sdk-aws/aws/data/trustedadvisor/checks";

    export default stack("example", () => {
      intent({ summary: "look up aws_trustedadvisor_checks" });

      data(Checks, "example", {
        awsService: "example",
        language: "example",
        maxResults: 1,
        pillar: "example",
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import Checks, ChecksConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_checks")

        ubx.data(Checks, "example", ChecksConfig(
            aws_service="example",
            language="example",
            max_results=1,
            pillar="example",
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_service" type="string">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="language" type="string">
  The real language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pillar" type="string">
  The real AWS Well-Architected pillar this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string">
  The real source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="check_summaries" type="list(object)">
  The real, summarized checks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
