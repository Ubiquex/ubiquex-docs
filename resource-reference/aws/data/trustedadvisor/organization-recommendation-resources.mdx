---
title: "aws_trustedadvisor_organization_recommendation_resources"
description: "`aws_trustedadvisor_organization_recommendation_resources` reads the real resources affected by an organization-wide Trusted Advisor recommendation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_organization_recommendation_resources"})

    		ubx.Data(trustedadvisor.OrganizationRecommendationResources, "example", trustedadvisor.OrganizationRecommendationResourcesConfig{
    			OrganizationRecommendationIdentifier: "example",
    			AffectedAccountId:                    "example",
    			ExclusionStatus:                      "example",
    			MaxResults:                           1,
    			RegionCode:                           "example",
    			Status:                               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationRecommendationResources } from "@ubx/sdk-aws/aws/data/trustedadvisor/organization_recommendation_resources";

    export default stack("example", () => {
      intent({
        summary: "look up aws_trustedadvisor_organization_recommendation_resources",
      });

      data(OrganizationRecommendationResources, "example", {
        organizationRecommendationIdentifier: "example",
        affectedAccountId: "example",
        exclusionStatus: "example",
        maxResults: 1,
        regionCode: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import OrganizationRecommendationResources, OrganizationRecommendationResourcesConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_organization_recommendation_resources")

        ubx.data(OrganizationRecommendationResources, "example", OrganizationRecommendationResourcesConfig(
            organization_recommendation_identifier="example",
            affected_account_id="example",
            exclusion_status="example",
            max_results=1,
            region_code="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="affected_account_id" type="string">
  The real, unique identifier of the affected account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclusion_status" type="string">
  The real, current status of the exclusion this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_recommendation_identifier" type="string" required>
  The real, unique identifier of the organization recommendation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="region_code" type="string">
  The real, unique code identifying the AWS region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_recommendation_resource_summaries" type="list(object)">
  The real, summarized organization recommendation resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
