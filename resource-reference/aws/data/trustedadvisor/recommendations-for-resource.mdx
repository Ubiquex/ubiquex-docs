---
title: "aws_trustedadvisor_recommendations_for_resource"
description: "`aws_trustedadvisor_recommendations_for_resource` reads the real Trusted Advisor recommendations affecting a specific resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_recommendations_for_resource"})

    		ubx.Data(trustedadvisor.RecommendationsForResource, "example", trustedadvisor.RecommendationsForResourceConfig{
    			AwsResourceArn: "arn:aws:iam::123456789012:policy/example",
    			CheckArn:       "arn:aws:iam::123456789012:policy/example",
    			Language:       "example",
    			MaxResults:     1,
    			Pillar:         "example",
    			Status:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecommendationsForResource } from "@ubx/sdk-aws/aws/data/trustedadvisor/recommendations_for_resource";

    export default stack("example", () => {
      intent({
        summary: "look up aws_trustedadvisor_recommendations_for_resource",
      });

      data(RecommendationsForResource, "example", {
        awsResourceArn: "arn:aws:iam::123456789012:policy/example",
        checkArn: "arn:aws:iam::123456789012:policy/example",
        language: "example",
        maxResults: 1,
        pillar: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import RecommendationsForResource, RecommendationsForResourceConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_recommendations_for_resource")

        ubx.data(RecommendationsForResource, "example", RecommendationsForResourceConfig(
            aws_resource_arn="arn:aws:iam::123456789012:policy/example",
            check_arn="arn:aws:iam::123456789012:policy/example",
            language="example",
            max_results=1,
            pillar="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the aws resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="check_arn" type="string">
  The real Amazon Resource Name (ARN) of the check this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="language" type="string">
  The real language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pillar" type="string">
  The real AWS Well-Architected pillar this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_for_resource_summaries" type="list(object)">
  The real, summarized recommendation for resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
