---
title: "aws_trustedadvisor_recommendations"
description: "`aws_trustedadvisor_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_trustedadvisor_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_recommendations"})

    		ubx.Data(trustedadvisor.Recommendations, "example", trustedadvisor.RecommendationsConfig{
    			AfterLastUpdatedAt:  "example",
    			AwsService:          "example",
    			BeforeLastUpdatedAt: "example",
    			CheckIdentifier:     "example",
    			Language:            "example",
    			MaxResults:          1,
    			Pillar:              "example",
    			Source:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Recommendations } from "@ubx/sdk-aws/aws/data/trustedadvisor/recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_trustedadvisor_recommendations" });

      data(Recommendations, "example", {
        afterLastUpdatedAt: "example",
        awsService: "example",
        beforeLastUpdatedAt: "example",
        checkIdentifier: "example",
        language: "example",
        maxResults: 1,
        pillar: "example",
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import Recommendations, RecommendationsConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_recommendations")

        ubx.data(Recommendations, "example", RecommendationsConfig(
            after_last_updated_at="example",
            aws_service="example",
            before_last_updated_at="example",
            check_identifier="example",
            language="example",
            max_results=1,
            pillar="example",
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_last_updated_at" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="aws_service" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before_last_updated_at" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="check_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="language" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="pillar" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
