---
title: "aws_trustedadvisor_organization_recommendation_accounts"
description: "`aws_trustedadvisor_organization_recommendation_accounts` reads the real accounts affected by an organization-wide Trusted Advisor recommendation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_organization_recommendation_accounts"})

    		ubx.Data(trustedadvisor.OrganizationRecommendationAccounts, "example", trustedadvisor.OrganizationRecommendationAccountsConfig{
    			OrganizationRecommendationIdentifier: "example",
    			AffectedAccountId:                    "example",
    			MaxResults:                           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationRecommendationAccounts } from "@ubx/sdk-aws/aws/data/trustedadvisor/organization_recommendation_accounts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_trustedadvisor_organization_recommendation_accounts",
      });

      data(OrganizationRecommendationAccounts, "example", {
        organizationRecommendationIdentifier: "example",
        affectedAccountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import OrganizationRecommendationAccounts, OrganizationRecommendationAccountsConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_organization_recommendation_accounts")

        ubx.data(OrganizationRecommendationAccounts, "example", OrganizationRecommendationAccountsConfig(
            organization_recommendation_identifier="example",
            affected_account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="affected_account_id" type="string">
  The real, unique identifier of the affected account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_recommendation_identifier" type="string" required>
  The real, unique identifier of the organization recommendation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_recommendation_lifecycle_summaries" type="list(object)">
  The real, summarized account recommendation lifecycles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
