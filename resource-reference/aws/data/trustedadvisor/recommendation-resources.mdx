---
title: "aws_trustedadvisor_recommendation_resources"
description: "`aws_trustedadvisor_recommendation_resources` reads the real resources affected by a Trusted Advisor recommendation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	trustedadvisor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/trustedadvisor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_trustedadvisor_recommendation_resources"})

    		ubx.Data(trustedadvisor.RecommendationResources, "example", trustedadvisor.RecommendationResourcesConfig{
    			RecommendationIdentifier: "example",
    			ExclusionStatus:          "example",
    			Language:                 "example",
    			MaxResults:               1,
    			RegionCode:               "example",
    			Status:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecommendationResources } from "@ubx/sdk-aws/aws/data/trustedadvisor/recommendation_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_trustedadvisor_recommendation_resources" });

      data(RecommendationResources, "example", {
        recommendationIdentifier: "example",
        exclusionStatus: "example",
        language: "example",
        maxResults: 1,
        regionCode: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.trustedadvisor import RecommendationResources, RecommendationResourcesConfig

    def describe():
        ubx.intent("look up aws_trustedadvisor_recommendation_resources")

        ubx.data(RecommendationResources, "example", RecommendationResourcesConfig(
            recommendation_identifier="example",
            exclusion_status="example",
            language="example",
            max_results=1,
            region_code="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclusion_status" type="string">
  The real, current status of the exclusion this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="language" type="string">
  The real language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_identifier" type="string" required>
  The real, unique identifier of the recommendation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="region_code" type="string">
  The real, unique code identifying the AWS region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_resource_summaries" type="list(object)">
  The real, summarized recommendation resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
