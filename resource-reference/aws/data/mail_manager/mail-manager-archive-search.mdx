---
title: "aws_mail_manager_archive_search"
description: "`aws_mail_manager_archive_search` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mail_manager_archive_search` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_archive_search"})

    		ubx.Data(mail_manager.MailManagerArchiveSearch, "example", mail_manager.MailManagerArchiveSearchConfig{
    			SearchId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailManagerArchiveSearch } from "@ubx/sdk-aws/aws/data/mail_manager/mail_manager_archive_search";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_archive_search" });

      data(MailManagerArchiveSearch, "example", {
        searchId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerArchiveSearch, MailManagerArchiveSearchConfig

    def describe():
        ubx.intent("look up aws_mail_manager_archive_search")

        ubx.data(MailManagerArchiveSearch, "example", MailManagerArchiveSearchConfig(
            search_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="search_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="archive_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;A set of filter conditions to include and/or exclude emails.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="include" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="unless" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="from_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="object">
  &lt;p&gt;The current status of an archive search job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="completion_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="error_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submission_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="to_timestamp" type="string">
  Returned by the lookup.
</ResponseField>
