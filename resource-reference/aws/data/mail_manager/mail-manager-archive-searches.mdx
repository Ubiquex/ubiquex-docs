---
title: "aws_mail_manager_archive_searches"
description: "`aws_mail_manager_archive_searches` reads the real searches run against a Mail Manager archive."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_archive_searches"})

    		ubx.Data(mail_manager.MailManagerArchiveSearches, "example", mail_manager.MailManagerArchiveSearchesConfig{
    			ArchiveId: "example",
    			PageSize:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MailManagerArchiveSearches } from "./local-sdk/aws/sdk/typescript/aws/data/mail_manager/mail_manager_archive_searches.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_archive_searches" });

      data(MailManagerArchiveSearches, "example", {
        archiveId: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerArchiveSearches, MailManagerArchiveSearchesConfig

    def describe():
        ubx.intent("look up aws_mail_manager_archive_searches")

        ubx.data(MailManagerArchiveSearches, "example", MailManagerArchiveSearchesConfig(
            archive_id="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="archive_id" type="string" required>
  The real, unique identifier of the archive this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="searches" type="list(object)">
  The real searches included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
