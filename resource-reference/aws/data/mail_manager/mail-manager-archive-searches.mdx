---
title: "aws_mail_manager_archive_searches"
description: "`aws_mail_manager_archive_searches` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mail_manager_archive_searches` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_archive_searches"})

    		ubx.Data(mail_manager.MailManagerArchiveSearches, "example", mail_manager.MailManagerArchiveSearchesConfig{
    			ArchiveId: "example",
    			PageSize:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailManagerArchiveSearches } from "@ubx/sdk-aws/aws/data/mail_manager/mail_manager_archive_searches";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_archive_searches" });

      data(MailManagerArchiveSearches, "example", {
        archiveId: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerArchiveSearches, MailManagerArchiveSearchesConfig

    def describe():
        ubx.intent("look up aws_mail_manager_archive_searches")

        ubx.data(MailManagerArchiveSearches, "example", MailManagerArchiveSearchesConfig(
            archive_id="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="archive_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="searches" type="list(object)">
  Returned by the lookup.
</ResponseField>
