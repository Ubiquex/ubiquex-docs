---
title: "aws_mail_manager_member_of_address_list"
description: "`aws_mail_manager_member_of_address_list` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mail_manager_member_of_address_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_member_of_address_list"})

    		ubx.Data(mail_manager.MailManagerMemberOfAddressList, "example", mail_manager.MailManagerMemberOfAddressListConfig{
    			Address:       "example",
    			AddressListId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailManagerMemberOfAddressList } from "@ubx/sdk-aws/aws/data/mail_manager/mail_manager_member_of_address_list";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_member_of_address_list" });

      data(MailManagerMemberOfAddressList, "example", {
        address: "example",
        addressListId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerMemberOfAddressList, MailManagerMemberOfAddressListConfig

    def describe():
        ubx.intent("look up aws_mail_manager_member_of_address_list")

        ubx.data(MailManagerMemberOfAddressList, "example", MailManagerMemberOfAddressListConfig(
            address="example",
            address_list_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="address" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="address_list_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_timestamp" type="string">
  Returned by the lookup.
</ResponseField>
