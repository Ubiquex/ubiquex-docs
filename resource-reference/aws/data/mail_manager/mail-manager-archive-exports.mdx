---
title: "aws_mail_manager_archive_exports"
description: "`aws_mail_manager_archive_exports` reads the real jobs exporting messages from a Mail Manager archive."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_archive_exports"})

    		ubx.Data(mail_manager.MailManagerArchiveExports, "example", mail_manager.MailManagerArchiveExportsConfig{
    			ArchiveId: "example",
    			PageSize:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailManagerArchiveExports } from "@ubx/sdk-aws/aws/data/mail_manager/mail_manager_archive_exports";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_archive_exports" });

      data(MailManagerArchiveExports, "example", {
        archiveId: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerArchiveExports, MailManagerArchiveExportsConfig

    def describe():
        ubx.intent("look up aws_mail_manager_archive_exports")

        ubx.data(MailManagerArchiveExports, "example", MailManagerArchiveExportsConfig(
            archive_id="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="archive_id" type="string" required>
  The real, unique identifier of the archive this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="exports" type="list(object)">
  The real exports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
