---
title: "aws_mail_manager_address_list_import_jobs"
description: "`aws_mail_manager_address_list_import_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mail_manager_address_list_import_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_address_list_import_jobs"})

    		ubx.Data(mail_manager.MailManagerAddressListImportJobs, "example", mail_manager.MailManagerAddressListImportJobsConfig{
    			AddressListId: "example",
    			PageSize:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailManagerAddressListImportJobs } from "@ubx/sdk-aws/aws/data/mail_manager/mail_manager_address_list_import_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_address_list_import_jobs" });

      data(MailManagerAddressListImportJobs, "example", {
        addressListId: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerAddressListImportJobs, MailManagerAddressListImportJobsConfig

    def describe():
        ubx.intent("look up aws_mail_manager_address_list_import_jobs")

        ubx.data(MailManagerAddressListImportJobs, "example", MailManagerAddressListImportJobsConfig(
            address_list_id="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="address_list_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="import_jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
