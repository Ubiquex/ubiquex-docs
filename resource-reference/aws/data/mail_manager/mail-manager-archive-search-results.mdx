---
title: "aws_mail_manager_archive_search_results"
description: "`aws_mail_manager_archive_search_results` reads the real result rows of a search run against a Mail Manager archive."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mail_manager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mail_manager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mail_manager_archive_search_results"})

    		ubx.Data(mail_manager.MailManagerArchiveSearchResults, "example", mail_manager.MailManagerArchiveSearchResultsConfig{
    			SearchId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MailManagerArchiveSearchResults } from "./local-sdk/aws/sdk/typescript/aws/data/mail_manager/mail_manager_archive_search_results.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mail_manager_archive_search_results" });

      data(MailManagerArchiveSearchResults, "example", {
        searchId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mail_manager import MailManagerArchiveSearchResults, MailManagerArchiveSearchResultsConfig

    def describe():
        ubx.intent("look up aws_mail_manager_archive_search_results")

        ubx.data(MailManagerArchiveSearchResults, "example", MailManagerArchiveSearchResultsConfig(
            search_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="search_id" type="string" required>
  The real, unique identifier of the search this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rows" type="list(object)">
  The real rows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
