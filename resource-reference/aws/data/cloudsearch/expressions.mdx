---
title: "aws_cloudsearch_expressions"
description: "`aws_cloudsearch_expressions` reads the real computed expressions configured for a CloudSearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudsearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudsearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudsearch_expressions"})

    		ubx.Data(cloudsearch.Expressions, "example", cloudsearch.ExpressionsConfig{
    			DomainName:      "example-domain-name",
    			Deployed:        true,
    			ExpressionNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Expressions } from "./local-sdk/aws/sdk/typescript/aws/data/cloudsearch/expressions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudsearch_expressions" });

      data(Expressions, "example", {
        domainName: "example-domain-name",
        deployed: true,
        expressionNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudsearch import Expressions, ExpressionsConfig

    def describe():
        ubx.intent("look up aws_cloudsearch_expressions")

        ubx.data(Expressions, "example", ExpressionsConfig(
            domain_name="example-domain-name",
            deployed=True,
            expression_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployed" type="bool">
  Whether this object has been deployed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;A string that represents the name of a domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="expression_names" type="list(string)">
  The real names of the expressions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expressions" type="list(object)">
  &lt;p&gt;Contains the status of multiple expressions.&lt;/p&gt; Returned by the lookup.
</ResponseField>
