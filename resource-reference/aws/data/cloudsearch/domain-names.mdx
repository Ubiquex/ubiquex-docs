---
title: "aws_cloudsearch_domain_names"
description: "`aws_cloudsearch_domain_names` reads the real names of the CloudSearch domains on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudsearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudsearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudsearch_domain_names"})

    		ubx.Data(cloudsearch.DomainNames, "example", cloudsearch.DomainNamesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainNames } from "@ubx/sdk-aws/aws/data/cloudsearch/domain_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudsearch_domain_names" });

      data(DomainNames, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudsearch import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_cloudsearch_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="domain_names" type="map(string)">
  &lt;p&gt;A collection of domain names.&lt;/p&gt; Returned by the lookup.
</ResponseField>
