---
title: "aws_application_cost_profiler_report_definitions"
description: "`aws_application_cost_profiler_report_definitions` reads the real Application Cost Profiler report definitions on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application_cost_profiler "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_cost_profiler"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_cost_profiler_report_definitions"})

    		ubx.Data(application_cost_profiler.ApplicationCostProfilerReportDefinitions, "example", application_cost_profiler.ApplicationCostProfilerReportDefinitionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationCostProfilerReportDefinitions } from "./local-sdk/aws/sdk/typescript/aws/data/application_cost_profiler/application_cost_profiler_report_definitions.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_application_cost_profiler_report_definitions",
      });

      data(ApplicationCostProfilerReportDefinitions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.application_cost_profiler import ApplicationCostProfilerReportDefinitions, ApplicationCostProfilerReportDefinitionsConfig

    def describe():
        ubx.intent("look up aws_application_cost_profiler_report_definitions")

        ubx.data(ApplicationCostProfilerReportDefinitions, "example", ApplicationCostProfilerReportDefinitionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="report_definitions" type="list(object)">
  The real report definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
