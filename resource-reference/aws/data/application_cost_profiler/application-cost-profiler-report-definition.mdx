---
title: "aws_application_cost_profiler_report_definition"
description: "`aws_application_cost_profiler_report_definition` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_application_cost_profiler_report_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_cost_profiler "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_cost_profiler"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_cost_profiler_report_definition"})

    		ubx.Data(application_cost_profiler.ApplicationCostProfilerReportDefinition, "example", application_cost_profiler.ApplicationCostProfilerReportDefinitionConfig{
    			ReportId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationCostProfilerReportDefinition } from "@ubx/sdk-aws/aws/data/application_cost_profiler/application_cost_profiler_report_definition";

    export default stack("example", () => {
      intent({
        summary: "look up aws_application_cost_profiler_report_definition",
      });

      data(ApplicationCostProfilerReportDefinition, "example", {
        reportId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_cost_profiler import ApplicationCostProfilerReportDefinition, ApplicationCostProfilerReportDefinitionConfig

    def describe():
        ubx.intent("look up aws_application_cost_profiler_report_definition")

        ubx.data(ApplicationCostProfilerReportDefinition, "example", ApplicationCostProfilerReportDefinitionConfig(
            report_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="report_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="destination_s3_location" type="object">
  &lt;p&gt;Represents the Amazon Simple Storage Service (Amazon S3) location where AWS Application Cost Profiler reports are generated and then written to.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="prefix" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="format" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="report_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="report_frequency" type="string">
  Returned by the lookup.
</ResponseField>
