---
title: "aws_personalize_runtime_recommendations"
description: "`aws_personalize_runtime_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_personalize_runtime_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize_runtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize_runtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_runtime_recommendations"})

    		ubx.Data(personalize_runtime.PersonalizeRuntimeRecommendations, "example", personalize_runtime.PersonalizeRuntimeRecommendationsConfig{
    			CampaignArn:     "arn:aws:iam::123456789012:policy/example",
    			Context:         map[string]string{"managed-by": "ubx"},
    			FilterArn:       "arn:aws:iam::123456789012:policy/example",
    			FilterValues:    map[string]string{"managed-by": "ubx"},
    			ItemId:          "example",
    			MetadataColumns: map[string]string{"managed-by": "ubx"},
    			NumResults:      1,
    			Promotions:      []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PersonalizeRuntimeRecommendations } from "@ubx/sdk-aws/aws/data/personalize_runtime/personalize_runtime_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_runtime_recommendations" });

      data(PersonalizeRuntimeRecommendations, "example", {
        campaignArn: "arn:aws:iam::123456789012:policy/example",
        context: { "managed-by": "ubx" },
        filterArn: "arn:aws:iam::123456789012:policy/example",
        filterValues: { "managed-by": "ubx" },
        itemId: "example",
        metadataColumns: { "managed-by": "ubx" },
        numResults: 1,
        promotions: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize_runtime import PersonalizeRuntimeRecommendations, PersonalizeRuntimeRecommendationsConfig

    def describe():
        ubx.intent("look up aws_personalize_runtime_recommendations")

        ubx.data(PersonalizeRuntimeRecommendations, "example", PersonalizeRuntimeRecommendationsConfig(
            campaign_arn="arn:aws:iam::123456789012:policy/example",
            context={"managed-by": "ubx"},
            filter_arn="arn:aws:iam::123456789012:policy/example",
            filter_values={"managed-by": "ubx"},
            item_id="example",
            metadata_columns={"managed-by": "ubx"},
            num_results=1,
            promotions=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="context" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter_values" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="item_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metadata_columns" type="map(list(string))">
  Optional lookup argument.
</ResponseField>

<ResponseField name="num_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="promotions" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recommender_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="item_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_id" type="string">
  Returned by the lookup.
</ResponseField>
