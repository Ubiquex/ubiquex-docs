---
title: "aws_personalize_runtime_personalized_ranking"
description: "`aws_personalize_runtime_personalized_ranking` reads a real, live-generated personalized ranking of a given item list from a Personalize campaign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize_runtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize_runtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_runtime_personalized_ranking"})

    		ubx.Data(personalize_runtime.PersonalizeRuntimePersonalizedRanking, "example", personalize_runtime.PersonalizeRuntimePersonalizedRankingConfig{
    			CampaignArn:     "arn:aws:iam::123456789012:policy/example",
    			InputList:       []string{"example"},
    			UserId:          "example",
    			Context:         map[string]string{"managed-by": "ubx"},
    			FilterArn:       "arn:aws:iam::123456789012:policy/example",
    			FilterValues:    map[string]string{"managed-by": "ubx"},
    			MetadataColumns: map[string]any{"key": "value"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PersonalizeRuntimePersonalizedRanking } from "@ubx/sdk-aws/aws/data/personalize_runtime/personalize_runtime_personalized_ranking";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_runtime_personalized_ranking" });

      data(PersonalizeRuntimePersonalizedRanking, "example", {
        campaignArn: "arn:aws:iam::123456789012:policy/example",
        inputList: ["example"],
        userId: "example",
        context: { "managed-by": "ubx" },
        filterArn: "arn:aws:iam::123456789012:policy/example",
        filterValues: { "managed-by": "ubx" },
        metadataColumns: { key: "value" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize_runtime import PersonalizeRuntimePersonalizedRanking, PersonalizeRuntimePersonalizedRankingConfig

    def describe():
        ubx.intent("look up aws_personalize_runtime_personalized_ranking")

        ubx.data(PersonalizeRuntimePersonalizedRanking, "example", PersonalizeRuntimePersonalizedRankingConfig(
            campaign_arn="arn:aws:iam::123456789012:policy/example",
            input_list=["example"],
            user_id="example",
            context={"managed-by": "ubx"},
            filter_arn="arn:aws:iam::123456789012:policy/example",
            filter_values={"managed-by": "ubx"},
            metadata_columns={"key": "value"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the campaign this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="context" type="map(string)">
  The real, structured context of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_values" type="map(string)">
  The real values a filter is matched against. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="input_list" type="list(string)" required>
  The real inputs included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metadata_columns" type="map(list(string))">
  The real metadata columns included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="personalized_ranking" type="list(object)">
  The real, personalized ranking computed for this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_id" type="string">
  The real, unique identifier of the recommendation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
