---
title: "aws_personalize_runtime_action_recommendations"
description: "`aws_personalize_runtime_action_recommendations` reads real, live-generated action recommendations from a Personalize campaign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	personalize_runtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize_runtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_runtime_action_recommendations"})

    		ubx.Data(personalize_runtime.PersonalizeRuntimeActionRecommendations, "example", personalize_runtime.PersonalizeRuntimeActionRecommendationsConfig{
    			CampaignArn:  "arn:aws:iam::123456789012:policy/example",
    			FilterArn:    "arn:aws:iam::123456789012:policy/example",
    			FilterValues: map[string]string{"managed-by": "ubx"},
    			NumResults:   1,
    			UserId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PersonalizeRuntimeActionRecommendations } from "./local-sdk/aws/sdk/typescript/aws/data/personalize_runtime/personalize_runtime_action_recommendations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_runtime_action_recommendations" });

      data(PersonalizeRuntimeActionRecommendations, "example", {
        campaignArn: "arn:aws:iam::123456789012:policy/example",
        filterArn: "arn:aws:iam::123456789012:policy/example",
        filterValues: { "managed-by": "ubx" },
        numResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.personalize_runtime import PersonalizeRuntimeActionRecommendations, PersonalizeRuntimeActionRecommendationsConfig

    def describe():
        ubx.intent("look up aws_personalize_runtime_action_recommendations")

        ubx.data(PersonalizeRuntimeActionRecommendations, "example", PersonalizeRuntimeActionRecommendationsConfig(
            campaign_arn="arn:aws:iam::123456789012:policy/example",
            filter_arn="arn:aws:iam::123456789012:policy/example",
            filter_values={"managed-by": "ubx"},
            num_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_arn" type="string">
  The real Amazon Resource Name (ARN) of the campaign this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_values" type="map(string)">
  The real values a filter is matched against. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="num_results" type="number">
  The real number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_list" type="list(object)">
  The real actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_id" type="string">
  The real, unique identifier of the recommendation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
