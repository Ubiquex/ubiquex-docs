---
title: "aws_budgets_budget_actions_for_budget"
description: "`data_aws_budgets_budget_actions_for_budget` looks up a real, already-existing `aws_budgets_budgets_action`."
---

It attaches a real, automatic response, applying an IAM policy that denies further provisioning, for instance, to an `aws_budgets_budget` crossing its threshold, turning a budget from a passive alert into an enforced spending control.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	budgets "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/budgets"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_budgets_budget_actions_for_budget"})

    		ubx.Data(budgets.BudgetActionsForBudget, "example", budgets.BudgetActionsForBudgetConfig{
    			AccountId:  "example",
    			BudgetName: "example-budget-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BudgetActionsForBudget } from "@ubx/sdk-aws/aws/data/budgets/budget_actions_for_budget";

    export default stack("example", () => {
      intent({ summary: "look up aws_budgets_budget_actions_for_budget" });

      data(BudgetActionsForBudget, "example", {
        accountId: "example",
        budgetName: "example-budget-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.budgets import BudgetActionsForBudget, BudgetActionsForBudgetConfig

    def describe():
        ubx.intent("look up aws_budgets_budget_actions_for_budget")

        ubx.data(BudgetActionsForBudget, "example", BudgetActionsForBudgetConfig(
            account_id="example",
            budget_name="example-budget-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  &lt;p&gt;The account ID of the user. It's a 12-digit number.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="budget_name" type="string" required>
  &lt;p&gt; A string that represents the budget name. The ":" and "\" characters, and the "/action/" substring, aren't allowed.&lt;/p&gt; &lt;p&gt;Budget names are validated for content. Names that contain phone numbers, URLs, or email addresses combined with certain terms may be rejected.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt; An integer that represents how many entries a paginated response contains. The maximum is 100.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions" type="list(object)">
  The real actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt; A generic string.&lt;/p&gt; Returned by the lookup.
</ResponseField>
