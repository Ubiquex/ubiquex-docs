---
title: "aws_budgets_budget_notifications_for_account"
description: "`aws_budgets_budget_notifications_for_account` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_budgets_budget_notifications_for_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	budgets "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/budgets"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_budgets_budget_notifications_for_account"})

    		ubx.Data(budgets.BudgetNotificationsForAccount, "example", budgets.BudgetNotificationsForAccountConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BudgetNotificationsForAccount } from "@ubx/sdk-aws/aws/data/budgets/budget_notifications_for_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_budgets_budget_notifications_for_account" });

      data(BudgetNotificationsForAccount, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.budgets import BudgetNotificationsForAccount, BudgetNotificationsForAccountConfig

    def describe():
        ubx.intent("look up aws_budgets_budget_notifications_for_account")

        ubx.data(BudgetNotificationsForAccount, "example", BudgetNotificationsForAccountConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  &lt;p&gt;The account ID of the user. It's a 12-digit number.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="budget_notifications_for_account" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt; A generic string.&lt;/p&gt; Returned by the lookup.
</ResponseField>
