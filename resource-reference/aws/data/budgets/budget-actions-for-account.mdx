---
title: "aws_budgets_budget_actions_for_account"
description: "`data_aws_budgets_budget_actions_for_account` looks up a real, already-existing `aws_budgets_budgets_action`."
---

It attaches a real, automatic response, applying an IAM policy that denies further provisioning, for instance, to an `aws_budgets_budget` crossing its threshold, turning a budget from a passive alert into an enforced spending control.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	budgets "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/budgets"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_budgets_budget_actions_for_account"})

    		ubx.Data(budgets.BudgetActionsForAccount, "example", budgets.BudgetActionsForAccountConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BudgetActionsForAccount } from "@ubx/sdk-aws/aws/data/budgets/budget_actions_for_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_budgets_budget_actions_for_account" });

      data(BudgetActionsForAccount, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.budgets import BudgetActionsForAccount, BudgetActionsForAccountConfig

    def describe():
        ubx.intent("look up aws_budgets_budget_actions_for_account")

        ubx.data(BudgetActionsForAccount, "example", BudgetActionsForAccountConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  &lt;p&gt;The account ID of the user. It's a 12-digit number.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt; An integer that represents how many entries a paginated response contains. The maximum is 100.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions" type="list(object)">
  The real actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt; A generic string.&lt;/p&gt; Returned by the lookup.
</ResponseField>
