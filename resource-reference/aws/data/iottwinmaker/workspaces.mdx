---
title: "aws_iottwinmaker_workspaces"
description: "`aws_iottwinmaker_workspaces` looks up a real IoT TwinMaker workspace, the top-level container for a digital twin."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_workspaces"})

    		ubx.Data(iottwinmaker.Workspaces, "example", iottwinmaker.WorkspacesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workspaces } from "@ubx/sdk-aws/aws/data/iottwinmaker/workspaces";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_workspaces" });

      data(Workspaces, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import Workspaces, WorkspacesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_workspaces")

        ubx.data(Workspaces, "example", WorkspacesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workspace_summaries" type="list(object)">
  The real, summarized workspaces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
