---
title: "aws_iottwinmaker_component_types"
description: "`aws_iottwinmaker_component_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iottwinmaker_component_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_component_types"})

    		ubx.Data(iottwinmaker.ComponentTypes, "example", iottwinmaker.ComponentTypesConfig{
    			WorkspaceId: "example",
    			Filters:     []map[string]any{{"extends_from": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComponentTypes } from "@ubx/sdk-aws/aws/data/iottwinmaker/component_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_component_types" });

      data(ComponentTypes, "example", {
        workspaceId: "example",
        filters: [{ extendsFrom: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import ComponentTypes, ComponentTypesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_component_types")

        ubx.data(ComponentTypes, "example", ComponentTypesConfig(
            workspace_id="example",
            filters=[{"extends_from": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_type_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
