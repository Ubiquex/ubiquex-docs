---
title: "aws_iottwinmaker_component_types"
description: "`aws_iottwinmaker_component_types` looks up a real IoT TwinMaker component type, defining the properties and behavior a component of that type shares."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_component_types"})

    		ubx.Data(iottwinmaker.ComponentTypes, "example", iottwinmaker.ComponentTypesConfig{
    			WorkspaceId: "example",
    			Filters:     []map[string]any{{"extends_from": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComponentTypes } from "./local-sdk/aws/sdk/typescript/aws/data/iottwinmaker/component_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_component_types" });

      data(ComponentTypes, "example", {
        workspaceId: "example",
        filters: [{ extendsFrom: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import ComponentTypes, ComponentTypesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_component_types")

        ubx.data(ComponentTypes, "example", ComponentTypesConfig(
            workspace_id="example",
            filters=[{"extends_from": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_type_summaries" type="list(object)">
  The real, summarized component types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
