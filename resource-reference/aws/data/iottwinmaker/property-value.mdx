---
title: "aws_iottwinmaker_property_value"
description: "`aws_iottwinmaker_property_value` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iottwinmaker_property_value` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_property_value"})

    		ubx.Data(iottwinmaker.PropertyValue, "example", iottwinmaker.PropertyValueConfig{
    			SelectedProperties: []string{"example"},
    			WorkspaceId:        "example",
    			ComponentName:      "example-component-name",
    			ComponentPath:      "example",
    			ComponentTypeId:    "example",
    			EntityId:           "example",
    			MaxResults:         1,
    			PropertyGroupName:  "example-property-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PropertyValue } from "@ubx/sdk-aws/aws/data/iottwinmaker/property_value";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_property_value" });

      data(PropertyValue, "example", {
        selectedProperties: ["example"],
        workspaceId: "example",
        componentName: "example-component-name",
        componentPath: "example",
        componentTypeId: "example",
        entityId: "example",
        maxResults: 1,
        propertyGroupName: "example-property-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import PropertyValue, PropertyValueConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_property_value")

        ubx.data(PropertyValue, "example", PropertyValueConfig(
            selected_properties=["example"],
            workspace_id="example",
            component_name="example-component-name",
            component_path="example",
            component_type_id="example",
            entity_id="example",
            max_results=1,
            property_group_name="example-property-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="component_path" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="component_type_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="entity_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="property_group_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="selected_properties" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="tabular_conditions" type="object">
  &lt;p&gt;The tabular conditions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="order_by" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="property_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="property_values" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tabular_property_values" type="list(list(map(object)))">
  Returned by the lookup.
</ResponseField>
