---
title: "aws_iottwinmaker_property_value"
description: "`aws_iottwinmaker_property_value` reads the real, current value of an IoT TwinMaker entity property."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_property_value"})

    		ubx.Data(iottwinmaker.PropertyValue, "example", iottwinmaker.PropertyValueConfig{
    			SelectedProperties: []string{"example"},
    			WorkspaceId:        "example",
    			ComponentName:      "example-component-name",
    			ComponentPath:      "example",
    			ComponentTypeId:    "example",
    			EntityId:           "example",
    			MaxResults:         1,
    			PropertyGroupName:  "example-property-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PropertyValue } from "./local-sdk/aws/sdk/typescript/aws/data/iottwinmaker/property_value.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_property_value" });

      data(PropertyValue, "example", {
        selectedProperties: ["example"],
        workspaceId: "example",
        componentName: "example-component-name",
        componentPath: "example",
        componentTypeId: "example",
        entityId: "example",
        maxResults: 1,
        propertyGroupName: "example-property-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import PropertyValue, PropertyValueConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_property_value")

        ubx.data(PropertyValue, "example", PropertyValueConfig(
            selected_properties=["example"],
            workspace_id="example",
            component_name="example-component-name",
            component_path="example",
            component_type_id="example",
            entity_id="example",
            max_results=1,
            property_group_name="example-property-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string">
  The real name of the component this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="component_path" type="string">
  The real path of the component. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="component_type_id" type="string">
  The real, unique identifier of the component type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The real, unique identifier of this entity. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="property_group_name" type="string">
  The real name of the property group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="selected_properties" type="list(string)" required>
  The real properties selected for this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tabular_conditions" type="object">
  &lt;p&gt;The tabular conditions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="order_by" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="property_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="property_values" type="map(object)">
  The real property values included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tabular_property_values" type="list(list(map(object)))">
  The real, tabular property values included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
