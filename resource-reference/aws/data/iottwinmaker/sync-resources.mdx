---
title: "aws_iottwinmaker_sync_resources"
description: "`aws_iottwinmaker_sync_resources` reads the real resources synchronized by an IoT TwinMaker sync job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_sync_resources"})

    		ubx.Data(iottwinmaker.SyncResources, "example", iottwinmaker.SyncResourcesConfig{
    			SyncSource:  "example",
    			WorkspaceId: "example",
    			Filters:     []map[string]any{{"external_id": "example"}},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SyncResources } from "./local-sdk/aws/sdk/typescript/aws/data/iottwinmaker/sync_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_sync_resources" });

      data(SyncResources, "example", {
        syncSource: "example",
        workspaceId: "example",
        filters: [{ externalId: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import SyncResources, SyncResourcesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_sync_resources")

        ubx.data(SyncResources, "example", SyncResourcesConfig(
            sync_source="example",
            workspace_id="example",
            filters=[{"external_id": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sync_source" type="string" required>
  The real source this object synchronizes from. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_resources" type="list(object)">
  The real resources synchronized by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
