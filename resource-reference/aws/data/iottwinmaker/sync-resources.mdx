---
title: "aws_iottwinmaker_sync_resources"
description: "`aws_iottwinmaker_sync_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iottwinmaker_sync_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_sync_resources"})

    		ubx.Data(iottwinmaker.SyncResources, "example", iottwinmaker.SyncResourcesConfig{
    			SyncSource:  "example",
    			WorkspaceId: "example",
    			Filters:     []map[string]any{{"external_id": "example"}},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncResources } from "@ubx/sdk-aws/aws/data/iottwinmaker/sync_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_sync_resources" });

      data(SyncResources, "example", {
        syncSource: "example",
        workspaceId: "example",
        filters: [{ externalId: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import SyncResources, SyncResourcesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_sync_resources")

        ubx.data(SyncResources, "example", SyncResourcesConfig(
            sync_source="example",
            workspace_id="example",
            filters=[{"external_id": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sync_source" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sync_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
