---
title: "aws_iottwinmaker_sync_jobs"
description: "`aws_iottwinmaker_sync_jobs` looks up a real IoT TwinMaker sync job, continuously synchronizing data from an external source into a workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_sync_jobs"})

    		ubx.Data(iottwinmaker.SyncJobs, "example", iottwinmaker.SyncJobsConfig{
    			WorkspaceId: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncJobs } from "@ubx/sdk-aws/aws/data/iottwinmaker/sync_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_sync_jobs" });

      data(SyncJobs, "example", {
        workspaceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import SyncJobs, SyncJobsConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_sync_jobs")

        ubx.data(SyncJobs, "example", SyncJobsConfig(
            workspace_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_job_summaries" type="list(object)">
  The real, summarized sync jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
