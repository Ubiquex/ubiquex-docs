---
title: "aws_iottwinmaker_metadata_transfer_jobs"
description: "`aws_iottwinmaker_metadata_transfer_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iottwinmaker_metadata_transfer_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_metadata_transfer_jobs"})

    		ubx.Data(iottwinmaker.MetadataTransferJobs, "example", iottwinmaker.MetadataTransferJobsConfig{
    			DestinationType: "example",
    			SourceType:      "example",
    			Filters:         []map[string]any{{"state": "example"}},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetadataTransferJobs } from "@ubx/sdk-aws/aws/data/iottwinmaker/metadata_transfer_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_metadata_transfer_jobs" });

      data(MetadataTransferJobs, "example", {
        destinationType: "example",
        sourceType: "example",
        filters: [{ state: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import MetadataTransferJobs, MetadataTransferJobsConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_metadata_transfer_jobs")

        ubx.data(MetadataTransferJobs, "example", MetadataTransferJobsConfig(
            destination_type="example",
            source_type="example",
            filters=[{"state": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="destination_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metadata_transfer_job_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
