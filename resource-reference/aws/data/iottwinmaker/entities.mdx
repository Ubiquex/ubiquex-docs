---
title: "aws_iottwinmaker_entities"
description: "`aws_iottwinmaker_entities` looks up a real IoT TwinMaker entity, representing one physical or logical object in a digital twin workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iottwinmaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iottwinmaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iottwinmaker_entities"})

    		ubx.Data(iottwinmaker.Entities, "example", iottwinmaker.EntitiesConfig{
    			WorkspaceId: "example",
    			Filters:     []map[string]any{{"component_type_id": "example"}},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Entities } from "@ubx/sdk-aws/aws/data/iottwinmaker/entities";

    export default stack("example", () => {
      intent({ summary: "look up aws_iottwinmaker_entities" });

      data(Entities, "example", {
        workspaceId: "example",
        filters: [{ componentTypeId: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iottwinmaker import Entities, EntitiesConfig

    def describe():
        ubx.intent("look up aws_iottwinmaker_entities")

        ubx.data(Entities, "example", EntitiesConfig(
            workspace_id="example",
            filters=[{"component_type_id": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_summaries" type="list(object)">
  The real, summarized entitys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
