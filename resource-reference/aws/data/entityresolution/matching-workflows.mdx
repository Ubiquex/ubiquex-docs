---
title: "aws_entityresolution_matching_workflows"
description: "`data_aws_entityresolution_matching_workflows` looks up a real, already-existing `aws_entity_resolution_matching_workflow`."
---

It is the real, actual sequence AWS Entity Resolution runs to match records from an `aws_entity_resolution_schema_mapping`-defined input, using rule-based, machine-learning, or data-service-provider matching, and write consolidated, linked output to S3.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_matching_workflows"})

    		ubx.Data(entityresolution.MatchingWorkflows, "example", entityresolution.MatchingWorkflowsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MatchingWorkflows } from "./local-sdk/aws/sdk/typescript/aws/data/entityresolution/matching_workflows.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_matching_workflows" });

      data(MatchingWorkflows, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import MatchingWorkflows, MatchingWorkflowsConfig

    def describe():
        ubx.intent("look up aws_entityresolution_matching_workflows")

        ubx.data(MatchingWorkflows, "example", MatchingWorkflowsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workflow_summaries" type="list(object)">
  The real, summarized workflows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
