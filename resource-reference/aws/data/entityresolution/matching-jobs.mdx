---
title: "aws_entityresolution_matching_jobs"
description: "`aws_entityresolution_matching_jobs` reads the real matching jobs run for an Entity Resolution workflow."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_matching_jobs"})

    		ubx.Data(entityresolution.MatchingJobs, "example", entityresolution.MatchingJobsConfig{
    			WorkflowName: "example-workflow-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MatchingJobs } from "@ubx/sdk-aws/aws/data/entityresolution/matching_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_matching_jobs" });

      data(MatchingJobs, "example", {
        workflowName: "example-workflow-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import MatchingJobs, MatchingJobsConfig

    def describe():
        ubx.intent("look up aws_entityresolution_matching_jobs")

        ubx.data(MatchingJobs, "example", MatchingJobsConfig(
            workflow_name="example-workflow-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
