---
title: "aws_entityresolution_matching_job"
description: "`aws_entityresolution_matching_job` looks up one real Entity Resolution matching job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_matching_job"})

    		ubx.Data(entityresolution.MatchingJob, "example", entityresolution.MatchingJobConfig{
    			JobId:        "example",
    			WorkflowName: "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MatchingJob } from "@ubx/sdk-aws/aws/data/entityresolution/matching_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_matching_job" });

      data(MatchingJob, "example", {
        jobId: "example",
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import MatchingJob, MatchingJobConfig

    def describe():
        ubx.intent("look up aws_entityresolution_matching_job")

        ubx.data(MatchingJob, "example", MatchingJobConfig(
            job_id="example",
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  The real name of the workflow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="object">
  &lt;p&gt;An object containing an error message, if there was an error.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error_message" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metrics" type="object">
  &lt;p&gt;An object containing &lt;code&gt;inputRecords&lt;/code&gt;, &lt;code&gt;totalRecordsProcessed&lt;/code&gt;, &lt;code&gt;matchIDs&lt;/code&gt;, and &lt;code&gt;recordsNotProcessed&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="delete_records_processed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="input_records" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="match_ids" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="records_not_processed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_records_processed" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="output_source_config" type="list(object)">
  The real configuration of the output source. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
