---
title: "aws_entityresolution_provider_service"
description: "`aws_entityresolution_provider_service` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_entityresolution_provider_service` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_provider_service"})

    		ubx.Data(entityresolution.ProviderService, "example", entityresolution.ProviderServiceConfig{
    			ProviderName:        "example-provider-name",
    			ProviderServiceName: "example-provider-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProviderService } from "@ubx/sdk-aws/aws/data/entityresolution/provider_service";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_provider_service" });

      data(ProviderService, "example", {
        providerName: "example-provider-name",
        providerServiceName: "example-provider-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import ProviderService, ProviderServiceConfig

    def describe():
        ubx.intent("look up aws_entityresolution_provider_service")

        ubx.data(ProviderService, "example", ProviderServiceConfig(
            provider_name="example-provider-name",
            provider_service_name="example-provider-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="provider_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider_service_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="anonymized_output" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_component_schema" type="object">
  &lt;p&gt;The input schema supported by provider service.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="provider_schema_attributes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="schemas" type="list(list(string))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="provider_configuration_definition" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_endpoint_configuration" type="object">
  &lt;p&gt;The required configuration fields to use with the provider service.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="marketplace_configuration" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="asset_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="data_set_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="listing_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="revision_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="provider_entity_output_definition" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_id_name_space_configuration" type="object">
  &lt;p&gt;The provider configuration required for different ID namespace types.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider_source_configuration_definition" type="any">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider_target_configuration_definition" type="any">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="provider_intermediate_data_access_configuration" type="object">
  &lt;p&gt;The required configuration fields to give intermediate access to a provider service.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="aws_account_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="required_bucket_actions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="provider_job_configuration" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_service_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_service_display_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provider_service_type" type="string">
  Returned by the lookup.
</ResponseField>
