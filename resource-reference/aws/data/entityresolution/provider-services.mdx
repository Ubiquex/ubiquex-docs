---
title: "aws_entityresolution_provider_services"
description: "`aws_entityresolution_provider_services` reads the real, third-party matching provider services available in Entity Resolution."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_provider_services"})

    		ubx.Data(entityresolution.ProviderServices, "example", entityresolution.ProviderServicesConfig{
    			MaxResults:   1,
    			ProviderName: "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProviderServices } from "@ubx/sdk-aws/aws/data/entityresolution/provider_services";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_provider_services" });

      data(ProviderServices, "example", {
        maxResults: 1,
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import ProviderServices, ProviderServicesConfig

    def describe():
        ubx.intent("look up aws_entityresolution_provider_services")

        ubx.data(ProviderServices, "example", ProviderServicesConfig(
            max_results=1,
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string">
  The real name of the provider this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provider_service_summaries" type="list(object)">
  The real, summarized provider services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
