---
title: "aws_entityresolution_match_id"
description: "`aws_entityresolution_match_id` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_entityresolution_match_id` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entityresolution "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entityresolution"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entityresolution_match_id"})

    		ubx.Data(entityresolution.MatchId, "example", entityresolution.MatchIdConfig{
    			Record:             map[string]string{"managed-by": "ubx"},
    			WorkflowName:       "example-workflow-name",
    			ApplyNormalization: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MatchId } from "@ubx/sdk-aws/aws/data/entityresolution/match_id";

    export default stack("example", () => {
      intent({ summary: "look up aws_entityresolution_match_id" });

      data(MatchId, "example", {
        record: { "managed-by": "ubx" },
        workflowName: "example-workflow-name",
        applyNormalization: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entityresolution import MatchId, MatchIdConfig

    def describe():
        ubx.intent("look up aws_entityresolution_match_id")

        ubx.data(MatchId, "example", MatchIdConfig(
            record={"managed-by": "ubx"},
            workflow_name="example-workflow-name",
            apply_normalization=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="apply_normalization" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="record" type="map(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workflow_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="match_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="match_rule" type="string">
  Returned by the lookup.
</ResponseField>
