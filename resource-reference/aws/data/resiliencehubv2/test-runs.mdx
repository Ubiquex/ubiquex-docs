---
title: "aws_resiliencehubv2_test_runs"
description: "`aws_resiliencehubv2_test_runs` reads the real runs of a Resilience Hub v2 resilience test."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_test_runs"})

    		ubx.Data(resiliencehubv2.TestRuns, "example", resiliencehubv2.TestRunsConfig{
    			ServiceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    			TestId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestRuns } from "@ubx/sdk-aws/aws/data/resiliencehubv2/test_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_test_runs" });

      data(TestRuns, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        testId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import TestRuns, TestRunsConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_test_runs")

        ubx.data(TestRuns, "example", TestRunsConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            test_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="test_id" type="string">
  &lt;p&gt;The unique identifier of a test.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="test_runs" type="list(object)">
  The real Device Farm test runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
