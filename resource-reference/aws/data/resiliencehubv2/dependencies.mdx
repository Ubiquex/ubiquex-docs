---
title: "aws_resiliencehubv2_dependencies"
description: "`aws_resiliencehubv2_dependencies` reads the real, discovered dependencies between resources within a Resilience Hub v2 application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_dependencies"})

    		ubx.Data(resiliencehubv2.Dependencies, "example", resiliencehubv2.DependenciesConfig{
    			MaxResults:            1,
    			QueryRangeEndTime:     "example",
    			QueryRangeGranularity: "example",
    			QueryRangeStartTime:   "example",
    			ServiceArn:            "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Dependencies } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehubv2/dependencies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_dependencies" });

      data(Dependencies, "example", {
        maxResults: 1,
        queryRangeEndTime: "example",
        queryRangeGranularity: "example",
        queryRangeStartTime: "example",
        serviceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import Dependencies, DependenciesConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_dependencies")

        ubx.data(Dependencies, "example", DependenciesConfig(
            max_results=1,
            query_range_end_time="example",
            query_range_granularity="example",
            query_range_start_time="example",
            service_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="query_range_end_time" type="string">
  The real timestamp of the query range end. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_range_granularity" type="string">
  The real time granularity this query's own range is computed at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_range_start_time" type="string">
  The real timestamp of the query range start. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string">
  &lt;p&gt;ARN identifier.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dependency_summaries" type="list(object)">
  The real, summarized dependencys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
