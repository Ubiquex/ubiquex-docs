---
title: "aws_resiliencehubv2_test_run_sources"
description: "`aws_resiliencehubv2_test_run_sources` reads the real sources (such as a FIS experiment) backing a Resilience Hub v2 resilience test run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_test_run_sources"})

    		ubx.Data(resiliencehubv2.TestRunSources, "example", resiliencehubv2.TestRunSourcesConfig{
    			ServiceArn: "arn:aws:iam::123456789012:policy/example",
    			TestRunId:  "example",
    			MaxResults: 1,
    			Type:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestRunSources } from "@ubx/sdk-aws/aws/data/resiliencehubv2/test_run_sources";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_test_run_sources" });

      data(TestRunSources, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        testRunId: "example",
        maxResults: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import TestRunSources, TestRunSourcesConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_test_run_sources")

        ubx.data(TestRunSources, "example", TestRunSourcesConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            test_run_id="example",
            max_results=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="test_run_id" type="string" required>
  &lt;p&gt;The unique identifier of a test run.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  &lt;p&gt;The type of a test run monitoring-source snapshot.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="test_run_sources" type="list(object)">
  The real, source configurations for this test run. **(AI-inferred)** Returned by the lookup.
</ResponseField>
