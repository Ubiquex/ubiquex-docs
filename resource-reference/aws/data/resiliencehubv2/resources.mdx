---
title: "aws_resiliencehubv2_resources"
description: "`aws_resiliencehubv2_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehubv2_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_resources"})

    		ubx.Data(resiliencehubv2.Resources, "example", resiliencehubv2.ResourcesConfig{
    			ServiceArn:    "arn:aws:iam::123456789012:policy/example",
    			AwsRegion:     "example",
    			Billable:      true,
    			MaxResults:    1,
    			ResourceTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resources } from "@ubx/sdk-aws/aws/data/resiliencehubv2/resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_resources" });

      data(Resources, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        awsRegion: "example",
        billable: true,
        maxResults: 1,
        resourceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import Resources, ResourcesConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_resources")

        ubx.data(Resources, "example", ResourcesConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            aws_region="example",
            billable=True,
            max_results=1,
            resource_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_region" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="billable" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="service_function_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
