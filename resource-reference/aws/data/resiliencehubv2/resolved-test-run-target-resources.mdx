---
title: "aws_resiliencehubv2_resolved_test_run_target_resources"
description: "`aws_resiliencehubv2_resolved_test_run_target_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehubv2_resolved_test_run_target_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_resolved_test_run_target_resources"})

    		ubx.Data(resiliencehubv2.ResolvedTestRunTargetResources, "example", resiliencehubv2.ResolvedTestRunTargetResourcesConfig{
    			ServiceArn: "arn:aws:iam::123456789012:policy/example",
    			TestRunId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolvedTestRunTargetResources } from "@ubx/sdk-aws/aws/data/resiliencehubv2/resolved_test_run_target_resources";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehubv2_resolved_test_run_target_resources",
      });

      data(ResolvedTestRunTargetResources, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        testRunId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import ResolvedTestRunTargetResources, ResolvedTestRunTargetResourcesConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_resolved_test_run_target_resources")

        ubx.data(ResolvedTestRunTargetResources, "example", ResolvedTestRunTargetResourcesConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            test_run_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="test_run_id" type="string" required>
  &lt;p&gt;The unique identifier of a test run.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="resolved_target_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
