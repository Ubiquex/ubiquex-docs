---
title: "aws_resiliencehubv2_test_sources"
description: "`aws_resiliencehubv2_test_sources` reads the real, available sources a Resilience Hub v2 resilience test can be built from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_test_sources"})

    		ubx.Data(resiliencehubv2.TestSources, "example", resiliencehubv2.TestSourcesConfig{
    			ServiceArn: "arn:aws:iam::123456789012:policy/example",
    			TestId:     "example",
    			MaxResults: 1,
    			Type:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestSources } from "@ubx/sdk-aws/aws/data/resiliencehubv2/test_sources";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_test_sources" });

      data(TestSources, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        testId: "example",
        maxResults: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import TestSources, TestSourcesConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_test_sources")

        ubx.data(TestSources, "example", TestSourcesConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            test_id="example",
            max_results=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="test_id" type="string" required>
  &lt;p&gt;The unique identifier of a test.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  &lt;p&gt;The purpose of a test monitoring source.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="test_sources" type="list(object)">
  The real sources for this test. **(AI-inferred)** Returned by the lookup.
</ResponseField>
