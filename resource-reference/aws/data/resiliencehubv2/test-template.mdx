---
title: "aws_resiliencehubv2_test_template"
description: "`aws_resiliencehubv2_test_template` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehubv2_test_template` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_test_template"})

    		ubx.Data(resiliencehubv2.TestTemplate, "example", resiliencehubv2.TestTemplateConfig{
    			TestTemplateArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestTemplate } from "@ubx/sdk-aws/aws/data/resiliencehubv2/test_template";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_test_template" });

      data(TestTemplate, "example", {
        testTemplateArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import TestTemplate, TestTemplateConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_test_template")

        ubx.data(TestTemplate, "example", TestTemplateConfig(
            test_template_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="test_template_arn" type="string" required>
  &lt;p&gt;An ARN owned by the service. Accepts either a standard 12-digit account ID or the literal "aws" for AWS-managed resources, such as AWS-managed test templates.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="test_template" type="object">
  &lt;p&gt;A pre-configured, AWS recommended test that defines which resilience capability to validate, the fault actions it runs, and the parameters it accepts.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="test_template_arn" type="string" required>
      &lt;p&gt;An ARN owned by the service. Accepts either a standard 12-digit account ID or the literal "aws" for AWS-managed resources, such as AWS-managed test templates.&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
