---
title: "aws_resiliencehubv2_failure_mode_assessments"
description: "`aws_resiliencehubv2_failure_mode_assessments` reads the real failure mode assessments generated for a Resilience Hub v2 application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehubv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehubv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehubv2_failure_mode_assessments"})

    		ubx.Data(resiliencehubv2.FailureModeAssessments, "example", resiliencehubv2.FailureModeAssessmentsConfig{
    			ServiceArn:         "arn:aws:iam::123456789012:policy/example",
    			AssessmentStatuses: []string{"example"},
    			EndedBefore:        "example",
    			MaxResults:         1,
    			SortBy:             "example",
    			SortOrder:          "example",
    			StartedAfter:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FailureModeAssessments } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehubv2/failure_mode_assessments.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehubv2_failure_mode_assessments" });

      data(FailureModeAssessments, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        assessmentStatuses: ["example"],
        endedBefore: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        startedAfter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehubv2 import FailureModeAssessments, FailureModeAssessmentsConfig

    def describe():
        ubx.intent("look up aws_resiliencehubv2_failure_mode_assessments")

        ubx.data(FailureModeAssessments, "example", FailureModeAssessmentsConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            assessment_statuses=["example"],
            ended_before="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
            started_after="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_statuses" type="list(string)">
  The real, current statuses of the assessments included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ended_before" type="string">
  Restricts results to objects that ended before this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Pagination page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  &lt;p&gt;ARN identifier.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  &lt;p&gt;The field by which to sort failure mode assessment results.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  &lt;p&gt;The order in which to sort results.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="started_after" type="string">
  Restricts results to objects that started after this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_summaries" type="list(object)">
  The real, summarized assessments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
