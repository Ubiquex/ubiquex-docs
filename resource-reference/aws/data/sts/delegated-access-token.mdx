---
title: "aws_sts_delegated_access_token"
description: "`aws_sts_delegated_access_token` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sts_delegated_access_token` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sts_delegated_access_token"})

    		ubx.Data(sts.DelegatedAccessToken, "example", sts.DelegatedAccessTokenConfig{
    			TradeInToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DelegatedAccessToken } from "@ubx/sdk-aws/aws/data/sts/delegated_access_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_sts_delegated_access_token" });

      data(DelegatedAccessToken, "example", {
        tradeInToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sts import DelegatedAccessToken, DelegatedAccessTokenConfig

    def describe():
        ubx.intent("look up aws_sts_delegated_access_token")

        ubx.data(DelegatedAccessToken, "example", DelegatedAccessTokenConfig(
            trade_in_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="trade_in_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assumed_principal" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="credentials" type="object">
  &lt;p&gt;Amazon Web Services credentials for API authentication.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_key_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="expiration" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="secret_access_key" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="session_token" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="packed_policy_size" type="number">
  Returned by the lookup.
</ResponseField>
