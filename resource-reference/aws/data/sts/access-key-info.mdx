---
title: "aws_sts_access_key_info"
description: "`aws_sts_access_key_info` reads real information about the AWS account that issued a given access key ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sts_access_key_info"})

    		ubx.Data(sts.AccessKeyInfo, "example", sts.AccessKeyInfoConfig{
    			AccessKeyId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccessKeyInfo } from "./local-sdk/aws/sdk/typescript/aws/data/sts/access_key_info.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sts_access_key_info" });

      data(AccessKeyInfo, "example", {
        accessKeyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sts import AccessKeyInfo, AccessKeyInfoConfig

    def describe():
        ubx.intent("look up aws_sts_access_key_info")

        ubx.data(AccessKeyInfo, "example", AccessKeyInfoConfig(
            access_key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_key_id" type="string" required>
  The real AWS access key ID this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account" type="string">
  The real AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
