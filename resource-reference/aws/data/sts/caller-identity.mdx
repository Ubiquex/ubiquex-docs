---
title: "aws_sts_caller_identity"
description: "`aws_sts_caller_identity` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sts_caller_identity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sts_caller_identity"})

    		ubx.Data(sts.CallerIdentity, "example", sts.CallerIdentityConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CallerIdentity } from "@ubx/sdk-aws/aws/data/sts/caller_identity";

    export default stack("example", () => {
      intent({ summary: "look up aws_sts_caller_identity" });

      data(CallerIdentity, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sts import CallerIdentity, CallerIdentityConfig

    def describe():
        ubx.intent("look up aws_sts_caller_identity")

        ubx.data(CallerIdentity, "example", CallerIdentityConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_id" type="string">
  Returned by the lookup.
</ResponseField>
