---
title: "aws_sts_web_identity_token"
description: "`aws_sts_web_identity_token` reads real, temporary security credentials issued by STS in exchange for a web identity token (Google, Facebook, or OIDC)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sts_web_identity_token"})

    		ubx.Data(sts.WebIdentityToken, "example", sts.WebIdentityTokenConfig{
    			Audience:         []string{"example"},
    			SigningAlgorithm: "example",
    			DurationSeconds:  7200,
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WebIdentityToken } from "@ubx/sdk-aws/aws/data/sts/web_identity_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_sts_web_identity_token" });

      data(WebIdentityToken, "example", {
        audience: ["example"],
        signingAlgorithm: "example",
        durationSeconds: 7200,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sts import WebIdentityToken, WebIdentityTokenConfig

    def describe():
        ubx.intent("look up aws_sts_web_identity_token")

        ubx.data(WebIdentityToken, "example", WebIdentityTokenConfig(
            audience=["example"],
            signing_algorithm="example",
            duration_seconds=7200,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audience" type="list(string)" required>
  The real intended audience of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="signing_algorithm" type="string" required>
  The real algorithm used to sign this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expiration" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="web_identity_token" type="string">
  The real, opaque web identity token. **(AI-inferred)** Returned by the lookup.
</ResponseField>
