---
title: "aws_mgn_import_file_enrichments"
description: "`aws_mgn_import_file_enrichments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mgn_import_file_enrichments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mgn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgn_import_file_enrichments"})

    		ubx.Data(mgn.ImportFileEnrichments, "example", mgn.ImportFileEnrichmentsConfig{
    			Filters:    map[string]any{"job_ids": []string{"example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImportFileEnrichments } from "@ubx/sdk-aws/aws/data/mgn/import_file_enrichments";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgn_import_file_enrichments" });

      data(ImportFileEnrichments, "example", {
        filters: { jobIds: ["example"] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mgn import ImportFileEnrichments, ImportFileEnrichmentsConfig

    def describe():
        ubx.intent("look up aws_mgn_import_file_enrichments")

        ubx.data(ImportFileEnrichments, "example", ImportFileEnrichmentsConfig(
            filters={"job_ids": ["example"]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Filters for listing import file enrichment jobs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="job_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
