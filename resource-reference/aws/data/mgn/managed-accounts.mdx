---
title: "aws_mgn_managed_accounts"
description: "`aws_mgn_managed_accounts` reads the real member accounts managed through Application Migration Service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mgn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgn_managed_accounts"})

    		ubx.Data(mgn.ManagedAccounts, "example", mgn.ManagedAccountsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedAccounts } from "@ubx/sdk-aws/aws/data/mgn/managed_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgn_managed_accounts" });

      data(ManagedAccounts, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mgn import ManagedAccounts, ManagedAccountsConfig

    def describe():
        ubx.intent("look up aws_mgn_managed_accounts")

        ubx.data(ManagedAccounts, "example", ManagedAccountsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
