---
title: "aws_machinelearning_data_sources"
description: "`aws_machinelearning_data_sources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_machinelearning_data_sources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearning "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/machinelearning"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_machinelearning_data_sources"})

    		ubx.Data(machinelearning.DataSources, "example", machinelearning.DataSourcesConfig{
    			Eq:             "example",
    			FilterVariable: "example",
    			Ge:             "example",
    			Gt:             "example",
    			Le:             "example",
    			Limit:          1,
    			Lt:             "example",
    			Ne:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSources } from "@ubx/sdk-aws/aws/data/machinelearning/data_sources";

    export default stack("example", () => {
      intent({ summary: "look up aws_machinelearning_data_sources" });

      data(DataSources, "example", {
        eq: "example",
        filterVariable: "example",
        ge: "example",
        gt: "example",
        le: "example",
        limit: 1,
        lt: "example",
        ne: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.machinelearning import DataSources, DataSourcesConfig

    def describe():
        ubx.intent("look up aws_machinelearning_data_sources")

        ubx.data(DataSources, "example", DataSourcesConfig(
            eq="example",
            filter_variable="example",
            ge="example",
            gt="example",
            le="example",
            limit=1,
            lt="example",
            ne="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="eq" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="filter_variable" type="string">
  &lt;p&gt;A list of the variables to use in searching or filtering &lt;code&gt;DataSource&lt;/code&gt;.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CreatedAt&lt;/code&gt; - Sets the search criteria to &lt;code&gt;DataSource&lt;/code&gt; creation date.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Status&lt;/code&gt; - Sets the search criteria to &lt;code&gt;DataSource&lt;/code&gt; status.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Name&lt;/code&gt; - Sets the search criteria to the contents of &lt;code&gt;DataSource&lt;/code&gt; &lt;code&gt;Name&lt;/code&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;DataUri&lt;/code&gt; - Sets the search criteria to the URI of data files used to create the &lt;code&gt;DataSource&lt;/code&gt;. The URI can identify either a file or an Amazon Simple Storage Service (Amazon S3) bucket or directory.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;IAMUser&lt;/code&gt; - Sets the search criteria to the user account that invoked the &lt;code&gt;DataSource&lt;/code&gt; creation.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; &lt;p&gt; &lt;b&gt;Note:&lt;/b&gt; The variable names should match the variable names in the &lt;code&gt;DataSource&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="ge" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="gt" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="le" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="lt" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="ne" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="prefix" type="string">
  &lt;p&gt;The value specified in a filtering condition. The &lt;code&gt;ComparatorValue&lt;/code&gt; becomes the reference value when matching or evaluating data values in filtering and searching functions.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  &lt;p&gt;The sort order specified in a listing condition. Possible values include the following:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;asc&lt;/code&gt; - Present the information in ascending order (from A-Z).&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;dsc&lt;/code&gt; - Present the information in descending order (from Z-A).&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;String type.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  Returned by the lookup.
</ResponseField>
