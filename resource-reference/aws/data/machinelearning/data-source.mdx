---
title: "aws_machinelearning_data_source"
description: "`aws_machinelearning_data_source` looks up one real Amazon Machine Learning data source."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	machinelearning "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/machinelearning"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_machinelearning_data_source"})

    		ubx.Data(machinelearning.DataSource, "example", machinelearning.DataSourceConfig{
    			DataSourceId: "example",
    			Verbose:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSource } from "@ubx/sdk-aws/aws/data/machinelearning/data_source";

    export default stack("example", () => {
      intent({ summary: "look up aws_machinelearning_data_source" });

      data(DataSource, "example", {
        dataSourceId: "example",
        verbose: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.machinelearning import DataSource, DataSourceConfig

    def describe():
        ubx.intent("look up aws_machinelearning_data_source")

        ubx.data(DataSource, "example", DataSourceConfig(
            data_source_id="example",
            verbose=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_source_id" type="string" required>
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="verbose" type="bool">
  &lt;p&gt;Specifies whether a describe operation should return exhaustive or abbreviated information.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compute_statistics" type="bool">
  The real, computed statistics for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="compute_time" type="number">
  &lt;p&gt;Long integer type that is a 64-bit signed number.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  &lt;p&gt;A timestamp represented in epoch time.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="created_by_iam_user" type="string">
  &lt;p&gt;An Amazon Web Service (AWS) user account identifier. The account identifier can be an AWS root account or an AWS Identity and Access Management (IAM) user.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="data_location_s3" type="string">
  &lt;p&gt;A reference to a file or bucket on Amazon Simple Storage Service (Amazon S3).&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="data_rearrangement" type="string">
  The real, custom rearrangement rule applied to this data. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_size_in_bytes" type="number">
  &lt;p&gt;Long integer type that is a 64-bit signed number.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="data_source_schema" type="string">
  &lt;p&gt;The schema of a &lt;code&gt;DataSource&lt;/code&gt;. The &lt;code&gt;DataSchema&lt;/code&gt; defines the structure of the observation data in the data file(s) referenced in the &lt;code&gt;DataSource&lt;/code&gt;. The DataSource schema is expressed in JSON format.&lt;/p&gt; &lt;p&gt; &lt;code&gt;DataSchema&lt;/code&gt; is not required if you specify a &lt;code&gt;DataSchemaUri&lt;/code&gt; &lt;/p&gt; &lt;p&gt;&#123; "version": "1.0", "recordAnnotationFieldName": "F1", "recordWeightFieldName": "F2", "targetFieldName": "F3", "dataFormat": "CSV", "dataFileContainsHeader": true, "variables": [ &#123; "fieldName": "F1", "fieldType": "TEXT" &#125;, &#123; "fieldName": "F2", "fieldType": "NUMERIC" &#125;, &#123; "fieldName": "F3", "fieldType": "CATEGORICAL" &#125;, &#123; "fieldName": "F4", "fieldType": "NUMERIC" &#125;, &#123; "fieldName": "F5", "fieldType": "CATEGORICAL" &#125;, &#123; "fieldName": "F6", "fieldType": "TEXT" &#125;, &#123; "fieldName": "F7", "fieldType": "WEIGHTED_INT_SEQUENCE" &#125;, &#123; "fieldName": "F8", "fieldType": "WEIGHTED_STRING_SEQUENCE" &#125; ], "excludedVariableNames": [ "F6" ] &#125;&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="finished_at" type="string">
  &lt;p&gt;A timestamp represented in epoch time.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  &lt;p&gt;A timestamp represented in epoch time.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="log_uri" type="string">
  The real, resolvable URI of the log this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  &lt;p&gt;Description of the most recent details about an object.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  &lt;p&gt;A user-supplied name or description of the Amazon ML resource.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_files" type="number">
  &lt;p&gt;Long integer type that is a 64-bit signed number.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="rdsmetadata" type="object">
  &lt;p&gt;The datasource details that are specific to Amazon RDS.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_pipeline_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="database" type="object">
      &lt;p&gt;The database details of an Amazon RDS database.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="database_name" type="string" required>
          &lt;p&gt;The name of a database hosted on an RDS DB instance.&lt;/p&gt; Required lookup argument.
        </ResponseField>
        <ResponseField name="instance_identifier" type="string" required>
          Identifier of RDS DB Instances. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="database_user_name" type="string">
      &lt;p&gt;The username to be used by Amazon ML to connect to database on an Amazon RDS instance. The username should have sufficient permissions to execute an &lt;code&gt;RDSSelectSqlQuery&lt;/code&gt; query.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_role" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="select_sql_query" type="string">
      &lt;p&gt;The SQL query to be executed against the Amazon RDS database. The SQL query should be valid for the Amazon RDS type being used.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="service_role" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="redshift_metadata" type="object">
  &lt;p&gt;Describes the &lt;code&gt;DataSource&lt;/code&gt; details specific to Amazon Redshift.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="database_user_name" type="string">
      &lt;p&gt;A username to be used by Amazon Machine Learning (Amazon ML)to connect to a database on an Amazon Redshift cluster. The username should have sufficient permissions to execute the &lt;code&gt;RedshiftSelectSqlQuery&lt;/code&gt; query. The username should be valid for an Amazon Redshift &lt;a href="https://docs.aws.amazon.com/redshift/latest/dg/r_CREATE_USER.html"&gt;USER&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="redshift_database" type="object">
      &lt;p&gt;Describes the database details required to connect to an Amazon Redshift database.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cluster_identifier" type="string" required>
          &lt;p&gt;The ID of an Amazon Redshift cluster.&lt;/p&gt; Required lookup argument.
        </ResponseField>
        <ResponseField name="database_name" type="string" required>
          &lt;p&gt;The name of a database hosted on an Amazon Redshift cluster.&lt;/p&gt; Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="select_sql_query" type="string">
      &lt;p&gt; Describes the SQL query to execute on the Amazon Redshift database. The SQL query should be valid for an Amazon Redshift &lt;a href="https://docs.aws.amazon.com/redshift/latest/dg/r_SELECT_synopsis.html"&gt;SELECT&lt;/a&gt;. &lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of an &lt;a href="https://docs.aws.amazon.com/IAM/latest/UserGuide/roles-toplevel.html#roles-about-termsandconcepts"&gt;AWS IAM Role&lt;/a&gt;, such as the following: arn:aws:iam::account:role/rolename. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="started_at" type="string">
  &lt;p&gt;A timestamp represented in epoch time.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;Object status with the following possible values:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;PENDING&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;INPROGRESS&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;FAILED&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;COMPLETED&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;DELETED&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
</ResponseField>
