---
title: "aws_iotthingsgraph_namespace_deletion_status"
description: "`aws_iotthingsgraph_namespace_deletion_status` reads the real, current status of deleting an IoT Things Graph namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_namespace_deletion_status"})

    		ubx.Data(iotthingsgraph.NamespaceDeletionStatus, "example", iotthingsgraph.NamespaceDeletionStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NamespaceDeletionStatus } from "@ubx/sdk-aws/aws/data/iotthingsgraph/namespace_deletion_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_namespace_deletion_status" });

      data(NamespaceDeletionStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import NamespaceDeletionStatus, NamespaceDeletionStatusConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_namespace_deletion_status")

        ubx.data(NamespaceDeletionStatus, "example", NamespaceDeletionStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="error_code" type="string">
  The real code identifying the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_arn" type="string">
  The real Amazon Resource Name (ARN) of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_name" type="string">
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
