---
title: "aws_iotthingsgraph_entities"
description: "`aws_iotthingsgraph_entities` reads the real entities defined in an IoT Things Graph workflow, a retired AWS service for visually building IoT workflows."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_entities"})

    		ubx.Data(iotthingsgraph.Entities, "example", iotthingsgraph.EntitiesConfig{
    			Ids:              []string{"example"},
    			NamespaceVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Entities } from "@ubx/sdk-aws/aws/data/iotthingsgraph/entities";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_entities" });

      data(Entities, "example", {
        ids: ["example"],
        namespaceVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import Entities, EntitiesConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_entities")

        ubx.data(Entities, "example", EntitiesConfig(
            ids=["example"],
            namespace_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ids" type="list(string)" required>
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace_version" type="number">
  The real version of the namespace. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="descriptions" type="list(object)">
  The real, free-text descriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
