---
title: "aws_iotthingsgraph_upload_status"
description: "`aws_iotthingsgraph_upload_status` reads the real, current status of uploading an IoT Things Graph entity definition document."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_upload_status"})

    		ubx.Data(iotthingsgraph.UploadStatus, "example", iotthingsgraph.UploadStatusConfig{
    			UploadId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UploadStatus } from "@ubx/sdk-aws/aws/data/iotthingsgraph/upload_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_upload_status" });

      data(UploadStatus, "example", {
        uploadId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import UploadStatus, UploadStatusConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_upload_status")

        ubx.data(UploadStatus, "example", UploadStatusConfig(
            upload_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="upload_id" type="string" required>
  The real, unique identifier of the upload this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="list(string)">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_arn" type="string">
  The real Amazon Resource Name (ARN) of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_name" type="string">
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_version" type="number">
  The real version of the namespace. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="upload_status" type="string">
  The real, current status of the upload this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
