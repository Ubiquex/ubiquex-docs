---
title: "aws_iotthingsgraph_namespace"
description: "`aws_iotthingsgraph_namespace` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotthingsgraph_namespace` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_namespace"})

    		ubx.Data(iotthingsgraph.Namespace, "example", iotthingsgraph.NamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-aws/aws/data/iotthingsgraph/namespace";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_namespace" });

      data(Namespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import Namespace, NamespaceConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_namespace")

        ubx.data(Namespace, "example", NamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="namespace_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_version" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tracking_namespace_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tracking_namespace_version" type="number">
  Returned by the lookup.
</ResponseField>
