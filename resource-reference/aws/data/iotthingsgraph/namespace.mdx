---
title: "aws_iotthingsgraph_namespace"
description: "`aws_iotthingsgraph_namespace` reads the real, current IoT Things Graph namespace definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_namespace"})

    		ubx.Data(iotthingsgraph.Namespace, "example", iotthingsgraph.NamespaceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Namespace } from "@ubx/sdk-aws/aws/data/iotthingsgraph/namespace";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_namespace" });

      data(Namespace, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import Namespace, NamespaceConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_namespace")

        ubx.data(Namespace, "example", NamespaceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="namespace_arn" type="string">
  The real Amazon Resource Name (ARN) of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_name" type="string">
  The real name of the namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace_version" type="number">
  The real version of the namespace. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tracking_namespace_name" type="string">
  The real name of the tracking namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tracking_namespace_version" type="number">
  The real version of the tracking namespace. **(AI-inferred)** Returned by the lookup.
</ResponseField>
