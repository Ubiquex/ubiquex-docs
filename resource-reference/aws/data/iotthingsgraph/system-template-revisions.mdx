---
title: "aws_iotthingsgraph_system_template_revisions"
description: "`aws_iotthingsgraph_system_template_revisions` reads a real, saved revision of an IoT Things Graph system template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotthingsgraph "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotthingsgraph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotthingsgraph_system_template_revisions"})

    		ubx.Data(iotthingsgraph.SystemTemplateRevisions, "example", iotthingsgraph.SystemTemplateRevisionsConfig{
    			Id:         "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SystemTemplateRevisions } from "@ubx/sdk-aws/aws/data/iotthingsgraph/system_template_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotthingsgraph_system_template_revisions" });

      data(SystemTemplateRevisions, "example", {
        id: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotthingsgraph import SystemTemplateRevisions, SystemTemplateRevisionsConfig

    def describe():
        ubx.intent("look up aws_iotthingsgraph_system_template_revisions")

        ubx.data(SystemTemplateRevisions, "example", SystemTemplateRevisionsConfig(
            id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
