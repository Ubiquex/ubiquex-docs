---
title: "aws_apprunner_custom_domains"
description: "`aws_apprunner_custom_domains` reads the real custom domains associated with an App Runner service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apprunner_custom_domains"})

    		ubx.Data(apprunner.CustomDomains, "example", apprunner.CustomDomainsConfig{
    			ServiceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CustomDomains } from "./local-sdk/aws/sdk/typescript/aws/data/apprunner/custom_domains.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apprunner_custom_domains" });

      data(CustomDomains, "example", {
        serviceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apprunner import CustomDomains, CustomDomainsConfig

    def describe():
        ubx.intent("look up aws_apprunner_custom_domains")

        ubx.data(CustomDomains, "example", CustomDomainsConfig(
            service_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_domains" type="list(object)">
  The real custom domains bound to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dnstarget" type="string">
  The real DNS target this object resolves to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_dnstargets" type="list(object)">
  The real VPC DNS targets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
