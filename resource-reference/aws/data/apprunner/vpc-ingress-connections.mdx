---
title: "aws_apprunner_vpc_ingress_connections"
description: "`aws_apprunner_vpc_ingress_connections` looks up a real App Runner VPC ingress connection, letting a VPC reach a private App Runner service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apprunner_vpc_ingress_connections"})

    		ubx.Data(apprunner.VpcIngressConnections, "example", apprunner.VpcIngressConnectionsConfig{
    			Filter:     map[string]any{"service_arn": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcIngressConnections } from "@ubx/sdk-aws/aws/data/apprunner/vpc_ingress_connections";

    export default stack("example", () => {
      intent({ summary: "look up aws_apprunner_vpc_ingress_connections" });

      data(VpcIngressConnections, "example", {
        filter: { serviceArn: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apprunner import VpcIngressConnections, VpcIngressConnectionsConfig

    def describe():
        ubx.intent("look up aws_apprunner_vpc_ingress_connections")

        ubx.data(VpcIngressConnections, "example", VpcIngressConnectionsConfig(
            filter={"service_arn": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Returns a list of VPC Ingress Connections based on the filter provided. It can return either &lt;code&gt;ServiceArn&lt;/code&gt; or &lt;code&gt;VpcEndpointId&lt;/code&gt;, or both.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="service_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vpc_endpoint_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_ingress_connection_summary_list" type="list(object)">
  The real vpc ingress connection summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
