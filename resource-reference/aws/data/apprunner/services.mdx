---
title: "aws_apprunner_services"
description: "`aws_apprunner_services` looks up a real App Runner service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apprunner_services"})

    		ubx.Data(apprunner.Services, "example", apprunner.ServicesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Services } from "@ubx/sdk-aws/aws/data/apprunner/services";

    export default stack("example", () => {
      intent({ summary: "look up aws_apprunner_services" });

      data(Services, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apprunner import Services, ServicesConfig

    def describe():
        ubx.intent("look up aws_apprunner_services")

        ubx.data(Services, "example", ServicesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_summary_list" type="list(object)">
  The real service summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
