---
title: "aws_apprunner_observability_configurations"
description: "`aws_apprunner_observability_configurations` looks up a real App Runner observability configuration, enabling X-Ray tracing for a service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apprunner_observability_configurations"})

    		ubx.Data(apprunner.ObservabilityConfigurations, "example", apprunner.ObservabilityConfigurationsConfig{
    			LatestOnly:                     true,
    			MaxResults:                     1,
    			ObservabilityConfigurationName: "example-observability-configuration-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ObservabilityConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/apprunner/observability_configurations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apprunner_observability_configurations" });

      data(ObservabilityConfigurations, "example", {
        latestOnly: true,
        maxResults: 1,
        observabilityConfigurationName: "example-observability-configuration-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apprunner import ObservabilityConfigurations, ObservabilityConfigurationsConfig

    def describe():
        ubx.intent("look up aws_apprunner_observability_configurations")

        ubx.data(ObservabilityConfigurations, "example", ObservabilityConfigurationsConfig(
            latest_only=True,
            max_results=1,
            observability_configuration_name="example-observability-configuration-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="latest_only" type="bool">
  Whether the response is restricted to only the latest version. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="observability_configuration_name" type="string">
  The real name of the observability configuration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="observability_configuration_summary_list" type="list(object)">
  The real observability configuration summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
