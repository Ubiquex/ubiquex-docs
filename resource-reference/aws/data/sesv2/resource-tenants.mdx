---
title: "aws_sesv2_resource_tenants"
description: "`aws_sesv2_resource_tenants` reads the real SES multi-tenant management tenants a resource is associated with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_resource_tenants"})

    		ubx.Data(sesv2.ResourceTenants, "example", sesv2.ResourceTenantsConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			PageSize:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceTenants } from "./local-sdk/aws/sdk/typescript/aws/data/sesv2/resource_tenants.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_resource_tenants" });

      data(ResourceTenants, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import ResourceTenants, ResourceTenantsConfig

    def describe():
        ubx.intent("look up aws_sesv2_resource_tenants")

        ubx.data(ResourceTenants, "example", ResourceTenantsConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_tenants" type="list(object)">
  &lt;p&gt;A list of tenant metadata objects associated with a resource.&lt;/p&gt; Returned by the lookup.
</ResponseField>
