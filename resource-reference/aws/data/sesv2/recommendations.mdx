---
title: "aws_sesv2_recommendations"
description: "`aws_sesv2_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_recommendations"})

    		ubx.Data(sesv2.Recommendations, "example", sesv2.RecommendationsConfig{
    			Filter:   map[string]string{"managed-by": "ubx"},
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Recommendations } from "@ubx/sdk-aws/aws/data/sesv2/recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_recommendations" });

      data(Recommendations, "example", {
        filter: { "managed-by": "ubx" },
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import Recommendations, RecommendationsConfig

    def describe():
        ubx.intent("look up aws_sesv2_recommendations")

        ubx.data(Recommendations, "example", RecommendationsConfig(
            filter={"managed-by": "ubx"},
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="map(string)">
  &lt;p&gt;An object that contains mapping between &lt;code&gt;ListRecommendationsFilterKey&lt;/code&gt; and &lt;code&gt;ListRecommendationFilterValue&lt;/code&gt; to filter by.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
