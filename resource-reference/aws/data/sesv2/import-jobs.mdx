---
title: "aws_sesv2_import_jobs"
description: "`aws_sesv2_import_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_import_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_import_jobs"})

    		ubx.Data(sesv2.ImportJobs, "example", sesv2.ImportJobsConfig{
    			ImportDestinationType: "example",
    			PageSize:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImportJobs } from "@ubx/sdk-aws/aws/data/sesv2/import_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_import_jobs" });

      data(ImportJobs, "example", {
        importDestinationType: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import ImportJobs, ImportJobsConfig

    def describe():
        ubx.intent("look up aws_sesv2_import_jobs")

        ubx.data(ImportJobs, "example", ImportJobsConfig(
            import_destination_type="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="import_destination_type" type="string">
  &lt;p&gt;The destination of the import job, which can be used to list import jobs that have a certain &lt;code&gt;ImportDestinationType&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="import_jobs" type="list(object)">
  &lt;p&gt;A list of the import job summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
