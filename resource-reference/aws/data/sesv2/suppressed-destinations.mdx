---
title: "aws_sesv2_suppressed_destinations"
description: "`aws_sesv2_suppressed_destinations` reads the real email addresses on the SES account-level suppression list."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_suppressed_destinations"})

    		ubx.Data(sesv2.SuppressedDestinations, "example", sesv2.SuppressedDestinationsConfig{
    			EndDate:    "example",
    			PageSize:   1,
    			Reasons:    []string{"example"},
    			StartDate:  "example",
    			TenantName: "example-tenant-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SuppressedDestinations } from "./local-sdk/aws/sdk/typescript/aws/data/sesv2/suppressed_destinations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_suppressed_destinations" });

      data(SuppressedDestinations, "example", {
        endDate: "example",
        pageSize: 1,
        reasons: ["example"],
        startDate: "example",
        tenantName: "example-tenant-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import SuppressedDestinations, SuppressedDestinationsConfig

    def describe():
        ubx.intent("look up aws_sesv2_suppressed_destinations")

        ubx.data(SuppressedDestinations, "example", SuppressedDestinationsConfig(
            end_date="example",
            page_size=1,
            reasons=["example"],
            start_date="example",
            tenant_name="example-tenant-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_date" type="string">
  The latest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reasons" type="list(string)">
  The real reasons included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tenant_name" type="string">
  &lt;p&gt;The name of a tenant. The name can contain up to 64 alphanumeric characters, including letters, numbers, hyphens (-) and underscores (_) only.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="suppressed_destination_summaries" type="list(object)">
  The real, summarized suppressed destinations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
