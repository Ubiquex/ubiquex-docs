---
title: "aws_sesv2_suppressed_destinations"
description: "`aws_sesv2_suppressed_destinations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_suppressed_destinations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_suppressed_destinations"})

    		ubx.Data(sesv2.SuppressedDestinations, "example", sesv2.SuppressedDestinationsConfig{
    			EndDate:    "example",
    			PageSize:   1,
    			Reasons:    []string{"example"},
    			StartDate:  "example",
    			TenantName: "example-tenant-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SuppressedDestinations } from "@ubx/sdk-aws/aws/data/sesv2/suppressed_destinations";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_suppressed_destinations" });

      data(SuppressedDestinations, "example", {
        endDate: "example",
        pageSize: 1,
        reasons: ["example"],
        startDate: "example",
        tenantName: "example-tenant-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import SuppressedDestinations, SuppressedDestinationsConfig

    def describe():
        ubx.intent("look up aws_sesv2_suppressed_destinations")

        ubx.data(SuppressedDestinations, "example", SuppressedDestinationsConfig(
            end_date="example",
            page_size=1,
            reasons=["example"],
            start_date="example",
            tenant_name="example-tenant-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_date" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reasons" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tenant_name" type="string">
  &lt;p&gt;The name of a tenant. The name can contain up to 64 alphanumeric characters, including letters, numbers, hyphens (-) and underscores (_) only.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="suppressed_destination_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
