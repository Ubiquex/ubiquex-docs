---
title: "aws_sesv2_tenant_resources"
description: "`aws_sesv2_tenant_resources` reads the real SES resources (identities, configuration sets) associated with an SES multi-tenant management tenant."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_tenant_resources"})

    		ubx.Data(sesv2.TenantResources, "example", sesv2.TenantResourcesConfig{
    			TenantName: "example-tenant-name",
    			Filter:     map[string]string{"managed-by": "ubx"},
    			PageSize:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TenantResources } from "@ubx/sdk-aws/aws/data/sesv2/tenant_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_tenant_resources" });

      data(TenantResources, "example", {
        tenantName: "example-tenant-name",
        filter: { "managed-by": "ubx" },
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import TenantResources, TenantResourcesConfig

    def describe():
        ubx.intent("look up aws_sesv2_tenant_resources")

        ubx.data(TenantResources, "example", TenantResourcesConfig(
            tenant_name="example-tenant-name",
            filter={"managed-by": "ubx"},
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="map(string)">
  &lt;p&gt;An object that contains mapping between &lt;code&gt;ListTenantResourcesFilterKey&lt;/code&gt; and &lt;code&gt;ListTenantResourcesFilterValue&lt;/code&gt; to filter by.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tenant_name" type="string" required>
  &lt;p&gt;The name of a tenant. The name can contain up to 64 alphanumeric characters, including letters, numbers, hyphens (-) and underscores (_) only.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tenant_resources" type="list(object)">
  &lt;p&gt;A list of resources associated with a tenant.&lt;/p&gt; Returned by the lookup.
</ResponseField>
