---
title: "aws_sesv2_export_jobs"
description: "`aws_sesv2_export_jobs` reads the real jobs exporting SES message insights data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_export_jobs"})

    		ubx.Data(sesv2.ExportJobs, "example", sesv2.ExportJobsConfig{
    			ExportSourceType: "example",
    			JobStatus:        "example",
    			PageSize:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExportJobs } from "./local-sdk/aws/sdk/typescript/aws/data/sesv2/export_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_export_jobs" });

      data(ExportJobs, "example", {
        exportSourceType: "example",
        jobStatus: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import ExportJobs, ExportJobsConfig

    def describe():
        ubx.intent("look up aws_sesv2_export_jobs")

        ubx.data(ExportJobs, "example", ExportJobsConfig(
            export_source_type="example",
            job_status="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_source_type" type="string">
  &lt;p&gt;The type of data source of an export, can be one of the following:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;METRICS_DATA&lt;/code&gt; - The metrics export.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;MESSAGE_INSIGHTS&lt;/code&gt; - The Message Insights export.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  &lt;p&gt;The status of a job.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CREATED&lt;/code&gt;  --  Job has just been created.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;PROCESSING&lt;/code&gt;  --  Job is processing.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;ERROR&lt;/code&gt;  --  An error occurred during processing.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;COMPLETED&lt;/code&gt;  --  Job has completed processing successfully.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_jobs" type="list(object)">
  &lt;p&gt;A list of the export job summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
