---
title: "aws_sesv2_dedicated_ip_pools"
description: "`data_aws_sesv2_dedicated_ip_pools` looks up a real, already-existing `aws_ses_dedicated_ip_pool`."
---

It groups real dedicated sending IP addresses under an `aws_ses_configuration_set`, so a chosen subset of an account's dedicated IPs is used for a particular category of email, keeping sending reputations isolated between categories.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_dedicated_ip_pools"})

    		ubx.Data(sesv2.DedicatedIpPools, "example", sesv2.DedicatedIpPoolsConfig{
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DedicatedIpPools } from "@ubx/sdk-aws/aws/data/sesv2/dedicated_ip_pools";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_dedicated_ip_pools" });

      data(DedicatedIpPools, "example", {
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import DedicatedIpPools, DedicatedIpPoolsConfig

    def describe():
        ubx.intent("look up aws_sesv2_dedicated_ip_pools")

        ubx.data(DedicatedIpPools, "example", DedicatedIpPoolsConfig(
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dedicated_ip_pools" type="list(string)">
  &lt;p&gt;A list of dedicated IP pools that are associated with your Amazon Web Services account.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
