---
title: "aws_sesv2_blacklist_reports"
description: "`aws_sesv2_blacklist_reports` reads real, reported blacklisting status of an SES sending IP address across DNS-based blocklists."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_blacklist_reports"})

    		ubx.Data(sesv2.BlacklistReports, "example", sesv2.BlacklistReportsConfig{
    			BlacklistItemNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BlacklistReports } from "@ubx/sdk-aws/aws/data/sesv2/blacklist_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_blacklist_reports" });

      data(BlacklistReports, "example", {
        blacklistItemNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import BlacklistReports, BlacklistReportsConfig

    def describe():
        ubx.intent("look up aws_sesv2_blacklist_reports")

        ubx.data(BlacklistReports, "example", BlacklistReportsConfig(
            blacklist_item_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="blacklist_item_names" type="list(string)" required>
  The real names of the blacklist items included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blacklist_report" type="map(list(object))">
  The real report for the blacklist. **(AI-inferred)** Returned by the lookup.
</ResponseField>
