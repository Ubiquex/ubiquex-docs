---
title: "aws_sesv2_domain_deliverability_campaign"
description: "`aws_sesv2_domain_deliverability_campaign` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_domain_deliverability_campaign` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_domain_deliverability_campaign"})

    		ubx.Data(sesv2.DomainDeliverabilityCampaign, "example", sesv2.DomainDeliverabilityCampaignConfig{
    			CampaignId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainDeliverabilityCampaign } from "@ubx/sdk-aws/aws/data/sesv2/domain_deliverability_campaign";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_domain_deliverability_campaign" });

      data(DomainDeliverabilityCampaign, "example", {
        campaignId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import DomainDeliverabilityCampaign, DomainDeliverabilityCampaignConfig

    def describe():
        ubx.intent("look up aws_sesv2_domain_deliverability_campaign")

        ubx.data(DomainDeliverabilityCampaign, "example", DomainDeliverabilityCampaignConfig(
            campaign_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_deliverability_campaign" type="object">
  &lt;p&gt;An object that contains the deliverability data for a specific campaign. This data is available for a campaign only if the campaign sent email by using a domain that the Deliverability dashboard is enabled for (&lt;code&gt;PutDeliverabilityDashboardOption&lt;/code&gt; operation).&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="campaign_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="delete_rate" type="number">
      &lt;p&gt;An object that contains information about inbox placement percentages.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="esps" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="first_seen_date_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="from_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="inbox_count" type="number">
      &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_seen_date_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="projected_volume" type="number">
      &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="read_delete_rate" type="number">
      &lt;p&gt;An object that contains information about inbox placement percentages.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="read_rate" type="number">
      &lt;p&gt;An object that contains information about inbox placement percentages.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="sending_ips" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="spam_count" type="number">
      &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="subject" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
