---
title: "aws_sesv2_dedicated_ip"
description: "`aws_sesv2_dedicated_ip` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_dedicated_ip` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_dedicated_ip"})

    		ubx.Data(sesv2.DedicatedIp, "example", sesv2.DedicatedIpConfig{
    			Ip: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DedicatedIp } from "@ubx/sdk-aws/aws/data/sesv2/dedicated_ip";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_dedicated_ip" });

      data(DedicatedIp, "example", {
        ip: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import DedicatedIp, DedicatedIpConfig

    def describe():
        ubx.intent("look up aws_sesv2_dedicated_ip")

        ubx.data(DedicatedIp, "example", DedicatedIpConfig(
            ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ip" type="string" required>
  &lt;p&gt;An IPv4 address.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dedicated_ip" type="object">
  &lt;p&gt;Contains information about a dedicated IP address that is associated with your Amazon SES account.&lt;/p&gt; &lt;p&gt;To learn more about requesting dedicated IP addresses, see &lt;a href="https://docs.aws.amazon.com/ses/latest/DeveloperGuide/dedicated-ip-case.html"&gt;Requesting and Relinquishing Dedicated IP Addresses&lt;/a&gt; in the &lt;i&gt;Amazon SES Developer Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ip" type="string" required>
      &lt;p&gt;An IPv4 address.&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="pool_name" type="string">
      &lt;p&gt;The name of a dedicated IP pool.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="warmup_percentage" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="warmup_status" type="string" required>
      &lt;p&gt;The warmup status of a dedicated IP.&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
