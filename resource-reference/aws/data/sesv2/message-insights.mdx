---
title: "aws_sesv2_message_insights"
description: "`aws_sesv2_message_insights` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_message_insights` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_message_insights"})

    		ubx.Data(sesv2.MessageInsights, "example", sesv2.MessageInsightsConfig{
    			MessageId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessageInsights } from "@ubx/sdk-aws/aws/data/sesv2/message_insights";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_message_insights" });

      data(MessageInsights, "example", {
        messageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import MessageInsights, MessageInsightsConfig

    def describe():
        ubx.intent("look up aws_sesv2_message_insights")

        ubx.data(MessageInsights, "example", MessageInsightsConfig(
            message_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="message_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="email_tags" type="list(object)">
  &lt;p&gt;A list of message tags.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="from_email_address" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="insights" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="subject" type="string">
  Returned by the lookup.
</ResponseField>
