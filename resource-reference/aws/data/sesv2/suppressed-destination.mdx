---
title: "aws_sesv2_suppressed_destination"
description: "`aws_sesv2_suppressed_destination` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_suppressed_destination` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_suppressed_destination"})

    		ubx.Data(sesv2.SuppressedDestination, "example", sesv2.SuppressedDestinationConfig{
    			EmailAddress: "example",
    			TenantName:   "example-tenant-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SuppressedDestination } from "@ubx/sdk-aws/aws/data/sesv2/suppressed_destination";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_suppressed_destination" });

      data(SuppressedDestination, "example", {
        emailAddress: "example",
        tenantName: "example-tenant-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import SuppressedDestination, SuppressedDestinationConfig

    def describe():
        ubx.intent("look up aws_sesv2_suppressed_destination")

        ubx.data(SuppressedDestination, "example", SuppressedDestinationConfig(
            email_address="example",
            tenant_name="example-tenant-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="email_address" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="tenant_name" type="string">
  &lt;p&gt;The name of a tenant. The name can contain up to 64 alphanumeric characters, including letters, numbers, hyphens (-) and underscores (_) only.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="suppressed_destination" type="object">
  &lt;p&gt;An object that contains information about an email address that is on the suppression list for your account or for a specific tenant.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      &lt;p&gt;An object that contains additional attributes that are related an email address that is on the suppression list for your account or for a specific tenant.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="feedback_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="message_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="email_address" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="last_update_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="reason" type="string" required>
      &lt;p&gt;The reason that the address was added to the suppression list for your account or for a specific tenant. The value can be one of the following:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;COMPLAINT&lt;/code&gt;  --  Amazon SES added an email address to the suppression list for your account or for a specific tenant because a message sent to that address results in a complaint.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;BOUNCE&lt;/code&gt;  --  Amazon SES added an email address to the suppression list for your account or for a specific tenant because a message sent to that address results in a hard bounce.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="tenant_name" type="string">
      &lt;p&gt;The name of a tenant. The name can contain up to 64 alphanumeric characters, including letters, numbers, hyphens (-) and underscores (_) only.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
