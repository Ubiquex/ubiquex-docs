---
title: "aws_sesv2_reputation_entities"
description: "`aws_sesv2_reputation_entities` reads the real reputation entities (an identity or configuration set) tracked by SES."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_reputation_entities"})

    		ubx.Data(sesv2.ReputationEntities, "example", sesv2.ReputationEntitiesConfig{
    			Filter:   map[string]string{"managed-by": "ubx"},
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReputationEntities } from "./local-sdk/aws/sdk/typescript/aws/data/sesv2/reputation_entities.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_reputation_entities" });

      data(ReputationEntities, "example", {
        filter: { "managed-by": "ubx" },
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import ReputationEntities, ReputationEntitiesConfig

    def describe():
        ubx.intent("look up aws_sesv2_reputation_entities")

        ubx.data(ReputationEntities, "example", ReputationEntitiesConfig(
            filter={"managed-by": "ubx"},
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="map(string)">
  &lt;p&gt;An object that contains filters to apply when listing reputation entities.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reputation_entities" type="list(object)">
  &lt;p&gt;A list of reputation entities.&lt;/p&gt; Returned by the lookup.
</ResponseField>
