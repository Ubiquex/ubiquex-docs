---
title: "aws_sesv2_dedicated_ips"
description: "`aws_sesv2_dedicated_ips` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sesv2_dedicated_ips` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sesv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sesv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sesv2_dedicated_ips"})

    		ubx.Data(sesv2.DedicatedIps, "example", sesv2.DedicatedIpsConfig{
    			PageSize: 1,
    			PoolName: "example-pool-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DedicatedIps } from "@ubx/sdk-aws/aws/data/sesv2/dedicated_ips";

    export default stack("example", () => {
      intent({ summary: "look up aws_sesv2_dedicated_ips" });

      data(DedicatedIps, "example", {
        pageSize: 1,
        poolName: "example-pool-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sesv2 import DedicatedIps, DedicatedIpsConfig

    def describe():
        ubx.intent("look up aws_sesv2_dedicated_ips")

        ubx.data(DedicatedIps, "example", DedicatedIpsConfig(
            page_size=1,
            pool_name="example-pool-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string">
  &lt;p&gt;The name of a dedicated IP pool.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dedicated_ips" type="list(object)">
  &lt;p&gt;A list of dedicated IP addresses that are associated with your Amazon Web Services account.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
