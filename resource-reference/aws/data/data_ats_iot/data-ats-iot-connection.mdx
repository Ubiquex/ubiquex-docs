---
title: "aws_data_ats_iot_connection"
description: "`aws_data_ats_iot_connection` reads the real, current MQTT connection status of an IoT Core device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_ats_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_ats_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_ats_iot_connection"})

    		ubx.Data(data_ats_iot.DataAtsIotConnection, "example", data_ats_iot.DataAtsIotConnectionConfig{
    			ClientId:                 "example",
    			IncludeSocketInformation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataAtsIotConnection } from "@ubx/sdk-aws/aws/data/data_ats_iot/data_ats_iot_connection";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_ats_iot_connection" });

      data(DataAtsIotConnection, "example", {
        clientId: "example",
        includeSocketInformation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_ats_iot import DataAtsIotConnection, DataAtsIotConnectionConfig

    def describe():
        ubx.intent("look up aws_data_ats_iot_connection")

        ubx.data(DataAtsIotConnection, "example", DataAtsIotConnectionConfig(
            client_id="example",
            include_socket_information=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_id" type="string" required>
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_socket_information" type="bool">
  Whether the real, underlying socket information is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clean_session" type="bool">
  Whether this MQTT connection starts with a clean session. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connected" type="bool">
  Whether this object is currently connected. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connected_since" type="number">
  The real timestamp this object has been connected since. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="disconnect_reason" type="string">
  The real, human-readable reason for the disconnect. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="disconnected_since" type="number">
  The real timestamp this object has been disconnected since. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="keep_alive_duration" type="number">
  The real duration this connection is kept alive without activity, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_expiry" type="number">
  The real timestamp this session expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_ip" type="string">
  The real source IP address of this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_port" type="number">
  The real source port of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_ip" type="string">
  The real target IP address of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_port" type="number">
  The real target port of this connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thing_name" type="string">
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  The real, unique identifier of the vpc endpoint this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
