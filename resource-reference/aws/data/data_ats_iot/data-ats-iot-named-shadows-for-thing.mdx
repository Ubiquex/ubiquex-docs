---
title: "aws_data_ats_iot_named_shadows_for_thing"
description: "`aws_data_ats_iot_named_shadows_for_thing` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_ats_iot_named_shadows_for_thing` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_ats_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_ats_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_ats_iot_named_shadows_for_thing"})

    		ubx.Data(data_ats_iot.DataAtsIotNamedShadowsForThing, "example", data_ats_iot.DataAtsIotNamedShadowsForThingConfig{
    			ThingName: "example-thing-name",
    			PageSize:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataAtsIotNamedShadowsForThing } from "@ubx/sdk-aws/aws/data/data_ats_iot/data_ats_iot_named_shadows_for_thing";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_ats_iot_named_shadows_for_thing" });

      data(DataAtsIotNamedShadowsForThing, "example", {
        thingName: "example-thing-name",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_ats_iot import DataAtsIotNamedShadowsForThing, DataAtsIotNamedShadowsForThingConfig

    def describe():
        ubx.intent("look up aws_data_ats_iot_named_shadows_for_thing")

        ubx.data(DataAtsIotNamedShadowsForThing, "example", DataAtsIotNamedShadowsForThingConfig(
            thing_name="example-thing-name",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="number">
  Returned by the lookup.
</ResponseField>
