---
title: "aws_data_ats_iot_thing_shadow"
description: "`aws_data_ats_iot_thing_shadow` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_ats_iot_thing_shadow` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_ats_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_ats_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_ats_iot_thing_shadow"})

    		ubx.Data(data_ats_iot.DataAtsIotThingShadow, "example", data_ats_iot.DataAtsIotThingShadowConfig{
    			ThingName:  "example-thing-name",
    			ShadowName: "example-shadow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataAtsIotThingShadow } from "@ubx/sdk-aws/aws/data/data_ats_iot/data_ats_iot_thing_shadow";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_ats_iot_thing_shadow" });

      data(DataAtsIotThingShadow, "example", {
        thingName: "example-thing-name",
        shadowName: "example-shadow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_ats_iot import DataAtsIotThingShadow, DataAtsIotThingShadowConfig

    def describe():
        ubx.intent("look up aws_data_ats_iot_thing_shadow")

        ubx.data(DataAtsIotThingShadow, "example", DataAtsIotThingShadowConfig(
            thing_name="example-thing-name",
            shadow_name="example-shadow-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shadow_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="string">
  Returned by the lookup.
</ResponseField>
