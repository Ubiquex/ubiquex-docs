---
title: "aws_data_ats_iot_retained_messages"
description: "`aws_data_ats_iot_retained_messages` reads the real MQTT retained messages published to IoT Core topics."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data_ats_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_ats_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_ats_iot_retained_messages"})

    		ubx.Data(data_ats_iot.DataAtsIotRetainedMessages, "example", data_ats_iot.DataAtsIotRetainedMessagesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataAtsIotRetainedMessages } from "./local-sdk/aws/sdk/typescript/aws/data/data_ats_iot/data_ats_iot_retained_messages.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_ats_iot_retained_messages" });

      data(DataAtsIotRetainedMessages, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.data_ats_iot import DataAtsIotRetainedMessages, DataAtsIotRetainedMessagesConfig

    def describe():
        ubx.intent("look up aws_data_ats_iot_retained_messages")

        ubx.data(DataAtsIotRetainedMessages, "example", DataAtsIotRetainedMessagesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retained_topics" type="list(object)">
  The real MQTT topics whose last message is retained. **(AI-inferred)** Returned by the lookup.
</ResponseField>
