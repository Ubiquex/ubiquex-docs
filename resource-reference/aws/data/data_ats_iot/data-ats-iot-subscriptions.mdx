---
title: "aws_data_ats_iot_subscriptions"
description: "`aws_data_ats_iot_subscriptions` reads the real MQTT topic subscriptions of an IoT Core device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_ats_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/data_ats_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_ats_iot_subscriptions"})

    		ubx.Data(data_ats_iot.DataAtsIotSubscriptions, "example", data_ats_iot.DataAtsIotSubscriptionsConfig{
    			ClientId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataAtsIotSubscriptions } from "@ubx/sdk-aws/aws/data/data_ats_iot/data_ats_iot_subscriptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_ats_iot_subscriptions" });

      data(DataAtsIotSubscriptions, "example", {
        clientId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_ats_iot import DataAtsIotSubscriptions, DataAtsIotSubscriptionsConfig

    def describe():
        ubx.intent("look up aws_data_ats_iot_subscriptions")

        ubx.data(DataAtsIotSubscriptions, "example", DataAtsIotSubscriptionsConfig(
            client_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_id" type="string" required>
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscriptions" type="list(object)">
  The real subscriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
