---
title: "aws_schemas_code_binding"
description: "`aws_schemas_code_binding` reads the real, generated code binding (a language-specific data class) for an EventBridge schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	schemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/schemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_schemas_code_binding"})

    		ubx.Data(schemas.CodeBinding, "example", schemas.CodeBindingConfig{
    			Language:     "example",
    			RegistryName: "example-registry-name",
    			SchemaName:   "example-schema-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodeBinding } from "./local-sdk/aws/sdk/typescript/aws/data/schemas/code_binding.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_schemas_code_binding" });

      data(CodeBinding, "example", {
        language: "example",
        registryName: "example-registry-name",
        schemaName: "example-schema-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.schemas import CodeBinding, CodeBindingConfig

    def describe():
        ubx.intent("look up aws_schemas_code_binding")

        ubx.data(CodeBinding, "example", CodeBindingConfig(
            language="example",
            registry_name="example-registry-name",
            schema_name="example-schema-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string" required>
  The real language this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="schema_name" type="string" required>
  The real name of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_version" type="string">
  The real version of the schema. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
