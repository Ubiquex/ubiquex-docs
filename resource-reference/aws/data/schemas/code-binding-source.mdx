---
title: "aws_schemas_code_binding_source"
description: "`aws_schemas_code_binding_source` reads the real source code of a generated code binding for an EventBridge schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	schemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/schemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_schemas_code_binding_source"})

    		ubx.Data(schemas.CodeBindingSource, "example", schemas.CodeBindingSourceConfig{
    			Language:      "example",
    			RegistryName:  "example-registry-name",
    			SchemaName:    "example-schema-name",
    			SchemaVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeBindingSource } from "@ubx/sdk-aws/aws/data/schemas/code_binding_source";

    export default stack("example", () => {
      intent({ summary: "look up aws_schemas_code_binding_source" });

      data(CodeBindingSource, "example", {
        language: "example",
        registryName: "example-registry-name",
        schemaName: "example-schema-name",
        schemaVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.schemas import CodeBindingSource, CodeBindingSourceConfig

    def describe():
        ubx.intent("look up aws_schemas_code_binding_source")

        ubx.data(CodeBindingSource, "example", CodeBindingSourceConfig(
            language="example",
            registry_name="example-registry-name",
            schema_name="example-schema-name",
            schema_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string" required>
  The real language this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="schema_name" type="string" required>
  The real name of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="schema_version" type="string">
  The real version of the schema. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  The real, raw body of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
