---
title: "aws_schemas_registries"
description: "`aws_schemas_registries` looks up a real EventBridge schema registry, a collection of related event schemas."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	schemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/schemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_schemas_registries"})

    		ubx.Data(schemas.Registries, "example", schemas.RegistriesConfig{
    			Limit:              1,
    			RegistryNamePrefix: "example",
    			Scope:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Registries } from "./local-sdk/aws/sdk/typescript/aws/data/schemas/registries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_schemas_registries" });

      data(Registries, "example", {
        limit: 1,
        registryNamePrefix: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.schemas import Registries, RegistriesConfig

    def describe():
        ubx.intent("look up aws_schemas_registries")

        ubx.data(Registries, "example", RegistriesConfig(
            limit=1,
            registry_name_prefix="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_name_prefix" type="string">
  The real S3 key prefix used for the registry name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string">
  The real scope this lookup is applied at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registries" type="list(object)">
  The real registries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
