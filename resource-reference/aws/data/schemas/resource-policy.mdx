---
title: "aws_schemas_resource_policy"
description: "`aws_schemas_resource_policy` reads the real resource-based policy attached to an EventBridge schema registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	schemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/schemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_schemas_resource_policy"})

    		ubx.Data(schemas.ResourcePolicy, "example", schemas.ResourcePolicyConfig{
    			RegistryName: "example-registry-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/data/schemas/resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_schemas_resource_policy" });

      data(ResourcePolicy, "example", {
        registryName: "example-registry-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.schemas import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_schemas_resource_policy")

        ubx.data(ResourcePolicy, "example", ResourcePolicyConfig(
            registry_name="example-registry-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="registry_name" type="string">
  The real name of the registry this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revision_id" type="string">
  The real, unique identifier of this object's own revision. **(AI-inferred)** Returned by the lookup.
</ResponseField>
