---
title: "aws_s3_outposts_outposts_with_s3"
description: "`aws_s3_outposts_outposts_with_s3` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_outposts_outposts_with_s3` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_outposts_outposts_with_s3"})

    		ubx.Data(s3_outposts.S3OutpostsOutpostsWithS3, "example", s3_outposts.S3OutpostsOutpostsWithS3Config{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3OutpostsOutpostsWithS3 } from "@ubx/sdk-aws/aws/data/s3_outposts/s3_outposts_outposts_with_s3";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_outposts_outposts_with_s3" });

      data(S3OutpostsOutpostsWithS3, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_outposts import S3OutpostsOutpostsWithS3, S3OutpostsOutpostsWithS3Config

    def describe():
        ubx.intent("look up aws_s3_outposts_outposts_with_s3")

        ubx.data(S3OutpostsOutpostsWithS3, "example", S3OutpostsOutpostsWithS3Config(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="outposts" type="list(object)">
  Returned by the lookup.
</ResponseField>
