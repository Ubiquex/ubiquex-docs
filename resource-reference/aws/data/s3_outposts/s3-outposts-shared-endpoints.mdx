---
title: "aws_s3_outposts_shared_endpoints"
description: "`aws_s3_outposts_shared_endpoints` reads the real S3 on Outposts endpoints shared with the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/s3_outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_outposts_shared_endpoints"})

    		ubx.Data(s3_outposts.S3OutpostsSharedEndpoints, "example", s3_outposts.S3OutpostsSharedEndpointsConfig{
    			OutpostId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3OutpostsSharedEndpoints } from "@ubx/sdk-aws/aws/data/s3_outposts/s3_outposts_shared_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_outposts_shared_endpoints" });

      data(S3OutpostsSharedEndpoints, "example", {
        outpostId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_outposts import S3OutpostsSharedEndpoints, S3OutpostsSharedEndpointsConfig

    def describe():
        ubx.intent("look up aws_s3_outposts_shared_endpoints")

        ubx.data(S3OutpostsSharedEndpoints, "example", S3OutpostsSharedEndpointsConfig(
            outpost_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_id" type="string" required>
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="endpoints" type="list(object)">
  The real endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
