---
title: "aws_omics_s3_access_policy"
description: "`aws_omics_s3_access_policy` reads the real resource-based S3 access policy attached to an Amazon Omics sequence or reference store."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	omics "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/omics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_omics_s3_access_policy"})

    		ubx.Data(omics.S3AccessPolicy, "example", omics.S3AccessPolicyConfig{
    			S3AccessPointArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3AccessPolicy } from "@ubx/sdk-aws/aws/data/omics/s3_access_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_omics_s3_access_policy" });

      data(S3AccessPolicy, "example", {
        s3AccessPointArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.omics import S3AccessPolicy, S3AccessPolicyConfig

    def describe():
        ubx.intent("look up aws_omics_s3_access_policy")

        ubx.data(S3AccessPolicy, "example", S3AccessPolicyConfig(
            s3_access_point_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="s3_access_point_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the s3 access point this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="s3_access_policy" type="string">
  The real s3 access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="store_id" type="string">
  The real, unique identifier of the store this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="store_type" type="string">
  The real, discriminating type of the store this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
