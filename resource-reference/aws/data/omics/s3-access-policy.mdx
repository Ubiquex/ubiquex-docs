---
title: "aws_omics_s3_access_policy"
description: "`aws_omics_s3_access_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_omics_s3_access_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	omics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/omics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_omics_s3_access_policy"})

    		ubx.Data(omics.S3AccessPolicy, "example", omics.S3AccessPolicyConfig{
    			S3AccessPointArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3AccessPolicy } from "@ubx/sdk-aws/aws/data/omics/s3_access_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_omics_s3_access_policy" });

      data(S3AccessPolicy, "example", {
        s3AccessPointArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.omics import S3AccessPolicy, S3AccessPolicyConfig

    def describe():
        ubx.intent("look up aws_omics_s3_access_policy")

        ubx.data(S3AccessPolicy, "example", S3AccessPolicyConfig(
            s3_access_point_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="s3_access_point_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="s3_access_policy" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="store_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="store_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Returned by the lookup.
</ResponseField>
