---
title: "aws_lambda_microvms_microvm_image_versions"
description: "`aws_lambda_microvms_microvm_image_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_microvms_microvm_image_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_microvm_image_versions"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsMicrovmImageVersions, "example", lambda_microvms.LambdaMicrovmsMicrovmImageVersionsConfig{
    			ImageIdentifier: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LambdaMicrovmsMicrovmImageVersions } from "@ubx/sdk-aws/aws/data/lambda_microvms/lambda_microvms_microvm_image_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_microvms_microvm_image_versions" });

      data(LambdaMicrovmsMicrovmImageVersions, "example", {
        imageIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsMicrovmImageVersions, LambdaMicrovmsMicrovmImageVersionsConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_microvm_image_versions")

        ubx.data(LambdaMicrovmsMicrovmImageVersions, "example", LambdaMicrovmsMicrovmImageVersionsConfig(
            image_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_identifier" type="string" required>
  The ARN or ID of the MicroVmImage Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  List of MicroVM image version summaries Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
