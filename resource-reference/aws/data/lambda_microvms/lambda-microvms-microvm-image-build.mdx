---
title: "aws_lambda_microvms_microvm_image_build"
description: "`aws_lambda_microvms_microvm_image_build` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_microvms_microvm_image_build` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_microvm_image_build"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsMicrovmImageBuild, "example", lambda_microvms.LambdaMicrovmsMicrovmImageBuildConfig{
    			BuildId:         "example",
    			ImageIdentifier: "example",
    			ImageVersion:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LambdaMicrovmsMicrovmImageBuild } from "@ubx/sdk-aws/aws/data/lambda_microvms/lambda_microvms_microvm_image_build";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_microvms_microvm_image_build" });

      data(LambdaMicrovmsMicrovmImageBuild, "example", {
        buildId: "example",
        imageIdentifier: "example",
        imageVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsMicrovmImageBuild, LambdaMicrovmsMicrovmImageBuildConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_microvm_image_build")

        ubx.data(LambdaMicrovmsMicrovmImageBuild, "example", LambdaMicrovmsMicrovmImageBuildConfig(
            build_id="example",
            image_identifier="example",
            image_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="build_id" type="string" required>
  A string which is not empty or blank (only whitespace). Required lookup argument.
</ResponseField>

<ResponseField name="image_identifier" type="string" required>
  The ARN or ID of the MicroVmImage Required lookup argument.
</ResponseField>

<ResponseField name="image_version" type="string" required>
  A string which is not empty or blank (only whitespace). Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="architecture" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="build_state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="chipset" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="chipset_generation" type="string">
  A string which is not empty or blank (only whitespace). Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="image_arn" type="string">
  A string which is not empty or blank (only whitespace). Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot_build" type="object">
  &lt;p&gt;Contains size information about a MicroVM image snapshot build.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code_install_size_in_bytes" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="disk_snapshot_size_in_bytes" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="memory_snapshot_size_in_bytes" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state_reason" type="string">
  Returned by the lookup.
</ResponseField>
