---
title: "aws_lambda_microvms_managed_microvm_image_versions"
description: "`aws_lambda_microvms_managed_microvm_image_versions` reads a real, AWS-managed Lambda MicroVMs base image version."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_managed_microvm_image_versions"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsManagedMicrovmImageVersions, "example", lambda_microvms.LambdaMicrovmsManagedMicrovmImageVersionsConfig{
    			ImageIdentifier: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LambdaMicrovmsManagedMicrovmImageVersions } from "./local-sdk/aws/sdk/typescript/aws/data/lambda_microvms/lambda_microvms_managed_microvm_image_versions.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_lambda_microvms_managed_microvm_image_versions",
      });

      data(LambdaMicrovmsManagedMicrovmImageVersions, "example", {
        imageIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsManagedMicrovmImageVersions, LambdaMicrovmsManagedMicrovmImageVersionsConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_managed_microvm_image_versions")

        ubx.data(LambdaMicrovmsManagedMicrovmImageVersions, "example", LambdaMicrovmsManagedMicrovmImageVersionsConfig(
            image_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_identifier" type="string" required>
  The ARN or ID of the MicroVmImage Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
