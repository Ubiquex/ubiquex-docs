---
title: "aws_lambda_microvms_microvm_image_version"
description: "`aws_lambda_microvms_microvm_image_version` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_microvms_microvm_image_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_microvm_image_version"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsMicrovmImageVersion, "example", lambda_microvms.LambdaMicrovmsMicrovmImageVersionConfig{
    			ImageIdentifier: "example",
    			ImageVersion:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LambdaMicrovmsMicrovmImageVersion } from "@ubx/sdk-aws/aws/data/lambda_microvms/lambda_microvms_microvm_image_version";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_microvms_microvm_image_version" });

      data(LambdaMicrovmsMicrovmImageVersion, "example", {
        imageIdentifier: "example",
        imageVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsMicrovmImageVersion, LambdaMicrovmsMicrovmImageVersionConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_microvm_image_version")

        ubx.data(LambdaMicrovmsMicrovmImageVersion, "example", LambdaMicrovmsMicrovmImageVersionConfig(
            image_identifier="example",
            image_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_identifier" type="string" required>
  The ARN or ID of the MicroVmImage Required lookup argument.
</ResponseField>

<ResponseField name="image_version" type="string" required>
  A string which is not empty or blank (only whitespace). Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="additional_os_capabilities" type="list(string)">
  List of capabilities granted to the application when booted Returned by the lookup.
</ResponseField>

<ResponseField name="base_image_arn" type="string">
  A string which is not empty or blank (only whitespace). Returned by the lookup.
</ResponseField>

<ResponseField name="base_image_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="build_role_arn" type="string">
  The ARN of an IAM role that the service assumes to perform actions on behalf of the caller. Returned by the lookup.
</ResponseField>

<ResponseField name="code_artifact" type="object">
  &lt;p&gt;Contains the location of the code artifact for a MicroVM image.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="uri" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cpu_configurations" type="list(object)">
  List of CPU architectures Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="egress_network_connectors" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="environment_variables" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="hooks" type="object">
  &lt;p&gt;Lifecycle hook configuration for MicroVMs and MicroVM images.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="microvm_hooks" type="object">
      &lt;p&gt;Configuration for lifecycle hooks invoked during MicroVM events such as run, resume, suspend, and terminate.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="resume" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="resume_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="run" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="run_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="suspend" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="suspend_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="terminate" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="terminate_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="microvm_image_hooks" type="object">
      &lt;p&gt;Configuration for hooks invoked during MicroVM image build events such as ready and validate.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="ready" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="ready_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="validate" type="string">
          Whether invocation hooks are enabled or disabled on a MicroVm. Optional lookup argument.
        </ResponseField>
        <ResponseField name="validate_timeout_in_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_arn" type="string">
  A string which is not empty or blank (only whitespace). Returned by the lookup.
</ResponseField>

<ResponseField name="logging" type="object">
  &lt;p&gt;Configuration for MicroVM logging output. Specify exactly one: cloudWatch to enable CloudWatch logging, or disabled to turn off logging.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cloud_watch" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="log_group" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="log_stream" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="disabled" type="any">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resources" type="list(object)">
  List of resources Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Returned by the lookup.
</ResponseField>
