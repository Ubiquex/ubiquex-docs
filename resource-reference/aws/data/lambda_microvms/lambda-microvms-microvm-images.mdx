---
title: "aws_lambda_microvms_microvm_images"
description: "`aws_lambda_microvms_microvm_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_microvms_microvm_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_microvm_images"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsMicrovmImages, "example", lambda_microvms.LambdaMicrovmsMicrovmImagesConfig{
    			MaxResults: 1,
    			NameFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LambdaMicrovmsMicrovmImages } from "@ubx/sdk-aws/aws/data/lambda_microvms/lambda_microvms_microvm_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_microvms_microvm_images" });

      data(LambdaMicrovmsMicrovmImages, "example", {
        maxResults: 1,
        nameFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsMicrovmImages, LambdaMicrovmsMicrovmImagesConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_microvm_images")

        ubx.data(LambdaMicrovmsMicrovmImages, "example", LambdaMicrovmsMicrovmImagesConfig(
            max_results=1,
            name_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_filter" type="string">
  A string which is not empty or blank (only whitespace). Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  List of MicroVM image summaries Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
