---
title: "aws_lambda_microvms_tags"
description: "`aws_lambda_microvms_tags` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_microvms_tags` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda_microvms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda_microvms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_microvms_tags"})

    		ubx.Data(lambda_microvms.LambdaMicrovmsTags, "example", lambda_microvms.LambdaMicrovmsTagsConfig{
    			Resource: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LambdaMicrovmsTags } from "@ubx/sdk-aws/aws/data/lambda_microvms/lambda_microvms_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_microvms_tags" });

      data(LambdaMicrovmsTags, "example", {
        resource: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_microvms import LambdaMicrovmsTags, LambdaMicrovmsTagsConfig

    def describe():
        ubx.intent("look up aws_lambda_microvms_tags")

        ubx.data(LambdaMicrovmsTags, "example", LambdaMicrovmsTagsConfig(
            resource="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource" type="string" required>
  ARN of a taggable Lambda resource. Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
