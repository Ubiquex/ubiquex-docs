---
title: "aws_inspector2_cis_scan_result_details"
description: "`aws_inspector2_cis_scan_result_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector2_cis_scan_result_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_cis_scan_result_details"})

    		ubx.Data(inspector2.CisScanResultDetails, "example", inspector2.CisScanResultDetailsConfig{
    			AccountId:        "example",
    			ScanArn:          "arn:aws:iam::123456789012:policy/example",
    			TargetResourceId: "example",
    			FilterCriteria:   map[string]any{"check_id_filters": []map[string]any{{"comparison": "example"}}},
    			MaxResults:       1,
    			SortBy:           "example",
    			SortOrder:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CisScanResultDetails } from "@ubx/sdk-aws/aws/data/inspector2/cis_scan_result_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_cis_scan_result_details" });

      data(CisScanResultDetails, "example", {
        accountId: "example",
        scanArn: "arn:aws:iam::123456789012:policy/example",
        targetResourceId: "example",
        filterCriteria: { checkIdFilters: [{ comparison: "example" }] },
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import CisScanResultDetails, CisScanResultDetailsConfig

    def describe():
        ubx.intent("look up aws_inspector2_cis_scan_result_details")

        ubx.data(CisScanResultDetails, "example", CisScanResultDetailsConfig(
            account_id="example",
            scan_arn="arn:aws:iam::123456789012:policy/example",
            target_resource_id="example",
            filter_criteria={"check_id_filters": [{"comparison": "example"}]},
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_criteria" type="object">
  &lt;p&gt;The CIS scan result details filter criteria.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="check_id_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="finding_arn_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="finding_status_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="security_level_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="title_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scan_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target_resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_result_details" type="list(object)">
  Returned by the lookup.
</ResponseField>
