---
title: "aws_inspector2_delegated_admin_accounts"
description: "`aws_inspector2_delegated_admin_accounts` reads the real accounts designated as the Inspector delegated administrator."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_delegated_admin_accounts"})

    		ubx.Data(inspector2.DelegatedAdminAccounts, "example", inspector2.DelegatedAdminAccountsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DelegatedAdminAccounts } from "@ubx/sdk-aws/aws/data/inspector2/delegated_admin_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_delegated_admin_accounts" });

      data(DelegatedAdminAccounts, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import DelegatedAdminAccounts, DelegatedAdminAccountsConfig

    def describe():
        ubx.intent("look up aws_inspector2_delegated_admin_accounts")

        ubx.data(DelegatedAdminAccounts, "example", DelegatedAdminAccountsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delegated_admin_accounts" type="list(object)">
  The real, delegated administrator accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
