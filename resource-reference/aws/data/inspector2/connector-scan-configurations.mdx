---
title: "aws_inspector2_connector_scan_configurations"
description: "`aws_inspector2_connector_scan_configurations` reads the real scan configuration for an Inspector code security connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_connector_scan_configurations"})

    		ubx.Data(inspector2.ConnectorScanConfigurations, "example", inspector2.ConnectorScanConfigurationsConfig{
    			AwsConfigConnectorArns: []string{"example"},
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectorScanConfigurations } from "@ubx/sdk-aws/aws/data/inspector2/connector_scan_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_connector_scan_configurations" });

      data(ConnectorScanConfigurations, "example", {
        awsConfigConnectorArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import ConnectorScanConfigurations, ConnectorScanConfigurationsConfig

    def describe():
        ubx.intent("look up aws_inspector2_connector_scan_configurations")

        ubx.data(ConnectorScanConfigurations, "example", ConnectorScanConfigurationsConfig(
            aws_config_connector_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_config_connector_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the aws config connectors this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scan_configurations" type="list(object)">
  The real scan configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
