---
title: "aws_inspector2_clusters_for_image"
description: "`aws_inspector2_clusters_for_image` reads the real ECS or EKS clusters running a given container image scanned by Inspector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_clusters_for_image"})

    		ubx.Data(inspector2.ClustersForImage, "example", inspector2.ClustersForImageConfig{
    			Filter:     map[string]any{"resource_id": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClustersForImage } from "@ubx/sdk-aws/aws/data/inspector2/clusters_for_image";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_clusters_for_image" });

      data(ClustersForImage, "example", {
        filter: { resourceId: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import ClustersForImage, ClustersForImageConfig

    def describe():
        ubx.intent("look up aws_inspector2_clusters_for_image")

        ubx.data(ClustersForImage, "example", ClustersForImageConfig(
            filter={"resource_id": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object" required>
  &lt;p&gt;The filter criteria to be used.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="resource_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster" type="list(object)">
  The real cluster this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
