---
title: "aws_inspector2_cis_scan_report"
description: "`aws_inspector2_cis_scan_report` reads a real, generated report from an Inspector CIS benchmark scan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_cis_scan_report"})

    		ubx.Data(inspector2.CisScanReport, "example", inspector2.CisScanReportConfig{
    			ScanArn:        "arn:aws:iam::123456789012:policy/example",
    			ReportFormat:   "example",
    			TargetAccounts: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CisScanReport } from "@ubx/sdk-aws/aws/data/inspector2/cis_scan_report";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_cis_scan_report" });

      data(CisScanReport, "example", {
        scanArn: "arn:aws:iam::123456789012:policy/example",
        reportFormat: "example",
        targetAccounts: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import CisScanReport, CisScanReportConfig

    def describe():
        ubx.intent("look up aws_inspector2_cis_scan_report")

        ubx.data(CisScanReport, "example", CisScanReportConfig(
            scan_arn="arn:aws:iam::123456789012:policy/example",
            report_format="example",
            target_accounts=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="report_format" type="string">
  The real, requested format of the report. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scan_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the scan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="target_accounts" type="list(string)">
  The real target AWS accounts included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
