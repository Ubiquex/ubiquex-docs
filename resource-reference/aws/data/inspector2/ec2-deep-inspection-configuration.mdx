---
title: "aws_inspector2_ec2_deep_inspection_configuration"
description: "`aws_inspector2_ec2_deep_inspection_configuration` reads the real EC2 deep inspection configuration (package inventory scanning) for Inspector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_ec2_deep_inspection_configuration"})

    		ubx.Data(inspector2.Ec2DeepInspectionConfiguration, "example", inspector2.Ec2DeepInspectionConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Ec2DeepInspectionConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/inspector2/ec2_deep_inspection_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_inspector2_ec2_deep_inspection_configuration",
      });

      data(Ec2DeepInspectionConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import Ec2DeepInspectionConfiguration, Ec2DeepInspectionConfigurationConfig

    def describe():
        ubx.intent("look up aws_inspector2_ec2_deep_inspection_configuration")

        ubx.data(Ec2DeepInspectionConfiguration, "example", Ec2DeepInspectionConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="org_package_paths" type="list(string)">
  The real organization package paths included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="package_paths" type="list(string)">
  The real package paths included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
