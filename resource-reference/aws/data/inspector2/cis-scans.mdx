---
title: "aws_inspector2_cis_scans"
description: "`aws_inspector2_cis_scans` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector2_cis_scans` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_cis_scans"})

    		ubx.Data(inspector2.CisScans, "example", inspector2.CisScansConfig{
    			DetailLevel:    "example",
    			FilterCriteria: map[string]any{"failed_checks_filters": []map[string]any{{"lower_inclusive": 1}}},
    			MaxResults:     1,
    			SortBy:         "example",
    			SortOrder:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CisScans } from "@ubx/sdk-aws/aws/data/inspector2/cis_scans";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_cis_scans" });

      data(CisScans, "example", {
        detailLevel: "example",
        filterCriteria: { failedChecksFilters: [{ lowerInclusive: 1 }] },
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import CisScans, CisScansConfig

    def describe():
        ubx.intent("look up aws_inspector2_cis_scans")

        ubx.data(CisScans, "example", CisScansConfig(
            detail_level="example",
            filter_criteria={"failed_checks_filters": [{"lower_inclusive": 1}]},
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detail_level" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter_criteria" type="object">
  &lt;p&gt;A list of CIS scans filter criteria.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="failed_checks_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_arn_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_at_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_configuration_arn_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_name_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_status_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scheduled_by_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_account_id_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_resource_id_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_resource_tag_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scans" type="list(object)">
  Returned by the lookup.
</ResponseField>
