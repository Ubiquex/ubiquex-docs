---
title: "aws_inspector2_account_permissions"
description: "`aws_inspector2_account_permissions` reads real, account-level Inspector permission status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_account_permissions"})

    		ubx.Data(inspector2.AccountPermissions, "example", inspector2.AccountPermissionsConfig{
    			MaxResults: 1,
    			Service:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountPermissions } from "@ubx/sdk-aws/aws/data/inspector2/account_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector2_account_permissions" });

      data(AccountPermissions, "example", {
        maxResults: 1,
        service: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import AccountPermissions, AccountPermissionsConfig

    def describe():
        ubx.intent("look up aws_inspector2_account_permissions")

        ubx.data(AccountPermissions, "example", AccountPermissionsConfig(
            max_results=1,
            service="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
