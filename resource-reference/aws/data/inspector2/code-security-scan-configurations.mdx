---
title: "aws_inspector2_code_security_scan_configurations"
description: "`data_aws_inspector2_code_security_scan_configurations` looks up a real, already-existing `aws_inspector_v2_code_security_scan_configuration`."
---

It defines real scanning rules, applied by default to every new repository or scoped to specific ones by tag, under an `aws_inspector_v2_code_security_integration`, up to three default configurations per project.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_code_security_scan_configurations"})

    		ubx.Data(inspector2.CodeSecurityScanConfigurations, "example", inspector2.CodeSecurityScanConfigurationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodeSecurityScanConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/inspector2/code_security_scan_configurations.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_inspector2_code_security_scan_configurations",
      });

      data(CodeSecurityScanConfigurations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import CodeSecurityScanConfigurations, CodeSecurityScanConfigurationsConfig

    def describe():
        ubx.intent("look up aws_inspector2_code_security_scan_configurations")

        ubx.data(CodeSecurityScanConfigurations, "example", CodeSecurityScanConfigurationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configurations" type="list(object)">
  The real configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
