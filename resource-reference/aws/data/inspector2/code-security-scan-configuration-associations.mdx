---
title: "aws_inspector2_code_security_scan_configuration_associations"
description: "`aws_inspector2_code_security_scan_configuration_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector2_code_security_scan_configuration_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector2_code_security_scan_configuration_associations"})

    		ubx.Data(inspector2.CodeSecurityScanConfigurationAssociations, "example", inspector2.CodeSecurityScanConfigurationAssociationsConfig{
    			ScanConfigurationArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeSecurityScanConfigurationAssociations } from "@ubx/sdk-aws/aws/data/inspector2/code_security_scan_configuration_associations";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_inspector2_code_security_scan_configuration_associations",
      });

      data(CodeSecurityScanConfigurationAssociations, "example", {
        scanConfigurationArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector2 import CodeSecurityScanConfigurationAssociations, CodeSecurityScanConfigurationAssociationsConfig

    def describe():
        ubx.intent("look up aws_inspector2_code_security_scan_configuration_associations")

        ubx.data(CodeSecurityScanConfigurationAssociations, "example", CodeSecurityScanConfigurationAssociationsConfig(
            scan_configuration_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scan_configuration_arn" type="string" required>
  arn:aws:inspector2:&lt;region&gt;:&lt;account-id&gt;:owner/&lt;owner-id&gt;/codesecurity-configuration/&lt;uuid&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
