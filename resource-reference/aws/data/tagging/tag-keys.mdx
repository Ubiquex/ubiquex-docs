---
title: "aws_tagging_tag_keys"
description: "`aws_tagging_tag_keys` reads the real, distinct tag keys currently in use across the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_tag_keys"})

    		ubx.Data(tagging.TagKeys, "example", tagging.TagKeysConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagKeys } from "@ubx/sdk-aws/aws/data/tagging/tag_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_tag_keys" });

      data(TagKeys, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import TagKeys, TagKeysConfig

    def describe():
        ubx.intent("look up aws_tagging_tag_keys")

        ubx.data(TagKeys, "example", TagKeysConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="pagination_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_keys" type="list(string)">
  The real, unique keys of the tags included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
