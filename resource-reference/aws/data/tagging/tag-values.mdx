---
title: "aws_tagging_tag_values"
description: "`aws_tagging_tag_values` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_tagging_tag_values` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_tag_values"})

    		ubx.Data(tagging.TagValues, "example", tagging.TagValuesConfig{
    			Key: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagValues } from "@ubx/sdk-aws/aws/data/tagging/tag_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_tag_values" });

      data(TagValues, "example", {
        key: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import TagValues, TagValuesConfig

    def describe():
        ubx.intent("look up aws_tagging_tag_values")

        ubx.data(TagValues, "example", TagValuesConfig(
            key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pagination_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tag_values" type="list(string)">
  Returned by the lookup.
</ResponseField>
