---
title: "aws_tagging_tag_values"
description: "`aws_tagging_tag_values` reads the real, distinct values used for a given tag key across the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_tag_values"})

    		ubx.Data(tagging.TagValues, "example", tagging.TagValuesConfig{
    			Key: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TagValues } from "./local-sdk/aws/sdk/typescript/aws/data/tagging/tag_values.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_tag_values" });

      data(TagValues, "example", {
        key: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import TagValues, TagValuesConfig

    def describe():
        ubx.intent("look up aws_tagging_tag_values")

        ubx.data(TagValues, "example", TagValuesConfig(
            key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key" type="string" required>
  The real, unique key of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pagination_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_values" type="list(string)">
  The real values of the tags included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
