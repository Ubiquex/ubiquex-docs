---
title: "aws_tagging_report_creation"
description: "`aws_tagging_report_creation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_tagging_report_creation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_report_creation"})

    		ubx.Data(tagging.ReportCreation, "example", tagging.ReportCreationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReportCreation } from "@ubx/sdk-aws/aws/data/tagging/report_creation";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_report_creation" });

      data(ReportCreation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import ReportCreation, ReportCreationConfig

    def describe():
        ubx.intent("look up aws_tagging_report_creation")

        ubx.data(ReportCreation, "example", ReportCreationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="error_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="s3_location" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
