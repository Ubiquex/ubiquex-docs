---
title: "aws_tagging_report_creation"
description: "`aws_tagging_report_creation` reads the real, current status of generating a tag policy compliance report."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_report_creation"})

    		ubx.Data(tagging.ReportCreation, "example", tagging.ReportCreationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReportCreation } from "@ubx/sdk-aws/aws/data/tagging/report_creation";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_report_creation" });

      data(ReportCreation, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import ReportCreation, ReportCreationConfig

    def describe():
        ubx.intent("look up aws_tagging_report_creation")

        ubx.data(ReportCreation, "example", ReportCreationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_location" type="string">
  The real S3 location of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_date" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
