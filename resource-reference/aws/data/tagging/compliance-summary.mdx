---
title: "aws_tagging_compliance_summary"
description: "`aws_tagging_compliance_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_tagging_compliance_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tagging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tagging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tagging_compliance_summary"})

    		ubx.Data(tagging.ComplianceSummary, "example", tagging.ComplianceSummaryConfig{
    			GroupBy:             []string{"example"},
    			MaxResults:          1,
    			RegionFilters:       []string{"example"},
    			ResourceTypeFilters: []string{"example"},
    			TagKeyFilters:       []string{"example"},
    			TargetIdFilters:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceSummary } from "@ubx/sdk-aws/aws/data/tagging/compliance_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_tagging_compliance_summary" });

      data(ComplianceSummary, "example", {
        groupBy: ["example"],
        maxResults: 1,
        regionFilters: ["example"],
        resourceTypeFilters: ["example"],
        tagKeyFilters: ["example"],
        targetIdFilters: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tagging import ComplianceSummary, ComplianceSummaryConfig

    def describe():
        ubx.intent("look up aws_tagging_compliance_summary")

        ubx.data(ComplianceSummary, "example", ComplianceSummaryConfig(
            group_by=["example"],
            max_results=1,
            region_filters=["example"],
            resource_type_filters=["example"],
            tag_key_filters=["example"],
            target_id_filters=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_by" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="region_filters" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type_filters" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tag_key_filters" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target_id_filters" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pagination_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
