---
title: "aws_api_detective_investigation"
description: "`aws_api_detective_investigation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_detective_investigation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_detective_investigation"})

    		ubx.Data(api_detective.ApiDetectiveInvestigation, "example", api_detective.ApiDetectiveInvestigationConfig{
    			GraphArn:        "arn:aws:iam::123456789012:policy/example",
    			InvestigationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiDetectiveInvestigation } from "@ubx/sdk-aws/aws/data/api_detective/api_detective_investigation";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_detective_investigation" });

      data(ApiDetectiveInvestigation, "example", {
        graphArn: "arn:aws:iam::123456789012:policy/example",
        investigationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_detective import ApiDetectiveInvestigation, ApiDetectiveInvestigationConfig

    def describe():
        ubx.intent("look up aws_api_detective_investigation")

        ubx.data(ApiDetectiveInvestigation, "example", ApiDetectiveInvestigationConfig(
            graph_arn="arn:aws:iam::123456789012:policy/example",
            investigation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="graph_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="investigation_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="entity_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="entity_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scope_end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scope_start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="severity" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
