---
title: "aws_api_detective_indicators"
description: "`aws_api_detective_indicators` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_detective_indicators` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_detective_indicators"})

    		ubx.Data(api_detective.ApiDetectiveIndicators, "example", api_detective.ApiDetectiveIndicatorsConfig{
    			GraphArn:        "arn:aws:iam::123456789012:policy/example",
    			InvestigationId: "example",
    			IndicatorType:   "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiDetectiveIndicators } from "@ubx/sdk-aws/aws/data/api_detective/api_detective_indicators";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_detective_indicators" });

      data(ApiDetectiveIndicators, "example", {
        graphArn: "arn:aws:iam::123456789012:policy/example",
        investigationId: "example",
        indicatorType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_detective import ApiDetectiveIndicators, ApiDetectiveIndicatorsConfig

    def describe():
        ubx.intent("look up aws_api_detective_indicators")

        ubx.data(ApiDetectiveIndicators, "example", ApiDetectiveIndicatorsConfig(
            graph_arn="arn:aws:iam::123456789012:policy/example",
            investigation_id="example",
            indicator_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="graph_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="indicator_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="investigation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indicators" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
