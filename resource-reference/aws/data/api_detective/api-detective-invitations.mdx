---
title: "aws_api_detective_invitations"
description: "`aws_api_detective_invitations` reads the real, pending Detective member-account invitations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_detective_invitations"})

    		ubx.Data(api_detective.ApiDetectiveInvitations, "example", api_detective.ApiDetectiveInvitationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiDetectiveInvitations } from "@ubx/sdk-aws/aws/data/api_detective/api_detective_invitations";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_detective_invitations" });

      data(ApiDetectiveInvitations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_detective import ApiDetectiveInvitations, ApiDetectiveInvitationsConfig

    def describe():
        ubx.intent("look up aws_api_detective_invitations")

        ubx.data(ApiDetectiveInvitations, "example", ApiDetectiveInvitationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="invitations" type="list(object)">
  The real invitations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
