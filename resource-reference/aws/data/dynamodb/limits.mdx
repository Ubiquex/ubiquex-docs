---
title: "aws_dynamodb_limits"
description: "`aws_dynamodb_limits` reads real, account-level DynamoDB throughput limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dynamodb_limits"})

    		ubx.Data(dynamodb.Limits, "example", dynamodb.LimitsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Limits } from "@ubx/sdk-aws/aws/data/dynamodb/limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_dynamodb_limits" });

      data(Limits, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dynamodb import Limits, LimitsConfig

    def describe():
        ubx.intent("look up aws_dynamodb_limits")

        ubx.data(Limits, "example", LimitsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_max_read_capacity_units" type="number">
  The real, maximum read capacity units allowed for this account. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="account_max_write_capacity_units" type="number">
  The real, maximum write capacity units allowed for this account. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="table_max_read_capacity_units" type="number">
  The real, maximum read capacity units allowed for this table. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="table_max_write_capacity_units" type="number">
  The real, maximum write capacity units allowed for this table. **(AI-inferred)** Returned by the lookup.
</ResponseField>
