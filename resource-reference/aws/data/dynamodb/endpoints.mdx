---
title: "aws_dynamodb_endpoints"
description: "`aws_dynamodb_endpoints` reads DynamoDB's own real, published service endpoints across Regions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dynamodb_endpoints"})

    		ubx.Data(dynamodb.Endpoints, "example", dynamodb.EndpointsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Endpoints } from "@ubx/sdk-aws/aws/data/dynamodb/endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_dynamodb_endpoints" });

      data(Endpoints, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dynamodb import Endpoints, EndpointsConfig

    def describe():
        ubx.intent("look up aws_dynamodb_endpoints")

        ubx.data(Endpoints, "example", EndpointsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="endpoints" type="list(object)">
  The real endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
