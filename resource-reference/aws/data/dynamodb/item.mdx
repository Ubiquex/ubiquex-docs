---
title: "aws_dynamodb_item"
description: "`aws_dynamodb_item` looks up one real item stored in a DynamoDB table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dynamodb_item"})

    		ubx.Data(dynamodb.Item, "example", dynamodb.ItemConfig{
    			Key:                      map[string]any{"key": map[string]any{"b": "example"}},
    			TableName:                "example-table-name",
    			AttributesToGet:          []string{"example"},
    			ConsistentRead:           true,
    			ExpressionAttributeNames: map[string]string{"managed-by": "ubx"},
    			ProjectionExpression:     "example",
    			ReturnConsumedCapacity:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Item } from "@ubx/sdk-aws/aws/data/dynamodb/item";

    export default stack("example", () => {
      intent({ summary: "look up aws_dynamodb_item" });

      data(Item, "example", {
        key: { key: { b: "example" } },
        tableName: "example-table-name",
        attributesToGet: ["example"],
        consistentRead: true,
        expressionAttributeNames: { "managed-by": "ubx" },
        projectionExpression: "example",
        returnConsumedCapacity: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dynamodb import Item, ItemConfig
    from ubx.aws.data.dynamodb.item import Item_Item

    def describe():
        ubx.intent("look up aws_dynamodb_item")

        ubx.data(Item, "example", ItemConfig(
            key={"key": Item_Item(b="example")},
            table_name="example-table-name",
            attributes_to_get=["example"],
            consistent_read=True,
            expression_attribute_names={"managed-by": "ubx"},
            projection_expression="example",
            return_consumed_capacity="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes_to_get" type="list(string)">
  Restricts the response to only these attributes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="consistent_read" type="bool">
  Whether this request uses strongly consistent reads. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="expression_attribute_names" type="map(string)">
  The real names of the expression attributes included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="map(object)" required>
  The real, unique key of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="projection_expression" type="string">
  The real expression identifying the attributes to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="return_consumed_capacity" type="string">
  &lt;p&gt;Determines the level of detail about either provisioned or on-demand throughput consumption that is returned in the response:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;INDEXES&lt;/code&gt; - The response includes the aggregate &lt;code&gt;ConsumedCapacity&lt;/code&gt; for the operation, together with &lt;code&gt;ConsumedCapacity&lt;/code&gt; for each table and secondary index that was accessed.&lt;/p&gt; &lt;p&gt;Note that some operations, such as &lt;code&gt;GetItem&lt;/code&gt; and &lt;code&gt;BatchGetItem&lt;/code&gt;, do not access any indexes at all. In these cases, specifying &lt;code&gt;INDEXES&lt;/code&gt; will only return &lt;code&gt;ConsumedCapacity&lt;/code&gt; information for table(s).&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;TOTAL&lt;/code&gt; - The response includes only the aggregate &lt;code&gt;ConsumedCapacity&lt;/code&gt; for the operation.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;NONE&lt;/code&gt; - No &lt;code&gt;ConsumedCapacity&lt;/code&gt; details are included in the response.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="consumed_capacity" type="object">
  &lt;p&gt;The capacity units consumed by an operation. The data returned includes the total provisioned throughput consumed, along with statistics for the table and any indexes involved in the operation. &lt;code&gt;ConsumedCapacity&lt;/code&gt; is only returned if the request asked for it. For more information, see &lt;a href="https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html"&gt;Provisioned capacity mode&lt;/a&gt; in the &lt;i&gt;Amazon DynamoDB Developer Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="capacity_units" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="global_secondary_indexes" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="local_secondary_indexes" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="read_capacity_units" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="table" type="object">
      &lt;p&gt;Represents the amount of provisioned throughput capacity consumed on a table or an index.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="capacity_units" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="read_capacity_units" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="write_capacity_units" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="table_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vector_indexes" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="write_capacity_units" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="item" type="map(object)">
  The real item this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
