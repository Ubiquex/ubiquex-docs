---
title: "aws_dynamodb_kinesis_streaming_destination"
description: "`aws_dynamodb_kinesis_streaming_destination` reads the real Kinesis Data Streams destination a DynamoDB table replicates its own item-level changes to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dynamodb_kinesis_streaming_destination"})

    		ubx.Data(dynamodb.KinesisStreamingDestination, "example", dynamodb.KinesisStreamingDestinationConfig{
    			TableName: "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KinesisStreamingDestination } from "@ubx/sdk-aws/aws/data/dynamodb/kinesis_streaming_destination";

    export default stack("example", () => {
      intent({ summary: "look up aws_dynamodb_kinesis_streaming_destination" });

      data(KinesisStreamingDestination, "example", {
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dynamodb import KinesisStreamingDestination, KinesisStreamingDestinationConfig

    def describe():
        ubx.intent("look up aws_dynamodb_kinesis_streaming_destination")

        ubx.data(KinesisStreamingDestination, "example", KinesisStreamingDestinationConfig(
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kinesis_data_stream_destinations" type="list(object)">
  The real Kinesis Data Streams destinations configured for this table. **(AI-inferred)** Returned by the lookup.
</ResponseField>
