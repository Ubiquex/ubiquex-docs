---
title: "aws_dataexchange_data_set_revisions"
description: "`aws_dataexchange_data_set_revisions` reads a real, published revision of a Data Exchange dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_data_set_revisions"})

    		ubx.Data(dataexchange.DataSetRevisions, "example", dataexchange.DataSetRevisionsConfig{
    			DataSetId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSetRevisions } from "@ubx/sdk-aws/aws/data/dataexchange/data_set_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_data_set_revisions" });

      data(DataSetRevisions, "example", {
        dataSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import DataSetRevisions, DataSetRevisionsConfig

    def describe():
        ubx.intent("look up aws_dataexchange_data_set_revisions")

        ubx.data(DataSetRevisions, "example", DataSetRevisionsConfig(
            data_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_set_id" type="string" required>
  The real, unique identifier of the data set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revisions" type="list(object)">
  The real revisions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
