---
title: "aws_dataexchange_data_sets"
description: "`data_aws_dataexchange_data_sets` looks up a real, already-existing `aws_data_exchange_data_set`."
---

It is the real, curated resource a data provider assembles in AWS Data Exchange, files, an API, Redshift, S3, or Lake Formation data, that a receiver gains access to once they accept a real data grant or subscribe to a Marketplace data product built around it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_data_sets"})

    		ubx.Data(dataexchange.DataSets, "example", dataexchange.DataSetsConfig{
    			MaxResults: 1,
    			Origin:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataSets } from "./local-sdk/aws/sdk/typescript/aws/data/dataexchange/data_sets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_data_sets" });

      data(DataSets, "example", {
        maxResults: 1,
        origin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import DataSets, DataSetsConfig

    def describe():
        ubx.intent("look up aws_dataexchange_data_sets")

        ubx.data(DataSets, "example", DataSetsConfig(
            max_results=1,
            origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="origin" type="string">
  The real origin of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_sets" type="list(object)">
  The real data sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
