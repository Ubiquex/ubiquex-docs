---
title: "aws_dataexchange_received_data_grant"
description: "`aws_dataexchange_received_data_grant` looks up one real Data Exchange data grant received from another account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_received_data_grant"})

    		ubx.Data(dataexchange.ReceivedDataGrant, "example", dataexchange.ReceivedDataGrantConfig{
    			DataGrantArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReceivedDataGrant } from "@ubx/sdk-aws/aws/data/dataexchange/received_data_grant";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_received_data_grant" });

      data(ReceivedDataGrant, "example", {
        dataGrantArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import ReceivedDataGrant, ReceivedDataGrantConfig

    def describe():
        ubx.intent("look up aws_dataexchange_received_data_grant")

        ubx.data(ReceivedDataGrant, "example", ReceivedDataGrantConfig(
            data_grant_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_grant_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the data grant this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acceptance_state" type="string">
  The real, current state of the acceptance this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="accepted_at" type="string">
  The real timestamp this object was accepted. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_set_id" type="string">
  The real, unique identifier of the data set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ends_at" type="string">
  The real timestamp this object ends. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="grant_distribution_scope" type="string">
  The real scope this grant's own distribution applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="receiver_principal" type="string">
  The real IAM principal receiving this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sender_principal" type="string">
  The real IAM principal sending this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
