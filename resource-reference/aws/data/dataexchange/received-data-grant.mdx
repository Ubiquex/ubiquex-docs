---
title: "aws_dataexchange_received_data_grant"
description: "`aws_dataexchange_received_data_grant` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_dataexchange_received_data_grant` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_received_data_grant"})

    		ubx.Data(dataexchange.ReceivedDataGrant, "example", dataexchange.ReceivedDataGrantConfig{
    			DataGrantArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReceivedDataGrant } from "@ubx/sdk-aws/aws/data/dataexchange/received_data_grant";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_received_data_grant" });

      data(ReceivedDataGrant, "example", {
        dataGrantArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import ReceivedDataGrant, ReceivedDataGrantConfig

    def describe():
        ubx.intent("look up aws_dataexchange_received_data_grant")

        ubx.data(ReceivedDataGrant, "example", ReceivedDataGrantConfig(
            data_grant_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_grant_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acceptance_state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="accepted_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="data_set_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ends_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="grant_distribution_scope" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="receiver_principal" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sender_principal" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Returned by the lookup.
</ResponseField>
