---
title: "aws_dataexchange_received_data_grants"
description: "`aws_dataexchange_received_data_grants` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_dataexchange_received_data_grants` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_received_data_grants"})

    		ubx.Data(dataexchange.ReceivedDataGrants, "example", dataexchange.ReceivedDataGrantsConfig{
    			AcceptanceState: []string{"example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReceivedDataGrants } from "@ubx/sdk-aws/aws/data/dataexchange/received_data_grants";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_received_data_grants" });

      data(ReceivedDataGrants, "example", {
        acceptanceState: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import ReceivedDataGrants, ReceivedDataGrantsConfig

    def describe():
        ubx.intent("look up aws_dataexchange_received_data_grants")

        ubx.data(ReceivedDataGrants, "example", ReceivedDataGrantsConfig(
            acceptance_state=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="acceptance_state" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_grant_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
