---
title: "aws_dataexchange_revision_assets"
description: "`aws_dataexchange_revision_assets` reads the real assets within a Data Exchange dataset revision."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dataexchange_revision_assets"})

    		ubx.Data(dataexchange.RevisionAssets, "example", dataexchange.RevisionAssetsConfig{
    			DataSetId:  "example",
    			RevisionId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RevisionAssets } from "./local-sdk/aws/sdk/typescript/aws/data/dataexchange/revision_assets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dataexchange_revision_assets" });

      data(RevisionAssets, "example", {
        dataSetId: "example",
        revisionId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dataexchange import RevisionAssets, RevisionAssetsConfig

    def describe():
        ubx.intent("look up aws_dataexchange_revision_assets")

        ubx.data(RevisionAssets, "example", RevisionAssetsConfig(
            data_set_id="example",
            revision_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_set_id" type="string" required>
  The real, unique identifier of the data set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="revision_id" type="string" required>
  The real, unique identifier of this object's own revision. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assets" type="list(object)">
  The real assets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
