---
title: "aws_cloudformation_stack_instances"
description: "`aws_cloudformation_stack_instances` looks up one real stack instance, a per-account, per-Region deployment of a CloudFormation StackSet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_instances"})

    		ubx.Data(cloudformation.StackInstances, "example", cloudformation.StackInstancesConfig{
    			StackSetName:         "example-stack-set-name",
    			CallAs:               "example",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxResults:           1,
    			StackInstanceAccount: "example",
    			StackInstanceRegion:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StackInstances } from "@ubx/sdk-aws/aws/data/cloudformation/stack_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_stack_instances" });

      data(StackInstances, "example", {
        stackSetName: "example-stack-set-name",
        callAs: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        stackInstanceAccount: "example",
        stackInstanceRegion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackInstances, StackInstancesConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_instances")

        ubx.data(StackInstances, "example", StackInstancesConfig(
            stack_set_name="example-stack-set-name",
            call_as="example",
            filters=[{"name": "example-name"}],
            max_results=1,
            stack_instance_account="example",
            stack_instance_region="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="call_as" type="string">
  The real AWS Organizations identity (self or delegated administrator) this request is made as. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_instance_account" type="string">
  The real AWS account this CloudFormation StackSet instance is deployed to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_instance_region" type="string">
  The real AWS region this CloudFormation StackSet instance is deployed to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_set_name" type="string" required>
  The real name of the CloudFormation stack set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
