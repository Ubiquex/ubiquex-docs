---
title: "aws_cloudformation_types"
description: "`aws_cloudformation_types` reads the real resource types and modules registered in the CloudFormation Registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_types"})

    		ubx.Data(cloudformation.Types, "example", cloudformation.TypesConfig{
    			DeprecatedStatus: "example",
    			Filters:          map[string]any{"category": "example"},
    			MaxResults:       1,
    			ProvisioningType: "example",
    			Type:             "example",
    			Visibility:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Types } from "@ubx/sdk-aws/aws/data/cloudformation/types";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_types" });

      data(Types, "example", {
        deprecatedStatus: "example",
        filters: { category: "example" },
        maxResults: 1,
        provisioningType: "example",
        type: "example",
        visibility: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import Types, TypesConfig

    def describe():
        ubx.intent("look up aws_cloudformation_types")

        ubx.data(Types, "example", TypesConfig(
            deprecated_status="example",
            filters={"category": "example"},
            max_results=1,
            provisioning_type="example",
            type="example",
            visibility="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deprecated_status" type="string">
  The real, current status of the deprecated this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;Filter criteria to use in determining which extensions to return.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="category" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="publisher_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type_name_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioning_type" type="string">
  The real, discriminating type of the provisioning this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="visibility" type="string">
  The real visibility (such as public or private) of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_summaries" type="list(object)">
  The real, summarized types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
