---
title: "aws_cloudformation_organizations_access"
description: "`aws_cloudformation_organizations_access` reads whether real CloudFormation StackSets trusted access with AWS Organizations is currently enabled."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_organizations_access"})

    		ubx.Data(cloudformation.OrganizationsAccess, "example", cloudformation.OrganizationsAccessConfig{
    			CallAs: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationsAccess } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/organizations_access.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_organizations_access" });

      data(OrganizationsAccess, "example", {
        callAs: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import OrganizationsAccess, OrganizationsAccessConfig

    def describe():
        ubx.intent("look up aws_cloudformation_organizations_access")

        ubx.data(OrganizationsAccess, "example", OrganizationsAccessConfig(
            call_as="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="call_as" type="string">
  The real AWS Organizations identity (self or delegated administrator) this request is made as. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
