---
title: "aws_cloudformation_publisher"
description: "`aws_cloudformation_publisher` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_publisher` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_publisher"})

    		ubx.Data(cloudformation.Publisher, "example", cloudformation.PublisherConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Publisher } from "@ubx/sdk-aws/aws/data/cloudformation/publisher";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_publisher" });

      data(Publisher, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import Publisher, PublisherConfig

    def describe():
        ubx.intent("look up aws_cloudformation_publisher")

        ubx.data(Publisher, "example", PublisherConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="identity_provider" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="publisher_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="publisher_profile" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="publisher_status" type="string">
  Returned by the lookup.
</ResponseField>
