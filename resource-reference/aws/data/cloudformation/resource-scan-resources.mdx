---
title: "aws_cloudformation_resource_scan_resources"
description: "`aws_cloudformation_resource_scan_resources` reads the real resources discovered by a CloudFormation resource scan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_resource_scan_resources"})

    		ubx.Data(cloudformation.ResourceScanResources, "example", cloudformation.ResourceScanResourcesConfig{
    			ResourceScanId:     "example",
    			MaxResults:         1,
    			ResourceIdentifier: "example",
    			ResourceTypePrefix: "example",
    			TagKey:             "example",
    			TagValue:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceScanResources } from "@ubx/sdk-aws/aws/data/cloudformation/resource_scan_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_resource_scan_resources" });

      data(ResourceScanResources, "example", {
        resourceScanId: "example",
        maxResults: 1,
        resourceIdentifier: "example",
        resourceTypePrefix: "example",
        tagKey: "example",
        tagValue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import ResourceScanResources, ResourceScanResourcesConfig

    def describe():
        ubx.intent("look up aws_cloudformation_resource_scan_resources")

        ubx.data(ResourceScanResources, "example", ResourceScanResourcesConfig(
            resource_scan_id="example",
            max_results=1,
            resource_identifier="example",
            resource_type_prefix="example",
            tag_key="example",
            tag_value="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_scan_id" type="string" required>
  The real, unique identifier of the resource scan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type_prefix" type="string">
  The real S3 key prefix used for the resource type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tag_key" type="string">
  The real, unique key of the tag this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tag_value" type="string">
  The real value of the tag this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The real AWS resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
