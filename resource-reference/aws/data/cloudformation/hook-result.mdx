---
title: "aws_cloudformation_hook_result"
description: "`aws_cloudformation_hook_result` reads the real result of a CloudFormation Hook invocation, evaluated during a stack operation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_hook_result"})

    		ubx.Data(cloudformation.HookResult, "example", cloudformation.HookResultConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HookResult } from "@ubx/sdk-aws/aws/data/cloudformation/hook_result";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_hook_result" });

      data(HookResult, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import HookResult, HookResultConfig

    def describe():
        ubx.intent("look up aws_cloudformation_hook_result")

        ubx.data(HookResult, "example", HookResultConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="annotations" type="list(object)">
  The real annotations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_mode" type="string">
  The real mode this object failed in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hook_result_id" type="string">
  The real, unique identifier of the hook result this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hook_status_reason" type="string">
  The real, human-readable reason for the hook status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="invocation_point" type="string">
  The real invocation point. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="invoked_at" type="string">
  The real timestamp this object was invoked. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="original_type_name" type="string">
  The real name of the original type this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target" type="object">
  &lt;p&gt;The &lt;code&gt;HookTarget&lt;/code&gt; data type.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="action" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_type_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type_arn" type="string">
  The real Amazon Resource Name (ARN) of this object's own type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_configuration_version_id" type="string">
  The real, unique identifier of the type configuration version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_name" type="string">
  The real name of this object's own type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_version_id" type="string">
  The real, unique identifier of the type version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
