---
title: "aws_cloudformation_template_summary"
description: "`aws_cloudformation_template_summary` reads a real, computed summary of a CloudFormation template's own resources, parameters, and capabilities."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_template_summary"})

    		ubx.Data(cloudformation.TemplateSummary, "example", cloudformation.TemplateSummaryConfig{
    			CallAs:                "example",
    			StackName:             "example-stack-name",
    			StackSetName:          "example-stack-set-name",
    			TemplateBody:          "example",
    			TemplateSummaryConfig: map[string]any{"treat_unrecognized_resource_types_as_warnings": true},
    			TemplateUrl:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TemplateSummary } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/template_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_template_summary" });

      data(TemplateSummary, "example", {
        callAs: "example",
        stackName: "example-stack-name",
        stackSetName: "example-stack-set-name",
        templateBody: "example",
        templateSummaryConfig: { treatUnrecognizedResourceTypesAsWarnings: true },
        templateUrl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import TemplateSummary, TemplateSummaryConfig

    def describe():
        ubx.intent("look up aws_cloudformation_template_summary")

        ubx.data(TemplateSummary, "example", TemplateSummaryConfig(
            call_as="example",
            stack_name="example-stack-name",
            stack_set_name="example-stack-set-name",
            template_body="example",
            template_summary_config={"treat_unrecognized_resource_types_as_warnings": True},
            template_url="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="call_as" type="string">
  The real AWS Organizations identity (self or delegated administrator) this request is made as. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_name" type="string">
  The real name of the CloudFormation stack this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_set_name" type="string">
  The real name of the CloudFormation stack set this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_body" type="string">
  The real, raw body of the template. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_summary_config" type="object">
  &lt;p&gt;Options for the &lt;code&gt;GetTemplateSummary&lt;/code&gt; API action.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="treat_unrecognized_resource_types_as_warnings" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="template_url" type="string">
  The real, resolvable URL of the template this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capabilities" type="list(string)">
  The real capabilities of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="capabilities_reason" type="string">
  The real, human-readable reason for the capabilities. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="declared_transforms" type="list(string)">
  The real, declared data transforms for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="string">
  The real, structured metadata of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_identifier_summaries" type="list(object)">
  The real, summarized resource identifiers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="object">
  &lt;p&gt;Contains any warnings returned by the &lt;code&gt;GetTemplateSummary&lt;/code&gt; API action.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="unrecognized_resource_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
