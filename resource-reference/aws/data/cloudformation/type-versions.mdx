---
title: "aws_cloudformation_type_versions"
description: "`aws_cloudformation_type_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_type_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_type_versions"})

    		ubx.Data(cloudformation.TypeVersions, "example", cloudformation.TypeVersionsConfig{
    			Arn:              "arn:aws:iam::123456789012:policy/example",
    			DeprecatedStatus: "example",
    			MaxResults:       1,
    			PublisherId:      "example",
    			Type:             "example",
    			TypeName:         "example-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TypeVersions } from "@ubx/sdk-aws/aws/data/cloudformation/type_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_type_versions" });

      data(TypeVersions, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        deprecatedStatus: "example",
        maxResults: 1,
        publisherId: "example",
        type: "example",
        typeName: "example-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import TypeVersions, TypeVersionsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_type_versions")

        ubx.data(TypeVersions, "example", TypeVersionsConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            deprecated_status="example",
            max_results=1,
            publisher_id="example",
            type="example",
            type_name="example-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="deprecated_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="publisher_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type_version_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
