---
title: "aws_cloudformation_generated_templates"
description: "`data_aws_cloudformation_generated_templates` looks up a real, already-existing `aws_cloud_formation_generated_template`."
---

It produces a real CloudFormation template generated FROM a set of existing, already-deployed resources, CloudFormation's own IaC-generator feature for bringing unmanaged infrastructure under an `aws_cloud_formation_stack` without hand-writing the template first.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_generated_templates"})

    		ubx.Data(cloudformation.GeneratedTemplates, "example", cloudformation.GeneratedTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GeneratedTemplates } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/generated_templates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_generated_templates" });

      data(GeneratedTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import GeneratedTemplates, GeneratedTemplatesConfig

    def describe():
        ubx.intent("look up aws_cloudformation_generated_templates")

        ubx.data(GeneratedTemplates, "example", GeneratedTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
