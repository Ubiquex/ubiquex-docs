---
title: "aws_cloudformation_stack_resource_drifts"
description: "`aws_cloudformation_stack_resource_drifts` reads the real, detected drift between a CloudFormation stack's own template and each resource's real, live..."
---

`aws_cloudformation_stack_resource_drifts` reads the real, detected drift between a CloudFormation stack's own template and each resource's real, live configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_resource_drifts"})

    		ubx.Data(cloudformation.StackResourceDrifts, "example", cloudformation.StackResourceDriftsConfig{
    			StackName:                       "example-stack-name",
    			MaxResults:                      1,
    			StackResourceDriftStatusFilters: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StackResourceDrifts } from "@ubx/sdk-aws/aws/data/cloudformation/stack_resource_drifts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_stack_resource_drifts" });

      data(StackResourceDrifts, "example", {
        stackName: "example-stack-name",
        maxResults: 1,
        stackResourceDriftStatusFilters: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackResourceDrifts, StackResourceDriftsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_resource_drifts")

        ubx.data(StackResourceDrifts, "example", StackResourceDriftsConfig(
            stack_name="example-stack-name",
            max_results=1,
            stack_resource_drift_status_filters=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  The real name of the CloudFormation stack this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stack_resource_drift_status_filters" type="list(string)">
  Restricts results to these CloudFormation stack resource drift statuses. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stack_resource_drifts" type="list(object)">
  The real CloudFormation stack resource drifts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
