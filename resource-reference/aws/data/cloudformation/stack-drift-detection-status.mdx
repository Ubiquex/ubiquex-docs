---
title: "aws_cloudformation_stack_drift_detection_status"
description: "`aws_cloudformation_stack_drift_detection_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_stack_drift_detection_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_drift_detection_status"})

    		ubx.Data(cloudformation.StackDriftDetectionStatus, "example", cloudformation.StackDriftDetectionStatusConfig{
    			StackDriftDetectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StackDriftDetectionStatus } from "@ubx/sdk-aws/aws/data/cloudformation/stack_drift_detection_status";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudformation_stack_drift_detection_status",
      });

      data(StackDriftDetectionStatus, "example", {
        stackDriftDetectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackDriftDetectionStatus, StackDriftDetectionStatusConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_drift_detection_status")

        ubx.data(StackDriftDetectionStatus, "example", StackDriftDetectionStatusConfig(
            stack_drift_detection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="stack_drift_detection_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="detection_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="detection_status_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="drifted_stack_resource_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stack_drift_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stack_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="string">
  Returned by the lookup.
</ResponseField>
