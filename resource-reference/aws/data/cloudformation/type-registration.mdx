---
title: "aws_cloudformation_type_registration"
description: "`aws_cloudformation_type_registration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_type_registration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_type_registration"})

    		ubx.Data(cloudformation.TypeRegistration, "example", cloudformation.TypeRegistrationConfig{
    			RegistrationToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TypeRegistration } from "@ubx/sdk-aws/aws/data/cloudformation/type_registration";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_type_registration" });

      data(TypeRegistration, "example", {
        registrationToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import TypeRegistration, TypeRegistrationConfig

    def describe():
        ubx.intent("look up aws_cloudformation_type_registration")

        ubx.data(TypeRegistration, "example", TypeRegistrationConfig(
            registration_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="registration_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="progress_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type_version_arn" type="string">
  Returned by the lookup.
</ResponseField>
