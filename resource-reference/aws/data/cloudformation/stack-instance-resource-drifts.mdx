---
title: "aws_cloudformation_stack_instance_resource_drifts"
description: "`aws_cloudformation_stack_instance_resource_drifts` reads the real resource-level drift detected in one stack instance of a CloudFormation StackSet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_instance_resource_drifts"})

    		ubx.Data(cloudformation.StackInstanceResourceDrifts, "example", cloudformation.StackInstanceResourceDriftsConfig{
    			OperationId:                        "example",
    			StackInstanceAccount:               "example",
    			StackInstanceRegion:                "example",
    			StackSetName:                       "example-stack-set-name",
    			CallAs:                             "example",
    			MaxResults:                         1,
    			StackInstanceResourceDriftStatuses: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { StackInstanceResourceDrifts } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/stack_instance_resource_drifts.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudformation_stack_instance_resource_drifts",
      });

      data(StackInstanceResourceDrifts, "example", {
        operationId: "example",
        stackInstanceAccount: "example",
        stackInstanceRegion: "example",
        stackSetName: "example-stack-set-name",
        callAs: "example",
        maxResults: 1,
        stackInstanceResourceDriftStatuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackInstanceResourceDrifts, StackInstanceResourceDriftsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_instance_resource_drifts")

        ubx.data(StackInstanceResourceDrifts, "example", StackInstanceResourceDriftsConfig(
            operation_id="example",
            stack_instance_account="example",
            stack_instance_region="example",
            stack_set_name="example-stack-set-name",
            call_as="example",
            max_results=1,
            stack_instance_resource_drift_statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="call_as" type="string">
  The real AWS Organizations identity (self or delegated administrator) this request is made as. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="operation_id" type="string" required>
  The real, unique identifier of the operation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stack_instance_account" type="string" required>
  The real AWS account this CloudFormation StackSet instance is deployed to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stack_instance_region" type="string" required>
  The real AWS region this CloudFormation StackSet instance is deployed to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stack_instance_resource_drift_statuses" type="list(string)">
  The real, current statuses of the stack instance resource drifts included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_set_name" type="string" required>
  The real name of the CloudFormation stack set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
