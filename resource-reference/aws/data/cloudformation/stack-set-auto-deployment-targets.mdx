---
title: "aws_cloudformation_stack_set_auto_deployment_targets"
description: "`aws_cloudformation_stack_set_auto_deployment_targets` reads the real AWS Organizations targets a CloudFormation StackSet automatically deploys to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_set_auto_deployment_targets"})

    		ubx.Data(cloudformation.StackSetAutoDeploymentTargets, "example", cloudformation.StackSetAutoDeploymentTargetsConfig{
    			StackSetName: "example-stack-set-name",
    			CallAs:       "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StackSetAutoDeploymentTargets } from "@ubx/sdk-aws/aws/data/cloudformation/stack_set_auto_deployment_targets";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudformation_stack_set_auto_deployment_targets",
      });

      data(StackSetAutoDeploymentTargets, "example", {
        stackSetName: "example-stack-set-name",
        callAs: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackSetAutoDeploymentTargets, StackSetAutoDeploymentTargetsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_set_auto_deployment_targets")

        ubx.data(StackSetAutoDeploymentTargets, "example", StackSetAutoDeploymentTargetsConfig(
            stack_set_name="example-stack-set-name",
            call_as="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="call_as" type="string">
  The real AWS Organizations identity (self or delegated administrator) this request is made as. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_set_name" type="string" required>
  The real name of the CloudFormation stack set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
