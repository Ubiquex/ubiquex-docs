---
title: "aws_cloudformation_stack_refactors"
description: "`aws_cloudformation_stack_refactors` looks up a real CloudFormation stack refactor operation, moving resources between stacks without replacing them."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_stack_refactors"})

    		ubx.Data(cloudformation.StackRefactors, "example", cloudformation.StackRefactorsConfig{
    			ExecutionStatusFilter: []string{"example"},
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { StackRefactors } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/stack_refactors.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_stack_refactors" });

      data(StackRefactors, "example", {
        executionStatusFilter: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import StackRefactors, StackRefactorsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_stack_refactors")

        ubx.data(StackRefactors, "example", StackRefactorsConfig(
            execution_status_filter=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="execution_status_filter" type="list(string)">
  Restricts results to this execution status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stack_refactor_summaries" type="list(object)">
  The real, summarized stack refactors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
