---
title: "aws_cloudformation_type_registrations"
description: "`aws_cloudformation_type_registrations` reads the real registration attempts for a resource type or module in the CloudFormation Registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_type_registrations"})

    		ubx.Data(cloudformation.TypeRegistrations, "example", cloudformation.TypeRegistrationsConfig{
    			MaxResults:               1,
    			RegistrationStatusFilter: "example",
    			Type:                     "example",
    			TypeArn:                  "arn:aws:iam::123456789012:policy/example",
    			TypeName:                 "example-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TypeRegistrations } from "@ubx/sdk-aws/aws/data/cloudformation/type_registrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_type_registrations" });

      data(TypeRegistrations, "example", {
        maxResults: 1,
        registrationStatusFilter: "example",
        type: "example",
        typeArn: "arn:aws:iam::123456789012:policy/example",
        typeName: "example-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import TypeRegistrations, TypeRegistrationsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_type_registrations")

        ubx.data(TypeRegistrations, "example", TypeRegistrationsConfig(
            max_results=1,
            registration_status_filter="example",
            type="example",
            type_arn="arn:aws:iam::123456789012:policy/example",
            type_name="example-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_status_filter" type="string">
  Restricts results to this registration status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_arn" type="string">
  The real Amazon Resource Name (ARN) of this object's own type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string">
  The real name of this object's own type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_token_list" type="list(string)">
  The real registration tokens included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
