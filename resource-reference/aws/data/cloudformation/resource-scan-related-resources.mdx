---
title: "aws_cloudformation_resource_scan_related_resources"
description: "`aws_cloudformation_resource_scan_related_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_resource_scan_related_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_resource_scan_related_resources"})

    		ubx.Data(cloudformation.ResourceScanRelatedResources, "example", cloudformation.ResourceScanRelatedResourcesConfig{
    			ResourceScanId: "example",
    			Resources:      []map[string]any{{"resource_identifier": map[string]string{"key": "value"}}},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceScanRelatedResources } from "@ubx/sdk-aws/aws/data/cloudformation/resource_scan_related_resources";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudformation_resource_scan_related_resources",
      });

      data(ResourceScanRelatedResources, "example", {
        resourceScanId: "example",
        resources: [{ resourceIdentifier: { key: "value" } }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import ResourceScanRelatedResources, ResourceScanRelatedResourcesConfig

    def describe():
        ubx.intent("look up aws_cloudformation_resource_scan_related_resources")

        ubx.data(ResourceScanRelatedResources, "example", ResourceScanRelatedResourcesConfig(
            resource_scan_id="example",
            resources=[{"resource_identifier": {"key": "value"}}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_scan_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resources" type="list(object)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="related_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
