---
title: "aws_cloudformation_account_limits"
description: "`aws_cloudformation_account_limits` reads the real, account-wide CloudFormation limits, such as the maximum number of stacks."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_account_limits"})

    		ubx.Data(cloudformation.AccountLimits, "example", cloudformation.AccountLimitsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLimits } from "@ubx/sdk-aws/aws/data/cloudformation/account_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_account_limits" });

      data(AccountLimits, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import AccountLimits, AccountLimitsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_account_limits")

        ubx.data(AccountLimits, "example", AccountLimitsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_limits" type="list(object)">
  The real, current account-level limits for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
