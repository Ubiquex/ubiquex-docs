---
title: "aws_cloudformation_change_set_hooks"
description: "`aws_cloudformation_change_set_hooks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_change_set_hooks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_change_set_hooks"})

    		ubx.Data(cloudformation.ChangeSetHooks, "example", cloudformation.ChangeSetHooksConfig{
    			ChangeSetName:     "example-change-set-name",
    			LogicalResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChangeSetHooks } from "@ubx/sdk-aws/aws/data/cloudformation/change_set_hooks";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_change_set_hooks" });

      data(ChangeSetHooks, "example", {
        changeSetName: "example-change-set-name",
        logicalResourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import ChangeSetHooks, ChangeSetHooksConfig

    def describe():
        ubx.intent("look up aws_cloudformation_change_set_hooks")

        ubx.data(ChangeSetHooks, "example", ChangeSetHooksConfig(
            change_set_name="example-change-set-name",
            logical_resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="change_set_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="logical_resource_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_set_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="hooks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stack_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
