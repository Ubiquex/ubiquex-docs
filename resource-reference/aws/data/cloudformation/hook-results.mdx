---
title: "aws_cloudformation_hook_results"
description: "`aws_cloudformation_hook_results` reads the real results of CloudFormation Hook invocations for a stack operation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_hook_results"})

    		ubx.Data(cloudformation.HookResults, "example", cloudformation.HookResultsConfig{
    			Status:  "example",
    			TypeArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HookResults } from "@ubx/sdk-aws/aws/data/cloudformation/hook_results";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_hook_results" });

      data(HookResults, "example", {
        status: "example",
        typeArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import HookResults, HookResultsConfig

    def describe():
        ubx.intent("look up aws_cloudformation_hook_results")

        ubx.data(HookResults, "example", HookResultsConfig(
            status="example",
            type_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_arn" type="string">
  The real Amazon Resource Name (ARN) of this object's own type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hook_results" type="list(object)">
  The real CloudFormation hook results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_id" type="string">
  The real, unique identifier of the target this action applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_type" type="string">
  The real type of the target this action applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
