---
title: "aws_cloudformation_resource_scan"
description: "`aws_cloudformation_resource_scan` looks up one real CloudFormation IaC generator resource scan of an account's own unmanaged resources."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_resource_scan"})

    		ubx.Data(cloudformation.ResourceScan, "example", cloudformation.ResourceScanConfig{
    			ResourceScanId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceScan } from "./local-sdk/aws/sdk/typescript/aws/data/cloudformation/resource_scan.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_resource_scan" });

      data(ResourceScan, "example", {
        resourceScanId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import ResourceScan, ResourceScanConfig

    def describe():
        ubx.intent("look up aws_cloudformation_resource_scan")

        ubx.data(ResourceScan, "example", ResourceScanConfig(
            resource_scan_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_scan_id" type="string" required>
  The real, unique identifier of the resource scan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="percentage_completed" type="number">
  The real, current progress of this operation, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources_read" type="number">
  The real number of resources read while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources_scanned" type="number">
  The real number of resources scanned while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scan_filters" type="list(object)">
  The real filters applied to restrict this scan. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  The real, human-readable reason for the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
