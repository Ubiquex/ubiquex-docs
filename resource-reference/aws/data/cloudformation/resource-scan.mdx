---
title: "aws_cloudformation_resource_scan"
description: "`aws_cloudformation_resource_scan` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudformation_resource_scan` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudformation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudformation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudformation_resource_scan"})

    		ubx.Data(cloudformation.ResourceScan, "example", cloudformation.ResourceScanConfig{
    			ResourceScanId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceScan } from "@ubx/sdk-aws/aws/data/cloudformation/resource_scan";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudformation_resource_scan" });

      data(ResourceScan, "example", {
        resourceScanId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudformation import ResourceScan, ResourceScanConfig

    def describe():
        ubx.intent("look up aws_cloudformation_resource_scan")

        ubx.data(ResourceScan, "example", ResourceScanConfig(
            resource_scan_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_scan_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="percentage_completed" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resources_read" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resources_scanned" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_filters" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Returned by the lookup.
</ResponseField>
