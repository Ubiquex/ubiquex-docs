---
title: "aws_wafv2_top_path_statistics_by_traffic"
description: "`aws_wafv2_top_path_statistics_by_traffic` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wafv2_top_path_statistics_by_traffic` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_top_path_statistics_by_traffic"})

    		ubx.Data(wafv2.TopPathStatisticsByTraffic, "example", wafv2.TopPathStatisticsByTrafficConfig{
    			Limit:                         1,
    			NumberOfTopTrafficBotsPerPath: 1,
    			Scope:                         "example",
    			TimeWindow:                    map[string]any{"end_time": "example"},
    			WebAclArn:                     "arn:aws:iam::123456789012:policy/example",
    			BotCategory:                   "example",
    			BotName:                       "example-bot-name",
    			BotOrganization:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopPathStatisticsByTraffic } from "@ubx/sdk-aws/aws/data/wafv2/top_path_statistics_by_traffic";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_top_path_statistics_by_traffic" });

      data(TopPathStatisticsByTraffic, "example", {
        limit: 1,
        numberOfTopTrafficBotsPerPath: 1,
        scope: "example",
        timeWindow: { endTime: "example" },
        webAclArn: "arn:aws:iam::123456789012:policy/example",
        botCategory: "example",
        botName: "example-bot-name",
        botOrganization: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import TopPathStatisticsByTraffic, TopPathStatisticsByTrafficConfig

    def describe():
        ubx.intent("look up aws_wafv2_top_path_statistics_by_traffic")

        ubx.data(TopPathStatisticsByTraffic, "example", TopPathStatisticsByTrafficConfig(
            limit=1,
            number_of_top_traffic_bots_per_path=1,
            scope="example",
            time_window={"end_time": "example"},
            web_acl_arn="arn:aws:iam::123456789012:policy/example",
            bot_category="example",
            bot_name="example-bot-name",
            bot_organization="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_category" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="bot_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="bot_organization" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="number_of_top_traffic_bots_per_path" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="time_window" type="object" required>
  &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; request, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which you want WAF to return a sample of web requests.&lt;/p&gt; &lt;p&gt;You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, &lt;code&gt;Z&lt;/code&gt;. For example, &lt;code&gt;"2016-09-27T14:50Z"&lt;/code&gt;. You can specify any time range in the previous three hours.&lt;/p&gt; &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; response, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, &lt;code&gt;EndTime&lt;/code&gt; is the time that WAF received the 5,000th request.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uri_path_prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="web_acl_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="path_statistics" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="top_categories" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_request_count" type="number">
  Returned by the lookup.
</ResponseField>
