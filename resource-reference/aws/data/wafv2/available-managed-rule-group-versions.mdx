---
title: "aws_wafv2_available_managed_rule_group_versions"
description: "`aws_wafv2_available_managed_rule_group_versions` reads the real, available versions of a WAFv2 managed rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_available_managed_rule_group_versions"})

    		ubx.Data(wafv2.AvailableManagedRuleGroupVersions, "example", wafv2.AvailableManagedRuleGroupVersionsConfig{
    			Name:       "example",
    			Scope:      "example",
    			VendorName: "example-vendor-name",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailableManagedRuleGroupVersions } from "@ubx/sdk-aws/aws/data/wafv2/available_managed_rule_group_versions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_wafv2_available_managed_rule_group_versions",
      });

      data(AvailableManagedRuleGroupVersions, "example", {
        name: "example",
        scope: "example",
        vendorName: "example-vendor-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import AvailableManagedRuleGroupVersions, AvailableManagedRuleGroupVersionsConfig

    def describe():
        ubx.intent("look up aws_wafv2_available_managed_rule_group_versions")

        ubx.data(AvailableManagedRuleGroupVersions, "example", AvailableManagedRuleGroupVersionsConfig(
            name="example",
            scope="example",
            vendor_name="example-vendor-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vendor_name" type="string" required>
  The real name of the vendor this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="current_default_version" type="string">
  The real version of the current default. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  The real versions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
