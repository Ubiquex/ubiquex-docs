---
title: "aws_wafv2_available_managed_rule_groups"
description: "`aws_wafv2_available_managed_rule_groups` reads the real, available managed rule groups for a WAFv2 web ACL."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_available_managed_rule_groups"})

    		ubx.Data(wafv2.AvailableManagedRuleGroups, "example", wafv2.AvailableManagedRuleGroupsConfig{
    			Scope: "example",
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailableManagedRuleGroups } from "@ubx/sdk-aws/aws/data/wafv2/available_managed_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_available_managed_rule_groups" });

      data(AvailableManagedRuleGroups, "example", {
        scope: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import AvailableManagedRuleGroups, AvailableManagedRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_wafv2_available_managed_rule_groups")

        ubx.data(AvailableManagedRuleGroups, "example", AvailableManagedRuleGroupsConfig(
            scope="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_rule_groups" type="list(object)">
  The real managed rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
