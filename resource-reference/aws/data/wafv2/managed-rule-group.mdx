---
title: "aws_wafv2_managed_rule_group"
description: "`data_aws_wafv2_managed_rule_group` looks up a real, already-existing `aws_wafv2_rule_group`."
---

It defines a real, reusable, named set of WAFv2 rules that can be referenced from one or more `aws_wafv2_web_acl`s, so the same rule logic doesn't have to be redefined in every ACL that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_managed_rule_group"})

    		ubx.Data(wafv2.ManagedRuleGroup, "example", wafv2.ManagedRuleGroupConfig{
    			Name:       "example",
    			Scope:      "example",
    			VendorName: "example-vendor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedRuleGroup } from "@ubx/sdk-aws/aws/data/wafv2/managed_rule_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_managed_rule_group" });

      data(ManagedRuleGroup, "example", {
        name: "example",
        scope: "example",
        vendorName: "example-vendor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import ManagedRuleGroup, ManagedRuleGroupConfig

    def describe():
        ubx.intent("look up aws_wafv2_managed_rule_group")

        ubx.data(ManagedRuleGroup, "example", ManagedRuleGroupConfig(
            name="example",
            scope="example",
            vendor_name="example-vendor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vendor_name" type="string" required>
  The real name of the vendor this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="available_labels" type="list(object)">
  The real labels available for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="capacity" type="number">
  The real, provisioned capacity of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="consumed_labels" type="list(object)">
  The real, consumed labels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="label_namespace" type="string">
  The real namespace this label belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  The real Amazon Resource Name (ARN) of the sns topic this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_name" type="string">
  The real name of the version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
