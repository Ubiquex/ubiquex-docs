---
title: "aws_wafv2_managed_rule_sets"
description: "`aws_wafv2_managed_rule_sets` reads the real WAFv2 managed rule sets a vendor has published."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_managed_rule_sets"})

    		ubx.Data(wafv2.ManagedRuleSets, "example", wafv2.ManagedRuleSetsConfig{
    			Scope: "example",
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedRuleSets } from "@ubx/sdk-aws/aws/data/wafv2/managed_rule_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_managed_rule_sets" });

      data(ManagedRuleSets, "example", {
        scope: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import ManagedRuleSets, ManagedRuleSetsConfig

    def describe():
        ubx.intent("look up aws_wafv2_managed_rule_sets")

        ubx.data(ManagedRuleSets, "example", ManagedRuleSetsConfig(
            scope="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_rule_sets" type="list(object)">
  The real managed rule sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
