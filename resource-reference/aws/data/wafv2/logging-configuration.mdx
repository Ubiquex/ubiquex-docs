---
title: "aws_wafv2_logging_configuration"
description: "`data_aws_wafv2_logging_configuration` looks up a real, already-existing `aws_wafv2_logging_configuration`."
---

It streams a real log of every request an `aws_wafv2_web_acl` evaluates, along with which rule matched, to a CloudWatch Logs group, S3 bucket, or Firehose delivery stream.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own logging configuration"})

    		ubx.Resource(wafv2.LoggingConfiguration, "example", wafv2.LoggingConfigurationConfig{
    			LogDestinationConfigs: []string{"example"},
    			ResourceArn:           "arn:aws:iam::123456789012:policy/example",
    			LoggingFilter:         map[string]any{"default_behavior": "example", "filters": []map[string]any{{"behavior": "example"}}},
    			RedactedFields:        []map[string]any{{"method": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoggingConfiguration } from "@ubx/sdk-aws/aws/wafv2/logging_configuration";

    export default stack("example", () => {
      intent({ summary: "example own logging configuration" });

      resource(LoggingConfiguration, "example", {
        logDestinationConfigs: ["example"],
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        loggingFilter: {
          defaultBehavior: "example",
          filters: [{ behavior: "example" }],
        },
        redactedFields: [{ method: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import LoggingConfiguration, LoggingConfigurationConfig
    from ubx.aws.wafv2.logging_configuration import LoggingConfiguration_LoggingFilter, LoggingConfiguration_LoggingFilter_Filters, LoggingConfiguration_RedactedFields

    def describe():
        ubx.intent("example own logging configuration")

        ubx.resource(LoggingConfiguration, "example", LoggingConfigurationConfig(
            log_destination_configs=["example"],
            resource_arn="arn:aws:iam::123456789012:policy/example",
            logging_filter=LoggingConfiguration_LoggingFilter(default_behavior="example", filters=[LoggingConfiguration_LoggingFilter_Filters(behavior="example")]),
            redacted_fields=[LoggingConfiguration_RedactedFields(method="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_logging_configuration, with log destination
    configs a set value, resource arn
    arn:aws:iam::123456789012:policy/example, logging filter a set
    value, redacted fields a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="log_scope" type="string">
  The real scope logging is applied at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="log_type" type="string">
  The real, discriminating type of the log this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_configuration" type="object">
  &lt;p&gt;Defines an association between logging destinations and a web ACL resource, for logging from WAF. As part of the association, you can specify parts of the standard logging fields to keep out of the logs and you can specify filters so that you log only a subset of the logging records. &lt;/p&gt; &lt;p&gt;If you configure data protection for the web ACL, the protection applies to the data that WAF sends to the logs. &lt;/p&gt; &lt;note&gt; &lt;p&gt;You can define one logging destination per web ACL.&lt;/p&gt; &lt;/note&gt; &lt;p&gt;You can access information about the traffic that WAF inspects using the following steps:&lt;/p&gt; &lt;ol&gt; &lt;li&gt; &lt;p&gt;Create your logging destination. You can use an Amazon CloudWatch Logs log group, an Amazon Simple Storage Service (Amazon S3) bucket, or an Amazon Kinesis Data Firehose. &lt;/p&gt; &lt;p&gt;The name that you give the destination must start with &lt;code&gt;aws-waf-logs-&lt;/code&gt;. Depending on the type of destination, you might need to configure additional settings or permissions. &lt;/p&gt; &lt;p&gt;For configuration requirements and pricing information for each destination type, see &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/logging.html"&gt;Logging web ACL traffic&lt;/a&gt; in the &lt;i&gt;WAF Developer Guide&lt;/i&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;Associate your logging destination to your web ACL using a &lt;code&gt;PutLoggingConfiguration&lt;/code&gt; request.&lt;/p&gt; &lt;/li&gt; &lt;/ol&gt; &lt;p&gt;When you successfully enable logging using a &lt;code&gt;PutLoggingConfiguration&lt;/code&gt; request, WAF creates an additional role or policy that is required to write logs to the logging destination. For an Amazon CloudWatch Logs log group, WAF creates a resource policy on the log group. For an Amazon S3 bucket, WAF creates a bucket policy. For an Amazon Kinesis Data Firehose, WAF creates a service-linked role.&lt;/p&gt; &lt;p&gt;For additional information about web ACL logging, see &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/logging.html"&gt;Logging web ACL traffic information&lt;/a&gt; in the &lt;i&gt;WAF Developer Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="log_destination_configs" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="log_scope" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="log_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="logging_filter" type="object">
      &lt;p&gt;Filtering that specifies which web requests are kept in the logs and which are dropped, defined for a web ACL's &lt;a&gt;LoggingConfiguration&lt;/a&gt;. &lt;/p&gt; &lt;p&gt;You can filter on the rule action and on the web request labels that were applied by matching rules during web ACL evaluation. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="default_behavior" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="filters" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_by_firewall_manager" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="redacted_fields" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
