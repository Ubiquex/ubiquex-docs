---
title: "aws_wafv2_all_managed_products"
description: "`aws_wafv2_all_managed_products` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wafv2_all_managed_products` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_all_managed_products"})

    		ubx.Data(wafv2.AllManagedProducts, "example", wafv2.AllManagedProductsConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AllManagedProducts } from "@ubx/sdk-aws/aws/data/wafv2/all_managed_products";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_all_managed_products" });

      data(AllManagedProducts, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import AllManagedProducts, AllManagedProductsConfig

    def describe():
        ubx.intent("look up aws_wafv2_all_managed_products")

        ubx.data(AllManagedProducts, "example", AllManagedProductsConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_products" type="list(object)">
  Returned by the lookup.
</ResponseField>
