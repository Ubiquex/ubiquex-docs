---
title: "aws_wafv2_all_managed_products"
description: "`aws_wafv2_all_managed_products` reads every real, available managed rule product (AWS-managed and Marketplace) for WAFv2."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_all_managed_products"})

    		ubx.Data(wafv2.AllManagedProducts, "example", wafv2.AllManagedProductsConfig{
    			Scope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AllManagedProducts } from "./local-sdk/aws/sdk/typescript/aws/data/wafv2/all_managed_products.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_all_managed_products" });

      data(AllManagedProducts, "example", {
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import AllManagedProducts, AllManagedProductsConfig

    def describe():
        ubx.intent("look up aws_wafv2_all_managed_products")

        ubx.data(AllManagedProducts, "example", AllManagedProductsConfig(
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_products" type="list(object)">
  The real, managed products included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
