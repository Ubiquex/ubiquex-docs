---
title: "aws_wafv2_revenue_statistics_time_series"
description: "`aws_wafv2_revenue_statistics_time_series` reads real, time-series revenue statistics for a WAFv2 Marketplace managed rule product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_revenue_statistics_time_series"})

    		ubx.Data(wafv2.RevenueStatisticsTimeSeries, "example", wafv2.RevenueStatisticsTimeSeriesConfig{
    			Currency:      "example",
    			Interval:      "example",
    			Scope:         "example",
    			StatisticType: "example",
    			TimeWindow:    map[string]any{"end_time": "example", "start_time": "example"},
    			Filters:       []map[string]any{{"name": "example-name"}},
    			GroupBy:       "example",
    			Limit:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RevenueStatisticsTimeSeries } from "@ubx/sdk-aws/aws/data/wafv2/revenue_statistics_time_series";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_revenue_statistics_time_series" });

      data(RevenueStatisticsTimeSeries, "example", {
        currency: "example",
        interval: "example",
        scope: "example",
        statisticType: "example",
        timeWindow: { endTime: "example", startTime: "example" },
        filters: [{ name: "example-name" }],
        groupBy: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import RevenueStatisticsTimeSeries, RevenueStatisticsTimeSeriesConfig
    from ubx.aws.data.wafv2.revenue_statistics_time_series import RevenueStatisticsTimeSeries_TimeWindow, RevenueStatisticsTimeSeries_Filters

    def describe():
        ubx.intent("look up aws_wafv2_revenue_statistics_time_series")

        ubx.data(RevenueStatisticsTimeSeries, "example", RevenueStatisticsTimeSeriesConfig(
            currency="example",
            interval="example",
            scope="example",
            statistic_type="example",
            time_window=RevenueStatisticsTimeSeries_TimeWindow(end_time="example", start_time="example"),
            filters=[RevenueStatisticsTimeSeries_Filters(name="example-name")],
            group_by="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="currency" type="string" required>
  The real currency this value is denominated in. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="string">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="interval" type="string" required>
  The real time interval results are aggregated over. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistic_type" type="string" required>
  The real, discriminating type of the statistic this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="time_window" type="object" required>
  &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; request, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which you want WAF to return a sample of web requests.&lt;/p&gt; &lt;p&gt;You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, &lt;code&gt;Z&lt;/code&gt;. For example, &lt;code&gt;"2016-09-27T14:50Z"&lt;/code&gt;. You can specify any time range in the previous three hours.&lt;/p&gt; &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; response, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, &lt;code&gt;EndTime&lt;/code&gt; is the time that WAF received the 5,000th request.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="data_points" type="list(object)">
  The real data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
