---
title: "aws_wafv2_mobile_sdk_releases"
description: "`aws_wafv2_mobile_sdk_releases` reads the real, available releases of the WAF mobile SDK."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_mobile_sdk_releases"})

    		ubx.Data(wafv2.MobileSdkReleases, "example", wafv2.MobileSdkReleasesConfig{
    			Platform: "example",
    			Limit:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MobileSdkReleases } from "@ubx/sdk-aws/aws/data/wafv2/mobile_sdk_releases";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_mobile_sdk_releases" });

      data(MobileSdkReleases, "example", {
        platform: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import MobileSdkReleases, MobileSdkReleasesConfig

    def describe():
        ubx.intent("look up aws_wafv2_mobile_sdk_releases")

        ubx.data(MobileSdkReleases, "example", MobileSdkReleasesConfig(
            platform="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="platform" type="string" required>
  The real platform this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="release_summaries" type="list(object)">
  The real, summarized releases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
