---
title: "aws_wafv2_managed_products_by_vendor"
description: "`aws_wafv2_managed_products_by_vendor` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wafv2_managed_products_by_vendor` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_managed_products_by_vendor"})

    		ubx.Data(wafv2.ManagedProductsByVendor, "example", wafv2.ManagedProductsByVendorConfig{
    			Scope:      "example",
    			VendorName: "example-vendor-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedProductsByVendor } from "@ubx/sdk-aws/aws/data/wafv2/managed_products_by_vendor";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_managed_products_by_vendor" });

      data(ManagedProductsByVendor, "example", {
        scope: "example",
        vendorName: "example-vendor-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import ManagedProductsByVendor, ManagedProductsByVendorConfig

    def describe():
        ubx.intent("look up aws_wafv2_managed_products_by_vendor")

        ubx.data(ManagedProductsByVendor, "example", ManagedProductsByVendorConfig(
            scope="example",
            vendor_name="example-vendor-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scope" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vendor_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_products" type="list(object)">
  Returned by the lookup.
</ResponseField>
