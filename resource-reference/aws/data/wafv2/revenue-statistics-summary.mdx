---
title: "aws_wafv2_revenue_statistics_summary"
description: "`aws_wafv2_revenue_statistics_summary` reads a real, aggregated summary of revenue statistics for a WAFv2 Marketplace managed rule product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_revenue_statistics_summary"})

    		ubx.Data(wafv2.RevenueStatisticsSummary, "example", wafv2.RevenueStatisticsSummaryConfig{
    			Currency:   "example",
    			Scope:      "example",
    			TimeWindow: map[string]any{"end_time": "example"},
    			Filters:    []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RevenueStatisticsSummary } from "@ubx/sdk-aws/aws/data/wafv2/revenue_statistics_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_revenue_statistics_summary" });

      data(RevenueStatisticsSummary, "example", {
        currency: "example",
        scope: "example",
        timeWindow: { endTime: "example" },
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import RevenueStatisticsSummary, RevenueStatisticsSummaryConfig

    def describe():
        ubx.intent("look up aws_wafv2_revenue_statistics_summary")

        ubx.data(RevenueStatisticsSummary, "example", RevenueStatisticsSummaryConfig(
            currency="example",
            scope="example",
            time_window={"end_time": "example"},
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="currency" type="string" required>
  The real currency this value is denominated in. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="time_window" type="object" required>
  &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; request, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which you want WAF to return a sample of web requests.&lt;/p&gt; &lt;p&gt;You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, &lt;code&gt;Z&lt;/code&gt;. For example, &lt;code&gt;"2016-09-27T14:50Z"&lt;/code&gt;. You can specify any time range in the previous three hours.&lt;/p&gt; &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; response, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, &lt;code&gt;EndTime&lt;/code&gt; is the time that WAF received the 5,000th request.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="revenue_breakdown" type="object">
  &lt;p&gt;A summary of AI bot monetization revenue, including total revenue, revenue by verification tier, and request counts.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="currency" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_amount" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_monetize_served" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_settled" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="unverified_amount" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="verified_amount" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
