---
title: "aws_wafv2_rate_based_statement_managed_keys"
description: "`aws_wafv2_rate_based_statement_managed_keys` reads the real keys (such as IP addresses) currently rate-limited by a WAFv2 rate-based rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_rate_based_statement_managed_keys"})

    		ubx.Data(wafv2.RateBasedStatementManagedKeys, "example", wafv2.RateBasedStatementManagedKeysConfig{
    			RuleName:          "example-rule-name",
    			Scope:             "example",
    			WebAclid:          "example",
    			WebAclname:        "example",
    			RuleGroupRuleName: "example-rule-group-rule-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RateBasedStatementManagedKeys } from "@ubx/sdk-aws/aws/data/wafv2/rate_based_statement_managed_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_rate_based_statement_managed_keys" });

      data(RateBasedStatementManagedKeys, "example", {
        ruleName: "example-rule-name",
        scope: "example",
        webAclid: "example",
        webAclname: "example",
        ruleGroupRuleName: "example-rule-group-rule-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import RateBasedStatementManagedKeys, RateBasedStatementManagedKeysConfig

    def describe():
        ubx.intent("look up aws_wafv2_rate_based_statement_managed_keys")

        ubx.data(RateBasedStatementManagedKeys, "example", RateBasedStatementManagedKeysConfig(
            rule_name="example-rule-name",
            scope="example",
            web_aclid="example",
            web_aclname="example",
            rule_group_rule_name="example-rule-group-rule-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="rule_group_rule_name" type="string">
  The real name of the rule group rule this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The real name of the rule this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scope" type="string" required>
  The real scope this lookup is applied at. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="web_aclid" type="string" required>
  The real, unique identifier of the AWS WAF web ACL this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="web_aclname" type="string" required>
  The real name of the AWS WAF web ACL this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_keys_ipv4" type="object">
  &lt;p&gt;The set of IP addresses that are currently blocked for a &lt;a&gt;RateBasedStatement&lt;/a&gt;. This is only available for rate-based rules that aggregate on just the IP address, with the &lt;code&gt;AggregateKeyType&lt;/code&gt; set to &lt;code&gt;IP&lt;/code&gt; or &lt;code&gt;FORWARDED_IP&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;A rate-based rule applies its rule action to requests from IP addresses that are in the rule's managed keys list and that match the rule's scope-down statement. When a rule has no scope-down statement, it applies the action to all requests from the IP addresses that are in the list. The rule applies its rule action to rate limit the matching requests. The action is usually Block but it can be any valid rule action except for Allow. &lt;/p&gt; &lt;p&gt;The maximum number of IP addresses that can be rate limited by a single rate-based rule instance is 10,000. If more than 10,000 addresses exceed the rate limit, WAF limits those with the highest rates. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="addresses" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ipaddress_version" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="managed_keys_ipv6" type="object">
  &lt;p&gt;The set of IP addresses that are currently blocked for a &lt;a&gt;RateBasedStatement&lt;/a&gt;. This is only available for rate-based rules that aggregate on just the IP address, with the &lt;code&gt;AggregateKeyType&lt;/code&gt; set to &lt;code&gt;IP&lt;/code&gt; or &lt;code&gt;FORWARDED_IP&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;A rate-based rule applies its rule action to requests from IP addresses that are in the rule's managed keys list and that match the rule's scope-down statement. When a rule has no scope-down statement, it applies the action to all requests from the IP addresses that are in the list. The rule applies its rule action to rate limit the matching requests. The action is usually Block but it can be any valid rule action except for Allow. &lt;/p&gt; &lt;p&gt;The maximum number of IP addresses that can be rate limited by a single rate-based rule instance is 10,000. If more than 10,000 addresses exceed the rate limit, WAF limits those with the highest rates. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="addresses" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ipaddress_version" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
