---
title: "aws_wafv2_web_aclfor_resource"
description: "`aws_wafv2_web_aclfor_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wafv2_web_aclfor_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wafv2_web_aclfor_resource"})

    		ubx.Data(wafv2.WebAclforResource, "example", wafv2.WebAclforResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WebAclforResource } from "@ubx/sdk-aws/aws/data/wafv2/web_aclfor_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_wafv2_web_aclfor_resource" });

      data(WebAclforResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wafv2 import WebAclforResource, WebAclforResourceConfig

    def describe():
        ubx.intent("look up aws_wafv2_web_aclfor_resource")

        ubx.data(WebAclforResource, "example", WebAclforResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="web_acl" type="object">
  &lt;p&gt; A web ACL defines a collection of rules to use to inspect and control web requests. Each rule has a statement that defines what to look for in web requests and an action that WAF applies to requests that match the statement. In the web ACL, you assign a default action to take (allow, block) for any request that does not match any of the rules. The rules in a web ACL can be a combination of the types &lt;a&gt;Rule&lt;/a&gt;, &lt;a&gt;RuleGroup&lt;/a&gt;, and managed rule group. You can associate a web ACL with one or more Amazon Web Services resources to protect. The resource types include Amazon CloudFront distribution, Amazon API Gateway REST API, Application Load Balancer, AppSync GraphQL API, Amazon Cognito user pool, App Runner service, Amplify application, Amazon Web Services Verified Access instance, and Amazon Bedrock AgentCore Gateway. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_config" type="object">
      &lt;p&gt;A list of &lt;code&gt;ApplicationAttribute&lt;/code&gt;s that contains information about the application.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="attributes" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="association_config" type="object">
      &lt;p&gt;Specifies custom configurations for the associations between the web ACL and protected resources. &lt;/p&gt; &lt;p&gt;Use this to customize the maximum size of the request body that your protected resources forward to WAF for inspection. You can customize this setting for CloudFront, API Gateway, Amazon Cognito, App Runner, or Verified Access resources. The default setting is 16 KB (16,384 bytes). &lt;/p&gt; &lt;note&gt; &lt;p&gt;You are charged additional fees when your protected resources forward body sizes that are larger than the default. For more information, see &lt;a href="http://aws.amazon.com/waf/pricing/"&gt;WAF Pricing&lt;/a&gt;.&lt;/p&gt; &lt;/note&gt; &lt;p&gt;For Application Load Balancer and AppSync, the limit is fixed at 8 KB (8,192 bytes).&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="request_body" type="map(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="capacity" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="captcha_config" type="object">
      &lt;p&gt;Specifies how WAF should handle &lt;code&gt;CAPTCHA&lt;/code&gt; evaluations. This is available at the web ACL level and in each rule. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="immunity_time_property" type="object">
          &lt;p&gt;Used for CAPTCHA and challenge token settings. Determines how long a &lt;code&gt;CAPTCHA&lt;/code&gt; or challenge timestamp remains valid after WAF updates it for a successful &lt;code&gt;CAPTCHA&lt;/code&gt; or challenge response. &lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="immunity_time" type="number" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="challenge_config" type="object">
      &lt;p&gt;Specifies how WAF should handle &lt;code&gt;Challenge&lt;/code&gt; evaluations. This is available at the web ACL level and in each rule. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="immunity_time_property" type="object">
          &lt;p&gt;Used for CAPTCHA and challenge token settings. Determines how long a &lt;code&gt;CAPTCHA&lt;/code&gt; or challenge timestamp remains valid after WAF updates it for a successful &lt;code&gt;CAPTCHA&lt;/code&gt; or challenge response. &lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="immunity_time" type="number" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_response_bodies" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="data_protection_config" type="object">
      &lt;p&gt;Specifies data protection to apply to the web request data for the web ACL. This is a web ACL level data protection option. &lt;/p&gt; &lt;p&gt;The data protection that you configure for the web ACL alters the data that's available for any other data collection activity, including your WAF logging destinations, web ACL request sampling, and Amazon Security Lake data collection and management. Your other option for data protection is in the logging configuration, which only affects logging. &lt;/p&gt; &lt;p&gt;This is part of the data protection configuration for a web ACL. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="data_protections" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_action" type="object" required>
      &lt;p&gt;In a &lt;a&gt;WebACL&lt;/a&gt;, this is the action that you want WAF to perform when a web request doesn't match any of the rules in the &lt;code&gt;WebACL&lt;/code&gt;. The default action must be a terminating action.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="allow" type="object">
          &lt;p&gt;Specifies that WAF should allow the request and optionally defines additional custom handling for the request.&lt;/p&gt; &lt;p&gt;This is used in the context of other settings, for example to specify values for &lt;a&gt;RuleAction&lt;/a&gt; and web ACL &lt;a&gt;DefaultAction&lt;/a&gt;. &lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="custom_request_handling" type="object">
              &lt;p&gt;Custom request handling behavior that inserts custom headers into a web request. You can add custom request handling for WAF to use when the rule action doesn't block the request. For example, &lt;code&gt;CaptchaAction&lt;/code&gt; for requests with valid t okens, and &lt;code&gt;AllowAction&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;For information about customizing web requests and responses, see &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/waf-custom-request-response.html"&gt;Customizing web requests and responses in WAF&lt;/a&gt; in the &lt;i&gt;WAF Developer Guide&lt;/i&gt;. &lt;/p&gt; Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="insert_headers" type="list(object)" required>
                  Required lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="block" type="object">
          &lt;p&gt;Specifies that WAF should block the request and optionally defines additional custom handling for the response to the web request.&lt;/p&gt; &lt;p&gt;This is used in the context of other settings, for example to specify values for &lt;a&gt;RuleAction&lt;/a&gt; and web ACL &lt;a&gt;DefaultAction&lt;/a&gt;. &lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="custom_response" type="object">
              &lt;p&gt;A custom response to send to the client. You can define a custom response for rule actions and default web ACL actions that are set to &lt;a&gt;BlockAction&lt;/a&gt;. &lt;/p&gt; &lt;p&gt;For information about customizing web requests and responses, see &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/waf-custom-request-response.html"&gt;Customizing web requests and responses in WAF&lt;/a&gt; in the &lt;i&gt;WAF Developer Guide&lt;/i&gt;. &lt;/p&gt; Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="custom_response_body_key" type="string">
                  Optional lookup argument.
                </ResponseField>
                <ResponseField name="response_code" type="number" required>
                  Required lookup argument.
                </ResponseField>
                <ResponseField name="response_headers" type="list(object)">
                  Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="label_namespace" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="managed_by_firewall_manager" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="monetization_config" type="object">
      &lt;p&gt;The monetization configuration for a web ACL or rule group. Specifies the cryptocurrency payment networks and currency mode for AI bot monetization. You must provide this configuration when any rule in the web ACL or rule group uses the &lt;code&gt;Monetize&lt;/code&gt; action.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="crypto_config" type="object">
          &lt;p&gt;The cryptocurrency payment configuration for AI bot monetization. Contains the list of blockchain payment networks where you receive payments.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="payment_networks" type="list(object)" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="currency_mode" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="on_source_ddo_sprotection_config" type="object">
      &lt;p&gt;Configures the level of DDoS protection that applies to web ACLs associated with Application Load Balancers.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="alblow_reputation_mode" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="post_process_firewall_manager_rule_groups" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pre_process_firewall_manager_rule_groups" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="retrofitted_by_firewall_manager" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="rules" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="token_domains" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="visibility_config" type="object" required>
      &lt;p&gt;Defines and enables Amazon CloudWatch metrics and web request sample collection. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="cloud_watch_metrics_enabled" type="bool" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="metric_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="sampled_requests_enabled" type="bool" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
