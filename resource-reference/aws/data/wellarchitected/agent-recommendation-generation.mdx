---
title: "aws_wellarchitected_agent_recommendation_generation"
description: "`aws_wellarchitected_agent_recommendation_generation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wellarchitected_agent_recommendation_generation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_agent_recommendation_generation"})

    		ubx.Data(wellarchitected.AgentRecommendationGeneration, "example", wellarchitected.AgentRecommendationGenerationConfig{
    			GenerationId: "example",
    			ProfileArn:   "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRecommendationGeneration } from "@ubx/sdk-aws/aws/data/wellarchitected/agent_recommendation_generation";

    export default stack("example", () => {
      intent({
        summary: "look up aws_wellarchitected_agent_recommendation_generation",
      });

      data(AgentRecommendationGeneration, "example", {
        generationId: "example",
        profileArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import AgentRecommendationGeneration, AgentRecommendationGenerationConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_agent_recommendation_generation")

        ubx.data(AgentRecommendationGeneration, "example", AgentRecommendationGenerationConfig(
            generation_id="example",
            profile_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="generation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="additional_context" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ended_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="object">
  &lt;p&gt;Details about an error that occurred during recommendation generation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="estimated_completion_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_by" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="progress" type="object">
  &lt;p&gt;Progress information for a recommendation generation process.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="completion_percentage" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="steps_completed" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="total_steps" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="object">
  &lt;p&gt;Defines the scope for recommendation generation, specifying which pillars and goals to focus on.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="goal_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pillars" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="started_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
