---
title: "aws_wellarchitected_review_templates"
description: "`data_aws_wellarchitected_review_templates` looks up a real, already-existing `aws_well_architected_review_template`."
---

It defines a real, reusable review configuration, which lenses and pillars to include, that can be applied to an `aws_well_architected_workload`, so a consistent review structure can be reused across many workloads instead of configured individually each time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_review_templates"})

    		ubx.Data(wellarchitected.ReviewTemplates, "example", wellarchitected.ReviewTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReviewTemplates } from "./local-sdk/aws/sdk/typescript/aws/data/wellarchitected/review_templates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_review_templates" });

      data(ReviewTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import ReviewTemplates, ReviewTemplatesConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_review_templates")

        ubx.data(ReviewTemplates, "example", ReviewTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="review_templates" type="list(object)">
  The real AWS Audit Manager review templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
