---
title: "aws_wellarchitected_lens_review_report"
description: "`aws_wellarchitected_lens_review_report` reads a real, generated report of a Well-Architected lens review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_lens_review_report"})

    		ubx.Data(wellarchitected.LensReviewReport, "example", wellarchitected.LensReviewReportConfig{
    			LensAlias:  "example",
    			WorkloadId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LensReviewReport } from "./local-sdk/aws/sdk/typescript/aws/data/wellarchitected/lens_review_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_lens_review_report" });

      data(LensReviewReport, "example", {
        lensAlias: "example",
        workloadId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import LensReviewReport, LensReviewReportConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_lens_review_report")

        ubx.data(LensReviewReport, "example", LensReviewReportConfig(
            lens_alias="example",
            workload_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_alias" type="string" required>
  &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lens_review_report" type="object">
  &lt;p&gt;A report of a lens review.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="base64_string" type="string">
      &lt;p&gt;The Base64-encoded string representation of a lens review report.&lt;/p&gt; &lt;p&gt;This data can be used to create a PDF file.&lt;/p&gt; &lt;p&gt;Only returned by &lt;a&gt;GetConsolidatedReport&lt;/a&gt; when &lt;code&gt;PDF&lt;/code&gt; format is requested.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_alias" type="string">
      &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="milestone_number" type="number">
  &lt;p&gt;The milestone number.&lt;/p&gt; &lt;p&gt;A workload can have a maximum of 100 milestones.&lt;/p&gt; Returned by the lookup.
</ResponseField>
