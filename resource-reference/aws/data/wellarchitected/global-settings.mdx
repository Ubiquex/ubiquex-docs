---
title: "aws_wellarchitected_global_settings"
description: "`aws_wellarchitected_global_settings` reads the real, account-wide global settings for the Well-Architected Tool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_global_settings"})

    		ubx.Data(wellarchitected.GlobalSettings, "example", wellarchitected.GlobalSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GlobalSettings } from "@ubx/sdk-aws/aws/data/wellarchitected/global_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_global_settings" });

      data(GlobalSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import GlobalSettings, GlobalSettingsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_global_settings")

        ubx.data(GlobalSettings, "example", GlobalSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="discovery_integration_status" type="string">
  The real, current status of the discovery integration this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="jira_configuration" type="object">
  &lt;p&gt;Account-level: Output configuration of the Jira integration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="integration_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="issue_management_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="issue_management_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="jira_project_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subdomain" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="organization_sharing_status" type="string">
  The real, current status of the organization sharing this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
