---
title: "aws_wellarchitected_notifications"
description: "`aws_wellarchitected_notifications` reads real, system-generated notifications (such as a lens version update) for the Well-Architected Tool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_notifications"})

    		ubx.Data(wellarchitected.Notifications, "example", wellarchitected.NotificationsConfig{
    			MaxResults:  1,
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			WorkloadId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Notifications } from "@ubx/sdk-aws/aws/data/wellarchitected/notifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_notifications" });

      data(Notifications, "example", {
        maxResults: 1,
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        workloadId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import Notifications, NotificationsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_notifications")

        ubx.data(Notifications, "example", NotificationsConfig(
            max_results=1,
            resource_arn="arn:aws:iam::123456789012:policy/example",
            workload_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string">
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="notification_summaries" type="list(object)">
  The real, summarized notifications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
