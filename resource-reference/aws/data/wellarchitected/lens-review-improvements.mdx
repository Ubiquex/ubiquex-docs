---
title: "aws_wellarchitected_lens_review_improvements"
description: "`aws_wellarchitected_lens_review_improvements` reads the real, suggested improvement plan items for a Well-Architected lens review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_lens_review_improvements"})

    		ubx.Data(wellarchitected.LensReviewImprovements, "example", wellarchitected.LensReviewImprovementsConfig{
    			LensAlias:        "example",
    			WorkloadId:       "example",
    			MaxResults:       1,
    			PillarId:         "example",
    			QuestionPriority: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LensReviewImprovements } from "@ubx/sdk-aws/aws/data/wellarchitected/lens_review_improvements";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_lens_review_improvements" });

      data(LensReviewImprovements, "example", {
        lensAlias: "example",
        workloadId: "example",
        maxResults: 1,
        pillarId: "example",
        questionPriority: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import LensReviewImprovements, LensReviewImprovementsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_lens_review_improvements")

        ubx.data(LensReviewImprovements, "example", LensReviewImprovementsConfig(
            lens_alias="example",
            workload_id="example",
            max_results=1,
            pillar_id="example",
            question_priority="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_alias" type="string" required>
  &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="pillar_id" type="string">
  &lt;p&gt;The ID used to identify a pillar, for example, &lt;code&gt;security&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;A pillar is identified by its &lt;a&gt;PillarReviewSummary$PillarId&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="question_priority" type="string">
  The real, relative priority of this question. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="improvement_summaries" type="list(object)">
  &lt;p&gt;List of improvement summaries of lens review in a workload.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="lens_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Well-Architected lens this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="milestone_number" type="number">
  &lt;p&gt;The milestone number.&lt;/p&gt; &lt;p&gt;A workload can have a maximum of 100 milestones.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
