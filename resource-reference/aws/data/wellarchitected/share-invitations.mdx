---
title: "aws_wellarchitected_share_invitations"
description: "`aws_wellarchitected_share_invitations` reads the real, pending Well-Architected workload or lens share invitations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_share_invitations"})

    		ubx.Data(wellarchitected.ShareInvitations, "example", wellarchitected.ShareInvitationsConfig{
    			LensNamePrefix:     "example",
    			MaxResults:         1,
    			ProfileNamePrefix:  "example",
    			ShareResourceType:  "example",
    			TemplateNamePrefix: "example",
    			WorkloadNamePrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ShareInvitations } from "@ubx/sdk-aws/aws/data/wellarchitected/share_invitations";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_share_invitations" });

      data(ShareInvitations, "example", {
        lensNamePrefix: "example",
        maxResults: 1,
        profileNamePrefix: "example",
        shareResourceType: "example",
        templateNamePrefix: "example",
        workloadNamePrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import ShareInvitations, ShareInvitationsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_share_invitations")

        ubx.data(ShareInvitations, "example", ShareInvitationsConfig(
            lens_name_prefix="example",
            max_results=1,
            profile_name_prefix="example",
            share_resource_type="example",
            template_name_prefix="example",
            workload_name_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_name_prefix" type="string">
  The real S3 key prefix used for the lens name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="profile_name_prefix" type="string">
  The real S3 key prefix used for the profile name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="share_resource_type" type="string">
  The real, discriminating type of the share resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_name_prefix" type="string">
  The real S3 key prefix used for the template name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workload_name_prefix" type="string">
  &lt;p&gt;An optional string added to the beginning of each workload name returned in the results.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="share_invitation_summaries" type="list(object)">
  The real, summarized share invitations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
