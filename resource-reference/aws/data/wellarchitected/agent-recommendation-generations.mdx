---
title: "aws_wellarchitected_agent_recommendation_generations"
description: "`aws_wellarchitected_agent_recommendation_generations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_wellarchitected_agent_recommendation_generations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_agent_recommendation_generations"})

    		ubx.Data(wellarchitected.AgentRecommendationGenerations, "example", wellarchitected.AgentRecommendationGenerationsConfig{
    			ProfileArn:         "arn:aws:iam::123456789012:policy/example",
    			MaxResults:         1,
    			RecommendationType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRecommendationGenerations } from "@ubx/sdk-aws/aws/data/wellarchitected/agent_recommendation_generations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_wellarchitected_agent_recommendation_generations",
      });

      data(AgentRecommendationGenerations, "example", {
        profileArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        recommendationType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import AgentRecommendationGenerations, AgentRecommendationGenerationsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_agent_recommendation_generations")

        ubx.data(AgentRecommendationGenerations, "example", AgentRecommendationGenerationsConfig(
            profile_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            recommendation_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="profile_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
