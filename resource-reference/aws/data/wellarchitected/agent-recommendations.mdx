---
title: "aws_wellarchitected_agent_recommendations"
description: "`aws_wellarchitected_agent_recommendations` reads the real recommendations generated by a Well-Architected generative-AI agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_agent_recommendations"})

    		ubx.Data(wellarchitected.AgentRecommendations, "example", wellarchitected.AgentRecommendationsConfig{
    			ProfileArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    			Pillar:     "example",
    			State:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRecommendations } from "@ubx/sdk-aws/aws/data/wellarchitected/agent_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_agent_recommendations" });

      data(AgentRecommendations, "example", {
        profileArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        pillar: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import AgentRecommendations, AgentRecommendationsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_agent_recommendations")

        ubx.data(AgentRecommendations, "example", AgentRecommendationsConfig(
            profile_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            pillar="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="pillar" type="string">
  The real AWS Well-Architected pillar this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="profile_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
