---
title: "aws_wellarchitected_lens_review"
description: "`aws_wellarchitected_lens_review` looks up one real lens review within a Well-Architected workload."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_lens_review"})

    		ubx.Data(wellarchitected.LensReview, "example", wellarchitected.LensReviewConfig{
    			LensAlias:  "example",
    			WorkloadId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LensReview } from "./local-sdk/aws/sdk/typescript/aws/data/wellarchitected/lens_review.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_lens_review" });

      data(LensReview, "example", {
        lensAlias: "example",
        workloadId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import LensReview, LensReviewConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_lens_review")

        ubx.data(LensReview, "example", LensReviewConfig(
            lens_alias="example",
            workload_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_alias" type="string" required>
  &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lens_review" type="object">
  &lt;p&gt;A lens review of a question.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="jira_configuration" type="object">
      &lt;p&gt;Selected questions in the workload.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="selected_pillars" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lens_alias" type="string">
      &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_name" type="string">
      &lt;p&gt;The full name of the lens.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_version" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="notes" type="string">
      &lt;p&gt;The notes associated with the workload.&lt;/p&gt; &lt;p&gt;For a review template, these are the notes that will be associated with the workload when the template is applied.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="pillar_review_summaries" type="list(object)">
      &lt;p&gt;List of pillar review summaries of lens review in a workload.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="prioritized_risk_counts" type="map(number)">
      &lt;p&gt;A map from risk names to the count of how many questions have that rating.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="profiles" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="risk_counts" type="map(number)">
      &lt;p&gt;A map from risk names to the count of how many questions have that rating.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="milestone_number" type="number">
  &lt;p&gt;The milestone number.&lt;/p&gt; &lt;p&gt;A workload can have a maximum of 100 milestones.&lt;/p&gt; Returned by the lookup.
</ResponseField>
