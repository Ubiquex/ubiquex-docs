---
title: "aws_wellarchitected_profiles"
description: "`data_aws_wellarchitected_profiles` looks up a real, already-existing `aws_well_architected_profile`."
---

It defines a real, reusable set of questions and priorities, tailored to a specific business context, that can be applied to an `aws_well_architected_workload`'s review, layered on top of the standard Well-Architected Framework pillars.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_profiles"})

    		ubx.Data(wellarchitected.Profiles, "example", wellarchitected.ProfilesConfig{
    			MaxResults:        1,
    			ProfileNamePrefix: "example",
    			ProfileOwnerType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Profiles } from "@ubx/sdk-aws/aws/data/wellarchitected/profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_profiles" });

      data(Profiles, "example", {
        maxResults: 1,
        profileNamePrefix: "example",
        profileOwnerType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import Profiles, ProfilesConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_profiles")

        ubx.data(Profiles, "example", ProfilesConfig(
            max_results=1,
            profile_name_prefix="example",
            profile_owner_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="profile_name_prefix" type="string">
  The real S3 key prefix used for the profile name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="profile_owner_type" type="string">
  The real, discriminating type of the profile owner this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="profile_summaries" type="list(object)">
  The real, summarized profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
