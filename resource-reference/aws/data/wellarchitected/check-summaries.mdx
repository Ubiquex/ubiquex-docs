---
title: "aws_wellarchitected_check_summaries"
description: "`aws_wellarchitected_check_summaries` reads a real, summarized status of Trusted Advisor checks surfaced within a Well-Architected review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_check_summaries"})

    		ubx.Data(wellarchitected.CheckSummaries, "example", wellarchitected.CheckSummariesConfig{
    			ChoiceId:   "example",
    			LensArn:    "arn:aws:iam::123456789012:policy/example",
    			PillarId:   "example",
    			QuestionId: "example",
    			WorkloadId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CheckSummaries } from "@ubx/sdk-aws/aws/data/wellarchitected/check_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_check_summaries" });

      data(CheckSummaries, "example", {
        choiceId: "example",
        lensArn: "arn:aws:iam::123456789012:policy/example",
        pillarId: "example",
        questionId: "example",
        workloadId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import CheckSummaries, CheckSummariesConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_check_summaries")

        ubx.data(CheckSummaries, "example", CheckSummariesConfig(
            choice_id="example",
            lens_arn="arn:aws:iam::123456789012:policy/example",
            pillar_id="example",
            question_id="example",
            workload_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="choice_id" type="string" required>
  &lt;p&gt;The ID of a choice.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="lens_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the AWS Well-Architected lens this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="pillar_id" type="string" required>
  &lt;p&gt;The ID used to identify a pillar, for example, &lt;code&gt;security&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;A pillar is identified by its &lt;a&gt;PillarReviewSummary$PillarId&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="question_id" type="string" required>
  &lt;p&gt;The ID of the question.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="check_summaries" type="list(object)">
  The real, summarized checks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
