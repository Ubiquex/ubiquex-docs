---
title: "aws_wellarchitected_consolidated_report"
description: "`aws_wellarchitected_consolidated_report` reads a real, generated report consolidating multiple Well-Architected workload reviews."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_consolidated_report"})

    		ubx.Data(wellarchitected.ConsolidatedReport, "example", wellarchitected.ConsolidatedReportConfig{
    			Format:                 "example",
    			IncludeSharedResources: true,
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConsolidatedReport } from "@ubx/sdk-aws/aws/data/wellarchitected/consolidated_report";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_consolidated_report" });

      data(ConsolidatedReport, "example", {
        format: "example",
        includeSharedResources: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import ConsolidatedReport, ConsolidatedReportConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_consolidated_report")

        ubx.data(ConsolidatedReport, "example", ConsolidatedReportConfig(
            format="example",
            include_shared_resources=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_shared_resources" type="bool">
  Whether shared resources are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="base64_string" type="string">
  &lt;p&gt;The Base64-encoded string representation of a lens review report.&lt;/p&gt; &lt;p&gt;This data can be used to create a PDF file.&lt;/p&gt; &lt;p&gt;Only returned by &lt;a&gt;GetConsolidatedReport&lt;/a&gt; when &lt;code&gt;PDF&lt;/code&gt; format is requested.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="metrics" type="list(object)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
