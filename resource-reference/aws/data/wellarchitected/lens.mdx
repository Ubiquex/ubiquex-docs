---
title: "aws_wellarchitected_lens"
description: "`data_aws_wellarchitected_lens` looks up a real, already-existing `aws_well_architected_lens`."
---

It defines a real, custom set of best-practice questions, beyond the standard Well-Architected Framework pillars, that can be applied to an `aws_well_architected_workload`'s review, for a specific industry or technology an organization wants to assess against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_lens"})

    		ubx.Data(wellarchitected.Lens, "example", wellarchitected.LensConfig{
    			LensAlias:   "example",
    			LensVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Lens } from "@ubx/sdk-aws/aws/data/wellarchitected/lens";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_lens" });

      data(Lens, "example", {
        lensAlias: "example",
        lensVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import Lens, LensConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_lens")

        ubx.data(Lens, "example", LensConfig(
            lens_alias="example",
            lens_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_alias" type="string" required>
  &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="lens_version" type="string">
  The real version of the lens. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lens" type="object">
  &lt;p&gt;A lens return object.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      &lt;p&gt;The description of the lens.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="lens_version" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      &lt;p&gt;The full name of the lens.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="share_invitation_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
