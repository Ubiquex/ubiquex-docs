---
title: "aws_wellarchitected_agent_recommendation"
description: "`aws_wellarchitected_agent_recommendation` looks up one real recommendation generated by a Well-Architected generative-AI agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_agent_recommendation"})

    		ubx.Data(wellarchitected.AgentRecommendation, "example", wellarchitected.AgentRecommendationConfig{
    			RecommendationArn: "arn:aws:iam::123456789012:policy/example",
    			RemediationType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRecommendation } from "@ubx/sdk-aws/aws/data/wellarchitected/agent_recommendation";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_agent_recommendation" });

      data(AgentRecommendation, "example", {
        recommendationArn: "arn:aws:iam::123456789012:policy/example",
        remediationType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import AgentRecommendation, AgentRecommendationConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_agent_recommendation")

        ubx.data(AgentRecommendation, "example", AgentRecommendationConfig(
            recommendation_arn="arn:aws:iam::123456789012:policy/example",
            remediation_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="recommendation_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the recommendation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="remediation_type" type="string">
  The real, discriminating type of the remediation this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(string)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aws_services" type="list(string)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="business_units" type="list(string)">
  The real business units included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="string">
  The real identity that created this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="cross_pillar_benefits" type="list(object)">
  The real, cross-pillar benefits of this AWS Well-Architected improvement. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="effort" type="string">
  The real, estimated effort required for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="goals" type="list(object)">
  The real goals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="highlights" type="list(string)">
  The real, highlighted matches included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="impact" type="string">
  The real, estimated impact of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="impact_details" type="list(string)">
  The real, detailed information about the impact. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="insights" type="list(object)">
  The real insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_by" type="string">
  The real identity that last modified this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_resources" type="number">
  The real number of resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pillar" type="string">
  The real AWS Well-Architected pillar this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="priority" type="string">
  The real priority of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="profile_arn" type="string">
  The real Amazon Resource Name (ARN) of the profile this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="remediation_summary" type="object">
  &lt;p&gt;A high-level remediation summary returned in the detail response.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="recommendation" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="steps" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="remediations" type="list(object)">
  The real remediations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="roi" type="object">
  &lt;p&gt;A return-on-investment estimate with context.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="detail" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="estimate" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sources" type="list(string)">
  The real sources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="title" type="string">
  The real, human-readable title of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="trade_offs" type="list(object)">
  The real trade-offs of this recommendation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_reason" type="string">
  The real, human-readable reason for the update. **(AI-inferred)** Returned by the lookup.
</ResponseField>
