---
title: "aws_wellarchitected_review_template_answer"
description: "`data_aws_wellarchitected_review_template_answer` looks up a real, already-existing `aws_well_architected_review_template`."
---

It defines a real, reusable review configuration, which lenses and pillars to include, that can be applied to an `aws_well_architected_workload`, so a consistent review structure can be reused across many workloads instead of configured individually each time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_review_template_answer"})

    		ubx.Data(wellarchitected.ReviewTemplateAnswer, "example", wellarchitected.ReviewTemplateAnswerConfig{
    			LensAlias:   "example",
    			QuestionId:  "example",
    			TemplateArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReviewTemplateAnswer } from "@ubx/sdk-aws/aws/data/wellarchitected/review_template_answer";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_review_template_answer" });

      data(ReviewTemplateAnswer, "example", {
        lensAlias: "example",
        questionId: "example",
        templateArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import ReviewTemplateAnswer, ReviewTemplateAnswerConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_review_template_answer")

        ubx.data(ReviewTemplateAnswer, "example", ReviewTemplateAnswerConfig(
            lens_alias="example",
            question_id="example",
            template_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="lens_alias" type="string" required>
  &lt;p&gt;The alias of the lens.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is either the lens alias, such as &lt;code&gt;serverless&lt;/code&gt;, or the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-east-1::lens/serverless&lt;/code&gt;. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the lens ARN, such as &lt;code&gt;arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;Each lens is identified by its &lt;a&gt;LensSummary$LensAlias&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="question_id" type="string" required>
  &lt;p&gt;The ID of the question.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="template_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="answer" type="object">
  &lt;p&gt;An answer of the question.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="answer_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="choice_answers" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="choices" type="list(object)">
      &lt;p&gt;List of choices available for a question.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="helpful_resource_display_text" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="helpful_resource_url" type="string">
      &lt;p&gt;The helpful resource URL.&lt;/p&gt; &lt;p&gt;For Amazon Web Services official lenses, this is the helpful resource URL for a question or choice.&lt;/p&gt; &lt;p&gt;For custom lenses, this is the helpful resource URL for a question and is only provided if &lt;code&gt;HelpfulResourceDisplayText&lt;/code&gt; was specified for the question.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="improvement_plan_url" type="string">
      &lt;p&gt;The improvement plan URL for a question in an Amazon Web Services official lenses.&lt;/p&gt; &lt;p&gt;This value is only available if the question has been answered.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_applicable" type="bool">
      &lt;p&gt;Defines whether this question is applicable to a lens review.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="notes" type="string">
      &lt;p&gt;The notes associated with the workload.&lt;/p&gt; &lt;p&gt;For a review template, these are the notes that will be associated with the workload when the template is applied.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="pillar_id" type="string">
      &lt;p&gt;The ID used to identify a pillar, for example, &lt;code&gt;security&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;A pillar is identified by its &lt;a&gt;PillarReviewSummary$PillarId&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="question_description" type="string">
      &lt;p&gt;The description of the question.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="question_id" type="string">
      &lt;p&gt;The ID of the question.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="question_title" type="string">
      &lt;p&gt;The title of the question.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="reason" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="selected_choices" type="list(string)">
      &lt;p&gt;List of selected choice IDs in a question answer.&lt;/p&gt; &lt;p&gt;The values entered replace the previously selected choices.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
