---
title: "aws_wellarchitected_lens_reviews"
description: "`aws_wellarchitected_lens_reviews` reads the real lens reviews within a Well-Architected workload."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wellarchitected "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/wellarchitected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_wellarchitected_lens_reviews"})

    		ubx.Data(wellarchitected.LensReviews, "example", wellarchitected.LensReviewsConfig{
    			WorkloadId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LensReviews } from "@ubx/sdk-aws/aws/data/wellarchitected/lens_reviews";

    export default stack("example", () => {
      intent({ summary: "look up aws_wellarchitected_lens_reviews" });

      data(LensReviews, "example", {
        workloadId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.wellarchitected import LensReviews, LensReviewsConfig

    def describe():
        ubx.intent("look up aws_wellarchitected_lens_reviews")

        ubx.data(LensReviews, "example", LensReviewsConfig(
            workload_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for this request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  &lt;p&gt;The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lens_review_summaries" type="list(object)">
  &lt;p&gt;List of lens summaries of lens reviews of a workload.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="milestone_number" type="number">
  &lt;p&gt;The milestone number.&lt;/p&gt; &lt;p&gt;A workload can have a maximum of 100 milestones.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to use to retrieve the next set of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
