---
title: "aws_license_manager_user_subscriptions_instances"
description: "`aws_license_manager_user_subscriptions_instances` reads the real instances tracked by License Manager User Subscriptions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	license_manager_user_subscriptions "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager_user_subscriptions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_user_subscriptions_instances"})

    		ubx.Data(license_manager_user_subscriptions.LicenseManagerUserSubscriptionsInstances, "example", license_manager_user_subscriptions.LicenseManagerUserSubscriptionsInstancesConfig{
    			Filters:    []map[string]any{{"attribute": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LicenseManagerUserSubscriptionsInstances } from "./local-sdk/aws/sdk/typescript/aws/data/license_manager_user_subscriptions/license_manager_user_subscriptions_instances.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_license_manager_user_subscriptions_instances",
      });

      data(LicenseManagerUserSubscriptionsInstances, "example", {
        filters: [{ attribute: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager_user_subscriptions import LicenseManagerUserSubscriptionsInstances, LicenseManagerUserSubscriptionsInstancesConfig

    def describe():
        ubx.intent("look up aws_license_manager_user_subscriptions_instances")

        ubx.data(LicenseManagerUserSubscriptionsInstances, "example", LicenseManagerUserSubscriptionsInstancesConfig(
            filters=[{"attribute": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_summaries" type="list(object)">
  The real, summarized instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
