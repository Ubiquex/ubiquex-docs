---
title: "aws_license_manager_user_subscriptions_identity_providers"
description: "`aws_license_manager_user_subscriptions_identity_providers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_user_subscriptions_identity_providers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager_user_subscriptions "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager_user_subscriptions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_user_subscriptions_identity_providers"})

    		ubx.Data(license_manager_user_subscriptions.LicenseManagerUserSubscriptionsIdentityProviders, "example", license_manager_user_subscriptions.LicenseManagerUserSubscriptionsIdentityProvidersConfig{
    			Filters:    []map[string]any{{"attribute": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerUserSubscriptionsIdentityProviders } from "@ubx/sdk-aws/aws/data/license_manager_user_subscriptions/license_manager_user_subscriptions_identity_providers";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_license_manager_user_subscriptions_identity_providers",
      });

      data(LicenseManagerUserSubscriptionsIdentityProviders, "example", {
        filters: [{ attribute: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager_user_subscriptions import LicenseManagerUserSubscriptionsIdentityProviders, LicenseManagerUserSubscriptionsIdentityProvidersConfig

    def describe():
        ubx.intent("look up aws_license_manager_user_subscriptions_identity_providers")

        ubx.data(LicenseManagerUserSubscriptionsIdentityProviders, "example", LicenseManagerUserSubscriptionsIdentityProvidersConfig(
            filters=[{"attribute": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="identity_provider_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
