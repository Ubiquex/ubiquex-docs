---
title: "aws_securityhub_standards_control_associations"
description: "`aws_securityhub_standards_control_associations` reads the real association between a Security Hub security control and the standards it belongs to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_standards_control_associations"})

    		ubx.Data(securityhub.StandardsControlAssociations, "example", securityhub.StandardsControlAssociationsConfig{
    			SecurityControlId: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StandardsControlAssociations } from "@ubx/sdk-aws/aws/data/securityhub/standards_control_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_standards_control_associations" });

      data(StandardsControlAssociations, "example", {
        securityControlId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import StandardsControlAssociations, StandardsControlAssociationsConfig

    def describe():
        ubx.intent("look up aws_securityhub_standards_control_associations")

        ubx.data(StandardsControlAssociations, "example", StandardsControlAssociationsConfig(
            security_control_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="security_control_id" type="string" required>
  The real, unique identifier of the security control this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="standards_control_association_summaries" type="list(object)">
  The real, summarized standards control associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
