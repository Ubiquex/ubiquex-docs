---
title: "aws_securityhub_invitations_count"
description: "`aws_securityhub_invitations_count` reads the real count of pending Security Hub member-account invitations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_invitations_count"})

    		ubx.Data(securityhub.InvitationsCount, "example", securityhub.InvitationsCountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InvitationsCount } from "./local-sdk/aws/sdk/typescript/aws/data/securityhub/invitations_count.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_invitations_count" });

      data(InvitationsCount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import InvitationsCount, InvitationsCountConfig

    def describe():
        ubx.intent("look up aws_securityhub_invitations_count")

        ubx.data(InvitationsCount, "example", InvitationsCountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="invitations_count" type="number">
  The real count of invitationss. **(AI-inferred)** Returned by the lookup.
</ResponseField>
