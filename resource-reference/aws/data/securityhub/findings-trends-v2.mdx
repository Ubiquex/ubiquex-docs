---
title: "aws_securityhub_findings_trends_v2"
description: "`aws_securityhub_findings_trends_v2` reads real, historical trend data for Security Hub findings across the new, OCSF-based finding schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_findings_trends_v2"})

    		ubx.Data(securityhub.FindingsTrendsV2, "example", securityhub.FindingsTrendsV2Config{
    			EndTime:    "example",
    			StartTime:  "example",
    			Filters:    map[string]any{"composite_filters": []map[string]any{{"nested_composite_filters": []map[string]any{{}}}}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FindingsTrendsV2 } from "@ubx/sdk-aws/aws/data/securityhub/findings_trends_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_findings_trends_v2" });

      data(FindingsTrendsV2, "example", {
        endTime: "example",
        startTime: "example",
        filters: { compositeFilters: [{ nestedCompositeFilters: [{}] }] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import FindingsTrendsV2, FindingsTrendsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_findings_trends_v2")

        ubx.data(FindingsTrendsV2, "example", FindingsTrendsV2Config(
            end_time="example",
            start_time="example",
            filters={"composite_filters": [{"nested_composite_filters": [{}]}]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;The structure that defines filters to apply to findings trend data queries.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="composite_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="composite_operator" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="granularity" type="string">
  The real time granularity results are aggregated at. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="trends_metrics" type="list(object)">
  The real trend metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
