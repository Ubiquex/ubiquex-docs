---
title: "aws_securityhub_free_trial_statuses_v2"
description: "`aws_securityhub_free_trial_statuses_v2` reads the real, current status of a Security Hub free trial for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_free_trial_statuses_v2"})

    		ubx.Data(securityhub.FreeTrialStatusesV2, "example", securityhub.FreeTrialStatusesV2Config{
    			AccountIds: []string{"example"},
    			MaxResults: 1,
    			Statuses:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FreeTrialStatusesV2 } from "@ubx/sdk-aws/aws/data/securityhub/free_trial_statuses_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_free_trial_statuses_v2" });

      data(FreeTrialStatusesV2, "example", {
        accountIds: ["example"],
        maxResults: 1,
        statuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import FreeTrialStatusesV2, FreeTrialStatusesV2Config

    def describe():
        ubx.intent("look up aws_securityhub_free_trial_statuses_v2")

        ubx.data(FreeTrialStatusesV2, "example", FreeTrialStatusesV2Config(
            account_ids=["example"],
            max_results=1,
            statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statuses" type="list(string)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_free_trial_statuses" type="list(object)">
  The real, current statuses of the account free trials included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
