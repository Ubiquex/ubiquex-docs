---
title: "aws_securityhub_products_v2"
description: "`aws_securityhub_products_v2` looks up a real, third-party product that can send findings into Security Hub under the new, OCSF-based finding schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_products_v2"})

    		ubx.Data(securityhub.ProductsV2, "example", securityhub.ProductsV2Config{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProductsV2 } from "@ubx/sdk-aws/aws/data/securityhub/products_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_products_v2" });

      data(ProductsV2, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ProductsV2, ProductsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_products_v2")

        ubx.data(ProductsV2, "example", ProductsV2Config(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="products_v2" type="list(object)">
  The real, v2 products included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
