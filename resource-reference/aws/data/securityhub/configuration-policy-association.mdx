---
title: "aws_securityhub_configuration_policy_association"
description: "`data_aws_securityhub_configuration_policy_association` looks up a real, already-existing `aws_security_hub_configuration_policy`."
---

It defines a real, centrally managed configuration, which standards and controls are enabled, applied across many accounts in an AWS Organization through Security Hub's own central configuration feature, rather than configuring each account's Security Hub individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_configuration_policy_association"})

    		ubx.Data(securityhub.ConfigurationPolicyAssociation, "example", securityhub.ConfigurationPolicyAssociationConfig{
    			Target: map[string]any{"account_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationPolicyAssociation } from "@ubx/sdk-aws/aws/data/securityhub/configuration_policy_association";

    export default stack("example", () => {
      intent({
        summary: "look up aws_securityhub_configuration_policy_association",
      });

      data(ConfigurationPolicyAssociation, "example", {
        target: { accountId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ConfigurationPolicyAssociation, ConfigurationPolicyAssociationConfig
    from ubx.aws.data.securityhub.configuration_policy_association import ConfigurationPolicyAssociation_Target

    def describe():
        ubx.intent("look up aws_securityhub_configuration_policy_association")

        ubx.data(ConfigurationPolicyAssociation, "example", ConfigurationPolicyAssociationConfig(
            target=ConfigurationPolicyAssociation_Target(account_id="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="target" type="object" required>
  &lt;p&gt; The target account, organizational unit, or the root that is associated with an Security Hub CSPM configuration. The configuration can be a configuration policy or self-managed behavior. &lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="organizational_unit_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="root_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="association_status" type="string">
  The real, current status of the association. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="association_status_message" type="string">
  The real, human-readable message describing the association status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="association_type" type="string">
  The real type of this association. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="configuration_policy_id" type="string">
  The real, unique identifier of the configuration policy this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_id" type="string">
  The real, unique identifier of the target this action applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_type" type="string">
  The real type of the target this action applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
