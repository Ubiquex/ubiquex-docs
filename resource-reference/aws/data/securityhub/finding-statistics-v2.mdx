---
title: "aws_securityhub_finding_statistics_v2"
description: "`aws_securityhub_finding_statistics_v2` reads real, aggregated statistics of Security Hub findings across the new, OCSF-based finding schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_finding_statistics_v2"})

    		ubx.Data(securityhub.FindingStatisticsV2, "example", securityhub.FindingStatisticsV2Config{
    			GroupByRules:        []map[string]any{{"filters": map[string]any{"composite_filters": []map[string]any{{"boolean_filters": []map[string]any{{"field_name": "example-field-name"}}}}}}},
    			MaxStatisticResults: 1,
    			Scopes:              map[string]any{"aws_organizations": []map[string]any{{"organization_id": "example"}}},
    			SortOrder:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FindingStatisticsV2 } from "./local-sdk/aws/sdk/typescript/aws/data/securityhub/finding_statistics_v2.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_finding_statistics_v2" });

      data(FindingStatisticsV2, "example", {
        groupByRules: [{
          filters: {
            compositeFilters: [{
              booleanFilters: [{ fieldName: "example-field-name" }],
            }],
          },
        }],
        maxStatisticResults: 1,
        scopes: { awsOrganizations: [{ organizationId: "example" }] },
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import FindingStatisticsV2, FindingStatisticsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_finding_statistics_v2")

        ubx.data(FindingStatisticsV2, "example", FindingStatisticsV2Config(
            group_by_rules=[{"filters": {"composite_filters": [{"boolean_filters": [{"field_name": "example-field-name"}]}]}}],
            max_statistic_results=1,
            scopes={"aws_organizations": [{"organization_id": "example"}]},
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_by_rules" type="list(object)" required>
  The real rules for the group by. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_statistic_results" type="number">
  The real, maximum number of statistic results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scopes" type="object">
  &lt;p&gt;Defines the data boundary for a findings query. Scopes determine which organizational units or organizations to retrieve data from.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="aws_organizations" type="list(object)">
      &lt;p&gt;A list of Organizations scopes. You can specify up to 10 entries.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_by_results" type="list(object)">
  The real, grouped results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
