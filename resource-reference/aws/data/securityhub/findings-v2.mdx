---
title: "aws_securityhub_findings_v2"
description: "`aws_securityhub_findings_v2` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_findings_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_findings_v2"})

    		ubx.Data(securityhub.FindingsV2, "example", securityhub.FindingsV2Config{
    			Filters:      map[string]any{"composite_filters": []map[string]any{{"boolean_filters": []map[string]any{{"field_name": "example-field-name"}}}}},
    			MaxResults:   1,
    			Scopes:       map[string]any{"aws_organizations": []map[string]any{{"organization_id": "example"}}},
    			SortCriteria: []map[string]any{{"field": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FindingsV2 } from "@ubx/sdk-aws/aws/data/securityhub/findings_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_findings_v2" });

      data(FindingsV2, "example", {
        filters: {
          compositeFilters: [{
            booleanFilters: [{ fieldName: "example-field-name" }],
          }],
        },
        maxResults: 1,
        scopes: { awsOrganizations: [{ organizationId: "example" }] },
        sortCriteria: [{ field: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import FindingsV2, FindingsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_findings_v2")

        ubx.data(FindingsV2, "example", FindingsV2Config(
            filters={"composite_filters": [{"boolean_filters": [{"field_name": "example-field-name"}]}]},
            max_results=1,
            scopes={"aws_organizations": [{"organization_id": "example"}]},
            sort_criteria=[{"field": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Specifies the filtering criteria for security findings using OCSF.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="composite_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="composite_operator" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scopes" type="object">
  &lt;p&gt;Defines the data boundary for a findings query. Scopes determine which organizational units or organizations to retrieve data from.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="aws_organizations" type="list(object)">
      &lt;p&gt;A list of Organizations scopes. You can specify up to 10 entries.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sort_criteria" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(any)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
