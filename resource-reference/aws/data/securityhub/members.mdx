---
title: "aws_securityhub_members"
description: "`aws_securityhub_members` looks up a real Security Hub member account within an administrator's own organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_members"})

    		ubx.Data(securityhub.Members, "example", securityhub.MembersConfig{
    			MaxResults:     1,
    			OnlyAssociated: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Members } from "./local-sdk/aws/sdk/typescript/aws/data/securityhub/members.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_members" });

      data(Members, "example", {
        maxResults: 1,
        onlyAssociated: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import Members, MembersConfig

    def describe():
        ubx.intent("look up aws_securityhub_members")

        ubx.data(Members, "example", MembersConfig(
            max_results=1,
            only_associated=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="only_associated" type="bool">
  Whether the response is restricted to objects that are currently associated. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="members" type="list(object)">
  The real members included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
