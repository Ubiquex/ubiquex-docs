---
title: "aws_securityhub_enabled_standards"
description: "`aws_securityhub_enabled_standards` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_enabled_standards` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_enabled_standards"})

    		ubx.Data(securityhub.EnabledStandards, "example", securityhub.EnabledStandardsConfig{
    			MaxResults:                1,
    			Providers:                 []string{"example"},
    			StandardsSubscriptionArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnabledStandards } from "@ubx/sdk-aws/aws/data/securityhub/enabled_standards";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_enabled_standards" });

      data(EnabledStandards, "example", {
        maxResults: 1,
        providers: ["example"],
        standardsSubscriptionArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import EnabledStandards, EnabledStandardsConfig

    def describe():
        ubx.intent("look up aws_securityhub_enabled_standards")

        ubx.data(EnabledStandards, "example", EnabledStandardsConfig(
            max_results=1,
            providers=["example"],
            standards_subscription_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="providers" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="standards_subscription_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="standards_subscriptions" type="list(object)">
  Returned by the lookup.
</ResponseField>
