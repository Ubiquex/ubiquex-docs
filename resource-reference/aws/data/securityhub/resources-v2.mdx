---
title: "aws_securityhub_resources_v2"
description: "`aws_securityhub_resources_v2` looks up a real resource tracked by Security Hub under the new, OCSF-based schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_resources_v2"})

    		ubx.Data(securityhub.ResourcesV2, "example", securityhub.ResourcesV2Config{
    			Filters:      map[string]any{"composite_filters": []map[string]any{{"date_filters": []map[string]any{{"field_name": "example-field-name"}}}}},
    			MaxResults:   1,
    			Scopes:       map[string]any{"aws_organizations": []map[string]any{{"organization_id": "example"}}},
    			SortCriteria: []map[string]any{{"field": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcesV2 } from "@ubx/sdk-aws/aws/data/securityhub/resources_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_resources_v2" });

      data(ResourcesV2, "example", {
        filters: {
          compositeFilters: [{
            dateFilters: [{ fieldName: "example-field-name" }],
          }],
        },
        maxResults: 1,
        scopes: { awsOrganizations: [{ organizationId: "example" }] },
        sortCriteria: [{ field: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ResourcesV2, ResourcesV2Config

    def describe():
        ubx.intent("look up aws_securityhub_resources_v2")

        ubx.data(ResourcesV2, "example", ResourcesV2Config(
            filters={"composite_filters": [{"date_filters": [{"field_name": "example-field-name"}]}]},
            max_results=1,
            scopes={"aws_organizations": [{"organization_id": "example"}]},
            sort_criteria=[{"field": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Enables filtering of Amazon Web Services resources based on data.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="composite_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="composite_operator" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scopes" type="object">
  &lt;p&gt;Defines the data boundary for a resources query. Scopes determine which organizational units or organizations to retrieve data from.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="aws_organizations" type="list(object)">
      &lt;p&gt;A list of Organizations scopes. You can specify up to 10 entries.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sort_criteria" type="list(object)">
  The real criteria results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The real AWS resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
