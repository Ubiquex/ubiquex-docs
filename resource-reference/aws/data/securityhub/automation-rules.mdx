---
title: "aws_securityhub_automation_rules"
description: "`data_aws_securityhub_automation_rules` looks up a real, already-existing `aws_security_hub_automation_rule`."
---

It defines a real rule that automatically updates matching findings, suppressing, reprioritizing, adding notes, in the original Security Hub model, without a human triaging each one individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_automation_rules"})

    		ubx.Data(securityhub.AutomationRules, "example", securityhub.AutomationRulesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AutomationRules } from "./local-sdk/aws/sdk/typescript/aws/data/securityhub/automation_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_automation_rules" });

      data(AutomationRules, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import AutomationRules, AutomationRulesConfig

    def describe():
        ubx.intent("look up aws_securityhub_automation_rules")

        ubx.data(AutomationRules, "example", AutomationRulesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="automation_rules_metadata" type="list(object)">
  The real, structured metadata of the automation rules. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
