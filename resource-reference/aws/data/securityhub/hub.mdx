---
title: "aws_securityhub_hub"
description: "`aws_securityhub_hub` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_hub` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_hub"})

    		ubx.Data(securityhub.Hub, "example", securityhub.HubConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Hub } from "@ubx/sdk-aws/aws/data/securityhub/hub";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_hub" });

      data(Hub, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import Hub, HubConfig

    def describe():
        ubx.intent("look up aws_securityhub_hub")

        ubx.data(Hub, "example", HubConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="auto_enable_controls" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="control_finding_generator" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="hub_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="subscribed_at" type="string">
  Returned by the lookup.
</ResponseField>
