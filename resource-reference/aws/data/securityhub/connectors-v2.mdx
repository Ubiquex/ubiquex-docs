---
title: "aws_securityhub_connectors_v2"
description: "`aws_securityhub_connectors_v2` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_connectors_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_connectors_v2"})

    		ubx.Data(securityhub.ConnectorsV2, "example", securityhub.ConnectorsV2Config{
    			ConnectorStatus:  "example",
    			EnablementStatus: "example",
    			MaxResults:       1,
    			ProviderName:     "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectorsV2 } from "@ubx/sdk-aws/aws/data/securityhub/connectors_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_connectors_v2" });

      data(ConnectorsV2, "example", {
        connectorStatus: "example",
        enablementStatus: "example",
        maxResults: 1,
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ConnectorsV2, ConnectorsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_connectors_v2")

        ubx.data(ConnectorsV2, "example", ConnectorsV2Config(
            connector_status="example",
            enablement_status="example",
            max_results=1,
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connector_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="enablement_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connectors" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
