---
title: "aws_securityhub_resources_trends_v2"
description: "`aws_securityhub_resources_trends_v2` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_resources_trends_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_resources_trends_v2"})

    		ubx.Data(securityhub.ResourcesTrendsV2, "example", securityhub.ResourcesTrendsV2Config{
    			EndTime:    "example",
    			StartTime:  "example",
    			Filters:    map[string]any{"composite_filters": []map[string]any{{"nested_composite_filters": []map[string]any{{}}}}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcesTrendsV2 } from "@ubx/sdk-aws/aws/data/securityhub/resources_trends_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_resources_trends_v2" });

      data(ResourcesTrendsV2, "example", {
        endTime: "example",
        startTime: "example",
        filters: { compositeFilters: [{ nestedCompositeFilters: [{}] }] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ResourcesTrendsV2, ResourcesTrendsV2Config

    def describe():
        ubx.intent("look up aws_securityhub_resources_trends_v2")

        ubx.data(ResourcesTrendsV2, "example", ResourcesTrendsV2Config(
            end_time="example",
            start_time="example",
            filters={"composite_filters": [{"nested_composite_filters": [{}]}]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;The structure that defines filters to apply to resources trend data queries.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="composite_filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="composite_operator" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="granularity" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="trends_metrics" type="list(object)">
  Returned by the lookup.
</ResponseField>
