---
title: "aws_securityhub_standards_controls"
description: "`aws_securityhub_standards_controls` reads the real, individual controls within a Security Hub security standard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_standards_controls"})

    		ubx.Data(securityhub.StandardsControls, "example", securityhub.StandardsControlsConfig{
    			StandardsSubscriptionArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StandardsControls } from "@ubx/sdk-aws/aws/data/securityhub/standards_controls";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_standards_controls" });

      data(StandardsControls, "example", {
        standardsSubscriptionArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import StandardsControls, StandardsControlsConfig

    def describe():
        ubx.intent("look up aws_securityhub_standards_controls")

        ubx.data(StandardsControls, "example", StandardsControlsConfig(
            standards_subscription_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="standards_subscription_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the standards subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="controls" type="list(object)">
  The real controls included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
