---
title: "aws_securityhub_enabled_products_for_import"
description: "`aws_securityhub_enabled_products_for_import` reads which real third-party security product integrations are currently enabled to import findings into..."
---

`aws_securityhub_enabled_products_for_import` reads which real third-party security product integrations are currently enabled to import findings into Security Hub.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_enabled_products_for_import"})

    		ubx.Data(securityhub.EnabledProductsForImport, "example", securityhub.EnabledProductsForImportConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EnabledProductsForImport } from "./local-sdk/aws/sdk/typescript/aws/data/securityhub/enabled_products_for_import.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_enabled_products_for_import" });

      data(EnabledProductsForImport, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import EnabledProductsForImport, EnabledProductsForImportConfig

    def describe():
        ubx.intent("look up aws_securityhub_enabled_products_for_import")

        ubx.data(EnabledProductsForImport, "example", EnabledProductsForImportConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="product_subscriptions" type="list(string)">
  The real AWS Marketplace product subscriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
