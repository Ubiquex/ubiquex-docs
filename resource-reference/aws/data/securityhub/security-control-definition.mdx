---
title: "aws_securityhub_security_control_definition"
description: "`aws_securityhub_security_control_definition` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityhub_security_control_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_security_control_definition"})

    		ubx.Data(securityhub.SecurityControlDefinition, "example", securityhub.SecurityControlDefinitionConfig{
    			SecurityControlId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityControlDefinition } from "@ubx/sdk-aws/aws/data/securityhub/security_control_definition";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_security_control_definition" });

      data(SecurityControlDefinition, "example", {
        securityControlId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import SecurityControlDefinition, SecurityControlDefinitionConfig

    def describe():
        ubx.intent("look up aws_securityhub_security_control_definition")

        ubx.data(SecurityControlDefinition, "example", SecurityControlDefinitionConfig(
            security_control_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="security_control_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="security_control_definition" type="object">
  &lt;p&gt; Provides metadata for a security control, including its unique standard-agnostic identifier, title, description, severity, availability in Amazon Web Services Regions, and a link to remediation steps. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="current_region_availability" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="customizable_properties" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="parameter_definitions" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="remediation_url" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="security_control_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="severity_rating" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="title" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
