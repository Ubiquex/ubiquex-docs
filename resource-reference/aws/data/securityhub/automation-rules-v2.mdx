---
title: "aws_securityhub_automation_rules_v2"
description: "`data_aws_securityhub_automation_rules_v2` looks up a real, already-existing `aws_security_hub_automation_rule_v2`."
---

It defines a real, equivalent automation rule under Security Hub's next-generation (HubV2) model, launched at re:Invent in December 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_automation_rules_v2"})

    		ubx.Data(securityhub.AutomationRulesV2, "example", securityhub.AutomationRulesV2Config{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutomationRulesV2 } from "@ubx/sdk-aws/aws/data/securityhub/automation_rules_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityhub_automation_rules_v2" });

      data(AutomationRulesV2, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import AutomationRulesV2, AutomationRulesV2Config

    def describe():
        ubx.intent("look up aws_securityhub_automation_rules_v2")

        ubx.data(AutomationRulesV2, "example", AutomationRulesV2Config(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
