---
title: "aws_securityhub_configuration_policy_associations"
description: "`data_aws_securityhub_configuration_policy_associations` looks up a real, already-existing `aws_security_hub_configuration_policy`."
---

It defines a real, centrally managed configuration, which standards and controls are enabled, applied across many accounts in an AWS Organization through Security Hub's own central configuration feature, rather than configuring each account's Security Hub individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityhub_configuration_policy_associations"})

    		ubx.Data(securityhub.ConfigurationPolicyAssociations, "example", securityhub.ConfigurationPolicyAssociationsConfig{
    			Filters:    map[string]any{"association_status": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationPolicyAssociations } from "@ubx/sdk-aws/aws/data/securityhub/configuration_policy_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_securityhub_configuration_policy_associations",
      });

      data(ConfigurationPolicyAssociations, "example", {
        filters: { associationStatus: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityhub import ConfigurationPolicyAssociations, ConfigurationPolicyAssociationsConfig
    from ubx.aws.data.securityhub.configuration_policy_associations import ConfigurationPolicyAssociations_Filters

    def describe():
        ubx.intent("look up aws_securityhub_configuration_policy_associations")

        ubx.data(ConfigurationPolicyAssociations, "example", ConfigurationPolicyAssociationsConfig(
            filters=ConfigurationPolicyAssociations_Filters(association_status="example"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt; Options for filtering the &lt;code&gt;ListConfigurationPolicyAssociations&lt;/code&gt; response. You can filter by the Amazon Resource Name (ARN) or universally unique identifier (UUID) of a configuration policy, &lt;code&gt;AssociationType&lt;/code&gt;, or &lt;code&gt;AssociationStatus&lt;/code&gt;. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="association_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="association_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="configuration_policy_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_policy_association_summaries" type="list(object)">
  The real, summarized configuration policy associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
