---
title: "aws_chime_global_settings"
description: "`aws_chime_global_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_chime_global_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_global_settings"})

    		ubx.Data(chime.GlobalSettings, "example", chime.GlobalSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GlobalSettings } from "@ubx/sdk-aws/aws/data/chime/global_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_global_settings" });

      data(GlobalSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import GlobalSettings, GlobalSettingsConfig

    def describe():
        ubx.intent("look up aws_chime_global_settings")

        ubx.data(GlobalSettings, "example", GlobalSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="business_calling" type="object">
  &lt;p&gt;The Amazon Chime Business Calling settings for the administrator's AWS account. Includes any Amazon S3 buckets designated for storing call detail records.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cdr_bucket" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="voice_connector" type="object">
  &lt;p&gt;The Amazon Chime Voice Connector settings. Includes any Amazon S3 buckets designated for storing call detail records.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cdr_bucket" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
