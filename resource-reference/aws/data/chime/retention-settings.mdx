---
title: "aws_chime_retention_settings"
description: "`aws_chime_retention_settings` reads the real data retention settings of a legacy Amazon Chime account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_retention_settings"})

    		ubx.Data(chime.RetentionSettings, "example", chime.RetentionSettingsConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RetentionSettings } from "./local-sdk/aws/sdk/typescript/aws/data/chime/retention_settings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_retention_settings" });

      data(RetentionSettings, "example", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.chime import RetentionSettings, RetentionSettingsConfig

    def describe():
        ubx.intent("look up aws_chime_retention_settings")

        ubx.data(RetentionSettings, "example", RetentionSettingsConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="initiate_deletion_timestamp" type="string">
  The real timestamp of the initiate deletion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retention_settings" type="object">
  &lt;p&gt;The retention settings for an Amazon Chime Enterprise account that determine how long to retain items such as chat-room messages and chat-conversation messages.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="conversation_retention_settings" type="object">
      &lt;p&gt;The retention settings that determine how long to retain conversation messages for an Amazon Chime Enterprise account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="retention_days" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="room_retention_settings" type="object">
      &lt;p&gt;The retention settings that determine how long to retain chat-room messages for an Amazon Chime Enterprise account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="retention_days" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
