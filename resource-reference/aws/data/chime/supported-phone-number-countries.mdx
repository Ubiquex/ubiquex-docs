---
title: "aws_chime_supported_phone_number_countries"
description: "`aws_chime_supported_phone_number_countries` reads the real countries legacy Amazon Chime supports provisioning phone numbers in."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_supported_phone_number_countries"})

    		ubx.Data(chime.SupportedPhoneNumberCountries, "example", chime.SupportedPhoneNumberCountriesConfig{
    			ProductType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SupportedPhoneNumberCountries } from "./local-sdk/aws/sdk/typescript/aws/data/chime/supported_phone_number_countries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_supported_phone_number_countries" });

      data(SupportedPhoneNumberCountries, "example", {
        productType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.chime import SupportedPhoneNumberCountries, SupportedPhoneNumberCountriesConfig

    def describe():
        ubx.intent("look up aws_chime_supported_phone_number_countries")

        ubx.data(SupportedPhoneNumberCountries, "example", SupportedPhoneNumberCountriesConfig(
            product_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="product_type" type="string" required>
  The real type of this product. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="phone_number_countries" type="list(object)">
  The real countries this phone number can be provisioned in. **(AI-inferred)** Returned by the lookup.
</ResponseField>
