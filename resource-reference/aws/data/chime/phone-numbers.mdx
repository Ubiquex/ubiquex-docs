---
title: "aws_chime_phone_numbers"
description: "`aws_chime_phone_numbers` reads the real phone numbers provisioned through legacy Amazon Chime."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_phone_numbers"})

    		ubx.Data(chime.PhoneNumbers, "example", chime.PhoneNumbersConfig{
    			FilterName:  "example-filter-name",
    			FilterValue: "example",
    			MaxResults:  1,
    			ProductType: "example",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PhoneNumbers } from "@ubx/sdk-aws/aws/data/chime/phone_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_phone_numbers" });

      data(PhoneNumbers, "example", {
        filterName: "example-filter-name",
        filterValue: "example",
        maxResults: 1,
        productType: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import PhoneNumbers, PhoneNumbersConfig

    def describe():
        ubx.intent("look up aws_chime_phone_numbers")

        ubx.data(PhoneNumbers, "example", PhoneNumbersConfig(
            filter_name="example-filter-name",
            filter_value="example",
            max_results=1,
            product_type="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_name" type="string">
  The real name of the filter this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_value" type="string">
  The real value a filter is matched against. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_type" type="string">
  The real type of this product. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  The real phone numbers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
