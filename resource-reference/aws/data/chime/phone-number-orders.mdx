---
title: "aws_chime_phone_number_orders"
description: "`aws_chime_phone_number_orders` reads the real orders placed to provision new legacy Amazon Chime phone numbers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_phone_number_orders"})

    		ubx.Data(chime.PhoneNumberOrders, "example", chime.PhoneNumberOrdersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PhoneNumberOrders } from "@ubx/sdk-aws/aws/data/chime/phone_number_orders";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_phone_number_orders" });

      data(PhoneNumberOrders, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import PhoneNumberOrders, PhoneNumberOrdersConfig

    def describe():
        ubx.intent("look up aws_chime_phone_number_orders")

        ubx.data(PhoneNumberOrders, "example", PhoneNumberOrdersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_number_orders" type="list(object)">
  The real phone number orders included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
