---
title: "aws_chime_rooms"
description: "`aws_chime_rooms` looks up a real chat room on a legacy Amazon Chime account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_rooms"})

    		ubx.Data(chime.Rooms, "example", chime.RoomsConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    			MemberId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Rooms } from "@ubx/sdk-aws/aws/data/chime/rooms";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_rooms" });

      data(Rooms, "example", {
        accountId: "example",
        maxResults: 1,
        memberId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import Rooms, RoomsConfig

    def describe():
        ubx.intent("look up aws_chime_rooms")

        ubx.data(Rooms, "example", RoomsConfig(
            account_id="example",
            max_results=1,
            member_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="member_id" type="string">
  The real, unique identifier of the member this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rooms" type="list(object)">
  The real Amazon Chime rooms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
