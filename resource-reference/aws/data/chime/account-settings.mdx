---
title: "aws_chime_account_settings"
description: "`aws_chime_account_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_chime_account_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_account_settings"})

    		ubx.Data(chime.AccountSettings, "example", chime.AccountSettingsConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountSettings } from "@ubx/sdk-aws/aws/data/chime/account_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_account_settings" });

      data(AccountSettings, "example", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import AccountSettings, AccountSettingsConfig

    def describe():
        ubx.intent("look up aws_chime_account_settings")

        ubx.data(AccountSettings, "example", AccountSettingsConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_settings" type="object">
  &lt;p&gt;Settings related to the Amazon Chime account. This includes settings that start or stop remote control of shared screens, or start or stop the dial-out option in the Amazon Chime web application. For more information about these settings, see &lt;a href="https://docs.aws.amazon.com/chime/latest/ag/policies.html"&gt;Use the Policies Page&lt;/a&gt; in the &lt;i&gt;Amazon Chime Administration Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="disable_remote_control" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enable_dial_out" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
