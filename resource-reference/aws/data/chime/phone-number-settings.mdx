---
title: "aws_chime_phone_number_settings"
description: "`aws_chime_phone_number_settings` reads the real, account-wide default settings for new legacy Amazon Chime phone numbers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_phone_number_settings"})

    		ubx.Data(chime.PhoneNumberSettings, "example", chime.PhoneNumberSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PhoneNumberSettings } from "@ubx/sdk-aws/aws/data/chime/phone_number_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_phone_number_settings" });

      data(PhoneNumberSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import PhoneNumberSettings, PhoneNumberSettingsConfig

    def describe():
        ubx.intent("look up aws_chime_phone_number_settings")

        ubx.data(PhoneNumberSettings, "example", PhoneNumberSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="calling_name" type="string">
  The real name of the calling this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="calling_name_updated_timestamp" type="string">
  The real timestamp of the calling name updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
