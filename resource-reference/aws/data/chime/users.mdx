---
title: "aws_chime_users"
description: "`aws_chime_users` looks up a real user on a legacy Amazon Chime account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_users"})

    		ubx.Data(chime.Users, "example", chime.UsersConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    			UserEmail:  "example",
    			UserType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Users } from "@ubx/sdk-aws/aws/data/chime/users";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_users" });

      data(Users, "example", {
        accountId: "example",
        maxResults: 1,
        userEmail: "example",
        userType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import Users, UsersConfig

    def describe():
        ubx.intent("look up aws_chime_users")

        ubx.data(Users, "example", UsersConfig(
            account_id="example",
            max_results=1,
            user_email="example",
            user_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_email" type="string">
  The real email address of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_type" type="string">
  The real, discriminating type of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
