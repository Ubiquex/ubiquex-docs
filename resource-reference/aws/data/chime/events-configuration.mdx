---
title: "aws_chime_events_configuration"
description: "`aws_chime_events_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_chime_events_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chime_events_configuration"})

    		ubx.Data(chime.EventsConfiguration, "example", chime.EventsConfigurationConfig{
    			AccountId: "example",
    			BotId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EventsConfiguration } from "@ubx/sdk-aws/aws/data/chime/events_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_chime_events_configuration" });

      data(EventsConfiguration, "example", {
        accountId: "example",
        botId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chime import EventsConfiguration, EventsConfigurationConfig

    def describe():
        ubx.intent("look up aws_chime_events_configuration")

        ubx.data(EventsConfiguration, "example", EventsConfigurationConfig(
            account_id="example",
            bot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="events_configuration" type="object">
  &lt;p&gt;The configuration that allows a bot to receive outgoing events. Can be either an HTTPS endpoint or a Lambda function ARN.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bot_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="lambda_function_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="outbound_events_httpsendpoint" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
