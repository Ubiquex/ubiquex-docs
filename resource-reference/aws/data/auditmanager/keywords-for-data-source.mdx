---
title: "aws_auditmanager_keywords_for_data_source"
description: "`aws_auditmanager_keywords_for_data_source` reads the real, suggested search keywords for a given Audit Manager evidence data source type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_keywords_for_data_source"})

    		ubx.Data(auditmanager.KeywordsForDataSource, "example", auditmanager.KeywordsForDataSourceConfig{
    			Source:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeywordsForDataSource } from "@ubx/sdk-aws/aws/data/auditmanager/keywords_for_data_source";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_keywords_for_data_source" });

      data(KeywordsForDataSource, "example", {
        source: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import KeywordsForDataSource, KeywordsForDataSourceConfig

    def describe():
        ubx.intent("look up aws_auditmanager_keywords_for_data_source")

        ubx.data(KeywordsForDataSource, "example", KeywordsForDataSourceConfig(
            source="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string" required>
  The real source this object was derived from. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="keywords" type="list(string)">
  The real keywords included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
