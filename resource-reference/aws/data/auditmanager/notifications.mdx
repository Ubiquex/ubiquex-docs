---
title: "aws_auditmanager_notifications"
description: "`aws_auditmanager_notifications` reads real, system-generated notifications for Audit Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_notifications"})

    		ubx.Data(auditmanager.Notifications, "example", auditmanager.NotificationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Notifications } from "@ubx/sdk-aws/aws/data/auditmanager/notifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_notifications" });

      data(Notifications, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import Notifications, NotificationsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_notifications")

        ubx.data(Notifications, "example", NotificationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notifications" type="list(object)">
  The real notifications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
