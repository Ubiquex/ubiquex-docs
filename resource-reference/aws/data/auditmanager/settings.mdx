---
title: "aws_auditmanager_settings"
description: "`aws_auditmanager_settings` reads the real, account-wide settings (default S3 export destination, SNS topic) for Audit Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_settings"})

    		ubx.Data(auditmanager.Settings, "example", auditmanager.SettingsConfig{
    			Attribute: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Settings } from "@ubx/sdk-aws/aws/data/auditmanager/settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_settings" });

      data(Settings, "example", {
        attribute: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import Settings, SettingsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_settings")

        ubx.data(Settings, "example", SettingsConfig(
            attribute="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute" type="string" required>
  The real attribute this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="settings" type="object">
  &lt;p&gt; The settings object that holds all supported Audit Manager settings. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_assessment_reports_destination" type="object">
      &lt;p&gt; The location where Audit Manager saves assessment reports for the given assessment. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="destination" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="destination_type" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_export_destination" type="object">
      &lt;p&gt;The default s3 bucket where Audit Manager saves the files that you export from evidence finder.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="destination" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="destination_type" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_process_owners" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="deregistration_policy" type="object">
      &lt;p&gt;The deregistration policy for the data that's stored in Audit Manager. You can use this attribute to determine how your data is handled when you &lt;a href="https://docs.aws.amazon.com/audit-manager/latest/APIReference/API_DeregisterAccount.html"&gt;deregister Audit Manager&lt;/a&gt;.&lt;/p&gt; &lt;p&gt;By default, Audit Manager retains evidence data for two years from the time of its creation. Other Audit Manager resources (including assessments, custom controls, and custom frameworks) remain in Audit Manager indefinitely, and are available if you &lt;a href="https://docs.aws.amazon.com/audit-manager/latest/APIReference/API_RegisterAccount.html"&gt;re-register Audit Manager&lt;/a&gt; in the future. For more information about data retention, see &lt;a href="https://docs.aws.amazon.com/audit-manager/latest/userguide/data-protection.html"&gt;Data Protection&lt;/a&gt; in the &lt;i&gt;Audit Manager User Guide&lt;/i&gt;.&lt;/p&gt; &lt;important&gt; &lt;p&gt;If you choose to delete all data, this action permanently deletes all evidence data in your account within seven days. It also deletes all of the Audit Manager resources that you created, including assessments, custom controls, and custom frameworks. Your data will not be available if you re-register Audit Manager in the future.&lt;/p&gt; &lt;/important&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delete_resources" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="evidence_finder_enablement" type="object">
      &lt;p&gt;The settings object that specifies whether evidence finder is enabled. This object also describes the related event data store, and the backfill status for populating the event data store with evidence data.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="backfill_status" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="enablement_status" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="error" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="event_data_store_arn" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_aws_org_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="kms_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sns_topic" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
