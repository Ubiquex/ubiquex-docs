---
title: "aws_auditmanager_evidence"
description: "`aws_auditmanager_evidence` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_auditmanager_evidence` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_evidence"})

    		ubx.Data(auditmanager.Evidence, "example", auditmanager.EvidenceConfig{
    			AssessmentId:     "example",
    			ControlSetId:     "example",
    			EvidenceFolderId: "example",
    			EvidenceId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Evidence } from "@ubx/sdk-aws/aws/data/auditmanager/evidence";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_evidence" });

      data(Evidence, "example", {
        assessmentId: "example",
        controlSetId: "example",
        evidenceFolderId: "example",
        evidenceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import Evidence, EvidenceConfig

    def describe():
        ubx.intent("look up aws_auditmanager_evidence")

        ubx.data(Evidence, "example", EvidenceConfig(
            assessment_id="example",
            control_set_id="example",
            evidence_folder_id="example",
            evidence_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="control_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="evidence_folder_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="evidence_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evidence" type="object">
  &lt;p&gt; A record that contains the information needed to demonstrate compliance with the requirements specified by a control. Examples of evidence include change activity invoked by a user, or a system configuration snapshot. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="assessment_report_selection" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="attributes" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="aws_account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="aws_organization" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="compliance_check" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="data_source" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="event_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="event_source" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="evidence_aws_account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="evidence_by_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="evidence_folder_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resources_included" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
