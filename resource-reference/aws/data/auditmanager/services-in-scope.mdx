---
title: "aws_auditmanager_services_in_scope"
description: "`aws_auditmanager_services_in_scope` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_auditmanager_services_in_scope` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_services_in_scope"})

    		ubx.Data(auditmanager.ServicesInScope, "example", auditmanager.ServicesInScopeConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicesInScope } from "@ubx/sdk-aws/aws/data/auditmanager/services_in_scope";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_services_in_scope" });

      data(ServicesInScope, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import ServicesInScope, ServicesInScopeConfig

    def describe():
        ubx.intent("look up aws_auditmanager_services_in_scope")

        ubx.data(ServicesInScope, "example", ServicesInScopeConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="service_metadata" type="list(object)">
  Returned by the lookup.
</ResponseField>
