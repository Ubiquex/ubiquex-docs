---
title: "aws_auditmanager_assessment_framework_share_requests"
description: "`aws_auditmanager_assessment_framework_share_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_auditmanager_assessment_framework_share_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_assessment_framework_share_requests"})

    		ubx.Data(auditmanager.AssessmentFrameworkShareRequests, "example", auditmanager.AssessmentFrameworkShareRequestsConfig{
    			RequestType: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssessmentFrameworkShareRequests } from "@ubx/sdk-aws/aws/data/auditmanager/assessment_framework_share_requests";

    export default stack("example", () => {
      intent({
        summary: "look up aws_auditmanager_assessment_framework_share_requests",
      });

      data(AssessmentFrameworkShareRequests, "example", {
        requestType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import AssessmentFrameworkShareRequests, AssessmentFrameworkShareRequestsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_assessment_framework_share_requests")

        ubx.data(AssessmentFrameworkShareRequests, "example", AssessmentFrameworkShareRequestsConfig(
            request_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

<ResponseField name="request_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_framework_share_requests" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
