---
title: "aws_auditmanager_control_insights_by_control_domain"
description: "`aws_auditmanager_control_insights_by_control_domain` reads real, aggregated compliance insights for the controls within a control domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_control_insights_by_control_domain"})

    		ubx.Data(auditmanager.ControlInsightsByControlDomain, "example", auditmanager.ControlInsightsByControlDomainConfig{
    			ControlDomainId: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlInsightsByControlDomain } from "@ubx/sdk-aws/aws/data/auditmanager/control_insights_by_control_domain";

    export default stack("example", () => {
      intent({
        summary: "look up aws_auditmanager_control_insights_by_control_domain",
      });

      data(ControlInsightsByControlDomain, "example", {
        controlDomainId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import ControlInsightsByControlDomain, ControlInsightsByControlDomainConfig

    def describe():
        ubx.intent("look up aws_auditmanager_control_insights_by_control_domain")

        ubx.data(ControlInsightsByControlDomain, "example", ControlInsightsByControlDomainConfig(
            control_domain_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="control_domain_id" type="string" required>
  The real, unique identifier of the control domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="control_insights_metadata" type="list(object)">
  The real, structured metadata of the control insights. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
