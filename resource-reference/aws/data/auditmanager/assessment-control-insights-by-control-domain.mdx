---
title: "aws_auditmanager_assessment_control_insights_by_control_domain"
description: "`aws_auditmanager_assessment_control_insights_by_control_domain` reads real, aggregated compliance insights for the controls within a control domain..."
---

`aws_auditmanager_assessment_control_insights_by_control_domain` reads real, aggregated compliance insights for the controls within a control domain, scoped to one assessment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_assessment_control_insights_by_control_domain"})

    		ubx.Data(auditmanager.AssessmentControlInsightsByControlDomain, "example", auditmanager.AssessmentControlInsightsByControlDomainConfig{
    			AssessmentId:    "example",
    			ControlDomainId: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AssessmentControlInsightsByControlDomain } from "./local-sdk/aws/sdk/typescript/aws/data/auditmanager/assessment_control_insights_by_control_domain.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_auditmanager_assessment_control_insights_by_control_domain",
      });

      data(AssessmentControlInsightsByControlDomain, "example", {
        assessmentId: "example",
        controlDomainId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import AssessmentControlInsightsByControlDomain, AssessmentControlInsightsByControlDomainConfig

    def describe():
        ubx.intent("look up aws_auditmanager_assessment_control_insights_by_control_domain")

        ubx.data(AssessmentControlInsightsByControlDomain, "example", AssessmentControlInsightsByControlDomainConfig(
            assessment_id="example",
            control_domain_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="control_domain_id" type="string" required>
  The real, unique identifier of the control domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="control_insights_by_assessment" type="list(object)">
  The real, per-assessment insights for this AWS Audit Manager control. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
