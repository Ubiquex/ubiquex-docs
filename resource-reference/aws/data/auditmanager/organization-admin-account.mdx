---
title: "aws_auditmanager_organization_admin_account"
description: "`aws_auditmanager_organization_admin_account` reads the real account designated as the Audit Manager delegated administrator for an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_organization_admin_account"})

    		ubx.Data(auditmanager.OrganizationAdminAccount, "example", auditmanager.OrganizationAdminAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationAdminAccount } from "./local-sdk/aws/sdk/typescript/aws/data/auditmanager/organization_admin_account.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_organization_admin_account" });

      data(OrganizationAdminAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import OrganizationAdminAccount, OrganizationAdminAccountConfig

    def describe():
        ubx.intent("look up aws_auditmanager_organization_admin_account")

        ubx.data(OrganizationAdminAccount, "example", OrganizationAdminAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="admin_account_id" type="string">
  The real, unique identifier of the admin account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_id" type="string">
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
