---
title: "aws_auditmanager_evidence_by_evidence_folder"
description: "`aws_auditmanager_evidence_by_evidence_folder` reads the real evidence collected within a specific Audit Manager evidence folder."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_evidence_by_evidence_folder"})

    		ubx.Data(auditmanager.EvidenceByEvidenceFolder, "example", auditmanager.EvidenceByEvidenceFolderConfig{
    			AssessmentId:     "example",
    			ControlSetId:     "example",
    			EvidenceFolderId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EvidenceByEvidenceFolder } from "./local-sdk/aws/sdk/typescript/aws/data/auditmanager/evidence_by_evidence_folder.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_evidence_by_evidence_folder" });

      data(EvidenceByEvidenceFolder, "example", {
        assessmentId: "example",
        controlSetId: "example",
        evidenceFolderId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import EvidenceByEvidenceFolder, EvidenceByEvidenceFolderConfig

    def describe():
        ubx.intent("look up aws_auditmanager_evidence_by_evidence_folder")

        ubx.data(EvidenceByEvidenceFolder, "example", EvidenceByEvidenceFolderConfig(
            assessment_id="example",
            control_set_id="example",
            evidence_folder_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="control_set_id" type="string" required>
  The real, unique identifier of the control set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="evidence_folder_id" type="string" required>
  The real, unique identifier of the evidence folder this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evidence" type="list(object)">
  The real evidence included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
