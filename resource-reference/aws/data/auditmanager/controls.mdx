---
title: "aws_auditmanager_controls"
description: "`aws_auditmanager_controls` looks up a real Audit Manager control, a defined check mapping to a compliance requirement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_controls"})

    		ubx.Data(auditmanager.Controls, "example", auditmanager.ControlsConfig{
    			ControlType:      "example",
    			ControlCatalogId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Controls } from "./local-sdk/aws/sdk/typescript/aws/data/auditmanager/controls.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_controls" });

      data(Controls, "example", {
        controlType: "example",
        controlCatalogId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import Controls, ControlsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_controls")

        ubx.data(Controls, "example", ControlsConfig(
            control_type="example",
            control_catalog_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="control_catalog_id" type="string">
  The real, unique identifier of the control catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="control_type" type="string" required>
  The real, discriminating type of the control this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="control_metadata_list" type="list(object)">
  The real control metadatas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
