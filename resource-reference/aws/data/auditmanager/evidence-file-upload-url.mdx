---
title: "aws_auditmanager_evidence_file_upload_url"
description: "`aws_auditmanager_evidence_file_upload_url` reads a real, generated URL for uploading manual evidence to an Audit Manager assessment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_evidence_file_upload_url"})

    		ubx.Data(auditmanager.EvidenceFileUploadUrl, "example", auditmanager.EvidenceFileUploadUrlConfig{
    			FileName: "example-file-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EvidenceFileUploadUrl } from "@ubx/sdk-aws/aws/data/auditmanager/evidence_file_upload_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_evidence_file_upload_url" });

      data(EvidenceFileUploadUrl, "example", {
        fileName: "example-file-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import EvidenceFileUploadUrl, EvidenceFileUploadUrlConfig

    def describe():
        ubx.intent("look up aws_auditmanager_evidence_file_upload_url")

        ubx.data(EvidenceFileUploadUrl, "example", EvidenceFileUploadUrlConfig(
            file_name="example-file-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_name" type="string" required>
  The real name of the file this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evidence_file_name" type="string">
  The real name of the evidence file this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="upload_url" type="string">
  The real, resolvable URL of the upload this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
