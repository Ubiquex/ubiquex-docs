---
title: "aws_auditmanager_control_domain_insights"
description: "`aws_auditmanager_control_domain_insights` reads real, aggregated compliance insights across every control domain on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_control_domain_insights"})

    		ubx.Data(auditmanager.ControlDomainInsights, "example", auditmanager.ControlDomainInsightsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ControlDomainInsights } from "./local-sdk/aws/sdk/typescript/aws/data/auditmanager/control_domain_insights.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_control_domain_insights" });

      data(ControlDomainInsights, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import ControlDomainInsights, ControlDomainInsightsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_control_domain_insights")

        ubx.data(ControlDomainInsights, "example", ControlDomainInsightsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="control_domain_insights" type="list(object)">
  The real, domain-level insights for this AWS Audit Manager control. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
