---
title: "aws_auditmanager_assessment_reports"
description: "`aws_auditmanager_assessment_reports` looks up a real, generated Audit Manager assessment report."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_assessment_reports"})

    		ubx.Data(auditmanager.AssessmentReports, "example", auditmanager.AssessmentReportsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssessmentReports } from "@ubx/sdk-aws/aws/data/auditmanager/assessment_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_assessment_reports" });

      data(AssessmentReports, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import AssessmentReports, AssessmentReportsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_assessment_reports")

        ubx.data(AssessmentReports, "example", AssessmentReportsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_reports" type="list(object)">
  The real reports for the assessment. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
