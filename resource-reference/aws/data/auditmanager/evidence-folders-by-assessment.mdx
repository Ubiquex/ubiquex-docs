---
title: "aws_auditmanager_evidence_folders_by_assessment"
description: "`aws_auditmanager_evidence_folders_by_assessment` reads the real evidence folders within an Audit Manager assessment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_evidence_folders_by_assessment"})

    		ubx.Data(auditmanager.EvidenceFoldersByAssessment, "example", auditmanager.EvidenceFoldersByAssessmentConfig{
    			AssessmentId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EvidenceFoldersByAssessment } from "@ubx/sdk-aws/aws/data/auditmanager/evidence_folders_by_assessment";

    export default stack("example", () => {
      intent({
        summary: "look up aws_auditmanager_evidence_folders_by_assessment",
      });

      data(EvidenceFoldersByAssessment, "example", {
        assessmentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import EvidenceFoldersByAssessment, EvidenceFoldersByAssessmentConfig

    def describe():
        ubx.intent("look up aws_auditmanager_evidence_folders_by_assessment")

        ubx.data(EvidenceFoldersByAssessment, "example", EvidenceFoldersByAssessmentConfig(
            assessment_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evidence_folders" type="list(object)">
  The real AWS Audit Manager evidence folders included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
