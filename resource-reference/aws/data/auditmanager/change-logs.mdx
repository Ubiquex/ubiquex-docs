---
title: "aws_auditmanager_change_logs"
description: "`aws_auditmanager_change_logs` reads the real, recorded changes made to an Audit Manager assessment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_change_logs"})

    		ubx.Data(auditmanager.ChangeLogs, "example", auditmanager.ChangeLogsConfig{
    			AssessmentId: "example",
    			ControlId:    "example",
    			ControlSetId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChangeLogs } from "@ubx/sdk-aws/aws/data/auditmanager/change_logs";

    export default stack("example", () => {
      intent({ summary: "look up aws_auditmanager_change_logs" });

      data(ChangeLogs, "example", {
        assessmentId: "example",
        controlId: "example",
        controlSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import ChangeLogs, ChangeLogsConfig

    def describe():
        ubx.intent("look up aws_auditmanager_change_logs")

        ubx.data(ChangeLogs, "example", ChangeLogsConfig(
            assessment_id="example",
            control_id="example",
            control_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="control_id" type="string">
  The real, unique identifier of the control this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="control_set_id" type="string">
  The real, unique identifier of the control set this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_logs" type="list(object)">
  The real change logs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
