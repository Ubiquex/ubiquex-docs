---
title: "aws_auditmanager_evidence_folders_by_assessment_control"
description: "`aws_auditmanager_evidence_folders_by_assessment_control` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_auditmanager_evidence_folders_by_assessment_control` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_auditmanager_evidence_folders_by_assessment_control"})

    		ubx.Data(auditmanager.EvidenceFoldersByAssessmentControl, "example", auditmanager.EvidenceFoldersByAssessmentControlConfig{
    			AssessmentId: "example",
    			ControlId:    "example",
    			ControlSetId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EvidenceFoldersByAssessmentControl } from "@ubx/sdk-aws/aws/data/auditmanager/evidence_folders_by_assessment_control";

    export default stack("example", () => {
      intent({
        summary: "look up aws_auditmanager_evidence_folders_by_assessment_control",
      });

      data(EvidenceFoldersByAssessmentControl, "example", {
        assessmentId: "example",
        controlId: "example",
        controlSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.auditmanager import EvidenceFoldersByAssessmentControl, EvidenceFoldersByAssessmentControlConfig

    def describe():
        ubx.intent("look up aws_auditmanager_evidence_folders_by_assessment_control")

        ubx.data(EvidenceFoldersByAssessmentControl, "example", EvidenceFoldersByAssessmentControlConfig(
            assessment_id="example",
            control_id="example",
            control_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="control_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="control_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max results in the page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evidence_folders" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
