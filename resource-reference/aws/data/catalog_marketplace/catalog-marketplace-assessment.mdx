---
title: "aws_catalog_marketplace_assessment"
description: "`aws_catalog_marketplace_assessment` looks up one real AWS Marketplace Catalog assessment, validating a proposed listing change."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_assessment"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceAssessment, "example", catalog_marketplace.CatalogMarketplaceAssessmentConfig{
    			AssessmentIdentifier: "example",
    			Catalog:              "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceAssessment } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_assessment";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_assessment" });

      data(CatalogMarketplaceAssessment, "example", {
        assessmentIdentifier: "example",
        catalog: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceAssessment, CatalogMarketplaceAssessmentConfig

    def describe():
        ubx.intent("look up aws_catalog_marketplace_assessment")

        ubx.data(CatalogMarketplaceAssessment, "example", CatalogMarketplaceAssessmentConfig(
            assessment_identifier="example",
            catalog="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_identifier" type="string" required>
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_arn" type="string">
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="assessment_id" type="string">
  The real, unique identifier of the assessment this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="assessment_result" type="string">
  The real result of this assessment. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="assessment_target_summary" type="object">
  &lt;p&gt;Identifies the entity or change set that was assessed.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="change_set_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="entity_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="control_assessments" type="list(object)">
  The real control assessments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expires_at" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="framework_id" type="string">
  The real, unique identifier of the framework this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="framework_summary" type="object">
  &lt;p&gt;The framework-specific details of the assessed resource. Exactly one member is set, corresponding to the framework that was assessed.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="amisecurity_summary" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delivery_option_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_security_summary" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delivery_option_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
