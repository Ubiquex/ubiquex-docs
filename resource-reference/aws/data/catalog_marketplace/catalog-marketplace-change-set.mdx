---
title: "aws_catalog_marketplace_change_set"
description: "`aws_catalog_marketplace_change_set` looks up one real AWS Marketplace Catalog change set, applying a batch of listing changes."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_change_set"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceChangeSet, "example", catalog_marketplace.CatalogMarketplaceChangeSetConfig{
    			Catalog:     "example",
    			ChangeSetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceChangeSet } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_change_set";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_change_set" });

      data(CatalogMarketplaceChangeSet, "example", {
        catalog: "example",
        changeSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceChangeSet, CatalogMarketplaceChangeSetConfig

    def describe():
        ubx.intent("look up aws_catalog_marketplace_change_set")

        ubx.data(CatalogMarketplaceChangeSet, "example", CatalogMarketplaceChangeSetConfig(
            catalog="example",
            change_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="change_set_id" type="string" required>
  The real, unique identifier of the change set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_set" type="list(object)">
  The real change set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="change_set_arn" type="string">
  The real Amazon Resource Name (ARN) of the change set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="change_set_name" type="string">
  The real name of the change set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_code" type="string">
  The real code identifying the reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_description" type="string">
  The real, free-text description of the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="intent" type="string">
  The real Amazon Lex intent this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
