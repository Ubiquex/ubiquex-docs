---
title: "aws_catalog_marketplace_change_set"
description: "`aws_catalog_marketplace_change_set` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_catalog_marketplace_change_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_change_set"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceChangeSet, "example", catalog_marketplace.CatalogMarketplaceChangeSetConfig{
    			Catalog:     "example",
    			ChangeSetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceChangeSet } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_change_set";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_change_set" });

      data(CatalogMarketplaceChangeSet, "example", {
        catalog: "example",
        changeSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceChangeSet, CatalogMarketplaceChangeSetConfig

    def describe():
        ubx.intent("look up aws_catalog_marketplace_change_set")

        ubx.data(CatalogMarketplaceChangeSet, "example", CatalogMarketplaceChangeSetConfig(
            catalog="example",
            change_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="change_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_set" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="change_set_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="change_set_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_code" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="intent" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
