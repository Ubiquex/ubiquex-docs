---
title: "aws_catalog_marketplace_change_sets"
description: "`aws_catalog_marketplace_change_sets` reads the real change sets submitted to AWS Marketplace Catalog."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_change_sets"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceChangeSets, "example", catalog_marketplace.CatalogMarketplaceChangeSetsConfig{
    			Catalog:    "example",
    			FilterList: []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			Sort:       map[string]any{"sort_by": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceChangeSets } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_change_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_change_sets" });

      data(CatalogMarketplaceChangeSets, "example", {
        catalog: "example",
        filterList: [{ name: "example-name" }],
        maxResults: 1,
        sort: { sortBy: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceChangeSets, CatalogMarketplaceChangeSetsConfig
    from ubx.aws.data.catalog_marketplace.catalog_marketplace_change_sets import CatalogMarketplaceChangeSets_FilterList, CatalogMarketplaceChangeSets_Sort

    def describe():
        ubx.intent("look up aws_catalog_marketplace_change_sets")

        ubx.data(CatalogMarketplaceChangeSets, "example", CatalogMarketplaceChangeSetsConfig(
            catalog="example",
            filter_list=[CatalogMarketplaceChangeSets_FilterList(name="example-name")],
            max_results=1,
            sort=CatalogMarketplaceChangeSets_Sort(sort_by="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_list" type="list(object)">
  The real filters included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="object">
  &lt;p&gt;An object that contains two attributes, &lt;code&gt;SortBy&lt;/code&gt; and &lt;code&gt;SortOrder&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="sort_by" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sort_order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="change_set_summary_list" type="list(object)">
  The real change set summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
