---
title: "aws_catalog_marketplace_assessments"
description: "`aws_catalog_marketplace_assessments` reads the real assessments run against AWS Marketplace Catalog listings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_assessments"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceAssessments, "example", catalog_marketplace.CatalogMarketplaceAssessmentsConfig{
    			Catalog:                "example",
    			AssessmentTargetFilter: map[string]any{"change_set_id": "example"},
    			FrameworkFilters:       map[string]any{"amisecurity_filters": map[string]any{"delivery_option_id": "example"}},
    			FrameworkId:            "example",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceAssessments } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_assessments";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_assessments" });

      data(CatalogMarketplaceAssessments, "example", {
        catalog: "example",
        assessmentTargetFilter: { changeSetId: "example" },
        frameworkFilters: { amisecurityFilters: { deliveryOptionId: "example" } },
        frameworkId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceAssessments, CatalogMarketplaceAssessmentsConfig

    def describe():
        ubx.intent("look up aws_catalog_marketplace_assessments")

        ubx.data(CatalogMarketplaceAssessments, "example", CatalogMarketplaceAssessmentsConfig(
            catalog="example",
            assessment_target_filter={"change_set_id": "example"},
            framework_filters={"amisecurity_filters": {"delivery_option_id": "example"}},
            framework_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_target_filter" type="object">
  &lt;p&gt;Filters assessment list results by the resource that was assessed. Provide an entity identifier, a change set identifier, or both.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="change_set_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="entity_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="framework_filters" type="object">
  &lt;p&gt;Framework-specific filters used to scope &lt;code&gt;ListAssessments&lt;/code&gt; results. Set exactly one member, corresponding to the framework you want to filter by.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="amisecurity_filters" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delivery_option_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_security_filters" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delivery_option_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="framework_id" type="string">
  The real, unique identifier of the framework this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_summary_list" type="list(object)">
  The real assessment summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
