---
title: "aws_catalog_marketplace_entity"
description: "`aws_catalog_marketplace_entity` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_catalog_marketplace_entity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	catalog_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/catalog_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_catalog_marketplace_entity"})

    		ubx.Data(catalog_marketplace.CatalogMarketplaceEntity, "example", catalog_marketplace.CatalogMarketplaceEntityConfig{
    			Catalog:  "example",
    			EntityId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogMarketplaceEntity } from "@ubx/sdk-aws/aws/data/catalog_marketplace/catalog_marketplace_entity";

    export default stack("example", () => {
      intent({ summary: "look up aws_catalog_marketplace_entity" });

      data(CatalogMarketplaceEntity, "example", {
        catalog: "example",
        entityId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.catalog_marketplace import CatalogMarketplaceEntity, CatalogMarketplaceEntityConfig

    def describe():
        ubx.intent("look up aws_catalog_marketplace_entity")

        ubx.data(CatalogMarketplaceEntity, "example", CatalogMarketplaceEntityConfig(
            catalog="example",
            entity_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="entity_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="details" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="details_document" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="entity_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="entity_identifier" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="entity_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  Returned by the lookup.
</ResponseField>
