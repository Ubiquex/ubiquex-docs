---
title: "aws_health_agent_subscriptions"
description: "`aws_health_agent_subscriptions` reads the real subscriptions configured for an AWS HealthScribe agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health_agent "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/health_agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_agent_subscriptions"})

    		ubx.Data(health_agent.HealthAgentSubscriptions, "example", health_agent.HealthAgentSubscriptionsConfig{
    			DomainId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthAgentSubscriptions } from "@ubx/sdk-aws/aws/data/health_agent/health_agent_subscriptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_agent_subscriptions" });

      data(HealthAgentSubscriptions, "example", {
        domainId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health_agent import HealthAgentSubscriptions, HealthAgentSubscriptionsConfig

    def describe():
        ubx.intent("look up aws_health_agent_subscriptions")

        ubx.data(HealthAgentSubscriptions, "example", HealthAgentSubscriptionsConfig(
            domain_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscriptions" type="list(object)">
  The real subscriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
