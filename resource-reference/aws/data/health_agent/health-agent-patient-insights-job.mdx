---
title: "aws_health_agent_patient_insights_job"
description: "`aws_health_agent_patient_insights_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_health_agent_patient_insights_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health_agent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health_agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_agent_patient_insights_job"})

    		ubx.Data(health_agent.HealthAgentPatientInsightsJob, "example", health_agent.HealthAgentPatientInsightsJobConfig{
    			DomainId: "example",
    			JobId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthAgentPatientInsightsJob } from "@ubx/sdk-aws/aws/data/health_agent/health_agent_patient_insights_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_agent_patient_insights_job" });

      data(HealthAgentPatientInsightsJob, "example", {
        domainId: "example",
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health_agent import HealthAgentPatientInsightsJob, HealthAgentPatientInsightsJobConfig

    def describe():
        ubx.intent("look up aws_health_agent_patient_insights_job")

        ubx.data(HealthAgentPatientInsightsJob, "example", HealthAgentPatientInsightsJobConfig(
            domain_id="example",
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="encounter_context" type="object">
  &lt;p&gt;Details for an encounter&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="encounter_reason" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="input_data_config" type="object">
  &lt;p&gt;Configuration details for input patient data&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="fhir_server" type="object">
      &lt;p&gt;FHIR server configuration for input data source&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="fhir_endpoint" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="oauth_token" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="s3_sources" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="insights_context" type="object">
  &lt;p&gt;Details for insights that user wants to generate&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="insights_type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="insights_output" type="object">
  &lt;p&gt;Output of patient insights job&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="uri" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="output_data_config" type="object">
  &lt;p&gt;Configuration details for insights output.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_output_path" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="patient_context" type="object">
  &lt;p&gt;Details for a patient&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="date_of_birth" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="patient_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="pronouns" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status_details" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="updated_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_context" type="object">
  &lt;p&gt;Details for user initiating insights job&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="role" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="specialty" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
