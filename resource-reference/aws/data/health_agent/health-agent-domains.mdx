---
title: "aws_health_agent_domains"
description: "`aws_health_agent_domains` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_health_agent_domains` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health_agent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health_agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_agent_domains"})

    		ubx.Data(health_agent.HealthAgentDomains, "example", health_agent.HealthAgentDomainsConfig{
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthAgentDomains } from "@ubx/sdk-aws/aws/data/health_agent/health_agent_domains";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_agent_domains" });

      data(HealthAgentDomains, "example", {
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health_agent import HealthAgentDomains, HealthAgentDomainsConfig

    def describe():
        ubx.intent("look up aws_health_agent_domains")

        ubx.data(HealthAgentDomains, "example", HealthAgentDomainsConfig(
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domains" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
