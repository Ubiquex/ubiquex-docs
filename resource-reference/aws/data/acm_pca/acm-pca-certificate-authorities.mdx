---
title: "aws_acm_pca_certificate_authorities"
description: "`data_aws_acm_pca_certificate_authorities` looks up a real, already-existing `aws_acmpca_certificate_authority`."
---

It creates a private certificate authority, root or subordinate, that issues and revokes X.509 certificates for internal PKI. It's regionally scoped, integrates with services like ACM and IoT Core for automated certificate issuance, and its private key never leaves AWS's managed HSM boundary.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm_pca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm_pca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_pca_certificate_authorities"})

    		ubx.Data(acm_pca.AcmPcaCertificateAuthorities, "example", acm_pca.AcmPcaCertificateAuthoritiesConfig{
    			MaxResults:    1,
    			ResourceOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmPcaCertificateAuthorities } from "@ubx/sdk-aws/aws/data/acm_pca/acm_pca_certificate_authorities";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_pca_certificate_authorities" });

      data(AcmPcaCertificateAuthorities, "example", {
        maxResults: 1,
        resourceOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm_pca import AcmPcaCertificateAuthorities, AcmPcaCertificateAuthoritiesConfig

    def describe():
        ubx.intent("look up aws_acm_pca_certificate_authorities")

        ubx.data(AcmPcaCertificateAuthorities, "example", AcmPcaCertificateAuthoritiesConfig(
            max_results=1,
            resource_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  The real owner of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate_authorities" type="list(object)">
  The real certificate authorities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
