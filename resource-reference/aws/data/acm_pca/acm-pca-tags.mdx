---
title: "aws_acm_pca_tags"
description: "`aws_acm_pca_tags` reads the real tags applied to an ACM Private Certificate Authority."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm_pca "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/acm_pca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_pca_tags"})

    		ubx.Data(acm_pca.AcmPcaTags, "example", acm_pca.AcmPcaTagsConfig{
    			CertificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmPcaTags } from "@ubx/sdk-aws/aws/data/acm_pca/acm_pca_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_pca_tags" });

      data(AcmPcaTags, "example", {
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm_pca import AcmPcaTags, AcmPcaTagsConfig

    def describe():
        ubx.intent("look up aws_acm_pca_tags")

        ubx.data(AcmPcaTags, "example", AcmPcaTagsConfig(
            certificate_authority_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_authority_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the certificate authority this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
