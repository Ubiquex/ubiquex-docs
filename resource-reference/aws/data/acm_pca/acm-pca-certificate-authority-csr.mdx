---
title: "aws_acm_pca_certificate_authority_csr"
description: "`aws_acm_pca_certificate_authority_csr` reads the real certificate signing request (CSR) of an ACM Private Certificate Authority."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm_pca "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/acm_pca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_pca_certificate_authority_csr"})

    		ubx.Data(acm_pca.AcmPcaCertificateAuthorityCsr, "example", acm_pca.AcmPcaCertificateAuthorityCsrConfig{
    			CertificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmPcaCertificateAuthorityCsr } from "@ubx/sdk-aws/aws/data/acm_pca/acm_pca_certificate_authority_csr";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_pca_certificate_authority_csr" });

      data(AcmPcaCertificateAuthorityCsr, "example", {
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm_pca import AcmPcaCertificateAuthorityCsr, AcmPcaCertificateAuthorityCsrConfig

    def describe():
        ubx.intent("look up aws_acm_pca_certificate_authority_csr")

        ubx.data(AcmPcaCertificateAuthorityCsr, "example", AcmPcaCertificateAuthorityCsrConfig(
            certificate_authority_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_authority_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the certificate authority this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="csr" type="string">
  The real, PEM-encoded certificate signing request (CSR) for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
