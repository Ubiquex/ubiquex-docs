---
title: "aws_acm_pca_policy"
description: "`aws_acm_pca_policy` reads the real resource-based policy attached to an ACM Private Certificate Authority."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	acm_pca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm_pca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_pca_policy"})

    		ubx.Data(acm_pca.AcmPcaPolicy, "example", acm_pca.AcmPcaPolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AcmPcaPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/acm_pca/acm_pca_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_pca_policy" });

      data(AcmPcaPolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.acm_pca import AcmPcaPolicy, AcmPcaPolicyConfig

    def describe():
        ubx.intent("look up aws_acm_pca_policy")

        ubx.data(AcmPcaPolicy, "example", AcmPcaPolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
