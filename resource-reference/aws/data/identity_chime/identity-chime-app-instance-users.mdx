---
title: "aws_identity_chime_app_instance_users"
description: "`aws_identity_chime_app_instance_users` looks up a real Chime SDK Identity app instance user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/identity_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identity_chime_app_instance_users"})

    		ubx.Data(identity_chime.IdentityChimeAppInstanceUsers, "example", identity_chime.IdentityChimeAppInstanceUsersConfig{
    			AppInstanceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityChimeAppInstanceUsers } from "@ubx/sdk-aws/aws/data/identity_chime/identity_chime_app_instance_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_identity_chime_app_instance_users" });

      data(IdentityChimeAppInstanceUsers, "example", {
        appInstanceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identity_chime import IdentityChimeAppInstanceUsers, IdentityChimeAppInstanceUsersConfig

    def describe():
        ubx.intent("look up aws_identity_chime_app_instance_users")

        ubx.data(IdentityChimeAppInstanceUsers, "example", IdentityChimeAppInstanceUsersConfig(
            app_instance_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the Amazon Chime app instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_instance_users" type="list(object)">
  The real Amazon Chime app instance users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
