---
title: "aws_identity_chime_app_instance_retention_settings"
description: "`aws_identity_chime_app_instance_retention_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_identity_chime_app_instance_retention_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/identity_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identity_chime_app_instance_retention_settings"})

    		ubx.Data(identity_chime.IdentityChimeAppInstanceRetentionSettings, "example", identity_chime.IdentityChimeAppInstanceRetentionSettingsConfig{
    			AppInstanceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityChimeAppInstanceRetentionSettings } from "@ubx/sdk-aws/aws/data/identity_chime/identity_chime_app_instance_retention_settings";

    export default stack("example", () => {
      intent({
        summary: "look up aws_identity_chime_app_instance_retention_settings",
      });

      data(IdentityChimeAppInstanceRetentionSettings, "example", {
        appInstanceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identity_chime import IdentityChimeAppInstanceRetentionSettings, IdentityChimeAppInstanceRetentionSettingsConfig

    def describe():
        ubx.intent("look up aws_identity_chime_app_instance_retention_settings")

        ubx.data(IdentityChimeAppInstanceRetentionSettings, "example", IdentityChimeAppInstanceRetentionSettingsConfig(
            app_instance_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_instance_retention_settings" type="object">
  &lt;p&gt;The details of the data-retention settings for an &lt;code&gt;AppInstance&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="channel_retention_settings" type="object">
      &lt;p&gt;The details of the retention settings for a channel.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="retention_days" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="initiate_deletion_timestamp" type="string">
  Returned by the lookup.
</ResponseField>
