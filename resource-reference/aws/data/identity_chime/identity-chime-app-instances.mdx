---
title: "aws_identity_chime_app_instances"
description: "`aws_identity_chime_app_instances` looks up a real Chime SDK Identity app instance, the top-level container for messaging users and bots."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/identity_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identity_chime_app_instances"})

    		ubx.Data(identity_chime.IdentityChimeAppInstances, "example", identity_chime.IdentityChimeAppInstancesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityChimeAppInstances } from "@ubx/sdk-aws/aws/data/identity_chime/identity_chime_app_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_identity_chime_app_instances" });

      data(IdentityChimeAppInstances, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identity_chime import IdentityChimeAppInstances, IdentityChimeAppInstancesConfig

    def describe():
        ubx.intent("look up aws_identity_chime_app_instances")

        ubx.data(IdentityChimeAppInstances, "example", IdentityChimeAppInstancesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_instances" type="list(object)">
  The real Amazon Chime app instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
