---
title: "aws_identity_chime_app_instance_user_endpoint"
description: "`aws_identity_chime_app_instance_user_endpoint` looks up one real device endpoint registered for a Chime SDK Identity app instance user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/identity_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identity_chime_app_instance_user_endpoint"})

    		ubx.Data(identity_chime.IdentityChimeAppInstanceUserEndpoint, "example", identity_chime.IdentityChimeAppInstanceUserEndpointConfig{
    			AppInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
    			EndpointId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityChimeAppInstanceUserEndpoint } from "@ubx/sdk-aws/aws/data/identity_chime/identity_chime_app_instance_user_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up aws_identity_chime_app_instance_user_endpoint" });

      data(IdentityChimeAppInstanceUserEndpoint, "example", {
        appInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
        endpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identity_chime import IdentityChimeAppInstanceUserEndpoint, IdentityChimeAppInstanceUserEndpointConfig

    def describe():
        ubx.intent("look up aws_identity_chime_app_instance_user_endpoint")

        ubx.data(IdentityChimeAppInstanceUserEndpoint, "example", IdentityChimeAppInstanceUserEndpointConfig(
            app_instance_user_arn="arn:aws:iam::123456789012:policy/example",
            endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_user_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the Amazon Chime app instance user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="endpoint_id" type="string" required>
  The real, unique identifier of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_instance_user_endpoint" type="object">
  &lt;p&gt;An endpoint under an Amazon Chime &lt;code&gt;AppInstanceUser&lt;/code&gt; that receives messages for a user. For push notifications, the endpoint is a mobile device used to receive mobile push notifications for a user.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allow_messages" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="app_instance_user_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="endpoint_attributes" type="object">
      &lt;p&gt;The attributes of an &lt;code&gt;Endpoint&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="device_token" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="voip_device_token" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoint_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="endpoint_state" type="object">
      &lt;p&gt;A read-only field that represents the state of an &lt;code&gt;AppInstanceUserEndpoint&lt;/code&gt;. Supported values:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;ACTIVE&lt;/code&gt;: The &lt;code&gt;AppInstanceUserEndpoint&lt;/code&gt; is active and able to receive messages. When &lt;code&gt;ACTIVE&lt;/code&gt;, the &lt;code&gt;EndpointStatusReason&lt;/code&gt; remains empty.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;INACTIVE&lt;/code&gt;: The &lt;code&gt;AppInstanceUserEndpoint&lt;/code&gt; is inactive and can't receive message. When INACTIVE, the corresponding reason will be conveyed through EndpointStatusReason.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;INVALID_DEVICE_TOKEN&lt;/code&gt; indicates that an &lt;code&gt;AppInstanceUserEndpoint&lt;/code&gt; is &lt;code&gt;INACTIVE&lt;/code&gt; due to invalid device token&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;INVALID_PINPOINT_ARN&lt;/code&gt; indicates that an &lt;code&gt;AppInstanceUserEndpoint&lt;/code&gt; is &lt;code&gt;INACTIVE&lt;/code&gt; due to an invalid pinpoint ARN that was input through the &lt;code&gt;ResourceArn&lt;/code&gt; field.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="status" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="status_reason" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_updated_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
