---
title: "aws_bedrock_agentcore_resource_oauth2_token"
description: "`aws_bedrock_agentcore_resource_oauth2_token` reads a real, stored OAuth2 token a Bedrock AgentCore agent uses to call an external resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock_agentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bedrock_agentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bedrock_agentcore_resource_oauth2_token"})

    		ubx.Data(bedrock_agentcore.BedrockAgentcoreResourceOauth2Token, "example", bedrock_agentcore.BedrockAgentcoreResourceOauth2TokenConfig{
    			Oauth2Flow:                     "example",
    			ResourceCredentialProviderName: "example-resource-credential-provider-name",
    			Scopes:                         []string{"example"},
    			WorkloadIdentityToken:          "example",
    			Audiences:                      []string{"example"},
    			CustomParameters:               map[string]string{"managed-by": "ubx"},
    			CustomState:                    "example",
    			ForceAuthentication:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BedrockAgentcoreResourceOauth2Token } from "@ubx/sdk-aws/aws/data/bedrock_agentcore/bedrock_agentcore_resource_oauth2_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_bedrock_agentcore_resource_oauth2_token" });

      data(BedrockAgentcoreResourceOauth2Token, "example", {
        oauth2Flow: "example",
        resourceCredentialProviderName: "example-resource-credential-provider-name",
        scopes: ["example"],
        workloadIdentityToken: "example",
        audiences: ["example"],
        customParameters: { "managed-by": "ubx" },
        customState: "example",
        forceAuthentication: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bedrock_agentcore import BedrockAgentcoreResourceOauth2Token, BedrockAgentcoreResourceOauth2TokenConfig

    def describe():
        ubx.intent("look up aws_bedrock_agentcore_resource_oauth2_token")

        ubx.data(BedrockAgentcoreResourceOauth2Token, "example", BedrockAgentcoreResourceOauth2TokenConfig(
            oauth2_flow="example",
            resource_credential_provider_name="example-resource-credential-provider-name",
            scopes=["example"],
            workload_identity_token="example",
            audiences=["example"],
            custom_parameters={"managed-by": "ubx"},
            custom_state="example",
            force_authentication=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audiences" type="list(string)">
  The real audiences included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="custom_parameters" type="map(string)">
  The real, caller-supplied custom parameters for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="custom_state" type="string">
  The real, current state of the custom this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="force_authentication" type="bool">
  Whether authentication is required, even if a session already exists. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="oauth2_flow" type="string" required>
  The real OAuth 2.0 flow used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_credential_provider_name" type="string" required>
  The real name of the resource credential provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_oauth2_return_url" type="string">
  The real, resolvable URL of the resource oauth2 return this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resources" type="list(string)">
  The real AWS resources included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scopes" type="list(string)" required>
  The real scopes included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workload_identity_token" type="string" required>
  The real, opaque workload identity token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_token" type="string">
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="authorization_url" type="string">
  The real, resolvable URL of the authorization this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_status" type="string">
  The real, current status of the session this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_uri" type="string">
  The real, resolvable URI of the session this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
