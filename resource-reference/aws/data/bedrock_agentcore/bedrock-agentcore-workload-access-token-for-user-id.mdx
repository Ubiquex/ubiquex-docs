---
title: "aws_bedrock_agentcore_workload_access_token_for_user_id"
description: "`aws_bedrock_agentcore_workload_access_token_for_user_id` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_bedrock_agentcore_workload_access_token_for_user_id` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock_agentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bedrock_agentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bedrock_agentcore_workload_access_token_for_user_id"})

    		ubx.Data(bedrock_agentcore.BedrockAgentcoreWorkloadAccessTokenForUserId, "example", bedrock_agentcore.BedrockAgentcoreWorkloadAccessTokenForUserIdConfig{
    			UserId:       "example",
    			WorkloadName: "example-workload-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BedrockAgentcoreWorkloadAccessTokenForUserId } from "@ubx/sdk-aws/aws/data/bedrock_agentcore/bedrock_agentcore_workload_access_token_for_user_id";

    export default stack("example", () => {
      intent({
        summary: "look up aws_bedrock_agentcore_workload_access_token_for_user_id",
      });

      data(BedrockAgentcoreWorkloadAccessTokenForUserId, "example", {
        userId: "example",
        workloadName: "example-workload-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bedrock_agentcore import BedrockAgentcoreWorkloadAccessTokenForUserId, BedrockAgentcoreWorkloadAccessTokenForUserIdConfig

    def describe():
        ubx.intent("look up aws_bedrock_agentcore_workload_access_token_for_user_id")

        ubx.data(BedrockAgentcoreWorkloadAccessTokenForUserId, "example", BedrockAgentcoreWorkloadAccessTokenForUserIdConfig(
            user_id="example",
            workload_name="example-workload-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workload_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="workload_access_token" type="string">
  Returned by the lookup.
</ResponseField>
