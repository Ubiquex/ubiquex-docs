---
title: "aws_bedrock_agentcore_resource_api_key"
description: "`aws_bedrock_agentcore_resource_api_key` reads a real, stored API key credential a Bedrock AgentCore agent uses to call an external resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	bedrock_agentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bedrock_agentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bedrock_agentcore_resource_api_key"})

    		ubx.Data(bedrock_agentcore.BedrockAgentcoreResourceApiKey, "example", bedrock_agentcore.BedrockAgentcoreResourceApiKeyConfig{
    			ResourceCredentialProviderName: "example-resource-credential-provider-name",
    			WorkloadIdentityToken:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BedrockAgentcoreResourceApiKey } from "./local-sdk/aws/sdk/typescript/aws/data/bedrock_agentcore/bedrock_agentcore_resource_api_key.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_bedrock_agentcore_resource_api_key" });

      data(BedrockAgentcoreResourceApiKey, "example", {
        resourceCredentialProviderName: "example-resource-credential-provider-name",
        workloadIdentityToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.bedrock_agentcore import BedrockAgentcoreResourceApiKey, BedrockAgentcoreResourceApiKeyConfig

    def describe():
        ubx.intent("look up aws_bedrock_agentcore_resource_api_key")

        ubx.data(BedrockAgentcoreResourceApiKey, "example", BedrockAgentcoreResourceApiKeyConfig(
            resource_credential_provider_name="example-resource-credential-provider-name",
            workload_identity_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_credential_provider_name" type="string" required>
  The real name of the resource credential provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workload_identity_token" type="string" required>
  The real, opaque workload identity token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="api_key" type="string">
  The real, unique key of the api this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
