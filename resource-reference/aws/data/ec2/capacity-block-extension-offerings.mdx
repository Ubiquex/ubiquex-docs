---
title: "aws_ec2_capacity_block_extension_offerings"
description: "`aws_ec2_capacity_block_extension_offerings` reads a real, available offer to extend the duration of an existing EC2 Capacity Block reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_block_extension_offerings"})

    		ubx.Data(ec2.CapacityBlockExtensionOfferings, "example", ec2.CapacityBlockExtensionOfferingsConfig{
    			CapacityBlockExtensionDurationHours: 7200,
    			CapacityReservationId:               "example",
    			DryRun:                              true,
    			MaxResults:                          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityBlockExtensionOfferings } from "@ubx/sdk-aws/aws/data/ec2/capacity_block_extension_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_block_extension_offerings" });

      data(CapacityBlockExtensionOfferings, "example", {
        capacityBlockExtensionDurationHours: 7200,
        capacityReservationId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityBlockExtensionOfferings, CapacityBlockExtensionOfferingsConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_block_extension_offerings")

        ubx.data(CapacityBlockExtensionOfferings, "example", CapacityBlockExtensionOfferingsConfig(
            capacity_block_extension_duration_hours=7200,
            capacity_reservation_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_block_extension_duration_hours" type="number" required>
  The real duration this EC2 Capacity Block extension covers, in hours. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="capacity_reservation_id" type="string" required>
  The real, unique identifier of the capacity reservation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_block_extension_offerings" type="list(object)">
  The real EC2 Capacity Block extension offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
