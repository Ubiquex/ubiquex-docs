---
title: "aws_ec2_ipam_internet_registry_associations"
description: "`aws_ec2_ipam_internet_registry_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_ipam_internet_registry_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_internet_registry_associations"})

    		ubx.Data(ec2.IpamInternetRegistryAssociations, "example", ec2.IpamInternetRegistryAssociationsConfig{
    			DryRun:                             true,
    			Filters:                            []map[string]any{{"name": "example-name"}},
    			IpamInternetRegistryAssociationIds: []string{"example"},
    			MaxResults:                         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamInternetRegistryAssociations } from "@ubx/sdk-aws/aws/data/ec2/ipam_internet_registry_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_internet_registry_associations" });

      data(IpamInternetRegistryAssociations, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        ipamInternetRegistryAssociationIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamInternetRegistryAssociations, IpamInternetRegistryAssociationsConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_internet_registry_associations")

        ubx.data(IpamInternetRegistryAssociations, "example", IpamInternetRegistryAssociationsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            ipam_internet_registry_association_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_internet_registry_association_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_internet_registry_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
