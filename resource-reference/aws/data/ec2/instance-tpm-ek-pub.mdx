---
title: "aws_ec2_instance_tpm_ek_pub"
description: "`aws_ec2_instance_tpm_ek_pub` reads the real, public endorsement key of an EC2 instance's own virtual Trusted Platform Module (TPM)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_tpm_ek_pub"})

    		ubx.Data(ec2.InstanceTpmEkPub, "example", ec2.InstanceTpmEkPubConfig{
    			InstanceId: "example",
    			KeyFormat:  "example",
    			KeyType:    "example",
    			DryRun:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceTpmEkPub } from "@ubx/sdk-aws/aws/data/ec2/instance_tpm_ek_pub";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_tpm_ek_pub" });

      data(InstanceTpmEkPub, "example", {
        instanceId: "example",
        keyFormat: "example",
        keyType: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceTpmEkPub, InstanceTpmEkPubConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_tpm_ek_pub")

        ubx.data(InstanceTpmEkPub, "example", InstanceTpmEkPubConfig(
            instance_id="example",
            key_format="example",
            key_type="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="key_format" type="string" required>
  The real format of this key. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="key_type" type="string" required>
  The real, discriminating type of the key this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_value" type="string">
  The real value associated with this key. **(AI-inferred)** Returned by the lookup.
</ResponseField>
