---
title: "aws_ec2_import_snapshot_tasks"
description: "`aws_ec2_import_snapshot_tasks` reads the real, current status of an EC2 VM Import task importing a disk as a new EBS snapshot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_import_snapshot_tasks"})

    		ubx.Data(ec2.ImportSnapshotTasks, "example", ec2.ImportSnapshotTasksConfig{
    			DryRun:        true,
    			Filters:       []map[string]any{{"name": "example-name"}},
    			ImportTaskIds: []string{"example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ImportSnapshotTasks } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/import_snapshot_tasks.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_import_snapshot_tasks" });

      data(ImportSnapshotTasks, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        importTaskIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImportSnapshotTasks, ImportSnapshotTasksConfig

    def describe():
        ubx.intent("look up aws_ec2_import_snapshot_tasks")

        ubx.data(ImportSnapshotTasks, "example", ImportSnapshotTasksConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            import_task_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="import_task_ids" type="list(string)">
  The real, unique identifiers of the import tasks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="import_snapshot_tasks" type="list(object)">
  The real EC2 snapshot import tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
