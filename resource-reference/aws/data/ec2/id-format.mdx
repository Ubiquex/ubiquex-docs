---
title: "aws_ec2_id_format"
description: "`aws_ec2_id_format` reads the real, current ID format setting (short or long IDs) for one EC2 resource type on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_id_format"})

    		ubx.Data(ec2.IdFormat, "example", ec2.IdFormatConfig{
    			Resource: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdFormat } from "@ubx/sdk-aws/aws/data/ec2/id_format";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_id_format" });

      data(IdFormat, "example", {
        resource: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IdFormat, IdFormatConfig

    def describe():
        ubx.intent("look up aws_ec2_id_format")

        ubx.data(IdFormat, "example", IdFormatConfig(
            resource="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource" type="string">
  The real AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statuses" type="list(object)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
