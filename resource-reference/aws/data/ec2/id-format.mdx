---
title: "aws_ec2_id_format"
description: "`aws_ec2_id_format` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_id_format` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_id_format"})

    		ubx.Data(ec2.IdFormat, "example", ec2.IdFormatConfig{
    			Resource: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdFormat } from "@ubx/sdk-aws/aws/data/ec2/id_format";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_id_format" });

      data(IdFormat, "example", {
        resource: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IdFormat, IdFormatConfig

    def describe():
        ubx.intent("look up aws_ec2_id_format")

        ubx.data(IdFormat, "example", IdFormatConfig(
            resource="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statuses" type="list(object)">
  Returned by the lookup.
</ResponseField>
