---
title: "aws_ec2_addresses_attribute"
description: "`aws_ec2_addresses_attribute` reads a real, configurable attribute (such as the reverse DNS record) of an Elastic IP address."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_addresses_attribute"})

    		ubx.Data(ec2.AddressesAttribute, "example", ec2.AddressesAttributeConfig{
    			AllocationIds: []string{"example"},
    			Attribute:     "example",
    			DryRun:        true,
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AddressesAttribute } from "@ubx/sdk-aws/aws/data/ec2/addresses_attribute";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_addresses_attribute" });

      data(AddressesAttribute, "example", {
        allocationIds: ["example"],
        attribute: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AddressesAttribute, AddressesAttributeConfig

    def describe():
        ubx.intent("look up aws_ec2_addresses_attribute")

        ubx.data(AddressesAttribute, "example", AddressesAttributeConfig(
            allocation_ids=["example"],
            attribute="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="allocation_ids" type="list(string)">
  The real, unique identifiers of the allocations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="attribute" type="string">
  The real attribute this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="addresses" type="list(object)">
  The real addresses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
