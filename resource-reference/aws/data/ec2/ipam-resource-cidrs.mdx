---
title: "aws_ec2_ipam_resource_cidrs"
description: "`aws_ec2_ipam_resource_cidrs` reads a real IP address range managed by IPAM and attached to a resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_resource_cidrs"})

    		ubx.Data(ec2.IpamResourceCidrs, "example", ec2.IpamResourceCidrsConfig{
    			IpamScopeId:   "example",
    			DryRun:        true,
    			Filters:       []map[string]any{{"name": "example-name"}},
    			IpamPoolId:    "example",
    			MaxResults:    1,
    			ResourceId:    "example",
    			ResourceOwner: "example",
    			ResourceTag:   map[string]any{"key": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IpamResourceCidrs } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ipam_resource_cidrs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_resource_cidrs" });

      data(IpamResourceCidrs, "example", {
        ipamScopeId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        ipamPoolId: "example",
        maxResults: 1,
        resourceId: "example",
        resourceOwner: "example",
        resourceTag: { key: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamResourceCidrs, IpamResourceCidrsConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_resource_cidrs")

        ubx.data(IpamResourceCidrs, "example", IpamResourceCidrsConfig(
            ipam_scope_id="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            ipam_pool_id="example",
            max_results=1,
            resource_id="example",
            resource_owner="example",
            resource_tag={"key": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string">
  The real, unique identifier of the ipam pool this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_scope_id" type="string" required>
  The real, unique identifier of the ipam scope this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  The real owner of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_tag" type="object">
  &lt;p&gt;A tag on an IPAM resource.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_resource_cidrs" type="list(object)">
  The real VPC IPAM resource CIDRs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
