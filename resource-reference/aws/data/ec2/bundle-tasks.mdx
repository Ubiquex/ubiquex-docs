---
title: "aws_ec2_bundle_tasks"
description: "`aws_ec2_bundle_tasks` reads the real, current status of bundling a running instance store-backed Windows instance into a new AMI."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_bundle_tasks"})

    		ubx.Data(ec2.BundleTasks, "example", ec2.BundleTasksConfig{
    			BundleIds: []string{"example"},
    			DryRun:    true,
    			Filters:   []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BundleTasks } from "@ubx/sdk-aws/aws/data/ec2/bundle_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_bundle_tasks" });

      data(BundleTasks, "example", {
        bundleIds: ["example"],
        dryRun: true,
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import BundleTasks, BundleTasksConfig
    from ubx.aws.data.ec2.bundle_tasks import BundleTasks_Filters

    def describe():
        ubx.intent("look up aws_ec2_bundle_tasks")

        ubx.data(BundleTasks, "example", BundleTasksConfig(
            bundle_ids=["example"],
            dry_run=True,
            filters=[BundleTasks_Filters(name="example-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bundle_ids" type="list(string)">
  The real, unique identifiers of the bundles this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bundle_tasks" type="list(object)">
  The real EC2 bundle tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
