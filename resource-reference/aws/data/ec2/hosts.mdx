---
title: "aws_ec2_hosts"
description: "`aws_ec2_hosts` looks up a real EC2 Dedicated Host -- a physical server dedicated entirely to one account's own instances."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_hosts"})

    		ubx.Data(ec2.Hosts, "example", ec2.HostsConfig{
    			Filter:     []map[string]any{{"name": "example-name"}},
    			HostIds:    []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Hosts } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/hosts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_hosts" });

      data(Hosts, "example", {
        filter: [{ name: "example-name" }],
        hostIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import Hosts, HostsConfig

    def describe():
        ubx.intent("look up aws_ec2_hosts")

        ubx.data(Hosts, "example", HostsConfig(
            filter=[{"name": "example-name"}],
            host_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="list(object)">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="host_ids" type="list(string)">
  The real, unique identifiers of the hosts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hosts" type="list(object)">
  The real hosts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
