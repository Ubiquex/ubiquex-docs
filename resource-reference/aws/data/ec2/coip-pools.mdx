---
title: "aws_ec2_coip_pools"
description: "`aws_ec2_coip_pools` looks up a real customer-owned IP address pool (COIP), used on AWS Outposts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_coip_pools"})

    		ubx.Data(ec2.CoipPools, "example", ec2.CoipPoolsConfig{
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			PoolIds:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoipPools } from "@ubx/sdk-aws/aws/data/ec2/coip_pools";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_coip_pools" });

      data(CoipPools, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        poolIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CoipPools, CoipPoolsConfig
    from ubx.aws.data.ec2.coip_pools import CoipPools_Filters

    def describe():
        ubx.intent("look up aws_ec2_coip_pools")

        ubx.data(CoipPools, "example", CoipPoolsConfig(
            dry_run=True,
            filters=[CoipPools_Filters(name="example-name")],
            max_results=1,
            pool_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pool_ids" type="list(string)">
  The real, unique identifiers of the pools this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="coip_pools" type="list(object)">
  The real Customer-Owned IP (CoIP) address pools included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
