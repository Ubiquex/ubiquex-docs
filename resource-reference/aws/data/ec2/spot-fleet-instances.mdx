---
title: "aws_ec2_spot_fleet_instances"
description: "`data_aws_ec2_spot_fleet_instances` looks up a real, already-existing `aws_ec2_spot_fleet`."
---

It launches and maintains a real target amount of Spot Instance capacity across multiple instance types and Availability Zones, automatically requesting replacement capacity if Spot Instances are interrupted, the Spot-focused predecessor to the more general `aws_ec2_fleet`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_spot_fleet_instances"})

    		ubx.Data(ec2.SpotFleetInstances, "example", ec2.SpotFleetInstancesConfig{
    			SpotFleetRequestId: "example",
    			DryRun:             true,
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SpotFleetInstances } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/spot_fleet_instances.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_spot_fleet_instances" });

      data(SpotFleetInstances, "example", {
        spotFleetRequestId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SpotFleetInstances, SpotFleetInstancesConfig

    def describe():
        ubx.intent("look up aws_ec2_spot_fleet_instances")

        ubx.data(SpotFleetInstances, "example", SpotFleetInstancesConfig(
            spot_fleet_request_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="spot_fleet_request_id" type="string" required>
  The real, unique identifier of the spot fleet request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_instances" type="list(object)">
  The real, currently active instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
