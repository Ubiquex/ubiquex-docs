---
title: "aws_ec2_capacity_manager_metric_dimensions"
description: "`aws_ec2_capacity_manager_metric_dimensions` reads the real, available metric dimensions EC2 Capacity Manager can break utilization data down by."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_manager_metric_dimensions"})

    		ubx.Data(ec2.CapacityManagerMetricDimensions, "example", ec2.CapacityManagerMetricDimensionsConfig{
    			EndTime:     "example",
    			GroupBy:     []string{"example"},
    			MetricNames: []string{"example"},
    			StartTime:   "example",
    			DryRun:      true,
    			FilterBy:    []map[string]any{{"dimension_condition": map[string]any{"comparison": "example"}}},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CapacityManagerMetricDimensions } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/capacity_manager_metric_dimensions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_manager_metric_dimensions" });

      data(CapacityManagerMetricDimensions, "example", {
        endTime: "example",
        groupBy: ["example"],
        metricNames: ["example"],
        startTime: "example",
        dryRun: true,
        filterBy: [{ dimensionCondition: { comparison: "example" } }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityManagerMetricDimensions, CapacityManagerMetricDimensionsConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_manager_metric_dimensions")

        ubx.data(CapacityManagerMetricDimensions, "example", CapacityManagerMetricDimensionsConfig(
            end_time="example",
            group_by=["example"],
            metric_names=["example"],
            start_time="example",
            dry_run=True,
            filter_by=[{"dimension_condition": {"comparison": "example"}}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="list(object)">
  The real field results are filtered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)" required>
  The real field results are grouped by. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_names" type="list(string)" required>
  The real names of the metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_dimension_results" type="list(object)">
  The real, computed metric results grouped by dimension. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
