---
title: "aws_ec2_instance_uefi_data"
description: "`aws_ec2_instance_uefi_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_instance_uefi_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_uefi_data"})

    		ubx.Data(ec2.InstanceUefiData, "example", ec2.InstanceUefiDataConfig{
    			InstanceId: "example",
    			DryRun:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceUefiData } from "@ubx/sdk-aws/aws/data/ec2/instance_uefi_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_uefi_data" });

      data(InstanceUefiData, "example", {
        instanceId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceUefiData, InstanceUefiDataConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_uefi_data")

        ubx.data(InstanceUefiData, "example", InstanceUefiDataConfig(
            instance_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="uefi_data" type="string">
  Returned by the lookup.
</ResponseField>
