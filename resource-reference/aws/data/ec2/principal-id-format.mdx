---
title: "aws_ec2_principal_id_format"
description: "`aws_ec2_principal_id_format` reads the real, current ID format setting (short or long IDs) for an EC2 resource type, scoped to a specific IAM principal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_principal_id_format"})

    		ubx.Data(ec2.PrincipalIdFormat, "example", ec2.PrincipalIdFormatConfig{
    			DryRun:     true,
    			MaxResults: 1,
    			Resources:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrincipalIdFormat } from "@ubx/sdk-aws/aws/data/ec2/principal_id_format";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_principal_id_format" });

      data(PrincipalIdFormat, "example", {
        dryRun: true,
        maxResults: 1,
        resources: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import PrincipalIdFormat, PrincipalIdFormatConfig

    def describe():
        ubx.intent("look up aws_ec2_principal_id_format")

        ubx.data(PrincipalIdFormat, "example", PrincipalIdFormatConfig(
            dry_run=True,
            max_results=1,
            resources=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resources" type="list(string)">
  The real AWS resources included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="principals" type="list(object)">
  The real IAM principals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
