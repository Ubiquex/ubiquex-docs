---
title: "aws_ec2_ipam_route_origin_authorizations"
description: "`aws_ec2_ipam_route_origin_authorizations` reads a real Route Origin Authorization (ROA) record IPAM manages for BYOIP address space."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_route_origin_authorizations"})

    		ubx.Data(ec2.IpamRouteOriginAuthorizations, "example", ec2.IpamRouteOriginAuthorizationsConfig{
    			IpamInternetRegistryAssociationId: "example",
    			Cidr:                              "example",
    			DryRun:                            true,
    			MaxResults:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IpamRouteOriginAuthorizations } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ipam_route_origin_authorizations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_route_origin_authorizations" });

      data(IpamRouteOriginAuthorizations, "example", {
        ipamInternetRegistryAssociationId: "example",
        cidr: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamRouteOriginAuthorizations, IpamRouteOriginAuthorizationsConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_route_origin_authorizations")

        ubx.data(IpamRouteOriginAuthorizations, "example", IpamRouteOriginAuthorizationsConfig(
            ipam_internet_registry_association_id="example",
            cidr="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cidr" type="string">
  The real CIDR block of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_internet_registry_association_id" type="string" required>
  The real, unique identifier of the ipam internet registry association this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_route_origin_authorizations" type="list(object)">
  The real VPC IPAM route origin authorizations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
