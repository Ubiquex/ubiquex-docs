---
title: "aws_ec2_password_data"
description: "`aws_ec2_password_data` reads the real, encrypted administrator password of a Windows EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_password_data"})

    		ubx.Data(ec2.PasswordData, "example", ec2.PasswordDataConfig{
    			InstanceId: "example",
    			DryRun:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PasswordData } from "@ubx/sdk-aws/aws/data/ec2/password_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_password_data" });

      data(PasswordData, "example", {
        instanceId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import PasswordData, PasswordDataConfig

    def describe():
        ubx.intent("look up aws_ec2_password_data")

        ubx.data(PasswordData, "example", PasswordDataConfig(
            instance_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="password_data" type="string">
  The real, raw data of the password. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="string">
  The real timestamp of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
