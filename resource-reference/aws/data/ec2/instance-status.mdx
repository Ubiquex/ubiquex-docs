---
title: "aws_ec2_instance_status"
description: "`aws_ec2_instance_status` reads the real, current status checks (system and instance) of an EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_status"})

    		ubx.Data(ec2.InstanceStatus, "example", ec2.InstanceStatusConfig{
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			IncludeAllInstances:     true,
    			IncludeManagedResources: true,
    			InstanceIds:             []string{"example"},
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceStatus } from "@ubx/sdk-aws/aws/data/ec2/instance_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_status" });

      data(InstanceStatus, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        includeAllInstances: true,
        includeManagedResources: true,
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceStatus, InstanceStatusConfig
    from ubx.aws.data.ec2.instance_status import InstanceStatus_Filters

    def describe():
        ubx.intent("look up aws_ec2_instance_status")

        ubx.data(InstanceStatus, "example", InstanceStatusConfig(
            dry_run=True,
            filters=[InstanceStatus_Filters(name="example-name")],
            include_all_instances=True,
            include_managed_resources=True,
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_all_instances" type="bool">
  Whether every real instance is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_managed_resources" type="bool">
  Whether resources managed by another AWS service are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_statuses" type="list(object)">
  The real, current statuses of the instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
