---
title: "aws_ec2_security_group_rules"
description: "`data_aws_ec2_security_group_rules` looks up a real, already-existing `aws_ec2_security_group_egress`."
---

It defines one real, specific outbound traffic rule on a security group, the actual, individually addressable rule resource, as distinct from managing a security group's entire rule set as one inline block.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_security_group_rules"})

    		ubx.Data(ec2.SecurityGroupRules, "example", ec2.SecurityGroupRulesConfig{
    			DryRun:               true,
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxResults:           1,
    			SecurityGroupRuleIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SecurityGroupRules } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/security_group_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_security_group_rules" });

      data(SecurityGroupRules, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        securityGroupRuleIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SecurityGroupRules, SecurityGroupRulesConfig

    def describe():
        ubx.intent("look up aws_ec2_security_group_rules")

        ubx.data(SecurityGroupRules, "example", SecurityGroupRulesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            security_group_rule_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="security_group_rule_ids" type="list(string)">
  The real, unique identifiers of the security group rules this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_group_rules" type="list(object)">
  The real rules for the security group. **(AI-inferred)** Returned by the lookup.
</ResponseField>
