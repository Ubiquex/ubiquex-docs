---
title: "aws_ec2_iam_instance_profile_associations"
description: "`aws_ec2_iam_instance_profile_associations` reads the real association between an IAM instance profile and a running EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_iam_instance_profile_associations"})

    		ubx.Data(ec2.IamInstanceProfileAssociations, "example", ec2.IamInstanceProfileAssociationsConfig{
    			AssociationIds: []string{"example"},
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IamInstanceProfileAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/iam_instance_profile_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_iam_instance_profile_associations" });

      data(IamInstanceProfileAssociations, "example", {
        associationIds: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IamInstanceProfileAssociations, IamInstanceProfileAssociationsConfig

    def describe():
        ubx.intent("look up aws_ec2_iam_instance_profile_associations")

        ubx.data(IamInstanceProfileAssociations, "example", IamInstanceProfileAssociationsConfig(
            association_ids=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_ids" type="list(string)">
  The real, unique identifiers of the associations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="iam_instance_profile_associations" type="list(object)">
  The real IAM instance profile associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
