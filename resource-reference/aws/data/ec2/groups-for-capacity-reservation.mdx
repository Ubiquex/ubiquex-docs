---
title: "aws_ec2_groups_for_capacity_reservation"
description: "`aws_ec2_groups_for_capacity_reservation` reads the real resource groups a Capacity Reservation is a member of."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_groups_for_capacity_reservation"})

    		ubx.Data(ec2.GroupsForCapacityReservation, "example", ec2.GroupsForCapacityReservationConfig{
    			CapacityReservationId: "example",
    			DryRun:                true,
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupsForCapacityReservation } from "@ubx/sdk-aws/aws/data/ec2/groups_for_capacity_reservation";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_groups_for_capacity_reservation" });

      data(GroupsForCapacityReservation, "example", {
        capacityReservationId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import GroupsForCapacityReservation, GroupsForCapacityReservationConfig

    def describe():
        ubx.intent("look up aws_ec2_groups_for_capacity_reservation")

        ubx.data(GroupsForCapacityReservation, "example", GroupsForCapacityReservationConfig(
            capacity_reservation_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_reservation_id" type="string" required>
  The real, unique identifier of the capacity reservation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_reservation_groups" type="list(object)">
  The real capacity reservation groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
