---
title: "aws_ec2_vpc_endpoint_services"
description: "`aws_ec2_vpc_endpoint_services` reads a real, available VPC endpoint service that a VPC endpoint can connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_endpoint_services"})

    		ubx.Data(ec2.VpcEndpointServices, "example", ec2.VpcEndpointServicesConfig{
    			DryRun:         true,
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			ServiceRegions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcEndpointServices } from "@ubx/sdk-aws/aws/data/ec2/vpc_endpoint_services";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_endpoint_services" });

      data(VpcEndpointServices, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        serviceRegions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcEndpointServices, VpcEndpointServicesConfig
    from ubx.aws.data.ec2.vpc_endpoint_services import VpcEndpointServices_Filters

    def describe():
        ubx.intent("look up aws_ec2_vpc_endpoint_services")

        ubx.data(VpcEndpointServices, "example", VpcEndpointServicesConfig(
            dry_run=True,
            filters=[VpcEndpointServices_Filters(name="example-name")],
            max_results=1,
            service_regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_regions" type="list(string)">
  The real AWS regions this service is available in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_details" type="list(object)">
  The real, detailed information about the service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_names" type="list(string)">
  The real names of the services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
