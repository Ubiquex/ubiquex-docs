---
title: "aws_ec2_instance_image_metadata"
description: "`aws_ec2_instance_image_metadata` reads real metadata about the AMI an EC2 instance was launched from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_image_metadata"})

    		ubx.Data(ec2.InstanceImageMetadata, "example", ec2.InstanceImageMetadataConfig{
    			DryRun:      true,
    			Filters:     []map[string]any{{"name": "example-name"}},
    			InstanceIds: []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceImageMetadata } from "@ubx/sdk-aws/aws/data/ec2/instance_image_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_image_metadata" });

      data(InstanceImageMetadata, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceImageMetadata, InstanceImageMetadataConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_image_metadata")

        ubx.data(InstanceImageMetadata, "example", InstanceImageMetadataConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_image_metadata" type="list(object)">
  The real, structured metadata of the instance image. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
