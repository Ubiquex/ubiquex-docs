---
title: "aws_ec2_application_status"
description: "`aws_ec2_application_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_application_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_application_status"})

    		ubx.Data(ec2.ApplicationStatus, "example", ec2.ApplicationStatusConfig{
    			DryRun:      true,
    			Filters:     []map[string]any{{"name": "example-name"}},
    			InstanceIds: []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationStatus } from "@ubx/sdk-aws/aws/data/ec2/application_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_application_status" });

      data(ApplicationStatus, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ApplicationStatus, ApplicationStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_application_status")

        ubx.data(ApplicationStatus, "example", ApplicationStatusConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_statuses" type="object">
  &lt;p&gt;Describes the application statuses for instances.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="instances" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
