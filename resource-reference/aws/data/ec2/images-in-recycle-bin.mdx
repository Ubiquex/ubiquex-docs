---
title: "aws_ec2_images_in_recycle_bin"
description: "`aws_ec2_images_in_recycle_bin` looks up a real, deleted AMI that's currently retained in EC2's own Recycle Bin."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_images_in_recycle_bin"})

    		ubx.Data(ec2.ImagesInRecycleBin, "example", ec2.ImagesInRecycleBinConfig{
    			DryRun:     true,
    			ImageIds:   []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ImagesInRecycleBin } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/images_in_recycle_bin.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_images_in_recycle_bin" });

      data(ImagesInRecycleBin, "example", {
        dryRun: true,
        imageIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImagesInRecycleBin, ImagesInRecycleBinConfig

    def describe():
        ubx.intent("look up aws_ec2_images_in_recycle_bin")

        ubx.data(ImagesInRecycleBin, "example", ImagesInRecycleBinConfig(
            dry_run=True,
            image_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="image_ids" type="list(string)">
  The real, unique identifiers of the images this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="images" type="list(object)">
  The real images included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
