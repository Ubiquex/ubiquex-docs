---
title: "aws_ec2_export_tasks"
description: "`aws_ec2_export_tasks` reads the real, current status of an EC2 instance export task to a VM file in S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_export_tasks"})

    		ubx.Data(ec2.ExportTasks, "example", ec2.ExportTasksConfig{
    			ExportTaskIds: []string{"example"},
    			Filters:       []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExportTasks } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/export_tasks.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_export_tasks" });

      data(ExportTasks, "example", {
        exportTaskIds: ["example"],
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ExportTasks, ExportTasksConfig

    def describe():
        ubx.intent("look up aws_ec2_export_tasks")

        ubx.data(ExportTasks, "example", ExportTasksConfig(
            export_task_ids=["example"],
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_task_ids" type="list(string)">
  The real, unique identifiers of the export tasks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_tasks" type="list(object)">
  The real export tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
