---
title: "aws_ec2_capacity_blocks"
description: "`aws_ec2_capacity_blocks` looks up a real, purchased EC2 Capacity Block reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_blocks"})

    		ubx.Data(ec2.CapacityBlocks, "example", ec2.CapacityBlocksConfig{
    			CapacityBlockIds: []string{"example"},
    			DryRun:           true,
    			Filters:          []map[string]any{{"name": "example-name"}},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityBlocks } from "@ubx/sdk-aws/aws/data/ec2/capacity_blocks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_blocks" });

      data(CapacityBlocks, "example", {
        capacityBlockIds: ["example"],
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityBlocks, CapacityBlocksConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_blocks")

        ubx.data(CapacityBlocks, "example", CapacityBlocksConfig(
            capacity_block_ids=["example"],
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_block_ids" type="list(string)">
  The real, unique identifiers of the capacity blocks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_blocks" type="list(object)">
  The real EC2 Capacity Blocks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
