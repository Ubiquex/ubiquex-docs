---
title: "aws_ec2_instance_types"
description: "`aws_ec2_instance_types` reads the real specifications (vCPUs, memory, network performance) of an EC2 instance type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_types"})

    		ubx.Data(ec2.InstanceTypes, "example", ec2.InstanceTypesConfig{
    			DryRun:                     true,
    			Filters:                    []map[string]any{{"name": "example-name"}},
    			IncludeUnsupportedInRegion: true,
    			MaxResults:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceTypes } from "@ubx/sdk-aws/aws/data/ec2/instance_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_types" });

      data(InstanceTypes, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        includeUnsupportedInRegion: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceTypes, InstanceTypesConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_types")

        ubx.data(InstanceTypes, "example", InstanceTypesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            include_unsupported_in_region=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_unsupported_in_region" type="bool">
  Whether objects unsupported in this region are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_types" type="list(string)">
  The real instance types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
