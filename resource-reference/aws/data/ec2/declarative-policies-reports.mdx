---
title: "aws_ec2_declarative_policies_reports"
description: "`aws_ec2_declarative_policies_reports` reads a real, generated report of EC2 account attribute declarative policy compliance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_declarative_policies_reports"})

    		ubx.Data(ec2.DeclarativePoliciesReports, "example", ec2.DeclarativePoliciesReportsConfig{
    			DryRun:     true,
    			MaxResults: 1,
    			ReportIds:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeclarativePoliciesReports } from "@ubx/sdk-aws/aws/data/ec2/declarative_policies_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_declarative_policies_reports" });

      data(DeclarativePoliciesReports, "example", {
        dryRun: true,
        maxResults: 1,
        reportIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import DeclarativePoliciesReports, DeclarativePoliciesReportsConfig

    def describe():
        ubx.intent("look up aws_ec2_declarative_policies_reports")

        ubx.data(DeclarativePoliciesReports, "example", DeclarativePoliciesReportsConfig(
            dry_run=True,
            max_results=1,
            report_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="report_ids" type="list(string)">
  The real, unique identifiers of the reports this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reports" type="list(object)">
  The real reports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
