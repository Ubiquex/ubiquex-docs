---
title: "aws_ec2_default_credit_specification"
description: "`aws_ec2_default_credit_specification` reads the real, default CPU credit option (standard or unlimited) newly launched burstable (T-family) instances use."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_default_credit_specification"})

    		ubx.Data(ec2.DefaultCreditSpecification, "example", ec2.DefaultCreditSpecificationConfig{
    			InstanceFamily: "example",
    			DryRun:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultCreditSpecification } from "@ubx/sdk-aws/aws/data/ec2/default_credit_specification";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_default_credit_specification" });

      data(DefaultCreditSpecification, "example", {
        instanceFamily: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import DefaultCreditSpecification, DefaultCreditSpecificationConfig

    def describe():
        ubx.intent("look up aws_ec2_default_credit_specification")

        ubx.data(DefaultCreditSpecification, "example", DefaultCreditSpecificationConfig(
            instance_family="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_family" type="string" required>
  The real instance family. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_family_credit_specification" type="object">
  &lt;p&gt;Describes the default credit option for CPU usage of a burstable performance instance family.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cpu_credits" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_family" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
