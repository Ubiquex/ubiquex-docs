---
title: "aws_ec2_host_reservation_purchase_preview"
description: "`aws_ec2_host_reservation_purchase_preview` reads a real, previewed cost and configuration for purchasing an EC2 Dedicated Host Reservation before..."
---

`aws_ec2_host_reservation_purchase_preview` reads a real, previewed cost and configuration for purchasing an EC2 Dedicated Host Reservation before committing to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_host_reservation_purchase_preview"})

    		ubx.Data(ec2.HostReservationPurchasePreview, "example", ec2.HostReservationPurchasePreviewConfig{
    			HostIdSet:  []string{"example"},
    			OfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { HostReservationPurchasePreview } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/host_reservation_purchase_preview.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_host_reservation_purchase_preview" });

      data(HostReservationPurchasePreview, "example", {
        hostIdSet: ["example"],
        offeringId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import HostReservationPurchasePreview, HostReservationPurchasePreviewConfig

    def describe():
        ubx.intent("look up aws_ec2_host_reservation_purchase_preview")

        ubx.data(HostReservationPurchasePreview, "example", HostReservationPurchasePreviewConfig(
            host_id_set=["example"],
            offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="host_id_set" type="list(string)" required>
  The real host id set. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="offering_id" type="string" required>
  The real, unique identifier of the offering this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="currency_code" type="string">
  The real currency this value is denominated in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="purchase" type="list(object)">
  The real purchase this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_hourly_price" type="string">
  The real, total hourly price of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_upfront_price" type="string">
  The real, total upfront price of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
