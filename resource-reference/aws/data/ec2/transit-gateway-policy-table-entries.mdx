---
title: "aws_ec2_transit_gateway_policy_table_entries"
description: "`data_aws_ec2_transit_gateway_policy_table_entries` looks up a real, already-existing `aws_ec2_transit_gateway_policy_table_entry`."
---

It is one real, specific matching rule inside an `aws_ec2_transit_gateway_policy_table`, the actual, individually addressable rule within a policy table's broader rule set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_transit_gateway_policy_table_entries"})

    		ubx.Data(ec2.TransitGatewayPolicyTableEntries, "example", ec2.TransitGatewayPolicyTableEntriesConfig{
    			TransitGatewayPolicyTableId: "example",
    			DryRun:                      true,
    			Filters:                     []map[string]any{{"name": "example-name"}},
    			MaxResults:                  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransitGatewayPolicyTableEntries } from "@ubx/sdk-aws/aws/data/ec2/transit_gateway_policy_table_entries";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_transit_gateway_policy_table_entries" });

      data(TransitGatewayPolicyTableEntries, "example", {
        transitGatewayPolicyTableId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import TransitGatewayPolicyTableEntries, TransitGatewayPolicyTableEntriesConfig
    from ubx.aws.data.ec2.transit_gateway_policy_table_entries import TransitGatewayPolicyTableEntries_Filters

    def describe():
        ubx.intent("look up aws_ec2_transit_gateway_policy_table_entries")

        ubx.data(TransitGatewayPolicyTableEntries, "example", TransitGatewayPolicyTableEntriesConfig(
            transit_gateway_policy_table_id="example",
            dry_run=True,
            filters=[TransitGatewayPolicyTableEntries_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_policy_table_id" type="string" required>
  The real, unique identifier of the transit gateway policy table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_policy_table_entries" type="list(object)">
  The real transit gateway policy table entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
