---
title: "aws_ec2_spot_instance_requests"
description: "`aws_ec2_spot_instance_requests` looks up a real Spot Instance request."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_spot_instance_requests"})

    		ubx.Data(ec2.SpotInstanceRequests, "example", ec2.SpotInstanceRequestsConfig{
    			DryRun:                 true,
    			Filters:                []map[string]any{{"name": "example-name"}},
    			MaxResults:             1,
    			SpotInstanceRequestIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SpotInstanceRequests } from "@ubx/sdk-aws/aws/data/ec2/spot_instance_requests";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_spot_instance_requests" });

      data(SpotInstanceRequests, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        spotInstanceRequestIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SpotInstanceRequests, SpotInstanceRequestsConfig

    def describe():
        ubx.intent("look up aws_ec2_spot_instance_requests")

        ubx.data(SpotInstanceRequests, "example", SpotInstanceRequestsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            spot_instance_request_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="spot_instance_request_ids" type="list(string)">
  The real, unique identifiers of the spot instance requests this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="spot_instance_requests" type="list(object)">
  The real EC2 Spot Instance requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
