---
title: "aws_ec2_identity_id_format"
description: "`aws_ec2_identity_id_format` reads the real, current ID format setting (short or long IDs) for one EC2 resource type, scoped to a specific IAM principal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_identity_id_format"})

    		ubx.Data(ec2.IdentityIdFormat, "example", ec2.IdentityIdFormatConfig{
    			PrincipalArn: "arn:aws:iam::123456789012:policy/example",
    			Resource:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IdentityIdFormat } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/identity_id_format.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_identity_id_format" });

      data(IdentityIdFormat, "example", {
        principalArn: "arn:aws:iam::123456789012:policy/example",
        resource: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IdentityIdFormat, IdentityIdFormatConfig

    def describe():
        ubx.intent("look up aws_ec2_identity_id_format")

        ubx.data(IdentityIdFormat, "example", IdentityIdFormatConfig(
            principal_arn="arn:aws:iam::123456789012:policy/example",
            resource="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="principal_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource" type="string">
  The real AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statuses" type="list(object)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
