---
title: "aws_ec2_serial_console_access_status"
description: "`aws_ec2_serial_console_access_status` reads the real, account-wide setting controlling whether the EC2 Serial Console is enabled."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_serial_console_access_status"})

    		ubx.Data(ec2.SerialConsoleAccessStatus, "example", ec2.SerialConsoleAccessStatusConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SerialConsoleAccessStatus } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/serial_console_access_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_serial_console_access_status" });

      data(SerialConsoleAccessStatus, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SerialConsoleAccessStatus, SerialConsoleAccessStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_serial_console_access_status")

        ubx.data(SerialConsoleAccessStatus, "example", SerialConsoleAccessStatusConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="serial_console_access_enabled" type="bool">
  Whether the serial console access is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>
