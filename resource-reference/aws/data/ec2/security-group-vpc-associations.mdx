---
title: "aws_ec2_security_group_vpc_associations"
description: "`data_aws_ec2_security_group_vpc_associations` looks up a real, already-existing `aws_ec2_security_group_vpc_association`."
---

It shares an existing security group across additional VPCs in the same Region, a real, direct alternative to recreating the same rule set in every VPC that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_security_group_vpc_associations"})

    		ubx.Data(ec2.SecurityGroupVpcAssociations, "example", ec2.SecurityGroupVpcAssociationsConfig{
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityGroupVpcAssociations } from "@ubx/sdk-aws/aws/data/ec2/security_group_vpc_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_security_group_vpc_associations" });

      data(SecurityGroupVpcAssociations, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SecurityGroupVpcAssociations, SecurityGroupVpcAssociationsConfig
    from ubx.aws.data.ec2.security_group_vpc_associations import SecurityGroupVpcAssociations_Filters

    def describe():
        ubx.intent("look up aws_ec2_security_group_vpc_associations")

        ubx.data(SecurityGroupVpcAssociations, "example", SecurityGroupVpcAssociationsConfig(
            dry_run=True,
            filters=[SecurityGroupVpcAssociations_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_group_vpc_associations" type="list(object)">
  The real associations between this security group and its own VPCs. **(AI-inferred)** Returned by the lookup.
</ResponseField>
