---
title: "aws_ec2_ipam_prefix_list_resolver_version_entries"
description: "`aws_ec2_ipam_prefix_list_resolver_version_entries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_ipam_prefix_list_resolver_version_entries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_prefix_list_resolver_version_entries"})

    		ubx.Data(ec2.IpamPrefixListResolverVersionEntries, "example", ec2.IpamPrefixListResolverVersionEntriesConfig{
    			IpamPrefixListResolverId:      "example",
    			IpamPrefixListResolverVersion: 1,
    			DryRun:                        true,
    			MaxResults:                    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamPrefixListResolverVersionEntries } from "@ubx/sdk-aws/aws/data/ec2/ipam_prefix_list_resolver_version_entries";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_ipam_prefix_list_resolver_version_entries",
      });

      data(IpamPrefixListResolverVersionEntries, "example", {
        ipamPrefixListResolverId: "example",
        ipamPrefixListResolverVersion: 1,
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamPrefixListResolverVersionEntries, IpamPrefixListResolverVersionEntriesConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_prefix_list_resolver_version_entries")

        ubx.data(IpamPrefixListResolverVersionEntries, "example", IpamPrefixListResolverVersionEntriesConfig(
            ipam_prefix_list_resolver_id="example",
            ipam_prefix_list_resolver_version=1,
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_version" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
