---
title: "aws_ec2_mac_hosts"
description: "`aws_ec2_mac_hosts` looks up a real EC2 Dedicated Host running an Apple Mac EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_mac_hosts"})

    		ubx.Data(ec2.MacHosts, "example", ec2.MacHostsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			HostIds:    []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MacHosts } from "@ubx/sdk-aws/aws/data/ec2/mac_hosts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_mac_hosts" });

      data(MacHosts, "example", {
        filters: [{ name: "example-name" }],
        hostIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import MacHosts, MacHostsConfig

    def describe():
        ubx.intent("look up aws_ec2_mac_hosts")

        ubx.data(MacHosts, "example", MacHostsConfig(
            filters=[{"name": "example-name"}],
            host_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="host_ids" type="list(string)">
  The real, unique identifiers of the hosts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mac_hosts" type="list(object)">
  The real EC2 Dedicated Hosts for Mac instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
