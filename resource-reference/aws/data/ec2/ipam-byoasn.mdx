---
title: "aws_ec2_ipam_byoasn"
description: "`data_aws_ec2_ipam_byoasn` looks up a real, already-existing `aws_ec2_ipam`."
---

It is the real, top-level IP Address Manager resource, planning, tracking, and monitoring IP address usage across an AWS organization's accounts and Regions from one place rather than each account managing its own address space independently.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_byoasn"})

    		ubx.Data(ec2.IpamByoasn, "example", ec2.IpamByoasnConfig{
    			DryRun:     true,
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IpamByoasn } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ipam_byoasn.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_byoasn" });

      data(IpamByoasn, "example", {
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamByoasn, IpamByoasnConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_byoasn")

        ubx.data(IpamByoasn, "example", IpamByoasnConfig(
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="byoasns" type="list(object)">
  The real Bring Your Own ASN (BYOASN) autonomous system numbers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
