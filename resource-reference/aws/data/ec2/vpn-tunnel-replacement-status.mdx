---
title: "aws_ec2_vpn_tunnel_replacement_status"
description: "`aws_ec2_vpn_tunnel_replacement_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_vpn_tunnel_replacement_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpn_tunnel_replacement_status"})

    		ubx.Data(ec2.VpnTunnelReplacementStatus, "example", ec2.VpnTunnelReplacementStatusConfig{
    			VpnConnectionId:           "example",
    			VpnTunnelOutsideIpAddress: "example",
    			DryRun:                    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpnTunnelReplacementStatus } from "@ubx/sdk-aws/aws/data/ec2/vpn_tunnel_replacement_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpn_tunnel_replacement_status" });

      data(VpnTunnelReplacementStatus, "example", {
        vpnConnectionId: "example",
        vpnTunnelOutsideIpAddress: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpnTunnelReplacementStatus, VpnTunnelReplacementStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_vpn_tunnel_replacement_status")

        ubx.data(VpnTunnelReplacementStatus, "example", VpnTunnelReplacementStatusConfig(
            vpn_connection_id="example",
            vpn_tunnel_outside_ip_address="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vpn_tunnel_outside_ip_address" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="customer_gateway_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="maintenance_details" type="object">
  &lt;p&gt;Details for Site-to-Site VPN tunnel endpoint maintenance events.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="last_maintenance_applied" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="maintenance_auto_applied_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pending_maintenance" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_gateway_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vpn_gateway_id" type="string">
  Returned by the lookup.
</ResponseField>
