---
title: "aws_ec2_capacity_manager_monitored_tag_keys"
description: "`aws_ec2_capacity_manager_monitored_tag_keys` reads the real tag keys EC2 Capacity Manager is currently configured to track utilization by."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_manager_monitored_tag_keys"})

    		ubx.Data(ec2.CapacityManagerMonitoredTagKeys, "example", ec2.CapacityManagerMonitoredTagKeysConfig{
    			DryRun:     true,
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityManagerMonitoredTagKeys } from "@ubx/sdk-aws/aws/data/ec2/capacity_manager_monitored_tag_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_manager_monitored_tag_keys" });

      data(CapacityManagerMonitoredTagKeys, "example", {
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityManagerMonitoredTagKeys, CapacityManagerMonitoredTagKeysConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_manager_monitored_tag_keys")

        ubx.data(CapacityManagerMonitoredTagKeys, "example", CapacityManagerMonitoredTagKeysConfig(
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_manager_tag_keys" type="list(object)">
  The real, unique keys of the capacity manager tags included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
