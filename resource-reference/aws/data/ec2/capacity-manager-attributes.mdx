---
title: "aws_ec2_capacity_manager_attributes"
description: "`data_aws_ec2_capacity_manager_attributes` looks up a real, already-existing `aws_ec2_capacity_manager_data_export`."
---

It configures a real, scheduled export of EC2 Capacity Manager's own usage data, On-Demand, Spot, and Capacity Reservation metrics across an organization's accounts and Regions, to an S3 bucket. Amazon EC2 Capacity Manager reached general availability October 17, 2025, roughly ten months ago.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_manager_attributes"})

    		ubx.Data(ec2.CapacityManagerAttributes, "example", ec2.CapacityManagerAttributesConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CapacityManagerAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/capacity_manager_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_manager_attributes" });

      data(CapacityManagerAttributes, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityManagerAttributes, CapacityManagerAttributesConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_manager_attributes")

        ubx.data(CapacityManagerAttributes, "example", CapacityManagerAttributesConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_manager_status" type="string">
  The real, current status of the capacity manager this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_export_count" type="number">
  The real count of data exports. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="earliest_datapoint_timestamp" type="string">
  The real timestamp of the earliest datapoint. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ingestion_status" type="string">
  The real, current status of the ingestion this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ingestion_status_message" type="string">
  The real, human-readable message describing the ingestion status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="latest_datapoint_timestamp" type="string">
  The real timestamp of the latest datapoint. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organizations_access" type="bool">
  The real access configuration of the organizations. **(AI-inferred)** Returned by the lookup.
</ResponseField>
