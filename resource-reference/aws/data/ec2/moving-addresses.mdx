---
title: "aws_ec2_moving_addresses"
description: "`aws_ec2_moving_addresses` reads the real, current status of an Elastic IP address migrating between EC2-Classic and a VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_moving_addresses"})

    		ubx.Data(ec2.MovingAddresses, "example", ec2.MovingAddressesConfig{
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			PublicIps:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MovingAddresses } from "@ubx/sdk-aws/aws/data/ec2/moving_addresses";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_moving_addresses" });

      data(MovingAddresses, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        publicIps: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import MovingAddresses, MovingAddressesConfig

    def describe():
        ubx.intent("look up aws_ec2_moving_addresses")

        ubx.data(MovingAddresses, "example", MovingAddressesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            public_ips=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="public_ips" type="list(string)">
  The real public IP addresses included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="moving_address_statuses" type="list(object)">
  The real, current statuses of the moving addresss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
