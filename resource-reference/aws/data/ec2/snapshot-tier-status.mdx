---
title: "aws_ec2_snapshot_tier_status"
description: "`aws_ec2_snapshot_tier_status` reads the real, current storage tier (standard or archive) an EBS snapshot is stored in."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_snapshot_tier_status"})

    		ubx.Data(ec2.SnapshotTierStatus, "example", ec2.SnapshotTierStatusConfig{
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotTierStatus } from "@ubx/sdk-aws/aws/data/ec2/snapshot_tier_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_snapshot_tier_status" });

      data(SnapshotTierStatus, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SnapshotTierStatus, SnapshotTierStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_snapshot_tier_status")

        ubx.data(SnapshotTierStatus, "example", SnapshotTierStatusConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot_tier_statuses" type="list(object)">
  The real, current statuses of the snapshot tiers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
