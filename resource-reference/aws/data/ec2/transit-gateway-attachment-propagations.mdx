---
title: "aws_ec2_transit_gateway_attachment_propagations"
description: "`data_aws_ec2_transit_gateway_attachment_propagations` looks up a real, already-existing `aws_ec2_transit_gateway_attachment`."
---

It is the real, general resource connecting a VPC, VPN, Direct Connect gateway, or another transit gateway to an `aws_ec2_transit_gateway`, the actual link every specific attachment type shares the same underlying mechanism for.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_transit_gateway_attachment_propagations"})

    		ubx.Data(ec2.TransitGatewayAttachmentPropagations, "example", ec2.TransitGatewayAttachmentPropagationsConfig{
    			TransitGatewayAttachmentId: "example",
    			DryRun:                     true,
    			Filters:                    []map[string]any{{"name": "example-name"}},
    			MaxResults:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TransitGatewayAttachmentPropagations } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/transit_gateway_attachment_propagations.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_transit_gateway_attachment_propagations",
      });

      data(TransitGatewayAttachmentPropagations, "example", {
        transitGatewayAttachmentId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import TransitGatewayAttachmentPropagations, TransitGatewayAttachmentPropagationsConfig

    def describe():
        ubx.intent("look up aws_ec2_transit_gateway_attachment_propagations")

        ubx.data(TransitGatewayAttachmentPropagations, "example", TransitGatewayAttachmentPropagationsConfig(
            transit_gateway_attachment_id="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  The real, unique identifier of the transit gateway attachment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_attachment_propagations" type="list(object)">
  The real transit gateway attachment route propagations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
