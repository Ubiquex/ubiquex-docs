---
title: "aws_ec2_verified_access_endpoint_targets"
description: "`data_aws_ec2_verified_access_endpoint_targets` looks up a real, already-existing `aws_ec2_verified_access_endpoint`."
---

It is the real, actual application Verified Access provides access to, an IP address, load balancer, or network interface, evaluated against its `aws_ec2_verified_access_group`'s policy on every request before traffic is allowed through.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_verified_access_endpoint_targets"})

    		ubx.Data(ec2.VerifiedAccessEndpointTargets, "example", ec2.VerifiedAccessEndpointTargetsConfig{
    			VerifiedAccessEndpointId: "example",
    			DryRun:                   true,
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VerifiedAccessEndpointTargets } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/verified_access_endpoint_targets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_verified_access_endpoint_targets" });

      data(VerifiedAccessEndpointTargets, "example", {
        verifiedAccessEndpointId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VerifiedAccessEndpointTargets, VerifiedAccessEndpointTargetsConfig

    def describe():
        ubx.intent("look up aws_ec2_verified_access_endpoint_targets")

        ubx.data(VerifiedAccessEndpointTargets, "example", VerifiedAccessEndpointTargetsConfig(
            verified_access_endpoint_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="verified_access_endpoint_id" type="string" required>
  The real, unique identifier of the verified access endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="verified_access_endpoint_targets" type="list(object)">
  The real targets of the verified access endpoint. **(AI-inferred)** Returned by the lookup.
</ResponseField>
