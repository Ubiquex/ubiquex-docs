---
title: "aws_ec2_network_insights_access_scope_analyses"
description: "`data_aws_ec2_network_insights_access_scope_analyses` looks up a real, already-existing `aws_ec2_network_insights_access_scope`."
---

It defines a real, broader set of network paths and conditions to evaluate together, going beyond one `aws_ec2_network_insights_path`'s single source-destination pair to check an entire access boundary, everything that should or shouldn't be reachable from the internet, for instance, at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_network_insights_access_scope_analyses"})

    		ubx.Data(ec2.NetworkInsightsAccessScopeAnalyses, "example", ec2.NetworkInsightsAccessScopeAnalysesConfig{
    			AnalysisStartTimeBegin:                "example",
    			AnalysisStartTimeEnd:                  "example",
    			DryRun:                                true,
    			Filters:                               []map[string]any{{"name": "example-name"}},
    			MaxResults:                            1,
    			NetworkInsightsAccessScopeAnalysisIds: []string{"example"},
    			NetworkInsightsAccessScopeId:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkInsightsAccessScopeAnalyses } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/network_insights_access_scope_analyses.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_network_insights_access_scope_analyses" });

      data(NetworkInsightsAccessScopeAnalyses, "example", {
        analysisStartTimeBegin: "example",
        analysisStartTimeEnd: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        networkInsightsAccessScopeAnalysisIds: ["example"],
        networkInsightsAccessScopeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import NetworkInsightsAccessScopeAnalyses, NetworkInsightsAccessScopeAnalysesConfig

    def describe():
        ubx.intent("look up aws_ec2_network_insights_access_scope_analyses")

        ubx.data(NetworkInsightsAccessScopeAnalyses, "example", NetworkInsightsAccessScopeAnalysesConfig(
            analysis_start_time_begin="example",
            analysis_start_time_end="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            network_insights_access_scope_analysis_ids=["example"],
            network_insights_access_scope_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analysis_start_time_begin" type="string">
  The earliest real analysis start timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="analysis_start_time_end" type="string">
  The latest real analysis start timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_insights_access_scope_analysis_ids" type="list(string)">
  The real, unique identifiers of the network insights access scope analysiss this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_insights_access_scope_id" type="string">
  The real, unique identifier of the network insights access scope this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="network_insights_access_scope_analyses" type="list(object)">
  The real VPC Network Access Analyzer access scope analyses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
