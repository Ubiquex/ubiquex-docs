---
title: "aws_ec2_reserved_instances_offerings"
description: "`aws_ec2_reserved_instances_offerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_reserved_instances_offerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_reserved_instances_offerings"})

    		ubx.Data(ec2.ReservedInstancesOfferings, "example", ec2.ReservedInstancesOfferingsConfig{
    			AvailabilityZone:   "example",
    			AvailabilityZoneId: "example",
    			DryRun:             true,
    			Filters:            []map[string]any{{"name": "example-name"}},
    			IncludeMarketplace: true,
    			InstanceTenancy:    "example",
    			InstanceType:       "example",
    			MaxDuration:        7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedInstancesOfferings } from "@ubx/sdk-aws/aws/data/ec2/reserved_instances_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_reserved_instances_offerings" });

      data(ReservedInstancesOfferings, "example", {
        availabilityZone: "example",
        availabilityZoneId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        includeMarketplace: true,
        instanceTenancy: "example",
        instanceType: "example",
        maxDuration: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ReservedInstancesOfferings, ReservedInstancesOfferingsConfig

    def describe():
        ubx.intent("look up aws_ec2_reserved_instances_offerings")

        ubx.data(ReservedInstancesOfferings, "example", ReservedInstancesOfferingsConfig(
            availability_zone="example",
            availability_zone_id="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            include_marketplace=True,
            instance_tenancy="example",
            instance_type="example",
            max_duration=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="availability_zone" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_marketplace" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_tenancy" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_duration" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_instance_count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_duration" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_class" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product_description" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instances_offering_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_instances_offerings" type="list(object)">
  Returned by the lookup.
</ResponseField>
