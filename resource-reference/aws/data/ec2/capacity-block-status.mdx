---
title: "aws_ec2_capacity_block_status"
description: "`aws_ec2_capacity_block_status` reads the real, current status of an EC2 Capacity Block reservation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_block_status"})

    		ubx.Data(ec2.CapacityBlockStatus, "example", ec2.CapacityBlockStatusConfig{
    			CapacityBlockIds: []string{"example"},
    			DryRun:           true,
    			Filters:          []map[string]any{{"name": "example-name"}},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityBlockStatus } from "@ubx/sdk-aws/aws/data/ec2/capacity_block_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_block_status" });

      data(CapacityBlockStatus, "example", {
        capacityBlockIds: ["example"],
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityBlockStatus, CapacityBlockStatusConfig
    from ubx.aws.data.ec2.capacity_block_status import CapacityBlockStatus_Filters

    def describe():
        ubx.intent("look up aws_ec2_capacity_block_status")

        ubx.data(CapacityBlockStatus, "example", CapacityBlockStatusConfig(
            capacity_block_ids=["example"],
            dry_run=True,
            filters=[CapacityBlockStatus_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_block_ids" type="list(string)">
  The real, unique identifiers of the capacity blocks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_block_statuses" type="list(object)">
  The real, current statuses of the capacity blocks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
