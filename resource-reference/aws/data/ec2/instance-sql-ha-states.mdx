---
title: "aws_ec2_instance_sql_ha_states"
description: "`aws_ec2_instance_sql_ha_states` reads the real, current SQL Server high-availability cluster state reported by an EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_sql_ha_states"})

    		ubx.Data(ec2.InstanceSqlHaStates, "example", ec2.InstanceSqlHaStatesConfig{
    			DryRun:      true,
    			Filters:     []map[string]any{{"name": "example-name"}},
    			InstanceIds: []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstanceSqlHaStates } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/instance_sql_ha_states.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_sql_ha_states" });

      data(InstanceSqlHaStates, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceSqlHaStates, InstanceSqlHaStatesConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_sql_ha_states")

        ubx.data(InstanceSqlHaStates, "example", InstanceSqlHaStatesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="list(object)">
  The real instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
