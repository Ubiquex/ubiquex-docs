---
title: "aws_ec2_verified_access_instance_logging_configurations"
description: "`data_aws_ec2_verified_access_instance_logging_configurations` looks up a real, already-existing `aws_ec2_verified_access_instance`."
---

It is the real, top-level AWS Verified Access resource that `aws_ec2_verified_access_trust_provider`s attach to, evaluating every request against zero-trust policies instead of relying on a traditional VPN for private application access.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_verified_access_instance_logging_configurations"})

    		ubx.Data(ec2.VerifiedAccessInstanceLoggingConfigurations, "example", ec2.VerifiedAccessInstanceLoggingConfigurationsConfig{
    			DryRun:                    true,
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			MaxResults:                1,
    			VerifiedAccessInstanceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VerifiedAccessInstanceLoggingConfigurations } from "@ubx/sdk-aws/aws/data/ec2/verified_access_instance_logging_configurations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_verified_access_instance_logging_configurations",
      });

      data(VerifiedAccessInstanceLoggingConfigurations, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        verifiedAccessInstanceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VerifiedAccessInstanceLoggingConfigurations, VerifiedAccessInstanceLoggingConfigurationsConfig
    from ubx.aws.data.ec2.verified_access_instance_logging_configurations import VerifiedAccessInstanceLoggingConfigurations_Filters

    def describe():
        ubx.intent("look up aws_ec2_verified_access_instance_logging_configurations")

        ubx.data(VerifiedAccessInstanceLoggingConfigurations, "example", VerifiedAccessInstanceLoggingConfigurationsConfig(
            dry_run=True,
            filters=[VerifiedAccessInstanceLoggingConfigurations_Filters(name="example-name")],
            max_results=1,
            verified_access_instance_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="verified_access_instance_ids" type="list(string)">
  The real, unique identifiers of the verified access instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_configurations" type="list(object)">
  The real logging configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
