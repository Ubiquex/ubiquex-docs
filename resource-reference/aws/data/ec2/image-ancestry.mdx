---
title: "aws_ec2_image_ancestry"
description: "`aws_ec2_image_ancestry` reads the real chain of parent AMIs an AMI was itself derived from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_image_ancestry"})

    		ubx.Data(ec2.ImageAncestry, "example", ec2.ImageAncestryConfig{
    			ImageId: "example",
    			DryRun:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ImageAncestry } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/image_ancestry.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_image_ancestry" });

      data(ImageAncestry, "example", {
        imageId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImageAncestry, ImageAncestryConfig

    def describe():
        ubx.intent("look up aws_ec2_image_ancestry")

        ubx.data(ImageAncestry, "example", ImageAncestryConfig(
            image_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  The real, unique identifier of the image this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_ancestry_entries" type="list(object)">
  The real ancestry entries for this image. **(AI-inferred)** Returned by the lookup.
</ResponseField>
