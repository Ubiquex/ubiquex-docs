---
title: "aws_ec2_image_ancestry"
description: "`aws_ec2_image_ancestry` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_image_ancestry` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_image_ancestry"})

    		ubx.Data(ec2.ImageAncestry, "example", ec2.ImageAncestryConfig{
    			ImageId: "example",
    			DryRun:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageAncestry } from "@ubx/sdk-aws/aws/data/ec2/image_ancestry";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_image_ancestry" });

      data(ImageAncestry, "example", {
        imageId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImageAncestry, ImageAncestryConfig

    def describe():
        ubx.intent("look up aws_ec2_image_ancestry")

        ubx.data(ImageAncestry, "example", ImageAncestryConfig(
            image_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_ancestry_entries" type="list(object)">
  Returned by the lookup.
</ResponseField>
