---
title: "aws_ec2_allowed_images_settings"
description: "`aws_ec2_allowed_images_settings` reads the real, account-wide policy restricting which AMIs are allowed to launch EC2 instances."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_allowed_images_settings"})

    		ubx.Data(ec2.AllowedImagesSettings, "example", ec2.AllowedImagesSettingsConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AllowedImagesSettings } from "@ubx/sdk-aws/aws/data/ec2/allowed_images_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_allowed_images_settings" });

      data(AllowedImagesSettings, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AllowedImagesSettings, AllowedImagesSettingsConfig

    def describe():
        ubx.intent("look up aws_ec2_allowed_images_settings")

        ubx.data(AllowedImagesSettings, "example", AllowedImagesSettingsConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_criteria" type="list(object)">
  The real criteria used to select this image. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
