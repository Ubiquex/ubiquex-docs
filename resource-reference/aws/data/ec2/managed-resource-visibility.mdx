---
title: "aws_ec2_managed_resource_visibility"
description: "`aws_ec2_managed_resource_visibility` reads the real, account-wide setting controlling whether AWS-managed EC2 resources are visible in API responses."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_managed_resource_visibility"})

    		ubx.Data(ec2.ManagedResourceVisibility, "example", ec2.ManagedResourceVisibilityConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ManagedResourceVisibility } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/managed_resource_visibility.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_managed_resource_visibility" });

      data(ManagedResourceVisibility, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ManagedResourceVisibility, ManagedResourceVisibilityConfig

    def describe():
        ubx.intent("look up aws_ec2_managed_resource_visibility")

        ubx.data(ManagedResourceVisibility, "example", ManagedResourceVisibilityConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="visibility" type="object">
  &lt;p&gt;Describes the managed resource visibility settings for the account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_visibility" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
