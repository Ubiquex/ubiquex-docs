---
title: "aws_ec2_managed_resource_visibility"
description: "`aws_ec2_managed_resource_visibility` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_managed_resource_visibility` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_managed_resource_visibility"})

    		ubx.Data(ec2.ManagedResourceVisibility, "example", ec2.ManagedResourceVisibilityConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedResourceVisibility } from "@ubx/sdk-aws/aws/data/ec2/managed_resource_visibility";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_managed_resource_visibility" });

      data(ManagedResourceVisibility, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ManagedResourceVisibility, ManagedResourceVisibilityConfig

    def describe():
        ubx.intent("look up aws_ec2_managed_resource_visibility")

        ubx.data(ManagedResourceVisibility, "example", ManagedResourceVisibilityConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="visibility" type="object">
  &lt;p&gt;Describes the managed resource visibility settings for the account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_visibility" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
