---
title: "aws_ec2_aggregate_id_format"
description: "`aws_ec2_aggregate_id_format` reads the real, current ID format setting (short or long IDs) across every EC2 resource type on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_aggregate_id_format"})

    		ubx.Data(ec2.AggregateIdFormat, "example", ec2.AggregateIdFormatConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AggregateIdFormat } from "@ubx/sdk-aws/aws/data/ec2/aggregate_id_format";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_aggregate_id_format" });

      data(AggregateIdFormat, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AggregateIdFormat, AggregateIdFormatConfig

    def describe():
        ubx.intent("look up aws_ec2_aggregate_id_format")

        ubx.data(AggregateIdFormat, "example", AggregateIdFormatConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statuses" type="list(object)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="use_long_ids_aggregated" type="bool">
  Whether long-format resource IDs are used, aggregated across this account. **(AI-inferred)** Returned by the lookup.
</ResponseField>
