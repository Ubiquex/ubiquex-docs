---
title: "aws_ec2_capacity_block_offerings"
description: "`aws_ec2_capacity_block_offerings` reads a real, available EC2 Capacity Block offering -- a time-bound, guaranteed block of GPU or ML-accelerator capacity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_block_offerings"})

    		ubx.Data(ec2.CapacityBlockOfferings, "example", ec2.CapacityBlockOfferingsConfig{
    			CapacityDurationHours: 7200,
    			AllAvailabilityZones:  true,
    			DryRun:                true,
    			EndDateRange:          "example",
    			InstanceCount:         1,
    			InstanceType:          "example",
    			MaxResults:            1,
    			StartDateRange:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityBlockOfferings } from "@ubx/sdk-aws/aws/data/ec2/capacity_block_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_block_offerings" });

      data(CapacityBlockOfferings, "example", {
        capacityDurationHours: 7200,
        allAvailabilityZones: true,
        dryRun: true,
        endDateRange: "example",
        instanceCount: 1,
        instanceType: "example",
        maxResults: 1,
        startDateRange: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityBlockOfferings, CapacityBlockOfferingsConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_block_offerings")

        ubx.data(CapacityBlockOfferings, "example", CapacityBlockOfferingsConfig(
            capacity_duration_hours=7200,
            all_availability_zones=True,
            dry_run=True,
            end_date_range="example",
            instance_count=1,
            instance_type="example",
            max_results=1,
            start_date_range="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="all_availability_zones" type="bool">
  The real zones of the all availability. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="capacity_duration_hours" type="number" required>
  The real duration this capacity reservation covers, in hours. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_date_range" type="string">
  The real, latest end date included in this range. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_count" type="number">
  The real count of instances. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string">
  The real instance type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_date_range" type="string">
  The real, earliest start date included in this range. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ultraserver_count" type="number">
  The real count of ultraservers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ultraserver_type" type="string">
  The real, discriminating type of the ultraserver this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_block_offerings" type="list(object)">
  The real EC2 Capacity Block offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
