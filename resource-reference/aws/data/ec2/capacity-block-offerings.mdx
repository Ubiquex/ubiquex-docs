---
title: "aws_ec2_capacity_block_offerings"
description: "`aws_ec2_capacity_block_offerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_capacity_block_offerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_block_offerings"})

    		ubx.Data(ec2.CapacityBlockOfferings, "example", ec2.CapacityBlockOfferingsConfig{
    			CapacityDurationHours: 7200,
    			AllAvailabilityZones:  true,
    			DryRun:                true,
    			EndDateRange:          "example",
    			InstanceCount:         1,
    			InstanceType:          "example",
    			MaxResults:            1,
    			StartDateRange:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityBlockOfferings } from "@ubx/sdk-aws/aws/data/ec2/capacity_block_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_block_offerings" });

      data(CapacityBlockOfferings, "example", {
        capacityDurationHours: 7200,
        allAvailabilityZones: true,
        dryRun: true,
        endDateRange: "example",
        instanceCount: 1,
        instanceType: "example",
        maxResults: 1,
        startDateRange: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityBlockOfferings, CapacityBlockOfferingsConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_block_offerings")

        ubx.data(CapacityBlockOfferings, "example", CapacityBlockOfferingsConfig(
            capacity_duration_hours=7200,
            all_availability_zones=True,
            dry_run=True,
            end_date_range="example",
            instance_count=1,
            instance_type="example",
            max_results=1,
            start_date_range="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="all_availability_zones" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="capacity_duration_hours" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_date_range" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_date_range" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ultraserver_count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ultraserver_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_block_offerings" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
