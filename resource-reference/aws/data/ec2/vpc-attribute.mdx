---
title: "aws_ec2_vpc_attribute"
description: "`aws_ec2_vpc_attribute` reads a real, configurable attribute (such as DNS support) of a VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_attribute"})

    		ubx.Data(ec2.VpcAttribute, "example", ec2.VpcAttributeConfig{
    			Attribute: "example",
    			VpcId:     "example",
    			DryRun:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VpcAttribute } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/vpc_attribute.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_attribute" });

      data(VpcAttribute, "example", {
        attribute: "example",
        vpcId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcAttribute, VpcAttributeConfig

    def describe():
        ubx.intent("look up aws_ec2_vpc_attribute")

        ubx.data(VpcAttribute, "example", VpcAttributeConfig(
            attribute="example",
            vpc_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute" type="string" required>
  The real attribute this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The real, unique identifier of the VPC this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enable_dns_hostnames" type="object">
  &lt;p&gt;Describes a value for a resource attribute that is a Boolean value.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="value" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="enable_dns_support" type="object">
  &lt;p&gt;Describes a value for a resource attribute that is a Boolean value.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="value" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="enable_network_address_usage_metrics" type="object">
  &lt;p&gt;Describes a value for a resource attribute that is a Boolean value.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="value" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
