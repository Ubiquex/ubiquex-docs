---
title: "aws_ec2_snapshot_attribute"
description: "`aws_ec2_snapshot_attribute` reads a real, configurable attribute (such as create-volume permissions) of an EBS snapshot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_snapshot_attribute"})

    		ubx.Data(ec2.SnapshotAttribute, "example", ec2.SnapshotAttributeConfig{
    			Attribute:  "example",
    			SnapshotId: "example",
    			DryRun:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotAttribute } from "@ubx/sdk-aws/aws/data/ec2/snapshot_attribute";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_snapshot_attribute" });

      data(SnapshotAttribute, "example", {
        attribute: "example",
        snapshotId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SnapshotAttribute, SnapshotAttributeConfig

    def describe():
        ubx.intent("look up aws_ec2_snapshot_attribute")

        ubx.data(SnapshotAttribute, "example", SnapshotAttributeConfig(
            attribute="example",
            snapshot_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute" type="string" required>
  The real attribute this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  The real, unique identifier of the snapshot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_volume_permissions" type="list(object)">
  The real permissions granted to create volumes from this snapshot. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="product_codes" type="list(object)">
  The real product codes associated with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
