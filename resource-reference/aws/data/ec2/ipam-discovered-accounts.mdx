---
title: "aws_ec2_ipam_discovered_accounts"
description: "`aws_ec2_ipam_discovered_accounts` reads a real AWS account IPAM discovered within its own monitored resource discovery scope."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_discovered_accounts"})

    		ubx.Data(ec2.IpamDiscoveredAccounts, "example", ec2.IpamDiscoveredAccountsConfig{
    			DiscoveryRegion:         "example",
    			IpamResourceDiscoveryId: "example",
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IpamDiscoveredAccounts } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ipam_discovered_accounts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_discovered_accounts" });

      data(IpamDiscoveredAccounts, "example", {
        discoveryRegion: "example",
        ipamResourceDiscoveryId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamDiscoveredAccounts, IpamDiscoveredAccountsConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_discovered_accounts")

        ubx.data(IpamDiscoveredAccounts, "example", IpamDiscoveredAccountsConfig(
            discovery_region="example",
            ipam_resource_discovery_id="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="discovery_region" type="string" required>
  The real AWS region resource discovery runs in. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_resource_discovery_id" type="string" required>
  The real, unique identifier of the VPC IPAM resource discovery this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_discovered_accounts" type="list(object)">
  The real VPC IPAM discovered accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
