---
title: "aws_ec2_transit_gateway_attachments"
description: "`data_aws_ec2_transit_gateway_attachments` looks up a real, already-existing `aws_ec2_transit_gateway_attachment`."
---

It is the real, general resource connecting a VPC, VPN, Direct Connect gateway, or another transit gateway to an `aws_ec2_transit_gateway`, the actual link every specific attachment type shares the same underlying mechanism for.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_transit_gateway_attachments"})

    		ubx.Data(ec2.TransitGatewayAttachments, "example", ec2.TransitGatewayAttachmentsConfig{
    			DryRun:                      true,
    			Filters:                     []map[string]any{{"name": "example-name"}},
    			MaxResults:                  1,
    			TransitGatewayAttachmentIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransitGatewayAttachments } from "@ubx/sdk-aws/aws/data/ec2/transit_gateway_attachments";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_transit_gateway_attachments" });

      data(TransitGatewayAttachments, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        transitGatewayAttachmentIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import TransitGatewayAttachments, TransitGatewayAttachmentsConfig

    def describe():
        ubx.intent("look up aws_ec2_transit_gateway_attachments")

        ubx.data(TransitGatewayAttachments, "example", TransitGatewayAttachmentsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            transit_gateway_attachment_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_attachment_ids" type="list(string)">
  The real, unique identifiers of the transit gateway attachments this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_attachments" type="list(object)">
  The real transit gateway attachments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
