---
title: "aws_ec2_instance_type_offerings"
description: "`aws_ec2_instance_type_offerings` reads which real EC2 instance types are offered in a given Region, Availability Zone, or Local Zone."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_type_offerings"})

    		ubx.Data(ec2.InstanceTypeOfferings, "example", ec2.InstanceTypeOfferingsConfig{
    			DryRun:       true,
    			Filters:      []map[string]any{{"name": "example-name"}},
    			LocationType: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceTypeOfferings } from "@ubx/sdk-aws/aws/data/ec2/instance_type_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_type_offerings" });

      data(InstanceTypeOfferings, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        locationType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceTypeOfferings, InstanceTypeOfferingsConfig
    from ubx.aws.data.ec2.instance_type_offerings import InstanceTypeOfferings_Filters

    def describe():
        ubx.intent("look up aws_ec2_instance_type_offerings")

        ubx.data(InstanceTypeOfferings, "example", InstanceTypeOfferingsConfig(
            dry_run=True,
            filters=[InstanceTypeOfferings_Filters(name="example-name")],
            location_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="location_type" type="string">
  The real, discriminating type of the location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_type_offerings" type="list(object)">
  The real instance type offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
