---
title: "aws_ec2_stale_security_groups"
description: "`aws_ec2_stale_security_groups` reads a real security group rule referencing a peered VPC that's since become invalid."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_stale_security_groups"})

    		ubx.Data(ec2.StaleSecurityGroups, "example", ec2.StaleSecurityGroupsConfig{
    			VpcId:      "example",
    			DryRun:     true,
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StaleSecurityGroups } from "@ubx/sdk-aws/aws/data/ec2/stale_security_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_stale_security_groups" });

      data(StaleSecurityGroups, "example", {
        vpcId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import StaleSecurityGroups, StaleSecurityGroupsConfig

    def describe():
        ubx.intent("look up aws_ec2_stale_security_groups")

        ubx.data(StaleSecurityGroups, "example", StaleSecurityGroupsConfig(
            vpc_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The real, unique identifier of the VPC this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stale_security_group_set" type="list(object)">
  The real stale security group set. **(AI-inferred)** Returned by the lookup.
</ResponseField>
