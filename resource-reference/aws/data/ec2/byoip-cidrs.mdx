---
title: "aws_ec2_byoip_cidrs"
description: "`aws_ec2_byoip_cidrs` reads a real, customer-owned IP address range (BYOIP) provisioned for use in AWS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_byoip_cidrs"})

    		ubx.Data(ec2.ByoipCidrs, "example", ec2.ByoipCidrsConfig{
    			MaxResults: 1,
    			DryRun:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ByoipCidrs } from "@ubx/sdk-aws/aws/data/ec2/byoip_cidrs";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_byoip_cidrs" });

      data(ByoipCidrs, "example", {
        maxResults: 1,
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ByoipCidrs, ByoipCidrsConfig

    def describe():
        ubx.intent("look up aws_ec2_byoip_cidrs")

        ubx.data(ByoipCidrs, "example", ByoipCidrsConfig(
            max_results=1,
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="byoip_cidrs" type="list(object)">
  The real Bring Your Own IP (BYOIP) CIDR blocks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
