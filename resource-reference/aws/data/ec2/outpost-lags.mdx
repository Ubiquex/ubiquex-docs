---
title: "aws_ec2_outpost_lags"
description: "`aws_ec2_outpost_lags` looks up a real link aggregation group (LAG) of local network interfaces on an AWS Outpost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_outpost_lags"})

    		ubx.Data(ec2.OutpostLags, "example", ec2.OutpostLagsConfig{
    			DryRun:        true,
    			Filters:       []map[string]any{{"name": "example-name"}},
    			MaxResults:    1,
    			OutpostLagIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutpostLags } from "@ubx/sdk-aws/aws/data/ec2/outpost_lags";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_outpost_lags" });

      data(OutpostLags, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        outpostLagIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import OutpostLags, OutpostLagsConfig

    def describe():
        ubx.intent("look up aws_ec2_outpost_lags")

        ubx.data(OutpostLags, "example", OutpostLagsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            outpost_lag_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_lag_ids" type="list(string)">
  The real, unique identifiers of the outpost lags this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="outpost_lags" type="list(object)">
  The real link aggregation groups (LAGs) on this AWS Outpost. **(AI-inferred)** Returned by the lookup.
</ResponseField>
