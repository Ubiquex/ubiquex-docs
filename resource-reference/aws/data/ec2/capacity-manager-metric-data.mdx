---
title: "aws_ec2_capacity_manager_metric_data"
description: "`data_aws_ec2_capacity_manager_metric_data` looks up a real, already-existing `aws_ec2_capacity_manager_data_export`."
---

It configures a real, scheduled export of EC2 Capacity Manager's own usage data, On-Demand, Spot, and Capacity Reservation metrics across an organization's accounts and Regions, to an S3 bucket. Amazon EC2 Capacity Manager reached general availability October 17, 2025, roughly ten months ago.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_manager_metric_data"})

    		ubx.Data(ec2.CapacityManagerMetricData, "example", ec2.CapacityManagerMetricDataConfig{
    			EndTime:     "example",
    			MetricNames: []string{"example"},
    			Period:      1,
    			StartTime:   "example",
    			DryRun:      true,
    			FilterBy:    []map[string]any{{"dimension_condition": map[string]any{"comparison": "example"}}},
    			GroupBy:     []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityManagerMetricData } from "@ubx/sdk-aws/aws/data/ec2/capacity_manager_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_manager_metric_data" });

      data(CapacityManagerMetricData, "example", {
        endTime: "example",
        metricNames: ["example"],
        period: 1,
        startTime: "example",
        dryRun: true,
        filterBy: [{ dimensionCondition: { comparison: "example" } }],
        groupBy: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityManagerMetricData, CapacityManagerMetricDataConfig
    from ubx.aws.data.ec2.capacity_manager_metric_data import CapacityManagerMetricData_FilterBy, CapacityManagerMetricData_FilterBy_DimensionCondition

    def describe():
        ubx.intent("look up aws_ec2_capacity_manager_metric_data")

        ubx.data(CapacityManagerMetricData, "example", CapacityManagerMetricDataConfig(
            end_time="example",
            metric_names=["example"],
            period=1,
            start_time="example",
            dry_run=True,
            filter_by=[CapacityManagerMetricData_FilterBy(dimension_condition=CapacityManagerMetricData_FilterBy_DimensionCondition(comparison="example"))],
            group_by=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="list(object)">
  The real field results are filtered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_names" type="list(string)" required>
  The real names of the metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data_results" type="list(object)">
  The real metric data results included in this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
