---
title: "aws_ec2_capacity_manager_metric_data"
description: "`aws_ec2_capacity_manager_metric_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_capacity_manager_metric_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_manager_metric_data"})

    		ubx.Data(ec2.CapacityManagerMetricData, "example", ec2.CapacityManagerMetricDataConfig{
    			EndTime:     "example",
    			MetricNames: []string{"example"},
    			Period:      1,
    			StartTime:   "example",
    			DryRun:      true,
    			FilterBy:    []map[string]any{{"dimension_condition": map[string]any{"comparison": "example"}}},
    			GroupBy:     []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityManagerMetricData } from "@ubx/sdk-aws/aws/data/ec2/capacity_manager_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_manager_metric_data" });

      data(CapacityManagerMetricData, "example", {
        endTime: "example",
        metricNames: ["example"],
        period: 1,
        startTime: "example",
        dryRun: true,
        filterBy: [{ dimensionCondition: { comparison: "example" } }],
        groupBy: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityManagerMetricData, CapacityManagerMetricDataConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_manager_metric_data")

        ubx.data(CapacityManagerMetricData, "example", CapacityManagerMetricDataConfig(
            end_time="example",
            metric_names=["example"],
            period=1,
            start_time="example",
            dry_run=True,
            filter_by=[{"dimension_condition": {"comparison": "example"}}],
            group_by=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metric_names" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data_results" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
