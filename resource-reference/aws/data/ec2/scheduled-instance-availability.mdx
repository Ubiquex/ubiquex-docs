---
title: "aws_ec2_scheduled_instance_availability"
description: "`aws_ec2_scheduled_instance_availability` reads a real, available EC2 Scheduled Instance purchase offering..."
---

`aws_ec2_scheduled_instance_availability` reads a real, available EC2 Scheduled Instance purchase offering, a retired AWS feature for reserving capacity on a recurring schedule.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_scheduled_instance_availability"})

    		ubx.Data(ec2.ScheduledInstanceAvailability, "example", ec2.ScheduledInstanceAvailabilityConfig{
    			FirstSlotStartTimeRange: map[string]any{"earliest_time": "example"},
    			Recurrence:              map[string]any{"frequency": "example"},
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			MaxResults:              1,
    			MaxSlotDurationInHours:  7200,
    			MinSlotDurationInHours:  7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ScheduledInstanceAvailability } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/scheduled_instance_availability.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_scheduled_instance_availability" });

      data(ScheduledInstanceAvailability, "example", {
        firstSlotStartTimeRange: { earliestTime: "example" },
        recurrence: { frequency: "example" },
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        maxSlotDurationInHours: 7200,
        minSlotDurationInHours: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ScheduledInstanceAvailability, ScheduledInstanceAvailabilityConfig

    def describe():
        ubx.intent("look up aws_ec2_scheduled_instance_availability")

        ubx.data(ScheduledInstanceAvailability, "example", ScheduledInstanceAvailabilityConfig(
            first_slot_start_time_range={"earliest_time": "example"},
            recurrence={"frequency": "example"},
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            max_slot_duration_in_hours=7200,
            min_slot_duration_in_hours=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="first_slot_start_time_range" type="object" required>
  &lt;p&gt;Describes the time period for a Scheduled Instance to start its first schedule. The time period must span less than one day.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="earliest_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="latest_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_slot_duration_in_hours" type="number">
  The real, maximum duration of this reservation slot, in hours. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="min_slot_duration_in_hours" type="number">
  The real, minimum duration of this reservation slot, in hours. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recurrence" type="object" required>
  &lt;p&gt;Describes the recurring schedule for a Scheduled Instance.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="frequency" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="interval" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="occurrence_days" type="list(number)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="occurrence_relative_to_end" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="occurrence_unit" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_instance_availability_set" type="list(object)">
  The real scheduled instance availability set. **(AI-inferred)** Returned by the lookup.
</ResponseField>
