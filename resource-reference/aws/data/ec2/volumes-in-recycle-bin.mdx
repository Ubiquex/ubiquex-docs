---
title: "aws_ec2_volumes_in_recycle_bin"
description: "`aws_ec2_volumes_in_recycle_bin` looks up a real, deleted EBS volume that's currently retained in EC2's own Recycle Bin."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_volumes_in_recycle_bin"})

    		ubx.Data(ec2.VolumesInRecycleBin, "example", ec2.VolumesInRecycleBinConfig{
    			DryRun:     true,
    			MaxResults: 1,
    			VolumeIds:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VolumesInRecycleBin } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/volumes_in_recycle_bin.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_volumes_in_recycle_bin" });

      data(VolumesInRecycleBin, "example", {
        dryRun: true,
        maxResults: 1,
        volumeIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VolumesInRecycleBin, VolumesInRecycleBinConfig

    def describe():
        ubx.intent("look up aws_ec2_volumes_in_recycle_bin")

        ubx.data(VolumesInRecycleBin, "example", VolumesInRecycleBinConfig(
            dry_run=True,
            max_results=1,
            volume_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="volume_ids" type="list(string)">
  The real, unique identifiers of the volumes this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="volumes" type="list(object)">
  The real volumes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
