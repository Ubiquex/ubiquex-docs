---
title: "aws_ec2_conversion_tasks"
description: "`aws_ec2_conversion_tasks` reads the real, current status of an EC2 VM Import/Export conversion task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_conversion_tasks"})

    		ubx.Data(ec2.ConversionTasks, "example", ec2.ConversionTasksConfig{
    			ConversionTaskIds: []string{"example"},
    			DryRun:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConversionTasks } from "@ubx/sdk-aws/aws/data/ec2/conversion_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_conversion_tasks" });

      data(ConversionTasks, "example", {
        conversionTaskIds: ["example"],
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ConversionTasks, ConversionTasksConfig

    def describe():
        ubx.intent("look up aws_ec2_conversion_tasks")

        ubx.data(ConversionTasks, "example", ConversionTasksConfig(
            conversion_task_ids=["example"],
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="conversion_task_ids" type="list(string)">
  The real, unique identifiers of the conversion tasks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="conversion_tasks" type="list(object)">
  The real EC2 conversion tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
