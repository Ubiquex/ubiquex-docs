---
title: "aws_ec2_reserved_instances_exchange_quote"
description: "`aws_ec2_reserved_instances_exchange_quote` reads a real, previewed quote for exchanging one or more Convertible Reserved Instances for another..."
---

`aws_ec2_reserved_instances_exchange_quote` reads a real, previewed quote for exchanging one or more Convertible Reserved Instances for another configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_reserved_instances_exchange_quote"})

    		ubx.Data(ec2.ReservedInstancesExchangeQuote, "example", ec2.ReservedInstancesExchangeQuoteConfig{
    			ReservedInstanceIds:  []string{"example"},
    			DryRun:               true,
    			TargetConfigurations: []map[string]any{{"instance_count": 1}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReservedInstancesExchangeQuote } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/reserved_instances_exchange_quote.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_reserved_instances_exchange_quote" });

      data(ReservedInstancesExchangeQuote, "example", {
        reservedInstanceIds: ["example"],
        dryRun: true,
        targetConfigurations: [{ instanceCount: 1 }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ReservedInstancesExchangeQuote, ReservedInstancesExchangeQuoteConfig

    def describe():
        ubx.intent("look up aws_ec2_reserved_instances_exchange_quote")

        ubx.data(ReservedInstancesExchangeQuote, "example", ReservedInstancesExchangeQuoteConfig(
            reserved_instance_ids=["example"],
            dry_run=True,
            target_configurations=[{"instance_count": 1}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instance_ids" type="list(string)" required>
  The real, unique identifiers of the reserved instances this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="target_configurations" type="list(object)">
  The real, target configurations for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="currency_code" type="string">
  The real currency this value is denominated in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_valid_exchange" type="bool">
  Whether this reserved instance exchange offering is currently valid. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="output_reserved_instances_will_expire_at" type="string">
  The real timestamp the output Reserved Instances will expire. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="payment_due" type="string">
  The real, outstanding payment due for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_instance_value_rollup" type="object">
  &lt;p&gt;The cost associated with the Reserved Instance.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="hourly_price" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="remaining_total_value" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="remaining_upfront_value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reserved_instance_value_set" type="list(object)">
  The real reserved instance value set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_configuration_value_rollup" type="object">
  &lt;p&gt;The cost associated with the Reserved Instance.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="hourly_price" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="remaining_total_value" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="remaining_upfront_value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_configuration_value_set" type="list(object)">
  The real target configuration value set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="validation_failure_reason" type="string">
  The real, human-readable reason for the validation failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>
