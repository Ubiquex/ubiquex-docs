---
title: "aws_ec2_network_interface_attribute"
description: "`aws_ec2_network_interface_attribute` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_network_interface_attribute` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_network_interface_attribute"})

    		ubx.Data(ec2.NetworkInterfaceAttribute, "example", ec2.NetworkInterfaceAttributeConfig{
    			NetworkInterfaceId: "example",
    			Attribute:          "example",
    			DryRun:             true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkInterfaceAttribute } from "@ubx/sdk-aws/aws/data/ec2/network_interface_attribute";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_network_interface_attribute" });

      data(NetworkInterfaceAttribute, "example", {
        networkInterfaceId: "example",
        attribute: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import NetworkInterfaceAttribute, NetworkInterfaceAttributeConfig

    def describe():
        ubx.intent("look up aws_ec2_network_interface_attribute")

        ubx.data(NetworkInterfaceAttribute, "example", NetworkInterfaceAttributeConfig(
            network_interface_id="example",
            attribute="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network_interface_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associate_public_ip_address" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="attachment" type="object">
  &lt;p&gt;Describes a network interface attachment.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attach_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="attachment_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="delete_on_termination" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_index" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ena_queue_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ena_srd_specification" type="object">
      &lt;p&gt;ENA Express uses Amazon Web Services Scalable Reliable Datagram (SRD) technology to increase the maximum bandwidth used per stream and minimize tail latency of network traffic between EC2 instances. With ENA Express, you can communicate between two EC2 instances in the same subnet within the same account, or in different accounts. Both sending and receiving instances must have ENA Express enabled.&lt;/p&gt; &lt;p&gt;To improve the reliability of network packet delivery, ENA Express reorders network packets on the receiving end by default. However, some UDP-based applications are designed to handle network packets that are out of order to reduce the overhead for packet delivery at the network layer. When ENA Express is enabled, you can specify whether UDP network traffic uses it.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="ena_srd_enabled" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="ena_srd_udp_specification" type="object">
          &lt;p&gt;ENA Express is compatible with both TCP and UDP transport protocols. When it's enabled, TCP traffic automatically uses it. However, some UDP-based applications are designed to handle network packets that are out of order, without a need for retransmission, such as live video broadcasting or other near-real-time applications. For UDP traffic, you can specify whether to use ENA Express, based on your application environment needs.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="ena_srd_udp_enabled" type="bool">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_owner_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network_card_index" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="object">
  &lt;p&gt;Describes a value for a resource attribute that is a String.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="groups" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="source_dest_check" type="object">
  &lt;p&gt;Describes a value for a resource attribute that is a Boolean value.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="value" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
