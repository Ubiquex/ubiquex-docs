---
title: "aws_ec2_route_server_propagations"
description: "`aws_ec2_route_server_propagations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_route_server_propagations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_route_server_propagations"})

    		ubx.Data(ec2.RouteServerPropagations, "example", ec2.RouteServerPropagationsConfig{
    			RouteServerId: "example",
    			DryRun:        true,
    			RouteTableId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RouteServerPropagations } from "@ubx/sdk-aws/aws/data/ec2/route_server_propagations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_route_server_propagations" });

      data(RouteServerPropagations, "example", {
        routeServerId: "example",
        dryRun: true,
        routeTableId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import RouteServerPropagations, RouteServerPropagationsConfig

    def describe():
        ubx.intent("look up aws_ec2_route_server_propagations")

        ubx.data(RouteServerPropagations, "example", RouteServerPropagationsConfig(
            route_server_id="example",
            dry_run=True,
            route_table_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="route_server_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="route_table_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="route_server_propagations" type="list(object)">
  Returned by the lookup.
</ResponseField>
