---
title: "aws_ec2_account_attributes"
description: "`aws_ec2_account_attributes` reads real, account-level EC2 attributes and limits, such as the default VPC and the maximum number of instances allowed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_account_attributes"})

    		ubx.Data(ec2.AccountAttributes, "example", ec2.AccountAttributesConfig{
    			AttributeNames: []string{"example"},
    			DryRun:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAttributes } from "@ubx/sdk-aws/aws/data/ec2/account_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_account_attributes" });

      data(AccountAttributes, "example", {
        attributeNames: ["example"],
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_ec2_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
            attribute_names=["example"],
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_names" type="list(string)">
  The real names of the attributes included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_attributes" type="list(object)">
  The real account-level attributes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
