---
title: "aws_ec2_transit_gateway_multicast_domain_associations"
description: "`data_aws_ec2_transit_gateway_multicast_domain_associations` looks up a real, already-existing `aws_ec2_transit_gateway_multicast_domain_association`."
---

It associates a real VPC attachment and subnet with an `aws_ec2_transit_gateway_multicast_domain`, the resource that actually makes that subnet's resources eligible to send or receive multicast traffic through the domain.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_transit_gateway_multicast_domain_associations"})

    		ubx.Data(ec2.TransitGatewayMulticastDomainAssociations, "example", ec2.TransitGatewayMulticastDomainAssociationsConfig{
    			TransitGatewayMulticastDomainId: "example",
    			DryRun:                          true,
    			Filters:                         []map[string]any{{"name": "example-name"}},
    			MaxResults:                      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransitGatewayMulticastDomainAssociations } from "@ubx/sdk-aws/aws/data/ec2/transit_gateway_multicast_domain_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_transit_gateway_multicast_domain_associations",
      });

      data(TransitGatewayMulticastDomainAssociations, "example", {
        transitGatewayMulticastDomainId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import TransitGatewayMulticastDomainAssociations, TransitGatewayMulticastDomainAssociationsConfig
    from ubx.aws.data.ec2.transit_gateway_multicast_domain_associations import TransitGatewayMulticastDomainAssociations_Filters

    def describe():
        ubx.intent("look up aws_ec2_transit_gateway_multicast_domain_associations")

        ubx.data(TransitGatewayMulticastDomainAssociations, "example", TransitGatewayMulticastDomainAssociationsConfig(
            transit_gateway_multicast_domain_id="example",
            dry_run=True,
            filters=[TransitGatewayMulticastDomainAssociations_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_multicast_domain_id" type="string" required>
  The real, unique identifier of the transit gateway multicast domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="multicast_domain_associations" type="list(object)">
  The real transit gateway multicast domain associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
