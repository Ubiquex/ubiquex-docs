---
title: "aws_ec2_ebs_default_kms_key_id"
description: "`aws_ec2_ebs_default_kms_key_id` reads the real, default KMS key used to encrypt new EBS volumes on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ebs_default_kms_key_id"})

    		ubx.Data(ec2.EbsDefaultKmsKeyId, "example", ec2.EbsDefaultKmsKeyIdConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EbsDefaultKmsKeyId } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ebs_default_kms_key_id.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ebs_default_kms_key_id" });

      data(EbsDefaultKmsKeyId, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import EbsDefaultKmsKeyId, EbsDefaultKmsKeyIdConfig

    def describe():
        ubx.intent("look up aws_ec2_ebs_default_kms_key_id")

        ubx.data(EbsDefaultKmsKeyId, "example", EbsDefaultKmsKeyIdConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kms_key_id" type="string">
  The real, unique identifier of the AWS KMS key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
