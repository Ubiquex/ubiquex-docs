---
title: "aws_ec2_vpn_connection_device_sample_configuration"
description: "`aws_ec2_vpn_connection_device_sample_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_vpn_connection_device_sample_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpn_connection_device_sample_configuration"})

    		ubx.Data(ec2.VpnConnectionDeviceSampleConfiguration, "example", ec2.VpnConnectionDeviceSampleConfigurationConfig{
    			VpnConnectionDeviceTypeId:  "example",
    			VpnConnectionId:            "example",
    			DryRun:                     true,
    			InternetKeyExchangeVersion: "example",
    			SampleType:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpnConnectionDeviceSampleConfiguration } from "@ubx/sdk-aws/aws/data/ec2/vpn_connection_device_sample_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_vpn_connection_device_sample_configuration",
      });

      data(VpnConnectionDeviceSampleConfiguration, "example", {
        vpnConnectionDeviceTypeId: "example",
        vpnConnectionId: "example",
        dryRun: true,
        internetKeyExchangeVersion: "example",
        sampleType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpnConnectionDeviceSampleConfiguration, VpnConnectionDeviceSampleConfigurationConfig

    def describe():
        ubx.intent("look up aws_ec2_vpn_connection_device_sample_configuration")

        ubx.data(VpnConnectionDeviceSampleConfiguration, "example", VpnConnectionDeviceSampleConfigurationConfig(
            vpn_connection_device_type_id="example",
            vpn_connection_id="example",
            dry_run=True,
            internet_key_exchange_version="example",
            sample_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="internet_key_exchange_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sample_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vpn_connection_device_type_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="vpn_connection_device_sample_configuration" type="string">
  Returned by the lookup.
</ResponseField>
