---
title: "aws_ec2_ebs_encryption_by_default"
description: "`aws_ec2_ebs_encryption_by_default` reads the real, account-wide setting requiring EBS encryption by default for every new volume."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ebs_encryption_by_default"})

    		ubx.Data(ec2.EbsEncryptionByDefault, "example", ec2.EbsEncryptionByDefaultConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EbsEncryptionByDefault } from "@ubx/sdk-aws/aws/data/ec2/ebs_encryption_by_default";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ebs_encryption_by_default" });

      data(EbsEncryptionByDefault, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import EbsEncryptionByDefault, EbsEncryptionByDefaultConfig

    def describe():
        ubx.intent("look up aws_ec2_ebs_encryption_by_default")

        ubx.data(EbsEncryptionByDefault, "example", EbsEncryptionByDefaultConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ebs_encryption_by_default" type="bool">
  Whether new EBS volumes are encrypted by default. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sse_type" type="string">
  The real, discriminating type of the sse this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
