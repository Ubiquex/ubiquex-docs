---
title: "aws_ec2_vpc_endpoint_associations"
description: "`aws_ec2_vpc_endpoint_associations` reads a real association between a VPC endpoint and a service network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_endpoint_associations"})

    		ubx.Data(ec2.VpcEndpointAssociations, "example", ec2.VpcEndpointAssociationsConfig{
    			DryRun:         true,
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			VpcEndpointIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VpcEndpointAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/vpc_endpoint_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_endpoint_associations" });

      data(VpcEndpointAssociations, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        vpcEndpointIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcEndpointAssociations, VpcEndpointAssociationsConfig

    def describe():
        ubx.intent("look up aws_ec2_vpc_endpoint_associations")

        ubx.data(VpcEndpointAssociations, "example", VpcEndpointAssociationsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            vpc_endpoint_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_endpoint_ids" type="list(string)">
  The real, unique identifiers of the vpc endpoints this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_associations" type="list(object)">
  The real VPC endpoint associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
