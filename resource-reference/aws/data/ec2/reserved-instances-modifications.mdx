---
title: "aws_ec2_reserved_instances_modifications"
description: "`aws_ec2_reserved_instances_modifications` reads the real, current status of a submitted modification to an EC2 Reserved Instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_reserved_instances_modifications"})

    		ubx.Data(ec2.ReservedInstancesModifications, "example", ec2.ReservedInstancesModificationsConfig{
    			Filters:                          []map[string]any{{"name": "example-name"}},
    			ReservedInstancesModificationIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReservedInstancesModifications } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/reserved_instances_modifications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_reserved_instances_modifications" });

      data(ReservedInstancesModifications, "example", {
        filters: [{ name: "example-name" }],
        reservedInstancesModificationIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ReservedInstancesModifications, ReservedInstancesModificationsConfig

    def describe():
        ubx.intent("look up aws_ec2_reserved_instances_modifications")

        ubx.data(ReservedInstancesModifications, "example", ReservedInstancesModificationsConfig(
            filters=[{"name": "example-name"}],
            reserved_instances_modification_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instances_modification_ids" type="list(string)">
  The real, unique identifiers of the reserved instances modifications this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_instances_modifications" type="list(object)">
  The real Reserved Instances modifications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
