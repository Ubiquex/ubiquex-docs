---
title: "aws_ec2_service_link_virtual_interfaces"
description: "`aws_ec2_service_link_virtual_interfaces` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_service_link_virtual_interfaces` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_service_link_virtual_interfaces"})

    		ubx.Data(ec2.ServiceLinkVirtualInterfaces, "example", ec2.ServiceLinkVirtualInterfacesConfig{
    			DryRun:                         true,
    			Filters:                        []map[string]any{{"name": "example-name"}},
    			MaxResults:                     1,
    			ServiceLinkVirtualInterfaceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceLinkVirtualInterfaces } from "@ubx/sdk-aws/aws/data/ec2/service_link_virtual_interfaces";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_service_link_virtual_interfaces" });

      data(ServiceLinkVirtualInterfaces, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        serviceLinkVirtualInterfaceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ServiceLinkVirtualInterfaces, ServiceLinkVirtualInterfacesConfig

    def describe():
        ubx.intent("look up aws_ec2_service_link_virtual_interfaces")

        ubx.data(ServiceLinkVirtualInterfaces, "example", ServiceLinkVirtualInterfacesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            service_link_virtual_interface_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_link_virtual_interface_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_link_virtual_interfaces" type="list(object)">
  Returned by the lookup.
</ResponseField>
