---
title: "aws_ec2_service_link_virtual_interfaces"
description: "`aws_ec2_service_link_virtual_interfaces` reads a real virtual interface used by an AWS Outpost's own service link connection back to the parent Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_service_link_virtual_interfaces"})

    		ubx.Data(ec2.ServiceLinkVirtualInterfaces, "example", ec2.ServiceLinkVirtualInterfacesConfig{
    			DryRun:                         true,
    			Filters:                        []map[string]any{{"name": "example-name"}},
    			MaxResults:                     1,
    			ServiceLinkVirtualInterfaceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ServiceLinkVirtualInterfaces } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/service_link_virtual_interfaces.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_service_link_virtual_interfaces" });

      data(ServiceLinkVirtualInterfaces, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        serviceLinkVirtualInterfaceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ServiceLinkVirtualInterfaces, ServiceLinkVirtualInterfacesConfig

    def describe():
        ubx.intent("look up aws_ec2_service_link_virtual_interfaces")

        ubx.data(ServiceLinkVirtualInterfaces, "example", ServiceLinkVirtualInterfacesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            service_link_virtual_interface_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_link_virtual_interface_ids" type="list(string)">
  The real, unique identifiers of the service link virtual interfaces this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_link_virtual_interfaces" type="list(object)">
  The real AWS Direct Connect service-link virtual interfaces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
