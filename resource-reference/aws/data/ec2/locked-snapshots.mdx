---
title: "aws_ec2_locked_snapshots"
description: "`aws_ec2_locked_snapshots` reads the real lock configuration (compliance or governance mode) applied to an EBS snapshot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_locked_snapshots"})

    		ubx.Data(ec2.LockedSnapshots, "example", ec2.LockedSnapshotsConfig{
    			DryRun:      true,
    			Filters:     []map[string]any{{"name": "example-name"}},
    			MaxResults:  1,
    			SnapshotIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LockedSnapshots } from "@ubx/sdk-aws/aws/data/ec2/locked_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_locked_snapshots" });

      data(LockedSnapshots, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        snapshotIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import LockedSnapshots, LockedSnapshotsConfig
    from ubx.aws.data.ec2.locked_snapshots import LockedSnapshots_Filters

    def describe():
        ubx.intent("look up aws_ec2_locked_snapshots")

        ubx.data(LockedSnapshots, "example", LockedSnapshotsConfig(
            dry_run=True,
            filters=[LockedSnapshots_Filters(name="example-name")],
            max_results=1,
            snapshot_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_ids" type="list(string)">
  The real, unique identifiers of the snapshots this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="snapshots" type="list(object)">
  The real snapshots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
