---
title: "aws_ec2_declarative_policies_report_summary"
description: "`aws_ec2_declarative_policies_report_summary` reads a real, summarized report of an EC2 account attribute's own declarative policy compliance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_declarative_policies_report_summary"})

    		ubx.Data(ec2.DeclarativePoliciesReportSummary, "example", ec2.DeclarativePoliciesReportSummaryConfig{
    			ReportId: "example",
    			DryRun:   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DeclarativePoliciesReportSummary } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/declarative_policies_report_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_declarative_policies_report_summary" });

      data(DeclarativePoliciesReportSummary, "example", {
        reportId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import DeclarativePoliciesReportSummary, DeclarativePoliciesReportSummaryConfig

    def describe():
        ubx.intent("look up aws_ec2_declarative_policies_report_summary")

        ubx.data(DeclarativePoliciesReportSummary, "example", DeclarativePoliciesReportSummaryConfig(
            report_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="report_id" type="string" required>
  The real, unique identifier of the report this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute_summaries" type="list(object)">
  The real, summarized attributes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_accounts" type="number">
  The real number of accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_failed_accounts" type="number">
  The real number of accounts that failed to process. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_bucket" type="string">
  The real S3 bucket used by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  The real S3 key prefix used for the s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_id" type="string">
  The real, unique identifier of the target this action applies to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
