---
title: "aws_ec2_ipam_discovered_resource_cidrs"
description: "`aws_ec2_ipam_discovered_resource_cidrs` reads a real IP address range IPAM discovered attached to a resource within its own monitored scope."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_discovered_resource_cidrs"})

    		ubx.Data(ec2.IpamDiscoveredResourceCidrs, "example", ec2.IpamDiscoveredResourceCidrsConfig{
    			IpamResourceDiscoveryId: "example",
    			ResourceRegion:          "example",
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamDiscoveredResourceCidrs } from "@ubx/sdk-aws/aws/data/ec2/ipam_discovered_resource_cidrs";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_discovered_resource_cidrs" });

      data(IpamDiscoveredResourceCidrs, "example", {
        ipamResourceDiscoveryId: "example",
        resourceRegion: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamDiscoveredResourceCidrs, IpamDiscoveredResourceCidrsConfig
    from ubx.aws.data.ec2.ipam_discovered_resource_cidrs import IpamDiscoveredResourceCidrs_Filters

    def describe():
        ubx.intent("look up aws_ec2_ipam_discovered_resource_cidrs")

        ubx.data(IpamDiscoveredResourceCidrs, "example", IpamDiscoveredResourceCidrsConfig(
            ipam_resource_discovery_id="example",
            resource_region="example",
            dry_run=True,
            filters=[IpamDiscoveredResourceCidrs_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_resource_discovery_id" type="string" required>
  The real, unique identifier of the VPC IPAM resource discovery this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_region" type="string" required>
  The real AWS region the resource is located in. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_discovered_resource_cidrs" type="list(object)">
  The real VPC IPAM discovered resource CIDRs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
