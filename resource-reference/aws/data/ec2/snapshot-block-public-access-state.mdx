---
title: "aws_ec2_snapshot_block_public_access_state"
description: "`data_aws_ec2_snapshot_block_public_access_state` looks up a real, already-existing `aws_ec2_snapshot_block_public_access`."
---

It sets a real, account-wide block on public access to EBS snapshots, preventing any `aws_ec2_volume` snapshot from being made public even if an individual snapshot's own permissions would otherwise allow it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_snapshot_block_public_access_state"})

    		ubx.Data(ec2.SnapshotBlockPublicAccessState, "example", ec2.SnapshotBlockPublicAccessStateConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotBlockPublicAccessState } from "@ubx/sdk-aws/aws/data/ec2/snapshot_block_public_access_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_snapshot_block_public_access_state" });

      data(SnapshotBlockPublicAccessState, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import SnapshotBlockPublicAccessState, SnapshotBlockPublicAccessStateConfig

    def describe():
        ubx.intent("look up aws_ec2_snapshot_block_public_access_state")

        ubx.data(SnapshotBlockPublicAccessState, "example", SnapshotBlockPublicAccessStateConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
