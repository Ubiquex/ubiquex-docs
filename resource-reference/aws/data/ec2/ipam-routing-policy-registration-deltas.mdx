---
title: "aws_ec2_ipam_routing_policy_registration_deltas"
description: "`aws_ec2_ipam_routing_policy_registration_deltas` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_ipam_routing_policy_registration_deltas` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_routing_policy_registration_deltas"})

    		ubx.Data(ec2.IpamRoutingPolicyRegistrationDeltas, "example", ec2.IpamRoutingPolicyRegistrationDeltasConfig{
    			IpamInternetRegistryAssociationId: "example",
    			ChronologicalOrder:                "example",
    			DeltaId:                           "example",
    			DryRun:                            true,
    			EndTime:                           "example",
    			MaxResults:                        1,
    			StartTime:                         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamRoutingPolicyRegistrationDeltas } from "@ubx/sdk-aws/aws/data/ec2/ipam_routing_policy_registration_deltas";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_ipam_routing_policy_registration_deltas",
      });

      data(IpamRoutingPolicyRegistrationDeltas, "example", {
        ipamInternetRegistryAssociationId: "example",
        chronologicalOrder: "example",
        deltaId: "example",
        dryRun: true,
        endTime: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamRoutingPolicyRegistrationDeltas, IpamRoutingPolicyRegistrationDeltasConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_routing_policy_registration_deltas")

        ubx.data(IpamRoutingPolicyRegistrationDeltas, "example", IpamRoutingPolicyRegistrationDeltasConfig(
            ipam_internet_registry_association_id="example",
            chronological_order="example",
            delta_id="example",
            dry_run=True,
            end_time="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="chronological_order" type="string">
  &lt;p&gt;The chronological order for returning results.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="delta_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_internet_registry_association_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_routing_policy_registration_deltas" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
