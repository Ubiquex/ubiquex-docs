---
title: "aws_ec2_enabled_ipam_policy"
description: "`aws_ec2_enabled_ipam_policy` reads the real, currently enabled IP address management (IPAM) policy on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_enabled_ipam_policy"})

    		ubx.Data(ec2.EnabledIpamPolicy, "example", ec2.EnabledIpamPolicyConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnabledIpamPolicy } from "@ubx/sdk-aws/aws/data/ec2/enabled_ipam_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_enabled_ipam_policy" });

      data(EnabledIpamPolicy, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import EnabledIpamPolicy, EnabledIpamPolicyConfig

    def describe():
        ubx.intent("look up aws_ec2_enabled_ipam_policy")

        ubx.data(EnabledIpamPolicy, "example", EnabledIpamPolicyConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_policy_enabled" type="bool">
  Whether the ipam policy is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ipam_policy_id" type="string">
  The real, unique identifier of the ipam policy this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
