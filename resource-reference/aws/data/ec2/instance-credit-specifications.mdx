---
title: "aws_ec2_instance_credit_specifications"
description: "`aws_ec2_instance_credit_specifications` reads the real CPU credit option (standard or unlimited) currently applied to a burstable (T-family) instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_instance_credit_specifications"})

    		ubx.Data(ec2.InstanceCreditSpecifications, "example", ec2.InstanceCreditSpecificationsConfig{
    			DryRun:      true,
    			Filters:     []map[string]any{{"name": "example-name"}},
    			InstanceIds: []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceCreditSpecifications } from "@ubx/sdk-aws/aws/data/ec2/instance_credit_specifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_instance_credit_specifications" });

      data(InstanceCreditSpecifications, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import InstanceCreditSpecifications, InstanceCreditSpecificationsConfig

    def describe():
        ubx.intent("look up aws_ec2_instance_credit_specifications")

        ubx.data(InstanceCreditSpecifications, "example", InstanceCreditSpecificationsConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_credit_specifications" type="list(object)">
  The real burstable-performance credit specifications for these instances. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
