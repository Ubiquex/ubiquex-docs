---
title: "aws_ec2_reserved_instances"
description: "`aws_ec2_reserved_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_reserved_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_reserved_instances"})

    		ubx.Data(ec2.ReservedInstances, "example", ec2.ReservedInstancesConfig{
    			DryRun:               true,
    			Filters:              []map[string]any{{"name": "example-name"}},
    			OfferingClass:        "example",
    			OfferingType:         "example",
    			ReservedInstancesIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedInstances } from "@ubx/sdk-aws/aws/data/ec2/reserved_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_reserved_instances" });

      data(ReservedInstances, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        offeringClass: "example",
        offeringType: "example",
        reservedInstancesIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ReservedInstances, ReservedInstancesConfig

    def describe():
        ubx.intent("look up aws_ec2_reserved_instances")

        ubx.data(ReservedInstances, "example", ReservedInstancesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            offering_class="example",
            offering_type="example",
            reserved_instances_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_class" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instances_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="reserved_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
