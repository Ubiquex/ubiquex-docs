---
title: "aws_ec2_network_insights_access_scope_content"
description: "`data_aws_ec2_network_insights_access_scope_content` looks up a real, already-existing `aws_ec2_network_insights_access_scope`."
---

It defines a real, broader set of network paths and conditions to evaluate together, going beyond one `aws_ec2_network_insights_path`'s single source-destination pair to check an entire access boundary, everything that should or shouldn't be reachable from the internet, for instance, at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_network_insights_access_scope_content"})

    		ubx.Data(ec2.NetworkInsightsAccessScopeContent, "example", ec2.NetworkInsightsAccessScopeContentConfig{
    			NetworkInsightsAccessScopeId: "example",
    			DryRun:                       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkInsightsAccessScopeContent } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/network_insights_access_scope_content.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_network_insights_access_scope_content" });

      data(NetworkInsightsAccessScopeContent, "example", {
        networkInsightsAccessScopeId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import NetworkInsightsAccessScopeContent, NetworkInsightsAccessScopeContentConfig

    def describe():
        ubx.intent("look up aws_ec2_network_insights_access_scope_content")

        ubx.data(NetworkInsightsAccessScopeContent, "example", NetworkInsightsAccessScopeContentConfig(
            network_insights_access_scope_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_insights_access_scope_id" type="string" required>
  The real, unique identifier of the network insights access scope this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="network_insights_access_scope_content" type="object">
  &lt;p&gt;Describes the Network Access Scope content.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="exclude_paths" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="match_paths" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network_insights_access_scope_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
