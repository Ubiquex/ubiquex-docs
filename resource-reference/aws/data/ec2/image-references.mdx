---
title: "aws_ec2_image_references"
description: "`aws_ec2_image_references` reads the real EC2 resources (such as launch templates) that currently reference a given AMI."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_image_references"})

    		ubx.Data(ec2.ImageReferences, "example", ec2.ImageReferencesConfig{
    			ImageIds:                []string{"example"},
    			DryRun:                  true,
    			IncludeAllResourceTypes: true,
    			MaxResults:              1,
    			ResourceTypes:           []map[string]any{{"resource_type": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageReferences } from "@ubx/sdk-aws/aws/data/ec2/image_references";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_image_references" });

      data(ImageReferences, "example", {
        imageIds: ["example"],
        dryRun: true,
        includeAllResourceTypes: true,
        maxResults: 1,
        resourceTypes: [{ resourceType: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImageReferences, ImageReferencesConfig

    def describe():
        ubx.intent("look up aws_ec2_image_references")

        ubx.data(ImageReferences, "example", ImageReferencesConfig(
            image_ids=["example"],
            dry_run=True,
            include_all_resource_types=True,
            max_results=1,
            resource_types=[{"resource_type": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="image_ids" type="list(string)" required>
  The real, unique identifiers of the images this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_all_resource_types" type="bool">
  The real, discriminating types of include all resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(object)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_references" type="list(object)">
  The real references to this image. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
