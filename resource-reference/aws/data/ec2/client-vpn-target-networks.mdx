---
title: "aws_ec2_client_vpn_target_networks"
description: "`data_aws_ec2_client_vpn_target_networks` looks up a real, already-existing `aws_ec2_client_vpn_target_network_association`."
---

It is the real, actual link between an `aws_ec2_client_vpn_endpoint` and a specific VPC subnet. An endpoint with no target network association has nowhere to actually route connected clients' traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_client_vpn_target_networks"})

    		ubx.Data(ec2.ClientVpnTargetNetworks, "example", ec2.ClientVpnTargetNetworksConfig{
    			ClientVpnEndpointId: "example",
    			AssociationIds:      []string{"example"},
    			DryRun:              true,
    			Filters:             []map[string]any{{"name": "example-name"}},
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClientVpnTargetNetworks } from "@ubx/sdk-aws/aws/data/ec2/client_vpn_target_networks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_client_vpn_target_networks" });

      data(ClientVpnTargetNetworks, "example", {
        clientVpnEndpointId: "example",
        associationIds: ["example"],
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ClientVpnTargetNetworks, ClientVpnTargetNetworksConfig
    from ubx.aws.data.ec2.client_vpn_target_networks import ClientVpnTargetNetworks_Filters

    def describe():
        ubx.intent("look up aws_ec2_client_vpn_target_networks")

        ubx.data(ClientVpnTargetNetworks, "example", ClientVpnTargetNetworksConfig(
            client_vpn_endpoint_id="example",
            association_ids=["example"],
            dry_run=True,
            filters=[ClientVpnTargetNetworks_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_ids" type="list(string)">
  The real, unique identifiers of the associations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="client_vpn_endpoint_id" type="string" required>
  The real, unique identifier of the client vpn endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_vpn_target_networks" type="list(object)">
  The real Client VPN target networks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
