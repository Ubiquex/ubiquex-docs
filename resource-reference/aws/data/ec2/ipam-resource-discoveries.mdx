---
title: "aws_ec2_ipam_resource_discoveries"
description: "`aws_ec2_ipam_resource_discoveries` looks up a real IPAM resource discovery -- the scope of accounts and Regions IPAM monitors for IP usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_resource_discoveries"})

    		ubx.Data(ec2.IpamResourceDiscoveries, "example", ec2.IpamResourceDiscoveriesConfig{
    			DryRun:                   true,
    			Filters:                  []map[string]any{{"name": "example-name"}},
    			IpamResourceDiscoveryIds: []string{"example"},
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IpamResourceDiscoveries } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/ipam_resource_discoveries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_resource_discoveries" });

      data(IpamResourceDiscoveries, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        ipamResourceDiscoveryIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamResourceDiscoveries, IpamResourceDiscoveriesConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_resource_discoveries")

        ubx.data(IpamResourceDiscoveries, "example", IpamResourceDiscoveriesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            ipam_resource_discovery_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_resource_discovery_ids" type="list(string)">
  The real, unique identifiers of the ipam resource discoverys this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_resource_discoveries" type="list(object)">
  The real VPC IPAM resource discoveries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
