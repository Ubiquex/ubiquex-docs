---
title: "aws_ec2_host_reservation_offerings"
description: "`aws_ec2_host_reservation_offerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_host_reservation_offerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_host_reservation_offerings"})

    		ubx.Data(ec2.HostReservationOfferings, "example", ec2.HostReservationOfferingsConfig{
    			Filter:      []map[string]any{{"name": "example-name"}},
    			MaxDuration: 7200,
    			MaxResults:  1,
    			MinDuration: 7200,
    			OfferingId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HostReservationOfferings } from "@ubx/sdk-aws/aws/data/ec2/host_reservation_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_host_reservation_offerings" });

      data(HostReservationOfferings, "example", {
        filter: [{ name: "example-name" }],
        maxDuration: 7200,
        maxResults: 1,
        minDuration: 7200,
        offeringId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import HostReservationOfferings, HostReservationOfferingsConfig

    def describe():
        ubx.intent("look up aws_ec2_host_reservation_offerings")

        ubx.data(HostReservationOfferings, "example", HostReservationOfferingsConfig(
            filter=[{"name": "example-name"}],
            max_duration=7200,
            max_results=1,
            min_duration=7200,
            offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_duration" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_duration" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="offering_set" type="list(object)">
  Returned by the lookup.
</ResponseField>
