---
title: "aws_ec2_address_transfers"
description: "`aws_ec2_address_transfers` reads the real, in-progress transfer of an Elastic IP address to another AWS account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_address_transfers"})

    		ubx.Data(ec2.AddressTransfers, "example", ec2.AddressTransfersConfig{
    			AllocationIds: []string{"example"},
    			DryRun:        true,
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AddressTransfers } from "@ubx/sdk-aws/aws/data/ec2/address_transfers";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_address_transfers" });

      data(AddressTransfers, "example", {
        allocationIds: ["example"],
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AddressTransfers, AddressTransfersConfig

    def describe():
        ubx.intent("look up aws_ec2_address_transfers")

        ubx.data(AddressTransfers, "example", AddressTransfersConfig(
            allocation_ids=["example"],
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="allocation_ids" type="list(string)">
  The real, unique identifiers of the allocations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="address_transfers" type="list(object)">
  The real IP address transfers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
