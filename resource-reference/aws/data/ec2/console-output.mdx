---
title: "aws_ec2_console_output"
description: "`aws_ec2_console_output` reads the real, captured console output of a running EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_console_output"})

    		ubx.Data(ec2.ConsoleOutput, "example", ec2.ConsoleOutputConfig{
    			InstanceId: "example",
    			DryRun:     true,
    			Latest:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConsoleOutput } from "@ubx/sdk-aws/aws/data/ec2/console_output";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_console_output" });

      data(ConsoleOutput, "example", {
        instanceId: "example",
        dryRun: true,
        latest: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ConsoleOutput, ConsoleOutputConfig

    def describe():
        ubx.intent("look up aws_ec2_console_output")

        ubx.data(ConsoleOutput, "example", ConsoleOutputConfig(
            instance_id="example",
            dry_run=True,
            latest=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="latest" type="bool">
  Whether this is the latest version of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="output" type="string">
  The real output of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="string">
  The real timestamp of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
