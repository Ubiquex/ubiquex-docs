---
title: "aws_ec2_vpc_classic_link_dns_support"
description: "`aws_ec2_vpc_classic_link_dns_support` reads whether ClassicLink DNS support is currently enabled on a VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_classic_link_dns_support"})

    		ubx.Data(ec2.VpcClassicLinkDnsSupport, "example", ec2.VpcClassicLinkDnsSupportConfig{
    			MaxResults: 1,
    			VpcIds:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VpcClassicLinkDnsSupport } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/vpc_classic_link_dns_support.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_classic_link_dns_support" });

      data(VpcClassicLinkDnsSupport, "example", {
        maxResults: 1,
        vpcIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcClassicLinkDnsSupport, VpcClassicLinkDnsSupportConfig

    def describe():
        ubx.intent("look up aws_ec2_vpc_classic_link_dns_support")

        ubx.data(VpcClassicLinkDnsSupport, "example", VpcClassicLinkDnsSupportConfig(
            max_results=1,
            vpc_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_ids" type="list(string)">
  The real, unique identifiers of the vpcs this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpcs" type="list(object)">
  The real VPCs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
