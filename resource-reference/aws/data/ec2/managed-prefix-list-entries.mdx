---
title: "aws_ec2_managed_prefix_list_entries"
description: "`aws_ec2_managed_prefix_list_entries` reads a real CIDR entry within a managed prefix list."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_managed_prefix_list_entries"})

    		ubx.Data(ec2.ManagedPrefixListEntries, "example", ec2.ManagedPrefixListEntriesConfig{
    			PrefixListId:  "example",
    			DryRun:        true,
    			MaxResults:    1,
    			TargetVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedPrefixListEntries } from "@ubx/sdk-aws/aws/data/ec2/managed_prefix_list_entries";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_managed_prefix_list_entries" });

      data(ManagedPrefixListEntries, "example", {
        prefixListId: "example",
        dryRun: true,
        maxResults: 1,
        targetVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ManagedPrefixListEntries, ManagedPrefixListEntriesConfig

    def describe():
        ubx.intent("look up aws_ec2_managed_prefix_list_entries")

        ubx.data(ManagedPrefixListEntries, "example", ManagedPrefixListEntriesConfig(
            prefix_list_id="example",
            dry_run=True,
            max_results=1,
            target_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="prefix_list_id" type="string" required>
  The real, unique identifier of the prefix list this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="target_version" type="number">
  The real version of the target. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entries" type="list(object)">
  The real entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
