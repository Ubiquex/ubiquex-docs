---
title: "aws_ec2_fpga_image_attribute"
description: "`aws_ec2_fpga_image_attribute` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_fpga_image_attribute` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_fpga_image_attribute"})

    		ubx.Data(ec2.FpgaImageAttribute, "example", ec2.FpgaImageAttributeConfig{
    			Attribute:   "example",
    			FpgaImageId: "example",
    			DryRun:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FpgaImageAttribute } from "@ubx/sdk-aws/aws/data/ec2/fpga_image_attribute";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_fpga_image_attribute" });

      data(FpgaImageAttribute, "example", {
        attribute: "example",
        fpgaImageId: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import FpgaImageAttribute, FpgaImageAttributeConfig

    def describe():
        ubx.intent("look up aws_ec2_fpga_image_attribute")

        ubx.data(FpgaImageAttribute, "example", FpgaImageAttributeConfig(
            attribute="example",
            fpga_image_id="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="fpga_image_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fpga_image_attribute" type="object">
  &lt;p&gt;Describes an Amazon FPGA image (AFI) attribute.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="fpga_image_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="load_permissions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="product_codes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
