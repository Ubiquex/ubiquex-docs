---
title: "aws_ec2_image_block_public_access_state"
description: "`aws_ec2_image_block_public_access_state` reads the real, account-wide setting blocking AMIs from ever being made public."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_image_block_public_access_state"})

    		ubx.Data(ec2.ImageBlockPublicAccessState, "example", ec2.ImageBlockPublicAccessStateConfig{
    			DryRun: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageBlockPublicAccessState } from "@ubx/sdk-aws/aws/data/ec2/image_block_public_access_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_image_block_public_access_state" });

      data(ImageBlockPublicAccessState, "example", {
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ImageBlockPublicAccessState, ImageBlockPublicAccessStateConfig

    def describe():
        ubx.intent("look up aws_ec2_image_block_public_access_state")

        ubx.data(ImageBlockPublicAccessState, "example", ImageBlockPublicAccessStateConfig(
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_block_public_access_state" type="string">
  The real, current state of the image block public access this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
