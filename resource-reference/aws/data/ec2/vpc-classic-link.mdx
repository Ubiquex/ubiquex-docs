---
title: "aws_ec2_vpc_classic_link"
description: "`aws_ec2_vpc_classic_link` reads the real, current status of ClassicLink, AWS's legacy pre-VPC networking bridge, on a VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_classic_link"})

    		ubx.Data(ec2.VpcClassicLink, "example", ec2.VpcClassicLinkConfig{
    			DryRun:  true,
    			Filters: []map[string]any{{"name": "example-name"}},
    			VpcIds:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcClassicLink } from "@ubx/sdk-aws/aws/data/ec2/vpc_classic_link";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_classic_link" });

      data(VpcClassicLink, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        vpcIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcClassicLink, VpcClassicLinkConfig
    from ubx.aws.data.ec2.vpc_classic_link import VpcClassicLink_Filters

    def describe():
        ubx.intent("look up aws_ec2_vpc_classic_link")

        ubx.data(VpcClassicLink, "example", VpcClassicLinkConfig(
            dry_run=True,
            filters=[VpcClassicLink_Filters(name="example-name")],
            vpc_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_ids" type="list(string)">
  The real, unique identifiers of the vpcs this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="vpcs" type="list(object)">
  The real VPCs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
