---
title: "aws_ec2_vpc_classic_link"
description: "`aws_ec2_vpc_classic_link` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_vpc_classic_link` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_classic_link"})

    		ubx.Data(ec2.VpcClassicLink, "example", ec2.VpcClassicLinkConfig{
    			DryRun:  true,
    			Filters: []map[string]any{{"name": "example-name"}},
    			VpcIds:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcClassicLink } from "@ubx/sdk-aws/aws/data/ec2/vpc_classic_link";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_classic_link" });

      data(VpcClassicLink, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        vpcIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcClassicLink, VpcClassicLinkConfig

    def describe():
        ubx.intent("look up aws_ec2_vpc_classic_link")

        ubx.data(VpcClassicLink, "example", VpcClassicLinkConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            vpc_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="vpcs" type="list(object)">
  Returned by the lookup.
</ResponseField>
