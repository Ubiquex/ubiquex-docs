---
title: "aws_ec2_launch_templates"
description: "`aws_ec2_launch_templates` looks up a real EC2 launch template, a saved set of parameters for launching an instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_launch_templates"})

    		ubx.Data(ec2.LaunchTemplates, "example", ec2.LaunchTemplatesConfig{
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			IncludeManagedResources: true,
    			LaunchTemplateIds:       []string{"example"},
    			LaunchTemplateNames:     []string{"example"},
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LaunchTemplates } from "@ubx/sdk-aws/aws/data/ec2/launch_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_launch_templates" });

      data(LaunchTemplates, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        includeManagedResources: true,
        launchTemplateIds: ["example"],
        launchTemplateNames: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import LaunchTemplates, LaunchTemplatesConfig
    from ubx.aws.data.ec2.launch_templates import LaunchTemplates_Filters

    def describe():
        ubx.intent("look up aws_ec2_launch_templates")

        ubx.data(LaunchTemplates, "example", LaunchTemplatesConfig(
            dry_run=True,
            filters=[LaunchTemplates_Filters(name="example-name")],
            include_managed_resources=True,
            launch_template_ids=["example"],
            launch_template_names=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_managed_resources" type="bool">
  Whether resources managed by another AWS service are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="launch_template_ids" type="list(string)">
  The real, unique identifiers of the launch templates this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="launch_template_names" type="list(string)">
  The real names of the launch templates included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="launch_templates" type="list(object)">
  The real EC2 launch templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
