---
title: "aws_ec2_elastic_gpus"
description: "`aws_ec2_elastic_gpus` looks up a real Elastic GPU attached to an EC2 instance, a retired AWS feature for adding graphics acceleration to an instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_elastic_gpus"})

    		ubx.Data(ec2.ElasticGpus, "example", ec2.ElasticGpusConfig{
    			DryRun:        true,
    			ElasticGpuIds: []string{"example"},
    			Filters:       []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticGpus } from "@ubx/sdk-aws/aws/data/ec2/elastic_gpus";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_elastic_gpus" });

      data(ElasticGpus, "example", {
        dryRun: true,
        elasticGpuIds: ["example"],
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ElasticGpus, ElasticGpusConfig

    def describe():
        ubx.intent("look up aws_ec2_elastic_gpus")

        ubx.data(ElasticGpus, "example", ElasticGpusConfig(
            dry_run=True,
            elastic_gpu_ids=["example"],
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="elastic_gpu_ids" type="list(string)">
  The real, unique identifiers of the elastic gpus this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="elastic_gpu_set" type="list(object)">
  The real elastic gpu set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
