---
title: "aws_ec2_vpc_endpoint_service_permissions"
description: "`aws_ec2_vpc_endpoint_service_permissions` reads which real principals are permitted to create a VPC endpoint connecting to a VPC endpoint service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_vpc_endpoint_service_permissions"})

    		ubx.Data(ec2.VpcEndpointServicePermissions, "example", ec2.VpcEndpointServicePermissionsConfig{
    			ServiceId:  "example",
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcEndpointServicePermissions } from "@ubx/sdk-aws/aws/data/ec2/vpc_endpoint_service_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_vpc_endpoint_service_permissions" });

      data(VpcEndpointServicePermissions, "example", {
        serviceId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VpcEndpointServicePermissions, VpcEndpointServicePermissionsConfig
    from ubx.aws.data.ec2.vpc_endpoint_service_permissions import VpcEndpointServicePermissions_Filters

    def describe():
        ubx.intent("look up aws_ec2_vpc_endpoint_service_permissions")

        ubx.data(VpcEndpointServicePermissions, "example", VpcEndpointServicePermissionsConfig(
            service_id="example",
            dry_run=True,
            filters=[VpcEndpointServicePermissions_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  The real, unique identifier of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allowed_principals" type="list(object)">
  The real IAM principals allowed to access this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
