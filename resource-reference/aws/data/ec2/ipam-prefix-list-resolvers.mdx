---
title: "aws_ec2_ipam_prefix_list_resolvers"
description: "`aws_ec2_ipam_prefix_list_resolvers` looks up a real IPAM prefix list resolver, which dynamically populates a managed prefix list from IPAM data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_prefix_list_resolvers"})

    		ubx.Data(ec2.IpamPrefixListResolvers, "example", ec2.IpamPrefixListResolversConfig{
    			DryRun:                    true,
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			IpamPrefixListResolverIds: []string{"example"},
    			MaxResults:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamPrefixListResolvers } from "@ubx/sdk-aws/aws/data/ec2/ipam_prefix_list_resolvers";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_ipam_prefix_list_resolvers" });

      data(IpamPrefixListResolvers, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        ipamPrefixListResolverIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamPrefixListResolvers, IpamPrefixListResolversConfig
    from ubx.aws.data.ec2.ipam_prefix_list_resolvers import IpamPrefixListResolvers_Filters

    def describe():
        ubx.intent("look up aws_ec2_ipam_prefix_list_resolvers")

        ubx.data(IpamPrefixListResolvers, "example", IpamPrefixListResolversConfig(
            dry_run=True,
            filters=[IpamPrefixListResolvers_Filters(name="example-name")],
            ipam_prefix_list_resolver_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_prefix_list_resolver_ids" type="list(string)">
  The real, unique identifiers of the ipam prefix list resolvers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_prefix_list_resolvers" type="list(object)">
  The real VPC IPAM prefix list resolvers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
