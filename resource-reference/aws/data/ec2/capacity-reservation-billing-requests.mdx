---
title: "aws_ec2_capacity_reservation_billing_requests"
description: "`data_aws_ec2_capacity_reservation_billing_requests` looks up a real, already-existing `aws_ec2_capacity_reservation`."
---

It reserves real EC2 capacity for a specific instance type and Availability Zone, guaranteeing it's available when needed regardless of what else is running in that zone, independent of any particular Reserved Instance or Savings Plan billing commitment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_capacity_reservation_billing_requests"})

    		ubx.Data(ec2.CapacityReservationBillingRequests, "example", ec2.CapacityReservationBillingRequestsConfig{
    			Role:                   "example",
    			CapacityReservationIds: []string{"example"},
    			DryRun:                 true,
    			Filters:                []map[string]any{{"name": "example-name"}},
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CapacityReservationBillingRequests } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/capacity_reservation_billing_requests.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_capacity_reservation_billing_requests" });

      data(CapacityReservationBillingRequests, "example", {
        role: "example",
        capacityReservationIds: ["example"],
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import CapacityReservationBillingRequests, CapacityReservationBillingRequestsConfig

    def describe():
        ubx.intent("look up aws_ec2_capacity_reservation_billing_requests")

        ubx.data(CapacityReservationBillingRequests, "example", CapacityReservationBillingRequestsConfig(
            role="example",
            capacity_reservation_ids=["example"],
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_reservation_ids" type="list(string)">
  The real, unique identifiers of the capacity reservations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="role" type="string" required>
  The real IAM role this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_reservation_billing_requests" type="list(object)">
  The real EC2 Capacity Reservation billing transfer requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
