---
title: "aws_ec2_mac_modification_tasks"
description: "`aws_ec2_mac_modification_tasks` reads the real, current status of a modification task (such as a disk repair) on an EC2 Mac instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_mac_modification_tasks"})

    		ubx.Data(ec2.MacModificationTasks, "example", ec2.MacModificationTasksConfig{
    			DryRun:                 true,
    			Filters:                []map[string]any{{"name": "example-name"}},
    			MacModificationTaskIds: []string{"example"},
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MacModificationTasks } from "@ubx/sdk-aws/aws/data/ec2/mac_modification_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_mac_modification_tasks" });

      data(MacModificationTasks, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        macModificationTaskIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import MacModificationTasks, MacModificationTasksConfig
    from ubx.aws.data.ec2.mac_modification_tasks import MacModificationTasks_Filters

    def describe():
        ubx.intent("look up aws_ec2_mac_modification_tasks")

        ubx.data(MacModificationTasks, "example", MacModificationTasksConfig(
            dry_run=True,
            filters=[MacModificationTasks_Filters(name="example-name")],
            mac_modification_task_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="mac_modification_task_ids" type="list(string)">
  The real, unique identifiers of the mac modification tasks this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mac_modification_tasks" type="list(object)">
  The real EC2 Mac System Integrity Protection modification tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
