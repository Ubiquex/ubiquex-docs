---
title: "aws_ec2_fast_launch_images"
description: "`aws_ec2_fast_launch_images` reads the real Windows AMI configuration for Fast Launch, which pre-provisions snapshots to speed up instance boot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_fast_launch_images"})

    		ubx.Data(ec2.FastLaunchImages, "example", ec2.FastLaunchImagesConfig{
    			DryRun:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			ImageIds:   []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FastLaunchImages } from "@ubx/sdk-aws/aws/data/ec2/fast_launch_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_fast_launch_images" });

      data(FastLaunchImages, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        imageIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import FastLaunchImages, FastLaunchImagesConfig

    def describe():
        ubx.intent("look up aws_ec2_fast_launch_images")

        ubx.data(FastLaunchImages, "example", FastLaunchImagesConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            image_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="image_ids" type="list(string)">
  The real, unique identifiers of the images this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fast_launch_images" type="list(object)">
  The real fast-launch-configured AMIs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
