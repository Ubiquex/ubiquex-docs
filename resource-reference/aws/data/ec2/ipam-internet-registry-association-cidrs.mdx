---
title: "aws_ec2_ipam_internet_registry_association_cidrs"
description: "`aws_ec2_ipam_internet_registry_association_cidrs` reads the real CIDR blocks associated with an IPAM's own Regional Internet Registry association."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_ipam_internet_registry_association_cidrs"})

    		ubx.Data(ec2.IpamInternetRegistryAssociationCidrs, "example", ec2.IpamInternetRegistryAssociationCidrsConfig{
    			IpamInternetRegistryAssociationId: "example",
    			DryRun:                            true,
    			Filters:                           []map[string]any{{"name": "example-name"}},
    			MaxResults:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpamInternetRegistryAssociationCidrs } from "@ubx/sdk-aws/aws/data/ec2/ipam_internet_registry_association_cidrs";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_ipam_internet_registry_association_cidrs",
      });

      data(IpamInternetRegistryAssociationCidrs, "example", {
        ipamInternetRegistryAssociationId: "example",
        dryRun: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import IpamInternetRegistryAssociationCidrs, IpamInternetRegistryAssociationCidrsConfig

    def describe():
        ubx.intent("look up aws_ec2_ipam_internet_registry_association_cidrs")

        ubx.data(IpamInternetRegistryAssociationCidrs, "example", IpamInternetRegistryAssociationCidrsConfig(
            ipam_internet_registry_association_id="example",
            dry_run=True,
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ipam_internet_registry_association_id" type="string" required>
  The real, unique identifier of the ipam internet registry association this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipam_internet_registry_association_cidrs" type="list(object)">
  The real VPC IPAM internet registry association CIDRs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
