---
title: "aws_ec2_aws_network_performance_data"
description: "`aws_ec2_aws_network_performance_data` reads real, published network performance metrics (latency, throughput) between AWS Regions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_aws_network_performance_data"})

    		ubx.Data(ec2.AwsNetworkPerformanceData, "example", ec2.AwsNetworkPerformanceDataConfig{
    			DataQueries: []map[string]any{{"destination": "example"}},
    			DryRun:      true,
    			EndTime:     "example",
    			MaxResults:  1,
    			StartTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AwsNetworkPerformanceData } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/aws_network_performance_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_aws_network_performance_data" });

      data(AwsNetworkPerformanceData, "example", {
        dataQueries: [{ destination: "example" }],
        dryRun: true,
        endTime: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AwsNetworkPerformanceData, AwsNetworkPerformanceDataConfig

    def describe():
        ubx.intent("look up aws_ec2_aws_network_performance_data")

        ubx.data(AwsNetworkPerformanceData, "example", AwsNetworkPerformanceDataConfig(
            data_queries=[{"destination": "example"}],
            dry_run=True,
            end_time="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_queries" type="list(object)">
  The real data queries included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_responses" type="list(object)">
  The real, raw data responses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
