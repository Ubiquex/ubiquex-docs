---
title: "aws_ec2_aws_network_performance_data"
description: "`aws_ec2_aws_network_performance_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_aws_network_performance_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_aws_network_performance_data"})

    		ubx.Data(ec2.AwsNetworkPerformanceData, "example", ec2.AwsNetworkPerformanceDataConfig{
    			DataQueries: []map[string]any{{"destination": "example"}},
    			DryRun:      true,
    			EndTime:     "example",
    			MaxResults:  1,
    			StartTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AwsNetworkPerformanceData } from "@ubx/sdk-aws/aws/data/ec2/aws_network_performance_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_aws_network_performance_data" });

      data(AwsNetworkPerformanceData, "example", {
        dataQueries: [{ destination: "example" }],
        dryRun: true,
        endTime: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AwsNetworkPerformanceData, AwsNetworkPerformanceDataConfig

    def describe():
        ubx.intent("look up aws_ec2_aws_network_performance_data")

        ubx.data(AwsNetworkPerformanceData, "example", AwsNetworkPerformanceDataConfig(
            data_queries=[{"destination": "example"}],
            dry_run=True,
            end_time="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_queries" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_responses" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
