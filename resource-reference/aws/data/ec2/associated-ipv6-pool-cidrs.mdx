---
title: "aws_ec2_associated_ipv6_pool_cidrs"
description: "`aws_ec2_associated_ipv6_pool_cidrs` reads the real IPv6 CIDR blocks associated with a customer-owned IPv6 address pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_associated_ipv6_pool_cidrs"})

    		ubx.Data(ec2.AssociatedIpv6PoolCidrs, "example", ec2.AssociatedIpv6PoolCidrsConfig{
    			PoolId:     "example",
    			DryRun:     true,
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AssociatedIpv6PoolCidrs } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/associated_ipv6_pool_cidrs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_associated_ipv6_pool_cidrs" });

      data(AssociatedIpv6PoolCidrs, "example", {
        poolId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import AssociatedIpv6PoolCidrs, AssociatedIpv6PoolCidrsConfig

    def describe():
        ubx.intent("look up aws_ec2_associated_ipv6_pool_cidrs")

        ubx.data(AssociatedIpv6PoolCidrs, "example", AssociatedIpv6PoolCidrsConfig(
            pool_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pool_id" type="string" required>
  The real, unique identifier of the pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ipv6_cidr_associations" type="list(object)">
  The real IPv6 CIDR block associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
