---
title: "aws_ec2_console_screenshot"
description: "`aws_ec2_console_screenshot` reads a real, captured screenshot of an EC2 instance's own console."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_console_screenshot"})

    		ubx.Data(ec2.ConsoleScreenshot, "example", ec2.ConsoleScreenshotConfig{
    			InstanceId: "example",
    			DryRun:     true,
    			WakeUp:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConsoleScreenshot } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/console_screenshot.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_console_screenshot" });

      data(ConsoleScreenshot, "example", {
        instanceId: "example",
        dryRun: true,
        wakeUp: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ConsoleScreenshot, ConsoleScreenshotConfig

    def describe():
        ubx.intent("look up aws_ec2_console_screenshot")

        ubx.data(ConsoleScreenshot, "example", ConsoleScreenshotConfig(
            instance_id="example",
            dry_run=True,
            wake_up=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="wake_up" type="bool">
  Whether this device is currently woken up. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_data" type="string">
  The real, raw data of the image. **(AI-inferred)** Returned by the lookup.
</ResponseField>
