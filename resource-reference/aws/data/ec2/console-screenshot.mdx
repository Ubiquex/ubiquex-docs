---
title: "aws_ec2_console_screenshot"
description: "`aws_ec2_console_screenshot` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ec2_console_screenshot` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_console_screenshot"})

    		ubx.Data(ec2.ConsoleScreenshot, "example", ec2.ConsoleScreenshotConfig{
    			InstanceId: "example",
    			DryRun:     true,
    			WakeUp:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConsoleScreenshot } from "@ubx/sdk-aws/aws/data/ec2/console_screenshot";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_console_screenshot" });

      data(ConsoleScreenshot, "example", {
        instanceId: "example",
        dryRun: true,
        wakeUp: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ConsoleScreenshot, ConsoleScreenshotConfig

    def describe():
        ubx.intent("look up aws_ec2_console_screenshot")

        ubx.data(ConsoleScreenshot, "example", ConsoleScreenshotConfig(
            instance_id="example",
            dry_run=True,
            wake_up=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="wake_up" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_data" type="string">
  Returned by the lookup.
</ResponseField>
