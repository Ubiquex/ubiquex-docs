---
title: "aws_ec2_volume_status"
description: "`data_aws_ec2_volume_status` looks up a real, already-existing `aws_ec2_volume`."
---

It creates a real, persistent Amazon EBS block storage volume, attachable to an EC2 instance, that keeps its own data independently of any single instance's own lifecycle.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_volume_status"})

    		ubx.Data(ec2.VolumeStatus, "example", ec2.VolumeStatusConfig{
    			DryRun:                  true,
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			IncludeManagedResources: true,
    			MaxResults:              1,
    			VolumeIds:               []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VolumeStatus } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/volume_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_volume_status" });

      data(VolumeStatus, "example", {
        dryRun: true,
        filters: [{ name: "example-name" }],
        includeManagedResources: true,
        maxResults: 1,
        volumeIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import VolumeStatus, VolumeStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_volume_status")

        ubx.data(VolumeStatus, "example", VolumeStatusConfig(
            dry_run=True,
            filters=[{"name": "example-name"}],
            include_managed_resources=True,
            max_results=1,
            volume_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_managed_resources" type="bool">
  Whether resources managed by another AWS service are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="volume_ids" type="list(string)">
  The real, unique identifiers of the volumes this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="volume_statuses" type="list(object)">
  The real, current statuses of the volumes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
