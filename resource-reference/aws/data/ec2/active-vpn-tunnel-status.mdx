---
title: "aws_ec2_active_vpn_tunnel_status"
description: "`aws_ec2_active_vpn_tunnel_status` reads the real, current status of an active Site-to-Site VPN tunnel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_active_vpn_tunnel_status"})

    		ubx.Data(ec2.ActiveVpnTunnelStatus, "example", ec2.ActiveVpnTunnelStatusConfig{
    			VpnConnectionId:           "example",
    			VpnTunnelOutsideIpAddress: "example",
    			DryRun:                    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ActiveVpnTunnelStatus } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/active_vpn_tunnel_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ec2_active_vpn_tunnel_status" });

      data(ActiveVpnTunnelStatus, "example", {
        vpnConnectionId: "example",
        vpnTunnelOutsideIpAddress: "example",
        dryRun: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import ActiveVpnTunnelStatus, ActiveVpnTunnelStatusConfig

    def describe():
        ubx.intent("look up aws_ec2_active_vpn_tunnel_status")

        ubx.data(ActiveVpnTunnelStatus, "example", ActiveVpnTunnelStatusConfig(
            vpn_connection_id="example",
            vpn_tunnel_outside_ip_address="example",
            dry_run=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  The real, unique identifier of the vpn connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vpn_tunnel_outside_ip_address" type="string" required>
  The real, public-facing IP address of this VPN tunnel. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_vpn_tunnel_status" type="object">
  &lt;p&gt;Contains information about the current security configuration of an active VPN tunnel.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ike_version" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase1_dhgroup" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase1_encryption_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase1_integrity_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase2_dhgroup" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase2_encryption_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phase2_integrity_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_status_reason" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
