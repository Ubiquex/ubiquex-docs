---
title: "aws_ec2_network_insights_access_scope_analysis_findings"
description: "`data_aws_ec2_network_insights_access_scope_analysis_findings` looks up a real, already-existing `aws_ec2_network_insights_access_scope_analysis`."
---

It runs a real, point-in-time check of an `aws_ec2_network_insights_access_scope`, the access-scope counterpart to what an `aws_ec2_network_insights_analysis` does for a single `aws_ec2_network_insights_path`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ec2_network_insights_access_scope_analysis_findings"})

    		ubx.Data(ec2.NetworkInsightsAccessScopeAnalysisFindings, "example", ec2.NetworkInsightsAccessScopeAnalysisFindingsConfig{
    			NetworkInsightsAccessScopeAnalysisId: "example",
    			DryRun:                               true,
    			MaxResults:                           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkInsightsAccessScopeAnalysisFindings } from "./local-sdk/aws/sdk/typescript/aws/data/ec2/network_insights_access_scope_analysis_findings.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ec2_network_insights_access_scope_analysis_findings",
      });

      data(NetworkInsightsAccessScopeAnalysisFindings, "example", {
        networkInsightsAccessScopeAnalysisId: "example",
        dryRun: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ec2 import NetworkInsightsAccessScopeAnalysisFindings, NetworkInsightsAccessScopeAnalysisFindingsConfig

    def describe():
        ubx.intent("look up aws_ec2_network_insights_access_scope_analysis_findings")

        ubx.data(NetworkInsightsAccessScopeAnalysisFindings, "example", NetworkInsightsAccessScopeAnalysisFindingsConfig(
            network_insights_access_scope_analysis_id="example",
            dry_run=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_insights_access_scope_analysis_id" type="string" required>
  The real, unique identifier of the network insights access scope analysis this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analysis_findings" type="list(object)">
  The real findings from this analysis. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="analysis_status" type="string">
  The real, current status of the analysis this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
