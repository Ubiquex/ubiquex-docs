---
title: "aws_mgh_migration_tasks"
description: "`aws_mgh_migration_tasks` reads the real migration tasks tracked by a Migration Hub progress-update stream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mgh "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgh_migration_tasks"})

    		ubx.Data(mgh.MigrationTasks, "example", mgh.MigrationTasksConfig{
    			MaxResults:   1,
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MigrationTasks } from "./local-sdk/aws/sdk/typescript/aws/data/mgh/migration_tasks.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgh_migration_tasks" });

      data(MigrationTasks, "example", {
        maxResults: 1,
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mgh import MigrationTasks, MigrationTasksConfig

    def describe():
        ubx.intent("look up aws_mgh_migration_tasks")

        ubx.data(MigrationTasks, "example", MigrationTasksConfig(
            max_results=1,
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string">
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="migration_task_summary_list" type="list(object)">
  The real migration task summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
