---
title: "aws_mgh_created_artifacts"
description: "`aws_mgh_created_artifacts` reads the real artifacts (such as an AMI) created for a Migration Hub migration task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mgh "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgh_created_artifacts"})

    		ubx.Data(mgh.CreatedArtifacts, "example", mgh.CreatedArtifactsConfig{
    			MigrationTaskName:    "example-migration-task-name",
    			ProgressUpdateStream: "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CreatedArtifacts } from "@ubx/sdk-aws/aws/data/mgh/created_artifacts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgh_created_artifacts" });

      data(CreatedArtifacts, "example", {
        migrationTaskName: "example-migration-task-name",
        progressUpdateStream: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mgh import CreatedArtifacts, CreatedArtifactsConfig

    def describe():
        ubx.intent("look up aws_mgh_created_artifacts")

        ubx.data(CreatedArtifacts, "example", CreatedArtifactsConfig(
            migration_task_name="example-migration-task-name",
            progress_update_stream="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="migration_task_name" type="string" required>
  The real name of the migration task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="progress_update_stream" type="string" required>
  The real name of the Migration Hub progress-update stream this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_artifact_list" type="list(object)">
  The real created artifacts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
