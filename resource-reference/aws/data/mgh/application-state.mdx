---
title: "aws_mgh_application_state"
description: "`aws_mgh_application_state` reads the real, current migration state of a Migration Hub application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mgh "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgh_application_state"})

    		ubx.Data(mgh.ApplicationState, "example", mgh.ApplicationStateConfig{
    			ApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationState } from "./local-sdk/aws/sdk/typescript/aws/data/mgh/application_state.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgh_application_state" });

      data(ApplicationState, "example", {
        applicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mgh import ApplicationState, ApplicationStateConfig

    def describe():
        ubx.intent("look up aws_mgh_application_state")

        ubx.data(ApplicationState, "example", ApplicationStateConfig(
            application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_status" type="string">
  The real, current status of the application this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
