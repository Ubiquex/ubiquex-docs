---
title: "aws_mgh_application_state"
description: "`aws_mgh_application_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mgh_application_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mgh "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mgh"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mgh_application_state"})

    		ubx.Data(mgh.ApplicationState, "example", mgh.ApplicationStateConfig{
    			ApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationState } from "@ubx/sdk-aws/aws/data/mgh/application_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_mgh_application_state" });

      data(ApplicationState, "example", {
        applicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mgh import ApplicationState, ApplicationStateConfig

    def describe():
        ubx.intent("look up aws_mgh_application_state")

        ubx.data(ApplicationState, "example", ApplicationStateConfig(
            application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Returned by the lookup.
</ResponseField>
