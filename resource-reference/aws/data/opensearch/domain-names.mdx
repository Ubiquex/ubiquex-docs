---
title: "aws_opensearch_domain_names"
description: "`aws_opensearch_domain_names` reads the real names of the OpenSearch domains on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_names"})

    		ubx.Data(opensearch.DomainNames, "example", opensearch.DomainNamesConfig{
    			EngineType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DomainNames } from "./local-sdk/aws/sdk/typescript/aws/data/opensearch/domain_names.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_names" });

      data(DomainNames, "example", {
        engineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
            engine_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  The real database or compute engine type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_names" type="list(object)">
  &lt;p&gt;Contains a list of information about a domain.&lt;/p&gt; Returned by the lookup.
</ResponseField>
