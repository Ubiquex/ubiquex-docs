---
title: "aws_opensearch_domain_names"
description: "`aws_opensearch_domain_names` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_opensearch_domain_names` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_names"})

    		ubx.Data(opensearch.DomainNames, "example", opensearch.DomainNamesConfig{
    			EngineType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainNames } from "@ubx/sdk-aws/aws/data/opensearch/domain_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_names" });

      data(DomainNames, "example", {
        engineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
            engine_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_names" type="list(object)">
  &lt;p&gt;Contains a list of information about a domain.&lt;/p&gt; Returned by the lookup.
</ResponseField>
