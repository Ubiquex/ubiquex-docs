---
title: "aws_opensearch_domain_nodes"
description: "`aws_opensearch_domain_nodes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_opensearch_domain_nodes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_nodes"})

    		ubx.Data(opensearch.DomainNodes, "example", opensearch.DomainNodesConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainNodes } from "@ubx/sdk-aws/aws/data/opensearch/domain_nodes";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_nodes" });

      data(DomainNodes, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainNodes, DomainNodesConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_nodes")

        ubx.data(DomainNodes, "example", DomainNodesConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_nodes_status_list" type="list(object)">
  &lt;p&gt;List of &lt;code&gt;DomainNodesStatus&lt;/code&gt; with details about nodes on the requested domain.&lt;/p&gt; Returned by the lookup.
</ResponseField>
