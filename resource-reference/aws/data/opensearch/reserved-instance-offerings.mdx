---
title: "aws_opensearch_reserved_instance_offerings"
description: "`aws_opensearch_reserved_instance_offerings` reads a real, available offering for purchasing an OpenSearch Reserved Instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_reserved_instance_offerings"})

    		ubx.Data(opensearch.ReservedInstanceOfferings, "example", opensearch.ReservedInstanceOfferingsConfig{
    			MaxResults:                 1,
    			ReservedInstanceOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReservedInstanceOfferings } from "./local-sdk/aws/sdk/typescript/aws/data/opensearch/reserved_instance_offerings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_reserved_instance_offerings" });

      data(ReservedInstanceOfferings, "example", {
        maxResults: 1,
        reservedInstanceOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import ReservedInstanceOfferings, ReservedInstanceOfferingsConfig

    def describe():
        ubx.intent("look up aws_opensearch_reserved_instance_offerings")

        ubx.data(ReservedInstanceOfferings, "example", ReservedInstanceOfferingsConfig(
            max_results=1,
            reserved_instance_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;An optional parameter that specifies the maximum number of results to return for a given request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instance_offering_id" type="string">
  The real, unique identifier of the reserved instance offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_instance_offerings" type="list(object)">
  The real Reserved Instance offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
