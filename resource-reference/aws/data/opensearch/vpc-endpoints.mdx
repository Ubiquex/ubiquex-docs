---
title: "aws_opensearch_vpc_endpoints"
description: "`aws_opensearch_vpc_endpoints` looks up a real VPC endpoint connecting privately to an OpenSearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_vpc_endpoints"})

    		ubx.Data(opensearch.VpcEndpoints, "example", opensearch.VpcEndpointsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcEndpoints } from "@ubx/sdk-aws/aws/data/opensearch/vpc_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_vpc_endpoints" });

      data(VpcEndpoints, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import VpcEndpoints, VpcEndpointsConfig

    def describe():
        ubx.intent("look up aws_opensearch_vpc_endpoints")

        ubx.data(VpcEndpoints, "example", VpcEndpointsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_summary_list" type="list(object)">
  The real, summarized VPC endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
