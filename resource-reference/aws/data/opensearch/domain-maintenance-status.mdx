---
title: "aws_opensearch_domain_maintenance_status"
description: "`aws_opensearch_domain_maintenance_status` reads the real, current status of a scheduled maintenance action on an OpenSearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_maintenance_status"})

    		ubx.Data(opensearch.DomainMaintenanceStatus, "example", opensearch.DomainMaintenanceStatusConfig{
    			DomainName:    "example-domain-name",
    			MaintenanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainMaintenanceStatus } from "@ubx/sdk-aws/aws/data/opensearch/domain_maintenance_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_maintenance_status" });

      data(DomainMaintenanceStatus, "example", {
        domainName: "example-domain-name",
        maintenanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainMaintenanceStatus, DomainMaintenanceStatusConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_maintenance_status")

        ubx.data(DomainMaintenanceStatus, "example", DomainMaintenanceStatusConfig(
            domain_name="example-domain-name",
            maintenance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="maintenance_id" type="string" required>
  The real, unique identifier of the maintenance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action" type="string">
  The real action this object describes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="node_id" type="string">
  The real, unique identifier of the node this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
