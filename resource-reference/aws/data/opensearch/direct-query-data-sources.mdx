---
title: "aws_opensearch_direct_query_data_sources"
description: "`aws_opensearch_direct_query_data_sources` reads the real direct query data sources configured on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_direct_query_data_sources"})

    		ubx.Data(opensearch.DirectQueryDataSources, "example", opensearch.DirectQueryDataSourcesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DirectQueryDataSources } from "./local-sdk/aws/sdk/typescript/aws/data/opensearch/direct_query_data_sources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_direct_query_data_sources" });

      data(DirectQueryDataSources, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DirectQueryDataSources, DirectQueryDataSourcesConfig

    def describe():
        ubx.intent("look up aws_opensearch_direct_query_data_sources")

        ubx.data(DirectQueryDataSources, "example", DirectQueryDataSourcesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="direct_query_data_sources" type="list(object)">
  The real, direct-query data sources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>
