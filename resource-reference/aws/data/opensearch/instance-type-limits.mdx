---
title: "aws_opensearch_instance_type_limits"
description: "`aws_opensearch_instance_type_limits` reads the real limits (such as maximum EBS volume size) applicable to an OpenSearch instance type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_instance_type_limits"})

    		ubx.Data(opensearch.InstanceTypeLimits, "example", opensearch.InstanceTypeLimitsConfig{
    			EngineVersion: "example",
    			InstanceType:  "example",
    			DomainName:    "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceTypeLimits } from "@ubx/sdk-aws/aws/data/opensearch/instance_type_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_instance_type_limits" });

      data(InstanceTypeLimits, "example", {
        engineVersion: "example",
        instanceType: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import InstanceTypeLimits, InstanceTypeLimitsConfig

    def describe():
        ubx.intent("look up aws_opensearch_instance_type_limits")

        ubx.data(InstanceTypeLimits, "example", InstanceTypeLimitsConfig(
            engine_version="example",
            instance_type="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string">
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string" required>
  The real version of the database or compute engine this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  The real instance type this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="limits_by_role" type="map(object)">
  &lt;p&gt; The role of a given instance and all applicable limits. The role performed by a given OpenSearch instance can be one of the following: &lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;data&lt;/b&gt; - A data node.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;master&lt;/b&gt; - A dedicated master node.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;ultra_warm&lt;/b&gt; - An UltraWarm node.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
</ResponseField>
