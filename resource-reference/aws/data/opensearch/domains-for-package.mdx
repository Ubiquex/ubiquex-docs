---
title: "aws_opensearch_domains_for_package"
description: "`aws_opensearch_domains_for_package` reads the real OpenSearch domains a given package (such as a custom dictionary) is associated with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domains_for_package"})

    		ubx.Data(opensearch.DomainsForPackage, "example", opensearch.DomainsForPackageConfig{
    			PackageId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainsForPackage } from "@ubx/sdk-aws/aws/data/opensearch/domains_for_package";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domains_for_package" });

      data(DomainsForPackage, "example", {
        packageId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainsForPackage, DomainsForPackageConfig

    def describe():
        ubx.intent("look up aws_opensearch_domains_for_package")

        ubx.data(DomainsForPackage, "example", DomainsForPackageConfig(
            package_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;An optional parameter that specifies the maximum number of results to return for a given request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="package_id" type="string" required>
  The real, unique identifier of the package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_package_details_list" type="list(object)">
  The real, detailed domain packages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>
