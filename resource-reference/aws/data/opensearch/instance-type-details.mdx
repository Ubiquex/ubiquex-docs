---
title: "aws_opensearch_instance_type_details"
description: "`aws_opensearch_instance_type_details` reads the real specifications and limits of an OpenSearch instance type in a given Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_instance_type_details"})

    		ubx.Data(opensearch.InstanceTypeDetails, "example", opensearch.InstanceTypeDetailsConfig{
    			EngineVersion: "example",
    			DomainName:    "example-domain-name",
    			InstanceType:  "example",
    			MaxResults:    1,
    			RetrieveAzs:   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceTypeDetails } from "@ubx/sdk-aws/aws/data/opensearch/instance_type_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_instance_type_details" });

      data(InstanceTypeDetails, "example", {
        engineVersion: "example",
        domainName: "example-domain-name",
        instanceType: "example",
        maxResults: 1,
        retrieveAzs: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import InstanceTypeDetails, InstanceTypeDetailsConfig

    def describe():
        ubx.intent("look up aws_opensearch_instance_type_details")

        ubx.data(InstanceTypeDetails, "example", InstanceTypeDetailsConfig(
            engine_version="example",
            domain_name="example-domain-name",
            instance_type="example",
            max_results=1,
            retrieve_azs=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string">
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string" required>
  The real version of the database or compute engine this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string">
  The real instance type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;An optional parameter that specifies the maximum number of results to return for a given request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="retrieve_azs" type="bool">
  Whether Availability Zones are retrieved alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_type_details" type="list(object)">
  The real, detailed information about the instance type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>
