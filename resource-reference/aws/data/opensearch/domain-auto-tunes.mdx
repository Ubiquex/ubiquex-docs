---
title: "aws_opensearch_domain_auto_tunes"
description: "`aws_opensearch_domain_auto_tunes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_opensearch_domain_auto_tunes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_auto_tunes"})

    		ubx.Data(opensearch.DomainAutoTunes, "example", opensearch.DomainAutoTunesConfig{
    			DomainName: "example-domain-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainAutoTunes } from "@ubx/sdk-aws/aws/data/opensearch/domain_auto_tunes";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_auto_tunes" });

      data(DomainAutoTunes, "example", {
        domainName: "example-domain-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainAutoTunes, DomainAutoTunesConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_auto_tunes")

        ubx.data(DomainAutoTunes, "example", DomainAutoTunesConfig(
            domain_name="example-domain-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;An optional parameter that specifies the maximum number of results to return for a given request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_tunes" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>
