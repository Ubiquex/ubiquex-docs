---
title: "aws_opensearch_reserved_instances"
description: "`aws_opensearch_reserved_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_opensearch_reserved_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_reserved_instances"})

    		ubx.Data(opensearch.ReservedInstances, "example", opensearch.ReservedInstancesConfig{
    			MaxResults:         1,
    			ReservedInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedInstances } from "@ubx/sdk-aws/aws/data/opensearch/reserved_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_reserved_instances" });

      data(ReservedInstances, "example", {
        maxResults: 1,
        reservedInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import ReservedInstances, ReservedInstancesConfig

    def describe():
        ubx.intent("look up aws_opensearch_reserved_instances")

        ubx.data(ReservedInstances, "example", ReservedInstancesConfig(
            max_results=1,
            reserved_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;An optional parameter that specifies the maximum number of results to return for a given request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_instance_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;When &lt;code&gt;nextToken&lt;/code&gt; is returned, there are more results available. The value of &lt;code&gt;nextToken&lt;/code&gt; is a unique pagination token for each page. Send the request again using the returned token to retrieve the next page.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
