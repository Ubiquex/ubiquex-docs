---
title: "aws_opensearch_compatible_versions"
description: "`aws_opensearch_compatible_versions` reads the real OpenSearch or Elasticsearch versions a domain can be upgraded to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_compatible_versions"})

    		ubx.Data(opensearch.CompatibleVersions, "example", opensearch.CompatibleVersionsConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CompatibleVersions } from "./local-sdk/aws/sdk/typescript/aws/data/opensearch/compatible_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_compatible_versions" });

      data(CompatibleVersions, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import CompatibleVersions, CompatibleVersionsConfig

    def describe():
        ubx.intent("look up aws_opensearch_compatible_versions")

        ubx.data(CompatibleVersions, "example", CompatibleVersionsConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string">
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compatible_versions" type="list(object)">
  The real versions of the compatible included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
