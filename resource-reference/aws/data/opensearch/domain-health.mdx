---
title: "aws_opensearch_domain_health"
description: "`aws_opensearch_domain_health` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_opensearch_domain_health` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_health"})

    		ubx.Data(opensearch.DomainHealth, "example", opensearch.DomainHealthConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainHealth } from "@ubx/sdk-aws/aws/data/opensearch/domain_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_health" });

      data(DomainHealth, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainHealth, DomainHealthConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_health")

        ubx.data(DomainHealth, "example", DomainHealthConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_availability_zone_count" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="availability_zone_count" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="cluster_health" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="data_node_count" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="dedicated_master" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="domain_state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="environment_information" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="master_eligible_node_count" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="master_node" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stand_by_availability_zone_count" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_shards" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_un_assigned_shards" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="warm_node_count" type="string">
  Returned by the lookup.
</ResponseField>
