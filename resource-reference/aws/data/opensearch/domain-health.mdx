---
title: "aws_opensearch_domain_health"
description: "`aws_opensearch_domain_health` reads the real, current cluster health of an OpenSearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_opensearch_domain_health"})

    		ubx.Data(opensearch.DomainHealth, "example", opensearch.DomainHealthConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainHealth } from "@ubx/sdk-aws/aws/data/opensearch/domain_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_opensearch_domain_health" });

      data(DomainHealth, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.opensearch import DomainHealth, DomainHealthConfig

    def describe():
        ubx.intent("look up aws_opensearch_domain_health")

        ubx.data(DomainHealth, "example", DomainHealthConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an OpenSearch Service domain. Domain names are unique across the domains owned by an account within an Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_availability_zone_count" type="string">
  The real count of active availability zones. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="availability_zone_count" type="string">
  The real count of availability zones. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="cluster_health" type="string">
  The real, current health of this cluster. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_node_count" type="string">
  The real count of data nodes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dedicated_master" type="bool">
  Whether this OpenSearch domain uses dedicated master nodes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="domain_state" type="string">
  The real, current state of the domain this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="environment_information" type="list(object)">
  The real, detailed information about this environment. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="master_eligible_node_count" type="string">
  The real count of master eligible nodes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="master_node" type="string">
  The real master node of this cluster. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stand_by_availability_zone_count" type="string">
  The real count of stand by availability zones. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_shards" type="string">
  The real, total number of shards for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_un_assigned_shards" type="string">
  The real, total number of unassigned shards for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="warm_node_count" type="string">
  The real count of warm nodes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
