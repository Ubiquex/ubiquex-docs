---
title: "aws_mediapackage_harvest_jobs"
description: "`aws_mediapackage_harvest_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mediapackage_harvest_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediapackage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediapackage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediapackage_harvest_jobs"})

    		ubx.Data(mediapackage.HarvestJobs, "example", mediapackage.HarvestJobsConfig{
    			IncludeChannelId: "example",
    			IncludeStatus:    "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HarvestJobs } from "@ubx/sdk-aws/aws/data/mediapackage/harvest_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediapackage_harvest_jobs" });

      data(HarvestJobs, "example", {
        includeChannelId: "example",
        includeStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediapackage import HarvestJobs, HarvestJobsConfig

    def describe():
        ubx.intent("look up aws_mediapackage_harvest_jobs")

        ubx.data(HarvestJobs, "example", HarvestJobsConfig(
            include_channel_id="example",
            include_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_channel_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="harvest_jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
