---
title: "aws_networkmanager_link_associations"
description: "`aws_networkmanager_link_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_link_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_link_associations"})

    		ubx.Data(networkmanager.LinkAssociations, "example", networkmanager.LinkAssociationsConfig{
    			GlobalNetworkId: "example",
    			DeviceId:        "example",
    			LinkId:          "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LinkAssociations } from "@ubx/sdk-aws/aws/data/networkmanager/link_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_link_associations" });

      data(LinkAssociations, "example", {
        globalNetworkId: "example",
        deviceId: "example",
        linkId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import LinkAssociations, LinkAssociationsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_link_associations")

        ubx.data(LinkAssociations, "example", LinkAssociationsConfig(
            global_network_id="example",
            device_id="example",
            link_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="link_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="link_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
