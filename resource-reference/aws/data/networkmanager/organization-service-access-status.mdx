---
title: "aws_networkmanager_organization_service_access_status"
description: "`aws_networkmanager_organization_service_access_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_organization_service_access_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_organization_service_access_status"})

    		ubx.Data(networkmanager.OrganizationServiceAccessStatus, "example", networkmanager.OrganizationServiceAccessStatusConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationServiceAccessStatus } from "@ubx/sdk-aws/aws/data/networkmanager/organization_service_access_status";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_organization_service_access_status",
      });

      data(OrganizationServiceAccessStatus, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import OrganizationServiceAccessStatus, OrganizationServiceAccessStatusConfig

    def describe():
        ubx.intent("look up aws_networkmanager_organization_service_access_status")

        ubx.data(OrganizationServiceAccessStatus, "example", OrganizationServiceAccessStatusConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="organization_status" type="object">
  &lt;p&gt;The status of an Amazon Web Services Organization and the accounts within that organization.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_status_list" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="organization_aws_service_access_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="organization_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="slrdeployment_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
