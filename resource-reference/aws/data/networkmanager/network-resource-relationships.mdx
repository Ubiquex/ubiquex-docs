---
title: "aws_networkmanager_network_resource_relationships"
description: "`aws_networkmanager_network_resource_relationships` reads the real relationships between resources within a Network Manager global network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_network_resource_relationships"})

    		ubx.Data(networkmanager.NetworkResourceRelationships, "example", networkmanager.NetworkResourceRelationshipsConfig{
    			GlobalNetworkId:      "example",
    			AccountId:            "example",
    			AwsRegion:            "example",
    			CoreNetworkId:        "example",
    			MaxResults:           1,
    			RegisteredGatewayArn: "arn:aws:iam::123456789012:policy/example",
    			ResourceArn:          "arn:aws:iam::123456789012:policy/example",
    			ResourceType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkResourceRelationships } from "@ubx/sdk-aws/aws/data/networkmanager/network_resource_relationships";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_network_resource_relationships",
      });

      data(NetworkResourceRelationships, "example", {
        globalNetworkId: "example",
        accountId: "example",
        awsRegion: "example",
        coreNetworkId: "example",
        maxResults: 1,
        registeredGatewayArn: "arn:aws:iam::123456789012:policy/example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import NetworkResourceRelationships, NetworkResourceRelationshipsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_network_resource_relationships")

        ubx.data(NetworkResourceRelationships, "example", NetworkResourceRelationshipsConfig(
            global_network_id="example",
            account_id="example",
            aws_region="example",
            core_network_id="example",
            max_results=1,
            registered_gateway_arn="arn:aws:iam::123456789012:policy/example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="aws_region" type="string">
  The real AWS region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN global network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registered_gateway_arn" type="string">
  The real Amazon Resource Name (ARN) of the registered gateway this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="relationships" type="list(object)">
  The real relationships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
