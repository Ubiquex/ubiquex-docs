---
title: "aws_networkmanager_peerings"
description: "`aws_networkmanager_peerings` looks up a real Cloud WAN peering connection, such as a Transit Gateway peering into a core network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_peerings"})

    		ubx.Data(networkmanager.Peerings, "example", networkmanager.PeeringsConfig{
    			CoreNetworkId: "example",
    			EdgeLocation:  "example",
    			MaxResults:    1,
    			PeeringType:   "example",
    			State:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Peerings } from "@ubx/sdk-aws/aws/data/networkmanager/peerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_peerings" });

      data(Peerings, "example", {
        coreNetworkId: "example",
        edgeLocation: "example",
        maxResults: 1,
        peeringType: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import Peerings, PeeringsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_peerings")

        ubx.data(Peerings, "example", PeeringsConfig(
            core_network_id="example",
            edge_location="example",
            max_results=1,
            peering_type="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_network_id" type="string">
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="edge_location" type="string">
  The real CloudFront edge location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="peering_type" type="string">
  The real, discriminating type of the peering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="peerings" type="list(object)">
  The real network peerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
