---
title: "aws_networkmanager_peerings"
description: "`aws_networkmanager_peerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_peerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_peerings"})

    		ubx.Data(networkmanager.Peerings, "example", networkmanager.PeeringsConfig{
    			CoreNetworkId: "example",
    			EdgeLocation:  "example",
    			MaxResults:    1,
    			PeeringType:   "example",
    			State:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Peerings } from "@ubx/sdk-aws/aws/data/networkmanager/peerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_peerings" });

      data(Peerings, "example", {
        coreNetworkId: "example",
        edgeLocation: "example",
        maxResults: 1,
        peeringType: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import Peerings, PeeringsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_peerings")

        ubx.data(Peerings, "example", PeeringsConfig(
            core_network_id="example",
            edge_location="example",
            max_results=1,
            peering_type="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_network_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="edge_location" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="peering_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="peerings" type="list(object)">
  Returned by the lookup.
</ResponseField>
