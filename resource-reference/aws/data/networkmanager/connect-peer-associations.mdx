---
title: "aws_networkmanager_connect_peer_associations"
description: "`data_aws_networkmanager_connect_peer_associations` looks up a real, already-existing `aws_network_manager_connect_peer`."
---

It establishes a real BGP peering session over an `aws_network_manager_connect_attachment`, exchanging routes between the core network and an external or on-premises device.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_connect_peer_associations"})

    		ubx.Data(networkmanager.ConnectPeerAssociations, "example", networkmanager.ConnectPeerAssociationsConfig{
    			GlobalNetworkId: "example",
    			ConnectPeerIds:  []string{"example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConnectPeerAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/networkmanager/connect_peer_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_connect_peer_associations" });

      data(ConnectPeerAssociations, "example", {
        globalNetworkId: "example",
        connectPeerIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import ConnectPeerAssociations, ConnectPeerAssociationsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_connect_peer_associations")

        ubx.data(ConnectPeerAssociations, "example", ConnectPeerAssociationsConfig(
            global_network_id="example",
            connect_peer_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connect_peer_ids" type="list(string)">
  The real, unique identifiers of the connect peers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN global network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connect_peer_associations" type="list(object)">
  The real AWS Cloud WAN Connect peer associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
