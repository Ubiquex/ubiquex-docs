---
title: "aws_networkmanager_core_network_policy"
description: "`data_aws_networkmanager_core_network_policy` looks up a real, already-existing `aws_network_manager_core_network`."
---

It creates a real, centrally managed global network backbone, the core resource behind AWS Cloud WAN, that VPCs, Direct Connect gateways, and Site-to-Site VPNs attach to and route through.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_core_network_policy"})

    		ubx.Data(networkmanager.CoreNetworkPolicy, "example", networkmanager.CoreNetworkPolicyConfig{
    			CoreNetworkId:   "example",
    			Alias:           "example",
    			PolicyVersionId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoreNetworkPolicy } from "@ubx/sdk-aws/aws/data/networkmanager/core_network_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_core_network_policy" });

      data(CoreNetworkPolicy, "example", {
        coreNetworkId: "example",
        alias: "example",
        policyVersionId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import CoreNetworkPolicy, CoreNetworkPolicyConfig

    def describe():
        ubx.intent("look up aws_networkmanager_core_network_policy")

        ubx.data(CoreNetworkPolicy, "example", CoreNetworkPolicyConfig(
            core_network_id="example",
            alias="example",
            policy_version_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alias" type="string">
  The real, human-readable alias for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="core_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_version_id" type="number">
  The real, unique identifier of the policy version this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_network_policy" type="object">
  &lt;p&gt;Describes a core network policy. You can have only one LIVE Core Policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="alias" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="change_set_state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="core_network_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy_document" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy_errors" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy_version_id" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
