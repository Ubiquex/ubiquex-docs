---
title: "aws_networkmanager_customer_gateway_associations"
description: "`aws_networkmanager_customer_gateway_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_customer_gateway_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_customer_gateway_associations"})

    		ubx.Data(networkmanager.CustomerGatewayAssociations, "example", networkmanager.CustomerGatewayAssociationsConfig{
    			GlobalNetworkId:     "example",
    			CustomerGatewayArns: []string{"example"},
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomerGatewayAssociations } from "@ubx/sdk-aws/aws/data/networkmanager/customer_gateway_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_customer_gateway_associations",
      });

      data(CustomerGatewayAssociations, "example", {
        globalNetworkId: "example",
        customerGatewayArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import CustomerGatewayAssociations, CustomerGatewayAssociationsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_customer_gateway_associations")

        ubx.data(CustomerGatewayAssociations, "example", CustomerGatewayAssociationsConfig(
            global_network_id="example",
            customer_gateway_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="customer_gateway_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="customer_gateway_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
