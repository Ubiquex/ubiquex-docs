---
title: "aws_networkmanager_core_network_change_set"
description: "`aws_networkmanager_core_network_change_set` reads the real set of changes a pending Cloud WAN core network policy update would apply."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_core_network_change_set"})

    		ubx.Data(networkmanager.CoreNetworkChangeSet, "example", networkmanager.CoreNetworkChangeSetConfig{
    			CoreNetworkId:   "example",
    			PolicyVersionId: 1,
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CoreNetworkChangeSet } from "./local-sdk/aws/sdk/typescript/aws/data/networkmanager/core_network_change_set.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_core_network_change_set" });

      data(CoreNetworkChangeSet, "example", {
        coreNetworkId: "example",
        policyVersionId: 1,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import CoreNetworkChangeSet, CoreNetworkChangeSetConfig

    def describe():
        ubx.intent("look up aws_networkmanager_core_network_change_set")

        ubx.data(CoreNetworkChangeSet, "example", CoreNetworkChangeSetConfig(
            core_network_id="example",
            policy_version_id=1,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_version_id" type="number" required>
  The real, unique identifier of the policy version this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_network_changes" type="list(object)">
  The real, pending changes to this AWS Cloud WAN core network. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
