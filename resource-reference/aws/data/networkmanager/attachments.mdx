---
title: "aws_networkmanager_attachments"
description: "`aws_networkmanager_attachments` looks up a real Cloud WAN network attachment, connecting a VPC, VPN, or other resource into a core network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_attachments"})

    		ubx.Data(networkmanager.Attachments, "example", networkmanager.AttachmentsConfig{
    			AttachmentType: "example",
    			CoreNetworkId:  "example",
    			EdgeLocation:   "example",
    			MaxResults:     1,
    			State:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attachments } from "@ubx/sdk-aws/aws/data/networkmanager/attachments";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_attachments" });

      data(Attachments, "example", {
        attachmentType: "example",
        coreNetworkId: "example",
        edgeLocation: "example",
        maxResults: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import Attachments, AttachmentsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_attachments")

        ubx.data(Attachments, "example", AttachmentsConfig(
            attachment_type="example",
            core_network_id="example",
            edge_location="example",
            max_results=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attachment_type" type="string">
  The real, discriminating type of the attachment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="edge_location" type="string">
  The real CloudFront edge location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attachments" type="list(object)">
  The real attachments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
