---
title: "aws_networkmanager_transit_gateway_connect_peer_associations"
description: "`aws_networkmanager_transit_gateway_connect_peer_associations` reads the real Transit Gateway Connect peer associations within a Network Manager global..."
---

`aws_networkmanager_transit_gateway_connect_peer_associations` reads the real Transit Gateway Connect peer associations within a Network Manager global network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_transit_gateway_connect_peer_associations"})

    		ubx.Data(networkmanager.TransitGatewayConnectPeerAssociations, "example", networkmanager.TransitGatewayConnectPeerAssociationsConfig{
    			GlobalNetworkId:               "example",
    			MaxResults:                    1,
    			TransitGatewayConnectPeerArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransitGatewayConnectPeerAssociations } from "@ubx/sdk-aws/aws/data/networkmanager/transit_gateway_connect_peer_associations";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_networkmanager_transit_gateway_connect_peer_associations",
      });

      data(TransitGatewayConnectPeerAssociations, "example", {
        globalNetworkId: "example",
        maxResults: 1,
        transitGatewayConnectPeerArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import TransitGatewayConnectPeerAssociations, TransitGatewayConnectPeerAssociationsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_transit_gateway_connect_peer_associations")

        ubx.data(TransitGatewayConnectPeerAssociations, "example", TransitGatewayConnectPeerAssociationsConfig(
            global_network_id="example",
            max_results=1,
            transit_gateway_connect_peer_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="global_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN global network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_connect_peer_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the transit gateway connect peers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_connect_peer_associations" type="list(object)">
  The real transit gateway Connect peer associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
