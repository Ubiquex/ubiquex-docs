---
title: "aws_networkmanager_network_telemetry"
description: "`aws_networkmanager_network_telemetry` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_network_telemetry` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_network_telemetry"})

    		ubx.Data(networkmanager.NetworkTelemetry, "example", networkmanager.NetworkTelemetryConfig{
    			GlobalNetworkId:      "example",
    			AccountId:            "example",
    			AwsRegion:            "example",
    			CoreNetworkId:        "example",
    			MaxResults:           1,
    			RegisteredGatewayArn: "arn:aws:iam::123456789012:policy/example",
    			ResourceArn:          "arn:aws:iam::123456789012:policy/example",
    			ResourceType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkTelemetry } from "@ubx/sdk-aws/aws/data/networkmanager/network_telemetry";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_network_telemetry" });

      data(NetworkTelemetry, "example", {
        globalNetworkId: "example",
        accountId: "example",
        awsRegion: "example",
        coreNetworkId: "example",
        maxResults: 1,
        registeredGatewayArn: "arn:aws:iam::123456789012:policy/example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import NetworkTelemetry, NetworkTelemetryConfig

    def describe():
        ubx.intent("look up aws_networkmanager_network_telemetry")

        ubx.data(NetworkTelemetry, "example", NetworkTelemetryConfig(
            global_network_id="example",
            account_id="example",
            aws_region="example",
            core_network_id="example",
            max_results=1,
            registered_gateway_arn="arn:aws:iam::123456789012:policy/example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="aws_region" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="core_network_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="registered_gateway_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="network_telemetry" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
