---
title: "aws_networkmanager_core_network_policy_versions"
description: "`aws_networkmanager_core_network_policy_versions` reads a real, saved version of a Cloud WAN core network policy document."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_core_network_policy_versions"})

    		ubx.Data(networkmanager.CoreNetworkPolicyVersions, "example", networkmanager.CoreNetworkPolicyVersionsConfig{
    			CoreNetworkId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoreNetworkPolicyVersions } from "@ubx/sdk-aws/aws/data/networkmanager/core_network_policy_versions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_core_network_policy_versions",
      });

      data(CoreNetworkPolicyVersions, "example", {
        coreNetworkId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import CoreNetworkPolicyVersions, CoreNetworkPolicyVersionsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_core_network_policy_versions")

        ubx.data(CoreNetworkPolicyVersions, "example", CoreNetworkPolicyVersionsConfig(
            core_network_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN core network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_network_policy_versions" type="list(object)">
  The real versions of the core network policy included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
