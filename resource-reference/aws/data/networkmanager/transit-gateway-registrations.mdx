---
title: "aws_networkmanager_transit_gateway_registrations"
description: "`aws_networkmanager_transit_gateway_registrations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_transit_gateway_registrations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_transit_gateway_registrations"})

    		ubx.Data(networkmanager.TransitGatewayRegistrations, "example", networkmanager.TransitGatewayRegistrationsConfig{
    			GlobalNetworkId:    "example",
    			MaxResults:         1,
    			TransitGatewayArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransitGatewayRegistrations } from "@ubx/sdk-aws/aws/data/networkmanager/transit_gateway_registrations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_transit_gateway_registrations",
      });

      data(TransitGatewayRegistrations, "example", {
        globalNetworkId: "example",
        maxResults: 1,
        transitGatewayArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import TransitGatewayRegistrations, TransitGatewayRegistrationsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_transit_gateway_registrations")

        ubx.data(TransitGatewayRegistrations, "example", TransitGatewayRegistrationsConfig(
            global_network_id="example",
            max_results=1,
            transit_gateway_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="global_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="transit_gateway_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_registrations" type="list(object)">
  Returned by the lookup.
</ResponseField>
