---
title: "aws_networkmanager_core_network_routing_information"
description: "`aws_networkmanager_core_network_routing_information` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_core_network_routing_information` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_core_network_routing_information"})

    		ubx.Data(networkmanager.CoreNetworkRoutingInformation, "example", networkmanager.CoreNetworkRoutingInformationConfig{
    			CoreNetworkId:          "example",
    			EdgeLocation:           "example",
    			SegmentName:            "example-segment-name",
    			CommunityMatches:       []string{"example"},
    			ExactAsPathMatches:     []string{"example"},
    			LocalPreferenceMatches: []string{"example"},
    			MaxResults:             1,
    			MedMatches:             []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoreNetworkRoutingInformation } from "@ubx/sdk-aws/aws/data/networkmanager/core_network_routing_information";

    export default stack("example", () => {
      intent({
        summary: "look up aws_networkmanager_core_network_routing_information",
      });

      data(CoreNetworkRoutingInformation, "example", {
        coreNetworkId: "example",
        edgeLocation: "example",
        segmentName: "example-segment-name",
        communityMatches: ["example"],
        exactAsPathMatches: ["example"],
        localPreferenceMatches: ["example"],
        maxResults: 1,
        medMatches: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import CoreNetworkRoutingInformation, CoreNetworkRoutingInformationConfig

    def describe():
        ubx.intent("look up aws_networkmanager_core_network_routing_information")

        ubx.data(CoreNetworkRoutingInformation, "example", CoreNetworkRoutingInformationConfig(
            core_network_id="example",
            edge_location="example",
            segment_name="example-segment-name",
            community_matches=["example"],
            exact_as_path_matches=["example"],
            local_preference_matches=["example"],
            max_results=1,
            med_matches=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="community_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="core_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="edge_location" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="exact_as_path_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="local_preference_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="med_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="next_hop_filters" type="map(list(string))">
  Optional lookup argument.
</ResponseField>

<ResponseField name="segment_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_network_routing_information" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
