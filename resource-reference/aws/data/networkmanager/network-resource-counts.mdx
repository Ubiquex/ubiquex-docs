---
title: "aws_networkmanager_network_resource_counts"
description: "`aws_networkmanager_network_resource_counts` reads real, aggregated resource counts within a Network Manager global network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_network_resource_counts"})

    		ubx.Data(networkmanager.NetworkResourceCounts, "example", networkmanager.NetworkResourceCountsConfig{
    			GlobalNetworkId: "example",
    			MaxResults:      1,
    			ResourceType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkResourceCounts } from "./local-sdk/aws/sdk/typescript/aws/data/networkmanager/network_resource_counts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_network_resource_counts" });

      data(NetworkResourceCounts, "example", {
        globalNetworkId: "example",
        maxResults: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import NetworkResourceCounts, NetworkResourceCountsConfig

    def describe():
        ubx.intent("look up aws_networkmanager_network_resource_counts")

        ubx.data(NetworkResourceCounts, "example", NetworkResourceCountsConfig(
            global_network_id="example",
            max_results=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="global_network_id" type="string" required>
  The real, unique identifier of the AWS Cloud WAN global network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="network_resource_counts" type="list(object)">
  The real counts of network resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
