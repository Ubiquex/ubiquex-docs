---
title: "aws_networkmanager_network_routes"
description: "`aws_networkmanager_network_routes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_networkmanager_network_routes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_networkmanager_network_routes"})

    		ubx.Data(networkmanager.NetworkRoutes, "example", networkmanager.NetworkRoutesConfig{
    			GlobalNetworkId:      "example",
    			RouteTableIdentifier: map[string]any{"core_network_network_function_group": map[string]any{"core_network_id": "example"}},
    			DestinationFilters:   map[string]string{"managed-by": "ubx"},
    			ExactCidrMatches:     []string{"example"},
    			LongestPrefixMatches: []string{"example"},
    			PrefixListIds:        []string{"example"},
    			States:               []string{"example"},
    			SubnetOfMatches:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkRoutes } from "@ubx/sdk-aws/aws/data/networkmanager/network_routes";

    export default stack("example", () => {
      intent({ summary: "look up aws_networkmanager_network_routes" });

      data(NetworkRoutes, "example", {
        globalNetworkId: "example",
        routeTableIdentifier: {
          coreNetworkNetworkFunctionGroup: { coreNetworkId: "example" },
        },
        destinationFilters: { "managed-by": "ubx" },
        exactCidrMatches: ["example"],
        longestPrefixMatches: ["example"],
        prefixListIds: ["example"],
        states: ["example"],
        subnetOfMatches: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.networkmanager import NetworkRoutes, NetworkRoutesConfig

    def describe():
        ubx.intent("look up aws_networkmanager_network_routes")

        ubx.data(NetworkRoutes, "example", NetworkRoutesConfig(
            global_network_id="example",
            route_table_identifier={"core_network_network_function_group": {"core_network_id": "example"}},
            destination_filters={"managed-by": "ubx"},
            exact_cidr_matches=["example"],
            longest_prefix_matches=["example"],
            prefix_list_ids=["example"],
            states=["example"],
            subnet_of_matches=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="destination_filters" type="map(list(string))">
  Optional lookup argument.
</ResponseField>

<ResponseField name="exact_cidr_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="global_network_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="longest_prefix_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="prefix_list_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="route_table_identifier" type="object" required>
  &lt;p&gt;Describes a route table.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="core_network_network_function_group" type="object">
      &lt;p&gt;Describes a core network &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="core_network_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="edge_location" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="network_function_group_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="core_network_segment_edge" type="object">
      &lt;p&gt;Returns details about a core network edge.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="core_network_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="edge_location" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="segment_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="transit_gateway_route_table_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="states" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="subnet_of_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="supernet_of_matches" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="types" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_network_segment_edge" type="object">
  &lt;p&gt;Returns details about a core network edge.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="core_network_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="edge_location" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="segment_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_routes" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="route_table_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="route_table_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="route_table_type" type="string">
  Returned by the lookup.
</ResponseField>
