---
title: "aws_transcribe_vocabulary_filters"
description: "`data_aws_transcribe_vocabulary_filters` looks up a real, already-existing `aws_transcribe_vocabulary_filter`."
---

It defines a real list of words that Amazon Transcribe should mask, remove, or flag whenever they appear in a transcription's output, used for profanity filtering or redacting sensitive terms.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_vocabulary_filters"})

    		ubx.Data(transcribe.VocabularyFilters, "example", transcribe.VocabularyFiltersConfig{
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VocabularyFilters } from "./local-sdk/aws/sdk/typescript/aws/data/transcribe/vocabulary_filters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_vocabulary_filters" });

      data(VocabularyFilters, "example", {
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import VocabularyFilters, VocabularyFiltersConfig

    def describe():
        ubx.intent("look up aws_transcribe_vocabulary_filters")

        ubx.data(VocabularyFilters, "example", VocabularyFiltersConfig(
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vocabulary_filters" type="list(object)">
  The real Amazon Transcribe vocabulary filters included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
