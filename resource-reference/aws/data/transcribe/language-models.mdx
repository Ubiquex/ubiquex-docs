---
title: "aws_transcribe_language_models"
description: "`aws_transcribe_language_models` looks up a real, custom Transcribe language model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_language_models"})

    		ubx.Data(transcribe.LanguageModels, "example", transcribe.LanguageModelsConfig{
    			MaxResults:   1,
    			NameContains: "example",
    			StatusEquals: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LanguageModels } from "@ubx/sdk-aws/aws/data/transcribe/language_models";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_language_models" });

      data(LanguageModels, "example", {
        maxResults: 1,
        nameContains: "example",
        statusEquals: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import LanguageModels, LanguageModelsConfig

    def describe():
        ubx.intent("look up aws_transcribe_language_models")

        ubx.data(LanguageModels, "example", LanguageModelsConfig(
            max_results=1,
            name_contains="example",
            status_equals="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status_equals" type="string">
  Restricts results to objects whose status equals this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="models" type="list(object)">
  The real models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
