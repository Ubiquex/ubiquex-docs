---
title: "aws_transcribe_medical_scribe_jobs"
description: "`aws_transcribe_medical_scribe_jobs` reads the real Transcribe MedicalScribe jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_medical_scribe_jobs"})

    		ubx.Data(transcribe.MedicalScribeJobs, "example", transcribe.MedicalScribeJobsConfig{
    			JobNameContains: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MedicalScribeJobs } from "@ubx/sdk-aws/aws/data/transcribe/medical_scribe_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_medical_scribe_jobs" });

      data(MedicalScribeJobs, "example", {
        jobNameContains: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import MedicalScribeJobs, MedicalScribeJobsConfig

    def describe():
        ubx.intent("look up aws_transcribe_medical_scribe_jobs")

        ubx.data(MedicalScribeJobs, "example", MedicalScribeJobsConfig(
            job_name_contains="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_name_contains" type="string">
  Restricts results to jobs whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="medical_scribe_job_summaries" type="list(object)">
  The real, summarized medical scribe jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
