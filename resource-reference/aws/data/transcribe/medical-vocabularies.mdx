---
title: "aws_transcribe_medical_vocabularies"
description: "`aws_transcribe_medical_vocabularies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_transcribe_medical_vocabularies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_medical_vocabularies"})

    		ubx.Data(transcribe.MedicalVocabularies, "example", transcribe.MedicalVocabulariesConfig{
    			MaxResults:   1,
    			NameContains: "example",
    			StateEquals:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MedicalVocabularies } from "@ubx/sdk-aws/aws/data/transcribe/medical_vocabularies";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_medical_vocabularies" });

      data(MedicalVocabularies, "example", {
        maxResults: 1,
        nameContains: "example",
        stateEquals: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import MedicalVocabularies, MedicalVocabulariesConfig

    def describe():
        ubx.intent("look up aws_transcribe_medical_vocabularies")

        ubx.data(MedicalVocabularies, "example", MedicalVocabulariesConfig(
            max_results=1,
            name_contains="example",
            state_equals="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state_equals" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vocabularies" type="list(object)">
  Returned by the lookup.
</ResponseField>
