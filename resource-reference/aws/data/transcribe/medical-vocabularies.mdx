---
title: "aws_transcribe_medical_vocabularies"
description: "`aws_transcribe_medical_vocabularies` looks up a real, custom Transcribe Medical vocabulary."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_medical_vocabularies"})

    		ubx.Data(transcribe.MedicalVocabularies, "example", transcribe.MedicalVocabulariesConfig{
    			MaxResults:   1,
    			NameContains: "example",
    			StateEquals:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MedicalVocabularies } from "@ubx/sdk-aws/aws/data/transcribe/medical_vocabularies";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_medical_vocabularies" });

      data(MedicalVocabularies, "example", {
        maxResults: 1,
        nameContains: "example",
        stateEquals: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import MedicalVocabularies, MedicalVocabulariesConfig

    def describe():
        ubx.intent("look up aws_transcribe_medical_vocabularies")

        ubx.data(MedicalVocabularies, "example", MedicalVocabulariesConfig(
            max_results=1,
            name_contains="example",
            state_equals="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state_equals" type="string">
  Restricts results to objects whose state equals this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vocabularies" type="list(object)">
  The real vocabularies included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
