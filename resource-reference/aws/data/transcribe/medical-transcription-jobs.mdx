---
title: "aws_transcribe_medical_transcription_jobs"
description: "`aws_transcribe_medical_transcription_jobs` reads the real Transcribe Medical transcription jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	transcribe "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transcribe"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transcribe_medical_transcription_jobs"})

    		ubx.Data(transcribe.MedicalTranscriptionJobs, "example", transcribe.MedicalTranscriptionJobsConfig{
    			JobNameContains: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MedicalTranscriptionJobs } from "./local-sdk/aws/sdk/typescript/aws/data/transcribe/medical_transcription_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_transcribe_medical_transcription_jobs" });

      data(MedicalTranscriptionJobs, "example", {
        jobNameContains: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.transcribe import MedicalTranscriptionJobs, MedicalTranscriptionJobsConfig

    def describe():
        ubx.intent("look up aws_transcribe_medical_transcription_jobs")

        ubx.data(MedicalTranscriptionJobs, "example", MedicalTranscriptionJobsConfig(
            job_name_contains="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_name_contains" type="string">
  Restricts results to jobs whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="medical_transcription_job_summaries" type="list(object)">
  The real, summarized medical transcription jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
