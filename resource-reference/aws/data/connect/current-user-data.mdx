---
title: "aws_connect_current_user_data"
description: "`data_aws_connect_current_user_data` looks up a real, already-existing `aws_connect_user`."
---

It creates a real, individual agent, supervisor, or administrator account inside an `aws_connect_instance`, with an attached `aws_connect_security_profile` and `aws_connect_routing_profile` determining what they can do and what contacts they receive.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_current_user_data"})

    		ubx.Data(connect.CurrentUserData, "example", connect.CurrentUserDataConfig{
    			Filters:    map[string]any{"agents": []string{"example"}},
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CurrentUserData } from "@ubx/sdk-aws/aws/data/connect/current_user_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_current_user_data" });

      data(CurrentUserData, "example", {
        filters: { agents: ["example"] },
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import CurrentUserData, CurrentUserDataConfig

    def describe():
        ubx.intent("look up aws_connect_current_user_data")

        ubx.data(CurrentUserData, "example", CurrentUserDataConfig(
            filters={"agents": ["example"]},
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object" required>
  &lt;p&gt;A filter for the user data.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="agents" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="contact_filter" type="object">
      &lt;p&gt;Filters user data based on the contact information that is associated to the users. It contains a list of &lt;a href="https://docs.aws.amazon.com/connect/latest/adminguide/about-contact-states.html"&gt;contact states&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="contact_states" type="list(string)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="queues" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="routing_profiles" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_hierarchy_groups" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approximate_total_count" type="number">
  The real count of approximate totals. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_data_list" type="list(object)">
  The real user datas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
