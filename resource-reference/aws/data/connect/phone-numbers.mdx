---
title: "aws_connect_phone_numbers"
description: "`aws_connect_phone_numbers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_phone_numbers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_phone_numbers"})

    		ubx.Data(connect.PhoneNumbers, "example", connect.PhoneNumbersConfig{
    			InstanceId:              "example",
    			MaxResults:              1,
    			PhoneNumberCountryCodes: []string{"example"},
    			PhoneNumberTypes:        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PhoneNumbers } from "@ubx/sdk-aws/aws/data/connect/phone_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_phone_numbers" });

      data(PhoneNumbers, "example", {
        instanceId: "example",
        maxResults: 1,
        phoneNumberCountryCodes: ["example"],
        phoneNumberTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import PhoneNumbers, PhoneNumbersConfig

    def describe():
        ubx.intent("look up aws_connect_phone_numbers")

        ubx.data(PhoneNumbers, "example", PhoneNumbersConfig(
            instance_id="example",
            max_results=1,
            phone_number_country_codes=["example"],
            phone_number_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_country_codes" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="phone_number_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
