---
title: "aws_connect_routing_profiles"
description: "`data_aws_connect_routing_profiles` looks up a real, already-existing `aws_connect_routing_profile`."
---

It assigns an `aws_connect_user` to one or more `aws_connect_queue`s with real priority and channel concurrency settings, the mechanism that determines which queues an agent actually receives contacts from and how many at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_routing_profiles"})

    		ubx.Data(connect.RoutingProfiles, "example", connect.RoutingProfilesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RoutingProfiles } from "./local-sdk/aws/sdk/typescript/aws/data/connect/routing_profiles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_routing_profiles" });

      data(RoutingProfiles, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import RoutingProfiles, RoutingProfilesConfig

    def describe():
        ubx.intent("look up aws_connect_routing_profiles")

        ubx.data(RoutingProfiles, "example", RoutingProfilesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="routing_profile_summary_list" type="list(object)">
  The real routing profile summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
