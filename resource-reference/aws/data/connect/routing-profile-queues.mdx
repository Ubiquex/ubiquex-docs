---
title: "aws_connect_routing_profile_queues"
description: "`data_aws_connect_routing_profile_queues` looks up a real, already-existing `aws_connect_routing_profile`."
---

It assigns an `aws_connect_user` to one or more `aws_connect_queue`s with real priority and channel concurrency settings, the mechanism that determines which queues an agent actually receives contacts from and how many at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_routing_profile_queues"})

    		ubx.Data(connect.RoutingProfileQueues, "example", connect.RoutingProfileQueuesConfig{
    			InstanceId:       "example",
    			RoutingProfileId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RoutingProfileQueues } from "@ubx/sdk-aws/aws/data/connect/routing_profile_queues";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_routing_profile_queues" });

      data(RoutingProfileQueues, "example", {
        instanceId: "example",
        routingProfileId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import RoutingProfileQueues, RoutingProfileQueuesConfig

    def describe():
        ubx.intent("look up aws_connect_routing_profile_queues")

        ubx.data(RoutingProfileQueues, "example", RoutingProfileQueuesConfig(
            instance_id="example",
            routing_profile_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="routing_profile_id" type="string" required>
  The real, unique identifier of the routing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="routing_profile_queue_config_summary_list" type="list(object)">
  The real routing profile queue config summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
