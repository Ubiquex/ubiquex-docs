---
title: "aws_connect_user_notifications"
description: "`data_aws_connect_user_notifications` looks up a real, already-existing `aws_connect_notification`."
---

It configures a real, automated notification, sent by email or in-app, tied to an event on an `aws_connect_instance`, such as an evaluation being completed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_user_notifications"})

    		ubx.Data(connect.UserNotifications, "example", connect.UserNotificationsConfig{
    			InstanceId: "example",
    			UserId:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { UserNotifications } from "./local-sdk/aws/sdk/typescript/aws/data/connect/user_notifications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_user_notifications" });

      data(UserNotifications, "example", {
        instanceId: "example",
        userId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import UserNotifications, UserNotificationsConfig

    def describe():
        ubx.intent("look up aws_connect_user_notifications")

        ubx.data(UserNotifications, "example", UserNotificationsConfig(
            instance_id="example",
            user_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_notifications" type="list(object)">
  &lt;p&gt;A list of user notification summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
