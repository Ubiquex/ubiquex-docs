---
title: "aws_connect_current_metric_data"
description: "`aws_connect_current_metric_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_current_metric_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_current_metric_data"})

    		ubx.Data(connect.CurrentMetricData, "example", connect.CurrentMetricDataConfig{
    			CurrentMetrics: []map[string]any{{"name": "example-name"}},
    			Filters:        map[string]any{"agent_statuses": []string{"example"}},
    			InstanceId:     "example",
    			Groupings:      []string{"example"},
    			MaxResults:     1,
    			SortCriteria:   []map[string]any{{"sort_by_metric": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CurrentMetricData } from "@ubx/sdk-aws/aws/data/connect/current_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_current_metric_data" });

      data(CurrentMetricData, "example", {
        currentMetrics: [{ name: "example-name" }],
        filters: { agentStatuses: ["example"] },
        instanceId: "example",
        groupings: ["example"],
        maxResults: 1,
        sortCriteria: [{ sortByMetric: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import CurrentMetricData, CurrentMetricDataConfig

    def describe():
        ubx.intent("look up aws_connect_current_metric_data")

        ubx.data(CurrentMetricData, "example", CurrentMetricDataConfig(
            current_metrics=[{"name": "example-name"}],
            filters={"agent_statuses": ["example"]},
            instance_id="example",
            groupings=["example"],
            max_results=1,
            sort_criteria=[{"sort_by_metric": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="current_metrics" type="list(object)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object" required>
  &lt;p&gt;Contains the filter to apply when retrieving metrics.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="agent_statuses" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="channels" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="queues" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="routing_profiles" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="routing_step_expressions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subtypes" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="validation_test_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="groupings" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approximate_total_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="data_snapshot_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="metric_results" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
