---
title: "aws_connect_workspaces"
description: "`data_aws_connect_workspaces` looks up a real, already-existing `aws_connect_workspace`."
---

It is the real, single web application agents use day to day, the Contact Control Panel, step-by-step guides, customer profiles, cases, and AI assistance combined, rather than agents switching between several separate tools.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_workspaces"})

    		ubx.Data(connect.Workspaces, "example", connect.WorkspacesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Workspaces } from "./local-sdk/aws/sdk/typescript/aws/data/connect/workspaces.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_workspaces" });

      data(Workspaces, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Workspaces, WorkspacesConfig

    def describe():
        ubx.intent("look up aws_connect_workspaces")

        ubx.data(Workspaces, "example", WorkspacesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workspace_summary_list" type="list(object)">
  The real workspace summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
