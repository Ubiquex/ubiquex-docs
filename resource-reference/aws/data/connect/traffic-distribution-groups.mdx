---
title: "aws_connect_traffic_distribution_groups"
description: "`data_aws_connect_traffic_distribution_groups` looks up a real, already-existing `aws_connect_traffic_distribution_group`."
---

It links `aws_connect_instance`s in different AWS Regions and distributes real inbound traffic to phone numbers attached to the group between them, the mechanism behind multi-Region resiliency and disaster recovery for a contact center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_traffic_distribution_groups"})

    		ubx.Data(connect.TrafficDistributionGroups, "example", connect.TrafficDistributionGroupsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrafficDistributionGroups } from "@ubx/sdk-aws/aws/data/connect/traffic_distribution_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_traffic_distribution_groups" });

      data(TrafficDistributionGroups, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import TrafficDistributionGroups, TrafficDistributionGroupsConfig

    def describe():
        ubx.intent("look up aws_connect_traffic_distribution_groups")

        ubx.data(TrafficDistributionGroups, "example", TrafficDistributionGroupsConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_distribution_group_summary_list" type="list(object)">
  The real traffic distribution group summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
