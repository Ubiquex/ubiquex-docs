---
title: "aws_connect_phone_numbers_v2"
description: "`data_aws_connect_phone_numbers_v2` looks up a real, already-existing `aws_connect_phone_number`."
---

It claims a real phone number, direct-dial or toll-free, and assigns it to an `aws_connect_instance`, the number a caller actually dials to reach that contact center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_phone_numbers_v2"})

    		ubx.Data(connect.PhoneNumbersV2, "example", connect.PhoneNumbersV2Config{
    			InstanceId:              "example",
    			MaxResults:              1,
    			PhoneNumberCountryCodes: []string{"example"},
    			PhoneNumberPrefix:       "example",
    			PhoneNumberTypes:        []string{"example"},
    			TargetArn:               "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PhoneNumbersV2 } from "@ubx/sdk-aws/aws/data/connect/phone_numbers_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_phone_numbers_v2" });

      data(PhoneNumbersV2, "example", {
        instanceId: "example",
        maxResults: 1,
        phoneNumberCountryCodes: ["example"],
        phoneNumberPrefix: "example",
        phoneNumberTypes: ["example"],
        targetArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import PhoneNumbersV2, PhoneNumbersV2Config

    def describe():
        ubx.intent("look up aws_connect_phone_numbers_v2")

        ubx.data(PhoneNumbersV2, "example", PhoneNumbersV2Config(
            instance_id="example",
            max_results=1,
            phone_number_country_codes=["example"],
            phone_number_prefix="example",
            phone_number_types=["example"],
            target_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_country_codes" type="list(string)">
  The real country codes this phone number can be provisioned in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_prefix" type="string">
  The real S3 key prefix used for the phone number. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_types" type="list(string)">
  The real, discriminating types of phone numbers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_arn" type="string">
  The real Amazon Resource Name (ARN) of the target this action applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="list_phone_numbers_summary_list" type="list(object)">
  The real list phone numbers summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
