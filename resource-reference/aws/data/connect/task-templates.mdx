---
title: "aws_connect_task_templates"
description: "`data_aws_connect_task_templates` looks up a real, already-existing `aws_connect_task_template`."
---

It defines the real fields an agent fills in to create or complete a task, a follow-up, a case, a callback, so every task of that type captures the same structured information instead of an agent typing free-form notes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_task_templates"})

    		ubx.Data(connect.TaskTemplates, "example", connect.TaskTemplatesConfig{
    			InstanceId: "example",
    			Name:       "example",
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaskTemplates } from "@ubx/sdk-aws/aws/data/connect/task_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_task_templates" });

      data(TaskTemplates, "example", {
        instanceId: "example",
        name: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import TaskTemplates, TaskTemplatesConfig

    def describe():
        ubx.intent("look up aws_connect_task_templates")

        ubx.data(TaskTemplates, "example", TaskTemplatesConfig(
            instance_id="example",
            name="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_templates" type="list(object)">
  The real Amazon Connect task templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
