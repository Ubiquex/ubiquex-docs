---
title: "aws_connect_security_profile_applications"
description: "`data_aws_connect_security_profile_applications` looks up a real, already-existing `aws_connect_security_profile`."
---

It defines a real set of permissions, which parts of the Connect instance and which actions, that gets attached to an `aws_connect_user`, the actual access-control mechanism behind what an agent, supervisor, or administrator can see and do.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_security_profile_applications"})

    		ubx.Data(connect.SecurityProfileApplications, "example", connect.SecurityProfileApplicationsConfig{
    			InstanceId:        "example",
    			SecurityProfileId: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SecurityProfileApplications } from "./local-sdk/aws/sdk/typescript/aws/data/connect/security_profile_applications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_security_profile_applications" });

      data(SecurityProfileApplications, "example", {
        instanceId: "example",
        securityProfileId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import SecurityProfileApplications, SecurityProfileApplicationsConfig

    def describe():
        ubx.intent("look up aws_connect_security_profile_applications")

        ubx.data(SecurityProfileApplications, "example", SecurityProfileApplicationsConfig(
            instance_id="example",
            security_profile_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="security_profile_id" type="string" required>
  The real, unique identifier of the security profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
