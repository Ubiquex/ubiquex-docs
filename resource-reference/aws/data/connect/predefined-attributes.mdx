---
title: "aws_connect_predefined_attributes"
description: "`data_aws_connect_predefined_attributes` looks up a real, already-existing `aws_connect_predefined_attribute`."
---

It defines a real, named attribute, with a fixed or free-text value type, that can be assigned to `aws_connect_user`s or contacts on an `aws_connect_instance` and referenced in routing logic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_predefined_attributes"})

    		ubx.Data(connect.PredefinedAttributes, "example", connect.PredefinedAttributesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PredefinedAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/connect/predefined_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_predefined_attributes" });

      data(PredefinedAttributes, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import PredefinedAttributes, PredefinedAttributesConfig

    def describe():
        ubx.intent("look up aws_connect_predefined_attributes")

        ubx.data(PredefinedAttributes, "example", PredefinedAttributesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="predefined_attribute_summary_list" type="list(object)">
  The real predefined attribute summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
