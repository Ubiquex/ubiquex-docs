---
title: "aws_connect_views"
description: "`data_aws_connect_views` looks up a real, already-existing `aws_connect_view`."
---

It defines a real, custom UI template, built with HTML or JSX, that an `aws_connect_contact_flow`'s Show View block displays inside the agent workspace, guiding an agent through a task with a purpose-built screen instead of a generic one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_views"})

    		ubx.Data(connect.Views, "example", connect.ViewsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    			Type:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Views } from "@ubx/sdk-aws/aws/data/connect/views";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_views" });

      data(Views, "example", {
        instanceId: "example",
        maxResults: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Views, ViewsConfig

    def describe():
        ubx.intent("look up aws_connect_views")

        ubx.data(Views, "example", ViewsConfig(
            instance_id="example",
            max_results=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="views_summary_list" type="list(object)">
  The real views summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
