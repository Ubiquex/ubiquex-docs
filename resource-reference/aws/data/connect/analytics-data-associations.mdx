---
title: "aws_connect_analytics_data_associations"
description: "`aws_connect_analytics_data_associations` reads the real analytics datasets a Connect instance has associated with its own analytics data lake."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_analytics_data_associations"})

    		ubx.Data(connect.AnalyticsDataAssociations, "example", connect.AnalyticsDataAssociationsConfig{
    			InstanceId: "example",
    			DataSetId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnalyticsDataAssociations } from "@ubx/sdk-aws/aws/data/connect/analytics_data_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_analytics_data_associations" });

      data(AnalyticsDataAssociations, "example", {
        instanceId: "example",
        dataSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import AnalyticsDataAssociations, AnalyticsDataAssociationsConfig

    def describe():
        ubx.intent("look up aws_connect_analytics_data_associations")

        ubx.data(AnalyticsDataAssociations, "example", AnalyticsDataAssociationsConfig(
            instance_id="example",
            data_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_set_id" type="string">
  The real, unique identifier of the data set this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
