---
title: "aws_connect_prompts"
description: "`data_aws_connect_prompts` looks up a real, already-existing `aws_connect_prompt`."
---

It registers a real, uploaded audio file as a named prompt an `aws_connect_contact_flow` can play back, IVR greetings, hold messages, configurable and reusable rather than referencing a raw audio file inline.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_prompts"})

    		ubx.Data(connect.Prompts, "example", connect.PromptsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Prompts } from "@ubx/sdk-aws/aws/data/connect/prompts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_prompts" });

      data(Prompts, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Prompts, PromptsConfig

    def describe():
        ubx.intent("look up aws_connect_prompts")

        ubx.data(Prompts, "example", PromptsConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="prompt_summary_list" type="list(object)">
  The real prompt summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
