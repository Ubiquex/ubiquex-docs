---
title: "aws_connect_contact_evaluations"
description: "`aws_connect_contact_evaluations` reads the real quality evaluations completed for a Connect contact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_evaluations"})

    		ubx.Data(connect.ContactEvaluations, "example", connect.ContactEvaluationsConfig{
    			ContactId:  "example",
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactEvaluations } from "@ubx/sdk-aws/aws/data/connect/contact_evaluations";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_evaluations" });

      data(ContactEvaluations, "example", {
        contactId: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactEvaluations, ContactEvaluationsConfig

    def describe():
        ubx.intent("look up aws_connect_contact_evaluations")

        ubx.data(ContactEvaluations, "example", ContactEvaluationsConfig(
            contact_id="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  The real, unique identifier of the contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evaluation_summary_list" type="list(object)">
  The real evaluation summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
