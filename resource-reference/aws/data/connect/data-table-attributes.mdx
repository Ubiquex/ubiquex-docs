---
title: "aws_connect_data_table_attributes"
description: "`data_aws_connect_data_table_attributes` looks up a real, already-existing `aws_connect_data_table_attribute`."
---

It defines one real column, its name and data type, in an `aws_connect_data_table`, the schema an `aws_connect_data_table_record` populates values against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_data_table_attributes"})

    		ubx.Data(connect.DataTableAttributes, "example", connect.DataTableAttributesConfig{
    			DataTableId:  "example",
    			InstanceId:   "example",
    			AttributeIds: []string{"example"},
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataTableAttributes } from "@ubx/sdk-aws/aws/data/connect/data_table_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_data_table_attributes" });

      data(DataTableAttributes, "example", {
        dataTableId: "example",
        instanceId: "example",
        attributeIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import DataTableAttributes, DataTableAttributesConfig

    def describe():
        ubx.intent("look up aws_connect_data_table_attributes")

        ubx.data(DataTableAttributes, "example", DataTableAttributesConfig(
            data_table_id="example",
            instance_id="example",
            attribute_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_ids" type="list(string)">
  The real, unique identifiers of the attributes this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_table_id" type="string" required>
  The real, unique identifier of the data table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="list(object)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
