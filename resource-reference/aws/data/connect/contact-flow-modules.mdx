---
title: "aws_connect_contact_flow_modules"
description: "`data_aws_connect_contact_flow_modules` looks up a real, already-existing `aws_connect_contact_flow_module`."
---

It is a real, reusable block of logic that can be embedded inside multiple `aws_connect_contact_flow`s, so shared routing logic, a business-hours check, for instance, is maintained once rather than duplicated across every flow that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_flow_modules"})

    		ubx.Data(connect.ContactFlowModules, "example", connect.ContactFlowModulesConfig{
    			InstanceId:             "example",
    			ContactFlowModuleState: "example",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ContactFlowModules } from "./local-sdk/aws/sdk/typescript/aws/data/connect/contact_flow_modules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_flow_modules" });

      data(ContactFlowModules, "example", {
        instanceId: "example",
        contactFlowModuleState: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactFlowModules, ContactFlowModulesConfig

    def describe():
        ubx.intent("look up aws_connect_contact_flow_modules")

        ubx.data(ContactFlowModules, "example", ContactFlowModulesConfig(
            instance_id="example",
            contact_flow_module_state="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_flow_module_state" type="string">
  The real, current state of the contact flow module this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contact_flow_modules_summary_list" type="list(object)">
  The real contact flow modules summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
