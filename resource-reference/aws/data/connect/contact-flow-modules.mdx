---
title: "aws_connect_contact_flow_modules"
description: "`aws_connect_contact_flow_modules` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_contact_flow_modules` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_flow_modules"})

    		ubx.Data(connect.ContactFlowModules, "example", connect.ContactFlowModulesConfig{
    			InstanceId:             "example",
    			ContactFlowModuleState: "example",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactFlowModules } from "@ubx/sdk-aws/aws/data/connect/contact_flow_modules";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_flow_modules" });

      data(ContactFlowModules, "example", {
        instanceId: "example",
        contactFlowModuleState: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactFlowModules, ContactFlowModulesConfig

    def describe():
        ubx.intent("look up aws_connect_contact_flow_modules")

        ubx.data(ContactFlowModules, "example", ContactFlowModulesConfig(
            instance_id="example",
            contact_flow_module_state="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_flow_module_state" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contact_flow_modules_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
