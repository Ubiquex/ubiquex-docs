---
title: "aws_connect_contact_flows"
description: "`data_aws_connect_contact_flows` looks up a real, already-existing `aws_connect_contact_flow`."
---

It defines the real logic, IVR menus, routing decisions, Lambda invocations, that determines what happens to a contact, a call, a chat, a task, from the moment it enters an `aws_connect_instance` to the moment it's resolved.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_flows"})

    		ubx.Data(connect.ContactFlows, "example", connect.ContactFlowsConfig{
    			InstanceId:       "example",
    			ContactFlowTypes: []string{"example"},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ContactFlows } from "./local-sdk/aws/sdk/typescript/aws/data/connect/contact_flows.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_flows" });

      data(ContactFlows, "example", {
        instanceId: "example",
        contactFlowTypes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactFlows, ContactFlowsConfig

    def describe():
        ubx.intent("look up aws_connect_contact_flows")

        ubx.data(ContactFlows, "example", ContactFlowsConfig(
            instance_id="example",
            contact_flow_types=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_flow_types" type="list(string)">
  The real, discriminating types of contact flows this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contact_flow_summary_list" type="list(object)">
  The real contact flow summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
