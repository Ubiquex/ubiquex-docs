---
title: "aws_connect_users"
description: "`data_aws_connect_users` looks up a real, already-existing `aws_connect_user`."
---

It creates a real, individual agent, supervisor, or administrator account inside an `aws_connect_instance`, with an attached `aws_connect_security_profile` and `aws_connect_routing_profile` determining what they can do and what contacts they receive.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_users"})

    		ubx.Data(connect.Users, "example", connect.UsersConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Users } from "@ubx/sdk-aws/aws/data/connect/users";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_users" });

      data(Users, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Users, UsersConfig

    def describe():
        ubx.intent("look up aws_connect_users")

        ubx.data(Users, "example", UsersConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_summary_list" type="list(object)">
  The real user summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
