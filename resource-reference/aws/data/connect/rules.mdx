---
title: "aws_connect_rules"
description: "`aws_connect_rules` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_rules` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_rules"})

    		ubx.Data(connect.Rules, "example", connect.RulesConfig{
    			InstanceId:      "example",
    			EventSourceName: "example-event-source-name",
    			MaxResults:      1,
    			PublishStatus:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Rules } from "@ubx/sdk-aws/aws/data/connect/rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_rules" });

      data(Rules, "example", {
        instanceId: "example",
        eventSourceName: "example-event-source-name",
        maxResults: 1,
        publishStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Rules, RulesConfig

    def describe():
        ubx.intent("look up aws_connect_rules")

        ubx.data(Rules, "example", RulesConfig(
            instance_id="example",
            event_source_name="example-event-source-name",
            max_results=1,
            publish_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_source_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="publish_status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rule_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
