---
title: "aws_connect_rules"
description: "`data_aws_connect_rules` looks up a real, already-existing `aws_connect_rule`."
---

It automates a real action, generating an EventBridge event, sending an email, creating a task, the moment a defined condition is met, a metric threshold breach, a Contact Lens finding, an evaluation completion, rather than a supervisor watching dashboards and reacting manually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_rules"})

    		ubx.Data(connect.Rules, "example", connect.RulesConfig{
    			InstanceId:      "example",
    			EventSourceName: "example-event-source-name",
    			MaxResults:      1,
    			PublishStatus:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Rules } from "@ubx/sdk-aws/aws/data/connect/rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_rules" });

      data(Rules, "example", {
        instanceId: "example",
        eventSourceName: "example-event-source-name",
        maxResults: 1,
        publishStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Rules, RulesConfig

    def describe():
        ubx.intent("look up aws_connect_rules")

        ubx.data(Rules, "example", RulesConfig(
            instance_id="example",
            event_source_name="example-event-source-name",
            max_results=1,
            publish_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_source_name" type="string">
  The real name of the event source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="publish_status" type="string">
  The real, current status of the publish this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_summary_list" type="list(object)">
  The real rule summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
