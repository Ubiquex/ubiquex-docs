---
title: "aws_connect_quick_connects"
description: "`data_aws_connect_quick_connects` looks up a real, already-existing `aws_connect_quick_connect`."
---

It creates a real, one-click transfer target for an agent, another queue, a specific user, or an external phone number, so a warm transfer or task assignment doesn't require dialing or searching for the destination each time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_quick_connects"})

    		ubx.Data(connect.QuickConnects, "example", connect.QuickConnectsConfig{
    			InstanceId:        "example",
    			MaxResults:        1,
    			QuickConnectTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { QuickConnects } from "./local-sdk/aws/sdk/typescript/aws/data/connect/quick_connects.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_quick_connects" });

      data(QuickConnects, "example", {
        instanceId: "example",
        maxResults: 1,
        quickConnectTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import QuickConnects, QuickConnectsConfig

    def describe():
        ubx.intent("look up aws_connect_quick_connects")

        ubx.data(QuickConnects, "example", QuickConnectsConfig(
            instance_id="example",
            max_results=1,
            quick_connect_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="quick_connect_types" type="list(string)">
  The real, discriminating types of quick connects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="quick_connect_summary_list" type="list(object)">
  The real quick connect summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
