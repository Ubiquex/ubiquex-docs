---
title: "aws_connect_user_proficiencies"
description: "`data_aws_connect_user_proficiencies` looks up a real, already-existing `aws_connect_user`."
---

It creates a real, individual agent, supervisor, or administrator account inside an `aws_connect_instance`, with an attached `aws_connect_security_profile` and `aws_connect_routing_profile` determining what they can do and what contacts they receive.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_user_proficiencies"})

    		ubx.Data(connect.UserProficiencies, "example", connect.UserProficienciesConfig{
    			InstanceId: "example",
    			UserId:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserProficiencies } from "@ubx/sdk-aws/aws/data/connect/user_proficiencies";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_user_proficiencies" });

      data(UserProficiencies, "example", {
        instanceId: "example",
        userId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import UserProficiencies, UserProficienciesConfig

    def describe():
        ubx.intent("look up aws_connect_user_proficiencies")

        ubx.data(UserProficiencies, "example", UserProficienciesConfig(
            instance_id="example",
            user_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_proficiency_list" type="list(object)">
  The real user proficiencys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
