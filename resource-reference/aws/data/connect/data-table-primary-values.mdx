---
title: "aws_connect_data_table_primary_values"
description: "`aws_connect_data_table_primary_values` reads the real primary key values available in a Connect data table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_data_table_primary_values"})

    		ubx.Data(connect.DataTablePrimaryValues, "example", connect.DataTablePrimaryValuesConfig{
    			DataTableId:            "example",
    			InstanceId:             "example",
    			MaxResults:             1,
    			PrimaryAttributeValues: []map[string]any{{"attribute_name": "example-attribute-name"}},
    			RecordIds:              []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataTablePrimaryValues } from "@ubx/sdk-aws/aws/data/connect/data_table_primary_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_data_table_primary_values" });

      data(DataTablePrimaryValues, "example", {
        dataTableId: "example",
        instanceId: "example",
        maxResults: 1,
        primaryAttributeValues: [{ attributeName: "example-attribute-name" }],
        recordIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import DataTablePrimaryValues, DataTablePrimaryValuesConfig

    def describe():
        ubx.intent("look up aws_connect_data_table_primary_values")

        ubx.data(DataTablePrimaryValues, "example", DataTablePrimaryValuesConfig(
            data_table_id="example",
            instance_id="example",
            max_results=1,
            primary_attribute_values=[{"attribute_name": "example-attribute-name"}],
            record_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_table_id" type="string" required>
  The real, unique identifier of the data table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="primary_attribute_values" type="list(object)">
  The real values of this object's own primary attributes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="record_ids" type="list(string)">
  The real, unique identifiers of the records this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="primary_values_list" type="list(object)">
  The real primary valuess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
