---
title: "aws_connect_authentication_profiles"
description: "`aws_connect_authentication_profiles` reads the real authentication profiles on a Connect instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_authentication_profiles"})

    		ubx.Data(connect.AuthenticationProfiles, "example", connect.AuthenticationProfilesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuthenticationProfiles } from "@ubx/sdk-aws/aws/data/connect/authentication_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_authentication_profiles" });

      data(AuthenticationProfiles, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import AuthenticationProfiles, AuthenticationProfilesConfig

    def describe():
        ubx.intent("look up aws_connect_authentication_profiles")

        ubx.data(AuthenticationProfiles, "example", AuthenticationProfilesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authentication_profile_summary_list" type="list(object)">
  The real authentication profile summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
