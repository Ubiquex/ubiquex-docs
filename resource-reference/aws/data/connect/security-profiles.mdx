---
title: "aws_connect_security_profiles"
description: "`data_aws_connect_security_profiles` looks up a real, already-existing `aws_connect_security_profile`."
---

It defines a real set of permissions, which parts of the Connect instance and which actions, that gets attached to an `aws_connect_user`, the actual access-control mechanism behind what an agent, supervisor, or administrator can see and do.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_security_profiles"})

    		ubx.Data(connect.SecurityProfiles, "example", connect.SecurityProfilesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityProfiles } from "@ubx/sdk-aws/aws/data/connect/security_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_security_profiles" });

      data(SecurityProfiles, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import SecurityProfiles, SecurityProfilesConfig

    def describe():
        ubx.intent("look up aws_connect_security_profiles")

        ubx.data(SecurityProfiles, "example", SecurityProfilesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_profile_summary_list" type="list(object)">
  The real security profile summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
