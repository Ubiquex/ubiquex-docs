---
title: "aws_connect_security_keys"
description: "`data_aws_connect_security_keys` looks up a real, already-existing `aws_connect_security_key`."
---

It registers a real public key with an `aws_connect_instance`, used to validate the authenticity of requests, such as those from an embedded contact control panel, that the instance receives.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_security_keys"})

    		ubx.Data(connect.SecurityKeys, "example", connect.SecurityKeysConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityKeys } from "@ubx/sdk-aws/aws/data/connect/security_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_security_keys" });

      data(SecurityKeys, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import SecurityKeys, SecurityKeysConfig

    def describe():
        ubx.intent("look up aws_connect_security_keys")

        ubx.data(SecurityKeys, "example", SecurityKeysConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_keys" type="list(object)">
  The real, unique keys of the securitys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
