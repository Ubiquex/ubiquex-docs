---
title: "aws_connect_prompt_file"
description: "`data_aws_connect_prompt_file` looks up a real, already-existing `aws_connect_prompt`."
---

It registers a real, uploaded audio file as a named prompt an `aws_connect_contact_flow` can play back, IVR greetings, hold messages, configurable and reusable rather than referencing a raw audio file inline.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_prompt_file"})

    		ubx.Data(connect.PromptFile, "example", connect.PromptFileConfig{
    			InstanceId: "example",
    			PromptId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PromptFile } from "@ubx/sdk-aws/aws/data/connect/prompt_file";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_prompt_file" });

      data(PromptFile, "example", {
        instanceId: "example",
        promptId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import PromptFile, PromptFileConfig

    def describe():
        ubx.intent("look up aws_connect_prompt_file")

        ubx.data(PromptFile, "example", PromptFileConfig(
            instance_id="example",
            prompt_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="prompt_id" type="string" required>
  The real, unique identifier of the prompt this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="prompt_presigned_url" type="string">
  The real, resolvable URL of the prompt presigned this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
