---
title: "aws_connect_prompt_file"
description: "`aws_connect_prompt_file` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_prompt_file` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_prompt_file"})

    		ubx.Data(connect.PromptFile, "example", connect.PromptFileConfig{
    			InstanceId: "example",
    			PromptId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PromptFile } from "@ubx/sdk-aws/aws/data/connect/prompt_file";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_prompt_file" });

      data(PromptFile, "example", {
        instanceId: "example",
        promptId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import PromptFile, PromptFileConfig

    def describe():
        ubx.intent("look up aws_connect_prompt_file")

        ubx.data(PromptFile, "example", PromptFileConfig(
            instance_id="example",
            prompt_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="prompt_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="prompt_presigned_url" type="string">
  Returned by the lookup.
</ResponseField>
