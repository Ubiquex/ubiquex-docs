---
title: "aws_connect_agent_statuses"
description: "`aws_connect_agent_statuses` looks up a real Connect agent status, such as Available or a custom status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_agent_statuses"})

    		ubx.Data(connect.AgentStatuses, "example", connect.AgentStatusesConfig{
    			InstanceId:       "example",
    			AgentStatusTypes: []string{"example"},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgentStatuses } from "./local-sdk/aws/sdk/typescript/aws/data/connect/agent_statuses.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_agent_statuses" });

      data(AgentStatuses, "example", {
        instanceId: "example",
        agentStatusTypes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import AgentStatuses, AgentStatusesConfig

    def describe():
        ubx.intent("look up aws_connect_agent_statuses")

        ubx.data(AgentStatuses, "example", AgentStatusesConfig(
            instance_id="example",
            agent_status_types=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_status_types" type="list(string)">
  The real, discriminating types of agent statuss this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agent_status_summary_list" type="list(object)">
  The real agent status summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
