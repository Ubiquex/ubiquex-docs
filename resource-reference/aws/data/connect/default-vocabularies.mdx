---
title: "aws_connect_default_vocabularies"
description: "`aws_connect_default_vocabularies` reads the real, default custom vocabulary configured for Connect Contact Lens transcription."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_default_vocabularies"})

    		ubx.Data(connect.DefaultVocabularies, "example", connect.DefaultVocabulariesConfig{
    			InstanceId:   "example",
    			LanguageCode: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultVocabularies } from "@ubx/sdk-aws/aws/data/connect/default_vocabularies";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_default_vocabularies" });

      data(DefaultVocabularies, "example", {
        instanceId: "example",
        languageCode: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import DefaultVocabularies, DefaultVocabulariesConfig

    def describe():
        ubx.intent("look up aws_connect_default_vocabularies")

        ubx.data(DefaultVocabularies, "example", DefaultVocabulariesConfig(
            instance_id="example",
            language_code="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="language_code" type="string">
  The real, unique code identifying the language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_vocabulary_list" type="list(object)">
  The real default vocabularys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
