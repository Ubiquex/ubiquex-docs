---
title: "aws_connect_instance_attribute"
description: "`aws_connect_instance_attribute` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_instance_attribute` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_instance_attribute"})

    		ubx.Data(connect.InstanceAttribute, "example", connect.InstanceAttributeConfig{
    			AttributeType: "example",
    			InstanceId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceAttribute } from "@ubx/sdk-aws/aws/data/connect/instance_attribute";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_instance_attribute" });

      data(InstanceAttribute, "example", {
        attributeType: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import InstanceAttribute, InstanceAttributeConfig

    def describe():
        ubx.intent("look up aws_connect_instance_attribute")

        ubx.data(InstanceAttribute, "example", InstanceAttributeConfig(
            attribute_type="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute" type="object">
  &lt;p&gt;A toggle for an individual feature at the instance level.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attribute_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
