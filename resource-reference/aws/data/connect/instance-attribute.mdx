---
title: "aws_connect_instance_attribute"
description: "`data_aws_connect_instance_attribute` looks up a real, already-existing `aws_connect_instance`."
---

It provisions the real, top-level container for a contact center, telephony, chat, and every other Connect resource, agents, queues, contact flows, attaches to it. As of this session, AWS now uses "Amazon Connect" as the name of a broader portfolio of agentic business-function solutions, and refers to the contact center product this resource belongs to as "Amazon Connect Customer" (or simply Customer) in its own documentation, the two names used interchangeably during the transition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_instance_attribute"})

    		ubx.Data(connect.InstanceAttribute, "example", connect.InstanceAttributeConfig{
    			AttributeType: "example",
    			InstanceId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstanceAttribute } from "./local-sdk/aws/sdk/typescript/aws/data/connect/instance_attribute.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_instance_attribute" });

      data(InstanceAttribute, "example", {
        attributeType: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import InstanceAttribute, InstanceAttributeConfig

    def describe():
        ubx.intent("look up aws_connect_instance_attribute")

        ubx.data(InstanceAttribute, "example", InstanceAttributeConfig(
            attribute_type="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_type" type="string" required>
  The real, discriminating type of the attribute this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute" type="object">
  &lt;p&gt;A toggle for an individual feature at the instance level.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attribute_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
