---
title: "aws_connect_federation_token"
description: "`aws_connect_federation_token` reads a real, temporary federated sign-in URL and token for the Connect agent workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_federation_token"})

    		ubx.Data(connect.FederationToken, "example", connect.FederationTokenConfig{
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FederationToken } from "@ubx/sdk-aws/aws/data/connect/federation_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_federation_token" });

      data(FederationToken, "example", {
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import FederationToken, FederationTokenConfig

    def describe():
        ubx.intent("look up aws_connect_federation_token")

        ubx.data(FederationToken, "example", FederationTokenConfig(
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credentials" type="object">
  &lt;p&gt;Contains credentials to use for federation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="access_token_expiration" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="refresh_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="refresh_token_expiration" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sign_in_url" type="string">
  The real, resolvable URL of the sign in this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
