---
title: "aws_connect_queue_quick_connects"
description: "`data_aws_connect_queue_quick_connects` looks up a real, already-existing `aws_connect_quick_connect`."
---

It creates a real, one-click transfer target for an agent, another queue, a specific user, or an external phone number, so a warm transfer or task assignment doesn't require dialing or searching for the destination each time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_queue_quick_connects"})

    		ubx.Data(connect.QueueQuickConnects, "example", connect.QueueQuickConnectsConfig{
    			InstanceId: "example",
    			QueueId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueueQuickConnects } from "@ubx/sdk-aws/aws/data/connect/queue_quick_connects";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_queue_quick_connects" });

      data(QueueQuickConnects, "example", {
        instanceId: "example",
        queueId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import QueueQuickConnects, QueueQuickConnectsConfig

    def describe():
        ubx.intent("look up aws_connect_queue_quick_connects")

        ubx.data(QueueQuickConnects, "example", QueueQuickConnectsConfig(
            instance_id="example",
            queue_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="queue_id" type="string" required>
  The real, unique identifier of the queue this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="quick_connect_summary_list" type="list(object)">
  The real quick connect summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
