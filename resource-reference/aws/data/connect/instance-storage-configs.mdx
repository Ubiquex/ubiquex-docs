---
title: "aws_connect_instance_storage_configs"
description: "`aws_connect_instance_storage_configs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_instance_storage_configs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_instance_storage_configs"})

    		ubx.Data(connect.InstanceStorageConfigs, "example", connect.InstanceStorageConfigsConfig{
    			InstanceId:   "example",
    			ResourceType: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceStorageConfigs } from "@ubx/sdk-aws/aws/data/connect/instance_storage_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_instance_storage_configs" });

      data(InstanceStorageConfigs, "example", {
        instanceId: "example",
        resourceType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import InstanceStorageConfigs, InstanceStorageConfigsConfig

    def describe():
        ubx.intent("look up aws_connect_instance_storage_configs")

        ubx.data(InstanceStorageConfigs, "example", InstanceStorageConfigsConfig(
            instance_id="example",
            resource_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="storage_configs" type="list(object)">
  Returned by the lookup.
</ResponseField>
