---
title: "aws_connect_instance_storage_configs"
description: "`data_aws_connect_instance_storage_configs` looks up a real, already-existing `aws_connect_instance_storage_config`."
---

It configures where an `aws_connect_instance` stores a specific category of real data, call recordings, chat transcripts, contact trace records, in S3, Kinesis, or Kinesis Firehose.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_instance_storage_configs"})

    		ubx.Data(connect.InstanceStorageConfigs, "example", connect.InstanceStorageConfigsConfig{
    			InstanceId:   "example",
    			ResourceType: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstanceStorageConfigs } from "./local-sdk/aws/sdk/typescript/aws/data/connect/instance_storage_configs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_instance_storage_configs" });

      data(InstanceStorageConfigs, "example", {
        instanceId: "example",
        resourceType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import InstanceStorageConfigs, InstanceStorageConfigsConfig

    def describe():
        ubx.intent("look up aws_connect_instance_storage_configs")

        ubx.data(InstanceStorageConfigs, "example", InstanceStorageConfigsConfig(
            instance_id="example",
            resource_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="storage_configs" type="list(object)">
  The real storage configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
