---
title: "aws_connect_workspace_media"
description: "`data_aws_connect_workspace_media` looks up a real, already-existing `aws_connect_workspace`."
---

It is the real, single web application agents use day to day, the Contact Control Panel, step-by-step guides, customer profiles, cases, and AI assistance combined, rather than agents switching between several separate tools.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_workspace_media"})

    		ubx.Data(connect.WorkspaceMedia, "example", connect.WorkspaceMediaConfig{
    			InstanceId:  "example",
    			WorkspaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WorkspaceMedia } from "./local-sdk/aws/sdk/typescript/aws/data/connect/workspace_media.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_workspace_media" });

      data(WorkspaceMedia, "example", {
        instanceId: "example",
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import WorkspaceMedia, WorkspaceMediaConfig

    def describe():
        ubx.intent("look up aws_connect_workspace_media")

        ubx.data(WorkspaceMedia, "example", WorkspaceMediaConfig(
            instance_id="example",
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The real, unique identifier of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="media" type="list(object)">
  The real media included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
