---
title: "aws_connect_realtime_contact_analysis_segments_v2"
description: "`aws_connect_realtime_contact_analysis_segments_v2` reads the real, real-time Contact Lens transcript and analysis segments for an in-progress contact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_realtime_contact_analysis_segments_v2"})

    		ubx.Data(connect.RealtimeContactAnalysisSegmentsV2, "example", connect.RealtimeContactAnalysisSegmentsV2Config{
    			ContactId:    "example",
    			InstanceId:   "example",
    			OutputType:   "example",
    			SegmentTypes: []string{"example"},
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RealtimeContactAnalysisSegmentsV2 } from "./local-sdk/aws/sdk/typescript/aws/data/connect/realtime_contact_analysis_segments_v2.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_connect_realtime_contact_analysis_segments_v2",
      });

      data(RealtimeContactAnalysisSegmentsV2, "example", {
        contactId: "example",
        instanceId: "example",
        outputType: "example",
        segmentTypes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import RealtimeContactAnalysisSegmentsV2, RealtimeContactAnalysisSegmentsV2Config

    def describe():
        ubx.intent("look up aws_connect_realtime_contact_analysis_segments_v2")

        ubx.data(RealtimeContactAnalysisSegmentsV2, "example", RealtimeContactAnalysisSegmentsV2Config(
            contact_id="example",
            instance_id="example",
            output_type="example",
            segment_types=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  The real, unique identifier of the contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="output_type" type="string" required>
  The real, discriminating type of the output this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="segment_types" type="list(string)" required>
  The real, discriminating types of segments this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel" type="string">
  The real channel this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="segments" type="list(object)">
  The real segments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
