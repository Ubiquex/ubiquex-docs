---
title: "aws_connect_realtime_contact_analysis_segments_v2"
description: "`aws_connect_realtime_contact_analysis_segments_v2` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_realtime_contact_analysis_segments_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_realtime_contact_analysis_segments_v2"})

    		ubx.Data(connect.RealtimeContactAnalysisSegmentsV2, "example", connect.RealtimeContactAnalysisSegmentsV2Config{
    			ContactId:    "example",
    			InstanceId:   "example",
    			OutputType:   "example",
    			SegmentTypes: []string{"example"},
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RealtimeContactAnalysisSegmentsV2 } from "@ubx/sdk-aws/aws/data/connect/realtime_contact_analysis_segments_v2";

    export default stack("example", () => {
      intent({
        summary: "look up aws_connect_realtime_contact_analysis_segments_v2",
      });

      data(RealtimeContactAnalysisSegmentsV2, "example", {
        contactId: "example",
        instanceId: "example",
        outputType: "example",
        segmentTypes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import RealtimeContactAnalysisSegmentsV2, RealtimeContactAnalysisSegmentsV2Config

    def describe():
        ubx.intent("look up aws_connect_realtime_contact_analysis_segments_v2")

        ubx.data(RealtimeContactAnalysisSegmentsV2, "example", RealtimeContactAnalysisSegmentsV2Config(
            contact_id="example",
            instance_id="example",
            output_type="example",
            segment_types=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="output_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="segment_types" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="segments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
