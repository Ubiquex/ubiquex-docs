---
title: "aws_connect_flow_association"
description: "`aws_connect_flow_association` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_flow_association` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_flow_association"})

    		ubx.Data(connect.FlowAssociation, "example", connect.FlowAssociationConfig{
    			InstanceId:   "example",
    			ResourceId:   "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FlowAssociation } from "@ubx/sdk-aws/aws/data/connect/flow_association";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_flow_association" });

      data(FlowAssociation, "example", {
        instanceId: "example",
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import FlowAssociation, FlowAssociationConfig

    def describe():
        ubx.intent("look up aws_connect_flow_association")

        ubx.data(FlowAssociation, "example", FlowAssociationConfig(
            instance_id="example",
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="flow_id" type="string">
  Returned by the lookup.
</ResponseField>
