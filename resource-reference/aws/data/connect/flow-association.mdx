---
title: "aws_connect_flow_association"
description: "`aws_connect_flow_association` reads a real association between a Connect flow and another resource, such as a phone number."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_flow_association"})

    		ubx.Data(connect.FlowAssociation, "example", connect.FlowAssociationConfig{
    			InstanceId:   "example",
    			ResourceId:   "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FlowAssociation } from "./local-sdk/aws/sdk/typescript/aws/data/connect/flow_association.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_flow_association" });

      data(FlowAssociation, "example", {
        instanceId: "example",
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import FlowAssociation, FlowAssociationConfig

    def describe():
        ubx.intent("look up aws_connect_flow_association")

        ubx.data(FlowAssociation, "example", FlowAssociationConfig(
            instance_id="example",
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="flow_id" type="string">
  The real, unique identifier of the flow this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
