---
title: "aws_connect_test_cases"
description: "`data_aws_connect_test_cases` looks up a real, already-existing `aws_connect_test_case`."
---

It defines a real, simulated voice or chat interaction, the starting contact flow, phone number, caller intent, and expected response, that Connect's own native testing and simulation capability runs and scores pass or fail, validating a contact flow change before it reaches real customers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_test_cases"})

    		ubx.Data(connect.TestCases, "example", connect.TestCasesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestCases } from "@ubx/sdk-aws/aws/data/connect/test_cases";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_test_cases" });

      data(TestCases, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import TestCases, TestCasesConfig

    def describe():
        ubx.intent("look up aws_connect_test_cases")

        ubx.data(TestCases, "example", TestCasesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="test_case_summary_list" type="list(object)">
  The real test case summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
