---
title: "aws_connect_attached_files_configurations"
description: "`aws_connect_attached_files_configurations` reads the real file attachment configurations for a Connect instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_attached_files_configurations"})

    		ubx.Data(connect.AttachedFilesConfigurations, "example", connect.AttachedFilesConfigurationsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachedFilesConfigurations } from "@ubx/sdk-aws/aws/data/connect/attached_files_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_attached_files_configurations" });

      data(AttachedFilesConfigurations, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import AttachedFilesConfigurations, AttachedFilesConfigurationsConfig

    def describe():
        ubx.intent("look up aws_connect_attached_files_configurations")

        ubx.data(AttachedFilesConfigurations, "example", AttachedFilesConfigurationsConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attached_files_configurations" type="list(object)">
  &lt;p&gt;A list of attached files configuration summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
