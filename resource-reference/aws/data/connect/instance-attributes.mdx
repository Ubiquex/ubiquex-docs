---
title: "aws_connect_instance_attributes"
description: "`data_aws_connect_instance_attributes` looks up a real, already-existing `aws_connect_instance`."
---

It provisions the real, top-level container for a contact center, telephony, chat, and every other Connect resource, agents, queues, contact flows, attaches to it. As of this session, AWS now uses "Amazon Connect" as the name of a broader portfolio of agentic business-function solutions, and refers to the contact center product this resource belongs to as "Amazon Connect Customer" (or simply Customer) in its own documentation, the two names used interchangeably during the transition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_instance_attributes"})

    		ubx.Data(connect.InstanceAttributes, "example", connect.InstanceAttributesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceAttributes } from "@ubx/sdk-aws/aws/data/connect/instance_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_instance_attributes" });

      data(InstanceAttributes, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import InstanceAttributes, InstanceAttributesConfig

    def describe():
        ubx.intent("look up aws_connect_instance_attributes")

        ubx.data(InstanceAttributes, "example", InstanceAttributesConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="list(object)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
