---
title: "aws_connect_evaluation_form_versions"
description: "`data_aws_connect_evaluation_form_versions` looks up a real, already-existing `aws_connect_evaluation_form`."
---

It defines a real, weighted scorecard, sections and questions, used to grade a recorded or live contact, the structure Connect's own quality-management feature uses to produce a consistent, comparable score across every evaluation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_evaluation_form_versions"})

    		ubx.Data(connect.EvaluationFormVersions, "example", connect.EvaluationFormVersionsConfig{
    			EvaluationFormId: "example",
    			InstanceId:       "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EvaluationFormVersions } from "./local-sdk/aws/sdk/typescript/aws/data/connect/evaluation_form_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_evaluation_form_versions" });

      data(EvaluationFormVersions, "example", {
        evaluationFormId: "example",
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import EvaluationFormVersions, EvaluationFormVersionsConfig

    def describe():
        ubx.intent("look up aws_connect_evaluation_form_versions")

        ubx.data(EvaluationFormVersions, "example", EvaluationFormVersionsConfig(
            evaluation_form_id="example",
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="evaluation_form_id" type="string" required>
  The real, unique identifier of the evaluation form this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evaluation_form_version_summary_list" type="list(object)">
  The real evaluation form version summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
