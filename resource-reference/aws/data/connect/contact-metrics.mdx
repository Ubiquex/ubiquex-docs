---
title: "aws_connect_contact_metrics"
description: "`data_aws_connect_contact_metrics` looks up a real, already-existing `aws_connect_metric`."
---

It defines a real, custom real-time or historical metric, built with advanced filters and functions on Connect's own metric primitives, for use in dashboards and the metrics API, beyond the fixed set of metrics Connect provides out of the box.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_metrics"})

    		ubx.Data(connect.ContactMetrics, "example", connect.ContactMetricsConfig{
    			ContactId:  "example",
    			InstanceId: "example",
    			Metrics:    []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactMetrics } from "@ubx/sdk-aws/aws/data/connect/contact_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_metrics" });

      data(ContactMetrics, "example", {
        contactId: "example",
        instanceId: "example",
        metrics: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactMetrics, ContactMetricsConfig

    def describe():
        ubx.intent("look up aws_connect_contact_metrics")

        ubx.data(ContactMetrics, "example", ContactMetricsConfig(
            contact_id="example",
            instance_id="example",
            metrics=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  The real, unique identifier of the contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(object)" required>
  The real metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metric_results" type="list(object)">
  The real metric results included in this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
