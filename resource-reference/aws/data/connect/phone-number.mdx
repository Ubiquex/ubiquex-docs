---
title: "aws_connect_phone_number"
description: "`data_aws_connect_phone_number` looks up a real, already-existing `aws_connect_phone_number`."
---

It claims a real phone number, direct-dial or toll-free, and assigns it to an `aws_connect_instance`, the number a caller actually dials to reach that contact center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own phone number"})

    		ubx.Resource(connect.PhoneNumber, "example", connect.PhoneNumberConfig{
    			TargetArn:            "arn:aws:iam::123456789012:policy/example",
    			CountryCode:          "example",
    			Description:          "Managed by ubx.",
    			Prefix:               "example",
    			SourcePhoneNumberArn: "arn:aws:iam::123456789012:policy/example",
    			Tags:                 []map[string]any{{"key": "example"}},
    			Type:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PhoneNumber } from "@ubx/sdk-aws/aws/connect/phone_number";

    export default stack("example", () => {
      intent({ summary: "example own phone number" });

      resource(PhoneNumber, "example", {
        targetArn: "arn:aws:iam::123456789012:policy/example",
        countryCode: "example",
        description: "Managed by ubx.",
        prefix: "example",
        sourcePhoneNumberArn: "arn:aws:iam::123456789012:policy/example",
        tags: [{ key: "example" }],
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import PhoneNumber, PhoneNumberConfig
    from ubx.aws.connect.phone_number import PhoneNumber_Tags

    def describe():
        ubx.intent("example own phone number")

        ubx.resource(PhoneNumber, "example", PhoneNumberConfig(
            target_arn="arn:aws:iam::123456789012:policy/example",
            country_code="example",
            description="Managed by ubx.",
            prefix="example",
            source_phone_number_arn="arn:aws:iam::123456789012:policy/example",
            tags=[PhoneNumber_Tags(key="example")],
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_phone_number, with target arn
    arn:aws:iam::123456789012:policy/example, country code example,
    description Managed by ubx., prefix example, source phone number
    arn arn:aws:iam::123456789012:policy/example, tags a set value,
    type example.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="phone_number_id" type="string" required>
  The real, unique identifier of the phone number this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="claimed_phone_number_summary" type="object">
  &lt;p&gt;Information about a phone number that has been claimed to your Connect Customer instance or traffic distribution group.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="instance_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_country_code" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_status" type="object">
      &lt;p&gt;The status of the phone number.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CLAIMED&lt;/code&gt; means the previous &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_ClaimPhoneNumber.html"&gt;ClaimPhoneNumber&lt;/a&gt; or &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_UpdatePhoneNumber.html"&gt;UpdatePhoneNumber&lt;/a&gt; operation succeeded.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;IN_PROGRESS&lt;/code&gt; means a &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_ClaimPhoneNumber.html"&gt;ClaimPhoneNumber&lt;/a&gt;, &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_UpdatePhoneNumber.html"&gt;UpdatePhoneNumber&lt;/a&gt;, or &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_UpdatePhoneNumberMetadata.html"&gt;UpdatePhoneNumberMetadata&lt;/a&gt; operation is still in progress and has not yet completed. You can call &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_DescribePhoneNumber.html"&gt;DescribePhoneNumber&lt;/a&gt; at a later time to verify if the previous operation has completed.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;FAILED&lt;/code&gt; indicates that the previous &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_ClaimPhoneNumber.html"&gt;ClaimPhoneNumber&lt;/a&gt; or &lt;a href="https://docs.aws.amazon.com/connect/latest/APIReference/API_UpdatePhoneNumber.html"&gt;UpdatePhoneNumber&lt;/a&gt; operation has failed. It will include a message indicating the failure reason. A common reason for a failure may be that the &lt;code&gt;TargetArn&lt;/code&gt; value you are claiming or updating a phone number to has reached its limit of total claimed numbers. If you received a &lt;code&gt;FAILED&lt;/code&gt; status from a &lt;code&gt;ClaimPhoneNumber&lt;/code&gt; API call, you have one day to retry claiming the phone number before the number is released back to the inventory for other customers to claim.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="phone_number_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_phone_number_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
