---
title: "aws_connect_contact_attributes"
description: "`aws_connect_contact_attributes` reads the real, custom attributes attached to a Connect contact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_attributes"})

    		ubx.Data(connect.ContactAttributes, "example", connect.ContactAttributesConfig{
    			InitialContactId: "example",
    			InstanceId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactAttributes } from "@ubx/sdk-aws/aws/data/connect/contact_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_attributes" });

      data(ContactAttributes, "example", {
        initialContactId: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactAttributes, ContactAttributesConfig

    def describe():
        ubx.intent("look up aws_connect_contact_attributes")

        ubx.data(ContactAttributes, "example", ContactAttributesConfig(
            initial_contact_id="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="initial_contact_id" type="string" required>
  The real, unique identifier of the initial contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(string)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
