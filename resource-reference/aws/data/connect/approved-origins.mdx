---
title: "aws_connect_approved_origins"
description: "`data_aws_connect_approved_origins` looks up a real, already-existing `aws_connect_approved_origin`."
---

It allow-lists a real domain that is permitted to embed an `aws_connect_instance`'s contact control panel through the Amazon Connect Streams API, the mechanism that lets a custom CRM or web app host Connect's own softphone UI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_approved_origins"})

    		ubx.Data(connect.ApprovedOrigins, "example", connect.ApprovedOriginsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApprovedOrigins } from "@ubx/sdk-aws/aws/data/connect/approved_origins";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_approved_origins" });

      data(ApprovedOrigins, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ApprovedOrigins, ApprovedOriginsConfig

    def describe():
        ubx.intent("look up aws_connect_approved_origins")

        ubx.data(ApprovedOrigins, "example", ApprovedOriginsConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="origins" type="list(string)">
  The real origins included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
