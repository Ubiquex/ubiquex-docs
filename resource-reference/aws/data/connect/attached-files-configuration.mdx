---
title: "aws_connect_attached_files_configuration"
description: "`aws_connect_attached_files_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_attached_files_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_attached_files_configuration"})

    		ubx.Data(connect.AttachedFilesConfiguration, "example", connect.AttachedFilesConfigurationConfig{
    			AttachmentScope: "example",
    			InstanceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachedFilesConfiguration } from "@ubx/sdk-aws/aws/data/connect/attached_files_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_attached_files_configuration" });

      data(AttachedFilesConfiguration, "example", {
        attachmentScope: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import AttachedFilesConfiguration, AttachedFilesConfigurationConfig

    def describe():
        ubx.intent("look up aws_connect_attached_files_configuration")

        ubx.data(AttachedFilesConfiguration, "example", AttachedFilesConfigurationConfig(
            attachment_scope="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attachment_scope" type="string" required>
  &lt;p&gt;The scope of the attachment. Valid values are:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;EMAIL&lt;/code&gt; - Attachments for email messages.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CHAT&lt;/code&gt; - Attachments for chat conversations.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CASE&lt;/code&gt; - Attachments for cases.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;TASK&lt;/code&gt; - Attachments for tasks.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attached_files_configuration" type="object">
  &lt;p&gt;The configuration for attached files for a specific attachment scope.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attachment_scope" type="string" required>
      &lt;p&gt;The scope of the attachment. Valid values are:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;EMAIL&lt;/code&gt; - Attachments for email messages.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CHAT&lt;/code&gt; - Attachments for chat conversations.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;CASE&lt;/code&gt; - Attachments for cases.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;TASK&lt;/code&gt; - Attachments for tasks.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="extension_configuration" type="object">
      &lt;p&gt;The configuration for allowed file extensions.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="allowed_extensions" type="list(object)" required>
          &lt;p&gt;A list of allowed file extensions.&lt;/p&gt; Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="maximum_size_limit_in_bytes" type="number">
      &lt;p&gt;The maximum size limit for attached files in bytes. The minimum value is 1 and the maximum value is 104857600 (100 MB).&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
