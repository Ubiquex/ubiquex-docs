---
title: "aws_connect_data_table_values"
description: "`data_aws_connect_data_table_values` looks up a real, already-existing `aws_connect_data_table`."
---

It is a real, no-code, JSON-like data structure an administrator manages directly, then reads from or writes to inside an `aws_connect_contact_flow` via the Data Table flow block, dynamic configuration, agent extension routing, seasonal messaging, that changes contact center behavior without a code deployment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_data_table_values"})

    		ubx.Data(connect.DataTableValues, "example", connect.DataTableValuesConfig{
    			DataTableId:            "example",
    			InstanceId:             "example",
    			MaxResults:             1,
    			PrimaryAttributeValues: []map[string]any{{"attribute_name": "example-attribute-name"}},
    			RecordIds:              []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataTableValues } from "@ubx/sdk-aws/aws/data/connect/data_table_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_data_table_values" });

      data(DataTableValues, "example", {
        dataTableId: "example",
        instanceId: "example",
        maxResults: 1,
        primaryAttributeValues: [{ attributeName: "example-attribute-name" }],
        recordIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import DataTableValues, DataTableValuesConfig
    from ubx.aws.data.connect.data_table_values import DataTableValues_PrimaryAttributeValues

    def describe():
        ubx.intent("look up aws_connect_data_table_values")

        ubx.data(DataTableValues, "example", DataTableValuesConfig(
            data_table_id="example",
            instance_id="example",
            max_results=1,
            primary_attribute_values=[DataTableValues_PrimaryAttributeValues(attribute_name="example-attribute-name")],
            record_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_table_id" type="string" required>
  The real, unique identifier of the data table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="primary_attribute_values" type="list(object)">
  The real values of this object's own primary attributes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="record_ids" type="list(string)">
  The real, unique identifiers of the records this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="values" type="list(object)">
  The real, primary values of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
