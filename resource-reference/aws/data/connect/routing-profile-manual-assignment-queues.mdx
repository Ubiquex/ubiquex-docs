---
title: "aws_connect_routing_profile_manual_assignment_queues"
description: "`aws_connect_routing_profile_manual_assignment_queues` reads the real queues configured for manual contact assignment on a Connect routing profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_routing_profile_manual_assignment_queues"})

    		ubx.Data(connect.RoutingProfileManualAssignmentQueues, "example", connect.RoutingProfileManualAssignmentQueuesConfig{
    			InstanceId:       "example",
    			RoutingProfileId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RoutingProfileManualAssignmentQueues } from "@ubx/sdk-aws/aws/data/connect/routing_profile_manual_assignment_queues";

    export default stack("example", () => {
      intent({
        summary: "look up aws_connect_routing_profile_manual_assignment_queues",
      });

      data(RoutingProfileManualAssignmentQueues, "example", {
        instanceId: "example",
        routingProfileId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import RoutingProfileManualAssignmentQueues, RoutingProfileManualAssignmentQueuesConfig

    def describe():
        ubx.intent("look up aws_connect_routing_profile_manual_assignment_queues")

        ubx.data(RoutingProfileManualAssignmentQueues, "example", RoutingProfileManualAssignmentQueuesConfig(
            instance_id="example",
            routing_profile_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="routing_profile_id" type="string" required>
  The real, unique identifier of the routing profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="routing_profile_manual_assignment_queue_config_summary_list" type="list(object)">
  The real routing profile manual assignment queue config summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
