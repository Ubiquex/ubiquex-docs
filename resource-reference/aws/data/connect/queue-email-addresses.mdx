---
title: "aws_connect_queue_email_addresses"
description: "`data_aws_connect_queue_email_addresses` looks up a real, already-existing `aws_connect_email_address`."
---

It registers a real, dedicated email address an `aws_connect_instance` sends and receives customer email through, the email counterpart to a phone number for voice or an endpoint for chat.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_queue_email_addresses"})

    		ubx.Data(connect.QueueEmailAddresses, "example", connect.QueueEmailAddressesConfig{
    			InstanceId: "example",
    			QueueId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueueEmailAddresses } from "@ubx/sdk-aws/aws/data/connect/queue_email_addresses";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_queue_email_addresses" });

      data(QueueEmailAddresses, "example", {
        instanceId: "example",
        queueId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import QueueEmailAddresses, QueueEmailAddressesConfig

    def describe():
        ubx.intent("look up aws_connect_queue_email_addresses")

        ubx.data(QueueEmailAddresses, "example", QueueEmailAddressesConfig(
            instance_id="example",
            queue_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="queue_id" type="string" required>
  The real, unique identifier of the queue this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="email_address_metadata_list" type="list(object)">
  The real email address metadatas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_region" type="string">
  The real AWS region this object was last modified in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
