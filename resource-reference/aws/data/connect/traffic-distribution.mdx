---
title: "aws_connect_traffic_distribution"
description: "`data_aws_connect_traffic_distribution` looks up a real, already-existing `aws_connect_traffic_distribution_group`."
---

It links `aws_connect_instance`s in different AWS Regions and distributes real inbound traffic to phone numbers attached to the group between them, the mechanism behind multi-Region resiliency and disaster recovery for a contact center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_traffic_distribution"})

    		ubx.Data(connect.TrafficDistribution, "example", connect.TrafficDistributionConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TrafficDistribution } from "./local-sdk/aws/sdk/typescript/aws/data/connect/traffic_distribution.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_traffic_distribution" });

      data(TrafficDistribution, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect import TrafficDistribution, TrafficDistributionConfig

    def describe():
        ubx.intent("look up aws_connect_traffic_distribution")

        ubx.data(TrafficDistribution, "example", TrafficDistributionConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agent_config" type="object">
  &lt;p&gt;The distribution of agents between the instance and its replica(s).&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="distributions" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sign_in_config" type="object">
  &lt;p&gt;The distribution that determines which Amazon Web Services Regions should be used to sign in agents in to both the instance and its replica(s).&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="distributions" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="telephony_config" type="object">
  &lt;p&gt;The distribution of traffic between the instance and its replicas.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="distributions" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
