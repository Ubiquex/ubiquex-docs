---
title: "aws_connect_metrics"
description: "`aws_connect_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_metrics"})

    		ubx.Data(connect.Metrics, "example", connect.MetricsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    			Type:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Metrics } from "@ubx/sdk-aws/aws/data/connect/metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_metrics" });

      data(Metrics, "example", {
        instanceId: "example",
        maxResults: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Metrics, MetricsConfig

    def describe():
        ubx.intent("look up aws_connect_metrics")

        ubx.data(Metrics, "example", MetricsConfig(
            instance_id="example",
            max_results=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  &lt;p&gt;The type of the metric. Valid values: &lt;code&gt;AWS_MANAGED&lt;/code&gt; | &lt;code&gt;CUSTOMER_MANAGED&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
