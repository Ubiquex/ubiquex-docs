---
title: "aws_connect_contact_flow_module_aliases"
description: "`data_aws_connect_contact_flow_module_aliases` looks up a real, already-existing `aws_connect_contact_flow_module`."
---

It is a real, reusable block of logic that can be embedded inside multiple `aws_connect_contact_flow`s, so shared routing logic, a business-hours check, for instance, is maintained once rather than duplicated across every flow that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_flow_module_aliases"})

    		ubx.Data(connect.ContactFlowModuleAliases, "example", connect.ContactFlowModuleAliasesConfig{
    			ContactFlowModuleId: "example",
    			InstanceId:          "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactFlowModuleAliases } from "@ubx/sdk-aws/aws/data/connect/contact_flow_module_aliases";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_flow_module_aliases" });

      data(ContactFlowModuleAliases, "example", {
        contactFlowModuleId: "example",
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactFlowModuleAliases, ContactFlowModuleAliasesConfig

    def describe():
        ubx.intent("look up aws_connect_contact_flow_module_aliases")

        ubx.data(ContactFlowModuleAliases, "example", ContactFlowModuleAliasesConfig(
            contact_flow_module_id="example",
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_flow_module_id" type="string" required>
  The real, unique identifier of the contact flow module this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contact_flow_module_alias_summary_list" type="list(object)">
  The real contact flow module alias summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
