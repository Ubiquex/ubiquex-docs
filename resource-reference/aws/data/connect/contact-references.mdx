---
title: "aws_connect_contact_references"
description: "`aws_connect_contact_references` reads the real reference attachments (URLs or files) linked to a Connect contact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_contact_references"})

    		ubx.Data(connect.ContactReferences, "example", connect.ContactReferencesConfig{
    			ContactId:      "example",
    			InstanceId:     "example",
    			ReferenceTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContactReferences } from "@ubx/sdk-aws/aws/data/connect/contact_references";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_contact_references" });

      data(ContactReferences, "example", {
        contactId: "example",
        instanceId: "example",
        referenceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import ContactReferences, ContactReferencesConfig

    def describe():
        ubx.intent("look up aws_connect_contact_references")

        ubx.data(ContactReferences, "example", ContactReferencesConfig(
            contact_id="example",
            instance_id="example",
            reference_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  The real, unique identifier of the contact this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="reference_types" type="list(string)" required>
  The real, discriminating types of references this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reference_summary_list" type="list(object)">
  The real reference summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
