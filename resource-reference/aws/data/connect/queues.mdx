---
title: "aws_connect_queues"
description: "`data_aws_connect_queues` looks up a real, already-existing `aws_connect_queue`."
---

It holds real, waiting contacts until an agent, matched by an `aws_connect_routing_profile`, becomes available, the actual point contacts sit at between entering an `aws_connect_contact_flow` and being answered.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_queues"})

    		ubx.Data(connect.Queues, "example", connect.QueuesConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    			QueueTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Queues } from "@ubx/sdk-aws/aws/data/connect/queues";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_queues" });

      data(Queues, "example", {
        instanceId: "example",
        maxResults: 1,
        queueTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect import Queues, QueuesConfig

    def describe():
        ubx.intent("look up aws_connect_queues")

        ubx.data(Queues, "example", QueuesConfig(
            instance_id="example",
            max_results=1,
            queue_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="queue_types" type="list(string)">
  The real, discriminating types of queues this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queue_summary_list" type="list(object)">
  The real queue summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
