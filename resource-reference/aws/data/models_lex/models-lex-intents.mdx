---
title: "aws_models_lex_intents"
description: "`aws_models_lex_intents` reads the real Lex V1 intents on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_intents"})

    		ubx.Data(models_lex.ModelsLexIntents, "example", models_lex.ModelsLexIntentsConfig{
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexIntents } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_intents";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_intents" });

      data(ModelsLexIntents, "example", {
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexIntents, ModelsLexIntentsConfig

    def describe():
        ubx.intent("look up aws_models_lex_intents")

        ubx.data(ModelsLexIntents, "example", ModelsLexIntentsConfig(
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="intents" type="list(object)">
  The real Amazon Lex intents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
