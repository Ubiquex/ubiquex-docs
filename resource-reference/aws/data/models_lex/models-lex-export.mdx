---
title: "aws_models_lex_export"
description: "`aws_models_lex_export` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_lex_export` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_export"})

    		ubx.Data(models_lex.ModelsLexExport, "example", models_lex.ModelsLexExportConfig{
    			ExportType:   "example",
    			Name:         "example",
    			ResourceType: "example",
    			Version:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexExport } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_export";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_export" });

      data(ModelsLexExport, "example", {
        exportType: "example",
        name: "example",
        resourceType: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexExport, ModelsLexExportConfig

    def describe():
        ubx.intent("look up aws_models_lex_export")

        ubx.data(ModelsLexExport, "example", ModelsLexExportConfig(
            export_type="example",
            name="example",
            resource_type="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  Returned by the lookup.
</ResponseField>
