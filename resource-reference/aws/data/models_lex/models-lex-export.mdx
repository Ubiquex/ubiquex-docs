---
title: "aws_models_lex_export"
description: "`aws_models_lex_export` reads a real, exported Lex V1 bot, intent, or slot type definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_export"})

    		ubx.Data(models_lex.ModelsLexExport, "example", models_lex.ModelsLexExportConfig{
    			ExportType:   "example",
    			Name:         "example",
    			ResourceType: "example",
    			Version:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexExport } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_export";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_export" });

      data(ModelsLexExport, "example", {
        exportType: "example",
        name: "example",
        resourceType: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexExport, ModelsLexExportConfig

    def describe():
        ubx.intent("look up aws_models_lex_export")

        ubx.data(ModelsLexExport, "example", ModelsLexExportConfig(
            export_type="example",
            name="example",
            resource_type="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_type" type="string" required>
  The real, discriminating type of the export this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_status" type="string">
  The real, current status of the export this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
