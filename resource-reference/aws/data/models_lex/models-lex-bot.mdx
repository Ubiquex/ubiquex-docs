---
title: "aws_models_lex_bot"
description: "`aws_models_lex_bot` looks up a real, published Lex V1 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_bot"})

    		ubx.Data(models_lex.ModelsLexBot, "example", models_lex.ModelsLexBotConfig{
    			Name:           "example",
    			VersionOrAlias: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexBot } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_bot";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_bot" });

      data(ModelsLexBot, "example", {
        name: "example",
        versionOrAlias: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBot, ModelsLexBotConfig

    def describe():
        ubx.intent("look up aws_models_lex_bot")

        ubx.data(ModelsLexBot, "example", ModelsLexBotConfig(
            name="example",
            version_or_alias="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version_or_alias" type="string" required>
  The real version or alias this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="abort_statement" type="object">
  &lt;p&gt;A collection of messages that convey information to the user. At runtime, Amazon Lex selects the message to convey. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="messages" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="response_card" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="child_directed" type="bool">
  Whether this object is directed at children under 13, per COPPA. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="clarification_prompt" type="object">
  &lt;p&gt;Obtains information from the user. To define a prompt, provide one or more messages and specify the number of attempts to get information from the user. If you provide more than one message, Amazon Lex chooses one of the messages to use to prompt the user. For more information, see &lt;a&gt;how-it-works&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="max_attempts" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="messages" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="response_card" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="detect_sentiment" type="bool">
  Whether sentiment analysis is performed for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="enable_model_improvements" type="bool">
  Whether automatic model improvements are enabled for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="idle_session_ttlin_seconds" type="number">
  The real, maximum idle session duration before expiry, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="intents" type="list(object)">
  The real Amazon Lex intents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="nlu_intent_confidence_threshold" type="number">
  The real, minimum confidence score required for Amazon Lex to recognize an intent. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="voice_id" type="string">
  The real, unique identifier of the voice this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
