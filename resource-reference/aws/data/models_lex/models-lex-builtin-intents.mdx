---
title: "aws_models_lex_builtin_intents"
description: "`aws_models_lex_builtin_intents` reads the real, built-in intents Lex V1 provides."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_builtin_intents"})

    		ubx.Data(models_lex.ModelsLexBuiltinIntents, "example", models_lex.ModelsLexBuiltinIntentsConfig{
    			Locale:            "example",
    			MaxResults:        1,
    			SignatureContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexBuiltinIntents } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_builtin_intents.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_builtin_intents" });

      data(ModelsLexBuiltinIntents, "example", {
        locale: "example",
        maxResults: 1,
        signatureContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBuiltinIntents, ModelsLexBuiltinIntentsConfig

    def describe():
        ubx.intent("look up aws_models_lex_builtin_intents")

        ubx.data(ModelsLexBuiltinIntents, "example", ModelsLexBuiltinIntentsConfig(
            locale="example",
            max_results=1,
            signature_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="signature_contains" type="string">
  Restricts results to objects whose signature contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="intents" type="list(object)">
  The real Amazon Lex intents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
