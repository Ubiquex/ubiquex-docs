---
title: "aws_models_lex_bot_alias"
description: "`aws_models_lex_bot_alias` looks up a real Lex V1 bot alias, a named, versioned pointer used to deploy a bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_bot_alias"})

    		ubx.Data(models_lex.ModelsLexBotAlias, "example", models_lex.ModelsLexBotAliasConfig{
    			BotName: "example-bot-name",
    			Name:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexBotAlias } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_bot_alias.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_bot_alias" });

      data(ModelsLexBotAlias, "example", {
        botName: "example-bot-name",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBotAlias, ModelsLexBotAliasConfig

    def describe():
        ubx.intent("look up aws_models_lex_bot_alias")

        ubx.data(ModelsLexBotAlias, "example", ModelsLexBotAliasConfig(
            bot_name="example-bot-name",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_name" type="string" required>
  The real name of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_version" type="string">
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="conversation_logs" type="object">
  &lt;p&gt;Contains information about conversation log settings.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="iam_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="log_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
