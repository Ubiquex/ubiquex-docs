---
title: "aws_models_lex_migrations"
description: "`aws_models_lex_migrations` reads the real Lex V1-to-V2 bot migrations on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_migrations"})

    		ubx.Data(models_lex.ModelsLexMigrations, "example", models_lex.ModelsLexMigrationsConfig{
    			MaxResults:            1,
    			MigrationStatusEquals: "example",
    			SortByAttribute:       "example",
    			SortByOrder:           "example",
    			V1BotNameContains:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexMigrations } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_migrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_migrations" });

      data(ModelsLexMigrations, "example", {
        maxResults: 1,
        migrationStatusEquals: "example",
        sortByAttribute: "example",
        sortByOrder: "example",
        v1BotNameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexMigrations, ModelsLexMigrationsConfig

    def describe():
        ubx.intent("look up aws_models_lex_migrations")

        ubx.data(ModelsLexMigrations, "example", ModelsLexMigrationsConfig(
            max_results=1,
            migration_status_equals="example",
            sort_by_attribute="example",
            sort_by_order="example",
            v1_bot_name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="migration_status_equals" type="string">
  Restricts results to this exact migration status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by_attribute" type="string">
  The real attribute results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by_order" type="string">
  The real order results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="v1_bot_name_contains" type="string">
  Restricts results to Amazon Lex V1 bots whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="migration_summaries" type="list(object)">
  The real, summarized migrations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
