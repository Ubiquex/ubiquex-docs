---
title: "aws_models_lex_import"
description: "`aws_models_lex_import` reads the real, current status of importing a Lex V1 bot, intent, or slot type definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_import"})

    		ubx.Data(models_lex.ModelsLexImport, "example", models_lex.ModelsLexImportConfig{
    			ImportId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexImport } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_import.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_import" });

      data(ModelsLexImport, "example", {
        importId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexImport, ModelsLexImportConfig

    def describe():
        ubx.intent("look up aws_models_lex_import")

        ubx.data(ModelsLexImport, "example", ModelsLexImportConfig(
            import_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="import_id" type="string" required>
  The real, unique identifier of the import this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="list(string)">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_status" type="string">
  The real, current status of the import. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="merge_strategy" type="string">
  The real strategy used to merge this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
