---
title: "aws_models_lex_bot_channel_associations"
description: "`aws_models_lex_bot_channel_associations` reads the real messaging platform channels a Lex V1 bot is connected to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_bot_channel_associations"})

    		ubx.Data(models_lex.ModelsLexBotChannelAssociations, "example", models_lex.ModelsLexBotChannelAssociationsConfig{
    			BotAlias:     "example",
    			BotName:      "example-bot-name",
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexBotChannelAssociations } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_bot_channel_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_bot_channel_associations" });

      data(ModelsLexBotChannelAssociations, "example", {
        botAlias: "example",
        botName: "example-bot-name",
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBotChannelAssociations, ModelsLexBotChannelAssociationsConfig

    def describe():
        ubx.intent("look up aws_models_lex_bot_channel_associations")

        ubx.data(ModelsLexBotChannelAssociations, "example", ModelsLexBotChannelAssociationsConfig(
            bot_alias="example",
            bot_name="example-bot-name",
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_alias" type="string" required>
  The real Amazon Lex bot alias this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_name" type="string" required>
  The real name of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_channel_associations" type="list(object)">
  The real associations between this Amazon Lex bot and its own channels. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
