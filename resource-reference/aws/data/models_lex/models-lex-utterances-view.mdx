---
title: "aws_models_lex_utterances_view"
description: "`aws_models_lex_utterances_view` reads real, aggregated utterance data reported by a Lex V1 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_utterances_view"})

    		ubx.Data(models_lex.ModelsLexUtterancesView, "example", models_lex.ModelsLexUtterancesViewConfig{
    			BotName:     "example-bot-name",
    			BotVersions: []string{"example"},
    			StatusType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexUtterancesView } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_utterances_view.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_utterances_view" });

      data(ModelsLexUtterancesView, "example", {
        botName: "example-bot-name",
        botVersions: ["example"],
        statusType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexUtterancesView, ModelsLexUtterancesViewConfig

    def describe():
        ubx.intent("look up aws_models_lex_utterances_view")

        ubx.data(ModelsLexUtterancesView, "example", ModelsLexUtterancesViewConfig(
            bot_name="example-bot-name",
            bot_versions=["example"],
            status_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_name" type="string" required>
  The real name of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_versions" type="list(string)" required>
  The real versions of the bot included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status_type" type="string" required>
  The real, discriminating type of the status this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="utterances" type="list(object)">
  The real Amazon Lex utterances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
