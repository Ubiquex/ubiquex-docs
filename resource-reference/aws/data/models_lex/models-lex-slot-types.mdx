---
title: "aws_models_lex_slot_types"
description: "`aws_models_lex_slot_types` reads the real Lex V1 slot types on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_slot_types"})

    		ubx.Data(models_lex.ModelsLexSlotTypes, "example", models_lex.ModelsLexSlotTypesConfig{
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexSlotTypes } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_slot_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_slot_types" });

      data(ModelsLexSlotTypes, "example", {
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexSlotTypes, ModelsLexSlotTypesConfig

    def describe():
        ubx.intent("look up aws_models_lex_slot_types")

        ubx.data(ModelsLexSlotTypes, "example", ModelsLexSlotTypesConfig(
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slot_types" type="list(object)">
  The real, discriminating types of slots this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
