---
title: "aws_models_lex_bot_channel_association"
description: "`aws_models_lex_bot_channel_association` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_lex_bot_channel_association` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_bot_channel_association"})

    		ubx.Data(models_lex.ModelsLexBotChannelAssociation, "example", models_lex.ModelsLexBotChannelAssociationConfig{
    			BotAlias: "example",
    			BotName:  "example-bot-name",
    			Name:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexBotChannelAssociation } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_bot_channel_association";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_bot_channel_association" });

      data(ModelsLexBotChannelAssociation, "example", {
        botAlias: "example",
        botName: "example-bot-name",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBotChannelAssociation, ModelsLexBotChannelAssociationConfig

    def describe():
        ubx.intent("look up aws_models_lex_bot_channel_association")

        ubx.data(ModelsLexBotChannelAssociation, "example", ModelsLexBotChannelAssociationConfig(
            bot_alias="example",
            bot_name="example-bot-name",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_alias" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bot_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_configuration" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Returned by the lookup.
</ResponseField>
