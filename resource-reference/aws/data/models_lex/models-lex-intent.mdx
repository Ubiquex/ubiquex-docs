---
title: "aws_models_lex_intent"
description: "`aws_models_lex_intent` looks up a real Lex V1 intent, the user goal a conversation is designed to fulfill."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_intent"})

    		ubx.Data(models_lex.ModelsLexIntent, "example", models_lex.ModelsLexIntentConfig{
    			Name:    "example",
    			Version: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexIntent } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_intent";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_intent" });

      data(ModelsLexIntent, "example", {
        name: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexIntent, ModelsLexIntentConfig

    def describe():
        ubx.intent("look up aws_models_lex_intent")

        ubx.data(ModelsLexIntent, "example", ModelsLexIntentConfig(
            name="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="conclusion_statement" type="object">
  &lt;p&gt;A collection of messages that convey information to the user. At runtime, Amazon Lex selects the message to convey. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="messages" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="response_card" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="confirmation_prompt" type="object">
  &lt;p&gt;Obtains information from the user. To define a prompt, provide one or more messages and specify the number of attempts to get information from the user. If you provide more than one message, Amazon Lex chooses one of the messages to use to prompt the user. For more information, see &lt;a&gt;how-it-works&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="max_attempts" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="messages" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="response_card" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dialog_code_hook" type="object">
  &lt;p&gt;Specifies a Lambda function that verifies requests to a bot or fulfills the user's request to a bot..&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="message_version" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="uri" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="follow_up_prompt" type="object">
  &lt;p&gt;A prompt for additional activity after an intent is fulfilled. For example, after the &lt;code&gt;OrderPizza&lt;/code&gt; intent is fulfilled, you might prompt the user to find out whether the user wants to order drinks.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="prompt" type="object" required>
      &lt;p&gt;Obtains information from the user. To define a prompt, provide one or more messages and specify the number of attempts to get information from the user. If you provide more than one message, Amazon Lex chooses one of the messages to use to prompt the user. For more information, see &lt;a&gt;how-it-works&lt;/a&gt;.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="max_attempts" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="messages" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="response_card" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rejection_statement" type="object" required>
      &lt;p&gt;A collection of messages that convey information to the user. At runtime, Amazon Lex selects the message to convey. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="messages" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="response_card" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fulfillment_activity" type="object">
  &lt;p&gt; Describes how the intent is fulfilled after the user provides all of the information required for the intent. You can provide a Lambda function to process the intent, or you can return the intent information to the client application. We recommend that you use a Lambda function so that the relevant logic lives in the Cloud and limit the client-side code primarily to presentation. If you need to update the logic, you only update the Lambda function; you don't need to upgrade your client application. &lt;/p&gt; &lt;p&gt;Consider the following examples:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;In a pizza ordering application, after the user provides all of the information for placing an order, you use a Lambda function to place an order with a pizzeria. &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;In a gaming application, when a user says "pick up a rock," this information must go back to the client application so that it can perform the operation and update the graphics. In this case, you want Amazon Lex to return the intent data to the client. &lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code_hook" type="object">
      &lt;p&gt;Specifies a Lambda function that verifies requests to a bot or fulfills the user's request to a bot..&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="message_version" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="uri" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="input_contexts" type="list(object)">
  The real, structured input contexts of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="kendra_configuration" type="object">
  &lt;p&gt;Provides configuration information for the AMAZON.KendraSearchIntent intent. When you use this intent, Amazon Lex searches the specified Amazon Kendra index and returns documents from the index that match the user's utterance. For more information, see &lt;a href="http://docs.aws.amazon.com/lex/latest/dg/built-in-intent-kendra-search.html"&gt; AMAZON.KendraSearchIntent&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kendra_index" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="query_filter_string" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="role" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="output_contexts" type="list(object)">
  The real, structured output contexts of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parent_intent_signature" type="string">
  The real, unique signature of the parent Amazon Lex intent. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rejection_statement" type="object">
  &lt;p&gt;A collection of messages that convey information to the user. At runtime, Amazon Lex selects the message to convey. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="messages" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="response_card" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sample_utterances" type="list(string)">
  The real, sample Amazon Lex utterances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slots" type="list(object)">
  The real Amazon Lex slots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
