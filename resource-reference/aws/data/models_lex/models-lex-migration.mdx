---
title: "aws_models_lex_migration"
description: "`aws_models_lex_migration` reads the real, current status of migrating a Lex V1 bot to Lex V2."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_migration"})

    		ubx.Data(models_lex.ModelsLexMigration, "example", models_lex.ModelsLexMigrationConfig{
    			MigrationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexMigration } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_migration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_migration" });

      data(ModelsLexMigration, "example", {
        migrationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexMigration, ModelsLexMigrationConfig

    def describe():
        ubx.intent("look up aws_models_lex_migration")

        ubx.data(ModelsLexMigration, "example", ModelsLexMigrationConfig(
            migration_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="migration_id" type="string" required>
  The real, unique identifier of the migration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alerts" type="list(object)">
  The real alerts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="migration_status" type="string">
  The real, current status of the migration this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="migration_strategy" type="string">
  The real, recommended migration strategy for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="migration_timestamp" type="string">
  The real timestamp of the migration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="v1_bot_locale" type="string">
  The real locale of this Amazon Lex V1 bot. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="v1_bot_name" type="string">
  The real name of the v1 bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="v1_bot_version" type="string">
  The real version of the v1 bot. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="v2_bot_id" type="string">
  The real, unique identifier of the v2 bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="v2_bot_role" type="string">
  The real IAM role of the v2 bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
