---
title: "aws_models_lex_bot_aliases"
description: "`aws_models_lex_bot_aliases` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_lex_bot_aliases` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_bot_aliases"})

    		ubx.Data(models_lex.ModelsLexBotAliases, "example", models_lex.ModelsLexBotAliasesConfig{
    			BotName:      "example-bot-name",
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexBotAliases } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_bot_aliases";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_bot_aliases" });

      data(ModelsLexBotAliases, "example", {
        botName: "example-bot-name",
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexBotAliases, ModelsLexBotAliasesConfig

    def describe():
        ubx.intent("look up aws_models_lex_bot_aliases")

        ubx.data(ModelsLexBotAliases, "example", ModelsLexBotAliasesConfig(
            bot_name="example-bot-name",
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_aliases" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
