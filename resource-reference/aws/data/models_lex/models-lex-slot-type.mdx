---
title: "aws_models_lex_slot_type"
description: "`aws_models_lex_slot_type` looks up a real Lex V1 slot type, the set of valid values a slot can accept."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_slot_type"})

    		ubx.Data(models_lex.ModelsLexSlotType, "example", models_lex.ModelsLexSlotTypeConfig{
    			Name:    "example",
    			Version: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsLexSlotType } from "./local-sdk/aws/sdk/typescript/aws/data/models_lex/models_lex_slot_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_slot_type" });

      data(ModelsLexSlotType, "example", {
        name: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexSlotType, ModelsLexSlotTypeConfig

    def describe():
        ubx.intent("look up aws_models_lex_slot_type")

        ubx.data(ModelsLexSlotType, "example", ModelsLexSlotTypeConfig(
            name="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="enumeration_values" type="list(object)">
  The real, enumerated values allowed for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parent_slot_type_signature" type="string">
  The real, unique signature of the parent Amazon Lex slot type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slot_type_configurations" type="list(object)">
  The real Amazon Lex slot type configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="value_selection_strategy" type="string">
  The real strategy used to select a value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
