---
title: "aws_models_lex_slot_type"
description: "`aws_models_lex_slot_type` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_lex_slot_type` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_lex_slot_type"})

    		ubx.Data(models_lex.ModelsLexSlotType, "example", models_lex.ModelsLexSlotTypeConfig{
    			Name:    "example",
    			Version: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsLexSlotType } from "@ubx/sdk-aws/aws/data/models_lex/models_lex_slot_type";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_lex_slot_type" });

      data(ModelsLexSlotType, "example", {
        name: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_lex import ModelsLexSlotType, ModelsLexSlotTypeConfig

    def describe():
        ubx.intent("look up aws_models_lex_slot_type")

        ubx.data(ModelsLexSlotType, "example", ModelsLexSlotTypeConfig(
            name="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="checksum" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="enumeration_values" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parent_slot_type_signature" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="slot_type_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value_selection_strategy" type="string">
  Returned by the lookup.
</ResponseField>
