---
title: "aws_globalaccelerator_custom_routing_accelerators"
description: "`aws_globalaccelerator_custom_routing_accelerators` looks up a real Global Accelerator custom routing accelerator..."
---

`aws_globalaccelerator_custom_routing_accelerators` looks up a real Global Accelerator custom routing accelerator, mapping fixed IPs directly to specific destination ports and IPs in a VPC.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_custom_routing_accelerators"})

    		ubx.Data(globalaccelerator.CustomRoutingAccelerators, "example", globalaccelerator.CustomRoutingAcceleratorsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CustomRoutingAccelerators } from "./local-sdk/aws/sdk/typescript/aws/data/globalaccelerator/custom_routing_accelerators.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_globalaccelerator_custom_routing_accelerators",
      });

      data(CustomRoutingAccelerators, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CustomRoutingAccelerators, CustomRoutingAcceleratorsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_custom_routing_accelerators")

        ubx.data(CustomRoutingAccelerators, "example", CustomRoutingAcceleratorsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accelerators" type="list(object)">
  The real AWS Global Accelerator accelerators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
