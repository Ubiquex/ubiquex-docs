---
title: "aws_globalaccelerator_cross_account_resources"
description: "`aws_globalaccelerator_cross_account_resources` reads the real cross-account endpoints shared with a Global Accelerator via AWS RAM."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_cross_account_resources"})

    		ubx.Data(globalaccelerator.CrossAccountResources, "example", globalaccelerator.CrossAccountResourcesConfig{
    			ResourceOwnerAwsAccountId: "example",
    			AcceleratorArn:            "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CrossAccountResources } from "@ubx/sdk-aws/aws/data/globalaccelerator/cross_account_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_globalaccelerator_cross_account_resources" });

      data(CrossAccountResources, "example", {
        resourceOwnerAwsAccountId: "example",
        acceleratorArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CrossAccountResources, CrossAccountResourcesConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_cross_account_resources")

        ubx.data(CrossAccountResources, "example", CrossAccountResourcesConfig(
            resource_owner_aws_account_id="example",
            accelerator_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accelerator_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Global Accelerator this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_owner_aws_account_id" type="string" required>
  The real, unique identifier of the resource owner aws account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cross_account_resources" type="list(object)">
  The real, cross-account resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
