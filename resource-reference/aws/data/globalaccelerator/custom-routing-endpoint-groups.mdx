---
title: "aws_globalaccelerator_custom_routing_endpoint_groups"
description: "`aws_globalaccelerator_custom_routing_endpoint_groups` looks up a real Global Accelerator custom routing endpoint group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_custom_routing_endpoint_groups"})

    		ubx.Data(globalaccelerator.CustomRoutingEndpointGroups, "example", globalaccelerator.CustomRoutingEndpointGroupsConfig{
    			ListenerArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomRoutingEndpointGroups } from "@ubx/sdk-aws/aws/data/globalaccelerator/custom_routing_endpoint_groups";

    export default stack("example", () => {
      intent({
        summary: "look up aws_globalaccelerator_custom_routing_endpoint_groups",
      });

      data(CustomRoutingEndpointGroups, "example", {
        listenerArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CustomRoutingEndpointGroups, CustomRoutingEndpointGroupsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_custom_routing_endpoint_groups")

        ubx.data(CustomRoutingEndpointGroups, "example", CustomRoutingEndpointGroupsConfig(
            listener_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="listener_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the listener this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="endpoint_groups" type="list(object)">
  The real endpoint groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
