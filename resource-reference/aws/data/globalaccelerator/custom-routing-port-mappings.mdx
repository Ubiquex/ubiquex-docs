---
title: "aws_globalaccelerator_custom_routing_port_mappings"
description: "`aws_globalaccelerator_custom_routing_port_mappings` reads the real port mappings of a Global Accelerator custom routing endpoint group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_custom_routing_port_mappings"})

    		ubx.Data(globalaccelerator.CustomRoutingPortMappings, "example", globalaccelerator.CustomRoutingPortMappingsConfig{
    			AcceleratorArn:   "arn:aws:iam::123456789012:policy/example",
    			EndpointGroupArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CustomRoutingPortMappings } from "./local-sdk/aws/sdk/typescript/aws/data/globalaccelerator/custom_routing_port_mappings.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_globalaccelerator_custom_routing_port_mappings",
      });

      data(CustomRoutingPortMappings, "example", {
        acceleratorArn: "arn:aws:iam::123456789012:policy/example",
        endpointGroupArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CustomRoutingPortMappings, CustomRoutingPortMappingsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_custom_routing_port_mappings")

        ubx.data(CustomRoutingPortMappings, "example", CustomRoutingPortMappingsConfig(
            accelerator_arn="arn:aws:iam::123456789012:policy/example",
            endpoint_group_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accelerator_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the AWS Global Accelerator this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="endpoint_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the endpoint group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="port_mappings" type="list(object)">
  The real port mappings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
