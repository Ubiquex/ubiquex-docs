---
title: "aws_globalaccelerator_cross_account_attachments"
description: "`data_aws_globalaccelerator_cross_account_attachments` looks up a real, already-existing `aws_global_accelerator_cross_account_attachment`."
---

It authorizes a real, specific set of endpoint resources owned by another AWS account to be added to this account's own `aws_global_accelerator_accelerator`, a real cross-account grant rather than every accelerator needing to live in the same account as every endpoint it targets.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_cross_account_attachments"})

    		ubx.Data(globalaccelerator.CrossAccountAttachments, "example", globalaccelerator.CrossAccountAttachmentsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CrossAccountAttachments } from "@ubx/sdk-aws/aws/data/globalaccelerator/cross_account_attachments";

    export default stack("example", () => {
      intent({
        summary: "look up aws_globalaccelerator_cross_account_attachments",
      });

      data(CrossAccountAttachments, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CrossAccountAttachments, CrossAccountAttachmentsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_cross_account_attachments")

        ubx.data(CrossAccountAttachments, "example", CrossAccountAttachmentsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cross_account_attachments" type="list(object)">
  The real cross-account attachments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
