---
title: "aws_globalaccelerator_cross_account_resource_accounts"
description: "`aws_globalaccelerator_cross_account_resource_accounts` reads the real accounts that have shared endpoints with a Global Accelerator via AWS RAM."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_cross_account_resource_accounts"})

    		ubx.Data(globalaccelerator.CrossAccountResourceAccounts, "example", globalaccelerator.CrossAccountResourceAccountsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CrossAccountResourceAccounts } from "./local-sdk/aws/sdk/typescript/aws/data/globalaccelerator/cross_account_resource_accounts.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_globalaccelerator_cross_account_resource_accounts",
      });

      data(CrossAccountResourceAccounts, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CrossAccountResourceAccounts, CrossAccountResourceAccountsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_cross_account_resource_accounts")

        ubx.data(CrossAccountResourceAccounts, "example", CrossAccountResourceAccountsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="resource_owner_aws_account_ids" type="list(string)">
  The real, unique identifiers of the resource owner aws accounts this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
