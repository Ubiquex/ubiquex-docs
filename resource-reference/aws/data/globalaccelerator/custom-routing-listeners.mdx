---
title: "aws_globalaccelerator_custom_routing_listeners"
description: "`aws_globalaccelerator_custom_routing_listeners` looks up a real Global Accelerator custom routing listener."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_custom_routing_listeners"})

    		ubx.Data(globalaccelerator.CustomRoutingListeners, "example", globalaccelerator.CustomRoutingListenersConfig{
    			AcceleratorArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomRoutingListeners } from "@ubx/sdk-aws/aws/data/globalaccelerator/custom_routing_listeners";

    export default stack("example", () => {
      intent({ summary: "look up aws_globalaccelerator_custom_routing_listeners" });

      data(CustomRoutingListeners, "example", {
        acceleratorArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import CustomRoutingListeners, CustomRoutingListenersConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_custom_routing_listeners")

        ubx.data(CustomRoutingListeners, "example", CustomRoutingListenersConfig(
            accelerator_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accelerator_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the AWS Global Accelerator this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="listeners" type="list(object)">
  The real load balancer listeners included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
