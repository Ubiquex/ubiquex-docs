---
title: "aws_globalaccelerator_accelerators"
description: "`data_aws_globalaccelerator_accelerators` looks up a real, already-existing `aws_global_accelerator_accelerator`."
---

It creates two real, static anycast IP addresses that route traffic over AWS's own global network backbone to the closest healthy endpoint, real, consistent public entry points independent of the endpoints' own Regions or IP addresses changing behind them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_globalaccelerator_accelerators"})

    		ubx.Data(globalaccelerator.Accelerators, "example", globalaccelerator.AcceleratorsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Accelerators } from "./local-sdk/aws/sdk/typescript/aws/data/globalaccelerator/accelerators.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_globalaccelerator_accelerators" });

      data(Accelerators, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.globalaccelerator import Accelerators, AcceleratorsConfig

    def describe():
        ubx.intent("look up aws_globalaccelerator_accelerators")

        ubx.data(Accelerators, "example", AcceleratorsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accelerators" type="list(object)">
  The real AWS Global Accelerator accelerators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
