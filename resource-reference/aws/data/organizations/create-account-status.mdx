---
title: "aws_organizations_create_account_status"
description: "`aws_organizations_create_account_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_organizations_create_account_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_create_account_status"})

    		ubx.Data(organizations.CreateAccountStatus, "example", organizations.CreateAccountStatusConfig{
    			CreateAccountRequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CreateAccountStatus } from "@ubx/sdk-aws/aws/data/organizations/create_account_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_create_account_status" });

      data(CreateAccountStatus, "example", {
        createAccountRequestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import CreateAccountStatus, CreateAccountStatusConfig

    def describe():
        ubx.intent("look up aws_organizations_create_account_status")

        ubx.data(CreateAccountStatus, "example", CreateAccountStatusConfig(
            create_account_request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="create_account_request_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_account_status" type="object">
  &lt;p&gt;Contains the status about a &lt;a&gt;CreateAccount&lt;/a&gt; or &lt;a&gt;CreateGovCloudAccount&lt;/a&gt; request to create an Amazon Web Services account or an Amazon Web Services GovCloud (US) account in an organization.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="account_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="completed_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="failure_reason" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="gov_cloud_account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="requested_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
