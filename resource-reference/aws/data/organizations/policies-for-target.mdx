---
title: "aws_organizations_policies_for_target"
description: "`aws_organizations_policies_for_target` reads the real AWS Organizations policies attached to a given account, OU, or root."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_policies_for_target"})

    		ubx.Data(organizations.PoliciesForTarget, "example", organizations.PoliciesForTargetConfig{
    			Filter:     "example",
    			TargetId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PoliciesForTarget } from "@ubx/sdk-aws/aws/data/organizations/policies_for_target";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_policies_for_target" });

      data(PoliciesForTarget, "example", {
        filter: "example",
        targetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import PoliciesForTarget, PoliciesForTargetConfig

    def describe():
        ubx.intent("look up aws_organizations_policies_for_target")

        ubx.data(PoliciesForTarget, "example", PoliciesForTargetConfig(
            filter="example",
            target_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string" required>
  A real filter expression restricting which results are returned. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  The real, unique identifier of the target this action applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policies" type="list(object)">
  The real access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
