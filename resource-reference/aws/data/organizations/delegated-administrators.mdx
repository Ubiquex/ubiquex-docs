---
title: "aws_organizations_delegated_administrators"
description: "`aws_organizations_delegated_administrators` reads the real accounts registered as a delegated administrator for an AWS service within an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_delegated_administrators"})

    		ubx.Data(organizations.DelegatedAdministrators, "example", organizations.DelegatedAdministratorsConfig{
    			MaxResults:       1,
    			ServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DelegatedAdministrators } from "@ubx/sdk-aws/aws/data/organizations/delegated_administrators";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_delegated_administrators" });

      data(DelegatedAdministrators, "example", {
        maxResults: 1,
        servicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import DelegatedAdministrators, DelegatedAdministratorsConfig

    def describe():
        ubx.intent("look up aws_organizations_delegated_administrators")

        ubx.data(DelegatedAdministrators, "example", DelegatedAdministratorsConfig(
            max_results=1,
            service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_principal" type="string">
  The real AWS service principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delegated_administrators" type="list(object)">
  The real, delegated administrators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
