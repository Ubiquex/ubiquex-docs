---
title: "aws_organizations_handshakes_for_organization"
description: "`aws_organizations_handshakes_for_organization` reads the real AWS Organizations handshakes involving a given organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_handshakes_for_organization"})

    		ubx.Data(organizations.HandshakesForOrganization, "example", organizations.HandshakesForOrganizationConfig{
    			Filter:     map[string]any{"action_type": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HandshakesForOrganization } from "@ubx/sdk-aws/aws/data/organizations/handshakes_for_organization";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_handshakes_for_organization" });

      data(HandshakesForOrganization, "example", {
        filter: { actionType: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import HandshakesForOrganization, HandshakesForOrganizationConfig

    def describe():
        ubx.intent("look up aws_organizations_handshakes_for_organization")

        ubx.data(HandshakesForOrganization, "example", HandshakesForOrganizationConfig(
            filter={"action_type": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Contains the filter used to select the handshakes for an operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="action_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent_handshake_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="handshakes" type="list(object)">
  The real AWS Organizations handshakes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
