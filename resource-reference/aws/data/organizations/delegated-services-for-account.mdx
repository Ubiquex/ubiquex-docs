---
title: "aws_organizations_delegated_services_for_account"
description: "`aws_organizations_delegated_services_for_account` reads the real AWS services a given account is registered as a delegated administrator for."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_delegated_services_for_account"})

    		ubx.Data(organizations.DelegatedServicesForAccount, "example", organizations.DelegatedServicesForAccountConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DelegatedServicesForAccount } from "@ubx/sdk-aws/aws/data/organizations/delegated_services_for_account";

    export default stack("example", () => {
      intent({
        summary: "look up aws_organizations_delegated_services_for_account",
      });

      data(DelegatedServicesForAccount, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import DelegatedServicesForAccount, DelegatedServicesForAccountConfig

    def describe():
        ubx.intent("look up aws_organizations_delegated_services_for_account")

        ubx.data(DelegatedServicesForAccount, "example", DelegatedServicesForAccountConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delegated_services" type="list(object)">
  The real AWS services with a delegated administrator for this account. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
