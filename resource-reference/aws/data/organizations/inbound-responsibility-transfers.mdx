---
title: "aws_organizations_inbound_responsibility_transfers"
description: "`aws_organizations_inbound_responsibility_transfers` reads real, incoming account responsibility transfer requests within an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_inbound_responsibility_transfers"})

    		ubx.Data(organizations.InboundResponsibilityTransfers, "example", organizations.InboundResponsibilityTransfersConfig{
    			Type:       "example",
    			Id:         "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InboundResponsibilityTransfers } from "@ubx/sdk-aws/aws/data/organizations/inbound_responsibility_transfers";

    export default stack("example", () => {
      intent({
        summary: "look up aws_organizations_inbound_responsibility_transfers",
      });

      data(InboundResponsibilityTransfers, "example", {
        type: "example",
        id: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import InboundResponsibilityTransfers, InboundResponsibilityTransfersConfig

    def describe():
        ubx.intent("look up aws_organizations_inbound_responsibility_transfers")

        ubx.data(InboundResponsibilityTransfers, "example", InboundResponsibilityTransfersConfig(
            type="example",
            id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  The real, discriminating type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="responsibility_transfers" type="list(object)">
  The real, transferred security responsibilities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
