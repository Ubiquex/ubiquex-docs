---
title: "aws_organizations_parents"
description: "`aws_organizations_parents` reads the real, immediate parent organizational unit or root of a given account or OU."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_parents"})

    		ubx.Data(organizations.Parents, "example", organizations.ParentsConfig{
    			ChildId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Parents } from "@ubx/sdk-aws/aws/data/organizations/parents";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_parents" });

      data(Parents, "example", {
        childId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import Parents, ParentsConfig

    def describe():
        ubx.intent("look up aws_organizations_parents")

        ubx.data(Parents, "example", ParentsConfig(
            child_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="child_id" type="string" required>
  The real, unique identifier of the child this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parents" type="list(object)">
  The real parent objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
