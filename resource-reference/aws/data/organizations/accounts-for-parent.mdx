---
title: "aws_organizations_accounts_for_parent"
description: "`aws_organizations_accounts_for_parent` reads the real member accounts directly under a given organizational unit or root."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_accounts_for_parent"})

    		ubx.Data(organizations.AccountsForParent, "example", organizations.AccountsForParentConfig{
    			ParentId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountsForParent } from "./local-sdk/aws/sdk/typescript/aws/data/organizations/accounts_for_parent.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_accounts_for_parent" });

      data(AccountsForParent, "example", {
        parentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import AccountsForParent, AccountsForParentConfig

    def describe():
        ubx.intent("look up aws_organizations_accounts_for_parent")

        ubx.data(AccountsForParent, "example", AccountsForParentConfig(
            parent_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parent_id" type="string" required>
  The real, unique identifier of the parent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accounts" type="list(object)">
  The real AWS accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
