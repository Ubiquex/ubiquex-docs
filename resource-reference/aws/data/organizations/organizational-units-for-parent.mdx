---
title: "aws_organizations_organizational_units_for_parent"
description: "`data_aws_organizations_organizational_units_for_parent` looks up a real, already-existing `aws_organizations_organizational_unit`."
---

It creates a real container within an AWS Organization's hierarchy that groups `aws_organizations_account`s and other OUs, so `aws_organizations_policy` resources like service control policies can be attached once and apply to everything nested under it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_organizational_units_for_parent"})

    		ubx.Data(organizations.OrganizationalUnitsForParent, "example", organizations.OrganizationalUnitsForParentConfig{
    			ParentId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationalUnitsForParent } from "./local-sdk/aws/sdk/typescript/aws/data/organizations/organizational_units_for_parent.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_organizations_organizational_units_for_parent",
      });

      data(OrganizationalUnitsForParent, "example", {
        parentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import OrganizationalUnitsForParent, OrganizationalUnitsForParentConfig

    def describe():
        ubx.intent("look up aws_organizations_organizational_units_for_parent")

        ubx.data(OrganizationalUnitsForParent, "example", OrganizationalUnitsForParentConfig(
            parent_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parent_id" type="string" required>
  The real, unique identifier of the parent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organizational_units" type="list(object)">
  The real AWS Organizations organizational units included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
