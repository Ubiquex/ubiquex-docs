---
title: "aws_organizations_handshake"
description: "`aws_organizations_handshake` looks up one real AWS Organizations handshake, a real invitation or request awaiting acceptance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_organizations_handshake"})

    		ubx.Data(organizations.Handshake, "example", organizations.HandshakeConfig{
    			HandshakeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Handshake } from "./local-sdk/aws/sdk/typescript/aws/data/organizations/handshake.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_organizations_handshake" });

      data(Handshake, "example", {
        handshakeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.organizations import Handshake, HandshakeConfig

    def describe():
        ubx.intent("look up aws_organizations_handshake")

        ubx.data(Handshake, "example", HandshakeConfig(
            handshake_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="handshake_id" type="string" required>
  The real, unique identifier of the handshake this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="handshake" type="object">
  &lt;p&gt;Contains details for a handshake. A handshake is the secure exchange of information between two Amazon Web Services accounts: a sender and a recipient.&lt;/p&gt; &lt;p&gt; &lt;b&gt;Note:&lt;/b&gt; Handshakes that are &lt;code&gt;CANCELED&lt;/code&gt;, &lt;code&gt;ACCEPTED&lt;/code&gt;, &lt;code&gt;DECLINED&lt;/code&gt;, or &lt;code&gt;EXPIRED&lt;/code&gt; show up in lists for only 30 days after entering that state After that they are deleted.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expiration_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parties" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="requested_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resources" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
