---
title: "aws_a2i_runtime_sagemaker_human_loops"
description: "`aws_a2i_runtime_sagemaker_human_loops` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_a2i_runtime_sagemaker_human_loops` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	a2i_runtime_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/a2i_runtime_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_a2i_runtime_sagemaker_human_loops"})

    		ubx.Data(a2i_runtime_sagemaker.A2iRuntimeSagemakerHumanLoops, "example", a2i_runtime_sagemaker.A2iRuntimeSagemakerHumanLoopsConfig{
    			FlowDefinitionArn:  "arn:aws:iam::123456789012:policy/example",
    			CreationTimeAfter:  "example",
    			CreationTimeBefore: "example",
    			MaxResults:         1,
    			SortOrder:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { A2iRuntimeSagemakerHumanLoops } from "@ubx/sdk-aws/aws/data/a2i_runtime_sagemaker/a2i_runtime_sagemaker_human_loops";

    export default stack("example", () => {
      intent({ summary: "look up aws_a2i_runtime_sagemaker_human_loops" });

      data(A2iRuntimeSagemakerHumanLoops, "example", {
        flowDefinitionArn: "arn:aws:iam::123456789012:policy/example",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.a2i_runtime_sagemaker import A2iRuntimeSagemakerHumanLoops, A2iRuntimeSagemakerHumanLoopsConfig

    def describe():
        ubx.intent("look up aws_a2i_runtime_sagemaker_human_loops")

        ubx.data(A2iRuntimeSagemakerHumanLoops, "example", A2iRuntimeSagemakerHumanLoopsConfig(
            flow_definition_arn="arn:aws:iam::123456789012:policy/example",
            creation_time_after="example",
            creation_time_before="example",
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="flow_definition_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="human_loop_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
