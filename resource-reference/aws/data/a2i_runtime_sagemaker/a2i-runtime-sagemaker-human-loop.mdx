---
title: "aws_a2i_runtime_sagemaker_human_loop"
description: "`aws_a2i_runtime_sagemaker_human_loop` looks up one real Amazon Augmented AI (A2I) human review loop."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	a2i_runtime_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/a2i_runtime_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_a2i_runtime_sagemaker_human_loop"})

    		ubx.Data(a2i_runtime_sagemaker.A2iRuntimeSagemakerHumanLoop, "example", a2i_runtime_sagemaker.A2iRuntimeSagemakerHumanLoopConfig{
    			HumanLoopName: "example-human-loop-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { A2iRuntimeSagemakerHumanLoop } from "./local-sdk/aws/sdk/typescript/aws/data/a2i_runtime_sagemaker/a2i_runtime_sagemaker_human_loop.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_a2i_runtime_sagemaker_human_loop" });

      data(A2iRuntimeSagemakerHumanLoop, "example", {
        humanLoopName: "example-human-loop-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.a2i_runtime_sagemaker import A2iRuntimeSagemakerHumanLoop, A2iRuntimeSagemakerHumanLoopConfig

    def describe():
        ubx.intent("look up aws_a2i_runtime_sagemaker_human_loop")

        ubx.data(A2iRuntimeSagemakerHumanLoop, "example", A2iRuntimeSagemakerHumanLoopConfig(
            human_loop_name="example-human-loop-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="human_loop_name" type="string" required>
  The real name of the human loop this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_code" type="string">
  The real code identifying the reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="flow_definition_arn" type="string">
  The real Amazon Resource Name (ARN) of the flow definition this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="human_loop_arn" type="string">
  The real Amazon Resource Name (ARN) of the human loop this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="human_loop_output" type="object">
  &lt;p&gt;Information about where the human output will be stored.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="output_s3_uri" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="human_loop_status" type="string">
  The real, current status of the human loop this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
