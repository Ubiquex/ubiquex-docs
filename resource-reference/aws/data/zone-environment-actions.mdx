---
title: "aws_data_zone_environment_actions"
description: "`aws_data_zone_environment_actions` defines a real, custom action, a link to an external console or tool, available on an `aws_data_zone_environment`..."
---

`aws_data_zone_environment_actions` defines a real, custom action, a link to an external console or tool, available on an `aws_data_zone_environment`, extending what a data user can do directly from DataZone's own interface.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own zone environment actions"})

    		ubx.Resource(data.ZoneEnvironmentActions, "example", data.ZoneEnvironmentActionsConfig{
    			Name:                  "example",
    			Description:           "Managed by ubx.",
    			DomainIdentifier:      "example",
    			EnvironmentIdentifier: "example",
    			Identifier:            "example",
    			Parameters:            map[string]any{"uri": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneEnvironmentActions } from "./local-sdk/aws/sdk/typescript/aws/data/zone_environment_actions.ts";

    export default stack("example", () => {
      intent({ summary: "example own zone environment actions" });

      resource(ZoneEnvironmentActions, "example", {
        name: "example",
        description: "Managed by ubx.",
        domainIdentifier: "example",
        environmentIdentifier: "example",
        identifier: "example",
        parameters: { uri: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import ZoneEnvironmentActions, ZoneEnvironmentActionsConfig

    def describe():
        ubx.intent("example own zone environment actions")

        ubx.resource(ZoneEnvironmentActions, "example", ZoneEnvironmentActionsConfig(
            name="example",
            description="Managed by ubx.",
            domain_identifier="example",
            environment_identifier="example",
            identifier="example",
            parameters={"uri": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_environment_actions called "example", with
    description Managed by ubx., domain identifier example,
    environment identifier example, identifier example, parameters a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the Amazon DataZone environment action. Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string">
  The identifier of the Amazon DataZone domain in which the environment would be created. Optional.
</ResponseField>

<ResponseField name="environment_identifier" type="string">
  The identifier of the Amazon DataZone environment in which the action is taking place Optional.
</ResponseField>

<ResponseField name="identifier" type="string">
  The ID of the Amazon DataZone environment action. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the environment action. Required.
</ResponseField>

<ResponseField name="parameters" type="object">
  The parameters of the console link specified as part of the environment action Optional.
  <Expandable title="properties">
    <ResponseField name="uri" type="string">
      The URI of the console link specified as part of the environment action. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="domain_id" type="string">
  The identifier of the Amazon DataZone domain in which the environment is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="environment_id" type="string">
  The identifier of the Amazon DataZone environment in which the action is taking place Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the Amazon DataZone environment action. Computed by AWS after `ship`.
</ResponseField>
