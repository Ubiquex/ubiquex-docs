---
title: "aws_swf_workflow_types"
description: "`aws_swf_workflow_types` reads the real workflow types registered in an SWF domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	swf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/swf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_swf_workflow_types"})

    		ubx.Data(swf.WorkflowTypes, "example", swf.WorkflowTypesConfig{
    			Domain:             "example",
    			RegistrationStatus: "example",
    			Name:               "example",
    			MaximumPageSize:    1,
    			ReverseOrder:       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkflowTypes } from "@ubx/sdk-aws/aws/data/swf/workflow_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_swf_workflow_types" });

      data(WorkflowTypes, "example", {
        domain: "example",
        registrationStatus: "example",
        name: "example",
        maximumPageSize: 1,
        reverseOrder: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.swf import WorkflowTypes, WorkflowTypesConfig

    def describe():
        ubx.intent("look up aws_swf_workflow_types")

        ubx.data(WorkflowTypes, "example", WorkflowTypesConfig(
            domain="example",
            registration_status="example",
            name="example",
            maximum_page_size=1,
            reverse_order=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="maximum_page_size" type="number">
  The real size of the maximum page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_status" type="string" required>
  The real, current status of the registration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="reverse_order" type="bool">
  Whether results are returned in reverse order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_infos" type="list(object)">
  The real, detailed type information included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
