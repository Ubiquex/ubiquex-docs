---
title: "aws_swf_domain"
description: "`aws_swf_domain` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_swf_domain` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	swf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/swf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_swf_domain"})

    		ubx.Data(swf.Domain, "example", swf.DomainConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Domain } from "@ubx/sdk-aws/aws/data/swf/domain";

    export default stack("example", () => {
      intent({ summary: "look up aws_swf_domain" });

      data(Domain, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.swf import Domain, DomainConfig

    def describe():
        ubx.intent("look up aws_swf_domain")

        ubx.data(Domain, "example", DomainConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration" type="object">
  &lt;p&gt;Contains the configuration settings of a domain.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="workflow_execution_retention_period_in_days" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_info" type="object">
  &lt;p&gt;Contains general information about a domain.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
