---
title: "aws_swf_domains"
description: "`aws_swf_domains` reads the real SWF domains on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	swf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/swf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_swf_domains"})

    		ubx.Data(swf.Domains, "example", swf.DomainsConfig{
    			RegistrationStatus: "example",
    			MaximumPageSize:    1,
    			ReverseOrder:       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Domains } from "@ubx/sdk-aws/aws/data/swf/domains";

    export default stack("example", () => {
      intent({ summary: "look up aws_swf_domains" });

      data(Domains, "example", {
        registrationStatus: "example",
        maximumPageSize: 1,
        reverseOrder: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.swf import Domains, DomainsConfig

    def describe():
        ubx.intent("look up aws_swf_domains")

        ubx.data(Domains, "example", DomainsConfig(
            registration_status="example",
            maximum_page_size=1,
            reverse_order=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="maximum_page_size" type="number">
  The real size of the maximum page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_status" type="string" required>
  The real, current status of the registration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="reverse_order" type="bool">
  Whether results are returned in reverse order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_infos" type="list(object)">
  The real, detailed domain information included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
