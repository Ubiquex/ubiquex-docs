---
title: "aws_neptune_graph_queries"
description: "`aws_neptune_graph_queries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_graph_queries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_graph "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_graph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_graph_queries"})

    		ubx.Data(neptune_graph.NeptuneGraphQueries, "example", neptune_graph.NeptuneGraphQueriesConfig{
    			GraphIdentifier: "example",
    			MaxResults:      1,
    			State:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneGraphQueries } from "@ubx/sdk-aws/aws/data/neptune_graph/neptune_graph_queries";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_graph_queries" });

      data(NeptuneGraphQueries, "example", {
        graphIdentifier: "example",
        maxResults: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_graph import NeptuneGraphQueries, NeptuneGraphQueriesConfig

    def describe():
        ubx.intent("look up aws_neptune_graph_queries")

        ubx.data(NeptuneGraphQueries, "example", NeptuneGraphQueriesConfig(
            graph_identifier="example",
            max_results=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="graph_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="queries" type="list(object)">
  Returned by the lookup.
</ResponseField>
