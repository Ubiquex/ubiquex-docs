---
title: "aws_neptune_graph_queries"
description: "`aws_neptune_graph_queries` reads the real, currently running queries on a Neptune Analytics graph."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_graph "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_graph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_graph_queries"})

    		ubx.Data(neptune_graph.NeptuneGraphQueries, "example", neptune_graph.NeptuneGraphQueriesConfig{
    			GraphIdentifier: "example",
    			MaxResults:      1,
    			State:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneGraphQueries } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_graph/neptune_graph_queries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_graph_queries" });

      data(NeptuneGraphQueries, "example", {
        graphIdentifier: "example",
        maxResults: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_graph import NeptuneGraphQueries, NeptuneGraphQueriesConfig

    def describe():
        ubx.intent("look up aws_neptune_graph_queries")

        ubx.data(NeptuneGraphQueries, "example", NeptuneGraphQueriesConfig(
            graph_identifier="example",
            max_results=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="graph_identifier" type="string" required>
  The real, unique identifier of the graph this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="queries" type="list(object)">
  The real queries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
