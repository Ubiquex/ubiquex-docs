---
title: "aws_neptune_graph_query"
description: "`aws_neptune_graph_query` reads the real, current status of a specific query running on a Neptune Analytics graph."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_graph "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/neptune_graph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_graph_query"})

    		ubx.Data(neptune_graph.NeptuneGraphQuery, "example", neptune_graph.NeptuneGraphQueryConfig{
    			GraphIdentifier: "example",
    			QueryId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneGraphQuery } from "@ubx/sdk-aws/aws/data/neptune_graph/neptune_graph_query";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_graph_query" });

      data(NeptuneGraphQuery, "example", {
        graphIdentifier: "example",
        queryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_graph import NeptuneGraphQuery, NeptuneGraphQueryConfig

    def describe():
        ubx.intent("look up aws_neptune_graph_query")

        ubx.data(NeptuneGraphQuery, "example", NeptuneGraphQueryConfig(
            graph_identifier="example",
            query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="graph_identifier" type="string" required>
  The real, unique identifier of the graph this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="query_id" type="string" required>
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="elapsed" type="number">
  The real, elapsed time for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_string" type="string">
  The real, raw query string this request runs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="waited" type="number">
  Whether this operation waited before completing. **(AI-inferred)** Returned by the lookup.
</ResponseField>
