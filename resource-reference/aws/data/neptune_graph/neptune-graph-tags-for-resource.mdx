---
title: "aws_neptune_graph_tags_for_resource"
description: "`aws_neptune_graph_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_graph_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_graph "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_graph"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_graph_tags_for_resource"})

    		ubx.Data(neptune_graph.NeptuneGraphTagsForResource, "example", neptune_graph.NeptuneGraphTagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneGraphTagsForResource } from "@ubx/sdk-aws/aws/data/neptune_graph/neptune_graph_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_graph_tags_for_resource" });

      data(NeptuneGraphTagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_graph import NeptuneGraphTagsForResource, NeptuneGraphTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_neptune_graph_tags_for_resource")

        ubx.data(NeptuneGraphTagsForResource, "example", NeptuneGraphTagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
