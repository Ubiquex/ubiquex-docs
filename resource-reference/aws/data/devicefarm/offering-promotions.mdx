---
title: "aws_devicefarm_offering_promotions"
description: "`aws_devicefarm_offering_promotions` reads a real, available promotional Device Farm offering."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_offering_promotions"})

    		ubx.Data(devicefarm.OfferingPromotions, "example", devicefarm.OfferingPromotionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OfferingPromotions } from "@ubx/sdk-aws/aws/data/devicefarm/offering_promotions";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_offering_promotions" });

      data(OfferingPromotions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import OfferingPromotions, OfferingPromotionsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_offering_promotions")

        ubx.data(OfferingPromotions, "example", OfferingPromotionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="offering_promotions" type="list(object)">
  The real promotions available on this offering. **(AI-inferred)** Returned by the lookup.
</ResponseField>
