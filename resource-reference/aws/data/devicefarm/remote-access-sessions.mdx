---
title: "aws_devicefarm_remote_access_sessions"
description: "`aws_devicefarm_remote_access_sessions` looks up a real Device Farm remote access session, an interactive, real-time session on a physical device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_remote_access_sessions"})

    		ubx.Data(devicefarm.RemoteAccessSessions, "example", devicefarm.RemoteAccessSessionsConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RemoteAccessSessions } from "@ubx/sdk-aws/aws/data/devicefarm/remote_access_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_remote_access_sessions" });

      data(RemoteAccessSessions, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import RemoteAccessSessions, RemoteAccessSessionsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_remote_access_sessions")

        ubx.data(RemoteAccessSessions, "example", RemoteAccessSessionsConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="remote_access_sessions" type="list(object)">
  The real Device Farm remote access sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
