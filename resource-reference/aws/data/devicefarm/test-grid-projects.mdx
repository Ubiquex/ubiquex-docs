---
title: "aws_devicefarm_test_grid_projects"
description: "`aws_devicefarm_test_grid_projects` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devicefarm_test_grid_projects` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_test_grid_projects"})

    		ubx.Data(devicefarm.TestGridProjects, "example", devicefarm.TestGridProjectsConfig{
    			MaxResult: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestGridProjects } from "@ubx/sdk-aws/aws/data/devicefarm/test_grid_projects";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_test_grid_projects" });

      data(TestGridProjects, "example", {
        maxResult: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import TestGridProjects, TestGridProjectsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_test_grid_projects")

        ubx.data(TestGridProjects, "example", TestGridProjectsConfig(
            max_result=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_result" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="test_grid_projects" type="list(object)">
  Returned by the lookup.
</ResponseField>
