---
title: "aws_devicefarm_test_grid_projects"
description: "`aws_devicefarm_test_grid_projects` looks up a real Device Farm Selenium Test Grid project, for running Selenium tests against real mobile browsers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_test_grid_projects"})

    		ubx.Data(devicefarm.TestGridProjects, "example", devicefarm.TestGridProjectsConfig{
    			MaxResult: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestGridProjects } from "@ubx/sdk-aws/aws/data/devicefarm/test_grid_projects";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_test_grid_projects" });

      data(TestGridProjects, "example", {
        maxResult: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import TestGridProjects, TestGridProjectsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_test_grid_projects")

        ubx.data(TestGridProjects, "example", TestGridProjectsConfig(
            max_result=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_result" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="test_grid_projects" type="list(object)">
  The real Device Farm test grid projects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
