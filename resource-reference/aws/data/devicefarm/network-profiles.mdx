---
title: "aws_devicefarm_network_profiles"
description: "`aws_devicefarm_network_profiles` looks up a real Device Farm network profile, simulating network conditions (latency, packet loss) during a test."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_network_profiles"})

    		ubx.Data(devicefarm.NetworkProfiles, "example", devicefarm.NetworkProfilesConfig{
    			Arn:  "arn:aws:iam::123456789012:policy/example",
    			Type: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkProfiles } from "./local-sdk/aws/sdk/typescript/aws/data/devicefarm/network_profiles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_network_profiles" });

      data(NetworkProfiles, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import NetworkProfiles, NetworkProfilesConfig

    def describe():
        ubx.intent("look up aws_devicefarm_network_profiles")

        ubx.data(NetworkProfiles, "example", NetworkProfilesConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="network_profiles" type="list(object)">
  The real network profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
