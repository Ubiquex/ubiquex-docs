---
title: "aws_devicefarm_offering_status"
description: "`aws_devicefarm_offering_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devicefarm_offering_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_offering_status"})

    		ubx.Data(devicefarm.OfferingStatus, "example", devicefarm.OfferingStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OfferingStatus } from "@ubx/sdk-aws/aws/data/devicefarm/offering_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_offering_status" });

      data(OfferingStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import OfferingStatus, OfferingStatusConfig

    def describe():
        ubx.intent("look up aws_devicefarm_offering_status")

        ubx.data(OfferingStatus, "example", OfferingStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="current" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_period" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
