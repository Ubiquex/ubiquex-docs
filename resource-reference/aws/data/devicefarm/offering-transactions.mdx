---
title: "aws_devicefarm_offering_transactions"
description: "`aws_devicefarm_offering_transactions` reads the real purchase and renewal transactions for Device Farm offerings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_offering_transactions"})

    		ubx.Data(devicefarm.OfferingTransactions, "example", devicefarm.OfferingTransactionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OfferingTransactions } from "@ubx/sdk-aws/aws/data/devicefarm/offering_transactions";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_offering_transactions" });

      data(OfferingTransactions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import OfferingTransactions, OfferingTransactionsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_offering_transactions")

        ubx.data(OfferingTransactions, "example", OfferingTransactionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="offering_transactions" type="list(object)">
  The real transactions for this offering. **(AI-inferred)** Returned by the lookup.
</ResponseField>
