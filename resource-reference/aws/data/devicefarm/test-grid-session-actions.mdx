---
title: "aws_devicefarm_test_grid_session_actions"
description: "`aws_devicefarm_test_grid_session_actions` reads the real browser actions recorded during a Selenium Test Grid session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_test_grid_session_actions"})

    		ubx.Data(devicefarm.TestGridSessionActions, "example", devicefarm.TestGridSessionActionsConfig{
    			SessionArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResult:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TestGridSessionActions } from "./local-sdk/aws/sdk/typescript/aws/data/devicefarm/test_grid_session_actions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_test_grid_session_actions" });

      data(TestGridSessionActions, "example", {
        sessionArn: "arn:aws:iam::123456789012:policy/example",
        maxResult: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import TestGridSessionActions, TestGridSessionActionsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_test_grid_session_actions")

        ubx.data(TestGridSessionActions, "example", TestGridSessionActionsConfig(
            session_arn="arn:aws:iam::123456789012:policy/example",
            max_result=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_result" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions" type="list(object)">
  The real actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
