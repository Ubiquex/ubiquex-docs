---
title: "aws_devicefarm_test_grid_sessions"
description: "`aws_devicefarm_test_grid_sessions` reads the real Selenium Test Grid sessions run against a Test Grid project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_test_grid_sessions"})

    		ubx.Data(devicefarm.TestGridSessions, "example", devicefarm.TestGridSessionsConfig{
    			ProjectArn:         "arn:aws:iam::123456789012:policy/example",
    			CreationTimeAfter:  "example",
    			CreationTimeBefore: "example",
    			EndTimeAfter:       "example",
    			EndTimeBefore:      "example",
    			MaxResult:          1,
    			Status:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestGridSessions } from "@ubx/sdk-aws/aws/data/devicefarm/test_grid_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_test_grid_sessions" });

      data(TestGridSessions, "example", {
        projectArn: "arn:aws:iam::123456789012:policy/example",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        endTimeAfter: "example",
        endTimeBefore: "example",
        maxResult: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import TestGridSessions, TestGridSessionsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_test_grid_sessions")

        ubx.data(TestGridSessions, "example", TestGridSessionsConfig(
            project_arn="arn:aws:iam::123456789012:policy/example",
            creation_time_after="example",
            creation_time_before="example",
            end_time_after="example",
            end_time_before="example",
            max_result=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_after" type="string">
  The earliest real end timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time_before" type="string">
  The latest real end timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_result" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="test_grid_sessions" type="list(object)">
  The real Device Farm test grid sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
