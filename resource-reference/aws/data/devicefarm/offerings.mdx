---
title: "aws_devicefarm_offerings"
description: "`aws_devicefarm_offerings` reads the real, available Device Farm offerings that can be purchased."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_offerings"})

    		ubx.Data(devicefarm.Offerings, "example", devicefarm.OfferingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Offerings } from "@ubx/sdk-aws/aws/data/devicefarm/offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_offerings" });

      data(Offerings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import Offerings, OfferingsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_offerings")

        ubx.data(Offerings, "example", OfferingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="offerings" type="list(object)">
  The real offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
