---
title: "aws_devicefarm_devices"
description: "`aws_devicefarm_devices` reads the real physical and virtual devices available in the Device Farm device pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_devices"})

    		ubx.Data(devicefarm.Devices, "example", devicefarm.DevicesConfig{
    			Arn:     "arn:aws:iam::123456789012:policy/example",
    			Filters: []map[string]any{{"attribute": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Devices } from "@ubx/sdk-aws/aws/data/devicefarm/devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_devices" });

      data(Devices, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        filters: [{ attribute: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import Devices, DevicesConfig

    def describe():
        ubx.intent("look up aws_devicefarm_devices")

        ubx.data(Devices, "example", DevicesConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            filters=[{"attribute": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="devices" type="list(object)">
  The real devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
