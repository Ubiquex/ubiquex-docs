---
title: "aws_devicefarm_vpceconfigurations"
description: "`aws_devicefarm_vpceconfigurations` looks up a real Device Farm VPC endpoint configuration, letting tests reach resources inside a private VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_vpceconfigurations"})

    		ubx.Data(devicefarm.Vpceconfigurations, "example", devicefarm.VpceconfigurationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Vpceconfigurations } from "@ubx/sdk-aws/aws/data/devicefarm/vpceconfigurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_vpceconfigurations" });

      data(Vpceconfigurations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import Vpceconfigurations, VpceconfigurationsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_vpceconfigurations")

        ubx.data(Vpceconfigurations, "example", VpceconfigurationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpce_configurations" type="list(object)">
  The real VPC endpoint configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
