---
title: "aws_devicefarm_test_grid_session_artifacts"
description: "`aws_devicefarm_test_grid_session_artifacts` reads the real artifacts (logs, video) generated by a Selenium Test Grid session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_test_grid_session_artifacts"})

    		ubx.Data(devicefarm.TestGridSessionArtifacts, "example", devicefarm.TestGridSessionArtifactsConfig{
    			SessionArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResult:  1,
    			Type:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestGridSessionArtifacts } from "@ubx/sdk-aws/aws/data/devicefarm/test_grid_session_artifacts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_test_grid_session_artifacts" });

      data(TestGridSessionArtifacts, "example", {
        sessionArn: "arn:aws:iam::123456789012:policy/example",
        maxResult: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import TestGridSessionArtifacts, TestGridSessionArtifactsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_test_grid_session_artifacts")

        ubx.data(TestGridSessionArtifacts, "example", TestGridSessionArtifactsConfig(
            session_arn="arn:aws:iam::123456789012:policy/example",
            max_result=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_result" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="artifacts" type="list(object)">
  The real artifacts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
