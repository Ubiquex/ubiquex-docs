---
title: "aws_devicefarm_unique_problems"
description: "`aws_devicefarm_unique_problems` reads the real, deduplicated set of distinct problems (failures) found across a Device Farm test run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devicefarm_unique_problems"})

    		ubx.Data(devicefarm.UniqueProblems, "example", devicefarm.UniqueProblemsConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UniqueProblems } from "@ubx/sdk-aws/aws/data/devicefarm/unique_problems";

    export default stack("example", () => {
      intent({ summary: "look up aws_devicefarm_unique_problems" });

      data(UniqueProblems, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devicefarm import UniqueProblems, UniqueProblemsConfig

    def describe():
        ubx.intent("look up aws_devicefarm_unique_problems")

        ubx.data(UniqueProblems, "example", UniqueProblemsConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unique_problems" type="map(list(object))">
  The real, unique problems detected in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
