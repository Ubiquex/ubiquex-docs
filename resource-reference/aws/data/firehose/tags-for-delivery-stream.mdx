---
title: "aws_firehose_tags_for_delivery_stream"
description: "`aws_firehose_tags_for_delivery_stream` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_firehose_tags_for_delivery_stream` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	firehose "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/firehose"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_firehose_tags_for_delivery_stream"})

    		ubx.Data(firehose.TagsForDeliveryStream, "example", firehose.TagsForDeliveryStreamConfig{
    			DeliveryStreamName:   "example-delivery-stream-name",
    			ExclusiveStartTagKey: "example",
    			Limit:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForDeliveryStream } from "@ubx/sdk-aws/aws/data/firehose/tags_for_delivery_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_firehose_tags_for_delivery_stream" });

      data(TagsForDeliveryStream, "example", {
        deliveryStreamName: "example-delivery-stream-name",
        exclusiveStartTagKey: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.firehose import TagsForDeliveryStream, TagsForDeliveryStreamConfig

    def describe():
        ubx.intent("look up aws_firehose_tags_for_delivery_stream")

        ubx.data(TagsForDeliveryStream, "example", TagsForDeliveryStreamConfig(
            delivery_stream_name="example-delivery-stream-name",
            exclusive_start_tag_key="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="delivery_stream_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="exclusive_start_tag_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_more_tags" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
