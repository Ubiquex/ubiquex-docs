---
title: "aws_firehose_delivery_streams"
description: "`aws_firehose_delivery_streams` looks up a real Data Firehose delivery stream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	firehose "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/firehose"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_firehose_delivery_streams"})

    		ubx.Data(firehose.DeliveryStreams, "example", firehose.DeliveryStreamsConfig{
    			DeliveryStreamType:               "example",
    			ExclusiveStartDeliveryStreamName: "example-exclusive-start-delivery-stream-name",
    			Limit:                            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeliveryStreams } from "@ubx/sdk-aws/aws/data/firehose/delivery_streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_firehose_delivery_streams" });

      data(DeliveryStreams, "example", {
        deliveryStreamType: "example",
        exclusiveStartDeliveryStreamName:
          "example-exclusive-start-delivery-stream-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.firehose import DeliveryStreams, DeliveryStreamsConfig

    def describe():
        ubx.intent("look up aws_firehose_delivery_streams")

        ubx.data(DeliveryStreams, "example", DeliveryStreamsConfig(
            delivery_stream_type="example",
            exclusive_start_delivery_stream_name="example-exclusive-start-delivery-stream-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="delivery_stream_type" type="string">
  The real, discriminating type of the delivery stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclusive_start_delivery_stream_name" type="string">
  The real name of the exclusive start delivery stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delivery_stream_names" type="list(string)">
  The real names of the delivery streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="has_more_delivery_streams" type="bool">
  Whether additional real delivery streams are available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
