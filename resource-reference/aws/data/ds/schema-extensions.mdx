---
title: "aws_ds_schema_extensions"
description: "`aws_ds_schema_extensions` reads the real Active Directory schema extensions applied to a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_schema_extensions"})

    		ubx.Data(ds.SchemaExtensions, "example", ds.SchemaExtensionsConfig{
    			DirectoryId: "example",
    			Limit:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SchemaExtensions } from "./local-sdk/aws/sdk/typescript/aws/data/ds/schema_extensions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_schema_extensions" });

      data(SchemaExtensions, "example", {
        directoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ds import SchemaExtensions, SchemaExtensionsConfig

    def describe():
        ubx.intent("look up aws_ds_schema_extensions")

        ubx.data(SchemaExtensions, "example", SchemaExtensionsConfig(
            directory_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_extensions_info" type="list(object)">
  The real, detailed information about this directory's own schema extensions. **(AI-inferred)** Returned by the lookup.
</ResponseField>
