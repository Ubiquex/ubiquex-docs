---
title: "aws_ds_client_authentication_settings"
description: "`aws_ds_client_authentication_settings` reads the real client authentication (smart card) settings of a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_client_authentication_settings"})

    		ubx.Data(ds.ClientAuthenticationSettings, "example", ds.ClientAuthenticationSettingsConfig{
    			DirectoryId: "example",
    			Limit:       1,
    			Type:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClientAuthenticationSettings } from "@ubx/sdk-aws/aws/data/ds/client_authentication_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_client_authentication_settings" });

      data(ClientAuthenticationSettings, "example", {
        directoryId: "example",
        limit: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import ClientAuthenticationSettings, ClientAuthenticationSettingsConfig

    def describe():
        ubx.intent("look up aws_ds_client_authentication_settings")

        ubx.data(ClientAuthenticationSettings, "example", ClientAuthenticationSettingsConfig(
            directory_id="example",
            limit=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_authentication_settings_info" type="list(object)">
  The real, configured client authentication settings for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
