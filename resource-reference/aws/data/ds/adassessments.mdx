---
title: "aws_ds_adassessments"
description: "`aws_ds_adassessments` reads the real Active Directory assessments run against a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_adassessments"})

    		ubx.Data(ds.Adassessments, "example", ds.AdassessmentsConfig{
    			DirectoryId: "example",
    			Limit:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Adassessments } from "@ubx/sdk-aws/aws/data/ds/adassessments";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_adassessments" });

      data(Adassessments, "example", {
        directoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import Adassessments, AdassessmentsConfig

    def describe():
        ubx.intent("look up aws_ds_adassessments")

        ubx.data(Adassessments, "example", AdassessmentsConfig(
            directory_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string">
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessments" type="list(object)">
  The real assessments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
