---
title: "aws_ds_ip_routes"
description: "`aws_ds_ip_routes` reads the real, added IP routes for a Directory Service directory's own conditional forwarders."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_ip_routes"})

    		ubx.Data(ds.IpRoutes, "example", ds.IpRoutesConfig{
    			DirectoryId: "example",
    			Limit:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpRoutes } from "@ubx/sdk-aws/aws/data/ds/ip_routes";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_ip_routes" });

      data(IpRoutes, "example", {
        directoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import IpRoutes, IpRoutesConfig

    def describe():
        ubx.intent("look up aws_ds_ip_routes")

        ubx.data(IpRoutes, "example", IpRoutesConfig(
            directory_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ip_routes_info" type="list(object)">
  The real, detailed routing information for these IP addresses. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
