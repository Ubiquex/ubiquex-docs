---
title: "aws_ds_caenrollment_policy"
description: "`aws_ds_caenrollment_policy` reads the real certificate authority enrollment policy configured for a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_caenrollment_policy"})

    		ubx.Data(ds.CaenrollmentPolicy, "example", ds.CaenrollmentPolicyConfig{
    			DirectoryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CaenrollmentPolicy } from "@ubx/sdk-aws/aws/data/ds/caenrollment_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_caenrollment_policy" });

      data(CaenrollmentPolicy, "example", {
        directoryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import CaenrollmentPolicy, CaenrollmentPolicyConfig

    def describe():
        ubx.intent("look up aws_ds_caenrollment_policy")

        ubx.data(CaenrollmentPolicy, "example", CaenrollmentPolicyConfig(
            directory_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ca_enrollment_policy_status" type="string">
  The real, current status of the ca enrollment policy this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ca_enrollment_policy_status_reason" type="string">
  The real, human-readable reason for the ca enrollment policy status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pca_connector_arn" type="string">
  The real Amazon Resource Name (ARN) of the pca connector this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
