---
title: "aws_ds_caenrollment_policy"
description: "`aws_ds_caenrollment_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ds_caenrollment_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_caenrollment_policy"})

    		ubx.Data(ds.CaenrollmentPolicy, "example", ds.CaenrollmentPolicyConfig{
    			DirectoryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CaenrollmentPolicy } from "@ubx/sdk-aws/aws/data/ds/caenrollment_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_caenrollment_policy" });

      data(CaenrollmentPolicy, "example", {
        directoryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import CaenrollmentPolicy, CaenrollmentPolicyConfig

    def describe():
        ubx.intent("look up aws_ds_caenrollment_policy")

        ubx.data(CaenrollmentPolicy, "example", CaenrollmentPolicyConfig(
            directory_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ca_enrollment_policy_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ca_enrollment_policy_status_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pca_connector_arn" type="string">
  Returned by the lookup.
</ResponseField>
