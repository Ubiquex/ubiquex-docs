---
title: "aws_ds_ldapssettings"
description: "`aws_ds_ldapssettings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ds_ldapssettings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_ldapssettings"})

    		ubx.Data(ds.Ldapssettings, "example", ds.LdapssettingsConfig{
    			DirectoryId: "example",
    			Limit:       1,
    			Type:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Ldapssettings } from "@ubx/sdk-aws/aws/data/ds/ldapssettings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_ldapssettings" });

      data(Ldapssettings, "example", {
        directoryId: "example",
        limit: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import Ldapssettings, LdapssettingsConfig

    def describe():
        ubx.intent("look up aws_ds_ldapssettings")

        ubx.data(Ldapssettings, "example", LdapssettingsConfig(
            directory_id="example",
            limit=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ldapssettings_info" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
