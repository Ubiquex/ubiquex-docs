---
title: "aws_ds_directory_data_access"
description: "`aws_ds_directory_data_access` reads the real, current status of the Directory Service Data API access for a directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_directory_data_access"})

    		ubx.Data(ds.DirectoryDataAccess, "example", ds.DirectoryDataAccessConfig{
    			DirectoryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DirectoryDataAccess } from "@ubx/sdk-aws/aws/data/ds/directory_data_access";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_directory_data_access" });

      data(DirectoryDataAccess, "example", {
        directoryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import DirectoryDataAccess, DirectoryDataAccessConfig

    def describe():
        ubx.intent("look up aws_ds_directory_data_access")

        ubx.data(DirectoryDataAccess, "example", DirectoryDataAccessConfig(
            directory_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_access_status" type="string">
  The real, current status of the data access this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
