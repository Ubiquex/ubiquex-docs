---
title: "aws_ds_update_directory"
description: "`aws_ds_update_directory` reads the real, current status of an update applied to a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_update_directory"})

    		ubx.Data(ds.UpdateDirectory, "example", ds.UpdateDirectoryConfig{
    			DirectoryId: "example",
    			UpdateType:  "example",
    			RegionName:  "example-region-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { UpdateDirectory } from "./local-sdk/aws/sdk/typescript/aws/data/ds/update_directory.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_update_directory" });

      data(UpdateDirectory, "example", {
        directoryId: "example",
        updateType: "example",
        regionName: "example-region-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ds import UpdateDirectory, UpdateDirectoryConfig

    def describe():
        ubx.intent("look up aws_ds_update_directory")

        ubx.data(UpdateDirectory, "example", UpdateDirectoryConfig(
            directory_id="example",
            update_type="example",
            region_name="example-region-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="region_name" type="string">
  The real name of the region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="update_type" type="string" required>
  The real, discriminating type of the update this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_activities" type="list(object)">
  The real update activities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
