---
title: "aws_ds_certificates"
description: "`aws_ds_certificates` reads the real certificates registered for LDAPS with a Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_certificates"})

    		ubx.Data(ds.Certificates, "example", ds.CertificatesConfig{
    			DirectoryId: "example",
    			Limit:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Certificates } from "@ubx/sdk-aws/aws/data/ds/certificates";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_certificates" });

      data(Certificates, "example", {
        directoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import Certificates, CertificatesConfig

    def describe():
        ubx.intent("look up aws_ds_certificates")

        ubx.data(Certificates, "example", CertificatesConfig(
            directory_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificates_info" type="list(object)">
  The real, detailed certificate information included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
