---
title: "aws_ds_tags_for_resource"
description: "`aws_ds_tags_for_resource` reads the real tags applied to a Directory Service resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_tags_for_resource"})

    		ubx.Data(ds.TagsForResource, "example", ds.TagsForResourceConfig{
    			ResourceId: "example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForResource } from "@ubx/sdk-aws/aws/data/ds/tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_tags_for_resource" });

      data(TagsForResource, "example", {
        resourceId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import TagsForResource, TagsForResourceConfig

    def describe():
        ubx.intent("look up aws_ds_tags_for_resource")

        ubx.data(TagsForResource, "example", TagsForResourceConfig(
            resource_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
