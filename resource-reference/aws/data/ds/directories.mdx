---
title: "aws_ds_directories"
description: "`aws_ds_directories` looks up a real AWS Directory Service directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_directories"})

    		ubx.Data(ds.Directories, "example", ds.DirectoriesConfig{
    			DirectoryIds: []string{"example"},
    			Limit:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Directories } from "@ubx/sdk-aws/aws/data/ds/directories";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_directories" });

      data(Directories, "example", {
        directoryIds: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds import Directories, DirectoriesConfig

    def describe():
        ubx.intent("look up aws_ds_directories")

        ubx.data(Directories, "example", DirectoriesConfig(
            directory_ids=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_ids" type="list(string)">
  &lt;p&gt;A list of directory identifiers.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="directory_descriptions" type="list(object)">
  &lt;p&gt;A list of directory descriptions.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
