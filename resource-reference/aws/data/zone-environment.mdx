---
title: "aws_data_zone_environment"
description: "`aws_data_zone_environment` deploys the real, underlying AWS resources, a Redshift cluster, a Glue database..."
---

`aws_data_zone_environment` deploys the real, underlying AWS resources, a Redshift cluster, a Glue database, that an `aws_data_zone_project`'s members actually query and analyze data through, provisioned from an `aws_data_zone_environment_profile` rather than set up by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own zone environment"})

    		ubx.Resource(data.ZoneEnvironment, "ci-runner", data.ZoneEnvironmentConfig{
    			DomainIdentifier:               "example",
    			Name:                           "ci-runner",
    			ProjectIdentifier:              "example",
    			DeploymentOrder:                1,
    			Description:                    "Managed by ubx.",
    			EnvironmentAccountIdentifier:   "example",
    			EnvironmentAccountRegion:       "example",
    			EnvironmentBlueprintIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneEnvironment } from "./local-sdk/aws/sdk/typescript/aws/data/zone_environment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own zone environment" });

      resource(ZoneEnvironment, "ci-runner", {
        domainIdentifier: "example",
        name: "ci-runner",
        projectIdentifier: "example",
        deploymentOrder: 1,
        description: "Managed by ubx.",
        environmentAccountIdentifier: "example",
        environmentAccountRegion: "example",
        environmentBlueprintIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import ZoneEnvironment, ZoneEnvironmentConfig

    def describe():
        ubx.intent("payments own zone environment")

        ubx.resource(ZoneEnvironment, "ci-runner", ZoneEnvironmentConfig(
            domain_identifier="example",
            name="ci-runner",
            project_identifier="example",
            deployment_order=1,
            description="Managed by ubx.",
            environment_account_identifier="example",
            environment_account_region="example",
            environment_blueprint_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_environment called "ci-runner", with domain
    identifier example, project identifier example, deployment order
    1, description Managed by ubx., environment account identifier
    example, environment account region example, environment
    blueprint identifier example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deployment_order" type="number">
  The deployment order for the environment. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the Amazon DataZone environment. Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The identifier of the Amazon DataZone domain in which the environment would be created. Required.
</ResponseField>

<ResponseField name="environment_account_identifier" type="string">
  The AWS account in which the Amazon DataZone environment is created. Optional.
</ResponseField>

<ResponseField name="environment_account_region" type="string">
  The AWS region in which the Amazon DataZone environment is created. Optional.
</ResponseField>

<ResponseField name="environment_blueprint_identifier" type="string">
  The identifier of the environment blueprint. Optional.
</ResponseField>

<ResponseField name="environment_configuration_id" type="string">
  The identifier of the environment configuration. Optional.
</ResponseField>

<ResponseField name="environment_profile_identifier" type="string">
  The ID of the environment profile with which the Amazon DataZone environment would be created. Optional.
</ResponseField>

<ResponseField name="environment_role_arn" type="string">
  Environment role arn for custom aws environment permissions Optional.
</ResponseField>

<ResponseField name="glossary_terms" type="list(string)">
  The glossary terms that can be used in the Amazon DataZone environment. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the environment. Required.
</ResponseField>

<ResponseField name="project_identifier" type="string" required>
  The ID of the Amazon DataZone project in which the environment would be created. Required.
</ResponseField>

<ResponseField name="user_parameters" type="list(object)">
  The user parameters of the Amazon DataZone environment. Optional.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Specifies the name of a user parameter for the environment, which must match a parameter defined by the environment blueprint to configure the deployed environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value for a user-defined parameter (keyed by the sibling 'name') used to configure the AWS DataZone environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="aws_account_id" type="string">
  The AWS account in which the Amazon DataZone environment is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_region" type="string">
  The AWS region in which the Amazon DataZone environment is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of when the environment was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  The Amazon DataZone user who created the environment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_id" type="string">
  The identifier of the Amazon DataZone domain in which the environment is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="environment_blueprint_id" type="string">
  The ID of the blueprint with which the Amazon DataZone environment was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="environment_profile_id" type="string">
  The ID of the environment profile with which the Amazon DataZone environment was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the Amazon DataZone environment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_id" type="string">
  The ID of the Amazon DataZone project in which the environment is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provider" type="string">
  The provider of the Amazon DataZone environment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the Amazon DataZone environment. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of when the environment was updated. Computed by AWS after `ship`.
</ResponseField>
