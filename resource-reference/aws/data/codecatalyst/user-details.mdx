---
title: "aws_codecatalyst_user_details"
description: "`aws_codecatalyst_user_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codecatalyst_user_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecatalyst "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codecatalyst"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codecatalyst_user_details"})

    		ubx.Data(codecatalyst.UserDetails, "example", codecatalyst.UserDetailsConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserDetails } from "@ubx/sdk-aws/aws/data/codecatalyst/user_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_codecatalyst_user_details" });

      data(UserDetails, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codecatalyst import UserDetails, UserDetailsConfig

    def describe():
        ubx.intent("look up aws_codecatalyst_user_details")

        ubx.data(UserDetails, "example", UserDetailsConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="display_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="primary_email" type="object">
  &lt;p&gt;Information about an email address.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="email" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="verified" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Returned by the lookup.
</ResponseField>
