---
title: "aws_codeconnections_repository_links"
description: "`aws_codeconnections_repository_links` looks up a real CodeConnections repository link, associating a source repository with a sync configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeconnections_repository_links"})

    		ubx.Data(codeconnections.RepositoryLinks, "example", codeconnections.RepositoryLinksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RepositoryLinks } from "./local-sdk/aws/sdk/typescript/aws/data/codeconnections/repository_links.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeconnections_repository_links" });

      data(RepositoryLinks, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeconnections import RepositoryLinks, RepositoryLinksConfig

    def describe():
        ubx.intent("look up aws_codeconnections_repository_links")

        ubx.data(RepositoryLinks, "example", RepositoryLinksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_links" type="list(object)">
  The real repository links included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
