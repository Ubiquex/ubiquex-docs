---
title: "aws_codeconnections_repository_sync_definitions"
description: "`aws_codeconnections_repository_sync_definitions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeconnections_repository_sync_definitions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeconnections_repository_sync_definitions"})

    		ubx.Data(codeconnections.RepositorySyncDefinitions, "example", codeconnections.RepositorySyncDefinitionsConfig{
    			RepositoryLinkId: "example",
    			SyncType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositorySyncDefinitions } from "@ubx/sdk-aws/aws/data/codeconnections/repository_sync_definitions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_codeconnections_repository_sync_definitions",
      });

      data(RepositorySyncDefinitions, "example", {
        repositoryLinkId: "example",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeconnections import RepositorySyncDefinitions, RepositorySyncDefinitionsConfig

    def describe():
        ubx.intent("look up aws_codeconnections_repository_sync_definitions")

        ubx.data(RepositorySyncDefinitions, "example", RepositorySyncDefinitionsConfig(
            repository_link_id="example",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="repository_link_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="repository_sync_definitions" type="list(object)">
  Returned by the lookup.
</ResponseField>
