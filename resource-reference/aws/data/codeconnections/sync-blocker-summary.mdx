---
title: "aws_codeconnections_sync_blocker_summary"
description: "`aws_codeconnections_sync_blocker_summary` reads a real, summarized blocker currently preventing a CodeConnections sync from completing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeconnections_sync_blocker_summary"})

    		ubx.Data(codeconnections.SyncBlockerSummary, "example", codeconnections.SyncBlockerSummaryConfig{
    			ResourceName: "example-resource-name",
    			SyncType:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SyncBlockerSummary } from "./local-sdk/aws/sdk/typescript/aws/data/codeconnections/sync_blocker_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeconnections_sync_blocker_summary" });

      data(SyncBlockerSummary, "example", {
        resourceName: "example-resource-name",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeconnections import SyncBlockerSummary, SyncBlockerSummaryConfig

    def describe():
        ubx.intent("look up aws_codeconnections_sync_blocker_summary")

        ubx.data(SyncBlockerSummary, "example", SyncBlockerSummaryConfig(
            resource_name="example-resource-name",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  The real type of synchronization this request performs. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="sync_blocker_summary" type="object">
  &lt;p&gt;A summary for sync blockers.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="latest_blockers" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent_resource_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
