---
title: "aws_codeconnections_repository_sync_status"
description: "`aws_codeconnections_repository_sync_status` reads the real, current sync status between a repository and its own CodeConnections sync configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeconnections_repository_sync_status"})

    		ubx.Data(codeconnections.RepositorySyncStatus, "example", codeconnections.RepositorySyncStatusConfig{
    			Branch:           "example",
    			RepositoryLinkId: "example",
    			SyncType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RepositorySyncStatus } from "./local-sdk/aws/sdk/typescript/aws/data/codeconnections/repository_sync_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeconnections_repository_sync_status" });

      data(RepositorySyncStatus, "example", {
        branch: "example",
        repositoryLinkId: "example",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeconnections import RepositorySyncStatus, RepositorySyncStatusConfig

    def describe():
        ubx.intent("look up aws_codeconnections_repository_sync_status")

        ubx.data(RepositorySyncStatus, "example", RepositorySyncStatusConfig(
            branch="example",
            repository_link_id="example",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  The real, source-control branch this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository_link_id" type="string" required>
  The real, unique identifier of the repository link this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  The real type of synchronization this request performs. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="latest_sync" type="object">
  &lt;p&gt;Information about a repository sync attempt for a repository with a sync configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="started_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
