---
title: "aws_codeconnections_hosts"
description: "`aws_codeconnections_hosts` looks up a real CodeConnections host, connecting to a self-managed GitHub Enterprise or GitLab instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeconnections_hosts"})

    		ubx.Data(codeconnections.Hosts, "example", codeconnections.HostsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Hosts } from "@ubx/sdk-aws/aws/data/codeconnections/hosts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeconnections_hosts" });

      data(Hosts, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeconnections import Hosts, HostsConfig

    def describe():
        ubx.intent("look up aws_codeconnections_hosts")

        ubx.data(Hosts, "example", HostsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hosts" type="list(object)">
  The real hosts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
