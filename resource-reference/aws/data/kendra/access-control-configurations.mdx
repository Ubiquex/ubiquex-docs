---
title: "aws_kendra_access_control_configurations"
description: "`aws_kendra_access_control_configurations` looks up a real Kendra access control configuration, defining document-level user and group permissions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_access_control_configurations"})

    		ubx.Data(kendra.AccessControlConfigurations, "example", kendra.AccessControlConfigurationsConfig{
    			IndexId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessControlConfigurations } from "@ubx/sdk-aws/aws/data/kendra/access_control_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_access_control_configurations" });

      data(AccessControlConfigurations, "example", {
        indexId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import AccessControlConfigurations, AccessControlConfigurationsConfig

    def describe():
        ubx.intent("look up aws_kendra_access_control_configurations")

        ubx.data(AccessControlConfigurations, "example", AccessControlConfigurationsConfig(
            index_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_control_configurations" type="list(object)">
  The real access control configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
