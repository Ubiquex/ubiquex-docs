---
title: "aws_kendra_featured_results_sets"
description: "`aws_kendra_featured_results_sets` looks up a real Kendra featured results set, pinning specific documents to the top of matching queries."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_featured_results_sets"})

    		ubx.Data(kendra.FeaturedResultsSets, "example", kendra.FeaturedResultsSetsConfig{
    			IndexId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FeaturedResultsSets } from "./local-sdk/aws/sdk/typescript/aws/data/kendra/featured_results_sets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_featured_results_sets" });

      data(FeaturedResultsSets, "example", {
        indexId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import FeaturedResultsSets, FeaturedResultsSetsConfig

    def describe():
        ubx.intent("look up aws_kendra_featured_results_sets")

        ubx.data(FeaturedResultsSets, "example", FeaturedResultsSetsConfig(
            index_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="featured_results_set_summary_items" type="list(object)">
  The real, summarized featured results sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
