---
title: "aws_kendra_data_source_sync_jobs"
description: "`aws_kendra_data_source_sync_jobs` reads the real sync jobs run for a Kendra data source connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_data_source_sync_jobs"})

    		ubx.Data(kendra.DataSourceSyncJobs, "example", kendra.DataSourceSyncJobsConfig{
    			Id:              "example",
    			IndexId:         "example",
    			MaxResults:      1,
    			StartTimeFilter: map[string]any{"end_time": "example"},
    			StatusFilter:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSourceSyncJobs } from "@ubx/sdk-aws/aws/data/kendra/data_source_sync_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_data_source_sync_jobs" });

      data(DataSourceSyncJobs, "example", {
        id: "example",
        indexId: "example",
        maxResults: 1,
        startTimeFilter: { endTime: "example" },
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import DataSourceSyncJobs, DataSourceSyncJobsConfig
    from ubx.aws.data.kendra.data_source_sync_jobs import DataSourceSyncJobs_StartTimeFilter

    def describe():
        ubx.intent("look up aws_kendra_data_source_sync_jobs")

        ubx.data(DataSourceSyncJobs, "example", DataSourceSyncJobsConfig(
            id="example",
            index_id="example",
            max_results=1,
            start_time_filter=DataSourceSyncJobs_StartTimeFilter(end_time="example"),
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time_filter" type="object">
  &lt;p&gt;Provides a range of time.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status_filter" type="string">
  Restricts results to objects matching this status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="history" type="list(object)">
  The real history of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
