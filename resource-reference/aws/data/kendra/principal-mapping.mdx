---
title: "aws_kendra_principal_mapping"
description: "`aws_kendra_principal_mapping` reads the real, current status of mapping a user to their own groups for Kendra access control."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_principal_mapping"})

    		ubx.Data(kendra.PrincipalMapping, "example", kendra.PrincipalMappingConfig{
    			GroupId: "example",
    			IndexId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PrincipalMapping } from "@ubx/sdk-aws/aws/data/kendra/principal_mapping";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_principal_mapping" });

      data(PrincipalMapping, "example", {
        groupId: "example",
        indexId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import PrincipalMapping, PrincipalMappingConfig

    def describe():
        ubx.intent("look up aws_kendra_principal_mapping")

        ubx.data(PrincipalMapping, "example", PrincipalMappingConfig(
            group_id="example",
            index_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_source_id" type="string">
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="group_ordering_id_summaries" type="list(object)">
  The real, summarized group ordering ids included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
