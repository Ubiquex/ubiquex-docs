---
title: "aws_kendra_experience_entities"
description: "`aws_kendra_experience_entities` reads the real users and groups granted access to a Kendra Experience."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_experience_entities"})

    		ubx.Data(kendra.ExperienceEntities, "example", kendra.ExperienceEntitiesConfig{
    			Id:      "example",
    			IndexId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExperienceEntities } from "@ubx/sdk-aws/aws/data/kendra/experience_entities";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_experience_entities" });

      data(ExperienceEntities, "example", {
        id: "example",
        indexId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import ExperienceEntities, ExperienceEntitiesConfig

    def describe():
        ubx.intent("look up aws_kendra_experience_entities")

        ubx.data(ExperienceEntities, "example", ExperienceEntitiesConfig(
            id="example",
            index_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary_items" type="list(object)">
  The real, summarized items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
