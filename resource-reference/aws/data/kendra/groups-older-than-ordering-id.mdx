---
title: "aws_kendra_groups_older_than_ordering_id"
description: "`aws_kendra_groups_older_than_ordering_id` reads the real Kendra access control groups older than a given ordering ID, for principal mapping cleanup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_groups_older_than_ordering_id"})

    		ubx.Data(kendra.GroupsOlderThanOrderingId, "example", kendra.GroupsOlderThanOrderingIdConfig{
    			IndexId:      "example",
    			OrderingId:   1,
    			DataSourceId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupsOlderThanOrderingId } from "@ubx/sdk-aws/aws/data/kendra/groups_older_than_ordering_id";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_groups_older_than_ordering_id" });

      data(GroupsOlderThanOrderingId, "example", {
        indexId: "example",
        orderingId: 1,
        dataSourceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import GroupsOlderThanOrderingId, GroupsOlderThanOrderingIdConfig

    def describe():
        ubx.intent("look up aws_kendra_groups_older_than_ordering_id")

        ubx.data(GroupsOlderThanOrderingId, "example", GroupsOlderThanOrderingIdConfig(
            index_id="example",
            ordering_id=1,
            data_source_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_source_id" type="string">
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ordering_id" type="number" required>
  The real, unique identifier of the ordering this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups_summaries" type="list(object)">
  The real, summarized groupss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
