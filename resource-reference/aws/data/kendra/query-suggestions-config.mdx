---
title: "aws_kendra_query_suggestions_config"
description: "`aws_kendra_query_suggestions_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kendra_query_suggestions_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_query_suggestions_config"})

    		ubx.Data(kendra.QuerySuggestionsConfig, "example", kendra.QuerySuggestionsConfigConfig{
    			IndexId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QuerySuggestionsConfig } from "@ubx/sdk-aws/aws/data/kendra/query_suggestions_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_query_suggestions_config" });

      data(QuerySuggestionsConfig, "example", {
        indexId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import QuerySuggestionsConfig, QuerySuggestionsConfigConfig

    def describe():
        ubx.intent("look up aws_kendra_query_suggestions_config")

        ubx.data(QuerySuggestionsConfig, "example", QuerySuggestionsConfigConfig(
            index_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="index_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute_suggestions_config" type="object">
  &lt;p&gt;Gets information on the configuration of document fields/attributes that you want to base query suggestions on. To change your configuration, use &lt;a href="https://docs.aws.amazon.com/kendra/latest/dg/API_AttributeSuggestionsUpdateConfig.html"&gt;AttributeSuggestionsUpdateConfig&lt;/a&gt; and then call &lt;a href="https://docs.aws.amazon.com/kendra/latest/dg/API_UpdateQuerySuggestionsConfig.html"&gt;UpdateQuerySuggestionsConfig&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attribute_suggestions_mode" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="suggestable_config_list" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="include_queries_without_user_information" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_clear_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_suggestions_build_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="minimum_number_of_querying_users" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="minimum_query_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="mode" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="query_log_look_back_window_in_days" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_suggestions_count" type="number">
  Returned by the lookup.
</ResponseField>
