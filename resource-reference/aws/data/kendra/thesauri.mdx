---
title: "aws_kendra_thesauri"
description: "`aws_kendra_thesauri` looks up a real Kendra thesaurus, expanding search queries with synonyms."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kendra_thesauri"})

    		ubx.Data(kendra.Thesauri, "example", kendra.ThesauriConfig{
    			IndexId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Thesauri } from "@ubx/sdk-aws/aws/data/kendra/thesauri";

    export default stack("example", () => {
      intent({ summary: "look up aws_kendra_thesauri" });

      data(Thesauri, "example", {
        indexId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kendra import Thesauri, ThesauriConfig

    def describe():
        ubx.intent("look up aws_kendra_thesauri")

        ubx.data(Thesauri, "example", ThesauriConfig(
            index_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thesaurus_summary_items" type="list(object)">
  The real, summarized Amazon Kendra thesaurus entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
