---
title: "aws_mpa_policy_versions"
description: "`aws_mpa_policy_versions` reads the real, saved versions of a Multi-party Approval policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mpa "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/mpa"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mpa_policy_versions"})

    		ubx.Data(mpa.PolicyVersions, "example", mpa.PolicyVersionsConfig{
    			PolicyArn:  "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PolicyVersions } from "@ubx/sdk-aws/aws/data/mpa/policy_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_mpa_policy_versions" });

      data(PolicyVersions, "example", {
        policyArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mpa import PolicyVersions, PolicyVersionsConfig

    def describe():
        ubx.intent("look up aws_mpa_policy_versions")

        ubx.data(PolicyVersions, "example", PolicyVersionsConfig(
            policy_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_versions" type="list(object)">
  The real versions of the policy included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
