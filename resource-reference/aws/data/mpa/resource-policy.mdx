---
title: "aws_mpa_resource_policy"
description: "`aws_mpa_resource_policy` reads the real resource-based policy attached to a Multi-party Approval resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mpa "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mpa"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mpa_resource_policy"})

    		ubx.Data(mpa.ResourcePolicy, "example", mpa.ResourcePolicyConfig{
    			PolicyName:  "example-policy-name",
    			PolicyType:  "example",
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/data/mpa/resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_mpa_resource_policy" });

      data(ResourcePolicy, "example", {
        policyName: "example-policy-name",
        policyType: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mpa import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_mpa_resource_policy")

        ubx.data(ResourcePolicy, "example", ResourcePolicyConfig(
            policy_name="example-policy-name",
            policy_type="example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_type" type="string" required>
  The real type of policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_document" type="string">
  The real, JSON-formatted policy document attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_version_arn" type="string">
  The real Amazon Resource Name (ARN) of the policy version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
