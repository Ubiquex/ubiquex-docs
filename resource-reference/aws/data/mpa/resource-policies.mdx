---
title: "aws_mpa_resource_policies"
description: "`aws_mpa_resource_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mpa_resource_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mpa "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mpa"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mpa_resource_policies"})

    		ubx.Data(mpa.ResourcePolicies, "example", mpa.ResourcePoliciesConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePolicies } from "@ubx/sdk-aws/aws/data/mpa/resource_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_mpa_resource_policies" });

      data(ResourcePolicies, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mpa import ResourcePolicies, ResourcePoliciesConfig

    def describe():
        ubx.intent("look up aws_mpa_resource_policies")

        ubx.data(ResourcePolicies, "example", ResourcePoliciesConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>
