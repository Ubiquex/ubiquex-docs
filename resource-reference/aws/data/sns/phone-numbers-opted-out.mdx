---
title: "aws_sns_phone_numbers_opted_out"
description: "`aws_sns_phone_numbers_opted_out` reads the real phone numbers that have opted out of receiving SNS SMS messages."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_phone_numbers_opted_out"})

    		ubx.Data(sns.PhoneNumbersOptedOut, "example", sns.PhoneNumbersOptedOutConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PhoneNumbersOptedOut } from "./local-sdk/aws/sdk/typescript/aws/data/sns/phone_numbers_opted_out.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_phone_numbers_opted_out" });

      data(PhoneNumbersOptedOut, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sns import PhoneNumbersOptedOut, PhoneNumbersOptedOutConfig

    def describe():
        ubx.intent("look up aws_sns_phone_numbers_opted_out")

        ubx.data(PhoneNumbersOptedOut, "example", PhoneNumbersOptedOutConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(string)">
  The real phone numbers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
