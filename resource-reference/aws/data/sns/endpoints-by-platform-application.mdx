---
title: "aws_sns_endpoints_by_platform_application"
description: "`aws_sns_endpoints_by_platform_application` reads the real mobile push endpoints registered under an SNS platform application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_endpoints_by_platform_application"})

    		ubx.Data(sns.EndpointsByPlatformApplication, "example", sns.EndpointsByPlatformApplicationConfig{
    			PlatformApplicationArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EndpointsByPlatformApplication } from "./local-sdk/aws/sdk/typescript/aws/data/sns/endpoints_by_platform_application.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_endpoints_by_platform_application" });

      data(EndpointsByPlatformApplication, "example", {
        platformApplicationArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sns import EndpointsByPlatformApplication, EndpointsByPlatformApplicationConfig

    def describe():
        ubx.intent("look up aws_sns_endpoints_by_platform_application")

        ubx.data(EndpointsByPlatformApplication, "example", EndpointsByPlatformApplicationConfig(
            platform_application_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="platform_application_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the platform application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="endpoints" type="list(object)">
  The real endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
