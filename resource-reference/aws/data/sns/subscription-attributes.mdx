---
title: "aws_sns_subscription_attributes"
description: "`aws_sns_subscription_attributes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sns_subscription_attributes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_subscription_attributes"})

    		ubx.Data(sns.SubscriptionAttributes, "example", sns.SubscriptionAttributesConfig{
    			SubscriptionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionAttributes } from "@ubx/sdk-aws/aws/data/sns/subscription_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_subscription_attributes" });

      data(SubscriptionAttributes, "example", {
        subscriptionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sns import SubscriptionAttributes, SubscriptionAttributesConfig

    def describe():
        ubx.intent("look up aws_sns_subscription_attributes")

        ubx.data(SubscriptionAttributes, "example", SubscriptionAttributesConfig(
            subscription_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subscription_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(string)">
  Returned by the lookup.
</ResponseField>
