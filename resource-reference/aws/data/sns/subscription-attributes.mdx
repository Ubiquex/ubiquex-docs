---
title: "aws_sns_subscription_attributes"
description: "`data_aws_sns_subscription_attributes` looks up a real, already-existing `aws_sns_subscription`."
---

It subscribes a real endpoint, an SQS queue, Lambda function, HTTPS URL, email, or phone number, to an `aws_sns_topic`, the resource that actually receives every message the topic publishes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_subscription_attributes"})

    		ubx.Data(sns.SubscriptionAttributes, "example", sns.SubscriptionAttributesConfig{
    			SubscriptionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SubscriptionAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/sns/subscription_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_subscription_attributes" });

      data(SubscriptionAttributes, "example", {
        subscriptionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sns import SubscriptionAttributes, SubscriptionAttributesConfig

    def describe():
        ubx.intent("look up aws_sns_subscription_attributes")

        ubx.data(SubscriptionAttributes, "example", SubscriptionAttributesConfig(
            subscription_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subscription_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the subscription this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(string)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
