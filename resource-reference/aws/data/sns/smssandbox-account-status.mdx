---
title: "aws_sns_smssandbox_account_status"
description: "`aws_sns_smssandbox_account_status` reads whether a real SNS account is still in the SMS sandbox, limited to verified destination numbers only."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_smssandbox_account_status"})

    		ubx.Data(sns.SmssandboxAccountStatus, "example", sns.SmssandboxAccountStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmssandboxAccountStatus } from "@ubx/sdk-aws/aws/data/sns/smssandbox_account_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_smssandbox_account_status" });

      data(SmssandboxAccountStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sns import SmssandboxAccountStatus, SmssandboxAccountStatusConfig

    def describe():
        ubx.intent("look up aws_sns_smssandbox_account_status")

        ubx.data(SmssandboxAccountStatus, "example", SmssandboxAccountStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="is_in_sandbox" type="bool">
  Whether this object runs in a sandbox environment. **(AI-inferred)** Returned by the lookup.
</ResponseField>
