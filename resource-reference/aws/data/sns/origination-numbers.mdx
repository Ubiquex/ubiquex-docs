---
title: "aws_sns_origination_numbers"
description: "`aws_sns_origination_numbers` reads the real, dedicated origination phone numbers configured for SNS SMS sending."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_origination_numbers"})

    		ubx.Data(sns.OriginationNumbers, "example", sns.OriginationNumbersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OriginationNumbers } from "@ubx/sdk-aws/aws/data/sns/origination_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_origination_numbers" });

      data(OriginationNumbers, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sns import OriginationNumbers, OriginationNumbersConfig

    def describe():
        ubx.intent("look up aws_sns_origination_numbers")

        ubx.data(OriginationNumbers, "example", OriginationNumbersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  List of customer owned phone numbers. Returned by the lookup.
</ResponseField>
