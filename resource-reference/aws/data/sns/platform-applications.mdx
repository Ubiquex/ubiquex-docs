---
title: "aws_sns_platform_applications"
description: "`aws_sns_platform_applications` looks up a real SNS platform application, registering a mobile push notification service (APNs..."
---

`aws_sns_platform_applications` looks up a real SNS platform application, registering a mobile push notification service (APNs, FCM) for delivering to device endpoints.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_platform_applications"})

    		ubx.Data(sns.PlatformApplications, "example", sns.PlatformApplicationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PlatformApplications } from "./local-sdk/aws/sdk/typescript/aws/data/sns/platform_applications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_platform_applications" });

      data(PlatformApplications, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sns import PlatformApplications, PlatformApplicationsConfig

    def describe():
        ubx.intent("look up aws_sns_platform_applications")

        ubx.data(PlatformApplications, "example", PlatformApplicationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platform_applications" type="list(object)">
  The real SNS platform applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
