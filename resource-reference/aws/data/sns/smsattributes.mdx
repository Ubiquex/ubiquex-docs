---
title: "aws_sns_smsattributes"
description: "`aws_sns_smsattributes` reads the real, account-wide SMS sending attributes (default sender ID, spend limit) for SNS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_smsattributes"})

    		ubx.Data(sns.Smsattributes, "example", sns.SmsattributesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Smsattributes } from "./local-sdk/aws/sdk/typescript/aws/data/sns/smsattributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_smsattributes" });

      data(Smsattributes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sns import Smsattributes, SmsattributesConfig

    def describe():
        ubx.intent("look up aws_sns_smsattributes")

        ubx.data(Smsattributes, "example", SmsattributesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="attributes" type="list(string)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
