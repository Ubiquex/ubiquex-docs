---
title: "aws_sns_endpoint_attributes"
description: "`aws_sns_endpoint_attributes` reads the real, configured attributes of an SNS mobile push platform endpoint."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sns_endpoint_attributes"})

    		ubx.Data(sns.EndpointAttributes, "example", sns.EndpointAttributesConfig{
    			EndpointArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EndpointAttributes } from "@ubx/sdk-aws/aws/data/sns/endpoint_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_sns_endpoint_attributes" });

      data(EndpointAttributes, "example", {
        endpointArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sns import EndpointAttributes, EndpointAttributesConfig

    def describe():
        ubx.intent("look up aws_sns_endpoint_attributes")

        ubx.data(EndpointAttributes, "example", EndpointAttributesConfig(
            endpoint_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="endpoint_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="map(string)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
