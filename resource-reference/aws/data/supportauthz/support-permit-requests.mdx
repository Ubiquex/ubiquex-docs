---
title: "aws_supportauthz_support_permit_requests"
description: "`aws_supportauthz_support_permit_requests` reads the real, pending requests for AWS Support to access account resources."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	supportauthz "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/supportauthz"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_supportauthz_support_permit_requests"})

    		ubx.Data(supportauthz.SupportPermitRequests, "example", supportauthz.SupportPermitRequestsConfig{
    			MaxResults:           1,
    			SupportCaseDisplayId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SupportPermitRequests } from "./local-sdk/aws/sdk/typescript/aws/data/supportauthz/support_permit_requests.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_supportauthz_support_permit_requests" });

      data(SupportPermitRequests, "example", {
        maxResults: 1,
        supportCaseDisplayId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.supportauthz import SupportPermitRequests, SupportPermitRequestsConfig

    def describe():
        ubx.intent("look up aws_supportauthz_support_permit_requests")

        ubx.data(SupportPermitRequests, "example", SupportPermitRequestsConfig(
            max_results=1,
            support_case_display_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="support_case_display_id" type="string">
  The real, unique identifier of the support case display this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_permit_requests" type="list(object)">
  The real AWS Support permit requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
