---
title: "aws_supportauthz_action"
description: "`aws_supportauthz_action` looks up one real support authorization action."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	supportauthz "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/supportauthz"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_supportauthz_action"})

    		ubx.Data(supportauthz.Action, "example", supportauthz.ActionConfig{
    			Action: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Action } from "@ubx/sdk-aws/aws/data/supportauthz/action";

    export default stack("example", () => {
      intent({ summary: "look up aws_supportauthz_action" });

      data(Action, "example", {
        action: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.supportauthz import Action, ActionConfig

    def describe():
        ubx.intent("look up aws_supportauthz_action")

        ubx.data(Action, "example", ActionConfig(
            action="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action" type="string" required>
  The real action this object describes. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service" type="string">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
