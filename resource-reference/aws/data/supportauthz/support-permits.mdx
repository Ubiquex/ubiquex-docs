---
title: "aws_supportauthz_support_permits"
description: "`aws_supportauthz_support_permits` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_supportauthz_support_permits` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	supportauthz "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/supportauthz"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_supportauthz_support_permits"})

    		ubx.Data(supportauthz.SupportPermits, "example", supportauthz.SupportPermitsConfig{
    			MaxResults:            1,
    			SupportPermitStatuses: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SupportPermits } from "@ubx/sdk-aws/aws/data/supportauthz/support_permits";

    export default stack("example", () => {
      intent({ summary: "look up aws_supportauthz_support_permits" });

      data(SupportPermits, "example", {
        maxResults: 1,
        supportPermitStatuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.supportauthz import SupportPermits, SupportPermitsConfig

    def describe():
        ubx.intent("look up aws_supportauthz_support_permits")

        ubx.data(SupportPermits, "example", SupportPermitsConfig(
            max_results=1,
            support_permit_statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="support_permit_statuses" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_permits" type="list(object)">
  Returned by the lookup.
</ResponseField>
