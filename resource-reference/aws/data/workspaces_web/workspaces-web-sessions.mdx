---
title: "aws_workspaces_web_sessions"
description: "`aws_workspaces_web_sessions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_web_sessions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces_web "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces_web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_web_sessions"})

    		ubx.Data(workspaces_web.WorkspacesWebSessions, "example", workspaces_web.WorkspacesWebSessionsConfig{
    			PortalId:   "example",
    			MaxResults: 1,
    			SessionId:  "example",
    			SortBy:     "example",
    			Status:     "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesWebSessions } from "@ubx/sdk-aws/aws/data/workspaces_web/workspaces_web_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_web_sessions" });

      data(WorkspacesWebSessions, "example", {
        portalId: "example",
        maxResults: 1,
        sessionId: "example",
        sortBy: "example",
        status: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces_web import WorkspacesWebSessions, WorkspacesWebSessionsConfig

    def describe():
        ubx.intent("look up aws_workspaces_web_sessions")

        ubx.data(WorkspacesWebSessions, "example", WorkspacesWebSessionsConfig(
            portal_id="example",
            max_results=1,
            session_id="example",
            sort_by="example",
            status="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="portal_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  Returned by the lookup.
</ResponseField>
