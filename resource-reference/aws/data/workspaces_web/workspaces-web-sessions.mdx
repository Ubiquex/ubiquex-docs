---
title: "aws_workspaces_web_sessions"
description: "`aws_workspaces_web_sessions` reads the real WorkSpaces Secure Browser sessions on a portal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workspaces_web "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces_web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_web_sessions"})

    		ubx.Data(workspaces_web.WorkspacesWebSessions, "example", workspaces_web.WorkspacesWebSessionsConfig{
    			PortalId:   "example",
    			MaxResults: 1,
    			SessionId:  "example",
    			SortBy:     "example",
    			Status:     "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WorkspacesWebSessions } from "./local-sdk/aws/sdk/typescript/aws/data/workspaces_web/workspaces_web_sessions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_web_sessions" });

      data(WorkspacesWebSessions, "example", {
        portalId: "example",
        maxResults: 1,
        sessionId: "example",
        sortBy: "example",
        status: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces_web import WorkspacesWebSessions, WorkspacesWebSessionsConfig

    def describe():
        ubx.intent("look up aws_workspaces_web_sessions")

        ubx.data(WorkspacesWebSessions, "example", WorkspacesWebSessionsConfig(
            portal_id="example",
            max_results=1,
            session_id="example",
            sort_by="example",
            status="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="portal_id" type="string" required>
  The real, unique identifier of the portal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string">
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  The real sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
