---
title: "aws_workspaces_web_session"
description: "`aws_workspaces_web_session` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_web_session` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces_web "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces_web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_web_session"})

    		ubx.Data(workspaces_web.WorkspacesWebSession, "example", workspaces_web.WorkspacesWebSessionConfig{
    			PortalId:  "example",
    			SessionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesWebSession } from "@ubx/sdk-aws/aws/data/workspaces_web/workspaces_web_session";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_web_session" });

      data(WorkspacesWebSession, "example", {
        portalId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces_web import WorkspacesWebSession, WorkspacesWebSessionConfig

    def describe():
        ubx.intent("look up aws_workspaces_web_session")

        ubx.data(WorkspacesWebSession, "example", WorkspacesWebSessionConfig(
            portal_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="portal_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="session" type="object">
  &lt;p&gt;Information about a secure browser session.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="client_ip_addresses" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="portal_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="username" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
