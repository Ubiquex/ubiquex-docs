---
title: "aws_cleanrooms_ml_collaboration_mlinput_channels"
description: "`aws_cleanrooms_ml_collaboration_mlinput_channels` looks up a real Clean Rooms ML input channel, the data a collaboration member contributes for training."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cleanrooms_ml "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cleanrooms_ml"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cleanrooms_ml_collaboration_mlinput_channels"})

    		ubx.Data(cleanrooms_ml.CleanroomsMlCollaborationMlinputChannels, "example", cleanrooms_ml.CleanroomsMlCollaborationMlinputChannelsConfig{
    			CollaborationIdentifier: "example",
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CleanroomsMlCollaborationMlinputChannels } from "@ubx/sdk-aws/aws/data/cleanrooms_ml/cleanrooms_ml_collaboration_mlinput_channels";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cleanrooms_ml_collaboration_mlinput_channels",
      });

      data(CleanroomsMlCollaborationMlinputChannels, "example", {
        collaborationIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cleanrooms_ml import CleanroomsMlCollaborationMlinputChannels, CleanroomsMlCollaborationMlinputChannelsConfig

    def describe():
        ubx.intent("look up aws_cleanrooms_ml_collaboration_mlinput_channels")

        ubx.data(CleanroomsMlCollaborationMlinputChannels, "example", CleanroomsMlCollaborationMlinputChannelsConfig(
            collaboration_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="collaboration_identifier" type="string" required>
  The real, unique identifier of the collaboration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="collaboration_mlinput_channels_list" type="list(object)">
  The real collaboration mlinput channelss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
