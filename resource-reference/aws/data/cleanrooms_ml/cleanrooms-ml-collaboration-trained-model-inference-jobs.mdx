---
title: "aws_cleanrooms_ml_collaboration_trained_model_inference_jobs"
description: "`aws_cleanrooms_ml_collaboration_trained_model_inference_jobs` reads the real inference jobs run against a Clean Rooms ML trained model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cleanrooms_ml "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cleanrooms_ml"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cleanrooms_ml_collaboration_trained_model_inference_jobs"})

    		ubx.Data(cleanrooms_ml.CleanroomsMlCollaborationTrainedModelInferenceJobs, "example", cleanrooms_ml.CleanroomsMlCollaborationTrainedModelInferenceJobsConfig{
    			CollaborationIdentifier:       "example",
    			MaxResults:                    1,
    			TrainedModelArn:               "arn:aws:iam::123456789012:policy/example",
    			TrainedModelVersionIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CleanroomsMlCollaborationTrainedModelInferenceJobs } from "@ubx/sdk-aws/aws/data/cleanrooms_ml/cleanrooms_ml_collaboration_trained_model_inference_jobs";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_cleanrooms_ml_collaboration_trained_model_inference_jobs",
      });

      data(CleanroomsMlCollaborationTrainedModelInferenceJobs, "example", {
        collaborationIdentifier: "example",
        maxResults: 1,
        trainedModelArn: "arn:aws:iam::123456789012:policy/example",
        trainedModelVersionIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cleanrooms_ml import CleanroomsMlCollaborationTrainedModelInferenceJobs, CleanroomsMlCollaborationTrainedModelInferenceJobsConfig

    def describe():
        ubx.intent("look up aws_cleanrooms_ml_collaboration_trained_model_inference_jobs")

        ubx.data(CleanroomsMlCollaborationTrainedModelInferenceJobs, "example", CleanroomsMlCollaborationTrainedModelInferenceJobsConfig(
            collaboration_identifier="example",
            max_results=1,
            trained_model_arn="arn:aws:iam::123456789012:policy/example",
            trained_model_version_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="collaboration_identifier" type="string" required>
  The real, unique identifier of the collaboration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="trained_model_arn" type="string">
  The real Amazon Resource Name (ARN) of the trained model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="trained_model_version_identifier" type="string">
  The real, unique identifier of the trained model version this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="collaboration_trained_model_inference_jobs" type="list(object)">
  The real Clean Rooms ML trained model inference jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
