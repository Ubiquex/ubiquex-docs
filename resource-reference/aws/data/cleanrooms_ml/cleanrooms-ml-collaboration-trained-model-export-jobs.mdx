---
title: "aws_cleanrooms_ml_collaboration_trained_model_export_jobs"
description: "`aws_cleanrooms_ml_collaboration_trained_model_export_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cleanrooms_ml_collaboration_trained_model_export_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cleanrooms_ml "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cleanrooms_ml"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cleanrooms_ml_collaboration_trained_model_export_jobs"})

    		ubx.Data(cleanrooms_ml.CleanroomsMlCollaborationTrainedModelExportJobs, "example", cleanrooms_ml.CleanroomsMlCollaborationTrainedModelExportJobsConfig{
    			CollaborationIdentifier:       "example",
    			TrainedModelArn:               "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                    1,
    			TrainedModelVersionIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CleanroomsMlCollaborationTrainedModelExportJobs } from "@ubx/sdk-aws/aws/data/cleanrooms_ml/cleanrooms_ml_collaboration_trained_model_export_jobs";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_cleanrooms_ml_collaboration_trained_model_export_jobs",
      });

      data(CleanroomsMlCollaborationTrainedModelExportJobs, "example", {
        collaborationIdentifier: "example",
        trainedModelArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        trainedModelVersionIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cleanrooms_ml import CleanroomsMlCollaborationTrainedModelExportJobs, CleanroomsMlCollaborationTrainedModelExportJobsConfig

    def describe():
        ubx.intent("look up aws_cleanrooms_ml_collaboration_trained_model_export_jobs")

        ubx.data(CleanroomsMlCollaborationTrainedModelExportJobs, "example", CleanroomsMlCollaborationTrainedModelExportJobsConfig(
            collaboration_identifier="example",
            trained_model_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            trained_model_version_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="collaboration_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="trained_model_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="trained_model_version_identifier" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="collaboration_trained_model_export_jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
