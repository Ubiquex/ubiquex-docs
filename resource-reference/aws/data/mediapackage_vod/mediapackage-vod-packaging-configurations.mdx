---
title: "aws_mediapackage_vod_packaging_configurations"
description: "`aws_mediapackage_vod_packaging_configurations` looks up a real MediaPackage VOD packaging configuration, defining an output format for an asset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediapackage_vod "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediapackage_vod"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediapackage_vod_packaging_configurations"})

    		ubx.Data(mediapackage_vod.MediapackageVodPackagingConfigurations, "example", mediapackage_vod.MediapackageVodPackagingConfigurationsConfig{
    			MaxResults:       1,
    			PackagingGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediapackageVodPackagingConfigurations } from "@ubx/sdk-aws/aws/data/mediapackage_vod/mediapackage_vod_packaging_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediapackage_vod_packaging_configurations" });

      data(MediapackageVodPackagingConfigurations, "example", {
        maxResults: 1,
        packagingGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediapackage_vod import MediapackageVodPackagingConfigurations, MediapackageVodPackagingConfigurationsConfig

    def describe():
        ubx.intent("look up aws_mediapackage_vod_packaging_configurations")

        ubx.data(MediapackageVodPackagingConfigurations, "example", MediapackageVodPackagingConfigurationsConfig(
            max_results=1,
            packaging_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="packaging_group_id" type="string">
  The real, unique identifier of the packaging group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="packaging_configurations" type="list(object)">
  The real MediaPackage packaging configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
