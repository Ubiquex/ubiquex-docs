---
title: "aws_mediapackage_vod_packaging_groups"
description: "`aws_mediapackage_vod_packaging_groups` looks up a real MediaPackage VOD packaging group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediapackage_vod "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediapackage_vod"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediapackage_vod_packaging_groups"})

    		ubx.Data(mediapackage_vod.MediapackageVodPackagingGroups, "example", mediapackage_vod.MediapackageVodPackagingGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediapackageVodPackagingGroups } from "@ubx/sdk-aws/aws/data/mediapackage_vod/mediapackage_vod_packaging_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediapackage_vod_packaging_groups" });

      data(MediapackageVodPackagingGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediapackage_vod import MediapackageVodPackagingGroups, MediapackageVodPackagingGroupsConfig

    def describe():
        ubx.intent("look up aws_mediapackage_vod_packaging_groups")

        ubx.data(MediapackageVodPackagingGroups, "example", MediapackageVodPackagingGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="packaging_groups" type="list(object)">
  The real packaging groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
