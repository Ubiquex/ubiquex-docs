---
title: "aws_mediapackage_vod_assets"
description: "`aws_mediapackage_vod_assets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mediapackage_vod_assets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediapackage_vod "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediapackage_vod"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediapackage_vod_assets"})

    		ubx.Data(mediapackage_vod.MediapackageVodAssets, "example", mediapackage_vod.MediapackageVodAssetsConfig{
    			MaxResults:       1,
    			PackagingGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediapackageVodAssets } from "@ubx/sdk-aws/aws/data/mediapackage_vod/mediapackage_vod_assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediapackage_vod_assets" });

      data(MediapackageVodAssets, "example", {
        maxResults: 1,
        packagingGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediapackage_vod import MediapackageVodAssets, MediapackageVodAssetsConfig

    def describe():
        ubx.intent("look up aws_mediapackage_vod_assets")

        ubx.data(MediapackageVodAssets, "example", MediapackageVodAssetsConfig(
            max_results=1,
            packaging_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="packaging_group_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assets" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
