---
title: "aws_mediaconvert_presets"
description: "`aws_mediaconvert_presets` looks up a real MediaConvert output preset, a saved, reusable output format configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_presets"})

    		ubx.Data(mediaconvert.Presets, "example", mediaconvert.PresetsConfig{
    			Category:   "example",
    			ListBy:     "example",
    			MaxResults: 1,
    			Order:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Presets } from "@ubx/sdk-aws/aws/data/mediaconvert/presets";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_presets" });

      data(Presets, "example", {
        category: "example",
        listBy: "example",
        maxResults: 1,
        order: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import Presets, PresetsConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_presets")

        ubx.data(Presets, "example", PresetsConfig(
            category="example",
            list_by="example",
            max_results=1,
            order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category" type="string">
  The real category this object belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_by" type="string">
  Optional. When you request a list of presets, you can choose to list them alphabetically by NAME or chronologically by CREATION_DATE. If you don't specify, the service will list them by name. Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  Optional. When you request lists of resources, you can specify whether they are sorted in ASCENDING or DESCENDING order. Default varies by resource. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="presets" type="list(object)">
  The real presets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
