---
title: "aws_mediaconvert_job_templates"
description: "`aws_mediaconvert_job_templates` looks up a real MediaConvert job template, a saved, reusable transcoding configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_job_templates"})

    		ubx.Data(mediaconvert.JobTemplates, "example", mediaconvert.JobTemplatesConfig{
    			Category:   "example",
    			ListBy:     "example",
    			MaxResults: 1,
    			Order:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { JobTemplates } from "./local-sdk/aws/sdk/typescript/aws/data/mediaconvert/job_templates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_job_templates" });

      data(JobTemplates, "example", {
        category: "example",
        listBy: "example",
        maxResults: 1,
        order: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import JobTemplates, JobTemplatesConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_job_templates")

        ubx.data(JobTemplates, "example", JobTemplatesConfig(
            category="example",
            list_by="example",
            max_results=1,
            order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category" type="string">
  The real category this object belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_by" type="string">
  Optional. When you request a list of job templates, you can choose to list them alphabetically by NAME or chronologically by CREATION_DATE. If you don't specify, the service will list them by name. Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  Optional. When you request lists of resources, you can specify whether they are sorted in ASCENDING or DESCENDING order. Default varies by resource. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_templates" type="list(object)">
  The real job templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
