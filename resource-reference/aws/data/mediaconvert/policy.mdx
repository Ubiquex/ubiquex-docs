---
title: "aws_mediaconvert_policy"
description: "`aws_mediaconvert_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mediaconvert_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_policy"})

    		ubx.Data(mediaconvert.Policy, "example", mediaconvert.PolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Policy } from "@ubx/sdk-aws/aws/data/mediaconvert/policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_policy" });

      data(Policy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import Policy, PolicyConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_policy")

        ubx.data(Policy, "example", PolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="policy" type="object">
  A policy configures behavior that you allow or disallow for your account. For information about MediaConvert policies, see the user guide at http://docs.aws.amazon.com/mediaconvert/latest/ug/what-is.html Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="http_inputs" type="string">
      An input policy allows or disallows a job you submit to run based on the conditions that you specify. Optional lookup argument.
    </ResponseField>
    <ResponseField name="https_inputs" type="string">
      An input policy allows or disallows a job you submit to run based on the conditions that you specify. Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3_inputs" type="string">
      An input policy allows or disallows a job you submit to run based on the conditions that you specify. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
