---
title: "aws_mediaconvert_queues"
description: "`aws_mediaconvert_queues` looks up a real MediaConvert queue, controlling how jobs are prioritized and processed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_queues"})

    		ubx.Data(mediaconvert.Queues, "example", mediaconvert.QueuesConfig{
    			ListBy:     "example",
    			MaxResults: 1,
    			Order:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Queues } from "@ubx/sdk-aws/aws/data/mediaconvert/queues";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_queues" });

      data(Queues, "example", {
        listBy: "example",
        maxResults: 1,
        order: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import Queues, QueuesConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_queues")

        ubx.data(Queues, "example", QueuesConfig(
            list_by="example",
            max_results=1,
            order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="list_by" type="string">
  Optional. When you request a list of queues, you can choose to list them alphabetically by NAME or chronologically by CREATION_DATE. If you don't specify, the service will list them by creation date. Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  Optional. When you request lists of resources, you can specify whether they are sorted in ASCENDING or DESCENDING order. Default varies by resource. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queues" type="list(object)">
  The real queues included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_concurrent_jobs" type="number">
  The real, total number of jobs currently running concurrently. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unallocated_concurrent_jobs" type="number">
  The real, currently unallocated concurrent job capacity. **(AI-inferred)** Returned by the lookup.
</ResponseField>
