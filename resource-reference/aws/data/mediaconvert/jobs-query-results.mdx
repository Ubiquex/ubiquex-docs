---
title: "aws_mediaconvert_jobs_query_results"
description: "`aws_mediaconvert_jobs_query_results` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mediaconvert_jobs_query_results` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_jobs_query_results"})

    		ubx.Data(mediaconvert.JobsQueryResults, "example", mediaconvert.JobsQueryResultsConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobsQueryResults } from "@ubx/sdk-aws/aws/data/mediaconvert/jobs_query_results";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_jobs_query_results" });

      data(JobsQueryResults, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import JobsQueryResults, JobsQueryResultsConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_jobs_query_results")

        ubx.data(JobsQueryResults, "example", JobsQueryResultsConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  A job query's status can be SUBMITTED, PROGRESSING, COMPLETE, or ERROR. Returned by the lookup.
</ResponseField>
