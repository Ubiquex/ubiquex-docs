---
title: "aws_mediaconvert_jobs"
description: "`aws_mediaconvert_jobs` looks up one real MediaConvert transcoding job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconvert "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconvert"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconvert_jobs"})

    		ubx.Data(mediaconvert.Jobs, "example", mediaconvert.JobsConfig{
    			MaxResults: 1,
    			Order:      "example",
    			Queue:      "example",
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Jobs } from "@ubx/sdk-aws/aws/data/mediaconvert/jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconvert_jobs" });

      data(Jobs, "example", {
        maxResults: 1,
        order: "example",
        queue: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconvert import Jobs, JobsConfig

    def describe():
        ubx.intent("look up aws_mediaconvert_jobs")

        ubx.data(Jobs, "example", JobsConfig(
            max_results=1,
            order="example",
            queue="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  Optional. When you request lists of resources, you can specify whether they are sorted in ASCENDING or DESCENDING order. Default varies by resource. Optional lookup argument.
</ResponseField>

<ResponseField name="queue" type="string">
  The real queue this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  A job's status can be SUBMITTED, PROGRESSING, COMPLETE, CANCELED, or ERROR. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
