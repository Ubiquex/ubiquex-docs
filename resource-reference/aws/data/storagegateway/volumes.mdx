---
title: "aws_storagegateway_volumes"
description: "`aws_storagegateway_volumes` looks up a real iSCSI volume on a Volume Gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_volumes"})

    		ubx.Data(storagegateway.Volumes, "example", storagegateway.VolumesConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Volumes } from "@ubx/sdk-aws/aws/data/storagegateway/volumes";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_volumes" });

      data(Volumes, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import Volumes, VolumesConfig

    def describe():
        ubx.intent("look up aws_storagegateway_volumes")

        ubx.data(Volumes, "example", VolumesConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gateway_arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="volume_infos" type="list(object)">
  The real, detailed volume information included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
