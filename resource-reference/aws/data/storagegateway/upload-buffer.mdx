---
title: "aws_storagegateway_upload_buffer"
description: "`aws_storagegateway_upload_buffer` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_storagegateway_upload_buffer` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_upload_buffer"})

    		ubx.Data(storagegateway.UploadBuffer, "example", storagegateway.UploadBufferConfig{
    			GatewayArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UploadBuffer } from "@ubx/sdk-aws/aws/data/storagegateway/upload_buffer";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_upload_buffer" });

      data(UploadBuffer, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import UploadBuffer, UploadBufferConfig

    def describe():
        ubx.intent("look up aws_storagegateway_upload_buffer")

        ubx.data(UploadBuffer, "example", UploadBufferConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="disk_ids" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="upload_buffer_allocated_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="upload_buffer_used_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>
