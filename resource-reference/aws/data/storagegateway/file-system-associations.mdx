---
title: "aws_storagegateway_file_system_associations"
description: "`aws_storagegateway_file_system_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_storagegateway_file_system_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_file_system_associations"})

    		ubx.Data(storagegateway.FileSystemAssociations, "example", storagegateway.FileSystemAssociationsConfig{
    			FileSystemAssociationArnlist: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FileSystemAssociations } from "@ubx/sdk-aws/aws/data/storagegateway/file_system_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_file_system_associations" });

      data(FileSystemAssociations, "example", {
        fileSystemAssociationArnlist: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import FileSystemAssociations, FileSystemAssociationsConfig

    def describe():
        ubx.intent("look up aws_storagegateway_file_system_associations")

        ubx.data(FileSystemAssociations, "example", FileSystemAssociationsConfig(
            file_system_association_arnlist=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_system_association_arnlist" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="file_system_association_info_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
