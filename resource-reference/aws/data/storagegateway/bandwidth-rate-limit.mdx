---
title: "aws_storagegateway_bandwidth_rate_limit"
description: "`aws_storagegateway_bandwidth_rate_limit` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_storagegateway_bandwidth_rate_limit` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_bandwidth_rate_limit"})

    		ubx.Data(storagegateway.BandwidthRateLimit, "example", storagegateway.BandwidthRateLimitConfig{
    			GatewayArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BandwidthRateLimit } from "@ubx/sdk-aws/aws/data/storagegateway/bandwidth_rate_limit";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_bandwidth_rate_limit" });

      data(BandwidthRateLimit, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import BandwidthRateLimit, BandwidthRateLimitConfig

    def describe():
        ubx.intent("look up aws_storagegateway_bandwidth_rate_limit")

        ubx.data(BandwidthRateLimit, "example", BandwidthRateLimitConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="average_download_rate_limit_in_bits_per_sec" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="average_upload_rate_limit_in_bits_per_sec" type="number">
  Returned by the lookup.
</ResponseField>
