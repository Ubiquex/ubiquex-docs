---
title: "aws_storagegateway_maintenance_start_time"
description: "`aws_storagegateway_maintenance_start_time` reads the real, scheduled maintenance window start time of a Storage Gateway appliance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_maintenance_start_time"})

    		ubx.Data(storagegateway.MaintenanceStartTime, "example", storagegateway.MaintenanceStartTimeConfig{
    			GatewayArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaintenanceStartTime } from "@ubx/sdk-aws/aws/data/storagegateway/maintenance_start_time";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_maintenance_start_time" });

      data(MaintenanceStartTime, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import MaintenanceStartTime, MaintenanceStartTimeConfig

    def describe():
        ubx.intent("look up aws_storagegateway_maintenance_start_time")

        ubx.data(MaintenanceStartTime, "example", MaintenanceStartTimeConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="day_of_month" type="number">
  The real day of the month this schedule runs on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="day_of_week" type="number">
  The real day of the week this schedule runs on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hour_of_day" type="number">
  The real hour of the day this schedule runs on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="minute_of_hour" type="number">
  The real minute of the hour this schedule runs on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="software_update_preferences" type="object">
  &lt;p&gt;A set of variables indicating the software update preferences for the gateway.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="automatic_update_policy" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timezone" type="string">
  The real time zone of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
