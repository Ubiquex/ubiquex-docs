---
title: "aws_storagegateway_gateways"
description: "`aws_storagegateway_gateways` looks up a real Storage Gateway appliance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_gateways"})

    		ubx.Data(storagegateway.Gateways, "example", storagegateway.GatewaysConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Gateways } from "@ubx/sdk-aws/aws/data/storagegateway/gateways";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_gateways" });

      data(Gateways, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import Gateways, GatewaysConfig

    def describe():
        ubx.intent("look up aws_storagegateway_gateways")

        ubx.data(Gateways, "example", GatewaysConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gateways" type="list(object)">
  The real gateways included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
