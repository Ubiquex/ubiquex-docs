---
title: "aws_storagegateway_gateway_information"
description: "`aws_storagegateway_gateway_information` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_storagegateway_gateway_information` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_gateway_information"})

    		ubx.Data(storagegateway.GatewayInformation, "example", storagegateway.GatewayInformationConfig{
    			GatewayArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GatewayInformation } from "@ubx/sdk-aws/aws/data/storagegateway/gateway_information";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_gateway_information" });

      data(GatewayInformation, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import GatewayInformation, GatewayInformationConfig

    def describe():
        ubx.intent("look up aws_storagegateway_gateway_information")

        ubx.data(GatewayInformation, "example", GatewayInformationConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_watch_log_group_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="deprecation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ec2_instance_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ec2_instance_region" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="endpoint_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_capacity" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_network_interfaces" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_timezone" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="gateway_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="host_environment" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="host_environment_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_software_update" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_update_availability_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="software_updates_end_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="software_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="supported_gateway_capacities" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vpcendpoint" type="string">
  Returned by the lookup.
</ResponseField>
