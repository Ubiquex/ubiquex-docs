---
title: "aws_storagegateway_tape_recovery_points"
description: "`aws_storagegateway_tape_recovery_points` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_storagegateway_tape_recovery_points` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_tape_recovery_points"})

    		ubx.Data(storagegateway.TapeRecoveryPoints, "example", storagegateway.TapeRecoveryPointsConfig{
    			GatewayArn: "arn:aws:iam::123456789012:policy/example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TapeRecoveryPoints } from "@ubx/sdk-aws/aws/data/storagegateway/tape_recovery_points";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_tape_recovery_points" });

      data(TapeRecoveryPoints, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import TapeRecoveryPoints, TapeRecoveryPointsConfig

    def describe():
        ubx.intent("look up aws_storagegateway_tape_recovery_points")

        ubx.data(TapeRecoveryPoints, "example", TapeRecoveryPointsConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tape_recovery_point_infos" type="list(object)">
  Returned by the lookup.
</ResponseField>
