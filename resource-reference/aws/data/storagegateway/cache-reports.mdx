---
title: "aws_storagegateway_cache_reports"
description: "`aws_storagegateway_cache_reports` reads the real cache reports generated for a File Gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_cache_reports"})

    		ubx.Data(storagegateway.CacheReports, "example", storagegateway.CacheReportsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CacheReports } from "@ubx/sdk-aws/aws/data/storagegateway/cache_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_cache_reports" });

      data(CacheReports, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import CacheReports, CacheReportsConfig

    def describe():
        ubx.intent("look up aws_storagegateway_cache_reports")

        ubx.data(CacheReports, "example", CacheReportsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="cache_report_list" type="list(object)">
  The real cache reports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
