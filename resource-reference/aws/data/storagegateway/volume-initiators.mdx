---
title: "aws_storagegateway_volume_initiators"
description: "`aws_storagegateway_volume_initiators` reads the real iSCSI initiators currently connected to a Storage Gateway volume."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_volume_initiators"})

    		ubx.Data(storagegateway.VolumeInitiators, "example", storagegateway.VolumeInitiatorsConfig{
    			VolumeArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VolumeInitiators } from "./local-sdk/aws/sdk/typescript/aws/data/storagegateway/volume_initiators.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_volume_initiators" });

      data(VolumeInitiators, "example", {
        volumeArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import VolumeInitiators, VolumeInitiatorsConfig

    def describe():
        ubx.intent("look up aws_storagegateway_volume_initiators")

        ubx.data(VolumeInitiators, "example", VolumeInitiatorsConfig(
            volume_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="volume_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the volume this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="initiators" type="list(string)">
  The real iSCSI initiators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
