---
title: "aws_storagegateway_vtldevices"
description: "`aws_storagegateway_vtldevices` looks up a real virtual tape library (VTL) device on a Tape Gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storagegateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/storagegateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_storagegateway_vtldevices"})

    		ubx.Data(storagegateway.Vtldevices, "example", storagegateway.VtldevicesConfig{
    			GatewayArn:    "arn:aws:iam::123456789012:policy/example",
    			Limit:         1,
    			VtldeviceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Vtldevices } from "@ubx/sdk-aws/aws/data/storagegateway/vtldevices";

    export default stack("example", () => {
      intent({ summary: "look up aws_storagegateway_vtldevices" });

      data(Vtldevices, "example", {
        gatewayArn: "arn:aws:iam::123456789012:policy/example",
        limit: 1,
        vtldeviceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.storagegateway import Vtldevices, VtldevicesConfig

    def describe():
        ubx.intent("look up aws_storagegateway_vtldevices")

        ubx.data(Vtldevices, "example", VtldevicesConfig(
            gateway_arn="arn:aws:iam::123456789012:policy/example",
            limit=1,
            vtldevice_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gateway_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN) of the gateway. Use the &lt;a&gt;ListGateways&lt;/a&gt; operation to return a list of gateways for your account and Amazon Web Services Region.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vtldevice_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the vtldevices this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vtldevices" type="list(object)">
  The real Storage Gateway virtual tape library (VTL) devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
