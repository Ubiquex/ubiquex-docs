---
title: "aws_neptune_db_rdfgraph_summary"
description: "`aws_neptune_db_rdfgraph_summary` reads a real, summarized schema of a Neptune database's own RDF graph data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_rdfgraph_summary"})

    		ubx.Data(neptune_db.NeptuneDbRdfgraphSummary, "example", neptune_db.NeptuneDbRdfgraphSummaryConfig{
    			Mode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneDbRdfgraphSummary } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_db/neptune_db_rdfgraph_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_rdfgraph_summary" });

      data(NeptuneDbRdfgraphSummary, "example", {
        mode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbRdfgraphSummary, NeptuneDbRdfgraphSummaryConfig

    def describe():
        ubx.intent("look up aws_neptune_db_rdfgraph_summary")

        ubx.data(NeptuneDbRdfgraphSummary, "example", NeptuneDbRdfgraphSummaryConfig(
            mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="mode" type="string">
  The real, current mode this object operates in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="object">
  &lt;p&gt;Payload for an RDF graph summary response.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="graph_summary" type="object">
      &lt;p&gt;The RDF graph summary API returns a read-only list of classes and predicate keys, along with counts of quads, subjects, and predicates.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="classes" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_classes" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_distinct_predicates" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_distinct_subjects" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_quads" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="predicates" type="list(map(number))">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="subject_structures" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_statistics_computation_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="version" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status_code" type="number">
  The real status code returned for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
