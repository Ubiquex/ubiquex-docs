---
title: "aws_neptune_db_sparql_stream"
description: "`aws_neptune_db_sparql_stream` reads the real, ordered change-log stream of a Neptune database's own RDF graph data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_sparql_stream"})

    		ubx.Data(neptune_db.NeptuneDbSparqlStream, "example", neptune_db.NeptuneDbSparqlStreamConfig{
    			CommitNum:    1,
    			Encoding:     "example",
    			IteratorType: "example",
    			Limit:        1,
    			OpNum:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbSparqlStream } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_sparql_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_sparql_stream" });

      data(NeptuneDbSparqlStream, "example", {
        commitNum: 1,
        encoding: "example",
        iteratorType: "example",
        limit: 1,
        opNum: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbSparqlStream, NeptuneDbSparqlStreamConfig

    def describe():
        ubx.intent("look up aws_neptune_db_sparql_stream")

        ubx.data(NeptuneDbSparqlStream, "example", NeptuneDbSparqlStreamConfig(
            commit_num=1,
            encoding="example",
            iterator_type="example",
            limit=1,
            op_num=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="commit_num" type="number">
  The real, ordinal number of this commit within its own connection. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="encoding" type="string">
  The real character encoding of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="iterator_type" type="string">
  The real, discriminating type of the iterator this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="op_num" type="number">
  The real, ordinal number of this operation within its own connection. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="format" type="string">
  The real, requested output format of the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_event_id" type="map(string)">
  The real, unique identifier of the last event this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_trx_timestamp_in_millis" type="number">
  The real timestamp of the last transaction, in milliseconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  The real records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_records" type="number">
  The real, total number of records in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
