---
title: "aws_neptune_db_mldata_processing_jobs"
description: "`aws_neptune_db_mldata_processing_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_mldata_processing_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_mldata_processing_jobs"})

    		ubx.Data(neptune_db.NeptuneDbMldataProcessingJobs, "example", neptune_db.NeptuneDbMldataProcessingJobsConfig{
    			MaxItems:          1,
    			NeptuneIamRoleArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbMldataProcessingJobs } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_mldata_processing_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_mldata_processing_jobs" });

      data(NeptuneDbMldataProcessingJobs, "example", {
        maxItems: 1,
        neptuneIamRoleArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbMldataProcessingJobs, NeptuneDbMldataProcessingJobsConfig

    def describe():
        ubx.intent("look up aws_neptune_db_mldata_processing_jobs")

        ubx.data(NeptuneDbMldataProcessingJobs, "example", NeptuneDbMldataProcessingJobsConfig(
            max_items=1,
            neptune_iam_role_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="neptune_iam_role_arn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  Returned by the lookup.
</ResponseField>
