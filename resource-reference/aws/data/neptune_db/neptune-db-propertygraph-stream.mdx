---
title: "aws_neptune_db_propertygraph_stream"
description: "`aws_neptune_db_propertygraph_stream` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_propertygraph_stream` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_propertygraph_stream"})

    		ubx.Data(neptune_db.NeptuneDbPropertygraphStream, "example", neptune_db.NeptuneDbPropertygraphStreamConfig{
    			CommitNum:    1,
    			Encoding:     "example",
    			IteratorType: "example",
    			Limit:        1,
    			OpNum:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbPropertygraphStream } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_propertygraph_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_propertygraph_stream" });

      data(NeptuneDbPropertygraphStream, "example", {
        commitNum: 1,
        encoding: "example",
        iteratorType: "example",
        limit: 1,
        opNum: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbPropertygraphStream, NeptuneDbPropertygraphStreamConfig

    def describe():
        ubx.intent("look up aws_neptune_db_propertygraph_stream")

        ubx.data(NeptuneDbPropertygraphStream, "example", NeptuneDbPropertygraphStreamConfig(
            commit_num=1,
            encoding="example",
            iterator_type="example",
            limit=1,
            op_num=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="commit_num" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="encoding" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="iterator_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="op_num" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="format" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_event_id" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_trx_timestamp_in_millis" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_records" type="number">
  Returned by the lookup.
</ResponseField>
