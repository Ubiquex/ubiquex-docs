---
title: "aws_neptune_db_loader_job_status"
description: "`aws_neptune_db_loader_job_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_loader_job_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_loader_job_status"})

    		ubx.Data(neptune_db.NeptuneDbLoaderJobStatus, "example", neptune_db.NeptuneDbLoaderJobStatusConfig{
    			LoadId:        "example",
    			Details:       true,
    			Errors:        true,
    			ErrorsPerPage: 1,
    			Page:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbLoaderJobStatus } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_loader_job_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_loader_job_status" });

      data(NeptuneDbLoaderJobStatus, "example", {
        loadId: "example",
        details: true,
        errors: true,
        errorsPerPage: 1,
        page: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbLoaderJobStatus, NeptuneDbLoaderJobStatusConfig

    def describe():
        ubx.intent("look up aws_neptune_db_loader_job_status")

        ubx.data(NeptuneDbLoaderJobStatus, "example", NeptuneDbLoaderJobStatusConfig(
            load_id="example",
            details=True,
            errors=True,
            errors_per_page=1,
            page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="details" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="errors" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="errors_per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="load_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="any">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
