---
title: "aws_neptune_db_loader_job_status"
description: "`aws_neptune_db_loader_job_status` reads the real, current status of a Neptune bulk loader job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_loader_job_status"})

    		ubx.Data(neptune_db.NeptuneDbLoaderJobStatus, "example", neptune_db.NeptuneDbLoaderJobStatusConfig{
    			LoadId:        "example",
    			Details:       true,
    			Errors:        true,
    			ErrorsPerPage: 1,
    			Page:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneDbLoaderJobStatus } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_db/neptune_db_loader_job_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_loader_job_status" });

      data(NeptuneDbLoaderJobStatus, "example", {
        loadId: "example",
        details: true,
        errors: true,
        errorsPerPage: 1,
        page: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbLoaderJobStatus, NeptuneDbLoaderJobStatusConfig

    def describe():
        ubx.intent("look up aws_neptune_db_loader_job_status")

        ubx.data(NeptuneDbLoaderJobStatus, "example", NeptuneDbLoaderJobStatusConfig(
            load_id="example",
            details=True,
            errors=True,
            errors_per_page=1,
            page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="details" type="bool">
  The real, structured details of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="errors" type="bool">
  The real errors encountered while processing this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="errors_per_page" type="number">
  The real, maximum number of errors to return per page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="load_id" type="string" required>
  The real, unique identifier of the load this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The real page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="any">
  The real, raw payload of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
