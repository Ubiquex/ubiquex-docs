---
title: "aws_neptune_db_propertygraph_summary"
description: "`aws_neptune_db_propertygraph_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_propertygraph_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_propertygraph_summary"})

    		ubx.Data(neptune_db.NeptuneDbPropertygraphSummary, "example", neptune_db.NeptuneDbPropertygraphSummaryConfig{
    			Mode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbPropertygraphSummary } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_propertygraph_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_propertygraph_summary" });

      data(NeptuneDbPropertygraphSummary, "example", {
        mode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbPropertygraphSummary, NeptuneDbPropertygraphSummaryConfig

    def describe():
        ubx.intent("look up aws_neptune_db_propertygraph_summary")

        ubx.data(NeptuneDbPropertygraphSummary, "example", NeptuneDbPropertygraphSummaryConfig(
            mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="mode" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="object">
  &lt;p&gt;Payload for the property graph summary response.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="graph_summary" type="object">
      &lt;p&gt;The graph summary API returns a read-only list of node and edge labels and property keys, along with counts of nodes, edges, and properties. See &lt;a href="https://docs.aws.amazon.com/neptune/latest/userguide/neptune-graph-summary.html#neptune-graph-summary-pg-response"&gt;Graph summary response for a property graph (PG)&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="edge_labels" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="edge_properties" type="list(map(number))">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="edge_structures" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="node_labels" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="node_properties" type="list(map(number))">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="node_structures" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_edge_labels" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_edge_properties" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_edges" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_node_labels" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_node_properties" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="num_nodes" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_edge_property_values" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_node_property_values" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_statistics_computation_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="version" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status_code" type="number">
  Returned by the lookup.
</ResponseField>
