---
title: "aws_neptune_db_gremlin_queries"
description: "`aws_neptune_db_gremlin_queries` reads the real, currently running Gremlin queries on a Neptune database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_gremlin_queries"})

    		ubx.Data(neptune_db.NeptuneDbGremlinQueries, "example", neptune_db.NeptuneDbGremlinQueriesConfig{
    			IncludeWaiting: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneDbGremlinQueries } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_db/neptune_db_gremlin_queries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_gremlin_queries" });

      data(NeptuneDbGremlinQueries, "example", {
        includeWaiting: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbGremlinQueries, NeptuneDbGremlinQueriesConfig

    def describe():
        ubx.intent("look up aws_neptune_db_gremlin_queries")

        ubx.data(NeptuneDbGremlinQueries, "example", NeptuneDbGremlinQueriesConfig(
            include_waiting=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_waiting" type="bool">
  Whether items waiting to be processed are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accepted_query_count" type="number">
  The real count of accepted querys. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queries" type="list(object)">
  The real queries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="running_query_count" type="number">
  The real count of running querys. **(AI-inferred)** Returned by the lookup.
</ResponseField>
