---
title: "aws_neptune_db_open_cypher_queries"
description: "`aws_neptune_db_open_cypher_queries` reads the real, currently running openCypher queries on a Neptune database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_open_cypher_queries"})

    		ubx.Data(neptune_db.NeptuneDbOpenCypherQueries, "example", neptune_db.NeptuneDbOpenCypherQueriesConfig{
    			IncludeWaiting: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbOpenCypherQueries } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_open_cypher_queries";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_open_cypher_queries" });

      data(NeptuneDbOpenCypherQueries, "example", {
        includeWaiting: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbOpenCypherQueries, NeptuneDbOpenCypherQueriesConfig

    def describe():
        ubx.intent("look up aws_neptune_db_open_cypher_queries")

        ubx.data(NeptuneDbOpenCypherQueries, "example", NeptuneDbOpenCypherQueriesConfig(
            include_waiting=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_waiting" type="bool">
  Whether items waiting to be processed are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accepted_query_count" type="number">
  The real count of accepted querys. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queries" type="list(object)">
  The real queries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="running_query_count" type="number">
  The real count of running querys. **(AI-inferred)** Returned by the lookup.
</ResponseField>
