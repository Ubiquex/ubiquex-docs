---
title: "aws_neptune_db_engine_status"
description: "`aws_neptune_db_engine_status` reads the real, current engine status of a Neptune database instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_engine_status"})

    		ubx.Data(neptune_db.NeptuneDbEngineStatus, "example", neptune_db.NeptuneDbEngineStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneDbEngineStatus } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_db/neptune_db_engine_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_engine_status" });

      data(NeptuneDbEngineStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbEngineStatus, NeptuneDbEngineStatusConfig

    def describe():
        ubx.intent("look up aws_neptune_db_engine_status")

        ubx.data(NeptuneDbEngineStatus, "example", NeptuneDbEngineStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="db_engine_version" type="string">
  The real version of the db engine. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dfe_query_engine" type="string">
  The real, dynamic query engine used for this data federation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="features" type="map(any)">
  The real features included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="gremlin" type="object">
  &lt;p&gt;Structure for expressing the query language version.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="version" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lab_mode" type="map(string)">
  The real, configured lab mode for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="opencypher" type="object">
  &lt;p&gt;Structure for expressing the query language version.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="version" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role" type="string">
  The real IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rolling_back_trx_count" type="number">
  The real count of rolling back trxs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rolling_back_trx_earliest_start_time" type="string">
  The real timestamp of the rolling back trx earliest start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="settings" type="map(string)">
  The real settings applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sparql" type="object">
  &lt;p&gt;Structure for expressing the query language version.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="version" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
