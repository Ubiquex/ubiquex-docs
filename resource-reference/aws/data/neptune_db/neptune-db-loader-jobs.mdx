---
title: "aws_neptune_db_loader_jobs"
description: "`aws_neptune_db_loader_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_loader_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_loader_jobs"})

    		ubx.Data(neptune_db.NeptuneDbLoaderJobs, "example", neptune_db.NeptuneDbLoaderJobsConfig{
    			IncludeQueuedLoads: true,
    			Limit:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbLoaderJobs } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_loader_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_loader_jobs" });

      data(NeptuneDbLoaderJobs, "example", {
        includeQueuedLoads: true,
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbLoaderJobs, NeptuneDbLoaderJobsConfig

    def describe():
        ubx.intent("look up aws_neptune_db_loader_jobs")

        ubx.data(NeptuneDbLoaderJobs, "example", NeptuneDbLoaderJobsConfig(
            include_queued_loads=True,
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_queued_loads" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="payload" type="object">
  &lt;p&gt;Contains a list of load IDs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="load_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
