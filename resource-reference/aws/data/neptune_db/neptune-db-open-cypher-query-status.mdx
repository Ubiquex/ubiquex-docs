---
title: "aws_neptune_db_open_cypher_query_status"
description: "`aws_neptune_db_open_cypher_query_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_open_cypher_query_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_open_cypher_query_status"})

    		ubx.Data(neptune_db.NeptuneDbOpenCypherQueryStatus, "example", neptune_db.NeptuneDbOpenCypherQueryStatusConfig{
    			QueryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbOpenCypherQueryStatus } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_open_cypher_query_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_open_cypher_query_status" });

      data(NeptuneDbOpenCypherQueryStatus, "example", {
        queryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbOpenCypherQueryStatus, NeptuneDbOpenCypherQueryStatusConfig

    def describe():
        ubx.intent("look up aws_neptune_db_open_cypher_query_status")

        ubx.data(NeptuneDbOpenCypherQueryStatus, "example", NeptuneDbOpenCypherQueryStatusConfig(
            query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="query_eval_stats" type="object">
  &lt;p&gt;Structure to capture query statistics such as how many queries are running, accepted or waiting and their details.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cancelled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="elapsed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subqueries" type="any">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="waited" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_string" type="string">
  Returned by the lookup.
</ResponseField>
