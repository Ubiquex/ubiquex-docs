---
title: "aws_neptune_db_sparql_statistics"
description: "`aws_neptune_db_sparql_statistics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_neptune_db_sparql_statistics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_sparql_statistics"})

    		ubx.Data(neptune_db.NeptuneDbSparqlStatistics, "example", neptune_db.NeptuneDbSparqlStatisticsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbSparqlStatistics } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_sparql_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_sparql_statistics" });

      data(NeptuneDbSparqlStatistics, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbSparqlStatistics, NeptuneDbSparqlStatisticsConfig

    def describe():
        ubx.intent("look up aws_neptune_db_sparql_statistics")

        ubx.data(NeptuneDbSparqlStatistics, "example", NeptuneDbSparqlStatisticsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="payload" type="object">
  &lt;p&gt;Contains statistics information. The DFE engine uses information about the data in your Neptune graph to make effective trade-offs when planning query execution. This information takes the form of statistics that include so-called characteristic sets and predicate statistics that can guide query planning. See &lt;a href="https://docs.aws.amazon.com/neptune/latest/userguide/neptune-dfe-statistics.html"&gt;Managing statistics for the Neptune DFE to use&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="active" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="auto_compute" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="note" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="signature_info" type="object">
      &lt;p&gt;Information about the characteristic sets generated in the statistics.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="instance_count" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="predicate_count" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="signature_count" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="statistics_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
