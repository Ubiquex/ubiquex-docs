---
title: "aws_neptune_db_mlendpoints"
description: "`aws_neptune_db_mlendpoints` looks up a real Neptune ML inference endpoint."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_mlendpoints"})

    		ubx.Data(neptune_db.NeptuneDbMlendpoints, "example", neptune_db.NeptuneDbMlendpointsConfig{
    			MaxItems:          1,
    			NeptuneIamRoleArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NeptuneDbMlendpoints } from "@ubx/sdk-aws/aws/data/neptune_db/neptune_db_mlendpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_mlendpoints" });

      data(NeptuneDbMlendpoints, "example", {
        maxItems: 1,
        neptuneIamRoleArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbMlendpoints, NeptuneDbMlendpointsConfig

    def describe():
        ubx.intent("look up aws_neptune_db_mlendpoints")

        ubx.data(NeptuneDbMlendpoints, "example", NeptuneDbMlendpointsConfig(
            max_items=1,
            neptune_iam_role_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="neptune_iam_role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role Neptune assumes for this lookup. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
