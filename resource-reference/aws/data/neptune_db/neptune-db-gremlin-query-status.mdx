---
title: "aws_neptune_db_gremlin_query_status"
description: "`aws_neptune_db_gremlin_query_status` reads the real, current status of a specific Gremlin query running on a Neptune database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	neptune_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/neptune_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_neptune_db_gremlin_query_status"})

    		ubx.Data(neptune_db.NeptuneDbGremlinQueryStatus, "example", neptune_db.NeptuneDbGremlinQueryStatusConfig{
    			QueryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NeptuneDbGremlinQueryStatus } from "./local-sdk/aws/sdk/typescript/aws/data/neptune_db/neptune_db_gremlin_query_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_neptune_db_gremlin_query_status" });

      data(NeptuneDbGremlinQueryStatus, "example", {
        queryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.neptune_db import NeptuneDbGremlinQueryStatus, NeptuneDbGremlinQueryStatusConfig

    def describe():
        ubx.intent("look up aws_neptune_db_gremlin_query_status")

        ubx.data(NeptuneDbGremlinQueryStatus, "example", NeptuneDbGremlinQueryStatusConfig(
            query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query_id" type="string" required>
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="query_eval_stats" type="object">
  &lt;p&gt;Structure to capture query statistics such as how many queries are running, accepted or waiting and their details.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cancelled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="elapsed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subqueries" type="any">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="waited" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_string" type="string">
  The real, raw query string this request runs. **(AI-inferred)** Returned by the lookup.
</ResponseField>
