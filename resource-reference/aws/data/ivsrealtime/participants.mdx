---
title: "aws_ivsrealtime_participants"
description: "`aws_ivsrealtime_participants` reads the real participants in an IVS real-time stage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_participants"})

    		ubx.Data(ivsrealtime.Participants, "example", ivsrealtime.ParticipantsConfig{
    			SessionId:              "example",
    			StageArn:               "arn:aws:iam::123456789012:policy/example",
    			FilterByPublished:      true,
    			FilterByRecordingState: "example",
    			FilterByState:          "example",
    			FilterByUserId:         "example",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Participants } from "@ubx/sdk-aws/aws/data/ivsrealtime/participants";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_participants" });

      data(Participants, "example", {
        sessionId: "example",
        stageArn: "arn:aws:iam::123456789012:policy/example",
        filterByPublished: true,
        filterByRecordingState: "example",
        filterByState: "example",
        filterByUserId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import Participants, ParticipantsConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_participants")

        ubx.data(Participants, "example", ParticipantsConfig(
            session_id="example",
            stage_arn="arn:aws:iam::123456789012:policy/example",
            filter_by_published=True,
            filter_by_recording_state="example",
            filter_by_state="example",
            filter_by_user_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_by_published" type="bool">
  Restricts results to objects matching this published state. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_recording_state" type="string">
  The real, current state of the filter by recording this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_state" type="string">
  The real, current state of the filter by this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_user_id" type="string">
  The real, unique identifier of the filter by user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stage_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the stage this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="participants" type="list(object)">
  The real participants included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
