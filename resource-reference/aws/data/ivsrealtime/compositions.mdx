---
title: "aws_ivsrealtime_compositions"
description: "`aws_ivsrealtime_compositions` reads the real compositions run for an IVS real-time stage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_compositions"})

    		ubx.Data(ivsrealtime.Compositions, "example", ivsrealtime.CompositionsConfig{
    			FilterByEncoderConfigurationArn: "arn:aws:iam::123456789012:policy/example",
    			FilterByStageArn:                "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Compositions } from "./local-sdk/aws/sdk/typescript/aws/data/ivsrealtime/compositions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_compositions" });

      data(Compositions, "example", {
        filterByEncoderConfigurationArn: "arn:aws:iam::123456789012:policy/example",
        filterByStageArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import Compositions, CompositionsConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_compositions")

        ubx.data(Compositions, "example", CompositionsConfig(
            filter_by_encoder_configuration_arn="arn:aws:iam::123456789012:policy/example",
            filter_by_stage_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_by_encoder_configuration_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter by encoder configuration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_stage_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter by stage this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compositions" type="list(object)">
  The real compositions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
