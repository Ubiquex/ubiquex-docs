---
title: "aws_ivsrealtime_stage_sessions"
description: "`data_aws_ivsrealtime_stage_sessions` looks up a real, already-existing `aws_ivs_stage`."
---

It is Amazon IVS's real, low-latency, multi-participant video collaboration resource, up to several hosts and guests interacting live together, distinct from an `aws_ivs_channel`'s own single-broadcaster-to-many-viewers model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_stage_sessions"})

    		ubx.Data(ivsrealtime.StageSessions, "example", ivsrealtime.StageSessionsConfig{
    			StageArn:   "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StageSessions } from "@ubx/sdk-aws/aws/data/ivsrealtime/stage_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_stage_sessions" });

      data(StageSessions, "example", {
        stageArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import StageSessions, StageSessionsConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_stage_sessions")

        ubx.data(StageSessions, "example", StageSessionsConfig(
            stage_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stage_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the stage this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stage_sessions" type="list(object)">
  The real stage sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
