---
title: "aws_ivsrealtime_stages"
description: "`data_aws_ivsrealtime_stages` looks up a real, already-existing `aws_ivs_stage`."
---

It is Amazon IVS's real, low-latency, multi-participant video collaboration resource, up to several hosts and guests interacting live together, distinct from an `aws_ivs_channel`'s own single-broadcaster-to-many-viewers model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_stages"})

    		ubx.Data(ivsrealtime.Stages, "example", ivsrealtime.StagesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Stages } from "@ubx/sdk-aws/aws/data/ivsrealtime/stages";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_stages" });

      data(Stages, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import Stages, StagesConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_stages")

        ubx.data(Stages, "example", StagesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stages" type="list(object)">
  The real stages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
