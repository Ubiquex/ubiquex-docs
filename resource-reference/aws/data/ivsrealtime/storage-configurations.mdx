---
title: "aws_ivsrealtime_storage_configurations"
description: "`data_aws_ivsrealtime_storage_configurations` looks up a real, already-existing `aws_ivs_storage_configuration`."
---

It defines a real S3 destination that an `aws_ivs_stage`'s own composite recording gets written to, the stage counterpart to `aws_ivs_recording_configuration`'s channel-level recording.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_storage_configurations"})

    		ubx.Data(ivsrealtime.StorageConfigurations, "example", ivsrealtime.StorageConfigurationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { StorageConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/ivsrealtime/storage_configurations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_storage_configurations" });

      data(StorageConfigurations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import StorageConfigurations, StorageConfigurationsConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_storage_configurations")

        ubx.data(StorageConfigurations, "example", StorageConfigurationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="storage_configurations" type="list(object)">
  The real storage configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
