---
title: "aws_ivsrealtime_participant_replicas"
description: "`aws_ivsrealtime_participant_replicas` reads the real replicated copies of an IVS real-time stage participant, mirrored into another stage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_participant_replicas"})

    		ubx.Data(ivsrealtime.ParticipantReplicas, "example", ivsrealtime.ParticipantReplicasConfig{
    			ParticipantId:  "example",
    			SourceStageArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ParticipantReplicas } from "@ubx/sdk-aws/aws/data/ivsrealtime/participant_replicas";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_participant_replicas" });

      data(ParticipantReplicas, "example", {
        participantId: "example",
        sourceStageArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import ParticipantReplicas, ParticipantReplicasConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_participant_replicas")

        ubx.data(ParticipantReplicas, "example", ParticipantReplicasConfig(
            participant_id="example",
            source_stage_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="participant_id" type="string" required>
  The real, unique identifier of the participant this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="source_stage_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the source stage this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replicas" type="list(object)">
  The real replicas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
