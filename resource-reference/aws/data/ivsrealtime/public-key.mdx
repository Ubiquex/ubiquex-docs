---
title: "aws_ivsrealtime_public_key"
description: "`data_aws_ivsrealtime_public_key` looks up a real, already-existing `aws_ivs_public_key`."
---

It registers a real public key IVS validates participant tokens for an `aws_ivs_stage` against, the Real-Time Streaming counterpart to `aws_ivs_playback_key_pair`'s own channel-playback authorization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivsrealtime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivsrealtime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivsrealtime_public_key"})

    		ubx.Data(ivsrealtime.PublicKey, "example", ivsrealtime.PublicKeyConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PublicKey } from "@ubx/sdk-aws/aws/data/ivsrealtime/public_key";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivsrealtime_public_key" });

      data(PublicKey, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivsrealtime import PublicKey, PublicKeyConfig

    def describe():
        ubx.intent("look up aws_ivsrealtime_public_key")

        ubx.data(PublicKey, "example", PublicKeyConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="public_key" type="object">
  &lt;p&gt;Object specifying a public key used to sign stage participant tokens.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="fingerprint" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="public_key_material" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
