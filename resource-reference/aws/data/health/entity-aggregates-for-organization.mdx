---
title: "aws_health_entity_aggregates_for_organization"
description: "`aws_health_entity_aggregates_for_organization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_health_entity_aggregates_for_organization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_entity_aggregates_for_organization"})

    		ubx.Data(health.EntityAggregatesForOrganization, "example", health.EntityAggregatesForOrganizationConfig{
    			EventArns:     []string{"example"},
    			AwsAccountIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityAggregatesForOrganization } from "@ubx/sdk-aws/aws/data/health/entity_aggregates_for_organization";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_entity_aggregates_for_organization" });

      data(EntityAggregatesForOrganization, "example", {
        eventArns: ["example"],
        awsAccountIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health import EntityAggregatesForOrganization, EntityAggregatesForOrganizationConfig

    def describe():
        ubx.intent("look up aws_health_entity_aggregates_for_organization")

        ubx.data(EntityAggregatesForOrganization, "example", EntityAggregatesForOrganizationConfig(
            event_arns=["example"],
            aws_account_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="event_arns" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="organization_entity_aggregates" type="list(object)">
  Returned by the lookup.
</ResponseField>
