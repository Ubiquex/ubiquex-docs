---
title: "aws_health_entity_aggregates"
description: "`aws_health_entity_aggregates` reads real, aggregated counts of resources affected by AWS Health events."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_entity_aggregates"})

    		ubx.Data(health.EntityAggregates, "example", health.EntityAggregatesConfig{
    			EventArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EntityAggregates } from "./local-sdk/aws/sdk/typescript/aws/data/health/entity_aggregates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_entity_aggregates" });

      data(EntityAggregates, "example", {
        eventArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.health import EntityAggregates, EntityAggregatesConfig

    def describe():
        ubx.intent("look up aws_health_entity_aggregates")

        ubx.data(EntityAggregates, "example", EntityAggregatesConfig(
            event_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the events this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_aggregates" type="list(object)">
  The real, aggregated entity results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
