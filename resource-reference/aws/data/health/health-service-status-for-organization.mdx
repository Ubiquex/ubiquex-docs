---
title: "aws_health_health_service_status_for_organization"
description: "`aws_health_health_service_status_for_organization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_health_health_service_status_for_organization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_health_service_status_for_organization"})

    		ubx.Data(health.HealthServiceStatusForOrganization, "example", health.HealthServiceStatusForOrganizationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthServiceStatusForOrganization } from "@ubx/sdk-aws/aws/data/health/health_service_status_for_organization";

    export default stack("example", () => {
      intent({
        summary: "look up aws_health_health_service_status_for_organization",
      });

      data(HealthServiceStatusForOrganization, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health import HealthServiceStatusForOrganization, HealthServiceStatusForOrganizationConfig

    def describe():
        ubx.intent("look up aws_health_health_service_status_for_organization")

        ubx.data(HealthServiceStatusForOrganization, "example", HealthServiceStatusForOrganizationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="health_service_access_status_for_organization" type="string">
  Returned by the lookup.
</ResponseField>
