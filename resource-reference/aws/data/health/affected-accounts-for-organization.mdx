---
title: "aws_health_affected_accounts_for_organization"
description: "`aws_health_affected_accounts_for_organization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_health_affected_accounts_for_organization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_affected_accounts_for_organization"})

    		ubx.Data(health.AffectedAccountsForOrganization, "example", health.AffectedAccountsForOrganizationConfig{
    			EventArn:   "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AffectedAccountsForOrganization } from "@ubx/sdk-aws/aws/data/health/affected_accounts_for_organization";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_affected_accounts_for_organization" });

      data(AffectedAccountsForOrganization, "example", {
        eventArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health import AffectedAccountsForOrganization, AffectedAccountsForOrganizationConfig

    def describe():
        ubx.intent("look up aws_health_affected_accounts_for_organization")

        ubx.data(AffectedAccountsForOrganization, "example", AffectedAccountsForOrganizationConfig(
            event_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="affected_accounts" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="event_scope_code" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
