---
title: "aws_health_affected_entities"
description: "`aws_health_affected_entities` reads the real resources affected by an AWS Health event."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_affected_entities"})

    		ubx.Data(health.AffectedEntities, "example", health.AffectedEntitiesConfig{
    			Filter:     map[string]any{"event_arns": []string{"example"}},
    			Locale:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AffectedEntities } from "./local-sdk/aws/sdk/typescript/aws/data/health/affected_entities.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_affected_entities" });

      data(AffectedEntities, "example", {
        filter: { eventArns: ["example"] },
        locale: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.health import AffectedEntities, AffectedEntitiesConfig

    def describe():
        ubx.intent("look up aws_health_affected_entities")

        ubx.data(AffectedEntities, "example", AffectedEntitiesConfig(
            filter={"event_arns": ["example"]},
            locale="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object" required>
  &lt;p&gt;The values to use to filter results from the &lt;a href="https://docs.aws.amazon.com/health/latest/APIReference/API_DescribeAffectedEntities.html"&gt;DescribeAffectedEntities&lt;/a&gt; operation.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="entity_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="entity_values" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="event_arns" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_times" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_codes" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="list(map(string))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entities" type="list(object)">
  The real entities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
