---
title: "aws_health_affected_entities_for_organization"
description: "`aws_health_affected_entities_for_organization` reads the real resources affected by an AWS Health event, across an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	health "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/health"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_health_affected_entities_for_organization"})

    		ubx.Data(health.AffectedEntitiesForOrganization, "example", health.AffectedEntitiesForOrganizationConfig{
    			Locale:                           "example",
    			MaxResults:                       1,
    			OrganizationEntityAccountFilters: []map[string]any{{"aws_account_id": "example"}},
    			OrganizationEntityFilters:        []map[string]any{{"aws_account_id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AffectedEntitiesForOrganization } from "@ubx/sdk-aws/aws/data/health/affected_entities_for_organization";

    export default stack("example", () => {
      intent({ summary: "look up aws_health_affected_entities_for_organization" });

      data(AffectedEntitiesForOrganization, "example", {
        locale: "example",
        maxResults: 1,
        organizationEntityAccountFilters: [{ awsAccountId: "example" }],
        organizationEntityFilters: [{ awsAccountId: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.health import AffectedEntitiesForOrganization, AffectedEntitiesForOrganizationConfig

    def describe():
        ubx.intent("look up aws_health_affected_entities_for_organization")

        ubx.data(AffectedEntitiesForOrganization, "example", AffectedEntitiesForOrganizationConfig(
            locale="example",
            max_results=1,
            organization_entity_account_filters=[{"aws_account_id": "example"}],
            organization_entity_filters=[{"aws_account_id": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_entity_account_filters" type="list(object)">
  The real account filters applied to this organization entity. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_entity_filters" type="list(object)">
  The real filters applied to this organization entity. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entities" type="list(object)">
  The real entities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failed_set" type="list(object)">
  The real failed set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
