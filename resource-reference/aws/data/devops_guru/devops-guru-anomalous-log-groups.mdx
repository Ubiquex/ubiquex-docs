---
title: "aws_devops_guru_anomalous_log_groups"
description: "`aws_devops_guru_anomalous_log_groups` reads the real CloudWatch log groups DevOps Guru found anomalous log patterns in."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_anomalous_log_groups"})

    		ubx.Data(devops_guru.DevopsGuruAnomalousLogGroups, "example", devops_guru.DevopsGuruAnomalousLogGroupsConfig{
    			InsightId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruAnomalousLogGroups } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_anomalous_log_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_anomalous_log_groups" });

      data(DevopsGuruAnomalousLogGroups, "example", {
        insightId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruAnomalousLogGroups, DevopsGuruAnomalousLogGroupsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_anomalous_log_groups")

        ubx.data(DevopsGuruAnomalousLogGroups, "example", DevopsGuruAnomalousLogGroupsConfig(
            insight_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="insight_id" type="string" required>
  The real, unique identifier of the insight this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="anomalous_log_groups" type="list(object)">
  The real anomalous log groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
