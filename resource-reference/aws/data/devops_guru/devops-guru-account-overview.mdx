---
title: "aws_devops_guru_account_overview"
description: "`aws_devops_guru_account_overview` reads a real, aggregated overview of DevOps Guru insights and mean-time-to-recover for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_account_overview"})

    		ubx.Data(devops_guru.DevopsGuruAccountOverview, "example", devops_guru.DevopsGuruAccountOverviewConfig{
    			FromTime: "example",
    			ToTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DevopsGuruAccountOverview } from "./local-sdk/aws/sdk/typescript/aws/data/devops_guru/devops_guru_account_overview.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_account_overview" });

      data(DevopsGuruAccountOverview, "example", {
        fromTime: "example",
        toTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruAccountOverview, DevopsGuruAccountOverviewConfig

    def describe():
        ubx.intent("look up aws_devops_guru_account_overview")

        ubx.data(DevopsGuruAccountOverview, "example", DevopsGuruAccountOverviewConfig(
            from_time="example",
            to_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from_time" type="string" required>
  The real timestamp of the from. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="to_time" type="string">
  The real timestamp of the to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mean_time_to_recover_in_milliseconds" type="number">
  The real, mean time to recover, in milliseconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="proactive_insights" type="number">
  The real, proactively detected insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reactive_insights" type="number">
  The real, reactively detected insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
