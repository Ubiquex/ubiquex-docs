---
title: "aws_devops_guru_account_overview"
description: "`aws_devops_guru_account_overview` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_account_overview` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_account_overview"})

    		ubx.Data(devops_guru.DevopsGuruAccountOverview, "example", devops_guru.DevopsGuruAccountOverviewConfig{
    			FromTime: "example",
    			ToTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruAccountOverview } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_account_overview";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_account_overview" });

      data(DevopsGuruAccountOverview, "example", {
        fromTime: "example",
        toTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruAccountOverview, DevopsGuruAccountOverviewConfig

    def describe():
        ubx.intent("look up aws_devops_guru_account_overview")

        ubx.data(DevopsGuruAccountOverview, "example", DevopsGuruAccountOverviewConfig(
            from_time="example",
            to_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="to_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mean_time_to_recover_in_milliseconds" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="proactive_insights" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reactive_insights" type="number">
  Returned by the lookup.
</ResponseField>
