---
title: "aws_devops_guru_organization_resource_collection_health"
description: "`aws_devops_guru_organization_resource_collection_health` reads real, aggregated DevOps Guru health for a resource collection across an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_organization_resource_collection_health"})

    		ubx.Data(devops_guru.DevopsGuruOrganizationResourceCollectionHealth, "example", devops_guru.DevopsGuruOrganizationResourceCollectionHealthConfig{
    			OrganizationResourceCollectionType: "example",
    			AccountIds:                         []string{"example"},
    			MaxResults:                         1,
    			OrganizationalUnitIds:              []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruOrganizationResourceCollectionHealth } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_organization_resource_collection_health";

    export default stack("example", () => {
      intent({
        summary: "look up aws_devops_guru_organization_resource_collection_health",
      });

      data(DevopsGuruOrganizationResourceCollectionHealth, "example", {
        organizationResourceCollectionType: "example",
        accountIds: ["example"],
        maxResults: 1,
        organizationalUnitIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruOrganizationResourceCollectionHealth, DevopsGuruOrganizationResourceCollectionHealthConfig

    def describe():
        ubx.intent("look up aws_devops_guru_organization_resource_collection_health")

        ubx.data(DevopsGuruOrganizationResourceCollectionHealth, "example", DevopsGuruOrganizationResourceCollectionHealthConfig(
            organization_resource_collection_type="example",
            account_ids=["example"],
            max_results=1,
            organizational_unit_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_resource_collection_type" type="string" required>
  The real, discriminating type of the organization resource collection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organizational_unit_ids" type="list(string)">
  The real, unique identifiers of the AWS Organizations organizational units this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account" type="list(object)">
  The real AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="cloud_formation" type="list(object)">
  The real CloudFormation stack associated with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service" type="list(object)">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
