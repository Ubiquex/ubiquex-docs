---
title: "aws_devops_guru_resource_collection_health"
description: "`data_aws_devops_guru_resource_collection_health` looks up a real, already-existing `aws_dev_ops_guru_resource_collection`."
---

It scopes which real AWS resources, identified by CloudFormation stack or by tag, DevOps Guru actually analyzes CloudWatch metrics and CloudTrail events for, so anomaly detection covers the resources that matter rather than every resource in the account indiscriminately.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_resource_collection_health"})

    		ubx.Data(devops_guru.DevopsGuruResourceCollectionHealth, "example", devops_guru.DevopsGuruResourceCollectionHealthConfig{
    			ResourceCollectionType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruResourceCollectionHealth } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_resource_collection_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_resource_collection_health" });

      data(DevopsGuruResourceCollectionHealth, "example", {
        resourceCollectionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruResourceCollectionHealth, DevopsGuruResourceCollectionHealthConfig

    def describe():
        ubx.intent("look up aws_devops_guru_resource_collection_health")

        ubx.data(DevopsGuruResourceCollectionHealth, "example", DevopsGuruResourceCollectionHealthConfig(
            resource_collection_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_collection_type" type="string" required>
  The real, discriminating type of the resource collection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_formation" type="list(object)">
  The real CloudFormation stack associated with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service" type="list(object)">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
