---
title: "aws_devops_guru_organization_overview"
description: "`aws_devops_guru_organization_overview` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_organization_overview` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_organization_overview"})

    		ubx.Data(devops_guru.DevopsGuruOrganizationOverview, "example", devops_guru.DevopsGuruOrganizationOverviewConfig{
    			FromTime:              "example",
    			AccountIds:            []string{"example"},
    			OrganizationalUnitIds: []string{"example"},
    			ToTime:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruOrganizationOverview } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_organization_overview";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_organization_overview" });

      data(DevopsGuruOrganizationOverview, "example", {
        fromTime: "example",
        accountIds: ["example"],
        organizationalUnitIds: ["example"],
        toTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruOrganizationOverview, DevopsGuruOrganizationOverviewConfig

    def describe():
        ubx.intent("look up aws_devops_guru_organization_overview")

        ubx.data(DevopsGuruOrganizationOverview, "example", DevopsGuruOrganizationOverviewConfig(
            from_time="example",
            account_ids=["example"],
            organizational_unit_ids=["example"],
            to_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="from_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="organizational_unit_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="to_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="proactive_insights" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reactive_insights" type="number">
  Returned by the lookup.
</ResponseField>
