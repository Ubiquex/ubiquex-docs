---
title: "aws_devops_guru_organization_health"
description: "`aws_devops_guru_organization_health` reads real, aggregated DevOps Guru insight health across an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_organization_health"})

    		ubx.Data(devops_guru.DevopsGuruOrganizationHealth, "example", devops_guru.DevopsGuruOrganizationHealthConfig{
    			AccountIds:            []string{"example"},
    			OrganizationalUnitIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DevopsGuruOrganizationHealth } from "./local-sdk/aws/sdk/typescript/aws/data/devops_guru/devops_guru_organization_health.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_organization_health" });

      data(DevopsGuruOrganizationHealth, "example", {
        accountIds: ["example"],
        organizationalUnitIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruOrganizationHealth, DevopsGuruOrganizationHealthConfig

    def describe():
        ubx.intent("look up aws_devops_guru_organization_health")

        ubx.data(DevopsGuruOrganizationHealth, "example", DevopsGuruOrganizationHealthConfig(
            account_ids=["example"],
            organizational_unit_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organizational_unit_ids" type="list(string)">
  The real, unique identifiers of the AWS Organizations organizational units this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metrics_analyzed" type="number">
  The real, total number of metrics analyzed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="open_proactive_insights" type="number">
  The real, currently open proactive insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="open_reactive_insights" type="number">
  The real, currently open reactive insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_hours" type="number">
  The real, total resource-hours consumed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
