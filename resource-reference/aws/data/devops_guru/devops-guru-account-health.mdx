---
title: "aws_devops_guru_account_health"
description: "`aws_devops_guru_account_health` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_account_health` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_account_health"})

    		ubx.Data(devops_guru.DevopsGuruAccountHealth, "example", devops_guru.DevopsGuruAccountHealthConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruAccountHealth } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_account_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_account_health" });

      data(DevopsGuruAccountHealth, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruAccountHealth, DevopsGuruAccountHealthConfig

    def describe():
        ubx.intent("look up aws_devops_guru_account_health")

        ubx.data(DevopsGuruAccountHealth, "example", DevopsGuruAccountHealthConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="analyzed_resource_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="metrics_analyzed" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="open_proactive_insights" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="open_reactive_insights" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_hours" type="number">
  Returned by the lookup.
</ResponseField>
