---
title: "aws_devops_guru_recommendations"
description: "`aws_devops_guru_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_recommendations"})

    		ubx.Data(devops_guru.DevopsGuruRecommendations, "example", devops_guru.DevopsGuruRecommendationsConfig{
    			InsightId: "example",
    			AccountId: "example",
    			Locale:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruRecommendations } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_recommendations" });

      data(DevopsGuruRecommendations, "example", {
        insightId: "example",
        accountId: "example",
        locale: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruRecommendations, DevopsGuruRecommendationsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_recommendations")

        ubx.data(DevopsGuruRecommendations, "example", DevopsGuruRecommendationsConfig(
            insight_id="example",
            account_id="example",
            locale="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="insight_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="locale" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
