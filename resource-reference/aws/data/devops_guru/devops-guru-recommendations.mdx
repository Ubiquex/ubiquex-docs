---
title: "aws_devops_guru_recommendations"
description: "`aws_devops_guru_recommendations` reads real, system-generated remediation recommendations for a DevOps Guru insight."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_recommendations"})

    		ubx.Data(devops_guru.DevopsGuruRecommendations, "example", devops_guru.DevopsGuruRecommendationsConfig{
    			InsightId: "example",
    			AccountId: "example",
    			Locale:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruRecommendations } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_recommendations" });

      data(DevopsGuruRecommendations, "example", {
        insightId: "example",
        accountId: "example",
        locale: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruRecommendations, DevopsGuruRecommendationsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_recommendations")

        ubx.data(DevopsGuruRecommendations, "example", DevopsGuruRecommendationsConfig(
            insight_id="example",
            account_id="example",
            locale="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="insight_id" type="string" required>
  The real, unique identifier of the insight this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendations" type="list(object)">
  The real recommendations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
