---
title: "aws_devops_guru_monitored_resources"
description: "`aws_devops_guru_monitored_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_monitored_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_monitored_resources"})

    		ubx.Data(devops_guru.DevopsGuruMonitoredResources, "example", devops_guru.DevopsGuruMonitoredResourcesConfig{
    			Filters:    map[string]any{"resource_permission": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruMonitoredResources } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_monitored_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_monitored_resources" });

      data(DevopsGuruMonitoredResources, "example", {
        filters: { resourcePermission: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruMonitoredResources, DevopsGuruMonitoredResourcesConfig

    def describe():
        ubx.intent("look up aws_devops_guru_monitored_resources")

        ubx.data(DevopsGuruMonitoredResources, "example", DevopsGuruMonitoredResourcesConfig(
            filters={"resource_permission": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt; Filters to determine which monitored resources you want to retrieve. You can filter by resource type or resource permission status. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="resource_permission" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_type_filters" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="monitored_resource_identifiers" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
