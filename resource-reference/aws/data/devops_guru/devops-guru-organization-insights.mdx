---
title: "aws_devops_guru_organization_insights"
description: "`aws_devops_guru_organization_insights` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_organization_insights` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_organization_insights"})

    		ubx.Data(devops_guru.DevopsGuruOrganizationInsights, "example", devops_guru.DevopsGuruOrganizationInsightsConfig{
    			StatusFilter:          map[string]any{"any": map[string]any{"start_time_range": map[string]any{"from_time": "example"}}},
    			AccountIds:            []string{"example"},
    			MaxResults:            1,
    			OrganizationalUnitIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruOrganizationInsights } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_organization_insights";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_organization_insights" });

      data(DevopsGuruOrganizationInsights, "example", {
        statusFilter: { any: { startTimeRange: { fromTime: "example" } } },
        accountIds: ["example"],
        maxResults: 1,
        organizationalUnitIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruOrganizationInsights, DevopsGuruOrganizationInsightsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_organization_insights")

        ubx.data(DevopsGuruOrganizationInsights, "example", DevopsGuruOrganizationInsightsConfig(
            status_filter={"any": {"start_time_range": {"from_time": "example"}}},
            account_ids=["example"],
            max_results=1,
            organizational_unit_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organizational_unit_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="object" required>
  &lt;p&gt; A filter used by &lt;code&gt;ListInsights&lt;/code&gt; to specify which insights to return. &lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="any" type="object">
      &lt;p&gt; Used to filter for insights that have any status. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="start_time_range" type="object" required>
          &lt;p&gt; A time range used to specify when the behavior of an insight or anomaly started. &lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="from_time" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="to_time" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="closed" type="object">
      &lt;p&gt; Used to filter for insights that have the status &lt;code&gt;CLOSED&lt;/code&gt;. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_time_range" type="object" required>
          &lt;p&gt; A range of time that specifies when anomalous behavior in an anomaly or insight ended. &lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="from_time" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="to_time" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ongoing" type="object">
      &lt;p&gt; Used to filter for insights that have the status &lt;code&gt;ONGOING&lt;/code&gt;. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="proactive_insights" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reactive_insights" type="list(object)">
  Returned by the lookup.
</ResponseField>
