---
title: "aws_devops_guru_notification_channels"
description: "`data_aws_devops_guru_notification_channels` looks up a real, already-existing `aws_dev_ops_guru_notification_channel`."
---

It sends a real SNS notification, filterable by severity, the moment DevOps Guru creates a new insight, up to two topics configurable so the right people or systems find out about a detected operational anomaly without polling the console.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_notification_channels"})

    		ubx.Data(devops_guru.DevopsGuruNotificationChannels, "example", devops_guru.DevopsGuruNotificationChannelsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DevopsGuruNotificationChannels } from "./local-sdk/aws/sdk/typescript/aws/data/devops_guru/devops_guru_notification_channels.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_notification_channels" });

      data(DevopsGuruNotificationChannels, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruNotificationChannels, DevopsGuruNotificationChannelsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_notification_channels")

        ubx.data(DevopsGuruNotificationChannels, "example", DevopsGuruNotificationChannelsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="channels" type="list(object)">
  The real channels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
