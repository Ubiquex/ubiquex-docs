---
title: "aws_devops_guru_notification_channels"
description: "`aws_devops_guru_notification_channels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_notification_channels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_notification_channels"})

    		ubx.Data(devops_guru.DevopsGuruNotificationChannels, "example", devops_guru.DevopsGuruNotificationChannelsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruNotificationChannels } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_notification_channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_notification_channels" });

      data(DevopsGuruNotificationChannels, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruNotificationChannels, DevopsGuruNotificationChannelsConfig

    def describe():
        ubx.intent("look up aws_devops_guru_notification_channels")

        ubx.data(DevopsGuruNotificationChannels, "example", DevopsGuruNotificationChannelsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="channels" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
