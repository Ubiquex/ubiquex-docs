---
title: "aws_devops_guru_anomalies_for_insight"
description: "`aws_devops_guru_anomalies_for_insight` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_devops_guru_anomalies_for_insight` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devops_guru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/devops_guru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_devops_guru_anomalies_for_insight"})

    		ubx.Data(devops_guru.DevopsGuruAnomaliesForInsight, "example", devops_guru.DevopsGuruAnomaliesForInsightConfig{
    			InsightId:      "example",
    			AccountId:      "example",
    			Filters:        map[string]any{"service_collection": map[string]any{"service_names": []string{"example"}}},
    			MaxResults:     1,
    			StartTimeRange: map[string]any{"from_time": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevopsGuruAnomaliesForInsight } from "@ubx/sdk-aws/aws/data/devops_guru/devops_guru_anomalies_for_insight";

    export default stack("example", () => {
      intent({ summary: "look up aws_devops_guru_anomalies_for_insight" });

      data(DevopsGuruAnomaliesForInsight, "example", {
        insightId: "example",
        accountId: "example",
        filters: { serviceCollection: { serviceNames: ["example"] } },
        maxResults: 1,
        startTimeRange: { fromTime: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.devops_guru import DevopsGuruAnomaliesForInsight, DevopsGuruAnomaliesForInsightConfig

    def describe():
        ubx.intent("look up aws_devops_guru_anomalies_for_insight")

        ubx.data(DevopsGuruAnomaliesForInsight, "example", DevopsGuruAnomaliesForInsightConfig(
            insight_id="example",
            account_id="example",
            filters={"service_collection": {"service_names": ["example"]}},
            max_results=1,
            start_time_range={"from_time": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt; Specifies one or more service names that are used to list anomalies. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="service_collection" type="object">
      &lt;p&gt;A collection of the names of Amazon Web Services services.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="service_names" type="list(string)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="insight_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time_range" type="object">
  &lt;p&gt; A time range used to specify when the behavior of an insight or anomaly started. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="from_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="to_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="proactive_anomalies" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reactive_anomalies" type="list(object)">
  Returned by the lookup.
</ResponseField>
