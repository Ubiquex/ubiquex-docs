---
title: "aws_kms_retirable_grants"
description: "`aws_kms_retirable_grants` reads the real KMS grants the caller is permitted to retire."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kms_retirable_grants"})

    		ubx.Data(kms.RetirableGrants, "example", kms.RetirableGrantsConfig{
    			Limit:                    1,
    			Marker:                   "example",
    			RetiringPrincipal:        "example",
    			RetiringServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RetirableGrants } from "./local-sdk/aws/sdk/typescript/aws/data/kms/retirable_grants.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kms_retirable_grants" });

      data(RetirableGrants, "example", {
        limit: 1,
        marker: "example",
        retiringPrincipal: "example",
        retiringServicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kms import RetirableGrants, RetirableGrantsConfig

    def describe():
        ubx.intent("look up aws_kms_retirable_grants")

        ubx.data(RetirableGrants, "example", RetirableGrantsConfig(
            limit=1,
            marker="example",
            retiring_principal="example",
            retiring_service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="retiring_principal" type="string">
  The real IAM principal that can retire this AWS License Manager grant. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="retiring_service_principal" type="string">
  The real AWS service principal that can retire this AWS License Manager grant. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grants" type="list(object)">
  The real grants included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
