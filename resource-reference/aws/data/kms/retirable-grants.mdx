---
title: "aws_kms_retirable_grants"
description: "`aws_kms_retirable_grants` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kms_retirable_grants` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kms_retirable_grants"})

    		ubx.Data(kms.RetirableGrants, "example", kms.RetirableGrantsConfig{
    			Limit:                    1,
    			Marker:                   "example",
    			RetiringPrincipal:        "example",
    			RetiringServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RetirableGrants } from "@ubx/sdk-aws/aws/data/kms/retirable_grants";

    export default stack("example", () => {
      intent({ summary: "look up aws_kms_retirable_grants" });

      data(RetirableGrants, "example", {
        limit: 1,
        marker: "example",
        retiringPrincipal: "example",
        retiringServicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kms import RetirableGrants, RetirableGrantsConfig

    def describe():
        ubx.intent("look up aws_kms_retirable_grants")

        ubx.data(RetirableGrants, "example", RetirableGrantsConfig(
            limit=1,
            marker="example",
            retiring_principal="example",
            retiring_service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="retiring_principal" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="retiring_service_principal" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grants" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="truncated" type="bool">
  Returned by the lookup.
</ResponseField>
