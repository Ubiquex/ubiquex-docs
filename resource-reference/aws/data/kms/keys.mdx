---
title: "aws_kms_keys"
description: "`data_aws_kms_keys` looks up a real, already-existing `aws_kms_key`."
---

It creates a real cryptographic key in AWS Key Management Service, symmetric encryption keys, asymmetric encryption or signing keys, or symmetric HMAC keys, and can create the multi-Region primary key that an `aws_kms_replica_key` replicates from. Changing a key's spec, usage, origin, or multi-Region setting on an existing key is blocked outright, a deliberate guard against silently destroying key material.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kms_keys"})

    		ubx.Data(kms.Keys, "example", kms.KeysConfig{
    			Limit:  1,
    			Marker: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Keys } from "./local-sdk/aws/sdk/typescript/aws/data/kms/keys.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kms_keys" });

      data(Keys, "example", {
        limit: 1,
        marker: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kms import Keys, KeysConfig

    def describe():
        ubx.intent("look up aws_kms_keys")

        ubx.data(Keys, "example", KeysConfig(
            limit=1,
            marker="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="keys" type="list(object)">
  The real, unique keys of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
