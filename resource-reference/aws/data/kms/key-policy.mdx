---
title: "aws_kms_key_policy"
description: "`data_aws_kms_key_policy` looks up a real, already-existing `aws_kms_key`."
---

It creates a real cryptographic key in AWS Key Management Service, symmetric encryption keys, asymmetric encryption or signing keys, or symmetric HMAC keys, and can create the multi-Region primary key that an `aws_kms_replica_key` replicates from. Changing a key's spec, usage, origin, or multi-Region setting on an existing key is blocked outright, a deliberate guard against silently destroying key material.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kms_key_policy"})

    		ubx.Data(kms.KeyPolicy, "example", kms.KeyPolicyConfig{
    			KeyId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeyPolicy } from "@ubx/sdk-aws/aws/data/kms/key_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_kms_key_policy" });

      data(KeyPolicy, "example", {
        keyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kms import KeyPolicy, KeyPolicyConfig

    def describe():
        ubx.intent("look up aws_kms_key_policy")

        ubx.data(KeyPolicy, "example", KeyPolicyConfig(
            key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key_id" type="string" required>
  The real, unique identifier of the encryption key this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_name" type="string">
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
