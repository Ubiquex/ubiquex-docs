---
title: "aws_agent_registry_discoverable_registry_records"
description: "`data_aws_agent_registry_discoverable_registry_records` looks up a real, already-existing `aws_agent_registry_registry_record`."
---

It represents the metadata for one individual resource, an agent, a tool, an MCP server, published into an `aws_agent_registry_registry`. It's the actual catalog entry a consumer discovers and resolves, not the underlying resource itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agent_registry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agent_registry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agent_registry_discoverable_registry_records"})

    		ubx.Data(agent_registry.AgentRegistryDiscoverableRegistryRecords, "example", agent_registry.AgentRegistryDiscoverableRegistryRecordsConfig{
    			RegistryId: "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRegistryDiscoverableRegistryRecords } from "@ubx/sdk-aws/aws/data/agent_registry/agent_registry_discoverable_registry_records";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agent_registry_discoverable_registry_records",
      });

      data(AgentRegistryDiscoverableRegistryRecords, "example", {
        registryId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agent_registry import AgentRegistryDiscoverableRegistryRecords, AgentRegistryDiscoverableRegistryRecordsConfig

    def describe():
        ubx.intent("look up aws_agent_registry_discoverable_registry_records")

        ubx.data(AgentRegistryDiscoverableRegistryRecords, "example", AgentRegistryDiscoverableRegistryRecordsConfig(
            registry_id="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  &lt;p&gt; The list of filters applied to a &lt;code&gt;ListDiscoverableRegistryRecords&lt;/code&gt; request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string" required>
  &lt;p&gt;Registry identifier that accepts either ARN or ID format&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registry_records" type="list(object)">
  The real registry records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
