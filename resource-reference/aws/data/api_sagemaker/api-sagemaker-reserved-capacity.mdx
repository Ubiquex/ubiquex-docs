---
title: "aws_api_sagemaker_reserved_capacity"
description: "`aws_api_sagemaker_reserved_capacity` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_reserved_capacity` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_reserved_capacity"})

    		ubx.Data(api_sagemaker.ApiSagemakerReservedCapacity, "example", api_sagemaker.ApiSagemakerReservedCapacityConfig{
    			ReservedCapacityArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerReservedCapacity } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_reserved_capacity";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_reserved_capacity" });

      data(ApiSagemakerReservedCapacity, "example", {
        reservedCapacityArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerReservedCapacity, ApiSagemakerReservedCapacityConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_reserved_capacity")

        ubx.data(ApiSagemakerReservedCapacity, "example", ApiSagemakerReservedCapacityConfig(
            reserved_capacity_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="reserved_capacity_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="availability_zone" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="available_instance_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="duration_hours" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="duration_minutes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="in_use_instance_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="instance_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_capacity_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_instance_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="ultra_server_summary" type="object">
  &lt;p&gt;A summary of UltraServer resources and their current status.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="available_spare_instance_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="ultra_server_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ultra_server_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="unhealthy_instance_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
