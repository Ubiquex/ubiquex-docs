---
title: "aws_api_sagemaker_training_plans"
description: "`aws_api_sagemaker_training_plans` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_training_plans` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_training_plans"})

    		ubx.Data(api_sagemaker.ApiSagemakerTrainingPlans, "example", api_sagemaker.ApiSagemakerTrainingPlansConfig{
    			Filters:         []map[string]any{{"name": "example-name"}},
    			MaxResults:      1,
    			SortBy:          "example",
    			SortOrder:       "example",
    			StartTimeAfter:  "example",
    			StartTimeBefore: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerTrainingPlans } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_training_plans";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_training_plans" });

      data(ApiSagemakerTrainingPlans, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        startTimeAfter: "example",
        startTimeBefore: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerTrainingPlans, ApiSagemakerTrainingPlansConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_training_plans")

        ubx.data(ApiSagemakerTrainingPlans, "example", ApiSagemakerTrainingPlansConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            sort_by="example",
            sort_order="example",
            start_time_after="example",
            start_time_before="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time_before" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="training_plan_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
