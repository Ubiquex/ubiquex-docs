---
title: "aws_api_sagemaker_feature_metadata"
description: "`aws_api_sagemaker_feature_metadata` reads real, descriptive metadata attached to one feature within a SageMaker Feature Store feature group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_feature_metadata"})

    		ubx.Data(api_sagemaker.ApiSagemakerFeatureMetadata, "example", api_sagemaker.ApiSagemakerFeatureMetadataConfig{
    			FeatureGroupName: "example-feature-group-name",
    			FeatureName:      "example-feature-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerFeatureMetadata } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_feature_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_feature_metadata" });

      data(ApiSagemakerFeatureMetadata, "example", {
        featureGroupName: "example-feature-group-name",
        featureName: "example-feature-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerFeatureMetadata, ApiSagemakerFeatureMetadataConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_feature_metadata")

        ubx.data(ApiSagemakerFeatureMetadata, "example", ApiSagemakerFeatureMetadataConfig(
            feature_group_name="example-feature-group-name",
            feature_name="example-feature-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="feature_group_name" type="string" required>
  The real name of the feature group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="feature_name" type="string" required>
  The real name of the feature this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="feature_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the feature group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="feature_type" type="string">
  The real, discriminating type of the feature this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
