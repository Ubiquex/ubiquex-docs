---
title: "aws_api_sagemaker_devices"
description: "`aws_api_sagemaker_devices` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_devices` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_devices"})

    		ubx.Data(api_sagemaker.ApiSagemakerDevices, "example", api_sagemaker.ApiSagemakerDevicesConfig{
    			DeviceFleetName:      "example-device-fleet-name",
    			LatestHeartbeatAfter: "example",
    			MaxResults:           1,
    			ModelName:            "example-model-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerDevices } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_devices" });

      data(ApiSagemakerDevices, "example", {
        deviceFleetName: "example-device-fleet-name",
        latestHeartbeatAfter: "example",
        maxResults: 1,
        modelName: "example-model-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerDevices, ApiSagemakerDevicesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_devices")

        ubx.data(ApiSagemakerDevices, "example", ApiSagemakerDevicesConfig(
            device_fleet_name="example-device-fleet-name",
            latest_heartbeat_after="example",
            max_results=1,
            model_name="example-model-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_fleet_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="latest_heartbeat_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="device_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
