---
title: "aws_api_sagemaker_aliases"
description: "`aws_api_sagemaker_aliases` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_aliases` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_aliases"})

    		ubx.Data(api_sagemaker.ApiSagemakerAliases, "example", api_sagemaker.ApiSagemakerAliasesConfig{
    			ImageName:  "example-image-name",
    			Alias:      "example",
    			MaxResults: 1,
    			Version:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerAliases } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_aliases";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_aliases" });

      data(ApiSagemakerAliases, "example", {
        imageName: "example-image-name",
        alias: "example",
        maxResults: 1,
        version: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerAliases, ApiSagemakerAliasesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_aliases")

        ubx.data(ApiSagemakerAliases, "example", ApiSagemakerAliasesConfig(
            image_name="example-image-name",
            alias="example",
            max_results=1,
            version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alias" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="image_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="version" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sage_maker_image_version_aliases" type="list(string)">
  Returned by the lookup.
</ResponseField>
