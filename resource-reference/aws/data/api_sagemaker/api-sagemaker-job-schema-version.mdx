---
title: "aws_api_sagemaker_job_schema_version"
description: "`aws_api_sagemaker_job_schema_version` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_job_schema_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_job_schema_version"})

    		ubx.Data(api_sagemaker.ApiSagemakerJobSchemaVersion, "example", api_sagemaker.ApiSagemakerJobSchemaVersionConfig{
    			JobCategory: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerJobSchemaVersion } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_job_schema_version";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_job_schema_version" });

      data(ApiSagemakerJobSchemaVersion, "example", {
        jobCategory: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerJobSchemaVersion, ApiSagemakerJobSchemaVersionConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_job_schema_version")

        ubx.data(ApiSagemakerJobSchemaVersion, "example", ApiSagemakerJobSchemaVersionConfig(
            job_category="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_category" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_config_schema" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_config_schema_version" type="string">
  Returned by the lookup.
</ResponseField>
