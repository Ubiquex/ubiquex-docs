---
title: "aws_api_sagemaker_stage_devices"
description: "`aws_api_sagemaker_stage_devices` reads the real edge devices within one deployment stage of a SageMaker Edge Manager deployment plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_stage_devices"})

    		ubx.Data(api_sagemaker.ApiSagemakerStageDevices, "example", api_sagemaker.ApiSagemakerStageDevicesConfig{
    			EdgeDeploymentPlanName:             "example-edge-deployment-plan-name",
    			StageName:                          "example-stage-name",
    			ExcludeDevicesDeployedInOtherStage: true,
    			MaxResults:                         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerStageDevices } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_stage_devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_stage_devices" });

      data(ApiSagemakerStageDevices, "example", {
        edgeDeploymentPlanName: "example-edge-deployment-plan-name",
        stageName: "example-stage-name",
        excludeDevicesDeployedInOtherStage: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerStageDevices, ApiSagemakerStageDevicesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_stage_devices")

        ubx.data(ApiSagemakerStageDevices, "example", ApiSagemakerStageDevicesConfig(
            edge_deployment_plan_name="example-edge-deployment-plan-name",
            stage_name="example-stage-name",
            exclude_devices_deployed_in_other_stage=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="edge_deployment_plan_name" type="string" required>
  The real name of the edge deployment plan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="exclude_devices_deployed_in_other_stage" type="bool">
  Whether devices already deployed to another stage are excluded from the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stage_name" type="string" required>
  The real name of the stage this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="device_deployment_summaries" type="list(object)">
  The real, summarized device deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
