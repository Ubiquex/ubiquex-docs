---
title: "aws_api_sagemaker_lineage_groups"
description: "`aws_api_sagemaker_lineage_groups` reads the real SageMaker ML Lineage groups on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_lineage_groups"})

    		ubx.Data(api_sagemaker.ApiSagemakerLineageGroups, "example", api_sagemaker.ApiSagemakerLineageGroupsConfig{
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    			SortBy:        "example",
    			SortOrder:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerLineageGroups } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_lineage_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_lineage_groups" });

      data(ApiSagemakerLineageGroups, "example", {
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerLineageGroups, ApiSagemakerLineageGroupsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_lineage_groups")

        ubx.data(ApiSagemakerLineageGroups, "example", ApiSagemakerLineageGroupsConfig(
            created_after="example",
            created_before="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lineage_group_summaries" type="list(object)">
  The real, summarized lineage groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
