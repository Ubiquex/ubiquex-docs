---
title: "aws_api_sagemaker_cluster_nodes"
description: "`aws_api_sagemaker_cluster_nodes` reads the real compute nodes within a SageMaker HyperPod cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_cluster_nodes"})

    		ubx.Data(api_sagemaker.ApiSagemakerClusterNodes, "example", api_sagemaker.ApiSagemakerClusterNodesConfig{
    			ClusterName:               "example-cluster-name",
    			CreationTimeAfter:         "example",
    			CreationTimeBefore:        "example",
    			IncludeNodeLogicalIds:     true,
    			InstanceGroupNameContains: "example",
    			MaxResults:                1,
    			SortBy:                    "example",
    			SortOrder:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerClusterNodes } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_cluster_nodes";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_cluster_nodes" });

      data(ApiSagemakerClusterNodes, "example", {
        clusterName: "example-cluster-name",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        includeNodeLogicalIds: true,
        instanceGroupNameContains: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerClusterNodes, ApiSagemakerClusterNodesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_cluster_nodes")

        ubx.data(ApiSagemakerClusterNodes, "example", ApiSagemakerClusterNodesConfig(
            cluster_name="example-cluster-name",
            creation_time_after="example",
            creation_time_before="example",
            include_node_logical_ids=True,
            instance_group_name_contains="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="creation_time_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_node_logical_ids" type="bool">
  The real, unique identifiers of the include node logicals this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_group_name_contains" type="string">
  Restricts results to instance groups whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_node_summaries" type="list(object)">
  The real, summarized cluster nodes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
