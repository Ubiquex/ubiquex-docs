---
title: "aws_api_sagemaker_pipeline_versions"
description: "`aws_api_sagemaker_pipeline_versions` reads a real, saved version of a SageMaker Pipelines workflow definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_pipeline_versions"})

    		ubx.Data(api_sagemaker.ApiSagemakerPipelineVersions, "example", api_sagemaker.ApiSagemakerPipelineVersionsConfig{
    			PipelineName:  "example-pipeline-name",
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    			SortOrder:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerPipelineVersions } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_pipeline_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_pipeline_versions" });

      data(ApiSagemakerPipelineVersions, "example", {
        pipelineName: "example-pipeline-name",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerPipelineVersions, ApiSagemakerPipelineVersionsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_pipeline_versions")

        ubx.data(ApiSagemakerPipelineVersions, "example", ApiSagemakerPipelineVersionsConfig(
            pipeline_name="example-pipeline-name",
            created_after="example",
            created_before="example",
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pipeline_name" type="string" required>
  The real name of the pipeline this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_version_summaries" type="list(object)">
  The real, summarized pipeline versions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
