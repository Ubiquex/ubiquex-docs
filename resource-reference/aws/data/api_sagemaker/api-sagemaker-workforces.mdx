---
title: "aws_api_sagemaker_workforces"
description: "`aws_api_sagemaker_workforces` looks up a real SageMaker Ground Truth workforce -- the pool of human workers (private, vendor..."
---

`aws_api_sagemaker_workforces` looks up a real SageMaker Ground Truth workforce -- the pool of human workers (private, vendor, or Mechanical Turk) available to a labeling job.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_workforces"})

    		ubx.Data(api_sagemaker.ApiSagemakerWorkforces, "example", api_sagemaker.ApiSagemakerWorkforcesConfig{
    			MaxResults:   1,
    			NameContains: "example",
    			SortBy:       "example",
    			SortOrder:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerWorkforces } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_workforces";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_workforces" });

      data(ApiSagemakerWorkforces, "example", {
        maxResults: 1,
        nameContains: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerWorkforces, ApiSagemakerWorkforcesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_workforces")

        ubx.data(ApiSagemakerWorkforces, "example", ApiSagemakerWorkforcesConfig(
            max_results=1,
            name_contains="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workforces" type="list(object)">
  The real SageMaker Ground Truth workforces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
