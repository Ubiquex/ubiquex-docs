---
title: "aws_api_sagemaker_training_jobs_for_hyper_parameter_tuning_job"
description: "`aws_api_sagemaker_training_jobs_for_hyper_parameter_tuning_job` reads the real training jobs launched by a SageMaker hyperparameter tuning job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_training_jobs_for_hyper_parameter_tuning_job"})

    		ubx.Data(api_sagemaker.ApiSagemakerTrainingJobsForHyperParameterTuningJob, "example", api_sagemaker.ApiSagemakerTrainingJobsForHyperParameterTuningJobConfig{
    			HyperParameterTuningJobName: "example-hyper-parameter-tuning-job-name",
    			MaxResults:                  1,
    			SortBy:                      "example",
    			SortOrder:                   "example",
    			StatusEquals:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerTrainingJobsForHyperParameterTuningJob } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_training_jobs_for_hyper_parameter_tuning_job.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_api_sagemaker_training_jobs_for_hyper_parameter_tuning_job",
      });

      data(ApiSagemakerTrainingJobsForHyperParameterTuningJob, "example", {
        hyperParameterTuningJobName: "example-hyper-parameter-tuning-job-name",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        statusEquals: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerTrainingJobsForHyperParameterTuningJob, ApiSagemakerTrainingJobsForHyperParameterTuningJobConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_training_jobs_for_hyper_parameter_tuning_job")

        ubx.data(ApiSagemakerTrainingJobsForHyperParameterTuningJob, "example", ApiSagemakerTrainingJobsForHyperParameterTuningJobConfig(
            hyper_parameter_tuning_job_name="example-hyper-parameter-tuning-job-name",
            max_results=1,
            sort_by="example",
            sort_order="example",
            status_equals="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="hyper_parameter_tuning_job_name" type="string" required>
  The real name of the hyper parameter tuning job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status_equals" type="string">
  Restricts results to objects whose status equals this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="training_job_summaries" type="list(object)">
  The real, summarized training jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
