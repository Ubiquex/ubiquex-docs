---
title: "aws_api_sagemaker_inference_recommendations_job_steps"
description: "`aws_api_sagemaker_inference_recommendations_job_steps` reads one real benchmarking step within a SageMaker Inference Recommender job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_inference_recommendations_job_steps"})

    		ubx.Data(api_sagemaker.ApiSagemakerInferenceRecommendationsJobSteps, "example", api_sagemaker.ApiSagemakerInferenceRecommendationsJobStepsConfig{
    			JobName:    "example-job-name",
    			MaxResults: 1,
    			Status:     "example",
    			StepType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerInferenceRecommendationsJobSteps } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_inference_recommendations_job_steps.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_api_sagemaker_inference_recommendations_job_steps",
      });

      data(ApiSagemakerInferenceRecommendationsJobSteps, "example", {
        jobName: "example-job-name",
        maxResults: 1,
        status: "example",
        stepType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerInferenceRecommendationsJobSteps, ApiSagemakerInferenceRecommendationsJobStepsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_inference_recommendations_job_steps")

        ubx.data(ApiSagemakerInferenceRecommendationsJobSteps, "example", ApiSagemakerInferenceRecommendationsJobStepsConfig(
            job_name="example-job-name",
            max_results=1,
            status="example",
            step_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_name" type="string" required>
  The real name of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="step_type" type="string">
  The real, discriminating type of the step this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  The real, ordered steps included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
