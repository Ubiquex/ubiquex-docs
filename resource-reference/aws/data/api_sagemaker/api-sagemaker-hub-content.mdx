---
title: "aws_api_sagemaker_hub_content"
description: "`aws_api_sagemaker_hub_content` looks up one real piece of content (a model or notebook) published in a SageMaker JumpStart private hub."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_hub_content"})

    		ubx.Data(api_sagemaker.ApiSagemakerHubContent, "example", api_sagemaker.ApiSagemakerHubContentConfig{
    			HubContentName: "example-hub-content-name",
    			HubContentType: "example",
    			HubName:        "example-hub-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerHubContent } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_hub_content.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_hub_content" });

      data(ApiSagemakerHubContent, "example", {
        hubContentName: "example-hub-content-name",
        hubContentType: "example",
        hubName: "example-hub-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerHubContent, ApiSagemakerHubContentConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_hub_content")

        ubx.data(ApiSagemakerHubContent, "example", ApiSagemakerHubContentConfig(
            hub_content_name="example-hub-content-name",
            hub_content_type="example",
            hub_name="example-hub-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="hub_content_name" type="string" required>
  The real name of the hub content this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="hub_content_type" type="string" required>
  The real, discriminating type of the hub content this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  The real name of the hub this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document_schema_version" type="string">
  The real version of the document schema. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_arn" type="string">
  The real Amazon Resource Name (ARN) of the hub this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_arn" type="string">
  The real Amazon Resource Name (ARN) of the hub content this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_dependencies" type="list(object)">
  The real dependencies of this SageMaker hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_description" type="string">
  The real, free-text description of the hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_display_name" type="string">
  The real name of the hub content display this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_document" type="string">
  The real document for the hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_markdown" type="string">
  The real, Markdown-formatted documentation for this SageMaker hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_search_keywords" type="list(string)">
  The real search keywords for this SageMaker hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_status" type="string">
  The real, current status of the hub content this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_content_version" type="string">
  The real version of the hub content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reference_min_version" type="string">
  The real version of the reference min. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sage_maker_public_hub_content_arn" type="string">
  The real Amazon Resource Name (ARN) of the sage maker public hub content this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_status" type="string">
  The real, current status of the support this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
