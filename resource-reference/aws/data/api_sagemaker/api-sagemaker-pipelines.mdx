---
title: "aws_api_sagemaker_pipelines"
description: "`aws_api_sagemaker_pipelines` looks up a real SageMaker Pipelines workflow, an orchestrated sequence of ML steps."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_pipelines"})

    		ubx.Data(api_sagemaker.ApiSagemakerPipelines, "example", api_sagemaker.ApiSagemakerPipelinesConfig{
    			CreatedAfter:       "example",
    			CreatedBefore:      "example",
    			MaxResults:         1,
    			PipelineNamePrefix: "example",
    			SortBy:             "example",
    			SortOrder:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerPipelines } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_pipelines";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_pipelines" });

      data(ApiSagemakerPipelines, "example", {
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        pipelineNamePrefix: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerPipelines, ApiSagemakerPipelinesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_pipelines")

        ubx.data(ApiSagemakerPipelines, "example", ApiSagemakerPipelinesConfig(
            created_after="example",
            created_before="example",
            max_results=1,
            pipeline_name_prefix="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pipeline_name_prefix" type="string">
  The real S3 key prefix used for the pipeline name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_summaries" type="list(object)">
  The real, summarized pipelines included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
