---
title: "aws_api_sagemaker_subscribed_workteam"
description: "`aws_api_sagemaker_subscribed_workteam` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_subscribed_workteam` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_subscribed_workteam"})

    		ubx.Data(api_sagemaker.ApiSagemakerSubscribedWorkteam, "example", api_sagemaker.ApiSagemakerSubscribedWorkteamConfig{
    			WorkteamArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerSubscribedWorkteam } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_subscribed_workteam";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_subscribed_workteam" });

      data(ApiSagemakerSubscribedWorkteam, "example", {
        workteamArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerSubscribedWorkteam, ApiSagemakerSubscribedWorkteamConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_subscribed_workteam")

        ubx.data(ApiSagemakerSubscribedWorkteam, "example", ApiSagemakerSubscribedWorkteamConfig(
            workteam_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workteam_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="subscribed_workteam" type="object">
  &lt;p&gt;Describes a work team of a vendor that does the labelling job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="listing_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marketplace_description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marketplace_title" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="seller_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="workteam_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
