---
title: "aws_api_sagemaker_feature_groups"
description: "`aws_api_sagemaker_feature_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_feature_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_feature_groups"})

    		ubx.Data(api_sagemaker.ApiSagemakerFeatureGroups, "example", api_sagemaker.ApiSagemakerFeatureGroupsConfig{
    			CreationTimeAfter:        "example",
    			CreationTimeBefore:       "example",
    			FeatureGroupStatusEquals: "example",
    			MaxResults:               1,
    			NameContains:             "example",
    			OfflineStoreStatusEquals: "example",
    			SortBy:                   "example",
    			SortOrder:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerFeatureGroups } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_feature_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_feature_groups" });

      data(ApiSagemakerFeatureGroups, "example", {
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        featureGroupStatusEquals: "example",
        maxResults: 1,
        nameContains: "example",
        offlineStoreStatusEquals: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerFeatureGroups, ApiSagemakerFeatureGroupsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_feature_groups")

        ubx.data(ApiSagemakerFeatureGroups, "example", ApiSagemakerFeatureGroupsConfig(
            creation_time_after="example",
            creation_time_before="example",
            feature_group_status_equals="example",
            max_results=1,
            name_contains="example",
            offline_store_status_equals="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="feature_group_status_equals" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offline_store_status_equals" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="feature_group_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
