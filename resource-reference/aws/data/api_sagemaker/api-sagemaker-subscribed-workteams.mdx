---
title: "aws_api_sagemaker_subscribed_workteams"
description: "`aws_api_sagemaker_subscribed_workteams` reads the real, third-party labeling work teams subscribed to through AWS Marketplace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_subscribed_workteams"})

    		ubx.Data(api_sagemaker.ApiSagemakerSubscribedWorkteams, "example", api_sagemaker.ApiSagemakerSubscribedWorkteamsConfig{
    			MaxResults:   1,
    			NameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerSubscribedWorkteams } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_subscribed_workteams";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_subscribed_workteams" });

      data(ApiSagemakerSubscribedWorkteams, "example", {
        maxResults: 1,
        nameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerSubscribedWorkteams, ApiSagemakerSubscribedWorkteamsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_subscribed_workteams")

        ubx.data(ApiSagemakerSubscribedWorkteams, "example", ApiSagemakerSubscribedWorkteamsConfig(
            max_results=1,
            name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscribed_workteams" type="list(object)">
  The real SageMaker Ground Truth subscribed work teams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
