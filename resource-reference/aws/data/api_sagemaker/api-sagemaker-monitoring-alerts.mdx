---
title: "aws_api_sagemaker_monitoring_alerts"
description: "`aws_api_sagemaker_monitoring_alerts` reads a real, triggered alert from a SageMaker Model Monitor schedule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_monitoring_alerts"})

    		ubx.Data(api_sagemaker.ApiSagemakerMonitoringAlerts, "example", api_sagemaker.ApiSagemakerMonitoringAlertsConfig{
    			MonitoringScheduleName: "example-monitoring-schedule-name",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerMonitoringAlerts } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_monitoring_alerts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_monitoring_alerts" });

      data(ApiSagemakerMonitoringAlerts, "example", {
        monitoringScheduleName: "example-monitoring-schedule-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerMonitoringAlerts, ApiSagemakerMonitoringAlertsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_monitoring_alerts")

        ubx.data(ApiSagemakerMonitoringAlerts, "example", ApiSagemakerMonitoringAlertsConfig(
            monitoring_schedule_name="example-monitoring-schedule-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="monitoring_schedule_name" type="string" required>
  The real name of the monitoring schedule this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="monitoring_alert_summaries" type="list(object)">
  The real, summarized monitoring alerts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
