---
title: "aws_api_sagemaker_actions"
description: "`aws_api_sagemaker_actions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_actions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_actions"})

    		ubx.Data(api_sagemaker.ApiSagemakerActions, "example", api_sagemaker.ApiSagemakerActionsConfig{
    			ActionType:    "example",
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    			SortBy:        "example",
    			SortOrder:     "example",
    			SourceUri:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerActions } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_actions" });

      data(ApiSagemakerActions, "example", {
        actionType: "example",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        sourceUri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerActions, ApiSagemakerActionsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_actions")

        ubx.data(ApiSagemakerActions, "example", ApiSagemakerActionsConfig(
            action_type="example",
            created_after="example",
            created_before="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
            source_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="source_uri" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
