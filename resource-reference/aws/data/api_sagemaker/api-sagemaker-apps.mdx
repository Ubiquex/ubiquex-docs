---
title: "aws_api_sagemaker_apps"
description: "`aws_api_sagemaker_apps` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_apps` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_apps"})

    		ubx.Data(api_sagemaker.ApiSagemakerApps, "example", api_sagemaker.ApiSagemakerAppsConfig{
    			DomainIdEquals:        "example",
    			MaxResults:            1,
    			SortBy:                "example",
    			SortOrder:             "example",
    			SpaceNameEquals:       "example",
    			UserProfileNameEquals: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerApps } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_apps";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_apps" });

      data(ApiSagemakerApps, "example", {
        domainIdEquals: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        spaceNameEquals: "example",
        userProfileNameEquals: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerApps, ApiSagemakerAppsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_apps")

        ubx.data(ApiSagemakerApps, "example", ApiSagemakerAppsConfig(
            domain_id_equals="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
            space_name_equals="example",
            user_profile_name_equals="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id_equals" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="space_name_equals" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_profile_name_equals" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="apps" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
