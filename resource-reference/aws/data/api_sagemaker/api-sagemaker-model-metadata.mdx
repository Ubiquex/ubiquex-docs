---
title: "aws_api_sagemaker_model_metadata"
description: "`aws_api_sagemaker_model_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_model_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_model_metadata"})

    		ubx.Data(api_sagemaker.ApiSagemakerModelMetadata, "example", api_sagemaker.ApiSagemakerModelMetadataConfig{
    			MaxResults:       1,
    			SearchExpression: map[string]any{"filters": []map[string]any{{"name": "example-name"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerModelMetadata } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_model_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_model_metadata" });

      data(ApiSagemakerModelMetadata, "example", {
        maxResults: 1,
        searchExpression: { filters: [{ name: "example-name" }] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerModelMetadata, ApiSagemakerModelMetadataConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_model_metadata")

        ubx.data(ApiSagemakerModelMetadata, "example", ApiSagemakerModelMetadataConfig(
            max_results=1,
            search_expression={"filters": [{"name": "example-name"}]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search_expression" type="object">
  &lt;p&gt;One or more filters that searches for the specified resource or resources in a search. All resource objects that satisfy the expression's condition are included in the search results&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="filters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="model_metadata_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
