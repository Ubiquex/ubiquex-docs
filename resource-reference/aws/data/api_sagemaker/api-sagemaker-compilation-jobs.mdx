---
title: "aws_api_sagemaker_compilation_jobs"
description: "`aws_api_sagemaker_compilation_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_compilation_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_compilation_jobs"})

    		ubx.Data(api_sagemaker.ApiSagemakerCompilationJobs, "example", api_sagemaker.ApiSagemakerCompilationJobsConfig{
    			CreationTimeAfter:      "example",
    			CreationTimeBefore:     "example",
    			LastModifiedTimeAfter:  "example",
    			LastModifiedTimeBefore: "example",
    			MaxResults:             1,
    			NameContains:           "example",
    			SortBy:                 "example",
    			SortOrder:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerCompilationJobs } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_compilation_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_compilation_jobs" });

      data(ApiSagemakerCompilationJobs, "example", {
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        lastModifiedTimeAfter: "example",
        lastModifiedTimeBefore: "example",
        maxResults: 1,
        nameContains: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerCompilationJobs, ApiSagemakerCompilationJobsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_compilation_jobs")

        ubx.data(ApiSagemakerCompilationJobs, "example", ApiSagemakerCompilationJobsConfig(
            creation_time_after="example",
            creation_time_before="example",
            last_modified_time_after="example",
            last_modified_time_before="example",
            max_results=1,
            name_contains="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="last_modified_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="last_modified_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status_equals" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compilation_job_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
