---
title: "aws_api_sagemaker_hub_contents"
description: "`aws_api_sagemaker_hub_contents` reads the real content published in a SageMaker JumpStart private hub."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_hub_contents"})

    		ubx.Data(api_sagemaker.ApiSagemakerHubContents, "example", api_sagemaker.ApiSagemakerHubContentsConfig{
    			HubContentType:     "example",
    			HubName:            "example-hub-name",
    			CreationTimeAfter:  "example",
    			CreationTimeBefore: "example",
    			MaxResults:         1,
    			MaxSchemaVersion:   "example",
    			NameContains:       "example",
    			SortBy:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerHubContents } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_hub_contents";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_hub_contents" });

      data(ApiSagemakerHubContents, "example", {
        hubContentType: "example",
        hubName: "example-hub-name",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        maxResults: 1,
        maxSchemaVersion: "example",
        nameContains: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerHubContents, ApiSagemakerHubContentsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_hub_contents")

        ubx.data(ApiSagemakerHubContents, "example", ApiSagemakerHubContentsConfig(
            hub_content_type="example",
            hub_name="example-hub-name",
            creation_time_after="example",
            creation_time_before="example",
            max_results=1,
            max_schema_version="example",
            name_contains="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="hub_content_type" type="string" required>
  The real, discriminating type of the hub content this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  The real name of the hub this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_schema_version" type="string">
  The real version of the max schema. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_contains" type="string">
  Restricts results to objects whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hub_content_summaries" type="list(object)">
  The real, summarized hub contents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
