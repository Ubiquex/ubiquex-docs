---
title: "aws_api_sagemaker_hub_content_versions"
description: "`aws_api_sagemaker_hub_content_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_hub_content_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_hub_content_versions"})

    		ubx.Data(api_sagemaker.ApiSagemakerHubContentVersions, "example", api_sagemaker.ApiSagemakerHubContentVersionsConfig{
    			HubContentName:     "example-hub-content-name",
    			HubContentType:     "example",
    			HubName:            "example-hub-name",
    			CreationTimeAfter:  "example",
    			CreationTimeBefore: "example",
    			MaxResults:         1,
    			MaxSchemaVersion:   "example",
    			MinVersion:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerHubContentVersions } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_hub_content_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_hub_content_versions" });

      data(ApiSagemakerHubContentVersions, "example", {
        hubContentName: "example-hub-content-name",
        hubContentType: "example",
        hubName: "example-hub-name",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        maxResults: 1,
        maxSchemaVersion: "example",
        minVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerHubContentVersions, ApiSagemakerHubContentVersionsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_hub_content_versions")

        ubx.data(ApiSagemakerHubContentVersions, "example", ApiSagemakerHubContentVersionsConfig(
            hub_content_name="example-hub-content-name",
            hub_content_type="example",
            hub_name="example-hub-name",
            creation_time_after="example",
            creation_time_before="example",
            max_results=1,
            max_schema_version="example",
            min_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="hub_content_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="hub_content_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_schema_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hub_content_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
