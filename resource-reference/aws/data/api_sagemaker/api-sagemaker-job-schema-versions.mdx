---
title: "aws_api_sagemaker_job_schema_versions"
description: "`aws_api_sagemaker_job_schema_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_job_schema_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_job_schema_versions"})

    		ubx.Data(api_sagemaker.ApiSagemakerJobSchemaVersions, "example", api_sagemaker.ApiSagemakerJobSchemaVersionsConfig{
    			JobCategory: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerJobSchemaVersions } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_job_schema_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_job_schema_versions" });

      data(ApiSagemakerJobSchemaVersions, "example", {
        jobCategory: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerJobSchemaVersions, ApiSagemakerJobSchemaVersionsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_job_schema_versions")

        ubx.data(ApiSagemakerJobSchemaVersions, "example", ApiSagemakerJobSchemaVersionsConfig(
            job_category="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_category" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_config_schemas" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
