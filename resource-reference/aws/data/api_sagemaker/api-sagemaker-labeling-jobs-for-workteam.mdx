---
title: "aws_api_sagemaker_labeling_jobs_for_workteam"
description: "`aws_api_sagemaker_labeling_jobs_for_workteam` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_labeling_jobs_for_workteam` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_labeling_jobs_for_workteam"})

    		ubx.Data(api_sagemaker.ApiSagemakerLabelingJobsForWorkteam, "example", api_sagemaker.ApiSagemakerLabelingJobsForWorkteamConfig{
    			WorkteamArn:              "arn:aws:iam::123456789012:policy/example",
    			CreationTimeAfter:        "example",
    			CreationTimeBefore:       "example",
    			JobReferenceCodeContains: "example",
    			MaxResults:               1,
    			SortBy:                   "example",
    			SortOrder:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerLabelingJobsForWorkteam } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_labeling_jobs_for_workteam";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_labeling_jobs_for_workteam" });

      data(ApiSagemakerLabelingJobsForWorkteam, "example", {
        workteamArn: "arn:aws:iam::123456789012:policy/example",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        jobReferenceCodeContains: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerLabelingJobsForWorkteam, ApiSagemakerLabelingJobsForWorkteamConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_labeling_jobs_for_workteam")

        ubx.data(ApiSagemakerLabelingJobsForWorkteam, "example", ApiSagemakerLabelingJobsForWorkteamConfig(
            workteam_arn="arn:aws:iam::123456789012:policy/example",
            creation_time_after="example",
            creation_time_before="example",
            job_reference_code_contains="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="job_reference_code_contains" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workteam_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="labeling_job_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
