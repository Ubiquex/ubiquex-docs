---
title: "aws_api_sagemaker_domains"
description: "`aws_api_sagemaker_domains` looks up a real SageMaker Studio domain -- the top-level resource grouping users, storage, and networking for Studio."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_domains"})

    		ubx.Data(api_sagemaker.ApiSagemakerDomains, "example", api_sagemaker.ApiSagemakerDomainsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerDomains } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_domains";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_domains" });

      data(ApiSagemakerDomains, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerDomains, ApiSagemakerDomainsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_domains")

        ubx.data(ApiSagemakerDomains, "example", ApiSagemakerDomainsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domains" type="list(object)">
  The real domains included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
