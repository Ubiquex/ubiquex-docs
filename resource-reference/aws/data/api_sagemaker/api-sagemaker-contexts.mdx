---
title: "aws_api_sagemaker_contexts"
description: "`aws_api_sagemaker_contexts` looks up a real SageMaker ML Lineage context -- a logical grouping (such as an experiment) that other lineage entities..."
---

`aws_api_sagemaker_contexts` looks up a real SageMaker ML Lineage context -- a logical grouping (such as an experiment) that other lineage entities belong to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_contexts"})

    		ubx.Data(api_sagemaker.ApiSagemakerContexts, "example", api_sagemaker.ApiSagemakerContextsConfig{
    			ContextType:   "example",
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    			SortBy:        "example",
    			SortOrder:     "example",
    			SourceUri:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerContexts } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_contexts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_contexts" });

      data(ApiSagemakerContexts, "example", {
        contextType: "example",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        sourceUri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerContexts, ApiSagemakerContextsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_contexts")

        ubx.data(ApiSagemakerContexts, "example", ApiSagemakerContextsConfig(
            context_type="example",
            created_after="example",
            created_before="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
            source_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="context_type" type="string">
  The real, discriminating type of the context this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_uri" type="string">
  The real, resolvable URI of the source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="context_summaries" type="list(object)">
  The real, summarized contexts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
