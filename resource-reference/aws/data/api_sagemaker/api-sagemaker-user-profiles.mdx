---
title: "aws_api_sagemaker_user_profiles"
description: "`aws_api_sagemaker_user_profiles` looks up a real user profile within a SageMaker Studio domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_user_profiles"})

    		ubx.Data(api_sagemaker.ApiSagemakerUserProfiles, "example", api_sagemaker.ApiSagemakerUserProfilesConfig{
    			DomainIdEquals:          "example",
    			MaxResults:              1,
    			SortBy:                  "example",
    			SortOrder:               "example",
    			UserProfileNameContains: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerUserProfiles } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_user_profiles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_user_profiles" });

      data(ApiSagemakerUserProfiles, "example", {
        domainIdEquals: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
        userProfileNameContains: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerUserProfiles, ApiSagemakerUserProfilesConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_user_profiles")

        ubx.data(ApiSagemakerUserProfiles, "example", ApiSagemakerUserProfilesConfig(
            domain_id_equals="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
            user_profile_name_contains="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id_equals" type="string">
  Restricts results to this exact domain identifier. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_profile_name_contains" type="string">
  Restricts results to user profiles whose name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_profiles" type="list(object)">
  The real SageMaker user profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
