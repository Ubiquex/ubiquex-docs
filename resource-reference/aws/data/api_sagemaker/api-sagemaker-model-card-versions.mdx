---
title: "aws_api_sagemaker_model_card_versions"
description: "`aws_api_sagemaker_model_card_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_model_card_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_model_card_versions"})

    		ubx.Data(api_sagemaker.ApiSagemakerModelCardVersions, "example", api_sagemaker.ApiSagemakerModelCardVersionsConfig{
    			ModelCardName:      "example-model-card-name",
    			CreationTimeAfter:  "example",
    			CreationTimeBefore: "example",
    			MaxResults:         1,
    			ModelCardStatus:    "example",
    			SortBy:             "example",
    			SortOrder:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerModelCardVersions } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_model_card_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_model_card_versions" });

      data(ApiSagemakerModelCardVersions, "example", {
        modelCardName: "example-model-card-name",
        creationTimeAfter: "example",
        creationTimeBefore: "example",
        maxResults: 1,
        modelCardStatus: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerModelCardVersions, ApiSagemakerModelCardVersionsConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_model_card_versions")

        ubx.data(ApiSagemakerModelCardVersions, "example", ApiSagemakerModelCardVersionsConfig(
            model_card_name="example-model-card-name",
            creation_time_after="example",
            creation_time_before="example",
            max_results=1,
            model_card_status="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="creation_time_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="creation_time_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="model_card_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="model_card_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="model_card_version_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
