---
title: "aws_api_sagemaker_device"
description: "`aws_api_sagemaker_device` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_sagemaker_device` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_device"})

    		ubx.Data(api_sagemaker.ApiSagemakerDevice, "example", api_sagemaker.ApiSagemakerDeviceConfig{
    			DeviceFleetName: "example-device-fleet-name",
    			DeviceName:      "example-device-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiSagemakerDevice } from "@ubx/sdk-aws/aws/data/api_sagemaker/api_sagemaker_device";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_device" });

      data(ApiSagemakerDevice, "example", {
        deviceFleetName: "example-device-fleet-name",
        deviceName: "example-device-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerDevice, ApiSagemakerDeviceConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_device")

        ubx.data(ApiSagemakerDevice, "example", ApiSagemakerDeviceConfig(
            device_fleet_name="example-device-fleet-name",
            device_name="example-device-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_fleet_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="device_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agent_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="device_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="iot_thing_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_heartbeat" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_models" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="models" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="registration_time" type="string">
  Returned by the lookup.
</ResponseField>
