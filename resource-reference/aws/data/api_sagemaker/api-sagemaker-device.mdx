---
title: "aws_api_sagemaker_device"
description: "`aws_api_sagemaker_device` looks up a real edge device registered with SageMaker Edge Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_sagemaker_device"})

    		ubx.Data(api_sagemaker.ApiSagemakerDevice, "example", api_sagemaker.ApiSagemakerDeviceConfig{
    			DeviceFleetName: "example-device-fleet-name",
    			DeviceName:      "example-device-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiSagemakerDevice } from "./local-sdk/aws/sdk/typescript/aws/data/api_sagemaker/api_sagemaker_device.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_sagemaker_device" });

      data(ApiSagemakerDevice, "example", {
        deviceFleetName: "example-device-fleet-name",
        deviceName: "example-device-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_sagemaker import ApiSagemakerDevice, ApiSagemakerDeviceConfig

    def describe():
        ubx.intent("look up aws_api_sagemaker_device")

        ubx.data(ApiSagemakerDevice, "example", ApiSagemakerDeviceConfig(
            device_fleet_name="example-device-fleet-name",
            device_name="example-device-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_fleet_name" type="string" required>
  The real name of the SageMaker device fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="device_name" type="string" required>
  The real name of the device this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agent_version" type="string">
  The real version of the agent. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="device_arn" type="string">
  The real Amazon Resource Name (ARN) of the device this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="iot_thing_name" type="string">
  The real name of the iot thing this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="latest_heartbeat" type="string">
  The real timestamp this object last sent a heartbeat. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_models" type="number">
  The real max models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="models" type="list(object)">
  The real models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_time" type="string">
  The real timestamp of the registration. **(AI-inferred)** Returned by the lookup.
</ResponseField>
