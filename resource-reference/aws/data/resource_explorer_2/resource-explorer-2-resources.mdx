---
title: "aws_resource_explorer_2_resources"
description: "`aws_resource_explorer_2_resources` searches for real AWS resources indexed by Resource Explorer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_resources"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2Resources, "example", resource_explorer_2.ResourceExplorer2ResourcesConfig{
    			Filters:    map[string]any{"filter_string": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceExplorer2Resources } from "./local-sdk/aws/sdk/typescript/aws/data/resource_explorer_2/resource_explorer_2_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_resources" });

      data(ResourceExplorer2Resources, "example", {
        filters: { filterString: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2Resources, ResourceExplorer2ResourcesConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_resources")

        ubx.data(ResourceExplorer2Resources, "example", ResourceExplorer2ResourcesConfig(
            filters={"filter_string": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;A search filter defines which resources can be part of a search query result set.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="filter_string" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The real AWS resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="view_arn" type="string">
  The real Amazon Resource Name (ARN) of the view this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
