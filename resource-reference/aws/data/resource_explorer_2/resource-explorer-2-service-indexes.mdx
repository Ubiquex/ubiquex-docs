---
title: "aws_resource_explorer_2_service_indexes"
description: "`aws_resource_explorer_2_service_indexes` reads the real, service-managed Resource Explorer indexes on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_service_indexes"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2ServiceIndexes, "example", resource_explorer_2.ResourceExplorer2ServiceIndexesConfig{
    			MaxResults: 1,
    			Regions:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2ServiceIndexes } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_service_indexes";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_service_indexes" });

      data(ResourceExplorer2ServiceIndexes, "example", {
        maxResults: 1,
        regions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2ServiceIndexes, ResourceExplorer2ServiceIndexesConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_service_indexes")

        ubx.data(ResourceExplorer2ServiceIndexes, "example", ResourceExplorer2ServiceIndexesConfig(
            max_results=1,
            regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="regions" type="list(string)">
  The real AWS regions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indexes" type="list(object)">
  The real indexes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
