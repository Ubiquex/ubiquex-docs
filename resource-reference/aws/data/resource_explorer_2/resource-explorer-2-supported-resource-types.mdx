---
title: "aws_resource_explorer_2_supported_resource_types"
description: "`aws_resource_explorer_2_supported_resource_types` reads the real AWS resource types currently indexed by Resource Explorer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_supported_resource_types"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2SupportedResourceTypes, "example", resource_explorer_2.ResourceExplorer2SupportedResourceTypesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2SupportedResourceTypes } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_supported_resource_types";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resource_explorer_2_supported_resource_types",
      });

      data(ResourceExplorer2SupportedResourceTypes, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2SupportedResourceTypes, ResourceExplorer2SupportedResourceTypesConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_supported_resource_types")

        ubx.data(ResourceExplorer2SupportedResourceTypes, "example", ResourceExplorer2SupportedResourceTypesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(object)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
