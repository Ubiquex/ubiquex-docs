---
title: "aws_resource_explorer_2_service_view"
description: "`aws_resource_explorer_2_service_view` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resource_explorer_2_service_view` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_service_view"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2ServiceView, "example", resource_explorer_2.ResourceExplorer2ServiceViewConfig{
    			ServiceViewArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2ServiceView } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_service_view";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_service_view" });

      data(ResourceExplorer2ServiceView, "example", {
        serviceViewArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2ServiceView, ResourceExplorer2ServiceViewConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_service_view")

        ubx.data(ResourceExplorer2ServiceView, "example", ResourceExplorer2ServiceViewConfig(
            service_view_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="service_view_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="view" type="object">
  &lt;p&gt;Contains the configuration and properties of a Resource Explorer service view.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="filters" type="object">
      &lt;p&gt;A search filter defines which resources can be part of a search query result set.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="filter_string" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="included_properties" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scope_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="service_linked_recorder" type="object">
      &lt;p&gt;Contains information about the service-linked recorder paired with a service view.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="recorder_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="recorder_type" type="string">
          &lt;p&gt;The type of a service-linked recorder.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;AWS&lt;/code&gt;  --  Managed by an Amazon Web Services service.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;THIRD_PARTY&lt;/code&gt;  --  Managed by a third-party service.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
        </ResponseField>
        <ResponseField name="service_principal" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_view_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="service_view_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="streaming_access_for_service" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
