---
title: "aws_resource_explorer_2_indexes_for_members"
description: "`aws_resource_explorer_2_indexes_for_members` reads the real Resource Explorer indexes of member accounts within an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_indexes_for_members"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2IndexesForMembers, "example", resource_explorer_2.ResourceExplorer2IndexesForMembersConfig{
    			AccountIdList: []string{"example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceExplorer2IndexesForMembers } from "./local-sdk/aws/sdk/typescript/aws/data/resource_explorer_2/resource_explorer_2_indexes_for_members.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_indexes_for_members" });

      data(ResourceExplorer2IndexesForMembers, "example", {
        accountIdList: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2IndexesForMembers, ResourceExplorer2IndexesForMembersConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_indexes_for_members")

        ubx.data(ResourceExplorer2IndexesForMembers, "example", ResourceExplorer2IndexesForMembersConfig(
            account_id_list=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id_list" type="list(string)" required>
  The real account ids included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indexes" type="list(object)">
  The real indexes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
