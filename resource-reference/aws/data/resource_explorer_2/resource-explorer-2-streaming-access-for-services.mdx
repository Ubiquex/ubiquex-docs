---
title: "aws_resource_explorer_2_streaming_access_for_services"
description: "`aws_resource_explorer_2_streaming_access_for_services` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resource_explorer_2_streaming_access_for_services` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_streaming_access_for_services"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2StreamingAccessForServices, "example", resource_explorer_2.ResourceExplorer2StreamingAccessForServicesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2StreamingAccessForServices } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_streaming_access_for_services";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resource_explorer_2_streaming_access_for_services",
      });

      data(ResourceExplorer2StreamingAccessForServices, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2StreamingAccessForServices, ResourceExplorer2StreamingAccessForServicesConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_streaming_access_for_services")

        ubx.data(ResourceExplorer2StreamingAccessForServices, "example", ResourceExplorer2StreamingAccessForServicesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="streaming_access_for_services" type="list(object)">
  Returned by the lookup.
</ResponseField>
