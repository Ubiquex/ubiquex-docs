---
title: "aws_resource_explorer_2_index"
description: "`aws_resource_explorer_2_index` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resource_explorer_2_index` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_index"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2Index, "example", resource_explorer_2.ResourceExplorer2IndexConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2Index } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_index";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_index" });

      data(ResourceExplorer2Index, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2Index, ResourceExplorer2IndexConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_index")

        ubx.data(ResourceExplorer2Index, "example", ResourceExplorer2IndexConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="replicating_from" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="replicating_to" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Returned by the lookup.
</ResponseField>
