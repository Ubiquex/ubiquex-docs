---
title: "aws_resource_explorer_2_managed_views"
description: "`aws_resource_explorer_2_managed_views` reads the real, AWS-managed Resource Explorer views available on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_explorer_2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_explorer_2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_explorer_2_managed_views"})

    		ubx.Data(resource_explorer_2.ResourceExplorer2ManagedViews, "example", resource_explorer_2.ResourceExplorer2ManagedViewsConfig{
    			MaxResults:       1,
    			ServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceExplorer2ManagedViews } from "@ubx/sdk-aws/aws/data/resource_explorer_2/resource_explorer_2_managed_views";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_explorer_2_managed_views" });

      data(ResourceExplorer2ManagedViews, "example", {
        maxResults: 1,
        servicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_explorer_2 import ResourceExplorer2ManagedViews, ResourceExplorer2ManagedViewsConfig

    def describe():
        ubx.intent("look up aws_resource_explorer_2_managed_views")

        ubx.data(ResourceExplorer2ManagedViews, "example", ResourceExplorer2ManagedViewsConfig(
            max_results=1,
            service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_principal" type="string">
  The real AWS service principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_views" type="list(string)">
  The real Resource Explorer managed views included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
