---
title: "aws_codeguru_reviewer_recommendations"
description: "`aws_codeguru_reviewer_recommendations` reads the real recommendations from a CodeGuru Reviewer code review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeguru_reviewer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_reviewer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_reviewer_recommendations"})

    		ubx.Data(codeguru_reviewer.CodeguruReviewerRecommendations, "example", codeguru_reviewer.CodeguruReviewerRecommendationsConfig{
    			CodeReviewArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodeguruReviewerRecommendations } from "./local-sdk/aws/sdk/typescript/aws/data/codeguru_reviewer/codeguru_reviewer_recommendations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_reviewer_recommendations" });

      data(CodeguruReviewerRecommendations, "example", {
        codeReviewArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_reviewer import CodeguruReviewerRecommendations, CodeguruReviewerRecommendationsConfig

    def describe():
        ubx.intent("look up aws_codeguru_reviewer_recommendations")

        ubx.data(CodeguruReviewerRecommendations, "example", CodeguruReviewerRecommendationsConfig(
            code_review_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="code_review_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the code review this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_summaries" type="list(object)">
  The real, summarized recommendations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
