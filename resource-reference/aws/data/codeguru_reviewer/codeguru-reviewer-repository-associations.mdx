---
title: "aws_codeguru_reviewer_repository_associations"
description: "`aws_codeguru_reviewer_repository_associations` reads the real repositories associated with CodeGuru Reviewer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_reviewer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_reviewer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_reviewer_repository_associations"})

    		ubx.Data(codeguru_reviewer.CodeguruReviewerRepositoryAssociations, "example", codeguru_reviewer.CodeguruReviewerRepositoryAssociationsConfig{
    			MaxResults:    1,
    			Names:         []string{"example"},
    			Owners:        []string{"example"},
    			ProviderTypes: []string{"example"},
    			States:        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruReviewerRepositoryAssociations } from "@ubx/sdk-aws/aws/data/codeguru_reviewer/codeguru_reviewer_repository_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_reviewer_repository_associations" });

      data(CodeguruReviewerRepositoryAssociations, "example", {
        maxResults: 1,
        names: ["example"],
        owners: ["example"],
        providerTypes: ["example"],
        states: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_reviewer import CodeguruReviewerRepositoryAssociations, CodeguruReviewerRepositoryAssociationsConfig

    def describe():
        ubx.intent("look up aws_codeguru_reviewer_repository_associations")

        ubx.data(CodeguruReviewerRepositoryAssociations, "example", CodeguruReviewerRepositoryAssociationsConfig(
            max_results=1,
            names=["example"],
            owners=["example"],
            provider_types=["example"],
            states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owners" type="list(string)">
  The real owners included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provider_types" type="list(string)">
  The real, discriminating types of providers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  The real, current states of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_association_summaries" type="list(object)">
  The real, summarized repository associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
