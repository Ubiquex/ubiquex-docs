---
title: "aws_codeguru_reviewer_recommendation_feedback"
description: "`aws_codeguru_reviewer_recommendation_feedback` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_reviewer_recommendation_feedback` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_reviewer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_reviewer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_reviewer_recommendation_feedback"})

    		ubx.Data(codeguru_reviewer.CodeguruReviewerRecommendationFeedback, "example", codeguru_reviewer.CodeguruReviewerRecommendationFeedbackConfig{
    			CodeReviewArn:    "arn:aws:iam::123456789012:policy/example",
    			RecommendationId: "example",
    			UserId:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruReviewerRecommendationFeedback } from "@ubx/sdk-aws/aws/data/codeguru_reviewer/codeguru_reviewer_recommendation_feedback";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_reviewer_recommendation_feedback" });

      data(CodeguruReviewerRecommendationFeedback, "example", {
        codeReviewArn: "arn:aws:iam::123456789012:policy/example",
        recommendationId: "example",
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_reviewer import CodeguruReviewerRecommendationFeedback, CodeguruReviewerRecommendationFeedbackConfig

    def describe():
        ubx.intent("look up aws_codeguru_reviewer_recommendation_feedback")

        ubx.data(CodeguruReviewerRecommendationFeedback, "example", CodeguruReviewerRecommendationFeedbackConfig(
            code_review_arn="arn:aws:iam::123456789012:policy/example",
            recommendation_id="example",
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="code_review_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="recommendation_feedback" type="object">
  &lt;p&gt;Information about the recommendation feedback.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code_review_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_time_stamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_time_stamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="reactions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="recommendation_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
