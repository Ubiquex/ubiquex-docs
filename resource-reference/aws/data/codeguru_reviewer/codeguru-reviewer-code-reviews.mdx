---
title: "aws_codeguru_reviewer_code_reviews"
description: "`aws_codeguru_reviewer_code_reviews` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_reviewer_code_reviews` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_reviewer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_reviewer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_reviewer_code_reviews"})

    		ubx.Data(codeguru_reviewer.CodeguruReviewerCodeReviews, "example", codeguru_reviewer.CodeguruReviewerCodeReviewsConfig{
    			Type:            "example",
    			MaxResults:      1,
    			ProviderTypes:   []string{"example"},
    			RepositoryNames: []string{"example"},
    			States:          []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruReviewerCodeReviews } from "@ubx/sdk-aws/aws/data/codeguru_reviewer/codeguru_reviewer_code_reviews";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeguru_reviewer_code_reviews" });

      data(CodeguruReviewerCodeReviews, "example", {
        type: "example",
        maxResults: 1,
        providerTypes: ["example"],
        repositoryNames: ["example"],
        states: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_reviewer import CodeguruReviewerCodeReviews, CodeguruReviewerCodeReviewsConfig

    def describe():
        ubx.intent("look up aws_codeguru_reviewer_code_reviews")

        ubx.data(CodeguruReviewerCodeReviews, "example", CodeguruReviewerCodeReviewsConfig(
            type="example",
            max_results=1,
            provider_types=["example"],
            repository_names=["example"],
            states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="provider_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="code_review_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
