---
title: "aws_fsx_shared_vpc_configuration"
description: "`aws_fsx_shared_vpc_configuration` reads the real, cross-account VPC sharing configuration for FSx."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fsx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fsx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fsx_shared_vpc_configuration"})

    		ubx.Data(fsx.SharedVpcConfiguration, "example", fsx.SharedVpcConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SharedVpcConfiguration } from "@ubx/sdk-aws/aws/data/fsx/shared_vpc_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_fsx_shared_vpc_configuration" });

      data(SharedVpcConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fsx import SharedVpcConfiguration, SharedVpcConfigurationConfig

    def describe():
        ubx.intent("look up aws_fsx_shared_vpc_configuration")

        ubx.data(SharedVpcConfiguration, "example", SharedVpcConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="enable_fsx_route_table_updates_from_participant_accounts" type="string">
  Whether FSx route table updates from participant accounts are enabled for this transit gateway multicast domain. **(AI-inferred)** Returned by the lookup.
</ResponseField>
