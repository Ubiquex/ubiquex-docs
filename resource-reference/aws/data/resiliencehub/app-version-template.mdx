---
title: "aws_resiliencehub_app_version_template"
description: "`aws_resiliencehub_app_version_template` reads the real, generated resiliency policy template for a Resilience Hub application version."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version_template"})

    		ubx.Data(resiliencehub.AppVersionTemplate, "example", resiliencehub.AppVersionTemplateConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppVersionTemplate } from "@ubx/sdk-aws/aws/data/resiliencehub/app_version_template";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_version_template" });

      data(AppVersionTemplate, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersionTemplate, AppVersionTemplateConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version_template")

        ubx.data(AppVersionTemplate, "example", AppVersionTemplateConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_template_body" type="string">
  The real body of the app template. **(AI-inferred)** Returned by the lookup.
</ResponseField>
