---
title: "aws_resiliencehub_app_component_recommendations"
description: "`aws_resiliencehub_app_component_recommendations` reads real, system-recommended resiliency improvements for a Resilience Hub application component."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_component_recommendations"})

    		ubx.Data(resiliencehub.AppComponentRecommendations, "example", resiliencehub.AppComponentRecommendationsConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppComponentRecommendations } from "@ubx/sdk-aws/aws/data/resiliencehub/app_component_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehub_app_component_recommendations",
      });

      data(AppComponentRecommendations, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppComponentRecommendations, AppComponentRecommendationsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_component_recommendations")

        ubx.data(AppComponentRecommendations, "example", AppComponentRecommendationsConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_recommendations" type="list(object)">
  The real, recommended changes for each component. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
