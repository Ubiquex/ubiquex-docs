---
title: "aws_resiliencehub_app_version_resource_mappings"
description: "`aws_resiliencehub_app_version_resource_mappings` reads the real mappings from a Resilience Hub application version's own logical resources to real AWS..."
---

`aws_resiliencehub_app_version_resource_mappings` reads the real mappings from a Resilience Hub application version's own logical resources to real AWS resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version_resource_mappings"})

    		ubx.Data(resiliencehub.AppVersionResourceMappings, "example", resiliencehub.AppVersionResourceMappingsConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppVersionResourceMappings } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/app_version_resource_mappings.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehub_app_version_resource_mappings",
      });

      data(AppVersionResourceMappings, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersionResourceMappings, AppVersionResourceMappingsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version_resource_mappings")

        ubx.data(AppVersionResourceMappings, "example", AppVersionResourceMappingsConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_mappings" type="list(object)">
  The real resource mappings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
