---
title: "aws_resiliencehub_app_version_resources"
description: "`aws_resiliencehub_app_version_resources` reads the real resources included in a version of a Resilience Hub application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version_resources"})

    		ubx.Data(resiliencehub.AppVersionResources, "example", resiliencehub.AppVersionResourcesConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppVersionResources } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/app_version_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_version_resources" });

      data(AppVersionResources, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersionResources, AppVersionResourcesConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version_resources")

        ubx.data(AppVersionResources, "example", AppVersionResourcesConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="physical_resources" type="list(object)">
  The real, underlying physical resources for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolution_id" type="string">
  The real, unique identifier of the resolution this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
