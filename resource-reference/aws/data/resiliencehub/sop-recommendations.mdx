---
title: "aws_resiliencehub_sop_recommendations"
description: "`aws_resiliencehub_sop_recommendations` reads real, system-recommended standard operating procedures (SOPs) for a Resilience Hub application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_sop_recommendations"})

    		ubx.Data(resiliencehub.SopRecommendations, "example", resiliencehub.SopRecommendationsConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SopRecommendations } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/sop_recommendations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_sop_recommendations" });

      data(SopRecommendations, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import SopRecommendations, SopRecommendationsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_sop_recommendations")

        ubx.data(SopRecommendations, "example", SopRecommendationsConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sop_recommendations" type="list(object)">
  The real, recommended standard operating procedures (SOPs) included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
