---
title: "aws_resiliencehub_metrics"
description: "`aws_resiliencehub_metrics` reads real, computed resiliency metrics for a Resilience Hub assessment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_metrics"})

    		ubx.Data(resiliencehub.Metrics, "example", resiliencehub.MetricsConfig{
    			Conditions: []map[string]any{{"field": "example"}},
    			DataSource: "example",
    			Fields:     []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			Sorts:      []map[string]any{{"ascending": true}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Metrics } from "@ubx/sdk-aws/aws/data/resiliencehub/metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_metrics" });

      data(Metrics, "example", {
        conditions: [{ field: "example" }],
        dataSource: "example",
        fields: [{ name: "example-name" }],
        maxResults: 1,
        sorts: [{ ascending: true }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import Metrics, MetricsConfig
    from ubx.aws.data.resiliencehub.metrics import Metrics_Conditions, Metrics_Fields, Metrics_Sorts

    def describe():
        ubx.intent("look up aws_resiliencehub_metrics")

        ubx.data(Metrics, "example", MetricsConfig(
            conditions=[Metrics_Conditions(field="example")],
            data_source="example",
            fields=[Metrics_Fields(name="example-name")],
            max_results=1,
            sorts=[Metrics_Sorts(ascending=True)],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="conditions" type="list(object)">
  The real conditions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_source" type="string">
  The real data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="fields" type="list(object)">
  The real fields included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sorts" type="list(object)">
  The real sort criteria applied to this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rows" type="list(list(string))">
  The real rows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
