---
title: "aws_resiliencehub_app_assessment"
description: "`aws_resiliencehub_app_assessment` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehub_app_assessment` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_assessment"})

    		ubx.Data(resiliencehub.AppAssessment, "example", resiliencehub.AppAssessmentConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppAssessment } from "@ubx/sdk-aws/aws/data/resiliencehub/app_assessment";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_assessment" });

      data(AppAssessment, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppAssessment, AppAssessmentConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_assessment")

        ubx.data(AppAssessment, "example", AppAssessmentConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment" type="object">
  &lt;p&gt;Defines an application assessment.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="app_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="app_version" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="assessment_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="assessment_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="assessment_status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="compliance" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="compliance_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cost" type="object">
      &lt;p&gt;Defines a cost object.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="amount" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="currency" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="frequency" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="drift_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="invoker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy" type="object">
      &lt;p&gt;Defines a resiliency policy.&lt;/p&gt; &lt;note&gt; &lt;p&gt;Resilience Hub allows you to provide a value of zero for &lt;code&gt;rtoInSecs&lt;/code&gt; and &lt;code&gt;rpoInSecs&lt;/code&gt; of your resiliency policy. But, while assessing your application, the lowest possible assessment result is near zero. Hence, if you provide value zero for &lt;code&gt;rtoInSecs&lt;/code&gt; and &lt;code&gt;rpoInSecs&lt;/code&gt;, the estimated workload RTO and estimated workload RPO result will be near zero and the &lt;b&gt;Compliance status&lt;/b&gt; for your application will be set to &lt;b&gt;Policy breached&lt;/b&gt;.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="creation_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="data_location_constraint" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="estimated_cost_tier" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy" type="map(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_arn" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_description" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="tags" type="map(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="tier" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resiliency_score" type="object">
      &lt;p&gt;The overall resiliency score, returned as an object that includes the disruption score and outage score.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="component_score" type="map(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="disruption_score" type="map(number)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="score" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_errors_details" type="object">
      &lt;p&gt; A list of errors retrieving an application's resources. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="has_more_errors" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="resource_errors" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="summary" type="object">
      &lt;p&gt;Indicates the AI-generated summary for the Resilience Hub assessment, providing a concise overview that highlights the top risks and recommendations.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This property is available only in the US East (N. Virginia) Region.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="risk_recommendations" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="summary" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="version_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
