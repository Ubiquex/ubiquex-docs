---
title: "aws_resiliencehub_app_version_app_components"
description: "`aws_resiliencehub_app_version_app_components` reads the real application components defined in a version of a Resilience Hub application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version_app_components"})

    		ubx.Data(resiliencehub.AppVersionAppComponents, "example", resiliencehub.AppVersionAppComponentsConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppVersionAppComponents } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/app_version_app_components.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_version_app_components" });

      data(AppVersionAppComponents, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersionAppComponents, AppVersionAppComponentsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version_app_components")

        ubx.data(AppVersionAppComponents, "example", AppVersionAppComponentsConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_components" type="list(object)">
  The real application components included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
