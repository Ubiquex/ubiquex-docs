---
title: "aws_resiliencehub_draft_app_version_resources_import_status"
description: "`aws_resiliencehub_draft_app_version_resources_import_status` reads the real, current status of importing resources into the draft version of..."
---

`aws_resiliencehub_draft_app_version_resources_import_status` reads the real, current status of importing resources into the draft version of a Resilience Hub application.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_draft_app_version_resources_import_status"})

    		ubx.Data(resiliencehub.DraftAppVersionResourcesImportStatus, "example", resiliencehub.DraftAppVersionResourcesImportStatusConfig{
    			AppArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DraftAppVersionResourcesImportStatus } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/draft_app_version_resources_import_status.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_resiliencehub_draft_app_version_resources_import_status",
      });

      data(DraftAppVersionResourcesImportStatus, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import DraftAppVersionResourcesImportStatus, DraftAppVersionResourcesImportStatusConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_draft_app_version_resources_import_status")

        ubx.data(DraftAppVersionResourcesImportStatus, "example", DraftAppVersionResourcesImportStatusConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_version" type="string">
  The real version of the app this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="list(object)">
  The real, detailed information about the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_change_time" type="string">
  The real timestamp of the status change. **(AI-inferred)** Returned by the lookup.
</ResponseField>
