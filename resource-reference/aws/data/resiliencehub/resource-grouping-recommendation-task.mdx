---
title: "aws_resiliencehub_resource_grouping_recommendation_task"
description: "`aws_resiliencehub_resource_grouping_recommendation_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehub_resource_grouping_recommendation_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_resource_grouping_recommendation_task"})

    		ubx.Data(resiliencehub.ResourceGroupingRecommendationTask, "example", resiliencehub.ResourceGroupingRecommendationTaskConfig{
    			AppArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupingRecommendationTask } from "@ubx/sdk-aws/aws/data/resiliencehub/resource_grouping_recommendation_task";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehub_resource_grouping_recommendation_task",
      });

      data(ResourceGroupingRecommendationTask, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import ResourceGroupingRecommendationTask, ResourceGroupingRecommendationTaskConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_resource_grouping_recommendation_task")

        ubx.data(ResourceGroupingRecommendationTask, "example", ResourceGroupingRecommendationTaskConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="grouping_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
