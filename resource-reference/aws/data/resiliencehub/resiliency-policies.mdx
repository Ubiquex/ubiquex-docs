---
title: "aws_resiliencehub_resiliency_policies"
description: "`aws_resiliencehub_resiliency_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehub_resiliency_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_resiliency_policies"})

    		ubx.Data(resiliencehub.ResiliencyPolicies, "example", resiliencehub.ResiliencyPoliciesConfig{
    			MaxResults: 1,
    			PolicyName: "example-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResiliencyPolicies } from "@ubx/sdk-aws/aws/data/resiliencehub/resiliency_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_resiliency_policies" });

      data(ResiliencyPolicies, "example", {
        maxResults: 1,
        policyName: "example-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import ResiliencyPolicies, ResiliencyPoliciesConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_resiliency_policies")

        ubx.data(ResiliencyPolicies, "example", ResiliencyPoliciesConfig(
            max_results=1,
            policy_name="example-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resiliency_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>
