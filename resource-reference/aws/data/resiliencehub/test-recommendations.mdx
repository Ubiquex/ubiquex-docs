---
title: "aws_resiliencehub_test_recommendations"
description: "`aws_resiliencehub_test_recommendations` reads real, system-recommended resilience tests for a Resilience Hub application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_test_recommendations"})

    		ubx.Data(resiliencehub.TestRecommendations, "example", resiliencehub.TestRecommendationsConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestRecommendations } from "@ubx/sdk-aws/aws/data/resiliencehub/test_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_test_recommendations" });

      data(TestRecommendations, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import TestRecommendations, TestRecommendationsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_test_recommendations")

        ubx.data(TestRecommendations, "example", TestRecommendationsConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="test_recommendations" type="list(object)">
  The real, recommended tests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
