---
title: "aws_resiliencehub_app_version"
description: "`aws_resiliencehub_app_version` looks up one real, saved version of a Resilience Hub application definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version"})

    		ubx.Data(resiliencehub.AppVersion, "example", resiliencehub.AppVersionConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppVersion } from "@ubx/sdk-aws/aws/data/resiliencehub/app_version";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_version" });

      data(AppVersion, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersion, AppVersionConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version")

        ubx.data(AppVersion, "example", AppVersionConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="additional_info" type="map(list(string))">
  The real, additional information about this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
