---
title: "aws_resiliencehub_app_assessments"
description: "`aws_resiliencehub_app_assessments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_resiliencehub_app_assessments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_assessments"})

    		ubx.Data(resiliencehub.AppAssessments, "example", resiliencehub.AppAssessmentsConfig{
    			AppArn:           "arn:aws:iam::123456789012:policy/example",
    			AssessmentName:   "example-assessment-name",
    			AssessmentStatus: []string{"example"},
    			ComplianceStatus: "example",
    			Invoker:          "example",
    			MaxResults:       1,
    			ReverseOrder:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppAssessments } from "@ubx/sdk-aws/aws/data/resiliencehub/app_assessments";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_assessments" });

      data(AppAssessments, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        assessmentName: "example-assessment-name",
        assessmentStatus: ["example"],
        complianceStatus: "example",
        invoker: "example",
        maxResults: 1,
        reverseOrder: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppAssessments, AppAssessmentsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_assessments")

        ubx.data(AppAssessments, "example", AppAssessmentsConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            assessment_name="example-assessment-name",
            assessment_status=["example"],
            compliance_status="example",
            invoker="example",
            max_results=1,
            reverse_order=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="assessment_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="assessment_status" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="compliance_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="invoker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reverse_order" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
