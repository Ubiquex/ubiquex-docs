---
title: "aws_resiliencehub_app_assessment_compliance_drifts"
description: "`aws_resiliencehub_app_assessment_compliance_drifts` reads real, detected compliance drift between two Resilience Hub assessments of the same application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_assessment_compliance_drifts"})

    		ubx.Data(resiliencehub.AppAssessmentComplianceDrifts, "example", resiliencehub.AppAssessmentComplianceDriftsConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppAssessmentComplianceDrifts } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/app_assessment_compliance_drifts.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehub_app_assessment_compliance_drifts",
      });

      data(AppAssessmentComplianceDrifts, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppAssessmentComplianceDrifts, AppAssessmentComplianceDriftsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_assessment_compliance_drifts")

        ubx.data(AppAssessmentComplianceDrifts, "example", AppAssessmentComplianceDriftsConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance_drifts" type="list(object)">
  The real compliance drifts detected for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
