---
title: "aws_resiliencehub_metrics_export"
description: "`aws_resiliencehub_metrics_export` reads the real, current status of exporting Resilience Hub resiliency metrics."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_metrics_export"})

    		ubx.Data(resiliencehub.MetricsExport, "example", resiliencehub.MetricsExportConfig{
    			MetricsExportId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetricsExport } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/metrics_export.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_metrics_export" });

      data(MetricsExport, "example", {
        metricsExportId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import MetricsExport, MetricsExportConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_metrics_export")

        ubx.data(MetricsExport, "example", MetricsExportConfig(
            metrics_export_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metrics_export_id" type="string" required>
  The real, unique identifier of the metrics export this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="export_location" type="object">
  &lt;p&gt;The location of the Amazon S3 bucket.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
