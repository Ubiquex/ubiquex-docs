---
title: "aws_resiliencehub_app_component_compliances"
description: "`aws_resiliencehub_app_component_compliances` reads the real, per-component resiliency policy compliance results of a Resilience Hub assessment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_component_compliances"})

    		ubx.Data(resiliencehub.AppComponentCompliances, "example", resiliencehub.AppComponentCompliancesConfig{
    			AssessmentArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppComponentCompliances } from "@ubx/sdk-aws/aws/data/resiliencehub/app_component_compliances";

    export default stack("example", () => {
      intent({ summary: "look up aws_resiliencehub_app_component_compliances" });

      data(AppComponentCompliances, "example", {
        assessmentArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppComponentCompliances, AppComponentCompliancesConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_component_compliances")

        ubx.data(AppComponentCompliances, "example", AppComponentCompliancesConfig(
            assessment_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_compliances" type="list(object)">
  The real compliance results for each component. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
