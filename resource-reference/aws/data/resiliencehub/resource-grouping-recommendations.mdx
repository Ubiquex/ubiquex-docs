---
title: "aws_resiliencehub_resource_grouping_recommendations"
description: "`aws_resiliencehub_resource_grouping_recommendations` reads real, system-recommended groupings of resources into Resilience Hub application components."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_resource_grouping_recommendations"})

    		ubx.Data(resiliencehub.ResourceGroupingRecommendations, "example", resiliencehub.ResourceGroupingRecommendationsConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupingRecommendations } from "@ubx/sdk-aws/aws/data/resiliencehub/resource_grouping_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_resiliencehub_resource_grouping_recommendations",
      });

      data(ResourceGroupingRecommendations, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import ResourceGroupingRecommendations, ResourceGroupingRecommendationsConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_resource_grouping_recommendations")

        ubx.data(ResourceGroupingRecommendations, "example", ResourceGroupingRecommendationsConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string">
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grouping_recommendations" type="list(object)">
  The real, recommended groupings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
