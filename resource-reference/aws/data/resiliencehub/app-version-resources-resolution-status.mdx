---
title: "aws_resiliencehub_app_version_resources_resolution_status"
description: "`aws_resiliencehub_app_version_resources_resolution_status` reads the real, current status of resolving a Resilience Hub application version's..."
---

`aws_resiliencehub_app_version_resources_resolution_status` reads the real, current status of resolving a Resilience Hub application version's own resources from its input sources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resiliencehub_app_version_resources_resolution_status"})

    		ubx.Data(resiliencehub.AppVersionResourcesResolutionStatus, "example", resiliencehub.AppVersionResourcesResolutionStatusConfig{
    			AppArn:     "arn:aws:iam::123456789012:policy/example",
    			AppVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppVersionResourcesResolutionStatus } from "./local-sdk/aws/sdk/typescript/aws/data/resiliencehub/app_version_resources_resolution_status.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_resiliencehub_app_version_resources_resolution_status",
      });

      data(AppVersionResourcesResolutionStatus, "example", {
        appArn: "arn:aws:iam::123456789012:policy/example",
        appVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resiliencehub import AppVersionResourcesResolutionStatus, AppVersionResourcesResolutionStatusConfig

    def describe():
        ubx.intent("look up aws_resiliencehub_app_version_resources_resolution_status")

        ubx.data(AppVersionResourcesResolutionStatus, "example", AppVersionResourcesResolutionStatusConfig(
            app_arn="arn:aws:iam::123456789012:policy/example",
            app_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="app_version" type="string" required>
  The real version of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolution_id" type="string">
  The real, unique identifier of the resolution this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
