---
title: "aws_sqs_message_move_tasks"
description: "`aws_sqs_message_move_tasks` reads the real, in-progress or completed tasks moving messages from an SQS dead-letter queue back to its source queue."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sqs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sqs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sqs_message_move_tasks"})

    		ubx.Data(sqs.MessageMoveTasks, "example", sqs.MessageMoveTasksConfig{
    			SourceArn:  "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessageMoveTasks } from "@ubx/sdk-aws/aws/data/sqs/message_move_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_sqs_message_move_tasks" });

      data(MessageMoveTasks, "example", {
        sourceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sqs import MessageMoveTasks, MessageMoveTasksConfig

    def describe():
        ubx.intent("look up aws_sqs_message_move_tasks")

        ubx.data(MessageMoveTasks, "example", MessageMoveTasksConfig(
            source_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the source this object was derived from. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
