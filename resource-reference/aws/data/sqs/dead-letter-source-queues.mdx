---
title: "aws_sqs_dead_letter_source_queues"
description: "`aws_sqs_dead_letter_source_queues` reads the real source queues configured to send failed messages to a given SQS dead-letter queue."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sqs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sqs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sqs_dead_letter_source_queues"})

    		ubx.Data(sqs.DeadLetterSourceQueues, "example", sqs.DeadLetterSourceQueuesConfig{
    			QueueUrl:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeadLetterSourceQueues } from "@ubx/sdk-aws/aws/data/sqs/dead_letter_source_queues";

    export default stack("example", () => {
      intent({ summary: "look up aws_sqs_dead_letter_source_queues" });

      data(DeadLetterSourceQueues, "example", {
        queueUrl: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sqs import DeadLetterSourceQueues, DeadLetterSourceQueuesConfig

    def describe():
        ubx.intent("look up aws_sqs_dead_letter_source_queues")

        ubx.data(DeadLetterSourceQueues, "example", DeadLetterSourceQueuesConfig(
            queue_url="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="queue_url" type="string" required>
  The real, resolvable URL of the queue this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queue_urls" type="list(string)">
  The real, resolvable URLs of the queues included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
