---
title: "aws_entitlement_marketplace_entitlements"
description: "`aws_entitlement_marketplace_entitlements` reads the real AWS Marketplace entitlements granted to the account for a purchased product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entitlement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/entitlement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_entitlement_marketplace_entitlements"})

    		ubx.Data(entitlement_marketplace.EntitlementMarketplaceEntitlements, "example", entitlement_marketplace.EntitlementMarketplaceEntitlementsConfig{
    			ProductCode: "example",
    			Filter:      map[string]string{"managed-by": "ubx"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntitlementMarketplaceEntitlements } from "@ubx/sdk-aws/aws/data/entitlement_marketplace/entitlement_marketplace_entitlements";

    export default stack("example", () => {
      intent({ summary: "look up aws_entitlement_marketplace_entitlements" });

      data(EntitlementMarketplaceEntitlements, "example", {
        productCode: "example",
        filter: { "managed-by": "ubx" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.entitlement_marketplace import EntitlementMarketplaceEntitlements, EntitlementMarketplaceEntitlementsConfig

    def describe():
        ubx.intent("look up aws_entitlement_marketplace_entitlements")

        ubx.data(EntitlementMarketplaceEntitlements, "example", EntitlementMarketplaceEntitlementsConfig(
            product_code="example",
            filter={"managed-by": "ubx"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="map(list(string))">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_code" type="string" required>
  The real code identifying the product. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entitlements" type="list(object)">
  The real entitlements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
