---
title: "aws_comprehendmedical_snomedctinference_jobs"
description: "`aws_comprehendmedical_snomedctinference_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_comprehendmedical_snomedctinference_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehendmedical "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/comprehendmedical"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_comprehendmedical_snomedctinference_jobs"})

    		ubx.Data(comprehendmedical.SnomedctinferenceJobs, "example", comprehendmedical.SnomedctinferenceJobsConfig{
    			Filter:     map[string]any{"job_name": "example-job-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnomedctinferenceJobs } from "@ubx/sdk-aws/aws/data/comprehendmedical/snomedctinference_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_comprehendmedical_snomedctinference_jobs" });

      data(SnomedctinferenceJobs, "example", {
        filter: { jobName: "example-job-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.comprehendmedical import SnomedctinferenceJobs, SnomedctinferenceJobsConfig

    def describe():
        ubx.intent("look up aws_comprehendmedical_snomedctinference_jobs")

        ubx.data(SnomedctinferenceJobs, "example", SnomedctinferenceJobsConfig(
            filter={"job_name": "example-job-name"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Provides information for filtering a list of detection jobs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="comprehend_medical_async_job_properties_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
