---
title: "aws_resource_groups_grouping_statuses"
description: "`aws_resource_groups_grouping_statuses` reads the real, current status of resources being added to or removed from a Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_grouping_statuses"})

    		ubx.Data(resource_groups.ResourceGroupsGroupingStatuses, "example", resource_groups.ResourceGroupsGroupingStatusesConfig{
    			Group:      "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupsGroupingStatuses } from "@ubx/sdk-aws/aws/data/resource_groups/resource_groups_grouping_statuses";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_grouping_statuses" });

      data(ResourceGroupsGroupingStatuses, "example", {
        group: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsGroupingStatuses, ResourceGroupsGroupingStatusesConfig
    from ubx.aws.data.resource_groups.resource_groups_grouping_statuses import ResourceGroupsGroupingStatuses_Filters

    def describe():
        ubx.intent("look up aws_resource_groups_grouping_statuses")

        ubx.data(ResourceGroupsGroupingStatuses, "example", ResourceGroupsGroupingStatusesConfig(
            group="example",
            filters=[ResourceGroupsGroupingStatuses_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group" type="string" required>
  The real group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grouping_statuses" type="list(object)">
  The real, current statuses of the groupings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
