---
title: "aws_resource_groups_group_query"
description: "`aws_resource_groups_group_query` reads the real, tag-based query defining the membership of a Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_group_query"})

    		ubx.Data(resource_groups.ResourceGroupsGroupQuery, "example", resource_groups.ResourceGroupsGroupQueryConfig{
    			Group:     "example",
    			GroupName: "example-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceGroupsGroupQuery } from "./local-sdk/aws/sdk/typescript/aws/data/resource_groups/resource_groups_group_query.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_group_query" });

      data(ResourceGroupsGroupQuery, "example", {
        group: "example",
        groupName: "example-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsGroupQuery, ResourceGroupsGroupQueryConfig

    def describe():
        ubx.intent("look up aws_resource_groups_group_query")

        ubx.data(ResourceGroupsGroupQuery, "example", ResourceGroupsGroupQueryConfig(
            group="example",
            group_name="example-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group" type="string">
  The real group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string">
  The real name of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_query" type="object">
  &lt;p&gt;A mapping of a query attached to a resource group that determines the Amazon Web Services resources that are members of the group.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="group_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_query" type="object" required>
      &lt;p&gt;The query you can use to define a resource group or a search for resources. A &lt;code&gt;ResourceQuery&lt;/code&gt; specifies both a query &lt;code&gt;Type&lt;/code&gt; and a &lt;code&gt;Query&lt;/code&gt; string as JSON string objects. See the examples section for example JSON strings. For more information about creating a resource group with a resource query, see &lt;a href="https://docs.aws.amazon.com/ARG/latest/userguide/gettingstarted-query.html"&gt;Build queries and groups in Resource Groups&lt;/a&gt; in the &lt;i&gt;Resource Groups User Guide&lt;/i&gt; &lt;/p&gt; &lt;p&gt;When you combine all of the elements together into a single string, any double quotes that are embedded inside another double quote pair must be escaped by preceding the embedded double quote with a backslash character (\). For example, a complete &lt;code&gt;ResourceQuery&lt;/code&gt; parameter must be formatted like the following CLI parameter example:&lt;/p&gt; &lt;p&gt; &lt;code&gt;--resource-query '&#123;"Type":"TAG_FILTERS_1_0","Query":"&#123;\"ResourceTypeFilters\":[\"AWS::AllSupported\"],\"TagFilters\":[&#123;\"Key\":\"Stage\",\"Values\":[\"Test\"]&#125;]&#125;"&#125;'&lt;/code&gt; &lt;/p&gt; &lt;p&gt;In the preceding example, all of the double quote characters in the value part of the &lt;code&gt;Query&lt;/code&gt; element must be escaped because the value itself is surrounded by double quotes. For more information, see &lt;a href="https://docs.aws.amazon.com/cli/latest/userguide/cli-usage-parameters-quoting-strings.html"&gt;Quoting strings&lt;/a&gt; in the &lt;i&gt;Command Line Interface User Guide&lt;/i&gt;.&lt;/p&gt; &lt;p&gt;For the complete list of resource types that you can use in the array value for &lt;code&gt;ResourceTypeFilters&lt;/code&gt;, see &lt;a href="https://docs.aws.amazon.com/ARG/latest/userguide/supported-resources.html"&gt;Resources you can use with Resource Groups and Tag Editor&lt;/a&gt; in the &lt;i&gt;Resource Groups User Guide&lt;/i&gt;. For example:&lt;/p&gt; &lt;p&gt; &lt;code&gt;"ResourceTypeFilters":["AWS::S3::Bucket", "AWS::EC2::Instance"]&lt;/code&gt; &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="query" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
