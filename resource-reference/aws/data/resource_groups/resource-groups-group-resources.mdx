---
title: "aws_resource_groups_group_resources"
description: "`aws_resource_groups_group_resources` reads the real resources currently matching a Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_group_resources"})

    		ubx.Data(resource_groups.ResourceGroupsGroupResources, "example", resource_groups.ResourceGroupsGroupResourcesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			Group:      "example",
    			GroupName:  "example-group-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupsGroupResources } from "@ubx/sdk-aws/aws/data/resource_groups/resource_groups_group_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_group_resources" });

      data(ResourceGroupsGroupResources, "example", {
        filters: [{ name: "example-name" }],
        group: "example",
        groupName: "example-group-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsGroupResources, ResourceGroupsGroupResourcesConfig

    def describe():
        ubx.intent("look up aws_resource_groups_group_resources")

        ubx.data(ResourceGroupsGroupResources, "example", ResourceGroupsGroupResourcesConfig(
            filters=[{"name": "example-name"}],
            group="example",
            group_name="example-group-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group" type="string">
  The real group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string">
  The real name of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_errors" type="list(object)">
  The real errors encountered while running this query. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_identifiers" type="list(object)">
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The real AWS resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
