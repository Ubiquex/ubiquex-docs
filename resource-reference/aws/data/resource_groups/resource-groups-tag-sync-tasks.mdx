---
title: "aws_resource_groups_tag_sync_tasks"
description: "`aws_resource_groups_tag_sync_tasks` reads the real tag-sync tasks running for a Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_tag_sync_tasks"})

    		ubx.Data(resource_groups.ResourceGroupsTagSyncTasks, "example", resource_groups.ResourceGroupsTagSyncTasksConfig{
    			Filters:    []map[string]any{{"group_arn": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupsTagSyncTasks } from "@ubx/sdk-aws/aws/data/resource_groups/resource_groups_tag_sync_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_tag_sync_tasks" });

      data(ResourceGroupsTagSyncTasks, "example", {
        filters: [{ groupArn: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsTagSyncTasks, ResourceGroupsTagSyncTasksConfig

    def describe():
        ubx.intent("look up aws_resource_groups_tag_sync_tasks")

        ubx.data(ResourceGroupsTagSyncTasks, "example", ResourceGroupsTagSyncTasksConfig(
            filters=[{"group_arn": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_sync_tasks" type="list(object)">
  The real Amazon Connect tag sync tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
