---
title: "aws_resource_groups_tags"
description: "`aws_resource_groups_tags` reads the real tags applied to a Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_tags"})

    		ubx.Data(resource_groups.ResourceGroupsTags, "example", resource_groups.ResourceGroupsTagsConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupsTags } from "@ubx/sdk-aws/aws/data/resource_groups/resource_groups_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_tags" });

      data(ResourceGroupsTags, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsTags, ResourceGroupsTagsConfig

    def describe():
        ubx.intent("look up aws_resource_groups_tags")

        ubx.data(ResourceGroupsTags, "example", ResourceGroupsTagsConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
