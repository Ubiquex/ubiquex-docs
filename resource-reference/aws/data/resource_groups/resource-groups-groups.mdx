---
title: "aws_resource_groups_groups"
description: "`aws_resource_groups_groups` looks up a real Resource Group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resource_groups "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resource_groups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_groups"})

    		ubx.Data(resource_groups.ResourceGroupsGroups, "example", resource_groups.ResourceGroupsGroupsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceGroupsGroups } from "@ubx/sdk-aws/aws/data/resource_groups/resource_groups_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_groups" });

      data(ResourceGroupsGroups, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resource_groups import ResourceGroupsGroups, ResourceGroupsGroupsConfig
    from ubx.aws.data.resource_groups.resource_groups_groups import ResourceGroupsGroups_Filters

    def describe():
        ubx.intent("look up aws_resource_groups_groups")

        ubx.data(ResourceGroupsGroups, "example", ResourceGroupsGroupsConfig(
            filters=[ResourceGroupsGroups_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_identifiers" type="list(object)">
  The real, unique identifiers of the groups this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
