---
title: "aws_partnercentral_account_verification"
description: "`aws_partnercentral_account_verification` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_partnercentral_account_verification` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnercentral_account "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/partnercentral_account"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_partnercentral_account_verification"})

    		ubx.Data(partnercentral_account.PartnercentralAccountVerification, "example", partnercentral_account.PartnercentralAccountVerificationConfig{
    			VerificationType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PartnercentralAccountVerification } from "@ubx/sdk-aws/aws/data/partnercentral_account/partnercentral_account_verification";

    export default stack("example", () => {
      intent({ summary: "look up aws_partnercentral_account_verification" });

      data(PartnercentralAccountVerification, "example", {
        verificationType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.partnercentral_account import PartnercentralAccountVerification, PartnercentralAccountVerificationConfig

    def describe():
        ubx.intent("look up aws_partnercentral_account_verification")

        ubx.data(PartnercentralAccountVerification, "example", PartnercentralAccountVerificationConfig(
            verification_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="verification_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="completed_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="started_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="verification_response_details" type="object">
  &lt;p&gt;A union structure containing the response details specific to different types of verification processes, providing type-specific information and results.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="business_verification_response" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="business_verification_details" type="object">
          Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="country_code" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="jurisdiction_of_incorporation" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="legal_name" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="registration_id" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="completion_url" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="completion_url_expires_at" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="registrant_verification_response" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="completion_url" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="completion_url_expires_at" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="verification_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="verification_status_reason" type="string">
  Returned by the lookup.
</ResponseField>
