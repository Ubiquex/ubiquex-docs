---
title: "aws_ram_resource_types"
description: "`aws_ram_resource_types` reads the real AWS resource types that support sharing via AWS RAM."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_resource_types"})

    		ubx.Data(ram.ResourceTypes, "example", ram.ResourceTypesConfig{
    			MaxResults:          1,
    			ResourceRegionScope: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceTypes } from "./local-sdk/aws/sdk/typescript/aws/data/ram/resource_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_resource_types" });

      data(ResourceTypes, "example", {
        maxResults: 1,
        resourceRegionScope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ram import ResourceTypes, ResourceTypesConfig

    def describe():
        ubx.intent("look up aws_ram_resource_types")

        ubx.data(ResourceTypes, "example", ResourceTypesConfig(
            max_results=1,
            resource_region_scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_region_scope" type="string">
  The real AWS regions this resource's own scope applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_types" type="list(object)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
