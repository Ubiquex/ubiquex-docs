---
title: "aws_ram_permission_associations"
description: "`aws_ram_permission_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ram_permission_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_permission_associations"})

    		ubx.Data(ram.PermissionAssociations, "example", ram.PermissionAssociationsConfig{
    			AssociationStatus: "example",
    			DefaultVersion:    true,
    			FeatureSet:        "example",
    			MaxResults:        1,
    			PermissionArn:     "arn:aws:iam::123456789012:policy/example",
    			PermissionVersion: 1,
    			ResourceType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PermissionAssociations } from "@ubx/sdk-aws/aws/data/ram/permission_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_permission_associations" });

      data(PermissionAssociations, "example", {
        associationStatus: "example",
        defaultVersion: true,
        featureSet: "example",
        maxResults: 1,
        permissionArn: "arn:aws:iam::123456789012:policy/example",
        permissionVersion: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import PermissionAssociations, PermissionAssociationsConfig

    def describe():
        ubx.intent("look up aws_ram_permission_associations")

        ubx.data(PermissionAssociations, "example", PermissionAssociationsConfig(
            association_status="example",
            default_version=True,
            feature_set="example",
            max_results=1,
            permission_arn="arn:aws:iam::123456789012:policy/example",
            permission_version=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="default_version" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="feature_set" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="permission_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="permission_version" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  Returned by the lookup.
</ResponseField>
