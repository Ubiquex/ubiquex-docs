---
title: "aws_ram_resource_share_associations"
description: "`data_aws_ram_resource_share_associations` looks up a real, already-existing `aws_ram_resource_share`."
---

It creates a real cross-account resource share, listing the specific resource ARNs, principal accounts or organizational units, and permissions involved, the core AWS Resource Access Manager resource that actually grants another account access to a resource without duplicating it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_resource_share_associations"})

    		ubx.Data(ram.ResourceShareAssociations, "example", ram.ResourceShareAssociationsConfig{
    			AssociationType:   "example",
    			AssociationStatus: "example",
    			MaxResults:        1,
    			Principal:         "example",
    			ResourceArn:       "arn:aws:iam::123456789012:policy/example",
    			ResourceShareArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourceShareAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/ram/resource_share_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_resource_share_associations" });

      data(ResourceShareAssociations, "example", {
        associationType: "example",
        associationStatus: "example",
        maxResults: 1,
        principal: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceShareArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ram import ResourceShareAssociations, ResourceShareAssociationsConfig

    def describe():
        ubx.intent("look up aws_ram_resource_share_associations")

        ubx.data(ResourceShareAssociations, "example", ResourceShareAssociationsConfig(
            association_type="example",
            association_status="example",
            max_results=1,
            principal="example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_share_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_status" type="string">
  The real, current status of the association. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="association_type" type="string" required>
  The real type of this association. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal" type="string">
  The real IAM principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the resource shares this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_share_associations" type="list(object)">
  The real AWS Resource Access Manager resource share associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
