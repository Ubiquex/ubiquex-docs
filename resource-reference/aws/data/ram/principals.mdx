---
title: "aws_ram_principals"
description: "`aws_ram_principals` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ram_principals` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_principals"})

    		ubx.Data(ram.Principals, "example", ram.PrincipalsConfig{
    			ResourceOwner:     "example",
    			MaxResults:        1,
    			ResourceArn:       "arn:aws:iam::123456789012:policy/example",
    			ResourceShareArns: []string{"example"},
    			ResourceType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Principals } from "@ubx/sdk-aws/aws/data/ram/principals";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_principals" });

      data(Principals, "example", {
        resourceOwner: "example",
        maxResults: 1,
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceShareArns: ["example"],
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import Principals, PrincipalsConfig

    def describe():
        ubx.intent("look up aws_ram_principals")

        ubx.data(Principals, "example", PrincipalsConfig(
            resource_owner="example",
            max_results=1,
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_share_arns=["example"],
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_share_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="principals" type="list(string)">
  Returned by the lookup.
</ResponseField>
