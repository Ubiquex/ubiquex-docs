---
title: "aws_ram_source_associations"
description: "`aws_ram_source_associations` reads the real associations between an AWS RAM resource share and its own permissions or resources."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_source_associations"})

    		ubx.Data(ram.SourceAssociations, "example", ram.SourceAssociationsConfig{
    			AssociationStatus: "example",
    			MaxResults:        1,
    			ResourceShareArns: []string{"example"},
    			SourceId:          "example",
    			SourceType:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SourceAssociations } from "@ubx/sdk-aws/aws/data/ram/source_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_source_associations" });

      data(SourceAssociations, "example", {
        associationStatus: "example",
        maxResults: 1,
        resourceShareArns: ["example"],
        sourceId: "example",
        sourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import SourceAssociations, SourceAssociationsConfig

    def describe():
        ubx.intent("look up aws_ram_source_associations")

        ubx.data(SourceAssociations, "example", SourceAssociationsConfig(
            association_status="example",
            max_results=1,
            resource_share_arns=["example"],
            source_id="example",
            source_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_status" type="string">
  The real, current status of the association. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the resource shares this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_id" type="string">
  The real, unique identifier of the source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string">
  The real type of the source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_associations" type="list(object)">
  The real associations for this source. **(AI-inferred)** Returned by the lookup.
</ResponseField>
