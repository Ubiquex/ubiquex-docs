---
title: "aws_ram_pending_invitation_resources"
description: "`aws_ram_pending_invitation_resources` reads the real resources included in a pending AWS RAM resource share invitation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_pending_invitation_resources"})

    		ubx.Data(ram.PendingInvitationResources, "example", ram.PendingInvitationResourcesConfig{
    			ResourceShareInvitationArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                 1,
    			ResourceRegionScope:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PendingInvitationResources } from "@ubx/sdk-aws/aws/data/ram/pending_invitation_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_pending_invitation_resources" });

      data(PendingInvitationResources, "example", {
        resourceShareInvitationArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        resourceRegionScope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import PendingInvitationResources, PendingInvitationResourcesConfig

    def describe():
        ubx.intent("look up aws_ram_pending_invitation_resources")

        ubx.data(PendingInvitationResources, "example", PendingInvitationResourcesConfig(
            resource_share_invitation_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            resource_region_scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_region_scope" type="string">
  The real AWS regions this resource's own scope applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_invitation_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the resource share invitation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  The real AWS resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
