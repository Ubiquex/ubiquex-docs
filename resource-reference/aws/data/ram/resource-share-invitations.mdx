---
title: "aws_ram_resource_share_invitations"
description: "`aws_ram_resource_share_invitations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ram_resource_share_invitations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_resource_share_invitations"})

    		ubx.Data(ram.ResourceShareInvitations, "example", ram.ResourceShareInvitationsConfig{
    			MaxResults:                  1,
    			ResourceShareArns:           []string{"example"},
    			ResourceShareInvitationArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceShareInvitations } from "@ubx/sdk-aws/aws/data/ram/resource_share_invitations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_resource_share_invitations" });

      data(ResourceShareInvitations, "example", {
        maxResults: 1,
        resourceShareArns: ["example"],
        resourceShareInvitationArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import ResourceShareInvitations, ResourceShareInvitationsConfig

    def describe():
        ubx.intent("look up aws_ram_resource_share_invitations")

        ubx.data(ResourceShareInvitations, "example", ResourceShareInvitationsConfig(
            max_results=1,
            resource_share_arns=["example"],
            resource_share_invitation_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_invitation_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_share_invitations" type="list(object)">
  Returned by the lookup.
</ResponseField>
