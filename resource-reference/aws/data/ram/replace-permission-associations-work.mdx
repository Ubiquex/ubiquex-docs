---
title: "aws_ram_replace_permission_associations_work"
description: "`aws_ram_replace_permission_associations_work` reads the real, current status of replacing an AWS RAM resource share's own managed permission."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ram_replace_permission_associations_work"})

    		ubx.Data(ram.ReplacePermissionAssociationsWork, "example", ram.ReplacePermissionAssociationsWorkConfig{
    			MaxResults: 1,
    			Status:     "example",
    			WorkIds:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReplacePermissionAssociationsWork } from "@ubx/sdk-aws/aws/data/ram/replace_permission_associations_work";

    export default stack("example", () => {
      intent({ summary: "look up aws_ram_replace_permission_associations_work" });

      data(ReplacePermissionAssociationsWork, "example", {
        maxResults: 1,
        status: "example",
        workIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ram import ReplacePermissionAssociationsWork, ReplacePermissionAssociationsWorkConfig

    def describe():
        ubx.intent("look up aws_ram_replace_permission_associations_work")

        ubx.data(ReplacePermissionAssociationsWork, "example", ReplacePermissionAssociationsWorkConfig(
            max_results=1,
            status="example",
            work_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="work_ids" type="list(string)">
  The real, unique identifiers of the works this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replace_permission_associations_works" type="list(object)">
  The real, in-progress permission association replacement work items. **(AI-inferred)** Returned by the lookup.
</ResponseField>
