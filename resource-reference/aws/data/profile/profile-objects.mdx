---
title: "aws_profile_profile_objects"
description: "`aws_profile_profile_objects` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_profile_objects` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_profile_objects"})

    		ubx.Data(profile.ProfileObjects, "example", profile.ProfileObjectsConfig{
    			DomainName:     "example-domain-name",
    			ObjectTypeName: "example-object-type-name",
    			ProfileId:      "example",
    			MaxResults:     1,
    			ObjectFilter:   map[string]any{"key_name": "example-key-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileObjects } from "@ubx/sdk-aws/aws/data/profile/profile_objects";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_profile_objects" });

      data(ProfileObjects, "example", {
        domainName: "example-domain-name",
        objectTypeName: "example-object-type-name",
        profileId: "example",
        maxResults: 1,
        objectFilter: { keyName: "example-key-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ProfileObjects, ProfileObjectsConfig

    def describe():
        ubx.intent("look up aws_profile_profile_objects")

        ubx.data(ProfileObjects, "example", ProfileObjectsConfig(
            domain_name="example-domain-name",
            object_type_name="example-object-type-name",
            profile_id="example",
            max_results=1,
            object_filter={"key_name": "example-key-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="object_filter" type="object">
  &lt;p&gt;The filter applied to &lt;code&gt;ListProfileObjects&lt;/code&gt; response to include profile objects with the specified index values.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="key_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="values" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="object_type_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
