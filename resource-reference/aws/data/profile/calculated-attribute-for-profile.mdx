---
title: "aws_profile_calculated_attribute_for_profile"
description: "`aws_profile_calculated_attribute_for_profile` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_calculated_attribute_for_profile` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_calculated_attribute_for_profile"})

    		ubx.Data(profile.CalculatedAttributeForProfile, "example", profile.CalculatedAttributeForProfileConfig{
    			CalculatedAttributeName: "example-calculated-attribute-name",
    			DomainName:              "example-domain-name",
    			ProfileId:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CalculatedAttributeForProfile } from "@ubx/sdk-aws/aws/data/profile/calculated_attribute_for_profile";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_calculated_attribute_for_profile" });

      data(CalculatedAttributeForProfile, "example", {
        calculatedAttributeName: "example-calculated-attribute-name",
        domainName: "example-domain-name",
        profileId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import CalculatedAttributeForProfile, CalculatedAttributeForProfileConfig

    def describe():
        ubx.intent("look up aws_profile_calculated_attribute_for_profile")

        ubx.data(CalculatedAttributeForProfile, "example", CalculatedAttributeForProfileConfig(
            calculated_attribute_name="example-calculated-attribute-name",
            domain_name="example-domain-name",
            profile_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="calculated_attribute_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="display_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_data_partial" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_object_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="string">
  Returned by the lookup.
</ResponseField>
