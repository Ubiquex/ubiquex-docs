---
title: "aws_profile_object_type_attribute_values"
description: "`aws_profile_object_type_attribute_values` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_object_type_attribute_values` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_object_type_attribute_values"})

    		ubx.Data(profile.ObjectTypeAttributeValues, "example", profile.ObjectTypeAttributeValuesConfig{
    			AttributeName:  "example-attribute-name",
    			DomainName:     "example-domain-name",
    			ObjectTypeName: "example-object-type-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectTypeAttributeValues } from "@ubx/sdk-aws/aws/data/profile/object_type_attribute_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_object_type_attribute_values" });

      data(ObjectTypeAttributeValues, "example", {
        attributeName: "example-attribute-name",
        domainName: "example-domain-name",
        objectTypeName: "example-object-type-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ObjectTypeAttributeValues, ObjectTypeAttributeValuesConfig

    def describe():
        ubx.intent("look up aws_profile_object_type_attribute_values")

        ubx.data(ObjectTypeAttributeValues, "example", ObjectTypeAttributeValuesConfig(
            attribute_name="example-attribute-name",
            domain_name="example-domain-name",
            object_type_name="example-object-type-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="object_type_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
