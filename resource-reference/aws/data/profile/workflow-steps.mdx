---
title: "aws_profile_workflow_steps"
description: "`aws_profile_workflow_steps` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_workflow_steps` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_workflow_steps"})

    		ubx.Data(profile.WorkflowSteps, "example", profile.WorkflowStepsConfig{
    			DomainName: "example-domain-name",
    			WorkflowId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkflowSteps } from "@ubx/sdk-aws/aws/data/profile/workflow_steps";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_workflow_steps" });

      data(WorkflowSteps, "example", {
        domainName: "example-domain-name",
        workflowId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import WorkflowSteps, WorkflowStepsConfig

    def describe():
        ubx.intent("look up aws_profile_workflow_steps")

        ubx.data(WorkflowSteps, "example", WorkflowStepsConfig(
            domain_name="example-domain-name",
            workflow_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workflow_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="workflow_type" type="string">
  Returned by the lookup.
</ResponseField>
