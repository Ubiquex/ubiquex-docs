---
title: "aws_profile_workflows"
description: "`aws_profile_workflows` reads the real integration workflows configured within a Customer Profiles domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_workflows"})

    		ubx.Data(profile.Workflows, "example", profile.WorkflowsConfig{
    			DomainName:     "example-domain-name",
    			MaxResults:     1,
    			QueryEndDate:   "example",
    			QueryStartDate: "example",
    			Status:         "example",
    			WorkflowType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workflows } from "@ubx/sdk-aws/aws/data/profile/workflows";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_workflows" });

      data(Workflows, "example", {
        domainName: "example-domain-name",
        maxResults: 1,
        queryEndDate: "example",
        queryStartDate: "example",
        status: "example",
        workflowType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import Workflows, WorkflowsConfig

    def describe():
        ubx.intent("look up aws_profile_workflows")

        ubx.data(Workflows, "example", WorkflowsConfig(
            domain_name="example-domain-name",
            max_results=1,
            query_end_date="example",
            query_start_date="example",
            status="example",
            workflow_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_end_date" type="string">
  The real date of the query end. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_start_date" type="string">
  The real date of the query start. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workflow_type" type="string">
  The real, discriminating type of the workflow this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
