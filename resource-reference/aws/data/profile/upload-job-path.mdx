---
title: "aws_profile_upload_job_path"
description: "`aws_profile_upload_job_path` reads the real, generated S3 upload path for a Customer Profiles bulk upload job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_upload_job_path"})

    		ubx.Data(profile.UploadJobPath, "example", profile.UploadJobPathConfig{
    			DomainName: "example-domain-name",
    			JobId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UploadJobPath } from "@ubx/sdk-aws/aws/data/profile/upload_job_path";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_upload_job_path" });

      data(UploadJobPath, "example", {
        domainName: "example-domain-name",
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import UploadJobPath, UploadJobPathConfig

    def describe():
        ubx.intent("look up aws_profile_upload_job_path")

        ubx.data(UploadJobPath, "example", UploadJobPathConfig(
            domain_name="example-domain-name",
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_token" type="string">
  A real, caller-supplied token guaranteeing this request's own idempotency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="valid_until" type="string">
  The real timestamp this object remains valid until. **(AI-inferred)** Returned by the lookup.
</ResponseField>
