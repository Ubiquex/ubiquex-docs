---
title: "aws_profile_identity_resolution_job"
description: "`aws_profile_identity_resolution_job` looks up one real run of Customer Profiles' own identity resolution (auto-merge) job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_identity_resolution_job"})

    		ubx.Data(profile.IdentityResolutionJob, "example", profile.IdentityResolutionJobConfig{
    			DomainName: "example-domain-name",
    			JobId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityResolutionJob } from "@ubx/sdk-aws/aws/data/profile/identity_resolution_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_identity_resolution_job" });

      data(IdentityResolutionJob, "example", {
        domainName: "example-domain-name",
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import IdentityResolutionJob, IdentityResolutionJobConfig

    def describe():
        ubx.intent("look up aws_profile_identity_resolution_job")

        ubx.data(IdentityResolutionJob, "example", IdentityResolutionJobConfig(
            domain_name="example-domain-name",
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_merging" type="object">
  &lt;p&gt;Configuration settings for how to perform the auto-merging of profiles.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="conflict_resolution" type="object">
      &lt;p&gt;How the auto-merging process should resolve conflicts between different profiles.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="conflict_resolving_model" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="source_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="consolidation" type="object">
      &lt;p&gt;The matching criteria to be used during the auto-merging process. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="matching_attributes_list" type="list(list(string))" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="min_allowed_confidence_score_for_merging" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="exporting_location" type="object">
  &lt;p&gt;The S3 location where Identity Resolution Jobs write result files.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_exporting" type="object">
      &lt;p&gt;The S3 location where Identity Resolution Jobs write result files.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_bucket_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="s3_key_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_end_time" type="string">
  The real timestamp of the job end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_expiration_time" type="string">
  The real timestamp of the job expiration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_start_time" type="string">
  The real timestamp of the job start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_stats" type="object">
  &lt;p&gt;Statistics about the Identity Resolution Job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="number_of_matches_found" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="number_of_merges_done" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="number_of_profiles_reviewed" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
