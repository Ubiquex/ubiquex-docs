---
title: "aws_profile_auto_merging_preview"
description: "`aws_profile_auto_merging_preview` reads a real, previewed result of Customer Profiles' own automatic profile-merging logic before applying it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_auto_merging_preview"})

    		ubx.Data(profile.AutoMergingPreview, "example", profile.AutoMergingPreviewConfig{
    			ConflictResolution:                  map[string]any{"conflict_resolving_model": "example"},
    			Consolidation:                       map[string]any{"matching_attributes_list": []string{"example"}},
    			DomainName:                          "example-domain-name",
    			MinAllowedConfidenceScoreForMerging: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutoMergingPreview } from "@ubx/sdk-aws/aws/data/profile/auto_merging_preview";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_auto_merging_preview" });

      data(AutoMergingPreview, "example", {
        conflictResolution: { conflictResolvingModel: "example" },
        consolidation: { matchingAttributesList: ["example"] },
        domainName: "example-domain-name",
        minAllowedConfidenceScoreForMerging: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import AutoMergingPreview, AutoMergingPreviewConfig

    def describe():
        ubx.intent("look up aws_profile_auto_merging_preview")

        ubx.data(AutoMergingPreview, "example", AutoMergingPreviewConfig(
            conflict_resolution={"conflict_resolving_model": "example"},
            consolidation={"matching_attributes_list": ["example"]},
            domain_name="example-domain-name",
            min_allowed_confidence_score_for_merging=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="conflict_resolution" type="object" required>
  &lt;p&gt;How the auto-merging process should resolve conflicts between different profiles.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="conflict_resolving_model" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="source_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consolidation" type="object" required>
  &lt;p&gt;The matching criteria to be used during the auto-merging process. &lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="matching_attributes_list" type="list(list(string))" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="min_allowed_confidence_score_for_merging" type="number">
  The real, minimum confidence score required to merge these results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="number_of_matches_in_sample" type="number">
  The real number of matches found in this sample. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_profiles_in_sample" type="number">
  The real number of profiles included in this sample. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_profiles_will_be_merged" type="number">
  The real number of profiles that will be merged by this operation. **(AI-inferred)** Returned by the lookup.
</ResponseField>
