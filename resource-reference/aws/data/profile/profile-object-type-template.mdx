---
title: "aws_profile_profile_object_type_template"
description: "`aws_profile_profile_object_type_template` looks up a real, predefined object type template (such as for Salesforce or Zendesk data) provided by..."
---

`aws_profile_profile_object_type_template` looks up a real, predefined object type template (such as for Salesforce or Zendesk data) provided by Customer Profiles.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_profile_object_type_template"})

    		ubx.Data(profile.ProfileObjectTypeTemplate, "example", profile.ProfileObjectTypeTemplateConfig{
    			TemplateId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ProfileObjectTypeTemplate } from "./local-sdk/aws/sdk/typescript/aws/data/profile/profile_object_type_template.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_profile_object_type_template" });

      data(ProfileObjectTypeTemplate, "example", {
        templateId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ProfileObjectTypeTemplate, ProfileObjectTypeTemplateConfig

    def describe():
        ubx.intent("look up aws_profile_profile_object_type_template")

        ubx.data(ProfileObjectTypeTemplate, "example", ProfileObjectTypeTemplateConfig(
            template_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="template_id" type="string" required>
  The real, unique identifier of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allow_profile_creation" type="bool">
  Whether new IAM Identity Center profiles may be created automatically. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fields" type="map(object)">
  The real fields included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="keys" type="map(list(object))">
  The real, unique keys of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_last_updated_timestamp_format" type="string">
  The real format of this source's own last-updated timestamp. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_name" type="string">
  The real name of the source this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_object" type="string">
  The real source object this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
