---
title: "aws_profile_similar_profiles"
description: "`aws_profile_similar_profiles` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_similar_profiles` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_similar_profiles"})

    		ubx.Data(profile.SimilarProfiles, "example", profile.SimilarProfilesConfig{
    			DomainName:  "example-domain-name",
    			MatchType:   "example",
    			SearchKey:   "example",
    			SearchValue: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SimilarProfiles } from "@ubx/sdk-aws/aws/data/profile/similar_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_similar_profiles" });

      data(SimilarProfiles, "example", {
        domainName: "example-domain-name",
        matchType: "example",
        searchKey: "example",
        searchValue: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import SimilarProfiles, SimilarProfilesConfig

    def describe():
        ubx.intent("look up aws_profile_similar_profiles")

        ubx.data(SimilarProfiles, "example", SimilarProfilesConfig(
            domain_name="example-domain-name",
            match_type="example",
            search_key="example",
            search_value="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="match_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search_key" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="search_value" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="confidence_score" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="match_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="profile_ids" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rule_level" type="number">
  Returned by the lookup.
</ResponseField>
