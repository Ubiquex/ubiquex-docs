---
title: "aws_profile_integrations"
description: "`aws_profile_integrations` reads the real data integrations feeding a Customer Profiles domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_integrations"})

    		ubx.Data(profile.Integrations, "example", profile.IntegrationsConfig{
    			DomainName:    "example-domain-name",
    			IncludeHidden: true,
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Integrations } from "@ubx/sdk-aws/aws/data/profile/integrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_integrations" });

      data(Integrations, "example", {
        domainName: "example-domain-name",
        includeHidden: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import Integrations, IntegrationsConfig

    def describe():
        ubx.intent("look up aws_profile_integrations")

        ubx.data(Integrations, "example", IntegrationsConfig(
            domain_name="example-domain-name",
            include_hidden=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_hidden" type="bool">
  Whether hidden items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
