---
title: "aws_profile_account_integrations"
description: "`aws_profile_account_integrations` reads the real, account-wide integrations configured for Amazon Connect Customer Profiles."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_account_integrations"})

    		ubx.Data(profile.AccountIntegrations, "example", profile.AccountIntegrationsConfig{
    			Uri:           "example",
    			IncludeHidden: true,
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountIntegrations } from "./local-sdk/aws/sdk/typescript/aws/data/profile/account_integrations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_account_integrations" });

      data(AccountIntegrations, "example", {
        uri: "example",
        includeHidden: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.profile import AccountIntegrations, AccountIntegrationsConfig

    def describe():
        ubx.intent("look up aws_profile_account_integrations")

        ubx.data(AccountIntegrations, "example", AccountIntegrationsConfig(
            uri="example",
            include_hidden=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_hidden" type="bool">
  Whether hidden items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="uri" type="string" required>
  The real, resolvable URI of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
