---
title: "aws_profile_domain_object_type"
description: "`aws_profile_domain_object_type` looks up one real object type mapping defined for a Customer Profiles domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_domain_object_type"})

    		ubx.Data(profile.DomainObjectType, "example", profile.DomainObjectTypeConfig{
    			DomainName:     "example-domain-name",
    			ObjectTypeName: "example-object-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainObjectType } from "@ubx/sdk-aws/aws/data/profile/domain_object_type";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_domain_object_type" });

      data(DomainObjectType, "example", {
        domainName: "example-domain-name",
        objectTypeName: "example-object-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import DomainObjectType, DomainObjectTypeConfig

    def describe():
        ubx.intent("look up aws_profile_domain_object_type")

        ubx.data(DomainObjectType, "example", DomainObjectTypeConfig(
            domain_name="example-domain-name",
            object_type_name="example-object-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="object_type_name" type="string" required>
  The real name of the object's own type. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_key" type="string">
  The real, unique key of the encryption this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fields" type="map(object)">
  The real fields included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
