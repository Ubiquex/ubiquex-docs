---
title: "aws_profile_segment_membership"
description: "`aws_profile_segment_membership` reads which real customer profiles currently belong to a segment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_segment_membership"})

    		ubx.Data(profile.SegmentMembership, "example", profile.SegmentMembershipConfig{
    			DomainName:            "example-domain-name",
    			ProfileIds:            []string{"example"},
    			SegmentDefinitionName: "example-segment-definition-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SegmentMembership } from "./local-sdk/aws/sdk/typescript/aws/data/profile/segment_membership.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_segment_membership" });

      data(SegmentMembership, "example", {
        domainName: "example-domain-name",
        profileIds: ["example"],
        segmentDefinitionName: "example-segment-definition-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.profile import SegmentMembership, SegmentMembershipConfig

    def describe():
        ubx.intent("look up aws_profile_segment_membership")

        ubx.data(SegmentMembership, "example", SegmentMembershipConfig(
            domain_name="example-domain-name",
            profile_ids=["example"],
            segment_definition_name="example-segment-definition-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="profile_ids" type="list(string)" required>
  The real, unique identifiers of the profiles this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="segment_definition_name" type="string" required>
  The real name of the segment definition this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failures" type="list(object)">
  The real failures included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_computed_at" type="string">
  The real timestamp this value was last computed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="profiles" type="list(object)">
  The real profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
