---
title: "aws_profile_profile_object_type_templates"
description: "`aws_profile_profile_object_type_templates` reads the real, predefined object type templates provided by Customer Profiles."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_profile_object_type_templates"})

    		ubx.Data(profile.ProfileObjectTypeTemplates, "example", profile.ProfileObjectTypeTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileObjectTypeTemplates } from "@ubx/sdk-aws/aws/data/profile/profile_object_type_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_profile_object_type_templates" });

      data(ProfileObjectTypeTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ProfileObjectTypeTemplates, ProfileObjectTypeTemplatesConfig

    def describe():
        ubx.intent("look up aws_profile_profile_object_type_templates")

        ubx.data(ProfileObjectTypeTemplates, "example", ProfileObjectTypeTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
