---
title: "aws_profile_domain_layouts"
description: "`aws_profile_domain_layouts` looks up a real Customer Profiles layout, controlling how profile data is displayed to Connect agents."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_domain_layouts"})

    		ubx.Data(profile.DomainLayouts, "example", profile.DomainLayoutsConfig{
    			DomainName: "example-domain-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DomainLayouts } from "./local-sdk/aws/sdk/typescript/aws/data/profile/domain_layouts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_domain_layouts" });

      data(DomainLayouts, "example", {
        domainName: "example-domain-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.profile import DomainLayouts, DomainLayoutsConfig

    def describe():
        ubx.intent("look up aws_profile_domain_layouts")

        ubx.data(DomainLayouts, "example", DomainLayoutsConfig(
            domain_name="example-domain-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
