---
title: "aws_profile_profile_attribute_values"
description: "`aws_profile_profile_attribute_values` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_profile_attribute_values` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_profile_attribute_values"})

    		ubx.Data(profile.ProfileAttributeValues, "example", profile.ProfileAttributeValuesConfig{
    			AttributeName: "example-attribute-name",
    			DomainName:    "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileAttributeValues } from "@ubx/sdk-aws/aws/data/profile/profile_attribute_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_profile_attribute_values" });

      data(ProfileAttributeValues, "example", {
        attributeName: "example-attribute-name",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ProfileAttributeValues, ProfileAttributeValuesConfig

    def describe():
        ubx.intent("look up aws_profile_profile_attribute_values")

        ubx.data(ProfileAttributeValues, "example", ProfileAttributeValuesConfig(
            attribute_name="example-attribute-name",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_code" type="number">
  Returned by the lookup.
</ResponseField>
