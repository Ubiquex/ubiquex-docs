---
title: "aws_profile_recommender_recipes"
description: "`aws_profile_recommender_recipes` reads the real recipe (algorithm) options available for a Customer Profiles recommender."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_recommender_recipes"})

    		ubx.Data(profile.RecommenderRecipes, "example", profile.RecommenderRecipesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecommenderRecipes } from "@ubx/sdk-aws/aws/data/profile/recommender_recipes";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_recommender_recipes" });

      data(RecommenderRecipes, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import RecommenderRecipes, RecommenderRecipesConfig

    def describe():
        ubx.intent("look up aws_profile_recommender_recipes")

        ubx.data(RecommenderRecipes, "example", RecommenderRecipesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommender_recipes" type="list(object)">
  The real Amazon Personalize recommender recipes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
