---
title: "aws_profile_rule_based_matches"
description: "`aws_profile_rule_based_matches` reads a real, potential duplicate profile match identified by rule-based (rather than ML) matching."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_rule_based_matches"})

    		ubx.Data(profile.RuleBasedMatches, "example", profile.RuleBasedMatchesConfig{
    			DomainName: "example-domain-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuleBasedMatches } from "@ubx/sdk-aws/aws/data/profile/rule_based_matches";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_rule_based_matches" });

      data(RuleBasedMatches, "example", {
        domainName: "example-domain-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import RuleBasedMatches, RuleBasedMatchesConfig

    def describe():
        ubx.intent("look up aws_profile_rule_based_matches")

        ubx.data(RuleBasedMatches, "example", RuleBasedMatchesConfig(
            domain_name="example-domain-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="match_ids" type="list(string)">
  The real, unique identifiers of the matchs this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
