---
title: "aws_profile_profile_recommendations"
description: "`aws_profile_profile_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_profile_profile_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	profile "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/profile"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_profile_profile_recommendations"})

    		ubx.Data(profile.ProfileRecommendations, "example", profile.ProfileRecommendationsConfig{
    			DomainName:      "example-domain-name",
    			ProfileId:       "example",
    			RecommenderName: "example-recommender-name",
    			CandidateIds:    []string{"example"},
    			Context:         map[string]string{"managed-by": "ubx"},
    			DiversityConfig: map[string]any{"enabled": true},
    			MaxResults:      1,
    			MetadataConfig:  map[string]any{"metadata_columns": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileRecommendations } from "@ubx/sdk-aws/aws/data/profile/profile_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_profile_profile_recommendations" });

      data(ProfileRecommendations, "example", {
        domainName: "example-domain-name",
        profileId: "example",
        recommenderName: "example-recommender-name",
        candidateIds: ["example"],
        context: { "managed-by": "ubx" },
        diversityConfig: { enabled: true },
        maxResults: 1,
        metadataConfig: { metadataColumns: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.profile import ProfileRecommendations, ProfileRecommendationsConfig

    def describe():
        ubx.intent("look up aws_profile_profile_recommendations")

        ubx.data(ProfileRecommendations, "example", ProfileRecommendationsConfig(
            domain_name="example-domain-name",
            profile_id="example",
            recommender_name="example-recommender-name",
            candidate_ids=["example"],
            context={"managed-by": "ubx"},
            diversity_config={"enabled": True},
            max_results=1,
            metadata_config={"metadata_columns": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="candidate_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="context" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="diversity_config" type="object">
  &lt;p&gt;Runtime diversity configuration for a &lt;code&gt;GetProfileRecommendations&lt;/code&gt; request.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="values" type="map(number)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metadata_config" type="object">
  &lt;p&gt;Configuration for metadata to include in recommendation responses.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="metadata_columns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommender_filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recommender_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommender_promotional_filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="recommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
