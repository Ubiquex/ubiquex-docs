---
title: "aws_fis_target_resource_types"
description: "`aws_fis_target_resource_types` reads the real, available FIS target resource types."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fis_target_resource_types"})

    		ubx.Data(fis.TargetResourceTypes, "example", fis.TargetResourceTypesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TargetResourceTypes } from "./local-sdk/aws/sdk/typescript/aws/data/fis/target_resource_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_fis_target_resource_types" });

      data(TargetResourceTypes, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fis import TargetResourceTypes, TargetResourceTypesConfig

    def describe():
        ubx.intent("look up aws_fis_target_resource_types")

        ubx.data(TargetResourceTypes, "example", TargetResourceTypesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_resource_types" type="list(object)">
  The real, discriminating types of target resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
