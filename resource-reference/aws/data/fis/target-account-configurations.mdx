---
title: "aws_fis_target_account_configurations"
description: "`data_aws_fis_target_account_configurations` looks up a real, already-existing `aws_fis_target_account_configuration`."
---

It registers a real member account, and the IAM role FIS should assume in it, as an eligible target for an `aws_fis_experiment_template` run from a different, orchestrating account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fis_target_account_configurations"})

    		ubx.Data(fis.TargetAccountConfigurations, "example", fis.TargetAccountConfigurationsConfig{
    			ExperimentTemplateId: "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TargetAccountConfigurations } from "@ubx/sdk-aws/aws/data/fis/target_account_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_fis_target_account_configurations" });

      data(TargetAccountConfigurations, "example", {
        experimentTemplateId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fis import TargetAccountConfigurations, TargetAccountConfigurationsConfig

    def describe():
        ubx.intent("look up aws_fis_target_account_configurations")

        ubx.data(TargetAccountConfigurations, "example", TargetAccountConfigurationsConfig(
            experiment_template_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="experiment_template_id" type="string" required>
  The real, unique identifier of the experiment template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_account_configurations" type="list(object)">
  The real target account configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
