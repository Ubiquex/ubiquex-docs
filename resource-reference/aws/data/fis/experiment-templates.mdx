---
title: "aws_fis_experiment_templates"
description: "`data_aws_fis_experiment_templates` looks up a real, already-existing `aws_fis_experiment_template`."
---

It defines a real chaos engineering experiment for AWS Fault Injection Service, which actions to inject, terminating instances, throttling an API, spiking CPU, against which real targets, and what stop conditions abort the experiment automatically if things go wrong.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fis_experiment_templates"})

    		ubx.Data(fis.ExperimentTemplates, "example", fis.ExperimentTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExperimentTemplates } from "./local-sdk/aws/sdk/typescript/aws/data/fis/experiment_templates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_fis_experiment_templates" });

      data(ExperimentTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fis import ExperimentTemplates, ExperimentTemplatesConfig

    def describe():
        ubx.intent("look up aws_fis_experiment_templates")

        ubx.data(ExperimentTemplates, "example", ExperimentTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="experiment_templates" type="list(object)">
  The real AWS Fault Injection Simulator experiment templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
