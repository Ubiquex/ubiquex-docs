---
title: "aws_fis_experiment_resolved_targets"
description: "`aws_fis_experiment_resolved_targets` reads the real, resolved target resources of an FIS experiment run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fis_experiment_resolved_targets"})

    		ubx.Data(fis.ExperimentResolvedTargets, "example", fis.ExperimentResolvedTargetsConfig{
    			ExperimentId: "example",
    			MaxResults:   1,
    			TargetName:   "example-target-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExperimentResolvedTargets } from "./local-sdk/aws/sdk/typescript/aws/data/fis/experiment_resolved_targets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_fis_experiment_resolved_targets" });

      data(ExperimentResolvedTargets, "example", {
        experimentId: "example",
        maxResults: 1,
        targetName: "example-target-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fis import ExperimentResolvedTargets, ExperimentResolvedTargetsConfig

    def describe():
        ubx.intent("look up aws_fis_experiment_resolved_targets")

        ubx.data(ExperimentResolvedTargets, "example", ExperimentResolvedTargetsConfig(
            experiment_id="example",
            max_results=1,
            target_name="example-target-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="experiment_id" type="string" required>
  The real, unique identifier of the experiment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_name" type="string">
  The real name of the target this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolved_targets" type="list(object)">
  The real targets of the resolved. **(AI-inferred)** Returned by the lookup.
</ResponseField>
