---
title: "aws_fis_experiment_target_account_configuration"
description: "`data_aws_fis_experiment_target_account_configuration` looks up a real, already-existing `aws_fis_target_account_configuration`."
---

It registers a real member account, and the IAM role FIS should assume in it, as an eligible target for an `aws_fis_experiment_template` run from a different, orchestrating account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fis_experiment_target_account_configuration"})

    		ubx.Data(fis.ExperimentTargetAccountConfiguration, "example", fis.ExperimentTargetAccountConfigurationConfig{
    			AccountId:    "example",
    			ExperimentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExperimentTargetAccountConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/fis/experiment_target_account_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_fis_experiment_target_account_configuration",
      });

      data(ExperimentTargetAccountConfiguration, "example", {
        accountId: "example",
        experimentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fis import ExperimentTargetAccountConfiguration, ExperimentTargetAccountConfigurationConfig

    def describe():
        ubx.intent("look up aws_fis_experiment_target_account_configuration")

        ubx.data(ExperimentTargetAccountConfiguration, "example", ExperimentTargetAccountConfigurationConfig(
            account_id="example",
            experiment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="experiment_id" type="string" required>
  The real, unique identifier of the experiment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="target_account_configuration" type="object">
  &lt;p&gt;Describes a target account configuration for an experiment.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
