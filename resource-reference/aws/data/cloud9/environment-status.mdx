---
title: "aws_cloud9_environment_status"
description: "`aws_cloud9_environment_status` reads the real, current status of a Cloud9 environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud9 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloud9"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloud9_environment_status"})

    		ubx.Data(cloud9.EnvironmentStatus, "example", cloud9.EnvironmentStatusConfig{
    			EnvironmentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnvironmentStatus } from "@ubx/sdk-aws/aws/data/cloud9/environment_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloud9_environment_status" });

      data(EnvironmentStatus, "example", {
        environmentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloud9 import EnvironmentStatus, EnvironmentStatusConfig

    def describe():
        ubx.intent("look up aws_cloud9_environment_status")

        ubx.data(EnvironmentStatus, "example", EnvironmentStatusConfig(
            environment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
