---
title: "aws_acm_tags_for_certificate"
description: "`aws_acm_tags_for_certificate` reads the real tags applied to an ACM certificate."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_tags_for_certificate"})

    		ubx.Data(acm.TagsForCertificate, "example", acm.TagsForCertificateConfig{
    			CertificateArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForCertificate } from "@ubx/sdk-aws/aws/data/acm/tags_for_certificate";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_tags_for_certificate" });

      data(TagsForCertificate, "example", {
        certificateArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm import TagsForCertificate, TagsForCertificateConfig

    def describe():
        ubx.intent("look up aws_acm_tags_for_certificate")

        ubx.data(TagsForCertificate, "example", TagsForCertificateConfig(
            certificate_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the certificate this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
