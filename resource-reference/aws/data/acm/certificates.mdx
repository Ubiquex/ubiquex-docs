---
title: "aws_acm_certificates"
description: "`aws_acm_certificates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_acm_certificates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_certificates"})

    		ubx.Data(acm.Certificates, "example", acm.CertificatesConfig{
    			CertificateKeyPairOrigins: []string{"example"},
    			CertificateStatuses:       []string{"example"},
    			Includes:                  map[string]any{"export_option": "example"},
    			MaxItems:                  1,
    			SortBy:                    "example",
    			SortOrder:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Certificates } from "@ubx/sdk-aws/aws/data/acm/certificates";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_certificates" });

      data(Certificates, "example", {
        certificateKeyPairOrigins: ["example"],
        certificateStatuses: ["example"],
        includes: { exportOption: "example" },
        maxItems: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm import Certificates, CertificatesConfig

    def describe():
        ubx.intent("look up aws_acm_certificates")

        ubx.data(Certificates, "example", CertificatesConfig(
            certificate_key_pair_origins=["example"],
            certificate_statuses=["example"],
            includes={"export_option": "example"},
            max_items=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_key_pair_origins" type="list(string)">
  &lt;p&gt;A list of &lt;code&gt;CertificateKeyPairOrigin&lt;/code&gt; values used to filter certificates.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="certificate_statuses" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="includes" type="object">
  &lt;p&gt;This structure can be used in the &lt;a&gt;ListCertificates&lt;/a&gt; action to filter the output of the certificate list. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="export_option" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="extended_key_usage" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="key_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="key_usage" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="managed_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
