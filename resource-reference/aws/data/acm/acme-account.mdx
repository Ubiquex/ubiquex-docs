---
title: "aws_acm_acme_account"
description: "`aws_acm_acme_account` looks up one real ACME account registered with ACM's own Private CA integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_acme_account"})

    		ubx.Data(acm.AcmeAccount, "example", acm.AcmeAccountConfig{
    			AccountUrl:      "example",
    			AcmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmeAccount } from "@ubx/sdk-aws/aws/data/acm/acme_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_acme_account" });

      data(AcmeAccount, "example", {
        accountUrl: "example",
        acmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm import AcmeAccount, AcmeAccountConfig

    def describe():
        ubx.intent("look up aws_acm_acme_account")

        ubx.data(AcmeAccount, "example", AcmeAccountConfig(
            account_url="example",
            acme_endpoint_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_url" type="string" required>
  The real, resolvable URL of the account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="acme_endpoint_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the ACM Private CA ACME endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acme_account" type="object">
  &lt;p&gt;Contains detailed information about an ACME account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="acme_external_account_binding_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="contacts" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="public_key_thumbprint" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
