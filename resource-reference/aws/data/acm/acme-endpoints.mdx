---
title: "aws_acm_acme_endpoints"
description: "`aws_acm_acme_endpoints` reads the real, available ACME endpoints for ACM's own Private CA integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_acme_endpoints"})

    		ubx.Data(acm.AcmeEndpoints, "example", acm.AcmeEndpointsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmeEndpoints } from "@ubx/sdk-aws/aws/data/acm/acme_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_acme_endpoints" });

      data(AcmeEndpoints, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm import AcmeEndpoints, AcmeEndpointsConfig

    def describe():
        ubx.intent("look up aws_acm_acme_endpoints")

        ubx.data(AcmeEndpoints, "example", AcmeEndpointsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acme_endpoints" type="list(object)">
  The real ACM Private CA ACME endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
