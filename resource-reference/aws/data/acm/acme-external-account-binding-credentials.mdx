---
title: "aws_acm_acme_external_account_binding_credentials"
description: "`aws_acm_acme_external_account_binding_credentials` reads real, generated external account binding credentials for ACM's own ACME integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_acme_external_account_binding_credentials"})

    		ubx.Data(acm.AcmeExternalAccountBindingCredentials, "example", acm.AcmeExternalAccountBindingCredentialsConfig{
    			AcmeExternalAccountBindingArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AcmeExternalAccountBindingCredentials } from "./local-sdk/aws/sdk/typescript/aws/data/acm/acme_external_account_binding_credentials.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_acm_acme_external_account_binding_credentials",
      });

      data(AcmeExternalAccountBindingCredentials, "example", {
        acmeExternalAccountBindingArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.acm import AcmeExternalAccountBindingCredentials, AcmeExternalAccountBindingCredentialsConfig

    def describe():
        ubx.intent("look up aws_acm_acme_external_account_binding_credentials")

        ubx.data(AcmeExternalAccountBindingCredentials, "example", AcmeExternalAccountBindingCredentialsConfig(
            acme_external_account_binding_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="acme_external_account_binding_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the acme external account binding this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_id" type="string">
  The real, unique identifier of the encryption key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="mac_key" type="string">
  The real, unique key of the mac this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
