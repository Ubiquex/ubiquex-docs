---
title: "aws_acm_acme_external_account_bindings"
description: "`aws_acm_acme_external_account_bindings` reads the real external account bindings configured for ACM's own ACME integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_acme_external_account_bindings"})

    		ubx.Data(acm.AcmeExternalAccountBindings, "example", acm.AcmeExternalAccountBindingsConfig{
    			AcmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcmeExternalAccountBindings } from "@ubx/sdk-aws/aws/data/acm/acme_external_account_bindings";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_acme_external_account_bindings" });

      data(AcmeExternalAccountBindings, "example", {
        acmeEndpointArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.acm import AcmeExternalAccountBindings, AcmeExternalAccountBindingsConfig

    def describe():
        ubx.intent("look up aws_acm_acme_external_account_bindings")

        ubx.data(AcmeExternalAccountBindings, "example", AcmeExternalAccountBindingsConfig(
            acme_endpoint_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="acme_endpoint_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the ACM Private CA ACME endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="external_account_bindings" type="list(object)">
  The real external account bindings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
