---
title: "aws_acm_account_configuration"
description: "`aws_acm_account_configuration` reads the real, account-wide ACM configuration, such as certificate expiry event settings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_acm_account_configuration"})

    		ubx.Data(acm.AccountConfiguration, "example", acm.AccountConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/acm/account_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_acm_account_configuration" });

      data(AccountConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.acm import AccountConfiguration, AccountConfigurationConfig

    def describe():
        ubx.intent("look up aws_acm_account_configuration")

        ubx.data(AccountConfiguration, "example", AccountConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="expiry_events" type="object">
  &lt;p&gt;Object containing expiration events options associated with an Amazon Web Services account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="days_before_expiry" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
