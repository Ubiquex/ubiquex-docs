---
title: "aws_aco_automation_automation_rule_preview"
description: "`aws_aco_automation_automation_rule_preview` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aco_automation_automation_rule_preview` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aco_automation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aco_automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aco_automation_automation_rule_preview"})

    		ubx.Data(aco_automation.AcoAutomationAutomationRulePreview, "example", aco_automation.AcoAutomationAutomationRulePreviewConfig{
    			RecommendedActionTypes: []string{"example"},
    			RuleType:               "example",
    			Criteria:               map[string]any{"ebs_volume_size_in_gib": []map[string]any{{"comparison": "example"}}},
    			MaxResults:             1,
    			OrganizationScope:      map[string]any{"account_ids": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcoAutomationAutomationRulePreview } from "@ubx/sdk-aws/aws/data/aco_automation/aco_automation_automation_rule_preview";

    export default stack("example", () => {
      intent({ summary: "look up aws_aco_automation_automation_rule_preview" });

      data(AcoAutomationAutomationRulePreview, "example", {
        recommendedActionTypes: ["example"],
        ruleType: "example",
        criteria: { ebsVolumeSizeInGib: [{ comparison: "example" }] },
        maxResults: 1,
        organizationScope: { accountIds: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aco_automation import AcoAutomationAutomationRulePreview, AcoAutomationAutomationRulePreviewConfig

    def describe():
        ubx.intent("look up aws_aco_automation_automation_rule_preview")

        ubx.data(AcoAutomationAutomationRulePreview, "example", AcoAutomationAutomationRulePreviewConfig(
            recommended_action_types=["example"],
            rule_type="example",
            criteria={"ebs_volume_size_in_gib": [{"comparison": "example"}]},
            max_results=1,
            organization_scope={"account_ids": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="criteria" type="object">
  &lt;p&gt; A set of conditions that specify which recommended action qualify for implementation. When a rule is active and a recommended action matches these criteria, Compute Optimizer implements the action at the scheduled run time. You can specify up to 20 conditions per filter criteria and 20 values per condition. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="ebs_volume_size_in_gib" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ebs_volume_type" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="estimated_monthly_savings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="look_back_period_in_days" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="region" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_tag" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="restart_needed" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_scope" type="object">
  &lt;p&gt; Defines the scope for organization-level rules when previewing matching actions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="recommended_action_types" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rule_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="preview_results" type="list(object)">
  Returned by the lookup.
</ResponseField>
