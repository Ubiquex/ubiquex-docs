---
title: "aws_aco_automation_accounts"
description: "`aws_aco_automation_accounts` reads the real accounts enrolled in AWS cost optimization automation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aco_automation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/aco_automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aco_automation_accounts"})

    		ubx.Data(aco_automation.AcoAutomationAccounts, "example", aco_automation.AcoAutomationAccountsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcoAutomationAccounts } from "@ubx/sdk-aws/aws/data/aco_automation/aco_automation_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_aco_automation_accounts" });

      data(AcoAutomationAccounts, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aco_automation import AcoAutomationAccounts, AcoAutomationAccountsConfig

    def describe():
        ubx.intent("look up aws_aco_automation_accounts")

        ubx.data(AcoAutomationAccounts, "example", AcoAutomationAccountsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accounts" type="list(object)">
  The real AWS accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
