---
title: "aws_aco_automation_enrollment_configuration"
description: "`aws_aco_automation_enrollment_configuration` reads the real enrollment configuration for cost optimization automation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aco_automation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/aco_automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aco_automation_enrollment_configuration"})

    		ubx.Data(aco_automation.AcoAutomationEnrollmentConfiguration, "example", aco_automation.AcoAutomationEnrollmentConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcoAutomationEnrollmentConfiguration } from "@ubx/sdk-aws/aws/data/aco_automation/aco_automation_enrollment_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_aco_automation_enrollment_configuration" });

      data(AcoAutomationEnrollmentConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aco_automation import AcoAutomationEnrollmentConfiguration, AcoAutomationEnrollmentConfigurationConfig

    def describe():
        ubx.intent("look up aws_aco_automation_enrollment_configuration")

        ubx.data(AcoAutomationEnrollmentConfiguration, "example", AcoAutomationEnrollmentConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="last_updated_timestamp" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_rule_mode" type="string">
  The real mode this organization-wide rule runs in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  The real, human-readable reason for the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
