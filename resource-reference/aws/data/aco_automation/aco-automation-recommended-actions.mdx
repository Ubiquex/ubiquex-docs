---
title: "aws_aco_automation_recommended_actions"
description: "`aws_aco_automation_recommended_actions` reads real, system-generated recommended cost optimization actions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aco_automation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/aco_automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aco_automation_recommended_actions"})

    		ubx.Data(aco_automation.AcoAutomationRecommendedActions, "example", aco_automation.AcoAutomationRecommendedActionsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AcoAutomationRecommendedActions } from "@ubx/sdk-aws/aws/data/aco_automation/aco_automation_recommended_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_aco_automation_recommended_actions" });

      data(AcoAutomationRecommendedActions, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aco_automation import AcoAutomationRecommendedActions, AcoAutomationRecommendedActionsConfig

    def describe():
        ubx.intent("look up aws_aco_automation_recommended_actions")

        ubx.data(AcoAutomationRecommendedActions, "example", AcoAutomationRecommendedActionsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommended_actions" type="list(object)">
  The real, recommended actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
