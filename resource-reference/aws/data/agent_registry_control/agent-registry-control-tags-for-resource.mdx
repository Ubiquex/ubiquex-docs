---
title: "aws_agent_registry_control_tags_for_resource"
description: "`aws_agent_registry_control_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_agent_registry_control_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agent_registry_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agent_registry_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agent_registry_control_tags_for_resource"})

    		ubx.Data(agent_registry_control.AgentRegistryControlTagsForResource, "example", agent_registry_control.AgentRegistryControlTagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentRegistryControlTagsForResource } from "@ubx/sdk-aws/aws/data/agent_registry_control/agent_registry_control_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_agent_registry_control_tags_for_resource" });

      data(AgentRegistryControlTagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agent_registry_control import AgentRegistryControlTagsForResource, AgentRegistryControlTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_agent_registry_control_tags_for_resource")

        ubx.data(AgentRegistryControlTagsForResource, "example", AgentRegistryControlTagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  ARN of a taggable Agent Registry resource. Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  A map of tag keys to tag values returned by read operations (may be empty). Returned by the lookup.
</ResponseField>
