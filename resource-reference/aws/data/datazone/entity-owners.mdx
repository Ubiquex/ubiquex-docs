---
title: "aws_datazone_entity_owners"
description: "`aws_datazone_entity_owners` reads the real owners assigned to a DataZone catalog entity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_entity_owners"})

    		ubx.Data(datazone.EntityOwners, "example", datazone.EntityOwnersConfig{
    			DomainIdentifier: "example",
    			EntityIdentifier: "example",
    			EntityType:       "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityOwners } from "@ubx/sdk-aws/aws/data/datazone/entity_owners";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_entity_owners" });

      data(EntityOwners, "example", {
        domainIdentifier: "example",
        entityIdentifier: "example",
        entityType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import EntityOwners, EntityOwnersConfig

    def describe():
        ubx.intent("look up aws_datazone_entity_owners")

        ubx.data(EntityOwners, "example", EntityOwnersConfig(
            domain_identifier="example",
            entity_identifier="example",
            entity_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_identifier" type="string" required>
  The real, unique identifier of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_type" type="string" required>
  The real type of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="owners" type="list(object)">
  The real owners included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
