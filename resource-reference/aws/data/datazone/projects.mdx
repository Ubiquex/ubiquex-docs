---
title: "aws_datazone_projects"
description: "`aws_datazone_projects` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_projects` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_projects"})

    		ubx.Data(datazone.Projects, "example", datazone.ProjectsConfig{
    			DomainIdentifier: "example",
    			Name:             "example",
    			GroupIdentifier:  "example",
    			MaxResults:       1,
    			ProjectCategory:  "example",
    			UserIdentifier:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Projects } from "@ubx/sdk-aws/aws/data/datazone/projects";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_projects" });

      data(Projects, "example", {
        domainIdentifier: "example",
        name: "example",
        groupIdentifier: "example",
        maxResults: 1,
        projectCategory: "example",
        userIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Projects, ProjectsConfig

    def describe():
        ubx.intent("look up aws_datazone_projects")

        ubx.data(Projects, "example", ProjectsConfig(
            domain_identifier="example",
            name="example",
            group_identifier="example",
            max_results=1,
            project_category="example",
            user_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="group_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project_category" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_identifier" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
