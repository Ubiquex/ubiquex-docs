---
title: "aws_datazone_projects"
description: "`aws_datazone_projects` looks up a real DataZone project, grouping members, environments, and assets around a shared business use case."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_projects"})

    		ubx.Data(datazone.Projects, "example", datazone.ProjectsConfig{
    			DomainIdentifier: "example",
    			Name:             "example",
    			GroupIdentifier:  "example",
    			MaxResults:       1,
    			ProjectCategory:  "example",
    			UserIdentifier:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Projects } from "@ubx/sdk-aws/aws/data/datazone/projects";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_projects" });

      data(Projects, "example", {
        domainIdentifier: "example",
        name: "example",
        groupIdentifier: "example",
        maxResults: 1,
        projectCategory: "example",
        userIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Projects, ProjectsConfig

    def describe():
        ubx.intent("look up aws_datazone_projects")

        ubx.data(Projects, "example", ProjectsConfig(
            domain_identifier="example",
            name="example",
            group_identifier="example",
            max_results=1,
            project_category="example",
            user_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="group_identifier" type="string">
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_category" type="string">
  The real category of the project this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_identifier" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
