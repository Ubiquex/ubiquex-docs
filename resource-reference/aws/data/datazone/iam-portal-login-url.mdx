---
title: "aws_datazone_iam_portal_login_url"
description: "`aws_datazone_iam_portal_login_url` reads a real, generated single sign-on URL for the DataZone data portal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_iam_portal_login_url"})

    		ubx.Data(datazone.IamPortalLoginUrl, "example", datazone.IamPortalLoginUrlConfig{
    			DomainIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IamPortalLoginUrl } from "./local-sdk/aws/sdk/typescript/aws/data/datazone/iam_portal_login_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_iam_portal_login_url" });

      data(IamPortalLoginUrl, "example", {
        domainIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import IamPortalLoginUrl, IamPortalLoginUrlConfig

    def describe():
        ubx.intent("look up aws_datazone_iam_portal_login_url")

        ubx.data(IamPortalLoginUrl, "example", IamPortalLoginUrlConfig(
            domain_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auth_code_url" type="string">
  The real, resolvable URL of the auth code this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_profile_id" type="string">
  The real, unique identifier of the user profile this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
