---
title: "aws_datazone_subscriptions"
description: "`aws_datazone_subscriptions` reads the real subscriptions granting DataZone projects access to published data assets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_subscriptions"})

    		ubx.Data(datazone.Subscriptions, "example", datazone.SubscriptionsConfig{
    			DomainIdentifier:      "example",
    			ApproverProjectId:     "example",
    			MaxResults:            1,
    			OwningGroupId:         "example",
    			OwningIamPrincipalArn: "arn:aws:iam::123456789012:policy/example",
    			OwningProjectId:       "example",
    			OwningUserId:          "example",
    			SortBy:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Subscriptions } from "@ubx/sdk-aws/aws/data/datazone/subscriptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_subscriptions" });

      data(Subscriptions, "example", {
        domainIdentifier: "example",
        approverProjectId: "example",
        maxResults: 1,
        owningGroupId: "example",
        owningIamPrincipalArn: "arn:aws:iam::123456789012:policy/example",
        owningProjectId: "example",
        owningUserId: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Subscriptions, SubscriptionsConfig

    def describe():
        ubx.intent("look up aws_datazone_subscriptions")

        ubx.data(Subscriptions, "example", SubscriptionsConfig(
            domain_identifier="example",
            approver_project_id="example",
            max_results=1,
            owning_group_id="example",
            owning_iam_principal_arn="arn:aws:iam::123456789012:policy/example",
            owning_project_id="example",
            owning_user_id="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="approver_project_id" type="string">
  The real, unique identifier of the approver project this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owning_group_id" type="string">
  The real, unique identifier of the owning group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owning_iam_principal_arn" type="string">
  The real Amazon Resource Name (ARN) of the owning iam principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owning_project_id" type="string">
  The real, unique identifier of the owning project this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owning_user_id" type="string">
  The real, unique identifier of the owning user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subscribed_listing_id" type="string">
  The real, unique identifier of the subscribed listing this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subscription_request_identifier" type="string">
  The real, unique identifier of the subscription request this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
