---
title: "aws_datazone_data_source_run_activities"
description: "`aws_datazone_data_source_run_activities` reads the real, individual activities performed during one run of a DataZone data source."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_data_source_run_activities"})

    		ubx.Data(datazone.DataSourceRunActivities, "example", datazone.DataSourceRunActivitiesConfig{
    			DomainIdentifier: "example",
    			Identifier:       "example",
    			MaxResults:       1,
    			Status:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSourceRunActivities } from "@ubx/sdk-aws/aws/data/datazone/data_source_run_activities";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_data_source_run_activities" });

      data(DataSourceRunActivities, "example", {
        domainIdentifier: "example",
        identifier: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import DataSourceRunActivities, DataSourceRunActivitiesConfig

    def describe():
        ubx.intent("look up aws_datazone_data_source_run_activities")

        ubx.data(DataSourceRunActivities, "example", DataSourceRunActivitiesConfig(
            domain_identifier="example",
            identifier="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
