---
title: "aws_datazone_environment_credentials"
description: "`aws_datazone_environment_credentials` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_environment_credentials` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_environment_credentials"})

    		ubx.Data(datazone.EnvironmentCredentials, "example", datazone.EnvironmentCredentialsConfig{
    			DomainIdentifier:      "example",
    			EnvironmentIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnvironmentCredentials } from "@ubx/sdk-aws/aws/data/datazone/environment_credentials";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_environment_credentials" });

      data(EnvironmentCredentials, "example", {
        domainIdentifier: "example",
        environmentIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import EnvironmentCredentials, EnvironmentCredentialsConfig

    def describe():
        ubx.intent("look up aws_datazone_environment_credentials")

        ubx.data(EnvironmentCredentials, "example", EnvironmentCredentialsConfig(
            domain_identifier="example",
            environment_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_key_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="secret_access_key" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_token" type="string">
  Returned by the lookup.
</ResponseField>
