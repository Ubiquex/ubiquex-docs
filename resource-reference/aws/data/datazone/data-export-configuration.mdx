---
title: "aws_datazone_data_export_configuration"
description: "`aws_datazone_data_export_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_data_export_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_data_export_configuration"})

    		ubx.Data(datazone.DataExportConfiguration, "example", datazone.DataExportConfigurationConfig{
    			DomainIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataExportConfiguration } from "@ubx/sdk-aws/aws/data/datazone/data_export_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_data_export_configuration" });

      data(DataExportConfiguration, "example", {
        domainIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import DataExportConfiguration, DataExportConfigurationConfig

    def describe():
        ubx.intent("look up aws_datazone_data_export_configuration")

        ubx.data(DataExportConfiguration, "example", DataExportConfigurationConfig(
            domain_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_configuration" type="object">
  &lt;p&gt;The encryption configuration details.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sse_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_export_enabled" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="s3_table_bucket_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Returned by the lookup.
</ResponseField>
