---
title: "aws_datazone_time_series_data_points"
description: "`aws_datazone_time_series_data_points` reads the real time-series data points attached to a DataZone asset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_time_series_data_points"})

    		ubx.Data(datazone.TimeSeriesDataPoints, "example", datazone.TimeSeriesDataPointsConfig{
    			DomainIdentifier: "example",
    			EntityIdentifier: "example",
    			EntityType:       "example",
    			FormName:         "example-form-name",
    			EndedAt:          "example",
    			MaxResults:       1,
    			StartedAt:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TimeSeriesDataPoints } from "@ubx/sdk-aws/aws/data/datazone/time_series_data_points";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_time_series_data_points" });

      data(TimeSeriesDataPoints, "example", {
        domainIdentifier: "example",
        entityIdentifier: "example",
        entityType: "example",
        formName: "example-form-name",
        endedAt: "example",
        maxResults: 1,
        startedAt: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import TimeSeriesDataPoints, TimeSeriesDataPointsConfig

    def describe():
        ubx.intent("look up aws_datazone_time_series_data_points")

        ubx.data(TimeSeriesDataPoints, "example", TimeSeriesDataPointsConfig(
            domain_identifier="example",
            entity_identifier="example",
            entity_type="example",
            form_name="example-form-name",
            ended_at="example",
            max_results=1,
            started_at="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="ended_at" type="string">
  The real timestamp this object ended. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="entity_identifier" type="string" required>
  The real, unique identifier of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_type" type="string" required>
  The real type of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="form_name" type="string" required>
  The real name of the form this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="started_at" type="string">
  The real timestamp this object started. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
