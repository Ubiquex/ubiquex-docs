---
title: "aws_datazone_subscription_grants"
description: "`aws_datazone_subscription_grants` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_subscription_grants` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_subscription_grants"})

    		ubx.Data(datazone.SubscriptionGrants, "example", datazone.SubscriptionGrantsConfig{
    			DomainIdentifier:      "example",
    			EnvironmentId:         "example",
    			MaxResults:            1,
    			OwningGroupId:         "example",
    			OwningIamPrincipalArn: "arn:aws:iam::123456789012:policy/example",
    			OwningProjectId:       "example",
    			OwningUserId:          "example",
    			SortBy:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionGrants } from "@ubx/sdk-aws/aws/data/datazone/subscription_grants";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_subscription_grants" });

      data(SubscriptionGrants, "example", {
        domainIdentifier: "example",
        environmentId: "example",
        maxResults: 1,
        owningGroupId: "example",
        owningIamPrincipalArn: "arn:aws:iam::123456789012:policy/example",
        owningProjectId: "example",
        owningUserId: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import SubscriptionGrants, SubscriptionGrantsConfig

    def describe():
        ubx.intent("look up aws_datazone_subscription_grants")

        ubx.data(SubscriptionGrants, "example", SubscriptionGrantsConfig(
            domain_identifier="example",
            environment_id="example",
            max_results=1,
            owning_group_id="example",
            owning_iam_principal_arn="arn:aws:iam::123456789012:policy/example",
            owning_project_id="example",
            owning_user_id="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owning_group_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owning_iam_principal_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owning_project_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owning_user_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="subscribed_listing_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="subscription_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="subscription_target_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
