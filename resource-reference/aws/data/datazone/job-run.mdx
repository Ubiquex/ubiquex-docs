---
title: "aws_datazone_job_run"
description: "`aws_datazone_job_run` looks up one real run of a DataZone job, such as a data source run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_job_run"})

    		ubx.Data(datazone.JobRun, "example", datazone.JobRunConfig{
    			DomainIdentifier: "example",
    			Identifier:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { JobRun } from "./local-sdk/aws/sdk/typescript/aws/data/datazone/job_run.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_job_run" });

      data(JobRun, "example", {
        domainIdentifier: "example",
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import JobRun, JobRunConfig

    def describe():
        ubx.intent("look up aws_datazone_job_run")

        ubx.data(JobRun, "example", JobRunConfig(
            domain_identifier="example",
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="string">
  The real identity that created this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="details" type="object">
  &lt;p&gt;The details of a job run.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="lineage_run_details" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="sql_query_run_details" type="object">
          Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="error_messages" type="list(string)">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="num_queries_failed" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="query_end_time" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="query_start_time" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="total_queries_processed" type="number">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_id" type="string">
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error" type="object">
  &lt;p&gt;The job run error.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="message" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_id" type="string">
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_type" type="string">
  The real, discriminating type of the job this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="run_mode" type="string">
  The real mode this run executes in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
