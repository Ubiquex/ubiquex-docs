---
title: "aws_datazone_notifications"
description: "`aws_datazone_notifications` reads real, generated notifications for a DataZone user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_notifications"})

    		ubx.Data(datazone.Notifications, "example", datazone.NotificationsConfig{
    			DomainIdentifier: "example",
    			Type:             "example",
    			AfterTimestamp:   "example",
    			BeforeTimestamp:  "example",
    			MaxResults:       1,
    			Subjects:         []string{"example"},
    			TaskStatus:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Notifications } from "./local-sdk/aws/sdk/typescript/aws/data/datazone/notifications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_notifications" });

      data(Notifications, "example", {
        domainIdentifier: "example",
        type: "example",
        afterTimestamp: "example",
        beforeTimestamp: "example",
        maxResults: 1,
        subjects: ["example"],
        taskStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Notifications, NotificationsConfig

    def describe():
        ubx.intent("look up aws_datazone_notifications")

        ubx.data(Notifications, "example", NotificationsConfig(
            domain_identifier="example",
            type="example",
            after_timestamp="example",
            before_timestamp="example",
            max_results=1,
            subjects=["example"],
            task_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_timestamp" type="string">
  The real timestamp of the after. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before_timestamp" type="string">
  The real timestamp of the before. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subjects" type="list(string)">
  The real subjects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="task_status" type="string">
  The real, current status of the task. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  The real, discriminating type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notifications" type="list(object)">
  The real notifications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
