---
title: "aws_datazone_environment_profiles"
description: "`aws_datazone_environment_profiles` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_environment_profiles` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_environment_profiles"})

    		ubx.Data(datazone.EnvironmentProfiles, "example", datazone.EnvironmentProfilesConfig{
    			DomainIdentifier:               "example",
    			Name:                           "example",
    			AwsAccountId:                   "example",
    			AwsAccountRegion:               "example",
    			EnvironmentBlueprintIdentifier: "example",
    			MaxResults:                     1,
    			ProjectIdentifier:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnvironmentProfiles } from "@ubx/sdk-aws/aws/data/datazone/environment_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_environment_profiles" });

      data(EnvironmentProfiles, "example", {
        domainIdentifier: "example",
        name: "example",
        awsAccountId: "example",
        awsAccountRegion: "example",
        environmentBlueprintIdentifier: "example",
        maxResults: 1,
        projectIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import EnvironmentProfiles, EnvironmentProfilesConfig

    def describe():
        ubx.intent("look up aws_datazone_environment_profiles")

        ubx.data(EnvironmentProfiles, "example", EnvironmentProfilesConfig(
            domain_identifier="example",
            name="example",
            aws_account_id="example",
            aws_account_region="example",
            environment_blueprint_identifier="example",
            max_results=1,
            project_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="aws_account_region" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_blueprint_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project_identifier" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
