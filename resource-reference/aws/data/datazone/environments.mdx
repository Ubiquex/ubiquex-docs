---
title: "aws_datazone_environments"
description: "`aws_datazone_environments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_environments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_environments"})

    		ubx.Data(datazone.Environments, "example", datazone.EnvironmentsConfig{
    			DomainIdentifier:               "example",
    			ProjectIdentifier:              "example",
    			Name:                           "example",
    			AwsAccountId:                   "example",
    			AwsAccountRegion:               "example",
    			EnvironmentBlueprintIdentifier: "example",
    			EnvironmentProfileIdentifier:   "example",
    			MaxResults:                     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Environments } from "@ubx/sdk-aws/aws/data/datazone/environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_environments" });

      data(Environments, "example", {
        domainIdentifier: "example",
        projectIdentifier: "example",
        name: "example",
        awsAccountId: "example",
        awsAccountRegion: "example",
        environmentBlueprintIdentifier: "example",
        environmentProfileIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Environments, EnvironmentsConfig

    def describe():
        ubx.intent("look up aws_datazone_environments")

        ubx.data(Environments, "example", EnvironmentsConfig(
            domain_identifier="example",
            project_identifier="example",
            name="example",
            aws_account_id="example",
            aws_account_region="example",
            environment_blueprint_identifier="example",
            environment_profile_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="aws_account_region" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_blueprint_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="environment_profile_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="project_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
