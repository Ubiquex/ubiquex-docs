---
title: "aws_datazone_environments"
description: "`aws_datazone_environments` looks up a real DataZone environment, the provisioned AWS resources a project's own members work within."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_environments"})

    		ubx.Data(datazone.Environments, "example", datazone.EnvironmentsConfig{
    			DomainIdentifier:               "example",
    			ProjectIdentifier:              "example",
    			Name:                           "example",
    			AwsAccountId:                   "example",
    			AwsAccountRegion:               "example",
    			EnvironmentBlueprintIdentifier: "example",
    			EnvironmentProfileIdentifier:   "example",
    			MaxResults:                     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Environments } from "@ubx/sdk-aws/aws/data/datazone/environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_environments" });

      data(Environments, "example", {
        domainIdentifier: "example",
        projectIdentifier: "example",
        name: "example",
        awsAccountId: "example",
        awsAccountRegion: "example",
        environmentBlueprintIdentifier: "example",
        environmentProfileIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import Environments, EnvironmentsConfig

    def describe():
        ubx.intent("look up aws_datazone_environments")

        ubx.data(Environments, "example", EnvironmentsConfig(
            domain_identifier="example",
            project_identifier="example",
            name="example",
            aws_account_id="example",
            aws_account_region="example",
            environment_blueprint_identifier="example",
            environment_profile_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="aws_account_region" type="string">
  The real AWS region this account is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment_blueprint_identifier" type="string">
  The real, unique identifier of the environment blueprint this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="environment_profile_identifier" type="string">
  The real, unique identifier of the environment profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_identifier" type="string" required>
  The real, unique identifier of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string">
  The real provider this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
