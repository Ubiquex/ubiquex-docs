---
title: "aws_datazone_subscription_targets"
description: "`aws_datazone_subscription_targets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datazone_subscription_targets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_subscription_targets"})

    		ubx.Data(datazone.SubscriptionTargets, "example", datazone.SubscriptionTargetsConfig{
    			DomainIdentifier:      "example",
    			EnvironmentIdentifier: "example",
    			MaxResults:            1,
    			SortBy:                "example",
    			SortOrder:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionTargets } from "@ubx/sdk-aws/aws/data/datazone/subscription_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_subscription_targets" });

      data(SubscriptionTargets, "example", {
        domainIdentifier: "example",
        environmentIdentifier: "example",
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import SubscriptionTargets, SubscriptionTargetsConfig

    def describe():
        ubx.intent("look up aws_datazone_subscription_targets")

        ubx.data(SubscriptionTargets, "example", SubscriptionTargetsConfig(
            domain_identifier="example",
            environment_identifier="example",
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
