---
title: "aws_datazone_environment_blueprints"
description: "`aws_datazone_environment_blueprints` looks up a real DataZone environment blueprint, the template a DataZone environment is provisioned from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_environment_blueprints"})

    		ubx.Data(datazone.EnvironmentBlueprints, "example", datazone.EnvironmentBlueprintsConfig{
    			DomainIdentifier: "example",
    			Name:             "example",
    			Managed:          true,
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnvironmentBlueprints } from "@ubx/sdk-aws/aws/data/datazone/environment_blueprints";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_environment_blueprints" });

      data(EnvironmentBlueprints, "example", {
        domainIdentifier: "example",
        name: "example",
        managed: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import EnvironmentBlueprints, EnvironmentBlueprintsConfig

    def describe():
        ubx.intent("look up aws_datazone_environment_blueprints")

        ubx.data(EnvironmentBlueprints, "example", EnvironmentBlueprintsConfig(
            domain_identifier="example",
            name="example",
            managed=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="managed" type="bool">
  Whether this object is managed by AWS. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
