---
title: "aws_datazone_time_series_data_point"
description: "`aws_datazone_time_series_data_point` looks up one real, submitted time-series data point (such as a data quality result) attached to a DataZone asset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datazone "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datazone"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datazone_time_series_data_point"})

    		ubx.Data(datazone.TimeSeriesDataPoint, "example", datazone.TimeSeriesDataPointConfig{
    			DomainIdentifier: "example",
    			EntityIdentifier: "example",
    			EntityType:       "example",
    			FormName:         "example-form-name",
    			Identifier:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TimeSeriesDataPoint } from "@ubx/sdk-aws/aws/data/datazone/time_series_data_point";

    export default stack("example", () => {
      intent({ summary: "look up aws_datazone_time_series_data_point" });

      data(TimeSeriesDataPoint, "example", {
        domainIdentifier: "example",
        entityIdentifier: "example",
        entityType: "example",
        formName: "example-form-name",
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datazone import TimeSeriesDataPoint, TimeSeriesDataPointConfig

    def describe():
        ubx.intent("look up aws_datazone_time_series_data_point")

        ubx.data(TimeSeriesDataPoint, "example", TimeSeriesDataPointConfig(
            domain_identifier="example",
            entity_identifier="example",
            entity_type="example",
            form_name="example-form-name",
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_identifier" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_identifier" type="string" required>
  The real, unique identifier of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_type" type="string" required>
  The real type of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="form_name" type="string" required>
  The real name of the form this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_id" type="string">
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The real, unique identifier of this entity. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="form" type="object">
  &lt;p&gt;The time series data points form.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="content" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="form_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timestamp" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type_identifier" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type_revision" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
