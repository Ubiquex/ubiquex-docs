---
title: "aws_connectcampaignsv2_campaign_state_batch"
description: "`aws_connectcampaignsv2_campaign_state_batch` reads the real, current state of a batch of Connect outbound campaigns."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connectcampaignsv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connectcampaignsv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connectcampaignsv2_campaign_state_batch"})

    		ubx.Data(connectcampaignsv2.CampaignStateBatch, "example", connectcampaignsv2.CampaignStateBatchConfig{
    			CampaignIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CampaignStateBatch } from "./local-sdk/aws/sdk/typescript/aws/data/connectcampaignsv2/campaign_state_batch.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connectcampaignsv2_campaign_state_batch" });

      data(CampaignStateBatch, "example", {
        campaignIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connectcampaignsv2 import CampaignStateBatch, CampaignStateBatchConfig

    def describe():
        ubx.intent("look up aws_connectcampaignsv2_campaign_state_batch")

        ubx.data(CampaignStateBatch, "example", CampaignStateBatchConfig(
            campaign_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_ids" type="list(string)" required>
  List of CampaignId Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failed_requests" type="list(object)">
  List of failed requests of campaign state Returned by the lookup.
</ResponseField>

<ResponseField name="successful_requests" type="list(object)">
  List of successful response of campaign state Returned by the lookup.
</ResponseField>
