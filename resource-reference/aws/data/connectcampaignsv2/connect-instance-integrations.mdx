---
title: "aws_connectcampaignsv2_connect_instance_integrations"
description: "`aws_connectcampaignsv2_connect_instance_integrations` reads the real integrations configured for Connect outbound campaigns on a Connect instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connectcampaignsv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connectcampaignsv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connectcampaignsv2_connect_instance_integrations"})

    		ubx.Data(connectcampaignsv2.ConnectInstanceIntegrations, "example", connectcampaignsv2.ConnectInstanceIntegrationsConfig{
    			ConnectInstanceId: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectInstanceIntegrations } from "@ubx/sdk-aws/aws/data/connectcampaignsv2/connect_instance_integrations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_connectcampaignsv2_connect_instance_integrations",
      });

      data(ConnectInstanceIntegrations, "example", {
        connectInstanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connectcampaignsv2 import ConnectInstanceIntegrations, ConnectInstanceIntegrationsConfig

    def describe():
        ubx.intent("look up aws_connectcampaignsv2_connect_instance_integrations")

        ubx.data(ConnectInstanceIntegrations, "example", ConnectInstanceIntegrationsConfig(
            connect_instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connect_instance_id" type="string" required>
  Amazon Connect Instance Id Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return per page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="integration_summary_list" type="list(object)">
  A list of Amazon Connect Instance Integrations. Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  The token for the next set of results. Returned by the lookup.
</ResponseField>
