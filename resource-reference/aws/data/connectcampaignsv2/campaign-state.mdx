---
title: "aws_connectcampaignsv2_campaign_state"
description: "`aws_connectcampaignsv2_campaign_state` reads the real, current state of a Connect outbound campaign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connectcampaignsv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connectcampaignsv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connectcampaignsv2_campaign_state"})

    		ubx.Data(connectcampaignsv2.CampaignState, "example", connectcampaignsv2.CampaignStateConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CampaignState } from "@ubx/sdk-aws/aws/data/connectcampaignsv2/campaign_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_connectcampaignsv2_campaign_state" });

      data(CampaignState, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connectcampaignsv2 import CampaignState, CampaignStateConfig

    def describe():
        ubx.intent("look up aws_connectcampaignsv2_campaign_state")

        ubx.data(CampaignState, "example", CampaignStateConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Identifier representing a Campaign Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="state" type="string">
  State of a campaign Returned by the lookup.
</ResponseField>
