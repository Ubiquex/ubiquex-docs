---
title: "aws_connectcampaignsv2_connect_instance_config"
description: "`aws_connectcampaignsv2_connect_instance_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connectcampaignsv2_connect_instance_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connectcampaignsv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connectcampaignsv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connectcampaignsv2_connect_instance_config"})

    		ubx.Data(connectcampaignsv2.ConnectInstanceConfig, "example", connectcampaignsv2.ConnectInstanceConfigConfig{
    			ConnectInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectInstanceConfig } from "@ubx/sdk-aws/aws/data/connectcampaignsv2/connect_instance_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_connectcampaignsv2_connect_instance_config" });

      data(ConnectInstanceConfig, "example", {
        connectInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connectcampaignsv2 import ConnectInstanceConfig, ConnectInstanceConfigConfig

    def describe():
        ubx.intent("look up aws_connectcampaignsv2_connect_instance_config")

        ubx.data(ConnectInstanceConfig, "example", ConnectInstanceConfigConfig(
            connect_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connect_instance_id" type="string" required>
  Amazon Connect Instance Id Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connect_instance_config" type="object">
  Instance config object Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="connect_instance_id" type="string" required>
      Amazon Connect Instance Id Required lookup argument.
    </ResponseField>
    <ResponseField name="encryption_config" type="object" required>
      Encryption config for Connect Instance. Note that sensitive data will always be encrypted. If disabled, service will perform encryption with its own key. If enabled, a KMS key id needs to be provided and KMS charges will apply. KMS is only type supported Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool" required>
          Boolean to indicate if custom encryption has been enabled. Required lookup argument.
        </ResponseField>
        <ResponseField name="encryption_type" type="string">
          Server-side encryption type. Optional lookup argument.
        </ResponseField>
        <ResponseField name="key_arn" type="string">
          KMS key id/arn for encryption config. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_linked_role_arn" type="string" required>
      Service linked role arn Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
