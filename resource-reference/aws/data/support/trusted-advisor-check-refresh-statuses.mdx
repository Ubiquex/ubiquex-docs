---
title: "aws_support_trusted_advisor_check_refresh_statuses"
description: "`aws_support_trusted_advisor_check_refresh_statuses` reads the real, current refresh status of a Trusted Advisor check."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_trusted_advisor_check_refresh_statuses"})

    		ubx.Data(support.TrustedAdvisorCheckRefreshStatuses, "example", support.TrustedAdvisorCheckRefreshStatusesConfig{
    			CheckIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrustedAdvisorCheckRefreshStatuses } from "@ubx/sdk-aws/aws/data/support/trusted_advisor_check_refresh_statuses";

    export default stack("example", () => {
      intent({
        summary: "look up aws_support_trusted_advisor_check_refresh_statuses",
      });

      data(TrustedAdvisorCheckRefreshStatuses, "example", {
        checkIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import TrustedAdvisorCheckRefreshStatuses, TrustedAdvisorCheckRefreshStatusesConfig

    def describe():
        ubx.intent("look up aws_support_trusted_advisor_check_refresh_statuses")

        ubx.data(TrustedAdvisorCheckRefreshStatuses, "example", TrustedAdvisorCheckRefreshStatusesConfig(
            check_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="check_ids" type="list(string)" required>
  The real, unique identifiers of the checks this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statuses" type="list(object)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
