---
title: "aws_support_services"
description: "`aws_support_services` reads the real AWS services and categories available for filing an AWS Support case."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_services"})

    		ubx.Data(support.Services, "example", support.ServicesConfig{
    			Language:        "example",
    			ServiceCodeList: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Services } from "./local-sdk/aws/sdk/typescript/aws/data/support/services.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_services" });

      data(Services, "example", {
        language: "example",
        serviceCodeList: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.support import Services, ServicesConfig

    def describe():
        ubx.intent("look up aws_support_services")

        ubx.data(Services, "example", ServicesConfig(
            language="example",
            service_code_list=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string">
  The real language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_code_list" type="list(string)">
  The real service codes included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="services" type="list(object)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
