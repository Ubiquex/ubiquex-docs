---
title: "aws_support_trusted_advisor_checks"
description: "`aws_support_trusted_advisor_checks` reads the real, available Trusted Advisor checks for the account's own support plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_trusted_advisor_checks"})

    		ubx.Data(support.TrustedAdvisorChecks, "example", support.TrustedAdvisorChecksConfig{
    			Language: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrustedAdvisorChecks } from "@ubx/sdk-aws/aws/data/support/trusted_advisor_checks";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_trusted_advisor_checks" });

      data(TrustedAdvisorChecks, "example", {
        language: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import TrustedAdvisorChecks, TrustedAdvisorChecksConfig

    def describe():
        ubx.intent("look up aws_support_trusted_advisor_checks")

        ubx.data(TrustedAdvisorChecks, "example", TrustedAdvisorChecksConfig(
            language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string" required>
  The real language this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="checks" type="list(object)">
  The real checks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
