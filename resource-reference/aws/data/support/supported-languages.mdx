---
title: "aws_support_supported_languages"
description: "`aws_support_supported_languages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_support_supported_languages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_supported_languages"})

    		ubx.Data(support.SupportedLanguages, "example", support.SupportedLanguagesConfig{
    			CategoryCode: "example",
    			IssueType:    "example",
    			ServiceCode:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SupportedLanguages } from "@ubx/sdk-aws/aws/data/support/supported_languages";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_supported_languages" });

      data(SupportedLanguages, "example", {
        categoryCode: "example",
        issueType: "example",
        serviceCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import SupportedLanguages, SupportedLanguagesConfig

    def describe():
        ubx.intent("look up aws_support_supported_languages")

        ubx.data(SupportedLanguages, "example", SupportedLanguagesConfig(
            category_code="example",
            issue_type="example",
            service_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category_code" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="issue_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="supported_languages" type="list(object)">
  Returned by the lookup.
</ResponseField>
