---
title: "aws_support_trusted_advisor_check_summaries"
description: "`aws_support_trusted_advisor_check_summaries` reads a real, summarized result of a Trusted Advisor check."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_trusted_advisor_check_summaries"})

    		ubx.Data(support.TrustedAdvisorCheckSummaries, "example", support.TrustedAdvisorCheckSummariesConfig{
    			CheckIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TrustedAdvisorCheckSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/support/trusted_advisor_check_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_trusted_advisor_check_summaries" });

      data(TrustedAdvisorCheckSummaries, "example", {
        checkIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.support import TrustedAdvisorCheckSummaries, TrustedAdvisorCheckSummariesConfig

    def describe():
        ubx.intent("look up aws_support_trusted_advisor_check_summaries")

        ubx.data(TrustedAdvisorCheckSummaries, "example", TrustedAdvisorCheckSummariesConfig(
            check_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="check_ids" type="list(string)" required>
  The real, unique identifiers of the checks this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
