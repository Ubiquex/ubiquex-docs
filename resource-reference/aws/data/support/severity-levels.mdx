---
title: "aws_support_severity_levels"
description: "`aws_support_severity_levels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_support_severity_levels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_severity_levels"})

    		ubx.Data(support.SeverityLevels, "example", support.SeverityLevelsConfig{
    			Language: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SeverityLevels } from "@ubx/sdk-aws/aws/data/support/severity_levels";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_severity_levels" });

      data(SeverityLevels, "example", {
        language: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import SeverityLevels, SeverityLevelsConfig

    def describe():
        ubx.intent("look up aws_support_severity_levels")

        ubx.data(SeverityLevels, "example", SeverityLevelsConfig(
            language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="severity_levels" type="list(object)">
  Returned by the lookup.
</ResponseField>
