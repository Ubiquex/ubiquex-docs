---
title: "aws_support_severity_levels"
description: "`aws_support_severity_levels` reads the real, available severity levels for an AWS Support case, based on the account's own support plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_severity_levels"})

    		ubx.Data(support.SeverityLevels, "example", support.SeverityLevelsConfig{
    			Language: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SeverityLevels } from "./local-sdk/aws/sdk/typescript/aws/data/support/severity_levels.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_severity_levels" });

      data(SeverityLevels, "example", {
        language: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.support import SeverityLevels, SeverityLevelsConfig

    def describe():
        ubx.intent("look up aws_support_severity_levels")

        ubx.data(SeverityLevels, "example", SeverityLevelsConfig(
            language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string">
  The real language this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="severity_levels" type="list(object)">
  The real severity levels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
