---
title: "aws_support_create_case_options"
description: "`aws_support_create_case_options` reads the real, available category and issue type options for creating an AWS Support case."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_create_case_options"})

    		ubx.Data(support.CreateCaseOptions, "example", support.CreateCaseOptionsConfig{
    			CategoryCode: "example",
    			IssueType:    "example",
    			Language:     "example",
    			ServiceCode:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CreateCaseOptions } from "@ubx/sdk-aws/aws/data/support/create_case_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_create_case_options" });

      data(CreateCaseOptions, "example", {
        categoryCode: "example",
        issueType: "example",
        language: "example",
        serviceCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import CreateCaseOptions, CreateCaseOptionsConfig

    def describe():
        ubx.intent("look up aws_support_create_case_options")

        ubx.data(CreateCaseOptions, "example", CreateCaseOptionsConfig(
            category_code="example",
            issue_type="example",
            language="example",
            service_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category_code" type="string" required>
  The real code identifying the category. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="issue_type" type="string" required>
  The real, discriminating type of the issue this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="language" type="string" required>
  The real language this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  The real, unique code identifying the AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="communication_types" type="list(object)">
  The real, discriminating types of communications this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="language_availability" type="string">
  The real languages this object is available in. **(AI-inferred)** Returned by the lookup.
</ResponseField>
