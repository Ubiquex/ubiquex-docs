---
title: "aws_support_create_case_options"
description: "`aws_support_create_case_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_support_create_case_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_create_case_options"})

    		ubx.Data(support.CreateCaseOptions, "example", support.CreateCaseOptionsConfig{
    			CategoryCode: "example",
    			IssueType:    "example",
    			Language:     "example",
    			ServiceCode:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CreateCaseOptions } from "@ubx/sdk-aws/aws/data/support/create_case_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_create_case_options" });

      data(CreateCaseOptions, "example", {
        categoryCode: "example",
        issueType: "example",
        language: "example",
        serviceCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import CreateCaseOptions, CreateCaseOptionsConfig

    def describe():
        ubx.intent("look up aws_support_create_case_options")

        ubx.data(CreateCaseOptions, "example", CreateCaseOptionsConfig(
            category_code="example",
            issue_type="example",
            language="example",
            service_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category_code" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="issue_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="language" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="communication_types" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="language_availability" type="string">
  Returned by the lookup.
</ResponseField>
