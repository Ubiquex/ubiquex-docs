---
title: "aws_support_communications"
description: "`aws_support_communications` reads the real, exchanged communications on an AWS Support case."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_communications"})

    		ubx.Data(support.Communications, "example", support.CommunicationsConfig{
    			CaseId:     "example",
    			AfterTime:  "example",
    			BeforeTime: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Communications } from "./local-sdk/aws/sdk/typescript/aws/data/support/communications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_communications" });

      data(Communications, "example", {
        caseId: "example",
        afterTime: "example",
        beforeTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.support import Communications, CommunicationsConfig

    def describe():
        ubx.intent("look up aws_support_communications")

        ubx.data(Communications, "example", CommunicationsConfig(
            case_id="example",
            after_time="example",
            before_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_time" type="string">
  The real timestamp of the after. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before_time" type="string">
  The real timestamp of the before. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="case_id" type="string" required>
  The real, unique identifier of the case this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="communications" type="list(object)">
  The real communications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
