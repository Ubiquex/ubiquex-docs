---
title: "aws_support_communications"
description: "`aws_support_communications` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_support_communications` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_support_communications"})

    		ubx.Data(support.Communications, "example", support.CommunicationsConfig{
    			CaseId:     "example",
    			AfterTime:  "example",
    			BeforeTime: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Communications } from "@ubx/sdk-aws/aws/data/support/communications";

    export default stack("example", () => {
      intent({ summary: "look up aws_support_communications" });

      data(Communications, "example", {
        caseId: "example",
        afterTime: "example",
        beforeTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.support import Communications, CommunicationsConfig

    def describe():
        ubx.intent("look up aws_support_communications")

        ubx.data(Communications, "example", CommunicationsConfig(
            case_id="example",
            after_time="example",
            before_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="case_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="communications" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
