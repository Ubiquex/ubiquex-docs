---
title: "aws_medical_imaging_image_set_metadata"
description: "`aws_medical_imaging_image_set_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_medical_imaging_image_set_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medical_imaging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medical_imaging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medical_imaging_image_set_metadata"})

    		ubx.Data(medical_imaging.MedicalImagingImageSetMetadata, "example", medical_imaging.MedicalImagingImageSetMetadataConfig{
    			DatastoreId: "example",
    			ImageSetId:  "example",
    			VersionId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MedicalImagingImageSetMetadata } from "@ubx/sdk-aws/aws/data/medical_imaging/medical_imaging_image_set_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_medical_imaging_image_set_metadata" });

      data(MedicalImagingImageSetMetadata, "example", {
        datastoreId: "example",
        imageSetId: "example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medical_imaging import MedicalImagingImageSetMetadata, MedicalImagingImageSetMetadataConfig

    def describe():
        ubx.intent("look up aws_medical_imaging_image_set_metadata")

        ubx.data(MedicalImagingImageSetMetadata, "example", MedicalImagingImageSetMetadataConfig(
            datastore_id="example",
            image_set_id="example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="image_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_encoding" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="image_set_metadata_blob" type="string">
  Returned by the lookup.
</ResponseField>
