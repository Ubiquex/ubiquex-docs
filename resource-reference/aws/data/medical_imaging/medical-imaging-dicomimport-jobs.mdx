---
title: "aws_medical_imaging_dicomimport_jobs"
description: "`aws_medical_imaging_dicomimport_jobs` reads the real HealthImaging DICOM import jobs run for a data store."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medical_imaging "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/medical_imaging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medical_imaging_dicomimport_jobs"})

    		ubx.Data(medical_imaging.MedicalImagingDicomimportJobs, "example", medical_imaging.MedicalImagingDicomimportJobsConfig{
    			DatastoreId: "example",
    			JobStatus:   "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MedicalImagingDicomimportJobs } from "@ubx/sdk-aws/aws/data/medical_imaging/medical_imaging_dicomimport_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_medical_imaging_dicomimport_jobs" });

      data(MedicalImagingDicomimportJobs, "example", {
        datastoreId: "example",
        jobStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medical_imaging import MedicalImagingDicomimportJobs, MedicalImagingDicomimportJobsConfig

    def describe():
        ubx.intent("look up aws_medical_imaging_dicomimport_jobs")

        ubx.data(MedicalImagingDicomimportJobs, "example", MedicalImagingDicomimportJobsConfig(
            datastore_id="example",
            job_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  The real, unique identifier of the data store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_summaries" type="list(object)">
  The real, summarized jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
