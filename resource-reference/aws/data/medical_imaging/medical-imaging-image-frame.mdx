---
title: "aws_medical_imaging_image_frame"
description: "`aws_medical_imaging_image_frame` reads one real image frame within a HealthImaging image set."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medical_imaging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medical_imaging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medical_imaging_image_frame"})

    		ubx.Data(medical_imaging.MedicalImagingImageFrame, "example", medical_imaging.MedicalImagingImageFrameConfig{
    			DatastoreId:           "example",
    			ImageFrameInformation: map[string]any{"image_frame_id": "example"},
    			ImageSetId:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MedicalImagingImageFrame } from "./local-sdk/aws/sdk/typescript/aws/data/medical_imaging/medical_imaging_image_frame.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medical_imaging_image_frame" });

      data(MedicalImagingImageFrame, "example", {
        datastoreId: "example",
        imageFrameInformation: { imageFrameId: "example" },
        imageSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medical_imaging import MedicalImagingImageFrame, MedicalImagingImageFrameConfig

    def describe():
        ubx.intent("look up aws_medical_imaging_image_frame")

        ubx.data(MedicalImagingImageFrame, "example", MedicalImagingImageFrameConfig(
            datastore_id="example",
            image_frame_information={"image_frame_id": "example"},
            image_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  The real, unique identifier of the data store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="image_frame_information" type="object" required>
  &lt;p&gt;Information about the image frame (pixel data) identifier.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="image_frame_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_set_id" type="string" required>
  The real, unique identifier of the HealthImaging image set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="image_frame_blob" type="string">
  The real, raw binary content of this image frame. **(AI-inferred)** Returned by the lookup.
</ResponseField>
