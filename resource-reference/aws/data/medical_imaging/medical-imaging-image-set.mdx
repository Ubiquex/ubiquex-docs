---
title: "aws_medical_imaging_image_set"
description: "`aws_medical_imaging_image_set` looks up one real HealthImaging image set."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medical_imaging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medical_imaging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medical_imaging_image_set"})

    		ubx.Data(medical_imaging.MedicalImagingImageSet, "example", medical_imaging.MedicalImagingImageSetConfig{
    			DatastoreId: "example",
    			ImageSetId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MedicalImagingImageSet } from "./local-sdk/aws/sdk/typescript/aws/data/medical_imaging/medical_imaging_image_set.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medical_imaging_image_set" });

      data(MedicalImagingImageSet, "example", {
        datastoreId: "example",
        imageSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medical_imaging import MedicalImagingImageSet, MedicalImagingImageSetConfig

    def describe():
        ubx.intent("look up aws_medical_imaging_image_set")

        ubx.data(MedicalImagingImageSet, "example", MedicalImagingImageSetConfig(
            datastore_id="example",
            image_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  The real, unique identifier of the data store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="image_set_id" type="string" required>
  The real, unique identifier of the HealthImaging image set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="deleted_at" type="string">
  The real timestamp this object was deleted. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="image_set_arn" type="string">
  The real Amazon Resource Name (ARN) of the image set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="image_set_state" type="string">
  The real, current state of the image set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="image_set_workflow_status" type="string">
  The real, current status of the image set workflow this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_primary" type="bool">
  Whether this object is the primary one for its own scope. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_accessed_at" type="string">
  The real timestamp this object was last accessed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="overrides" type="object">
  &lt;p&gt;Specifies the overrides used in image set modification calls to &lt;code&gt;CopyImageSet&lt;/code&gt; and &lt;code&gt;UpdateImageSetMetadata&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="forced" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="storage_tier" type="string">
  Storage tier for image sets Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_id" type="string">
  The real, unique version identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
