---
title: "aws_medical_imaging_image_set_versions"
description: "`aws_medical_imaging_image_set_versions` reads a real, saved version of a HealthImaging image set."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medical_imaging "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medical_imaging"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medical_imaging_image_set_versions"})

    		ubx.Data(medical_imaging.MedicalImagingImageSetVersions, "example", medical_imaging.MedicalImagingImageSetVersionsConfig{
    			DatastoreId: "example",
    			ImageSetId:  "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MedicalImagingImageSetVersions } from "./local-sdk/aws/sdk/typescript/aws/data/medical_imaging/medical_imaging_image_set_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medical_imaging_image_set_versions" });

      data(MedicalImagingImageSetVersions, "example", {
        datastoreId: "example",
        imageSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medical_imaging import MedicalImagingImageSetVersions, MedicalImagingImageSetVersionsConfig

    def describe():
        ubx.intent("look up aws_medical_imaging_image_set_versions")

        ubx.data(MedicalImagingImageSetVersions, "example", MedicalImagingImageSetVersionsConfig(
            datastore_id="example",
            image_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  The real, unique identifier of the data store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="image_set_id" type="string" required>
  The real, unique identifier of the HealthImaging image set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_set_properties_list" type="list(object)">
  The real image set propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
