---
title: "aws_data_zone_project"
description: "`aws_data_zone_project` is a real, collaborative workspace inside an `aws_data_zone_domain` where data workers, engineers, analysts, data scientists..."
---

`aws_data_zone_project` is a real, collaborative workspace inside an `aws_data_zone_domain` where data workers, engineers, analysts, data scientists, actually discover, request access to, and work with cataloged data together.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own zone project"})

    		ubx.Resource(data.ZoneProject, "ci-runner", data.ZoneProjectConfig{
    			DomainIdentifier:      "example",
    			Name:                  "ci-runner",
    			Description:           "Managed by ubx.",
    			DomainUnitId:          "example",
    			GlossaryTerms:         []string{"example"},
    			MembershipAssignments: []map[string]any{{"designation": "example"}},
    			ProjectCategory:       "example",
    			ProjectExecutionRole:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneProject } from "./local-sdk/aws/sdk/typescript/aws/data/zone_project.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own zone project" });

      resource(ZoneProject, "ci-runner", {
        domainIdentifier: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        domainUnitId: "example",
        glossaryTerms: ["example"],
        membershipAssignments: [{ designation: "example" }],
        projectCategory: "example",
        projectExecutionRole: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import ZoneProject, ZoneProjectConfig

    def describe():
        ubx.intent("payments own zone project")

        ubx.resource(ZoneProject, "ci-runner", ZoneProjectConfig(
            domain_identifier="example",
            name="ci-runner",
            description="Managed by ubx.",
            domain_unit_id="example",
            glossary_terms=["example"],
            membership_assignments=[{"designation": "example"}],
            project_category="example",
            project_execution_role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_project called "ci-runner", with domain
    identifier example, description Managed by ubx., domain unit id
    example, glossary terms a set value, membership assignments a set
    value, project category example, project execution role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the Amazon DataZone project. Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The ID of the Amazon DataZone domain in which this project is created. Required.
</ResponseField>

<ResponseField name="domain_unit_id" type="string">
  The ID of the domain unit. Optional.
</ResponseField>

<ResponseField name="glossary_terms" type="list(string)">
  The glossary terms that can be used in this Amazon DataZone project. Optional.
</ResponseField>

<ResponseField name="membership_assignments" type="list(object)">
  The project membership assignments. Optional.
  <Expandable title="properties">
    <ResponseField name="designation" type="string">
      Specifies the access role (e.g., OWNER or CONTRIBUTOR) granted to the member in the project assignment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="member" type="object">
      The member object identifies the user or group in Amazon DataZone that is assigned to the project, specifying the principal's identifier and whether it is a USER or GROUP. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="group_identifier" type="string">
          The identifier of the AWS DataZone group that is granted membership in the project. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_identifier" type="string">
          Specifies the user identifier for a user assigned to the project via a membership assignment, used to grant that user access to project resources in AWS DataZone. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the Amazon DataZone project. Required.
</ResponseField>

<ResponseField name="project_category" type="string">
  The project category. Optional.
</ResponseField>

<ResponseField name="project_execution_role" type="string">
  The project execution role ARN. Optional.
</ResponseField>

<ResponseField name="project_profile_id" type="string">
  The project profile ID. Optional.
</ResponseField>

<ResponseField name="project_profile_version" type="string">
  The project profile version to which the project should be updated. You can only specify the following string for this parameter: latest. Optional.
</ResponseField>

<ResponseField name="resource_tags" type="list(object)">
  The resource tags of the project. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a resource tag associated with the AWS Data Zone project, specifying the tag name used for identifying and organizing the project resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag (key-value pair) attached to the AWS DataZone project, used for organizing and managing project resources. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_parameters" type="list(object)">
  The user parameters of the project. Optional.
  <Expandable title="properties">
    <ResponseField name="environment_configuration_name" type="string">
      Specifies the name of the environment configuration in the DataZone project to which this user parameter applies, allowing parameters to be scoped to a specific environment configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="environment_id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="environment_parameters" type="list(object)">
      This field specifies a list of environment parameter objects that define configuration values for AWS Data Zone environments associated with the project, such as resource names or compute settings, which are applied when environments are created from the project's environment profiles. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name (key) of an environment parameter configured for the AWS DataZone project's environment, used to pass configuration values when the environment is provisioned. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value of an environment parameter in the project's user parameters, which is passed to the DataZone environment when the project is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp of when the project was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  The Amazon DataZone user who created the project. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_id" type="string">
  The identifier of the Amazon DataZone domain in which the project was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the Amazon DataZone project. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The timestamp of when the project was last updated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_status" type="string">
  The current lifecycle status of the Data Zone project, such as 'ACTIVE' or 'DELETING'. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
