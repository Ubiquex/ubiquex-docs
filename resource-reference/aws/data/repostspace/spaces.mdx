---
title: "aws_repostspace_spaces"
description: "`aws_repostspace_spaces` looks up a real re:Post Private space, a private, organization-scoped Q&A knowledge base."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	repostspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/repostspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_repostspace_spaces"})

    		ubx.Data(repostspace.Spaces, "example", repostspace.SpacesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Spaces } from "@ubx/sdk-aws/aws/data/repostspace/spaces";

    export default stack("example", () => {
      intent({ summary: "look up aws_repostspace_spaces" });

      data(Spaces, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.repostspace import Spaces, SpacesConfig

    def describe():
        ubx.intent("look up aws_repostspace_spaces")

        ubx.data(Spaces, "example", SpacesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="spaces" type="list(object)">
  The real spaces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
