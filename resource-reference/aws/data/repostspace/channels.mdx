---
title: "aws_repostspace_channels"
description: "`aws_repostspace_channels` looks up a real re:Post Private channel, a topic-scoped area within a private knowledge base."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	repostspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/repostspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_repostspace_channels"})

    		ubx.Data(repostspace.Channels, "example", repostspace.ChannelsConfig{
    			SpaceId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Channels } from "@ubx/sdk-aws/aws/data/repostspace/channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_repostspace_channels" });

      data(Channels, "example", {
        spaceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.repostspace import Channels, ChannelsConfig

    def describe():
        ubx.intent("look up aws_repostspace_channels")

        ubx.data(Channels, "example", ChannelsConfig(
            space_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="space_id" type="string" required>
  The real, unique identifier of the space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channels" type="list(object)">
  The real channels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
