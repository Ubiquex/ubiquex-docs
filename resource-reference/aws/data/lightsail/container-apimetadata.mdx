---
title: "aws_lightsail_container_apimetadata"
description: "`data_aws_lightsail_container_apimetadata` looks up a real, already-existing `aws_lightsail_container`."
---

It creates a real Lightsail Container Service, a managed environment that deploys and runs containerized applications, images pushed via Docker, without the operational surface of a full ECS or EKS cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_container_apimetadata"})

    		ubx.Data(lightsail.ContainerApimetadata, "example", lightsail.ContainerApimetadataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerApimetadata } from "@ubx/sdk-aws/aws/data/lightsail/container_apimetadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_container_apimetadata" });

      data(ContainerApimetadata, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import ContainerApimetadata, ContainerApimetadataConfig

    def describe():
        ubx.intent("look up aws_lightsail_container_apimetadata")

        ubx.data(ContainerApimetadata, "example", ContainerApimetadataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="metadata" type="list(map(string))">
  The real, structured metadata of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
