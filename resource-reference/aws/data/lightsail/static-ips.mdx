---
title: "aws_lightsail_static_ips"
description: "`aws_lightsail_static_ips` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_static_ips` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_static_ips"})

    		ubx.Data(lightsail.StaticIps, "example", lightsail.StaticIpsConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StaticIps } from "@ubx/sdk-aws/aws/data/lightsail/static_ips";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_static_ips" });

      data(StaticIps, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import StaticIps, StaticIpsConfig

    def describe():
        ubx.intent("look up aws_lightsail_static_ips")

        ubx.data(StaticIps, "example", StaticIpsConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="static_ips" type="list(object)">
  Returned by the lookup.
</ResponseField>
