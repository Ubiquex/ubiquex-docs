---
title: "aws_lightsail_bucket_bundles"
description: "`data_aws_lightsail_bucket_bundles` looks up a real, already-existing `aws_lightsail_bucket`."
---

It creates a real Lightsail object storage bucket, built on S3, for storing files at Lightsail's simplified, predictable-pricing tier rather than through the full S3 console.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_bucket_bundles"})

    		ubx.Data(lightsail.BucketBundles, "example", lightsail.BucketBundlesConfig{
    			IncludeInactive: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketBundles } from "@ubx/sdk-aws/aws/data/lightsail/bucket_bundles";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_bucket_bundles" });

      data(BucketBundles, "example", {
        includeInactive: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import BucketBundles, BucketBundlesConfig

    def describe():
        ubx.intent("look up aws_lightsail_bucket_bundles")

        ubx.data(BucketBundles, "example", BucketBundlesConfig(
            include_inactive=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_inactive" type="bool">
  Whether inactive objects are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bundles" type="list(object)">
  The real bundles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
