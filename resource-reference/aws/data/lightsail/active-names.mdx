---
title: "aws_lightsail_active_names"
description: "`aws_lightsail_active_names` reads the real names currently in use across every Lightsail resource on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_active_names"})

    		ubx.Data(lightsail.ActiveNames, "example", lightsail.ActiveNamesConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActiveNames } from "@ubx/sdk-aws/aws/data/lightsail/active_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_active_names" });

      data(ActiveNames, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import ActiveNames, ActiveNamesConfig

    def describe():
        ubx.intent("look up aws_lightsail_active_names")

        ubx.data(ActiveNames, "example", ActiveNamesConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_names" type="list(string)">
  The real names of the actives included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
