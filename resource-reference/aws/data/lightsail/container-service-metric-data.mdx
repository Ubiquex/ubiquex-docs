---
title: "aws_lightsail_container_service_metric_data"
description: "`aws_lightsail_container_service_metric_data` reads real, reported metric data for a Lightsail container service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_container_service_metric_data"})

    		ubx.Data(lightsail.ContainerServiceMetricData, "example", lightsail.ContainerServiceMetricDataConfig{
    			EndTime:     "example",
    			MetricName:  "example-metric-name",
    			Period:      1,
    			ServiceName: "example-service-name",
    			StartTime:   "example",
    			Statistics:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ContainerServiceMetricData } from "./local-sdk/aws/sdk/typescript/aws/data/lightsail/container_service_metric_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_container_service_metric_data" });

      data(ContainerServiceMetricData, "example", {
        endTime: "example",
        metricName: "example-metric-name",
        period: 1,
        serviceName: "example-service-name",
        startTime: "example",
        statistics: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import ContainerServiceMetricData, ContainerServiceMetricDataConfig

    def describe():
        ubx.intent("look up aws_lightsail_container_service_metric_data")

        ubx.data(ContainerServiceMetricData, "example", ContainerServiceMetricDataConfig(
            end_time="example",
            metric_name="example-metric-name",
            period=1,
            service_name="example-service-name",
            start_time="example",
            statistics=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  The real name of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)" required>
  The real, computed statistics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data" type="list(object)">
  The real, raw metric data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
