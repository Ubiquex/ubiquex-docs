---
title: "aws_lightsail_relational_database_parameters"
description: "`aws_lightsail_relational_database_parameters` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_relational_database_parameters` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_parameters"})

    		ubx.Data(lightsail.RelationalDatabaseParameters, "example", lightsail.RelationalDatabaseParametersConfig{
    			RelationalDatabaseName: "example-relational-database-name",
    			PageToken:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelationalDatabaseParameters } from "@ubx/sdk-aws/aws/data/lightsail/relational_database_parameters";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_relational_database_parameters" });

      data(RelationalDatabaseParameters, "example", {
        relationalDatabaseName: "example-relational-database-name",
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseParameters, RelationalDatabaseParametersConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_parameters")

        ubx.data(RelationalDatabaseParameters, "example", RelationalDatabaseParametersConfig(
            relational_database_name="example-relational-database-name",
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="relational_database_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  Returned by the lookup.
</ResponseField>
