---
title: "aws_lightsail_bundles"
description: "`aws_lightsail_bundles` reads the real, available Lightsail instance bundles (a fixed compute, storage, and price plan)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_bundles"})

    		ubx.Data(lightsail.Bundles, "example", lightsail.BundlesConfig{
    			AppCategory:     "example",
    			IncludeInactive: true,
    			PageToken:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Bundles } from "@ubx/sdk-aws/aws/data/lightsail/bundles";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_bundles" });

      data(Bundles, "example", {
        appCategory: "example",
        includeInactive: true,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import Bundles, BundlesConfig

    def describe():
        ubx.intent("look up aws_lightsail_bundles")

        ubx.data(Bundles, "example", BundlesConfig(
            app_category="example",
            include_inactive=True,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_category" type="string">
  The real category of the app this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_inactive" type="bool">
  Whether inactive objects are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bundles" type="list(object)">
  The real bundles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
