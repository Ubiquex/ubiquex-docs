---
title: "aws_lightsail_blueprints"
description: "`aws_lightsail_blueprints` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_blueprints` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_blueprints"})

    		ubx.Data(lightsail.Blueprints, "example", lightsail.BlueprintsConfig{
    			AppCategory:     "example",
    			IncludeInactive: true,
    			PageToken:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Blueprints } from "@ubx/sdk-aws/aws/data/lightsail/blueprints";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_blueprints" });

      data(Blueprints, "example", {
        appCategory: "example",
        includeInactive: true,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import Blueprints, BlueprintsConfig

    def describe():
        ubx.intent("look up aws_lightsail_blueprints")

        ubx.data(Blueprints, "example", BlueprintsConfig(
            app_category="example",
            include_inactive=True,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_category" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_inactive" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blueprints" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>
