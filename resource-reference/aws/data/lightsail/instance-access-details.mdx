---
title: "aws_lightsail_instance_access_details"
description: "`aws_lightsail_instance_access_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_instance_access_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_instance_access_details"})

    		ubx.Data(lightsail.InstanceAccessDetails, "example", lightsail.InstanceAccessDetailsConfig{
    			InstanceName: "example-instance-name",
    			Protocol:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceAccessDetails } from "@ubx/sdk-aws/aws/data/lightsail/instance_access_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_instance_access_details" });

      data(InstanceAccessDetails, "example", {
        instanceName: "example-instance-name",
        protocol: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import InstanceAccessDetails, InstanceAccessDetailsConfig

    def describe():
        ubx.intent("look up aws_lightsail_instance_access_details")

        ubx.data(InstanceAccessDetails, "example", InstanceAccessDetailsConfig(
            instance_name="example-instance-name",
            protocol="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="protocol" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_details" type="object">
  &lt;p&gt;The parameters for gaining temporary access to one of your Amazon Lightsail instances.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cert_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expires_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="host_keys" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ip_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ipv6_addresses" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="password" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="password_data" type="object">
      &lt;p&gt;The password data for the Windows Server-based instance, including the ciphertext and the key pair name.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="ciphertext" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="key_pair_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="protocol" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="username" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
