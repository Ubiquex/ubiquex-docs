---
title: "aws_lightsail_disk_snapshots"
description: "`data_aws_lightsail_disk_snapshots` looks up a real, already-existing `aws_lightsail_disk_snapshot`."
---

It creates a real point-in-time snapshot of an `aws_lightsail_disk`, which can later be used to create a new disk with the same data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_disk_snapshots"})

    		ubx.Data(lightsail.DiskSnapshots, "example", lightsail.DiskSnapshotsConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DiskSnapshots } from "@ubx/sdk-aws/aws/data/lightsail/disk_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_disk_snapshots" });

      data(DiskSnapshots, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import DiskSnapshots, DiskSnapshotsConfig

    def describe():
        ubx.intent("look up aws_lightsail_disk_snapshots")

        ubx.data(DiskSnapshots, "example", DiskSnapshotsConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="disk_snapshots" type="list(object)">
  The real disk snapshots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
