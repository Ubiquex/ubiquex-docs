---
title: "aws_lightsail_relational_database_log_streams"
description: "`aws_lightsail_relational_database_log_streams` reads the real log streams available for a Lightsail managed database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_log_streams"})

    		ubx.Data(lightsail.RelationalDatabaseLogStreams, "example", lightsail.RelationalDatabaseLogStreamsConfig{
    			RelationalDatabaseName: "example-relational-database-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RelationalDatabaseLogStreams } from "./local-sdk/aws/sdk/typescript/aws/data/lightsail/relational_database_log_streams.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_relational_database_log_streams" });

      data(RelationalDatabaseLogStreams, "example", {
        relationalDatabaseName: "example-relational-database-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseLogStreams, RelationalDatabaseLogStreamsConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_log_streams")

        ubx.data(RelationalDatabaseLogStreams, "example", RelationalDatabaseLogStreamsConfig(
            relational_database_name="example-relational-database-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="relational_database_name" type="string" required>
  The real name of the Lightsail relational database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="log_streams" type="list(string)">
  The real CloudWatch Logs log streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
