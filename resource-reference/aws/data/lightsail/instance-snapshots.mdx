---
title: "aws_lightsail_instance_snapshots"
description: "`data_aws_lightsail_instance_snapshots` looks up a real, already-existing `aws_lightsail_instance_snapshot`."
---

It creates a real point-in-time snapshot of an `aws_lightsail_instance`, including its attached disks, which can later be used to launch a new instance with the same state.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_instance_snapshots"})

    		ubx.Data(lightsail.InstanceSnapshots, "example", lightsail.InstanceSnapshotsConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstanceSnapshots } from "./local-sdk/aws/sdk/typescript/aws/data/lightsail/instance_snapshots.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_instance_snapshots" });

      data(InstanceSnapshots, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import InstanceSnapshots, InstanceSnapshotsConfig

    def describe():
        ubx.intent("look up aws_lightsail_instance_snapshots")

        ubx.data(InstanceSnapshots, "example", InstanceSnapshotsConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_snapshots" type="list(object)">
  The real instance snapshots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
