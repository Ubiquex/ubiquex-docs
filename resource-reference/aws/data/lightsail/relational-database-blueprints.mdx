---
title: "aws_lightsail_relational_database_blueprints"
description: "`aws_lightsail_relational_database_blueprints` reads the real, available Lightsail managed database engine blueprints."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_blueprints"})

    		ubx.Data(lightsail.RelationalDatabaseBlueprints, "example", lightsail.RelationalDatabaseBlueprintsConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelationalDatabaseBlueprints } from "@ubx/sdk-aws/aws/data/lightsail/relational_database_blueprints";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_relational_database_blueprints" });

      data(RelationalDatabaseBlueprints, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseBlueprints, RelationalDatabaseBlueprintsConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_blueprints")

        ubx.data(RelationalDatabaseBlueprints, "example", RelationalDatabaseBlueprintsConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blueprints" type="list(object)">
  The real blueprints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
