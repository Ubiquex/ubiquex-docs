---
title: "aws_lightsail_load_balancer_metric_data"
description: "`aws_lightsail_load_balancer_metric_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_load_balancer_metric_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_load_balancer_metric_data"})

    		ubx.Data(lightsail.LoadBalancerMetricData, "example", lightsail.LoadBalancerMetricDataConfig{
    			EndTime:          "example",
    			LoadBalancerName: "example-load-balancer-name",
    			MetricName:       "example-metric-name",
    			Period:           1,
    			StartTime:        "example",
    			Statistics:       []string{"example"},
    			Unit:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoadBalancerMetricData } from "@ubx/sdk-aws/aws/data/lightsail/load_balancer_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_load_balancer_metric_data" });

      data(LoadBalancerMetricData, "example", {
        endTime: "example",
        loadBalancerName: "example-load-balancer-name",
        metricName: "example-metric-name",
        period: 1,
        startTime: "example",
        statistics: ["example"],
        unit: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import LoadBalancerMetricData, LoadBalancerMetricDataConfig

    def describe():
        ubx.intent("look up aws_lightsail_load_balancer_metric_data")

        ubx.data(LoadBalancerMetricData, "example", LoadBalancerMetricDataConfig(
            end_time="example",
            load_balancer_name="example-load-balancer-name",
            metric_name="example-metric-name",
            period=1,
            start_time="example",
            statistics=["example"],
            unit="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="load_balancer_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="unit" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data" type="list(object)">
  Returned by the lookup.
</ResponseField>
