---
title: "aws_lightsail_relational_database_bundles"
description: "`aws_lightsail_relational_database_bundles` reads the real, available Lightsail managed database bundles."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_bundles"})

    		ubx.Data(lightsail.RelationalDatabaseBundles, "example", lightsail.RelationalDatabaseBundlesConfig{
    			IncludeInactive: true,
    			PageToken:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RelationalDatabaseBundles } from "./local-sdk/aws/sdk/typescript/aws/data/lightsail/relational_database_bundles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_relational_database_bundles" });

      data(RelationalDatabaseBundles, "example", {
        includeInactive: true,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseBundles, RelationalDatabaseBundlesConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_bundles")

        ubx.data(RelationalDatabaseBundles, "example", RelationalDatabaseBundlesConfig(
            include_inactive=True,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_inactive" type="bool">
  Whether inactive objects are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bundles" type="list(object)">
  The real bundles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
