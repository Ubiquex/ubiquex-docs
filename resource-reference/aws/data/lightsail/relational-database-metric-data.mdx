---
title: "aws_lightsail_relational_database_metric_data"
description: "`aws_lightsail_relational_database_metric_data` reads real, reported metric data for a Lightsail managed database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_metric_data"})

    		ubx.Data(lightsail.RelationalDatabaseMetricData, "example", lightsail.RelationalDatabaseMetricDataConfig{
    			EndTime:                "example",
    			MetricName:             "example-metric-name",
    			Period:                 1,
    			RelationalDatabaseName: "example-relational-database-name",
    			StartTime:              "example",
    			Statistics:             []string{"example"},
    			Unit:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelationalDatabaseMetricData } from "@ubx/sdk-aws/aws/data/lightsail/relational_database_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_relational_database_metric_data" });

      data(RelationalDatabaseMetricData, "example", {
        endTime: "example",
        metricName: "example-metric-name",
        period: 1,
        relationalDatabaseName: "example-relational-database-name",
        startTime: "example",
        statistics: ["example"],
        unit: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseMetricData, RelationalDatabaseMetricDataConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_metric_data")

        ubx.data(RelationalDatabaseMetricData, "example", RelationalDatabaseMetricDataConfig(
            end_time="example",
            metric_name="example-metric-name",
            period=1,
            relational_database_name="example-relational-database-name",
            start_time="example",
            statistics=["example"],
            unit="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="relational_database_name" type="string" required>
  The real name of the Lightsail relational database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)" required>
  The real, computed statistics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="unit" type="string" required>
  The real unit of measurement for this value. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data" type="list(object)">
  The real, raw metric data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
