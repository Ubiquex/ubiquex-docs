---
title: "aws_lightsail_container_images"
description: "`aws_lightsail_container_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_container_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_container_images"})

    		ubx.Data(lightsail.ContainerImages, "example", lightsail.ContainerImagesConfig{
    			ServiceName: "example-service-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerImages } from "@ubx/sdk-aws/aws/data/lightsail/container_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_container_images" });

      data(ContainerImages, "example", {
        serviceName: "example-service-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import ContainerImages, ContainerImagesConfig

    def describe():
        ubx.intent("look up aws_lightsail_container_images")

        ubx.data(ContainerImages, "example", ContainerImagesConfig(
            service_name="example-service-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="service_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="container_images" type="list(object)">
  Returned by the lookup.
</ResponseField>
