---
title: "aws_lightsail_relational_database_master_user_password"
description: "`aws_lightsail_relational_database_master_user_password` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_relational_database_master_user_password` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_relational_database_master_user_password"})

    		ubx.Data(lightsail.RelationalDatabaseMasterUserPassword, "example", lightsail.RelationalDatabaseMasterUserPasswordConfig{
    			RelationalDatabaseName: "example-relational-database-name",
    			PasswordVersion:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelationalDatabaseMasterUserPassword } from "@ubx/sdk-aws/aws/data/lightsail/relational_database_master_user_password";

    export default stack("example", () => {
      intent({
        summary: "look up aws_lightsail_relational_database_master_user_password",
      });

      data(RelationalDatabaseMasterUserPassword, "example", {
        relationalDatabaseName: "example-relational-database-name",
        passwordVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import RelationalDatabaseMasterUserPassword, RelationalDatabaseMasterUserPasswordConfig

    def describe():
        ubx.intent("look up aws_lightsail_relational_database_master_user_password")

        ubx.data(RelationalDatabaseMasterUserPassword, "example", RelationalDatabaseMasterUserPasswordConfig(
            relational_database_name="example-relational-database-name",
            password_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="password_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="relational_database_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="master_user_password" type="string">
  Returned by the lookup.
</ResponseField>
