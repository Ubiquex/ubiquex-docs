---
title: "aws_lightsail_instance_metric_data"
description: "`data_aws_lightsail_instance_metric_data` looks up a real, already-existing `aws_lightsail_instance`."
---

It creates a real virtual private server, a bundled combination of compute, storage, and a fixed data transfer allowance, at Lightsail's simplified, predictable monthly pricing.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_instance_metric_data"})

    		ubx.Data(lightsail.InstanceMetricData, "example", lightsail.InstanceMetricDataConfig{
    			EndTime:      "example",
    			InstanceName: "example-instance-name",
    			MetricName:   "example-metric-name",
    			Period:       1,
    			StartTime:    "example",
    			Statistics:   []string{"example"},
    			Unit:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstanceMetricData } from "./local-sdk/aws/sdk/typescript/aws/data/lightsail/instance_metric_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_instance_metric_data" });

      data(InstanceMetricData, "example", {
        endTime: "example",
        instanceName: "example-instance-name",
        metricName: "example-metric-name",
        period: 1,
        startTime: "example",
        statistics: ["example"],
        unit: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import InstanceMetricData, InstanceMetricDataConfig

    def describe():
        ubx.intent("look up aws_lightsail_instance_metric_data")

        ubx.data(InstanceMetricData, "example", InstanceMetricDataConfig(
            end_time="example",
            instance_name="example-instance-name",
            metric_name="example-metric-name",
            period=1,
            start_time="example",
            statistics=["example"],
            unit="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_name" type="string" required>
  The real name of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)" required>
  The real, computed statistics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="unit" type="string" required>
  The real unit of measurement for this value. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_data" type="list(object)">
  The real, raw metric data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
