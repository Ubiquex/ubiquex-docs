---
title: "aws_lightsail_instance_port_states"
description: "`aws_lightsail_instance_port_states` reads the real, open port states of a Lightsail instance's own firewall."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_instance_port_states"})

    		ubx.Data(lightsail.InstancePortStates, "example", lightsail.InstancePortStatesConfig{
    			InstanceName: "example-instance-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstancePortStates } from "@ubx/sdk-aws/aws/data/lightsail/instance_port_states";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_instance_port_states" });

      data(InstancePortStates, "example", {
        instanceName: "example-instance-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import InstancePortStates, InstancePortStatesConfig

    def describe():
        ubx.intent("look up aws_lightsail_instance_port_states")

        ubx.data(InstancePortStates, "example", InstancePortStatesConfig(
            instance_name="example-instance-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_name" type="string" required>
  The real name of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="port_states" type="list(object)">
  The real, current states of the ports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
