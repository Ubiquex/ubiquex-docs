---
title: "aws_lightsail_disks"
description: "`data_aws_lightsail_disks` looks up a real, already-existing `aws_lightsail_disk`."
---

It creates a real, SSD-based block storage volume that can be attached to a Lightsail instance for additional or persistent storage beyond the instance's own bundled disk.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_disks"})

    		ubx.Data(lightsail.Disks, "example", lightsail.DisksConfig{
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Disks } from "@ubx/sdk-aws/aws/data/lightsail/disks";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_disks" });

      data(Disks, "example", {
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import Disks, DisksConfig

    def describe():
        ubx.intent("look up aws_lightsail_disks")

        ubx.data(Disks, "example", DisksConfig(
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="disks" type="list(object)">
  The real disks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
