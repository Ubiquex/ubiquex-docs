---
title: "aws_lightsail_auto_snapshots"
description: "`aws_lightsail_auto_snapshots` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lightsail_auto_snapshots` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lightsail_auto_snapshots"})

    		ubx.Data(lightsail.AutoSnapshots, "example", lightsail.AutoSnapshotsConfig{
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutoSnapshots } from "@ubx/sdk-aws/aws/data/lightsail/auto_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_lightsail_auto_snapshots" });

      data(AutoSnapshots, "example", {
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lightsail import AutoSnapshots, AutoSnapshotsConfig

    def describe():
        ubx.intent("look up aws_lightsail_auto_snapshots")

        ubx.data(AutoSnapshots, "example", AutoSnapshotsConfig(
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_snapshots" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Returned by the lookup.
</ResponseField>
