---
title: "aws_application_autoscaling_scalable_targets"
description: "`aws_application_autoscaling_scalable_targets` looks up a real Application Auto Scaling scalable target..."
---

`aws_application_autoscaling_scalable_targets` looks up a real Application Auto Scaling scalable target, registering a resource (such as an ECS service or DynamoDB table) for scaling.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application_autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_autoscaling_scalable_targets"})

    		ubx.Data(application_autoscaling.ApplicationAutoscalingScalableTargets, "example", application_autoscaling.ApplicationAutoscalingScalableTargetsConfig{
    			ServiceNamespace:  "example",
    			MaxResults:        1,
    			ResourceIds:       []string{"example"},
    			ScalableDimension: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationAutoscalingScalableTargets } from "./local-sdk/aws/sdk/typescript/aws/data/application_autoscaling/application_autoscaling_scalable_targets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_autoscaling_scalable_targets" });

      data(ApplicationAutoscalingScalableTargets, "example", {
        serviceNamespace: "example",
        maxResults: 1,
        resourceIds: ["example"],
        scalableDimension: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.application_autoscaling import ApplicationAutoscalingScalableTargets, ApplicationAutoscalingScalableTargetsConfig

    def describe():
        ubx.intent("look up aws_application_autoscaling_scalable_targets")

        ubx.data(ApplicationAutoscalingScalableTargets, "example", ApplicationAutoscalingScalableTargetsConfig(
            service_namespace="example",
            max_results=1,
            resource_ids=["example"],
            scalable_dimension="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_ids" type="list(string)">
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string">
  The real Application Auto Scaling scalable dimension this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  The real Application Auto Scaling service namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scalable_targets" type="list(object)">
  The real targets of the scalable. **(AI-inferred)** Returned by the lookup.
</ResponseField>
