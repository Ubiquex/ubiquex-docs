---
title: "aws_application_autoscaling_scaling_policies"
description: "`aws_application_autoscaling_scaling_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_application_autoscaling_scaling_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_autoscaling_scaling_policies"})

    		ubx.Data(application_autoscaling.ApplicationAutoscalingScalingPolicies, "example", application_autoscaling.ApplicationAutoscalingScalingPoliciesConfig{
    			ServiceNamespace:  "example",
    			MaxResults:        1,
    			PolicyNames:       []string{"example"},
    			ResourceId:        "example",
    			ScalableDimension: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationAutoscalingScalingPolicies } from "@ubx/sdk-aws/aws/data/application_autoscaling/application_autoscaling_scaling_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_autoscaling_scaling_policies" });

      data(ApplicationAutoscalingScalingPolicies, "example", {
        serviceNamespace: "example",
        maxResults: 1,
        policyNames: ["example"],
        resourceId: "example",
        scalableDimension: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_autoscaling import ApplicationAutoscalingScalingPolicies, ApplicationAutoscalingScalingPoliciesConfig

    def describe():
        ubx.intent("look up aws_application_autoscaling_scaling_policies")

        ubx.data(ApplicationAutoscalingScalingPolicies, "example", ApplicationAutoscalingScalingPoliciesConfig(
            service_namespace="example",
            max_results=1,
            policy_names=["example"],
            resource_id="example",
            scalable_dimension="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="policy_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>
