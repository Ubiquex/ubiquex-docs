---
title: "aws_application_autoscaling_scaling_activities"
description: "`aws_application_autoscaling_scaling_activities` reads a real, historical scaling activity of an Application Auto Scaling scalable target."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application_autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_autoscaling_scaling_activities"})

    		ubx.Data(application_autoscaling.ApplicationAutoscalingScalingActivities, "example", application_autoscaling.ApplicationAutoscalingScalingActivitiesConfig{
    			ServiceNamespace:           "example",
    			IncludeNotScaledActivities: true,
    			MaxResults:                 1,
    			ResourceId:                 "example",
    			ScalableDimension:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationAutoscalingScalingActivities } from "./local-sdk/aws/sdk/typescript/aws/data/application_autoscaling/application_autoscaling_scaling_activities.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_autoscaling_scaling_activities" });

      data(ApplicationAutoscalingScalingActivities, "example", {
        serviceNamespace: "example",
        includeNotScaledActivities: true,
        maxResults: 1,
        resourceId: "example",
        scalableDimension: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.application_autoscaling import ApplicationAutoscalingScalingActivities, ApplicationAutoscalingScalingActivitiesConfig

    def describe():
        ubx.intent("look up aws_application_autoscaling_scaling_activities")

        ubx.data(ApplicationAutoscalingScalingActivities, "example", ApplicationAutoscalingScalingActivitiesConfig(
            service_namespace="example",
            include_not_scaled_activities=True,
            max_results=1,
            resource_id="example",
            scalable_dimension="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_not_scaled_activities" type="bool">
  Whether activities that did not result in scaling are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string">
  The real Application Auto Scaling scalable dimension this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  The real Application Auto Scaling service namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_activities" type="list(object)">
  The real Auto Scaling activities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
