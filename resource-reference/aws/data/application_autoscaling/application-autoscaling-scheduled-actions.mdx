---
title: "aws_application_autoscaling_scheduled_actions"
description: "`aws_application_autoscaling_scheduled_actions` looks up a real Application Auto Scaling scheduled action..."
---

`aws_application_autoscaling_scheduled_actions` looks up a real Application Auto Scaling scheduled action, changing a scalable target's own capacity at a specific time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application_autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_autoscaling_scheduled_actions"})

    		ubx.Data(application_autoscaling.ApplicationAutoscalingScheduledActions, "example", application_autoscaling.ApplicationAutoscalingScheduledActionsConfig{
    			ServiceNamespace:     "example",
    			MaxResults:           1,
    			ResourceId:           "example",
    			ScalableDimension:    "example",
    			ScheduledActionNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationAutoscalingScheduledActions } from "./local-sdk/aws/sdk/typescript/aws/data/application_autoscaling/application_autoscaling_scheduled_actions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_autoscaling_scheduled_actions" });

      data(ApplicationAutoscalingScheduledActions, "example", {
        serviceNamespace: "example",
        maxResults: 1,
        resourceId: "example",
        scalableDimension: "example",
        scheduledActionNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.application_autoscaling import ApplicationAutoscalingScheduledActions, ApplicationAutoscalingScheduledActionsConfig

    def describe():
        ubx.intent("look up aws_application_autoscaling_scheduled_actions")

        ubx.data(ApplicationAutoscalingScheduledActions, "example", ApplicationAutoscalingScheduledActionsConfig(
            service_namespace="example",
            max_results=1,
            resource_id="example",
            scalable_dimension="example",
            scheduled_action_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string">
  The real Application Auto Scaling scalable dimension this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scheduled_action_names" type="list(string)">
  The real names of the scheduled actions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  The real Application Auto Scaling service namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_actions" type="list(object)">
  The real scheduled actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
