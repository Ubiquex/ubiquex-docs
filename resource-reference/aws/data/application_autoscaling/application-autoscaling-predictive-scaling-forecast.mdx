---
title: "aws_application_autoscaling_predictive_scaling_forecast"
description: "`aws_application_autoscaling_predictive_scaling_forecast` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_application_autoscaling_predictive_scaling_forecast` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_autoscaling_predictive_scaling_forecast"})

    		ubx.Data(application_autoscaling.ApplicationAutoscalingPredictiveScalingForecast, "example", application_autoscaling.ApplicationAutoscalingPredictiveScalingForecastConfig{
    			EndTime:           "example",
    			PolicyName:        "example-policy-name",
    			ResourceId:        "example",
    			ScalableDimension: "example",
    			ServiceNamespace:  "example",
    			StartTime:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationAutoscalingPredictiveScalingForecast } from "@ubx/sdk-aws/aws/data/application_autoscaling/application_autoscaling_predictive_scaling_forecast";

    export default stack("example", () => {
      intent({
        summary: "look up aws_application_autoscaling_predictive_scaling_forecast",
      });

      data(ApplicationAutoscalingPredictiveScalingForecast, "example", {
        endTime: "example",
        policyName: "example-policy-name",
        resourceId: "example",
        scalableDimension: "example",
        serviceNamespace: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_autoscaling import ApplicationAutoscalingPredictiveScalingForecast, ApplicationAutoscalingPredictiveScalingForecastConfig

    def describe():
        ubx.intent("look up aws_application_autoscaling_predictive_scaling_forecast")

        ubx.data(ApplicationAutoscalingPredictiveScalingForecast, "example", ApplicationAutoscalingPredictiveScalingForecastConfig(
            end_time="example",
            policy_name="example-policy-name",
            resource_id="example",
            scalable_dimension="example",
            service_namespace="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_forecast" type="object">
  &lt;p&gt; A &lt;code&gt;GetPredictiveScalingForecast&lt;/code&gt; call returns the capacity forecast for a predictive scaling policy. This structure includes the data points for that capacity forecast, along with the timestamps of those data points. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="timestamps" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="values" type="list(number)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="load_forecast" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  Returned by the lookup.
</ResponseField>
