---
title: "aws_mediaconnect_entitlements"
description: "`aws_mediaconnect_entitlements` looks up a real MediaConnect entitlement, granting another account access to a flow's own output."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconnect_entitlements"})

    		ubx.Data(mediaconnect.Entitlements, "example", mediaconnect.EntitlementsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Entitlements } from "@ubx/sdk-aws/aws/data/mediaconnect/entitlements";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconnect_entitlements" });

      data(Entitlements, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconnect import Entitlements, EntitlementsConfig

    def describe():
        ubx.intent("look up aws_mediaconnect_entitlements")

        ubx.data(Entitlements, "example", EntitlementsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entitlements" type="list(object)">
  The real entitlements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
