---
title: "aws_mediaconnect_tags_for_global_resource"
description: "`aws_mediaconnect_tags_for_global_resource` reads the real tags applied to a global MediaConnect resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mediaconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mediaconnect_tags_for_global_resource"})

    		ubx.Data(mediaconnect.TagsForGlobalResource, "example", mediaconnect.TagsForGlobalResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForGlobalResource } from "@ubx/sdk-aws/aws/data/mediaconnect/tags_for_global_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_mediaconnect_tags_for_global_resource" });

      data(TagsForGlobalResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mediaconnect import TagsForGlobalResource, TagsForGlobalResourceConfig

    def describe():
        ubx.intent("look up aws_mediaconnect_tags_for_global_resource")

        ubx.data(TagsForGlobalResource, "example", TagsForGlobalResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
