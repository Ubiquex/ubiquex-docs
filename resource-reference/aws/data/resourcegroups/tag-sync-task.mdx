---
title: "aws_resource_groups_tag_sync_task"
description: "`aws_resource_groups_tag_sync_task` looks up one real Resource Groups tag-sync task, keeping a group's own resources tagged to match its membership query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resourcegroups "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/resourcegroups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_resource_groups_tag_sync_task"})

    		ubx.Data(resourcegroups.TagSyncTask, "example", resourcegroups.TagSyncTaskConfig{
    			TaskArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagSyncTask } from "@ubx/sdk-aws/aws/data/resourcegroups/tag_sync_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_resource_groups_tag_sync_task" });

      data(TagSyncTask, "example", {
        taskArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.resourcegroups import TagSyncTask, TagSyncTaskConfig

    def describe():
        ubx.intent("look up aws_resource_groups_tag_sync_task")

        ubx.data(TagSyncTask, "example", TagSyncTaskConfig(
            task_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="group_arn" type="string">
  The real Amazon Resource Name (ARN) of the group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="group_name" type="string">
  The real name of the group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_query" type="object">
  &lt;p&gt;The query you can use to define a resource group or a search for resources. A &lt;code&gt;ResourceQuery&lt;/code&gt; specifies both a query &lt;code&gt;Type&lt;/code&gt; and a &lt;code&gt;Query&lt;/code&gt; string as JSON string objects. See the examples section for example JSON strings. For more information about creating a resource group with a resource query, see &lt;a href="https://docs.aws.amazon.com/ARG/latest/userguide/gettingstarted-query.html"&gt;Build queries and groups in Resource Groups&lt;/a&gt; in the &lt;i&gt;Resource Groups User Guide&lt;/i&gt; &lt;/p&gt; &lt;p&gt;When you combine all of the elements together into a single string, any double quotes that are embedded inside another double quote pair must be escaped by preceding the embedded double quote with a backslash character (\). For example, a complete &lt;code&gt;ResourceQuery&lt;/code&gt; parameter must be formatted like the following CLI parameter example:&lt;/p&gt; &lt;p&gt; &lt;code&gt;--resource-query '&#123;"Type":"TAG_FILTERS_1_0","Query":"&#123;\"ResourceTypeFilters\":[\"AWS::AllSupported\"],\"TagFilters\":[&#123;\"Key\":\"Stage\",\"Values\":[\"Test\"]&#125;]&#125;"&#125;'&lt;/code&gt; &lt;/p&gt; &lt;p&gt;In the preceding example, all of the double quote characters in the value part of the &lt;code&gt;Query&lt;/code&gt; element must be escaped because the value itself is surrounded by double quotes. For more information, see &lt;a href="https://docs.aws.amazon.com/cli/latest/userguide/cli-usage-parameters-quoting-strings.html"&gt;Quoting strings&lt;/a&gt; in the &lt;i&gt;Command Line Interface User Guide&lt;/i&gt;.&lt;/p&gt; &lt;p&gt;For the complete list of resource types that you can use in the array value for &lt;code&gt;ResourceTypeFilters&lt;/code&gt;, see &lt;a href="https://docs.aws.amazon.com/ARG/latest/userguide/supported-resources.html"&gt;Resources you can use with Resource Groups and Tag Editor&lt;/a&gt; in the &lt;i&gt;Resource Groups User Guide&lt;/i&gt;. For example:&lt;/p&gt; &lt;p&gt; &lt;code&gt;"ResourceTypeFilters":["AWS::S3::Bucket", "AWS::EC2::Instance"]&lt;/code&gt; &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="query" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_key" type="string">
  The real, unique key of the tag this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_value" type="string">
  The real value of the tag this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
