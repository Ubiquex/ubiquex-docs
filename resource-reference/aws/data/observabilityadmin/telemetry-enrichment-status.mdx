---
title: "aws_observabilityadmin_telemetry_enrichment_status"
description: "`aws_observabilityadmin_telemetry_enrichment_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_observabilityadmin_telemetry_enrichment_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observabilityadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/observabilityadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_observabilityadmin_telemetry_enrichment_status"})

    		ubx.Data(observabilityadmin.TelemetryEnrichmentStatus, "example", observabilityadmin.TelemetryEnrichmentStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TelemetryEnrichmentStatus } from "@ubx/sdk-aws/aws/data/observabilityadmin/telemetry_enrichment_status";

    export default stack("example", () => {
      intent({
        summary: "look up aws_observabilityadmin_telemetry_enrichment_status",
      });

      data(TelemetryEnrichmentStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.observabilityadmin import TelemetryEnrichmentStatus, TelemetryEnrichmentStatusConfig

    def describe():
        ubx.intent("look up aws_observabilityadmin_telemetry_enrichment_status")

        ubx.data(TelemetryEnrichmentStatus, "example", TelemetryEnrichmentStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="aws_resource_explorer_managed_view_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
