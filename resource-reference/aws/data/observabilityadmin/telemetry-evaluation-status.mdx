---
title: "aws_observabilityadmin_telemetry_evaluation_status"
description: "`aws_observabilityadmin_telemetry_evaluation_status` reads the real, current status of an Observability Admin telemetry coverage evaluation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observabilityadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/observabilityadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_observabilityadmin_telemetry_evaluation_status"})

    		ubx.Data(observabilityadmin.TelemetryEvaluationStatus, "example", observabilityadmin.TelemetryEvaluationStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TelemetryEvaluationStatus } from "@ubx/sdk-aws/aws/data/observabilityadmin/telemetry_evaluation_status";

    export default stack("example", () => {
      intent({
        summary: "look up aws_observabilityadmin_telemetry_evaluation_status",
      });

      data(TelemetryEvaluationStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.observabilityadmin import TelemetryEvaluationStatus, TelemetryEvaluationStatusConfig

    def describe():
        ubx.intent("look up aws_observabilityadmin_telemetry_evaluation_status")

        ubx.data(TelemetryEvaluationStatus, "example", TelemetryEvaluationStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="failure_reason" type="string">
  The real, human-readable reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="home_region" type="string">
  The real AWS region this object's own home Region is set to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="region_statuses" type="list(object)">
  The real, current statuses of the regions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
