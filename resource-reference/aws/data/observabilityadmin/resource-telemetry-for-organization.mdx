---
title: "aws_observabilityadmin_resource_telemetry_for_organization"
description: "`aws_observabilityadmin_resource_telemetry_for_organization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_observabilityadmin_resource_telemetry_for_organization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observabilityadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/observabilityadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_observabilityadmin_resource_telemetry_for_organization"})

    		ubx.Data(observabilityadmin.ResourceTelemetryForOrganization, "example", observabilityadmin.ResourceTelemetryForOrganizationConfig{
    			AccountIdentifiers:          []string{"example"},
    			MaxResults:                  1,
    			ResourceIdentifierPrefix:    "example",
    			ResourceTags:                map[string]string{"managed-by": "ubx"},
    			ResourceTypes:               []string{"example"},
    			TelemetryConfigurationState: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceTelemetryForOrganization } from "@ubx/sdk-aws/aws/data/observabilityadmin/resource_telemetry_for_organization";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_observabilityadmin_resource_telemetry_for_organization",
      });

      data(ResourceTelemetryForOrganization, "example", {
        accountIdentifiers: ["example"],
        maxResults: 1,
        resourceIdentifierPrefix: "example",
        resourceTags: { "managed-by": "ubx" },
        resourceTypes: ["example"],
        telemetryConfigurationState: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.observabilityadmin import ResourceTelemetryForOrganization, ResourceTelemetryForOrganizationConfig

    def describe():
        ubx.intent("look up aws_observabilityadmin_resource_telemetry_for_organization")

        ubx.data(ResourceTelemetryForOrganization, "example", ResourceTelemetryForOrganizationConfig(
            account_identifiers=["example"],
            max_results=1,
            resource_identifier_prefix="example",
            resource_tags={"managed-by": "ubx"},
            resource_types=["example"],
            telemetry_configuration_state={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_identifiers" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier_prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_tags" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="telemetry_configuration_state" type="map(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="telemetry_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>
