---
title: "aws_migrationhub_strategy_analyzable_servers"
description: "`aws_migrationhub_strategy_analyzable_servers` reads the real, discovered servers eligible for Migration Hub Strategy Recommendations analysis."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	migrationhub_strategy "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/migrationhub_strategy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_migrationhub_strategy_analyzable_servers"})

    		ubx.Data(migrationhub_strategy.MigrationhubStrategyAnalyzableServers, "example", migrationhub_strategy.MigrationhubStrategyAnalyzableServersConfig{
    			MaxResults: 1,
    			Sort:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MigrationhubStrategyAnalyzableServers } from "@ubx/sdk-aws/aws/data/migrationhub_strategy/migrationhub_strategy_analyzable_servers";

    export default stack("example", () => {
      intent({ summary: "look up aws_migrationhub_strategy_analyzable_servers" });

      data(MigrationhubStrategyAnalyzableServers, "example", {
        maxResults: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.migrationhub_strategy import MigrationhubStrategyAnalyzableServers, MigrationhubStrategyAnalyzableServersConfig

    def describe():
        ubx.intent("look up aws_migrationhub_strategy_analyzable_servers")

        ubx.data(MigrationhubStrategyAnalyzableServers, "example", MigrationhubStrategyAnalyzableServersConfig(
            max_results=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analyzable_servers" type="list(object)">
  The real servers eligible for analysis. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
