---
title: "aws_migrationhub_strategy_server_strategies"
description: "`aws_migrationhub_strategy_server_strategies` reads real, recommended migration strategies for a discovered server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	migrationhub_strategy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/migrationhub_strategy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_migrationhub_strategy_server_strategies"})

    		ubx.Data(migrationhub_strategy.MigrationhubStrategyServerStrategies, "example", migrationhub_strategy.MigrationhubStrategyServerStrategiesConfig{
    			ServerId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MigrationhubStrategyServerStrategies } from "./local-sdk/aws/sdk/typescript/aws/data/migrationhub_strategy/migrationhub_strategy_server_strategies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_migrationhub_strategy_server_strategies" });

      data(MigrationhubStrategyServerStrategies, "example", {
        serverId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.migrationhub_strategy import MigrationhubStrategyServerStrategies, MigrationhubStrategyServerStrategiesConfig

    def describe():
        ubx.intent("look up aws_migrationhub_strategy_server_strategies")

        ubx.data(MigrationhubStrategyServerStrategies, "example", MigrationhubStrategyServerStrategiesConfig(
            server_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_id" type="string" required>
  The real, unique identifier of the server this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="server_strategies" type="list(object)">
  The real, recommended migration strategies for these servers. **(AI-inferred)** Returned by the lookup.
</ResponseField>
