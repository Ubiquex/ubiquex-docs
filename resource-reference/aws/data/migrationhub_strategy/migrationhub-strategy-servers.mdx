---
title: "aws_migrationhub_strategy_servers"
description: "`aws_migrationhub_strategy_servers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_migrationhub_strategy_servers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	migrationhub_strategy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/migrationhub_strategy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_migrationhub_strategy_servers"})

    		ubx.Data(migrationhub_strategy.MigrationhubStrategyServers, "example", migrationhub_strategy.MigrationhubStrategyServersConfig{
    			FilterValue:    "example",
    			GroupIdFilter:  []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			ServerCriteria: "example",
    			Sort:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MigrationhubStrategyServers } from "@ubx/sdk-aws/aws/data/migrationhub_strategy/migrationhub_strategy_servers";

    export default stack("example", () => {
      intent({ summary: "look up aws_migrationhub_strategy_servers" });

      data(MigrationhubStrategyServers, "example", {
        filterValue: "example",
        groupIdFilter: [{ name: "example-name" }],
        maxResults: 1,
        serverCriteria: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.migrationhub_strategy import MigrationhubStrategyServers, MigrationhubStrategyServersConfig

    def describe():
        ubx.intent("look up aws_migrationhub_strategy_servers")

        ubx.data(MigrationhubStrategyServers, "example", MigrationhubStrategyServersConfig(
            filter_value="example",
            group_id_filter=[{"name": "example-name"}],
            max_results=1,
            server_criteria="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_value" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_id_filter" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="server_criteria" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="server_infos" type="list(object)">
  Returned by the lookup.
</ResponseField>
