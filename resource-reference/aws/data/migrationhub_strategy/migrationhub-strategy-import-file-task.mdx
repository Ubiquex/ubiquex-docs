---
title: "aws_migrationhub_strategy_import_file_task"
description: "`aws_migrationhub_strategy_import_file_task` reads the real, current status of importing a server inventory file into Migration Hub Strategy..."
---

`aws_migrationhub_strategy_import_file_task` reads the real, current status of importing a server inventory file into Migration Hub Strategy Recommendations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	migrationhub_strategy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/migrationhub_strategy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_migrationhub_strategy_import_file_task"})

    		ubx.Data(migrationhub_strategy.MigrationhubStrategyImportFileTask, "example", migrationhub_strategy.MigrationhubStrategyImportFileTaskConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MigrationhubStrategyImportFileTask } from "@ubx/sdk-aws/aws/data/migrationhub_strategy/migrationhub_strategy_import_file_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_migrationhub_strategy_import_file_task" });

      data(MigrationhubStrategyImportFileTask, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.migrationhub_strategy import MigrationhubStrategyImportFileTask, MigrationhubStrategyImportFileTaskConfig

    def describe():
        ubx.intent("look up aws_migrationhub_strategy_import_file_task")

        ubx.data(MigrationhubStrategyImportFileTask, "example", MigrationhubStrategyImportFileTaskConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="completion_time" type="string">
  The real timestamp of the completion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_name" type="string">
  The real name of the import this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="input_s3_bucket" type="string">
  The real S3 bucket used for the input s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="input_s3_key" type="string">
  The real, unique key of the input s3 this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_records_failed" type="number">
  The real number of records that failed to process. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_records_success" type="number">
  The real number of records that processed successfully. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_report_s3_bucket" type="string">
  The real S3 bucket used for the status report s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_report_s3_key" type="string">
  The real, unique key of the status report s3 this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
