---
title: "aws_greengrassv2_service_role_for_account"
description: "`aws_greengrassv2_service_role_for_account` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrassv2_service_role_for_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_service_role_for_account"})

    		ubx.Data(greengrassv2.ServiceRoleForAccount, "example", greengrassv2.ServiceRoleForAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceRoleForAccount } from "@ubx/sdk-aws/aws/data/greengrassv2/service_role_for_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_service_role_for_account" });

      data(ServiceRoleForAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import ServiceRoleForAccount, ServiceRoleForAccountConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_service_role_for_account")

        ubx.data(ServiceRoleForAccount, "example", ServiceRoleForAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="associated_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Returned by the lookup.
</ResponseField>
