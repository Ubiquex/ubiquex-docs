---
title: "aws_greengrassv2_components"
description: "`data_aws_greengrassv2_components` looks up a real, already-existing `aws_greengrass_v2_component_version`."
---

It is a real, versioned software module, AWS-provided or a caller's own, deployed to AWS IoT Greengrass V2 core devices through an `aws_greengrass_v2_deployment`, V2's software-component model that replaced V1's separate function, connector, and resource definitions with one unified concept.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_components"})

    		ubx.Data(greengrassv2.Components, "example", greengrassv2.ComponentsConfig{
    			MaxResults: 1,
    			Scope:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Components } from "@ubx/sdk-aws/aws/data/greengrassv2/components";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_components" });

      data(Components, "example", {
        maxResults: 1,
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import Components, ComponentsConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_components")

        ubx.data(Components, "example", ComponentsConfig(
            max_results=1,
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string">
  The real scope this lookup is applied at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="components" type="list(object)">
  The real components included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
