---
title: "aws_greengrassv2_core_device"
description: "`aws_greengrassv2_core_device` looks up one real Greengrass V2 core device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_core_device"})

    		ubx.Data(greengrassv2.CoreDevice, "example", greengrassv2.CoreDeviceConfig{
    			CoreDeviceThingName: "example-core-device-thing-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CoreDevice } from "./local-sdk/aws/sdk/typescript/aws/data/greengrassv2/core_device.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_core_device" });

      data(CoreDevice, "example", {
        coreDeviceThingName: "example-core-device-thing-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import CoreDevice, CoreDeviceConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_core_device")

        ubx.data(CoreDevice, "example", CoreDeviceConfig(
            core_device_thing_name="example-core-device-thing-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_device_thing_name" type="string" required>
  The real name of the AWS IoT Greengrass core device's own thing this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="architecture" type="string">
  The real CPU architecture of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="core_version" type="string">
  The real version of the core. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_status_update_timestamp" type="string">
  The real timestamp of the last status update. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platform" type="string">
  The real platform this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="runtime" type="string">
  The real runtime this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
