---
title: "aws_greengrassv2_deployments"
description: "`data_aws_greengrassv2_deployments` looks up a real, already-existing `aws_greengrass_v2_deployment`."
---

It pushes a real, defined set of `aws_greengrass_v2_component_version`s to a target group of Greengrass V2 core devices, with rollout and rollback configuration, the actual mechanism that gets software running on real edge devices.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_deployments"})

    		ubx.Data(greengrassv2.Deployments, "example", greengrassv2.DeploymentsConfig{
    			HistoryFilter:   "example",
    			MaxResults:      1,
    			ParentTargetArn: "arn:aws:iam::123456789012:policy/example",
    			TargetArn:       "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Deployments } from "./local-sdk/aws/sdk/typescript/aws/data/greengrassv2/deployments.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_deployments" });

      data(Deployments, "example", {
        historyFilter: "example",
        maxResults: 1,
        parentTargetArn: "arn:aws:iam::123456789012:policy/example",
        targetArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import Deployments, DeploymentsConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_deployments")

        ubx.data(Deployments, "example", DeploymentsConfig(
            history_filter="example",
            max_results=1,
            parent_target_arn="arn:aws:iam::123456789012:policy/example",
            target_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="history_filter" type="string">
  The real filter applied to this history. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parent_target_arn" type="string">
  The real Amazon Resource Name (ARN) of the parent target this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_arn" type="string">
  The real Amazon Resource Name (ARN) of the target this action applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployments" type="list(object)">
  The real deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
