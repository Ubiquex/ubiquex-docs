---
title: "aws_greengrassv2_core_devices"
description: "`aws_greengrassv2_core_devices` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrassv2_core_devices` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_core_devices"})

    		ubx.Data(greengrassv2.CoreDevices, "example", greengrassv2.CoreDevicesConfig{
    			MaxResults:    1,
    			Runtime:       "example",
    			Status:        "example",
    			ThingGroupArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoreDevices } from "@ubx/sdk-aws/aws/data/greengrassv2/core_devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_core_devices" });

      data(CoreDevices, "example", {
        maxResults: 1,
        runtime: "example",
        status: "example",
        thingGroupArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import CoreDevices, CoreDevicesConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_core_devices")

        ubx.data(CoreDevices, "example", CoreDevicesConfig(
            max_results=1,
            runtime="example",
            status="example",
            thing_group_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="runtime" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_group_arn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_devices" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
