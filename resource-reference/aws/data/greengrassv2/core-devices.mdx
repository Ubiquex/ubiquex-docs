---
title: "aws_greengrassv2_core_devices"
description: "`aws_greengrassv2_core_devices` reads the real Greengrass V2 core devices on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_core_devices"})

    		ubx.Data(greengrassv2.CoreDevices, "example", greengrassv2.CoreDevicesConfig{
    			MaxResults:    1,
    			Runtime:       "example",
    			Status:        "example",
    			ThingGroupArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CoreDevices } from "@ubx/sdk-aws/aws/data/greengrassv2/core_devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_core_devices" });

      data(CoreDevices, "example", {
        maxResults: 1,
        runtime: "example",
        status: "example",
        thingGroupArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import CoreDevices, CoreDevicesConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_core_devices")

        ubx.data(CoreDevices, "example", CoreDevicesConfig(
            max_results=1,
            runtime="example",
            status="example",
            thing_group_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="runtime" type="string">
  The real runtime this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="thing_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the thing group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="core_devices" type="list(object)">
  The real AWS IoT Greengrass core devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
