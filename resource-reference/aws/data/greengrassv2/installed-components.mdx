---
title: "aws_greengrassv2_installed_components"
description: "`aws_greengrassv2_installed_components` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrassv2_installed_components` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_installed_components"})

    		ubx.Data(greengrassv2.InstalledComponents, "example", greengrassv2.InstalledComponentsConfig{
    			CoreDeviceThingName: "example-core-device-thing-name",
    			MaxResults:          1,
    			TopologyFilter:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstalledComponents } from "@ubx/sdk-aws/aws/data/greengrassv2/installed_components";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_installed_components" });

      data(InstalledComponents, "example", {
        coreDeviceThingName: "example-core-device-thing-name",
        maxResults: 1,
        topologyFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import InstalledComponents, InstalledComponentsConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_installed_components")

        ubx.data(InstalledComponents, "example", InstalledComponentsConfig(
            core_device_thing_name="example-core-device-thing-name",
            max_results=1,
            topology_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="core_device_thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="topology_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="installed_components" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
