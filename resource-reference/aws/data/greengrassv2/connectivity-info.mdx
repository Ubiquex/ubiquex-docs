---
title: "aws_greengrassv2_connectivity_info"
description: "`aws_greengrassv2_connectivity_info` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrassv2_connectivity_info` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrassv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrassv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrassv2_connectivity_info"})

    		ubx.Data(greengrassv2.ConnectivityInfo, "example", greengrassv2.ConnectivityInfoConfig{
    			ThingName: "example-thing-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectivityInfo } from "@ubx/sdk-aws/aws/data/greengrassv2/connectivity_info";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrassv2_connectivity_info" });

      data(ConnectivityInfo, "example", {
        thingName: "example-thing-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrassv2 import ConnectivityInfo, ConnectivityInfoConfig

    def describe():
        ubx.intent("look up aws_greengrassv2_connectivity_info")

        ubx.data(ConnectivityInfo, "example", ConnectivityInfoConfig(
            thing_name="example-thing-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connectivity_info" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  Returned by the lookup.
</ResponseField>
