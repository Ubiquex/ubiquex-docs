---
title: "aws_textract_lending_analysis_summary"
description: "`aws_textract_lending_analysis_summary` reads a real, summarized result of an asynchronous Textract lending document analysis job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	textract "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/textract"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_textract_lending_analysis_summary"})

    		ubx.Data(textract.LendingAnalysisSummary, "example", textract.LendingAnalysisSummaryConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LendingAnalysisSummary } from "./local-sdk/aws/sdk/typescript/aws/data/textract/lending_analysis_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_textract_lending_analysis_summary" });

      data(LendingAnalysisSummary, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.textract import LendingAnalysisSummary, LendingAnalysisSummaryConfig

    def describe():
        ubx.intent("look up aws_textract_lending_analysis_summary")

        ubx.data(LendingAnalysisSummary, "example", LendingAnalysisSummaryConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analyze_lending_model_version" type="string">
  The real version of the analyze lending model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document_metadata" type="object">
  &lt;p&gt;Information about the input document.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pages" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary" type="object">
  &lt;p&gt;Contains information regarding DocumentGroups and UndetectedDocumentTypes.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="document_groups" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="undetected_document_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  The real warnings encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
