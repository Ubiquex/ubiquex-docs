---
title: "aws_textract_document_text_detection"
description: "`aws_textract_document_text_detection` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_textract_document_text_detection` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	textract "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/textract"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_textract_document_text_detection"})

    		ubx.Data(textract.DocumentTextDetection, "example", textract.DocumentTextDetectionConfig{
    			JobId:      "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentTextDetection } from "@ubx/sdk-aws/aws/data/textract/document_text_detection";

    export default stack("example", () => {
      intent({ summary: "look up aws_textract_document_text_detection" });

      data(DocumentTextDetection, "example", {
        jobId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.textract import DocumentTextDetection, DocumentTextDetectionConfig

    def describe():
        ubx.intent("look up aws_textract_document_text_detection")

        ubx.data(DocumentTextDetection, "example", DocumentTextDetectionConfig(
            job_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blocks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="detect_document_text_model_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="document_metadata" type="object">
  &lt;p&gt;Information about the input document.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pages" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  Returned by the lookup.
</ResponseField>
