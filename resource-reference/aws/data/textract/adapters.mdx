---
title: "aws_textract_adapters"
description: "`aws_textract_adapters` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_textract_adapters` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	textract "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/textract"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_textract_adapters"})

    		ubx.Data(textract.Adapters, "example", textract.AdaptersConfig{
    			AfterCreationTime:  "example",
    			BeforeCreationTime: "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Adapters } from "@ubx/sdk-aws/aws/data/textract/adapters";

    export default stack("example", () => {
      intent({ summary: "look up aws_textract_adapters" });

      data(Adapters, "example", {
        afterCreationTime: "example",
        beforeCreationTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.textract import Adapters, AdaptersConfig

    def describe():
        ubx.intent("look up aws_textract_adapters")

        ubx.data(Adapters, "example", AdaptersConfig(
            after_creation_time="example",
            before_creation_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_creation_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before_creation_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="adapters" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
