---
title: "aws_textract_adapter_versions"
description: "`aws_textract_adapter_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_textract_adapter_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	textract "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/textract"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_textract_adapter_versions"})

    		ubx.Data(textract.AdapterVersions, "example", textract.AdapterVersionsConfig{
    			AdapterId:          "example",
    			AfterCreationTime:  "example",
    			BeforeCreationTime: "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdapterVersions } from "@ubx/sdk-aws/aws/data/textract/adapter_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_textract_adapter_versions" });

      data(AdapterVersions, "example", {
        adapterId: "example",
        afterCreationTime: "example",
        beforeCreationTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.textract import AdapterVersions, AdapterVersionsConfig

    def describe():
        ubx.intent("look up aws_textract_adapter_versions")

        ubx.data(AdapterVersions, "example", AdapterVersionsConfig(
            adapter_id="example",
            after_creation_time="example",
            before_creation_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="adapter_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="after_creation_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before_creation_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="adapter_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
