---
title: "aws_textract_adapter_versions"
description: "`data_aws_textract_adapter_versions` looks up a real, already-existing `aws_textract_adapter`."
---

It creates a real, custom adapter that plugs into Amazon Textract's pretrained Queries model, trained on an organization's own annotated sample documents to improve extraction accuracy on business-specific document types beyond what the generic pretrained model recognizes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	textract "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/textract"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_textract_adapter_versions"})

    		ubx.Data(textract.AdapterVersions, "example", textract.AdapterVersionsConfig{
    			AdapterId:          "example",
    			AfterCreationTime:  "example",
    			BeforeCreationTime: "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdapterVersions } from "@ubx/sdk-aws/aws/data/textract/adapter_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_textract_adapter_versions" });

      data(AdapterVersions, "example", {
        adapterId: "example",
        afterCreationTime: "example",
        beforeCreationTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.textract import AdapterVersions, AdapterVersionsConfig

    def describe():
        ubx.intent("look up aws_textract_adapter_versions")

        ubx.data(AdapterVersions, "example", AdapterVersionsConfig(
            adapter_id="example",
            after_creation_time="example",
            before_creation_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="adapter_id" type="string">
  The real, unique identifier of the adapter this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="after_creation_time" type="string">
  The real timestamp of the after creation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before_creation_time" type="string">
  The real timestamp of the before creation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="adapter_versions" type="list(object)">
  The real versions of the adapter included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
