---
title: "aws_freetier_account_plan_state"
description: "`aws_freetier_account_plan_state` reads the real, current Free Tier plan state (Free or Paid) of an account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	freetier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/freetier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_freetier_account_plan_state"})

    		ubx.Data(freetier.AccountPlanState, "example", freetier.AccountPlanStateConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountPlanState } from "./local-sdk/aws/sdk/typescript/aws/data/freetier/account_plan_state.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_freetier_account_plan_state" });

      data(AccountPlanState, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.freetier import AccountPlanState, AccountPlanStateConfig

    def describe():
        ubx.intent("look up aws_freetier_account_plan_state")

        ubx.data(AccountPlanState, "example", AccountPlanStateConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="account_plan_expiration_date" type="string">
  The real date of the account plan expiration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="account_plan_remaining_credits" type="object">
  &lt;p&gt; The monetary amount of the credit. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="amount" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="unit" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="account_plan_status" type="string">
  The real, current status of the account plan this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="account_plan_type" type="string">
  The real, discriminating type of the account plan this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
