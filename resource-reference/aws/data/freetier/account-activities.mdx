---
title: "aws_freetier_account_activities"
description: "`aws_freetier_account_activities` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_freetier_account_activities` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	freetier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/freetier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_freetier_account_activities"})

    		ubx.Data(freetier.AccountActivities, "example", freetier.AccountActivitiesConfig{
    			FilterActivityStatuses: []string{"example"},
    			LanguageCode:           "example",
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountActivities } from "@ubx/sdk-aws/aws/data/freetier/account_activities";

    export default stack("example", () => {
      intent({ summary: "look up aws_freetier_account_activities" });

      data(AccountActivities, "example", {
        filterActivityStatuses: ["example"],
        languageCode: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.freetier import AccountActivities, AccountActivitiesConfig

    def describe():
        ubx.intent("look up aws_freetier_account_activities")

        ubx.data(AccountActivities, "example", AccountActivitiesConfig(
            filter_activity_statuses=["example"],
            language_code="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_activity_statuses" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="language_code" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activities" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
