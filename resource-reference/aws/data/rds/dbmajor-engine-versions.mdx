---
title: "aws_rds_dbmajor_engine_versions"
description: "`aws_rds_dbmajor_engine_versions` reads the real, major database engine versions available for RDS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbmajor_engine_versions"})

    		ubx.Data(rds.DbmajorEngineVersions, "example", rds.DbmajorEngineVersionsConfig{
    			Engine:             "example",
    			MajorEngineVersion: "example",
    			MaxRecords:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DbmajorEngineVersions } from "./local-sdk/aws/sdk/typescript/aws/data/rds/dbmajor_engine_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbmajor_engine_versions" });

      data(DbmajorEngineVersions, "example", {
        engine: "example",
        majorEngineVersion: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.rds import DbmajorEngineVersions, DbmajorEngineVersionsConfig

    def describe():
        ubx.intent("look up aws_rds_dbmajor_engine_versions")

        ubx.data(DbmajorEngineVersions, "example", DbmajorEngineVersionsConfig(
            engine="example",
            major_engine_version="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine" type="string">
  The real database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="major_engine_version" type="string">
  The real version of the major engine. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbmajor_engine_versions" type="list(object)">
  The real versions of the dbmajor engine included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
