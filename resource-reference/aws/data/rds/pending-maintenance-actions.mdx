---
title: "aws_rds_pending_maintenance_actions"
description: "`aws_rds_pending_maintenance_actions` reads the real, pending maintenance actions for an RDS resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_pending_maintenance_actions"})

    		ubx.Data(rds.PendingMaintenanceActions, "example", rds.PendingMaintenanceActionsConfig{
    			Filters:            []map[string]any{{"name": "example-name"}},
    			MaxRecords:         1,
    			ResourceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PendingMaintenanceActions } from "@ubx/sdk-aws/aws/data/rds/pending_maintenance_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_pending_maintenance_actions" });

      data(PendingMaintenanceActions, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        resourceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import PendingMaintenanceActions, PendingMaintenanceActionsConfig
    from ubx.aws.data.rds.pending_maintenance_actions import PendingMaintenanceActions_Filters

    def describe():
        ubx.intent("look up aws_rds_pending_maintenance_actions")

        ubx.data(PendingMaintenanceActions, "example", PendingMaintenanceActionsConfig(
            filters=[PendingMaintenanceActions_Filters(name="example-name")],
            max_records=1,
            resource_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_identifier" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pending_maintenance_actions" type="list(object)">
  The real, pending maintenance actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
