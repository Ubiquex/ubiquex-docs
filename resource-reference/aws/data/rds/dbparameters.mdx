---
title: "aws_rds_dbparameters"
description: "`data_aws_rds_dbparameters` looks up a real, already-existing `aws_rds_dbparameter_group`."
---

It defines a real set of instance-level engine parameters that can be applied to an `aws_rds_dbinstance` instead of hand-tuning the instance individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbparameters"})

    		ubx.Data(rds.Dbparameters, "example", rds.DbparametersConfig{
    			DbparameterGroupName: "example-dbparameter-group-name",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxRecords:           1,
    			Source:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Dbparameters } from "@ubx/sdk-aws/aws/data/rds/dbparameters";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbparameters" });

      data(Dbparameters, "example", {
        dbparameterGroupName: "example-dbparameter-group-name",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import Dbparameters, DbparametersConfig

    def describe():
        ubx.intent("look up aws_rds_dbparameters")

        ubx.data(Dbparameters, "example", DbparametersConfig(
            dbparameter_group_name="example-dbparameter-group-name",
            filters=[{"name": "example-name"}],
            max_records=1,
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbparameter_group_name" type="string" required>
  The real name of the dbparameter group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string">
  The real source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
