---
title: "aws_rds_dbsnapshots"
description: "`aws_rds_dbsnapshots` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_dbsnapshots` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbsnapshots"})

    		ubx.Data(rds.Dbsnapshots, "example", rds.DbsnapshotsConfig{
    			DbiResourceId:        "example",
    			DbinstanceIdentifier: "example",
    			DbsnapshotIdentifier: "example",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			IncludePublic:        true,
    			IncludeShared:        true,
    			MaxRecords:           1,
    			SnapshotType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Dbsnapshots } from "@ubx/sdk-aws/aws/data/rds/dbsnapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbsnapshots" });

      data(Dbsnapshots, "example", {
        dbiResourceId: "example",
        dbinstanceIdentifier: "example",
        dbsnapshotIdentifier: "example",
        filters: [{ name: "example-name" }],
        includePublic: true,
        includeShared: true,
        maxRecords: 1,
        snapshotType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import Dbsnapshots, DbsnapshotsConfig

    def describe():
        ubx.intent("look up aws_rds_dbsnapshots")

        ubx.data(Dbsnapshots, "example", DbsnapshotsConfig(
            dbi_resource_id="example",
            dbinstance_identifier="example",
            dbsnapshot_identifier="example",
            filters=[{"name": "example-name"}],
            include_public=True,
            include_shared=True,
            max_records=1,
            snapshot_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbi_resource_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbinstance_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbsnapshot_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_public" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_shared" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbsnapshots" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
