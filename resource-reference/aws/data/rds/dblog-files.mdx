---
title: "aws_rds_dblog_files"
description: "`aws_rds_dblog_files` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_dblog_files` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dblog_files"})

    		ubx.Data(rds.DblogFiles, "example", rds.DblogFilesConfig{
    			DbinstanceIdentifier: "example",
    			FileLastWritten:      1,
    			FileSize:             1,
    			FilenameContains:     "example",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxRecords:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DblogFiles } from "@ubx/sdk-aws/aws/data/rds/dblog_files";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dblog_files" });

      data(DblogFiles, "example", {
        dbinstanceIdentifier: "example",
        fileLastWritten: 1,
        fileSize: 1,
        filenameContains: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import DblogFiles, DblogFilesConfig

    def describe():
        ubx.intent("look up aws_rds_dblog_files")

        ubx.data(DblogFiles, "example", DblogFilesConfig(
            dbinstance_identifier="example",
            file_last_written=1,
            file_size=1,
            filename_contains="example",
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="file_last_written" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="file_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filename_contains" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="describe_dblog_files" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
