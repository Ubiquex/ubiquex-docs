---
title: "aws_rds_account_attributes"
description: "`aws_rds_account_attributes` reads real, account-level RDS attributes and quotas."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_account_attributes"})

    		ubx.Data(rds.AccountAttributes, "example", rds.AccountAttributesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAttributes } from "@ubx/sdk-aws/aws/data/rds/account_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_account_attributes" });

      data(AccountAttributes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_rds_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_quotas" type="list(object)">
  The real, current account-level quotas for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>
