---
title: "aws_rds_dbproxies"
description: "`data_aws_rds_dbproxies` looks up a real, already-existing `aws_rds_dbproxy`."
---

It creates a real, fully managed connection pooler in front of one or more `aws_rds_dbinstance`s or an `aws_rds_dbcluster`, absorbing bursts of application connections without exhausting the database's own native connection limit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbproxies"})

    		ubx.Data(rds.Dbproxies, "example", rds.DbproxiesConfig{
    			DbproxyName: "example-dbproxy-name",
    			Filters:     []map[string]any{{"name": "example-name"}},
    			MaxRecords:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Dbproxies } from "./local-sdk/aws/sdk/typescript/aws/data/rds/dbproxies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbproxies" });

      data(Dbproxies, "example", {
        dbproxyName: "example-dbproxy-name",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.rds import Dbproxies, DbproxiesConfig

    def describe():
        ubx.intent("look up aws_rds_dbproxies")

        ubx.data(Dbproxies, "example", DbproxiesConfig(
            dbproxy_name="example-dbproxy-name",
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbproxy_name" type="string">
  The real name of the dbproxy this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbproxies" type="list(object)">
  The real RDS Proxy endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
