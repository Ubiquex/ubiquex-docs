---
title: "aws_rds_reserved_dbinstances_offerings"
description: "`aws_rds_reserved_dbinstances_offerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_reserved_dbinstances_offerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_reserved_dbinstances_offerings"})

    		ubx.Data(rds.ReservedDbinstancesOfferings, "example", rds.ReservedDbinstancesOfferingsConfig{
    			DbinstanceClass:               "example",
    			Duration:                      "example",
    			Filters:                       []map[string]any{{"name": "example-name"}},
    			MaxRecords:                    1,
    			MultiAz:                       true,
    			OfferingType:                  "example",
    			ProductDescription:            "example",
    			ReservedDbinstancesOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedDbinstancesOfferings } from "@ubx/sdk-aws/aws/data/rds/reserved_dbinstances_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_reserved_dbinstances_offerings" });

      data(ReservedDbinstancesOfferings, "example", {
        dbinstanceClass: "example",
        duration: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        multiAz: true,
        offeringType: "example",
        productDescription: "example",
        reservedDbinstancesOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import ReservedDbinstancesOfferings, ReservedDbinstancesOfferingsConfig

    def describe():
        ubx.intent("look up aws_rds_reserved_dbinstances_offerings")

        ubx.data(ReservedDbinstancesOfferings, "example", ReservedDbinstancesOfferingsConfig(
            dbinstance_class="example",
            duration="example",
            filters=[{"name": "example-name"}],
            max_records=1,
            multi_az=True,
            offering_type="example",
            product_description="example",
            reserved_dbinstances_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_class" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="duration" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="multi_az" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product_description" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_dbinstances_offering_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_dbinstances_offerings" type="list(object)">
  Returned by the lookup.
</ResponseField>
