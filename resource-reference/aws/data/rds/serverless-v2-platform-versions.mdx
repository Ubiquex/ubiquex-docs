---
title: "aws_rds_serverless_v2_platform_versions"
description: "`aws_rds_serverless_v2_platform_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_serverless_v2_platform_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_serverless_v2_platform_versions"})

    		ubx.Data(rds.ServerlessV2PlatformVersions, "example", rds.ServerlessV2PlatformVersionsConfig{
    			DefaultOnly:                 true,
    			Engine:                      "example",
    			Filters:                     []map[string]any{{"name": "example-name"}},
    			IncludeAll:                  true,
    			MaxRecords:                  1,
    			ServerlessV2PlatformVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServerlessV2PlatformVersions } from "@ubx/sdk-aws/aws/data/rds/serverless_v2_platform_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_serverless_v2_platform_versions" });

      data(ServerlessV2PlatformVersions, "example", {
        defaultOnly: true,
        engine: "example",
        filters: [{ name: "example-name" }],
        includeAll: true,
        maxRecords: 1,
        serverlessV2PlatformVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import ServerlessV2PlatformVersions, ServerlessV2PlatformVersionsConfig

    def describe():
        ubx.intent("look up aws_rds_serverless_v2_platform_versions")

        ubx.data(ServerlessV2PlatformVersions, "example", ServerlessV2PlatformVersionsConfig(
            default_only=True,
            engine="example",
            filters=[{"name": "example-name"}],
            include_all=True,
            max_records=1,
            serverless_v2_platform_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_only" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_all" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="serverless_v2_platform_version" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="serverless_v2_platform_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
