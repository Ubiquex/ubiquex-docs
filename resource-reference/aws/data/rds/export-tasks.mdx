---
title: "aws_rds_export_tasks"
description: "`aws_rds_export_tasks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_export_tasks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_export_tasks"})

    		ubx.Data(rds.ExportTasks, "example", rds.ExportTasksConfig{
    			ExportTaskIdentifier: "example",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxRecords:           1,
    			SourceArn:            "arn:aws:iam::123456789012:policy/example",
    			SourceType:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExportTasks } from "@ubx/sdk-aws/aws/data/rds/export_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_export_tasks" });

      data(ExportTasks, "example", {
        exportTaskIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        sourceArn: "arn:aws:iam::123456789012:policy/example",
        sourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import ExportTasks, ExportTasksConfig

    def describe():
        ubx.intent("look up aws_rds_export_tasks")

        ubx.data(ExportTasks, "example", ExportTasksConfig(
            export_task_identifier="example",
            filters=[{"name": "example-name"}],
            max_records=1,
            source_arn="arn:aws:iam::123456789012:policy/example",
            source_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_task_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="source_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_tasks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
