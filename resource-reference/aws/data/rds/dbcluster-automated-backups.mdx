---
title: "aws_rds_dbcluster_automated_backups"
description: "`aws_rds_dbcluster_automated_backups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_dbcluster_automated_backups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbcluster_automated_backups"})

    		ubx.Data(rds.DbclusterAutomatedBackups, "example", rds.DbclusterAutomatedBackupsConfig{
    			DbClusterResourceId: "example",
    			DbclusterIdentifier: "example",
    			Filters:             []map[string]any{{"name": "example-name"}},
    			MaxRecords:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbclusterAutomatedBackups } from "@ubx/sdk-aws/aws/data/rds/dbcluster_automated_backups";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbcluster_automated_backups" });

      data(DbclusterAutomatedBackups, "example", {
        dbClusterResourceId: "example",
        dbclusterIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import DbclusterAutomatedBackups, DbclusterAutomatedBackupsConfig

    def describe():
        ubx.intent("look up aws_rds_dbcluster_automated_backups")

        ubx.data(DbclusterAutomatedBackups, "example", DbclusterAutomatedBackupsConfig(
            db_cluster_resource_id="example",
            dbcluster_identifier="example",
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="db_cluster_resource_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbcluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbcluster_automated_backups" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
