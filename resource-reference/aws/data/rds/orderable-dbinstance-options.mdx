---
title: "aws_rds_orderable_dbinstance_options"
description: "`aws_rds_orderable_dbinstance_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_orderable_dbinstance_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_orderable_dbinstance_options"})

    		ubx.Data(rds.OrderableDbinstanceOptions, "example", rds.OrderableDbinstanceOptionsConfig{
    			Engine:                "example",
    			AvailabilityZoneGroup: "example",
    			DbinstanceClass:       "example",
    			EngineVersion:         "example",
    			Filters:               []map[string]any{{"name": "example-name"}},
    			LicenseModel:          "example",
    			MaxRecords:            1,
    			Vpc:                   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrderableDbinstanceOptions } from "@ubx/sdk-aws/aws/data/rds/orderable_dbinstance_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_orderable_dbinstance_options" });

      data(OrderableDbinstanceOptions, "example", {
        engine: "example",
        availabilityZoneGroup: "example",
        dbinstanceClass: "example",
        engineVersion: "example",
        filters: [{ name: "example-name" }],
        licenseModel: "example",
        maxRecords: 1,
        vpc: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import OrderableDbinstanceOptions, OrderableDbinstanceOptionsConfig

    def describe():
        ubx.intent("look up aws_rds_orderable_dbinstance_options")

        ubx.data(OrderableDbinstanceOptions, "example", OrderableDbinstanceOptionsConfig(
            engine="example",
            availability_zone_group="example",
            dbinstance_class="example",
            engine_version="example",
            filters=[{"name": "example-name"}],
            license_model="example",
            max_records=1,
            vpc=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="availability_zone_group" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbinstance_class" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="license_model" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vpc" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="orderable_dbinstance_options" type="list(object)">
  Returned by the lookup.
</ResponseField>
