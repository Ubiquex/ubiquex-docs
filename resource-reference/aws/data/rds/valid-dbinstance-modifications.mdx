---
title: "aws_rds_valid_dbinstance_modifications"
description: "`aws_rds_valid_dbinstance_modifications` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_valid_dbinstance_modifications` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_valid_dbinstance_modifications"})

    		ubx.Data(rds.ValidDbinstanceModifications, "example", rds.ValidDbinstanceModificationsConfig{
    			DbinstanceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ValidDbinstanceModifications } from "@ubx/sdk-aws/aws/data/rds/valid_dbinstance_modifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_valid_dbinstance_modifications" });

      data(ValidDbinstanceModifications, "example", {
        dbinstanceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import ValidDbinstanceModifications, ValidDbinstanceModificationsConfig

    def describe():
        ubx.intent("look up aws_rds_valid_dbinstance_modifications")

        ubx.data(ValidDbinstanceModifications, "example", ValidDbinstanceModificationsConfig(
            dbinstance_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="valid_dbinstance_modifications_message" type="object">
  &lt;p&gt;Information about valid modifications that you can make to your DB instance. Contains the result of a successful call to the &lt;code&gt;DescribeValidDBInstanceModifications&lt;/code&gt; action. You can use this information when you call &lt;code&gt;ModifyDBInstance&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="additional_storage" type="object">
      &lt;p&gt;Contains the valid options for additional storage volumes for a DB instance. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="supports_additional_storage_volumes" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="volumes" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="storage" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="supports_dedicated_log_volume" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="valid_processor_features" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
