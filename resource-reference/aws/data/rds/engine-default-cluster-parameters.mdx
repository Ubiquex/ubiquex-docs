---
title: "aws_rds_engine_default_cluster_parameters"
description: "`aws_rds_engine_default_cluster_parameters` reads the real, default cluster parameters for an RDS or Aurora DB cluster parameter group family."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_engine_default_cluster_parameters"})

    		ubx.Data(rds.EngineDefaultClusterParameters, "example", rds.EngineDefaultClusterParametersConfig{
    			DbparameterGroupFamily: "example",
    			Filters:                []map[string]any{{"name": "example-name"}},
    			Marker:                 "example",
    			MaxRecords:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EngineDefaultClusterParameters } from "@ubx/sdk-aws/aws/data/rds/engine_default_cluster_parameters";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_engine_default_cluster_parameters" });

      data(EngineDefaultClusterParameters, "example", {
        dbparameterGroupFamily: "example",
        filters: [{ name: "example-name" }],
        marker: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import EngineDefaultClusterParameters, EngineDefaultClusterParametersConfig

    def describe():
        ubx.intent("look up aws_rds_engine_default_cluster_parameters")

        ubx.data(EngineDefaultClusterParameters, "example", EngineDefaultClusterParametersConfig(
            dbparameter_group_family="example",
            filters=[{"name": "example-name"}],
            marker="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbparameter_group_family" type="string" required>
  The real database parameter group family this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="engine_defaults" type="object">
  &lt;p&gt;Contains the result of a successful invocation of the &lt;code&gt;DescribeEngineDefaultParameters&lt;/code&gt; action.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="dbparameter_group_family" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
