---
title: "aws_rds_dbcluster_snapshots"
description: "`aws_rds_dbcluster_snapshots` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_dbcluster_snapshots` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbcluster_snapshots"})

    		ubx.Data(rds.DbclusterSnapshots, "example", rds.DbclusterSnapshotsConfig{
    			DbClusterResourceId:         "example",
    			DbclusterIdentifier:         "example",
    			DbclusterSnapshotIdentifier: "example",
    			Filters:                     []map[string]any{{"name": "example-name"}},
    			IncludePublic:               true,
    			IncludeShared:               true,
    			MaxRecords:                  1,
    			SnapshotType:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbclusterSnapshots } from "@ubx/sdk-aws/aws/data/rds/dbcluster_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbcluster_snapshots" });

      data(DbclusterSnapshots, "example", {
        dbClusterResourceId: "example",
        dbclusterIdentifier: "example",
        dbclusterSnapshotIdentifier: "example",
        filters: [{ name: "example-name" }],
        includePublic: true,
        includeShared: true,
        maxRecords: 1,
        snapshotType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import DbclusterSnapshots, DbclusterSnapshotsConfig

    def describe():
        ubx.intent("look up aws_rds_dbcluster_snapshots")

        ubx.data(DbclusterSnapshots, "example", DbclusterSnapshotsConfig(
            db_cluster_resource_id="example",
            dbcluster_identifier="example",
            dbcluster_snapshot_identifier="example",
            filters=[{"name": "example-name"}],
            include_public=True,
            include_shared=True,
            max_records=1,
            snapshot_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="db_cluster_resource_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbcluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="dbcluster_snapshot_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_public" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_shared" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbcluster_snapshots" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
