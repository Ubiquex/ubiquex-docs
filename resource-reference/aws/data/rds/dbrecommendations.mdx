---
title: "aws_rds_dbrecommendations"
description: "`aws_rds_dbrecommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_dbrecommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbrecommendations"})

    		ubx.Data(rds.Dbrecommendations, "example", rds.DbrecommendationsConfig{
    			Filters:           []map[string]any{{"name": "example-name"}},
    			LastUpdatedAfter:  "example",
    			LastUpdatedBefore: "example",
    			Locale:            "example",
    			MaxRecords:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Dbrecommendations } from "@ubx/sdk-aws/aws/data/rds/dbrecommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbrecommendations" });

      data(Dbrecommendations, "example", {
        filters: [{ name: "example-name" }],
        lastUpdatedAfter: "example",
        lastUpdatedBefore: "example",
        locale: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import Dbrecommendations, DbrecommendationsConfig

    def describe():
        ubx.intent("look up aws_rds_dbrecommendations")

        ubx.data(Dbrecommendations, "example", DbrecommendationsConfig(
            filters=[{"name": "example-name"}],
            last_updated_after="example",
            last_updated_before="example",
            locale="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="last_updated_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="last_updated_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="locale" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbrecommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
