---
title: "aws_rds_dbrecommendations"
description: "`aws_rds_dbrecommendations` reads real, system-generated RDS recommendations for a database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbrecommendations"})

    		ubx.Data(rds.Dbrecommendations, "example", rds.DbrecommendationsConfig{
    			Filters:           []map[string]any{{"name": "example-name"}},
    			LastUpdatedAfter:  "example",
    			LastUpdatedBefore: "example",
    			Locale:            "example",
    			MaxRecords:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Dbrecommendations } from "@ubx/sdk-aws/aws/data/rds/dbrecommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbrecommendations" });

      data(Dbrecommendations, "example", {
        filters: [{ name: "example-name" }],
        lastUpdatedAfter: "example",
        lastUpdatedBefore: "example",
        locale: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import Dbrecommendations, DbrecommendationsConfig

    def describe():
        ubx.intent("look up aws_rds_dbrecommendations")

        ubx.data(Dbrecommendations, "example", DbrecommendationsConfig(
            filters=[{"name": "example-name"}],
            last_updated_after="example",
            last_updated_before="example",
            locale="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="last_updated_after" type="string">
  The earliest real last-updated timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="last_updated_before" type="string">
  The latest real last-updated timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbrecommendations" type="list(object)">
  The real, recommended database configuration changes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
