---
title: "aws_rds_reserved_dbinstances"
description: "`data_aws_rds_reserved_dbinstances` looks up a real, already-existing `aws_rds_dbinstance`."
---

It creates a real managed relational database instance, MySQL, PostgreSQL, MariaDB, Oracle, SQL Server, or an Aurora cluster's own compute, the core resource RDS provisions and patches on the customer's behalf.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_reserved_dbinstances"})

    		ubx.Data(rds.ReservedDbinstances, "example", rds.ReservedDbinstancesConfig{
    			DbinstanceClass:    "example",
    			Duration:           "example",
    			Filters:            []map[string]any{{"name": "example-name"}},
    			LeaseId:            "example",
    			MaxRecords:         1,
    			MultiAz:            true,
    			OfferingType:       "example",
    			ProductDescription: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedDbinstances } from "@ubx/sdk-aws/aws/data/rds/reserved_dbinstances";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_reserved_dbinstances" });

      data(ReservedDbinstances, "example", {
        dbinstanceClass: "example",
        duration: "example",
        filters: [{ name: "example-name" }],
        leaseId: "example",
        maxRecords: 1,
        multiAz: true,
        offeringType: "example",
        productDescription: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import ReservedDbinstances, ReservedDbinstancesConfig

    def describe():
        ubx.intent("look up aws_rds_reserved_dbinstances")

        ubx.data(ReservedDbinstances, "example", ReservedDbinstancesConfig(
            dbinstance_class="example",
            duration="example",
            filters=[{"name": "example-name"}],
            lease_id="example",
            max_records=1,
            multi_az=True,
            offering_type="example",
            product_description="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbinstance_class" type="string">
  The real compute and memory class of the database instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="duration" type="string">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="lease_id" type="string">
  The real, unique identifier of the lease this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="multi_az" type="bool">
  Whether this object is deployed across multiple Availability Zones. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  The real, discriminating type of the offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_description" type="string">
  The real, free-text product description set on this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_dbinstance_id" type="string">
  The real, unique identifier of the reserved dbinstance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_dbinstances_offering_id" type="string">
  The real, unique identifier of the reserved dbinstances offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_dbinstances" type="list(object)">
  The real RDS Reserved DB Instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
