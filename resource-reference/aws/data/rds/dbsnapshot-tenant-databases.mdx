---
title: "aws_rds_dbsnapshot_tenant_databases"
description: "`aws_rds_dbsnapshot_tenant_databases` reads the real tenant databases contained within an RDS for SQL Server multi-tenant DB snapshot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_dbsnapshot_tenant_databases"})

    		ubx.Data(rds.DbsnapshotTenantDatabases, "example", rds.DbsnapshotTenantDatabasesConfig{
    			DbiResourceId:        "example",
    			DbinstanceIdentifier: "example",
    			DbsnapshotIdentifier: "example",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			MaxRecords:           1,
    			SnapshotType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DbsnapshotTenantDatabases } from "@ubx/sdk-aws/aws/data/rds/dbsnapshot_tenant_databases";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_dbsnapshot_tenant_databases" });

      data(DbsnapshotTenantDatabases, "example", {
        dbiResourceId: "example",
        dbinstanceIdentifier: "example",
        dbsnapshotIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        snapshotType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import DbsnapshotTenantDatabases, DbsnapshotTenantDatabasesConfig

    def describe():
        ubx.intent("look up aws_rds_dbsnapshot_tenant_databases")

        ubx.data(DbsnapshotTenantDatabases, "example", DbsnapshotTenantDatabasesConfig(
            dbi_resource_id="example",
            dbinstance_identifier="example",
            dbsnapshot_identifier="example",
            filters=[{"name": "example-name"}],
            max_records=1,
            snapshot_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbi_resource_id" type="string">
  The real, unique identifier of the dbi resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dbinstance_identifier" type="string">
  The real, unique identifier of the database instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dbsnapshot_identifier" type="string">
  The real, unique identifier of the dbsnapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  The real type of snapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dbsnapshot_tenant_databases" type="list(object)">
  The real, tenant databases included in this multi-tenant database snapshot. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
