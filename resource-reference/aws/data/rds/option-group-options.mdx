---
title: "aws_rds_option_group_options"
description: "`aws_rds_option_group_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_rds_option_group_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rds_option_group_options"})

    		ubx.Data(rds.OptionGroupOptions, "example", rds.OptionGroupOptionsConfig{
    			EngineName:         "example-engine-name",
    			Filters:            []map[string]any{{"name": "example-name"}},
    			MajorEngineVersion: "example",
    			MaxRecords:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OptionGroupOptions } from "@ubx/sdk-aws/aws/data/rds/option_group_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_rds_option_group_options" });

      data(OptionGroupOptions, "example", {
        engineName: "example-engine-name",
        filters: [{ name: "example-name" }],
        majorEngineVersion: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.rds import OptionGroupOptions, OptionGroupOptionsConfig

    def describe():
        ubx.intent("look up aws_rds_option_group_options")

        ubx.data(OptionGroupOptions, "example", OptionGroupOptionsConfig(
            engine_name="example-engine-name",
            filters=[{"name": "example-name"}],
            major_engine_version="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="major_engine_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="option_group_options" type="list(object)">
  &lt;p&gt;List of available option group options.&lt;/p&gt; Returned by the lookup.
</ResponseField>
