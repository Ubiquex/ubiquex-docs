---
title: "aws_discovery_export_configurations"
description: "`aws_discovery_export_configurations` reads the real, one-time data export jobs configured for Application Discovery Service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	discovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_export_configurations"})

    		ubx.Data(discovery.ExportConfigurations, "example", discovery.ExportConfigurationsConfig{
    			ExportIds:  []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExportConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/discovery/export_configurations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_export_configurations" });

      data(ExportConfigurations, "example", {
        exportIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.discovery import ExportConfigurations, ExportConfigurationsConfig

    def describe():
        ubx.intent("look up aws_discovery_export_configurations")

        ubx.data(ExportConfigurations, "example", ExportConfigurationsConfig(
            export_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_ids" type="list(string)">
  The real, unique identifiers of the exports this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="exports_info" type="list(object)">
  The real, detailed export information included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
