---
title: "aws_discovery_server_neighbors"
description: "`aws_discovery_server_neighbors` reads real, discovered network connections between a server and its own neighbors."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_server_neighbors"})

    		ubx.Data(discovery.ServerNeighbors, "example", discovery.ServerNeighborsConfig{
    			ConfigurationId:          "example",
    			MaxResults:               1,
    			NeighborConfigurationIds: []string{"example"},
    			PortInformationNeeded:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServerNeighbors } from "@ubx/sdk-aws/aws/data/discovery/server_neighbors";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_server_neighbors" });

      data(ServerNeighbors, "example", {
        configurationId: "example",
        maxResults: 1,
        neighborConfigurationIds: ["example"],
        portInformationNeeded: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.discovery import ServerNeighbors, ServerNeighborsConfig

    def describe():
        ubx.intent("look up aws_discovery_server_neighbors")

        ubx.data(ServerNeighbors, "example", ServerNeighborsConfig(
            configuration_id="example",
            max_results=1,
            neighbor_configuration_ids=["example"],
            port_information_needed=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_id" type="string" required>
  The real, unique identifier of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="neighbor_configuration_ids" type="list(string)">
  The real, unique identifiers of the neighbor configurations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="port_information_needed" type="bool">
  Whether port information is required for this connection. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="known_dependency_count" type="number">
  The real count of known dependencys. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="neighbors" type="list(object)">
  The real, network-adjacent neighbors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
