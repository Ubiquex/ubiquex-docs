---
title: "aws_api_iotwireless_wireless_devices"
description: "`aws_api_iotwireless_wireless_devices` looks up a real IoT Wireless device (LoRaWAN or Sidewalk)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_devices"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessDevices, "example", api_iotwireless.ApiIotwirelessWirelessDevicesConfig{
    			DestinationName:    "example-destination-name",
    			DeviceProfileId:    "example",
    			FuotaTaskId:        "example",
    			MaxResults:         1,
    			MulticastGroupId:   "example",
    			ServiceProfileId:   "example",
    			WirelessDeviceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessWirelessDevices } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_wireless_devices.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_wireless_devices" });

      data(ApiIotwirelessWirelessDevices, "example", {
        destinationName: "example-destination-name",
        deviceProfileId: "example",
        fuotaTaskId: "example",
        maxResults: 1,
        multicastGroupId: "example",
        serviceProfileId: "example",
        wirelessDeviceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessDevices, ApiIotwirelessWirelessDevicesConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_devices")

        ubx.data(ApiIotwirelessWirelessDevices, "example", ApiIotwirelessWirelessDevicesConfig(
            destination_name="example-destination-name",
            device_profile_id="example",
            fuota_task_id="example",
            max_results=1,
            multicast_group_id="example",
            service_profile_id="example",
            wireless_device_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="destination_name" type="string">
  The real name of the destination this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="device_profile_id" type="string">
  The real, unique identifier of the device profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="fuota_task_id" type="string">
  &lt;p&gt;The ID of a FUOTA task.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="multicast_group_id" type="string">
  &lt;p&gt;The ID of the multicast group.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="service_profile_id" type="string">
  The real, unique identifier of the service profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="wireless_device_type" type="string">
  The real, discriminating type of the wireless device this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="wireless_device_list" type="list(object)">
  The real wireless devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
