---
title: "aws_api_iotwireless_partner_account"
description: "`aws_api_iotwireless_partner_account` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_partner_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_partner_account"})

    		ubx.Data(api_iotwireless.ApiIotwirelessPartnerAccount, "example", api_iotwireless.ApiIotwirelessPartnerAccountConfig{
    			PartnerAccountId: "example",
    			PartnerType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessPartnerAccount } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_partner_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_partner_account" });

      data(ApiIotwirelessPartnerAccount, "example", {
        partnerAccountId: "example",
        partnerType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessPartnerAccount, ApiIotwirelessPartnerAccountConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_partner_account")

        ubx.data(ApiIotwirelessPartnerAccount, "example", ApiIotwirelessPartnerAccountConfig(
            partner_account_id="example",
            partner_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="partner_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="partner_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_linked" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sidewalk" type="object">
  &lt;p&gt;Information about a Sidewalk account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="amazon_id" type="string">
      &lt;p&gt;The Sidewalk Amazon ID.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="fingerprint" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
