---
title: "aws_api_iotwireless_wireless_gateways"
description: "`aws_api_iotwireless_wireless_gateways` looks up a real IoT Wireless LoRaWAN gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_gateways"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessGateways, "example", api_iotwireless.ApiIotwirelessWirelessGatewaysConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessWirelessGateways } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_wireless_gateways";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_wireless_gateways" });

      data(ApiIotwirelessWirelessGateways, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessGateways, ApiIotwirelessWirelessGatewaysConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_gateways")

        ubx.data(ApiIotwirelessWirelessGateways, "example", ApiIotwirelessWirelessGatewaysConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="wireless_gateway_list" type="list(object)">
  The real wireless gateways included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
