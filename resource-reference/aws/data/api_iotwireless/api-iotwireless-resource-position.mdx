---
title: "aws_api_iotwireless_resource_position"
description: "`aws_api_iotwireless_resource_position` reads the real, configured position of an IoT Wireless resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_resource_position"})

    		ubx.Data(api_iotwireless.ApiIotwirelessResourcePosition, "example", api_iotwireless.ApiIotwirelessResourcePositionConfig{
    			ResourceIdentifier: "example",
    			ResourceType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessResourcePosition } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_resource_position";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_resource_position" });

      data(ApiIotwirelessResourcePosition, "example", {
        resourceIdentifier: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessResourcePosition, ApiIotwirelessResourcePositionConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_resource_position")

        ubx.data(ApiIotwirelessResourcePosition, "example", ApiIotwirelessResourcePositionConfig(
            resource_identifier="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_identifier" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="geo_json_payload" type="string">
  The real, GeoJSON-formatted geographic payload of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
