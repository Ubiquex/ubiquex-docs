---
title: "aws_api_iotwireless_wireless_gateway_task_definitions"
description: "`aws_api_iotwireless_wireless_gateway_task_definitions` looks up a real task definition (such as a firmware update job) for IoT Wireless LoRaWAN gateways."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_gateway_task_definitions"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessGatewayTaskDefinitions, "example", api_iotwireless.ApiIotwirelessWirelessGatewayTaskDefinitionsConfig{
    			MaxResults:         1,
    			TaskDefinitionType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessWirelessGatewayTaskDefinitions } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_wireless_gateway_task_definitions.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_api_iotwireless_wireless_gateway_task_definitions",
      });

      data(ApiIotwirelessWirelessGatewayTaskDefinitions, "example", {
        maxResults: 1,
        taskDefinitionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessGatewayTaskDefinitions, ApiIotwirelessWirelessGatewayTaskDefinitionsConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_gateway_task_definitions")

        ubx.data(ApiIotwirelessWirelessGatewayTaskDefinitions, "example", ApiIotwirelessWirelessGatewayTaskDefinitionsConfig(
            max_results=1,
            task_definition_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="task_definition_type" type="string">
  The real, discriminating type of the task definition this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_definitions" type="list(object)">
  The real ECS task definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
