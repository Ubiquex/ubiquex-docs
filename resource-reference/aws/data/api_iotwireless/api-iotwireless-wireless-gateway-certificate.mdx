---
title: "aws_api_iotwireless_wireless_gateway_certificate"
description: "`aws_api_iotwireless_wireless_gateway_certificate` reads the real certificate associated with an IoT Wireless LoRaWAN gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_gateway_certificate"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessGatewayCertificate, "example", api_iotwireless.ApiIotwirelessWirelessGatewayCertificateConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessWirelessGatewayCertificate } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_wireless_gateway_certificate.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_api_iotwireless_wireless_gateway_certificate",
      });

      data(ApiIotwirelessWirelessGatewayCertificate, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessGatewayCertificate, ApiIotwirelessWirelessGatewayCertificateConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_gateway_certificate")

        ubx.data(ApiIotwirelessWirelessGatewayCertificate, "example", ApiIotwirelessWirelessGatewayCertificateConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="iot_certificate_id" type="string">
  The real, unique identifier of the iot certificate this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="lo_ra_wannetwork_server_certificate_id" type="string">
  The real, unique identifier of the lo ra wannetwork server certificate this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
