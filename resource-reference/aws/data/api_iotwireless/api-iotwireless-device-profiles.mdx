---
title: "aws_api_iotwireless_device_profiles"
description: "`aws_api_iotwireless_device_profiles` looks up a real IoT Wireless device profile, defining the LoRaWAN capabilities a device supports."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_device_profiles"})

    		ubx.Data(api_iotwireless.ApiIotwirelessDeviceProfiles, "example", api_iotwireless.ApiIotwirelessDeviceProfilesConfig{
    			DeviceProfileType: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessDeviceProfiles } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_device_profiles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_device_profiles" });

      data(ApiIotwirelessDeviceProfiles, "example", {
        deviceProfileType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessDeviceProfiles, ApiIotwirelessDeviceProfilesConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_device_profiles")

        ubx.data(ApiIotwirelessDeviceProfiles, "example", ApiIotwirelessDeviceProfilesConfig(
            device_profile_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_profile_type" type="string">
  The real, discriminating type of the device profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="device_profile_list" type="list(object)">
  The real device profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
