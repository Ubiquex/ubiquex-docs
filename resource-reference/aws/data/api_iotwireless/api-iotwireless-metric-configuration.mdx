---
title: "aws_api_iotwireless_metric_configuration"
description: "`aws_api_iotwireless_metric_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_metric_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_metric_configuration"})

    		ubx.Data(api_iotwireless.ApiIotwirelessMetricConfiguration, "example", api_iotwireless.ApiIotwirelessMetricConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessMetricConfiguration } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_metric_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_metric_configuration" });

      data(ApiIotwirelessMetricConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessMetricConfiguration, ApiIotwirelessMetricConfigurationConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_metric_configuration")

        ubx.data(ApiIotwirelessMetricConfiguration, "example", ApiIotwirelessMetricConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="summary_metric" type="object">
  &lt;p&gt;The configuration of summary metrics.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
