---
title: "aws_api_iotwireless_wireless_device_statistics"
description: "`aws_api_iotwireless_wireless_device_statistics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_wireless_device_statistics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_device_statistics"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessDeviceStatistics, "example", api_iotwireless.ApiIotwirelessWirelessDeviceStatisticsConfig{
    			WirelessDeviceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessWirelessDeviceStatistics } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_wireless_device_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_wireless_device_statistics" });

      data(ApiIotwirelessWirelessDeviceStatistics, "example", {
        wirelessDeviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessDeviceStatistics, ApiIotwirelessWirelessDeviceStatisticsConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_device_statistics")

        ubx.data(ApiIotwirelessWirelessDeviceStatistics, "example", ApiIotwirelessWirelessDeviceStatisticsConfig(
            wireless_device_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="wireless_device_id" type="string" required>
  &lt;p&gt;The ID of the wireless device.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_uplink_received_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="lo_ra_wan" type="object">
  &lt;p&gt;LoRaWAN device metatdata.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_rate" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="dev_eui" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="fport" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="frequency" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="gateways" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="public_gateways" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sidewalk" type="object">
  &lt;p&gt;MetaData for Sidewalk device.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="battery_level" type="string">
      &lt;p&gt;Sidewalk device battery level.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_state" type="string">
      &lt;p&gt;Device state defines the device status of sidewalk device.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="event" type="string">
      &lt;p&gt;Sidewalk device status notification.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="rssi" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
