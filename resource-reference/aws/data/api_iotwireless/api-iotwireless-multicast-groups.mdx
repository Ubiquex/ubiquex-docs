---
title: "aws_api_iotwireless_multicast_groups"
description: "`aws_api_iotwireless_multicast_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_multicast_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_multicast_groups"})

    		ubx.Data(api_iotwireless.ApiIotwirelessMulticastGroups, "example", api_iotwireless.ApiIotwirelessMulticastGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessMulticastGroups } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_multicast_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_multicast_groups" });

      data(ApiIotwirelessMulticastGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessMulticastGroups, ApiIotwirelessMulticastGroupsConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_multicast_groups")

        ubx.data(ApiIotwirelessMulticastGroups, "example", ApiIotwirelessMulticastGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="multicast_group_list" type="list(object)">
  &lt;p&gt;List of multicast groups.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
