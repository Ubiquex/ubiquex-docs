---
title: "aws_api_iotwireless_multicast_group_session"
description: "`aws_api_iotwireless_multicast_group_session` reads the real, active session for an IoT Wireless LoRaWAN multicast group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_multicast_group_session"})

    		ubx.Data(api_iotwireless.ApiIotwirelessMulticastGroupSession, "example", api_iotwireless.ApiIotwirelessMulticastGroupSessionConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessMulticastGroupSession } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_multicast_group_session";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_multicast_group_session" });

      data(ApiIotwirelessMulticastGroupSession, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessMulticastGroupSession, ApiIotwirelessMulticastGroupSessionConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_multicast_group_session")

        ubx.data(ApiIotwirelessMulticastGroupSession, "example", ApiIotwirelessMulticastGroupSessionConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  &lt;p&gt;The ID of the multicast group.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lo_ra_wan" type="object">
  &lt;p&gt;The LoRaWAN information used with the multicast session.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="dl_dr" type="number">
      &lt;p&gt;Downlink data rate.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="dl_freq" type="number">
      &lt;p&gt;Downlink frequency.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="ping_slot_period" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_start_time" type="string">
      &lt;p&gt;Timestamp of when the multicast group session is to start.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_timeout" type="number">
      &lt;p&gt;How long before a multicast group session is to timeout.&lt;/p&gt; &lt;note&gt; &lt;p&gt;We recommend that you provide a timeout value that is a power-of-two (such as 64, 128, 256). If a non-power-of-two value is provided, it will automatically be rounded up to the next supported power-of-two within the allowed range.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
