---
title: "aws_api_iotwireless_resource_log_level"
description: "`aws_api_iotwireless_resource_log_level` reads the real CloudWatch log level configured for a specific IoT Wireless resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_resource_log_level"})

    		ubx.Data(api_iotwireless.ApiIotwirelessResourceLogLevel, "example", api_iotwireless.ApiIotwirelessResourceLogLevelConfig{
    			ResourceIdentifier: "example",
    			ResourceType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessResourceLogLevel } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_resource_log_level.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_resource_log_level" });

      data(ApiIotwirelessResourceLogLevel, "example", {
        resourceIdentifier: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessResourceLogLevel, ApiIotwirelessResourceLogLevelConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_resource_log_level")

        ubx.data(ApiIotwirelessResourceLogLevel, "example", ApiIotwirelessResourceLogLevelConfig(
            resource_identifier="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_identifier" type="string" required>
  &lt;p&gt;The unique identifier of the resource, which can be the wireless gateway ID, the wireless device ID, or the FUOTA task ID.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="log_level" type="string">
  &lt;p&gt;The log level for a log message. The log levels can be disabled, or set to &lt;code&gt;ERROR&lt;/code&gt; to display less verbose logs containing only error information, or to &lt;code&gt;INFO&lt;/code&gt; for more detailed logs.&lt;/p&gt; Returned by the lookup.
</ResponseField>
