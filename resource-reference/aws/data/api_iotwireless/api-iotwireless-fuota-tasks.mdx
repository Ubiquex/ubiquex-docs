---
title: "aws_api_iotwireless_fuota_tasks"
description: "`aws_api_iotwireless_fuota_tasks` looks up a real firmware-update-over-the-air (FUOTA) task for LoRaWAN devices."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_fuota_tasks"})

    		ubx.Data(api_iotwireless.ApiIotwirelessFuotaTasks, "example", api_iotwireless.ApiIotwirelessFuotaTasksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessFuotaTasks } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_fuota_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_fuota_tasks" });

      data(ApiIotwirelessFuotaTasks, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessFuotaTasks, ApiIotwirelessFuotaTasksConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_fuota_tasks")

        ubx.data(ApiIotwirelessFuotaTasks, "example", ApiIotwirelessFuotaTasksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fuota_task_list" type="list(object)">
  &lt;p&gt;Lists the FUOTA tasks registered to your AWS account.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
