---
title: "aws_api_iotwireless_queued_messages"
description: "`aws_api_iotwireless_queued_messages` reads the real downlink messages currently queued for an IoT Wireless device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_queued_messages"})

    		ubx.Data(api_iotwireless.ApiIotwirelessQueuedMessages, "example", api_iotwireless.ApiIotwirelessQueuedMessagesConfig{
    			Id:                 "example",
    			MaxResults:         1,
    			WirelessDeviceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessQueuedMessages } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_queued_messages";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_queued_messages" });

      data(ApiIotwirelessQueuedMessages, "example", {
        id: "example",
        maxResults: 1,
        wirelessDeviceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessQueuedMessages, ApiIotwirelessQueuedMessagesConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_queued_messages")

        ubx.data(ApiIotwirelessQueuedMessages, "example", ApiIotwirelessQueuedMessagesConfig(
            id="example",
            max_results=1,
            wireless_device_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  &lt;p&gt;The ID of the wireless device.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return in this operation.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="wireless_device_type" type="string">
  The real, discriminating type of the wireless device this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="downlink_queue_messages_list" type="list(object)">
  The real downlink queue messagess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
