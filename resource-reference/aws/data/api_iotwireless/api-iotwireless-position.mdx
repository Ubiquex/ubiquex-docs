---
title: "aws_api_iotwireless_position"
description: "`aws_api_iotwireless_position` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_position` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_position"})

    		ubx.Data(api_iotwireless.ApiIotwirelessPosition, "example", api_iotwireless.ApiIotwirelessPositionConfig{
    			ResourceIdentifier: "example",
    			ResourceType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessPosition } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_position";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_position" });

      data(ApiIotwirelessPosition, "example", {
        resourceIdentifier: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessPosition, ApiIotwirelessPositionConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_position")

        ubx.data(ApiIotwirelessPosition, "example", ApiIotwirelessPositionConfig(
            resource_identifier="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accuracy" type="object">
  &lt;p&gt;The accuracy of the estimated position in meters. An empty value indicates that no position data is available. A value of ‘0.0’ value indicates that position data is available. This data corresponds to the position information that you specified instead of the position computed by solver.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="horizontal_accuracy" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vertical_accuracy" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="position" type="list(number)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="solver_provider" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="solver_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="solver_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="timestamp" type="string">
  Returned by the lookup.
</ResponseField>
