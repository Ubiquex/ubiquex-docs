---
title: "aws_api_iotwireless_wireless_gateway_statistics"
description: "`aws_api_iotwireless_wireless_gateway_statistics` reads real, reported connectivity statistics for an IoT Wireless LoRaWAN gateway."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_gateway_statistics"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessGatewayStatistics, "example", api_iotwireless.ApiIotwirelessWirelessGatewayStatisticsConfig{
    			WirelessGatewayId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessWirelessGatewayStatistics } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_wireless_gateway_statistics.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_api_iotwireless_wireless_gateway_statistics",
      });

      data(ApiIotwirelessWirelessGatewayStatistics, "example", {
        wirelessGatewayId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessGatewayStatistics, ApiIotwirelessWirelessGatewayStatisticsConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_gateway_statistics")

        ubx.data(ApiIotwirelessWirelessGatewayStatistics, "example", ApiIotwirelessWirelessGatewayStatisticsConfig(
            wireless_gateway_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="wireless_gateway_id" type="string" required>
  The real, unique identifier of the wireless gateway this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connection_status" type="string">
  The real, current status of the connection this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_uplink_received_at" type="string">
  The real timestamp the last uplink message was received. **(AI-inferred)** Returned by the lookup.
</ResponseField>
