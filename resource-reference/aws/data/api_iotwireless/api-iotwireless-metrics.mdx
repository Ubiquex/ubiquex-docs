---
title: "aws_api_iotwireless_metrics"
description: "`aws_api_iotwireless_metrics` reads real, reported summary metrics for IoT Wireless."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_metrics"})

    		ubx.Data(api_iotwireless.ApiIotwirelessMetrics, "example", api_iotwireless.ApiIotwirelessMetricsConfig{
    			SummaryMetricQueries: []map[string]any{{"aggregation_period": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiIotwirelessMetrics } from "./local-sdk/aws/sdk/typescript/aws/data/api_iotwireless/api_iotwireless_metrics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_iotwireless_metrics" });

      data(ApiIotwirelessMetrics, "example", {
        summaryMetricQueries: [{ aggregationPeriod: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessMetrics, ApiIotwirelessMetricsConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_metrics")

        ubx.data(ApiIotwirelessMetrics, "example", ApiIotwirelessMetricsConfig(
            summary_metric_queries=[{"aggregation_period": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="summary_metric_queries" type="list(object)">
  The real, summarized metric queries included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="summary_metric_query_results" type="list(object)">
  The real results of the summarized metric queries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
