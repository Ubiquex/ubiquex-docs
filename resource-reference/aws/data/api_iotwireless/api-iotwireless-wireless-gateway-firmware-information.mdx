---
title: "aws_api_iotwireless_wireless_gateway_firmware_information"
description: "`aws_api_iotwireless_wireless_gateway_firmware_information` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_iotwireless_wireless_gateway_firmware_information` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_iotwireless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_iotwireless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_iotwireless_wireless_gateway_firmware_information"})

    		ubx.Data(api_iotwireless.ApiIotwirelessWirelessGatewayFirmwareInformation, "example", api_iotwireless.ApiIotwirelessWirelessGatewayFirmwareInformationConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiIotwirelessWirelessGatewayFirmwareInformation } from "@ubx/sdk-aws/aws/data/api_iotwireless/api_iotwireless_wireless_gateway_firmware_information";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_api_iotwireless_wireless_gateway_firmware_information",
      });

      data(ApiIotwirelessWirelessGatewayFirmwareInformation, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_iotwireless import ApiIotwirelessWirelessGatewayFirmwareInformation, ApiIotwirelessWirelessGatewayFirmwareInformationConfig

    def describe():
        ubx.intent("look up aws_api_iotwireless_wireless_gateway_firmware_information")

        ubx.data(ApiIotwirelessWirelessGatewayFirmwareInformation, "example", ApiIotwirelessWirelessGatewayFirmwareInformationConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lo_ra_wan" type="object">
  &lt;p&gt;LoRaWANGatewayCurrentVersion object.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="current_version" type="object">
      &lt;p&gt;LoRaWANGatewayVersion object.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="model" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="package_version" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="station" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
