---
title: "aws_amplifybackend_backend_jobs"
description: "`aws_amplifybackend_backend_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_amplifybackend_backend_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplifybackend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/amplifybackend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplifybackend_backend_jobs"})

    		ubx.Data(amplifybackend.BackendJobs, "example", amplifybackend.BackendJobsConfig{
    			AppId:                  "example",
    			BackendEnvironmentName: "example-backend-environment-name",
    			JobId:                  "example",
    			MaxResults:             1,
    			Operation:              "example",
    			Status:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackendJobs } from "@ubx/sdk-aws/aws/data/amplifybackend/backend_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplifybackend_backend_jobs" });

      data(BackendJobs, "example", {
        appId: "example",
        backendEnvironmentName: "example-backend-environment-name",
        jobId: "example",
        maxResults: 1,
        operation: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.amplifybackend import BackendJobs, BackendJobsConfig

    def describe():
        ubx.intent("look up aws_amplifybackend_backend_jobs")

        ubx.data(BackendJobs, "example", BackendJobsConfig(
            app_id="example",
            backend_environment_name="example-backend-environment-name",
            job_id="example",
            max_results=1,
            operation="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="backend_environment_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="operation" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
