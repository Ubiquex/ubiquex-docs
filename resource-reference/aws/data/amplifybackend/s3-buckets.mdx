---
title: "aws_amplifybackend_s3_buckets"
description: "`aws_amplifybackend_s3_buckets` reads the real S3 buckets provisioned for an Amplify Backend."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplifybackend "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/amplifybackend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplifybackend_s3_buckets"})

    		ubx.Data(amplifybackend.S3Buckets, "example", amplifybackend.S3BucketsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3Buckets } from "@ubx/sdk-aws/aws/data/amplifybackend/s3_buckets";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplifybackend_s3_buckets" });

      data(S3Buckets, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.amplifybackend import S3Buckets, S3BucketsConfig

    def describe():
        ubx.intent("look up aws_amplifybackend_s3_buckets")

        ubx.data(S3Buckets, "example", S3BucketsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="buckets" type="list(object)">
  The real S3 buckets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
