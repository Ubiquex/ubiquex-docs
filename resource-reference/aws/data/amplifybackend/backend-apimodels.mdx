---
title: "aws_amplifybackend_backend_apimodels"
description: "`aws_amplifybackend_backend_apimodels` reads the real GraphQL data models generated for an Amplify backend API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amplifybackend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/amplifybackend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplifybackend_backend_apimodels"})

    		ubx.Data(amplifybackend.BackendApimodels, "example", amplifybackend.BackendApimodelsConfig{
    			AppId:                  "example",
    			BackendEnvironmentName: "example-backend-environment-name",
    			ResourceName:           "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BackendApimodels } from "./local-sdk/aws/sdk/typescript/aws/data/amplifybackend/backend_apimodels.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplifybackend_backend_apimodels" });

      data(BackendApimodels, "example", {
        appId: "example",
        backendEnvironmentName: "example-backend-environment-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.amplifybackend import BackendApimodels, BackendApimodelsConfig

    def describe():
        ubx.intent("look up aws_amplifybackend_backend_apimodels")

        ubx.data(BackendApimodels, "example", BackendApimodelsConfig(
            app_id="example",
            backend_environment_name="example-backend-environment-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The real, unique identifier of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="backend_environment_name" type="string" required>
  The real name of the Amplify backend environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="model_introspection_schema" type="string">
  The real, introspected GraphQL schema for this model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="models" type="string">
  The real models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
