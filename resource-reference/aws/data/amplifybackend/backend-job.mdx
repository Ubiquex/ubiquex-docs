---
title: "aws_amplifybackend_backend_job"
description: "`aws_amplifybackend_backend_job` looks up one real Amplify Backend job, provisioning or updating backend resources."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplifybackend "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/amplifybackend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplifybackend_backend_job"})

    		ubx.Data(amplifybackend.BackendJob, "example", amplifybackend.BackendJobConfig{
    			AppId:                  "example",
    			BackendEnvironmentName: "example-backend-environment-name",
    			JobId:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackendJob } from "@ubx/sdk-aws/aws/data/amplifybackend/backend_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplifybackend_backend_job" });

      data(BackendJob, "example", {
        appId: "example",
        backendEnvironmentName: "example-backend-environment-name",
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.amplifybackend import BackendJob, BackendJobConfig

    def describe():
        ubx.intent("look up aws_amplifybackend_backend_job")

        ubx.data(BackendJob, "example", BackendJobConfig(
            app_id="example",
            backend_environment_name="example-backend-environment-name",
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The real, unique identifier of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="backend_environment_name" type="string" required>
  The real name of the Amplify backend environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error" type="string">
  The real error encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="operation" type="string">
  The real operation this object describes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
