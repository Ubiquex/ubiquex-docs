---
title: "aws_proton_repository_sync_status"
description: "`aws_proton_repository_sync_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_proton_repository_sync_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	proton "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/proton"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_proton_repository_sync_status"})

    		ubx.Data(proton.RepositorySyncStatus, "example", proton.RepositorySyncStatusConfig{
    			Branch:             "example",
    			RepositoryName:     "example-repository-name",
    			RepositoryProvider: "example",
    			SyncType:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositorySyncStatus } from "@ubx/sdk-aws/aws/data/proton/repository_sync_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_proton_repository_sync_status" });

      data(RepositorySyncStatus, "example", {
        branch: "example",
        repositoryName: "example-repository-name",
        repositoryProvider: "example",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.proton import RepositorySyncStatus, RepositorySyncStatusConfig

    def describe():
        ubx.intent("look up aws_proton_repository_sync_status")

        ubx.data(RepositorySyncStatus, "example", RepositorySyncStatusConfig(
            branch="example",
            repository_name="example-repository-name",
            repository_provider="example",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repository_provider" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="latest_sync" type="object">
  &lt;p&gt;Detail data for a repository sync attempt activated by a push to a repository.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="started_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
