---
title: "aws_proton_repository_sync_definitions"
description: "`aws_proton_repository_sync_definitions` reads the real, tracked resources a Proton repository sync definition manages."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	proton "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/proton"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_proton_repository_sync_definitions"})

    		ubx.Data(proton.RepositorySyncDefinitions, "example", proton.RepositorySyncDefinitionsConfig{
    			RepositoryName:     "example-repository-name",
    			RepositoryProvider: "example",
    			SyncType:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RepositorySyncDefinitions } from "./local-sdk/aws/sdk/typescript/aws/data/proton/repository_sync_definitions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_proton_repository_sync_definitions" });

      data(RepositorySyncDefinitions, "example", {
        repositoryName: "example-repository-name",
        repositoryProvider: "example",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.proton import RepositorySyncDefinitions, RepositorySyncDefinitionsConfig

    def describe():
        ubx.intent("look up aws_proton_repository_sync_definitions")

        ubx.data(RepositorySyncDefinitions, "example", RepositorySyncDefinitionsConfig(
            repository_name="example-repository-name",
            repository_provider="example",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository_provider" type="string" required>
  The real source-control provider hosting this repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  The real type of synchronization this request performs. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_definitions" type="list(object)">
  The real sync definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
