---
title: "aws_bedrock_data_automation_data_automation_library_entities"
description: "`aws_bedrock_data_automation_data_automation_library_entities` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_bedrock_data_automation_data_automation_library_entities` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock_data_automation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bedrock_data_automation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bedrock_data_automation_data_automation_library_entities"})

    		ubx.Data(bedrock_data_automation.BedrockDataAutomationDataAutomationLibraryEntities, "example", bedrock_data_automation.BedrockDataAutomationDataAutomationLibraryEntitiesConfig{
    			EntityType: "example",
    			LibraryArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BedrockDataAutomationDataAutomationLibraryEntities } from "@ubx/sdk-aws/aws/data/bedrock_data_automation/bedrock_data_automation_data_automation_library_entities";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_bedrock_data_automation_data_automation_library_entities",
      });

      data(BedrockDataAutomationDataAutomationLibraryEntities, "example", {
        entityType: "example",
        libraryArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bedrock_data_automation import BedrockDataAutomationDataAutomationLibraryEntities, BedrockDataAutomationDataAutomationLibraryEntitiesConfig

    def describe():
        ubx.intent("look up aws_bedrock_data_automation_data_automation_library_entities")

        ubx.data(BedrockDataAutomationDataAutomationLibraryEntities, "example", BedrockDataAutomationDataAutomationLibraryEntitiesConfig(
            entity_type="example",
            library_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_type" type="string" required>
  Entity types supported in DataAutomationLibraries Required lookup argument.
</ResponseField>

<ResponseField name="library_arn" type="string" required>
  ARN generated at the server side when a DataAutomationLibrary is created Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Max Results Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entities" type="list(object)">
  List of entity summary object Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Pagination token Returned by the lookup.
</ResponseField>
