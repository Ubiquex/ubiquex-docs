---
title: "aws_discovery_marketplace_offer_set"
description: "`aws_discovery_marketplace_offer_set` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_discovery_marketplace_offer_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_marketplace_offer_set"})

    		ubx.Data(discovery_marketplace.DiscoveryMarketplaceOfferSet, "example", discovery_marketplace.DiscoveryMarketplaceOfferSetConfig{
    			OfferSetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DiscoveryMarketplaceOfferSet } from "@ubx/sdk-aws/aws/data/discovery_marketplace/discovery_marketplace_offer_set";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_marketplace_offer_set" });

      data(DiscoveryMarketplaceOfferSet, "example", {
        offerSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.discovery_marketplace import DiscoveryMarketplaceOfferSet, DiscoveryMarketplaceOfferSetConfig

    def describe():
        ubx.intent("look up aws_discovery_marketplace_offer_set")

        ubx.data(DiscoveryMarketplaceOfferSet, "example", DiscoveryMarketplaceOfferSetConfig(
            offer_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="offer_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associated_entities" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="available_from_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="badges" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="buyer_notes" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="catalog" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="expiration_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="offer_set_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="seller_of_record" type="object">
  &lt;p&gt;Information about a seller, including the profile identifier and display name.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="seller_profile_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
