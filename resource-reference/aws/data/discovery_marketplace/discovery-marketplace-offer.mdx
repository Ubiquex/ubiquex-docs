---
title: "aws_discovery_marketplace_offer"
description: "`aws_discovery_marketplace_offer` looks up one real AWS Marketplace offer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	discovery_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_marketplace_offer"})

    		ubx.Data(discovery_marketplace.DiscoveryMarketplaceOffer, "example", discovery_marketplace.DiscoveryMarketplaceOfferConfig{
    			OfferId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DiscoveryMarketplaceOffer } from "./local-sdk/aws/sdk/typescript/aws/data/discovery_marketplace/discovery_marketplace_offer.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_marketplace_offer" });

      data(DiscoveryMarketplaceOffer, "example", {
        offerId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.discovery_marketplace import DiscoveryMarketplaceOffer, DiscoveryMarketplaceOfferConfig

    def describe():
        ubx.intent("look up aws_discovery_marketplace_offer")

        ubx.data(DiscoveryMarketplaceOffer, "example", DiscoveryMarketplaceOfferConfig(
            offer_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="offer_id" type="string" required>
  The real, unique identifier of the offer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agreement_proposal_id" type="string">
  The real, unique identifier of the agreement proposal this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="associated_entities" type="list(object)">
  The real entities associated with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="available_from_time" type="string">
  The real timestamp of the available from. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="badges" type="list(object)">
  The real badges included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="catalog" type="string">
  The real catalog this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration_time" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="offer_name" type="string">
  The real name of the offer this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pricing_model" type="object">
  &lt;p&gt;A pricing model that determines how buyers are charged for a listing, such as usage-based, contract, BYOL, or free.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="pricing_model_type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="replacement_agreement_id" type="string">
  The real, unique identifier of the replacement agreement this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="seller_of_record" type="object">
  &lt;p&gt;Information about a seller, including the profile identifier and display name.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="seller_profile_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
