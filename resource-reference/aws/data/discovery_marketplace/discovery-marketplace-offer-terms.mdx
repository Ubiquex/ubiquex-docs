---
title: "aws_discovery_marketplace_offer_terms"
description: "`aws_discovery_marketplace_offer_terms` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_discovery_marketplace_offer_terms` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_marketplace_offer_terms"})

    		ubx.Data(discovery_marketplace.DiscoveryMarketplaceOfferTerms, "example", discovery_marketplace.DiscoveryMarketplaceOfferTermsConfig{
    			OfferId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DiscoveryMarketplaceOfferTerms } from "@ubx/sdk-aws/aws/data/discovery_marketplace/discovery_marketplace_offer_terms";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_marketplace_offer_terms" });

      data(DiscoveryMarketplaceOfferTerms, "example", {
        offerId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.discovery_marketplace import DiscoveryMarketplaceOfferTerms, DiscoveryMarketplaceOfferTermsConfig

    def describe():
        ubx.intent("look up aws_discovery_marketplace_offer_terms")

        ubx.data(DiscoveryMarketplaceOfferTerms, "example", DiscoveryMarketplaceOfferTermsConfig(
            offer_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offer_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="offer_terms" type="list(object)">
  Returned by the lookup.
</ResponseField>
