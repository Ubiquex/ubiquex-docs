---
title: "aws_discovery_marketplace_fulfillment_options"
description: "`aws_discovery_marketplace_fulfillment_options` reads the real, available fulfillment options for an AWS Marketplace listing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/discovery_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_marketplace_fulfillment_options"})

    		ubx.Data(discovery_marketplace.DiscoveryMarketplaceFulfillmentOptions, "example", discovery_marketplace.DiscoveryMarketplaceFulfillmentOptionsConfig{
    			ProductId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DiscoveryMarketplaceFulfillmentOptions } from "@ubx/sdk-aws/aws/data/discovery_marketplace/discovery_marketplace_fulfillment_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_marketplace_fulfillment_options" });

      data(DiscoveryMarketplaceFulfillmentOptions, "example", {
        productId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.discovery_marketplace import DiscoveryMarketplaceFulfillmentOptions, DiscoveryMarketplaceFulfillmentOptionsConfig

    def describe():
        ubx.intent("look up aws_discovery_marketplace_fulfillment_options")

        ubx.data(DiscoveryMarketplaceFulfillmentOptions, "example", DiscoveryMarketplaceFulfillmentOptionsConfig(
            product_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_id" type="string" required>
  The real, unique identifier of the product this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fulfillment_options" type="list(object)">
  The real, available options for the fulfillment. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
