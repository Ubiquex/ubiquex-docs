---
title: "aws_discovery_marketplace_purchase_options"
description: "`aws_discovery_marketplace_purchase_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_discovery_marketplace_purchase_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	discovery_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/discovery_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_discovery_marketplace_purchase_options"})

    		ubx.Data(discovery_marketplace.DiscoveryMarketplacePurchaseOptions, "example", discovery_marketplace.DiscoveryMarketplacePurchaseOptionsConfig{
    			Filters:    []map[string]any{{"filter_type": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DiscoveryMarketplacePurchaseOptions } from "@ubx/sdk-aws/aws/data/discovery_marketplace/discovery_marketplace_purchase_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_discovery_marketplace_purchase_options" });

      data(DiscoveryMarketplacePurchaseOptions, "example", {
        filters: [{ filterType: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.discovery_marketplace import DiscoveryMarketplacePurchaseOptions, DiscoveryMarketplacePurchaseOptionsConfig

    def describe():
        ubx.intent("look up aws_discovery_marketplace_purchase_options")

        ubx.data(DiscoveryMarketplacePurchaseOptions, "example", DiscoveryMarketplacePurchaseOptionsConfig(
            filters=[{"filter_type": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="purchase_options" type="list(object)">
  Returned by the lookup.
</ResponseField>
