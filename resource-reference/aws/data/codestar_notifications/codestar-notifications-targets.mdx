---
title: "aws_codestar_notifications_targets"
description: "`aws_codestar_notifications_targets` reads the real notification targets (chat channels, SNS topics) registered with CodeStar Notifications."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codestar_notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_notifications_targets"})

    		ubx.Data(codestar_notifications.CodestarNotificationsTargets, "example", codestar_notifications.CodestarNotificationsTargetsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarNotificationsTargets } from "@ubx/sdk-aws/aws/data/codestar_notifications/codestar_notifications_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_notifications_targets" });

      data(CodestarNotificationsTargets, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_notifications import CodestarNotificationsTargets, CodestarNotificationsTargetsConfig

    def describe():
        ubx.intent("look up aws_codestar_notifications_targets")

        ubx.data(CodestarNotificationsTargets, "example", CodestarNotificationsTargetsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="targets" type="list(object)">
  The real targets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
