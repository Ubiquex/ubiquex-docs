---
title: "aws_codestar_notifications_tags_for_resource"
description: "`aws_codestar_notifications_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codestar_notifications_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_notifications_tags_for_resource"})

    		ubx.Data(codestar_notifications.CodestarNotificationsTagsForResource, "example", codestar_notifications.CodestarNotificationsTagsForResourceConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarNotificationsTagsForResource } from "@ubx/sdk-aws/aws/data/codestar_notifications/codestar_notifications_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_notifications_tags_for_resource" });

      data(CodestarNotificationsTagsForResource, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_notifications import CodestarNotificationsTagsForResource, CodestarNotificationsTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_codestar_notifications_tags_for_resource")

        ubx.data(CodestarNotificationsTagsForResource, "example", CodestarNotificationsTagsForResourceConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
