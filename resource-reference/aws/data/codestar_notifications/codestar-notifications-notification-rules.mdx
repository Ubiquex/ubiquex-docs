---
title: "aws_codestar_notifications_notification_rules"
description: "`aws_codestar_notifications_notification_rules` looks up a real CodeStar notification rule, routing events from a DevOps resource to a chat or SNS target."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codestar_notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_notifications_notification_rules"})

    		ubx.Data(codestar_notifications.CodestarNotificationsNotificationRules, "example", codestar_notifications.CodestarNotificationsNotificationRulesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodestarNotificationsNotificationRules } from "./local-sdk/aws/sdk/typescript/aws/data/codestar_notifications/codestar_notifications_notification_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_notifications_notification_rules" });

      data(CodestarNotificationsNotificationRules, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_notifications import CodestarNotificationsNotificationRules, CodestarNotificationsNotificationRulesConfig

    def describe():
        ubx.intent("look up aws_codestar_notifications_notification_rules")

        ubx.data(CodestarNotificationsNotificationRules, "example", CodestarNotificationsNotificationRulesConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notification_rules" type="list(object)">
  The real rules for the notification. **(AI-inferred)** Returned by the lookup.
</ResponseField>
