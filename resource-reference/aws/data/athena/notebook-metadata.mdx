---
title: "aws_athena_notebook_metadata"
description: "`aws_athena_notebook_metadata` reads real metadata about an Athena notebook."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_notebook_metadata"})

    		ubx.Data(athena.NotebookMetadata, "example", athena.NotebookMetadataConfig{
    			WorkGroup:  "example",
    			Filters:    map[string]any{"name": "example-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotebookMetadata } from "@ubx/sdk-aws/aws/data/athena/notebook_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_notebook_metadata" });

      data(NotebookMetadata, "example", {
        workGroup: "example",
        filters: { name: "example-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.athena import NotebookMetadata, NotebookMetadataConfig
    from ubx.aws.data.athena.notebook_metadata import NotebookMetadata_Filters

    def describe():
        ubx.intent("look up aws_athena_notebook_metadata")

        ubx.data(NotebookMetadata, "example", NotebookMetadataConfig(
            work_group="example",
            filters=NotebookMetadata_Filters(name="example-name"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;A string for searching notebook names.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="work_group" type="string" required>
  The real Amazon Athena workgroup this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notebook_metadata_list" type="list(object)">
  The real notebook metadatas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
