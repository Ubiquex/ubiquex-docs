---
title: "aws_athena_application_dpusizes"
description: "`aws_athena_application_dpusizes` reads the real, available DPU (data processing unit) sizes for an Athena for Apache Spark session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_application_dpusizes"})

    		ubx.Data(athena.ApplicationDpusizes, "example", athena.ApplicationDpusizesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationDpusizes } from "./local-sdk/aws/sdk/typescript/aws/data/athena/application_dpusizes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_application_dpusizes" });

      data(ApplicationDpusizes, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.athena import ApplicationDpusizes, ApplicationDpusizesConfig

    def describe():
        ubx.intent("look up aws_athena_application_dpusizes")

        ubx.data(ApplicationDpusizes, "example", ApplicationDpusizesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_dpusizes" type="list(object)">
  The real, available data processing unit (DPU) sizes for this application. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
