---
title: "aws_athena_engine_versions"
description: "`aws_athena_engine_versions` reads the real, available Athena engine versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_engine_versions"})

    		ubx.Data(athena.EngineVersions, "example", athena.EngineVersionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EngineVersions } from "@ubx/sdk-aws/aws/data/athena/engine_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_engine_versions" });

      data(EngineVersions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.athena import EngineVersions, EngineVersionsConfig

    def describe():
        ubx.intent("look up aws_athena_engine_versions")

        ubx.data(EngineVersions, "example", EngineVersionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="engine_versions" type="list(object)">
  The real versions of the database or compute engine included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
