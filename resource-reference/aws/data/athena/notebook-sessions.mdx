---
title: "aws_athena_notebook_sessions"
description: "`aws_athena_notebook_sessions` reads the real Athena for Apache Spark sessions associated with a notebook."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_notebook_sessions"})

    		ubx.Data(athena.NotebookSessions, "example", athena.NotebookSessionsConfig{
    			NotebookId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NotebookSessions } from "./local-sdk/aws/sdk/typescript/aws/data/athena/notebook_sessions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_notebook_sessions" });

      data(NotebookSessions, "example", {
        notebookId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.athena import NotebookSessions, NotebookSessionsConfig

    def describe():
        ubx.intent("look up aws_athena_notebook_sessions")

        ubx.data(NotebookSessions, "example", NotebookSessionsConfig(
            notebook_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="notebook_id" type="string" required>
  The real, unique identifier of the notebook this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notebook_sessions_list" type="list(object)">
  &lt;p&gt;A list of notebook sessions.&lt;/p&gt; Returned by the lookup.
</ResponseField>
