---
title: "aws_athena_session_endpoint"
description: "`aws_athena_session_endpoint` reads the real, connection endpoint for an Athena for Apache Spark session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_session_endpoint"})

    		ubx.Data(athena.SessionEndpoint, "example", athena.SessionEndpointConfig{
    			SessionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SessionEndpoint } from "@ubx/sdk-aws/aws/data/athena/session_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_session_endpoint" });

      data(SessionEndpoint, "example", {
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.athena import SessionEndpoint, SessionEndpointConfig

    def describe():
        ubx.intent("look up aws_athena_session_endpoint")

        ubx.data(SessionEndpoint, "example", SessionEndpointConfig(
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="session_id" type="string" required>
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auth_token" type="string">
  The real, opaque auth token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="auth_token_expiration_time" type="string">
  The real timestamp of the auth token expiration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="endpoint_url" type="string">
  The real, resolvable URL of the endpoint this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
