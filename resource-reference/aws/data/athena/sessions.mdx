---
title: "aws_athena_sessions"
description: "`aws_athena_sessions` reads the real Athena for Apache Spark sessions on a work group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_athena_sessions"})

    		ubx.Data(athena.Sessions, "example", athena.SessionsConfig{
    			WorkGroup:   "example",
    			MaxResults:  1,
    			StateFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sessions } from "@ubx/sdk-aws/aws/data/athena/sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_athena_sessions" });

      data(Sessions, "example", {
        workGroup: "example",
        maxResults: 1,
        stateFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.athena import Sessions, SessionsConfig

    def describe():
        ubx.intent("look up aws_athena_sessions")

        ubx.data(Sessions, "example", SessionsConfig(
            work_group="example",
            max_results=1,
            state_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state_filter" type="string">
  Restricts results to this state. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="work_group" type="string" required>
  The real Amazon Athena workgroup this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  The real sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
