---
title: "aws_s3files_mount_targets"
description: "`aws_s3files_mount_targets` reads the real mount targets of an S3 file system."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3files "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/s3files"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3files_mount_targets"})

    		ubx.Data(s3files.MountTargets, "example", s3files.MountTargetsConfig{
    			AccessPointId: "example",
    			FileSystemId:  "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MountTargets } from "@ubx/sdk-aws/aws/data/s3files/mount_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3files_mount_targets" });

      data(MountTargets, "example", {
        accessPointId: "example",
        fileSystemId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3files import MountTargets, MountTargetsConfig

    def describe():
        ubx.intent("look up aws_s3files_mount_targets")

        ubx.data(MountTargets, "example", MountTargetsConfig(
            access_point_id="example",
            file_system_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_point_id" type="string">
  The real, unique identifier of the access point this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="file_system_id" type="string">
  The real, unique identifier of the file system this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mount_targets" type="list(object)">
  The real targets of the mount. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
