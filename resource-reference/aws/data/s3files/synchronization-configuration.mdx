---
title: "aws_s3files_synchronization_configuration"
description: "`aws_s3files_synchronization_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3files_synchronization_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3files "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3files"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3files_synchronization_configuration"})

    		ubx.Data(s3files.SynchronizationConfiguration, "example", s3files.SynchronizationConfigurationConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SynchronizationConfiguration } from "@ubx/sdk-aws/aws/data/s3files/synchronization_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3files_synchronization_configuration" });

      data(SynchronizationConfiguration, "example", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3files import SynchronizationConfiguration, SynchronizationConfigurationConfig

    def describe():
        ubx.intent("look up aws_s3files_synchronization_configuration")

        ubx.data(SynchronizationConfiguration, "example", SynchronizationConfigurationConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_system_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expiration_data_rules" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="import_data_rules" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_version_number" type="number">
  Returned by the lookup.
</ResponseField>
