---
title: "aws_codeartifact_repository_permissions_policy"
description: "`aws_codeartifact_repository_permissions_policy` reads the real resource-based policy attached to a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_repository_permissions_policy"})

    		ubx.Data(codeartifact.RepositoryPermissionsPolicy, "example", codeartifact.RepositoryPermissionsPolicyConfig{
    			Domain:      "example",
    			Repository:  "example",
    			DomainOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositoryPermissionsPolicy } from "@ubx/sdk-aws/aws/data/codeartifact/repository_permissions_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_repository_permissions_policy" });

      data(RepositoryPermissionsPolicy, "example", {
        domain: "example",
        repository: "example",
        domainOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import RepositoryPermissionsPolicy, RepositoryPermissionsPolicyConfig

    def describe():
        ubx.intent("look up aws_codeartifact_repository_permissions_policy")

        ubx.data(RepositoryPermissionsPolicy, "example", RepositoryPermissionsPolicyConfig(
            domain="example",
            repository="example",
            domain_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="object">
  &lt;p&gt; An CodeArtifact resource policy that contains a resource ARN, document details, and a revision. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="document" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="revision" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
