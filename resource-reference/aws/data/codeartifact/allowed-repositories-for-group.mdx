---
title: "aws_codeartifact_allowed_repositories_for_group"
description: "`aws_codeartifact_allowed_repositories_for_group` reads the real CodeArtifact repositories a package group's own origin control policy allows."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_allowed_repositories_for_group"})

    		ubx.Data(codeartifact.AllowedRepositoriesForGroup, "example", codeartifact.AllowedRepositoriesForGroupConfig{
    			Domain:                "example",
    			OriginRestrictionType: "example",
    			PackageGroup:          "example",
    			DomainOwner:           "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AllowedRepositoriesForGroup } from "./local-sdk/aws/sdk/typescript/aws/data/codeartifact/allowed_repositories_for_group.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_codeartifact_allowed_repositories_for_group",
      });

      data(AllowedRepositoriesForGroup, "example", {
        domain: "example",
        originRestrictionType: "example",
        packageGroup: "example",
        domainOwner: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import AllowedRepositoriesForGroup, AllowedRepositoriesForGroupConfig

    def describe():
        ubx.intent("look up aws_codeartifact_allowed_repositories_for_group")

        ubx.data(AllowedRepositoriesForGroup, "example", AllowedRepositoriesForGroupConfig(
            domain="example",
            origin_restriction_type="example",
            package_group="example",
            domain_owner="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="origin_restriction_type" type="string" required>
  The real, discriminating type of the origin restriction this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package_group" type="string" required>
  The real package group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allowed_repositories" type="list(string)">
  The real repositories this object is allowed to access. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
