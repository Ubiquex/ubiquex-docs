---
title: "aws_codeartifact_package_version_readme"
description: "`aws_codeartifact_package_version_readme` reads the real README content of a specific package version in a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_version_readme"})

    		ubx.Data(codeartifact.PackageVersionReadme, "example", codeartifact.PackageVersionReadmeConfig{
    			Domain:         "example",
    			Format:         "example",
    			Package:        "example",
    			PackageVersion: "example",
    			Repository:     "example",
    			DomainOwner:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersionReadme } from "@ubx/sdk-aws/aws/data/codeartifact/package_version_readme";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_version_readme" });

      data(PackageVersionReadme, "example", {
        domain: "example",
        format: "example",
        package: "example",
        packageVersion: "example",
        repository: "example",
        domainOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersionReadme, PackageVersionReadmeConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_version_readme")

        ubx.data(PackageVersionReadme, "example", PackageVersionReadmeConfig(
            domain="example",
            format="example",
            package="example",
            package_version="example",
            repository="example",
            domain_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  The real package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package_version" type="string" required>
  The real version of the package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="readme" type="string">
  The real, free-text README content for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_revision" type="string">
  The real revision of this object's own version. **(AI-inferred)** Returned by the lookup.
</ResponseField>
