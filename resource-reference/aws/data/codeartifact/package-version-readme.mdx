---
title: "aws_codeartifact_package_version_readme"
description: "`aws_codeartifact_package_version_readme` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeartifact_package_version_readme` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_version_readme"})

    		ubx.Data(codeartifact.PackageVersionReadme, "example", codeartifact.PackageVersionReadmeConfig{
    			Domain:         "example",
    			Format:         "example",
    			Package:        "example",
    			PackageVersion: "example",
    			Repository:     "example",
    			DomainOwner:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersionReadme } from "@ubx/sdk-aws/aws/data/codeartifact/package_version_readme";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_version_readme" });

      data(PackageVersionReadme, "example", {
        domain: "example",
        format: "example",
        package: "example",
        packageVersion: "example",
        repository: "example",
        domainOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersionReadme, PackageVersionReadmeConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_version_readme")

        ubx.data(PackageVersionReadme, "example", PackageVersionReadmeConfig(
            domain="example",
            format="example",
            package="example",
            package_version="example",
            repository="example",
            domain_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="package_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="namespace" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="readme" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="version_revision" type="string">
  Returned by the lookup.
</ResponseField>
