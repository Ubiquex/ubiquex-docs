---
title: "aws_codeartifact_repositories_in_domain"
description: "`aws_codeartifact_repositories_in_domain` reads the real repositories within a CodeArtifact domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_repositories_in_domain"})

    		ubx.Data(codeartifact.RepositoriesInDomain, "example", codeartifact.RepositoriesInDomainConfig{
    			Domain:               "example",
    			AdministratorAccount: "example",
    			DomainOwner:          "example",
    			MaxResults:           1,
    			RepositoryPrefix:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositoriesInDomain } from "@ubx/sdk-aws/aws/data/codeartifact/repositories_in_domain";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_repositories_in_domain" });

      data(RepositoriesInDomain, "example", {
        domain: "example",
        administratorAccount: "example",
        domainOwner: "example",
        maxResults: 1,
        repositoryPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import RepositoriesInDomain, RepositoriesInDomainConfig

    def describe():
        ubx.intent("look up aws_codeartifact_repositories_in_domain")

        ubx.data(RepositoriesInDomain, "example", RepositoriesInDomainConfig(
            domain="example",
            administrator_account="example",
            domain_owner="example",
            max_results=1,
            repository_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="administrator_account" type="string">
  The real, delegated administrator account for this service. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_prefix" type="string">
  The real S3 key prefix used for the repository. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repositories" type="list(object)">
  The real repositories included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
