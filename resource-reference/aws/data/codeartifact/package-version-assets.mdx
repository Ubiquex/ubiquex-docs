---
title: "aws_codeartifact_package_version_assets"
description: "`aws_codeartifact_package_version_assets` reads the real assets within a specific package version in a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_version_assets"})

    		ubx.Data(codeartifact.PackageVersionAssets, "example", codeartifact.PackageVersionAssetsConfig{
    			Domain:         "example",
    			Format:         "example",
    			Package:        "example",
    			PackageVersion: "example",
    			Repository:     "example",
    			DomainOwner:    "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersionAssets } from "@ubx/sdk-aws/aws/data/codeartifact/package_version_assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_version_assets" });

      data(PackageVersionAssets, "example", {
        domain: "example",
        format: "example",
        package: "example",
        packageVersion: "example",
        repository: "example",
        domainOwner: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersionAssets, PackageVersionAssetsConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_version_assets")

        ubx.data(PackageVersionAssets, "example", PackageVersionAssetsConfig(
            domain="example",
            format="example",
            package="example",
            package_version="example",
            repository="example",
            domain_owner="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  The real package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package_version" type="string" required>
  The real version of the package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assets" type="list(object)">
  The real assets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_revision" type="string">
  The real revision of this object's own version. **(AI-inferred)** Returned by the lookup.
</ResponseField>
