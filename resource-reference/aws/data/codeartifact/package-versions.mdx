---
title: "aws_codeartifact_package_versions"
description: "`aws_codeartifact_package_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeartifact_package_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_versions"})

    		ubx.Data(codeartifact.PackageVersions, "example", codeartifact.PackageVersionsConfig{
    			Domain:      "example",
    			Format:      "example",
    			Package:     "example",
    			Repository:  "example",
    			DomainOwner: "example",
    			MaxResults:  1,
    			OriginType:  "example",
    			SortBy:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersions } from "@ubx/sdk-aws/aws/data/codeartifact/package_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_versions" });

      data(PackageVersions, "example", {
        domain: "example",
        format: "example",
        package: "example",
        repository: "example",
        domainOwner: "example",
        maxResults: 1,
        originType: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersions, PackageVersionsConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_versions")

        ubx.data(PackageVersions, "example", PackageVersionsConfig(
            domain="example",
            format="example",
            package="example",
            repository="example",
            domain_owner="example",
            max_results=1,
            origin_type="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="origin_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_display_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="namespace" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
