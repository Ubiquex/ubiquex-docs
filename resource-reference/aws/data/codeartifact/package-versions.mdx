---
title: "aws_codeartifact_package_versions"
description: "`aws_codeartifact_package_versions` reads the real, published versions of a package within a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_versions"})

    		ubx.Data(codeartifact.PackageVersions, "example", codeartifact.PackageVersionsConfig{
    			Domain:      "example",
    			Format:      "example",
    			Package:     "example",
    			Repository:  "example",
    			DomainOwner: "example",
    			MaxResults:  1,
    			OriginType:  "example",
    			SortBy:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersions } from "@ubx/sdk-aws/aws/data/codeartifact/package_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_versions" });

      data(PackageVersions, "example", {
        domain: "example",
        format: "example",
        package: "example",
        repository: "example",
        domainOwner: "example",
        maxResults: 1,
        originType: "example",
        sortBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersions, PackageVersionsConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_versions")

        ubx.data(PackageVersions, "example", PackageVersionsConfig(
            domain="example",
            format="example",
            package="example",
            repository="example",
            domain_owner="example",
            max_results=1,
            origin_type="example",
            sort_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="origin_type" type="string">
  The real, discriminating type of the origin this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  The real package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_display_version" type="string">
  The real version of the default display. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  The real versions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
