---
title: "aws_codeartifact_authorization_token"
description: "`aws_codeartifact_authorization_token` reads a real, temporary authorization token for authenticating a package manager client to CodeArtifact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_authorization_token"})

    		ubx.Data(codeartifact.AuthorizationToken, "example", codeartifact.AuthorizationTokenConfig{
    			Domain:          "example",
    			DomainOwner:     "example",
    			DurationSeconds: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AuthorizationToken } from "./local-sdk/aws/sdk/typescript/aws/data/codeartifact/authorization_token.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_authorization_token" });

      data(AuthorizationToken, "example", {
        domain: "example",
        domainOwner: "example",
        durationSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import AuthorizationToken, AuthorizationTokenConfig

    def describe():
        ubx.intent("look up aws_codeartifact_authorization_token")

        ubx.data(AuthorizationToken, "example", AuthorizationTokenConfig(
            domain="example",
            domain_owner="example",
            duration_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authorization_token" type="string">
  The real, opaque authorization token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>
