---
title: "aws_codeartifact_packages"
description: "`aws_codeartifact_packages` reads the real packages within a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_packages"})

    		ubx.Data(codeartifact.Packages, "example", codeartifact.PackagesConfig{
    			Domain:        "example",
    			Repository:    "example",
    			DomainOwner:   "example",
    			Format:        "example",
    			MaxResults:    1,
    			Namespace:     "example",
    			PackagePrefix: "example",
    			Publish:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Packages } from "@ubx/sdk-aws/aws/data/codeartifact/packages";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_packages" });

      data(Packages, "example", {
        domain: "example",
        repository: "example",
        domainOwner: "example",
        format: "example",
        maxResults: 1,
        namespace: "example",
        packagePrefix: "example",
        publish: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import Packages, PackagesConfig

    def describe():
        ubx.intent("look up aws_codeartifact_packages")

        ubx.data(Packages, "example", PackagesConfig(
            domain="example",
            repository="example",
            domain_owner="example",
            format="example",
            max_results=1,
            namespace="example",
            package_prefix="example",
            publish="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string">
  The real, requested output format of the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="package_prefix" type="string">
  The real S3 key prefix used for the package. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="publish" type="string">
  Whether this object is published. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="upstream" type="string">
  The real, upstream source of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="packages" type="list(object)">
  The real packages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
