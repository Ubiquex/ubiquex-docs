---
title: "aws_codeartifact_associated_package_group"
description: "`data_aws_codeartifact_associated_package_group` looks up a real, already-existing `aws_code_artifact_package_group`."
---

It applies real origin control and configuration consistently across every package in an `aws_code_artifact_domain` whose name matches the group's pattern, rather than a caller setting the same policy on each individual package by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_associated_package_group"})

    		ubx.Data(codeartifact.AssociatedPackageGroup, "example", codeartifact.AssociatedPackageGroupConfig{
    			Domain:      "example",
    			Format:      "example",
    			Package:     "example",
    			DomainOwner: "example",
    			Namespace:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedPackageGroup } from "@ubx/sdk-aws/aws/data/codeartifact/associated_package_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_associated_package_group" });

      data(AssociatedPackageGroup, "example", {
        domain: "example",
        format: "example",
        package: "example",
        domainOwner: "example",
        namespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import AssociatedPackageGroup, AssociatedPackageGroupConfig

    def describe():
        ubx.intent("look up aws_codeartifact_associated_package_group")

        ubx.data(AssociatedPackageGroup, "example", AssociatedPackageGroupConfig(
            domain="example",
            format="example",
            package="example",
            domain_owner="example",
            namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  The real package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="association_type" type="string">
  The real type of this association. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="package_group" type="object">
  &lt;p&gt; The description of the package group. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="contact_info" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="domain_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="domain_owner" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="origin_configuration" type="object">
      &lt;p&gt;The package group origin configuration that determines how package versions can enter repositories.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="restrictions" type="map(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="parent" type="object">
      &lt;p&gt;Information about the identifiers of a package group.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="arn" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="pattern" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pattern" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
