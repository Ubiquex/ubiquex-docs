---
title: "aws_codeartifact_package_version_dependencies"
description: "`aws_codeartifact_package_version_dependencies` reads the real declared dependencies of a specific package version in a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_package_version_dependencies"})

    		ubx.Data(codeartifact.PackageVersionDependencies, "example", codeartifact.PackageVersionDependenciesConfig{
    			Domain:         "example",
    			Format:         "example",
    			Package:        "example",
    			PackageVersion: "example",
    			Repository:     "example",
    			DomainOwner:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PackageVersionDependencies } from "@ubx/sdk-aws/aws/data/codeartifact/package_version_dependencies";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_package_version_dependencies" });

      data(PackageVersionDependencies, "example", {
        domain: "example",
        format: "example",
        package: "example",
        packageVersion: "example",
        repository: "example",
        domainOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import PackageVersionDependencies, PackageVersionDependenciesConfig

    def describe():
        ubx.intent("look up aws_codeartifact_package_version_dependencies")

        ubx.data(PackageVersionDependencies, "example", PackageVersionDependenciesConfig(
            domain="example",
            format="example",
            package="example",
            package_version="example",
            repository="example",
            domain_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package" type="string" required>
  The real package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="package_version" type="string" required>
  The real version of the package this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dependencies" type="list(object)">
  The real dependencies of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_revision" type="string">
  The real revision of this object's own version. **(AI-inferred)** Returned by the lookup.
</ResponseField>
