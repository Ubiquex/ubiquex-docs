---
title: "aws_codeartifact_repository_endpoint"
description: "`aws_codeartifact_repository_endpoint` reads the real, package-format-specific endpoint URL of a CodeArtifact repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_repository_endpoint"})

    		ubx.Data(codeartifact.RepositoryEndpoint, "example", codeartifact.RepositoryEndpointConfig{
    			Domain:       "example",
    			Format:       "example",
    			Repository:   "example",
    			DomainOwner:  "example",
    			EndpointType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositoryEndpoint } from "@ubx/sdk-aws/aws/data/codeartifact/repository_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_repository_endpoint" });

      data(RepositoryEndpoint, "example", {
        domain: "example",
        format: "example",
        repository: "example",
        domainOwner: "example",
        endpointType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import RepositoryEndpoint, RepositoryEndpointConfig

    def describe():
        ubx.intent("look up aws_codeartifact_repository_endpoint")

        ubx.data(RepositoryEndpoint, "example", RepositoryEndpointConfig(
            domain="example",
            format="example",
            repository="example",
            domain_owner="example",
            endpoint_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="endpoint_type" type="string">
  The real, discriminating type of the endpoint this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository" type="string" required>
  The real repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="repository_endpoint" type="string">
  The real endpoint used to access this repository. **(AI-inferred)** Returned by the lookup.
</ResponseField>
