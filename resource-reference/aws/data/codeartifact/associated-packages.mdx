---
title: "aws_codeartifact_associated_packages"
description: "`aws_codeartifact_associated_packages` reads the real packages associated with a CodeArtifact package group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_associated_packages"})

    		ubx.Data(codeartifact.AssociatedPackages, "example", codeartifact.AssociatedPackagesConfig{
    			Domain:       "example",
    			PackageGroup: "example",
    			DomainOwner:  "example",
    			MaxResults:   1,
    			Preview:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedPackages } from "@ubx/sdk-aws/aws/data/codeartifact/associated_packages";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_associated_packages" });

      data(AssociatedPackages, "example", {
        domain: "example",
        packageGroup: "example",
        domainOwner: "example",
        maxResults: 1,
        preview: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import AssociatedPackages, AssociatedPackagesConfig

    def describe():
        ubx.intent("look up aws_codeartifact_associated_packages")

        ubx.data(AssociatedPackages, "example", AssociatedPackagesConfig(
            domain="example",
            package_group="example",
            domain_owner="example",
            max_results=1,
            preview=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  The real domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  The real AWS account ID that owns the domain this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="package_group" type="string" required>
  The real package group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="preview" type="bool">
  The real, previewed content of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="packages" type="list(object)">
  The real packages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
