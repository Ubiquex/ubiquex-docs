---
title: "aws_codeartifact_associated_packages"
description: "`aws_codeartifact_associated_packages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeartifact_associated_packages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeartifact_associated_packages"})

    		ubx.Data(codeartifact.AssociatedPackages, "example", codeartifact.AssociatedPackagesConfig{
    			Domain:       "example",
    			PackageGroup: "example",
    			DomainOwner:  "example",
    			MaxResults:   1,
    			Preview:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedPackages } from "@ubx/sdk-aws/aws/data/codeartifact/associated_packages";

    export default stack("example", () => {
      intent({ summary: "look up aws_codeartifact_associated_packages" });

      data(AssociatedPackages, "example", {
        domain: "example",
        packageGroup: "example",
        domainOwner: "example",
        maxResults: 1,
        preview: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeartifact import AssociatedPackages, AssociatedPackagesConfig

    def describe():
        ubx.intent("look up aws_codeartifact_associated_packages")

        ubx.data(AssociatedPackages, "example", AssociatedPackagesConfig(
            domain="example",
            package_group="example",
            domain_owner="example",
            max_results=1,
            preview=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="package_group" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="preview" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="packages" type="list(object)">
  Returned by the lookup.
</ResponseField>
