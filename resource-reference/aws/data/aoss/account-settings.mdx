---
title: "aws_aoss_account_settings"
description: "`aws_aoss_account_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aoss_account_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aoss "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aoss"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aoss_account_settings"})

    		ubx.Data(aoss.AccountSettings, "example", aoss.AccountSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountSettings } from "@ubx/sdk-aws/aws/data/aoss/account_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_aoss_account_settings" });

      data(AccountSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aoss import AccountSettings, AccountSettingsConfig

    def describe():
        ubx.intent("look up aws_aoss_account_settings")

        ubx.data(AccountSettings, "example", AccountSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_settings_detail" type="object">
  &lt;p&gt;OpenSearch Serverless-related information for the current account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="capacity_limits" type="object">
      &lt;p&gt;The maximum capacity limits for all OpenSearch Serverless collections, in OpenSearch Compute Units (OCUs). These limits are used to scale your collections based on the current workload. For more information, see &lt;a href="https://docs.aws.amazon.com/opensearch-service/latest/developerguide/serverless-scaling.html"&gt;Managing capacity limits for Amazon OpenSearch Serverless&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="max_indexing_capacity_in_ocu" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="max_search_capacity_in_ocu" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
