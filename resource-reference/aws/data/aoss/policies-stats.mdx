---
title: "aws_aoss_policies_stats"
description: "`aws_aoss_policies_stats` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aoss_policies_stats` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aoss "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aoss"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aoss_policies_stats"})

    		ubx.Data(aoss.PoliciesStats, "example", aoss.PoliciesStatsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PoliciesStats } from "@ubx/sdk-aws/aws/data/aoss/policies_stats";

    export default stack("example", () => {
      intent({ summary: "look up aws_aoss_policies_stats" });

      data(PoliciesStats, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aoss import PoliciesStats, PoliciesStatsConfig

    def describe():
        ubx.intent("look up aws_aoss_policies_stats")

        ubx.data(PoliciesStats, "example", PoliciesStatsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="access_policy_stats" type="object">
  &lt;p&gt;Statistics for an OpenSearch Serverless access policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_policy_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lifecycle_policy_stats" type="object">
  &lt;p&gt;Statistics for an OpenSearch Serverless lifecycle policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="retention_policy_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="security_config_stats" type="object">
  &lt;p&gt;Statistics for an OpenSearch Serverless security configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="saml_config_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="security_policy_stats" type="object">
  &lt;p&gt;Statistics for an OpenSearch Serverless security policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="encryption_policy_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network_policy_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_policy_count" type="number">
  Returned by the lookup.
</ResponseField>
