---
title: "aws_kinesisvideo_signaling_channels"
description: "`aws_kinesisvideo_signaling_channels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_signaling_channels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_signaling_channels"})

    		ubx.Data(kinesisvideo.SignalingChannels, "example", kinesisvideo.SignalingChannelsConfig{
    			ChannelNameCondition: map[string]any{"comparison_operator": "example"},
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SignalingChannels } from "@ubx/sdk-aws/aws/data/kinesisvideo/signaling_channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_signaling_channels" });

      data(SignalingChannels, "example", {
        channelNameCondition: { comparisonOperator: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import SignalingChannels, SignalingChannelsConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_signaling_channels")

        ubx.data(SignalingChannels, "example", SignalingChannelsConfig(
            channel_name_condition={"comparison_operator": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_name_condition" type="object">
  &lt;p&gt;An optional input parameter for the &lt;code&gt;ListSignalingChannels&lt;/code&gt; API. When this parameter is specified while invoking &lt;code&gt;ListSignalingChannels&lt;/code&gt;, the API returns only the channels that satisfy a condition specified in &lt;code&gt;ChannelNameCondition&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="comparison_operator" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="comparison_value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_info_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
