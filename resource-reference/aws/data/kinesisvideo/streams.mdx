---
title: "aws_kinesisvideo_streams"
description: "`aws_kinesisvideo_streams` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_streams` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_streams"})

    		ubx.Data(kinesisvideo.Streams, "example", kinesisvideo.StreamsConfig{
    			MaxResults:          1,
    			StreamNameCondition: map[string]any{"comparison_operator": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Streams } from "@ubx/sdk-aws/aws/data/kinesisvideo/streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_streams" });

      data(Streams, "example", {
        maxResults: 1,
        streamNameCondition: { comparisonOperator: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import Streams, StreamsConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_streams")

        ubx.data(Streams, "example", StreamsConfig(
            max_results=1,
            stream_name_condition={"comparison_operator": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name_condition" type="object">
  &lt;p&gt;Specifies the condition that streams must satisfy to be returned when you list streams (see the &lt;code&gt;ListStreams&lt;/code&gt; API). A condition has a comparison operation and a value. Currently, you can specify only the &lt;code&gt;BEGINS_WITH&lt;/code&gt; operator, which finds streams whose names start with a given prefix. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="comparison_operator" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="comparison_value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_info_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
