---
title: "aws_kinesisvideo_dashstreaming_session_url"
description: "`aws_kinesisvideo_dashstreaming_session_url` reads a real, generated URL for playing back a Kinesis Video Streams stream via MPEG-DASH."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_dashstreaming_session_url"})

    		ubx.Data(kinesisvideo.DashstreamingSessionUrl, "example", kinesisvideo.DashstreamingSessionUrlConfig{
    			DashfragmentSelector:       map[string]any{"fragment_selector_type": "example"},
    			DisplayFragmentNumber:      "example",
    			DisplayFragmentTimestamp:   "example",
    			Expires:                    1,
    			MaxManifestFragmentResults: 1,
    			PlaybackMode:               "example",
    			StreamArn:                  "arn:aws:iam::123456789012:policy/example",
    			StreamName:                 "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DashstreamingSessionUrl } from "./local-sdk/aws/sdk/typescript/aws/data/kinesisvideo/dashstreaming_session_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_dashstreaming_session_url" });

      data(DashstreamingSessionUrl, "example", {
        dashfragmentSelector: { fragmentSelectorType: "example" },
        displayFragmentNumber: "example",
        displayFragmentTimestamp: "example",
        expires: 1,
        maxManifestFragmentResults: 1,
        playbackMode: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import DashstreamingSessionUrl, DashstreamingSessionUrlConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_dashstreaming_session_url")

        ubx.data(DashstreamingSessionUrl, "example", DashstreamingSessionUrlConfig(
            dashfragment_selector={"fragment_selector_type": "example"},
            display_fragment_number="example",
            display_fragment_timestamp="example",
            expires=1,
            max_manifest_fragment_results=1,
            playback_mode="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dashfragment_selector" type="object">
  &lt;p&gt;Contains the range of timestamps for the requested media, and the source of the timestamps. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="fragment_selector_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timestamp_range" type="object">
      &lt;p&gt;The start and end of the timestamp range for the requested media.&lt;/p&gt; &lt;p&gt;This value should not be present if &lt;code&gt;PlaybackType&lt;/code&gt; is &lt;code&gt;LIVE&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;The values in &lt;code&gt;DASHimestampRange&lt;/code&gt; are inclusive. Fragments that start exactly at or after the start time are included in the session. Fragments that start before the start time and continue past it are not included in the session.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_timestamp" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="start_timestamp" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_fragment_number" type="string">
  The real number of the display fragment. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="display_fragment_timestamp" type="string">
  The real timestamp of the display fragment. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="expires" type="number">
  The real timestamp this object expires. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_manifest_fragment_results" type="number">
  The real, maximum number of manifest fragment results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="playback_mode" type="string">
  The real, current playback mode of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  The real Amazon Resource Name (ARN) of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  The real name of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dashstreaming_session_url" type="string">
  The real, resolvable URL of the dashstreaming session this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
