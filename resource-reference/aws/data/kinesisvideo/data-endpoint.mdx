---
title: "aws_kinesisvideo_data_endpoint"
description: "`aws_kinesisvideo_data_endpoint` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_data_endpoint` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_data_endpoint"})

    		ubx.Data(kinesisvideo.DataEndpoint, "example", kinesisvideo.DataEndpointConfig{
    			Apiname:    "example",
    			StreamArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamName: "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataEndpoint } from "@ubx/sdk-aws/aws/data/kinesisvideo/data_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_data_endpoint" });

      data(DataEndpoint, "example", {
        apiname: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import DataEndpoint, DataEndpointConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_data_endpoint")

        ubx.data(DataEndpoint, "example", DataEndpointConfig(
            apiname="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="apiname" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_endpoint" type="string">
  Returned by the lookup.
</ResponseField>
