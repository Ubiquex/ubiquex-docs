---
title: "aws_kinesisvideo_ice_server_config"
description: "`aws_kinesisvideo_ice_server_config` reads real, temporary ICE server (TURN/STUN) credentials for a Kinesis Video Streams signaling channel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_ice_server_config"})

    		ubx.Data(kinesisvideo.IceServerConfig, "example", kinesisvideo.IceServerConfigConfig{
    			ChannelArn: "arn:aws:iam::123456789012:policy/example",
    			ClientId:   "example",
    			Service:    "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IceServerConfig } from "./local-sdk/aws/sdk/typescript/aws/data/kinesisvideo/ice_server_config.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_ice_server_config" });

      data(IceServerConfig, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        clientId: "example",
        service: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import IceServerConfig, IceServerConfigConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_ice_server_config")

        ubx.data(IceServerConfig, "example", IceServerConfigConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            client_id="example",
            service="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="client_id" type="string">
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ice_server_list" type="list(object)">
  The real ice servers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
