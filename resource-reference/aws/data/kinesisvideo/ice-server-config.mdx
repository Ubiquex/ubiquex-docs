---
title: "aws_kinesisvideo_ice_server_config"
description: "`aws_kinesisvideo_ice_server_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_ice_server_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_ice_server_config"})

    		ubx.Data(kinesisvideo.IceServerConfig, "example", kinesisvideo.IceServerConfigConfig{
    			ChannelArn: "arn:aws:iam::123456789012:policy/example",
    			ClientId:   "example",
    			Service:    "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IceServerConfig } from "@ubx/sdk-aws/aws/data/kinesisvideo/ice_server_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_ice_server_config" });

      data(IceServerConfig, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        clientId: "example",
        service: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import IceServerConfig, IceServerConfigConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_ice_server_config")

        ubx.data(IceServerConfig, "example", IceServerConfigConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            client_id="example",
            service="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="client_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="username" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ice_server_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
