---
title: "aws_kinesisvideo_media_storage_configuration"
description: "`aws_kinesisvideo_media_storage_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_media_storage_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_media_storage_configuration"})

    		ubx.Data(kinesisvideo.MediaStorageConfiguration, "example", kinesisvideo.MediaStorageConfigurationConfig{
    			ChannelArn:  "arn:aws:iam::123456789012:policy/example",
    			ChannelName: "example-channel-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediaStorageConfiguration } from "@ubx/sdk-aws/aws/data/kinesisvideo/media_storage_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_media_storage_configuration" });

      data(MediaStorageConfiguration, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        channelName: "example-channel-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import MediaStorageConfiguration, MediaStorageConfigurationConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_media_storage_configuration")

        ubx.data(MediaStorageConfiguration, "example", MediaStorageConfigurationConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            channel_name="example-channel-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="channel_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="media_storage_configuration" type="object">
  &lt;p&gt;A structure that encapsulates, or contains, the media storage configuration properties.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;If &lt;code&gt;StorageStatus&lt;/code&gt; is enabled, the data will be stored in the &lt;code&gt;StreamARN&lt;/code&gt; provided. In order for WebRTC Ingestion to work, the stream must have data retention enabled.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;If &lt;code&gt;StorageStatus&lt;/code&gt; is disabled, no data will be stored, and the &lt;code&gt;StreamARN&lt;/code&gt; parameter will not be needed. &lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="stream_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
