---
title: "aws_kinesisvideo_stream_storage_configuration"
description: "`aws_kinesisvideo_stream_storage_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_stream_storage_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_stream_storage_configuration"})

    		ubx.Data(kinesisvideo.StreamStorageConfiguration, "example", kinesisvideo.StreamStorageConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamStorageConfiguration } from "@ubx/sdk-aws/aws/data/kinesisvideo/stream_storage_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_stream_storage_configuration" });

      data(StreamStorageConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import StreamStorageConfiguration, StreamStorageConfigurationConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_stream_storage_configuration")

        ubx.data(StreamStorageConfiguration, "example", StreamStorageConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="stream_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_storage_configuration" type="object">
  &lt;p&gt;The configuration for stream storage, including the default storage tier for stream data. This configuration determines how stream data is stored and accessed, with different tiers offering varying levels of performance and cost optimization.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_storage_tier" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
