---
title: "aws_kinesisvideo_edge_configuration"
description: "`aws_kinesisvideo_edge_configuration` reads the real edge configuration of a Kinesis Video Streams stream, when local recording is enabled."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_edge_configuration"})

    		ubx.Data(kinesisvideo.EdgeConfiguration, "example", kinesisvideo.EdgeConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EdgeConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/kinesisvideo/edge_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_edge_configuration" });

      data(EdgeConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import EdgeConfiguration, EdgeConfigurationConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_edge_configuration")

        ubx.data(EdgeConfiguration, "example", EdgeConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="edge_agent_status" type="object">
  &lt;p&gt;An object that contains the latest status details for an edge agent's recorder and uploader jobs. Use this information to determine the current health of an edge agent.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="last_recorder_status" type="object">
      &lt;p&gt;The latest status of a stream's edge recording job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="job_status_details" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_collected_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_updated_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="recorder_status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_uploader_status" type="object">
      &lt;p&gt;The latest status of a stream’s edge to cloud uploader job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="job_status_details" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_collected_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_updated_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="uploader_status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="edge_config" type="object">
  &lt;p&gt;A description of the stream's edge configuration that will be used to sync with the Edge Agent IoT Greengrass component. The Edge Agent component will run on an IoT Hub Device setup at your premise.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="deletion_config" type="object">
      &lt;p&gt;The configuration details required to delete the connection of the stream from the Edge Agent.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delete_after_upload" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="edge_retention_in_hours" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="local_size_config" type="object">
          &lt;p&gt;The configuration details that include the maximum size of the media (&lt;code&gt;MaxLocalMediaSizeInMB&lt;/code&gt;) that you want to store for a stream on the Edge Agent, as well as the strategy that should be used (&lt;code&gt;StrategyOnFullSize&lt;/code&gt;) when a stream's maximum size has been reached.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="max_local_media_size_in_mb" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="strategy_on_full_size" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hub_device_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="recorder_config" type="object" required>
      &lt;p&gt;The recorder configuration consists of the local &lt;code&gt;MediaSourceConfig&lt;/code&gt; details that are used as credentials to access the local media files streamed on the camera. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="media_source_config" type="object" required>
          &lt;p&gt;The configuration details that consist of the credentials required (&lt;code&gt;MediaUriSecretArn&lt;/code&gt; and &lt;code&gt;MediaUriType&lt;/code&gt;) to access the media files that are streamed to the camera.&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="media_uri_secret_arn" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="media_uri_type" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schedule_config" type="object">
          &lt;p&gt;This API enables you to specify the duration that the camera, or local media file, should record onto the Edge Agent. The &lt;code&gt;ScheduleConfig&lt;/code&gt; consists of the &lt;code&gt;ScheduleExpression&lt;/code&gt; and the &lt;code&gt;DurationInMinutes&lt;/code&gt; attributes. &lt;/p&gt; &lt;p&gt;If the &lt;code&gt;ScheduleConfig&lt;/code&gt; is not provided in the &lt;code&gt;RecorderConfig&lt;/code&gt;, then the Edge Agent will always be set to recording mode.&lt;/p&gt; &lt;p&gt;If the &lt;code&gt;ScheduleConfig&lt;/code&gt; is not provided in the &lt;code&gt;UploaderConfig&lt;/code&gt;, then the Edge Agent will upload at regular intervals (every 1 hour).&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="duration_in_seconds" type="number" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="schedule_expression" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uploader_config" type="object">
      &lt;p&gt;The configuration that consists of the &lt;code&gt;ScheduleExpression&lt;/code&gt; and the &lt;code&gt;DurationInMinutes&lt;/code&gt; details that specify the scheduling to record from a camera, or local media file, onto the Edge Agent. If the &lt;code&gt;ScheduleConfig&lt;/code&gt; is not provided in the &lt;code&gt;UploaderConfig&lt;/code&gt;, then the Edge Agent will upload at regular intervals (every 1 hour). &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="schedule_config" type="object" required>
          &lt;p&gt;This API enables you to specify the duration that the camera, or local media file, should record onto the Edge Agent. The &lt;code&gt;ScheduleConfig&lt;/code&gt; consists of the &lt;code&gt;ScheduleExpression&lt;/code&gt; and the &lt;code&gt;DurationInMinutes&lt;/code&gt; attributes. &lt;/p&gt; &lt;p&gt;If the &lt;code&gt;ScheduleConfig&lt;/code&gt; is not provided in the &lt;code&gt;RecorderConfig&lt;/code&gt;, then the Edge Agent will always be set to recording mode.&lt;/p&gt; &lt;p&gt;If the &lt;code&gt;ScheduleConfig&lt;/code&gt; is not provided in the &lt;code&gt;UploaderConfig&lt;/code&gt;, then the Edge Agent will upload at regular intervals (every 1 hour).&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="duration_in_seconds" type="number" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="schedule_expression" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="failed_status_details" type="string">
  The real, detailed information about the failed status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  The real Amazon Resource Name (ARN) of the stream this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_name" type="string">
  The real name of the stream this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_status" type="string">
  The real, current status of the sync this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
