---
title: "aws_kinesisvideo_images"
description: "`aws_kinesisvideo_images` reads a real, generated image extracted from a Kinesis Video Streams stream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_images"})

    		ubx.Data(kinesisvideo.Images, "example", kinesisvideo.ImagesConfig{
    			EndTimestamp:      "example",
    			Format:            "example",
    			ImageSelectorType: "example",
    			StartTimestamp:    "example",
    			FormatConfig:      map[string]string{"managed-by": "ubx"},
    			HeightPixels:      1,
    			MaxResults:        1,
    			SamplingInterval:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Images } from "@ubx/sdk-aws/aws/data/kinesisvideo/images";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_images" });

      data(Images, "example", {
        endTimestamp: "example",
        format: "example",
        imageSelectorType: "example",
        startTimestamp: "example",
        formatConfig: { "managed-by": "ubx" },
        heightPixels: 1,
        maxResults: 1,
        samplingInterval: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import Images, ImagesConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_images")

        ubx.data(Images, "example", ImagesConfig(
            end_timestamp="example",
            format="example",
            image_selector_type="example",
            start_timestamp="example",
            format_config={"managed-by": "ubx"},
            height_pixels=1,
            max_results=1,
            sampling_interval=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_timestamp" type="string" required>
  The real timestamp of the end. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  The real, requested output format of the response. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="format_config" type="map(string)">
  The real configuration of the format. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="height_pixels" type="number">
  The real height of this object, in pixels. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="image_selector_type" type="string" required>
  The real, discriminating type of the image selector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sampling_interval" type="number">
  The real interval samples are collected at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_timestamp" type="string" required>
  The real timestamp of the start. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  The real Amazon Resource Name (ARN) of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  The real name of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="width_pixels" type="number">
  The real width of this object, in pixels. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="images" type="list(object)">
  The real images included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
