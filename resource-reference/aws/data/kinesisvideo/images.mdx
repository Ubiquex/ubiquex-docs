---
title: "aws_kinesisvideo_images"
description: "`aws_kinesisvideo_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_images"})

    		ubx.Data(kinesisvideo.Images, "example", kinesisvideo.ImagesConfig{
    			EndTimestamp:      "example",
    			Format:            "example",
    			ImageSelectorType: "example",
    			StartTimestamp:    "example",
    			FormatConfig:      map[string]string{"managed-by": "ubx"},
    			HeightPixels:      1,
    			MaxResults:        1,
    			SamplingInterval:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Images } from "@ubx/sdk-aws/aws/data/kinesisvideo/images";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_images" });

      data(Images, "example", {
        endTimestamp: "example",
        format: "example",
        imageSelectorType: "example",
        startTimestamp: "example",
        formatConfig: { "managed-by": "ubx" },
        heightPixels: 1,
        maxResults: 1,
        samplingInterval: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import Images, ImagesConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_images")

        ubx.data(Images, "example", ImagesConfig(
            end_timestamp="example",
            format="example",
            image_selector_type="example",
            start_timestamp="example",
            format_config={"managed-by": "ubx"},
            height_pixels=1,
            max_results=1,
            sampling_interval=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_timestamp" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="format" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="format_config" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="height_pixels" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="image_selector_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sampling_interval" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_timestamp" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="width_pixels" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="images" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
