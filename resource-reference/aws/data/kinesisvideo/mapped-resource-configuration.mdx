---
title: "aws_kinesisvideo_mapped_resource_configuration"
description: "`aws_kinesisvideo_mapped_resource_configuration` reads a real Kinesis Video Streams resource, such as a channel, mapped to another AWS resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_mapped_resource_configuration"})

    		ubx.Data(kinesisvideo.MappedResourceConfiguration, "example", kinesisvideo.MappedResourceConfigurationConfig{
    			MaxResults: 1,
    			StreamArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamName: "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MappedResourceConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/kinesisvideo/mapped_resource_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_mapped_resource_configuration" });

      data(MappedResourceConfiguration, "example", {
        maxResults: 1,
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import MappedResourceConfiguration, MappedResourceConfigurationConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_mapped_resource_configuration")

        ubx.data(MappedResourceConfiguration, "example", MappedResourceConfigurationConfig(
            max_results=1,
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  The real Amazon Resource Name (ARN) of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  The real name of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mapped_resource_configuration_list" type="list(object)">
  The real mapped resource configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
