---
title: "aws_kinesisvideo_hlsstreaming_session_url"
description: "`aws_kinesisvideo_hlsstreaming_session_url` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_hlsstreaming_session_url` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_hlsstreaming_session_url"})

    		ubx.Data(kinesisvideo.HlsstreamingSessionUrl, "example", kinesisvideo.HlsstreamingSessionUrlConfig{
    			ContainerFormat:                 "example",
    			DiscontinuityMode:               "example",
    			DisplayFragmentTimestamp:        "example",
    			Expires:                         1,
    			HlsfragmentSelector:             map[string]any{"fragment_selector_type": "example"},
    			MaxMediaPlaylistFragmentResults: 1,
    			PlaybackMode:                    "example",
    			StreamArn:                       "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HlsstreamingSessionUrl } from "@ubx/sdk-aws/aws/data/kinesisvideo/hlsstreaming_session_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_hlsstreaming_session_url" });

      data(HlsstreamingSessionUrl, "example", {
        containerFormat: "example",
        discontinuityMode: "example",
        displayFragmentTimestamp: "example",
        expires: 1,
        hlsfragmentSelector: { fragmentSelectorType: "example" },
        maxMediaPlaylistFragmentResults: 1,
        playbackMode: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import HlsstreamingSessionUrl, HlsstreamingSessionUrlConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_hlsstreaming_session_url")

        ubx.data(HlsstreamingSessionUrl, "example", HlsstreamingSessionUrlConfig(
            container_format="example",
            discontinuity_mode="example",
            display_fragment_timestamp="example",
            expires=1,
            hlsfragment_selector={"fragment_selector_type": "example"},
            max_media_playlist_fragment_results=1,
            playback_mode="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_format" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="discontinuity_mode" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="display_fragment_timestamp" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="expires" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="hlsfragment_selector" type="object">
  &lt;p&gt;Contains the range of timestamps for the requested media, and the source of the timestamps.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="fragment_selector_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timestamp_range" type="object">
      &lt;p&gt;The start and end of the timestamp range for the requested media.&lt;/p&gt; &lt;p&gt;This value should not be present if &lt;code&gt;PlaybackType&lt;/code&gt; is &lt;code&gt;LIVE&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_timestamp" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="start_timestamp" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_media_playlist_fragment_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="playback_mode" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hlsstreaming_session_url" type="string">
  Returned by the lookup.
</ResponseField>
