---
title: "aws_kinesisvideo_clip"
description: "`aws_kinesisvideo_clip` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_clip` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_clip"})

    		ubx.Data(kinesisvideo.Clip, "example", kinesisvideo.ClipConfig{
    			ClipFragmentSelector: map[string]any{"fragment_selector_type": "example"},
    			StreamArn:            "arn:aws:iam::123456789012:policy/example",
    			StreamName:           "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Clip } from "@ubx/sdk-aws/aws/data/kinesisvideo/clip";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_clip" });

      data(Clip, "example", {
        clipFragmentSelector: { fragmentSelectorType: "example" },
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import Clip, ClipConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_clip")

        ubx.data(Clip, "example", ClipConfig(
            clip_fragment_selector={"fragment_selector_type": "example"},
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="clip_fragment_selector" type="object" required>
  &lt;p&gt;Describes the timestamp range and timestamp origin of a range of fragments.&lt;/p&gt; &lt;p&gt;Fragments that have duplicate producer timestamps are deduplicated. This means that if producers are producing a stream of fragments with producer timestamps that are approximately equal to the true clock time, the clip will contain all of the fragments within the requested timestamp range. If some fragments are ingested within the same time range and very different points in time, only the oldest ingested collection of fragments are returned.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="fragment_selector_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="timestamp_range" type="object" required>
      &lt;p&gt;The range of timestamps for which to return fragments.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_timestamp" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="start_timestamp" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="payload" type="string">
  Returned by the lookup.
</ResponseField>
