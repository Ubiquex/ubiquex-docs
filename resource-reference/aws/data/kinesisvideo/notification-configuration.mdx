---
title: "aws_kinesisvideo_notification_configuration"
description: "`aws_kinesisvideo_notification_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_notification_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_notification_configuration"})

    		ubx.Data(kinesisvideo.NotificationConfiguration, "example", kinesisvideo.NotificationConfigurationConfig{
    			StreamArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamName: "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationConfiguration } from "@ubx/sdk-aws/aws/data/kinesisvideo/notification_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_notification_configuration" });

      data(NotificationConfiguration, "example", {
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import NotificationConfiguration, NotificationConfigurationConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_notification_configuration")

        ubx.data(NotificationConfiguration, "example", NotificationConfigurationConfig(
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="notification_configuration" type="object">
  &lt;p&gt;Use this API to configure Amazon Simple Notification Service (Amazon SNS) notifications for when fragments become available in a stream. If this parameter is null, the configuration will be deleted from the stream.&lt;/p&gt; &lt;p&gt;See &lt;a href="https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/notifications.html"&gt;Notifications in Kinesis Video Streams&lt;/a&gt; for more information.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="destination_config" type="object" required>
      &lt;p&gt;The structure that contains the information required to deliver a notification to a customer.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="uri" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
