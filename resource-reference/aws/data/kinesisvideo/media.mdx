---
title: "aws_kinesisvideo_media"
description: "`aws_kinesisvideo_media` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_media` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_media"})

    		ubx.Data(kinesisvideo.Media, "example", kinesisvideo.MediaConfig{
    			StartSelector: map[string]any{"start_selector_type": "example"},
    			StreamArn:     "arn:aws:iam::123456789012:policy/example",
    			StreamName:    "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Media } from "@ubx/sdk-aws/aws/data/kinesisvideo/media";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_media" });

      data(Media, "example", {
        startSelector: { startSelectorType: "example" },
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import Media, MediaConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_media")

        ubx.data(Media, "example", MediaConfig(
            start_selector={"start_selector_type": "example"},
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="start_selector" type="object" required>
  &lt;p&gt;Identifies the chunk on the Kinesis video stream where you want the &lt;code&gt;GetMedia&lt;/code&gt; API to start returning media data. You have the following options to identify the starting chunk: &lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;Choose the latest (or oldest) chunk.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;Identify a specific chunk. You can identify a specific chunk either by providing a fragment number or timestamp (server or producer). &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;Each chunk's metadata includes a continuation token as a Matroska (MKV) tag (&lt;code&gt;AWS_KINESISVIDEO_CONTINUATION_TOKEN&lt;/code&gt;). If your previous &lt;code&gt;GetMedia&lt;/code&gt; request terminated, you can use this tag value in your next &lt;code&gt;GetMedia&lt;/code&gt; request. The API then starts returning chunks starting where the last API ended.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="after_fragment_number" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="continuation_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_selector_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="payload" type="string">
  Returned by the lookup.
</ResponseField>
