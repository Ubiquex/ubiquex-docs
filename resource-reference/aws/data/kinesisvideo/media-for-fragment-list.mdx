---
title: "aws_kinesisvideo_media_for_fragment_list"
description: "`aws_kinesisvideo_media_for_fragment_list` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesisvideo_media_for_fragment_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisvideo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesisvideo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesisvideo_media_for_fragment_list"})

    		ubx.Data(kinesisvideo.MediaForFragmentList, "example", kinesisvideo.MediaForFragmentListConfig{
    			Fragments:  []string{"example"},
    			StreamArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamName: "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediaForFragmentList } from "@ubx/sdk-aws/aws/data/kinesisvideo/media_for_fragment_list";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesisvideo_media_for_fragment_list" });

      data(MediaForFragmentList, "example", {
        fragments: ["example"],
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesisvideo import MediaForFragmentList, MediaForFragmentListConfig

    def describe():
        ubx.intent("look up aws_kinesisvideo_media_for_fragment_list")

        ubx.data(MediaForFragmentList, "example", MediaForFragmentListConfig(
            fragments=["example"],
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fragments" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="payload" type="string">
  Returned by the lookup.
</ResponseField>
