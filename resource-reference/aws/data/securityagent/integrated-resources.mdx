---
title: "aws_securityagent_integrated_resources"
description: "`aws_securityagent_integrated_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityagent_integrated_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_integrated_resources"})

    		ubx.Data(securityagent.IntegratedResources, "example", securityagent.IntegratedResourcesConfig{
    			AgentSpaceId:  "example",
    			IntegrationId: "example",
    			MaxResults:    1,
    			ResourceType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegratedResources } from "@ubx/sdk-aws/aws/data/securityagent/integrated_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_integrated_resources" });

      data(IntegratedResources, "example", {
        agentSpaceId: "example",
        integrationId: "example",
        maxResults: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import IntegratedResources, IntegratedResourcesConfig

    def describe():
        ubx.intent("look up aws_securityagent_integrated_resources")

        ubx.data(IntegratedResources, "example", IntegratedResourcesConfig(
            agent_space_id="example",
            integration_id="example",
            max_results=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  &lt;p&gt;Unique identifier of the agent space.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="integration_id" type="string">
  &lt;p&gt;Unique identifier for an integration.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Maximum results for pagination.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  &lt;p&gt;Type of resource.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="integrated_resource_summaries" type="list(object)">
  &lt;p&gt;List of integrated resource items.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
