---
title: "aws_securityagent_tags_for_resource"
description: "`aws_securityagent_tags_for_resource` reads the real tags applied to a security agent resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_tags_for_resource"})

    		ubx.Data(securityagent.TagsForResource, "example", securityagent.TagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForResource } from "@ubx/sdk-aws/aws/data/securityagent/tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_tags_for_resource" });

      data(TagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import TagsForResource, TagsForResourceConfig

    def describe():
        ubx.intent("look up aws_securityagent_tags_for_resource")

        ubx.data(TagsForResource, "example", TagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  &lt;p&gt;ARN of a taggable resource.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  &lt;p&gt;Map of tags for a resource.&lt;/p&gt; Returned by the lookup.
</ResponseField>
