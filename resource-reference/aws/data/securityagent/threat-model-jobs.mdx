---
title: "aws_securityagent_threat_model_jobs"
description: "`aws_securityagent_threat_model_jobs` reads the real AI security agent threat modeling jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_threat_model_jobs"})

    		ubx.Data(securityagent.ThreatModelJobs, "example", securityagent.ThreatModelJobsConfig{
    			AgentSpaceId:  "example",
    			ThreatModelId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThreatModelJobs } from "@ubx/sdk-aws/aws/data/securityagent/threat_model_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_threat_model_jobs" });

      data(ThreatModelJobs, "example", {
        agentSpaceId: "example",
        threatModelId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import ThreatModelJobs, ThreatModelJobsConfig

    def describe():
        ubx.intent("look up aws_securityagent_threat_model_jobs")

        ubx.data(ThreatModelJobs, "example", ThreatModelJobsConfig(
            agent_space_id="example",
            threat_model_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Maximum results for pagination.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="threat_model_id" type="string" required>
  The real, unique identifier of the threat model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="threat_model_job_summaries" type="list(object)">
  &lt;p&gt;List of threat model job summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
