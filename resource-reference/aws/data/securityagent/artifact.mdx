---
title: "aws_securityagent_artifact"
description: "`aws_securityagent_artifact` looks up one real artifact produced by an AI security agent job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_artifact"})

    		ubx.Data(securityagent.Artifact, "example", securityagent.ArtifactConfig{
    			AgentSpaceId: "example",
    			ArtifactId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Artifact } from "@ubx/sdk-aws/aws/data/securityagent/artifact";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_artifact" });

      data(Artifact, "example", {
        agentSpaceId: "example",
        artifactId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import Artifact, ArtifactConfig

    def describe():
        ubx.intent("look up aws_securityagent_artifact")

        ubx.data(Artifact, "example", ArtifactConfig(
            agent_space_id="example",
            artifact_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  &lt;p&gt;Unique identifier of the agent space.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="artifact_id" type="string" required>
  &lt;p&gt;The id of the artifact.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="artifact" type="object">
  &lt;p&gt;Represents an artifact that provides context for security testing, such as documentation, diagrams, or configuration files.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="contents" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      &lt;p&gt;Supported file extension types for artifacts.&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="file_name" type="string">
  The real name of the file this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
