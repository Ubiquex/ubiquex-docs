---
title: "aws_securityagent_code_review_jobs_for_code_review"
description: "`aws_securityagent_code_review_jobs_for_code_review` reads the real AI security agent jobs run for a given code review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_code_review_jobs_for_code_review"})

    		ubx.Data(securityagent.CodeReviewJobsForCodeReview, "example", securityagent.CodeReviewJobsForCodeReviewConfig{
    			AgentSpaceId: "example",
    			CodeReviewId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeReviewJobsForCodeReview } from "@ubx/sdk-aws/aws/data/securityagent/code_review_jobs_for_code_review";

    export default stack("example", () => {
      intent({
        summary: "look up aws_securityagent_code_review_jobs_for_code_review",
      });

      data(CodeReviewJobsForCodeReview, "example", {
        agentSpaceId: "example",
        codeReviewId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import CodeReviewJobsForCodeReview, CodeReviewJobsForCodeReviewConfig

    def describe():
        ubx.intent("look up aws_securityagent_code_review_jobs_for_code_review")

        ubx.data(CodeReviewJobsForCodeReview, "example", CodeReviewJobsForCodeReviewConfig(
            agent_space_id="example",
            code_review_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="code_review_id" type="string" required>
  The real, unique identifier of the code review this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Maximum results for pagination.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="code_review_job_summaries" type="list(object)">
  &lt;p&gt;List of code review job summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
