---
title: "aws_securityagent_security_requirements"
description: "`aws_securityagent_security_requirements` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_securityagent_security_requirements` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_security_requirements"})

    		ubx.Data(securityagent.SecurityRequirements, "example", securityagent.SecurityRequirementsConfig{
    			PackId:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityRequirements } from "@ubx/sdk-aws/aws/data/securityagent/security_requirements";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_security_requirements" });

      data(SecurityRequirements, "example", {
        packId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import SecurityRequirements, SecurityRequirementsConfig

    def describe():
        ubx.intent("look up aws_securityagent_security_requirements")

        ubx.data(SecurityRequirements, "example", SecurityRequirementsConfig(
            pack_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;Maximum results for pagination.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="pack_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="security_requirement_summaries" type="list(object)">
  &lt;p&gt;List of security requirement summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
