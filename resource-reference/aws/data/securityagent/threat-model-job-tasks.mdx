---
title: "aws_securityagent_threat_model_job_tasks"
description: "`aws_securityagent_threat_model_job_tasks` reads the real, individual tasks within an AI security agent threat modeling job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/securityagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_securityagent_threat_model_job_tasks"})

    		ubx.Data(securityagent.ThreatModelJobTasks, "example", securityagent.ThreatModelJobTasksConfig{
    			AgentSpaceId:     "example",
    			ThreatModelJobId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThreatModelJobTasks } from "@ubx/sdk-aws/aws/data/securityagent/threat_model_job_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_securityagent_threat_model_job_tasks" });

      data(ThreatModelJobTasks, "example", {
        agentSpaceId: "example",
        threatModelJobId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.securityagent import ThreatModelJobTasks, ThreatModelJobTasksConfig

    def describe():
        ubx.intent("look up aws_securityagent_threat_model_job_tasks")

        ubx.data(ThreatModelJobTasks, "example", ThreatModelJobTasksConfig(
            agent_space_id="example",
            threat_model_job_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;Maximum results for pagination.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="threat_model_job_id" type="string" required>
  The real, unique identifier of the threat model job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="threat_model_job_task_summaries" type="list(object)">
  &lt;p&gt;List of threat model job task summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
