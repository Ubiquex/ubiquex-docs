---
title: "aws_ds_data_users"
description: "`aws_ds_data_users` looks up a real Active Directory user, via Directory Service Data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ds_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_data_users"})

    		ubx.Data(ds_data.DsDataUsers, "example", ds_data.DsDataUsersConfig{
    			DirectoryId: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DsDataUsers } from "@ubx/sdk-aws/aws/data/ds_data/ds_data_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_data_users" });

      data(DsDataUsers, "example", {
        directoryId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds_data import DsDataUsers, DsDataUsersConfig

    def describe():
        ubx.intent("look up aws_ds_data_users")

        ubx.data(DsDataUsers, "example", DsDataUsersConfig(
            directory_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="realm" type="string">
  The real realm this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
