---
title: "aws_ds_data_groups_for_member"
description: "`aws_ds_data_groups_for_member` reads the real Active Directory groups a given member belongs to, via Directory Service Data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ds_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_data_groups_for_member"})

    		ubx.Data(ds_data.DsDataGroupsForMember, "example", ds_data.DsDataGroupsForMemberConfig{
    			DirectoryId:    "example",
    			SamaccountName: "example-samaccount-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DsDataGroupsForMember } from "./local-sdk/aws/sdk/typescript/aws/data/ds_data/ds_data_groups_for_member.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_data_groups_for_member" });

      data(DsDataGroupsForMember, "example", {
        directoryId: "example",
        samaccountName: "example-samaccount-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ds_data import DsDataGroupsForMember, DsDataGroupsForMemberConfig

    def describe():
        ubx.intent("look up aws_ds_data_groups_for_member")

        ubx.data(DsDataGroupsForMember, "example", DsDataGroupsForMemberConfig(
            directory_id="example",
            samaccount_name="example-samaccount-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  The real, unique identifier of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="samaccount_name" type="string" required>
  The real name of the samaccount this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="member_realm" type="string">
  The real realm the member account belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="realm" type="string">
  The real realm this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
