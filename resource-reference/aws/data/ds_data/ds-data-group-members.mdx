---
title: "aws_ds_data_group_members"
description: "`aws_ds_data_group_members` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ds_data_group_members` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ds_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ds_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ds_data_group_members"})

    		ubx.Data(ds_data.DsDataGroupMembers, "example", ds_data.DsDataGroupMembersConfig{
    			DirectoryId:    "example",
    			SamaccountName: "example-samaccount-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DsDataGroupMembers } from "@ubx/sdk-aws/aws/data/ds_data/ds_data_group_members";

    export default stack("example", () => {
      intent({ summary: "look up aws_ds_data_group_members" });

      data(DsDataGroupMembers, "example", {
        directoryId: "example",
        samaccountName: "example-samaccount-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ds_data import DsDataGroupMembers, DsDataGroupMembersConfig

    def describe():
        ubx.intent("look up aws_ds_data_group_members")

        ubx.data(DsDataGroupMembers, "example", DsDataGroupMembersConfig(
            directory_id="example",
            samaccount_name="example-samaccount-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="samaccount_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="member_realm" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="members" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="realm" type="string">
  Returned by the lookup.
</ResponseField>
