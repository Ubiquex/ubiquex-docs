---
title: "aws_fms_protection_status"
description: "`aws_fms_protection_status` reads the real, current DDoS protection status of a resource protected by a Firewall Manager Shield Advanced policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_protection_status"})

    		ubx.Data(fms.ProtectionStatus, "example", fms.ProtectionStatusConfig{
    			PolicyId:        "example",
    			EndTime:         "example",
    			MaxResults:      1,
    			MemberAccountId: "example",
    			StartTime:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProtectionStatus } from "@ubx/sdk-aws/aws/data/fms/protection_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_protection_status" });

      data(ProtectionStatus, "example", {
        policyId: "example",
        endTime: "example",
        maxResults: 1,
        memberAccountId: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ProtectionStatus, ProtectionStatusConfig

    def describe():
        ubx.intent("look up aws_fms_protection_status")

        ubx.data(ProtectionStatus, "example", ProtectionStatusConfig(
            policy_id="example",
            end_time="example",
            max_results=1,
            member_account_id="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="member_account_id" type="string">
  The real, unique identifier of the member account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The real, unique identifier of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="admin_account_id" type="string">
  The real, unique identifier of the admin account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data" type="string">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_type" type="string">
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
