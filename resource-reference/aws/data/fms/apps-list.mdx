---
title: "aws_fms_apps_list"
description: "`aws_fms_apps_list` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_fms_apps_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_apps_list"})

    		ubx.Data(fms.AppsList, "example", fms.AppsListConfig{
    			ListId:      "example",
    			DefaultList: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppsList } from "@ubx/sdk-aws/aws/data/fms/apps_list";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_apps_list" });

      data(AppsList, "example", {
        listId: "example",
        defaultList: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import AppsList, AppsListConfig

    def describe():
        ubx.intent("look up aws_fms_apps_list")

        ubx.data(AppsList, "example", AppsListConfig(
            list_id="example",
            default_list=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_list" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="list_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="apps_list" type="object">
  &lt;p&gt;An Firewall Manager applications list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="apps_list" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_update_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="list_update_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="previous_apps_list" type="map(list(object))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="apps_list_arn" type="string">
  Returned by the lookup.
</ResponseField>
