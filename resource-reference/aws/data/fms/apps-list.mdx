---
title: "aws_fms_apps_list"
description: "`aws_fms_apps_list` looks up one real Firewall Manager applications list, a reusable set of port/protocol definitions for use in security group policies."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_apps_list"})

    		ubx.Data(fms.AppsList, "example", fms.AppsListConfig{
    			ListId:      "example",
    			DefaultList: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AppsList } from "./local-sdk/aws/sdk/typescript/aws/data/fms/apps_list.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_apps_list" });

      data(AppsList, "example", {
        listId: "example",
        defaultList: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fms import AppsList, AppsListConfig

    def describe():
        ubx.intent("look up aws_fms_apps_list")

        ubx.data(AppsList, "example", AppsListConfig(
            list_id="example",
            default_list=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_list" type="bool">
  The real defaults included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="list_id" type="string" required>
  The real, unique identifier of the list this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="apps_list" type="object">
  &lt;p&gt;An Firewall Manager applications list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="apps_list" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="create_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_update_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="list_update_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="previous_apps_list" type="map(list(object))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="apps_list_arn" type="string">
  The real Amazon Resource Name (ARN) of the apps list this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
