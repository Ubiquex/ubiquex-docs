---
title: "aws_fms_admin_account"
description: "`aws_fms_admin_account` reads the real Firewall Manager administrator account for an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_admin_account"})

    		ubx.Data(fms.AdminAccount, "example", fms.AdminAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminAccount } from "@ubx/sdk-aws/aws/data/fms/admin_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_admin_account" });

      data(AdminAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import AdminAccount, AdminAccountConfig

    def describe():
        ubx.intent("look up aws_fms_admin_account")

        ubx.data(AdminAccount, "example", AdminAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="admin_account" type="string">
  The real, delegated administrator account for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_status" type="string">
  The real, current status of the role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
