---
title: "aws_fms_violation_details"
description: "`aws_fms_violation_details` reads real, detailed policy violations found for a resource under a Firewall Manager policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_violation_details"})

    		ubx.Data(fms.ViolationDetails, "example", fms.ViolationDetailsConfig{
    			MemberAccount: "example",
    			PolicyId:      "example",
    			ResourceId:    "example",
    			ResourceType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ViolationDetails } from "@ubx/sdk-aws/aws/data/fms/violation_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_violation_details" });

      data(ViolationDetails, "example", {
        memberAccount: "example",
        policyId: "example",
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ViolationDetails, ViolationDetailsConfig

    def describe():
        ubx.intent("look up aws_fms_violation_details")

        ubx.data(ViolationDetails, "example", ViolationDetailsConfig(
            member_account="example",
            policy_id="example",
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="member_account" type="string" required>
  The real AWS Organizations member account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The real, unique identifier of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="violation_detail" type="object">
  &lt;p&gt;Violations for a resource based on the specified Firewall Manager policy and Amazon Web Services account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="member_account" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="policy_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_tags" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_violations" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
