---
title: "aws_fms_protocols_lists"
description: "`aws_fms_protocols_lists` reads the real Firewall Manager protocols lists on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_protocols_lists"})

    		ubx.Data(fms.ProtocolsLists, "example", fms.ProtocolsListsConfig{
    			MaxResults:   1,
    			DefaultLists: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ProtocolsLists } from "./local-sdk/aws/sdk/typescript/aws/data/fms/protocols_lists.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_protocols_lists" });

      data(ProtocolsLists, "example", {
        maxResults: 1,
        defaultLists: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ProtocolsLists, ProtocolsListsConfig

    def describe():
        ubx.intent("look up aws_fms_protocols_lists")

        ubx.data(ProtocolsLists, "example", ProtocolsListsConfig(
            max_results=1,
            default_lists=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_lists" type="bool">
  The real, default lists included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="protocols_lists" type="list(object)">
  The real AWS Network Firewall protocol lists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
