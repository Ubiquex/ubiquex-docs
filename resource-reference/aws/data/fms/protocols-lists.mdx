---
title: "aws_fms_protocols_lists"
description: "`aws_fms_protocols_lists` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_fms_protocols_lists` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_protocols_lists"})

    		ubx.Data(fms.ProtocolsLists, "example", fms.ProtocolsListsConfig{
    			MaxResults:   1,
    			DefaultLists: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProtocolsLists } from "@ubx/sdk-aws/aws/data/fms/protocols_lists";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_protocols_lists" });

      data(ProtocolsLists, "example", {
        maxResults: 1,
        defaultLists: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ProtocolsLists, ProtocolsListsConfig

    def describe():
        ubx.intent("look up aws_fms_protocols_lists")

        ubx.data(ProtocolsLists, "example", ProtocolsListsConfig(
            max_results=1,
            default_lists=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_lists" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="protocols_lists" type="list(object)">
  Returned by the lookup.
</ResponseField>
