---
title: "aws_fms_protocols_list"
description: "`aws_fms_protocols_list` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_fms_protocols_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_protocols_list"})

    		ubx.Data(fms.ProtocolsList, "example", fms.ProtocolsListConfig{
    			ListId:      "example",
    			DefaultList: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProtocolsList } from "@ubx/sdk-aws/aws/data/fms/protocols_list";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_protocols_list" });

      data(ProtocolsList, "example", {
        listId: "example",
        defaultList: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ProtocolsList, ProtocolsListConfig

    def describe():
        ubx.intent("look up aws_fms_protocols_list")

        ubx.data(ProtocolsList, "example", ProtocolsListConfig(
            list_id="example",
            default_list=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_list" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="list_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="protocols_list" type="object">
  &lt;p&gt;An Firewall Manager protocols list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_update_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="list_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="list_update_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="previous_protocols_list" type="map(list(string))">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="protocols_list" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocols_list_arn" type="string">
  Returned by the lookup.
</ResponseField>
