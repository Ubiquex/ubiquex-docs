---
title: "aws_fms_apps_lists"
description: "`aws_fms_apps_lists` reads the real Firewall Manager applications lists on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_apps_lists"})

    		ubx.Data(fms.AppsLists, "example", fms.AppsListsConfig{
    			MaxResults:   1,
    			DefaultLists: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppsLists } from "@ubx/sdk-aws/aws/data/fms/apps_lists";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_apps_lists" });

      data(AppsLists, "example", {
        maxResults: 1,
        defaultLists: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import AppsLists, AppsListsConfig

    def describe():
        ubx.intent("look up aws_fms_apps_lists")

        ubx.data(AppsLists, "example", AppsListsConfig(
            max_results=1,
            default_lists=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_lists" type="bool">
  The real, default lists included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="apps_lists" type="list(object)">
  The real application lists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
