---
title: "aws_fms_compliance_status"
description: "`aws_fms_compliance_status` reads a real, summarized compliance status of a member account against a Firewall Manager policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_compliance_status"})

    		ubx.Data(fms.ComplianceStatus, "example", fms.ComplianceStatusConfig{
    			PolicyId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceStatus } from "@ubx/sdk-aws/aws/data/fms/compliance_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_compliance_status" });

      data(ComplianceStatus, "example", {
        policyId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ComplianceStatus, ComplianceStatusConfig

    def describe():
        ubx.intent("look up aws_fms_compliance_status")

        ubx.data(ComplianceStatus, "example", ComplianceStatusConfig(
            policy_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The real, unique identifier of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_compliance_status_list" type="list(object)">
  The real policy compliance statuss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
