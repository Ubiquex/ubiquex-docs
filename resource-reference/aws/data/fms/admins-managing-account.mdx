---
title: "aws_fms_admins_managing_account"
description: "`aws_fms_admins_managing_account` reads the real Firewall Manager administrator accounts that manage a given member account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_admins_managing_account"})

    		ubx.Data(fms.AdminsManagingAccount, "example", fms.AdminsManagingAccountConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdminsManagingAccount } from "@ubx/sdk-aws/aws/data/fms/admins_managing_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_admins_managing_account" });

      data(AdminsManagingAccount, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import AdminsManagingAccount, AdminsManagingAccountConfig

    def describe():
        ubx.intent("look up aws_fms_admins_managing_account")

        ubx.data(AdminsManagingAccount, "example", AdminsManagingAccountConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="admin_accounts" type="list(string)">
  The real, delegated administrator accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
