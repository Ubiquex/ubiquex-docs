---
title: "aws_fms_third_party_firewall_association_status"
description: "`aws_fms_third_party_firewall_association_status` reads the real, current status of associating a third-party firewall vendor account with Firewall..."
---

`aws_fms_third_party_firewall_association_status` reads the real, current status of associating a third-party firewall vendor account with Firewall Manager.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_third_party_firewall_association_status"})

    		ubx.Data(fms.ThirdPartyFirewallAssociationStatus, "example", fms.ThirdPartyFirewallAssociationStatusConfig{
    			ThirdPartyFirewall: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ThirdPartyFirewallAssociationStatus } from "./local-sdk/aws/sdk/typescript/aws/data/fms/third_party_firewall_association_status.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_fms_third_party_firewall_association_status",
      });

      data(ThirdPartyFirewallAssociationStatus, "example", {
        thirdPartyFirewall: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ThirdPartyFirewallAssociationStatus, ThirdPartyFirewallAssociationStatusConfig

    def describe():
        ubx.intent("look up aws_fms_third_party_firewall_association_status")

        ubx.data(ThirdPartyFirewallAssociationStatus, "example", ThirdPartyFirewallAssociationStatusConfig(
            third_party_firewall="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="third_party_firewall" type="string" required>
  The real, third-party firewall service this configuration applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marketplace_onboarding_status" type="string">
  The real, current status of the marketplace onboarding this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="third_party_firewall_status" type="string">
  The real, current status of the third party firewall this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
