---
title: "aws_fms_resource_set_resources"
description: "`data_aws_fms_resource_set_resources` looks up a real, already-existing `aws_fms_resource_set`."
---

It is a real, named collection of specific resources that an `aws_fms_policy` can reference directly, scoping a policy to an explicit resource list rather than only a broader tag- or account-based filter.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_resource_set_resources"})

    		ubx.Data(fms.ResourceSetResources, "example", fms.ResourceSetResourcesConfig{
    			Identifier: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceSetResources } from "@ubx/sdk-aws/aws/data/fms/resource_set_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_resource_set_resources" });

      data(ResourceSetResources, "example", {
        identifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ResourceSetResources, ResourceSetResourcesConfig

    def describe():
        ubx.intent("look up aws_fms_resource_set_resources")

        ubx.data(ResourceSetResources, "example", ResourceSetResourcesConfig(
            identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
