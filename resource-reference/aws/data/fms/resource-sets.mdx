---
title: "aws_fms_resource_sets"
description: "`data_aws_fms_resource_sets` looks up a real, already-existing `aws_fms_resource_set`."
---

It is a real, named collection of specific resources that an `aws_fms_policy` can reference directly, scoping a policy to an explicit resource list rather than only a broader tag- or account-based filter.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/fms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_fms_resource_sets"})

    		ubx.Data(fms.ResourceSets, "example", fms.ResourceSetsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceSets } from "@ubx/sdk-aws/aws/data/fms/resource_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_fms_resource_sets" });

      data(ResourceSets, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.fms import ResourceSets, ResourceSetsConfig

    def describe():
        ubx.intent("look up aws_fms_resource_sets")

        ubx.data(ResourceSets, "example", ResourceSetsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_sets" type="list(object)">
  The real resource sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
