---
title: "aws_ecs_task_definition"
description: "`aws_ecs_task_definition` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ecs_task_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ecs_task_definition"})

    		ubx.Data(ecs.TaskDefinition, "example", ecs.TaskDefinitionConfig{
    			TaskDefinition: "example",
    			Include:        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaskDefinition } from "@ubx/sdk-aws/aws/data/ecs/task_definition";

    export default stack("example", () => {
      intent({ summary: "look up aws_ecs_task_definition" });

      data(TaskDefinition, "example", {
        taskDefinition: "example",
        include: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ecs import TaskDefinition, TaskDefinitionConfig

    def describe():
        ubx.intent("look up aws_ecs_task_definition")

        ubx.data(TaskDefinition, "example", TaskDefinitionConfig(
            task_definition="example",
            include=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="task_definition" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
