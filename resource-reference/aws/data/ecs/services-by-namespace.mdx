---
title: "aws_ecs_services_by_namespace"
description: "`aws_ecs_services_by_namespace` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ecs_services_by_namespace` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ecs_services_by_namespace"})

    		ubx.Data(ecs.ServicesByNamespace, "example", ecs.ServicesByNamespaceConfig{
    			Namespace:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicesByNamespace } from "@ubx/sdk-aws/aws/data/ecs/services_by_namespace";

    export default stack("example", () => {
      intent({ summary: "look up aws_ecs_services_by_namespace" });

      data(ServicesByNamespace, "example", {
        namespace: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ecs import ServicesByNamespace, ServicesByNamespaceConfig

    def describe():
        ubx.intent("look up aws_ecs_services_by_namespace")

        ubx.data(ServicesByNamespace, "example", ServicesByNamespaceConfig(
            namespace="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_arns" type="list(string)">
  Returned by the lookup.
</ResponseField>
