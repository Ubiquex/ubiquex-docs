---
title: "aws_ecs_services_by_namespace"
description: "`aws_ecs_services_by_namespace` reads the real ECS services registered under a given Cloud Map namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ecs_services_by_namespace"})

    		ubx.Data(ecs.ServicesByNamespace, "example", ecs.ServicesByNamespaceConfig{
    			Namespace:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ServicesByNamespace } from "./local-sdk/aws/sdk/typescript/aws/data/ecs/services_by_namespace.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ecs_services_by_namespace" });

      data(ServicesByNamespace, "example", {
        namespace: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ecs import ServicesByNamespace, ServicesByNamespaceConfig

    def describe():
        ubx.intent("look up aws_ecs_services_by_namespace")

        ubx.data(ServicesByNamespace, "example", ServicesByNamespaceConfig(
            namespace="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The real namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the services this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
