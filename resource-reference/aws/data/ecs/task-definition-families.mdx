---
title: "aws_ecs_task_definition_families"
description: "`data_aws_ecs_task_definition_families` looks up a real, already-existing `aws_ecs_task_definition`."
---

It is the real blueprint, container images, CPU and memory, networking mode, IAM roles, that an `aws_ecs_service` or a standalone task actually launches from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ecs_task_definition_families"})

    		ubx.Data(ecs.TaskDefinitionFamilies, "example", ecs.TaskDefinitionFamiliesConfig{
    			FamilyPrefix: "example",
    			MaxResults:   1,
    			Status:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaskDefinitionFamilies } from "@ubx/sdk-aws/aws/data/ecs/task_definition_families";

    export default stack("example", () => {
      intent({ summary: "look up aws_ecs_task_definition_families" });

      data(TaskDefinitionFamilies, "example", {
        familyPrefix: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ecs import TaskDefinitionFamilies, TaskDefinitionFamiliesConfig

    def describe():
        ubx.intent("look up aws_ecs_task_definition_families")

        ubx.data(TaskDefinitionFamilies, "example", TaskDefinitionFamiliesConfig(
            family_prefix="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="family_prefix" type="string">
  The real S3 key prefix used for the family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="families" type="list(string)">
  The real families included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
