---
title: "aws_ecs_account_settings"
description: "`aws_ecs_account_settings` reads real, account-level ECS settings, such as its own opted-in resource ID formats."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ecs_account_settings"})

    		ubx.Data(ecs.AccountSettings, "example", ecs.AccountSettingsConfig{
    			Name:              "example",
    			EffectiveSettings: true,
    			MaxResults:        1,
    			PrincipalArn:      "arn:aws:iam::123456789012:policy/example",
    			Value:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountSettings } from "./local-sdk/aws/sdk/typescript/aws/data/ecs/account_settings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ecs_account_settings" });

      data(AccountSettings, "example", {
        name: "example",
        effectiveSettings: true,
        maxResults: 1,
        principalArn: "arn:aws:iam::123456789012:policy/example",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ecs import AccountSettings, AccountSettingsConfig

    def describe():
        ubx.intent("look up aws_ecs_account_settings")

        ubx.data(AccountSettings, "example", AccountSettingsConfig(
            name="example",
            effective_settings=True,
            max_results=1,
            principal_arn="arn:aws:iam::123456789012:policy/example",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="effective_settings" type="bool">
  The real settings of the effective. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="value" type="string">
  The real, current value of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="settings" type="list(object)">
  The real settings applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
