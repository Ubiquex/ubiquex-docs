---
title: "aws_appflow_connector_entities"
description: "`aws_appflow_connector_entities` reads the real, discoverable entities (objects) an AppFlow connector exposes."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appflow_connector_entities"})

    		ubx.Data(appflow.ConnectorEntities, "example", appflow.ConnectorEntitiesConfig{
    			ApiVersion:           "example",
    			ConnectorProfileName: "example-connector-profile-name",
    			ConnectorType:        "example",
    			EntitiesPath:         "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectorEntities } from "@ubx/sdk-aws/aws/data/appflow/connector_entities";

    export default stack("example", () => {
      intent({ summary: "look up aws_appflow_connector_entities" });

      data(ConnectorEntities, "example", {
        apiVersion: "example",
        connectorProfileName: "example-connector-profile-name",
        connectorType: "example",
        entitiesPath: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appflow import ConnectorEntities, ConnectorEntitiesConfig

    def describe():
        ubx.intent("look up aws_appflow_connector_entities")

        ubx.data(ConnectorEntities, "example", ConnectorEntitiesConfig(
            api_version="example",
            connector_profile_name="example-connector-profile-name",
            connector_type="example",
            entities_path="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string">
  The real version of the api. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connector_profile_name" type="string">
  The real name of the connector profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connector_type" type="string">
  The real, discriminating type of the connector this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="entities_path" type="string">
  The real path of the entities. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connector_entity_map" type="map(list(object))">
  The real mapping of entities exposed by this connector. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
