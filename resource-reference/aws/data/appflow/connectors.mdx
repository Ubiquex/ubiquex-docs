---
title: "aws_appflow_connectors"
description: "`aws_appflow_connectors` reads the real connectors registered with AppFlow."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appflow_connectors"})

    		ubx.Data(appflow.Connectors, "example", appflow.ConnectorsConfig{
    			ConnectorTypes: []string{"example"},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Connectors } from "@ubx/sdk-aws/aws/data/appflow/connectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_appflow_connectors" });

      data(Connectors, "example", {
        connectorTypes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appflow import Connectors, ConnectorsConfig

    def describe():
        ubx.intent("look up aws_appflow_connectors")

        ubx.data(Connectors, "example", ConnectorsConfig(
            connector_types=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connector_types" type="list(string)">
  The real, discriminating types of connectors this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connector_configurations" type="map(object)">
  The real connector configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connectors" type="list(object)">
  The real connectors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
