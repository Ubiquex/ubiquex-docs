---
title: "aws_appflow_connector_entity"
description: "`aws_appflow_connector_entity` reads real, detailed field metadata for one entity an AppFlow connector exposes."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appflow_connector_entity"})

    		ubx.Data(appflow.ConnectorEntity, "example", appflow.ConnectorEntityConfig{
    			ConnectorEntityName:  "example-connector-entity-name",
    			ApiVersion:           "example",
    			ConnectorProfileName: "example-connector-profile-name",
    			ConnectorType:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConnectorEntity } from "./local-sdk/aws/sdk/typescript/aws/data/appflow/connector_entity.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appflow_connector_entity" });

      data(ConnectorEntity, "example", {
        connectorEntityName: "example-connector-entity-name",
        apiVersion: "example",
        connectorProfileName: "example-connector-profile-name",
        connectorType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appflow import ConnectorEntity, ConnectorEntityConfig

    def describe():
        ubx.intent("look up aws_appflow_connector_entity")

        ubx.data(ConnectorEntity, "example", ConnectorEntityConfig(
            connector_entity_name="example-connector-entity-name",
            api_version="example",
            connector_profile_name="example-connector-profile-name",
            connector_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_version" type="string">
  The real version of the api. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connector_entity_name" type="string" required>
  The real name of the connector entity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="connector_profile_name" type="string">
  The real name of the connector profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connector_type" type="string">
  The real, discriminating type of the connector this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connector_entity_fields" type="list(object)">
  The real entity fields exposed by this connector. **(AI-inferred)** Returned by the lookup.
</ResponseField>
