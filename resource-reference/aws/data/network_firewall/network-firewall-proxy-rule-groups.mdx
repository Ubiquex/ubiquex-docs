---
title: "aws_network_firewall_proxy_rule_groups"
description: "`aws_network_firewall_proxy_rule_groups` reads the real rule groups associated with a Network Firewall Explicit Proxy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_proxy_rule_groups"})

    		ubx.Data(network_firewall.NetworkFirewallProxyRuleGroups, "example", network_firewall.NetworkFirewallProxyRuleGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallProxyRuleGroups } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_proxy_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_proxy_rule_groups" });

      data(NetworkFirewallProxyRuleGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallProxyRuleGroups, NetworkFirewallProxyRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_network_firewall_proxy_rule_groups")

        ubx.data(NetworkFirewallProxyRuleGroups, "example", NetworkFirewallProxyRuleGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="proxy_rule_groups" type="list(object)">
  The real proxy rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
