---
title: "aws_network_firewall_proxies"
description: "`aws_network_firewall_proxies` looks up a real Network Firewall Explicit Proxy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_proxies"})

    		ubx.Data(network_firewall.NetworkFirewallProxies, "example", network_firewall.NetworkFirewallProxiesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkFirewallProxies } from "./local-sdk/aws/sdk/typescript/aws/data/network_firewall/network_firewall_proxies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_proxies" });

      data(NetworkFirewallProxies, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallProxies, NetworkFirewallProxiesConfig

    def describe():
        ubx.intent("look up aws_network_firewall_proxies")

        ubx.data(NetworkFirewallProxies, "example", NetworkFirewallProxiesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="proxies" type="list(object)">
  The real proxies included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
