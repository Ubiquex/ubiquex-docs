---
title: "aws_network_firewall_firewalls"
description: "`aws_network_firewall_firewalls` looks up a real Network Firewall."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_firewalls"})

    		ubx.Data(network_firewall.NetworkFirewallFirewalls, "example", network_firewall.NetworkFirewallFirewallsConfig{
    			MaxResults: 1,
    			VpcIds:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NetworkFirewallFirewalls } from "./local-sdk/aws/sdk/typescript/aws/data/network_firewall/network_firewall_firewalls.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_firewalls" });

      data(NetworkFirewallFirewalls, "example", {
        maxResults: 1,
        vpcIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallFirewalls, NetworkFirewallFirewallsConfig

    def describe():
        ubx.intent("look up aws_network_firewall_firewalls")

        ubx.data(NetworkFirewallFirewalls, "example", NetworkFirewallFirewallsConfig(
            max_results=1,
            vpc_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_ids" type="list(string)">
  The real, unique identifiers of the vpcs this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewalls" type="list(object)">
  The real firewalls included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
