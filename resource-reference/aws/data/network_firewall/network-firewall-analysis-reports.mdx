---
title: "aws_network_firewall_analysis_reports"
description: "`aws_network_firewall_analysis_reports` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_network_firewall_analysis_reports` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_analysis_reports"})

    		ubx.Data(network_firewall.NetworkFirewallAnalysisReports, "example", network_firewall.NetworkFirewallAnalysisReportsConfig{
    			FirewallArn:  "arn:aws:iam::123456789012:policy/example",
    			FirewallName: "example-firewall-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallAnalysisReports } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_analysis_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_analysis_reports" });

      data(NetworkFirewallAnalysisReports, "example", {
        firewallArn: "arn:aws:iam::123456789012:policy/example",
        firewallName: "example-firewall-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallAnalysisReports, NetworkFirewallAnalysisReportsConfig

    def describe():
        ubx.intent("look up aws_network_firewall_analysis_reports")

        ubx.data(NetworkFirewallAnalysisReports, "example", NetworkFirewallAnalysisReportsConfig(
            firewall_arn="arn:aws:iam::123456789012:policy/example",
            firewall_name="example-firewall-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="firewall_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analysis_reports" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
