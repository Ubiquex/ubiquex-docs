---
title: "aws_network_firewall_logging_configuration"
description: "`aws_network_firewall_logging_configuration` reads the real logging configuration (alert and flow logs) of a Network Firewall."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_logging_configuration"})

    		ubx.Data(network_firewall.NetworkFirewallLoggingConfiguration, "example", network_firewall.NetworkFirewallLoggingConfigurationConfig{
    			FirewallName: "example-firewall-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallLoggingConfiguration } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_logging_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_logging_configuration" });

      data(NetworkFirewallLoggingConfiguration, "example", {
        firewallName: "example-firewall-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallLoggingConfiguration, NetworkFirewallLoggingConfigurationConfig

    def describe():
        ubx.intent("look up aws_network_firewall_logging_configuration")

        ubx.data(NetworkFirewallLoggingConfiguration, "example", NetworkFirewallLoggingConfigurationConfig(
            firewall_name="example-firewall-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_name" type="string">
  The real name of the firewall this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enable_monitoring_dashboard" type="bool">
  Whether a monitoring dashboard is enabled for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="firewall_arn" type="string">
  The real Amazon Resource Name (ARN) of the firewall this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="logging_configuration" type="object">
  &lt;p&gt;Defines how Network Firewall performs logging for a &lt;a&gt;Firewall&lt;/a&gt;. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="log_destination_configs" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
