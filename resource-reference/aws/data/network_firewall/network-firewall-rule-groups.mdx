---
title: "aws_network_firewall_rule_groups"
description: "`aws_network_firewall_rule_groups` looks up a real Network Firewall rule group, containing the stateful or stateless rules a firewall policy references."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_rule_groups"})

    		ubx.Data(network_firewall.NetworkFirewallRuleGroups, "example", network_firewall.NetworkFirewallRuleGroupsConfig{
    			ManagedType:        "example",
    			MaxResults:         1,
    			Scope:              "example",
    			SubscriptionStatus: "example",
    			Type:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallRuleGroups } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_rule_groups" });

      data(NetworkFirewallRuleGroups, "example", {
        managedType: "example",
        maxResults: 1,
        scope: "example",
        subscriptionStatus: "example",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallRuleGroups, NetworkFirewallRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_network_firewall_rule_groups")

        ubx.data(NetworkFirewallRuleGroups, "example", NetworkFirewallRuleGroupsConfig(
            managed_type="example",
            max_results=1,
            scope="example",
            subscription_status="example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_type" type="string">
  The real, discriminating type of the managed this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string">
  The real scope this lookup is applied at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subscription_status" type="string">
  The real, current status of the subscription this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_groups" type="list(object)">
  The real rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
