---
title: "aws_network_firewall_firewall_metadata"
description: "`aws_network_firewall_firewall_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_network_firewall_firewall_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_firewall_metadata"})

    		ubx.Data(network_firewall.NetworkFirewallFirewallMetadata, "example", network_firewall.NetworkFirewallFirewallMetadataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallFirewallMetadata } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_firewall_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_firewall_metadata" });

      data(NetworkFirewallFirewallMetadata, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallFirewallMetadata, NetworkFirewallFirewallMetadataConfig

    def describe():
        ubx.intent("look up aws_network_firewall_firewall_metadata")

        ubx.data(NetworkFirewallFirewallMetadata, "example", NetworkFirewallFirewallMetadataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="firewall_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="firewall_policy_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="supported_availability_zones" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  Returned by the lookup.
</ResponseField>
