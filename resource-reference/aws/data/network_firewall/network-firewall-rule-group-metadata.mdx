---
title: "aws_network_firewall_rule_group_metadata"
description: "`aws_network_firewall_rule_group_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_network_firewall_rule_group_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_rule_group_metadata"})

    		ubx.Data(network_firewall.NetworkFirewallRuleGroupMetadata, "example", network_firewall.NetworkFirewallRuleGroupMetadataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallRuleGroupMetadata } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_rule_group_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_rule_group_metadata" });

      data(NetworkFirewallRuleGroupMetadata, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallRuleGroupMetadata, NetworkFirewallRuleGroupMetadataConfig

    def describe():
        ubx.intent("look up aws_network_firewall_rule_group_metadata")

        ubx.data(NetworkFirewallRuleGroupMetadata, "example", NetworkFirewallRuleGroupMetadataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="capacity" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="listing_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="product_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rule_group_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rule_group_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stateful_rule_options" type="object">
  &lt;p&gt;Additional options governing how Network Firewall handles the rule group. You can only use these for stateful rule groups.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rule_order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vendor_name" type="string">
  Returned by the lookup.
</ResponseField>
