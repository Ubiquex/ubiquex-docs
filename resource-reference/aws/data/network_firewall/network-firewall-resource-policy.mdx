---
title: "aws_network_firewall_resource_policy"
description: "`aws_network_firewall_resource_policy` reads the real resource-based policy attached to a Network Firewall rule group or policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	network_firewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/network_firewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_network_firewall_resource_policy"})

    		ubx.Data(network_firewall.NetworkFirewallResourcePolicy, "example", network_firewall.NetworkFirewallResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NetworkFirewallResourcePolicy } from "@ubx/sdk-aws/aws/data/network_firewall/network_firewall_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_network_firewall_resource_policy" });

      data(NetworkFirewallResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.network_firewall import NetworkFirewallResourcePolicy, NetworkFirewallResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_network_firewall_resource_policy")

        ubx.data(NetworkFirewallResourcePolicy, "example", NetworkFirewallResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
