---
title: "aws_transfer_security_policy"
description: "`aws_transfer_security_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_transfer_security_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transfer_security_policy"})

    		ubx.Data(transfer.SecurityPolicy, "example", transfer.SecurityPolicyConfig{
    			SecurityPolicyName: "example-security-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityPolicy } from "@ubx/sdk-aws/aws/data/transfer/security_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_transfer_security_policy" });

      data(SecurityPolicy, "example", {
        securityPolicyName: "example-security-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transfer import SecurityPolicy, SecurityPolicyConfig

    def describe():
        ubx.intent("look up aws_transfer_security_policy")

        ubx.data(SecurityPolicy, "example", SecurityPolicyConfig(
            security_policy_name="example-security-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="security_policy_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="security_policy" type="object">
  &lt;p&gt;Describes the properties of a security policy that you specify. For more information about security policies, see &lt;a href="https://docs.aws.amazon.com/transfer/latest/userguide/security-policies.html"&gt;Working with security policies for servers&lt;/a&gt; or &lt;a href="https://docs.aws.amazon.com/transfer/latest/userguide/security-policies-connectors.html"&gt;Working with security policies for SFTP connectors&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="fips" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="protocols" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="security_policy_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="ssh_ciphers" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ssh_host_key_algorithms" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ssh_kexs" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ssh_macs" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tls_ciphers" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
