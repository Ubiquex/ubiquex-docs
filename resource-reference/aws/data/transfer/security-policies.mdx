---
title: "aws_transfer_security_policies"
description: "`aws_transfer_security_policies` reads the real, available security policies (cipher suites) for a Transfer Family server."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transfer_security_policies"})

    		ubx.Data(transfer.SecurityPolicies, "example", transfer.SecurityPoliciesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityPolicies } from "@ubx/sdk-aws/aws/data/transfer/security_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_transfer_security_policies" });

      data(SecurityPolicies, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.transfer import SecurityPolicies, SecurityPoliciesConfig

    def describe():
        ubx.intent("look up aws_transfer_security_policies")

        ubx.data(SecurityPolicies, "example", SecurityPoliciesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="security_policy_names" type="list(string)">
  The real names of the security policys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
