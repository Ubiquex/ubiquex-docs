---
title: "aws_transfer_file_transfer_results"
description: "`aws_transfer_file_transfer_results` reads the real result of a Transfer Family connector file transfer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_transfer_file_transfer_results"})

    		ubx.Data(transfer.FileTransferResults, "example", transfer.FileTransferResultsConfig{
    			ConnectorId: "example",
    			TransferId:  "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FileTransferResults } from "./local-sdk/aws/sdk/typescript/aws/data/transfer/file_transfer_results.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_transfer_file_transfer_results" });

      data(FileTransferResults, "example", {
        connectorId: "example",
        transferId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.transfer import FileTransferResults, FileTransferResultsConfig

    def describe():
        ubx.intent("look up aws_transfer_file_transfer_results")

        ubx.data(FileTransferResults, "example", FileTransferResultsConfig(
            connector_id="example",
            transfer_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connector_id" type="string" required>
  The real, unique identifier of the connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="transfer_id" type="string" required>
  The real, unique identifier of the transfer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="file_transfer_results" type="list(object)">
  The real file transfer results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
