---
title: "aws_license_manager_linux_subscriptions_registered_subscription_provider"
description: "`aws_license_manager_linux_subscriptions_registered_subscription_provider` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_license_manager_linux_subscriptions_registered_subscription_provider` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license_manager_linux_subscriptions "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/license_manager_linux_subscriptions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_license_manager_linux_subscriptions_registered_subscription_provider"})

    		ubx.Data(license_manager_linux_subscriptions.LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProvider, "example", license_manager_linux_subscriptions.LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProviderConfig{
    			SubscriptionProviderArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProvider } from "@ubx/sdk-aws/aws/data/license_manager_linux_subscriptions/license_manager_linux_subscriptions_registered_subscription_provider";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_license_manager_linux_subscriptions_registered_subscription_provider",
      });

      data(
        LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProvider,
        "example",
        {
          subscriptionProviderArn: "arn:aws:iam::123456789012:policy/example",
        },
      );
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.license_manager_linux_subscriptions import LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProvider, LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProviderConfig

    def describe():
        ubx.intent("look up aws_license_manager_linux_subscriptions_registered_subscription_provider")

        ubx.data(LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProvider, "example", LicenseManagerLinuxSubscriptionsRegisteredSubscriptionProviderConfig(
            subscription_provider_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="subscription_provider_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_successful_data_retrieval_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_provider_source" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_provider_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_provider_status_message" type="string">
  Returned by the lookup.
</ResponseField>
