---
title: "aws_elemental_inference_fixture"
description: "`aws_elemental_inference_fixture` looks up one real Elemental Inference fixture."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elemental_inference "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elemental_inference"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elemental_inference_fixture"})

    		ubx.Data(elemental_inference.ElementalInferenceFixture, "example", elemental_inference.ElementalInferenceFixtureConfig{
    			FixtureId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ElementalInferenceFixture } from "./local-sdk/aws/sdk/typescript/aws/data/elemental_inference/elemental_inference_fixture.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elemental_inference_fixture" });

      data(ElementalInferenceFixture, "example", {
        fixtureId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elemental_inference import ElementalInferenceFixture, ElementalInferenceFixtureConfig

    def describe():
        ubx.intent("look up aws_elemental_inference_fixture")

        ubx.data(ElementalInferenceFixture, "example", ElementalInferenceFixtureConfig(
            fixture_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fixture_id" type="string" required>
  The real, unique identifier of the fixture this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="competitors" type="list(object)">
  The real competitors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fixture_group" type="string">
  The real fixture group. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_start" type="string">
  The real, scheduled start timestamp of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
