---
title: "aws_elemental_inference_fixture"
description: "`aws_elemental_inference_fixture` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elemental_inference_fixture` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elemental_inference "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elemental_inference"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elemental_inference_fixture"})

    		ubx.Data(elemental_inference.ElementalInferenceFixture, "example", elemental_inference.ElementalInferenceFixtureConfig{
    			FixtureId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElementalInferenceFixture } from "@ubx/sdk-aws/aws/data/elemental_inference/elemental_inference_fixture";

    export default stack("example", () => {
      intent({ summary: "look up aws_elemental_inference_fixture" });

      data(ElementalInferenceFixture, "example", {
        fixtureId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elemental_inference import ElementalInferenceFixture, ElementalInferenceFixtureConfig

    def describe():
        ubx.intent("look up aws_elemental_inference_fixture")

        ubx.data(ElementalInferenceFixture, "example", ElementalInferenceFixtureConfig(
            fixture_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fixture_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="competitors" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="fixture_group" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_start" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
