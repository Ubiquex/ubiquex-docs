---
title: "aws_codestar_connections_resource_sync_status"
description: "`aws_codestar_connections_resource_sync_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codestar_connections_resource_sync_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_connections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_connections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_connections_resource_sync_status"})

    		ubx.Data(codestar_connections.CodestarConnectionsResourceSyncStatus, "example", codestar_connections.CodestarConnectionsResourceSyncStatusConfig{
    			ResourceName: "example-resource-name",
    			SyncType:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarConnectionsResourceSyncStatus } from "@ubx/sdk-aws/aws/data/codestar_connections/codestar_connections_resource_sync_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_connections_resource_sync_status" });

      data(CodestarConnectionsResourceSyncStatus, "example", {
        resourceName: "example-resource-name",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_connections import CodestarConnectionsResourceSyncStatus, CodestarConnectionsResourceSyncStatusConfig

    def describe():
        ubx.intent("look up aws_codestar_connections_resource_sync_status")

        ubx.data(CodestarConnectionsResourceSyncStatus, "example", CodestarConnectionsResourceSyncStatusConfig(
            resource_name="example-resource-name",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="desired_state" type="object">
  &lt;p&gt;Information about the revision for a specific sync event, such as the branch, owner ID, and name of the repository.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="branch" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="directory" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="provider_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="repository_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="sha" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="latest_successful_sync" type="object">
  &lt;p&gt;Information about a resource sync attempt.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="initial_revision" type="object" required>
      &lt;p&gt;Information about the revision for a specific sync event, such as the branch, owner ID, and name of the repository.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="branch" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="directory" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="owner_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="provider_type" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="repository_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="sha" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="started_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_revision" type="object" required>
      &lt;p&gt;Information about the revision for a specific sync event, such as the branch, owner ID, and name of the repository.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="branch" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="directory" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="owner_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="provider_type" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="repository_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="sha" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="latest_sync" type="object">
  &lt;p&gt;Information about a resource sync attempt.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="initial_revision" type="object" required>
      &lt;p&gt;Information about the revision for a specific sync event, such as the branch, owner ID, and name of the repository.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="branch" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="directory" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="owner_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="provider_type" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="repository_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="sha" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="started_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_revision" type="object" required>
      &lt;p&gt;Information about the revision for a specific sync event, such as the branch, owner ID, and name of the repository.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="branch" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="directory" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="owner_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="provider_type" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="repository_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="sha" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
