---
title: "aws_codestar_connections_sync_configurations"
description: "`aws_codestar_connections_sync_configurations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codestar_connections_sync_configurations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_connections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_connections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_connections_sync_configurations"})

    		ubx.Data(codestar_connections.CodestarConnectionsSyncConfigurations, "example", codestar_connections.CodestarConnectionsSyncConfigurationsConfig{
    			RepositoryLinkId: "example",
    			SyncType:         "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarConnectionsSyncConfigurations } from "@ubx/sdk-aws/aws/data/codestar_connections/codestar_connections_sync_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_connections_sync_configurations" });

      data(CodestarConnectionsSyncConfigurations, "example", {
        repositoryLinkId: "example",
        syncType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_connections import CodestarConnectionsSyncConfigurations, CodestarConnectionsSyncConfigurationsConfig

    def describe():
        ubx.intent("look up aws_codestar_connections_sync_configurations")

        ubx.data(CodestarConnectionsSyncConfigurations, "example", CodestarConnectionsSyncConfigurationsConfig(
            repository_link_id="example",
            sync_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_link_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sync_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>
