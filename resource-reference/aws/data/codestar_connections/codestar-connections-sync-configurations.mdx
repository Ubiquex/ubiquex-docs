---
title: "aws_codestar_connections_sync_configurations"
description: "`aws_codestar_connections_sync_configurations` looks up a real CodeStar (CodeConnections) sync configuration..."
---

`aws_codestar_connections_sync_configurations` looks up a real CodeStar (CodeConnections) sync configuration, keeping a target resource continuously synced from a repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codestar_connections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_connections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_connections_sync_configurations"})

    		ubx.Data(codestar_connections.CodestarConnectionsSyncConfigurations, "example", codestar_connections.CodestarConnectionsSyncConfigurationsConfig{
    			RepositoryLinkId: "example",
    			SyncType:         "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CodestarConnectionsSyncConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/codestar_connections/codestar_connections_sync_configurations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_connections_sync_configurations" });

      data(CodestarConnectionsSyncConfigurations, "example", {
        repositoryLinkId: "example",
        syncType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_connections import CodestarConnectionsSyncConfigurations, CodestarConnectionsSyncConfigurationsConfig

    def describe():
        ubx.intent("look up aws_codestar_connections_sync_configurations")

        ubx.data(CodestarConnectionsSyncConfigurations, "example", CodestarConnectionsSyncConfigurationsConfig(
            repository_link_id="example",
            sync_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_link_id" type="string" required>
  The real, unique identifier of the repository link this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  The real type of synchronization this request performs. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_configurations" type="list(object)">
  The real sync configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
