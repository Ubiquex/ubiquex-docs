---
title: "aws_codestar_connections_repository_sync_status"
description: "`aws_codestar_connections_repository_sync_status` reads the real, current sync status between a repository and its own CodeStar (CodeConnections) sync..."
---

`aws_codestar_connections_repository_sync_status` reads the real, current sync status between a repository and its own CodeStar (CodeConnections) sync configuration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_connections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_connections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_connections_repository_sync_status"})

    		ubx.Data(codestar_connections.CodestarConnectionsRepositorySyncStatus, "example", codestar_connections.CodestarConnectionsRepositorySyncStatusConfig{
    			Branch:           "example",
    			RepositoryLinkId: "example",
    			SyncType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarConnectionsRepositorySyncStatus } from "@ubx/sdk-aws/aws/data/codestar_connections/codestar_connections_repository_sync_status";

    export default stack("example", () => {
      intent({
        summary: "look up aws_codestar_connections_repository_sync_status",
      });

      data(CodestarConnectionsRepositorySyncStatus, "example", {
        branch: "example",
        repositoryLinkId: "example",
        syncType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_connections import CodestarConnectionsRepositorySyncStatus, CodestarConnectionsRepositorySyncStatusConfig

    def describe():
        ubx.intent("look up aws_codestar_connections_repository_sync_status")

        ubx.data(CodestarConnectionsRepositorySyncStatus, "example", CodestarConnectionsRepositorySyncStatusConfig(
            branch="example",
            repository_link_id="example",
            sync_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  The real, source-control branch this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repository_link_id" type="string" required>
  The real, unique identifier of the repository link this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sync_type" type="string" required>
  The real type of synchronization this request performs. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="latest_sync" type="object">
  &lt;p&gt;Information about a repository sync attempt for a repository with a sync configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="started_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
