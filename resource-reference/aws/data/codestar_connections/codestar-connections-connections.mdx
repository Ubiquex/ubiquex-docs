---
title: "aws_codestar_connections_connections"
description: "`aws_codestar_connections_connections` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codestar_connections_connections` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestar_connections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codestar_connections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codestar_connections_connections"})

    		ubx.Data(codestar_connections.CodestarConnectionsConnections, "example", codestar_connections.CodestarConnectionsConnectionsConfig{
    			HostArnFilter:      "example",
    			MaxResults:         1,
    			ProviderTypeFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodestarConnectionsConnections } from "@ubx/sdk-aws/aws/data/codestar_connections/codestar_connections_connections";

    export default stack("example", () => {
      intent({ summary: "look up aws_codestar_connections_connections" });

      data(CodestarConnectionsConnections, "example", {
        hostArnFilter: "example",
        maxResults: 1,
        providerTypeFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codestar_connections import CodestarConnectionsConnections, CodestarConnectionsConnectionsConfig

    def describe():
        ubx.intent("look up aws_codestar_connections_connections")

        ubx.data(CodestarConnectionsConnections, "example", CodestarConnectionsConnectionsConfig(
            host_arn_filter="example",
            max_results=1,
            provider_type_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="host_arn_filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="provider_type_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connections" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
