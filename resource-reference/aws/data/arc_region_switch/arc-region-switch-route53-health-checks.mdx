---
title: "aws_arc_region_switch_route53_health_checks"
description: "`aws_arc_region_switch_route53_health_checks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_arc_region_switch_route53_health_checks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arc_region_switch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/arc_region_switch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_arc_region_switch_route53_health_checks"})

    		ubx.Data(arc_region_switch.ArcRegionSwitchRoute53HealthChecks, "example", arc_region_switch.ArcRegionSwitchRoute53HealthChecksConfig{
    			Arn:          "arn:aws:iam::123456789012:policy/example",
    			HostedZoneId: "example",
    			MaxResults:   1,
    			RecordName:   "example-record-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ArcRegionSwitchRoute53HealthChecks } from "@ubx/sdk-aws/aws/data/arc_region_switch/arc_region_switch_route53_health_checks";

    export default stack("example", () => {
      intent({ summary: "look up aws_arc_region_switch_route53_health_checks" });

      data(ArcRegionSwitchRoute53HealthChecks, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        hostedZoneId: "example",
        maxResults: 1,
        recordName: "example-record-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.arc_region_switch import ArcRegionSwitchRoute53HealthChecks, ArcRegionSwitchRoute53HealthChecksConfig

    def describe():
        ubx.intent("look up aws_arc_region_switch_route53_health_checks")

        ubx.data(ArcRegionSwitchRoute53HealthChecks, "example", ArcRegionSwitchRoute53HealthChecksConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            hosted_zone_id="example",
            max_results=1,
            record_name="example-record-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="hosted_zone_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="record_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="health_checks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
