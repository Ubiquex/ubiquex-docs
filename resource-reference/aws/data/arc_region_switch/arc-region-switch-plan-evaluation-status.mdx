---
title: "aws_arc_region_switch_plan_evaluation_status"
description: "`aws_arc_region_switch_plan_evaluation_status` reads the real, current evaluation status of an AWS Region Switch plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	arc_region_switch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/arc_region_switch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_arc_region_switch_plan_evaluation_status"})

    		ubx.Data(arc_region_switch.ArcRegionSwitchPlanEvaluationStatus, "example", arc_region_switch.ArcRegionSwitchPlanEvaluationStatusConfig{
    			PlanArn:    "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ArcRegionSwitchPlanEvaluationStatus } from "./local-sdk/aws/sdk/typescript/aws/data/arc_region_switch/arc_region_switch_plan_evaluation_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_arc_region_switch_plan_evaluation_status" });

      data(ArcRegionSwitchPlanEvaluationStatus, "example", {
        planArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.arc_region_switch import ArcRegionSwitchPlanEvaluationStatus, ArcRegionSwitchPlanEvaluationStatusConfig

    def describe():
        ubx.intent("look up aws_arc_region_switch_plan_evaluation_status")

        ubx.data(ArcRegionSwitchPlanEvaluationStatus, "example", ArcRegionSwitchPlanEvaluationStatusConfig(
            plan_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plan_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the plan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evaluation_state" type="string">
  The real, current state of the evaluation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_evaluated_version" type="string">
  The real version of the last evaluated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_evaluation_time" type="string">
  The real timestamp of the last evaluation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="region" type="string">
  The real AWS region this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="list(object)">
  The real warnings encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
