---
title: "aws_servicediscovery_instances"
description: "`aws_servicediscovery_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicediscovery_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicediscovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicediscovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicediscovery_instances"})

    		ubx.Data(servicediscovery.Instances, "example", servicediscovery.InstancesConfig{
    			ServiceId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Instances } from "@ubx/sdk-aws/aws/data/servicediscovery/instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicediscovery_instances" });

      data(Instances, "example", {
        serviceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicediscovery import Instances, InstancesConfig

    def describe():
        ubx.intent("look up aws_servicediscovery_instances")

        ubx.data(Instances, "example", InstancesConfig(
            service_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  Returned by the lookup.
</ResponseField>
