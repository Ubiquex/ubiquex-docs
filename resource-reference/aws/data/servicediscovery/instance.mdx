---
title: "aws_servicediscovery_instance"
description: "`data_aws_servicediscovery_instance` looks up a real, already-existing `aws_service_discovery_instance`."
---

It registers a real, discoverable endpoint, an IP address or ARN plus custom attributes, under an `aws_service_discovery_service`, the actual instance that discovery queries against that service return.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicediscovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicediscovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicediscovery_instance"})

    		ubx.Data(servicediscovery.Instance, "example", servicediscovery.InstanceConfig{
    			InstanceId: "example",
    			ServiceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Instance } from "@ubx/sdk-aws/aws/data/servicediscovery/instance";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicediscovery_instance" });

      data(Instance, "example", {
        instanceId: "example",
        serviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicediscovery import Instance, InstanceConfig

    def describe():
        ubx.intent("look up aws_servicediscovery_instance")

        ubx.data(Instance, "example", InstanceConfig(
            instance_id="example",
            service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  The real, unique identifier of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance" type="object">
  &lt;p&gt;A complex type that contains information about an instance that Cloud Map creates when you submit a &lt;code&gt;RegisterInstance&lt;/code&gt; request.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_by_account" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creator_request_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_owner" type="string">
  The real owner of the resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
