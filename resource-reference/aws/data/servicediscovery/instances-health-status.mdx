---
title: "aws_servicediscovery_instances_health_status"
description: "`aws_servicediscovery_instances_health_status` reads the real, current health status of an instance registered with a Cloud Map service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	servicediscovery "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicediscovery"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicediscovery_instances_health_status"})

    		ubx.Data(servicediscovery.InstancesHealthStatus, "example", servicediscovery.InstancesHealthStatusConfig{
    			ServiceId:  "example",
    			Instances:  []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InstancesHealthStatus } from "./local-sdk/aws/sdk/typescript/aws/data/servicediscovery/instances_health_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicediscovery_instances_health_status" });

      data(InstancesHealthStatus, "example", {
        serviceId: "example",
        instances: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.servicediscovery import InstancesHealthStatus, InstancesHealthStatusConfig

    def describe():
        ubx.intent("look up aws_servicediscovery_instances_health_status")

        ubx.data(InstancesHealthStatus, "example", InstancesHealthStatusConfig(
            service_id="example",
            instances=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instances" type="list(string)">
  The real instances included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  The real, unique identifier of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="map(string)">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
