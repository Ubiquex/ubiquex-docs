---
title: "aws_voiceid_fraudsters"
description: "`aws_voiceid_fraudsters` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voiceid_fraudsters` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_fraudsters"})

    		ubx.Data(voiceid.Fraudsters, "example", voiceid.FraudstersConfig{
    			DomainId:    "example",
    			MaxResults:  1,
    			WatchlistId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Fraudsters } from "@ubx/sdk-aws/aws/data/voiceid/fraudsters";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_fraudsters" });

      data(Fraudsters, "example", {
        domainId: "example",
        maxResults: 1,
        watchlistId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import Fraudsters, FraudstersConfig

    def describe():
        ubx.intent("look up aws_voiceid_fraudsters")

        ubx.data(Fraudsters, "example", FraudstersConfig(
            domain_id="example",
            max_results=1,
            watchlist_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="watchlist_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fraudster_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
