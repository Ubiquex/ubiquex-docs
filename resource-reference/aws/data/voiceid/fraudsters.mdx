---
title: "aws_voiceid_fraudsters"
description: "`aws_voiceid_fraudsters` reads the real fraudsters registered in a Voice ID domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_fraudsters"})

    		ubx.Data(voiceid.Fraudsters, "example", voiceid.FraudstersConfig{
    			DomainId:    "example",
    			MaxResults:  1,
    			WatchlistId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Fraudsters } from "@ubx/sdk-aws/aws/data/voiceid/fraudsters";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_fraudsters" });

      data(Fraudsters, "example", {
        domainId: "example",
        maxResults: 1,
        watchlistId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import Fraudsters, FraudstersConfig

    def describe():
        ubx.intent("look up aws_voiceid_fraudsters")

        ubx.data(Fraudsters, "example", FraudstersConfig(
            domain_id="example",
            max_results=1,
            watchlist_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="watchlist_id" type="string">
  The real, unique identifier of the watchlist this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fraudster_summaries" type="list(object)">
  The real, summarized fraudsters included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
