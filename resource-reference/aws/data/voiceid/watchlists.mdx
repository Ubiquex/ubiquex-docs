---
title: "aws_voiceid_watchlists"
description: "`aws_voiceid_watchlists` looks up a real Voice ID watchlist, a curated set of fraudsters checked against during a call."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_watchlists"})

    		ubx.Data(voiceid.Watchlists, "example", voiceid.WatchlistsConfig{
    			DomainId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Watchlists } from "./local-sdk/aws/sdk/typescript/aws/data/voiceid/watchlists.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_watchlists" });

      data(Watchlists, "example", {
        domainId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import Watchlists, WatchlistsConfig

    def describe():
        ubx.intent("look up aws_voiceid_watchlists")

        ubx.data(Watchlists, "example", WatchlistsConfig(
            domain_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="watchlist_summaries" type="list(object)">
  The real, summarized watchlists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
