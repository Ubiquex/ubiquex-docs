---
title: "aws_voiceid_speaker_enrollment_jobs"
description: "`aws_voiceid_speaker_enrollment_jobs` reads the real speaker enrollment jobs run for a Voice ID domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_speaker_enrollment_jobs"})

    		ubx.Data(voiceid.SpeakerEnrollmentJobs, "example", voiceid.SpeakerEnrollmentJobsConfig{
    			DomainId:   "example",
    			JobStatus:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SpeakerEnrollmentJobs } from "@ubx/sdk-aws/aws/data/voiceid/speaker_enrollment_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_speaker_enrollment_jobs" });

      data(SpeakerEnrollmentJobs, "example", {
        domainId: "example",
        jobStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import SpeakerEnrollmentJobs, SpeakerEnrollmentJobsConfig

    def describe():
        ubx.intent("look up aws_voiceid_speaker_enrollment_jobs")

        ubx.data(SpeakerEnrollmentJobs, "example", SpeakerEnrollmentJobsConfig(
            domain_id="example",
            job_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_summaries" type="list(object)">
  The real, summarized jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
