---
title: "aws_voiceid_speaker_enrollment_job"
description: "`aws_voiceid_speaker_enrollment_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voiceid_speaker_enrollment_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_speaker_enrollment_job"})

    		ubx.Data(voiceid.SpeakerEnrollmentJob, "example", voiceid.SpeakerEnrollmentJobConfig{
    			DomainId: "example",
    			JobId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SpeakerEnrollmentJob } from "@ubx/sdk-aws/aws/data/voiceid/speaker_enrollment_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_speaker_enrollment_job" });

      data(SpeakerEnrollmentJob, "example", {
        domainId: "example",
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import SpeakerEnrollmentJob, SpeakerEnrollmentJobConfig

    def describe():
        ubx.intent("look up aws_voiceid_speaker_enrollment_job")

        ubx.data(SpeakerEnrollmentJob, "example", SpeakerEnrollmentJobConfig(
            domain_id="example",
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job" type="object">
  &lt;p&gt;Contains all the information about a speaker enrollment job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="data_access_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="domain_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ended_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enrollment_config" type="object">
      &lt;p&gt;Contains configurations defining enrollment behavior for the batch job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="existing_enrollment_action" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="fraud_detection_config" type="object">
          &lt;p&gt;The fraud detection configuration to be used during the batch speaker enrollment job.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="fraud_detection_action" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="risk_threshold" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="watchlist_ids" type="list(string)">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="failure_details" type="object">
      &lt;p&gt;Contains error details for a failed batch job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="status_code" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="input_data_config" type="object">
      &lt;p&gt;The configuration containing input file information for a batch job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_uri" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="job_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_progress" type="object">
      &lt;p&gt;Indicates the completion progress for a batch job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="percent_complete" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="job_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="output_data_config" type="object">
      &lt;p&gt;The configuration containing output file information for a batch job.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="kms_key_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="s3_uri" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
