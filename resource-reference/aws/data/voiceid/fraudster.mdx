---
title: "aws_voiceid_fraudster"
description: "`aws_voiceid_fraudster` looks up one real fraudster registered in a Voice ID domain's own watchlist."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voiceid "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voiceid"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voiceid_fraudster"})

    		ubx.Data(voiceid.Fraudster, "example", voiceid.FraudsterConfig{
    			DomainId:    "example",
    			FraudsterId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Fraudster } from "@ubx/sdk-aws/aws/data/voiceid/fraudster";

    export default stack("example", () => {
      intent({ summary: "look up aws_voiceid_fraudster" });

      data(Fraudster, "example", {
        domainId: "example",
        fraudsterId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voiceid import Fraudster, FraudsterConfig

    def describe():
        ubx.intent("look up aws_voiceid_fraudster")

        ubx.data(Fraudster, "example", FraudsterConfig(
            domain_id="example",
            fraudster_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_id" type="string" required>
  The real, unique identifier of the domain this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fraudster_id" type="string" required>
  The real, unique identifier of the fraudster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fraudster" type="object">
  &lt;p&gt;Contains all the information about a fraudster.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="domain_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="generated_fraudster_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="watchlist_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
