---
title: "aws_pinpoint_email_account"
description: "`aws_pinpoint_email_account` reads the real, account-wide Pinpoint Email sending status, quota, and reputation details."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint_email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint_email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_email_account"})

    		ubx.Data(pinpoint_email.PinpointEmailAccount, "example", pinpoint_email.PinpointEmailAccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PinpointEmailAccount } from "@ubx/sdk-aws/aws/data/pinpoint_email/pinpoint_email_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_email_account" });

      data(PinpointEmailAccount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint_email import PinpointEmailAccount, PinpointEmailAccountConfig

    def describe():
        ubx.intent("look up aws_pinpoint_email_account")

        ubx.data(PinpointEmailAccount, "example", PinpointEmailAccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="dedicated_ip_auto_warmup_enabled" type="bool">
  Whether the dedicated ip auto warmup is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="enforcement_status" type="string">
  The real, current status of the enforcement this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="production_access_enabled" type="bool">
  Whether the production access is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="send_quota" type="object">
  &lt;p&gt;An object that contains information about the per-day and per-second sending limits for your Amazon Pinpoint account in the current AWS Region.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="max24_hour_send" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_send_rate" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sent_last24_hours" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sending_enabled" type="bool">
  Whether the sending is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>
