---
title: "aws_pinpoint_email_dedicated_ips"
description: "`aws_pinpoint_email_dedicated_ips` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_email_dedicated_ips` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint_email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint_email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_email_dedicated_ips"})

    		ubx.Data(pinpoint_email.PinpointEmailDedicatedIps, "example", pinpoint_email.PinpointEmailDedicatedIpsConfig{
    			PageSize: 1,
    			PoolName: "example-pool-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PinpointEmailDedicatedIps } from "@ubx/sdk-aws/aws/data/pinpoint_email/pinpoint_email_dedicated_ips";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_email_dedicated_ips" });

      data(PinpointEmailDedicatedIps, "example", {
        pageSize: 1,
        poolName: "example-pool-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint_email import PinpointEmailDedicatedIps, PinpointEmailDedicatedIpsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_email_dedicated_ips")

        ubx.data(PinpointEmailDedicatedIps, "example", PinpointEmailDedicatedIpsConfig(
            page_size=1,
            pool_name="example-pool-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="pool_name" type="string">
  &lt;p&gt;The name of a dedicated IP pool.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dedicated_ips" type="list(object)">
  &lt;p&gt;A list of dedicated IP addresses that are associated with your Amazon Pinpoint account.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
