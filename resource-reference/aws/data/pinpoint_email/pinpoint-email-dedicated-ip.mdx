---
title: "aws_pinpoint_email_dedicated_ip"
description: "`aws_pinpoint_email_dedicated_ip` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_email_dedicated_ip` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint_email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint_email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_email_dedicated_ip"})

    		ubx.Data(pinpoint_email.PinpointEmailDedicatedIp, "example", pinpoint_email.PinpointEmailDedicatedIpConfig{
    			Ip: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PinpointEmailDedicatedIp } from "@ubx/sdk-aws/aws/data/pinpoint_email/pinpoint_email_dedicated_ip";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_email_dedicated_ip" });

      data(PinpointEmailDedicatedIp, "example", {
        ip: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint_email import PinpointEmailDedicatedIp, PinpointEmailDedicatedIpConfig

    def describe():
        ubx.intent("look up aws_pinpoint_email_dedicated_ip")

        ubx.data(PinpointEmailDedicatedIp, "example", PinpointEmailDedicatedIpConfig(
            ip="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ip" type="string" required>
  &lt;p&gt;A dedicated IP address that is associated with your Amazon Pinpoint account.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dedicated_ip" type="object">
  &lt;p&gt;Contains information about a dedicated IP address that is associated with your Amazon Pinpoint account.&lt;/p&gt; &lt;p&gt;&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ip" type="string" required>
      &lt;p&gt;A dedicated IP address that is associated with your Amazon Pinpoint account.&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="pool_name" type="string">
      &lt;p&gt;The name of a dedicated IP pool.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="warmup_percentage" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="warmup_status" type="string" required>
      &lt;p&gt;The warmup status of a dedicated IP.&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
