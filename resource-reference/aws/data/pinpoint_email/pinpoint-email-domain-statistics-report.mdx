---
title: "aws_pinpoint_email_domain_statistics_report"
description: "`aws_pinpoint_email_domain_statistics_report` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_email_domain_statistics_report` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint_email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint_email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_email_domain_statistics_report"})

    		ubx.Data(pinpoint_email.PinpointEmailDomainStatisticsReport, "example", pinpoint_email.PinpointEmailDomainStatisticsReportConfig{
    			Domain:    "example",
    			EndDate:   "example",
    			StartDate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PinpointEmailDomainStatisticsReport } from "@ubx/sdk-aws/aws/data/pinpoint_email/pinpoint_email_domain_statistics_report";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_email_domain_statistics_report" });

      data(PinpointEmailDomainStatisticsReport, "example", {
        domain: "example",
        endDate: "example",
        startDate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint_email import PinpointEmailDomainStatisticsReport, PinpointEmailDomainStatisticsReportConfig

    def describe():
        ubx.intent("look up aws_pinpoint_email_domain_statistics_report")

        ubx.data(PinpointEmailDomainStatisticsReport, "example", PinpointEmailDomainStatisticsReportConfig(
            domain="example",
            end_date="example",
            start_date="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="end_date" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="daily_volumes" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="overall_volume" type="object">
  &lt;p&gt;An object that contains information about email that was sent from the selected domain.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="domain_isp_placements" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="read_rate_percent" type="number">
      &lt;p&gt;An object that contains information about inbox placement percentages.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="volume_statistics" type="object">
      &lt;p&gt;An object that contains information about the amount of email that was delivered to recipients.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="inbox_raw_count" type="number">
          &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
        </ResponseField>
        <ResponseField name="projected_inbox" type="number">
          &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
        </ResponseField>
        <ResponseField name="projected_spam" type="number">
          &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
        </ResponseField>
        <ResponseField name="spam_raw_count" type="number">
          &lt;p&gt;An object that contains information about inbox placement volume.&lt;/p&gt; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
