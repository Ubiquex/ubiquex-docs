---
title: "aws_pinpoint_email_configuration_sets"
description: "`aws_pinpoint_email_configuration_sets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_email_configuration_sets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint_email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint_email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_email_configuration_sets"})

    		ubx.Data(pinpoint_email.PinpointEmailConfigurationSets, "example", pinpoint_email.PinpointEmailConfigurationSetsConfig{
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PinpointEmailConfigurationSets } from "@ubx/sdk-aws/aws/data/pinpoint_email/pinpoint_email_configuration_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_email_configuration_sets" });

      data(PinpointEmailConfigurationSets, "example", {
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint_email import PinpointEmailConfigurationSets, PinpointEmailConfigurationSetsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_email_configuration_sets")

        ubx.data(PinpointEmailConfigurationSets, "example", PinpointEmailConfigurationSetsConfig(
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_sets" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
