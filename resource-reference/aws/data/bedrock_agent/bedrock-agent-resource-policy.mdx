---
title: "aws_bedrock_agent_resource_policy"
description: "`aws_bedrock_agent_resource_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_bedrock_agent_resource_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock_agent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bedrock_agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bedrock_agent_resource_policy"})

    		ubx.Data(bedrock_agent.BedrockAgentResourcePolicy, "example", bedrock_agent.BedrockAgentResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BedrockAgentResourcePolicy } from "@ubx/sdk-aws/aws/data/bedrock_agent/bedrock_agent_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_bedrock_agent_resource_policy" });

      data(BedrockAgentResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bedrock_agent import BedrockAgentResourcePolicy, BedrockAgentResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_bedrock_agent_resource_policy")

        ubx.data(BedrockAgentResourcePolicy, "example", BedrockAgentResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Returned by the lookup.
</ResponseField>
