---
title: "aws_waf_web_acls"
description: "`data_aws_waf_web_acls` looks up a real, already-existing `aws_waf_web_acl`."
---

It creates a real, top-level web ACL under AWS WAF Classic (global, for CloudFront), evaluating its ordered list of `aws_waf_rule`s against every request and allowing or blocking based on the first match. AWS WAF Classic support ended September 30, 2025, roughly eleven months in the past; AWS's current, actively developed offering is `aws_wafv2_web_acl`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_web_acls"})

    		ubx.Data(waf.WebAcls, "example", waf.WebAclsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WebAcls } from "@ubx/sdk-aws/aws/data/waf/web_acls";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_web_acls" });

      data(WebAcls, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import WebAcls, WebAclsConfig

    def describe():
        ubx.intent("look up aws_waf_web_acls")

        ubx.data(WebAcls, "example", WebAclsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="web_acls" type="list(object)">
  The real AWS WAF web ACLs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
