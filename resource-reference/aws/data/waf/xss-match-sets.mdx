---
title: "aws_waf_xss_match_sets"
description: "`data_aws_waf_xss_match_sets` looks up a real, already-existing `aws_waf_xss_match_set`."
---

It defines a real condition, matching cross-site-scripting-like patterns in a request part, that an `aws_waf_rule` references. Like the rest of AWS WAF Classic, support for this resource ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_xss_match_sets"})

    		ubx.Data(waf.XssMatchSets, "example", waf.XssMatchSetsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { XssMatchSets } from "@ubx/sdk-aws/aws/data/waf/xss_match_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_xss_match_sets" });

      data(XssMatchSets, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import XssMatchSets, XssMatchSetsConfig

    def describe():
        ubx.intent("look up aws_waf_xss_match_sets")

        ubx.data(XssMatchSets, "example", XssMatchSetsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="xss_match_sets" type="list(object)">
  The real xss match sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
