---
title: "aws_waf_logging_configurations"
description: "`aws_waf_logging_configurations` reads the real logging configurations of WAF Classic Global web ACLs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_logging_configurations"})

    		ubx.Data(waf.LoggingConfigurations, "example", waf.LoggingConfigurationsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoggingConfigurations } from "@ubx/sdk-aws/aws/data/waf/logging_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_logging_configurations" });

      data(LoggingConfigurations, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import LoggingConfigurations, LoggingConfigurationsConfig

    def describe():
        ubx.intent("look up aws_waf_logging_configurations")

        ubx.data(LoggingConfigurations, "example", LoggingConfigurationsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_configurations" type="list(object)">
  The real logging configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
