---
title: "aws_waf_rate_based_rules"
description: "`data_aws_waf_rate_based_rules` looks up a real, already-existing `aws_wafregional_rate_based_rule`."
---

It defines a real rule under AWS WAF Classic Regional that automatically tracks request rate per source IP and blocks addresses that exceed a configured threshold, distinct from `aws_wafregional_rule`'s own static, condition-based matching. Support for AWS WAF Classic ended September 30, 2025, roughly eleven months in the past; AWS's current offering, AWS WAFv2, has its own built-in rate-based rule statement type.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_rate_based_rules"})

    		ubx.Data(waf.RateBasedRules, "example", waf.RateBasedRulesConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RateBasedRules } from "@ubx/sdk-aws/aws/data/waf/rate_based_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_rate_based_rules" });

      data(RateBasedRules, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import RateBasedRules, RateBasedRulesConfig

    def describe():
        ubx.intent("look up aws_waf_rate_based_rules")

        ubx.data(RateBasedRules, "example", RateBasedRulesConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
