---
title: "aws_waf_activated_rules_in_rule_group"
description: "`aws_waf_activated_rules_in_rule_group` reads the real rules activated within a WAF Classic Global rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_activated_rules_in_rule_group"})

    		ubx.Data(waf.ActivatedRulesInRuleGroup, "example", waf.ActivatedRulesInRuleGroupConfig{
    			Limit:       1,
    			RuleGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ActivatedRulesInRuleGroup } from "./local-sdk/aws/sdk/typescript/aws/data/waf/activated_rules_in_rule_group.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_activated_rules_in_rule_group" });

      data(ActivatedRulesInRuleGroup, "example", {
        limit: 1,
        ruleGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf import ActivatedRulesInRuleGroup, ActivatedRulesInRuleGroupConfig

    def describe():
        ubx.intent("look up aws_waf_activated_rules_in_rule_group")

        ubx.data(ActivatedRulesInRuleGroup, "example", ActivatedRulesInRuleGroupConfig(
            limit=1,
            rule_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rule_group_id" type="string">
  The real, unique identifier of the rule group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activated_rules" type="list(object)">
  The real rules for the activated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
