---
title: "aws_waf_permission_policy"
description: "`aws_waf_permission_policy` reads the real resource-based policy attached to a WAF Classic Global rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_permission_policy"})

    		ubx.Data(waf.PermissionPolicy, "example", waf.PermissionPolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PermissionPolicy } from "@ubx/sdk-aws/aws/data/waf/permission_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_permission_policy" });

      data(PermissionPolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import PermissionPolicy, PermissionPolicyConfig

    def describe():
        ubx.intent("look up aws_waf_permission_policy")

        ubx.data(PermissionPolicy, "example", PermissionPolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
