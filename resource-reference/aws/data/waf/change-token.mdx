---
title: "aws_waf_change_token"
description: "`aws_waf_change_token` reads a real, unique token required before making a WAF Classic Global API mutation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_change_token"})

    		ubx.Data(waf.ChangeToken, "example", waf.ChangeTokenConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ChangeToken } from "./local-sdk/aws/sdk/typescript/aws/data/waf/change_token.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_change_token" });

      data(ChangeToken, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf import ChangeToken, ChangeTokenConfig

    def describe():
        ubx.intent("look up aws_waf_change_token")

        ubx.data(ChangeToken, "example", ChangeTokenConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="change_token" type="string">
  An opaque token from a previous response, required to make another change to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
