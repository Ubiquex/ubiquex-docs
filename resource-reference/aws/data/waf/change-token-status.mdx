---
title: "aws_waf_change_token_status"
description: "`aws_waf_change_token_status` reads the real, current propagation status of a WAF Classic Global change token."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_change_token_status"})

    		ubx.Data(waf.ChangeTokenStatus, "example", waf.ChangeTokenStatusConfig{
    			ChangeToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ChangeTokenStatus } from "./local-sdk/aws/sdk/typescript/aws/data/waf/change_token_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_change_token_status" });

      data(ChangeTokenStatus, "example", {
        changeToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf import ChangeTokenStatus, ChangeTokenStatusConfig

    def describe():
        ubx.intent("look up aws_waf_change_token_status")

        ubx.data(ChangeTokenStatus, "example", ChangeTokenStatusConfig(
            change_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="change_token" type="string" required>
  An opaque token from a previous response, required to make another change to this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_token_status" type="string">
  The real, current status of the change token this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
