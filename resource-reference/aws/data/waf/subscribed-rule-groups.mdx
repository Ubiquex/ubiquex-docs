---
title: "aws_waf_subscribed_rule_groups"
description: "`aws_waf_subscribed_rule_groups` reads the real WAF Marketplace rule groups subscribed to for WAF Classic Global."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_subscribed_rule_groups"})

    		ubx.Data(waf.SubscribedRuleGroups, "example", waf.SubscribedRuleGroupsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscribedRuleGroups } from "@ubx/sdk-aws/aws/data/waf/subscribed_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_subscribed_rule_groups" });

      data(SubscribedRuleGroups, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf import SubscribedRuleGroups, SubscribedRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_waf_subscribed_rule_groups")

        ubx.data(SubscribedRuleGroups, "example", SubscribedRuleGroupsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_groups" type="list(object)">
  The real rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
