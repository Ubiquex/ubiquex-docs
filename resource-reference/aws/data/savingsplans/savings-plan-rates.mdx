---
title: "aws_savingsplans_savings_plan_rates"
description: "`aws_savingsplans_savings_plan_rates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_savingsplans_savings_plan_rates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	savingsplans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/savingsplans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_savingsplans_savings_plan_rates"})

    		ubx.Data(savingsplans.SavingsPlanRates, "example", savingsplans.SavingsPlanRatesConfig{
    			SavingsPlanId: "example",
    			Filters:       []map[string]any{{"name": "example-name"}},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SavingsPlanRates } from "@ubx/sdk-aws/aws/data/savingsplans/savings_plan_rates";

    export default stack("example", () => {
      intent({ summary: "look up aws_savingsplans_savings_plan_rates" });

      data(SavingsPlanRates, "example", {
        savingsPlanId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.savingsplans import SavingsPlanRates, SavingsPlanRatesConfig

    def describe():
        ubx.intent("look up aws_savingsplans_savings_plan_rates")

        ubx.data(SavingsPlanRates, "example", SavingsPlanRatesConfig(
            savings_plan_id="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="savings_plan_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="search_results" type="list(object)">
  Returned by the lookup.
</ResponseField>
