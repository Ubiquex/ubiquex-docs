---
title: "aws_savingsplans_savings_plans_offering_rates"
description: "`aws_savingsplans_savings_plans_offering_rates` reads the real rates for a real, available Savings Plans offering."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	savingsplans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/savingsplans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_savingsplans_savings_plans_offering_rates"})

    		ubx.Data(savingsplans.SavingsPlansOfferingRates, "example", savingsplans.SavingsPlansOfferingRatesConfig{
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			MaxResults:                1,
    			Operations:                []string{"example"},
    			Products:                  []string{"example"},
    			SavingsPlanOfferingIds:    []string{"example"},
    			SavingsPlanPaymentOptions: []string{"example"},
    			SavingsPlanTypes:          []string{"example"},
    			ServiceCodes:              []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SavingsPlansOfferingRates } from "@ubx/sdk-aws/aws/data/savingsplans/savings_plans_offering_rates";

    export default stack("example", () => {
      intent({ summary: "look up aws_savingsplans_savings_plans_offering_rates" });

      data(SavingsPlansOfferingRates, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        operations: ["example"],
        products: ["example"],
        savingsPlanOfferingIds: ["example"],
        savingsPlanPaymentOptions: ["example"],
        savingsPlanTypes: ["example"],
        serviceCodes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.savingsplans import SavingsPlansOfferingRates, SavingsPlansOfferingRatesConfig
    from ubx.aws.data.savingsplans.savings_plans_offering_rates import SavingsPlansOfferingRates_Filters

    def describe():
        ubx.intent("look up aws_savingsplans_savings_plans_offering_rates")

        ubx.data(SavingsPlansOfferingRates, "example", SavingsPlansOfferingRatesConfig(
            filters=[SavingsPlansOfferingRates_Filters(name="example-name")],
            max_results=1,
            operations=["example"],
            products=["example"],
            savings_plan_offering_ids=["example"],
            savings_plan_payment_options=["example"],
            savings_plan_types=["example"],
            service_codes=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="operations" type="list(string)">
  The real operations included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="products" type="list(string)">
  The real products included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="savings_plan_offering_ids" type="list(string)">
  The real, unique identifiers of the savings plan offerings this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="savings_plan_payment_options" type="list(string)">
  The real, available options for the savings plan payment. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="savings_plan_types" type="list(string)">
  The real, discriminating types of savings plans this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_codes" type="list(string)">
  The real, unique codes identifying the AWS services included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="usage_types" type="list(string)">
  The real, discriminating types of usages this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="search_results" type="list(object)">
  The real search results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
