---
title: "aws_savingsplans_savings_plans_offerings"
description: "`aws_savingsplans_savings_plans_offerings` reads a real, available Savings Plans offering that can be purchased."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	savingsplans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/savingsplans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_savingsplans_savings_plans_offerings"})

    		ubx.Data(savingsplans.SavingsPlansOfferings, "example", savingsplans.SavingsPlansOfferingsConfig{
    			Currencies:     []string{"example"},
    			Descriptions:   []string{"example"},
    			Durations:      []string{"example"},
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			OfferingIds:    []string{"example"},
    			Operations:     []string{"example"},
    			PaymentOptions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SavingsPlansOfferings } from "./local-sdk/aws/sdk/typescript/aws/data/savingsplans/savings_plans_offerings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_savingsplans_savings_plans_offerings" });

      data(SavingsPlansOfferings, "example", {
        currencies: ["example"],
        descriptions: ["example"],
        durations: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        offeringIds: ["example"],
        operations: ["example"],
        paymentOptions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.savingsplans import SavingsPlansOfferings, SavingsPlansOfferingsConfig

    def describe():
        ubx.intent("look up aws_savingsplans_savings_plans_offerings")

        ubx.data(SavingsPlansOfferings, "example", SavingsPlansOfferingsConfig(
            currencies=["example"],
            descriptions=["example"],
            durations=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
            offering_ids=["example"],
            operations=["example"],
            payment_options=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="currencies" type="list(string)">
  The real currencies included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="descriptions" type="list(string)">
  The real, free-text descriptions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="durations" type="list(number)">
  The real durations included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="offering_ids" type="list(string)">
  The real, unique identifiers of the offerings this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="operations" type="list(string)">
  The real operations included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="payment_options" type="list(string)">
  The real, available options for the payment. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plan_types" type="list(string)">
  The real, discriminating types of plans this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_type" type="string">
  The real type of this product. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_codes" type="list(string)">
  The real, unique codes identifying the AWS services included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="usage_types" type="list(string)">
  The real, discriminating types of usages this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="search_results" type="list(object)">
  The real search results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
