---
title: "aws_data_sync_task"
description: "`aws_data_sync_task` is the real, actual transfer definition in AWS DataSync..."
---

`aws_data_sync_task` is the real, actual transfer definition in AWS DataSync, pairing a source `aws_data_sync_location_s3` (or another location type) with a destination location and running the encrypted, integrity-validated copy between them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sync task"})

    		ubx.Resource(data.SyncTask, "example", data.SyncTaskConfig{
    			DestinationLocationArn: "arn:aws:iam::123456789012:policy/example",
    			SourceLocationArn:      "arn:aws:iam::123456789012:policy/example",
    			Name:                   "example",
    			CloudWatchLogGroupArn:  "arn:aws:iam::123456789012:policy/example",
    			Excludes:               []map[string]any{{"filter_type": "example"}},
    			Includes:               []map[string]any{{"filter_type": "example"}},
    			ManifestConfig:         map[string]any{"source": map[string]any{"s3": map[string]any{"bucket_access_role_arn": "example"}}},
    			Options:                map[string]any{"atime": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncTask } from "./local-sdk/aws/sdk/typescript/aws/data/sync_task.ts";

    export default stack("example", () => {
      intent({ summary: "example own sync task" });

      resource(SyncTask, "example", {
        destinationLocationArn: "arn:aws:iam::123456789012:policy/example",
        sourceLocationArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        cloudWatchLogGroupArn: "arn:aws:iam::123456789012:policy/example",
        excludes: [{ filterType: "example" }],
        includes: [{ filterType: "example" }],
        manifestConfig: { source: { s3: { bucketAccessRoleArn: "example" } } },
        options: { atime: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import SyncTask, SyncTaskConfig

    def describe():
        ubx.intent("example own sync task")

        ubx.resource(SyncTask, "example", SyncTaskConfig(
            destination_location_arn="arn:aws:iam::123456789012:policy/example",
            source_location_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            cloud_watch_log_group_arn="arn:aws:iam::123456789012:policy/example",
            excludes=[{"filter_type": "example"}],
            includes=[{"filter_type": "example"}],
            manifest_config={"source": {"s3": {"bucket_access_role_arn": "example"}}},
            options={"atime": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_sync_task called "example", with destination
    location arn arn:aws:iam::123456789012:policy/example, source
    location arn arn:aws:iam::123456789012:policy/example, cloud
    watch log group arn arn:aws:iam::123456789012:policy/example,
    excludes a set value, includes a set value, manifest config a set
    value, options a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_watch_log_group_arn" type="string">
  The ARN of the Amazon CloudWatch log group that is used to monitor and log events in the task. Optional.
</ResponseField>

<ResponseField name="destination_location_arn" type="string" required>
  The ARN of an AWS storage resource's location. Required.
</ResponseField>

<ResponseField name="excludes" type="list(object)">
  Specifies the filter rules that define which files or directories to exclude from the data transfer, using pattern-based matching to skip matching paths during the sync task. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="filter_type" type="string">
      Specifies the type of filter rule used to exclude files from the DataSync task, where the only supported value is 'SIMPLE_PATTERN' for simple pattern matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies a pattern value for an entry in the task's Excludes filter list, which tells DataSync which files or objects to skip during a transfer using the simple pattern matching syntax. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="includes" type="list(object)">
  Specifies a list of filter rules that determine which files or S3 objects to include in the DataSync task, with each rule defining an Attribute (e.g., file path) and a Value pattern to match. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="filter_type" type="string">
      Specifies the filter type for the DataSync task's includes list, which determines how the filter pattern is applied (e.g., SIMPLE_PATTERN) to include files or objects in the transfer. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value pattern for an include filter rule, such as a file path or wildcard expression, that specifies which files or objects DataSync includes in the transfer. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="manifest_config" type="object">
  Configures a manifest, which is a list of files or objects that you want DataSync to transfer. Optional.
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Specifies what DataSync uses the manifest for. Optional.
    </ResponseField>
    <ResponseField name="format" type="string">
      Specifies the file format of your manifest. Optional.
    </ResponseField>
    <ResponseField name="source" type="object" required>
      Specifies the manifest that you want DataSync to use and where it's hosted. Required.
      <Expandable title="properties">
        <ResponseField name="s3" type="object">
          Specifies the S3 bucket where you're hosting the manifest that you want AWS DataSync to use. Optional.
          <Expandable title="properties">
            <ResponseField name="bucket_access_role_arn" type="string">
              Specifies the AWS Identity and Access Management (IAM) role that allows DataSync to access your manifest. Optional.
            </ResponseField>
            <ResponseField name="manifest_object_path" type="string">
              Specifies the Amazon S3 object key of your manifest. Optional.
            </ResponseField>
            <ResponseField name="manifest_object_version_id" type="string">
              Specifies the object version ID of the manifest that you want DataSync to use. Optional.
            </ResponseField>
            <ResponseField name="s3_bucket_arn" type="string">
              Specifies the Amazon Resource Name (ARN) of the S3 bucket where you're hosting your manifest. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The name of a task. This value is a text reference that is used to identify the task in the console. Optional.
</ResponseField>

<ResponseField name="options" type="object">
  Represents the options that are available to control the behavior of a StartTaskExecution operation. Optional.
  <Expandable title="properties">
    <ResponseField name="atime" type="string">
      A file metadata value that shows the last time a file was accessed (that is, when the file was read or written to). Optional.
    </ResponseField>
    <ResponseField name="bytes_per_second" type="number">
      A value that limits the bandwidth used by AWS DataSync. Optional.
    </ResponseField>
    <ResponseField name="gid" type="string">
      The group ID (GID) of the file's owners. Optional.
    </ResponseField>
    <ResponseField name="log_level" type="string">
      A value that determines the types of logs that DataSync publishes to a log stream in the Amazon CloudWatch log group that you provide. Optional.
    </ResponseField>
    <ResponseField name="mtime" type="string">
      A value that indicates the last time that a file was modified (that is, a file was written to) before the PREPARING phase. Optional.
    </ResponseField>
    <ResponseField name="object_tags" type="string">
      A value that determines whether object tags should be read from the source object store and written to the destination object store. Optional.
    </ResponseField>
    <ResponseField name="overwrite_mode" type="string">
      A value that determines whether files at the destination should be overwritten or preserved when copying files. Optional.
    </ResponseField>
    <ResponseField name="posix_permissions" type="string">
      A value that determines which users or groups can access a file for a specific purpose such as reading, writing, or execution of the file. Optional.
    </ResponseField>
    <ResponseField name="preserve_deleted_files" type="string">
      A value that specifies whether files in the destination that don't exist in the source file system should be preserved. Optional.
    </ResponseField>
    <ResponseField name="preserve_devices" type="string">
      A value that determines whether AWS DataSync should preserve the metadata of block and character devices in the source file system, and recreate the files with that device name and metadata on the destination. Optional.
    </ResponseField>
    <ResponseField name="security_descriptor_copy_flags" type="string">
      A value that determines which components of the SMB security descriptor are copied during transfer. Optional.
    </ResponseField>
    <ResponseField name="task_queueing" type="string">
      A value that determines whether tasks should be queued before executing the tasks. Optional.
    </ResponseField>
    <ResponseField name="transfer_mode" type="string">
      A value that determines whether DataSync transfers only the data and metadata that differ between the source and the destination location, or whether DataSync transfers all the content from the source, without comparing to the destination location. Optional.
    </ResponseField>
    <ResponseField name="uid" type="string">
      The user ID (UID) of the file's owner. Optional.
    </ResponseField>
    <ResponseField name="verify_mode" type="string">
      A value that determines whether a data integrity verification should be performed at the end of a task execution after all data and metadata have been transferred. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schedule" type="object">
  Specifies the schedule you want your task to use for repeated executions. Optional.
  <Expandable title="properties">
    <ResponseField name="schedule_expression" type="string">
      A cron expression that specifies when AWS DataSync initiates a scheduled transfer from a source to a destination location Optional.
    </ResponseField>
    <ResponseField name="status" type="string">
      Specifies status of a schedule. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_location_arn" type="string" required>
  The ARN of the source location for the task. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to associate with the AWS DataSync task, enabling you to categorize and manage tasks by metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of the tag assigned to the DataSync task, used for organizing and managing the task in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="task_mode" type="string">
  Specifies the task mode for the task. Optional.
</ResponseField>

<ResponseField name="task_report_config" type="object">
  Specifies how you want to configure a task report, which provides detailed information about for your Datasync transfer. Optional.
  <Expandable title="properties">
    <ResponseField name="destination" type="object" required>
      Specifies where DataSync uploads your task report. Required.
      <Expandable title="properties">
        <ResponseField name="s3" type="object">
          Specifies the Amazon S3 bucket where DataSync uploads your task report. Optional.
          <Expandable title="properties">
            <ResponseField name="bucket_access_role_arn" type="string">
              Specifies the Amazon Resource Name (ARN) of the IAM policy that allows Datasync to upload a task report to your S3 bucket. Optional.
            </ResponseField>
            <ResponseField name="s3_bucket_arn" type="string">
              Specifies the ARN of the S3 bucket where Datasync uploads your report. Optional.
            </ResponseField>
            <ResponseField name="subdirectory" type="string">
              Specifies a bucket prefix for your report. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="object_version_ids" type="string">
      Specifies whether your task report includes the new version of each object transferred into an S3 bucket, this only applies if you enable versioning on your bucket. Optional.
    </ResponseField>
    <ResponseField name="output_type" type="string" required>
      Specifies the type of task report that you want. Required.
    </ResponseField>
    <ResponseField name="overrides" type="object">
      Customizes the reporting level for aspects of your task report. For example, your report might generally only include errors, but you could specify that you want a list of successes and errors just for the files that Datasync attempted to delete in your destination location. Optional.
      <Expandable title="properties">
        <ResponseField name="deleted" type="object">
          Specifies the level of reporting for the files, objects, and directories that Datasync attempted to delete in your destination location. This only applies if you configure your task to delete data in the destination that isn't in the source. Optional.
          <Expandable title="properties">
            <ResponseField name="report_level" type="string">
              Specifies whether you want your task report to include only what went wrong with your transfer or a list of what succeeded and didn't. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="skipped" type="object">
          Specifies the level of reporting for the files, objects, and directories that Datasync attempted to skip during your transfer. Optional.
          <Expandable title="properties">
            <ResponseField name="report_level" type="string">
              Specifies whether you want your task report to include only what went wrong with your transfer or a list of what succeeded and didn't. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transferred" type="object">
          Specifies the level of reporting for the files, objects, and directories that Datasync attempted to transfer. Optional.
          <Expandable title="properties">
            <ResponseField name="report_level" type="string">
              Specifies whether you want your task report to include only what went wrong with your transfer or a list of what succeeded and didn't. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="verified" type="object">
          Specifies the level of reporting for the files, objects, and directories that Datasync attempted to verify at the end of your transfer. This only applies if you configure your task to verify data during and after the transfer (which Datasync does by default) Optional.
          <Expandable title="properties">
            <ResponseField name="report_level" type="string">
              Specifies whether you want your task report to include only what went wrong with your transfer or a list of what succeeded and didn't. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="report_level" type="string">
      Specifies whether you want your task report to include only what went wrong with your transfer or a list of what succeeded and didn't. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="destination_network_interface_arns" type="list(string)">
  The Amazon Resource Names (ARNs) of the destination ENIs (Elastic Network Interfaces) that were created for your subnet. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_network_interface_arns" type="list(string)">
  The Amazon Resource Names (ARNs) of the source ENIs (Elastic Network Interfaces) that were created for your subnet. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the task that was described. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_arn" type="string">
  The ARN of the task. Computed by AWS after `ship`.
</ResponseField>
