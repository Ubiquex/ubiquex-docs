---
title: "aws_eks_associated_access_policies"
description: "`aws_eks_associated_access_policies` reads the real EKS access policies associated with a cluster access entry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_associated_access_policies"})

    		ubx.Data(eks.AssociatedAccessPolicies, "example", eks.AssociatedAccessPoliciesConfig{
    			ClusterName:  "example-cluster-name",
    			PrincipalArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedAccessPolicies } from "@ubx/sdk-aws/aws/data/eks/associated_access_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_associated_access_policies" });

      data(AssociatedAccessPolicies, "example", {
        clusterName: "example-cluster-name",
        principalArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import AssociatedAccessPolicies, AssociatedAccessPoliciesConfig

    def describe():
        ubx.intent("look up aws_eks_associated_access_policies")

        ubx.data(AssociatedAccessPolicies, "example", AssociatedAccessPoliciesConfig(
            cluster_name="example-cluster-name",
            principal_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associated_access_policies" type="list(object)">
  The real associated access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
