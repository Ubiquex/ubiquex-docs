---
title: "aws_eks_insights_refresh"
description: "`aws_eks_insights_refresh` reads the real, current status of refreshing EKS cluster insights."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_insights_refresh"})

    		ubx.Data(eks.InsightsRefresh, "example", eks.InsightsRefreshConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsRefresh } from "@ubx/sdk-aws/aws/data/eks/insights_refresh";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_insights_refresh" });

      data(InsightsRefresh, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import InsightsRefresh, InsightsRefreshConfig

    def describe():
        ubx.intent("look up aws_eks_insights_refresh")

        ubx.data(InsightsRefresh, "example", InsightsRefreshConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ended_at" type="string">
  The real timestamp this object ended. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="started_at" type="string">
  The real timestamp this object started. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
