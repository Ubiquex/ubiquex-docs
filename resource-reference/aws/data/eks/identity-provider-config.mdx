---
title: "aws_eks_identity_provider_config"
description: "`aws_eks_identity_provider_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_eks_identity_provider_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_identity_provider_config"})

    		ubx.Data(eks.IdentityProviderConfig, "example", eks.IdentityProviderConfigConfig{
    			ClusterName:            "example-cluster-name",
    			IdentityProviderConfig: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityProviderConfig } from "@ubx/sdk-aws/aws/data/eks/identity_provider_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_identity_provider_config" });

      data(IdentityProviderConfig, "example", {
        clusterName: "example-cluster-name",
        identityProviderConfig: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import IdentityProviderConfig, IdentityProviderConfigConfig

    def describe():
        ubx.intent("look up aws_eks_identity_provider_config")

        ubx.data(IdentityProviderConfig, "example", IdentityProviderConfigConfig(
            cluster_name="example-cluster-name",
            identity_provider_config={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="identity_provider_config" type="object" required>
  &lt;p&gt;An object representing an identity provider configuration.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

_No computed result fields._
