---
title: "aws_eks_cluster_versions"
description: "`aws_eks_cluster_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_eks_cluster_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_cluster_versions"})

    		ubx.Data(eks.ClusterVersions, "example", eks.ClusterVersionsConfig{
    			ClusterType:   "example",
    			DefaultOnly:   true,
    			IncludeAll:    true,
    			MaxResults:    1,
    			Status:        "example",
    			VersionStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterVersions } from "@ubx/sdk-aws/aws/data/eks/cluster_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_cluster_versions" });

      data(ClusterVersions, "example", {
        clusterType: "example",
        defaultOnly: true,
        includeAll: true,
        maxResults: 1,
        status: "example",
        versionStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import ClusterVersions, ClusterVersionsConfig

    def describe():
        ubx.intent("look up aws_eks_cluster_versions")

        ubx.data(ClusterVersions, "example", ClusterVersionsConfig(
            cluster_type="example",
            default_only=True,
            include_all=True,
            max_results=1,
            status="example",
            version_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="default_only" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_all" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="version_status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_versions" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
