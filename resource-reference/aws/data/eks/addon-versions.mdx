---
title: "aws_eks_addon_versions"
description: "`aws_eks_addon_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_eks_addon_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_addon_versions"})

    		ubx.Data(eks.AddonVersions, "example", eks.AddonVersionsConfig{
    			AddonName:         "example-addon-name",
    			KubernetesVersion: "example",
    			MaxResults:        1,
    			Owners:            []string{"example"},
    			Publishers:        []string{"example"},
    			Types:             []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AddonVersions } from "@ubx/sdk-aws/aws/data/eks/addon_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_addon_versions" });

      data(AddonVersions, "example", {
        addonName: "example-addon-name",
        kubernetesVersion: "example",
        maxResults: 1,
        owners: ["example"],
        publishers: ["example"],
        types: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import AddonVersions, AddonVersionsConfig

    def describe():
        ubx.intent("look up aws_eks_addon_versions")

        ubx.data(AddonVersions, "example", AddonVersionsConfig(
            addon_name="example-addon-name",
            kubernetes_version="example",
            max_results=1,
            owners=["example"],
            publishers=["example"],
            types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="addon_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="kubernetes_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owners" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="publishers" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="types" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="addons" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
