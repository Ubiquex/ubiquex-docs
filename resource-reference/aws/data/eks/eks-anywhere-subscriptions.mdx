---
title: "aws_eks_eks_anywhere_subscriptions"
description: "`aws_eks_eks_anywhere_subscriptions` looks up a real EKS Anywhere subscription, for licensing and support of on-premises EKS clusters."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_eks_anywhere_subscriptions"})

    		ubx.Data(eks.EksAnywhereSubscriptions, "example", eks.EksAnywhereSubscriptionsConfig{
    			IncludeStatus: []string{"example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EksAnywhereSubscriptions } from "@ubx/sdk-aws/aws/data/eks/eks_anywhere_subscriptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_eks_anywhere_subscriptions" });

      data(EksAnywhereSubscriptions, "example", {
        includeStatus: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import EksAnywhereSubscriptions, EksAnywhereSubscriptionsConfig

    def describe():
        ubx.intent("look up aws_eks_eks_anywhere_subscriptions")

        ubx.data(EksAnywhereSubscriptions, "example", EksAnywhereSubscriptionsConfig(
            include_status=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_status" type="list(string)">
  The real, current status of the include this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscriptions" type="list(object)">
  The real subscriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
