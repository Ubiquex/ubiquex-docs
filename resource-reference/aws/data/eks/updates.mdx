---
title: "aws_eks_updates"
description: "`aws_eks_updates` reads the real updates run against an EKS cluster or node group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_updates"})

    		ubx.Data(eks.Updates, "example", eks.UpdatesConfig{
    			Name:           "example",
    			AddonName:      "example-addon-name",
    			CapabilityName: "example-capability-name",
    			MaxResults:     1,
    			NodegroupName:  "example-nodegroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Updates } from "@ubx/sdk-aws/aws/data/eks/updates";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_updates" });

      data(Updates, "example", {
        name: "example",
        addonName: "example-addon-name",
        capabilityName: "example-capability-name",
        maxResults: 1,
        nodegroupName: "example-nodegroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import Updates, UpdatesConfig

    def describe():
        ubx.intent("look up aws_eks_updates")

        ubx.data(Updates, "example", UpdatesConfig(
            name="example",
            addon_name="example-addon-name",
            capability_name="example-capability-name",
            max_results=1,
            nodegroup_name="example-nodegroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="addon_name" type="string">
  The real name of the Amazon EKS add-on this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="capability_name" type="string">
  The real name of the capability this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="nodegroup_name" type="string">
  The real name of the nodegroup this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_ids" type="list(string)">
  The real, unique identifiers of the updates this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
