---
title: "aws_eks_pod_identity_associations"
description: "`aws_eks_pod_identity_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_eks_pod_identity_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_pod_identity_associations"})

    		ubx.Data(eks.PodIdentityAssociations, "example", eks.PodIdentityAssociationsConfig{
    			ClusterName:    "example-cluster-name",
    			MaxResults:     1,
    			Namespace:      "example",
    			ServiceAccount: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PodIdentityAssociations } from "@ubx/sdk-aws/aws/data/eks/pod_identity_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_pod_identity_associations" });

      data(PodIdentityAssociations, "example", {
        clusterName: "example-cluster-name",
        maxResults: 1,
        namespace: "example",
        serviceAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import PodIdentityAssociations, PodIdentityAssociationsConfig

    def describe():
        ubx.intent("look up aws_eks_pod_identity_associations")

        ubx.data(PodIdentityAssociations, "example", PodIdentityAssociationsConfig(
            cluster_name="example-cluster-name",
            max_results=1,
            namespace="example",
            service_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_account" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
