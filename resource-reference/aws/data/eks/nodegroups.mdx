---
title: "aws_eks_nodegroups"
description: "`data_aws_eks_nodegroups` looks up a real, already-existing `aws_eks_nodegroup`."
---

It provisions and manages a real group of EC2 worker nodes, patched and scaled by EKS itself, that join an `aws_eks_cluster` to actually run pods, an alternative to Fargate for workloads that need EC2-level control.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_nodegroups"})

    		ubx.Data(eks.Nodegroups, "example", eks.NodegroupsConfig{
    			ClusterName: "example-cluster-name",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Nodegroups } from "@ubx/sdk-aws/aws/data/eks/nodegroups";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_nodegroups" });

      data(Nodegroups, "example", {
        clusterName: "example-cluster-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eks import Nodegroups, NodegroupsConfig

    def describe():
        ubx.intent("look up aws_eks_nodegroups")

        ubx.data(Nodegroups, "example", NodegroupsConfig(
            cluster_name="example-cluster-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="nodegroups" type="list(string)">
  The real Amazon EKS node groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
