---
title: "aws_eks_identity_provider_configs"
description: "`data_aws_eks_identity_provider_configs` looks up a real, already-existing `aws_eks_identity_provider_config`."
---

It federates an `aws_eks_cluster`'s Kubernetes API with an external OIDC identity provider, letting a caller authenticate to the cluster with their own organization's existing identity system instead of only IAM.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eks_identity_provider_configs"})

    		ubx.Data(eks.IdentityProviderConfigs, "example", eks.IdentityProviderConfigsConfig{
    			ClusterName: "example-cluster-name",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IdentityProviderConfigs } from "./local-sdk/aws/sdk/typescript/aws/data/eks/identity_provider_configs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_eks_identity_provider_configs" });

      data(IdentityProviderConfigs, "example", {
        clusterName: "example-cluster-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.eks import IdentityProviderConfigs, IdentityProviderConfigsConfig

    def describe():
        ubx.intent("look up aws_eks_identity_provider_configs")

        ubx.data(IdentityProviderConfigs, "example", IdentityProviderConfigsConfig(
            cluster_name="example-cluster-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="identity_provider_configs" type="list(object)">
  The real identity provider configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
