---
title: "aws_b2bi_transformer_job"
description: "`aws_b2bi_transformer_job` looks up one real B2B Data Interchange transformer job, converting an EDI document to or from a structured format."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	b2bi "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/b2bi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_b2bi_transformer_job"})

    		ubx.Data(b2bi.TransformerJob, "example", b2bi.TransformerJobConfig{
    			TransformerId:    "example",
    			TransformerJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TransformerJob } from "@ubx/sdk-aws/aws/data/b2bi/transformer_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_b2bi_transformer_job" });

      data(TransformerJob, "example", {
        transformerId: "example",
        transformerJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.b2bi import TransformerJob, TransformerJobConfig

    def describe():
        ubx.intent("look up aws_b2bi_transformer_job")

        ubx.data(TransformerJob, "example", TransformerJobConfig(
            transformer_id="example",
            transformer_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="transformer_id" type="string" required>
  The real, unique identifier of the transformer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="transformer_job_id" type="string" required>
  The real, unique identifier of the transformer job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="output_files" type="list(object)">
  The real files for the output. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
