---
title: "aws_medialive_reservations"
description: "`aws_medialive_reservations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_medialive_reservations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_reservations"})

    		ubx.Data(medialive.Reservations, "example", medialive.ReservationsConfig{
    			ChannelClass:     "example",
    			Codec:            "example",
    			MaxResults:       1,
    			MaximumBitrate:   "example",
    			MaximumFramerate: "example",
    			Resolution:       "example",
    			ResourceType:     "example",
    			SpecialFeature:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Reservations } from "@ubx/sdk-aws/aws/data/medialive/reservations";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_reservations" });

      data(Reservations, "example", {
        channelClass: "example",
        codec: "example",
        maxResults: 1,
        maximumBitrate: "example",
        maximumFramerate: "example",
        resolution: "example",
        resourceType: "example",
        specialFeature: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import Reservations, ReservationsConfig

    def describe():
        ubx.intent("look up aws_medialive_reservations")

        ubx.data(Reservations, "example", ReservationsConfig(
            channel_class="example",
            codec="example",
            max_results=1,
            maximum_bitrate="example",
            maximum_framerate="example",
            resolution="example",
            resource_type="example",
            special_feature="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_class" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="codec" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

<ResponseField name="maximum_bitrate" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="maximum_framerate" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="resolution" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="special_feature" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

<ResponseField name="video_quality" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>

<ResponseField name="reservations" type="list(object)">
  Placeholder documentation for __listOfReservation Returned by the lookup.
</ResponseField>
