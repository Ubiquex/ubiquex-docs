---
title: "aws_medialive_multiplex_alerts"
description: "`aws_medialive_multiplex_alerts` reads a real, active alert reported by a MediaLive multiplex."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_multiplex_alerts"})

    		ubx.Data(medialive.MultiplexAlerts, "example", medialive.MultiplexAlertsConfig{
    			MultiplexId: "example",
    			MaxResults:  1,
    			StateFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MultiplexAlerts } from "./local-sdk/aws/sdk/typescript/aws/data/medialive/multiplex_alerts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_multiplex_alerts" });

      data(MultiplexAlerts, "example", {
        multiplexId: "example",
        maxResults: 1,
        stateFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import MultiplexAlerts, MultiplexAlertsConfig

    def describe():
        ubx.intent("look up aws_medialive_multiplex_alerts")

        ubx.data(MultiplexAlerts, "example", MultiplexAlertsConfig(
            multiplex_id="example",
            max_results=1,
            state_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

<ResponseField name="multiplex_id" type="string" required>
  Placeholder documentation for __string Required lookup argument.
</ResponseField>

<ResponseField name="state_filter" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alerts" type="list(object)">
  Placeholder documentation for __listOfMultiplexAlert Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
