---
title: "aws_medialive_cluster_alerts"
description: "`aws_medialive_cluster_alerts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_medialive_cluster_alerts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_cluster_alerts"})

    		ubx.Data(medialive.ClusterAlerts, "example", medialive.ClusterAlertsConfig{
    			ClusterId:   "example",
    			MaxResults:  1,
    			StateFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterAlerts } from "@ubx/sdk-aws/aws/data/medialive/cluster_alerts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_cluster_alerts" });

      data(ClusterAlerts, "example", {
        clusterId: "example",
        maxResults: 1,
        stateFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import ClusterAlerts, ClusterAlertsConfig

    def describe():
        ubx.intent("look up aws_medialive_cluster_alerts")

        ubx.data(ClusterAlerts, "example", ClusterAlertsConfig(
            cluster_id="example",
            max_results=1,
            state_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  Placeholder documentation for __string Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

<ResponseField name="state_filter" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alerts" type="list(object)">
  Placeholder documentation for __listOfClusterAlert Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
