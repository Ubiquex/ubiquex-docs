---
title: "aws_medialive_clusters"
description: "`aws_medialive_clusters` looks up a real MediaLive Anywhere cluster, running MediaLive channels on customer-managed infrastructure."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_clusters"})

    		ubx.Data(medialive.Clusters, "example", medialive.ClustersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Clusters } from "@ubx/sdk-aws/aws/data/medialive/clusters";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_clusters" });

      data(Clusters, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import Clusters, ClustersConfig

    def describe():
        ubx.intent("look up aws_medialive_clusters")

        ubx.data(Clusters, "example", ClustersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clusters" type="list(object)">
  Placeholder documentation for __listOfDescribeClusterSummary Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
