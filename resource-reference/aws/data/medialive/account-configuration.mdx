---
title: "aws_medialive_account_configuration"
description: "`aws_medialive_account_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_medialive_account_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_account_configuration"})

    		ubx.Data(medialive.AccountConfiguration, "example", medialive.AccountConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountConfiguration } from "@ubx/sdk-aws/aws/data/medialive/account_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_account_configuration" });

      data(AccountConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import AccountConfiguration, AccountConfigurationConfig

    def describe():
        ubx.intent("look up aws_medialive_account_configuration")

        ubx.data(AccountConfiguration, "example", AccountConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_configuration" type="object">
  Placeholder documentation for AccountConfiguration Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string">
      Placeholder documentation for __string Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
