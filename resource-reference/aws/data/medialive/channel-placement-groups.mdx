---
title: "aws_medialive_channel_placement_groups"
description: "`aws_medialive_channel_placement_groups` looks up a real MediaLive channel placement group, controlling which cluster nodes a channel can run on."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_channel_placement_groups"})

    		ubx.Data(medialive.ChannelPlacementGroups, "example", medialive.ChannelPlacementGroupsConfig{
    			ClusterId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChannelPlacementGroups } from "@ubx/sdk-aws/aws/data/medialive/channel_placement_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_channel_placement_groups" });

      data(ChannelPlacementGroups, "example", {
        clusterId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import ChannelPlacementGroups, ChannelPlacementGroupsConfig

    def describe():
        ubx.intent("look up aws_medialive_channel_placement_groups")

        ubx.data(ChannelPlacementGroups, "example", ChannelPlacementGroupsConfig(
            cluster_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  Placeholder documentation for __string Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_placement_groups" type="list(object)">
  Placeholder documentation for __listOfDescribeChannelPlacementGroupSummary Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
