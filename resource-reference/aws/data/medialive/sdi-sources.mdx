---
title: "aws_medialive_sdi_sources"
description: "`aws_medialive_sdi_sources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_medialive_sdi_sources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_sdi_sources"})

    		ubx.Data(medialive.SdiSources, "example", medialive.SdiSourcesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SdiSources } from "@ubx/sdk-aws/aws/data/medialive/sdi_sources";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_sdi_sources" });

      data(SdiSources, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import SdiSources, SdiSourcesConfig

    def describe():
        ubx.intent("look up aws_medialive_sdi_sources")

        ubx.data(SdiSources, "example", SdiSourcesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>

<ResponseField name="sdi_sources" type="list(object)">
  Placeholder documentation for __listOfSdiSourceSummary Returned by the lookup.
</ResponseField>
