---
title: "aws_medialive_versions"
description: "`aws_medialive_versions` reads the real, available MediaLive channel software versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_versions"})

    		ubx.Data(medialive.Versions, "example", medialive.VersionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Versions } from "./local-sdk/aws/sdk/typescript/aws/data/medialive/versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_versions" });

      data(Versions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import Versions, VersionsConfig

    def describe():
        ubx.intent("look up aws_medialive_versions")

        ubx.data(Versions, "example", VersionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="versions" type="list(object)">
  Placeholder documentation for __listOfChannelEngineVersionResponse Returned by the lookup.
</ResponseField>
