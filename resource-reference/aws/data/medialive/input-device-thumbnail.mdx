---
title: "aws_medialive_input_device_thumbnail"
description: "`aws_medialive_input_device_thumbnail` reads a real, captured thumbnail image from a MediaLive input device's own current feed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_input_device_thumbnail"})

    		ubx.Data(medialive.InputDeviceThumbnail, "example", medialive.InputDeviceThumbnailConfig{
    			Accept:        "example",
    			InputDeviceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InputDeviceThumbnail } from "./local-sdk/aws/sdk/typescript/aws/data/medialive/input_device_thumbnail.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_input_device_thumbnail" });

      data(InputDeviceThumbnail, "example", {
        accept: "example",
        inputDeviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import InputDeviceThumbnail, InputDeviceThumbnailConfig

    def describe():
        ubx.intent("look up aws_medialive_input_device_thumbnail")

        ubx.data(InputDeviceThumbnail, "example", InputDeviceThumbnailConfig(
            accept="example",
            input_device_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept" type="string" required>
  The HTTP Accept header. Indicates the requested type fothe thumbnail. Required lookup argument.
</ResponseField>

<ResponseField name="input_device_id" type="string" required>
  Placeholder documentation for __string Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  The binary data for the thumbnail that the Link device has most recently sent to MediaLive. Returned by the lookup.
</ResponseField>

<ResponseField name="content_length" type="number">
  Placeholder documentation for __long Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  Specifies the media type of the thumbnail. Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified" type="string">
  Placeholder documentation for __timestamp Returned by the lookup.
</ResponseField>
