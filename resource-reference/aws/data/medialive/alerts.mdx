---
title: "aws_medialive_alerts"
description: "`aws_medialive_alerts` reads a real, active alert reported by a MediaLive channel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_alerts"})

    		ubx.Data(medialive.Alerts, "example", medialive.AlertsConfig{
    			ChannelId:   "example",
    			MaxResults:  1,
    			StateFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Alerts } from "@ubx/sdk-aws/aws/data/medialive/alerts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_alerts" });

      data(Alerts, "example", {
        channelId: "example",
        maxResults: 1,
        stateFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import Alerts, AlertsConfig

    def describe():
        ubx.intent("look up aws_medialive_alerts")

        ubx.data(Alerts, "example", AlertsConfig(
            channel_id="example",
            max_results=1,
            state_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_id" type="string" required>
  Placeholder documentation for __string Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

<ResponseField name="state_filter" type="string">
  Placeholder documentation for __string Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alerts" type="list(object)">
  Placeholder documentation for __listOfChannelAlert Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
