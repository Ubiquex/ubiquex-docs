---
title: "aws_medialive_input_devices"
description: "`aws_medialive_input_devices` reads the real MediaLive input devices registered on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	medialive "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/medialive"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_medialive_input_devices"})

    		ubx.Data(medialive.InputDevices, "example", medialive.InputDevicesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InputDevices } from "./local-sdk/aws/sdk/typescript/aws/data/medialive/input_devices.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_medialive_input_devices" });

      data(InputDevices, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.medialive import InputDevices, InputDevicesConfig

    def describe():
        ubx.intent("look up aws_medialive_input_devices")

        ubx.data(InputDevices, "example", InputDevicesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Placeholder documentation for MaxResults Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="input_devices" type="list(object)">
  Placeholder documentation for __listOfInputDeviceSummary Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Placeholder documentation for __string Returned by the lookup.
</ResponseField>
