---
title: "aws_access_analyzer_findings"
description: "`aws_access_analyzer_findings` reads the real findings from an IAM Access Analyzer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_findings"})

    		ubx.Data(access_analyzer.AccessAnalyzerFindings, "example", access_analyzer.AccessAnalyzerFindingsConfig{
    			AnalyzerArn: "arn:aws:iam::123456789012:policy/example",
    			Filter:      map[string]string{"managed-by": "ubx"},
    			MaxResults:  1,
    			Sort:        map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerFindings } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_findings" });

      data(AccessAnalyzerFindings, "example", {
        analyzerArn: "arn:aws:iam::123456789012:policy/example",
        filter: { "managed-by": "ubx" },
        maxResults: 1,
        sort: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerFindings, AccessAnalyzerFindingsConfig

    def describe():
        ubx.intent("look up aws_access_analyzer_findings")

        ubx.data(AccessAnalyzerFindings, "example", AccessAnalyzerFindingsConfig(
            analyzer_arn="arn:aws:iam::123456789012:policy/example",
            filter={"managed-by": "ubx"},
            max_results=1,
            sort={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analyzer_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM Access Analyzer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="map(object)">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="object">
  &lt;p&gt;The criteria used to sort.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
