---
title: "aws_access_analyzer_findings_v2"
description: "`aws_access_analyzer_findings_v2` reads the real findings from an IAM Access Analyzer under the newer, unified finding schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_findings_v2"})

    		ubx.Data(access_analyzer.AccessAnalyzerFindingsV2, "example", access_analyzer.AccessAnalyzerFindingsV2Config{
    			AnalyzerArn: "arn:aws:iam::123456789012:policy/example",
    			Filter:      map[string]string{"managed-by": "ubx"},
    			MaxResults:  1,
    			Sort:        map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerFindingsV2 } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_findings_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_findings_v2" });

      data(AccessAnalyzerFindingsV2, "example", {
        analyzerArn: "arn:aws:iam::123456789012:policy/example",
        filter: { "managed-by": "ubx" },
        maxResults: 1,
        sort: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerFindingsV2, AccessAnalyzerFindingsV2Config
    from ubx.aws.data.access_analyzer.access_analyzer_findings_v2 import AccessAnalyzerFindingsV2_Sort

    def describe():
        ubx.intent("look up aws_access_analyzer_findings_v2")

        ubx.data(AccessAnalyzerFindingsV2, "example", AccessAnalyzerFindingsV2Config(
            analyzer_arn="arn:aws:iam::123456789012:policy/example",
            filter={"managed-by": "ubx"},
            max_results=1,
            sort=AccessAnalyzerFindingsV2_Sort(attribute_name="example-attribute-name"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analyzer_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM Access Analyzer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="map(object)">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="object">
  &lt;p&gt;The criteria used to sort.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
