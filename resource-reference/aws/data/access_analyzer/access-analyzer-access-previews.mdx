---
title: "aws_access_analyzer_access_previews"
description: "`aws_access_analyzer_access_previews` looks up one real IAM Access Analyzer access preview."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_access_previews"})

    		ubx.Data(access_analyzer.AccessAnalyzerAccessPreviews, "example", access_analyzer.AccessAnalyzerAccessPreviewsConfig{
    			AnalyzerArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerAccessPreviews } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_access_previews";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_access_previews" });

      data(AccessAnalyzerAccessPreviews, "example", {
        analyzerArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerAccessPreviews, AccessAnalyzerAccessPreviewsConfig

    def describe():
        ubx.intent("look up aws_access_analyzer_access_previews")

        ubx.data(AccessAnalyzerAccessPreviews, "example", AccessAnalyzerAccessPreviewsConfig(
            analyzer_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analyzer_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM Access Analyzer this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_previews" type="list(object)">
  The real IAM Access Analyzer access previews included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
