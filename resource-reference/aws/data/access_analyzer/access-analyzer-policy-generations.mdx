---
title: "aws_access_analyzer_policy_generations"
description: "`aws_access_analyzer_policy_generations` reads the real, current status of an IAM Access Analyzer policy generation job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_policy_generations"})

    		ubx.Data(access_analyzer.AccessAnalyzerPolicyGenerations, "example", access_analyzer.AccessAnalyzerPolicyGenerationsConfig{
    			MaxResults:   1,
    			PrincipalArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerPolicyGenerations } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_policy_generations";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_policy_generations" });

      data(AccessAnalyzerPolicyGenerations, "example", {
        maxResults: 1,
        principalArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerPolicyGenerations, AccessAnalyzerPolicyGenerationsConfig

    def describe():
        ubx.intent("look up aws_access_analyzer_policy_generations")

        ubx.data(AccessAnalyzerPolicyGenerations, "example", AccessAnalyzerPolicyGenerationsConfig(
            max_results=1,
            principal_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_generations" type="list(object)">
  The real IAM Access Analyzer policy generations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
