---
title: "aws_access_analyzer_analyzed_resources"
description: "`aws_access_analyzer_analyzed_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_access_analyzer_analyzed_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_analyzed_resources"})

    		ubx.Data(access_analyzer.AccessAnalyzerAnalyzedResources, "example", access_analyzer.AccessAnalyzerAnalyzedResourcesConfig{
    			AnalyzerArn:  "arn:aws:iam::123456789012:policy/example",
    			MaxResults:   1,
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerAnalyzedResources } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_analyzed_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_analyzed_resources" });

      data(AccessAnalyzerAnalyzedResources, "example", {
        analyzerArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerAnalyzedResources, AccessAnalyzerAnalyzedResourcesConfig

    def describe():
        ubx.intent("look up aws_access_analyzer_analyzed_resources")

        ubx.data(AccessAnalyzerAnalyzedResources, "example", AccessAnalyzerAnalyzedResourcesConfig(
            analyzer_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analyzer_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analyzed_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
