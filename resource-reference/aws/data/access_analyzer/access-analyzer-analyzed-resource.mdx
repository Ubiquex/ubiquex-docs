---
title: "aws_access_analyzer_analyzed_resource"
description: "`aws_access_analyzer_analyzed_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_access_analyzer_analyzed_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	access_analyzer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/access_analyzer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_access_analyzer_analyzed_resource"})

    		ubx.Data(access_analyzer.AccessAnalyzerAnalyzedResource, "example", access_analyzer.AccessAnalyzerAnalyzedResourceConfig{
    			AnalyzerArn: "arn:aws:iam::123456789012:policy/example",
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessAnalyzerAnalyzedResource } from "@ubx/sdk-aws/aws/data/access_analyzer/access_analyzer_analyzed_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_access_analyzer_analyzed_resource" });

      data(AccessAnalyzerAnalyzedResource, "example", {
        analyzerArn: "arn:aws:iam::123456789012:policy/example",
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.access_analyzer import AccessAnalyzerAnalyzedResource, AccessAnalyzerAnalyzedResourceConfig

    def describe():
        ubx.intent("look up aws_access_analyzer_analyzed_resource")

        ubx.data(AccessAnalyzerAnalyzedResource, "example", AccessAnalyzerAnalyzedResourceConfig(
            analyzer_arn="arn:aws:iam::123456789012:policy/example",
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analyzer_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource" type="object">
  &lt;p&gt;Contains details about the analyzed resource.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="analyzed_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="error" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_public" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_owner_account" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="shared_via" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
