---
title: "aws_api_ecr_public_image_tags"
description: "`aws_api_ecr_public_image_tags` reads the real tags applied to an image in a public ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr_public "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_ecr_public"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_public_image_tags"})

    		ubx.Data(api_ecr_public.ApiEcrPublicImageTags, "example", api_ecr_public.ApiEcrPublicImageTagsConfig{
    			RepositoryName: "example-repository-name",
    			MaxResults:     1,
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrPublicImageTags } from "@ubx/sdk-aws/aws/data/api_ecr_public/api_ecr_public_image_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_public_image_tags" });

      data(ApiEcrPublicImageTags, "example", {
        repositoryName: "example-repository-name",
        maxResults: 1,
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr_public import ApiEcrPublicImageTags, ApiEcrPublicImageTagsConfig

    def describe():
        ubx.intent("look up aws_api_ecr_public_image_tags")

        ubx.data(ApiEcrPublicImageTags, "example", ApiEcrPublicImageTagsConfig(
            repository_name="example-repository-name",
            max_results=1,
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_tag_details" type="list(object)">
  The real, detailed information about the image tag. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
