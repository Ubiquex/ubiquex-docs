---
title: "aws_api_ecr_public_registries"
description: "`aws_api_ecr_public_registries` reads the real public ECR registries on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_ecr_public "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr_public"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_public_registries"})

    		ubx.Data(api_ecr_public.ApiEcrPublicRegistries, "example", api_ecr_public.ApiEcrPublicRegistriesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiEcrPublicRegistries } from "./local-sdk/aws/sdk/typescript/aws/data/api_ecr_public/api_ecr_public_registries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_public_registries" });

      data(ApiEcrPublicRegistries, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr_public import ApiEcrPublicRegistries, ApiEcrPublicRegistriesConfig

    def describe():
        ubx.intent("look up aws_api_ecr_public_registries")

        ubx.data(ApiEcrPublicRegistries, "example", ApiEcrPublicRegistriesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registries" type="list(object)">
  The real registries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
