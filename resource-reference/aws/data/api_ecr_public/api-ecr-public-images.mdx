---
title: "aws_api_ecr_public_images"
description: "`aws_api_ecr_public_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_public_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr_public "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr_public"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_public_images"})

    		ubx.Data(api_ecr_public.ApiEcrPublicImages, "example", api_ecr_public.ApiEcrPublicImagesConfig{
    			RepositoryName: "example-repository-name",
    			ImageIds:       []map[string]any{{"image_digest": "example"}},
    			MaxResults:     1,
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrPublicImages } from "@ubx/sdk-aws/aws/data/api_ecr_public/api_ecr_public_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_public_images" });

      data(ApiEcrPublicImages, "example", {
        repositoryName: "example-repository-name",
        imageIds: [{ imageDigest: "example" }],
        maxResults: 1,
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr_public import ApiEcrPublicImages, ApiEcrPublicImagesConfig

    def describe():
        ubx.intent("look up aws_api_ecr_public_images")

        ubx.data(ApiEcrPublicImages, "example", ApiEcrPublicImagesConfig(
            repository_name="example-repository-name",
            image_ids=[{"image_digest": "example"}],
            max_results=1,
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_ids" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_details" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
