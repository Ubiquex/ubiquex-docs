---
title: "aws_api_ecr_public_repositories"
description: "`aws_api_ecr_public_repositories` looks up a real public ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr_public "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_ecr_public"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_public_repositories"})

    		ubx.Data(api_ecr_public.ApiEcrPublicRepositories, "example", api_ecr_public.ApiEcrPublicRepositoriesConfig{
    			MaxResults:      1,
    			RegistryId:      "example",
    			RepositoryNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrPublicRepositories } from "@ubx/sdk-aws/aws/data/api_ecr_public/api_ecr_public_repositories";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_public_repositories" });

      data(ApiEcrPublicRepositories, "example", {
        maxResults: 1,
        registryId: "example",
        repositoryNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr_public import ApiEcrPublicRepositories, ApiEcrPublicRepositoriesConfig

    def describe():
        ubx.intent("look up aws_api_ecr_public_repositories")

        ubx.data(ApiEcrPublicRepositories, "example", ApiEcrPublicRepositoriesConfig(
            max_results=1,
            registry_id="example",
            repository_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_names" type="list(string)">
  The real names of the repositories included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repositories" type="list(object)">
  The real repositories included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
