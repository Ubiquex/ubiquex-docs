---
title: "aws_redshift_table_restore_status"
description: "`aws_redshift_table_restore_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_table_restore_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_table_restore_status"})

    		ubx.Data(redshift.TableRestoreStatus, "example", redshift.TableRestoreStatusConfig{
    			ClusterIdentifier:     "example",
    			MaxRecords:            1,
    			TableRestoreRequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TableRestoreStatus } from "@ubx/sdk-aws/aws/data/redshift/table_restore_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_table_restore_status" });

      data(TableRestoreStatus, "example", {
        clusterIdentifier: "example",
        maxRecords: 1,
        tableRestoreRequestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import TableRestoreStatus, TableRestoreStatusConfig

    def describe():
        ubx.intent("look up aws_redshift_table_restore_status")

        ubx.data(TableRestoreStatus, "example", TableRestoreStatusConfig(
            cluster_identifier="example",
            max_records=1,
            table_restore_request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="table_restore_request_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="table_restore_status_details" type="list(object)">
  Returned by the lookup.
</ResponseField>
