---
title: "aws_redshift_cluster_credentials_with_iam"
description: "`aws_redshift_cluster_credentials_with_iam` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_cluster_credentials_with_iam` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_cluster_credentials_with_iam"})

    		ubx.Data(redshift.ClusterCredentialsWithIam, "example", redshift.ClusterCredentialsWithIamConfig{
    			ClusterIdentifier: "example",
    			CustomDomainName:  "example-custom-domain-name",
    			DbName:            "example-db-name",
    			DurationSeconds:   7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterCredentialsWithIam } from "@ubx/sdk-aws/aws/data/redshift/cluster_credentials_with_iam";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_cluster_credentials_with_iam" });

      data(ClusterCredentialsWithIam, "example", {
        clusterIdentifier: "example",
        customDomainName: "example-custom-domain-name",
        dbName: "example-db-name",
        durationSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ClusterCredentialsWithIam, ClusterCredentialsWithIamConfig

    def describe():
        ubx.intent("look up aws_redshift_cluster_credentials_with_iam")

        ubx.data(ClusterCredentialsWithIam, "example", ClusterCredentialsWithIamConfig(
            cluster_identifier="example",
            custom_domain_name="example-custom-domain-name",
            db_name="example-db-name",
            duration_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="custom_domain_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="db_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="db_password" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="db_user" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_refresh_time" type="string">
  Returned by the lookup.
</ResponseField>
