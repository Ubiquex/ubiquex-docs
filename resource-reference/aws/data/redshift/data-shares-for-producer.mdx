---
title: "aws_redshift_data_shares_for_producer"
description: "`aws_redshift_data_shares_for_producer` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_data_shares_for_producer` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_shares_for_producer"})

    		ubx.Data(redshift.DataSharesForProducer, "example", redshift.DataSharesForProducerConfig{
    			MaxRecords:  1,
    			ProducerArn: "arn:aws:iam::123456789012:policy/example",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSharesForProducer } from "@ubx/sdk-aws/aws/data/redshift/data_shares_for_producer";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_shares_for_producer" });

      data(DataSharesForProducer, "example", {
        maxRecords: 1,
        producerArn: "arn:aws:iam::123456789012:policy/example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import DataSharesForProducer, DataSharesForProducerConfig

    def describe():
        ubx.intent("look up aws_redshift_data_shares_for_producer")

        ubx.data(DataSharesForProducer, "example", DataSharesForProducerConfig(
            max_records=1,
            producer_arn="arn:aws:iam::123456789012:policy/example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="producer_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_shares" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
