---
title: "aws_redshift_identity_center_auth_token"
description: "`aws_redshift_identity_center_auth_token` reads a real, temporary authentication token for a Redshift cluster issued through IAM Identity Center..."
---

`aws_redshift_identity_center_auth_token` reads a real, temporary authentication token for a Redshift cluster issued through IAM Identity Center trusted identity propagation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_identity_center_auth_token"})

    		ubx.Data(redshift.IdentityCenterAuthToken, "example", redshift.IdentityCenterAuthTokenConfig{
    			ClusterIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IdentityCenterAuthToken } from "./local-sdk/aws/sdk/typescript/aws/data/redshift/identity_center_auth_token.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_identity_center_auth_token" });

      data(IdentityCenterAuthToken, "example", {
        clusterIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import IdentityCenterAuthToken, IdentityCenterAuthTokenConfig

    def describe():
        ubx.intent("look up aws_redshift_identity_center_auth_token")

        ubx.data(IdentityCenterAuthToken, "example", IdentityCenterAuthTokenConfig(
            cluster_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_ids" type="list(string)" required>
  The real, unique identifiers of the clusters this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expiration_time" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
