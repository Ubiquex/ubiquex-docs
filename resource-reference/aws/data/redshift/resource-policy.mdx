---
title: "aws_redshift_resource_policy"
description: "`aws_redshift_resource_policy` reads the real resource-based policy attached to a Redshift resource, such as a datashare."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_resource_policy"})

    		ubx.Data(redshift.ResourcePolicy, "example", redshift.ResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourcePolicy } from "./local-sdk/aws/sdk/typescript/aws/data/redshift/resource_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_resource_policy" });

      data(ResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_redshift_resource_policy")

        ubx.data(ResourcePolicy, "example", ResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_policy" type="object">
  &lt;p&gt;The policy that is attached to a resource.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="policy" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
