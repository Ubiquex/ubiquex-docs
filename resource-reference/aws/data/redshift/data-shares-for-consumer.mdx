---
title: "aws_redshift_data_shares_for_consumer"
description: "`aws_redshift_data_shares_for_consumer` reads the real Redshift datashares a given consumer namespace has access to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_shares_for_consumer"})

    		ubx.Data(redshift.DataSharesForConsumer, "example", redshift.DataSharesForConsumerConfig{
    			ConsumerArn: "arn:aws:iam::123456789012:policy/example",
    			MaxRecords:  1,
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSharesForConsumer } from "@ubx/sdk-aws/aws/data/redshift/data_shares_for_consumer";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_shares_for_consumer" });

      data(DataSharesForConsumer, "example", {
        consumerArn: "arn:aws:iam::123456789012:policy/example",
        maxRecords: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import DataSharesForConsumer, DataSharesForConsumerConfig

    def describe():
        ubx.intent("look up aws_redshift_data_shares_for_consumer")

        ubx.data(DataSharesForConsumer, "example", DataSharesForConsumerConfig(
            consumer_arn="arn:aws:iam::123456789012:policy/example",
            max_records=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consumer_arn" type="string">
  The real Amazon Resource Name (ARN) of the consumer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_shares" type="list(object)">
  The real Redshift data shares included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
