---
title: "aws_redshift_node_configuration_options"
description: "`aws_redshift_node_configuration_options` reads the real, available node type and count combinations for resizing a Redshift cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_node_configuration_options"})

    		ubx.Data(redshift.NodeConfigurationOptions, "example", redshift.NodeConfigurationOptionsConfig{
    			ActionType:         "example",
    			ClusterIdentifier:  "example",
    			Filters:            []map[string]any{{"name": "example-name"}},
    			MaxRecords:         1,
    			OwnerAccount:       "example",
    			SnapshotArn:        "arn:aws:iam::123456789012:policy/example",
    			SnapshotIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NodeConfigurationOptions } from "@ubx/sdk-aws/aws/data/redshift/node_configuration_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_node_configuration_options" });

      data(NodeConfigurationOptions, "example", {
        actionType: "example",
        clusterIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        ownerAccount: "example",
        snapshotArn: "arn:aws:iam::123456789012:policy/example",
        snapshotIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import NodeConfigurationOptions, NodeConfigurationOptionsConfig

    def describe():
        ubx.intent("look up aws_redshift_node_configuration_options")

        ubx.data(NodeConfigurationOptions, "example", NodeConfigurationOptionsConfig(
            action_type="example",
            cluster_identifier="example",
            filters=[{"name": "example-name"}],
            max_records=1,
            owner_account="example",
            snapshot_arn="arn:aws:iam::123456789012:policy/example",
            snapshot_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string" required>
  The real type of this action. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="cluster_identifier" type="string">
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner_account" type="string">
  The real AWS account that owns this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_arn" type="string">
  The real Amazon Resource Name (ARN) of the snapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_identifier" type="string">
  The real, unique identifier of the snapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="node_configuration_option_list" type="list(object)">
  The real node configuration options included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
