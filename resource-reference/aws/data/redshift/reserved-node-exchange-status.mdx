---
title: "aws_redshift_reserved_node_exchange_status"
description: "`aws_redshift_reserved_node_exchange_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_reserved_node_exchange_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_reserved_node_exchange_status"})

    		ubx.Data(redshift.ReservedNodeExchangeStatus, "example", redshift.ReservedNodeExchangeStatusConfig{
    			MaxRecords:                    1,
    			ReservedNodeExchangeRequestId: "example",
    			ReservedNodeId:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedNodeExchangeStatus } from "@ubx/sdk-aws/aws/data/redshift/reserved_node_exchange_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_reserved_node_exchange_status" });

      data(ReservedNodeExchangeStatus, "example", {
        maxRecords: 1,
        reservedNodeExchangeRequestId: "example",
        reservedNodeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ReservedNodeExchangeStatus, ReservedNodeExchangeStatusConfig

    def describe():
        ubx.intent("look up aws_redshift_reserved_node_exchange_status")

        ubx.data(ReservedNodeExchangeStatus, "example", ReservedNodeExchangeStatusConfig(
            max_records=1,
            reserved_node_exchange_request_id="example",
            reserved_node_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_node_exchange_request_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_node_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_node_exchange_status_details" type="list(object)">
  Returned by the lookup.
</ResponseField>
