---
title: "aws_redshift_account_attributes"
description: "`aws_redshift_account_attributes` reads real, account-level Redshift attributes and quotas."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_account_attributes"})

    		ubx.Data(redshift.AccountAttributes, "example", redshift.AccountAttributesConfig{
    			AttributeNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAttributes } from "@ubx/sdk-aws/aws/data/redshift/account_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_account_attributes" });

      data(AccountAttributes, "example", {
        attributeNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_redshift_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
            attribute_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_names" type="list(string)">
  The real names of the attributes included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_attributes" type="list(object)">
  The real account-level attributes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
