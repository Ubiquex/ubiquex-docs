---
title: "aws_redshift_storage"
description: "`aws_redshift_storage` reads real, aggregated storage usage across a Redshift cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_storage"})

    		ubx.Data(redshift.Storage, "example", redshift.StorageConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Storage } from "@ubx/sdk-aws/aws/data/redshift/storage";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_storage" });

      data(Storage, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import Storage, StorageConfig

    def describe():
        ubx.intent("look up aws_redshift_storage")

        ubx.data(Storage, "example", StorageConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="total_backup_size_in_mega_bytes" type="number">
  The real size of the total backup size in mega, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_provisioned_storage_in_mega_bytes" type="number">
  The real size of the total provisioned storage in mega, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
