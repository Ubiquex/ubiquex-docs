---
title: "aws_redshift_reserved_nodes"
description: "`aws_redshift_reserved_nodes` looks up a real, purchased Redshift Reserved Node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_reserved_nodes"})

    		ubx.Data(redshift.ReservedNodes, "example", redshift.ReservedNodesConfig{
    			MaxRecords:     1,
    			ReservedNodeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedNodes } from "@ubx/sdk-aws/aws/data/redshift/reserved_nodes";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_reserved_nodes" });

      data(ReservedNodes, "example", {
        maxRecords: 1,
        reservedNodeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ReservedNodes, ReservedNodesConfig

    def describe():
        ubx.intent("look up aws_redshift_reserved_nodes")

        ubx.data(ReservedNodes, "example", ReservedNodesConfig(
            max_records=1,
            reserved_node_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_node_id" type="string">
  The real, unique identifier of the reserved node this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_nodes" type="list(object)">
  The real Reserved Nodes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
