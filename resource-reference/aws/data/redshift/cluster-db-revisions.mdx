---
title: "aws_redshift_cluster_db_revisions"
description: "`aws_redshift_cluster_db_revisions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_cluster_db_revisions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_cluster_db_revisions"})

    		ubx.Data(redshift.ClusterDbRevisions, "example", redshift.ClusterDbRevisionsConfig{
    			ClusterIdentifier: "example",
    			MaxRecords:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterDbRevisions } from "@ubx/sdk-aws/aws/data/redshift/cluster_db_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_cluster_db_revisions" });

      data(ClusterDbRevisions, "example", {
        clusterIdentifier: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ClusterDbRevisions, ClusterDbRevisionsConfig

    def describe():
        ubx.intent("look up aws_redshift_cluster_db_revisions")

        ubx.data(ClusterDbRevisions, "example", ClusterDbRevisionsConfig(
            cluster_identifier="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_db_revisions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
