---
title: "aws_redshift_reserved_node_exchange_configuration_options"
description: "`aws_redshift_reserved_node_exchange_configuration_options` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_reserved_node_exchange_configuration_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_reserved_node_exchange_configuration_options"})

    		ubx.Data(redshift.ReservedNodeExchangeConfigurationOptions, "example", redshift.ReservedNodeExchangeConfigurationOptionsConfig{
    			ActionType:         "example",
    			ClusterIdentifier:  "example",
    			MaxRecords:         1,
    			SnapshotIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedNodeExchangeConfigurationOptions } from "@ubx/sdk-aws/aws/data/redshift/reserved_node_exchange_configuration_options";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_redshift_reserved_node_exchange_configuration_options",
      });

      data(ReservedNodeExchangeConfigurationOptions, "example", {
        actionType: "example",
        clusterIdentifier: "example",
        maxRecords: 1,
        snapshotIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ReservedNodeExchangeConfigurationOptions, ReservedNodeExchangeConfigurationOptionsConfig

    def describe():
        ubx.intent("look up aws_redshift_reserved_node_exchange_configuration_options")

        ubx.data(ReservedNodeExchangeConfigurationOptions, "example", ReservedNodeExchangeConfigurationOptionsConfig(
            action_type="example",
            cluster_identifier="example",
            max_records=1,
            snapshot_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_identifier" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_node_configuration_option_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
