---
title: "aws_redshift_reserved_node_exchange_offerings"
description: "`aws_redshift_reserved_node_exchange_offerings` reads a real, available offering for exchanging an existing Redshift Reserved Node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_reserved_node_exchange_offerings"})

    		ubx.Data(redshift.ReservedNodeExchangeOfferings, "example", redshift.ReservedNodeExchangeOfferingsConfig{
    			ReservedNodeId: "example",
    			MaxRecords:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedNodeExchangeOfferings } from "@ubx/sdk-aws/aws/data/redshift/reserved_node_exchange_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_reserved_node_exchange_offerings" });

      data(ReservedNodeExchangeOfferings, "example", {
        reservedNodeId: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ReservedNodeExchangeOfferings, ReservedNodeExchangeOfferingsConfig

    def describe():
        ubx.intent("look up aws_redshift_reserved_node_exchange_offerings")

        ubx.data(ReservedNodeExchangeOfferings, "example", ReservedNodeExchangeOfferingsConfig(
            reserved_node_id="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_node_id" type="string" required>
  The real, unique identifier of the reserved node this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_node_offerings" type="list(object)">
  The real Reserved Node offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
