---
title: "aws_redshift_orderable_cluster_options"
description: "`aws_redshift_orderable_cluster_options` reads the real, orderable combinations of Redshift engine version, node type, and Availability Zone."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_orderable_cluster_options"})

    		ubx.Data(redshift.OrderableClusterOptions, "example", redshift.OrderableClusterOptionsConfig{
    			ClusterVersion: "example",
    			MaxRecords:     1,
    			NodeType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrderableClusterOptions } from "@ubx/sdk-aws/aws/data/redshift/orderable_cluster_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_orderable_cluster_options" });

      data(OrderableClusterOptions, "example", {
        clusterVersion: "example",
        maxRecords: 1,
        nodeType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import OrderableClusterOptions, OrderableClusterOptionsConfig

    def describe():
        ubx.intent("look up aws_redshift_orderable_cluster_options")

        ubx.data(OrderableClusterOptions, "example", OrderableClusterOptionsConfig(
            cluster_version="example",
            max_records=1,
            node_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_version" type="string">
  The real version of the cluster. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="node_type" type="string">
  The real, discriminating type of the node this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="orderable_cluster_options" type="list(object)">
  The real, available options for the orderable cluster. **(AI-inferred)** Returned by the lookup.
</ResponseField>
