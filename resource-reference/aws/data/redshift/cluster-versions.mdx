---
title: "aws_redshift_cluster_versions"
description: "`data_aws_redshift_cluster_versions` looks up a real, already-existing `aws_redshift_cluster`."
---

It creates a real, provisioned Amazon Redshift data warehouse cluster, its leader and compute nodes, the original Redshift architecture distinct from the auto-scaling `aws_redshift_serverless_namespace`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_cluster_versions"})

    		ubx.Data(redshift.ClusterVersions, "example", redshift.ClusterVersionsConfig{
    			ClusterParameterGroupFamily: "example",
    			ClusterVersion:              "example",
    			MaxRecords:                  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ClusterVersions } from "./local-sdk/aws/sdk/typescript/aws/data/redshift/cluster_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_cluster_versions" });

      data(ClusterVersions, "example", {
        clusterParameterGroupFamily: "example",
        clusterVersion: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ClusterVersions, ClusterVersionsConfig

    def describe():
        ubx.intent("look up aws_redshift_cluster_versions")

        ubx.data(ClusterVersions, "example", ClusterVersionsConfig(
            cluster_parameter_group_family="example",
            cluster_version="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_parameter_group_family" type="string">
  The real cluster parameter group family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="cluster_version" type="string">
  The real version of the cluster. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_versions" type="list(object)">
  The real versions of the cluster included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
