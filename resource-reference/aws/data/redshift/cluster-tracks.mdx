---
title: "aws_redshift_cluster_tracks"
description: "`aws_redshift_cluster_tracks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_cluster_tracks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_cluster_tracks"})

    		ubx.Data(redshift.ClusterTracks, "example", redshift.ClusterTracksConfig{
    			MaintenanceTrackName: "example-maintenance-track-name",
    			MaxRecords:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterTracks } from "@ubx/sdk-aws/aws/data/redshift/cluster_tracks";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_cluster_tracks" });

      data(ClusterTracks, "example", {
        maintenanceTrackName: "example-maintenance-track-name",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ClusterTracks, ClusterTracksConfig

    def describe():
        ubx.intent("look up aws_redshift_cluster_tracks")

        ubx.data(ClusterTracks, "example", ClusterTracksConfig(
            maintenance_track_name="example-maintenance-track-name",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="maintenance_track_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="maintenance_tracks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
