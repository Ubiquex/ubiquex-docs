---
title: "aws_redshift_partners"
description: "`aws_redshift_partners` reads the real, integrated partner applications authorized against a Redshift cluster or datashare."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_partners"})

    		ubx.Data(redshift.Partners, "example", redshift.PartnersConfig{
    			AccountId:         "example",
    			ClusterIdentifier: "example",
    			DatabaseName:      "example-database-name",
    			PartnerName:       "example-partner-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Partners } from "@ubx/sdk-aws/aws/data/redshift/partners";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_partners" });

      data(Partners, "example", {
        accountId: "example",
        clusterIdentifier: "example",
        databaseName: "example-database-name",
        partnerName: "example-partner-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import Partners, PartnersConfig

    def describe():
        ubx.intent("look up aws_redshift_partners")

        ubx.data(Partners, "example", PartnersConfig(
            account_id="example",
            cluster_identifier="example",
            database_name="example-database-name",
            partner_name="example-partner-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string">
  The real name of the database this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="partner_name" type="string">
  The real name of the partner this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="partner_integration_info_list" type="list(object)">
  The real partner integration infos included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
