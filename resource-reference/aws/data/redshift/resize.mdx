---
title: "aws_redshift_resize"
description: "`aws_redshift_resize` reads the real, current status of a Redshift cluster resize operation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_resize"})

    		ubx.Data(redshift.Resize, "example", redshift.ResizeConfig{
    			ClusterIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resize } from "@ubx/sdk-aws/aws/data/redshift/resize";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_resize" });

      data(Resize, "example", {
        clusterIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import Resize, ResizeConfig

    def describe():
        ubx.intent("look up aws_redshift_resize")

        ubx.data(Resize, "example", ResizeConfig(
            cluster_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="avg_resize_rate_in_mega_bytes_per_second" type="number">
  The real, average resize rate for this cluster, in megabytes per second. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_transfer_progress_percent" type="number">
  The real, current progress of this data transfer, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="elapsed_time_in_seconds" type="number">
  The real, elapsed time for this object, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="estimated_time_to_completion_in_seconds" type="number">
  The real, estimated time remaining until this operation completes, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_tables_completed" type="list(string)">
  The real DynamoDB table imports that have completed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_tables_in_progress" type="list(string)">
  The real DynamoDB table imports currently in progress. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_tables_not_started" type="list(string)">
  The real DynamoDB table imports that have not yet started. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="progress_in_mega_bytes" type="number">
  The real size of the progress in mega, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resize_type" type="string">
  The real, discriminating type of the resize this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_cluster_type" type="string">
  The real, discriminating type of the target cluster this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_encryption_type" type="string">
  The real, discriminating type of the target encryption this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_node_type" type="string">
  The real, discriminating type of the target node this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_number_of_nodes" type="number">
  The real, target number of nodes for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_resize_data_in_mega_bytes" type="number">
  The real size of the total resize data in mega, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>
