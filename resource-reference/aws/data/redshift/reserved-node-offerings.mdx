---
title: "aws_redshift_reserved_node_offerings"
description: "`aws_redshift_reserved_node_offerings` reads a real, available offering for purchasing a Redshift Reserved Node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_reserved_node_offerings"})

    		ubx.Data(redshift.ReservedNodeOfferings, "example", redshift.ReservedNodeOfferingsConfig{
    			MaxRecords:             1,
    			ReservedNodeOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedNodeOfferings } from "@ubx/sdk-aws/aws/data/redshift/reserved_node_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_reserved_node_offerings" });

      data(ReservedNodeOfferings, "example", {
        maxRecords: 1,
        reservedNodeOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift import ReservedNodeOfferings, ReservedNodeOfferingsConfig

    def describe():
        ubx.intent("look up aws_redshift_reserved_node_offerings")

        ubx.data(ReservedNodeOfferings, "example", ReservedNodeOfferingsConfig(
            max_records=1,
            reserved_node_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_node_offering_id" type="string">
  The real, unique identifier of the reserved node offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_node_offerings" type="list(object)">
  The real Reserved Node offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
