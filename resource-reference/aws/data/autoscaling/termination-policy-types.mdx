---
title: "aws_autoscaling_termination_policy_types"
description: "`aws_autoscaling_termination_policy_types` reads the real, available termination policy types an Auto Scaling group can use to choose which instance to..."
---

`aws_autoscaling_termination_policy_types` reads the real, available termination policy types an Auto Scaling group can use to choose which instance to terminate first.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_termination_policy_types"})

    		ubx.Data(autoscaling.TerminationPolicyTypes, "example", autoscaling.TerminationPolicyTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TerminationPolicyTypes } from "@ubx/sdk-aws/aws/data/autoscaling/termination_policy_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_termination_policy_types" });

      data(TerminationPolicyTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import TerminationPolicyTypes, TerminationPolicyTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_termination_policy_types")

        ubx.data(TerminationPolicyTypes, "example", TerminationPolicyTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="termination_policy_types" type="list(string)">
  The real, discriminating types of termination policys this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
