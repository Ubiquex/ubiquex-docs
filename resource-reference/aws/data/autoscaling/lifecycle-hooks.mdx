---
title: "aws_autoscaling_lifecycle_hooks"
description: "`aws_autoscaling_lifecycle_hooks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_autoscaling_lifecycle_hooks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_lifecycle_hooks"})

    		ubx.Data(autoscaling.LifecycleHooks, "example", autoscaling.LifecycleHooksConfig{
    			AutoScalingGroupName: "example-auto-scaling-group-name",
    			LifecycleHookNames:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LifecycleHooks } from "@ubx/sdk-aws/aws/data/autoscaling/lifecycle_hooks";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_lifecycle_hooks" });

      data(LifecycleHooks, "example", {
        autoScalingGroupName: "example-auto-scaling-group-name",
        lifecycleHookNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import LifecycleHooks, LifecycleHooksConfig

    def describe():
        ubx.intent("look up aws_autoscaling_lifecycle_hooks")

        ubx.data(LifecycleHooks, "example", LifecycleHooksConfig(
            auto_scaling_group_name="example-auto-scaling-group-name",
            lifecycle_hook_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="auto_scaling_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="lifecycle_hook_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lifecycle_hooks" type="list(object)">
  Returned by the lookup.
</ResponseField>
