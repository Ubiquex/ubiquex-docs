---
title: "aws_autoscaling_scaling_activities"
description: "`aws_autoscaling_scaling_activities` reads a real, historical scaling activity (a launch or termination event) of an Auto Scaling group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_scaling_activities"})

    		ubx.Data(autoscaling.ScalingActivities, "example", autoscaling.ScalingActivitiesConfig{
    			ActivityIds:          []string{"example"},
    			AutoScalingGroupName: "example-auto-scaling-group-name",
    			Filters:              []map[string]any{{"name": "example-name"}},
    			IncludeDeletedGroups: true,
    			MaxRecords:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScalingActivities } from "@ubx/sdk-aws/aws/data/autoscaling/scaling_activities";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_scaling_activities" });

      data(ScalingActivities, "example", {
        activityIds: ["example"],
        autoScalingGroupName: "example-auto-scaling-group-name",
        filters: [{ name: "example-name" }],
        includeDeletedGroups: true,
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import ScalingActivities, ScalingActivitiesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_scaling_activities")

        ubx.data(ScalingActivities, "example", ScalingActivitiesConfig(
            activity_ids=["example"],
            auto_scaling_group_name="example-auto-scaling-group-name",
            filters=[{"name": "example-name"}],
            include_deleted_groups=True,
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="activity_ids" type="list(string)">
  The real, unique identifiers of the activitys this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="auto_scaling_group_name" type="string">
  The real name of the Auto Scaling group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_deleted_groups" type="bool">
  The real include deleted groups included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activities" type="list(object)">
  The real activities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
