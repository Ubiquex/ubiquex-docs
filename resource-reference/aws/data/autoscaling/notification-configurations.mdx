---
title: "aws_autoscaling_notification_configurations"
description: "`aws_autoscaling_notification_configurations` reads the real SNS notification configuration of an Auto Scaling group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_notification_configurations"})

    		ubx.Data(autoscaling.NotificationConfigurations, "example", autoscaling.NotificationConfigurationsConfig{
    			AutoScalingGroupNames: []string{"example"},
    			MaxRecords:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationConfigurations } from "@ubx/sdk-aws/aws/data/autoscaling/notification_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_notification_configurations" });

      data(NotificationConfigurations, "example", {
        autoScalingGroupNames: ["example"],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import NotificationConfigurations, NotificationConfigurationsConfig

    def describe():
        ubx.intent("look up aws_autoscaling_notification_configurations")

        ubx.data(NotificationConfigurations, "example", NotificationConfigurationsConfig(
            auto_scaling_group_names=["example"],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="auto_scaling_group_names" type="list(string)">
  The real names of the auto scaling groups included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notification_configurations" type="list(object)">
  The real notification configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
