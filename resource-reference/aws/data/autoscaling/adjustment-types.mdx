---
title: "aws_autoscaling_adjustment_types"
description: "`aws_autoscaling_adjustment_types` reads the real, available scaling adjustment types (such as ChangeInCapacity or ExactCapacity) for Auto Scaling..."
---

`aws_autoscaling_adjustment_types` reads the real, available scaling adjustment types (such as ChangeInCapacity or ExactCapacity) for Auto Scaling policies.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_adjustment_types"})

    		ubx.Data(autoscaling.AdjustmentTypes, "example", autoscaling.AdjustmentTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AdjustmentTypes } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/adjustment_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_adjustment_types" });

      data(AdjustmentTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import AdjustmentTypes, AdjustmentTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_adjustment_types")

        ubx.data(AdjustmentTypes, "example", AdjustmentTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="adjustment_types" type="list(object)">
  The real, discriminating types of adjustments this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
