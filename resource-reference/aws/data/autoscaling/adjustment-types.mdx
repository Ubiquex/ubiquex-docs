---
title: "aws_autoscaling_adjustment_types"
description: "`aws_autoscaling_adjustment_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_autoscaling_adjustment_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_adjustment_types"})

    		ubx.Data(autoscaling.AdjustmentTypes, "example", autoscaling.AdjustmentTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdjustmentTypes } from "@ubx/sdk-aws/aws/data/autoscaling/adjustment_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_adjustment_types" });

      data(AdjustmentTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import AdjustmentTypes, AdjustmentTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_adjustment_types")

        ubx.data(AdjustmentTypes, "example", AdjustmentTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="adjustment_types" type="list(object)">
  Returned by the lookup.
</ResponseField>
