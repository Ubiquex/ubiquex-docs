---
title: "aws_autoscaling_account_limits"
description: "`aws_autoscaling_account_limits` reads real, account-level EC2 Auto Scaling limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_account_limits"})

    		ubx.Data(autoscaling.AccountLimits, "example", autoscaling.AccountLimitsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLimits } from "@ubx/sdk-aws/aws/data/autoscaling/account_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_account_limits" });

      data(AccountLimits, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import AccountLimits, AccountLimitsConfig

    def describe():
        ubx.intent("look up aws_autoscaling_account_limits")

        ubx.data(AccountLimits, "example", AccountLimitsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="max_number_of_auto_scaling_groups" type="number">
  The real max number of auto scaling groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_number_of_launch_configurations" type="number">
  The real, maximum number of Auto Scaling launch configurations this account may create. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_auto_scaling_groups" type="number">
  The real number of auto scaling groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_launch_configurations" type="number">
  The real number of Auto Scaling launch configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
