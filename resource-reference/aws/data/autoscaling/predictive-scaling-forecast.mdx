---
title: "aws_autoscaling_predictive_scaling_forecast"
description: "`aws_autoscaling_predictive_scaling_forecast` reads the real, forecasted load and capacity for an Auto Scaling group's own predictive scaling policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_predictive_scaling_forecast"})

    		ubx.Data(autoscaling.PredictiveScalingForecast, "example", autoscaling.PredictiveScalingForecastConfig{
    			AutoScalingGroupName: "example-auto-scaling-group-name",
    			EndTime:              "example",
    			PolicyName:           "example-policy-name",
    			StartTime:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PredictiveScalingForecast } from "@ubx/sdk-aws/aws/data/autoscaling/predictive_scaling_forecast";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_predictive_scaling_forecast" });

      data(PredictiveScalingForecast, "example", {
        autoScalingGroupName: "example-auto-scaling-group-name",
        endTime: "example",
        policyName: "example-policy-name",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import PredictiveScalingForecast, PredictiveScalingForecastConfig

    def describe():
        ubx.intent("look up aws_autoscaling_predictive_scaling_forecast")

        ubx.data(PredictiveScalingForecast, "example", PredictiveScalingForecastConfig(
            auto_scaling_group_name="example-auto-scaling-group-name",
            end_time="example",
            policy_name="example-policy-name",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="auto_scaling_group_name" type="string" required>
  The real name of the Auto Scaling group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The real name of the policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_forecast" type="object">
  &lt;p&gt;A &lt;code&gt;GetPredictiveScalingForecast&lt;/code&gt; call returns the capacity forecast for a predictive scaling policy. This structure includes the data points for that capacity forecast, along with the timestamps of those data points. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="timestamps" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="values" type="list(number)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="load_forecast" type="list(object)">
  The real, forecasted load included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
