---
title: "aws_autoscaling_auto_scaling_instances"
description: "`aws_autoscaling_auto_scaling_instances` looks up one real EC2 instance managed by an Auto Scaling group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_auto_scaling_instances"})

    		ubx.Data(autoscaling.AutoScalingInstances, "example", autoscaling.AutoScalingInstancesConfig{
    			InstanceIds: []string{"example"},
    			MaxRecords:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AutoScalingInstances } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/auto_scaling_instances.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_auto_scaling_instances" });

      data(AutoScalingInstances, "example", {
        instanceIds: ["example"],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import AutoScalingInstances, AutoScalingInstancesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_auto_scaling_instances")

        ubx.data(AutoScalingInstances, "example", AutoScalingInstancesConfig(
            instance_ids=["example"],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_scaling_instances" type="list(object)">
  The real Auto Scaling instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
