---
title: "aws_autoscaling_scaling_process_types"
description: "`aws_autoscaling_scaling_process_types` reads the real, available scaling process types (such as Launch or AZRebalance) an Auto Scaling group can..."
---

`aws_autoscaling_scaling_process_types` reads the real, available scaling process types (such as Launch or AZRebalance) an Auto Scaling group can suspend or resume.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_scaling_process_types"})

    		ubx.Data(autoscaling.ScalingProcessTypes, "example", autoscaling.ScalingProcessTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScalingProcessTypes } from "@ubx/sdk-aws/aws/data/autoscaling/scaling_process_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_scaling_process_types" });

      data(ScalingProcessTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import ScalingProcessTypes, ScalingProcessTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_scaling_process_types")

        ubx.data(ScalingProcessTypes, "example", ScalingProcessTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="processes" type="list(object)">
  The real processes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
