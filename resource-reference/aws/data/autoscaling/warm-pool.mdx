---
title: "aws_autoscaling_warm_pool"
description: "`aws_autoscaling_warm_pool` reads the real warm pool configuration of an Auto Scaling group, pre-initializing instances to reduce scale-out latency."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_warm_pool"})

    		ubx.Data(autoscaling.WarmPool, "example", autoscaling.WarmPoolConfig{
    			AutoScalingGroupName: "example-auto-scaling-group-name",
    			MaxRecords:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WarmPool } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/warm_pool.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_warm_pool" });

      data(WarmPool, "example", {
        autoScalingGroupName: "example-auto-scaling-group-name",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import WarmPool, WarmPoolConfig

    def describe():
        ubx.intent("look up aws_autoscaling_warm_pool")

        ubx.data(WarmPool, "example", WarmPoolConfig(
            auto_scaling_group_name="example-auto-scaling-group-name",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="auto_scaling_group_name" type="string" required>
  The real name of the Auto Scaling group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="list(object)">
  The real instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="warm_pool_configuration" type="object">
  &lt;p&gt;Describes a warm pool configuration. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="instance_reuse_policy" type="object">
      &lt;p&gt;Describes an instance reuse policy for a warm pool. &lt;/p&gt; &lt;p&gt;For more information, see &lt;a href="https://docs.aws.amazon.com/autoscaling/ec2/userguide/ec2-auto-scaling-warm-pools.html"&gt;Warm pools for Amazon EC2 Auto Scaling&lt;/a&gt; in the &lt;i&gt;Amazon EC2 Auto Scaling User Guide&lt;/i&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="reuse_on_scale_in" type="bool">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_group_prepared_capacity" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="min_size" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pool_state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
