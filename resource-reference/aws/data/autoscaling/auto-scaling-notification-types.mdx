---
title: "aws_autoscaling_auto_scaling_notification_types"
description: "`aws_autoscaling_auto_scaling_notification_types` reads the real, available notification event types an Auto Scaling group can publish."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_auto_scaling_notification_types"})

    		ubx.Data(autoscaling.AutoScalingNotificationTypes, "example", autoscaling.AutoScalingNotificationTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AutoScalingNotificationTypes } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/auto_scaling_notification_types.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_autoscaling_auto_scaling_notification_types",
      });

      data(AutoScalingNotificationTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import AutoScalingNotificationTypes, AutoScalingNotificationTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_auto_scaling_notification_types")

        ubx.data(AutoScalingNotificationTypes, "example", AutoScalingNotificationTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="auto_scaling_notification_types" type="list(string)">
  The real, discriminating types of auto scaling notifications this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
