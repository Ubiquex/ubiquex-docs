---
title: "aws_autoscaling_metric_collection_types"
description: "`aws_autoscaling_metric_collection_types` reads the real, available CloudWatch metric types an Auto Scaling group can enable detailed monitoring for."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_metric_collection_types"})

    		ubx.Data(autoscaling.MetricCollectionTypes, "example", autoscaling.MetricCollectionTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetricCollectionTypes } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/metric_collection_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_metric_collection_types" });

      data(MetricCollectionTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import MetricCollectionTypes, MetricCollectionTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_metric_collection_types")

        ubx.data(MetricCollectionTypes, "example", MetricCollectionTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="granularities" type="list(object)">
  The real time granularities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metrics" type="list(object)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
