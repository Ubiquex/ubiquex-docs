---
title: "aws_autoscaling_lifecycle_hook_types"
description: "`aws_autoscaling_lifecycle_hook_types` reads the real, available lifecycle transition types an Auto Scaling lifecycle hook can attach to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_lifecycle_hook_types"})

    		ubx.Data(autoscaling.LifecycleHookTypes, "example", autoscaling.LifecycleHookTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LifecycleHookTypes } from "@ubx/sdk-aws/aws/data/autoscaling/lifecycle_hook_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_lifecycle_hook_types" });

      data(LifecycleHookTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import LifecycleHookTypes, LifecycleHookTypesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_lifecycle_hook_types")

        ubx.data(LifecycleHookTypes, "example", LifecycleHookTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="lifecycle_hook_types" type="list(string)">
  The real, discriminating types of lifecycle hooks this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
