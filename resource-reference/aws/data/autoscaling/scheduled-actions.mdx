---
title: "aws_autoscaling_scheduled_actions"
description: "`aws_autoscaling_scheduled_actions` looks up a real scheduled action, changing an Auto Scaling group's own capacity at a specific time."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_scheduled_actions"})

    		ubx.Data(autoscaling.ScheduledActions, "example", autoscaling.ScheduledActionsConfig{
    			AutoScalingGroupName: "example-auto-scaling-group-name",
    			EndTime:              "example",
    			MaxRecords:           1,
    			ScheduledActionNames: []string{"example"},
    			StartTime:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ScheduledActions } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling/scheduled_actions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_scheduled_actions" });

      data(ScheduledActions, "example", {
        autoScalingGroupName: "example-auto-scaling-group-name",
        endTime: "example",
        maxRecords: 1,
        scheduledActionNames: ["example"],
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling import ScheduledActions, ScheduledActionsConfig

    def describe():
        ubx.intent("look up aws_autoscaling_scheduled_actions")

        ubx.data(ScheduledActions, "example", ScheduledActionsConfig(
            auto_scaling_group_name="example-auto-scaling-group-name",
            end_time="example",
            max_records=1,
            scheduled_action_names=["example"],
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="auto_scaling_group_name" type="string">
  The real name of the Auto Scaling group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scheduled_action_names" type="list(string)">
  The real names of the scheduled actions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_update_group_actions" type="list(object)">
  The real Auto Scaling scheduled update-group actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
