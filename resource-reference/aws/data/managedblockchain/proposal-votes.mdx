---
title: "aws_managedblockchain_proposal_votes"
description: "`aws_managedblockchain_proposal_votes` reads the real, cast votes on a Managed Blockchain governance proposal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_proposal_votes"})

    		ubx.Data(managedblockchain.ProposalVotes, "example", managedblockchain.ProposalVotesConfig{
    			NetworkId:  "example",
    			ProposalId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProposalVotes } from "@ubx/sdk-aws/aws/data/managedblockchain/proposal_votes";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_proposal_votes" });

      data(ProposalVotes, "example", {
        networkId: "example",
        proposalId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain import ProposalVotes, ProposalVotesConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_proposal_votes")

        ubx.data(ProposalVotes, "example", ProposalVotesConfig(
            network_id="example",
            proposal_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="network_id" type="string" required>
  The real, unique identifier of the network this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="proposal_id" type="string" required>
  The real, unique identifier of the proposal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="proposal_votes" type="list(object)">
  The real votes cast on this proposal. **(AI-inferred)** Returned by the lookup.
</ResponseField>
