---
title: "aws_dms_target_selection_rules"
description: "`aws_dms_target_selection_rules` reads real DMS Fleet Advisor rules selecting the target engine recommended for a discovered database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_target_selection_rules"})

    		ubx.Data(dms.TargetSelectionRules, "example", dms.TargetSelectionRulesConfig{
    			MigrationProjectIdentifier: "example",
    			SelectionRules:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TargetSelectionRules } from "@ubx/sdk-aws/aws/data/dms/target_selection_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_target_selection_rules" });

      data(TargetSelectionRules, "example", {
        migrationProjectIdentifier: "example",
        selectionRules: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import TargetSelectionRules, TargetSelectionRulesConfig

    def describe():
        ubx.intent("look up aws_dms_target_selection_rules")

        ubx.data(TargetSelectionRules, "example", TargetSelectionRulesConfig(
            migration_project_identifier="example",
            selection_rules="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="migration_project_identifier" type="string" required>
  The real, unique identifier of the migration project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="selection_rules" type="string" required>
  The real rules used to select this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="target_selection_rules" type="string">
  The real rules for the target selection. **(AI-inferred)** Returned by the lookup.
</ResponseField>
