---
title: "aws_dms_metadata_model_exports_to_target"
description: "`aws_dms_metadata_model_exports_to_target` reads the real status of applying a DMS Schema Conversion metadata model directly to a target database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_metadata_model_exports_to_target"})

    		ubx.Data(dms.MetadataModelExportsToTarget, "example", dms.MetadataModelExportsToTargetConfig{
    			MigrationProjectIdentifier: "example",
    			Filters:                    []map[string]any{{"name": "example-name"}},
    			MaxRecords:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetadataModelExportsToTarget } from "@ubx/sdk-aws/aws/data/dms/metadata_model_exports_to_target";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_metadata_model_exports_to_target" });

      data(MetadataModelExportsToTarget, "example", {
        migrationProjectIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import MetadataModelExportsToTarget, MetadataModelExportsToTargetConfig
    from ubx.aws.data.dms.metadata_model_exports_to_target import MetadataModelExportsToTarget_Filters

    def describe():
        ubx.intent("look up aws_dms_metadata_model_exports_to_target")

        ubx.data(MetadataModelExportsToTarget, "example", MetadataModelExportsToTargetConfig(
            migration_project_identifier="example",
            filters=[MetadataModelExportsToTarget_Filters(name="example-name")],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="migration_project_identifier" type="string" required>
  The real, unique identifier of the migration project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="requests" type="list(object)">
  The real requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
