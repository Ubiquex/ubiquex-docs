---
title: "aws_dms_replication_task_assessment_results"
description: "`data_aws_dms_replication_task_assessment_results` looks up a real, already-existing `aws_dms_replication_task`."
---

It is the real, actual migration or CDC job that runs on an `aws_dms_replication_instance`, moving data from a source `aws_dms_endpoint` to a target one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_replication_task_assessment_results"})

    		ubx.Data(dms.ReplicationTaskAssessmentResults, "example", dms.ReplicationTaskAssessmentResultsConfig{
    			MaxRecords:         1,
    			ReplicationTaskArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReplicationTaskAssessmentResults } from "@ubx/sdk-aws/aws/data/dms/replication_task_assessment_results";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_replication_task_assessment_results" });

      data(ReplicationTaskAssessmentResults, "example", {
        maxRecords: 1,
        replicationTaskArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import ReplicationTaskAssessmentResults, ReplicationTaskAssessmentResultsConfig

    def describe():
        ubx.intent("look up aws_dms_replication_task_assessment_results")

        ubx.data(ReplicationTaskAssessmentResults, "example", ReplicationTaskAssessmentResultsConfig(
            max_records=1,
            replication_task_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replication_task_arn" type="string">
  The real Amazon Resource Name (ARN) of the replication task this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bucket_name" type="string">
  The real name of the bucket this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replication_task_assessment_results" type="list(object)">
  The real DMS replication task assessment results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
