---
title: "aws_dms_orderable_replication_instances"
description: "`aws_dms_orderable_replication_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_dms_orderable_replication_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_orderable_replication_instances"})

    		ubx.Data(dms.OrderableReplicationInstances, "example", dms.OrderableReplicationInstancesConfig{
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrderableReplicationInstances } from "@ubx/sdk-aws/aws/data/dms/orderable_replication_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_orderable_replication_instances" });

      data(OrderableReplicationInstances, "example", {
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import OrderableReplicationInstances, OrderableReplicationInstancesConfig

    def describe():
        ubx.intent("look up aws_dms_orderable_replication_instances")

        ubx.data(OrderableReplicationInstances, "example", OrderableReplicationInstancesConfig(
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="orderable_replication_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
