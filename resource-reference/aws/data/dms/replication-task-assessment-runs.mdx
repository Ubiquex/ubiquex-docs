---
title: "aws_dms_replication_task_assessment_runs"
description: "`data_aws_dms_replication_task_assessment_runs` looks up a real, already-existing `aws_dms_replication_task`."
---

It is the real, actual migration or CDC job that runs on an `aws_dms_replication_instance`, moving data from a source `aws_dms_endpoint` to a target one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_replication_task_assessment_runs"})

    		ubx.Data(dms.ReplicationTaskAssessmentRuns, "example", dms.ReplicationTaskAssessmentRunsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReplicationTaskAssessmentRuns } from "@ubx/sdk-aws/aws/data/dms/replication_task_assessment_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_replication_task_assessment_runs" });

      data(ReplicationTaskAssessmentRuns, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import ReplicationTaskAssessmentRuns, ReplicationTaskAssessmentRunsConfig

    def describe():
        ubx.intent("look up aws_dms_replication_task_assessment_runs")

        ubx.data(ReplicationTaskAssessmentRuns, "example", ReplicationTaskAssessmentRunsConfig(
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replication_task_assessment_runs" type="list(object)">
  The real DMS replication task assessment runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
