---
title: "aws_dms_fleet_advisor_schemas"
description: "`aws_dms_fleet_advisor_schemas` reads the real database schemas discovered by a DMS Fleet Advisor collector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_fleet_advisor_schemas"})

    		ubx.Data(dms.FleetAdvisorSchemas, "example", dms.FleetAdvisorSchemasConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FleetAdvisorSchemas } from "./local-sdk/aws/sdk/typescript/aws/data/dms/fleet_advisor_schemas.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_fleet_advisor_schemas" });

      data(FleetAdvisorSchemas, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import FleetAdvisorSchemas, FleetAdvisorSchemasConfig

    def describe():
        ubx.intent("look up aws_dms_fleet_advisor_schemas")

        ubx.data(FleetAdvisorSchemas, "example", FleetAdvisorSchemasConfig(
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_advisor_schemas" type="list(object)">
  The real DMS Fleet Advisor schemas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
