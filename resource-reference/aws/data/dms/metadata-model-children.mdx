---
title: "aws_dms_metadata_model_children"
description: "`aws_dms_metadata_model_children` reads the real child schema objects of a node in a DMS Schema Conversion metadata model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_metadata_model_children"})

    		ubx.Data(dms.MetadataModelChildren, "example", dms.MetadataModelChildrenConfig{
    			MigrationProjectIdentifier: "example",
    			Origin:                     "example",
    			SelectionRules:             "example",
    			MaxRecords:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetadataModelChildren } from "./local-sdk/aws/sdk/typescript/aws/data/dms/metadata_model_children.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_metadata_model_children" });

      data(MetadataModelChildren, "example", {
        migrationProjectIdentifier: "example",
        origin: "example",
        selectionRules: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import MetadataModelChildren, MetadataModelChildrenConfig

    def describe():
        ubx.intent("look up aws_dms_metadata_model_children")

        ubx.data(MetadataModelChildren, "example", MetadataModelChildrenConfig(
            migration_project_identifier="example",
            origin="example",
            selection_rules="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="migration_project_identifier" type="string" required>
  The real, unique identifier of the migration project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="origin" type="string" required>
  The real origin of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="selection_rules" type="string" required>
  The real rules used to select this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metadata_model_children" type="list(object)">
  The real child objects of this metadata model. **(AI-inferred)** Returned by the lookup.
</ResponseField>
