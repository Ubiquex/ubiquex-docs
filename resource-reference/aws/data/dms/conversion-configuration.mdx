---
title: "aws_dms_conversion_configuration"
description: "`aws_dms_conversion_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_dms_conversion_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_conversion_configuration"})

    		ubx.Data(dms.ConversionConfiguration, "example", dms.ConversionConfigurationConfig{
    			MigrationProjectIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConversionConfiguration } from "@ubx/sdk-aws/aws/data/dms/conversion_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_conversion_configuration" });

      data(ConversionConfiguration, "example", {
        migrationProjectIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import ConversionConfiguration, ConversionConfigurationConfig

    def describe():
        ubx.intent("look up aws_dms_conversion_configuration")

        ubx.data(ConversionConfiguration, "example", ConversionConfigurationConfig(
            migration_project_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="migration_project_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="conversion_configuration" type="string">
  Returned by the lookup.
</ResponseField>
