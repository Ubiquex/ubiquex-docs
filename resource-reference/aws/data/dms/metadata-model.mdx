---
title: "aws_dms_metadata_model"
description: "`aws_dms_metadata_model` looks up one real schema object within a DMS Schema Conversion migration project's own metadata model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_metadata_model"})

    		ubx.Data(dms.MetadataModel, "example", dms.MetadataModelConfig{
    			MigrationProjectIdentifier: "example",
    			Origin:                     "example",
    			SelectionRules:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetadataModel } from "@ubx/sdk-aws/aws/data/dms/metadata_model";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_metadata_model" });

      data(MetadataModel, "example", {
        migrationProjectIdentifier: "example",
        origin: "example",
        selectionRules: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import MetadataModel, MetadataModelConfig

    def describe():
        ubx.intent("look up aws_dms_metadata_model")

        ubx.data(MetadataModel, "example", MetadataModelConfig(
            migration_project_identifier="example",
            origin="example",
            selection_rules="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="migration_project_identifier" type="string" required>
  The real, unique identifier of the migration project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="origin" type="string" required>
  The real origin of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="selection_rules" type="string" required>
  The real rules used to select this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="definition" type="string">
  The real, structured definition of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metadata_model_name" type="string">
  The real name of the metadata model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metadata_model_type" type="string">
  The real, discriminating type of the metadata model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_metadata_models" type="list(object)">
  The real target metadata models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
