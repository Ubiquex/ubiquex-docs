---
title: "aws_dms_metadata_model_creations"
description: "`aws_dms_metadata_model_creations` reads the real jobs that created a DMS Schema Conversion metadata model from a source database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_metadata_model_creations"})

    		ubx.Data(dms.MetadataModelCreations, "example", dms.MetadataModelCreationsConfig{
    			MigrationProjectIdentifier: "example",
    			Filters:                    []map[string]any{{"name": "example-name"}},
    			MaxRecords:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetadataModelCreations } from "./local-sdk/aws/sdk/typescript/aws/data/dms/metadata_model_creations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_metadata_model_creations" });

      data(MetadataModelCreations, "example", {
        migrationProjectIdentifier: "example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import MetadataModelCreations, MetadataModelCreationsConfig

    def describe():
        ubx.intent("look up aws_dms_metadata_model_creations")

        ubx.data(MetadataModelCreations, "example", MetadataModelCreationsConfig(
            migration_project_identifier="example",
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="migration_project_identifier" type="string" required>
  The real, unique identifier of the migration project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="requests" type="list(object)">
  The real requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
