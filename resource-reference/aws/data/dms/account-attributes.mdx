---
title: "aws_dms_account_attributes"
description: "`aws_dms_account_attributes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_dms_account_attributes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_account_attributes"})

    		ubx.Data(dms.AccountAttributes, "example", dms.AccountAttributesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAttributes } from "@ubx/sdk-aws/aws/data/dms/account_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_account_attributes" });

      data(AccountAttributes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_dms_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_quotas" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="unique_account_identifier" type="string">
  Returned by the lookup.
</ResponseField>
