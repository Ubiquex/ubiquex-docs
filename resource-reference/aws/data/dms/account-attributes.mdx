---
title: "aws_dms_account_attributes"
description: "`aws_dms_account_attributes` reads real, account-level DMS attributes and quotas."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_account_attributes"})

    		ubx.Data(dms.AccountAttributes, "example", dms.AccountAttributesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/dms/account_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_account_attributes" });

      data(AccountAttributes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import AccountAttributes, AccountAttributesConfig

    def describe():
        ubx.intent("look up aws_dms_account_attributes")

        ubx.data(AccountAttributes, "example", AccountAttributesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_quotas" type="list(object)">
  The real, current account-level quotas for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unique_account_identifier" type="string">
  The real, unique identifier of the unique account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
