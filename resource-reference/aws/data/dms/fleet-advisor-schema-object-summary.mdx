---
title: "aws_dms_fleet_advisor_schema_object_summary"
description: "`aws_dms_fleet_advisor_schema_object_summary` reads a real, summarized inventory of schema objects discovered by a DMS Fleet Advisor collector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_fleet_advisor_schema_object_summary"})

    		ubx.Data(dms.FleetAdvisorSchemaObjectSummary, "example", dms.FleetAdvisorSchemaObjectSummaryConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetAdvisorSchemaObjectSummary } from "@ubx/sdk-aws/aws/data/dms/fleet_advisor_schema_object_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_fleet_advisor_schema_object_summary" });

      data(FleetAdvisorSchemaObjectSummary, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import FleetAdvisorSchemaObjectSummary, FleetAdvisorSchemaObjectSummaryConfig

    def describe():
        ubx.intent("look up aws_dms_fleet_advisor_schema_object_summary")

        ubx.data(FleetAdvisorSchemaObjectSummary, "example", FleetAdvisorSchemaObjectSummaryConfig(
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_advisor_schema_objects" type="list(object)">
  The real DMS Fleet Advisor schema objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
