---
title: "aws_dms_endpoint_types"
description: "`data_aws_dms_endpoint_types` looks up a real, already-existing `aws_dms_endpoint`."
---

It defines the real connection details, engine type, credentials, and location, for one source or target database that an `aws_dms_replication_task` or `aws_dms_replication_config` actually migrates data to or from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_endpoint_types"})

    		ubx.Data(dms.EndpointTypes, "example", dms.EndpointTypesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EndpointTypes } from "./local-sdk/aws/sdk/typescript/aws/data/dms/endpoint_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_endpoint_types" });

      data(EndpointTypes, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import EndpointTypes, EndpointTypesConfig

    def describe():
        ubx.intent("look up aws_dms_endpoint_types")

        ubx.data(EndpointTypes, "example", EndpointTypesConfig(
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="supported_endpoint_types" type="list(object)">
  The real, discriminating types of supported endpoints this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
