---
title: "aws_dms_fleet_advisor_databases"
description: "`aws_dms_fleet_advisor_databases` looks up a real, discovered database within a DMS Fleet Advisor collector's own inventory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_fleet_advisor_databases"})

    		ubx.Data(dms.FleetAdvisorDatabases, "example", dms.FleetAdvisorDatabasesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxRecords: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FleetAdvisorDatabases } from "./local-sdk/aws/sdk/typescript/aws/data/dms/fleet_advisor_databases.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_fleet_advisor_databases" });

      data(FleetAdvisorDatabases, "example", {
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.dms import FleetAdvisorDatabases, FleetAdvisorDatabasesConfig

    def describe():
        ubx.intent("look up aws_dms_fleet_advisor_databases")

        ubx.data(FleetAdvisorDatabases, "example", FleetAdvisorDatabasesConfig(
            filters=[{"name": "example-name"}],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="databases" type="list(object)">
  The real databases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
