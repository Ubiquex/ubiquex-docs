---
title: "aws_dms_table_statistics"
description: "`aws_dms_table_statistics` reads real, per-table replication statistics for a DMS replication task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_dms_table_statistics"})

    		ubx.Data(dms.TableStatistics, "example", dms.TableStatisticsConfig{
    			ReplicationTaskArn: "arn:aws:iam::123456789012:policy/example",
    			Filters:            []map[string]any{{"name": "example-name"}},
    			MaxRecords:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TableStatistics } from "@ubx/sdk-aws/aws/data/dms/table_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_dms_table_statistics" });

      data(TableStatistics, "example", {
        replicationTaskArn: "arn:aws:iam::123456789012:policy/example",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.dms import TableStatistics, TableStatisticsConfig
    from ubx.aws.data.dms.table_statistics import TableStatistics_Filters

    def describe():
        ubx.intent("look up aws_dms_table_statistics")

        ubx.data(TableStatistics, "example", TableStatisticsConfig(
            replication_task_arn="arn:aws:iam::123456789012:policy/example",
            filters=[TableStatistics_Filters(name="example-name")],
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replication_task_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the replication task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="table_statistics" type="list(object)">
  The real, computed statistics for this table. **(AI-inferred)** Returned by the lookup.
</ResponseField>
