---
title: "aws_participant_connect_attachment"
description: "`aws_participant_connect_attachment` reads real, uploaded attachment data from a Connect chat participant."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	participant_connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/participant_connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_participant_connect_attachment"})

    		ubx.Data(participant_connect.ParticipantConnectAttachment, "example", participant_connect.ParticipantConnectAttachmentConfig{
    			AttachmentId:       "example",
    			ConnectionToken:    "example",
    			UrlExpiryInSeconds: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ParticipantConnectAttachment } from "@ubx/sdk-aws/aws/data/participant_connect/participant_connect_attachment";

    export default stack("example", () => {
      intent({ summary: "look up aws_participant_connect_attachment" });

      data(ParticipantConnectAttachment, "example", {
        attachmentId: "example",
        connectionToken: "example",
        urlExpiryInSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.participant_connect import ParticipantConnectAttachment, ParticipantConnectAttachmentConfig

    def describe():
        ubx.intent("look up aws_participant_connect_attachment")

        ubx.data(ParticipantConnectAttachment, "example", ParticipantConnectAttachmentConfig(
            attachment_id="example",
            connection_token="example",
            url_expiry_in_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attachment_id" type="string" required>
  The real, unique identifier of the attachment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="connection_token" type="string" required>
  The real, opaque token identifying this connection. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="url_expiry_in_seconds" type="number">
  The real duration this URL remains valid, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attachment_size_in_bytes" type="number">
  The real size of the attachment size in, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url_expiry" type="string">
  The real timestamp this URL expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>
