---
title: "aws_participant_connect_transcript"
description: "`aws_participant_connect_transcript` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_participant_connect_transcript` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	participant_connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/participant_connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_participant_connect_transcript"})

    		ubx.Data(participant_connect.ParticipantConnectTranscript, "example", participant_connect.ParticipantConnectTranscriptConfig{
    			ConnectionToken: "example",
    			ContactId:       "example",
    			MaxResults:      1,
    			ScanDirection:   "example",
    			SortOrder:       "example",
    			StartPosition:   map[string]any{"absolute_time": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ParticipantConnectTranscript } from "@ubx/sdk-aws/aws/data/participant_connect/participant_connect_transcript";

    export default stack("example", () => {
      intent({ summary: "look up aws_participant_connect_transcript" });

      data(ParticipantConnectTranscript, "example", {
        connectionToken: "example",
        contactId: "example",
        maxResults: 1,
        scanDirection: "example",
        sortOrder: "example",
        startPosition: { absoluteTime: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.participant_connect import ParticipantConnectTranscript, ParticipantConnectTranscriptConfig

    def describe():
        ubx.intent("look up aws_participant_connect_transcript")

        ubx.data(ParticipantConnectTranscript, "example", ParticipantConnectTranscriptConfig(
            connection_token="example",
            contact_id="example",
            max_results=1,
            scan_direction="example",
            sort_order="example",
            start_position={"absolute_time": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="contact_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scan_direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_position" type="object">
  &lt;p&gt;A filtering option for where to start. For example, if you sent 100 messages, start with message 50. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="absolute_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="most_recent" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="initial_contact_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transcript" type="list(object)">
  Returned by the lookup.
</ResponseField>
