---
title: "aws_participant_connect_view"
description: "`aws_participant_connect_view` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_participant_connect_view` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	participant_connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/participant_connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_participant_connect_view"})

    		ubx.Data(participant_connect.ParticipantConnectView, "example", participant_connect.ParticipantConnectViewConfig{
    			ConnectionToken: "example",
    			ViewToken:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ParticipantConnectView } from "@ubx/sdk-aws/aws/data/participant_connect/participant_connect_view";

    export default stack("example", () => {
      intent({ summary: "look up aws_participant_connect_view" });

      data(ParticipantConnectView, "example", {
        connectionToken: "example",
        viewToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.participant_connect import ParticipantConnectView, ParticipantConnectViewConfig

    def describe():
        ubx.intent("look up aws_participant_connect_view")

        ubx.data(ParticipantConnectView, "example", ParticipantConnectViewConfig(
            connection_token="example",
            view_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="view_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="view" type="object">
  &lt;p&gt;A view resource object. Contains metadata and content necessary to render the view.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="content" type="object">
      &lt;p&gt;View content containing all content necessary to render a view except for runtime input data.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="actions" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="input_schema" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="template" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="version" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
