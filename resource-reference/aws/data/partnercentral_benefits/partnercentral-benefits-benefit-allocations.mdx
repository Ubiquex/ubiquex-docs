---
title: "aws_partnercentral_benefits_benefit_allocations"
description: "`aws_partnercentral_benefits_benefit_allocations` reads the real allocations of an AWS Partner Central benefit."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnercentral_benefits "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/partnercentral_benefits"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_partnercentral_benefits_benefit_allocations"})

    		ubx.Data(partnercentral_benefits.PartnercentralBenefitsBenefitAllocations, "example", partnercentral_benefits.PartnercentralBenefitsBenefitAllocationsConfig{
    			Catalog:                       "example",
    			BenefitApplicationIdentifiers: []string{"example"},
    			BenefitIdentifiers:            []string{"example"},
    			FulfillmentTypes:              []string{"example"},
    			MaxResults:                    1,
    			Status:                        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PartnercentralBenefitsBenefitAllocations } from "@ubx/sdk-aws/aws/data/partnercentral_benefits/partnercentral_benefits_benefit_allocations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_partnercentral_benefits_benefit_allocations",
      });

      data(PartnercentralBenefitsBenefitAllocations, "example", {
        catalog: "example",
        benefitApplicationIdentifiers: ["example"],
        benefitIdentifiers: ["example"],
        fulfillmentTypes: ["example"],
        maxResults: 1,
        status: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.partnercentral_benefits import PartnercentralBenefitsBenefitAllocations, PartnercentralBenefitsBenefitAllocationsConfig

    def describe():
        ubx.intent("look up aws_partnercentral_benefits_benefit_allocations")

        ubx.data(PartnercentralBenefitsBenefitAllocations, "example", PartnercentralBenefitsBenefitAllocationsConfig(
            catalog="example",
            benefit_application_identifiers=["example"],
            benefit_identifiers=["example"],
            fulfillment_types=["example"],
            max_results=1,
            status=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="benefit_application_identifiers" type="list(string)">
  The real, unique identifiers of the benefit applications this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="benefit_identifiers" type="list(string)">
  The real, unique identifiers of the benefits this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fulfillment_types" type="list(string)">
  The real fulfillment types included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="list(string)">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="benefit_allocation_summaries" type="list(object)">
  The real, summarized benefit allocations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
