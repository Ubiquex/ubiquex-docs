---
title: "aws_partnercentral_benefits_benefits"
description: "`aws_partnercentral_benefits_benefits` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_partnercentral_benefits_benefits` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnercentral_benefits "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/partnercentral_benefits"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_partnercentral_benefits_benefits"})

    		ubx.Data(partnercentral_benefits.PartnercentralBenefitsBenefits, "example", partnercentral_benefits.PartnercentralBenefitsBenefitsConfig{
    			Catalog:          "example",
    			FulfillmentTypes: []string{"example"},
    			MaxResults:       1,
    			Programs:         []string{"example"},
    			Status:           []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PartnercentralBenefitsBenefits } from "@ubx/sdk-aws/aws/data/partnercentral_benefits/partnercentral_benefits_benefits";

    export default stack("example", () => {
      intent({ summary: "look up aws_partnercentral_benefits_benefits" });

      data(PartnercentralBenefitsBenefits, "example", {
        catalog: "example",
        fulfillmentTypes: ["example"],
        maxResults: 1,
        programs: ["example"],
        status: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.partnercentral_benefits import PartnercentralBenefitsBenefits, PartnercentralBenefitsBenefitsConfig

    def describe():
        ubx.intent("look up aws_partnercentral_benefits_benefits")

        ubx.data(PartnercentralBenefitsBenefits, "example", PartnercentralBenefitsBenefitsConfig(
            catalog="example",
            fulfillment_types=["example"],
            max_results=1,
            programs=["example"],
            status=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="fulfillment_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="programs" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="benefit_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
