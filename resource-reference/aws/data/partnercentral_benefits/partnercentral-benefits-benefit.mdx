---
title: "aws_partnercentral_benefits_benefit"
description: "`aws_partnercentral_benefits_benefit` looks up one real AWS Partner Central benefit."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	partnercentral_benefits "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/partnercentral_benefits"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_partnercentral_benefits_benefit"})

    		ubx.Data(partnercentral_benefits.PartnercentralBenefitsBenefit, "example", partnercentral_benefits.PartnercentralBenefitsBenefitConfig{
    			Catalog:    "example",
    			Identifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PartnercentralBenefitsBenefit } from "./local-sdk/aws/sdk/typescript/aws/data/partnercentral_benefits/partnercentral_benefits_benefit.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_partnercentral_benefits_benefit" });

      data(PartnercentralBenefitsBenefit, "example", {
        catalog: "example",
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.partnercentral_benefits import PartnercentralBenefitsBenefit, PartnercentralBenefitsBenefitConfig

    def describe():
        ubx.intent("look up aws_partnercentral_benefits_benefit")

        ubx.data(PartnercentralBenefitsBenefit, "example", PartnercentralBenefitsBenefitConfig(
            catalog="example",
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  The real catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="benefit_request_schema" type="any">
  The real, structured schema for this benefit request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fulfillment_types" type="list(string)">
  The real fulfillment types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="programs" type="list(string)">
  The real programs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
