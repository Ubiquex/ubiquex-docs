---
title: "aws_docdb_dbcluster_parameters"
description: "`data_aws_docdb_dbcluster_parameters` looks up a real, already-existing `aws_doc_db_dbcluster_parameter_group`."
---

It sets real, engine-level configuration for an `aws_doc_db_dbcluster`, kept as its own resource so the same parameter set can be applied consistently across multiple clusters.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	docdb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/docdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_docdb_dbcluster_parameters"})

    		ubx.Data(docdb.DbclusterParameters, "example", docdb.DbclusterParametersConfig{
    			DbclusterParameterGroupName: "example-dbcluster-parameter-group-name",
    			Filters:                     []map[string]any{{"name": "example-name"}},
    			MaxRecords:                  1,
    			Source:                      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DbclusterParameters } from "./local-sdk/aws/sdk/typescript/aws/data/docdb/dbcluster_parameters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_docdb_dbcluster_parameters" });

      data(DbclusterParameters, "example", {
        dbclusterParameterGroupName: "example-dbcluster-parameter-group-name",
        filters: [{ name: "example-name" }],
        maxRecords: 1,
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.docdb import DbclusterParameters, DbclusterParametersConfig

    def describe():
        ubx.intent("look up aws_docdb_dbcluster_parameters")

        ubx.data(DbclusterParameters, "example", DbclusterParametersConfig(
            dbcluster_parameter_group_name="example-dbcluster-parameter-group-name",
            filters=[{"name": "example-name"}],
            max_records=1,
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dbcluster_parameter_group_name" type="string" required>
  The real name of the dbcluster parameter group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string">
  The real source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
