---
title: "aws_portal_sso_account_roles"
description: "`aws_portal_sso_account_roles` reads the real IAM Identity Center roles available to the caller for a given AWS account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	portal_sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/portal_sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_portal_sso_account_roles"})

    		ubx.Data(portal_sso.PortalSsoAccountRoles, "example", portal_sso.PortalSsoAccountRolesConfig{
    			AccessToken: "example",
    			AccountId:   "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PortalSsoAccountRoles } from "@ubx/sdk-aws/aws/data/portal_sso/portal_sso_account_roles";

    export default stack("example", () => {
      intent({ summary: "look up aws_portal_sso_account_roles" });

      data(PortalSsoAccountRoles, "example", {
        accessToken: "example",
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.portal_sso import PortalSsoAccountRoles, PortalSsoAccountRolesConfig

    def describe():
        ubx.intent("look up aws_portal_sso_account_roles")

        ubx.data(PortalSsoAccountRoles, "example", PortalSsoAccountRolesConfig(
            access_token="example",
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_list" type="list(object)">
  The real roles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
