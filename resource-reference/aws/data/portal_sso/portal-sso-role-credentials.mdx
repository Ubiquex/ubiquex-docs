---
title: "aws_portal_sso_role_credentials"
description: "`aws_portal_sso_role_credentials` reads real, temporary AWS credentials for an IAM Identity Center role assignment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	portal_sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/portal_sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_portal_sso_role_credentials"})

    		ubx.Data(portal_sso.PortalSsoRoleCredentials, "example", portal_sso.PortalSsoRoleCredentialsConfig{
    			AccessToken: "example",
    			AccountId:   "example",
    			RoleName:    "example-role-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PortalSsoRoleCredentials } from "./local-sdk/aws/sdk/typescript/aws/data/portal_sso/portal_sso_role_credentials.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_portal_sso_role_credentials" });

      data(PortalSsoRoleCredentials, "example", {
        accessToken: "example",
        accountId: "example",
        roleName: "example-role-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.portal_sso import PortalSsoRoleCredentials, PortalSsoRoleCredentialsConfig

    def describe():
        ubx.intent("look up aws_portal_sso_role_credentials")

        ubx.data(PortalSsoRoleCredentials, "example", PortalSsoRoleCredentialsConfig(
            access_token="example",
            account_id="example",
            role_name="example-role-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  The real name of the IAM role this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="role_credentials" type="object">
  &lt;p&gt;Provides information about the role credentials that are assigned to the user.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expiration" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="secret_access_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
