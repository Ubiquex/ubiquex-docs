---
title: "aws_portal_sso_accounts"
description: "`aws_portal_sso_accounts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_portal_sso_accounts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	portal_sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/portal_sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_portal_sso_accounts"})

    		ubx.Data(portal_sso.PortalSsoAccounts, "example", portal_sso.PortalSsoAccountsConfig{
    			AccessToken: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PortalSsoAccounts } from "@ubx/sdk-aws/aws/data/portal_sso/portal_sso_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_portal_sso_accounts" });

      data(PortalSsoAccounts, "example", {
        accessToken: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.portal_sso import PortalSsoAccounts, PortalSsoAccountsConfig

    def describe():
        ubx.intent("look up aws_portal_sso_accounts")

        ubx.data(PortalSsoAccounts, "example", PortalSsoAccountsConfig(
            access_token="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
