---
title: "aws_cognito_idp_resource_servers"
description: "`aws_cognito_idp_resource_servers` looks up a real Cognito resource server, defining custom OAuth scopes for a user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_resource_servers"})

    		ubx.Data(cognito_idp.CognitoIdpResourceServers, "example", cognito_idp.CognitoIdpResourceServersConfig{
    			UserPoolId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdpResourceServers } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_idp/cognito_idp_resource_servers.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_resource_servers" });

      data(CognitoIdpResourceServers, "example", {
        userPoolId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpResourceServers, CognitoIdpResourceServersConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_resource_servers")

        ubx.data(CognitoIdpResourceServers, "example", CognitoIdpResourceServersConfig(
            user_pool_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_servers" type="list(object)">
  The real Amazon Cognito resource servers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
