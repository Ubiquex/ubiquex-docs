---
title: "aws_cognito_idp_devices"
description: "`aws_cognito_idp_devices` reads the real devices remembered for a Cognito user pool user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_devices"})

    		ubx.Data(cognito_idp.CognitoIdpDevices, "example", cognito_idp.CognitoIdpDevicesConfig{
    			AccessToken: "example",
    			Limit:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpDevices } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_devices" });

      data(CognitoIdpDevices, "example", {
        accessToken: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpDevices, CognitoIdpDevicesConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_devices")

        ubx.data(CognitoIdpDevices, "example", CognitoIdpDevicesConfig(
            access_token="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="devices" type="list(object)">
  The real devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pagination_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
