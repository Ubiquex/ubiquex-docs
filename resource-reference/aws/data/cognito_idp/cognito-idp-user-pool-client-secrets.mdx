---
title: "aws_cognito_idp_user_pool_client_secrets"
description: "`data_aws_cognito_idp_user_pool_client_secrets` looks up a real, already-existing `aws_cognito_user_pool_client`."
---

It registers a real, specific application, a web app, a mobile app, a backend service, against an `aws_cognito_user_pool`, with its own allowed OAuth flows, callback URLs, and token settings, the actual identity a caller authenticates as when it talks to the user pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_pool_client_secrets"})

    		ubx.Data(cognito_idp.CognitoIdpUserPoolClientSecrets, "example", cognito_idp.CognitoIdpUserPoolClientSecretsConfig{
    			ClientId:   "example",
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserPoolClientSecrets } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_pool_client_secrets";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_pool_client_secrets" });

      data(CognitoIdpUserPoolClientSecrets, "example", {
        clientId: "example",
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserPoolClientSecrets, CognitoIdpUserPoolClientSecretsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_pool_client_secrets")

        ubx.data(CognitoIdpUserPoolClientSecrets, "example", CognitoIdpUserPoolClientSecretsConfig(
            client_id="example",
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_id" type="string" required>
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_secrets" type="list(object)">
  &lt;p&gt;Contains metadata about a client secret for list operations. This type excludes the actual secret value for security reasons.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
