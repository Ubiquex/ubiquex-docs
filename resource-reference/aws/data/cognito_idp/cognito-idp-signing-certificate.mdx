---
title: "aws_cognito_idp_signing_certificate"
description: "`aws_cognito_idp_signing_certificate` reads the real SAML signing certificate for a Cognito user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_signing_certificate"})

    		ubx.Data(cognito_idp.CognitoIdpSigningCertificate, "example", cognito_idp.CognitoIdpSigningCertificateConfig{
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdpSigningCertificate } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_idp/cognito_idp_signing_certificate.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_signing_certificate" });

      data(CognitoIdpSigningCertificate, "example", {
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpSigningCertificate, CognitoIdpSigningCertificateConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_signing_certificate")

        ubx.data(CognitoIdpSigningCertificate, "example", CognitoIdpSigningCertificateConfig(
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate" type="string">
  The real certificate this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
