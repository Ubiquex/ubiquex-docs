---
title: "aws_cognito_idp_signing_certificate"
description: "`aws_cognito_idp_signing_certificate` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_signing_certificate` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_signing_certificate"})

    		ubx.Data(cognito_idp.CognitoIdpSigningCertificate, "example", cognito_idp.CognitoIdpSigningCertificateConfig{
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpSigningCertificate } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_signing_certificate";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_signing_certificate" });

      data(CognitoIdpSigningCertificate, "example", {
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpSigningCertificate, CognitoIdpSigningCertificateConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_signing_certificate")

        ubx.data(CognitoIdpSigningCertificate, "example", CognitoIdpSigningCertificateConfig(
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_pool_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate" type="string">
  Returned by the lookup.
</ResponseField>
