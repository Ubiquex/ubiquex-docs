---
title: "aws_cognito_idp_user_import_jobs"
description: "`aws_cognito_idp_user_import_jobs` looks up a real Cognito user pool CSV bulk-import job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_import_jobs"})

    		ubx.Data(cognito_idp.CognitoIdpUserImportJobs, "example", cognito_idp.CognitoIdpUserImportJobsConfig{
    			MaxResults: 1,
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserImportJobs } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_import_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_import_jobs" });

      data(CognitoIdpUserImportJobs, "example", {
        maxResults: 1,
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserImportJobs, CognitoIdpUserImportJobsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_import_jobs")

        ubx.data(CognitoIdpUserImportJobs, "example", CognitoIdpUserImportJobsConfig(
            max_results=1,
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pagination_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_import_jobs" type="list(object)">
  The real Amazon Cognito user import jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
