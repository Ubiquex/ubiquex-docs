---
title: "aws_cognito_idp_user_pool_clients"
description: "`data_aws_cognito_idp_user_pool_clients` looks up a real, already-existing `aws_cognito_user_pool_client`."
---

It registers a real, specific application, a web app, a mobile app, a backend service, against an `aws_cognito_user_pool`, with its own allowed OAuth flows, callback URLs, and token settings, the actual identity a caller authenticates as when it talks to the user pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_pool_clients"})

    		ubx.Data(cognito_idp.CognitoIdpUserPoolClients, "example", cognito_idp.CognitoIdpUserPoolClientsConfig{
    			UserPoolId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserPoolClients } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_pool_clients";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_pool_clients" });

      data(CognitoIdpUserPoolClients, "example", {
        userPoolId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserPoolClients, CognitoIdpUserPoolClientsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_pool_clients")

        ubx.data(CognitoIdpUserPoolClients, "example", CognitoIdpUserPoolClientsConfig(
            user_pool_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_pool_clients" type="list(object)">
  The real Amazon Cognito user pool clients included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
