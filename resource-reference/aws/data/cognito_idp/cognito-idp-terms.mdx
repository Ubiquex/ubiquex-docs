---
title: "aws_cognito_idp_terms"
description: "`aws_cognito_idp_terms` reads the real terms and conditions document configured for a Cognito user pool's own managed login."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_terms"})

    		ubx.Data(cognito_idp.CognitoIdpTerms, "example", cognito_idp.CognitoIdpTermsConfig{
    			UserPoolId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpTerms } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_terms";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_terms" });

      data(CognitoIdpTerms, "example", {
        userPoolId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpTerms, CognitoIdpTermsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_terms")

        ubx.data(CognitoIdpTerms, "example", CognitoIdpTermsConfig(
            user_pool_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="terms" type="list(object)">
  The real terms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
