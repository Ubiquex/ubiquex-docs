---
title: "aws_cognito_idp_web_authn_credentials"
description: "`aws_cognito_idp_web_authn_credentials` reads the real WebAuthn (passkey) credentials registered for a Cognito user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_web_authn_credentials"})

    		ubx.Data(cognito_idp.CognitoIdpWebAuthnCredentials, "example", cognito_idp.CognitoIdpWebAuthnCredentialsConfig{
    			AccessToken: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpWebAuthnCredentials } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_web_authn_credentials";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_web_authn_credentials" });

      data(CognitoIdpWebAuthnCredentials, "example", {
        accessToken: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpWebAuthnCredentials, CognitoIdpWebAuthnCredentialsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_web_authn_credentials")

        ubx.data(CognitoIdpWebAuthnCredentials, "example", CognitoIdpWebAuthnCredentialsConfig(
            access_token="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credentials" type="list(object)">
  The real, temporary security credentials issued for this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
