---
title: "aws_cognito_idp_user_attribute_verification_code"
description: "`aws_cognito_idp_user_attribute_verification_code` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_user_attribute_verification_code` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_attribute_verification_code"})

    		ubx.Data(cognito_idp.CognitoIdpUserAttributeVerificationCode, "example", cognito_idp.CognitoIdpUserAttributeVerificationCodeConfig{
    			AccessToken:    "example",
    			AttributeName:  "example-attribute-name",
    			ClientMetadata: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserAttributeVerificationCode } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_attribute_verification_code";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cognito_idp_user_attribute_verification_code",
      });

      data(CognitoIdpUserAttributeVerificationCode, "example", {
        accessToken: "example",
        attributeName: "example-attribute-name",
        clientMetadata: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserAttributeVerificationCode, CognitoIdpUserAttributeVerificationCodeConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_attribute_verification_code")

        ubx.data(CognitoIdpUserAttributeVerificationCode, "example", CognitoIdpUserAttributeVerificationCodeConfig(
            access_token="example",
            attribute_name="example-attribute-name",
            client_metadata={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="attribute_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="client_metadata" type="map(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="code_delivery_details" type="object">
  &lt;p&gt;The delivery details for an email or SMS message that Amazon Cognito sent for authentication or verification.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="delivery_medium" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
