---
title: "aws_cognito_idp_user_pool_mfa_config"
description: "`aws_cognito_idp_user_pool_mfa_config` reads the real MFA configuration (SMS, TOTP, or required) of a Cognito user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_pool_mfa_config"})

    		ubx.Data(cognito_idp.CognitoIdpUserPoolMfaConfig, "example", cognito_idp.CognitoIdpUserPoolMfaConfigConfig{
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserPoolMfaConfig } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_pool_mfa_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_pool_mfa_config" });

      data(CognitoIdpUserPoolMfaConfig, "example", {
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserPoolMfaConfig, CognitoIdpUserPoolMfaConfigConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_pool_mfa_config")

        ubx.data(CognitoIdpUserPoolMfaConfig, "example", CognitoIdpUserPoolMfaConfigConfig(
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="email_mfa_configuration" type="object">
  &lt;p&gt;Sets or shows configuration for user pool email message MFA and sign-in with one-time passwords (OTPs). Includes the subject and body of the email message template for sign-in and MFA messages. To activate this setting, your user pool must be in the &lt;a href="https://docs.aws.amazon.com/cognito/latest/developerguide/feature-plans-features-essentials.html"&gt; Essentials tier&lt;/a&gt; or higher.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="subject" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mfa_configuration" type="string">
  The real configuration of the mfa. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sms_mfa_configuration" type="object">
  &lt;p&gt;The configuration of multi-factor authentication (MFA) with SMS messages in a user pool.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="sms_authentication_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sms_configuration" type="object">
      &lt;p&gt;User pool configuration for delivery of SMS messages with Amazon Simple Notification Service. To send SMS messages with Amazon SNS in the Amazon Web Services Region that you want, the Amazon Cognito user pool uses an Identity and Access Management (IAM) role in your Amazon Web Services account.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="eums_sms" type="object">
          &lt;p&gt;The configuration that Amazon Cognito uses to send SMS messages through Amazon Web Services End User Messaging SMS. Provide this structure in the &lt;code&gt;EumsSms&lt;/code&gt; member of &lt;code&gt;SmsConfigurationType&lt;/code&gt; to use Amazon Web Services End User Messaging SMS instead of Amazon SNS.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="caller_arn" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="configuration_set_name" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="external_id" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="in_entity_id" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="in_template_id" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="origination_identity" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="region" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="sns_caller_arn" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="sns_region" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="software_token_mfa_configuration" type="object">
  &lt;p&gt;Settings for time-based one-time password (TOTP) multi-factor authentication (MFA) in a user pool. Enables and disables availability of this feature.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_authn_configuration" type="object">
  &lt;p&gt;Settings for authentication (MFA) with passkey, or webauthN, biometric and security-key devices in a user pool. Configures the following:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;Configuration for requiring user-verification support in passkeys.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;The user pool relying-party ID. This is the domain, typically your user pool domain, that user's passkey providers should trust as a receiver of passkey authentication.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;The providers that you want to allow as origins for passkey authentication.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="factor_configuration" type="string">
      &lt;p&gt;The configuration of passkey authentication as a single factor or a multi-factor authentication (MFA) method. When set to &lt;code&gt;MULTI_FACTOR_WITH_USER_VERIFICATION&lt;/code&gt;, your user pool requires passkey authenticators to perform &lt;a href="https://www.w3.org/TR/webauthn-2/#user-verification"&gt;user verification&lt;/a&gt;, for example a biometric or PIN. User verification combined with the passkey constitutes multi-factor authentication. When set to &lt;code&gt;SINGLE_FACTOR&lt;/code&gt;, passkeys are a single authentication factor.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="relying_party_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_verification" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
