---
title: "aws_cognito_idp_user"
description: "`aws_cognito_idp_user` looks up one real Cognito user pool user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user"})

    		ubx.Data(cognito_idp.CognitoIdpUser, "example", cognito_idp.CognitoIdpUserConfig{
    			AccessToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdpUser } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_idp/cognito_idp_user.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user" });

      data(CognitoIdpUser, "example", {
        accessToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUser, CognitoIdpUserConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user")

        ubx.data(CognitoIdpUser, "example", CognitoIdpUserConfig(
            access_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  The real, opaque access token used to authenticate this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mfaoptions" type="list(object)">
  The real MFA options configured for this identity. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="preferred_mfa_setting" type="string">
  The real, preferred MFA method configured for this user. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_attributes" type="list(object)">
  The real, structured attributes of this user. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_mfasetting_list" type="list(string)">
  The real user mfasettings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="username" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
