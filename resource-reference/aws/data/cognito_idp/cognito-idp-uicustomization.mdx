---
title: "aws_cognito_idp_uicustomization"
description: "`aws_cognito_idp_uicustomization` reads the real, legacy hosted UI CSS and logo customization of a Cognito user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_uicustomization"})

    		ubx.Data(cognito_idp.CognitoIdpUicustomization, "example", cognito_idp.CognitoIdpUicustomizationConfig{
    			UserPoolId: "example",
    			ClientId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUicustomization } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_uicustomization";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_uicustomization" });

      data(CognitoIdpUicustomization, "example", {
        userPoolId: "example",
        clientId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUicustomization, CognitoIdpUicustomizationConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_uicustomization")

        ubx.data(CognitoIdpUicustomization, "example", CognitoIdpUicustomizationConfig(
            user_pool_id="example",
            client_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_id" type="string">
  The real, unique identifier of the client this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="uicustomization" type="object">
  &lt;p&gt;A container for the UI customization information for the hosted UI in a user pool.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="css" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cssversion" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_pool_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
