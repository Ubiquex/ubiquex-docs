---
title: "aws_cognito_idp_groups"
description: "`aws_cognito_idp_groups` looks up a real Cognito user pool group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_groups"})

    		ubx.Data(cognito_idp.CognitoIdpGroups, "example", cognito_idp.CognitoIdpGroupsConfig{
    			UserPoolId: "example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpGroups } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_groups" });

      data(CognitoIdpGroups, "example", {
        userPoolId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpGroups, CognitoIdpGroupsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_groups")

        ubx.data(CognitoIdpGroups, "example", CognitoIdpGroupsConfig(
            user_pool_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
