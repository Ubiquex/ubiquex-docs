---
title: "aws_cognito_idp_device"
description: "`aws_cognito_idp_device` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_device` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_device"})

    		ubx.Data(cognito_idp.CognitoIdpDevice, "example", cognito_idp.CognitoIdpDeviceConfig{
    			DeviceKey:   "example",
    			AccessToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpDevice } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_device";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_device" });

      data(CognitoIdpDevice, "example", {
        deviceKey: "example",
        accessToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpDevice, CognitoIdpDeviceConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_device")

        ubx.data(CognitoIdpDevice, "example", CognitoIdpDeviceConfig(
            device_key="example",
            access_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="device_key" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="device" type="object">
  &lt;p&gt;Information about a user's device that they've registered for device SRP authentication in your application. For more information, see &lt;a href="https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-device-tracking.html"&gt;Working with user devices in your user pool&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="device_attributes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_create_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_last_authenticated_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="device_last_modified_date" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
