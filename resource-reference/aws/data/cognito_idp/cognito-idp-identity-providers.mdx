---
title: "aws_cognito_idp_identity_providers"
description: "`aws_cognito_idp_identity_providers` reads the real, external identity providers (SAML, OIDC, social) federated into a Cognito user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_identity_providers"})

    		ubx.Data(cognito_idp.CognitoIdpIdentityProviders, "example", cognito_idp.CognitoIdpIdentityProvidersConfig{
    			UserPoolId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdpIdentityProviders } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_idp/cognito_idp_identity_providers.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_identity_providers" });

      data(CognitoIdpIdentityProviders, "example", {
        userPoolId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpIdentityProviders, CognitoIdpIdentityProvidersConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_identity_providers")

        ubx.data(CognitoIdpIdentityProviders, "example", CognitoIdpIdentityProvidersConfig(
            user_pool_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="providers" type="list(object)">
  The real providers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
