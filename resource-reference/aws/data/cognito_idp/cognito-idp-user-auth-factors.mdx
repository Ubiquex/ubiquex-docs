---
title: "aws_cognito_idp_user_auth_factors"
description: "`aws_cognito_idp_user_auth_factors` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_user_auth_factors` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_auth_factors"})

    		ubx.Data(cognito_idp.CognitoIdpUserAuthFactors, "example", cognito_idp.CognitoIdpUserAuthFactorsConfig{
    			AccessToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserAuthFactors } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_auth_factors";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_auth_factors" });

      data(CognitoIdpUserAuthFactors, "example", {
        accessToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserAuthFactors, CognitoIdpUserAuthFactorsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_auth_factors")

        ubx.data(CognitoIdpUserAuthFactors, "example", CognitoIdpUserAuthFactorsConfig(
            access_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="access_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configured_user_auth_factors" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="preferred_mfa_setting" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_mfasetting_list" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="username" type="string">
  Returned by the lookup.
</ResponseField>
