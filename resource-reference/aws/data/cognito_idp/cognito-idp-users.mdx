---
title: "aws_cognito_idp_users"
description: "`aws_cognito_idp_users` reads the real users registered in a Cognito user pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_users"})

    		ubx.Data(cognito_idp.CognitoIdpUsers, "example", cognito_idp.CognitoIdpUsersConfig{
    			UserPoolId:      "example",
    			AttributesToGet: []string{"example"},
    			Filter:          "example",
    			Limit:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUsers } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_users" });

      data(CognitoIdpUsers, "example", {
        userPoolId: "example",
        attributesToGet: ["example"],
        filter: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUsers, CognitoIdpUsersConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_users")

        ubx.data(CognitoIdpUsers, "example", CognitoIdpUsersConfig(
            user_pool_id="example",
            attributes_to_get=["example"],
            filter="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes_to_get" type="list(string)">
  Restricts the response to only these attributes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pagination_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
