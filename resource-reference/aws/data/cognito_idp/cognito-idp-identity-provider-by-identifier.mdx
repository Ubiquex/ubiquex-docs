---
title: "aws_cognito_idp_identity_provider_by_identifier"
description: "`aws_cognito_idp_identity_provider_by_identifier` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_identity_provider_by_identifier` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_identity_provider_by_identifier"})

    		ubx.Data(cognito_idp.CognitoIdpIdentityProviderByIdentifier, "example", cognito_idp.CognitoIdpIdentityProviderByIdentifierConfig{
    			IdpIdentifier: "example",
    			UserPoolId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpIdentityProviderByIdentifier } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_identity_provider_by_identifier";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cognito_idp_identity_provider_by_identifier",
      });

      data(CognitoIdpIdentityProviderByIdentifier, "example", {
        idpIdentifier: "example",
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpIdentityProviderByIdentifier, CognitoIdpIdentityProviderByIdentifierConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_identity_provider_by_identifier")

        ubx.data(CognitoIdpIdentityProviderByIdentifier, "example", CognitoIdpIdentityProviderByIdentifierConfig(
            idp_identifier="example",
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="idp_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="identity_provider" type="object">
  &lt;p&gt;A user pool identity provider (IdP). Contains information about a third-party IdP to a user pool, the attributes that it populates to user profiles, and the trust relationship between the IdP and your user pool.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attribute_mapping" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="idp_identifiers" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider_details" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provider_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_pool_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
