---
title: "aws_cognito_idp_csvheader"
description: "`aws_cognito_idp_csvheader` reads the real column headers required for a Cognito user pool CSV bulk-import file."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_csvheader"})

    		ubx.Data(cognito_idp.CognitoIdpCsvheader, "example", cognito_idp.CognitoIdpCsvheaderConfig{
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpCsvheader } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_csvheader";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_csvheader" });

      data(CognitoIdpCsvheader, "example", {
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpCsvheader, CognitoIdpCsvheaderConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_csvheader")

        ubx.data(CognitoIdpCsvheader, "example", CognitoIdpCsvheaderConfig(
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="csvheader" type="list(string)">
  The real column headers of this CSV data. **(AI-inferred)** Returned by the lookup.
</ResponseField>
