---
title: "aws_cognito_idp_users_in_group"
description: "`aws_cognito_idp_users_in_group` reads the real users belonging to a Cognito user pool group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_users_in_group"})

    		ubx.Data(cognito_idp.CognitoIdpUsersInGroup, "example", cognito_idp.CognitoIdpUsersInGroupConfig{
    			GroupName:  "example-group-name",
    			UserPoolId: "example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUsersInGroup } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_users_in_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_users_in_group" });

      data(CognitoIdpUsersInGroup, "example", {
        groupName: "example-group-name",
        userPoolId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUsersInGroup, CognitoIdpUsersInGroupConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_users_in_group")

        ubx.data(CognitoIdpUsersInGroup, "example", CognitoIdpUsersInGroupConfig(
            group_name="example-group-name",
            user_pool_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_name" type="string" required>
  The real name of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito user pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
