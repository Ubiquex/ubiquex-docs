---
title: "aws_cognito_idp_user_pools"
description: "`aws_cognito_idp_user_pools` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_user_pools` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_user_pools"})

    		ubx.Data(cognito_idp.CognitoIdpUserPools, "example", cognito_idp.CognitoIdpUserPoolsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpUserPools } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_user_pools";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_user_pools" });

      data(CognitoIdpUserPools, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpUserPools, CognitoIdpUserPoolsConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_user_pools")

        ubx.data(CognitoIdpUserPools, "example", CognitoIdpUserPoolsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_pools" type="list(object)">
  Returned by the lookup.
</ResponseField>
