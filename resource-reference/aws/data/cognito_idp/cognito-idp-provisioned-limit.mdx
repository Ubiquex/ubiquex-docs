---
title: "aws_cognito_idp_provisioned_limit"
description: "`aws_cognito_idp_provisioned_limit` reads the real, currently provisioned rate limit for a Cognito user pool operation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_provisioned_limit"})

    		ubx.Data(cognito_idp.CognitoIdpProvisionedLimit, "example", cognito_idp.CognitoIdpProvisionedLimitConfig{
    			LimitDefinition: map[string]any{"attributes": map[string]string{"key": "value"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdpProvisionedLimit } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_idp/cognito_idp_provisioned_limit.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_provisioned_limit" });

      data(CognitoIdpProvisionedLimit, "example", {
        limitDefinition: { attributes: { key: "value" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpProvisionedLimit, CognitoIdpProvisionedLimitConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_provisioned_limit")

        ubx.data(CognitoIdpProvisionedLimit, "example", CognitoIdpProvisionedLimitConfig(
            limit_definition={"attributes": {"key": "value"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit_definition" type="object" required>
  &lt;p&gt;The class and attributes that identify a specific limit at the account level.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="attributes" type="map(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="limit_class" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="limit" type="object">
  &lt;p&gt;The limit definition and current limit values for a provisioned limit.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="free_limit_value" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="limit_definition" type="object" required>
      &lt;p&gt;The class and attributes that identify a specific limit at the account level.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="attributes" type="map(string)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="limit_class" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="provisioned_limit_value" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
