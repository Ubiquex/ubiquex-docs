---
title: "aws_cognito_idp_tokens_from_refresh_token"
description: "`aws_cognito_idp_tokens_from_refresh_token` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_idp_tokens_from_refresh_token` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_idp "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_idp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_idp_tokens_from_refresh_token"})

    		ubx.Data(cognito_idp.CognitoIdpTokensFromRefreshToken, "example", cognito_idp.CognitoIdpTokensFromRefreshTokenConfig{
    			ClientId:       "example",
    			RefreshToken:   "example",
    			ClientMetadata: map[string]string{"managed-by": "ubx"},
    			ClientSecret:   "example",
    			DeviceKey:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdpTokensFromRefreshToken } from "@ubx/sdk-aws/aws/data/cognito_idp/cognito_idp_tokens_from_refresh_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_idp_tokens_from_refresh_token" });

      data(CognitoIdpTokensFromRefreshToken, "example", {
        clientId: "example",
        refreshToken: "example",
        clientMetadata: { "managed-by": "ubx" },
        clientSecret: "example",
        deviceKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_idp import CognitoIdpTokensFromRefreshToken, CognitoIdpTokensFromRefreshTokenConfig

    def describe():
        ubx.intent("look up aws_cognito_idp_tokens_from_refresh_token")

        ubx.data(CognitoIdpTokensFromRefreshToken, "example", CognitoIdpTokensFromRefreshTokenConfig(
            client_id="example",
            refresh_token="example",
            client_metadata={"managed-by": "ubx"},
            client_secret="example",
            device_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="client_metadata" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="client_secret" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="device_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="refresh_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authentication_result" type="object">
  &lt;p&gt;The object that your application receives after authentication. Contains tokens and information for device authentication.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expires_in" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="new_device_metadata" type="object">
      &lt;p&gt;Information that your user pool responds with in &lt;code&gt;AuthenticationResult&lt;/code&gt;when you configure it to remember devices and a user signs in with an unrecognized device. Amazon Cognito presents a new device key that you can use to set up &lt;a href="https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-device-tracking.html"&gt;device authentication&lt;/a&gt; in a "Remember me on this device" authentication model.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="device_group_key" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="device_key" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="refresh_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="token_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
