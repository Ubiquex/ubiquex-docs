---
title: "aws_cloudhsm_hapgs"
description: "`aws_cloudhsm_hapgs` looks up a real CloudHSM Classic high-availability partition group (HAPG)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudhsm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudhsm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudhsm_hapgs"})

    		ubx.Data(cloudhsm.Hapgs, "example", cloudhsm.HapgsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Hapgs } from "@ubx/sdk-aws/aws/data/cloudhsm/hapgs";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudhsm_hapgs" });

      data(Hapgs, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudhsm import Hapgs, HapgsConfig

    def describe():
        ubx.intent("look up aws_cloudhsm_hapgs")

        ubx.data(Hapgs, "example", HapgsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="hapg_list" type="list(string)">
  The real hapgs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
