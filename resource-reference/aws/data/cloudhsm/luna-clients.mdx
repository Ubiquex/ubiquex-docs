---
title: "aws_cloudhsm_luna_clients"
description: "`aws_cloudhsm_luna_clients` looks up a real CloudHSM Classic Luna client, an application registered to connect to an HSM."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudhsm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudhsm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudhsm_luna_clients"})

    		ubx.Data(cloudhsm.LunaClients, "example", cloudhsm.LunaClientsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LunaClients } from "@ubx/sdk-aws/aws/data/cloudhsm/luna_clients";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudhsm_luna_clients" });

      data(LunaClients, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudhsm import LunaClients, LunaClientsConfig

    def describe():
        ubx.intent("look up aws_cloudhsm_luna_clients")

        ubx.data(LunaClients, "example", LunaClientsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="client_list" type="list(string)">
  The real clients included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
