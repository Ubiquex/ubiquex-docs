---
title: "aws_cloudhsm_hsms"
description: "`aws_cloudhsm_hsms` looks up a real CloudHSM Classic hardware security module."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudhsm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudhsm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudhsm_hsms"})

    		ubx.Data(cloudhsm.Hsms, "example", cloudhsm.HsmsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Hsms } from "@ubx/sdk-aws/aws/data/cloudhsm/hsms";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudhsm_hsms" });

      data(Hsms, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudhsm import Hsms, HsmsConfig

    def describe():
        ubx.intent("look up aws_cloudhsm_hsms")

        ubx.data(Hsms, "example", HsmsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="hsm_list" type="list(string)">
  &lt;p&gt;Contains a list of ARNs that identify the HSMs.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
