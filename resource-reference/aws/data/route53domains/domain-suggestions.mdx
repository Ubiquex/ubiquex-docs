---
title: "aws_route53domains_domain_suggestions"
description: "`aws_route53domains_domain_suggestions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53domains_domain_suggestions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53domains "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53domains"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53domains_domain_suggestions"})

    		ubx.Data(route53domains.DomainSuggestions, "example", route53domains.DomainSuggestionsConfig{
    			DomainName:      "example-domain-name",
    			OnlyAvailable:   true,
    			SuggestionCount: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainSuggestions } from "@ubx/sdk-aws/aws/data/route53domains/domain_suggestions";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53domains_domain_suggestions" });

      data(DomainSuggestions, "example", {
        domainName: "example-domain-name",
        onlyAvailable: true,
        suggestionCount: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53domains import DomainSuggestions, DomainSuggestionsConfig

    def describe():
        ubx.intent("look up aws_route53domains_domain_suggestions")

        ubx.data(DomainSuggestions, "example", DomainSuggestionsConfig(
            domain_name="example-domain-name",
            only_available=True,
            suggestion_count=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="only_available" type="bool" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="suggestion_count" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="suggestions_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
