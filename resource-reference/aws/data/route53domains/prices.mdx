---
title: "aws_route53domains_prices"
description: "`aws_route53domains_prices` reads the real registration, renewal, and transfer prices for a domain TLD through Route 53 Domains."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53domains "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53domains"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53domains_prices"})

    		ubx.Data(route53domains.Prices, "example", route53domains.PricesConfig{
    			Marker:   "example",
    			MaxItems: 1,
    			Tld:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Prices } from "@ubx/sdk-aws/aws/data/route53domains/prices";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53domains_prices" });

      data(Prices, "example", {
        marker: "example",
        maxItems: 1,
        tld: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53domains import Prices, PricesConfig

    def describe():
        ubx.intent("look up aws_route53domains_prices")

        ubx.data(Prices, "example", PricesConfig(
            marker="example",
            max_items=1,
            tld="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tld" type="string">
  The real top-level domain of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="prices" type="list(object)">
  The real prices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
