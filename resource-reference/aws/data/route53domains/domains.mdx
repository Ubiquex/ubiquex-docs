---
title: "aws_route53domains_domains"
description: "`aws_route53domains_domains` looks up a real domain registered through Route 53 Domains."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53domains "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53domains"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53domains_domains"})

    		ubx.Data(route53domains.Domains, "example", route53domains.DomainsConfig{
    			FilterConditions: []map[string]any{{"name": "example-name"}},
    			Marker:           "example",
    			MaxItems:         1,
    			SortCondition:    map[string]any{"name": "example-name", "sort_order": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Domains } from "@ubx/sdk-aws/aws/data/route53domains/domains";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53domains_domains" });

      data(Domains, "example", {
        filterConditions: [{ name: "example-name" }],
        marker: "example",
        maxItems: 1,
        sortCondition: { name: "example-name", sortOrder: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53domains import Domains, DomainsConfig
    from ubx.aws.data.route53domains.domains import Domains_FilterConditions, Domains_SortCondition

    def describe():
        ubx.intent("look up aws_route53domains_domains")

        ubx.data(Domains, "example", DomainsConfig(
            filter_conditions=[Domains_FilterConditions(name="example-name")],
            marker="example",
            max_items=1,
            sort_condition=Domains_SortCondition(name="example-name", sort_order="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_conditions" type="list(object)">
  The real conditions applied to filter this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_condition" type="object">
  &lt;p&gt;Information for sorting a list of domains.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="sort_order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="domains" type="list(object)">
  The real domains included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
