---
title: "aws_ssm_patch_group_state"
description: "`aws_ssm_patch_group_state` reads the real, current summary patch state of a Patch Manager patch group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_group_state"})

    		ubx.Data(ssm.PatchGroupState, "example", ssm.PatchGroupStateConfig{
    			PatchGroup: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatchGroupState } from "@ubx/sdk-aws/aws/data/ssm/patch_group_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_group_state" });

      data(PatchGroupState, "example", {
        patchGroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchGroupState, PatchGroupStateConfig

    def describe():
        ubx.intent("look up aws_ssm_patch_group_state")

        ubx.data(PatchGroupState, "example", PatchGroupStateConfig(
            patch_group="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="patch_group" type="string" required>
  The real patch group. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="number">
  The real instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_available_security_updates" type="number">
  The real number of instances with available security updates. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_critical_non_compliant_patches" type="number">
  The real number of instances with critical, non-compliant patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_failed_patches" type="number">
  The real number of instances with failed patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_installed_other_patches" type="number">
  The real number of instances with other installed patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_installed_patches" type="number">
  The real number of instances with installed patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_installed_pending_reboot_patches" type="number">
  The real number of instances with installed patches pending reboot. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_installed_rejected_patches" type="number">
  The real number of instances with installed, rejected patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_missing_patches" type="number">
  The real number of instances with missing patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_not_applicable_patches" type="number">
  The real number of instances with patches not applicable to them. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_other_non_compliant_patches" type="number">
  The real number of instances with other non-compliant patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_security_non_compliant_patches" type="number">
  The real number of instances with non-compliant security patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instances_with_unreported_not_applicable_patches" type="number">
  The real number of instances with unreported, not-applicable patches. **(AI-inferred)** Returned by the lookup.
</ResponseField>
