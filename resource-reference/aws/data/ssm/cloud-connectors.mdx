---
title: "aws_ssm_cloud_connectors"
description: "`data_aws_ssm_cloud_connectors` looks up a real, already-existing `aws_ssm_cloud_connector`."
---

It connects a real non-AWS cloud account, an Azure subscription, for example, to AWS Systems Manager, letting Systems Manager extend its own management and visibility to resources outside AWS. This resource has only thin official documentation, verified live this session against AWS's own field-level schema rather than assumed from general knowledge, since it did not turn up a dedicated conceptual guide page the way Systems Manager's older resources do.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_cloud_connectors"})

    		ubx.Data(ssm.CloudConnectors, "example", ssm.CloudConnectorsConfig{
    			Filters:    []map[string]any{{"filter_key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudConnectors } from "@ubx/sdk-aws/aws/data/ssm/cloud_connectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_cloud_connectors" });

      data(CloudConnectors, "example", {
        filters: [{ filterKey: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import CloudConnectors, CloudConnectorsConfig
    from ubx.aws.data.ssm.cloud_connectors import CloudConnectors_Filters

    def describe():
        ubx.intent("look up aws_ssm_cloud_connectors")

        ubx.data(CloudConnectors, "example", CloudConnectorsConfig(
            filters=[CloudConnectors_Filters(filter_key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_connectors" type="list(object)">
  The real cloud connectors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
