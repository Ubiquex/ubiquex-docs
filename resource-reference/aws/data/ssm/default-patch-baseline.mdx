---
title: "aws_ssm_default_patch_baseline"
description: "`aws_ssm_default_patch_baseline` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_default_patch_baseline` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_default_patch_baseline"})

    		ubx.Data(ssm.DefaultPatchBaseline, "example", ssm.DefaultPatchBaselineConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultPatchBaseline } from "@ubx/sdk-aws/aws/data/ssm/default_patch_baseline";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_default_patch_baseline" });

      data(DefaultPatchBaseline, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import DefaultPatchBaseline, DefaultPatchBaselineConfig

    def describe():
        ubx.intent("look up aws_ssm_default_patch_baseline")

        ubx.data(DefaultPatchBaseline, "example", DefaultPatchBaselineConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="baseline_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="operating_system" type="string">
  Returned by the lookup.
</ResponseField>
