---
title: "aws_ssm_compliance_items"
description: "`aws_ssm_compliance_items` reads a real compliance item (such as a missing patch) reported for a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_compliance_items"})

    		ubx.Data(ssm.ComplianceItems, "example", ssm.ComplianceItemsConfig{
    			Filters:       []map[string]any{{"key": "example"}},
    			MaxResults:    1,
    			ResourceIds:   []string{"example"},
    			ResourceTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceItems } from "@ubx/sdk-aws/aws/data/ssm/compliance_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_compliance_items" });

      data(ComplianceItems, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
        resourceIds: ["example"],
        resourceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import ComplianceItems, ComplianceItemsConfig

    def describe():
        ubx.intent("look up aws_ssm_compliance_items")

        ubx.data(ComplianceItems, "example", ComplianceItemsConfig(
            filters=[{"key": "example"}],
            max_results=1,
            resource_ids=["example"],
            resource_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_ids" type="list(string)">
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance_items" type="list(object)">
  The real Systems Manager compliance items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
