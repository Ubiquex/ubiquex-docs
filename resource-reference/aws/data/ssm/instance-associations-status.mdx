---
title: "aws_ssm_instance_associations_status"
description: "`aws_ssm_instance_associations_status` reads the real, current status of a State Manager association applied to a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_instance_associations_status"})

    		ubx.Data(ssm.InstanceAssociationsStatus, "example", ssm.InstanceAssociationsStatusConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceAssociationsStatus } from "@ubx/sdk-aws/aws/data/ssm/instance_associations_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_instance_associations_status" });

      data(InstanceAssociationsStatus, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InstanceAssociationsStatus, InstanceAssociationsStatusConfig

    def describe():
        ubx.intent("look up aws_ssm_instance_associations_status")

        ubx.data(InstanceAssociationsStatus, "example", InstanceAssociationsStatusConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_association_status_infos" type="list(object)">
  The real, detailed status information for this instance's own Systems Manager associations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
