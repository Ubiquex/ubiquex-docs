---
title: "aws_ssm_deployable_patch_snapshot_for_instance"
description: "`aws_ssm_deployable_patch_snapshot_for_instance` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_deployable_patch_snapshot_for_instance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_deployable_patch_snapshot_for_instance"})

    		ubx.Data(ssm.DeployablePatchSnapshotForInstance, "example", ssm.DeployablePatchSnapshotForInstanceConfig{
    			InstanceId:             "example",
    			SnapshotId:             "example",
    			BaselineOverride:       map[string]any{"approval_rules": map[string]any{"patch_rules": []map[string]any{{"approve_after_days": 1}}}},
    			UseS3DualStackEndpoint: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeployablePatchSnapshotForInstance } from "@ubx/sdk-aws/aws/data/ssm/deployable_patch_snapshot_for_instance";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_deployable_patch_snapshot_for_instance" });

      data(DeployablePatchSnapshotForInstance, "example", {
        instanceId: "example",
        snapshotId: "example",
        baselineOverride: {
          approvalRules: { patchRules: [{ approveAfterDays: 1 }] },
        },
        useS3DualStackEndpoint: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import DeployablePatchSnapshotForInstance, DeployablePatchSnapshotForInstanceConfig

    def describe():
        ubx.intent("look up aws_ssm_deployable_patch_snapshot_for_instance")

        ubx.data(DeployablePatchSnapshotForInstance, "example", DeployablePatchSnapshotForInstanceConfig(
            instance_id="example",
            snapshot_id="example",
            baseline_override={"approval_rules": {"patch_rules": [{"approve_after_days": 1}]}},
            use_s3_dual_stack_endpoint=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="baseline_override" type="object">
  &lt;p&gt;Defines the basic information about a patch baseline override.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="approval_rules" type="object">
      &lt;p&gt;A set of rules defining the approval rules for a patch baseline.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="patch_rules" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="approved_patches" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="approved_patches_compliance_level" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="approved_patches_enable_non_security" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="available_security_updates_compliance_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="global_filters" type="object">
      &lt;p&gt;A set of patch filters, typically used for approval rules.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="patch_filters" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="operating_system" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="rejected_patches" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="rejected_patches_action" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sources" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="use_s3_dual_stack_endpoint" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="product" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot_download_url" type="string">
  Returned by the lookup.
</ResponseField>
