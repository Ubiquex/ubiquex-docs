---
title: "aws_ssm_parameters"
description: "`data_aws_ssm_parameters` looks up a real, already-existing `aws_ssm_parameter`."
---

It stores a real string, string list, or secure string value in Parameter Store, that applications and other AWS resources reference by name at runtime instead of hardcoding configuration or (for SecureString) secrets directly.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_parameters"})

    		ubx.Data(ssm.Parameters, "example", ssm.ParametersConfig{
    			Filters:          []map[string]any{{"key": "example"}},
    			MaxResults:       1,
    			ParameterFilters: []map[string]any{{"key": "example"}},
    			Shared:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Parameters } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/parameters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_parameters" });

      data(Parameters, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
        parameterFilters: [{ key: "example" }],
        shared: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import Parameters, ParametersConfig

    def describe():
        ubx.intent("look up aws_ssm_parameters")

        ubx.data(Parameters, "example", ParametersConfig(
            filters=[{"key": "example"}],
            max_results=1,
            parameter_filters=[{"key": "example"}],
            shared=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parameter_filters" type="list(object)">
  The real filters applied to the parameters in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shared" type="bool">
  Whether this object is shared. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
