---
title: "aws_ssm_maintenance_window_schedule"
description: "`data_aws_ssm_maintenance_window_schedule` looks up a real, already-existing `aws_ssm_maintenance_window`."
---

It defines a real, recurring time window during which `aws_ssm_maintenance_window_task`s are allowed to run against `aws_ssm_maintenance_window_target`s, so disruptive operations like patching only happen when they're actually scheduled to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_maintenance_window_schedule"})

    		ubx.Data(ssm.MaintenanceWindowSchedule, "example", ssm.MaintenanceWindowScheduleConfig{
    			Filters:      []map[string]any{{"key": "example"}},
    			MaxResults:   1,
    			ResourceType: "example",
    			Targets:      []map[string]any{{"key": "example"}},
    			WindowId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaintenanceWindowSchedule } from "@ubx/sdk-aws/aws/data/ssm/maintenance_window_schedule";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_maintenance_window_schedule" });

      data(MaintenanceWindowSchedule, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
        resourceType: "example",
        targets: [{ key: "example" }],
        windowId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import MaintenanceWindowSchedule, MaintenanceWindowScheduleConfig

    def describe():
        ubx.intent("look up aws_ssm_maintenance_window_schedule")

        ubx.data(MaintenanceWindowSchedule, "example", MaintenanceWindowScheduleConfig(
            filters=[{"key": "example"}],
            max_results=1,
            resource_type="example",
            targets=[{"key": "example"}],
            window_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="targets" type="list(object)">
  The real targets included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="window_id" type="string">
  The real, unique identifier of the maintenance window this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_window_executions" type="list(object)">
  The real, scheduled Systems Manager maintenance window executions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
