---
title: "aws_ssm_calendar_state"
description: "`aws_ssm_calendar_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_calendar_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_calendar_state"})

    		ubx.Data(ssm.CalendarState, "example", ssm.CalendarStateConfig{
    			CalendarNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CalendarState } from "@ubx/sdk-aws/aws/data/ssm/calendar_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_calendar_state" });

      data(CalendarState, "example", {
        calendarNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import CalendarState, CalendarStateConfig

    def describe():
        ubx.intent("look up aws_ssm_calendar_state")

        ubx.data(CalendarState, "example", CalendarStateConfig(
            calendar_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="calendar_names" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="at_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_transition_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>
