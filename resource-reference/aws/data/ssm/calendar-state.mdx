---
title: "aws_ssm_calendar_state"
description: "`aws_ssm_calendar_state` reads the real, current open/closed state of a Systems Manager Change Calendar."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_calendar_state"})

    		ubx.Data(ssm.CalendarState, "example", ssm.CalendarStateConfig{
    			CalendarNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CalendarState } from "@ubx/sdk-aws/aws/data/ssm/calendar_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_calendar_state" });

      data(CalendarState, "example", {
        calendarNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import CalendarState, CalendarStateConfig

    def describe():
        ubx.intent("look up aws_ssm_calendar_state")

        ubx.data(CalendarState, "example", CalendarStateConfig(
            calendar_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="calendar_names" type="list(string)" required>
  The real names of the calendars included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="at_time" type="string">
  The real timestamp of the at. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_transition_time" type="string">
  The real timestamp of the next transition. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
