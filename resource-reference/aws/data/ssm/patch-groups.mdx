---
title: "aws_ssm_patch_groups"
description: "`aws_ssm_patch_groups` reads the real patch groups a Patch Manager baseline is registered against."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_groups"})

    		ubx.Data(ssm.PatchGroups, "example", ssm.PatchGroupsConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatchGroups } from "@ubx/sdk-aws/aws/data/ssm/patch_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_groups" });

      data(PatchGroups, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchGroups, PatchGroupsConfig
    from ubx.aws.data.ssm.patch_groups import PatchGroups_Filters

    def describe():
        ubx.intent("look up aws_ssm_patch_groups")

        ubx.data(PatchGroups, "example", PatchGroupsConfig(
            filters=[PatchGroups_Filters(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mappings" type="list(object)">
  The real mappings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
