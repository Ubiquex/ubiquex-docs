---
title: "aws_ssm_ops_item_related_items"
description: "`data_aws_ssm_ops_item_related_items` looks up a real, already-existing `aws_ssm_ops_item`."
---

It creates a real, trackable operational issue, an incident, change request, or general to-do, in OpsCenter, with its own status, priority, and related resources, for centralizing operational work instead of tracking it in a separate ticketing system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_ops_item_related_items"})

    		ubx.Data(ssm.OpsItemRelatedItems, "example", ssm.OpsItemRelatedItemsConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    			OpsItemId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpsItemRelatedItems } from "@ubx/sdk-aws/aws/data/ssm/ops_item_related_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_ops_item_related_items" });

      data(OpsItemRelatedItems, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
        opsItemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import OpsItemRelatedItems, OpsItemRelatedItemsConfig

    def describe():
        ubx.intent("look up aws_ssm_ops_item_related_items")

        ubx.data(OpsItemRelatedItems, "example", OpsItemRelatedItemsConfig(
            filters=[{"key": "example"}],
            max_results=1,
            ops_item_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ops_item_id" type="string">
  The real, unique identifier of the ops item this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  The real, summarized objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
