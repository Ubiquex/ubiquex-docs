---
title: "aws_ssm_ops_item_related_items"
description: "`aws_ssm_ops_item_related_items` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_ops_item_related_items` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_ops_item_related_items"})

    		ubx.Data(ssm.OpsItemRelatedItems, "example", ssm.OpsItemRelatedItemsConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    			OpsItemId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpsItemRelatedItems } from "@ubx/sdk-aws/aws/data/ssm/ops_item_related_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_ops_item_related_items" });

      data(OpsItemRelatedItems, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
        opsItemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import OpsItemRelatedItems, OpsItemRelatedItemsConfig

    def describe():
        ubx.intent("look up aws_ssm_ops_item_related_items")

        ubx.data(OpsItemRelatedItems, "example", OpsItemRelatedItemsConfig(
            filters=[{"key": "example"}],
            max_results=1,
            ops_item_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ops_item_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
