---
title: "aws_ssm_effective_instance_associations"
description: "`aws_ssm_effective_instance_associations` reads the real State Manager associations currently effective on a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_effective_instance_associations"})

    		ubx.Data(ssm.EffectiveInstanceAssociations, "example", ssm.EffectiveInstanceAssociationsConfig{
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EffectiveInstanceAssociations } from "@ubx/sdk-aws/aws/data/ssm/effective_instance_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_effective_instance_associations" });

      data(EffectiveInstanceAssociations, "example", {
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import EffectiveInstanceAssociations, EffectiveInstanceAssociationsConfig

    def describe():
        ubx.intent("look up aws_ssm_effective_instance_associations")

        ubx.data(EffectiveInstanceAssociations, "example", EffectiveInstanceAssociationsConfig(
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  The real associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
