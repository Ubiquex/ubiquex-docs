---
title: "aws_ssm_resource_compliance_summaries"
description: "`aws_ssm_resource_compliance_summaries` reads a real, aggregated summary of Systems Manager compliance status across resource types."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_resource_compliance_summaries"})

    		ubx.Data(ssm.ResourceComplianceSummaries, "example", ssm.ResourceComplianceSummariesConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceComplianceSummaries } from "@ubx/sdk-aws/aws/data/ssm/resource_compliance_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_resource_compliance_summaries" });

      data(ResourceComplianceSummaries, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import ResourceComplianceSummaries, ResourceComplianceSummariesConfig

    def describe():
        ubx.intent("look up aws_ssm_resource_compliance_summaries")

        ubx.data(ResourceComplianceSummaries, "example", ResourceComplianceSummariesConfig(
            filters=[{"key": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_compliance_summary_items" type="list(object)">
  The real, summarized resource compliance results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
