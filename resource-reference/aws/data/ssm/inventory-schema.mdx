---
title: "aws_ssm_inventory_schema"
description: "`aws_ssm_inventory_schema` reads the real schema of an inventory type collected by Systems Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_inventory_schema"})

    		ubx.Data(ssm.InventorySchema, "example", ssm.InventorySchemaConfig{
    			Aggregator: true,
    			MaxResults: 1,
    			SubType:    true,
    			TypeName:   "example-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InventorySchema } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/inventory_schema.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_inventory_schema" });

      data(InventorySchema, "example", {
        aggregator: true,
        maxResults: 1,
        subType: true,
        typeName: "example-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InventorySchema, InventorySchemaConfig

    def describe():
        ubx.intent("look up aws_ssm_inventory_schema")

        ubx.data(InventorySchema, "example", InventorySchemaConfig(
            aggregator=True,
            max_results=1,
            sub_type=True,
            type_name="example-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregator" type="bool">
  The real AWS Config aggregator this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sub_type" type="bool">
  The real, discriminating type of the sub this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string">
  The real name of this object's own type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schemas" type="list(object)">
  The real schemas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
