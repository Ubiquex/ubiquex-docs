---
title: "aws_ssm_inventory_schema"
description: "`aws_ssm_inventory_schema` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_inventory_schema` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_inventory_schema"})

    		ubx.Data(ssm.InventorySchema, "example", ssm.InventorySchemaConfig{
    			Aggregator: true,
    			MaxResults: 1,
    			SubType:    true,
    			TypeName:   "example-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InventorySchema } from "@ubx/sdk-aws/aws/data/ssm/inventory_schema";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_inventory_schema" });

      data(InventorySchema, "example", {
        aggregator: true,
        maxResults: 1,
        subType: true,
        typeName: "example-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InventorySchema, InventorySchemaConfig

    def describe():
        ubx.intent("look up aws_ssm_inventory_schema")

        ubx.data(InventorySchema, "example", InventorySchemaConfig(
            aggregator=True,
            max_results=1,
            sub_type=True,
            type_name="example-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregator" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sub_type" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="schemas" type="list(object)">
  Returned by the lookup.
</ResponseField>
