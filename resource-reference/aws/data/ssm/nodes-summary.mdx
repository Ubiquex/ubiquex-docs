---
title: "aws_ssm_nodes_summary"
description: "`aws_ssm_nodes_summary` reads a real, aggregated summary of nodes tracked by Systems Manager's own Fleet Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_nodes_summary"})

    		ubx.Data(ssm.NodesSummary, "example", ssm.NodesSummaryConfig{
    			Aggregators: []map[string]any{{"aggregator_type": "example"}},
    			Filters:     []map[string]any{{"key": "example"}},
    			MaxResults:  1,
    			SyncName:    "example-sync-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { NodesSummary } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/nodes_summary.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_nodes_summary" });

      data(NodesSummary, "example", {
        aggregators: [{ aggregatorType: "example" }],
        filters: [{ key: "example" }],
        maxResults: 1,
        syncName: "example-sync-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import NodesSummary, NodesSummaryConfig

    def describe():
        ubx.intent("look up aws_ssm_nodes_summary")

        ubx.data(NodesSummary, "example", NodesSummaryConfig(
            aggregators=[{"aggregator_type": "example"}],
            filters=[{"key": "example"}],
            max_results=1,
            sync_name="example-sync-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregators" type="list(object)" required>
  The real AWS Config aggregators included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sync_name" type="string">
  The real name of the sync this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary" type="list(map(string))">
  The real, summarized content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
