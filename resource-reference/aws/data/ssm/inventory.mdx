---
title: "aws_ssm_inventory"
description: "`aws_ssm_inventory` reads real, collected software and configuration inventory data for a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_inventory"})

    		ubx.Data(ssm.Inventory, "example", ssm.InventoryConfig{
    			Aggregators:      []map[string]any{{"aggregators": []map[string]any{{}}}},
    			Filters:          []map[string]any{{"key": "example"}},
    			MaxResults:       1,
    			ResultAttributes: []map[string]any{{"type_name": "example-type-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Inventory } from "@ubx/sdk-aws/aws/data/ssm/inventory";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_inventory" });

      data(Inventory, "example", {
        aggregators: [{ aggregators: [{}] }],
        filters: [{ key: "example" }],
        maxResults: 1,
        resultAttributes: [{ typeName: "example-type-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import Inventory, InventoryConfig
    from ubx.aws.data.ssm.inventory import Inventory_Aggregators, Inventory_Aggregators_Aggregators, Inventory_Aggregators_Groups_Filters, Inventory_ResultAttributes

    def describe():
        ubx.intent("look up aws_ssm_inventory")

        ubx.data(Inventory, "example", InventoryConfig(
            aggregators=[Inventory_Aggregators(aggregators=[Inventory_Aggregators_Aggregators()])],
            filters=[Inventory_Aggregators_Groups_Filters(key="example")],
            max_results=1,
            result_attributes=[Inventory_ResultAttributes(type_name="example-type-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregators" type="list(object)">
  The real AWS Config aggregators included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="result_attributes" type="list(object)">
  The real attributes included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entities" type="list(object)">
  The real entities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
