---
title: "aws_ssm_inventory_deletions"
description: "`aws_ssm_inventory_deletions` reads the real, current status of a request to delete collected inventory data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_inventory_deletions"})

    		ubx.Data(ssm.InventoryDeletions, "example", ssm.InventoryDeletionsConfig{
    			DeletionId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InventoryDeletions } from "@ubx/sdk-aws/aws/data/ssm/inventory_deletions";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_inventory_deletions" });

      data(InventoryDeletions, "example", {
        deletionId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InventoryDeletions, InventoryDeletionsConfig

    def describe():
        ubx.intent("look up aws_ssm_inventory_deletions")

        ubx.data(InventoryDeletions, "example", InventoryDeletionsConfig(
            deletion_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deletion_id" type="string">
  The real, unique identifier of the deletion this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="inventory_deletions" type="list(object)">
  The real Systems Manager inventory deletions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
