---
title: "aws_ssm_service_setting"
description: "`aws_ssm_service_setting` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_service_setting` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_service_setting"})

    		ubx.Data(ssm.ServiceSetting, "example", ssm.ServiceSettingConfig{
    			SettingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceSetting } from "@ubx/sdk-aws/aws/data/ssm/service_setting";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_service_setting" });

      data(ServiceSetting, "example", {
        settingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import ServiceSetting, ServiceSettingConfig

    def describe():
        ubx.intent("look up aws_ssm_service_setting")

        ubx.data(ServiceSetting, "example", ServiceSettingConfig(
            setting_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="setting_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="service_setting" type="object">
  &lt;p&gt;The service setting data structure.&lt;/p&gt; &lt;p&gt; &lt;code&gt;ServiceSetting&lt;/code&gt; is an account-level setting for an Amazon Web Services service. This setting defines how a user interacts with or uses a service or a feature of a service. For example, if an Amazon Web Services service charges money to the account based on feature or service usage, then the Amazon Web Services service team might create a default setting of "false". This means the user can't use this feature unless they change the setting to "true" and intentionally opt in for a paid feature.&lt;/p&gt; &lt;p&gt;Services map a &lt;code&gt;SettingId&lt;/code&gt; object to a setting value. Amazon Web Services services teams define the default value for a &lt;code&gt;SettingId&lt;/code&gt;. You can't create a new &lt;code&gt;SettingId&lt;/code&gt;, but you can overwrite the default value if you have the &lt;code&gt;ssm:UpdateServiceSetting&lt;/code&gt; permission for the setting. Use the &lt;a&gt;UpdateServiceSetting&lt;/a&gt; API operation to change the default setting. Or, use the &lt;a&gt;ResetServiceSetting&lt;/a&gt; to change the value back to the original value defined by the Amazon Web Services service team.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_user" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="setting_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="setting_value" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
