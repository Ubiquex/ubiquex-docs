---
title: "aws_ssm_instance_patch_states_for_patch_group"
description: "`aws_ssm_instance_patch_states_for_patch_group` reads the real patch compliance state of every managed node in a Patch Manager patch group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_instance_patch_states_for_patch_group"})

    		ubx.Data(ssm.InstancePatchStatesForPatchGroup, "example", ssm.InstancePatchStatesForPatchGroupConfig{
    			PatchGroup: "example",
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstancePatchStatesForPatchGroup } from "@ubx/sdk-aws/aws/data/ssm/instance_patch_states_for_patch_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_instance_patch_states_for_patch_group" });

      data(InstancePatchStatesForPatchGroup, "example", {
        patchGroup: "example",
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InstancePatchStatesForPatchGroup, InstancePatchStatesForPatchGroupConfig

    def describe():
        ubx.intent("look up aws_ssm_instance_patch_states_for_patch_group")

        ubx.data(InstancePatchStatesForPatchGroup, "example", InstancePatchStatesForPatchGroupConfig(
            patch_group="example",
            filters=[{"key": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="patch_group" type="string" required>
  The real patch group. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_patch_states" type="list(object)">
  The real, current states of the instance patchs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
