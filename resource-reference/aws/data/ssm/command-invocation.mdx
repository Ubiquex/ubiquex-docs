---
title: "aws_ssm_command_invocation"
description: "`aws_ssm_command_invocation` looks up one real, per-instance invocation of a Systems Manager Run Command."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_command_invocation"})

    		ubx.Data(ssm.CommandInvocation, "example", ssm.CommandInvocationConfig{
    			CommandId:  "example",
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommandInvocation } from "@ubx/sdk-aws/aws/data/ssm/command_invocation";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_command_invocation" });

      data(CommandInvocation, "example", {
        commandId: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import CommandInvocation, CommandInvocationConfig

    def describe():
        ubx.intent("look up aws_ssm_command_invocation")

        ubx.data(CommandInvocation, "example", CommandInvocationConfig(
            command_id="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="command_id" type="string" required>
  The real, unique identifier of the command this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_watch_output_config" type="object">
  &lt;p&gt;Configuration options for sending command output to Amazon CloudWatch Logs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cloud_watch_log_group_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cloud_watch_output_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="comment" type="string">
  The real, free-text comment on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document_name" type="string">
  The real name of the document this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="document_version" type="string">
  The real version of the document. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="execution_elapsed_time" type="string">
  The real timestamp of the execution elapsed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="execution_end_date_time" type="string">
  The real timestamp of the execution end date. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="execution_start_date_time" type="string">
  The real timestamp of the execution start date. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="plugin_name" type="string">
  The real name of the plugin this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="response_code" type="number">
  The real code identifying the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="standard_error_content" type="string">
  The real content of the standard error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="standard_error_url" type="string">
  The real, resolvable URL of the standard error this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="standard_output_content" type="string">
  The real content of the standard output. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="standard_output_url" type="string">
  The real, resolvable URL of the standard output this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_details" type="string">
  The real, detailed information about the status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
