---
title: "aws_ssm_command_invocations"
description: "`aws_ssm_command_invocations` reads the real, per-instance invocations of a Systems Manager Run Command."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_command_invocations"})

    		ubx.Data(ssm.CommandInvocations, "example", ssm.CommandInvocationsConfig{
    			CommandId:  "example",
    			Details:    true,
    			Filters:    []map[string]any{{"key": "example"}},
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommandInvocations } from "@ubx/sdk-aws/aws/data/ssm/command_invocations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_command_invocations" });

      data(CommandInvocations, "example", {
        commandId: "example",
        details: true,
        filters: [{ key: "example" }],
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import CommandInvocations, CommandInvocationsConfig

    def describe():
        ubx.intent("look up aws_ssm_command_invocations")

        ubx.data(CommandInvocations, "example", CommandInvocationsConfig(
            command_id="example",
            details=True,
            filters=[{"key": "example"}],
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="command_id" type="string">
  The real, unique identifier of the command this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="details" type="bool">
  The real, structured details of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="command_invocations" type="list(object)">
  The real Systems Manager command invocations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
