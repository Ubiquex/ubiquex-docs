---
title: "aws_ssm_ops_metadata"
description: "`data_aws_ssm_ops_metadata` looks up a real, already-existing `aws_ssm_ops_item`."
---

It creates a real, trackable operational issue, an incident, change request, or general to-do, in OpsCenter, with its own status, priority, and related resources, for centralizing operational work instead of tracking it in a separate ticketing system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_ops_metadata"})

    		ubx.Data(ssm.OpsMetadata, "example", ssm.OpsMetadataConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OpsMetadata } from "@ubx/sdk-aws/aws/data/ssm/ops_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_ops_metadata" });

      data(OpsMetadata, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import OpsMetadata, OpsMetadataConfig
    from ubx.aws.data.ssm.ops_metadata import OpsMetadata_Filters

    def describe():
        ubx.intent("look up aws_ssm_ops_metadata")

        ubx.data(OpsMetadata, "example", OpsMetadataConfig(
            filters=[OpsMetadata_Filters(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ops_metadata_list" type="list(object)">
  The real ops metadatas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
