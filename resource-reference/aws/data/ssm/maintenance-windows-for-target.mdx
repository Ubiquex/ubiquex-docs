---
title: "aws_ssm_maintenance_windows_for_target"
description: "`data_aws_ssm_maintenance_windows_for_target` looks up a real, already-existing `aws_ssm_maintenance_window_target`."
---

It registers a real set of instances or resources, selected by tag or ID, as eligible targets for an `aws_ssm_maintenance_window`'s tasks.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_maintenance_windows_for_target"})

    		ubx.Data(ssm.MaintenanceWindowsForTarget, "example", ssm.MaintenanceWindowsForTargetConfig{
    			ResourceType: "example",
    			Targets:      []map[string]any{{"key": "example"}},
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaintenanceWindowsForTarget } from "@ubx/sdk-aws/aws/data/ssm/maintenance_windows_for_target";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_maintenance_windows_for_target" });

      data(MaintenanceWindowsForTarget, "example", {
        resourceType: "example",
        targets: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import MaintenanceWindowsForTarget, MaintenanceWindowsForTargetConfig
    from ubx.aws.data.ssm.maintenance_windows_for_target import MaintenanceWindowsForTarget_Targets

    def describe():
        ubx.intent("look up aws_ssm_maintenance_windows_for_target")

        ubx.data(MaintenanceWindowsForTarget, "example", MaintenanceWindowsForTargetConfig(
            resource_type="example",
            targets=[MaintenanceWindowsForTarget_Targets(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="targets" type="list(object)" required>
  The real targets included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="window_identities" type="list(object)">
  The real identities associated with this maintenance window. **(AI-inferred)** Returned by the lookup.
</ResponseField>
