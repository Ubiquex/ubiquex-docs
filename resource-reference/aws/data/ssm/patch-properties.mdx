---
title: "aws_ssm_patch_properties"
description: "`aws_ssm_patch_properties` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_patch_properties` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_properties"})

    		ubx.Data(ssm.PatchProperties, "example", ssm.PatchPropertiesConfig{
    			OperatingSystem: "example",
    			Property:        "example",
    			MaxResults:      1,
    			PatchSet:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatchProperties } from "@ubx/sdk-aws/aws/data/ssm/patch_properties";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_properties" });

      data(PatchProperties, "example", {
        operatingSystem: "example",
        property: "example",
        maxResults: 1,
        patchSet: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchProperties, PatchPropertiesConfig

    def describe():
        ubx.intent("look up aws_ssm_patch_properties")

        ubx.data(PatchProperties, "example", PatchPropertiesConfig(
            operating_system="example",
            property="example",
            max_results=1,
            patch_set="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="patch_set" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="property" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="list(map(string))">
  Returned by the lookup.
</ResponseField>
