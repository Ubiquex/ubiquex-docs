---
title: "aws_ssm_patch_properties"
description: "`aws_ssm_patch_properties` reads the real, available patch properties (such as classification or severity) for a given operating system."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_properties"})

    		ubx.Data(ssm.PatchProperties, "example", ssm.PatchPropertiesConfig{
    			OperatingSystem: "example",
    			Property:        "example",
    			MaxResults:      1,
    			PatchSet:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatchProperties } from "@ubx/sdk-aws/aws/data/ssm/patch_properties";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_properties" });

      data(PatchProperties, "example", {
        operatingSystem: "example",
        property: "example",
        maxResults: 1,
        patchSet: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchProperties, PatchPropertiesConfig

    def describe():
        ubx.intent("look up aws_ssm_patch_properties")

        ubx.data(PatchProperties, "example", PatchPropertiesConfig(
            operating_system="example",
            property="example",
            max_results=1,
            patch_set="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  The real operating system of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="patch_set" type="string">
  The real patch set. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="property" type="string" required>
  The real property this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="properties" type="list(map(string))">
  The real, structured properties of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
