---
title: "aws_ssm_patch_baseline_for_patch_group"
description: "`data_aws_ssm_patch_baseline_for_patch_group` looks up a real, already-existing `aws_ssm_patch_baseline`."
---

It defines a real set of rules, auto-approval delays, severity filters, an explicit approved or rejected patch list, that determines which OS patches Systems Manager Patch Manager treats as approved for a given set of instances.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_baseline_for_patch_group"})

    		ubx.Data(ssm.PatchBaselineForPatchGroup, "example", ssm.PatchBaselineForPatchGroupConfig{
    			PatchGroup: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatchBaselineForPatchGroup } from "@ubx/sdk-aws/aws/data/ssm/patch_baseline_for_patch_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_baseline_for_patch_group" });

      data(PatchBaselineForPatchGroup, "example", {
        patchGroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchBaselineForPatchGroup, PatchBaselineForPatchGroupConfig

    def describe():
        ubx.intent("look up aws_ssm_patch_baseline_for_patch_group")

        ubx.data(PatchBaselineForPatchGroup, "example", PatchBaselineForPatchGroupConfig(
            patch_group="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="patch_group" type="string" required>
  The real patch group. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="baseline_id" type="string">
  The real, unique identifier of the baseline this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="operating_system" type="string">
  The real operating system of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
