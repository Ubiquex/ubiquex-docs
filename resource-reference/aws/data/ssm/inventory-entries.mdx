---
title: "aws_ssm_inventory_entries"
description: "`aws_ssm_inventory_entries` reads the real, individual inventory entries of a given type collected from a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_inventory_entries"})

    		ubx.Data(ssm.InventoryEntries, "example", ssm.InventoryEntriesConfig{
    			InstanceId: "example",
    			TypeName:   "example-type-name",
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InventoryEntries } from "@ubx/sdk-aws/aws/data/ssm/inventory_entries";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_inventory_entries" });

      data(InventoryEntries, "example", {
        instanceId: "example",
        typeName: "example-type-name",
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InventoryEntries, InventoryEntriesConfig
    from ubx.aws.data.ssm.inventory_entries import InventoryEntries_Filters

    def describe():
        ubx.intent("look up aws_ssm_inventory_entries")

        ubx.data(InventoryEntries, "example", InventoryEntriesConfig(
            instance_id="example",
            type_name="example-type-name",
            filters=[InventoryEntries_Filters(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type_name" type="string" required>
  The real name of this object's own type. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capture_time" type="string">
  The real timestamp of the capture. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="entries" type="list(map(string))">
  The real entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_version" type="string">
  The real version of the schema. **(AI-inferred)** Returned by the lookup.
</ResponseField>
