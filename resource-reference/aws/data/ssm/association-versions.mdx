---
title: "aws_ssm_association_versions"
description: "`data_aws_ssm_association_versions` looks up a real, already-existing `aws_ssm_association`."
---

It attaches a real `aws_ssm_document` to a set of target instances or resources on a recurring schedule, the resource that actually turns a document's defined actions into ongoing, applied state rather than a one-time run.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_association_versions"})

    		ubx.Data(ssm.AssociationVersions, "example", ssm.AssociationVersionsConfig{
    			AssociationId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AssociationVersions } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/association_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_association_versions" });

      data(AssociationVersions, "example", {
        associationId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import AssociationVersions, AssociationVersionsConfig

    def describe():
        ubx.intent("look up aws_ssm_association_versions")

        ubx.data(AssociationVersions, "example", AssociationVersionsConfig(
            association_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_id" type="string" required>
  The real, unique identifier of the association this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="association_versions" type="list(object)">
  The real versions of the association included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
