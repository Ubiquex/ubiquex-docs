---
title: "aws_ssm_instance_properties"
description: "`aws_ssm_instance_properties` reads real, detailed properties of a Systems Manager managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_instance_properties"})

    		ubx.Data(ssm.InstanceProperties, "example", ssm.InstancePropertiesConfig{
    			FiltersWithOperator:        []map[string]any{{"key": "example"}},
    			InstancePropertyFilterList: []map[string]any{{"key": "example"}},
    			MaxResults:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceProperties } from "@ubx/sdk-aws/aws/data/ssm/instance_properties";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_instance_properties" });

      data(InstanceProperties, "example", {
        filtersWithOperator: [{ key: "example" }],
        instancePropertyFilterList: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InstanceProperties, InstancePropertiesConfig
    from ubx.aws.data.ssm.instance_properties import InstanceProperties_FiltersWithOperator, InstanceProperties_InstancePropertyFilterList

    def describe():
        ubx.intent("look up aws_ssm_instance_properties")

        ubx.data(InstanceProperties, "example", InstancePropertiesConfig(
            filters_with_operator=[InstanceProperties_FiltersWithOperator(key="example")],
            instance_property_filter_list=[InstanceProperties_InstancePropertyFilterList(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters_with_operator" type="list(object)">
  The real filters, each paired with its own comparison operator. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_property_filter_list" type="list(object)">
  The real instance property filters included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_properties" type="list(object)">
  The real, structured properties of this instance. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
