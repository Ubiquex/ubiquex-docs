---
title: "aws_ssm_maintenance_window_targets"
description: "`aws_ssm_maintenance_window_targets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_maintenance_window_targets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_maintenance_window_targets"})

    		ubx.Data(ssm.MaintenanceWindowTargets, "example", ssm.MaintenanceWindowTargetsConfig{
    			WindowId:   "example",
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaintenanceWindowTargets } from "@ubx/sdk-aws/aws/data/ssm/maintenance_window_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_maintenance_window_targets" });

      data(MaintenanceWindowTargets, "example", {
        windowId: "example",
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import MaintenanceWindowTargets, MaintenanceWindowTargetsConfig

    def describe():
        ubx.intent("look up aws_ssm_maintenance_window_targets")

        ubx.data(MaintenanceWindowTargets, "example", MaintenanceWindowTargetsConfig(
            window_id="example",
            filters=[{"key": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="window_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="targets" type="list(object)">
  Returned by the lookup.
</ResponseField>
