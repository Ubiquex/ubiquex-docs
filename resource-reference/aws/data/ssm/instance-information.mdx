---
title: "aws_ssm_instance_information"
description: "`aws_ssm_instance_information` reads real, current information (platform, agent version, ping status) about a Systems Manager managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_instance_information"})

    		ubx.Data(ssm.InstanceInformation, "example", ssm.InstanceInformationConfig{
    			Filters:                       []map[string]any{{"key": "example"}},
    			InstanceInformationFilterList: []map[string]any{{"key": "example"}},
    			MaxResults:                    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceInformation } from "@ubx/sdk-aws/aws/data/ssm/instance_information";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_instance_information" });

      data(InstanceInformation, "example", {
        filters: [{ key: "example" }],
        instanceInformationFilterList: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InstanceInformation, InstanceInformationConfig
    from ubx.aws.data.ssm.instance_information import InstanceInformation_Filters, InstanceInformation_InstanceInformationFilterList

    def describe():
        ubx.intent("look up aws_ssm_instance_information")

        ubx.data(InstanceInformation, "example", InstanceInformationConfig(
            filters=[InstanceInformation_Filters(key="example")],
            instance_information_filter_list=[InstanceInformation_InstanceInformationFilterList(key="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_information_filter_list" type="list(object)">
  The real instance information filters included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_information_list" type="list(object)">
  The real instance informations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
