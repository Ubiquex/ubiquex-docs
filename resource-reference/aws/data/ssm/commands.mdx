---
title: "aws_ssm_commands"
description: "`aws_ssm_commands` looks up a real Systems Manager Run Command execution."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_commands"})

    		ubx.Data(ssm.Commands, "example", ssm.CommandsConfig{
    			CommandId:  "example",
    			Filters:    []map[string]any{{"key": "example"}},
    			InstanceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Commands } from "@ubx/sdk-aws/aws/data/ssm/commands";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_commands" });

      data(Commands, "example", {
        commandId: "example",
        filters: [{ key: "example" }],
        instanceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import Commands, CommandsConfig
    from ubx.aws.data.ssm.commands import Commands_Filters

    def describe():
        ubx.intent("look up aws_ssm_commands")

        ubx.data(Commands, "example", CommandsConfig(
            command_id="example",
            filters=[Commands_Filters(key="example")],
            instance_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="command_id" type="string">
  The real, unique identifier of the command this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="commands" type="list(object)">
  The real commands included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
