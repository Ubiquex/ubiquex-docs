---
title: "aws_ssm_ops_items"
description: "`data_aws_ssm_ops_items` looks up a real, already-existing `aws_ssm_ops_item`."
---

It creates a real, trackable operational issue, an incident, change request, or general to-do, in OpsCenter, with its own status, priority, and related resources, for centralizing operational work instead of tracking it in a separate ticketing system.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_ops_items"})

    		ubx.Data(ssm.OpsItems, "example", ssm.OpsItemsConfig{
    			MaxResults:     1,
    			OpsItemFilters: []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OpsItems } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/ops_items.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_ops_items" });

      data(OpsItems, "example", {
        maxResults: 1,
        opsItemFilters: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import OpsItems, OpsItemsConfig

    def describe():
        ubx.intent("look up aws_ssm_ops_items")

        ubx.data(OpsItems, "example", OpsItemsConfig(
            max_results=1,
            ops_item_filters=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ops_item_filters" type="list(object)">
  The real filters applied to Systems Manager OpsItems in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ops_item_summaries" type="list(object)">
  The real, summarized ops items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
