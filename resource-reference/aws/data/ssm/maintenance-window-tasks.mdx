---
title: "aws_ssm_maintenance_window_tasks"
description: "`data_aws_ssm_maintenance_window_tasks` looks up a real, already-existing `aws_ssm_maintenance_window_task`."
---

It schedules a real Systems Manager task, running an `aws_ssm_document`, a Lambda function, or a Step Functions state machine, against an `aws_ssm_maintenance_window`'s registered targets whenever its window opens.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_maintenance_window_tasks"})

    		ubx.Data(ssm.MaintenanceWindowTasks, "example", ssm.MaintenanceWindowTasksConfig{
    			WindowId:   "example",
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaintenanceWindowTasks } from "@ubx/sdk-aws/aws/data/ssm/maintenance_window_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_maintenance_window_tasks" });

      data(MaintenanceWindowTasks, "example", {
        windowId: "example",
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import MaintenanceWindowTasks, MaintenanceWindowTasksConfig

    def describe():
        ubx.intent("look up aws_ssm_maintenance_window_tasks")

        ubx.data(MaintenanceWindowTasks, "example", MaintenanceWindowTasksConfig(
            window_id="example",
            filters=[{"key": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="window_id" type="string" required>
  The real, unique identifier of the maintenance window this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  The real tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
