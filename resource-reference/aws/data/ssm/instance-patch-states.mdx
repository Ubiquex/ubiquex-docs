---
title: "aws_ssm_instance_patch_states"
description: "`aws_ssm_instance_patch_states` reads the real, current patch compliance state of a managed node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_instance_patch_states"})

    		ubx.Data(ssm.InstancePatchStates, "example", ssm.InstancePatchStatesConfig{
    			InstanceIds: []string{"example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstancePatchStates } from "@ubx/sdk-aws/aws/data/ssm/instance_patch_states";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_instance_patch_states" });

      data(InstancePatchStates, "example", {
        instanceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import InstancePatchStates, InstancePatchStatesConfig

    def describe():
        ubx.intent("look up aws_ssm_instance_patch_states")

        ubx.data(InstancePatchStates, "example", InstancePatchStatesConfig(
            instance_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_ids" type="list(string)" required>
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_patch_states" type="list(object)">
  The real, current states of the instance patchs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
