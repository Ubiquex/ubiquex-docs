---
title: "aws_ssm_patch_baselines"
description: "`data_aws_ssm_patch_baselines` looks up a real, already-existing `aws_ssm_patch_baseline`."
---

It defines a real set of rules, auto-approval delays, severity filters, an explicit approved or rejected patch list, that determines which OS patches Systems Manager Patch Manager treats as approved for a given set of instances.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_patch_baselines"})

    		ubx.Data(ssm.PatchBaselines, "example", ssm.PatchBaselinesConfig{
    			Filters:    []map[string]any{{"key": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PatchBaselines } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/patch_baselines.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_patch_baselines" });

      data(PatchBaselines, "example", {
        filters: [{ key: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import PatchBaselines, PatchBaselinesConfig

    def describe():
        ubx.intent("look up aws_ssm_patch_baselines")

        ubx.data(PatchBaselines, "example", PatchBaselinesConfig(
            filters=[{"key": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="baseline_identities" type="list(object)">
  The real identities included in this security baseline. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
