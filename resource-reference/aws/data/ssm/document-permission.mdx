---
title: "aws_ssm_document_permission"
description: "`data_aws_ssm_document_permission` looks up a real, already-existing `aws_ssm_document`."
---

It defines a real, reusable automation, command, or policy document, in JSON or YAML, that Systems Manager runs against target instances, either directly or through an `aws_ssm_association`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_document_permission"})

    		ubx.Data(ssm.DocumentPermission, "example", ssm.DocumentPermissionConfig{
    			Name:           "example",
    			PermissionType: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentPermission } from "@ubx/sdk-aws/aws/data/ssm/document_permission";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_document_permission" });

      data(DocumentPermission, "example", {
        name: "example",
        permissionType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import DocumentPermission, DocumentPermissionConfig

    def describe():
        ubx.intent("look up aws_ssm_document_permission")

        ubx.data(DocumentPermission, "example", DocumentPermissionConfig(
            name="example",
            permission_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="permission_type" type="string" required>
  The real, discriminating type of the permission this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="account_sharing_info_list" type="list(object)">
  &lt;p&gt;A list of Amazon Web Services accounts where the current document is shared and the version shared with each account.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
