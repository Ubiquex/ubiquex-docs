---
title: "aws_ssm_connection_status"
description: "`aws_ssm_connection_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_connection_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_connection_status"})

    		ubx.Data(ssm.ConnectionStatus, "example", ssm.ConnectionStatusConfig{
    			Target: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectionStatus } from "@ubx/sdk-aws/aws/data/ssm/connection_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_connection_status" });

      data(ConnectionStatus, "example", {
        target: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import ConnectionStatus, ConnectionStatusConfig

    def describe():
        ubx.intent("look up aws_ssm_connection_status")

        ubx.data(ConnectionStatus, "example", ConnectionStatusConfig(
            target="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="target" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
