---
title: "aws_ssm_parameters_by_path"
description: "`aws_ssm_parameters_by_path` reads the real Parameter Store parameters under a given hierarchical path."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_parameters_by_path"})

    		ubx.Data(ssm.ParametersByPath, "example", ssm.ParametersByPathConfig{
    			Path:             "/example/",
    			MaxResults:       1,
    			ParameterFilters: []map[string]any{{"key": "example"}},
    			Recursive:        true,
    			WithDecryption:   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ParametersByPath } from "./local-sdk/aws/sdk/typescript/aws/data/ssm/parameters_by_path.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_parameters_by_path" });

      data(ParametersByPath, "example", {
        path: "/example/",
        maxResults: 1,
        parameterFilters: [{ key: "example" }],
        recursive: true,
        withDecryption: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm import ParametersByPath, ParametersByPathConfig

    def describe():
        ubx.intent("look up aws_ssm_parameters_by_path")

        ubx.data(ParametersByPath, "example", ParametersByPathConfig(
            path="/example/",
            max_results=1,
            parameter_filters=[{"key": "example"}],
            recursive=True,
            with_decryption=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parameter_filters" type="list(object)">
  The real filters applied to the parameters in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path" type="string" required>
  The real path of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="recursive" type="bool">
  Whether this action applies recursively. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="with_decryption" type="bool">
  Whether SecureString parameters are decrypted in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
