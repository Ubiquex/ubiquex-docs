---
title: "aws_imagebuilder_container_recipes"
description: "`data_aws_imagebuilder_container_recipes` looks up a real, already-existing `aws_image_builder_container_recipe`."
---

It defines the real base container image and the ordered build and test components applied to it, that an `aws_image_builder_image_pipeline` runs to actually produce a new container image, the container-image counterpart to `aws_image_builder_image_recipe`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_container_recipes"})

    		ubx.Data(imagebuilder.ContainerRecipes, "example", imagebuilder.ContainerRecipesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			Owner:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ContainerRecipes } from "./local-sdk/aws/sdk/typescript/aws/data/imagebuilder/container_recipes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_container_recipes" });

      data(ContainerRecipes, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        owner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import ContainerRecipes, ContainerRecipesConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_container_recipes")

        ubx.data(ContainerRecipes, "example", ContainerRecipesConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  The real owner of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="container_recipe_summary_list" type="list(object)">
  The real container recipe summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
