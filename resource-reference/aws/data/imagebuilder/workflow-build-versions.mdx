---
title: "aws_imagebuilder_workflow_build_versions"
description: "`aws_imagebuilder_workflow_build_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_imagebuilder_workflow_build_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_workflow_build_versions"})

    		ubx.Data(imagebuilder.WorkflowBuildVersions, "example", imagebuilder.WorkflowBuildVersionsConfig{
    			MaxResults:         1,
    			WorkflowVersionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkflowBuildVersions } from "@ubx/sdk-aws/aws/data/imagebuilder/workflow_build_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_workflow_build_versions" });

      data(WorkflowBuildVersions, "example", {
        maxResults: 1,
        workflowVersionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import WorkflowBuildVersions, WorkflowBuildVersionsConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_workflow_build_versions")

        ubx.data(WorkflowBuildVersions, "example", WorkflowBuildVersionsConfig(
            max_results=1,
            workflow_version_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workflow_version_arn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="workflow_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
