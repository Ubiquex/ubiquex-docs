---
title: "aws_imagebuilder_image_policy"
description: "`aws_imagebuilder_image_policy` reads the real resource-based policy attached to an Image Builder image."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_image_policy"})

    		ubx.Data(imagebuilder.ImagePolicy, "example", imagebuilder.ImagePolicyConfig{
    			ImageArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ImagePolicy } from "./local-sdk/aws/sdk/typescript/aws/data/imagebuilder/image_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_image_policy" });

      data(ImagePolicy, "example", {
        imageArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import ImagePolicy, ImagePolicyConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_image_policy")

        ubx.data(ImagePolicy, "example", ImagePolicyConfig(
            image_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the image this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
