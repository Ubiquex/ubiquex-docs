---
title: "aws_imagebuilder_waiting_workflow_steps"
description: "`aws_imagebuilder_waiting_workflow_steps` reads the real, paused Image Builder workflow steps currently awaiting manual approval."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_waiting_workflow_steps"})

    		ubx.Data(imagebuilder.WaitingWorkflowSteps, "example", imagebuilder.WaitingWorkflowStepsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WaitingWorkflowSteps } from "@ubx/sdk-aws/aws/data/imagebuilder/waiting_workflow_steps";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_waiting_workflow_steps" });

      data(WaitingWorkflowSteps, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import WaitingWorkflowSteps, WaitingWorkflowStepsConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_waiting_workflow_steps")

        ubx.data(WaitingWorkflowSteps, "example", WaitingWorkflowStepsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  The real, ordered steps included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
