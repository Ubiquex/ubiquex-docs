---
title: "aws_imagebuilder_image_recipes"
description: "`data_aws_imagebuilder_image_recipes` looks up a real, already-existing `aws_image_builder_image_recipe`."
---

It defines the real base AMI and the ordered build and test components applied to it, that an `aws_image_builder_image_pipeline` runs to actually produce a new, hardened, updated AMI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_image_recipes"})

    		ubx.Data(imagebuilder.ImageRecipes, "example", imagebuilder.ImageRecipesConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			Owner:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageRecipes } from "@ubx/sdk-aws/aws/data/imagebuilder/image_recipes";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_image_recipes" });

      data(ImageRecipes, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        owner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import ImageRecipes, ImageRecipesConfig
    from ubx.aws.data.imagebuilder.image_recipes import ImageRecipes_Filters

    def describe():
        ubx.intent("look up aws_imagebuilder_image_recipes")

        ubx.data(ImageRecipes, "example", ImageRecipesConfig(
            filters=[ImageRecipes_Filters(name="example-name")],
            max_results=1,
            owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  The real owner of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_recipe_summary_list" type="list(object)">
  The real image recipe summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
