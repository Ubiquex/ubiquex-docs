---
title: "aws_imagebuilder_image_pipeline_images"
description: "`aws_imagebuilder_image_pipeline_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_imagebuilder_image_pipeline_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_image_pipeline_images"})

    		ubx.Data(imagebuilder.ImagePipelineImages, "example", imagebuilder.ImagePipelineImagesConfig{
    			ImagePipelineArn: "arn:aws:iam::123456789012:policy/example",
    			Filters:          []map[string]any{{"name": "example-name"}},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImagePipelineImages } from "@ubx/sdk-aws/aws/data/imagebuilder/image_pipeline_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_image_pipeline_images" });

      data(ImagePipelineImages, "example", {
        imagePipelineArn: "arn:aws:iam::123456789012:policy/example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import ImagePipelineImages, ImagePipelineImagesConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_image_pipeline_images")

        ubx.data(ImagePipelineImages, "example", ImagePipelineImagesConfig(
            image_pipeline_arn="arn:aws:iam::123456789012:policy/example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="image_pipeline_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_summary_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>
