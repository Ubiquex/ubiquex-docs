---
title: "aws_imagebuilder_image_scan_finding_aggregations"
description: "`aws_imagebuilder_image_scan_finding_aggregations` reads real, aggregated Inspector vulnerability scan findings across Image Builder images."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_image_scan_finding_aggregations"})

    		ubx.Data(imagebuilder.ImageScanFindingAggregations, "example", imagebuilder.ImageScanFindingAggregationsConfig{
    			Filter: map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageScanFindingAggregations } from "@ubx/sdk-aws/aws/data/imagebuilder/image_scan_finding_aggregations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_imagebuilder_image_scan_finding_aggregations",
      });

      data(ImageScanFindingAggregations, "example", {
        filter: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import ImageScanFindingAggregations, ImageScanFindingAggregationsConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_image_scan_finding_aggregations")

        ubx.data(ImageScanFindingAggregations, "example", ImageScanFindingAggregationsConfig(
            filter={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;A filter name and value pair that is used to return a more specific list of results from a list operation. Filters can be used to match a set of resources by specific criteria, such as tags, attributes, or IDs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="values" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="aggregation_type" type="string">
  The real, discriminating type of the aggregation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="responses" type="list(object)">
  The real responses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
