---
title: "aws_imagebuilder_marketplace_resource"
description: "`aws_imagebuilder_marketplace_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_imagebuilder_marketplace_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_marketplace_resource"})

    		ubx.Data(imagebuilder.MarketplaceResource, "example", imagebuilder.MarketplaceResourceConfig{
    			ResourceArn:      "arn:aws:iam::123456789012:policy/example",
    			ResourceType:     "example",
    			ResourceLocation: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MarketplaceResource } from "@ubx/sdk-aws/aws/data/imagebuilder/marketplace_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_marketplace_resource" });

      data(MarketplaceResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        resourceType: "example",
        resourceLocation: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import MarketplaceResource, MarketplaceResourceConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_marketplace_resource")

        ubx.data(MarketplaceResource, "example", MarketplaceResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            resource_type="example",
            resource_location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_location" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  Returned by the lookup.
</ResponseField>
