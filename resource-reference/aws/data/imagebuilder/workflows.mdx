---
title: "aws_imagebuilder_workflows"
description: "`aws_imagebuilder_workflows` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_imagebuilder_workflows` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_imagebuilder_workflows"})

    		ubx.Data(imagebuilder.Workflows, "example", imagebuilder.WorkflowsConfig{
    			ByName:     true,
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			Owner:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workflows } from "@ubx/sdk-aws/aws/data/imagebuilder/workflows";

    export default stack("example", () => {
      intent({ summary: "look up aws_imagebuilder_workflows" });

      data(Workflows, "example", {
        byName: true,
        filters: [{ name: "example-name" }],
        maxResults: 1,
        owner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.imagebuilder import Workflows, WorkflowsConfig

    def describe():
        ubx.intent("look up aws_imagebuilder_workflows")

        ubx.data(Workflows, "example", WorkflowsConfig(
            by_name=True,
            filters=[{"name": "example-name"}],
            max_results=1,
            owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="by_name" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="workflow_version_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
