---
title: "aws_memory_db_reserved_nodes"
description: "`aws_memory_db_reserved_nodes` looks up a real, purchased MemoryDB Reserved Node."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memory_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/memory_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_memory_db_reserved_nodes"})

    		ubx.Data(memory_db.MemoryDbReservedNodes, "example", memory_db.MemoryDbReservedNodesConfig{
    			Duration:                "example",
    			MaxResults:              1,
    			NodeType:                "example",
    			OfferingType:            "example",
    			ReservationId:           "example",
    			ReservedNodesOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MemoryDbReservedNodes } from "@ubx/sdk-aws/aws/data/memory_db/memory_db_reserved_nodes";

    export default stack("example", () => {
      intent({ summary: "look up aws_memory_db_reserved_nodes" });

      data(MemoryDbReservedNodes, "example", {
        duration: "example",
        maxResults: 1,
        nodeType: "example",
        offeringType: "example",
        reservationId: "example",
        reservedNodesOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.memory_db import MemoryDbReservedNodes, MemoryDbReservedNodesConfig

    def describe():
        ubx.intent("look up aws_memory_db_reserved_nodes")

        ubx.data(MemoryDbReservedNodes, "example", MemoryDbReservedNodesConfig(
            duration="example",
            max_results=1,
            node_type="example",
            offering_type="example",
            reservation_id="example",
            reserved_nodes_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="duration" type="string">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="node_type" type="string">
  The real, discriminating type of the node this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  The real, discriminating type of the offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reservation_id" type="string">
  The real, unique identifier of the reservation this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_nodes_offering_id" type="string">
  The real, unique identifier of the reserved nodes offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_nodes" type="list(object)">
  The real Reserved Nodes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
