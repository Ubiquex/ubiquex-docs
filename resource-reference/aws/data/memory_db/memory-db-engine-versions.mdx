---
title: "aws_memory_db_engine_versions"
description: "`aws_memory_db_engine_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_memory_db_engine_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memory_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/memory_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_memory_db_engine_versions"})

    		ubx.Data(memory_db.MemoryDbEngineVersions, "example", memory_db.MemoryDbEngineVersionsConfig{
    			DefaultOnly:          true,
    			Engine:               "example",
    			EngineVersion:        "example",
    			MaxResults:           1,
    			ParameterGroupFamily: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MemoryDbEngineVersions } from "@ubx/sdk-aws/aws/data/memory_db/memory_db_engine_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_memory_db_engine_versions" });

      data(MemoryDbEngineVersions, "example", {
        defaultOnly: true,
        engine: "example",
        engineVersion: "example",
        maxResults: 1,
        parameterGroupFamily: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.memory_db import MemoryDbEngineVersions, MemoryDbEngineVersionsConfig

    def describe():
        ubx.intent("look up aws_memory_db_engine_versions")

        ubx.data(MemoryDbEngineVersions, "example", MemoryDbEngineVersionsConfig(
            default_only=True,
            engine="example",
            engine_version="example",
            max_results=1,
            parameter_group_family="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="default_only" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="parameter_group_family" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="engine_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
