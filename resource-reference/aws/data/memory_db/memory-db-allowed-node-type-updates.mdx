---
title: "aws_memory_db_allowed_node_type_updates"
description: "`aws_memory_db_allowed_node_type_updates` reads the real node types a MemoryDB cluster is currently allowed to scale to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	memory_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/memory_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_memory_db_allowed_node_type_updates"})

    		ubx.Data(memory_db.MemoryDbAllowedNodeTypeUpdates, "example", memory_db.MemoryDbAllowedNodeTypeUpdatesConfig{
    			ClusterName: "example-cluster-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MemoryDbAllowedNodeTypeUpdates } from "./local-sdk/aws/sdk/typescript/aws/data/memory_db/memory_db_allowed_node_type_updates.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_memory_db_allowed_node_type_updates" });

      data(MemoryDbAllowedNodeTypeUpdates, "example", {
        clusterName: "example-cluster-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.memory_db import MemoryDbAllowedNodeTypeUpdates, MemoryDbAllowedNodeTypeUpdatesConfig

    def describe():
        ubx.intent("look up aws_memory_db_allowed_node_type_updates")

        ubx.data(MemoryDbAllowedNodeTypeUpdates, "example", MemoryDbAllowedNodeTypeUpdatesConfig(
            cluster_name="example-cluster-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="scale_down_node_types" type="list(string)">
  The real, discriminating types of scale down nodes this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scale_up_node_types" type="list(string)">
  The real, discriminating types of scale up nodes this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
