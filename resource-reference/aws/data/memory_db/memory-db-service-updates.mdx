---
title: "aws_memory_db_service_updates"
description: "`aws_memory_db_service_updates` reads the real, available service updates (engine patches) for MemoryDB."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	memory_db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/memory_db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_memory_db_service_updates"})

    		ubx.Data(memory_db.MemoryDbServiceUpdates, "example", memory_db.MemoryDbServiceUpdatesConfig{
    			ClusterNames:      []string{"example"},
    			MaxResults:        1,
    			ServiceUpdateName: "example-service-update-name",
    			Status:            []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MemoryDbServiceUpdates } from "@ubx/sdk-aws/aws/data/memory_db/memory_db_service_updates";

    export default stack("example", () => {
      intent({ summary: "look up aws_memory_db_service_updates" });

      data(MemoryDbServiceUpdates, "example", {
        clusterNames: ["example"],
        maxResults: 1,
        serviceUpdateName: "example-service-update-name",
        status: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.memory_db import MemoryDbServiceUpdates, MemoryDbServiceUpdatesConfig

    def describe():
        ubx.intent("look up aws_memory_db_service_updates")

        ubx.data(MemoryDbServiceUpdates, "example", MemoryDbServiceUpdatesConfig(
            cluster_names=["example"],
            max_results=1,
            service_update_name="example-service-update-name",
            status=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_names" type="list(string)">
  The real names of the clusters included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_name" type="string">
  The real name of the service update this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="list(string)">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_updates" type="list(object)">
  The real service updates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
