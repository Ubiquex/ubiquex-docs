---
title: "aws_ssm_contacts_engagements"
description: "`aws_ssm_contacts_engagements` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_contacts_engagements` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_engagements"})

    		ubx.Data(ssm_contacts.SsmContactsEngagements, "example", ssm_contacts.SsmContactsEngagementsConfig{
    			IncidentId:     "example",
    			MaxResults:     1,
    			TimeRangeValue: map[string]any{"end_time": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsEngagements } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_engagements";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_engagements" });

      data(SsmContactsEngagements, "example", {
        incidentId: "example",
        maxResults: 1,
        timeRangeValue: { endTime: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsEngagements, SsmContactsEngagementsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_engagements")

        ubx.data(SsmContactsEngagements, "example", SsmContactsEngagementsConfig(
            incident_id="example",
            max_results=1,
            time_range_value={"end_time": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time_range_value" type="object">
  &lt;p&gt;A range of between two set times&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="engagements" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
