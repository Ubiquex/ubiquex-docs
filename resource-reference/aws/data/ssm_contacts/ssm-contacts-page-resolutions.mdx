---
title: "aws_ssm_contacts_page_resolutions"
description: "`aws_ssm_contacts_page_resolutions` reads the real, recorded resolution of an Incident Manager page."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_page_resolutions"})

    		ubx.Data(ssm_contacts.SsmContactsPageResolutions, "example", ssm_contacts.SsmContactsPageResolutionsConfig{
    			PageId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsPageResolutions } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_page_resolutions";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_page_resolutions" });

      data(SsmContactsPageResolutions, "example", {
        pageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsPageResolutions, SsmContactsPageResolutionsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_page_resolutions")

        ubx.data(SsmContactsPageResolutions, "example", SsmContactsPageResolutionsConfig(
            page_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_id" type="string" required>
  The real, unique identifier of the page this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="page_resolutions" type="list(object)">
  The real resolutions available for this page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
