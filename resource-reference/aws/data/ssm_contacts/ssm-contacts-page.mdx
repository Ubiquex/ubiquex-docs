---
title: "aws_ssm_contacts_page"
description: "`aws_ssm_contacts_page` looks up one real Incident Manager page, a single notification attempt sent to a contact."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_page"})

    		ubx.Data(ssm_contacts.SsmContactsPage, "example", ssm_contacts.SsmContactsPageConfig{
    			PageId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SsmContactsPage } from "./local-sdk/aws/sdk/typescript/aws/data/ssm_contacts/ssm_contacts_page.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_page" });

      data(SsmContactsPage, "example", {
        pageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsPage, SsmContactsPageConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_page")

        ubx.data(SsmContactsPage, "example", SsmContactsPageConfig(
            page_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_id" type="string" required>
  The real, unique identifier of the page this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contact_arn" type="string">
  The real Amazon Resource Name (ARN) of the contact this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content" type="string">
  The real content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="delivery_time" type="string">
  The real timestamp of the delivery. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="engagement_arn" type="string">
  The real Amazon Resource Name (ARN) of the engagement this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="incident_id" type="string">
  The real, unique identifier of the incident this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="page_arn" type="string">
  The real Amazon Resource Name (ARN) of the page this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_content" type="string">
  The real content of the public. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_subject" type="string">
  The real, public-facing subject of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="read_time" type="string">
  The real timestamp of the read. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sender" type="string">
  The real sender of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sent_time" type="string">
  The real timestamp of the sent. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subject" type="string">
  The real subject of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
