---
title: "aws_ssm_contacts_rotation_overrides"
description: "`aws_ssm_contacts_rotation_overrides` reads the real, manual overrides applied to an Incident Manager on-call rotation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_rotation_overrides"})

    		ubx.Data(ssm_contacts.SsmContactsRotationOverrides, "example", ssm_contacts.SsmContactsRotationOverridesConfig{
    			EndTime:    "example",
    			RotationId: "example",
    			StartTime:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsRotationOverrides } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_rotation_overrides";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_rotation_overrides" });

      data(SsmContactsRotationOverrides, "example", {
        endTime: "example",
        rotationId: "example",
        startTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsRotationOverrides, SsmContactsRotationOverridesConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_rotation_overrides")

        ubx.data(SsmContactsRotationOverrides, "example", SsmContactsRotationOverridesConfig(
            end_time="example",
            rotation_id="example",
            start_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rotation_id" type="string" required>
  The real, unique identifier of the rotation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rotation_overrides" type="list(object)">
  The real, overridden rotation entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
