---
title: "aws_ssm_contacts_contact_policy"
description: "`aws_ssm_contacts_contact_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_contacts_contact_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_contact_policy"})

    		ubx.Data(ssm_contacts.SsmContactsContactPolicy, "example", ssm_contacts.SsmContactsContactPolicyConfig{
    			ContactArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsContactPolicy } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_contact_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_contact_policy" });

      data(SsmContactsContactPolicy, "example", {
        contactArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsContactPolicy, SsmContactsContactPolicyConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_contact_policy")

        ubx.data(SsmContactsContactPolicy, "example", SsmContactsContactPolicyConfig(
            contact_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
