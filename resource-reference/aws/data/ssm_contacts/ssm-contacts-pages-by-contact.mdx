---
title: "aws_ssm_contacts_pages_by_contact"
description: "`aws_ssm_contacts_pages_by_contact` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_contacts_pages_by_contact` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_pages_by_contact"})

    		ubx.Data(ssm_contacts.SsmContactsPagesByContact, "example", ssm_contacts.SsmContactsPagesByContactConfig{
    			ContactId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsPagesByContact } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_pages_by_contact";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_pages_by_contact" });

      data(SsmContactsPagesByContact, "example", {
        contactId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsPagesByContact, SsmContactsPagesByContactConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_pages_by_contact")

        ubx.data(SsmContactsPagesByContact, "example", SsmContactsPagesByContactConfig(
            contact_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contact_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pages" type="list(object)">
  Returned by the lookup.
</ResponseField>
