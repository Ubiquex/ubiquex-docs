---
title: "aws_ssm_contacts_rotation_shifts"
description: "`aws_ssm_contacts_rotation_shifts` reads the real, computed shifts of an Incident Manager on-call rotation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_rotation_shifts"})

    		ubx.Data(ssm_contacts.SsmContactsRotationShifts, "example", ssm_contacts.SsmContactsRotationShiftsConfig{
    			EndTime:    "example",
    			RotationId: "example",
    			MaxResults: 1,
    			StartTime:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsRotationShifts } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_rotation_shifts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_rotation_shifts" });

      data(SsmContactsRotationShifts, "example", {
        endTime: "example",
        rotationId: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsRotationShifts, SsmContactsRotationShiftsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_rotation_shifts")

        ubx.data(SsmContactsRotationShifts, "example", SsmContactsRotationShiftsConfig(
            end_time="example",
            rotation_id="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rotation_id" type="string" required>
  The real, unique identifier of the rotation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rotation_shifts" type="list(object)">
  The real, scheduled rotation shifts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
