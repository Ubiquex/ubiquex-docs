---
title: "aws_ssm_contacts_rotation_shifts"
description: "`aws_ssm_contacts_rotation_shifts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_contacts_rotation_shifts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_rotation_shifts"})

    		ubx.Data(ssm_contacts.SsmContactsRotationShifts, "example", ssm_contacts.SsmContactsRotationShiftsConfig{
    			EndTime:    "example",
    			RotationId: "example",
    			MaxResults: 1,
    			StartTime:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsRotationShifts } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_rotation_shifts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_rotation_shifts" });

      data(SsmContactsRotationShifts, "example", {
        endTime: "example",
        rotationId: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsRotationShifts, SsmContactsRotationShiftsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_rotation_shifts")

        ubx.data(SsmContactsRotationShifts, "example", SsmContactsRotationShiftsConfig(
            end_time="example",
            rotation_id="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rotation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rotation_shifts" type="list(object)">
  Returned by the lookup.
</ResponseField>
