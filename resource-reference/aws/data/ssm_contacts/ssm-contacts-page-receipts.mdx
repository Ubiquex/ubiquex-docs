---
title: "aws_ssm_contacts_page_receipts"
description: "`aws_ssm_contacts_page_receipts` reads the real delivery receipts for an Incident Manager page."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_page_receipts"})

    		ubx.Data(ssm_contacts.SsmContactsPageReceipts, "example", ssm_contacts.SsmContactsPageReceiptsConfig{
    			PageId:     "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsPageReceipts } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_page_receipts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_page_receipts" });

      data(SsmContactsPageReceipts, "example", {
        pageId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsPageReceipts, SsmContactsPageReceiptsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_page_receipts")

        ubx.data(SsmContactsPageReceipts, "example", SsmContactsPageReceiptsConfig(
            page_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_id" type="string" required>
  The real, unique identifier of the page this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="receipts" type="list(object)">
  The real receipts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
