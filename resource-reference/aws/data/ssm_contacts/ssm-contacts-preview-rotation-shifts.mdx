---
title: "aws_ssm_contacts_preview_rotation_shifts"
description: "`aws_ssm_contacts_preview_rotation_shifts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_contacts_preview_rotation_shifts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_preview_rotation_shifts"})

    		ubx.Data(ssm_contacts.SsmContactsPreviewRotationShifts, "example", ssm_contacts.SsmContactsPreviewRotationShiftsConfig{
    			EndTime:           "example",
    			Members:           []string{"example"},
    			Recurrence:        map[string]any{"number_of_on_calls": 1},
    			TimeZoneId:        "example",
    			MaxResults:        1,
    			Overrides:         []map[string]any{{"end_time": "example"}},
    			RotationStartTime: "example",
    			StartTime:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsPreviewRotationShifts } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_preview_rotation_shifts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_preview_rotation_shifts" });

      data(SsmContactsPreviewRotationShifts, "example", {
        endTime: "example",
        members: ["example"],
        recurrence: { numberOfOnCalls: 1 },
        timeZoneId: "example",
        maxResults: 1,
        overrides: [{ endTime: "example" }],
        rotationStartTime: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsPreviewRotationShifts, SsmContactsPreviewRotationShiftsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_preview_rotation_shifts")

        ubx.data(SsmContactsPreviewRotationShifts, "example", SsmContactsPreviewRotationShiftsConfig(
            end_time="example",
            members=["example"],
            recurrence={"number_of_on_calls": 1},
            time_zone_id="example",
            max_results=1,
            overrides=[{"end_time": "example"}],
            rotation_start_time="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="members" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="overrides" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recurrence" type="object" required>
  &lt;p&gt;Information about when an on-call rotation is in effect and how long the rotation period lasts.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="daily_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="monthly_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="number_of_on_calls" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="recurrence_multiplier" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="shift_coverages" type="map(list(object))">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="weekly_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rotation_start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time_zone_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rotation_shifts" type="list(object)">
  Returned by the lookup.
</ResponseField>
