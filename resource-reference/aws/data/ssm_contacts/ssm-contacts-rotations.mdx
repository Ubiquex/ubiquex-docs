---
title: "aws_ssm_contacts_rotations"
description: "`aws_ssm_contacts_rotations` looks up a real Incident Manager on-call rotation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_contacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_contacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_contacts_rotations"})

    		ubx.Data(ssm_contacts.SsmContactsRotations, "example", ssm_contacts.SsmContactsRotationsConfig{
    			MaxResults:         1,
    			RotationNamePrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmContactsRotations } from "@ubx/sdk-aws/aws/data/ssm_contacts/ssm_contacts_rotations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_contacts_rotations" });

      data(SsmContactsRotations, "example", {
        maxResults: 1,
        rotationNamePrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_contacts import SsmContactsRotations, SsmContactsRotationsConfig

    def describe():
        ubx.intent("look up aws_ssm_contacts_rotations")

        ubx.data(SsmContactsRotations, "example", SsmContactsRotationsConfig(
            max_results=1,
            rotation_name_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rotation_name_prefix" type="string">
  The real S3 key prefix used for the rotation name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rotations" type="list(object)">
  The real rotations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
