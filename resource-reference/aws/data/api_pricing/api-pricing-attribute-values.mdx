---
title: "aws_api_pricing_attribute_values"
description: "`aws_api_pricing_attribute_values` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_pricing_attribute_values` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_pricing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_pricing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_pricing_attribute_values"})

    		ubx.Data(api_pricing.ApiPricingAttributeValues, "example", api_pricing.ApiPricingAttributeValuesConfig{
    			AttributeName: "example-attribute-name",
    			ServiceCode:   "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPricingAttributeValues } from "@ubx/sdk-aws/aws/data/api_pricing/api_pricing_attribute_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_pricing_attribute_values" });

      data(ApiPricingAttributeValues, "example", {
        attributeName: "example-attribute-name",
        serviceCode: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_pricing import ApiPricingAttributeValues, ApiPricingAttributeValuesConfig

    def describe():
        ubx.intent("look up aws_api_pricing_attribute_values")

        ubx.data(ApiPricingAttributeValues, "example", ApiPricingAttributeValuesConfig(
            attribute_name="example-attribute-name",
            service_code="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute_values" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
