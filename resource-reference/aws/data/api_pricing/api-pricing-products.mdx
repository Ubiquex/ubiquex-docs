---
title: "aws_api_pricing_products"
description: "`aws_api_pricing_products` reads real, priced products for an AWS service, via the Price List API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_pricing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_pricing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_pricing_products"})

    		ubx.Data(api_pricing.ApiPricingProducts, "example", api_pricing.ApiPricingProductsConfig{
    			ServiceCode: "example",
    			Filters:     []map[string]any{{"field": "example"}},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPricingProducts } from "@ubx/sdk-aws/aws/data/api_pricing/api_pricing_products";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_pricing_products" });

      data(ApiPricingProducts, "example", {
        serviceCode: "example",
        filters: [{ field: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_pricing import ApiPricingProducts, ApiPricingProductsConfig

    def describe():
        ubx.intent("look up aws_api_pricing_products")

        ubx.data(ApiPricingProducts, "example", ApiPricingProductsConfig(
            service_code="example",
            filters=[{"field": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  The real, unique code identifying the AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="format_version" type="string">
  The real version of the format. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="price_list" type="list(string)">
  The real prices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
