---
title: "aws_api_pricing_services"
description: "`aws_api_pricing_services` reads the real AWS services available through the Price List API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_pricing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_pricing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_pricing_services"})

    		ubx.Data(api_pricing.ApiPricingServices, "example", api_pricing.ApiPricingServicesConfig{
    			MaxResults:  1,
    			ServiceCode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPricingServices } from "@ubx/sdk-aws/aws/data/api_pricing/api_pricing_services";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_pricing_services" });

      data(ApiPricingServices, "example", {
        maxResults: 1,
        serviceCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_pricing import ApiPricingServices, ApiPricingServicesConfig

    def describe():
        ubx.intent("look up aws_api_pricing_services")

        ubx.data(ApiPricingServices, "example", ApiPricingServicesConfig(
            max_results=1,
            service_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string">
  The real, unique code identifying the AWS service this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="format_version" type="string">
  The real version of the format. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="services" type="list(object)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
