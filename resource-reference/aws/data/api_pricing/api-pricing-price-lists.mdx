---
title: "aws_api_pricing_price_lists"
description: "`aws_api_pricing_price_lists` reads the real, available Price List API price lists for a service and Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_pricing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_pricing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_pricing_price_lists"})

    		ubx.Data(api_pricing.ApiPricingPriceLists, "example", api_pricing.ApiPricingPriceListsConfig{
    			CurrencyCode:  "example",
    			EffectiveDate: "example",
    			ServiceCode:   "example",
    			MaxResults:    1,
    			RegionCode:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPricingPriceLists } from "@ubx/sdk-aws/aws/data/api_pricing/api_pricing_price_lists";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_pricing_price_lists" });

      data(ApiPricingPriceLists, "example", {
        currencyCode: "example",
        effectiveDate: "example",
        serviceCode: "example",
        maxResults: 1,
        regionCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_pricing import ApiPricingPriceLists, ApiPricingPriceListsConfig

    def describe():
        ubx.intent("look up aws_api_pricing_price_lists")

        ubx.data(ApiPricingPriceLists, "example", ApiPricingPriceListsConfig(
            currency_code="example",
            effective_date="example",
            service_code="example",
            max_results=1,
            region_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="currency_code" type="string" required>
  The real currency this value is denominated in. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="effective_date" type="string" required>
  The real date of the effective. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="region_code" type="string">
  The real, unique code identifying the AWS region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  The real, unique code identifying the AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="price_lists" type="list(object)">
  The real price lists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
