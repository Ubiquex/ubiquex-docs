---
title: "aws_api_pricing_price_lists"
description: "`aws_api_pricing_price_lists` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_pricing_price_lists` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_pricing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_pricing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_pricing_price_lists"})

    		ubx.Data(api_pricing.ApiPricingPriceLists, "example", api_pricing.ApiPricingPriceListsConfig{
    			CurrencyCode:  "example",
    			EffectiveDate: "example",
    			ServiceCode:   "example",
    			MaxResults:    1,
    			RegionCode:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPricingPriceLists } from "@ubx/sdk-aws/aws/data/api_pricing/api_pricing_price_lists";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_pricing_price_lists" });

      data(ApiPricingPriceLists, "example", {
        currencyCode: "example",
        effectiveDate: "example",
        serviceCode: "example",
        maxResults: 1,
        regionCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_pricing import ApiPricingPriceLists, ApiPricingPriceListsConfig

    def describe():
        ubx.intent("look up aws_api_pricing_price_lists")

        ubx.data(ApiPricingPriceLists, "example", ApiPricingPriceListsConfig(
            currency_code="example",
            effective_date="example",
            service_code="example",
            max_results=1,
            region_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="currency_code" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="effective_date" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="region_code" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_code" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="price_lists" type="list(object)">
  Returned by the lookup.
</ResponseField>
