---
title: "aws_groundstation_agent_task_response_url"
description: "`aws_groundstation_agent_task_response_url` reads a real, generated URL for a Ground Station agent to submit a task response."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	groundstation "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/groundstation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_groundstation_agent_task_response_url"})

    		ubx.Data(groundstation.AgentTaskResponseUrl, "example", groundstation.AgentTaskResponseUrlConfig{
    			AgentId: "example",
    			TaskId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgentTaskResponseUrl } from "./local-sdk/aws/sdk/typescript/aws/data/groundstation/agent_task_response_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_groundstation_agent_task_response_url" });

      data(AgentTaskResponseUrl, "example", {
        agentId: "example",
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.groundstation import AgentTaskResponseUrl, AgentTaskResponseUrlConfig

    def describe():
        ubx.intent("look up aws_groundstation_agent_task_response_url")

        ubx.data(AgentTaskResponseUrl, "example", AgentTaskResponseUrlConfig(
            agent_id="example",
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_id" type="string" required>
  The real, unique identifier of the agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="task_id" type="string" required>
  The real, unique identifier of the task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="presigned_log_url" type="string">
  The real, resolvable URL of the presigned log this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
