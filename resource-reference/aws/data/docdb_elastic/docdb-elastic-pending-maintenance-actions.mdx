---
title: "aws_docdb_elastic_pending_maintenance_actions"
description: "`aws_docdb_elastic_pending_maintenance_actions` reads the real, pending maintenance actions for a DocumentDB Elastic Cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	docdb_elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/docdb_elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_docdb_elastic_pending_maintenance_actions"})

    		ubx.Data(docdb_elastic.DocdbElasticPendingMaintenanceActions, "example", docdb_elastic.DocdbElasticPendingMaintenanceActionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocdbElasticPendingMaintenanceActions } from "@ubx/sdk-aws/aws/data/docdb_elastic/docdb_elastic_pending_maintenance_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_docdb_elastic_pending_maintenance_actions" });

      data(DocdbElasticPendingMaintenanceActions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.docdb_elastic import DocdbElasticPendingMaintenanceActions, DocdbElasticPendingMaintenanceActionsConfig

    def describe():
        ubx.intent("look up aws_docdb_elastic_pending_maintenance_actions")

        ubx.data(DocdbElasticPendingMaintenanceActions, "example", DocdbElasticPendingMaintenanceActionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Token or cursor used in paginated operations. When this value is provided as operation input, the service returns results from where the previous response left off. When this value is present in operation output, it indicates that there are more results to retrieve. This should be opaque to not expose implementation details and potentially versioned to allow evolution of pagination strategy. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_pending_maintenance_actions" type="list(object)">
  The real, pending maintenance actions for this resource. **(AI-inferred)** Returned by the lookup.
</ResponseField>
