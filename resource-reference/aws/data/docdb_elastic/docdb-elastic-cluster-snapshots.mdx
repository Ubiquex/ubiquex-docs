---
title: "aws_docdb_elastic_cluster_snapshots"
description: "`aws_docdb_elastic_cluster_snapshots` looks up a real DocumentDB Elastic Cluster snapshot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	docdb_elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/docdb_elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_docdb_elastic_cluster_snapshots"})

    		ubx.Data(docdb_elastic.DocdbElasticClusterSnapshots, "example", docdb_elastic.DocdbElasticClusterSnapshotsConfig{
    			ClusterArn:   "arn:aws:iam::123456789012:policy/example",
    			MaxResults:   1,
    			SnapshotType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocdbElasticClusterSnapshots } from "@ubx/sdk-aws/aws/data/docdb_elastic/docdb_elastic_cluster_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_docdb_elastic_cluster_snapshots" });

      data(DocdbElasticClusterSnapshots, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        snapshotType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.docdb_elastic import DocdbElasticClusterSnapshots, DocdbElasticClusterSnapshotsConfig

    def describe():
        ubx.intent("look up aws_docdb_elastic_cluster_snapshots")

        ubx.data(DocdbElasticClusterSnapshots, "example", DocdbElasticClusterSnapshotsConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            snapshot_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string">
  The real Amazon Resource Name (ARN) of the cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  The real type of snapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Token or cursor used in paginated operations. When this value is provided as operation input, the service returns results from where the previous response left off. When this value is present in operation output, it indicates that there are more results to retrieve. This should be opaque to not expose implementation details and potentially versioned to allow evolution of pagination strategy. Returned by the lookup.
</ResponseField>

<ResponseField name="snapshots" type="list(object)">
  The real snapshots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
