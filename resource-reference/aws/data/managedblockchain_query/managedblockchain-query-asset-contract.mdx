---
title: "aws_managedblockchain_query_asset_contract"
description: "`aws_managedblockchain_query_asset_contract` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_managedblockchain_query_asset_contract` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_asset_contract"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryAssetContract, "example", managedblockchain_query.ManagedblockchainQueryAssetContractConfig{
    			ContractIdentifier: map[string]any{"contract_address": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedblockchainQueryAssetContract } from "@ubx/sdk-aws/aws/data/managedblockchain_query/managedblockchain_query_asset_contract";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_asset_contract" });

      data(ManagedblockchainQueryAssetContract, "example", {
        contractIdentifier: { contractAddress: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryAssetContract, ManagedblockchainQueryAssetContractConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_query_asset_contract")

        ubx.data(ManagedblockchainQueryAssetContract, "example", ManagedblockchainQueryAssetContractConfig(
            contract_identifier={"contract_address": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contract_identifier" type="object" required>
  &lt;p&gt;Container for the blockchain address and network information about a contract.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="contract_address" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="deployer_address" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="object">
  &lt;p&gt;The metadata of the contract.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="decimals" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="symbol" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_standard" type="string">
  Returned by the lookup.
</ResponseField>
