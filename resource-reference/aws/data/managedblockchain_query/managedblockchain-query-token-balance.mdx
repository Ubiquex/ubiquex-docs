---
title: "aws_managedblockchain_query_token_balance"
description: "`aws_managedblockchain_query_token_balance` reads a real, on-chain token balance for an address, via Managed Blockchain Query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_token_balance"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryTokenBalance, "example", managedblockchain_query.ManagedblockchainQueryTokenBalanceConfig{
    			OwnerIdentifier: map[string]any{"address": "example"},
    			TokenIdentifier: map[string]any{"network": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ManagedblockchainQueryTokenBalance } from "./local-sdk/aws/sdk/typescript/aws/data/managedblockchain_query/managedblockchain_query_token_balance.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_token_balance" });

      data(ManagedblockchainQueryTokenBalance, "example", {
        ownerIdentifier: { address: "example" },
        tokenIdentifier: { network: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryTokenBalance, ManagedblockchainQueryTokenBalanceConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_query_token_balance")

        ubx.data(ManagedblockchainQueryTokenBalance, "example", ManagedblockchainQueryTokenBalanceConfig(
            owner_identifier={"address": "example"},
            token_identifier={"network": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner_identifier" type="object" required>
  &lt;p&gt;The container for the owner identifier.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="address" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_identifier" type="object" required>
  &lt;p&gt;The container for the identifier for the token including the unique token ID and its blockchain network.&lt;/p&gt; &lt;note&gt; &lt;p&gt;Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported.&lt;/p&gt; &lt;/note&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="contract_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="token_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="at_blockchain_instant" type="object">
  &lt;p&gt;The container for time.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="balance" type="string">
  The real, current balance of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="object">
  &lt;p&gt;The container for time.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
