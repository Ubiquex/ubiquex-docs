---
title: "aws_managedblockchain_query_transactions"
description: "`aws_managedblockchain_query_transactions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_managedblockchain_query_transactions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_transactions"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryTransactions, "example", managedblockchain_query.ManagedblockchainQueryTransactionsConfig{
    			Address:                  "example",
    			Network:                  "example",
    			ConfirmationStatusFilter: map[string]any{"include": []string{"example"}},
    			FromBlockchainInstant:    map[string]any{"time": "example"},
    			MaxResults:               1,
    			Sort:                     map[string]any{"sort_by": "example"},
    			ToBlockchainInstant:      map[string]any{"time": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedblockchainQueryTransactions } from "@ubx/sdk-aws/aws/data/managedblockchain_query/managedblockchain_query_transactions";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_transactions" });

      data(ManagedblockchainQueryTransactions, "example", {
        address: "example",
        network: "example",
        confirmationStatusFilter: { include: ["example"] },
        fromBlockchainInstant: { time: "example" },
        maxResults: 1,
        sort: { sortBy: "example" },
        toBlockchainInstant: { time: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryTransactions, ManagedblockchainQueryTransactionsConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_query_transactions")

        ubx.data(ManagedblockchainQueryTransactions, "example", ManagedblockchainQueryTransactionsConfig(
            address="example",
            network="example",
            confirmation_status_filter={"include": ["example"]},
            from_blockchain_instant={"time": "example"},
            max_results=1,
            sort={"sort_by": "example"},
            to_blockchain_instant={"time": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="address" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="confirmation_status_filter" type="object">
  &lt;p&gt;The container for the &lt;code&gt;ConfirmationStatusFilter&lt;/code&gt; that filters for the &lt;a href="https://docs.aws.amazon.com/managed-blockchain/latest/ambq-dg/key-concepts.html#finality"&gt; &lt;i&gt;finality&lt;/i&gt; &lt;/a&gt; of the results.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="include" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="from_blockchain_instant" type="object">
  &lt;p&gt;The container for time.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="network" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="object">
  &lt;p&gt;The container for determining how the list transaction result will be sorted.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="sort_by" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sort_order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="to_blockchain_instant" type="object">
  &lt;p&gt;The container for time.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transactions" type="list(object)">
  Returned by the lookup.
</ResponseField>
