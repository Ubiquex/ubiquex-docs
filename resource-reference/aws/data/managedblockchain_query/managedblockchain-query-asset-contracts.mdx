---
title: "aws_managedblockchain_query_asset_contracts"
description: "`aws_managedblockchain_query_asset_contracts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_managedblockchain_query_asset_contracts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_asset_contracts"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryAssetContracts, "example", managedblockchain_query.ManagedblockchainQueryAssetContractsConfig{
    			ContractFilter: map[string]any{"deployer_address": "example"},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedblockchainQueryAssetContracts } from "@ubx/sdk-aws/aws/data/managedblockchain_query/managedblockchain_query_asset_contracts";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_asset_contracts" });

      data(ManagedblockchainQueryAssetContracts, "example", {
        contractFilter: { deployerAddress: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryAssetContracts, ManagedblockchainQueryAssetContractsConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_query_asset_contracts")

        ubx.data(ManagedblockchainQueryAssetContracts, "example", ManagedblockchainQueryAssetContractsConfig(
            contract_filter={"deployer_address": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="contract_filter" type="object" required>
  &lt;p&gt;The contract or wallet address by which to filter the request.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="deployer_address" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="token_standard" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contracts" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
