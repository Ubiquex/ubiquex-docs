---
title: "aws_managedblockchain_query_transaction"
description: "`aws_managedblockchain_query_transaction` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_managedblockchain_query_transaction` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_transaction"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryTransaction, "example", managedblockchain_query.ManagedblockchainQueryTransactionConfig{
    			Network:         "example",
    			TransactionHash: "example",
    			TransactionId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedblockchainQueryTransaction } from "@ubx/sdk-aws/aws/data/managedblockchain_query/managedblockchain_query_transaction";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_transaction" });

      data(ManagedblockchainQueryTransaction, "example", {
        network: "example",
        transactionHash: "example",
        transactionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryTransaction, ManagedblockchainQueryTransactionConfig

    def describe():
        ubx.intent("look up aws_managedblockchain_query_transaction")

        ubx.data(ManagedblockchainQueryTransaction, "example", ManagedblockchainQueryTransactionConfig(
            network="example",
            transaction_hash="example",
            transaction_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="network" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="transaction_hash" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="transaction_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="transaction" type="object">
  &lt;p&gt;There are two possible types of transactions used for this data type:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;A Bitcoin transaction is a movement of BTC from one address to another.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;An Ethereum transaction refers to an action initiated by an externally owned account, which is an account managed by a human, not a contract. For example, if Bob sends Alice 1 ETH, Bob's account must be debited and Alice's must be credited. This state-changing action occurs within a transaction.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="block_hash" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="block_number" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="confirmation_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="contract_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cumulative_gas_used" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="effective_gas_price" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="execution_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="from" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="gas_used" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="number_of_transactions" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="signature_r" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="signature_s" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="signature_v" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="to" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="transaction_fee" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transaction_hash" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="transaction_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transaction_index" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="transaction_timestamp" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
