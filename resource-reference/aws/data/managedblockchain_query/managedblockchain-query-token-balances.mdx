---
title: "aws_managedblockchain_query_token_balances"
description: "`aws_managedblockchain_query_token_balances` reads real, on-chain token balances for an address, via Managed Blockchain Query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	managedblockchain_query "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/managedblockchain_query"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_managedblockchain_query_token_balances"})

    		ubx.Data(managedblockchain_query.ManagedblockchainQueryTokenBalances, "example", managedblockchain_query.ManagedblockchainQueryTokenBalancesConfig{
    			TokenFilter: map[string]any{"network": "example"},
    			MaxResults:  1,
    			OwnerFilter: map[string]any{"address": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedblockchainQueryTokenBalances } from "@ubx/sdk-aws/aws/data/managedblockchain_query/managedblockchain_query_token_balances";

    export default stack("example", () => {
      intent({ summary: "look up aws_managedblockchain_query_token_balances" });

      data(ManagedblockchainQueryTokenBalances, "example", {
        tokenFilter: { network: "example" },
        maxResults: 1,
        ownerFilter: { address: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.managedblockchain_query import ManagedblockchainQueryTokenBalances, ManagedblockchainQueryTokenBalancesConfig
    from ubx.aws.data.managedblockchain_query.managedblockchain_query_token_balances import ManagedblockchainQueryTokenBalances_TokenBalances_TokenIdentifier, ManagedblockchainQueryTokenBalances_OwnerFilter

    def describe():
        ubx.intent("look up aws_managedblockchain_query_token_balances")

        ubx.data(ManagedblockchainQueryTokenBalances, "example", ManagedblockchainQueryTokenBalancesConfig(
            token_filter=ManagedblockchainQueryTokenBalances_TokenBalances_TokenIdentifier(network="example"),
            max_results=1,
            owner_filter=ManagedblockchainQueryTokenBalances_OwnerFilter(address="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner_filter" type="object">
  &lt;p&gt;The container for the owner information to filter by.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="address" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_filter" type="object" required>
  &lt;p&gt;The container of the token filter like the contract address on a given blockchain network or a unique token identifier on a given blockchain network.&lt;/p&gt; &lt;note&gt; &lt;p&gt;You must always specify the network property of this container when using this operation.&lt;/p&gt; &lt;/note&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="contract_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="network" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="token_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="token_balances" type="list(object)">
  The real token balances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
