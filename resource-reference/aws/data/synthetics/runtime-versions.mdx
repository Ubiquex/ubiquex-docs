---
title: "aws_synthetics_runtime_versions"
description: "`aws_synthetics_runtime_versions` reads the real, available CloudWatch Synthetics canary runtime versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_runtime_versions"})

    		ubx.Data(synthetics.RuntimeVersions, "example", synthetics.RuntimeVersionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuntimeVersions } from "@ubx/sdk-aws/aws/data/synthetics/runtime_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_runtime_versions" });

      data(RuntimeVersions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import RuntimeVersions, RuntimeVersionsConfig

    def describe():
        ubx.intent("look up aws_synthetics_runtime_versions")

        ubx.data(RuntimeVersions, "example", RuntimeVersionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="runtime_versions" type="list(object)">
  The real versions of the runtime included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
