---
title: "aws_synthetics_canary_runs"
description: "`data_aws_synthetics_canary_runs` looks up a real, already-existing `aws_synthetics_canary`."
---

It creates a real, scheduled script, a Node.js or Python Selenium/Playwright-style script, that CloudWatch Synthetics runs on a recurring cadence against a live endpoint or UI flow, proactively detecting an outage or broken user journey before a real user hits it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_canary_runs"})

    		ubx.Data(synthetics.CanaryRuns, "example", synthetics.CanaryRunsConfig{
    			Name:       "example",
    			DryRunId:   "example",
    			MaxResults: 1,
    			RunType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CanaryRuns } from "@ubx/sdk-aws/aws/data/synthetics/canary_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_canary_runs" });

      data(CanaryRuns, "example", {
        name: "example",
        dryRunId: "example",
        maxResults: 1,
        runType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import CanaryRuns, CanaryRunsConfig

    def describe():
        ubx.intent("look up aws_synthetics_canary_runs")

        ubx.data(CanaryRuns, "example", CanaryRunsConfig(
            name="example",
            dry_run_id="example",
            max_results=1,
            run_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run_id" type="string">
  The real, unique identifier of the dry run this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="run_type" type="string">
  The real, discriminating type of the run this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="canary_runs" type="list(object)">
  The real CloudWatch Synthetics canary runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
