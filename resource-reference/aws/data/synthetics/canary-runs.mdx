---
title: "aws_synthetics_canary_runs"
description: "`aws_synthetics_canary_runs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_synthetics_canary_runs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_canary_runs"})

    		ubx.Data(synthetics.CanaryRuns, "example", synthetics.CanaryRunsConfig{
    			Name:       "example",
    			DryRunId:   "example",
    			MaxResults: 1,
    			RunType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CanaryRuns } from "@ubx/sdk-aws/aws/data/synthetics/canary_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_canary_runs" });

      data(CanaryRuns, "example", {
        name: "example",
        dryRunId: "example",
        maxResults: 1,
        runType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import CanaryRuns, CanaryRunsConfig

    def describe():
        ubx.intent("look up aws_synthetics_canary_runs")

        ubx.data(CanaryRuns, "example", CanaryRunsConfig(
            name="example",
            dry_run_id="example",
            max_results=1,
            run_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dry_run_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="canary_runs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
