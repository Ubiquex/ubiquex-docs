---
title: "aws_synthetics_groups"
description: "`data_aws_synthetics_groups` looks up a real, already-existing `aws_synthetics_group`."
---

It groups real `aws_synthetics_canary`s together, so their combined pass/fail status and metrics can be viewed and alarmed on as one unit rather than canary by canary.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_groups"})

    		ubx.Data(synthetics.Groups, "example", synthetics.GroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Groups } from "@ubx/sdk-aws/aws/data/synthetics/groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_groups" });

      data(Groups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import Groups, GroupsConfig

    def describe():
        ubx.intent("look up aws_synthetics_groups")

        ubx.data(Groups, "example", GroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
