---
title: "aws_synthetics_canaries"
description: "`data_aws_synthetics_canaries` looks up a real, already-existing `aws_synthetics_canary`."
---

It creates a real, scheduled script, a Node.js or Python Selenium/Playwright-style script, that CloudWatch Synthetics runs on a recurring cadence against a live endpoint or UI flow, proactively detecting an outage or broken user journey before a real user hits it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_canaries"})

    		ubx.Data(synthetics.Canaries, "example", synthetics.CanariesConfig{
    			MaxResults: 1,
    			Names:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Canaries } from "./local-sdk/aws/sdk/typescript/aws/data/synthetics/canaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_canaries" });

      data(Canaries, "example", {
        maxResults: 1,
        names: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import Canaries, CanariesConfig

    def describe():
        ubx.intent("look up aws_synthetics_canaries")

        ubx.data(Canaries, "example", CanariesConfig(
            max_results=1,
            names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="canaries" type="list(object)">
  The real CloudWatch Synthetics canaries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
