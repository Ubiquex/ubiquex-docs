---
title: "aws_synthetics_canaries_last_run"
description: "`aws_synthetics_canaries_last_run` reads the real, most recent run result of a CloudWatch Synthetics canary."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_synthetics_canaries_last_run"})

    		ubx.Data(synthetics.CanariesLastRun, "example", synthetics.CanariesLastRunConfig{
    			BrowserType: "example",
    			MaxResults:  1,
    			Names:       []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CanariesLastRun } from "./local-sdk/aws/sdk/typescript/aws/data/synthetics/canaries_last_run.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_synthetics_canaries_last_run" });

      data(CanariesLastRun, "example", {
        browserType: "example",
        maxResults: 1,
        names: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.synthetics import CanariesLastRun, CanariesLastRunConfig

    def describe():
        ubx.intent("look up aws_synthetics_canaries_last_run")

        ubx.data(CanariesLastRun, "example", CanariesLastRunConfig(
            browser_type="example",
            max_results=1,
            names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="browser_type" type="string">
  The real, discriminating type of the browser this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="canaries_last_run" type="list(object)">
  The real, most recent run of each canary included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
