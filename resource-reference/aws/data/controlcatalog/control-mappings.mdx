---
title: "aws_controlcatalog_control_mappings"
description: "`aws_controlcatalog_control_mappings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_controlcatalog_control_mappings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controlcatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/controlcatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_controlcatalog_control_mappings"})

    		ubx.Data(controlcatalog.ControlMappings, "example", controlcatalog.ControlMappingsConfig{
    			Filter:     map[string]any{"common_control_arns": []string{"example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlMappings } from "@ubx/sdk-aws/aws/data/controlcatalog/control_mappings";

    export default stack("example", () => {
      intent({ summary: "look up aws_controlcatalog_control_mappings" });

      data(ControlMappings, "example", {
        filter: { commonControlArns: ["example"] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.controlcatalog import ControlMappings, ControlMappingsConfig

    def describe():
        ubx.intent("look up aws_controlcatalog_control_mappings")

        ubx.data(ControlMappings, "example", ControlMappingsConfig(
            filter={"common_control_arns": ["example"]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;A structure that defines filtering criteria for the ListControlMappings operation. You can use this filter to narrow down the list of control mappings based on control ARNs, common control ARNs, or mapping types.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="common_control_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="control_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="mapping_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="control_mappings" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
