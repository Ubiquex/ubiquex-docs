---
title: "aws_email_send_statistics"
description: "`aws_email_send_statistics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_email_send_statistics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_send_statistics"})

    		ubx.Data(email.SendStatistics, "example", email.SendStatisticsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SendStatistics } from "@ubx/sdk-aws/aws/data/email/send_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_send_statistics" });

      data(SendStatistics, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import SendStatistics, SendStatisticsConfig

    def describe():
        ubx.intent("look up aws_email_send_statistics")

        ubx.data(SendStatistics, "example", SendStatisticsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="send_data_points" type="list(object)">
  Returned by the lookup.
</ResponseField>
