---
title: "aws_email_identity_mail_from_domain_attributes"
description: "`aws_email_identity_mail_from_domain_attributes` reads the real custom MAIL FROM domain configuration of an SES identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_identity_mail_from_domain_attributes"})

    		ubx.Data(email.IdentityMailFromDomainAttributes, "example", email.IdentityMailFromDomainAttributesConfig{
    			Identities: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IdentityMailFromDomainAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/email/identity_mail_from_domain_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_identity_mail_from_domain_attributes" });

      data(IdentityMailFromDomainAttributes, "example", {
        identities: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.email import IdentityMailFromDomainAttributes, IdentityMailFromDomainAttributesConfig

    def describe():
        ubx.intent("look up aws_email_identity_mail_from_domain_attributes")

        ubx.data(IdentityMailFromDomainAttributes, "example", IdentityMailFromDomainAttributesConfig(
            identities=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identities" type="list(string)" required>
  The real identities included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mail_from_domain_attributes" type="map(object)">
  The real MAIL FROM domain attributes for this SES identity. **(AI-inferred)** Returned by the lookup.
</ResponseField>
