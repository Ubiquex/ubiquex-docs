---
title: "aws_email_templates"
description: "`aws_email_templates` looks up a real SES v1 email template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_templates"})

    		ubx.Data(email.Templates, "example", email.TemplatesConfig{
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Templates } from "@ubx/sdk-aws/aws/data/email/templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_templates" });

      data(Templates, "example", {
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import Templates, TemplatesConfig

    def describe():
        ubx.intent("look up aws_email_templates")

        ubx.data(Templates, "example", TemplatesConfig(
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="templates_metadata" type="list(object)">
  The real, structured metadata of the templates. **(AI-inferred)** Returned by the lookup.
</ResponseField>
