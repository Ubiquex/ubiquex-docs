---
title: "aws_email_identity_policies"
description: "`aws_email_identity_policies` reads the real resource-based policies attached to an SES identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_identity_policies"})

    		ubx.Data(email.IdentityPolicies, "example", email.IdentityPoliciesConfig{
    			Identity:    "example",
    			PolicyNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityPolicies } from "@ubx/sdk-aws/aws/data/email/identity_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_identity_policies" });

      data(IdentityPolicies, "example", {
        identity: "example",
        policyNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import IdentityPolicies, IdentityPoliciesConfig

    def describe():
        ubx.intent("look up aws_email_identity_policies")

        ubx.data(IdentityPolicies, "example", IdentityPoliciesConfig(
            identity="example",
            policy_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity" type="string" required>
  The real identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="policy_names" type="list(string)" required>
  The real names of the policies included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policies" type="map(string)">
  The real access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
