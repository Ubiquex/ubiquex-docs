---
title: "aws_email_verified_email_addresses"
description: "`aws_email_verified_email_addresses` reads the real email addresses verified for sending through SES v1."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_verified_email_addresses"})

    		ubx.Data(email.VerifiedEmailAddresses, "example", email.VerifiedEmailAddressesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VerifiedEmailAddresses } from "./local-sdk/aws/sdk/typescript/aws/data/email/verified_email_addresses.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_verified_email_addresses" });

      data(VerifiedEmailAddresses, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.email import VerifiedEmailAddresses, VerifiedEmailAddressesConfig

    def describe():
        ubx.intent("look up aws_email_verified_email_addresses")

        ubx.data(VerifiedEmailAddresses, "example", VerifiedEmailAddressesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="verified_email_addresses" type="list(string)">
  The real, verified email addresses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
