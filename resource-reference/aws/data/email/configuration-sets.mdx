---
title: "aws_email_configuration_sets"
description: "`aws_email_configuration_sets` looks up a real SES v1 configuration set, tracking sending events for a group of emails."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_configuration_sets"})

    		ubx.Data(email.ConfigurationSets, "example", email.ConfigurationSetsConfig{
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationSets } from "@ubx/sdk-aws/aws/data/email/configuration_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_configuration_sets" });

      data(ConfigurationSets, "example", {
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import ConfigurationSets, ConfigurationSetsConfig

    def describe():
        ubx.intent("look up aws_email_configuration_sets")

        ubx.data(ConfigurationSets, "example", ConfigurationSetsConfig(
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_sets" type="list(object)">
  The real configuration sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
