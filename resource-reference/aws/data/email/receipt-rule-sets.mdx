---
title: "aws_email_receipt_rule_sets"
description: "`aws_email_receipt_rule_sets` looks up a real SES receipt rule set, controlling how inbound email is processed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_receipt_rule_sets"})

    		ubx.Data(email.ReceiptRuleSets, "example", email.ReceiptRuleSetsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReceiptRuleSets } from "./local-sdk/aws/sdk/typescript/aws/data/email/receipt_rule_sets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_receipt_rule_sets" });

      data(ReceiptRuleSets, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.email import ReceiptRuleSets, ReceiptRuleSetsConfig

    def describe():
        ubx.intent("look up aws_email_receipt_rule_sets")

        ubx.data(ReceiptRuleSets, "example", ReceiptRuleSetsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_sets" type="list(object)">
  The real rule sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
