---
title: "aws_email_active_receipt_rule_set"
description: "`aws_email_active_receipt_rule_set` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_email_active_receipt_rule_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_active_receipt_rule_set"})

    		ubx.Data(email.ActiveReceiptRuleSet, "example", email.ActiveReceiptRuleSetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActiveReceiptRuleSet } from "@ubx/sdk-aws/aws/data/email/active_receipt_rule_set";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_active_receipt_rule_set" });

      data(ActiveReceiptRuleSet, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import ActiveReceiptRuleSet, ActiveReceiptRuleSetConfig

    def describe():
        ubx.intent("look up aws_email_active_receipt_rule_set")

        ubx.data(ActiveReceiptRuleSet, "example", ActiveReceiptRuleSetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="metadata" type="object">
  &lt;p&gt;Information about a receipt rule set.&lt;/p&gt; &lt;p&gt;A receipt rule set is a collection of rules that specify what Amazon SES should do with mail it receives on behalf of your account's verified domains.&lt;/p&gt; &lt;p&gt;For information about setting up receipt rule sets, see the &lt;a href="https://docs.aws.amazon.com/ses/latest/dg/receiving-email-concepts.html#receiving-email-concepts-rules"&gt;Amazon SES Developer Guide&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rules" type="list(object)">
  Returned by the lookup.
</ResponseField>
