---
title: "aws_email_custom_verification_email_templates"
description: "`aws_email_custom_verification_email_templates` looks up a real, custom SES email address verification template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_custom_verification_email_templates"})

    		ubx.Data(email.CustomVerificationEmailTemplates, "example", email.CustomVerificationEmailTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomVerificationEmailTemplates } from "@ubx/sdk-aws/aws/data/email/custom_verification_email_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_custom_verification_email_templates" });

      data(CustomVerificationEmailTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.email import CustomVerificationEmailTemplates, CustomVerificationEmailTemplatesConfig

    def describe():
        ubx.intent("look up aws_email_custom_verification_email_templates")

        ubx.data(CustomVerificationEmailTemplates, "example", CustomVerificationEmailTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_verification_email_templates" type="list(object)">
  The real SES custom verification email templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
