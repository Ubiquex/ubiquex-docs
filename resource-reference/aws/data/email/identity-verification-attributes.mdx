---
title: "aws_email_identity_verification_attributes"
description: "`aws_email_identity_verification_attributes` reads the real, current verification status of an SES identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_identity_verification_attributes"})

    		ubx.Data(email.IdentityVerificationAttributes, "example", email.IdentityVerificationAttributesConfig{
    			Identities: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IdentityVerificationAttributes } from "./local-sdk/aws/sdk/typescript/aws/data/email/identity_verification_attributes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_identity_verification_attributes" });

      data(IdentityVerificationAttributes, "example", {
        identities: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.email import IdentityVerificationAttributes, IdentityVerificationAttributesConfig

    def describe():
        ubx.intent("look up aws_email_identity_verification_attributes")

        ubx.data(IdentityVerificationAttributes, "example", IdentityVerificationAttributesConfig(
            identities=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identities" type="list(string)" required>
  The real identities included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="verification_attributes" type="map(object)">
  The real SES verification attributes for this identity. **(AI-inferred)** Returned by the lookup.
</ResponseField>
