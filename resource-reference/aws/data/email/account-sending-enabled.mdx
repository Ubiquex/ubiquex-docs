---
title: "aws_email_account_sending_enabled"
description: "`aws_email_account_sending_enabled` reads whether real SES sending is currently enabled for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	email "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/email"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_email_account_sending_enabled"})

    		ubx.Data(email.AccountSendingEnabled, "example", email.AccountSendingEnabledConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountSendingEnabled } from "./local-sdk/aws/sdk/typescript/aws/data/email/account_sending_enabled.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_email_account_sending_enabled" });

      data(AccountSendingEnabled, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.email import AccountSendingEnabled, AccountSendingEnabledConfig

    def describe():
        ubx.intent("look up aws_email_account_sending_enabled")

        ubx.data(AccountSendingEnabled, "example", AccountSendingEnabledConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="enabled" type="bool">
  Whether this feature is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>
