---
title: "aws_kafkaconnect_connectors"
description: "`data_aws_kafkaconnect_connectors` looks up a real, already-existing `aws_kafka_connect_connector`."
---

It runs a real, managed Kafka Connect connector, MSK Connect provisioning and scaling the underlying compute, moving data between an Apache Kafka or Amazon MSK cluster and an external system without a caller operating their own Kafka Connect worker cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafkaconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kafkaconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafkaconnect_connectors"})

    		ubx.Data(kafkaconnect.Connectors, "example", kafkaconnect.ConnectorsConfig{
    			ConnectorNamePrefix: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Connectors } from "@ubx/sdk-aws/aws/data/kafkaconnect/connectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafkaconnect_connectors" });

      data(Connectors, "example", {
        connectorNamePrefix: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafkaconnect import Connectors, ConnectorsConfig

    def describe():
        ubx.intent("look up aws_kafkaconnect_connectors")

        ubx.data(Connectors, "example", ConnectorsConfig(
            connector_name_prefix="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connector_name_prefix" type="string">
  The real S3 key prefix used for the connector name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connectors" type="list(object)">
  The real connectors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
