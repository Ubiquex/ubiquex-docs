---
title: "aws_kafkaconnect_custom_plugins"
description: "`aws_kafkaconnect_custom_plugins` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kafkaconnect_custom_plugins` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafkaconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafkaconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafkaconnect_custom_plugins"})

    		ubx.Data(kafkaconnect.CustomPlugins, "example", kafkaconnect.CustomPluginsConfig{
    			MaxResults: 1,
    			NamePrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomPlugins } from "@ubx/sdk-aws/aws/data/kafkaconnect/custom_plugins";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafkaconnect_custom_plugins" });

      data(CustomPlugins, "example", {
        maxResults: 1,
        namePrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafkaconnect import CustomPlugins, CustomPluginsConfig

    def describe():
        ubx.intent("look up aws_kafkaconnect_custom_plugins")

        ubx.data(CustomPlugins, "example", CustomPluginsConfig(
            max_results=1,
            name_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_plugins" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
