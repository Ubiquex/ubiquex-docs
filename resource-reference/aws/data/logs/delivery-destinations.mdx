---
title: "aws_logs_delivery_destinations"
description: "`data_aws_logs_delivery_destinations` looks up a real, already-existing `aws_logs_delivery_destination`."
---

It represents a real AWS service, CloudWatch Logs, S3, Firehose, or X-Ray, configured to receive vended logs through the newer CloudWatch Logs delivery framework, the target end of an `aws_logs_delivery`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_delivery_destinations"})

    		ubx.Data(logs.DeliveryDestinations, "example", logs.DeliveryDestinationsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DeliveryDestinations } from "./local-sdk/aws/sdk/typescript/aws/data/logs/delivery_destinations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_delivery_destinations" });

      data(DeliveryDestinations, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.logs import DeliveryDestinations, DeliveryDestinationsConfig

    def describe():
        ubx.intent("look up aws_logs_delivery_destinations")

        ubx.data(DeliveryDestinations, "example", DeliveryDestinationsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delivery_destinations" type="list(object)">
  The real CloudWatch Logs delivery destinations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>
