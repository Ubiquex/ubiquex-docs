---
title: "aws_logs_syslog_configurations"
description: "`aws_logs_syslog_configurations` reads the real syslog subscription configuration of a CloudWatch Logs log group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_syslog_configurations"})

    		ubx.Data(logs.SyslogConfigurations, "example", logs.SyslogConfigurationsConfig{
    			LogGroupIdentifier: "example",
    			MaxResults:         1,
    			VpcEndpointId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyslogConfigurations } from "@ubx/sdk-aws/aws/data/logs/syslog_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_syslog_configurations" });

      data(SyslogConfigurations, "example", {
        logGroupIdentifier: "example",
        maxResults: 1,
        vpcEndpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import SyslogConfigurations, SyslogConfigurationsConfig

    def describe():
        ubx.intent("look up aws_logs_syslog_configurations")

        ubx.data(SyslogConfigurations, "example", SyslogConfigurationsConfig(
            log_group_identifier="example",
            max_results=1,
            vpc_endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="log_group_identifier" type="string">
  The real, unique identifier of the CloudWatch Logs log group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  The real, unique identifier of the vpc endpoint this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="syslog_configurations" type="list(object)">
  The real syslog configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
