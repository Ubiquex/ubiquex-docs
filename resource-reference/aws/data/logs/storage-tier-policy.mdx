---
title: "aws_logs_storage_tier_policy"
description: "`aws_logs_storage_tier_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_logs_storage_tier_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_storage_tier_policy"})

    		ubx.Data(logs.StorageTierPolicy, "example", logs.StorageTierPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StorageTierPolicy } from "@ubx/sdk-aws/aws/data/logs/storage_tier_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_storage_tier_policy" });

      data(StorageTierPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import StorageTierPolicy, StorageTierPolicyConfig

    def describe():
        ubx.intent("look up aws_logs_storage_tier_policy")

        ubx.data(StorageTierPolicy, "example", StorageTierPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="last_updated_time" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="storage_tier" type="string">
  Returned by the lookup.
</ResponseField>
