---
title: "aws_logs_log_groups"
description: "`data_aws_logs_log_groups` looks up a real, already-existing `aws_logs_log_group`."
---

It creates a real CloudWatch Logs log group, the container that log streams are organized under and that retention, encryption, and access settings are configured against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_log_groups"})

    		ubx.Data(logs.LogGroups, "example", logs.LogGroupsConfig{
    			AccountIdentifiers:    []string{"example"},
    			DataSources:           []map[string]any{{"name": "example-name"}},
    			FieldIndexNames:       []string{"example"},
    			IncludeLinkedAccounts: true,
    			Limit:                 1,
    			LogGroupClass:         "example",
    			LogGroupNamePattern:   "example",
    			LogGroupTags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LogGroups } from "./local-sdk/aws/sdk/typescript/aws/data/logs/log_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_log_groups" });

      data(LogGroups, "example", {
        accountIdentifiers: ["example"],
        dataSources: [{ name: "example-name" }],
        fieldIndexNames: ["example"],
        includeLinkedAccounts: true,
        limit: 1,
        logGroupClass: "example",
        logGroupNamePattern: "example",
        logGroupTags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.logs import LogGroups, LogGroupsConfig

    def describe():
        ubx.intent("look up aws_logs_log_groups")

        ubx.data(LogGroups, "example", LogGroupsConfig(
            account_identifiers=["example"],
            data_sources=[{"name": "example-name"}],
            field_index_names=["example"],
            include_linked_accounts=True,
            limit=1,
            log_group_class="example",
            log_group_name_pattern="example",
            log_group_tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_identifiers" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_sources" type="list(object)">
  The real data sources included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="field_index_names" type="list(string)">
  The real names of the field indexs included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_linked_accounts" type="bool">
  Whether results also include AWS Organizations member accounts linked to this account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="log_group_class" type="string">
  The real class of the log group. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="log_group_name_pattern" type="string">
  Restricts results to log groups whose name matches this pattern. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="log_group_tags" type="list(object)">
  The real log group tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="log_groups" type="list(object)">
  The real log groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>
