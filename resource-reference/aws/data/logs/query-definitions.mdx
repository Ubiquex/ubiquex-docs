---
title: "aws_logs_query_definitions"
description: "`aws_logs_query_definitions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_logs_query_definitions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_query_definitions"})

    		ubx.Data(logs.QueryDefinitions, "example", logs.QueryDefinitionsConfig{
    			MaxResults:                1,
    			QueryDefinitionNamePrefix: "example",
    			QueryLanguage:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueryDefinitions } from "@ubx/sdk-aws/aws/data/logs/query_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_query_definitions" });

      data(QueryDefinitions, "example", {
        maxResults: 1,
        queryDefinitionNamePrefix: "example",
        queryLanguage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import QueryDefinitions, QueryDefinitionsConfig

    def describe():
        ubx.intent("look up aws_logs_query_definitions")

        ubx.data(QueryDefinitions, "example", QueryDefinitionsConfig(
            max_results=1,
            query_definition_name_prefix="example",
            query_language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_definition_name_prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_language" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="query_definitions" type="list(object)">
  Returned by the lookup.
</ResponseField>
