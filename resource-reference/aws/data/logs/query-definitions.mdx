---
title: "aws_logs_query_definitions"
description: "`data_aws_logs_query_definitions` looks up a real, already-existing `aws_logs_query_definition`."
---

It saves a real, named CloudWatch Logs Insights query for reuse from the console or API, rather than re-typing it for every ad hoc investigation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_query_definitions"})

    		ubx.Data(logs.QueryDefinitions, "example", logs.QueryDefinitionsConfig{
    			MaxResults:                1,
    			QueryDefinitionNamePrefix: "example",
    			QueryLanguage:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { QueryDefinitions } from "./local-sdk/aws/sdk/typescript/aws/data/logs/query_definitions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_query_definitions" });

      data(QueryDefinitions, "example", {
        maxResults: 1,
        queryDefinitionNamePrefix: "example",
        queryLanguage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.logs import QueryDefinitions, QueryDefinitionsConfig

    def describe():
        ubx.intent("look up aws_logs_query_definitions")

        ubx.data(QueryDefinitions, "example", QueryDefinitionsConfig(
            max_results=1,
            query_definition_name_prefix="example",
            query_language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_definition_name_prefix" type="string">
  The real S3 key prefix used for the query definition name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_language" type="string">
  The real query language this request is expressed in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="query_definitions" type="list(object)">
  The real CloudWatch Logs Insights query definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
