---
title: "aws_logs_tags_log_group"
description: "`aws_logs_tags_log_group` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_logs_tags_log_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_tags_log_group"})

    		ubx.Data(logs.TagsLogGroup, "example", logs.TagsLogGroupConfig{
    			LogGroupName: "example-log-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsLogGroup } from "@ubx/sdk-aws/aws/data/logs/tags_log_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_tags_log_group" });

      data(TagsLogGroup, "example", {
        logGroupName: "example-log-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import TagsLogGroup, TagsLogGroupConfig

    def describe():
        ubx.intent("look up aws_logs_tags_log_group")

        ubx.data(TagsLogGroup, "example", TagsLogGroupConfig(
            log_group_name="example-log-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="log_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
