---
title: "aws_logs_log_group_fields"
description: "`aws_logs_log_group_fields` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_logs_log_group_fields` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_log_group_fields"})

    		ubx.Data(logs.LogGroupFields, "example", logs.LogGroupFieldsConfig{
    			LogGroupIdentifier: "example",
    			LogGroupName:       "example-log-group-name",
    			Time:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LogGroupFields } from "@ubx/sdk-aws/aws/data/logs/log_group_fields";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_log_group_fields" });

      data(LogGroupFields, "example", {
        logGroupIdentifier: "example",
        logGroupName: "example-log-group-name",
        time: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import LogGroupFields, LogGroupFieldsConfig

    def describe():
        ubx.intent("look up aws_logs_log_group_fields")

        ubx.data(LogGroupFields, "example", LogGroupFieldsConfig(
            log_group_identifier="example",
            log_group_name="example-log-group-name",
            time=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="log_group_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="log_group_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="log_group_fields" type="list(object)">
  Returned by the lookup.
</ResponseField>
