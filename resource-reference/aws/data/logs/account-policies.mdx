---
title: "aws_logs_account_policies"
description: "`aws_logs_account_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_logs_account_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_account_policies"})

    		ubx.Data(logs.AccountPolicies, "example", logs.AccountPoliciesConfig{
    			PolicyType:         "example",
    			AccountIdentifiers: []string{"example"},
    			PolicyName:         "example-policy-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountPolicies } from "@ubx/sdk-aws/aws/data/logs/account_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_account_policies" });

      data(AccountPolicies, "example", {
        policyType: "example",
        accountIdentifiers: ["example"],
        policyName: "example-policy-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import AccountPolicies, AccountPoliciesConfig

    def describe():
        ubx.intent("look up aws_logs_account_policies")

        ubx.data(AccountPolicies, "example", AccountPoliciesConfig(
            policy_type="example",
            account_identifiers=["example"],
            policy_name="example-policy-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_identifiers" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="policy_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="policy_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>
