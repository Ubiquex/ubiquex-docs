---
title: "aws_logs_configuration_templates"
description: "`aws_logs_configuration_templates` reads real, published CloudWatch Logs delivery configuration templates."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_logs_configuration_templates"})

    		ubx.Data(logs.ConfigurationTemplates, "example", logs.ConfigurationTemplatesConfig{
    			DeliveryDestinationTypes: []string{"example"},
    			Limit:                    1,
    			LogTypes:                 []string{"example"},
    			ResourceTypes:            []string{"example"},
    			Service:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationTemplates } from "@ubx/sdk-aws/aws/data/logs/configuration_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_logs_configuration_templates" });

      data(ConfigurationTemplates, "example", {
        deliveryDestinationTypes: ["example"],
        limit: 1,
        logTypes: ["example"],
        resourceTypes: ["example"],
        service: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.logs import ConfigurationTemplates, ConfigurationTemplatesConfig

    def describe():
        ubx.intent("look up aws_logs_configuration_templates")

        ubx.data(ConfigurationTemplates, "example", ConfigurationTemplatesConfig(
            delivery_destination_types=["example"],
            limit=1,
            log_types=["example"],
            resource_types=["example"],
            service="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="delivery_destination_types" type="list(string)">
  The real, discriminating types of delivery destinations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="log_types" type="list(string)">
  The real, discriminating types of logs this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string">
  The real AWS service this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_templates" type="list(object)">
  The real Elastic Beanstalk configuration templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token for the next set of items to return. The token expires after 24 hours.&lt;/p&gt; Returned by the lookup.
</ResponseField>
