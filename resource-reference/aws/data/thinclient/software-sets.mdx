---
title: "aws_thinclient_software_sets"
description: "`aws_thinclient_software_sets` reads the real, available WorkSpaces Thin Client software sets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	thinclient "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/thinclient"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_thinclient_software_sets"})

    		ubx.Data(thinclient.SoftwareSets, "example", thinclient.SoftwareSetsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SoftwareSets } from "./local-sdk/aws/sdk/typescript/aws/data/thinclient/software_sets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_thinclient_software_sets" });

      data(SoftwareSets, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.thinclient import SoftwareSets, SoftwareSetsConfig

    def describe():
        ubx.intent("look up aws_thinclient_software_sets")

        ubx.data(SoftwareSets, "example", SoftwareSetsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="software_sets" type="list(object)">
  The real software sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
