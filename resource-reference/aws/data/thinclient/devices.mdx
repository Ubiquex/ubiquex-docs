---
title: "aws_thinclient_devices"
description: "`aws_thinclient_devices` reads the real WorkSpaces Thin Client devices on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	thinclient "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/thinclient"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_thinclient_devices"})

    		ubx.Data(thinclient.Devices, "example", thinclient.DevicesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Devices } from "@ubx/sdk-aws/aws/data/thinclient/devices";

    export default stack("example", () => {
      intent({ summary: "look up aws_thinclient_devices" });

      data(Devices, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.thinclient import Devices, DevicesConfig

    def describe():
        ubx.intent("look up aws_thinclient_devices")

        ubx.data(Devices, "example", DevicesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="devices" type="list(object)">
  The real devices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
