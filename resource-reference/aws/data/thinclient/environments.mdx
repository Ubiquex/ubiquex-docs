---
title: "aws_thinclient_environments"
description: "`aws_thinclient_environments` looks up a real WorkSpaces Thin Client environment, defining which virtual desktop provider its own devices connect to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	thinclient "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/thinclient"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_thinclient_environments"})

    		ubx.Data(thinclient.Environments, "example", thinclient.EnvironmentsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Environments } from "@ubx/sdk-aws/aws/data/thinclient/environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_thinclient_environments" });

      data(Environments, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.thinclient import Environments, EnvironmentsConfig

    def describe():
        ubx.intent("look up aws_thinclient_environments")

        ubx.data(Environments, "example", EnvironmentsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="environments" type="list(object)">
  The real environments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
