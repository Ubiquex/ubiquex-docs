---
title: "aws_connect_campaigns_connect_instance_config"
description: "`aws_connect_campaigns_connect_instance_config` reads the real Connect outbound campaigns configuration of a Connect instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect_campaigns "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect_campaigns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_campaigns_connect_instance_config"})

    		ubx.Data(connect_campaigns.ConnectCampaignsConnectInstanceConfig, "example", connect_campaigns.ConnectCampaignsConnectInstanceConfigConfig{
    			ConnectInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectCampaignsConnectInstanceConfig } from "@ubx/sdk-aws/aws/data/connect_campaigns/connect_campaigns_connect_instance_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_campaigns_connect_instance_config" });

      data(ConnectCampaignsConnectInstanceConfig, "example", {
        connectInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect_campaigns import ConnectCampaignsConnectInstanceConfig, ConnectCampaignsConnectInstanceConfigConfig

    def describe():
        ubx.intent("look up aws_connect_campaigns_connect_instance_config")

        ubx.data(ConnectCampaignsConnectInstanceConfig, "example", ConnectCampaignsConnectInstanceConfigConfig(
            connect_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connect_instance_id" type="string" required>
  Amazon Connect Instance Id Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connect_instance_config" type="object">
  Instance config object Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="connect_instance_id" type="string" required>
      Amazon Connect Instance Id Required lookup argument.
    </ResponseField>
    <ResponseField name="encryption_config" type="object" required>
      Encryption config for Connect Instance. Note that sensitive data will always be encrypted. If disabled, service will perform encryption with its own key. If enabled, a KMS key id needs to be provided and KMS charges will apply. KMS is only type supported Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool" required>
          Boolean to indicate if custom encryption has been enabled. Required lookup argument.
        </ResponseField>
        <ResponseField name="encryption_type" type="string">
          Server-side encryption type. Optional lookup argument.
        </ResponseField>
        <ResponseField name="key_arn" type="string">
          KMS key id/arn for encryption config. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_linked_role_arn" type="string" required>
      Service linked role arn Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
