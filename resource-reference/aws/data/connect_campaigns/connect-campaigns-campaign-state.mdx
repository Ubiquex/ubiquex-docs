---
title: "aws_connect_campaigns_campaign_state"
description: "`aws_connect_campaigns_campaign_state` reads the real, current state of a Connect outbound campaign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect_campaigns "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/connect_campaigns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_campaigns_campaign_state"})

    		ubx.Data(connect_campaigns.ConnectCampaignsCampaignState, "example", connect_campaigns.ConnectCampaignsCampaignStateConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectCampaignsCampaignState } from "@ubx/sdk-aws/aws/data/connect_campaigns/connect_campaigns_campaign_state";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_campaigns_campaign_state" });

      data(ConnectCampaignsCampaignState, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect_campaigns import ConnectCampaignsCampaignState, ConnectCampaignsCampaignStateConfig

    def describe():
        ubx.intent("look up aws_connect_campaigns_campaign_state")

        ubx.data(ConnectCampaignsCampaignState, "example", ConnectCampaignsCampaignStateConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Identifier representing a Campaign Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="state" type="string">
  State of a campaign Returned by the lookup.
</ResponseField>
