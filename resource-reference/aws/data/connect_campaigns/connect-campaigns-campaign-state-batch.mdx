---
title: "aws_connect_campaigns_campaign_state_batch"
description: "`aws_connect_campaigns_campaign_state_batch` reads the real, current state of a batch of Connect outbound campaigns."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect_campaigns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect_campaigns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_campaigns_campaign_state_batch"})

    		ubx.Data(connect_campaigns.ConnectCampaignsCampaignStateBatch, "example", connect_campaigns.ConnectCampaignsCampaignStateBatchConfig{
    			CampaignIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConnectCampaignsCampaignStateBatch } from "./local-sdk/aws/sdk/typescript/aws/data/connect_campaigns/connect_campaigns_campaign_state_batch.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_campaigns_campaign_state_batch" });

      data(ConnectCampaignsCampaignStateBatch, "example", {
        campaignIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.connect_campaigns import ConnectCampaignsCampaignStateBatch, ConnectCampaignsCampaignStateBatchConfig

    def describe():
        ubx.intent("look up aws_connect_campaigns_campaign_state_batch")

        ubx.data(ConnectCampaignsCampaignStateBatch, "example", ConnectCampaignsCampaignStateBatchConfig(
            campaign_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="campaign_ids" type="list(string)" required>
  List of CampaignId Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failed_requests" type="list(object)">
  List of failed requests of campaign state Returned by the lookup.
</ResponseField>

<ResponseField name="successful_requests" type="list(object)">
  List of successful response of campaign state Returned by the lookup.
</ResponseField>
