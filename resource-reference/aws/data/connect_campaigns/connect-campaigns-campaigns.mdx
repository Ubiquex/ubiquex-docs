---
title: "aws_connect_campaigns_campaigns"
description: "`aws_connect_campaigns_campaigns` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_connect_campaigns_campaigns` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect_campaigns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/connect_campaigns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_connect_campaigns_campaigns"})

    		ubx.Data(connect_campaigns.ConnectCampaignsCampaigns, "example", connect_campaigns.ConnectCampaignsCampaignsConfig{
    			Filters:    map[string]any{"instance_id_filter": map[string]any{"operator": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectCampaignsCampaigns } from "@ubx/sdk-aws/aws/data/connect_campaigns/connect_campaigns_campaigns";

    export default stack("example", () => {
      intent({ summary: "look up aws_connect_campaigns_campaigns" });

      data(ConnectCampaignsCampaigns, "example", {
        filters: { instanceIdFilter: { operator: "example" } },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.connect_campaigns import ConnectCampaignsCampaigns, ConnectCampaignsCampaignsConfig

    def describe():
        ubx.intent("look up aws_connect_campaigns_campaigns")

        ubx.data(ConnectCampaignsCampaigns, "example", ConnectCampaignsCampaignsConfig(
            filters={"instance_id_filter": {"operator": "example"}},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  Filter model by type Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="instance_id_filter" type="object">
      Connect instance identifier filter Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="operator" type="string" required>
          Operators for Connect instance identifier filter Required lookup argument.
        </ResponseField>
        <ResponseField name="value" type="string" required>
          Amazon Connect Instance Id Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return per page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="campaign_summary_list" type="list(object)">
  A list of Amazon Connect campaigns. Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  The token for the next set of results. Returned by the lookup.
</ResponseField>
