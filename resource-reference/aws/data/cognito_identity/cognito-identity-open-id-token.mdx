---
title: "aws_cognito_identity_open_id_token"
description: "`aws_cognito_identity_open_id_token` reads a real, generated OpenID Connect token for a Cognito identity pool identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_identity_open_id_token"})

    		ubx.Data(cognito_identity.CognitoIdentityOpenIdToken, "example", cognito_identity.CognitoIdentityOpenIdTokenConfig{
    			IdentityId: "example",
    			Logins:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdentityOpenIdToken } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_identity/cognito_identity_open_id_token.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_identity_open_id_token" });

      data(CognitoIdentityOpenIdToken, "example", {
        identityId: "example",
        logins: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_identity import CognitoIdentityOpenIdToken, CognitoIdentityOpenIdTokenConfig

    def describe():
        ubx.intent("look up aws_cognito_identity_open_id_token")

        ubx.data(CognitoIdentityOpenIdToken, "example", CognitoIdentityOpenIdTokenConfig(
            identity_id="example",
            logins={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_id" type="string" required>
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="logins" type="map(string)">
  The real login events included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
