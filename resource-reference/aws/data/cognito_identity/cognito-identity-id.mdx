---
title: "aws_cognito_identity_id"
description: "`aws_cognito_identity_id` reads a real, generated identity ID for a Cognito identity pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_identity_id"})

    		ubx.Data(cognito_identity.CognitoIdentityId, "example", cognito_identity.CognitoIdentityIdConfig{
    			IdentityPoolId: "example",
    			AccountId:      "example",
    			Logins:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdentityId } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_identity/cognito_identity_id.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_identity_id" });

      data(CognitoIdentityId, "example", {
        identityPoolId: "example",
        accountId: "example",
        logins: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_identity import CognitoIdentityId, CognitoIdentityIdConfig

    def describe():
        ubx.intent("look up aws_cognito_identity_id")

        ubx.data(CognitoIdentityId, "example", CognitoIdentityIdConfig(
            identity_pool_id="example",
            account_id="example",
            logins={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito identity pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="logins" type="map(string)">
  The real login events included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="identity_id" type="string">
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
