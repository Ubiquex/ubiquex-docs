---
title: "aws_cognito_identity_credentials_for_identity"
description: "`aws_cognito_identity_credentials_for_identity` reads real, temporary AWS credentials for a Cognito identity pool identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_identity_credentials_for_identity"})

    		ubx.Data(cognito_identity.CognitoIdentityCredentialsForIdentity, "example", cognito_identity.CognitoIdentityCredentialsForIdentityConfig{
    			IdentityId:    "example",
    			CustomRoleArn: "arn:aws:iam::123456789012:policy/example",
    			Logins:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoIdentityCredentialsForIdentity } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_identity/cognito_identity_credentials_for_identity.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_identity_credentials_for_identity" });

      data(CognitoIdentityCredentialsForIdentity, "example", {
        identityId: "example",
        customRoleArn: "arn:aws:iam::123456789012:policy/example",
        logins: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_identity import CognitoIdentityCredentialsForIdentity, CognitoIdentityCredentialsForIdentityConfig

    def describe():
        ubx.intent("look up aws_cognito_identity_credentials_for_identity")

        ubx.data(CognitoIdentityCredentialsForIdentity, "example", CognitoIdentityCredentialsForIdentityConfig(
            identity_id="example",
            custom_role_arn="arn:aws:iam::123456789012:policy/example",
            logins={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="custom_role_arn" type="string">
  The real Amazon Resource Name (ARN) of the custom role this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identity_id" type="string" required>
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="logins" type="map(string)">
  The real login events included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credentials" type="object">
  &lt;p&gt;Credentials for the provided identity ID.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expiration" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="secret_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
