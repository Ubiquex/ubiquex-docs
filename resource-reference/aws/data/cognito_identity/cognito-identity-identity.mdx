---
title: "aws_cognito_identity_identity"
description: "`aws_cognito_identity_identity` looks up one real identity within a Cognito identity pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_identity_identity"})

    		ubx.Data(cognito_identity.CognitoIdentityIdentity, "example", cognito_identity.CognitoIdentityIdentityConfig{
    			IdentityId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdentityIdentity } from "@ubx/sdk-aws/aws/data/cognito_identity/cognito_identity_identity";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_identity_identity" });

      data(CognitoIdentityIdentity, "example", {
        identityId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_identity import CognitoIdentityIdentity, CognitoIdentityIdentityConfig

    def describe():
        ubx.intent("look up aws_cognito_identity_identity")

        ubx.data(CognitoIdentityIdentity, "example", CognitoIdentityIdentityConfig(
            identity_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_id" type="string" required>
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="logins" type="list(string)">
  The real login events included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
