---
title: "aws_cognito_identity_principal_tag_attribute_map"
description: "`aws_cognito_identity_principal_tag_attribute_map` reads the real mapping from identity provider attributes to principal tags for a Cognito identity pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_identity "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_identity_principal_tag_attribute_map"})

    		ubx.Data(cognito_identity.CognitoIdentityPrincipalTagAttributeMap, "example", cognito_identity.CognitoIdentityPrincipalTagAttributeMapConfig{
    			IdentityPoolId:       "example",
    			IdentityProviderName: "example-identity-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoIdentityPrincipalTagAttributeMap } from "@ubx/sdk-aws/aws/data/cognito_identity/cognito_identity_principal_tag_attribute_map";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cognito_identity_principal_tag_attribute_map",
      });

      data(CognitoIdentityPrincipalTagAttributeMap, "example", {
        identityPoolId: "example",
        identityProviderName: "example-identity-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_identity import CognitoIdentityPrincipalTagAttributeMap, CognitoIdentityPrincipalTagAttributeMapConfig

    def describe():
        ubx.intent("look up aws_cognito_identity_principal_tag_attribute_map")

        ubx.data(CognitoIdentityPrincipalTagAttributeMap, "example", CognitoIdentityPrincipalTagAttributeMapConfig(
            identity_pool_id="example",
            identity_provider_name="example-identity-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito identity pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  The real name of the identity provider this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="principal_tags" type="map(string)">
  The real principal tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="use_defaults" type="bool">
  Whether default values are used for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
