---
title: "aws_cognito_sync_bulk_publish_details"
description: "`aws_cognito_sync_bulk_publish_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cognito_sync_bulk_publish_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_sync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_sync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_sync_bulk_publish_details"})

    		ubx.Data(cognito_sync.CognitoSyncBulkPublishDetails, "example", cognito_sync.CognitoSyncBulkPublishDetailsConfig{
    			IdentityPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoSyncBulkPublishDetails } from "@ubx/sdk-aws/aws/data/cognito_sync/cognito_sync_bulk_publish_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_sync_bulk_publish_details" });

      data(CognitoSyncBulkPublishDetails, "example", {
        identityPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_sync import CognitoSyncBulkPublishDetails, CognitoSyncBulkPublishDetailsConfig

    def describe():
        ubx.intent("look up aws_cognito_sync_bulk_publish_details")

        ubx.data(CognitoSyncBulkPublishDetails, "example", CognitoSyncBulkPublishDetailsConfig(
            identity_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_pool_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bulk_publish_complete_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="bulk_publish_start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="bulk_publish_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_message" type="string">
  Returned by the lookup.
</ResponseField>
