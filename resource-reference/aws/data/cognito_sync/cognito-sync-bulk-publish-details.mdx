---
title: "aws_cognito_sync_bulk_publish_details"
description: "`aws_cognito_sync_bulk_publish_details` reads the real, current status of a Cognito Sync bulk publish operation, a retired AWS feature."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito_sync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_sync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_sync_bulk_publish_details"})

    		ubx.Data(cognito_sync.CognitoSyncBulkPublishDetails, "example", cognito_sync.CognitoSyncBulkPublishDetailsConfig{
    			IdentityPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CognitoSyncBulkPublishDetails } from "./local-sdk/aws/sdk/typescript/aws/data/cognito_sync/cognito_sync_bulk_publish_details.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_sync_bulk_publish_details" });

      data(CognitoSyncBulkPublishDetails, "example", {
        identityPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_sync import CognitoSyncBulkPublishDetails, CognitoSyncBulkPublishDetailsConfig

    def describe():
        ubx.intent("look up aws_cognito_sync_bulk_publish_details")

        ubx.data(CognitoSyncBulkPublishDetails, "example", CognitoSyncBulkPublishDetailsConfig(
            identity_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito identity pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bulk_publish_complete_time" type="string">
  The real timestamp of the bulk publish complete. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bulk_publish_start_time" type="string">
  The real timestamp of the bulk publish start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bulk_publish_status" type="string">
  The real, current status of the bulk publish this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_message" type="string">
  The real, human-readable message describing the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>
