---
title: "aws_cognito_sync_datasets"
description: "`aws_cognito_sync_datasets` reads the real Cognito Sync datasets for an identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_sync "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cognito_sync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_sync_datasets"})

    		ubx.Data(cognito_sync.CognitoSyncDatasets, "example", cognito_sync.CognitoSyncDatasetsConfig{
    			IdentityId:     "example",
    			IdentityPoolId: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoSyncDatasets } from "@ubx/sdk-aws/aws/data/cognito_sync/cognito_sync_datasets";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_sync_datasets" });

      data(CognitoSyncDatasets, "example", {
        identityId: "example",
        identityPoolId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_sync import CognitoSyncDatasets, CognitoSyncDatasetsConfig

    def describe():
        ubx.intent("look up aws_cognito_sync_datasets")

        ubx.data(CognitoSyncDatasets, "example", CognitoSyncDatasetsConfig(
            identity_id="example",
            identity_pool_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_id" type="string" required>
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito identity pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  The real, total count of matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="datasets" type="list(object)">
  The real datasets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
