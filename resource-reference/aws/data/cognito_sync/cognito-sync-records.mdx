---
title: "aws_cognito_sync_records"
description: "`aws_cognito_sync_records` reads the real key-value records within a Cognito Sync dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito_sync "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cognito_sync"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cognito_sync_records"})

    		ubx.Data(cognito_sync.CognitoSyncRecords, "example", cognito_sync.CognitoSyncRecordsConfig{
    			DatasetName:    "example-dataset-name",
    			IdentityId:     "example",
    			IdentityPoolId: "example",
    			LastSyncCount:  1,
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CognitoSyncRecords } from "@ubx/sdk-aws/aws/data/cognito_sync/cognito_sync_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_cognito_sync_records" });

      data(CognitoSyncRecords, "example", {
        datasetName: "example-dataset-name",
        identityId: "example",
        identityPoolId: "example",
        lastSyncCount: 1,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cognito_sync import CognitoSyncRecords, CognitoSyncRecordsConfig

    def describe():
        ubx.intent("look up aws_cognito_sync_records")

        ubx.data(CognitoSyncRecords, "example", CognitoSyncRecordsConfig(
            dataset_name="example-dataset-name",
            identity_id="example",
            identity_pool_id="example",
            last_sync_count=1,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_name" type="string" required>
  The real name of the dataset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identity_id" type="string" required>
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  The real, unique identifier of the Amazon Cognito identity pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="last_sync_count" type="number">
  The real count of last syncs. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  The real, total count of matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_deleted_after_requested_sync_count" type="bool">
  The real count of dataset deleted after requested syncs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_exists" type="bool">
  Whether this dataset already exists. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_sync_count" type="number">
  The real count of dataset syncs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_by" type="string">
  The real identity that last modified this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="merged_dataset_names" type="list(string)">
  The real names of the merged datasets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  The real records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sync_session_token" type="string">
  The real, opaque sync session token. **(AI-inferred)** Returned by the lookup.
</ResponseField>
