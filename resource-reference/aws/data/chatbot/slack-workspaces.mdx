---
title: "aws_chatbot_slack_workspaces"
description: "`aws_chatbot_slack_workspaces` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_chatbot_slack_workspaces` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chatbot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chatbot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chatbot_slack_workspaces"})

    		ubx.Data(chatbot.SlackWorkspaces, "example", chatbot.SlackWorkspacesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SlackWorkspaces } from "@ubx/sdk-aws/aws/data/chatbot/slack_workspaces";

    export default stack("example", () => {
      intent({ summary: "look up aws_chatbot_slack_workspaces" });

      data(SlackWorkspaces, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chatbot import SlackWorkspaces, SlackWorkspacesConfig

    def describe():
        ubx.intent("look up aws_chatbot_slack_workspaces")

        ubx.data(SlackWorkspaces, "example", SlackWorkspacesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="slack_workspaces" type="list(object)">
  Returned by the lookup.
</ResponseField>
