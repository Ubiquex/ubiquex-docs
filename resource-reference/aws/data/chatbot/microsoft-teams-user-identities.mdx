---
title: "aws_chatbot_microsoft_teams_user_identities"
description: "`aws_chatbot_microsoft_teams_user_identities` reads the real Microsoft Teams user identities linked to AWS Chatbot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chatbot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chatbot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chatbot_microsoft_teams_user_identities"})

    		ubx.Data(chatbot.MicrosoftTeamsUserIdentities, "example", chatbot.MicrosoftTeamsUserIdentitiesConfig{
    			ChatConfigurationArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MicrosoftTeamsUserIdentities } from "@ubx/sdk-aws/aws/data/chatbot/microsoft_teams_user_identities";

    export default stack("example", () => {
      intent({ summary: "look up aws_chatbot_microsoft_teams_user_identities" });

      data(MicrosoftTeamsUserIdentities, "example", {
        chatConfigurationArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chatbot import MicrosoftTeamsUserIdentities, MicrosoftTeamsUserIdentitiesConfig

    def describe():
        ubx.intent("look up aws_chatbot_microsoft_teams_user_identities")

        ubx.data(MicrosoftTeamsUserIdentities, "example", MicrosoftTeamsUserIdentitiesConfig(
            chat_configuration_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="chat_configuration_arn" type="string">
  The real Amazon Resource Name (ARN) of the chat configuration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="teams_user_identities" type="list(object)">
  The real Microsoft Teams user identities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
