---
title: "aws_chatbot_associations"
description: "`aws_chatbot_associations` reads the real resources associated with an AWS Chatbot channel configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	chatbot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chatbot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chatbot_associations"})

    		ubx.Data(chatbot.Associations, "example", chatbot.AssociationsConfig{
    			ChatConfiguration: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Associations } from "./local-sdk/aws/sdk/typescript/aws/data/chatbot/associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_chatbot_associations" });

      data(Associations, "example", {
        chatConfiguration: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.chatbot import Associations, AssociationsConfig

    def describe():
        ubx.intent("look up aws_chatbot_associations")

        ubx.data(Associations, "example", AssociationsConfig(
            chat_configuration="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="chat_configuration" type="string" required>
  The real configuration of the chat. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  The real associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
