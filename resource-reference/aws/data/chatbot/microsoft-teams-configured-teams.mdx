---
title: "aws_chatbot_microsoft_teams_configured_teams"
description: "`aws_chatbot_microsoft_teams_configured_teams` reads the real Microsoft Teams teams configured with AWS Chatbot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chatbot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/chatbot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chatbot_microsoft_teams_configured_teams"})

    		ubx.Data(chatbot.MicrosoftTeamsConfiguredTeams, "example", chatbot.MicrosoftTeamsConfiguredTeamsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MicrosoftTeamsConfiguredTeams } from "@ubx/sdk-aws/aws/data/chatbot/microsoft_teams_configured_teams";

    export default stack("example", () => {
      intent({ summary: "look up aws_chatbot_microsoft_teams_configured_teams" });

      data(MicrosoftTeamsConfiguredTeams, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chatbot import MicrosoftTeamsConfiguredTeams, MicrosoftTeamsConfiguredTeamsConfig

    def describe():
        ubx.intent("look up aws_chatbot_microsoft_teams_configured_teams")

        ubx.data(MicrosoftTeamsConfiguredTeams, "example", MicrosoftTeamsConfiguredTeamsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configured_teams" type="list(object)">
  The real teams configured for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
