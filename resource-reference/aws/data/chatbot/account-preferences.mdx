---
title: "aws_chatbot_account_preferences"
description: "`aws_chatbot_account_preferences` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_chatbot_account_preferences` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chatbot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/chatbot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_chatbot_account_preferences"})

    		ubx.Data(chatbot.AccountPreferences, "example", chatbot.AccountPreferencesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountPreferences } from "@ubx/sdk-aws/aws/data/chatbot/account_preferences";

    export default stack("example", () => {
      intent({ summary: "look up aws_chatbot_account_preferences" });

      data(AccountPreferences, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.chatbot import AccountPreferences, AccountPreferencesConfig

    def describe():
        ubx.intent("look up aws_chatbot_account_preferences")

        ubx.data(AccountPreferences, "example", AccountPreferencesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_preferences" type="object">
  &lt;p&gt;Preferences related to AWS Chatbot usage in the calling AWS account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="training_data_collection_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_authorization_required" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
