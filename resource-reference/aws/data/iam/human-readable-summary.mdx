---
title: "aws_iam_human_readable_summary"
description: "`aws_iam_human_readable_summary` reads a real, plain-language summary of what an IAM policy actually grants."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_human_readable_summary"})

    		ubx.Data(iam.HumanReadableSummary, "example", iam.HumanReadableSummaryConfig{
    			EntityArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HumanReadableSummary } from "@ubx/sdk-aws/aws/data/iam/human_readable_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_human_readable_summary" });

      data(HumanReadableSummary, "example", {
        entityArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import HumanReadableSummary, HumanReadableSummaryConfig

    def describe():
        ubx.intent("look up aws_iam_human_readable_summary")

        ubx.data(HumanReadableSummary, "example", HumanReadableSummaryConfig(
            entity_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN). ARNs are unique identifiers for Amazon Web Services resources.&lt;/p&gt; &lt;p&gt;For more information about ARNs, go to &lt;a href="https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html"&gt;Amazon Resource Names (ARNs)&lt;/a&gt; in the &lt;i&gt;Amazon Web Services General Reference&lt;/i&gt;. &lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary_content" type="string">
  The real content of the summary. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary_state" type="string">
  The real, current state of the summary this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
