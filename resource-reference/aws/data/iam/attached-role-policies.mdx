---
title: "aws_iam_attached_role_policies"
description: "`aws_iam_attached_role_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_attached_role_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_attached_role_policies"})

    		ubx.Data(iam.AttachedRolePolicies, "example", iam.AttachedRolePoliciesConfig{
    			RoleName:   "example-role-name",
    			MaxItems:   1,
    			PathPrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachedRolePolicies } from "@ubx/sdk-aws/aws/data/iam/attached_role_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_attached_role_policies" });

      data(AttachedRolePolicies, "example", {
        roleName: "example-role-name",
        maxItems: 1,
        pathPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import AttachedRolePolicies, AttachedRolePoliciesConfig

    def describe():
        ubx.intent("look up aws_iam_attached_role_policies")

        ubx.data(AttachedRolePolicies, "example", AttachedRolePoliciesConfig(
            role_name="example-role-name",
            max_items=1,
            path_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="path_prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attached_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
