---
title: "aws_iam_account_summary"
description: "`aws_iam_account_summary` reads real, aggregated IAM usage counts (users, roles, policies) and quotas for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_account_summary"})

    		ubx.Data(iam.AccountSummary, "example", iam.AccountSummaryConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountSummary } from "@ubx/sdk-aws/aws/data/iam/account_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_account_summary" });

      data(AccountSummary, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import AccountSummary, AccountSummaryConfig

    def describe():
        ubx.intent("look up aws_iam_account_summary")

        ubx.data(AccountSummary, "example", AccountSummaryConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="summary_map" type="map(number)">
  The real, structured summary mapping for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
