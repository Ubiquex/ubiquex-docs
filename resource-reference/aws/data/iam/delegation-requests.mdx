---
title: "aws_iam_delegation_requests"
description: "`aws_iam_delegation_requests` reads real, pending IAM Identity Center delegation requests."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_delegation_requests"})

    		ubx.Data(iam.DelegationRequests, "example", iam.DelegationRequestsConfig{
    			MaxItems: 1,
    			OwnerId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DelegationRequests } from "@ubx/sdk-aws/aws/data/iam/delegation_requests";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_delegation_requests" });

      data(DelegationRequests, "example", {
        maxItems: 1,
        ownerId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import DelegationRequests, DelegationRequestsConfig

    def describe():
        ubx.intent("look up aws_iam_delegation_requests")

        ubx.data(DelegationRequests, "example", DelegationRequestsConfig(
            max_items=1,
            owner_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner_id" type="string">
  The real, unique identifier of the owner this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delegation_requests" type="list(object)">
  The real delegation requests included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
