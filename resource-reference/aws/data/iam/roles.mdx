---
title: "aws_iam_roles"
description: "`data_aws_iam_roles` looks up a real, already-existing `aws_iam_role`: An IAM role grants a set of permissions to whatever's allowed to assume..."
---

`data_aws_iam_roles` looks up a real, already-existing `aws_iam_role`: An IAM role grants a set of permissions to whatever's allowed to assume it, an EC2 instance, a Lambda function, another AWS account. `aws_iam_role` creates the role and its trust policy, real permissions are attached separately, via `aws_iam_role_policy_attachment`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_roles"})

    		ubx.Data(iam.Roles, "example", iam.RolesConfig{
    			MaxItems:   1,
    			PathPrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Roles } from "./local-sdk/aws/sdk/typescript/aws/data/iam/roles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_roles" });

      data(Roles, "example", {
        maxItems: 1,
        pathPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import Roles, RolesConfig

    def describe():
        ubx.intent("look up aws_iam_roles")

        ubx.data(Roles, "example", RolesConfig(
            max_items=1,
            path_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path_prefix" type="string">
  Restricts results to objects whose path starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="roles" type="list(object)">
  &lt;p&gt;Contains a list of IAM roles.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_ListRoles.html"&gt;ListRoles&lt;/a&gt; operation.&lt;/p&gt; Returned by the lookup.
</ResponseField>
