---
title: "aws_iam_account_authorization_details"
description: "`aws_iam_account_authorization_details` reads a real, complete snapshot of every IAM user, group, role, and policy on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_account_authorization_details"})

    		ubx.Data(iam.AccountAuthorizationDetails, "example", iam.AccountAuthorizationDetailsConfig{
    			Filter:   []string{"example"},
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAuthorizationDetails } from "@ubx/sdk-aws/aws/data/iam/account_authorization_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_account_authorization_details" });

      data(AccountAuthorizationDetails, "example", {
        filter: ["example"],
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import AccountAuthorizationDetails, AccountAuthorizationDetailsConfig

    def describe():
        ubx.intent("look up aws_iam_account_authorization_details")

        ubx.data(AccountAuthorizationDetails, "example", AccountAuthorizationDetailsConfig(
            filter=["example"],
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="list(string)">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_detail_list" type="list(object)">
  The real group details included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policies" type="list(object)">
  The real access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_detail_list" type="list(object)">
  The real role details included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_detail_list" type="list(object)">
  The real user details included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
