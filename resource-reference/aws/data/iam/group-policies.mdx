---
title: "aws_iam_group_policies"
description: "`aws_iam_group_policies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_group_policies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_group_policies"})

    		ubx.Data(iam.GroupPolicies, "example", iam.GroupPoliciesConfig{
    			GroupName: "example-group-name",
    			MaxItems:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupPolicies } from "@ubx/sdk-aws/aws/data/iam/group_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_group_policies" });

      data(GroupPolicies, "example", {
        groupName: "example-group-name",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import GroupPolicies, GroupPoliciesConfig

    def describe():
        ubx.intent("look up aws_iam_group_policies")

        ubx.data(GroupPolicies, "example", GroupPoliciesConfig(
            group_name="example-group-name",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="policy_names" type="list(string)">
  &lt;p&gt;Contains a list of policy names.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_ListPolicies.html"&gt;ListPolicies&lt;/a&gt; operation.&lt;/p&gt; Returned by the lookup.
</ResponseField>
