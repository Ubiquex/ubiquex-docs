---
title: "aws_iam_instance_profile_tags"
description: "`aws_iam_instance_profile_tags` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_instance_profile_tags` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_instance_profile_tags"})

    		ubx.Data(iam.InstanceProfileTags, "example", iam.InstanceProfileTagsConfig{
    			InstanceProfileName: "example-instance-profile-name",
    			MaxItems:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceProfileTags } from "@ubx/sdk-aws/aws/data/iam/instance_profile_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_instance_profile_tags" });

      data(InstanceProfileTags, "example", {
        instanceProfileName: "example-instance-profile-name",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import InstanceProfileTags, InstanceProfileTagsConfig

    def describe():
        ubx.intent("look up aws_iam_instance_profile_tags")

        ubx.data(InstanceProfileTags, "example", InstanceProfileTagsConfig(
            instance_profile_name="example-instance-profile-name",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_profile_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
