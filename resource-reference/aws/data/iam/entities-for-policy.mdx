---
title: "aws_iam_entities_for_policy"
description: "`aws_iam_entities_for_policy` reads the real IAM users, groups, and roles a managed policy is attached to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_entities_for_policy"})

    		ubx.Data(iam.EntitiesForPolicy, "example", iam.EntitiesForPolicyConfig{
    			PolicyArn:         "arn:aws:iam::123456789012:policy/example",
    			EntityFilter:      "example",
    			MaxItems:          1,
    			PathPrefix:        "example",
    			PolicyUsageFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EntitiesForPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/iam/entities_for_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_entities_for_policy" });

      data(EntitiesForPolicy, "example", {
        policyArn: "arn:aws:iam::123456789012:policy/example",
        entityFilter: "example",
        maxItems: 1,
        pathPrefix: "example",
        policyUsageFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import EntitiesForPolicy, EntitiesForPolicyConfig

    def describe():
        ubx.intent("look up aws_iam_entities_for_policy")

        ubx.data(EntitiesForPolicy, "example", EntitiesForPolicyConfig(
            policy_arn="arn:aws:iam::123456789012:policy/example",
            entity_filter="example",
            max_items=1,
            path_prefix="example",
            policy_usage_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_filter" type="string">
  The real filter applied to entities in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path_prefix" type="string">
  Restricts results to objects whose path starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN). ARNs are unique identifiers for Amazon Web Services resources.&lt;/p&gt; &lt;p&gt;For more information about ARNs, go to &lt;a href="https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html"&gt;Amazon Resource Names (ARNs)&lt;/a&gt; in the &lt;i&gt;Amazon Web Services General Reference&lt;/i&gt;. &lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="policy_usage_filter" type="string">
  &lt;p&gt;The policy usage type that indicates whether the policy is used as a permissions policy or as the permissions boundary for an entity.&lt;/p&gt; &lt;p&gt;For more information about permissions boundaries, see &lt;a href="https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html"&gt;Permissions boundaries for IAM identities &lt;/a&gt; in the &lt;i&gt;IAM User Guide&lt;/i&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_groups" type="list(object)">
  The real policy groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_roles" type="list(object)">
  The real IAM roles this policy is attached to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_users" type="list(object)">
  The real IAM users this policy is attached to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
