---
title: "aws_iam_mfadevice"
description: "`aws_iam_mfadevice` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_mfadevice` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_mfadevice"})

    		ubx.Data(iam.Mfadevice, "example", iam.MfadeviceConfig{
    			SerialNumber: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Mfadevice } from "@ubx/sdk-aws/aws/data/iam/mfadevice";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_mfadevice" });

      data(Mfadevice, "example", {
        serialNumber: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import Mfadevice, MfadeviceConfig

    def describe():
        ubx.intent("look up aws_iam_mfadevice")

        ubx.data(Mfadevice, "example", MfadeviceConfig(
            serial_number="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="serial_number" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certifications" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="enable_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_name" type="string">
  Returned by the lookup.
</ResponseField>
