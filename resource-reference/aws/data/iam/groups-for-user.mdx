---
title: "aws_iam_groups_for_user"
description: "`aws_iam_groups_for_user` reads the real IAM groups a user belongs to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_groups_for_user"})

    		ubx.Data(iam.GroupsForUser, "example", iam.GroupsForUserConfig{
    			UserName: "example-user-name",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupsForUser } from "@ubx/sdk-aws/aws/data/iam/groups_for_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_groups_for_user" });

      data(GroupsForUser, "example", {
        userName: "example-user-name",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import GroupsForUser, GroupsForUserConfig

    def describe():
        ubx.intent("look up aws_iam_groups_for_user")

        ubx.data(GroupsForUser, "example", GroupsForUserConfig(
            user_name="example-user-name",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  The real username this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  &lt;p&gt;Contains a list of IAM groups.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_ListGroups.html"&gt;ListGroups&lt;/a&gt; operation.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
