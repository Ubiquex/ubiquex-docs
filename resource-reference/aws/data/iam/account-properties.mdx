---
title: "aws_iam_account_properties"
description: "`aws_iam_account_properties` reads real, account-wide IAM properties, such as its own root user status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_account_properties"})

    		ubx.Data(iam.AccountProperties, "example", iam.AccountPropertiesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountProperties } from "@ubx/sdk-aws/aws/data/iam/account_properties";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_account_properties" });

      data(AccountProperties, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import AccountProperties, AccountPropertiesConfig

    def describe():
        ubx.intent("look up aws_iam_account_properties")

        ubx.data(AccountProperties, "example", AccountPropertiesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="properties" type="map(string)">
  The real, structured properties of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
