---
title: "aws_iam_open_idconnect_providers"
description: "`aws_iam_open_idconnect_providers` reads the real OpenID Connect (OIDC) identity providers registered with IAM."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_open_idconnect_providers"})

    		ubx.Data(iam.OpenIdconnectProviders, "example", iam.OpenIdconnectProvidersConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OpenIdconnectProviders } from "./local-sdk/aws/sdk/typescript/aws/data/iam/open_idconnect_providers.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_open_idconnect_providers" });

      data(OpenIdconnectProviders, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import OpenIdconnectProviders, OpenIdconnectProvidersConfig

    def describe():
        ubx.intent("look up aws_iam_open_idconnect_providers")

        ubx.data(OpenIdconnectProviders, "example", OpenIdconnectProvidersConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="open_idconnect_provider_list" type="list(object)">
  &lt;p&gt;Contains a list of IAM OpenID Connect providers.&lt;/p&gt; Returned by the lookup.
</ResponseField>
