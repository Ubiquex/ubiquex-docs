---
title: "aws_iam_service_last_accessed_details"
description: "`aws_iam_service_last_accessed_details` reads real, tracked data on when an IAM principal last accessed a given AWS service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_service_last_accessed_details"})

    		ubx.Data(iam.ServiceLastAccessedDetails, "example", iam.ServiceLastAccessedDetailsConfig{
    			JobId:    "example",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceLastAccessedDetails } from "@ubx/sdk-aws/aws/data/iam/service_last_accessed_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_service_last_accessed_details" });

      data(ServiceLastAccessedDetails, "example", {
        jobId: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import ServiceLastAccessedDetails, ServiceLastAccessedDetailsConfig

    def describe():
        ubx.intent("look up aws_iam_service_last_accessed_details")

        ubx.data(ServiceLastAccessedDetails, "example", ServiceLastAccessedDetailsConfig(
            job_id="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error" type="object">
  &lt;p&gt;Contains information about the reason that the operation failed.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetOrganizationsAccessReport.html"&gt;GetOrganizationsAccessReport&lt;/a&gt;, &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetails.html"&gt;GetServiceLastAccessedDetails&lt;/a&gt;, and &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetailsWithEntities.html"&gt;GetServiceLastAccessedDetailsWithEntities&lt;/a&gt; operations.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_completion_date" type="string">
  The real date of the job completion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_creation_date" type="string">
  The real date of the job creation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_type" type="string">
  The real, discriminating type of the job this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="services_last_accessed" type="list(object)">
  The real, most recent access timestamp for each service this identity used. **(AI-inferred)** Returned by the lookup.
</ResponseField>
