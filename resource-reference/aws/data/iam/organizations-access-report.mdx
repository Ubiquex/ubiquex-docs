---
title: "aws_iam_organizations_access_report"
description: "`aws_iam_organizations_access_report` reads a real, generated IAM Access Analyzer report on unused permissions across an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_organizations_access_report"})

    		ubx.Data(iam.OrganizationsAccessReport, "example", iam.OrganizationsAccessReportConfig{
    			JobId:    "example",
    			MaxItems: 1,
    			SortKey:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationsAccessReport } from "./local-sdk/aws/sdk/typescript/aws/data/iam/organizations_access_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_organizations_access_report" });

      data(OrganizationsAccessReport, "example", {
        jobId: "example",
        maxItems: 1,
        sortKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import OrganizationsAccessReport, OrganizationsAccessReportConfig

    def describe():
        ubx.intent("look up aws_iam_organizations_access_report")

        ubx.data(OrganizationsAccessReport, "example", OrganizationsAccessReportConfig(
            job_id="example",
            max_items=1,
            sort_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_key" type="string">
  The real, unique key of the sort this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_details" type="list(object)">
  The real, detailed information about the access. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="object">
  &lt;p&gt;Contains information about the reason that the operation failed.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetOrganizationsAccessReport.html"&gt;GetOrganizationsAccessReport&lt;/a&gt;, &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetails.html"&gt;GetServiceLastAccessedDetails&lt;/a&gt;, and &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetailsWithEntities.html"&gt;GetServiceLastAccessedDetailsWithEntities&lt;/a&gt; operations.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_completion_date" type="string">
  The real date of the job completion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_creation_date" type="string">
  The real date of the job creation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_services_accessible" type="number">
  The real number of AWS services this identity can access. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_services_not_accessed" type="number">
  The real number of AWS services this identity has not accessed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
