---
title: "aws_iam_outbound_web_identity_federation_info"
description: "`aws_iam_outbound_web_identity_federation_info` reads real information about outbound web identity federation from an IAM role."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_outbound_web_identity_federation_info"})

    		ubx.Data(iam.OutboundWebIdentityFederationInfo, "example", iam.OutboundWebIdentityFederationInfoConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutboundWebIdentityFederationInfo } from "@ubx/sdk-aws/aws/data/iam/outbound_web_identity_federation_info";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_outbound_web_identity_federation_info" });

      data(OutboundWebIdentityFederationInfo, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import OutboundWebIdentityFederationInfo, OutboundWebIdentityFederationInfoConfig

    def describe():
        ubx.intent("look up aws_iam_outbound_web_identity_federation_info")

        ubx.data(OutboundWebIdentityFederationInfo, "example", OutboundWebIdentityFederationInfoConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="issuer_identifier" type="string">
  The real, unique identifier of the issuer this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="jwt_vending_enabled" type="bool">
  Whether the jwt vending is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>
