---
title: "aws_iam_sshpublic_keys"
description: "`aws_iam_sshpublic_keys` reads the real SSH public keys uploaded to an IAM user, for AWS CodeCommit authentication."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_sshpublic_keys"})

    		ubx.Data(iam.SshpublicKeys, "example", iam.SshpublicKeysConfig{
    			MaxItems: 1,
    			UserName: "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SshpublicKeys } from "./local-sdk/aws/sdk/typescript/aws/data/iam/sshpublic_keys.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_sshpublic_keys" });

      data(SshpublicKeys, "example", {
        maxItems: 1,
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import SshpublicKeys, SshpublicKeysConfig

    def describe():
        ubx.intent("look up aws_iam_sshpublic_keys")

        ubx.data(SshpublicKeys, "example", SshpublicKeysConfig(
            max_items=1,
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string">
  The real username this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sshpublic_keys" type="list(object)">
  The real, unique keys of the sshpublics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
