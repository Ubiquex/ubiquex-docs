---
title: "aws_iam_policies"
description: "`data_aws_iam_policies` looks up a real, already-existing `aws_iam_policy`."
---

It creates a standalone, reusable IAM policy, a real JSON document describing what's allowed. It grants nothing on its own, attach it to a role or user with `aws_iam_role_policy_attachment` to actually apply it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_policies"})

    		ubx.Data(iam.Policies, "example", iam.PoliciesConfig{
    			MaxItems:          1,
    			OnlyAttached:      true,
    			PathPrefix:        "example",
    			PolicyUsageFilter: "example",
    			Scope:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Policies } from "@ubx/sdk-aws/aws/data/iam/policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_policies" });

      data(Policies, "example", {
        maxItems: 1,
        onlyAttached: true,
        pathPrefix: "example",
        policyUsageFilter: "example",
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import Policies, PoliciesConfig

    def describe():
        ubx.intent("look up aws_iam_policies")

        ubx.data(Policies, "example", PoliciesConfig(
            max_items=1,
            only_attached=True,
            path_prefix="example",
            policy_usage_filter="example",
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="only_attached" type="bool">
  Whether the response is restricted to attached objects only. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path_prefix" type="string">
  Restricts results to objects whose path starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="policy_usage_filter" type="string">
  &lt;p&gt;The policy usage type that indicates whether the policy is used as a permissions policy or as the permissions boundary for an entity.&lt;/p&gt; &lt;p&gt;For more information about permissions boundaries, see &lt;a href="https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html"&gt;Permissions boundaries for IAM identities &lt;/a&gt; in the &lt;i&gt;IAM User Guide&lt;/i&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string">
  The real scope this lookup is applied at. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policies" type="list(object)">
  The real access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
