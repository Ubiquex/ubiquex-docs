---
title: "aws_iam_policies_granting_service_access"
description: "`aws_iam_policies_granting_service_access` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_policies_granting_service_access` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_policies_granting_service_access"})

    		ubx.Data(iam.PoliciesGrantingServiceAccess, "example", iam.PoliciesGrantingServiceAccessConfig{
    			Arn:               "arn:aws:iam::123456789012:policy/example",
    			ServiceNamespaces: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PoliciesGrantingServiceAccess } from "@ubx/sdk-aws/aws/data/iam/policies_granting_service_access";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_policies_granting_service_access" });

      data(PoliciesGrantingServiceAccess, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        serviceNamespaces: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import PoliciesGrantingServiceAccess, PoliciesGrantingServiceAccessConfig

    def describe():
        ubx.intent("look up aws_iam_policies_granting_service_access")

        ubx.data(PoliciesGrantingServiceAccess, "example", PoliciesGrantingServiceAccessConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            service_namespaces=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN). ARNs are unique identifiers for Amazon Web Services resources.&lt;/p&gt; &lt;p&gt;For more information about ARNs, go to &lt;a href="https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html"&gt;Amazon Resource Names (ARNs)&lt;/a&gt; in the &lt;i&gt;Amazon Web Services General Reference&lt;/i&gt;. &lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="service_namespaces" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="policies_granting_service_access" type="list(object)">
  Returned by the lookup.
</ResponseField>
