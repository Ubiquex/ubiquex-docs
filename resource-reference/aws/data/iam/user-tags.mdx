---
title: "aws_iam_user_tags"
description: "`data_aws_iam_user_tags` looks up a real, already-existing `aws_iam_user`."
---

It creates a real, individual IAM identity, distinct from an `aws_iam_role`, meant for a long-lived person or application that needs its own durable credentials rather than assuming temporary ones.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_user_tags"})

    		ubx.Data(iam.UserTags, "example", iam.UserTagsConfig{
    			UserName: "example-user-name",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { UserTags } from "./local-sdk/aws/sdk/typescript/aws/data/iam/user_tags.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_user_tags" });

      data(UserTags, "example", {
        userName: "example-user-name",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import UserTags, UserTagsConfig

    def describe():
        ubx.intent("look up aws_iam_user_tags")

        ubx.data(UserTags, "example", UserTagsConfig(
            user_name="example-user-name",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  The real username this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
