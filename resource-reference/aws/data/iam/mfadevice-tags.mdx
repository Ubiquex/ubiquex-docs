---
title: "aws_iam_mfadevice_tags"
description: "`aws_iam_mfadevice_tags` reads the real tags applied to an IAM virtual MFA device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_mfadevice_tags"})

    		ubx.Data(iam.MfadeviceTags, "example", iam.MfadeviceTagsConfig{
    			SerialNumber: "example",
    			MaxItems:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MfadeviceTags } from "@ubx/sdk-aws/aws/data/iam/mfadevice_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_mfadevice_tags" });

      data(MfadeviceTags, "example", {
        serialNumber: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import MfadeviceTags, MfadeviceTagsConfig

    def describe():
        ubx.intent("look up aws_iam_mfadevice_tags")

        ubx.data(MfadeviceTags, "example", MfadeviceTagsConfig(
            serial_number="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="serial_number" type="string" required>
  The real number of the serial. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
