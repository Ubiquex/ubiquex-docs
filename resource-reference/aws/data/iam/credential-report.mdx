---
title: "aws_iam_credential_report"
description: "`aws_iam_credential_report` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_credential_report` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_credential_report"})

    		ubx.Data(iam.CredentialReport, "example", iam.CredentialReportConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CredentialReport } from "@ubx/sdk-aws/aws/data/iam/credential_report";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_credential_report" });

      data(CredentialReport, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import CredentialReport, CredentialReportConfig

    def describe():
        ubx.intent("look up aws_iam_credential_report")

        ubx.data(CredentialReport, "example", CredentialReportConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="content" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="generated_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="report_format" type="string">
  Returned by the lookup.
</ResponseField>
