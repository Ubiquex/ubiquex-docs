---
title: "aws_iam_credential_report"
description: "`aws_iam_credential_report` reads a real, generated IAM credential report, listing every user's own password and access key age and usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_credential_report"})

    		ubx.Data(iam.CredentialReport, "example", iam.CredentialReportConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CredentialReport } from "./local-sdk/aws/sdk/typescript/aws/data/iam/credential_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_credential_report" });

      data(CredentialReport, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import CredentialReport, CredentialReportConfig

    def describe():
        ubx.intent("look up aws_iam_credential_report")

        ubx.data(CredentialReport, "example", CredentialReportConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="content" type="string">
  The real content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="generated_time" type="string">
  The real timestamp of the generated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="report_format" type="string">
  The real, requested format of the report. **(AI-inferred)** Returned by the lookup.
</ResponseField>
