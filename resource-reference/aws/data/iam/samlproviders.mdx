---
title: "aws_iam_samlproviders"
description: "`aws_iam_samlproviders` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_samlproviders` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_samlproviders"})

    		ubx.Data(iam.Samlproviders, "example", iam.SamlprovidersConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Samlproviders } from "@ubx/sdk-aws/aws/data/iam/samlproviders";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_samlproviders" });

      data(Samlproviders, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import Samlproviders, SamlprovidersConfig

    def describe():
        ubx.intent("look up aws_iam_samlproviders")

        ubx.data(Samlproviders, "example", SamlprovidersConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="samlprovider_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
