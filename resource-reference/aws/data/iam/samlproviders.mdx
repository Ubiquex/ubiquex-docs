---
title: "aws_iam_samlproviders"
description: "`data_aws_iam_samlproviders` looks up a real, already-existing `aws_iam_samlprovider`."
---

It establishes real trust between IAM and an external SAML 2.0 identity provider, a corporate Active Directory Federation Services setup, for instance, so a workforce user can federate into an `aws_iam_role` using their existing enterprise credentials rather than a separate IAM identity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_samlproviders"})

    		ubx.Data(iam.Samlproviders, "example", iam.SamlprovidersConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Samlproviders } from "@ubx/sdk-aws/aws/data/iam/samlproviders";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_samlproviders" });

      data(Samlproviders, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import Samlproviders, SamlprovidersConfig

    def describe():
        ubx.intent("look up aws_iam_samlproviders")

        ubx.data(Samlproviders, "example", SamlprovidersConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="samlprovider_list" type="list(object)">
  The real samlproviders included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
