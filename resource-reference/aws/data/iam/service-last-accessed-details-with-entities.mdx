---
title: "aws_iam_service_last_accessed_details_with_entities"
description: "`aws_iam_service_last_accessed_details_with_entities` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iam_service_last_accessed_details_with_entities` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_service_last_accessed_details_with_entities"})

    		ubx.Data(iam.ServiceLastAccessedDetailsWithEntities, "example", iam.ServiceLastAccessedDetailsWithEntitiesConfig{
    			JobId:            "example",
    			ServiceNamespace: "example",
    			MaxItems:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceLastAccessedDetailsWithEntities } from "@ubx/sdk-aws/aws/data/iam/service_last_accessed_details_with_entities";

    export default stack("example", () => {
      intent({
        summary: "look up aws_iam_service_last_accessed_details_with_entities",
      });

      data(ServiceLastAccessedDetailsWithEntities, "example", {
        jobId: "example",
        serviceNamespace: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import ServiceLastAccessedDetailsWithEntities, ServiceLastAccessedDetailsWithEntitiesConfig

    def describe():
        ubx.intent("look up aws_iam_service_last_accessed_details_with_entities")

        ubx.data(ServiceLastAccessedDetailsWithEntities, "example", ServiceLastAccessedDetailsWithEntitiesConfig(
            job_id="example",
            service_namespace="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_details_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="error" type="object">
  &lt;p&gt;Contains information about the reason that the operation failed.&lt;/p&gt; &lt;p&gt;This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetOrganizationsAccessReport.html"&gt;GetOrganizationsAccessReport&lt;/a&gt;, &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetails.html"&gt;GetServiceLastAccessedDetails&lt;/a&gt;, and &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServiceLastAccessedDetailsWithEntities.html"&gt;GetServiceLastAccessedDetailsWithEntities&lt;/a&gt; operations.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_completion_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_creation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
