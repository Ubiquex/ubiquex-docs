---
title: "aws_iam_open_idconnect_provider_tags"
description: "`aws_iam_open_idconnect_provider_tags` reads the real tags applied to an IAM OpenID Connect (OIDC) identity provider."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_open_idconnect_provider_tags"})

    		ubx.Data(iam.OpenIdconnectProviderTags, "example", iam.OpenIdconnectProviderTagsConfig{
    			OpenIdconnectProviderArn: "arn:aws:iam::123456789012:policy/example",
    			MaxItems:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OpenIdconnectProviderTags } from "./local-sdk/aws/sdk/typescript/aws/data/iam/open_idconnect_provider_tags.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_open_idconnect_provider_tags" });

      data(OpenIdconnectProviderTags, "example", {
        openIdconnectProviderArn: "arn:aws:iam::123456789012:policy/example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iam import OpenIdconnectProviderTags, OpenIdconnectProviderTagsConfig

    def describe():
        ubx.intent("look up aws_iam_open_idconnect_provider_tags")

        ubx.data(OpenIdconnectProviderTags, "example", OpenIdconnectProviderTagsConfig(
            open_idconnect_provider_arn="arn:aws:iam::123456789012:policy/example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="open_idconnect_provider_arn" type="string" required>
  &lt;p&gt;The Amazon Resource Name (ARN). ARNs are unique identifiers for Amazon Web Services resources.&lt;/p&gt; &lt;p&gt;For more information about ARNs, go to &lt;a href="https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html"&gt;Amazon Resource Names (ARNs)&lt;/a&gt; in the &lt;i&gt;Amazon Web Services General Reference&lt;/i&gt;. &lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
