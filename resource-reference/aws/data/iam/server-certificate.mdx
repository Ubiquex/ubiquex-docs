---
title: "aws_iam_server_certificate"
description: "`data_aws_iam_server_certificate` looks up a real, already-existing `aws_iam_server_certificate`."
---

It uploads a real X.509 certificate and private key to IAM for use with a Classic Load Balancer or CloudFront, IAM's original, legacy certificate storage from before AWS Certificate Manager existed to handle this instead.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own server certificate"})

    		ubx.Resource(iam.ServerCertificate, "example", iam.ServerCertificateConfig{
    			CertificateBody:       "example",
    			CertificateChain:      "example",
    			Path:                  "/example/",
    			PrivateKey:            "example",
    			ServerCertificateName: "example-server-certificate-name",
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServerCertificate } from "@ubx/sdk-aws/aws/iam/server_certificate";

    export default stack("example", () => {
      intent({ summary: "example own server certificate" });

      resource(ServerCertificate, "example", {
        certificateBody: "example",
        certificateChain: "example",
        path: "/example/",
        privateKey: "example",
        serverCertificateName: "example-server-certificate-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import ServerCertificate, ServerCertificateConfig
    from ubx.aws.iam.server_certificate import ServerCertificate_Tags

    def describe():
        ubx.intent("example own server certificate")

        ubx.resource(ServerCertificate, "example", ServerCertificateConfig(
            certificate_body="example",
            certificate_chain="example",
            path="/example/",
            private_key="example",
            server_certificate_name="example-server-certificate-name",
            tags=[ServerCertificate_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_server_certificate, with certificate body example,
    certificate chain example, path /example/, private key example,
    server certificate name example-server-certificate-name, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="server_certificate_name" type="string" required>
  The real name of the server certificate this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="server_certificate" type="object">
  &lt;p&gt;Contains information about a server certificate.&lt;/p&gt; &lt;p&gt; This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_GetServerCertificate.html"&gt;GetServerCertificate&lt;/a&gt; operation. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="certificate_body" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="certificate_chain" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="server_certificate_metadata" type="object" required>
      &lt;p&gt;Contains information about a server certificate without its certificate body, certificate chain, and private key.&lt;/p&gt; &lt;p&gt; This data type is used as a response element in the &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_UploadServerCertificate.html"&gt;UploadServerCertificate&lt;/a&gt; and &lt;a href="https://docs.aws.amazon.com/IAM/latest/APIReference/API_ListServerCertificates.html"&gt;ListServerCertificates&lt;/a&gt; operations. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="arn" type="string" required>
          &lt;p&gt;The Amazon Resource Name (ARN). ARNs are unique identifiers for Amazon Web Services resources.&lt;/p&gt; &lt;p&gt;For more information about ARNs, go to &lt;a href="https://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html"&gt;Amazon Resource Names (ARNs)&lt;/a&gt; in the &lt;i&gt;Amazon Web Services General Reference&lt;/i&gt;. &lt;/p&gt; Required lookup argument.
        </ResponseField>
        <ResponseField name="expiration" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="path" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="server_certificate_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="server_certificate_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="upload_date" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
