---
title: "aws_iam_organizations_features"
description: "`aws_iam_organizations_features` reads which real, optional IAM features (such as root credentials management) are enabled for an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iam_organizations_features"})

    		ubx.Data(iam.OrganizationsFeatures, "example", iam.OrganizationsFeaturesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationsFeatures } from "@ubx/sdk-aws/aws/data/iam/organizations_features";

    export default stack("example", () => {
      intent({ summary: "look up aws_iam_organizations_features" });

      data(OrganizationsFeatures, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iam import OrganizationsFeatures, OrganizationsFeaturesConfig

    def describe():
        ubx.intent("look up aws_iam_organizations_features")

        ubx.data(OrganizationsFeatures, "example", OrganizationsFeaturesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="enabled_features" type="list(string)">
  The real features currently enabled for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_id" type="string">
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
