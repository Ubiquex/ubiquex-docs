---
title: "aws_eventbridge_replay"
description: "`aws_eventbridge_replay` looks up one real EventBridge replay, resending archived events back onto a bus."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventbridge "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eventbridge"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eventbridge_replay"})

    		ubx.Data(eventbridge.Replay, "example", eventbridge.ReplayConfig{
    			ReplayName: "example-replay-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Replay } from "@ubx/sdk-aws/aws/data/eventbridge/replay";

    export default stack("example", () => {
      intent({ summary: "look up aws_eventbridge_replay" });

      data(Replay, "example", {
        replayName: "example-replay-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eventbridge import Replay, ReplayConfig

    def describe():
        ubx.intent("look up aws_eventbridge_replay")

        ubx.data(Replay, "example", ReplayConfig(
            replay_name="example-replay-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="replay_name" type="string" required>
  The real name of the replay this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="destination" type="object">
  &lt;p&gt;A &lt;code&gt;ReplayDestination&lt;/code&gt; object that contains details about a replay.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="filter_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="event_end_time" type="string">
  The real timestamp of the event end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_last_replayed_time" type="string">
  The real timestamp of the event last replayed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_source_arn" type="string">
  The real Amazon Resource Name (ARN) of the event source this object was derived from. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_start_time" type="string">
  The real timestamp of the event start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replay_arn" type="string">
  The real Amazon Resource Name (ARN) of the replay this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replay_end_time" type="string">
  The real timestamp of the replay end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replay_start_time" type="string">
  The real timestamp of the replay start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state_reason" type="string">
  The real, human-readable reason for the current state. **(AI-inferred)** Returned by the lookup.
</ResponseField>
