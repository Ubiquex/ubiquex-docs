---
title: "aws_eventbridge_replays"
description: "`aws_eventbridge_replays` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_eventbridge_replays` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventbridge "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eventbridge"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eventbridge_replays"})

    		ubx.Data(eventbridge.Replays, "example", eventbridge.ReplaysConfig{
    			EventSourceArn: "arn:aws:iam::123456789012:policy/example",
    			Limit:          1,
    			NamePrefix:     "example",
    			State:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Replays } from "@ubx/sdk-aws/aws/data/eventbridge/replays";

    export default stack("example", () => {
      intent({ summary: "look up aws_eventbridge_replays" });

      data(Replays, "example", {
        eventSourceArn: "arn:aws:iam::123456789012:policy/example",
        limit: 1,
        namePrefix: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eventbridge import Replays, ReplaysConfig

    def describe():
        ubx.intent("look up aws_eventbridge_replays")

        ubx.data(Replays, "example", ReplaysConfig(
            event_source_arn="arn:aws:iam::123456789012:policy/example",
            limit=1,
            name_prefix="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_source_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="replays" type="list(object)">
  Returned by the lookup.
</ResponseField>
