---
title: "aws_eventbridge_archives"
description: "`aws_eventbridge_archives` looks up a real EventBridge archive, retaining a copy of events on a bus for later replay."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventbridge "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/eventbridge"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eventbridge_archives"})

    		ubx.Data(eventbridge.Archives, "example", eventbridge.ArchivesConfig{
    			EventSourceArn: "arn:aws:iam::123456789012:policy/example",
    			Limit:          1,
    			NamePrefix:     "example",
    			State:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Archives } from "@ubx/sdk-aws/aws/data/eventbridge/archives";

    export default stack("example", () => {
      intent({ summary: "look up aws_eventbridge_archives" });

      data(Archives, "example", {
        eventSourceArn: "arn:aws:iam::123456789012:policy/example",
        limit: 1,
        namePrefix: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eventbridge import Archives, ArchivesConfig

    def describe():
        ubx.intent("look up aws_eventbridge_archives")

        ubx.data(Archives, "example", ArchivesConfig(
            event_source_arn="arn:aws:iam::123456789012:policy/example",
            limit=1,
            name_prefix="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_source_arn" type="string">
  The real Amazon Resource Name (ARN) of the event source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Restricts results to objects whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="archives" type="list(object)">
  The real EventBridge archives included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
