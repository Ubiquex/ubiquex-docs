---
title: "aws_eventbridge_rule_names_by_target"
description: "`aws_eventbridge_rule_names_by_target` reads the real EventBridge rule names that deliver to a given target."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventbridge "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/eventbridge"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_eventbridge_rule_names_by_target"})

    		ubx.Data(eventbridge.RuleNamesByTarget, "example", eventbridge.RuleNamesByTargetConfig{
    			TargetArn:    "arn:aws:iam::123456789012:policy/example",
    			EventBusName: "example-event-bus-name",
    			Limit:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuleNamesByTarget } from "@ubx/sdk-aws/aws/data/eventbridge/rule_names_by_target";

    export default stack("example", () => {
      intent({ summary: "look up aws_eventbridge_rule_names_by_target" });

      data(RuleNamesByTarget, "example", {
        targetArn: "arn:aws:iam::123456789012:policy/example",
        eventBusName: "example-event-bus-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.eventbridge import RuleNamesByTarget, RuleNamesByTargetConfig

    def describe():
        ubx.intent("look up aws_eventbridge_rule_names_by_target")

        ubx.data(RuleNamesByTarget, "example", RuleNamesByTargetConfig(
            target_arn="arn:aws:iam::123456789012:policy/example",
            event_bus_name="example-event-bus-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_bus_name" type="string">
  The real name of the event bus this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the target this action applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_names" type="list(string)">
  The real names of the rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
