---
title: "aws_application_signals_service_level_objective_exclusion_windows"
description: "`aws_application_signals_service_level_objective_exclusion_windows` reads the real, excluded time windows configured on an Application Signals SLO."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_service_level_objective_exclusion_windows"})

    		ubx.Data(application_signals.ApplicationSignalsServiceLevelObjectiveExclusionWindows, "example", application_signals.ApplicationSignalsServiceLevelObjectiveExclusionWindowsConfig{
    			Id:         "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsServiceLevelObjectiveExclusionWindows } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_service_level_objective_exclusion_windows";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_application_signals_service_level_objective_exclusion_windows",
      });

      data(ApplicationSignalsServiceLevelObjectiveExclusionWindows, "example", {
        id: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsServiceLevelObjectiveExclusionWindows, ApplicationSignalsServiceLevelObjectiveExclusionWindowsConfig

    def describe():
        ubx.intent("look up aws_application_signals_service_level_objective_exclusion_windows")

        ubx.data(ApplicationSignalsServiceLevelObjectiveExclusionWindows, "example", ApplicationSignalsServiceLevelObjectiveExclusionWindowsConfig(
            id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="exclusion_windows" type="list(object)">
  The real, scheduled windows during which this action is excluded from running. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
