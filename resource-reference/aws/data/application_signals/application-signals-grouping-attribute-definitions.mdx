---
title: "aws_application_signals_grouping_attribute_definitions"
description: "`aws_application_signals_grouping_attribute_definitions` reads the real attribute definitions Application Signals uses to group related services."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_grouping_attribute_definitions"})

    		ubx.Data(application_signals.ApplicationSignalsGroupingAttributeDefinitions, "example", application_signals.ApplicationSignalsGroupingAttributeDefinitionsConfig{
    			AwsAccountId:          "example",
    			IncludeLinkedAccounts: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsGroupingAttributeDefinitions } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_grouping_attribute_definitions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_application_signals_grouping_attribute_definitions",
      });

      data(ApplicationSignalsGroupingAttributeDefinitions, "example", {
        awsAccountId: "example",
        includeLinkedAccounts: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsGroupingAttributeDefinitions, ApplicationSignalsGroupingAttributeDefinitionsConfig

    def describe():
        ubx.intent("look up aws_application_signals_grouping_attribute_definitions")

        ubx.data(ApplicationSignalsGroupingAttributeDefinitions, "example", ApplicationSignalsGroupingAttributeDefinitionsConfig(
            aws_account_id="example",
            include_linked_accounts=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_linked_accounts" type="bool">
  Whether results also include AWS Organizations member accounts linked to this account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="grouping_attribute_definitions" type="list(object)">
  The real, structured definitions for the attributes results are grouped by. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
