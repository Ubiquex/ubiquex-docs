---
title: "aws_application_signals_instrumentation_configurations"
description: "`aws_application_signals_instrumentation_configurations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_application_signals_instrumentation_configurations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_instrumentation_configurations"})

    		ubx.Data(application_signals.ApplicationSignalsInstrumentationConfigurations, "example", application_signals.ApplicationSignalsInstrumentationConfigurationsConfig{
    			Environment:         "example",
    			InstrumentationType: "example",
    			Service:             "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsInstrumentationConfigurations } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_instrumentation_configurations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_application_signals_instrumentation_configurations",
      });

      data(ApplicationSignalsInstrumentationConfigurations, "example", {
        environment: "example",
        instrumentationType: "example",
        service: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsInstrumentationConfigurations, ApplicationSignalsInstrumentationConfigurationsConfig

    def describe():
        ubx.intent("look up aws_application_signals_instrumentation_configurations")

        ubx.data(ApplicationSignalsInstrumentationConfigurations, "example", ApplicationSignalsInstrumentationConfigurationsConfig(
            environment="example",
            instrumentation_type="example",
            service="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instrumentation_type" type="string" required>
  Type of instrumentation configuration Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="changed" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_configurations" type="list(object)">
  &lt;p&gt;An array of instrumentation configurations that omit service and environment, used when those values are provided elsewhere in the response.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sync_interval" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="synced_at" type="string">
  Returned by the lookup.
</ResponseField>
