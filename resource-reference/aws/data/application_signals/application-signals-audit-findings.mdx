---
title: "aws_application_signals_audit_findings"
description: "`aws_application_signals_audit_findings` reads real, detected findings from an Application Signals SLO audit."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_audit_findings"})

    		ubx.Data(application_signals.ApplicationSignalsAuditFindings, "example", application_signals.ApplicationSignalsAuditFindingsConfig{
    			AuditTargets: []map[string]any{{"data": map[string]any{"canary": map[string]any{"canary_name": "example-canary-name"}}}},
    			EndTime:      "example",
    			StartTime:    "example",
    			Auditors:     []string{"example"},
    			DetailLevel:  "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsAuditFindings } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_audit_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_signals_audit_findings" });

      data(ApplicationSignalsAuditFindings, "example", {
        auditTargets: [{ data: { canary: { canaryName: "example-canary-name" } } }],
        endTime: "example",
        startTime: "example",
        auditors: ["example"],
        detailLevel: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsAuditFindings, ApplicationSignalsAuditFindingsConfig
    from ubx.aws.data.application_signals.application_signals_audit_findings import ApplicationSignalsAuditFindings_AuditTargets, ApplicationSignalsAuditFindings_AuditTargets_Data, ApplicationSignalsAuditFindings_AuditTargets_Data_Canary

    def describe():
        ubx.intent("look up aws_application_signals_audit_findings")

        ubx.data(ApplicationSignalsAuditFindings, "example", ApplicationSignalsAuditFindingsConfig(
            audit_targets=[ApplicationSignalsAuditFindings_AuditTargets(data=ApplicationSignalsAuditFindings_AuditTargets_Data(canary=ApplicationSignalsAuditFindings_AuditTargets_Data_Canary(canary_name="example-canary-name")))],
            end_time="example",
            start_time="example",
            auditors=["example"],
            detail_level="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audit_targets" type="list(object)" required>
  The real targets of the audit. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="auditors" type="list(string)">
  The real auditors included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="detail_level" type="string">
  The real level of the detail. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="audit_findings" type="list(object)">
  The real AWS IoT Device Defender audit findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
