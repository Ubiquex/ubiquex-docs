---
title: "aws_application_signals_audit_findings"
description: "`aws_application_signals_audit_findings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_application_signals_audit_findings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_audit_findings"})

    		ubx.Data(application_signals.ApplicationSignalsAuditFindings, "example", application_signals.ApplicationSignalsAuditFindingsConfig{
    			AuditTargets: []map[string]any{{"data": map[string]any{"canary": map[string]any{"canary_name": "example-canary-name"}}}},
    			EndTime:      "example",
    			StartTime:    "example",
    			Auditors:     []string{"example"},
    			DetailLevel:  "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsAuditFindings } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_audit_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_signals_audit_findings" });

      data(ApplicationSignalsAuditFindings, "example", {
        auditTargets: [{ data: { canary: { canaryName: "example-canary-name" } } }],
        endTime: "example",
        startTime: "example",
        auditors: ["example"],
        detailLevel: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsAuditFindings, ApplicationSignalsAuditFindingsConfig

    def describe():
        ubx.intent("look up aws_application_signals_audit_findings")

        ubx.data(ApplicationSignalsAuditFindings, "example", ApplicationSignalsAuditFindingsConfig(
            audit_targets=[{"data": {"canary": {"canary_name": "example-canary-name"}}}],
            end_time="example",
            start_time="example",
            auditors=["example"],
            detail_level="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audit_targets" type="list(object)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="auditors" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="detail_level" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="audit_findings" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
