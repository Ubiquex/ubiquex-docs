---
title: "aws_application_signals_service_states"
description: "`aws_application_signals_service_states` reads the real, current health states of services tracked by Application Signals."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_service_states"})

    		ubx.Data(application_signals.ApplicationSignalsServiceStates, "example", application_signals.ApplicationSignalsServiceStatesConfig{
    			EndTime:               "example",
    			StartTime:             "example",
    			AttributeFilters:      []map[string]any{{"attribute_filter_name": "example-attribute-filter-name"}},
    			AwsAccountId:          "example",
    			IncludeLinkedAccounts: true,
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsServiceStates } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_service_states";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_signals_service_states" });

      data(ApplicationSignalsServiceStates, "example", {
        endTime: "example",
        startTime: "example",
        attributeFilters: [{
          attributeFilterName: "example-attribute-filter-name",
        }],
        awsAccountId: "example",
        includeLinkedAccounts: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsServiceStates, ApplicationSignalsServiceStatesConfig

    def describe():
        ubx.intent("look up aws_application_signals_service_states")

        ubx.data(ApplicationSignalsServiceStates, "example", ApplicationSignalsServiceStatesConfig(
            end_time="example",
            start_time="example",
            attribute_filters=[{"attribute_filter_name": "example-attribute-filter-name"}],
            aws_account_id="example",
            include_linked_accounts=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_filters" type="list(object)">
  The real filters applied to the attributes in this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_linked_accounts" type="bool">
  Whether results also include AWS Organizations member accounts linked to this account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_states" type="list(object)">
  The real, current states of the services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
