---
title: "aws_application_signals_services"
description: "`aws_application_signals_services` reads the real services tracked by Application Signals."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_services"})

    		ubx.Data(application_signals.ApplicationSignalsServices, "example", application_signals.ApplicationSignalsServicesConfig{
    			EndTime:               "example",
    			StartTime:             "example",
    			AwsAccountId:          "example",
    			IncludeLinkedAccounts: true,
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationSignalsServices } from "./local-sdk/aws/sdk/typescript/aws/data/application_signals/application_signals_services.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_signals_services" });

      data(ApplicationSignalsServices, "example", {
        endTime: "example",
        startTime: "example",
        awsAccountId: "example",
        includeLinkedAccounts: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsServices, ApplicationSignalsServicesConfig

    def describe():
        ubx.intent("look up aws_application_signals_services")

        ubx.data(ApplicationSignalsServices, "example", ApplicationSignalsServicesConfig(
            end_time="example",
            start_time="example",
            aws_account_id="example",
            include_linked_accounts=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_linked_accounts" type="bool">
  Whether results also include AWS Organizations member accounts linked to this account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_summaries" type="list(object)">
  The real, summarized services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
