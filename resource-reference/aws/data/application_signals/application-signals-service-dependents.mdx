---
title: "aws_application_signals_service_dependents"
description: "`aws_application_signals_service_dependents` reads the real, discovered upstream dependents of a service tracked by Application Signals."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application_signals "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/application_signals"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_application_signals_service_dependents"})

    		ubx.Data(application_signals.ApplicationSignalsServiceDependents, "example", application_signals.ApplicationSignalsServiceDependentsConfig{
    			EndTime:       "example",
    			KeyAttributes: map[string]string{"managed-by": "ubx"},
    			StartTime:     "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationSignalsServiceDependents } from "@ubx/sdk-aws/aws/data/application_signals/application_signals_service_dependents";

    export default stack("example", () => {
      intent({ summary: "look up aws_application_signals_service_dependents" });

      data(ApplicationSignalsServiceDependents, "example", {
        endTime: "example",
        keyAttributes: { "managed-by": "ubx" },
        startTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.application_signals import ApplicationSignalsServiceDependents, ApplicationSignalsServiceDependentsConfig

    def describe():
        ubx.intent("look up aws_application_signals_service_dependents")

        ubx.data(ApplicationSignalsServiceDependents, "example", ApplicationSignalsServiceDependentsConfig(
            end_time="example",
            key_attributes={"managed-by": "ubx"},
            start_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="key_attributes" type="map(string)" required>
  The real, structured key attributes identifying this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_dependents" type="list(object)">
  The real objects that depend on this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>
