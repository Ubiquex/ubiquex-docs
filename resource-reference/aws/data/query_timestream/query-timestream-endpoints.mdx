---
title: "aws_query_timestream_endpoints"
description: "`aws_query_timestream_endpoints` reads the real, account-specific query endpoint for Timestream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	query_timestream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/query_timestream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_query_timestream_endpoints"})

    		ubx.Data(query_timestream.QueryTimestreamEndpoints, "example", query_timestream.QueryTimestreamEndpointsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueryTimestreamEndpoints } from "@ubx/sdk-aws/aws/data/query_timestream/query_timestream_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_query_timestream_endpoints" });

      data(QueryTimestreamEndpoints, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.query_timestream import QueryTimestreamEndpoints, QueryTimestreamEndpointsConfig

    def describe():
        ubx.intent("look up aws_query_timestream_endpoints")

        ubx.data(QueryTimestreamEndpoints, "example", QueryTimestreamEndpointsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="endpoints" type="list(object)">
  The real endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
