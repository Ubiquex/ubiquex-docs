---
title: "aws_applicationinsights_log_pattern_sets"
description: "`aws_applicationinsights_log_pattern_sets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_applicationinsights_log_pattern_sets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_log_pattern_sets"})

    		ubx.Data(applicationinsights.LogPatternSets, "example", applicationinsights.LogPatternSetsConfig{
    			ResourceGroupName: "example-resource-group-name",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LogPatternSets } from "@ubx/sdk-aws/aws/data/applicationinsights/log_pattern_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_applicationinsights_log_pattern_sets" });

      data(LogPatternSets, "example", {
        resourceGroupName: "example-resource-group-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import LogPatternSets, LogPatternSetsConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_log_pattern_sets")

        ubx.data(LogPatternSets, "example", LogPatternSetsConfig(
            resource_group_name="example-resource-group-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="log_pattern_sets" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
