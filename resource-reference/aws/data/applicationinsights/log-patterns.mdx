---
title: "aws_applicationinsights_log_patterns"
description: "`aws_applicationinsights_log_patterns` reads the real log patterns Application Insights matches against a component's own logs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_log_patterns"})

    		ubx.Data(applicationinsights.LogPatterns, "example", applicationinsights.LogPatternsConfig{
    			ResourceGroupName: "example-resource-group-name",
    			MaxResults:        1,
    			PatternSetName:    "example-pattern-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { LogPatterns } from "./local-sdk/aws/sdk/typescript/aws/data/applicationinsights/log_patterns.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_applicationinsights_log_patterns" });

      data(LogPatterns, "example", {
        resourceGroupName: "example-resource-group-name",
        maxResults: 1,
        patternSetName: "example-pattern-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import LogPatterns, LogPatternsConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_log_patterns")

        ubx.data(LogPatterns, "example", LogPatternsConfig(
            resource_group_name="example-resource-group-name",
            max_results=1,
            pattern_set_name="example-pattern-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pattern_set_name" type="string">
  The real name of the pattern set this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="log_patterns" type="list(object)">
  The real, detected patterns in this log. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
