---
title: "aws_applicationinsights_problems"
description: "`aws_applicationinsights_problems` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_applicationinsights_problems` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_problems"})

    		ubx.Data(applicationinsights.Problems, "example", applicationinsights.ProblemsConfig{
    			ComponentName: "example-component-name",
    			EndTime:       "example",
    			MaxResults:    1,
    			StartTime:     "example",
    			Visibility:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Problems } from "@ubx/sdk-aws/aws/data/applicationinsights/problems";

    export default stack("example", () => {
      intent({ summary: "look up aws_applicationinsights_problems" });

      data(Problems, "example", {
        componentName: "example-component-name",
        endTime: "example",
        maxResults: 1,
        startTime: "example",
        visibility: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import Problems, ProblemsConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_problems")

        ubx.data(Problems, "example", ProblemsConfig(
            component_name="example-component-name",
            end_time="example",
            max_results=1,
            start_time="example",
            visibility="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="visibility" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="problem_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_group_name" type="string">
  Returned by the lookup.
</ResponseField>
