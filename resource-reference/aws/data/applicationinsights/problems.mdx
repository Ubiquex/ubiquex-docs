---
title: "aws_applicationinsights_problems"
description: "`aws_applicationinsights_problems` reads the real problems detected by Application Insights for an application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_problems"})

    		ubx.Data(applicationinsights.Problems, "example", applicationinsights.ProblemsConfig{
    			ComponentName: "example-component-name",
    			EndTime:       "example",
    			MaxResults:    1,
    			StartTime:     "example",
    			Visibility:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Problems } from "./local-sdk/aws/sdk/typescript/aws/data/applicationinsights/problems.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_applicationinsights_problems" });

      data(Problems, "example", {
        componentName: "example-component-name",
        endTime: "example",
        maxResults: 1,
        startTime: "example",
        visibility: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import Problems, ProblemsConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_problems")

        ubx.data(Problems, "example", ProblemsConfig(
            component_name="example-component-name",
            end_time="example",
            max_results=1,
            start_time="example",
            visibility="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string">
  The real name of the component this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="visibility" type="string">
  The real visibility (such as public or private) of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="problem_list" type="list(object)">
  The real problems included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_group_name" type="string">
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
