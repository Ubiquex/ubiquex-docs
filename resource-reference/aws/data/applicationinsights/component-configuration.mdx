---
title: "aws_applicationinsights_component_configuration"
description: "`aws_applicationinsights_component_configuration` reads the real, current monitoring configuration of an Application Insights component."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_component_configuration"})

    		ubx.Data(applicationinsights.ComponentConfiguration, "example", applicationinsights.ComponentConfigurationConfig{
    			ComponentName:     "example-component-name",
    			ResourceGroupName: "example-resource-group-name",
    			AccountId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComponentConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/applicationinsights/component_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_applicationinsights_component_configuration",
      });

      data(ComponentConfiguration, "example", {
        componentName: "example-component-name",
        resourceGroupName: "example-resource-group-name",
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import ComponentConfiguration, ComponentConfigurationConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_component_configuration")

        ubx.data(ComponentConfiguration, "example", ComponentConfigurationConfig(
            component_name="example-component-name",
            resource_group_name="example-resource-group-name",
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="component_name" type="string" required>
  The real name of the component this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_configuration" type="string">
  The real configuration of the component. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="monitor" type="bool">
  The real monitor this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tier" type="string">
  The real service tier this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
