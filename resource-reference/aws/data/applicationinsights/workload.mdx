---
title: "aws_applicationinsights_workload"
description: "`aws_applicationinsights_workload` looks up one real workload configuration within an Application Insights component."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_workload"})

    		ubx.Data(applicationinsights.Workload, "example", applicationinsights.WorkloadConfig{
    			ComponentName:     "example-component-name",
    			ResourceGroupName: "example-resource-group-name",
    			WorkloadId:        "example",
    			AccountId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workload } from "@ubx/sdk-aws/aws/data/applicationinsights/workload";

    export default stack("example", () => {
      intent({ summary: "look up aws_applicationinsights_workload" });

      data(Workload, "example", {
        componentName: "example-component-name",
        resourceGroupName: "example-resource-group-name",
        workloadId: "example",
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import Workload, WorkloadConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_workload")

        ubx.data(Workload, "example", WorkloadConfig(
            component_name="example-component-name",
            resource_group_name="example-resource-group-name",
            workload_id="example",
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="component_name" type="string" required>
  The real name of the component this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workload_id" type="string" required>
  The real, unique identifier of the workload this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="workload_configuration" type="object">
  &lt;p&gt;The configuration of the workload.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="configuration" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tier" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="workload_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_remarks" type="string">
  The real, free-text remarks about this AWS Well-Architected workload. **(AI-inferred)** Returned by the lookup.
</ResponseField>
