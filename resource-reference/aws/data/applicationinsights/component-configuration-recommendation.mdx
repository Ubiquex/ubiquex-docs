---
title: "aws_applicationinsights_component_configuration_recommendation"
description: "`aws_applicationinsights_component_configuration_recommendation` reads a real, system-recommended monitoring configuration for an Application Insights..."
---

`aws_applicationinsights_component_configuration_recommendation` reads a real, system-recommended monitoring configuration for an Application Insights component.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_applicationinsights_component_configuration_recommendation"})

    		ubx.Data(applicationinsights.ComponentConfigurationRecommendation, "example", applicationinsights.ComponentConfigurationRecommendationConfig{
    			ComponentName:      "example-component-name",
    			ResourceGroupName:  "example-resource-group-name",
    			Tier:               "example",
    			RecommendationType: "example",
    			WorkloadName:       "example-workload-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComponentConfigurationRecommendation } from "./local-sdk/aws/sdk/typescript/aws/data/applicationinsights/component_configuration_recommendation.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_applicationinsights_component_configuration_recommendation",
      });

      data(ComponentConfigurationRecommendation, "example", {
        componentName: "example-component-name",
        resourceGroupName: "example-resource-group-name",
        tier: "example",
        recommendationType: "example",
        workloadName: "example-workload-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.applicationinsights import ComponentConfigurationRecommendation, ComponentConfigurationRecommendationConfig

    def describe():
        ubx.intent("look up aws_applicationinsights_component_configuration_recommendation")

        ubx.data(ComponentConfigurationRecommendation, "example", ComponentConfigurationRecommendationConfig(
            component_name="example-component-name",
            resource_group_name="example-resource-group-name",
            tier="example",
            recommendation_type="example",
            workload_name="example-workload-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="component_name" type="string" required>
  The real name of the component this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_type" type="string">
  The real type of this recommendation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  The real name of the resource group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="tier" type="string" required>
  The real service tier this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="workload_name" type="string">
  The real name of the workload this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="component_configuration" type="string">
  The real configuration of the component. **(AI-inferred)** Returned by the lookup.
</ResponseField>
