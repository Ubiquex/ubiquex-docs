---
title: "aws_cost_optimization_hub_recommendations"
description: "`aws_cost_optimization_hub_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cost_optimization_hub_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost_optimization_hub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cost_optimization_hub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cost_optimization_hub_recommendations"})

    		ubx.Data(cost_optimization_hub.CostOptimizationHubRecommendations, "example", cost_optimization_hub.CostOptimizationHubRecommendationsConfig{
    			Filter:                    map[string]any{"account_ids": []string{"example"}},
    			IncludeAllRecommendations: true,
    			MaxResults:                1,
    			OrderBy:                   map[string]any{"dimension": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CostOptimizationHubRecommendations } from "@ubx/sdk-aws/aws/data/cost_optimization_hub/cost_optimization_hub_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_cost_optimization_hub_recommendations" });

      data(CostOptimizationHubRecommendations, "example", {
        filter: { accountIds: ["example"] },
        includeAllRecommendations: true,
        maxResults: 1,
        orderBy: { dimension: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cost_optimization_hub import CostOptimizationHubRecommendations, CostOptimizationHubRecommendationsConfig

    def describe():
        ubx.intent("look up aws_cost_optimization_hub_recommendations")

        ubx.data(CostOptimizationHubRecommendations, "example", CostOptimizationHubRecommendationsConfig(
            filter={"account_ids": ["example"]},
            include_all_recommendations=True,
            max_results=1,
            order_by={"dimension": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Describes a filter that returns a more specific list of recommendations. Filters recommendations by different dimensions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="action_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="implementation_efforts" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="recommendation_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="regions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="restart_needed" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="rollback_possible" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="include_all_recommendations" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="object">
  &lt;p&gt;Defines how rows will be sorted in the response.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimension" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
