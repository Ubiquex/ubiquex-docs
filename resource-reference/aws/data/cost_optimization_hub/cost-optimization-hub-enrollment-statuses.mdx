---
title: "aws_cost_optimization_hub_enrollment_statuses"
description: "`aws_cost_optimization_hub_enrollment_statuses` reads the real Cost Optimization Hub enrollment status of accounts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost_optimization_hub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cost_optimization_hub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cost_optimization_hub_enrollment_statuses"})

    		ubx.Data(cost_optimization_hub.CostOptimizationHubEnrollmentStatuses, "example", cost_optimization_hub.CostOptimizationHubEnrollmentStatusesConfig{
    			AccountId:               "example",
    			IncludeOrganizationInfo: true,
    			MaxResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CostOptimizationHubEnrollmentStatuses } from "@ubx/sdk-aws/aws/data/cost_optimization_hub/cost_optimization_hub_enrollment_statuses";

    export default stack("example", () => {
      intent({ summary: "look up aws_cost_optimization_hub_enrollment_statuses" });

      data(CostOptimizationHubEnrollmentStatuses, "example", {
        accountId: "example",
        includeOrganizationInfo: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cost_optimization_hub import CostOptimizationHubEnrollmentStatuses, CostOptimizationHubEnrollmentStatusesConfig

    def describe():
        ubx.intent("look up aws_cost_optimization_hub_enrollment_statuses")

        ubx.data(CostOptimizationHubEnrollmentStatuses, "example", CostOptimizationHubEnrollmentStatusesConfig(
            account_id="example",
            include_organization_info=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_organization_info" type="bool">
  Whether AWS Organizations information is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="include_member_accounts" type="bool">
  Whether AWS Organizations member accounts are included in the results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
