---
title: "aws_cost_optimization_hub_preferences"
description: "`aws_cost_optimization_hub_preferences` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cost_optimization_hub_preferences` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost_optimization_hub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cost_optimization_hub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cost_optimization_hub_preferences"})

    		ubx.Data(cost_optimization_hub.CostOptimizationHubPreferences, "example", cost_optimization_hub.CostOptimizationHubPreferencesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CostOptimizationHubPreferences } from "@ubx/sdk-aws/aws/data/cost_optimization_hub/cost_optimization_hub_preferences";

    export default stack("example", () => {
      intent({ summary: "look up aws_cost_optimization_hub_preferences" });

      data(CostOptimizationHubPreferences, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cost_optimization_hub import CostOptimizationHubPreferences, CostOptimizationHubPreferencesConfig

    def describe():
        ubx.intent("look up aws_cost_optimization_hub_preferences")

        ubx.data(CostOptimizationHubPreferences, "example", CostOptimizationHubPreferencesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="member_account_discount_visibility" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="preferred_commitment" type="object">
  &lt;p&gt;The preferred configuration for Reserved Instances and Savings Plans commitment-based discounts, consisting of a payment option and a commitment duration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="payment_option" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="term" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="savings_estimation_mode" type="string">
  Returned by the lookup.
</ResponseField>
