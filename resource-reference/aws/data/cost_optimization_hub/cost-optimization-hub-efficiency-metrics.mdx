---
title: "aws_cost_optimization_hub_efficiency_metrics"
description: "`aws_cost_optimization_hub_efficiency_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cost_optimization_hub_efficiency_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cost_optimization_hub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cost_optimization_hub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cost_optimization_hub_efficiency_metrics"})

    		ubx.Data(cost_optimization_hub.CostOptimizationHubEfficiencyMetrics, "example", cost_optimization_hub.CostOptimizationHubEfficiencyMetricsConfig{
    			Granularity: "example",
    			TimePeriod:  map[string]any{"end": "example"},
    			GroupBy:     "example",
    			MaxResults:  1,
    			OrderBy:     map[string]any{"dimension": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CostOptimizationHubEfficiencyMetrics } from "@ubx/sdk-aws/aws/data/cost_optimization_hub/cost_optimization_hub_efficiency_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_cost_optimization_hub_efficiency_metrics" });

      data(CostOptimizationHubEfficiencyMetrics, "example", {
        granularity: "example",
        timePeriod: { end: "example" },
        groupBy: "example",
        maxResults: 1,
        orderBy: { dimension: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cost_optimization_hub import CostOptimizationHubEfficiencyMetrics, CostOptimizationHubEfficiencyMetricsConfig

    def describe():
        ubx.intent("look up aws_cost_optimization_hub_efficiency_metrics")

        ubx.data(CostOptimizationHubEfficiencyMetrics, "example", CostOptimizationHubEfficiencyMetricsConfig(
            granularity="example",
            time_period={"end": "example"},
            group_by="example",
            max_results=1,
            order_by={"dimension": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="granularity" type="string" required>
  &lt;p&gt;The time granularity for aggregating the cost efficiency metrics.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="group_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="object">
  &lt;p&gt;Defines how rows will be sorted in the response.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimension" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="time_period" type="object" required>
  &lt;p&gt;Specifies a date range for retrieving efficiency metrics. The start date is inclusive and the end date is exclusive.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="efficiency_metrics_by_group" type="list(object)">
  &lt;p&gt;A list of cost efficiency metrics grouped by the specified dimension.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
