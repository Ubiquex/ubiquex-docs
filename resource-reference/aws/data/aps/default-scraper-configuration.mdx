---
title: "aws_aps_default_scraper_configuration"
description: "`aws_aps_default_scraper_configuration` reads the real, default scrape configuration for a Managed Service for Prometheus scraper."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aps "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/aps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aps_default_scraper_configuration"})

    		ubx.Data(aps.DefaultScraperConfiguration, "example", aps.DefaultScraperConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultScraperConfiguration } from "@ubx/sdk-aws/aws/data/aps/default_scraper_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_aps_default_scraper_configuration" });

      data(DefaultScraperConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aps import DefaultScraperConfiguration, DefaultScraperConfigurationConfig

    def describe():
        ubx.intent("look up aws_aps_default_scraper_configuration")

        ubx.data(DefaultScraperConfiguration, "example", DefaultScraperConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration" type="string">
  The real, structured configuration of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
