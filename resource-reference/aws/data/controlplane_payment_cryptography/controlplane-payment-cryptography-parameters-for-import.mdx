---
title: "aws_controlplane_payment_cryptography_parameters_for_import"
description: "`aws_controlplane_payment_cryptography_parameters_for_import` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_controlplane_payment_cryptography_parameters_for_import` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controlplane_payment_cryptography "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/controlplane_payment_cryptography"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_controlplane_payment_cryptography_parameters_for_import"})

    		ubx.Data(controlplane_payment_cryptography.ControlplanePaymentCryptographyParametersForImport, "example", controlplane_payment_cryptography.ControlplanePaymentCryptographyParametersForImportConfig{
    			KeyMaterialType:         "example",
    			WrappingKeyAlgorithm:    "example",
    			ReuseLastGeneratedToken: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlplanePaymentCryptographyParametersForImport } from "@ubx/sdk-aws/aws/data/controlplane_payment_cryptography/controlplane_payment_cryptography_parameters_for_import";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_controlplane_payment_cryptography_parameters_for_import",
      });

      data(ControlplanePaymentCryptographyParametersForImport, "example", {
        keyMaterialType: "example",
        wrappingKeyAlgorithm: "example",
        reuseLastGeneratedToken: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.controlplane_payment_cryptography import ControlplanePaymentCryptographyParametersForImport, ControlplanePaymentCryptographyParametersForImportConfig

    def describe():
        ubx.intent("look up aws_controlplane_payment_cryptography_parameters_for_import")

        ubx.data(ControlplanePaymentCryptographyParametersForImport, "example", ControlplanePaymentCryptographyParametersForImportConfig(
            key_material_type="example",
            wrapping_key_algorithm="example",
            reuse_last_generated_token=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key_material_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="reuse_last_generated_token" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="wrapping_key_algorithm" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="import_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parameters_valid_until_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="wrapping_key_certificate" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="wrapping_key_certificate_chain" type="string">
  Returned by the lookup.
</ResponseField>
