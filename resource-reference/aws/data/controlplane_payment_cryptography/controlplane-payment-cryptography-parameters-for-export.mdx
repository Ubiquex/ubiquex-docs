---
title: "aws_controlplane_payment_cryptography_parameters_for_export"
description: "`aws_controlplane_payment_cryptography_parameters_for_export` reads the real parameters required to export a Payment Cryptography key."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controlplane_payment_cryptography "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/controlplane_payment_cryptography"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_controlplane_payment_cryptography_parameters_for_export"})

    		ubx.Data(controlplane_payment_cryptography.ControlplanePaymentCryptographyParametersForExport, "example", controlplane_payment_cryptography.ControlplanePaymentCryptographyParametersForExportConfig{
    			KeyMaterialType:         "example",
    			SigningKeyAlgorithm:     "example",
    			ReuseLastGeneratedToken: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlplanePaymentCryptographyParametersForExport } from "@ubx/sdk-aws/aws/data/controlplane_payment_cryptography/controlplane_payment_cryptography_parameters_for_export";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_controlplane_payment_cryptography_parameters_for_export",
      });

      data(ControlplanePaymentCryptographyParametersForExport, "example", {
        keyMaterialType: "example",
        signingKeyAlgorithm: "example",
        reuseLastGeneratedToken: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.controlplane_payment_cryptography import ControlplanePaymentCryptographyParametersForExport, ControlplanePaymentCryptographyParametersForExportConfig

    def describe():
        ubx.intent("look up aws_controlplane_payment_cryptography_parameters_for_export")

        ubx.data(ControlplanePaymentCryptographyParametersForExport, "example", ControlplanePaymentCryptographyParametersForExportConfig(
            key_material_type="example",
            signing_key_algorithm="example",
            reuse_last_generated_token=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key_material_type" type="string" required>
  The real, discriminating type of the key material this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="reuse_last_generated_token" type="bool">
  The real, opaque reuse last generated token. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="signing_key_algorithm" type="string" required>
  The real algorithm of the signing key. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_token" type="string">
  The real, opaque export token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters_valid_until_timestamp" type="string">
  The real timestamp of the parameters valid until. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signing_key_certificate" type="string">
  The real, PEM-encoded certificate for the signing key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signing_key_certificate_chain" type="string">
  The real, full certificate chain for the signing key. **(AI-inferred)** Returned by the lookup.
</ResponseField>
