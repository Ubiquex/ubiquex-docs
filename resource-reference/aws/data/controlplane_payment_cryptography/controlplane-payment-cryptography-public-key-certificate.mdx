---
title: "aws_controlplane_payment_cryptography_public_key_certificate"
description: "`aws_controlplane_payment_cryptography_public_key_certificate` reads the real public key certificate of a Payment Cryptography key."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controlplane_payment_cryptography "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/controlplane_payment_cryptography"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_controlplane_payment_cryptography_public_key_certificate"})

    		ubx.Data(controlplane_payment_cryptography.ControlplanePaymentCryptographyPublicKeyCertificate, "example", controlplane_payment_cryptography.ControlplanePaymentCryptographyPublicKeyCertificateConfig{
    			KeyIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlplanePaymentCryptographyPublicKeyCertificate } from "@ubx/sdk-aws/aws/data/controlplane_payment_cryptography/controlplane_payment_cryptography_public_key_certificate";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_controlplane_payment_cryptography_public_key_certificate",
      });

      data(ControlplanePaymentCryptographyPublicKeyCertificate, "example", {
        keyIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.controlplane_payment_cryptography import ControlplanePaymentCryptographyPublicKeyCertificate, ControlplanePaymentCryptographyPublicKeyCertificateConfig

    def describe():
        ubx.intent("look up aws_controlplane_payment_cryptography_public_key_certificate")

        ubx.data(ControlplanePaymentCryptographyPublicKeyCertificate, "example", ControlplanePaymentCryptographyPublicKeyCertificateConfig(
            key_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key_identifier" type="string" required>
  &lt;p&gt;A key identifier that can be either a key ARN or an alias name. This allows flexible key identification in operations.&lt;/p&gt; &lt;p&gt;When using a key ARN, it must be a fully qualified ARN in the format: &lt;code&gt;arn:aws:payment-cryptography:region:account:key/key-id&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;When using an alias, it must begin with &lt;code&gt;alias/&lt;/code&gt; followed by the alias name.&lt;/p&gt; &lt;important&gt; &lt;p&gt;Do not include confidential or sensitive information in this field. This field may be displayed in plaintext in CloudTrail logs and other output.&lt;/p&gt; &lt;/important&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_certificate" type="string">
  The real, PEM-encoded certificate for this key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="key_certificate_chain" type="string">
  The real, full certificate chain for this key. **(AI-inferred)** Returned by the lookup.
</ResponseField>
