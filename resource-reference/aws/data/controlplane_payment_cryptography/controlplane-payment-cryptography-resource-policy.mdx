---
title: "aws_controlplane_payment_cryptography_resource_policy"
description: "`aws_controlplane_payment_cryptography_resource_policy` reads the real resource-based policy attached to a Payment Cryptography key."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controlplane_payment_cryptography "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/controlplane_payment_cryptography"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_controlplane_payment_cryptography_resource_policy"})

    		ubx.Data(controlplane_payment_cryptography.ControlplanePaymentCryptographyResourcePolicy, "example", controlplane_payment_cryptography.ControlplanePaymentCryptographyResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlplanePaymentCryptographyResourcePolicy } from "@ubx/sdk-aws/aws/data/controlplane_payment_cryptography/controlplane_payment_cryptography_resource_policy";

    export default stack("example", () => {
      intent({
        summary: "look up aws_controlplane_payment_cryptography_resource_policy",
      });

      data(ControlplanePaymentCryptographyResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.controlplane_payment_cryptography import ControlplanePaymentCryptographyResourcePolicy, ControlplanePaymentCryptographyResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_controlplane_payment_cryptography_resource_policy")

        ubx.data(ControlplanePaymentCryptographyResourcePolicy, "example", ControlplanePaymentCryptographyResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
