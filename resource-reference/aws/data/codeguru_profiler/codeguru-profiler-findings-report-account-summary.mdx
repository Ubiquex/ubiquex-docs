---
title: "aws_codeguru_profiler_findings_report_account_summary"
description: "`aws_codeguru_profiler_findings_report_account_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codeguru_profiler_findings_report_account_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeguru_profiler "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codeguru_profiler"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codeguru_profiler_findings_report_account_summary"})

    		ubx.Data(codeguru_profiler.CodeguruProfilerFindingsReportAccountSummary, "example", codeguru_profiler.CodeguruProfilerFindingsReportAccountSummaryConfig{
    			DailyReportsOnly: true,
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeguruProfilerFindingsReportAccountSummary } from "@ubx/sdk-aws/aws/data/codeguru_profiler/codeguru_profiler_findings_report_account_summary";

    export default stack("example", () => {
      intent({
        summary: "look up aws_codeguru_profiler_findings_report_account_summary",
      });

      data(CodeguruProfilerFindingsReportAccountSummary, "example", {
        dailyReportsOnly: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codeguru_profiler import CodeguruProfilerFindingsReportAccountSummary, CodeguruProfilerFindingsReportAccountSummaryConfig

    def describe():
        ubx.intent("look up aws_codeguru_profiler_findings_report_account_summary")

        ubx.data(CodeguruProfilerFindingsReportAccountSummary, "example", CodeguruProfilerFindingsReportAccountSummaryConfig(
            daily_reports_only=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="daily_reports_only" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="report_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
