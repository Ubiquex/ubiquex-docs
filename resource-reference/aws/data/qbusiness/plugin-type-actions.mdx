---
title: "aws_qbusiness_plugin_type_actions"
description: "`aws_qbusiness_plugin_type_actions` reads the real, available actions for a given Amazon Q Business plugin type (such as Jira or Salesforce)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_plugin_type_actions"})

    		ubx.Data(qbusiness.PluginTypeActions, "example", qbusiness.PluginTypeActionsConfig{
    			PluginType: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PluginTypeActions } from "@ubx/sdk-aws/aws/data/qbusiness/plugin_type_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_plugin_type_actions" });

      data(PluginTypeActions, "example", {
        pluginType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import PluginTypeActions, PluginTypeActionsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_plugin_type_actions")

        ubx.data(PluginTypeActions, "example", PluginTypeActionsConfig(
            plugin_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plugin_type" type="string" required>
  The real, discriminating type of the plugin this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
