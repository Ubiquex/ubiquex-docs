---
title: "aws_qbusiness_groups"
description: "`aws_qbusiness_groups` reads the real user groups mapped into an Amazon Q Business application's own access control."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_groups"})

    		ubx.Data(qbusiness.Groups, "example", qbusiness.GroupsConfig{
    			ApplicationId:      "example",
    			IndexId:            "example",
    			UpdatedEarlierThan: "example",
    			DataSourceId:       "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Groups } from "./local-sdk/aws/sdk/typescript/aws/data/qbusiness/groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_groups" });

      data(Groups, "example", {
        applicationId: "example",
        indexId: "example",
        updatedEarlierThan: "example",
        dataSourceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Groups, GroupsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_groups")

        ubx.data(Groups, "example", GroupsConfig(
            application_id="example",
            index_id="example",
            updated_earlier_than="example",
            data_source_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_source_id" type="string">
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="updated_earlier_than" type="string" required>
  Restricts results to objects updated earlier than this timestamp. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
