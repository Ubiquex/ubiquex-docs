---
title: "aws_qbusiness_group"
description: "`aws_qbusiness_group` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_qbusiness_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_group"})

    		ubx.Data(qbusiness.Group, "example", qbusiness.GroupConfig{
    			ApplicationId: "example",
    			GroupName:     "example-group-name",
    			IndexId:       "example",
    			DataSourceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Group } from "@ubx/sdk-aws/aws/data/qbusiness/group";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_group" });

      data(Group, "example", {
        applicationId: "example",
        groupName: "example-group-name",
        indexId: "example",
        dataSourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Group, GroupConfig

    def describe():
        ubx.intent("look up aws_qbusiness_group")

        ubx.data(Group, "example", GroupConfig(
            application_id="example",
            group_name="example-group-name",
            index_id="example",
            data_source_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="data_source_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="object">
  &lt;p&gt;Provides the details of a group's status.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error_detail" type="object">
      &lt;p&gt;Provides information about a Amazon Q Business request error.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="error_code" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="error_message" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_updated_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status_history" type="list(object)">
  Returned by the lookup.
</ResponseField>
