---
title: "aws_qbusiness_data_source_sync_jobs"
description: "`aws_qbusiness_data_source_sync_jobs` reads the real sync jobs run for an Amazon Q Business data source connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_data_source_sync_jobs"})

    		ubx.Data(qbusiness.DataSourceSyncJobs, "example", qbusiness.DataSourceSyncJobsConfig{
    			ApplicationId: "example",
    			DataSourceId:  "example",
    			IndexId:       "example",
    			EndTime:       "example",
    			MaxResults:    1,
    			StartTime:     "example",
    			StatusFilter:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSourceSyncJobs } from "@ubx/sdk-aws/aws/data/qbusiness/data_source_sync_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_data_source_sync_jobs" });

      data(DataSourceSyncJobs, "example", {
        applicationId: "example",
        dataSourceId: "example",
        indexId: "example",
        endTime: "example",
        maxResults: 1,
        startTime: "example",
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import DataSourceSyncJobs, DataSourceSyncJobsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_data_source_sync_jobs")

        ubx.data(DataSourceSyncJobs, "example", DataSourceSyncJobsConfig(
            application_id="example",
            data_source_id="example",
            index_id="example",
            end_time="example",
            max_results=1,
            start_time="example",
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_source_id" type="string" required>
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="string">
  Restricts results to objects matching this status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="history" type="list(object)">
  The real history of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
