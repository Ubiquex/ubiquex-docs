---
title: "aws_qbusiness_policy"
description: "`aws_qbusiness_policy` reads the real resource-based policy attached to an Amazon Q Business application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_policy"})

    		ubx.Data(qbusiness.Policy, "example", qbusiness.PolicyConfig{
    			ApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Policy } from "@ubx/sdk-aws/aws/data/qbusiness/policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_policy" });

      data(Policy, "example", {
        applicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Policy, PolicyConfig

    def describe():
        ubx.intent("look up aws_qbusiness_policy")

        ubx.data(Policy, "example", PolicyConfig(
            application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
