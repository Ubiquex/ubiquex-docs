---
title: "aws_qbusiness_documents"
description: "`aws_qbusiness_documents` looks up a real document indexed within an Amazon Q Business application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_documents"})

    		ubx.Data(qbusiness.Documents, "example", qbusiness.DocumentsConfig{
    			ApplicationId: "example",
    			IndexId:       "example",
    			DataSourceIds: []string{"example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Documents } from "@ubx/sdk-aws/aws/data/qbusiness/documents";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_documents" });

      data(Documents, "example", {
        applicationId: "example",
        indexId: "example",
        dataSourceIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Documents, DocumentsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_documents")

        ubx.data(Documents, "example", DocumentsConfig(
            application_id="example",
            index_id="example",
            data_source_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_source_ids" type="list(string)">
  The real, unique identifiers of the data sources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document_detail_list" type="list(object)">
  The real document details included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
