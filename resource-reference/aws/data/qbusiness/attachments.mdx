---
title: "aws_qbusiness_attachments"
description: "`aws_qbusiness_attachments` reads the real file attachments included in an Amazon Q Business conversation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_attachments"})

    		ubx.Data(qbusiness.Attachments, "example", qbusiness.AttachmentsConfig{
    			ApplicationId:  "example",
    			ConversationId: "example",
    			MaxResults:     1,
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attachments } from "@ubx/sdk-aws/aws/data/qbusiness/attachments";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_attachments" });

      data(Attachments, "example", {
        applicationId: "example",
        conversationId: "example",
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Attachments, AttachmentsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_attachments")

        ubx.data(Attachments, "example", AttachmentsConfig(
            application_id="example",
            conversation_id="example",
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="conversation_id" type="string">
  The real, unique identifier of the conversation this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attachments" type="list(object)">
  The real attachments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
