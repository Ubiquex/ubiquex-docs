---
title: "aws_qbusiness_attachments"
description: "`aws_qbusiness_attachments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_qbusiness_attachments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_attachments"})

    		ubx.Data(qbusiness.Attachments, "example", qbusiness.AttachmentsConfig{
    			ApplicationId:  "example",
    			ConversationId: "example",
    			MaxResults:     1,
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attachments } from "@ubx/sdk-aws/aws/data/qbusiness/attachments";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_attachments" });

      data(Attachments, "example", {
        applicationId: "example",
        conversationId: "example",
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Attachments, AttachmentsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_attachments")

        ubx.data(Attachments, "example", AttachmentsConfig(
            application_id="example",
            conversation_id="example",
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="conversation_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attachments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
