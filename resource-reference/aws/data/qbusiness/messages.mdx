---
title: "aws_qbusiness_messages"
description: "`aws_qbusiness_messages` reads the real messages exchanged within an Amazon Q Business conversation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_messages"})

    		ubx.Data(qbusiness.Messages, "example", qbusiness.MessagesConfig{
    			ApplicationId:  "example",
    			ConversationId: "example",
    			MaxResults:     1,
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Messages } from "./local-sdk/aws/sdk/typescript/aws/data/qbusiness/messages.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_messages" });

      data(Messages, "example", {
        applicationId: "example",
        conversationId: "example",
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import Messages, MessagesConfig

    def describe():
        ubx.intent("look up aws_qbusiness_messages")

        ubx.data(Messages, "example", MessagesConfig(
            application_id="example",
            conversation_id="example",
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="conversation_id" type="string" required>
  The real, unique identifier of the conversation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="messages" type="list(object)">
  The real messages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
