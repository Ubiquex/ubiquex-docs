---
title: "aws_qbusiness_plugin_type_metadata"
description: "`aws_qbusiness_plugin_type_metadata` reads real metadata describing a given Amazon Q Business plugin type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_plugin_type_metadata"})

    		ubx.Data(qbusiness.PluginTypeMetadata, "example", qbusiness.PluginTypeMetadataConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PluginTypeMetadata } from "@ubx/sdk-aws/aws/data/qbusiness/plugin_type_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_plugin_type_metadata" });

      data(PluginTypeMetadata, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import PluginTypeMetadata, PluginTypeMetadataConfig

    def describe():
        ubx.intent("look up aws_qbusiness_plugin_type_metadata")

        ubx.data(PluginTypeMetadata, "example", PluginTypeMetadataConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
