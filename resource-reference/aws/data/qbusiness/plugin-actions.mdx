---
title: "aws_qbusiness_plugin_actions"
description: "`data_aws_qbusiness_plugin_actions` looks up a real, already-existing `aws_qbusiness_plugin`."
---

It connects a real third-party or custom action, such as creating a ticket or updating a record in another system, to an `aws_qbusiness_application`, letting its assistant take actions rather than only answer questions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_plugin_actions"})

    		ubx.Data(qbusiness.PluginActions, "example", qbusiness.PluginActionsConfig{
    			ApplicationId: "example",
    			PluginId:      "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PluginActions } from "./local-sdk/aws/sdk/typescript/aws/data/qbusiness/plugin_actions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_plugin_actions" });

      data(PluginActions, "example", {
        applicationId: "example",
        pluginId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import PluginActions, PluginActionsConfig

    def describe():
        ubx.intent("look up aws_qbusiness_plugin_actions")

        ubx.data(PluginActions, "example", PluginActionsConfig(
            application_id="example",
            plugin_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plugin_id" type="string" required>
  The real, unique identifier of the plugin this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
