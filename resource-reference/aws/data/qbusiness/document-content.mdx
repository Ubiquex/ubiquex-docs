---
title: "aws_qbusiness_document_content"
description: "`aws_qbusiness_document_content` reads the real, indexed content of a document ingested into an Amazon Q Business application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	qbusiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/qbusiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_qbusiness_document_content"})

    		ubx.Data(qbusiness.DocumentContent, "example", qbusiness.DocumentContentConfig{
    			ApplicationId: "example",
    			DocumentId:    "example",
    			IndexId:       "example",
    			DataSourceId:  "example",
    			OutputFormat:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentContent } from "@ubx/sdk-aws/aws/data/qbusiness/document_content";

    export default stack("example", () => {
      intent({ summary: "look up aws_qbusiness_document_content" });

      data(DocumentContent, "example", {
        applicationId: "example",
        documentId: "example",
        indexId: "example",
        dataSourceId: "example",
        outputFormat: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.qbusiness import DocumentContent, DocumentContentConfig

    def describe():
        ubx.intent("look up aws_qbusiness_document_content")

        ubx.data(DocumentContent, "example", DocumentContentConfig(
            application_id="example",
            document_id="example",
            index_id="example",
            data_source_id="example",
            output_format="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_source_id" type="string">
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  The real, unique identifier of the document this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The real, unique identifier of the index this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="output_format" type="string">
  The real, requested output format of the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mime_type" type="string">
  The real, discriminating type of the mime this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="presigned_url" type="string">
  The real, resolvable URL of the presigned this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
