---
title: "aws_appfabric_ingestions"
description: "`aws_appfabric_ingestions` looks up a real AppFabric ingestion, continuously pulling audit log data from a connected SaaS application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appfabric_ingestions"})

    		ubx.Data(appfabric.Ingestions, "example", appfabric.IngestionsConfig{
    			AppBundleIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Ingestions } from "./local-sdk/aws/sdk/typescript/aws/data/appfabric/ingestions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appfabric_ingestions" });

      data(Ingestions, "example", {
        appBundleIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appfabric import Ingestions, IngestionsConfig

    def describe():
        ubx.intent("look up aws_appfabric_ingestions")

        ubx.data(Ingestions, "example", IngestionsConfig(
            app_bundle_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_bundle_identifier" type="string" required>
  The real, unique identifier of the app bundle this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ingestions" type="list(object)">
  The real ingestions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
