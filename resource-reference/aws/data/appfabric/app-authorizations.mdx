---
title: "aws_appfabric_app_authorizations"
description: "`aws_appfabric_app_authorizations` looks up a real AppFabric app authorization, the connected credentials for a SaaS application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appfabric_app_authorizations"})

    		ubx.Data(appfabric.AppAuthorizations, "example", appfabric.AppAuthorizationsConfig{
    			AppBundleIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppAuthorizations } from "@ubx/sdk-aws/aws/data/appfabric/app_authorizations";

    export default stack("example", () => {
      intent({ summary: "look up aws_appfabric_app_authorizations" });

      data(AppAuthorizations, "example", {
        appBundleIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appfabric import AppAuthorizations, AppAuthorizationsConfig

    def describe():
        ubx.intent("look up aws_appfabric_app_authorizations")

        ubx.data(AppAuthorizations, "example", AppAuthorizationsConfig(
            app_bundle_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_bundle_identifier" type="string" required>
  The real, unique identifier of the app bundle this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_authorization_summary_list" type="list(object)">
  The real app authorization summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
