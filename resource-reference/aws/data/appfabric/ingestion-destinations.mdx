---
title: "aws_appfabric_ingestion_destinations"
description: "`aws_appfabric_ingestion_destinations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appfabric_ingestion_destinations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appfabric_ingestion_destinations"})

    		ubx.Data(appfabric.IngestionDestinations, "example", appfabric.IngestionDestinationsConfig{
    			AppBundleIdentifier: "example",
    			IngestionIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IngestionDestinations } from "@ubx/sdk-aws/aws/data/appfabric/ingestion_destinations";

    export default stack("example", () => {
      intent({ summary: "look up aws_appfabric_ingestion_destinations" });

      data(IngestionDestinations, "example", {
        appBundleIdentifier: "example",
        ingestionIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appfabric import IngestionDestinations, IngestionDestinationsConfig

    def describe():
        ubx.intent("look up aws_appfabric_ingestion_destinations")

        ubx.data(IngestionDestinations, "example", IngestionDestinationsConfig(
            app_bundle_identifier="example",
            ingestion_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_bundle_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="ingestion_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ingestion_destinations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
