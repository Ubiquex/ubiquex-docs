---
title: "aws_appfabric_app_bundles"
description: "`aws_appfabric_app_bundles` looks up a real AppFabric app bundle, the top-level container for connected SaaS applications."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appfabric_app_bundles"})

    		ubx.Data(appfabric.AppBundles, "example", appfabric.AppBundlesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppBundles } from "@ubx/sdk-aws/aws/data/appfabric/app_bundles";

    export default stack("example", () => {
      intent({ summary: "look up aws_appfabric_app_bundles" });

      data(AppBundles, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appfabric import AppBundles, AppBundlesConfig

    def describe():
        ubx.intent("look up aws_appfabric_app_bundles")

        ubx.data(AppBundles, "example", AppBundlesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_bundle_summary_list" type="list(object)">
  The real app bundle summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
