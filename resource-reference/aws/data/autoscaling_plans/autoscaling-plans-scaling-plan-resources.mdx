---
title: "aws_autoscaling_plans_scaling_plan_resources"
description: "`data_aws_autoscaling_plans_scaling_plan_resources` looks up a real, already-existing `aws_auto_scaling_plans_scaling_plan`."
---

It combines target tracking and predictive scaling into one set of instructions applied across a tagged or CloudFormation-scoped group of resources, generating the real, underlying scaling policies on the caller's behalf from a single higher-level strategy like optimize-for-cost or optimize-for-availability.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	autoscaling_plans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling_plans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_plans_scaling_plan_resources"})

    		ubx.Data(autoscaling_plans.AutoscalingPlansScalingPlanResources, "example", autoscaling_plans.AutoscalingPlansScalingPlanResourcesConfig{
    			ScalingPlanName:    "example-scaling-plan-name",
    			ScalingPlanVersion: 1,
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AutoscalingPlansScalingPlanResources } from "./local-sdk/aws/sdk/typescript/aws/data/autoscaling_plans/autoscaling_plans_scaling_plan_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_plans_scaling_plan_resources" });

      data(AutoscalingPlansScalingPlanResources, "example", {
        scalingPlanName: "example-scaling-plan-name",
        scalingPlanVersion: 1,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling_plans import AutoscalingPlansScalingPlanResources, AutoscalingPlansScalingPlanResourcesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_plans_scaling_plan_resources")

        ubx.data(AutoscalingPlansScalingPlanResources, "example", AutoscalingPlansScalingPlanResourcesConfig(
            scaling_plan_name="example-scaling-plan-name",
            scaling_plan_version=1,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_name" type="string" required>
  The real name of the scaling plan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_version" type="number" required>
  The real version of the scaling plan. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_plan_resources" type="list(object)">
  The real AWS Auto Scaling scaling plan resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
