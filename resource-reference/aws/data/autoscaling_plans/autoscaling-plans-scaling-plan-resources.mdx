---
title: "aws_autoscaling_plans_scaling_plan_resources"
description: "`aws_autoscaling_plans_scaling_plan_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_autoscaling_plans_scaling_plan_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling_plans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling_plans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_plans_scaling_plan_resources"})

    		ubx.Data(autoscaling_plans.AutoscalingPlansScalingPlanResources, "example", autoscaling_plans.AutoscalingPlansScalingPlanResourcesConfig{
    			ScalingPlanName:    "example-scaling-plan-name",
    			ScalingPlanVersion: 1,
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutoscalingPlansScalingPlanResources } from "@ubx/sdk-aws/aws/data/autoscaling_plans/autoscaling_plans_scaling_plan_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_autoscaling_plans_scaling_plan_resources" });

      data(AutoscalingPlansScalingPlanResources, "example", {
        scalingPlanName: "example-scaling-plan-name",
        scalingPlanVersion: 1,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling_plans import AutoscalingPlansScalingPlanResources, AutoscalingPlansScalingPlanResourcesConfig

    def describe():
        ubx.intent("look up aws_autoscaling_plans_scaling_plan_resources")

        ubx.data(AutoscalingPlansScalingPlanResources, "example", AutoscalingPlansScalingPlanResourcesConfig(
            scaling_plan_name="example-scaling-plan-name",
            scaling_plan_version=1,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_version" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_plan_resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
