---
title: "aws_autoscaling_plans_scaling_plan_resource_forecast_data"
description: "`aws_autoscaling_plans_scaling_plan_resource_forecast_data` reads real, forecasted load and capacity data for an AWS Auto Scaling scaling plan resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	autoscaling_plans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/autoscaling_plans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_autoscaling_plans_scaling_plan_resource_forecast_data"})

    		ubx.Data(autoscaling_plans.AutoscalingPlansScalingPlanResourceForecastData, "example", autoscaling_plans.AutoscalingPlansScalingPlanResourceForecastDataConfig{
    			EndTime:            "example",
    			ForecastDataType:   "example",
    			ResourceId:         "example",
    			ScalableDimension:  "example",
    			ScalingPlanName:    "example-scaling-plan-name",
    			ScalingPlanVersion: 1,
    			ServiceNamespace:   "example",
    			StartTime:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutoscalingPlansScalingPlanResourceForecastData } from "@ubx/sdk-aws/aws/data/autoscaling_plans/autoscaling_plans_scaling_plan_resource_forecast_data";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_autoscaling_plans_scaling_plan_resource_forecast_data",
      });

      data(AutoscalingPlansScalingPlanResourceForecastData, "example", {
        endTime: "example",
        forecastDataType: "example",
        resourceId: "example",
        scalableDimension: "example",
        scalingPlanName: "example-scaling-plan-name",
        scalingPlanVersion: 1,
        serviceNamespace: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.autoscaling_plans import AutoscalingPlansScalingPlanResourceForecastData, AutoscalingPlansScalingPlanResourceForecastDataConfig

    def describe():
        ubx.intent("look up aws_autoscaling_plans_scaling_plan_resource_forecast_data")

        ubx.data(AutoscalingPlansScalingPlanResourceForecastData, "example", AutoscalingPlansScalingPlanResourceForecastDataConfig(
            end_time="example",
            forecast_data_type="example",
            resource_id="example",
            scalable_dimension="example",
            scaling_plan_name="example-scaling-plan-name",
            scaling_plan_version=1,
            service_namespace="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="forecast_data_type" type="string" required>
  The real, discriminating type of the forecast data this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  The real Application Auto Scaling scalable dimension this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_name" type="string" required>
  The real name of the scaling plan this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scaling_plan_version" type="number" required>
  The real version of the scaling plan. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  The real Application Auto Scaling service namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="datapoints" type="list(object)">
  The real, computed data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
