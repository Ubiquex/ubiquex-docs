---
title: "aws_route53_recovery_readiness_cell_readiness_summary"
description: "`aws_route53_recovery_readiness_cell_readiness_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_recovery_readiness_cell_readiness_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53_recovery_readiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_readiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_readiness_cell_readiness_summary"})

    		ubx.Data(route53_recovery_readiness.Route53RecoveryReadinessCellReadinessSummary, "example", route53_recovery_readiness.Route53RecoveryReadinessCellReadinessSummaryConfig{
    			CellName:   "example-cell-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Route53RecoveryReadinessCellReadinessSummary } from "@ubx/sdk-aws/aws/data/route53_recovery_readiness/route53_recovery_readiness_cell_readiness_summary";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53_recovery_readiness_cell_readiness_summary",
      });

      data(Route53RecoveryReadinessCellReadinessSummary, "example", {
        cellName: "example-cell-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_readiness import Route53RecoveryReadinessCellReadinessSummary, Route53RecoveryReadinessCellReadinessSummaryConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_readiness_cell_readiness_summary")

        ubx.data(Route53RecoveryReadinessCellReadinessSummary, "example", Route53RecoveryReadinessCellReadinessSummaryConfig(
            cell_name="example-cell-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cell_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="readiness" type="string">
  &lt;p&gt;The readiness status.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="readiness_checks" type="list(object)">
  Returned by the lookup.
</ResponseField>
