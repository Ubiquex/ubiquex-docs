---
title: "aws_route53_recovery_readiness_architecture_recommendations"
description: "`aws_route53_recovery_readiness_architecture_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_recovery_readiness_architecture_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53_recovery_readiness "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_readiness"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_readiness_architecture_recommendations"})

    		ubx.Data(route53_recovery_readiness.Route53RecoveryReadinessArchitectureRecommendations, "example", route53_recovery_readiness.Route53RecoveryReadinessArchitectureRecommendationsConfig{
    			RecoveryGroupName: "example-recovery-group-name",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Route53RecoveryReadinessArchitectureRecommendations } from "@ubx/sdk-aws/aws/data/route53_recovery_readiness/route53_recovery_readiness_architecture_recommendations";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_route53_recovery_readiness_architecture_recommendations",
      });

      data(Route53RecoveryReadinessArchitectureRecommendations, "example", {
        recoveryGroupName: "example-recovery-group-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_readiness import Route53RecoveryReadinessArchitectureRecommendations, Route53RecoveryReadinessArchitectureRecommendationsConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_readiness_architecture_recommendations")

        ubx.data(Route53RecoveryReadinessArchitectureRecommendations, "example", Route53RecoveryReadinessArchitectureRecommendationsConfig(
            recovery_group_name="example-recovery-group-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recovery_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_audit_timestamp" type="string">
  &lt;p&gt;The time that a recovery group was last assessed for recommendations, in UTC ISO-8601 format.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
