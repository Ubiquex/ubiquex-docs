---
title: "aws_app_integrations_data_integrations"
description: "`aws_app_integrations_data_integrations` looks up a real App Integrations data integration, syncing external data into Amazon Connect."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app_integrations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/app_integrations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_app_integrations_data_integrations"})

    		ubx.Data(app_integrations.AppIntegrationsDataIntegrations, "example", app_integrations.AppIntegrationsDataIntegrationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppIntegrationsDataIntegrations } from "@ubx/sdk-aws/aws/data/app_integrations/app_integrations_data_integrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_app_integrations_data_integrations" });

      data(AppIntegrationsDataIntegrations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.app_integrations import AppIntegrationsDataIntegrations, AppIntegrationsDataIntegrationsConfig

    def describe():
        ubx.intent("look up aws_app_integrations_data_integrations")

        ubx.data(AppIntegrationsDataIntegrations, "example", AppIntegrationsDataIntegrationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_integrations" type="list(object)">
  The real data integrations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
