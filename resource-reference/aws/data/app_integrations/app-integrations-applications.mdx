---
title: "aws_app_integrations_applications"
description: "`aws_app_integrations_applications` looks up a real App Integrations application, connecting a third-party app into Connect's own agent workspace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app_integrations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/app_integrations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_app_integrations_applications"})

    		ubx.Data(app_integrations.AppIntegrationsApplications, "example", app_integrations.AppIntegrationsApplicationsConfig{
    			ApplicationType: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppIntegrationsApplications } from "@ubx/sdk-aws/aws/data/app_integrations/app_integrations_applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_app_integrations_applications" });

      data(AppIntegrationsApplications, "example", {
        applicationType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.app_integrations import AppIntegrationsApplications, AppIntegrationsApplicationsConfig

    def describe():
        ubx.intent("look up aws_app_integrations_applications")

        ubx.data(AppIntegrationsApplications, "example", AppIntegrationsApplicationsConfig(
            application_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_type" type="string">
  &lt;value&gt;The type of application&lt;/value&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
