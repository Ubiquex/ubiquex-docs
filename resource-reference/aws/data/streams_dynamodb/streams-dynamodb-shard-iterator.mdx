---
title: "aws_streams_dynamodb_shard_iterator"
description: "`aws_streams_dynamodb_shard_iterator` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_streams_dynamodb_shard_iterator` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	streams_dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/streams_dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_streams_dynamodb_shard_iterator"})

    		ubx.Data(streams_dynamodb.StreamsDynamodbShardIterator, "example", streams_dynamodb.StreamsDynamodbShardIteratorConfig{
    			ShardId:           "example",
    			ShardIteratorType: "example",
    			StreamArn:         "arn:aws:iam::123456789012:policy/example",
    			SequenceNumber:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamsDynamodbShardIterator } from "@ubx/sdk-aws/aws/data/streams_dynamodb/streams_dynamodb_shard_iterator";

    export default stack("example", () => {
      intent({ summary: "look up aws_streams_dynamodb_shard_iterator" });

      data(StreamsDynamodbShardIterator, "example", {
        shardId: "example",
        shardIteratorType: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
        sequenceNumber: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.streams_dynamodb import StreamsDynamodbShardIterator, StreamsDynamodbShardIteratorConfig

    def describe():
        ubx.intent("look up aws_streams_dynamodb_shard_iterator")

        ubx.data(StreamsDynamodbShardIterator, "example", StreamsDynamodbShardIteratorConfig(
            shard_id="example",
            shard_iterator_type="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
            sequence_number="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sequence_number" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shard_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="shard_iterator_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="shard_iterator" type="string">
  Returned by the lookup.
</ResponseField>
