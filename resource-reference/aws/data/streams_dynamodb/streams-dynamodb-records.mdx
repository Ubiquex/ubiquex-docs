---
title: "aws_streams_dynamodb_records"
description: "`aws_streams_dynamodb_records` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_streams_dynamodb_records` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	streams_dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/streams_dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_streams_dynamodb_records"})

    		ubx.Data(streams_dynamodb.StreamsDynamodbRecords, "example", streams_dynamodb.StreamsDynamodbRecordsConfig{
    			ShardIterator: "example",
    			Limit:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamsDynamodbRecords } from "@ubx/sdk-aws/aws/data/streams_dynamodb/streams_dynamodb_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_streams_dynamodb_records" });

      data(StreamsDynamodbRecords, "example", {
        shardIterator: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.streams_dynamodb import StreamsDynamodbRecords, StreamsDynamodbRecordsConfig

    def describe():
        ubx.intent("look up aws_streams_dynamodb_records")

        ubx.data(StreamsDynamodbRecords, "example", StreamsDynamodbRecordsConfig(
            shard_iterator="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shard_iterator" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_shard_iterator" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  Returned by the lookup.
</ResponseField>
