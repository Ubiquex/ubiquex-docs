---
title: "aws_workspaces_workspace_directories"
description: "`aws_workspaces_workspace_directories` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_workspace_directories` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspace_directories"})

    		ubx.Data(workspaces.WorkspaceDirectories, "example", workspaces.WorkspaceDirectoriesConfig{
    			DirectoryIds:            []string{"example"},
    			Filters:                 []map[string]any{{"name": "example-name"}},
    			Limit:                   1,
    			WorkspaceDirectoryNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspaceDirectories } from "@ubx/sdk-aws/aws/data/workspaces/workspace_directories";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspace_directories" });

      data(WorkspaceDirectories, "example", {
        directoryIds: ["example"],
        filters: [{ name: "example-name" }],
        limit: 1,
        workspaceDirectoryNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspaceDirectories, WorkspaceDirectoriesConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspace_directories")

        ubx.data(WorkspaceDirectories, "example", WorkspaceDirectoriesConfig(
            directory_ids=["example"],
            filters=[{"name": "example-name"}],
            limit=1,
            workspace_directory_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_directory_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="directories" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
