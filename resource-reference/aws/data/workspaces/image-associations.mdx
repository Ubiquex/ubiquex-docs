---
title: "aws_workspaces_image_associations"
description: "`aws_workspaces_image_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_image_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_image_associations"})

    		ubx.Data(workspaces.ImageAssociations, "example", workspaces.ImageAssociationsConfig{
    			AssociatedResourceTypes: []string{"example"},
    			ImageId:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageAssociations } from "@ubx/sdk-aws/aws/data/workspaces/image_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_image_associations" });

      data(ImageAssociations, "example", {
        associatedResourceTypes: ["example"],
        imageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import ImageAssociations, ImageAssociationsConfig

    def describe():
        ubx.intent("look up aws_workspaces_image_associations")

        ubx.data(ImageAssociations, "example", ImageAssociationsConfig(
            associated_resource_types=["example"],
            image_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="associated_resource_types" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="image_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
