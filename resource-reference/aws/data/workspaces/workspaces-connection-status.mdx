---
title: "aws_workspaces_workspaces_connection_status"
description: "`aws_workspaces_workspaces_connection_status` reads the real, current connection status of a WorkSpace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspaces_connection_status"})

    		ubx.Data(workspaces.WorkspacesConnectionStatus, "example", workspaces.WorkspacesConnectionStatusConfig{
    			WorkspaceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesConnectionStatus } from "@ubx/sdk-aws/aws/data/workspaces/workspaces_connection_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspaces_connection_status" });

      data(WorkspacesConnectionStatus, "example", {
        workspaceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspacesConnectionStatus, WorkspacesConnectionStatusConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspaces_connection_status")

        ubx.data(WorkspacesConnectionStatus, "example", WorkspacesConnectionStatusConfig(
            workspace_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_ids" type="list(string)">
  The real, unique identifiers of the workspaces this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workspaces_connection_status" type="list(object)">
  The real, current status of the workspaces connection this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
