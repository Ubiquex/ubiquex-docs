---
title: "aws_workspaces_available_management_cidr_ranges"
description: "`aws_workspaces_available_management_cidr_ranges` reads the real, available CIDR ranges for WorkSpaces' own management network interface."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_available_management_cidr_ranges"})

    		ubx.Data(workspaces.AvailableManagementCidrRanges, "example", workspaces.AvailableManagementCidrRangesConfig{
    			ManagementCidrRangeConstraint: "example",
    			MaxResults:                    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailableManagementCidrRanges } from "@ubx/sdk-aws/aws/data/workspaces/available_management_cidr_ranges";

    export default stack("example", () => {
      intent({
        summary: "look up aws_workspaces_available_management_cidr_ranges",
      });

      data(AvailableManagementCidrRanges, "example", {
        managementCidrRangeConstraint: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import AvailableManagementCidrRanges, AvailableManagementCidrRangesConfig

    def describe():
        ubx.intent("look up aws_workspaces_available_management_cidr_ranges")

        ubx.data(AvailableManagementCidrRanges, "example", AvailableManagementCidrRangesConfig(
            management_cidr_range_constraint="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="management_cidr_range_constraint" type="string" required>
  The real CIDR range constraint applied to management traffic. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="management_cidr_ranges" type="list(string)">
  The real CIDR ranges used for management traffic. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
