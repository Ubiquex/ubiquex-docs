---
title: "aws_workspaces_client_properties"
description: "`aws_workspaces_client_properties` reads the real client device properties configured for a WorkSpaces directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_client_properties"})

    		ubx.Data(workspaces.ClientProperties, "example", workspaces.ClientPropertiesConfig{
    			ResourceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ClientProperties } from "./local-sdk/aws/sdk/typescript/aws/data/workspaces/client_properties.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_client_properties" });

      data(ClientProperties, "example", {
        resourceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import ClientProperties, ClientPropertiesConfig

    def describe():
        ubx.intent("look up aws_workspaces_client_properties")

        ubx.data(ClientProperties, "example", ClientPropertiesConfig(
            resource_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_ids" type="list(string)" required>
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_properties_list" type="list(object)">
  The real client propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
