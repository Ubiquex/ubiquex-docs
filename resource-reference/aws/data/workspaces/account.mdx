---
title: "aws_workspaces_account"
description: "`aws_workspaces_account` reads real, account-level WorkSpaces settings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_account"})

    		ubx.Data(workspaces.Account, "example", workspaces.AccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Account } from "@ubx/sdk-aws/aws/data/workspaces/account";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_account" });

      data(Account, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import Account, AccountConfig

    def describe():
        ubx.intent("look up aws_workspaces_account")

        ubx.data(Account, "example", AccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="dedicated_tenancy_account_type" type="string">
  The real, discriminating type of the dedicated tenancy account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dedicated_tenancy_management_cidr_range" type="string">
  The real CIDR range used to manage dedicated-tenancy WorkSpaces. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dedicated_tenancy_support" type="string">
  Whether dedicated tenancy is supported for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
