---
title: "aws_workspaces_bundle_associations"
description: "`aws_workspaces_bundle_associations` reads the real applications associated with a WorkSpaces bundle."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_bundle_associations"})

    		ubx.Data(workspaces.BundleAssociations, "example", workspaces.BundleAssociationsConfig{
    			AssociatedResourceTypes: []string{"example"},
    			BundleId:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BundleAssociations } from "./local-sdk/aws/sdk/typescript/aws/data/workspaces/bundle_associations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_bundle_associations" });

      data(BundleAssociations, "example", {
        associatedResourceTypes: ["example"],
        bundleId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import BundleAssociations, BundleAssociationsConfig

    def describe():
        ubx.intent("look up aws_workspaces_bundle_associations")

        ubx.data(BundleAssociations, "example", BundleAssociationsConfig(
            associated_resource_types=["example"],
            bundle_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="associated_resource_types" type="list(string)" required>
  The real resource types associated with this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bundle_id" type="string" required>
  The real, unique identifier of the bundle this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  The real associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
