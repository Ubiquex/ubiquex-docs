---
title: "aws_workspaces_applications"
description: "`aws_workspaces_applications` looks up a real WorkSpaces Application, an app deployable onto WorkSpaces Personal desktops via App Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_applications"})

    		ubx.Data(workspaces.Applications, "example", workspaces.ApplicationsConfig{
    			ApplicationIds:       []string{"example"},
    			ComputeTypeNames:     []string{"example"},
    			LicenseType:          "example",
    			MaxResults:           1,
    			OperatingSystemNames: []string{"example"},
    			Owner:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Applications } from "./local-sdk/aws/sdk/typescript/aws/data/workspaces/applications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_applications" });

      data(Applications, "example", {
        applicationIds: ["example"],
        computeTypeNames: ["example"],
        licenseType: "example",
        maxResults: 1,
        operatingSystemNames: ["example"],
        owner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import Applications, ApplicationsConfig

    def describe():
        ubx.intent("look up aws_workspaces_applications")

        ubx.data(Applications, "example", ApplicationsConfig(
            application_ids=["example"],
            compute_type_names=["example"],
            license_type="example",
            max_results=1,
            operating_system_names=["example"],
            owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_ids" type="list(string)">
  The real, unique identifiers of the applications this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="compute_type_names" type="list(string)">
  The real names of the compute types included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="license_type" type="string">
  The real, discriminating type of the license this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="operating_system_names" type="list(string)">
  The real names of the operating systems included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  The real owner of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
