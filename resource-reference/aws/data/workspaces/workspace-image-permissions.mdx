---
title: "aws_workspaces_workspace_image_permissions"
description: "`aws_workspaces_workspace_image_permissions` reads which real AWS accounts a WorkSpaces image has been shared with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspace_image_permissions"})

    		ubx.Data(workspaces.WorkspaceImagePermissions, "example", workspaces.WorkspaceImagePermissionsConfig{
    			ImageId:    "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WorkspaceImagePermissions } from "./local-sdk/aws/sdk/typescript/aws/data/workspaces/workspace_image_permissions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspace_image_permissions" });

      data(WorkspaceImagePermissions, "example", {
        imageId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspaceImagePermissions, WorkspaceImagePermissionsConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspace_image_permissions")

        ubx.data(WorkspaceImagePermissions, "example", WorkspaceImagePermissionsConfig(
            image_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="string" required>
  The real, unique identifier of the image this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_permissions" type="list(object)">
  The real permissions granted on this image. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
