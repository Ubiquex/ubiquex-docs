---
title: "aws_workspaces_custom_workspace_image_import"
description: "`aws_workspaces_custom_workspace_image_import` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_custom_workspace_image_import` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_custom_workspace_image_import"})

    		ubx.Data(workspaces.CustomWorkspaceImageImport, "example", workspaces.CustomWorkspaceImageImportConfig{
    			ImageId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomWorkspaceImageImport } from "@ubx/sdk-aws/aws/data/workspaces/custom_workspace_image_import";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_custom_workspace_image_import" });

      data(CustomWorkspaceImageImport, "example", {
        imageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import CustomWorkspaceImageImport, CustomWorkspaceImageImportConfig

    def describe():
        ubx.intent("look up aws_workspaces_custom_workspace_image_import")

        ubx.data(CustomWorkspaceImageImport, "example", CustomWorkspaceImageImportConfig(
            image_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="image_builder_instance_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="image_source" type="object">
  &lt;p&gt;Describes the image import source.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ec2_image_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ec2_import_task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_build_version_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="infrastructure_configuration_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="progress_percentage" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state_message" type="string">
  Returned by the lookup.
</ResponseField>
