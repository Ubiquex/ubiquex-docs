---
title: "aws_workspaces_workspaces_pool_sessions"
description: "`aws_workspaces_workspaces_pool_sessions` reads the real, active user sessions within a WorkSpaces Pools non-persistent pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspaces_pool_sessions"})

    		ubx.Data(workspaces.WorkspacesPoolSessions, "example", workspaces.WorkspacesPoolSessionsConfig{
    			PoolId: "example",
    			Limit:  1,
    			UserId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesPoolSessions } from "@ubx/sdk-aws/aws/data/workspaces/workspaces_pool_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspaces_pool_sessions" });

      data(WorkspacesPoolSessions, "example", {
        poolId: "example",
        limit: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspacesPoolSessions, WorkspacesPoolSessionsConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspaces_pool_sessions")

        ubx.data(WorkspacesPoolSessions, "example", WorkspacesPoolSessionsConfig(
            pool_id="example",
            limit=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pool_id" type="string" required>
  The real, unique identifier of the pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  The real sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
