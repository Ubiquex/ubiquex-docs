---
title: "aws_workspaces_account_link"
description: "`aws_workspaces_account_link` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_account_link` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_account_link"})

    		ubx.Data(workspaces.AccountLink, "example", workspaces.AccountLinkConfig{
    			LinkId:          "example",
    			LinkedAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLink } from "@ubx/sdk-aws/aws/data/workspaces/account_link";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_account_link" });

      data(AccountLink, "example", {
        linkId: "example",
        linkedAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import AccountLink, AccountLinkConfig

    def describe():
        ubx.intent("look up aws_workspaces_account_link")

        ubx.data(AccountLink, "example", AccountLinkConfig(
            link_id="example",
            linked_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="link_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="linked_account_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_link" type="object">
  &lt;p&gt;Information about about the account link.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_link_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="account_link_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_account_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
