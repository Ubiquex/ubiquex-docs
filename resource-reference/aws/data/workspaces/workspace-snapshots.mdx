---
title: "aws_workspaces_workspace_snapshots"
description: "`aws_workspaces_workspace_snapshots` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_workspace_snapshots` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspace_snapshots"})

    		ubx.Data(workspaces.WorkspaceSnapshots, "example", workspaces.WorkspaceSnapshotsConfig{
    			WorkspaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspaceSnapshots } from "@ubx/sdk-aws/aws/data/workspaces/workspace_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspace_snapshots" });

      data(WorkspaceSnapshots, "example", {
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspaceSnapshots, WorkspaceSnapshotsConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspace_snapshots")

        ubx.data(WorkspaceSnapshots, "example", WorkspaceSnapshotsConfig(
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rebuild_snapshots" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="restore_snapshots" type="list(object)">
  Returned by the lookup.
</ResponseField>
