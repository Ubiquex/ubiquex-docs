---
title: "aws_workspaces_workspace_associations"
description: "`aws_workspaces_workspace_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_workspace_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_workspace_associations"})

    		ubx.Data(workspaces.WorkspaceAssociations, "example", workspaces.WorkspaceAssociationsConfig{
    			AssociatedResourceTypes: []string{"example"},
    			WorkspaceId:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspaceAssociations } from "@ubx/sdk-aws/aws/data/workspaces/workspace_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_workspace_associations" });

      data(WorkspaceAssociations, "example", {
        associatedResourceTypes: ["example"],
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import WorkspaceAssociations, WorkspaceAssociationsConfig

    def describe():
        ubx.intent("look up aws_workspaces_workspace_associations")

        ubx.data(WorkspaceAssociations, "example", WorkspaceAssociationsConfig(
            associated_resource_types=["example"],
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="associated_resource_types" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
