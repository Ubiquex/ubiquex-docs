---
title: "aws_workspaces_account_links"
description: "`aws_workspaces_account_links` reads the real links between a WorkSpaces account and other AWS accounts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_account_links"})

    		ubx.Data(workspaces.AccountLinks, "example", workspaces.AccountLinksConfig{
    			LinkStatusFilter: []string{"example"},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLinks } from "@ubx/sdk-aws/aws/data/workspaces/account_links";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_account_links" });

      data(AccountLinks, "example", {
        linkStatusFilter: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces import AccountLinks, AccountLinksConfig

    def describe():
        ubx.intent("look up aws_workspaces_account_links")

        ubx.data(AccountLinks, "example", AccountLinksConfig(
            link_status_filter=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="link_status_filter" type="list(string)">
  Restricts results to this link status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_links" type="list(object)">
  The real account links included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
