---
title: "aws_deadline_queue_fleet_associations"
description: "`data_aws_deadline_queue_fleet_associations` looks up a real, already-existing `aws_deadline_queue_fleet_association`."
---

It is the real, actual link between an `aws_deadline_queue` and an `aws_deadline_fleet`. A queue with no fleet association has nowhere to actually schedule its jobs to run.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deadline "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/deadline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_deadline_queue_fleet_associations"})

    		ubx.Data(deadline.QueueFleetAssociations, "example", deadline.QueueFleetAssociationsConfig{
    			FarmId:     "example",
    			FleetId:    "example",
    			MaxResults: 1,
    			QueueId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueueFleetAssociations } from "@ubx/sdk-aws/aws/data/deadline/queue_fleet_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_deadline_queue_fleet_associations" });

      data(QueueFleetAssociations, "example", {
        farmId: "example",
        fleetId: "example",
        maxResults: 1,
        queueId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.deadline import QueueFleetAssociations, QueueFleetAssociationsConfig

    def describe():
        ubx.intent("look up aws_deadline_queue_fleet_associations")

        ubx.data(QueueFleetAssociations, "example", QueueFleetAssociationsConfig(
            farm_id="example",
            fleet_id="example",
            max_results=1,
            queue_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="farm_id" type="string" required>
  The real, unique identifier of the farm this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fleet_id" type="string">
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="queue_id" type="string">
  The real, unique identifier of the queue this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queue_fleet_associations" type="list(object)">
  The real Device Farm queue-to-fleet associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
