---
title: "aws_deadline_sessions_statistics_aggregation"
description: "`aws_deadline_sessions_statistics_aggregation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_deadline_sessions_statistics_aggregation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deadline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/deadline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_deadline_sessions_statistics_aggregation"})

    		ubx.Data(deadline.SessionsStatisticsAggregation, "example", deadline.SessionsStatisticsAggregationConfig{
    			AggregationId: "example",
    			FarmId:        "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SessionsStatisticsAggregation } from "@ubx/sdk-aws/aws/data/deadline/sessions_statistics_aggregation";

    export default stack("example", () => {
      intent({ summary: "look up aws_deadline_sessions_statistics_aggregation" });

      data(SessionsStatisticsAggregation, "example", {
        aggregationId: "example",
        farmId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.deadline import SessionsStatisticsAggregation, SessionsStatisticsAggregationConfig

    def describe():
        ubx.intent("look up aws_deadline_sessions_statistics_aggregation")

        ubx.data(SessionsStatisticsAggregation, "example", SessionsStatisticsAggregationConfig(
            aggregation_id="example",
            farm_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="farm_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="statistics" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>
