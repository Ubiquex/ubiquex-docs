---
title: "aws_deadline_queue_limit_associations"
description: "`aws_deadline_queue_limit_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_deadline_queue_limit_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deadline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/deadline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_deadline_queue_limit_associations"})

    		ubx.Data(deadline.QueueLimitAssociations, "example", deadline.QueueLimitAssociationsConfig{
    			FarmId:     "example",
    			LimitId:    "example",
    			MaxResults: 1,
    			QueueId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueueLimitAssociations } from "@ubx/sdk-aws/aws/data/deadline/queue_limit_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_deadline_queue_limit_associations" });

      data(QueueLimitAssociations, "example", {
        farmId: "example",
        limitId: "example",
        maxResults: 1,
        queueId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.deadline import QueueLimitAssociations, QueueLimitAssociationsConfig

    def describe():
        ubx.intent("look up aws_deadline_queue_limit_associations")

        ubx.data(QueueLimitAssociations, "example", QueueLimitAssociationsConfig(
            farm_id="example",
            limit_id="example",
            max_results=1,
            queue_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="farm_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="queue_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="queue_limit_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
