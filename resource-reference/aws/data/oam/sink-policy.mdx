---
title: "aws_oam_sink_policy"
description: "`aws_oam_sink_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_oam_sink_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/oam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_oam_sink_policy"})

    		ubx.Data(oam.SinkPolicy, "example", oam.SinkPolicyConfig{
    			SinkIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SinkPolicy } from "@ubx/sdk-aws/aws/data/oam/sink_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_oam_sink_policy" });

      data(SinkPolicy, "example", {
        sinkIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.oam import SinkPolicy, SinkPolicyConfig

    def describe():
        ubx.intent("look up aws_oam_sink_policy")

        ubx.data(SinkPolicy, "example", SinkPolicyConfig(
            sink_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sink_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sink_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sink_id" type="string">
  Returned by the lookup.
</ResponseField>
