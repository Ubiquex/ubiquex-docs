---
title: "aws_oam_sink_policy"
description: "`data_aws_oam_sink_policy` looks up a real, already-existing `aws_oam_sink`."
---

It creates a real attachment point in a monitoring account under CloudWatch Observability Access Manager, the resource that an `aws_oam_link` from a source account actually connects to; a sink is managed by the monitoring account, one per Region, and must exist along with its own sink policy before any source account can link to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oam "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/oam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_oam_sink_policy"})

    		ubx.Data(oam.SinkPolicy, "example", oam.SinkPolicyConfig{
    			SinkIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SinkPolicy } from "@ubx/sdk-aws/aws/data/oam/sink_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_oam_sink_policy" });

      data(SinkPolicy, "example", {
        sinkIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.oam import SinkPolicy, SinkPolicyConfig

    def describe():
        ubx.intent("look up aws_oam_sink_policy")

        ubx.data(SinkPolicy, "example", SinkPolicyConfig(
            sink_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sink_identifier" type="string" required>
  The real, unique identifier of the sink this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sink_arn" type="string">
  The real Amazon Resource Name (ARN) of the sink this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sink_id" type="string">
  The real, unique identifier of the sink this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
