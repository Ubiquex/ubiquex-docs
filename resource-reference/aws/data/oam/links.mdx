---
title: "aws_oam_links"
description: "`data_aws_oam_links` looks up a real, already-existing `aws_oam_link`."
---

It creates a real cross-account link from a source account into a monitoring account under CloudWatch Observability Access Manager, sharing a filtered set of that source account's logs, metrics, and traces so they can be viewed centrally without switching accounts.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	oam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/oam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_oam_links"})

    		ubx.Data(oam.Links, "example", oam.LinksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Links } from "./local-sdk/aws/sdk/typescript/aws/data/oam/links.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_oam_links" });

      data(Links, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.oam import Links, LinksConfig

    def describe():
        ubx.intent("look up aws_oam_links")

        ubx.data(Links, "example", LinksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
