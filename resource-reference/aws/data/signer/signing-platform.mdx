---
title: "aws_signer_signing_platform"
description: "`aws_signer_signing_platform` looks up one real, supported AWS Signer signing platform (a target environment such as AWS Lambda or AWS IoT)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signer_signing_platform"})

    		ubx.Data(signer.SigningPlatform, "example", signer.SigningPlatformConfig{
    			PlatformId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SigningPlatform } from "@ubx/sdk-aws/aws/data/signer/signing_platform";

    export default stack("example", () => {
      intent({ summary: "look up aws_signer_signing_platform" });

      data(SigningPlatform, "example", {
        platformId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.signer import SigningPlatform, SigningPlatformConfig

    def describe():
        ubx.intent("look up aws_signer_signing_platform")

        ubx.data(SigningPlatform, "example", SigningPlatformConfig(
            platform_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="platform_id" type="string" required>
  The real, unique identifier of the platform this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="category" type="string">
  The real category this object belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="display_name" type="string">
  The real, human-readable display name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_size_in_mb" type="number">
  The real, maximum size of this object, in megabytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="partner" type="string">
  The real partner this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revocation_supported" type="bool">
  Whether revocation is supported for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signing_configuration" type="object">
  &lt;p&gt;The configuration of a signing operation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="encryption_algorithm_options" type="object" required>
      &lt;p&gt;The encryption algorithm options that are available to a code-signing job.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="allowed_values" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="default_value" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hash_algorithm_options" type="object" required>
      &lt;p&gt;The hash algorithms that are available to a code-signing job.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="allowed_values" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="default_value" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="signing_image_format" type="object">
  &lt;p&gt;The image format of a AWS Signer platform or profile.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_format" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="supported_formats" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target" type="string">
  The real object this action targets. **(AI-inferred)** Returned by the lookup.
</ResponseField>
