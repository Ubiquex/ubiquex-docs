---
title: "aws_signer_signing_profiles"
description: "`data_aws_signer_signing_profiles` looks up a real, already-existing `aws_signer_signing_profile`."
---

It creates a real, reusable code-signing configuration, its signing platform and certificate validity, that AWS Signer uses to produce a verifiable, tamper-evident signature over deployment artifacts such as Lambda code or IoT firmware.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signer_signing_profiles"})

    		ubx.Data(signer.SigningProfiles, "example", signer.SigningProfilesConfig{
    			IncludeCanceled: true,
    			MaxResults:      1,
    			PlatformId:      "example",
    			Statuses:        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SigningProfiles } from "./local-sdk/aws/sdk/typescript/aws/data/signer/signing_profiles.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_signer_signing_profiles" });

      data(SigningProfiles, "example", {
        includeCanceled: true,
        maxResults: 1,
        platformId: "example",
        statuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.signer import SigningProfiles, SigningProfilesConfig

    def describe():
        ubx.intent("look up aws_signer_signing_profiles")

        ubx.data(SigningProfiles, "example", SigningProfilesConfig(
            include_canceled=True,
            max_results=1,
            platform_id="example",
            statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_canceled" type="bool">
  Whether canceled items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="platform_id" type="string">
  The real, unique identifier of the platform this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statuses" type="list(string)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="profiles" type="list(object)">
  The real profiles included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
