---
title: "aws_signer_signing_platforms"
description: "`aws_signer_signing_platforms` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_signer_signing_platforms` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signer_signing_platforms"})

    		ubx.Data(signer.SigningPlatforms, "example", signer.SigningPlatformsConfig{
    			Category:   "example",
    			MaxResults: 1,
    			Partner:    "example",
    			Target:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SigningPlatforms } from "@ubx/sdk-aws/aws/data/signer/signing_platforms";

    export default stack("example", () => {
      intent({ summary: "look up aws_signer_signing_platforms" });

      data(SigningPlatforms, "example", {
        category: "example",
        maxResults: 1,
        partner: "example",
        target: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.signer import SigningPlatforms, SigningPlatformsConfig

    def describe():
        ubx.intent("look up aws_signer_signing_platforms")

        ubx.data(SigningPlatforms, "example", SigningPlatformsConfig(
            category="example",
            max_results=1,
            partner="example",
            target="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="partner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="platforms" type="list(object)">
  Returned by the lookup.
</ResponseField>
