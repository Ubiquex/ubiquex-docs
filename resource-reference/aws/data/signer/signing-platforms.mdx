---
title: "aws_signer_signing_platforms"
description: "`aws_signer_signing_platforms` reads the real, supported AWS Signer signing platforms."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signer_signing_platforms"})

    		ubx.Data(signer.SigningPlatforms, "example", signer.SigningPlatformsConfig{
    			Category:   "example",
    			MaxResults: 1,
    			Partner:    "example",
    			Target:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SigningPlatforms } from "./local-sdk/aws/sdk/typescript/aws/data/signer/signing_platforms.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_signer_signing_platforms" });

      data(SigningPlatforms, "example", {
        category: "example",
        maxResults: 1,
        partner: "example",
        target: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.signer import SigningPlatforms, SigningPlatformsConfig

    def describe():
        ubx.intent("look up aws_signer_signing_platforms")

        ubx.data(SigningPlatforms, "example", SigningPlatformsConfig(
            category="example",
            max_results=1,
            partner="example",
            target="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category" type="string">
  The real category this object belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="partner" type="string">
  The real partner this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target" type="string">
  The real object this action targets. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="platforms" type="list(object)">
  The real platforms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
