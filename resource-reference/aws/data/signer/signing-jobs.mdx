---
title: "aws_signer_signing_jobs"
description: "`aws_signer_signing_jobs` reads the real signing jobs run against an AWS Signer signing profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signer_signing_jobs"})

    		ubx.Data(signer.SigningJobs, "example", signer.SigningJobsConfig{
    			IsRevoked:              true,
    			JobInvoker:             "example",
    			MaxResults:             1,
    			PlatformId:             "example",
    			RequestedBy:            "example",
    			SignatureExpiresAfter:  "example",
    			SignatureExpiresBefore: "example",
    			Status:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SigningJobs } from "@ubx/sdk-aws/aws/data/signer/signing_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_signer_signing_jobs" });

      data(SigningJobs, "example", {
        isRevoked: true,
        jobInvoker: "example",
        maxResults: 1,
        platformId: "example",
        requestedBy: "example",
        signatureExpiresAfter: "example",
        signatureExpiresBefore: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.signer import SigningJobs, SigningJobsConfig

    def describe():
        ubx.intent("look up aws_signer_signing_jobs")

        ubx.data(SigningJobs, "example", SigningJobsConfig(
            is_revoked=True,
            job_invoker="example",
            max_results=1,
            platform_id="example",
            requested_by="example",
            signature_expires_after="example",
            signature_expires_before="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="is_revoked" type="bool">
  Whether this object has been revoked. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_invoker" type="string">
  The real identity that invoked the job. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="platform_id" type="string">
  The real, unique identifier of the platform this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="requested_by" type="string">
  The real identity that made this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="signature_expires_after" type="string">
  The real timestamp after which this signature is no longer valid. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="signature_expires_before" type="string">
  The latest real signature-expiry timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
