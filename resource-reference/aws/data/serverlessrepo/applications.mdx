---
title: "aws_serverlessrepo_applications"
description: "`data_aws_serverlessrepo_applications` looks up a real, already-existing `aws_serverless_repo_application`."
---

It publishes a real SAM application to the AWS Serverless Application Repository, packaging its template and code so other accounts can discover, review, and deploy it directly from the repository rather than copying source by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	serverlessrepo "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/serverlessrepo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_serverlessrepo_applications"})

    		ubx.Data(serverlessrepo.Applications, "example", serverlessrepo.ApplicationsConfig{
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Applications } from "@ubx/sdk-aws/aws/data/serverlessrepo/applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_serverlessrepo_applications" });

      data(Applications, "example", {
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.serverlessrepo import Applications, ApplicationsConfig

    def describe():
        ubx.intent("look up aws_serverlessrepo_applications")

        ubx.data(Applications, "example", ApplicationsConfig(
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
