---
title: "aws_serverlessrepo_application_policy"
description: "`aws_serverlessrepo_application_policy` reads the real resource-based policy attached to a Serverless Application Repository application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	serverlessrepo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/serverlessrepo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_serverlessrepo_application_policy"})

    		ubx.Data(serverlessrepo.ApplicationPolicy, "example", serverlessrepo.ApplicationPolicyConfig{
    			ApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationPolicy } from "@ubx/sdk-aws/aws/data/serverlessrepo/application_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_serverlessrepo_application_policy" });

      data(ApplicationPolicy, "example", {
        applicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.serverlessrepo import ApplicationPolicy, ApplicationPolicyConfig

    def describe():
        ubx.intent("look up aws_serverlessrepo_application_policy")

        ubx.data(ApplicationPolicy, "example", ApplicationPolicyConfig(
            application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="statements" type="list(object)">
  The real statements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
