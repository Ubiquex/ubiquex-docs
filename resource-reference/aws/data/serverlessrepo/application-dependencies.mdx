---
title: "aws_serverlessrepo_application_dependencies"
description: "`aws_serverlessrepo_application_dependencies` reads the real nested application dependencies of a Serverless Application Repository application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	serverlessrepo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/serverlessrepo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_serverlessrepo_application_dependencies"})

    		ubx.Data(serverlessrepo.ApplicationDependencies, "example", serverlessrepo.ApplicationDependenciesConfig{
    			ApplicationId:   "example",
    			MaxItems:        1,
    			SemanticVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationDependencies } from "@ubx/sdk-aws/aws/data/serverlessrepo/application_dependencies";

    export default stack("example", () => {
      intent({ summary: "look up aws_serverlessrepo_application_dependencies" });

      data(ApplicationDependencies, "example", {
        applicationId: "example",
        maxItems: 1,
        semanticVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.serverlessrepo import ApplicationDependencies, ApplicationDependenciesConfig

    def describe():
        ubx.intent("look up aws_serverlessrepo_application_dependencies")

        ubx.data(ApplicationDependencies, "example", ApplicationDependenciesConfig(
            application_id="example",
            max_items=1,
            semantic_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="semantic_version" type="string">
  The real version of the semantic. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dependencies" type="list(object)">
  The real dependencies of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
