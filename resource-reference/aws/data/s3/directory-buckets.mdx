---
title: "aws_s3_directory_buckets"
description: "`data_aws_s3_directory_buckets` looks up a real, already-existing `aws_s3_bucket`."
---

It creates the bucket itself. Versioning, encryption, public-access blocking, and lifecycle rules are each their own separate resource, attached to this bucket by reference, real Terraform provider behavior since v4.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_directory_buckets"})

    		ubx.Data(s3.DirectoryBuckets, "example", s3.DirectoryBucketsConfig{
    			MaxDirectoryBuckets: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DirectoryBuckets } from "./local-sdk/aws/sdk/typescript/aws/data/s3/directory_buckets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_directory_buckets" });

      data(DirectoryBuckets, "example", {
        maxDirectoryBuckets: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import DirectoryBuckets, DirectoryBucketsConfig

    def describe():
        ubx.intent("look up aws_s3_directory_buckets")

        ubx.data(DirectoryBuckets, "example", DirectoryBucketsConfig(
            max_directory_buckets=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_directory_buckets" type="number">
  The real, maximum number of S3 directory buckets this account may create. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="buckets" type="list(object)">
  The real S3 buckets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
