---
title: "aws_s3_object_annotations"
description: "`aws_s3_object_annotations` reads the real, structured annotations attached to an S3 object."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_object_annotations"})

    		ubx.Data(s3.ObjectAnnotations, "example", s3.ObjectAnnotationsConfig{
    			Bucket:              "example",
    			Key:                 "example",
    			ExpectedBucketOwner: "example",
    			RequestPayer:        "example",
    			VersionId:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectAnnotations } from "@ubx/sdk-aws/aws/data/s3/object_annotations";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_object_annotations" });

      data(ObjectAnnotations, "example", {
        bucket: "example",
        key: "example",
        expectedBucketOwner: "example",
        requestPayer: "example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import ObjectAnnotations, ObjectAnnotationsConfig

    def describe():
        ubx.intent("look up aws_s3_object_annotations")

        ubx.data(ObjectAnnotations, "example", ObjectAnnotationsConfig(
            bucket="example",
            key="example",
            expected_bucket_owner="example",
            request_payer="example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string" required>
  The real, unique key of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="request_payer" type="string">
  &lt;p&gt;Confirms that the requester knows that they will be charged for the request. Bucket owners need not specify this parameter in their requests. If either the source or destination S3 bucket has Requester Pays enabled, the requester will pay for the corresponding charges. For information about downloading objects from Requester Pays buckets, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/ObjectsinRequesterPaysBuckets.html"&gt;Downloading Objects in Requester Pays Buckets&lt;/a&gt; in the &lt;i&gt;Amazon S3 User Guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="version_id" type="string">
  The real, unique version identifier of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="annotation_count" type="number">
  The real count of annotations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="annotation_prefix" type="string">
  The real S3 key prefix used for the annotation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="annotations" type="list(object)">
  The real annotations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_annotation_results" type="number">
  The real, maximum number of annotation results to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="object_version_id" type="string">
  The real, unique identifier of the object version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_charged" type="string">
  &lt;p&gt;If present, indicates that the requester was successfully charged for the request. For more information, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/RequesterPaysBuckets.html"&gt;Using Requester Pays buckets for storage transfers and usage&lt;/a&gt; in the &lt;i&gt;Amazon Simple Storage Service user guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Returned by the lookup.
</ResponseField>
