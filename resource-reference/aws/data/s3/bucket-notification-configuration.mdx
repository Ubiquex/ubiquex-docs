---
title: "aws_s3_bucket_notification_configuration"
description: "`aws_s3_bucket_notification_configuration` reads the real event notification configuration (SNS, SQS, Lambda) of an S3 bucket."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_notification_configuration"})

    		ubx.Data(s3.BucketNotificationConfiguration, "example", s3.BucketNotificationConfigurationConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketNotificationConfiguration } from "@ubx/sdk-aws/aws/data/s3/bucket_notification_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_notification_configuration" });

      data(BucketNotificationConfiguration, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketNotificationConfiguration, BucketNotificationConfigurationConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_notification_configuration")

        ubx.data(BucketNotificationConfiguration, "example", BucketNotificationConfigurationConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="event_bridge_configuration" type="any">
  &lt;p&gt;A container for specifying the configuration for Amazon EventBridge.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="lambda_function_configurations" type="list(object)">
  The real Lambda function configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queue_configurations" type="list(object)">
  The real queue configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="topic_configurations" type="list(object)">
  The real, configured topics for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
