---
title: "aws_s3_bucket_logging"
description: "`aws_s3_bucket_logging` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_bucket_logging` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_logging"})

    		ubx.Data(s3.BucketLogging, "example", s3.BucketLoggingConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketLogging } from "@ubx/sdk-aws/aws/data/s3/bucket_logging";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_logging" });

      data(BucketLogging, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketLogging, BucketLoggingConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_logging")

        ubx.data(BucketLogging, "example", BucketLoggingConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_enabled" type="object">
  &lt;p&gt;Describes where logs are stored and the prefix that Amazon S3 assigns to all log object keys for a bucket. For more information, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/API/RESTBucketPUTlogging.html"&gt;PUT Bucket logging&lt;/a&gt; in the &lt;i&gt;Amazon S3 API Reference&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="target_bucket" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="target_grants" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_object_key_format" type="object">
      &lt;p&gt;Amazon S3 key format for log objects. Only one format, PartitionedPrefix or SimplePrefix, is allowed.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="partitioned_prefix" type="object">
          &lt;p&gt;Amazon S3 keys for log objects are partitioned in the following format:&lt;/p&gt; &lt;p&gt; &lt;code&gt;[DestinationPrefix][SourceAccountId]/[SourceRegion]/[SourceBucket]/[YYYY]/[MM]/[DD]/[YYYY]-[MM]-[DD]-[hh]-[mm]-[ss]-[UniqueString]&lt;/code&gt; &lt;/p&gt; &lt;p&gt;PartitionedPrefix defaults to EventTime delivery when server access logs are delivered.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="partition_date_source" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="simple_prefix" type="any">
          &lt;p&gt;To use simple format for S3 keys for log objects, set SimplePrefix to an empty object.&lt;/p&gt; &lt;p&gt; &lt;code&gt;[DestinationPrefix][YYYY]-[MM]-[DD]-[hh]-[mm]-[ss]-[UniqueString]&lt;/code&gt; &lt;/p&gt; Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_prefix" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
