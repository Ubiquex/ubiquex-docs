---
title: "aws_s3_object"
description: "`aws_s3_object` looks up one real object stored in an S3 bucket."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_object"})

    		ubx.Data(s3.Object, "example", s3.ObjectConfig{
    			Bucket:              "example",
    			Key:                 "example",
    			ChecksumMode:        "example",
    			ExpectedBucketOwner: "example",
    			IfMatch:             "example",
    			IfModifiedSince:     "example",
    			IfNoneMatch:         "example",
    			IfUnmodifiedSince:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Object } from "./local-sdk/aws/sdk/typescript/aws/data/s3/object.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_object" });

      data(Object, "example", {
        bucket: "example",
        key: "example",
        checksumMode: "example",
        expectedBucketOwner: "example",
        ifMatch: "example",
        ifModifiedSince: "example",
        ifNoneMatch: "example",
        ifUnmodifiedSince: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import Object, ObjectConfig

    def describe():
        ubx.intent("look up aws_s3_object")

        ubx.data(Object, "example", ObjectConfig(
            bucket="example",
            key="example",
            checksum_mode="example",
            expected_bucket_owner="example",
            if_match="example",
            if_modified_since="example",
            if_none_match="example",
            if_unmodified_since="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="checksum_mode" type="string">
  The real, requested mode for including this object's own checksum in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="if_match" type="string">
  Restricts this request to only apply if the object's ETag matches this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="if_modified_since" type="string">
  Restricts this request to only apply if the object was modified since this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="if_none_match" type="string">
  Restricts this request to only apply if the object's ETag does not match this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="if_unmodified_since" type="string">
  Restricts this request to only apply if the object was not modified since this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string" required>
  The real, unique key of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="part_number" type="number">
  The real number of the part. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="range" type="string">
  The real byte range requested for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="request_payer" type="string">
  &lt;p&gt;Confirms that the requester knows that they will be charged for the request. Bucket owners need not specify this parameter in their requests. If either the source or destination S3 bucket has Requester Pays enabled, the requester will pay for the corresponding charges. For information about downloading objects from Requester Pays buckets, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/ObjectsinRequesterPaysBuckets.html"&gt;Downloading Objects in Requester Pays Buckets&lt;/a&gt; in the &lt;i&gt;Amazon S3 User Guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="response_cache_control" type="string">
  The real Cache-Control header value to include in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="response_content_disposition" type="string">
  The real Content-Disposition header value to include in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="response_content_encoding" type="string">
  The real Content-Encoding header value to include in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="response_content_language" type="string">
  The real Content-Language header value to include in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="response_content_type" type="string">
  The real, discriminating type of the response content this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="response_expires" type="string">
  The real Expires header value to include in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ssecustomer_key" type="string">
  The real, unique key of the ssecustomer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accept_ranges" type="string">
  Whether this object accepts byte-range requests. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="body" type="string">
  The real, raw body of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bucket_key_enabled" type="bool">
  Whether the bucket key is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="cache_control" type="string">
  The real Cache-Control header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_crc32" type="string">
  The real, base64-encoded CRC32 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_crc32_c" type="string">
  The real, base64-encoded CRC32C checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_crc64_nvme" type="string">
  The real, base64-encoded CRC64NVME checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_md5" type="string">
  The real, base64-encoded MD5 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_sha1" type="string">
  The real, base64-encoded SHA-1 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_sha256" type="string">
  The real, base64-encoded SHA-256 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_sha512" type="string">
  The real, base64-encoded SHA-512 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_type" type="string">
  The real, discriminating type of the checksum this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_xxhash128" type="string">
  The real, base64-encoded XXH128 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_xxhash3" type="string">
  The real, base64-encoded XXH3 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_xxhash64" type="string">
  The real, base64-encoded XXH64 checksum of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_disposition" type="string">
  The real Content-Disposition header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_encoding" type="string">
  The real Content-Encoding header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_language" type="string">
  The real Content-Language header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_length" type="number">
  The real length of the content, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_range" type="string">
  The real Content-Range header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="delete_marker" type="bool">
  Whether this object version is a delete marker. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expires" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  The real, structured metadata of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="missing_meta" type="number">
  The real number of metadata entries missing for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="object_lock_legal_hold_status" type="string">
  The real, current status of the object lock legal hold this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="object_lock_mode" type="string">
  The real S3 Object Lock retention mode applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="object_lock_retain_until_date" type="string">
  The real date of the object lock retain until. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parts_count" type="number">
  The real count of partss. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replication_status" type="string">
  The real, current status of the replication this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_charged" type="string">
  &lt;p&gt;If present, indicates that the requester was successfully charged for the request. For more information, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/RequesterPaysBuckets.html"&gt;Using Requester Pays buckets for storage transfers and usage&lt;/a&gt; in the &lt;i&gt;Amazon Simple Storage Service user guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="restore" type="string">
  The real S3 Glacier restore status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="server_side_encryption" type="string">
  The real server-side encryption algorithm used for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ssecustomer_algorithm" type="string">
  The real server-side encryption algorithm used with the customer-provided key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ssecustomer_key_md5" type="string">
  The real, base64-encoded MD5 digest of the customer-provided server-side encryption key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ssekmskey_id" type="string">
  The real, unique identifier of the ssekmskey this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="storage_class" type="string">
  The real storage class this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_count" type="number">
  The real count of tags. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_id" type="string">
  The real, unique version identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="website_redirect_location" type="string">
  The real URL this S3 object redirects to, when configured as a website endpoint. **(AI-inferred)** Returned by the lookup.
</ResponseField>
