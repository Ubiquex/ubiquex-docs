---
title: "aws_s3_bucket_encryption"
description: "`data_aws_s3_bucket_encryption` looks up a real, already-existing `aws_s3_bucket`."
---

It creates the bucket itself. Versioning, encryption, public-access blocking, and lifecycle rules are each their own separate resource, attached to this bucket by reference, real Terraform provider behavior since v4.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_encryption"})

    		ubx.Data(s3.BucketEncryption, "example", s3.BucketEncryptionConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketEncryption } from "@ubx/sdk-aws/aws/data/s3/bucket_encryption";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_encryption" });

      data(BucketEncryption, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketEncryption, BucketEncryptionConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_encryption")

        ubx.data(BucketEncryption, "example", BucketEncryptionConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="server_side_encryption_configuration" type="object">
  &lt;p&gt;Specifies the default server-side-encryption configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
