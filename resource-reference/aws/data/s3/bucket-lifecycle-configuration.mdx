---
title: "aws_s3_bucket_lifecycle_configuration"
description: "`aws_s3_bucket_lifecycle_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_bucket_lifecycle_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_lifecycle_configuration"})

    		ubx.Data(s3.BucketLifecycleConfiguration, "example", s3.BucketLifecycleConfigurationConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketLifecycleConfiguration } from "@ubx/sdk-aws/aws/data/s3/bucket_lifecycle_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_lifecycle_configuration" });

      data(BucketLifecycleConfiguration, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketLifecycleConfiguration, BucketLifecycleConfigurationConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_lifecycle_configuration")

        ubx.data(BucketLifecycleConfiguration, "example", BucketLifecycleConfigurationConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rules" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="transition_default_minimum_object_size" type="string">
  Returned by the lookup.
</ResponseField>
