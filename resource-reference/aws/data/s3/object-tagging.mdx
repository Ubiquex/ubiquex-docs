---
title: "aws_s3_object_tagging"
description: "`aws_s3_object_tagging` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_object_tagging` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_object_tagging"})

    		ubx.Data(s3.ObjectTagging, "example", s3.ObjectTaggingConfig{
    			Bucket:              "example",
    			Key:                 "example",
    			ExpectedBucketOwner: "example",
    			RequestPayer:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectTagging } from "@ubx/sdk-aws/aws/data/s3/object_tagging";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_object_tagging" });

      data(ObjectTagging, "example", {
        bucket: "example",
        key: "example",
        expectedBucketOwner: "example",
        requestPayer: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import ObjectTagging, ObjectTaggingConfig

    def describe():
        ubx.intent("look up aws_s3_object_tagging")

        ubx.data(ObjectTagging, "example", ObjectTaggingConfig(
            bucket="example",
            key="example",
            expected_bucket_owner="example",
            request_payer="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="request_payer" type="string">
  &lt;p&gt;Confirms that the requester knows that they will be charged for the request. Bucket owners need not specify this parameter in their requests. If either the source or destination S3 bucket has Requester Pays enabled, the requester will pay for the corresponding charges. For information about downloading objects from Requester Pays buckets, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/ObjectsinRequesterPaysBuckets.html"&gt;Downloading Objects in Requester Pays Buckets&lt;/a&gt; in the &lt;i&gt;Amazon S3 User Guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tag_set" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="version_id" type="string">
  Returned by the lookup.
</ResponseField>
