---
title: "aws_s3_bucket_inventory_configurations"
description: "`aws_s3_bucket_inventory_configurations` reads the real S3 Inventory configurations on a bucket."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_inventory_configurations"})

    		ubx.Data(s3.BucketInventoryConfigurations, "example", s3.BucketInventoryConfigurationsConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketInventoryConfigurations } from "@ubx/sdk-aws/aws/data/s3/bucket_inventory_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_inventory_configurations" });

      data(BucketInventoryConfigurations, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketInventoryConfigurations, BucketInventoryConfigurationsConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_inventory_configurations")

        ubx.data(BucketInventoryConfigurations, "example", BucketInventoryConfigurationsConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="inventory_configuration_list" type="list(object)">
  The real inventory configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_continuation_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
