---
title: "aws_s3_parts"
description: "`aws_s3_parts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_parts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_parts"})

    		ubx.Data(s3.Parts, "example", s3.PartsConfig{
    			Bucket:               "example",
    			Key:                  "example",
    			UploadId:             "example",
    			ExpectedBucketOwner:  "example",
    			RequestPayer:         "example",
    			SsecustomerAlgorithm: "example",
    			SsecustomerKey:       "example",
    			SsecustomerKeyMd5:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Parts } from "@ubx/sdk-aws/aws/data/s3/parts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_parts" });

      data(Parts, "example", {
        bucket: "example",
        key: "example",
        uploadId: "example",
        expectedBucketOwner: "example",
        requestPayer: "example",
        ssecustomerAlgorithm: "example",
        ssecustomerKey: "example",
        ssecustomerKeyMd5: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import Parts, PartsConfig

    def describe():
        ubx.intent("look up aws_s3_parts")

        ubx.data(Parts, "example", PartsConfig(
            bucket="example",
            key="example",
            upload_id="example",
            expected_bucket_owner="example",
            request_payer="example",
            ssecustomer_algorithm="example",
            ssecustomer_key="example",
            ssecustomer_key_md5="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="request_payer" type="string">
  &lt;p&gt;Confirms that the requester knows that they will be charged for the request. Bucket owners need not specify this parameter in their requests. If either the source or destination S3 bucket has Requester Pays enabled, the requester will pay for the corresponding charges. For information about downloading objects from Requester Pays buckets, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/ObjectsinRequesterPaysBuckets.html"&gt;Downloading Objects in Requester Pays Buckets&lt;/a&gt; in the &lt;i&gt;Amazon S3 User Guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="ssecustomer_algorithm" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ssecustomer_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ssecustomer_key_md5" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="upload_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="abort_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="abort_rule_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_algorithm" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="initiator" type="object">
  &lt;p&gt;Container element that identifies who initiated the multipart upload. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_parts" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_part_number_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="owner" type="object">
  &lt;p&gt;Container for the owner's display name and ID.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="display_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="part_number_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parts" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_charged" type="string">
  &lt;p&gt;If present, indicates that the requester was successfully charged for the request. For more information, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/RequesterPaysBuckets.html"&gt;Using Requester Pays buckets for storage transfers and usage&lt;/a&gt; in the &lt;i&gt;Amazon Simple Storage Service user guide&lt;/i&gt;.&lt;/p&gt; &lt;note&gt; &lt;p&gt;This functionality is not supported for directory buckets.&lt;/p&gt; &lt;/note&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="storage_class" type="string">
  Returned by the lookup.
</ResponseField>
