---
title: "aws_s3_public_access_block"
description: "`aws_s3_public_access_block` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_public_access_block` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_public_access_block"})

    		ubx.Data(s3.PublicAccessBlock, "example", s3.PublicAccessBlockConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PublicAccessBlock } from "@ubx/sdk-aws/aws/data/s3/public_access_block";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_public_access_block" });

      data(PublicAccessBlock, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import PublicAccessBlock, PublicAccessBlockConfig

    def describe():
        ubx.intent("look up aws_s3_public_access_block")

        ubx.data(PublicAccessBlock, "example", PublicAccessBlockConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="public_access_block_configuration" type="object">
  &lt;p&gt;The PublicAccessBlock configuration that you want to apply to this Amazon S3 bucket. You can enable the configuration options in any combination. Bucket-level settings work alongside account-level settings (which may inherit from organization-level policies). For more information about when Amazon S3 considers a bucket or object public, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/access-control-block-public-access.html#access-control-block-public-access-policy-status"&gt;The Meaning of "Public"&lt;/a&gt; in the &lt;i&gt;Amazon S3 User Guide&lt;/i&gt;. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="block_public_acls" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="block_public_policy" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ignore_public_acls" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="restrict_public_buckets" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
