---
title: "aws_s3_bucket_policy_status"
description: "`data_aws_s3_bucket_policy_status` looks up a real, already-existing `aws_s3_bucket_policy`."
---

It attaches a real resource-based access policy to an `aws_s3_bucket`, controlling which principals, including from other accounts, can read from or write to it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_policy_status"})

    		ubx.Data(s3.BucketPolicyStatus, "example", s3.BucketPolicyStatusConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BucketPolicyStatus } from "./local-sdk/aws/sdk/typescript/aws/data/s3/bucket_policy_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_policy_status" });

      data(BucketPolicyStatus, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketPolicyStatus, BucketPolicyStatusConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_policy_status")

        ubx.data(BucketPolicyStatus, "example", BucketPolicyStatusConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  The real AWS account ID expected to own the S3 bucket, for cross-account request validation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_status" type="object">
  &lt;p&gt;The container element for a bucket's policy status.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_public" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
