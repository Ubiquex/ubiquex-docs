---
title: "aws_s3_bucket_policy_status"
description: "`aws_s3_bucket_policy_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_bucket_policy_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_bucket_policy_status"})

    		ubx.Data(s3.BucketPolicyStatus, "example", s3.BucketPolicyStatusConfig{
    			Bucket:              "example",
    			ExpectedBucketOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BucketPolicyStatus } from "@ubx/sdk-aws/aws/data/s3/bucket_policy_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_bucket_policy_status" });

      data(BucketPolicyStatus, "example", {
        bucket: "example",
        expectedBucketOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3 import BucketPolicyStatus, BucketPolicyStatusConfig

    def describe():
        ubx.intent("look up aws_s3_bucket_policy_status")

        ubx.data(BucketPolicyStatus, "example", BucketPolicyStatusConfig(
            bucket="example",
            expected_bucket_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_status" type="object">
  &lt;p&gt;The container element for a bucket's policy status.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_public" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
