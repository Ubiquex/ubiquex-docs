---
title: "aws_datapipeline_objects"
description: "`aws_datapipeline_objects` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datapipeline_objects` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datapipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datapipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datapipeline_objects"})

    		ubx.Data(datapipeline.Objects, "example", datapipeline.ObjectsConfig{
    			ObjectIds:           []string{"example"},
    			PipelineId:          "example",
    			EvaluateExpressions: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Objects } from "@ubx/sdk-aws/aws/data/datapipeline/objects";

    export default stack("example", () => {
      intent({ summary: "look up aws_datapipeline_objects" });

      data(Objects, "example", {
        objectIds: ["example"],
        pipelineId: "example",
        evaluateExpressions: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datapipeline import Objects, ObjectsConfig

    def describe():
        ubx.intent("look up aws_datapipeline_objects")

        ubx.data(Objects, "example", ObjectsConfig(
            object_ids=["example"],
            pipeline_id="example",
            evaluate_expressions=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="evaluate_expressions" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="object_ids" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="pipeline_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_more_results" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_objects" type="list(object)">
  Returned by the lookup.
</ResponseField>
