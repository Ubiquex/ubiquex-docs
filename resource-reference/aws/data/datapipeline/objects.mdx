---
title: "aws_datapipeline_objects"
description: "`aws_datapipeline_objects` reads the real pipeline objects (activities, data nodes) defined within a Data Pipeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datapipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/datapipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datapipeline_objects"})

    		ubx.Data(datapipeline.Objects, "example", datapipeline.ObjectsConfig{
    			ObjectIds:           []string{"example"},
    			PipelineId:          "example",
    			EvaluateExpressions: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Objects } from "@ubx/sdk-aws/aws/data/datapipeline/objects";

    export default stack("example", () => {
      intent({ summary: "look up aws_datapipeline_objects" });

      data(Objects, "example", {
        objectIds: ["example"],
        pipelineId: "example",
        evaluateExpressions: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datapipeline import Objects, ObjectsConfig

    def describe():
        ubx.intent("look up aws_datapipeline_objects")

        ubx.data(Objects, "example", ObjectsConfig(
            object_ids=["example"],
            pipeline_id="example",
            evaluate_expressions=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="evaluate_expressions" type="bool">
  Whether expressions are evaluated in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="object_ids" type="list(string)" required>
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="pipeline_id" type="string" required>
  The real, unique identifier of the pipeline this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_more_results" type="bool">
  Whether additional real results are available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_objects" type="list(object)">
  The real Data Pipeline objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
