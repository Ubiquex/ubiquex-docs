---
title: "aws_datapipeline_pipeline_definition"
description: "`aws_datapipeline_pipeline_definition` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_datapipeline_pipeline_definition` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datapipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datapipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datapipeline_pipeline_definition"})

    		ubx.Data(datapipeline.PipelineDefinition, "example", datapipeline.PipelineDefinitionConfig{
    			PipelineId: "example",
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PipelineDefinition } from "@ubx/sdk-aws/aws/data/datapipeline/pipeline_definition";

    export default stack("example", () => {
      intent({ summary: "look up aws_datapipeline_pipeline_definition" });

      data(PipelineDefinition, "example", {
        pipelineId: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datapipeline import PipelineDefinition, PipelineDefinitionConfig

    def describe():
        ubx.intent("look up aws_datapipeline_pipeline_definition")

        ubx.data(PipelineDefinition, "example", PipelineDefinitionConfig(
            pipeline_id="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pipeline_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="parameter_objects" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parameter_values" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_objects" type="list(object)">
  Returned by the lookup.
</ResponseField>
