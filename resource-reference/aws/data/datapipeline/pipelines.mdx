---
title: "aws_datapipeline_pipelines"
description: "`aws_datapipeline_pipelines` looks up a real Data Pipeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datapipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/datapipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_datapipeline_pipelines"})

    		ubx.Data(datapipeline.Pipelines, "example", datapipeline.PipelinesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Pipelines } from "@ubx/sdk-aws/aws/data/datapipeline/pipelines";

    export default stack("example", () => {
      intent({ summary: "look up aws_datapipeline_pipelines" });

      data(Pipelines, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.datapipeline import Pipelines, PipelinesConfig

    def describe():
        ubx.intent("look up aws_datapipeline_pipelines")

        ubx.data(Pipelines, "example", PipelinesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="has_more_results" type="bool">
  Whether additional real results are available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_id_list" type="list(object)">
  The real pipeline ids included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
