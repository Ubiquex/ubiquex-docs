---
title: "aws_ebs_changed_blocks"
description: "`aws_ebs_changed_blocks` reads the real, changed blocks between two EBS snapshots, via the EBS direct APIs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ebs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ebs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ebs_changed_blocks"})

    		ubx.Data(ebs.ChangedBlocks, "example", ebs.ChangedBlocksConfig{
    			SecondSnapshotId:   "example",
    			FirstSnapshotId:    "example",
    			MaxResults:         1,
    			StartingBlockIndex: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChangedBlocks } from "@ubx/sdk-aws/aws/data/ebs/changed_blocks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ebs_changed_blocks" });

      data(ChangedBlocks, "example", {
        secondSnapshotId: "example",
        firstSnapshotId: "example",
        maxResults: 1,
        startingBlockIndex: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ebs import ChangedBlocks, ChangedBlocksConfig

    def describe():
        ubx.intent("look up aws_ebs_changed_blocks")

        ubx.data(ChangedBlocks, "example", ChangedBlocksConfig(
            second_snapshot_id="example",
            first_snapshot_id="example",
            max_results=1,
            starting_block_index=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="first_snapshot_id" type="string">
  The real, unique identifier of the first snapshot this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="second_snapshot_id" type="string" required>
  The real, unique identifier of the second snapshot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="starting_block_index" type="number">
  The real, starting block index this request begins reading from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="block_size" type="number">
  The real size of the block. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="changed_blocks" type="list(object)">
  The real blocks that have changed for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiry_time" type="string">
  The real timestamp of the expiry. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="volume_size" type="number">
  The real size of the volume. **(AI-inferred)** Returned by the lookup.
</ResponseField>
