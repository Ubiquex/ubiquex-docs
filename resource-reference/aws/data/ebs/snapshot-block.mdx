---
title: "aws_ebs_snapshot_block"
description: "`aws_ebs_snapshot_block` reads the real content of one block within an EBS snapshot, via the EBS direct APIs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ebs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ebs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ebs_snapshot_block"})

    		ubx.Data(ebs.SnapshotBlock, "example", ebs.SnapshotBlockConfig{
    			BlockIndex: 1,
    			BlockToken: "example",
    			SnapshotId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SnapshotBlock } from "./local-sdk/aws/sdk/typescript/aws/data/ebs/snapshot_block.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ebs_snapshot_block" });

      data(SnapshotBlock, "example", {
        blockIndex: 1,
        blockToken: "example",
        snapshotId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ebs import SnapshotBlock, SnapshotBlockConfig

    def describe():
        ubx.intent("look up aws_ebs_snapshot_block")

        ubx.data(SnapshotBlock, "example", SnapshotBlockConfig(
            block_index=1,
            block_token="example",
            snapshot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="block_index" type="number" required>
  The real, ordinal index of this block. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="block_token" type="string" required>
  The real, opaque block token. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  The real, unique identifier of the snapshot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="block_data" type="string">
  The real, raw data of the block. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_algorithm" type="string">
  The real algorithm used to compute this object's own checksum. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_length" type="number">
  The real length of the data. **(AI-inferred)** Returned by the lookup.
</ResponseField>
