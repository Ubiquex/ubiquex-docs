---
title: "aws_ebs_snapshot_block"
description: "`aws_ebs_snapshot_block` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ebs_snapshot_block` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ebs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ebs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ebs_snapshot_block"})

    		ubx.Data(ebs.SnapshotBlock, "example", ebs.SnapshotBlockConfig{
    			BlockIndex: 1,
    			BlockToken: "example",
    			SnapshotId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotBlock } from "@ubx/sdk-aws/aws/data/ebs/snapshot_block";

    export default stack("example", () => {
      intent({ summary: "look up aws_ebs_snapshot_block" });

      data(SnapshotBlock, "example", {
        blockIndex: 1,
        blockToken: "example",
        snapshotId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ebs import SnapshotBlock, SnapshotBlockConfig

    def describe():
        ubx.intent("look up aws_ebs_snapshot_block")

        ubx.data(SnapshotBlock, "example", SnapshotBlockConfig(
            block_index=1,
            block_token="example",
            snapshot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="block_index" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="block_token" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="block_data" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="checksum" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="checksum_algorithm" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="data_length" type="number">
  Returned by the lookup.
</ResponseField>
