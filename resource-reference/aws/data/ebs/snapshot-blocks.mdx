---
title: "aws_ebs_snapshot_blocks"
description: "`aws_ebs_snapshot_blocks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ebs_snapshot_blocks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ebs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ebs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ebs_snapshot_blocks"})

    		ubx.Data(ebs.SnapshotBlocks, "example", ebs.SnapshotBlocksConfig{
    			SnapshotId:         "example",
    			MaxResults:         1,
    			StartingBlockIndex: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SnapshotBlocks } from "@ubx/sdk-aws/aws/data/ebs/snapshot_blocks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ebs_snapshot_blocks" });

      data(SnapshotBlocks, "example", {
        snapshotId: "example",
        maxResults: 1,
        startingBlockIndex: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ebs import SnapshotBlocks, SnapshotBlocksConfig

    def describe():
        ubx.intent("look up aws_ebs_snapshot_blocks")

        ubx.data(SnapshotBlocks, "example", SnapshotBlocksConfig(
            snapshot_id="example",
            max_results=1,
            starting_block_index=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="snapshot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="starting_block_index" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="block_size" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="blocks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="expiry_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="volume_size" type="number">
  Returned by the lookup.
</ResponseField>
