---
title: "aws_interconnect_attach_points"
description: "`aws_interconnect_attach_points` reads the real, available network interconnect attachment points."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	interconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/interconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_interconnect_attach_points"})

    		ubx.Data(interconnect.AttachPoints, "example", interconnect.AttachPointsConfig{
    			EnvironmentId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachPoints } from "@ubx/sdk-aws/aws/data/interconnect/attach_points";

    export default stack("example", () => {
      intent({ summary: "look up aws_interconnect_attach_points" });

      data(AttachPoints, "example", {
        environmentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.interconnect import AttachPoints, AttachPointsConfig

    def describe():
        ubx.intent("look up aws_interconnect_attach_points")

        ubx.data(AttachPoints, "example", AttachPointsConfig(
            environment_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attach_points" type="list(object)">
  The real attach points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
