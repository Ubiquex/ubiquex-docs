---
title: "aws_signin_console_authorization_configuration"
description: "`aws_signin_console_authorization_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_signin_console_authorization_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	signin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/signin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_signin_console_authorization_configuration"})

    		ubx.Data(signin.ConsoleAuthorizationConfiguration, "example", signin.ConsoleAuthorizationConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConsoleAuthorizationConfiguration } from "@ubx/sdk-aws/aws/data/signin/console_authorization_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_signin_console_authorization_configuration" });

      data(ConsoleAuthorizationConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.signin import ConsoleAuthorizationConfiguration, ConsoleAuthorizationConfigurationConfig

    def describe():
        ubx.intent("look up aws_signin_console_authorization_configuration")

        ubx.data(ConsoleAuthorizationConfiguration, "example", ConsoleAuthorizationConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="console_authorization_enabled" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scope" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="target_id" type="string">
  Target account identifier for console authorization Returned by the lookup.
</ResponseField>
