---
title: "aws_tax_tax_registrations"
description: "`aws_tax_tax_registrations` reads the real tax registrations recorded across accounts in an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tax "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tax_tax_registrations"})

    		ubx.Data(tax.TaxRegistrations, "example", tax.TaxRegistrationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaxRegistrations } from "@ubx/sdk-aws/aws/data/tax/tax_registrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_tax_tax_registrations" });

      data(TaxRegistrations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tax import TaxRegistrations, TaxRegistrationsConfig

    def describe():
        ubx.intent("look up aws_tax_tax_registrations")

        ubx.data(TaxRegistrations, "example", TaxRegistrationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_details" type="list(object)">
  The real, detailed information about the account. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
