---
title: "aws_tax_tax_registration_document"
description: "`aws_tax_tax_registration_document` reads a real, uploaded tax registration document for an account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tax "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tax_tax_registration_document"})

    		ubx.Data(tax.TaxRegistrationDocument, "example", tax.TaxRegistrationDocumentConfig{
    			TaxDocumentMetadata:   map[string]any{"tax_document_access_token": "example", "tax_document_name": "example-tax-document-name"},
    			DestinationS3Location: map[string]any{"bucket": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaxRegistrationDocument } from "@ubx/sdk-aws/aws/data/tax/tax_registration_document";

    export default stack("example", () => {
      intent({ summary: "look up aws_tax_tax_registration_document" });

      data(TaxRegistrationDocument, "example", {
        taxDocumentMetadata: {
          taxDocumentAccessToken: "example",
          taxDocumentName: "example-tax-document-name",
        },
        destinationS3Location: { bucket: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tax import TaxRegistrationDocument, TaxRegistrationDocumentConfig
    from ubx.aws.data.tax.tax_registration_document import TaxRegistrationDocument_TaxDocumentMetadata, TaxRegistrationDocument_DestinationS3Location

    def describe():
        ubx.intent("look up aws_tax_tax_registration_document")

        ubx.data(TaxRegistrationDocument, "example", TaxRegistrationDocumentConfig(
            tax_document_metadata=TaxRegistrationDocument_TaxDocumentMetadata(tax_document_access_token="example", tax_document_name="example-tax-document-name"),
            destination_s3_location=TaxRegistrationDocument_DestinationS3Location(bucket="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="destination_s3_location" type="object">
  &lt;p&gt;The location of the Amazon S3 bucket that you specify to download your tax documents to.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tax_document_metadata" type="object" required>
  &lt;p&gt;The metadata for your tax document.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="tax_document_access_token" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="tax_document_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="destination_file_path" type="string">
  The real path of the destination file. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="presigned_s3_url" type="string">
  The real, resolvable URL of the presigned s3 this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
