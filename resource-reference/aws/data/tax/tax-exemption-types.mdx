---
title: "aws_tax_tax_exemption_types"
description: "`aws_tax_tax_exemption_types` reads the real, available tax exemption types for an account's own jurisdiction."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tax "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tax_tax_exemption_types"})

    		ubx.Data(tax.TaxExemptionTypes, "example", tax.TaxExemptionTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaxExemptionTypes } from "@ubx/sdk-aws/aws/data/tax/tax_exemption_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_tax_tax_exemption_types" });

      data(TaxExemptionTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tax import TaxExemptionTypes, TaxExemptionTypesConfig

    def describe():
        ubx.intent("look up aws_tax_tax_exemption_types")

        ubx.data(TaxExemptionTypes, "example", TaxExemptionTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="tax_exemption_types" type="list(object)">
  The real, discriminating types of tax exemptions this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
