---
title: "aws_tax_tax_inheritance"
description: "`aws_tax_tax_inheritance` reads the real tax settings inheritance configuration across an AWS Organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tax "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tax"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tax_tax_inheritance"})

    		ubx.Data(tax.TaxInheritance, "example", tax.TaxInheritanceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TaxInheritance } from "@ubx/sdk-aws/aws/data/tax/tax_inheritance";

    export default stack("example", () => {
      intent({ summary: "look up aws_tax_tax_inheritance" });

      data(TaxInheritance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tax import TaxInheritance, TaxInheritanceConfig

    def describe():
        ubx.intent("look up aws_tax_tax_inheritance")

        ubx.data(TaxInheritance, "example", TaxInheritanceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="heritage_status" type="string">
  The real, current status of the heritage this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
