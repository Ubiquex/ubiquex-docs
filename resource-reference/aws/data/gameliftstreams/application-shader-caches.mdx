---
title: "aws_gameliftstreams_application_shader_caches"
description: "`aws_gameliftstreams_application_shader_caches` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gameliftstreams_application_shader_caches` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gameliftstreams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gameliftstreams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gameliftstreams_application_shader_caches"})

    		ubx.Data(gameliftstreams.ApplicationShaderCaches, "example", gameliftstreams.ApplicationShaderCachesConfig{
    			Identifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationShaderCaches } from "@ubx/sdk-aws/aws/data/gameliftstreams/application_shader_caches";

    export default stack("example", () => {
      intent({ summary: "look up aws_gameliftstreams_application_shader_caches" });

      data(ApplicationShaderCaches, "example", {
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gameliftstreams import ApplicationShaderCaches, ApplicationShaderCachesConfig

    def describe():
        ubx.intent("look up aws_gameliftstreams_application_shader_caches")

        ubx.data(ApplicationShaderCaches, "example", ApplicationShaderCachesConfig(
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>
