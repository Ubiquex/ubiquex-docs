---
title: "aws_gameliftstreams_stream_sessions"
description: "`aws_gameliftstreams_stream_sessions` reads the real GameLift Streams streaming sessions on a stream group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gameliftstreams "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gameliftstreams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gameliftstreams_stream_sessions"})

    		ubx.Data(gameliftstreams.StreamSessions, "example", gameliftstreams.StreamSessionsConfig{
    			Identifier:        "example",
    			ExportFilesStatus: "example",
    			MaxResults:        1,
    			Status:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamSessions } from "@ubx/sdk-aws/aws/data/gameliftstreams/stream_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_gameliftstreams_stream_sessions" });

      data(StreamSessions, "example", {
        identifier: "example",
        exportFilesStatus: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gameliftstreams import StreamSessions, StreamSessionsConfig

    def describe():
        ubx.intent("look up aws_gameliftstreams_stream_sessions")

        ubx.data(StreamSessions, "example", StreamSessionsConfig(
            identifier="example",
            export_files_status="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_files_status" type="string">
  The real, current status of the export files this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
