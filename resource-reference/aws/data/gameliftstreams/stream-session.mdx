---
title: "aws_gameliftstreams_stream_session"
description: "`aws_gameliftstreams_stream_session` looks up one real GameLift Streams streaming session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	gameliftstreams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gameliftstreams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gameliftstreams_stream_session"})

    		ubx.Data(gameliftstreams.StreamSession, "example", gameliftstreams.StreamSessionConfig{
    			Identifier:              "example",
    			StreamSessionIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { StreamSession } from "./local-sdk/aws/sdk/typescript/aws/data/gameliftstreams/stream_session.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_gameliftstreams_stream_session" });

      data(StreamSession, "example", {
        identifier: "example",
        streamSessionIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.gameliftstreams import StreamSession, StreamSessionConfig

    def describe():
        ubx.intent("look up aws_gameliftstreams_stream_session")

        ubx.data(StreamSession, "example", StreamSessionConfig(
            identifier="example",
            stream_session_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stream_session_identifier" type="string" required>
  The real, unique identifier of the stream session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="additional_environment_variables" type="map(string)">
  The real, additional environment variables set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="additional_launch_args" type="list(string)">
  The real, additional launch arguments for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="application_arn" type="string">
  The real Amazon Resource Name (ARN) of the application this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connection_timeout_seconds" type="number">
  The real, maximum time this connection waits before timing out, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="display_configuration" type="object">
  &lt;p&gt;The virtual monitor settings for a stream session, including the resolution. If not specified, the stream session uses the default resolution of 1920 × 1080.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="resolution" type="object">
      &lt;p&gt;Contains the width and height dimensions, in pixels, that define the resolution of the stream session's virtual monitor. The total number of pixels (width × height) must not exceed 2,073,600 (equivalent to 1920 × 1080).&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="height" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="width" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="export_files_metadata" type="object">
  &lt;p&gt;Provides details about the stream session's exported files. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="output_uri" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_reason" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="log_file_location_uri" type="string">
  The real, resolvable URI of the log file location this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="performance_stats_configuration" type="object">
  &lt;p&gt;Configuration settings for sharing the stream session's performance stats with the client&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="shared_with_client" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocol" type="string">
  The real network protocol used by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_length_seconds" type="number">
  The real length of this session, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signal_request" type="string">
  The real, raw signaling request for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signal_response" type="string">
  The real, raw signaling response for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  The real, human-readable reason for the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_group_id" type="string">
  The real, unique identifier of the stream group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="web_sdk_protocol_url" type="string">
  The real, resolvable URL of the web sdk protocol this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
