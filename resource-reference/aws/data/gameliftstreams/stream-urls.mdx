---
title: "aws_gameliftstreams_stream_urls"
description: "`aws_gameliftstreams_stream_urls` reads the real, connection URLs for a GameLift Streams streaming session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gameliftstreams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gameliftstreams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gameliftstreams_stream_urls"})

    		ubx.Data(gameliftstreams.StreamUrls, "example", gameliftstreams.StreamUrlsConfig{
    			MaxResults:            1,
    			Status:                "example",
    			StreamGroupIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamUrls } from "@ubx/sdk-aws/aws/data/gameliftstreams/stream_urls";

    export default stack("example", () => {
      intent({ summary: "look up aws_gameliftstreams_stream_urls" });

      data(StreamUrls, "example", {
        maxResults: 1,
        status: "example",
        streamGroupIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gameliftstreams import StreamUrls, StreamUrlsConfig

    def describe():
        ubx.intent("look up aws_gameliftstreams_stream_urls")

        ubx.data(StreamUrls, "example", StreamUrlsConfig(
            max_results=1,
            status="example",
            stream_group_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_group_identifier" type="string">
  The real, unique identifier of the stream group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
