---
title: "aws_gameliftstreams_stream_sessions_by_account"
description: "`aws_gameliftstreams_stream_sessions_by_account` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gameliftstreams_stream_sessions_by_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gameliftstreams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gameliftstreams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gameliftstreams_stream_sessions_by_account"})

    		ubx.Data(gameliftstreams.StreamSessionsByAccount, "example", gameliftstreams.StreamSessionsByAccountConfig{
    			ExportFilesStatus: "example",
    			MaxResults:        1,
    			Status:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamSessionsByAccount } from "@ubx/sdk-aws/aws/data/gameliftstreams/stream_sessions_by_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_gameliftstreams_stream_sessions_by_account" });

      data(StreamSessionsByAccount, "example", {
        exportFilesStatus: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gameliftstreams import StreamSessionsByAccount, StreamSessionsByAccountConfig

    def describe():
        ubx.intent("look up aws_gameliftstreams_stream_sessions_by_account")

        ubx.data(StreamSessionsByAccount, "example", StreamSessionsByAccountConfig(
            export_files_status="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_files_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
