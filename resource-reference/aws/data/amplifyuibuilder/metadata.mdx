---
title: "aws_amplifyuibuilder_metadata"
description: "`aws_amplifyuibuilder_metadata` reads real metadata about an Amplify UI Builder app."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amplifyuibuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/amplifyuibuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplifyuibuilder_metadata"})

    		ubx.Data(amplifyuibuilder.Metadata, "example", amplifyuibuilder.MetadataConfig{
    			AppId:           "example",
    			EnvironmentName: "example-environment-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Metadata } from "./local-sdk/aws/sdk/typescript/aws/data/amplifyuibuilder/metadata.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplifyuibuilder_metadata" });

      data(Metadata, "example", {
        appId: "example",
        environmentName: "example-environment-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.amplifyuibuilder import Metadata, MetadataConfig

    def describe():
        ubx.intent("look up aws_amplifyuibuilder_metadata")

        ubx.data(Metadata, "example", MetadataConfig(
            app_id="example",
            environment_name="example-environment-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The real, unique identifier of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string" required>
  The real name of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="features" type="map(string)">
  The real features included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
