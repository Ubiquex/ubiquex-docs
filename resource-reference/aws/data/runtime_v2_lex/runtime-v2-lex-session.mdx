---
title: "aws_runtime_v2_lex_session"
description: "`aws_runtime_v2_lex_session` reads the real, current state of a Lex V2 bot conversation session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	runtime_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/runtime_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_runtime_v2_lex_session"})

    		ubx.Data(runtime_v2_lex.RuntimeV2LexSession, "example", runtime_v2_lex.RuntimeV2LexSessionConfig{
    			BotAliasId: "example",
    			BotId:      "example",
    			LocaleId:   "example",
    			SessionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuntimeV2LexSession } from "@ubx/sdk-aws/aws/data/runtime_v2_lex/runtime_v2_lex_session";

    export default stack("example", () => {
      intent({ summary: "look up aws_runtime_v2_lex_session" });

      data(RuntimeV2LexSession, "example", {
        botAliasId: "example",
        botId: "example",
        localeId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.runtime_v2_lex import RuntimeV2LexSession, RuntimeV2LexSessionConfig

    def describe():
        ubx.intent("look up aws_runtime_v2_lex_session")

        ubx.data(RuntimeV2LexSession, "example", RuntimeV2LexSessionConfig(
            bot_alias_id="example",
            bot_id="example",
            locale_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_alias_id" type="string" required>
  The real, unique identifier of the bot alias this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="interpretations" type="list(object)">
  The real Amazon Lex interpretations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="messages" type="list(object)">
  The real messages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_state" type="object">
  &lt;p&gt;The state of the user's session with Amazon Lex V2.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="active_contexts" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="dialog_action" type="object">
      &lt;p&gt;The next action that Amazon Lex V2 should take.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="slot_elicitation_style" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="slot_to_elicit" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="sub_slot_to_elicit" type="object">
          &lt;p&gt;The specific constituent sub slot of the composite slot to elicit in dialog action.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="name" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="sub_slot_to_elicit" type="any">
              &lt;p&gt;The specific constituent sub slot of the composite slot to elicit in dialog action.&lt;/p&gt; Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intent" type="object">
      &lt;p&gt;The current intent that Amazon Lex V2 is attempting to fulfill.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="confirmation_state" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="slots" type="map(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="state" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="originating_request_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="runtime_hints" type="object">
      &lt;p&gt;You can provide Amazon Lex V2 with hints to the phrases that a customer is likely to use for a slot. When a slot with hints is resolved, the phrases in the runtime hints are preferred in the resolution. You can provide hints for a maximum of 100 intents. You can provide a maximum of 100 slots.&lt;/p&gt; &lt;p&gt;Before you can use runtime hints with an existing bot, you must first rebuild the bot.&lt;/p&gt; &lt;p&gt;For more information, see &lt;a href="https://docs.aws.amazon.com/lexv2/latest/dg/using-hints.html"&gt;Using runtime hints to improve recognition of slot values&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="slot_hints" type="map(map(object))">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="session_attributes" type="map(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
