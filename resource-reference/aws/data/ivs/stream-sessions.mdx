---
title: "aws_ivs_stream_sessions"
description: "`aws_ivs_stream_sessions` reads the real streaming sessions on an IVS channel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivs_stream_sessions"})

    		ubx.Data(ivs.StreamSessions, "example", ivs.StreamSessionsConfig{
    			ChannelArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamSessions } from "@ubx/sdk-aws/aws/data/ivs/stream_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivs_stream_sessions" });

      data(StreamSessions, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivs import StreamSessions, StreamSessionsConfig

    def describe():
        ubx.intent("look up aws_ivs_stream_sessions")

        ubx.data(StreamSessions, "example", StreamSessionsConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_sessions" type="list(object)">
  The real stream sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
