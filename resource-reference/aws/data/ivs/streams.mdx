---
title: "aws_ivs_streams"
description: "`data_aws_ivs_streams` looks up a real, already-existing `aws_ivs_stream_key`."
---

It is the real, secret credential streaming software uses to authenticate and publish video to a specific `aws_ivs_channel`, distinct from the channel's own public ingest endpoint URL.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivs_streams"})

    		ubx.Data(ivs.Streams, "example", ivs.StreamsConfig{
    			FilterBy:   map[string]any{"health": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Streams } from "@ubx/sdk-aws/aws/data/ivs/streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivs_streams" });

      data(Streams, "example", {
        filterBy: { health: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivs import Streams, StreamsConfig
    from ubx.aws.data.ivs.streams import Streams_FilterBy

    def describe():
        ubx.intent("look up aws_ivs_streams")

        ubx.data(Streams, "example", StreamsConfig(
            filter_by=Streams_FilterBy(health="example"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_by" type="object">
  &lt;p&gt;Object specifying the stream attribute on which to filter.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="health" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="streams" type="list(object)">
  The real streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
