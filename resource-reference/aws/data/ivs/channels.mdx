---
title: "aws_ivs_channels"
description: "`data_aws_ivs_channels` looks up a real, already-existing `aws_ivs_channel`."
---

It is the real, core Amazon IVS resource a broadcaster streams video into and viewers watch from, with its own ingest endpoint, playback URL, and configurable latency mode.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ivs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivs_channels"})

    		ubx.Data(ivs.Channels, "example", ivs.ChannelsConfig{
    			FilterByAdConfigurationArn:           "arn:aws:iam::123456789012:policy/example",
    			FilterByName:                         "example-filter-by-name",
    			FilterByPlaybackRestrictionPolicyArn: "arn:aws:iam::123456789012:policy/example",
    			FilterByRecordingConfigurationArn:    "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Channels } from "./local-sdk/aws/sdk/typescript/aws/data/ivs/channels.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivs_channels" });

      data(Channels, "example", {
        filterByAdConfigurationArn: "arn:aws:iam::123456789012:policy/example",
        filterByName: "example-filter-by-name",
        filterByPlaybackRestrictionPolicyArn:
          "arn:aws:iam::123456789012:policy/example",
        filterByRecordingConfigurationArn:
          "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ivs import Channels, ChannelsConfig

    def describe():
        ubx.intent("look up aws_ivs_channels")

        ubx.data(Channels, "example", ChannelsConfig(
            filter_by_ad_configuration_arn="arn:aws:iam::123456789012:policy/example",
            filter_by_name="example-filter-by-name",
            filter_by_playback_restriction_policy_arn="arn:aws:iam::123456789012:policy/example",
            filter_by_recording_configuration_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_by_ad_configuration_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter by ad configuration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_name" type="string">
  The real name of the filter by this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_playback_restriction_policy_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter by playback restriction policy this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by_recording_configuration_arn" type="string">
  The real Amazon Resource Name (ARN) of the filter by recording configuration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channels" type="list(object)">
  The real channels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
