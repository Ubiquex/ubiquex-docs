---
title: "aws_api_tunneling_iot_tunnels"
description: "`aws_api_tunneling_iot_tunnels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_tunneling_iot_tunnels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_tunneling_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_tunneling_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_tunneling_iot_tunnels"})

    		ubx.Data(api_tunneling_iot.ApiTunnelingIotTunnels, "example", api_tunneling_iot.ApiTunnelingIotTunnelsConfig{
    			MaxResults: 1,
    			ThingName:  "example-thing-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiTunnelingIotTunnels } from "@ubx/sdk-aws/aws/data/api_tunneling_iot/api_tunneling_iot_tunnels";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_tunneling_iot_tunnels" });

      data(ApiTunnelingIotTunnels, "example", {
        maxResults: 1,
        thingName: "example-thing-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_tunneling_iot import ApiTunnelingIotTunnels, ApiTunnelingIotTunnelsConfig

    def describe():
        ubx.intent("look up aws_api_tunneling_iot_tunnels")

        ubx.data(ApiTunnelingIotTunnels, "example", ApiTunnelingIotTunnelsConfig(
            max_results=1,
            thing_name="example-thing-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="thing_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tunnel_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
