---
title: "aws_api_tunneling_iot_tunnel"
description: "`aws_api_tunneling_iot_tunnel` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_tunneling_iot_tunnel` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_tunneling_iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_tunneling_iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_tunneling_iot_tunnel"})

    		ubx.Data(api_tunneling_iot.ApiTunnelingIotTunnel, "example", api_tunneling_iot.ApiTunnelingIotTunnelConfig{
    			TunnelId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiTunnelingIotTunnel } from "@ubx/sdk-aws/aws/data/api_tunneling_iot/api_tunneling_iot_tunnel";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_tunneling_iot_tunnel" });

      data(ApiTunnelingIotTunnel, "example", {
        tunnelId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_tunneling_iot import ApiTunnelingIotTunnel, ApiTunnelingIotTunnelConfig

    def describe():
        ubx.intent("look up aws_api_tunneling_iot_tunnel")

        ubx.data(ApiTunnelingIotTunnel, "example", ApiTunnelingIotTunnelConfig(
            tunnel_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="tunnel_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tunnel" type="object">
  &lt;p&gt;A connection between a source computer and a destination device.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_config" type="object">
      &lt;p&gt;The destination configuration.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="services" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="thing_name" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination_connection_state" type="object">
      &lt;p&gt;The state of a connection.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="last_updated_at" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_updated_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_connection_state" type="object">
      &lt;p&gt;The state of a connection.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="last_updated_at" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timeout_config" type="object">
      &lt;p&gt;Tunnel timeout configuration.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="max_lifetime_timeout_minutes" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tunnel_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tunnel_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
