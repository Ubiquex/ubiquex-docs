---
title: "aws_waf_regional_byte_match_sets"
description: "`data_aws_waf_regional_byte_match_sets` looks up a real, already-existing `aws_waf_byte_match_set`."
---

It defines a real condition, matching a specific byte sequence in a request part, header, URI, body, that an `aws_waf_rule` references. This is part of AWS WAF Classic (global, for CloudFront), which AWS deprecated: support ended September 30, 2025, roughly eleven months in the past. AWS's current, actively developed offering is AWS WAFv2 (`aws_wafv2_web_acl` and related resources).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_byte_match_sets"})

    		ubx.Data(waf_regional.WafRegionalByteMatchSets, "example", waf_regional.WafRegionalByteMatchSetsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalByteMatchSets } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_byte_match_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_byte_match_sets" });

      data(WafRegionalByteMatchSets, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalByteMatchSets, WafRegionalByteMatchSetsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_byte_match_sets")

        ubx.data(WafRegionalByteMatchSets, "example", WafRegionalByteMatchSetsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="byte_match_sets" type="list(object)">
  The real byte match sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
