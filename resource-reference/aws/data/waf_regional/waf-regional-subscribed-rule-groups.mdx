---
title: "aws_waf_regional_subscribed_rule_groups"
description: "`aws_waf_regional_subscribed_rule_groups` reads the real WAF Marketplace rule groups subscribed to for WAF Classic Regional."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_subscribed_rule_groups"})

    		ubx.Data(waf_regional.WafRegionalSubscribedRuleGroups, "example", waf_regional.WafRegionalSubscribedRuleGroupsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WafRegionalSubscribedRuleGroups } from "./local-sdk/aws/sdk/typescript/aws/data/waf_regional/waf_regional_subscribed_rule_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_subscribed_rule_groups" });

      data(WafRegionalSubscribedRuleGroups, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalSubscribedRuleGroups, WafRegionalSubscribedRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_subscribed_rule_groups")

        ubx.data(WafRegionalSubscribedRuleGroups, "example", WafRegionalSubscribedRuleGroupsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_groups" type="list(object)">
  The real rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
