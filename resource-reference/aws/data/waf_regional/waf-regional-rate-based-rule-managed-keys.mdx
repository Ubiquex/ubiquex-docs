---
title: "aws_waf_regional_rate_based_rule_managed_keys"
description: "`aws_waf_regional_rate_based_rule_managed_keys` reads the real keys currently rate-limited by a WAF Classic Regional rate-based rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_rate_based_rule_managed_keys"})

    		ubx.Data(waf_regional.WafRegionalRateBasedRuleManagedKeys, "example", waf_regional.WafRegionalRateBasedRuleManagedKeysConfig{
    			RuleId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WafRegionalRateBasedRuleManagedKeys } from "./local-sdk/aws/sdk/typescript/aws/data/waf_regional/waf_regional_rate_based_rule_managed_keys.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_rate_based_rule_managed_keys" });

      data(WafRegionalRateBasedRuleManagedKeys, "example", {
        ruleId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalRateBasedRuleManagedKeys, WafRegionalRateBasedRuleManagedKeysConfig

    def describe():
        ubx.intent("look up aws_waf_regional_rate_based_rule_managed_keys")

        ubx.data(WafRegionalRateBasedRuleManagedKeys, "example", WafRegionalRateBasedRuleManagedKeysConfig(
            rule_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="rule_id" type="string" required>
  The real, unique identifier of the rule this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_keys" type="list(string)">
  The real, unique keys of the manageds included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
