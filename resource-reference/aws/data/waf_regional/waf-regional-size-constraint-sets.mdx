---
title: "aws_waf_regional_size_constraint_sets"
description: "`data_aws_waf_regional_size_constraint_sets` looks up a real, already-existing `aws_waf_size_constraint_set`."
---

It defines a real condition, matching a request part's size against a threshold, that an `aws_waf_rule` references. Like the rest of AWS WAF Classic, support for this resource ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_size_constraint_sets"})

    		ubx.Data(waf_regional.WafRegionalSizeConstraintSets, "example", waf_regional.WafRegionalSizeConstraintSetsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalSizeConstraintSets } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_size_constraint_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_size_constraint_sets" });

      data(WafRegionalSizeConstraintSets, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalSizeConstraintSets, WafRegionalSizeConstraintSetsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_size_constraint_sets")

        ubx.data(WafRegionalSizeConstraintSets, "example", WafRegionalSizeConstraintSetsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="size_constraint_sets" type="list(object)">
  The real size constraint sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
