---
title: "aws_waf_regional_sampled_requests"
description: "`aws_waf_regional_sampled_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_waf_regional_sampled_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_sampled_requests"})

    		ubx.Data(waf_regional.WafRegionalSampledRequests, "example", waf_regional.WafRegionalSampledRequestsConfig{
    			MaxItems:   1,
    			RuleId:     "example",
    			TimeWindow: map[string]any{"end_time": "example"},
    			WebAclId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalSampledRequests } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_sampled_requests";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_sampled_requests" });

      data(WafRegionalSampledRequests, "example", {
        maxItems: 1,
        ruleId: "example",
        timeWindow: { endTime: "example" },
        webAclId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalSampledRequests, WafRegionalSampledRequestsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_sampled_requests")

        ubx.data(WafRegionalSampledRequests, "example", WafRegionalSampledRequestsConfig(
            max_items=1,
            rule_id="example",
            time_window={"end_time": "example"},
            web_acl_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_items" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rule_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="time_window" type="object" required>
  &lt;note&gt; &lt;p&gt;This is &lt;b&gt;AWS WAF Classic&lt;/b&gt; documentation. For more information, see &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/classic-waf-chapter.html"&gt;AWS WAF Classic&lt;/a&gt; in the developer guide.&lt;/p&gt; &lt;p&gt; &lt;b&gt;For the latest version of AWS WAF&lt;/b&gt;, use the AWS WAFV2 API and see the &lt;a href="https://docs.aws.amazon.com/waf/latest/developerguide/waf-chapter.html"&gt;AWS WAF Developer Guide&lt;/a&gt;. With the latest version, AWS WAF has a single set of endpoints for regional and global use. &lt;/p&gt; &lt;/note&gt; &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; request, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which you want AWS WAF to return a sample of web requests.&lt;/p&gt; &lt;p&gt;You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, &lt;code&gt;Z&lt;/code&gt;. For example, &lt;code&gt;"2016-09-27T14:50Z"&lt;/code&gt;. &lt;/p&gt; &lt;p&gt;In a &lt;a&gt;GetSampledRequests&lt;/a&gt; response, the &lt;code&gt;StartTime&lt;/code&gt; and &lt;code&gt;EndTime&lt;/code&gt; objects specify the time range for which AWS WAF actually returned a sample of web requests. AWS WAF gets the specified number of requests from among the first 5,000 requests that your AWS resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, AWS WAF stops sampling after the 5,000th request. In that case, &lt;code&gt;EndTime&lt;/code&gt; is the time that AWS WAF received the 5,000th request. &lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_acl_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="population_size" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sampled_requests" type="list(object)">
  Returned by the lookup.
</ResponseField>
