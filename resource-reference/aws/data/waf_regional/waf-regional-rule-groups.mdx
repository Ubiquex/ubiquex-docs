---
title: "aws_waf_regional_rule_groups"
description: "`aws_waf_regional_rule_groups` looks up a real WAF Classic Regional rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_rule_groups"})

    		ubx.Data(waf_regional.WafRegionalRuleGroups, "example", waf_regional.WafRegionalRuleGroupsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalRuleGroups } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_rule_groups" });

      data(WafRegionalRuleGroups, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalRuleGroups, WafRegionalRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_rule_groups")

        ubx.data(WafRegionalRuleGroups, "example", WafRegionalRuleGroupsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_groups" type="list(object)">
  The real rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
