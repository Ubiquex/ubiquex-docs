---
title: "aws_waf_regional_change_token_status"
description: "`aws_waf_regional_change_token_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_waf_regional_change_token_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_change_token_status"})

    		ubx.Data(waf_regional.WafRegionalChangeTokenStatus, "example", waf_regional.WafRegionalChangeTokenStatusConfig{
    			ChangeToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalChangeTokenStatus } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_change_token_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_change_token_status" });

      data(WafRegionalChangeTokenStatus, "example", {
        changeToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalChangeTokenStatus, WafRegionalChangeTokenStatusConfig

    def describe():
        ubx.intent("look up aws_waf_regional_change_token_status")

        ubx.data(WafRegionalChangeTokenStatus, "example", WafRegionalChangeTokenStatusConfig(
            change_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="change_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_token_status" type="string">
  Returned by the lookup.
</ResponseField>
