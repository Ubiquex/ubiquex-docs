---
title: "aws_waf_regional_regex_match_sets"
description: "`aws_waf_regional_regex_match_sets` looks up a real WAF Classic Regional regex match set."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_regex_match_sets"})

    		ubx.Data(waf_regional.WafRegionalRegexMatchSets, "example", waf_regional.WafRegionalRegexMatchSetsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalRegexMatchSets } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_regex_match_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_regex_match_sets" });

      data(WafRegionalRegexMatchSets, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalRegexMatchSets, WafRegionalRegexMatchSetsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_regex_match_sets")

        ubx.data(WafRegionalRegexMatchSets, "example", WafRegionalRegexMatchSetsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="regex_match_sets" type="list(object)">
  The real regex match sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
