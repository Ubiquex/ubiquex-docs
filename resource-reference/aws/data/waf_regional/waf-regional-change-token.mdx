---
title: "aws_waf_regional_change_token"
description: "`aws_waf_regional_change_token` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_waf_regional_change_token` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_change_token"})

    		ubx.Data(waf_regional.WafRegionalChangeToken, "example", waf_regional.WafRegionalChangeTokenConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalChangeToken } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_change_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_change_token" });

      data(WafRegionalChangeToken, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalChangeToken, WafRegionalChangeTokenConfig

    def describe():
        ubx.intent("look up aws_waf_regional_change_token")

        ubx.data(WafRegionalChangeToken, "example", WafRegionalChangeTokenConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="change_token" type="string">
  Returned by the lookup.
</ResponseField>
