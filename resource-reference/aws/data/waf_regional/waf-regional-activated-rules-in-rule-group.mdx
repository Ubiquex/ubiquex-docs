---
title: "aws_waf_regional_activated_rules_in_rule_group"
description: "`aws_waf_regional_activated_rules_in_rule_group` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_waf_regional_activated_rules_in_rule_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_activated_rules_in_rule_group"})

    		ubx.Data(waf_regional.WafRegionalActivatedRulesInRuleGroup, "example", waf_regional.WafRegionalActivatedRulesInRuleGroupConfig{
    			Limit:       1,
    			RuleGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalActivatedRulesInRuleGroup } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_activated_rules_in_rule_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_activated_rules_in_rule_group" });

      data(WafRegionalActivatedRulesInRuleGroup, "example", {
        limit: 1,
        ruleGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalActivatedRulesInRuleGroup, WafRegionalActivatedRulesInRuleGroupConfig

    def describe():
        ubx.intent("look up aws_waf_regional_activated_rules_in_rule_group")

        ubx.data(WafRegionalActivatedRulesInRuleGroup, "example", WafRegionalActivatedRulesInRuleGroupConfig(
            limit=1,
            rule_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rule_group_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="activated_rules" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>
