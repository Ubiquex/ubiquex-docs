---
title: "aws_waf_regional_geo_match_sets"
description: "`data_aws_waf_regional_geo_match_sets` looks up a real, already-existing `aws_wafregional_geo_match_set`."
---

It defines a real condition, matching a request's country of origin, that an `aws_wafregional_rule` references. Like the rest of AWS WAF Classic Regional, support for this resource ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_geo_match_sets"})

    		ubx.Data(waf_regional.WafRegionalGeoMatchSets, "example", waf_regional.WafRegionalGeoMatchSetsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WafRegionalGeoMatchSets } from "./local-sdk/aws/sdk/typescript/aws/data/waf_regional/waf_regional_geo_match_sets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_geo_match_sets" });

      data(WafRegionalGeoMatchSets, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalGeoMatchSets, WafRegionalGeoMatchSetsConfig

    def describe():
        ubx.intent("look up aws_waf_regional_geo_match_sets")

        ubx.data(WafRegionalGeoMatchSets, "example", WafRegionalGeoMatchSetsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="geo_match_sets" type="list(object)">
  The real geo match sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
