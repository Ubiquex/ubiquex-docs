---
title: "aws_waf_regional_resources_for_web_acl"
description: "`aws_waf_regional_resources_for_web_acl` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_waf_regional_resources_for_web_acl` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_resources_for_web_acl"})

    		ubx.Data(waf_regional.WafRegionalResourcesForWebAcl, "example", waf_regional.WafRegionalResourcesForWebAclConfig{
    			WebAclid:     "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalResourcesForWebAcl } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_resources_for_web_acl";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_resources_for_web_acl" });

      data(WafRegionalResourcesForWebAcl, "example", {
        webAclid: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalResourcesForWebAcl, WafRegionalResourcesForWebAclConfig

    def describe():
        ubx.intent("look up aws_waf_regional_resources_for_web_acl")

        ubx.data(WafRegionalResourcesForWebAcl, "example", WafRegionalResourcesForWebAclConfig(
            web_aclid="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="web_aclid" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_arns" type="list(string)">
  Returned by the lookup.
</ResponseField>
