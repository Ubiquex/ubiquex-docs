---
title: "aws_waf_regional_resources_for_web_acl"
description: "`aws_waf_regional_resources_for_web_acl` reads the real AWS resources a WAF Classic Regional web ACL is currently associated with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_resources_for_web_acl"})

    		ubx.Data(waf_regional.WafRegionalResourcesForWebAcl, "example", waf_regional.WafRegionalResourcesForWebAclConfig{
    			WebAclid:     "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WafRegionalResourcesForWebAcl } from "./local-sdk/aws/sdk/typescript/aws/data/waf_regional/waf_regional_resources_for_web_acl.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_resources_for_web_acl" });

      data(WafRegionalResourcesForWebAcl, "example", {
        webAclid: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalResourcesForWebAcl, WafRegionalResourcesForWebAclConfig

    def describe():
        ubx.intent("look up aws_waf_regional_resources_for_web_acl")

        ubx.data(WafRegionalResourcesForWebAcl, "example", WafRegionalResourcesForWebAclConfig(
            web_aclid="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="web_aclid" type="string" required>
  The real, unique identifier of the AWS WAF web ACL this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
