---
title: "aws_waf_regional_permission_policy"
description: "`aws_waf_regional_permission_policy` reads the real resource-based policy attached to a WAF Classic Regional rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf_regional "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/waf_regional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_waf_regional_permission_policy"})

    		ubx.Data(waf_regional.WafRegionalPermissionPolicy, "example", waf_regional.WafRegionalPermissionPolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WafRegionalPermissionPolicy } from "@ubx/sdk-aws/aws/data/waf_regional/waf_regional_permission_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_waf_regional_permission_policy" });

      data(WafRegionalPermissionPolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.waf_regional import WafRegionalPermissionPolicy, WafRegionalPermissionPolicyConfig

    def describe():
        ubx.intent("look up aws_waf_regional_permission_policy")

        ubx.data(WafRegionalPermissionPolicy, "example", WafRegionalPermissionPolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
