---
title: "aws_cloudfront_keyvaluestore_key_value_store"
description: "`aws_cloudfront_keyvaluestore_key_value_store` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_keyvaluestore_key_value_store` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront_keyvaluestore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront_keyvaluestore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_keyvaluestore_key_value_store"})

    		ubx.Data(cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKeyValueStore, "example", cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKeyValueStoreConfig{
    			KvsArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudfrontKeyvaluestoreKeyValueStore } from "@ubx/sdk-aws/aws/data/cloudfront_keyvaluestore/cloudfront_keyvaluestore_key_value_store";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_keyvaluestore_key_value_store" });

      data(CloudfrontKeyvaluestoreKeyValueStore, "example", {
        kvsArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront_keyvaluestore import CloudfrontKeyvaluestoreKeyValueStore, CloudfrontKeyvaluestoreKeyValueStoreConfig

    def describe():
        ubx.intent("look up aws_cloudfront_keyvaluestore_key_value_store")

        ubx.data(CloudfrontKeyvaluestoreKeyValueStore, "example", CloudfrontKeyvaluestoreKeyValueStoreConfig(
            kvs_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="kvs_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="item_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_size_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>
