---
title: "aws_cloudfront_keyvaluestore_keys"
description: "`aws_cloudfront_keyvaluestore_keys` reads the real keys stored in a CloudFront KeyValueStore."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront_keyvaluestore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront_keyvaluestore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_keyvaluestore_keys"})

    		ubx.Data(cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKeys, "example", cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKeysConfig{
    			KvsArn:     "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudfrontKeyvaluestoreKeys } from "@ubx/sdk-aws/aws/data/cloudfront_keyvaluestore/cloudfront_keyvaluestore_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_keyvaluestore_keys" });

      data(CloudfrontKeyvaluestoreKeys, "example", {
        kvsArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront_keyvaluestore import CloudfrontKeyvaluestoreKeys, CloudfrontKeyvaluestoreKeysConfig

    def describe():
        ubx.intent("look up aws_cloudfront_keyvaluestore_keys")

        ubx.data(CloudfrontKeyvaluestoreKeys, "example", CloudfrontKeyvaluestoreKeysConfig(
            kvs_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="kvs_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the kvs this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
