---
title: "aws_cloudfront_keyvaluestore_key"
description: "`aws_cloudfront_keyvaluestore_key` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_keyvaluestore_key` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront_keyvaluestore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront_keyvaluestore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_keyvaluestore_key"})

    		ubx.Data(cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKey, "example", cloudfront_keyvaluestore.CloudfrontKeyvaluestoreKeyConfig{
    			Key:    "example",
    			KvsArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudfrontKeyvaluestoreKey } from "@ubx/sdk-aws/aws/data/cloudfront_keyvaluestore/cloudfront_keyvaluestore_key";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_keyvaluestore_key" });

      data(CloudfrontKeyvaluestoreKey, "example", {
        key: "example",
        kvsArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront_keyvaluestore import CloudfrontKeyvaluestoreKey, CloudfrontKeyvaluestoreKeyConfig

    def describe():
        ubx.intent("look up aws_cloudfront_keyvaluestore_key")

        ubx.data(CloudfrontKeyvaluestoreKey, "example", CloudfrontKeyvaluestoreKeyConfig(
            key="example",
            kvs_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="kvs_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="item_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_size_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="value" type="string">
  Returned by the lookup.
</ResponseField>
