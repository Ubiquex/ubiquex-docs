---
title: "aws_m2_engine_versions"
description: "`aws_m2_engine_versions` reads the real, available Mainframe Modernization engine versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	m2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/m2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_m2_engine_versions"})

    		ubx.Data(m2.EngineVersions, "example", m2.EngineVersionsConfig{
    			EngineType: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EngineVersions } from "@ubx/sdk-aws/aws/data/m2/engine_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_m2_engine_versions" });

      data(EngineVersions, "example", {
        engineType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.m2 import EngineVersions, EngineVersionsConfig

    def describe():
        ubx.intent("look up aws_m2_engine_versions")

        ubx.data(EngineVersions, "example", EngineVersionsConfig(
            engine_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  The real database or compute engine type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="engine_versions" type="list(object)">
  The real versions of the database or compute engine included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
