---
title: "aws_m2_signed_bluinsights_url"
description: "`aws_m2_signed_bluinsights_url` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_m2_signed_bluinsights_url` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	m2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/m2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_m2_signed_bluinsights_url"})

    		ubx.Data(m2.SignedBluinsightsUrl, "example", m2.SignedBluinsightsUrlConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SignedBluinsightsUrl } from "@ubx/sdk-aws/aws/data/m2/signed_bluinsights_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_m2_signed_bluinsights_url" });

      data(SignedBluinsightsUrl, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.m2 import SignedBluinsightsUrl, SignedBluinsightsUrlConfig

    def describe():
        ubx.intent("look up aws_m2_signed_bluinsights_url")

        ubx.data(SignedBluinsightsUrl, "example", SignedBluinsightsUrlConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="signed_bi_url" type="string">
  Returned by the lookup.
</ResponseField>
