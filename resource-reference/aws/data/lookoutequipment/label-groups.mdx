---
title: "aws_lookoutequipment_label_groups"
description: "`aws_lookoutequipment_label_groups` looks up a real Lookout for Equipment label group, a collection of labeled historical maintenance events."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_label_groups"})

    		ubx.Data(lookoutequipment.LabelGroups, "example", lookoutequipment.LabelGroupsConfig{
    			LabelGroupNameBeginsWith: "example",
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LabelGroups } from "@ubx/sdk-aws/aws/data/lookoutequipment/label_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_label_groups" });

      data(LabelGroups, "example", {
        labelGroupNameBeginsWith: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import LabelGroups, LabelGroupsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_label_groups")

        ubx.data(LabelGroups, "example", LabelGroupsConfig(
            label_group_name_begins_with="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="label_group_name_begins_with" type="string">
  Restricts results to label groups whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="label_group_summaries" type="list(object)">
  The real, summarized label groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
