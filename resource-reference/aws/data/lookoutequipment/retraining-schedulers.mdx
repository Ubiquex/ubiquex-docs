---
title: "aws_lookoutequipment_retraining_schedulers"
description: "`aws_lookoutequipment_retraining_schedulers` looks up a real Lookout for Equipment retraining scheduler..."
---

`aws_lookoutequipment_retraining_schedulers` looks up a real Lookout for Equipment retraining scheduler, automatically retraining a model on a recurring schedule.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_retraining_schedulers"})

    		ubx.Data(lookoutequipment.RetrainingSchedulers, "example", lookoutequipment.RetrainingSchedulersConfig{
    			MaxResults:          1,
    			ModelNameBeginsWith: "example",
    			Status:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RetrainingSchedulers } from "@ubx/sdk-aws/aws/data/lookoutequipment/retraining_schedulers";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_retraining_schedulers" });

      data(RetrainingSchedulers, "example", {
        maxResults: 1,
        modelNameBeginsWith: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import RetrainingSchedulers, RetrainingSchedulersConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_retraining_schedulers")

        ubx.data(RetrainingSchedulers, "example", RetrainingSchedulersConfig(
            max_results=1,
            model_name_begins_with="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_name_begins_with" type="string">
  Restricts results to models whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retraining_scheduler_summaries" type="list(object)">
  The real, summarized retraining schedulers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
