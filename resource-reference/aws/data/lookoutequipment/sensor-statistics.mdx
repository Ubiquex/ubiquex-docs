---
title: "aws_lookoutequipment_sensor_statistics"
description: "`aws_lookoutequipment_sensor_statistics` reads real, computed sensor statistics for a Lookout for Equipment dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_sensor_statistics"})

    		ubx.Data(lookoutequipment.SensorStatistics, "example", lookoutequipment.SensorStatisticsConfig{
    			DatasetName:    "example-dataset-name",
    			IngestionJobId: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SensorStatistics } from "@ubx/sdk-aws/aws/data/lookoutequipment/sensor_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_sensor_statistics" });

      data(SensorStatistics, "example", {
        datasetName: "example-dataset-name",
        ingestionJobId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import SensorStatistics, SensorStatisticsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_sensor_statistics")

        ubx.data(SensorStatistics, "example", SensorStatisticsConfig(
            dataset_name="example-dataset-name",
            ingestion_job_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_name" type="string" required>
  The real name of the dataset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="ingestion_job_id" type="string">
  The real, unique identifier of the ingestion job this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sensor_statistics_summaries" type="list(object)">
  The real, summarized sensor statisticss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
