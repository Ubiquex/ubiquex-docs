---
title: "aws_lookoutequipment_labels"
description: "`aws_lookoutequipment_labels` reads the real labels within a Lookout for Equipment label group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_labels"})

    		ubx.Data(lookoutequipment.Labels, "example", lookoutequipment.LabelsConfig{
    			LabelGroupName:    "example-label-group-name",
    			Equipment:         "example",
    			FaultCode:         "example",
    			IntervalEndTime:   "example",
    			IntervalStartTime: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Labels } from "@ubx/sdk-aws/aws/data/lookoutequipment/labels";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_labels" });

      data(Labels, "example", {
        labelGroupName: "example-label-group-name",
        equipment: "example",
        faultCode: "example",
        intervalEndTime: "example",
        intervalStartTime: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import Labels, LabelsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_labels")

        ubx.data(Labels, "example", LabelsConfig(
            label_group_name="example-label-group-name",
            equipment="example",
            fault_code="example",
            interval_end_time="example",
            interval_start_time="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="equipment" type="string">
  The real equipment included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="fault_code" type="string">
  The real code identifying the fault. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="interval_end_time" type="string">
  The real timestamp of the interval end. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="interval_start_time" type="string">
  The real timestamp of the interval start. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="label_group_name" type="string" required>
  The real name of the label group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="label_summaries" type="list(object)">
  The real, summarized labels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
