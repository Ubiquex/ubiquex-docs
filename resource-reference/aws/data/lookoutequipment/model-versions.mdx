---
title: "aws_lookoutequipment_model_versions"
description: "`aws_lookoutequipment_model_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lookoutequipment_model_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_model_versions"})

    		ubx.Data(lookoutequipment.ModelVersions, "example", lookoutequipment.ModelVersionsConfig{
    			ModelName:          "example-model-name",
    			CreatedAtEndTime:   "example",
    			CreatedAtStartTime: "example",
    			MaxModelVersion:    1,
    			MaxResults:         1,
    			MinModelVersion:    1,
    			SourceType:         "example",
    			Status:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelVersions } from "@ubx/sdk-aws/aws/data/lookoutequipment/model_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_model_versions" });

      data(ModelVersions, "example", {
        modelName: "example-model-name",
        createdAtEndTime: "example",
        createdAtStartTime: "example",
        maxModelVersion: 1,
        maxResults: 1,
        minModelVersion: 1,
        sourceType: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import ModelVersions, ModelVersionsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_model_versions")

        ubx.data(ModelVersions, "example", ModelVersionsConfig(
            model_name="example-model-name",
            created_at_end_time="example",
            created_at_start_time="example",
            max_model_version=1,
            max_results=1,
            min_model_version=1,
            source_type="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_at_end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_at_start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_model_version" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_model_version" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="model_version_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
