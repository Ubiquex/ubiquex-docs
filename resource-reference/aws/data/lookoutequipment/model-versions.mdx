---
title: "aws_lookoutequipment_model_versions"
description: "`aws_lookoutequipment_model_versions` reads the real, trained versions of a Lookout for Equipment model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_model_versions"})

    		ubx.Data(lookoutequipment.ModelVersions, "example", lookoutequipment.ModelVersionsConfig{
    			ModelName:          "example-model-name",
    			CreatedAtEndTime:   "example",
    			CreatedAtStartTime: "example",
    			MaxModelVersion:    1,
    			MaxResults:         1,
    			MinModelVersion:    1,
    			SourceType:         "example",
    			Status:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelVersions } from "@ubx/sdk-aws/aws/data/lookoutequipment/model_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_model_versions" });

      data(ModelVersions, "example", {
        modelName: "example-model-name",
        createdAtEndTime: "example",
        createdAtStartTime: "example",
        maxModelVersion: 1,
        maxResults: 1,
        minModelVersion: 1,
        sourceType: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import ModelVersions, ModelVersionsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_model_versions")

        ubx.data(ModelVersions, "example", ModelVersionsConfig(
            model_name="example-model-name",
            created_at_end_time="example",
            created_at_start_time="example",
            max_model_version=1,
            max_results=1,
            min_model_version=1,
            source_type="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_at_end_time" type="string">
  The real timestamp of the created at end. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_at_start_time" type="string">
  The real timestamp of the created at start. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_model_version" type="number">
  The real version of the max model. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="min_model_version" type="number">
  The real version of the min model. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_name" type="string" required>
  The real name of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string">
  The real type of the source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="model_version_summaries" type="list(object)">
  The real, summarized model versions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
