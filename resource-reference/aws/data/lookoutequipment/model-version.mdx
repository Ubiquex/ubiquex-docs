---
title: "aws_lookoutequipment_model_version"
description: "`aws_lookoutequipment_model_version` looks up one real, trained version of a Lookout for Equipment model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_model_version"})

    		ubx.Data(lookoutequipment.ModelVersion, "example", lookoutequipment.ModelVersionConfig{
    			ModelName:    "example-model-name",
    			ModelVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelVersion } from "./local-sdk/aws/sdk/typescript/aws/data/lookoutequipment/model_version.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_model_version" });

      data(ModelVersion, "example", {
        modelName: "example-model-name",
        modelVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import ModelVersion, ModelVersionConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_model_version")

        ubx.data(ModelVersion, "example", ModelVersionConfig(
            model_name="example-model-name",
            model_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="model_name" type="string" required>
  The real name of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="model_version" type="number" required>
  The real version of the model. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_promotion_result" type="string">
  The real result of automatically promoting this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="auto_promotion_result_reason" type="string">
  The real, human-readable reason for the auto promotion result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_pre_processing_configuration" type="object">
  &lt;p&gt;The configuration is the &lt;code&gt;TargetSamplingRate&lt;/code&gt;, which is the sampling rate of the data after post processing by Amazon Lookout for Equipment. For example, if you provide data that has been collected at a 1 second level and you want the system to resample the data at a 1 minute rate before training, the &lt;code&gt;TargetSamplingRate&lt;/code&gt; is 1 minute.&lt;/p&gt; &lt;p&gt;When providing a value for the &lt;code&gt;TargetSamplingRate&lt;/code&gt;, you must attach the prefix "PT" to the rate you want. The value for a 1 second rate is therefore &lt;i&gt;PT1S&lt;/i&gt;, the value for a 15 minute rate is &lt;i&gt;PT15M&lt;/i&gt;, and the value for a 1 hour rate is &lt;i&gt;PT1H&lt;/i&gt; &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="target_sampling_rate" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dataset_arn" type="string">
  The real Amazon Resource Name (ARN) of the dataset this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_name" type="string">
  The real name of the dataset this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="evaluation_data_end_time" type="string">
  The real timestamp of the evaluation data end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="evaluation_data_start_time" type="string">
  The real timestamp of the evaluation data start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failed_reason" type="string">
  The real, human-readable reason for the failed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_job_end_time" type="string">
  The real timestamp of the import job end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_job_start_time" type="string">
  The real timestamp of the import job start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="imported_data_size_in_bytes" type="number">
  The real size of the imported data size in, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="labels_input_configuration" type="object">
  &lt;p&gt;Contains the configuration information for the S3 location being used to hold label data. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="label_group_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3_input_configuration" type="object">
      &lt;p&gt;The location information (prefix and bucket name) for the s3 location being used for label data. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model_arn" type="string">
  The real Amazon Resource Name (ARN) of the model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model_diagnostics_output_configuration" type="object">
  &lt;p&gt;Output configuration information for the pointwise model diagnostics for an Amazon Lookout for Equipment model.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3_output_configuration" type="object" required>
      &lt;p&gt;The Amazon S3 location for the pointwise model diagnostics for an Amazon Lookout for Equipment model. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_diagnostics_results_object" type="object">
  &lt;p&gt;Contains information about an S3 bucket. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="key" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_metrics" type="string">
  The real metrics computed for this model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model_quality" type="string">
  The real, computed quality metrics for this model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model_version_arn" type="string">
  The real Amazon Resource Name (ARN) of the model version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="off_condition" type="string">
  The real condition under which this object is turned off. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="prior_model_metrics" type="string">
  The real metrics computed for the prior version of this model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retraining_available_data_in_days" type="number">
  The real number of days of new data available for retraining. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema" type="string">
  The real schema this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="server_side_kms_key_id" type="string">
  The real, unique identifier of the server side kms key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_model_version_arn" type="string">
  The real Amazon Resource Name (ARN) of the source model version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_type" type="string">
  The real type of the source this object was derived from. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="training_data_end_time" type="string">
  The real timestamp of the training data end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="training_data_start_time" type="string">
  The real timestamp of the training data start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="training_execution_end_time" type="string">
  The real timestamp of the training execution end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="training_execution_start_time" type="string">
  The real timestamp of the training execution start. **(AI-inferred)** Returned by the lookup.
</ResponseField>
