---
title: "aws_lookoutequipment_data_ingestion_jobs"
description: "`aws_lookoutequipment_data_ingestion_jobs` reads the real data ingestion jobs run for a Lookout for Equipment dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_data_ingestion_jobs"})

    		ubx.Data(lookoutequipment.DataIngestionJobs, "example", lookoutequipment.DataIngestionJobsConfig{
    			DatasetName: "example-dataset-name",
    			MaxResults:  1,
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataIngestionJobs } from "./local-sdk/aws/sdk/typescript/aws/data/lookoutequipment/data_ingestion_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_data_ingestion_jobs" });

      data(DataIngestionJobs, "example", {
        datasetName: "example-dataset-name",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import DataIngestionJobs, DataIngestionJobsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_data_ingestion_jobs")

        ubx.data(DataIngestionJobs, "example", DataIngestionJobsConfig(
            dataset_name="example-dataset-name",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_name" type="string">
  The real name of the dataset this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_ingestion_job_summaries" type="list(object)">
  The real, summarized data ingestion jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
