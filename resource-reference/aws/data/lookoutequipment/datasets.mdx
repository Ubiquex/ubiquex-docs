---
title: "aws_lookoutequipment_datasets"
description: "`aws_lookoutequipment_datasets` looks up a real Lookout for Equipment dataset, holding sensor time-series data for a piece of equipment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lookoutequipment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lookoutequipment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lookoutequipment_datasets"})

    		ubx.Data(lookoutequipment.Datasets, "example", lookoutequipment.DatasetsConfig{
    			DatasetNameBeginsWith: "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Datasets } from "@ubx/sdk-aws/aws/data/lookoutequipment/datasets";

    export default stack("example", () => {
      intent({ summary: "look up aws_lookoutequipment_datasets" });

      data(Datasets, "example", {
        datasetNameBeginsWith: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lookoutequipment import Datasets, DatasetsConfig

    def describe():
        ubx.intent("look up aws_lookoutequipment_datasets")

        ubx.data(Datasets, "example", DatasetsConfig(
            dataset_name_begins_with="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_name_begins_with" type="string">
  Restricts results to datasets whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dataset_summaries" type="list(object)">
  The real, summarized datasets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
