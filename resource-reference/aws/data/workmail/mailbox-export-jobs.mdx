---
title: "aws_workmail_mailbox_export_jobs"
description: "`aws_workmail_mailbox_export_jobs` reads the real jobs exporting WorkMail mailbox content to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mailbox_export_jobs"})

    		ubx.Data(workmail.MailboxExportJobs, "example", workmail.MailboxExportJobsConfig{
    			OrganizationId: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailboxExportJobs } from "@ubx/sdk-aws/aws/data/workmail/mailbox_export_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mailbox_export_jobs" });

      data(MailboxExportJobs, "example", {
        organizationId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MailboxExportJobs, MailboxExportJobsConfig

    def describe():
        ubx.intent("look up aws_workmail_mailbox_export_jobs")

        ubx.data(MailboxExportJobs, "example", MailboxExportJobsConfig(
            organization_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
