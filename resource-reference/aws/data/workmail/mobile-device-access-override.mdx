---
title: "aws_workmail_mobile_device_access_override"
description: "`aws_workmail_mobile_device_access_override` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_mobile_device_access_override` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mobile_device_access_override"})

    		ubx.Data(workmail.MobileDeviceAccessOverride, "example", workmail.MobileDeviceAccessOverrideConfig{
    			DeviceId:       "example",
    			OrganizationId: "example",
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MobileDeviceAccessOverride } from "@ubx/sdk-aws/aws/data/workmail/mobile_device_access_override";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mobile_device_access_override" });

      data(MobileDeviceAccessOverride, "example", {
        deviceId: "example",
        organizationId: "example",
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MobileDeviceAccessOverride, MobileDeviceAccessOverrideConfig

    def describe():
        ubx.intent("look up aws_workmail_mobile_device_access_override")

        ubx.data(MobileDeviceAccessOverride, "example", MobileDeviceAccessOverrideConfig(
            device_id="example",
            organization_id="example",
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="date_created" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="date_modified" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="effect" type="string">
  Returned by the lookup.
</ResponseField>
