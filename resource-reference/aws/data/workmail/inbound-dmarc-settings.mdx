---
title: "aws_workmail_inbound_dmarc_settings"
description: "`aws_workmail_inbound_dmarc_settings` reads the real inbound DMARC enforcement setting of a WorkMail organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_inbound_dmarc_settings"})

    		ubx.Data(workmail.InboundDmarcSettings, "example", workmail.InboundDmarcSettingsConfig{
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InboundDmarcSettings } from "@ubx/sdk-aws/aws/data/workmail/inbound_dmarc_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_inbound_dmarc_settings" });

      data(InboundDmarcSettings, "example", {
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import InboundDmarcSettings, InboundDmarcSettingsConfig

    def describe():
        ubx.intent("look up aws_workmail_inbound_dmarc_settings")

        ubx.data(InboundDmarcSettings, "example", InboundDmarcSettingsConfig(
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enforced" type="bool">
  Whether this setting is currently enforced. **(AI-inferred)** Returned by the lookup.
</ResponseField>
