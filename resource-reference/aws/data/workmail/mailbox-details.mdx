---
title: "aws_workmail_mailbox_details"
description: "`aws_workmail_mailbox_details` reads real, detailed mailbox information (size, state) for a WorkMail user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mailbox_details"})

    		ubx.Data(workmail.MailboxDetails, "example", workmail.MailboxDetailsConfig{
    			OrganizationId: "example",
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailboxDetails } from "@ubx/sdk-aws/aws/data/workmail/mailbox_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mailbox_details" });

      data(MailboxDetails, "example", {
        organizationId: "example",
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MailboxDetails, MailboxDetailsConfig

    def describe():
        ubx.intent("look up aws_workmail_mailbox_details")

        ubx.data(MailboxDetails, "example", MailboxDetailsConfig(
            organization_id="example",
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="mailbox_quota" type="number">
  The real, configured mailbox storage quota for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="mailbox_size" type="number">
  The real size of the mailbox. **(AI-inferred)** Returned by the lookup.
</ResponseField>
