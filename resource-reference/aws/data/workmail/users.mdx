---
title: "aws_workmail_users"
description: "`aws_workmail_users` looks up a real WorkMail user mailbox."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_users"})

    		ubx.Data(workmail.Users, "example", workmail.UsersConfig{
    			OrganizationId: "example",
    			Filters:        map[string]any{"display_name_prefix": "example"},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Users } from "@ubx/sdk-aws/aws/data/workmail/users";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_users" });

      data(Users, "example", {
        organizationId: "example",
        filters: { displayNamePrefix: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import Users, UsersConfig
    from ubx.aws.data.workmail.users import Users_Filters

    def describe():
        ubx.intent("look up aws_workmail_users")

        ubx.data(Users, "example", UsersConfig(
            organization_id="example",
            filters=Users_Filters(display_name_prefix="example"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt; Filtering options for &lt;i&gt;ListUsers&lt;/i&gt; operation. This is only used as input to Operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="display_name_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="identity_provider_user_id_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="primary_email_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="username_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
