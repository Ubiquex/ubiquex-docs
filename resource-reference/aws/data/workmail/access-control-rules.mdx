---
title: "aws_workmail_access_control_rules"
description: "`aws_workmail_access_control_rules` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_access_control_rules` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_access_control_rules"})

    		ubx.Data(workmail.AccessControlRules, "example", workmail.AccessControlRulesConfig{
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessControlRules } from "@ubx/sdk-aws/aws/data/workmail/access_control_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_access_control_rules" });

      data(AccessControlRules, "example", {
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import AccessControlRules, AccessControlRulesConfig

    def describe():
        ubx.intent("look up aws_workmail_access_control_rules")

        ubx.data(AccessControlRules, "example", AccessControlRulesConfig(
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rules" type="list(object)">
  Returned by the lookup.
</ResponseField>
