---
title: "aws_workmail_resources"
description: "`aws_workmail_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_resources"})

    		ubx.Data(workmail.Resources, "example", workmail.ResourcesConfig{
    			OrganizationId: "example",
    			Filters:        map[string]any{"name_prefix": "example"},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resources } from "@ubx/sdk-aws/aws/data/workmail/resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_resources" });

      data(Resources, "example", {
        organizationId: "example",
        filters: { namePrefix: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import Resources, ResourcesConfig

    def describe():
        ubx.intent("look up aws_workmail_resources")

        ubx.data(Resources, "example", ResourcesConfig(
            organization_id="example",
            filters={"name_prefix": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Filtering options for &lt;i&gt;ListResources&lt;/i&gt; operation. This is only used as input to Operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="primary_email_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
