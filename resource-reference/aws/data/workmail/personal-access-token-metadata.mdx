---
title: "aws_workmail_personal_access_token_metadata"
description: "`aws_workmail_personal_access_token_metadata` reads real metadata about a WorkMail personal access token, used for IMAP/SMTP app access."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_personal_access_token_metadata"})

    		ubx.Data(workmail.PersonalAccessTokenMetadata, "example", workmail.PersonalAccessTokenMetadataConfig{
    			OrganizationId:        "example",
    			PersonalAccessTokenId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PersonalAccessTokenMetadata } from "@ubx/sdk-aws/aws/data/workmail/personal_access_token_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_personal_access_token_metadata" });

      data(PersonalAccessTokenMetadata, "example", {
        organizationId: "example",
        personalAccessTokenId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import PersonalAccessTokenMetadata, PersonalAccessTokenMetadataConfig

    def describe():
        ubx.intent("look up aws_workmail_personal_access_token_metadata")

        ubx.data(PersonalAccessTokenMetadata, "example", PersonalAccessTokenMetadataConfig(
            organization_id="example",
            personal_access_token_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="personal_access_token_id" type="string" required>
  The real, unique identifier of the personal access token this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="date_created" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="date_last_used" type="string">
  The real timestamp this object was last used. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expires_time" type="string">
  The real timestamp of the expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scopes" type="list(string)">
  The real scopes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
