---
title: "aws_workmail_default_retention_policy"
description: "`aws_workmail_default_retention_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_default_retention_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_default_retention_policy"})

    		ubx.Data(workmail.DefaultRetentionPolicy, "example", workmail.DefaultRetentionPolicyConfig{
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultRetentionPolicy } from "@ubx/sdk-aws/aws/data/workmail/default_retention_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_default_retention_policy" });

      data(DefaultRetentionPolicy, "example", {
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import DefaultRetentionPolicy, DefaultRetentionPolicyConfig

    def describe():
        ubx.intent("look up aws_workmail_default_retention_policy")

        ubx.data(DefaultRetentionPolicy, "example", DefaultRetentionPolicyConfig(
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="folder_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>
