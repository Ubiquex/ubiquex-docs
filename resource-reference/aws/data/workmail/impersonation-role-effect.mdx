---
title: "aws_workmail_impersonation_role_effect"
description: "`aws_workmail_impersonation_role_effect` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_impersonation_role_effect` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_impersonation_role_effect"})

    		ubx.Data(workmail.ImpersonationRoleEffect, "example", workmail.ImpersonationRoleEffectConfig{
    			ImpersonationRoleId: "example",
    			OrganizationId:      "example",
    			TargetUser:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImpersonationRoleEffect } from "@ubx/sdk-aws/aws/data/workmail/impersonation_role_effect";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_impersonation_role_effect" });

      data(ImpersonationRoleEffect, "example", {
        impersonationRoleId: "example",
        organizationId: "example",
        targetUser: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import ImpersonationRoleEffect, ImpersonationRoleEffectConfig

    def describe():
        ubx.intent("look up aws_workmail_impersonation_role_effect")

        ubx.data(ImpersonationRoleEffect, "example", ImpersonationRoleEffectConfig(
            impersonation_role_id="example",
            organization_id="example",
            target_user="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="impersonation_role_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="target_user" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="effect" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="matched_rules" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  Returned by the lookup.
</ResponseField>
