---
title: "aws_workmail_impersonation_role_effect"
description: "`aws_workmail_impersonation_role_effect` reads the real, computed effect of a WorkMail impersonation role for a given target mailbox."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_impersonation_role_effect"})

    		ubx.Data(workmail.ImpersonationRoleEffect, "example", workmail.ImpersonationRoleEffectConfig{
    			ImpersonationRoleId: "example",
    			OrganizationId:      "example",
    			TargetUser:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ImpersonationRoleEffect } from "./local-sdk/aws/sdk/typescript/aws/data/workmail/impersonation_role_effect.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_impersonation_role_effect" });

      data(ImpersonationRoleEffect, "example", {
        impersonationRoleId: "example",
        organizationId: "example",
        targetUser: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import ImpersonationRoleEffect, ImpersonationRoleEffectConfig

    def describe():
        ubx.intent("look up aws_workmail_impersonation_role_effect")

        ubx.data(ImpersonationRoleEffect, "example", ImpersonationRoleEffectConfig(
            impersonation_role_id="example",
            organization_id="example",
            target_user="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="impersonation_role_id" type="string" required>
  The real, unique identifier of the impersonation role this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="target_user" type="string" required>
  The real user this action targets. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="effect" type="string">
  The real effect (allow or deny) this policy statement applies. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="matched_rules" type="list(object)">
  The real rules for the matched. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
