---
title: "aws_workmail_mobile_device_access_effect"
description: "`aws_workmail_mobile_device_access_effect` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_mobile_device_access_effect` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mobile_device_access_effect"})

    		ubx.Data(workmail.MobileDeviceAccessEffect, "example", workmail.MobileDeviceAccessEffectConfig{
    			OrganizationId:        "example",
    			DeviceModel:           "example",
    			DeviceOperatingSystem: "example",
    			DeviceType:            "example",
    			DeviceUserAgent:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MobileDeviceAccessEffect } from "@ubx/sdk-aws/aws/data/workmail/mobile_device_access_effect";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mobile_device_access_effect" });

      data(MobileDeviceAccessEffect, "example", {
        organizationId: "example",
        deviceModel: "example",
        deviceOperatingSystem: "example",
        deviceType: "example",
        deviceUserAgent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MobileDeviceAccessEffect, MobileDeviceAccessEffectConfig

    def describe():
        ubx.intent("look up aws_workmail_mobile_device_access_effect")

        ubx.data(MobileDeviceAccessEffect, "example", MobileDeviceAccessEffectConfig(
            organization_id="example",
            device_model="example",
            device_operating_system="example",
            device_type="example",
            device_user_agent="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_model" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="device_operating_system" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="device_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="device_user_agent" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="effect" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="matched_rules" type="list(object)">
  Returned by the lookup.
</ResponseField>
