---
title: "aws_workmail_mobile_device_access_effect"
description: "`aws_workmail_mobile_device_access_effect` reads the real, computed access effect (allow or deny) a mobile device access rule applies for a given device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mobile_device_access_effect"})

    		ubx.Data(workmail.MobileDeviceAccessEffect, "example", workmail.MobileDeviceAccessEffectConfig{
    			OrganizationId:        "example",
    			DeviceModel:           "example",
    			DeviceOperatingSystem: "example",
    			DeviceType:            "example",
    			DeviceUserAgent:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MobileDeviceAccessEffect } from "@ubx/sdk-aws/aws/data/workmail/mobile_device_access_effect";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mobile_device_access_effect" });

      data(MobileDeviceAccessEffect, "example", {
        organizationId: "example",
        deviceModel: "example",
        deviceOperatingSystem: "example",
        deviceType: "example",
        deviceUserAgent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MobileDeviceAccessEffect, MobileDeviceAccessEffectConfig

    def describe():
        ubx.intent("look up aws_workmail_mobile_device_access_effect")

        ubx.data(MobileDeviceAccessEffect, "example", MobileDeviceAccessEffectConfig(
            organization_id="example",
            device_model="example",
            device_operating_system="example",
            device_type="example",
            device_user_agent="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_model" type="string">
  The real device model. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="device_operating_system" type="string">
  The real operating system of this device. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="device_type" type="string">
  The real, discriminating type of the device this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="device_user_agent" type="string">
  The real user agent string reported by this device. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="effect" type="string">
  The real effect (allow or deny) this policy statement applies. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="matched_rules" type="list(object)">
  The real rules for the matched. **(AI-inferred)** Returned by the lookup.
</ResponseField>
