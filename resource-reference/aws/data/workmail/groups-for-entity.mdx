---
title: "aws_workmail_groups_for_entity"
description: "`aws_workmail_groups_for_entity` reads the real WorkMail groups a given user or entity belongs to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_groups_for_entity"})

    		ubx.Data(workmail.GroupsForEntity, "example", workmail.GroupsForEntityConfig{
    			EntityId:       "example",
    			OrganizationId: "example",
    			Filters:        map[string]any{"group_name_prefix": "example"},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GroupsForEntity } from "./local-sdk/aws/sdk/typescript/aws/data/workmail/groups_for_entity.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_groups_for_entity" });

      data(GroupsForEntity, "example", {
        entityId: "example",
        organizationId: "example",
        filters: { groupNamePrefix: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import GroupsForEntity, GroupsForEntityConfig

    def describe():
        ubx.intent("look up aws_workmail_groups_for_entity")

        ubx.data(GroupsForEntity, "example", GroupsForEntityConfig(
            entity_id="example",
            organization_id="example",
            filters={"group_name_prefix": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_id" type="string" required>
  The real, unique identifier of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt; Filtering options for &lt;i&gt;ListGroupsForEntity&lt;/i&gt; operation. This is only used as input to Operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="group_name_prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
