---
title: "aws_workmail_email_monitoring_configuration"
description: "`aws_workmail_email_monitoring_configuration` reads the real email flow monitoring configuration (log delivery to S3) of a WorkMail organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_email_monitoring_configuration"})

    		ubx.Data(workmail.EmailMonitoringConfiguration, "example", workmail.EmailMonitoringConfigurationConfig{
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EmailMonitoringConfiguration } from "@ubx/sdk-aws/aws/data/workmail/email_monitoring_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_email_monitoring_configuration" });

      data(EmailMonitoringConfiguration, "example", {
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import EmailMonitoringConfiguration, EmailMonitoringConfigurationConfig

    def describe():
        ubx.intent("look up aws_workmail_email_monitoring_configuration")

        ubx.data(EmailMonitoringConfiguration, "example", EmailMonitoringConfigurationConfig(
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="log_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the log group this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
