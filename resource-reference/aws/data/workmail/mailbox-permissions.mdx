---
title: "aws_workmail_mailbox_permissions"
description: "`aws_workmail_mailbox_permissions` reads the real, delegated access permissions (full access, send-as) granted on a WorkMail mailbox."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mailbox_permissions"})

    		ubx.Data(workmail.MailboxPermissions, "example", workmail.MailboxPermissionsConfig{
    			EntityId:       "example",
    			OrganizationId: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailboxPermissions } from "@ubx/sdk-aws/aws/data/workmail/mailbox_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mailbox_permissions" });

      data(MailboxPermissions, "example", {
        entityId: "example",
        organizationId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MailboxPermissions, MailboxPermissionsConfig

    def describe():
        ubx.intent("look up aws_workmail_mailbox_permissions")

        ubx.data(MailboxPermissions, "example", MailboxPermissionsConfig(
            entity_id="example",
            organization_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_id" type="string" required>
  The real, unique identifier of this entity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
