---
title: "aws_workmail_mail_domain"
description: "`aws_workmail_mail_domain` looks up one real email domain registered with a WorkMail organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mail_domain"})

    		ubx.Data(workmail.MailDomain, "example", workmail.MailDomainConfig{
    			DomainName:     "example-domain-name",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MailDomain } from "./local-sdk/aws/sdk/typescript/aws/data/workmail/mail_domain.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mail_domain" });

      data(MailDomain, "example", {
        domainName: "example-domain-name",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MailDomain, MailDomainConfig

    def describe():
        ubx.intent("look up aws_workmail_mail_domain")

        ubx.data(MailDomain, "example", MailDomainConfig(
            domain_name="example-domain-name",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  The real domain name this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dkim_verification_status" type="string">
  The real, current status of the dkim verification this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Whether this object is the default one for its own scope. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_test_domain" type="bool">
  Whether this domain is a test domain. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ownership_verification_status" type="string">
  The real, current status of the ownership verification this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  The real records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
