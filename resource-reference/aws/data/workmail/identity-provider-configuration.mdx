---
title: "aws_workmail_identity_provider_configuration"
description: "`aws_workmail_identity_provider_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmail_identity_provider_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_identity_provider_configuration"})

    		ubx.Data(workmail.IdentityProviderConfiguration, "example", workmail.IdentityProviderConfigurationConfig{
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityProviderConfiguration } from "@ubx/sdk-aws/aws/data/workmail/identity_provider_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_identity_provider_configuration" });

      data(IdentityProviderConfiguration, "example", {
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import IdentityProviderConfiguration, IdentityProviderConfigurationConfig

    def describe():
        ubx.intent("look up aws_workmail_identity_provider_configuration")

        ubx.data(IdentityProviderConfiguration, "example", IdentityProviderConfigurationConfig(
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authentication_mode" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="identity_center_configuration" type="object">
  &lt;p&gt; The IAM Identity Center configuration. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="instance_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="personal_access_token_configuration" type="object">
  &lt;p&gt; Displays the Personal Access Token status. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="lifetime_in_days" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
