---
title: "aws_workmail_entity"
description: "`aws_workmail_entity` looks up one real WorkMail entity (a user, group, or resource) by its own email address or ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_entity"})

    		ubx.Data(workmail.Entity, "example", workmail.EntityConfig{
    			Email:          "example",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Entity } from "@ubx/sdk-aws/aws/data/workmail/entity";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_entity" });

      data(Entity, "example", {
        email: "example",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import Entity, EntityConfig

    def describe():
        ubx.intent("look up aws_workmail_entity")

        ubx.data(Entity, "example", EntityConfig(
            email="example",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="email" type="string" required>
  The real email address of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entity_id" type="string">
  The real, unique identifier of this entity. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
