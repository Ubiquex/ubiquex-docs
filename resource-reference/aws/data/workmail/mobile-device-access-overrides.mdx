---
title: "aws_workmail_mobile_device_access_overrides"
description: "`aws_workmail_mobile_device_access_overrides` reads the real, per-user overrides of a WorkMail organization's own mobile device access rules."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mobile_device_access_overrides"})

    		ubx.Data(workmail.MobileDeviceAccessOverrides, "example", workmail.MobileDeviceAccessOverridesConfig{
    			OrganizationId: "example",
    			DeviceId:       "example",
    			MaxResults:     1,
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MobileDeviceAccessOverrides } from "@ubx/sdk-aws/aws/data/workmail/mobile_device_access_overrides";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mobile_device_access_overrides" });

      data(MobileDeviceAccessOverrides, "example", {
        organizationId: "example",
        deviceId: "example",
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MobileDeviceAccessOverrides, MobileDeviceAccessOverridesConfig

    def describe():
        ubx.intent("look up aws_workmail_mobile_device_access_overrides")

        ubx.data(MobileDeviceAccessOverrides, "example", MobileDeviceAccessOverridesConfig(
            organization_id="example",
            device_id="example",
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_id" type="string">
  The real, unique identifier of the device this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="overrides" type="list(object)">
  The real overrides applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
