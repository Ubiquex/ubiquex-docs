---
title: "aws_workmail_personal_access_tokens"
description: "`aws_workmail_personal_access_tokens` reads the real personal access tokens issued to a WorkMail user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_personal_access_tokens"})

    		ubx.Data(workmail.PersonalAccessTokens, "example", workmail.PersonalAccessTokensConfig{
    			OrganizationId: "example",
    			MaxResults:     1,
    			UserId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PersonalAccessTokens } from "@ubx/sdk-aws/aws/data/workmail/personal_access_tokens";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_personal_access_tokens" });

      data(PersonalAccessTokens, "example", {
        organizationId: "example",
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import PersonalAccessTokens, PersonalAccessTokensConfig

    def describe():
        ubx.intent("look up aws_workmail_personal_access_tokens")

        ubx.data(PersonalAccessTokens, "example", PersonalAccessTokensConfig(
            organization_id="example",
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="personal_access_token_summaries" type="list(object)">
  The real, summarized personal access tokens included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
