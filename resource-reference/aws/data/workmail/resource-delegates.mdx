---
title: "aws_workmail_resource_delegates"
description: "`aws_workmail_resource_delegates` reads the real users delegated to manage booking requests for a WorkMail resource (a room or equipment mailbox)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_resource_delegates"})

    		ubx.Data(workmail.ResourceDelegates, "example", workmail.ResourceDelegatesConfig{
    			OrganizationId: "example",
    			ResourceId:     "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceDelegates } from "@ubx/sdk-aws/aws/data/workmail/resource_delegates";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_resource_delegates" });

      data(ResourceDelegates, "example", {
        organizationId: "example",
        resourceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import ResourceDelegates, ResourceDelegatesConfig

    def describe():
        ubx.intent("look up aws_workmail_resource_delegates")

        ubx.data(ResourceDelegates, "example", ResourceDelegatesConfig(
            organization_id="example",
            resource_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delegates" type="list(object)">
  The real delegates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
