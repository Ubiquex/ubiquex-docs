---
title: "aws_workmail_mailbox_export_job"
description: "`aws_workmail_mailbox_export_job` looks up one real job exporting a WorkMail mailbox's own content to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_mailbox_export_job"})

    		ubx.Data(workmail.MailboxExportJob, "example", workmail.MailboxExportJobConfig{
    			JobId:          "example",
    			OrganizationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MailboxExportJob } from "@ubx/sdk-aws/aws/data/workmail/mailbox_export_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_mailbox_export_job" });

      data(MailboxExportJob, "example", {
        jobId: "example",
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import MailboxExportJob, MailboxExportJobConfig

    def describe():
        ubx.intent("look up aws_workmail_mailbox_export_job")

        ubx.data(MailboxExportJob, "example", MailboxExportJobConfig(
            job_id="example",
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="entity_id" type="string">
  The real, unique identifier of this entity. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_info" type="string">
  The real, detailed information about the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="estimated_progress" type="number">
  The real, estimated progress of this operation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS KMS key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string">
  The real name of the s3 bucket this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_path" type="string">
  The real path of the s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  The real S3 key prefix used for the s3. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
