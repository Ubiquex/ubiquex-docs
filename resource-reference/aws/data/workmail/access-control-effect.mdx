---
title: "aws_workmail_access_control_effect"
description: "`aws_workmail_access_control_effect` reads the real, computed access control effect (allow or deny) WorkMail applies for a given request context."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmail_access_control_effect"})

    		ubx.Data(workmail.AccessControlEffect, "example", workmail.AccessControlEffectConfig{
    			Action:              "example",
    			IpAddress:           "example",
    			OrganizationId:      "example",
    			ImpersonationRoleId: "example",
    			UserId:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessControlEffect } from "@ubx/sdk-aws/aws/data/workmail/access_control_effect";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmail_access_control_effect" });

      data(AccessControlEffect, "example", {
        action: "example",
        ipAddress: "example",
        organizationId: "example",
        impersonationRoleId: "example",
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmail import AccessControlEffect, AccessControlEffectConfig

    def describe():
        ubx.intent("look up aws_workmail_access_control_effect")

        ubx.data(AccessControlEffect, "example", AccessControlEffectConfig(
            action="example",
            ip_address="example",
            organization_id="example",
            impersonation_role_id="example",
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action" type="string" required>
  The real action this object describes. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="impersonation_role_id" type="string">
  The real, unique identifier of the impersonation role this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ip_address" type="string" required>
  The real IP address this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="effect" type="string">
  The real effect (allow or deny) this policy statement applies. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="matched_rules" type="list(string)">
  The real rules for the matched. **(AI-inferred)** Returned by the lookup.
</ResponseField>
