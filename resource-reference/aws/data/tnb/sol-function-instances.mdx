---
title: "aws_tnb_sol_function_instances"
description: "`aws_tnb_sol_function_instances` reads the real network function instances deployed through Telco Network Builder."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tnb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tnb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tnb_sol_function_instances"})

    		ubx.Data(tnb.SolFunctionInstances, "example", tnb.SolFunctionInstancesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SolFunctionInstances } from "@ubx/sdk-aws/aws/data/tnb/sol_function_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_tnb_sol_function_instances" });

      data(SolFunctionInstances, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tnb import SolFunctionInstances, SolFunctionInstancesConfig

    def describe():
        ubx.intent("look up aws_tnb_sol_function_instances")

        ubx.data(SolFunctionInstances, "example", SolFunctionInstancesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="function_instances" type="list(object)">
  The real function instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
