---
title: "aws_tnb_sol_function_instance"
description: "`aws_tnb_sol_function_instance` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_tnb_sol_function_instance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tnb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/tnb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tnb_sol_function_instance"})

    		ubx.Data(tnb.SolFunctionInstance, "example", tnb.SolFunctionInstanceConfig{
    			VnfInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SolFunctionInstance } from "@ubx/sdk-aws/aws/data/tnb/sol_function_instance";

    export default stack("example", () => {
      intent({ summary: "look up aws_tnb_sol_function_instance" });

      data(SolFunctionInstance, "example", {
        vnfInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tnb import SolFunctionInstance, SolFunctionInstanceConfig

    def describe():
        ubx.intent("look up aws_tnb_sol_function_instance")

        ubx.data(SolFunctionInstance, "example", SolFunctionInstanceConfig(
            vnf_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="vnf_instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="instantiated_vnf_info" type="object">
  &lt;p&gt;Information about the network function.&lt;/p&gt; &lt;p&gt;A network function instance is a function in a function package .&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="vnf_state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vnfc_resource_info" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instantiation_state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="object">
  &lt;p&gt;The metadata of a network function instance.&lt;/p&gt; &lt;p&gt;A network function instance is a function in a function package .&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_at" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="last_modified" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ns_instance_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vnf_pkg_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vnf_product_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vnf_provider" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vnfd_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vnfd_version" type="string">
  Returned by the lookup.
</ResponseField>
