---
title: "aws_tnb_sol_network_package_content"
description: "`aws_tnb_sol_network_package_content` reads the real content of a Telco Network Builder network package."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tnb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/tnb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_tnb_sol_network_package_content"})

    		ubx.Data(tnb.SolNetworkPackageContent, "example", tnb.SolNetworkPackageContentConfig{
    			Accept:    "example",
    			NsdInfoId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SolNetworkPackageContent } from "@ubx/sdk-aws/aws/data/tnb/sol_network_package_content";

    export default stack("example", () => {
      intent({ summary: "look up aws_tnb_sol_network_package_content" });

      data(SolNetworkPackageContent, "example", {
        accept: "example",
        nsdInfoId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.tnb import SolNetworkPackageContent, SolNetworkPackageContentConfig

    def describe():
        ubx.intent("look up aws_tnb_sol_network_package_content")

        ubx.data(SolNetworkPackageContent, "example", SolNetworkPackageContentConfig(
            accept="example",
            nsd_info_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept" type="string" required>
  The real Accept header value sent with this request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="nsd_info_id" type="string" required>
  The real, unique identifier of the nsd info this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="nsd_content" type="string">
  The real content of the nsd. **(AI-inferred)** Returned by the lookup.
</ResponseField>
