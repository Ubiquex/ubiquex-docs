---
title: "aws_mturk_requester_review_policy_results_for_hit"
description: "`aws_mturk_requester_review_policy_results_for_hit` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mturk_requester_review_policy_results_for_hit` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_review_policy_results_for_hit"})

    		ubx.Data(mturk_requester.MturkRequesterReviewPolicyResultsForHit, "example", mturk_requester.MturkRequesterReviewPolicyResultsForHitConfig{
    			Hitid:           "example",
    			MaxResults:      1,
    			PolicyLevels:    []string{"example"},
    			RetrieveActions: true,
    			RetrieveResults: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterReviewPolicyResultsForHit } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_review_policy_results_for_hit";

    export default stack("example", () => {
      intent({
        summary: "look up aws_mturk_requester_review_policy_results_for_hit",
      });

      data(MturkRequesterReviewPolicyResultsForHit, "example", {
        hitid: "example",
        maxResults: 1,
        policyLevels: ["example"],
        retrieveActions: true,
        retrieveResults: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterReviewPolicyResultsForHit, MturkRequesterReviewPolicyResultsForHitConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_review_policy_results_for_hit")

        ubx.data(MturkRequesterReviewPolicyResultsForHit, "example", MturkRequesterReviewPolicyResultsForHitConfig(
            hitid="example",
            max_results=1,
            policy_levels=["example"],
            retrieve_actions=True,
            retrieve_results=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="hitid" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="policy_levels" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="retrieve_actions" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="retrieve_results" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assignment_review_policy" type="object">
  &lt;p&gt; HIT Review Policy data structures represent HIT review policies, which you specify when you create a HIT. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="assignment_review_report" type="object">
  &lt;p&gt; Contains both ReviewResult and ReviewAction elements for a particular HIT. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="review_actions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="review_results" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hitreview_policy" type="object">
  &lt;p&gt; HIT Review Policy data structures represent HIT review policies, which you specify when you create a HIT. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="policy_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hitreview_report" type="object">
  &lt;p&gt; Contains both ReviewResult and ReviewAction elements for a particular HIT. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="review_actions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="review_results" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>
