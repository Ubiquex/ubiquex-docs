---
title: "aws_mturk_requester_qualification_types"
description: "`aws_mturk_requester_qualification_types` looks up a real Mechanical Turk qualification type, used to restrict which workers can accept a HIT."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_qualification_types"})

    		ubx.Data(mturk_requester.MturkRequesterQualificationTypes, "example", mturk_requester.MturkRequesterQualificationTypesConfig{
    			MustBeRequestable:   true,
    			MaxResults:          1,
    			MustBeOwnedByCaller: true,
    			Query:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterQualificationTypes } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_qualification_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_qualification_types" });

      data(MturkRequesterQualificationTypes, "example", {
        mustBeRequestable: true,
        maxResults: 1,
        mustBeOwnedByCaller: true,
        query: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterQualificationTypes, MturkRequesterQualificationTypesConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_qualification_types")

        ubx.data(MturkRequesterQualificationTypes, "example", MturkRequesterQualificationTypesConfig(
            must_be_requestable=True,
            max_results=1,
            must_be_owned_by_caller=True,
            query="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="must_be_owned_by_caller" type="bool">
  Whether results are restricted to objects owned by the caller. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="must_be_requestable" type="bool" required>
  Whether results are restricted to objects that can be requested. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  The real query this request runs. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  The real number of results to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="qualification_types" type="list(object)">
  The real, discriminating types of qualifications this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
