---
title: "aws_mturk_requester_assignments_for_hit"
description: "`aws_mturk_requester_assignments_for_hit` reads the real assignments submitted for a Mechanical Turk HIT."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_assignments_for_hit"})

    		ubx.Data(mturk_requester.MturkRequesterAssignmentsForHit, "example", mturk_requester.MturkRequesterAssignmentsForHitConfig{
    			Hitid:              "example",
    			AssignmentStatuses: []string{"example"},
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MturkRequesterAssignmentsForHit } from "./local-sdk/aws/sdk/typescript/aws/data/mturk_requester/mturk_requester_assignments_for_hit.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_assignments_for_hit" });

      data(MturkRequesterAssignmentsForHit, "example", {
        hitid: "example",
        assignmentStatuses: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterAssignmentsForHit, MturkRequesterAssignmentsForHitConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_assignments_for_hit")

        ubx.data(MturkRequesterAssignmentsForHit, "example", MturkRequesterAssignmentsForHitConfig(
            hitid="example",
            assignment_statuses=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assignment_statuses" type="list(string)">
  The real, current statuses of the assignments included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="hitid" type="string" required>
  The real, unique identifier of the Amazon Mechanical Turk HIT this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assignments" type="list(object)">
  The real assignments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  The real number of results to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>
