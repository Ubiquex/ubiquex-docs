---
title: "aws_mturk_requester_hits_for_qualification_type"
description: "`aws_mturk_requester_hits_for_qualification_type` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mturk_requester_hits_for_qualification_type` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_hits_for_qualification_type"})

    		ubx.Data(mturk_requester.MturkRequesterHitsForQualificationType, "example", mturk_requester.MturkRequesterHitsForQualificationTypeConfig{
    			QualificationTypeId: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterHitsForQualificationType } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_hits_for_qualification_type";

    export default stack("example", () => {
      intent({
        summary: "look up aws_mturk_requester_hits_for_qualification_type",
      });

      data(MturkRequesterHitsForQualificationType, "example", {
        qualificationTypeId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterHitsForQualificationType, MturkRequesterHitsForQualificationTypeConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_hits_for_qualification_type")

        ubx.data(MturkRequesterHitsForQualificationType, "example", MturkRequesterHitsForQualificationTypeConfig(
            qualification_type_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="qualification_type_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hits" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  Returned by the lookup.
</ResponseField>
