---
title: "aws_mturk_requester_qualification_requests"
description: "`aws_mturk_requester_qualification_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mturk_requester_qualification_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_qualification_requests"})

    		ubx.Data(mturk_requester.MturkRequesterQualificationRequests, "example", mturk_requester.MturkRequesterQualificationRequestsConfig{
    			MaxResults:          1,
    			QualificationTypeId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterQualificationRequests } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_qualification_requests";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_qualification_requests" });

      data(MturkRequesterQualificationRequests, "example", {
        maxResults: 1,
        qualificationTypeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterQualificationRequests, MturkRequesterQualificationRequestsConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_qualification_requests")

        ubx.data(MturkRequesterQualificationRequests, "example", MturkRequesterQualificationRequestsConfig(
            max_results=1,
            qualification_type_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="qualification_type_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="qualification_requests" type="list(object)">
  Returned by the lookup.
</ResponseField>
