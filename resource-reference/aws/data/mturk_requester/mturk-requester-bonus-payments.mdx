---
title: "aws_mturk_requester_bonus_payments"
description: "`aws_mturk_requester_bonus_payments` reads the real bonus payments issued to a Mechanical Turk worker."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_bonus_payments"})

    		ubx.Data(mturk_requester.MturkRequesterBonusPayments, "example", mturk_requester.MturkRequesterBonusPaymentsConfig{
    			AssignmentId: "example",
    			Hitid:        "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterBonusPayments } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_bonus_payments";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_bonus_payments" });

      data(MturkRequesterBonusPayments, "example", {
        assignmentId: "example",
        hitid: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterBonusPayments, MturkRequesterBonusPaymentsConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_bonus_payments")

        ubx.data(MturkRequesterBonusPayments, "example", MturkRequesterBonusPaymentsConfig(
            assignment_id="example",
            hitid="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assignment_id" type="string">
  The real, unique identifier of the assignment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="hitid" type="string">
  The real, unique identifier of the Amazon Mechanical Turk HIT this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bonus_payments" type="list(object)">
  The real Amazon Mechanical Turk bonus payments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  The real number of results to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>
