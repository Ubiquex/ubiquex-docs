---
title: "aws_mturk_requester_reviewable_hits"
description: "`aws_mturk_requester_reviewable_hits` reads the real Mechanical Turk HITs whose own assignments are ready for review."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_reviewable_hits"})

    		ubx.Data(mturk_requester.MturkRequesterReviewableHits, "example", mturk_requester.MturkRequesterReviewableHitsConfig{
    			HittypeId:  "example",
    			MaxResults: 1,
    			Status:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterReviewableHits } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_reviewable_hits";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_reviewable_hits" });

      data(MturkRequesterReviewableHits, "example", {
        hittypeId: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterReviewableHits, MturkRequesterReviewableHitsConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_reviewable_hits")

        ubx.data(MturkRequesterReviewableHits, "example", MturkRequesterReviewableHitsConfig(
            hittype_id="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="hittype_id" type="string">
  The real, unique identifier of the hittype this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hits" type="list(object)">
  The real, matching search results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;If the previous response was incomplete (because there is more data to retrieve), Amazon Mechanical Turk returns a pagination token in the response. You can use this pagination token to retrieve the next set of results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="num_results" type="number">
  The real number of results to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>
