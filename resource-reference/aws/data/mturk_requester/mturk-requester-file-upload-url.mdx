---
title: "aws_mturk_requester_file_upload_url"
description: "`aws_mturk_requester_file_upload_url` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mturk_requester_file_upload_url` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_file_upload_url"})

    		ubx.Data(mturk_requester.MturkRequesterFileUploadUrl, "example", mturk_requester.MturkRequesterFileUploadUrlConfig{
    			AssignmentId:       "example",
    			QuestionIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterFileUploadUrl } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_file_upload_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_file_upload_url" });

      data(MturkRequesterFileUploadUrl, "example", {
        assignmentId: "example",
        questionIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterFileUploadUrl, MturkRequesterFileUploadUrlConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_file_upload_url")

        ubx.data(MturkRequesterFileUploadUrl, "example", MturkRequesterFileUploadUrlConfig(
            assignment_id="example",
            question_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assignment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="question_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="file_upload_url" type="string">
  Returned by the lookup.
</ResponseField>
