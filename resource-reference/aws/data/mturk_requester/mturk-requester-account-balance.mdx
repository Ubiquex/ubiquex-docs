---
title: "aws_mturk_requester_account_balance"
description: "`aws_mturk_requester_account_balance` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_mturk_requester_account_balance` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mturk_requester "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mturk_requester"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mturk_requester_account_balance"})

    		ubx.Data(mturk_requester.MturkRequesterAccountBalance, "example", mturk_requester.MturkRequesterAccountBalanceConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MturkRequesterAccountBalance } from "@ubx/sdk-aws/aws/data/mturk_requester/mturk_requester_account_balance";

    export default stack("example", () => {
      intent({ summary: "look up aws_mturk_requester_account_balance" });

      data(MturkRequesterAccountBalance, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mturk_requester import MturkRequesterAccountBalance, MturkRequesterAccountBalanceConfig

    def describe():
        ubx.intent("look up aws_mturk_requester_account_balance")

        ubx.data(MturkRequesterAccountBalance, "example", MturkRequesterAccountBalanceConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="available_balance" type="string">
  &lt;p&gt;A string representing a currency amount.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="on_hold_balance" type="string">
  &lt;p&gt;A string representing a currency amount.&lt;/p&gt; Returned by the lookup.
</ResponseField>
