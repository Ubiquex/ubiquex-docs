---
title: "aws_featurestore_runtime_sagemaker_record"
description: "`aws_featurestore_runtime_sagemaker_record` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_featurestore_runtime_sagemaker_record` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	featurestore_runtime_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/featurestore_runtime_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_featurestore_runtime_sagemaker_record"})

    		ubx.Data(featurestore_runtime_sagemaker.FeaturestoreRuntimeSagemakerRecord, "example", featurestore_runtime_sagemaker.FeaturestoreRuntimeSagemakerRecordConfig{
    			FeatureGroupName:              "example-feature-group-name",
    			RecordIdentifierValueAsString: "example",
    			ExpirationTimeResponse:        "example",
    			FeatureNames:                  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FeaturestoreRuntimeSagemakerRecord } from "@ubx/sdk-aws/aws/data/featurestore_runtime_sagemaker/featurestore_runtime_sagemaker_record";

    export default stack("example", () => {
      intent({ summary: "look up aws_featurestore_runtime_sagemaker_record" });

      data(FeaturestoreRuntimeSagemakerRecord, "example", {
        featureGroupName: "example-feature-group-name",
        recordIdentifierValueAsString: "example",
        expirationTimeResponse: "example",
        featureNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.featurestore_runtime_sagemaker import FeaturestoreRuntimeSagemakerRecord, FeaturestoreRuntimeSagemakerRecordConfig

    def describe():
        ubx.intent("look up aws_featurestore_runtime_sagemaker_record")

        ubx.data(FeaturestoreRuntimeSagemakerRecord, "example", FeaturestoreRuntimeSagemakerRecordConfig(
            feature_group_name="example-feature-group-name",
            record_identifier_value_as_string="example",
            expiration_time_response="example",
            feature_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="expiration_time_response" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="feature_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="feature_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="record_identifier_value_as_string" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expires_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="record" type="list(object)">
  Returned by the lookup.
</ResponseField>
