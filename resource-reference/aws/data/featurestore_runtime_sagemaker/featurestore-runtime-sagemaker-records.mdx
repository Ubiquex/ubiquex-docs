---
title: "aws_featurestore_runtime_sagemaker_records"
description: "`aws_featurestore_runtime_sagemaker_records` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_featurestore_runtime_sagemaker_records` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	featurestore_runtime_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/featurestore_runtime_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_featurestore_runtime_sagemaker_records"})

    		ubx.Data(featurestore_runtime_sagemaker.FeaturestoreRuntimeSagemakerRecords, "example", featurestore_runtime_sagemaker.FeaturestoreRuntimeSagemakerRecordsConfig{
    			FeatureGroupName:          "example-feature-group-name",
    			IncludeSoftDeletedRecords: true,
    			MaxResults:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FeaturestoreRuntimeSagemakerRecords } from "@ubx/sdk-aws/aws/data/featurestore_runtime_sagemaker/featurestore_runtime_sagemaker_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_featurestore_runtime_sagemaker_records" });

      data(FeaturestoreRuntimeSagemakerRecords, "example", {
        featureGroupName: "example-feature-group-name",
        includeSoftDeletedRecords: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.featurestore_runtime_sagemaker import FeaturestoreRuntimeSagemakerRecords, FeaturestoreRuntimeSagemakerRecordsConfig

    def describe():
        ubx.intent("look up aws_featurestore_runtime_sagemaker_records")

        ubx.data(FeaturestoreRuntimeSagemakerRecords, "example", FeaturestoreRuntimeSagemakerRecordsConfig(
            feature_group_name="example-feature-group-name",
            include_soft_deleted_records=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="feature_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="include_soft_deleted_records" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="record_identifiers" type="list(string)">
  Returned by the lookup.
</ResponseField>
