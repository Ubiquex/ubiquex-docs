---
title: "aws_translate_parallel_data"
description: "`data_aws_translate_parallel_data` looks up a real, already-existing `aws_translate_parallel_data`."
---

It imports a real set of source-and-target sentence pairs that Amazon Translate uses to customize its own translation output, favoring an organization's preferred terminology and phrasing over the engine's generic default translations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	translate "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/translate"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_translate_parallel_data"})

    		ubx.Data(translate.ParallelData, "example", translate.ParallelDataConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ParallelData } from "@ubx/sdk-aws/aws/data/translate/parallel_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_translate_parallel_data" });

      data(ParallelData, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.translate import ParallelData, ParallelDataConfig

    def describe():
        ubx.intent("look up aws_translate_parallel_data")

        ubx.data(ParallelData, "example", ParallelDataConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parallel_data_properties_list" type="list(object)">
  The real parallel data propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
