---
title: "aws_translate_terminologies"
description: "`aws_translate_terminologies` reads the real, custom Translate terminologies on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	translate "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/translate"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_translate_terminologies"})

    		ubx.Data(translate.Terminologies, "example", translate.TerminologiesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Terminologies } from "@ubx/sdk-aws/aws/data/translate/terminologies";

    export default stack("example", () => {
      intent({ summary: "look up aws_translate_terminologies" });

      data(Terminologies, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.translate import Terminologies, TerminologiesConfig

    def describe():
        ubx.intent("look up aws_translate_terminologies")

        ubx.data(Terminologies, "example", TerminologiesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="terminology_properties_list" type="list(object)">
  The real terminology propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
