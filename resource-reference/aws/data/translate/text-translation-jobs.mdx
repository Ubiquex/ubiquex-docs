---
title: "aws_translate_text_translation_jobs"
description: "`aws_translate_text_translation_jobs` reads the real asynchronous batch translation jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	translate "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/translate"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_translate_text_translation_jobs"})

    		ubx.Data(translate.TextTranslationJobs, "example", translate.TextTranslationJobsConfig{
    			Filter:     map[string]any{"job_name": "example-job-name"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TextTranslationJobs } from "@ubx/sdk-aws/aws/data/translate/text_translation_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_translate_text_translation_jobs" });

      data(TextTranslationJobs, "example", {
        filter: { jobName: "example-job-name" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.translate import TextTranslationJobs, TextTranslationJobsConfig
    from ubx.aws.data.translate.text_translation_jobs import TextTranslationJobs_Filter

    def describe():
        ubx.intent("look up aws_translate_text_translation_jobs")

        ubx.data(TextTranslationJobs, "example", TextTranslationJobsConfig(
            filter=TextTranslationJobs_Filter(job_name="example-job-name"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Provides information for filtering a list of translation jobs. For more information, see &lt;a&gt;ListTextTranslationJobs&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submitted_after_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submitted_before_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="text_translation_job_properties_list" type="list(object)">
  The real text translation job propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
