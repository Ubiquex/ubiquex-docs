---
title: "aws_sdb_export"
description: "`aws_sdb_export` reads the real, current status of exporting a SimpleDB domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sdb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sdb_export"})

    		ubx.Data(sdb.Export, "example", sdb.ExportConfig{
    			ExportArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Export } from "./local-sdk/aws/sdk/typescript/aws/data/sdb/export.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sdb_export" });

      data(Export, "example", {
        exportArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sdb import Export, ExportConfig

    def describe():
        ubx.intent("look up aws_sdb_export")

        ubx.data(Export, "example", ExportConfig(
            export_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="export_arn" type="string" required>
  A unique ARN identifier for the export. Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="client_token" type="string">
  A real, caller-supplied token guaranteeing this request's own idempotency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="domain_name" type="string">
  The domain name that uniquely identifies a SimpleDB domain within your account. Returned by the lookup.
</ResponseField>

<ResponseField name="export_data_cutoff_time" type="string">
  The real timestamp of the export data cutoff. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="export_manifest" type="string">
  The real, structured manifest describing the exported data. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="export_status" type="string">
  The current state of the export. Current possible values include : PENDING - export request received, IN_PROGRESS - export is being processed, SUCCEEDED - export completed successfully, and FAILED - export encountered an error. Returned by the lookup.
</ResponseField>

<ResponseField name="failure_code" type="string">
  The real code identifying the reason for the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_message" type="string">
  The real, human-readable message describing the failure. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="items_count" type="number">
  The real count of itemss. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="requested_at" type="string">
  Timestamp when the export (or any other operation) was requested. Returned by the lookup.
</ResponseField>

<ResponseField name="s3_bucket" type="string">
  The real S3 bucket used by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_bucket_owner" type="string">
  The real AWS account that owns this S3 bucket. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  The real S3 key prefix used by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_sse_algorithm" type="string">
  The real S3 server-side encryption algorithm used for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="s3_sse_kms_key_id" type="string">
  The real, unique identifier of the s3 sse kms key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
