---
title: "aws_healthlake_fhirexport_jobs"
description: "`aws_healthlake_fhirexport_jobs` reads the real HealthLake FHIR export jobs run for a data store."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	healthlake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/healthlake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_healthlake_fhirexport_jobs"})

    		ubx.Data(healthlake.FhirexportJobs, "example", healthlake.FhirexportJobsConfig{
    			DatastoreId:     "example",
    			JobName:         "example-job-name",
    			JobStatus:       "example",
    			MaxResults:      1,
    			SubmittedAfter:  "example",
    			SubmittedBefore: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FhirexportJobs } from "./local-sdk/aws/sdk/typescript/aws/data/healthlake/fhirexport_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_healthlake_fhirexport_jobs" });

      data(FhirexportJobs, "example", {
        datastoreId: "example",
        jobName: "example-job-name",
        jobStatus: "example",
        maxResults: 1,
        submittedAfter: "example",
        submittedBefore: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.healthlake import FhirexportJobs, FhirexportJobsConfig

    def describe():
        ubx.intent("look up aws_healthlake_fhirexport_jobs")

        ubx.data(FhirexportJobs, "example", FhirexportJobsConfig(
            datastore_id="example",
            job_name="example-job-name",
            job_status="example",
            max_results=1,
            submitted_after="example",
            submitted_before="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  The identifier of a data store. Required lookup argument.
</ResponseField>

<ResponseField name="job_name" type="string">
  The human-readable name of a job. Optional lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  The status of a job. Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return per page. Optional lookup argument.
</ResponseField>

<ResponseField name="submitted_after" type="string">
  A timestamp value. Optional lookup argument.
</ResponseField>

<ResponseField name="submitted_before" type="string">
  A timestamp value. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_job_properties_list" type="list(object)">
  The real export job propertiess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token. Returned by the lookup.
</ResponseField>
