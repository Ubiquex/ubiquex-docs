---
title: "aws_healthlake_data_transformation_profiles"
description: "`aws_healthlake_data_transformation_profiles` looks up a real HealthLake data transformation profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthlake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/healthlake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_healthlake_data_transformation_profiles"})

    		ubx.Data(healthlake.DataTransformationProfiles, "example", healthlake.DataTransformationProfilesConfig{
    			SourceFormat: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataTransformationProfiles } from "@ubx/sdk-aws/aws/data/healthlake/data_transformation_profiles";

    export default stack("example", () => {
      intent({ summary: "look up aws_healthlake_data_transformation_profiles" });

      data(DataTransformationProfiles, "example", {
        sourceFormat: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.healthlake import DataTransformationProfiles, DataTransformationProfilesConfig

    def describe():
        ubx.intent("look up aws_healthlake_data_transformation_profiles")

        ubx.data(DataTransformationProfiles, "example", DataTransformationProfilesConfig(
            source_format="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_format" type="string" required>
  The real format of the source data. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
