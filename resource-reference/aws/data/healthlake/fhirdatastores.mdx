---
title: "aws_healthlake_fhirdatastores"
description: "`aws_healthlake_fhirdatastores` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_healthlake_fhirdatastores` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthlake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/healthlake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_healthlake_fhirdatastores"})

    		ubx.Data(healthlake.Fhirdatastores, "example", healthlake.FhirdatastoresConfig{
    			Filter:     map[string]any{"created_after": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Fhirdatastores } from "@ubx/sdk-aws/aws/data/healthlake/fhirdatastores";

    export default stack("example", () => {
      intent({ summary: "look up aws_healthlake_fhirdatastores" });

      data(Fhirdatastores, "example", {
        filter: { createdAfter: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.healthlake import Fhirdatastores, FhirdatastoresConfig

    def describe():
        ubx.intent("look up aws_healthlake_fhirdatastores")

        ubx.data(Fhirdatastores, "example", FhirdatastoresConfig(
            filter={"created_after": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;The filters applied to a data store query.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="created_after" type="string">
      A timestamp value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_before" type="string">
      A timestamp value. Optional lookup argument.
    </ResponseField>
    <ResponseField name="datastore_name" type="string">
      The name of a data store. Optional lookup argument.
    </ResponseField>
    <ResponseField name="datastore_status" type="string">
      The status of a data store. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return per page. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="datastore_properties_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token. Returned by the lookup.
</ResponseField>
