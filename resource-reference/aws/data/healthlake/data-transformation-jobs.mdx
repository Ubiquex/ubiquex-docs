---
title: "aws_healthlake_data_transformation_jobs"
description: "`aws_healthlake_data_transformation_jobs` reads the real HealthLake data transformation jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	healthlake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/healthlake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_healthlake_data_transformation_jobs"})

    		ubx.Data(healthlake.DataTransformationJobs, "example", healthlake.DataTransformationJobsConfig{
    			JobName:         "example-job-name",
    			JobStatus:       "example",
    			MaxResults:      1,
    			SubmittedAfter:  "example",
    			SubmittedBefore: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataTransformationJobs } from "./local-sdk/aws/sdk/typescript/aws/data/healthlake/data_transformation_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_healthlake_data_transformation_jobs" });

      data(DataTransformationJobs, "example", {
        jobName: "example-job-name",
        jobStatus: "example",
        maxResults: 1,
        submittedAfter: "example",
        submittedBefore: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.healthlake import DataTransformationJobs, DataTransformationJobsConfig

    def describe():
        ubx.intent("look up aws_healthlake_data_transformation_jobs")

        ubx.data(DataTransformationJobs, "example", DataTransformationJobsConfig(
            job_name="example-job-name",
            job_status="example",
            max_results=1,
            submitted_after="example",
            submitted_before="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_name" type="string">
  The real name of the job this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="submitted_after" type="string">
  Restricts results to objects submitted after this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="submitted_before" type="string">
  Restricts results to objects submitted before this timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
