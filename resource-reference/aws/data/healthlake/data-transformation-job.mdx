---
title: "aws_healthlake_data_transformation_job"
description: "`aws_healthlake_data_transformation_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_healthlake_data_transformation_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	healthlake "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/healthlake"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_healthlake_data_transformation_job"})

    		ubx.Data(healthlake.DataTransformationJob, "example", healthlake.DataTransformationJobConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataTransformationJob } from "@ubx/sdk-aws/aws/data/healthlake/data_transformation_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_healthlake_data_transformation_job" });

      data(DataTransformationJob, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.healthlake import DataTransformationJob, DataTransformationJobConfig

    def describe():
        ubx.intent("look up aws_healthlake_data_transformation_job")

        ubx.data(DataTransformationJob, "example", DataTransformationJobConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="transformation_job_properties" type="object">
  &lt;p&gt;Contains the properties of a data transformation job, including its status, configuration, and progress information. You retrieve this structure by calling &lt;code&gt;DescribeDataTransformationJob&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="data_access_role_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="drift_detection_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="input_data_config" type="object" required>
      &lt;p&gt;The Amazon S3 location and source format configuration for input data in a transformation job.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_uri" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="source_format" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="job_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="job_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="job_progress_report" type="object">
      &lt;p&gt;Contains progress metrics for a data transformation job, including counts of files scanned, converted, and failed.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_files_converted" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="total_files_failed" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="total_files_scanned" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="total_resources_generated" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="job_status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="output_data_config" type="object" required>
      &lt;p&gt;The Amazon S3 output location and encryption configuration for a transformation job.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_configuration" type="object" required>
          &lt;p&gt;The Amazon S3 output configuration for a data transformation job, including the output location and encryption settings.&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="kms_key_id" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="profile_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="profile_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="profile_version" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provenance_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="submit_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
