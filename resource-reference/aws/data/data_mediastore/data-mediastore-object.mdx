---
title: "aws_data_mediastore_object"
description: "`aws_data_mediastore_object` looks up one real object stored in a MediaStore container."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_mediastore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_mediastore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_mediastore_object"})

    		ubx.Data(data_mediastore.DataMediastoreObject, "example", data_mediastore.DataMediastoreObjectConfig{
    			Path: "/example/",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataMediastoreObject } from "@ubx/sdk-aws/aws/data/data_mediastore/data_mediastore_object";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_mediastore_object" });

      data(DataMediastoreObject, "example", {
        path: "/example/",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_mediastore import DataMediastoreObject, DataMediastoreObjectConfig

    def describe():
        ubx.intent("look up aws_data_mediastore_object")

        ubx.data(DataMediastoreObject, "example", DataMediastoreObjectConfig(
            path="/example/",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="path" type="string" required>
  The real path of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cache_control" type="string">
  The real Cache-Control header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_length" type="number">
  The real length of the content, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>
