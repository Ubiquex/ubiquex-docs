---
title: "aws_data_mediastore_items"
description: "`aws_data_mediastore_items` reads the real objects stored in a MediaStore container."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_mediastore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_mediastore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_mediastore_items"})

    		ubx.Data(data_mediastore.DataMediastoreItems, "example", data_mediastore.DataMediastoreItemsConfig{
    			MaxResults: 1,
    			Path:       "/example/",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataMediastoreItems } from "@ubx/sdk-aws/aws/data/data_mediastore/data_mediastore_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_mediastore_items" });

      data(DataMediastoreItems, "example", {
        maxResults: 1,
        path: "/example/",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_mediastore import DataMediastoreItems, DataMediastoreItemsConfig

    def describe():
        ubx.intent("look up aws_data_mediastore_items")

        ubx.data(DataMediastoreItems, "example", DataMediastoreItemsConfig(
            max_results=1,
            path="/example/",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path" type="string">
  The real path of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
