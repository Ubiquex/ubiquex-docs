---
title: "aws_drs_extensible_source_servers"
description: "`aws_drs_extensible_source_servers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_drs_extensible_source_servers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	drs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/drs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_drs_extensible_source_servers"})

    		ubx.Data(drs.ExtensibleSourceServers, "example", drs.ExtensibleSourceServersConfig{
    			StagingAccountId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExtensibleSourceServers } from "@ubx/sdk-aws/aws/data/drs/extensible_source_servers";

    export default stack("example", () => {
      intent({ summary: "look up aws_drs_extensible_source_servers" });

      data(ExtensibleSourceServers, "example", {
        stagingAccountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.drs import ExtensibleSourceServers, ExtensibleSourceServersConfig

    def describe():
        ubx.intent("look up aws_drs_extensible_source_servers")

        ubx.data(ExtensibleSourceServers, "example", ExtensibleSourceServersConfig(
            staging_account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="staging_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
