---
title: "aws_drs_recovery_plans"
description: "`aws_drs_recovery_plans` reads the real Elastic Disaster Recovery recovery plans on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	drs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/drs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_drs_recovery_plans"})

    		ubx.Data(drs.RecoveryPlans, "example", drs.RecoveryPlansConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RecoveryPlans } from "./local-sdk/aws/sdk/typescript/aws/data/drs/recovery_plans.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_drs_recovery_plans" });

      data(RecoveryPlans, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.drs import RecoveryPlans, RecoveryPlansConfig

    def describe():
        ubx.intent("look up aws_drs_recovery_plans")

        ubx.data(RecoveryPlans, "example", RecoveryPlansConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_plans" type="list(object)">
  The real recovery plans included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
