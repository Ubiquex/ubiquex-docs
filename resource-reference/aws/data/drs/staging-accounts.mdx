---
title: "aws_drs_staging_accounts"
description: "`aws_drs_staging_accounts` reads the real staging accounts registered for cross-account Elastic Disaster Recovery replication."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	drs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/drs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_drs_staging_accounts"})

    		ubx.Data(drs.StagingAccounts, "example", drs.StagingAccountsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StagingAccounts } from "@ubx/sdk-aws/aws/data/drs/staging_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_drs_staging_accounts" });

      data(StagingAccounts, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.drs import StagingAccounts, StagingAccountsConfig

    def describe():
        ubx.intent("look up aws_drs_staging_accounts")

        ubx.data(StagingAccounts, "example", StagingAccountsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accounts" type="list(object)">
  The real AWS accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
