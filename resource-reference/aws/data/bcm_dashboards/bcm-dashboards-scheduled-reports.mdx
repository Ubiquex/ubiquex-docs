---
title: "aws_bcm_dashboards_scheduled_reports"
description: "`aws_bcm_dashboards_scheduled_reports` looks up a real scheduled report generated from a Billing and Cost Management dashboard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bcm_dashboards "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/bcm_dashboards"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bcm_dashboards_scheduled_reports"})

    		ubx.Data(bcm_dashboards.BcmDashboardsScheduledReports, "example", bcm_dashboards.BcmDashboardsScheduledReportsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BcmDashboardsScheduledReports } from "@ubx/sdk-aws/aws/data/bcm_dashboards/bcm_dashboards_scheduled_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_bcm_dashboards_scheduled_reports" });

      data(BcmDashboardsScheduledReports, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bcm_dashboards import BcmDashboardsScheduledReports, BcmDashboardsScheduledReportsConfig

    def describe():
        ubx.intent("look up aws_bcm_dashboards_scheduled_reports")

        ubx.data(BcmDashboardsScheduledReports, "example", BcmDashboardsScheduledReportsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scheduled_reports" type="list(object)">
  The real reports for the scheduled. **(AI-inferred)** Returned by the lookup.
</ResponseField>
