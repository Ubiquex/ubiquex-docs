---
title: "aws_bcm_dashboards_resource_policy"
description: "`aws_bcm_dashboards_resource_policy` reads the real resource-based policy attached to a Billing and Cost Management dashboard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bcm_dashboards "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bcm_dashboards"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bcm_dashboards_resource_policy"})

    		ubx.Data(bcm_dashboards.BcmDashboardsResourcePolicy, "example", bcm_dashboards.BcmDashboardsResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BcmDashboardsResourcePolicy } from "@ubx/sdk-aws/aws/data/bcm_dashboards/bcm_dashboards_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_bcm_dashboards_resource_policy" });

      data(BcmDashboardsResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bcm_dashboards import BcmDashboardsResourcePolicy, BcmDashboardsResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_bcm_dashboards_resource_policy")

        ubx.data(BcmDashboardsResourcePolicy, "example", BcmDashboardsResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_document" type="string">
  The real, JSON-formatted policy document attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
