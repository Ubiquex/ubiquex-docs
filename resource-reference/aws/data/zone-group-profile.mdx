---
title: "aws_data_zone_group_profile"
description: "`aws_data_zone_group_profile` registers a real IAM Identity Center group as a recognized identity within an `aws_data_zone_domain`..."
---

`aws_data_zone_group_profile` registers a real IAM Identity Center group as a recognized identity within an `aws_data_zone_domain`, so group-based permissions and project membership can be managed the same way individual `aws_data_zone_user_profile`s already are.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own zone group profile"})

    		ubx.Resource(data.ZoneGroupProfile, "example", data.ZoneGroupProfileConfig{
    			DomainIdentifier: "example",
    			GroupIdentifier:  "example",
    			GroupType:        "example",
    			RolePrincipalArn: "arn:aws:iam::123456789012:policy/example",
    			Status:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneGroupProfile } from "./local-sdk/aws/sdk/typescript/aws/data/zone_group_profile.ts";

    export default stack("example", () => {
      intent({ summary: "example own zone group profile" });

      resource(ZoneGroupProfile, "example", {
        domainIdentifier: "example",
        groupIdentifier: "example",
        groupType: "example",
        rolePrincipalArn: "arn:aws:iam::123456789012:policy/example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import ZoneGroupProfile, ZoneGroupProfileConfig

    def describe():
        ubx.intent("example own zone group profile")

        ubx.resource(ZoneGroupProfile, "example", ZoneGroupProfileConfig(
            domain_identifier="example",
            group_identifier="example",
            group_type="example",
            role_principal_arn="arn:aws:iam::123456789012:policy/example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_group_profile, with domain identifier
    example, group identifier example, group type example, role
    principal arn arn:aws:iam::123456789012:policy/example, status
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_identifier" type="string" required>
  The identifier of the Amazon DataZone domain in which the group profile would be created. Required.
</ResponseField>

<ResponseField name="group_identifier" type="string">
  The ID of the group. Optional.
</ResponseField>

<ResponseField name="group_type" type="string">
  The type of the group. Optional.
</ResponseField>

<ResponseField name="role_principal_arn" type="string">
  The ARN of the role principal for the group profile. Optional.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the group profile. Optional.
</ResponseField>

## Output properties

<ResponseField name="domain_id" type="string">
  The identifier of the Amazon DataZone domain in which the group profile is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_name" type="string">
  The group-name of the Group Profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the Amazon DataZone group profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="role_principal_id" type="string">
  The ID of the role principal for the group profile. Computed by AWS after `ship`.
</ResponseField>
