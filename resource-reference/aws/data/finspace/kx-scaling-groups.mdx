---
title: "aws_finspace_kx_scaling_groups"
description: "`aws_finspace_kx_scaling_groups` looks up a real FinSpace kdb Insights scaling group, a shared compute pool clusters can run on."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	finspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_kx_scaling_groups"})

    		ubx.Data(finspace.KxScalingGroups, "example", finspace.KxScalingGroupsConfig{
    			EnvironmentId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { KxScalingGroups } from "./local-sdk/aws/sdk/typescript/aws/data/finspace/kx_scaling_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_kx_scaling_groups" });

      data(KxScalingGroups, "example", {
        environmentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.finspace import KxScalingGroups, KxScalingGroupsConfig

    def describe():
        ubx.intent("look up aws_finspace_kx_scaling_groups")

        ubx.data(KxScalingGroups, "example", KxScalingGroupsConfig(
            environment_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_groups" type="list(object)">
  The real scaling groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
