---
title: "aws_finspace_kx_volumes"
description: "`aws_finspace_kx_volumes` looks up a real FinSpace kdb Insights volume, shared network storage attached to a cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_kx_volumes"})

    		ubx.Data(finspace.KxVolumes, "example", finspace.KxVolumesConfig{
    			EnvironmentId: "example",
    			MaxResults:    1,
    			VolumeType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KxVolumes } from "@ubx/sdk-aws/aws/data/finspace/kx_volumes";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_kx_volumes" });

      data(KxVolumes, "example", {
        environmentId: "example",
        maxResults: 1,
        volumeType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace import KxVolumes, KxVolumesConfig

    def describe():
        ubx.intent("look up aws_finspace_kx_volumes")

        ubx.data(KxVolumes, "example", KxVolumesConfig(
            environment_id="example",
            max_results=1,
            volume_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="volume_type" type="string">
  The real, discriminating type of the volume this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kx_volume_summaries" type="list(object)">
  The real, summarized kx volumes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
