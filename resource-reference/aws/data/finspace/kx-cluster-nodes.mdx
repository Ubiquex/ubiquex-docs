---
title: "aws_finspace_kx_cluster_nodes"
description: "`aws_finspace_kx_cluster_nodes` reads the real compute nodes within a FinSpace kdb Insights cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	finspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_kx_cluster_nodes"})

    		ubx.Data(finspace.KxClusterNodes, "example", finspace.KxClusterNodesConfig{
    			ClusterName:   "example-cluster-name",
    			EnvironmentId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { KxClusterNodes } from "./local-sdk/aws/sdk/typescript/aws/data/finspace/kx_cluster_nodes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_kx_cluster_nodes" });

      data(KxClusterNodes, "example", {
        clusterName: "example-cluster-name",
        environmentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.finspace import KxClusterNodes, KxClusterNodesConfig

    def describe():
        ubx.intent("look up aws_finspace_kx_cluster_nodes")

        ubx.data(KxClusterNodes, "example", KxClusterNodesConfig(
            cluster_name="example-cluster-name",
            environment_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  The real name of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="nodes" type="list(object)">
  The real nodes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
