---
title: "aws_finspace_kx_connection_string"
description: "`aws_finspace_kx_connection_string` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_finspace_kx_connection_string` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_kx_connection_string"})

    		ubx.Data(finspace.KxConnectionString, "example", finspace.KxConnectionStringConfig{
    			ClusterName:   "example-cluster-name",
    			EnvironmentId: "example",
    			UserArn:       "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KxConnectionString } from "@ubx/sdk-aws/aws/data/finspace/kx_connection_string";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_kx_connection_string" });

      data(KxConnectionString, "example", {
        clusterName: "example-cluster-name",
        environmentId: "example",
        userArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace import KxConnectionString, KxConnectionStringConfig

    def describe():
        ubx.intent("look up aws_finspace_kx_connection_string")

        ubx.data(KxConnectionString, "example", KxConnectionStringConfig(
            cluster_name="example-cluster-name",
            environment_id="example",
            user_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="user_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="signed_connection_string" type="string">
  Returned by the lookup.
</ResponseField>
