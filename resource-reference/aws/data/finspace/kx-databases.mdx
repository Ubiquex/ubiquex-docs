---
title: "aws_finspace_kx_databases"
description: "`aws_finspace_kx_databases` looks up a real FinSpace kdb Insights database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/finspace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_kx_databases"})

    		ubx.Data(finspace.KxDatabases, "example", finspace.KxDatabasesConfig{
    			EnvironmentId: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KxDatabases } from "@ubx/sdk-aws/aws/data/finspace/kx_databases";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_kx_databases" });

      data(KxDatabases, "example", {
        environmentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace import KxDatabases, KxDatabasesConfig

    def describe():
        ubx.intent("look up aws_finspace_kx_databases")

        ubx.data(KxDatabases, "example", KxDatabasesConfig(
            environment_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_id" type="string" required>
  The real, unique identifier of the environment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kx_databases" type="list(object)">
  The real FinSpace kdb databases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
