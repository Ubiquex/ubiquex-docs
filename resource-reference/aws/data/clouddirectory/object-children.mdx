---
title: "aws_clouddirectory_object_children"
description: "`aws_clouddirectory_object_children` reads the real child objects of an object within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_object_children"})

    		ubx.Data(clouddirectory.ObjectChildren, "example", clouddirectory.ObjectChildrenConfig{
    			DirectoryArn:     "arn:aws:iam::123456789012:policy/example",
    			ObjectReference:  map[string]any{"selector": "example"},
    			ConsistencyLevel: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ObjectChildren } from "./local-sdk/aws/sdk/typescript/aws/data/clouddirectory/object_children.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_object_children" });

      data(ObjectChildren, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        consistencyLevel: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ObjectChildren, ObjectChildrenConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_object_children")

        ubx.data(ObjectChildren, "example", ObjectChildrenConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference={"selector": "example"},
            consistency_level="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  The real, requested level of read consistency for this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="children" type="map(string)">
  The real child objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
