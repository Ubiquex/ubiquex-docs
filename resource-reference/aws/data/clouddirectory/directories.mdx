---
title: "aws_clouddirectory_directories"
description: "`aws_clouddirectory_directories` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_clouddirectory_directories` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_directories"})

    		ubx.Data(clouddirectory.Directories, "example", clouddirectory.DirectoriesConfig{
    			MaxResults: 1,
    			State:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Directories } from "@ubx/sdk-aws/aws/data/clouddirectory/directories";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_directories" });

      data(Directories, "example", {
        maxResults: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import Directories, DirectoriesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_directories")

        ubx.data(Directories, "example", DirectoriesConfig(
            max_results=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="directories" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
