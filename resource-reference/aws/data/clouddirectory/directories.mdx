---
title: "aws_clouddirectory_directories"
description: "`aws_clouddirectory_directories` looks up a real Cloud Directory, a managed hierarchical data store for a directory structure."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_directories"})

    		ubx.Data(clouddirectory.Directories, "example", clouddirectory.DirectoriesConfig{
    			MaxResults: 1,
    			State:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Directories } from "@ubx/sdk-aws/aws/data/clouddirectory/directories";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_directories" });

      data(Directories, "example", {
        maxResults: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import Directories, DirectoriesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_directories")

        ubx.data(Directories, "example", DirectoriesConfig(
            max_results=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="directories" type="list(object)">
  The real directories included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
