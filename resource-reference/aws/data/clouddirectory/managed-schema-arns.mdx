---
title: "aws_clouddirectory_managed_schema_arns"
description: "`aws_clouddirectory_managed_schema_arns` reads the real, AWS-published managed schema ARNs available for use in Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_managed_schema_arns"})

    		ubx.Data(clouddirectory.ManagedSchemaArns, "example", clouddirectory.ManagedSchemaArnsConfig{
    			MaxResults: 1,
    			SchemaArn:  "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedSchemaArns } from "@ubx/sdk-aws/aws/data/clouddirectory/managed_schema_arns";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_managed_schema_arns" });

      data(ManagedSchemaArns, "example", {
        maxResults: 1,
        schemaArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ManagedSchemaArns, ManagedSchemaArnsConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_managed_schema_arns")

        ubx.data(ManagedSchemaArns, "example", ManagedSchemaArnsConfig(
            max_results=1,
            schema_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="schema_arn" type="string">
  The real Amazon Resource Name (ARN) of the schema this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the schemas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
