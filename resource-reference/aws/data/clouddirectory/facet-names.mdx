---
title: "aws_clouddirectory_facet_names"
description: "`aws_clouddirectory_facet_names` reads the real facet names defined within a Cloud Directory schema."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_facet_names"})

    		ubx.Data(clouddirectory.FacetNames, "example", clouddirectory.FacetNamesConfig{
    			SchemaArn:  "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FacetNames } from "@ubx/sdk-aws/aws/data/clouddirectory/facet_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_facet_names" });

      data(FacetNames, "example", {
        schemaArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import FacetNames, FacetNamesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_facet_names")

        ubx.data(FacetNames, "example", FacetNamesConfig(
            schema_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="schema_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="facet_names" type="list(string)">
  The real names of the facets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
