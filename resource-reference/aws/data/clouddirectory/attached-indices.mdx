---
title: "aws_clouddirectory_attached_indices"
description: "`aws_clouddirectory_attached_indices` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_clouddirectory_attached_indices` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_attached_indices"})

    		ubx.Data(clouddirectory.AttachedIndices, "example", clouddirectory.AttachedIndicesConfig{
    			DirectoryArn:     "arn:aws:iam::123456789012:policy/example",
    			TargetReference:  map[string]any{"selector": "example"},
    			ConsistencyLevel: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AttachedIndices } from "@ubx/sdk-aws/aws/data/clouddirectory/attached_indices";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_attached_indices" });

      data(AttachedIndices, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        targetReference: { selector: "example" },
        consistencyLevel: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import AttachedIndices, AttachedIndicesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_attached_indices")

        ubx.data(AttachedIndices, "example", AttachedIndicesConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            target_reference={"selector": "example"},
            consistency_level="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="index_attachments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
