---
title: "aws_clouddirectory_object_parent_paths"
description: "`aws_clouddirectory_object_parent_paths` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_clouddirectory_object_parent_paths` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_object_parent_paths"})

    		ubx.Data(clouddirectory.ObjectParentPaths, "example", clouddirectory.ObjectParentPathsConfig{
    			DirectoryArn:    "arn:aws:iam::123456789012:policy/example",
    			ObjectReference: map[string]any{"selector": "example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectParentPaths } from "@ubx/sdk-aws/aws/data/clouddirectory/object_parent_paths";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_object_parent_paths" });

      data(ObjectParentPaths, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ObjectParentPaths, ObjectParentPathsConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_object_parent_paths")

        ubx.data(ObjectParentPaths, "example", ObjectParentPathsConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference={"selector": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="directory_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="path_to_object_identifiers_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
