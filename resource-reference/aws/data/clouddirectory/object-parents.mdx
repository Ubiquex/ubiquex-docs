---
title: "aws_clouddirectory_object_parents"
description: "`aws_clouddirectory_object_parents` reads the real parent objects of an object within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_object_parents"})

    		ubx.Data(clouddirectory.ObjectParents, "example", clouddirectory.ObjectParentsConfig{
    			DirectoryArn:                "arn:aws:iam::123456789012:policy/example",
    			ObjectReference:             map[string]any{"selector": "example"},
    			ConsistencyLevel:            "example",
    			IncludeAllLinksToEachParent: true,
    			MaxResults:                  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectParents } from "@ubx/sdk-aws/aws/data/clouddirectory/object_parents";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_object_parents" });

      data(ObjectParents, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        consistencyLevel: "example",
        includeAllLinksToEachParent: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ObjectParents, ObjectParentsConfig
    from ubx.aws.data.clouddirectory.object_parents import ObjectParents_ObjectReference

    def describe():
        ubx.intent("look up aws_clouddirectory_object_parents")

        ubx.data(ObjectParents, "example", ObjectParentsConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference=ObjectParents_ObjectReference(selector="example"),
            consistency_level="example",
            include_all_links_to_each_parent=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  The real, requested level of read consistency for this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_all_links_to_each_parent" type="bool">
  Whether every real link to each parent is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parent_links" type="list(object)">
  The real links to the parent objects of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parents" type="map(string)">
  The real parent objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
