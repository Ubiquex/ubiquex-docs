---
title: "aws_clouddirectory_typed_link_facet_attributes"
description: "`aws_clouddirectory_typed_link_facet_attributes` reads the real attributes defined on a Cloud Directory typed link facet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_typed_link_facet_attributes"})

    		ubx.Data(clouddirectory.TypedLinkFacetAttributes, "example", clouddirectory.TypedLinkFacetAttributesConfig{
    			Name:       "example",
    			SchemaArn:  "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TypedLinkFacetAttributes } from "@ubx/sdk-aws/aws/data/clouddirectory/typed_link_facet_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_typed_link_facet_attributes" });

      data(TypedLinkFacetAttributes, "example", {
        name: "example",
        schemaArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import TypedLinkFacetAttributes, TypedLinkFacetAttributesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_typed_link_facet_attributes")

        ubx.data(TypedLinkFacetAttributes, "example", TypedLinkFacetAttributesConfig(
            name="example",
            schema_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="schema_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attributes" type="list(object)">
  The real, structured attributes of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
