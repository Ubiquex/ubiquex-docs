---
title: "aws_clouddirectory_link_attributes"
description: "`aws_clouddirectory_link_attributes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_clouddirectory_link_attributes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_link_attributes"})

    		ubx.Data(clouddirectory.LinkAttributes, "example", clouddirectory.LinkAttributesConfig{
    			AttributeNames:     []string{"example"},
    			DirectoryArn:       "arn:aws:iam::123456789012:policy/example",
    			TypedLinkSpecifier: map[string]any{"identity_attribute_values": []map[string]any{{"attribute_name": "example-attribute-name"}}},
    			ConsistencyLevel:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LinkAttributes } from "@ubx/sdk-aws/aws/data/clouddirectory/link_attributes";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_link_attributes" });

      data(LinkAttributes, "example", {
        attributeNames: ["example"],
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        typedLinkSpecifier: {
          identityAttributeValues: [{ attributeName: "example-attribute-name" }],
        },
        consistencyLevel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import LinkAttributes, LinkAttributesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_link_attributes")

        ubx.data(LinkAttributes, "example", LinkAttributesConfig(
            attribute_names=["example"],
            directory_arn="arn:aws:iam::123456789012:policy/example",
            typed_link_specifier={"identity_attribute_values": [{"attribute_name": "example-attribute-name"}]},
            consistency_level="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attribute_names" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="consistency_level" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="typed_link_specifier" type="object" required>
  &lt;p&gt;Contains all the information that is used to uniquely identify a typed link. The parameters discussed in this topic are used to uniquely specify the typed link being operated on. The &lt;a&gt;AttachTypedLink&lt;/a&gt; API returns a typed link specifier while the &lt;a&gt;DetachTypedLink&lt;/a&gt; API accepts one as input. Similarly, the &lt;a&gt;ListIncomingTypedLinks&lt;/a&gt; and &lt;a&gt;ListOutgoingTypedLinks&lt;/a&gt; API operations provide typed link specifiers as output. You can also construct a typed link specifier from scratch.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="identity_attribute_values" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="source_object_reference" type="object" required>
      &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="selector" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="target_object_reference" type="object" required>
      &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="selector" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="typed_link_facet" type="object" required>
      &lt;p&gt;Identifies the schema Amazon Resource Name (ARN) and facet name for the typed link.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="schema_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="typed_link_name" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="attributes" type="list(object)">
  Returned by the lookup.
</ResponseField>
