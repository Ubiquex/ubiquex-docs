---
title: "aws_clouddirectory_object_information"
description: "`aws_clouddirectory_object_information` reads real, structural information about an object within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_object_information"})

    		ubx.Data(clouddirectory.ObjectInformation, "example", clouddirectory.ObjectInformationConfig{
    			DirectoryArn:     "arn:aws:iam::123456789012:policy/example",
    			ObjectReference:  map[string]any{"selector": "example"},
    			ConsistencyLevel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectInformation } from "@ubx/sdk-aws/aws/data/clouddirectory/object_information";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_object_information" });

      data(ObjectInformation, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        consistencyLevel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ObjectInformation, ObjectInformationConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_object_information")

        ubx.data(ObjectInformation, "example", ObjectInformationConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference={"selector": "example"},
            consistency_level="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  The real, requested level of read consistency for this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="object_identifier" type="string">
  The real, unique identifier of the object this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_facets" type="list(object)">
  The real Cloud Directory schema facets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
