---
title: "aws_clouddirectory_outgoing_typed_links"
description: "`aws_clouddirectory_outgoing_typed_links` reads the real, outgoing typed links originating from an object within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_outgoing_typed_links"})

    		ubx.Data(clouddirectory.OutgoingTypedLinks, "example", clouddirectory.OutgoingTypedLinksConfig{
    			DirectoryArn:          "arn:aws:iam::123456789012:policy/example",
    			ObjectReference:       map[string]any{"selector": "example"},
    			ConsistencyLevel:      "example",
    			FilterAttributeRanges: []map[string]any{{"attribute_name": "example-attribute-name"}},
    			FilterTypedLink:       map[string]any{"schema_arn": "example"},
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OutgoingTypedLinks } from "./local-sdk/aws/sdk/typescript/aws/data/clouddirectory/outgoing_typed_links.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_outgoing_typed_links" });

      data(OutgoingTypedLinks, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        consistencyLevel: "example",
        filterAttributeRanges: [{ attributeName: "example-attribute-name" }],
        filterTypedLink: { schemaArn: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import OutgoingTypedLinks, OutgoingTypedLinksConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_outgoing_typed_links")

        ubx.data(OutgoingTypedLinks, "example", OutgoingTypedLinksConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference={"selector": "example"},
            consistency_level="example",
            filter_attribute_ranges=[{"attribute_name": "example-attribute-name"}],
            filter_typed_link={"schema_arn": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  The real, requested level of read consistency for this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_attribute_ranges" type="list(object)">
  The real, valid ranges for each filterable attribute. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_typed_link" type="object">
  &lt;p&gt;Identifies the schema Amazon Resource Name (ARN) and facet name for the typed link.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="schema_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="typed_link_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="typed_link_specifiers" type="list(object)">
  The real Cloud Directory typed link specifiers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
