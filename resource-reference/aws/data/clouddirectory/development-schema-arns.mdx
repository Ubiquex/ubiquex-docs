---
title: "aws_clouddirectory_development_schema_arns"
description: "`aws_clouddirectory_development_schema_arns` reads the real, unpublished development schema ARNs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_development_schema_arns"})

    		ubx.Data(clouddirectory.DevelopmentSchemaArns, "example", clouddirectory.DevelopmentSchemaArnsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DevelopmentSchemaArns } from "@ubx/sdk-aws/aws/data/clouddirectory/development_schema_arns";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_development_schema_arns" });

      data(DevelopmentSchemaArns, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import DevelopmentSchemaArns, DevelopmentSchemaArnsConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_development_schema_arns")

        ubx.data(DevelopmentSchemaArns, "example", DevelopmentSchemaArnsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the schemas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
