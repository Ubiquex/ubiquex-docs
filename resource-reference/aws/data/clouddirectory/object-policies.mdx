---
title: "aws_clouddirectory_object_policies"
description: "`aws_clouddirectory_object_policies` reads the real policy objects attached to an object within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_object_policies"})

    		ubx.Data(clouddirectory.ObjectPolicies, "example", clouddirectory.ObjectPoliciesConfig{
    			DirectoryArn:     "arn:aws:iam::123456789012:policy/example",
    			ObjectReference:  map[string]any{"selector": "example"},
    			ConsistencyLevel: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ObjectPolicies } from "@ubx/sdk-aws/aws/data/clouddirectory/object_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_object_policies" });

      data(ObjectPolicies, "example", {
        directoryArn: "arn:aws:iam::123456789012:policy/example",
        objectReference: { selector: "example" },
        consistencyLevel: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import ObjectPolicies, ObjectPoliciesConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_object_policies")

        ubx.data(ObjectPolicies, "example", ObjectPoliciesConfig(
            directory_arn="arn:aws:iam::123456789012:policy/example",
            object_reference={"selector": "example"},
            consistency_level="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="consistency_level" type="string">
  The real, requested level of read consistency for this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="directory_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the directory this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="object_reference" type="object" required>
  &lt;p&gt;The reference that identifies an object.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="selector" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="attached_policy_ids" type="list(string)">
  The real, unique identifiers of the attached policys this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
