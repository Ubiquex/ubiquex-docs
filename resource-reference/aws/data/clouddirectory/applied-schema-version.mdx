---
title: "aws_clouddirectory_applied_schema_version"
description: "`aws_clouddirectory_applied_schema_version` reads the real, applied version of a schema within a Cloud Directory."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clouddirectory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/clouddirectory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_clouddirectory_applied_schema_version"})

    		ubx.Data(clouddirectory.AppliedSchemaVersion, "example", clouddirectory.AppliedSchemaVersionConfig{
    			SchemaArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AppliedSchemaVersion } from "@ubx/sdk-aws/aws/data/clouddirectory/applied_schema_version";

    export default stack("example", () => {
      intent({ summary: "look up aws_clouddirectory_applied_schema_version" });

      data(AppliedSchemaVersion, "example", {
        schemaArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.clouddirectory import AppliedSchemaVersion, AppliedSchemaVersionConfig

    def describe():
        ubx.intent("look up aws_clouddirectory_applied_schema_version")

        ubx.data(AppliedSchemaVersion, "example", AppliedSchemaVersionConfig(
            schema_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="schema_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the schema this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applied_schema_arn" type="string">
  The real Amazon Resource Name (ARN) of the applied schema this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
