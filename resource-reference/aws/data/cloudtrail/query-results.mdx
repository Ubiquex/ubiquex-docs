---
title: "aws_cloudtrail_query_results"
description: "`aws_cloudtrail_query_results` reads the real, returned rows of a CloudTrail Lake query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_query_results"})

    		ubx.Data(cloudtrail.QueryResults, "example", cloudtrail.QueryResultsConfig{
    			QueryId:                      "example",
    			EventDataStore:               "example",
    			EventDataStoreOwnerAccountId: "example",
    			MaxQueryResults:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { QueryResults } from "./local-sdk/aws/sdk/typescript/aws/data/cloudtrail/query_results.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_query_results" });

      data(QueryResults, "example", {
        queryId: "example",
        eventDataStore: "example",
        eventDataStoreOwnerAccountId: "example",
        maxQueryResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import QueryResults, QueryResultsConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_query_results")

        ubx.data(QueryResults, "example", QueryResultsConfig(
            query_id="example",
            event_data_store="example",
            event_data_store_owner_account_id="example",
            max_query_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_data_store" type="string">
  The real CloudTrail event data store this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="event_data_store_owner_account_id" type="string">
  The real, unique identifier of the event data store owner account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_query_results" type="number">
  The real, maximum number of query results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_id" type="string" required>
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_result_rows" type="list(list(map(string)))">
  The real rows returned by this query. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_statistics" type="object">
  &lt;p&gt;Metadata about a query, such as the number of results.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bytes_scanned" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="results_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_results_count" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_status" type="string">
  The real, current status of the query this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
