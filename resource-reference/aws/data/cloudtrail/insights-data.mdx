---
title: "aws_cloudtrail_insights_data"
description: "`aws_cloudtrail_insights_data` reads a real, unusual activity pattern detected by CloudTrail Insights."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_insights_data"})

    		ubx.Data(cloudtrail.InsightsData, "example", cloudtrail.InsightsDataConfig{
    			DataType:      "example",
    			InsightSource: "example",
    			Dimensions:    map[string]string{"managed-by": "ubx"},
    			EndTime:       "example",
    			MaxResults:    1,
    			StartTime:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsData } from "@ubx/sdk-aws/aws/data/cloudtrail/insights_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_insights_data" });

      data(InsightsData, "example", {
        dataType: "example",
        insightSource: "example",
        dimensions: { "managed-by": "ubx" },
        endTime: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import InsightsData, InsightsDataConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_insights_data")

        ubx.data(InsightsData, "example", InsightsDataConfig(
            data_type="example",
            insight_source="example",
            dimensions={"managed-by": "ubx"},
            end_time="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_type" type="string" required>
  The real data type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dimensions" type="map(string)">
  The real, structured dimensions of this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="insight_source" type="string" required>
  The real source that produced this insight. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="events" type="list(object)">
  The real events included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
