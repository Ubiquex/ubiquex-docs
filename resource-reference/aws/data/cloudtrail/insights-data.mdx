---
title: "aws_cloudtrail_insights_data"
description: "`aws_cloudtrail_insights_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudtrail_insights_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_insights_data"})

    		ubx.Data(cloudtrail.InsightsData, "example", cloudtrail.InsightsDataConfig{
    			DataType:      "example",
    			InsightSource: "example",
    			Dimensions:    map[string]string{"managed-by": "ubx"},
    			EndTime:       "example",
    			MaxResults:    1,
    			StartTime:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsData } from "@ubx/sdk-aws/aws/data/cloudtrail/insights_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_insights_data" });

      data(InsightsData, "example", {
        dataType: "example",
        insightSource: "example",
        dimensions: { "managed-by": "ubx" },
        endTime: "example",
        maxResults: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import InsightsData, InsightsDataConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_insights_data")

        ubx.data(InsightsData, "example", InsightsDataConfig(
            data_type="example",
            insight_source="example",
            dimensions={"managed-by": "ubx"},
            end_time="example",
            max_results=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="dimensions" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="insight_source" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="events" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
