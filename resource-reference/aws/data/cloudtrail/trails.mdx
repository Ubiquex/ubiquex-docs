---
title: "aws_cloudtrail_trails"
description: "`aws_cloudtrail_trails` looks up a real CloudTrail trail, continuously delivering account activity logs to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_trails"})

    		ubx.Data(cloudtrail.Trails, "example", cloudtrail.TrailsConfig{
    			IncludeShadowTrails: true,
    			TrailNameList:       []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Trails } from "./local-sdk/aws/sdk/typescript/aws/data/cloudtrail/trails.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_trails" });

      data(Trails, "example", {
        includeShadowTrails: true,
        trailNameList: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import Trails, TrailsConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_trails")

        ubx.data(Trails, "example", TrailsConfig(
            include_shadow_trails=True,
            trail_name_list=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_shadow_trails" type="bool">
  Whether CloudTrail shadow trails are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="trail_name_list" type="list(string)">
  The real trail names included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="trail_list" type="list(object)">
  The real trails included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
